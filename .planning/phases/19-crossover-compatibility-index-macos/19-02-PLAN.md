---
phase: 19-crossover-compatibility-index-macos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - meta/measureCrossoverMatching.ts
  - src/backend/crossover_index/normalize.ts
  - src/backend/crossover_index/__tests__/normalize.test.ts
autonomous: true
requirements: [CXIDX-03, CXIDX-08]

must_haves:
  truths:
    - "Running the measurement harness prints an explicit PASS or FAIL against the pre-committed D-02 gate (<2% wrong AND >30% hit) scored on the 123-pair Steam AppID ground-truth set."
    - "The harness scores three samples with SEPARATE denominators — the 123-pair ground-truth set, the synthetic adversarial set, and the whole-dump self-collision test — and never pools them into one number."
    - "The D-02 gate bounds are fixed as named constants in code before the run and are printed verbatim in the report, so the verdict cannot be rationalized after the fact."
    - "The winning normalizer is exported from normalize.ts as a pure function that is structurally distinct from slugify() (D-20): it never imports slugify()/naiveSlugify()."
    - "normalize.ts exports a boolean NAME_MATCHING_SHIPS whose value equals the report's recorded D-02 verdict and defaults to false (Steam-AppID-only) unless the harness demonstrably passed."
    - "normalize.test.ts fails loudly if the normalizer regresses on U+2019 apostrophe variants, roman-vs-arabic numerals, edition suffixes, or duplicate-record cases."
    - "The committed report carries aggregate counts + the synthetic cases only — never a full dump of the user's owned titles."
  artifacts:
    - path: "meta/measureCrossoverMatching.ts"
      provides: "Re-runnable measurement harness + D-02 gate evaluator + dated report writer"
      min_lines: 120
    - path: "src/backend/crossover_index/normalize.ts"
      provides: "The CrossOver matching key + the machine-readable D-02 gate verdict (NAME_MATCHING_SHIPS) consumed by 19-05"
      exports: ["normalize", "NAME_MATCHING_SHIPS"]
    - path: "src/backend/crossover_index/__tests__/normalize.test.ts"
      provides: "Permanent adversarial regression suite for the matching key"
  key_links:
    - from: "meta/measureCrossoverMatching.ts"
      to: "src/backend/crossover_index/normalize.ts"
      via: "import { normalize } (and candidate variants) — harness scores the same function that ships"
      pattern: "from ['\"].*crossover_index/normalize"
    - from: "src/backend/crossover_index/normalize.ts"
      to: "NOT src/backend/wiki_game_info/codeweavers/utils.ts"
      via: "must NOT import slugify/naiveSlugify (D-20 separation)"
      pattern: "slugify"
    - from: "src/backend/crossover_index/index.ts (plan 19-05)"
      to: "NAME_MATCHING_SHIPS (this plan)"
      via: "19-05 consumes the boolean to conditionally skip the byName lookup — cross-plan contract, symbol name must stay stable"
      pattern: "NAME_MATCHING_SHIPS"
---

<objective>
Build the measurement harness that decides — empirically, before any UI ships — whether
non-Steam library titles can be matched onto the CrossOver dump's canonical names, and
extract the winning name normalizer into its own file as the CrossOver **matching key**,
kept structurally distinct from the URL `slugify()` (D-20).

Purpose: D-02's promotion gate is the hinge of this phase. Its verdict (does non-Steam name
matching ship in v1, or defer to a follow-up phase?) governs a user-facing correctness claim
("this game runs / won't run on your Mac"). The gate bounds are pre-committed so the number
cannot be rationalized after the fact; this plan makes the gate a runnable, auditable
instrument rather than a theory — AND emits the verdict as a machine-readable boolean
(`NAME_MATCHING_SHIPS`) so the runtime lookup (plan 19-05) actually honors it. Without that
exported symbol the gate would be decorative: name matching would ship regardless of outcome,
violating D-02.

Output:
- `meta/measureCrossoverMatching.ts` — scores candidate normalizers against THREE
  separately-denominated samples plus a whole-dump self-collision proxy, evaluates the
  pre-committed D-02 gate on the 123-pair ground-truth set, and writes a dated Markdown report.
- `src/backend/crossover_index/normalize.ts` — the winning normalizer, exported as `normalize()`,
  plus the `NAME_MATCHING_SHIPS` boolean carrying the recorded D-02 verdict (default false).
- `src/backend/crossover_index/__tests__/normalize.test.ts` — the synthetic adversarial set as
  a permanent Jest regression suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/19-crossover-compatibility-index-macos/19-CONTEXT.md
@.planning/phases/19-crossover-compatibility-index-macos/19-RESEARCH.md
@.planning/phases/19-crossover-compatibility-index-macos/19-PATTERNS.md
@.planning/notes/crossover-tie-dump-findings.md

# The anti-pattern boundary this plan must respect — read baseSlugify()'s SHAPE, do NOT import it:
@src/backend/wiki_game_info/codeweavers/utils.ts

<facts>
<!-- Load-bearing constants the executor must use verbatim — no re-derivation. -->

- Dump URL (public, no auth): https://ftp.codeweavers.com/pub/crossover/tie/crossover.tie.gz
  (~3.09 MB gzipped → 23.7 MB XML; Content-Length verified 3,088,416 bytes.)
- Dump XML shape (Pitfall T-02): root path is `c4p > applications > app[]`; the Mac medal and
  steamid live INSIDE `<appprofile>`, NOT as direct children of `<app>`. Extracting against the
  wrong shape silently yields ZERO records.
- fast-xml-parser@5 entity-limit fix (Pitfall T-01): pass a `processEntities` object with
  `enabled: true`, `maxTotalExpansions: 500_000`, `maxExpandedLength: 50_000_000`,
  `maxEntityCount: 1_000`. Do NOT use `processEntities: false` — that would leave `&amp;`
  undecoded in names like "Command & Conquer". `isArray` must cover `['app','name','medal','category']`.
- Medal rule → rating: 5/4/3/2/1 on the highest-cxversion medal (verified 6/6 against the live
  site in the findings note). Rating spread across the 2,866 Mac-medal games:
  1054×5 / 655×4 / 475×3 / 347×2 / 335×1.
- Local library caches (VERIFIED paths): `~/Library/Application Support/GameLib/store_cache/`
  → `steam_library.json` (377 titles), `legendary_library.json` (15 Epic, ~10 DLC/art/wallpaper),
  `gog_library.json` (0), `nile_library.json` (0). Locate them via `CacheStore`'s on-disk
  convention (`cwd: 'store_cache'`) rather than hardcoding an absolute path (see src/backend/cache.ts:22-26).
- Ground-truth set: Steam library ∩ dump-by-AppID = 123 pairs where the correct dump record is
  known by exact AppID join. n=123 makes a single wrong hit 0.81% — below the 2% bound.
- Research feasibility preview (NOT the measurement — Steam titles are the easiest case,
  optimistically biased): candidates A/B/C scored 0 wrong hits at 77.2% / 80.5% / 84.6% hit.
  Self-collision over 2,866 names: punctuation stripping = 0 rating-disagreeing collisions (free);
  edition-suffix stripping = 11 disagreeing keys / 33 records at risk (1.2%) — the live trade-off.
- D-02 verdict contract: `NAME_MATCHING_SHIPS` is the boolean gate verdict. Default `false`
  (Steam-AppID-only). Flip to `true` ONLY when the recorded harness report shows PASS
  (wrong hits < 2% AND hit rate > 30% on the 123-pair set). Plan 19-05's runtime lookup imports
  this symbol to conditionally skip the byName matching — the name `NAME_MATCHING_SHIPS` is a
  stable cross-plan contract, do not rename it.
</facts>

<interfaces>
<!-- The SHAPE to copy for normalize.ts's baseNormalize step. Copy the shape, NOT the function.
     normalize.ts must NOT import from codeweavers/utils.ts (D-20). -->

From src/backend/wiki_game_info/codeweavers/utils.ts:39-46 (baseSlugify — shape reference only):
```
normalize('NFKD') → replace(COMBINING_DIACRITIC_RE, '') → toLowerCase()
  → replace(/[^a-z0-9]+/g, '-') → replace(/^-+|-+$/g, '')
```
The matching key differs from the slug at exactly one knob: whether to strip edition suffixes
(e.g. "Game of the Year Edition", "Definitive Edition", "Remastered"). Punctuation stripping is
free; edition-suffix stripping is the measured trade-off D-02 adjudicates.

The verdict export (contract consumed by plan 19-05's index.ts):
```
export const NAME_MATCHING_SHIPS: boolean  // default false; true only if the recorded report PASSED
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The matching normalizer (normalize.ts) + NAME_MATCHING_SHIPS verdict + adversarial regression suite</name>
  <files>src/backend/crossover_index/normalize.ts, src/backend/crossover_index/__tests__/normalize.test.ts</files>
  <read_first>
    - src/backend/wiki_game_info/codeweavers/utils.ts (baseSlugify shape lines 39-46; slugify/naiveSlugify — the functions this file must NOT reuse)
    - 19-PATTERNS.md § "src/backend/crossover_index/normalize.ts" (the D-20 sibling-not-reuse boundary)
    - 19-RESEARCH.md lines 515-538 (candidate normalizers A/B/C and the self-collision trade-off)
    - 19-CONTEXT.md D-01/D-02/D-20 (the pre-committed gate; slug distinct from matching key)
  </read_first>
  <behavior>
    - Test: "Alekhine's Gun" and "Alekhine’s Gun" (U+2019 curly apostrophe) both normalize to the same key — apostrophe variants must not diverge.
    - Test: "Quake II" and "Quake 2" — document the chosen roman/arabic policy explicitly with a test asserting the key the harness measured as the winner (do NOT silently convert; the SLUG keeps verbatim numerals per D-20, and the matching key must be tested for whichever behavior ships).
    - Test: an edition-suffix pair, e.g. "Baldur's Gate 3 (Digital Deluxe Edition)" vs "Baldur's Gate 3" — assert the winning normalizer's documented edition-suffix behavior (strip vs keep, per the measured result).
    - Test: two distinct duplicate-`<app>` records that share a base title stay distinguishable OR collapse per the measured policy — assert the chosen behavior, not an accident.
    - Test: output character-class is constrained (only `[a-z0-9-]`, matching baseSlugify's guarantee) so a crafted title cannot inject anything downstream.
    - Test: NAME_MATCHING_SHIPS is a boolean; it is `false` unless the recorded report verdict is PASS.
  </behavior>
  <action>Create `src/backend/crossover_index/normalize.ts` exporting (a) a pure `normalize(title: string): string` that is the CrossOver MATCHING key — structurally DISTINCT from `slugify()` (D-20) — and (b) `export const NAME_MATCHING_SHIPS: boolean`, the machine-readable D-02 gate verdict. Copy the SHAPE of `baseSlugify` (NFKD normalize, strip combining diacritics, lowercase, collapse non-alphanumeric runs to a single separator, trim) but do NOT import or call `slugify()`/`naiveSlugify()`/`baseSlugify()` from `codeweavers/utils.ts` — the slug and the matching key have opposite correctness criteria (verbatim numerals are correct for slugs; edition-suffix handling is the open question for matching) and must never drift into each other. Implement the candidate variants the harness scores (at minimum: exact-lowercase, punctuation-stripped, and punctuation+edition-suffix-stripped) and export the one the measurement selects as the default `normalize`. Keep the candidate variants exported (named) so `meta/measureCrossoverMatching.ts` scores the exact functions that ship — no separate copies. For `NAME_MATCHING_SHIPS`: initialize it to `false` (fail-safe — v1 ships Steam-AppID-only badges) and set it to `true` ONLY after Task 2's harness runs and its dated report records a PASS (wrong hits < 2% AND hit rate > 30% on the 123-pair ground-truth set). The executor sets this value FROM the recorded harness verdict in the report — it is a committed literal, not runtime-computed, because the harness cannot run inside the app and the constant must be readable synchronously by 19-05. Add a comment above the constant citing the report filename and the exact PASS/FAIL numbers it was set from, so the value is auditable. Then create `src/backend/crossover_index/__tests__/normalize.test.ts` as a permanent Jest regression suite covering the U+2019 apostrophe, roman-vs-arabic numeral, edition-suffix, and duplicate-record cases, the `[a-z0-9-]`-only output guarantee, and a `typeof NAME_MATCHING_SHIPS === 'boolean'` assertion. Encode the roman/arabic and edition-suffix policies as explicit assertions of the WINNING behavior — the test is the enforcement mechanism for whatever the measurement decided, so a later "improvement" that breaks it fails loudly. Do NOT delete or modify `slugify()` here — that is D-20's other half, owned by plan 19-05.</action>
  <acceptance_criteria>
    - `normalize.ts` exports a pure `normalize()` plus the named candidate variants the harness imports.
    - `normalize.ts` contains NO import from `codeweavers/utils.ts` (grep for `slugify`/`baseSlugify`/`codeweavers/utils` returns 0 import lines) — D-20 separation intact.
    - `normalize.ts` exports `NAME_MATCHING_SHIPS` as a boolean, defaulting `false`, with its value matching the report's recorded PASS/FAIL verdict and an auditable comment citing the report filename + numbers.
    - `normalize.test.ts` passes and covers: U+2019 apostrophe equivalence, roman-vs-arabic policy, edition-suffix policy, duplicate-record policy, `[a-z0-9-]`-only output, and `typeof NAME_MATCHING_SHIPS === 'boolean'`.
    - `pnpm test -- --testPathPattern=crossover_index/normalize` is green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --testPathPattern=crossover_index/normalize</automated>
    <automated>grep -Eq "export const NAME_MATCHING_SHIPS" src/backend/crossover_index/normalize.ts || (echo "FAIL: NAME_MATCHING_SHIPS not exported — gate would be decorative" && exit 1)</automated>
    <automated>grep -q "from.*codeweavers/utils" src/backend/crossover_index/normalize.ts && (echo "FAIL: normalize.ts imports slugify — D-20 violated" && exit 1) || echo "OK: no slugify import"</automated>
  </verify>
  <done>normalize.ts exports a pure normalize(), its candidate variants, and a boolean NAME_MATCHING_SHIPS (default false, set from the report verdict); it contains no import from codeweavers/utils.ts; normalize.test.ts passes and covers U+2019, roman-vs-arabic, edition-suffix, duplicate-record cases, the constrained-output guarantee, and the NAME_MATCHING_SHIPS boolean type.</done>
</task>

<task type="auto">
  <name>Task 2: The measurement harness + pre-committed D-02 gate + dated report</name>
  <files>meta/measureCrossoverMatching.ts</files>
  <read_first>
    - meta/lintTranslations.ts (the meta/ script convention — imperative TS, console diagnostics, esbuild-piped)
    - src/backend/cache.ts:22-26 (the store_cache on-disk convention for locating library JSON)
    - 19-RESEARCH.md lines 495-538 (Pitfall 8/9, the three-set structure, the self-collision proxy)
    - 19-RESEARCH.md lines 784-824 (Validation Architecture — the harness is a script, not jest; the privacy note)
    - 19-CONTEXT.md D-01/D-02/D-03 (the pre-committed gate, the 123-pair amendment)
    - src/backend/crossover_index/normalize.ts (from Task 1 — import its candidate variants)
  </read_first>
  <action>Create `meta/measureCrossoverMatching.ts` following the `meta/lintTranslations.ts` convention (top-level imperative TypeScript, `console.log` diagnostics, run via esbuild→node). Because this plan is disjoint from the builder plan (19-01) and depends on nothing, the harness fetches and parses the dump itself: download the `.tie.gz` from `https://ftp.codeweavers.com/pub/crossover/tie/crossover.tie.gz`, `gunzipSync` it, and parse with `fast-xml-parser` using the raised entity limits (`processEntities` object: `maxTotalExpansions: 500_000`, `maxExpandedLength: 50_000_000`, `maxEntityCount: 1_000`; `isArray` covering `['app','name','medal','category']`) against the real `c4p > applications > app[]` shape with steamid/medal read from `<appprofile>`. Bound the fetch with a `maxContentLength` ceiling so a runaway payload cannot exhaust memory. Fix the D-02 gate bounds as named constants at the TOP of the file BEFORE any scoring runs — `WRONG_HIT_MAX = 0.02` and `HIT_RATE_MIN = 0.30` — and print them verbatim into the report header so the verdict is demonstrably pre-committed, not fitted. Score THREE samples with SEPARATE denominators, NEVER pooled: (1) the **123-pair Steam ∩ dump-by-AppID ground-truth set** — hold out the AppID, match on name only via each candidate `normalize` variant imported from `crossover_index/normalize.ts`, score against the record the AppID proves correct; this is the ONLY sample the D-02 gate is evaluated against (per the D-02/D-03 amendment — NOT the raw non-Steam library); (2) the **real non-Steam set** (~5 base games after filtering Legendary DLC/add-ons out) — report as a per-title qualitative table only, NO percentage, since n≈5 is statistically meaningless; (3) the **synthetic adversarial set** (edition suffixes, roman-vs-arabic numerals, U+2019 apostrophe variants, duplicate-`<app>` records) — report pass/fail per failure mode, not a rate. Additionally run a **whole-dump self-collision test over the 2,866 Mac-medal names** — a large-denominator wrong-hit proxy needing no library — counting distinct keys, colliding keys, and keys where the collided records DISAGREE on rating (the harmful case). Read the local library caches via the `store_cache` convention (do not hardcode absolute paths); tolerate their absence gracefully (empty GOG/Amazon caches, or a machine without a Steam cache) by reporting "sample unavailable" rather than crashing. Compute the D-02 verdict in code (`wrongHitRate < WRONG_HIT_MAX && hitRate > HIT_RATE_MIN`) and write an explicit **PASS** or **FAIL** with the exact numbers into a dated Markdown report via `writeFileSync`. The report is the source of truth the executor uses to set `NAME_MATCHING_SHIPS` in `normalize.ts` (Task 1) — so it MUST state the verdict unambiguously (the word PASS or FAIL plus the wrongHitRate and hitRate values). The report MUST carry aggregate counts + the synthetic cases ONLY — never a full dump of the user's owned titles (privacy, RESEARCH line 824). State the Steam-title optimistic bias explicitly in the report so the ground-truth number is read honestly. Do NOT add a `measure-crossover-match` entry to `package.json` in this plan — `package.json` is owned by the sibling builder plan (19-01) in the same wave; run the harness directly via the equivalent `pnpm exec esbuild … | node` command shown in verify.</action>
  <acceptance_criteria>
    - `meta/measureCrossoverMatching.ts` type-checks (`pnpm codecheck` green) and imports the candidate normalizers from `crossover_index/normalize.ts` (scores the shipped functions, not copies).
    - The D-02 bounds `WRONG_HIT_MAX = 0.02` and `HIT_RATE_MIN = 0.30` exist as named constants declared before any scoring and are printed into the report header.
    - Three samples are scored with SEPARATE denominators (123-pair ground-truth, real non-Steam qualitative table, synthetic adversarial pass/fail) plus a whole-dump self-collision test — never pooled into one denominator.
    - The D-02 gate is evaluated ONLY on the 123-pair ground-truth set; the harness writes a dated Markdown report stating an unambiguous PASS or FAIL with the exact wrongHitRate and hitRate numbers.
    - The report carries aggregate counts + synthetic cases only — no full owned-title list (privacy).
    - The recorded PASS/FAIL is the value the executor transfers into `NAME_MATCHING_SHIPS` (Task 1).
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit -p src/backend 2>/dev/null || pnpm codecheck</automated>
    <automated>grep -Eq "WRONG_HIT_MAX *= *0\.02|0\.02" meta/measureCrossoverMatching.ts && grep -Eq "HIT_RATE_MIN *= *0\.30|0\.3" meta/measureCrossoverMatching.ts || (echo "FAIL: pre-committed gate bounds not present as constants" && exit 1)</automated>
    <automated>grep -Eq "123|ground.?truth" meta/measureCrossoverMatching.ts && grep -Eq "self.?collision|collid" meta/measureCrossoverMatching.ts && grep -Eiq "synthetic|adversar" meta/measureCrossoverMatching.ts || (echo "FAIL: three-sample structure not evidenced" && exit 1)</automated>
    <human-check>Run `pnpm exec esbuild --bundle --platform=node --target=node21 meta/measureCrossoverMatching.ts | node` on a machine with the Steam library cache present; confirm the dated report is written, states an unambiguous PASS/FAIL (with wrongHitRate + hitRate) against the printed pre-committed bounds on the 123-pair set, shows three separately-denominated samples plus the self-collision table, contains NO full owned-title list, and that NAME_MATCHING_SHIPS in normalize.ts matches that verdict.</human-check>
  </verify>
  <done>The harness type-checks, fixes the D-02 bounds as constants printed into the report, scores the 123-pair / real-non-Steam / synthetic samples with separate denominators plus a whole-dump self-collision proxy, evaluates the gate on the 123-pair set only, writes a dated Markdown report with an explicit PASS/FAIL (with numbers) and no owned-title leak, and that verdict is transferred into NAME_MATCHING_SHIPS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CodeWeavers `.tie` dump → harness | Untrusted external 23.7 MB XML crosses into the parser in CI/dev. |
| Local library caches → harness | Semi-trusted on-disk JSON containing the user's owned-title list (PII-adjacent). |
| Harness verdict → NAME_MATCHING_SHIPS → runtime lookup | The gate's PASS/FAIL, carried by the exported boolean into plan 19-05, governs whether name matching runs at all — a user-facing correctness claim ("won't run on your Mac"). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Repudiation | D-02 gate verdict in measureCrossoverMatching.ts | mitigate | Bounds fixed as named constants (`WRONG_HIT_MAX=0.02`, `HIT_RATE_MIN=0.30`) at the top of the file BEFORE any scoring, printed verbatim into the report header, and the verdict computed in code — so "the gate was rationalized after the run" is structurally impossible to hide. |
| T-19-02-02 | Tampering | normalizer selection / wrong-hit records | mitigate | The 123-pair ground-truth set gives objective wrong-hit labels; the whole-dump self-collision test is a large-denominator guard for rating-disagreeing collisions (the case where a title gets the WRONG game's medal). The gate refuses promotion unless wrong hits < 2%. |
| T-19-02-06 | Tampering | NAME_MATCHING_SHIPS verdict flag | mitigate | Fail-safe default `false` (Steam-AppID-only) — the flag can only be flipped `true` from a recorded PASS report, with an auditable comment citing the report + numbers, and 19-05 reads it synchronously. A missing or unset verdict therefore ships the SAFE outcome, never accidental name matching. |
| T-19-02-03 | Information disclosure | dated Markdown report | mitigate | Report carries aggregate counts + synthetic cases ONLY; never writes the user's full owned-title list (RESEARCH line 824). Real non-Steam sample is reported per-title but is ~5 base games, not the whole library. |
| T-19-02-04 | Denial of Service | fast-xml-parser on 23.7 MB XML | mitigate | Raised-but-bounded entity limits (not unbounded, not disabled); `maxContentLength` ceiling on the dump fetch; sync parse of a known ~3 MB gz payload. CI/dev-only script — never runs in the shipped app. |
| T-19-02-05 | Tampering | dependency supply chain | accept | No new package installs — `fast-xml-parser@5.5.7` and `zod` are already project deps [VERIFIED: package.json]; no `## Package Legitimacy Audit` gate required for this plan. |
</threat_model>

<verification>
- `pnpm test -- --testPathPattern=crossover_index/normalize` passes (adversarial regression green).
- `pnpm codecheck` (`tsc --noEmit`) green with the new files.
- `normalize.ts` contains no import from `codeweavers/utils.ts` (D-20 separation intact).
- `normalize.ts` exports `NAME_MATCHING_SHIPS` (boolean, default false), value matching the report verdict.
- `measureCrossoverMatching.ts` runs to completion and writes a dated report with an explicit
  PASS/FAIL (with numbers) against the printed pre-committed D-02 bounds, on the 123-pair ground-truth set.
- Report shows three separately-denominated samples + the self-collision table, and leaks no
  owned-title list.
</verification>

<success_criteria>
- The phase now has a re-runnable instrument that decides D-02 empirically, with an auditable,
  privacy-safe, dated report and an un-rationalizable pre-committed verdict.
- The verdict is machine-readable: `NAME_MATCHING_SHIPS` (default false) carries the outcome into
  plan 19-05's runtime lookup, so a FAIL genuinely ships Steam-AppID-only — the gate is enforced,
  not decorative.
- The winning matching key ships as `crossover_index/normalize.ts`, provably distinct from the
  URL slug (D-20), with its behavior locked by a permanent adversarial test suite.
- CXIDX-03 (measurement + gate) and the normalize.ts half of CXIDX-08 (matching key distinct
  from slug) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/19-crossover-compatibility-index-macos/19-02-SUMMARY.md` when done.
Record the measurement's actual PASS/FAIL verdict, the value set into `NAME_MATCHING_SHIPS`, and
which normalizer variant won — the runtime lookup (19-05) consumes NAME_MATCHING_SHIPS, and the UI
plans (19-06/19-07/19-08) plus D-16's "no mark for non-looked-up games" behavior depend on it.
</output>
