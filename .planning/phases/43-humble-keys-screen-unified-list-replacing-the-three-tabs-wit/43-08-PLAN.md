---
phase: 43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit
plan: 08
type: execute
wave: 4
depends_on:
  - 43-04
  - 43-07
files_modified:
  - src/frontend/screens/Humble/Keys/Waiting/index.tsx
  - src/frontend/screens/Humble/Keys/Waiting/__tests__/index.test.tsx
  - src/frontend/screens/Humble/Keys/Spares/index.tsx
  - src/frontend/screens/Humble/Keys/All/index.tsx
  - src/frontend/screens/Humble/Keys/All/__tests__/index.test.tsx
  - src/frontend/screens/Humble/Keys/components/HumbleKeyGroup/index.tsx
  - src/common/humble/groupKeys.ts
  - src/backend/humble/__tests__/groupKeys.test.ts
  - src/common/humble/viewFilters.ts
  - src/backend/humble/__tests__/viewFilters.test.ts
  - src/frontend/screens/Humble/Keys/stateLabels.ts
  - meta/i18nGateScope.json
  - meta/i18nForkTouchedFiles.json
  - meta/hardcodedStringGate.ts
autonomous: true
requirements:
  - REQ-43-17
  - REQ-43-18
  - REQ-43-22

must_haves:
  truths:
    - "HumbleKeyGroup, groupAndSortKeys, GROUP_ORDER, byExpiringSoonest and partitionWaitingByUrgency no longer exist anywhere in the tree"
    - "selectKeysWaiting still exists and its four consumers outside this screen still compile and pass"
    - "The four deleted files leave meta/i18nGateScope.json and meta/i18nForkTouchedFiles.json in the SAME commit as the deletion"
    - "No surviving comment names a file this phase deleted, except one deliberately-annotated dated census snapshot"
  artifacts:
    - path: "meta/i18nGateScope.json"
      provides: "the blocking hardcoded-string gate's scope, minus the four deleted files"
      contains: "HumbleKeyRow/index.tsx"
    - path: "meta/i18nForkTouchedFiles.json"
      provides: "the A-17 ratchet's committed input, minus the four deleted files"
      contains: "HumbleKeyRow/index.tsx"
    - path: "src/frontend/screens/Humble/Keys/stateLabels.ts"
      provides: "STATE_LABEL_KEYS with a docblock naming its surviving caller"
      contains: "STATE_LABEL_KEYS"
  key_links:
    - from: "meta/i18nForkTouchedFiles.json"
      to: "meta/i18nGateScope.json"
      via: "the A-03 ratchet's unscanned-set comparison"
      pattern: "Humble/Keys"
    - from: "src/frontend/screens/Humble/Keys/stateLabels.ts"
      to: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow"
      via: "docblock naming the surviving consumer"
      pattern: "HumbleKeyRow"
---

<objective>
Delete everything the unified list made dead, atomically with the gate configuration that enrols it,
and correct the surviving comments that would otherwise describe a codebase that no longer exists.

Purpose: dead code that survives a restructure is how the next reader gets misled — and in this repo
a deletion that outruns its gate configuration also reddens CI. Both halves have to land together.

Output: five deleted source files, two deleted test files, two pruned pure functions, two edited
gate artifacts, and three corrected comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-CONTEXT.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-PATTERNS.md
</context>

<interfaces>
Measured against the repo during planning. The first item overturns a stated research assumption and
is the single most likely way this plan reddens CI.

- **`meta/i18nForkTouchedFiles.json` ALSO enrols all four deleted files**, at lines 115
  (`All/index.tsx`), 116 (`Spares/index.tsx`), 117 (`Waiting/index.tsx`) and 119
  (`HumbleKeyGroup/index.tsx`). Line 118 is `HumbleClaimWizard/index.tsx` — NOT deleted, do not
  touch it. `43-RESEARCH.md`'s assumption A3 says no other gate config enrols these paths. It is
  wrong. Three assertions in `meta/__tests__/genI18nGateScope.test.ts` bind here:
  - `:355` `A-03 RATCHET` asserts `forkTouched` minus `scope` equals `DECLARED_UNSCANNED_DEBT`
    exactly. Removing the four from `i18nGateScope.json` alone grows that difference by four and
    fails it **by name**.
  - `:613` `A-17 ANTI-ROT` asserts the committed `i18nForkTouchedFiles.json` equals the LIVE git
    derivation. `meta/genI18nGateScope.ts:155` skips `D`-status diff entries, so a deleted file
    leaves the live derivation and the committed artifact must lose it too.
  - `:477` asserts every path in `i18nGateScope.json` still exists on disk.
  Measured current state: scope 174 files, forkTouched 216 files, unscanned difference 42. After a
  correct edit: 170 / 212 / 42 — the difference is unchanged because all four are currently in
  scope, so `DECLARED_UNSCANNED_DEBT` needs no edit.
- **Hand-edit both artifacts; do not run `pnpm gen-i18n-gate-scope`.** The test file's own header at
  `meta/__tests__/genI18nGateScope.test.ts:92-93` records the precedent ("Hand-edited surgically,
  NOT regenerated, per this file's own precedent"), and a regeneration would sweep in every
  unrelated fork drift since the artifact's `generatedAt` of 2026-08-30 — the recorded "regenerating
  an artifact breaks the pins that guard it" failure.
- `meta/i18nGateScope.json` lines 94, 95, 96, 98 hold the four paths; line 97 is
  `HumbleClaimWizard/index.tsx` and stays. Lines 99-102 (`HumbleKeyRow`, `UrgencyBadge`,
  `Keys/index.tsx`, `stateLabels.ts`) all stay — those files survive.
- `src/frontend/screens/Humble/Keys/components/HumbleKeyGroup/` contains **only `index.tsx`**.
  `43-PATTERNS.md` says "(index.tsx, index.css)". There is no `index.css` in that directory.
- `src/frontend/screens/Humble/Keys/Spares/` has **no `__tests__` directory**. Only `Waiting/` and
  `All/` do — confirmed by the pre-phase suite census of four suites under `Humble/Keys`.
- `grep -rln "from.*groupKeys" src` finds exactly five files: `viewFilters.ts` and
  `keyTypePresentation.ts` (both repointed by plan 43-04), `All/index.tsx` and
  `HumbleKeyGroup/index.tsx` (both deleted here), and `src/backend/humble/__tests__/groupKeys.test.ts`
  (deleted here).
- **`selectKeysWaiting` must NOT be deleted.** Its consumers outside this screen, each importing
  only that one symbol: `src/frontend/screens/StoreSearch/index.tsx:8`,
  `src/frontend/screens/Discounts/index.tsx:16`, `src/backend/humble/__tests__/library.test.ts:302`,
  `src/backend/discounts/__tests__/badges.test.ts:13`. Two research documents state its only call
  sites are the deleted tab files; both are wrong.
- `partitionWaitingByUrgency` (`viewFilters.ts:91-105`) — sole call site is `Waiting/index.tsx:50-52`,
  deleted here. `selectGiftableSpares` (`viewFilters.ts:75-77`) — call sites were `Spares/index.tsx:25`
  and `Keys/index.tsx:111`, both gone after plan 43-07. Neither is imported by any external consumer.
- `src/frontend/screens/Humble/Keys/stateLabels.ts:4-11` — the docblock says the table is "used by
  HumbleKeyGroup (group headings) and HumbleKeyRow (state badges)" and explains the WR-09 circular
  import via "HumbleKeyGroup imported the row component". Both are prose, not live imports, and both
  become false the moment `HumbleKeyGroup` is deleted.
- `meta/hardcodedStringGate.ts:1207-1224` — a dated ts-morph census comment listing six users of the
  `{key, defaultText}` exemption shape "over all 174 scope files on 2026-09-09", one row of which is
  `HumbleKeyGroup/index.tsx`. The comment says its numbers are "a snapshot, not a constant" and that
  a reopener must re-run the census first, so it is self-guarding — but it names a file that will not
  exist.
- Measured baselines: `groupKeys.test.ts` 12 passed; `Waiting/__tests__` 13 passed; `All/__tests__`
  9 passed; `genI18nGateScope.test.ts` 26 passed / 1 skipped;
  `hardcodedStringGate.test.ts` 151 passed in about 74 seconds.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the four gate-scoped frontend files and both artifacts' entries, in one commit</name>
  <read_first>
    - `meta/i18nGateScope.json` lines 90-105 — which four lines go, and which two neighbours
      (97 `HumbleClaimWizard`, 99 `HumbleKeyRow`) must survive.
    - `meta/i18nForkTouchedFiles.json` lines 110-125 — the same four paths at 115, 116, 117, 119,
      with `HumbleClaimWizard` at 118 between them.
    - `meta/__tests__/genI18nGateScope.test.ts:330-390` (the CI-readable ratchet block) and
      `:600-625` (the ANTI-ROT block) — read the assertions so the edit satisfies the tests rather
      than being discovered by them.
    - `src/frontend/screens/Humble/Keys/Waiting/index.tsx`, `.../Spares/index.tsx`,
      `.../All/index.tsx` and `.../components/HumbleKeyGroup/index.tsx` — read each before deleting,
      to confirm nothing in them is still referenced. Plan 43-07 ported their lifecycles and its
      Task 3 ported their assertions; this is the last read.
    - `src/frontend/App.tsx` — confirm the three `lazy` imports are already gone (plan 43-07 Task 2).
      If they are not, STOP: deleting the modules first would break the build.
  </read_first>
  <files>
    src/frontend/screens/Humble/Keys/Waiting/index.tsx,
    src/frontend/screens/Humble/Keys/Waiting/__tests__/index.test.tsx,
    src/frontend/screens/Humble/Keys/Spares/index.tsx,
    src/frontend/screens/Humble/Keys/All/index.tsx,
    src/frontend/screens/Humble/Keys/All/__tests__/index.test.tsx,
    src/frontend/screens/Humble/Keys/components/HumbleKeyGroup/index.tsx,
    meta/i18nGateScope.json,
    meta/i18nForkTouchedFiles.json
  </files>
  <action>
    Delete the three tab directories (`Waiting/`, `Spares/`, `All/`, including the `__tests__`
    directories under `Waiting/` and `All/` — `Spares/` has none) and the `HumbleKeyGroup/`
    directory, which contains only `index.tsx`.

    In the SAME task and the SAME commit, remove exactly four entries from each of
    `meta/i18nGateScope.json` and `meta/i18nForkTouchedFiles.json`:
    `src/frontend/screens/Humble/Keys/All/index.tsx`, `.../Spares/index.tsx`, `.../Waiting/index.tsx`
    and `.../components/HumbleKeyGroup/index.tsx`. Leave every other entry byte-identical — in
    particular `HumbleClaimWizard/index.tsx`, `HumbleKeyRow/index.tsx`, `UrgencyBadge/index.tsx`,
    `Keys/index.tsx` and `stateLabels.ts` stay in both files.

    Hand-edit both JSON files. Do NOT run `pnpm gen-i18n-gate-scope` or `pnpm gen-i18n-scope:rewrite`
    — both regenerate against current HEAD and would sweep in unrelated fork drift accumulated since
    the artifacts' 2026-08-30 `generatedAt`. Do not change `baseCommit`, `baseVersion` or
    `generatedAt`; the artifacts still describe the same merge-base, and `genI18nGateScope.test.ts:351`
    asserts `baseCommit` matches `package.json`'s `upstream.baseCommit`.

    Do not touch `meta/i18nGateAllowlist.json` — grep confirms it holds no Humble/Keys entry.

    Commit the deletions and both artifact edits as ONE commit. A deletion-only commit followed by a
    scope-edit commit leaves a red CI window between them, observable on the intermediate commit.
  </action>
  <acceptance_criteria>
    - `test ! -d src/frontend/screens/Humble/Keys/Waiting` and the same for `Spares`, `All` and
      `components/HumbleKeyGroup` all succeed.
    - `grep -c 'Keys/All/index.tsx\|Keys/Spares/index.tsx\|Keys/Waiting/index.tsx\|HumbleKeyGroup/index.tsx' meta/i18nGateScope.json meta/i18nForkTouchedFiles.json`
      returns `0` for both files.
    - `grep -c 'HumbleClaimWizard/index.tsx' meta/i18nGateScope.json meta/i18nForkTouchedFiles.json`
      returns `1` for each file.
    - A python one-liner reading both JSON files prints scope `170`, forkTouched `212`, and an
      unscanned difference of `42` — unchanged, which is what the A-03 ratchet compares against
      `DECLARED_UNSCANNED_DEBT`.
    - `baseCommit` and `generatedAt` in `meta/i18nForkTouchedFiles.json` are byte-identical to their
      pre-edit values.
    - `npx jest --selectProjects Meta meta/__tests__/genI18nGateScope.test.ts` reports
      `Tests: 26 passed, 1 skipped, 27 total` and zero failures — the measured pre-phase shape. A
      regression here names the exact drifted file; read the failure text rather than re-running.
    - `git show --stat HEAD` for this task's commit lists both `meta/*.json` files AND the deleted
      source files — one commit, not two.
    - `npx tsc --noEmit` completes with zero errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx jest --selectProjects Meta meta/__tests__/genI18nGateScope.test.ts 2>&amp;1 | grep -E '^Tests:'</automated>
  </verify>
  <done>The four files are gone and both gate artifacts stopped naming them in the same commit, with the A-17 ratchet and the ANTI-ROT check still green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete groupKeys.ts and prune the two zero-caller viewFilters exports</name>
  <read_first>
    - `src/common/humble/groupKeys.ts` — the whole file. Confirm `GENERIC_KEY_PLATFORM` already moved
      to `genericKeyPlatform.ts` (plan 43-04) and that nothing else survives: `HumbleKeyGroupId`,
      `GROUP_ORDER`, `byExpiringSoonest` and `groupAndSortKeys` are all D-43-20 casualties.
    - `src/common/humble/viewFilters.ts` after plan 43-04 — the whole file, especially
      `selectKeysWaiting` (SURVIVES), `selectGiftableSpares` at `:75-77` and
      `partitionWaitingByUrgency` at `:91-105` (both go), and the three exports plan 43-04 added.
    - `src/backend/humble/__tests__/viewFilters.test.ts` after plan 43-04 — read every `describe`, so
      the prune removes exactly the blocks whose subjects are deleted and nothing else.
    - `src/backend/humble/__tests__/groupKeys.test.ts` — all 12 tests, to confirm every subject is
      being deleted and none pins behaviour that moved elsewhere.
    - `grep -rn "selectKeysWaiting" src` — re-verify the four surviving consumers yourself rather
      than trusting this plan's list.
  </read_first>
  <files>src/common/humble/groupKeys.ts, src/backend/humble/__tests__/groupKeys.test.ts, src/common/humble/viewFilters.ts, src/backend/humble/__tests__/viewFilters.test.ts</files>
  <action>
    Delete `src/common/humble/groupKeys.ts` and `src/backend/humble/__tests__/groupKeys.test.ts`.
    This removes `byExpiringSoonest` as a side effect, which is how D-43-06's "pick one comparator
    and delete the other" resolves — by construction, not by a separate decision. Plan 43-04 already
    pinned `compareWaiting`'s tiebreak with a mutation-proven test precisely so this deletion cannot
    silently change the shipped ordering.

    In `viewFilters.ts`, delete `selectGiftableSpares` and `partitionWaitingByUrgency`. Both are now
    zero-caller exports, and a zero-caller export that survives a restructure is exactly the dead
    code D-43-20 exists to remove. `partitionWaitingByUrgency`'s fate is under-specified by D-43-19,
    which says only that it "loses its only caller"; this plan closes it explicitly rather than
    leaving it ambiguous. Its replacements are `isGiftableSpare` for the spare case and nothing at
    all for the urgency partition — D-43-06's default sort already brings expiring keys to the top,
    and `UrgencyBadge` still marks them.

    **Do not delete `selectKeysWaiting`.** Run `grep -rn "selectKeysWaiting" src` first. If it shows
    anything other than the four consumers listed in `<interfaces>`, stop and record the discrepancy
    rather than proceeding.

    In `viewFilters.test.ts`, delete exactly the `partitionWaitingByUrgency` and
    `selectGiftableSpares` describe blocks and their imports. Keep every `selectKeysWaiting` test and
    every block plan 43-04 added. Record the before count, the after count, and the number of tests
    the two deleted blocks contained — a drop larger than that means something else went with them.

    Update `viewFilters.ts`'s module docblock: it currently says the frontend TABS map the returned
    flat arrays to rows and cross-references `groupKeys.ts` as a same-tier sibling. Both are false
    now. Also fix `selectKeysWaiting`'s own doc comment where it says generic-platform entries "still
    surface via the All tab's 'Other' bucket (round-7 decision)" — that bucket and that tab are both
    gone, and under D-43-01 generic keys are ordinary rows on the unified list.
  </action>
  <acceptance_criteria>
    - `test ! -f src/common/humble/groupKeys.ts` and
      `test ! -f src/backend/humble/__tests__/groupKeys.test.ts` both succeed.
    - `grep -rn "groupAndSortKeys\|GROUP_ORDER\|byExpiringSoonest\|partitionWaitingByUrgency" src --include="*.ts" --include="*.tsx"`
      returns zero lines (REQ-43-17, REQ-43-18).
    - `grep -c 'export function selectKeysWaiting' src/common/humble/viewFilters.ts` returns `1`.
    - `grep -c 'selectGiftableSpares\|partitionWaitingByUrgency' src/common/humble/viewFilters.ts`
      returns `0`.
    - `grep -c "Other' bucket\|All tab\|groupKeys" src/common/humble/viewFilters.ts` returns `0` —
      no surviving comment claims the tabs consume these arrays or that `groupKeys.ts` is a sibling.
    - `npx jest --selectProjects Backend src/backend/humble/__tests__/viewFilters.test.ts` reports
      `Tests: N passed, N total` with zero failures, where N equals plan 43-04's recorded count minus
      the two deleted blocks' test count. Record all three numbers.
    - `npx jest --selectProjects Backend src/backend/humble/__tests__/library.test.ts src/backend/discounts/__tests__/badges.test.ts`
      reports zero failures — the two backend consumers of `selectKeysWaiting` still pass.
    - `npx tsc --noEmit` completes with zero errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test ! -f src/common/humble/groupKeys.ts &amp;&amp; npx jest --selectProjects Backend src/backend/humble/__tests__/viewFilters.test.ts src/backend/humble/__tests__/library.test.ts 2>&amp;1 | grep -E '^Tests:'</automated>
  </verify>
  <done>The grouped presentation and both zero-caller exports are gone, the surviving comparator is the one with the pinned tiebreak, and selectKeysWaiting's four external consumers are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the surviving comments that name deleted files</name>
  <read_first>
    - `src/frontend/screens/Humble/Keys/stateLabels.ts` — the whole file (24 lines). Its docblock at
      `:4-11` names `HumbleKeyGroup` twice, in prose, as a live consumer and as one half of the
      WR-09 circular import the file was extracted to break.
    - `meta/hardcodedStringGate.ts:1200-1224` — the dated ts-morph census comment listing six users
      of the `{key, defaultText}` exemption shape across "all 174 scope files on 2026-09-09",
      including a `HumbleKeyGroup/index.tsx` row.
    - `src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx` — to name the surviving
      consumer accurately: `STATE_LABEL_KEYS` is now read only by the row's KEY-column state badge.
  </read_first>
  <files>src/frontend/screens/Humble/Keys/stateLabels.ts, meta/hardcodedStringGate.ts</files>
  <action>
    Rewrite `stateLabels.ts`'s docblock. It must say the table is used by `HumbleKeyRow`'s KEY-column
    state badge, and it must keep the WR-09 lesson — the file exists as its own leaf module so no two
    components import a constant through each other — stated in terms that survive `HumbleKeyGroup`'s
    deletion rather than narrating a circular import between two files one of which no longer exists.
    Keep the D-30 note about `UNREDEEMABLE`'s locked internal name and its user-visible "Expired"
    label verbatim; that is still true and still load-bearing.

    Do not change `STATE_LABEL_KEYS` itself. It stays a `[key, defaultText]` tuple table with
    unprefixed `humbleKeys.state.*` keys — the shape `meta/hardcodedStringGate.ts` exempts and which
    the row consumes as `t(labelKey, labelDefault)`. That idiom is only broken by a `gamelib:`
    namespace prefix, and these keys carry none.

    In `meta/hardcodedStringGate.ts`, append ONE dated line to the census comment recording that
    Phase 43 deleted `HumbleKeyGroup/index.tsx` and moved the scope count from 174 to 170, so the
    snapshot now lists five live users rather than six. Do NOT re-run the census and do NOT change
    any predicate — the comment's own text says the numbers are a snapshot and that a reopener must
    re-measure first, so an annotated snapshot stays honest while a silently-edited one would not.

    Finally sweep for any other surviving reference to a deleted file. Plan 43-07 rewrote
    `Keys/index.tsx`'s top-of-module comment and plan 43-06 owned `HumbleKeyRow/index.tsx:314`'s
    Keys-waiting reference — verify both landed rather than assuming, and record what the sweep found.
  </action>
  <acceptance_criteria>
    - `grep -c 'HumbleKeyGroup' src/frontend/screens/Humble/Keys/stateLabels.ts` returns `0`.
    - `grep -c 'HumbleKeyRow' src/frontend/screens/Humble/Keys/stateLabels.ts` returns at least `1`.
    - `grep -c 'WR-09' src/frontend/screens/Humble/Keys/stateLabels.ts` returns `1` — the lesson
      survived the rewrite.
    - `grep -c 'Expired' src/frontend/screens/Humble/Keys/stateLabels.ts` returns at least `1`.
    - A repo-wide grep for `HumbleKeyGroup`, `Keys/Waiting`, `Keys/Spares` and `Keys/All` across
      `src` and `meta` (`*.ts`, `*.tsx`, `*.json`), excluding `meta/hardcodedStringGate.ts`, returns
      zero lines — the only surviving mention anywhere is the deliberately-annotated census comment.
    - `npx jest --selectProjects Meta meta/__tests__/hardcodedStringGate.test.ts` reports
      `Tests: 151 passed` and zero failures. This suite takes about 74 seconds; do not treat a
      timeout as a pass.
    - `npx jest --selectProjects Frontend src/frontend/screens/Humble/Keys` reports zero failures and
      `Test Suites: 3 passed` — HumbleKeyRow, HumbleClaimWizard and the new unified screen suite,
      down from the pre-phase four because two tab suites went and one new one arrived.
    - `npx tsc --noEmit` completes with zero errors.
    - `node meta/lintScoped.cjs` passes both scopes. The deletions free warnings, so the two
      zero-padding ceilings (`SRC_CEILING = 1123`, `TESTS_CEILING = 638`) now have headroom — record
      the measured warning counts, since plan 43-09 will spend some of it.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node meta/lintScoped.cjs &amp;&amp; npx jest --selectProjects Frontend src/frontend/screens/Humble/Keys 2>&amp;1 | grep -E '^(Tests|Test Suites):'</automated>
  </verify>
  <done>No surviving comment describes a file that no longer exists, the census snapshot is annotated rather than silently rewritten, and both blocking gates are green after the deletions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed gate artifacts to CI verdict | Two JSON snapshots decide which source files the blocking hardcoded-string gate can even see |
| deleted module to surviving importer | A stranded import is a build break; a stranded comment is a lie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | **The C5 isolation wall — a Humble `code` is a secret that must never reach a logger.** | mitigate | Not reachable here: this plan deletes frontend components and pure predicates and edits two JSON scope lists. It adds no logging, no HTTP call and no IPC, and none of the deleted or edited modules ever holds a key value. Named because narrowing the i18n gate's scope from 174 to 170 files narrows what a gate can SEE — Task 1's criteria therefore assert the four removals are exactly the four deleted files and that every survivor stays enrolled. |
| T-43-24 | Tampering | Narrowing a blocking gate's scope past the files that were actually deleted, blinding it to live code | mitigate | Task 1 asserts exact counts (170 / 212 / 42), asserts `HumbleClaimWizard` and `HumbleKeyRow` remain enrolled in both artifacts, and runs the A-03 and A-17 assertions that fail by name on any other drift |
| T-43-25 | Repudiation | Regenerating an artifact and silently absorbing unrelated fork drift as if it were this phase's | mitigate | Hand-edit is mandated with the file's own precedent cited; `baseCommit` and `generatedAt` are asserted byte-identical; `pnpm gen-i18n-gate-scope` is named and forbidden |
| T-43-26 | Repudiation | A surviving doc comment describing a deleted file, misleading the next reader | mitigate | Task 3 rewrites `stateLabels.ts`'s docblock, annotates the dated census snapshot rather than editing it silently, and requires a repo-wide sweep whose only permitted hit is that one annotated comment |
| T-43-27 | Denial of Service | Deleting `selectKeysWaiting` and breaking StoreSearch, Discounts and two backend suites | mitigate | `<interfaces>` names all four consumers with file:line, Task 2 mandates re-verifying by grep before editing and forbids the deletion, and its criteria run both backend consumer suites |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (`43-RESEARCH.md` section Package Legitimacy Audit). No install task exists in this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `meta/__tests__/genI18nGateScope.test.ts` reports 26 passed / 1 skipped, zero failures.
- `meta/__tests__/hardcodedStringGate.test.ts` reports 151 passed, zero failures.
- `npx jest --selectProjects Frontend src/frontend/screens/Humble/Keys` reports 3 suites, zero
  failures.
- A repo-wide grep for the five deleted identifiers returns zero lines outside the one annotated
  census comment.
- `node meta/lintScoped.cjs` passes both scopes, with the measured warning counts recorded.
</verification>

<success_criteria>
Everything the unified list made dead is gone, nothing that survives depends on it, both blocking
gates are green on the same commit as the deletion, and no comment in the tree describes a file that
no longer exists.
</success_criteria>

<output>
Create `.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-08-SUMMARY.md` when done
</output>
