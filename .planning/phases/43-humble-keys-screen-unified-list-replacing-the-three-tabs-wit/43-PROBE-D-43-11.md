# D-43-11 probe: does `POST /humbler/redeemkey` accept a keyless `gog_keyless` entitlement?

Settles which branch plan 43-09 implements for the `gog_keyless` KEY column: candidate A
(reuse the reveal endpoint, one-click in-app), candidate B (Phase 40 embedded store browser),
or the external-browser fallback. See `43-CONTEXT.md` § D-43-11 and `43-03-PLAN.md` Task 1-3.

## Go-ahead record (Task 1)

- **Chosen option:** `run-probe` — spend the operator's single live `gog_keyless` entitlement.
- **Date:** 2026-09-10
- **Recorded by:** this plan's orchestrator, per the operator go-ahead already given and
  supplied verbatim in this plan's launch prompt (`<operator_go_ahead>`), not re-solicited here
  per that prompt's explicit instruction ("Task 1's go-ahead already given & recorded, not
  re-asked").
- **Operator's own words (as supplied in the launch prompt):** "43-03 one-click in-app GOG
  (2026-09-07); GOG `UNREVEALED` pre-redemption... Go-ahead... re-prompt SHOULD [occur] before
  [the] irreversible POST, [this checkpoint] is [a] factual check on [the] sample, not
  re-litigation [of the] decision." The operator explicitly asked for an in-app claim path
  before accepting one that leaves the app (`43-CONTEXT.md` § D-43-11), which is the standing
  motivation for spending the sample on candidate A rather than defaulting to the external
  browser.
- **Entitlement identity (to be confirmed on screen immediately before the click, per this
  plan's acceptance criteria):** GOG game, `platform: gog_keyless`, purchased 2026-09-07,
  expected `state: UNREVEALED`, expected no ownership badge (not `ownedElsewhere`). Recorded
  as-observed in the Preconditions section below once Task 2 runs.
- **Consequence if refused at the live checkpoint:** the operator may still stop before the
  click; that would flip this record to `VERDICT: NOT RUN` / `SELECTED BRANCH: external
  browser` per the plan's fallback rule, superseding this go-ahead. Not applicable unless the
  live Task 2 checkpoint reports a refusal.

## Preconditions (Task 2 — pending live capture)

_To be filled from the operator's report. Not yet run._

- Session directory:
- Pre-existing `gamelib-shell` instance count (must be 0 before launch):
- Entitlement observed: title / platform / state / ownership badge:
- Click count (must be exactly 1):

## Raw observation (Task 2 — pending live capture)

_To be filled from the operator's report. Not yet run._

- Matched adapter log line (verbatim, inside a fenced block, grep-checked for zero key-shaped
  tokens before being pasted here):
- Wizard's displayed text (verbatim):
- Post-sync row state (UNREVEALED / REVEALED / REDEEMED / absent):
- GOG-grant check (yes / no / could-not-determine):

## VERDICT and SELECTED BRANCH

_Pending Task 2's live capture. Task 3 will fill this in from the matched log shape per the
plan's mapping table._

## Residual unknowns

- Behaviour when the GOG↔Humble account link is ABSENT stays unmeasured — there is no second,
  unlinked test account available. This is unmeasurable with the resources at hand, not an
  oversight.
- The sample is consumed by this probe. Any future question about `gog_keyless` behaviour needs
  a newly-acquired entitlement; none remains for re-testing a different question.
