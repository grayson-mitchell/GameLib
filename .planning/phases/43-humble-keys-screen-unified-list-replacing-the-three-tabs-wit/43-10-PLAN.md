---
phase: 43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit
plan: 10
type: execute
wave: 5
depends_on:
  - 43-05
  - 43-07
  - 43-08
  - 43-09
files_modified:
  - .planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md
  - .planning/todos/pending/2026-09-08-humble-key-row-store-icon-geometry-unverified-live.md
  - .planning/todos/pending/2026-09-08-humble-key-row-store-logo-fill-currentcolor-unverified-live.md
autonomous: false
requirements:
  - REQ-43-19

must_haves:
  truths:
    - "A gate contract exists whose every item passed a seven-test Structural Reachability Review with a recorded verdict and cited evidence per row"
    - "The TYPE and KEY column widths were MEASURED, not eyeballed, on a packaged Tauri build"
    - "The row separator was confirmed to render as a visible hairline after the flex-to-grid conversion"
    - "The store logo's fill and its alignment to the title line-box were re-measured in the new TYPE column across at least two themes"
    - "The two folded todos close only on a PASS verdict, and stay open on anything else"
  artifacts:
    - path: ".planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md"
      provides: "the contract, its reachability review, the run's evidence, and the scored verdict"
      contains: "VERDICT"
  key_links:
    - from: ".planning/phases/43-.../43-LIVE-GATE.md"
      to: ".planning/todos/pending/2026-09-08-humble-key-row-store-icon-geometry-unverified-live.md"
      via: "the folded todo closed only on PASS"
      pattern: "store-icon-geometry"
---

<objective>
Author and run the one requirement in this phase that no automated test can reach: REQ-43-19, the
column geometry and the row separator, measured on a packaged Tauri build.

Purpose: `src/frontend/jest.config.js` sets `testEnvironment: 'node'` deliberately — there is no
jsdom, no `jest-environment-jsdom` and no browser automation anywhere in this repo. Nothing here can
measure a rendered width, a computed fill, or whether a 1px border painted. A component test
asserting `grid-template-columns` would prove only that the string was written. D-43-21 also
inherits two folded todos whose claims about the store icon's geometry and its `fill: currentColor`
were never verified against a live render, and this phase changed the inheritance chain around both.

Output: a contract that survives the Structural Reachability Review, a scored run, and two todos
closed on evidence or left open on the absence of it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-UI-SPEC.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-CONTEXT.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-VALIDATION.md
</context>

<interfaces>
Measured against the repo during planning.

- **The standing rule, from `live-gate-contract-authoring.md`: whoever authors a gate contract may
  never be the one who runs it.** Task 1 authors; Task 2 is the operator's run; Task 3 records what
  the operator reported. The authoring agent must not score its own contract.
- **A green test suite never closes a live gate** (same reference, Section 4 rule ii). Every blocking
  defect found across Phases 34.4.1 and 34.4.2 — the sheet-attachment wedge, the autofill non-fill,
  the origin-confusion race, the Humble disconnect deadlock — was found by a human driving the UI on
  real hardware while `cargo test` and `npm run test:ci` were green throughout.
- `43-UI-SPEC.md` § "Column Geometry Contract" — `grid-template-columns: 6.5rem minmax(0, 1fr) 20rem`
  with `column-gap: var(--space-md)`. The two fixed tracks are explicitly labelled code-level
  estimates, not pixel measurements, and both are flagged as needing live re-verification.
- `43-UI-SPEC.md` § "The row-separator landmine" — a 1px border on a fractional (`1fr`) grid TRACK
  vanishes in WKWebView. Plan 43-05 kept the border on `.humbleKeyRow`, the grid CONTAINER. That is
  the mitigation; this gate is what confirms it worked.
- The two folded todos, both `area: humble-keys-ui`, `severity: minor`, `platform: any`,
  `ready: live-gate`:
  `.planning/todos/pending/2026-09-08-humble-key-row-store-icon-geometry-unverified-live.md` and
  `.planning/todos/pending/2026-09-08-humble-key-row-store-logo-fill-currentcolor-unverified-live.md`.
  Read both — the first names `.humbleKeyRowStoreLogo` and `.humbleKeyRowTitle` in `index.css`, the
  second names the `color: var(--text-secondary)` rule at `index.css:265-272` that the fill inherits
  from. This phase moved the logo into a new `TYPE` grid cell, changing that inheritance chain.
- `43-CONTEXT.md` § Landmines item 6 — tests clobber the real Humble store; `humble_library.json`
  was wiped to `{}` on 2026-09-07 by that defect. Re-sync and confirm the cache is populated before
  claiming any measurement.
- The dual-sink evidence standard (`live-gate-contract-authoring.md` § Section 3): one session
  directory per run named with a UTC timestamp; the transcript appended with `tee -a`, never `>`;
  a delimiter line per launch; `gamelib.log` archived per launch because the logger rotates to
  `.old` on every launch (`src/backend/logger/log_writer.ts:72-74`); exactly ONE app instance for
  the whole run, asserted before and after, because a second instance splits the `[shell]` sink
  while sharing `gamelib.log` and produces the most dangerous evidence shape the standard names; and
  a closing inventory listing the session directory with per-file line counts.
- Repo lesson, binding on this contract: live UI layout claims need PIXEL MEASUREMENT — a `cat`-style
  render or a visual "looks aligned" has been wrong twice. Screenshot-and-measure or read the
  accessibility tree; do not eyeball.
- Repo lesson, also binding: the macOS accessibility API is blind to some Tauri surfaces, and a
  picker-driven gate run can measure nothing at all. If AX cannot see the row, fall back to a
  screenshot with a known-scale reference and measure in pixels.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the REQ-43-19 gate contract and pass it through the Structural Reachability Review</name>
  <read_first>
    - `.claude/skills/spike-findings-gamelib/SKILL.md` — the index, to know which references exist.
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` — the WHOLE
      file. Section 2's seven defect-class tests, Section 3's dual-sink standard and Section 4's two
      rules are all mandatory, not guidance.
    - `43-UI-SPEC.md` § "Column Geometry Contract" and § "KEY-Column Scenario Matrix" — the geometry
      claims to be measured and the five row shapes the KEY column must hold at one width.
    - `.planning/todos/pending/2026-09-08-humble-key-row-store-icon-geometry-unverified-live.md` and
      `...-store-logo-fill-currentcolor-unverified-live.md` — both in full, including frontmatter.
      Their claims become gate items; a folded todo that closes without its own claim being measured
      is exactly the "accepted is not discharged" failure this repo has recorded before.
    - `src/frontend/screens/Humble/Keys/index.css` as it stands after plans 43-05 and 43-07 — the
      shipped declarations the gate is scoring.
  </read_first>
  <files>.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md</files>
  <action>
    Write `43-LIVE-GATE.md` with a Preconditions section, a numbered item list, a Structural
    Reachability Review table, an Evidence-capture protocol, and an empty Verdict section for the
    runner to fill.

    Items to author, each with an explicit measurement method and an explicit pass threshold — not
    "looks right":
    1. The `TYPE` column's rendered width is identical on the column-header row and on every data
       row, within a stated tolerance in device-independent pixels. Measure, do not eyeball.
    2. The `KEY` column's rendered width is identical across all five KEY-scenario row shapes:
       a full-width single button (`pick`), a side-by-side pair (`claim-and-gift`), bare text with a
       small text-link (`settled` / `override-*`), bare text with no control (`expired`), and the
       Pitfall-C disabled caption. This is the UI-SPEC's core invariant — the KEY column must not
       widen or narrow with its content.
    3. The `GAME` column's title wraps rather than overflowing when a long title is present, and the
       column's left edge does not shift between a logo row and a no-logo row, nor for an UNPICKED
       row whose TYPE cell is empty.
    4. The row separator renders as a visible hairline between rows and is absent after the last row
       — the WKWebView fractional-track border trap, confirmed after the flex-to-grid conversion.
    5. Folded todo 1: the store logo's size, position and alignment to the title's line-box in the
       new `TYPE` column. The old measurements are moot because the icon moved into a real column;
       measure the new ones.
    6. Folded todo 2: the store logo's `fill: currentColor` resolves to the intended colour in at
       least two themes, at least one of them dark. The `TYPE` cell changed the inheritance chain
       that `index.css:265-272` documents as easy to lose.
    7. The controls row and column-header row align with the data rows' column boundaries — the
       single shared `grid-template-columns` declaration doing its job in a real renderer.

    Then apply ALL SEVEN reachability tests to every item, sub-check and precondition — including to
    the evidence-capture instructions themselves — and record the result as a table with columns:
    Item / Surface it names / What it asks for / Verdict (REACHABLE, IMPOSSIBLE or CONDITIONAL) /
    Evidence (a file:line or a command). Specifically:
    - Test 1 (origin/scheme): does any item need a URL or origin the shipping code rejects before the
      surface renders?
    - Test 2 (concurrency): does any item ask for two things driven at once that the platform's own
      modality rules make impossible?
    - Test 3 (log-line emitter, with the SINK clause): for every literal log line an item requires,
      grep it in current source, record the file:line, AND identify which sink its emitter writes to,
      AND confirm the capture protocol actually reads that sink. Sidecar `logInfo`/`logWarning` reach
      `gamelib.log` and never the terminal transcript; Rust `[shell]` lines reach the transcript and
      never `gamelib.log`; sidecar `console.*` reaches nothing at all. A required line whose emitter
      exists but whose sink is not captured is IMPOSSIBLE just as surely as one with no emitter.
    - Test 4 (absence-observability): for every item asserting an ABSENCE, state what PRESENCE would
      have looked like and whether it could have appeared. Distinguish a falsifiable absence from a
      structural guarantee, and label which each item is, so "confirmation of an existing structural
      guarantee" is never later misread as "a novel live discovery".
    - Test 5 (requirement-interaction): review PAIRS of items, not only items alone.
    - Test 6 (pre-existing external state): does any item depend on state the machine already
      carries? Item 5 and 6 both do — they need a populated Humble library, which landmine 6 says
      can be wiped by a test run.
    - Test 7 (UI-level reachability, distinct from backend-logic reachability): can the operator
      actually reach each row shape in the UI with the library they have? Item 2 needs all five KEY
      scenarios on screen at once. If the operator's real library cannot produce, for example, an
      UNPICKED row or an `override-pending` row, that sub-check is CONDITIONAL and the contract must
      say how to reach it or record it as not attemptable — not leave the runner to improvise.

    Any item scoring IMPOSSIBLE must be rewritten or removed BEFORE the run, not discovered during
    it. Record the rewrite in the document.

    Write the Evidence-capture protocol in full, per the dual-sink standard, with the exact commands
    the runner will paste. Do not summarise it by reference.

    **Do not run the gate in this task.** The author may not be the runner. Leave the Verdict section
    empty with a per-item PASS/FAIL/NOT-ATTEMPTED table for the runner to fill.
  </action>
  <acceptance_criteria>
    - `43-LIVE-GATE.md` contains a `## Structural Reachability Review` table with at least one row
      per item AND at least one row per precondition AND at least one row per evidence-capture
      instruction — count them and state the count in the document.
    - Every review row's Verdict cell is exactly one of `REACHABLE`, `IMPOSSIBLE` or `CONDITIONAL`,
      and every row's Evidence cell contains a `file:line` reference or a runnable command.
    - `grep -c 'IMPOSSIBLE' .planning/phases/43-*/43-LIVE-GATE.md` — for each occurrence, the
      document contains an adjacent note saying the item was rewritten or removed, and the surviving
      item list contains no IMPOSSIBLE row.
    - The document contains a `## Evidence capture` section naming a session directory, `tee -a`
      (never a bare `tee` or `>`), a per-launch delimiter line, `gamelib.log` archiving with a
      launch ordinal, the single-instance assertion (`pgrep` before and after), and a closing
      inventory (`ls -la` plus `wc -l`).
    - The document contains a `## Verdict` section whose per-item table is entirely unfilled.
    - The document states, in its own words, that its author may not score it.
    - `grep -c 'looks correct\|looks right\|looks aligned\|appears to' .planning/phases/43-*/43-LIVE-GATE.md`
      returns `0` — every threshold is a measurement, not an impression.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/43-*/43-LIVE-GATE.md &amp;&amp; grep -c 'REACHABLE\|CONDITIONAL' .planning/phases/43-*/43-LIVE-GATE.md &amp;&amp; test "$(grep -c 'looks correct\|looks right\|looks aligned' .planning/phases/43-*/43-LIVE-GATE.md)" = "0"</automated>
  </verify>
  <done>A contract exists whose every item, precondition and capture instruction has a recorded reachability verdict with cited evidence, and which its author is forbidden to score.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator drives and scores the gate on a packaged Tauri build</name>
  <read_first>
    - `.planning/phases/43-.../43-LIVE-GATE.md` — the contract authored in Task 1, in full, including
      its Evidence-capture section's exact commands.
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` § Section 3,
      for why each capture rule exists — a runner who understands the single-instance rule will not
      skip it under time pressure.
  </read_first>
  <action>
    Pause for the operator. The contract's author may not score it, and no automated check in this
    repository can measure a rendered width, a computed fill or a painted border. The operator
    builds a packaged Tauri bundle, follows the contract's evidence-capture protocol, and reports a
    per-item verdict table with raw measurements. No file is edited here.
  </action>
  <what-built>
    The unified Humble Keys screen: a three-column CSS Grid row and a column-header row sharing one
    `grid-template-columns` declaration, a KEY column holding five different content shapes at one
    fixed width, and a row separator kept on the grid container rather than a fractional track. All
    of it is code-level and typecheck-level verified; none of it has ever been rendered and measured.
  </what-built>
  <how-to-verify>
    1. Follow the contract's Preconditions and Evidence-capture sections exactly. Create the session
       directory first, assert zero pre-existing instances, and archive `gamelib.log` and its `.old`
       before the first launch.
    2. Re-sync the Humble library and confirm the Keys screen shows a populated list before taking
       any measurement. An empty list means landmine 6 struck and nothing measured here is scorable.
    3. Build and run a packaged Tauri build — not `tauri:dev`. Two recorded reasons: `tauri:dev`
       serves a stale static bundle in some configurations, and a `--debug` packaged run can ship a
       stale SEA sidecar. Record the build command and the resulting bundle path.
    4. Score each item in order, recording for each: the launch ordinal it was covered by, the raw
       measurement (numbers, not adjectives), the threshold from the contract, and PASS or FAIL.
    5. **Measure, do not eyeball.** Take a screenshot and measure pixels, or read the accessibility
       tree — the repo has recorded two occasions where a visual layout claim was wrong. If the
       accessibility API cannot see the row, say so and fall back to screenshot measurement rather
       than downgrading the item to an impression.
    6. For item 6, switch themes at least once and record the resolved logo colour in each, as a
       measured colour value rather than a description.
    7. Closing inventory: `ls -la $SESSION/` and `wc -l $SESSION/*`. A zero-length capture must be
       visible now, while the app is still open, not discovered during write-up.

    Expected outcome: every item PASS. Any FAIL is a real finding and should be recorded with its
    measurement, not retried until it passes.
  </how-to-verify>
  <acceptance_criteria>
    - Every item in the contract's Verdict table carries PASS, FAIL or NOT ATTEMPTED, plus a raw
      measurement and the launch ordinal that produced it. No item is left blank.
    - The build was a packaged Tauri build, and the build command and bundle path are recorded.
    - `pgrep -f 'gamelib'` was recorded as exactly one instance while the window was up, before and
      after. Any other count aborts the run — the evidence is not scorable.
    - The Keys screen was confirmed populated before the first measurement.
    - At least one item's evidence is a numeric pixel or colour measurement, not a description.
    - The closing inventory shows no zero-length capture file.
    - The person scoring this gate is NOT the agent that authored the contract in Task 1.
  </acceptance_criteria>
  <done>Every contract item carries PASS, FAIL or NOT ATTEMPTED with a raw measurement and a launch ordinal, the build was packaged rather than `tauri:dev`, and the single-instance assertion held throughout.</done>
  <resume-signal>Paste the per-item verdict table with raw measurements, the build command and bundle path, and the `pgrep` counts. Type "scored" when the session inventory is recorded.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verdict and close the two folded todos only on evidence</name>
  <read_first>
    - `.planning/phases/43-.../43-LIVE-GATE.md` — the filled Verdict table from Task 2.
    - `.planning/todos/pending/2026-09-08-humble-key-row-store-icon-geometry-unverified-live.md` and
      `...-store-logo-fill-currentcolor-unverified-live.md` — both in full, including their
      `severity`, `platform` and `ready` frontmatter keys.
    - `./CLAUDE.md` § "Todo triage frontmatter (enforced by CI)" — `severity`, `platform` and `ready`
      are all required on every file in `.planning/todos/pending/`, matched bare and lowercase, and
      `pnpm planning-gates` enforces this in CI. The `completed/` directory is deliberately exempt.
    - `.planning/todos/completed/` — for the naming convention of a closed todo.
  </read_first>
  <files>
    .planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-LIVE-GATE.md,
    .planning/todos/pending/2026-09-08-humble-key-row-store-icon-geometry-unverified-live.md,
    .planning/todos/pending/2026-09-08-humble-key-row-store-logo-fill-currentcolor-unverified-live.md
  </files>
  <action>
    Write the overall verdict into `43-LIVE-GATE.md` as a single `**VERDICT:**` line reading `PASS`
    or `FAIL n/m`, with the per-item table filled from the operator's report verbatim — including
    the raw measurements. Do not paraphrase a measurement into an adjective.

    Then dispose of the two folded todos, strictly on evidence:
    - Item 5 PASS closes the store-icon geometry todo; item 6 PASS closes the
      `fill: currentColor` todo. Move each closed file to `.planning/todos/completed/` and record in
      it the measurement that closed it and the gate document it came from.
    - If an item FAILED or was NOT ATTEMPTED, its todo stays in `pending/` with its frontmatter
      intact, and gains a dated note recording what was measured and why it did not close. Do not
      close a todo because the phase is otherwise finished — that is the "accepted is not
      discharged" failure this repo has recorded before, and a folded todo closed without its own
      claim being measured is exactly what D-43-21 was written to prevent.

    If any item FAILED, also record whether the failure is a defect in this phase's shipped CSS or a
    limitation of the measurement, and name the follow-up: a gap plan via
    `/gsd-plan-phase 43 --gaps` for the former, a contract revision for the latter.

    Do not edit `.planning/STATE.md` and do not invoke any `gsd-sdk state.*` or `roadmap.*` verb.
  </action>
  <acceptance_criteria>
    - `grep -c '^\*\*VERDICT:\*\*' .planning/phases/43-*/43-LIVE-GATE.md` returns `1`.
    - The Verdict table has zero blank cells: every item row carries a result and a measurement.
    - Todo disposition matches the item results exactly. If item 5 PASSED, the store-icon todo is in
      `.planning/todos/completed/` and absent from `pending/`; if it did not, the reverse, and the
      pending file still carries all three of `severity:`, `platform:` and `ready:` as bare
      lowercase values.
    - `python3 meta/runPlanningGates.py` prints `7/7 planning gates passed.` with zero `[FAIL]`
      lines — the todo frontmatter gate is one of them, and moving or annotating a todo is the exact
      operation that trips it.
    - `git status --porcelain src/` is empty — this plan changes no source code.
    - `git diff --name-only` does not list `.planning/STATE.md`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^\*\*VERDICT:\*\*' .planning/phases/43-*/43-LIVE-GATE.md)" = "1" &amp;&amp; test -z "$(git status --porcelain src/)" &amp;&amp; python3 meta/runPlanningGates.py</automated>
  </verify>
  <done>The gate's result is recorded with its raw measurements, and each folded todo closed only because its own claim was measured — or stayed open, saying why.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered pixels to recorded claim | A visual impression entered as a measurement becomes a shipped, unfalsifiable assertion |
| gate author to gate runner | An author who scores their own contract cannot discover that the contract was unrunnable |
| live app run to `gamelib.log` | A second concurrent instance silently splits one sink while sharing the other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | **The C5 isolation wall — a Humble `code` is a secret that must never reach a logger.** This gate drives a live, signed-in Humble session and archives `gamelib.log` into a session directory | mitigate | The gate performs no reveal and no redemption — it measures layout only, so no code-bearing path is exercised. The archived `gamelib.log` is nonetheless a real session log: Task 2's evidence must be scanned for key-shaped tokens before any excerpt is pasted into `43-LIVE-GATE.md`, and only measurements and status lines may be quoted. The shipping redaction discipline at `adapter.ts:610-620` remains the wall itself; this plan changes no source. |
| T-43-31 | Repudiation | Scoring a gate item from a visual impression rather than a measurement | mitigate | Every item carries a numeric threshold; Task 1 asserts zero occurrences of impression language in the contract; Task 2 requires at least one numeric pixel or colour measurement and forbids downgrading an item to an impression when the accessibility API cannot see the surface |
| T-43-32 | Repudiation | The contract's author scoring their own contract, so an unrunnable item is never discovered | mitigate | The standing rule is stated in `<interfaces>`, restated inside the contract by Task 1, and asserted in Task 2's acceptance criteria |
| T-43-33 | Tampering | A second app instance splitting the `[shell]` sink while sharing `gamelib.log`, producing a populated but incomplete evidence set | mitigate | The single-instance assertion is a Task 2 acceptance criterion with `pgrep` before and after, and any count other than 1 aborts the run rather than being noted |
| T-43-34 | Repudiation | Closing a folded todo because the phase finished rather than because its claim was measured | mitigate | Task 3 binds each todo's disposition to a specific item's result, and requires a pending todo that did not close to keep its full triage frontmatter plus a dated note |
| T-43-35 | Denial of Service | Measuring a stale bundle — `tauri:dev` serving a stale static bundle, or a `--debug` packaged run shipping a stale SEA sidecar | mitigate | Task 2 mandates a packaged build, not `tauri:dev`, and requires the build command and bundle path be recorded so the measured artifact is identifiable |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (`43-RESEARCH.md` section Package Legitimacy Audit). This plan builds and measures; it installs nothing. |
</threat_model>

<verification>
- `43-LIVE-GATE.md` carries a Structural Reachability Review with a verdict and cited evidence on
  every row, and no surviving IMPOSSIBLE item.
- The Verdict table is fully populated with raw measurements and launch ordinals.
- Each folded todo's location matches its corresponding item's result.
- `python3 meta/runPlanningGates.py` prints `7/7 planning gates passed.`
- `git status --porcelain src/` is empty.
</verification>

<success_criteria>
The one requirement no test in this repository can reach has been measured on real hardware by
someone other than its contract's author, and the two visual claims this phase inherited are closed
on evidence or left open on the honest absence of it.
</success_criteria>

<output>
Create `.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-10-SUMMARY.md` when done
</output>
