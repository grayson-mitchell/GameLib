---
phase: 43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-VALIDATION.md
autonomous: true
requirements:
  - REQ-43-01
  - REQ-43-02
  - REQ-43-03
  - REQ-43-04
  - REQ-43-05
  - REQ-43-06
  - REQ-43-07
  - REQ-43-08
  - REQ-43-09
  - REQ-43-10
  - REQ-43-11
  - REQ-43-12
  - REQ-43-13
  - REQ-43-14
  - REQ-43-15
  - REQ-43-16
  - REQ-43-17
  - REQ-43-18
  - REQ-43-19
  - REQ-43-20
  - REQ-43-21
  - REQ-43-22
  - REQ-43-23
  - REQ-43-24

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` contains a Phase 43 section defining REQ-43-01 through REQ-43-24"
    - "`.planning/ROADMAP.md`'s Phase 43 `**Requirements**:` line names the real ID range, not `TBD`"
    - "`43-VALIDATION.md`'s Per-Task Verification Map contains zero `TBD` task IDs"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-43-01..24 definitions plus 24 Traceability rows"
      contains: "REQ-43-24"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 43 requirement range"
      contains: "REQ-43-01"
    - path: ".planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-VALIDATION.md"
      provides: "Per-Task Verification Map with real plan/task IDs"
      contains: "43-04"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement ID range citation"
      pattern: "REQ-43-01"
    - from: ".planning/phases/43-.../43-VALIDATION.md"
      to: "the plan set"
      via: "task IDs in the verification map"
      pattern: "43-0[4-9]"
---

<objective>
Mint this phase's requirement set into the project records, so every plan in the set can carry
real `REQ-43-*` IDs and the phase can be verified against a written contract instead of prose.

Purpose: `.planning/REQUIREMENTS.md` has no `REQ-43-*` entries and ROADMAP.md's Phase 43 section
reads `**Requirements**: TBD`. `43-RESEARCH.md`'s `## Requirements (Proposed)` section already
carries a complete, PASS/FAIL-scoreable `REQ-43-01 … REQ-43-24` table mapped 1:1 to the locked
decisions — this plan adopts it verbatim rather than re-deriving it.

Output: a Phase 43 requirements section, 24 Traceability rows, a corrected ROADMAP line, and a
fully-populated Per-Task Verification Map in `43-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@./CLAUDE.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-RESEARCH.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-CONTEXT.md
</context>

<hard_constraints>
**Never invoke `gsd-sdk state.*`, `roadmap.update-*`, `roadmap.annotate-*` or `phase.complete`.**
These verbs silently corrupt `STATE.md` and `ROADMAP.md` in this repository — one measured
incident deleted 865 lines and wrote invalid YAML. Every edit in this plan is a hand-authored,
surgical `Edit` against a specific anchor string. Silence about a verb is not permission to use
it. This applies to `.planning/STATE.md` as well, which this plan does not touch at all.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the REQ-43-01..24 section into REQUIREMENTS.md</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read lines 1366-1420 (the `## Phase 40 Requirements` section)
      for the exact section shape to mirror: an `## Phase NN Requirements — <title>` heading, a
      minting provenance paragraph, an optional "planning-time corrections" block, then one
      `- [ ] **REQ-NN-XX**: <text>` bullet per requirement, then a `*Last updated: ...*` footer.
      Read lines 440-452 for the `| REQ-ID | Phase N | Pending |` Traceability row shape.
    - `.planning/phases/43-.../43-RESEARCH.md` lines 237-272 — the `## Requirements (Proposed)`
      table. This is the source of truth for the 24 requirement texts. Adopt it.
    - `.planning/phases/43-.../43-CONTEXT.md` — the D-43-01..D-43-21 decisions each requirement
      cites.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Append a new section `## Phase 43 Requirements — Humble Keys screen: unified list replacing
    the three tabs` immediately after the existing Phase 40 section (which currently ends the
    file at its `*Last updated: 2026-09-04 ...*` footer).

    Structure it exactly like the Phase 40 section:

    1. A provenance paragraph stating the set was minted during `/gsd-plan-phase 43` from
       `43-RESEARCH.md`'s `## Requirements (Proposed)` table, that `43-CONTEXT.md`'s D-43-01..D-43-21
       supply the shipping decisions, and that `43-UI-SPEC.md` (approved 6/6) is the binding
       design contract for REQ-43-19/-20/-21/-23.

    2. A **planning-time corrections** block recording the four repo facts measured during
       planning that change what a plan must assert. Each changes a requirement's meaning, so
       each belongs in the requirement record rather than buried in one plan:
       - `selectKeysWaiting` has THREE surviving production consumers outside the Humble Keys
         screen (`src/frontend/screens/StoreSearch/index.tsx:8,39`,
         `src/frontend/screens/Discounts/index.tsx:16,84`, plus
         `src/backend/humble/__tests__/library.test.ts:2147` and
         `src/backend/discounts/__tests__/badges.test.ts`). `43-RESEARCH.md`'s Execution
         Sequencing Risk point 5 and `43-PATTERNS.md`'s viewFilters assignment both state its
         only call sites are the deleted tab files. That is FALSE. REQ-43-18's "no longer exists
         in the tree" scope covers `HumbleKeyGroup`/`groupAndSortKeys`/`GROUP_ORDER`/
         `byExpiringSoonest`/`partitionWaitingByUrgency` only — `selectKeysWaiting` survives.
       - `compareWaiting` is module-private (`src/common/humble/viewFilters.ts:27`, declared
         `function compareWaiting`, no `export`). REQ-43-05's "the survivor" must therefore be
         EXPORTED before the unified list can sort with it.
       - `meta/i18nForkTouchedFiles.json` ALSO enrols the four deleted files (lines 115, 116,
         117, 119). `43-RESEARCH.md`'s assumption A3 ("no OTHER gate config also enrols these
         four paths") is FALSE. `meta/__tests__/genI18nGateScope.test.ts:338-389`'s
         `A-03 RATCHET` compares `forkTouched \ scope` against `DECLARED_UNSCANNED_DEBT`, and
         `:613` `A-17 ANTI-ROT` asserts the committed artifact equals the live git derivation —
         so REQ-43-22's scope is BOTH json artifacts, not one.
       - Neither `Waiting/__tests__/index.test.tsx` nor `All/__tests__/index.test.tsx` asserts
         the pinned "Expiring soon" section, `GROUP_ORDER`, or `defaultExpanded`/collapse
         behaviour (measured: `grep -n "expiringSoon\|Expiring soon\|humbleKeysPinnedSection\|
         pinned"` returns zero hits in the Waiting test; the All test's only `collapse` hits are
         in its own header prose). `43-RESEARCH.md`'s Deleted-Tab Salvage section predicts both.
         Both predictions are FALSE — those 22 assertions pin lifecycle behaviour that MUST
         survive, so REQ-43-12/-17's triage is "port forward", not "delete".

    3. Twenty-four `- [ ] **REQ-43-NN**: <text>` bullets, one per row of the RESEARCH table,
       preserving each row's requirement text and appending its `Source Decision` and
       `Verified By` columns as a trailing sentence. Keep REQ-43-24's `(CONDITIONAL)` marker and
       its "not shippable until the D-43-11 spike closes" clause verbatim.

    4. A `*Last updated: 2026-09-09 -- Phase 43 (REQ-43-01..24) minted during
       `/gsd-plan-phase 43` ...*` footer naming the four corrections above.

    Then append 24 rows to the `## Traceability` table (which begins at line 261) in the existing
    `| REQ-43-NN | Phase 43 | Pending |` shape, inserted after the last existing row rather than
    interleaved.
  </action>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*REQ-43-' .planning/REQUIREMENTS.md` returns `24`.
    - `grep -c '^| REQ-43-[0-9][0-9] | Phase 43 |' .planning/REQUIREMENTS.md` returns `24`.
    - For each N in 01..24, `grep -c "REQ-43-${N}" .planning/REQUIREMENTS.md` returns a value
      greater than or equal to `2` (one definition bullet + one traceability row).
    - `grep -n 'REQ-43-24' .planning/REQUIREMENTS.md` output includes the substring `CONDITIONAL`.
    - `grep -c 'selectKeysWaiting' .planning/REQUIREMENTS.md` returns a value >= 1, and the
      matching line names `StoreSearch` and `Discounts`.
    - `grep -c 'i18nForkTouchedFiles' .planning/REQUIREMENTS.md` returns a value >= 1.
    - `python3 -c "import re,sys; s=open('.planning/REQUIREMENTS.md').read(); print(len(set(re.findall(r'REQ-43-\d\d', s))))"` prints `24`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^- \[ \] \*\*REQ-43-' .planning/REQUIREMENTS.md)" = "24" &amp;&amp; test "$(grep -c '^| REQ-43-[0-9][0-9] | Phase 43 |' .planning/REQUIREMENTS.md)" = "24" &amp;&amp; echo REQUIREMENTS_OK</automated>
  </verify>
  <done>REQUIREMENTS.md defines and traces exactly 24 Phase 43 requirements, and records the four measured planning-time corrections.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ROADMAP.md's Phase 43 `TBD` requirements line</name>
  <read_first>
    - `.planning/ROADMAP.md` lines 5089-5137 — the Phase 43 section. Line 5105 currently reads
      `**Requirements**: TBD (run /gsd-ui-phase 43, then /gsd-plan-phase 43)`.
    - `.planning/ROADMAP.md` — the Phase 42 section immediately above, for the convention this
      line must match (`**Requirements**: REQ-42-01 … REQ-42-08 (see ...)`).
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Make exactly two surgical `Edit` replacements in the Phase 43 section, each anchored on a
    unique existing string:

    1. Replace the `**Requirements**: TBD (run /gsd-ui-phase 43, then /gsd-plan-phase 43)` line
       with `**Requirements**: REQ-43-01 … REQ-43-24 (see .planning/REQUIREMENTS.md § "Phase 43
       Requirements")`.

    2. Replace the `**Plans:** 0 plans` line and the `Plans:` / `- [ ] TBD (run /gsd-ui-phase 43,
       then /gsd-plan-phase 43 to break down)` stub beneath it with `**Plans:** 10 plans` and a
       ten-entry checklist naming each plan file and its one-line objective, matching the Phase 42
       list's `- [ ] 43-NN — <objective> (wave N)` shape.

    Additionally append a short `**Deliberate deviations from this section's own goal text:**`
    paragraph inside the Phase 43 section recording that the roadmap goal names a
    `Most recent`/`Alphabetical` sort picker and a `Hide redeemed keys` checkbox, and that
    D-43-06/D-43-08 supersede both (default is `Expiring soonest`; the checkbox is
    `Redeemable keys only`, polarity inverted). Leave the original goal prose in place — do not
    rewrite it. An auditable superseding note is the record; a silent rewrite is not.

    Do NOT touch any other phase's section, and do NOT touch `.planning/STATE.md`.
  </action>
  <acceptance_criteria>
    - `grep -n 'Requirements.*TBD' .planning/ROADMAP.md` returns zero lines containing `43`.
    - `grep -c 'REQ-43-01 … REQ-43-24' .planning/ROADMAP.md` returns `1`.
    - `grep -c '^- \[ \] 43-' .planning/ROADMAP.md` returns `10`.
    - `grep -c 'Redeemable keys only' .planning/ROADMAP.md` returns a value >= 1.
    - `git diff --stat .planning/ROADMAP.md` shows deletions fewer than 20 lines — a large
      deletion count means an unintended rewrite and must be reverted before proceeding.
    - `git diff --name-only` does NOT list `.planning/STATE.md`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^- \[ \] 43-' .planning/ROADMAP.md)" = "10" &amp;&amp; test -z "$(grep -n 'Requirements.*TBD' .planning/ROADMAP.md | grep 43)" &amp;&amp; echo ROADMAP_OK</automated>
  </verify>
  <done>ROADMAP.md's Phase 43 section names the real requirement range and the ten plans, with the two deliberate deviations from its own goal text recorded rather than hidden.</done>
</task>

<task type="auto">
  <name>Task 3: Fill 43-VALIDATION.md's Per-Task Verification Map with real task IDs</name>
  <read_first>
    - `.planning/phases/43-.../43-VALIDATION.md` — the whole file. Its `## Per-Task Verification
      Map` is seeded with `TBD` in the Task ID, Plan and Wave columns; its frontmatter carries
      `nyquist_compliant: false` and `wave_0_complete: false`.
    - All ten `43-NN-PLAN.md` files in this directory — the map's Task ID column must cite real
      task numbers (`43-04 T2`, `43-06 T1`, …), so read each plan's `<tasks>` before filling a row.
  </read_first>
  <files>.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-VALIDATION.md</files>
  <action>
    Replace every `TBD` in the Per-Task Verification Map's Task ID / Plan / Wave columns with the
    real plan and task identifiers from this plan set. Add rows so that all 24 requirements appear
    at least once; the seeded table covers only 8 of them.

    Correct three commands the seeded table gets wrong, and say so in the table's own notes:

    - `npx jest --selectProjects Backend -t "compareWaiting"` matches ZERO tests today and exits 0
      (measured this session: `Tests: 4786 skipped, 4786 total`, exit 0). The same is true of
      `-t "Redeemable"` and `-t "search"`. Replace every `-t`-filtered command in the map with the
      positional test-path form plus an asserted count, e.g.
      `npx jest --selectProjects Backend src/backend/humble/__tests__/viewFilters.test.ts` and the
      expected `Tests: N passed` figure.
    - Record the measured pre-phase baselines as the map's reference point:
      `viewFilters.test.ts` 35 passed, `groupKeys.test.ts` 12 passed,
      `HumbleKeyRow/__tests__/index.test.tsx` 47 passed, `All/__tests__/index.test.tsx` 9 passed,
      `Waiting/__tests__/index.test.tsx` 13 passed, `HumbleClaimWizard/__tests__` 17 passed,
      `meta/__tests__/genI18nGateScope.test.ts` 26 passed / 1 skipped,
      `meta/__tests__/hardcodedStringGate.test.ts` 151 passed (73s).
    - Resolve the map's "the i18n gate's exact command name is unverified" gap: the gate is the
      `Meta` jest project's `meta/__tests__/hardcodedStringGate.test.ts` (reading
      `meta/i18nGateScope.json` via `meta/hardcodedStringGate.ts:1815`), and the scope-artifact
      ratchet is `meta/__tests__/genI18nGateScope.test.ts`. There is no separate CI script name.

    Update the `## Known Gaps in This Strategy` list: strike gap 2 (i18n gate command name — now
    verified) and gap 4 (`pnpm test` does exist: `package.json:42` `"test": "jest"`). Keep gaps
    1, 3 and 5, and add one new gap recording that `pnpm lint`'s two ceilings
    (`meta/lintScoped.cjs:49-50` `SRC_CEILING = 1123`, `TESTS_CEILING = 638`) carry ZERO padding,
    so this phase's new code must not add net warnings beyond whatever the deletions free.

    Set `nyquist_compliant: true` in the frontmatter ONLY if every one of the 24 requirements has
    a populated row with a real Task ID and a non-`TBD` command. If any row cannot be completed,
    leave it `false` and say which row blocked it.
  </action>
  <acceptance_criteria>
    - `grep -c 'TBD' .planning/phases/43-*/43-VALIDATION.md` returns `0`.
    - `python3 -c "import re;s=open([p for p in __import__('glob').glob('.planning/phases/43-*/43-VALIDATION.md')][0]).read();print(len(set(re.findall(r'REQ-43-\d\d',s))))"` prints `24`.
    - `grep -c ' -t "' .planning/phases/43-*/43-VALIDATION.md` returns `0` — no `-t`-filtered
      jest command survives in the map.
    - `grep -c 'nyquist_compliant: true' .planning/phases/43-*/43-VALIDATION.md` returns `1`.
    - `grep -c 'SRC_CEILING' .planning/phases/43-*/43-VALIDATION.md` returns a value >= 1.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'TBD' .planning/phases/43-*/43-VALIDATION.md)" = "0" &amp;&amp; test "$(grep -c ' -t "' .planning/phases/43-*/43-VALIDATION.md)" = "0" &amp;&amp; echo VALIDATION_OK</automated>
  </verify>
  <done>The Per-Task Verification Map names a real plan/task for all 24 requirements, every command asserts a test count rather than an exit code, and the three unverified claims in the seeded table are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → project records | This plan writes the contract every later plan is scored against; a wrong record silently mis-scores the whole phase |
| GSD tooling → `.planning/*.md` | `gsd-sdk state.*` / `roadmap.*` verbs are a measured corruption source in this repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | Humble `code`/`key` value reaching a log line (C5 isolation wall, `src/backend/humble/adapter.ts`) | mitigate | Not reachable from this plan — it writes only `.planning/*.md` and touches no `src/` file. The wall is enforced in plans 43-02, 43-03 and 43-09, which name it in their own registers. Recorded here so the C5 obligation is visible in every plan of the phase, not just the ones that touch the backend. |
| T-43-06 | Tampering | `.planning/ROADMAP.md` / `.planning/STATE.md` mass-deletion by an SDK write verb | mitigate | `<hard_constraints>` forbids every `gsd-sdk state.*` / `roadmap.*` verb; Task 2's acceptance criteria assert `git diff --stat` shows fewer than 20 deletions and that `STATE.md` is untouched |
| T-43-07 | Repudiation | Requirement text silently drifting from the locked decisions it claims to encode | mitigate | Requirement texts are adopted verbatim from `43-RESEARCH.md`'s table, and the four measured contradictions are recorded in-record rather than quietly applied |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | `43-RESEARCH.md` § Package Legitimacy Audit: this phase installs zero new packages of any kind. No install task exists anywhere in this plan set, so the Package Legitimacy Gate's own scope condition is not met. If any later plan adds an install, that plan must run the gate protocol first. |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` defines 24 requirements and traces 24 rows.
- `.planning/ROADMAP.md` Phase 43 carries the real ID range, 10 plan entries, and the recorded
  deviations from its own goal text.
- `43-VALIDATION.md` has zero `TBD`s and zero `-t`-filtered jest commands.
- `git diff --name-only` lists exactly three files; `.planning/STATE.md` is not among them.
</verification>

<success_criteria>
Every later plan in this set can cite a real `REQ-43-*` ID that resolves to a written,
PASS/FAIL-scoreable requirement, and the verification map names the task that closes each one.
</success_criteria>

<output>
Create `.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-01-SUMMARY.md` when done
</output>
