---
phase: 43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit
plan: 09
type: execute
wave: 4
depends_on:
  - 43-03
  - 43-06
files_modified:
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx
  - public/locales/en/gamelib.json
  - src/backend/humble/adapter.ts
  - src/backend/humble/library.ts
  - src/backend/humble/__tests__/adapter.test.ts
autonomous: true
requirements:
  - REQ-43-24

must_haves:
  truths:
    - "A `gog_keyless` row's KEY column offers exactly one claim action, and that action is the one the D-43-11 probe selected"
    - "The button's label names the destination the click actually reaches, not a destination the user might assume"
    - "No Humble key value or code is logged on any path this plan touches or adds"
  artifacts:
    - path: ".planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-PROBE-D-43-11.md"
      provides: "the SELECTED BRANCH this plan implements"
      contains: "SELECTED BRANCH"
    - path: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      provides: "the gog_keyless KEY destination"
      contains: "gog_keyless"
  key_links:
    - from: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      to: "the selected destination"
      via: "the scenario switch's gog_keyless arm"
      pattern: "gog_keyless"
---

<objective>
Ship the one `gog_keyless` claim path that plan 43-03's probe selected, and close REQ-43-24.

Purpose: a keyless GOG entitlement carries no code — Humble redeems straight to the linked GOG
account — which is why `gog_keyless` is deliberately absent from `REDEEM_URL_BUILDERS` and why
Phase 42's GOG deep link has no reachable user. Until now that left the row with a claim button
routed to a generic help page. This plan gives it the destination the probe evidenced.

Output: one implemented branch, with the two rejected branches recorded as rejected.

**The phase ships whichever branch was selected.** The UI-SPEC's CONDITIONAL table specifies
identical geometry for all three — same `20rem` KEY column, same button placement — differing only
in label, icon and click behaviour. There is no outcome in which this plan cannot execute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-PROBE-D-43-11.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-UI-SPEC.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-CONTEXT.md
</context>

<interfaces>
Measured against the repo during planning.

- `src/common/humble/keyTypePresentation.ts:89` — `gog_keyless` presents as
  `{ kind: 'branded', name: 'GOG', logo: 'gog' }`, and `:121-125` records the T-UIC-01 reason it is
  deliberately absent from `REDEEM_URL_BUILDERS`: a keyless entitlement has no code, so any URL
  built for it would be broken or would carry a secret it should not.
- `src/backend/humble/adapter.ts:614-620` — the branch a keyless success currently lands in:
  `success !== true || !parsed.data.key` logs `reveal did not succeed success=<v> keyPresent=<bool>
  errorMsgLength=<n>` and returns `{ status: 'schema_error', raw: undefined }`. Candidate A's
  implementation is a change to THIS classification, not a frontend label swap.
- `src/backend/humble/adapter.ts:74-75` — the file's own comment: `error_msg` may echo a key value
  and is never logged or forwarded verbatim. That discipline binds any new branch added here.
- `src/backend/humble/library.ts:1213-1250` — `doRevealKey`'s `UNREVEALED`-only gate, its C2
  `ownedElsewhere` block, and the cooldown gate. Any new keyless outcome must flow through these
  unchanged; they are the backend's sole-authority controls (D-69/D-70, T-14-03).
- `43-UI-SPEC.md` section "`gog_keyless` — CONDITIONAL on D-43-11's spike" — the three-column table
  giving the exact label, click behaviour and icon convention per branch. Do not re-derive it;
  implement the selected column.
- `.claude/skills/spike-findings-gamelib/references/tauri-embedded-store-browser.md` — required
  reading ONLY if candidate B was selected. The two costs it names: the embed composites ABOVE the
  main webview so the claim flow's modal needs the `hide()`-before-modal dance, and there is one
  default cookie jar per PROCESS so the embed inherits the Humble session the login webview
  established.
- `src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx` after plan 43-06 — the
  `resolveKeyScenario` switch with its `const _exhaustive: never` default, and the
  `humbleKeyGiftButton` chrome every KEY button reuses.
- `src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx:1-3` — `faExternalLinkAlt` is
  already imported and is the established external-link icon convention (used by the gift button).
- Measured baselines: `adapter.test.ts` — measure before editing; `HumbleKeyRow` frontend tests at
  least 70 passing after plan 43-06.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement the branch the probe selected</name>
  <read_first>
    - `.planning/phases/43-.../43-PROBE-D-43-11.md` — read the `**SELECTED BRANCH:**` line FIRST.
      Everything else in this task is conditional on it. If the file is missing or has no
      SELECTED BRANCH line, STOP and report — do not choose a branch yourself.
    - `43-UI-SPEC.md` section "`gog_keyless` — CONDITIONAL on D-43-11's spike" — the column matching
      the selected branch.
    - `src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx` — `resolveKeyScenario`
      and the KEY-cell render, after plan 43-06.
    - If and only if the branch is **candidate A**: `src/backend/humble/adapter.ts:555-660`
      (`revealKey` in full) and `src/backend/humble/library.ts:1213-1300` (`doRevealKey`'s gates and
      its post-reveal handling), plus `src/backend/humble/__tests__/adapter.test.ts` for its
      response-fixture and redaction-assertion conventions.
    - If and only if the branch is **candidate B**:
      `.claude/skills/spike-findings-gamelib/references/tauri-embedded-store-browser.md` in full,
      and `src/backend/sidecar/storeEmbedFlowRegistration.ts` plus the renderer's `storeEmbedSeam`
      call shape.
  </read_first>
  <files>
    src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx,
    public/locales/en/gamelib.json,
    src/backend/humble/adapter.ts,
    src/backend/humble/library.ts,
    src/backend/humble/__tests__/adapter.test.ts
  </files>
  <action>
    Implement exactly the branch named on the probe document's `**SELECTED BRANCH:**` line, and
    record in the summary which branch that was and which two were therefore not built.

    **If `candidate A`** — the reveal endpoint accepts a keyless entitlement:
    Add an explicit keyless-success outcome to `revealKey` in `adapter.ts`. Today `success === true`
    with no `key` falls into the `:614-620` shape-drift branch and returns `schema_error`, which
    would surface to the user as a failure after a redemption that actually happened. Introduce a
    distinct status — a keyless success — and return it instead, but ONLY when the caller's key_type
    is known to be keyless; a missing `key` on a normally key-bearing reveal must keep returning
    `schema_error`, because that case really is drift. Keep the log line's redaction shape exactly
    as it is: status, `keyPresent` boolean, `errorMsgLength`. Never the value, never `error_msg`
    verbatim. Thread the new outcome through `library.ts`'s reveal handling so the key advances
    state the same way a keyed success does, and so the claim wizard reports success rather than
    failure. Do not touch `doRevealKey`'s `UNREVEALED`-only gate, its C2 `ownedElsewhere` block, or
    the cooldown gate. In the row, `gog_keyless` takes the ordinary `'claim-and-gift'` /
    `'login-and-claim'` scenarios with the `claimOnStore` label ("Claim on GOG"), no external-link
    icon, and one in-place click — the same interaction shape as Steam's Activate. Mint no new i18n
    key.

    **If `candidate B`** — the Phase 40 embedded browser:
    Mint `humbleKeys.claimOnHumble` ("Claim on Humble") in `public/locales/en/gamelib.json` and use
    it for `gog_keyless` rows. It must NOT say "Claim on GOG": the click opens Humble's site, not
    GOG's, and D-43-12's honesty standard applies to this button identically. Wire the click to
    `storeEmbedOpen('https://www.humblebundle.com/home/keys', ...)`. Honour the two recorded costs:
    the embed composites above the main webview, so the claim flow's modal requires the
    `hide()`-before-modal dance, and the process-wide cookie jar means the embed inherits the
    login webview's Humble session — do not add a second login step. No external-link icon: the
    path stays inside the window. Make no backend change to `revealKey`.

    **If `external browser`:**
    Mint `humbleKeys.claimOnHumble` as above and use the same label, plus the existing
    `faExternalLinkAlt` icon convention, because the path leaves the app entirely. Wire the click to
    the repo's existing external-open path. Make no backend change to `revealKey`.

    In every branch: `gog_keyless`'s geometry is unchanged — same `20rem` KEY column, same
    full-width or paired button placement plan 43-05 and 43-06 already built. Only the label, the
    icon and the click behaviour differ. Do not add a `gog_keyless` entry to `REDEEM_URL_BUILDERS`
    under any branch; T-UIC-01's reasoning is unchanged by the probe.
  </action>
  <acceptance_criteria>
    - The summary names the implemented branch verbatim from the probe document, and names the two
      not built.
    - `grep -c 'gog_keyless' src/common/humble/keyTypePresentation.ts` shows it is still absent from
      the `REDEEM_URL_BUILDERS` object:
      `sed -n '/^const REDEEM_URL_BUILDERS/,/^}/p' src/common/humble/keyTypePresentation.ts | grep -c 'gog_keyless'`
      returns `0` in every branch.
    - Branch-conditional label assertion, exactly one of:
      candidate A — `python3 -c "import json;print('claimOnHumble' in json.load(open('public/locales/en/gamelib.json'))['humbleKeys'])"` prints `False`;
      candidate B or external — it prints `True` and the value is `"Claim on Humble"`, and
      `grep -c 'Claim on GOG' src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx`
      returns `0`.
    - Candidate A only: `grep -c 'parsed.data.key' src/backend/humble/adapter.ts` shows the new
      branch reads only the key's PRESENCE, and
      `sed -n '/export async function revealKey/,/^}/p' src/backend/humble/adapter.ts | grep -c 'parsed.data.key}\|${parsed.data.key\|error_msg}'`
      returns `0` — no key value and no `error_msg` value is interpolated into any string.
    - Candidate A only: `npx jest --selectProjects Backend src/backend/humble/__tests__/adapter.test.ts`
      reports a test count strictly greater than the pre-task baseline, zero failures, and includes
      a named test asserting that a `success: true, key: null` response for a keyless key_type
      returns the keyless-success status while the same response for a keyed key_type still returns
      `schema_error`.
    - `git diff --name-only public/locales/` lists at most `public/locales/en/gamelib.json`.
    - `npx tsc --noEmit` completes with zero errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test "$(sed -n '/^const REDEEM_URL_BUILDERS/,/^}/p' src/common/humble/keyTypePresentation.ts | grep -c 'gog_keyless')" = "0" &amp;&amp; echo BRANCH_OK</automated>
  </verify>
  <done>The selected `gog_keyless` claim path ships, its label names the destination the click actually reaches, and no key value crossed into a log line or a URL.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the gog_keyless destination and its honesty constraint</name>
  <read_first>
    - `src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx` after plan
      43-06 — reuse `collectElements`, `findByClassNamePart`, `textContent` and the single i18n mock.
    - `.planning/phases/43-.../43-PROBE-D-43-11.md` — the SELECTED BRANCH and its recorded residual
      unknowns, one of which must become a comment in the test file rather than being dropped.
    - `src/backend/humble/__tests__/adapter.test.ts` (candidate A only) for its redaction-assertion
      convention.
  </read_first>
  <files>src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx</files>
  <action>
    Add a `describe('gog_keyless KEY destination (REQ-43-24, D-43-11)')` block asserting, for a
    `platform: 'gog_keyless'` key in a live state:
    - exactly one claim affordance renders in the KEY cell — not two, and not zero;
    - its text matches the selected branch's label exactly ("Claim on GOG" for candidate A,
      "Claim on Humble" for candidate B and external browser);
    - for candidate B and external browser, the label does NOT contain "GOG" — the click does not
      reach GOG's site and the label must not imply it does;
    - for external browser only, the `faExternalLinkAlt` icon element is present; for candidates A
      and B, it is absent, because those paths stay inside the app;
    - the TYPE cell still renders the GOG branded logo (`gog_keyless` presents as branded), so the
      destination choice did not leak into the platform identity;
    - the row's geometry is unchanged: it still has exactly three grid children with the same class
      names, and the REQ-43-15 TYPE/GAME zero-interactivity gate still passes for this platform.

    Record in a comment at the top of the block, carried from the probe document, the residual
    unknown the probe could not settle: behaviour when the GOG-to-Humble account link is ABSENT is
    unmeasured, because there is no second, unlinked test account. State it as a known limit of this
    coverage rather than letting a green block imply the case was tested.
  </action>
  <acceptance_criteria>
    - `npx jest --selectProjects Frontend src/frontend/screens/Humble/Keys/components/HumbleKeyRow`
      reports `Tests: N passed, N total` with `N` at least `75` and zero failures. Record N.
    - Mutation proof, recorded verbatim: temporarily change the `gog_keyless` arm's label to the
      other branch's string, re-run, confirm the label test FAILS by name; revert and confirm
      `git diff --quiet` on the source file plus a green re-run.
    - `grep -c 'account link' src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx`
      returns at least `1` — the residual unknown is recorded in the test file, not only in the
      probe document.
    - `npx jest --selectProjects Meta meta/__tests__/hardcodedStringGate.test.ts` reports
      `Tests: 151 passed` and zero failures. Allow about 74 seconds; a timeout is not a pass.
    - `node meta/lintScoped.cjs` passes both scopes.
  </acceptance_criteria>
  <verify>
    <automated>npx jest --selectProjects Frontend src/frontend/screens/Humble/Keys/components/HumbleKeyRow 2>&amp;1 | grep -E '^Tests:'</automated>
  </verify>
  <done>The shipped destination is pinned by a mutation-proven label test, the platform identity is unaffected, and the probe's one unmeasurable case is recorded where a future reader of the tests will see it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameLib to Humble `POST /humbler/redeemkey` | An irreversible third-party call whose response body may carry a key value |
| button label to user expectation | A label naming the wrong destination is a UI lie the user acts on |
| embedded webview to process cookie jar (candidate B only) | The embed inherits the login webview's live Humble session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | **The C5 isolation wall — a Humble `code` is a secret that must never reach a logger.** Candidate A adds a new branch inside `revealKey`, the ONE function in the codebase whose response body carries a key value | mitigate | The new branch reads `parsed.data.key`'s PRESENCE only and reuses the existing log shape (status, `keyPresent` boolean, `errorMsgLength`) established at `adapter.ts:610-620`. `error_msg` is never logged or forwarded verbatim — the file's own comment at `:74-75` records that it may echo a key value. Task 1's acceptance criteria grep the whole `revealKey` body for any interpolation of `parsed.data.key` or `error_msg` and require `0`. |
| T-43-19 | Spoofing | A button labelled "Claim on GOG" that actually opens Humble's site | mitigate | Task 1 forbids the GOG label for candidates B and external browser; Task 2 asserts the label does not contain "GOG" in those branches, and asserts the external-link icon appears only for the branch that genuinely leaves the app |
| T-43-28 | Elevation of Privilege | Bypassing the backend's sole-authority reveal gates while adding a keyless outcome | mitigate | Task 1 forbids touching `doRevealKey`'s `UNREVEALED`-only gate, its C2 `ownedElsewhere` block and its cooldown gate; the new outcome is a classification change downstream of all three |
| T-43-29 | Tampering | A fabricated redeem URL for a keyless entitlement | mitigate | `gog_keyless` stays absent from `REDEEM_URL_BUILDERS` in every branch — asserted by a sed-scoped grep returning `0` — because a keyless entitlement has no code and any URL built for it would be broken or would carry a secret (T-UIC-01, unchanged by the probe) |
| T-43-30 | Information Disclosure | Candidate B's embedded browser inheriting the Humble session into a surface with no allowlist | accept | The embed opens one hard-coded Humble URL, and the process-wide shared cookie jar is the documented Phase 40 behaviour that makes the flow work at all. This is the same accepted cost REQ-40-04 recorded. No new origin is introduced. |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (`43-RESEARCH.md` section Package Legitimacy Audit). Every mechanism in all three branches already ships. No install task exists in this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `HumbleKeyRow` frontend tests report at least 75 passing, zero failures.
- Candidate A only: `adapter.test.ts` gains a named keyless-vs-keyed discrimination test and reports
  zero failures.
- The mutation proof is recorded verbatim, in both directions.
- `gog_keyless` is absent from `REDEEM_URL_BUILDERS`.
- `meta/__tests__/hardcodedStringGate.test.ts` reports 151 passing.
</verification>

<success_criteria>
A `gog_keyless` row offers one claim action whose label names where the click actually goes, backed
by the probe's evidence rather than an assumption, with the two rejected branches recorded as
rejected.
</success_criteria>

<output>
Create `.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-09-SUMMARY.md` when done
</output>
