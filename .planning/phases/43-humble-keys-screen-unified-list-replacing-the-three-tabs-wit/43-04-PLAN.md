---
phase: 43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/humble/genericKeyPlatform.ts
  - src/common/humble/viewFilters.ts
  - src/common/humble/keyTypePresentation.ts
  - src/backend/humble/__tests__/viewFilters.test.ts
autonomous: true
requirements:
  - REQ-43-05
  - REQ-43-07
  - REQ-43-09
  - REQ-43-18
  - REQ-43-21

must_haves:
  truths:
    - "`GENERIC_KEY_PLATFORM` resolves from a leaf module that does not import `groupKeys.ts`, and both surviving importers point at it"
    - "The unified list's sort comparator is an exported function whose undated-key tiebreak is pinned by a test that fails against the other comparator"
    - "A title-only search predicate exists in `src/common/humble/`, unit-tested, and a query matching only `origin` returns no match"
    - "`pnpm codecheck` passes with `groupKeys.ts` still present — proving the relocation landed before any deletion"
  artifacts:
    - path: "src/common/humble/genericKeyPlatform.ts"
      provides: "GENERIC_KEY_PLATFORM as a leaf module with no groupKeys dependency"
      exports: ["GENERIC_KEY_PLATFORM"]
      min_lines: 10
    - path: "src/common/humble/viewFilters.ts"
      provides: "exported compareWaiting, matchesKeySearch, isGiftableSpare; repointed GENERIC_KEY_PLATFORM import"
      exports: ["WAITING_STATES", "compareWaiting", "matchesKeySearch", "isGiftableSpare", "selectKeysWaiting"]
    - path: "src/backend/humble/__tests__/viewFilters.test.ts"
      provides: "the comparator-differentiating tiebreak case, the state-only field-independence case, and the title-only search cases"
      contains: "compareWaiting"
  key_links:
    - from: "src/common/humble/keyTypePresentation.ts"
      to: "src/common/humble/genericKeyPlatform.ts"
      via: "import"
      pattern: "from './genericKeyPlatform'"
    - from: "src/common/humble/viewFilters.ts"
      to: "src/common/humble/genericKeyPlatform.ts"
      via: "import"
      pattern: "from './genericKeyPlatform'"
---

<objective>
Land every pure-module change the unified list needs, and land the `GENERIC_KEY_PLATFORM`
relocation that must precede `groupKeys.ts`'s deletion.

Purpose: `groupKeys.ts` is deleted later in this phase (D-43-20), but it exports one constant that
two surviving modules import. Deleting it first breaks the build. This plan removes that edge and
adds the three pure predicates the screen will consume, all unit-tested from the Backend jest
project per the repo's tier convention.

Output: a leaf constant module, three new exported predicates, and tests that pin the exact
semantics the later plans depend on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-CONTEXT.md
@.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-PATTERNS.md
</context>

<interfaces>
Measured against the repo during planning. Two of these contradict `43-RESEARCH.md` and
`43-PATTERNS.md`; the repo wins.

- `src/common/humble/groupKeys.ts:18-19` — the exact two lines to move:
  a `/** ... */` doc comment plus `export const GENERIC_KEY_PLATFORM = 'generic'`.
- `grep -rn "GENERIC_KEY_PLATFORM" src` — production importers are exactly
  `src/common/humble/viewFilters.ts:2` and `src/common/humble/keyTypePresentation.ts:1`, both
  `import { GENERIC_KEY_PLATFORM } from './groupKeys'`. The only other hit is
  `src/backend/humble/__tests__/groupKeys.test.ts`, deleted with its subject in plan 43-08.
- `src/common/humble/viewFilters.ts:27` declares `function compareWaiting(a, b): number` —
  **module-private, no `export`.** `43-RESEARCH.md` and `43-VALIDATION.md` both assume it is
  callable from outside. It is not, yet.
- `src/common/humble/viewFilters.ts:27-34` (`compareWaiting`) vs
  `src/common/humble/groupKeys.ts:40-46` (`byExpiringSoonest`) differ ONLY in the two-undated case:
  `compareWaiting` returns `a.title.localeCompare(b.title)`; `byExpiringSoonest` returns `0`.
  A test that only checks "dated sorts before undated" passes against both.
- **`selectKeysWaiting` MUST NOT be deleted.** It has three surviving production/test consumers
  outside the Humble Keys screen: `src/frontend/screens/StoreSearch/index.tsx:8,39`,
  `src/frontend/screens/Discounts/index.tsx:16,84`, `src/backend/humble/__tests__/library.test.ts:2147`
  and `src/backend/discounts/__tests__/badges.test.ts:13`. `43-RESEARCH.md`'s Execution Sequencing
  Risk point 5 and `43-PATTERNS.md`'s viewFilters assignment both state its only call sites are the
  deleted tab files. Both are wrong.
- `src/common/humble/viewFilters.ts:17-21` — `WAITING_STATES` is
  `new Set(['UNPICKED','UNREVEALED','REVEALED'])`. D-43-08's predicate is
  `WAITING_STATES.has(key.state)` used directly. **Do not add a wrapper predicate for it** —
  D-43-08 says zero new predicate, explicitly.
- Test placement: `src/common/humble/*` modules are tested from the **Backend** jest project —
  `viewFilters.test.ts` and `groupKeys.test.ts` live under `src/backend/humble/__tests__/`, not
  under `src/common`. A `Common` jest project exists (`displayName: 'Common'`,
  `roots: ['<rootDir>/src/common']`) but is not where this tier's tests live. Placing a test under
  `src/common/humble/__tests__/` would collect it in the wrong project.
- Measured pre-plan baseline: `npx jest --selectProjects Backend src/backend/humble/__tests__/viewFilters.test.ts`
  reports `Tests: 35 passed, 35 total`.
- `43-CONTEXT.md` D-43-10: search matches `title` only. 20 of 33 live keys carry the junk gift
  string "A very special gift just for you" in `origin`, which names no game.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the genericKeyPlatform leaf module and repoint both importers</name>
  <read_first>
    - `src/common/humble/groupKeys.ts` — read lines 1-40 for the constant, its doc comment, and the
      module docblock's tier statement.
    - `src/common/humble/urgencyBadge.ts` and `src/common/humble/expirationDisplay.ts` — the two
      single-purpose sibling leaf modules whose docblock shape the new file must copy: (1) pure, no
      React/i18n/I/O, (2) unit-testable from the backend jest project, (3) names its consumers.
    - `src/common/humble/viewFilters.ts` line 2 and `src/common/humble/keyTypePresentation.ts`
      line 1 — the two import statements being repointed.
    - `src/common/humble/keyTypePresentation.ts:66-98` — the `KEY_TYPE_PRESENTATIONS` table where
      `[GENERIC_KEY_PLATFORM]: { kind: 'unknown' }` is the computed key, so the constant's new
      path must resolve at module scope, not at render time.
  </read_first>
  <files>src/common/humble/genericKeyPlatform.ts, src/common/humble/viewFilters.ts, src/common/humble/keyTypePresentation.ts</files>
  <action>
    Create `src/common/humble/genericKeyPlatform.ts` exporting `GENERIC_KEY_PLATFORM = 'generic'`.
    Give it the three-part module docblock the sibling leaf modules use, naming `viewFilters.ts` and
    `keyTypePresentation.ts` as its consumers and citing D-43-20 as the reason it exists as its own
    module (WR-09's precedent — `stateLabels.ts` was extracted for the same reason after a circular
    import).

    Rewrite the constant's own doc comment. The current text says the value "routes an entry into
    the Other group", and D-43-01 retires the Other group entirely. The new comment must describe
    the constant's surviving role: it identifies the `key_type`/platform value that resolves to
    `keyTypePresentation.ts`'s neutral `{ kind: 'unknown' }` TYPE-column branch, and it is the
    value `selectKeysWaiting` excludes from the Keys-waiting set. This is the one line in the move
    that is not a pure copy.

    Repoint `viewFilters.ts:2` and `keyTypePresentation.ts:1` from `'./groupKeys'` to
    `'./genericKeyPlatform'`.

    Leave `groupKeys.ts` in place, still exporting the constant, for now — plan 43-08 deletes it.
    A same-task delete would put the deletion before the codecheck that proves the repoint worked.
    Do NOT create a test file for this constant; `keyTypePresentation`'s existing unknown-branch
    assertions already cover its value indirectly, and a `expect(X).toBe('generic')` test pins
    nothing worth pinning.
  </action>
  <acceptance_criteria>
    - `grep -c "from './groupKeys'" src/common/humble/viewFilters.ts src/common/humble/keyTypePresentation.ts`
      returns `0` for both files.
    - `grep -c "from './genericKeyPlatform'" src/common/humble/viewFilters.ts src/common/humble/keyTypePresentation.ts`
      returns `1` for each file.
    - `grep -v '^\s*\*' src/common/humble/genericKeyPlatform.ts | grep -c "Other group"` returns `0`
      — the retired grouping language is gone from live code AND the rewritten comment.
    - `grep -c 'import' src/common/humble/genericKeyPlatform.ts` returns `0` — it is a leaf.
    - `npx tsc --noEmit` completes with zero errors while `src/common/humble/groupKeys.ts` still
      exists on disk (`test -f src/common/humble/groupKeys.ts` succeeds).
    - `npx jest --selectProjects Backend src/backend/humble/__tests__/keyTypePresentation.test.ts src/backend/humble/__tests__/groupKeys.test.ts`
      reports the same counts as before this task (`groupKeys.test.ts` baseline: 12 passed).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test -f src/common/humble/groupKeys.ts &amp;&amp; test "$(grep -c "from './groupKeys'" src/common/humble/viewFilters.ts src/common/humble/keyTypePresentation.ts | grep -cv ':0')" = "0" &amp;&amp; echo RELOCATION_OK</automated>
  </verify>
  <done>Both surviving importers read the constant from a leaf module, `groupKeys.ts` is now import-free from the surviving tier, and the build proves it before anything is deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Export compareWaiting and add matchesKeySearch and isGiftableSpare</name>
  <read_first>
    - `src/common/humble/viewFilters.ts` — the whole file (105 lines). Note especially the module
      docblock's tier statement, `WAITING_STATES` at :17-21, the private `compareWaiting` at
      :27-34, `selectKeysWaiting` at :59-68, `selectGiftableSpares` at :75-77 and
      `partitionWaitingByUrgency` at :91-105.
    - `src/common/humble/groupKeys.ts:38-46` — `byExpiringSoonest`, so the two comparators'
      divergence can be stated precisely in `compareWaiting`'s new doc comment.
    - `src/common/types/humble.ts:80-126` — `HumbleKeyState` and the `HumbleKey` field list, to
      confirm `title` is the only searchable display field and that no date field exists.
  </read_first>
  <files>src/common/humble/viewFilters.ts</files>
  <action>
    Three changes, all inside `viewFilters.ts`:

    1. Add `export` to `compareWaiting` (currently `function compareWaiting` at :27). Extend its
       doc comment to state that D-43-06 makes it the unified list's default `Expiring soonest`
       comparator, that `groupKeys.ts`'s `byExpiringSoonest` is deleted in plan 43-08 rather than
       kept as a second implementation, and that the two differ ONLY in the two-undated case
       (alphabetical vs. insertion order) — so this comparator's tiebreak is load-bearing, not
       incidental.

    2. Add `export function matchesKeySearch(key: HumbleKey, query: string): boolean`, implementing
       D-43-10: a case-insensitive substring match against `key.title` and nothing else. An empty
       or whitespace-only query matches everything. Its doc comment must cite D-43-10 and record
       WHY `origin` is excluded — 20 of 33 live keys carry the gift string "A very special gift
       just for you" in that field, which names no game, so matching it would produce hits a user
       cannot explain. Do not lowercase or trim `key.title` destructively; compare copies.

    3. Add `export function isGiftableSpare(key: HumbleKey): boolean` returning
       `key.ownedElsewhere && key.state === 'UNREVEALED'` — the per-row form of scenario 3's
       trigger. Its doc comment must cite D-54/D-55 (owned + REVEALED keys are deliberately
       excluded because reveal forfeits the gift link) and note it supersedes the array-returning
       `selectGiftableSpares`, which plan 43-08 deletes once its last caller is gone.

    Do NOT add a wrapper around `WAITING_STATES.has(...)` — D-43-08 specifies zero new predicate
    for the checkbox and the set is already exported and unit-tested.

    Do NOT delete `selectKeysWaiting`, `selectGiftableSpares` or `partitionWaitingByUrgency` in
    this task. `selectKeysWaiting` has three surviving consumers outside this screen and must never
    be deleted at all; the other two are plan 43-08's business, after their call sites are gone.
  </action>
  <acceptance_criteria>
    - `grep -c '^export function compareWaiting' src/common/humble/viewFilters.ts` returns `1`.
    - `grep -c '^export function matchesKeySearch' src/common/humble/viewFilters.ts` returns `1`.
    - `grep -c '^export function isGiftableSpare' src/common/humble/viewFilters.ts` returns `1`.
    - `grep -c 'export function selectKeysWaiting' src/common/humble/viewFilters.ts` returns `1` —
      it still exists.
    - `grep -c 'origin' src/common/humble/viewFilters.ts` returns a value >= 1 and the matching
      comment explains why `origin` is not searched.
    - `matchesKeySearch` contains no reference to `key.origin`, `key.platform`, `key.state` or
      `key.machineName`: `sed -n '/export function matchesKeySearch/,/^}/p' src/common/humble/viewFilters.ts | grep -c 'key\.\(origin\|platform\|state\|machineName\)'`
      returns `0`.
    - `npx tsc --noEmit` completes with zero errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test "$(grep -c '^export function \(compareWaiting\|matchesKeySearch\|isGiftableSpare\)' src/common/humble/viewFilters.ts)" = "3" &amp;&amp; test "$(sed -n '/export function matchesKeySearch/,/^}/p' src/common/humble/viewFilters.ts | grep -c 'key\.\(origin\|platform\|state\|machineName\)')" = "0" &amp;&amp; echo PREDICATES_OK</automated>
  </verify>
  <done>The unified list's comparator is callable, and the two new per-row predicates exist in the pure tier with their reasons recorded inline.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the three new semantics with differentiating unit tests</name>
  <read_first>
    - `src/backend/humble/__tests__/viewFilters.test.ts` — the whole file (293 lines, 35 tests).
      Reuse its existing `makeKey` factory and `describe` layout; do not introduce a second key
      factory or a second import style.
    - `src/common/humble/viewFilters.ts` after Task 2, for the three exports' exact signatures.
    - `src/common/humble/groupKeys.ts:38-46` — `byExpiringSoonest`, whose two-undated return of `0`
      is what the differentiating test must rule out.
  </read_first>
  <files>src/backend/humble/__tests__/viewFilters.test.ts</files>
  <action>
    Add three `describe` blocks to the existing file, importing the three new exports alongside the
    existing ones.

    `describe('compareWaiting — the surviving comparator (REQ-43-05)')`: the load-bearing case is
    TWO UNDATED keys supplied in non-alphabetical order, asserting the result is ordered
    alphabetically by title. State in a comment that this is the ONE case that distinguishes
    `compareWaiting` from the deleted `byExpiringSoonest`, which returns `0` there and would leave
    Humble's insertion order — a silent, cosmetic regression nobody would catch in review. Also
    pin the dated-before-undated rule and the soonest-first rule so the block reads as a complete
    contract rather than one edge case.

    `describe('WAITING_STATES as the Redeemable-keys-only predicate (REQ-43-07)')`: a
    parametrised field-independence test. For each of the five `HumbleKeyState` values, build the
    same key twice — once with `ownedElsewhere: false, platform: 'steam', matchConfidence: 'none'`
    and once with `ownedElsewhere: true, platform: GENERIC_KEY_PLATFORM, matchConfidence: 'fuzzy'`
    — and assert `WAITING_STATES.has(key.state)` returns the identical boolean for both. Assert
    explicitly that UNPICKED is IN the set (D-43-08 keeps it deliberately) and that REDEEMED and
    UNREDEEMABLE are OUT.

    `describe('matchesKeySearch — title only (REQ-43-09, REQ-43-21)')`: a key whose `title` is
    `'Some Game'` and whose `origin` is `'A very special gift just for you'`. Assert a query of
    `'special gift'` returns `false` (matches only `origin`), a query of `'some ga'` returns `true`
    (case-insensitive title match), a query of `'SOME GAME'` returns `true`, and an empty query
    and a whitespace-only query both return `true`. Add one case asserting the AND combination
    REQ-43-21 requires: a REDEEMED key whose title matches is excluded once
    `WAITING_STATES.has(...)` is also required, expressed as the composed boolean the screen will
    use.

    `describe('isGiftableSpare (scenario 3)')`: assert `true` only for `ownedElsewhere: true` AND
    `state: 'UNREVEALED'`; assert `false` for owned+REVEALED, for unowned+UNREVEALED, and for
    owned+REDEEMED.

    Do not delete or weaken any of the 35 existing tests in this file. Plan 43-08 prunes the ones
    whose subjects it deletes.
  </action>
  <acceptance_criteria>
    - `npx jest --selectProjects Backend src/backend/humble/__tests__/viewFilters.test.ts` reports
      `Tests: N passed, N total` with `N` strictly greater than the measured baseline of `35`, and
      zero failures. Record the new N in the summary. (Asserting the count, not the exit code:
      `--selectProjects` exits 0 on a zero-test run, and `-t` patterns are regexes that silently
      match nothing — measured this session, `-t "viewFilters"` selected 0 of 4786 tests and
      exited 0.)
    - Mutation proof for the comparator test, recorded verbatim in the summary: temporarily replace
      `compareWaiting`'s final `return a.title.localeCompare(b.title)` with `return 0` (which is
      exactly `byExpiringSoonest`'s behaviour), re-run the file, and confirm the two-undated test
      FAILS by name. Restore the line and confirm the file is byte-identical
      (`git diff --quiet src/common/humble/viewFilters.ts`) and the suite passes again.
    - `grep -c 'matchesKeySearch' src/backend/humble/__tests__/viewFilters.test.ts` returns a
      value >= 5.
    - `npx tsc --noEmit` completes with zero errors.
    - `node meta/lintScoped.cjs --tests` passes — this task adds test-scope code and the tests
      ceiling (`meta/lintScoped.cjs:50`, `TESTS_CEILING = 638`) carries zero padding.
  </acceptance_criteria>
  <verify>
    <automated>npx jest --selectProjects Backend src/backend/humble/__tests__/viewFilters.test.ts 2>&amp;1 | grep -E '^Tests:' | grep -q 'failed' &amp;&amp; exit 1; npx jest --selectProjects Backend src/backend/humble/__tests__/viewFilters.test.ts 2>&amp;1 | grep -E '^Tests:'</automated>
  </verify>
  <done>The comparator's tiebreak, the checkbox predicate's state-only nature, the title-only search and the giftable-spare predicate are each pinned by a test that fails when the behaviour changes — proven by mutation, not asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-typed search query → `Array.filter` | Untrusted display-layer input crosses into a pure predicate |
| `key_type` string from Humble → presentation/redeem lookup | Untrusted remote string selects a UI branch and, elsewhere, a URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | The C5 isolation wall — a Humble `code` must never reach a logger | mitigate | Not reachable here: `src/common/humble/*` modules are no-I/O and no-logging by tier convention (stated in each module's own docblock), and none of the three new predicates touches a key value, a code, or a logger. Task 2's acceptance criteria assert `matchesKeySearch` reads `key.title` alone. Named rather than omitted because this plan edits the module tier that the reveal path's callers also import. |
| T-43-03 | Tampering | A fabricated store URL built from an untrusted `key_type` string | accept | Unchanged by this plan. `keyTypePresentation.ts`'s `REDEEM_URL_BUILDERS` stays a closed two-entry set (`:126-132`); this plan changes only that file's import line. The existing control remains correct and is the pattern any new redeem-target logic must follow. |
| T-43-13 | Denial of Service | `matchesKeySearch` running per-key per-keystroke over an unbounded query | accept | The list is the user's own Humble library (33 keys live). A substring compare over tens of items per keystroke is not a meaningful cost, and there is no regex construction from user input — `includes`, not `RegExp` — so no catastrophic-backtracking surface exists. |
| T-43-14 | Tampering | Deleting `selectKeysWaiting` and silently breaking StoreSearch and Discounts | mitigate | `<interfaces>` names all four surviving consumers with file:line, Task 2 forbids the deletion explicitly, and its acceptance criteria assert the export still exists |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (`43-RESEARCH.md` § Package Legitimacy Audit). No install task exists in this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with `groupKeys.ts` still on disk.
- `npx jest --selectProjects Backend src/backend/humble/__tests__/viewFilters.test.ts` reports more
  than 35 passing tests and zero failures.
- The comparator mutation proof is recorded verbatim in the summary, both directions.
- `grep -rn "from './groupKeys'" src/common/` returns nothing.
</verification>

<success_criteria>
`groupKeys.ts` can be deleted in plan 43-08 without breaking a single surviving importer, and the
screen has an exported comparator plus two tested predicates to build on.
</success_criteria>

<output>
Create `.planning/phases/43-humble-keys-screen-unified-list-replacing-the-three-tabs-wit/43-04-SUMMARY.md` when done
</output>
