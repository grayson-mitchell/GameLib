---
phase: 32-tauri-ipc-re-plumb-slice-3-downloads-and-queue
plan: 03
type: execute
wave: 3
depends_on: ["32-01", "32-02"]
files_modified:
  - .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md
  - .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
autonomous: true
requirements: [REQ-32-06, REQ-32-07]
must_haves:
  truths:
    - "32-PORTED-CHANNELS.md declares every ported channel: the five queue channels, progressUpdate, changedDMQueueInformation, and the install/updateGame addToQueue re-route"
    - "The D-05 disabled boot auto-resume and the D-04 pause=abort-then-reconciled-restart boundary are declared, not left implied"
    - "32-HUMAN-UAT.md logs one deferred-UAT item naming BOTH G-30-01 and G-30-02 as the live-E2E blockers"
    - "The queue-channel and byte-progress rows move out of SEAM.md §3 (deferred) into §1 (ported)"
  artifacts:
    - path: ".planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md"
      provides: "The declared ported-channel list mirroring 30-/31-PORTED-CHANNELS.md, incl. changedDMQueueInformation and the D-04/D-05 boundaries"
      contains: "changedDMQueueInformation"
    - path: ".planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md"
      provides: "The doubly-gated deferred-UAT item naming G-30-01 + G-30-02"
      contains: "G-30-02"
  key_links:
    - from: ".planning/phases/32-.../32-PORTED-CHANNELS.md"
      to: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      via: "channels moved from SEAM §3 deferred table into §1 ported"
      pattern: "changedDMQueueInformation"
---

<objective>
Ship the two doc artifacts that make Phase 32's boundary "declared, not discovered": the
`32-PORTED-CHANNELS.md` channel list (including the research-surfaced `changedDMQueueInformation`)
and the `32-HUMAN-UAT.md` doubly-gated deferred-UAT item. Complete SEAM.md checklist step 5 by
moving the ported rows from §3 (deferred) into §1.

Purpose: Slices are only trustworthy if the ported surface is enumerated and the honest
claim-scope ("wired and unit-proven", never "hardware-proven") is recorded. Covers REQ-32-06/07.

Output: `32-PORTED-CHANNELS.md`, `32-HUMAN-UAT.md`, and the SEAM.md §3→§1 edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-CONTEXT.md
@.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-RESEARCH.md
@.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PATTERNS.md
@.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-01-SUMMARY.md
@.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 32-PORTED-CHANNELS.md (all channels + D-04/D-05 boundaries)</name>
  <files>.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-PORTED-CHANNELS.md (FULL — table shape L35-46, claim-scope note L8-15, "Deliberately NOT ported" section structure)
    - .planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-PORTED-CHANNELS.md (the queue-channel + byte-progress rows listed there as "Phase 32's cluster" — the remainder this phase retires)
    - .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PATTERNS.md (the exact declared-channel table to copy, PATTERNS §32-PORTED-CHANNELS.md, L238-257)
    - .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-01-SUMMARY.md and 32-02-SUMMARY.md (the actual as-shipped wiring to declare accurately)
  </read_first>
  <action>
    Create `32-PORTED-CHANNELS.md` mirroring `31-PORTED-CHANNELS.md`'s shape. Declare EVERY channel this
    phase ported: `getDMQueueInformation` (invoke), `removeFromDMQueue`/`pauseCurrentDownload`/
    `resumeCurrentDownload`/`cancelDownload` (send/listener), `install`/`updateGame` (invoke, re-routed to
    `addToQueue()`, replacing the Phase 30 D-05a bypass), `progressUpdate` (push, depot.ts already-throttled),
    and `changedDMQueueInformation` (push — the research-surfaced "fifth" channel with 5 call sites in
    downloadqueue.ts; do NOT omit — Pitfall 4). For each row note the registration module and the real
    code reached, and the requirement ID. Include a "Deliberately NOT ported" section: D-05's disabled
    boot-time `initQueue(isStartup=true)` auto-resume (cite `main.ts:579` as the call NOT replicated;
    note pre-`initQueue` cancelability via `downloadqueue.ts:49` is preserved) and the CrossOver bottle /
    macOS bridge install branches (Phase 30 D-07). State the D-04 nuance explicitly: pause/resume are REAL
    but implemented as abort-then-reconciled-restart (Phase 23 `reconcilePartialState`), not true
    in-flight suspend (Pitfall 5) — never claim true pause. Add a claim-scope note stating "unit-proven"
    ≠ "hardware-proven" and naming BOTH G-30-01 and G-30-02 as the live-E2E blockers (D-06 doubly-gated —
    do NOT reuse Phase 30/31's single-blocker wording).
  </action>
  <verify>
    <automated>grep -q "changedDMQueueInformation" .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md && grep -q "abort-then" .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md && grep -q "G-30-01" .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md && grep -q "G-30-02" .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md</automated>
  </verify>
  <acceptance_criteria>
    - The channel table includes all nine entries: the five queue channels, `install`, `updateGame`, `progressUpdate`, and `changedDMQueueInformation` — each with kind, real code reached, and REQ ID.
    - Pause/resume is declared as abort-then-reconciled-restart (not true suspend); D-05's disabled boot auto-resume is in a "Deliberately NOT ported" section.
    - The claim-scope note names both G-30-01 and G-30-02.
  </acceptance_criteria>
  <done>32-PORTED-CHANNELS.md declares every ported channel including changedDMQueueInformation, with the D-04 pause caveat and D-05 boot-resume deferral explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Write 32-HUMAN-UAT.md (doubly-gated deferred item) + move SEAM.md §3→§1 rows</name>
  <files>.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md, .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</files>
  <read_first>
    - .planning/phases/26-steam-key-redemption/26-HUMAN-UAT.md (the standalone deferred-UAT doc section shape, per PATTERNS §32-HUMAN-UAT.md)
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md (§3 deferred backlog table — the queue-channel + byte-progress `progressUpdate` throughput rows to move; §1 ported list — the destination; §"Accepted Constraints" D-05a entry to mark superseded)
    - .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-VALIDATION.md (§Manual-Only Verifications — the doubly-gated live-E2E rationale and dual-build smoke to record)
  </read_first>
  <action>
    Create `32-HUMAN-UAT.md` with exactly one deferred-UAT item: live queue E2E (enqueue install →
    observe progress bar → pause/resume/cancel from the Tauri UI), marked deferred and DOUBLY-gated,
    naming BOTH blockers — G-30-01 (Tauri QR login unresponsive, blocks reaching a signed-in library)
    and G-30-02 (install-hang, parked to Phase 33, blocks a running install to act on). State the honest
    claim: "wired and unit-proven", NEVER "hardware-proven" and specifically NOT "the live queue was
    exercised" (D-06 — do not word as "same as slice 1/2"). Also record the REQ-32-08 dual-build smoke
    (`npm start` + `npm run tauri:dev` both boot) as a Manual-Only check per Phase 30/31 precedent. Then
    edit `SEAM.md`: move the queue-channel rows and the byte-progress `progressUpdate` throughput row out
    of §3's deferred table into §1's ported list (checklist step 5), and mark the §"Accepted Constraints"
    D-05a direct-bypass entry as closed/superseded by Phase 32.
  </action>
  <verify>
    <automated>grep -q "G-30-01" .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md && grep -q "G-30-02" .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md && grep -qi "unit-proven" .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `32-HUMAN-UAT.md` contains exactly one deferred live-E2E item naming both G-30-01 and G-30-02, with the "unit-proven ≠ hardware-proven" claim-scope.
    - The REQ-32-08 dual-build smoke is recorded as Manual-Only.
    - `SEAM.md` §3 no longer lists the queue channels / byte-progress `progressUpdate` as deferred; they appear in §1; the D-05a entry is marked superseded.
  </acceptance_criteria>
  <done>32-HUMAN-UAT.md logs the doubly-gated deferred item and SEAM.md reflects the §3→§1 move with D-05a marked superseded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (documentation-only plan) | No runtime trust boundary crossed; artifacts are planning docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Repudiation | claim-scope overstatement (reader mistakes "wired" for "live-exercised") | mitigate | 32-PORTED-CHANNELS.md + 32-HUMAN-UAT.md both explicitly record "unit-proven ≠ hardware-proven" and name both live-E2E blockers (D-06) |
| T-32-07 | Tampering | undeclared ported channel (changedDMQueueInformation) | mitigate | The channel is enumerated in 32-PORTED-CHANNELS.md so the surface is declared, not discovered (REQ-32-07) |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs — docs only |
</threat_model>

<verification>
- `32-PORTED-CHANNELS.md` and `32-HUMAN-UAT.md` exist with all required declarations (grep gates above).
- SEAM.md §3 no longer lists the queue channels / byte-progress as deferred; §1 does; D-05a marked superseded.
</verification>

<success_criteria>
- Every ported channel (incl. `changedDMQueueInformation`) is declared with its D-04/D-05 boundaries.
- The deferred-UAT item is doubly-gated and honestly scoped.
- SEAM.md checklist step 5 (§3→§1 move) is complete.
</success_criteria>

<output>
Create `.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-03-SUMMARY.md` when done
</output>
