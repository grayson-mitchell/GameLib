---
phase: 32-tauri-ipc-re-plumb-slice-3-downloads-and-queue
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/backend/sidecar/installFlowRegistration.ts
  - src/backend/sidecar/__tests__/downloadQueueFlows.test.ts
autonomous: true
requirements: [REQ-32-01]
must_haves:
  truths:
    - "install and updateGame enqueue via addToQueue() instead of the Phase 30 direct SteamGame.install() bypass"
    - "install and updateGame resolve Promise<void> once the element is QUEUED, not once the install finishes"
    - "The Phase 30 bypass's hand-rolled sendGameStatusUpdate('queued'/'installing') and try/catch/finally status-suppression logic is DELETED, not wrapped"
  artifacts:
    - path: "src/backend/sidecar/installFlowRegistration.ts"
      provides: "install/updateGame handlers re-routed to addToQueue(), matching Electron's ipc_handler.ts shape"
      contains: "addToQueue"
    - path: "src/backend/sidecar/__tests__/downloadQueueFlows.test.ts"
      provides: "install→addToQueue enqueue assertion (Promise<void>, no direct bypass)"
      contains: "addToQueue"
  key_links:
    - from: "src/backend/sidecar/installFlowRegistration.ts"
      to: "src/backend/downloadmanager/downloadqueue.ts"
      via: "addToQueue(dmQueueElement) in the install/updateGame handlers"
      pattern: "addToQueue\\("
---

<objective>
Retire the Phase 30 D-05a direct-bypass: re-route the sidecar's `install` and `updateGame`
handlers to enqueue via `addToQueue()` exactly like Electron's `ipc_handler.ts`, so the queue
channels ported in Plan 32-01 have a real running install to act on (D-01).

Purpose: The queue-management channels are structurally hollow if installs never enqueue.
This plan populates the queue and corrects `install`'s resolve semantics to the real
`Promise<void>` contract (resolves once queued, not once installed).

Output: modified `installFlowRegistration.ts` + install-enqueue assertions added to the
shared `downloadQueueFlows.test.ts`. Covers REQ-32-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-CONTEXT.md
@.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-RESEARCH.md
@.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PATTERNS.md
@.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-01-SUMMARY.md

<interfaces>
<!-- The exact re-route parity shape — from src/backend/downloadmanager/ipc_handler.ts:13-22, 46-62 -->

install handler (parity):
- build DMQueueElement { params: args, type: 'install', addToQueueTime: Date.now(), endTime: 0, startTime: 0 }
- await addToQueue(dmQueueElement)   # returns void — resolves once QUEUED
- (legendary DLC fan-out loop omitted — not Steam-relevant)

updateGame handler (parity):
- build DMQueueElement { params: { ...args, path: install_path, platformToInstall: platform }, type: 'update', addToQueueTime: Date.now(), endTime: 0, startTime: 0 }
- await addToQueue(dmQueueElement)

Real typed contract (src/common/types/ipc.ts:394, 404):
- install:    (args: InstallParams) => Promise<void>
- updateGame: (args) => Promise<void>
  # addToQueue() has NO return value — resolve undefined/void, never reconstruct a {status} shape (Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add install/updateGame → addToQueue enqueue assertions to downloadQueueFlows.test.ts (RED)</name>
  <files>src/backend/sidecar/__tests__/downloadQueueFlows.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/downloadQueueFlows.test.ts (the harness created in Plan 32-01 — extend it, reuse startSidecar/writeInvoke/flush/collectFrames)
    - src/backend/downloadmanager/ipc_handler.ts (L13-22 install → addToQueue; L46-62 updateGame → addToQueue — the exact enqueue shape to assert against)
    - src/common/types/ipc.ts (L394 install: Promise<void>, L404 updateGame — the resolve-once-queued contract)
  </read_first>
  <behavior>
    - install (invoke): writeInvoke('install', [{ appName, runner: 'steam', path }]) resolves ok with result === undefined (Promise<void>, Pitfall 3) AND the queue side effect fires — assert a spied/mocked addToQueue was called with a matching DMQueueElement (type: 'install'), OR the downloadManager 'queue' store now contains the element.
    - updateGame (invoke): writeInvoke('updateGame', [args-with-gameInfo.install]) resolves ok with result === undefined AND addToQueue called with type: 'update' and params.path/platformToInstall derived from gameInfo.install.
    - no-bypass regression: assert the direct SteamGame.install() path is NOT reached for the install invoke (spy on the bypass entry / assert addToQueue is the sole enqueue path).
    - single-push regression (Open Question 1): each status string is pushed at most once per install (no duplicate 'queued'/'installing' from a leftover manual push).
  </behavior>
  <action>
    Extend the shared `downloadQueueFlows.test.ts` (do NOT create a new file). Add the install/updateGame
    enqueue assertions from the behavior block, reusing the existing harness helpers and boundary mocks.
    Spy on `addToQueue` from `../../downloadmanager/downloadqueue` (or assert the `downloadManager` store
    queue contents) to prove the enqueue path. These assertions MUST fail RED now because
    `installFlowRegistration.ts` still runs the Phase 30 direct bypass. Do NOT weaken the assertions to
    pass against the bypass.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/downloadQueueFlows.test.ts -t "install|updateGame" 2>&1 | grep -Eq "(fail|FAIL|✕)" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The install/updateGame enqueue tests exist and FAIL RED because the handlers still use the Phase 30 bypass.
    - The install test asserts `result === undefined` (Promise<void>), not a `{status}` shape.
    - The tests assert `addToQueue` was called (enqueue side effect), not merely that the invoke returned ok.
  </acceptance_criteria>
  <done>Install/updateGame enqueue assertions added to the shared test file, failing RED pending the re-route in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-route install/updateGame to addToQueue in installFlowRegistration.ts; delete the Phase 30 bypass (GREEN)</name>
  <files>src/backend/sidecar/installFlowRegistration.ts</files>
  <read_first>
    - src/backend/sidecar/installFlowRegistration.ts (FULL — the Phase 30 bypass to replace: install body L120-237, updateGame body L266-309, the runner!=='steam' CR-01 guard L135-139, the manual sendGameStatusUpdate pushes L144-159, the deferredToSetup/wasAborted/hadError try/catch/finally L161-235; KEEP unchanged: uninstall L248-264, checkGameUpdates L311, listSteamLibraryTargets L313-315; the load-bearing `import '../storeManagers'` L104 stays)
    - src/backend/downloadmanager/ipc_handler.ts (L13-22, L46-62 — the parity shape adapted to ipcMain.handle)
    - src/backend/downloadmanager/utils.ts (installQueueElement/updateQueueElement — the queued→installing→done transitions reached automatically via addToQueue()→initQueue(); do NOT hand-roll a second copy)
  </read_first>
  <action>
    Replace the entire Phase 30 direct-bypass bodies of the `install` and `updateGame` handlers with the
    `addToQueue()` re-route matching `ipc_handler.ts`: build the `DMQueueElement` (type `'install'` /
    `'update'`, `addToQueueTime: Date.now()`, `endTime: 0`, `startTime: 0`; for updateGame derive
    `params.path`/`platformToInstall` from `args.gameInfo.install`) and `await addToQueue(dmQueueElement)`.
    Resolve `Promise<void>` — do NOT reconstruct a `{status}` shape (Pitfall 3; `addToQueue` has no return
    value). DELETE the bypass's manual `sendGameStatusUpdate` pushes and the deferredToSetup/wasAborted/
    hadError try/catch/finally — `installQueueElement`/`updateQueueElement` reached via `addToQueue()`→
    `initQueue()` already reproduce them exactly (do not wrap, delete). Import `addToQueue` from
    `../downloadmanager/downloadqueue`; drop the now-unused `sendGameStatusUpdate` import only after
    confirming `uninstall`/`checkGameUpdates`/`listSteamLibraryTargets` don't use it. Before implementing,
    re-grep `window.api.install(` / `window.api.updateGame(` (RESEARCH Assumption A2) to confirm no
    frontend call site branches on the resolved value. Whether the re-route stays Steam-only or goes
    runner-generic is a planner-discretion call within D-01 — either is acceptable as long as no second
    status-transition implementation is hand-rolled.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/downloadQueueFlows.test.ts src/backend/downloadmanager/__tests__/downloadqueue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - install/updateGame enqueue tests GREEN; `result === undefined` (Promise<void>) proven.
    - The Phase 30 bypass is gone: `grep -n "sendGameStatusUpdate" src/backend/sidecar/installFlowRegistration.ts` shows no calls in the install/updateGame handlers (deleted, not wrapped); `grep -c "addToQueue(" src/backend/sidecar/installFlowRegistration.ts` returns >= 2.
    - No `{ status:` reconstruction in the install/updateGame handler bodies.
    - `uninstall`/`checkGameUpdates`/`listSteamLibraryTargets` are unchanged; `downloadqueue.test.ts` stays green.
  </acceptance_criteria>
  <done>install/updateGame enqueue via addToQueue(), the Phase 30 bypass is deleted, resolve is Promise<void>, and the shared test file + downloadqueue.test.ts are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar (invoke) | `install`/`updateGame` params (`InstallParams`) arrive from the RPC frame and are wrapped into a `DMQueueElement` passed to `addToQueue()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | `install`/`updateGame` args → `DMQueueElement.params` | mitigate | Params are consumed by the unmodified `addToQueue()`→`installQueueElement` path which dispatches via `libraryManagerMap[runner]` and depot.ts's shipped path containment (`resolveContainedPath`, Phase 21) — no new interpolation surface introduced by the re-route |
| T-32-05 | Repudiation | resolve-shape divergence (bypass `{status}` vs real `Promise<void>`) | mitigate | Match the typed `Promise<void>` contract exactly and delete the bypass's synthetic status; regression test asserts each status string is pushed at most once (no double-push artifact) |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — Package Legitimacy Gate not applicable |
</threat_model>

<verification>
- `npx jest src/backend/sidecar/__tests__/downloadQueueFlows.test.ts src/backend/downloadmanager/__tests__/downloadqueue.test.ts` green.
- Bypass deleted (grep gates above); resolve is `Promise<void>`.
- REQ-32-08 dual-build smoke (Manual-Only per precedent): `npm start` + `npm run tauri:dev` both boot unchanged after this plan.
</verification>

<success_criteria>
- `install`/`updateGame` enqueue via `addToQueue()`; the Phase 30 direct bypass is fully removed.
- Resolve semantics match `Promise<void>` (queued, not installed); no synthetic `{status}` shape.
- No second status-transition implementation is hand-rolled; `installQueueElement`/`updateQueueElement` do the work.
</success_criteria>

<output>
Create `.planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-02-SUMMARY.md` when done
</output>
