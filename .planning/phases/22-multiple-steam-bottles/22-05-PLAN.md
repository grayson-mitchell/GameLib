---
phase: 22-multiple-steam-bottles
plan: 05
type: execute
wave: 5
depends_on: ["22-02", "22-04"]
files_modified:
  - src/backend/main.ts
  - src/common/types/ipc.ts
  - src/preload/api/steam.ts
autonomous: true
requirements: [Req-4, Req-7]
must_haves:
  truths:
    - "The single-bottle steamBottleStatus/steamBottleProvision/isSteamBottleProvisioned handlers are DELETED, not left alongside the new group (D-12, Pitfall 5)"
    - "A cohesive family IPC group is registered: listFamilies/createFamily/renameFamily/deleteFamily/setFamilyWine/assignGameToFamily/familyStatusForApp (D-12)"
    - "migrateSteamBottleStore() runs once at Steam startup BEFORE any family-reading IPC handler can execute (D-07)"
    - "The steamBottleSetupRequired frontend message payload declares { appName, bottleName } (Pitfall 4)"
    - "ipc.ts AsyncIPCFunctions and preload/api/steam.ts expose exactly the new family group with matching signatures"
  artifacts:
    - path: "src/backend/main.ts"
      provides: "family IPC handler group + startup migration invocation; old trio removed"
      contains: "listFamilies"
    - path: "src/common/types/ipc.ts"
      provides: "family IPC typedefs + updated steamBottleSetupRequired payload"
      contains: "familyStatusForApp"
    - path: "src/preload/api/steam.ts"
      provides: "preload wrappers for the family IPC group"
      contains: "createFamily"
  key_links:
    - from: "src/backend/main.ts"
      to: "families.ts family functions"
      via: "addHandler registrations"
      pattern: "addHandler\\('(listFamilies|createFamily|deleteFamily)'"
    - from: "Steam startup init"
      to: "migrateSteamBottleStore()"
      via: "awaited before family handlers read the store"
      pattern: "migrateSteamBottleStore\\("
---

<objective>
Fold the three single-bottle IPC handlers into one cohesive, family-aware IPC
group (D-12) across the three IPC-surface files (backend `main.ts`, shared
`common/types/ipc.ts`, `preload/api/steam.ts`), and wire the one-time startup
migration (D-07) so it completes before any family-reading handler runs. Hard-
remove the old `steamBottleStatus`/`steamBottleProvision`/`isSteamBottleProvisioned`
so a stale wrong-family read surfaces as a compile/registration gap, not a silent
runtime bug (Pitfall 5).

Purpose: this is the seam the frontend (Plans 06–08) calls. One family-aware
status source avoids the drift/dead-signal problem Phase 17 hit with `loggedIn`.

Output: reshaped IPC surface + migration invocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-multiple-steam-bottles/22-RESEARCH.md
@.planning/phases/22-multiple-steam-bottles/22-PATTERNS.md
@.planning/phases/22-multiple-steam-bottles/22-02-SUMMARY.md
@.planning/phases/22-multiple-steam-bottles/22-04-SUMMARY.md

**Test runner:** Jest. IPC wiring has no dedicated unit suite; verify via the steam
backend jest suite staying green plus grep gates on handler/typedef/preload names.

<interfaces>
<!-- From families.ts (Plan 02): listFamilies/createFamily/renameFamily/deleteFamily/
     setFamilyWine/assignGameToFamily/familyStatusForApp/migrateSteamBottleStore. -->

Handler shapes to register (mirror existing multi-arg steamStartCredentials/steamSubmitGuard):
```typescript
addHandler('listFamilies', async () => listFamilies())
addHandler('createFamily', async (e, displayName) => createFamily(displayName))
addHandler('renameFamily', async (e, bottleName, displayName) => renameFamily(bottleName, displayName))
addHandler('deleteFamily', async (e, bottleName) => deleteFamily(bottleName))
addHandler('setFamilyWine', async (e, bottleName, wineVersion) => setFamilyWine(bottleName, wineVersion))
addHandler('assignGameToFamily', async (e, appId, bottleName) => assignGameToFamily(appId, bottleName))
addHandler('familyStatusForApp', async (e, appId) => familyStatusForApp(appId))
```

Removed (Pitfall 5): steamBottleStatus, steamBottleProvision, isSteamBottleProvisioned.
Note: SteamBottleSetup.tsx still references steamBottleProvision — it is repointed in
Plan 06; expect app-wide tsc to be red until Plan 06 lands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold IPC handlers + wire startup migration in main.ts (D-12, D-07)</name>
  <files>src/backend/main.ts</files>
  <read_first>
    - src/backend/main.ts (existing Steam bottle IPC block: steamBottleProvision/isSteamBottleProvisioned/steamBottleStatus lines ~940-962; the multi-arg steamStartCredentials/steamSubmitGuard handlers ~915-922 as the addHandler shape; the Steam init sequence where the store is first read)
    - src/backend/storeManagers/steam/families.ts (Plan 02 exports)
    - .planning/phases/22-multiple-steam-bottles/22-PATTERNS.md (§ main.ts — target handler group + hard-removal requirement)
  </read_first>
  <action>
    Delete the `steamBottleProvision`, `isSteamBottleProvisioned`, and
    `steamBottleStatus` `addHandler` registrations entirely (Pitfall 5 — do not leave
    them "for compatibility"; the flat keys they read no longer exist). Register the
    family IPC group from the interfaces block, importing the functions from
    `storeManagers/steam/families`. Wire `await migrateSteamBottleStore()` into the
    Steam startup path so it completes BEFORE any of these family handlers can run
    (D-07 — per RESEARCH it must run before any Steam IPC handler reads the store);
    place it in the existing Steam init sequence, not lazily inside a handler. Keep the
    existing `steamBottleProvision`-adjacent guided-setup handler only if it is the
    provision entry point still used by the guided flow — note: the guided flow keeps
    provisioning through `provisionBottle` (Plan 06 repoints its call), so retain a
    `provisionBottle`-backed handler if one is still needed by SteamBottleSetup, but
    named/typed consistently with the new group; if provisioning now routes through
    `createFamily`, remove the standalone provision handler.
  </action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "addHandler\('(steamBottleStatus|isSteamBottleProvisioned)'" src/backend/main.ts` returns 0.
    - `grep -cE "addHandler\('(listFamilies|createFamily|renameFamily|deleteFamily|setFamilyWine|assignGameToFamily|familyStatusForApp)'" src/backend/main.ts` returns 7.
    - `grep -c "migrateSteamBottleStore(" src/backend/main.ts` >= 1 and it is awaited in the Steam init path (not inside an addHandler callback).
  </acceptance_criteria>
  <done>Old trio removed; family group registered; migration invoked once at startup before handlers read the store.</done>
</task>

<task type="auto">
  <name>Task 2: Family IPC typedefs + preload wrappers (D-12, Pitfall 4/5)</name>
  <files>src/common/types/ipc.ts, src/preload/api/steam.ts</files>
  <read_first>
    - src/common/types/ipc.ts (AsyncIPCFunctions block ~L176; the existing steamBottleStatus/steamBottleProvision/isSteamBottleProvisioned entries; the steamBottleSetupRequired frontend-message payload typedef)
    - src/preload/api/steam.ts (existing steamBottleStatus/steamBottleProvision preload wrappers to replace)
    - .planning/phases/22-multiple-steam-bottles/22-RESEARCH.md (§ State of the Art — IPC fold)
  </read_first>
  <action>
    In `common/types/ipc.ts`, remove the `steamBottleStatus`/`steamBottleProvision`/
    `isSteamBottleProvisioned` `AsyncIPCFunctions` entries and add the seven family-group
    signatures matching the handler shapes (return types matching families.ts:
    `listFamilies` → `{ families: SteamFamilyOption[]; preselected?: string }`,
    `createFamily(displayName?)` → `{ bottleName: string }`, `familyStatusForApp(appId)`
    → `{ status; bottleName; eligible: boolean }`, etc.). Update the
    `steamBottleSetupRequired` frontend-message payload typedef to `{ appName: string;
    bottleName: string }` (Pitfall 4). In `preload/api/steam.ts`, replace the old
    single-bottle wrappers with `createFamily`/`renameFamily`/`deleteFamily`/
    `setFamilyWine`/`assignGameToFamily`/`familyStatusForApp`/`listFamilies` wrappers,
    mirroring the existing preload wrapper style (ipcRenderer.invoke pass-through).
    Export `SteamFamilyOption` from `common/types/steam.ts` (or families.ts) for the
    typedef if not already shared.
  </action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "steamBottleStatus|isSteamBottleProvisioned" src/common/types/ipc.ts` returns 0.
    - `grep -cE "createFamily|familyStatusForApp|listFamilies" src/common/types/ipc.ts` >= 3.
    - `grep -c "bottleName" src/common/types/ipc.ts` shows the steamBottleSetupRequired payload now includes bottleName (manually confirm on that typedef line).
    - `grep -cE "createFamily|deleteFamily|familyStatusForApp" src/preload/api/steam.ts` >= 3 and old wrappers removed.
  </acceptance_criteria>
  <done>ipc.ts + preload expose the family group with matching signatures; steamBottleSetupRequired payload carries bottleName; old single-bottle IPC typedefs/wrappers gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | family CRUD/status args cross from untrusted renderer into backend family service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11 | Tampering | leaving old single-bottle IPC handlers alongside the new group (Pitfall 5) | mitigate | Hard-delete the trio; flat keys removed in Plan 01 mean any lingering reference is a compile error, not a silent wrong-family read |
| T-22-12 | Elevation of Privilege | family CRUD IPC args (displayName/bottleName/appId) from renderer | mitigate | All validation/sanitization lives in families.ts (Plan 02: sanitizeBottleName, uniqueness, last-family guard, provisionBottle CR-01); handlers are thin pass-throughs, adding no new bypass |
| T-22-13 | Tampering | migration racing family handlers (D-07) | mitigate | migrateSteamBottleStore() is awaited in the Steam init sequence before any family handler can execute |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase |
</threat_model>

<verification>
- `npx jest src/backend/storeManagers/steam` green.
- Old trio absent from main.ts/ipc.ts/preload; seven family handlers present; migration awaited at startup.
- (Expected) app-wide `npm run codecheck` still RED — SteamBottleSetup.tsx references the removed provision IPC until Plan 06.
</verification>

<success_criteria>
- One family-aware IPC source (D-12); old trio removed (Pitfall 5).
- Startup migration wired before handlers (D-07).
- steamBottleSetupRequired payload carries bottleName (Pitfall 4).
</success_criteria>

<output>
Create `.planning/phases/22-multiple-steam-bottles/22-05-SUMMARY.md` when done.
</output>
