---
phase: 22-multiple-steam-bottles
plan: 08
type: execute
wave: 7
depends_on: ["22-05", "22-06"]
files_modified:
  - src/frontend/screens/Settings/components/SteamFamilies.tsx
  - src/frontend/screens/Settings/components/SteamFamilies.scss
  - src/frontend/screens/Settings/components/index.ts
  - src/frontend/screens/Settings/sections/GeneralSettings/index.tsx
autonomous: true
requirements: [Req-4, Req-5]
must_haves:
  truths:
    - "A macOS-only 'Steam Families' Settings section lists families with per-row rename, CrossOver-only Wine-version select, and delete, plus a 'Create family' action (Req 4, D-11)"
    - "Create family uses an auto-name then opens the guided setup parameterized by the new bottleName (D-11)"
    - "Delete is confirm-gated via showDialogModal naming affected games; the last remaining family's delete is visually disabled and shows an inline blocked message, never reaching the dialog (Req 5, D-09)"
    - "The Wine-version dropdown lists CrossOver engines only — GPTK/toolkit/plain Wine never appear (SPEC constraint)"
    - "Rename edits only displayName (frozen bottleName); every icon-only button carries aria-label + title (UI-SPEC a11y)"
  artifacts:
    - path: "src/frontend/screens/Settings/components/SteamFamilies.tsx"
      provides: "family management Settings section (create/rename/set-wine/delete)"
      contains: "SteamFamilies"
  key_links:
    - from: "SteamFamilies delete button"
      to: "showDialogModal"
      via: "confirm naming affected games; last-family blocked before dialog"
      pattern: "showDialogModal"
    - from: "Create family"
      to: "createFamily → useSteamBottleSetup.open"
      via: "inline create then guided setup"
      pattern: "createFamily"
---

<objective>
Build the "Steam Families" Settings section (Surface 2/3, Req 4/5, D-11): a
macOS-only section listing families with per-row rename (displayName only),
CrossOver-only Wine-version select, and confirm-gated delete, plus a "Create
family" action that reuses the Plan 06 guided setup. It composes three existing
patterns — `TwoColTableInput` row/icons, `WineVersionSelector` (CrossOver-filtered),
and `showDialogModal` (delete confirm) — into one new component.

Purpose: this is Req 4/5's lifecycle-management surface. The last-family delete
guard and the CrossOver-only engine filter are hard SPEC constraints; the delete
confirm must name affected games (Req 5) via the one shared dialog mechanism.

Output: new `SteamFamilies.tsx` + `.scss`, barrel + GeneralSettings registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-multiple-steam-bottles/22-RESEARCH.md
@.planning/phases/22-multiple-steam-bottles/22-PATTERNS.md
@.planning/phases/22-multiple-steam-bottles/22-UI-SPEC.md
@.planning/phases/22-multiple-steam-bottles/22-05-SUMMARY.md
@.planning/phases/22-multiple-steam-bottles/22-06-SUMMARY.md

**Frontend verify note:** no jsdom harness. Per-task automated verify = `eslint` on
touched files + source grep gates; runtime CRUD behavior is in the phase Manual-Only
table (and Req 4/5 acceptance).

<interfaces>
<!-- From Plan 05: listFamilies() → { families: SteamFamilyOption[]; preselected? };
     createFamily(displayName?) → { bottleName }; renameFamily(bottleName, displayName);
     setFamilyWine(bottleName, wineVersion); deleteFamily(bottleName) → { status; affectedAppIds? };
     From Plan 06: useSteamBottleSetup.getState().open(appName, bottleName). -->

Compose:
- Settings-component + isMac gate: EnableDXVKFpsLimit.tsx early-return `<></>` on !isMac.
- Row/icon pattern: TwoColTableInput (EditIcon accent, RemoveCircleIcon danger, AddBoxIcon success) — DO reuse the icon conventions, add aria-label+title (analog omits them; do not copy that omission).
- Wine select: WineVersionSelector rendering, filtered to `w.type === 'crossover'`.
- Delete confirm: showDialogModal via useContext(ContextProvider) — mirror helpers/library.ts handleStopInstallation.
- Barrel: Settings/components/index.ts (mirror `export { default as CrossoverBottle } from './CrossoverBottle'`).
- Placement: GeneralSettings/index.tsx adjacent to <DefaultSteamPath/> + <EnableSteamNativeInstall/>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SteamFamilies section — list, rename, CrossOver-only Wine select, create (Req 4, D-11)</name>
  <files>src/frontend/screens/Settings/components/SteamFamilies.tsx, src/frontend/screens/Settings/components/SteamFamilies.scss, src/frontend/screens/Settings/components/index.ts, src/frontend/screens/Settings/sections/GeneralSettings/index.tsx</files>
  <read_first>
    - src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx (Settings-component shape + gate)
    - src/frontend/screens/Settings/components/EnableDXVKFpsLimit.tsx (isMac early-return `<></>` gate pattern)
    - src/frontend/components/UI/TwoColTableInput/index.tsx (row layout + EditIcon/RemoveCircleIcon/AddBoxIcon SvgButton conventions ~lines 158-206)
    - src/frontend/screens/Settings/components/WineVersionSelector.tsx (SelectField + icon-prefixed MenuItem rendering ~136-149, 220-225; filter to type==='crossover')
    - src/frontend/screens/Settings/components/index.ts (barrel export style) and src/frontend/screens/Settings/sections/GeneralSettings/index.tsx (where DefaultSteamPath/EnableSteamNativeInstall render)
    - .planning/phases/22-multiple-steam-bottles/22-UI-SPEC.md (Surface 2: heading/intro/row copy keys, focal point, a11y, provisioning status label)
  </read_first>
  <action>
    Create `SteamFamilies.tsx` gated by `isMac` (early-return `<></>` mirroring
    EnableDXVKFpsLimit; NOT isDefault). Load families via `window.api.listFamilies()`.
    Render an `<h4 className="settingSubheader">` heading
    `t('settings.steamFamilies.heading', 'Steam Families')` + one-line intro
    `t('settings.steamFamilies.description', ...)`. For each family render a row
    (`var(--background-darker)`, `padding: var(--space-md)`): a `TextInputField` for
    `displayName` with inline rename (pencil `EditIcon`, populate-then-commit; commit on
    blur/Enter via `window.api.renameFamily(bottleName, displayName)`; Escape cancels;
    rename NEVER changes bottleName); a `SelectField` Wine/CrossOver dropdown whose
    options are `altWine.filter(w => w.type === 'crossover')` (GPTK/toolkit/plain Wine
    must never appear — SPEC), committing via `window.api.setFamilyWine(bottleName,
    wineVersion)`; a delete `SvgButton` + `RemoveCircleIcon` (`color: var(--danger)`)
    wired to Task 2's confirm. A small `--text-xs`/`--bold` `t('settings.steamFamilies.
    notProvisioned', 'Not set up yet')` label shows only when `provisioned === false`.
    Add a "Create family" row: `SvgButton` + `AddBoxIcon` (`color: var(--success)`) +
    visible label `t('settings.steamFamilies.create', 'Create family')` that calls
    `window.api.createFamily()` then `useSteamBottleSetup.getState().open(appName?,
    newBottleName)` — for a Settings-initiated create with no game context, open guided
    setup keyed by bottleName only. Every icon-only button carries `aria-label` + `title`
    (rename/delete per UI-SPEC keys). Create `SteamFamilies.scss` using only existing
    `var(--space-*)`/`var(--...)` tokens (mirror SteamBottleSetup.scss). Register in the
    `Settings/components/index.ts` barrel and render `<SteamFamilies />` in
    `GeneralSettings/index.tsx` adjacent to `<DefaultSteamPath />` / `<EnableSteamNativeInstall />`.
  </action>
  <verify>
    <automated>npx eslint src/frontend/screens/Settings/components/SteamFamilies.tsx src/frontend/screens/Settings/components/index.ts src/frontend/screens/Settings/sections/GeneralSettings/index.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SteamFamilies" src/frontend/screens/Settings/components/index.ts` >= 1 (barrel export) and `grep -c "SteamFamilies" src/frontend/screens/Settings/sections/GeneralSettings/index.tsx` >= 1 (rendered).
    - `grep -cE "type === 'crossover'|type==='crossover'" src/frontend/screens/Settings/components/SteamFamilies.tsx` >= 1 (CrossOver-only filter).
    - `grep -c "aria-label" src/frontend/screens/Settings/components/SteamFamilies.tsx` >= 2 (rename + delete icon buttons).
    - `grep -c "renameFamily" src/frontend/screens/Settings/components/SteamFamilies.tsx` >= 1 and `grep -c "createFamily" src/frontend/screens/Settings/components/SteamFamilies.tsx` >= 1.
    - `npx eslint` clean on the touched files.
  </acceptance_criteria>
  <done>macOS-only Steam Families section renders list + rename + CrossOver-only Wine select + Create family, registered in the barrel and GeneralSettings.</done>
</task>

<task type="auto">
  <name>Task 2: Guarded delete — confirm dialog + last-family block (Req 5, D-09)</name>
  <files>src/frontend/screens/Settings/components/SteamFamilies.tsx, src/frontend/screens/Settings/components/SteamFamilies.scss</files>
  <read_first>
    - src/frontend/helpers/library.ts (handleStopInstallation showDialogModal call shape ~lines 102-127)
    - src/frontend/state/ContextProvider or GlobalState.tsx (showDialogModal access via useContext(ContextProvider))
    - .planning/phases/22-multiple-steam-bottles/22-UI-SPEC.md (Surface 3: message composition rules 0 / 1-5 / 6+ games, irreversible line; Delete-disabled state copy)
  </read_first>
  <action>
    Access `showDialogModal` via `useContext(ContextProvider)`. Wire the per-row delete
    button: when more than one family exists, open `showDialogModal({ title:
    t('settings.steamFamilies.deleteTitle', 'Delete "{{name}}"?', {name}), message:
    <composed>, buttons: [{ text: t('box.cancel') }, { text:
    t('settings.steamFamilies.deleteConfirm', 'Delete family'), onClick: () =>
    window.api.deleteFamily(bottleName) }] })` — confirm is the right-most, danger-styled
    button. Compose `message` per UI-SPEC: 0 installed games → deleteEmptyBody; 1–5 →
    lead sentence + `<ul>` of titles; 6+ → first 5 + `t('settings.steamFamilies.deleteMore',
    'and {{count}} more')`; always append `t('settings.steamFamilies.deleteIrreversible',
    'This cannot be undone.')` styled `var(--danger)`. Derive installed-game titles for
    the family from the existing library state filtered by the family's assignments.
    When exactly one family exists, render the delete button visually disabled (reduced
    opacity, `cursor: not-allowed`, no hover color) and, on click, show the inline
    `t('settings.steamFamilies.lastFamilyBlocked', ...)` message under the row — do NOT
    call showDialogModal in that case (never a dismiss-only dialog). Add the disabled +
    blocked-message styles to `SteamFamilies.scss` using existing tokens.
  </action>
  <verify>
    <automated>npx eslint src/frontend/screens/Settings/components/SteamFamilies.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "showDialogModal" src/frontend/screens/Settings/components/SteamFamilies.tsx` >= 1 and `grep -c "deleteFamily" src/frontend/screens/Settings/components/SteamFamilies.tsx` >= 1.
    - `grep -cE "lastFamilyBlocked|length <= 1|length === 1" src/frontend/screens/Settings/components/SteamFamilies.tsx` >= 1 (last-family guard path).
    - `grep -c "deleteIrreversible" src/frontend/screens/Settings/components/SteamFamilies.tsx` >= 1 (irreversibility line).
    - `npx eslint` clean on the file.
  </acceptance_criteria>
  <done>Delete is confirm-gated naming affected games; last-family delete is visually disabled with an inline blocked message and never opens the dialog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → backend family CRUD | rename/set-wine/delete/create cross into the family service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-20 | Denial of Service | deleting the last family (orphaning all Steam-bottle games) | mitigate | Delete button visually disabled + inline blocked message when one family remains; backend deleteFamily also guards (Plan 02) — defense in depth |
| T-22-21 | Tampering | destructive delete without informed consent (Req 5) | mitigate | showDialogModal confirm names affected games + irreversible warning; delete only fires from the danger-styled confirm button's onClick |
| T-22-22 | Elevation of Privilege | offering a non-CrossOver engine for a family (broken-bottle bug) | mitigate | Wine dropdown filtered to `type === 'crossover'`; GPTK/toolkit/plain Wine never rendered (SPEC constraint) |
| T-22-23 | Elevation of Privilege | rename/create passing unsanitized names to paths | mitigate | rename edits displayName only (frozen bottleName); all sanitization + uniqueness enforced backend-side in families.ts (Plan 02) |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase |
</threat_model>

<verification>
- `npx eslint` clean on all touched files.
- CrossOver-only Wine filter, aria-labels, last-family guard, and irreversible warning present.
- Manual (phase Manual-Only): create/rename/set-wine/delete persist across restart; delete confirm lists affected games; last-family delete blocked with a message.
</verification>

<success_criteria>
- Family lifecycle management section (Req 4, D-11).
- Guarded, confirm-gated delete with last-family block (Req 5, D-09).
- CrossOver-only engine constraint enforced in the UI.
</success_criteria>

<output>
Create `.planning/phases/22-multiple-steam-bottles/22-08-SUMMARY.md` when done.
</output>
