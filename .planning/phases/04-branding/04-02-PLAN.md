---
phase: 04-branding
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements: [BRAND-01]

must_haves:
  truths:
    - "Running app window/chrome shows 'GameLib'"
    - "Sidebar shows 'GameLib Version: x.x.x'"
    - "Settings → System Info shows 'GameLib: x.x.x' and Copy-to-clipboard pastes text starting with 'GameLib:'"
  artifacts: []
  key_links: []
---

<objective>
Human verification that the BRAND-01 GameLib identity renders correctly in the live, running app — the parts that cannot be asserted by static checks (window chrome, React render + i18n resolution, clipboard output).

Purpose: Close the MVP loop. 04-01's automated smoke script proves the source values are correct; this checkpoint proves a real user sees "GameLib" everywhere the success criteria require.

Output: Human sign-off on BRAND-01 SC-1/SC-2 in the running application.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-branding/04-VALIDATION.md
@.planning/phases/04-branding/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify GameLib identity in the running app</name>
  <files>(none — verification only)</files>
  <action>Launch the running app and walk the human through the five live-surface checks below (window title, sidebar version label, About page label, Copy-to-clipboard text). Pause for the user's "approved" signal. This is a verification-only checkpoint; no files are edited.</action>
  <what-built>04-01 renamed the sidebar version label, About-page label, and clipboard system-info text to "GameLib", and set package/distribution metadata (name=gamelib, appId=com.gamelib.app, Linux Name=GameLib). The OS window title and macOS About panel were already "GameLib". Automated checks (smoke script + codecheck) are GREEN.</what-built>
  <how-to-verify>
    1. Launch the app: `pnpm start`.
    2. Window chrome: confirm the OS window title / title bar reads "GameLib" (no "Heroic").
    3. Sidebar: bottom-left version label reads "GameLib Version: 2.22.0 …" (no "Heroic Version").
    4. Open Settings → System Info: the version label reads "GameLib: 2.22.0 …" (no "Heroic:").
    5. Click "Copy to clipboard" in System Info, paste into any text field, and confirm the pasted block's software line starts with "GameLib:" (no "Heroic:").
    Expected: every user-visible identity string above says "GameLib". Nothing should still say "Heroic" on these surfaces. (Note: internal/legitimate Heroic references elsewhere are intentionally left per D-04 and are not part of this check.)
  </how-to-verify>
  <verify>
    <human-check>All five live-app surfaces (title bar, sidebar version label, About page label, clipboard text) read "GameLib".</human-check>
  </verify>
  <resume-signal>Type "approved" if all five checks read "GameLib", or describe which surface still shows "Heroic".</resume-signal>
  <done>User has confirmed (typed "approved") that title bar, sidebar version label, About page label, and clipboard text all read "GameLib" in the running app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary. This plan is a human-verification checkpoint over a display/metadata change; it introduces no code, no packages, and no new surface (see 04-01 threat model — intentionally minimal). ASVS V2/V3/V4/V5/V6 do not apply.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | n/a | No threats. Verification-only plan; nothing installed or executed beyond launching the app. |
</threat_model>

<verification>
Human confirms all five live-app surfaces read "GameLib".
</verification>

<success_criteria>
- BRAND-01 SC-1 confirmed live: title bar + sidebar version label show "GameLib".
- BRAND-01 SC-2 confirmed live: About page label and clipboard text show "GameLib".
- User has typed "approved".
</success_criteria>

<output>
Create `.planning/phases/04-branding/04-02-SUMMARY.md` when done.
</output>
