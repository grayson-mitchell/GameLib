---
phase: 40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/WebView/index.tsx
  - src/frontend/screens/WebView/components/HumbleLoginSurface.tsx
  - src/frontend/screens/WebView/components/humbleLoginChromeCss.ts
  - src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx
  - src/frontend/screens/WebView/__tests__/humbleLoginChromeCss.test.ts
  - src/frontend/screens/WebView/__tests__/WebViewAmazonLoginDataSpawn.test.ts
  - src/frontend/components/UI/WebviewControls/index.tsx
  - src/frontend/components/UI/WebviewControls/index.css
  - meta/i18nForkTouchedFiles.json
  - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md
autonomous: true
requirements: [REQ-40-10]

must_haves:
  truths:
    - "D-09/D-09a: no file under src/frontend/ renders a `<webview>` element"
    - "D-09: the `!webviewPreloadPath` guard no longer exists in WebView/index.tsx or HumbleLoginSurface.tsx — the store/wiki arm is reached unconditionally"
    - "D-09: the WebviewControls component directory is deleted, not merely unreferenced"
    - "D-10: `/store/*` and `/wiki` still render WebviewUnavailablePanel after this plan — store routes are never broken by the unwind"
    - "The census is derived by predicate against the live tree, not copied from the ROADMAP's stale Sidebar/index.tsx line"
    - "Every test pin invalidated by the deletion is dispositioned with a labelled verdict, not deleted silently"
  artifacts:
    - path: ".planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md"
      provides: "Predicate-derived census of every Model A site, with the reproducing command"
      contains: "Reproducing command"
    - path: "src/frontend/screens/WebView/index.tsx"
      provides: "Store/wiki screen with zero Model A code"
      min_lines: 200
  key_links:
    - from: "src/frontend/screens/WebView/index.tsx"
      to: "WebviewUnavailablePanel"
      via: "unconditional store/wiki return"
      pattern: "WebviewUnavailablePanel"
---

<objective>
Retire Model A (the renderer-owned Electron `<webview>`) from the frontend, on a census derived by
predicate against the live tree rather than copied from the ROADMAP's partly-stale list.

Purpose: D-10 requires the unwind to land FIRST so Model B is built onto a clean surface and nobody
reads two webview models in one file. Everything deleted here is provably unreachable:
`getWebviewPreloadPath` returns a declared-empty `''` unconditionally under Tauri
(`appShellFlowRegistration.ts:262-266`) and Tauri is the only shell since Phase 35, so
`!webviewPreloadPath` is ALWAYS true and every branch behind it is dead.

Output: a Model A census artifact; a WebView screen and HumbleLoginSurface with zero Model A code;
the WebviewControls component directory deleted; every invalidated test pin dispositioned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-CONTEXT.md
@.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-PATTERNS.md

<planning_findings>
Findings measured at plan time against the live tree. They correct or extend CONTEXT.md and
PATTERNS.md; treat them as binding, and RE-MEASURE rather than trusting these counts.

1. **The ROADMAP's `Sidebar/index.tsx:92,103` line is dead.** That file does not exist (Phase
   34.10's NavShell replaced it). It is absent from the predicate census below. Do not restore it.

2. **`meta/i18nForkTouchedFiles.json` pins two files this plan DELETES** — line 51
   (`src/frontend/components/UI/WebviewControls/index.tsx`) and line 203
   (`src/frontend/screens/WebView/components/humbleLoginChromeCss.ts`). Deleting a file named in
   that artifact is the exact failure shape recorded in project memory as "deleting a SCOPED source
   file breaks the blocking i18n gate 3 ways". This must be dispositioned in Task 3, not discovered
   in CI. `meta/i18nGateScope.json` does NOT name either file (it names only `TauriLoginPanel.tsx`
   and `WebviewUnavailablePanel.tsx` at :157-158), so the exposure is the fork-touched list only —
   confirm that by measurement, do not assume it.

3. **Three test files pin source text this plan changes:**
   - `src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx` —
     `hasTwoDistinctArms()` extracts the block at the literal string `if (!webviewPreloadPath)` and
     returns `false` when that string is absent. Deleting the guard turns this gate RED. It carries
     four self-tests (three rejection cases, one positive control).
   - `src/frontend/screens/WebView/__tests__/humbleLoginChromeCss.test.ts:221` — asserts the source
     `toContain('window.api.humbleLoginNavigated()')`. That call site is deleted here.
   - `src/frontend/screens/WebView/__tests__/WebViewAmazonLoginDataSpawn.test.ts` — pins the
     `amazonLoginData` deadness comment block at `index.tsx:223-237`.

4. **`humbleGetLoginUserAgent`'s only renderer consumer dies here too.** `HumbleLoginSurface.tsx`
   returns `TauriLoginPanel` at `:174` BEFORE the `!humbleLoginUserAgent` gate at `:181`, so the
   fetched UA is never consumed. REQ-34.4.1-04 recorded that this channel travelled to 34.4.1 only
   because its consumer was the deferred `<webview>`'s `useragent` attribute. The CHANNEL itself is
   out of scope here — it is re-censused in plan `40-03` alongside D-11's `humbleLoginNavigated`.
   Delete only the renderer-side fetch effect and its state.
</planning_findings>

<interfaces>
Contracts the executor needs; extracted from the live tree. No exploration required.

From `src/backend/platform/types.ts` (the shim being orphaned here, DELETED in plan 40-03):
```
export interface WebviewTag extends HTMLElement { ... }   // :167
export interface DidFailLoadEvent { validatedURL: ... }   // :125
```

From `src/frontend/screens/WebView/loginRoutes.ts`:
```
export function isLoginPathname(pathname: string): boolean   // :29
export const EPIC_LOGIN_URL, GOG_LOGIN_URL, ZOOM_LOGIN_URL
```

From `src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx` (SURVIVES, edited in
plan 40-10, untouched here):
```
const WebviewUnavailablePanel = ({ url }: { url: string }) => ...
```

From `src/frontend/screens/WebView/components/TauriLoginPanel.tsx` (SURVIVES, untouched):
```
({ runner, state }: Props)   // :87
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Census Model A by predicate against the live tree</name>
  <read_first>
    - src/frontend/screens/WebView/index.tsx (full file — the pre-image being rewritten)
    - src/frontend/screens/WebView/components/HumbleLoginSurface.tsx (full file)
    - src/frontend/components/UI/WebviewControls/index.tsx (full file)
    - src/backend/platform/types.ts lines 120-235 (the shim's own "Consumed by" comment, which
      names its call sites and is itself a stale-line-number hazard)
    - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-CONTEXT.md
      §"Retiring Model A" (D-09 through D-13)
  </read_first>
  <files>.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md</files>
  <action>
Run four predicate greps over `src/` and record every hit with file, line and a one-line verdict
(DELETE / KEEP-COMMENT-ONLY / OUT-OF-SCOPE-THIS-PLAN). The four predicates are the literal tokens
`<webview`, `WebviewTag`, `DidFailLoadEvent`, `webviewPreloadPath` (which subsumes
`getWebviewPreloadPath`). Scope each grep to `src/` and record the exact command used.

Split every hit into four buckets and label them in the artifact:
  (a) LIVE RENDER OR EFFECT — deleted by this plan
  (b) TYPE SHIM AND ITS PIN — `src/backend/platform/types.ts`, `src/backend/platform/index.ts`,
      `src/backend/platform/__tests__/types.usage.test.ts` — OUT OF SCOPE, owned by plan `40-03`
  (c) BACKEND DECLARED-EMPTY RETURN — `src/backend/sidecar/appShellFlowRegistration.ts:262-266`
      and its test `src/backend/sidecar/__tests__/appShellFlows.test.ts:398` — KEEP, this is the
      declared-dead return that MAKES the census correct; it is not Model A
  (d) COMMENT-ONLY MENTIONS — prose in `useTauriOAuthLogin.ts`, `loginRoutes.ts`,
      `oauthLoginCapture.ts`, `tauriChildWindows.ts`, `Login/components/HumbleLogin/index.scss`,
      `common/humble/loginChromeCss.ts` — record each and decide KEEP or REWORD; none are code.

Then run a fifth, NEGATIVE predicate that proves the ROADMAP's stale line: grep for
`Sidebar/index.tsx` under `src/` and record the zero result explicitly as a falsified claim, with
the Phase 34.10 NavShell replacement named as the reason. A census that silently omits it is
indistinguishable from a census that forgot to look.

Record in the artifact, verbatim, the exact reproducing command block so a later reader can re-run
the census rather than trust the counts. State the tree state the census was taken against
(`git rev-parse --short HEAD`).
  </action>
  <verify>
    <automated>test -f .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md &amp;&amp; grep -q "Reproducing command" .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md &amp;&amp; grep -q "Sidebar/index.tsx" .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - `40-MODEL-A-CENSUS.md` exists and names all four predicates as literal tokens.
    - Every hit is labelled with exactly one of DELETE / KEEP-COMMENT-ONLY / OUT-OF-SCOPE-THIS-PLAN.
    - The artifact contains a section named `Reproducing command` holding runnable grep commands.
    - The artifact records the zero-result for `Sidebar/index.tsx` as an explicitly falsified
      ROADMAP claim, naming Phase 34.10's NavShell as the reason.
    - The artifact records `git rev-parse --short HEAD` for the tree it was measured against.
    - The artifact classifies `appShellFlowRegistration.ts`'s declared-empty return as KEEP.
  </acceptance_criteria>
  <done>A predicate-derived census artifact exists that a reader can re-run, and it contradicts the ROADMAP's stale line in writing rather than by omission.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the three Model A sites and the WebviewControls component</name>
  <read_first>
    - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md (Task 1's output — the DELETE bucket is this task's worklist)
    - src/frontend/screens/WebView/index.tsx (full file)
    - src/frontend/screens/WebView/components/HumbleLoginSurface.tsx (full file)
    - src/frontend/components/UI/WebviewControls/index.tsx (full file)
    - src/frontend/screens/WebView/components/humbleLoginChromeCss.ts (full file — the helper whose only consumer dies)
    - src/frontend/screens/WebView/components/TauriLoginPanel.tsx (the surviving Model B reference; its "logged, never silent" terminal-branch discipline is the shape the surviving returns must keep)
  </read_first>
  <files>src/frontend/screens/WebView/index.tsx, src/frontend/screens/WebView/components/HumbleLoginSurface.tsx, src/frontend/screens/WebView/components/humbleLoginChromeCss.ts, src/frontend/components/UI/WebviewControls/index.tsx, src/frontend/components/UI/WebviewControls/index.css</files>
  <action>
In `src/frontend/screens/WebView/index.tsx`, delete: the `WebviewTag`/`DidFailLoadEvent` type import
(`:18`); the `WebviewControls` import (`:13`); the `webviewRef` declaration (`:69`); the
`amazonLoginData` state (`:67`) and its four read sites (`:201`, `:240`, `:251-254`, `:373`); the
`handleAmazonLogin` function (`:239-259`); the `webviewPreloadPath` state and its fetch effect
(`:265-273`); the `dom-ready`/`did-fail-load`/`page-title-updated` `useLayoutEffect` (`:275-373`);
the `did-navigate`/`did-navigate-in-page` `useEffect` (`:375-416`); the mouse back/forward
`useEffect` (`:454-483`); the `!webviewPreloadPath` guard itself (`:501`) — hoisting BOTH of its
arms to top level so the login arm keeps its `isLoginPathname(pathname)` condition and the
store/wiki arm keeps its `window.api.logInfo` line plus `return <WebviewUnavailablePanel url={startUrl} />`
UNGUARDED; and the entire trailing `<div className="WebView">` return (`:531-596`) including the
`<webview>` element, the `WebviewControls` render, the `UpdateComponent` loading render, the
`LoginWarning` render and the adtraction `Dialog`.

Do NOT delete: `validStoredUrl` (`:37-52`), the `urls` route map (`:190-204`), the `startUrl`
resolution and `sessionStorage` restore (`:205-221`), the `store-page` query-param branch
(`:215-221`), the `showLoginWarningFor` state and its effect (`:418-448`), the
`showAdtractionWarning`/`dontShowAdtractionWarning` state, the `runner === 'humble'` early return
(`:492-499`), `handleSuccessfulLogin`, `handleTauriOAuthSuccess`, `handleTauriOAuthCancelled`,
`mountedRef`, or the `useTauriOAuthLogin` call. All of those are Model B or route logic and are
re-consumed by later plans in this phase.

`urls['/loginweb/nile']` currently reads `amazonLoginData ? amazonLoginData.url : ''`. With the
state gone it becomes the constant `''`. Keep the key present with the `''` value and leave a
comment naming the folded todo
(`.planning/todos/pending/2026-09-01-webview-amazonlogindata-is-permanently-null.md`) and the fact
that `useTauriOAuthLogin.ts`'s own `getAmazonLoginData()` is the single remaining owner of that
fetch. Do NOT re-add a fetch here — the double-spawn cost is measured and recorded at `:225-236`.

Any state left with no reader after these deletions (`loading`, `setLoading`, `t`, `connectivity`,
`epic`, `gog`, `amazon`, `zoom` destructures) must be removed too — re-derive which by compiling,
not by reading. `tsc --noEmit` and `pnpm lint` are the arbiters.

In `src/frontend/screens/WebView/components/HumbleLoginSurface.tsx`, delete: the `WebviewTag` import
(`:7`); `webviewRef` (`:34`); the `webviewPreloadPath` state and fetch effect (`:36-44`); the
`humbleLoginUserAgent` state and its `humbleGetLoginUserAgent` fetch effect; the D-17 navigation
relay `useLayoutEffect` (`:141-160`); the `attachHumbleLoginChromeCss` `useLayoutEffect` (`:166-172`)
and its import; the `!webviewPreloadPath` guard (`:174-176`) — hoisting `return <TauriLoginPanel
runner="humble" state={humbleLoginState} />` to be the component's unconditional return; the
`!humbleLoginUserAgent` gate (`:180-182`); and the `<webview>` render (`:185-194`). Update the file's
module doc comment (`:25-31`) so it no longer describes a render order that no longer exists.

Delete `src/frontend/screens/WebView/components/humbleLoginChromeCss.ts` and
`src/frontend/components/UI/WebviewControls/index.tsx` and
`src/frontend/components/UI/WebviewControls/index.css` — remove the directory, do not leave an empty
one. `src/common/humble/loginChromeCss.ts` SURVIVES: it has three other importers
(`src/backend/humble/__tests__/loginChromeCss.test.ts`,
`src/backend/__tests__/loginChromeCssInjection.test.ts`, and the renderer helper being deleted) and
the backend injection path still consumes it. Verify that by grep before touching it.

The store chrome is REBUILT in plan `40-07`, not ported. Do not preserve `WebviewControls` "for
reference" — the census artifact and git history are the reference.
  </action>
  <verify>
    <automated>pnpm codecheck &amp;&amp; test ! -d src/frontend/components/UI/WebviewControls &amp;&amp; test ! -f src/frontend/screens/WebView/components/humbleLoginChromeCss.ts &amp;&amp; ! grep -rq "<webview" src/frontend/ &amp;&amp; ! grep -rq "WebviewTag" src/frontend/</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm codecheck` exits 0.
    - `grep -rn "<webview" src/frontend/` returns zero matches.
    - `grep -rn "WebviewTag\|DidFailLoadEvent" src/frontend/` returns zero matches.
    - `grep -rn "webviewPreloadPath" src/frontend/ | grep -v '^\s*//' ` returns zero non-comment matches.
    - `src/frontend/components/UI/WebviewControls/` does not exist.
    - `src/frontend/screens/WebView/components/humbleLoginChromeCss.ts` does not exist.
    - `src/common/humble/loginChromeCss.ts` still exists and `pnpm codecheck` is clean, proving its
      surviving importers were not broken.
    - `src/frontend/screens/WebView/index.tsx` still exports a default `WebView` component whose
      store/wiki path returns `WebviewUnavailablePanel` with no enclosing guard.
    - `src/frontend/screens/WebView/components/HumbleLoginSurface.tsx` returns `TauriLoginPanel`
      unconditionally and imports neither `WebviewTag` nor `attachHumbleLoginChromeCss`.
  </acceptance_criteria>
  <done>Model A no longer exists in the frontend; the store/wiki routes still render the panel; the Humble surface still returns TauriLoginPanel; typecheck is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Disposition every pin the deletion invalidated</name>
  <read_first>
    - src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx lines 180-330 (the `hasTwoDistinctArms` gate and its four self-tests)
    - src/frontend/screens/WebView/__tests__/humbleLoginChromeCss.test.ts lines 200-240 (the `window.api.humbleLoginNavigated()` source pin)
    - src/frontend/screens/WebView/__tests__/WebViewAmazonLoginDataSpawn.test.ts (full file)
    - meta/i18nForkTouchedFiles.json (lines 45-60 and 195-210 — the two entries this plan's deletions orphan)
    - meta/i18nGateScope.json lines 150-165 (proves which of the two artifacts is actually affected)
    - meta/genI18nGateScope.test.ts (the regeneration contract — memory records that regenerating this artifact cascades into its own pins)
  </read_first>
  <files>src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx, src/frontend/screens/WebView/__tests__/humbleLoginChromeCss.test.ts, src/frontend/screens/WebView/__tests__/WebViewAmazonLoginDataSpawn.test.ts, meta/i18nForkTouchedFiles.json</files>
  <action>
Give each invalidated pin a labelled verdict from the D-35-14-02 vocabulary (RE-POINT / RE-DERIVE /
INVERT / RETIRE) and record the verdict in a comment at the top of the changed block, naming
`REQ-40-10` and the reason. A pin removed without a written verdict is indistinguishable from a pin
lost.

`WebviewUnavailablePanel.test.tsx`'s `hasTwoDistinctArms` — the guard string
`if (!webviewPreloadPath)` no longer exists, so the gate cannot pass. Verdict: INVERT. Its purpose
was to prove the store/wiki arm is never silently re-gated behind a predicate. That purpose SURVIVES
the guard's deletion and gets sharper: rewrite the check so it extracts the WebView component body
rather than the guard block, and asserts (a) a login arm gated on `isLoginPathname(pathname)`
returning `TauriLoginPanel`, and (b) a store/wiki return of `WebviewUnavailablePanel` that is not
preceded by any `if (` between the login arm's closing brace and it. Keep all four existing
self-tests working against the new extraction — they are the anti-vacuity proof — and update their
synthetic sources to drop the guard. Add a FIFTH self-test that rejects a synthetic source in which
the guard was RE-ADDED under a new name, so the INVERT cannot be quietly undone.

`humbleLoginChromeCss.test.ts:221` — `toContain('window.api.humbleLoginNavigated()')` now pins a
deleted call. Verdict: RETIRE, with the retirement comment naming plan `40-03` as the owner of the
channel-level re-census (D-11). Do NOT weaken the assertion to a substring that still passes;
delete it and say why. Check the surrounding `describe` for assertions about
`attachHumbleLoginChromeCss` that also reference the deleted renderer helper and retire those in
the same pass; assertions that only touch `common/humble/loginChromeCss` SURVIVE.

`WebViewAmazonLoginDataSpawn.test.ts` — verdict depends on measurement. Read it first: if it pins
the absence of a `getAmazonLoginData` spawn in `WebView/index.tsx`, that property is STRENGTHENED by
this plan (the state is gone entirely) and the verdict is RE-POINT — re-point it at the surviving
comment or at the structural absence. If it pins the comment text verbatim, verdict RE-DERIVE
against the new comment. Do not retire it: the double-spawn it guards against is a measured 12.8 s
per-call regression.

`meta/i18nForkTouchedFiles.json` — remove the two entries naming files this plan deleted
(`src/frontend/components/UI/WebviewControls/index.tsx`,
`src/frontend/screens/WebView/components/humbleLoginChromeCss.ts`). BEFORE editing, run
`pnpm lint-translations` and `pnpm gen-i18n-gate-scope` and capture their exit codes and output, so
the pre-edit state is measured rather than assumed; run both again after and confirm both still
exit 0. Per project memory, regenerating this artifact cascades into the pins that guard it — if
`meta/__tests__/genI18nGateScope.test.ts` goes red, fix the pin, do not regenerate blindly with
`--rewrite-scope`.
  </action>
  <verify>
    <automated>pnpm exec jest src/frontend/screens/WebView src/frontend/components/UI meta/__tests__/genI18nGateScope.test.ts &amp;&amp; pnpm lint-translations &amp;&amp; pnpm gen-i18n-gate-scope</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec jest src/frontend/screens/WebView` exits 0.
    - `pnpm exec jest meta/__tests__/genI18nGateScope.test.ts` exits 0.
    - `pnpm lint-translations` exits 0 and `pnpm gen-i18n-gate-scope` exits 0.
    - `grep -c "WebviewControls/index.tsx\|humbleLoginChromeCss.ts" meta/i18nForkTouchedFiles.json` returns 0.
    - `WebviewUnavailablePanel.test.tsx` contains a comment carrying the literal token `INVERT` and
      the literal token `REQ-40-10`.
    - `WebviewUnavailablePanel.test.tsx` has at least five self-tests, and running the suite with the
      store-arm guard synthetically re-added produces a failure (mutation-proven, recorded in the
      SUMMARY with the exact synthetic source used).
    - `humbleLoginChromeCss.test.ts` contains a comment carrying the literal token `RETIRE` and
      naming plan `40-03`.
    - No test file was deleted wholesale; each change is a labelled edit.
  </acceptance_criteria>
  <done>Every gate invalidated by the deletion carries a written verdict, the INVERTed gate is mutation-proven in both directions, and both i18n gate scripts still exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed by this plan | This plan is subtractive: it deletes renderer code and edits test/meta artifacts. It introduces no new input path, no new process, and no new network or filesystem reach. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Tampering | `meta/i18nForkTouchedFiles.json` / `meta/i18nGateScope.json` | mitigate | The deletion orphans two entries in a BLOCKING i18n gate's input artifact. Task 3 measures both gate scripts before and after the edit and requires exit 0 on both; a blind `--rewrite-scope` regeneration is explicitly forbidden. |
| T-40-01-02 | Repudiation | `WebviewUnavailablePanel.test.tsx` | mitigate | Deleting the guard would let the store/wiki arm be silently re-gated later with no gate to catch it. Task 3 INVERTs rather than retires the gate and adds a fifth self-test that rejects a re-added guard under any name. |
| T-40-01-03 | Denial of Service | `/store/*`, `/wiki` routes | accept | D-10 requires the panel to keep working throughout. Task 2's acceptance criteria assert the unguarded `WebviewUnavailablePanel` return survives; the routes degrade to exactly today's behaviour, which is the declared baseline. |
| T-40-01-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. If any install task is introduced during execution, it is out of scope and must stop for a package-legitimacy checkpoint. |
</threat_model>

<verification>
- `pnpm codecheck` exits 0.
- `pnpm lint` exits 0 (the `--max-warnings 4157` ratchet must not be raised; deletions may lower the
  count, which is fine — do NOT lower the ratchet in this plan, that is Phase 39's artifact).
- `pnpm test` for the WebView, UI and meta suites exits 0.
- `pnpm lint-translations` and `pnpm gen-i18n-gate-scope` both exit 0.
- `python3 meta/runPlanningGates.py` still prints `7/7 planning gates passed.`
- Manual read: `src/frontend/screens/WebView/index.tsx` contains exactly one webview model — none.
</verification>

<success_criteria>
- `grep -rn "<webview\|WebviewTag\|DidFailLoadEvent" src/frontend/` returns zero matches.
- `grep -rn "webviewPreloadPath" src/frontend/ | grep -v "//"` returns zero non-comment matches.
- `src/frontend/components/UI/WebviewControls/` and
  `src/frontend/screens/WebView/components/humbleLoginChromeCss.ts` are gone.
- `40-MODEL-A-CENSUS.md` exists with a re-runnable command block and an explicit falsification of
  the ROADMAP's `Sidebar/index.tsx` line.
- Three invalidated pins each carry a labelled verdict; the INVERTed one is mutation-proven.
- Store and wiki routes still render `WebviewUnavailablePanel`.
</success_criteria>

<output>
Create `.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-01-SUMMARY.md` when done.
Record: the four predicate counts before and after; the exact synthetic source used to mutation-prove
the INVERTed gate; the pre- and post-edit exit codes of `lint-translations` and
`gen-i18n-gate-scope`; and any Model A site found by the census that this plan did NOT delete, with
the plan that owns it.
</output>
