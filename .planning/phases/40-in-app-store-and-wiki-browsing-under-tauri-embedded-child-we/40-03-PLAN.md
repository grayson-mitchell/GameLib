---
phase: 40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we
plan: 03
type: execute
wave: 3
depends_on: [40-01, 40-05]
files_modified:
  - src/backend/platform/types.ts
  - src/backend/platform/index.ts
  - src/backend/platform/__tests__/types.usage.test.ts
  - src/common/types/ipc.ts
  - src/preload/api/humble.ts
  - src/backend/sidecar/humbleLoginFlowRegistration.ts
  - src/backend/humble/ipc_handler.ts
  - src/backend/sidecar/__tests__/humbleLoginFlows.test.ts
  - src/backend/sidecar/__tests__/humbleFlows.test.ts
  - .planning/IPC-PORT-INVENTORY.md
  - meta/runPlanningGates.py
  - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/model-a-retirement-gate.py
  - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-CHANNEL-RECENSUS.md
autonomous: true
requirements: [REQ-40-10]

must_haves:
  truths:
    - "D-12: the WebviewTag / DidFailLoadEvent shim and its type-usage pin no longer exist"
    - "D-11: humbleLoginNavigated is removed ONLY if a sweep proves zero live callers, and the sweep is written down either way"
    - "D-13: a mechanical predicate gate fails if the tokens for Model A reappear under src/frontend/"
    - "D-13: the gate is mutation-proven in BOTH directions — RED against a reintroduced token, GREEN against the real post-Phase-40 tree"
    - "D-13: the gate's vocabulary is MEASURED against the actual tree before it is committed, so it cannot convict correct code"
  artifacts:
    - path: ".planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/model-a-retirement-gate.py"
      provides: "The D-13 mechanical predicate gate, discovered by meta/runPlanningGates.py"
      contains: "self-test"
    - path: ".planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-CHANNEL-RECENSUS.md"
      provides: "D-11's zero-live-callers sweep, with a verdict per channel"
      contains: "VERDICT"
  key_links:
    - from: "meta/runPlanningGates.py"
      to: "model-a-retirement-gate.py"
      via: "gate-suffix rglob discovery under .planning/"
      pattern: "MINIMUM_EXPECTED_GATES"
---

<objective>
Delete the `WebviewTag` / `DidFailLoadEvent` shim now that plan `40-01` orphaned every consumer,
re-census the two IPC channels whose only renderer callers died with it, and build the mechanical
gate that stops Model A coming back.

Purpose: D-12 says the shim exists only to prove its members are load-bearing for three call sites —
when the call sites go, so does its reason to exist. D-13 says the retirement needs a gate, and that
the gate MUST be mutation-proven in both directions, because a gate that cannot fail proves nothing
and a gate whose vocabulary is too broad convicts correct code.

Output: the shim gone; a written channel re-census with a verdict per channel; a phase-directory
gate that `meta/runPlanningGates.py` discovers, mutation-proven both ways.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-CONTEXT.md
@.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-PATTERNS.md
@.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-01-SUMMARY.md
@.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md
@.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-05-SUMMARY.md

<planning_findings>
Measured at plan time. RE-MEASURE — plan `40-01` has run since and the counts will have moved.

1. **D-12's line list is INCOMPLETE.** It names `types.usage.test.ts` lines 47, 223, 259, 277, 670.
   The file also carries `DidFailLoadEvent` assertions at 46, 184, 188, 201, 247, 249 and 679, plus
   `WebviewTag` mentions at 35, 216, 263, 264, 275, 276 and 572. Re-derive the full deletion set by
   predicate; do not work from D-12's five.

2. **`meta/runPlanningGates.py` has an anti-vacuity floor of 7** and discovers gates by an
   `rglob` on the gate-file suffix under `.planning/`. Three gates live in the 34.4.1 phase
   directory. Adding an eighth PASSES the floor without a bump because the check is a
   less-than comparison, not an equality — but leaving the floor at 7 means the gate this plan ships
   can later be deleted with every gate still reporting green. Bump the floor in the same commit.

3. **`humbleLoginNavigated` sweep (pre-`40-01`)**: the only renderer caller was
   `HumbleLoginSurface.tsx:145`, deleted by `40-01`. The backend chain survives at
   `src/preload/api/humble.ts:11`, `src/common/types/ipc.ts:156`,
   `src/backend/sidecar/humbleLoginFlowRegistration.ts:403`, `src/backend/humble/ipc_handler.ts:127`
   which calls `HumbleUser.notifyLoginNavigated()`. A grep across `src-tauri/` for the channel name,
   its snake_case form and its SCREAMING form returned zero at plan time. That is the evidence D-11
   asks for, but it is a snapshot — re-run it, and additionally search for callers of
   `notifyLoginNavigated` directly, because the channel could be dead while the behaviour is live
   under a different name.

4. **A SECOND channel enters D-11's scope, unnamed in CONTEXT.md**: `humbleGetLoginUserAgent`. Its
   only renderer consumer was `HumbleLoginSurface`'s UA fetch, deleted by `40-01`, and
   REQ-34.4.1-04 recorded that it travelled to 34.4.1 only because its consumer was the deferred
   `<webview>` element's user-agent attribute. Apply D-11's exact discipline to it.

5. **`.planning/IPC-PORT-INVENTORY.md`'s Totals deliberately sit one above the union.** CONTEXT.md
   is explicit: do NOT "fix" that. If a channel is removed, follow the inventory's own convention and
   leave the deliberate offset alone.

6a. **This plan is sequenced AFTER plan `40-05`, deliberately.** Both touch
   `src/common/types/ipc.ts` and `.planning/IPC-PORT-INVENTORY.md` — `40-05` ADDS the store-embed
   channels, this plan REMOVES any channel its sweep condemns. Running them in parallel would have
   them fight over both files, and reconciling the inventory needs `40-05`'s additions already
   present. So this plan applies its delta on top of `40-05`'s, and Task 1 reads `40-05-SUMMARY.md`
   for the added-channel list before recomputing any total.

6. **`types.usage.test.ts` may entangle surviving types.** CONTEXT.md flags this as Claude's
   Discretion. Line 259 declares a props object typed against the shim and lines 263-277 explain that
   the shim must stay assignable to a structural stand-in — check whether that stand-in is used by
   anything that survives before removing the whole section.
</planning_findings>

<interfaces>
From `src/backend/platform/types.ts` — the deletion target:
```
export interface DidFailLoadEvent { validatedURL: string }        // :125
export interface WebviewTag extends HTMLElement {                 // :167
  insertCSS(css: string): Promise<string>
  addEventListener(event, listener: (e: DidFailLoadEvent) => void, opts): void   // :184
  removeEventListener(event, listener: (e: DidFailLoadEvent) => void): void      // :189
}
```

From `.planning/phases/34.4.1-.../ported-channels-gate.py` — the gate contract to copy exactly:
```
no-args invocation  -> CI mode: walk the real src/ tree, CHECK a committed artifact, write nothing
--write             -> regenerate the committed artifact deliberately (never the default)
--self-test         -> prove every check function rejects known-bad input before a clean run is trusted
```

From `meta/runPlanningGates.py`:
```
GATE_SUFFIX             = the gate filename suffix used by rglob discovery under .planning/
MINIMUM_EXPECTED_GATES  = 7      # anti-vacuity floor, currently one below the post-plan count
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-census the two orphaned channels and record a verdict for each (D-11)</name>
  <read_first>
    - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md (plan 40-01's output)
    - src/backend/sidecar/humbleLoginFlowRegistration.ts lines 1-60 and 395-420 (the module doc comment naming each channel's chain, and the humbleLoginNavigated listener arm)
    - src/backend/humble/ipc_handler.ts lines 118-135 (the addListener registration)
    - src/backend/humble/user.ts lines 35-60 (the comment explaining what the relay is FOR) plus the notifyLoginNavigated implementation
    - src/common/types/ipc.ts line 156 (the channel's type declaration)
    - src/preload/api/humble.ts (the preload binding)
    - .planning/IPC-PORT-INVENTORY.md lines 260-280 (the Phase 34.4.1 channel list and the Totals convention) AND its Phase 40 section as plan 40-05 left it
    - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-05-SUMMARY.md (the store-embed channels 40-05 already added — this plan's delta lands on top of them, so any total is recomputed from the post-40-05 state, never from the pre-phase state)
  </read_first>
  <files>.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-CHANNEL-RECENSUS.md, src/common/types/ipc.ts, src/preload/api/humble.ts, src/backend/sidecar/humbleLoginFlowRegistration.ts, src/backend/humble/ipc_handler.ts, .planning/IPC-PORT-INVENTORY.md</files>
  <action>
Sweep for live callers of `humbleLoginNavigated` AND `humbleGetLoginUserAgent`, and record a
`VERDICT:` of `REMOVE` or `KEEP` for each, with the evidence that produced it.

The sweep must cover four surfaces, because D-11's hazard is exactly a caller in a surface the grep
did not reach:
  (a) `src/` TypeScript — every `window.api` reference and every direct import of the preload binder;
  (b) `src-tauri/` Rust — the channel name, its snake_case form and its SCREAMING_SNAKE form;
  (c) the BEHAVIOUR rather than the name — for `humbleLoginNavigated`, find every caller of
      `notifyLoginNavigated` directly. The channel can be dead while the behaviour is live under a
      different channel; removing the channel is then correct and removing `notifyLoginNavigated`
      is not;
  (d) the test tree — `src/backend/sidecar/__tests__/humbleLoginFlows.test.ts:163` and
      `src/backend/sidecar/__tests__/humbleFlows.test.ts:453` both carry the channel in a
      send-channel list. A removal must update those lists; a KEEP must leave them alone.

Record per channel: the four sweep commands verbatim, the hit count per surface, the verdict, and —
for a REMOVE — the exhaustive list of files that must change. For a KEEP, state plainly WHY (for
example, a live native driver) so the next reader does not re-litigate it.

If a verdict is REMOVE, execute the removal in this task across `src/preload/api/humble.ts`,
`src/common/types/ipc.ts`, `src/backend/sidecar/humbleLoginFlowRegistration.ts`,
`src/backend/humble/ipc_handler.ts` and the two send-channel test lists, then update
`.planning/IPC-PORT-INVENTORY.md` per its own stated convention. Do NOT alter the inventory's
deliberate Totals offset — CONTEXT.md names it as not-a-defect.

If a verdict is KEEP, change nothing for that channel and say so. A channel kept with a written
reason is a better outcome than a channel deleted on a partial sweep: the failure mode D-11 guards
against is silently breaking Humble cookie revalidation.
  </action>
  <verify>
    <automated>pnpm codecheck &amp;&amp; pnpm exec jest src/backend/sidecar/__tests__/humbleLoginFlows.test.ts src/backend/sidecar/__tests__/humbleFlows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `40-CHANNEL-RECENSUS.md` carries exactly two `VERDICT:` lines, one per channel, each reading
      `REMOVE` or `KEEP`.
    - Each channel section lists four sweep commands with a hit count against each surface.
    - The behaviour-level sweep for `humbleLoginNavigated` names every caller of
      `notifyLoginNavigated`, not only callers of the channel.
    - `pnpm codecheck` exits 0 and `pnpm exec jest src/backend/sidecar` exits 0.
    - If nothing was removed, `git diff --stat .planning/IPC-PORT-INVENTORY.md` is empty. If
      something was removed, the SUMMARY quotes the inventory's before and after Totals lines
      verbatim and they differ only as that file's own convention requires.
    - No channel was removed without a `REMOVE` verdict recorded first.
  </acceptance_criteria>
  <done>Both orphaned channels carry a written verdict backed by a four-surface sweep, and any removal is complete across all five layers plus the inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the WebviewTag / DidFailLoadEvent shim and unpick its type pin (D-12)</name>
  <read_first>
    - src/backend/platform/types.ts lines 1-30 (the file's doc comment about dotted-member references) and lines 120-235 (the webview-element section, both interfaces, and the "Consumed by" comment naming the now-deleted call sites)
    - src/backend/platform/index.ts lines 1115-1140 (the barrel re-export block)
    - src/backend/platform/__tests__/types.usage.test.ts lines 30-60, 180-290, 560-580, 660-690
    - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md (the shim bucket, which is this task's worklist)
  </read_first>
  <files>src/backend/platform/types.ts, src/backend/platform/index.ts, src/backend/platform/__tests__/types.usage.test.ts</files>
  <action>
Delete the `WebviewTag` interface, the `DidFailLoadEvent` interface, their section header and the
"Consumed by" comment block from `src/backend/platform/types.ts`; drop both names from the
`src/backend/platform/index.ts` re-export list; and remove their assertions from
`src/backend/platform/__tests__/types.usage.test.ts`.

Before deleting anything from `types.ts`, run the entanglement check CONTEXT.md flags as Claude's
Discretion: confirm no SURVIVING interface in that file references either name in a member
signature, an extends clause, or a union. `WebviewTag` extends `HTMLElement`, so nothing inherits
from it, but a surviving type could still name it as a parameter. Record the result in the SUMMARY
as an explicit line, not as silence.

In `types.usage.test.ts`, re-derive the deletion set by predicate rather than from D-12's five line
numbers, which are incomplete. Remove both names from the import list, delete their assertion
sections, and delete any helper local to those sections. Where a section mixes a deleted type with a
surviving one — the structural stand-in around line 259 is the flagged candidate — keep the
surviving assertion and rewrite its comment so it no longer explains itself in terms of a type that
no longer exists. A comment describing a deleted type is a false statement the codebase makes about
itself, which is the class of defect this phase exists to remove.

Leave a short retirement note where the section was, naming `REQ-40-10` and the fact that Tauri has
no webview element. Do NOT leave either interface commented out — a commented-out interface is
indistinguishable from a deferral.

`src/backend/sidecar/appShellFlowRegistration.ts`'s `getWebviewPreloadPath` declared-empty return
SURVIVES untouched, along with its test at `src/backend/sidecar/__tests__/appShellFlows.test.ts:398`.
It is a declared-dead backend return, not Model A, and plan `40-01`'s census classified it KEEP.
  </action>
  <verify>
    <automated>pnpm codecheck &amp;&amp; pnpm exec jest src/backend/platform src/backend/sidecar/__tests__/appShellFlows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm codecheck` exits 0 and `pnpm exec jest src/backend/platform` exits 0.
    - `grep -rn "WebviewTag" src/` returns zero matches repo-wide.
    - `grep -rn "DidFailLoadEvent" src/` returns zero matches repo-wide.
    - `git diff --stat src/backend/sidecar/appShellFlowRegistration.ts` is empty.
    - `types.ts` contains a retirement note carrying the literal token `REQ-40-10`.
    - `grep -c "^// export interface" src/backend/platform/types.ts` returns 0, proving nothing was
      commented out instead of deleted.
    - The SUMMARY records the entanglement-check result as an explicit sentence.
  </acceptance_criteria>
  <done>The shim is gone from the type file, the barrel and its pin; typecheck and the platform suite are green; the declared-dead backend return is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Build the D-13 mechanical retirement gate, mutation-proven both ways</name>
  <read_first>
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py (the smallest precedent gate — read in full for the contract)
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py lines 1-120 (its doc comment stating the contract and the CI-mode entrypoint)
    - meta/runPlanningGates.py (full file — the discovery glob, the gate suffix, and the anti-vacuity floor)
    - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-MODEL-A-CENSUS.md (the post-deletion tree the gate must pass against)
    - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-CONTEXT.md D-13
  </read_first>
  <files>.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/model-a-retirement-gate.py, meta/runPlanningGates.py</files>
  <action>
Write `model-a-retirement-gate.py` in the phase directory, following the 34.4.1 gate contract
exactly: no-args invocation is CI mode (walk the real tree, check, write nothing); a write flag
regenerates any committed artifact deliberately and is never the default; a self-test flag proves
every check function rejects known-bad input before a clean run is trusted.

The predicate is narrow by design: fail if the literal tokens for the webview element, the shim type
name, or the preload-path identifier appear in any non-comment line under `src/frontend/` TypeScript
and TSX files. Take the three exact tokens from D-13.

MEASURE the vocabulary against the real tree BEFORE committing to it. Three false-positive risks are
already known and each must be handled and covered by a self-test:
  - `WebviewBuilder` and `WebviewUrl` appear in the new Rust code. The gate is scoped to
    `src/frontend/`, which excludes `src-tauri/`. Assert that scoping explicitly in the gate rather
    than relying on it, so a later scope widening cannot silently import the false positive.
  - `WebviewUnavailablePanel` SURVIVES and is a legitimate frontend identifier containing the
    substring `Webview`. The shim token must match as a whole word: a longer identifier that merely
    contains it as a prefix must NOT fail, while the bare token must. Prove both cases.
  - Comment lines. Project memory records that a count-based grep counts comments and that a gate's
    own header prose can be self-invalidating. Strip line comments, block comments and JSX comment
    expressions before matching, and include the gate's own docstring tokens as a self-test case
    proving the gate does not convict itself.

Mutation-prove in BOTH directions and record both runs in the SUMMARY with the exact commands:
  - RED: with a webview element temporarily added to a real frontend file, the gate exits non-zero
    and names the file and line. Restore the file afterwards and confirm `git status` is clean.
  - GREEN: against the real post-Phase-40 tree, the gate exits 0.
A gate observed only green is indistinguishable from a gate that cannot fail.

Bump the anti-vacuity floor in `meta/runPlanningGates.py` from 7 to 8 in the same change and update
the comment above it to name this gate. Leaving it at 7 would let this gate be deleted later with
every gate still reporting green, which is exactly the property that constant exists to hold.

Confirm the discovery glob finds the new file: `python3 meta/runPlanningGates.py` must report
`8/8 planning gates passed.`
  </action>
  <verify>
    <automated>python3 .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/model-a-retirement-gate.py --self-test &amp;&amp; python3 .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/model-a-retirement-gate.py &amp;&amp; python3 meta/runPlanningGates.py</automated>
  </verify>
  <acceptance_criteria>
    - The gate exits 0 with no arguments against the real tree.
    - The gate exits 0 with its self-test flag, and its self-test covers at least five cases: each of
      the three tokens rejected, `WebviewUnavailablePanel` accepted, and a comment-only occurrence
      accepted.
    - `python3 meta/runPlanningGates.py` prints `8/8 planning gates passed.` and exits 0.
    - `grep -vn '^\s*#' meta/runPlanningGates.py | grep -c "MINIMUM_EXPECTED_GATES = 8"` returns 1.
    - The SUMMARY records the RED run: the exact file mutated, the gate's non-zero exit code, its
      output naming that file and line, and `git status --porcelain` empty afterwards.
    - The SUMMARY records the GREEN run against the unmutated tree.
    - The gate writes nothing when invoked with no arguments (`git status --porcelain` is empty after
      a bare run).
  </acceptance_criteria>
  <done>A phase-directory gate is discovered by the runner, passes against the real tree, fails against a reintroduced token, and the anti-vacuity floor covers it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar IPC | Task 1 may remove two channels from the IPC surface. Removing a channel narrows the boundary; removing one a live caller needs breaks a security-relevant behaviour (Humble cookie revalidation). |
| CI → planning gates | Task 3 adds a gate that CI runs. A gate that cannot fail, or one that fails on correct code, degrades the signal every other gate is read against. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03-01 | Denial of Service | `humbleLoginNavigated` / `HumbleUser.notifyLoginNavigated` | mitigate | Removing a channel a native driver still needs would break Humble cookie revalidation silently — no error, no log. Task 1's sweep is four-surfaced and includes a behaviour-level search for `notifyLoginNavigated` callers, and a KEEP verdict with a written reason is an accepted outcome. |
| T-40-03-02 | Repudiation | `model-a-retirement-gate.py` | mitigate | A gate observed only green cannot be distinguished from a gate that cannot fail. Task 3 requires a recorded RED run against a real mutated file plus a clean `git status` afterwards, and a self-test with at least five cases including two negative controls. |
| T-40-03-03 | Tampering | `meta/runPlanningGates.py` anti-vacuity floor | mitigate | Leaving the floor at 7 while shipping an eighth gate would let the new gate be deleted with CI still green. The floor is bumped to 8 in the same change and the criterion greps the non-comment line to prove it. |
| T-40-03-04 | Information Disclosure | `.planning/IPC-PORT-INVENTORY.md` | accept | The inventory records channel names only, no secrets. Its deliberate Totals offset is preserved rather than "fixed", per CONTEXT.md. |
| T-40-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. The gate is stdlib Python matching its three predecessors. If any install becomes necessary, stop for a blocking package-legitimacy checkpoint. |
</threat_model>

<verification>
- `pnpm codecheck` exits 0.
- `pnpm exec jest src/backend/platform src/backend/sidecar` exits 0.
- `grep -rn "WebviewTag\|DidFailLoadEvent" src/` returns zero matches.
- `python3 meta/runPlanningGates.py` prints `8/8 planning gates passed.`
- The new gate has a recorded RED run and a recorded GREEN run.
- `40-CHANNEL-RECENSUS.md` exists with two verdicts.
</verification>

<success_criteria>
- The `WebviewTag` / `DidFailLoadEvent` shim, its barrel re-export and its type-usage pin are gone,
  with no interface left commented out.
- Both orphaned channels carry a four-surface sweep and a written REMOVE or KEEP verdict.
- A mechanical predicate gate exists in the phase directory, is discovered by the runner, and is
  mutation-proven in both directions with both runs recorded.
- The anti-vacuity floor covers the new gate.
- `getWebviewPreloadPath`'s declared-empty backend return is untouched.
</success_criteria>

<output>
Create `.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-03-SUMMARY.md` when done.
Record: the two channel verdicts with their per-surface hit counts; the entanglement-check sentence
for `types.ts`; the exact RED-run command, mutated file and gate output; the GREEN-run command; and
the before/after value of the anti-vacuity floor.
</output>
