---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 22
type: execute
wave: 4
gap_closure: true
closes_findings: [IN-03]
depends_on: ["34.9-21"]
files_modified:
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [REQ-34.9-08]

must_haves:
  truths:
    - "All six code-review findings (CR-01, WR-01, WR-02, IN-01, IN-02, IN-03) are enumerated from 34.9-REVIEW.md itself and each is mapped to either a landed fix naming its plan ID or a dated deferred-items.md item number — zero unmapped, the exact failure 34.9-17 committed."
    - "No document in this phase claims verify:runner-bundle is an automated tripwire without stating, in the same passage, that its coverage is arm64-only, local-script-only, and not in CI."
    - "Whether the wired guard's failing direction was PROVEN is read only from 34.9-GUARD-PROOF.md's own verdict — never from a plan Summary's claim, never from a green test suite, and never from this plan's expectations."
    - "Everything not fixed by gap cycle 2 has a dated ledger entry with a blocker mechanism, a named precondition and an OWNER — items 11, 12 and 13."
    - "ROADMAP.md carries exactly one 34.9-17-PLAN.md row, and the phase is left OPEN pending re-verification of truth 8 — no document in this plan marks Phase 34.9 complete."
  artifacts:
    - path: ".planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md"
      provides: "the six-finding disposition sweep plus deferred items 11-13"
      contains: "Code-review finding disposition"
    - path: ".planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md"
      provides: "the corrected, scope-carrying tripwire claim with a dated correction marker; RUN RECORD data untouched"
      contains: "[Corrected 2026-08-12 — plan 34.9-22]"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.9-08's row and checkbox reconciled to the post-cycle-2 truth"
    - path: ".planning/ROADMAP.md"
      provides: "gap cycle 2 plan block, corrected status note, single 34.9-17 row"
    - path: ".planning/STATE.md"
      provides: "Phase 34.9 post-cycle-2 position and hand-verified progress arithmetic"
  key_links:
    - from: ".planning/phases/.../deferred-items.md"
      to: ".planning/phases/.../34.9-REVIEW.md"
      via: "one disposition row per review finding ID, derived by grep from the review itself"
      pattern: "CR-01|WR-01|WR-02|IN-01|IN-02|IN-03"
    - from: ".planning/REQUIREMENTS.md and .planning/ROADMAP.md"
      to: ".planning/phases/.../34.9-GUARD-PROOF.md"
      via: "every guard claim citing the proof document and its verdict"
      pattern: "34.9-GUARD-PROOF"
---

<objective>
Close verification truth 8's second half: make the phase's own record TRUE about what the
`verify:runner-bundle` guard now does and does not cover, and give every review finding that was
not fixed a dated, owned home.

Purpose: gap cycle 1's reconciliation plan (34.9-17) wrote a 10-item ledger and still missed **all
six** code-review findings. `34.9-VERIFICATION.md`'s own words: CR-01 "was neither fixed nor triaged
into a dated, owned deferral — it fell through the phase's own reconciliation step." The mechanism
behind that miss is a standing project rule: **an audit whose unit is coarser than the defect's unit
cannot find the defect.** 34.9-17 swept at the granularity of *descoped requirements* and never
enumerated review findings at all. This plan sweeps at the granularity of *finding IDs*, by
set-difference, and records the difference. Do not sample. Sweep.

Output: `deferred-items.md` gains a six-row finding-disposition table and items 11-13; the
overclaiming tripwire prose in `34.9-LIVE-GATE-RERUN.md`, `ROADMAP.md` and `REQUIREMENTS.md` is
corrected to carry its own scope; `ROADMAP.md`'s duplicate `34.9-17` row is removed and gap cycle 2
is recorded; `STATE.md` carries the post-cycle-2 position.

**This plan does not close the phase.** `34.9-VERIFICATION.md` is `status: gaps_found` and truth 8
must be re-scored by the verifier against landed evidence. Leave the phase OPEN and name
re-verification as the explicit next step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-VERIFICATION.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-REVIEW.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md

<interfaces>
<!-- Decisions already made by the gap-cycle-2 outline. Do NOT re-derive, re-litigate or expand. -->

**The six findings and their intended dispositions** (`34.9-PLAN-OUTLINE.md`, "Fix vs defer, one
line each"). This table is the EXPECTATION, not the evidence — Task 1 must derive the actual
disposition from the landed artifacts and record any disagreement rather than transcribing this:

| Finding | Intended disposition | Intended home |
|---|---|---|
| CR-01 | FIX | 34.9-18 (throw) + 34.9-20 (wiring) + 34.9-21 (live proof) |
| WR-01 | FIX | 34.9-18 (`isContainedSymlinkTarget`) |
| WR-02 | FIX | 34.9-19 (`stub-absent` fixture) |
| IN-01 | FIX | 34.9-19 (comment precision only) |
| IN-02 | FIX | 34.9-19 (comment downgraded to defense-in-depth) |
| IN-03 | DEFER | `deferred-items.md` item 11, this plan |

**The three deferral entries this plan owns.**

- **Item 11 — IN-03.** The F-34.9-02 fix (`clean:dist-mac`, `meta/cleanDistMac.ts`) is macOS-only.
  `cleanDistMac.ts`'s own header explains the general electron-builder behaviour — it clears only
  the target-specific subdirectory it is about to populate, never top-level `dist/` — and that
  description is not macOS-specific, so `dist:win`/`dist:linux` plausibly carry the same
  stale-artifact false-pass. Genuine follow-up work, not a fence. Needs a dated `OWNER:`.
- **Item 12 — arm64-only guard coverage.** The wiring hardcodes `--arch=arm64` because
  `build/bin/x64/darwin` holds onefile binaries and would fail `verify:runner-bundle`'s file-count
  floor. State the x64 consequence as **UNPROVEN**, never as "x64 is fine" and never as "x64 is
  broken".
- **Item 13 — the guard is never exercised in CI.** `build-base.yml:48` runs `pnpm dist:mac` and
  `draft-release-mac.yml:37` runs `pnpm release:mac`, so the npm-script wiring is nominally reachable
  from CI — but the macOS CI leg **cannot reach the build at all**, because `install-deps` runs
  `pnpm download-helper-binaries`, which throws on the six `PENDING-CI-PUBLISH` sentinels in
  `meta/runnersOnedirDigests.json` (descoped REQ-34.9-02/03/04, `deferred-items.md` items 1-3).
  State this as **UNPROVEN in CI**, with that transitive blocker named.

**The scope fence is UNCHANGED and must not be reopened by any edit in this plan:**
REQ-34.9-02/03/04 (CI x64 leg, downloader, digests), REQ-34.9-09 (cold ratio — no third measurement
attempt), Tauri-PACKAGED resolution, real-certificate signing/notarization, upstream PR submission.
Existing `deferred-items.md` items 1-6 stay exactly as they are. **No new REQ IDs** — REQ-34.9-08
owns this entire cycle.

**The overclaiming passages, by file and locator:**

| File | Locator | The claim as it stands |
|---|---|---|
| `34.9-LIVE-GATE-RERUN.md` | ~line 75, Section 1's 34.9-13 bullet | "This makes Scored Item 2's `verify:runner-bundle` step a genuine tripwire against a regression of F-34.9-01" |
| `34.9-LIVE-GATE-RERUN.md` | ~line 509, Scored Item 2 criterion 6 | "this is the automated tripwire that, per Section 1 above, now fires on the exact F-34.9-01 shape if it regresses" |
| `.planning/ROADMAP.md` | ~lines 2459-2461, Phase 34.9 status note | "with plan 34.9-13 adding automated framework-structural-integrity enforcement to `pnpm verify:runner-bundle` so a regression cannot go silent again" |
| `.planning/REQUIREMENTS.md` | ~line 834, REQ-34.9-08 checkbox | "**Automated coverage added (plan 34.9-13):** ... enforces framework structural integrity so a regression cannot go silent again" |

**Conditional-outcome handling — this plan runs AFTER 34.9-21, whose verdict is not known at
planning time.** The branch is selected from `34.9-GUARD-PROOF.md`'s own frontmatter verdict, read
directly. A plan Summary's claim, a green `pnpm test:ci` run, and this plan's own expectations are
all **inadmissible** as the branch selector. Both branches are written below; execute exactly one
and state in the Summary which, and the verdict string that selected it.

**Ledger-tooling trap, standing and recurring:** `gsd-sdk state.record-session` has corrupted
`STATE.md` before — on 2026-08-09 it set `completed_plans` 247→248 and `percent` 92→65. Re-read
`STATE.md` after any SDK write and hand-correct it. Separately, `STATE.md`'s snapshot is known to go
stale against a concurrent session (its `stopped_at` reads `34.5-50` while `34.5-51` is committed),
so recompute the plan counts from the repository, not from the frontmatter's own prior values.

**Redaction discipline:** no SteamID, no account identifier, no PKCE value. `code_verifier`,
`serial` and `client_id` may appear only as field NAMES inside the existing item 9 prohibition,
never as values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep all six review findings by set-difference, and write deferred items 11-13</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md</files>
  <read_first>
    - `.planning/phases/34.9-.../34.9-REVIEW.md` — the source of list A. Derive the finding IDs by grepping its `^### ` headings; do NOT copy the list from this plan's `<interfaces>` table, which is an expectation and could be wrong.
    - `.planning/phases/34.9-.../34.9-18-SUMMARY.md`, `34.9-19-SUMMARY.md`, `34.9-20-SUMMARY.md`, `34.9-21-SUMMARY.md` — the source of list B. Each carries a `closes_findings` frontmatter field and a self-check; read what actually landed, not what was planned.
    - `.planning/phases/34.9-.../deferred-items.md` — the existing 10-item ledger. Match its format exactly: `### N. Title`, then **What it is**, then the blocker stated as a MECHANISM (not a summary), then a **Named precondition**, then an `OWNER:` line carrying a date.
    - `.planning/phases/34.9-.../34.9-VERIFICATION.md` — truth 8's `missing:` list, which names "a dated ledger entry" as an acceptable closure route for anything not fixed.
    - `.planning/phases/34.9-.../34.9-17-SUMMARY.md` — the reconciliation that missed all six findings. Grep it for `CR-01|WR-0|IN-0|REVIEW` and observe that it returns nothing. That absence is the defect this task exists to not repeat.
  </read_first>
  <action>
    Append two new sections to `deferred-items.md`. Do not alter items 1-10; their text is settled
    and any edit to them reopens a closed scope decision.

    **Section A — a new heading `## Code-review finding disposition (2026-08-12)`.** Open it with
    one paragraph stating why this section exists: `34.9-REVIEW.md`'s six findings appeared in no
    ledger after gap cycle 1's reconciliation, and this section is the set-difference that closes
    that class of miss — an audit at the granularity of the defect, not of the requirement.

    Build the table by set-difference, in this order, and show your work in the Summary:
    1. **List A** — every finding ID in `34.9-REVIEW.md`, obtained by grepping its `^### ` headings
       and extracting the `CR-`/`WR-`/`IN-` prefixed identifier. Record the raw grep output in the
       Summary. Do not hand-enumerate.
    2. **List B** — every finding ID claimed closed by a landed gap-cycle-2 plan, obtained from the
       `closes_findings` frontmatter of `34.9-18-SUMMARY.md`, `34.9-19-SUMMARY.md`,
       `34.9-20-SUMMARY.md` and `34.9-21-SUMMARY.md`, cross-checked against each Summary's own
       self-check section (a `closes_findings` entry whose Summary body reports the fix did not land
       counts as NOT closed, and the disagreement is recorded verbatim).
    3. **A minus B** — every ID in A with no landed fix. Each of these MUST receive a
       `deferred-items.md` item number in this same plan, or the sweep is incomplete.

    Then write the table with columns `Finding | Severity | Disposition | Evidence`. `Disposition`
    is one of `FIXED` or `DEFERRED`, nothing else — no "partially", no "mostly". `Evidence` for a
    `FIXED` row names the plan ID that landed it and the file it changed; `Evidence` for a
    `DEFERRED` row names the `deferred-items.md` item number. Every one of the six rows must have a
    non-empty `Evidence` cell. Close the section with an explicit one-line count statement: how many
    IDs were in list A, how many mapped to a landed fix, how many mapped to a ledger item, and that
    the unmapped count is zero.

    **Section B — three new numbered entries under the existing ledger's numbering, continuing at
    11.** Follow items 1-10's shape exactly.

    - `### 11. IN-03 — the F-34.9-02 stale-artifact fix is macOS-only`. What it is: `clean:dist-mac`
      runs first in `dist:mac`/`release:mac` only; `dist:win` and `dist:linux` have no counterpart.
      Blocker, as a mechanism: electron-builder clears only the target-specific subdirectory it is
      about to populate and never top-level `dist/`, which is exactly F-34.9-02's root cause and is
      not macOS-specific — a failed `dist:win`/`dist:linux` plausibly leaves stale `.exe`/`.AppImage`/
      `latest-*.yml` behind with the same "did the build produce an artifact?" false pass. State
      plainly that this is UNCONFIRMED on those two platforms — it is a plausible generalization of a
      confirmed macOS mechanism, not an observed win/linux defect. Named precondition: a win or linux
      build run that reproduces the stale-artifact false pass, or a decision to generalize
      `cleanDistMac.ts` into a platform-parameterized `cleanDist.ts` without waiting for one.
      `OWNER:` a follow-up packaging phase / developer action, dated 2026-08-12.

    - `### 12. The wired guard covers arm64 only`. What it is: the `verify:runner-bundle` invocation
      wired into `dist:mac` and `release:mac` by plan 34.9-20 hardcodes `--arch=arm64`. Blocker, as
      a mechanism: `build/bin/x64/darwin` holds onefile binaries, which have no `_internal`
      directory and no `Python.framework`, so pointing the guard at x64 fails its file-count floor
      for a reason unrelated to symlink integrity — the guard's own preconditions are not met by an
      x64 tree. State the coverage consequence as **UNPROVEN, not asserted**: whether an x64 darwin
      onedir tree would pass or fail the framework-structure checks has never been observed, because
      no x64 onedir tree exists anywhere (`deferred-items.md` item 1). Do not write that x64 is
      unprotected, and do not write that x64 is unaffected. Named precondition: an x64 darwin onedir
      tree existing on disk (transitively, REQ-34.9-02's own precondition). `OWNER:` the same
      follow-up phase that owns item 1, dated 2026-08-12.

    - `### 13. The guard has never been exercised in CI`. What it is: the wiring is npm-script-level,
      so `build-base.yml`'s `pnpm dist:mac` and `draft-release-mac.yml`'s `pnpm release:mac` would
      each invoke it — nominally. Blocker, as a mechanism: the macOS CI leg cannot reach the build
      step at all, because `install-deps` runs `pnpm download-helper-binaries`, which throws on the
      six `PENDING-CI-PUBLISH` sentinels in `meta/runnersOnedirDigests.json`; the job fails before
      any `dist:mac` invocation. State this as **UNPROVEN in CI** — the wiring is present and the CI
      path is structurally blocked upstream of it, so no CI run has ever executed the guard, in
      either direction. Named precondition: REQ-34.9-02/03/04's own precondition (real published
      darwin archives and real digests), which unblocks `install-deps`. `OWNER:` the same follow-up
      phase that owns items 1-3, dated 2026-08-12.

    Items 12 and 13 are the two entries where the temptation to overstate is highest. UNPROVEN means
    UNPROVEN: no sentence in either entry may assert an outcome for a path that has never run.
  </action>
  <acceptance_criteria>
    - `deferred-items.md` contains the literal heading `## Code-review finding disposition (2026-08-12)`.
    - Under that heading, all six literal IDs `CR-01`, `WR-01`, `WR-02`, `IN-01`, `IN-02`, `IN-03` each appear at least once, and the table has exactly six data rows.
    - Every one of the six rows' `Disposition` cell contains exactly one of the literal strings `FIXED` or `DEFERRED` — the strings `partially`, `mostly`, `n/a` and an empty cell are all failures.
    - Every one of the six rows' `Evidence` cell matches either `34.9-1[89]`/`34.9-2[01]` (a landed plan ID) or `item 1[123]` (a ledger item number). Zero rows with an unmatched Evidence cell — this is the set-difference result, and a non-zero count means the sweep failed.
    - The section contains an explicit count line stating that the unmapped count is `0`.
    - The three new headings `### 11.`, `### 12.` and `### 13.` all exist; `grep -c '^### 1[123]\.' <file>` returns `3`.
    - Each of items 11, 12 and 13 contains an `OWNER:` line and the literal `2026-08-12`.
    - Item 11 contains the literal `dist:win` and the literal `dist:linux`.
    - Item 12 contains the literals `--arch=arm64`, `UNPROVEN`, and `file-count floor`.
    - Item 13 contains the literals `install-deps`, `PENDING-CI-PUBLISH`, and `UNPROVEN`.
    - Items 1-10 are byte-identical to their pre-task state: `git diff -U0 -- <file>` produces only additions (`^+` lines), zero deletions (`^-` lines) outside the diff header.
    - `grep -in 'code_verifier\|client_id\|serial' <file>` returns only pre-existing item 9 lines; quote each in the Summary to show none is a value.
    - The Summary reproduces the raw `grep '^### ' 34.9-REVIEW.md` output (list A), the `closes_findings` values read from the four gap-cycle-2 Summaries (list B), and the A-minus-B result as three explicit lists.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md; node -e "const fs=require('fs');const s=fs.readFileSync(process.argv[1],'utf8');const i=s.indexOf('## Code-review finding disposition (2026-08-12)');if(i<0){console.error('missing disposition heading');process.exit(1)}const sec=s.slice(i);const rows=sec.split('\n').filter(l=>/^\|\s*(CR|WR|IN)-\d\d\s*\|/.test(l));if(rows.length!==6){console.error('rows='+rows.length);process.exit(1)}for(const r of rows){const c=r.split('|').map(x=>x.trim());const disp=c[3]||'';const ev=c[4]||'';if(!/^(FIXED|DEFERRED)$/.test(disp)){console.error('bad disposition: '+r);process.exit(1)}if(!/34\.9-(18|19|20|21)|item 1[123]/.test(ev)){console.error('unmapped evidence: '+r);process.exit(1)}}for(const id of ['CR-01','WR-01','WR-02','IN-01','IN-02','IN-03'])if(!sec.includes(id)){console.error('missing '+id);process.exit(1)}if((s.match(/^### 1[123]\./gm)||[]).length!==3){console.error('items 11-13 incomplete');process.exit(1)}for(const t of ['dist:win','dist:linux','--arch=arm64','file-count floor','install-deps','PENDING-CI-PUBLISH','UNPROVEN'])if(!s.includes(t)){console.error('missing token '+t);process.exit(1)}console.log('SWEEP-AND-DEFERRALS-OK')" "$F" && test "$(git diff -U0 -- "$F" | grep -c '^-[^-]')" -eq 0 && echo NO-DELETIONS</automated>
  </verify>
  <done>All six review findings are enumerated from the review itself and each maps to a landed plan ID or to ledger item 11/12/13, with zero unmapped; items 11-13 each carry a mechanism-level blocker, a named precondition, a dated OWNER, and state UNPROVEN where nothing has been observed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the overclaiming tripwire prose in all three narrative documents</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/34.9-.../34.9-GUARD-PROOF.md` — **read its frontmatter verdict FIRST.** This single value selects the branch below. If the file does not exist, or its verdict is `PENDING`, that is branch B.
    - `.planning/phases/34.9-.../34.9-LIVE-GATE-RERUN.md` — Section 1's 34.9-13 bullet (~line 75) and Scored Item 2 criterion 6 (~line 509). Read the surrounding passage before editing so the correction reads as part of the argument rather than bolted on.
    - `.planning/ROADMAP.md` — the Phase 34.9 status note (~lines 2455-2474), specifically the clause at ~2459-2461.
    - `.planning/REQUIREMENTS.md` — the REQ-34.9-08 status-table row (~line 372) and the REQ-34.9-08 checkbox entry (~line 834), plus the Phase 34.9 block's preamble sentence at ~line 825.
    - `.planning/phases/34.9-.../34.9-VERIFICATION.md` truth 8 — the exact wording of the claim being falsified, so the correction answers it directly.
  </read_first>
  <action>
    Every sentence corrected here must carry its own scope. A sentence that is true only under
    conditions it does not state is the same defect in a smaller font — that is the entire reason
    this task exists, and it is the acceptance bar.

    **Step 1 — select the branch from `34.9-GUARD-PROOF.md`'s frontmatter verdict.** Record the
    literal verdict string in the Summary. Nothing else may select the branch: not a plan Summary's
    claim, not a green `pnpm test:ci`, not this plan's expectations.

    **Branch A — verdict is PASS.** The corrected claim asserts a SCOPED truth with the proof run as
    its evidence. Every corrected passage must state, in the same passage: that
    `pnpm verify:runner-bundle build --arch=arm64` runs inside `dist:mac` and `release:mac` before
    `electron-builder`; that its failing direction was observed live against a deliberately
    dereferenced framework (cite `34.9-GUARD-PROOF.md` and its run date); that coverage is
    **arm64 only** (`deferred-items.md` item 12); and that it is **not in CI** — the macOS CI leg
    cannot reach the build (`deferred-items.md` item 13).

    **Branch B — verdict is FAIL, PENDING, or the proof was not attempted.** The corrected claim
    says plainly that the tripwire is **UNPROVEN**: the wiring is committed but its failing direction
    has never been observed against a real build, and the only evidence is unit-level. Name the
    verdict and the document. Do not soften, do not describe it as "expected to fire", and do not
    write a sentence whose subject is what the guard "will" do. The same arm64-only and not-in-CI
    scope clauses still apply and must still appear.

    **Step 2 — apply the correction to `34.9-LIVE-GATE-RERUN.md` at both locators.** This document is
    a signed-off run record: its RUN RECORD observations are the audit trail and must not be
    rewritten. Confine the edit to the two narrative claim passages. Append the literal marker
    `[Corrected 2026-08-12 — plan 34.9-22]` immediately after each corrected passage, so the record
    shows a dated correction rather than a silent rewrite of history. Do not touch any line beginning
    with `**Observed:`, any recorded command output, any hash, timestamp, PID or launch ordinal.

    **Step 3 — `ROADMAP.md`'s Phase 34.9 status note.** Replace the clause "so a regression cannot go
    silent again" with the branch-appropriate scoped claim, citing `34.9-GUARD-PROOF.md`. In the same
    edit, change the note's opening from `**Phase status 2026-08-11: CLOSES on the arm64 leg only.**`
    to a dated 2026-08-12 status that states the phase **does not close yet**: `34.9-VERIFICATION.md`
    is `status: gaps_found`, truth 8 failed, gap cycle 2 (`34.9-18`..`34.9-22`) landed the fixes and
    the ledger, and truth 8 must now be **re-scored by the verifier against landed evidence**. Do not
    write that the phase is complete and do not tick anything on that basis. Leave the warm-ratio
    measurement table and the retired-`~95x` caveat byte-identical; they are still correct.

    **Step 4 — `REQUIREMENTS.md`.** In the REQ-34.9-08 checkbox entry, replace the "**Automated
    coverage added (plan 34.9-13):** ... so a regression cannot go silent again" clause with the
    branch-appropriate scoped claim, and append a dated 2026-08-12 addendum recording gap cycle 2:
    what landed (naming the plan IDs), what the guard covers, and what `deferred-items.md` items 11,
    12 and 13 record. Mirror a one-sentence version of the same into the REQ-34.9-08 status-table
    row (~line 372) so the two never disagree. Also append a dated 2026-08-12 sentence to the Phase
    34.9 block preamble (~line 825), which currently attributes the phase's non-closure to the
    original gate's FAIL verdict: the accurate current cause is `34.9-VERIFICATION.md`'s truth 8, and
    the preamble must say so.

    REQ-34.9-08 stays **unchanged in its ticked/unticked state** by this plan. Its status is the
    verifier's to re-score; this plan corrects the prose describing it and adds the dated record. Do
    not mint new REQ IDs. Do not touch any other phase's rows.
  </action>
  <acceptance_criteria>
    - The literal string `a genuine tripwire` returns zero matches in `34.9-LIVE-GATE-RERUN.md`.
    - The literal string `so a regression cannot go silent again` returns zero matches across `34.9-LIVE-GATE-RERUN.md`, `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.
    - **Scope-carrying check:** for every occurrence of the substring `tripwire` in those three files, the 400-character window beginning at that occurrence contains the literal `arm64` AND the literal `not in CI`. Zero occurrences may fail this.
    - Every corrected passage cites `34.9-GUARD-PROOF` — `grep -c '34.9-GUARD-PROOF'` returns ≥ 1 in each of the three files.
    - Both corrected passages in `34.9-LIVE-GATE-RERUN.md` carry the literal marker `[Corrected 2026-08-12 — plan 34.9-22]`; `grep -c` for it returns exactly `2`.
    - No RUN RECORD observation was altered: `git diff -U0 -- 34.9-LIVE-GATE-RERUN.md | grep -c '^[-+].*\*\*Observed:'` returns `0`, and the same diff contains zero `^-` lines that include a `sha256`/hash string, a timestamp, or `launch ordinal`.
    - `ROADMAP.md` no longer contains the literal `**Phase status 2026-08-11: CLOSES on the arm64 leg only.**`, and does contain the literal `2026-08-12` within its Phase 34.9 status note together with the literal `does not close`.
    - `ROADMAP.md`'s warm-ratio table rows (`| nile | 4.23s | 0.13s | **32.5x** |` and its two siblings) and the `Measurement caveat — RESOLVED 2026-08-11` paragraph appear unchanged in `git diff` (zero `^-` lines matching `32.5x|26.6x|27.2x|~95x`).
    - `REQUIREMENTS.md` contains `2026-08-12` within both the REQ-34.9-08 status-table row and the REQ-34.9-08 checkbox entry, and both name `deferred-items.md` items 11-13.
    - REQ-34.9-08's checkbox marker is unchanged: `git diff -U0 -- .planning/REQUIREMENTS.md | grep -c '^[-+]- \[.\] \*\*REQ-34.9-08\*\*'` is either `0` or `2` with the identical marker character on the `-` and `+` lines.
    - The Summary states the literal verdict string read from `34.9-GUARD-PROOF.md`, which branch it selected, and quotes each corrected sentence in full.
  </acceptance_criteria>
  <verify>
    <automated>P=.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md; node -e "const fs=require('fs');const files=process.argv.slice(1);let bad=0;for(const f of files){const s=fs.readFileSync(f,'utf8');if(s.includes('so a regression cannot go silent again')){console.error('retired claim survives in '+f);bad++}if(!s.includes('34.9-GUARD-PROOF')){console.error('no guard-proof citation in '+f);bad++}for(const m of s.matchAll(/tripwire/g)){const w=s.slice(m.index,m.index+400);if(!/arm64/.test(w)||!/not in CI/.test(w)){console.error('unscoped tripwire claim in '+f+' at '+m.index);bad++}}}process.exit(bad?1:0)" "$P" .planning/ROADMAP.md .planning/REQUIREMENTS.md && test "$(grep -c 'a genuine tripwire' "$P")" -eq 0 && test "$(grep -c '\[Corrected 2026-08-12 — plan 34.9-22\]' "$P")" -eq 2 && test "$(git diff -U0 -- "$P" | grep -c '^[-+].*\*\*Observed:')" -eq 0 && test "$(git diff -U0 -- .planning/ROADMAP.md | grep -c '^-.*\(32\.5x\|26\.6x\|27\.2x\|~95x\)')" -eq 0 && echo PROSE-SCOPED-OK</automated>
  </verify>
  <done>No document in this phase claims an automated tripwire without stating arm64-only, local-script, not-in-CI scope in the same passage; the branch was selected from 34.9-GUARD-PROOF.md's own verdict; the run record carries dated correction markers and its observations are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP structure and STATE.md, and name re-verification as the next step</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` ~lines 2414-2424 (the `**Plans:**` line and the gap cycle 1 checked-row block) and ~lines 2503-2516 (the gap cycle 1 narrative block ending in the duplicate unchecked `34.9-17` row). The checked twin is at ~line 2424; the duplicate unchecked row is at ~line 2516.
    - `.planning/STATE.md` — frontmatter `stopped_at`, `last_updated`, `last_activity`, `progress.total_plans`, `progress.completed_plans`, `progress.percent`, and the Current Position section whose banner belongs to **Phase 34.5**, not this phase.
    - `.planning/phases/34.9-.../34.9-18-SUMMARY.md` … `34.9-21-SUMMARY.md` — one-line objectives for the gap-cycle-2 ROADMAP rows, and 34.9-21's verdict for its row.
    - `.planning/phases/34.9-.../34.9-VERIFICATION.md` frontmatter — `status: gaps_found`, the value that must remain the phase's recorded state after this plan.
  </read_first>
  <action>
    **ROADMAP.md, structural.**

    Delete the duplicate unchecked row `- [ ] 34.9-17-PLAN.md — Record the descoped/deferred set;
    reconcile REQUIREMENTS.md, this ROADMAP entry and STATE.md to post-gap-cycle truth (wave 4)` at
    ~line 2516. The checked twin at ~line 2424 is the survivor; do not edit it. A checkbox appearing
    twice with two different states is a ledger that disagrees with itself.

    Update the `**Plans:**` line (~line 2414) from `17 total — 17/17 plans executed` to the real
    post-cycle-2 total (17 + the five gap-cycle-2 plans `34.9-18`..`34.9-22` = 22) with the executed
    count derived by counting the `-SUMMARY.md` files actually present in the phase directory, not
    assumed. In the same line, correct the trailing clause `34.9-17 completed the full
    reconciliation` — it is false: 34.9-17's reconciliation missed all six code-review findings,
    which is what `34.9-VERIFICATION.md` truth 8 records. Replace it with a dated 2026-08-12 clause
    naming gap cycle 2 as the reconciliation that swept them.

    Append a `**Gap cycle 2 — planned 2026-08-12 at `/gsd-plan-phase 34.9 --gaps`.**` block after the
    gap cycle 1 block, stating what it closes (verification truth 8 / CR-01 plus the five review
    findings that appeared in no ledger), then one `- [x]` row per plan `34.9-18` … `34.9-22`, each
    naming its objective and its outcome. The `34.9-21` row must carry the literal verdict read from
    `34.9-GUARD-PROOF.md`, and the `34.9-22` row must say the phase awaits re-verification rather
    than that it closes.

    End the block with an explicit next-step line: **the phase does not close from these edits** —
    `34.9-VERIFICATION.md` is `status: gaps_found`, and truth 8 must be re-scored against the landed
    evidence by running `/gsd-verify-work 34.9`. Name that command literally.

    **STATE.md.**

    Do NOT touch the Current Position banner, which belongs to Phase 34.5's fourth failed gate and is
    that phase's active blocker. Add a separate, clearly-headed Phase 34.9 paragraph beneath it
    recording: gap cycle 2 executed (`34.9-18`..`34.9-22`), what it fixed, that
    `34.9-VERIFICATION.md` remains `status: gaps_found`, that truth 8 awaits re-scoring, and the
    literal next step `/gsd-verify-work 34.9`. State the guard's scope in the same paragraph —
    arm64-only, local `dist:mac`/`release:mac`, not in CI — so a reader of STATE alone cannot form
    the overclaim this plan exists to remove.

    Update the frontmatter: `last_updated` to the real current UTC timestamp, `last_activity` to
    `2026-08-12`, `stopped_at` to `Completed 34.9-22-PLAN.md`, `progress.total_plans` to the prior
    total plus the five gap-cycle-2 plans, `progress.completed_plans` recomputed, and
    and `progress.percent` left ALONE unless a phase completed. **`percent` tracks PHASES, not
    plans** — today's `74` is `completed_phases / total_phases` (17/23 = 73.9), not
    `completed_plans / total_plans` (279/292 = 96). Phase 34.9 does not complete in this plan, so
    `completed_phases`, `total_phases` and `percent` must all stay unchanged; writing a plan-derived
    percentage here would reproduce the 2026-08-09 corruption by hand. **Recompute the two plan
    counts from the repository, not from the frontmatter's own prior values** — the snapshot is known
    stale (its `stopped_at` reads `34.5-50` while `34.5-51` is committed). If any `gsd-sdk` write
    touches this file, re-read it and hand-correct: `state.record-session` corrupted this file on
    2026-08-09, setting `completed_plans` 247→248 and `percent` 92→65. Record the before and after
    values for all four progress fields in the Summary and state that the file was re-read after any
    SDK write.

    Do not mark Phase 34.9 complete anywhere. Do not touch any other phase's ROADMAP rows,
    requirements or notes. Do not push to `main`.
  </action>
  <acceptance_criteria>
    - `grep -c '^- \[ \] 34.9-17-PLAN.md' .planning/ROADMAP.md` returns `0`, and `grep -c '^- \[x\] 34.9-17-PLAN.md' .planning/ROADMAP.md` returns `1`.
    - `.planning/ROADMAP.md` contains one `- [x] 34.9-NN-PLAN.md` row for each of `34.9-18`, `34.9-19`, `34.9-20`, `34.9-21`, `34.9-22`, each appearing exactly once.
    - The Phase 34.9 `**Plans:**` line contains the literal `22` and no longer contains the literal `17/17`.
    - The literal `34.9-17 completed the full reconciliation` returns zero matches in `.planning/ROADMAP.md`.
    - `.planning/ROADMAP.md` contains the literal `/gsd-verify-work 34.9` within the Phase 34.9 section, and the literal `gaps_found` in the same section.
    - `.planning/ROADMAP.md` contains zero occurrences of the literals `Phase 34.9 COMPLETE`, `34.9 CLOSES`, and `Phase status 2026-08-11: CLOSES`.
    - `.planning/STATE.md` contains the literal `34.9-22`, the literal `/gsd-verify-work 34.9`, the literal `gaps_found`, and the literal `not in CI`.
    - `.planning/STATE.md`'s Phase 34.5 blocker banner is unchanged: `git diff -U0 -- .planning/STATE.md | grep -c '^-.*34.5-LIVE-GATE-RERUN-3'` returns `0`.
    - `.planning/STATE.md` frontmatter arithmetic holds on the PHASE axis, which is what `percent` tracks: `percent` equals `Math.round(completed_phases / total_phases * 100)`, and `completed_phases` ≤ `total_phases`.
    - `percent`, `completed_phases` and `total_phases` are **unchanged** by this plan (Phase 34.9 does not complete here): `git diff -U0 -- .planning/STATE.md | grep -c '^[-+]\s*\(percent\|completed_phases\|total_phases\):'` returns `0`.
    - `completed_plans` ≤ `total_plans`, and `total_plans` increased by exactly `5` (the gap-cycle-2 plans `34.9-18`..`34.9-22`).
    - The Summary records before/after values for `total_plans`, `completed_plans` and `percent`, states how the executed count was derived (the `-SUMMARY.md` file count), and states whether any `gsd-sdk` write touched `STATE.md` and that it was re-read afterwards.
    - `git diff --name-only` across this plan's commits equals exactly this plan's `files_modified` — no other phase's ledger rows were touched.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^- \[ \] 34.9-17-PLAN.md' .planning/ROADMAP.md)" -eq 0 && test "$(grep -c '^- \[x\] 34.9-17-PLAN.md' .planning/ROADMAP.md)" -eq 1 && test "$(grep -c '34.9-17 completed the full reconciliation' .planning/ROADMAP.md)" -eq 0 && for n in 18 19 20 21 22; do test "$(grep -c "^- \[x\] 34.9-$n-PLAN.md" .planning/ROADMAP.md)" -eq 1 || exit 1; done && grep -q '/gsd-verify-work 34.9' .planning/ROADMAP.md && grep -q '/gsd-verify-work 34.9' .planning/STATE.md && grep -q 'not in CI' .planning/STATE.md && test "$(git diff -U0 -- .planning/STATE.md | grep -c '^-.*34.5-LIVE-GATE-RERUN-3')" -eq 0 && test "$(git diff -U0 -- .planning/STATE.md | grep -c '^[-+][[:space:]]*\(percent\|completed_phases\|total_phases\):')" -eq 0 && node -e "const fs=require('fs');const fm=fs.readFileSync('.planning/STATE.md','utf8').split('---')[1];const g=k=>Number((fm.match(new RegExp(k+':\\\\s*(\\\\d+)'))||[])[1]);const tp=g('total_plans'),cp=g('completed_plans'),tf=g('total_phases'),cf=g('completed_phases'),p=g('percent');if(!tp||!cp||cp>tp){console.error('plan counts: total='+tp+' completed='+cp);process.exit(1)}if(p!==Math.round(cf/tf*100)){console.error('percent tracks phases: percent='+p+' expected='+Math.round(cf/tf*100));process.exit(1)}console.log('STATE-ARITHMETIC-OK')" && echo LEDGERS-RECONCILED</automated>
  </verify>
  <done>ROADMAP.md carries exactly one 34.9-17 row and a complete gap-cycle-2 block, its plan count and reconciliation claim are corrected, both ROADMAP and STATE name `/gsd-verify-work 34.9` as the next step and record the phase as still `gaps_found`, and STATE's arithmetic is hand-verified with before/after values recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase narrative → downstream reviewer's decision to check | The honest surface of this plan. A security-relevant guard described as active when it is partially inactive causes a reviewer, a future planner, or a release operator to **skip a check they would otherwise perform**. The claim "an automated tripwire now fires if it regresses" is precisely the sentence that retires a manual verification step from someone's mental checklist. Overstated coverage is therefore not a documentation nicety — it removes a control. |
| ledger → future phase scoping | An untriaged finding is indistinguishable from a nonexistent one at the next planning pass. This phase has already demonstrated the failure: six review findings survived a full reconciliation plan because nothing enumerated them. |
| signed-off run record → audit trail | `34.9-LIVE-GATE-RERUN.md` records observations from human-operated hardware. Silently rewriting its prose alongside its data would make the audit trail unfalsifiable. |
| unrun proof document → asserted evidence | `34.9-GUARD-PROOF.md`'s verdict is the only admissible selector for what this plan may assert. Any other source would let the plan certify its own expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.9G-28 | Repudiation | the tripwire claim in `34.9-LIVE-GATE-RERUN.md`, `ROADMAP.md`, `REQUIREMENTS.md` | mitigate | Every corrected passage must state arm64-only, local-`dist:mac`/`release:mac`, and not-in-CI scope. Enforced mechanically: for each occurrence of `tripwire`, the following 400-character window must contain `arm64` and `not in CI` (Task 2 automated check). A scoped claim cannot silently decay into an unscoped one. |
| T-34.9G-29 | Tampering | `34.9-LIVE-GATE-RERUN.md`'s RUN RECORD observations | mitigate | Edits confined to the two narrative claim passages; the literal marker `[Corrected 2026-08-12 — plan 34.9-22]` makes each correction dated and visible; acceptance criterion asserts `git diff` touches zero `**Observed:` lines and deletes no hash/timestamp/launch-ordinal line. |
| T-34.9G-30 | Spoofing | the guard's proof status | mitigate | The branch is selected ONLY from `34.9-GUARD-PROOF.md`'s frontmatter verdict, read directly and quoted in the Summary. The plan states in writing that a plan Summary's claim, a green `pnpm test:ci`, and this plan's own expectations are all inadmissible. Branch B forbids any sentence whose subject is what the guard "will" do. |
| T-34.9G-31 | Repudiation | a code-review finding vanishing from the record | mitigate | The exact 34.9-17 failure. Task 1 derives list A by grep from `34.9-REVIEW.md` (never from this plan's own table), derives list B from the four gap-cycle-2 Summaries' `closes_findings`, and the automated check fails on any of the six rows whose Evidence cell names neither a landed plan ID nor a ledger item number. Zero unmapped is a gate, not a goal. |
| T-34.9G-32 | Tampering | `gsd-sdk` corrupting `STATE.md` | mitigate | The 2026-08-09 corruption (`completed_plans` 247→248, `percent` 92→65) is named; the plan requires recomputation from the repository rather than from the stale snapshot, a re-read and hand-correction after any SDK write, before/after values in the Summary, and an automated arithmetic check that `percent === round(completed/total*100)`. |
| T-34.9G-33 | Tampering | edits leaking into Phase 34.5's active blocker banner or other phases' rows | mitigate | Acceptance criteria require `git diff --name-only` to equal exactly `files_modified`, zero deletions inside `deferred-items.md` items 1-10, and zero deletion of the 34.5 blocker banner lines. |
| T-34.9G-34 | Information Disclosure | identifiers transcribed into a ledger | mitigate | `code_verifier`/`serial`/`client_id` may appear only as the field NAMES inside the pre-existing item 9 prohibition; the acceptance criterion requires each matched line to be quoted in the Summary to prove it is not a value. No SteamID, no account identifier. |
| T-34.9G-35 | Elevation of Privilege | a documentation plan closing a phase the verifier has not re-scored | mitigate | The plan forbids marking Phase 34.9 complete; acceptance criteria assert zero occurrences of `Phase 34.9 COMPLETE`/`34.9 CLOSES` in ROADMAP.md and require `gaps_found` plus the literal `/gsd-verify-work 34.9` in both ROADMAP.md and STATE.md. |
| T-34.9G-SC | Tampering | npm/pip/cargo installs | N/A | **This plan installs no packages and changes no source code.** The Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- All six review finding IDs appear in `deferred-items.md`'s disposition table, each mapped to a landed plan ID or to item 11/12/13, unmapped count zero.
- Items 11, 12 and 13 exist, each with a mechanism-level blocker, a named precondition, an `OWNER:` line and the date `2026-08-12`; items 12 and 13 state UNPROVEN and assert no outcome for a path that never ran.
- `deferred-items.md` items 1-10 are unmodified (diff shows additions only).
- Zero unscoped `tripwire` claims across `34.9-LIVE-GATE-RERUN.md`, `ROADMAP.md` and `REQUIREMENTS.md`; the retired strings `a genuine tripwire` and `so a regression cannot go silent again` return zero matches.
- `34.9-LIVE-GATE-RERUN.md` carries exactly two `[Corrected 2026-08-12 — plan 34.9-22]` markers and no altered observation.
- `ROADMAP.md` carries one `34.9-17` row, `- [x]` rows for `34.9-18`..`34.9-22`, a plan count of 22, and no closure claim.
- `STATE.md`'s progress arithmetic is self-consistent and hand-verified; Phase 34.5's blocker banner is untouched.
- Both `ROADMAP.md` and `STATE.md` name `/gsd-verify-work 34.9` as the next step and record `gaps_found`.
- `git diff --name-only` across this plan's commits equals exactly `files_modified`.
- No user-facing string is added — the i18n gate is N/A; state this explicitly in the Summary.
- No push to `main`, no upstream submission, no new REQ IDs.
</verification>

<success_criteria>
The phase's own record now states what is true and only what is true: the guard is wired into
`dist:mac` and `release:mac` for arm64, its proof status is whatever `34.9-GUARD-PROOF.md` measured
and nothing more, it does not run in CI, and it does not cover x64. Every one of the six code-review
findings is either fixed with a named plan or deferred with a dated, owned ledger entry — the
set-difference is explicit and its unmapped count is zero, which is exactly what gap cycle 1's
reconciliation could not say. The phase is left OPEN with `/gsd-verify-work 34.9` named as the step
that re-scores truth 8 against landed evidence.
</success_criteria>

<output>
Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-22-SUMMARY.md` when done.
</output>
