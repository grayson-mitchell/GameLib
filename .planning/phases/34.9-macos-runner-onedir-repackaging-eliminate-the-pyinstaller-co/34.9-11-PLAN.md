---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 11
type: execute
wave: 7
depends_on: ["34.9-10"]
files_modified:
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [REQ-34.9-11]

must_haves:
  truths:
    - "The blocking contract has been RUN by a human on real macOS hardware, and its verdict field holds a real result rather than null"
    - "Every scored item's outcome is recorded with the launch ordinal and the sink excerpt that decides it"
    - "Requirement boxes are ticked only for what the run actually measured; anything unmeasured stays unticked with the reason"
    - "Tauri-PACKAGED and real-certificate notarization are recorded as NOT PROVEN with their owners named, and the ROADMAP's ~95x claim is reconciled against the per-runner measured figures"
  artifacts:
    - path: ".planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md"
      provides: "The same contract, now carrying a real verdict, per-item outcomes and the session evidence inventory"
      contains: "verdict:"
  key_links:
    - from: "34.9-LIVE-GATE.md verdict"
      to: ".planning/REQUIREMENTS.md REQ-34.9-* boxes"
      via: "a box is ticked only by a measured PASS on its own item"
      pattern: "REQ-34.9"
---

<objective>
Run the blocking live gate on real macOS hardware and record what actually happened.

Purpose: this project's automated suite has been green through every blocking defect it ever
shipped — the sheet-attachment wedge, the autofill non-fill, the Humble disconnect deadlock, the
`publicDir` spawn defect. A live gate exists because that suite cannot substitute for a human
driving the UI. This plan is the only plan in Phase 34.9 permitted to record a result.

Output: a scored `34.9-LIVE-GATE.md`, reconciled requirement boxes, and an updated ROADMAP entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-MEASUREMENT.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-CI-ROUNDTRIP.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PACKAGING-LIMITATIONS.md
@.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md

<no_partial_pass>
This project's D-08 discipline applies: the phase closes only on all scored items PASS. A partial
result is a gap cycle (`/gsd-plan-phase 34.9 --gaps`), not a deferred UAT entry, not an advisory
note, and not a pre-authorized override. Do not tick a requirement box on the strength of an item
that passed while another item failed, unless the contract's own verdict rule says so explicitly.
</no_partial_pass>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human runs the blocking live gate</name>
  <files>none in-repo — evidence is written to the session directory /tmp/gamelib-gate-{UTC-timestamp}/</files>
  <read_first>
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md (the contract to execute — read the Preconditions, Evidence capture and every scored item before starting)
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-CI-ROUNDTRIP.md (which arches have artifacts, and whether any item is marked NOT ATTEMPTED because of outcome B or C)
  </read_first>
  <action>
Hand control to the developer. Do not drive the app, do not simulate any item's gesture, and do not
record any outcome in this task — this project separates the plan that AUTHORS a gate contract from
the plan that RUNS it, and separates the agent that prepares a run from the human who performs it.
Present the contract, confirm every precondition is satisfiable, and wait for the developer's report.
  </action>
  <verify>
    <automated>grep -qE "^verdict: null" .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md && echo "contract still unscored at handoff -- correct"</automated>
  </verify>
  <done>The developer has reported PASS, FAIL or NOT ATTEMPTED for every scored item, plus the session directory path.</done>
  <what-built>
    macOS onedir repackaging for legendary, gogdl and nile: a build script and CI workflow that
    produce the bundles, a downloader that fetches and sha256-verifies them, a resolver that finds
    the nested path, and both packagers configured to carry the trees. Everything is unit-tested
    and the suite is green — which, in this project, is precisely why a human run is required.
  </what-built>
  <how-to-verify>
    1. Open `34.9-LIVE-GATE.md` and work top to bottom. Do not reorder the items: the contract's
       pairwise review fixed their order specifically because `altNileBin` mutates a persisted
       setting and `pnpm dist:mac` rebuilds `build/` from `public/`.
    2. Satisfy every precondition first, including `pgrep -f 'target/debug/gamelib-shell'` returning
       nothing and `npm run test:ci` being green. The suite result is a precondition only — it is
       not evidence for any item.
    3. Create the session directory and follow the evidence-capture section literally: `tee -a`
       (never a bare `tee`), the `=== GATE LAUNCH N` delimiter before each launch, and the
       `gamelib.log` archive copy BEFORE the next launch renames it to `.old`.
    4. Before and after each launch, assert exactly one app instance and record the PID. If the count
       is ever not 1, abort that launch and re-run it — its evidence is not scorable, because a
       second instance splits the `[shell]` transcript sink while leaving `gamelib.log` shared, which
       produces a populated log next to a transcript missing every scored literal.
    5. Record each item PASS or FAIL against its own stated criterion, quoting the deciding sink
       excerpt. If an item cannot be attempted, record NOT ATTEMPTED with the reason rather than
       guessing.
    6. Run the closing inventory (`ls -la` and `wc -l` on the session directory) while the session is
       still open, so a zero-length capture is visible now rather than discovered during write-up.
    7. Report back: the per-item outcomes, the session directory path, and anything that surprised
       you — including behaviour no item asked about.
  </how-to-verify>
  <acceptance_criteria>
    - Every scored item has an explicit PASS, FAIL or NOT ATTEMPTED with a reason.
    - The session directory exists and its closing inventory was captured during the run.
    - Each launch's single-PID assertion was recorded before and after.
    - No item's outcome rests on `npm run test:ci`.
  </acceptance_criteria>
  <resume-signal>Report each item's outcome plus the session directory path, or describe what blocked the run.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the verdict, the findings and the honest limitations</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md (the contract being scored, plus its verdict rule and its `What this gate does NOT prove` section)
    - the session directory the runner reported (the transcript and every archived `gamelib-launch-N.log`)
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-MEASUREMENT.md (the per-runner ratios that item 2 and 3's live intervals should be consistent with)
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md Section 4 (do not infer a path did not run from transcript silence)
  </read_first>
  <action>
Fill in `34.9-LIVE-GATE.md`'s frontmatter: `verdict` (PASS or FAIL), `items_passed`, `items_failed`,
`items_not_attempted`, `run_date`, and the session directory path.

Under each scored item, record the outcome, the launch ordinal that covered it, and the verbatim
sink excerpt that decides it — quoting from the archived log or transcript, not from memory or from
the runner's summary alone.

For every FAIL, mint a finding `F-34.9-NN` stating the observed behaviour, the sink excerpt, and
what it falsifies. Do not diagnose beyond what the evidence supports, and do not use transcript
silence to argue a backend path did not run — sidecar output structurally cannot reach the
transcript, so its absence there discriminates nothing.

Cross-check items 2 and 3's measured click-to-spawn intervals against `34.9-MEASUREMENT.md`'s
per-runner cold ratios. If they disagree materially, record that as a finding rather than
reconciling them by narrative — one of the two measurements is then wrong and which one is unknown.

Add a `## What this run did not prove` section repeating, with the run's own date attached:
Tauri-PACKAGED resolution (blocked on R-34.5-G1-PKG, owned by Phase 34.5's deferred item 12, and
pre-dating this phase); real-certificate signing and notarization (D-03/D-04, no Apple credentials
enrolled, so `codesign --verify` on an adhoc-signed bundle is the only proxy obtained); the x64 leg
if `34.9-CI-ROUNDTRIP.md` recorded outcome B or C; and Windows/Linux behaviour, unchanged by this
phase and deliberately not exercised.
  </action>
  <verify>
    <automated>grep -qE "^verdict: (PASS|FAIL)" .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md && grep -q "What this run did not prove" .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `verdict` is `PASS` or `FAIL`, never left null and never a hedge; `items_passed` + `items_failed` + `items_not_attempted` equals `items_total`.
    - Every item carries a verbatim sink excerpt from the session directory, with its launch ordinal.
    - Every FAIL has an `F-34.9-NN` finding; no finding asserts a path did not run on the basis of transcript silence.
    - The `What this run did not prove` section names R-34.5-G1-PKG with its owner, D-03/D-04 notarization, the x64 leg's status, and Windows/Linux.
    - Items 2 and 3's live intervals are compared against `34.9-MEASUREMENT.md`, with any disagreement recorded as a finding rather than smoothed over.
  </acceptance_criteria>
  <done>The contract carries a real verdict backed by quoted evidence, and everything the run could not reach is named with its owner.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS.md and the ROADMAP against what was measured</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being modified — read the tail's per-phase minting-rationale entries and the `Mapped to phases` tally to match the house format exactly)
    - .planning/ROADMAP.md Phase 34.9 section (the file being modified — its `Requirements: TBD`, `Plans: 0 plans`, plan list, and its measured-table prose carrying the ~95x claim)
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md (the scored verdict — the only thing permitted to tick a box)
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-MEASUREMENT.md (the per-runner figures that the ROADMAP prose must be reconciled against)
  </read_first>
  <action>
In `.planning/REQUIREMENTS.md`, add the Phase 34.9 requirement block REQ-34.9-01 through
REQ-34.9-11 in the house format, each with its checkbox state determined solely by evidence:

  REQ-34.9-01  committed onedir build script, upstream command derived not restated
  REQ-34.9-02  CI workflow builds x64 + arm64 and publishes to the rolling release
  REQ-34.9-03  downloader sources darwin archives from the rolling release; win32/linux unchanged
  REQ-34.9-04  darwin archives sha256-verified against in-repo digests before extraction
  REQ-34.9-05  layout change forces re-download without a version bump
  REQ-34.9-06  archSpecificBinary resolves nested darwin paths for exactly three runners
  REQ-34.9-07  alt*Bin overrides still bypass resolution and still work
  REQ-34.9-08  both packagers stage the trees; a built artifact is inspected, not trusted
  REQ-34.9-09  per-runner cold/warm win MEASURED, not inferred from nile
  REQ-34.9-10  upstream PR content prepared; submission is the developer's action
  REQ-34.9-11  live gate proves Tauri-dev and Electron-packaged; Tauri-packaged recorded UNPROVEN

Tick a box only where a live item or a committed artifact actually demonstrates it. Anything the
gate left FAIL or NOT ATTEMPTED stays unticked with a dated note naming the specific gate item that
is its precondition — following the same discipline the Phase 34.4.2 entries use.

Append a dated minting-rationale line to the file's tail recording that these eleven IDs were minted
during `/gsd-plan-phase 34.9` because ROADMAP.md carried `Requirements: TBD`, and update the
`Mapped to phases` tally and `Unmapped` count.

In `.planning/ROADMAP.md`'s Phase 34.9 section: replace `**Requirements**: TBD (run /gsd-plan-phase 34.9)`
with the eleven IDs; set `**Plans:** 11 plans` and replace the `- [ ] TBD` placeholder with the
eleven plan filenames and one-line objectives, ticking those whose SUMMARY exists; and reconcile the
measured-table prose. If `34.9-MEASUREMENT.md` shows legendary or gogdl did not reproduce an
order-of-magnitude win, replace the blanket ~95x framing with the per-runner figures and state that
the original table was nile-only — the ROADMAP must not carry a number the phase itself falsified.

If the verdict is FAIL, state in the ROADMAP that Phase 34.9 does not close and routes to
`/gsd-plan-phase 34.9 --gaps`, and leave the phase's status unclosed.
  </action>
  <verify>
    <automated>grep -c "REQ-34.9-" .planning/REQUIREMENTS.md | awk '$1>=11{exit 0} {exit 1}' && grep -q "REQ-34.9-11" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains all eleven REQ-34.9-NN entries, and every unticked box carries a dated note naming its gate-item precondition.
    - No box is ticked whose evidence is only a passing unit test where the contract assigned it a live item.
    - The `Mapped to phases` tally and `Unmapped: 0` line are updated consistently.
    - `.planning/ROADMAP.md`'s Phase 34.9 section lists the eleven requirement IDs, says `11 plans`, and lists all eleven plan files.
    - The ROADMAP's spawn-win prose matches `34.9-MEASUREMENT.md` per runner; if any runner fell short, the blanket ~95x claim is gone and the nile-only provenance of the original table is stated.
    - On a FAIL verdict, the ROADMAP says the phase does not close and names the gap-cycle command.
  </acceptance_criteria>
  <done>The requirement ledger and the ROADMAP tell the same story the evidence does, including where that story is smaller than the one the phase started with.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human-reported outcomes → the recorded verdict | what the runner says happened becomes the project's record |
| session evidence → requirement ledger | a ticked box is a durable claim later phases build on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.9-36 | Repudiation | verdict recorded from summary rather than evidence | mitigate | Every item's outcome must quote a verbatim excerpt from the session directory's transcript or archived `gamelib-launch-N.log`, with its launch ordinal — not the runner's prose summary. This project has previously recorded PASSes on an operator's verbatim word alone with no surviving transcript corroboration, and had to flag it as a finding. |
| T-34.9-37 | Information disclosure (false confidence) | ticking a requirement box on unit-test evidence | mitigate | Task 3 forbids ticking any box that the contract assigned a live item unless that item PASSED. D-08's no-partial-pass rule applies to the verdict, and an unticked box must carry a dated note naming the gate item that is its precondition. |
| T-34.9-35 | Repudiation | inferring absence from transcript silence | mitigate | Restated as an explicit prohibition in Task 2: sidecar output cannot reach the transcript, so its absence there discriminates nothing. Any "did not run" claim needs a channel that would show presence. |
| T-34.9-38 | Tampering | a falsified measurement surviving in the ROADMAP | mitigate | Task 3 requires the ROADMAP's ~95x prose to be reconciled against `34.9-MEASUREMENT.md`'s per-runner figures, and to state the original table's nile-only provenance if any runner fell short. A phase may not leave behind a headline number its own measurement falsified. |
| T-34.9-32 | Spoofing | a concurrent second instance corrupting the run | mitigate | The checkpoint's step 4 requires the single-PID assertion before and after each launch, and aborts any launch where the count is not 1, because the resulting evidence set — a populated shared `gamelib.log` beside a transcript missing every scored literal — reads as successfully measured to anyone checking only that the logs have content. |
| T-34.9-25 | Denial of Service (fail-safe) | packaged-Tauri and notarization gaps | accept (owned elsewhere) | Both are recorded in `## What this run did not prove` with their owners (Phase 34.5's deferred item 12; D-03/D-04) and the fact that the packaged-Tauri residual pre-dates this phase. |
| T-34.9-SC | Tampering | package installs | mitigate | This plan installs nothing; it runs an existing build, an existing verification tool and an existing app, and edits three markdown files. No legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `34.9-LIVE-GATE.md` carries a non-null verdict with per-item outcomes and quoted evidence.
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` both reference REQ-34.9-01..11 consistently.
- The ROADMAP's Phase 34.9 spawn-win prose agrees with `34.9-MEASUREMENT.md`.
- On FAIL, the ROADMAP names the gap-cycle route and the phase is not marked complete.
</verification>

<success_criteria>
- The gate ran on real hardware and its verdict is recorded with quoted, launch-attributed evidence.
- Requirement boxes reflect measurement, not intention.
- The ROADMAP no longer carries any claim this phase's own measurements contradict.
- Tauri-packaged, real-cert notarization and any missing x64 leg are named as unproven, with owners.
</success_criteria>

<output>
Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-11-SUMMARY.md` when done.
</output>
