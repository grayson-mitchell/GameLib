---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
review: 34.9-REVIEW.md
status: all_fixed
fixed: 2026-08-22
record_updated: 2026-08-23
findings_total: 6
findings_fixed: 6
outstanding: []
---

# Fix pass for 34.9-REVIEW.md — phase 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co

## Why this file exists

`34.9-REVIEW.md` carries `status: issues_found` and `findings.critical: 1`. That status records
what the review found on 2026-08-11 and, per `reviewStatus()`'s own doc comment in
`~/.vscode/extensions/gsd-phase-status/parse.js`, is "never rewritten when the fixes land" — the
review file itself is not the place to record remediation. The sanctioned way to state where a
review now stands is this fix-pass sibling: when a `REVIEW-FIX.md` exists beside a `REVIEW.md`,
`reviewStatus()` reads the fix pass's `status:` instead of falling back to the review's own
(stale) status or its unaddressed-critical rule.

This file changes no behaviour and touches no code. It is a record, sourced verbatim from
`deferred-items.md`, of where each of the six findings in `34.9-REVIEW.md` currently stands.

## Dispositions

| Finding | Severity | Disposition | Evidence |
|---|---|---|---|
| CR-01 | Critical | FIXED | 34.9-18 (`closeBundle` throw) + 34.9-20 (wired into `dist:mac`/`release:mac`) + 34.9-21 (`34.9-GUARD-PROOF.md`, live proof, verdict PASS) — `deferred-items.md:325` |
| WR-01 | Warning | FIXED | 34.9-18 (`isContainedSymlinkTarget`, `meta/preserveRunnerSymlinks.ts`) — `deferred-items.md:326` |
| WR-02 | Warning | FIXED | 34.9-19 (`summarise()`'s `!fw.topLevelStubExists` branch, `meta/verifyRunnerBundle.ts`) — `deferred-items.md:327` |
| IN-01 | Info | FIXED | 34.9-19 (`meta/cleanDistMac.ts` doc-comment correction, pinned) — `deferred-items.md:328` |
| IN-02 | Info | FIXED | 34.9-19 (`meta/cleanDistMac.ts` doc-comment downgraded to defense-in-depth, pinned) — `deferred-items.md:329` |
| IN-03 | Info | FIXED | quick task 260822-hrf (commits `df7af9f4a` rename + `ab1ee0448` generalization) — `deferred-items.md:336`, closure note at `deferred-items.md:363` |

## Outstanding — none

**Nothing is outstanding.** All six findings in `34.9-REVIEW.md` are `FIXED` per the table above.

**IN-03 closed on 2026-08-22, one day after this file first recorded it as deferred** (2026-08-22,
quick task `260822-h37`, commit `00a486adf`) — the two quick tasks ran the same day and this record
simply did not follow the fix. What landed, per item 11's closure note: `meta/cleanDistMac.ts` was
renamed to `meta/cleanDist.ts` (`df7af9f4a`) and generalized to a **required** `--platform=`
mac/win/linux argument with no silent default (`ab1ee0448`), keyed off `electron-builder.yml`'s
real per-platform artifact-name tokens. `dist:win`/`release:win`/`dist:linux`/`release:linux` now
run their own `clean:dist-win` / `clean:dist-linux` prefix, mirroring the `dist:mac` wiring IN-03
originally found missing. The named precondition's *second* branch is what landed — a decision to
generalize without waiting for a reproducing win/linux build — which is one of the two branches
item 11 itself named, not a substitution for it.

**The UNCONFIRMED caveat is carried forward unchanged, because closure did not retire it.** This
machine is macOS arm64 with no win/linux build to run live, so win/linux coverage is
**synthetic-fixture-only**: `meta/__tests__/cleanDist.test.ts`'s three-platform `dist/` fixture
proves each platform's clean removes only its own entries and leaves the other two byte-identical.
That is a fixture-level non-deletion proof, not a live win/linux build observation. macOS behaviour
is unchanged verbatim, and IN-01/IN-02's doc-comment pins were re-baselined against the new path.
No sentence here asserts win/linux was broken, and none asserts a live win/linux run has occurred.

- **Ledger item:** 11 — **CLOSED** (`deferred-items.md:363`)
- **Prior OWNER (discharged):** follow-up packaging phase / developer action, dated 2026-08-12.
  Superseded by the 2026-08-22 landing; no phase inherits this item.

## Scope — what this file does not cover

This artifact dispositions the findings of `34.9-REVIEW.md` **only**. `34.9-REVIEW-CYCLE2.md`,
`34.9-REVIEW-CYCLE3.md`, `34.9-REVIEW-CYCLE4.md`, and `34.9-REVIEW-CYCLE5.md` are separate review
documents whose own findings (`C2-01`..`C2-08`, `C3-01`..`C3-03`, `C4-01`..`C4-05`, `C5-01`,
`C5-02`) are dispositioned separately in `deferred-items.md` and are not restated here.

~~`artifactKind()` in `extension.js` matches a filename only when
`name === kind || name.endsWith('-' + kind)`. A file named `...-CYCLE4.md` ends in neither
`-REVIEW.md` nor `-REVIEW-FIX.md`, so the cycle-review files carry **no badge at all** in the
Explorer.~~ **Fixed 2026-08-23 by quick task `260823-d7j`** (gsd-phase-status v0.8.0): cycle
reviews are now matched by pattern and carry their own `disposition:` field, declared from the C2
through C5 tables in `deferred-items.md` — CYCLE2 `partial` (C2-05/C2-07 deferred to ledger items
18 and 19), CYCLE3/4/5 `closed`. A cycle review deliberately does **not** inherit this file's
status, precisely because of the scope statement above. This file still cannot and does not speak
for them: it is not a phase-wide all-clear, it is a record for one review file's six findings.

The phase's own sweep tool, re-run after this file was written, confirms both that the cycle
findings remain independently tracked and that this file did not inflate the sweep's ID census
(see Verification below for the verbatim output).

## Verification

Verbatim output. The three gates below were authored by quick task `260822-h37` and **re-run in
full** by `260823-ris` after the IN-03 flip — a status change invalidates all three transcripts, so
none was hand-edited. Each carries an observed rejection: a gate that only ever agrees with the
file in front of it measures nothing. Full transcripts in each task's SUMMARY.

```
DISPOSITIONS-MATCH-LEDGER 6/6      (IN-03 -> FIXED on both sides)
```

```
LIVE                           -> OK all_fixed (outstanding: none)
RED-PROOF row flipped          -> DISHONEST: status all_fixed with non-FIXED rows IN-03
RED-PROOF outstanding refilled -> DISHONEST: status all_fixed while outstanding names IN-03
HONESTY-GATE-OK (1 live pass, 2 observed rejections)
```

```
fix.status                 = all_fixed
REVIEW badge              -> complete
REVIEW-FIX badge          -> complete
RED-PROOF (status partial)-> inprogress
RED-PROOF (no fix pass)   -> blocked
ARTIFACT_KINDS order        = REVIEW-FIX.md at 0, REVIEW.md at 6 (ok)
BADGE-GATE-OK
```

The phase's own sweep tool, re-run from the phase directory after this edit, confirms the ID
census was not perturbed by it:

```
REVIEW-SWEEP-OK 24/24 mapped, unmapped 0        (exit 0)
```

```
DISCOVERED-REVIEW-FILES: 34.9-REVIEW-CYCLE2.md, 34.9-REVIEW-CYCLE3.md, 34.9-REVIEW-CYCLE4.md, 34.9-REVIEW-CYCLE5.md, 34.9-REVIEW-FIX.md, 34.9-REVIEW.md
LIST-A: 24 IDs (C2-01, C2-02, C2-03, C2-04, C2-05, C2-06, C2-07, C2-08, C3-01, C3-02, C3-03, C4-01, C4-02, C4-03, C4-04, C4-05, C5-01, C5-02, CR-01, WR-01, WR-02, IN-01, IN-02, IN-03)
COMPUTED-UNMAPPED: 0
REVIEW-SWEEP-OK 24/24 mapped, unmapped 0
SWEEP-UNPERTURBED-OK
```
