---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 26
subsystem: infra
tags: [esbuild, pnpm, build-scripts, exit-code, ci-guard, proof-contract]

# Dependency graph
requires:
  - phase: 34.9-25
    provides: the pipe-to-`&&` conversion under test (13 scripts, `esbuild --outfile=... && node ...`)
  - phase: 34.9-20/34.9-21
    provides: "34.9-GUARD-PROOF.md's author/runner separation precedent and its 2026-08-12 healthy-`dist:mac` same-session control (cited here as a prior control when this plan's own control was gated out)"
provides:
  - "34.9-PIPE-PROOF.md, a both-direction, known-bad-input, disk-evidence-only proof that plan 34.9-25's pipe-to-`&&` conversion converts silent success into loud failure -- per script (13/13, both failure shapes) and at chain level (both C-cheap and C-load)"
  - "`34.9-VERIFICATION.md` truth 8 satisfied by measurement, scoped strictly to arm64/local/non-CI"
  - "deferred-items.md item 17 -- the gen-i18n-gate-scope catalogue-drift finding (F-34.9-26-01) surfaced during Direction B, ledgered rather than left dangling"
affects: [34.9-27, 34.9-28, 34.9 gap cycle 3 verification (truth 8)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Task-level author/runner separation within a single plan (weaker than 34.9-GUARD-PROOF.md's cross-plan separation, stated as such): Task 1 authors an unrun contract, Tasks 2/3 are the only tasks permitted to fill any Observed:/verdict field"
    - "Independent, non-cited restore-audit recomputation as the final gate before a PASS verdict -- Task 3 re-derives shasum/grep/git-diff evidence itself rather than trusting Task 2's or its own earlier Section 6 claims"

key-files:
  created: []
  modified:
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PIPE-PROOF.md
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md

key-decisions:
  - "Resumed a killed executor session mid-Task-3 without re-running any already-recorded direction (Direction A/B, both chain proofs) -- verified via git status/diff that no meta/*.ts file was left mid-injection before proceeding, consistent with the harness's one-file-at-a-time restore discipline."
  - "Defined F-34.9-26-01 (present in frontmatter findings: list but undefined in the document body and unledgered at hand-off) as the gen-i18n-gate-scope catalogue-drift finding -- the exact defect pattern (a finding sitting in a document with no ledger entry) that made verification truth 8 FAIL originally, closed here rather than repeated."
  - "Did not open a second ledger row for the gated-out same-session chain control -- it is an explicitly contract-anticipated, machine-gated outcome already fully documented in Section 6 with its own citation and downgrade language, not a new discovery."
  - "Re-scored the verdict against the plan's full-conjunction discipline independently rather than inheriting the interrupted session's provisional PASS -- confirmed all four conjuncts true from measured evidence before accepting PASS."

patterns-established:
  - "A continuation executor picking up a killed session must recompute the audit steps still owed by that session's own procedure (here, the restore audit) rather than treat an uncommitted frontmatter verdict as authoritative."

requirements-completed: [REQ-34.9-08]

duration: 75min
completed: 2026-08-13
---

# Phase 34.9 Plan 26: Prove the pipe-to-`&&` conversion actually converts silent success into loud failure Summary

**Both-direction, known-bad-input proof (36/36 PASS) that plan 34.9-25's `esbuild --outfile=... && node ...` conversion aborts on a compile failure -- per script (13/13, both parse-error and unresolvable-import shapes) and at `dist:mac` chain level (both the cheap and load-bearing break points) -- closing `34.9-VERIFICATION.md` truth 8 by measurement, scoped to arm64/local/non-CI.**

## Performance

- **Duration:** ~75 min across the plan's three task commits (2026-08-13T17:38 -> 18:53)
- **Started:** 2026-08-13T17:38:00+12:00
- **Completed:** 2026-08-13T18:53:22+12:00
- **Tasks:** 3
- **Files modified:** 2 (`34.9-PIPE-PROOF.md`, `deferred-items.md`)

## Continuity note (this session)

Task 3 was started by a prior executor session that was **killed by an API error mid-task** --
after preconditions, chain proof C-cheap and chain proof C-load had already run, been scored, and
been recorded (uncommitted), but before the independent restore audit or the `# RUN RECORD` section
had been written. This continuation session verified, before doing anything else, that the working
tree held exactly one modified file (`34.9-PIPE-PROOF.md`), that `git diff -- meta/` and
`git diff -- package.json` were both empty, and that no `meta/*.ts` file was left mid-injection --
i.e. the interruption left no stranded mutation. It then completed Step 5 (the independent restore
audit, recomputed rather than cited) and Step 6 (the `# RUN RECORD`, findings table, and the
`deferred-items.md` ledger row), without re-running any already-recorded direction.

## Accomplishments
- **Direction A: 13/13 census scripts PASS in BOTH failure shapes** (S1 parse error, S2
  unresolvable import) -- 26/26 individual runs, each scored against all four criteria: non-zero
  exit, cache `.cjs` proven absent, esbuild's `[ERROR]` marker present, `Cannot find module` proven
  absent (positive evidence `node` was never reached).
- **Direction B: 8/8 safe-to-run scripts PASS** -- exit `0`, cache `.cjs` present and non-empty,
  and each script's own success evidence quoted verbatim from its own log. The 5 excluded scripts
  (network I/O or destructive) are named with reasons in the matrix and again in "What this proof
  does NOT prove" -- no silent coverage cap.
- **Chain proof C-cheap PASS**: breaking `meta/cleanDistMac.ts` aborts `pnpm dist:mac` at its second
  `&&` step, in seconds, with `dist/` byte-identical before and after and zero electron-builder
  banners.
- **Chain proof C-load PASS, the load-bearing one**: breaking `meta/verifyRunnerBundle.ts` lets the
  chain run `clean:dist-mac`, `build-steam-bridge`, and a full `electron-vite build`, then abort at
  `verify:runner-bundle` -- zero electron-builder banners, no `.dmg`/`.zip` newer than the marker.
  This is the exact case `34.9-GUARD-PROOF.md` section 7 states it does not cover, now closed.
- **The exit-status capture form was proven sound** (known-1 and known-0 controls, both exactly
  2-byte `.exit` files) before any reading was scored, forbidding `tee`/`${PIPESTATUS[0]}` per
  F-34.9-21-02.
- **The independent restore audit was recomputed by this task**, not cited from Task 2 or from
  Section 6's own chain-proof claims: all 12 entry-file `shasum -a 256` values match the
  pre-injection baseline byte-for-byte, `grep -rn` for both injection sentinels scoped to `meta/`
  returns zero matches, `git diff -- package.json` and `git diff -- meta/` are both empty, and
  `<session>/pristine/` still holds all 12 copies.
- **The same-session healthy chain control was correctly recorded NOT RUN**, gated on `df` = 13Gi
  free (below the plan's 20Gi bar); `34.9-GUARD-PROOF.md`'s 2026-08-12 line-457->458 observation is
  cited as a prior control, not a paired same-session one, with the chain claim downgraded in
  writing.
- **F-34.9-26-01 was defined and ledgered.** It existed only in the frontmatter `findings:` list at
  hand-off, undefined in the document body and absent from `deferred-items.md` -- the exact "finding
  with no ledger entry" pattern that made verification truth 8 FAIL originally. Determined from
  Task 2's own recorded evidence (the `gen-i18n-gate-scope` Direction B row already anticipated
  "Task 3's findings table" and "deferred-items.md item 17") to be the catalogue-drift finding:
  `meta/i18nGateScope.json`'s committed snapshot is stale by 12 lines against the live source tree.
  Defined in the RUN RECORD's findings table and ledgered as `deferred-items.md` item 17.
- **Verdict re-scored independently: PASS, 36/36 directions** (26 Direction A + 8 Direction B + 2
  chain proofs, 0 failed) -- all four conjuncts of the plan's verdict discipline (Direction A both
  shapes for all 13, Direction B for all 8 RUN scripts, both chain proofs, restore audit match)
  confirmed true from measured evidence, not inherited from the interrupted session's provisional
  frontmatter.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author the proof contract, unrun** - `1ac3f89bd` (docs) -- completed in a prior session
2. **Task 2: Run Direction A + B** - `f88f2cb92` (docs) -- completed in a prior session, 13/13 and 8/8
3. **Task 3: Prove the chain aborts, audit the restore independently, write the verdict** - `d34176240` (docs) -- this session, resumed after an API-error interruption mid-task

**Plan metadata:** committed separately, see final commit below.

## Files Created/Modified
- `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PIPE-PROOF.md` - Appended `# RUN RECORD -- 2026-08-13` (verdict, per-direction outcome table, independently recomputed restore audit, findings table, deviations, what-this-run-does-NOT-prove); fixed the notes block's completion narrative; removed the one remaining literal `(UNFILLED` occurrence from Contract Validation (c)'s prose, which had survived as descriptive text quoting the sentinel rather than an actual unfilled field, so `grep -c '(UNFILLED'` now returns `0` as Task 3's acceptance criteria require.
- `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md` - Opened item 17 (`meta/i18nGateScope.json`'s committed snapshot is stale against the live source tree, F-34.9-26-01) with a dated blocker, a named precondition, and an `OWNER:` line matching the existing entries' format.

## Decisions Made
- Resumed the killed Task 3 session without redoing Tasks 1/2 or the already-recorded chain proofs, after independently confirming (via `git status --porcelain`, `git diff -- meta/`, `git diff -- package.json`) that the interruption left no stranded mid-injection mutation.
- Identified F-34.9-26-01 from Task 2's own recorded evidence rather than guessing: the `gen-i18n-gate-scope` Direction B row in Section 4's matrix already named "Task 3's findings table" and "deferred-items.md item 17" as its destination, making the catalogue-drift finding the only candidate consistent with the document's own internal cross-references.
- Chose not to open a second ledger row for the gated-out same-session chain control, since it is a machine-gated, fully-documented, contract-anticipated outcome (Section 6's own citation and downgrade language), distinct in kind from a genuine new discovery.
- Fixed the one surviving `(UNFILLED` string in Contract Validation (c)'s prose (which quoted the sentinel literal as documentation, not as an actual unfilled field) to satisfy Task 3's `grep -c '(UNFILLED' == 0` acceptance criterion without deleting the substance of Task 1's authoring self-check statement.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed the one remaining `(UNFILLED` literal, which was blocking Task 3's own acceptance criterion**
- **Found during:** Task 3, running the plan's own automated verification command after writing the RUN RECORD.
- **Issue:** Contract Validation (c)'s authoring self-check prose (written by Task 1) quoted the sentinel literal `(UNFILLED -- plan 34.9-26 Task 2/3 writes this)` as descriptive text explaining what every result field carried at authoring time. This is legitimate prose, not an actual unfilled field -- but Task 3's acceptance criterion requires `grep -c '(UNFILLED' 34.9-PIPE-PROOF.md` to return `0` with no exception carved out for descriptive quotation.
- **Fix:** Rephrased the sentence to describe the same fact (every result field carried Task 1's placeholder at authoring time, since replaced by Task 2/3's measured values) without reproducing the literal string.
- **Verification:** `grep -c '(UNFILLED' 34.9-PIPE-PROOF.md` returns `0`; the plan's full Task 3 automated verify command passes end-to-end (`CHAIN-PROVEN-TREE-CLEAN`).
- **Committed in:** `d34176240` (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 -- a verification-blocking wording collision, not a substantive content change).
**Impact on plan:** No scope creep. The fix preserves Task 1's authoring self-check claim exactly; it only removes an accidental self-match against Task 3's own zero-unfilled-sentinels gate.

## Issues Encountered
- The prior Task 3 session was killed by an API error partway through, leaving Section 6 (both chain proofs) recorded but Section 6's own restore audit unrun and the RUN RECORD unwritten. Resolved by resuming from the documented state, independently re-verifying no stranded mutation existed, and completing the remaining steps without redoing already-scored work. See "Continuity note" above.
- The plan's Task 3 acceptance criteria required `grep -c '(UNFILLED' == 0`, but Task 1's own authoring self-check prose legitimately quoted that literal as documentation. This is a genuine wording collision between two of the plan's own acceptance criteria (Task 1's specimen-honesty requirement to state what the sentinel was, and Task 3's zero-unfilled-cells gate) -- resolved by rewording rather than by weakening either requirement.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `34.9-VERIFICATION.md` truth 8 is now satisfied by measurement (this plan's PASS verdict), scoped strictly to arm64/local/non-CI, consistent with every other proof this phase has produced.
- Plan 34.9-27 (not this plan) owns closing C2-04 with a wiring-pin regression test for `verify:runner-bundle`'s position in `dist:mac`/`release:mac`, and recording C2-05/C2-07 as dated, owned deferred entries. Plan 34.9-27 also owns `meta/__tests__/**`, untouched by this plan per the scope fence.
- Plan 34.9-28 owns the final gap-cycle-3 sweep (all eight `34.9-REVIEW-CYCLE2.md` findings by set-difference) and should confirm `deferred-items.md` item 17 is picked up in its own set-difference sweep, since it was opened by this plan.
- No new REQ IDs were minted. REQ-34.9-08 is the only requirement this plan advances; it remains the sole owner of gap cycle 3.
- The session directory
  (`/Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-pipe-proof/20260813T052011Z`)
  was NOT deleted, per this plan's instruction, and remains available for any future audit of the raw
  evidence.
- No `pnpm test:ci` run was required or performed by this plan -- it is a proof-contract-only plan
  touching two planning documents; no source, script, or test file was modified.

## Self-Check: PASSED

`.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PIPE-PROOF.md`:
FOUND. `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md`:
FOUND. This SUMMARY.md: FOUND. All three task commits (`1ac3f89bd`, `f88f2cb92`, `d34176240`)
verified present via `git log --oneline --all | grep`. No missing items.

---
*Phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co*
*Completed: 2026-08-13*
