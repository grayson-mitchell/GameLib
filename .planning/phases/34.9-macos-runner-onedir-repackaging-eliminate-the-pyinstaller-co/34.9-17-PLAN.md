---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 17
type: execute
wave: 4
gap_closure: true
closes_findings: []
depends_on: ["34.9-16"]
files_modified:
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [REQ-34.9-02, REQ-34.9-03, REQ-34.9-04, REQ-34.9-08, REQ-34.9-09, REQ-34.9-11]

must_haves:
  truths:
    - "Every descoped item has a dated ledger entry naming its blocker, its named precondition, and an owner — none is silently dropped and none is marked complete."
    - "The two out-of-scope UI defects the gate observed are recorded with their evidence and an owner, and are NOT fixed by this phase."
    - "REQUIREMENTS.md's REQ-34.9-01..11 rows and checkbox block match the measured post-gap-cycle truth, whatever the re-run's verdict was."
    - "ROADMAP.md's Phase 34.9 section and STATE.md carry the same truth as REQUIREMENTS.md — no ledger disagrees with another."
    - "The x64 onedir leg is recorded as permanently blocked FOR THIS PHASE with its precondition (land the workflow on the default branch, then dispatch), and no plan pushes to `main`."
  artifacts:
    - path: ".planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md"
      provides: "the phase's out-of-scope and blocked-item ledger"
      contains: "F-34.9"
    - path: ".planning/REQUIREMENTS.md"
      provides: "reconciled REQ-34.9-01..11 status rows and checkbox block"
    - path: ".planning/ROADMAP.md"
      provides: "reconciled Phase 34.9 status note and plan count"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "34.9-LIVE-GATE-RERUN.md"
      via: "each status citing the gate item and date that produced it"
      pattern: "34.9-LIVE-GATE-RERUN"
    - from: "deferred-items.md"
      to: "the owning phase for each deferred item"
      via: "a named owner per entry"
      pattern: "OWNER:"
---

<objective>
Reconcile every ledger to the post-gap-cycle truth, and record — honestly and datedly — everything
this gap cycle deliberately did NOT do.

Purpose: the scope fence for this gap cycle descoped six things. Descoped is not the same as done,
and it is not the same as forgotten. Each one needs a dated entry naming its blocker, its
precondition and its owner, so it surfaces as a decision rather than as an omission at the next
verification pass.

Output: a phase-level `deferred-items.md`, and `REQUIREMENTS.md` / `ROADMAP.md` / `STATE.md`
reconciled so no two ledgers disagree.

**This plan is outcome-agnostic.** It records what the re-run measured, whatever that was. If the
re-run FAILED, this plan records a second FAIL and routes to another gap cycle — it does not soften
the record or close the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-CI-ROUNDTRIP.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-MEASUREMENT.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PACKAGING-LIMITATIONS.md

<interfaces>
<!-- The six descoped items and their exact dispositions, decided by the user at gap-planning
     time (2026-08-11). Record these; do NOT plan work for any of them. -->

| Item | Disposition | Blocker | Named precondition | Owner |
|---|---|---|---|---|
| REQ-34.9-02 (CI x64 leg) | permanently blocked FOR THIS PHASE | GitHub's `workflow_dispatch` refuses any workflow absent from the repository's **default branch**, regardless of `--ref` (`34.9-CI-ROUNDTRIP.md`, Outcome C) | land `.github/workflows/build-runners-onedir-macos.yml` on the default branch, then dispatch | follow-up phase / developer action |
| REQ-34.9-03 (downloader sources darwin archives) | code complete, path unproven | REQ-34.9-02 | REQ-34.9-02 | same |
| REQ-34.9-04 (sha256 digest verification) | code complete, never exercised | REQ-34.9-02 — all six `meta/runnersOnedirDigests.json` entries remain `PENDING-CI-PUBLISH` sentinels that throw by design | REQ-34.9-02 | same |
| REQ-34.9-09 (cold spawn ratio) | satisfied-on-WARM, cold gap named | two cold-measurement attempts failed their own harness validity anchor | a valid cold-measurement run for legendary and gogdl | not scheduled; do NOT plan a third attempt |
| Tauri-PACKAGED resolution | UNPROVEN, unscored | `R-34.5-G1-PKG` (Phase 34.5 `deferred-items.md` item 12), which PRE-DATES this phase | whichever plan first exercises a packaged non-dev Tauri build | Phase 34.5's deferred item / the first packaged-Tauri plan |
| real-certificate signing & notarization | out of scope | D-03/D-04 — no Apple Developer credentials enrolled | Apple Developer Program enrollment | developer action |

**Consequence to record explicitly, not implied:** the x64 onedir leg **exists nowhere on any
machine or in the repo**, so Phase 34.9 closes on the **arm64 leg only**. The six
`PENDING-CI-PUBLISH` sentinels stay. **Do not plan a push to `main`** and do not submit anything
upstream — both are the developer's action, and both have already been declined or blocked for this
phase (`34.9-UPSTREAM-PRS.md`: all three upstream PRs DECLINED 2026-08-11, which makes GameLib's
local onedir build permanent and un-droppable).

**Also to record, and NOT fix:** the pre-existing plaintext PKCE logging at
`src/backend/storeManagers/nile/user.ts:62` — `logInfo(['Register data is:', output], ...)` writes
url/`code_verifier`/`serial`/`client_id` to `gamelib.log`. Already recorded as out-of-scope for a
future security pass; leave the code alone and keep the ledger entry.

**The two out-of-scope UI defects the 2026-08-11 gate observed, verbatim from its run record.**
Both live in generic UI components this phase never touched; no scored item depended on either.

1. **Silent, intermittent file-picker failure.** `electronStub.showOpenDialog` converts every
   failure — timeout, unknown channel, permission denial — into `{canceled: true, filePaths: []}`,
   byte-identical to a genuine user cancel, with its only diagnostic a `console.warn` from the
   sidecar, whose stdout IS the RPC pipe and reaches no readable sink. One picker attempt during
   that run beeped, would not navigate, and committed nothing; a later attempt succeeded.
   Intermittent, not categorically broken. **OWNER: Phase 35 (Electron cutover), which owns
   `electronStub.ts` directly.**
2. **`PathSelectionBox` discards typed input unless the field is blurred.** It commits via
   `onBlur={(e) => onPathChange(e.target.value)}` with no Enter handler anywhere in the
   `PathSelectionBox` → `TextInputWithIconField` → `TextInputField` chain. Pressing Enter does
   nothing and there is no affordance saying so. Separately, pasting into the field produced a
   repeating unrenderable glyph rather than the clipboard text, consistent with this project's
   standing note that clipboard behaviour under the Tauri/WKWebView host differs from Chromium.
   **OWNER: no UI phase remains after 34.11 — record as UNASSIGNED and surface it to the developer
   in this plan's Summary as an explicit decision owed. Do not invent a phase number.**

**Ledger-tooling trap, standing and recurring:** `gsd-sdk state.record-session` has corrupted
`STATE.md` before — on 2026-08-09 it set `completed_plans` 247→248 and `percent` 92→65. Always
re-read `STATE.md` after any SDK write and hand-correct it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the phase deferred-items ledger</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md</files>
  <read_first>
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md — the established format for this project: numbered items grouped by the plan that found them, each with evidence, a scope-boundary reason, and (where resolved) a dated resolution block
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md — the RUN RECORD's two out-of-scope defect bullets, verbatim
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-CI-ROUNDTRIP.md — Outcome C, for the exact `workflow_dispatch` mechanism
    - The `<interfaces>` block above — all six descoped items and both UI defects are already stated there; do not re-derive them
  </read_first>
  <action>
    Create
    `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md`.

    Open with a one-paragraph header stating what the file is: items this phase deliberately did NOT
    do, each recorded as a decision with a date, a blocker, a named precondition and an owner —
    never as an omission.

    Section **"Descoped by the 2026-08-11 gap-planning scope fence"**: one numbered entry per row of
    the `<interfaces>` table. Each entry carries: what it is, why it is blocked (the MECHANISM, not
    a summary — e.g. `workflow_dispatch` refuses a workflow absent from the default branch
    regardless of `--ref`), the named precondition to unblock it, and an `OWNER:` line. For
    REQ-34.9-02, state the consequence explicitly: the x64 onedir leg exists nowhere on any machine
    or in the repo, so Phase 34.9 closes on the arm64 leg only, and the six `PENDING-CI-PUBLISH`
    sentinels stay. State that pushing to the default branch is the developer's action and that this
    gap cycle deliberately did not do it.

    Section **"Out-of-scope defects observed during the 2026-08-11 gate run"**: the two UI defects,
    reproduced with their evidence and their `OWNER:` lines. Record, do not fix. For the
    `PathSelectionBox` entry, write `OWNER: UNASSIGNED — no UI-owning phase remains after 34.11;
    developer decision owed` and cross-reference this project's standing note that
    `navigator.clipboard` silently no-ops under the Tauri host, since the paste-glyph half of that
    defect is consistent with it.

    Section **"Pre-existing, recorded, not fixed"**: the plaintext PKCE logging at
    `src/backend/storeManagers/nile/user.ts:62`, with `OWNER: a future security pass` and the
    explicit note that it is unrelated to onedir packaging and predates this phase.

    Section **"Measurement gaps"**: REQ-34.9-09's cold ratio. State what IS measured and valid (the
    WARM ratio for all three runners, 26.6x–32.5x, `34.9-MEASUREMENT.md` §8.4) and what is NOT (the
    cold ratio, UNMEASURED, two void runs). State that the `~95x cold` headline is retired and that
    the one citable cold figure (nile ~20.84s → 0.22s) comes from the earlier
    `nile-spawn-app-side-latency` debug session, applies to nile alone, and is independent of this
    phase's invalidated runs. Record that a third cold-measurement attempt is deliberately NOT
    scheduled.

    Every entry gets a date. Transcribe no identifier, no PKCE value, no account id — this file
    names `code_verifier`/`serial`/`client_id` only as the field NAMES that must not be logged.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/34.9-.../deferred-items.md` succeeds
    - `grep -c "^OWNER:\|OWNER:" <file>` returns >= 9 — one owner per descoped item, per UI defect, and for the PKCE entry
    - All four section headings are present (verify with anchored `grep -n "^## "` and list them in the Summary)
    - The REQ-34.9-02 entry contains the literal `default branch` and the literal `workflow_dispatch` — the mechanism, not a summary
    - The entry for `PathSelectionBox` contains `OWNER: UNASSIGNED` and the Summary surfaces the decision owed to the developer as its own line
    - `grep -in "code_verifier\|client_id\|serial" <file>` returns only lines that are prohibitions or field-name references, never a value — quote each matched line in the Summary to show this
    - Every entry carries a date (`grep -c "2026-" <file>` >= 9)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md; test -f "$F" && test "$(grep -c 'OWNER:' "$F")" -ge 9 && grep -q "workflow_dispatch" "$F" && grep -q "OWNER: UNASSIGNED" "$F" && echo DEFERRED-LEDGER-OK</automated>
  </verify>
  <done>Every descoped item, both out-of-scope UI defects, the pre-existing PKCE logging and the cold-measurement gap each have a dated entry naming blocker, precondition and owner.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile REQUIREMENTS.md, ROADMAP.md and STATE.md to the measured truth</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md — its RUN RECORD and frontmatter verdict are the ONLY source for REQ-34.9-08 and REQ-34.9-11's new status
    - .planning/REQUIREMENTS.md — the REQ-34.9-01..11 status table rows (~line 365) AND the checkbox block (~line 825); BOTH must be updated, and they must agree
    - .planning/ROADMAP.md — the `### Phase 34.9:` section (~line 2273), specifically its `**Plans:**` line, its `Requirements:` line, and its `Phase status 2026-08-11: DOES NOT CLOSE` note
    - .planning/STATE.md — `stopped_at`, `progress.completed_plans`, `progress.percent`, `last_updated`, and the Current Position blocker note
  </read_first>
  <action>
    Update all three ledgers from the re-run's measured verdict. Do not infer any status from a
    green test suite, from a plan Summary's own claim, or from this plan's expectations.

    **REQUIREMENTS.md.** Update the status-table rows and the checkbox block for REQ-34.9-01..11 so
    they agree with each other and with the gate:
    - REQ-34.9-08 and REQ-34.9-11: set from the re-run's verdict. Tick only if the corresponding
      scored item PASSED. Each status must cite the gate document, the item, and the date that
      produced it. If the re-run FAILED, record a second dated FAIL with the new finding ID, and
      leave the boxes unticked with the new precondition named.
    - REQ-34.9-02/03/04: keep unticked; update each dated note to point at the new
      `deferred-items.md` entry and restate the named precondition.
    - REQ-34.9-09: keep unticked; restate as satisfied-on-WARM with the cold gap named, pointing at
      the `deferred-items.md` measurement-gaps section.
    - REQ-34.9-01/05/06/07/10: unchanged. Confirm each is still accurate rather than assuming it —
      state in the Summary that you re-read each and what you confirmed.

    **ROADMAP.md**, the `### Phase 34.9:` section:
    - Update `**Plans:**` to the real total (11 original + this gap cycle's 6 = 17) with the
      executed count.
    - Replace the `Phase status 2026-08-11: DOES NOT CLOSE` note with a dated status reflecting the
      re-run: what was fixed (F-34.9-01's mechanism, F-34.9-02's trap, F-34.9-03's payload), what
      the re-run measured, and — whatever the verdict — the arm64-only scope with the x64 leg named
      as blocked. Keep the existing measurement-caveat block (the retired `~95x cold` headline and
      the warm table) intact; it is still correct.
    - If the re-run PASSED both items, the note says the phase CLOSES on the arm64 leg with the
      descoped set recorded in `deferred-items.md`. If it FAILED, the note says it does not close
      and routes to another gap cycle.

    **STATE.md.** Update `stopped_at`, `last_updated`, `last_activity`, `progress.completed_plans`
    and `progress.percent`, and the Current Position note for Phase 34.9. **Re-read `STATE.md` after
    any `gsd-sdk` write and hand-correct it** — `state.record-session` has corrupted this file
    before, on 2026-08-09 setting `completed_plans` 247→248 and `percent` 92→65. Verify the
    frontmatter arithmetic by hand and record the before/after values in the Summary.

    Do NOT touch any other phase's rows, notes or requirements. Do NOT push to `main`.
  </action>
  <acceptance_criteria>
    - `grep -n "REQ-34.9-0[1-9]\|REQ-34.9-1[01]" .planning/REQUIREMENTS.md` shows the status-table row and the checkbox entry for each of the eleven IDs, and the Summary presents them side by side showing every pair agrees
    - REQ-34.9-08's and REQ-34.9-11's statuses each cite `34.9-LIVE-GATE-RERUN.md`, a specific item, and the run date
    - Every unticked REQ-34.9 box carries a dated precondition naming the specific blocker (not a generic "pending")
    - `grep -c "34.9-LIVE-GATE-RERUN" .planning/REQUIREMENTS.md` returns >= 2
    - ROADMAP.md's Phase 34.9 `**Plans:**` line states 17 plans and the correct executed count
    - ROADMAP.md's phase-status note carries the re-run's date and verdict, and the existing warm-ratio measurement block is byte-identical (`git diff` shows no change inside it)
    - STATE.md's `progress.completed_plans` and `percent` are arithmetically consistent; before/after values are recorded in the Summary, and the Summary states that STATE.md was re-read after any SDK write
    - `git diff --name-only` for this plan's commits equals exactly this plan's `files_modified` — no other phase's ledger rows were touched
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '34.9-LIVE-GATE-RERUN' .planning/REQUIREMENTS.md)" -ge 2 && grep -q "17 plans\|17/17\|/17" .planning/ROADMAP.md && node -e "const fs=require('fs');const m=fs.readFileSync('.planning/STATE.md','utf8').split('---')[1];if(!/completed_plans:\s*\d+/.test(m)||!/percent:\s*\d+/.test(m))process.exit(1)" && echo LEDGERS-RECONCILED</automated>
  </verify>
  <done>REQUIREMENTS.md, ROADMAP.md and STATE.md all carry the same measured post-gap-cycle truth, every unticked requirement names its blocker and precondition, and STATE.md's arithmetic was hand-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger contents → future planning decisions | A ledger that overstates completion causes a later phase to build on an unproven foundation. This project has already had four executors hit `requirements.mark-complete REQ-34.9-NN → not_found` because a ledger was out of step. |
| descoped item → silent disappearance | An item removed from scope without a dated record reads as an omission at the next verification pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.9G-23 | Repudiation | a descoped item vanishing from the record | mitigate | Every descoped item gets a dated `deferred-items.md` entry with blocker, named precondition and `OWNER:`. An acceptance criterion counts >= 9 owner lines. |
| T-34.9G-24 | Spoofing | a requirement ticked from something other than measured evidence | mitigate | REQ-34.9-08 and REQ-34.9-11 may be set ONLY from `34.9-LIVE-GATE-RERUN.md`'s verdict, and each status must cite the document, the item and the date. The plan states in writing that a green suite, a plan Summary's own claim, and this plan's expectations are all inadmissible. |
| T-34.9G-25 | Tampering | `gsd-sdk` corrupting STATE.md | mitigate | The known 2026-08-09 corruption (`completed_plans` 247→248, `percent` 92→65) is named; the plan requires a re-read and hand-correction after any SDK write, with before/after values recorded. |
| T-34.9G-26 | Tampering | edits leaking into other phases' ledger rows | mitigate | An acceptance criterion requires `git diff --name-only` to equal exactly this plan's `files_modified`, and the action forbids touching any other phase's rows. |
| T-34.9G-27 | Information Disclosure | identifiers transcribed into a ledger | mitigate | `code_verifier`/`serial`/`client_id` appear only as field NAMES in a prohibition; an acceptance criterion requires each matched line to be quoted in the Summary to prove it is not a value. |
| T-34.9G-SC | Tampering | npm/pip/cargo installs | N/A | **This plan installs no packages and writes no code.** The Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `.planning/phases/34.9-.../deferred-items.md` exists with all four sections and >= 9 `OWNER:` lines.
- REQUIREMENTS.md's table rows and checkbox block agree for all eleven REQ-34.9 IDs.
- ROADMAP.md's Phase 34.9 plan count is 17 and its status note carries the re-run's date and verdict.
- STATE.md's progress arithmetic is consistent and was hand-verified after any SDK write.
- `git diff --name-only` across both task commits equals exactly this plan's `files_modified`.
- No push to `main`, no upstream submission.
- No user-facing string added — the i18n gate is N/A; state this explicitly in the Summary.
</verification>

<success_criteria>
Nothing this gap cycle chose not to do has disappeared: each descoped item, each out-of-scope UI
defect and the cold-measurement gap carries a dated entry with a blocker, a named precondition and
an owner. The three ledgers agree with each other and with the gate's measured verdict, and the
x64-only-blocked, arm64-only-proven scope is stated plainly rather than implied.
</success_criteria>

<output>
Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-17-SUMMARY.md` when done.
</output>
