---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 33
type: execute
wave: 4
depends_on: ["34.9-29", "34.9-30", "34.9-32"]
files_modified:
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
gap_cycle: 4
requirements: [REQ-34.9-08]
closes_findings: [C3-01, C3-02, C3-03]
implements_decisions: [D-C4-04, D-C4-05]

must_haves:
  truths:
    - "C3-01, C3-02 and C3-03 each have a disposition entry that a grep for C3-0 will find."
    - "The generalised sweep reports 0 unmapped across every review file in the phase directory."
    - "The review-after-sweep ordering defect is recorded as a protocol, so a future cycle does not re-derive it from scratch."
    - "This cycle's own code review is swept and ledgered BEFORE /gsd-verify-work runs -- the loop is broken in practice, not only described."
  artifacts:
    - path: ".planning/phases/34.9-.../deferred-items.md"
      provides: "Gap-cycle-3 finding disposition table + the loop-breaking protocol"
      contains: "C3-01"
    - path: ".planning/ROADMAP.md"
      provides: "Gap cycle 4 plan list and the recorded closure protocol"
  key_links:
    - from: "deferred-items.md disposition table"
      to: "34.9-REVIEW-CYCLE3.md findings"
      via: "34.9-REVIEW-SWEEP-CHECK.cjs set-difference"
      pattern: "C3-0"
---

<objective>
Close the gap, and break the loop that created it.

The gap: `34.9-REVIEW-CYCLE3.md`'s three findings have zero disposition anywhere -- a direct grep
for `C3-0` returns nothing across `deferred-items.md`, `STATE.md`, `ROADMAP.md` and
`REQUIREMENTS.md`. This phase has now hit that exact shape four times: CR-01 (missed by 34.9-17),
C2-01..C2-08 (missed by 34.9-22), C3-01..C3-03 (missed by 34.9-28 -- a plan written specifically to
stop it recurring).

The cause is not any plan's diligence. `/gsd-execute-phase` orders its gates: run all waves, then
`code_review_gate`, then `regression_gate`, then `verify_phase_goal`. A phase's reconciliation sweep
IS its last wave, so it runs BEFORE the review that produces the findings it is supposed to sweep.
Every cycle's review is unledgered by construction the instant it is written. Improving the sweep's
rigor a fifth time cannot work -- the step is structurally incapable of seeing a document that does
not exist yet.

Per D-C4-04, this plan breaks that ordering in practice: it is `autonomous: false` and ends with a
blocking checkpoint that carries the sweep PAST the review gate, and it records the protocol so a
future cycle inherits it instead of rediscovering it.

Purpose: the last unledgered finding set in this phase, plus the reason it kept happening.
Output: disposition entries, a recorded protocol, reconciled planning documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-REVIEW-CYCLE3.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-VERIFICATION.md

<polarity_self_trap priority="read-this-first">
A TRAP THIS PLAN WILL WALK INTO UNLESS IT IS DESIGNED AROUND.

Plan 34.9-30 tightened the sweep so a FIXED row whose confirmation text DENIES the fix is rejected.
The deny-list matches phrases like `not actually fixed` and `still has the bug`.

C3-03's own confirmation cell documents that the tightened check rejects the reviewer's
counter-example string -- and that counter-example string IS
`not actually fixed, meta/foo.ts still has the bug, package.json unchanged`.

If C3-03's confirmation cell quotes that string verbatim, the tightened polarity check will reject
C3-03's own row. Two individually-correct requirements contradict only in combination, which is
exactly the class item-by-item review cannot catch.

Resolution: reference the counter-example INDIRECTLY in the table cell -- cite it as "the verbatim
counter-example in `34.9-REVIEW-CYCLE3.md` finding C3-03" and record the observed rejection token.
The verbatim string belongs in `34.9-30-SUMMARY.md` and in the prose above the table, never inside a
scored table cell.

Second interaction, same shape: C3-03's fix lives in a `.planning/` file. Plan 34.9-30 re-expressed
the confirmation rule so a reproducible command plus its observed result is acceptable evidence even
for a `.planning/` artifact. C3-03's cell must therefore cite the COMMAND and its observed output,
not merely the script's path.
</polarity_self_trap>

<expected_sweep_states>
Measured at planning time, so both the red and green states are known in advance and neither can be
mistaken for a tooling failure:

  Before this plan's Task 1:  list A = 17 IDs, unmapped = 3 (C3-01, C3-02, C3-03), exit non-zero.
  After  this plan's Task 1:  list A = 17 IDs, unmapped = 0, exit 0.

That transition is the sweep's non-vacuity demonstration against real repository state.
</expected_sweep_states>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ledger C3-01, C3-02 and C3-03 to a disposition table</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md</files>
  <read_first>
    - `deferred-items.md`'s `## Code-review finding disposition — gap cycle 2 review (2026-08-13)` section IN FULL -- its List A / List B / A-minus-B / table structure is the shape plan 34.9-28 established and this table must match, including the `| Finding | Severity | Disposition | Evidence | Independent confirmation (non-.planning) |` column set.
    - `.planning/phases/34.9-.../34.9-REVIEW-CYCLE3.md` -- all three findings, for severities (C3-01 Warning, C3-02 Warning, C3-03 Info) and for accurate one-line descriptions.
    - `.planning/phases/34.9-.../34.9-29-SUMMARY.md`, `34.9-30-SUMMARY.md`, `34.9-32-SUMMARY.md` -- the landed evidence each row cites. Read them for the FACTS, then confirm each fact against the repository itself; a plan SUMMARY is a mutating command's own report and is never accepted as proof of its own effect.
    - `.planning/phases/34.9-.../34.9-REVIEW-SWEEP-CHECK.cjs` -- the exact row shape and confirmation rule the table must satisfy.
    - This plan's `<polarity_self_trap>` block.
  </read_first>
  <action>
    Append a `## Code-review finding disposition — gap cycle 3 review (2026-08-13)` section to
    `deferred-items.md`, mirroring the cycle-2 section's structure: a short prose preamble naming the
    structural cause, an explicit List A derived from `34.9-REVIEW-CYCLE3.md`'s own `### C3-NN`
    headings, a List B of IDs with a landed fix confirmed against the repository, the A-minus-B set,
    and then the table.

    All three rows are FIXED per locked decisions D-C4-01/02/03 -- **conditional on
    `34.9-WRAPPER-PROOF.md`'s recorded verdict being PASS.** If that verdict is anything other than
    PASS (including Direction C scored INVALID because its positive control failed to reproduce
    cross-contamination against the old idiom), do NOT mark C3-01 FIXED and do NOT write a FIXED row
    for it: stop, escalate to the operator, and leave C3-01 undispositioned rather than ledgering a
    disposition the evidence does not support. A FIXED row asserting a fix the proof did not
    establish is precisely the "reports success while doing something other than what was asked"
    defect class this whole gap cycle exists to close. C3-02 and C3-03 are independent of the proof
    verdict and may still be ledgered.

    Each row's Evidence cell names the
    delivering plan; each Independent-confirmation cell must be verified live at execution time
    against repository state, NOT copied from a SUMMARY:
    - C3-01 (Warning, FIXED, plans 34.9-29 + 34.9-32): confirm by parsing `package.json`'s `scripts`
      object live -- zero values reference `node_modules/.cache`, and the converted scripts route
      through `node meta/runTs.cjs`; plus `meta/runTs.cjs` using `mkdtempSync` for a private
      per-invocation directory; plus `34.9-WRAPPER-PROOF.md`'s recorded verdict and its Direction C
      control-then-trial counts.
    - C3-02 (Warning, FIXED, plan 34.9-30): confirm by reading `meta/lintTranslations.ts`'s doc
      comment live -- no pipe claim, no argv-unreachability claim, and an explicit statement that
      argv is reachable via `process.argv`.
    - C3-03 (Info, FIXED, plan 34.9-30): confirm by RUNNING `34.9-REVIEW-SWEEP-CHECK.cjs` against
      the reviewer's counter-example and recording the observed polarity-rejection token, plus the
      green run against the real ledger. Cite the command and its observed output. Per
      `<polarity_self_trap>`, do NOT quote the counter-example string verbatim inside the cell.

    Do not renumber, reword or delete any existing item (1-19, plus any item 20+ opened by plan
    34.9-32). This section appends; it does not rewrite history.

    Then run `34.9-REVIEW-SWEEP-CHECK.cjs` from the phase directory and confirm it now reports 0
    unmapped and exits 0. If any of the three rows is rejected, fix the ROW, never the CHECK -- a
    gate loosened to admit the row it was built to scrutinise is worse than no gate.
  </action>
  <verify>
    <automated>cd .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co && node 34.9-REVIEW-SWEEP-CHECK.cjs && grep -c 'C3-0' deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'C3-0' deferred-items.md` returns at least 3.
    - `34.9-REVIEW-SWEEP-CHECK.cjs`, run with no arguments from the phase directory, prints 0 unmapped and exits 0.
    - The table has exactly three data rows -- `C3-01`, `C3-02`, `C3-03` -- with severities Warning, Warning, Info, all dispositioned FIXED.
    - Every Independent-confirmation cell was verified live against repository state or a re-run command during this task, and the section says so.
    - C3-03's confirmation cell does NOT contain the verbatim counter-example string, and the row passes the polarity check.
    - No existing `deferred-items.md` item was renumbered, reworded, or deleted -- `git diff` shows only an appended section.
    - The sweep tool itself is unchanged by this task: `git diff --stat` shows no modification to `34.9-REVIEW-SWEEP-CHECK.cjs`.
  </acceptance_criteria>
  <done>All three gap-cycle-3 findings have a disposition a grep will find, and the generalised sweep reports zero unmapped across all three review cycles.</done>
</task>

<task type="auto">
  <name>Task 2: Record the loop-breaking protocol and reconcile the planning documents</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md</files>
  <read_first>
    - `.planning/ROADMAP.md`'s Phase 34.9 section IN FULL, including all three prior gap-cycle blocks and the `**Plans:**` derivation line -- the new block must match their established shape and the plan count must be re-derived, not incremented blindly.
    - `.planning/REQUIREMENTS.md`'s REQ-34.9-08 entry and its dated addenda -- the new addendum appends in the same style; do NOT mint a new REQ ID.
    - `.planning/STATE.md` -- current position, decisions and blockers.
    - `.planning/phases/34.9-.../34.9-VERIFICATION.md` gaps[0] -- the four `missing` items this cycle delivered against; the reconciliation must state the delivery state of each.
    - `.claude/skills/spike-findings-gamelib/SKILL.md` if present -- to judge whether the protocol belongs there too.
  </read_first>
  <action>
    Record the protocol (D-C4-04c) so it survives a context reset. Write it in TWO places, because a
    single location has already proven insufficient in this phase:

    (a) `deferred-items.md`, as a new `## Closure protocol — why every cycle's own review is
    unledgered by construction` section. State the mechanism plainly: `/gsd-execute-phase` runs all
    waves, THEN `code_review_gate`, THEN `regression_gate`, THEN `verify_phase_goal`; the
    reconciliation sweep is the last wave, so it always precedes the review it is meant to sweep.
    Name the four instances (CR-01 / 34.9-17, C2-01..08 / 34.9-22, C3-01..03 / 34.9-28, and this
    cycle). State the remedy: the sweep must be re-run AFTER the review gate and its findings
    ledgered BEFORE `/gsd-verify-work` -- which is what this plan's checkpoint enforces. Also record
    the operational hazard that `code_review_gate` writes to the FIXED path
    `{phase_dir}/{padded_phase}-REVIEW.md` and SILENTLY OVERWRITES on re-review, so this phase's
    one-file-per-cycle convention only holds because the output was moved by hand each time.

    (b) `ROADMAP.md`'s Phase 34.9 section. A gap-cycle-4 block and an abbreviated closure-protocol
    note were ALREADY written there at planning time (2026-08-13), matching this phase's convention
    that the planner writes the block and the closing plan reconciles it. Do NOT create a second
    block. Reconcile the existing one: tick each plan's checkbox with a `**DONE 2026-08-13**` marker
    and its landed evidence, matching the gap-cycle-3 entries' style, and cross-reference the fuller
    protocol section written in `deferred-items.md` by (a). Re-derive the `**Plans:**` count from the
    plan files actually on disk rather than incrementing the old number.

    Update the two citations of the renamed sweep tool: `ROADMAP.md`'s gap-cycle-3 line and any
    reference in `deferred-items.md` now read `34.9-REVIEW-SWEEP-CHECK.cjs`. Note the rename inline
    so the history stays readable rather than appearing to have always had the new name.

    Append a dated gap-cycle-4 addendum to REQ-34.9-08 in `REQUIREMENTS.md` in the same style as its
    existing addenda: C3-01 closed by the `meta/runTs.cjs` conversion and its three-direction proof,
    C3-02 and C3-03 closed by plan 34.9-30, all three ledgered. Do NOT mint a new REQ ID, and do not
    tick any box that lacks measured evidence.

    Update `STATE.md` to the post-cycle-4 truth. Re-read what the SDK writes before accepting it --
    `state.record-session` has corrupted this file's counts before, so verify `completed_plans` and
    `percent` by hand against the plan files on disk and hand-correct if wrong.

    Reaffirm, without re-litigating: REQ-34.9-02/03/04 (x64 CI leg, downloader digest verification)
    and REQ-34.9-09 (per-runner cold-spawn ratio) remain out of scope by recorded user decision, and
    truths 1-7 plus truth 8's now-closed C2-01 mechanism are not reopened.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md','utf8');const r=fs.readFileSync('.planning/ROADMAP.md','utf8');const q=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');if(!/Closure protocol/i.test(d))throw new Error('protocol not recorded in deferred-items.md');if(!/code_review_gate/.test(d))throw new Error('protocol does not name the gate ordering');if(!/34\.9-33/.test(r))throw new Error('ROADMAP missing gap cycle 4 plan list');if(/C2-SWEEP-CHECK/.test(r))throw new Error('ROADMAP still cites the old sweep tool name');if(!/C3-0/.test(q))throw new Error('REQ-34.9-08 addendum missing C3 findings');console.log('RECONCILIATION-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` contains a `## Closure protocol` section naming `code_review_gate`, the wave-then-gate ordering, all four instances, and the fixed-path silent-overwrite hazard.
    - `ROADMAP.md`'s Phase 34.9 section contains a gap-cycle-4 block listing `34.9-29` .. `34.9-33` with one-line objectives.
    - `ROADMAP.md`'s `**Plans:**` count was re-derived from files on disk and matches `ls .planning/phases/34.9-*/34.9-*-PLAN.md | wc -l`.
    - `grep -c 'C2-SWEEP-CHECK' .planning/ROADMAP.md` returns 0; the renamed tool is cited instead, with the rename noted.
    - `REQUIREMENTS.md`'s REQ-34.9-08 carries a dated gap-cycle-4 addendum naming C3-01, C3-02 and C3-03, and no new REQ ID was minted (`grep -c 'REQ-34.9-12' .planning/REQUIREMENTS.md` returns 0).
    - `STATE.md`'s `completed_plans` and `percent` were verified by hand against the plan files on disk after any SDK write.
    - The out-of-scope set (REQ-34.9-02/03/04, REQ-34.9-09) is reaffirmed, not re-litigated or re-planned.
  </acceptance_criteria>
  <done>The protocol is recorded where a future cycle will find it, and ROADMAP, REQUIREMENTS and STATE match the post-cycle-4 truth.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator carries the sweep PAST the review gate before verification</name>
  <read_first>
    - `deferred-items.md`'s new `## Closure protocol` section -- the mechanism this checkpoint exists to defeat.
    - `.planning/phases/34.9-.../34.9-REVIEW-SWEEP-CHECK.cjs` -- the tool to run, and its exit-code meanings.
  </read_first>
  <action>
    Pause for the operator and hand off the post-review sweep protocol. This checkpoint is the
    mechanism that carries the reconciliation sweep PAST `code_review_gate`, which no wave-scheduled
    task can do. Present the steps below verbatim, and hold the phase here -- `/gsd-verify-work 34.9`
    must not run until the operator reports the sweep green against this cycle's own review.
  </action>
  <what-built>
    Gap cycle 4 closed C3-01 (shared-outfile race, fixed by `meta/runTs.cjs` across all 14 exposed
    scripts and proven in three directions on hardware), C3-02 (stale pipe/argv comment plus the
    audit that wrongly said it did not exist), and C3-03 (the sweep's missing polarity check). All
    three are ledgered, and the sweep tool is now cycle-agnostic -- it discovers findings from every
    `*-REVIEW*.md` in the phase directory, including files that do not exist yet.

    But the sweep just ran as the LAST WAVE, which is BEFORE `code_review_gate`. This cycle's own
    review has not been written yet. Left alone, gap cycle 4 reproduces the exact defect it was
    created to close -- for the fifth time.
  </what-built>
  <how-to-verify>
    **DO NOT RUN `/gsd-verify-work 34.9` YET.** These steps come first.

    1. Let `/gsd-execute-phase` finish its remaining gates. `code_review_gate` will write this
       cycle's review.
    2. Find where it landed. It writes to the FIXED path
       `.planning/phases/34.9-.../34.9-REVIEW.md` and SILENTLY OVERWRITES. That path already holds
       gap cycle 1's review. If it was overwritten, restore cycle 1's from git
       (`git show HEAD:<path> > <path>`) and move the new one to `34.9-REVIEW-CYCLE4.md` -- this
       phase's convention is one file per cycle, and it has only ever held because the file was
       moved by hand.
    3. From the phase directory, run: `node 34.9-REVIEW-SWEEP-CHECK.cjs`
       It should now discover four review files and report every cycle-4 finding ID as unmapped.
       If it reports 0 unmapped while `34.9-REVIEW-CYCLE4.md` contains `### ` findings, the tool did
       not discover the new file -- that is a tool defect, not a clean result. Say so rather than
       approving.
    4. Add one disposition row per cycle-4 finding to `deferred-items.md`, in a new
       `## Code-review finding disposition — gap cycle 4 review` section matching the cycle-2 and
       cycle-3 sections' shape. FIXED / DEFERRED / RISK-ACCEPTED are all acceptable dispositions --
       what is not acceptable is silence. Remember the polarity check: a confirmation cell must not
       contain text that denies the fix, and must not quote a negative example verbatim.
    5. Re-run `node 34.9-REVIEW-SWEEP-CHECK.cjs` and confirm 0 unmapped, exit 0.
    6. Only then run `/gsd-verify-work 34.9`.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms this cycle's review file exists at `34.9-REVIEW-CYCLE4.md` and that gap cycle 1's `34.9-REVIEW.md` is intact (restored from git if it was clobbered).
    - Operator confirms `34.9-REVIEW-SWEEP-CHECK.cjs` discovered the cycle-4 review file -- i.e. it reported the new findings as unmapped BEFORE the rows were added, rather than reporting a vacuous 0.
    - A `## Code-review finding disposition — gap cycle 4 review` section exists with one row per cycle-4 finding ID.
    - Final `node 34.9-REVIEW-SWEEP-CHECK.cjs` run reports 0 unmapped and exits 0.
    - `grep -c 'C4-0' deferred-items.md` (or the ID prefix the cycle-4 review actually used) is at least equal to the number of findings in that review.
    - Operator confirms `/gsd-verify-work 34.9` has NOT been run before steps 1-5 completed.
  </acceptance_criteria>
  <resume-signal>Type "swept" once the cycle-4 review is ledgered and the sweep reports 0 unmapped, or describe what the sweep found that blocks it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code_review_gate output -> fixed REVIEW.md path | An automated writer silently overwrites a historical artifact |
| disposition table -> phase closure | A row that passes the check without evidence closes the phase falsely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.9-C4-16 | Repudiation | disposition rows | mitigate | Every Independent-confirmation cell is verified live against repository state or a re-run command at execution time, never copied from a plan SUMMARY. Enforced by the tightened check from plan 34.9-30. |
| T-34.9-C4-17 | Tampering | `34.9-REVIEW.md` fixed-path overwrite by `code_review_gate` | mitigate | Checkpoint step 2 restores gap cycle 1's review from git if clobbered and moves the new review to a per-cycle filename. The hazard is also recorded in the protocol section so it is not rediscovered. |
| T-34.9-C4-18 | Spoofing | a vacuous 0-unmapped sweep result | mitigate | Checkpoint step 3 requires observing the tool report the cycle-4 findings as UNMAPPED before any row is added; a 0 result on an undiscovered file is explicitly named as a tool defect, not a pass. |
| T-34.9-C4-19 | Tampering | loosening the gate to admit a row | mitigate | Acceptance criterion: if a row is rejected, fix the ROW not the CHECK; `git diff --stat` must show the sweep tool unmodified by this plan. |
| T-34.9-C4-SC | Tampering | npm/pip/cargo installs | accept | No package installs. This plan edits markdown only. |
</threat_model>

<verification>
- `grep -c 'C3-0' deferred-items.md` >= 3.
- `34.9-REVIEW-SWEEP-CHECK.cjs` reports 0 unmapped, exit 0, across all review files present.
- `deferred-items.md` carries the `## Closure protocol` section naming `code_review_gate` and the four instances.
- ROADMAP gap-cycle-4 block present, plan count re-derived, old sweep-tool name gone.
- REQ-34.9-08 carries a dated cycle-4 addendum; no new REQ ID minted.
- Operator checkpoint completed: cycle-4 review ledgered and sweep green BEFORE `/gsd-verify-work 34.9`.
</verification>

<success_criteria>
- The verifier's fourth missing item -- ledger entries for C3-01 through C3-03 -- is delivered.
- The review-after-sweep loop is broken in practice by the checkpoint, and recorded as a protocol so it is not re-derived.
- No existing ledger item is renumbered or rewritten.
</success_criteria>

<output>
Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-33-SUMMARY.md` when done.
State explicitly that this plan does NOT close Phase 34.9 and does not score any truth -- the phase
awaits `/gsd-verify-work 34.9`, which may only run after the checkpoint's steps 1-5 are complete.
</output>
