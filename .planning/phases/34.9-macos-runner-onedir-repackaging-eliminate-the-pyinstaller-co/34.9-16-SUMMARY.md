---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 16
subsystem: infra
tags: [live-gate, macos, packaging, gap-cycle, hardware-verified]

# Dependency graph
requires:
  - phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
    provides: "34.9-LIVE-GATE-RERUN.md, the unrun contract authored by plan 34.9-15 (verdict:
      PENDING, both Observed: fields unfilled), plus the three gap-cycle fixes it re-scores
      (34.9-12 preserveRunnerSymlinksPlugin, 34.9-13 verifyRunnerBundle framework-structure
      enforcement, 34.9-14 clean:dist-mac)"
provides:
  - "34.9-LIVE-GATE-RERUN.md filled: verdict PASS, items_passed 2/2, items_failed 0, both
    Observed: blocks, preconditions, and a full RUN RECORD -- from archived hardware evidence
    at /tmp/gamelib-gate-20260811T023738Z"
  - "REQ-34.9-08 and REQ-34.9-11 ticked in REQUIREMENTS.md, their own stated pass bars now met"
affects: [34.9-17 (post-gap-cycle reconciliation; the phase can now close on the arm64 leg)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Author/runner separation carried through to its result-writing half: plan 34.9-15
      authored the contract and ran nothing; the live run itself was performed by the
      operator+orchestrator outside this plan's own execution; this plan's sole job was
      transcribing the archived evidence into the contract's Observed: fields and RUN RECORD,
      never re-running or re-deriving any result."
    - "Score only from archived evidence, never from re-running a mutating command -- this
      plan explicitly did not invoke pnpm dist:mac, pnpm tauri:dev, or any app launch; every
      cited value traces to a specific archived file under $GATE_SESSION_DIR."

key-files:
  created: []
  modified:
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md

key-decisions:
  - "Recorded the Launch-1 capture-form deviation prominently rather than smoothing it into an
    implied three-point single-instance confirmation -- Item 1's own designated evidence sinks
    (gamelib.log + per-runner logs, not the session transcript) were unaffected and the item
    stayed fully scorable, but the gap in Section-6 tee-capture conformance is real and is
    stated as such in three places (Section 7 precondition 9, Section 8's Observed: block, and
    the RUN RECORD's Deviations subsection)."
  - "Disambiguated two payload figures that could otherwise read as a discrepancy: criterion
    7's scored comparison (83MB packaged vs 83MB source, summed over the three runner
    directories only, +0.02%) versus a separate, unscored whole-tree du -sh figure (97M
    source / 94M packaged, over the entire arm64/darwin bin root). Both are individually
    correct measurements of different things; recorded as OBS-34.9-16-02 so a future reader
    does not mistake one for contradicting the other."
  - "Ticked REQ-34.9-08 and REQ-34.9-11 because both requirements' own stated pass bar --
    'the live gate itself PASSes on a full re-run' -- is now literally met by this run's
    verdict: PASS, 2/2 scored items. Neither requirement's own text makes any claim about the
    descoped x64 leg, so neither ticks on anything beyond what this run actually covered;
    Tauri-packaged stays correctly recorded UNPROVEN, which is what REQ-34.9-11's own text
    requires, not a gap."

requirements-completed: [REQ-34.9-08, REQ-34.9-11]

# Metrics
duration: ~35min (evidence review across 12 archived files + terminal.log; Task 2's live run
  was already performed before this plan started, per its own scope fence)
completed: 2026-08-11
---

# Phase 34.9 Plan 16: Record the live-gate re-run verdict from captured hardware evidence Summary

**Filled `34.9-LIVE-GATE-RERUN.md` from the already-performed, already-archived macOS arm64 hardware run at `/tmp/gamelib-gate-20260811T023738Z`: both scored items PASS (Tauri DEV nested resolution; Electron PACKAGED on all 8 on-disk criteria), verdict PASS, carried-forward items 2/3/5 not invalidated -- then ticked REQ-34.9-08 and REQ-34.9-11 in REQUIREMENTS.md, whose own stated pass bars are now met.**

## Performance

- **Duration:** ~35 min (reading STATE.md, REQUIREMENTS.md, the contract itself, and 12 archived
  evidence files -- `terminal.log` alone is 64KB -- then writing the filled contract, the two
  REQUIREMENTS.md entries, and this Summary)
- **Completed:** 2026-08-11
- **Tasks:** 2 of the plan's 3 (Task 2, the live run itself, was already performed by the
  operator+orchestrator before this plan started, per the plan's own explicit scope fence --
  this plan's job was Task 1's recording and Task 3's write-up)
- **Files modified:** 3 (`34.9-LIVE-GATE-RERUN.md`, `REQUIREMENTS.md`, `ROADMAP.md`)

## Accomplishments

- **Section 7 (Preconditions):** all 9 rows filled from measured evidence -- `git status
  --porcelain` clean, `test:ci` 238 suites/4639 passed/1 skipped/4640 total, shasum baseline and
  hazard-B re-check byte-identical (no drift), `altNileBin` positively confirmed empty,
  `download-helper-binaries` correctly not run. Two rows (`3`, `9`) recorded as PARTIALLY
  SATISFIED / NOT SATISFIED FOR LAUNCH 1, honestly, per the Launch-1 capture-form deviation.
- **Section 8 (Scored Item 1, Tauri DEV):** PASS. All three runners resolved the nested onedir
  path in `gamelib.log`, real non-"Invalid" versions in the per-runner logs (`0.20.43` / `1.2.1`
  / `1.1.2 Will A. Zeppeli`), single instance (PID 20961) confirmed at archive time.
- **Section 9 (Scored Item 2, Electron PACKAGED):** PASS, all 8 numbered criteria, each cited
  against the artifact's own on-disk shape (never a command's stdout): `BUILD_START` recorded,
  `pnpm dist:mac` exit 0 against an already-cleared `dist/`, new dmg (198463448 bytes)/zip
  (190691642 bytes) at +117s/+120s after `BUILD_START`, exactly 12 restored symlinks, zero
  `bundle format is ambiguous` occurrences against all three framework bundles,
  `verify:runner-bundle` exit 0 with framework-structure enforcement passing, +0.02% payload
  delta (85260 KB packaged vs 85244 KB source), and the packaged app's own Settings page
  resolving all three runners under the packaged tree with zero "Invalid" occurrences.
- **Frontmatter:** `status: run`, `verdict: PASS`, `run_date: 2026-08-11`, `items_passed: 2`,
  `items_failed: 0`, `items_not_attempted: 0`, `session_dir` and `launch_ordinals_used` filled.
- **RUN RECORD appended:** per-item outcome table, a two-row Findings table (both informational
  -- the Launch-1 capture-form deviation and the payload-figure disambiguation, no code or
  packaging defects), a Deviations subsection recording the Launch-1 capture-form gap and the
  launch-ordinal numbering split honestly, a "what this run did not prove" carry-forward, and
  the pre-commit identifier audit note.
- **Section 2's conditional evaluated and stated explicitly:** Scored Item 1 showed no DEV-side
  regression, so the conditional requiring items 2/3/5 to be re-run did NOT trigger -- their
  2026-08-11 PASS verdicts stand unchanged.
- **REQUIREMENTS.md reconciled:** REQ-34.9-08 and REQ-34.9-11 both ticked, their own stated pass
  bars ("gate item 4 PASSing on a full re-run" / "the re-run's two scored items both PASSing")
  now literally met.
- **ROADMAP.md's two 34.9-16 line items** (gap-cycle list and full plan list) both ticked with
  the verdict and evidence pointer.

## Task Commits

1. **Task 1 + Task 3 combined write into `34.9-LIVE-GATE-RERUN.md`** - `d535456a6` (docs) --
   Task 1's preflight recording (Section 7) and Task 3's Observed:/frontmatter/RUN RECORD
   write both land in a single file, committed together since both operate on the same
   already-final evidence set and neither has an intermediate state worth splitting.
2. **REQUIREMENTS.md reconciliation** - `4abcc1f08` (docs) -- REQ-34.9-08/11 ticked.

Task 2 (the human-operated live run itself) was already performed and archived before this plan
started, per the plan's own explicit instruction ("The live run has ALREADY BEEN PERFORMED... Your
job is Task 1's recording and Task 3") -- there is no Task-2 commit from this plan because this
plan launched no harness, ran no build, and opened no app.

## Files Created/Modified

- `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md` (modified) -- preconditions, both Observed: blocks, frontmatter result fields, RUN RECORD appended
- `.planning/REQUIREMENTS.md` (modified) -- REQ-34.9-08 and REQ-34.9-11 ticked with their evidence citations
- `.planning/ROADMAP.md` (modified) -- 34.9-16's two line items ticked with the verdict summary

## Decisions Made

- **Recorded the Launch-1 capture-form deviation as a deviation, not a silent gap-fill.** Rather
  than inferring a clean three-point single-instance record for Launch 1 from the single
  post-archive count that does exist, this plan states plainly that no pre-launch zero-count and
  no `terminal.log` coverage exist for Launch 1, and separately argues why this does not weaken
  the item's own scorability (its designated sinks are `gamelib.log` and the per-runner logs).
- **Disambiguated the two payload figures** (per-runner-sum 83MB vs whole-tree 97M/94M) inline in
  Section 9's Observed: block and as its own Findings-table row, so a future reader comparing
  numbers across this document does not mistake one for contradicting the other.
- **Ticked both requirements on their own literal pass-bar text**, not on intention -- neither
  REQ-34.9-08 nor REQ-34.9-11's body text makes any claim this run did not cover; the descoped
  x64 leg (REQ-34.9-02/03/04/09) is untouched by either requirement's own wording, so there was
  no tension to resolve there.

## Deviations from Plan

None beyond what the plan itself anticipated and instructed. The plan's own objective explicitly
scoped this execution to Task 1 + Task 3 (Task 2 already performed by the operator+orchestrator),
so proceeding without running Task 2 is conformance with the plan, not a deviation from it. The
additional REQUIREMENTS.md reconciliation was explicitly commissioned by the orchestrating
instructions accompanying this plan's execution (`<what_to_write>`), tracked here under Rule 2
(auto-add missing critical functionality implied by the plan's own requirements-completed
frontmatter field, `[REQ-34.9-08, REQ-34.9-11]`) rather than left undone.

## Issues Encountered

- **Precondition sourcing gap.** Two preconditions (`git status --porcelain` at session start, the
  pre-Launch-1 `pgrep` zero-count) have no directly archived file capturing their own command
  output -- both were reported facts from the operator/orchestrator's session rather than tee'd
  artifacts. Recorded honestly as a sourcing gap under Section 7 preconditions 1/3/9 and the RUN
  RECORD's Deviations subsection, rather than treated as silently satisfied.

## User Setup Required

None -- this plan wrote planning documents only.

## Verification Evidence

- `grep -c 'Observed: (unfilled — plan 34.9-16)' 34.9-LIVE-GATE-RERUN.md` → `0`.
- `grep -q '^status: run'`, `grep -q '^run_date: 20'`, `grep -q '# RUN RECORD'` — all present.
- Frontmatter: `verdict: PASS`, `items_passed: 2`, `items_failed: 0`, `items_not_attempted: 0`,
  `session_dir: /tmp/gamelib-gate-20260811T023738Z` — `items_passed + items_failed +
  items_not_attempted` = `2 + 0 + 0` = `items_total` (`2`).
- `git show --stat d535456a6` — exactly `.../34.9-LIVE-GATE-RERUN.md`, 239 insertions, 18
  deletions (no other file touched by this commit).
- `git status --porcelain` after both commits — clean.
- Pre-commit identifier audit: grepped the staged `34.9-LIVE-GATE-RERUN.md` for
  `code_verifier`, `client_id`, `serial`, `access_token`, `refresh_token`, `session_id=`,
  account-id/gamekey patterns before `git add` — zero matches carrying an actual value; the only
  hits were the pre-existing (plan 34.9-15) discipline text that names the prohibited field names
  themselves, not values. The archived evidence directory (`/tmp/gamelib-gate-20260811T023738Z`,
  never committed) was independently spot-checked the same way; its one live GOG OAuth block
  (`runners-launch-{1,3}-gog.log`) was already `[REDACTED]`-marked by the gate archiver before
  this plan read it.
- No `pnpm tauri:dev`, `pnpm dist:mac`, `pnpm clean:dist-mac`, or `pnpm verify:runner-bundle`
  invocation appears anywhere in this plan's own tool-call history — only `Read`, `Bash`
  (read-only: `git status`/`add`/`commit`, `grep`, `cat`, `ls`, `wc`, `sed`, `head`, `tail`),
  `Write` (this Summary), and `Edit` (against `34.9-LIVE-GATE-RERUN.md`, `REQUIREMENTS.md`,
  `ROADMAP.md` only).
- No user-facing string added — this plan authors planning documents only. The standing i18n
  gate is N/A, as required by `<verification>`.

## Threat Flags

None. All six threats in the plan's `<threat_model>` were mitigated exactly as specified:
T-34.9G-18 (PKCE/OAuth disclosure) by the redaction discipline and pre-commit audit above (no
value transcribed); T-34.9G-19 (stdout-as-proof spoofing) by every Section 9 criterion citing the
artifact's own on-disk shape, never a command's self-report; T-34.9G-20 (split-sink repudiation)
by the recorded single-instance counts at the points that do exist and by honestly flagging the
gap where they do not (Launch 1); T-34.9G-21 (`public/bin` drift) by the shasum baseline/re-check
match; T-34.9G-22 (`download-helper-binaries` DoS) by confirming it was not run; T-34.9G-SC
(package legitimacy) is N/A -- this plan installed no packages.

## Next Phase Readiness

The blocking live gate for Phase 34.9's gap cycle 1 has now PASSED (2/2 scored items), and
REQ-34.9-08/11 are ticked. Plan 34.9-17 (the final gap-cycle plan) is the correct next step: it
records the descoped/deferred set (REQ-34.9-02/03/04/09, the x64 CI leg, the cold-ratio
measurement) and reconciles the phase-level status across REQUIREMENTS.md, ROADMAP.md, and
STATE.md so Phase 34.9 can close on the arm64 leg only, per the user's own scope fence recorded
at the FAIL-run gate.

---
*Phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co*
*Completed: 2026-08-11*

## Self-Check: PASSED

- FOUND: `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md`
- FOUND commit `d535456a6` (Task 1 + Task 3 combined write)
- FOUND commit `4abcc1f08` (REQUIREMENTS.md reconciliation)
- FOUND: `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-16-SUMMARY.md`
