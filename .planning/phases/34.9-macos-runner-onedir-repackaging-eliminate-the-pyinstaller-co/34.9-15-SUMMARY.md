---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 15
subsystem: infra
tags: [live-gate, contract-authoring, macos, packaging, gap-cycle]

# Dependency graph
requires:
  - phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
    provides: "34.9-LIVE-GATE.md's FAIL run record (item 4 FAIL, F-34.9-01..04), and the three
      gap-cycle fixes it routes to (34.9-12 preserveRunnerSymlinksPlugin, 34.9-13 verifyRunnerBundle
      framework-structure enforcement, 34.9-14 clean:dist-mac)"
provides:
  - "34.9-LIVE-GATE-RERUN.md -- an authored, UNRUN re-run contract (verdict: PENDING, run_date:
    null, both Observed: fields unfilled)"
affects: [34.9-16 (the human-operated run of this contract), 34.9-17]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Author/runner separation (decision D-E): the plan that authors a live-gate contract commits
      no result field and runs no item; a separate plan is the sole writer of Observed:/verdict."
    - "Item renumbering across a re-run: the original item 4 (the failure) is re-scored as this
      contract's own Item 1/Item 2 ordering, with the mapping stated explicitly rather than left
      implicit, to avoid confusing it with carried-forward item 2 (nile)."

key-files:
  created:
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md
  modified: []

key-decisions:
  - "Split the single authored document across two commits (Task 1 = sections 1-13; Task 2 =
    appended Structural Reachability Review/Test 5 pairwise/Authoring self-check) by staging a
    truncated intermediate copy, so each plan task still lands its own atomic commit even though
    both tasks write into the same file."
  - "Item 5's carry-forward keeps its two FAIL-run contract defects (relative-path ENOENT,
    version-string non-discriminator) as amendments for a FUTURE run of item 5, not as triggers to
    re-run it now -- its own PASS verdict stands on the absolute-path evidence actually used live."
  - "The Section-2 carry-forward argument is written as a CONDITIONAL, not an unconditional
    carry-forward: any DEV-side regression in Scored Item 1 requires items 2/3/5 to be re-run before
    the phase closes, and the launch plan (Section 6.7) states this addendum explicitly rather than
    leaving it implied."

requirements-completed: []

# Metrics
duration: ~6min (tool-call wall time; most of the session was context-loading reads)
completed: 2026-08-11
---

# Phase 34.9 Plan 15: Author the live-gate re-run contract (unrun) Summary

**Authored `34.9-LIVE-GATE-RERUN.md`, a new and unrun re-run contract for REQ-34.9-11's blocking live gate: it re-scores the FAIL run's item 4 (Electron PACKAGED) and item 1 (Tauri DEV, as a regression canary), carries items 2/3/5 forward with a written and conditional non-invalidation argument, and builds F-34.9-01/02/04 directly into its pass criteria -- all while running nothing itself, per the standing author/runner separation rule.**

## Performance

- **Duration:** ~6 min of tool-call wall time (the bulk of the session was reading required context: the FAIL run record, the three gap-cycle Summaries, the contract-authoring reference, PACKAGING-LIMITATIONS.md, PROJECT.md, STATE.md, and current `AdvancedSettings/index.tsx` source)
- **Completed:** 2026-08-11T02:13:01Z
- **Tasks:** 2
- **Files created:** 1

## Accomplishments

- `34.9-LIVE-GATE-RERUN.md` created as a NEW file (`34.9-LIVE-GATE.md` untouched, `git status --porcelain` confirmed empty for it before and after both commits).
- Frontmatter: `verdict: PENDING`, `run_date: null`, `items_total: 2`, `items_carried_forward: 3`, notes stating the author/runner separation explicitly.
- Thirteen numbered body sections present (verified by anchored `## N.` heading grep): scope/what-changed, item-disposition table with carry-forward reasoning, sink partition, redaction discipline, a dedicated absolute-path rule (citing Deviation 4's ENOENT), the full dual-sink evidence-capture instruction adapted to this contract's two-launch shape, preconditions (9, each labelled and left blank for the operator), Scored Item 1 (Tauri DEV canary), Scored Item 2 (was item 4, Electron PACKAGED -- the failure, now with 8 numbered on-disk-shape criteria), carried-forward items 2/3/5 with their FAIL-run verdicts and two item-5 contract amendments, the unscored Tauri-PACKAGED item, the verdict rule, and what this gate does not prove.
- Appended a full Structural Reachability Review: a seven-test table covering both scored items, three representative preconditions, and the evidence-capture instruction as a whole; dedicated Test 3+4 detail on Scored Item 2's `bundle format is ambiguous` absence assertion (proven observable against the FAIL run's own transcript line, not vacuous); Test 4 on the `12`-vs-`0` symlink count; Test 6 on `altNileBin`'s positive re-check; Test 7 re-traced against CURRENT source (`AdvancedSettings/index.tsx:180-184`, read live at this authoring, not cited from the FAIL run).
- Test 5 pairwise pass: reduction stated (5 state-mutating x 9 evidence-bearing requirements), 11 pairs considered, 2 flagged and resolved in-document (the `clean:dist-mac` silent-no-op hazard and hazard B, `public/`->`build/` drift between the two launches).
- Authoring self-check: commands not run, no result field written, exactly one file touched per commit.
- Every automated acceptance-criteria grep from the plan re-run and confirmed passing (see Self-Check below).

## Task Commits

Each task was committed atomically:

1. **Task 1: Author 34.9-LIVE-GATE-RERUN.md (sections 1-13)** - `ea453c061` (docs)
2. **Task 2: Structural Reachability Review + Test 5 pairwise + Authoring self-check** - `8da076cd2` (docs)

Both tasks write into the same new file; each commit was staged from a distinct intermediate
content state (Task 1's commit contains only the first 547 lines; Task 2's commit adds the
remaining 118) so each lands as its own atomic, individually-diffable commit rather than one
combined commit, per the plan's own two-task structure.

## Files Created/Modified

- `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md` (new, 665 lines) -- the re-run contract, unrun

## Decisions Made

- **Two-commit split of one authored document**, described above under key-decisions -- necessary because the plan's own task boundaries land inside a single file's evolution (Task 2 appends to what Task 1 wrote), and the executor's task-commit protocol requires one commit per task.
- **Item-5 amendments recorded as forward-looking, not as re-run triggers** -- the FAIL run's item 5 PASSed on the corrected absolute-path evidence actually used live; only a HYPOTHETICAL future contract repeating the relative-path mistake would need the amendment, so it is filed as guidance for any future item-5 run rather than as a reason to re-run item 5 now.
- **The carry-forward argument is explicitly conditional**, not a blanket pass-through -- Section 2 and Section 6.7's launch-plan addendum both state that a DEV-side regression in Scored Item 1 requires items 2/3/5 to be re-run before the phase closes, so plan 34.9-16's own run record must state whether that conditional triggered.

## Deviations from Plan

None - plan executed exactly as written. The only structural choice made beyond the plan's literal text was the two-commit split described above, which implements the plan's own "Task 1" / "Task 2" boundary as two atomic commits rather than choosing (uninstructed) between one combined commit or two commits with duplicated content -- this is execution-protocol conformance, not a deviation from the plan's substance.

## Issues Encountered

- **`Observed: (unfilled — plan 34.9-16)` literal count discipline.** The plan's acceptance criteria require this exact literal to appear EXACTLY twice (once per scored item), never a third time even as a description of itself. A first draft's Authoring self-check paragraph quoted the literal a third time while describing it, which would have broken the `grep -c` count. Caught before committing by running the plan's own automated verify commands against the draft; reworded the self-check paragraph to describe the markers without repeating their exact bracketed text, then re-verified the count returned exactly 2.

## User Setup Required

None -- no external service configuration required. This plan authors a document only.

## Verification Evidence

- `test -f .planning/phases/34.9-.../34.9-LIVE-GATE-RERUN.md` -- succeeds.
- `git status --porcelain .planning/phases/34.9-.../34.9-LIVE-GATE.md` -- empty, both before and after this plan's commits.
- `grep -c '^verdict: PENDING' <file>` -- `1`.
- `grep -c '^run_date: null' <file>` -- `1`.
- `grep -c 'Observed: (unfilled — plan 34.9-16)' <file>` -- `2` (Sections 8 and 9, the two scored items).
- `grep -c '^\*\*Observed: PASS\|^\*\*Observed: FAIL' <file>` -- `0`.
- All thirteen numbered `## N.` section headings present and anchored-grep-verified, listed in Accomplishments above with their line numbers as of the final commit (51, 88, 141, 168, 181, 204, 312, 353, 391, 461, 507, 519, 527).
- `grep -c 'equals exactly 12'` -- `1` (Scored Item 2 criterion 4, the symlink count).
- `grep -c 'bundle format is ambiguous'` -- `4` (the FAIL-run precedent, the criterion-5 asserted-absence, and the Test-3/4 detail section's two citations of the same literal).
- `grep -c '1\.1\.2 Will A\. Zeppeli'` -- `2` (F-34.9-04's own text plus item 5's amendment section).
- `grep -c 'mtime'` -- `5` (Section 9's newness criterion plus its supporting references).
- `grep -in 'code_verifier\|client_id\|serial'` -- 3 lines, all inside the redaction-discipline paragraph and its immediate elaboration (Section 4), listing the PROHIBITED field names as part of stating what must never be transcribed with a real value -- no actual secret value appears anywhere in the document. Quoted below to show they are prohibitions, not values:
  - "No cookie value, gamekey, OAuth code, account id, or PKCE material (`code_verifier`, `serial`, `client_id`) is ever transcribed into this document or any captured artifact"
  - "`client_id`) is ever transcribed into this document or any captured artifact -- timestamps,"
  - "Nile)` writes the FULL `NileLoginData` object (url, `code_verifier`, `serial`, `client_id`) to"
- `grep -q "Structural Reachability Review"`, `grep -q "Test 5"`, `grep -q "Authoring self-check"` -- all present.
- `git show --stat --format="" ea453c061` -- exactly `.../34.9-LIVE-GATE-RERUN.md`, 547 insertions.
- `git show --stat --format="" 8da076cd2` -- exactly the same single file, 118 insertions.
- `git diff --diff-filter=D --name-only HEAD~1 HEAD` -- empty (no deletions in either commit).
- `ls -d /tmp/gamelib-gate-*` -- three PRE-EXISTING directories (20260806T072632Z, 20260806T112126Z, 20260810T233351Z, the last being the FAIL run's own session), none dated to this plan's execution window (2026-08-11T02:06-02:13Z) -- confirms no new session directory was created by this plan.
- No `pnpm tauri:dev`, `pnpm dist:mac`, `pnpm clean:dist-mac`, or `pnpm verify:runner-bundle` invocation appears in this plan's own tool-call history -- only `Read`, `Bash` (read-only: `gsd-sdk query`, `git status`/`log`/`diff`/`show`, `graphify query`, `grep`, `date`, `cp`, `wc`, `ls`), `Write` (this document, once), and `Edit` (one wording fix to avoid the literal-count collision above).
- No user-facing string added -- this plan authors a planning document only, never app source. The standing i18n gate is N/A, as required by `<verification>`.

## Threat Flags

None. All five threats in the plan's `<threat_model>` (T-34.9G-13 author/runner repudiation, T-34.9G-14 electron-builder-stdout spoofing, T-34.9G-15 PKCE information disclosure, T-34.9G-16 FAIL-record tampering, T-34.9G-17 carried-forward-item denial-of-service) were mitigated exactly as specified: the two-commit-per-task structure and unfilled `Observed:` markers address T-34.9G-13; every Scored Item 2 criterion asserts on-disk shape over command self-report for T-34.9G-14; the redaction-discipline paragraph and its self-auditing quote-back (see Verification Evidence above) address T-34.9G-15; the empty `git status --porcelain` check on `34.9-LIVE-GATE.md` addresses T-34.9G-16; and the explicit, binding conditional in Section 2/6.7 addresses T-34.9G-17. T-34.9G-SC (package-legitimacy) is N/A -- this plan installed no packages.

## Next Phase Readiness

The re-run contract is ready for plan 34.9-16, the human-operated run. It cannot be executed by an automated agent -- it requires macOS arm64 hardware, a real `pnpm dist:mac` build, and an operator driving the packaged `.app`'s UI. Plan 34.9-16 must fill every `Observed:` field, this document's `verdict`/`run_date`/`items_passed`/`items_failed`, and must explicitly record whether Scored Item 1 triggered the Section-2/6.7 conditional requiring items 2/3/5 to be re-run. Plan 34.9-17 (per the phase's `incomplete_plans` list) follows after the run's own verdict is known.

---
*Phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co*
*Completed: 2026-08-11*

## Self-Check: PASSED

- FOUND: `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md`
- FOUND: `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-15-SUMMARY.md`
- FOUND commit `ea453c061` (Task 1)
- FOUND commit `8da076cd2` (Task 2)
