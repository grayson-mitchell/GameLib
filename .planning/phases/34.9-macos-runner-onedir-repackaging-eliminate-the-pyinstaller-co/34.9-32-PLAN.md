---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 32
type: execute
wave: 3
depends_on: ["34.9-31"]
files_modified:
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-WRAPPER-PROOF.md
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
autonomous: false
gap_closure: true
gap_cycle: 4
requirements: [REQ-34.9-08]
closes_findings: [C3-01]
implements_decisions: [D-C4-01]

must_haves:
  truths:
    - "The wrapper conversion is proven on real macOS arm64 hardware, not asserted from code reading."
    - "A compile failure still exits non-zero and still never runs node, for every converted script, in both failure shapes."
    - "Two concurrent invocations of the same script no longer cross-contaminate -- measured, with a positive control proving the harness can still detect the defect."
    - "The proof states what it does not cover."
  artifacts:
    - path: ".planning/phases/34.9-.../34.9-WRAPPER-PROOF.md"
      provides: "Filled proof with verdict and per-direction outcome table"
      contains: "RUN RECORD"
  key_links:
    - from: "34.9-WRAPPER-PROOF.md verdict"
      to: "C3-01 disposition in deferred-items.md"
      via: "plan 34.9-33's ledger row"
      pattern: "C3-01"
---

<objective>
Run `34.9-WRAPPER-PROOF.md` on real macOS arm64 hardware and write its results. This plan is the
SOLE writer of every `Observed:` field, every frontmatter result field, and the verdict.

**This plan may not edit the contract's claims, PASS bars, direction list, or exclusion reasons.**
If a direction turns out to be unrunnable or mis-specified, record that as a DEVIATION with its
reason -- do not quietly rewrite the bar it failed. A runner that can edit the contract is not
independent of it, and the whole author/runner separation collapses.

Purpose: evidence, on hardware, that a change to how every build and guard script in the repo is
invoked did not regress the property gap cycle 3 bought, and did close the race it was made for.
Output: a filled proof document with a verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-WRAPPER-PROOF.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PIPE-PROOF.md
@meta/runTs.cjs
@package.json

<runner_hazards priority="read-this-first">
Four failure modes this phase has already paid for. Each one produced a finding on a PRIOR proof
run of this exact kind.

1. `deferred-items.md` item 15 -- an exit-capture idiom that silently no-ops under zsh, and
   `cat -A` being BSD-incompatible. Run the contract's Section 3 capture-form control FIRST and
   record its output before trusting any Direction A result.
2. `deferred-items.md` item 14 -- a `find -newer` emptiness check that was vacuous. Any check that
   cannot go red is not evidence; if a direction's check cannot be shown to distinguish pass from
   fail, mark that direction INVALID, not PASS.
3. `deferred-items.md` item 16 -- a CLI-argument-passthrough sub-claim that was never actually
   exercised. Direction B's argv rows must be RUN, not reasoned about.
4. A mutating command's own report is never proof of its own effect. After every restore, verify
   with `shasum -a 256` against the pre-injection digest, and record both digests.

Also: never use a tee'd transcript to prove a backend path did not run, and if any invocation hangs,
sample the process before force-killing it.
</runner_hazards>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the capture-form control, Direction A, and Direction B</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-WRAPPER-PROOF.md</files>
  <read_first>
    - `.planning/phases/34.9-.../34.9-WRAPPER-PROOF.md` IN FULL, before running anything -- Sections 2 (preconditions), 3 (capture-form control), 4 (Direction A), 5 (Direction B and its exclusion reasons), and 7 (the injection and restore recipe, which was written before any mutation existed and must be followed as written).
    - `.planning/phases/34.9-.../34.9-PIPE-PROOF.md`'s `# RUN RECORD` section -- the established shape for recording per-direction outcomes, deviations, and an interrupted session honestly.
    - `meta/runTs.cjs` -- so an unexpected result can be attributed to the wrapper rather than guessed at.
  </read_first>
  <action>
    Confirm Section 2's preconditions and record them: real macOS arm64 hardware, the repo's own
    installed esbuild resolved from `node_modules` (never a global or downloaded binary), a clean
    `git status --porcelain meta/ package.json`, and a created session directory whose absolute path
    is recorded in the frontmatter `session_dir` field.

    Run Section 3's capture-form control first and record its observed output. If the capture idiom
    does not demonstrably capture a non-zero exit under the operator's real shell, STOP and record
    a blocking deviation -- every Direction A result would otherwise be unreadable.

    Run Direction A for every converted script, in both broken shapes (S1 syntax error, S2
    unresolvable import), following Section 7's injection recipe exactly. For each row record the
    observed exit code, whether the script body's own output marker appeared, whether any false
    `Cannot find module` diagnostic appeared, and whether the temp directory was absent afterward.
    Restore each mutated file per Section 7 and record the `shasum -a 256` digest before injection
    and after restore for every file touched.

    Run Direction B for every script Section 5 selects as safely runnable, honoring its exclusion
    reasons exactly -- do not promote an excluded script into the run, and do not silently skip an
    included one. Explicitly exercise the argv rows by running them for real:
    `pnpm verify:runner-bundle build --arch=arm64` and the `--arch` forwarding path, plus the
    `export`-prefixed `lint-translations:gamelib` and the `pnpm build:sidecar &&`-chained
    `build:sidecar-sea` to whatever depth their exclusion reasons permit. Record observed evidence
    that the arguments and env prefix actually arrived, not that they should have.

    Fill only `Observed:` fields and the per-direction outcome rows in this task. Do not write the
    verdict -- Task 2 owns it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-WRAPPER-PROOF.md','utf8');const empty=(t.match(/Observed:\s*$/gm)||[]).length;const dirA=/Direction A/.test(t);if(!dirA)throw new Error('contract missing');if(empty>0)console.log('WARNING: '+empty+' Observed fields still empty -- expected only Direction C rows at this task');if(!/session_dir:\s*\S/.test(t))throw new Error('session_dir not recorded');console.log('DIRECTION-AB-RECORDED, remaining empty Observed: '+empty)"</automated>
  </verify>
  <acceptance_criteria>
    - `session_dir` in frontmatter is a recorded absolute path that exists on disk.
    - Section 3's capture-form control has a recorded observed output demonstrating a non-zero exit is actually captured under the operator's shell.
    - Every Direction A row has a filled `Observed:` field -- zero rows left blank, zero rows marked from inference rather than execution.
    - For every Direction A row: exit code recorded as non-zero, and the script body's own output marker recorded as ABSENT.
    - Every file mutated by an injection has a recorded pre-injection and post-restore `shasum -a 256`, and the two match.
    - `git status --porcelain meta/ package.json` is clean at the end of the task.
    - Every Direction B row is either filled with an observed result or carries the contract's own named exclusion reason -- no row is silently absent.
    - The two argv rows record observed evidence the arguments arrived (actual output, not reasoning).
    - No PASS bar, claim, direction, or exclusion reason in the contract was edited -- `git diff` on the contract shows changes confined to `Observed:` fields, outcome rows, and `session_dir`.
  </acceptance_criteria>
  <done>Directions A and B are executed and recorded on real hardware, with restores verified by digest.</done>
</task>

<task type="auto">
  <name>Task 2: Run Direction C and its positive control, then write the verdict</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-WRAPPER-PROOF.md, .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md</files>
  <read_first>
    - `.planning/phases/34.9-.../34.9-WRAPPER-PROOF.md` Section 6 (Direction C and its positive control) and Section 8 (what this does not prove).
    - `.planning/phases/34.9-.../34.9-REVIEW-CYCLE3.md` finding C3-01's harness description and its 60/60 result -- the control must reproduce approximately this.
    - `.planning/phases/34.9-.../34.9-VERIFICATION.md` gaps[0].reason -- the verifier's independent 20/20 reproduction, the second prior measurement.
    - `.planning/phases/34.9-.../deferred-items.md` -- the existing item numbering (items 1-19) so any new finding appends at item 20.
  </read_first>
  <action>
    Run Direction C's POSITIVE CONTROL FIRST, before the new-idiom trials. Point the harness at the
    OLD `--outfile=node_modules/.cache/<name>.cjs ... && node <same path>` idiom, inside the session
    directory, never by reverting `package.json`. Record the observed cross-contamination count and
    error count. The prior measurements are reviewer 60/60 and verifier 20/20, both 100%.

    If the control does NOT reproduce cross-contamination, Direction C is scored INVALID, not PASS,
    and the overall verdict cannot be PASS on Direction C's strength. A harness that cannot detect
    the defect cannot prove its absence -- record this outcome plainly rather than reporting the new
    idiom's clean result as a win.

    Only after the control reproduces, run the new-idiom trials: two concurrent invocations of the
    same script name with deliberately different source content, at the contract's stated trial
    count. Record cross-contamination count and error count. PASS bar is 0 cross-contamination.

    Then write the `# RUN RECORD` section mirroring `34.9-PIPE-PROOF.md`'s: the per-direction
    outcome table, the independently recomputed restore audit (recompute it in this task; do not
    cite Task 1's prose for it), the findings table, the deviations section recording any
    interruption or departure from the written procedure honestly, and a "what this run does NOT
    prove" section carrying forward Section 8's limits. Fill the frontmatter result fields:
    `verdict`, `run_date`, `directions_passed`, `directions_failed`, `runner`, `findings`,
    `status: run`.

    If the run opens any new finding, append it to `deferred-items.md` as item 20 (continuing the
    existing numbering) with the structured shape the existing items use -- `OWNER:`, a named
    precondition, and a blocker statement -- so the generalised sweep can resolve it. Do NOT write a
    C3-01/02/03 disposition row here; plan 34.9-33 owns the disposition table.

    Before committing, run a pre-commit identifier audit as `34.9-PIPE-PROOF.md` does: no Steam
    account identifier, no absolute home path beyond the recorded session directory, no credential.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-WRAPPER-PROOF.md','utf8');if(!/verdict:\s*(PASS|FAIL|PARTIAL)/i.test(t))throw new Error('no verdict written');if(!/RUN RECORD/.test(t))throw new Error('no RUN RECORD section');if(!/run_date:\s*\S/.test(t))throw new Error('run_date empty');const left=(t.match(/Observed:\s*$/gm)||[]).length;if(left>0)throw new Error(left+' Observed fields still empty');if(!/control/i.test(t))throw new Error('positive control not recorded');console.log('PROOF-RUN-COMPLETE')"</automated>
  </verify>
  <acceptance_criteria>
    - The positive control's observed cross-contamination count is recorded and is greater than zero, at approximately the previously measured 100% rate -- OR Direction C is explicitly scored INVALID with that stated as the reason.
    - The control was run BEFORE the new-idiom trials, and the RUN RECORD says so.
    - The new-idiom trials record 0 cross-contamination over the contract's stated trial count.
    - Zero `Observed:` fields remain empty across the whole document.
    - Frontmatter carries `verdict`, `run_date`, `directions_passed`, `directions_failed`, `runner`, `session_dir`, `status: run`, and `directions_passed + directions_failed` equals `directions_total`.
    - The restore audit in the RUN RECORD was recomputed in this task, and the document says so explicitly rather than citing Task 1.
    - `git status --porcelain meta/ package.json` is clean.
    - Any new finding is appended to `deferred-items.md` as item 20+ with `OWNER:`, a named precondition, and a blocker statement.
    - No C3-01/C3-02/C3-03 disposition row was added by this plan.
    - Pre-commit identifier audit recorded, with no account identifier or credential in the committed text.
  </acceptance_criteria>
  <done>Direction C is measured with a control that proves the harness works, and the proof carries a verdict written only from observation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirms the proof ran on real hardware</name>
  <read_first>
    - `.planning/phases/34.9-.../34.9-WRAPPER-PROOF.md`'s `# RUN RECORD` section and frontmatter verdict.
  </read_first>
  <action>
    Pause for the operator. Present the recorded verdict, the Direction C positive-control
    count and the new-idiom cross-contamination count from `34.9-WRAPPER-PROOF.md`, then walk the
    verification steps below. Do not proceed past this task, and do not treat a clean Direction C
    result as a pass, until the operator has confirmed the positive control actually reproduced the
    defect against the old idiom.
  </action>
  <what-built>
    Plan 34.9-29 rerouted all 14 exposed `package.json` scripts through `meta/runTs.cjs`, which
    compiles into a private per-invocation temp directory. This plan ran the three-direction proof of
    that change on this machine: Direction A (compile failure still exits non-zero and never runs
    node, every script, both failure shapes), Direction B (healthy scripts still work and still
    receive their arguments and env), and Direction C (two concurrent invocations of the same script
    no longer cross-contaminate) -- Direction C gated behind a positive control proving the harness
    can still detect the defect against the old idiom.
  </what-built>
  <how-to-verify>
    1. Open `34.9-WRAPPER-PROOF.md` and read the frontmatter `verdict`, `directions_passed`,
       `directions_failed`, and `session_dir`.
    2. Confirm `session_dir` points at a directory on THIS machine and that the run happened on
       macOS arm64 -- not in a container, not on a remote host.
    3. In the `# RUN RECORD`, confirm the Direction C positive control reproduced
       cross-contamination against the OLD idiom (a non-zero count). If it reports zero, the harness
       measured nothing and Direction C must read INVALID rather than PASS -- say so rather than
       approving.
    4. Confirm the new-idiom Direction C trials report 0 cross-contamination.
    5. Spot-check any two Direction A rows: exit code non-zero, and the script's own output marker
       recorded ABSENT.
    6. Run `git status --porcelain meta/ package.json` and confirm it prints nothing -- every
       injected mutation was restored.
    7. Confirm the "what this run does NOT prove" section still names x64, CI, and notarization.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms `session_dir` exists on this machine and the hardware is macOS arm64.
    - Operator confirms the positive control reproduced the defect (non-zero cross-contamination against the old idiom).
    - Operator confirms the new-idiom trials report 0 cross-contamination.
    - Operator confirms `git status --porcelain meta/ package.json` prints nothing.
    - Operator either approves the recorded verdict or names the specific direction they dispute.
  </acceptance_criteria>
  <resume-signal>Type "approved" to accept the recorded verdict, or name the direction you dispute and why.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| injected broken entry files -> repo working tree | Deliberate source mutation crosses into a tracked tree |
| harness temp state -> repo build outputs | Concurrency trials write compiled code near real build paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.9-C4-12 | Tampering | injected broken `meta/*.ts` entries | mitigate | Section 7's restore recipe, with `shasum -a 256` recorded before injection and after restore for every file, plus `git status --porcelain meta/ package.json` asserted clean at the end of each task. A mutating command's own report is never accepted as proof of restore. |
| T-34.9-C4-13 | Denial of Service | Direction C's concurrent trials | mitigate | Trials run inside the recorded session directory. The positive control must not leave a `node_modules/.cache` artifact a later direction then reads -- covered by the contract's pairwise review and re-checked here. |
| T-34.9-C4-14 | Information Disclosure | committed proof text | mitigate | Pre-commit identifier audit before commit -- no account identifier, no credential, no absolute home path beyond the recorded session directory. This phase has a recorded prior incident of an unredacted Steam account id reaching committed files. |
| T-34.9-C4-15 | Repudiation | runner editing the contract it is graded by | mitigate | Acceptance criterion pins `git diff` on the contract to `Observed:` fields, outcome rows, and result frontmatter only; any mis-specification is recorded as a DEVIATION rather than fixed in place. |
| T-34.9-C4-SC | Tampering | npm/pip/cargo installs | accept | No package installs. All tooling is the repo's already-installed esbuild, resolved from `node_modules`. |
</threat_model>

<verification>
- `34.9-WRAPPER-PROOF.md` has a verdict, a `run_date`, a real `session_dir`, and zero empty `Observed:` fields.
- Direction C's positive control reproduced the defect against the old idiom before the new-idiom trials ran.
- New-idiom Direction C: 0 cross-contamination.
- `git status --porcelain meta/ package.json` clean.
- Operator checkpoint approved.
</verification>

<success_criteria>
- The wrapper conversion is proven on hardware in all three directions, with a control that makes Direction C falsifiable.
- Every injected mutation is restored and digest-verified.
- The proof's limits are stated rather than implied.
</success_criteria>

<output>
Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-32-SUMMARY.md` when done.
Record the verdict, the control's observed count, the new-idiom count, and any deviation honestly.
</output>
