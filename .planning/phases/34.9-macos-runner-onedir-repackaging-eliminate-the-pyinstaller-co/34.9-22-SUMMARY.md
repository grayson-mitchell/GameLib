---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 22
subsystem: infra
tags: [reconciliation, gap-cycle, deferred-items, code-review, roadmap, requirements, state]

# Dependency graph
requires:
  - phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co (plan 18)
    provides: "closeBundle throw + isContainedSymlinkTarget (CR-01/WR-01 closed)"
  - phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co (plan 19)
    provides: "summarise() stub-absent branch (WR-02); IN-01/IN-02 doc-comment corrections"
  - phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co (plan 20)
    provides: "verify:runner-bundle wired into dist:mac/release:mac; 34.9-GUARD-PROOF.md authored"
  - phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co (plan 21)
    provides: "34.9-GUARD-PROOF.md RUN RECORD, verdict PASS, deferred-items.md items 14-16"
provides:
  - "deferred-items.md 'Code-review finding disposition' table: all six 34.9-REVIEW.md findings
    mapped by set-difference to a landed plan ID or a ledger item, unmapped count zero"
  - "deferred-items.md items 11 (IN-03, macOS-only stale-artifact fix), 12 (guard is arm64-only,
    UNPROVEN for x64) and 13 (guard never exercised in CI, UNPROVEN)"
  - "Corrected, scope-carrying tripwire prose in 34.9-LIVE-GATE-RERUN.md, ROADMAP.md and
    REQUIREMENTS.md -- every claim states arm64-only and not-in-CI in the same passage, dated
    [Corrected 2026-08-12 -- plan 34.9-22] markers in the run record"
  - "ROADMAP.md: one 34.9-17 row (duplicate removed), a Gap cycle 2 block with checked rows
    34.9-18..22, /gsd-verify-work 34.9 named as next step, phase explicitly not closed"
  - "STATE.md: Phase 34.9 paragraph updated to record 34.9-22's completion and gaps_found status;
    frontmatter hand-corrected (total_plans 297->298, completed_plans 283->284)"
affects: [any-future-gsd-verify-work-34.9-run, secure-phase-34.9]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Finding-ID-granularity sweep: enumerate list A (grep the review doc's own headings), list B
      (each landed plan's own closing claims), and report the set-difference explicitly with a
      zero-unmapped count -- the unit the prior reconciliation (34.9-17) got wrong by sweeping at
      the coarser 'descoped requirement' granularity instead"
    - "Scope-carrying correction markers: a dated [Corrected DATE -- plan ID] literal placed
      immediately after each rewritten claim, so a signed-off run record shows dated correction
      rather than a silent rewrite of history"

key-files:
  created: []
  modified:
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md

key-decisions:
  - "closes_findings frontmatter field does not literally exist on any of the four gap-cycle-2
    Summaries (18-21) -- worked around by deriving list B from each Summary's own explicit
    closing-language body text instead, documented as a plan/reality mismatch rather than silently
    substituted"
  - "Branch selected from 34.9-GUARD-PROOF.md's frontmatter verdict alone: PASS -> Branch A
    (scoped-but-real tripwire claim, citing the live run). Verified no other source (a Summary's
    claim, a green test run, this plan's own expectations) was used as the selector"
  - "ROADMAP.md's two 34.9-17 rows had both drifted to [x] (not one checked/one unchecked as the
    plan anticipated) -- kept the top narrative block's row per the plan's stated intent, deleted
    the bottom canonical wave-list's duplicate"
  - "STATE.md's percent/completed_phases/total_phases left unchanged (74/17/23) -- percent tracks
    PHASES not plans, per this file's own documented gsd-sdk-corruption history, and Phase 34.9
    does not complete in this plan"

# Metrics
metrics:
  duration: "~1 session, documentation-only plan (no build/test run required)"
  files_changed: 5
  commits: 3
---

# Phase 34.9 Plan 22: Close the Six-Finding Sweep and Correct the Tripwire Prose Summary

**Swept all six `34.9-REVIEW.md` findings by explicit set-difference (zero unmapped, closing the
exact class of miss 34.9-17 committed), corrected every "automated tripwire ... cannot go silent
again" claim across three documents to state its own arm64-only/not-in-CI scope, and reconciled
ROADMAP.md/STATE.md to name `/gsd-verify-work 34.9` as the next step -- the phase stays OPEN.**

## What Was Built

### Task 1 -- Six-finding sweep, deferred-items.md items 11-13

**List A** (from `grep -n '^### ' 34.9-REVIEW.md`, raw output reproduced in the ledger itself):

```
58:### CR-01: `closeBundle` doesn't fail the build on a skipped symlink restore...
131:### WR-01: `restoreSymlinks` validates the destination path but never the symlink *target*
161:### WR-02: Missing top-level framework stub is never flagged...
188:### IN-01: `MAC_DIR_PATTERN` branch requires `isDirectory()`...
205:### IN-02: `cleanDistMac`'s containment throw is untested and structurally unreachable...
224:### IN-03: The F-34.9-02 fix is macOS-only...
```

= `{CR-01, WR-01, WR-02, IN-01, IN-02, IN-03}`, 6 IDs.

**List B**: none of `34.9-18-SUMMARY.md`..`34.9-21-SUMMARY.md` carries a frontmatter field
literally named `closes_findings:` (a mismatch between this plan's own `<interfaces>` expectation
and what actually landed -- recorded rather than silently worked around). Derived instead from each
Summary's own explicit closing statement, cross-checked for disagreement against each self-check
section:

- `34.9-18-SUMMARY.md:45` -- "closing CR-01 and WR-01"
- `34.9-19-SUMMARY.md:60,62-63` -- WR-02 closed, IN-01/IN-02 doc comments corrected; `:148`
  explicitly states IN-03 "remains deferred, owned by plan 34.9-22's ledger entry" (i.e. 34.9-19
  itself records IN-03 as NOT closed by it)
- `34.9-20-SUMMARY.md:61` -- CR-01's wiring landed, not yet observed firing
- `34.9-21-SUMMARY.md:62` -- "CR-01 ... is closed and `34.9-VERIFICATION.md` truth 8 is satisfied"

= `{CR-01 -> 34.9-21, WR-01 -> 34.9-18, WR-02 -> 34.9-19, IN-01 -> 34.9-19, IN-02 -> 34.9-19}`, 5 IDs.

**A minus B = `{IN-03}`** -- exactly the one ID 34.9-19's own Summary already flagged as deferred to
this plan. Count line in `deferred-items.md`: 6 in list A, 5 mapped to a landed fix, 1 mapped to a
ledger item, **unmapped: 0**.

Added `deferred-items.md` items 11 (IN-03 -- macOS-only stale-artifact fix, `dist:win`/`dist:linux`
unconfirmed), 12 (guard covers arm64 only, x64 coverage stated UNPROVEN, not asserted either way),
13 (guard never exercised in CI, UNPROVEN, `install-deps` blocked upstream by
`PENDING-CI-PUBLISH` sentinels). Items 1-10 untouched (diff is additions only, confirmed by
`git diff -U0` showing zero `^-` lines).

### Task 2 -- Corrected the overclaiming tripwire prose

Read `34.9-GUARD-PROOF.md`'s frontmatter verdict first: **`verdict: PASS`** -- Branch A selected,
and only this value (never a Summary's claim, a green test run, or this plan's own expectations).

- `34.9-LIVE-GATE-RERUN.md`: corrected the two passages (Section 1's 34.9-13 bullet, Scored Item 2
  criterion 6) that claimed "a genuine tripwire" / "the automated tripwire ... now fires ... if it
  regresses" without scope. Both now state the guard's failing direction was observed live
  (`34.9-GUARD-PROOF.md`, run 2026-08-12) and that coverage is **arm64 only** and **not in CI** in
  the same passage. Both carry the literal marker `[Corrected 2026-08-12 -- plan 34.9-22]` (fixed a
  line-wrap that initially broke the marker across two lines -- confirmed unwrapped before commit).
  No `**Observed:` line touched (`git diff` shows zero).
- `ROADMAP.md`'s Phase 34.9 status note: replaced "Phase status 2026-08-11: CLOSES on the arm64 leg
  only" with a dated 2026-08-12 status stating the phase does **not** close yet, naming
  `34.9-VERIFICATION.md`'s `gaps_found` status and `/gsd-verify-work 34.9` as the next step.
  Warm-ratio table and the retired `~95x` caveat left byte-identical (confirmed by diff).
- `REQUIREMENTS.md`: REQ-34.9-08's status-table row (line 372) and checkbox entry (line 834) both
  gain a 2026-08-12 addendum naming `deferred-items.md` items 11-13 and stating the guard's scope in
  the same sentence. REQ-34.9-08's ticked state is unchanged -- this plan corrects prose, not
  verification status; that is the verifier's job. The Phase 34.9 block preamble (line 825) gained a
  correction: the accurate current non-closure cause is truth 8 (`gaps_found`), not the original
  gate's since-superseded FAIL verdict.

**Verify-script limitation, disclosed rather than worked around:** the plan's own automated check
scans the literal substring `tripwire` across the full text of all three files and requires
`arm64`/`not in CI` within a 400-character window at every occurrence. Running it as written flags
4 pre-existing, unrelated occurrences that predate this plan and are outside its scope:
`ROADMAP.md`'s Phase 34.2 rows (`34.2-10`/`34.2-18`/`34.2-23`, "containment tripwire" in test
infrastructure) and `REQUIREMENTS.md`'s `REQ-34.4-10` ("growth-tripwire failed"). Confirmed by
direct `grep -n 'tripwire'` inspection that these four are the ONLY remaining failures and none was
introduced or altered by this plan's commits. Per the deviation rules' SCOPE BOUNDARY ("only
auto-fix issues directly caused by the current task's changes"), these were left untouched rather
than edited to force the global check to pass -- editing unrelated Phase 34.2/34.4 ledger rows to
satisfy an unscoped regex would itself be an out-of-scope mutation. Every Phase-34.9 `tripwire`
occurrence this plan touches or introduces DOES carry `arm64` and `not in CI` in its own passage
(confirmed by isolating the check to just the edited passages).

### Task 3 -- ROADMAP structure and STATE.md

`ROADMAP.md`: both `34.9-17-PLAN.md` rows had drifted to `[x]` by execution time (not one checked
and one unchecked, as the plan's read_first anticipated -- content had moved since gap-cycle-2's
plans 18-21 each ran the standard `roadmap.update-plan-progress` step). Kept the top narrative
block's row (line 2433, `- [x] 34.9-17-PLAN.md`) per the plan's stated intent ("the checked twin ...
is the survivor"), deleted the bottom canonical wave-list's duplicate. Corrected the now-false claim
"34.9-17 completed the full reconciliation" (`34.9-VERIFICATION.md`'s own truth 8 found 34.9-17's
ledger named zero of the six code-review findings) and the `**Plans:**` line to state 22 total (17
original + 5 gap cycle 2), 21/22 executed on disk at edit time -- derived by counting the
`-SUMMARY.md` files actually present in the phase directory (21; `34.9-22`'s own Summary is written
after this task, standard closing-plan sequencing), not assumed. Appended a "Gap cycle 2 -- planned
2026-08-12" block with one `- [x]` row per `34.9-18`..`34.9-22`, the `34.9-21` row carrying the
literal verdict `PASS` read from `34.9-GUARD-PROOF.md`, and the `34.9-22` row stating the phase
awaits re-verification rather than that it closes. Left the top block's own separate, historically
accurate "6/6 gap-cycle plans complete (17/17 overall)" recap sentence (describing gap cycle 1's own
completion state as of 2026-08-11) untouched -- it is a true historical statement scoped to that
gap cycle, not a current-tense overclaim, and outside this task's named edit targets.

`STATE.md`: did not touch the Phase 34.5 blocker banner (confirmed unchanged by diff). Updated the
existing Phase 34.9 paragraph (added by plan 34.9-21's own commit, found via `read_first` rather
than assumed) to record 34.9-22's completion, `34.9-VERIFICATION.md`'s `gaps_found` status, and
`/gsd-verify-work 34.9` as the next step, with the guard's arm64-only/not-in-CI scope stated in the
same paragraph. Frontmatter: `stopped_at`/`last_updated`/`last_activity` updated to 34.9-22's
completion; `total_plans` 297->298 and `completed_plans` 283->284 (this plan's own single-plan
increment on top of the pre-existing count -- no gsd-sdk state-write verb was invoked against
STATE.md in this plan; all edits were direct hand-edits, sidestepping the documented corruption
class rather than triggering and re-correcting it). `percent`/`completed_phases`/`total_phases` left
**unchanged** at 74/17/23 -- Phase 34.9 does not complete in this plan, and `percent` tracks phases,
not plans, per this file's own standing note about the 2026-08-09 corruption.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - blocking] Line-wrap broke the `[Corrected ...]` marker across two lines**
- **Found during:** Task 2, first pass at the Section 1 correction
- **Issue:** the marker text `[Corrected 2026-08-12 — plan 34.9-22]` was split by the editor's
  80-column soft-wrap into `[Corrected\n  2026-08-12 — plan 34.9-22]`, which does not match the
  literal string the acceptance criteria and automated check require
- **Fix:** moved the marker onto its own short line so it survives unbroken
- **Files modified:** `34.9-LIVE-GATE-RERUN.md`
- **Commit:** `a42b28e20`

**2. [Rule 1 - bug in acceptance-criteria wording] REQUIREMENTS.md status-table row missed item 11**
- **Found during:** Task 2, running the "both name deferred-items.md items 11-13" acceptance check
- **Issue:** first draft of the REQ-34.9-08 status-table row cited only "items 12-13", omitting item
  11
- **Fix:** corrected to "items 11-13"
- **Files modified:** `REQUIREMENTS.md`
- **Commit:** `a42b28e20`

### Not fixed, documented instead

**3. Plan's Task 2 verify script scans `tripwire` unscoped, flagging pre-existing unrelated
content** -- see "Verify-script limitation" paragraph above. Not touched, per SCOPE BOUNDARY; four
occurrences in Phase 34.2/34.4 rows remain unscoped, exactly as they were before this plan started.

## i18n Gate

**N/A.** No user-facing string is added or modified by this plan -- every file touched is a
`.planning/` artifact, not application source.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern, or schema change at a trust boundary
is introduced by this plan -- it edits planning documents only.

## Next Steps

**The phase remains OPEN.** `34.9-VERIFICATION.md` is still `status: gaps_found`. Truth 8 must be
re-scored by the verifier against this plan's and gap cycle 2's landed evidence:
**`/gsd-verify-work 34.9`**.

## Self-Check: PASSED

All five files listed in `files_modified` (plus `34.9-GUARD-PROOF.md`, read-only) confirmed present
on disk; all three task commit hashes (`397990f4f`, `a42b28e20`, `ccf70ffb8`) confirmed present in
`git log --oneline --all`.
