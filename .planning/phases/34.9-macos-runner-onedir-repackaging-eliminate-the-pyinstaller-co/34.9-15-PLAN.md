---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 15
type: execute
wave: 2
gap_closure: true
closes_findings: [F-34.9-04]
depends_on: ["34.9-12", "34.9-13", "34.9-14"]
files_modified:
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md
autonomous: true
requirements: [REQ-34.9-11]

must_haves:
  truths:
    - "A re-run contract exists as a NEW document; `34.9-LIVE-GATE.md`'s FAIL run record from 2026-08-11 is preserved byte-for-byte and is not overwritten."
    - "The contract re-runs item 4 (Electron PACKAGED) and item 1 (Tauri DEV nested resolution), and carries items 2, 3 and 5 forward with a written non-invalidation argument rather than silently dropping them."
    - "Every path the contract asks the sidecar to spawn, or the operator to enter into a settings field, is ABSOLUTE — the 2026-08-11 run's item 5 failed ENOENT because the contract cited a repo-relative path and the sidecar spawns with cwd=<repo>/src-tauri."
    - "Item 4's PASS bar requires proof that a NEW dmg and zip were produced — mtime strictly after the recorded build-start timestamp — not merely that a dmg exists (F-34.9-02)."
    - "The contract warns that nile's version string is NOT a discriminator between override and default; only the spawned path is (F-34.9-04)."
    - "The contract requires the packaged tree's own on-disk shape to be proven directly, never electron-builder's stdout (12 symlinks under the packaged darwin bin root; codesign against the framework bundle itself)."
    - "This plan runs nothing: no launch, no `pnpm dist:mac`, no harness, and no `verdict`/`run_date`/`items_passed`/`items_failed`/`Observed:` value is written."
  artifacts:
    - path: ".planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md"
      provides: "the re-run contract, unrun"
      contains: "verdict: PENDING"
  key_links:
    - from: "34.9-LIVE-GATE-RERUN.md"
      to: "34.9-LIVE-GATE.md"
      via: "explicit citation of the 2026-08-11 FAIL and the carried-forward items"
      pattern: "34.9-LIVE-GATE.md"
    - from: "34.9-LIVE-GATE-RERUN.md item 4"
      to: "pnpm verify:runner-bundle"
      via: "the framework-structure enforcement added by plan 34.9-13"
      pattern: "verify:runner-bundle"
---

<objective>
Author the re-run contract for Phase 34.9's blocking live gate (REQ-34.9-11), covering the gap
cycle's fix.

Purpose: the 2026-08-11 run returned `verdict: FAIL` 4/5. Item 4 must be re-run and must PASS
before the phase can close. Three of the run's four findings are contract-relevant and must be
built into the re-run rather than left as prose: the relative-path defect (deviation 4), the
version-string non-discriminator (F-34.9-04), and the stale-artifact trap (F-34.9-02).

Output: `34.9-LIVE-GATE-RERUN.md`, a NEW, UNRUN document.

**This plan is FORBIDDEN from running any item it authors.** No `pnpm tauri:dev`, no
`pnpm dist:mac`, no `pnpm verify:runner-bundle` against a built app, no harness, no launch, and no
`verdict`/`run_date`/`items_passed`/`items_failed`/`Observed:` value. Plan 34.9-16 is the only
writer of results. This is the standing project rule
(`.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md`, decision D-E),
and it is the split plans 34.9-10 and 34.9-11 already used for the first run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PACKAGING-LIMITATIONS.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-CI-ROUNDTRIP.md
@.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md

<interfaces>
<!-- Established at planning time. Reuse, do not re-derive. -->

**The re-run's item set, and the reasoning that produced it — reproduce this reasoning IN the
contract, do not merely assert the conclusion.**

| Item | Disposition | Why |
|---|---|---|
| 4 — Electron PACKAGED | **RE-RUN, scored** | The failure. Its subject was never reached: the build aborted before any packaged app existed. |
| 1 — Tauri DEV nested resolution | **RE-RUN, scored** | Regression canary. Plan 34.9-12 edits `electron.vite.config.ts`, which `pnpm tauri:dev` also runs (`electron-vite build && pnpm build:sidecar && tauri dev`). A defect in the new `closeBundle` plugin would surface here. One launch, one gesture, cheap. |
| 2 — Amazon (nile) click → spawn | **CARRY FORWARD** | Requires a real Amazon login. The 2026-08-11 evidence resolved `/Users/.../GameLib/public/bin/arm64/darwin/nile/nile` — the DEV sidecar reads `publicDir` = `<repo>/public`, never `build/`. The gap cycle's fix changes only the `public/` → `build/` copy and `dist/` cleaning, both packaging-side. Nothing in item 2's evidence path was touched. |
| 3 — GOG (gogdl) capture → login | **CARRY FORWARD** | Same argument, same sink, requires a real GOG login. |
| 5 — `altNileBin` override | **CARRY FORWARD** | Same argument. Its two contract defects (relative path; version string non-discriminator) are recorded as contract amendments for any future run, not as re-run triggers — the item itself PASSed on absolute-path evidence. |
| *(unscored)* Tauri PACKAGED | **UNCHANGED, unscored** | Blocked on `R-34.5-G1-PKG` (Phase 34.5 `deferred-items.md` item 12), pre-dating this phase. |

**Deviation 4, verbatim from the 2026-08-11 run record — the rule it produces is standing:**
item 5's contract specified the repo-relative `public/bin/x64/darwin/nile`, but the sidecar spawns
with `cwd=<repo>/src-tauri`, so the path resolved to `src-tauri/public/bin/x64/darwin/nile` and
failed ENOENT. **Any contract citing a path for the sidecar to spawn must use an absolute path.**

**F-34.9-04, verbatim:** the Settings version field cannot discriminate override from default for
nile — the x64 onefile and arm64 onedir **both** report `1.1.2 Will A. Zeppeli`. Only the spawned
path in `gamelib.log` distinguishes them. This nearly produced a false PASS mid-run.

**F-34.9-02, verbatim:** `dist/GameLib-0.7.0-macOS-arm64.dmg` and `.zip` dated 2026-07-21 survived
a total build failure and read as its output. Plan 34.9-14 adds `pnpm clean:dist-mac` as the first
step of `dist:mac`, but the contract must still prove NEWNESS positively — never accept a mutating
command's own report as proof of its effect.

**New commands the re-run has that the first run did not:**
- `pnpm clean:dist-mac` (34.9-14) — now runs automatically as `dist:mac`'s first step.
- `pnpm verify:runner-bundle <app> --arch=arm64` (34.9-13) — now ENFORCES framework structural
  integrity (`Versions/Current` must be a symlink), not just presence/exec-bit/Mach-O/file-count.
- `find <root> -type l | wc -l` → the expected count is **12** (4 symlinks × 3 runners).

**Sink partition — authoritative, reproduce it in the contract unchanged.** Rust `[shell]` lines →
session transcript ONLY. Sidecar `logInfo`/`logWarning`/`logError` → `~/Library/Logs/GameLib/gamelib.log`
ONLY. Per-runner output → `~/Library/Logs/GameLib/runners/{legendary,gog,nile}.log`, a THIRD sink
(this is where the actual version STRING a `--version` probe prints lands). Sidecar STDERR →
transcript, prefixed `[sidecar:err]`. Sidecar `console.*` → nothing at all.

**Identifier-redaction discipline.** No cookie value, gamekey, OAuth code, account id, or PKCE
material (`code_verifier`, `serial`, `client_id`) in any plan or captured artifact — timestamps,
window/launch ordinals and file paths only. This binds on
`src/backend/storeManagers/nile/user.ts:62`'s `logInfo(['Register data is:', output], ...)`, which
writes PKCE material to `gamelib.log` in plaintext; use its TIMESTAMP only, never its contents.

**Preconditions unchanged from the first run and still true:** `meta/runnersOnedirDigests.json`
holds six `PENDING-CI-PUBLISH` sentinels, so `pnpm download-helper-binaries` throws by design and
must NOT be run; only arm64 is staged, locally, not digest-verified; x64 exists nowhere.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 34.9-LIVE-GATE-RERUN.md</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md</files>
  <read_first>
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md, in full — it is the template AND the source of every carried-forward fact; the re-run contract must not contradict it
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md — Section 3's dual-sink evidence-capture standard is mandatory and must be reproduced in full
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PACKAGING-LIMITATIONS.md — for the unscored Tauri-PACKAGED item's wording and owner
    - The `<interfaces>` block above
  </read_first>
  <action>
    Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md`
    as a NEW file. **Do not edit `34.9-LIVE-GATE.md`** — its FAIL run record is the permanent
    evidence for this gap cycle's existence and Phase 34.5's own precedent
    (`34.5-LIVE-GATE-RERUN-2.md`) is one file per run.

    Frontmatter: `phase`, `plan: 15`, `type: live-gate`, `status: authored`, `blocking: true`,
    `created: <today>`, `supersedes_run: 34.9-LIVE-GATE.md (verdict FAIL, 2026-08-11)`,
    `verdict: PENDING`, `run_date: null`, `items_total: 2`, `items_passed: null`,
    `items_failed: null`, `items_carried_forward: 3`, `items_not_attempted: null`, `runner: human`,
    `session_dir: null`, `findings: []`, and a `notes:` block stating that this document was
    authored by plan 34.9-15, BEFORE any live work, by a plan forbidden from running any item in
    it, and that plan 34.9-16 is the only writer of results.

    Body sections, in this order:

    1. **Scope and what changed since the FAIL run.** Name the gap cycle's three code plans
       (34.9-12 symlink preservation, 34.9-13 framework-structure enforcement, 34.9-14 stale-artifact
       clean) and what each makes newly checkable.

    2. **Item disposition table with the carry-forward argument.** Reproduce the `<interfaces>`
       table AND its reasoning in full. State plainly why items 2/3/5's evidence is not invalidated:
       the DEV sidecar resolves runners under `<repo>/public`, proven by the 2026-08-11 log lines
       themselves, while the fix changes only the `public/` → `build/` copy and `dist/` cleaning.
       State equally plainly what a carry-forward is NOT: it is not a fresh measurement, and if the
       re-run's item 1 shows any DEV-side regression, items 2/3/5 must be re-run before the phase
       closes. Make that conditional explicit rather than implied.

    3. **Sink partition** — reproduce unchanged from `34.9-LIVE-GATE.md`.

    4. **Identifier-redaction discipline** — reproduce unchanged.

    5. **Absolute-path rule** — its own named section, citing deviation 4 and the ENOENT it caused.
       State the rule: every path this contract asks the sidecar to spawn, or the operator to type
       or paste into a settings field, is written ABSOLUTE. Repo-relative paths appear ONLY in
       shell commands run from the repo root, and each such command says so.

    6. **Dual-sink evidence-capture instruction** — reproduce Section 3's standard in full: one
       UTC-named session directory created once; `tee -a` never bare `tee` and never `>`; the exact
       `=== GATE LAUNCH ${N} — <UTC> ===` delimiter literal; per-launch `gamelib.log` AND per-runner
       log archiving BEFORE the next launch starts (because `log_writer.ts:72-74` renames the live
       file to `.old` on the next process's first write); the pre-launch/at-window/at-teardown
       single-instance assertion with `pgrep -f 'target/debug/gamelib-shell'` for the Tauri launch
       and `pgrep -f 'GameLib.app'` for the Electron launch, and the rule that a count other than
       exactly 1 ABORTS the launch and makes its evidence non-scorable; per-item launch-ordinal
       recording; and the closing inventory (`ls -la` + `wc -l`).

    7. **Preconditions**, each labelled OPERATOR-AT-GATE-TIME or machine-satisfiable-in-preflight,
       every status left blank for plan 34.9-16 to fill: clean working tree; macOS arm64 hardware
       with a build at the tip including all of 34.9-12/13/14; no pre-existing `gamelib-shell` or
       `GameLib.app` instance; the three arm64 onedir runner trees present, executable, real Mach-O;
       **do NOT run `pnpm download-helper-binaries`** (six `PENDING-CI-PUBLISH` sentinels, throws by
       design); `npm run test:ci` green as a PRECONDITION ONLY, never as evidence for any item;
       a `shasum -a 256` baseline of the three arm64 runner binaries taken before launch 1 and
       re-checked immediately before `pnpm dist:mac`; `altNileBin` confirmed EMPTY in `config.json`
       before launch 1, positively (the 2026-08-11 run left it `''`, but a positive re-check is
       required — an item-5 override left set would make item 4's nile resolution read false, which
       is hazard A from the first contract's own Test 5 pass); and the dual-sink capture in place.

    8. **Scored item 1 — Tauri DEV, nested resolution, all three runners.** Gesture: launch
       `pnpm tauri:dev`, navigate to Settings > Advanced Settings (each of `AltLegendaryBin`,
       `AltGOGdlBin`, `AltNileBin` fires its version probe on mount — do NOT frame this as "on
       startup", which is false under the sidecar because `initHeadless()` skips the system-info
       dump `init()` performs for Electron; this correction is F-34.9-G1-01 from the first contract
       and must not regress). Required evidence, two sinks: three `Running command:` lines in
       `gamelib.log` containing `darwin/legendary/legendary`, `darwin/gogdl/gogdl`,
       `darwin/nile/nile`; and a real version in the per-runner logs or the Settings UI
       (`"0\.20\.43"` / `1\.2\.1` / `1\.1\.2`), with no field reading `Invalid`. PASS requires both
       sinks for all three runners.

    9. **Scored item 2 (was item 4) — Electron PACKAGED.** This is the item that failed. Its
       required evidence, all of it about the artifact's own on-disk shape rather than any command's
       self-report:
       - Record a UTC `BUILD_START` timestamp immediately before `pnpm dist:mac` and put it in the
         transcript.
       - `pnpm dist:mac` exits 0. (Note in the contract that `dist:mac` now runs
         `pnpm clean:dist-mac` first, so `dist/` starts with no macOS artifacts — and that this is
         precisely why the newness check below is still required rather than redundant: a clean
         that silently failed would leave the trap intact.)
       - A `.dmg` AND a `.zip` matching `GameLib-*-macOS-arm64.*` exist in `dist/` with mtime
         STRICTLY AFTER `BUILD_START`. Cite the F-34.9-02 trap by name and state that "a dmg exists"
         is not the criterion.
       - `find "<built .app>/Contents/Resources/app.asar.unpacked/build/bin/arm64/darwin" -type l | wc -l`
         equals exactly **12**. This is the direct on-disk proof that the symlinks survived
         electron-builder's own copy — a step the first run never had a check for.
       - `codesign -dv` against
         `<built .app>/Contents/Resources/app.asar.unpacked/build/bin/arm64/darwin/gogdl/_internal/Python.framework`
         — the framework BUNDLE, not the inner `gogdl/gogdl` binary. Record its output as DATA and
         assert it contains zero occurrences of `bundle format is ambiguous`. State explicitly why
         the bundle and not the binary: the 2026-08-11 run's `codesign --verify --deep` on
         `.../gogdl/gogdl` exited 0 while the framework was already malformed, and that false
         comfort is the lesson — verifying a nearby artifact is not verifying the one the toolchain
         will touch.
       - `pnpm verify:runner-bundle "<built .app>" --arch=arm64` exits 0. Its per-file
         signature-state report is DATA, never a pass condition (D-03/D-04, no Apple Developer
         credentials enrolled); `unsigned`/`adhoc` results are expected. Its framework-structure
         enforcement (plan 34.9-13) IS a pass condition.
       - `du -sk` of the three runner directories inside the packaged app sums to within 10% of the
         `public/bin/arm64/darwin` source sum (~84M). Record both. This is **F-34.9-03**
         corroboration that the symlinks genuinely survived, not an independent criterion — say so.
       - Open the built `.app`, navigate to Settings > Advanced Settings, and repeat item 1's
         two-sink check against the PACKAGED app's own `gamelib.log` and per-runner logs, with the
         resolved paths now under `Contents/Resources/app.asar.unpacked/build/bin/arm64/darwin/`.

    10. **Carried-forward items 2, 3 and 5** — a section, not a silence. Reproduce each item's
        2026-08-11 verdict, its launch ordinal, and its evidence file names, then the
        non-invalidation argument. Add, under item 5, both of its contract amendments for any future
        run: the ABSOLUTE-path requirement, and the **F-34.9-04** warning in its own words — the
        Settings version field cannot discriminate override from default for nile, because the x64
        onefile and the arm64 onedir both report `1.1.2 Will A. Zeppeli`; only the spawned path in
        `gamelib.log` separates them.

    11. **Unscored item — Tauri PACKAGED.** NOT ATTEMPTED, NOT SCORED, contributes to neither
        `items_total` nor the verdict. Blocked on `R-34.5-G1-PKG`. Reproduce the mechanism and the
        owner from `34.9-PACKAGING-LIMITATIONS.md`.

    12. **Verdict rule.** No-partial-pass across the two scored items. Any FAIL routes to another
        gap cycle per D-08. The unscored item, the carried-forward items and the BLOCKED-UNPROVEN
        x64 leg are never counted toward the bar.

    13. **What this gate does NOT prove.** Tauri-PACKAGED; real-certificate signing and
        notarization (D-03/D-04 — and note that F-34.9-01 made real notarization strictly worse than
        unproven, since a framework failing ADHOC signing would not survive real signing either);
        the x64 leg in full (BLOCKED-UNPROVEN, `34.9-CI-ROUNDTRIP.md` Outcome C, no artifact exists
        anywhere); the digest-verified download → verify → extract path (six sentinels); Windows and
        Linux; any cold-spawn ratio claim (`34.9-MEASUREMENT.md`'s cold ratio is UNMEASURED; only
        the warm ratio, 26.6x–32.5x, is measured and valid).

    Leave every `Observed:` field as the literal text `Observed: (unfilled — plan 34.9-16)`.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/34.9-.../34.9-LIVE-GATE-RERUN.md` succeeds and `git status --porcelain .planning/phases/34.9-.../34.9-LIVE-GATE.md` is EMPTY — the original run record is untouched
    - Frontmatter contains `verdict: PENDING` and `run_date: null`; `grep -c "^verdict: PENDING" <file>` returns exactly 1
    - `grep -c "Observed: (unfilled — plan 34.9-16)" <file>` returns exactly 2 (one per scored item) and `grep -c "^\*\*Observed: PASS\|^\*\*Observed: FAIL" <file>` returns 0
    - All thirteen numbered sections above are present; verify each by an anchored heading grep and list the thirteen matched headings in the Summary
    - The absolute-path rule holds inside the document: every path presented for the sidecar to spawn or for the operator to enter begins with `/`. Verify by listing every such path in the Summary and confirming each; a bare `grep -c 'public/bin'` is NOT sufficient because repo-root shell commands legitimately use relative paths
    - The word `12` appears as the expected symlink count in item 2's evidence list, and `bundle format is ambiguous` appears as an asserted ABSENCE
    - `grep -c "1\.1\.2 Will A\. Zeppeli" <file>` returns >= 1 — the F-34.9-04 non-discriminator warning is present with its literal
    - `grep -c "mtime" <file>` returns >= 1 — the F-34.9-02 newness criterion is present
    - No PKCE material, cookie value, account id or gamekey appears anywhere in the document (`grep -in "code_verifier\|client_id\|serial" <file>` returns only the redaction-discipline paragraph's own prohibition text, and the Summary quotes those lines to show they are prohibitions, not values)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md; test -f "$F" && test "$(grep -c '^verdict: PENDING' "$F")" = "1" && test "$(grep -c 'Observed: (unfilled — plan 34.9-16)' "$F")" = "2" && echo CONTRACT-AUTHORED-UNRUN</automated>
  </verify>
  <done>The re-run contract exists, is unrun, preserves the original FAIL record, re-runs two items and carries three forward with a written argument, and builds all three contract-relevant findings into its criteria.</done>
</task>

<task type="auto">
  <name>Task 2: Structural Reachability Review and the author/runner separation self-check</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md</files>
  <read_first>
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md — Section 2's seven tests and Section 4's two rules; Test 5's unit of review is the PAIR, and the reduction applied must be STATED
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md — its own Structural Reachability Review, for the per-item precedents (F-34.9-G1-01, F-34.9-G1-02) that must not regress
    - The document authored in Task 1
  </read_first>
  <action>
    Append a **Structural Reachability Review** section to `34.9-LIVE-GATE-RERUN.md`.

    Produce a per-item table with one column per test (Test 1 origin/scheme, Test 2 concurrency,
    Test 3 log-line emitter + SINK, Test 4 absence-observability, Test 5 interaction, Test 6
    pre-existing external state, Test 7 UI-level gesture), covering BOTH scored items, every
    precondition, and the evidence-capture instruction itself. Every cell states a specific reason
    with a file:line or a command — never a bare mark.

    Pay particular attention to these, which the re-run's own shape creates:
    - **Test 3 + Test 4 on item 2's `bundle format is ambiguous` ABSENCE assertion.** This is an
      absence check, so Test 4 applies directly: state what the PRESENCE case would look like and
      cite the 2026-08-11 run record as the proof that presence IS observable on this exact
      surface. An absence assertion whose presence case was never observed proves nothing — here it
      was observed, which is what makes this one legitimate.
    - **Test 4 on the symlink count.** `12` is a presence assertion with a known-bad counterpart
      (`0`, measured in `build/` before the fix), so state both numbers.
    - **Test 6 on `altNileBin`.** The premise "no override is set" is external state living in
      `config.json`. Per the standing rule that an emptied app-side store does not prove a clean
      state, the precondition must name a POSITIVE observable — a `Running command:` line resolving
      through the nested default path — not merely a field reading empty.
    - **Test 7 on item 1's gesture.** Trace the Settings > Advanced Settings mount to
      `src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx` and confirm the three
      `Alt*Bin` components still render there and still fire their version probes on mount. Do this
      by reading current source, not by citing the first contract — that contract was written
      2026-08-10 and a re-confirmation is cheap.

    Then run the **Test 5 pairwise pass** over the re-run's own requirement set. State the reduction
    applied (state-mutating requirements × evidence-bearing requirements), enumerate the pairs
    considered, and record how many were considered and which were flagged, so "found nothing" stays
    distinguishable from "did not look". The state-mutating requirements this contract has are:
    `pnpm clean:dist-mac`'s deletion of `dist/` macOS artifacts; `pnpm dist:mac`'s
    `electron-vite build` rewriting `build/` from `public/`; the app restart between the two
    launches; and `log_writer.ts`'s rename of `gamelib.log` to `.old` on each new process's first
    write. Hazard B from the first contract (the `shasum` re-check before `dist:mac`) still applies
    and must appear.

    Finally, append an **Authoring self-check** subsection recording, as assertions this plan's own
    executor performed:
    - the commands this plan did NOT run (`pnpm tauri:dev`, `pnpm dist:mac`,
      `pnpm verify:runner-bundle` against any built app, any harness, any launch);
    - that no `verdict`/`run_date`/`items_passed`/`items_failed`/`Observed:` value was written;
    - that `git log --stat` for this plan's commits shows changes to exactly one file,
      `34.9-LIVE-GATE-RERUN.md`, and to nothing else.
  </action>
  <acceptance_criteria>
    - The review table covers both scored items, all preconditions, and the evidence-capture instruction, with all seven test columns populated and no cell containing only a mark
    - The Test 5 pairwise section states the reduction applied AND the count of pairs considered AND which pairs were flagged
    - Test 7's item-1 row cites `src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx` and states that the three `Alt*Bin` components were re-confirmed against CURRENT source at this authoring, with the grep or read used
    - `git log --format=%H -n 5 -- .planning/phases/34.9-.../ | head -2` combined with `git show --stat` for this plan's commits shows exactly one file changed: `34.9-LIVE-GATE-RERUN.md`
    - `git status --porcelain` shows no modification to `34.9-LIVE-GATE.md`, no new file under `/tmp/gamelib-gate-*`, and no `dist/` or `build/` change attributable to this plan
    - The Summary states explicitly, as its own line: "This plan ran no gate item, launched no harness, and wrote no result field."
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md; grep -q "Structural Reachability Review" "$F" && grep -q "Test 5" "$F" && grep -q "Authoring self-check" "$F" && test -z "$(git status --porcelain .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE.md)" && echo REVIEW-OK-ORIGINAL-UNTOUCHED</automated>
  </verify>
  <done>The contract carries a full seven-test reachability review, a stated-reduction pairwise pass, and a self-check proving this plan authored without running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate contract → release decision | A defective contract produces a false PASS and ships a broken build. Contract authoring is a defect surface in its own right — this project has measured eight distinct contract-authoring defects. |
| gate artifacts → repository | Captured evidence can carry secrets (`gamelib.log` holds plaintext PKCE material at `nile/user.ts:62`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.9G-13 | Repudiation | the author also running the gate | mitigate | Decision D-E's author/runner separation, enforced by an acceptance criterion asserting `git show --stat` for this plan's commits touches exactly one file, and by leaving every `Observed:` field as a literal unfilled marker that plan 34.9-16 must replace. |
| T-34.9G-14 | Spoofing | electron-builder's stdout accepted as proof of its effect | mitigate | Every item-2 criterion asserts the artifact's own on-disk shape (mtime, `find -type l` count, `codesign` against the bundle, `du -sk`, `verify:runner-bundle` exit) rather than any command's self-report. |
| T-34.9G-15 | Information Disclosure | PKCE material transcribed into the contract or a captured artifact | mitigate | The redaction-discipline section is reproduced verbatim; `nile/user.ts:62` is named; only that line's TIMESTAMP may ever be used. An acceptance criterion requires the Summary to show that every `code_verifier`/`client_id`/`serial` occurrence in the document is a prohibition, not a value. |
| T-34.9G-16 | Tampering | overwriting the FAIL run record | mitigate | The re-run is a NEW file; an acceptance criterion asserts `git status --porcelain 34.9-LIVE-GATE.md` is empty. |
| T-34.9G-17 | Denial of Service | a carried-forward item hiding a real regression | mitigate | The carry-forward is conditional and the condition is written into the contract: any DEV-side regression surfaced by item 1 requires items 2/3/5 to be re-run before the phase closes. |
| T-34.9G-SC | Tampering | npm/pip/cargo installs | N/A | **This plan installs no packages and writes no code.** The Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- The re-run contract exists, `verdict: PENDING`, `run_date: null`, both `Observed:` fields unfilled.
- `git status --porcelain` for `34.9-LIVE-GATE.md` is empty.
- `git show --stat` for this plan's commits lists exactly one changed file.
- No `/tmp/gamelib-gate-*` directory was created by this plan.
- `npm run test:ci` was NOT run as evidence by this plan; if run at all it is recorded as a
  precondition of the repo state, never as gate evidence.
- No user-facing string added — the i18n gate is N/A; state this explicitly in the Summary.
</verification>

<success_criteria>
A re-run contract exists that a human operator can execute without interpretation, that re-runs the
failed item and one regression canary, that carries three items forward with a written and
conditional non-invalidation argument, that proves the packaged artifact's shape directly rather
than trusting electron-builder's stdout, and that has been authored by a plan which demonstrably ran
none of it.
</success_criteria>

<output>
Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-15-SUMMARY.md` when done.
</output>
