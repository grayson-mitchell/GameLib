---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 20
type: guard-proof
status: run
blocking: false
created: 2026-08-12
closes_finding: CR-01
closes_truth: "34.9-VERIFICATION.md truth 8"
verdict: PASS
run_date: 2026-08-12
directions_total: 2
directions_passed: 2
directions_failed: 0
runner: human
session_dir: "/Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof/20260812T084655Z"
findings: [F-34.9-21-01, F-34.9-21-02, F-34.9-21-03]
run_by: 34.9-21
notes: |
  Authored 2026-08-12 by plan 34.9-20, BEFORE any live work, by a plan FORBIDDEN from running any
  direction in this document, injecting any defect, or running `pnpm dist:mac` /
  `pnpm release:mac`. No `verdict`/`run_date`/`directions_passed`/`directions_failed`/`Observed:`
  value was written by that plan. Plan 34.9-21 is the only writer of results -- the same
  author/runner separation this project's standing reference
  (`.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md`, decision
  D-E) requires for every contract, and the same discipline `34.9-LIVE-GATE-RERUN.md` (plans
  34.9-15/16) already used.

  RUN COMPLETE 2026-08-12 by plan 34.9-21. Both directions observed and scored PASS from disk
  evidence (never from a mutating command's own report); restore independently audited twice
  (Task 2's in-flight check + Task 3's own re-run from the live tree) and matched the Task 1
  baseline on all five measures both times. See the "# RUN RECORD -- 2026-08-12" section below for
  the full per-direction outcome table, restore audit, findings and deviations.
---

# Phase 34.9 -- Guard-Wiring Proof Contract (closes CR-01, REQ-34.9-08)

**This plan (34.9-20) is FORBIDDEN from running any direction below.** No defect injection, no
`mv`/`cp -RL` against any `Python.framework`, no `rm -rf build/bin`, no `pnpm dist:mac`, no
`pnpm release:mac`, and no `verdict`/`run_date`/`directions_passed`/`directions_failed`/`Observed:`
value written. Plan **34.9-21** is the only plan permitted to fill any `Observed:` field or this
frontmatter's result fields. Authoring and running are separate plans, mirroring plan 34.9-15's
`34.9-LIVE-GATE-RERUN.md`.

## 1. What this proves and why it exists

`34.9-VERIFICATION.md` truth 8 scored **FAILED**: `meta/verifyRunnerBundle.ts` exists, is
unit-tested, and works when invoked by hand -- but was invoked by nothing in the build pipeline.
`34.9-REVIEW.md`'s sole Critical finding, **CR-01**, names this exact gap: `closeBundle` does not
fail the build on a skipped symlink restore, and nothing in the pipeline runs
`verify:runner-bundle` to catch a regression of that shape. Plan 34.9-20 wired the guard into
`dist:mac` and `release:mac` (Task 1 of that plan) -- but wiring a guard into a script and PROVING
it fires are two different claims, and this project's own narrative already made the mistake of
asserting the second without evidence once (`34.9-REVIEW.md` flags the prior claim that the guard
was already automated and self-enforcing, with no invocation anywhere, as CR-01's actual defect).

**The claim under test, stated in one sentence:** the guard wired by plan 34.9-20 aborts
`pnpm dist:mac` before `electron-builder` when a runner framework is dereferenced, and does not
obstruct a normal build when it is not.

An unfired guard is an unproven guard. This contract exists because CR-01 was a claim nobody had
tested -- it makes the guard's firing OBSERVABLE, in both directions, rather than asserted.

## 2. Preconditions

Every status below is left BLANK for plan 34.9-21 to fill. Each is labelled
OPERATOR-AT-GATE-TIME (requires a live decision or observation at run time) or
MACHINE-SATISFIABLE-IN-PREFLIGHT (a command the runner executes once before Direction A begins).

1. **[OPERATOR-AT-GATE-TIME]** macOS **arm64** hardware, at a tip containing plans 34.9-18,
   34.9-19 and 34.9-20 (`git log --oneline -5` should show all three plans' commits, including
   `feat(34.9-20): wire verify:runner-bundle into dist:mac and release:mac`).
2. **[MACHINE-SATISFIABLE-IN-PREFLIGHT]** The three arm64 onedir trees are staged at
   `/Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/{legendary,gogdl,nile}`, and
   `/Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework`
   shows `Python` and `Resources` as SYMLINKS and `Versions/Current` as a SYMLINK in
   `ls -la /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework`
   (and `ls -la .../Python.framework/Versions`). Record this verbatim as the **RESTORE BASELINE**
   before anything else in this document is touched.
3. **[MACHINE-SATISFIABLE-IN-PREFLIGHT]** `shasum -a 256` of the three arm64 runner binaries
   (`/Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/{legendary,gogdl,nile}/{legendary,gogdl,nile}`),
   taken before injection and re-checked byte-identical after restore (Section 4).
4. **[OPERATOR-AT-GATE-TIME] Do NOT run `pnpm download-helper-binaries`.** It throws by design on
   six `PENDING-CI-PUBLISH` sentinels in `meta/runnersOnedirDigests.json` -- this is expected, not
   a blocker, and is not part of this contract's scope.
5. **[MACHINE-SATISFIABLE-IN-PREFLIGHT]** No Xcode/electron-builder run already in flight; record
   the state of `/Users/graysonmitchell/Projects/GameLib/dist/` (`ls -la`) before starting.
6. **[MACHINE-SATISFIABLE-IN-PREFLIGHT]** A session directory, created ONCE, under
   `/Users/graysonmitchell/Projects/GameLib/node_modules/.cache/` with a UTC name (e.g.
   `/Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof-<ISO8601-UTC>/`)
   -- same volume as `public/` so `mv` is atomic and cheap, and git-ignored (`.gitignore:4`,
   `/node_modules`).
7. **[OPERATOR-AT-GATE-TIME]** `tee -a`, never bare `tee`, never `>`, for every captured command --
   stdout AND stderr must both land in the transcript, because `verifyRunnerBundle`'s FAIL block
   prints to **stdout** via `console.log`, not stderr.
   > **AMENDED BY v2 §A2 -- read both together.** The `2>&1` requirement above is UNCHANGED and
   > still mandatory. What changes is the mechanism: `> log 2>&1` replaces `| tee -a log`, because
   > the piped form's companion exit-capture idiom silently no-ops under zsh. This rule's real
   > intent -- **no block may overwrite another block's evidence** -- is preserved by giving every
   > block its own filename, which v2 makes mandatory. Do not read v2 as licence to reuse one log
   > file across blocks.

---

## Preflight results -- recorded by plan 34.9-21, Task 1 (2026-08-12)

**Session directory:**
`/Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof/20260812T084655Z`
(pointer file:
`/Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof/CURRENT`).
All paths below beginning `<session>/` are relative to this directory.

1. **[OPERATOR-AT-GATE-TIME]** Not evaluated by this task -- requires the live operator's own
   confirmation at Direction A/B time. `git log --oneline -5` at Task 1 time shows the tip carrying
   plans 34.9-18/19/20 (`a49f53a6c` and ancestors), consistent with this precondition.
2. **[MACHINE-SATISFIABLE-IN-PREFLIGHT] RESTORE BASELINE, filled.** `find
   public/bin/arm64/darwin -type l` returns exactly **12** symlinks. Evidence:
   `<session>/evidence/baseline-symlinks.txt` (all 12 paths + `readlink` targets),
   digest `<session>/evidence/baseline-symlinks-digest.txt` =
   `c28becd50ad1304668e1fedd9cafe9934abe90cb34443629328110b6aab6ae1a`. The `gogdl` rows confirm
   `_internal/Python.framework/Python` and `.../Resources` are symlinks, and
   `.../Versions/Current` is a symlink to `3.14`.
3. **[MACHINE-SATISFIABLE-IN-PREFLIGHT] Filled.** `shasum -a 256` of the three arm64 runner
   binaries, pre-injection (evidence: `<session>/evidence/baseline-shasum.txt`):
   - `legendary/legendary`: `0287ae10f817ac6bfba8e4dc9d622046d9057f2bed24bf786d13895e3dd9cfaa`
   - `gogdl/gogdl`: `3a642485b8e926cead8c64b2141dda5814d0df959b83b72fb4857b540826fa98`
   - `nile/nile`: `99e3529af6aee24bc37334296a1611c569a0274ed92e0c335aa52bf56dcbc132`
4. **[OPERATOR-AT-GATE-TIME]** `pnpm download-helper-binaries` will NOT be run at any point in
   this session -- standing instruction, restated here.
5. **[MACHINE-SATISFIABLE-IN-PREFLIGHT] Filled.** No Xcode/electron-builder run was in flight at
   preflight time. `dist/` pre-run listings are captured per-direction inside Task 2's own
   `FAIL_START`/`PASS_START` evidence, per the contract's own Direction A/B step ordering.
6. **[MACHINE-SATISFIABLE-IN-PREFLIGHT] Filled.** Session directory created once at the path
   above, under `node_modules/.cache/`, UTC-named (`20260812T084655Z`), same volume as `public/`
   (both under `/Users/graysonmitchell/Projects/GameLib`), git-ignored via the repo root
   `.gitignore`'s `/node_modules` rule.
7. **[OPERATOR-AT-GATE-TIME]** Restated as a standing instruction for Task 2: every captured
   command uses `tee -a` against both stdout and stderr.

### Additional preflight measurements (plan 34.9-21 Task 1, beyond the contract's numbered items)

- Regular-file count of `public/bin/arm64/darwin`: **290** (evidence: `baseline-file-count.txt`).
- `du -sk public/bin/arm64/darwin`: **99252** KB (evidence: `baseline-du-darwin-root.txt`).
- `du -sk public/bin/arm64/darwin/gogdl/_internal/Python.framework`: **5332** KB (evidence:
  `baseline-du-gogdl-framework.txt`).
- `git status --porcelain`: **empty**, 0 bytes (evidence: `precond-git-status.txt`).
- `git check-ignore -v` on the mutation target
  (`public/bin/arm64/darwin/gogdl/_internal/Python.framework`): **matched**, rule
  `public/bin/.gitignore:3:**/gogdl` (evidence: `precond-check-ignore.txt`).
- `df -h /Users/graysonmitchell/Projects/GameLib`: `/dev/disk3s5 926Gi 871Gi 11Gi 99% capacity`
  (evidence: `precond-df.txt`). **Flagged, not itself a stop condition**: only 11Gi free at 99%
  capacity, matching the planning-time hazard already named in this contract and in T-34.9P-03.
  Direction A/B must not attribute an ENOSPC abort to the guard.
- `dist:mac`/`release:mac` script bodies transcribed verbatim from `package.json`
  (evidence: `precond-script-bodies.txt`):
  - `dist:mac` (line 49): `export CSC_IDENTITY_AUTO_DISCOVERY=false && pnpm clean:dist-mac && pnpm
    build-steam-bridge && electron-vite build && pnpm verify:runner-bundle build --arch=arm64 &&
    electron-builder --mac`
  - `release:mac` (line 44): `pnpm clean:dist-mac && pnpm build-steam-bridge && electron-vite
    build && pnpm verify:runner-bundle build --arch=arm64 && electron-builder -p always --mac
    --x64 --arm64`
  - Both confirmed to run `pnpm verify:runner-bundle build --arch=arm64` immediately before
    `electron-builder`.
- `npm run test:ci`: **243/243 suites, 4762 passed / 1 skipped / 4763 total, exit 0** (evidence:
  `precond-test-ci.txt`). **This is a PRECONDITION ONLY and is NOT evidence for either direction
  of this proof contract.**

### PRECONDITION FAILURE -- STOP (2026-08-12)

`pgrep -f 'GameLib.app'` returned empty (exit 1 -- correct, this probe is satisfied).
`pgrep -f 'target/debug/gamelib-shell'` returned **PID 32883** (parent PID 32779, exit 0 --
evidence: `precond-pgrep-shell.txt`), independently confirmed live via
`ps -p 32883 -o pid,ppid,command` (`target/debug/gamelib-shell`, still running at measurement
time).

**This precondition is UNSATISFIED.** A running dev-shell instance can hold open file handles
into the vendored tree, or write concurrently to logs, while this run mutates and restores
`public/bin/arm64/darwin` -- exactly the class of hazard T-34.9P-02 (a partially-restored tree
read as restored on the operator's word) exists to guard against. Per this plan's own Task 1
instruction ("If ANY precondition fails, STOP and record why. Do not proceed on a failed
precondition."), Task 1 halts here.

**No mutation of the vendored tree has occurred.** The re-verification below confirms this.

**Required before Task 2 can begin:** the operator must quit the running GameLib dev instance
(kill or gracefully exit PID 32883 and its parent 32779, or use the app's own Quit), then both
`pgrep` probes must be re-run and confirmed empty before Direction A's first mutating step.

**RESOLVED (2026-08-12, same session).** The operator quit the running dev instance. Both probes
re-run and confirmed empty: `pgrep -f 'GameLib.app'` exit `1`, `pgrep -f
'target/debug/gamelib-shell'` exit `1` -- PID 32883 no longer exists. Independently re-verified
the vendored tree is still byte-identical to the Task 1 baseline before proceeding: symlink
inventory diffs clean against `<session>/evidence/baseline-symlinks.txt`, digest
`c28becd50ad1304668e1fedd9cafe9934abe90cb34443629328110b6aab6ae1a` on both, `git status
--porcelain` empty. **All Task 1 preconditions are now satisfied; Task 1 is complete. No
mutation has occurred at any point in Task 1.** Proceeding to Task 2.

### Restore recipe and abort-recovery path (written before any mutation exists)

- **Pristine framework will live at:**
  `/Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof/20260812T084655Z/pristine/Python.framework`
- **Restoring** is: `rm -rf
  /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework`
  followed by `mv
  /Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof/20260812T084655Z/pristine/Python.framework
  /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework`.
- **If the operator aborts** at any point after the `mv` in Direction A step 2 and before the
  restore in Section 4, the recovery is that identical two-step, using the identical absolute
  paths above.
- **If the session directory has been destroyed**, the ONLY remaining recovery is `pnpm
  build-runners-onedir` (requires network access and upstream sources).

### Vendored tree re-verified unmodified at Task 1 end

Symlink count re-measured: **12**. Re-measured inventory diffs clean against
`<session>/evidence/baseline-symlinks.txt` (byte-identical, digest
`c28becd50ad1304668e1fedd9cafe9934abe90cb34443629328110b6aab6ae1a` on both). `git status
--porcelain` re-measured empty. No mutation was performed by this task.

---

## RECOVERY NOTE -- read before Direction A

If this run is stopped or interrupted at any point between the `mv` in Direction A step 2 and the
restore in Section 4, the repository is left with a **dereferenced** `Python.framework` staged at
`/Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework`
-- indistinguishable on disk from a real regression, and it WOULD package into a real build if
`pnpm dist:mac` were run again before restoring. The session directory created in precondition 6
holds the original, untouched `Python.framework` for exactly this reason. To recover from any
interrupted state:

```
rm -rf /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework
mv /Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof-<SESSION>/Python.framework \
   /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework
```

(substituting the actual session directory name recorded in precondition 6), then re-run the
`ls -la` RESTORE BASELINE check from precondition 2 and the `shasum` check from precondition 3
before doing anything else, including before starting Direction B.

---

## 2.5 CONTRACT AMENDMENT v2 -- 2026-08-23 (quick task 260823-seg) -- READ BEFORE ACTING

**Status: NORMATIVE. Where this section and Sections 3-5 below disagree, this section wins.**

The 2026-08-12 run (plan 34.9-21) opened three defects **in this contract's own prescribed
commands** -- `F-34.9-21-01`, `-02`, `-03` in the RUN RECORD below, carried as items 14, 15 and 16
in `deferred-items.md`. They were recorded there and never fixed here, so a future operator
following Sections 3-5 verbatim would reproduce all three. This section fixes two of them and
constrains the third.

**Sections 3-5 are deliberately NOT rewritten.** Roughly the back half of this file is the recorded
transcript of a real PASS run scored against the text as it stands; rewriting the instructions in
place would leave that verdict uninterpretable -- a reader could no longer tell which contract
produced it. Superseded steps carry an inline marker pointing here.

### A1 (item 14 / `F-34.9-21-01`) -- Direction A PASS bar (d) is REPLACED

**Why:** §3's (d) asserts that no `GameLib-*-macOS-*.dmg`/`.zip` in `dist/` has an mtime after
`BUILD_START`. On the FAILING direction that check cannot produce output either way --
`clean:dist-mac` runs first in the `dist:mac` `&&` chain and empties `dist/` unconditionally,
whether or not the guard later fires. It passed on 2026-08-12 **by construction, not by
discrimination**: the second self-satisfying assertion this contract has produced (the first being
`--publish=never` self-matching a bare `publish` grep, caught at 34.9-20 authoring time).

**v2 (d), replacing §3's (d) in full** -- this is the check the 2026-08-12 run substituted and
scored on, so it is validated, not newly invented:

> **d.** `verify:runner-bundle` is the **LAST** pnpm lifecycle banner in `direction-a.log`. Record
> the banner and its line number. Combined with (c)'s two absence greps
> (`electron-builder  version=` and `building        target=`, each `grep -c` = 0), this is what
> discriminates "the chain aborted at the guard" from "the chain aborted somewhere else" -- both
> are real discriminators; either would be >= 1 had electron-builder run.

**SCOPE FENCE -- do NOT generalise this to Direction B.** §5's PASS bar (d) is the same shape but
is **genuinely load-bearing**: on the passing direction `dist/` gains new artifacts only if the
build actually completes, so the mtime-after-`BUILD_START` framing discriminates exactly as
intended (and is the F-34.9-02 stale-artifact guard). `F-34.9-21-01` is scoped to Direction A
alone. Deleting §5's (d) "for symmetry" would remove a working check.

### A2 (item 15 / `F-34.9-21-02`) -- exit capture is shell-portable now

**Why:** the prescribed `... 2>&1 | tee -a log` + `${PIPESTATUS[0]}` idiom **silently no-ops under
zsh**, the operator's actual default shell on this hardware. It wrote a 1-byte (bare newline)
exit-status file with no error, which would have been scored as an empty/unscorable result had the
run not inspected it with `xxd` first (Block 2, discarded unscored; re-run as Block 2R).

**v2 form, replacing the piped idiom at all four sites** (§3 step 4, §3 PASS bar (a), §5 step 2,
§5 PASS bar (a)). It behaves identically under zsh and bash:

```sh
pnpm dist:mac > <SESSION_DIR>/direction-a.log 2>&1; echo $? > <SESSION_DIR>/direction-a.exit
```

**Mandatory companion rule -- this is where v2 interacts with precondition 7.** Precondition 7
requires `tee -a`, "never bare `tee`, never `>`", specifically so a re-run cannot **truncate** an
earlier block's evidence. The redirect form reintroduces exactly that risk unless each block writes
its **own filename**. So: **every block gets a distinct log/exit filename** (`direction-a.log`,
`direction-a-rerun.log`, `direction-b.log`, ...), never a reused one. Precondition 7's *intent*
(no evidence is ever overwritten) is preserved; only its prescribed mechanism changes. Two rules in
silent conflict is how the original defect survived authoring -- they are reconciled here in the
open.

Read the exit file back with `xxd` before scoring it (`310a` = "1\n", `300a` = "0\n"). A 1-byte
file is the failure signature this defect produces.

**The `cat -A` half of item 15 needs no edit and is NOT claimed as fixed.** Item 15 also reports
`cat -A` as GNU-only (macOS BSD `cat` has no `-A`). Verified live 2026-08-23: `cat -A` appears
**only** in the RUN RECORD's `F-34.9-21-02` finding row, and **nowhere in this contract's
prescribed commands** -- the body already uses `xxd` at both byte-dump sites. Recorded as
verified-absent rather than repaired; there was nothing here to repair.

### A3 (item 16 / `F-34.9-21-03`) -- §5's invocation is NORMATIVE, and the sub-claim is STILL OPEN

**This one is not a missing-instruction defect, and item 16 must not be read as one.** §5 step 2
**already** prescribes `pnpm dist:mac --arm64 --publish=never`, already states the args-passthrough
rationale, and PASS bar (c) already asserts an arm64 `target=` line plus `grep -c Uploading` = 0.
**The contract was correct.**

What actually happened: **plan 34.9-21's own `how-to-verify` restated the step as "Run the identical
`pnpm dist:mac`" -- dropping the args -- and the 2026-08-12 run followed the plan's restatement
rather than this contract** (Deviation 6). A lossy paraphrase outranked a correct record in
practice, because the paraphrase is what the executor actually read.

**v2 precedence rule, binding on every future plan that drives this contract:**

1. §5's invocation string is **NORMATIVE**. A plan's `<how-to-verify>` may **cite** it; it must not
   **paraphrase** it. Where the two disagree, this contract wins.
2. Any deliberate departure is recorded as a deviation **before** the run, not reconstructed after.
3. The next hardware run **MUST** discharge `F-34.9-21-03`: run §5 step 2 with its args as written,
   then record the arm64 `target=` line **verbatim** and `grep -c Uploading` = 0 in its run record,
   naming `F-34.9-21-03` as discharged.

~~**Until that run happens, item 16 stays OPEN.** This amendment closes only the contract-side half
(precedence); the args-passthrough sub-claim itself remains unproven and cannot be closed by
editing a document.~~

**DISCHARGED 2026-08-23** (quick task `260823-suw`, hours after this amendment was written). The
run happened: `pnpm dist:mac --arm64 --publish=never` on macOS arm64, **verdict PASS 4/4**, with
the resolved script string at log line 3 showing both args landing on `electron-builder` while
`verify:runner-bundle` kept its own `--arch=arm64`. 2 arm64 `target=` lines, 0 x64, 0 `Uploading`.
Full evidence: § "RUN RECORD -- 2026-08-23 (Direction B only)" at the end of this file; ledger item
16 closed. **The precedence rule in points 1-3 above remains binding** — it is what makes the next
run reproducible, and it is not retired by this one run having gone well.

## 3. Direction A -- FAILING (the injection)

1. `rm -rf /Users/graysonmitchell/Projects/GameLib/build/bin`. This runs FIRST, before anything
   else in this direction. **Why:** the renderer's `emptyOutDir: false` means a stale, well-formed
   tree can survive in `build/` across builds. If it does, the guard would inspect that stale good
   tree instead of the freshly-injected one, PASS vacuously, and the whole proof would report a
   false PASS while never actually exercising the injected defect.
2. `mv /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework /Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof-<SESSION>/Python.framework`,
   then
   `cp -RL /Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof-<SESSION>/Python.framework /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework`.
   **Why `cp -RL`:** this is what reproduces vite's own `copyDir` behaviour, the exact mechanism
   that produced F-34.9-01 in the first place -- `-L` dereferences symlinks into real files/dirs
   during the copy.
3. `ls -la /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework`
   and
   `ls -la /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework/Versions`
   MUST now show `Python` as a REAL FILE (not `l` in the mode bits) and `Versions/Current` as a
   REAL DIRECTORY (not a symlink). Record this. **A proof whose known-bad state was never asserted
   is vacuous** -- the injection must be proven present before the guard is asked to catch it.
4. Record a UTC `BUILD_START` (`date -u +%Y-%m-%dT%H:%M:%SZ`). Run, from the repo root
   (`/Users/graysonmitchell/Projects/GameLib`):
   `pnpm dist:mac 2>&1 | tee -a /Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof-<SESSION>/direction-a.log`
   > **SUPERSEDED BY AMENDMENT v2 §A2** -- this piped form silently no-ops under zsh. Use the
   > redirect form: `pnpm dist:mac > <SESSION_DIR>/direction-a.log 2>&1; echo $? > <SESSION_DIR>/direction-a.exit`,
   > with a distinct filename per block.

**PASS bar for Direction A, ALL four required:**

a. `pnpm dist:mac` exits NON-ZERO. Record the exact exit code
   (`echo "exit=$?"` immediately after the `tee` pipeline, or `${PIPESTATUS[0]}` under bash).
   > **SUPERSEDED BY AMENDMENT v2 §A2** -- read the exit code from `direction-a.exit` written by
   > the redirect form, and verify it with `xxd` before scoring (a 1-byte file is the zsh
   > `PIPESTATUS` failure signature, not an exit code).
b. The transcript (`direction-a.log`) contains the literal
   `Versions/Current exists but is not a symlink (F-34.9-01)` and a line matching the regex
   `gogdl: framework .* is malformed`. **Exit-code-alone is NOT sufficient** -- `clean:dist-mac` or
   `build-steam-bridge` failing for an unrelated reason would also exit non-zero, and this
   criterion is what discriminates WHICH step actually aborted the chain.
c. The transcript contains **no electron-builder output**: assert the absence of both
   `electron-builder  version=` and `building        target=` in `direction-a.log`
   (`grep -c` each against `direction-a.log` only, must be `0`), and record the last command that
   DID produce output in the transcript (expected: the `pnpm verify:runner-bundle` FAIL block).
d. > **SUPERSEDED IN FULL BY AMENDMENT v2 §A1** -- the check below is VACUOUS on this direction
   > (`clean:dist-mac` empties `dist/` first, whether or not the guard fires) and passed on
   > 2026-08-12 by construction, not by discrimination. **v2 (d):** assert `verify:runner-bundle`
   > is the LAST pnpm lifecycle banner in `direction-a.log`, and record its line number. The
   > superseded text is retained below only so the 2026-08-12 RUN RECORD stays interpretable
   > against the contract it was scored under. **Do not run it.**

   ~~**No** `GameLib-*-macOS-*.dmg` or `GameLib-*-macOS-*.zip` exists in
   `/Users/graysonmitchell/Projects/GameLib/dist/` with an mtime AFTER `BUILD_START` from step 4.~~
   State explicitly why this needs to be checked this way rather than simply "is `dist/` empty":
   `clean:dist-mac` runs FIRST in the `dist:mac` chain and always empties `dist/` regardless of
   whether the guard later fires -- an empty `dist/` on its own proves nothing about
   electron-builder. Criterion (c)'s absence-of-output plus this criterion's
   mtime-strictly-after-`BUILD_START` framing are what make the pair a real discriminator, not
   the emptiness alone.

## 4. Restore, and prove the restore

1. `rm -rf /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework`
   (removes the dereferenced copy).
2. `mv /Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof-<SESSION>/Python.framework /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/_internal/Python.framework`
   (restores the original from the session dir).
3. Re-run the RESTORE BASELINE `ls -la` checks from precondition 2 and the `shasum -a 256` check
   from precondition 3. Both MUST match the pre-injection baseline byte-for-byte / symlink-for-
   symlink.
4. **Why a clean `git status` is NOT acceptable evidence here:** `public/bin/**` is git-ignored
   (`public/bin/.gitignore:3`, `**/gogdl`), so git is structurally blind to this entire injection
   and restoration. The `ls -la` + `shasum` re-check is the only valid restore evidence.
5. **This is a hard gate.** The run does not proceed to Direction B, and does not end, until the
   baseline matches exactly. If it does not match, stop and follow the RECOVERY NOTE above before
   doing anything else.

## 5. Direction B -- PASSING (normal build, args forwarded)

1. `rm -rf /Users/graysonmitchell/Projects/GameLib/build/bin` again, for the identical reason
   given in Direction A step 1 -- a stale good tree from a prior build must not mask this
   direction's own measurement either.
2. Record a fresh UTC `BUILD_START`. Run, from the repo root
   (`/Users/graysonmitchell/Projects/GameLib`):
   `pnpm dist:mac --arm64 --publish=never 2>&1 | tee -a /Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof-<SESSION>/direction-b.log`.
   Using appended args here is deliberate: it proves in ONE run both that the guard does not
   obstruct a normal build AND that appended CLI args still reach `electron-builder` -- the exact
   shape `build-base.yml:48` uses (`pnpm dist:mac --x64 --arm64 --publish=never`).
   > **AMENDMENT v2 §A2 + §A3.** Capture: use
   > `pnpm dist:mac --arm64 --publish=never > <SESSION_DIR>/direction-b.log 2>&1; echo $? > <SESSION_DIR>/direction-b.exit`.
   > Invocation: **the argument string above is NORMATIVE.** The 2026-08-12 run dropped
   > `--arm64 --publish=never` because plan 34.9-21's `how-to-verify` paraphrased this step as "run
   > the identical `pnpm dist:mac`" (Deviation 6) -- which is why `F-34.9-21-03` is still open. A
   > plan may cite this line; it must not paraphrase it. The next run MUST run it as written and
   > discharge `F-34.9-21-03`.

**PASS bar for Direction B, ALL four required:**

a. Exit code 0 (`${PIPESTATUS[0]}` under bash, or check immediately after the `tee` pipeline).
   > **SUPERSEDED BY AMENDMENT v2 §A2** -- read it from `direction-b.exit`, verified with `xxd`
   > (`300a` = "0\n"). A 1-byte file means the capture failed, not that the build passed.
b. The transcript (`direction-b.log`) contains the guard's PASS line
   (`PASS: all three onedir runners present, executable and Mach-O; tree sizes above the floor.`)
   and it appears **BEFORE** the first electron-builder output line in the file -- the ordering
   proof that the guard runs pre-packaging, not after. Check with line numbers
   (`grep -n` for the PASS line and for `electron-builder  version=`, compare line numbers).
c. electron-builder output IS present in the transcript, and honored the appended args: an arm64
   `target=` line, and no publish attempt. Assert the absence of `Uploading` in the transcript
   (`grep -c Uploading direction-b.log` must be `0`) -- **do NOT** grep for the bare word
   `publish`: the invocation line itself echoes `--publish=never`, so a bare `publish` search
   self-matches even on a correct run and proves nothing (found and fixed at authoring time,
   Contract Validation subsection (a) below).
d. A `.dmg` AND a `.zip` matching `GameLib-*-macOS-arm64.*` exist in
   `/Users/graysonmitchell/Projects/GameLib/dist/` with mtime STRICTLY AFTER this direction's own
   `BUILD_START` (step 2).
   > **NOT superseded -- deliberately retained. See AMENDMENT v2 §A1's scope fence.** This check
   > shares a shape with §3's retired (d) but is the opposite case: on the PASSING direction `dist/`
   > gains artifacts only if the build actually completes, so mtime-after-`BUILD_START` is a real
   > discriminator here and is also the F-34.9-02 stale-artifact guard. `F-34.9-21-01` is scoped to
   > Direction A alone. **Do not retire this one for symmetry.** **"A dmg exists" alone is never the criterion (F-34.9-02)** -- the
   mtime-after-`BUILD_START` framing is what makes it a real discriminator against a stale
   artifact left over from an earlier, unrelated build.

## 6. Verdict rule

**Both directions must pass.** A failing Direction A is the more serious outcome -- it means the
guard does not fire against a real defect -- and routes to another gap cycle. A failing Direction B
means the wiring broke a normal build and must be reverted before anything ships. Direction A alone
passing is not sufficient: a guard that fails every build (including a well-formed one) is not a
functioning guard either -- Direction B exists specifically to rule that out.

## 7. What this does NOT prove

State each explicitly as UNPROVEN. None of the following is asserted by this contract:

- **The packaged `.app`'s own shape.** The guard inspects the STAGED `build/` tree only, never the
  packaged artifact electron-builder produces from it.
- **The x64 leg.** Coverage is arm64-only; `release:mac` also builds x64, and that leg is
  unverified by this step (`build/bin/x64/darwin` holds onefile binaries and would fail the
  file-count floor).
- **CI.** The macOS CI leg cannot reach the build today -- `install-deps` runs
  `pnpm download-helper-binaries`, which throws on the six `PENDING-CI-PUBLISH` sentinels in
  `meta/runnersOnedirDigests.json` (descoped REQ-34.9-02/03/04). This wiring is
  never-exercised-in-CI.
- **`release:mac` itself.** Only `dist:mac` is exercised by this contract; `dist:mac` and
  `release:mac` share the identical inserted step by construction (Task 1's edit applied the same
  literal segment to both), which is an ARGUMENT for `release:mac` behaving identically, not a
  measurement of it.
- **Windows and Linux.** Unchanged by this phase, not exercised by any direction here.
- **Real-certificate signing and notarization.** D-03/D-04 deliberately defer Apple Developer
  Program enrollment; this repo has never exercised real signing.
- **The `closeBundle` throw from plan 34.9-18.** Its failing direction is proven at UNIT level
  only (`meta/__tests__/preserveRunnerSymlinks.test.ts`) and is not reachable from a real build
  today -- this contract's Direction A exercises `verifyRunnerBundle`'s guard, not
  `closeBundle`'s own throw path.

The arm64-only and never-in-CI limits above are `deferred-items.md` items 12 and 13, owned by plan
**34.9-22**. This contract states them; it does not record ledger entries for them.

## 8. Sink partition and redaction discipline

This proof touches build transcripts only -- no app launch, no login, so no `gamelib.log` and no
per-runner logs are involved, and there is no PKCE or credential surface. The project's standing
redaction rule is reproduced here anyway, and binds only on incidental paths:

No cookie value, gamekey, OAuth code, account id, or PKCE material (`code_verifier`, `serial`,
`client_id`) is ever transcribed into this document or any captured artifact -- timestamps,
launch/direction ordinals, and file paths only. Every filesystem path this contract asks the
operator to type, paste, or run is written ABSOLUTE, rooted at
`/Users/graysonmitchell/Projects/GameLib`, per the standing rule (Deviation 4,
`34.9-LIVE-GATE-RERUN.md` Section 5) -- repo-relative paths appear ONLY inside shell commands
explicitly labelled "run from the repo root" above (`pnpm dist:mac`, `pnpm dist:mac --arm64
--publish=never`), each stated at the point it is given.

Both directions' `Observed:` fields, filled by plan 34.9-21 from measured evidence:

- Direction A -- **Observed: PASS (of Guard B's failing direction).** Exit code `1`
  (`fail-direction.exit`, read byte-for-byte via `xxd` as `310a`). The F-34.9-01 literal
  `Versions/Current exists but is not a symlink (F-34.9-01)` present, count 2, in
  `fail-direction-rerun.log` (one on the outer `gogdl/_internal/Python.framework`, one on a nested
  `Python.framework/Python.framework` -- see Deviations below). Guard A's zero-skip line present at
  line 171: `[preserve-runner-symlinks] restored 9 symlink(s), skipped 0, rejected 0` -- proving
  Guard A did not fire; the abort is Guard B's (`verifyRunnerBundle`). No electron-builder banner
  anywhere in a 469-line log (`electron-builder  version=` count 0, `building        target=` count
  0); `verify:runner-bundle` is the terminal pnpm lifecycle step, at line 173 of 469. The contract's
  own `find -newer` dist/-emptiness check is VACUOUS in this direction (see Finding
  `F-34.9-21-01`) and is explicitly NOT scored; the terminal-lifecycle-step evidence above is the
  substitute. **First attempt (Block 2) was UNSCORABLE**, not scored as a pass or fail: under the
  operator's actual default shell (zsh), the contract's `${PIPESTATUS[0]}` idiom silently expanded
  to empty, writing a 1-byte (bare newline) `fail-direction.exit`. This was caught before being
  treated as evidence and the direction was cleanly re-run (Block 2R) using a shell-portable
  redirect form; see Finding `F-34.9-21-02` and Deviations.
- Direction B -- **Observed: PASS.** Exit code `0` (`pass-direction.exit` = `"0\n"`, `xxd` `300a`).
  `dist/` held before the run held only `builder-debug.yml` (direction A's own `clean:dist-mac` had
  already emptied it), so `find -newer PASS_START.marker \( -name '*.dmg' -o -name '*.zip' \)`
  returning `GameLib-0.7.0-macOS-arm64.zip` and `GameLib-0.7.0-macOS-arm64.dmg` is a genuine,
  provable-to-fail discriminator in this direction (unlike its vacuous counterpart in Direction A --
  Finding `F-34.9-21-01` is scoped to Direction A only). Guard A's line at 171:
  `[preserve-runner-symlinks] restored 12 symlink(s), skipped 0, rejected 0`. `verify:runner-bundle`'s
  PASS line (`PASS: all three onedir runners present, executable and Mach-O; tree sizes above the
  floor.`) at line 457, immediately followed by electron-builder's first banner
  (`electron-builder  version=26.8.1 os=25.5.0`) at line 458 -- the guard runs immediately before
  packaging, not merely somewhere in the log. `'Uploading'` count 0 (no publish attempt). **This is
  the strongest single piece of evidence in the run**: the same command, at the same position in the
  `&&` chain, on the same machine, produced `verify:runner-bundle` as the terminal step with zero
  electron-builder banners in Direction A, and `verify:runner-bundle`'s PASS immediately followed by
  electron-builder's first banner in Direction B -- electron-builder's execution flips in lockstep
  with the guard's verdict. That is a direct demonstration that the guard *gates* electron-builder,
  which is exactly the claim CR-01 said was unproven ("a guard that nothing invokes is not a
  guard"). **Deviation, honestly stated:** this run executed the bare `pnpm dist:mac` specified by
  plan 34.9-21's own Task 2 (`how-to-verify` Step 4.3: "Run the identical `pnpm dist:mac`"), which
  supersedes this contract's own Section 5 wording (`pnpm dist:mac --arm64 --publish=never`). The
  CLI-argument-passthrough sub-claim this contract's Section 5 was also designed to prove ("appended
  CLI args still reach electron-builder... the exact shape `build-base.yml:48` uses") was **not**
  exercised by this run -- see Finding `F-34.9-21-03`. The core claim under test (the guard gates
  electron-builder and does not obstruct a normal build) remains validly proven by the evidence
  above.

**Guard A's own failing direction remains unobserved at build level, in both directions of this
run** (`skipped 0, rejected 0` in both). Plan 34.9-18's SUMMARY already recorded this as unit-level
proof only, since the vendored darwin trees are git-ignored and the throw's triggering condition
(a symlink that fails to restore during `electron-vite build`) is unreachable from a real build
today. This run's injected defect -- a framework already dereferenced in `public/` before the build
starts -- exercises Guard B, not Guard A, and this record must not be read as having closed that
gap.

---

## Contract Validation

Performed by plan 34.9-20 at authoring time, per
`.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md`, before this
contract's first live run.

### (a) Specimen validation of every verification command

Two synthetic transcripts were constructed under the scratchpad (never in the repo):
`direction-a-specimen.log` (the failure literal, the `gogdl: framework .* is malformed` line, NO
electron-builder output) and `direction-b-specimen.log` (the guard's PASS line followed by
electron-builder output, including an arm64 `target=` line and no `Uploading` line). A third,
`direction-b-bad-publish-specimen.log`, was added specifically to give the "no publish attempt"
criterion a genuine presence case (see the fix below). Every assertion below was run against BOTH
of the primary two specimens and confirmed to match its own intended specimen and NOT match the
other; the publish-absence assertion was additionally run against the third specimen.

| Assertion | Scope | Matches A (bad tree) | Matches B (good tree) | Verdict |
|---|---|---|---|---|
| `grep -q 'Versions/Current exists but is not a symlink (F-34.9-01)'` | `direction-a.log` | YES | NO | DISCRIMINATES |
| `grep -qE 'gogdl: framework .* is malformed'` | `direction-a.log` | YES | NO | DISCRIMINATES |
| `grep -c 'electron-builder  version='` == 0 | `direction-a.log` ONLY | count 0 (pass) | n/a (not run against B) | DISCRIMINATES, see scoping note below |
| `grep -c 'building        target='` == 0 | `direction-a.log` ONLY | count 0 (pass) | n/a (not run against B) | DISCRIMINATES, see scoping note below |
| `grep -qF` the PASS line | `direction-b.log` | NO | YES | DISCRIMINATES |
| `grep -n` PASS line vs. `grep -n electron-builder  version=`, compare line numbers | `direction-b.log` | n/a (A has neither) | PASS at line 26, electron-builder at line 28 -- 26 < 28 | DISCRIMINATES, ordering confirmed on the specimen |
| `grep -c Uploading` == 0 | `direction-b.log` | n/a | count 0 (correct build) | DISCRIMINATES against the bad-publish specimen (count 1) -- see fix below |
| `grep -c 'target=DMG arch=arm64'` >= 1 | `direction-b.log` | count 0 | count 1 | DISCRIMINATES |
| exit-code checks (Direction A non-zero, Direction B zero) | process exit status, not a grep | n/a | n/a | Not specimen-testable via a text file; validated by construction (`${PIPESTATUS[0]}` immediately after the `tee` pipeline is the standard idiom this project already uses in `34.9-LIVE-GATE-RERUN.md`) |
| `find dist/ -newer <BUILD_START-marker-file>` mtime checks (Direction A: none exist; Direction B: both exist) | filesystem, not a grep | n/a | n/a | Not specimen-testable via a text file; validated logically -- this is the exact F-34.9-02 "a dmg exists is never the criterion" shape, already the reason the contract requires mtime STRICTLY AFTER `BUILD_START` rather than mere existence |
| `ls -la` restore-baseline re-check; `shasum -a 256` re-check | filesystem, not a grep | n/a | n/a | Not specimen-testable via a text file; the baseline values this authoring plan recorded (Precondition 2/3) were themselves taken from a live `ls -la` / would-be `shasum` against the actual repo state at authoring time, confirming the commands are well-formed and point at real, existing paths |

**Three specific self-satisfying risks, checked as instructed:**

1. **The absence assertion for `electron-builder  version=` / `building        target=`.** Confirmed
   the contract's OWN prose contains both literals verbatim (`grep -c 'electron-builder  version='
   34.9-GUARD-PROOF.md` returns `2`; `grep -c 'building        target=' 34.9-GUARD-PROOF.md` returns
   `1`) -- so running either assertion against the CONTRACT FILE ITSELF would self-match and prove
   nothing. The contract's own wording already scopes both assertions to `direction-a.log`
   specifically ("`grep -c` each against `direction-a.log` only, must be `0`"), never to the
   contract document. This scoping is what prevents the self-match; it was verified present in the
   contract's own Direction A criterion (c) wording, not merely assumed.
2. **`grep -c` over a file containing the contract's own explanatory text.** Same mitigation as
   above -- every count-based assertion in this contract names the transcript file
   (`direction-a.log` / `direction-b.log`) as its explicit scope, never a bare unscoped grep over
   "the output" or "the repo".
3. **The failure literal's byte-identity to its emitter.** Confirmed by direct comparison against
   `meta/verifyRunnerBundle.ts:447` (`Versions/Current exists but is not a symlink (F-34.9-01) --
   ` ...) and `meta/verifyRunnerBundle.ts:559` (`'PASS: all three onedir runners present,
   executable and Mach-O; ' + 'tree sizes above the floor.'`) -- both confirmed present verbatim in
   source via `grep -n`, and additionally cross-checked against this authoring session's own ONE
   permitted read-only run, whose outcome is recorded in this plan's SUMMARY only, never in this
   contract, per Task 3's instruction.

**Defect found and fixed:** Direction B criterion (c) originally read "no publish attempt (no
`publish` step reaching GitHub)" with no exact assertion given. Testing the shape against a
specimen surfaced that the obvious literal grep (`grep -c publish`) is vacuous: `dist:mac`'s own
invocation line echoes `--publish=never`, so a bare `publish` search returns a non-zero count even
on a CORRECT run (confirmed: count 1 on the good `direction-b-specimen.log`) -- an assertion that
cannot fail against the good case is not a real gate. **Before:** "no `publish` step reaching
GitHub" (unscoped, self-matching). **After:** `grep -c Uploading direction-b.log` must be `0`,
which correctly returns `0` on the good specimen and `1` on a constructed bad specimen carrying a
synthetic `Uploading  file=... provider=github` line -- proven to discriminate in both directions.
The contract's Direction B criterion (c) has been updated to this fixed wording.

**The one bounded read-only exception permitted by Task 3** (`pnpm verify:runner-bundle build
--arch=arm64`, run once, read-only, against a pre-existing local tree) was exercised during this
plan's authoring. Its outcome is deliberately NOT recorded in this contract -- Task 3 requires it
go in the plan's SUMMARY only, so this document stays exactly what it claims to be: unrun.

### (b) Pairwise interaction review

**Reduction applied:** the state-mutating requirements in this contract are {`rm -rf build/bin`
(both directions), the `mv` of `Python.framework` out of `public/`, the `cp -RL` back, `pnpm
clean:dist-mac`'s emptying of `dist/` (inside both `pnpm dist:mac` invocations), `electron-vite
build`'s rewrite of `build/` from `public/` (inside both invocations), and the restore `mv`}; the
evidence-bearing requirements are {Direction A's four PASS-bar criteria, Direction B's four
PASS-bar criteria, and the restore-baseline `ls -la`/`shasum` re-check}. This is a contract with 6
state-mutating requirements and 9 evidence-bearing requirements; per the M×N reduction, every
state-mutating requirement was considered against every evidence-bearing requirement it could
plausibly touch, not the full cross product. **Pairs considered: 7. Flagged as real hazards
requiring resolution: 5. Checked and found not to apply: 2.** (All five hazards are already
resolved in-document; restated here so "found and fixed" stays distinguishable from "did not
look".)

| Pair (state-mutating -> evidence-bearing) | Does A destroy B's evidence? |
|---|---|
| `clean:dist-mac` empties `dist/` -> Direction A criterion (d) "no new dmg/zip" | **YES if trusted alone -- RESOLVED** by criterion (d) never treating an empty `dist/` as its own evidence; the discrimination is carried by criterion (c)'s absence-of-electron-builder-output plus criterion (d)'s mtime-after-`BUILD_START` framing, not by emptiness. Stated explicitly in Section 3 step (d)'s own text. |
| `rm -rf build/bin` omitted -> `emptyOutDir: false` masking the injection | **YES -- RESOLVED** by making `rm -rf build/bin` the mandatory FIRST step of both Direction A and Direction B (Sections 3 and 5), with the masking mechanism stated inline both times. |
| Injection lives in `public/` (git-ignored) -> any "clean working tree" precondition | **YES if a clean `git status` were accepted as restore evidence -- RESOLVED** by Section 4 explicitly rejecting `git status` as evidence and requiring the structural `ls -la` + `shasum -a 256` re-check instead. |
| Direction A aborts mid-`dist:mac` -> Direction B's starting state | **YES if Direction B's own `rm -rf build/bin` were optional -- RESOLVED** by making it mandatory and unconditional at the start of Section 5, independent of whether Direction A's own `build/` was left partially written. |
| `mv` to the session dir -> an aborted or interrupted run | **YES -- RESOLVED** by the RECOVERY NOTE placed immediately before Direction A's first step (verified at line 93, before Direction A's own heading at line 115), naming the session-dir path pattern and the exact restore command. |
| `--arm64 --publish=never` appended in Direction B -> `dist:mac`'s leading `export CSC_IDENTITY_AUTO_DISCOVERY=false` | **NO, confirmed structurally** -- Task 1's own structural assertion (`/&& electron-builder [^&]*$/` matching both scripts) proves `electron-builder` is the last `&&` segment in both `dist:mac` and `release:mac`, so pnpm's argument-append lands on `electron-builder`, never on the leading `export`. Re-confirmed here by re-reading Task 1's committed diff. |
| `electron-vite build` rewriting `build/` from `public/` (both directions) -> the OTHER direction's own evidence, via a drifted `public/bin` tree between directions | **NO, already prevented** -- Section 4's hard-gate restore-and-re-verify means Direction B cannot begin until the `ls -la`/`shasum` baseline re-check in Section 4 has already passed, so `public/bin/arm64/darwin` is provably identical to its pre-Direction-A state before `electron-vite build` ever runs again in Direction B. |

### (c) Authoring self-check

- **Commands this plan did NOT run:** `pnpm dist:mac`, `pnpm release:mac`, `electron-vite build`,
  `rm -rf build/bin`, any `mv`/`cp -RL` against any `Python.framework`, and no injection or restore
  step against `public/bin/arm64/darwin/gogdl/_internal/Python.framework` (confirmed unchanged
  throughout -- `test -L public/bin/arm64/darwin/gogdl/_internal/Python.framework/Python` succeeds
  before and after this plan's work).
- **The one read-only command it DID run:** `pnpm verify:runner-bundle build --arch=arm64`, once,
  from the repo root, against this authoring session's own already-staged
  `build/bin/arm64/darwin` tree (left over from a prior local build, unrelated to this plan's own
  work). Outcome: exit 0, a runner table (legendary 109 files/104 Mach-O, gogdl 67 files/62
  Mach-O, nile 108 files/103 Mach-O), and the PASS line
  `PASS: all three onedir runners present, executable and Mach-O; tree sizes above the floor.` --
  byte-identical to the literal quoted throughout this contract. Read-only (`verifyRunnerBundle`
  only reads); no build, injection, or mutation of any kind was performed to obtain it. This
  outcome confirms argv plumbing (`build` parsed as root, `arm64` as arch, surviving pnpm's
  arg-append through the `| node -` idiom) and is NOT written into this contract's own
  `verdict`/`run_date`/`Observed:` fields, and does not count toward either direction's PASS bar
  -- it is argv-plumbing confirmation only, not a gate result.
- **No `verdict` / `run_date` / `directions_passed` / `directions_failed` / `Observed:` value was
  written** by this plan, other than the frontmatter's own explicit unfilled states
  (`verdict: PENDING`, `run_date: null`, `directions_passed: null`, `directions_failed: null`) and
  the two per-direction unfilled `Observed:` markers in Section 8, each reading identically.
- **`git show --stat` for this plan's commits lists exactly three files across the plan's three
  task commits:** `package.json`, `electron-builder.yml`, and this document
  (`34.9-GUARD-PROOF.md`) -- no fourth file, confirmed per-commit as each task was committed.

**This plan ran no direction of the proof contract, injected no defect, and wrote no result
field.**

---

# RUN RECORD -- 2026-08-12

Run by plan 34.9-21. Operator: human, on real macOS arm64 hardware (`Darwin ... arm64`, confirmed
at `git log --oneline -5` showing the tip carrying plans 34.9-18/19/20). Session directory:
`/Users/graysonmitchell/Projects/GameLib/node_modules/.cache/gamelib-guard-proof/20260812T084655Z`.

## Verdict: PASS

Both directions observed as specified, from disk evidence, never from a mutating command's own
report. The restore audit matched the Task 1 baseline on all five measures, independently, twice.
No partial pass; no softened FAIL. CR-01 is closed and `34.9-VERIFICATION.md` truth 8 is
satisfied, **scoped strictly to arm64, local, non-CI, as stated throughout this record.**

**Headline finding:** Direction A left `verify:runner-bundle` as the terminal pnpm lifecycle step
(line 173 of 469) with zero electron-builder banners anywhere in the transcript. Direction B, same
command, same position in the `&&` chain, same machine, produced `verify:runner-bundle`'s PASS line
immediately followed by electron-builder's first banner one line later (line 457 -> 458).
electron-builder's execution flips in lockstep with the guard's verdict. This is a direct
demonstration that the guard **gates** electron-builder, not merely runs somewhere near it in the
log -- the exact claim CR-01 said had never been observed.

## Per-direction outcome table

| Direction | Command | Exit | Key evidence | Result |
|---|---|---|---|---|
| A (failing, re-run as Block 2R after Block 2 was UNSCORABLE) | `pnpm dist:mac > fail-direction-rerun.log 2>&1` | `1` | F-34.9-01 literal x2; Guard A `restored 9, skipped 0, rejected 0`; zero electron-builder banners; `verify:runner-bundle` terminal at line 173/469 | **PASS** (of Guard B's failing direction) |
| B (passing) | `pnpm dist:mac > pass-direction.log 2>&1` | `0` | Guard A `restored 12, skipped 0, rejected 0`; `verify:runner-bundle` PASS at 457, electron-builder banner at 458; `.dmg`+`.zip` newer than `PASS_START.marker`; zero `Uploading` | **PASS** |

Direction A's first attempt (Block 2, piped through `tee` with `${PIPESTATUS[0]}` under the
operator's actual zsh shell) produced a 1-byte `fail-direction.exit` (a bare newline) because zsh
does not populate bash's `PIPESTATUS` array the same way. This was caught before being read as
evidence -- it was never scored as either a pass or a fail -- and Direction A was cleanly re-run as
Block 2R using a shell-portable redirect (`pnpm dist:mac > file 2>&1; echo $? > exit-file`), which
behaves identically under zsh and bash. All Direction A evidence in this record is drawn from
Block 2R's `fail-direction-rerun.log` / `fail-direction.exit` only, never from the cumulative,
now-thrice-written `transcript.log`.

## Restore audit (performed twice, independently)

**In-flight (Task 2, Block 4), read from the operator's own paste:** symlink inventory diff empty,
count 12; regular-file-count diff empty; both `du -sk` diffs empty; `shasum -a 256` diff empty
across all three runner binaries; `git status --porcelain` empty; `pristine/` drained to empty.

**Independent (Task 3, this task, recomputed from the live tree, not cited from Block 4):**

| Measure | Task 1 baseline | Task 3 re-measurement | Match |
|---|---|---|---|
| Symlink inventory (paths + targets) | 12 entries, digest `c28becd5...` | 12 entries, digest `c28becd50ad1304668e1fedd9cafe9934abe90cb34443629328110b6aab6ae1a` | YES, byte-identical |
| Regular-file count | 290 | 290 | YES |
| `du -sk` darwin root | 99252 KB | 99252 KB | YES |
| `du -sk` gogdl framework | 5332 KB | 5332 KB | YES |
| `shasum -a 256` (legendary/gogdl/nile) | `0287ae10...`/`3a642485...`/`99e3529a...` | `0287ae10...`/`3a642485...`/`99e3529a...` | YES, all three |
| `git status --porcelain` | empty | empty | YES |
| Nested `Python.framework/Python.framework` (Direction A's `cp -RL` artifact) | absent (pre-run) | absent (`find -mindepth 1 -name 'Python.framework'` returned nothing) | YES, artifact fully gone |

**T-34.9P-02 discharged by measurement**, twice, independently -- once by the operator's in-flight
diff, once by this task's own recomputation from the live tree, neither citing the other's result.
The vendored tree is byte-for-byte back to its pre-run shape. No recovery via `pnpm
build-runners-onedir` was needed.

## Findings

| ID | Severity | Summary |
|---|---|---|
| F-34.9-21-01 | Contract defect (methodology) | Direction A's `find -newer` dist/-emptiness check in this contract's Section 3 step (d) is vacuous by construction: `clean:dist-mac` runs first in the `pnpm dist:mac` `&&` chain and empties `dist/` down to one non-macOS survivor (`builder-debug.yml`) regardless of whether the guard later fires. It cannot produce output either way and passed by construction, not by discrimination -- the second self-satisfying assertion this contract has produced (the first was `--publish=never` self-matching a bare `publish` grep, caught and fixed at 34.9-20's authoring time). **Not present in Direction B**, where the same check IS load-bearing (dist/ genuinely gains new artifacts only if the build completes) -- this finding is scoped to Direction A only. **Scored on:** the two electron-builder-banner-absence greps plus the terminal-pnpm-lifecycle-step check (both measured, both real discriminators) substituted for the vacuous check in this run's scoring. |
| F-34.9-21-02 | Contract defect (shell portability) | This contract's prescribed exit-capture idiom, `pnpm dist:mac 2>&1 \| tee -a log; echo ${PIPESTATUS[0]}`, silently no-ops under zsh (zsh does not populate bash's `PIPESTATUS` the same way), producing an empty/1-byte exit-status file with no error. The operator's actual default shell on this hardware is zsh. Separately, this contract's restore-verification suggestion of `cat -A` is GNU-only; macOS `cat` has no `-A` flag (`cat -e` or `xxd` work). Both are defects in the contract's prescribed commands, not operator error. |
| F-34.9-21-03 | Coverage gap | This contract's Section 5 originally specified Direction B as `pnpm dist:mac --arm64 --publish=never`, deliberately proving that appended CLI arguments still reach electron-builder (the exact invocation shape `build-base.yml:48` uses). Plan 34.9-21's own Task 2 `how-to-verify` superseded this with a bare `pnpm dist:mac` ("Run the identical `pnpm dist:mac`"), which is what this run executed. The CLI-argument-passthrough sub-claim was therefore **not exercised** by this run. The core claim (guard gates electron-builder, does not obstruct a normal build) remains validly proven independent of this gap. |

Ledger rows opened in `deferred-items.md`, numbered from item 14 (34.9-22 claims 11-13), same
commit as this record: items 14, 15, 16 -- one per finding above, in order.

## Deviations from the proof procedure

1. **Task 1 preflight STOP, then resolved, before any mutation.** `pgrep -f
   'target/debug/gamelib-shell'` found a live process (PID 32883) during Task 1 preflight -- a
   precondition this plan added beyond the contract's own numbered list, to guard against a running
   dev instance holding file handles into the vendored tree during mutation. Task 1 halted per its
   own instruction ("If ANY precondition fails, STOP and record why"), recorded why, and returned a
   checkpoint. The operator quit the instance; both `pgrep` probes were re-confirmed empty and the
   vendored tree was independently re-verified unmodified (digest match) before Task 2 began. No
   mutation occurred during the stop.
2. **Block 1 ran under zsh, not bash.** Block 1 uses no `PIPESTATUS`-dependent construct, so its
   evidence (the known-bad assertions, `FAIL_START` timestamp, pre-run `dist/` listing) is
   unaffected and was not re-taken.
3. **Direction A's first attempt (Block 2) was UNSCORABLE and discarded before being read as
   evidence** -- see Finding `F-34.9-21-02` and the per-direction table above. Re-run cleanly as
   Block 2R with a shell-portable redirect. This is the run's most consequential deviation: it is
   exactly the "never accept a mutating command's own report, or an unverified side-channel, as
   proof" discipline this phase has now needed at a fourth layer (build banners, restore claims,
   exit-status capture mechanics).
4. **Stray paste artifacts** (`zsh: command not found: e`) appeared twice in the operator's terminal
   during early blocks -- confirmed to be paste-character noise, not command failures; both affected
   commands were immediately re-run correctly and the values in this record are from the correct
   re-runs.
5. **Nested `Python.framework/Python.framework`, an unanticipated but correct extra observation.**
   `cp -RL`, materializing the framework's own internal self-referencing symlinks (`Versions/Current
   -> Versions/3.14`, and the framework's back-references), produced a real nested copy of the
   framework inside itself. This is why Direction A's transcript shows 4 F-34.9-01-class rejections
   (2 wordings x 2 framework instances, outer and nested) rather than the 2 this contract's authored
   text anticipated. It is extra malformation the guard also correctly caught, not a miss and not a
   defect in the guard -- confirmed fully gone from the live tree after restore (Task 3's
   independent audit above). A future re-run of this contract should expect this nested duplication
   as inherent to `cp -RL` on this specific framework shape, not treat 4 rejections as a regression
   from 2.
   > **AMENDED 2026-08-23 -- DID NOT REPRODUCE. Expect 2, not 4.** The 2026-08-23 Direction A run
   > produced **no** nested `Python.framework` and scored **2** rejections. This framework holds
   > exactly three symlinks (`Python`, `Resources`, `Versions/Current`), none resolving to the
   > framework root or an ancestor, so `cp -RL` has nothing to recurse into; and the tree is
   > unchanged since `Aug 7 09:57`, so the shape did not change between runs. The "2 wordings" half
   > of this deviation IS confirmed -- both fired. Only the "2 framework instances" half failed to
   > reproduce. Full detail: "RUN RECORD -- 2026-08-23 (Direction A)" at the end of this file.
   > *(This annotation is the sole edit made to the 2026-08-12 record. It adds no claim and changes
   > no verdict -- it exists because the sentence above is an instruction to future readers that is
   > now known to be wrong, and a reader arriving here would otherwise act on it.)*
6. **Direction B ran without the contract's originally-specified `--arm64 --publish=never`
   arguments** -- see Finding `F-34.9-21-03`.
7. **Some Block 6 (Direction B verification) and one Task-3-adjacent read-only measurement
   (`fail-direction-rerun.log`'s Guard A line count) were executed by the orchestrator directly
   against files already written to disk, rather than round-tripped through the operator as a
   paste**, since they were read-only greps over evidence already captured. The operator drove every
   state-mutating step (Blocks 1, 2R, 4, 5) personally; only already-written, read-only evidence was
   read by the orchestrator.
8. WR-02 (34.9-19's absent/wrong-type-stub closure) is directly corroborated as reachable from the
   real build command: both the pre-existing `Versions/Current` branch and 34.9-19's new
   absent/wrong-type stub branch fired in Direction A's transcript, on both the outer and nested
   framework instances -- not merely from `verifyRunnerBundle.test.ts`'s unit suite.

## What this run did not prove

This run is **arm64-only**. It proves nothing about:

- **The x64 leg.** Never touched by any direction of this run; `release:mac`'s x64 build is
  unexercised.
- **CI.** The macOS CI leg cannot reach this build at all today -- `install-deps` runs `pnpm
  download-helper-binaries`, which throws by design on six `PENDING-CI-PUBLISH` sentinels in
  `meta/runnersOnedirDigests.json` (descoped REQ-34.9-02/03/04). This wiring remains
  never-exercised-in-CI.
- **Guard A's own failing direction at build level.** `closeBundle`'s throw-on-skipped-or-rejected-
  restore (plan 34.9-18) is proven at unit level only
  (`meta/__tests__/preserveRunnerSymlinks.test.ts`). Both directions of this run show Guard A's
  counters at `skipped 0, rejected 0` -- it neither fired nor had cause to, because this run's
  injected defect is a framework already dereferenced in `public/` before the build starts, not a
  symlink that fails to restore during `electron-vite build`. This gap is unchanged by this run.
- **Tauri-PACKAGED resolution**, real-certificate signing, or notarization -- all pre-existing,
  out-of-scope items restated in `deferred-items.md`, not touched by this run.
- **Any cold-spawn ratio.** Unrelated to this contract; see `deferred-items.md` item 4/10.
- **The CLI-argument-passthrough sub-claim** originally in this contract's Section 5 (`--arm64
  --publish=never`) -- see Finding `F-34.9-21-03`.
- **`release:mac` itself.** Only `dist:mac` was exercised. `dist:mac` and `release:mac` share the
  identical inserted `verify:runner-bundle` step by construction (34.9-20's Task 1 diff applied the
  same literal segment to both) -- an argument for `release:mac` behaving identically, not a
  measurement of it.
- **Windows and Linux.** Unchanged by this phase, not exercised by any direction here.

## Pre-commit identifier audit (this task)

Searched the full diff of `34.9-GUARD-PROOF.md` and `deferred-items.md` for `code_verifier`,
`client_id`, `serial`, cookie values, account ids and gamekeys before `git add`. All hits are the
standing redaction-discipline PROSE (field names quoted as names, in sentences describing the
rule itself) already present in the document before this run; no value of any of these fields
appears anywhere in this record. No `gamelib.log` or per-runner log was touched by this proof --
build transcripts only.

---

# RUN RECORD -- 2026-08-23 (Direction B only, quick task 260823-suw)

## Purpose and scope

**Single purpose: discharge `F-34.9-21-03` / ledger item 16** -- the CLI-argument-passthrough
sub-claim the 2026-08-12 run never exercised, because plan 34.9-21's `how-to-verify` paraphrased
§5 step 2 as "run the identical `pnpm dist:mac`" and dropped the arguments (Deviation 6).

**Direction B ONLY in THIS record. Direction A was run separately later the same day** — see
"RUN RECORD -- 2026-08-23 (Direction A, quick task 260823-t8i)" at the end of this file, and read
its "Ordering deviation" section before treating the two as one contract execution. As authored,
this record's scope note read as follows and remains accurate for this record alone:
**Direction A was not run as part of it.** Item 16's precondition is about
appended args reaching `electron-builder`; Direction A would require re-injecting a dereferenced
`Python.framework` into the vendored tree, which this discharge does not need and which carries
real risk to that tree. **This run therefore re-proves nothing about Direction A**, and the
2026-08-12 verdict remains the sole evidence for the failing direction.

Run under **CONTRACT AMENDMENT v2**: §A2's redirect capture form, §A3's normative invocation.

## Preconditions (recorded before any action)

- `pgrep -f 'target/debug/gamelib-shell'` -- empty. No dev instance holding file handles.
- No `electron`/`electron-builder`/`vite` process running.
- `dist/` contained only `builder-debug.yml` -- the expected non-macOS survivor, nothing to lose.
- `rm -rf build/bin` executed (§5 step 1), so no stale good tree could mask the measurement.
- Pre-run vendored snapshot: legendary 109 files, gogdl 67, nile 108; `Versions/Current` -> `3.14`
  in all three; full arm64/darwin symlink manifest sha256
  `573f5ed313c943eb095fdb2d0df25c4975124bbd425fa0e75670e6e93e900f72`.
- `BUILD_START` = `2026-08-23T08:47:04Z` (UTC).

## Invocation, verbatim

```
pnpm dist:mac --arm64 --publish=never > <SESSION_DIR>/direction-b.log 2>&1; echo $? > <SESSION_DIR>/direction-b.exit
```

Session dir: `node_modules/.cache/gamelib-guard-proof-20260823T084704Z/`. Log 484 lines.

## Verdict: PASS 4/4 -- `F-34.9-21-03` DISCHARGED

### (a) exit code

`direction-b.exit` read as bytes via `xxd -p`: **`300a`** = `"0\n"`. Exit **0**. The byte-level
read is the v2 §A2 requirement -- a 1-byte file is the zsh-`PIPESTATUS` capture-failure signature,
and reading the value alone cannot distinguish the two.

### (b) guard runs pre-packaging

| what | line |
|---|---|
| `PASS: all three onedir runners present, executable and Mach-O; tree sizes above the floor.` | 465 |
| `• electron-builder  version=26.8.1 os=25.5.0` | 466 |

Ordering **465 < 466** -- the guard completed before electron-builder emitted its first line.
Line 463, immediately above, carries the guard's own per-runner detail, e.g.
`nile: Python.framework Versions/Current symlink=true target=3.14 codesign=adhoc`.

### (c) appended CLI args reached electron-builder -- THE DISCHARGE

**The decisive evidence is the resolved script string at log line 3, not the `target=` lines** --
it shows the passthrough mechanism directly rather than by inference:

```
> export CSC_IDENTITY_AUTO_DISCOVERY=false && pnpm clean:dist-mac && pnpm build-steam-bridge && electron-vite build && pnpm verify:runner-bundle build --arch=arm64 && electron-builder --mac --arm64 --publish=never
```

pnpm appended `--arm64 --publish=never` to the **end** of the resolved chain with no `--`
separator, so both landed on `electron-builder` while `verify:runner-bundle` kept its own
hardcoded `--arch=arm64`. That is precisely the mechanism `deferred-items.md` item 18 detail 1
describes -- **now observed verbatim rather than reasoned about** (see the corroboration note
below).

Confirming effects:

```
481:  • building        target=macOS zip arch=arm64 file=dist/GameLib-0.7.0-macOS-arm64.zip
482:  • building        target=DMG arch=arm64 file=dist/GameLib-0.7.0-macOS-arm64.dmg
```

- arm64 `target=` lines: **2** (>= 1 required) -- `--arm64` was honored.
- x64 `target=` lines: **0** -- `--arm64` **narrowed** the build; it did not merely get accepted.
- `grep -c Uploading`: **0** -- `--publish=never` was honored.

Per §5(c), the bare word `publish` was **not** grepped: the invocation line above echoes
`--publish=never` and would self-match on a correct run.

### (d) artifacts newer than BUILD_START

| artifact | mtime (UTC) | vs BUILD_START |
|---|---|---|
| `GameLib-0.7.0-macOS-arm64.dmg` (220,032,155 B) | 2026-08-23T08:49:25Z | strictly after |
| `GameLib-0.7.0-macOS-arm64.zip` (211,374,303 B) | 2026-08-23T08:49:53Z | strictly after |

Total wall time `BUILD_START` -> last artifact: **2m49s**.

## Vendored tree integrity (this run's own check, not a contract criterion)

Post-run: legendary 109 files, gogdl 67, nile 108 -- unchanged. `Versions/Current` still a symlink
to `3.14` in all three. Symlink manifest sha256 **identical** to the pre-run snapshot
(`573f5ed3...`). The build did not disturb the vendored trees.

## Corroboration for ledger item 18 (C2-05), unplanned

Item 18's detail 1 asserts that pnpm appends extra CLI args with no `--` separator, "so the guard
still runs exactly once against arm64 only while `electron-builder` is handed both architectures
in the same job". Log line 3 **directly confirms the mechanism**: the guard's `--arch=arm64` is
fixed in the script body while the builder's arches come from the caller.

**This is corroboration of the MECHANISM, not an observation of the exposure.** This run passed
`--arm64`, so guard arch and build arch agreed and no gap opened. The x64 case -- where they would
diverge -- **remains unexercised and unproven**, exactly as items 1, 12, 13 and 18 all state. No
sentence here claims otherwise.

## What this run did NOT prove

- **Direction A.** Not run. The failing direction's evidence remains solely the 2026-08-12 record.
- **The x64 leg**, CI, `release:mac` itself, Tauri-packaged resolution, real-certificate signing or
  notarization, or any cold-spawn ratio. All unchanged and still recorded in `deferred-items.md`.
- **Guard A's failing direction at build level** -- unchanged from the 2026-08-12 record.

---

# RUN RECORD -- 2026-08-23 (Direction A, quick task 260823-t8i)

Run immediately after the Direction B record above, on the same tree and same day.
**Read the "Ordering deviation" section below before treating the two together as a full-contract
execution** -- they were two separate runs, and they ran B-then-A rather than the contract's A-then-B.

## Verdict: PASS 4/4

### (a) exit NON-ZERO

`direction-a.exit` read as bytes: **`310a`** = `"1\n"`. Exit **1**. (Byte-level read per
AMENDMENT v2 §A2 -- `0a` alone would be the capture-failure signature, not an exit code.)

### (b) the guard is what aborted the chain -- not merely a non-zero exit

The guard's per-runner structural table, log lines 469-471, **is an in-run negative control**:

```
Frameworks (structural integrity ENFORCED, F-34.9-01):
  legendary: Python.framework Versions/Current symlink=true  target=3.14 codesign=adhoc
  gogdl:     Python.framework Versions/Current symlink=false target=n/a  codesign=adhoc
  nile:      Python.framework Versions/Current symlink=true  target=3.14 codesign=adhoc
```

Only `gogdl` -- the runner actually injected -- reports `symlink=false`. The other two pass in the
same run. **The guard discriminates; it does not simply fail everything.** That control is what
makes the FAIL meaningful, and it was not available to a scoring rule that only counted rejections.

Both required signals present (lines 474-475, verbatim):

```
FAIL:
  - gogdl: framework .../build/bin/arm64/darwin/gogdl/_internal/Python.framework is malformed -- Versions/Current exists but is not a symlink (F-34.9-01) -- this is the exact dereferenced shape ...
  - gogdl: framework .../build/bin/arm64/darwin/gogdl/_internal/Python.framework is malformed -- top-level stub "Python" is a real file, not a symlink into Versions/Current (F-34.9-01)
```

F-34.9-01 literal: 1 match. `gogdl: framework .* is malformed` regex: 2 matches. §3(b)'s point
stands -- exit-code-alone would not discriminate, since `clean:dist-mac` or `build-steam-bridge`
failing would also exit non-zero. Line 475 is **WR-02's branch** (34.9-19's absent/wrong-type stub
closure) firing from the real build command, not from a unit test.

### (c) electron-builder never ran

`electron-builder  version=` count **0**. `building        target=` count **0**.

### (d) AMENDMENT v2 §A1's replacement criterion -- and it discriminated

All four pnpm lifecycle banners, in order: `dist:mac` (2), `clean:dist-mac` (6),
`build-steam-bridge` (21), **`verify:runner-bundle` (183)**. The last is `verify:runner-bundle`,
as required.

**This is the first exercise of the v2 (d) criterion**, and it did real work: it identifies *which*
step terminated the chain, which the retired (d) (an empty-`dist/` check that `clean:dist-mac`
guarantees regardless) structurally could not.

## Injection and restore

Injected per §3 steps 1-3: `mv` original to the session dir, `cp -RL` back (vite `copyDir`
behaviour). Known-bad state asserted **before** the build, per §3's "a proof whose known-bad state
was never asserted is vacuous": `Python` a real 5,454,496-byte file (was a symlink),
`Versions/Current` a real directory (was a symlink to `3.14`).

**Restore verified three independent ways** (§4 is a hard gate, and `public/bin/**` is git-ignored
so git is structurally blind to the whole operation):

1. Full framework manifest -- every file's sha256 and every symlink's target -- `diff`-identical to
   the pre-injection baseline. Digest `c7549b9ab414fb00...` before and after.
2. Whole-tree `arm64/darwin` symlink manifest sha256 `573f5ed313c943eb...` -- **matches the
   snapshot taken before Direction B**, i.e. unchanged across BOTH directions.
3. Per-runner file counts 109 / 67 / 108 with `Versions/Current -> 3.14` in all three.

The restored framework carries its original `Aug 7 09:57` timestamps, confirming it is the moved
original rather than a re-created copy.

**Extra safeguard taken beyond the contract.** The contract's only copy of the original lives in
`node_modules/.cache/`, which `pnpm install` clears -- and earlier the same day a plain
`git checkout` was observed firing this repo's post-checkout hook into `pnpm install`. Since the
darwin onedir archives **cannot be re-downloaded** (`download-helper-binaries` throws on the six
`PENDING-CI-PUBLISH` sentinels), losing that copy would have been unrecoverable. A tarball was
therefore taken **outside** `node_modules`, and **proven to extract byte-identical with symlinks
intact before any mutation began**. It was not needed. **A future run of this contract should do
the same: §3's `mv`-aside is a single point of failure against an irreplaceable tree.**

## Deviation from the 2026-08-12 record's stated expectation -- 2 rejections, not 4

That record's Deviation 5 advised: "A future re-run of this contract should expect this nested
duplication as inherent to `cp -RL` on this specific framework shape, not treat 4 rejections as a
regression from 2."

**No nested `Python.framework` was produced this time, and this run scored 2 rejections.**
Checked explicitly after the `cp -RL`: `[ -d Python.framework/Python.framework ]` was false.

The structural reason: this framework contains exactly **three** symlinks -- `Python`,
`Resources` and `Versions/Current` -- and **none of them resolves to the framework root or an
ancestor of it**, so `cp -RL` has nothing to recurse into. The tree is unchanged since `Aug 7
09:57` (mtimes preserved through this run's own mv/restore cycle), so it is not that the shape
changed between runs.

**The 2026-08-12 claim is left in place, not deleted, and is amended by this observation.** Its "2
wordings" half is confirmed -- both wordings fired. Its "2 framework instances" half did not
reproduce. A future run should expect **2** and treat 4 as the anomaly requiring explanation.

## Ordering deviation -- read before combining these two records

The contract runs **A, restore, then B** (§4 step 5: "The run does not proceed to Direction B ...
until the baseline matches exactly"). This session ran **B first, then A**.

**What that costs, stated plainly:** B was proven against a pristine tree (good), but **no build was
run after A's restore**, so the restore is verified *structurally* -- three independent manifest
audits -- and **not** functionally re-confirmed by a subsequent green build. The contract's own
ordering exists partly to provide that functional confirmation.

**These two records are therefore not a single full-contract execution**, and §6's verdict rule
("both directions must pass") should not be read as satisfied by simply concatenating them. Both
directions did pass, on the same tree and inputs, hours apart -- that is what is claimed here, and
nothing more.

## What this run did NOT prove

- **The x64 leg**, CI, `release:mac` itself, Tauri-packaged resolution, real-certificate signing,
  notarization, or any cold-spawn ratio. Unchanged; still recorded in `deferred-items.md`.
- **Guard A's own failing direction at build level.** Both this run and the 2026-08-12 run show
  Guard A at `skipped 0, rejected 0` -- the injected defect is a framework already dereferenced in
  `public/` before the build starts, not a symlink failing to restore during `electron-vite build`.
  This gap is unchanged.
