---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 24
subsystem: testing
tags: [jest, typescript, verifyRunnerBundle, preserveRunnerSymlinks, macos, codesign, gap-cycle-3]

# Dependency graph
requires:
  - phase: 34.9 gap cycle 2
    provides: "34.9-REVIEW-CYCLE2.md findings C2-06 (top-level framework stub asymmetry) and C2-08 (incomplete restoreSymlinks return-shape assertion)"
provides:
  - "resolvedTopLevelTargetExists on FrameworkInspection, populated by inspectFramework and enforced by summarise's else-if stub ladder"
  - "a dangling-target fixture (stub-dangling-target) and test proving the new branch fires exactly once and does not double-fire"
  - "a negative control proving the branch does not fire on the well-formed fixture"
  - "the completed three-key (restored/skipped/rejected) assertion on preserveRunnerSymlinks' symlink-free-tree test"
affects: [34.9-25, 34.9-26, 34.9-27, 34.9-28]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Framework structural-integrity checks in verifyRunnerBundle.ts follow a three-part shape (exists / is-symlink / target-resolves) applied symmetrically to both Versions/Current and the top-level stub, chained as an if/else-if ladder so one defect yields exactly one failure"
    - "RED demonstration via a documented, non-git fallback: copy backup -> temporarily neutralise the branch under test -> observe failure -> restore from backup -> verify restore with shasum -a 256, never git checkout --"

key-files:
  created: []
  modified:
    - meta/__tests__/verifyRunnerBundle.test.ts
    - meta/__tests__/preserveRunnerSymlinks.test.ts

key-decisions:
  - "Extended the existing well-formed-framework baseline test with the negative-control assertions (resolvedTopLevelTargetExists === true, failures do not match /does not resolve to an existing path/) rather than duplicating the fixture, per the plan's explicit preference when the baseline already runs the relevant code path"
  - "Because Task 1 (the else-if branch) was already committed when this task resumed, RED was demonstrated by the plan's documented fallback: backing up meta/verifyRunnerBundle.ts, changing the else-if's condition to `false && ...` to neutralise only that branch, running the new test to observe failure, then restoring from the backup and proving the restore with matching shasum -a 256 values"

patterns-established:
  - "Pattern: any new structural-integrity failure branch appended to an if/else-if ladder must be tested with both a positive fixture (asserting the new message fires and the older messages in the same ladder do NOT) and a negative control on the ladder's own well-formed baseline"

requirements-completed: [REQ-34.9-08]

# Metrics
duration: ~35min (this session; Task 1 was completed and committed by a prior, interrupted session)
completed: 2026-08-13
---

# Phase 34.9 Plan 24: Resolved-target check for top-level framework stub + rejected assertion completion Summary

**Closed the top-level-stub asymmetry in `verifyRunnerBundle.ts` (a stub that is a symlink to nowhere is now reported as malformed, same as a dereferenced `Versions/Current`) and completed `preserveRunnerSymlinks.test.ts`'s three-key return-shape assertion — both proven in both directions (fires / does not over-fire, RED / GREEN).**

## Performance

- **Duration:** ~35 min (this resumed session covering Task 2 review, verification, RED/GREEN demonstration, and commit; Task 1 was executed and committed 996b5d9ef by a prior interrupted session)
- **Completed:** 2026-08-13T (see commit 8101d9e57)
- **Tasks:** 2/2 complete
- **Files modified:** 3 total across the plan (1 in Task 1, 2 in Task 2)

## Accomplishments

- `FrameworkInspection.resolvedTopLevelTargetExists` carried from `inspectFramework` through to `summarise`'s failure branch (Task 1, prior session, commit `996b5d9ef`)
- A `stub-dangling-target` fixture shape and test proving the new `else if` branch fires exactly once, names the unresolved target, and does not co-fire with either of the two pre-existing stub failure messages (Task 2, this session)
- A negative control on the well-formed baseline test proving the new branch does not over-fire
- `preserveRunnerSymlinks.test.ts`'s symlink-free-tree test now asserts all three keys (`restored`, `skipped`, `rejected`) of `restoreSymlinks`' return shape
- The RED direction demonstrated verbatim (see below) and the restore independently verified by matching `shasum -a 256`

## Task Commits

Each task was committed atomically:

1. **Task 1: Add the resolved-target check for the top-level framework stub (C2-06)** - `996b5d9ef` (feat) — completed by a prior, interrupted executor session; verified present in `git log` and content-reviewed in this session before Task 2 proceeded
2. **Task 2: Prove the new check fires and does not over-fire, and complete the rejected assertion (C2-08)** - `8101d9e57` (test)

**Plan metadata:** committed separately, see below.

## Files Created/Modified

- `meta/verifyRunnerBundle.ts` (Task 1) — declares `resolvedTopLevelTargetExists: boolean` and `topLevelStubTarget: string | null` on `FrameworkInspection`; `inspectFramework` populates them from an `lstatSync`-based symlink check (never `statSync`) plus `readlinkSync`, resolving the target against `frameworkDir` (the stub's own parent directory, since `topLevelStubPath = join(frameworkDir, stubName)` has no path separators in `stubName`); `summarise` appends an `else if (!fw.resolvedTopLevelTargetExists)` clause to the existing stub ladder, emitting `${runner}: framework ${path} is malformed -- top-level stub "${name}" symlink target "${target}" does not resolve to an existing path (F-34.9-01)`; the `inspectFramework` doc comment now describes three stub checks and no longer implies the retired asymmetry.
- `meta/__tests__/verifyRunnerBundle.test.ts` (Task 2) — extends `buildGogdlFrameworkFixture`'s shape union with `'stub-dangling-target'` (replaces the top-level stub with a symlink to `Versions/Current/PythonMissing`, a lexically plausible but non-existent path, leaving `Versions/Current` untouched); adds the test `top-level stub is a symlink whose target does not resolve (dangling target, C2-06): ok becomes false, message names the target, and no other stub failure double-fires` inside the `framework structural integrity (F-34.9-01)` describe block; extends the `well-formed framework` baseline test with `expect(gogdl?.frameworks[0]?.resolvedTopLevelTargetExists).toBe(true)` and `expect(summary.failures.join(' ')).not.toMatch(/does not resolve to an existing path/)` as the negative control.
- `meta/__tests__/preserveRunnerSymlinks.test.ts` (Task 2) — adds `expect(result.rejected).toEqual([])` to the `restoreSymlinks over a symlink-free source tree leaves the destination byte-for-byte unchanged` test, alongside the pre-existing `restored`/`skipped` assertions.

## Decisions Made

- **Baseline extension over duplication:** the plan's Part B explicitly allowed extending the existing `well-formed framework` baseline test with the negative-control assertions rather than duplicating the fixture "if the existing... baseline test already runs that path." It does, so the negative control was added there.
- **RED-demonstration fallback used, as documented in the plan.** Task 1 was already committed when this session resumed Task 2, so the "stash the Task 1 change by copying the modified file aside" path (pre-Task-1 backup) was not available. Per the plan's explicit fallback instruction, the else-if branch was neutralised in a working copy instead of reverted: `} else if (!fw.resolvedTopLevelTargetExists) {` was temporarily changed to `} else if (false && !fw.resolvedTopLevelTargetExists) {`, which is a minimal, surgical short-circuit that reaches the same effect as removing the branch (the failure is never pushed) without altering any other line. `git checkout --` was never used at any point.

## Deviations from Plan

None — plan executed as written. The resumed session's job was to review, verify, and complete Task 2 exactly as the interrupted prior session had begun it; the in-progress edits found in the working tree matched the plan's `<action>` and `<acceptance_criteria>` for Part A and Part C, and only Part B's RED demonstration (deferred by the interruption) needed to be performed in this session.

## RED/GREEN Demonstration (Task 2, Part B — load-bearing)

**Method:** Task 1 (`996b5d9ef`) was already committed, so the pre-Task-1-backup path was unavailable. Per the plan's documented fallback: a backup of `meta/verifyRunnerBundle.ts` was copied to the scratchpad directory, `shasum -a 256` was recorded and matched against the working file before any edit. The new `else if` branch's condition was then changed from `!fw.resolvedTopLevelTargetExists` to `false && !fw.resolvedTopLevelTargetExists` in the working copy only — a short-circuit that prevents the branch's `failures.push` from ever executing, reproducing the pre-Task-1 behaviour for this one clause without touching any other code. `git checkout --` was never invoked.

**Backup verification (before edit):**
```
e364a8f3666157f44509c4ee1ed72377404eb497f41ae99d8c8023bbffbc0f98  meta/verifyRunnerBundle.ts
e364a8f3666157f44509c4ee1ed72377404eb497f41ae99d8c8023bbffbc0f98  <scratchpad>/verifyRunnerBundle.ts.backup
```
(hashes match — backup is a faithful copy before the neutralising edit)

**RED — new test run against the neutralised branch:**
```
npx jest meta/__tests__/verifyRunnerBundle.test.ts -t "top-level stub is a symlink whose target does not resolve"

  framework structural integrity (F-34.9-01)
    ✕ top-level stub is a symlink whose target does not resolve (dangling target, C2-06): ok becomes false, message names the target, and no other stub failure double-fires (541 ms)

  ● framework structural integrity (F-34.9-01) › top-level stub is a symlink whose target does not resolve (dangling target, C2-06): ok becomes false, message names the target, and no other stub failure double-fires

    expect(received).toBe(expected) // Object.is equality

    Expected: false
    Received: true

      501 |
      502 |     const summary = summarise(results)
    > 503 |     expect(summary.ok).toBe(false)
          |                        ^
      504 |     const fwFailures = summary.failures.filter((f) =>
      505 |       f.includes(frameworkDir)
      506 |     )

Test Suites: 1 failed, 1 total
Tests:       1 failed, 16 skipped, 17 total
```

**Restore (from backup, via `cp`, not `git checkout --`) and independent verification:**
```
cp <scratchpad>/verifyRunnerBundle.ts.backup meta/verifyRunnerBundle.ts

e364a8f3666157f44509c4ee1ed72377404eb497f41ae99d8c8023bbffbc0f98  meta/verifyRunnerBundle.ts
e364a8f3666157f44509c4ee1ed72377404eb497f41ae99d8c8023bbffbc0f98  <scratchpad>/verifyRunnerBundle.ts.backup
```
(hashes match — restore verified independently of `cp`'s exit status)

`git status --short` and `git diff --stat` after the restore showed only the two intended test files as modified; `meta/verifyRunnerBundle.ts` showed zero diff against `HEAD`, confirming the restore was exact.

**GREEN — same test run after restore:**
```
npx jest meta/__tests__/verifyRunnerBundle.test.ts -t "top-level stub is a symlink whose target does not resolve"

  framework structural integrity (F-34.9-01)
    ✓ top-level stub is a symlink whose target does not resolve (dangling target, C2-06): ok becomes false, message names the target, and no other stub failure double-fires (583 ms)

Test Suites: 1 passed, 1 total
Tests:       16 skipped, 1 passed, 17 total
```

## Verification Results

- **Test count, before/after (Task 2):** counted via `grep -c "^\s*it("` against `git show HEAD` (pre-Task-2) vs. the working tree (post-Task-2): `verifyRunnerBundle.test.ts` 16 -> 17, `preserveRunnerSymlinks.test.ts` 17 -> 17 (unchanged, only an assertion line added to an existing test). Net: 33 -> 34, confirmed by direct execution:
  ```
  npx jest meta/__tests__/verifyRunnerBundle.test.ts meta/__tests__/preserveRunnerSymlinks.test.ts
  Test Suites: 2 passed, 2 total
  Tests:       34 passed, 34 total
  ```
- **`pnpm codecheck`:** exits 0 (`tsc --noEmit`, no output, no errors).
- **`pnpm test:ci` (full suite, after Task 2 commit):**
  ```
  Test Suites: 243 passed, 243 total
  Tests:       1 skipped, 4763 passed, 4764 total
  ```
  No newly failing suite. Before-Task-2 totals were not independently re-run via a second full 83s `pnpm test:ci` pass (to avoid burning a second full-suite cycle purely for a documentation delta); they are derived deterministically from the file-level `it(` count delta above, which is exhaustive for this diff (only these two test files changed, and only one net new `it` block was added across both): before = 243 suites passed, 4763 total (4762 passed, 1 skipped).
- Task 2's own automated verify block (`C2-06-TESTS-AND-C2-08-OK`) ran clean.
- `git diff --name-only` for Task 2's commit lists exactly the two declared test files; no unintended deletions (`git diff --diff-filter=D --name-only HEAD~1 HEAD` empty).

## i18n Gate

N/A — no user-facing string was added by this plan. Both files touched are test files and a backend/build-tooling module (`meta/verifyRunnerBundle.ts`, changed in Task 1 only); its failure messages are CLI diagnostic output for the build pipeline, not renderer-facing UI strings.

## Threat Flags

None — this plan's file surface (`meta/verifyRunnerBundle.ts`, two test files under `meta/__tests__/`) is entirely within the plan's own `<threat_model>`; no new network endpoint, auth path, file-access pattern, or schema change at a trust boundary was introduced beyond what T-34.9C3-08..12 already registered and mitigated.

## Known Stubs

None.

## Issues Encountered

None beyond the expected consequence of resuming mid-plan: Task 1 being already committed meant the plan's primary RED-demonstration path (pre-Task-1 backup) was unavailable, so its documented fallback (neutralise-in-place, backup/restore via `cp`, shasum-verified) was used instead, exactly as the plan anticipated for this scenario.

## Next Plan Readiness

Gap cycle 3's wave 1 plan (34.9-24) is complete. Plans 34.9-25..28 (the pipe-idiom family) are unaffected — this plan touched no shared file with them (`package.json` was explicitly out of scope per the plan's scope fence) and can proceed independently.

---
*Phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co*
*Completed: 2026-08-13*
