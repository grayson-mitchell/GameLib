---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 17
subsystem: infra
tags: [reconciliation, gap-cycle, deferred-items, macos, packaging]

# Dependency graph
requires:
  - phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
    provides: "34.9-LIVE-GATE-RERUN.md's PASS verdict (2/2 scored items, plan 34.9-16) and the
      2026-08-11 gap-planning scope fence's six-item descope table (this plan's own <interfaces>
      block), both consumed as sole sources of truth -- never re-derived."
provides:
  - "deferred-items.md -- the phase's permanent out-of-scope/blocked-item ledger: 6 descoped
    items (REQ-34.9-02/03/04/09, Tauri-PACKAGED resolution, real-cert notarization), 2 out-of-scope
    UI defects, 1 pre-existing PKCE-logging note, and the cold-measurement gap, each with a dated
    blocker mechanism, precondition and OWNER"
  - "REQUIREMENTS.md, ROADMAP.md and STATE.md reconciled to the same measured truth: 7 of 11
    REQ-34.9 requirements ticked, 4 descoped (not failed), Phase 34.9 CLOSES on the arm64 leg only"
affects: [any future phase touching macOS packaging, CI dispatch, or the descoped items' owners
  (Phase 35 for the file-picker defect; a future security pass for PKCE logging)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A gap cycle's scope fence gets its own permanent ledger file (deferred-items.md), separate
      from the live-gate document that produced the findings -- descoped items are recorded as
      dated decisions with blocker/precondition/OWNER, never as silently dropped rows."
    - "Ledger reconciliation cross-references by item number (e.g. 'see deferred-items.md item 1')
      rather than restating the full rationale in three places -- REQUIREMENTS.md's own status
      text stays short, the ledger carries the detail once."

key-files:
  created:
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
  modified:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md

key-decisions:
  - "Recorded the scope fence's six descoped items and both out-of-scope UI defects as dated
    decisions with a named blocker MECHANISM (not a summary) and an OWNER line each -- 10 OWNER
    lines total (6 descoped + 2 UI defects + 1 PKCE + 1 duplicate cross-reference in the
    cold-measurement section), satisfying the >=9 threshold with margin."
  - "Corrected REQUIREMENTS.md's trailing footer note, which still read the stale FAIL-run count
    ('4 ticked, 7 unticked') left over from the original 2026-08-11 gate FAIL -- added a second
    dated paragraph stating the gap-cycle-1 outcome ('7 ticked, 4 unticked, all 4 DESCOPED not
    failed') rather than editing the historical paragraph in place, so the FAIL-run record stays
    intact as history and the current truth is unambiguous to a reader landing on the footer."
  - "Discovered and hand-corrected a frontmatter gap: plan 34.9-16's own commit (718a3e08d)
    updated STATE.md's Current Position BODY paragraph but never touched the frontmatter
    progress block, leaving completed_plans at 269 (as of plan 34.9-15) instead of 270 (after
    34.9-16). Corrected directly to 271, accounting for both the missed 34.9-16 increment and
    this plan's own 34.9-17 completion, rather than propagating the gap forward."
  - "Left completed_phases/total_phases (23/16) untouched -- the plan's task 2 action explicitly
    scopes the frontmatter edit to stopped_at/last_updated/last_activity/completed_plans/percent
    only, and phase-level counting conventions across this project's multi-tier phase numbering
    (34.1-34.11) were not established well enough to hand-verify safely within this plan's scope."
  - "Left ROADMAP.md's warm-ratio measurement-caveat table byte-identical, per the plan's own
    explicit instruction -- confirmed via git diff showing zero changes inside that block."

requirements-completed: [REQ-34.9-02, REQ-34.9-03, REQ-34.9-04, REQ-34.9-08, REQ-34.9-09, REQ-34.9-11]

# Metrics
duration: ~50min (reading 5 source documents including an 887-line live-gate contract and a
  293-line prior-phase deferred-items.md as a formatting precedent, writing a 201-line new ledger,
  then reconciling three separate cross-referencing ledgers by hand)
completed: 2026-08-11
---

# Phase 34.9 Plan 17: Reconcile every ledger to the post-gap-cycle truth Summary

**Wrote `deferred-items.md` (6 descoped items + 2 UI defects + PKCE note + cold-measurement gap, 10 dated OWNER entries) and reconciled REQUIREMENTS.md/ROADMAP.md/STATE.md so Phase 34.9 closes on the arm64 leg only, correcting a frontmatter progress-counter gap plan 34.9-16 silently left behind.**

## Performance

- **Duration:** ~50 min
- **Completed:** 2026-08-11
- **Tasks:** 2 of 2
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments

- **`deferred-items.md` created** with all four required sections (`Descoped by the 2026-08-11
  gap-planning scope fence`, `Out-of-scope defects observed during the 2026-08-11 gate run`,
  `Pre-existing, recorded, not fixed`, `Measurement gaps`) and 10 dated `OWNER:` lines (>=9
  required): REQ-34.9-02 (CI x64 leg, `workflow_dispatch`/default-branch mechanism named literally),
  REQ-34.9-03/04 (transitively blocked on REQ-34.9-02), REQ-34.9-09 (cold ratio, third attempt
  explicitly NOT authorized), Tauri-PACKAGED resolution (blocked on Phase 34.5's pre-existing
  `R-34.5-G1-PKG`), real-certificate notarization (D-03/D-04), the silent file-picker failure
  (owned by Phase 35), `PathSelectionBox`'s blur-only-commit/paste-glyph defect (`OWNER: UNASSIGNED`
  — explicit developer decision owed, no UI phase remains after 34.11), and the pre-existing PKCE
  plaintext logging at `nile/user.ts:62` (owner: a future security pass). The identifier-redaction
  discipline was honored throughout: `code_verifier`/`serial`/`client_id` appear exactly twice, both
  as prohibited FIELD NAMES inside the PKCE entry's own prose, never as values.
- **REQUIREMENTS.md reconciled.** REQ-34.9-02/03/04/09's status-table rows and checkbox entries
  now each carry a `**Descoped by gap cycle 1's 2026-08-11 scope fence — see `deferred-items.md`
  item N.**` cross-reference, restating the named precondition inline rather than only in the
  ledger. REQ-34.9-01/05/06/07/10 were re-read against current source and confirmed still accurate
  — no edit needed, since nothing in this gap cycle touched their subject matter. The trailing
  footer note (previously stating the stale FAIL-run "4 ticked, 7 unticked" count as if it were
  current) now carries a second, dated paragraph recording gap cycle 1's outcome: F-34.9-01/02/03
  closed, the re-run's PASS verdict, and the corrected current count — 7 ticked
  (01/05/06/07/08/10/11), 4 unticked (02/03/04/09), all four DESCOPED rather than failed.
- **ROADMAP.md reconciled.** The `**Plans:**` line now reads `17 total — 17/17 plans executed`;
  the gap-cycle checklist's 34.9-17 row is ticked with its own DONE marker. The `Phase status
  2026-08-11` note — previously `DOES NOT CLOSE` — is replaced with a `CLOSES on the arm64 leg
  only` note narrating F-34.9-01/02/03's closure mechanism-by-mechanism (plans 34.9-12/13/14), the
  re-run's measured PASS criteria (12 symlinks, zero `bundle format is ambiguous`,
  `verify:runner-bundle` exit 0, +0.02% payload delta, mtime-newness check), and a second paragraph
  stating the x64 leg's permanent-block-for-this-phase status as a **user decision**, not an
  unresolved defect, with a pointer to the new `deferred-items.md`. The existing warm-ratio
  measurement-caveat table (26.6x–32.5x) was left byte-identical — confirmed via `git diff` showing
  no changes inside that block.
- **STATE.md reconciled, and a silent frontmatter gap corrected.** Investigating the frontmatter
  before editing revealed that plan 34.9-16's own commit (`718a3e08d`) updated the Current Position
  BODY paragraph to describe the re-run's PASS verdict but never touched the frontmatter
  `stopped_at`/`last_updated`/`last_activity`/`progress` block — those fields were still frozen at
  plan 34.9-15's values (`completed_plans: 269`, `percent: 95`). Corrected by hand to
  `completed_plans: 271` (crediting both the missed 34.9-16 increment and this plan's own
  completion) and `percent: 96` (`271/283*100 = 95.76%`, rounds to 96, matching this project's
  established rounding convention verified against four prior plans' own committed diffs). The
  Current Position body was rewritten to mark Phase 34.9 `**CLOSED 2026-08-11, arm64 leg only**`,
  with the prior 34.9-16 paragraph preserved, unedited, under a `(Prior, now superseded: ...)`
  wrapper — the same nesting convention every prior plan in this gap cycle used.

## Task Commits

1. **Task 1: Write the phase deferred-items ledger** - `a17ef19bd` (docs)
2. **Task 2: Reconcile REQUIREMENTS.md, ROADMAP.md and STATE.md to the measured truth** - `c90975b8b` (docs)

`git diff --name-only a17ef19bd~1 c90975b8b` returns exactly this plan's four `files_modified`
entries — no other file, and no other phase's ledger rows, were touched.

## Files Created/Modified

- `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md` (created) — the phase's permanent out-of-scope/blocked-item ledger, 201 lines, 4 sections, 10 `OWNER:` lines
- `.planning/REQUIREMENTS.md` (modified) — REQ-34.9-02/03/04/09 status rows + checkboxes cross-reference the new ledger; footer note corrected with a dated second paragraph
- `.planning/ROADMAP.md` (modified) — Plans line 17/17, gap-cycle checklist complete, Phase status note rewritten to CLOSES
- `.planning/STATE.md` (modified) — frontmatter progress hand-corrected (269→271, 95%→96%), Current Position body marks Phase 34.9 CLOSED

## Decisions Made

See `key-decisions` in frontmatter above — summarized: (1) dated blocker/precondition/OWNER
entries for all 9 required items plus one cross-reference, satisfying the >=9 OWNER threshold; (2)
additive (not in-place) correction of REQUIREMENTS.md's stale footer count, preserving the FAIL-run
paragraph as history; (3) hand-discovered and corrected a silent STATE.md frontmatter progress gap
plan 34.9-16 left behind; (4) deliberately left `completed_phases`/`total_phases` untouched, outside
this plan's explicit scope; (5) left the warm-ratio measurement table byte-identical.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] STATE.md frontmatter progress counters were stale by one full plan**
- **Found during:** Task 2, while reading `stopped_at`/`progress` before editing
- **Issue:** Plan 34.9-16's own commit (`718a3e08d`) updated the Current Position body text to
  describe its PASS verdict, but never touched the frontmatter `stopped_at`/`last_updated`/
  `last_activity`/`progress.completed_plans`/`progress.percent` fields, which remained at plan
  34.9-15's values (`completed_plans: 269`).
- **Fix:** Hand-computed the correct value crediting both the missed 34.9-16 increment and this
  plan's own 34.9-17 completion: `completed_plans: 271`, `percent: 96` (`271/283≈95.76%`, verified
  against the project's established round-not-floor convention by cross-checking four prior
  plans' own committed diffs, `1f6d0039a`/`7e5cb5f23`/`52f0a5e71`/`ff022962e`).
- **Files modified:** `.planning/STATE.md`
- **Verification:** `node -e` frontmatter regex check (plan's own automated verify clause) passes;
  before/after values recorded explicitly in this Summary and inline in STATE.md's own
  `stopped_at` text, per the plan's acceptance criteria.
- **Committed in:** `c90975b8b` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — stale progress counters)
**Impact on plan:** Necessary for STATE.md arithmetic correctness, the plan's own explicit
acceptance criterion. No scope creep — the fix stayed entirely within STATE.md's already-in-scope
frontmatter fields.

## Issues Encountered

None beyond the frontmatter gap documented above.

## User Setup Required

None — this plan wrote planning documents only.

## Verification Evidence

- `test -f deferred-items.md` → exists. `grep -c 'OWNER:' deferred-items.md` → `10` (>= 9 required).
- `grep -q "workflow_dispatch"` and `grep -q "default branch"` (literal, not paraphrased) both
  present in the REQ-34.9-02 entry.
- `grep -q "OWNER: UNASSIGNED"` present (the `PathSelectionBox` entry).
- `grep -in "code_verifier\|client_id\|serial" deferred-items.md` → exactly 2 lines, both inside
  the PKCE entry's own prohibition prose (`including url, code_verifier, serial, and client_id —
  to gamelib.log in plaintext` and `names code_verifier, serial, and client_id only as` ) — quoted
  here to demonstrate neither carries a value.
- `grep -n "^## " deferred-items.md` → all 4 required section headings present, in order.
- `grep -c "2026-" deferred-items.md` → `21` (>= 9 required).
- `grep -c "34.9-LIVE-GATE-RERUN" .planning/REQUIREMENTS.md` → `4` (>= 2 required).
- `grep -q "17 plans\|17/17\|/17" .planning/ROADMAP.md` → present (`17/17 plans executed`).
- `node -e` STATE.md frontmatter regex check → passes (`completed_plans: 271`, `percent: 96`).
- `git diff --name-only a17ef19bd~1 c90975b8b` → exactly the plan's 4 `files_modified` entries,
  no other file touched.
- `git status --porcelain` after both commits → clean.
- No `git push`, no `gh pr create`, no upstream submission command appears anywhere in this
  plan's own tool-call history.
- No user-facing string added anywhere in this plan's diffs — all four files are planning
  documentation. The standing i18n gate is N/A, as `<verification>` requires this to be stated
  explicitly.

## Threat Flags

None. All six threats in the plan's `<threat_model>` were mitigated exactly as specified:
T-34.9G-23 (a descoped item vanishing from the record) by the 10 dated `OWNER:` entries;
T-34.9G-24 (a requirement ticked from something other than measured evidence) — no requirement
was newly ticked by this plan; REQ-34.9-08/11 were already ticked by plan 34.9-16 from
`34.9-LIVE-GATE-RERUN.md`'s own verdict, and this plan's `requirements-completed` list reflects
the plan's frontmatter `requirements:` field, not a new ticking action; T-34.9G-25 (`gsd-sdk`
corrupting STATE.md) avoided entirely — every edit in this plan was a direct hand-edit via the
`Edit` tool, never a `gsd-sdk` state-write verb; T-34.9G-26 (edits leaking into other phases' rows)
mitigated by the `git diff --name-only` check above matching exactly this plan's own
`files_modified`; T-34.9G-27 (identifiers transcribed into a ledger) mitigated by the redaction
discipline and the quoted-lines verification above; T-34.9G-SC (package legitimacy) is N/A — this
plan installed no packages and wrote no code.

## Next Phase Readiness

**Phase 34.9 is now CLOSED on the arm64 leg only.** Every requirement's status is measured and
cross-referenced: 7 ticked (REQ-34.9-01/05/06/07/08/10/11), 4 descoped-not-failed
(REQ-34.9-02/03/04/09), each with a dated blocker, precondition and owner in
`deferred-items.md`. The x64 CI leg, the digest-verification path, and a legendary/gogdl
cold-spawn measurement all remain genuinely open work for a future phase or developer action —
none of them silently disappeared. Two out-of-scope UI defects (Phase 35's file-picker stub;
`PathSelectionBox`'s Enter/paste gap, UNASSIGNED) and one pre-existing security finding (PKCE
plaintext logging) are recorded for whoever picks them up next. **No specific next phase is
pre-selected by this reconciliation** — the next session chooses what to plan or execute.

---
*Phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co*
*Completed: 2026-08-11*

## Self-Check: PASSED

- FOUND: `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md`
- FOUND: `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-17-SUMMARY.md`
- FOUND commit `a17ef19bd` (Task 1: write deferred-items ledger)
- FOUND commit `c90975b8b` (Task 2: reconcile REQUIREMENTS/ROADMAP/STATE)
