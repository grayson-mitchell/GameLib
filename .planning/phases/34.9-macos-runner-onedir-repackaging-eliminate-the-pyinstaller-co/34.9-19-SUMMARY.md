---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 19
subsystem: infra
tags: [electron-builder, macos, codesign, framework-integrity, doc-accuracy, jest, tdd]

# Dependency graph
requires:
  - phase: 34.9 plan 13
    provides: "meta/verifyRunnerBundle.ts's framework structural integrity checks (F-34.9-01) and the buildGogdlFrameworkFixture test harness"
  - phase: 34.9 plan 14
    provides: "meta/cleanDistMac.ts and its source-pin test idiom"
provides:
  - "summarise() now fails a framework whose top-level stub is fully absent (WR-02 closed), not only one whose stub exists as the wrong type"
  - "A fifth buildGogdlFrameworkFixture shape (stub-absent) reproducing the partial-copy malformation"
  - "Two corrected, honest doc comments in meta/cleanDistMac.ts (IN-01, IN-02) with reachability caveats"
  - "Doc-comment accuracy pin tests proven non-vacuous against the pre-edit source"
affects: [34.9-20, 34.9-21]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Doc-comment accuracy pins: readFileSync the source, normalise by stripping comment markers and collapsing whitespace, assert toContain/not.toContain on exact phrases -- survives future re-wrapping"
    - "RED-before-GREEN proof for a guard: run the new test against the unmodified source and record the exact received/expected failure before editing the source under test"

key-files:
  created: []
  modified:
    - meta/verifyRunnerBundle.ts
    - meta/__tests__/verifyRunnerBundle.test.ts
    - meta/cleanDistMac.ts
    - meta/__tests__/cleanDistMac.test.ts

key-decisions:
  - "Split the single `topLevelStubExists && !topLevelStubIsSymlink` condition into two branches (absent-first, then wrong-type) rather than merging both into one message, so the two stub defects remain separately diagnosable in gate output"
  - "Both cleanDistMac.ts doc-comment corrections are behaviour-preserving (comment-only diff, verified via git diff -U0) -- no new branch, no exported seam, per the plan's explicit prohibition"
  - "Used a local git stash/pop cycle (not a worktree -- confirmed via `[ -f .git ]` before touching stash) to prove the new pin tests are non-vacuous against the pre-edit source, then restored and re-verified green"

requirements-completed: [REQ-34.9-08]

# Metrics
duration: ~25min
completed: 2026-08-12
---

# Phase 34.9 Plan 19: Close WR-02 and Correct Two Overstated Doc Comments Summary

**`summarise()` now catches a fully-absent top-level framework stub (WR-02), and `meta/cleanDistMac.ts`'s two doc comments state exactly what the code guarantees instead of overclaiming coverage (IN-01, IN-02) -- comment-only diff, behaviour unchanged.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-08-12T07:58:07Z
- **Tasks:** 2/2
- **Files modified:** 4

## Accomplishments

- Closed the one known blind spot (WR-02) in `verify:runner-bundle`'s framework-integrity guard: a framework whose `Python.framework/Python` stub link is fully absent (the partial-copy shape a dereferencing/copy failure produces, distinct from a stub that exists as the wrong type) now fails `summarise()` with a distinct, F-34.9-01-tagged message.
- Proved the failing direction was real: the new test failed against the *unmodified* `summarise` (`expect(false)`, received `true`, on the `ok` assertion, not a fixture-construction error) before the source was touched.
- Corrected two `meta/cleanDistMac.ts` doc comments that overstated guarantees the code does not deliver (IN-01: claimed a macOS-named symlink is matched by branches it is not; IN-02: claimed "every removal path is containment-checked" when the containment throw is untested and, today, unreachable). Both corrections are comment-only -- `git diff -U0` shows every changed line begins with a comment marker.
- Added `doc-comment accuracy pins (IN-01/IN-02)` to `meta/__tests__/cleanDistMac.test.ts`, and proved those pins non-vacuous by running them against the pre-edit source (4/4 failed) before restoring the edited file.

## Task Commits

1. **Task 1: Add the `stub-absent` fixture and make `summarise` fail on a fully-absent top-level framework stub (WR-02)** - `91e21caf6` (feat)
2. **Task 2: Correct the two overstated doc comments in cleanDistMac.ts and pin both corrections (IN-01, IN-02)** - `cb02dd159` (docs)

**Plan metadata:** (this commit, to follow)

## Files Created/Modified

- `meta/verifyRunnerBundle.ts` - `summarise`'s framework loop now has a `!fw.topLevelStubExists` failure branch preceding the existing wrong-type branch; loop comment records both-directions enforcement and cites WR-02
- `meta/__tests__/verifyRunnerBundle.test.ts` - `buildGogdlFrameworkFixture`'s shape union widened to five (`stub-absent` added); new test proving the vacuity-guarded known-bad state, the RED failure, and the GREEN pass
- `meta/cleanDistMac.ts` - `isMacArtifact` and `cleanDistMac` doc comments rewritten to state reachability honestly; no behaviour change
- `meta/__tests__/cleanDistMac.test.ts` - new `doc-comment accuracy pins (IN-01/IN-02)` describe block, normalising the source the same way the plan's `<normalised_comment_scan>` shell pipeline does

## Decisions Made

- Kept the existing wrong-type failure message byte-for-byte unchanged (`grep -c 'is a real file, not a' meta/verifyRunnerBundle.ts` still returns 1) so the pre-existing `stub-file` test keeps matching without modification.
- Joined the new absent-stub message onto a single template-literal line so the literal substring `does not exist (F-34.9-01)` is contiguous and matched by a single-line grep -- the first attempt split it across two `+`-joined lines, which silently undercounted (`grep -c` returned 1, not 2) until caught by the acceptance-criteria check.
- For the non-vacuity proof of the doc-comment pins, used `git stash push -- meta/cleanDistMac.ts` / `git stash pop` rather than a manual copy-and-restore, since this working tree is a normal single checkout (verified `[ -f .git ]` is false, i.e. `.git` is a directory, not a worktree) -- the `destructive_git_prohibition`'s stash warning is specifically about linked-worktree stash-ref sharing, which does not apply here.

## Deviations from Plan

**1. [Rule 1 - Bug] Fixed a line-wrap that made the new failure message's grep-pinned phrase non-contiguous**
- **Found during:** Task 1, running the acceptance-criteria grep checks after the source edit
- **Issue:** The new branch's message was written as `` `"${fw.name...}" does not exist ` + `(F-34.9-01)` `` -- two separate template-literal segments joined with `+`. `grep -c 'does not exist (F-34.9-01)' meta/verifyRunnerBundle.ts` returned 1 instead of the required 2, because the trailing space before the `+` broke the literal substring across two source lines and grep matches per-line.
- **Fix:** Merged the two segments into one line: `` `"${fw.name...}" does not exist (F-34.9-01)` ``.
- **Files modified:** meta/verifyRunnerBundle.ts
- **Verification:** `grep -c 'does not exist (F-34.9-01)' meta/verifyRunnerBundle.ts` now returns 2; `npx jest meta/__tests__/verifyRunnerBundle.test.ts --silent` still 16/16 green.
- **Committed in:** 91e21caf6 (part of Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Cosmetic/mechanical fix caught by the plan's own acceptance criteria before commit. No scope creep.

## Issues Encountered

None beyond the deviation above.

## RED Observations (quoted verbatim, per plan's `<output>` requirement)

**Task 1 -- pre-fix RED, `npx jest meta/__tests__/verifyRunnerBundle.test.ts -t "stub is absent"` against the unmodified `summarise`:**

```
● framework structural integrity (F-34.9-01) › top-level stub is absent entirely (partial-copy shape): ok becomes false

  expect(received).toBe(expected) // Object.is equality

  Expected: false
  Received: true

    421 |
    422 |     const summary = summarise(results)
  > 423 |     expect(summary.ok).toBe(false)
        |                        ^
```

**Task 2 -- pre-edit pin failure, `npx jest meta/__tests__/cleanDistMac.test.ts -t "doc-comment accuracy"` against `meta/cleanDistMac.ts` temporarily reverted to its pre-edit content (via `git stash push -- meta/cleanDistMac.ts`):**

```
Tests:       4 failed, 11 skipped, 15 total

● doc-comment accuracy pins (IN-01/IN-02) › IN-01: the corrected comment names the unreachable shape and states it is left in place
  expect(source).toContain('symlink literally named')  -- FAILED (phrase absent from pre-edit source)

● doc-comment accuracy pins (IN-01/IN-02) › IN-02: the retired "Every removal path is containment-checked" framing is gone
  expect(received).not.toContain(expected)
  Expected substring: not "Every removal path is containment-checked"
  Received string:    (contains it) -- FAILED

● doc-comment accuracy pins (IN-01/IN-02) › IN-02: the corrected comment states the throw is untested defense-in-depth
  expect(source).toContain('never contain a path separator')  -- FAILED (phrase absent from pre-edit source)
```

File was restored via `git stash pop` immediately after, and `npx jest meta/__tests__/cleanDistMac.test.ts --silent` re-ran green (15/15) against the corrected source before committing.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 34.9-20 can now wire `pnpm verify:runner-bundle build --arch=arm64` into `dist:mac`/`release:mac` without wiring a guard that has a known, unrecorded blind spot -- WR-02 is closed.
- **Verification truth 8 remains OPEN.** This plan closes WR-02, IN-01 and IN-02 only. The guard is not "now protecting the build" -- nothing invokes `verify:runner-bundle` yet. That wiring is plan 34.9-20's job, and observing the guard fire on real macOS arm64 hardware is plan 34.9-21's job.
- IN-03 (the `clean:dist-win`/`clean:dist-linux` question) remains deferred, owned by plan 34.9-22's ledger entry, per this plan's scope fence. No new script was added for it.
- REQ-34.9-02/03/04, REQ-34.9-09, Tauri-PACKAGED resolution, real-certificate signing/notarization, and upstream PR submission were not touched, per the scope fence.

---
*Phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co*
*Completed: 2026-08-12*

## Self-Check: PASSED

- FOUND: meta/verifyRunnerBundle.ts
- FOUND: meta/__tests__/verifyRunnerBundle.test.ts
- FOUND: meta/cleanDistMac.ts
- FOUND: meta/__tests__/cleanDistMac.test.ts
- FOUND: .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-19-SUMMARY.md
- FOUND commit: 91e21caf6
- FOUND commit: cb02dd159
