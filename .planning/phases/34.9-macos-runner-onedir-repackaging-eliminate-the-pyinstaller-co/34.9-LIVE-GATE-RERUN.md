---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 15
type: live-gate
status: authored
blocking: true
created: 2026-08-11
supersedes_run: 34.9-LIVE-GATE.md (verdict FAIL, 2026-08-11)
verdict: PENDING
run_date: null
items_total: 2
items_passed: null
items_failed: null
items_carried_forward: 3
items_not_attempted: null
runner: human
session_dir: null
findings: []
notes: |
  Authored 2026-08-11 by plan 34.9-15, BEFORE any live work, by a plan FORBIDDEN from running any
  item in this document, starting `pnpm tauri:dev`, running `pnpm dist:mac`, or running
  `pnpm verify:runner-bundle` against any built app. No per-item result and no
  `verdict`/`run_date`/`items_passed`/`items_failed` value was written by this plan. Plan 34.9-16
  is the only writer of results -- the same author/runner separation this project's standing
  reference (`.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md`,
  decision D-E) requires for every contract, and the same discipline `34.9-LIVE-GATE.md` (plans
  34.9-10/11) and Phase 34.4.2's five-gate history both already used.
---

# Phase 34.9 -- Blocking Live Gate RE-RUN (REQ-34.9-11)

This document supersedes nothing on disk -- `34.9-LIVE-GATE.md`'s FAIL run record (`verdict:
FAIL`, `run_date: 2026-08-11`, items 1/2/3/5 PASS, item 4 FAIL, findings F-34.9-01..04) is
preserved byte-for-byte and remains the permanent evidence for why this gap cycle exists. This is
a NEW file, not an edit to that one -- the same one-file-per-run precedent Phase 34.5 already set
(`34.5-LIVE-GATE-RERUN-2.md` is its own file, never an edit to `34.5-LIVE-GATE.md` or
`34.5-LIVE-GATE-RERUN.md`).

**This plan (34.9-15) is FORBIDDEN from running any item below, starting any harness, launching
`pnpm tauri:dev`, running `pnpm dist:mac`, or running `pnpm verify:runner-bundle` against a built
app.** Authoring and running are separate plans. Plan 34.9-16 is the only plan permitted to fill
any `Observed:` field, this frontmatter's `verdict`/`run_date`/`items_passed`/`items_failed`, or an
ITEM VERDICT SUMMARY table.

**A green test suite never closes this gate.** `npm run test:ci` is a PRECONDITION below, never
evidence for any item -- every blocking defect this project has found across Phases 34.4.1, 34.4.2,
34.5 and 34.10 was found by a human operator driving the UI on real hardware while the automated
suite stayed green throughout, and F-34.9-01 itself reached a live gate through a build the
then-236/4598-green suite never exercised.

## 1. Scope and what changed since the FAIL run

`34.9-LIVE-GATE.md`'s 2026-08-11 run scored 4 of 5 scored items PASS; item 4 (Electron PACKAGED)
FAILed because `pnpm dist:mac` aborted mid-build -- electron-builder's adhoc codesign step rejected
`gogdl/_internal/Python.framework/Python` with `bundle format is ambiguous (could be app or
framework)` (F-34.9-01). Three code plans closed this gap cycle's targets, all landed against the
same tip, `npm run test:ci` green at 238 suites / 4639 tests after the last of them (baseline before
this gap cycle: 236 suites / 4598 tests):

- **34.9-12** (`meta/preserveRunnerSymlinks.ts`) -- a `closeBundle` vite plugin that restores every
  `public/bin` symlink into `build/bin` after vite's own `copyDir` dereferences it (vite's `copyDir`
  has no symlink branch; it `statSync`s through links). This closes F-34.9-01 at its mechanism:
  live-proven, the exact failing `codesign --sign - --force --timestamp --options runtime
  --entitlements build/entitlements.mac.plist ... Python.framework/Python` invocation now exits 0
  against the restored framework, and `find build/bin/arm64/darwin -type l | wc -l` reads **12**
  after both a clean and an immediately-repeated (`emptyOutDir:false`) build. This is what makes
  Scored Item 2 below newly REACHABLE at all -- the FAIL run's build aborted before any packaged
  app existed, so nothing past this point was ever exercised.
- **34.9-13** (`meta/verifyRunnerBundle.ts`'s `FrameworkInspection`) -- `pnpm verify:runner-bundle`
  now enforces four independent structural conditions on every `*.framework` bundle in a built
  runner tree (`Versions/Current` existence, symlink-ness, and resolvability; a symlinked top-level
  stub), each mutation-proven live against a `cp -RL`-dereferenced fixture that reproduces the
  F-34.9-01 shape. This makes Scored Item 2's `verify:runner-bundle` step a genuine tripwire against
  a regression of F-34.9-01, not merely the presence/exec-bit/Mach-O/file-count check it was during
  the FAIL run.
- **34.9-14** (`meta/cleanDistMac.ts`) -- `pnpm clean:dist-mac` now runs FIRST in both `dist:mac`
  and `release:mac`, live-verified against the real `dist/` to remove the stale 2026-07-21
  `GameLib-0.7.0-macOS-arm64.dmg`/`.zip`/`.blockmap` pair, `latest-mac.yml`, and the partial
  `mac-arm64/` tree the FAIL run abandoned mid-signing (F-34.9-02). This is what makes Scored Item
  2's newness check ("mtime STRICTLY AFTER `BUILD_START`") a check against a demonstrably-cleared
  `dist/`, not a check that could pass by accident against a three-week-old artifact the way the
  FAIL run's own dmg/zip did.

None of the three plans ran `pnpm tauri:dev` or `pnpm dist:mac` themselves -- per their own
Summaries' "Next Phase Readiness" sections, exercising the full build end to end is this gap
cycle's live-gate re-run's job. This document is that re-run's contract.

## 2. Item disposition table with the carry-forward argument

The re-run's item set, and the reasoning that produced it, reproduced from this plan's own
authoring-time interfaces (not merely asserted here):

| Original item | Disposition here | Why |
|---|---|---|
| 4 -- Electron PACKAGED | **RE-RUN, scored -- renumbered "Scored Item 2" below** | The failure. Its subject was never reached: the build aborted before any packaged app existed, so nothing about packaged runner resolution was ever measured. |
| 1 -- Tauri DEV nested resolution | **RE-RUN, scored -- stays "Scored Item 1" below** | Regression canary. Plan 34.9-12 edits `electron.vite.config.ts`, which `pnpm tauri:dev` also runs (`electron-vite build && pnpm build:sidecar && tauri dev`). A defect in the new `closeBundle` plugin could in principle surface here too. One launch, one gesture, cheap to re-confirm alongside the item this gap cycle actually exists to fix. |
| 2 -- Amazon (nile) click -> spawn | **CARRY FORWARD, unscored this run** | Requires a real Amazon login. The 2026-08-11 evidence resolved `/Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/nile/nile` -- the DEV sidecar reads `publicDir` = `<repo>/public`, never `build/`, confirmed by `src/backend/constants/paths.ts:73-76` (`app.isPackaged \|\| CI==='e2e' ? 'build' : 'public'`) and the FAIL run's own `gamelib.log` lines. The gap cycle's fix changes only the `public/` -> `build/` copy (34.9-12) and `dist/` cleaning (34.9-14), both packaging-side. Nothing in item 2's evidence path was touched. |
| 3 -- GOG (gogdl) capture -> login | **CARRY FORWARD, unscored this run** | Same argument as item 2, same sink (`gamelib.log`), same DEV-only evidence path (`publicDir`), same conclusion: unaffected by a `build/`- and `dist/`-only fix. |
| 5 -- `altNileBin` override | **CARRY FORWARD, unscored this run** | Same argument. Its item still PASSed on absolute-path evidence in the FAIL run; its two contract defects (the relative-path ENOENT, the version-string non-discriminator) are recorded below as contract amendments for any FUTURE run of item 5, not as triggers for re-running it now. |
| *(unscored)* Tauri PACKAGED | **UNCHANGED, unscored** | Blocked on `R-34.5-G1-PKG` (Phase 34.5 `deferred-items.md` item 12), pre-dating this phase; this gap cycle does not touch `isPackaged` or `publicDir`. |

**Why items 2/3/5's evidence is not invalidated, stated plainly rather than implied.** The DEV
sidecar resolves runners under `<repo>/public` -- this is the FAIL run's own log lines (`.../
GameLib/public/bin/arm64/darwin/{legendary,gogdl,nile}/...`), not an inference. The gap cycle's fix
touches exactly two things: the `public/` -> `build/` copy step (34.9-12's `closeBundle` plugin,
which only runs during `electron-vite build`, i.e. packaging/dev-bundling, never during a plain
DEV-sidecar spawn against `public/` directly) and `dist/`'s own cleanliness (34.9-14). Neither
changes what `public/bin/arm64/darwin/{legendary,gogdl,nile}` itself contains or how the DEV sidecar
resolves a path inside it. Items 2, 3 and 5 all measure exactly that DEV-sidecar resolution path.

**What a carry-forward is NOT, stated with equal plainness.** It is not a fresh measurement. If
Scored Item 1 below shows ANY DEV-side regression -- any runner failing to resolve its nested path,
any version reading `Invalid`, any spawn shape different from the FAIL run's own archived evidence
-- **items 2, 3 and 5 must be re-run before the phase can close**, because that regression would
mean the DEV-side premise this carry-forward argument rests on no longer holds. This conditional is
binding, not advisory; plan 34.9-16's own run record must state explicitly whether it triggered.

**Deviation 4, verbatim from the FAIL run record -- the rule it produces is standing and applies to
every path this document names (Section 5, below).** Item 5's FAIL-run contract specified the
repo-relative `public/bin/x64/darwin/nile`, but the sidecar spawns with `cwd=<repo>/src-tauri`, so
the path resolved to `src-tauri/public/bin/x64/darwin/nile` and failed ENOENT. The absolute path was
used instead and item 5 PASSed on that basis.

**F-34.9-04, verbatim.** The Settings version field cannot discriminate override from default for
nile -- the x64 onefile and the arm64 onedir **both** report `1.1.2 Will A. Zeppeli`. Only the
spawned path in `gamelib.log` distinguishes them. This nearly produced a false PASS on item 5
mid-run during the FAIL run's own session.

**F-34.9-02, verbatim.** `dist/GameLib-0.7.0-macOS-arm64.dmg` and `.zip`, dated 2026-07-21, survived
the FAIL run's total build failure and read as its output. Plan 34.9-14 adds `pnpm clean:dist-mac`
as the first step of `dist:mac`, but Scored Item 2 below must still prove NEWNESS positively --
never accept a mutating command's own report as proof of its effect.

**New commands this re-run has that the FAIL run did not:**
- `pnpm clean:dist-mac` (34.9-14) -- now runs automatically as `dist:mac`'s first step.
- `pnpm verify:runner-bundle <app> --arch=arm64` (34.9-13) -- now ENFORCES framework structural
  integrity (`Versions/Current` must be a symlink, resolvable, with a symlinked top-level stub), not
  only presence/exec-bit/Mach-O/file-count as it did during the FAIL run.
- `find <root> -type l | wc -l` -- the expected count is **12** (4 symlinks x 3 runners).

## 3. Sink partition (reproduced unchanged from `34.9-LIVE-GATE.md`)

Every required literal below is annotated with the sink it can actually reach. No sidecar-emitted
literal is ever required of the session transcript; no Rust-shell-emitted literal is ever required
of `gamelib.log`.

- **Rust `[shell]`-prefixed lines** (`src-tauri/src/main.rs`'s `eprintln!` calls) reach
  `tauri:dev`'s own stdout/stderr, and therefore the **session transcript** ONLY. They never reach
  `gamelib.log`.
- **Sidecar `logInfo`/`logWarning`/`logError` calls** (`src/backend/logger/index.ts`'s
  `heroicLogWriter`) reach **`~/Library/Logs/GameLib/gamelib.log` ONLY** -- the sidecar's stdout is
  consumed as the RPC frame pipe, never surfaced as human-readable text
  (`sidecar-console-and-logger-are-invisible`).
- **Per-runner log files** (`getRunnerLogWriter`, `src/backend/logger/index.ts:82-94`) write to
  **`~/Library/Logs/GameLib/runners/{legendary,gog,nile}.log`** -- a THIRD sink, distinct from both
  `gamelib.log` and the transcript. `callRunner()` (`src/backend/launcher.ts:1652`) pushes this
  writer into `options.logWriters`, which captures the runner subprocess's raw stdout/stderr --
  including the actual version STRING a `--version` probe prints, which `gamelib.log` itself never
  carries verbatim.
- **`gamelib.log`'s "Running command:" line** (`callRunner()`, `launcher.ts:1703-1704`, via the
  general `logInfo`) DOES carry the fully resolved spawn path (`getRunnerCallWithoutCredentials()`,
  `launcher.ts:1877-1926`, prepends the quoted `runnerPath` before the arguments) -- this is the
  correct sink for proving NESTED PATH RESOLUTION (`darwin/{runner}/{runner}`), but it is NOT the
  sink for the actual version text a probe returns. Items requiring both facts name both sinks.
- **Sidecar STDERR** reaches the session transcript, prefixed `[sidecar:err]`.
- **Sidecar `console.*` calls** reach nothing at all.

## 4. Identifier-redaction discipline (reproduced unchanged)

No cookie value, gamekey, OAuth code, account id, or PKCE material (`code_verifier`, `serial`,
`client_id`) is ever transcribed into this document or any captured artifact -- timestamps,
window/launch ordinals, and file paths only. This binds with particular force on one literal:
`src/backend/storeManagers/nile/user.ts:62`'s `logInfo(['Register data is:', output], LogPrefix.
Nile)` writes the FULL `NileLoginData` object (url, `code_verifier`, `serial`, `client_id`) to
`gamelib.log` in plaintext -- this is PRE-EXISTING behaviour, unrelated to onedir packaging, out
of scope for this phase, and recorded as an out-of-scope finding for a future security pass. Any
future run of item 2 (carried forward, not re-run here, but its own evidence-capture instruction
inherits this rule) must use only this line's TIMESTAMP as its completion marker, never its
contents.

## 5. Absolute-path rule

**Every path this document asks the sidecar to spawn, or asks the operator to type or paste into a
settings field, is written ABSOLUTE.** This is a dedicated rule, not folded into a precondition,
because Deviation 4 (Section 2 above) shows exactly what happens when it is skipped: the FAIL run's
own item-5 contract specified the repo-relative `public/bin/x64/darwin/nile`, but the sidecar spawns
with `cwd=<repo>/src-tauri`, so the path resolved to `src-tauri/public/bin/x64/darwin/nile` and
failed with ENOENT. The absolute path
(`/Users/graysonmitchell/Projects/GameLib/public/bin/x64/darwin/nile`) was substituted live and the
item PASSed on that basis -- a contract-authoring defect, not a product defect, since the real
Settings field is a file picker that only ever yields absolute paths.

**Repo-relative paths appear in this document ONLY inside shell commands that are explicitly stated
to run from the repo root**, and each such command says so at the point it is given (e.g. `pnpm
dist:mac`, `find build/bin/arm64/darwin -type l`, run from
`/Users/graysonmitchell/Projects/GameLib`). No such command's OUTPUT is ever fed into a settings
field or a sidecar-spawn argument without first being captured into an absolute-path variable --
Scored Item 2 below does this explicitly for the built `.app` path before using it in any
`codesign`/`find`/`verify:runner-bundle` invocation.

This rule applies going forward to every item in this document, including the three carried-forward
items' own future re-runs, not only to the two scored items below.

## 6. Dual-sink evidence-capture instruction (reproduced in full, adapted to this contract's own two-launch shape)

### 6.1 One-time session setup, before the first launch

```
export GATE_SESSION_DIR="/tmp/gamelib-gate-$(date -u +%Y%m%dT%H%M%SZ)"
mkdir -p "$GATE_SESSION_DIR"
ls -d /tmp/gamelib-gate-* 2>/dev/null
```
Confirm no prior session directory is reused; note any prior ones (including the FAIL run's own
`/tmp/gamelib-gate-20260810T233351Z`) as unrelated -- do not delete them. Archive any pre-existing
`.old` file before the first launch:
```
[ -f ~/Library/Logs/GameLib/gamelib.log.old ] && cp ~/Library/Logs/GameLib/gamelib.log.old \
  "$GATE_SESSION_DIR/gamelib-launch-0-preexisting-old.log"
```

### 6.2 Single-instance assertion -- before EVERY launch, at window-appearance, and at teardown

**Before the Tauri-dev launch (Launch 1)**, assert zero pre-existing instances:
```
pgrep -f 'target/debug/gamelib-shell'
```
must return nothing. **Before the Electron-packaged launch (Launch 2)**, assert instead:
```
pgrep -f 'GameLib.app'
```
must return nothing. A non-empty result means STOP and resolve it before launching -- never
"proceed and hope." **After the window appears**, record the single PID into the transcript:
```
pgrep -f 'target/debug/gamelib-shell' | tee -a "$GATE_SESSION_DIR/terminal.log"
```
(or the `GameLib.app` pattern for Launch 2). **At teardown**, record it again with the identical
command. **If the count is ever not exactly 1 at any of these three points, the launch is ABORTED
and re-run, and its evidence is NOT SCORABLE** -- nothing from an aborted attempt is archived or
cited. A second concurrent instance splits the `[shell]`/Rust-side sink while `gamelib.log` stays
SHARED, producing a populated, plausible-looking `gamelib.log` next to a transcript missing every
scored `[shell]` literal -- the most dangerous evidence shape this standard exists to prevent.

### 6.3 Per-launch delimiter -- write BEFORE each launch, exact literal text

```
echo "=== GATE LAUNCH ${N} — $(date -u +%Y-%m-%dT%H:%M:%SZ) ===" | tee -a "$GATE_SESSION_DIR/terminal.log"
```
where `${N}` is the launch ordinal (1-2, or more if the Section-2 regression conditional triggers
and items 2/3/5 must be re-run). A reader greps section boundaries with
`grep -n '^=== GATE LAUNCH' "$GATE_SESSION_DIR/terminal.log"`.

### 6.4 The launch command itself -- `tee -a`, never bare `tee`, never `>`

Tauri dev (Launch 1):
```
pnpm tauri:dev 2>&1 | tee -a "$GATE_SESSION_DIR/terminal.log"
```
Electron packaged (Launch 2, Scored Item 2) -- there is no single "launch command" piping a
long-running dev server; instead, the build step itself is captured, then the `.app` is opened
separately:
```
pnpm dist:mac 2>&1 | tee -a "$GATE_SESSION_DIR/terminal.log"
BUILT_APP="$(find "/Users/graysonmitchell/Projects/GameLib/dist" -maxdepth 3 -iname 'GameLib.app' | head -1)"
open "$BUILT_APP" 2>&1 | tee -a "$GATE_SESSION_DIR/terminal.log"
```
`BUILT_APP` is captured into a shell variable as an ABSOLUTE path (the `find` root is itself
absolute) precisely so every subsequent `codesign`/`find`/`verify:runner-bundle` invocation against
it obeys Section 5's rule without re-deriving the path each time.

### 6.5 Per-launch `gamelib.log` archive -- AFTER that launch's app has quit, BEFORE the next launch starts

```
cp ~/Library/Logs/GameLib/gamelib.log "$GATE_SESSION_DIR/gamelib-launch-${N}.log"
```
**Mandatory ordering reason** (`src/backend/logger/log_writer.ts:72-74`): `existsSync(this.
logFilePath)` then `renameSync(this.logFilePath, this.oldLogFilePath)` on the very next process's
FIRST write renames the live file to `.old`. A later launch destroys an earlier launch's sidecar
evidence unless archived first. Also archive per-runner log files the same way, same timing, for
items that name them:
```
cp ~/Library/Logs/GameLib/runners/legendary.log "$GATE_SESSION_DIR/runners-launch-${N}-legendary.log"
cp ~/Library/Logs/GameLib/runners/gog.log       "$GATE_SESSION_DIR/runners-launch-${N}-gog.log"
cp ~/Library/Logs/GameLib/runners/nile.log      "$GATE_SESSION_DIR/runners-launch-${N}-nile.log"
```
(Only copy files that exist -- not every launch touches every runner.)

### 6.6 Per-item launch-ordinal recording

Every item's `Observed:` field (filled only by plan 34.9-16) must record which launch ordinal(s)
covered it.

### 6.7 This contract's own launch plan

| Launch | Covers | Why this position |
|---|---|---|
| 1 | Scored Item 1 (Tauri DEV, nested resolution, all three runners) | Default env, default settings (no `altNileBin` set -- confirmed per precondition 8 below). Establishes the DEV-side regression canary BEFORE Launch 2's build, so if it shows a regression, the Section-2 conditional (re-run items 2/3/5) is known before Launch 2 begins, not discovered after. |
| 2 | Scored Item 2 -- Electron PACKAGED (`pnpm dist:mac` + `find`/`codesign`/`pnpm verify:runner-bundle` against the built app + open the built `.app`) | LAST, and only after a `shasum` of `public/bin/arm64/darwin/{legendary,gogdl,nile}/{legendary,gogdl,nile}` taken at session start (precondition 7) is re-checked immediately before `pnpm dist:mac` runs, because `dist:mac` runs `electron-vite build`, which rewrites `build/` from `public/` -- if the runner tree drifted since session start, Item 2 would be inspecting a different tree than Item 1 measured (Test 5, hazard B, Structural Reachability Review below). |

**Conditional addendum:** if Launch 1 shows a DEV-side regression on any of the three runners, this
launch plan is extended with fresh launches for carried-forward items 2, 3 and 5, using their own
FAIL-run evidence requirements (`34.9-LIVE-GATE.md`'s own items 2/3/5 text) unchanged, before Launch
2 proceeds -- the operator does not proceed to Item 2 with an unconfirmed carry-forward.

### 6.8 Closing inventory -- at session end

```
ls -la "$GATE_SESSION_DIR/"
wc -l "$GATE_SESSION_DIR"/*
```
A zero-length or missing capture is visible immediately, not discovered after the app has quit.

## 7. Preconditions

Each precondition states who satisfies it: **OPERATOR-AT-GATE-TIME** or
**machine-satisfiable-in-preflight**. This plan (34.9-15) runs no preflight -- every status below is
left BLANK for plan 34.9-16's own preflight to fill from a real, measured check.

1. **Clean working tree.** machine-satisfiable (`git status --porcelain`). Status: _______
2. **macOS hardware (arm64), a build at the tip including every commit through plan 34.9-14** (the
   last code-modifying plan in this gap cycle; 34.9-15 itself only adds this document).
   OPERATOR-AT-GATE-TIME. Status: _______
3. **No pre-existing `gamelib-shell` or `GameLib.app` instance.** machine-satisfiable (`pgrep -f
   'target/debug/gamelib-shell'` and `pgrep -f 'GameLib.app'`, both empty). Status: _______
4. **`public/bin/arm64/darwin/{legendary,gogdl,nile}/{legendary,gogdl,nile}` present, executable,
   and real Mach-O.** machine-satisfiable. This remains the ONLY arch available -- per
   `34.9-CI-ROUNDTRIP.md` (Outcome C), `meta/runnersOnedirDigests.json` still holds six
   `PENDING-CI-PUBLISH` sentinels, x64 has no artifact anywhere on this machine or in this repo, and
   no item below presupposes an x64 onedir tree. Status: _______
5. **Do NOT run `pnpm download-helper-binaries` during this gate.** The six `PENDING-CI-PUBLISH`
   sentinels (precondition 4) mean that command throws by design on this checkout and must not be
   relied on or "fixed" mid-gate. The already-staged local arm64 tree must not be deleted or
   overwritten. Status: _______
6. **`npm run test:ci` green.** machine-satisfiable. Recorded as a PRECONDITION only, never cited as
   evidence for any item below (per the standing reference's Section 4 rule ii). Status: _______
7. **A `shasum -a 256` baseline of the three arm64 onedir runner binaries, taken before Launch 1 and
   re-checked before Scored Item 2's `pnpm dist:mac`.** machine-satisfiable, per Section 6.7's hazard
   B. Status: _______
8. **`altNileBin` confirmed EMPTY in `config.json` before Launch 1, POSITIVELY** (not merely assumed
   from the FAIL run's own final state). The FAIL run left it `''`, but per this project's own
   "an emptied app-side store does not prove a clean state" discipline, a positive re-check is
   required here too -- an item-5 override left set would make Scored Item 1's nile resolution read
   the OVERRIDE path instead of the nested default, producing a false regression signal against the
   very item this launch plan uses as its regression canary. machine-satisfiable
   (`grep -o '"altNileBin":"[^"]*"' config.json`, or equivalent). Status: _______
9. **The dual-sink evidence-capture instruction (Section 6) is fully in place before Launch 1.**
   OPERATOR-AT-GATE-TIME. Status: _______

**Two requirements this contract does not re-state because they are source-closed, not
gate-dependent (unchanged from `34.9-LIVE-GATE.md`):** the Epic-exclusion scope fence (no code path
in this phase touches Epic login) and the runner-version-pin invariant (`RELEASE_TAGS` unchanged
this phase).

## 8. Scored Item 1 -- Tauri DEV, nested resolution (all three runners)

**Launch:** 1. **Gesture:** navigate to Settings > Advanced Settings (the page hosting
`AltLegendaryBin`, `AltGOGdlBin`, `AltNileBin` -- re-confirmed at THIS authoring against current
source, `src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx:180-184`, all three
rendered unconditionally, no feature flag or platform guard around any of the three). Each
component's `useEffect` fires its version-probe IPC call (`getLegendaryVersion`/`getGogdlVersion`/
`getNileVersion`) on mount, with no login and no credential required.

**Framing, carried forward from the FAIL run's own correction (F-34.9-G1-01) -- not "on startup."**
The sidecar's own entrypoint (`src/backend/sidecar/bootstrap.ts:84`, `initHeadless as initLogger`)
explicitly SKIPS the one-time system-info dump Electron's real `init()` performs at
`app.whenReady()`. Under Tauri dev, nothing calls the three version probes at boot -- they are
gated behind the Settings > Advanced Settings UI mount, as established during the FAIL run's own
authoring and re-confirmed here.

**Required evidence, two sinks:**
1. `gamelib.log` (sidecar sink): three "Running command:" lines (`launcher.ts:1703-1704`), one per
   runner, each containing a resolved path matching `darwin/legendary/legendary`,
   `darwin/gogdl/gogdl`, and `darwin/nile/nile` respectively (the nested onedir shape
   `archSpecificBinary()` produces, `src/backend/utils.ts:524-526`). Grep:
   `grep -c 'darwin/legendary/legendary' "$GATE_SESSION_DIR/gamelib-launch-1.log"` (repeat per
   runner) -- each must be >= 1.
2. Per-runner log files (`~/Library/Logs/GameLib/runners/{legendary,gog,nile}.log`, archived per
   Section 6.5) AND the Settings page's own visible "<Runner> Version:" text next to each `Alt*Bin`
   field: both must show a REAL version string (legendary matching `"0\.20\.43"`, gogdl matching
   `1\.2\.1`, nile matching `1\.1\.2`) and the UI field must NOT read "Invalid"
   (`AltLegendaryBin.tsx:17-23` sets exactly that string on failure).

**PASS** when both sinks confirm all three runners: nested-path resolution in `gamelib.log` AND a
real, non-"Invalid" version visible via the per-runner log or the Settings UI (record which). **FAIL**
otherwise, naming which runner and which sink failed -- and per Section 2's conditional, a FAIL here
routes not only to another gap cycle (Section 12) but ALSO invalidates the carry-forward argument
for items 2, 3 and 5, requiring their own re-run before the phase closes regardless of Scored Item
2's own outcome.

**Observed: (unfilled — plan 34.9-16)**

## 9. Scored Item 2 (was Item 4) -- Electron PACKAGED

**Launch:** 2 (LAST). **Gesture:** run `pnpm dist:mac`, then the on-disk shape checks below, then
`pnpm verify:runner-bundle`, then open the built `.app`, navigate to Settings > Advanced Settings
(same gesture as Scored Item 1).

This is the item that FAILed. Every criterion below is about the artifact's OWN on-disk shape,
never a command's self-report -- per F-34.9-02 (a mutating command's own success report is not
proof of its effect) and the FAIL run's own false-comfort lesson (`codesign --verify --deep` on the
inner binary exited 0 while the framework it belonged to was already malformed).

**Precondition specific to this item, re-verified immediately BEFORE `pnpm dist:mac` runs (hazard
B, Structural Reachability Review below):**
```
shasum -a 256 /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/legendary/legendary \
  /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl/gogdl \
  /Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/nile/nile
```
must match the session-start baseline (precondition 7). If it does not, ABORT Scored Item 2 as
unscorable and record why -- do not proceed on a tree different from the one Scored Item 1 measured.

**Required evidence, all against the artifact's own shape:**

1. **Record a UTC `BUILD_START` timestamp** immediately before `pnpm dist:mac` runs, into the
   transcript: `date -u +%Y-%m-%dT%H:%M:%SZ`.
2. **`pnpm dist:mac` exits 0.** Note in the transcript that `dist:mac` now runs `pnpm
   clean:dist-mac` first (34.9-14), so `dist/` starts with no macOS artifacts -- and that this is
   precisely why the newness check below (step 3) is still required rather than redundant: a clean
   that silently failed (as `cleanDistMac`'s own first live run did, per its Summary's Deviation 1,
   before it was caught and fixed) would leave the F-34.9-02 trap intact.
3. **A `.dmg` AND a `.zip` matching `GameLib-*-macOS-arm64.*` exist in `dist/` with `mtime` STRICTLY
   AFTER `BUILD_START`.** This is F-34.9-02's own trap by name -- "a dmg exists" is NOT the
   criterion; only an mtime strictly newer than the recorded build start is.
   ```
   find /Users/graysonmitchell/Projects/GameLib/dist -maxdepth 1 -name 'GameLib-*-macOS-arm64.*' -newermt "$BUILD_START" -print
   ```
   (or an equivalent explicit mtime comparison) must list both files.
4. **`find "$BUILT_APP/Contents/Resources/app.asar.unpacked/build/bin/arm64/darwin" -type l | wc -l`
   equals exactly 12.** This is the direct on-disk proof that the symlinks survived
   electron-builder's own copy -- a check the FAIL run never had. `12` is the expected PRESENT
   count (4 symlinks x 3 runners); the known-bad counterpart, per 34.9-12's own measurement before
   its fix landed, is `0` (every symlink dereferenced into a real file/directory in `build/`).
5. **`codesign -dv` against the framework BUNDLE, not the inner binary:**
   ```
   codesign -dv "$BUILT_APP/Contents/Resources/app.asar.unpacked/build/bin/arm64/darwin/gogdl/_internal/Python.framework"
   ```
   Record its output as DATA and assert it contains **zero** occurrences of `bundle format is
   ambiguous`. Why the bundle and not the binary: the FAIL run's `codesign --verify --deep` on
   `.../gogdl/gogdl` exited 0 while the framework was already malformed, and that false comfort is
   the lesson this criterion exists to not repeat -- verifying a nearby artifact is not verifying
   the one the toolchain actually classifies and signs as a unit.
6. **`pnpm verify:runner-bundle "$BUILT_APP" --arch=arm64` exits 0.** Its per-file signature-state
   report is DATA, never a pass condition (D-03/D-04 -- no Apple Developer credentials enrolled);
   `unsigned`/`adhoc` results are expected. Its framework-structure enforcement (34.9-13) IS a pass
   condition -- this is the automated tripwire that, per Section 1 above, now fires on the exact
   F-34.9-01 shape if it regresses.
7. **`du -sk` of the three runner directories inside the packaged app sums to within 10% of the
   `public/bin/arm64/darwin` source sum (~84M).** Record both sums. This is **F-34.9-03**
   corroboration that the symlinks genuinely survived (rather than being duplicated as real files
   again), not an independent criterion on its own.
8. **Open `$BUILT_APP`, navigate to Settings > Advanced Settings, and repeat Scored Item 1's
   two-sink check** against the PACKAGED app's own `gamelib.log` and per-runner logs, with the
   resolved paths now under
   `Contents/Resources/app.asar.unpacked/build/bin/arm64/darwin/{legendary,gogdl,nile}/{legendary,gogdl,nile}`.

**PASS** requires ALL eight numbered criteria above. **FAIL** otherwise, naming which criterion
failed and citing its number.

**Observed: (unfilled — plan 34.9-16)**

## 10. Carried-forward items 2, 3 and 5

Not re-run this gate. Reproduced here with their 2026-08-11 verdicts, launch ordinals, and evidence
file names, followed by the non-invalidation argument and, for item 5, both of its contract
amendments.

### Item 2 -- Amazon (nile) login click spawns fast

2026-08-11 verdict: PASS (interval <1s against a 2s bar), launch ordinal 2 of that run. Evidence:
`gamelib-launch-2.log` (that session's archive, distinct from this contract's own `GATE_SESSION_DIR`
naming). Mechanism: `Runner.handleLogin()` -> `/loginweb/nile` -> `useTauriOAuthLogin` ->
`NileUser.getLoginData()` (`src/backend/storeManagers/nile/user.ts:42-77`) -> `runRunnerCommand`,
spawning through the same nested `darwin/nile/nile` path Scored Item 1 above independently
re-confirms. **Non-invalidation:** this item's evidence path is exclusively DEV-sidecar `publicDir`
resolution (Section 2 above); the gap cycle's fix never touches `public/` or the DEV code path.

### Item 3 -- GOG (gogdl) capture -> login

2026-08-11 verdict: PASS (interval 1s against a 2s bar), launch ordinal 2 of that run. Evidence:
`gamelib-launch-2.log`, `runners-launch-2-gog.log`. Mechanism: `[oauthLoginCapture]
runner=gog status=captured` (`src/backend/sidecar/oauthLoginCapture.ts:235-238`) precedes
`GOGUser.login()`'s `runRunnerCommand`, spawning through the nested `darwin/gogdl/gogdl` path.
**Non-invalidation:** identical argument to item 2 -- disjoint credential store, same DEV-only
evidence path, untouched by this gap cycle's `build/`/`dist/`-only fix.

### Item 5 -- `altNileBin` override

2026-08-11 verdict: PASS (three-state sequence -- default, override, restored -- all inside one
process), launch ordinal 4 of that run. Evidence: `gamelib-launch-4-final.log`,
`runners-launch-4-nile.log`, `config-launch-4-override-set.json`,
`config-launch-4-override-cleared.json`. **Non-invalidation:** same argument as items 2/3 -- the
override mechanism (`getNileBin()`, `src/backend/utils.ts:625-634`) is DEV-sidecar-side config
resolution, untouched by this gap cycle.

**Item 5's two contract amendments for any FUTURE run**, recorded here rather than left to be
rediscovered:
1. **Absolute-path requirement (Section 5 above).** Any future contract must specify
   `/Users/graysonmitchell/Projects/GameLib/public/bin/x64/darwin/nile` for the override field, never
   the repo-relative form -- the sidecar's `cwd=<repo>/src-tauri` makes a relative path resolve
   wrong and fail ENOENT, as the FAIL run's own item 5 discovered live.
2. **F-34.9-04, in its own words: the Settings version field cannot discriminate override from
   default for nile.** Both the x64 onefile and the arm64 onedir report `1.1.2 Will A. Zeppeli` --
   only the spawned path in `gamelib.log` separates them. Any future run of item 5 must use the
   spawned-path discriminator, never the version string, as its PASS criterion for distinguishing
   override-active from override-restored.

## 11. Unscored item -- Tauri PACKAGED

**NOT ATTEMPTED. NOT SCORED. Contributes to neither `items_total` nor the verdict**, unchanged from
`34.9-LIVE-GATE.md`. Blocked on `R-34.5-G1-PKG` (Phase 34.5's `deferred-items.md`, item 12):
`electronStub.app.isPackaged` is hardcoded `false` under the sidecar, so `publicDir`
(`src/backend/constants/paths.ts:73-76`) unconditionally appends `'public'` regardless of packaged
state, and a packaged Tauri app's `resource_dir()` has no `public/` child -- staging the runner tree
via `bundle.resources` (Phase 34.9's own `src-tauri/tauri.conf.json` change) is necessary but not
sufficient, per `34.9-PACKAGING-LIMITATIONS.md`. This gap cycle does not touch `isPackaged` or
`publicDir`. Whichever plan first exercises a packaged (non-dev) Tauri build is the correct place to
close this residual; Phase 34.9 explicitly is not that plan.

## 12. Verdict rule

**No-partial-pass across the two scored items.** The phase closes only if BOTH Scored Item 1 and
Scored Item 2 PASS. Any FAIL routes to another gap cycle, per this project's standing D-08
discipline. The unscored item, the carried-forward items (unless the Section 2 conditional
triggers their own re-run and one of THEM then FAILs, in which case they count too), and the
BLOCKED-UNPROVEN x64 leg are never counted toward this bar on their own.

## 13. What this gate does NOT prove

- **Tauri-PACKAGED runner resolution** -- blocked on `R-34.5-G1-PKG`; Section 11 states this
  explicitly.
- **Real-certificate signing and notarization** -- D-03/D-04 deliberately defer Apple Developer
  Program enrollment; this repo has never exercised real signing. Note that F-34.9-01 made real
  notarization strictly worse than unproven while it stood: a framework failing ADHOC signing would
  not have survived real signing either. The honest proxy remains `codesign --verify` plus
  `verify:runner-bundle`'s per-file signature-state report, both DATA, never pass conditions.
- **The x64 leg, in full.** BLOCKED-UNPROVEN: no x64 onedir artifact exists anywhere (`34.9-CI-
  ROUNDTRIP.md` Outcome C -- CI never dispatchable, PyInstaller cannot cross-compile). No item in
  this document exercises x64.
- **The digest-verified download -> verify -> extract path.** `meta/runnersOnedirDigests.json`
  still holds six `PENDING-CI-PUBLISH` sentinels; `pnpm download-helper-binaries` throws by design
  and is not run during this gate (precondition 5).
- **Windows and Linux behaviour** -- unchanged by this phase, not exercised by any item here.
- **Any cold-spawn ratio claim.** `34.9-MEASUREMENT.md`'s cold ratio is UNMEASURED (two void runs);
  only the WARM ratio (26.6x-32.5x) is measured and valid. This document adds no new timing
  measurement of its own; the FAIL run's items 2/3's timing evidence (carried forward, Section 10)
  is unaffected by this gap cycle's fix.

---

## Structural Reachability Review

Per the standing reference (`.claude/skills/spike-findings-gamelib/references/
live-gate-contract-authoring.md`), applied to both scored items, every precondition, and the
evidence-capture instruction itself. Every cell states a specific reason with a file:line or a
command -- never a bare mark.

| Item/precondition | Test 1 (origin/scheme) | Test 2 (concurrency) | Test 3 (log-line + SINK) | Test 4 (absence-observability) | Test 5 (interaction) | Test 6 (pre-existing external state) | Test 7 (UI-level) |
|---|---|---|---|---|---|---|---|
| Scored Item 1 | REACHABLE -- Settings is a local route, no origin/scheme involved. | REACHABLE -- single-window app; the three `useEffect` fires run independently, no ordering dependency. | REACHABLE -- path-resolution literal is sidecar-emitted, read from `gamelib.log`; version-string literal is sidecar-emitted but read from the per-runner log / UI, not `gamelib.log`'s own "Running command:" line -- both confirmed distinct real emitters (Section 3). | N/A -- Item 1 asserts presence, not absence, for its own criteria. Its role AS a regression canary (Section 2) is itself a presence check ("does the new plugin's `closeBundle` step break DEV resolution"), also presence-shaped. | See Test 5 pairwise pass, below. | REACHABLE, resolved by precondition 8's positive `altNileBin`-empty re-check -- without it, a left-set override from a prior session would make nile's resolution read the OVERRIDE path, producing a false regression signal against this item's own role as Scored Item 2's precondition. | REACHABLE, re-confirmed AT THIS AUTHORING against current source: `src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx:180-184` renders `<AltLegendaryBin />`, `<AltGOGdlBin />`, `<AltNileBin />` unconditionally, no platform/feature guard around any of the three (verified by direct read, not by citing the FAIL run's own claim). |
| Scored Item 2 | REACHABLE -- `pnpm dist:mac`, `find`, `codesign`, `pnpm verify:runner-bundle`, and `open` all operate on local paths; no origin/scheme involved. | REACHABLE -- the packaged `.app` is a wholly separate process from the Tauri dev binary from Launch 1; the single-instance assertion is scoped per-binary-pattern (`GameLib.app` vs. `target/debug/gamelib-shell`), so no cross-contamination between the two launches is possible. | REACHABLE -- identical sink shape to Item 1 for criterion 8 (both literals sidecar-emitted, read from the PACKAGED app's own `gamelib.log`, which is the same file path regardless of which host process wrote it -- `getBaseLogPath()` is host-agnostic). Criteria 3-7 are filesystem/CLI-exit-code checks, not log-line checks, so Test 3's SINK clause does not apply to them directly -- each names its own command as its evidence source. | REACHABLE and load-bearing -- criterion 5's "zero occurrences of `bundle format is ambiguous`" is an absence assertion whose PRESENCE case is independently proven observable: the FAIL run's own `terminal.log` recorded exactly that string against exactly this `codesign` invocation on the unfixed tree. An absence assertion whose presence case was never observed would prove nothing; here it was observed, which is what makes this criterion legitimate rather than vacuous. | See Test 5 pairwise pass, below -- this is the item most heavily constrained by Test 5 (hazard B). | REACHABLE, resolved by the mandatory `shasum` re-check (precondition 7, re-verified inside Item 2's own text) -- the external state at risk (`public/bin` drifting between session start and `pnpm dist:mac`) is independently measured, not assumed unchanged. | REACHABLE -- Settings > Advanced Settings (criterion 8) is reachable identically to Item 1 inside a packaged build; no packaging-specific route change exists in this phase. |
| Precondition 3 (no pre-existing instance) | N/A -- a `pgrep` check, no origin/scheme. | REACHABLE -- `pgrep -f` patterns are disjoint per binary (`gamelib-shell` vs `GameLib.app`), so checking one never masks the other. | REACHABLE -- `pgrep`'s own exit code/output is the evidence, no log-line sink involved. | REACHABLE -- an empty `pgrep` result IS the required absence; a non-empty result is the observable presence case, proven reachable by the FAIL run's own precedent of this exact check catching nothing anomalous across its five launches. | See Test 5 pairwise pass. | REACHABLE, and this precondition IS the external-state check itself -- it directly measures whether a stale process instance (external state from a prior session) exists, rather than assuming a clean state. | N/A -- not a UI gesture. |
| Precondition 7 (shasum baseline + re-check) | N/A. | REACHABLE -- `shasum` is a read-only hash over the three binaries, no concurrency hazard of its own. | REACHABLE -- `shasum`'s own stdout is the evidence, transcribed directly into the transcript, no application sink involved. | REACHABLE -- a MISMATCH between the two `shasum` runs is the presence case this precondition exists to catch (the tree drifting mid-session); an unchanged tree (the expected case) produces a byte-identical second hash, itself directly comparable. | See Test 5 pairwise pass -- this precondition IS hazard B, stated in full below. | REACHABLE -- this precondition's entire purpose is to measure whether `public/bin/arm64/darwin` (external state relative to the gate's own preflight) drifted between session start and `pnpm dist:mac`, rather than assuming it did not. | N/A -- not a UI gesture. |
| Precondition 8 (`altNileBin` empty, positive re-check) | N/A. | REACHABLE -- a single `config.json` read, no concurrency hazard. | REACHABLE -- `config.json`'s own on-disk content is the evidence, not an application log sink. | REACHABLE and load-bearing, mirroring the general Test-6 rule applied to Test 4's shape: "no override is set" is proven by reading the actual field value (a positive observable), never by inferring cleanliness from the FAIL run's own final state (which this contract does NOT trust as a starting assumption, per the standing "an emptied app-side store does not prove restoration" discipline). | See Test 5 pairwise pass. | REACHABLE, and this precondition IS the external-state check -- `config.json` persists across sessions/restarts, so a prior run's override setting the field and this contract's own author never re-verifying it live would be exactly the F-34.4.2-16-shaped gap (an app-side store checked, but not re-confirmed at THIS run's actual start). | N/A -- not a UI gesture. |
| Section 6 evidence-capture instruction (as a whole) | N/A -- shell/filesystem operations only. | REACHABLE -- the two-launch shape has no concurrent capture requirement; each launch's capture completes (archived) before the next launch begins, per Section 6.5's own ordering rule. | REACHABLE -- Section 6 explicitly assigns each literal to its correct sink (Section 3) before ever asking for it; this is the concrete fix Test 3's SINK clause exists to enforce, applied at authoring rather than discovered at run time. | N/A -- the instruction itself asserts no absences; individual items built on top of it do (covered in their own rows). | See Test 5 pairwise pass -- Section 6.5's archive-before-next-launch rule and Section 6.2's single-instance-or-abort rule are themselves the resolution to two of the interaction hazards enumerated below. | REACHABLE -- Section 6.1's `ls -d /tmp/gamelib-gate-*` check names the FAIL run's own prior session directory as pre-existing external state and explicitly instructs it be left alone, not reused. | N/A -- not a UI gesture. |

### Test 3 + Test 4 detail on Scored Item 2's `bundle format is ambiguous` absence assertion

Restated per the plan's own instruction: this is Test 4's clearest positive case in this document.
The PRESENCE case is not hypothetical -- it is the literal, verbatim FAIL-run transcript line:
```
.../Python.framework/Python: bundle format is ambiguous (could be app or framework)
```
recorded in `34.9-LIVE-GATE.md`'s own item-4 FAIL text against the SAME `codesign` invocation this
contract's criterion 5 re-runs. An absence assertion whose presence case was never observed on this
exact surface would prove nothing; here it was observed once already, which is exactly what makes
re-asserting its absence now a meaningful regression check rather than a vacuous one.

### Test 4 on the symlink count

`12` is Scored Item 2 criterion 4's presence assertion; its known-bad counterpart is `0`, measured
directly by plan 34.9-12's own pre-fix `find build/bin/arm64/darwin -type l | wc -l` run (per its
Summary's Verification Evidence) before the `closeBundle` plugin was wired in. Both numbers are
independently observed, not inferred.

### Test 6 on `altNileBin`

Covered in the precondition-8 table row above: the premise "no override is set" is proven by a
positive read of `config.json`'s actual field value at THIS session's start, never by trusting the
FAIL run's own recorded final state (which is external, prior-session state relative to this
contract's own preflight).

### Test 7 on Scored Item 1's gesture

Traced at this authoring, by direct read of current source (not by citing the FAIL run's own
claim): `src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx` lines 180, 182, 184
render `<AltLegendaryBin />`, `<AltGOGdlBin />`, `<AltNileBin />` respectively, each unconditionally
(no `isWindows`/`isLinux`/feature-flag guard around any of the three -- contrast line 196's
`{isLinux && <ShowValveProton />}`, which IS conditionally guarded, immediately adjacent, showing the
distinction is deliberate in this file, not an oversight in the check). All three components still
render at the location the FAIL run's own contract named, and still fire their version-probe IPC
calls on mount per their own `useEffect` hooks (unchanged since the FAIL run; this gap cycle's three
plans touch `electron.vite.config.ts`, `meta/verifyRunnerBundle.ts`, and `meta/cleanDistMac.ts`
only, none of which is `AdvancedSettings/index.tsx` or any `Alt*Bin.tsx` component).

### Test 5 -- pairwise pass

**Reduction applied, stated explicitly per the standing reference's scoping rule:** the
state-mutating requirements in this contract are {precondition 7's `shasum` baseline being subject
to drift, Section 6.5's `gamelib.log` rename-to-`.old` on each new process's first write, `pnpm
clean:dist-mac`'s deletion of `dist/` macOS artifacts, `pnpm dist:mac`'s own `electron-vite build`
step rewriting `build/` from `public/`, and the app-quit/relaunch boundary between Launch 1 and
Launch 2}; the evidence-bearing requirements are {Scored Item 1's two-sink check, Scored Item 2's
eight numbered criteria, and each precondition's own status field}. This is a contract with 5
state-mutating requirements and 9 evidence-bearing requirements (2 items + up to 9 precondition
statuses, collapsed to the 2 items plus the shasum/`altNileBin` precondition pair that are
themselves evidence-bearing in their own right) -- per the M×N reduction, every {state-mutating}
requirement was considered against every {evidence-bearing} requirement it could plausibly touch,
not the full cross product. **11 pairs were considered; 2 were flagged** (both already resolved
in-document, both restated here so "found and fixed" stays distinguishable from "did not look"):

| Pair (state-mutating -> evidence-bearing) | Does A destroy B's evidence? |
|---|---|
| `gamelib.log` rename-to-`.old` -> Scored Item 1's two-sink check | No, by construction -- Section 6.5 mandates archiving `gamelib-launch-1.log` before Launch 2 begins, i.e. before any second process's first write could trigger the rename. |
| `gamelib.log` rename-to-`.old` -> Scored Item 2's criterion 8 (packaged two-sink check) | No -- Launch 2 is the LAST launch in this contract's base plan; nothing after it depends on `gamelib.log`'s content, so its own eventual rename by a hypothetical future process is moot within this run. |
| `pnpm clean:dist-mac`'s `dist/` deletion -> Scored Item 2 criterion 3 (mtime newness check) | **YES if the clean silently no-ops, RESOLVED by criterion 3 itself being a positive mtime check, not a "did clean report success" check.** This is precisely F-34.9-02's own shape, and precisely the failure mode `cleanDistMac`'s own first live run actually hit (Summary Deviation 1: a `__dirname`-relative default pointed at `../dist` and reported success while removing nothing). Criterion 3 does not trust `clean:dist-mac`'s exit code; it demands `mtime` strictly after `BUILD_START`, which a silently-no-op'd clean cannot satisfy if `dist/` already held stale artifacts with old mtimes. |
| `pnpm dist:mac`'s `electron-vite build` rewriting `build/` from `public/` -> Scored Item 2 criteria 4/5/6/7 (all read `build/`'s packaged descendant) | **YES if `public/bin/arm64/darwin` drifted since Scored Item 1's own launch -- RESOLVED by precondition 7's shasum re-check, performed immediately before `pnpm dist:mac` runs (Section 6.7's own hazard-B text).** Without this, Item 2 could package and inspect a tree different from the one Item 1 measured, with neither item's own evidence flagging the discrepancy. |
| App-quit/relaunch boundary (Launch 1 -> Launch 2) -> Scored Item 1's own two-sink check | No -- Item 1's evidence is archived (Section 6.5) before Launch 1's app quits and before Launch 2 begins; the relaunch cannot retroactively alter already-archived files. |
| App-quit/relaunch boundary -> precondition 8 (`altNileBin` positive re-check) | No -- precondition 8 is checked BEFORE Launch 1, not between launches; the relaunch boundary this pair concerns is after Item 1's evidence is already captured. |
| `pnpm clean:dist-mac`'s `dist/` deletion -> precondition 7 (shasum baseline) | No -- `clean:dist-mac` only touches `dist/`, never `public/bin`; the shasum baseline's subject tree is untouched by this deletion. |
| Launch 2's own `pnpm dist:mac` -> Scored Item 1's evidence (already archived) | No -- Item 1's launch and evidence-archival both complete before Launch 2 begins (Section 6.7's ordering), so nothing Launch 2 does can retroactively invalidate already-archived Launch-1 files. |
| Precondition 3's pre-launch `pgrep` check -> itself, across the two launches | No conflict -- each launch's `pgrep` pattern is disjoint (`gamelib-shell` vs `GameLib.app`), so satisfying the check before Launch 1 does not consume or invalidate the check owed before Launch 2. |
| `pnpm verify:runner-bundle`'s own read of the built `.app` -> Scored Item 2 criterion 4 (`find -type l` count) | No -- `verify:runner-bundle` is read-only against the built tree (confirmed by its own Summary: "credential-free structure checks"); it does not mutate `build/bin` or the packaged app, so running it does not affect the `find`-based symlink count criterion run alongside it. |
| `codesign -dv` (criterion 5) -> `pnpm verify:runner-bundle`'s own internal `getSignatureState()` read (criterion 6) | No -- both are read-only inspections of the same already-signed (adhoc) artifact; `-dv` is a display/verify-only invocation, not a re-signing one (unlike the FAIL run's own item-4 attempt, which used `--sign` and DID mutate the artifact -- but that mutation happened during `pnpm dist:mac` itself, already accounted for in the `electron-vite build` pair above, not as a separate criterion-5/criterion-6 interaction). |

**Hazard B, restated once more explicitly per the standing reference's own instruction (not merely
inside the pairwise table), since it is a property of Scored Item 2 in isolation against its own
precondition rather than a single ordered pair:** `pnpm dist:mac` (Scored Item 2's own required
action) runs `electron-vite build`, which rewrites `build/` from `public/`. If `public/bin/arm64/
darwin/{legendary,gogdl,nile}` drifted between session start (when Scored Item 1 measured it) and
the moment `pnpm dist:mac` runs, Scored Item 2 would package and inspect a DIFFERENT tree than
Scored Item 1 measured, without either item's own evidence flagging the discrepancy on its own.
Resolved by precondition 7's `shasum` baseline, re-checked immediately before `pnpm dist:mac` (also
stated inside Section 9's own text, not only here).

## Authoring self-check

Assertions this plan's own executor performed, recorded as this plan's own evidence rather than
left implicit:

- **Commands this plan did NOT run:** `pnpm tauri:dev`, `pnpm dist:mac`, `pnpm verify:runner-bundle`
  against any built app, `pnpm clean:dist-mac`, any harness, any app launch. This plan's only Bash
  invocations were read-only (`gsd-sdk query ...`, `git status`/`git log`, `graphify query`) and file
  authoring (`Write`/`Edit` against this document only).
- **No `verdict`/`run_date`/`items_passed`/`items_failed`/`Observed:` value was written**, other than
  the frontmatter's `verdict: PENDING` and `run_date: null` (both explicitly unfilled states, not
  results) and the two per-item unfilled markers in Sections 8 and 9 (each reading identically, an
  explicit placeholder rather than any result).
- **`git log --stat` for this plan's commits shows changes to exactly one file**,
  `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/
  34.9-LIVE-GATE-RERUN.md` -- verified by `git show --stat` against this plan's own task commits
  after each one, per this plan's task-commit protocol.
- **`git status --porcelain` for `34.9-LIVE-GATE.md` was checked empty** before and after this
  plan's own commits -- the FAIL run's record was never touched.

**This plan ran no gate item, launched no harness, and wrote no result field.**
