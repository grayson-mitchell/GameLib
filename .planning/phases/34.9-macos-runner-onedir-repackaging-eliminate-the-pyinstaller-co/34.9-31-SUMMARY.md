---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 31
subsystem: infra
tags: [proof-contract, author-runner-separation, concurrency, esbuild, wrapper-conversion]

# Dependency graph
requires:
  - phase: 34.9 gap cycle 4 plan 29
    provides: meta/runTs.cjs (the compile-to-private-tmpdir wrapper this contract proves) and its
      authoritative 15/15 converted-script census (34.9-29-SUMMARY.md)
  - phase: 34.9 gap cycle 3
    provides: C3-01 itself (34.9-REVIEW-CYCLE3.md, reviewer 60/60 cross-contamination) and its
      independent re-reproduction (34.9-VERIFICATION.md, 20/20)
provides:
  - 34.9-WRAPPER-PROOF.md, an unfilled, runnable three-direction proof contract (Direction A:
    30 compile-failure-propagation rows; Direction B: 11 healthy-run rows; Direction C: a 60-trial
    concurrency race check plus a mandatory positive control) for plan 34.9-32 to run on real
    macOS arm64 hardware
affects: [34.9 gap cycle 4 plan 32 (runs this contract), plan 33 (ledgers C3-01 based on its result)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A proof contract's positive control must reproduce the DEFECT on the RETIRED idiom in the
      same session, or the direction it gates is scored INVALID, not PASS -- zero cross-
      contamination is what both a fixed defect AND a broken measuring harness report"
    - "A contract-authoring plan validates its own check SHAPES (specimen validation) using
      single, sequential, throwaway-file invocations of the real tool under proof; it does not
      execute the concurrency harness itself, even against scratch files, when the plan text
      forbids concurrency harness execution -- the harness's DETECTOR LOGIC can still be
      validated via manually-fabricated fixture pairs that are never run concurrently"
    - "SIGKILL constructs a legitimate 'failing' specimen for a cleanup-on-exit check without
      mutating the code under test -- no process can catch SIGKILL, so interrupting a slow
      specimen mid-run reliably produces a leaked artifact the check must then correctly flag"

key-files:
  created:
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-WRAPPER-PROOF.md
  modified: []

key-decisions:
  - "Independently re-derived the runTs.cjs script census from the LIVE package.json before writing
    a single direction, per this plan's own must-resolve-first instruction. Result: 15, agreeing
    with 34.9-29-SUMMARY.md's own reported count and disagreeing with this plan's own frontmatter
    key-link (which cited a stale 14). Both numbers are stated explicitly in the document rather
    than one being silently adopted; they happen to agree."
  - "directions_total (43 = 30 Direction A + 11 Direction B + 2 Direction C) is filled in the
    frontmatter despite being listed among this plan's own 'UNFILLED result fields' instruction,
    because it is analytically computable from the contract's own structure without running
    anything -- unlike verdict/directions_passed/directions_failed/session_dir/findings/runner,
    which all require a live run. This is stated explicitly in the frontmatter's own notes field
    as a deliberate, reasoned exception, not an oversight or a separation violation."
  - "build:sidecar-sea is included in Direction B as a FULL RUN (not excluded for network reasons,
    unlike 34.9-PIPE-PROOF.md's treatment of the same script), because meta/buildSidecarSea.ts's
    cross-arch download path only fires when the target triple differs from the host triple --
    on real arm64 hardware building for arm64 it never reaches the network, confirmed both by
    reading the source (lines 552-592) and by 34.9-29-SUMMARY.md's own live end-to-end run of
    this exact script on this exact machine shape."
  - "verify:updater-key is excluded from Direction B for a CREDENTIAL reason (no
    TAURI_SIGNING_PRIVATE_KEY enrolled), not a network or destructive reason -- it is read-only
    and network-free, so it is SAFE to invoke, but cannot demonstrate the exit-0 PASS bar without
    a secret this machine does not have. Direction A (compile-failure propagation) still fully
    covers it; only its Direction B healthy-run behaviour is unproven."
  - "build-runners-onedir is scored as PARTIAL RUN in Direction B (argv-arrival only, terminated by
    SIGTERM the instant its first stdout line -- 'Building <runner> ... for <arch>...',
    meta/buildRunnersOnedir.ts:796-798 -- is captured, strictly before any network/destructive
    work begins), per this plan's own 'at whatever depth its exclusion reason permits' instruction
    for the second required argv-forwarding call site."

requirements-completed: [REQ-34.9-08]

# Metrics
duration: ~50min (research + authoring + specimen validation + commits)
completed: 2026-08-14
---

# Phase 34.9 Plan 31: Author 34.9-WRAPPER-PROOF.md, the runTs.cjs three-direction proof contract Summary

**`34.9-WRAPPER-PROOF.md` authored end to end -- Direction A (30 rows: 15 scripts x S1/S2 broken-compile shapes), Direction B (10 full-run + 1 partial-run scripts, 4 named exclusions), Direction C (a 60-trial concurrency race check with a MANDATORY positive control against the retired shared-outfile idiom, scored INVALID not PASS if the control fails to reproduce), plus a Contract Validation section whose 6 distinct check shapes were each proven against a real passing AND a real deliberately-failing specimen using the actual `meta/runTs.cjs`/`esbuild` -- all on throwaway scratchpad files, never the repo, never concurrently. This plan ran none of the directions it authored.**

## Performance

- **Duration:** ~50 min (research, independent census re-derivation, specimen validation work, document authoring, verification)
- **Tasks:** 2 (both completed)
- **Files created:** 1 (`34.9-WRAPPER-PROOF.md`, 544 lines)

## THIS PLAN RAN NO DIRECTION IT AUTHORED

Stated explicitly, per this plan's own `<objective>` and FORBIDDEN banner:

- **No injection into any `meta/*.ts` file.** `git status --porcelain -- meta/ package.json` confirmed empty both before authoring began and after every commit.
- **No invocation of any converted `pnpm <script>`, and no `pnpm dist:mac`.** Every command run during authoring invoked either `node meta/runTs.cjs <scratchpad-file>` directly (never a `package.json` script name) or was a pure read (`git status`, `grep`, `node -e` parsing `package.json`).
- **No concurrency harness execution of any kind**, including against throwaway scratch files. Direction C's cross-contamination DETECTOR LOGIC was validated using manually-fabricated fixture file pairs (`c-good/`, `c-bad/`) that were never run concurrently -- only their static content was compared, which is a strictly weaker (and permitted) operation than running the harness itself.
- **No `verdict`/`run_date`/`directions_passed`/`directions_failed`/`session_dir`/`findings`/`runner`/any `Observed:` field was written.** `directions_total: 43` is the sole filled result field, and it is filled deliberately -- it is an analytical count over the contract's own numbered directions (computable from the document's own structure, no execution required), documented explicitly as an exception in the frontmatter's `notes` field rather than silently smoothed in.

## Independent census re-derivation (must-resolve-first requirement)

Before writing a single direction, this plan re-derived the wrapped-script count from the CURRENT `package.json`, programmatically, using the defect's own predicate (`/node meta\/runTs\.cjs/` matched against every script string):

```
$ node -e "const s=require('./package.json').scripts;
  console.log(Object.entries(s).filter(([,v])=>/node meta\/runTs\.cjs/.test(v)).length)"
15
```

**Result: 15**, agreeing with `34.9-29-SUMMARY.md`'s own reported count and disagreeing with this plan's own frontmatter `<must_haves>` key-link, which cited a stale **14**. Both numbers are stated explicitly in the contract document (not silently adopted); a corroborating `grep -c "node meta/runTs.cjs" package.json` also returns 15, and `grep -c "node_modules/.cache" package.json` returns 0 (zero legacy-idiom survivors). Every count in the authored contract -- the 15-script census table, Direction A's 30 rows, Direction B's 15-script disposition table -- is built against 15, not 14.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author the three-direction proof contract** - `bcfbd3396` (feat) -- the full contract minus the Contract Validation section (472 lines)
2. **Task 2: Validate the contract against a filled specimen and a pairwise review** - `c843f464b` (test) -- adds the `## Contract Validation` section (72 lines: specimen validation, pairwise interaction review, authoring self-check)

Both tasks modify the same single new file; committed as two sequential commits against that file (Task 1's commit captures the document without `## Contract Validation`, Task 2's commit adds exactly that section as a 72-line diff) so each task's own scope is independently visible in `git log`, rather than folding both tasks into one commit.

## Direction A (30 rows)

15 census-matched scripts x 2 deliberately-broken entry shapes (S1 syntax error via a new sentinel `__WRAPPER_PROOF_SYNTAX__`, S2 unresolvable import via `__wrapper_proof_unresolvable__` -- both distinct from `34.9-PIPE-PROOF.md`'s own `__C201_PROOF_*` sentinels so the two documents' artifacts stay unambiguously separable in any shared log or grep). Per-row PASS bar has FIVE required criteria (one more than `34.9-PIPE-PROOF.md`'s four, since the private-tmpdir architecture adds a fifth, wrapper-specific discipline under test): wrapper exits non-zero; the script's own output is absent from stdout (operationalised script-agnostically as "the transcript contains ONLY esbuild's own diagnostic lines," since not every one of the 15 scripts has a fixed success literal to check against); no false `Cannot find module`; and the private `$TMPDIR/gamelib-runts-*` directory is absent afterward (`meta/runTs.cjs`'s own `cleanupAndExit` discipline, T-34.9-C4-03). Set-equality verified programmatically: all 15 live `package.json` scripts appear in the Direction A matrix, zero missing.

## Direction B (10 full-run + 1 partial-run + 4 named exclusions)

Selection rule adapted from `34.9-PIPE-PROOF.md` Section 5, four exclusion categories (network / destructive / credential / phase-forbidden) instead of three, since this cycle's new `verify:updater-key` script introduced a genuinely new exclusion shape (credential-gated, not network-or-destructive-gated). All five of this plan's own explicit Direction B inclusion requirements are satisfied: both argv-forwarding call sites (`verify:runner-bundle build --arch=arm64`, full run; `build-runners-onedir --arch=arm64`, partial run), the `export`-prefixed `lint-translations:gamelib`, the `pnpm build:sidecar &&`-chained `build:sidecar-sea`, and `--target=node22` coverage (two scripts: `build:sidecar-sea` and `gen-tray-icon-variants`).

`gen-tray-icon-variants` (new to this cycle) writes a TRACKED file, `public/icon-tray-template.png` (confirmed via `git ls-files`), so it carries the same backup/`shasum`-restore requirement as `gen-vtables` and `gen-i18n-gate-scope`.

## Direction C (2 directions: race test + mandatory positive control)

Harness shape adapted directly from the reviewer's own methodology (`34.9-REVIEW-CYCLE3.md`: "two processes ... concurrently with different source content"): two throwaway scratch entry files (`procA.ts`/`procB.ts`, each printing a per-trial random session-uid marker), 60 concurrent-pair trials (matching the reviewer's own 60, the larger of the two prior measurements -- reviewer 60/60, `34.9-VERIFICATION.md`'s re-verification 20/20). PASS bar: 0 cross-contamination across 120 individual marker checks.

**The mandatory positive control is its own numbered direction with its own PASS bar**, per this plan's `<positive_control>` instruction: the identical 60-trial harness, same session, same hardware, pointed at the RETIRED shared-outfile idiom instead of the wrapper (`esbuild --outfile=<one-fixed-throwaway-path> ... && node <same-path>`, both concurrent processes targeting that ONE path). Decision rule stated explicitly: if the control does not reproduce cross-contamination at a rate consistent with the two prior measurements (both 100%), Direction C is scored **INVALID, not PASS**, regardless of what the new-idiom test showed -- a harness that cannot detect the defect cannot prove its absence.

## Contract Validation (Task 2)

**(a) Specimen validation** -- 6 distinct check shapes, each run against a real passing AND (where meaningful) a real deliberately-failing specimen, using the actual `meta/runTs.cjs` and the actual repo `esbuild` binary, entirely under the session scratchpad:

- Direction A exit/absence/`Cannot find module` check (S1 shape): broken specimen exit `1`, own-marker absent, error marker present, `Cannot find module` absent; healthy counterpart exit `0`, own-marker present.
- Direction A S2 shape: broken specimen exit `1`, `Could not resolve "__wrapper_proof_unresolvable__"` present verbatim.
- Direction A temp-dir-absence check: passing case (0 leaked dirs after normal broken/healthy runs) AND a genuine failing case, constructed by launching a 5-second busy-wait specimen and `SIGKILL`-ing it 1.5s in -- `SIGKILL` cannot be caught by any process, so this reliably produces a leaked `gamelib-runts-*` directory (confirmed present, count 1) without mutating `meta/runTs.cjs` itself. Explicitly documented as validating the CHECK's discriminating power, not as evidence the wrapper has a bug.
- Direction B success-literal check: pass specimen (literal present) vs. fail specimen (a different, non-matching line -- literal correctly absent).
- Direction C cross-contamination DETECTOR logic: manually-fabricated fixture pairs (`c-good/` both logs show only their own marker, cross-count 0; `c-bad/` both logs show the OTHER's marker, cross-count 2) -- never an actual concurrent invocation, per this plan's own FORBIDDEN clause.
- `shasum` restore check: a restored file's hash matches its pristine baseline; a deliberately un-restored file's hash does not match its own pristine copy.

**(b) Pairwise interaction review** -- 4 required pairs reviewed explicitly. One real hazard found and mitigated (the Direction C positive control's throwaway shared-cache path must be deleted immediately after its own last trial, before any later direction runs, so no stale artifact from the OLD-idiom control could be mistaken for anything by a later check). The shared-entry-file pair (`meta/lintTranslations.ts` backing both `lint-translations` and `lint-translations:gamelib`) is reviewed explicitly and confirmed as intentional multiplexing, not a coverage gap: both rows are scored independently against their own transcripts, and a defect confined to the `export`-prefixed invocation path specifically would still be caught by row 4 even though both rows share one physical injection.

**(c) Authoring self-check** -- confirms zero `Observed:` fields filled, zero result fields filled (`directions_total` the sole, deliberate exception), `git status --porcelain -- meta/ package.json` clean, and that this plan ran none of the directions it authored.

## Files Created/Modified

- `.planning/phases/34.9-.../34.9-WRAPPER-PROOF.md` - the full unfilled proof contract (544 lines)

## Decisions Made

See `key-decisions` in frontmatter above -- census re-derivation (15, agreeing with 34.9-29), the `directions_total: 43` exception, `build:sidecar-sea`'s inclusion in full-run Direction B (same-arch build never reaches the network path), `verify:updater-key`'s credential-based exclusion, and `build-runners-onedir`'s partial-run (argv-arrival-only) treatment.

## Deviations from Plan

None requiring Rule 1-4 action. This plan's own instructions were followed as written throughout, including the harder-to-satisfy instructions (independent census re-derivation before authoring; specimen validation without running the concurrency harness; a genuine failing specimen for the temp-dir check constructed via `SIGKILL` rather than left as an honest limitation, which strengthens the Contract Validation beyond the minimum `34.9-PIPE-PROOF.md` precedent set for that one check shape).

## Issues Encountered

None.

## Known Stubs

None in the sense this term usually flags (hardcoded empty UI values, placeholder text reaching a user). Every `Observed:` field and every result field in `34.9-WRAPPER-PROOF.md` is deliberately unfilled -- that is the entire point of author/runner separation, explicitly required by this plan's own objective, not an oversight or an unfinished feature. Plan 34.9-32 is the sole writer of those fields.

## Threat Flags

None. This plan's own `<threat_model>` already names the two threats that matter for a proof-authoring plan (T-34.9-C4-09 author/runner separation, T-34.9-C4-10 Direction C false-PASS spoofing) and both are mitigated exactly as specified: separation is enforced by the automated verify checks (both re-run clean after every edit in this plan, including after the Task 2 specimen-validation edit) and Direction C's positive control is written as its own numbered direction with an explicit INVALID-not-PASS rule. T-34.9-C4-11 (tampering via contract authoring touching source) is mitigated and confirmed: `git status --porcelain -- meta/ package.json` clean at every checkpoint.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `34.9-WRAPPER-PROOF.md` exists, `status: authored`, ready for plan 34.9-32 to run on real macOS arm64 hardware.
- Waves 1-2 of gap cycle 4 (34.9-29, 34.9-30, 34.9-31) are now all complete and autonomous. Waves 3-4 (34.9-32, 34.9-33) still need real macOS arm64 hardware and cannot run autonomously -- 34.9-32 runs this contract and fills every result field; 34.9-33 depends on 34.9-32's verdict to ledger C3-01/C3-02/C3-03.
- No blockers introduced by this plan for 34.9-32.

## Self-Check: PASSED

- FOUND: `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-WRAPPER-PROOF.md`
- FOUND: commit `bcfbd3396` (Task 1)
- FOUND: commit `c843f464b` (Task 2)
- `node -e` Task 1 automated verify: `TASK1-OK`
- `node -e` Task 2 automated verify: `TASK2-OK`
- `git status --porcelain -- meta/ package.json`: empty
- `grep -c "node meta/runTs.cjs" package.json` = 15 (independently re-derived, matches 34.9-29-SUMMARY.md)

---
*Phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co*
*Completed: 2026-08-14*
