---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 20
subsystem: infra
tags: [electron-builder, macos, packaging, ci-scripts, guard-contract, author-runner-separation]

# Dependency graph
requires:
  - phase: 34.9 plan 18
    provides: "meta/preserveRunnerSymlinks.ts's isContainedSymlinkTarget and closeBundle throw-on-unrestored-symlink"
  - phase: 34.9 plan 19
    provides: "meta/verifyRunnerBundle.ts's WR-02 closure (fails on a fully-absent top-level framework stub, not only a wrong-typed one)"
provides:
  - "pnpm verify:runner-bundle build --arch=arm64 wired as an && step into both dist:mac and release:mac, immediately before electron-builder"
  - "electron-builder.yml's orphan comment retired -- now describes the wired guard, its staged-only scope, and its arm64-only coverage, with no overclaiming language"
  - "34.9-GUARD-PROOF.md: an unrun, author/runner-separated proof contract specifying a FAILING direction (injected dereferenced Python.framework) and a PASSING direction (normal build, appended CLI args honored), fully validated against synthetic specimens at authoring time"
affects: [34.9-21, 34.9-22]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Guard-wiring idiom: insert the verification step as an && segment immediately before the terminal command in a pnpm script string, never after, so pnpm's CLI-arg append (which always lands on the last command) is preserved"
    - "Author/runner separation for proof contracts (decision D-E): the plan that writes a live/guard proof contract may run ONE bounded read-only command to validate argv plumbing, but may never run the contract's own mutating directions or write its result fields -- mirrors 34.9-15/34.9-16's LIVE-GATE-RERUN precedent"
    - "Specimen-based assertion validation: before trusting a contract's grep/test commands, construct synthetic good/bad transcripts under the scratchpad and prove each assertion matches its intended specimen and not the other -- caught one self-satisfying assertion (see Deviations)"

key-files:
  created:
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-GUARD-PROOF.md
  modified:
    - package.json
    - electron-builder.yml

key-decisions:
  - "Inserted the guard step AFTER electron-vite build (which populates build/bin from public/) and IMMEDIATELY BEFORE electron-builder in both dist:mac and release:mac, keeping electron-builder the last && segment so CI's appended --x64 --arm64 --publish=never (build-base.yml:48) continues landing on electron-builder unchanged"
  - "Did not touch any .github/workflows/*.yml file -- one script edit covers both local and CI invocation paths, per the plan's settled interfaces"
  - "Direction B's original 'no publish attempt' criterion (implicit grep for the bare word `publish`) was replaced with an absence check on the literal `Uploading` before the contract was finalized -- the bare-word version would have self-matched on dist:mac's own `--publish=never` invocation line even on a fully correct run (see Deviations)"
  - "The one bounded read-only exception (`pnpm verify:runner-bundle build --arch=arm64`) was exercised once against a pre-existing local build/bin/arm64/darwin tree, confirming argv plumbing and the PASS-line literal's byte-identity to source; its outcome was recorded in this SUMMARY only, never written into the contract's own result fields"

requirements-completed: [REQ-34.9-08]

# Metrics
duration: 55min
completed: 2026-08-12
---

# Phase 34.9 Plan 20: Wire verify:runner-bundle into the build scripts and author its proof contract Summary

**Wired `pnpm verify:runner-bundle build --arch=arm64` into `dist:mac` and `release:mac` as a pre-packaging `&&` step, retired the orphan comment that described it as a manual tool, and authored (but did not run) `34.9-GUARD-PROOF.md` -- a two-direction proof contract, validated against synthetic transcripts, that plan 34.9-21 will use to prove the guard actually fires.**

## Performance

- **Duration:** ~55 min
- **Started:** 2026-08-12T19:40:00Z (approx, first Read)
- **Completed:** 2026-08-12T20:35:00Z (approx)
- **Tasks:** 3 completed
- **Files modified:** 3 (`package.json`, `electron-builder.yml`, `34.9-GUARD-PROOF.md` created)

## Accomplishments

- Closed the second half of CR-01 (`34.9-REVIEW.md`'s sole Critical finding): `verify:runner-bundle` was previously invoked by nothing in the build pipeline (`34.9-VERIFICATION.md` truth 8's own cited evidence). It is now an unconditional `&&` step in both macOS packaging scripts.
- Preserved arg-forwarding safety: `electron-builder` remains the last command in both `dist:mac` and `release:mac`'s `&&` chains, verified structurally (`/&& electron-builder [^&]*$/` matches both scripts), so CI's `pnpm dist:mac --x64 --arm64 --publish=never` (`build-base.yml:48`) is unaffected.
- Rewrote the `electron-builder.yml` comment to state exactly what is now automatic (staged `build/` tree, pre-packaging, arm64-only) and exactly what is not (packaged `.app`, x64 leg, CI), with no "tripwire"/"cannot go silent again" language -- the precise overclaim CR-01 flagged.
- Authored `34.9-GUARD-PROOF.md`: a FAILING direction (inject a dereferenced `Python.framework` reproducing vite's `copyDir` mechanism behind F-34.9-01, require the guard's own failure literal plus absence of electron-builder output) and a PASSING direction (normal build with appended CLI args, requiring the guard's PASS line to appear before any electron-builder output). Both directions include a hard, structural restore gate (git is blind to `public/bin/**`, which is git-ignored).
- Validated every verification command in the contract against synthetic good/bad transcripts built under the scratchpad, confirming each discriminates in both directions; ran a pairwise interaction review (7 pairs considered, 5 flagged hazards, all resolved in-document); added a RECOVERY note ahead of Direction A's first destructive step.
- This plan ran no direction of the proof contract, injected no defect, and wrote no result field -- author/runner separation (decision D-E) is preserved for plan 34.9-21.

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire verify:runner-bundle into dist:mac and release:mac, arg-forwarding-safe** - `8ed1f608e` (feat)
2. **Task 2: Author 34.9-GUARD-PROOF.md -- the unrun proof contract** - `573b38510` (docs)
3. **Task 3: Validate the contract against a filled specimen, review its pairs, and record the authoring self-check** - `34deb4d2e` (docs)

**Plan metadata:** (this commit, made immediately after this SUMMARY)

## Files Created/Modified

- `package.json` - Two script edits: `dist:mac` and `release:mac` now insert `pnpm verify:runner-bundle build --arch=arm64` as an `&&` step immediately before `electron-builder`
- `electron-builder.yml` - The `mac.files` comment block rewritten to describe the wired guard, its staged-tree/arm64-only scope, and the deliberate fail-fast consequence on a checkout with no staged runners
- `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-GUARD-PROOF.md` (new) - The unrun, author/runner-separated proof contract: preconditions, a RECOVERY note, Direction A (FAILING/injection), restore-and-prove-restore, Direction B (PASSING/args-forwarded), verdict rule, what-this-does-NOT-prove, sink partition, and a Contract Validation section (specimen validation, pairwise review, authoring self-check)

## Decisions Made

- Guard step placed after `electron-vite build` (populates `build/bin` from `public/`) and immediately before `electron-builder` (stays last so CI's appended args are unaffected) in both `dist:mac` and `release:mac`.
- No `.github/workflows/*.yml` file touched -- one script edit covers local and CI per the plan's settled interfaces.
- The RECOVERY note (naming the session-dir path pattern and exact restore command) is placed structurally before Direction A's first step (line 93, before Direction A's own heading at line 115), so an interrupted future run has an immediate, unambiguous recovery path.
- Fixed one self-satisfying assertion at authoring time (see Deviations) before publishing the contract, rather than leaving it for 34.9-21 to discover live.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug, caught during self-authored validation] Direction B's "no publish attempt" criterion would have self-matched on a correct run**

- **Found during:** Task 3, subsection (a) specimen validation
- **Issue:** The plan's own action text for Direction B criterion (c) specified "no publish attempt (no `publish` step reaching GitHub)" without naming an exact assertion. The obvious literal choice, `grep -c publish`, is vacuous: `dist:mac`'s own invocation line echoes `--publish=never`, so a bare `publish` search returns a non-zero count even on a fully correct run (confirmed live against the synthetic good-B specimen: count 1). An assertion that cannot fail against the good case is not a real gate (Test 4, absence-observability).
- **Fix:** Replaced the criterion with an absence check on the literal `Uploading` (`grep -c Uploading direction-b.log` must be `0`), which correctly returns `0` on the good specimen and `1` on a constructed bad-publish specimen carrying a synthetic `Uploading file=... provider=github` line.
- **Files modified:** `34.9-GUARD-PROOF.md` (Direction B PASS bar criterion (c))
- **Verification:** Both specimens tested; the fixed assertion discriminates in both directions (recorded in Contract Validation subsection (a)).
- **Committed in:** `573b38510` (Task 2 commit, since the fix was applied during authoring before the initial commit landed) -- the discovery and before/after record itself is documented in `34deb4d2e` (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 -- a contract-authoring defect caught by the plan's own mandated validation step, not a product defect)
**Impact on plan:** No scope creep -- this is exactly the class of defect the plan's Task 3 exists to catch before the contract reaches a human operator. Fixed inline, verified, no re-work needed.

## Issues Encountered

None beyond the deviation above. The one bounded read-only command (`pnpm verify:runner-bundle build --arch=arm64`) was run once from the repo root against a pre-existing local `build/bin/arm64/darwin` tree (left over from a prior, unrelated local build) and returned:

```
Runner       Exists  Exec   Mach-O  Files  Mach-O files
legendary    true    true   true    109    104
gogdl        true    true   true    67     62
nile         true    true   true    108    103
...
PASS: all three onedir runners present, executable and Mach-O; tree sizes above the floor.
```
Exit 0. This confirms argv plumbing (`build` parsed as root, `arm64` as arch, surviving pnpm's arg-append through the `| node -` idiom) and independently re-confirms the PASS line is byte-identical to the literal quoted throughout the contract, matching the static source comparison against `meta/verifyRunnerBundle.ts:559`. Per the plan's explicit instruction, this outcome is recorded here only -- it was never written into `34.9-GUARD-PROOF.md`'s own frontmatter or `Observed:` fields, and it does not count toward either direction's PASS bar.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `34.9-GUARD-PROOF.md` is ready for plan **34.9-21** to run on macOS arm64 hardware: both directions, their preconditions, the RECOVERY note, and the validated assertions are all in place. 34.9-21 is the sole writer of `verdict`/`run_date`/`directions_passed`/`directions_failed`/`Observed:` values.
- `deferred-items.md` items 12 (arm64-only coverage) and 13 (never-exercised-in-CI) are unaffected by this plan and remain owned by plan **34.9-22**, along with any ledger row the staged-vs-packaged distinction in the `electron-builder.yml` comment might warrant -- recommended here as a possible new ledger entry for 34.9-22 to consider, not written by this plan.
- No blockers. `pnpm codecheck` exits 0 throughout; no user-facing string was added (i18n gate N/A -- no `src/**` files touched by this plan).

## Self-Check: PASSED

- FOUND: `package.json`
- FOUND: `electron-builder.yml`
- FOUND: `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-GUARD-PROOF.md`
- FOUND: `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-20-SUMMARY.md`
- FOUND: `8ed1f608e` (Task 1 commit)
- FOUND: `573b38510` (Task 2 commit)
- FOUND: `34deb4d2e` (Task 3 commit)
- FOUND: `c4ab06c09` (plan metadata commit)
