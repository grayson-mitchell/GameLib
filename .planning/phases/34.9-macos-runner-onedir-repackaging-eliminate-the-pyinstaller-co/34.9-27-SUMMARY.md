---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 27
subsystem: testing
tags: [jest, package.json-wiring-pin, meta-scripts, deferred-items-ledger]

# Dependency graph
requires:
  - phase: 34.9-24
    provides: resolvedTopLevelTargetExists in meta/verifyRunnerBundle.ts + the shared test file this plan appends to
  - phase: 34.9-25
    provides: the 13-script esbuild-pipe-to-&& conversion that made package.json:44/:49's live contents current as of this plan
provides:
  - A C2-04 regression pin proving `pnpm verify:runner-bundle` is present AND ordered before `electron-builder` in both dist:mac and release:mac, proven red against both failure shapes on both scripts
  - Dated, owned deferred-items.md ledger entries (18, 19) for C2-05 (arm64-only guard live in active CI + auto-publish exposure) and C2-07 (doc-comment pins couple CI to wording)
affects: [phase-34.9-verification, future-x64-onboarding-plan]

# Tech tracking
tech-stack:
  added: []
  patterns: ["structural JSON.parse(package.json).scripts pin, mirrored from cleanDistMac.test.ts's existing pattern, duplicated deliberately rather than shared"]

key-files:
  created: []
  modified:
    - meta/__tests__/verifyRunnerBundle.test.ts
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md

key-decisions:
  - "C2-04's pin duplicates cleanDistMac.test.ts's six-line loadScripts() helper rather than importing it, so the pin survives a future retirement of cleanDistMac.test.ts"
  - "C2-05's ledger entry cross-references items 12/13 by number instead of restating them, adding only the two new details (live build-base.yml:48 invocation, -p always auto-publish exposure) per plan instruction"
  - "This plan does NOT close C2-01 and does NOT satisfy 34.9-VERIFICATION.md truth 8 -- scope-honesty statement repeated per plan objective"

patterns-established:
  - "A package.json script-wiring pin must be proven red against BOTH a deletion mutation (scores on the presence assertion) AND a reordering mutation (scores on the ordering assertion) before it is trusted -- mirrors this project's standing 'a grep assertion must be proven to FAIL against a known-bad input' rule, now applied to structural JSON assertions"

requirements-completed: [REQ-34.9-08]

# Metrics
duration: ~20min
completed: 2026-08-13
---

# Phase 34.9 Plan 27: package.json wiring pin (C2-04) + C2-05/C2-07 ledger entries Summary

**Added a jest pin proving `verify:runner-bundle` is present and correctly ordered before `electron-builder` in both macOS packaging scripts, proven red against all four known-bad mutations on real hardware, plus two dated ledger entries for C2-05 and C2-07 per locked decision D-C3-05.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-08-13T07:08:00Z
- **Tasks:** 3 (2 produced commits; Task 2 is a proof-only task with no persistent artifact)
- **Files modified:** 2

## Accomplishments

- `meta/__tests__/verifyRunnerBundle.test.ts` now has a `package.json wiring pin (C2-04)` describe block: two tests (`dist:mac`, `release:mac`), each asserting presence (`toContain('pnpm verify:runner-bundle')`) and ordering (`indexOf` before `electron-builder`) via structural `JSON.parse`, never raw-text grep
- All four deliberate mutations (M1/M2 deletion, M3/M4 relocation) were applied to `package.json`, run against real jest, and scored — see table below. Each restore was hash-verified before the next mutation began
- `deferred-items.md` gained a new `## Code-review finding disposition, gap cycle 2 (2026-08-13)` section with items 18 (C2-05) and 19 (C2-07), each with the four house fields, dated, owned, and mechanically greppable
- `package.json` ends byte-identical to its pre-plan state (`shasum -a 256` verified after every restore) and is absent from both commits

## Task Commits

Each task was committed atomically:

1. **Task 1: Pin verify:runner-bundle's presence and position in both macOS packaging scripts** - `477331e3e` (test)
2. **Task 2: Observe the pin go red against all four known-bad mutations, then restore package.json exactly** - no commit (proof-only task; `package.json` mutations were never persisted — restored by `cp` and hash-verified after every mutation, per the plan's own `<files>` note that it is "not a deliverable")
3. **Task 3: Add dated, owned ledger entries for C2-05 and C2-07** - `51ea0d5a0` (docs)

**Plan metadata:** (this commit, docs: complete plan — created after this Summary)

## Files Created/Modified

- `meta/__tests__/verifyRunnerBundle.test.ts` - added `package.json wiring pin (C2-04)` describe block (module-scope `PACKAGE_JSON_PATH` const + local `loadScripts()`, two tests)
- `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md` - added items 18 (C2-05) and 19 (C2-07) under a new gap-cycle-2 disposition section

## Mutation Proof Table (Task 2)

All four mutations were applied to a clean-restored `package.json`, scored, then individually restored and hash-verified before the next mutation began. Baseline hash (`shasum -a 256`), recorded before the first mutation and matched after every restore:
`ffd1d854afe283bc0b0e081be7a698b8d23afcc4b9c89b83bad41451e5bf4139`

| Mutation | Script | Change | Expected failing assertion | Observed failing assertion | Exit code | Restore hash match |
|---|---|---|---|---|---|---|
| M1 | `dist:mac` | deleted `pnpm verify:runner-bundle build --arch=arm64 && ` | PRESENCE (`toContain`) | PRESENCE — `expect(distMac).toContain('pnpm verify:runner-bundle')` failed, `release:mac` test still green | non-zero (jest FAIL, 1/19 failed) | MATCH |
| M2 | `release:mac` | deleted `pnpm verify:runner-bundle build --arch=arm64 && ` | PRESENCE (`toContain`) | PRESENCE — `expect(releaseMac).toContain('pnpm verify:runner-bundle')` failed, `dist:mac` test still green | non-zero (jest FAIL, 1/19 failed) | MATCH |
| M3 | `dist:mac` | relocated fragment to end, after `electron-builder --mac` | ORDERING (`toBeLessThan`), presence passing | ORDERING — `expect(distMac.indexOf(...)).toBeLessThan(...)` failed (`Expected: < 117, Received: 143`); presence assertion passed (test failure attributed to line 630, not 627) | non-zero (jest FAIL, 1/19 failed) | MATCH |
| M4 | `release:mac` | relocated fragment to end, after `electron-builder -p always --mac --x64 --arm64` | ORDERING (`toBeLessThan`), presence passing | ORDERING — `expect(releaseMac.indexOf(...)).toBeLessThan(...)` failed (`Expected: < 73, Received: 123`); presence assertion passed (test failure attributed to line 639, not 638) | non-zero (jest FAIL, 1/19 failed) | MATCH |

Every mutation's JSON validity was confirmed (`python3 -c "import json; json.load(...)"`) before running jest, so no red result could be mistaken for a JSON parse failure. After the final (M4) restore, the full suite was run once more against the restored manifest: **19/19 passed, exit 0**. No `git checkout --` and no `git stash` was run at any point; all restores used `cp` from a scratchpad backup outside the repo tree.

## Decisions Made

- C2-04's pin was placed in `verifyRunnerBundle.test.ts` (not appended to `cleanDistMac.test.ts`'s existing block), duplicating the `loadScripts()` helper, per the plan's explicit rationale: the pin belongs with the thing it pins, and a future refactor retiring `cleanDistMac` should not collaterally delete this guard.
- C2-05's ledger entry (item 18) does not restate the base fact already recorded in items 12/13 — it cross-references both by number and records only the two new details the review adds (live `build-base.yml:48` invocation shape, and the `-p always` auto-publish consequence), explicitly resolving the apparent tension with item 13 ("wired but never executed in CI" vs. "structurally live in the workflow file").
- No `gsd-sdk` state write happened while landing Task 3, so no STATE.md hand-correction was needed at that point (state updates below use the documented handlers directly).

## Deviations from Plan

None - plan executed exactly as written. Task 2 produced no commit because its own `<files>` note states `package.json` is "mutated and restored — MUST end byte-identical; not a deliverable" — there is no persistent artifact from that task to stage.

## Issues Encountered

None. All four mutation runs scored exactly as the plan's `<action>` predicted (M1/M2 red on presence, M3/M4 red on ordering with presence passing), so no investigation branch was triggered.

## Scope Honesty (repeated per plan objective)

**This plan does NOT close C2-01**, and this is stated explicitly per the plan's own objective: a string-position/structural assertion over `package.json` is blind to the `esbuild ... | node -` pipeline's swallowed exit code inside `scripts['verify:runner-bundle']` itself (that idiom no longer exists as of plan 34.9-25's conversion, but this plan's tests never assert on it either way — see the plan's `<action>` for Task 1, which explicitly forbids asserting on the pipe idiom). C2-04 is a distinct, narrower guarantee: *is the step present, and is it ordered before the thing it is supposed to gate.* `34.9-VERIFICATION.md` truth 8 remains the responsibility of the pipe-idiom conversion work landed in plans 34.9-25/26, not this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- C2-04, C2-05, and C2-07 are closed for this gap cycle's purposes (C2-04 by a proven-red test, C2-05/C2-07 by dated ledger entries per D-C3-05).
- `deferred-items.md` now carries 19 numbered items; the next gap-cycle-3 plan should re-read the file immediately before appending (collision-tolerant numbering) rather than assuming 19 is final.
- C2-05's named precondition (an x64 darwin onedir tree) remains blocked on REQ-34.9-02's own precondition — unchanged by this plan, tracked in items 1/12/13/18.

## Self-Check: PASSED

- `meta/__tests__/verifyRunnerBundle.test.ts` exists: FOUND
- `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md` exists: FOUND
- Commit `477331e3e`: FOUND (`git log --oneline --all | grep 477331e3e`)
- Commit `51ea0d5a0`: FOUND (`git log --oneline --all | grep 51ea0d5a0`)
- `pnpm test meta/__tests__/verifyRunnerBundle.test.ts` on restored manifest: 19/19 PASSED, exit 0
- `git status --porcelain package.json`: empty
- `grep -c '^### [0-9]*\. C2-0' deferred-items.md`: 2

---
*Phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co*
*Completed: 2026-08-13*
