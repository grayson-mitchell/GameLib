# Phase 34.9 — Gap Cycle 2 Plan Outline

Closes verification truth 8 (`34.9-VERIFICATION.md`, `status: gaps_found`) — CR-01, the code
review's sole Critical finding — plus the five review findings (WR-01, WR-02, IN-01, IN-02, IN-03)
that appear in **no** ledger. Plans `34.9-01`..`34.9-17` are executed and MUST NOT be touched.

| Plan ID | Objective | Wave | Depends On | Requirements | Autonomous |
|---|---|---|---|---|---|
| 34.9-18 | CR-01 half A + WR-01: make `preserveRunnerSymlinks.ts`'s `closeBundle` FAIL the build on a skipped restore, and reject any collected symlink whose target is absolute or escapes `destDir`; prove both failing directions with fixture unit tests that assert the known-bad state before calling | 1 | [] | [REQ-34.9-08] | true |
| 34.9-19 | WR-02 + IN-01 + IN-02: close `verifyRunnerBundle.summarise`'s missing-top-level-stub blind spot with a `stub-absent` fixture, and correct the two overstated doc comments in `cleanDistMac.ts` (`isMacArtifact`'s symlink guarantee; the containment throw's reachability) | 1 | [] | [REQ-34.9-08] | true |
| 34.9-20 | CR-01 half B: wire `pnpm verify:runner-bundle build --arch=arm64` into `dist:mac` and `release:mac` **before** `electron-builder` (arg-forwarding-safe), retire the orphan comment at `electron-builder.yml:63`, and AUTHOR `34.9-GUARD-PROOF.md` — forbidden from running any of it | 2 | [34.9-19] | [REQ-34.9-08] | true |
| 34.9-21 | RUN `34.9-GUARD-PROOF.md` on real macOS arm64 hardware; sole writer of results; observes the wired guard's FAILING direction against a deliberately dereferenced vendored framework, then the PASSING direction after restore | 3 | [34.9-18, 34.9-20] | [REQ-34.9-08] | false |
| 34.9-22 | Reconcile: correct the overclaiming tripwire prose, add dated `deferred-items.md` entries (items 11-13) for everything not fixed, fix the duplicate `34.9-17` `[ ]` row, and update REQ-34.9-08 / ROADMAP / STATE to the post-cycle-2 truth | 4 | [34.9-21] | [REQ-34.9-08] | true |

## Notes for per-plan authors

**`closeBundle` throw predicate (34.9-18).** Throw when `skipped.length > 0 || rejected.length > 0`.
This is safe on every non-macOS path because the vendored onedir trees are **git-ignored and
untracked** (`git ls-files public/bin/arm64/darwin` → 0 files; `public/bin/.gitignore:5`), so any
fresh checkout — and every Linux/Windows checkout — has no darwin symlinks at all, `collectSymlinks`
returns `[]`, and both buckets are empty. On macOS running `dist:win`/`dist:linux`, vite still
copies `public/` wholesale, so all 12 symlinks restore and `skipped` is 0. A skip means a
destination parent vite did not create — exactly CR-01's named regression scenarios (publicDir
restructure, vite copy-ordering change, build-exclude pattern). Guard A is defense-in-depth: it is
**not reachable from a real build today**, so its failing direction is proven at unit level ONLY —
say so plainly, do not claim a live proof.

**Fix vs defer, one line each.**
- **CR-01** — FIX (34.9-18 throw + 34.9-20 wiring + 34.9-21 live proof).
- **WR-01** — FIX (34.9-18): add an exported `isContainedSymlinkTarget(destDir, relPath, target)`; reject absolute targets and any target that escapes `resolve(destDir)` when resolved from the link's own directory. All 12 real targets (`3.14`, `Versions/Current/Python`, `Versions/Current/Resources`, `Python.framework/Versions/3.14/Python`) are relative and contained, so no false positive.
- **WR-02** — FIX (34.9-19), and it MUST land before the 34.9-20 wiring so we do not wire a guard with a known hole.
- **IN-01** — FIX (34.9-19), comment precision only; do NOT add the name-only fallback branch.
- **IN-02** — FIX (34.9-19), downgrade the comment to defense-in-depth-against-unreachable-input; do not expose an internal seam for a test.
- **IN-03** — DEFER (34.9-22, `deferred-items.md` item 11) with a dated OWNER; cross-platform `clean:dist-win`/`clean:dist-linux` is genuine follow-up work.

**Known-bad constructions.**
- Guard A (skipped restore) + WR-01 (escaping target) — unit fixtures in `meta/__tests__/preserveRunnerSymlinks.test.ts`, following that file's existing `mkdtempSync` + `cp -RL` + "vacuity guard asserts the bad state first" pattern.
- WR-02 — a fifth `buildGogdlFrameworkFixture` shape, `stub-absent` (delete the top-level `Python` stub entirely), alongside the existing `well-formed` / `dereferenced` / `stub-file` / `unresolvable-target`.
- Guard B (the wired `verify:runner-bundle`) — 34.9-21 only: session dir under `node_modules/.cache/` (same volume, git-ignored); `mv` gogdl's `Python.framework` out to the session dir, then `cp -RL` it back to create the dereferenced shape; restore by `rm -rf` + `mv` back. **`rm -rf build/bin` before each run** — renderer `emptyOutDir: false` means a stale good tree in `build/` would mask the injected defect. Expect exit non-zero, stdout containing `Versions/Current exists but is not a symlink (F-34.9-01)`, no electron-builder output, and no dmg/zip in `dist/`.

**Wiring is npm-script-only, not CI-only.** `build-base.yml:48` runs `pnpm dist:mac` and
`draft-release-mac.yml:37` runs `pnpm release:mac`, so one script edit covers local AND CI. Do NOT
add a separate CI step: the macOS CI leg cannot reach the build at all today — `install-deps` runs
`pnpm download-helper-binaries`, which throws on the six `PENDING-CI-PUBLISH` sentinels (descoped
REQ-34.9-02/03/04). Hardcode `--arch=arm64`: `build/bin/x64/darwin` holds onefile binaries and would
fail the file-count floor. The arm64-only coverage and the never-exercised-in-CI status are
`deferred-items.md` items 12 and 13 (34.9-22), stated as UNPROVEN, never asserted.

**Narrative correction is owned by 34.9-22**: `34.9-LIVE-GATE-RERUN.md` line ~75 ("a genuine
tripwire") and line ~509 ("the automated tripwire that... now fires"), `ROADMAP.md` line ~2459 ("so
a regression cannot go silent again"), the duplicate unchecked `- [ ] 34.9-17-PLAN.md` at
`ROADMAP.md:2516` (the checked twin is at line 2424), and `REQUIREMENTS.md`'s REQ-34.9-08 row.

**Scope fence UNCHANGED** — REQ-34.9-02/03/04 (CI x64 leg, downloader, digests), REQ-34.9-09 (cold
ratio, no third attempt), Tauri-PACKAGED, real-certificate signing/notarization, and upstream PR
submission are all OUT OF SCOPE. No plan may reopen any of them. No new REQ IDs: REQ-34.9-08 owns
this entire cycle.
