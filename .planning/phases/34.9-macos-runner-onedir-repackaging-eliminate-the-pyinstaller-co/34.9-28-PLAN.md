---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 28
type: execute
wave: 5
gap_closure: true
gap_cycle: 3
closes_findings: []
depends_on: ["34.9-23", "34.9-24", "34.9-25", "34.9-26", "34.9-27"]
files_modified:
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-C2-SWEEP-CHECK.cjs
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [REQ-34.9-08]

must_haves:
  truths:
    - "Every finding ID in `34.9-REVIEW-CYCLE2.md` — enumerated by a regex run against the review file itself at execution time, never from a hand-written list including this plan's own — maps to exactly one row of the disposition table, and the unmapped count COMPUTED by `34.9-C2-SWEEP-CHECK.cjs` is 0 and equals the count the document states in its own words."
    - "The sweep predicate is OBSERVED exiting non-zero, naming the specific finding ID, against four deliberately-mutated copies of `deferred-items.md` — a deleted table row, a stripped `OWNER:` line, a FIXED row whose confirmation cell points at a `.planning/` document, and a DEFERRED row pointing at a ledger item that does not carry the finding ID — so a genuinely unmapped finding goes RED rather than passes."
    - "A DEFERRED row is scored ONLY from a structured `### N.` ledger section carrying that finding ID together with a `Blocker` stated as a mechanism, a `Named precondition` and a dated `OWNER:` — a passing mention of the ID in prose, including inside the disposition table itself, can never satisfy the predicate."
    - "A FIXED row is scored ONLY from repository state outside `.planning/` (a source symbol, a `package.json` script string, a named green test), never from a plan SUMMARY's or a frontmatter `closes_findings`' claim; where a cycle-3 SUMMARY's claim disagrees with the repository, the disagreement is recorded verbatim and the row is NOT marked FIXED."
    - "Each of the three items on `34.9-VERIFICATION.md` truth 8's `missing:` list has its delivered state recorded from disk — the converted scripts (zero surviving esbuild-piped-into-node instances, measured by the census predicate against the live `package.json`), `34.9-PIPE-PROOF.md`'s both-direction record, the C2-04 wiring-pin test running green, and the C2-01..C2-08 ledger entries."
    - "No document written by this plan declares truth 8 satisfied or Phase 34.9 complete; `ROADMAP.md` and `STATE.md` both record `gaps_found` and name `/gsd-verify-work 34.9` as the step that scores truth 8."
  artifacts:
    - path: ".planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-C2-SWEEP-CHECK.cjs"
      provides: "the reusable, mutation-proven sweep predicate that derives list A from the review file and scores every row against structure rather than mention"
      contains: "COMPUTED-UNMAPPED"
    - path: ".planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md"
      provides: "the eight-finding C2 disposition table, the truth-8 missing-list delivery state, and a ledger item for anything A-minus-B leaves"
      contains: "Code-review finding disposition — gap cycle 2 review (2026-08-13)"
    - path: ".planning/ROADMAP.md"
      provides: "the gap cycle 3 plan block, the corrected plan count, and a phase status that still does not close"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.9-08's dated 2026-08-13 addendum recording gap cycle 3, ticked state unchanged"
    - path: ".planning/STATE.md"
      provides: "Phase 34.9 post-cycle-3 position with hand-verified progress arithmetic"
  key_links:
    - from: ".planning/phases/.../34.9-C2-SWEEP-CHECK.cjs"
      to: ".planning/phases/.../34.9-REVIEW-CYCLE2.md"
      via: "list A derived live by matching the review's own `### C2-NN` headings, never transcribed"
      pattern: "\\^### \\(C2-"
    - from: ".planning/phases/.../deferred-items.md disposition table FIXED rows"
      to: "package.json, meta/verifyRunnerBundle.ts, meta/__tests__/"
      via: "a confirmation cell naming a non-.planning artifact, enforced by the checker rejecting any `.planning/` or `SUMMARY` reference"
      pattern: "meta/|package\\.json"
    - from: ".planning/phases/.../deferred-items.md disposition table DEFERRED rows"
      to: ".planning/phases/.../deferred-items.md `### N.` sections"
      via: "an `item N` reference resolved to a structured section carrying the ID, Blocker, Named precondition and OWNER"
      pattern: "\\^### \\[0-9\\]\\+\\\\."
---

<objective>
Close gap cycle 3 by making the phase's record complete and legible at the granularity of the defect:
sweep all eight `34.9-REVIEW-CYCLE2.md` findings by set-difference, confirm each cycle-3 deliverable
against the repository rather than against a SUMMARY's claim, and leave `/gsd-verify-work 34.9` as
the step that scores truth 8.

Purpose: the ORIGINAL truth-8 failure happened because a Critical finding, proven live, sat in a
review document with no ledger entry and no fix — it fell through the phase's own reconciliation
step. `34.9-VERIFICATION.md` then found the identical shape had recurred: C2-01 through C2-08 appear
in NONE of `deferred-items.md`, `STATE.md`, `ROADMAP.md`, `REQUIREMENTS.md` (grep for `C2-0`, zero
matches in all four). Plan 34.9-22 is the structural precedent — it swept the six `34.9-REVIEW.md`
findings by set-difference and reported an explicit unmapped count of 0. This plan is its cycle-3
counterpart, and it is the step that must stop the pattern repeating a third time.

Output: `34.9-C2-SWEEP-CHECK.cjs`, a reusable sweep predicate proven RED against four deliberately
incomplete inputs; `deferred-items.md` gains the eight-row C2 disposition table, the truth-8
missing-list delivery state, and a ledger item for anything the set-difference leaves; `ROADMAP.md`,
`REQUIREMENTS.md` and `STATE.md` carry the post-cycle-3 position.

**This plan does not score truth 8 and does not close the phase.** Its job is to make the evidence
complete and legible enough that the verifier can judge, and to leave nothing unrecorded. Where a
cycle-3 plan's own SUMMARY records a partial or deferred outcome, this sweep records that plainly
rather than smoothing it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-VERIFICATION.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-REVIEW-CYCLE2.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-22-PLAN.md

<interfaces>
<!-- Decisions already made by the gap-cycle-3 outline. Do NOT re-derive, re-litigate or expand. -->

**The structural precedent.** `34.9-22-PLAN.md` Task 1 and its landed output — `deferred-items.md`'s
`## Code-review finding disposition (2026-08-12)` section, lines 269-333 — define the method this
plan mirrors: list A derived by grep from the review's own headings, list B derived from what landed,
`A minus B` stated explicitly, a table with one row per finding, and a closing count line ending
`Unmapped count: **0**`. Read that section before writing anything; match its shape, its honesty and
its refusal to write `partially`.

**What is different this time, and why a plain repeat of 34.9-22 is not sufficient.** 34.9-22 derived
list B from each SUMMARY's own closing-language body text — a plan's self-report. This project's
standing rule is that a mutating command's own report is never proof of effect, and a plan SUMMARY is
exactly such a report. This plan therefore scores every FIXED row from repository state outside
`.planning/` and treats the SUMMARY only as a claim to be checked. That is the single most important
strengthening over the precedent.

**The expected mapping — an EXPECTATION, not evidence.** Derive the real mapping at execution time
and record any disagreement rather than transcribing this table. If a plan's SUMMARY shows it did not
actually land what its frontmatter `closes_findings` claimed, the sweep reports that, it does not
paper over it.

| Finding | Severity in the review | Expected disposition | Expected home |
|---|---|---|---|
| C2-01 | Critical | FIX | 34.9-25 (conversion), proof in 34.9-26 |
| C2-02 | Warning | FIX | 34.9-25, proof in 34.9-26 |
| C2-03 | Warning | FIX | 34.9-25, proof in 34.9-26 |
| C2-04 | Warning | FIX | 34.9-27 (wiring-pin test) |
| C2-05 | Warning | DEFER | 34.9-27 (ledger entry) |
| C2-06 | Warning | FIX | 34.9-24 (`resolvedTopLevelTargetExists`) |
| C2-07 | Info | DEFER | 34.9-27 (ledger entry) |
| C2-08 | Info | FIX | 34.9-24 (`result.rejected` assertion) |

**Cycle-3 plan frontmatter, as authored** (read each plan's real frontmatter at execution time —
this is a convenience index, not a source of truth): 34.9-23 `closes_findings: []` (the pipe census,
`34.9-PIPE-AUDIT.md`); 34.9-24 `[C2-06, C2-08]`; 34.9-25 `[C2-01, C2-02, C2-03]`; 34.9-26
`closes_findings: []` (the both-direction proof, `34.9-PIPE-PROOF.md`); 34.9-27 `[C2-04, C2-05,
C2-07]`. Plans 34.9-26 and 34.9-27 both write `deferred-items.md`, so its item numbering will have
moved past 16 by the time this plan runs — derive the next free number, never assume it.

**The three items on truth 8's `missing:` list** (`34.9-VERIFICATION.md` lines 79-82), which this
plan records the delivery state of:
1. Fix the pipe idiom across all four affected scripts, verified against a deliberately-broken entry
   file, OR an explicit dated risk-acceptance entry.
2. A wiring-pin test (C2-04) mirroring `cleanDistMac.test.ts`'s existing `package.json` assertions.
3. Ledger entries for C2-01 through C2-08 in `deferred-items.md` (or a landed fix).

**Ledger house format** (`deferred-items.md` items 1-16): `### N. Title`, then `**What it is:**`,
then `**Blocker (mechanism, not a summary):**`, then `**Named precondition:**`, then `**OWNER:**`
with a date. Items 1-16 and every item opened by plans 34.9-26/27 are settled; editing any of them
reopens a closed scope decision.

**The scope fence is UNCHANGED and must not be reopened by any edit in this plan:** REQ-34.9-02/03/04
(CI x64 leg, downloader, digests), REQ-34.9-09 (cold ratio), Tauri-PACKAGED resolution,
real-certificate signing/notarization, upstream PR submission. **No new REQ IDs** — REQ-34.9-08 owns
this entire cycle.

**STATE.md baseline, as written by plan 34.9-22 and confirmed correct at planning time:**
`total_plans: 297`, `completed_plans: 284`, `percent: 74`, `completed_phases: 17`, `total_phases: 23`.
`percent` tracks PHASES, not plans. `total_plans` counts plans that EXIST — a completing plan is not
an added plan, which is why 34.9-22 had to correct a 297→298 bump. Repository-wide file counts do NOT
reconcile with these figures (447 `*-PLAN.md` and 424 `*-SUMMARY.md` files exist), so a whole-repo
recount is the wrong derivation — use the baseline-plus-delta formulas given in Task 3.

**Standing traps that apply directly to this plan:**
- `gsd-sdk` state writes corrupt `.planning/STATE.md` on essentially every plan. Plan 34.9-22
  sidestepped this entirely by hand-editing and invoking no `state.*` write verb. Do the same. If any
  SDK write does touch the file, re-read it and hand-correct all five progress fields.
- Never use `git checkout --` — it fires `.husky/post-checkout` into a network install. Restore any
  scratch mutation by deleting the scratch copy, never by checking out the real file.
- `.husky/pre-push` is blocked by ~50 pre-existing lint errors. Do not push.
- Plan 34.9-22's Task 2 verify script scanned the literal `tripwire` across whole files and flagged
  four pre-existing Phase 34.2/34.4 occurrences outside its scope. Do not repeat that: every prose
  check in this plan is scoped to lines this plan ADDS, per Task 3.

**Redaction discipline:** no SteamID, no account identifier, no PKCE value.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the sweep predicate and observe it RED before any content exists</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-C2-SWEEP-CHECK.cjs</files>
  <read_first>
    - `.planning/phases/34.9-.../34.9-REVIEW-CYCLE2.md` — confirm the heading shape the predicate must match: eight headings, each `### C2-NN: ...`, at lines 65, 98, 126, 152, 184, 221, 269, 285. `grep -c '^### C2-' <file>` returns 8 at planning time.
    - `.planning/phases/34.9-.../deferred-items.md` lines 269-333 — plan 34.9-22's landed disposition section. The predicate must accept a section of that shape and reject anything weaker.
    - `.planning/phases/34.9-.../deferred-items.md` lines 335-389 — items 11, 12, 13, the house format the predicate resolves a DEFERRED row to.
    - `.planning/phases/34.9-.../34.9-VERIFICATION.md` lines 77-82 — the `artifacts`/`missing` entries this sweep exists to satisfy.
  </read_first>
  <action>
    Write `34.9-C2-SWEEP-CHECK.cjs` in the phase directory: a dependency-free CommonJS Node script
    that takes two positional arguments — the `deferred-items.md` path and the
    `34.9-REVIEW-CYCLE2.md` path. It exists as a FILE, not as an inline one-liner, for one reason:
    Task 2 must run the identical predicate against deliberately mutated copies, and a predicate that
    cannot be re-pointed at a mutated input cannot be proven non-vacuous.

    **Step 1 — derive list A.** Read the review path and collect every capture of the multiline
    pattern `^### (C2-\d{2})`. If the result is empty, print `LIST-A-EMPTY` and exit `2`. This guard
    matters: an empty list A would make every set-membership check pass vacuously, which is the
    self-satisfying-assertion failure mode this plan exists to avoid. List A is never read from any
    hand-written list, including this plan's own `<interfaces>` table.

    **Step 2 — locate the section.** Read the deferred path and find the literal heading
    `## Code-review finding disposition — gap cycle 2 review (2026-08-13)`. If absent, print
    `MISSING-DISPOSITION-HEADING` and exit `1`. Slice from that heading to the next line matching
    `^## ` (or end of file); call the slice `section`. Every table read happens inside `section`
    only — never against the whole document, so a stray table elsewhere cannot satisfy the check.

    **Step 3 — parse rows.** Inside `section`, a data row is a line matching `^\|\s*(C2-\d{2})\s*\|`.
    Split each on `|` and trim; the cells are `Finding`, `Severity`, `Disposition`, `Evidence`,
    `Independent confirmation`. Reject a duplicate finding ID with `DUPLICATE-ROW` and exit `1`.

    **Step 4 — set equality both directions.** Any ID in list A with no row is unmapped, reason
    `NO-ROW`. Any row ID not in list A is reported as `EXTRA-ROW` and exits `1` — a table that has
    drifted away from the review is as broken as one that is short.

    **Step 5 — score each row.** `Disposition` must match `^(FIXED|DEFERRED)$` exactly; anything else,
    including `partially`, `mostly`, `n/a` and empty, is unmapped with reason `BAD-DISPOSITION`.
    - A `FIXED` row requires its `Evidence` cell to match `34\.9-2[3-7]` (a cycle-3 plan ID) AND its
      `Independent confirmation` cell to match `meta/|src/|package\.json` AND to contain neither
      `.planning/` nor `SUMMARY`. Reason on failure: `FIXED-NOT-CONFIRMED-OUTSIDE-PLANNING`. This is
      the arm that stops a plan's self-report from closing a finding.
    - A `DEFERRED` row requires its `Evidence` cell to match `item (\d+)`. Resolve that number to a
      section of the FULL deferred document beginning `^### N\.` and running to the next line matching
      `^#{2,3} `. The resolved section must contain the finding ID literal, the literal `OWNER:`, the
      literal `Named precondition`, and the literal `Blocker`. Missing section → `NO-SUCH-ITEM`;
      missing ID → `ITEM-DOES-NOT-NAME-FINDING`; missing any other token → `ITEM-NOT-STRUCTURED`.
      This is the arm that distinguishes a structured ledger entry from a passing mention: a paragraph
      that names `C2-05` in backticks satisfies none of it, and the disposition table itself lives
      under a `##` heading so it can never be mistaken for a `### N.` item section.

    **Step 6 — compare computed against stated.** Print `COMPUTED-UNMAPPED: <n>` followed by one line
    per unmapped ID as `<ID> <REASON>`. Then read the section's own count line, matching
    `Unmapped count:\s*\*\*(\d+)\*\*`; absent → print `NO-STATED-COUNT` and exit `1`; present but
    different from the computed value → print `COUNT-DISAGREES stated=<x> computed=<y>` and exit `1`.
    The document asserting its own count is not evidence; the script recomputing it independently and
    refusing to agree is.

    **Step 7 — exit.** Exit `0` only when the computed unmapped count is `0` and the stated count is
    `0`, printing exactly `C2-SWEEP-OK <mapped>/<listA.length> mapped, unmapped 0`. Any other state
    exits non-zero.

    **Step 8 — observe the RED baseline now, before Task 2 writes any content.** Run the script
    against the current `deferred-items.md` and the review file. It MUST print
    `MISSING-DISPOSITION-HEADING` and exit `1`, because the 2026-08-13 section does not exist yet.
    Record the literal output and exit status in the Summary. A predicate first observed green has
    never been shown capable of red.

    Do not modify `deferred-items.md` in this task. Do not add the script to `package.json` or to any
    jest config — it is a phase verification artifact, not application source, and adding it to the
    suite would drag a `.planning/` path into CI scope.
  </action>
  <acceptance_criteria>
    - `.planning/phases/34.9-.../34.9-C2-SWEEP-CHECK.cjs` exists and `node --check` on it exits `0`.
    - The file contains the literal strings `COMPUTED-UNMAPPED`, `MISSING-DISPOSITION-HEADING`, `LIST-A-EMPTY`, `COUNT-DISAGREES`, `ITEM-DOES-NOT-NAME-FINDING` and `FIXED-NOT-CONFIRMED-OUTSIDE-PLANNING`.
    - The file contains no `require(` call other than `require('node:fs')` or `require('fs')` — dependency-free; `grep -c "require(" <file>` equals the number of fs requires and no other module name appears.
    - Run against the CURRENT `deferred-items.md`: exit status is exactly `1` and the output contains the literal `MISSING-DISPOSITION-HEADING`. Both are quoted in the Summary.
    - Run with the review path pointed at a nonexistent-heading file (any file with zero `### C2-` headings, e.g. `.planning/ROADMAP.md`): exit status is exactly `2` and the output contains `LIST-A-EMPTY`. Quoted in the Summary.
    - `git status --porcelain` shows exactly one new untracked/added file from this task, the `.cjs` script; `deferred-items.md` is unmodified (`git diff --name-only` does not list it).
    - The Summary states the literal `grep -c '^### C2-' 34.9-REVIEW-CYCLE2.md` output observed at execution time (expected `8`) and notes that list A is derived from it, not transcribed.
  </acceptance_criteria>
  <verify>
    <automated>D=.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co; S=$D/34.9-C2-SWEEP-CHECK.cjs; node --check "$S" && for t in COMPUTED-UNMAPPED MISSING-DISPOSITION-HEADING LIST-A-EMPTY COUNT-DISAGREES ITEM-DOES-NOT-NAME-FINDING FIXED-NOT-CONFIRMED-OUTSIDE-PLANNING; do grep -q "$t" "$S" || { echo "missing token $t"; exit 1; }; done && OUT=$(node "$S" "$D/deferred-items.md" "$D/34.9-REVIEW-CYCLE2.md" 2>&1); RC=$?; echo "$OUT"; test "$RC" -eq 1 && echo "$OUT" | grep -q 'MISSING-DISPOSITION-HEADING' && OUT2=$(node "$S" "$D/deferred-items.md" .planning/ROADMAP.md 2>&1); RC2=$?; echo "$OUT2"; test "$RC2" -eq 2 && echo "$OUT2" | grep -q 'LIST-A-EMPTY' && test "$(git diff --name-only -- "$D/deferred-items.md" | wc -l)" -eq 0 && echo PREDICATE-RED-BASELINE-OK</automated>
  </verify>
  <done>A dependency-free sweep predicate exists as a re-pointable file, derives list A from the review's own headings, refuses to agree with a document's self-stated count, and was OBSERVED exiting 1 with `MISSING-DISPOSITION-HEADING` and 2 with `LIST-A-EMPTY` before any content was written.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep all eight C2 findings by set-difference, record the truth-8 delivery state, and prove the predicate goes red</name>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md</files>
  <read_first>
    - `.planning/phases/34.9-.../34.9-REVIEW-CYCLE2.md` — the source of list A. Derive the IDs by grepping its `^### C2-` headings; do NOT copy the list from this plan's `<interfaces>` table, which is an expectation and could be wrong.
    - `.planning/phases/34.9-.../34.9-23-SUMMARY.md` through `34.9-27-SUMMARY.md` — read each one's frontmatter and its Deviations and Self-Check sections. These are CLAIMS. A `closes_findings` entry whose SUMMARY body reports the fix did not land, or landed partially, counts as NOT closed, and the disagreement is quoted verbatim.
    - `.planning/phases/34.9-.../34.9-23-PLAN.md` .. `34.9-27-PLAN.md` frontmatter — the `closes_findings` each plan declared, so a SUMMARY that silently drops one is visible.
    - `.planning/phases/34.9-.../deferred-items.md` — items 1-16 plus everything plans 34.9-26/27 opened. Read the tail to find the highest existing `### N.` number; the next free number is that plus one.
    - `.planning/phases/34.9-.../34.9-PIPE-AUDIT.md` and `34.9-PIPE-PROOF.md` — the census and the both-direction proof, needed for the truth-8 delivery-state table.
    - `.planning/phases/34.9-.../34.9-VERIFICATION.md` lines 79-82 — the three `missing:` items, verbatim, so the delivery-state table answers them directly.
    - `package.json` scripts block, `meta/verifyRunnerBundle.ts`, `meta/__tests__/verifyRunnerBundle.test.ts`, `meta/__tests__/preserveRunnerSymlinks.test.ts` — the repository state that scores every FIXED row.
  </read_first>
  <action>
    Append one new section to `deferred-items.md`, plus any ledger items the set-difference demands.
    Do not alter items 1-16 or any item opened by plans 34.9-26/27; their text is settled.

    **Section heading, exactly:** `## Code-review finding disposition — gap cycle 2 review (2026-08-13)`.

    Open with one paragraph stating why the section exists: `34.9-REVIEW-CYCLE2.md`'s eight findings
    appeared in none of `deferred-items.md`, `STATE.md`, `ROADMAP.md` or `REQUIREMENTS.md` at the
    2026-08-12 verification (zero `C2-0` matches in all four), which is the identical shape that
    produced the original truth-8 failure; and that this section closes it at the granularity of the
    defect — one row per finding ID, never per file, because an audit whose unit is coarser than the
    defect's unit cannot find the defect.

    **Build the mapping by set-difference, in this order, and show the work in the Summary.**

    1. **List A** — every finding ID in `34.9-REVIEW-CYCLE2.md`, obtained by grepping its `^### C2-`
       headings. Reproduce the raw grep output verbatim in the section, as plan 34.9-22 did. Do not
       hand-enumerate.
    2. **List B** — every finding ID with a landed fix, established in two stages per ID.
       Stage one, the claim: the `closes_findings` frontmatter of each of `34.9-23-PLAN.md` through
       `34.9-27-PLAN.md`, cross-read against the corresponding SUMMARY. Stage two, the confirmation:
       a check against repository state OUTSIDE `.planning/`, run at execution time. An ID enters
       list B only when stage two passes. Record both stages per ID. Where the two disagree, quote
       the SUMMARY sentence and the observed repository state side by side and leave the ID OUT of
       list B — never accept a mutating command's own report, and a plan SUMMARY is exactly that.
       The confirmations, per ID (adapt the locator if the landed code named things differently, and
       say so):
       - C2-01/C2-02/C2-03 — run the census predicate against the live `package.json`: parse the
         `scripts` object and count values that contain `esbuild` and also match a pipe into node
         (`\|\s*node\b`). The expected count is `0`. Additionally confirm `verify:runner-bundle`,
         `clean:dist-mac`, `build-steam-bridge` and `build-runners-onedir` each contain `&& node `.
         Record the four script strings verbatim.
       - C2-04 — `meta/__tests__/verifyRunnerBundle.test.ts` contains assertions naming `dist:mac`,
         `release:mac`, `verify:runner-bundle`, `electron-builder` and an `indexOf` ordering
         comparison; and the file runs green.
       - C2-06 — `meta/verifyRunnerBundle.ts` contains `resolvedTopLevelTargetExists`, populated in
         `inspectFramework` and consumed by a failure branch in `summarise`; and its tests run green.
       - C2-08 — `meta/__tests__/preserveRunnerSymlinks.test.ts`'s symlink-free-tree test asserts
         `result.rejected`; and the file runs green.
       - C2-05 and C2-07 — expected DEFERRED, so their confirmation is a ledger section, not a code
         locator; they are scored by the DEFERRED arm below, not by list B.
    3. **A minus B** — every ID in A with no confirmed landed fix and no already-existing structured
       ledger entry. Each of these MUST receive a `deferred-items.md` item number in this same plan,
       at the next free number derived from the file, or the sweep is incomplete. If A minus B is
       empty apart from the two expected deferrals already owned by plan 34.9-27, say so explicitly
       and open no new items.

    **The table.** Columns, in this order: `Finding | Severity | Disposition | Evidence | Independent
    confirmation (non-.planning)`. `Disposition` is one of `FIXED` or `DEFERRED`, nothing else — no
    `partially`, no `mostly`. A `FIXED` row's `Evidence` names the cycle-3 plan ID that landed it;
    its `Independent confirmation` cell names the non-`.planning` artifact and the observed property,
    and may not contain `.planning/` or `SUMMARY`. A `DEFERRED` row's `Evidence` reads `item N`
    naming the structured ledger section; its confirmation cell names that section's heading. Eight
    data rows, one per list-A ID, no more and no fewer.

    Close the section with an explicit count line in the precedent's own words: how many IDs were in
    list A, how many mapped to a confirmed landed fix, how many mapped to a ledger item, and
    `Unmapped count: **0**`. The checker recomputes this and refuses to agree if it is wrong.

    **Then a sub-heading `### Truth 8 missing-list delivery state (2026-08-13)`** — deliberately NOT
    of the form `### N.`, so the checker's item parser cannot mistake it for a ledger item. A
    three-row table, one row per `missing:` item quoted verbatim from `34.9-VERIFICATION.md` lines
    79-82, with columns `Missing item (verbatim) | Delivered? | Evidence observed on disk | Partial
    or deferred outcome recorded by the delivering plan`. `Delivered?` is `YES`, `NO` or
    `PARTIAL — <what is missing>`. Fill the evidence column from disk: the converted script strings,
    `34.9-PIPE-PROOF.md`'s per-script both-direction rows cross-checked against `34.9-PIPE-AUDIT.md`'s
    census at SCRIPT granularity (name any census script with no proof row — a per-file or per-plan
    check would hide exactly that), the wiring-pin test's green run, and this section itself for item
    3. Where a cycle-3 SUMMARY records a partial or deferred outcome — a script excluded from the
    passing direction, a proof step not run, a deviation — record it plainly in the fourth column
    rather than smoothing it.

    End that sub-section with one sentence, unhedged: **this plan does not score truth 8; that
    judgment belongs to `/gsd-verify-work 34.9`.** Do not write that truth 8 is satisfied, closed,
    met or now true. Do not write that Phase 34.9 is complete.

    **Non-vacuity proof — four mutations, run after the section is written and the checker is green.**
    Work only in a scratch directory outside the repository (`"${TMPDIR:-/tmp}/34.9-28-sweep"`),
    copying `deferred-items.md` into it. Never mutate the real file, and never restore anything with
    `git checkout --`, which fires `.husky/post-checkout` into a network install. For each mutation,
    run `34.9-C2-SWEEP-CHECK.cjs` against the mutated copy and the real review file, and record the
    exit status and output verbatim in the Summary:
    - **M1, deleted row.** Delete one data row from the disposition table. Expect non-zero, output
      naming that ID with reason `NO-ROW`.
    - **M2, stripped OWNER.** In the ledger section a `DEFERRED` row points at, delete the `OWNER:`
      line. Expect non-zero, output naming that ID with reason `ITEM-NOT-STRUCTURED`.
    - **M3, self-referential confirmation.** Replace one `FIXED` row's `Independent confirmation`
      cell with a reference to a `.planning/` SUMMARY. Expect non-zero, reason
      `FIXED-NOT-CONFIRMED-OUTSIDE-PLANNING`.
    - **M4, mention without structure.** Repoint one `DEFERRED` row's `Evidence` at an existing item
      number whose section does not name that finding (item 1 is a safe target). Expect non-zero,
      reason `ITEM-DOES-NOT-NAME-FINDING`. This is the trap-1 shape in its purest form: the ID is
      present in prose, and the predicate still goes red.
    Delete the scratch directory afterwards and confirm `git status --porcelain` lists only
    `deferred-items.md`.
  </action>
  <acceptance_criteria>
    - `deferred-items.md` contains the literal heading `## Code-review finding disposition — gap cycle 2 review (2026-08-13)`.
    - Under it, all eight literal IDs `C2-01` through `C2-08` appear, and the table has exactly eight data rows matching `^\|\s*C2-\d\d\s*\|`.
    - Every row's `Disposition` cell is exactly `FIXED` or `DEFERRED`; `grep -c 'partially\|mostly'` inside the section returns `0`.
    - `node .planning/phases/34.9-.../34.9-C2-SWEEP-CHECK.cjs <deferred> <review>` exits `0` and prints a line matching `C2-SWEEP-OK 8/8 mapped, unmapped 0`.
    - The section contains the literal `Unmapped count: **0**`, and the checker's `COMPUTED-UNMAPPED: 0` agrees with it (the checker exits non-zero if they disagree, so a green run proves agreement).
    - The raw `grep '^### C2-' 34.9-REVIEW-CYCLE2.md` output — eight lines — is reproduced verbatim in the section.
    - The literal sub-heading `### Truth 8 missing-list delivery state (2026-08-13)` exists with exactly three data rows, and each row's first cell quotes one of `34.9-VERIFICATION.md` lines 79-82.
    - The section contains the literal `/gsd-verify-work 34.9` and contains zero occurrences of the literals `truth 8 is satisfied`, `truth 8 is closed`, `Phase 34.9 COMPLETE` and `phase closes`.
    - The pipe census run against the live `package.json` returns `0` scripts matching esbuild-piped-into-node, and that number is stated in the section; if it is non-zero, the C2-01/02/03 rows are NOT `FIXED` and the discrepancy is the section's headline.
    - `pnpm test:ci` completes with `0` failing tests, and the passed/failed counts are recorded in the Summary. If any test fails, name it and do not mark the corresponding row `FIXED`.
    - All four mutations M1-M4 exited non-zero with the expected reason token; all four transcripts are quoted in the Summary; the scratch directory no longer exists.
    - Items 1-16 and every item opened by plans 34.9-26/27 are unchanged: `git diff -U0 -- deferred-items.md` produces zero `^-` lines outside the diff header.
    - `grep -in 'code_verifier\|client_id\|serial' deferred-items.md` returns only pre-existing item 9 lines; quote each in the Summary to show none is a value.
    - The Summary reproduces list A, list B with both stages per ID, and A-minus-B as three explicit lists, plus every SUMMARY-versus-repository disagreement quoted verbatim.
  </acceptance_criteria>
  <verify>
    <automated>D=.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co; S=$D/34.9-C2-SWEEP-CHECK.cjs; F=$D/deferred-items.md; node "$S" "$F" "$D/34.9-REVIEW-CYCLE2.md" | tee /dev/stderr | grep -q 'C2-SWEEP-OK 8/8 mapped, unmapped 0' && node -e "const fs=require('fs');const s=fs.readFileSync(process.argv[1],'utf8');const i=s.indexOf('## Code-review finding disposition — gap cycle 2 review (2026-08-13)');if(i<0){console.error('missing heading');process.exit(1)}const sec=s.slice(i);const rows=sec.split('\n').filter(l=>/^\|\s*C2-\d\d\s*\|/.test(l));if(rows.length!==8){console.error('rows='+rows.length);process.exit(1)}if(/partially|mostly/i.test(sec.split('\n## ')[0])){console.error('hedged disposition');process.exit(1)}for(const t of ['Unmapped count: **0**','### Truth 8 missing-list delivery state (2026-08-13)','/gsd-verify-work 34.9'])if(!sec.includes(t)){console.error('missing '+t);process.exit(1)}for(const b of ['truth 8 is satisfied','truth 8 is closed','Phase 34.9 COMPLETE','phase closes'])if(sec.includes(b)){console.error('forbidden claim: '+b);process.exit(1)}console.log('SECTION-OK')" "$F" && node -e "const p=require('./package.json');const bad=Object.entries(p.scripts).filter(([k,v])=>/esbuild/.test(v)&&/\|\s*node\b/.test(v));if(bad.length){console.error('surviving pipe instances: '+bad.map(x=>x[0]).join(','));process.exit(1)}console.log('CENSUS-ZERO-SURVIVORS')" && M="${TMPDIR:-/tmp}/34.9-28-verify-mut"; rm -rf "$M"; mkdir -p "$M"; sed -E '/^\|[[:space:]]*C2-01[[:space:]]*\|/d' "$F" > "$M/m1.md"; test "$(grep -c '^|[[:space:]]*C2-01[[:space:]]*|' "$M/m1.md")" -eq 0 || { echo 'M1 mutation did not remove the row'; rm -rf "$M"; exit 1; }; if node "$S" "$M/m1.md" "$D/34.9-REVIEW-CYCLE2.md" >/dev/null 2>&1; then echo 'M1 FAILED TO GO RED'; rm -rf "$M"; exit 1; fi; rm -rf "$M" && test "$(git diff -U0 -- "$F" | grep -c '^-[^-]')" -eq 0 && echo SWEEP-GREEN-AND-FALSIFIABLE</automated>
  </verify>
  <done>All eight C2 findings are enumerated from the review itself and each maps to a confirmed landed fix or a structured ledger item with zero unmapped; every FIXED row is scored from repository state outside `.planning/`; the truth-8 missing-list delivery state is recorded from disk including any partial outcomes; and the predicate was OBSERVED going red against four deliberately incomplete inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP, REQUIREMENTS and STATE to the post-cycle-3 truth without closing the phase</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` ~lines 2414-2424 (the Phase 34.9 `**Plans:**` line, currently `22 total (17 original + 5 gap cycle 2) — 22/22 executed on disk`), ~lines 2473-2482 (the `**Phase status 2026-08-12: does not close yet.**` note), and ~lines 2545-2560 (the gap cycle 2 block ending with the `34.9-22` row — the new block goes after it).
    - `.planning/REQUIREMENTS.md` — the REQ-34.9-08 status-table row (~line 372) and the REQ-34.9-08 checkbox entry (~line 834), both of which already carry a 2026-08-12 addendum from plan 34.9-22.
    - `.planning/STATE.md` frontmatter (lines 6-14): `stopped_at: "Completed 34.9-22-PLAN.md"`, `total_plans: 297`, `completed_plans: 284`, `percent: 74`, `completed_phases: 17`, `total_phases: 23`; and the Phase 34.9 paragraph beneath the Current Position banner, which belongs to **Phase 34.5**, not this phase.
    - `.planning/phases/34.9-.../34.9-23-SUMMARY.md` .. `34.9-28`'s siblings — one-line objectives and outcomes for the gap-cycle-3 ROADMAP rows, including any deviation worth a row-level note.
    - `.planning/phases/34.9-.../34.9-VERIFICATION.md` frontmatter — `status: gaps_found`, the value that must remain the phase's recorded state after this plan.
  </read_first>
  <action>
    **ROADMAP.md.**

    Update the Phase 34.9 `**Plans:**` line to `28 total (17 original + 5 gap cycle 2 + 6 gap cycle
    3)`, with the executed count derived by counting the `-SUMMARY.md` files actually present in the
    phase directory at edit time plus this closing plan's own, exactly as the existing line documents
    its own derivation. State the derivation in the line, not just the number.

    Append a block after the gap cycle 2 block: `**Gap cycle 3 — planned 2026-08-13 at
    `/gsd-plan-phase 34.9 --gaps`.**` stating what it closes — `34.9-VERIFICATION.md` truth 8's second
    failure (C2-01, the `esbuild ... | node -` pipeline swallowing a compile failure) plus the seven
    other `34.9-REVIEW-CYCLE2.md` findings that appeared in no ledger — then one `- [x]` row per plan
    `34.9-23` through `34.9-28`, each naming its objective and its actual outcome read from its
    SUMMARY. The `34.9-26` row carries the literal verdict from `34.9-PIPE-PROOF.md`. The `34.9-28`
    row states that the phase awaits re-verification, not that it closes.

    Update the `**Phase status 2026-08-12: does not close yet.**` note: keep it, and append a dated
    2026-08-13 paragraph recording that gap cycle 3 landed the pipe-idiom conversion, its
    both-direction proof, the C2-06/C2-08 fixes, the C2-04 wiring pin, the C2-05/C2-07 ledger entries
    and the eight-finding sweep — and that truth 8 must STILL be re-scored by the verifier against
    that landed evidence. Do not rewrite the 2026-08-12 paragraph's own historical claims; append.
    End with the literal `/gsd-verify-work 34.9`.

    **REQUIREMENTS.md.** Append a dated 2026-08-13 addendum to both the REQ-34.9-08 status-table row
    and the REQ-34.9-08 checkbox entry, recording gap cycle 3: which plan IDs landed what, the
    conversion's scope, and which `deferred-items.md` items now carry C2-05 and C2-07. Mirror a
    one-sentence version into the status-table row so the two never disagree. REQ-34.9-08's
    ticked/unticked state is **unchanged** by this plan — its status is the verifier's to re-score.
    Do not mint new REQ IDs. Do not touch any other phase's rows.

    **STATE.md.** Do NOT touch the Current Position banner, which belongs to Phase 34.5's active
    blocker. Update the existing Phase 34.9 paragraph beneath it to record gap cycle 3's completion
    (`34.9-23`..`34.9-28`), what landed, that `34.9-VERIFICATION.md` remains `status: gaps_found`,
    that truth 8 awaits re-scoring, and the literal next step `/gsd-verify-work 34.9`. Keep the
    guard's scope statement — arm64-only, local `dist:mac`/`release:mac`, not in CI — in the same
    paragraph, so a reader of STATE alone cannot form an overclaim.

    Frontmatter, hand-edited. Invoke no `gsd-sdk state.*` write verb: plan 34.9-22 sidestepped the
    documented corruption class entirely by hand-editing, and that is the pattern here. If any SDK
    write does touch the file, re-read it and hand-correct every field below, and say so in the
    Summary.
    - `stopped_at` → `Completed 34.9-28-PLAN.md`; `last_updated` → the real current UTC timestamp;
      `last_activity` → a 2026-08-13 line naming gap cycle 3's completion and the phase still being
      `gaps_found`.
    - `total_plans` → `297` plus the count of `34.9-2[3-8]-PLAN.md` files present in the phase
      directory. This is absolute, not an increment, so re-running it cannot double-count. In the
      expected state that is `297 + 6 = 303`. `total_plans` counts plans that EXIST — a completing
      plan is not an added plan, which is the exact error plan 34.9-22 had to correct.
    - `completed_plans` → `284` plus the count of `34.9-2[3-8]-SUMMARY.md` files present at edit time
      plus `1` for this plan's own Summary, which is written after this task per the closing-plan
      convention plan 34.9-22 used. In the expected all-green state that is `284 + 5 + 1 = 290`. If
      fewer cycle-3 Summaries exist, the lower number is correct and the gap is named in the Summary.
    - `percent`, `completed_phases`, `total_phases` → **unchanged** at `74`, `17`, `23`. `percent`
      tracks PHASES, not plans, and Phase 34.9 does not complete in this plan. Writing a plan-derived
      percentage here would reproduce the 2026-08-09 corruption by hand.
    Record before and after values for all five progress fields in the Summary.

    **Scoped prose check — added lines only.** After editing, take this plan's added lines
    (`git diff -U0` filtered to `^+` across the three files plus `deferred-items.md`) and check them,
    not the whole files. Plan 34.9-22's whole-file `tripwire` scan flagged four pre-existing Phase
    34.2/34.4 occurrences outside its scope; repeating that would push this plan into editing
    unrelated ledger rows. Every added passage — a maximal run of consecutive added lines — that
    contains `tripwire` must also contain `arm64` and `not in CI` in the same passage. Every added
    passage that contains `compile failure`, `exit code`, `cannot go dark` or `can no longer` must
    cite `34.9-PIPE-PROOF` in the same passage. A claim that is true only under conditions it does
    not state is the same defect in a smaller font.

    Also scan the phase's own narrative documents for prose the conversion has now made stale —
    passages asserting that the guard chain's exit-code fidelity is unproven. Correct such passages
    only where they live in `ROADMAP.md` or `REQUIREMENTS.md`. Where one lives in a signed-off run
    record (`34.9-LIVE-GATE-RERUN.md`, `34.9-GUARD-PROOF.md`, `34.9-REVIEW*.md`,
    `34.9-VERIFICATION.md`), record the locator in the Summary as an observation and leave the file
    byte-identical: those documents' claims are scoped to the run that produced them and remain true
    of it, and rewriting a signed-off record to match a later change is itself a falsification risk.

    Do not mark Phase 34.9 complete anywhere. Do not touch any other phase's rows. Do not push.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains one `- [x] 34.9-NN-PLAN.md` row for each of `34.9-23`, `34.9-24`, `34.9-25`, `34.9-26`, `34.9-27`, `34.9-28`, each appearing exactly once.
    - The Phase 34.9 `**Plans:**` line contains the literal `28 total` and no longer contains the literal `22 total`.
    - `.planning/ROADMAP.md` contains the literal `Gap cycle 3 — planned 2026-08-13`, the literal `2026-08-13` within the Phase 34.9 status note, and the literal `/gsd-verify-work 34.9`.
    - `.planning/ROADMAP.md` contains zero occurrences of the literals `Phase 34.9 COMPLETE`, `34.9 CLOSES` and `truth 8 is satisfied`.
    - `.planning/REQUIREMENTS.md` contains `2026-08-13` within both the REQ-34.9-08 status-table row and the REQ-34.9-08 checkbox entry, and both name the C2-05/C2-07 ledger item numbers.
    - REQ-34.9-08's checkbox marker is unchanged: `git diff -U0 -- .planning/REQUIREMENTS.md | grep -c '^[-+]- \[.\] \*\*REQ-34.9-08\*\*'` is either `0`, or `2` with the identical marker character on the `-` and `+` lines.
    - `.planning/STATE.md` contains the literals `34.9-28`, `/gsd-verify-work 34.9`, `gaps_found` and `not in CI`.
    - `.planning/STATE.md`'s Phase 34.5 blocker banner is unchanged: `git diff -U0 -- .planning/STATE.md | grep -c '^-.*34.5-LIVE-GATE-RERUN-3'` returns `0`.
    - `percent`, `completed_phases` and `total_phases` are unchanged: `git diff -U0 -- .planning/STATE.md | grep -c '^[-+][[:space:]]*\(percent\|completed_phases\|total_phases\):'` returns `0`.
    - `total_plans` equals `297` plus the count of `34.9-2[3-8]-PLAN.md` files (expected `303`), and `completed_plans` equals `284` plus the count of `34.9-2[3-8]-SUMMARY.md` files plus `1` (expected `290`); `completed_plans` ≤ `total_plans`. Both formulas and both file counts are stated in the Summary.
    - Scoped prose check passes on ADDED lines only across the four files: zero added passages containing `tripwire` without `arm64` and `not in CI`; zero added passages containing `compile failure`, `exit code`, `cannot go dark` or `can no longer` without `34.9-PIPE-PROOF`.
    - `git diff --name-only` across this plan's commits equals exactly this plan's `files_modified` — no other phase's ledger rows were touched, and no signed-off run record was edited.
    - The Summary records before/after values for all five `progress` fields, states that no `gsd-sdk state.*` write verb was invoked (or that the file was re-read and hand-corrected after one was), and lists every stale-prose locator found in a signed-off record and deliberately left untouched.
  </acceptance_criteria>
  <verify>
    <automated>for n in 23 24 25 26 27 28; do test "$(grep -c "^- \[x\] 34.9-$n-PLAN.md" .planning/ROADMAP.md)" -eq 1 || { echo "missing row 34.9-$n"; exit 1; }; done && grep -q '28 total' .planning/ROADMAP.md && test "$(grep -c '22 total' .planning/ROADMAP.md)" -eq 0 && grep -q 'Gap cycle 3 — planned 2026-08-13' .planning/ROADMAP.md && grep -q '/gsd-verify-work 34.9' .planning/ROADMAP.md && for b in 'Phase 34.9 COMPLETE' '34.9 CLOSES' 'truth 8 is satisfied'; do test "$(grep -c "$b" .planning/ROADMAP.md)" -eq 0 || { echo "forbidden: $b"; exit 1; }; done && grep -q '/gsd-verify-work 34.9' .planning/STATE.md && grep -q 'gaps_found' .planning/STATE.md && grep -q 'not in CI' .planning/STATE.md && test "$(git diff -U0 -- .planning/STATE.md | grep -c '^-.*34.5-LIVE-GATE-RERUN-3')" -eq 0 && test "$(git diff -U0 -- .planning/STATE.md | grep -c '^[-+][[:space:]]*\(percent\|completed_phases\|total_phases\):')" -eq 0 && node -e "const fs=require('fs');const D='.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co';const fm=fs.readFileSync('.planning/STATE.md','utf8').split('---')[1];const g=k=>Number((fm.match(new RegExp(k+':\\\\s*(\\\\d+)'))||[])[1]);const plans=fs.readdirSync(D).filter(f=>/^34\.9-2[3-8]-PLAN\.md$/.test(f)).length;const sums=fs.readdirSync(D).filter(f=>/^34\.9-2[3-8]-SUMMARY\.md$/.test(f)).length;const tp=g('total_plans'),cp=g('completed_plans');if(tp!==297+plans){console.error('total_plans='+tp+' expected='+(297+plans));process.exit(1)}if(cp!==284+sums+1&&cp!==284+sums){console.error('completed_plans='+cp+' expected='+(284+sums+1));process.exit(1)}if(cp>tp){console.error('completed>total');process.exit(1)}console.log('STATE-ARITHMETIC-OK total_plans='+tp+' completed_plans='+cp)" && node -e "const {execSync}=require('child_process');const files=['.planning/ROADMAP.md','.planning/REQUIREMENTS.md','.planning/STATE.md','.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md'];let bad=0;for(const f of files){let d='';try{d=execSync('git diff -U0 -- '+f,{encoding:'utf8'})}catch(e){continue}const added=d.split('\n').filter(l=>/^\+[^+]/.test(l)).map(l=>l.slice(1));let buf=[];const flush=()=>{const p=buf.join(' ');if(p){if(/tripwire/.test(p)&&!(/arm64/.test(p)&&/not in CI/.test(p))){console.error('unscoped tripwire in '+f+': '+p.slice(0,120));bad++}if(/compile failure|exit code|cannot go dark|can no longer/.test(p)&&!/34\.9-PIPE-PROOF/.test(p)){console.error('uncited fidelity claim in '+f+': '+p.slice(0,120));bad++}}buf=[]};for(const l of added){if(l.trim()===''){flush()}else{buf.push(l)}}flush()}process.exit(bad?1:0)" && echo LEDGERS-RECONCILED</automated>
  </verify>
  <done>ROADMAP carries a complete gap-cycle-3 block and a corrected plan count, REQUIREMENTS and STATE carry dated 2026-08-13 addenda, STATE's arithmetic is hand-verified with before/after values recorded and the phase-axis fields untouched, every added prose passage carries its own scope, and all three documents record `gaps_found` with `/gsd-verify-work 34.9` as the next step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review document → phase ledger | The boundary this plan exists to police. A security-relevant finding that never crosses it is indistinguishable, at the next planning pass, from a finding that never existed. This phase has now demonstrated the failure twice: six findings survived gap cycle 1's reconciliation, and eight more survived gap cycle 2's. |
| the sweep itself → the verifier's judgment | **The plan's own most dangerous surface.** `/gsd-verify-work 34.9` will read this sweep as evidence. A sweep that is itself wrong — a table that passes because it satisfies its own predicate — manufactures exactly the false assurance the phase is trying to eliminate. The independent check is that the predicate is proven RED against four deliberately incomplete inputs, and that no `FIXED` row may be scored from any `.planning/` document. |
| plan SUMMARY claim → landed-fix status | A SUMMARY is a mutating agent's own report. Trusting it here would reproduce the failure mode proven three separate times in this phase (build-banner trust, restore-claim trust, exit-status-capture trust). |
| phase narrative → release operator's checklist | An overstated protection claim retires a manual check from someone's mental model. This is why every added passage must carry its own scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.9C3-29 | Repudiation | a code-review finding vanishing from the record | mitigate | List A is derived at execution time by matching `^### (C2-\d{2})` against `34.9-REVIEW-CYCLE2.md` itself, never from any hand-written list including this plan's. Set equality is enforced in both directions (`NO-ROW` and `EXTRA-ROW`). The checker exits non-zero on any unmapped ID. Zero unmapped is a gate, not a goal. |
| T-34.9C3-30 | Spoofing | a passing mention of a finding ID impersonating a ledger entry | mitigate | The `DEFERRED` arm resolves `item N` to a structured `### N.` section and requires the finding ID plus `Blocker`, `Named precondition` and `OWNER:` in that section. The disposition table lives under a `##` heading and can never be parsed as an item section. Mutation M4 proves the predicate goes red when the ID is present in prose but absent from the structured entry. |
| T-34.9C3-31 | Spoofing | a plan's self-report impersonating landed evidence | mitigate | Every `FIXED` row requires an `Independent confirmation` cell naming a `meta/`, `src/` or `package.json` artifact, and the checker rejects any cell containing `.planning/` or `SUMMARY`. Mutation M3 proves that arm goes red. Where a SUMMARY claim and the repository disagree, the plan requires the disagreement quoted verbatim and the row left un-FIXED. |
| T-34.9C3-32 | Tampering | the sweep certifying itself | mitigate | The document states a count; the checker recomputes it independently and exits non-zero on disagreement (`COUNT-DISAGREES`). An empty list A exits `2` (`LIST-A-EMPTY`) rather than passing vacuously. The predicate was OBSERVED red against the pre-content document (`MISSING-DISPOSITION-HEADING`) before it was ever green. Four mutations, each targeting a distinct predicate arm, are required with transcripts. |
| T-34.9C3-33 | Information Disclosure | an unrecorded security-relevant finding shipping silently | mitigate | This plan is the phase's last line of defence for that. The truth-8 delivery-state table records each `missing:` item's state from disk, at script granularity cross-checked between `34.9-PIPE-AUDIT.md`'s census and `34.9-PIPE-PROOF.md`'s matrix, so a censused script with no proof row is named rather than absorbed into a per-file "COVERED" verdict. |
| T-34.9C3-34 | Elevation of Privilege | a documentation plan closing a phase the verifier has not scored | mitigate | The plan forbids declaring truth 8 satisfied or the phase complete; the section-level check rejects the literals `truth 8 is satisfied`, `truth 8 is closed`, `Phase 34.9 COMPLETE` and `phase closes`, and ROADMAP is checked for the same. Both ROADMAP and STATE must record `gaps_found` and name `/gsd-verify-work 34.9`. |
| T-34.9C3-35 | Tampering | `gsd-sdk` corrupting `.planning/STATE.md` | mitigate | No `state.*` write verb is invoked; all edits are hand-edits, the pattern plan 34.9-22 used to sidestep the documented corruption class. `total_plans` and `completed_plans` are absolute baseline-plus-derived-count formulas, so a re-run cannot double-count. `percent`/`completed_phases`/`total_phases` are asserted unchanged by a diff check, since `percent` tracks phases and Phase 34.9 does not complete here. |
| T-34.9C3-36 | Tampering | edits leaking into a signed-off run record or another phase's rows | mitigate | Signed-off records (`34.9-LIVE-GATE-RERUN.md`, `34.9-GUARD-PROOF.md`, `34.9-REVIEW*.md`, `34.9-VERIFICATION.md`) are excluded from `files_modified`; stale prose found in them is reported as an observation, not edited. Acceptance criteria require `git diff --name-only` to equal `files_modified` exactly, zero deletions in `deferred-items.md`, and the Phase 34.5 banner untouched. Mutations run only on scratch copies outside the repository, and `git checkout --` is forbidden (it fires `.husky/post-checkout` into a network install). |
| T-34.9C3-37 | Repudiation | an unscoped prose claim retiring a real check | mitigate | The prose check runs on ADDED lines only, per passage: `tripwire` requires `arm64` and `not in CI`; `compile failure`/`exit code`/`cannot go dark`/`can no longer` require a `34.9-PIPE-PROOF` citation. Scoping to added lines is deliberate — plan 34.9-22's whole-file scan flagged four pre-existing Phase 34.2/34.4 occurrences and would have pushed this plan into out-of-scope edits. |
| T-34.9C3-SC | Tampering | npm/pip/cargo installs | N/A | **This plan installs no packages and changes no application source.** The only new file is a dependency-free `.cjs` verification script under `.planning/`, using `node:fs` alone and not added to `package.json` or any jest config. The Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- All eight `34.9-REVIEW-CYCLE2.md` finding IDs appear in the disposition table, each mapped to a confirmed landed fix or a structured ledger item; computed unmapped count `0`, agreeing with the document's stated count.
- `34.9-C2-SWEEP-CHECK.cjs` exits `0` against the real files and was observed exiting non-zero against all four mutated copies plus the pre-content baseline, with the reason token recorded each time.
- Every `FIXED` row's confirmation names a non-`.planning` artifact; the live `package.json` census returns zero surviving esbuild-piped-into-node scripts; `pnpm test:ci` reports zero failures.
- The truth-8 missing-list delivery-state table has one row per `missing:` item, filled from disk, with partial or deferred outcomes recorded rather than smoothed, and cross-checked at script granularity between `34.9-PIPE-AUDIT.md` and `34.9-PIPE-PROOF.md`.
- `deferred-items.md` items 1-16 and every item opened by plans 34.9-26/27 are unmodified (diff shows additions only).
- `ROADMAP.md` carries `- [x]` rows for `34.9-23`..`34.9-28`, a plan count of 28, a dated 2026-08-13 status paragraph, and no closure claim.
- `REQUIREMENTS.md` carries a dated 2026-08-13 addendum in both REQ-34.9-08 locations with the tick state unchanged; no new REQ IDs.
- `STATE.md`'s progress arithmetic is hand-verified with before/after values recorded; `percent`/`completed_phases`/`total_phases` unchanged; Phase 34.5's blocker banner untouched.
- Every added prose passage carries its own scope; no signed-off run record was edited.
- No user-facing string is added — the i18n gate is N/A; state this explicitly in the Summary.
- No push, no new REQ IDs, no `git checkout --`, no scratch files left behind.
</verification>

<success_criteria>
Every one of the eight gap-cycle-2 review findings now has exactly one home: a landed fix confirmed
against the repository itself, or a dated, owned ledger entry with a blocker stated as a mechanism
and a named precondition. The set-difference is explicit, its unmapped count is computed rather than
asserted, and the predicate that computes it has been observed going red against four deliberately
incomplete inputs — so a finding that really were unmapped would fail loudly instead of passing
quietly. The three items on truth 8's `missing:` list have their delivery state recorded from disk,
including any partial outcome a cycle-3 plan's own Summary reported. The phase's ledgers agree with
each other and with the repository, every added claim carries its own scope, and the phase is left
OPEN with `/gsd-verify-work 34.9` named as the step that scores truth 8 — a judgment this plan makes
the evidence sufficient for, and deliberately does not make itself.
</success_criteria>

<output>
Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-28-SUMMARY.md` when done.
</output>
