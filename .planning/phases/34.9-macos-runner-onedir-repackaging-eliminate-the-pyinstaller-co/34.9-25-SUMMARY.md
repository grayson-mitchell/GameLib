---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 25
subsystem: infra
tags: [esbuild, pnpm, build-scripts, exit-code, ci-guard]

# Dependency graph
requires:
  - phase: 34.9-23
    provides: 34.9-PIPE-AUDIT.md's census (13 scripts), compile baseline, caller map, __dirname/require.main measurements and the stale-comment inventory this plan corrects
  - phase: 34.9-24
    provides: meta/verifyRunnerBundle.ts's resolvedTopLevelTargetExists (shared file, untouched here beyond the same comment-accuracy fix applied elsewhere)
provides:
  - Every package.json script the audit's census matched now compiles to a per-script node_modules/.cache/*.cjs file and executes that same file behind `&&`, so a compile failure aborts the chain instead of `node` exiting 0 on empty stdin
  - Corrected meta/*.ts comments describing the new __dirname/require.main behaviour under the &&-chained idiom
  - "## 8. Conversion record" appended to 34.9-PIPE-AUDIT.md
affects: [34.9-26, 34.9-27, 34.9 gap cycle 3 verification (truth 8)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "esbuild --outfile=node_modules/.cache/<script-name-dashed>.cjs <entry> && node node_modules/.cache/<script-name-dashed>.cjs -- one cache file per SCRIPT (not per entry file), applied to every package.json build/lint/generator script that previously piped into node/node -"

key-files:
  created: []
  modified:
    - package.json
    - meta/cleanDistMac.ts
    - meta/genI18nGateScope.ts
    - meta/buildCrossoverIndex.ts
    - meta/gen_vtables.ts
    - meta/i18nCatalogChurnGuard.ts
    - meta/buildSteamBridgeShims.ts
    - meta/downloadHelperBinaries.ts
    - meta/buildRunnersOnedir.ts
    - meta/buildSidecarSea.ts
    - meta/verifyRunnerBundle.ts
    - meta/machineFillGamelib.ts
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PIPE-AUDIT.md

key-decisions:
  - "Converted meta/machineFillGamelib.ts's stale JEST_WORKER_ID comment even though it is absent from this plan's frontmatter files_modified list, because the plan's own <interfaces> block instructs the audit's Section 7 inventory to win over the frontmatter count when they disagree, and Section 7 names this file."
  - "Fixed one additional stale pipe-invocation reference at meta/buildSteamBridgeShims.ts:135 (bundledBinDir doc comment) that Section 7 did not enumerate but this task's own required grep sweep (`via .* | node`) surfaced -- kept the comment's real correctness claim (cwd-relative join() is unaffected by the __dirname change) while correcting the invocation description."
  - "Dropped cleanDistMac.ts's 'measured directly' claim about the old process.cwd() default rather than restate it for node_modules/.cache -- the plan explicitly requires a measurement claim to cite its measurement or be dropped, and no Section 7/Section 4 measurement supports a claim about the new cache-dir default specifically."

patterns-established:
  - "verify:updater-key's --outfile && node idiom is now the ONLY invocation shape for esbuild-bundled meta/ scripts in package.json -- zero surviving | node instances."

requirements-completed: [REQ-34.9-08]

duration: 26min
completed: 2026-08-13
---

# Phase 34.9 Plan 25: Convert every censused esbuild|node pipe to --outfile && node Summary

**Converted all 13 `esbuild --bundle ... | node` pipe-to-interpreter scripts in package.json to the `--outfile=node_modules/.cache/<name>.cjs && node <same path>` idiom, and corrected the 11 meta/*.ts comments the conversion made false.**

## Performance

- **Duration:** ~26 min
- **Started:** 2026-08-13T16:40:00+12:00 (approx)
- **Completed:** 2026-08-13T17:06:00+12:00
- **Tasks:** 2
- **Files modified:** 13 (package.json, 34.9-PIPE-AUDIT.md, 11 meta/*.ts files)

## Accomplishments
- Zero `esbuild ... | node` / `| node -` instances remain in `package.json` -- the census predicate from `34.9-PIPE-AUDIT.md` Section 1, re-run against the modified file, returns an empty bucket (a).
- Every converted script preserves its own `--target` (`node21` vs `build:sidecar-sea`'s `node22`), its entry file, and any chained prefix (`export LINT_TRANSLATIONS_NAMESPACES=gamelib &&`, `pnpm build:sidecar &&`) byte-identically.
- `lint-translations` and `lint-translations:gamelib` resolve to two distinct `.cjs` cache files, so an interleaved run of both cannot clobber the other's output.
- `verify:updater-key` -- the template this conversion follows -- is byte-identical to its pre-task state.
- Every `meta/*.ts` comment the audit's Section 7 "Stale comment inventory" named (11 files, including `meta/machineFillGamelib.ts`, which the inventory named but this plan's frontmatter file list omitted) now describes the real post-conversion mechanism instead of the retired stdin one. The `JEST_WORKER_ID` guard idiom itself is unchanged as code everywhere -- proven by a comment-only diff assertion across the whole `meta/` tree.
- The existing `doc-comment accuracy pins (IN-01/IN-02)` suite in `meta/__tests__/cleanDistMac.test.ts` still passes (all 4 pins, 15/15 tests in that file) -- this task's rewrite of the __dirname/require.main passages (lines 140-146, 163-167 pre-edit) did not touch the pinned phrases at lines 51-52/88-89.

## Task Commits

Each task was committed atomically:

1. **Task 1: Convert every censused script to the --outfile && node idiom** - `f3ed25465` (fix)
2. **Task 2: Correct every comment the conversion made false** - `dc7652e0b` (docs)

_No plan-metadata commit prefix conflict: both tasks committed cleanly with hooks running (no `--no-verify` used)._

## Files Created/Modified
- `package.json` - 13 scripts converted from `esbuild ... | node[-]` to `esbuild ... --outfile=node_modules/.cache/<name>.cjs <entry> && node node_modules/.cache/<name>.cjs`; `verify:updater-key` untouched.
- `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PIPE-AUDIT.md` - Appended `## 8. Conversion record`: per-script before/after/cache-path/status table, the re-run census predicate's empty output, and the empirical cache-directory-lifecycle experiment (removed, one script run, `.cjs` recreated by esbuild; searched for any mid-chain removal of `node_modules/.cache` -- none found).
- `meta/cleanDistMac.ts` - Corrected the `__dirname` passage (now names `node_modules/.cache` as the resolution target, dropped the unsupportable "measured directly" claim) and the `require.main`/`JEST_WORKER_ID` guard comment.
- `meta/genI18nGateScope.ts` - Same two corrections, parallel to `cleanDistMac.ts`.
- `meta/buildCrossoverIndex.ts`, `meta/gen_vtables.ts`, `meta/i18nCatalogChurnGuard.ts`, `meta/buildSteamBridgeShims.ts`, `meta/downloadHelperBinaries.ts`, `meta/buildRunnersOnedir.ts`, `meta/buildSidecarSea.ts`, `meta/verifyRunnerBundle.ts`, `meta/machineFillGamelib.ts` - Each file's `JEST_WORKER_ID`/`require.main` guard comment corrected to describe file execution instead of stdin execution.
- `meta/buildSteamBridgeShims.ts` also had one extra correction at line 135 (`bundledBinDir` doc comment), found by this task's own grep sweep rather than the audit's Section 7 list -- see Deviations.

## Decisions Made
- Trusted the audit's Section 7 inventory over this plan's own frontmatter `files_modified` list where they disagreed (`meta/machineFillGamelib.ts`), per the plan's explicit `<interfaces>` instruction: "do not narrow it to the files named in this plan's frontmatter if the inventory names more."
- Where a rewritten `__dirname` comment could no longer support a prior "measured directly" claim (cleanDistMac.ts), dropped the claim rather than restate it for a mechanism the audit never measured -- per the plan's explicit instruction on this exact passage.
- Used `verify:updater-key` itself (already correct, untouched by this plan) to exercise the shared cache-directory-lifecycle experiment in Task 1 Step 4, since none of this plan's own 13 converted scripts could be run to a clean exit in this environment (see Issues Encountered) without either a full macOS release-tooling context or the CI-blocked `download-helper-binaries` sentinel throw.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected a stale pipe-invocation reference outside the audit's Section 7 inventory**
- **Found during:** Task 2, running the task's own required grep sweep (`grep -n 'node -.*stdin\|read from stdin\|reads from stdin\|via .* | node' meta/*.ts`) after applying the 10 (11 counting machineFillGamelib.ts) Section-7-listed fixes.
- **Issue:** `meta/buildSteamBridgeShims.ts:135`'s `bundledBinDir` doc comment stated "this script runs from the repo root via `esbuild --bundle ... | node`" -- factually false after Task 1's conversion, and a match for the task's own acceptance criterion ("zero comment lines assert stdin execution... Quote any surviving match and justify it").
- **Fix:** Rewrote the comment to name the current `&&`-chained invocation (`node node_modules/.cache/build-steam-bridge.cjs`) while preserving its real, still-true claim: the script always runs from the repo root, so `join('public', 'bin', ...)` -- which resolves relative to `process.cwd()`, not this file's compiled location -- is unaffected by the conversion.
- **Files modified:** `meta/buildSteamBridgeShims.ts`
- **Verification:** Post-fix grep sweep (`grep -n 'node -.*stdin\|read from stdin\|reads from stdin\|via .* | node' meta/*.ts`) returns zero matches. Broader `grep -n "stdin\|node -\`" meta/*.ts` returns only an unrelated C header include (`#include <stdint.h>`) in `meta/gen_vtables.ts`'s code-generation template string.
- **Committed in:** `dc7652e0b` (Task 2 commit)

**2. [Rule 4 boundary check, resolved as in-scope] `meta/machineFillGamelib.ts` correction**
- Not an architectural change -- flagged here only to document why a file outside this plan's own frontmatter `files_modified` list was edited. The plan's `<interfaces>` block explicitly pre-authorizes this exact situation ("do not narrow it to the files named in this plan's frontmatter if the inventory names more"), so this required no Rule-4 checkpoint -- it is the plan's own designed behavior, not a discretionary judgment call.

---

**Total deviations:** 1 auto-fixed (Rule 1 -- bug/doc-accuracy), plus 1 plan-directed scope note (not a deviation under Rules 1-4, since the plan itself specifies the audit-over-frontmatter precedence).
**Impact on plan:** Both are strictly within this plan's own stated intent (correct every comment the conversion falsifies, per its own acceptance criteria and interfaces block). No scope creep beyond what the plan itself authorized.

## Issues Encountered
- None of this plan's 13 newly-converted scripts could be exercised to a clean, healthy exit in this local environment to directly prove Task 1 Step 4's cache-directory-lifecycle claim on one of THEM specifically: `download-helper-binaries` throws by design (the six `PENDING-CI-PUBLISH` digest sentinels, a known/recorded environment hazard, not something this plan touches or fixes), and the release/build scripts require macOS packaging tooling context outside this plan's scope. Used `verify:updater-key` (already correct, untouched) instead to exercise the identical shared idiom -- this proves the mechanism (esbuild creates `node_modules/.cache/` when absent, `&&` gates `node` on a zero exit) without requiring a healthy run of any script this task modified. This is a measurement-substitution, not a scope gap: Section 4 of the audit already independently confirmed all 12 distinct entry files compile cleanly (exit 0) on this checkout, so the compile half of every converted script is separately verified.
- `pnpm test:ci`'s full run left a Jest "did not exit one second after the test run" warning (pre-existing async-handle noise unrelated to this plan's changes -- not investigated further, out of this task's scope per the destructive-audit boundary).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 34.9-26 (not this plan) owns proving the fix actually works -- i.e., demonstrating that a deliberately broken compile in one of the 13 converted entry files now aborts its `&&` chain with a non-zero exit, in both directions (before: pipe swallows it; after: `&&` catches it). This plan makes no such claim and explicitly states it is not verified.
- Plan 34.9-27 owns the pin tests for this cycle's comment/idiom changes (`meta/__tests__/**` was out of scope here per the plan's scope fence).
- No new REQ IDs were minted. REQ-34.9-08 is the only requirement this plan advances, and it is not yet closeable until 34.9-26's proof lands.
- `pnpm codecheck` exits 0; `pnpm test:ci` is green at 243/243 suites, 4763/4764 tests (1 pre-existing skip) -- identical to plan 34.9-24's last recorded full-suite baseline, confirming this plan's changes (a script-value-only diff in Task 1, a comment-only diff in Task 2) introduced zero test-count or pass/fail deltas.

## Self-Check: PASSED

All 13 files listed under "Files Created/Modified" (plus this SUMMARY.md) verified present on disk via `[ -f "$f" ]`. Both task commits (`f3ed25465`, `dc7652e0b`) verified present via `git log --oneline --all | grep`. No missing items.

---
*Phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co*
*Completed: 2026-08-13*
