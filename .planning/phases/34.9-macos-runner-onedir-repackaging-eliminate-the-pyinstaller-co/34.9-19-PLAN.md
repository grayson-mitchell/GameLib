---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 19
type: execute
wave: 1
depends_on: []
files_modified:
  - meta/verifyRunnerBundle.ts
  - meta/__tests__/verifyRunnerBundle.test.ts
  - meta/cleanDistMac.ts
  - meta/__tests__/cleanDistMac.test.ts
autonomous: true
requirements: [REQ-34.9-08]

must_haves:
  truths:
    - "`summarise` fails a runner whose framework has NO top-level stub at all, not only one that exists as the wrong type — the blind spot WR-02 named is closed"
    - "The new stub-absent branch is proven to FAIL against a fixture that constructs the known-bad shape, with the bad shape asserted to exist BEFORE the guard is called"
    - "The new branch produces no false positive: the well-formed framework fixture still yields `ok: true` with zero failures"
    - "`isMacArtifact`'s doc comment no longer claims that a macOS-named symlink is always matched; it states which names actually match and that a `MAC_DIR_PATTERN`-only-named symlink matches no branch"
    - "`cleanDistMac`'s containment-throw doc comment states the throw is defense-in-depth against a currently-unreachable input, not an enforced and tested contract"
    - "Both corrected doc comments are pinned by tests, so the corrections cannot silently regress"
  artifacts:
    - path: "meta/verifyRunnerBundle.ts"
      provides: "`summarise` framework loop with a `!fw.topLevelStubExists` failure branch preceding the wrong-type branch"
      contains: "does not exist (F-34.9-01)"
    - path: "meta/__tests__/verifyRunnerBundle.test.ts"
      provides: "`stub-absent` — a fifth `buildGogdlFrameworkFixture` shape — plus a test asserting the known-bad state before calling `summarise`"
      contains: "stub-absent"
    - path: "meta/cleanDistMac.ts"
      provides: "Corrected `isMacArtifact` and `cleanDistMac` doc comments that state reachability honestly"
      contains: "defense-in-depth against a currently-unreachable input"
    - path: "meta/__tests__/cleanDistMac.test.ts"
      provides: "Doc-comment accuracy pins (IN-01/IN-02) following the file's existing source-pin idiom"
      contains: "doc-comment accuracy pins"
  key_links:
    - from: "meta/__tests__/verifyRunnerBundle.test.ts"
      to: "meta/verifyRunnerBundle.ts summarise()"
      via: "stub-absent fixture -> inspectRunnerTree -> summarise, asserting ok === false"
      pattern: "stub-absent"
    - from: "meta/__tests__/cleanDistMac.test.ts"
      to: "meta/cleanDistMac.ts source text"
      via: "readFileSync + whitespace-normalised substring assertions"
      pattern: "readFileSync\\(.*cleanDistMac"
---

<objective>
Close the one known hole in `verify:runner-bundle`'s framework-integrity guard (WR-02) and stop two
doc comments in `meta/cleanDistMac.ts` from overstating what the code guarantees (IN-01, IN-02).

**Purpose — why this plan is wave 1 and MUST land before plan 34.9-20's wiring.**
Plan 34.9-20 wires `pnpm verify:runner-bundle build --arch=arm64` into `dist:mac` and `release:mac`,
promoting that script from an unused tool to the build's mandatory gate. WR-02 is a *known blind
spot in that gate*: `summarise` fails a framework whose `Versions/Current` is missing, and fails one
whose top-level stub exists as the wrong type, but records **nothing** when the top-level stub is
absent entirely — a real malformation a partial dereferencing/copy failure produces (only the
`Versions/Current` symlink survives while `Python.framework/Python -> Versions/Current/Python` is
dropped). Wiring a guard with a hole we already know about would make the guard's future green runs
mean less than they appear to. **So the hole is closed here, first, and 34.9-20 wires a whole guard.**

The two `cleanDistMac.ts` findings are the same disease in prose form: comments that claim more than
the code delivers. Phase 34.9's verification failed on exactly this (truth 8 — narrative claimed "an
automated tripwire... now fires" for a script nothing invoked). Honesty about reachability is the
deliverable for IN-01/IN-02 — **not new code**.

**Scope fence, do not reopen:** REQ-34.9-02/03/04 (CI x64 leg, downloader, digests), REQ-34.9-09
(cold ratio), Tauri-PACKAGED resolution, real-certificate signing/notarization, upstream PR
submission. **IN-03 is NOT this plan's** — the cross-platform `clean:dist-win`/`clean:dist-linux`
question is DEFERRED and owned by plan 34.9-22's ledger entry; do not add either script here.
No new REQ IDs — REQ-34.9-08 owns this entire cycle.

Output: one new `summarise` failure branch, one new test fixture shape + test, two corrected doc
comments, and two doc-comment pin tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PLAN-OUTLINE.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-REVIEW.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-VERIFICATION.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

meta/verifyRunnerBundle.ts — the shape `summarise` consumes (line 98):

  export interface FrameworkInspection — fields: path, name, versionsCurrentExists,
  versionsCurrentIsSymlink, versionsCurrentTarget, resolvedVersionDirExists,
  topLevelStubExists, topLevelStubIsSymlink, codesignDisplay

  `inspectFramework` derives the stub path as join(frameworkDir, name minus a trailing
  ".framework") — i.e. for `Python.framework` the stub is `Python.framework/Python`. It sets
  topLevelStubExists=false when lstat of that path throws.

  export function summarise(results: RunnerInspection[]): Summary  // { ok, failures: string[] }
  export function inspectRunnerTree(root: string, arch?: string): RunnerInspection[]
  export const RUNNERS  // legendary, gogdl, nile

meta/cleanDistMac.ts:

  export const MAC_ARTIFACT_TOKEN = '-macOS-'
  export const MAC_STANDALONE_ENTRIES = ['latest-mac.yml']
  export const MAC_DIR_PATTERN = /^mac(-.+)?$/
  function isMacArtifact(name: string, isDirectory: boolean): boolean   // NOT exported
  export function macArtifactEntries(distDir: string): string[]
  export function cleanDistMac(distDir: string): { removed: string[]; kept: string[] }
  export function main(argv?: string[]): number

Test harness: jest.config.js declares `meta` as a project (ts-jest, node env). The established
invocation idiom across this phase's plans is `npx jest <path> --silent`.
</interfaces>

<normalised_comment_scan>
Doc-comment assertions in this plan are wrap-independent: they must survive any future re-wrapping
of the comment block. The canonical shell scan — strips each comment line's leading ` * ` marker,
joins the file to one line, squeezes runs of whitespace — is:

  sed -E 's/^[[:space:]]*\*[[:space:]]?//' meta/cleanDistMac.ts | tr '\n' ' ' | tr -s ' ' | grep -c '<phrase>'

`sed -E` is REQUIRED and has been verified on this machine's BSD sed. Do NOT substitute
`sed 's/...\*[[:space:]]\?...//'`: in BSD BRE, `\?` is a literal `?`, so the marker is never
stripped, and any phrase that spans a line break then silently returns 0 — a false "absent" reading
that would let a retired claim survive while the criterion reports success. This was caught at
authoring time by running the pipeline against the real pre-edit file.

`grep -c` over this single-line stream returns 1 (present) or 0 (absent). Reuse this exact pipeline
for every doc-comment acceptance criterion below; the jest pin tests perform the same normalisation
in TypeScript.
</normalised_comment_scan>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the `stub-absent` fixture and make `summarise` fail on a fully-absent top-level framework stub (WR-02)</name>

  <files>meta/__tests__/verifyRunnerBundle.test.ts, meta/verifyRunnerBundle.ts</files>

  <read_first>
    - `meta/__tests__/verifyRunnerBundle.test.ts` — the whole file. `buildGogdlFrameworkFixture`
      (line 104) is the source of truth for fixture construction; its four existing shapes
      (`well-formed` / `dereferenced` / `stub-file` / `unresolvable-target`) and the vacuity-guard
      idiom in the `dereferenced` test (lines 345-362: `expect(lstatSync(...).isSymbolicLink())`
      asserted BEFORE `summarise` runs) are the patterns to replicate exactly.
    - `meta/verifyRunnerBundle.ts` lines 98-108 (`FrameworkInspection`) and lines 428-461 (the
      framework-integrity loop inside `summarise`) — the code being modified.
    - `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-REVIEW.md`
      lines 161-184 (WR-02) — the finding and its suggested message text.
  </read_first>

  <behavior>
    Write the fixture and the test FIRST, observe RED, then change `summarise`.

    - Test (new, `stub-absent`): a gogdl `Python.framework` identical to `well-formed` except the
      top-level stub `Python.framework/Python` has been unlinked. Vacuity guard, asserted BEFORE
      `inspectRunnerTree`/`summarise` are called: `existsSync(join(frameworkDir, 'Python'))` is
      `false`, `lstatSync(join(frameworkDir, 'Versions', 'Current')).isSymbolicLink()` is `true`,
      and `existsSync(join(frameworkDir, 'Versions', '3.14', 'Python'))` is `true` — proving the
      framework is malformed in exactly ONE respect, so any failure is attributable to the stub and
      not to a co-occurring defect. Then: the gogdl inspection reports
      `topLevelStubExists === false`, `versionsCurrentIsSymlink === true`,
      `resolvedVersionDirExists === true`; `summarise(...).ok` is `false`; exactly one failure
      string mentions `frameworkDir`; that failure contains `top-level stub`, contains `"Python"`
      (the stub name, quoted), and contains `does not exist (F-34.9-01)`.
    - RED evidence (required, recorded in the SUMMARY): run this test against the UNMODIFIED
      `summarise` and observe it fail on `expect(summary.ok).toBe(false)` with received `true`.
      This is the "guard proven to fail against a known-bad input" requirement — a guard whose
      failing direction was never observed is not a guard.
    - Test (existing, unchanged, no-false-positive baseline): the `well-formed` fixture still
      yields `ok: true` and `failures: []` after the source change.
    - Test (existing, unchanged, discrimination): the `stub-file` shape still fails via the
      wrong-type branch and its message still matches `/top-level stub/` — the two stub defects
      must remain separately diagnosable, not collapsed into one message.
  </behavior>

  <action>
    In `meta/__tests__/verifyRunnerBundle.test.ts`:
    1. Widen `buildGogdlFrameworkFixture`'s `shape` union with a fifth member, `'stub-absent'`,
       and add the corresponding `else if` branch. That branch unlinks the framework's top-level
       stub — `rmSync(join(frameworkDir, 'Python'))` — and touches nothing else. Note precisely in
       a comment that the target is `Python.framework/Python` (the stub `inspectFramework` derives
       from the framework's own name), NOT the sibling `_internal/Python` symlink, which lives
       outside the framework directory and is not what `topLevelStubExists` reflects.
    2. Update that function's doc comment: it currently says "in one of four shapes" — make it five
       and describe `stub-absent` as the partial-copy shape where the `Versions/Current` symlink
       survived but the top-level stub link was dropped.
    3. Add the test described in `<behavior>` to the existing
       `describe('framework structural integrity (F-34.9-01)')` block, immediately after the
       existing `stub-file` test so the two stub shapes read as a pair. Name it so the shape is
       unmistakable, e.g. "top-level stub is absent entirely (partial-copy shape): ok becomes false".
       Use `mkdtempSync(join(tmpdir(), 'verify-runner-bundle-fw-stub-absent-'))` for `root` so the
       existing `afterEach` teardown covers it.
    4. Run the test file and record the RED failure before touching the source.

    Then in `meta/verifyRunnerBundle.ts`, inside `summarise`'s `for (const fw of r.frameworks)`
    loop: replace the single `if (fw.topLevelStubExists && !fw.topLevelStubIsSymlink)` condition
    with `if (!fw.topLevelStubExists) { ... } else if (!fw.topLevelStubIsSymlink) { ... }`. The new
    first branch pushes a failure reading, in the same style as its three siblings:
    `${r.runner}: framework ${fw.path} is malformed -- top-level stub "<stub name>" does not exist
    (F-34.9-01)`, where `<stub name>` is `fw.name` with a trailing `.framework` stripped — the same
    derivation the existing wrong-type message already uses. Leave the existing wrong-type message
    byte-for-byte unchanged so the `stub-file` test keeps matching it. Do not alter the three
    `Versions/Current` conditions. Extend the loop's leading comment to record that the stub is now
    enforced in both directions (absent AND wrong-type) and cite WR-02, so the asymmetry cannot be
    reintroduced as a "simplification".
  </action>

  <verify>
    <automated>npx jest meta/__tests__/verifyRunnerBundle.test.ts --silent</automated>
  </verify>

  <acceptance_criteria>
    - Before the source edit, `npx jest meta/__tests__/verifyRunnerBundle.test.ts -t "stub is absent"`
      FAILS, and the failure is on the `ok` assertion (expected `false`, received `true`) — not on a
      fixture-construction error. The exact received/expected lines are quoted in the SUMMARY.
    - After the source edit, with the `stub-absent` fixture in place,
      `npx jest meta/__tests__/verifyRunnerBundle.test.ts --silent` reports failure of no test and
      the previously-RED test passes, asserting a `summarise` failure string that contains all three
      of: `top-level stub`, `"Python"`, and `does not exist (F-34.9-01)`.
    - `grep -c 'stub-absent' meta/__tests__/verifyRunnerBundle.test.ts` returns at least 2 (the
      union member and the fixture branch).
    - `grep -c 'does not exist (F-34.9-01)' meta/verifyRunnerBundle.ts` returns exactly 2. Baseline
      measured at authoring time: it returns 1 today (the `Versions/Current does not exist`
      message), so a result of 1 means the new branch was not added and a result of 2 is
      attributable to it.
    - `grep -c 'is a real file, not a' meta/verifyRunnerBundle.ts` returns 1 — measured as 1 today,
      so the wrong-type message survived the refactor verbatim.
    - The `well-formed` test still asserts `summary.ok === true` and `summary.failures` equal to
      `[]` and passes — the new branch introduces no false positive.
    - `npx jest meta/__tests__/ --silent` passes with zero failing tests in the `meta` project.
    - `npx tsc --noEmit -p tsconfig.json` (or the repo's configured typecheck script) reports no new
      errors attributable to the widened `shape` union.
  </acceptance_criteria>

  <done>
    `summarise` fails on both stub malformations; the absent-stub direction has been observed
    failing against an unmodified `summarise` and passing against the modified one; the four
    pre-existing framework tests are unchanged and green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the two overstated doc comments in cleanDistMac.ts and pin both corrections (IN-01, IN-02)</name>

  <files>meta/cleanDistMac.ts, meta/__tests__/cleanDistMac.test.ts</files>

  <read_first>
    - `meta/cleanDistMac.ts` — the whole file (159 lines). The two comments being corrected are at
      lines 43-51 (above `isMacArtifact`) and lines 77-88 (above `cleanDistMac`); the containment
      throw itself is at lines 108-114.
    - `meta/__tests__/cleanDistMac.test.ts` lines 190-231 — `describe('electron-builder.yml
      artifactName pin (T-34.9G-12)')` and `describe('package.json wiring pin')` are this file's
      established source-pin idiom (read a file, assert on its contents). The new pins follow it.
      Line 162's symlink test shows the file already proves `rmSync` unlinks rather than follows.
    - `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-REVIEW.md`
      lines 188-222 (IN-01 and IN-02) — the two findings.
    - `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-PLAN-OUTLINE.md`
      lines 32-33 — the settled calls: comment precision ONLY.
  </read_first>

  <action>
    **Behaviour must not change.** Do NOT add the name-only fallback branch IN-01 offers as an
    alternative. Do NOT export `isMacArtifact` or expose any other internal seam to make IN-02's
    throw testable. Do NOT add `clean:dist-win` / `clean:dist-linux` (IN-03 — 34.9-22 owns it).
    `git diff` for `meta/cleanDistMac.ts` must contain no change outside comment lines.

    IN-01 — rewrite the tail of `isMacArtifact`'s doc comment. Delete the claim that a macOS-named
    symlink is matched by the token/standalone-name branches (currently spanning lines 48-50). In
    its place state, precisely: `isDirectory` is lstat-based, so the `MAC_DIR_PATTERN` branch never
    fires for a symlink; a symlink is therefore matched only when its own NAME contains
    `MAC_ARTIFACT_TOKEN` or is listed in `MAC_STANDALONE_ENTRIES`; a symlink whose name matches only
    `MAC_DIR_PATTERN` — the concrete example being a symlink literally named `mac-arm64` — matches
    no branch and is left in place. Add the reachability note: electron-builder always creates the
    per-arch staging directory as a real directory, so this shape is not reachable today; cite
    IN-01. The corrected comment must contain, verbatim, the phrases `symlink literally named` and
    `matches no branch and is left in place`.

    IN-02 — rewrite `cleanDistMac`'s doc comment so the containment check is described as what it
    is. Delete the universal framing "Every removal path is containment-checked". State instead:
    every `entry.name` reaching the check comes from `readdirSync(distDir, { withFileTypes: true })`,
    and real directory entries can never contain a path separator, so `path.resolve(distDir,
    entry.name)` cannot escape `resolve(distDir)` through this call path — the throw is
    defense-in-depth against a currently-unreachable input, not an enforced, tested contract, and no
    test exercises it. Keep the T-34.9G-09 reference and keep the still-true T-34.9G-11 sentence
    about `rmSync` unlinking a symlinked entry rather than following it (that one IS tested, at
    `meta/__tests__/cleanDistMac.test.ts:162`) — say so, and name that test line so a future reader
    can tell the two claims apart. Add the trigger for re-evaluation: if this module ever accepts
    externally-supplied names (e.g. a `--only` filter), the throw becomes reachable and MUST gain a
    test. The corrected comment must contain, verbatim, the phrases `never contain a path
    separator`, `defense-in-depth against a currently-unreachable input`, and `no test exercises it`.

    In `meta/__tests__/cleanDistMac.test.ts`, append a
    `describe('doc-comment accuracy pins (IN-01/IN-02)')` block following the file's existing
    source-pin idiom: `readFileSync` the `meta/cleanDistMac.ts` source, normalise it once in a
    helper that strips each line's leading comment marker and collapses all whitespace runs to
    single spaces (so the pins survive any future re-wrapping of the comments), then assert with
    `toContain` / `not.toContain` on the six phrases above — three required-present, and the two
    retired claims required-absent (`symlink is matched by the token/standalone-name branches` and
    `Every removal path is containment-checked`). Do not assert on the source of the test file
    itself. Resolve the source path from `__dirname` the way the existing pins resolve their targets.
  </action>

  <verify>
    <automated>npx jest meta/__tests__/cleanDistMac.test.ts --silent</automated>
  </verify>

  <acceptance_criteria>
    Using the `<normalised_comment_scan>` pipeline from `<context>` (note the mandatory `sed -E`)
    against `meta/cleanDistMac.ts`:

    - **Known-bad baseline (run BEFORE editing, quoted in the SUMMARY):** the pipeline returns `1`
      for `symlink is matched by the token/standalone-name branches` and `1` for
      `Every removal path is containment-checked`. Both were measured as `1` at authoring time. If
      either returns `0` before the edit, the pipeline is misbehaving or the phrase has drifted —
      the assertion would then be vacuous, so stop and re-derive rather than proceeding.
    - After the edit, the pipeline returns `0` for `symlink is matched by the token/standalone-name
      branches` — the retired IN-01 claim no longer appears.
    - After the edit, the pipeline returns `0` for `Every removal path is containment-checked` —
      the retired IN-02 framing no longer appears.
    - After the edit, the pipeline returns `1` for each of: `symlink literally named`,
      `matches no branch and is left in place`, `never contain a path separator`,
      `defense-in-depth against a currently-unreachable input`, `no test exercises it`. Each was
      measured as `0` at authoring time, so a `1` is attributable to this edit.
    - `npx jest meta/__tests__/cleanDistMac.test.ts --silent` passes, including the new
      `doc-comment accuracy pins (IN-01/IN-02)` block; `grep -c 'doc-comment accuracy pins'
      meta/__tests__/cleanDistMac.test.ts` returns 1.
    - The new pin block is proven non-vacuous: with `meta/cleanDistMac.ts` temporarily reverted to
      its pre-edit content, `npx jest meta/__tests__/cleanDistMac.test.ts -t "doc-comment accuracy"`
      FAILS; restore the edited file immediately afterward and re-run to green. Record both
      observations in the SUMMARY.
    - `git diff -U0 meta/cleanDistMac.ts` shows changes on comment lines only — every added and
      removed line begins (after leading whitespace) with `*`, `/**`, or `//`. No `export`,
      `function`, `if`, `return`, or `rmSync` line is added, removed, or reordered.
    - `git diff --stat` lists no change to `package.json`, so IN-03 stayed out of scope.
    - `npx jest meta/__tests__/ --silent` passes with zero failing tests.
  </acceptance_criteria>

  <done>
    Both doc comments state exactly what the code guarantees, including the honest reachability
    caveat for the containment throw; both corrections are pinned by a test proven to fail against
    the pre-edit source; `cleanDistMac`'s behaviour is byte-identical.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored runner tree (`public/bin/{arch}/darwin`) → vite `copyDir` → `build/bin` | Untrusted-by-omission transform: vite dereferences symlinks, silently changing framework structure. This is F-34.9-01's mechanism. |
| `build/bin` staged tree → electron-builder → `codesign`/notarization → `.dmg`/`.zip` | Structural defects that survive this boundary become **attested** — our signature vouches for a malformed framework. |
| signed artifact → end user's machine | The user's only integrity signal is our signature; they cannot distinguish a well-formed bundle from a partially-copied one. |
| repo `dist/` directory contents → `cleanDistMac`'s `rmSync` | Filesystem entries chosen by name drive a recursive delete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.9-19-01 | Tampering | `summarise` framework loop, `meta/verifyRunnerBundle.ts:454-460` | mitigate | The gate cannot see a fully-absent top-level framework stub, so a partial copy that drops `Python.framework/Python` while keeping `Versions/Current` passes, gets signed and notarized, and ships a broken dynamic-library stub inside an attested artifact. Task 1 adds the `!fw.topLevelStubExists` failure branch and proves its failing direction against the `stub-absent` fixture before the branch exists. This is the precondition for 34.9-20's wiring. |
| T-34.9-19-02 | Spoofing (of assurance) | `meta/verifyRunnerBundle.ts` — the gate as a whole, once 34.9-20 wires it | mitigate | A gate with an unrecorded blind spot spoofs coverage: its exit-0 is read as "the framework is well-formed" when it only means "not malformed in the three-and-a-half ways we check". Landing WR-02 before the wiring, and recording in-code that both stub directions are now enforced (WR-02 cited in the loop comment), keeps the gate's exit code and its actual coverage aligned. |
| T-34.9-19-03 | Repudiation | `meta/cleanDistMac.ts:77-88` doc comment (IN-02) | mitigate | Documenting an unreachable, untested branch as an enforced containment boundary (T-34.9G-09) causes a future auditor to mark containment as covered without re-deriving reachability — the exact failure that produced this phase's truth-8 gap. Task 2 downgrades the claim to defense-in-depth-against-unreachable-input, names the untested status explicitly, and states the trigger (externally-supplied names) that would make it reachable and demand a test. |
| T-34.9-19-04 | Tampering | `cleanDistMac`'s `rmSync(full, { recursive: true, force: true })` | accept | A hostile symlink planted in `dist/` with a macOS-matching name cannot cause an out-of-tree delete: `rmSync` unlinks the link itself rather than following it (T-34.9G-11, proven at `meta/__tests__/cleanDistMac.test.ts:162`), and `readdirSync` names are bare, separator-free segments. Accepted unchanged; this plan only corrects how the comment describes it. Writing to a developer's own `dist/` already implies local write access. |
| T-34.9-19-05 | Information disclosure | new `summarise` failure string | accept | The new message embeds an absolute framework path, which reaches CI logs once 34.9-20 wires the gate. Identical in kind and format to the three existing framework messages; contains no credential, token or user identity. Path disclosure in our own build logs is not a meaningful loss. |
| T-34.9-19-SC | Tampering | npm/pip/cargo installs | accept — not applicable | This plan installs no packages: both tasks edit four existing files in `meta/` using only Node builtins already imported there. Verified by acceptance criterion `git diff --stat` showing no change to `package.json` and no lockfile change. No `[ASSUMED]`/`[SUS]` package therefore exists to gate. |
</threat_model>

<verification>
Run from the repo root, in order:

1. `npx jest meta/__tests__/verifyRunnerBundle.test.ts --silent` — passes; the file now contains the
   four pre-existing framework tests plus the `stub-absent` test.
2. `npx jest meta/__tests__/cleanDistMac.test.ts --silent` — passes; includes the new
   `doc-comment accuracy pins (IN-01/IN-02)` block.
3. `npx jest meta/__tests__/ --silent` — the whole `meta` jest project is green, proving neither
   edit regressed a sibling build script's tests.
4. `git diff -U0 meta/cleanDistMac.ts` — every changed line is a comment line.
5. `git diff --stat` — touches exactly the four files in `files_modified`; `package.json`,
   `electron-builder.yml`, `meta/preserveRunnerSymlinks.ts` and every `.github/workflows/*.yml` are
   untouched (those belong to plans 34.9-18 and 34.9-20).
6. The SUMMARY records both RED observations — Task 1's pre-fix `ok`-assertion failure and Task 2's
   pre-edit pin failure — as quoted output. A guard whose failing direction was never observed does
   not count as closed for this plan.
</verification>

<success_criteria>
- `summarise` emits a distinct, F-34.9-01-tagged failure for a framework with no top-level stub, and
  a separate distinct failure for one whose stub is the wrong type; the two remain diagnosable apart.
- The `stub-absent` fixture builds a framework malformed in exactly one respect and asserts that
  known-bad state before invoking the guard.
- The failing direction of the new branch was observed against the unmodified `summarise` and
  recorded; the well-formed fixture still passes clean.
- Neither `cleanDistMac.ts` doc comment claims a guarantee the code does not deliver, and both
  corrections are pinned by a test that was proven to fail against the pre-edit source.
- `meta/cleanDistMac.ts`'s runtime behaviour is unchanged — comment-only diff.
- Plan 34.9-20 can now wire `verify:runner-bundle` into `dist:mac`/`release:mac` without wiring a
  known blind spot.
</success_criteria>

<output>
Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-19-SUMMARY.md` when done.

The SUMMARY must state plainly that this plan closes WR-02, IN-01 and IN-02 only — verification
truth 8 remains OPEN until plan 34.9-20 wires the guard and plan 34.9-21 observes it firing on real
macOS arm64 hardware. Do not describe the guard as "now protecting the build"; nothing invokes it
yet. Quote the two RED observations verbatim.
</output>
