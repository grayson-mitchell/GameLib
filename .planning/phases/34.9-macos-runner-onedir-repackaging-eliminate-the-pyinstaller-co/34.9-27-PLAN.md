---
phase: 34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co
plan: 27
type: execute
wave: 4
gap_closure: true
gap_cycle: 3
closes_findings: [C2-04, C2-05, C2-07]
depends_on: ["34.9-24", "34.9-25"]
files_modified:
  - meta/__tests__/verifyRunnerBundle.test.ts
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
autonomous: true
requirements: [REQ-34.9-08]

must_haves:
  truths:
    - "A future edit that deletes `pnpm verify:runner-bundle` from `dist:mac` turns the jest suite red"
    - "A future edit that deletes `pnpm verify:runner-bundle` from `release:mac` turns the jest suite red"
    - "A future edit that moves `pnpm verify:runner-bundle` to after `electron-builder` in `dist:mac` turns the jest suite red on the ORDERING assertion specifically"
    - "A future edit that moves `pnpm verify:runner-bundle` to after `electron-builder` in `release:mac` turns the jest suite red on the ORDERING assertion specifically"
    - "C2-05 has a dated, owned `deferred-items.md` entry that records the two details items 12/13 do not carry, and cross-references items 12 and 13 by number instead of restating them"
    - "C2-07 has a dated, owned `deferred-items.md` entry stating its blocker as a mechanism"
    - "`package.json` is byte-identical to its pre-plan state when this plan finishes, and is absent from this plan's commit"
  artifacts:
    - path: "meta/__tests__/verifyRunnerBundle.test.ts"
      provides: "package.json wiring pin for verify:runner-bundle (presence + ordering, both mac scripts)"
      contains: "C2-04"
    - path: ".planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md"
      provides: "Dated, owned deferral entries for C2-05 and C2-07 in the existing house format"
      contains: "C2-05"
  key_links:
    - from: "meta/__tests__/verifyRunnerBundle.test.ts"
      to: "package.json scripts['dist:mac'] and scripts['release:mac']"
      via: "structural JSON.parse of PACKAGE_JSON_PATH, then indexOf comparison"
      pattern: "indexOf.*electron-builder"
---

<objective>
Close C2-04 by adding a wiring-pin regression test that fails when `pnpm verify:runner-bundle`
is dropped from — or reordered after `electron-builder` within — `dist:mac` (`package.json:49`)
or `release:mac` (`package.json:44`). Record C2-05 and C2-07 as dated, owned entries in
`deferred-items.md` per locked user decision D-C3-05 (ledger only, no code fix).

**Scope honesty, stated up front and repeated in the Summary:** this plan does NOT close C2-01,
and the review says so itself — a string-position assertion over `package.json` is blind to the
`esbuild ... | node -` pipeline's swallowed exit code. C2-04 is a distinct, narrower guarantee:
*is the step present, and is it ordered before the thing it is supposed to gate.* Truth 8 of
`34.9-VERIFICATION.md` remains the responsibility of the pipe-idiom conversion work, not this plan.

Purpose: gap cycle 2's entire value is the `verify:runner-bundle` wiring landed by plan 34.9-20.
Nothing currently pins it. `verify:runner-bundle` appears in `meta/__tests__/` exactly once — as a
code comment at `preserveRunnerSymlinks.test.ts:30` — never in an assertion against `package.json`.
`clean:dist-mac` by contrast IS pinned, at `cleanDistMac.test.ts:208-232`. This plan removes that
asymmetry.

Output: two new jest tests observed red against four separate deliberate mutations, plus two ledger
entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
Facts already established — do NOT re-derive them by exploring the codebase.

- `package.json:44` is `release:mac`. Its value chains, in order: `pnpm clean:dist-mac`,
  `pnpm build-steam-bridge`, `electron-vite build`, `pnpm verify:runner-bundle build --arch=arm64`,
  `electron-builder -p always --mac --x64 --arm64`.
- `package.json:49` is `dist:mac`. Its value chains, in order: a `CSC_IDENTITY_AUTO_DISCOVERY=false`
  export, `pnpm clean:dist-mac`, `pnpm build-steam-bridge`, `electron-vite build`,
  `pnpm verify:runner-bundle build --arch=arm64`, `electron-builder --mac`.
- `package.json:71` defines the `verify:runner-bundle` script itself.
- `meta/__tests__/cleanDistMac.test.ts:32` declares `PACKAGE_JSON_PATH` as
  `join(__dirname, '..', '..', 'package.json')`.
- `meta/__tests__/cleanDistMac.test.ts:208-232` is the `describe('package.json wiring pin')` block.
  Its `loadScripts()` helper `JSON.parse`s that file and returns the `scripts` record; its two tests
  assert `toContain('clean:dist-mac')` and
  `indexOf('clean:dist-mac')` `.toBeLessThan(indexOf('electron-builder'))`. This is the pattern to
  mirror.
- `meta/__tests__/verifyRunnerBundle.test.ts` currently contains no reference to `package.json`.
- `deferred-items.md` uses `### <N>. <Title>` headings, numbered 1 through 16 at authoring time,
  each carrying: **What it is**, **Blocker (mechanism, not a summary)**, **Named precondition**,
  **OWNER:** with a bracketed date.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pin verify:runner-bundle's presence and position in both macOS packaging scripts</name>

  <read_first>
    - `meta/__tests__/verifyRunnerBundle.test.ts` (whole file — match its existing import block,
      const-declaration conventions and describe-block style; this is the file being modified)
    - `meta/__tests__/cleanDistMac.test.ts` lines 28-40 and 208-232 (the pattern source: the
      `PACKAGE_JSON_PATH` const, the `PackageJsonScripts` interface, the `loadScripts()` helper,
      and the two existing `clean:dist-mac` assertions)
    - `package.json` lines 36-50 and 71 (the `test` script's actual definition, plus the two
      script values being pinned)
  </read_first>

  <files>meta/__tests__/verifyRunnerBundle.test.ts</files>

  <action>
Add ONE new describe block, named `package.json wiring pin (C2-04)`, at the end of
`meta/__tests__/verifyRunnerBundle.test.ts`. Place it here rather than appending to
`cleanDistMac.test.ts`'s existing block on purpose: C2-04's own failure scenario is "a future
refactor drops the wiring", and a pin that lives inside the test file for a *different* meta script
is collateral damage in any refactor that retires `cleanDistMac`. The pin belongs with the thing it
pins. Duplicating the six-line `loadScripts()` helper is the accepted cost; note that rationale in a
short comment above the describe block so a future reader does not "helpfully" consolidate them.

Declare a module-scope `PACKAGE_JSON_PATH` const using the identical
`join(__dirname, '..', '..', 'package.json')` form from `cleanDistMac.test.ts:32`, and a local
`loadScripts()` that `JSON.parse`s it and returns `.scripts` typed as `Record<string, string>`.
Parse structurally — do NOT read `package.json` as raw text and grep it. Raw text would match the
`"verify:runner-bundle":` script *definition* at line 71 and pass even if both call sites were
deleted; that is precisely the self-satisfying-assertion class this phase has already shipped twice.
Reading only `scripts['dist:mac']` and `scripts['release:mac']` makes the definition unreachable
from the assertion.

Write two tests, one per script, named
`dist:mac invokes pnpm verify:runner-bundle before electron-builder` and
`release:mac invokes pnpm verify:runner-bundle before electron-builder`.

Each test makes exactly two assertions against its own script string:
1. Presence: `toContain` the literal `pnpm verify:runner-bundle`. Include the `pnpm ` prefix — the
   bare token would also be satisfied by a comment-like fragment or a differently-invoked mention;
   the `pnpm ` prefix pins it as an invoked pipeline step.
2. Ordering: the result of `indexOf('pnpm verify:runner-bundle')` `.toBeLessThan` the result of
   `indexOf('electron-builder')`. This is the load-bearing half — presence alone would still pass
   with the step relocated after `electron-builder`, where it gates nothing.

Do NOT assert on `--arch=arm64`, on `build`, or on any other argument of the invocation. That
argument is the subject of deferred item 12 and of C2-05 below; pinning it here would make a future
x64 fix turn this test red for a reason unrelated to C2-04's guarantee.

Do NOT assert anything about the `esbuild ... | node -` idiom inside `scripts['verify:runner-bundle']`.
That is C2-01, it is out of this plan's scope, and asserting it here would falsely imply this test
closes the Critical.
  </action>

  <verify>
    <automated>pnpm test meta/__tests__/verifyRunnerBundle.test.ts</automated>
  </verify>

  <done>
`pnpm test meta/__tests__/verifyRunnerBundle.test.ts` exits 0 with the file's pre-existing test count
plus exactly 2, and the two new test names appear in the reporter output. `grep -c 'C2-04'
meta/__tests__/verifyRunnerBundle.test.ts` returns at least 1. `git status --porcelain package.json`
prints nothing (this task modifies no manifest).
  </done>
</task>

<task type="auto">
  <name>Task 2: Observe the pin go red against all four known-bad mutations, then restore package.json exactly</name>

  <read_first>
    - `meta/__tests__/verifyRunnerBundle.test.ts` (as written by Task 1)
    - `package.json` lines 44 and 49 (the exact two strings to be mutated and restored)
  </read_first>

  <files>package.json (mutated and restored — MUST end byte-identical; not a deliverable)</files>

  <action>
A guard that has never been observed failing is decoration. Prove this one discriminates, against
both shapes it claims to catch, on both scripts — four mutations, run and scored one at a time.

Before touching anything: copy `package.json` to a backup OUTSIDE the repository working tree (the
session scratchpad directory, not `/tmp` directly, and not a sibling path inside the repo where it
could be committed). Record the backup's `shasum -a 256` and the current
`shasum -a 256 package.json` — they must match at the start and after every restore.

Restore is by `cp` from that backup, overwriting `package.json`. Do NOT use `git checkout --
package.json`: this repo's `.husky/post-checkout` hook fires a network install on checkout. Do NOT
use `git stash` for the same class of reason. `git diff` and `git status` are read-only and safe.

The four mutations, each applied to a clean-restored `package.json`, each followed by a scored test
run and an immediate restore:

- M1 — `dist:mac`, step deleted: remove the `pnpm verify:runner-bundle build --arch=arm64 && `
  fragment from `scripts["dist:mac"]`, leaving the rest of the chain intact and the JSON valid.
- M2 — `release:mac`, step deleted: the same removal from `scripts["release:mac"]`.
- M3 — `dist:mac`, step relocated after the gate: move that same fragment so the chain ends
  `... && electron-builder --mac && pnpm verify:runner-bundle build --arch=arm64`.
- M4 — `release:mac`, step relocated after the gate: the same relocation applied to
  `release:mac`'s `electron-builder -p always --mac --x64 --arm64` tail.

After applying each mutation, confirm the file is still valid JSON before running jest (a JSON parse
failure would turn the test red for the wrong reason and would score as a false positive). Then run
`pnpm test meta/__tests__/verifyRunnerBundle.test.ts` and capture the failing assertion, not merely
the exit code.

Score each run against a specific expectation, and treat a mismatch as a plan failure rather than a
curiosity:
- M1 and M2 must fail on the PRESENCE assertion (the `toContain`). If they fail on the ordering
  assertion instead, that is still red but for the wrong reason — investigate; `indexOf` returns -1
  on a missing needle, which is trivially less than any real index, so an M1/M2 failure surfacing
  ONLY as an ordering failure would mean the presence assertion is not doing its job.
- M3 and M4 must fail on the ORDERING assertion (the `toBeLessThan`), with the presence assertion
  passing. If M3 or M4 comes back green, the ordering assertion is decorative and Task 1 must be
  rewritten before this plan proceeds — the whole point of C2-04 is that a relocated step is
  indistinguishable from a correctly-placed one under a substring check.

After each individual restore, verify with `git status --porcelain package.json` printing nothing AND
`shasum -a 256 package.json` matching the recorded baseline hash. Verify after EVERY restore, not
only after the last one: an unnoticed partial restore would silently alter every subsequent build in
this phase, and `dist:mac` is the script the rest of gap cycle 3 depends on.

Finally, run the full meta test file once more against the restored manifest to confirm the suite is
green in the unmutated state, closing the loop.

Record all four mutation results — mutation, expected failing assertion, observed failing assertion,
exit code — as a table in `34.9-27-SUMMARY.md`. A run whose evidence is not written down did not
happen.

Do not run `git push` or anything that triggers `.husky/pre-push`; it is blocked by roughly 50
pre-existing lint errors unrelated to this plan.
  </action>

  <verify>
    <automated>git status --porcelain package.json | grep -c . | grep -qx 0 && pnpm test meta/__tests__/verifyRunnerBundle.test.ts</automated>
  </verify>

  <done>
Four mutation runs recorded. M1 and M2 each exited non-zero with the `toContain('pnpm
verify:runner-bundle')` assertion named in the failure output. M3 and M4 each exited non-zero with
the `toBeLessThan` ordering assertion named in the failure output and the presence assertion passing.
After the final restore, `git status --porcelain package.json` prints nothing, `shasum -a 256
package.json` equals the recorded baseline, and `pnpm test meta/__tests__/verifyRunnerBundle.test.ts`
exits 0. No `git checkout --` and no `git stash` was run at any point.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add dated, owned ledger entries for C2-05 and C2-07, reconciled against items 12 and 13</name>

  <read_first>
    - `.planning/phases/34.9-.../deferred-items.md` — the file header (lines 1-14, which states the
      required shape of an entry), items 12 and 13 in full (lines 355-388), item 11 (lines 335-354,
      the most recent single-finding entry and the cleanest format specimen), and the
      `## Code-review finding disposition (2026-08-12)` section header and its table (lines 269-334)
    - `.planning/phases/34.9-.../34.9-REVIEW-CYCLE2.md` — C2-05 in full (the two numbered details and
      the `build-base.yml:48` invocation) and C2-07 in full
  </read_first>

  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md</files>

  <action>
Per locked user decision D-C3-05 these two findings are recorded, not fixed. No code change.

**Numbering, collision-tolerantly.** Another plan in this gap cycle may be appending to this same
file concurrently. Re-read `deferred-items.md` immediately before writing, take the highest existing
`### <N>.` number, and use the next two integers. Never renumber, reword or reorder an existing item.
If items numbered above 16 already exist when you read, that is expected — continue from wherever
the file actually is.

**Placement.** Check whether a section heading for gap cycle 2's review disposition already exists
(a concurrently-landed plan may have created one). If it exists, append the two new entries under it
and add their rows to its table. If it does not, create
`## Code-review finding disposition, gap cycle 2 (2026-08-13)` with a two-row table
(finding / severity / disposition / evidence) covering C2-05 and C2-07 only, plus one explicit
sentence stating that C2-01, C2-02, C2-03, C2-06 and C2-08 are dispositioned elsewhere in this gap
cycle and are deliberately NOT claimed by this section. Do not write an A-minus-B set-difference over
all eight C2 IDs here — this plan owns three of them and a table implying full coverage would be a
worse defect than an honestly scoped one.

**Format is not optional.** Each entry is a `### <N>. C2-0X — <short title>` heading followed by the
four house fields, in the file's existing order and bolding: **What it is**, **Blocker (mechanism,
not a summary)**, **Named precondition**, **OWNER:** with the date 2026-08-13. A paragraph that
merely mentions `C2-05` in prose is not an entry — this project has already produced one
false-negative audit from exactly that ambiguity, where prose *about* an omission read identically to
coverage of it. Write these so a mechanical sweep can distinguish them: after writing,
`grep -n '^### [0-9]*\. C2-0' deferred-items.md` must return exactly two lines, one for C2-05 and one
for C2-07.

**Entry for C2-05 — the reconciliation is the hard part.** The base fact ("the guard is arm64-only;
`release:mac` also builds x64") is ALREADY recorded as items 12 and 13. Do not restate it as if new.
Cross-reference both by number, state plainly that they are not being reopened, and record only the
two concrete details the review adds on top of them:

1. The arm64-only invocation is live in real, currently-active CI rather than merely theoretically
   unwired: `.github/workflows/build-base.yml:48` runs `pnpm dist:mac --x64 --arm64 --publish=never`
   on a `macos-15` runner. pnpm appends extra CLI args to the end of the resolved script string with
   no `--` separator (unlike npm), so the guard still runs once against arm64 only while
   `electron-builder` is handed both architectures in the same job.
2. `release:mac` chains `-p always` (`package.json:44`), i.e. auto-publish to the GitHub releases
   feed that `electron-updater` consumes. An unverified x64 macOS build can therefore reach real
   users' auto-update channel with the guard green throughout — a materially worse consequence than
   "built locally but unchecked".

Detail 1 sits in apparent tension with item 13, which states the guard has never been exercised in CI
because `install-deps` throws on the six `PENDING-CI-PUBLISH` sentinels before the build step is
reached. Resolve that tension explicitly in the entry rather than leaving a reader to notice it: the
invocation is real and active in the workflow file, and the job is nonetheless blocked upstream of it,
so what detail 1 establishes is that the arm64-only exposure is structurally wired into the live CI
definition — not that it has ever executed. Do not write a sentence claiming the guard has run in CI.

State the blocker as a mechanism: an x64 darwin onedir tree does not exist anywhere (item 1), so the
guard cannot be pointed at x64 without failing its file-count floor for a reason unrelated to symlink
integrity (item 12's mechanism). Named precondition: REQ-34.9-02's own precondition, plus a decision
on whether `release:mac`'s `-p always` should be gated on a second `--arch=x64` guard invocation once
that tree exists. OWNER: the same follow-up phase that owns items 1, 12 and 13.

**Entry for C2-07.** The four tests in `meta/__tests__/cleanDistMac.test.ts:234-276` ("doc-comment
accuracy pins (IN-01/IN-02)") assert exact prose substrings of `meta/cleanDistMac.ts`'s normalised
comments. State clearly that they are NOT vacuous — they will genuinely fail if the wording changes —
so this is a coupling concern, not a correctness defect. Blocker as a mechanism: the `toContain` half
asserts documentation wording rather than program behaviour, so any future rewording that preserves
the identical technical claim still breaks CI, while the `not.toContain` half (which is what actually
prevents regression to the retired misleading claims) carries the real protection and is not
affected. Named precondition: a decision by whoever next edits `meta/cleanDistMac.ts`'s header
comments on whether to drop the positive `toContain` assertions and retain only the negative ones.
OWNER: whichever plan next edits those doc comments. Date 2026-08-13.

If any `gsd-sdk` state write happens while landing this, re-read `.planning/STATE.md` afterward and
hand-correct it — it has corrupted STATE.md on essentially every plan in this phase. `total_plans`
counts plans that EXIST; this plan already exists, so completing it adds nothing to that count.
  </action>

  <verify>
    <automated>grep -c '^### [0-9]*\. C2-0' .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md</automated>
  </verify>

  <done>
`grep -n '^### [0-9]*\. C2-0' deferred-items.md` returns exactly 2 lines, one naming C2-05 and one
naming C2-07. Each of the two new sections contains the literal strings `Blocker (mechanism, not a
summary)`, `Named precondition`, `OWNER:` and `2026-08-13`. The C2-05 section contains the literal
strings `item 12`, `item 13`, `build-base.yml:48` and `-p always`, and contains no sentence asserting
the guard has executed in CI. No pre-existing item number, heading or body text was altered —
confirmed by `git diff` on the file showing additions only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan execution → `package.json` | This plan deliberately mutates the repo's script and dependency manifest four times. Every subsequent build, test and packaging run in this phase reads it. |
| plan execution → git index | A mutated manifest reaching a commit would propagate silently. |
| `deferred-items.md` → future audit sweeps | The ledger is the artifact a reconciliation step greps. A malformed entry is invisible to that sweep. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.9C3-01 | Tampering | `package.json` during the Task 2 mutation proof | mitigate | Backup by `cp` to the session scratchpad outside the repo before the first mutation; restore by `cp` from that backup; `git checkout --` and `git stash` are forbidden (the former fires `.husky/post-checkout` into a network install). |
| T-34.9C3-02 | Tampering | `package.json` after an incomplete restore | mitigate | Restore verified after EACH of the four mutations, not only at the end, by `git status --porcelain package.json` printing nothing AND `shasum -a 256` matching the pre-mutation baseline. A mismatch aborts the plan. |
| T-34.9C3-03 | Denial of Service | `package.json` left as invalid JSON mid-proof | mitigate | JSON-parse the file after applying each mutation and before running jest, so a red test can never be mistaken for a red assertion when it was actually a parse error. |
| T-34.9C3-04 | Tampering | this plan's git commit | mitigate | `package.json` must be absent from the commit's changed-file list; stage `meta/__tests__/verifyRunnerBundle.test.ts` and `deferred-items.md` explicitly by path rather than with `git add -A`. |
| T-34.9C3-05 | Repudiation | `deferred-items.md` entries for C2-05/C2-07 | mitigate | Entries must be `### <N>. C2-0X` headings carrying all four house fields, verified by `grep -n '^### [0-9]*\. C2-0'` returning exactly 2 — prose merely mentioning the ID is indistinguishable from coverage otherwise, a confusion that has already produced one false-negative audit in this project. |
| T-34.9C3-06 | Information Disclosure | test assertions over `package.json` | accept | The test reads only the two script strings; no secret, token or account identifier is in scope. |
| T-34.9C3-SC | Tampering | npm/pip/cargo installs | accept | This plan runs no package-manager install task and adds no dependency. `pnpm test` executes already-installed, already-audited tooling. No new supply-chain surface is introduced, so the legitimacy checkpoint does not apply. |
</threat_model>

<verification>
- `pnpm test meta/__tests__/verifyRunnerBundle.test.ts` exits 0 against the restored manifest.
- Four mutation runs recorded in the Summary, with M1/M2 red on the presence assertion and M3/M4 red
  on the ordering assertion.
- `git status --porcelain package.json` prints nothing.
- `grep -n '^### [0-9]*\. C2-0' deferred-items.md` returns exactly 2 lines.
- `git show --stat HEAD` for this plan's commit lists exactly two files, neither of them
  `package.json`.
</verification>

<success_criteria>
C2-04 is closed by a test proven red against both failure shapes on both scripts, and the Summary
states explicitly that this does not close C2-01 or satisfy `34.9-VERIFICATION.md` truth 8. C2-05 and
C2-07 each have a dated, owned, mechanically-greppable ledger entry, with C2-05 reconciled against
items 12 and 13 rather than duplicating them and carrying both of the two details those items lack.
`package.json` is byte-identical to its pre-plan state.
</success_criteria>

<output>
Create `.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-27-SUMMARY.md` when done.
</output>
