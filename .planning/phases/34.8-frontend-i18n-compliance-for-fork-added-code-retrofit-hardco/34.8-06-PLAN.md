---
phase: 34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco
plan: 06
type: execute
wave: 5
depends_on: ["34.8-05"]
files_modified:
  - .planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-AUDIT.md
  - meta/i18nGlossary.json
autonomous: false
requirements: [REQ-34.8-11]
user_setup: []

must_haves:
  truths:
    - "The retrofit backlog is generated by the gate's own audit-mode run, not copied from the 2026-08-05 manual audit or from research's time-boxed heuristic sweep."
    - "Every violation the audit reports is triaged into exactly one disposition: retrofit, glossary-extension, file-exemption, or deferred-allowlist."
    - "If the backlog is materially larger than the phase was planned around, the developer decides how to proceed before any retrofit starts."
  artifacts:
    - path: ".planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-AUDIT.md"
      provides: "The authoritative, measured retrofit backlog with a disposition per violation"
      contains: "## Retrofit Backlog"
  key_links:
    - from: "34.8-AUDIT.md"
      to: "meta/hardcodedStringGate.ts scanScope"
      via: "formatReport output pasted verbatim as the raw-measurement section"
      pattern: "scanScope"
    - from: "34.8-AUDIT.md § Retrofit Backlog"
      to: "plans 34.8-07 and 34.8-08"
      via: "per-file work list consumed by the retrofit plans"
      pattern: "34.8-08"
---

<objective>
Run the gate in audit mode across the whole committed scope and turn its output into the
authoritative, triaged retrofit backlog.

Purpose: RESEARCH.md Pitfall 4 and Open Question 4 are explicit that the real target set is
materially larger than the four files ROADMAP names. 205 `src/frontend/**` files are touched
since the merge-base (104 net-new); a time-boxed heuristic sweep found at least one extra
confirmed violation (`appleRating.ts:31`'s bare `'Unrated'`) and did not resolve every
candidate. The 2026-08-05 manual audit names examples with "etc." and cannot drive scope. So the
retrofit wave's size is DISCOVERED here, not assumed — and this plan is where an over-large
result becomes a developer decision rather than a silent scope cut.

Output: `34.8-AUDIT.md` with a raw measurement section and a triaged backlog; possibly a small
glossary extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-CONTEXT.md
@.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-RESEARCH.md
@.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-05-SUMMARY.md

<interfaces>
`scanScope(opts?): ScopeScanReport` and `formatReport(report): string` are exported from
`meta/hardcodedStringGate.ts` (plan 05). `ScopeScanReport` carries `scannedFiles`,
`totalCandidates`, `violations`, `allowlisted`, `staleExemptions`, `fileExempt`.

The four dispositions this plan assigns, and who acts on each:
  - `retrofit`        → plan 34.8-07 (`copy.ts`) or plan 34.8-08 (everything else)
  - `glossary`        → this plan, task 2 (add the term to `meta/i18nGlossary.json`)
  - `file-exemption`  → plan 34.8-07 (add the `i18n-gate-exempt:` comment)
  - `deferred`        → already covered by `meta/i18nGateAllowlist.json`; no new entries without
                        a developer decision
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the gate in audit mode and record the raw measurement</name>
  <files>.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-AUDIT.md</files>
  <read_first>
    - `meta/hardcodedStringGate.ts` — `scanScope` and `formatReport` signatures and behaviour
    - `meta/i18nGateScope.json` — the file list about to be scanned, and its `baseCommit`
    - `meta/i18nGateAllowlist.json` — the two deferred files whose findings are reported
      separately
    - `.planning/phases/34.8-.../34.8-RESEARCH.md` § Common Pitfalls → Pitfall 4 and
      § Open Questions item 4 — why this run, not the prior audits, is authoritative
  </read_first>
  <action>
    Execute the gate over the committed scope and capture its full output. Run it via a throwaway
    esbuild-bundled invocation
    (`esbuild --bundle --platform=node --target=node21 --outfile=node_modules/.cache/i18n-audit.cjs`
    over a tiny inline entry that calls `scanScope()` and prints `formatReport()`), or via
    `npx jest --config meta/jest.config.js hardcodedStringGate -t "scope orchestration"` reading
    the logged report. Do NOT add a permanent `package.json` script for this — the gate's
    delivery vehicle is the jest test, and a second CLI entry point is a second thing to keep in
    sync. Delete any scratch entry file before finishing.

    Create `34.8-AUDIT.md` with these sections:

    `## Provenance` — the commit the scan ran at, `meta/i18nGateScope.json`'s `baseCommit` and
    `files.length`, the exact command used, and the date. State plainly that this run supersedes
    both the 2026-08-05 manual audit and 34.8-RESEARCH.md's time-boxed heuristic sweep as the
    scope source of truth.

    `## Raw Measurement` — `formatReport()`'s output verbatim, plus the four headline numbers:
    `scannedFiles`, `totalCandidates`, `violations.length`, and the `allowlisted` measured counts.
    If `totalCandidates` is zero, STOP — the scanner did not actually look, and a zero-violation
    report in that state is a false pass, not a clean bill of health.

    `## Files Skipped By Comment Exemption` — the `fileExempt` list. At this point in the phase
    it should be EMPTY, because `bootErrorSurface.ts`'s comment is plan 07's job. Any entry here
    now is an unexpected exemption and must be called out.

    `## Deferred Files (allowlist)` — the two D-17 entries with measured vs expected counts.

    Do not triage in this task. Record what the tool said, unedited.
  </action>
  <acceptance_criteria>
    - `34.8-AUDIT.md` exists with all four sections present as `##` headings
    - The `## Raw Measurement` section records a `totalCandidates` value greater than zero
    - The recorded `scannedFiles` equals `node -e "console.log(require('./meta/i18nGateScope.json').files.length)"`
    - The recorded commit SHA matches `git rev-parse HEAD` at run time
    - `git status --porcelain node_modules/.cache` and any scratch entry file are cleaned up; `git status --porcelain` shows only `34.8-AUDIT.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-AUDIT.md && grep -c '^## Raw Measurement' .planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-AUDIT.md</automated>
  </verify>
  <done>The gate's own first full-scope run is recorded verbatim, with proof that it genuinely scanned rather than returning an empty result.</done>
</task>

<task type="auto">
  <name>Task 2: Triage every violation into a disposition and write the retrofit backlog</name>
  <files>.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-AUDIT.md, meta/i18nGlossary.json</files>
  <read_first>
    - `34.8-AUDIT.md` § Raw Measurement (from task 1)
    - `meta/i18nGlossary.json` — the current term list, before deciding anything is a new brand term
    - `.planning/phases/34.8-.../34.8-CONTEXT.md` § D-19, § D-21, § D-22 and § Deferred Ideas
    - `src/frontend/screens/Game/GamePage/components/appleRating.ts` — the known extra violation
      (`'Unrated'` at line ~31), to confirm the tool found it and to classify it
    - `src/frontend/components/UI/RedeemSteamKeyDialog/copy.ts` — confirm its violations appear
      in the report, since it is plan 07's target
  </read_first>
  <action>
    Add a `## Triage` section to `34.8-AUDIT.md`: a table with one row per violation, columns
    `file:line`, `literal`, `kind`, `disposition`, `notes`. `disposition` is exactly one of
    `retrofit`, `glossary`, `file-exemption`, `deferred` — the four values declared in this
    plan's `<interfaces>` block. Every violation gets a row; there is no "unclassified" bucket.

    Assign dispositions with these rules:
      - A brand, store, platform or unit name not yet in the glossary → `glossary`. Add the term
        to `meta/i18nGlossary.json`'s `terms` (keeping the array sorted and de-duplicated) and
        note in `## Triage` the source file:line that motivated it. This is D-21's mechanism
        working as designed: the gate failing on an unglossed brand name IS the reminder.
      - Genuine user-facing prose → `retrofit`.
      - A file that runs before i18next is initialised, like `bootErrorSurface.ts` → `file-exemption`.
      - One of the two D-17 files → `deferred` (they already carry allowlist entries; nothing to do).

    Then add `## Retrofit Backlog` — grouped by file, listing every `retrofit`-dispositioned
    violation with its line and text, and split into two subsections:
      - `### Assigned to plan 34.8-07`: every file under
        `src/frontend/components/UI/RedeemSteamKeyDialog/` plus `src/frontend/bootErrorSurface.ts`.
        The whole directory, not just `copy.ts` — plans 07 and 08 run in the same wave, so their
        file sets must be strictly disjoint or they will collide.
      - `### Assigned to plan 34.8-08`: every other file. **This list is plan 34.8-08's scope of
        record** — plan 08's `files_modified` is defined by it. State explicitly that neither
        deferred D-17 file, and no file under `RedeemSteamKeyDialog/`, may appear here.

    Add `## Scale Assessment` recording: total `retrofit` violations, distinct files in
    `### Assigned to plan 34.8-08`, and whether either exceeds the planning threshold of
    **40 violations or 12 files**. Also record any violation you could not confidently classify,
    by name — an honest "I am not sure this is user-facing" belongs in the document, not
    resolved by guessing.

    Do NOT retrofit anything in this task and do NOT edit any file under `src/frontend/`.
  </action>
  <acceptance_criteria>
    - `34.8-AUDIT.md` contains `## Triage`, `## Retrofit Backlog`, `### Assigned to plan 34.8-07`, `### Assigned to plan 34.8-08` and `## Scale Assessment` headings
    - The number of rows in the `## Triage` table equals `violations.length` recorded in `## Raw Measurement`
    - Every `disposition` cell holds one of exactly `retrofit`, `glossary`, `file-exemption`, `deferred`
    - `grep -c 'SteamLogin\|useTauriOAuthLogin' <the "Assigned to plan 34.8-08" subsection>` is 0
    - `appleRating.ts` appears in the triage table (research's confirmed extra finding — its absence means the scanner missed a known violation and the gate is not trustworthy yet)
    - If `meta/i18nGlossary.json` changed, `npx jest --config meta/jest.config.js i18nGlossary` still exits 0 and the array is still sorted and de-duplicated
    - `git status --porcelain src/frontend/` is empty
  </acceptance_criteria>
  <verify>
    <automated>npx jest --config meta/jest.config.js i18nGlossary hardcodedStringGate</automated>
  </verify>
  <done>Every measured violation has exactly one disposition, and plans 07 and 08 have a concrete, named work list.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Developer decision if the retrofit backlog exceeds the planning threshold</name>
  <decision>The audit's retrofit backlog is larger than this phase's plan set was sized around. How should the remainder be handled?</decision>
  <context>
    RESEARCH.md warned (Pitfall 4, Assumption A3) that the true target set is unknown until this
    run. Plans 34.8-07 and 34.8-08 were written to absorb a backlog of up to ~40 violations
    across ~12 files. `34.8-AUDIT.md` § Scale Assessment records the measured figures.

    This checkpoint fires ONLY if `## Scale Assessment` records that the threshold was exceeded.
    If it was not exceeded, record "threshold not exceeded — no decision needed" and proceed.

    Nothing is being simplified or dropped here: the whole backlog gets retrofitted either way.
    The only question is whether it fits this phase's remaining plans or needs a phase split.
  </context>
  <options>
    <option id="absorb">
      <name>Absorb into plan 34.8-08 as-is</name>
      <pros>No replanning; the phase closes in one pass</pros>
      <cons>Plan 08 exceeds its context budget, which is how quality degrades and retrofits get done sloppily</cons>
    </option>
    <option id="split-plan">
      <name>Split plan 34.8-08 into 34.8-08a/08b/... by file group, same phase</name>
      <pros>Keeps every plan inside its context budget; still closes in this phase; plans run in parallel since file sets are disjoint</pros>
      <cons>More plans to execute; requires the orchestrator to mint the extra PLAN.md files</cons>
    </option>
    <option id="split-phase">
      <name>Split the retrofit remainder into a follow-on phase 34.8.1</name>
      <pros>Keeps 34.8 focused on the gate + catalog + the named D-19 targets; the gate can go blocking sooner</pros>
      <cons>The gate cannot block until the remainder is retrofitted or allowlisted, so this option REQUIRES new allowlist entries — which is a real weakening of D-12 and must be recorded as such</cons>
    </option>
  </options>
  <read_first>
    - `34.8-AUDIT.md` § Scale Assessment and § Retrofit Backlog
    - `.planning/phases/34.8-.../34.8-CONTEXT.md` § D-12 — "blocking from day one, no advisory
      grace period" is what option `split-phase` puts at risk
  </read_first>
  <acceptance_criteria>
    - `34.8-AUDIT.md` records either "threshold not exceeded — no decision needed" or the selected option id and the developer's verbatim direction
    - If `split-phase` is selected, the audit document records exactly which files would need new allowlist entries and states plainly that this weakens D-12
  </acceptance_criteria>
  <files>.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-AUDIT.md</files>
  <action>
    Read `34.8-AUDIT.md` § Scale Assessment. If it records that neither threshold (40 violations,
    12 files) was exceeded, append the line "threshold not exceeded — no decision needed" to that
    section and proceed without pausing.

    If either threshold WAS exceeded, present the three options above to the developer with the
    measured figures, wait for a selection, and append a `## Scope Decision` section to
    `34.8-AUDIT.md` recording the selected option id, the developer's verbatim direction, and the
    date. If `split-phase` is selected, additionally enumerate exactly which files would need new
    allowlist entries and state plainly that this weakens D-12's "blocking from day one" posture.

    Do not select an option on the developer's behalf and do not proceed past this task on an
    exceeded threshold without a recorded selection.
  </action>
  <verify>
    <automated>grep -qE 'threshold not exceeded|## Scope Decision' .planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-AUDIT.md</automated>
  </verify>
  <done>`34.8-AUDIT.md` records either "threshold not exceeded — no decision needed" or a developer-selected scope decision with its verbatim direction.</done>
  <resume-signal>Select: absorb, split-plan, or split-phase — or reply "not exceeded" if § Scale Assessment says the threshold was not hit</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit output → phase scope | An under-reported audit becomes a silently under-scoped retrofit |
| triage dispositions → glossary | Mislabelling prose as a "brand name" permanently exempts it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.8-15 | Repudiation | an empty or partial audit passed off as a clean scope | mitigate | Task 1 stops if `totalCandidates` is zero; `scannedFiles` is asserted equal to the snapshot's `files.length`; `appleRating.ts` is a known-answer control the report must contain |
| T-34.8-16 | Tampering | prose reclassified as a glossary term to make a violation disappear | mitigate | Every `glossary` disposition must record the motivating file:line in `## Triage`; the glossary test keeps the term list reviewable; plan 03's near-miss test keeps matching whole-string |
| T-34.8-17 | Elevation of Privilege | scope silently reduced because the backlog is large | mitigate | The `checkpoint:decision` is blocking and none of its three options drop work; `split-phase` explicitly requires recording that it weakens D-12 |
| T-34.8-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH.md § Package Legitimacy Audit). Any newly-required install → STOP and raise a blocking-human legitimacy checkpoint. |
</threat_model>

<verification>
- `npx jest --config meta/jest.config.js` — whole `Meta` project green
- `git status --porcelain src/ public/locales/` is empty
- `34.8-AUDIT.md` triage row count equals the recorded violation count
</verification>

<success_criteria>
- The retrofit backlog is measured by the gate, not inherited from a prior audit
- Every violation carries exactly one of the four dispositions
- Plans 07 and 08 have named, non-overlapping file lists that exclude both D-17 files
- An over-large backlog reaches the developer as a decision, never as a silent cut
</success_criteria>

<output>
Create `.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-06-SUMMARY.md` when done
</output>
