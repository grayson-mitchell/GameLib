---
phase: 34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco
plan: 08c
type: execute
wave: 6
depends_on: ["34.8-08b"]
files_modified:
  - meta/hardcodedStringGate.ts
  - meta/__tests__/hardcodedStringGate.test.ts
  - src/frontend/screens/Settings/components/WineVersionSelector.tsx
  - src/frontend/screens/Settings/index.tsx
  - .planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-08c-CLOSURE.md
autonomous: true
requirements: [REQ-34.8-01, REQ-34.8-11, REQ-34.8-17]
user_setup: []

must_haves:
  truths:
    - "The Wine Version selector displays a translated 'Wine Default' label (via t()) at both places it currently renders the raw literal — the SelectField's own selected-value display and the dropdown MenuItem list — whenever no real Wine version is configured yet."
    - "defaultWineVersion's runtime string value ('Wine Default', 'wine', '/usr/bin/wine') is byte-identical to before this plan — every consumer that persists, compares or reads it sees no behavior change."
    - "Settings/index.tsx:29's 'Wine Default' object-property literal is no longer reported by scanScope() — closed via a new, narrow, declaration-scoped exemption, not a full-file exemption and not an i18nGateAllowlist.json entry."
    - "The new declaration-scoped exemption mechanism does not weaken the gate anywhere else — a hardcoded string added to a DIFFERENT top-level declaration in Settings/index.tsx, or to any other file, still fails the gate exactly as before."
    - "None of the 13 other defaultWineVersion consumer files, and none of the three D-17-deferred Login files, were opened for editing."
  artifacts:
    - path: "meta/hardcodedStringGate.ts"
      provides: "A declaration-scoped i18n-gate-exempt: marker — exempts only the top-level statement it leads, not the whole file"
      contains: "getEnclosingTopLevelStatement"
    - path: "src/frontend/screens/Settings/components/WineVersionSelector.tsx"
      provides: "The one real render-site fix — WineVersionListItem translates the defaultWineVersion sentinel instead of passing its raw .name through"
      contains: "gamelib:settings.wineDefaultLabel"
    - path: "src/frontend/screens/Settings/index.tsx"
      provides: "defaultWineVersion's declaration carries the narrow exemption comment with its reason on the marker's own line"
      contains: "i18n-gate-exempt:"
    - path: ".planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-08c-CLOSURE.md"
      provides: "The single-item closure record for 34.8-08b's one blocked violation"
      contains: "closed"
  key_links:
    - from: "WineVersionListItem (WineVersionSelector.tsx)"
      to: "gamelib namespace"
      via: "tGamelib('gamelib:settings.wineDefaultLabel', 'Wine Default') at the render site, gated on name === defaultWineVersion.name"
      pattern: "gamelib:settings.wineDefaultLabel"
    - from: "Settings/index.tsx's defaultWineVersion VariableStatement"
      to: "the gate's new declaration-exempt check"
      via: "a same-line-reasoned i18n-gate-exempt: leading comment directly above the statement (not the file's first statement, so checkFileExemptMarker does not fire)"
      pattern: "i18n-gate-exempt:"
    - from: "34.8-08c-CLOSURE.md"
      to: "34.8-AUDIT.md § Closure (consolidated later, not by this plan)"
      via: "the same three-outcome (closed/reclassified/blocked) row shape 08a/08b already used"
      pattern: "closed"
---

<objective>
Close the single `blocked` item `34.8-08b-CLOSURE.md` left open — `Settings/index.tsx:29`'s
`defaultWineVersion.name = 'Wine Default'`, confirmed user-facing because `WineVersionSelector.tsx`
renders it literally when no real Wine version is configured — using the developer's selected
approach: **fix at the render site**, then exempt the now-genuinely-non-user-facing definition
literal narrowly.

Purpose: this is the last open item blocking `34.8-10`'s task 1, which STOPS and refuses to flip
the i18n gate to blocking while `34.8-AUDIT.md § Closure` records a non-zero remainder
(T-34.8-31). Closing it here — rather than deferring it into an allowlist entry, which D-18
reserves for genuinely deferred work behind other open phases, not for something this phase can
finish — keeps that gate story honest.

**Why this plan's files overlap 34.8-08b's `files_modified` despite sharing wave 6:** `08b` is
COMPLETE (not concurrently running), so the same-wave zero-file-overlap rule that protects
parallel execution does not apply here — there is no live plan to conflict with. `08a`, the other
wave-6 plan, touches neither `Settings/index.tsx` nor `WineVersionSelector.tsx`, so this plan is
still conflict-free against anything actually running in its own wave.

**Why not the two simpler options:**
- **Full-file `i18n-gate-exempt:` comment on `Settings/index.tsx`** (the one other file-level
  precedent, `bootErrorSurface.ts`) is rejected: that file has roughly a dozen real, already-`t()`-
  wrapped user-facing strings and is actively edited by ongoing Settings work — blanketing it would
  blind the gate to every *future* hardcoded string landing there, the exact "largest blast-radius
  exemption" risk the gate's own comments warn about. It would also break `34.8-10-PLAN.md` task
  1's own acceptance criterion that `report.fileExempt` contains **exactly** `bootErrorSurface.ts`
  — a second file-exempt entry would fail that assertion outright.
- **An `i18nGateAllowlist.json` entry** is rejected per the developer's explicit direction: the
  allowlist is D-18's mechanism for work deferred behind *other open phases* (the two D-17 Login
  files), not a way to paper over an item this plan can and does finish fixing.

Output: `WineVersionSelector.tsx` renders a translated sentinel label; `Settings/index.tsx`'s
`defaultWineVersion` declaration carries a new, narrow, declaration-scoped exemption; the gate
(`meta/hardcodedStringGate.ts`) gains that exemption mechanism with paired positive/negative
fixtures; `34.8-08c-CLOSURE.md` records the one item as `closed`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-CONTEXT.md
@.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-08b-CLOSURE.md
@.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-08b-SUMMARY.md

<interfaces>
<!-- Independently re-verified before writing this plan (2026-08-07): `git grep -l
"defaultWineVersion" -- src/frontend` returns exactly 14 consumers beyond `Settings/index.tsx`
itself: AdvertiseAvxForRosetta.tsx, AutoDXVK.tsx, AutoDXVKNVAPI.tsx, AutoVKD3D.tsx,
CrossoverBottle.tsx, DisableUMU.tsx, EnableDXVKFpsLimit.tsx, EnableMsync.tsx,
PreferSystemLibs.tsx, SteamRuntime.tsx, WinePrefix.tsx, WineVersionSelector.tsx,
sections/GamesSettings/index.tsx, sections/SyncSaves/index.tsx. Of those, only THREE read `.name`
at all (AutoDXVK.tsx:27, EnableDXVKFpsLimit.tsx:27, EnableMsync.tsx:18), and every one of those
three is `wineVersion.name.endsWith('-DXMT')` — an internal comparison, never rendered. Confirmed:
`WineVersionSelector.tsx` is the ONLY consumer that displays `.name`. Do not re-derive this list;
it is exhaustive as measured. -->

**The render path, read directly from `WineVersionSelector.tsx` (current state):**
`WineVersionSelector`'s `SelectField` has `value={wineVersion.name}` (line 150) and maps
`altWine` into `<MenuItem key={i} value={version.name}><WineVersionListItem version={version} />
</MenuItem>` (lines 226-230). MUI's `Select` (no `renderValue` prop set — confirmed in
`frontend/components/UI/SelectField/index.tsx`) renders the *matching* `MenuItem`'s **children**
as the selected-value display, not the raw `value` string — so `WineVersionListItem` is the ONE
component that needs to change, and fixing it fixes BOTH the collapsed SelectField display and the
open dropdown list, from one edit site.

`WineVersionListItem` (same file, lines 24-68) currently computes
`const substitutedName = useMemo(() => name.replace(/(Proton-GE-Proton|Proton-GE)/, 'GE-Proton'),
[name])` and renders `{substitutedName}`. `defaultWineVersion` is already imported at this file's
line 7 (`import { defaultWineVersion } from '..'`) and is in scope inside `WineVersionListItem`.
`useTranslation` is already imported at line 2; `WineVersionSelector`'s own default export already
calls `useTranslation('gamelib')` aliased to `tGamelib` at line 72 — `WineVersionListItem` needs
its OWN such call, since it is a separate component.

**`Settings/index.tsx`'s current declaration (lines 27-31, unchanged by 08b):**
```
export const defaultWineVersion: WineInstallation = {
  bin: '/usr/bin/wine',
  name: 'Wine Default',
  type: 'wine'
}
```
This is a top-level `VariableStatement`, NOT the file's first statement (imports precede it) — so
`meta/hardcodedStringGate.ts`'s existing `checkFileExemptMarker` (which only inspects
`sourceFile.getStatements()[0]`'s leading comment) does not and will not fire on a comment placed
here. That is exactly why a new mechanism is needed rather than reusing the existing one as-is.

**The gate's exemption mechanisms as they exist today (`meta/hardcodedStringGate.ts`), so this
plan does not reinvent one that already covers this case:** glossary exact-string match (D-02;
wrong fit — `'Wine Default'` is not a brand/platform term, and it no longer needs to render at
all after this plan's fix, which is a different claim than "leave it literal on purpose");
`[key, defaultText]` tuple shape (does not apply — this is a single string, not a pair); direct/
composed/`defaultValue`-property `t()`-call argument (does not apply — this constant is
explicitly NOT to be wrapped in `t()`, its runtime value must stay a plain string, per the
`<constraints>` below); assigned-then-passed-to-`t()` dataflow (same reason, does not apply);
`FILE_EXEMPT_MARKER`'s whole-file comment (rejected above — too broad for this file). None of the
five fit a single non-`t()`-wrapped literal inside one declaration among many in an actively-
edited file. This plan adds the missing narrow case: a declaration-scoped sibling of the existing
file-scoped marker, reusing the SAME `i18n-gate-exempt:` marker text and same-line-reason
requirement, distinguished purely by WHERE the comment sits (the file's first statement = whole
file, unchanged; any other top-level statement = that statement only, new).

**Two lessons this plan's executor must not relearn the hard way (both measured earlier in this
phase):**
(a) A green `npx jest --selectProjects Frontend` (or even a green whole-repo `pnpm test:ci`) is
NOT proof the gate is satisfied. Run the gate directly —
`npx jest --config meta/jest.config.js hardcodedStringGate` — and read its `formatReport()`-style
violation output (file:line:column, kind, literal text) before declaring either file clean.
(b) The `[key, defaultText]` tuple idiom does NOT gate-trace once the key carries a `gamelib:`
namespace prefix — `DOTTED_KEY_RE` rejects the colon (discovered the hard way in `34.8-08a`,
28 false violations on the first attempt). Not directly relevant to this plan's own fix (no tuple
table is used here), but if the executor is tempted to reach for that idiom anywhere in this
change, don't — use a direct `tGamelib('gamelib:…', '…')` call instead, as `WineVersionSelector.tsx`
already does everywhere else in the file.

**Forbidden files — do not open for editing under any circumstance (D-17):**
`src/frontend/screens/Login/components/SteamLogin/index.tsx`,
`src/frontend/screens/WebView/useTauriOAuthLogin.ts`,
`src/frontend/screens/Login/__tests__/loginInFlightUiReachability.test.tsx`.

**Do not edit `34.8-AUDIT.md`.** Consolidating this plan's `34.8-08c-CLOSURE.md` with `08a`'s and
`08b`'s into `34.8-AUDIT.md § Closure` — which `34.8-10-PLAN.md` reads before flipping the gate —
is orchestration work for whoever sequences this plan's completion, same as 08a/08b recorded.
</interfaces>
</context>

<constraints>
- Do NOT change `defaultWineVersion`'s runtime string values (`bin: '/usr/bin/wine'`,
  `name: 'Wine Default'`, `type: 'wine'`) in any way. It is a plain object, not a `t()`-wrapped
  call — it is persisted via `useSetting('wineVersion', defaultWineVersion)` / `electron-store`
  and compared against by name/type/bin across the 14 consumer files. Changing the literal, or
  converting it into a function/hook call, would change stored config semantics for existing users.
- Do NOT add an entry to `meta/i18nGateAllowlist.json`. Verify with
  `git diff --quiet meta/i18nGateAllowlist.json` before finishing.
- Do NOT use `FILE_EXEMPT_MARKER`'s existing whole-file behavior on `Settings/index.tsx` — see
  `<objective>` for why. The new exemption this plan adds must be provably scoped to the ONE
  top-level statement it leads, not the whole file.
- Do NOT edit any of the 13 other `defaultWineVersion` consumer files, or the three D-17-forbidden
  Login files.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a declaration-scoped `i18n-gate-exempt:` exemption to the gate</name>
  <files>meta/hardcodedStringGate.ts, meta/__tests__/hardcodedStringGate.test.ts</files>
  <read_first>
    - `meta/hardcodedStringGate.ts` — `FILE_EXEMPT_MARKER`, `checkFileExemptMarker`, and the
      `record()` function's ordered exemption checks (the `fileExemptResult` check runs first)
    - `meta/__tests__/hardcodedStringGate.test.ts` lines ~922-959 — the existing
      `describe('D-14: bootErrorSurface full-file exemption')` block, the pattern to mirror
  </read_first>
  <behavior>
    - A top-level statement that is NOT the source file's first statement, whose leading comment
      carries `i18n-gate-exempt:` plus a non-empty same-line reason, exempts only the literal(s)
      inside that one statement. A real violation elsewhere in the same file — before or after the
      exempted statement — still gets flagged, and `result.fileExempt` stays `false`.
    - A bare `i18n-gate-exempt:` marker with no reason on such a statement does NOT exempt anything
      (mirrors the existing whole-file marker's same requirement).
    - Placing the marker on the file's FIRST statement continues to trigger the EXISTING
      `checkFileExemptMarker` whole-file behavior, unchanged — this task must not regress that.
  </behavior>
  <action>
    Add a helper `getEnclosingTopLevelStatement(node: Node): Node | undefined` that walks `node`'s
    ancestors until it finds the direct child of the `SourceFile` (`Node.isSourceFile(parent)`),
    returning that child (or `undefined` if `node` has no such ancestor, e.g. it already IS a
    top-level node with no further parent chain). Add `checkDeclarationExemptMarker(node: Node):
    boolean`, structured exactly like `checkFileExemptMarker` (same
    `rangeText.indexOf(FILE_EXEMPT_MARKER)` / same-line-reason logic — extract the shared "does
    this comment range carry the marker plus a reason" check into one small helper both functions
    call, rather than duplicating the string-slicing) but operating on
    `getEnclosingTopLevelStatement(node)?.getLeadingCommentRanges()` instead of the file's first
    statement.

    Wire `checkDeclarationExemptMarker(node)` into `record()` immediately after the existing
    `fileExemptResult` check (same position tier — cheap, structural, checked before the glossary/
    tuple/dataflow checks): on a match, `exempted += 1; return` — do NOT set `result.fileExempt`
    (that field must continue to mean "the WHOLE file is exempt", exactly as `34.8-10-PLAN.md`'s
    own acceptance criteria already assume).

    Document the two-tier behavior directly above `FILE_EXEMPT_MARKER`'s existing docstring
    (Pattern 4): the same marker text now has two granularities depending on comment placement —
    the file's first statement's leading comment = whole-file (existing, `bootErrorSurface.ts`);
    any OTHER top-level statement's leading comment = that statement only (new, 34.8-08c,
    `Settings/index.tsx`'s `defaultWineVersion`). State explicitly that this is a deliberate
    reuse of one marker string at two scopes, not an accidental overlap, so a future reader is not
    surprised by the second reading.

    Add a new `describe('34.8-08c: declaration-scoped exemption marker')` block to the test file,
    modelled on the existing bootErrorSurface block, asserting all three `<behavior>` cases above
    with a fixture shaped like the real case — a source with an unrelated earlier real violation, a
    marked declaration later in the file carrying the sentinel-shaped object, and (for the bare-
    marker negative case) the same marked declaration with no reason. Also add the fourth
    non-regression case (marker on the file's actual first statement still sets
    `result.fileExempt = true`).
  </action>
  <acceptance_criteria>
    - `npx jest --config meta/jest.config.js hardcodedStringGate` exits 0
    - The new declaration-scoped positive-case test asserts `result.fileExempt` is `false` AND
      `result.violations` still contains the unrelated earlier real violation AND does not contain
      the marked declaration's literal
    - The new bare-marker negative-case test asserts the marked declaration's literal IS present in
      `result.violations`
    - The non-regression case asserts a first-statement marker still produces `result.fileExempt
      === true`, unchanged from the existing bootErrorSurface behavior
    - `grep -cE '\.skip|\.todo|xit\(|xdescribe\(' meta/__tests__/hardcodedStringGate.test.ts` returns 0
  </acceptance_criteria>
  <verify>
    <automated>npx jest --config meta/jest.config.js hardcodedStringGate</automated>
  </verify>
  <done>The gate can exempt one hardcoded literal at its exact declaration without blinding itself to the rest of the file, proven by fixtures that would fail if the exemption were accidentally file-wide or accidentally too broad within the statement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the render site, apply the exemption, and close the item</name>
  <files>src/frontend/screens/Settings/components/WineVersionSelector.tsx, src/frontend/screens/Settings/index.tsx, .planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-08c-CLOSURE.md</files>
  <read_first>
    - `34.8-08b-CLOSURE.md` § "`Settings/index.tsx:29` — `defaultWineVersion`'s `'Wine Default'`" —
      the full blocked-reasoning this task resolves
    - `34.8-08a-CLOSURE.md` — the row-table / "Post-retrofit measurement" / "Allowlist" section
      shape to mirror for this plan's own `34.8-08c-CLOSURE.md` (single-item version)
  </read_first>
  <behavior>
    - `WineVersionListItem`, given a `version` whose `name` equals `defaultWineVersion.name`,
      renders the translated sentinel text (falls through to `'Wine Default'` under a test i18n
      setup with no catalog entry, byte-identical to the string it replaces) instead of the raw
      `.name`. Given any OTHER `version.name` (e.g. a real installed Wine/Proton build), rendering
      is UNCHANGED from before this plan — the existing `Proton-GE`/`Proton-GE-Proton` →
      `GE-Proton` substitution still applies exactly as it did.
    - `defaultWineVersion`'s exported object is structurally and value-identical before and after
      this task — same three keys, same three string values.
  </behavior>
  <action>
    **`WineVersionSelector.tsx` — `WineVersionListItem`:** add
    `const { t: tGamelib } = useTranslation('gamelib')` inside the component (it is a real
    function component, hooks are safe here). Replace the `substitutedName` computation's body so
    that when `name === defaultWineVersion.name` it returns
    `tGamelib('gamelib:settings.wineDefaultLabel', 'Wine Default')`, and otherwise keeps the
    existing `name.replace(/(Proton-GE-Proton|Proton-GE)/, 'GE-Proton')` behavior unchanged. Keep
    the variable named `substitutedName` (or rename to `displayName` if clearer — either is fine,
    just update both the `useMemo` dependency array, if `tGamelib` is included, and the JSX
    `{...}` reference consistently) and render it exactly where `{substitutedName}` already renders
    today (line 65). Do not touch `value={wineVersion.name}` (line 150) or
    `value={version.name}` (line 227) — those are MUI `Select`/`MenuItem` VALUE props (internal
    identifiers used for matching and the `onChange` filter), not display text, and must keep using
    the raw untranslated name so selection/comparison logic is unaffected.

    **`Settings/index.tsx`:** directly above `export const defaultWineVersion: WineInstallation =
    {`, with NO blank line between the comment and the declaration (so ts-morph associates it as
    the statement's leading trivia), add a `/** ... */` block comment whose first content line is
    `i18n-gate-exempt: 'Wine Default' is a persisted-config fallback sentinel, not rendered
    directly` (marker text plus a non-empty reason, all on that one line — task 1's check only
    reads up to the first newline after the marker). Follow with 2-4 more lines of human-readable
    context on why: `WineVersionSelector.tsx`'s `WineVersionListItem` now translates the DISPLAY of
    this value at its one render site (34.8-08c); this literal's own runtime string must stay
    byte-identical because it is persisted in user config and read/compared by 14 other files
    (name/type/bin checks); see `34.8-08b-CLOSURE.md` and `34.8-08c-CLOSURE.md`. Do NOT change the
    object's `bin`/`name`/`type` values.

    **Verification, before writing the closure doc:** run `npx jest --config meta/jest.config.js
    hardcodedStringGate -t "scope orchestration"` and confirm the failure-message report (per this
    plan's lesson (a) — do not trust a green `npx jest --selectProjects Frontend` alone) shows zero
    violations for `src/frontend/screens/Settings/index.tsx` and
    `src/frontend/screens/Settings/components/WineVersionSelector.tsx`. If either still reports a
    violation, do not write the closure doc as `closed` — diagnose and fix first (do not fall back
    to a `blocked` outcome without first confirming task 1's mechanism actually reaches this
    literal; both files are already listed in the committed `meta/i18nGateScope.json`, confirmed
    during planning, so `scanScope()` genuinely exercises them).

    **`34.8-08c-CLOSURE.md`:** write it in the same shape as `34.8-08a-CLOSURE.md` /
    `34.8-08b-CLOSURE.md` — a one-row outcome table for this plan's single assigned item
    (`Settings/index.tsx:29`, `'Wine Default'`, outcome `closed`, with the `gamelib:` key that now
    displays it — `gamelib:settings.wineDefaultLabel`), a short section explaining the render-site
    fix and the declaration-scoped exemption (cross-referencing task 1's mechanism), the
    post-retrofit `scanScope()` measurement for both touched files, and an "Allowlist" section
    confirming `git diff meta/i18nGateAllowlist.json` is empty. State plainly that this closes
    34.8-08b's one remaining `blocked` item and that `34.8-AUDIT.md § Closure` consolidation
    (08a + 08b + 08c) remains orchestration work owed to whoever sequences this plan's completion,
    consistent with 08a's and 08b's own SUMMARYs.
  </action>
  <acceptance_criteria>
    - `grep -c "gamelib:settings.wineDefaultLabel" src/frontend/screens/Settings/components/WineVersionSelector.tsx` returns at least 1
    - `git diff src/frontend/screens/Settings/index.tsx` shows ONLY a comment addition — the
      `bin`/`name`/`type` values inside `defaultWineVersion` are byte-identical (confirm with
      `git diff src/frontend/screens/Settings/index.tsx | grep -E '^[+-]' | grep -v '^+++\|^---'`
      showing only added `+` comment lines, no `-` lines touching `bin`/`name`/`type`)
    - `npx tsc --noEmit` clean
    - `npx jest --selectProjects Frontend` exits 0
    - Direct gate run (`npx jest --config meta/jest.config.js hardcodedStringGate -t "scope
      orchestration"`) passes with both `Settings/index.tsx` and `WineVersionSelector.tsx`
      reporting zero violations — read the actual report output, not just the exit code
    - `git diff --quiet meta/i18nGateAllowlist.json`
    - `git status --porcelain src/frontend/screens/Login/ src/frontend/screens/WebView/useTauriOAuthLogin.ts` is EMPTY
    - `git status --porcelain` for the 13 other `defaultWineVersion` consumer files (listed in
      this plan's `<interfaces>`) is EMPTY
    - `34.8-08c-CLOSURE.md` records exactly one row, outcome `closed`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx jest --selectProjects Frontend && npx jest --config meta/jest.config.js hardcodedStringGate -t "scope orchestration"</automated>
  </verify>
  <done>Settings/index.tsx:29's sole remaining violation is genuinely closed — the sentinel string is translated at its one real render site, the definition literal is exempted narrowly with its reason on record, and 34.8-08c-CLOSURE.md says so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a new gate exemption mechanism → the gate's own trustworthiness | Every exemption is a hole by construction; a badly-scoped one silently weakens enforcement everywhere it applies, not just at its intended use site |
| declaration-scoped marker text ↔ existing file-scoped marker text | Reusing the SAME `i18n-gate-exempt:` string at two different granularities, distinguished only by comment placement, is a genuine confusability risk for a future author |
| `defaultWineVersion`'s persisted runtime value → 14 other consumer files | Any accidental change to the object's string values would change stored user-config semantics for existing installs |
| this plan's `-CLOSURE.md` → `34.8-AUDIT.md § Closure` plan 10 reads | Same shared-file risk 08a/08b already documented — this plan does not touch `34.8-AUDIT.md` either |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.8-51 | Tampering | the new declaration-scoped exemption accidentally exempting more than its one intended statement, or silently becoming file-wide | mitigate | Task 1's fixtures explicitly assert `result.fileExempt` stays `false` and an unrelated earlier violation in the same fixture source is still reported alongside the exempted one |
| T-34.8-52 | Repudiation | a future author placing `i18n-gate-exempt:` on a non-first statement believing it is file-wide (or vice versa), because both readings share one marker string | mitigate | The docstring above `FILE_EXEMPT_MARKER` is updated to state both granularities explicitly and why; this plan's own real usage in `Settings/index.tsx` is the second-ever example, giving a second reference point beside `bootErrorSurface.ts` |
| T-34.8-53 | Tampering | `defaultWineVersion`'s persisted runtime value drifting during this plan's edit, silently changing stored config semantics for the 14 consumers | mitigate | Acceptance criteria assert the `git diff` on `Settings/index.tsx` touches only added comment lines, never a `bin`/`name`/`type` value line |
| T-34.8-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH.md § Package Legitimacy Audit). Any newly-required install → STOP and raise a blocking-human legitimacy checkpoint. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx jest --selectProjects Frontend` green
- `npx jest --config meta/jest.config.js hardcodedStringGate` green (both the new declaration-marker fixtures and the scope-orchestration scan)
- `git diff --quiet meta/i18nGateAllowlist.json`
- `git status --porcelain` empty for every D-17-forbidden file and every one of the 13 other `defaultWineVersion` consumers
</verification>

<success_criteria>
- `Settings/index.tsx:29`'s violation is recorded `closed` in `34.8-08c-CLOSURE.md`, not `blocked` or silently allowlisted
- `WineVersionSelector.tsx` genuinely displays a translated label at the one confirmed render site, proven copy-preserving
- `defaultWineVersion`'s runtime value is unchanged
- The gate's new exemption mechanism is provably narrow (proven not to leak into full-file exemption) and documented
- No file outside this plan's `files_modified` was edited
</success_criteria>

<output>
Create `.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-08c-SUMMARY.md` when done
</output>
