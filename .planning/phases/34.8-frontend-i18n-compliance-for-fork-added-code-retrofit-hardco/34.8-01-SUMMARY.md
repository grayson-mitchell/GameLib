---
phase: 34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco
plan: 01
subsystem: i18n
tags: [i18next-parser, jest, meta-tooling, glossary]

# Dependency graph
requires: []
provides:
  - "keepRemoved: true in i18next-parser.config.js — pnpm i18n can no longer delete fork keys the static lexer cannot see"
  - "meta/i18nGlossary.json — machine-readable do-not-translate term list (D-02 + D-21)"
  - "meta/__tests__/i18nParserConfig.test.ts — pins keepRemoved plus namespace/key separators, output path, locales"
  - "meta/__tests__/i18nGlossary.test.ts — pins glossary shape and required-term coverage"
affects: [34.8-03, 34.8-04, 34.8-05, 34.8-09]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "meta/__tests__/*.test.ts requiring/reading a root-level config or data file via relative path, mirroring meta/lintTranslations.ts's readFile() idiom"

key-files:
  created:
    - meta/i18nGlossary.json
    - meta/__tests__/i18nParserConfig.test.ts
    - meta/__tests__/i18nGlossary.test.ts
  modified:
    - i18next-parser.config.js

key-decisions:
  - "keepRemoved flipped false->true (D-03) with a WHY comment citing the measured 36-key deletion, rather than fixing the lexer's dynamic-key blind spot"
  - "Glossary covers ConsoleMode/LogSettings/StoreSearch brand-label maps by exact-match term coverage instead of code retrofit (D-21) — verified all three files' literal values already appear verbatim in the required-term list, so zero source files touched"

requirements-completed: [REQ-34.8-02, REQ-34.8-03]

# Metrics
duration: 4min
completed: 2026-08-07
---

# Phase 34.8 Plan 01: i18next-parser keepRemoved flip + do-not-translate glossary Summary

**Flipped `keepRemoved: false -> true` in i18next-parser.config.js and authored `meta/i18nGlossary.json` as a machine-readable do-not-translate list, both pinned by new Meta-project jest tests.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-08-07T00:01:00Z (approx, from first commit)
- **Completed:** 2026-08-07T00:02:34Z
- **Tasks:** 2/2 completed
- **Files modified:** 4 (1 modified, 3 created)

## Accomplishments
- `pnpm i18n` can no longer silently delete fork keys the static lexer cannot see (D-03) — the measured landmine (36 keys: 33 `translation.json`, 3 `gamepage.json`) is defused, and a test fails loudly with the rationale in its output if anyone reverts the flag
- `meta/i18nGlossary.json` created as the `{terms, rationale}` machine-readable artifact plans 03-05's gate will consume, covering every D-02 brand/platform/unit term plus every D-21 compound/variant extension
- Verified — by reading the actual source, not just trusting the plan — that `ConsoleMode/index.tsx`, `LogSettings/index.tsx`, `StoreSearch/helpers.ts` and `PlatformSupport.tsx` need zero code change: every `label:`/`title:`/`STORE_DISPLAY_NAME` literal in those files already appears verbatim in the 17 required glossary terms

## Task Commits

Each task was committed atomically:

1. **Task 1: Flip keepRemoved to true and pin it with an assertion test** - `cd91a9139` (fix)
2. **Task 2: Author meta/i18nGlossary.json with the D-02 and D-21 terms, pinned by a test** - `72860ebbb` (feat)

_No plan-metadata commit yet — this SUMMARY commit is that step._

## Files Created/Modified
- `i18next-parser.config.js` - `keepRemoved` flipped to `true`; comment rewritten to record the D-03 measurement and accepted cost. No other key touched.
- `meta/i18nGlossary.json` - `{terms, rationale}` shape; 17 sorted, de-duplicated, case-sensitive do-not-translate terms
- `meta/__tests__/i18nParserConfig.test.ts` - asserts `keepRemoved===true` (with D-03 context baked into the failure payload), `namespaceSeparator`/`keySeparator`, `output`, and `locales===['en']`
- `meta/__tests__/i18nGlossary.test.ts` - asserts glossary shape, sorted/deduped terms, every one of the 17 required terms present via `it.each`, and a rationale mentioning both D-02 and D-21

## Decisions Made
- Confirmed by direct source read (not just the plan's `<read_first>` line numbers, which had drifted slightly from current file contents) that the required 17-term list already covers every literal in `ConsoleMode/index.tsx:170-190`, `LogSettings/index.tsx:157-166`, `StoreSearch/helpers.ts:24-29`, and `PlatformSupport.tsx` (`title="Windows"`/`"macOS"`/`"Linux"`) — no additional term needed adding beyond the plan's specified minimum, and no `src/frontend/` file was touched
- Kept the `keepRemoved` comment focused on the WHY (measurement + accepted cost) rather than restating what the boolean does, since the parser's own inline comments elsewhere in the file already describe mechanics

## Deviations from Plan

None - plan executed exactly as written. Both tasks' acceptance criteria were verified command-by-command (grep counts, targeted jest runs, node one-liners, `git status --porcelain` on `public/locales/` and `src/frontend/`) and all passed on the first attempt.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plans 03-05 (the enforcement gate) can now load `meta/i18nGlossary.json` via a `loadGlossary(path): string[]` helper as declared in this plan's `<interfaces>` contract, and any later plan that runs `pnpm i18n` (plan 09) does so against a parser config that only ever adds keys. Both new test files ride the existing `Meta` jest project (`npx jest --config meta/jest.config.js`, 8 suites / 152 tests, all green) with zero new CI wiring required. `pnpm codecheck` (`tsc --noEmit`) is clean.

---
*Phase: 34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco*
*Completed: 2026-08-07*

## Self-Check: PASSED

All 4 created/modified files confirmed present on disk; all 3 task/summary commit hashes (`cd91a9139`, `72860ebbb`, `108c013ef`) confirmed in `git log --oneline --all`.
