---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 19
type: execute
wave: 3
depends_on: ["34.4.2-17", "34.4.2-18"]
gap_closure: true
files_modified:
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REQ-34.4.2-01, REQ-34.4.2-02, REQ-34.4.2-03, REQ-34.4.2-04, REQ-34.4.2-05, REQ-34.4.2-06, REQ-34.4.2-07, REQ-34.4.2-08, REQ-34.4.2-09, REQ-34.4.2-10]

must_haves:
  truths:
    - "A fresh six-item blocking gate contract exists with verdict null, authored by a plan forbidden from running it"
    - "All six items are measured fresh in one self-contained run — no item's PASS is inherited from RERUN-3"
    - "Every launch's evidence, from BOTH sinks, survives to the end of the session and is separable per launch"
    - "Item 6(b) — the Epic absence check, never attempted in this phase's five-gate history — is reachable even if item 6(a) fails"
    - "Every item and every PAIR of requirements has been checked before any live run, with the five-test review's results recorded in the contract itself"
    - "Every required log literal names the sink it lands in, and the mandated capture reads that sink"
  artifacts:
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md"
      provides: "The fresh six-item blocking gate contract, results unwritten, carrying its own five-test Structural Reachability Review"
      contains: "verdict: null"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.4.2-09's row naming RERUN-4 as the contract of record, with no box ticked"
      contains: "34.4.2-LIVE-GATE-RERUN-4.md"
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md"
      provides: "A tenth §5 update mapping every open threat to its discharging item in the NEW contract"
      contains: "Tenth update"
  key_links:
    - from: "34.4.2-LIVE-GATE-RERUN-4.md Evidence-capture instruction"
      to: "the dual-sink standard in references/live-gate-contract-authoring.md"
      via: "a timestamped session directory, tee -a, per-launch delimiters, per-launch gamelib.log archiving"
      pattern: "tee -a"
    - from: "34.4.2-LIVE-GATE-RERUN-4.md Structural Reachability Review"
      to: "every item, sub-check, precondition AND every state-mutating/evidence-bearing requirement pair"
      via: "a per-row REACHABLE/IMPOSSIBLE/CONDITIONAL verdict with cited source evidence, plus a recorded pairing pass"
      pattern: "Test 5"
    - from: "34.4.2-LIVE-GATE-RERUN-4.md item 6(b)"
      to: "its own dedicated launch, ordered BEFORE 6(a)"
      via: "an explicit ordering instruction so a 6(a) regression cannot foreclose 6(b) a second time"
      pattern: "6(b)"
---

<objective>
Author `34.4.2-LIVE-GATE-RERUN-4.md` — the sixth blocking live-gate contract in this phase's history,
and the first whose evidence can actually survive its own instructions.

**This plan is FORBIDDEN from running any gate item, starting any harness, launching
`npm run tauri:dev`, or writing any value into `verdict` / `run_date` / `items_passed` / any
`Observed:` or `Verdict:` field.** Authoring and running are separate plans by binding decision
**D-E** — the same separation plans 05/06, 09/10, 11/12 and 15/16 used, which is what makes every
recorded FAIL in this phase trustworthy rather than back-filled from expectation (T-34.4.2-25). Plan
34.4.2-20 is the only writer of results.

**Operator decision D-F1, binding: RERUN-4 covers ALL SIX items and produces ONE fresh verdict.** Do
not carry items 1-5's RERUN-3 PASSes forward into a partial contract. The phase must record a single,
self-contained six-item full PASS in one document, satisfying D-08's no-partial-pass rule cleanly.
This also recovers the item-3 and item-5 evidence that F-34.4.2-11 destroyed, and captures
REQ-34.4.2-06's currently-thin evidence, both of which the fixed capture makes free.

**Three defects in the RERUN-3 contract this one must not repeat.**

1. **F-34.4.2-11.** RERUN-3 mandated `tee /tmp/gamelib-dev.log` with no `-a` while item 3(c)
   separately and correctly mandated a mid-run relaunch. Every relaunch truncated the log. Only the
   final launch's 214 lines survived, containing ZERO occurrences of item 3's and item 5's
   PASS-defining lines. Both items PASSED on the operator's word alone.
2. **F-34.4.2-14.** RERUN-3 required three sidecar-emitted lines of the terminal transcript, which
   structurally cannot carry them: `Humble sync finished:` (`src/backend/humble/library.ts:952`),
   `Humble disconnect: cookie census` (`src/backend/humble/user.ts:1048`) and
   `Humble login-window cookie read UNSUPPORTED_OR_ERROR` (`src/backend/humble/user.ts:460`) all go
   through `logInfo`/`logWarning` to `~/Library/Logs/GameLib/gamelib.log`. And that file rotates to
   `.old` on each launch's first write, so it needs per-launch archiving too.
3. **The 6(a)→6(b) coupling.** Item 6(b) — the Epic absence check — has NEVER been attempted in this
   phase's five-gate history. Every time, something upstream ended the session first. RERUN-4 must
   order and structure item 6 so that a 6(a) failure cannot foreclose 6(b) a sixth time.

**What is genuinely new since RERUN-3.** F-34.4.2-12's root cause is diagnosed, fixed (`6bad86227`)
and live-verified; the mechanically-identical `humble_login_cookies` residual is closed by plan
34.4.2-17; the regression pin is shape-robust across three arms; and the Structural Reachability
Review now has five defect-class tests, the fifth taking the requirement PAIR as its unit
(plan 34.4.2-18). Every one of those changes touched `src-tauri/src/main.rs`, which is precisely why
D-F1 requires all six items be re-measured rather than inherited: **this phase's standing rule is
that a fix does not inherit a previous run's result.**

Output: a six-item contract with `verdict: null`, its five-test review recorded inside it, a
dual-sink capture instruction, REQUIREMENTS.md naming it as contract of record with no box ticked,
and a tenth §5 threat-to-item mapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-3.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
@.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md

**Tooling rule for this repo (mandatory).** `graphify-out/graph.json` exists. Run
`graphify query "<question>"` / `graphify explain "<concept>"` before grepping or reading raw source.
Every log literal this contract requires must be re-grepped against CURRENT source at authoring time
— plan 17 changed `src-tauri/src/main.rs`, so line numbers quoted in RERUN-3 may have moved. Record
the line number you actually observe, never the one RERUN-3 quoted.

<interfaces>
<!-- The contract's own building blocks. Re-confirm each literal's line number against current
     source; the literal TEXT below is what RERUN-3 verified and is the starting point. -->

**Rust `[shell]` literals → tee'd `tauri:dev` transcript sink:**
- `[shell] login-window sheet: read-back attached=true for '{label}'` (was `main.rs:2436`)
- `[shell] humble_login_open: presentation requested visible=true width=900 height=700 center=true focus_once=true persistent_pin=false light_theme_requested=true sheet_presented=true` (was `main.rs:3698`)
- `[shell] login-sheet: cancel requested for '{label}' via strip` / `... via esc` (was `main.rs:1486`)
- `[shell] humble_login_open: REFUSED visible login window '{label}' -- '{incumbent_label}' is already pending or presented (T-34.4.2-39 single-flight)` (was `main.rs:3381-3383`)
- `[shell] humble_login_open: visible login window '{label}' armed as the single pending login flow (T-34.4.2-39)` (was `main.rs:3387-3389`)
- `[shell] WARN: login-window dismiss: '{label}' main-thread dispatch failed ...` and
  `... main-thread hop unconfirmed within 10s ...` (was `main.rs:2579` / `:2589`), record-only (WR-01)

**MUST-NOT-APPEAR literal (its absence is a PASS signal):**
- `[shell] login-window: parent deminiaturized, re-raising {N} attached child window(s)` — the
  observer was deleted outright by plan 07 Task 2.
- `post_autofill_right_click`, `autofill glyph injected for`, `autofill glyph injection SKIPPED`,
  `GAMELIB_AUTOFILL_GLYPH` — all deleted by plan 13 (D-A). Confirm each returns 0 from
  `grep -c` against current `src-tauri/src/main.rs` at authoring time.

**Sidecar `logInfo`/`logWarning` literals → `~/Library/Logs/GameLib/gamelib.log` sink ONLY:**
- `Humble sync finished: gamekeys={N} fetched={M}/{total} ...` — `src/backend/humble/library.ts:952`
- `Humble disconnect: cookie census before(total={N}, matched={M}) after(total={N2}, matched={M2}) deleted={D} survivingNonHumble={S}` — `src/backend/humble/user.ts:1048`
- `Humble login-window cookie read UNSUPPORTED_OR_ERROR for window {seamLabel} — aborting watch` — `src/backend/humble/user.ts:460`
All three carry **aggregate counts only** — no individual gamekey, order identifier, cookie value or
account id may be transcribed into the contract or into any `Observed:` field.

**Structural constraints that killed sub-checks in prior contracts — apply Test 1 and Test 2 against
them:**
- `login_window_url_arg` (was `main.rs:926`, test at `main.rs:5725`) rejects any scheme that is not
  exactly `https` before any OS window is built. Anything non-`https` in a Tauri-managed login window
  is IMPOSSIBLE. This killed two sub-checks and one precondition in gate run 2.
- A presented AppKit sheet is application-modal to its parent, and every login trigger lives inside
  the single `main` window. A human physically cannot click a second sign-in control while a sheet
  is up. This is why RERUN-3 deleted item 5's "Arm 2" and retired item 6's "while a visible sheet is
  separately open" framing.
- `status=cancelled reason=window-closed` is emitted by `oauthLoginCapture.ts:236`, gated to
  `OAuthRunner = 'legendary' | 'gog' | 'nile' | 'zoom'` (`src/common/types/oauthLogin.ts:15`).
  **Humble is not an OAuth runner and structurally cannot emit it.** Never require it of Humble.
- The DummyStore harness must NOT be started (T-34.4.2-27): the https-only gate makes it unreachable
  from a Tauri-managed login sheet, and REQ-34.4.2-07 forbids building or reading any credential
  store. Record its absence with the reason, as RERUN-3 did, so a future author does not re-add it.

**Fixed-by-this-cycle facts the contract must state as its premise:**
- F-34.4.2-12 is fixed at source by `6bad86227`; the `humble_login_cookies` residual by plan
  34.4.2-17. Item 6(a) is the LIVE DISCHARGE of both — a source fix closes nothing in this phase.
- The functional side-effect proof the debug session used and this contract may reuse:
  `~/Library/Application Support/gamelib/humble_store/config.json` becomes 2 bytes (`{}`) with an
  mtime matching the run when the disconnect genuinely empties the credential store. This is an
  independent, filesystem-channel signal that does not share a failure mode with either log sink.

**Document shape to mirror** (`34.4.2-LIVE-GATE-RERUN-3.md`): frontmatter with `verdict: null`,
`run_date: null`, `items_total: 6`, `items_passed: null`; then `## What changed since gate run 3`,
`## Evidence-capture instruction`, `## Preconditions`, `## Structural Reachability Review`,
`## The six items`, `## Findings recorded this run` (empty at authoring), `## ITEM VERDICT SUMMARY`,
`## Harness teardown`, `## What a PASS does and does not close`, `## Threat-to-item discharge table`.
The Review section goes BEFORE the Items section so a runner meets it first.

**Binding decisions to encode, never revisit:** D-A (the in-field autofill affordance is DELETED;
Cmd+V / Edit ▸ Paste is the sole credential-entry route — never re-propose it), D-C (AppKit sheet
presentation is permanent; child-window attachment is off the table forever), D-D / REQ-34.4.2-10
(the pristine Epic surface is byte-untouched; 6(b) is a read-only ABSENCE check and nothing more),
D-08 (no partial pass — one item's FAIL fails the whole gate), D-09 (non-macOS unchanged), D-E
(author ≠ runner), D-F1 (all six items, one fresh verdict).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 34.4.2-LIVE-GATE-RERUN-4.md — six items, verdict null, with an evidence-capture instruction whose evidence actually survives</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-3.md` in full — the document being succeeded. Its
      six items, its Evidence-capture instruction (lines 92-107), its Preconditions including the
      REMOVED-with-reasons list (lines 108-158), and its Findings section (lines 802-898).
      **Do not edit `-RERUN-3.md`.** It is the historical record of the run that made this rewrite
      necessary and must stay byte-unchanged.
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` — the
      dual-sink evidence-capture standard written by plan 34.4.2-18. Implement it literally; this
      contract is its first implementation.
    - `.planning/debug/resolved/humble-disconnect-main-wedge.md` — `## Resolution` (for what item
      6(a) is now discharging and for the `humble_store/config.json` functional proof) and
      `## Residual Risks` (for the never-attempted 6(b)).
    - `.planning/phases/.../34.4.2-17-SUMMARY.md` and `.../34.4.2-18-SUMMARY.md` — what actually
      landed this cycle, so `## What changed since gate run 3` describes reality rather than intent.
  </read_first>
  <action>
    Create `34.4.2-LIVE-GATE-RERUN-4.md`. Frontmatter: `phase`, `plan: 19`, `type: live-gate`,
    `status: pending`, `blocking: true`, `created: 2026-08-06`, `verdict: null`, `run_date: null`,
    `items_total: 6`, `items_passed: null`, and a `notes:` block stating that it was authored by a
    plan forbidden from running it (D-E), that plan 34.4.2-20 is the only writer of results, and that
    it succeeds `-RERUN-3.md` without erasing that document's own FAIL 5/6 record.

    **`## What changed since gate run 3`.** State the three source changes (F-34.4.2-12's fix at
    `6bad86227`; plan 17's `humble_login_cookies` hardening and shape-robust pin; no other runtime
    change) and the two process changes (the dual-sink capture standard; the review's fifth
    defect-class test). Then state D-F1's rule in the document's own voice: **all six items are
    re-measured, none inherited.** Cite the phase's own repeated finding — every blocking defect in
    phases 34.4.1 and 34.4.2 was found by a human driving the UI, none by a suite that was green
    throughout.

    **`## Evidence-capture instruction`.** Implement the dual-sink standard exactly. It must specify,
    as literal commands the runner can copy:
    - A ONE-TIME session setup before the first launch that creates a UTC-timestamped session
      directory under `/tmp` (name it so a prior gate run's directory can never be reused or mixed
      in), and exports its path.
    - A per-launch delimiter written into the session transcript before each launch, carrying the
      launch ordinal and a UTC timestamp. **State the delimiter's exact literal text** so the runner
      can emit it and a reader can grep for the boundaries.
    - The launch command itself using `tee -a` into the single session transcript — **never a bare
      `tee`, never `>`**. Carry forward RERUN-3's optional `GAMELIB_DEV_SECRET_VAULT=1` prefix with
      its existing justification (it bypasses only this project's own `TokenStore` path, not the
      macOS system Passwords store, so it affects no item's evidence).
    - A per-launch archive step copying `~/Library/Logs/GameLib/gamelib.log` into the session
      directory under a launch-numbered name, performed **after that launch's app has quit and
      before the next launch starts** — because `src/backend/logger/log_writer.ts:72-74` renames the
      file to `.old` on the next process's first write. Also archive any pre-existing `.old` on the
      first pass. Resolve and record the log file's exact filename at authoring time by running
      `ls -la ~/Library/Logs/GameLib/` and quoting the result; do not guess it.
    - An instruction that every item records the launch ordinal that covered it.
    - A closing inventory step: at session end, list the session directory with per-file line counts,
      so a zero-length or missing capture is visible immediately.

    **State the sink partition explicitly, in the instruction itself**, so no item can be written
    against the wrong sink again: Rust `[shell]` lines land ONLY in the session transcript; sidecar
    `logInfo`/`logWarning` lines land ONLY in `gamelib.log`; sidecar stderr reaches the transcript
    prefixed `[sidecar:err]`; sidecar `console.*` reaches nothing. **Every required literal in every
    item below must be annotated with its sink.**

    **`## Preconditions`.** Carry RERUN-3's four (macOS hardware with a build rebuilt against plans
    17/18's commits; a real Humble login available; a copied clipboard string staged before item 4;
    the dual-sink capture in place before launch 1), each labelled OPERATOR-AT-GATE-TIME or
    machine-satisfiable-in-preflight, with satisfaction status left BLANK for plan 20's preflight to
    fill from a real measured check. Carry forward RERUN-3's REMOVED-with-reasons list verbatim in
    substance (the DummyStore harness, the seeded Passwords entry, the `GAMELIB_AUTOFILL_GLYPH`
    default arm) so a future author does not re-add them without re-deriving why they were cut.

    **`## The six items`.** Author all six with fresh, measurable per-item PASS bars and named
    decisive lines annotated by sink. Base them on RERUN-3's items — that item set was structurally
    reviewed and every one of them was reached — with these mandatory changes:

    - **Item 1** (REQ-01/-02): sheet presentation and minimize/restore survival, sub-checks (a)-(e).
      Required transcript lines: the `read-back attached=true` line and the
      `presentation requested ... sheet_presented=true` line, in that order, and the RETIRED
      re-raise line MUST NOT appear. Keep RERUN-3's "do not accept `sheet_presented=` alone"
      reasoning.
    - **Item 2** (REQ-03): dismissability. Route (a) cancel strip and route (b) bare Esc are BOTH
      required PASSes with their own `via strip` / `via esc` transcript lines; RERUN-3 recorded
      route (b)'s line as absent from the surviving transcript, so this run must actually capture it.
      Keep the exactly-one-cancel-control sub-check, the main-window-interactive-after-dismissal
      sub-check, route (c) Cmd+W and route (d) titlebar-absence as record-only, and the WR-01
      record-only re-registration lines. Humble's cancellation signal is the
      `UNSUPPORTED_OR_ERROR` line — annotate it as a **gamelib.log** literal, not a transcript one.
      Never require `status=cancelled reason=window-closed` of Humble.
    - **Item 3** (REQ-04/-05): the autofill affordance is gone. Sub-checks (a) visual absence,
      (b) zero occurrences of the three deleted literals **in the session transcript across ALL
      launches** (now actually possible), (c) the `GAMELIB_AUTOFILL_GLYPH=0` relaunch changes
      nothing, (d) record-only real right-click still reaches system AutoFill. **Sub-check (c) is
      the relaunch that broke RERUN-3** — the contract must state that it is safe here only because
      of the append-and-archive capture, and Test 5 must have flagged and cleared this exact pair.
    - **Item 4** (REQ-06): Cmd+V and Edit ▸ Paste as the sole credential-entry route, with the Esc
      monitor sub-check, and the raised bar that a real Humble login must COMPLETE using paste alone.
      Its evidence is the aggregate `Humble sync finished: gamekeys={N} fetched={M}/{total}` line —
      annotate it **gamelib.log**, aggregate counts only. This closes REQ-34.4.2-06's currently-thin
      evidence, which the transcript-survival fix makes free.
    - **Item 5** (T-34.4.2-39/-41): single-flight login initiation. Keep RERUN-3's single scenario
      (click Amazon sign-in, then click Humble sign-in during nile's ~7-8s upstream delay) and its
      AMENDMENT deleting the structurally-impossible "Arm 2". Required transcript lines: the REFUSED
      line and the armed line. Decisive sub-check: no second sheet appears on dismissing the
      presented one. Keep the two record-only sub-checks (which incumbent branch fired; what the
      user actually sees, where "nothing at all" on the Humble arm is an EXPECTED recorded outcome,
      not a FAIL).
    - **Item 6** — restructure for reachability, and say why in the item's own text:
      - **6(b) runs FIRST, in its own launch.** Open the Epic login window once and record the
        required absences: no cancel strip for Epic's label, no sheet-presentation line for it, and
        no single-flight refusal attributable to it. If Epic's login is blocked by its own parked
        pre-auth 403, that is EXPECTED and does NOT fail the item, provided the page reached is
        observable enough to confirm the absences. State explicitly that this is an ABSENCE check
        only and is NOT, and cannot be read as, a statement that Epic's login UX works (D-D,
        T-34.4.2-32). **State the ordering rationale in the document:** 6(b) has never been attempted
        in five gate runs because something upstream always ended the session first; running it in
        its own launch, before 6(a), removes that coupling. Add an explicit clause: **if 6(a)
        subsequently fails, 6(b)'s already-recorded result stands and must be reported, not voided.**
      - **6(a) runs after, in its own launch.** The Humble disconnect route, opening a hidden window
        through the same `if visible` gate. Required absences: no cancel-strip line for the hidden
        window's label, no sheet-presentation line for it, and it must NOT be refused by the
        single-flight guard (which is scoped to `visible == true`; a refused hidden window would be
        a plan 14 regression and fails the item). Positive evidence, annotated **gamelib.log**: the
        cookie-census aggregate line. **Add the independent filesystem-channel proof** the debug
        session established: `~/Library/Application Support/gamelib/humble_store/config.json` is 2
        bytes (`{}`) with an mtime matching the run, versus its larger sibling backups — record its
        size and mtime before and after. State why this second channel is required: it does not
        share a failure mode with either log sink, and this phase has twice recorded a PASS whose
        own decisive log evidence was unavailable.
      - **Scoring stays D-08:** item 6 is ONE item and both sub-parts must satisfy their conditions
        for it to PASS. The restructure changes reachability, not the scoring rule.

    **`## Findings recorded this run`** — present but empty, with a one-line note that plan 34.4.2-20
    is its only writer.

    **`## ITEM VERDICT SUMMARY`** — the six-row table with every Verdict cell EMPTY.

    **`## Harness teardown`** — a "no harness is started" statement with RERUN-3's reason, not a PID
    record.

    **`## What a PASS does and does not close`** — a full six-item PASS discharges REQ-34.4.2-09 and
    ticks REQ-34.4.2-04/-05; it does NOT close F-34.4.2-10 (the still-open bounded-timeout debt in
    `clearHumbleStorage`, which F-34.4.2-12's fix did not touch and which was never reached in the
    run that produced the wedge), does NOT verify Windows or Linux (declared unverified, D-09), and
    does NOT make any statement about Epic's login UX beyond the 6(b) absences.

    **Write NO result of any kind.** Every `Observed:` and `Verdict:` field must be present and
    empty. Frontmatter `verdict` stays `null`.
  </action>
  <verify>
    <automated>G=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md; test -f "$G" && grep -q '^verdict: null' "$G" && grep -q '^items_passed: null' "$G" && test "$(grep -cE '^\*\*Verdict:\*\* *(PASS|FAIL|NOT ATTEMPTED|BLOCKED)' "$G")" = "0" && grep -q 'tee -a' "$G" && test "$(grep -cE 'tee /tmp|tee "\$' "$G")" = "0" && grep -q 'gamelib.log' "$G" && grep -q 'humble_store/config.json' "$G" && for r in 01 02 03 04 05 06 07 08 09 10; do grep -q "REQ-34.4.2-$r" "$G" || { echo "REQ-34.4.2-$r NOT COVERED"; exit 1; }; done; git diff --quiet -- .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-3.md && test "$(lsof -nP -iTCP:17940 -sTCP:LISTEN | wc -l | tr -d ' ')" = "0" && echo CONTRACT_AUTHORED_NO_RESULTS</automated>
  </verify>
  <acceptance_criteria>
    - `34.4.2-LIVE-GATE-RERUN-4.md` exists with `verdict: null`, `run_date: null`, `items_total: 6`,
      `items_passed: null` (asserted by the verify command).
    - Zero filled `Observed:` or `Verdict:` fields anywhere in the document (asserted by the verify
      command).
    - The Evidence-capture instruction uses `tee -a` and contains no bare `tee` into a fixed path
      (asserted by the verify command).
    - The instruction specifies: a UTC-timestamped session directory, the exact per-launch delimiter
      text, per-launch `gamelib.log` archiving with the rotation reason cited to
      `src/backend/logger/log_writer.ts`, per-item launch-ordinal recording, and a closing inventory
      with line counts.
    - The exact `gamelib.log` filename was resolved by running `ls -la ~/Library/Logs/GameLib/` and
      that output is quoted in the document.
    - Every required log literal in every item is annotated with its sink (`session transcript` or
      `gamelib.log`). No sidecar-emitted literal is required of the transcript.
    - Item 6(b) is ordered BEFORE 6(a), runs in its own launch, and the document states in its own
      text that a 6(a) failure does not void 6(b)'s recorded result.
    - Item 6(a) requires BOTH the `gamelib.log` cookie-census line AND the
      `humble_store/config.json` size/mtime filesystem proof.
    - Item 4 requires the aggregate `Humble sync finished: gamekeys=` line, annotated `gamelib.log`.
    - All ten REQ-34.4.2 IDs appear in the document, each mapped to a named item or recorded as
      source-closed and not gate-dependent (asserted by the verify command).
    - Every log literal quoted in the contract was re-grepped against CURRENT source at authoring
      time and its observed line number recorded — not copied from RERUN-3's line numbers.
    - `34.4.2-LIVE-GATE-RERUN-3.md`, `-RERUN-2.md`, `-RERUN.md` and `-LIVE-GATE.md` are all
      byte-unchanged (`git diff --quiet` on RERUN-3 asserted by the verify command; check the other
      three by hand).
    - `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing; no `tauri:dev` was launched.
  </acceptance_criteria>
  <done>A six-item blocking contract exists whose PASS conditions were fixed before the outcome was known, whose every required literal names a sink the mandated capture actually reads, whose evidence survives every relaunch from both sinks, and whose item 6(b) can finally be measured regardless of what item 6(a) does.</done>
</task>

<task type="auto">
  <name>Task 2: Run the five-test Structural Reachability Review against the contract, record it inside the contract, and fix what it finds — before any operator burns a sixth evening</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md</files>
  <read_first>
    - The contract Task 1 just wrote, in full — this task audits it and may send it back for edits.
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` — the five
      tests as plan 34.4.2-18 finally worded them, including Test 3's SINK clause and Test 5's
      pair-scoping reduction rule. Apply them as written; do not paraphrase them into something
      weaker.
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-3.md:314-411` — the review section's table shape
      and its "What the review found and changed" and requirement-coverage subsections, for format.
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-3.md:879-898` — the honest account of what the
      four-test review did and did not catch. Your review must be at least as candid.
    - `src-tauri/src/main.rs` around `login_window_url_arg` and its https-only rejection test — the
      single most productive source of contract impossibilities in this phase's history. Locate the
      current line numbers via graphify then a targeted grep; they may have moved since plan 17.
  </read_first>
  <action>
    Add a `## Structural Reachability Review` section to `34.4.2-LIVE-GATE-RERUN-4.md`, positioned
    BEFORE the `## The six items` section so a runner meets it first.

    **A. Per-requirement pass (Tests 1-4).** One table row per item, sub-check, precondition AND the
    evidence-capture instruction itself, with columns
    `Item/sub-check | Surface it names | What it asks for | REACHABLE / IMPOSSIBLE / CONDITIONAL | Evidence (file:line or command output)`.
    **Evidence must be a source citation or real command output — never an assertion.** Apply:
    - **Test 1, origin/scheme.** Does anything require a URL `login_window_url_arg` rejects?
    - **Test 2, concurrency.** Does anything require two flows driven at once through a UI where a
      presented sheet blocks its own parent? Item 5 is the one place this contract deliberately asks
      about two flows — verify explicitly, not by assumption, that the second initiation happens
      during the FIRST's pre-presentation window, when no sheet is up and the parent is still
      interactive, and record that reasoning in the row.
    - **Test 3, log-line emitter AND SINK.** For every literal any item requires: grep it in current
      source, record the file and line you actually observed, confirm the surface under test can
      reach that emitter, **and confirm the mandated capture reads the sink that emitter writes to.**
      A `logInfo` literal required of the terminal transcript is IMPOSSIBLE — that is F-34.4.2-14 and
      it must not recur. A literal deleted by plan 13 may appear only in a MUST-NOT-APPEAR list.
    - **Test 4, absence-observability.** For every required ABSENCE (item 3's zero-glyph, item 6's
      Epic absences, item 6(a)'s hidden-window absences, the retired re-raise line), confirm the
      PRESENCE case would have produced an observable signal, and state per row what makes each
      absence meaningful. An absence assertion over a path that logs nothing on success proves
      nothing — say so where it applies.

    **B. Pairwise pass (Test 5) — a separate, explicitly-labelled table.** For every pair (A, B) of
    requirements in this contract, ask whether satisfying A destroys, truncates, restarts, clears,
    overwrites or otherwise invalidates B's own evidence or preconditions. Apply the reference's
    scoping reduction (state which reduction you applied) rather than an unexplained subset: at
    minimum, cross every STATE-MUTATING requirement against every EVIDENCE-BEARING one. The
    state-mutating set in this contract includes at least: item 3(c)'s `GAMELIB_AUTOFILL_GLYPH=0`
    relaunch; item 6(b)'s and 6(a)'s own dedicated launches; item 6(a)'s Humble disconnect (which
    CLEARS the credential store and the cookie jar); item 2's repeated sheet dismissals; item 4's
    consumption of the staged clipboard string; item 5's Amazon initiation being refused.

    Record the count of pairs considered and the count flagged. **Three pairs are known in advance
    and must appear in the table with their resolutions**, so a reader can see the test doing real
    work rather than rubber-stamping:
    - *(item 3(c) relaunch, every prior item's transcript evidence)* — the original F-34.4.2-11.
      Resolved by `tee -a` plus per-launch delimiters. Say so.
    - *(item 3(c) relaunch, every prior item's `gamelib.log` evidence)* — the newly-found rotation
      hazard. Resolved by per-launch archiving before the next launch's first write. Say so.
    - *(item 6(a) disconnect, item 4's completed-login state)* — the disconnect empties the Humble
      credential store, so any item depending on a logged-in Humble session must be measured BEFORE
      6(a), and item 4's own `Humble sync finished:` evidence must already be archived. State the
      required ordering explicitly and confirm the contract's item order and launch plan enforce it.

    **C. Fix what the review finds, in the contract, now.** Any row marked IMPOSSIBLE, and any flagged
    pair, must be rewritten, re-ordered, replaced with an equivalent-route alternative, or deleted —
    before this plan ends. **Do not leave a known-impossible item or an unresolved interaction for the
    runner to discover.** Record every change the review caused, in the review section itself. If it
    found nothing in some category, say so explicitly along with what was checked, because "found
    nothing" and "did not look" must be distinguishable to whoever reads this later.

    **D. Requirement-coverage cross-check.** Confirm every REQ-34.4.2-01..10 is either claimed by a
    named item or explicitly recorded as source-closed and not gate-dependent (as -07 and -08 are).
    Record the mapping as a table. Any requirement with neither is a coverage hole — fix it here.

    **E. This plan writes no result.** Confirm at task end: no `Observed:` filled, no `Verdict:`
    filled, frontmatter still `verdict: null`, no `tauri:dev` launched, no harness started, no REQ
    box ticked.
  </action>
  <verify>
    <automated>G=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md; grep -q 'Structural Reachability Review' "$G" && for t in "Test 1" "Test 2" "Test 3" "Test 4" "Test 5"; do grep -q "$t" "$G" || { echo "MISSING $t"; exit 1; }; done; grep -qi 'pairs considered' "$G" && grep -q 'F-34.4.2-11' "$G" && grep -q 'F-34.4.2-14' "$G" && grep -q '^verdict: null' "$G" && test "$(grep -cE '^\*\*Verdict:\*\* *(PASS|FAIL|NOT ATTEMPTED|BLOCKED)' "$G")" = "0" && test "$(awk '/## Structural Reachability Review/{r=NR} /## The six items/{i=NR} END{print (r>0 && i>r) ? "OK" : "BAD"}' "$G")" = "OK" && test "$(lsof -nP -iTCP:17940 -sTCP:LISTEN | wc -l | tr -d ' ')" = "0" && echo REVIEW_COMPLETE_NO_RESULTS_WRITTEN</automated>
  </verify>
  <acceptance_criteria>
    - The `## Structural Reachability Review` section exists and is positioned BEFORE
      `## The six items` (asserted by the verify command's line-order check).
    - The per-requirement table has one row per item, sub-check, precondition and the
      evidence-capture instruction, each with a REACHABLE / IMPOSSIBLE / CONDITIONAL verdict and a
      source citation or command output as evidence — no row's evidence column is a bare assertion.
    - All five defect-class tests are named and their application is visible in the tables (asserted
      by the verify command).
    - Test 3's SINK clause is visibly applied: every required literal's row names its sink and states
      whether the mandated capture reads it.
    - The Test 5 pairwise pass is its OWN labelled table, records the count of pairs considered and
      the count flagged, and names the scoping reduction applied.
    - The three known pairs (3(c)-relaunch × transcript evidence; 3(c)-relaunch × `gamelib.log`
      evidence; 6(a)-disconnect × item 4's logged-in state) all appear with explicit resolutions.
    - The contract's item order and launch plan are confirmed to place every session-dependent item
      before item 6(a)'s disconnect.
    - Item 5's two-flow reachability is explicitly reasoned (the second initiation lands in the
      first's PRE-presentation window), not assumed.
    - Every change the review caused is listed; where it found nothing, that is stated together with
      what was checked.
    - All ten REQ-34.4.2 IDs are mapped to an item or recorded source-closed (asserted by Task 1's
      verify command and re-asserted here).
    - Frontmatter still reads `verdict: null` and no `Observed:`/`Verdict:` field is filled.
    - `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing; no `tauri:dev` was launched.
  </acceptance_criteria>
  <done>Every item, sub-check, precondition and evidence-capture instruction in the new contract carries a reachability verdict backed by a source citation; every state-mutating requirement has been crossed against every evidence-bearing one with the flagged pairs resolved by re-ordering or by the capture fix; and the contract still holds no result of any kind.</done>
</task>

<task type="auto">
  <name>Task 3: Name RERUN-4 as the contract of record and remap every open threat to its discharging item — ticking nothing</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the REQ-34.4.2-01..10 rows (the block beginning at the Phase
      34.4.2 header near line 731), specifically REQ-34.4.2-09's row and the current `[ ]`/`[x]`
      state of every row. Confirm the state before editing; the correct current state is 7 ticked
      (01/02/03/06/07/08/10) and 3 unticked (04/05/09).
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` §5 — the threat register roll-up (starts line
      427) and its nine existing dated update tables, ending with the "Ninth update (2026-08-05,
      plan 34.4.2-16 ...)" at line 689. All nine must survive; you append a tenth.
    - `.planning/phases/.../deferred-items.md`'s new `## Plans 17-18` section — the current
      dispositions of F-34.4.2-10/-11/-12/-13/-14 and T-34.4.2-43, so the tenth update agrees with
      the ledger instead of contradicting it.
  </read_first>
  <action>
    **A. REQUIREMENTS.md.** Append a dated note to REQ-34.4.2-09's row stating that
    `34.4.2-LIVE-GATE-RERUN-4.md` is now the contract of record, that it was structurally reviewed
    against five defect-class tests before any run, that it covers all six items freshly per D-F1,
    and that its `verdict` stays `null` until plan 34.4.2-20 records a measured result. Append a
    short dated note to REQ-34.4.2-04 and REQ-34.4.2-05 naming item 3 of the new contract as their
    live discharge, and to REQ-34.4.2-06 noting that its previously-thin evidence (the aggregate
    sync-completion line) is now a named required line of item 4 with its sink identified.
    **Tick no box.** REQ-34.4.2-04, -05 and -09 stay `[ ]`; every currently-ticked box stays as it is.

    **B. PLATFORM-SCOPE.md §5.** Append a **TENTH** update table, retaining all nine earlier ones.
    Head it `**Tenth update (2026-08-06, plan 34.4.2-19 — authoring 34.4.2-LIVE-GATE-RERUN-4.md and
    its five-test Structural Reachability Review)**`. Map each open threat to its discharging item in
    the NEW contract, so no threat's discharge still points at the superseded `-RERUN-3.md`:
    - **T-34.4.2-43** (DoS, the main-thread wedge) — source-fixed by `6bad86227` plus plan 17's third
      call site; live discharge is item 6(a). Not discharged until measured.
    - **T-34.4.2-39 / -41** (origin confusion / a latch that never clears) — live discharge is item 5.
      RERUN-3 recorded these as discharged on the operator's word only, with the REFUSED line absent
      from the surviving transcript; state that this run is their first transcript-corroborated
      measurement.
    - **T-34.4.2-40** (a deleted mechanism silently reintroduced) — static half source-closed by
      `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS`; live half is item 3.
    - **T-34.4.2-32** (Epic scope-boundary erosion) — source-closed; live absence check is item 6(b),
      which is being attempted for the first time in this phase's history.
    - **T-34.4.2-42** (a contract demanding evidence its surface cannot produce) — its completeness
      gap is now closed by Test 5 and by Test 3's sink clause; the measurable clause stays: the count
      of impossibilities and unresolved interactions discovered during the next run should be zero.
    - **T-34.4.2-25 / -26** (a back-filled result; a green suite mistaken for live proof) — held by
      D-E and by D-F1's re-measure-everything rule.
    - **T-34.4.2-27** (a test fixture reaching production) — the DummyStore harness is not started at
      all; REQ-34.4.2-07 stays source-closed.
    - **F-34.4.2-10** — record explicitly that it remains OPEN and is NOT discharged by anything in
      this contract, so a full PASS cannot be misread as closing it.
    Also record the new findings F-34.4.2-13 and F-34.4.2-14 and their dispositions, cross-referenced
    to the ledger rather than duplicated.

    **C.** Confirm at task end that no gate result exists anywhere, that
    `34.4.2-LIVE-GATE-RERUN-4.md`'s frontmatter still reads `verdict: null`, and that no requirement
    box changed state.
  </action>
  <verify>
    <automated>P=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md; grep -q 'Tenth update' "$P" && for u in "Second update" "Third update" "Fourth update" "Fifth update" "Sixth update" "Seventh update" "Eighth update" "Ninth update"; do grep -q "$u" "$P" || { echo "LOST $u"; exit 1; }; done; grep -q '34.4.2-LIVE-GATE-RERUN-4.md' .planning/REQUIREMENTS.md && test "$(grep -cE '^- \[x\] \*\*REQ-34.4.2-(04|05|09)' .planning/REQUIREMENTS.md)" = "0" && test "$(grep -cE '^- \[x\] \*\*REQ-34.4.2-(01|02|03|06|07|08|10)' .planning/REQUIREMENTS.md)" = "7" && grep -q '^verdict: null' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md && git diff --stat -- src src-tauri/src | grep -q . && echo UNEXPECTED_SOURCE_CHANGE || echo PROPAGATED_NO_TICKS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s REQ-34.4.2-09 row names `34.4.2-LIVE-GATE-RERUN-4.md` as the
      contract of record (asserted by the verify command).
    - REQ-34.4.2-04, -05 and -09 remain `[ ]`, and exactly seven REQ-34.4.2 boxes remain `[x]`
      (01/02/03/06/07/08/10) — both asserted by the verify command.
    - REQ-34.4.2-04, -05 and -06 each gain a dated note naming their discharging item in the new
      contract.
    - `34.4.2-PLATFORM-SCOPE.md` §5 gains a `Tenth update` table and all eight earlier named updates
      survive (asserted by the verify command).
    - The tenth update maps T-34.4.2-43, -39, -41, -40, -32, -42, -25, -26 and -27 to a discharging
      item or a source-closure, and records F-34.4.2-10 as still OPEN and not discharged by this
      contract.
    - F-34.4.2-13 and F-34.4.2-14 appear in the tenth update, cross-referenced to `deferred-items.md`
      rather than duplicated in full.
    - `git diff --stat -- src src-tauri/src` is EMPTY — this plan changes no code.
    - `34.4.2-LIVE-GATE-RERUN-4.md` still reads `verdict: null` (asserted by the verify command).
  </acceptance_criteria>
  <done>The requirements ledger and the threat roll-up both point at RERUN-4 as the contract of record, every open threat has a named discharging item in that contract, F-34.4.2-10 is explicitly excluded from what a PASS would close, and not one requirement box moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan author → gate result | An author who can also record results can back-fill an outcome from expectation. Held apart by D-E; plan 34.4.2-20 is the only writer. |
| contract text → the surface it names | A contract can demand evidence a surface cannot produce. Four confirmed instances in gate run 2. |
| contract text → the SINK its evidence lands in | New this cycle (F-34.4.2-14). An emitter can exist and be reachable while the mandated capture reads a different sink entirely. Test 3's sink clause polices this. |
| requirement A → requirement B's evidence | Two individually-correct requirements can destroy each other's evidence (F-34.4.2-11). Test 5 polices this. |
| the contract → the operator's evening | Every impossibility or unresolved interaction that survives review costs live-run time an operator does not get back. Five gate runs' worth so far. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-25 | Repudiation (a result back-filled from expectation) | `34.4.2-LIVE-GATE-RERUN-4.md` | mitigate — CLOSED by process | PASS conditions are fixed here, before the outcome is known. `verdict`/`run_date`/`items_passed` stay `null`; this plan is forbidden from running any item, starting any harness, or launching `tauri:dev`; acceptance criteria assert every `Observed:` field is empty and the `lsof` check asserts no harness is up. Plan 34.4.2-20 is the sole writer of results. |
| T-34.4.2-26 | Repudiation (a green suite mistaken for live proof) | the whole contract | mitigate — CLOSED by process | D-F1: all six items are RE-MEASURED, none inherited, and the contract says so in its own text. `src-tauri/src/main.rs` changed twice this cycle (`6bad86227` and plan 17), and this phase's rule is that a fix does not inherit a previous run's result. |
| T-34.4.2-42 | Repudiation (a contract demanding evidence its named surface cannot produce, wasting a live run) | contract authoring | mitigate — completeness gap closed | Task 2 applies five defect-class tests, not four: Test 3 now checks the SINK as well as the emitter (closing F-34.4.2-14's class) and Test 5 takes the requirement PAIR as its unit (closing F-34.4.2-11's class). Every row carries a source-cited verdict; every IMPOSSIBLE row and every flagged pair must be fixed before this plan ends. Measurable: the count discovered during the next run should be zero. |
| T-34.4.2-43 | Denial of service (a user-triggered action wedges the whole app) | Humble disconnect, item 6(a) | mitigate — source-fixed, live discharge item 6(a) | The contract gives it a discharging item with dual-channel evidence: the `gamelib.log` cookie-census aggregate line AND the independent `humble_store/config.json` size/mtime filesystem proof, which do not share a failure mode. Item 6(b) is ordered first so a 6(a) regression cannot foreclose it a sixth time. |
| T-34.4.2-32 | Tampering (Epic scope-boundary erosion) | item 6(b) | mitigate — CLOSED by source | D-D / REQ-34.4.2-10. The contract must state explicitly that 6(b) is an ABSENCE check only and cannot be read as a statement that Epic's login UX works. This plan writes no code and touches no Epic region; the mutation-tested absence guards in `tauriShellSource.test.ts` remain the enforcement. |
| T-34.4.2-40 | Tampering (a deleted mechanism silently reintroduced) | item 3 | mitigate — source-closed, live corroboration item 3 | The three deleted literals appear ONLY in the MUST-NOT-APPEAR list, never as a required positive, and item 3(b)'s zero-occurrence check is now measurable across ALL launches rather than only the last surviving one. |
| T-34.4.2-27 | Tampering (a test fixture reaching production) | DummyStore harness | mitigate — CLOSED, and moot | The harness is not started at all: the https-only gate makes it unreachable from a Tauri-managed login sheet, and REQ-34.4.2-07 forbids building or reading any credential store. The contract records its removal with the reason so a future author does not re-add it. |
| **T-34.4.2-49 (NEW)** | Information disclosure (a session credential, cookie value, gamekey, order id or account identifier transcribed into a committed gate document) | every `Observed:` field the runner will later fill, and every literal quoted here | mitigate | Every required literal in this contract is an aggregate-count or label-only shape; the contract must state, in the Evidence-capture instruction, that cookie VALUES, individual gamekeys, order identifiers and account ids are never to be transcribed into any `Observed:` field or quoted excerpt — counts, domains and window labels only. This phase already has a recorded incident of an unredacted account identifier reaching committed files, so this is an active class here. The session directory lives under `/tmp` and is never committed. |
| T-34.4.2-SC | Tampering (dependency surface) | none | mitigate — CLOSED | Documents-only: no `npm`/`pip`/`cargo` install, no source change. `git diff --stat -- src src-tauri/src` must be EMPTY at plan end. |
</threat_model>

<verification>
- `34.4.2-LIVE-GATE-RERUN-4.md` exists with six items, `verdict: null`, `run_date: null`, `items_passed: null`, and zero filled `Observed:`/`Verdict:` fields.
- The Evidence-capture instruction uses `tee -a`, a timestamped session directory, per-launch delimiters with stated exact text, per-launch `gamelib.log` archiving, per-item launch-ordinal recording, and a closing inventory.
- Every required literal is annotated with its sink; no sidecar-emitted literal is required of the terminal transcript.
- Item 6(b) precedes 6(a), runs in its own launch, and its result explicitly survives a 6(a) failure.
- Item 6(a) requires both the `gamelib.log` census line and the `humble_store/config.json` filesystem proof.
- The `## Structural Reachability Review` precedes `## The six items`, names all five tests, records the pairwise pass with counts and the scoping reduction, and lists every change the review caused.
- All ten REQ-34.4.2 IDs are accounted for; REQ-34.4.2-04/-05/-09 remain `[ ]` and exactly seven boxes remain `[x]`.
- `34.4.2-PLATFORM-SCOPE.md` §5 gains a tenth update; the nine earlier updates survive.
- All four prior gate documents are byte-unchanged.
- `git diff --stat -- src src-tauri/src` is EMPTY.
- `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing at plan end; `tauri:dev` was never launched.
</verification>

<success_criteria>
A six-item blocking contract exists whose PASS conditions were fixed before the outcome was known,
whose every required literal names a sink the mandated capture actually reads, whose evidence from
both sinks survives every relaunch and is separable per launch, whose item 6(b) is finally reachable
independently of item 6(a), and every one of whose items and requirement PAIRS has been proven
askable of the surface it names — so the next live run spends its evening measuring behaviour instead
of discovering, for a sixth time, that the contract asked for something it could never get.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-19-SUMMARY.md` when done.
Record: the exact Evidence-capture command block as authored, verbatim, including the delimiter text
and the resolved `gamelib.log` filename, since plan 34.4.2-20's runner must execute it literally; the
full Structural Reachability Review outcome including every row marked IMPOSSIBLE, every flagged
pair, and what was done about each (and, if a category found nothing, exactly what was checked); the
count of pairs considered and the scoping reduction applied; the REQ-to-item coverage mapping; and
the log literals re-grepped against current source with the line numbers actually observed. State
explicitly that this plan ran NO gate item, started NO harness, never launched `tauri:dev`, left every
result field null, ticked no requirement box, changed no source — and that the phase remains NOT
CLOSED until plan 34.4.2-20 records a measured verdict.
</output>
