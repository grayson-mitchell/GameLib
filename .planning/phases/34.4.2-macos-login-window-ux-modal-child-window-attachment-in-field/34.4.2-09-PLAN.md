---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 09
type: execute
wave: 3
depends_on: ["34.4.2-08"]
gap_closure: true
files_modified:
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REQ-34.4.2-05, REQ-34.4.2-08, REQ-34.4.2-09]

must_haves:
  truths:
    - "A rewritten six-item blocking gate contract exists with verdict null, written before any live work, by a plan forbidden from running it"
    - "Items 1 and 2 are stated in SHEET semantics, not reused child-window wording, and item 1 carries an explicit post-minimize/restore interactivity sub-check"
    - "Items 3-6 carry over intact so the four threats that went unmeasured on the failed run are still reachable"
    - "The platform-scope record and threat roll-up describe the sheet design, not the retired child-window design"
    - "No requirement row claims live proof that no gate has produced"
  artifacts:
    - path: ".planning/phases/34.4.2-.../34.4.2-LIVE-GATE-RERUN.md"
      provides: "The rewritten blocking gate contract"
      contains: "verdict: null"
    - path: ".planning/phases/34.4.2-.../34.4.2-PLATFORM-SCOPE.md"
      provides: "Per-symbol platform table and threat roll-up updated for sheet presentation"
      contains: "T-34.4.2-33"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.4.2-04/-05/-06/-09 checkbox state matching actual evidence"
      contains: "REQ-34.4.2-09"
  key_links:
    - from: "34.4.2-LIVE-GATE-RERUN.md item 2"
      to: "plan 34.4.2-08's two dismissal routes"
      via: "one required sub-check per route plus a record-only row for Cmd+W"
      pattern: "cancel strip|bare Esc"
    - from: "34.4.2-PLATFORM-SCOPE.md §5"
      to: "each gate item"
      via: "threat-to-item discharge mapping"
      pattern: "T-34.4.2-33"
---

<objective>
Write the rewritten blocking live-gate contract for the sheet design, and bring the phase's
platform-scope record, threat roll-up and requirement rows into agreement with what plans 07 and 08
actually built.

Purpose: the failed run's contract (`34.4.2-LIVE-GATE.md`) is written entirely in child-window
terms — item 1 measures `addChildWindow:ordered:` z-order behaviour and item 2 tests a titlebar
close button that a sheet does not have. Reusing it would test a mechanism that no longer exists and
would silently skip the one observation that matters most: whether a sheet stays INTERACTIVE through
the minimize/restore cycle that broke the child window (F-34.4.2-01). Items 3-6 were never reached
on the failed run and must carry over intact, unchanged in substance, so the four threats they
discharge are still reachable.

**This plan is FORBIDDEN from running any item in the contract it writes.** Writing the contract and
running it are separate plans by design — the same author/runner separation plans 05 and 06 used,
which is what made the failed run's result trustworthy rather than back-filled from expectation
(T-34.4.2-25). `verdict`, `run_date` and `items_passed` stay `null`; plan 34.4.2-10 is their only
writer.

Output: `34.4.2-LIVE-GATE-RERUN.md` (verdict null), an updated platform-scope/threat record, and
requirement rows that claim only what evidence exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-07-SUMMARY.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-08-SUMMARY.md
@.claude/skills/spike-findings-gamelib/references/oauth-login-test-harness.md
@.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the rewritten six-item blocking gate contract with verdict null</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE.md` in full — the failed contract. Its structure
      (frontmatter fields, Preconditions with per-precondition satisfier attribution, Items with
      What-to-do / PASS condition / Log lines / blank Observed / blank Verdict, ITEM VERDICT SUMMARY
      table, "What a PASS does and does not close", the threat-to-item table) is the template to
      follow; its items 1 and 2 are the parts being rewritten and its items 3-6 are the parts being
      carried over.
    - `.planning/phases/.../34.4.2-07-SUMMARY.md` and `34.4.2-08-SUMMARY.md` — the exact log-line
      literals, symbol names, and DECLARED UNVERIFIED notes the contract must cover
    - `.claude/skills/spike-findings-gamelib/references/oauth-login-test-harness.md` — the DummyStore
      harness start command, port 17940, and the `/events` oracle
    - `.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md` § 2 (sheet
      behaviour) and § 4's operator instructions (real-vs-synthesized right-click comparison, the
      `screencapture -R` region-grab rule, the `hit_id`/`hit_tag`/`hit_type` requirement, and the
      expected AutoFill-panel UX cost that must not be misfiled as a defect)
  </read_first>
  <action>
    Create `34.4.2-LIVE-GATE-RERUN.md` with frontmatter `plan: 09`, `type: live-gate`,
    `status: pending`, `blocking: true`, `verdict: null`, `run_date: null`, `items_total: 6`,
    `items_passed: null`, and a `notes:` block stating that this contract was written before any live
    work by a plan forbidden from running it, and that plan 34.4.2-10 is the only writer of results.

    **Items 1 and 2 — REWRITTEN for sheet semantics.**

    Item 1 — Sheet presentation and survival of the minimize/restore cycle (REQ-34.4.2-01/-02).
    With a Humble/GOG/Amazon login sheet presented, perform in order:
    (a) click the main window; (b) drag the main window to a new screen position; (c) minimize the
    main window to the Dock; (d) restore it from the Dock; (e) **immediately after the restore, type
    into the password field and then activate the cancel strip.**
    PASS conditions: (a) the sheet stays in front and the main window does not come over it — note
    explicitly that a sheet BLOCKS its parent, so the main window declining clicks is correct sheet
    semantics and is NOT a failure; (b) the sheet moves with the parent; (c) the sheet follows the
    parent into the Dock; (d) the sheet is visible and in front on restore; (e) **typed characters
    appear in the password field AND the cancel strip responds.** Sub-check (e) is the whole reason
    this gate exists — it is F-34.4.2-01 turned into a PASS condition. PASS = all five.
    Log line to capture, verbatim shape:
    `[shell] humble_login_open: presentation requested visible=true width=900 height=700 center=true focus_once=true persistent_pin=false light_theme_requested=true sheet_presented=true`
    State explicitly that the retired line
    `[shell] login-window: parent deminiaturized, re-raising ... attached child window(s)` MUST NOT
    appear, and that its absence is a PASS signal, not a missing observation — plan 07 Task 2 removed
    the mechanism deliberately.

    Item 2 — Dismissability under sheet semantics (REQ-34.4.2-03).
    Open a fresh login sheet per route and record each independently. State up front, in the item
    body, that the failed gate's item 2 wording ("close it via its own close button") is NOT reusable:
    a sheet renders no titlebar close button, so the routes that exist for a sheet are different ones
    and are enumerated here rather than inherited.
    - Route (a) **cancel strip click — REQUIRED PASS.** The sheet closes and the log carries
      `[shell] login-sheet: cancel requested for '{label}' via strip` followed by
      `status=cancelled reason=window-closed`.
    - Route (b) **bare Esc — REQUIRED PASS.** Same two lines with `via esc`.
    - Route (c) **Cmd+W — RECORD ONLY.** Whether AppKit delivers Cmd+W to a sheet is unknown to this
      project. Record what happens. A negative here is a recorded fact, not a FAIL.
    - Route (d) **titlebar close button — NOT A ROUTE.** Record its absence as EXPECTED. Do not score it.
    - **After each successful dismissal, confirm the MAIN WINDOW is interactive again** (click a
      sidebar item). A sheet that ends without releasing its parent is the lock-out this whole plan
      set exists to prevent (T-34.4.2-33).
    PASS = routes (a) and (b) both close the sheet with both log lines present, and the main window is
    interactive after each.

    **Items 3, 4, 5 and 6 — CARRIED OVER from `34.4.2-LIVE-GATE.md` substantially unchanged**, since
    none was ever attempted. Copy each item's What-to-do, PASS condition, operator instructions and
    log lines across faithfully, with exactly these adjustments:
    - Item 3 (glyph + AutoFill menu, REQ-34.4.2-04/05): keep both sub-items (DummyStore form, real
      Humble form), keep all four operator instructions verbatim (compare against a real right-click
      before concluding anything from a negative; `screencapture -x -o -R<x,y,w,h>` region grab, never
      `-l<windowID>`; record `hit_id`/`hit_tag`/`hit_type` and FAIL on a mismatch regardless of what
      menu appeared; the AutoFill panel's non-auto-matching, input-rejecting search box is expected UX
      cost, not a defect). Update the refusal-line text to the plan-07 wording
      (`is not a presented login sheet`). Add one sentence noting the glyph now sits inside a sheet
      rather than a child window, and that a menu failing to appear on a sheet-hosted webview is a
      real finding worth recording precisely because it is a new host context.
    - Item 4 (Cmd+V paste, REQ-34.4.2-06): carry over both surfaces and the Edit > Paste divergence
      diagnostic. **Add one sub-check:** confirm Esc does not fire while typing normal characters and
      that Cmd+V is not swallowed by the new Esc monitor.
    - Item 5 (kill-switch efficacy, T-34.4.2-15 residual): carry over both arms and the
      must-differ log-line requirement. **Add one mandatory sub-check to arm 2:** with
      `GAMELIB_AUTOFILL_GLYPH=0`, the CANCEL STRIP must still be present and must still dismiss the
      sheet. If the kill switch removes the only visible exit, the item FAILS regardless of the glyph
      result — that is the lock-out regression plan 08's assertion 1 exists to prevent, verified live.
    - Item 6 (hidden-window non-interference + Epic untouched, REQ-34.4.2-04/10, T-34.4.2-17/-32):
      carry over both sub-items. Update 6(b)'s absence list for the new design: no key glyph, no
      cancel strip, no autofill sentinel for Epic's label, and no sheet-presentation line for it. Keep
      the "a parked pre-auth 403 does NOT fail this item" rule verbatim. **Add to 6(a):** the hidden
      reveal window must receive no cancel strip either.

    **Preconditions** — carry the failed contract's six across with their per-precondition satisfier
    attribution (operator-at-gate-time vs already-evidenced), including precondition 4's sticky-jar
    trap verbatim: the login-window cookie jar persists across app restarts, so the operator must run
    the logout preamble (navigate a Tauri-managed login window to
    `http://127.0.0.1:17940/logout`) and take the `/events` baseline AFTER it — skipping this silently
    skips the login form and any "the form was served" assertion fails for the wrong reason. Leave
    each precondition's satisfaction status blank for plan 10's preflight to fill; this plan runs no
    preflight and starts no harness.

    Add a **"What changed since the failed gate"** section naming: the presentation switch, the
    retired observer and its retired log line, the two new dismissal routes, and the fact that items
    3-6 are carry-overs whose four threats (T-34.4.2-17, -21, -22, -15 residual) remain exactly as
    unmeasured as before.

    Add a **"What a PASS does and does not close"** section, carried over and updated: a PASS covers
    macOS only; proves nothing about Windows or Linux (and note the new asymmetry — the cancel strip
    IS macOS-gated while the autofill glyph still is not, per plan 08's SUMMARY); and proves nothing
    about Epic's login UX, item 6(b) being an absence check only.

    Add the **threat-to-item discharge table** covering T-34.4.2-05 (re-scoped), -07, -33, -34, -35,
    -17, -21, -22, -15 residual, -32.

    Leave every `Observed:` and `Verdict:` field blank, and leave the ITEM VERDICT SUMMARY table
    present with empty verdict cells. Write nothing into the frontmatter's result fields.
  </action>
  <verify>
    <automated>test -f .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md && grep -c 'verdict: null' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md && grep -c '^### Item' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists; frontmatter contains `verdict: null`, `run_date: null`, `items_passed: null`, `items_total: 6`, `blocking: true`.
    - `grep -c '^### Item'` returns exactly 6.
    - Item 1 contains the literal `sheet_presented=true` and the literal sub-check requiring typed characters to appear in the password field after the Dock restore.
    - Item 1 states that `parent deminiaturized, re-raising` must NOT appear.
    - Item 2 contains all four routes with the literal labels `REQUIRED PASS` (twice), `RECORD ONLY` (once) and `NOT A ROUTE` (once), and contains the main-window-interactive-after-dismissal check.
    - Item 2 explicitly states why the failed gate's item-2 wording is not reusable for a sheet.
    - Item 5 contains the mandatory arm-2 sub-check that the cancel strip survives `GAMELIB_AUTOFILL_GLYPH=0`.
    - `grep -c '17940' ` returns at least 2 (harness precondition + logout preamble) and the sticky-jar trap sentence is present.
    - `grep -c 'screencapture' ` returns at least 1 and the file contains `-R` region-grab guidance, not `-l`.
    - Every `Observed:` line in the file is followed by nothing but whitespace or a blank placeholder — `grep -A1 'Observed:'` shows no filled content.
    - The threat-to-item table lists T-34.4.2-33, -34 and -35.
    - No command in this task started the DummyStore harness or launched `tauri:dev`.
  </acceptance_criteria>
  <done>A six-item blocking contract exists with fixed PASS conditions and no results, stating sheet semantics for items 1-2 and carrying items 3-6 forward intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update the platform-scope record and threat roll-up for the sheet design</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` in full (§1's per-symbol table, the
      not-platform-gated finding, §2's off-macOS unverified statement, §3 EPIC-DEFERRED, §4's
      Electron-unchanged evidence, §5's threat roll-up and its post-gate update table, §6 package
      legitimacy)
    - `.planning/phases/.../34.4.2-07-SUMMARY.md` and `34.4.2-08-SUMMARY.md` (the symbols added and
      removed, their cfg gates, and plan 08's note that the cancel strip IS macOS-gated while the
      glyph is not)
  </read_first>
  <action>
    Amend `34.4.2-PLATFORM-SCOPE.md` in place — never rewrite it wholesale, and never delete the
    historical record of the child-window design or the failed gate.

    §1 per-symbol table: remove the rows for `attach_login_window_as_child`,
    `detach_login_window_from_parent`, `ATTACHED_LOGIN_CHILDREN` and the deminiaturize re-raise
    observer, replacing them with a short RETIRED subsection listing each removed symbol, the plan
    that removed it (07), and the binding decision that required it — the table is a live inventory,
    the retirement note is the history. Add rows for `present_login_window_as_sheet`,
    `dismiss_login_window_sheet`, `PRESENTED_LOGIN_SHEETS`, `PRESENTED_LOGIN_SHEETS_CAP`,
    `LOGIN_CANCEL_EXFIL_PATH`, `is_login_cancel_request`, `login_cancel_strip_script`,
    `request_login_sheet_cancel`, and the Esc local monitor, each with its source-verified cfg gate
    read from the current `src-tauri/src/main.rs` (not assumed), its macOS behaviour, and its
    Windows/Linux behaviour.

    Update the "the autofill glyph is injected on every platform" finding: it still stands for the
    glyph, and the new cancel strip is the counter-example — plan 08 gated it `macos` deliberately, so
    §1 now documents both a platform-gated and a non-platform-gated injection in the same arm. State
    that the glyph's un-gated injection remains an open, unfixed UX wart for whoever next touches
    Windows/Linux, unchanged by this gap cycle.

    §5 threat roll-up: add a new post-gap-cycle table. It must record:
    - T-34.4.2-05 **re-scoped and reset to OPEN-pending-gate** against the NEW mechanism. State
      plainly that its previous disposition was live-CONFIRMED-broken for child windows and that a
      new mechanism does not inherit a fix — it starts unproven.
    - T-34.4.2-07 same treatment, discharged by the rewritten item 2.
    - **T-34.4.2-33 (NEW)**, DoS / hard lock-out behind a parent-blocking sheet: mitigated by plan
      08's two routes, OPEN-pending-gate on rerun item 2.
    - **T-34.4.2-34 (NEW)**, spoofing via a page-forged cancel: accept, bounded — no payload, the only
      consequence is closing the user's own login window.
    - **T-34.4.2-35 (NEW)**, information disclosure via the app-wide key monitor: mitigate, CLOSED by
      source (`keyCode`/modifiers only, never `characters`, never logged), with the permanent negative
      test named.
    - T-34.4.2-15 residual, -17, -21, -22: STILL OPEN-pending-gate, reason NOT ATTEMPTED on the failed
      run and still unmeasured — no new evidence in either direction.
    - T-34.4.2-32: unchanged, CLOSED by source, symbol array extended twice this cycle.
    - T-34.4.2-SC: CLOSED, no install across plans 07-09, both lockfiles byte-identical.
    Retain the pre-existing tables above the new one; this is an append-and-supersede edit.

    §4: append a short subsection re-running the Electron-unchanged evidence for this gap cycle —
    the scoped `git diff --stat` over `src/backend src/frontend src/common` for plans 07-08's commits
    should show test files only, and both lockfile `git diff --exit-code` checks should exit 0. Quote
    the real measured output; do not assert a result you did not run.
  </action>
  <verify>
    <automated>grep -c 'T-34.4.2-33' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md && grep -c 'ATTACHED_LOGIN_CHILDREN' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'T-34.4.2-33'` returns at least 1; same for `T-34.4.2-34` and `T-34.4.2-35`.
    - `grep -c 'present_login_window_as_sheet'` returns at least 1 and the row states a source-verified cfg gate with a line number read from the current `main.rs`.
    - `ATTACHED_LOGIN_CHILDREN` still appears in the document (in the RETIRED subsection and the historical tables) — the history is preserved, not erased.
    - The new §5 table states that T-34.4.2-05 and T-34.4.2-07 are reset to OPEN-pending-gate against the new mechanism, with the "a new mechanism does not inherit a fix" reasoning written out.
    - §4's new subsection quotes real command output captured in this session; no fabricated figures.
    - The pre-existing §5 tables and the post-failed-gate update table are both still present.
  </acceptance_criteria>
  <done>The platform-scope record describes the sheet design as the live inventory, records the retired child-window symbols as history, and the threat roll-up carries the three new threats plus the honest reset of the two that were live-confirmed broken.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the premature requirement checkmarks and close the carried-forward deferred entry</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 731-760 (the REQ-34.4.2-01..10 rows and the phase's
      requirement-count note) — including the rows plan 07 already restated, so this task does not
      re-edit them
    - `.planning/phases/.../deferred-items.md` (the Plan 05 entry flagging REQ-34.4.2-01/02/03/06's
      premature `[x]` marks, plus the `helperProcess.test.ts` flake and `seam-parity-sweep.py`
      staleness entries)
    - `.planning/phases/.../34.4.2-06-SUMMARY.md` § "Scope note" (the explicit hand-off of this
      correction to the gap cycle)
  </read_first>
  <action>
    Make every REQ-34.4.2 checkbox state match the evidence that actually exists.

    In `.planning/REQUIREMENTS.md`:
    - **REQ-34.4.2-04** and **REQ-34.4.2-05**: these are code-complete (plans 03/04) but their live
      behaviour is UNMEASURED — gate items 3 and 5 were NOT ATTEMPTED. Set both to `[ ]` and append a
      one-line note naming `34.4.2-LIVE-GATE-RERUN.md` item 3 (and item 5 for the kill switch) as the
      box's precondition.
    - **REQ-34.4.2-06**: set to `[ ]`. Its own row already names "plan 06's live gate" as the
      precondition, and that gate returned NOT ATTEMPTED for item 4. Update the row to name the rerun
      contract's item 4 instead.
    - **REQ-34.4.2-09**: keep `[ ]`; update its text to name `34.4.2-LIVE-GATE-RERUN.md` (plan 10) as
      the gate that satisfies it, noting the first gate ran and FAILED 0/6.
    - **REQ-34.4.2-07, -08, -10**: leave as they are unless plans 07/08's SUMMARYs contradict them;
      if plan 08's macOS-gating of the cancel strip strengthens -08, note it in that row without
      changing the checkbox.
    - Do **not** touch REQ-34.4.2-01/-02/-03 — plan 07 already restated and unchecked them. Verify
      that is true and record the verification in the SUMMARY rather than re-editing.
    - Update the phase's requirement-count/provenance note at the foot of the section to mention the
      gap cycle and the binding decision, without renumbering or deleting any ID.

    In the phase's `deferred-items.md`: mark the Plan 05 entry about the premature `[x]` marks as
    CLOSED by this task, with the date and the specific rows corrected. Leave the
    `helperProcess.test.ts` full-suite-only flake entry and the `seam-parity-sweep.py`
    `SITE_PROFILES` staleness entry OPEN — both are out of this phase's scope and neither was fixed
    here. Add a new deferred entry recording that the autofill glyph's un-gated cross-platform
    injection (`34.4.2-PLATFORM-SCOPE.md` § 1's finding) remains unfixed, now with a
    macOS-gated sibling for contrast.
  </action>
  <verify>
    <automated>grep -n 'REQ-34.4.2-0[4-9]\|REQ-34.4.2-10' .planning/REQUIREMENTS.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - REQ-34.4.2-04, -05, -06 and -09 rows all begin with `- [ ]` (unchecked).
    - Each of those four rows names a specific item in `34.4.2-LIVE-GATE-RERUN.md` as its precondition.
    - REQ-34.4.2-01, -02, -03 rows are byte-identical to plan 07's output (confirm with `git diff` scoped to those lines showing no change from this task).
    - No REQ ID was renumbered or deleted — `grep -c 'REQ-34.4.2-' .planning/REQUIREMENTS.md` returns the same count as before this task for the ten IDs.
    - The `deferred-items.md` Plan 05 premature-`[x]` entry is marked CLOSED with today's date; the `helperProcess.test.ts` and `seam-parity-sweep.py` entries remain OPEN.
    - A new deferred entry exists for the glyph's un-gated cross-platform injection.
  </acceptance_criteria>
  <done>No requirement row claims live proof that no gate produced, the carried-forward correction from plan 06 is closed, and the still-open deferrals stay open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/author → gate result | A contract author who can also record results can back-fill an outcome from expectation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-25 | Repudiation (a result back-filled from expectation) | `34.4.2-LIVE-GATE-RERUN.md` | mitigate — CLOSED by process | PASS conditions are fixed in this plan, before the outcome is known; `verdict`/`run_date`/`items_passed` stay `null`; this plan is forbidden from running any item or starting the harness, and an acceptance criterion asserts every `Observed:` field is empty. Plan 34.4.2-10 is the only writer of results. |
| T-34.4.2-26 | Repudiation (a green suite mistaken for parity) | contract preamble | mitigate — CLOSED by process | The contract restates that across three 34.4.1 gate runs a fully green suite coexisted with live blocking defects, and that this phase's own 3722/3722 suite coexisted with F-34.4.2-01. Suite figures are recorded as context, never as evidence of behaviour. |
| T-34.4.2-33/-34/-35 | (carried from plan 08) | threat roll-up | document — mapped to items | Each is entered in §5 with its discharging gate item named, so no new threat is introduced without a measurement route. |
| T-34.4.2-SC | Tampering (dependency surface) | none | mitigate — CLOSED | This plan writes only planning documents; `git diff --stat -- src src-tauri/src` must be empty for it. |
</threat_model>

<verification>
- `34.4.2-LIVE-GATE-RERUN.md` exists with six items, `verdict: null`, and no filled `Observed:`/`Verdict:` fields.
- `git diff --stat -- src src-tauri/src` is EMPTY for this plan's commits — it touches no code.
- No process was started on port 17940 by this plan (`lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing at plan end, unless something outside this plan owns it).
- `34.4.2-PLATFORM-SCOPE.md` retains all pre-existing tables and gains the post-gap-cycle roll-up.
- All ten REQ-34.4.2 IDs still present, none renumbered.
</verification>

<success_criteria>
A blocking, fixed-before-the-fact contract exists for the sheet design covering all six concerns;
items 1-2 are stated in sheet terms with the interactivity sub-check that the failed run's defect
demands; items 3-6 carry forward intact; the phase's scope, threat and requirement records describe
what was actually built and claim no unearned proof.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-09-SUMMARY.md` when done.
State explicitly that this plan ran NO gate item, started NO harness, and left every result field
null — and that the phase remains NOT CLOSED until plan 34.4.2-10 records a measured verdict.
</output>
