---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 13
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src-tauri/src/main.rs
  - src-tauri/Cargo.toml
  - src/backend/__tests__/tauriShellSource.test.ts
  - src/backend/__tests__/cargoFeatures.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
autonomous: true
requirements: [REQ-34.4.2-04, REQ-34.4.2-05, REQ-34.4.2-08, REQ-34.4.2-10]

must_haves:
  truths:
    - "No key glyph is injected into any password field on any Tauri-managed login surface, on any platform"
    - "No synthesized NSEvent is ever constructed or posted by this application"
    - "The autofill sentinel path no longer exists as a navigation channel; only the cancel/reveal/clear-storage sentinels remain"
    - "The GAMELIB_AUTOFILL_GLYPH environment variable has no reader anywhere in the source"
    - "A source guard fails if any removed symbol is ever reintroduced"
    - "REQ-34.4.2-04 and REQ-34.4.2-05 state the shipped capability truthfully: no in-field autofill affordance, Cmd+V / Edit > Paste is the sole credential-entry route"
    - "The cancel strip, the Esc monitor and sheet presentation are FUNCTIONALLY untouched: their code, their generated script bodies and their observable behaviour are byte-identical, and the only edits anywhere near them are doc comments whose cross-references to deleted symbols would otherwise dangle"
    - "Every design rationale those comments carried survives the rewrite — in particular that the cancel strip has NO kill switch by design (T-34.4.2-15/-33), which is now permanent by construction rather than by convention"
    - "The pristine Epic login window is byte-untouched"
  artifacts:
    - path: "src-tauri/src/main.rs"
      provides: "The autofill mechanism deleted in full, with no orphaned helper and no dead_code warning"
      contains: "login_cancel_strip_script"
    - path: "src/backend/__tests__/tauriShellSource.test.ts"
      provides: "PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS absence guard plus the relocated T-34.4.2-20 credential-selector negatives"
      contains: "PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.4.2-04/-05 rewritten to the corrected scope, with the removal itself as the verifiable claim"
      contains: "REQ-34.4.2-05"
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md"
      provides: "Per-symbol RETIRED rows for the deleted mechanism and a sixth threat roll-up update retiring T-34.4.2-15 as moot"
      contains: "RETIRED (Plan 13"
  key_links:
    - from: "src/backend/__tests__/tauriShellSource.test.ts"
      to: "src-tauri/src/main.rs comment-stripped source"
      via: "PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS absence loop"
      pattern: "PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS"
    - from: "humble_login_open's .on_navigation( closure"
      to: "is_login_cancel_request"
      via: "the sole surviving sentinel arm"
      pattern: "is_login_cancel_request\\("
---

<objective>
Execute binding operator decision **D-A**: the in-field autofill glyph is DROPPED and its code is
DELETED — not disabled, not kept behind the kill switch, not re-attempted with a different synthesis
approach. Cmd+V (plus Edit ▸ Paste) becomes the sole credential-entry route for Tauri-managed login
windows. Make the deletion machine-enforceable, and rewrite REQ-34.4.2-04/-05 so the shipped
capability is stated truthfully.

Purpose: `34.4.2-LIVE-GATE-RERUN-2.md` item 3 measured the mechanism on real macOS hardware and it
FAILED at its terminal step. The synthesized right-click fires correctly (WR-07 branch 5/5), targets
the correct element (`hit_tag=Some("INPUT")`, `hit_type=Some("password")`) and pops the real system
menu with `AutoFill ›` present — **but the field never fills**, while an identical REAL right-click
in the same sheet, same field, same seeded entry DOES fill. That discriminator rules out the sheet
context, Humble's page, and the platform. **F-34.4.2-09**: this falsifies spike 022's own
Recommendation #4, the premise the whole affordance was built on — the spike's evidence only ever
showed the menu APPEARING; the last mile (does selecting an entry write to the field?) was never
measured until this gate measured it.

**F-34.4.2-08 (`String.fromCharCode(128273)` truncating U+1F511 to a PUA tofu box at
`src-tauri/src/main.rs:1499`) is MOOT and MUST NOT be fixed** — deleting the code supersedes it. Do
not apply `String.fromCodePoint`.

**Explicitly NOT in scope, and not to be re-litigated:** the cancel strip (D-B — separate mechanism,
plan 08, REQ-34.4.2-03, deliberately no kill switch, live-PASSED), sheet presentation and the 250 ms
`SHEET_PRESENT_WKWEBVIEW_WARMUP_DELAY` deferral (D-C — permanent; child-window attachment is off the
table forever), and the pristine Epic surface (D-D — byte-untouched, REQ-34.4.2-10).

Output: a smaller `main.rs` with the mechanism gone, a permanent absence guard, corrected
requirement text, and a propagated threat/platform record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-VERIFICATION.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md

<orientation>
`graphify-out/graph.json` exists in this repo. Before reading raw source, orient with
`graphify explain "autofill_glyph_script"`, `graphify explain "post_autofill_right_click"` and
`graphify path "parse_autofill_request()" "post_autofill_right_click()"`. The graph already confirms
`autofill_glyph_script()` has exactly four inbound callers (three cargo tests plus
`dispatch_rust_channel()`), which is the shape this deletion must reduce to zero.
</orientation>

<interfaces>
<!-- Every line number below was read from the current tree during this planning session. Re-verify
     each with grep before editing — a preceding deletion shifts every later number. -->

**Production symbols to DELETE from `src-tauri/src/main.rs`:**

- `const AUTOFILL_EXFIL_PATH: &str = "/autofill-request";`                (:1322)
- `struct AutofillRequest`                                                (:1333)
- `fn parse_autofill_request(url: &tauri::Url) -> Option<AutofillRequest>` (:1365)
- `fn autofill_glyph_script(exfil_host: &str) -> String`                  (:1448, glyph tofu at :1499)
- `fn css_rect_center_in_view(...)`                                       (:1738, macOS-gated)
- `fn clamp_point_to_view_bounds(...)`                                    (:1751, macOS-gated)
- `const AUTOFILL_POST_DEBOUNCE: Duration`                                 (:1771)
- `const AUTOFILL_POST_DEBOUNCE_MAP_CAP: usize`                            (:1779)
- `static LAST_AUTOFILL_POST: Mutex<Option<HashMap<String, Instant>>>`     (:1785)
- `fn synth_autofill_mouse_event(...)`                                     (:1795, macOS-gated)
- `fn post_autofill_right_click(app, label, req)`                          (:1851, macOS-gated)
- `fn login_window_wk_webview(app, label) -> Option<usize>`                (:2682, macOS-gated) —
  **its ONLY caller is `post_autofill_right_click` (:1892)**; its own doc comment at :2680 says so
  verbatim ("Caller: `post_autofill_right_click` (Phase 34.4.2 Plan 04, below)"). It becomes dead
  code the moment the poster goes, so it goes with it.

**Production call sites to DELETE:**

- The `GAMELIB_AUTOFILL_GLYPH` kill-switch if/else, INCLUDING the whole `if`/`else` statement and its
  ~20-line justification comment                                          (:3905-3927)
- `let app_for_autofill = app.clone();` and its `#[cfg_attr(...)]` line    (:4005-4006)
- The `if let Some(req) = parse_autofill_request(&url) { ... return false; }` arm of the
  `.on_navigation(` closure, both cfg arms                                (:4076-4084)

**Production code that MUST SURVIVE BYTE-IDENTICAL — scoped to FUNCTIONAL CODE, generated script
bodies and observable behaviour. Doc comments in these regions are covered by the separate
comment-rewrite carve-out immediately below, which is REQUIRED, not optional:**

- `login_cancel_strip_script` (:1603), `is_login_cancel_request` (:1565), `LOGIN_CANCEL_EXFIL_PATH`
  (:1554), `request_login_sheet_cancel` (:1684), the Esc local monitor (:5027 region).
- The `#[cfg(target_os = "macos")]` cancel-strip injection block           (:3938-3945)
- The `if is_login_cancel_request(&url) { ... return false; }` sentinel arm (:4067-4075) — after the
  autofill arm is removed this is the ONLY sentinel arm in the closure, and the closure still ends
  `true`.
- `let nav_sentinel_label = label.clone();` (**:3812**, doc comment **:3808-3811**) — still captured
  by the cancel arm. **Its doc comment names the autofill sentinel; that is rewrite site C7 below.
  Rewrite the comment, keep the binding.** (An earlier draft of this plan cited :3786-3789 for this;
  the verified numbers are :3808-3812.)
- `present_login_window_as_sheet` (:2818), `dismiss_login_window_sheet` (:3035),
  `register_presented_login_sheet` (:2985), `PRESENTED_LOGIN_SHEETS` (:2672),
  `SHEET_PRESENT_WKWEBVIEW_WARMUP_DELAY` (:2776), `LOGIN_SHEET_PRESENT_WATCHDOG_TIMEOUT` (:2727).
- `REVEAL_EXFIL_HOST` (still used by the strip, `/reveal` and `/clear-storage`).
- `open_pristine_epic_login_window` and `EpicPristineNavDelegate` — **byte-frozen with NO carve-out:
  not their code, not their comments, not one line. REQ-34.4.2-10, D-D.** None of the comment
  rewrites below goes anywhere near either region.
- `with_webview` at `humble_login_clear_cookies` (referenced by the comment at :2053) — a SEPARATE
  call site from `login_window_wk_webview`; do not touch it.

**REQUIRED COMMENT REWRITES — surviving doc comments that cross-reference deleted symbols.**

These are the sites where "delete the mechanism" and "leave the strip alone" would otherwise
contradict each other. Each is a doc comment attached to code that SURVIVES, whose prose names a
symbol that does NOT. Leaving them is a dangling reference; blind-deleting the sentences would
destroy load-bearing design rationale. **Rewrite each so it describes the post-deletion state while
preserving the reasoning it carries.** Line numbers are pre-deletion and WILL drift — after finishing
the deletions, re-discover the full set with the RECONCILIATION GREP below and reconcile against this
list.

**RECONCILIATION GREP — use this exact form, and understand why it is shaped this way:**

`grep -niE 'autofill|login_window_wk_webview|css_rect_center_in_view|clamp_point_to_view_bounds' src-tauri/src/main.rs`

**A symbol-name-scoped grep is NOT sufficient and must not be substituted for this one.** The first
draft of this plan enumerated only the thirteen top-level symbol names, and that list is blind to
three real classes of dangling reference this deletion creates: **local bindings** named in a sibling
doc comment (`app_for_autofill`, site C6 — the surviving `app_for_cancel` clone's comment cites it
twice), **prose descriptions** of the mechanism that name no symbol at all (`autofill sentinel` at
C7, `autofill glyph` at C4, `the reveal/clear-storage/autofill channels` at C1), and the
`/autofill-request` path literal. `graphify` cannot help here either — it models top-level symbols,
not local bindings or prose, so the graph reports a clean neighbourhood while these survive. The
case-insensitive `autofill` stem catches every one of them and subsumes nine of the thirteen symbol
names; the three explicitly-listed helpers are the only deleted names that do not contain the stem.

**After the deletions and the C1-C7 rewrites, this grep must return NOTHING.** A residual hit is
either a missed deletion, an unrewritten comment, or an eighth site this plan did not anticipate —
record which in the SUMMARY.

| # | Site | Pre-deletion lines | Dangling reference | What the rewrite must preserve |
|---|---|---|---|---|
| C1 | `LOGIN_CANCEL_EXFIL_PATH`'s doc comment | :1548, :1552, :1556, :1558, :1563 | `AUTOFILL_EXFIL_PATH` (x2), `parse_autofill_request` (x2), plus the prose "the reveal/clear-storage/autofill channels" at :1548 which names no symbol and would survive any symbol-scoped grep | The sentinel-disjointness reasoning (T-34.4.2-13) and the "carries deliberately NO payload" contrast. After deletion THREE sentinels share `REVEAL_EXFIL_HOST` — `/login-cancel`, `/reveal`, `/clear-storage` — so state the contrast against those instead of against the deleted parser. |
| C2 | `login_cancel_strip_script`'s doc comment | :1572, **:1573**, :1591, :1621 | `autofill_glyph_script` (x3), `GAMELIB_AUTOFILL_GLYPH` (x1) | **The single most important rationale in this table.** :1572-1574 currently reads "the glyph is kill-switchable via `GAMELIB_AUTOFILL_GLYPH`, this strip is NOT and must never become so. An env var that removes the only visible exit from a parent-blocking sheet is a lock-out switch, not a safety switch (T-34.4.2-33/T-34.4.2-15)." **Keep that reasoning verbatim in substance** — restate it without naming the deleted symbol, and add that the contrast is now moot because no kill-switchable injected control exists at all: the strip's lack of a kill switch is permanent by construction. :1591 and :1621 cite the glyph script as a precedent for the discretion and the `concat!`-of-single-line-pieces discipline; re-attribute both to the strip's own convention. |
| C3 | `PRESENTED_LOGIN_SHEETS`'s doc comment | :2706 | `post_autofill_right_click` | The registry's PURPOSE. It currently says the list is "consulted by `post_autofill_right_click`'s authorization gate ... before posting a synthesized event to it". After deletion its consumers are `present_login_window_as_sheet`, `dismiss_login_window_sheet` and the Esc local monitor's membership check — name those. **Keep** the bounded-growth/dedup/always-removed-on-dismiss reasoning (T-34.4.2-08) untouched. Plan 14 adds a fourth consumer; do not pre-empt it. |
| C4 | The cancel-strip injection block's own preceding comment | :3932, **:3934** | "the autofill glyph above uses", `GAMELIB_AUTOFILL_GLYPH` | Same rationale as C2, at the call site: "Deliberately OUTSIDE and INDEPENDENT of the `GAMELIB_AUTOFILL_GLYPH` branch above: the strip gets NO kill switch of its own." **Preserve the T-34.4.2-15 lock-out-versus-safety-switch reasoning and the macOS-gating rationale** ("only macOS presents this window as a sheet, so only macOS needs an in-page exit"). Drop the now-false comparison to the glyph's cross-platform-injected-but-inert shape — `34.4.2-PLATFORM-SCOPE.md` §1's recorded wart is closed by this very deletion (Task 3 marks it MOOT), so the sentence pointing at it becomes wrong, not merely stale. **The comment changes; the `#[cfg(target_os = "macos")]` block beneath it (:3938-3945) stays byte-identical.** |
| C5 | The `.on_navigation(` closure's leading doc comment | :4053, :4060 | `post_autofill_right_click`, `parse_autofill_request` | Already covered by the action's own on_navigation instruction — listed here for completeness so the reconciliation grep returns nothing unexplained. |
| C6 | The SURVIVING `app_for_cancel` clone's doc comment | **:4010, :4011** | `app_for_autofill` (x2) — a LOCAL BINDING, invisible to any symbol-scoped grep | The comment reads "mirrors `app_for_autofill`'s own convention immediately above, a separate clone rather than reusing `app_for_autofill` so each sentinel arm's capture is independently readable." Its sibling is deleted at :4005-4006, so the comparison has nothing left to compare to. **Rewrite it to describe `app_for_cancel` on its own terms** — the sole surviving owned `AppHandle` clone this arm's head still needs, because `request_login_sheet_cancel` needs `&AppHandle` to dismiss the sheet and close the window and `.on_navigation(` takes a `move` closure. Keep the `#[cfg_attr(not(target_os = "macos"), allow(unused_variables))]` attribute and the `let app_for_cancel = app.clone();` binding byte-identical. |
| C7 | `nav_sentinel_label`'s doc comment | **:3808-3811** (the binding itself is at :3812) | the prose "the autofill sentinel's `.on_navigation(` closure below" at :3809 | Corrects a line-number error in this plan's own earlier draft, which cited :3786-3789. The comment explains why this label is cloned SEPARATELY from `event_label` rather than shared. **Keep that reasoning**; re-attribute it to the cancel sentinel, which after this deletion is the only sentinel arm the closure has. The binding stays. |

**Cargo tests to DELETE from `mod tests` in `src-tauri/src/main.rs`:**

- The `parse_autofill_request` / `autofill_glyph_script` block (~:6329-6470), including the
  `autofill_url` helper (:6336) and `valid_autofill_payload` (:6348).
- The `css_rect_center_in_view` / `clamp_point_to_view_bounds` block (~:6673-6750), including the
  `autofill_req` helper (:6680).

**Cargo test to RETARGET, not delete:**

- `login_cancel_request_rejects_the_autofill_sentinel_path_on_the_same_host` (:6531-6540) references
  `AUTOFILL_EXFIL_PATH`. It asserts T-34.4.2-13 (sibling-sentinel disjointness), which still matters.
  Rename it to `login_cancel_request_rejects_an_unrelated_path_on_the_same_host` and assert against a
  **neutral literal such as `/some-other-sentinel`** — do NOT inline `/autofill-request`, and do NOT
  resurrect the const. The invariant worth keeping is "an arbitrary non-cancel path on the same host
  is rejected", which a neutral literal proves just as well; reusing the dead path would leave an
  `autofill` stem in the file and defeat the reconciliation grep's zero-hit bar.
- `login_cancel_request_rejects_reveal_path` (:6543) and
  `login_cancel_request_rejects_clear_storage_path` (:6549) are unaffected.

**Cargo.toml:**

- `objc2-app-kit`'s `NSGraphicsContext` feature (`src-tauri/Cargo.toml:120` region) exists SOLELY for
  `NSEvent::mouseEventWithType_location_modifierFlags_timestamp_windowNumber_context_eventNumber_clickCount_pressure`
  inside `synth_autofill_mouse_event` — the comment block at :111-119 records the `E0599` that forced
  it and states wry requests the DIFFERENT `NSGraphics` feature, so wry does not union it in.
</interfaces>

<guard_constraints>
<!-- These already-passing guards constrain the deletion. Each is either deleted with its subject,
     retargeted, or relocated — none may be silently dropped. Line numbers re-verified this session. -->

From `src/backend/__tests__/tauriShellSource.test.ts` (assertions run on COMMENT-STRIPPED source):

- **`PHASE_34_4_2_NEW_SYMBOLS` (array at :708-745)** currently lists `login_window_wk_webview` (:710),
  `AUTOFILL_EXFIL_PATH` (:719), `parse_autofill_request` (:721), `autofill_glyph_script` (:722),
  `synth_autofill_mouse_event` (:726), `post_autofill_right_click` (:727), `LAST_AUTOFILL_POST`
  (:728), `AUTOFILL_POST_DEBOUNCE` (:729) plus the `AutofillRequest`/`css_rect_center_in_view`/
  `clamp_point_to_view_bounds` members. Those members must MOVE to the new removed-symbols array —
  a name that no longer exists cannot be referenced from Epic, so leaving them in the Epic guard is
  vacuous, while asserting their file-wide ABSENCE is strictly stronger.
- **Plan 01 Test 1 (:782) and Test 2 (:789)** slice on `fn login_window_wk_webview(`. Both die with
  the symbol. Test 1 uses `code.indexOf('fn login_window_wk_webview(', start)` as the END anchor for
  `login_window_ns_window`'s slice — **re-anchor it on whatever function now follows
  `login_window_ns_window`, do not delete Test 1.**
- **Plan 03 describe block (:998-1176)** — deleted entirely, EXCEPT the T-34.4.2-20 permanent negative
  at :1139/:1141 (`_showDigitalCredentialsPicker`, `_canUseCredentialStorage`).
- **Plan 04 describe block (:1185-1324)** — deleted entirely, EXCEPT the same T-34.4.2-20 negative at
  :1281/:1283.
- **T-34.4.2-20 RELOCATION IS MANDATORY.** The permanent negative banning the four credential-shaped
  WKWebView selectors is independent of the poster: it exists to stop a FUTURE session reaching for a
  private "open the Passwords panel" call. Relocate it into this plan's new describe block, covering
  all four selectors spike 022 enumerated, asserted against the whole comment-stripped file. Losing
  it would be the most dangerous side effect of this deletion.
- **Plan 08 Test 1 (:1434-1447)** asserts `login_cancel_strip_script(` is called exactly once, inside
  `if visible {`, **and is ABSENT from both branches of the `GAMELIB_AUTOFILL_GLYPH` statement**. Its
  helper (`:1358-1372`) slices on `armBody.indexOf('if std::env::var("GAMELIB_AUTOFILL_GLYPH")')`.
  Both the helper and the kill-switch clause of the assertion must go; the exactly-once and
  inside-`if visible` clauses must STAY. Record in the SUMMARY that the kill-switch-independence
  assertion is retired because the kill switch itself no longer exists — not because it stopped
  mattering.
- **Plan 08 Test 2 (:1449-1462)** asserts `is_login_cancel_request` appears in the `.on_navigation(`
  closure BEFORE `parse_autofill_request`. Retighten to: the closure contains `is_login_cancel_request`
  and contains NO `parse_autofill_request` — an ordering assertion over a deleted sibling is
  meaningless, but the "this closure parses exactly one sentinel" property is the thing worth keeping.
- **Phase 34.5 Plan 27 test at :653-664** (in the `F-34.5-G6-04` describe at :622, a DIFFERENT phase's
  guard) asserts `onNavBody).toContain('parse_autofill_request')`. **This is a cross-phase test — do
  not delete the describe block.** Retighten the one assertion to `is_login_cancel_request`, preserving
  its real purpose (the closure never drives the login-watch relay: no `push_login_window_event`, no
  `set_title`, no `current_origin`, no `login_event_value`). State in the SUMMARY that a Phase 34.5
  guard was edited and why.
- **Plan 11 describe block (:1554+)** — unaffected; `register_presented_login_sheet`, the
  `.makeKeyAndOrderFront(None)` exactly-once count and the `window.top`/`tabindex` strip assertions
  all survive. **The `.makeKeyAndOrderFront(None)` guard is the one to watch**: after the poster is
  deleted the call has ZERO call sites, so an exactly-once assertion becomes a FAILING assertion.
  Convert it to a zero-occurrence negative and say so explicitly in its test name.
- **Plan 01 Test 5 / Plan 02 Test 6** (`beginSheet_completionHandler` and `endSheet` each exactly
  once) are untouched by this deletion and must stay green.
- **Grep-gate hygiene (standing repo rule).** Every count assertion you add or change must count in a
  form your own new comments and test names cannot satisfy. Assert against COMMENT-STRIPPED source
  (`loadMainRsCode`), and where a token will legitimately appear in prose, count the call/definition
  form instead of the bare token.

From `src-tauri/src/main.rs`'s own `mod tests`:

- `login_cancel_strip_script_is_wrapped_in_a_single_top_level_try_catch` (:6498 region) asserts
  `"try {"` appears exactly once in the STRIP script. Unaffected — but note the identically-named
  `autofill_glyph_script_is_wrapped_in_a_single_top_level_try_catch` (:6498) belongs to the deleted
  script and goes with it. Read both names carefully before deleting either.

From `src/backend/__tests__/cargoFeatures.test.ts`:

- The test at :129-133 asserts `objc2-app-kit`'s feature list CONTAINS `NSGraphicsContext`. The test
  at :135-144 asserts the pre-existing features are all still present. Task 2 decides this one.
</guard_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the autofill mechanism from src-tauri/src/main.rs — production symbols, call sites, and its own cargo tests</name>
  <files>src-tauri/src/main.rs</files>
  <read_first>
    - `src-tauri/src/main.rs:1315-1545` — `AUTOFILL_EXFIL_PATH`, `AutofillRequest`,
      `parse_autofill_request` and `autofill_glyph_script` in full, so you delete the doc comments
      that belong to them and nothing that belongs to `REVEAL_EXFIL_HOST` or the reveal/clear
      sentinels sharing the same host.
    - `src-tauri/src/main.rs:1720-2025` — `css_rect_center_in_view`, `clamp_point_to_view_bounds`,
      `AUTOFILL_POST_DEBOUNCE`, `AUTOFILL_POST_DEBOUNCE_MAP_CAP`, `LAST_AUTOFILL_POST`,
      `synth_autofill_mouse_event` and `post_autofill_right_click` in full. Note that the region
      opens at :1726 with a section comment about the live main-thread/WKWebView session that exists
      only to introduce the poster.
    - `src-tauri/src/main.rs:2650-2710` — `login_window_wk_webview`, including its `:2680` doc line
      naming `post_autofill_right_click` as its caller, and the neighbouring
      `PRESENTED_LOGIN_SHEETS_CAP`/`PRESENTED_LOGIN_SHEETS` declarations which MUST survive.
    - `src-tauri/src/main.rs:3780-3950` — the `humble_login_open` arm's head: `nav_sentinel_label`
      (:3789), the kill-switch statement (:3905-3927) and the cancel-strip injection block
      (:3938-3945) that must survive immediately after it.
    - `src-tauri/src/main.rs:3995-4090` — the two `app_for_*` clones and the `.on_navigation(`
      closure's two sentinel arms.
    - `src-tauri/src/main.rs:6320-6480` and `:6660-6760` — the two cargo test blocks being deleted,
      and `:6480-6560` — the cancel-strip tests that survive, one of which references
      `AUTOFILL_EXFIL_PATH`.
    - **The seven REQUIRED COMMENT REWRITE sites (C1-C7) in full, before editing any of them** —
      `src-tauri/src/main.rs:1546-1567` (`LOGIN_CANCEL_EXFIL_PATH`'s doc, C1),
      `:1568-1602` and `:1615-1625` (`login_cancel_strip_script`'s doc, C2 — read the whole comment,
      not just the four flagged lines, so the T-34.4.2-15/-33 lock-out reasoning is rewritten intact
      rather than clipped), `:2704-2715` (`PRESENTED_LOGIN_SHEETS`'s doc, C3), and `:3928-3945`
      (the cancel-strip injection comment AND the block beneath it, C4 — you are editing the former
      and must leave the latter byte-identical), `:3808-3812` (`nav_sentinel_label`'s doc and binding,
      C7), and `:4007-4014` (the surviving `app_for_cancel` clone's doc comment and binding, C6 —
      read it together with the `app_for_autofill` declaration above it that is being deleted, so the
      rewrite stands on its own rather than referring to something gone).
    - This plan's `<interfaces>` and `<guard_constraints>` blocks above, including the REQUIRED
      COMMENT REWRITES table and its reconciliation grep.
  </read_first>
  <action>
    Delete every symbol, call site and cargo test named in `<interfaces>` under "to DELETE", together
    with the doc comments that belong exclusively to them. This is a removal, not a refactor: do not
    replace any deleted function with a stub, a `#[allow(dead_code)]` shim, a feature flag, a
    commented-out block, or a "kept for reference" copy. **D-A is delete, not disable.**

    **Do NOT fix F-34.4.2-08.** `String.fromCharCode(128273)` at `:1499` is inside
    `autofill_glyph_script`, which is being deleted. Applying `String.fromCodePoint` would be work on
    a corpse.

    **Sequencing that avoids a broken intermediate state:** delete the call sites first (:4076-4084,
    then :4005-4006, then :3905-3927), then the leaf helpers, then `post_autofill_right_click`, then
    `login_window_wk_webview`, then the cargo tests. Run `cargo check --manifest-path
    src-tauri/Cargo.toml` after each group; the compiler's unresolved-name and `dead_code` errors are
    your completeness oracle for anything this plan's inventory missed.

    **Kill-switch removal shape, and the intro fragment above it.** The deletion range is
    **:3898-3927, not :3905-3927.** The five lines at :3898-3904 ("In-field autofill glyph injection
    (Phase 34.4.2 Plan 03, REQ-34.4.2-04/06/07/08). Gated on `visible` ... The pristine Epic surface
    ... is unreachable from here entirely -- REQ-34.4.2-10, locked user decision.") are the intro to
    the very block being deleted. They contain none of the grep-checked literals, so nothing would
    catch them — they would survive as a stale fragment introducing code that no longer exists
    beneath it. **Delete them with the block.** Then the entire
    `if std::env::var("GAMELIB_AUTOFILL_GLYPH").as_deref() == Ok("0") { ... } else { ... }` statement
    goes with its own justification comment. Do not leave a bare `else` body and do not preserve the
    `eprintln!` lines. The `#[cfg(target_os = "macos")]` cancel-strip injection block immediately
    below (:3938-3945) must be left **byte-identical**; its PRECEDING comment (:3928-3937) is
    rewrite site C4 and IS edited — see below.

    **Rewrite the seven dangling-cross-reference comments (C1-C7 in `<interfaces>`) — this is required,
    not optional, and it is the one place this plan authorizes touching prose inside surviving
    regions.** Without it the plan contradicts itself: three of those comments sit in code the plan
    guarantees, yet they name symbols the plan deletes. Rewrite each so it describes the
    post-deletion state, and **preserve the reasoning each carries rather than deleting the
    sentences** — above all C2's and C4's statement that the cancel strip has NO kill switch by
    design, because an env var that removes the only visible exit from a parent-blocking sheet is a
    lock-out switch, not a safety switch (T-34.4.2-15/-33). That reasoning gets STRONGER after this
    deletion, not weaker: there is now no kill-switchable injected control in this arm at all, so the
    strip's independence is permanent by construction. Say that.

    **Scope of the carve-out, stated precisely so it cannot be stretched:** doc comments only, at the
    seven sites named. No functional code, no generated script body, no `eprintln!` literal, no
    attribute and no signature in any surviving symbol changes. In particular
    `login_cancel_strip_script`'s generated JS must be byte-identical — its eight cargo tests plus
    plan 11's four assert its content, and the pure-function test
    `login_cancel_strip_script_is_pure_same_host_yields_identical_output` would catch a change. **The
    carve-out explicitly does NOT extend to `open_pristine_epic_login_window` or
    `EpicPristineNavDelegate` (D-D, REQ-34.4.2-10) — those stay byte-frozen including comments.**

    **Reconcile before committing.** Run the `<interfaces>` reconciliation grep. Every remaining hit
    must be either (a) a line you deliberately rewrote and can point at, or (b) zero. An unexplained
    hit means either a deletion was missed or a sixth dangling site exists that this plan did not
    anticipate — record it in the SUMMARY either way.

    **`.on_navigation(` closure shape after deletion.** The closure keeps exactly one sentinel arm
    (`is_login_cancel_request`), keeps its `return false` inside that arm, and still ends `true`.
    Rewrite the closure's leading doc comment (:4039-4066): it currently opens "Autofill sentinel
    interception ONLY (Phase 34.4.2 Plan 03, REQ-34.4.2-04/08)" and spends three paragraphs on the
    poster. It becomes cancel-sentinel-only. **Preserve verbatim** the F-34.5-G6-04 paragraph (spike
    013's measurement that 5 of 8 `on_navigation` events on Humble's real login page were third-party
    iframes, and the resulting ban on `push_login_window_event`/`current_origin`/`set_title` in this
    closure) — that invariant belongs to Phase 34.5 and is unaffected by this deletion.

    **`nav_sentinel_label`.** Keep the binding (:3812); rewrite its doc comment (**:3808-3811**, site
    C7) so it names the cancel sentinel rather than the autofill sentinel as the closure that needs its
    own owned label, preserving its reasoning for why the label is cloned separately from
    `event_label`.

    **`login_window_wk_webview` orphan check.** Before deleting it, run
    `grep -n "login_window_wk_webview" src-tauri/src/main.rs` and confirm every remaining hit is
    either its own definition, its own doc comment, or a line inside `post_autofill_right_click`.
    If a caller outside the poster exists, STOP and record it in the SUMMARY rather than deleting —
    the deletion inventory would then be wrong and the plan needs correcting, not working around.

    **Retarget, do not delete,** `login_cancel_request_rejects_the_autofill_sentinel_path_on_the_same_host`
    per `<interfaces>`: inline the literal `/autofill-request`, rename to
    `login_cancel_request_rejects_an_unrelated_path_on_the_same_host`, keep the T-34.4.2-13 reasoning
    in its comment.

    **Epic is byte-frozen (D-D, REQ-34.4.2-10).** Nothing in `open_pristine_epic_login_window` or
    `EpicPristineNavDelegate` changes. Verify with `git diff --unified=0` before committing.
  </action>
  <verify>
    <automated>set -o pipefail; cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && test "$(grep -ciE 'autofill|login_window_wk_webview|css_rect_center_in_view|clamp_point_to_view_bounds' src-tauri/src/main.rs)" = "0" && test "$(grep -c 'login_cancel_strip_script(REVEAL_EXFIL_HOST)' src-tauri/src/main.rs)" -ge 1 && test "$(grep -c 'T-34.4.2-15' src-tauri/src/main.rs)" -ge 1 && test "$(grep -c 'app_for_cancel' src-tauri/src/main.rs)" -ge 1 && test "$(git diff --unified=0 -- src-tauri/src/main.rs | grep -c 'open_pristine_epic_login_window\|EpicPristineNavDelegate')" = "0" && echo DELETION_COMPLETE_RATIONALE_KEPT_EPIC_UNTOUCHED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 with **zero warnings** — in particular no `dead_code`, `unused_imports` or `unused_variables` warning attributable to this deletion. A surviving `dead_code` warning means an orphan was missed.
    - `cargo test --manifest-path src-tauri/Cargo.toml` passes. The test count will DROP (roughly 20-25 tests are being deleted with their subjects); record the exact before/after figures in the SUMMARY. A drop here is expected and correct, unlike every other plan in this phase.
    - **The reconciliation grep returns NOTHING**: `grep -ciE 'autofill|login_window_wk_webview|css_rect_center_in_view|clamp_point_to_view_bounds' src-tauri/src/main.rs` is **0** (asserted by the verify command). This single bar subsumes all thirteen deleted symbol names, the `/autofill-request` path literal, the `app_for_autofill` local binding (C6), and every prose mention (C1, C4, C7) — including the three classes a symbol-name-scoped grep is structurally blind to. It also implies `grep -c "In-field autofill glyph injection"` is 0, so the orphaned `:3898-3904` intro fragment is caught by the same check.
    - `grep -c` over `src-tauri/src/main.rs` additionally returns **0** for `mouseEventWithType_location_modifierFlags_timestamp_windowNumber_context_eventNumber_clickCount_pressure`, `postEvent_atStart`, `AUTOFILL_POST_DEBOUNCE_MAP_CAP` and `String.fromCharCode(128273)` — the deleted names that carry no `autofill` stem beyond what the bar above covers.
    - `grep -c "fromCodePoint" src-tauri/src/main.rs` returns **0** — F-34.4.2-08 was NOT fixed, it was superseded.
    - `grep -c "login_cancel_strip_script\|is_login_cancel_request\|request_login_sheet_cancel\|LOGIN_CANCEL_EXFIL_PATH" src-tauri/src/main.rs` does not DECREASE from before this task (the strip's own symbols survive, D-B; the count may shift by a line or two as C1/C2/C4's prose is rewritten, but no symbol may disappear).
    - **The cancel strip's generated JS is byte-identical.** All eight pre-existing `login_cancel_strip_script_*` cargo tests plus plan 11's four pass UNCHANGED, including `login_cancel_strip_script_is_pure_same_host_yields_identical_output` and the single-`"try {"` assertion. A change to the script body would fail them; a change to the doc comment cannot.
    - **The rationale survived the comment rewrites.** `login_cancel_strip_script`'s doc comment (C2) still states in substance that the strip has NO kill switch and must never gain one, still cites T-34.4.2-15 and T-34.4.2-33, and still gives the lock-out-switch-versus-safety-switch reason. The cancel-strip injection comment (C4) still states the same at the call site and still gives the macOS-gating reason. Quote both rewritten comments in the SUMMARY so the preservation is auditable rather than asserted.
    - `PRESENTED_LOGIN_SHEETS`'s doc comment (C3) names its SURVIVING consumers and retains the bounded-growth / dedup-on-insert / always-removed-on-dismiss reasoning (T-34.4.2-08).
    - The `#[cfg(target_os = "macos")]` cancel-strip injection block itself (the `builder.initialization_script(&login_cancel_strip_script(...))` statement and its `eprintln!`) is byte-identical — only the comment above it changed. Confirm with `git diff` on that hunk and quote it in the SUMMARY.
    - The `:3898-3904` intro fragment ("In-field autofill glyph injection ... locked user decision") is GONE — `grep -c "In-field autofill glyph injection" src-tauri/src/main.rs` returns 0. No grep-checked literal would have caught this fragment; it is asserted separately for that reason.
    - **C6 and C7 specifically:** the surviving `app_for_cancel` clone's doc comment no longer names `app_for_autofill` and describes `app_for_cancel` on its own terms, with the `let app_for_cancel = app.clone();` binding and its `#[cfg_attr(...)]` attribute byte-identical; and `nav_sentinel_label`'s doc comment no longer says "autofill sentinel" while keeping its reasoning for the separate clone. `grep -c 'app_for_cancel' src-tauri/src/main.rs` is >= 1 (asserted by the verify command) — the binding survives, only its prose changed.
    - **Every automated verify command in this plan begins `set -o pipefail`.** Without it a pipeline's exit status is `tail`'s, which is 0 essentially always, so `cargo test` and `npx jest` failures would never reach the `&&` chain and the success sentinel could print over a real regression. This matters most here: Task 1's cargo-test pass is the strong backstop behind the deliberately-softened "strip symbol count must not decrease" criterion.
    - The reconciliation grep returns nothing unexplained. A residual hit — a missed deletion, an unrewritten comment, or an eighth site this plan did not anticipate — is recorded in the SUMMARY either way.
    - `grep -c "present_login_window_as_sheet\|dismiss_login_window_sheet\|register_presented_login_sheet\|SHEET_PRESENT_WKWEBVIEW_WARMUP_DELAY" src-tauri/src/main.rs` is unchanged from before this task (sheet presentation is untouched, D-C).
    - The `.on_navigation(` closure contains `is_login_cancel_request`, contains no `parse_autofill_request`, and still returns `true` as its final expression.
    - `git diff --unified=0 -- src-tauri/src/main.rs` shows zero changed lines inside `open_pristine_epic_login_window` and `EpicPristineNavDelegate` (asserted by the verify command, which echoes `DELETION_COMPLETE_RATIONALE_KEPT_EPIC_UNTOUCHED_OK`).
    - **Live behaviour is NOT claimed by this task.** That no glyph appears on a real login page, and that Cmd+V still works as the sole route, is the new gate's item 3 and item 4. State this explicitly in the SUMMARY.
  </acceptance_criteria>
  <done>The autofill glyph, its script builder, its parser, its sentinel arm, its poster, its synthesizer, its debounce, its coordinate helpers, its WKWebView resolver, its kill switch and its own cargo tests are gone from `main.rs`; the cancel strip, the Esc monitor, sheet presentation and Epic are untouched; and the compiler reports no orphan.</done>
</task>

<task type="auto">
  <name>Task 2: Make the deletion machine-enforceable — a removed-symbols absence guard, relocated T-34.4.2-20 negatives, and retargeted survivors</name>
  <files>src/backend/__tests__/tauriShellSource.test.ts, src/backend/__tests__/cargoFeatures.test.ts, src-tauri/Cargo.toml</files>
  <read_first>
    - `src/backend/__tests__/tauriShellSource.test.ts:704-745` — `PHASE_34_4_2_NEW_SYMBOLS` and the
      comment describing what the array is FOR, so the new array is written in the same idiom.
    - `src/backend/__tests__/tauriShellSource.test.ts:746-845` — the Plan 01 describe block, especially
      Test 1's use of `fn login_window_wk_webview(` as a slice END anchor (:782) and Test 2 (:789).
    - `src/backend/__tests__/tauriShellSource.test.ts:944-996` — Plan 02's Test 5, the worked example of
      this phase's own "RETIRED, now NEGATIVE" idiom (`NSWindowDidDeminiaturizeNotification` appears
      0 times). **Copy this idiom for every symbol this plan removes.**
    - `src/backend/__tests__/tauriShellSource.test.ts:998-1330` — the Plan 03 and Plan 04 describe blocks
      being deleted, reading the T-34.4.2-20 credential-selector negatives at :1136-1145 and
      :1278-1287 closely enough to relocate all four selector names verbatim.
    - `src/backend/__tests__/tauriShellSource.test.ts:1332-1552` — the Plan 08 describe block, its
      kill-switch slicing helper (:1358-1372), Test 1 (:1434) and Test 2 (:1449).
    - `src/backend/__tests__/tauriShellSource.test.ts:622-687` — the Phase 34.5 Plan 27 describe block
      whose Test at :653 must be retightened rather than deleted.
    - `src/backend/__tests__/tauriShellSource.test.ts:1554-1650` — the Plan 11 describe block, especially
      the `.makeKeyAndOrderFront(None)` exactly-once assertion that becomes a zero assertion.
    - `src/backend/__tests__/cargoFeatures.test.ts:108-150` — `extractObjc2AppKitFeatures` and the
      `NSGraphicsContext` assertions.
    - `src-tauri/Cargo.toml:100-125` — the `objc2-app-kit` dependency line and the comment block
      recording why `NSGraphicsContext` was added.
  </read_first>
  <action>
    **A. Mint the absence guard.** Add a module-level array
    `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS` beside `PHASE_34_4_2_NEW_SYMBOLS`, containing every name
    Task 1 deleted: `AUTOFILL_EXFIL_PATH`, `AutofillRequest`, `parse_autofill_request`,
    `autofill_glyph_script`, `css_rect_center_in_view`, `clamp_point_to_view_bounds`,
    `AUTOFILL_POST_DEBOUNCE`, `AUTOFILL_POST_DEBOUNCE_MAP_CAP`, `LAST_AUTOFILL_POST`,
    `synth_autofill_mouse_event`, `post_autofill_right_click`, `login_window_wk_webview`,
    `GAMELIB_AUTOFILL_GLYPH`. Remove those same names from `PHASE_34_4_2_NEW_SYMBOLS`.

    Create `describe('Phase 34.4.2 Plan 13 — the in-field autofill mechanism is DELETED (operator
    decision D-A)')` with:
    1. **The permanent absence negative.** Every `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS` member appears
       **0 times** in the comment-stripped production source. Name the array in the failure message so
       a future session that reintroduces one is told which decision it is violating.
    2. **AppKit event-synthesis is gone entirely.** `postEvent_atStart` and
       `mouseEventWithType_location_modifierFlags_timestamp_windowNumber_context_eventNumber_clickCount_pressure`
       each appear 0 times. This is the strictly stronger claim: not merely "our poster is gone" but
       "this application synthesizes no NSEvent at all."
    3. **T-34.4.2-20 RELOCATED (permanent, outlives the poster).** The comment-stripped source contains
       none of `_canUseCredentialStorage`, `_setCanUseCredentialStorage`,
       `_showDigitalCredentialsPicker`, `_dismissDigitalCredentialsPicker`. Write the test name to say
       explicitly that this negative is NOT tied to the deleted poster and must survive any future
       login-window work — it exists to stop a future session reaching for a private "open the
       Passwords panel" selector, which spike 022 proved does not exist.
    4. **The surviving sentinel is alone.** The `humble_login_open` arm's `.on_navigation(` closure
       contains `is_login_cancel_request` and contains no `parse_autofill_request`.
    5. **The strip survived the deletion.** `login_cancel_strip_script(` is still called exactly once,
       inside the arm's `if visible {` block.

    **B. Retarget the survivors** exactly as `<guard_constraints>` specifies: re-anchor Plan 01 Test 1's
    slice end; delete Plan 01 Test 2 with its subject; delete the Plan 03 and Plan 04 describe blocks
    (having first relocated their T-34.4.2-20 negatives into the new block); strip the
    kill-switch-independence clause and the slicing helper from Plan 08 Test 1 while keeping its
    exactly-once and inside-`if visible` clauses; retighten Plan 08 Test 2 to a
    contains-cancel/not-contains-autofill assertion; retighten the Phase 34.5 Plan 27 test at :653 to
    `is_login_cancel_request` while preserving its four relay-ban assertions; and convert Plan 11's
    `.makeKeyAndOrderFront(None)` exactly-once assertion into a zero-occurrence negative whose test
    name records that the last call site left with the poster.

    **C. The `NSGraphicsContext` Cargo feature.** It was added solely for the deleted synthesizer.
    Follow this decision procedure and record which branch you took, with real command output:
    remove `NSGraphicsContext` from `objc2-app-kit`'s feature list in `src-tauri/Cargo.toml`, run
    `cargo build --manifest-path src-tauri/Cargo.toml`, and
    - if it builds clean, keep the removal, update `src-tauri/Cargo.toml`'s comment block to record
      that the feature was retired with the mechanism that forced it, and invert
      `cargoFeatures.test.ts`'s :129 test into a negative asserting the feature is ABSENT (leaving the
      :135 pre-existing-features test untouched);
    - if it fails, restore the feature verbatim, leave both `cargoFeatures.test.ts` tests untouched,
      and record the exact compiler error in the SUMMARY as the reason.
    Either way, confirm `git diff --exit-code pnpm-lock.yaml` and `git diff --exit-code
    src-tauri/Cargo.lock` both exit 0 — removing a feature must not move the lockfile.

    **D. Mutation discipline (this phase's standing practice).** Prove the new absence guard actually
    fires: temporarily reintroduce one deleted identifier (e.g. a bare `// post_autofill_right_click`
    is NOT sufficient — the stripper removes comments; use a real `const _X: &str =
    "post_autofill_right_click";`), confirm the guard FAILS, then revert. Record the mutation and its
    reversion in the SUMMARY. A guard never observed failing is not a guard.
  </action>
  <verify>
    <automated>set -o pipefail; npx jest src/backend/__tests__/tauriShellSource.test.ts src/backend/__tests__/cargoFeatures.test.ts 2>&1 | tail -10 && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -3 && git diff --exit-code pnpm-lock.yaml && git diff --exit-code src-tauri/Cargo.lock && echo GUARDS_OK_LOCKFILES_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/backend/__tests__/tauriShellSource.test.ts` passes, with the new Plan 13 describe block present and every surviving pre-existing test (Plan 01 Test 1, Plan 02 Tests 1-6, Plan 08 Tests 1-8, Plan 11 Tests 1-5, and the Phase 34.5 Plan 27 block) green.
    - `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS` exists and contains all thirteen names listed in the action; none of those names remains in `PHASE_34_4_2_NEW_SYMBOLS`.
    - All four credential-shaped WKWebView selectors are asserted absent from the whole comment-stripped file, in the new describe block — `grep -c "_canUseCredentialStorage" src/backend/__tests__/tauriShellSource.test.ts` is >= 1 after the Plan 03/04 blocks are gone. **If this count is 0, T-34.4.2-20's permanent negative was lost and the task is not done.**
    - `postEvent_atStart` and the full `mouseEventWithType_...` selector are each asserted to appear 0 times in production source.
    - The mutation check is recorded in the SUMMARY: the exact identifier reintroduced, the test that failed, and confirmation it was reverted (`git diff --stat -- src-tauri/src/main.rs` clean of the mutation at task end).
    - `npx jest src/backend/__tests__/cargoFeatures.test.ts` passes on whichever `NSGraphicsContext` branch was taken, and the SUMMARY names the branch with its real command output.
    - `git diff --exit-code pnpm-lock.yaml` and `git diff --exit-code src-tauri/Cargo.lock` both exit 0 (asserted by the verify command, which echoes `GUARDS_OK_LOCKFILES_CLEAN`).
    - `npm run test:ci` passes; record the figure as CONTEXT, not as evidence of live behaviour (T-34.4.2-26 — four green suites have now coexisted with failed gates in this phase alone).
  </acceptance_criteria>
  <done>A permanent, mutation-proven absence guard makes reintroducing the deleted mechanism a test failure; T-34.4.2-20's credential-selector negatives survived the removal of the blocks that hosted them; every guard that referenced a deleted symbol is retargeted rather than dropped; and the Cargo feature decision is recorded with real evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite REQ-34.4.2-04/-05 to the corrected scope, retire T-34.4.2-15 as moot, and propagate the deletion into the platform-scope and deferred-item ledgers</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:731-742` — the ten REQ-34.4.2 rows and the phase provenance note at
      :758, including the established amend-in-place idiom (strike-through for superseded text, a
      dated `**CORRECTED ...**` clause appended, never a deletion or a renumber).
    - `.planning/phases/.../34.4.2-VERIFICATION.md` gaps frontmatter and the "On the operator's
      post-gate decision to drop the autofill glyph" paragraph in Gaps Summary — the verifier states
      explicitly that this gap closes **by scope correction** if a plan formally rewrites these two
      requirements, and stands as the acceptance bar if a plan instead tries to fix the fill.
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` §1 (the per-symbol table, the RETIRED subsection
      at :54-65 which is the idiom to copy, and the "Finding: the autofill glyph is injected on every
      platform" subsection at :97-139) and §5's five existing update tables (:342-505).
    - `.planning/phases/.../deferred-items.md` — the "Plan 09 — the autofill glyph's un-gated
      cross-platform injection remains unfixed" entry and the "Plan 11 — seven `34.4.2-REVIEW.md`
      findings deliberately OUT of scope" entry, specifically its WR-06 and WR-02 bullets.
  </read_first>
  <action>
    **A. `.planning/REQUIREMENTS.md`.** Mint NO new REQ ID; delete none; renumber none.

    Rewrite **REQ-34.4.2-04** and **REQ-34.4.2-05** so the shipped capability is stated truthfully and
    the removal itself is the verifiable claim. Follow the file's own amend-in-place idiom: strike
    through the superseded text (which currently promises an injected key glyph and a working
    synthesized-right-click autofill request), then append a dated
    `**SCOPE-CORRECTED 2026-08-05 (plan 13, operator decision D-A)**` clause stating:
    - **REQ-34.4.2-04** now reads: no in-field autofill affordance is shipped on any Tauri-managed
      login surface. No key glyph is injected, on any platform. No `GAMELIB_AUTOFILL_GLYPH` kill
      switch exists, because there is no longer anything to switch off. **Cmd+V and Edit ▸ Paste are
      the sole credential-entry route.** The verifiable claim is the ABSENCE: source-provable now by
      `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS`, live-provable by `34.4.2-LIVE-GATE-RERUN-3.md` item 3.
    - **REQ-34.4.2-05** now reads: this application synthesizes and posts NO `NSEvent` of any kind.
      The synthesized `RightMouseDown`/`RightMouseUp` poster is deleted. The prohibition on private
      selectors (never `_canUseCredentialStorage`, `_setCanUseCredentialStorage`,
      `_showDigitalCredentialsPicker`, `_dismissDigitalCredentialsPicker`) **remains in force
      permanently and independently of the deletion**, enforced by the relocated T-34.4.2-20 negative.
    - Both clauses must record WHY, in one sentence each, naming `34.4.2-LIVE-GATE-RERUN-2.md` item 3
      and F-34.4.2-09 (a synthesized right-click surfaces the AutoFill menu but never fills; a real
      one does; spike 022's Recommendation #4 is falsified). **Do not write "v1", "for now",
      "simplified", "future enhancement" or any language implying the affordance returns later** —
      this is a permanent scope correction, not a deferral.
    - **Both boxes stay `[ ]`** at the end of this plan. They are ticked only by a measured PASS on
      the new gate's item 3, recorded by the gate-runner plan and by no one else.

    Append a dated paragraph to the phase provenance note at :758 describing gap cycle 3: the
    operator's binding D-A deletion decision, the scope correction of REQ-04/-05, and that no ID was
    added, deleted or renumbered.

    **B. `34.4.2-PLATFORM-SCOPE.md` — amend in place, never rewrite, never delete history.**
    - §1: move every deleted symbol's row out of the live per-symbol table into a **new
      `RETIRED (Plan 13, 2026-08-05 — operator decision D-A)` subsection** modelled on the existing
      `RETIRED (Plan 07, 2026-08-04)` subsection at :54-65: one row per symbol, with the plan that
      retired it and a one-line why. Include `login_window_wk_webview` with the note that it was
      retired as an orphan (sole caller was the poster), not by its own defect.
    - §1: mark the "Finding: the autofill glyph is injected on every platform, not gated to macOS"
      subsection (:97-139) **MOOT — closed by deletion, 2026-08-05**, retaining the original text as
      history. State the consequence plainly: the standing REQ-34.4.2-08 wart this phase carried
      since plan 05 is gone, because the un-gated symbol it described no longer exists. The cancel
      strip's macOS-gated call site is now the ONLY injected control on the login form, so the
      platform-behaviour asymmetry the finding described is resolved rather than merely tracked.
    - §1: record the Cargo.toml outcome from Task 2's decision procedure, quoting the real command
      output, and re-confirm both lockfiles byte-identical with quoted `git diff --exit-code` runs.
    - §5: append a **SIXTH** update table (retain all five). **Its header must follow the section's
      existing convention exactly — a line beginning `**Sixth update (2026-08-05, plan 34.4.2-13 ...`
      — because this task's verify command counts those five headers plus yours and asserts exactly
      six.** Give every affected threat its post-deletion disposition, with the reason stated rather
      than implied:
      **RETIRED BY DELETION (the attack surface no longer exists)** — T-34.4.2-10, -11, -12, -14, -16,
      -17, -18, -19, -21, -36. Call out T-34.4.2-17 specifically: the elevation-of-privilege surface
      this phase's largest threat named (page-controlled event injection) is not mitigated, it is
      GONE — the strongest security consequence of D-A, and worth stating as such.
      **RETIRED AS MOOT** — T-34.4.2-15's kill-switch residual: an escape hatch for an injection that
      no longer happens. Note that the strip's deliberate lack of a kill switch (T-34.4.2-33) is now
      permanent by construction rather than by convention.
      **NARROWED** — T-34.4.2-13 (sentinel collision): three sentinels remain on `REVEAL_EXFIL_HOST`
      (`/login-cancel`, `/reveal`, `/clear-storage`), not four.
      **SURVIVES THE DELETION, PERMANENT** — T-34.4.2-20: the private-selector ban is relocated, not
      retired; name the test that now carries it.
      **RE-MEASUREMENT REQUIRED, not inherited** — T-34.4.2-05, -07, -22, -33, -34, -35, -38 were all
      CLOSED live-proven by gate run 2, but this plan changes `src-tauri/src/main.rs`, and this
      document's own repeated rule is that a fix does not close a threat until its item runs. Mark
      each **CLOSED-pending-re-measurement** against `34.4.2-LIVE-GATE-RERUN-3.md`, not silently
      carried forward.
      **NEW — T-34.4.2-40**: Tampering (a deleted mechanism silently reintroduced by a later plan or
      a merge). mitigate — source-fixed. Discharged by `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS` and the
      zero-occurrence event-synthesis negatives, both mutation-proven in Task 2.
      **UNCHANGED** — T-34.4.2-32 (Epic scope boundary), T-34.4.2-37 (source-fixed, unexercised),
      T-34.4.2-39 (OPEN — plan 14's subject, not this plan's), T-34.4.2-SC.

    **C. `deferred-items.md`.**
    - Mark the **"Plan 09 — the autofill glyph's un-gated cross-platform injection remains unfixed"**
      entry **CLOSED 2026-08-05 by plan 13's deletion** — not fixed by gating, but by removing the
      symbol. Note the irony worth recording for future sessions: an inert unlabelled key icon on
      Windows/Linux was logged as owed work for three plans, and the fix that finally landed was
      deleting the icon everywhere.
    - Mark the **WR-06** bullet (arbitrary debounce eviction, refusals arming the debounce) inside the
      Plan 11 entry **MOOT 2026-08-05 — its subject (`post_autofill_right_click`'s `LAST_AUTOFILL_POST`)
      no longer exists.**
    - Narrow the **WR-02** bullet: the poster's own worker-to-main `NSWindow` address hop is gone, so
      the use-after-free race now has strictly fewer sites (`present_login_window_as_sheet` and
      `dismiss_login_window_sheet` only). Still OPEN, still not fixed here.
    - Leave WR-05, WR-08, IN-01, IN-03, IN-04 exactly as they are. **Do not re-open them.** None sits
      on a route the new gate's items traverse.
    - Add one new entry for this plan recording what the deletion did NOT do: it did not fix
      F-34.4.2-08 (superseded), did not touch the cancel strip or sheet presentation (D-B/D-C), did
      not touch Epic (D-D), and did not attempt any alternative autofill trigger (D-A forbids it).
  </action>
  <verify>
    <automated>test "$(grep -c 'REQ-34.4.2-' .planning/REQUIREMENTS.md)" -ge 10 && test "$(grep -c '^- \[x\] \*\*REQ-34.4.2-04\|^- \[x\] \*\*REQ-34.4.2-05' .planning/REQUIREMENTS.md)" = "0" && grep -q 'RETIRED (Plan 13' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md && grep -q 'T-34.4.2-40' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md && test "$(grep -cE '^\*\*(Update|Second update|Third update|Fourth update|Fifth update|Sixth update) \(' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md)" = "6" && test -z "$(git diff -- .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md)" && echo PROPAGATION_OK_GATE2_UNCHANGED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` still contains all ten REQ-34.4.2 IDs; none added, deleted or renumbered (verify command asserts the count).
    - REQ-34.4.2-04 and REQ-34.4.2-05 both remain `[ ]` (asserted by the verify command). Ticking either here would be the exact "claim not matched by its artifact" defect this phase's own `deferred-items.md` already records twice.
    - Both rewritten rows state the ABSENCE as the verifiable claim and name Cmd+V / Edit ▸ Paste as the sole credential-entry route.
    - Neither rewritten row contains any of the strings `v1`, `for now`, `simplified`, `placeholder`, `future enhancement`, or `will be` — grep for each and record the result.
    - `34.4.2-PLATFORM-SCOPE.md` contains a `RETIRED (Plan 13` subsection listing every deleted symbol, a MOOT marker on the un-gated-glyph finding, and a SIXTH §5 update table; all five pre-existing update tables are still present.
    - The §5 sixth table gives an explicit disposition to every threat named in the action, including the `CLOSED-pending-re-measurement` set — no threat is silently carried forward as closed across a source change.
    - `T-34.4.2-40` appears in the §5 sixth table with its discharging guard named.
    - `deferred-items.md` marks the plan-09 glyph-gating entry CLOSED and WR-06 MOOT, narrows WR-02, and leaves WR-05/WR-08/IN-01/IN-03/IN-04 untouched.
    - `34.4.2-LIVE-GATE-RERUN-2.md`, `34.4.2-LIVE-GATE-RERUN.md` and `34.4.2-LIVE-GATE.md` are all byte-unchanged — they hold measured history (asserted by the verify command, which echoes `PROPAGATION_OK_GATE2_UNCHANGED`).
    - This task ran no gate item, started no harness, and never launched `tauri:dev`.
  </acceptance_criteria>
  <done>REQ-34.4.2-04/-05 state the shipped capability truthfully with the removal as their verifiable claim, both boxes still unticked; the platform-scope record retires the deleted symbols and gives every affected threat an explicit post-deletion disposition including a re-measurement flag for the seven that gate run 2 closed; and the deferred-item ledger records what the deletion closed, mooted, narrowed and deliberately left alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| login page JS → shell | Before this plan, an injected glyph script ran in every frame of a hostile-capable third-party credential document and could post a request that made the app synthesize an OS-level mouse event. This plan removes that boundary crossing entirely. |
| page-supplied coordinates → AppKit event queue | The only path by which page-controlled data reached `NSApplication::postEvent_atStart`. Deleted. |
| a later plan / a merge → the deleted mechanism | A removal is only as durable as the guard that detects its return. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-17 | Elevation of privilege (page-controlled event injection) | `post_autofill_right_click`, `synth_autofill_mouse_event` | **RETIRED BY DELETION** | Not mitigated — eliminated. This phase's largest threat surface, six source-provable constraints deep, is removed rather than defended. Task 2's zero-occurrence negatives on `postEvent_atStart` and the `mouseEventWithType_...` selector make the elimination permanent. |
| T-34.4.2-10 / -11 / -19 | Information disclosure (credential exfiltration, request data and coordinates reaching a log) | autofill sentinel channel | **RETIRED BY DELETION** | The channel, its payload schema and its logging all cease to exist. Task 1 acceptance criteria assert zero occurrences of `AUTOFILL_EXFIL_PATH` and `parse_autofill_request`. |
| T-34.4.2-12 / -14 / -18 | Spoofing / DoS (page forging or flooding autofill requests) | glyph script, `LAST_AUTOFILL_POST` debounce | **RETIRED BY DELETION** | Nothing to forge, nothing to flood. WR-06's debounce-fairness finding is mooted with its subject. |
| T-34.4.2-15 | Tampering (a kill switch removing the only visible exit) | `GAMELIB_AUTOFILL_GLYPH` | **RETIRED AS MOOT** | An escape hatch for an injection that no longer happens. The cancel strip's deliberate lack of a kill switch (T-34.4.2-33) becomes permanent by construction: after this plan there is no env var in this arm that can suppress an injected control. |
| T-34.4.2-16 / -21 | DoS (a throwing glyph breaking a login page) / Spoofing (menu forgery) | glyph script, system context menu | **RETIRED BY DELETION** | No script injected, no menu requested. |
| T-34.4.2-20 | Tampering (private-API drift toward a credential selector) | future login-window work | **mitigate — SURVIVES, PERMANENT** | **The one autofill-adjacent threat that must NOT retire with the mechanism.** Its negatives currently live inside the two describe blocks Task 2 deletes; relocating all four selector names into the Plan 13 block is a hard acceptance criterion, with a grep count asserting it. Spike 022 proved no public "open the Passwords panel" call exists — this ban is what stops a future session going looking for a private one. |
| **T-34.4.2-40 (NEW)** | Tampering (a deleted mechanism silently reintroduced) | `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS` | mitigate — source-fixed | Thirteen names asserted 0 times in comment-stripped production source, plus zero-occurrence negatives on the two AppKit event-synthesis selectors. Mutation-proven in Task 2 (a real `const` reintroducing one name must make the guard FAIL, then be reverted) — this phase's own standing practice, because a guard never observed failing is not a guard. |
| T-34.4.2-05 / -07 / -22 / -33 / -34 / -35 / -38 | (various, all CLOSED live-proven by gate run 2) | sheet presentation, cancel strip, Esc monitor | **CLOSED-pending-re-measurement** | This plan changes `src-tauri/src/main.rs`. `34.4.2-PLATFORM-SCOPE.md` §5's own repeated rule — "a new mechanism does not inherit a fix" — applies to a deletion as much as to a replacement. Task 3 marks all seven for re-measurement by `34.4.2-LIVE-GATE-RERUN-3.md`; none is carried forward as closed on the strength of a run against different source. |
| T-34.4.2-32 | Tampering (Epic scope-boundary erosion) | `PHASE_34_4_2_NEW_SYMBOLS` guard | mitigate — CLOSED by source | D-D: Epic byte-frozen. Both Task 1 and Task 2 carry a zero-line-diff acceptance criterion on `open_pristine_epic_login_window` and `EpicPristineNavDelegate`. Shrinking `PHASE_34_4_2_NEW_SYMBOLS` does not weaken this guard — the removed names are simultaneously asserted absent from the entire file, which is strictly stronger than being absent from two regions. |
| T-34.4.2-26 | Repudiation (a green suite mistaken for live proof) | Tasks 1-2 verification | mitigate — CLOSED by process | Both code tasks carry an explicit acceptance criterion stating live behaviour is NOT claimed, naming the gate item that owns each claim. Task 1's test count will DROP; that is expected and must be recorded rather than explained away. |
| T-34.4.2-SC | Tampering (dependency surface) | `src-tauri/Cargo.toml` | mitigate — CLOSED | No `npm`/`pip`/`cargo` install. The only manifest change is the possible REMOVAL of an already-declared feature; both `git diff --exit-code pnpm-lock.yaml` and `git diff --exit-code src-tauri/Cargo.lock` must exit 0, asserted by Task 2's verify command. |
</threat_model>

<verification>
- `cargo check` clean with **zero warnings**; `cargo test --manifest-path src-tauri/Cargo.toml` passes with a recorded, expected DROP in test count.
- Comment-stripped `main.rs` contains zero occurrences of every name in `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS`, plus zero `postEvent_atStart` and zero `mouseEventWithType_...`.
- Comment-stripped `main.rs` still contains `beginSheet_completionHandler` exactly once and `endSheet` exactly once (Plan 01 Test 5 / Plan 02 Test 6 stay green).
- `npx jest src/backend/__tests__/tauriShellSource.test.ts src/backend/__tests__/cargoFeatures.test.ts` passes; `npm run test:ci` passes (recorded as context only, T-34.4.2-26).
- All four credential-shaped WKWebView selectors are still asserted absent somewhere in the guard file.
- `git diff --unified=0 -- src-tauri/src/main.rs` shows zero changed lines in either pristine Epic region.
- All ten REQ-34.4.2 IDs present; REQ-34.4.2-04 and -05 rewritten and still `[ ]`.
- `34.4.2-PLATFORM-SCOPE.md` gains a Plan 13 RETIRED subsection and a sixth §5 update; the five earlier updates survive.
- All three prior gate documents byte-unchanged.
- `git diff --exit-code pnpm-lock.yaml` and `git diff --exit-code src-tauri/Cargo.lock` both exit 0.
- `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing at plan end — this plan never starts the harness.
</verification>

<success_criteria>
The in-field autofill mechanism is gone from the codebase rather than disabled within it; a
mutation-proven guard makes its return a test failure; the one autofill-adjacent security negative
worth keeping (T-34.4.2-20) survived the removal of the blocks that hosted it; REQ-34.4.2-04/-05
describe what actually ships, with the absence as the verifiable claim and no language implying the
affordance returns; and every threat the deletion touches has an explicit disposition, including the
seven that gate run 2 closed and that must now be re-measured against changed source.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-13-SUMMARY.md` when done.
Record: the before/after cargo and jest test counts (a DROP is expected — say why); the exact
identifier used for the mutation check and its reversion; which `NSGraphicsContext` branch was taken
with real command output; that a Phase 34.5 guard (the `F-34.5-G6-04` describe block) was edited and
why; and per removed symbol, that its removal is source-proven while its live consequence is deferred
to `34.4.2-LIVE-GATE-RERUN-3.md` item 3. State explicitly that this plan ran NO gate item, ticked NO
requirement box, and that the phase remains NOT CLOSED.
</output>
