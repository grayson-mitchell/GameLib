---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src-tauri/src/main.rs
  - src/backend/__tests__/tauriShellSource.test.ts
autonomous: true
requirements:
  - REQ-34.4.2-01
  - REQ-34.4.2-02
  - REQ-34.4.2-03
  - REQ-34.4.2-04
  - REQ-34.4.2-05
  - REQ-34.4.2-06
  - REQ-34.4.2-07
  - REQ-34.4.2-08
  - REQ-34.4.2-09
  - REQ-34.4.2-10

must_haves:
  truths:
    - "Every plan in this phase can name the requirement ID it satisfies, and those IDs resolve to real rows in REQUIREMENTS.md rather than only to the ROADMAP line."
    - "Rust code can resolve the NSWindow and the live WKWebView for a Tauri-managed login window label."
    - "The pristine Epic login window is provably untouched by this phase — a regression guard fails if any symbol this phase adds is referenced from it, or if its body changes."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.4.2-01..10 rows + minting rationale including the Epic descope decision"
      contains: "REQ-34.4.2-10"
    - path: "src-tauri/src/main.rs"
      provides: "login_window_ns_window / login_window_wk_webview resolvers"
      contains: "fn login_window_wk_webview"
    - path: "src/backend/__tests__/tauriShellSource.test.ts"
      provides: "resolver assertions + the EPIC-UNTOUCHED scope guard"
  key_links:
    - from: "src-tauri/src/main.rs :: login_window_wk_webview"
      to: "tauri::WebviewWindow::with_webview"
      via: "PlatformWebview::inner() for Tauri-managed windows"
      pattern: "with_webview"
    - from: "src/backend/__tests__/tauriShellSource.test.ts"
      to: "open_pristine_epic_login_window"
      via: "byte-unchanged + no-new-symbol-referenced scope guard"
      pattern: "open_pristine_epic_login_window"
---

<objective>
Declare Phase 34.4.2 (mint REQ-34.4.2-01..10 into REQUIREMENTS.md), build the small Rust foundation
the later plans need, and — new, and load-bearing — install the **machine guard that keeps the
pristine Epic login window out of this phase**.

**SCOPE DECISION (2026-08-04, user, LOCKED): Epic is out.** Epic is implemented LAST, after all other
accounts/runners are ported successfully, because its problems are unresolved (the parked
deterministic pre-auth 403, the zero-injection constraint the pristine surface exists to satisfy, and
the standing hCaptcha/UA constraint). **No code path in this phase may attach to, inject into,
resolve, or exercise the pristine Epic login window** — the webview-less `tauri::WindowBuilder` +
raw `WKWebView` surface built by `open_pristine_epic_login_window`. This phase targets only the
**Tauri-managed `WebviewWindow`** login surface: Humble plus the GOG/Amazon OAuth runners.

The Rust architecture may remain Epic-*ready* (a resolver whose lookup could later accept the
pristine window is fine), but nothing may wire, test, or claim Epic behaviour.

Purpose: plan 02 needs an `NSWindow`; plan 04 needs the `WKWebView`'s own `bounds()` to convert CSS
coordinates without the title-bar-sized error the spike findings call out as a silent landmine.
Without this plan those two would each grow their own lookup and diverge.

Output: the requirement record, two `#[cfg(target_os = "macos")]` resolvers, and the EPIC-UNTOUCHED
guard. **No behaviour change** — nothing calls the resolvers yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md
@.claude/skills/spike-findings-gamelib/references/tauri-login-webview-cookies.md

<interfaces>
<!-- Extracted from the codebase at plan time. Use these directly; do not go exploring. -->

**IN SCOPE — the Tauri-managed login surface.** `humble_login_open` match arm (~L2596) builds a
`tauri::WebviewWindowBuilder` window whose label comes from `next_login_window_label()`. Reachable
via `app.get_webview_window(label)`. This is the surface Humble, GOG and Amazon all use.

**OUT OF SCOPE — the pristine Epic surface.** On macOS, when `url.host_str() == Some(EPIC_LOGIN_HOST)`,
that arm early-`return`s into `open_pristine_epic_login_window(...)` (~L1613–L1891, bounded by the
`#[cfg(target_os = "macos")]` at L1613 and the next one at L1900), which builds a plain
`tauri::Window` and attaches a raw `WKWebView`. **This function must be byte-unchanged by this
phase.** `app.get_webview_window(label)` can never find that window; only `app.get_window(label)` can.

Existing process-static registry pattern in this file, for style reference only:
```
static LOGIN_WINDOW_EVENTS: Mutex<Option<HashMap<String, Vec<Value>>>> = Mutex::new(None);
const LOGIN_WINDOW_EVENTS_CAP: usize = 50;
fn push_login_window_event(label: &str, event: Value) { ... }
```

Existing `SendPtr` pattern for moving raw pointers across the `run_on_main_thread` boundary
(~L1642) — reuse that convention when a later plan needs it; do not invent a new one.

Cargo pins already present (`src-tauri/Cargo.toml`), no change needed by this plan:
`tauri` features include `unstable`; `objc2-app-kit` features include `NSView`, `NSWindow`,
`NSApplication`, `NSEvent`, `block2`; `objc2-web-kit` features include `WKWebView`.

Source-assertion test harness (`src/backend/__tests__/tauriShellSource.test.ts`): all assertions run
against a COMMENT-STRIPPED copy of main.rs via the file's own loader (`stripSourceComments` +
`stripTrailingLineComment`), so doc-comment prose can never satisfy a gate. Follow that existing
loader — do not read main.rs raw.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint REQ-34.4.2-01..10 into REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read the last ~60 lines: the per-phase count summary, the `D-XX -> REQ mapping` paragraphs, and the trailing `*Last updated: ...*` log — match their exact formatting)
    - .planning/ROADMAP.md (the `### Phase 34.4.2:` block — ALREADY POPULATED during the planning session that produced these plans: it carries the ten REQ IDs, the Epic-descope note and the six plan rows. Read it to source the requirement wording and confirm it still matches; do NOT expect a `TBD` to replace.)
    - .claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md (the requirement wording derives from its `## Requirements` list 1–6, narrowed to the in-scope surface)
  </read_first>
  <action>
Append Phase 34.4.2's requirement rows to `.planning/REQUIREMENTS.md` using the exact table/row
format the surrounding phases use. Mint exactly these ten. Every mention of a login surface means the
**Tauri-managed `WebviewWindow`** surface (Humble/GOG/Amazon) unless the row says otherwise — the
pristine Epic surface is out of scope per the locked user decision.

- REQ-34.4.2-01 — On macOS, a VISIBLE Tauri-managed login window is attached to the app's `main`
  window as an AppKit child window via `addChildWindow:ordered:` with `NSWindowOrderingMode::Above`,
  and detached via `removeChildWindow:` when it closes. Never Tauri's builder `.parent()`.
- REQ-34.4.2-02 — After the `main` window deminiaturizes, the attached child login window is
  re-raised (`makeKeyAndOrderFront:`); without this it returns behind the parent.
- REQ-34.4.2-03 — Sheets are NOT used for store login windows. `beginSheet`/`endSheet` must not
  appear in `src-tauri/src/main.rs`; the login window keeps its own dismissable OS window chrome.
- REQ-34.4.2-04 — An in-field key glyph is injected into password fields in the Tauri-managed login
  window and requests autofill through a CANCELLED navigation to the reserved exfil host; the request
  payload carries only a numeric field rect plus `document.elementFromPoint` hit metadata, never field
  contents.
- REQ-34.4.2-05 — The autofill request posts a synthesized `RightMouseDown`/`RightMouseUp` pair at
  the password field's centre, computed from the WKWebView's OWN `bounds()` (CSS top-left origin →
  view bottom-left origin → window coords), clamped to those bounds, with the `elementFromPoint`
  hit id/tag/type logged. Only public API — no private selector, and no attempt to open the AutoFill
  panel directly (platform-impossible: the menu item is injected at display time and is absent from
  the NSMenu).
- REQ-34.4.2-06 — Cmd+V paste continues to work in the Tauri-managed login window; the injected glyph
  script binds no keyboard listener and consumes no key event.
- REQ-34.4.2-07 — No credential store is built or read. GameLib never reads, stores, transmits or
  logs a store password. The spike-019 DummyStore harness is a test fixture only: it never ships in a
  production build and binds loopback only.
- REQ-34.4.2-08 — All new behaviour is `#[cfg(target_os = "macos")]`-gated. Windows and Linux
  behaviour is unchanged (declared unverified, per the skill's Constraints section), and the Electron
  build is behaviourally unchanged (the 34.x additive-and-reversible invariant).
- REQ-34.4.2-09 — A blocking live gate on real macOS hardware records the measured result of every
  behaviour above; the phase does not close on a green test suite alone.
- REQ-34.4.2-10 — **The pristine Epic login window is out of scope by locked user decision
  (2026-08-04).** No symbol added by this phase is referenced from `open_pristine_epic_login_window`,
  and that function's body is unchanged by this phase. Epic's login-window UX is deferred to its own
  follow-up phase, to run after all other runners are ported and proven.

Then update the per-phase count summary line (`- Phase 34.4.2 requirements: 10 total ...`), the
`Mapped to phases:` running total (currently 145 → 155) and the `Unmapped:` line, and append a
`*Last updated: 2026-08-04 — Phase 34.4.2 (REQ-34.4.2-01..10) minted during /gsd-plan-phase 34.4.2
...*` entry to the trailing log.

Record in the minting rationale: (a) this phase had NO CONTEXT.md and NO RESEARCH.md by explicit
developer decision — spikes 019/020/021/022 (validated 2026-08-04) are the research and
`login-window-ux-macos.md` is the authoritative design source; and (b) the **Epic descope is a locked
user decision, not a planner judgement** — REQ-34.4.2-01/04/06 were narrowed from "both login
surfaces" to the Tauri-managed surface, and REQ-34.4.2-10 was minted to make the exclusion
machine-enforceable rather than a comment. No ID was deleted or renumbered.

**ROADMAP.md is already done — do not edit it.** The Phase 34.4.2 block already carries the ten IDs,
the Epic-descope note, `**Plans:** 6 plans` and six unchecked plan rows. This task's only ROADMAP
obligation is a consistency check: confirm the ten IDs you write are exactly the ten the ROADMAP
already names, in the same order. On divergence the ROADMAP is the earlier record — reconcile toward
it and note the discrepancy in the SUMMARY rather than silently renumbering.
  </action>
  <acceptance_criteria>
    - Each of REQ-34.4.2-01 through REQ-34.4.2-10 appears at least once in `.planning/REQUIREMENTS.md`.
    - REQ-34.4.2-10's row names `open_pristine_epic_login_window` and the words "user decision".
    - REQ-34.4.2-01, -04 and -06 each scope to the Tauri-managed login window and none of the three says "both surfaces".
    - The ten IDs written are byte-identical to the ten already on ROADMAP.md's Phase 34.4.2 `**Requirements**:` line, in the same order (record the comparison in the SUMMARY).
    - `git diff --exit-code .planning/ROADMAP.md` returns 0 — this task edits REQUIREMENTS.md only.
    - The `Mapped to phases:` line includes `Phase 34.4.2: 10` and the `Unmapped:` line still reads `0 ✓`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'REQ-34.4.2-10' .planning/REQUIREMENTS.md && grep -q 'REQ-34.4.2-01' .planning/REQUIREMENTS.md && git diff --exit-code .planning/ROADMAP.md && echo REQ_MINT_OK</automated>
  </verify>
  <done>REQUIREMENTS.md carries ten REQ-34.4.2 rows including the Epic-descope requirement, with a minting rationale naming the locked user decision; ROADMAP.md is unmodified by this task.</done>
</task>

<task type="auto">
  <name>Task 2: The two Tauri-managed login-window handle resolvers</name>
  <files>src-tauri/src/main.rs</files>
  <read_first>
    - src-tauri/src/main.rs (L2596–L2812 for the `humble_login_open` arm — the ONLY login surface in scope; L440–L460 for the process-static convention; L1642 for the `SendPtr` convention. Read L1613–L1891 (`open_pristine_epic_login_window`) ONLY to know its boundaries so you can leave it alone — this task must not modify a single byte of it.)
    - src-tauri/Cargo.toml (confirm the `unstable` tauri feature and the objc2 feature lists — this task must add NOTHING to them)
    - .claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md (§ Constraints)
  </read_first>
  <action>
Add two `#[cfg(target_os = "macos")]` resolvers to `src-tauri/src/main.rs`. Nothing calls them in this
plan — they are the contract plans 02 and 04 build against.

**(a) `fn login_window_ns_window(app: &AppHandle, label: &str) -> Option<usize>`** — returns the
`NSWindow` address for `label` via `app.get_webview_window(label).and_then(|w| w.ns_window().ok())`.
Returns the raw address as `usize` rather than a `Retained<NSWindow>` so the value is `Send` and
callers reconstruct it inside their own main-thread closure using this file's existing `SendPtr`
convention — document that explicitly. `None` when no Tauri-managed window carries that label; a
missing window is never an error in this file (the `humble_login_close` "already closed is healthy"
convention).

**Epic-readiness without Epic wiring.** Add a doc comment recording that a future Epic phase would
extend this with an `app.get_window(label)` fallback (the pristine shell is a plain `tauri::Window`,
reachable only that way, and the `unstable` tauri feature that enables it is already on). **Do not
write that fallback now.** The locked user decision (REQ-34.4.2-10) is that no code path in this phase
resolves the pristine window, and an unused branch that resolves it is still a code path that does.
The comment is the whole Epic-readiness deliverable.

**(b) `fn login_window_wk_webview(app: &AppHandle, label: &str) -> Option<usize>`** — returns the live
`WKWebView` address for `label`. Use `app.get_webview_window(label)` then
`with_webview(|platform| platform.inner() as usize)`; the closure runs synchronously inline on macOS
(spike 016 Q1, already relied on elsewhere in this file), so capture the address out through a
`Cell`/`Option` in the enclosing scope rather than a channel. Same `usize`/`SendPtr` rationale as (a),
documented. Plan 04 needs this specifically because the coordinate flip must use the WEBVIEW's own
`bounds()`, never the window's content view — a title-bar-sized error there silently probes the wrong
element.

Both resolvers must be side-effect free and must never log a label's URL or any page content.

Non-macOS: both items are `#[cfg(target_os = "macos")]`-gated, so Windows and Linux compile and behave
exactly as before (REQ-34.4.2-08). Do not add `#[cfg(not(target_os = "macos"))]` stubs — no caller
exists yet.
  </action>
  <acceptance_criteria>
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds. `#[allow(dead_code)]` on the two resolvers is acceptable and must carry a comment naming plan 34.4.2-02/04 as the caller.
    - `git diff --exit-code src-tauri/Cargo.toml` returns 0.
    - `src-tauri/src/main.rs` contains `fn login_window_ns_window` and `fn login_window_wk_webview`.
    - Neither resolver calls `app.get_window(` — only `app.get_webview_window(`.
    - `git diff src-tauri/src/main.rs` shows ZERO changed lines between the `#[cfg(target_os = "macos")]` at the head of `open_pristine_epic_login_window` and the next top-level item after it (REQ-34.4.2-10).
    - No new `use` of a crate not already in `src-tauri/Cargo.toml`.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && git diff --exit-code src-tauri/Cargo.toml && echo FOUNDATION_BUILD_OK</automated>
  </verify>
  <done>Both resolvers compile against the Tauri-managed surface only; the pristine Epic function is byte-unchanged; Cargo.toml unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resolver assertions + the EPIC-UNTOUCHED scope guard</name>
  <files>src/backend/__tests__/tauriShellSource.test.ts</files>
  <read_first>
    - src/backend/__tests__/tauriShellSource.test.ts (read the header doc comment and the loader — assertions MUST run through its comment-stripped source loader; read at least two existing `describe`/`test` blocks to copy their naming and structure, e.g. the `humble_login_open` arm-scoped tests around L554 and L619)
    - src-tauri/src/main.rs (the symbols added in Task 2, and `open_pristine_epic_login_window`'s boundaries)
  </read_first>
  <behavior>
    - Test 1: `login_window_ns_window` resolves via `get_webview_window` and does NOT reference `get_window(` — the pristine surface is unreachable from it by construction.
    - Test 2: `login_window_wk_webview` references `with_webview`.
    - Test 3 (SCOPE GUARD, REQ-34.4.2-10): `open_pristine_epic_login_window`'s sliced body references NONE of the symbols this phase introduces. Implement as a list constant of this phase's new symbol names, asserted absent from that slice, so plans 02/03/04 extend the list rather than writing new tests.
    - Test 4 (SCOPE GUARD, REQ-34.4.2-10): the pristine slice still contains its pre-existing critical members — `addLocalMonitorForEventsMatchingMask_handler`, `makeFirstResponder`, `NSEvent::removeMonitor`, and all five selectors `paste:`, `copy:`, `cut:`, `selectAll:`, `undo:` — proving this phase removed nothing from a surface it is not allowed to touch.
    - Test 5 (NEGATIVE, REQ-34.4.2-03): the comment-stripped source contains no `beginSheet` and no `endSheet` anywhere.
  </behavior>
  <action>
Add a `describe('Phase 34.4.2 Plan 01 — login-window handle resolvers and the Epic scope guard', ...)`
block to `src/backend/__tests__/tauriShellSource.test.ts` implementing the five behaviors above.

Add a module-scope `PHASE_34_4_2_NEW_SYMBOLS` array constant seeded with this plan's two resolver
names. Test 3's assertion iterates it. Write a comment instructing plans 02, 03 and 04 to APPEND
their own new symbol names to this array — that is how the scope guard stays current without each
plan inventing its own version of it.

Add an `extractPristineLoginFnBody(code)` helper alongside the file's existing extractors, slicing
from `'fn open_pristine_epic_login_window('` to the next top-level `'#[cfg(target_os = "macos")]'`,
asserting both bounds were found (`expect(idx).toBeGreaterThan(-1)`) before slicing — an `indexOf`
miss returning `-1` must fail loudly rather than silently slice from the file start.

Use the file's existing comment-stripped loader for every assertion — never `readFileSync` on main.rs
directly.

Write Test 3 and Test 4's names so a reader understands they encode a **locked user scope decision**
(Epic is implemented last, after all other runners are proven), not a technical limitation — and that
loosening them requires the user's decision to change, not a planner's judgement.
  </action>
  <acceptance_criteria>
    - `npx jest src/backend/__tests__/tauriShellSource.test.ts` exits 0 with all pre-existing tests in that file still passing (count >= pre-task count + 5).
    - Every new assertion reads from the comment-stripped loader; `grep -n 'readFileSync' src/backend/__tests__/tauriShellSource.test.ts` shows no new call added by this task.
    - `PHASE_34_4_2_NEW_SYMBOLS` exists, contains both resolver names, and carries the append-here comment for plans 02/03/04.
    - Mutation check performed and recorded in the SUMMARY: temporarily insert a call to `login_window_ns_window` inside `open_pristine_epic_login_window`'s body, confirm Test 3 fails, then revert.
    - `npm run codecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/backend/__tests__/tauriShellSource.test.ts 2>&1 | tail -15 && npm run codecheck</automated>
  </verify>
  <done>Five assertions pass; the EPIC-UNTOUCHED guard is installed with an extensible symbol list; the mutation check was performed and its observed failure recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote login page → login WebviewWindow | Untrusted third-party HTML/JS renders inside a window this process owns. |
| login window → Rust shell | Only established channel is the 34.4.1 seam (`dispatch_rust_channel` arms + the `LOGIN_WINDOW_EVENTS` queue). |
| Rust shell → AppKit | Raw pointers to `NSWindow`/`WKWebView` cross a `run_on_main_thread` boundary. |
| this phase → the pristine Epic surface | A locked scope boundary the code must not cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-01 | Spoofing | window lookup by label | mitigate | Both resolvers go through `app.get_webview_window(label)`, which only resolves labels this process minted via `next_login_window_label()`. No page-derived value can reach them. |
| T-34.4.2-03 | Information disclosure | resolver logging | mitigate | Both resolvers are side-effect free and log nothing — no label URL, no page title, no page content (the T-34.4.1-21/T-28-04 convention this file already enforces). |
| T-34.4.2-32 | Tampering | scope-boundary erosion (Epic re-entering by accident) | mitigate | REQ-34.4.2-10 is enforced by two source-assertion tests, not a comment: Test 3 asserts none of this phase's symbols is referenced from the pristine function, Test 4 asserts nothing was removed from it, and an acceptance criterion requires a zero-line diff across its whole body. `login_window_ns_window` deliberately omits the `get_window` fallback that would make the pristine window resolvable — an unused branch that resolves it is still a code path that does. |
| T-34.4.2-SC | Tampering | dependency surface | mitigate | This plan installs NOTHING: no npm, no pip, no cargo add. `src-tauri/Cargo.toml` must be byte-identical after the task (asserted), so `Cargo.lock`'s `EXPECTED_LOCKFILE_CRATE_NAMES` pin in `cargoFeatures.test.ts` is untouched. No Package Legitimacy Gate applies to this phase. |
</threat_model>

<verification>
- `cargo build --manifest-path src-tauri/Cargo.toml` succeeds.
- `cargo test --manifest-path src-tauri/Cargo.toml` exits 0 with no regression against the pre-plan
  baseline (record the baseline count in the SUMMARY before changing anything).
- `npx jest src/backend/__tests__/tauriShellSource.test.ts` exits 0.
- `npm run codecheck` exits 0.
- `git diff --exit-code src-tauri/Cargo.toml` and `git diff --exit-code .planning/ROADMAP.md` both
  return 0.
- `git diff src-tauri/src/main.rs` shows zero changed lines inside `open_pristine_epic_login_window`.
- Electron build unaffected: this plan touches no `src/backend/**` runtime source, only a test file.
</verification>

<success_criteria>
- REQ-34.4.2-01..10 exist in REQUIREMENTS.md, matching the ROADMAP's list, with the Epic descope
  recorded as a locked user decision.
- `login_window_ns_window` and `login_window_wk_webview` resolve handles for the Tauri-managed login
  surface only.
- The pristine Epic function is byte-unchanged and guarded by two named tests plus an extensible
  symbol list that plans 02–04 append to.
- No dependency, no Cargo feature and no behaviour changed.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-01-SUMMARY.md` when done.
</output>
</content>
