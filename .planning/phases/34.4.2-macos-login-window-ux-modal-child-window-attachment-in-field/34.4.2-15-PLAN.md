---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 15
type: execute
wave: 3
depends_on: ["34.4.2-13", "34.4.2-14"]
gap_closure: true
files_modified:
  - .claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-3.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REQ-34.4.2-04, REQ-34.4.2-05, REQ-34.4.2-09]

must_haves:
  truths:
    - "A future session reading login-window-ux-macos.md is told Recommendation #4 is FALSIFIED and Recommendation #1 is SUPERSEDED, before it reaches either mechanism's code sample"
    - "Every finding and threat surfaced by gate run 2 lives in deferred-items.md, not only inside the gate document"
    - "A fresh six-item blocking gate contract exists with verdict null, authored by a plan forbidden from running it"
    - "The five items that PASSED in gate run 2 are re-measured, not inherited — this cycle changed src-tauri/src/main.rs"
    - "Every item and every precondition has been checked for structural reachability on the specific surface it names, before any live run"
    - "No item in the contract can be blocked at execution time by something provable at review time"
  artifacts:
    - path: ".claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md"
      provides: "Corrected project knowledge — both stale recommendations marked at the point of use, not only in a footer"
      contains: "FALSIFIED"
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-3.md"
      provides: "The fresh six-item blocking gate contract, results unwritten, carrying its own structural-reachability review"
      contains: "verdict: null"
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md"
      provides: "Gate run 2's orphaned findings and the contract-authoring-defect pattern, folded into the phase ledger"
      contains: "F-34.4.2-09"
  key_links:
    - from: "34.4.2-LIVE-GATE-RERUN-3.md item 3"
      to: "PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS"
      via: "an absence item whose static half is guard-proven and whose live half is the operator's observation"
      pattern: "PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS"
    - from: "34.4.2-LIVE-GATE-RERUN-3.md Structural Reachability Review"
      to: "every item and every precondition"
      via: "a per-row REACHABLE/IMPOSSIBLE verdict with cited source evidence"
      pattern: "Structural Reachability Review"
---

<objective>
Three deliverables, all of which must land before another live run is worth an operator's evening.

1. **Correct the falsified project-knowledge artifact.**
   `.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md` is the authoritative
   design source this entire phase was built from, and it now carries **two** stale recommendations.
   **#4** ("Ship an in-field affordance that posts a synthesized right-click — that is the only way to
   surface AutoFill from inside the field") is FALSIFIED by F-34.4.2-09: the spike's evidence only
   ever showed the menu APPEARING; the last mile — whether selecting a Passwords entry writes into the
   field — was never measured until gate run 2 measured it, and it does not. **#1** ("Attach the login
   window as an AppKit CHILD window ... never use a sheet without self-dismissal") is SUPERSEDED by the
   binding sheet decision, which shipped and live-PASSED. Left as-is, this file sends the next session
   straight back down both dead ends.

2. **Fold the orphaned ledger entries in.** F-34.4.2-06 through -10, T-34.4.2-39, and the
   four-instance contract-authoring-defect pattern currently live only inside
   `34.4.2-LIVE-GATE-RERUN-2.md` and `34.4.2-12-SUMMARY.md`. `34.4.2-VERIFICATION.md` flags this as a
   consistency gap against the phase's own established discipline.

3. **Author `34.4.2-LIVE-GATE-RERUN-3.md` (`verdict: null`) AND review every item in it for structural
   reachability before it is ever run.** Gate run 2 produced **four independent structural
   impossibilities in its own contract, every one found during execution rather than review**. Four in
   one contract is a pattern, not four one-offs: contract authoring is itself a defect surface. This
   plan gives the review its own task and its own section in the contract — it is a deliverable, not a
   footnote.

**This plan is FORBIDDEN from running any gate item, starting any harness, launching
`npm run tauri:dev`, or writing any value into `verdict` / `run_date` / `items_passed`.** Authoring
and running are separate plans by design (D-E) — the same separation plans 05/06, 09/10 and 11/12
used, which is what makes every recorded FAIL in this phase trustworthy rather than back-filled from
expectation (T-34.4.2-25). Plan 34.4.2-16 is the only writer of results.

**Binding decisions to encode, not revisit:** D-A (the glyph is deleted; Cmd+V and Edit ▸ Paste are
the sole credential-entry route), D-B (the cancel strip is intact and separate), D-C (sheet
presentation is permanent; child-window attachment is off the table forever), D-D (Epic
byte-untouched), D-F (no-partial-pass).

Output: a corrected skill reference, a complete ledger, and a six-item contract every item of which
has been proven askable of the surface it names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-VERIFICATION.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-13-SUMMARY.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-14-SUMMARY.md
@.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md

<orientation>
`graphify-out/graph.json` exists. Task 3's reachability review repeatedly needs to confirm a source
claim — orient with `graphify explain "<symbol>"` first, then read only the specific line region.
This plan writes documents; it modifies no source.
</orientation>

<interfaces>
<!-- What gate run 2 measured, and what it proved impossible. Quoted from
     34.4.2-LIVE-GATE-RERUN-2.md, verified during planning. -->

**Gate run 2 verdict:** FAIL, `items_passed: 5`, `items_total: 6`, run 2026-08-05 by plan 34.4.2-12.
Items 1, 2, 4, 5, 6 PASS; item 3 FAIL.

**The four structural impossibilities found DURING execution** — the pattern Task 3 exists to prevent
a fifth instance of:
1. **Items 3 and 4's DummyStore arms** required a `http://127.0.0.1:17940/...` origin.
   `login_window_url_arg` (`src-tauri/src/main.rs:926`) rejects any non-`https` scheme before any OS
   window is constructed; a dedicated test at `main.rs:5725` asserts `http://www.humblebundle.com/login`
   is rejected identically. **The DummyStore harness can never be loaded in a Tauri-managed login
   sheet at all.**
2. **Precondition 4's logout preamble** required navigating to that same forbidden `http://` origin —
   identical cause, recorded `INAPPLICABLE-THIS-RUN`.
3. **Item 6(a)'s framing** ("trigger a reveal WHILE a visible sheet is separately open") is
   structurally impossible: a sheet blocks its own parent by AppKit design, so no second flow can be
   driven concurrently through the UI. Measured instead via an alternate route exercising the identical
   `if visible` gate — a **Humble disconnect**, which opens a hidden window through the same
   `seam.open(..., {visible:false})` -> `humble_login_open` path (code comment at
   `src/backend/humble/user.ts:813`).
4. **Item 2's required log line** `status=cancelled reason=window-closed` is emitted by
   `oauthLoginCapture.ts:236`, gated to `OAuthRunner = 'legendary' | 'gog' | 'nile' | 'zoom'`
   (`src/common/types/oauthLogin.ts:15`). **Humble is not an OAuth runner and structurally cannot emit
   it.** The run's three occurrences were `runner=nile` (x2) and `runner=legendary` (x1). Humble's own
   equivalent cancellation signal, observed 7 times matching the dismissals, is
   `Humble login-window cookie read UNSUPPORTED_OR_ERROR for window '<label>' -- aborting watch`.

**Log lines that still exist after plans 13/14** — confirm each against current source before writing
it into the contract, since plan 13 deleted a large region and plan 14 added new lines:
- `[shell] login-window sheet: read-back attached=true for '{label}'` (the CR-02 falsifiable signal)
- `[shell] humble_login_open: presentation requested ... sheet_presented=true`
- `[shell] humble_login_open: login cancel strip injected for '{label}'`
- `[shell] login-sheet: cancel requested for '{label}' via strip` and `... via esc`
- `re-registered as still presented so the cancel routes stay reachable` (WR-01, record-only)
- plan 14's arm / REFUSED / clear lines around `PENDING_VISIBLE_LOGIN_WINDOW`

**Log lines that MUST NOT appear any more** — their absence is a PASS signal, not a missing
observation:
- `[shell] login-window: parent deminiaturized, re-raising {N} attached child window(s)` (retired,
  plan 07)
- `[shell] humble_login_open: autofill glyph injected for '{label}'` (deleted, plan 13)
- `[shell] humble_login_open: autofill glyph injection SKIPPED for '{label}' (kill switch set to 0)`
  (deleted, plan 13)
- any `post_autofill_right_click:` line at all, including the WR-07 skip branch (deleted, plan 13)

**Contract template.** `34.4.2-LIVE-GATE-RERUN-2.md`'s own structure is the template: frontmatter;
"What changed since ..."; Evidence-capture instruction; Preconditions with per-precondition satisfier
attribution; Items, each with What-to-do / PASS condition / log lines / blank `Observed:` / blank
`Verdict:`; ITEM VERDICT SUMMARY; Harness teardown; "What a PASS does and does not close";
Threat-to-item discharge table. Add one new top-level section: **Structural Reachability Review**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the falsified skill reference, and fold gate run 2's orphaned findings into the deferred-item ledger</name>
  <files>.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md</files>
  <read_first>
    - `.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md` in full (174 lines):
      Requirements #1 (:15-16) and #4 (:20-21); §1 "Un-losable login window — AppKit child attachment"
      (:28-56); §2 "Sheets — only with self-dismissal" (:58-67); §3's channel table (:71-80, whose
      `Right-click -> AutoFill -> Passwords panel | fills` row describes the REAL right-click only);
      §4 "The in-field affordance (the shippable win)" (:82-123); and "What to Avoid"'s "Trusting a
      synthesized-event negative" bullet (:150-152) — the one bullet that pointed at the truth all
      along.
    - `.claude/skills/spike-findings-gamelib/SKILL.md` — the skill's own convention for recording a
      superseded finding, so this correction matches house style rather than inventing one.
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-2.md` IN-RUN FINDINGS in full: F-34.4.2-06,
      F-34.4.2-07, F-34.4.2-08, F-34.4.2-09 (including its explicitly-labelled HYPOTHESIS about the
      3-item secure-field menu versus the 10-item generic text menu), F-34.4.2-10, the T-34.4.2-39
      minting paragraph, and the "Contract-authoring defects — recorded as a PATTERN, four instances"
      section.
    - `.planning/phases/.../deferred-items.md` as plans 13 and 14 left it — the entry format, and which
      of these findings they already recorded. Plan 14 was instructed to add F-34.4.2-06; do not
      duplicate it, cross-reference it.
  </read_first>
  <action>
    **A. Correct `login-window-ux-macos.md` at the point of use, not only in a footer.** A session
    reading §4 for the mechanism must be told it does not work BEFORE it reaches the code sample.
    Five edits:

    1. **Requirement #4 (:20-21)** — prefix with `**FALSIFIED 2026-08-05 (F-34.4.2-09):**` and state
       what was measured, in one sentence: on real macOS hardware, in a presented sheet, on Humble's
       real login form, with a seeded Passwords entry, the synthesized right-click pops the real system
       menu with `AutoFill` present and targets the correct element (`hit_tag=Some("INPUT")`,
       `hit_type=Some("password")`) — **and the field does not fill**, while an identical REAL
       right-click in the same sheet, same field, same entry does. Name the discriminator explicitly:
       it is what rules out the sheet context, Humble's page, and the platform.
    2. **§4's heading and body (:82-123)** — retitle to mark it FALSIFIED, and add a lead paragraph
       stating precisely what the spike's evidence covered: `menu-with-autofill.png` shows the menu
       APPEARING. **Nowhere did the spike measure the last mile.** Keep the code sample and the
       coordinate maths as historical record — they are correct as far as they go and cost real work —
       clearly marked as belonging to a mechanism that was shipped, measured, and then deleted.
    3. **Record the mechanism as a HYPOTHESIS, labelled as such.** This project's own recorded lesson
       is that when two readings of a measurement both fit, you build the discriminator rather than
       ship the nicer one. The hypothesis: a real click yields the 3-item secure-field menu while a
       synthesized one yields the 10-item generic text menu, so the synthesized event lands in a
       generic text context with no secure-field target bound, and the AutoFill panel opens with
       nothing to write to. **This is exactly what "What to Avoid"'s "Trusting a synthesized-event
       negative" bullet (:150-152) already warned about** — cross-reference the two so the file's own
       internal contradiction is visible rather than latent. Do NOT state it as a conclusion.
    4. **Requirement #1 (:15-16), §1 (:28-56) and §2 (:58-67)** — mark SUPERSEDED 2026-08-05 by the
       binding operator decision: the login window is presented as an **AppKit sheet** with a mandated,
       always-present in-page cancel strip plus a page-independent bare-Esc monitor. §2's own live
       observation ("the operator could not dismiss it at all") is precisely the trap the cancel strip
       was built to close, and gate run 2 PASSED both dismissal routes on real hardware — so §2 is not
       merely superseded, its stated objection is answered. Note that child-window attachment was
       itself measured live-broken (F-34.4.2-01/-02: unresponsive after minimize/restore, could not be
       closed), so §1's measured table is not a safe fallback either. **Child-window attachment is off
       the table permanently (D-C) — say so in those words.**
    5. **Add a "Current status (2026-08-05)" block near the top**, before a reader reaches either stale
       recommendation: sheet presentation + cancel strip + bare Esc = SHIPPED and live-PASSED; in-field
       autofill affordance = BUILT, MEASURED, FAILED, DELETED; **Cmd+V and Edit ▸ Paste are the sole
       credential-entry route**, live-proven; a REAL right-click still reaches AutoFill and still fills
       — that channel was never this project's to break and is unaffected by the deletion.

    Also correct §3's nuance while you are in the file: the operator's controlled observation
    **partially falsifies** the blanket claim that the AutoFill panel's search box unconditionally
    rejects typed input. It is **order-dependent, not field-dependent** — the first field searched
    works, the second beeps, reproduced in both orderings (username-then-password and
    password-then-username). Measured on the REAL right-click path, so it characterises the macOS panel
    itself, not this project's code.

    **B. Fold the orphaned entries into `deferred-items.md`**, one entry each, in the ledger's
    established found-during / symptom / scope / disposition format:
    - **F-34.4.2-07** — the pre-presentation window letting a second flow queue behind the first.
      Disposition: **MITIGATED by plan 14** (the single-flight guard); live discharge pending the new
      gate's single-flight item. Cross-reference T-34.4.2-39.
    - **F-34.4.2-08** — the glyph tofu (`String.fromCharCode(128273)` truncating U+1F511 to an
      unassigned PUA codepoint). Disposition: **MOOT — superseded by plan 13's deletion, deliberately
      NOT fixed.** Record the one-line fix that was identified and not applied, so a later session does
      not rediscover it and helpfully apply it to a symbol that no longer exists.
    - **F-34.4.2-09** — the falsified design premise. Disposition: **CLOSED by scope correction** (D-A
      plus plan 13's deletion plus the REQ-34.4.2-04/-05 rewrite), naming this task's own
      skill-reference correction as the knowledge-artifact half of that closure.
    - **F-34.4.2-10** — Humble disconnect's storage wipe times out (`clearHumbleStorage failed
      (continuing): Error: humble_login_clear_storage:timeout`, 20 s after a successful cookie census
      of 37 deleted / 34 surviving-non-Humble). Cookies cleared, localStorage/sessionStorage NOT.
      Non-fatal by design, but the result is a logout that leaves site storage intact. Disposition:
      **logged, not fixed, no owning plan** — a real functional gap in the logout path, outside this
      phase's scope, named by no gate item's PASS condition. **Flag it explicitly for whoever next
      touches the Humble disconnect path.**
    - **T-34.4.2-39** — cross-reference only. Its authoritative record is `34.4.2-PLATFORM-SCOPE.md`
      §5's fifth and seventh update tables. Do not duplicate the analysis.
    - **The four-instance contract-authoring-defect pattern** — its own entry, because it is a process
      finding rather than a code finding. List all four instances with their causes, and record that
      this cycle's response is a dedicated structural-reachability review step (Task 3) rather than a
      promise to be more careful. Disposition: **ADDRESSED by plan 15 Task 3** — and it is measurable:
      the count of impossibilities discovered during the next run should be zero.
    - **F-34.4.2-06** — cross-reference plan 14's entry; do not duplicate it.
  </action>
  <verify>
    <automated>SK=.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md; DI=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md; grep -q FALSIFIED "$SK" && grep -q SUPERSEDED "$SK" && grep -q 'Cmd+V' "$SK" && grep -q 'F-34.4.2-07' "$DI" && grep -q 'F-34.4.2-08' "$DI" && grep -q 'F-34.4.2-09' "$DI" && grep -q 'F-34.4.2-10' "$DI" && grep -q 'T-34.4.2-39' "$DI" && test -z "$(git diff -- .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md)" && echo SKILL_CORRECTED_LEDGER_FOLDED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `login-window-ux-macos.md` marks Recommendation #4 FALSIFIED **and** §4 FALSIFIED, with the real-versus-synthesized discriminator stated, and the menu-appearance-versus-fill distinction stated explicitly.
    - The same file marks Recommendation #1, §1 and §2 SUPERSEDED, states that child-window attachment is permanently off the table, and records that §1's own mechanism was measured live-broken (F-34.4.2-01/-02).
    - A "Current status (2026-08-05)" block appears near the TOP of the file, before either stale recommendation, and names Cmd+V / Edit ▸ Paste as the sole credential-entry route.
    - The synthesized-versus-real menu-shape explanation is labelled a HYPOTHESIS, not a conclusion, and cross-references the file's own "Trusting a synthesized-event negative" bullet.
    - §3's AutoFill-search-box claim is corrected to order-dependent rather than field-dependent, with both reproduced orderings named.
    - `deferred-items.md` contains an entry for each of F-34.4.2-07, -08, -09, -10, the T-34.4.2-39 cross-reference, and the contract-authoring-defect pattern (asserted by the verify command); F-34.4.2-06 is cross-referenced, not duplicated.
    - No previously-deferred finding (WR-02, WR-05, WR-08, IN-01, IN-03, IN-04) is re-opened or re-litigated.
    - All three prior gate documents are byte-unchanged (asserted by the verify command, which echoes `SKILL_CORRECTED_LEDGER_FOLDED_OK`).
    - This task ran no gate item and started no harness.
  </acceptance_criteria>
  <done>The skill reference tells the next session that both of its load-bearing recommendations are dead, at the point where that session would otherwise act on them; and every finding gate run 2 surfaced lives in the phase ledger with an explicit disposition rather than only inside a gate transcript.</done>
</task>

<task type="auto">
  <name>Task 2: Author 34.4.2-LIVE-GATE-RERUN-3.md — six items, verdict null, the five prior PASSes re-measured rather than inherited</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-3.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-2.md` in full — the structure to reproduce, every
      item's What-to-do / PASS condition / log lines, the Preconditions section with its
      per-precondition satisfier attribution, the sticky-jar trap, the `screencapture -R` guidance, the
      "What a PASS does and does not close" section, and the threat-to-item discharge table.
    - `.planning/phases/.../34.4.2-13-SUMMARY.md` and `34.4.2-14-SUMMARY.md` — the exact log-line
      literals plans 13 and 14 removed and added. **Write into the contract only literals those
      SUMMARYs confirm exist in current source**, not literals carried over from the previous contract.
    - `src-tauri/src/main.rs` — grep for each log literal you intend to require, and record the line
      number beside it in the contract. A contract that names a log line the binary cannot emit is
      exactly defect instance 4 from gate run 2.
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` §5's sixth and seventh update tables (plans 13
      and 14) — the threat dispositions this contract's discharge table must reflect.
  </read_first>
  <action>
    Author `34.4.2-LIVE-GATE-RERUN-3.md`. **Do NOT edit `34.4.2-LIVE-GATE-RERUN-2.md`** — it holds a
    measured FAIL that is real history.

    **Frontmatter:** `plan: 15`, `type: live-gate`, `status: pending`, `blocking: true`,
    `verdict: null`, `run_date: null`, `items_total: 6`, `items_passed: null`, plus a `notes:` block
    stating that this contract was authored before any live work by a plan forbidden from running it,
    that plan 34.4.2-16 is the only writer of results, and that it SUCCEEDS `-RERUN-2.md` without
    erasing that document's own FAIL.

    **"What changed since gate run 2" section**, in this order:
    1. **The in-field autofill glyph is DELETED, not disabled** (operator decision D-A, plan 13).
       Name what went: the glyph script, the parser, the sentinel arm, the poster, the event
       synthesizer, the debounce, the coordinate helpers, the WKWebView resolver, and the
       `GAMELIB_AUTOFILL_GLYPH` kill switch. Name the reason: F-34.4.2-09 — the synthesized right-click
       surfaces `AutoFill` and targets the correct element but never fills, while a real right-click
       does. **Cmd+V and Edit ▸ Paste are now the sole credential-entry route**, which makes item 4
       load-bearing rather than a side check.
    2. **A single-flight guard on visible login windows** (plan 14, T-34.4.2-39): a second visible
       login window is refused at the shell entry point while another is pending or presented, so
       AppKit is never asked to queue a second sheet. State the residual honestly — the guard cannot
       cover the interval between the user's click and `humble_login_open`, which on the Amazon path is
       the ~7-8 s nile spawn (F-34.4.2-06, pre-existing, not a 34.4.2 regression).
    3. **Everything gate run 2 PASSED must be RE-MEASURED, not inherited.** Say why in the contract
       itself: this cycle changed `src-tauri/src/main.rs` substantially, and this phase's own repeated
       rule is that a fix — or a deletion — does not inherit a previous run's result. Items 1, 2, 4 and
       6 were PASSes against different source.

    **Evidence-capture instruction, its own subsection before the Items.** The runner MUST launch
    `npm run tauri:dev` with stderr merged into stdout and piped through `tee /tmp/gamelib-dev.log`.
    State the reason inline: `gamelib.log` at `~/Library/Logs/GameLib/` carries **no**
    `[shell]`-prefixed Rust lines in any archived file, so without this transcript every `[shell]`
    assertion in the contract is INCONCLUSIVE rather than negative — and capturing it is what made
    rounds 2 and 3 of the debug arc diagnosable at all.

    **Preconditions.** Carry forward only those that survive Task 3's reachability review, each with
    its satisfier attribution (machine-satisfiable at preflight versus operator-at-gate-time), and each
    left BLANK for plan 16's preflight to fill from a measured check. Two changes are already known and
    must be applied here rather than discovered live:
    - **The DummyStore harness precondition and its logout preamble are REMOVED, not deferred.**
      `login_window_url_arg`'s https-only gate structurally forbids `http://127.0.0.1:17940` in a
      Tauri-managed login sheet. Starting a harness that no item can reach wasted a PID, a teardown
      step and a preflight slot last run. State the removal and its reason in the contract so a future
      author does not re-add it. Consequently the **Harness teardown** section becomes an explicit
      "no harness is started this run" statement rather than a PID record.
    - **A seeded Passwords-app entry is no longer a precondition of any item's PASS bar.** With the
      glyph gone, no item requires the AutoFill panel to fill anything. Item 3 asks the operator to
      confirm a REAL right-click still reaches AutoFill, which is a record-only observation about a
      platform channel this project does not own — say so, and mark an unseeded entry as making that
      one sub-check UNOBSERVABLE rather than failing the item.
    Add the transcript-capture precondition, carried from the last contract.

    **The six items.**

    - **Item 1 — Sheet presentation, un-losability, and survival of the minimize/restore cycle
      (REQ-34.4.2-01/-02).** Sub-checks (a) click the main window: the sheet stays in front — note
      explicitly that a sheet BLOCKS its parent by AppKit design, so the main window declining clicks
      is correct and is NOT a failure; (b) drag the main window: the sheet moves with it; (c) minimize:
      the sheet follows into the Dock; (d) restore: the sheet is visible and in front; (e) **MANDATORY**
      — immediately after restore, type into the password field AND activate the cancel strip; both
      must work. This is F-34.4.2-01, the defect that started the phase, turned into a PASS condition.
      **Gate run 2's sub-check (f) is DELETED** — it required a glyph click, and there is no glyph.
      Machine evidence: require BOTH `read-back attached=true for '{label}'` and
      `presentation requested ... sheet_presented=true`, and state inline why `sheet_presented=` alone
      is unacceptable (it was CR-02's unfalsifiable signal, true on nothing more than "the dispatch did
      not time out"). The retired `parent deminiaturized, re-raising` line must be ABSENT — its absence
      is a PASS signal. PASS = all five sub-checks.
    - **Item 2 — Dismissability under sheet semantics (REQ-34.4.2-03).** A fresh sheet per route.
      Route (a) cancel-strip click, REQUIRED PASS, plus the sub-check that exactly ONE "Cancel sign-in"
      control is visible including on Humble's real iframe-heavy form (spike 013: 5 of 8 navigations
      are third-party frames). Route (b) bare Esc, REQUIRED PASS. Route (c) Cmd+W, RECORD ONLY — a
      negative is a recorded fact, not a FAIL. Route (d) titlebar close button, NOT A ROUTE — a sheet
      has none; record its absence as EXPECTED and do not score it. After each of (a) and (b), confirm
      the MAIN WINDOW is interactive again. **Specify the correct log evidence up front:**
      `[shell] login-sheet: cancel requested for '{label}' via strip` / `via esc`, plus Humble's own
      cancellation signal `Humble login-window cookie read UNSUPPORTED_OR_ERROR for window '<label>'
      -- aborting watch`. **Do NOT require `status=cancelled reason=window-closed`** — it is
      OAuth-runner-only and Humble structurally cannot emit it (gate run 2 contract defect 4, now
      fixed at authoring time rather than discovered live). Record-only WR-01 sub-check: if a dismissal
      route ever appears not to take effect, look for `re-registered as still presented so the cancel
      routes stay reachable` and record whether a second attempt then succeeds.
    - **Item 3 — NEW: the autofill affordance is GONE, and Cmd+V is the route that remains
      (REQ-34.4.2-04/-05).** This item replaces gate run 2's item 3 and absorbs its item 5. Sub-checks:
      (a) no key glyph, icon or any injected control other than the cancel strip appears in any field
      of a Tauri-managed login sheet — on Humble, observed visually; (b) the transcript contains ZERO
      `post_autofill_right_click` lines, ZERO `autofill glyph injected` lines and ZERO
      `autofill glyph injection SKIPPED` lines; (c) **relaunch with `GAMELIB_AUTOFILL_GLYPH=0` and
      confirm NOTHING changes** — no glyph either way, no new log line, the cancel strip still present
      and still dismissing. This inverts gate run 2's item 5: the kill switch is not merely unused, it
      has no reader, and an env var that still produced a differing log line would mean the deletion
      was incomplete; (d) RECORD ONLY — a REAL right-click on the password field still reaches the
      system AutoFill menu and still fills, confirming the deletion took away only this project's own
      synthesized path and not the user's remaining channel. Mark (d) UNOBSERVABLE, not FAILED, if no
      Passwords entry is seeded. PASS = (a), (b) and (c). Note the static half is already
      guard-proven by `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS`; this item is the live half.
    - **Item 4 — Cmd+V paste, now the SOLE credential-entry route (REQ-34.4.2-06).** Copy a string,
      Cmd+V into the password field of a Tauri-managed login sheet, and also test Edit ▸ Paste from the
      menu bar. Sub-checks: Esc does NOT fire while typing ordinary characters; Cmd+V is NOT swallowed
      by the Esc local monitor; a divergence between Edit ▸ Paste and Cmd+V isolates a
      key-equivalent-delivery regression from a first-responder regression. **Raise the bar from gate
      run 2:** because this is now the only credential-entry route the product offers, require that a
      real Humble login **completes** using paste alone, evidenced by the aggregate sync-completion
      line (aggregate counts only — no individual gamekey or order identifiers, per this project's
      standing identifier-redaction discipline). Humble only; the DummyStore arm is removed with its
      precondition.
    - **Item 5 — NEW: single-flight login initiation (T-34.4.2-39, F-34.4.2-07).** Two arms.
      **Arm 1, the pre-presentation window:** click Amazon sign-in (the slow nile path), and during its
      ~7-8 s delay click Humble sign-in. Exactly one sheet must present; the other initiation must be
      REFUSED with plan 14's refusal line in the transcript; and — the decisive sub-check — **on
      dismissing the presented sheet, NO second sheet may appear.** That unrequested arrival is the
      precise failure gate run 2 observed. **Arm 2, the presented case:** with a sheet already up,
      attempt a second login; it must be refused the same way, with the sheet still dismissable
      afterwards by both routes. Also record, RECORD ONLY but explicitly: **what the user actually sees
      when a login is refused** — an error, a toast, a spinner that stops, or nothing at all. Plan 14's
      SUMMARY records what the code does; this item records what the human perceives, and "nothing at
      all" is a legitimate recorded outcome, not a FAIL, since surfacing it would have required an
      out-of-scope cross-platform change. PASS = both arms refuse, no unrequested sheet arrives, and
      the surviving sheet is still dismissable.
    - **Item 6 — Hidden-window non-interference and Epic untouched (REQ-34.4.2-04/-10,
      T-34.4.2-32).** (a) Use the **Humble disconnect** route — do NOT reuse gate run 2's impossible
      "while a visible sheet is separately open" framing; state in the contract that the framing was
      retired as structurally impossible and why. The hidden window must receive no cancel strip, no
      glyph, no sheet-presentation line, **and — new this cycle — must NOT be refused by plan 14's
      single-flight guard**, which is scoped to visible windows only. A refused hidden window would be
      a plan-14 regression and fails this item. Positive proof the hidden window existed and functioned
      is the cookie-census line (aggregate counts only). (b) Open the Epic login window once — the only
      place Epic is touched by this entire gate, an ABSENCE check, live confirmation of the locked
      scope decision. Required absences: no glyph, no cancel strip, no sheet-presentation line for its
      label, and no single-flight refusal attributable to it. If Epic's login is blocked by its own
      parked pre-auth 403, that is EXPECTED and does not fail this item, provided the absences remain
      observable.

    **ITEM VERDICT SUMMARY** table with six rows and empty verdict cells. Every `Observed:` and
    `Verdict:` field blank. **Harness teardown** section stating no harness is started this run, with
    the reason. **"What a PASS does and does not close"** carried forward and updated: macOS only,
    nothing proven about Windows or Linux; nothing proven about Epic's login UX (6(b) is an absence
    check); **and new this cycle — a PASS does not prove the AutoFill panel works, only that this
    project no longer tries to drive it**, and it does not close F-34.4.2-06's spawn tax or
    F-34.4.2-10's incomplete logout.

    **Threat-to-item discharge table** cross-referenced against `34.4.2-PLATFORM-SCOPE.md` §5's sixth
    and seventh updates: T-34.4.2-05 and -33 by item 1; -07, -33, -34, -37, -38 by item 2; -10/-11/-12/
    -14/-15/-16/-17/-18/-19/-21/-36 recorded as RETIRED BY DELETION with item 3 as their live
    confirmation of absence; -20 and -40 as source-closed with item 3 as corroboration; -22 and -35 by
    item 4; **-39 and -41 by item 5**; -32 by item 6(b).

    **Write no result anywhere.** Every `Observed:` line is followed by nothing but whitespace.
  </action>
  <verify>
    <automated>G=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-3.md; test -f "$G" && grep -q '^verdict: null' "$G" && grep -q '^run_date: null' "$G" && grep -q '^items_passed: null' "$G" && test "$(grep -c '^### Item' "$G")" = "6" && grep -q 'attached=true' "$G" && grep -q 'tee /tmp/gamelib-dev.log' "$G" && test "$(grep -c 'status=cancelled reason=window-closed' "$G")" -le 1 && test "$(grep -cE '^\*\*Verdict:\*\* *(PASS|FAIL|NOT ATTEMPTED|BLOCKED)' "$G")" = "0" && test -z "$(git diff -- .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md)" && echo CONTRACT_AUTHORED_RESULTS_NULL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `34.4.2-LIVE-GATE-RERUN-3.md` exists; frontmatter reads `verdict: null`, `run_date: null`, `items_passed: null`, `items_total: 6`, `blocking: true` (asserted by the verify command).
    - `grep -c '^### Item'` returns exactly 6.
    - Item 1 requires BOTH `read-back attached=true` and `sheet_presented=true` and explains inline why the latter alone is unacceptable; it contains the mandatory type-then-dismiss-after-restore sub-check; and it contains NO glyph-dependent sub-check.
    - Item 2 specifies Humble's own cancellation signal as its evidence. Any mention of `status=cancelled reason=window-closed` appears at most once and only as a recorded explanation of why it is NOT required (asserted by the verify command).
    - Item 3 requires ZERO `post_autofill_right_click` lines, ZERO glyph-injection lines, and a `GAMELIB_AUTOFILL_GLYPH=0` arm whose PASS condition is that **nothing changes**.
    - Item 4 requires a real Humble login to COMPLETE using paste alone, with aggregate-only evidence.
    - Item 5 exists, covers both the pre-presentation and already-presented arms, and its decisive sub-check is that no unrequested sheet arrives on dismissal.
    - Item 6(a) uses the Humble-disconnect route, states why the old concurrency framing was retired, and adds the not-refused-by-the-single-flight-guard sub-check.
    - No DummyStore precondition, no port 17940 reference as a required step, and the Harness teardown section states plainly that no harness is started.
    - The contract names the transcript-capture launch command explicitly and states that `gamelib.log` carries no `[shell]` lines.
    - Every log literal required by any item was grep-confirmed present in current `src-tauri/src/main.rs` during authoring, with its line number recorded beside it in the contract.
    - Every `Observed:` and `Verdict:` field is empty; no PASS/FAIL/NOT ATTEMPTED/BLOCKED string appears in any Verdict field (asserted by the verify command, which echoes `CONTRACT_AUTHORED_RESULTS_NULL_OK`).
    - `34.4.2-LIVE-GATE-RERUN-2.md`, `-RERUN.md` and `34.4.2-LIVE-GATE.md` are all byte-unchanged.
    - `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing at task end — this task started no harness.
  </acceptance_criteria>
  <done>A fresh six-item blocking contract exists with every PASS condition fixed before the outcome is known, its items rebuilt around what this cycle actually changed, its impossible DummyStore and OAuth-log-line requirements removed at authoring time rather than at execution time, and not one result field written.</done>
</task>

<task type="auto">
  <name>Task 3: Structural Reachability Review — prove every item and precondition is askable of the surface it names, before the operator burns a sixth live run</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-3.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - The contract Task 2 just wrote, in full — this task audits it and may send it back for edits.
    - `.planning/phases/.../34.4.2-VERIFICATION.md`'s "Process note" paragraph in Gaps Summary — the
      verifier's own framing: four independent structural impossibilities in one contract, none of
      which produced a false PASS or FAIL, but whose pattern means contract review should become its
      own step rather than be discovered live for a fifth time.
    - `src-tauri/src/main.rs:920-960` — `login_window_url_arg`'s https-only gate and
      `login_window_title`, plus the test at `main.rs:5725` asserting an `http://` login URL is
      rejected. This is the single most productive source of contract impossibilities in this phase's
      history (it killed two sub-checks and one precondition last run).
    - `src/common/types/oauthLogin.ts` — the `OAuthRunner` union, and `src/backend/sidecar/oauthLoginCapture.ts`
      around its `status=cancelled` emission, so runner-gated log lines are never required of a
      non-OAuth surface again.
    - `src/backend/humble/user.ts` around the disconnect path's `seam.open(..., {visible:false})` call
      and its comment — the alternate route item 6(a) now uses.
    - `.planning/phases/.../34.4.2-14-SUMMARY.md` — plan 14's recorded answer to "what does the user
      see when a login is refused", which item 5's record-only sub-check must be consistent with.
    - `.planning/REQUIREMENTS.md:731-742` — the ten REQ rows, to confirm every one is still claimed by
      some item of this contract or explicitly noted as source-closed.
  </read_first>
  <action>
    Add a **`## Structural Reachability Review`** section to `34.4.2-LIVE-GATE-RERUN-3.md`, placed
    BEFORE the Items section so a runner meets it first. It is a first-class deliverable of this cycle,
    not a footnote — gate run 2's own record names contract authoring as a defect surface with four
    confirmed instances.

    **A. Review every item, sub-check and precondition** against the specific surface it names, and
    record a table with one row per checkable thing: `Item/sub-check | Surface it names | What it asks
    for | REACHABLE / IMPOSSIBLE / CONDITIONAL | Evidence (file:line or command)`. **Evidence must be
    a source citation or a real command output — never an assertion.** Apply, at minimum, these four
    tests drawn from the four confirmed defect classes:
    1. **Origin/scheme reachability.** Does any sub-check or precondition require a URL that
       `login_window_url_arg` will reject? Anything non-`https` in a Tauri-managed login window is
       IMPOSSIBLE. (This is what killed items 3(a), 4's DummyStore half and precondition 4 last run.)
    2. **Concurrency reachability.** Does any sub-check require two things to be driven at once through
       a UI where a presented sheet blocks its own parent? If so it is IMPOSSIBLE as written and needs
       an alternate route that exercises the identical code gate. **Item 5 is the one place this cycle
       deliberately asks about two flows — verify it is reachable, because it drives the SECOND
       initiation during the FIRST's pre-presentation window, when no sheet is up yet and the parent is
       still interactive. That is precisely why the failure existed. Confirm that reasoning explicitly
       rather than assuming it.**
    3. **Log-line emitter reachability.** For every `[shell]` or sidecar literal any item requires:
       grep it in current source, record the file and line, and confirm the surface under test can
       actually reach that emitter. A runner-gated line must never be required of a non-OAuth surface
       (defect 4). A line deleted by plan 13 must appear only in the MUST-NOT-APPEAR list.
    4. **Absence-observability.** For every required ABSENCE (item 3's zero-glyph, item 6's four Epic
       absences, the retired re-raise line), confirm that the presence case would actually have
       produced an observable signal. **An absence assertion over a code path that logs nothing on
       success proves nothing** — say so per row where it applies, and state what makes the absence
       meaningful (for the glyph: injection logged on both arms before deletion, so its silence now is
       informative; for the poster: its own source comment stated it logs on every path with no silent
       branch, which is what made gate run 2's zero-lines conclusion sound).

    **B. Fix what the review finds, in the contract, now.** Any row marked IMPOSSIBLE must be rewritten,
    replaced with an equivalent-route alternative, or deleted — before this plan ends. **Do not leave a
    known-impossible item for the runner to discover.** Record every change made as a result of the
    review in the review section itself, so the review is auditable rather than self-congratulatory:
    if it found nothing, say so explicitly and state what it checked, because "found nothing" and
    "did not look" must be distinguishable to whoever reads this later.

    **C. Requirement-coverage cross-check.** Confirm every one of REQ-34.4.2-01..10 is either claimed by
    a named item of this contract or explicitly recorded as source-closed and not gate-dependent (as
    REQ-34.4.2-07 and -08 are). Record the mapping as a small table in the review section. Any
    requirement with no item and no source-closure note is a coverage hole — fix it before the plan
    ends.

    **D. Propagate.** Append a dated note to `.planning/REQUIREMENTS.md`'s REQ-34.4.2-09 row stating
    that `34.4.2-LIVE-GATE-RERUN-3.md` is now the contract of record, that it was structurally reviewed
    before any run, and that its `verdict` stays `null` until plan 34.4.2-16 records a measured result.
    **Tick no box.** Append to `34.4.2-PLATFORM-SCOPE.md` §5 an **EIGHTH** update table (retain all
    seven) mapping each open threat to its discharging item in the NEW contract, so no threat's
    discharge still points at the superseded `-RERUN-2.md`.

    **E. This plan writes no result.** Confirm at task end: no `Observed:` filled, no `Verdict:` filled,
    frontmatter still `verdict: null`, no `tauri:dev` launched, no harness started, no REQ box ticked.
  </action>
  <verify>
    <automated>G=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-3.md; grep -q 'Structural Reachability Review' "$G" && grep -q '^verdict: null' "$G" && test "$(grep -cE '^\*\*Verdict:\*\* *(PASS|FAIL|NOT ATTEMPTED|BLOCKED)' "$G")" = "0" && for r in 01 02 03 04 05 06 07 08 09 10; do grep -q "REQ-34.4.2-$r" "$G" || { echo "REQ-34.4.2-$r NOT COVERED"; exit 1; }; done; grep -q 'REQ-34.4.2-09' .planning/REQUIREMENTS.md && test "$(grep -c '^- \[x\] \*\*REQ-34.4.2-04\|^- \[x\] \*\*REQ-34.4.2-05\|^- \[x\] \*\*REQ-34.4.2-09' .planning/REQUIREMENTS.md)" = "0" && test "$(lsof -nP -iTCP:17940 -sTCP:LISTEN | wc -l | tr -d ' ')" = "0" && echo REVIEW_COMPLETE_NO_RESULTS_WRITTEN</automated>
  </verify>
  <acceptance_criteria>
    - The contract contains a `## Structural Reachability Review` section positioned BEFORE the Items section.
    - The review table has one row per item, sub-check and precondition, each with a REACHABLE / IMPOSSIBLE / CONDITIONAL verdict and a source citation or command output as evidence — no row's evidence column is a bare assertion.
    - All four defect-class tests (origin/scheme, concurrency, log-line emitter, absence-observability) are applied and their application is visible in the table.
    - Item 5's two-flow reachability is explicitly reasoned, not assumed: the second initiation happens during the first's PRE-presentation window, when no sheet blocks the parent.
    - Every change the review caused is listed in the review section; if it caused none, that is stated together with what was checked, so "found nothing" is distinguishable from "did not look".
    - All ten REQ-34.4.2 IDs appear in the contract, each either mapped to a named item or recorded as source-closed and not gate-dependent (asserted by the verify command).
    - `.planning/REQUIREMENTS.md`'s REQ-34.4.2-09 row names `34.4.2-LIVE-GATE-RERUN-3.md` as the contract of record; **REQ-34.4.2-04, -05 and -09 all remain `[ ]`** (asserted by the verify command).
    - `34.4.2-PLATFORM-SCOPE.md` §5 gains an EIGHTH update table mapping every open threat to its discharging item in the NEW contract; all seven earlier tables survive.
    - Frontmatter still reads `verdict: null`; no `Observed:` or `Verdict:` field is filled anywhere in the contract.
    - `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing (asserted by the verify command, which echoes `REVIEW_COMPLETE_NO_RESULTS_WRITTEN`); no `tauri:dev` process was launched by this plan.
  </acceptance_criteria>
  <done>Every item, sub-check and precondition in the new contract has been checked against the surface it names and carries a reachability verdict backed by a source citation; anything impossible was rewritten or removed here rather than left for the operator to discover; all ten requirements are accounted for; and the contract still holds no result of any kind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan author -> gate result | An author who can also record results can back-fill an outcome from expectation. Held apart by D-E. |
| contract text -> the surface it names | A contract can demand evidence a surface structurally cannot produce. Four confirmed instances last run; this is the boundary Task 3 polices. |
| stale project knowledge -> a future session's design | `login-window-ux-macos.md` is consulted as authoritative. Two of its recommendations are now dead, and it is the file this phase was built from. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-25 | Repudiation (a result back-filled from expectation) | `34.4.2-LIVE-GATE-RERUN-3.md` | mitigate — CLOSED by process | PASS conditions are fixed here, before the outcome is known; `verdict`/`run_date`/`items_passed` stay `null`; this plan is forbidden from running any item, starting any harness, or launching `tauri:dev`, and an acceptance criterion asserts every `Observed:` field is empty. Plan 34.4.2-16 is the sole writer of results. |
| T-34.4.2-26 | Repudiation (a green suite mistaken for live proof) | the whole contract | mitigate — CLOSED by process | Items 1, 2, 4 and 6 PASSED in gate run 2 against different source; the contract states in its own text that they are RE-MEASURED, not inherited. This phase's record is unambiguous: every blocking defect in 34.4.1 and 34.4.2 was found by a human driving the UI, none by a suite that was green throughout. |
| **T-34.4.2-42 (NEW)** | Repudiation (a contract demanding evidence its named surface cannot produce, wasting a live run and yielding an uninterpretable result) | contract authoring | mitigate — process-fixed | Task 3's Structural Reachability Review, with four named defect-class tests, a per-row source-cited verdict, and a requirement that any IMPOSSIBLE row be fixed before the plan ends. Gate run 2 produced four such defects, all found during execution; none caused a false PASS or FAIL, but each cost live-run time an operator does not get back. Measurable: the count discovered during the next run should be zero. |
| T-34.4.2-39 / T-34.4.2-41 | Spoofing (origin confusion) / DoS (single-flight latch never clears) | plan 14's guard | mitigate — source-fixed, live discharge item 5 | This plan gives T-34.4.2-39 the discharging item it has lacked since it was minted mid-gate, and gives T-34.4.2-41 the same item. The threat-to-item table and §5's eighth update both record the mapping so neither can be quietly closed without a measurement. |
| T-34.4.2-20 | Tampering (private-API drift toward a credential selector) | future login-window work | mitigate — source-closed (plan 13), corroborated by item 3 | The skill-reference correction is the knowledge half of this: §4's rewrite must keep, not weaken, the file's own finding that no public "open the Passwords panel" call exists and that the four credential-shaped selectors are not the answer. A correction that deleted §4 wholesale would remove that warning along with the falsified recommendation. |
| T-34.4.2-40 | Tampering (a deleted mechanism silently reintroduced) | `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS` | mitigate — CLOSED by source (plan 13), live corroboration item 3 | Item 3's zero-line requirements are the live counterpart of the static guard: the guard proves the symbols are absent from source, the item proves the behaviour is absent from the screen. |
| T-34.4.2-32 | Tampering (Epic scope-boundary erosion) | item 6(b) | mitigate — CLOSED by source | D-D. Item 6(b) is an ABSENCE check only and the contract must say so explicitly — it is not, and cannot be read as, a statement that Epic's login UX works. |
| T-34.4.2-27 | Tampering (a test fixture reaching production) | DummyStore harness | mitigate — CLOSED, and now moot for this gate | The harness is not started at all this run: the https-only gate makes it unreachable from a Tauri-managed login sheet. Plan 05's containment guard still asserts it never ships. Removing the precondition removes a fixture from the run entirely, which is strictly safer than starting one no item can use. |
| T-34.4.2-SC | Tampering (dependency surface) | none | mitigate — CLOSED | This plan is documents-only: no `npm`/`pip`/`cargo` install, no source change. `git diff --stat -- src src-tauri/src` must be EMPTY at plan end. |
</threat_model>

<verification>
- `login-window-ux-macos.md` marks #4 FALSIFIED and #1/§1/§2 SUPERSEDED, carries a Current-status block near the top, labels the menu-shape explanation a HYPOTHESIS, and preserves §4's private-selector warning.
- `deferred-items.md` contains entries for F-34.4.2-07, -08, -09, -10, the T-34.4.2-39 cross-reference, and the contract-authoring-defect pattern; F-34.4.2-06 cross-referenced not duplicated; no previously-deferred finding re-opened.
- `34.4.2-LIVE-GATE-RERUN-3.md` exists with six items, `verdict: null`, `run_date: null`, `items_passed: null`, no filled `Observed:`/`Verdict:`, no DummyStore precondition, no required `status=cancelled reason=window-closed`.
- Every log literal the contract requires was grep-confirmed in current source with its line number recorded.
- The contract carries a `## Structural Reachability Review` before its Items, with a source-cited verdict per row and every IMPOSSIBLE row already fixed.
- All ten REQ-34.4.2 IDs are accounted for in the contract; REQ-34.4.2-04/-05/-09 remain `[ ]`.
- `34.4.2-PLATFORM-SCOPE.md` §5 gains an eighth update; the seven earlier updates survive.
- All three prior gate documents byte-unchanged.
- `git diff --stat -- src src-tauri/src` is EMPTY — this plan changes no code.
- `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing at plan end; `tauri:dev` was never launched.
</verification>

<success_criteria>
The project-knowledge artifact this phase was designed from no longer recommends either of the two
things now known not to work; every finding gate run 2 produced lives in the phase ledger with an
explicit disposition; and a six-item blocking contract exists whose PASS conditions were fixed before
the outcome was known, whose items measure what this cycle actually changed rather than inheriting a
previous run's results, and every one of whose items has been proven askable of the surface it names —
so the next live run spends its time measuring behaviour instead of discovering, for a fifth time,
that the contract asked for something impossible.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-15-SUMMARY.md` when done.
Record: which of the skill reference's sections were corrected and how a future reader now meets the
correction before the stale recommendation; the full Structural Reachability Review outcome including
every row marked IMPOSSIBLE and what was done about it (and, if none were found, exactly what was
checked); the REQ-to-item coverage mapping; and the log literals that were grep-confirmed against
current source. State explicitly that this plan ran NO gate item, started NO harness, never launched
`tauri:dev`, left every result field null, ticked no requirement box, and changed no source — and that
the phase remains NOT CLOSED until plan 34.4.2-16 records a measured verdict.
</output>
