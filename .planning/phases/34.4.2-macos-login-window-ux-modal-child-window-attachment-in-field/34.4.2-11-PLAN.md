---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 11
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src-tauri/src/main.rs
  - src/backend/__tests__/tauriShellSource.test.ts
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REQ-34.4.2-01, REQ-34.4.2-02, REQ-34.4.2-03, REQ-34.4.2-04, REQ-34.4.2-05, REQ-34.4.2-06, REQ-34.4.2-09]

must_haves:
  truths:
    - "Clicking the autofill glyph on a presented login sheet cannot detach that sheet — the poster no longer re-orders a window that is already key by construction"
    - "A failed endSheet: main-thread hop can never strand the parent behind a sheet with no remaining dismissal route"
    - "The Cancel sign-in strip renders exactly once per document, in the top frame only — never duplicated inside a third-party iframe"
    - "A throw while first appending the strip can no longer kill the DOMContentLoaded retry or the MutationObserver for that document"
    - "The strip advertises no keyboard activation it cannot deliver; bare Esc remains the sanctioned keyboard route"
    - "A fresh six-item blocking gate contract exists with verdict null, authored by a plan forbidden from running it"
    - "The debug arc's three commits are propagated into the phase's platform-scope, threat and requirement records"
  artifacts:
    - path: "src-tauri/src/main.rs"
      provides: "WR-07/WR-01 AppKit-path fixes and WR-03/WR-04/IN-02 cancel-strip script fixes"
      contains: "register_presented_login_sheet"
    - path: "src/backend/__tests__/tauriShellSource.test.ts"
      provides: "Source guards for each of the five fixes, plus the extended PHASE_34_4_2_NEW_SYMBOLS inventory"
      contains: "PHASE_34_4_2_NEW_SYMBOLS"
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md"
      provides: "The fresh six-item blocking gate contract, results unwritten"
      contains: "verdict: null"
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md"
      provides: "Per-symbol rows for the debug arc's new symbols and the fourth threat roll-up update"
      contains: "SHEET_PRESENT_WKWEBVIEW_WARMUP_DELAY"
  key_links:
    - from: "post_autofill_right_click step 7"
      to: "NSWindow.isSheet()"
      via: "conditional guard around makeKeyAndOrderFront"
      pattern: "isSheet\\(\\)"
    - from: "dismiss_login_window_sheet failure arms"
      to: "PRESENTED_LOGIN_SHEETS"
      via: "register_presented_login_sheet re-insert"
      pattern: "register_presented_login_sheet\\("
    - from: "login_cancel_strip_script"
      to: "top frame only"
      via: "window.top !== window early return"
      pattern: "window\\.top"
    - from: "34.4.2-LIVE-GATE-RERUN-2.md item 1"
      to: "the debug arc's read-back signal"
      via: "attached=true is the required machine evidence, not sheet_presented= alone"
      pattern: "attached=true"
---

<objective>
Close the five `34.4.2-REVIEW.md` findings that sit directly on the paths gate items 2/3/5 must
traverse, propagate the debug arc's bookkeeping, and author a fresh blocking gate contract with no
results in it.

Purpose: gate item 1's ATTACHMENT sub-check is **CLOSED and live-proven** — the F-34.4.2-03/-04/-05
three-round arc ended with `read-back attached=true` on the operator's own hardware (commits
`751521663` → `56d4986f8` → `8b2fdb315`, recorded in
`.planning/debug/resolved/white-window-not-sheet-cr01.md`). This plan does **not** re-fix CR-01 or
CR-02 and does not re-litigate sheet-vs-child-window (binding operator decision: sheet + close
affordance). The real gap is that gate **items 2, 3, 4, 5 and 6 have never been attempted in any of
the four gate runs to date**, and five reviewed defects sit on exactly the routes those items must
travel:

- **WR-07** is the highest priority: it is the only finding that can UNDO item 1's now-proven
  attachment mid-session (a `makeKeyAndOrderFront` on a presented sheet).
- **WR-03/WR-04/IN-02** sit on item 2's own required routes (duplicate strips inside iframes, a
  strip that can silently never render, a keyboard affordance that does nothing).
- **WR-01** can permanently strand the parent behind a sheet with no dismissal route left.

Findings deliberately OUT of scope this cycle: WR-02, WR-05, WR-06, WR-08, IN-01, IN-03, IN-04.
They are recorded in `deferred-items.md`, not fixed. If execution shows one of them is a hard
blocker for items 2-6 (not merely adjacent), record that in the SUMMARY and stop — do not silently
expand scope.

**This plan is FORBIDDEN from running any gate item, starting the DummyStore harness, launching
`npm run tauri:dev`, or writing any value into `verdict`/`run_date`/`items_passed`.** Authoring the
contract and running it are separate plans by design — the same author/runner separation plans 05/06
and 09/10 used, which is what made all four prior FAIL results trustworthy rather than back-filled
from expectation (T-34.4.2-25). Plan 34.4.2-12 is the only writer of results.

Output: five landed fixes with source guards, a propagated bookkeeping record, and
`34.4.2-LIVE-GATE-RERUN-2.md` with `verdict: null`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-REVIEW.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
@.planning/debug/resolved/white-window-not-sheet-cr01.md
@.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md

<interfaces>
<!-- Contracts the executor needs. Read from the current tree and re-verified 2026-08-05 by direct
     grep — every line number below was confirmed, not carried over from an earlier session. -->

Current symbols in `src-tauri/src/main.rs`:

- `const LOGIN_CANCEL_EXFIL_PATH: &str = "/login-cancel";`                       (:1554)
- `fn is_login_cancel_request(url: &tauri::Url) -> bool`                         (:1565)
- `fn login_cancel_strip_script(exfil_host: &str) -> String`                     (:1603-1668)
    - `"try { "` opens at :1608; `__GAMELIB_LOGIN_CANCEL_STRIP__` read/set at :1609-1610
    - iframe append `(document.body || document.documentElement).appendChild(frame)` at :1620
    - strip append, same null-capable expression, at :1648
    - `strip.setAttribute('tabindex', '0')` at :1629
    - `function ensure()` at :1650; the INITIAL `ensure();` call at :1653
    - `DOMContentLoaded` listener registration at :1655
    - `observer.observe(document.documentElement, ...)` at :1663
- `#[cfg(target_os = "macos")] fn request_login_sheet_cancel(app, label, route)` (:1685)
- `#[cfg(target_os = "macos")] fn post_autofill_right_click(app, label, req)`    (:1820)
    - doc-comment step 7 at :1805; the UNCONDITIONAL `window.makeKeyAndOrderFront(None);` at :1936
    - `.makeKeyAndOrderFront(` has exactly ONE call site in the whole file today
- `const PRESENTED_LOGIN_SHEETS_CAP: usize = LOGIN_WINDOW_EVENTS_CAP;`           (:2660)
- `static PRESENTED_LOGIN_SHEETS: Mutex<Option<Vec<String>>>`                    (:2673)
- `const LOGIN_SHEET_PRESENT_WATCHDOG_TIMEOUT: Duration = 15s`                   (:2686)
- `const SHEET_PRESENT_WKWEBVIEW_WARMUP_DELAY: Duration = 250ms`                 (:2735)
- `#[cfg(target_os = "macos")] fn present_login_window_as_sheet(app, label) -> bool` (:2776)
    - the 250ms `dispatch2` deferral is set up at :2834-2848
    - CR-02 read-back (`child.isSheet()` + pointer-compared `parent.attachedSheet()`) at :2895-2900
    - registration block at :2931-2940: `if list.len() >= PRESENTED_LOGIN_SHEETS_CAP { list.remove(0); }`
      then `list.push(label.to_string());`
- `#[cfg(target_os = "macos")] fn dismiss_login_window_sheet(app, label)`        (:2971-3023)
    - `was_presented` computed and `list.retain(...)` removal FIRST at :2972-2985
    - `if !was_presented { return }` early-out at :2983
    - `let Some(child_addr) = login_window_ns_window(...) else { ... }` healthy path at :2993
    - `if let Err(e) = app.run_on_main_thread(move || { ... })` at :3005
    - `parent.endSheet(child)` at :3014
    - `let _ = rx.recv_timeout(Duration::from_secs(10));` at :3022 — result currently DISCARDED
- `humble_login_open` arm: sheet present + watchdog :4025-4064, visible-fallback :4083-4090,
  presentation record :4097-4099

`objc2_app_kit::NSWindow::isSheet()` and `::attachedSheet()` are already available under the
already-enabled `NSWindow` feature — no `Cargo.toml` change is needed (evidence:
`white-window-not-sheet-cr01.md`, 2026-08-04T00:05:00Z).
</interfaces>

<guard_constraints>
<!-- These already-passing guards constrain every change below. Violating one fails the suite.
     Every line number re-verified by direct grep 2026-08-05. -->

From `src/backend/__tests__/tauriShellSource.test.ts` (all assertions run on COMMENT-STRIPPED
source; the file is 1544 lines and its Phase 34.4.2 describe blocks start at :744 (Plan 01),
:853 (Plan 02), :996 (Plan 03), :1183 (Plan 04), :1330 (Plan 08)):

- **The exactly-once sheet-API count is enforced TWICE — Plan 01's Test 5 (:829-832) AND Plan 02's
  Test 6 (:949-951), deliberately duplicated so deleting either describe block leaves the guard in
  place.** Both assert `beginSheet_completionHandler` appears **EXACTLY ONCE** and `endSheet`
  appears **EXACTLY ONCE** in the comment-stripped file.
  → No new identifier, log string, WARN message, or test helper added to `main.rs` production code
  may contain either literal. A WARN message reading `"endSheet: hop failed"` would fail BOTH
  tests. The WARN wordings specified in Task 1 are already compliant — do not "improve" them into
  something that names `endSheet`.
- **Plan 02 Test 1/3:** `present_login_window_as_sheet(` is called exactly once, inside
  `let sheet_presented = if visible {`.
- **Plan 02 Test 2 (:899-920) / Plan 08 Test 3 (:1463-1477):** `dismiss_login_window_sheet(` is
  called exactly twice; `request_login_sheet_cancel(` exactly twice; inside
  `request_login_sheet_cancel` the dismiss-call index is strictly less than the `.close()` index.
- **Plan 08 Test 1 (:1432-1444):** `login_cancel_strip_script(` called exactly once in production
  source, inside `if visible {`, absent from BOTH branches of the `GAMELIB_AUTOFILL_GLYPH`
  statement.
- **Plan 08 Test 5/6 (:1496-1515):** the sliced `login_cancel_strip_script` body contains no
  `keydown`, `keyup`, `keypress`, `.value`, or `password`.
- **Plan 08 Test 7 (:1523-1526):** `addLocalMonitorForEventsMatchingMask_handler` appears exactly
  twice.
- **Scope guard (Plan 01 Tests 3/4 and their later extensions):** every `PHASE_34_4_2_NEW_SYMBOLS`
  member (array at :708) must be absent from `open_pristine_epic_login_window` and
  `EpicPristineNavDelegate`. **Epic stays byte-untouched (REQ-34.4.2-10).**
- **F-4 token-scoping test (the test at :559):** `.center()` must not appear in the
  `humble_login_open` arm outside its original `if visible {` block. This is why the debug arc's
  visible-fallback uses `window.show()`/`window.set_focus()` and NOT `window.center()`.

From `src-tauri/src/main.rs`'s own `mod tests` (cargo):
- **`login_cancel_strip_script_is_wrapped_in_a_single_top_level_try_catch` (:6534-6541)** asserts
  the generated script contains `"try {"` **EXACTLY ONCE**. → **WR-04 must NOT be fixed with an
  inner `try`/`catch`**, which is what the review literally suggested. Use the reordering +
  null-root-guard approach specified in Task 2 instead.
- `login_cancel_strip_script_binds_no_keyboard_listener` (:6512),
  `..._never_reads_field_value_or_password_content` (:6520),
  `..._delivers_via_a_hidden_iframe_never_location_href` (:6527),
  `..._embeds_the_exfil_host_exactly_once_as_a_json_escaped_literal` (:6488),
  `..._escapes_a_tricky_host_and_never_naively_interpolates_it` (:6495),
  `..._contains_the_sentinel_path` (:6506),
  `..._is_pure_same_host_yields_identical_output` (:6544).

**The debug arc's safety net is load-bearing and must survive these fixes intact (binding operator
constraint).** Task 1 edits code adjacent to it, so state the following as a hard invariant:
- `LOGIN_SHEET_PRESENT_WATCHDOG_TIMEOUT` (15s) must still race the WHOLE
  `present_login_window_as_sheet` call in `humble_login_open` (:4025-4064). Do not shorten it, do
  not move it inside `present_login_window_as_sheet`, do not make its firing conditional on the new
  registration helper.
- The visible-fallback (`window.show()` / `window.set_focus()` at :4083-4090) must still run
  whenever attachment is not confirmed. It must NOT gain a `window.center()` call — that would fail
  the F-4 token-scoping test at :559, which is exactly the mistake the debug arc made and reverted.
- The 250ms `dispatch2::DispatchQueue::main().after()` deferral (:2834-2848) stays nested INSIDE the
  existing `run_on_main_thread` closure. Adding a second `beginSheet_completionHandler` call site
  would fail both exactly-once guards.
- Net effect the user must never be able to reach: an invisible or unclosable login window. Every
  change below is additive to that guarantee, never a precondition of it.

**Grep-gate hygiene (this repo's standing rule).** Any assertion you add that counts a token MUST
count it in a form that your own new comments and log strings cannot satisfy. Specifically: after
Task 1, the bare token `makeKeyAndOrderFront` will appear in the doc comment, in the new WARN
literal, AND in the call — so count `.makeKeyAndOrderFront(None)` (the call form) against
comment-stripped source, never the bare token against the raw file.
</guard_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-07 and WR-01 — stop the poster detaching a live sheet, and stop a failed endSheet hop stranding the parent</name>
  <files>src-tauri/src/main.rs, src/backend/__tests__/tauriShellSource.test.ts</files>
  <read_first>
    - `src-tauri/src/main.rs:1795-1975` — `post_autofill_right_click`'s doc-comment step list
      (step 7 at :1805) and its step-7 body (:1934-1936). Note the existing WARN-line style: every
      refusal line names the label and states what was skipped.
    - `src-tauri/src/main.rs:2880-2941` — `present_login_window_as_sheet`'s CR-02 read-back and the
      registration block (:2931-2940) that Task 1 extracts into a shared helper. Preserve its
      dedup-on-insert and `PRESENTED_LOGIN_SHEETS_CAP` eviction semantics exactly (T-34.4.2-08).
    - `src-tauri/src/main.rs:2943-3023` — `dismiss_login_window_sheet` in full: the
      remove-first block, the `was_presented` early return, the `child_addr == None` healthy path,
      the `run_on_main_thread` error arm, and the unchecked `rx.recv_timeout` at :3022.
    - `.planning/phases/.../34.4.2-REVIEW.md` findings **WR-07** and **WR-01** verbatim — the fix
      shape and the failure mode each names.
    - `src/backend/__tests__/tauriShellSource.test.ts:708-745` (`PHASE_34_4_2_NEW_SYMBOLS`) and
      `:853-995` (the Plan 02 describe block, especially Test 6's exactly-once counts at :949) —
      the guard surface this task extends and must not break.
    - This plan's `<guard_constraints>` block above, including its grep-gate hygiene rule.
  </read_first>
  <action>
    **WR-07 — never re-order a window that is already a presented sheet.**

    In `post_autofill_right_click`, step 7 (`main.rs:1934-1936`), replace the unconditional
    `window.makeKeyAndOrderFront(None);` with a branch on `window.isSheet()`:
    - when `isSheet()` is FALSE, call `makeKeyAndOrderFront(None)` exactly as today (spike 022's
      measured requirement: the menu will not appear on a non-key window);
    - when `isSheet()` is TRUE, skip the call entirely — a presented sheet is key by construction —
      and emit exactly one diagnostic line so the live gate can observe which branch ran:
      `[shell] post_autofill_right_click: '{label}' target is a presented sheet -- skipping makeKeyAndOrderFront (already key, WR-07)`.

    `.makeKeyAndOrderFront(None)` must still appear exactly ONCE in the comment-stripped file after
    this change — put the single call inside the false branch, do not duplicate it into both arms.
    Update the step-7 line of the function's own doc comment (`main.rs:1805`) to record that spike
    022's non-key-window measurement was taken under the RETIRED child-window mechanism, and that
    under sheet presentation the re-order is both unnecessary and a live detachment risk.

    **WR-01 — a failed `endSheet:` hop must not remove the last dismissal route.**

    Extract the registration block currently inlined in `present_login_window_as_sheet`
    (`main.rs:2931-2940`) into a new `#[cfg(target_os = "macos")]`
    `fn register_presented_login_sheet(label: &str)` that preserves dedup-on-insert and the
    `PRESENTED_LOGIN_SHEETS_CAP` eviction verbatim (T-34.4.2-08's bounded-growth guarantee). Call it
    from `present_login_window_as_sheet` at the same point it registers today — only on confirmed
    attachment, never earlier (CR-02's invariant is not being relaxed).

    In `dismiss_login_window_sheet`, keep the remove-first ordering (T-34.4.2-08 hygiene) but make
    removal REVERSIBLE on a failed AppKit hop:
    - `run_on_main_thread` returns `Err` (:3005): re-register the label via
      `register_presented_login_sheet` before returning, and log
      `[shell] WARN: login-window dismiss: '{label}' main-thread dispatch failed -- re-registered as still presented so the cancel routes stay reachable`.
      Preserve the existing `({e})` detail in the message.
    - `rx.recv_timeout(...)` returns `Err` (timeout or disconnect): the `let _ =` at :3022 becomes a
      real match; on `Err`, re-register and log
      `[shell] WARN: login-window dismiss: '{label}' main-thread hop unconfirmed within 10s -- re-registered as still presented so the cancel routes stay reachable`.
    - The `child_addr == None` "already gone by the time Destroyed fires" path (:2993) must
      NOT re-register — that is the healthy path and it keeps the "always removed even when the
      window is gone" guarantee this doc comment already promises.

    **Guard-constraint compliance (verify before committing):** neither of the two new WARN strings,
    nor the new function name, may contain the literals `endSheet` or `beginSheet_completionHandler`
    — Plan 01 Test 5 AND Plan 02 Test 6 each count both exactly once in the comment-stripped file,
    and a string literal counts. The wording above is already compliant.

    Add `register_presented_login_sheet` to `PHASE_34_4_2_NEW_SYMBOLS` in
    `src/backend/__tests__/tauriShellSource.test.ts`, and add three source guards to a new
    `describe('Phase 34.4.2 Plan 11 — review-finding fixes on the item 2/3/5 routes')` block:
    1. `.makeKeyAndOrderFront(None)` appears exactly once in the comment-stripped production source
       (count the CALL form, not the bare token — the doc comment and the new WARN literal both
       contain the bare token by design), and the brace-matched block that call sits in has a
       condition referencing `isSheet()`.
    2. `dismiss_login_window_sheet`'s sliced body contains `register_presented_login_sheet(` at
       least twice, each at an index strictly greater than the index of `list.retain(` — removal
       still happens first, re-registration only after.
    3. `register_presented_login_sheet(` appears exactly three times as a call site in production
       source (present-path registration + the two dismiss failure arms), using the same
       negative-lookbehind style as Plan 08 Test 1 to exclude the `fn` definition itself.

    Then run the existing scope-guard mutation discipline this phase uses: confirm the new symbol
    does not appear in `open_pristine_epic_login_window` or `EpicPristineNavDelegate` (Epic stays
    byte-untouched, REQ-34.4.2-10).
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && npx jest src/backend/__tests__/tauriShellSource.test.ts 2>&1 | tail -8 && test "$(git diff --unified=0 -- src-tauri/src/main.rs | grep -c 'open_pristine_epic_login_window\|EpicPristineNavDelegate')" = "0" && echo EPIC_UNTOUCHED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0.
    - `cargo test --manifest-path src-tauri/Cargo.toml` passes with no fewer tests than the 131-pass baseline recorded in `white-window-not-sheet-cr01.md` (new tests may raise the count; a drop is a regression).
    - `npx jest src/backend/__tests__/tauriShellSource.test.ts` passes, including all pre-existing Plan 01/02/03/04/08 tests unchanged.
    - The comment-stripped source contains `.makeKeyAndOrderFront(None)` exactly once, and that call sits inside a branch whose condition reads `isSheet()`. (Do NOT assert on the bare token `makeKeyAndOrderFront` — after this task it also appears in the doc comment and in the new WARN literal.)
    - The comment-stripped source still contains `beginSheet_completionHandler` exactly once and `endSheet` exactly once — verified by Plan 01 Test 5 and Plan 02 Test 6 both staying green, not by inspection.
    - `dismiss_login_window_sheet` re-registers the label on BOTH the dispatch-`Err` and the `recv_timeout`-`Err` arms, and does NOT re-register on the `child_addr == None` path.
    - `PHASE_34_4_2_NEW_SYMBOLS` contains `register_presented_login_sheet`, and the scope-guard tests still pass with it appended.
    - `git diff --unified=0 -- src-tauri/src/main.rs` shows zero changed lines inside `open_pristine_epic_login_window` and `EpicPristineNavDelegate` (the verify command asserts this count is exactly 0 and echoes `EPIC_UNTOUCHED_OK`).
    - **Live behaviour is NOT claimed by this task.** Whether the poster's AutoFill menu actually appears on a sheet-hosted webview is gate item 3's job in `34.4.2-LIVE-GATE-RERUN-2.md`; whether the re-registration keeps a route open is recorded by gate item 2. State this explicitly in the SUMMARY.
  </acceptance_criteria>
  <done>The autofill poster no longer re-orders a presented sheet, a failed endSheet hop leaves the label registered so both cancel routes remain reachable, three new source guards fire on those invariants, and Epic is byte-untouched.</done>
</task>

<task type="auto">
  <name>Task 2: WR-03, WR-04 and IN-02 — the cancel strip renders once, in the top frame, and never silently fails to render</name>
  <files>src-tauri/src/main.rs, src/backend/__tests__/tauriShellSource.test.ts</files>
  <read_first>
    - `src-tauri/src/main.rs:1566-1668` — `login_cancel_strip_script` in full: its doc-comment
      contract bullets (:1577-1602, including the `tabindex="0"` claim at :1581), the
      `concat!`-of-single-line-pieces discipline, the exact current statement order (`try {` :1608 →
      `__GAMELIB_LOGIN_CANCEL_STRIP__` :1609-1610 → iframe append :1620 → `tabindex` :1629 → strip
      append :1648 → `function ensure()` :1650 → initial `ensure();` :1653 → DOMContentLoaded
      listener :1655 → debounce → `observer.observe` :1663), and the single top-level try/catch.
    - `src-tauri/src/main.rs:6486-6552` — the eight cargo tests over this script. In particular
      `login_cancel_strip_script_is_wrapped_in_a_single_top_level_try_catch` (:6534) asserts
      `"try {"` appears exactly once; `..._binds_no_keyboard_listener` (:6512) bans
      `keydown`/`keyup`/`keypress`; `..._never_reads_field_value_or_password_content` (:6520) bans
      `.value` and `password`.
    - `src/backend/__tests__/tauriShellSource.test.ts:1330-1544` — the Plan 08 describe block,
      especially Test 1 (:1432), Test 5 (:1496) and Test 6 (:1511).
    - `.planning/phases/.../34.4.2-REVIEW.md` findings **WR-03**, **WR-04** and **IN-02** verbatim.
    - This plan's `<guard_constraints>` block above — specifically the single-`try {` constraint,
      which rules out the review's literal WR-04 fix.
  </read_first>
  <action>
    All three changes are inside `login_cancel_strip_script`'s generated JS. Keep the existing
    `concat!`-of-single-line-pieces discipline: every JS string literal uses single quotes so each
    source line keeps an EVEN raw double-quote count (`longRunningChannels.test.ts`'s
    stripper-integrity guard).

    **WR-03 — top-frame guard.** Make the FIRST statement inside the top-level `try` an early return
    for subframes: `if (window.top !== window) { return; }`. It must sit BEFORE the
    `__GAMELIB_LOGIN_CANCEL_STRIP__` read at :1609 and before the flag is set at :1610, so a
    subframe leaves no state behind at all. It is already inside the existing try, so a cross-origin
    access that throws still bails through the existing catch. Rationale to record in the doc
    comment: spike 013 measured 5 of 8 navigations on Humble's real login page as third-party
    iframes; each such frame today builds its own fixed-position "Cancel sign-in" control pinned to
    ITS top-right, plus its own MutationObserver.

    **WR-04 — the retry and observer must survive a throwing first append.** Do **NOT** add an inner
    `try`/`catch`: the cargo test at :6534 asserts exactly one `"try {"` in the generated script.
    Fix the same failure mode two ways instead:
    1. **Reorder.** Register the `DOMContentLoaded` listener (currently :1655) and call
       `observer.observe(...)` (currently :1663) BEFORE the initial `ensure()` call (currently
       :1653), making that initial `ensure()` the LAST statement inside the try. Today a throw in
       `ensure()` skips both registrations because they are sequenced after it; with the reorder,
       both are already installed when the throw happens, so the strip still gets built on
       `DOMContentLoaded` or on the next mutation.
    2. **Remove the throw source.** In both the strip append (:1648) and the iframe append (:1620),
       resolve `(document.body || document.documentElement)` into a local first and `return` when it
       is falsy, instead of calling `appendChild` on `null`. This makes the initial `ensure()` a
       no-op rather than a throw in the document states the review names.

    **IN-02 — do not advertise a keyboard affordance the strip cannot deliver.** Remove
    `strip.setAttribute('tabindex', '0')` (:1629). Keep `role="button"` and `aria-label`. Add
    `strip.setAttribute('aria-keyshortcuts', 'Escape')` — a static attribute, not a listener — so
    the sanctioned keyboard route (the page-independent bare-Esc `NSEvent` monitor) is advertised
    truthfully. **Register no keyboard listener of any kind** (REQ-34.4.2-06, T-34.4.2-22, and two
    guards that ban the token). Update the doc comment's contract bullets — including the
    `tabindex="0"` claim at :1581, which becomes false — to state plainly that the strip is a
    pointer control and Esc is the sanctioned keyboard route.

    **Do not** introduce any new string containing `.value` or `password` anywhere in this function
    (cargo test :6520 + jest Test 6).

    Add four cargo tests in `mod tests` beside the existing eight:
    - the script contains `window.top !== window`, and its index is strictly less than the index of
      `__GAMELIB_LOGIN_CANCEL_STRIP__`;
    - the LAST occurrence of `ensure();` in the script is at an index strictly greater than both the
      index of `DOMContentLoaded` and the index of `observer.observe(` — the reorder, asserted
      positionally rather than by eyeballing. (This assertion fails against today's source, where
      the last `ensure();` sits inside the debounce callback ahead of `observer.observe(` — confirm
      that it does before you make the change, so you know the test discriminates.)
    - the script does NOT contain `tabindex`, and DOES contain `aria-keyshortcuts`;
    - `"try {"` still appears exactly once — leave the existing test at :6534 untouched and add this
      as an explicit WR-04-regression note.

    Add one jest guard to the Plan 11 describe block created in Task 1: the sliced
    `login_cancel_strip_script` body contains `window.top` and does not contain `tabindex`. Slice
    the body the same way Plan 08 Tests 5/6 do (from `fn login_cancel_strip_script(` to
    `fn request_login_sheet_cancel(`) so the neighbouring glyph script's own
    `tabindex('-1')` at :1498 cannot leak into the assertion.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && npx jest src/backend/__tests__/tauriShellSource.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path src-tauri/Cargo.toml` passes, including all eight pre-existing `login_cancel_strip_script_*` tests UNCHANGED plus the four new ones.
    - The generated script contains `"try {"` exactly once — verified by the pre-existing test at :6534 still passing, not by inspection.
    - The generated script contains `window.top !== window` before `__GAMELIB_LOGIN_CANCEL_STRIP__`.
    - The last `ensure();` call site sits after both `DOMContentLoaded` and `observer.observe(`.
    - The generated script contains no `tabindex`, no `keydown`/`keyup`/`keypress`, no `.value`, no `password`; it does contain `aria-keyshortcuts`.
    - `npx jest src/backend/__tests__/tauriShellSource.test.ts` passes, Plan 08 Tests 1/5/6 included and unmodified.
    - `git diff --unified=0 -- src-tauri/src/main.rs` shows zero changed lines inside `open_pristine_epic_login_window` and `EpicPristineNavDelegate`.
    - **Live behaviour is NOT claimed by this task.** That the strip renders exactly once on Humble's real (iframe-heavy) login page, and that it dismisses the sheet, is gate item 2's job in `34.4.2-LIVE-GATE-RERUN-2.md`. State this explicitly in the SUMMARY.
  </acceptance_criteria>
  <done>The cancel strip is top-frame-only, its retry and observer are registered before anything that can throw, appends are null-root-safe, it no longer advertises unusable keyboard activation, and five new assertions hold each of those properties in place.</done>
</task>

<task type="auto">
  <name>Task 3: Author 34.4.2-LIVE-GATE-RERUN-2.md (verdict null) and propagate the debug arc's bookkeeping</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN.md` in full — the contract being succeeded. Its
      structure (frontmatter, "What changed since the failed gate", Preconditions with
      per-precondition satisfier attribution, six Items each with What-to-do / PASS condition / log
      lines / `Observed:` / `Verdict:`, ITEM VERDICT SUMMARY, "What a PASS does and does not close",
      threat-to-item table) is the template. **Items 2-6 are carried over intact — they were never
      reached.** Item 1 is the only item being restated.
    - `.planning/debug/resolved/white-window-not-sheet-cr01.md` — the **Closure** section and the
      2026-08-04T03:15:00Z **Evidence** entry, which carries the verbatim 13-line `[shell]`
      transcript ending `read-back attached=true` / `sheet_presented=true`. That transcript is the
      evidence item 1's restatement rests on, and the `/tmp/gamelib-dev.log` capture method is what
      made rounds 2 and 3 diagnosable.
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` §1 (per-symbol table + the RETIRED subsection)
      and §5 (the three existing threat roll-up updates — this task appends a FOURTH, it does not
      rewrite them).
    - `.planning/REQUIREMENTS.md` lines 731-760 (REQ-34.4.2-01..10 rows and the provenance note).
    - `.planning/phases/.../34.4.2-REVIEW.md` — the seven findings NOT being fixed this cycle
      (WR-02, WR-05, WR-06, WR-08, IN-01, IN-03, IN-04), each of which needs a `deferred-items.md`
      entry naming its file, its symptom, and why it is out of scope.
  </read_first>
  <action>
    **A. Author the fresh contract, `34.4.2-LIVE-GATE-RERUN-2.md`.** Do NOT edit
    `34.4.2-LIVE-GATE-RERUN.md` in place — it holds a measured FAIL that is real history.

    Frontmatter: `plan: 11`, `type: live-gate`, `status: pending`, `blocking: true`,
    `verdict: null`, `run_date: null`, `items_total: 6`, `items_passed: null`, and a `notes:` block
    stating that this contract was authored before any live work by a plan forbidden from running
    it, that plan 34.4.2-12 is the only writer of results, and that it SUCCEEDS
    `34.4.2-LIVE-GATE-RERUN.md` without erasing that document's own FAIL.

    Add a **"What changed since the failed rerun"** section covering, in this order:
    1. **Gate item 1's ATTACHMENT sub-check is CLOSED and live-proven.** Name commits `751521663`
       (CR-01 build-hidden + CR-02 `attachedSheet()`/`isSheet()` read-back), `56d4986f8`
       (F-34.4.2-04: timed diagnostics + the 15s `LOGIN_SHEET_PRESENT_WATCHDOG_TIMEOUT` race) and
       `8b2fdb315` (F-34.4.2-05: `beginSheet_completionHandler` wedges the main thread on a
       just-created WKWebView window; fixed with a 250ms `dispatch2::DispatchQueue::main().after()`
       deferral nested INSIDE the existing `run_on_main_thread` closure — one call site, the
       exactly-once guards intact). Quote the round-3 transcript's three decisive lines verbatim:
       the `deferred beginSheet closure entered` line with `deferred_elapsed=260.328417ms`, the
       `beginSheet dispatch call returned` line, and the `read-back attached=true` line. Copy them
       exactly as they appear in `white-window-not-sheet-cr01.md`'s 2026-08-04T03:15:00Z evidence
       entry.
    2. **What that does NOT close:** item 1's un-losability, cannot-be-ordered-behind-parent, and
       survives-minimize/restore-AND-stays-interactive sub-checks are all still UNMEASURED, and
       items 2-6 have never been attempted in ANY of the four gate runs to date.
    3. **Plan 11's five review fixes** (WR-07, WR-03, WR-04, WR-01, IN-02), each named with the gate
       item it unblocks.

    **Item 1 — RESTATED.** State up front that attachment is already proven and is not being
    re-measured. The live sub-checks are:
    - (a) **Un-losable / cannot be ordered behind the parent.** Click the main window while the
      sheet is open. The sheet stays in front. Note explicitly, as the prior contract did, that a
      sheet BLOCKS its parent by AppKit design — the main window declining clicks is correct sheet
      semantics and is NOT a failure. Only the sheet going behind, or the parent accepting input
      while the sheet is up, fails.
    - (b) **Moves with the parent.** Drag the main window; the sheet moves with it.
    - (c) **Follows the parent into the Dock** on minimize, and is visible and in front on restore.
    - (d) **MANDATORY — survives minimize/restore AND STAYS INTERACTIVE.** Immediately after the
      restore, type into the password field and then activate the Cancel sign-in strip. Typed
      characters must appear AND the strip must dismiss the sheet. **This is the exact shape of
      F-34.4.2-01, the defect that started this phase.** A sheet that types but cannot be dismissed,
      or dismisses but will not accept typed input, both FAIL.
    - (e) **NEW, WR-07 regression check.** After clicking the autofill glyph at least once (item 3's
      action), re-run sub-check (a): the sheet must still be un-losable. A first glyph click
      converting the sheet back into a free-floating window is exactly the mid-session detachment
      WR-07 names.
    - PASS = all five.

    **Machine evidence for item 1 — do not accept `sheet_presented=` alone.** Require BOTH the
    `read-back attached=true for '{label}'` line and the
    `humble_login_open: presentation requested ... sheet_presented=true` line from the captured
    `tauri:dev` transcript, quoted in the contract in the same verbatim-shape style the prior
    contract used for its own expected lines. State the reason inline: `sheet_presented=` was
    CR-02's unfalsifiable signal (it read `true` on nothing more than "the dispatch did not time
    out"); `attached=true` comes from the read-back and is the falsifiable one. If `attached=` is
    absent but `sheet_presented=true` is present, that is itself a finding. The RETIRED line about
    the parent deminiaturizing and re-raising attached child windows must NOT appear; its absence is
    a PASS signal.

    **Evidence-capture instruction (own subsection, before the Items).** The runner MUST launch
    `npm run tauri:dev` piped through `tee /tmp/gamelib-dev.log` with stderr merged into stdout (or
    an equivalent that captures the process's own stdout AND stderr) and attach that transcript.
    `gamelib.log` at `~/Library/Logs/GameLib/` carries **no `[shell]`-prefixed Rust lines at all** —
    checked against every archived `gamelib.log*` — so relying on it makes every `[shell]` assertion
    in this contract INCONCLUSIVE rather than negative. Capturing this transcript is what finally
    made rounds 2 and 3 of the debug arc diagnosable; the round-1 run without it produced an
    unlocalizable "it hung" report.

    **Items 2, 3, 4, 5 and 6 — CARRIED OVER INTACT** from `34.4.2-LIVE-GATE-RERUN.md`. Copy each
    item's What-to-do, PASS condition, operator instructions and log lines faithfully. Exactly three
    adjustments, each tied to a plan-11 fix:
    - **Item 2** — add a RECORD-ONLY sub-check: if a dismissal route ever appears not to take
      effect, check the transcript for the phrase
      `re-registered as still presented so the cancel routes stay reachable` (WR-01's new WARN) and
      record whether a SECOND attempt on either route then succeeds. Also add to route (a): confirm
      exactly ONE "Cancel sign-in" control is visible on the page, including on Humble's real
      (iframe-heavy) login form — WR-03's fix, and the surface spike 013 measured 5-of-8 navigations
      as third-party iframes.
    - **Item 3** — add: record which WR-07 branch ran, by capturing either the poster's normal
      success line or the new
      `target is a presented sheet -- skipping makeKeyAndOrderFront (already key, WR-07)` line.
      Keep all four pre-existing operator instructions verbatim (compare against a REAL right-click
      before concluding anything from a negative; region-grab the menu with
      `screencapture -x -o -R<x,y,w,h>`, never `-l<windowID>`; record `hit_id`/`hit_tag`/`hit_type`
      and FAIL on a mismatch regardless of what menu appeared; the AutoFill panel's
      non-auto-matching, input-rejecting search box is expected UX cost, not a defect).
    - **Item 5** — keep the mandatory arm-2 sub-check (with `GAMELIB_AUTOFILL_GLYPH=0` the cancel
      strip must still be present AND still dismiss the sheet) exactly as written.

    **Preconditions.** Carry all six across with their per-precondition satisfier attribution,
    including precondition 4's sticky-jar trap verbatim (the login-window cookie jar persists across
    app restarts; the logout preamble to `http://127.0.0.1:17940/logout` and the `/events` baseline
    taken AFTER it are mandatory, or a "the form was served" assertion fails for the wrong reason).
    Add a seventh precondition: **the `tauri:dev` transcript capture is in place before launch.**
    Leave every satisfaction status BLANK for plan 12's own preflight to fill from a measured check.

    Carry the **"What a PASS does and does not close"** section across (macOS only; nothing proven
    about Windows/Linux; the cancel-strip-is-gated / glyph-is-not asymmetry; nothing proven about
    Epic's login UX — item 6(b) is an absence check only).

    Carry the **threat-to-item discharge table** across and extend it with the three new threats in
    this plan's own `<threat_model>` (T-34.4.2-36/-37/-38).

    Leave every `Observed:` and `Verdict:` field blank and the ITEM VERDICT SUMMARY table present
    with empty verdict cells.

    **B. Propagate the debug arc into `34.4.2-PLATFORM-SCOPE.md`** — amend in place, never rewrite,
    never delete history.
    - §1 per-symbol table: add rows for `LOGIN_SHEET_PRESENT_WATCHDOG_TIMEOUT` (:2686),
      `SHEET_PRESENT_WKWEBVIEW_WARMUP_DELAY` (:2735) and this plan's own new
      `register_presented_login_sheet`, each with its cfg gate READ FROM the current
      `src-tauri/src/main.rs` (not assumed) plus a line number, its macOS behaviour and its
      Windows/Linux behaviour. Update `present_login_window_as_sheet`'s existing row to describe the
      CR-02 read-back, the 250ms deferral and the visible-fallback, and
      `dismiss_login_window_sheet`'s row to describe the WR-01 re-registration.
    - Record the one dependency-manifest change the debug arc made: `dispatch2 = "0.3.1"` promoted
      from a transitive dependency to a direct macOS-target dependency of `gamelib-shell`
      (`Cargo.lock` gained exactly one line; no new crate or version entered the tree). Quote a real
      `git diff --stat src-tauri/Cargo.lock` and `git diff --exit-code pnpm-lock.yaml` run from this
      session — do not assert a result you did not run.
    - §5: append a **fourth** update table, leaving the three existing ones intact. It must record:
      T-34.4.2-05 moving from OPEN-CONFIRMED-broken to **PARTIALLY DISCHARGED — attachment
      live-proven (`attached=true`, commit `8b2fdb315`), un-losability and post-restore
      interactivity still UNMEASURED**; T-34.4.2-07/-33 still OPEN-pending-gate (item 2 still never
      attempted); T-34.4.2-15 residual/-17/-21/-22 still OPEN-pending-gate with "NOT ATTEMPTED on
      all four runs" as the stated reason; T-34.4.2-32/-34/-35 unchanged, CLOSED by source; and the
      three new threats T-34.4.2-36/-37/-38 with their discharging items.

    **C. `.planning/REQUIREMENTS.md`.** Mint NO new REQ IDs; renumber and delete none. Append a
    dated note to REQ-34.4.2-01 and -02 recording that sheet ATTACHMENT is now live-proven (naming
    the commit and the `attached=true` line) while the remaining live sub-checks stay unmeasured —
    **both boxes stay `[ ]`**. Update REQ-34.4.2-03/-04/-05/-06/-09 rows to name
    `34.4.2-LIVE-GATE-RERUN-2.md`'s items as their precondition instead of the superseded
    `-RERUN.md`. Update the phase's provenance note at the foot of the section to describe this
    second gap cycle. **Tick no box** — this plan produces no live proof.

    **D. `deferred-items.md`.** Add one entry per review finding NOT fixed this cycle — WR-02
    (use-after-free race on raw `NSWindow` addresses across the worker→main hop), WR-05 (the strip
    route destroys the window synchronously inside its own WKWebView navigation-policy callback),
    WR-06 (arbitrary, not-oldest debounce eviction; refusals arm the debounce), WR-08 (harness
    containment Test 1 never walks `src-tauri/src/`; unguarded `statSync`), IN-01
    (`PRESENTED_LOGIN_SHEETS` cap eviction drops a still-presented label), IN-03
    (`is_login_cancel_request` ignores the URL scheme), IN-04 (the whole automated surface for this
    phase is source-text assertion). Each entry: the finding's file/line, its symptom in one
    sentence, why it is out of THIS cycle's scope (operator decision D-A scoped this cycle to the
    five findings on the item 2/3/5 routes), and its disposition (logged, not fixed, no owning
    plan). Add one more entry recording that the debug arc's three commits landed with **no phase
    plan documenting them** — this task is that documentation, and future arcs should route through
    a plan.
  </action>
  <verify>
    <automated>test -f .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md && grep -c 'verdict: null' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md && test "$(grep -c '^### Item' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md)" = "6" && grep -c 'attached=true' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md && grep -c 'REQ-34.4.2-' .planning/REQUIREMENTS.md && test -z "$(git diff -- .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md)" && echo PRIOR_GATE_UNCHANGED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `34.4.2-LIVE-GATE-RERUN-2.md` exists; frontmatter reads `verdict: null`, `run_date: null`, `items_passed: null`, `items_total: 6`, `blocking: true`.
    - `grep -c '^### Item'` returns exactly 6 (asserted by the verify command).
    - Item 1 requires BOTH the `read-back attached=true` line and the `sheet_presented=true` line, and states in prose why `sheet_presented=` alone is not acceptable evidence (CR-02).
    - Item 1 contains the mandatory sub-check requiring typed characters to appear in the password field after the Dock restore AND the cancel strip to respond.
    - Item 1 contains the WR-07 post-glyph-click re-check of un-losability.
    - The contract names the transcript-capture launch command explicitly (`tee /tmp/gamelib-dev.log` with stderr merged) and states that `gamelib.log` carries no `[shell]` lines.
    - Item 2 contains a check that exactly ONE "Cancel sign-in" control is visible, including on the real Humble form.
    - Item 5 still contains the mandatory arm-2 cancel-strip sub-check.
    - `grep -c '17940'` returns at least 2 and the sticky-jar trap sentence is present; the `screencapture` guidance uses `-R`, not `-l`.
    - Every `Observed:` line is followed by nothing but whitespace or a blank placeholder.
    - `34.4.2-PLATFORM-SCOPE.md` contains `SHEET_PRESENT_WKWEBVIEW_WARMUP_DELAY`, `LOGIN_SHEET_PRESENT_WATCHDOG_TIMEOUT` and `register_presented_login_sheet` rows; all three pre-existing §5 update tables are still present and a fourth is appended.
    - `grep -c 'REQ-34.4.2-' .planning/REQUIREMENTS.md` returns the same count as before this task — no ID added, deleted or renumbered — and **no REQ-34.4.2 box changed from `[ ]` to `[x]`**.
    - `deferred-items.md` gains one entry per out-of-scope review finding (WR-02, WR-05, WR-06, WR-08, IN-01, IN-03, IN-04) plus the undocumented-debug-commits entry.
    - `34.4.2-LIVE-GATE-RERUN.md` is byte-unchanged (the verify command asserts an empty diff and echoes `PRIOR_GATE_UNCHANGED_OK`).
    - **No command in this task launched `npm run tauri:dev`, started the DummyStore harness, or bound port 17940** — `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing at task end.
  </acceptance_criteria>
  <done>A fresh six-item blocking contract exists with fixed PASS conditions and no results; item 1 is restated around what is actually still unmeasured; items 2-6 carry over intact with three fix-specific additions; and the debug arc's commits are recorded in the phase's platform-scope, threat, requirement and deferred-item ledgers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| login page JS → shell | The injected strip and glyph run inside a hostile-capable third-party document; every frame of it, including iframes, can script against the controls |
| worker thread → AppKit main thread | Every `NSWindow` mutation crosses a queue hop whose failure is silent |
| plan author → gate result | An author who can also record results can back-fill an outcome from expectation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-05 | Spoofing (login surface presentation) | `present_login_window_as_sheet` | mitigate — **PARTIALLY DISCHARGED** | Attachment live-proven (`attached=true`, commit `8b2fdb315`). Un-losability, ordering-behind-parent and post-restore interactivity remain UNMEASURED — discharged only by `34.4.2-LIVE-GATE-RERUN-2.md` item 1. Not claimed closed by this plan. |
| T-34.4.2-07 | Denial of service (user trapped by a modal login) | `dismiss_login_window_sheet`, `request_login_sheet_cancel` | mitigate — OPEN-pending-gate | WR-01's re-registration removes the "removal already happened, no route left" failure class. Live discharge is item 2, still never attempted on any of four runs. |
| T-34.4.2-33 | Denial of service (hard lock-out behind a parent-blocking sheet) | cancel strip + Esc backstop | mitigate — OPEN-pending-gate | WR-03/WR-04 make the strip render exactly once, in the top frame, with the retry and observer installed before anything that can throw. Live discharge is items 1(d) and 2. |
| T-34.4.2-15 (residual) | Tampering (kill switch removing the only visible exit) | `GAMELIB_AUTOFILL_GLYPH` | mitigate — OPEN-pending-gate | The strip carries no kill switch by design (Plan 08 Test 1, unchanged by this plan). Live discharge is item 5 arm 2. |
| T-34.4.2-17 | Elevation of privilege (page-controlled event injection) | `post_autofill_right_click` | mitigate — OPEN-pending-gate | All six pre-existing source constraints unchanged; WR-07 adds a seventh (never re-order a presented sheet). Live discharge is items 3 and 6. |
| T-34.4.2-21 | Spoofing (menu content forgery) | system context menu | accept — OPEN-pending-gate | Menu is built by WebKit/macOS. Live discharge is item 3. |
| T-34.4.2-22 | Denial of service (Cmd+V regression) | cancel strip, Esc monitor | mitigate — OPEN-pending-gate | IN-02 adds a static `aria-keyshortcuts` attribute only; the ban on `keydown`/`keyup`/`keypress` is re-asserted by both a cargo test and jest Test 5. Live discharge is item 4. |
| T-34.4.2-32 | Tampering (scope-boundary erosion — Epic re-entering) | `PHASE_34_4_2_NEW_SYMBOLS` guard | mitigate — CLOSED by source | The array gains `register_presented_login_sheet`; both pristine regions re-asserted symbol-free, with a zero-line-diff acceptance criterion on each task. |
| T-34.4.2-34 | Spoofing (page-forged cancel-strip click) | `/login-cancel` sentinel | accept, bounded — CLOSED by source | No payload crosses the channel; the only consequence is closing the user's own login sheet. Unchanged by this plan. WR-03's top-frame guard narrows the forging surface (a third-party iframe can no longer host a strip of its own). |
| T-34.4.2-35 | Information disclosure (app-wide Esc key monitor) | Esc local monitor | mitigate — CLOSED by source | Reads only `keyCode()`/`modifierFlags()`; permanent negative test unchanged by this plan. |
| **T-34.4.2-36 (NEW)** | Tampering (a presented sheet detached mid-session by the app's own poster) | `post_autofill_right_click` step 7 | mitigate — source-fixed, live discharge item 1(e) | WR-07: `makeKeyAndOrderFront` is skipped when `isSheet()` is true, with a distinct diagnostic line so the gate can observe which branch ran. A sheet that reverts to a free-standing window on the first glyph click would reintroduce F-34.4.2-03 mid-session. |
| **T-34.4.2-37 (NEW)** | Denial of service (permanent parent lock-out from a failed `endSheet:` hop) | `dismiss_login_window_sheet` | mitigate — source-fixed, live discharge item 2 | WR-01: the label is re-registered on both the dispatch-`Err` and `recv_timeout`-`Err` arms, so both cancel routes (strip, Esc) stay membership-authorized and a second attempt can still reach AppKit. The healthy `child_addr == None` path deliberately does not re-register. Bounded growth (T-34.4.2-08) is preserved because the shared re-insert helper keeps dedup and `PRESENTED_LOGIN_SHEETS_CAP`. |
| **T-34.4.2-38 (NEW)** | Spoofing (duplicate cancel controls inside third-party iframes) | `login_cancel_strip_script` | mitigate — source-fixed, live discharge item 2 | WR-03: `window.top !== window` early return, placed before the idempotence flag is even read, so a subframe leaves no state. Spike 013 measured 5 of 8 Humble login navigations as third-party frames; a "Cancel sign-in" control rendered by an ad or bot-management frame is a control the user cannot distinguish from the real one. |
| T-34.4.2-25 | Repudiation (a result back-filled from expectation) | `34.4.2-LIVE-GATE-RERUN-2.md` | mitigate — CLOSED by process | PASS conditions are fixed here, before the outcome is known; `verdict`/`run_date`/`items_passed` stay `null`; this plan is forbidden from running any item, starting the harness, or launching `tauri:dev`, and an acceptance criterion asserts every `Observed:` field is empty. |
| T-34.4.2-26 | Repudiation (a green suite mistaken for live proof) | Tasks 1-2 verification | mitigate — CLOSED by process | Jest 3735/3735 and cargo 131/131 have been green through all four failed gate runs. Both code tasks carry an explicit acceptance criterion stating live behaviour is NOT claimed and naming the gate item that owns each claim. |
| T-34.4.2-SC | Tampering (dependency surface) | `Cargo.toml` / `pnpm-lock.yaml` | mitigate — CLOSED | No `npm`/`pip`/`cargo` install in this plan. `dispatch2 = "0.3.1"` was already promoted by the debug arc (transitive → direct, one `Cargo.lock` line, no new crate) and is documented, not re-introduced. `git diff --exit-code pnpm-lock.yaml` must exit 0. |
</threat_model>

<verification>
- `cargo check` and `cargo test --manifest-path src-tauri/Cargo.toml` both clean; test count >= the 131-pass baseline.
- `npx jest src/backend/__tests__/tauriShellSource.test.ts` passes with every pre-existing Plan 01-08 test unmodified.
- `npm run test:ci` passes (record the figure as CONTEXT, not as evidence of live behaviour — T-34.4.2-26).
- `git diff --unified=0 -- src-tauri/src/main.rs` shows zero changed lines inside `open_pristine_epic_login_window` and `EpicPristineNavDelegate` (REQ-34.4.2-10).
- Comment-stripped `main.rs` still contains `beginSheet_completionHandler` exactly once and `endSheet` exactly once, and `.makeKeyAndOrderFront(None)` exactly once.
- The generated cancel-strip script still contains `"try {"` exactly once.
- `34.4.2-LIVE-GATE-RERUN-2.md` exists with six items, `verdict: null`, and no filled `Observed:`/`Verdict:` field.
- `34.4.2-LIVE-GATE-RERUN.md` is byte-unchanged by this plan — its measured FAIL is history.
- No REQ-34.4.2 checkbox changed state; all ten IDs still present.
- `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing at plan end — this plan never started the harness.
- `git diff --exit-code pnpm-lock.yaml` exits 0.
</verification>

<success_criteria>
The five review findings that sit on gate items 2/3/5's own routes are fixed and held in place by
source guards; the debug arc's three commits are documented in the phase's records for the first
time; a blocking, fixed-before-the-fact six-item contract exists whose item 1 measures only what is
actually still unmeasured and whose items 2-6 are finally reachable; and this plan claims no live
proof of anything.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-11-SUMMARY.md` when done.
State explicitly that this plan ran NO gate item, started NO harness, never launched `tauri:dev`,
left every result field null, and ticked no requirement box — and that the phase remains NOT CLOSED
until plan 34.4.2-12 records a measured verdict. List, per fix, exactly which claim is source-proven
and which is deferred to its gate item.
</output>
