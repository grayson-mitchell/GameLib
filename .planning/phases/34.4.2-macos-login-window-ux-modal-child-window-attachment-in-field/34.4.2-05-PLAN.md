---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 05
type: execute
wave: 5
depends_on: ["34.4.2-04"]
files_modified:
  - src/backend/__tests__/dummyStoreHarnessContainment.test.ts
  - src/backend/sidecar/__tests__/testContainment.test.ts
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
autonomous: true
requirements:
  - REQ-34.4.2-07
  - REQ-34.4.2-08
  - REQ-34.4.2-09
  - REQ-34.4.2-10

must_haves:
  truths:
    - "The DummyStore harness cannot reach a production build, and a future change that references it fails a test."
    - "A reader can tell, from one document, exactly what this phase does on Windows and Linux, what it deliberately does NOT do on the pristine Epic surface, and what evidence backs both claims."
    - "The Electron build is proven behaviorally unchanged by this phase, not merely asserted."
    - "A blocking live gate contract exists with a null verdict, so its result cannot be back-filled from expectation."
  artifacts:
    - path: "src/backend/__tests__/dummyStoreHarnessContainment.test.ts"
      provides: "guard that no shipped source references the harness path or port 17940"
    - path: ".planning/phases/.../34.4.2-PLATFORM-SCOPE.md"
      provides: "per-platform behaviour declaration + Electron-unchanged evidence"
    - path: ".planning/phases/.../34.4.2-LIVE-GATE.md"
      provides: "6-item blocking gate contract with verdict: null and per-item PASS conditions written BEFORE the run"
    - path: ".planning/phases/.../deferred-items.md"
      provides: "the EPIC-DEFERRED ledger entry recording the locked user decision and the follow-up phase"
  key_links:
    - from: "34.4.2-LIVE-GATE.md"
      to: ".claude/skills/spike-findings-gamelib/sources/019-dummy-oauth-store/store-server.mjs"
      via: "the offline fixture items 3 and 4 run against"
      pattern: "17940"
---

<objective>
Everything this phase built is macOS-only, page-driven, and unproven on hardware. This plan closes
the record obligations that must exist *before* the gate runs and cannot honestly be written *after*
it: a machine guard that the test-only OAuth harness can never ship, a written per-platform scope
declaration with its evidence, the **EPIC-DEFERRED record** of the locked user scope decision, and
the blocking live-gate contract itself with `verdict: null`.

Purpose: the phase's own history is the argument. Across 34.4.1's three gate runs the test suite was
fully green (3279/3279, then 3387/3387) while two blocking defects were live — **every blocking
defect in that phase was found by a human driving the UI, none by automation.** A gate contract
written after the run is not a gate; it is a rationalisation. This plan fixes the PASS conditions
while the outcome is still unknown.

Output: one new jest guard, one platform-scope record (including the EPIC-DEFERRED section), one
phase-local deferred-items ledger, and one gate contract. No production source changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md
@.claude/skills/spike-findings-gamelib/references/oauth-login-test-harness.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-02-SUMMARY.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-03-SUMMARY.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-04-SUMMARY.md

<interfaces>
<!-- Extracted at plan time. -->

**The harness** (`.claude/skills/spike-findings-gamelib/sources/019-dummy-oauth-store/store-server.mjs`):
```
const PORT = 17940
const ORIGIN = `http://127.0.0.1:${PORT}`
...listen(PORT, '127.0.0.1', ...)
```
Zero dependencies, in-memory state only, hardcoded demo credentials `demo` / `dummy-store-pw`,
client_id `gamelib-dummy`. Endpoints: `/login` (`?autologin=1` self-submits), `/authorize`,
`/callback`, `/token`, `/me`, `/logout`, `/events`. Enforces misuse: code replay → `invalid_grant`,
bad PKCE verifier → `invalid_grant`, `state` required. Run with:
`node .claude/skills/spike-findings-gamelib/sources/019-dummy-oauth-store/store-server.mjs`

**The sticky-jar trap** (`oauth-login-test-harness.md` § "The sticky-jar trap"): the login-window
cookie jar persists across app restarts, so "logged in" is sticky and a second run silently skips the
form. Every scripted or manual run must start with a logout preamble and baseline `/events`
*afterwards*. Any gate item that asserts "the form was served" must state this precondition.

**Existing containment-test precedent to copy**:
`src/backend/__tests__/` already holds config/source-shape guards (`cargoFeatures.test.ts`,
`tauriConf.test.ts`, `capabilitiesDefault.test.ts`, `aboutHtmlSource.test.ts`). There is also a
declared-suite gate in `src/backend/sidecar/__tests__/testContainment.test.ts`
(`STRUCTURALLY_CONTAINED_SUITES`) — a new suite file
may need registering there; check and register if so, exactly as `humbleSecretStore.test.ts` had to be
in 34.4.1.

**Electron-unchanged evidence sources already in the repo**:
- `src/backend/sidecar/__tests__/seamBranchParity.test.ts` — the enforced dual-build parity guard.
- `src/backend/sidecar/__tests__/electronReachLedger.test.ts` — the entry-point reach graph.
- `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep.py`
  — the two-axis seam-parity sweep script.

**Gate contract precedents** to match in structure (read one before writing):
`.planning/phases/34.4.1-.../34.4.1-LIVE-GATE-RERUN-3.md` and
`.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md`. Both carry frontmatter with `verdict: null`
until the run, numbered items with explicit PASS/FAIL conditions, preconditions, and a Verdict
section filled only by the executing plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Machine guard that the DummyStore harness can never reach a shipped build</name>
  <files>src/backend/__tests__/dummyStoreHarnessContainment.test.ts, src/backend/sidecar/__tests__/testContainment.test.ts</files>
  <read_first>
    - .claude/skills/spike-findings-gamelib/sources/019-dummy-oauth-store/store-server.mjs (confirm the bind address and port literals the guard will assert about)
    - src/backend/__tests__/cargoFeatures.test.ts (the config-guard suite style to copy: a `describe` per concern, exact assertions, an explanatory header comment)
    - src/backend/sidecar/__tests__/testContainment.test.ts (check whether a new suite file must be registered in `STRUCTURALLY_CONTAINED_SUITES`; register it if the gate requires it)
    - package.json (the `files`/`build` config and the scripts list — the guard asserts the harness is absent from every packaging input)
    - src-tauri/tauri.conf.json (the bundle configuration — same reason)
  </read_first>
  <behavior>
    - Test 1: no file under `src/` references the string `019-dummy-oauth-store`.
    - Test 2: no file under `src/` or `src-tauri/src/` references the port literal `17940`.
    - Test 3: `package.json` (whole file, including `scripts`, `dependencies`, `devDependencies`, `files` and any electron-builder/electron-vite config) contains neither `019-dummy-oauth-store` nor `17940`.
    - Test 4: `src-tauri/tauri.conf.json` contains neither `019-dummy-oauth-store` nor `17940`, and its bundle `resources`/`externalBin` lists do not include any path under `.claude/`.
    - Test 5 (POSITIVE, so the guard cannot pass vacuously by pointing at nothing): the harness file itself exists at the expected path and its source contains `listen(PORT, '127.0.0.1'` — proving the fixture binds loopback only and that the guard's target path is real.
    - Test 6: the harness source contains no `0.0.0.0` and no bare `.listen(PORT)` single-argument form.
  </behavior>
  <action>
Create `src/backend/__tests__/dummyStoreHarnessContainment.test.ts` implementing the six behaviors
above.

Header comment must state the requirement it enforces (REQ-34.4.2-07) and why a POSITIVE test sits
alongside the negatives: a containment suite made only of "X is absent" assertions passes trivially
if the thing it guards is renamed or deleted, so Test 5 anchors it to a file that must exist.

Implementation notes:
- Walk `src/` recursively with `node:fs`, skipping `node_modules` and any `__snapshots__`. Read each
  file as UTF-8 and assert the two forbidden substrings are absent. Report the offending path in the
  failure message — a bare `toBe(0)` on a count is useless when it fires.
- Exclude THIS test file itself from the walk (it necessarily contains both strings), and say so in a
  comment so the exclusion is obviously deliberate rather than a loophole.
- For Test 4, parse `tauri.conf.json` as JSON and assert on the parsed structure for the bundle
  lists, plus a raw-string check for the two literals.
- Register the new suite in `src/backend/sidecar/__tests__/testContainment.test.ts`'s
  `STRUCTURALLY_CONTAINED_SUITES` if that gate
  requires it; if registration is not required, state that in the SUMMARY with the reason.

Do not add any runtime source. This is a guard only.
  </action>
  <acceptance_criteria>
    - `npx jest src/backend/__tests__/dummyStoreHarnessContainment.test.ts` exits 0 with six passing tests.
    - Mutation check performed and recorded in the SUMMARY: temporarily add a comment containing `17940` to any file under `src/backend/`, confirm Test 2 fails AND names the offending path in its failure message, then revert.
    - `npx jest src/backend/sidecar/__tests__/testContainment.test.ts` exits 0 (the new suite is either registered or provably out of that gate's scope).
    - `npm run codecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/backend/__tests__/dummyStoreHarnessContainment.test.ts src/backend/sidecar/__tests__/testContainment.test.ts 2>&1 | tail -12 && npm run codecheck</automated>
  </verify>
  <done>Six-test containment guard passes, its failure message names offending paths, and the mutation check was performed and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Per-platform scope declaration, the EPIC-DEFERRED record, and Electron-unchanged evidence</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md</files>
  <read_first>
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-02-SUMMARY.md, 34.4.2-03-SUMMARY.md, 34.4.2-04-SUMMARY.md (the actual shipped symbols, their cfg gates, and every DECLARED UNVERIFIED note)
    - .claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md (§ Constraints — "macOS 26 only. Windows (WebView2) and Linux (webkit2gtk) autofill and window-attachment behaviour are entirely unverified")
    - src/backend/sidecar/__tests__/seamBranchParity.test.ts and src/backend/sidecar/__tests__/electronReachLedger.test.ts (what each actually proves, so the record does not overclaim)
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep.py (its usage line, so the record can name the exact command run)
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/deferred-items.md (the phase-local ledger FORMAT to copy — `## <plan> — <title> (<date>)` with Found during / Symptom / Scope / Disposition bullets)
  </read_first>
  <action>
Write `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md`.

Required sections:

**1. Per-platform behaviour table.** One row per new symbol shipped by plans 01–04 — source the list
from `PHASE_34_4_2_NEW_SYMBOLS` in `src/backend/__tests__/tauriShellSource.test.ts` rather than
retyping it, since that array is the phase's own maintained inventory (`login_window_ns_window`,
`login_window_wk_webview`, `attach_login_window_as_child`, `detach_login_window_from_parent`,
`ATTACHED_LOGIN_CHILDREN`, `MAIN_WINDOW_LABEL`, the deminiaturize observer, `AUTOFILL_EXFIL_PATH`,
`AutofillRequest`, `parse_autofill_request`, `autofill_glyph_script`, `css_rect_center_in_view`,
`clamp_point_to_view_bounds`, `synth_autofill_mouse_event`, `post_autofill_right_click`,
`LAST_AUTOFILL_POST`, `AUTOFILL_POST_DEBOUNCE`), with columns: symbol · cfg gate · macOS behaviour ·
Windows behaviour · Linux behaviour · evidence. If the array and your table disagree on membership,
the array is authoritative — reconcile and say so. Fill "Windows"/"Linux" with the literal outcome — for
`#[cfg(target_os = "macos")]` items that is "not compiled; login window behaves exactly as before
this phase". Derive the cfg gate by reading the source, not by assuming; if any symbol turns out NOT
to be macOS-gated, say so plainly and file it as a finding rather than papering over it.

**2. What is UNVERIFIED off macOS, and why that is acceptable.** Quote the skill's Constraints line.
State that no Windows or Linux hardware ran anything in this phase and that the correct future action
is re-measurement, not extrapolation — WebView2 and webkit2gtk have separate wry implementations and
the `cookies_for_url` precedent already proved one macOS-observed defect did not generalise.

**3. EPIC-DEFERRED.** A named section recording the locked user scope decision:

   - **Decision (2026-08-04, user, LOCKED):** Epic is implemented LAST, after all other
     accounts/runners are ported successfully. Epic is removed from Phase 34.4.2 entirely.
   - **Why:** Epic's problems are unresolved — the parked deterministic pre-auth 403 (anti-bot), the
     zero-injection constraint the pristine `WKWebView` surface exists specifically to satisfy, and
     the standing hCaptcha/UA constraint. The user chose to leave it alone until those are resolved.
   - **What was consequently NOT built** (list each with the plan that would have built it): the
     child-window attachment arm for the pristine surface (plan 02); the `WKUserScript` glyph
     injection and its `WKUserContentController`/`WKUserScript`/`WKUserScriptInjectionTime`
     `objc2-web-kit` features (plan 03); the sentinel arm in
     `EpicPristineNavDelegate::navigation_policy` (plan 03); the `post_autofill_right_click` call
     site for that arm (plan 04); the `login_window_ns_window` `get_window` fallback that would make
     the pristine window resolvable at all (plan 01); and the Epic injection A/B gate item (plan 05,
     replaced by a kill-switch efficacy item).
   - **What IS still true for Epic:** its login window keeps working exactly as it does today. This
     phase changed nothing about it — REQ-34.4.2-10, enforced by `PHASE_34_4_2_NEW_SYMBOLS` and a
     zero-line-diff requirement on `open_pristine_epic_login_window` and `EpicPristineNavDelegate`.
   - **Follow-up:** Epic's login-window UX gets its own phase after all other runners are ported and
     proven. Record what that phase inherits: the attach/detach pair, the glyph script builder, the
     pure parser and the poster are all runner-agnostic and already built — the Epic phase adds call
     sites and the `get_window` fallback, not a redesign.

Also create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md` (the phase has none yet) using the phase-local ledger
format, with this as its first entry: `## Phase scope — Epic pristine login surface deferred by user
decision (2026-08-04)`. Cross-reference the PLATFORM-SCOPE section rather than duplicating it, and
state the disposition as `deferred to a follow-up phase, not fixed here`.

**4. Electron-build-unchanged evidence.** Run and record the actual output of:
   - `npm run test:ci` (record suites/tests/exit code, and the delta against the baseline each plan
     recorded)
   - `npm run codecheck`
   - `npx jest src/backend/sidecar/__tests__/seamBranchParity.test.ts src/backend/sidecar/__tests__/electronReachLedger.test.ts src/backend/sidecar/__tests__/rustInvokeChannel.test.ts`
   - `python3 .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep.py`
     (read its own usage/arguments first; if it needs arguments, supply them and record the exact
     invocation)
   - `git diff --stat main...HEAD -- src/backend src/frontend src/common` — the phase must show ZERO
     runtime source changes outside `src/backend/__tests__/`. If that is not true, stop and record
     which file changed and why.
   State plainly what each command does and does NOT prove. In particular: a green suite is not
   evidence of parity — 34.4.1 shipped two blocking defects under a fully green suite three times.

**5. Threat register roll-up.** One table consolidating T-34.4.2-01 through T-34.4.2-22 plus
T-34.4.2-32 and T-34.4.2-SC from plans 01–04, with each threat's disposition and the specific test or
gate item that discharges it. Mark any threat whose only discharge is a live-gate item as OPEN until
plan 34.4.2-06 records a result. Note plainly that T-34.4.2-15's original framing (injection
defeating Epic's anti-bot posture) was largely retired by SCOPE REMOVAL rather than by mitigation.

**6. Package legitimacy.** Record that this phase installed nothing: no npm, no pip, no cargo crate.
After the Epic descope the only possible dependency-manifest change is at most ONE objc2-app-kit
FEATURE name (`NSGraphicsContext`, plan 04, added only if the compiler demands it). Cite
`git diff --exit-code src-tauri/Cargo.lock` returning 0 and `git diff package-lock.json` (or the
project's lockfile) being empty. State that no Package Legitimacy Gate applies for that reason.
  </action>
  <acceptance_criteria>
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` exists with all six sections, including one titled EPIC-DEFERRED.
    - The EPIC-DEFERRED section names the decision date (2026-08-04), the words "user" and "LOCKED", the six things consequently not built, and the follow-up phase.
    - `.planning/phases/.../deferred-items.md` exists and its first entry cross-references the EPIC-DEFERRED section.
    - Its per-platform table has one row per shipped symbol, and every row's cfg-gate column was read from `src-tauri/src/main.rs`, not assumed (spot-check three rows against the source and say so in the SUMMARY).
    - The Electron-evidence section contains real command output (numbers, exit codes), not a claim that a command "was run".
    - `git diff --stat` against the phase's base shows zero changes under `src/backend/` except `src/backend/__tests__/`; if not, the deviation is recorded with its reason.
    - `git diff --exit-code src-tauri/Cargo.lock` returns 0 and that is recorded verbatim.
    - The threat roll-up marks at least T-34.4.2-05, -07, -17, -21 and -22 as OPEN-pending-gate (no automated discharge), records T-34.4.2-15 as retired-by-scope-removal with a kill-switch residual, and records T-34.4.2-32 as CLOSED by the source guards.
  </acceptance_criteria>
  <verify>
    <automated>S=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md; test -f "$S" && grep -q 'T-34.4.2-17' "$S" && grep -q 'EPIC-DEFERRED' "$S" && test -f .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md && git diff --exit-code src-tauri/Cargo.lock && npm run test:ci 2>&1 | tail -8</automated>
  </verify>
  <done>The platform-scope record exists with a per-symbol table, the EPIC-DEFERRED section, real Electron-unchanged evidence, a consolidated threat roll-up and the no-installs declaration; the phase-local deferred-items ledger exists with the Epic entry.</done>
</task>

<task type="auto">
  <name>Task 3: Author the blocking live-gate contract with verdict: null</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-3.md (the structure to match: frontmatter, preconditions, numbered items with explicit PASS conditions, a Verdict section)
    - .claude/skills/spike-findings-gamelib/references/oauth-login-test-harness.md (§ "Run it", § "The sticky-jar trap" — the logout preamble and `/events` baselining are preconditions, not suggestions)
    - .claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md (§1's measured behaviour table — items 1(a)–1(d) come straight from it; § "What to Avoid" — the synthesized-event-negative warning and the screencapture warning are gate instructions)
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-02-SUMMARY.md, 34.4.2-03-SUMMARY.md, 34.4.2-04-SUMMARY.md (every DECLARED UNVERIFIED note — each must become a gate item or be explicitly out of scope with a reason)
  </read_first>
  <action>
Write `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md`.

Frontmatter: `verdict: null`, `run_date: null`, `items_passed: null`, `items_total: 6`. These stay
null — plan 34.4.2-06 fills them from the measured run. Say so explicitly in the document.

**Preconditions** (each must be checkable before the run starts, and each must say who satisfies it —
operator-at-gate-time or already-evidenced):
1. macOS hardware, `npm run tauri:dev` (dev build; note that `tauri:dev` only rebuilds the sidecar on
   launch, so confirm the new symbols are present in the built binary before trusting a negative).
2. The DummyStore harness running:
   `node .claude/skills/spike-findings-gamelib/sources/019-dummy-oauth-store/store-server.mjs`,
   liveness-confirmed by `curl -s http://127.0.0.1:17940/events`. Port 17940 must be free first —
   a leftover instance produces confusing "unreachable" states.
3. **A seeded Passwords-app entry for the test origin.** Without it the AutoFill panel has nothing to
   offer and a negative on item 3 is uninterpretable. This is the single most commonly-missed
   precondition in this area — state it in bold.
4. **Logout preamble + `/events` baseline taken AFTER it.** The login-window cookie jar persists
   across app restarts, so a second run silently skips the login form and any "the form was served"
   assertion fails for the wrong reason.
5. A real store login available for item 3(b) (Humble is the surface with the most proven path).
6. `GAMELIB_AUTOFILL_GLYPH` unset for the default arm; item 5 runs the `=0` arm in the same binary.

**Items** — six, each with an explicit PASS condition written now:

1. **Child-window attachment (REQ-34.4.2-01/02/03).** Four sub-observations, each recorded
   separately: (a) with the login window open, click the main window — login stays IN FRONT;
   (b) drag the main window — login follows; (c) minimize the main window — login follows into the
   Dock; (d) restore from the Dock — login is IN FRONT, not behind. (d) is the one the deminiaturize
   observer exists for and the one that fails without it. PASS = all four observed.
2. **Dismissability (REQ-34.4.2-03).** The login window can be closed by its own close button, by
   Cmd+W, and the app resolves the login as cancelled (`oauthLoginCapture` logs
   `status=cancelled reason=window-closed`). PASS = closed by both routes and the log line present.
   This is the anti-sheet-trap proof: spike 021's operator could not dismiss a sheet at all.
3. **The glyph and the AutoFill menu (REQ-34.4.2-04/05).** (a) On the DummyStore `/login` form in a
   Tauri-managed login window: the key glyph is visible inside the password field; clicking it pops
   the real system context menu; that menu contains `AutoFill ›`; choosing AutoFill → Passwords fills
   the field. (b) Repeat on a real store login page (Humble). PASS = glyph visible AND menu popped AND
   `AutoFill ›` present AND field filled, on both. Epic is NOT part of this item — it is out of scope
   by locked user decision (REQ-34.4.2-10) and receives no glyph.
   **Instructions the operator must follow, written into the item:**
   - Compare against a REAL right-click on the same field before concluding anything from a negative.
     A real click yields the 3-item secure-field menu; a synthesized one yields the 10-item generic
     text menu. A synthesized-event negative is not evidence on its own.
   - To photograph the menu, region-grab the owning window's rect
     (`/usr/sbin/screencapture -x -o -R<x,y,w,h>`). `screencapture -l<windowID>` MISSES menus —
     menus are their own windows. Do not use a wider region; it sweeps in unrelated windows.
   - Record the `[shell]` log line's `hit_id`/`hit_tag`/`hit_type`. If they are not
     `{id:'password', type:'password'}`-shaped, the coordinate conversion aimed at the wrong element
     and the item FAILS regardless of what menu appeared.
   - Expected UX cost, so it is not misfiled as a defect: the panel does not auto-match the current
     site and its search box rejects typed input (it beeps — system behaviour, reproduced in both
     surfaces). The user scrolls, then clicks the entry to fill.
4. **Cmd+V paste (REQ-34.4.2-06).** Copy a string, Cmd+V into the password field of a Tauri-managed
   login window, with the glyph present. PASS = text lands. Also record whether Edit > Paste from the
   menu bar works, since a divergence between the two isolates a key-equivalent-delivery regression
   from a first-responder regression. Do this on both the DummyStore form and the real Humble form,
   because only the latter has real page JS that could interact with the glyph.
5. **Kill-switch efficacy (T-34.4.2-15, REQ-34.4.2-04).** Open a Humble login window twice in one
   sitting from the SAME binary: once with `GAMELIB_AUTOFILL_GLYPH` unset, once with
   `GAMELIB_AUTOFILL_GLYPH=0`. PASS = with the variable unset the glyph is present and the login
   completes; with it set to `0` **no glyph is injected** and the login STILL completes normally.
   This is the escape hatch a user needs if injection ever trips a store's bot management and locks
   them out of their own library — an untested escape hatch is not one. Record the one-line
   `[shell]` log entry naming which arm was taken, for each arm; if that line is identical in both
   arms, the switch is not wired and the item FAILS regardless of what the UI showed.
6. **Hidden-window non-interference + Epic untouched (REQ-34.4.2-04/10, T-34.4.2-17/-32).**
   (a) Trigger a Humble reveal-key operation (which opens a hidden reveal window) while a visible
   login window is open. PASS(a) = no glyph is injected into the hidden window, no autofill sentinel
   is logged for its label, and the reveal completes exactly as before.
   (b) Open the **Epic** login window once and confirm it behaves exactly as it did before this
   phase: **no key glyph appears in any field**, no autofill sentinel is logged for its label, and no
   `login-window attach` line is logged for it. PASS(b) = all three absences observed.
   (b) is deliberately an ABSENCE check, not a feature test — it is the live confirmation of the
   locked scope decision, and it is the only place Epic is touched by this gate at all. If Epic's
   login is currently blocked by its own parked pre-auth 403, that is expected and does NOT fail this
   item: the observation needed is only that the page reached is the same one it reached before this
   phase, with no glyph on it.

Each item gets: what to do, the PASS condition, the exact log lines to capture, and a blank
`Observed:` / `Verdict:` field for plan 34.4.2-06 to fill.

**Closing section — "What a PASS does and does not close."** State that items 1–6 do not cover
Windows or Linux (see `34.4.2-PLATFORM-SCOPE.md`), and that they deliberately prove **nothing about
Epic's login UX** — Epic is deferred by locked user decision to its own follow-up phase after all
other runners are ported and proven, and item 6(b) checks only that this phase left it alone. List
which threat IDs from the roll-up each item discharges.

Do NOT run any gate item in this plan. Writing the contract and running it must be separate plans, so
the PASS conditions are fixed while the outcome is unknown.
  </action>
  <acceptance_criteria>
    - `.planning/phases/.../34.4.2-LIVE-GATE.md` exists with frontmatter `verdict: null`, `items_total: 6`.
    - It contains six numbered items, each with an explicit `PASS =` condition and an empty `Observed:` field.
    - It contains all six preconditions, with the seeded-Passwords-entry and logout-preamble ones present.
    - No item requires the Epic pristine surface to do anything NEW; the only Epic mention is item 6(b)'s absence check, and the closing section states plainly that a PASS proves nothing about Epic's login UX.
    - It names the `screencapture -R` region-grab instruction and the real-vs-synthesized-menu comparison instruction inside item 3.
    - Every `DECLARED UNVERIFIED` note from the 34.4.2-02/03/04 SUMMARYs maps to a gate item, or is listed in the closing section as deliberately out of scope with a reason. Enumerate that mapping in this plan's SUMMARY.
    - `grep -c 'PASS =' 34.4.2-LIVE-GATE.md` returns at least 6.
    - No gate item was executed by this plan (the SUMMARY states this explicitly).
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md; test -f "$F" && grep -q 'verdict: null' "$F" && [ "$(grep -c 'PASS =' "$F")" -ge 6 ] && grep -q 'screencapture' "$F" && echo GATE_CONTRACT_OK</automated>
  </verify>
  <done>A six-item blocking gate contract exists with null verdict, all six preconditions, per-item PASS conditions and empty Observed fields; nothing was executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → shipped artifact | An OAuth provider with hardcoded credentials lives in the repo; the boundary is whatever packaging includes. |
| harness process → network | The fixture opens a listening socket on the developer's machine. |
| written record → future reader | A gate contract and a platform declaration are the only durable evidence of what was and was not proven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-23 | Tampering | DummyStore shipping in a production build | mitigate | Task 1's six-test guard: no file under `src/`, `src-tauri/src/`, `package.json` or `tauri.conf.json` may reference `019-dummy-oauth-store` or `17940`, and the bundle resource/externalBin lists may not contain any `.claude/` path. The guard includes a POSITIVE anchor test so it cannot pass vacuously. Mutation-checked. |
| T-34.4.2-24 | Information disclosure | harness binding a non-loopback interface | mitigate | Task 1 Test 5 asserts the harness source contains `listen(PORT, '127.0.0.1'`; Test 6 asserts it contains no `0.0.0.0` and no single-argument `.listen(PORT)` form (which would bind all interfaces). The fixture also holds only in-memory state and hardcoded demo credentials that are printed on its own form — it must never be pointed at anything real, and the gate contract says so. |
| T-34.4.2-25 | Repudiation | a gate result back-filled from expectation | mitigate | The contract ships with `verdict: null` and empty `Observed:` fields, authored in a plan that is forbidden from running any item. Plan 34.4.2-06 is the only writer of results. This is the discipline 34.4.1 and 34.5 both used after gates that had to be re-run. |
| T-34.4.2-26 | Repudiation | overclaiming Electron parity from a green suite | mitigate | Task 2 requires stating what each command does NOT prove, and cites the measured precedent: 34.4.1's suite was fully green across three gate runs while two blocking defects were live, and every blocking defect was found by a human driving the UI. |
| T-34.4.2-SC | Tampering | dependency surface | mitigate | This plan installs nothing and adds no runtime source. Task 2 records `git diff --exit-code src-tauri/Cargo.lock` returning 0 and the JS lockfile being unchanged, and declares that no Package Legitimacy Gate applies to this phase because no npm/pip/cargo package was added at any point. |
</threat_model>

<verification>
- `npx jest src/backend/__tests__/dummyStoreHarnessContainment.test.ts src/backend/sidecar/__tests__/testContainment.test.ts` exits 0.
- `npm run codecheck` and `npm run test:ci` exit 0 with no regression vs baseline.
- `git diff --exit-code src-tauri/Cargo.lock` returns 0.
- `git diff --stat` shows no runtime source change under `src/backend/`, `src/frontend/` or
  `src/common/` from this plan (test files only).
- `34.4.2-PLATFORM-SCOPE.md` and `34.4.2-LIVE-GATE.md` both exist; the gate's `verdict` is `null`.
</verification>

<success_criteria>
- A mutation-checked guard prevents the DummyStore harness from ever reaching a shipped build, and
  proves it binds loopback only.
- A single document states what this phase does on macOS, Windows and Linux, per symbol, with the
  cfg gate read from source.
- Electron-unchanged is backed by recorded command output plus an honest statement of what that
  output does not prove.
- A six-item blocking gate contract exists with fixed PASS conditions and a null verdict.
- Every `DECLARED UNVERIFIED` note from plans 02–04 is either a gate item or an explicit, reasoned
  out-of-scope entry.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-05-SUMMARY.md` when done.
</output>
</content>
