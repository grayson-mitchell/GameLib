---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
verified: 2026-08-06T00:00:00Z
status: gaps_found
score: 6/11 truths verified, 3 uncertain, 2 failed (7/10 REQ IDs ticked: REQ-01/02/03/06/07/08/10; REQ-04/05/09 stay unchecked); blocking-gate verdict FAIL 5/6 (34.4.2-LIVE-GATE-RERUN-3.md), no-partial-pass (D-08)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 7/10 requirements verified; 5/6 blocking-gate items passed (verdict FAIL) — against 34.4.2-LIVE-GATE-RERUN-2.md, now superseded
  gaps_closed:
    - "REQ-34.4.2-04/-05's item-3 FAIL (synthesized right-click pops AutoFill but never fills, F-34.4.2-09) — CLOSED by scope correction, not by fixing the mechanism: operator decision D-A, plan 13 deleted the in-field autofill affordance in full (13 symbols + supporting helpers), independently re-confirmed absent in current source (0 grep hits for the autofill stem in src-tauri/src/main.rs, 0 hits for GAMELIB_AUTOFILL_GLYPH) and mutation-proven absent by a new whole-file test (tauriShellSource.test.ts:1021, PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS, independently run and confirmed passing)."
    - "T-34.4.2-39 (origin confusion — a second login flow can queue behind a slow upstream spawn and arrive as an unrequested sheet, F-34.4.2-07) — MITIGATED by plan 14's PENDING_VISIBLE_LOGIN_WINDOW single-flight latch, independently confirmed macOS-gated (#[cfg(target_os = \"macos\")]) and scoped to `if visible == true` in current source (main.rs:3355-3389). Live discharge attempted for the first time this cycle (gate run 3 item 5)."
    - "The gate contract itself (34.4.2-LIVE-GATE-RERUN-3.md) was authored, structurally reviewed for reachability BEFORE any live run, and then RAN in full on real macOS hardware — the first time in this phase's five-gate history that every item reached a genuine measured result without the gate aborting partway through."
  gaps_remaining:
    - "The blocking six-item hardware gate does not record a full PASS (REQ-34.4.2-09) — verdict FAIL, items_passed 5/6. This gap simply moved from item 3 (gate run 2) to item 6 (gate run 3); it has never yet been closed in this phase's five-gate history."
    - "Item 3 and item 5's own decisive live evidence did not survive the run (F-34.4.2-11, new this cycle) — both items are recorded PASS on the operator's verbatim word alone, not on transcript corroboration. Not a code regression, but a verification-confidence gap this cycle's own fresh finding names explicitly."
  regressions: []
gaps:
  - truth: "The Humble disconnect/logout control does not hang the application (item 6(a) of the blocking gate, and an implicit precondition of REQ-34.4.2-09's full-PASS requirement)"
    status: failed
    reason: "Measured live on real macOS hardware, 2026-08-05 (34.4.2-LIVE-GATE-RERUN-3.md item 6, run by plan 34.4.2-16). Clicking Humble's disconnect/logout control produced a hard, unbounded macOS main-thread wedge (spinning-wait cursor, unresponsive to all input); the operator had to force-kill the app. No graceful recovery, no error dialog, no log line survives in the transcript (it ends before any disconnect-specific line). This is F-34.4.2-12, an escalation of the pre-existing, previously bounded/non-fatal F-34.4.2-10 storage-wipe timeout (still present and unfixed in src/backend/humble/user.ts's clearHumbleStorage step, confirmed at user.ts:919-1116) to an unbounded, fatal hang. No root cause is asserted or diagnosed by any plan to date — three candidate layers are recorded without preference: (a) plan 13's removal of the /autofill-request arm from the shared .on_navigation( closure that also carries /reveal, /clear-storage and /login-cancel on the same REVEAL_EXFIL_HOST; (b) plan 14's single-flight latch interacting with the hidden reveal-window disconnect path (independently confirmed the latch is scoped to `if visible == true`, main.rs:3355, so this is the weaker candidate, but the hidden-window path was never actually reached live this run to confirm the exemption held); (c) the pre-existing humble_login_clear_storage exfil-channel wait itself. Item 6(b) (Epic's required absences) was consequently NOT ATTEMPTED — the force-kill ended the session before Epic's login window could be opened. Per the phase's own D-08 no-partial-pass rule, this single item FAIL fails the whole six-item gate regardless of items 1-5's PASSes."
    artifacts:
      - path: "src/backend/humble/user.ts"
        issue: "The disconnect/logout flow's hidden-window path (visible: false at user.ts:813 and user.ts:960) and its storage-wipe step (clearHumbleStorage, user.ts:919-1116) are the confirmed location of the pre-existing bounded timeout (F-34.4.2-10) this run's unbounded hang escalates from. No fix, bound, or timeout-recovery change has landed in this file since the hang was measured (confirmed: git log shows no commits after 34.4.2-16 touching this file; working tree is clean against HEAD for this path)."
      - path: "src-tauri/src/main.rs"
        issue: "The shared .on_navigation( closure carrying /reveal, /clear-storage, /login-cancel (and formerly /autofill-request, now removed) is candidate layer (a) for the hang. No discriminator test has been built to confirm or rule out this closure's control-flow change as the cause."
    missing:
      - "CLOSED 2026-08-06 (6bad86227) — A discriminator test isolating which of the three candidate layers (shared-closure arm removal, single-flight-latch/hidden-window interaction, or the pre-existing exfil-channel wait) actually causes the wedge, per this phase's own standing lesson that when multiple readings of a measurement fit, build a discriminator before shipping a fix. CORRECTION: a live hung-process `sample` (4185 of 4185 samples in one identical frame chain, durable at `.planning/debug/evidence/F-34.4.2-12-hang-sample-2026-08-06.txt`) named the blocked thread directly. Candidates (a) plan 13's `.on_navigation(` shared-closure arm removal and (b) plan 14's single-flight latch are FALSIFIED — no `.on_navigation` frame, no `/clear-storage` arm frame, and no `beginSheet` frame appear anywhere in the capture. Candidate (c) is SUBSUMED, not eliminated: execution deadlocks strictly before `clearHumbleStorage` runs, so (c)'s own bounded-timeout defect is neither confirmed nor refuted."
      - "CLOSED 2026-08-06 (6bad86227) — A fix bounding the disconnect flow so it can never hang unboundedly (timeout + graceful degradation, not merely a longer timeout). CORRECTION: the stated SHAPE was wrong. The root cause is a reentrancy self-deadlock on tao's `EventLoopHandler` handler mutex — the main thread relocks a non-reentrant mutex it already holds, from inside wry's `wait_for_blocking_operation` reentrant `NSRunLoop` pump. No timeout can fix it: the block sits below the level any Tauri-side receive timeout lives, so a timeout converts a guaranteed deadlock into a guaranteed timeout. The landed fix replaces the blocking `WebviewWindow::cookies()` getter with the async `WKHTTPCookieStore.getAllCookies(completionHandler:)` pattern at both implicated call sites. Verified across four independent channels: operator live repro no longer wedges; `cargo test` green (116 passed, 0 failed, 1 ignored) including the new regression pin; `humble_store/config.json` genuinely emptied to 2 bytes (`{}`); a fresh 197-line launch log reaching store teardown."
      - "A full re-run of gate item 6 (both 6(a) and 6(b)) recording a genuine PASS, including the still-NOT-ATTEMPTED Epic-absence sub-check. STILL OPEN — this is the phase's sole remaining closure blocker. Item 6(b) has NEVER been attempted in this phase's five-gate history, not merely un-re-run."
  - truth: "The blocking six-item gate's decisive machine evidence for items 3 and 5 (the two items gap cycle 3 existed to measure for the first time) survives the mandated relaunch and is available to corroborate the operator's PASS report"
    status: partial
    reason: "New process finding this cycle, F-34.4.2-11: the gate contract's own mandatory evidence-capture instruction (`npm run tauri:dev 2>&1 | tee /tmp/gamelib-dev.log`, no `-a`) truncates the log file to zero on every relaunch, while item 3(c) separately and correctly mandates a relaunch (`GAMELIB_AUTOFILL_GLYPH=0`). Only the FINAL launch's 214-line transcript survives; independently confirmed by direct grep that it contains ZERO occurrences of `post_autofill_right_click`, `autofill glyph injected for`, `autofill glyph injection SKIPPED`, `GAMELIB_AUTOFILL_GLYPH`, `REFUSED visible login window`, or `is already pending or presented` — i.e. every one of item 3's and item 5's own decisive, PASS-defining lines. Both items are recorded PASS on the operator's unqualified verbatim report alone. This is not evidence of a failure (the operator's account is not being second-guessed), but it means the two items this whole gap cycle existed to measure still have no independent machine corroboration after five gate runs and three separate finding-worthy contract-authoring defects in this phase's history. Plan 15's own Structural Reachability Review (four defect-class tests) could not catch this because the defect is an INTERACTION between two individually-reachable requirements (always-truncating tee + a mandated mid-run relaunch), a category the review was not designed to test."
    artifacts:
      - path: "34.4.2-LIVE-GATE-RERUN-3.md"
        issue: "Evidence-capture instruction mandates `tee` without `-a`; item 3(c) mandates a relaunch. No fix has landed — this document itself records the defect but, by its own author/runner discipline, does not repair it."
    missing:
      - "Append `-a` to the mandated `tee` invocation (or require a uniquely-named log file per relaunch) in the next gate contract, with per-launch markers separating sections."
      - "A fifth Structural-Reachability-Review defect-class test for requirement-interaction reachability (does satisfying requirement A ever undo requirement B's own evidence?), so this class of defect is caught before the next live run rather than after."
      - "Once evidence-capture is fixed, item 3 and item 5 should get one more re-run with intact transcript corroboration — not strictly required to close the phase (the operator's PASS stands), but this is the second time in this phase's history a PASS was recorded with its own decisive evidence unavailable, and it is worth closing the confidence gap the same gate cycle that finally fixes item 6."
deferred: []
human_verification: []
---

# Phase 34.4.2: macOS login-window UX — Verification Report

## CORRECTION (2026-08-06, plan 34.4.2-18)

This report was written at 2026-08-06T07:09Z. Commit `6bad86227` ("fix(F-34.4.2-12): stop Humble
disconnect wedging main thread via async cookie reads") landed at 2026-08-06T13:57Z the same day.
Two of gap 1's three `missing:` bullets below are consequently superseded by that commit — they
now describe work that is already done, and describe it in the wrong shape. The third bullet
remains genuinely open and is the phase's sole closure blocker. Each superseded bullet is marked
`CLOSED 2026-08-06 (6bad86227)` in place, with its original text preserved, so a reader sees both
what was asked for and why it is no longer the ask. This correction's own source of record is
`.planning/debug/resolved/humble-disconnect-main-wedge.md`.

**No requirement checkbox is changed by this correction.** REQ-34.4.2-04/-05/-09 remain `[ ]` in
`REQUIREMENTS.md`, exactly as this report originally recorded them. This plan corrects the report's
ASK for gap 1, not any measured verdict: the gate verdict `FAIL`, `items_passed: 5`, and
`status: gaps_found` below are all unchanged and remain authoritative.

**Phase Goal (corrected 2026-08-05 by plan 16, binding operator decision D-A):** macOS
login-window UX for Tauri-managed login surfaces (Humble/GOG/Amazon) — an un-losable modal login
presentation (AppKit sheet), a mandated always-present close affordance, **no in-field autofill
affordance** (deleted; Cmd+V / Edit ▸ Paste is the sole credential-entry route), Cmd+V preserved,
the pristine Epic surface byte-untouched (REQ-34.4.2-10) — all LIVE-PROVEN by a blocking six-item
hardware gate (REQ-34.4.2-09). The phase does not close on a green test suite alone.

**Verified:** 2026-08-06T00:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — the existing `34.4.2-VERIFICATION.md` (dated 2026-08-05T00:00:00Z) was
STALE. It described gate run 2 (`34.4.2-LIVE-GATE-RERUN-2.md`, FAIL 5/6, item 3 the in-field
autofill fill failure) and a phase goal that still promised a working autofill affordance and a
kill switch — both since deliberately deleted by operator decision D-A. This report replaces it in
full, verifying gate run 3 (`34.4.2-LIVE-GATE-RERUN-3.md`, FAIL 5/6, item 6 the Humble-disconnect
wedge) against the corrected goal and current source.

## Independent re-verification performed (not taken on the documents' own word)

- `strings`/grep-equivalent check on current `src-tauri/src/main.rs`: `grep -ciE 'autofill'` → **0**;
  `grep -c GAMELIB_AUTOFILL_GLYPH` → **0**; `grep -c post_autofill_right_click` → **0**. The
  deletion is real and complete at the source level, independent of any document's claim.
- Ran `npx jest src/backend/__tests__/tauriShellSource.test.ts -t "PHASE_34_4_2"` directly: the
  mutation-proven absence guard (`PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS`, Test 1) **passes**, along
  with the Plan 08/11/14 scope-guard tests confirming Epic's pristine regions reference none of this
  phase's new symbols.
- Read `PENDING_VISIBLE_LOGIN_WINDOW`'s refuse-or-arm block directly (`main.rs:3355-3389`):
  independently confirmed `#[cfg(target_os = "macos")]` and `if visible == true` gate it — matches
  the gate document's own claim, not merely trusted from it.
- Ran the full test suite and cargo suite myself rather than trusting the quoted baselines:
  `npm run test:ci` → **191/191 suites, 3747/3747 tests** (exact match to the plan 16 preflight's
  quoted figure); `cargo test --manifest-path src-tauri/Cargo.toml` → **115 passed, 1 ignored, 0
  failed** (exact match).
- Confirmed `git log` and `git status`: no commits or working-tree changes exist after
  `34.4.2-16-PLAN.md`'s completion (`b6a87e1bb`) touching `src-tauri/src/main.rs` or
  `src/backend/humble/user.ts` — the F-34.4.2-12 wedge is genuinely undiagnosed and unfixed as of
  this verification, not silently patched without documentation.
  **CORRECTED 2026-08-06 (this claim is now FALSE and must not be relied on):** commit
  `6bad86227`, landing at 13:57 the same day this report was verified, touches
  `src-tauri/src/main.rs` and diagnoses+fixes F-34.4.2-12. A reader must not come away believing
  the wedge is still unfixed in source — see the CORRECTION banner at the top of this document.
- Confirmed `login-window-ux-macos.md` (the skill reference) actually carries the FALSIFIED/
  SUPERSEDED markings plan 15 claimed, not merely a SUMMARY assertion.
- Cross-checked all ten REQ-34.4.2-01..10 rows in `REQUIREMENTS.md` against `34.4.2-LIVE-GATE-RERUN-3.md`'s
  own item results and found them consistent (7 ticked: 01/02/03/06/07/08/10; 3 unticked: 04/05/09) —
  no overclaim found.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | REQ-34.4.2-01: sheet presentation attaches, survives drag/minimize/Dock-restore | ✓ VERIFIED | Gate run 3 item 1 PASS; both required log lines (`read-back attached=true`, `sheet_presented=true`) independently confirmed present twice in the surviving transcript for two separate windows, in the correct order; retired re-raise line confirmed absent (0 occurrences). Source functions (`present_login_window_as_sheet` main.rs:2312, read-back line main.rs:2436) independently located and match. |
| 2 | REQ-34.4.2-02: sheet remains visible and interactive after minimize/restore | ✓ VERIFIED | Same item 1, sub-check (e) — first-ever live measurement of F-34.4.2-01's post-restore claim (typed input accepted, cancel strip responds immediately after restore) — across five gate runs. Rests on the operator's report for the gesture itself (no machine signal for mouse/keyboard/window-manager actions by this item's own design), but is the FIRST time this specific claim was even attempted, closing a real prior gap. |
| 3 | REQ-34.4.2-03: mandated close affordance (cancel-strip + bare-Esc) dismisses the sheet and releases the main window | ✓ VERIFIED | Gate run 3 item 2 PASS. Route (a), cancel-strip: independently confirmed present TWICE in the surviving transcript (`login-sheet: cancel requested for '...' via strip`). **Caveat, not downgraded to a gap:** route (b)'s own required `via esc` line does not appear anywhere in the surviving transcript — its evidence rests on the operator's report alone, same evidence-survival pattern as F-34.4.2-11 (though not formally attributed to it in the gate document). Source confirms `request_login_sheet_cancel` (main.rs:1481) emits both route variants from a single entry point. |
| 4 | REQ-34.4.2-04/-05: no in-field autofill affordance exists on any Tauri-managed login surface, on any platform; Cmd+V/Edit▸Paste is the sole credential-entry route | ? UNCERTAIN | **Static half VERIFIED independently** (see "Independent re-verification" above — 0 grep hits, mutation-proven test passes when run directly, not merely claimed). **Live half's decisive evidence is LOST** — gate run 3 item 3 sub-checks (b) zero-log-occurrence and (c) `GAMELIB_AUTOFILL_GLYPH=0` no-op have no surviving transcript corroboration (F-34.4.2-11, the mandatory `tee` truncating on the mandated item-3(c) relaunch). PASS is recorded on the operator's unqualified verbatim word only. Not treated as a failure — the operator's account stands — but the live corroboration this gap cycle specifically existed to produce did not survive. |
| 5 | REQ-34.4.2-06: Cmd+V and Edit ▸ Paste, now the SOLE credential-entry route, work undisturbed by the Esc monitor and complete a real login | ? UNCERTAIN | Gate run 3 item 4 PASS reported (Cmd+V + Edit▸Paste land text, Esc-monitor sub-check holds). **The required aggregate completion line (`Humble sync finished: gamekeys={N} fetched={M}/{total}`) is absent from the surviving transcript** — a JS/TS backend line, not `[shell]`-prefixed, so not necessarily subject to the same tee-truncation mechanism, but its absence is unexplained and unclaimed by any specific cause. Raised stakes: with the autofill affordance deleted, this is now the ONLY working credential-entry route the product offers, so its own evidence gap matters more than a cosmetic corroboration shortfall would elsewhere. |
| 6 | REQ-34.4.2-07: no credential store is built or read; DummyStore never ships | ✓ VERIFIED | Source-closed, unaffected by this cycle's plans (13-16 touch `src-tauri/src/main.rs`, `tauriShellSource.test.ts`, docs only). Containment guard pre-dates this cycle and is untouched. |
| 7 | REQ-34.4.2-08: all new behaviour is macOS-gated; other platforms/Electron unaffected | ✓ VERIFIED | Independently confirmed the single-flight latch (this cycle's only new runtime behaviour) is `#[cfg(target_os = "macos")]`-gated at `main.rs:3355`, not merely claimed. Pre-existing standing wart (autofill glyph call site previously un-gated) is now moot — the code it gated no longer exists. |
| 8 | Single-flight guard (T-34.4.2-39/-41) refuses a second visible login window; no unrequested sheet arrives on dismissal | ? UNCERTAIN | Guard mechanism independently source-verified correct, macOS-gated, and cleared by exact label match (`clear_pending_visible_login_window`, main.rs:2250-2259). Gate run 3 item 5 PASS reported by the operator, but **its own decisive evidence (the REFUSED line, the specific Amazon-then-Humble scenario) has zero occurrences in the surviving transcript** — the same F-34.4.2-11 evidence-survival defect. What IS present is an unrelated but genuine corroboration: two clean sequential arm/clear cycles of the latch on the visible path. |
| 9 | REQ-34.4.2-10: the pristine Epic login window is byte-untouched by this phase | ✓ VERIFIED | Source-provable and independently re-confirmed this session: `open_pristine_epic_login_window` and `EpicPristineNavDelegate` are covered by a machine-enforced, mutation-tested absence guard (`tauriShellSource.test.ts` Tests 3/4/5/8, run directly and confirmed passing) that fails if any of this phase's new symbols appear inside either region. **Live re-confirmation (item 6(b)) was NOT ATTEMPTED this cycle** — the item-6(a) force-kill ended the session before Epic's window could be opened — but this does not contradict the source-level proof, which remains the primary, unaffected basis for this tick. |
| 10 | The Humble disconnect/logout control does not hang the application | ✗ FAILED (source-fixed 2026-08-06, 6bad86227; awaiting live re-measurement) | Gate run 3 item 6(a): a hard, unbounded main-thread wedge, force-kill required. F-34.4.2-12, escalating the pre-existing bounded F-34.4.2-10 timeout. **CORRECTION 2026-08-06:** this row's original claim that "no fix or diagnosis has landed" and that "the code is exactly as it was when measured" is now FALSE and is struck. Root cause diagnosed (reentrancy self-deadlock on tao's `EventLoopHandler` handler mutex via wry's blocking `.cookies()` getter) and fixed in commit `6bad86227` (async `WKHTTPCookieStore.getAllCookies(completionHandler:)`), independently verified across four channels (operator live repro, `cargo test` green with regression pin, `humble_store/config.json` genuinely emptied, fresh launch log reaching store teardown) per `.planning/debug/resolved/humble-disconnect-main-wedge.md`. This row's status remains FAILED because that fix has not yet been re-measured by a live gate run — the status cell reflects "awaiting live re-measurement," not "still broken in source." See gaps below. |
| 11 | REQ-34.4.2-09: the blocking six-item hardware gate records a full PASS on real macOS hardware, not merely a measured result | ✗ FAILED | `34.4.2-LIVE-GATE-RERUN-3.md` frontmatter: `verdict: FAIL`, `items_passed: 5` of 6. Per this phase's own binding D-08 no-partial-pass rule, item 6's single FAIL fails the entire gate. This is the FIFTH gate run in this phase's history and the phase has never yet recorded a full PASS. |

**Score:** 6/11 truths VERIFIED, 3/11 UNCERTAIN, 2/11 FAILED.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src-tauri/src/main.rs` — `present_login_window_as_sheet` (L2312), `dismiss_login_window_sheet` (L2529) | Sheet presentation/dismissal mechanism | ✓ VERIFIED | Present, wired into `humble_login_open`'s visible-only arm; read-back and dismiss log lines independently located and confirmed live-firing (item 1/2 transcript evidence). |
| `src-tauri/src/main.rs` — `request_login_sheet_cancel` (L1481) | Shared cancel-strip/Esc dismissal entry point | ✓ VERIFIED | Present, called from both routes; cancel-strip route independently confirmed live-firing twice. |
| `src-tauri/src/main.rs` — autofill mechanism (13 symbols) | Must be ABSENT | ✓ VERIFIED ABSENT | Independently confirmed 0 grep hits for the autofill stem and `GAMELIB_AUTOFILL_GLYPH`; mutation-proven absence test run directly and passing. |
| `src-tauri/src/main.rs` — `PENDING_VISIBLE_LOGIN_WINDOW` single-flight latch (L2218-2259, L3355-3389) | Single-flight guard on visible login windows | ✓ VERIFIED, WIRED | Present, macOS-gated, scoped to `if visible == true`, clears on exact label match; live discharge UNCERTAIN (evidence lost, F-34.4.2-11). |
| `src-tauri/src/main.rs` — `open_pristine_epic_login_window`, `EpicPristineNavDelegate` | Must remain byte-identical | ✓ VERIFIED | Machine-enforced guard tests run directly and passing; no diff since this cycle's plans by construction (guard would fail otherwise). |
| `src/backend/humble/user.ts` — disconnect/logout flow (hidden-window path L813/L960, `clearHumbleStorage` L919-1116) | Must not hang | ✗ FAILED — live wedge (source-fixed 2026-08-06, awaiting live re-measurement) | F-34.4.2-12. **CORRECTED 2026-08-06:** the wedge's actual location was `src-tauri/src/main.rs`'s cookie-read arms (`humble_login_cookies_for_domain`, `humble_login_clear_cookies`), NOT this file's `clearHumbleStorage` storage-wipe step — execution deadlocked strictly before `clearHumbleStorage` ever ran. Fixed in `6bad86227`. F-34.4.2-10's bounded-timeout debt in THIS file's `clearHumbleStorage` (L919-1116) remains OPEN and unfixed — it was simply never reached in the run that measured this row, and must not be conflated with F-34.4.2-12's closure. |
| `34.4.2-LIVE-GATE-RERUN-3.md` | Authoritative measured gate result | ✓ VERIFIED (as a record) | Frontmatter and item-level detail internally consistent; independently cross-checked against `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, `34.4.2-PLATFORM-SCOPE.md` §5, all of which agree with it (no propagation gap found). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `humble_login_open` (visible arm) | `present_login_window_as_sheet` | direct call, after `.build()` | ✓ WIRED | Confirmed by source location and by two independent live firings in the transcript. |
| `humble_login_open` (visible arm) | `PENDING_VISIBLE_LOGIN_WINDOW` refuse-or-arm check | `#[cfg(target_os = "macos")] if visible == true` block, before the builder call | ✓ WIRED, macOS-gated | Independently confirmed ordering and gating in source. |
| Cancel strip / Esc monitor | `request_login_sheet_cancel` → `dismiss_login_window_sheet` | shared entry point | ✓ WIRED | Strip route live-confirmed twice; Esc route wired in source but its own live firing is uncorroborated by transcript (operator-reported only). |
| Humble disconnect click (frontend) | hidden `seam.open(..., {visible:false})` → shell | `src/backend/humble/user.ts:813/960` | ⚠️ WIRED BUT HANGS | The link exists and is reachable (that's how the wedge was triggered), but something downstream of it does not return — see gap. |

### Requirements Coverage

| Requirement | Source Plan(s) | Status | Evidence |
|---|---|---|---|
| REQ-34.4.2-01 | 01, 07, 12, 16 | ✓ SATISFIED (ticked `[x]`) | Gate item 1 PASS, re-measured against current source, independently corroborated. |
| REQ-34.4.2-02 | 07, 12, 16 | ✓ SATISFIED (ticked `[x]`) | Gate item 1 sub-check (e), first-ever measurement, re-confirmed. |
| REQ-34.4.2-03 | 08, 11, 12, 16 | ✓ SATISFIED (ticked `[x]`) | Gate item 2 PASS; route (a) machine-corroborated, route (b) operator-reported only. |
| REQ-34.4.2-04 | 03, 04, 13, 16 | ? NEEDS HUMAN / UNCERTAIN (correctly left unticked `[ ]`) | Static absence VERIFIED independently; live confirmation's decisive evidence lost to F-34.4.2-11. `REQUIREMENTS.md`'s own unticked state is CORRECT — not an overclaim. |
| REQ-34.4.2-05 | 04, 13, 16 | ? NEEDS HUMAN / UNCERTAIN (correctly left unticked `[ ]`) | Same basis as REQ-04. |
| REQ-34.4.2-06 | 12, 16 | ? Ticked `[x]` but evidence is thinner than the tick implies | Gate item 4 PASS reported; the requirement's own raised bar (a real login must COMPLETE via paste alone) has no surviving corroborating log line. Flagged here as a soft overclaim risk, not reversed — the operator's PASS report is not being second-guessed, but the tick rests on less machine evidence than REQ-01/02/03/07/08/10 do. |
| REQ-34.4.2-07 | 05 | ✓ SATISFIED (ticked `[x]`) | Source-closed, pre-existing, unaffected by this cycle. |
| REQ-34.4.2-08 | 01, 08, 09, 14 | ✓ SATISFIED (ticked `[x]`) | Independently confirmed macOS-gating on this cycle's only new runtime behaviour. |
| REQ-34.4.2-09 | 09, 10, 11, 12, 15, 16 | ✗ BLOCKED (correctly left unticked `[ ]`) | Gate verdict FAIL 5/6, item 6 the sole failure. No overclaim — `REQUIREMENTS.md` states this accurately. |
| REQ-34.4.2-10 | 01-05 (guard), 12, 16 | ✓ SATISFIED (ticked `[x]`) | Source-provable machine-enforced guard, independently re-run and confirmed passing; live re-confirmation (item 6(b)) NOT ATTEMPTED this cycle but does not undermine the source proof. |

**No orphaned requirements found** — all ten REQ-34.4.2 IDs are claimed by at least one plan's frontmatter and are individually accounted for in `REQUIREMENTS.md`.

**No overclaim found** in `REQUIREMENTS.md`'s checkbox state itself — every `[x]`/`[ ]` matches what the gate actually measured. The one soft concern (REQ-06's tick resting on a real-login-completion claim with no surviving corroborating log line) is noted above but not severe enough to recommend reversing the tick, since the operator's own account is specific and unqualified, and D-08's own no-partial-pass rule already keeps the phase from closing regardless.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `34.4.2-LIVE-GATE-RERUN-3.md` | Evidence-capture instruction | Mandatory `tee` (no `-a`) collides with a mandated mid-run relaunch | ⚠️ Warning (process, not code) | F-34.4.2-11 — already logged in `deferred-items.md`, not a new finding, but still unfixed. Affects future gate contracts, not just this one. |
| `src/backend/humble/user.ts` | `clearHumbleStorage` step (~L919-1116) | Pre-existing bounded-timeout debt (F-34.4.2-10) — **CORRECTED 2026-08-06: NOT the F-34.4.2-12 escalation site.** F-34.4.2-12's actual location was `src-tauri/src/main.rs`'s cookie-read arms, never reached this run before it deadlocked; `clearHumbleStorage`'s own F-34.4.2-10 timeout was never reached either and remains open, separately, unfixed | 🛑 Blocker (residual F-34.4.2-10 debt) | Not a TBD/FIXME/XXX marker — a measured live behavioural defect (F-34.4.2-10, still open). F-34.4.2-12 itself is source-fixed (`6bad86227`) and no longer blocks on this row; the phase still does not close per D-08 pending the item-6 gate re-run. |

No `TBD`/`FIXME`/`XXX` debt markers found in the files this cycle's plans (13-16) modified.

### Behavioral Spot-Checks

Skipped — this phase's own behaviours (AppKit sheet presentation, single-flight window arbitration, a main-thread hang) are not testable via a 10-second non-interactive command; they require the human-driven hardware gate this phase already mandates and ran (`34.4.2-LIVE-GATE-RERUN-3.md`). Independent verification instead re-ran the automated suites and re-grepped source directly (see "Independent re-verification performed" above) rather than substituting a synthetic spot-check for the gate's own live-hardware requirement.

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist for this phase, and neither the PLAN nor SUMMARY files declare any. This phase's own probe-equivalent is the blocking live gate (`34.4.2-LIVE-GATE-RERUN-3.md`), which is treated in this report as the authoritative measured result per the task's own instruction, not re-executed (it requires human-driven macOS hardware interaction, real Humble network access, and a real disconnect click that is already known to hang the app).

### Human Verification Required

None new. The phase's own blocking gate already IS the human-verification mechanism this phase mandates, and it already ran (FAIL 5/6). No additional human-verification items are being deferred to a HUMAN-UAT.md — the next step is gap-cycle planning against the two gaps below, not a fresh round of manual spot-checks.

### Gaps Summary

**What closed this cycle (gap cycle 3, plans 13-16):** The in-field autofill affordance's own root defect (F-34.4.2-09, the synthesized right-click surfacing AutoFill but never filling) is closed for good — not by fixing the mechanism, but by operator decision D-A deleting it in full. This is independently re-confirmed in current source (zero grep hits, a mutation-proven absence test that this verification ran directly and watched pass) rather than taken on the SUMMARY's word. The single-flight guard closing T-34.4.2-39 (an unrequested second sheet queuing behind a slow upstream spawn) is also independently confirmed present, correctly macOS-gated, and correctly scoped. `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, and `34.4.2-PLATFORM-SCOPE.md` are all internally consistent with the gate result and with each other — no propagation gap was found anywhere in this cross-check.

**What did not close, and why the phase still does not close:** The blocking six-item gate — the mechanism REQ-34.4.2-09 requires and the mechanism this whole phase is designed never to skip — recorded FAIL 5/6 for the fifth time running in this phase's five-gate history. This time the failure is **item 6(a): the Humble disconnect/logout control hangs the application with a hard, unbounded main-thread wedge, requiring a force-kill** (F-34.4.2-12). This is a NEW, BLOCKING finding, unrelated to and not caused by the item 3 defect that closed this cycle. As a direct consequence, item 6(b) — the live re-confirmation that Epic's login window remains untouched — was **never attempted**, because the app was killed before it could be opened. Per this phase's own binding D-08 no-partial-pass rule, one item's FAIL fails the entire gate, and the phase does not close on 5/6 regardless of which item is the failure.

**A separate, non-blocking but explicitly owed finding:** F-34.4.2-11, a fifth instance of this phase's own recurring contract-authoring-defect pattern (and the first that is an *interaction* between two individually-reachable requirements rather than a single unreachable one) — the gate contract's own mandatory `tee` truncates on every relaunch, and item 3(c) separately and correctly mandates a relaunch. The two items this entire gap cycle existed to measure for the first time (item 3: the deletion's live absence; item 5: the single-flight guard's live discharge) consequently PASSED on the operator's verbatim word alone, with zero surviving transcript corroboration for either item's own decisive evidence line. This verification does not treat those PASSes as failures — the operator's account is specific and unqualified — but it does not treat them as equivalent to the machine-corroborated PASSes on items 1, 2, and 4's primary route either. This is recorded honestly as a residual verification-confidence gap, per the task's own instruction not to downgrade to failure and not to inflate to full corroboration.

**What gap cycle 4 must scope against, in priority order (item 1 rewritten 2026-08-06 — see CORRECTION banner above):**
1. **F-34.4.2-12 is CLOSED at source (6bad86227); no discriminator and no timeout remain to be built.** Both were done in this cycle (see the corrected gap-1 record above): a live hung-process `sample` named the blocked thread directly (candidates (a)/(b) FALSIFIED, (c) SUBSUMED), and the fix is the async `WKHTTPCookieStore.getAllCookies(completionHandler:)` pattern — not a timeout, which cannot fix a reentrancy self-deadlock below the level any Tauri-side receive timeout lives. The `humble_login_cookies` residual (plan 34.4.2-17) is also closed. **The remaining scope is exactly one item: a full six-item gate re-run recording a genuine PASS, including the never-attempted 6(b) Epic sub-check.**
2. **F-34.4.2-11 (owed, non-blocking).** Fix the evidence-capture contract (append `-a` with per-launch markers, or a uniquely-named log per relaunch) and add a fifth Structural-Reachability-Review defect-class test for requirement-interaction reachability, before the next gate contract is authored — so this is not a sixth instance of the same pattern. Whether items 3 and 5 need a dedicated re-measurement with intact evidence, or can ride along with gap cycle 4's own item-6 re-run, is gap cycle 4's own scoping decision — this verification does not mandate a standalone re-run for items already PASSED on an unqualified operator report.
3. **REQ-34.4.2-06's thin evidence (soft, not a formal gap).** The now-load-bearing Cmd+V/paste-completes-a-real-login claim has no surviving corroborating log line. Not severe enough to reverse the tick, but worth capturing in the next gate run's own evidence if the transcript-survival fix from item 2 above is implemented, since it would cost nothing additional to also corroborate this line at the same time.

---

_Verified: 2026-08-06T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
