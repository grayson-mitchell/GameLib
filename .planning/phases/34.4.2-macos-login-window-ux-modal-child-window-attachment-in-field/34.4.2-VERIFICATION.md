---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
verified: 2026-08-05T00:00:00Z
status: gaps_found
score: 7/10 requirements verified (REQ-01/02/03/06/07/08/10); 5/6 blocking-gate items passed (verdict FAIL, no-partial-pass)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 3/10 requirements verified; 0/6 blocking-gate items passed
  gaps_closed:
    - "A visible Tauri-managed login window is presented as an AppKit sheet on `main`, un-losable, surviving minimize/restore (REQ-34.4.2-01/-02) — CLOSED: three-round debug arc (commits 751521663/56d4986f8/8b2fdb315) fixed CR-01/CR-02 outside any plan's own execution; live gate item 1 recorded PASS on all six sub-checks (a)-(f) on real macOS hardware, including the first-ever measurement of post-restore interactivity (sub-check e, F-34.4.2-01)."
    - "The sheet carries a mandated, always-present close affordance that resolves the login as cancelled (REQ-34.4.2-03) — CLOSED: plan 11's WR-03/WR-04/WR-01 fixes landed; live gate item 2 recorded PASS on both required routes (cancel-strip click, bare Esc), main window re-interactive after each, exactly one Cancel-sign-in control on Humble's real iframe-heavy form."
    - "Cmd+V paste works undisturbed by the new Esc monitor (REQ-34.4.2-06) — CLOSED: live gate item 4 recorded PASS on the Humble surface (DummyStore half BLOCKED-BY-DESIGN, excluded from the PASS bar by contract amendment); Edit > Paste also verified with no divergence from Cmd+V."
    - "The `GAMELIB_AUTOFILL_GLYPH=0` kill switch disables the glyph while the cancel strip still works (T-34.4.2-15 residual) — CLOSED: live gate item 5 recorded PASS on both arms; the two arms' log lines differ, proving the switch is genuinely wired, and the cancel strip survived the kill switch as the mandatory sub-check requires."
    - "Hidden reveal windows get no glyph/strip, and the pristine Epic login window is byte-untouched (REQ-34.4.2-04 partial / REQ-34.4.2-10) — the Epic-untouched half is now LIVE-RECONFIRMED (item 6(b), all four required absences observed on hardware), not merely source-provable as in the prior verification."
  gaps_remaining:
    - "The in-field key glyph pops the real system AutoFill menu but does NOT fill the field on the synthesized-right-click path (REQ-34.4.2-04/-05) — this is now a genuine MEASURED FAIL (item 3), not a NOT-ATTEMPTED precondition block as in the prior verification. The failure moved from 'nothing was reachable' to 'the specific mechanism does not work,' which is a different and more actionable finding, not a regression."
    - "A blocking live gate records a measured PASS across every item, not merely a measured result (REQ-34.4.2-09) — still unmet: the gate ran to completion (satisfying the literal 'records a measured result' clause, a step forward from two prior gates that aborted at item 1), but verdict is FAIL, items_passed 5/6, and D-08 no-partial-pass means the phase does not close."
  regressions: []
gaps:
  - truth: "The in-field key glyph, via a synthesized right-click, surfaces the AutoFill menu and successfully fills the password field on a real store login (REQ-34.4.2-04/-05)"
    status: failed
    reason: "Measured live on real macOS hardware, 2026-08-05 (34.4.2-LIVE-GATE-RERUN-2.md item 3, run by plan 34.4.2-12, contract authored by the separate, non-executing plan 34.4.2-11). The synthesized-right-click poster (post_autofill_right_click, src-tauri/src/main.rs:1851) fires correctly 5/5 activations, correctly skips makeKeyAndOrderFront under the WR-07 sheet-presented branch, and targets the correct element (hit_tag=Some(\"INPUT\"), hit_type=Some(\"password\")). The real system context menu pops with 'AutoFill ›' present. A seeded Passwords entry was selected. The field did NOT fill. Decisive discriminator: a REAL right-click in the identical sheet/field/seeded-entry DOES fill, ruling out the sheet context, Humble's page, and the platform as causes — the failure is specific to the synthesized-event path itself. New finding F-34.4.2-09 records that this falsifies spike 022's own Recommendation #4 (login-window-ux-macos.md), whose evidence only ever showed the menu appearing, never that fill succeeds. Separately, F-34.4.2-08 (confirmed present in current source at src-tauri/src/main.rs:1499, `String.fromCharCode(128273)`, a BMP-truncation bug that renders the glyph as an invisible tofu box instead of a key icon) compounds the discoverability failure even before the fill defect is considered."
    artifacts:
      - path: "src-tauri/src/main.rs"
        issue: "post_autofill_right_click (line 1851) and synth_autofill_mouse_event (line 1795) construct and post a synthesized RightMouseDown/RightMouseUp NSEvent pair via public API only; this is source-complete and now live-confirmed to reach the correct element and pop the correct menu, but the underlying AppKit/WebKit mechanism does not carry through to an actual fill when the event is synthesized rather than user-originated. No further source-level fix is evidenced in the current tree — this is a live-measured behavioral gap in the design premise (spike 022 Recommendation #4), not a coding defect fixable by adjusting this function's own logic."
      - path: "src-tauri/src/main.rs"
        issue: "Line 1499: glyph.textContent = String.fromCharCode(128273) truncates codepoint U+1F511 (KEY) to the BMP, rendering U+F511 (an unassigned Private Use Area codepoint, empty box in every font). Confirmed present in current source, unfixed. Deliberately deferred by plan 12 so the measured gate run was not contaminated by a mid-run rebuild (F-34.4.2-08)."
    missing:
      - "A decision on the glyph's future: drop it entirely in favor of Cmd+V/real-right-click (operator's own post-gate direction, recorded in this session's context but not yet reflected in any committed plan or deferred-items.md entry — see note below), keep it as a discoverability hint only, or find a different synthesis approach that actually fills."
      - "If the glyph is kept in any form, fix F-34.4.2-08 (one-line: String.fromCodePoint(128273))."
      - "Correct login-window-ux-macos.md's Recommendation #4, which is now a stale/falsified project knowledge artifact that will mislead future work if left as-is (it already carries one other stale recommendation, #1, superseded by the sheet-based design)."
  - truth: "A blocking live gate on real macOS hardware records a measured PASS across all items; the phase does not close on a green suite alone (REQ-34.4.2-09)"
    status: failed
    reason: "The gate (34.4.2-LIVE-GATE-RERUN-2.md, run by plan 34.4.2-12 on 2026-08-05) reached completion for the first time in this phase's history — all six items got a real Observed:/Verdict: result (a genuine advance over the prior verification's 0/6, where five items were NOT ATTEMPTED). Verdict: FAIL. items_passed: 5 of 6. Per D-08 (no-partial-pass), one FAIL fails the whole gate regardless of how many items PASS. The requirement's literal clause ('a gate records a measured result') is now satisfied; its substantive intent ('the behaviours actually work, live, not merely tested in isolation') is not, because item 3 fails."
    artifacts:
      - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md"
        issue: "Frontmatter: verdict FAIL, items_passed 5, items_total 6. Item 3's own Observed:/Verdict: fields record the field-fill failure in full detail with a decisive real-vs-synthesized discriminator."
    missing:
      - "Resolution of item 3's gap (see above) followed by a genuine 6/6 PASS re-run, OR a scoped descope of the glyph (with REQ-34.4.2-04/-05 correspondingly rewritten) that lets the phase close on the remaining five items plus an explicit, deliberate exclusion of in-field AutoFill fill as a shipped capability."
deferred: []
human_verification: []
---

# Phase 34.4.2: macOS login-window UX — Verification Report

**Phase Goal:** macOS login-window UX for Tauri-managed login surfaces (Humble/GOG/Amazon): an
un-losable modal login presentation (AppKit sheet), a mandated always-present close affordance,
an in-field autofill affordance (key glyph → synthesized right-click → AutoFill menu), Cmd+V
preserved, a kill switch, the pristine Epic surface byte-untouched (REQ-34.4.2-10) — all
LIVE-PROVEN by a blocking six-item hardware gate (REQ-34.4.2-09); the phase does not close on a
green test suite alone.

**Verified:** 2026-08-05T00:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — after gap closure (previous verification 2026-08-04T09:30:00Z scored
3/10, verdict driven by CR-01/CR-02, sheet presentation itself never worked). **That diagnosis is
now STALE and superseded**: CR-01/CR-02 were closed by a three-round debug arc
(`.planning/debug/resolved/white-window-not-sheet-cr01.md`, commits `751521663` →
`56d4986f8` → `8b2fdb315`) before this gate ran. The phase advanced substantially (0/6 → 5/6,
5 items NOT ATTEMPTED → 0 items NOT ATTEMPTED) but a NEW, more specific, better-discriminated
failure (item 3 / F-34.4.2-09) still blocks closure. This is progress, not a stall — but D-08
(no-partial-pass) still applies and the phase still does not close.

## Goal Achievement

**Authoritative source used for this verification:** `34.4.2-LIVE-GATE-RERUN-2.md` (verdict FAIL,
items_passed 5/6, run 2026-08-05 by plan 34.4.2-12, contract authored by the non-executing plan
34.4.2-11 — author/runner separation held). Cross-checked against `34.4.2-11/-12-SUMMARY.md`,
`.planning/REQUIREMENTS.md`, `34.4.2-PLATFORM-SCOPE.md`, and independently re-read against current
`src-tauri/src/main.rs` source (not merely trusted from the gate document's own line-number
citations).

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | A visible Tauri-managed login window is presented as an AppKit sheet on `main`, un-losable, surviving minimize/restore, staying interactive (REQ-34.4.2-01/-02) | ✓ VERIFIED | `34.4.2-LIVE-GATE-RERUN-2.md` item 1: all six sub-checks (a)-(f) PASS on real hardware. Sub-check (e) is the first-ever live measurement of F-34.4.2-01's post-restore interactivity claim across five gate runs; sub-check (f) is the first-ever live proof of WR-07's mid-session-detachment fix. `attached=true` read-back confirmed for every login window across the transcript (7 windows, 570-636ms elapsed). |
| 2 | The sheet carries a mandated, always-present close affordance (cancel strip + bare Esc) that resolves the login as cancelled, and releases the main window (REQ-34.4.2-03) | ✓ VERIFIED | Item 2: both required routes (strip click, bare Esc) PASS, main window confirmed interactive after each, exactly one Cancel-sign-in control on Humble's real iframe-heavy form (WR-03 sub-check). Note: the contract's own literal `status=cancelled reason=window-closed` line is an OAuth-runner-only signal Humble structurally cannot emit (contract defect, not app defect — Humble's own equivalent cancellation signal was used instead, itself observed 7 times matching the dismissals). |
| 3 | The in-field key glyph pops the real system AutoFill menu AND fills the field, on a real store login (REQ-34.4.2-04/-05) | ✗ FAILED | Item 3: glyph visible, menu pops, `AutoFill ›` present, element targeting correct — but the field does NOT fill on the synthesized-right-click path. A real right-click in the identical sheet/field/seeded-entry DOES fill. F-34.4.2-09: this falsifies the phase's own design premise (spike 022 Recommendation #4). DummyStore sub-item BLOCKED-BY-DESIGN (https-only shell gate, `main.rs:926`), excluded from the PASS bar by contract amendment — item 3 is decided by the Humble sub-item alone, and that sub-item FAILS. |
| 4 | Cmd+V paste works undisturbed by the new Esc monitor, on the Humble surface (REQ-34.4.2-06) | ✓ VERIFIED | Item 4: Cmd+V lands text with the glyph present; Esc does not fire while typing; Cmd+V is not swallowed by the Esc monitor; Edit ▸ Paste also works with no divergence. Now load-bearing: with item 3 FAILing, Cmd+V is the ONLY working credential-entry route this gate proved. |
| 5 | The `GAMELIB_AUTOFILL_GLYPH=0` kill switch disables the glyph while the cancel strip still works, on both arms (T-34.4.2-15, REQ-34.4.2-04) | ✓ VERIFIED | Item 5: arm 1 (unset) glyph present, login completes (`Humble sync finished: gamekeys=31 fetched=31/31`); arm 2 (`=0`, via relaunch) no glyph injected, cancel strip still present and still dismisses, login still completes. The two arms' log lines DIFFER, proving the switch is genuinely wired, not a no-op. |
| 6 | Hidden reveal windows get no glyph/strip, and the pristine Epic login window is byte-untouched (REQ-34.4.2-04 partial / REQ-34.4.2-10) | ✓ VERIFIED | Item 6(a): measured by an alternate equivalent route (Humble disconnect, same `if visible` gate) after the contract's own concurrency framing proved structurally impossible — no glyph/strip/sheet line attributable to any hidden window. Item 6(b): Epic's login window shows all four required absences (no glyph, no cancel strip, no autofill sentinel, no sheet-presentation line) on real hardware — REQ-34.4.2-10's locked scope decision LIVE-RECONFIRMED, not merely source-provable as in the prior verification. |
| 7 | No credential store is built; the DummyStore harness never ships in production, loopback-only (REQ-34.4.2-07) | ✓ VERIFIED | `dummyStoreHarnessContainment.test.ts` guard passing (part of the green suite, 191/191 suites at plan 12's preflight); source-provable, non-live claim, unaffected by this gate's findings. |
| 8 | All new behaviour is `#[cfg(target_os = "macos")]`-gated; Windows/Linux/Electron behaviour unchanged (REQ-34.4.2-08) | ✓ VERIFIED (with a standing, documented, non-blocking wart) | `34.4.2-PLATFORM-SCOPE.md` §1 per-symbol cfg table; the autofill glyph's own call site and `parse_autofill_request`'s call site remain un-gated (render inertly off-macOS) — a pre-existing, tracked wart, not new this cycle, and not fixed here (out of scope). |
| 9 | A blocking live gate on real macOS hardware records the measured result of every behaviour, and that result is an all-PASS, not inferred from a green suite (REQ-34.4.2-09) | ✗ FAILED | The gate ran to full completion for the first time (a genuine advance from the prior 0/6-with-5-NOT-ATTEMPTED state) — but items_passed is 5/6, verdict FAIL. D-08 no-partial-pass applies regardless of the improvement. |
| 10 | The pristine Epic login window is provably untouched (REQ-34.4.2-10) | ✓ VERIFIED | Source guard (`PHASE_34_4_2_NEW_SYMBOLS` + Tests 3/4 in `tauriShellSource.test.ts`) unchanged across all 12 plans; `git diff` on `open_pristine_epic_login_window` remains zero lines; item 6(b) above is the live re-confirmation on top of this static proof. |

**Score:** 7/10 requirement-level truths verified (REQ-01/02/03/06/07/08/10). REQ-34.4.2-04/-05
FAILED on a genuine live measurement (not a precondition block). REQ-34.4.2-09 FAILED on
no-partial-pass. **5/6 blocking gate items passed; verdict FAIL (D-08 applies).**

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src-tauri/src/main.rs :: present_login_window_as_sheet` / `dismiss_login_window_sheet` | Sheet presentation + dismissal mechanism | ✓ VERIFIED | Live-proven `attached=true` on every window this gate run (7/7); dismissal live-proven on both routes (item 2). CR-01/CR-02 fixes confirmed present in current source (`SHEET_PRESENT_WKWEBVIEW_WARMUP_DELAY` at main.rs:2776, the 250ms deferral; `PRESENTED_LOGIN_SHEETS` registration only on confirmed attachment). |
| `login_cancel_strip_script` / `request_login_sheet_cancel` / Esc local monitor | Mandated close affordance | ✓ VERIFIED | Call site confirmed macOS-gated (`main.rs:3668-3675` region); live-proven both dismissal routes PASS with exactly-one-control sub-check (WR-03) holding. |
| `autofill_glyph_script` / `post_autofill_right_click` / `synth_autofill_mouse_event` | In-field key glyph → synthesized right-click → AutoFill | ⚠️ HOLLOW (wired, correctly targeted, but does not achieve its stated purpose) | Source confirmed present and correctly gated (`post_autofill_right_click` at main.rs:1851, WR-07 skip-branch log line confirmed verbatim in current source at main.rs:1974). Live-measured: fires correctly, targets correctly, pops the correct menu — but does not fill. This is a genuine mechanism-level failure (F-34.4.2-09), not a wiring gap. Separately, the glyph itself renders as an invisible tofu box (`String.fromCharCode(128273)` confirmed unfixed at main.rs:1499 — F-34.4.2-08). |
| `GAMELIB_AUTOFILL_GLYPH` kill switch | Escape hatch if injection ever trips bot management | ✓ VERIFIED | Confirmed present in current source at `main.rs:3919` (`std::env::var("GAMELIB_AUTOFILL_GLYPH").as_deref() == Ok("0")`); live-proven wired (differing log lines across the two arms) and non-lock-out (cancel strip survives the kill switch). |
| `34.4.2-LIVE-GATE-RERUN-2.md` | Six-item blocking gate with measured verdict | ✓ VERIFIED as a document (correctly records FAIL 5/6) | `verdict: FAIL`, `items_passed: 5`, `items_total: 6`. Item-by-item Observed:/Verdict: fields are internally consistent with the ITEM VERDICT SUMMARY table and with the frontmatter. |
| `.planning/REQUIREMENTS.md` REQ-34.4.2-01..10 | Checkbox state matching evidence | ✓ VERIFIED accurate | REQ-01/02/03/06/07/08/10 correctly `[x]`; REQ-04/05/09 correctly `[ ]`, each carrying a dated note naming the exact item and finding that keeps it unchecked. No overclaiming found — cross-checked against the gate document's own item verdicts, not merely trusted. |
| `34.4.2-PLATFORM-SCOPE.md` §5 threat roll-up | Threat-to-item discharge table reflecting this run | ✓ VERIFIED accurate | Section present in `34.4.2-LIVE-GATE-RERUN-2.md`'s own "Threat-to-item discharge table"; cross-references T-34.4.2-05/-07/-33/-34/-35/-17/-21/-22/-15/-32/-36/-37/-38 correctly against which item discharged (or left OPEN) each. |
| `deferred-items.md` | Non-blocking findings ledger | ✓ VERIFIED present and current through plan 11 | WR-02/WR-05/WR-06/WR-08/IN-01/IN-03/IN-04 all logged with reasoning. **Gap noted:** F-34.4.2-06 through -10, T-34.4.2-39, and the four-instance contract-authoring-defect pattern (all surfaced by plan 12's live run) do not yet have their own `deferred-items.md` entries — they currently live only inside the gate document and the SUMMARY. Not a blocker (they are fully documented and cross-referenced elsewhere), but worth folding into `deferred-items.md`'s ledger in the next planning session for consistency with this phase's own established discipline. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `humble_login_open` visible-build arm | `present_login_window_as_sheet` | direct call after hidden build + 250ms deferral | ✓ WIRED and effective | Live-proven: `attached=true` read-back confirmed for every window this run. |
| `WindowEvent::Destroyed` hook | `dismiss_login_window_sheet` | close-detection arm | ✓ WIRED and effective | Live-proven via item 2's dismissal routes; WR-01 re-registration arms present in source (main.rs, both dispatch-Err and recv_timeout-Err paths) though this run's transcript did not need to exercise the re-registration recovery path itself (no failed hop observed). |
| Cancel strip / Esc monitor | `request_login_sheet_cancel` | shared dismissal entry point | ✓ WIRED and effective | Live-proven, item 2 both routes PASS. |
| `.on_navigation(` sentinel closure | `parse_autofill_request` → `post_autofill_right_click` | cancelled-navigation exfil parse → poster | ⚠️ WIRED, correctly targeted, but the end effect (fill) is absent | Live-proven the chain runs correctly end-to-end through menu-popping; the platform/AppKit behavior beyond this project's own code does not carry a synthesized event through to a fill. This is a Level-4 "data flows but produces the wrong terminal effect" case, not a wiring defect. |
| `GAMELIB_AUTOFILL_GLYPH` env read | glyph injection skip | conditional at the injection call site | ✓ WIRED and effective | Live-proven: differing log lines between arms, confirmed genuinely wired not a no-op. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `read-back attached=` log line | sheet attachment boolean | `child.isSheet()` + pointer-compared `parent.attachedSheet()`, read back inside the main-thread closure AFTER `beginSheet_completionHandler` returns | YES — a falsifiable, post-hoc read-back, not the unfalsifiable pre-CR-02 `sheet_presented=true` | ✓ FLOWING — live-proven `attached=true` on 7/7 windows this run |
| `post_autofill_right_click` success line | `hit_tag`/`hit_type`/coordinates | `document.elementFromPoint` hit metadata + WKWebView `bounds()` → AppKit view coordinates | YES for targeting (correct element identified); NO for the terminal user-visible effect (field fill) | ⚠️ HOLLOW at the terminal step — the mechanism produces correct intermediate data (correct element, correct menu) but the final effect the requirement cares about (a filled field) does not occur |
| `GAMELIB_AUTOFILL_GLYPH` arm-differentiated log lines | kill-switch state | `std::env::var` read once per process start | YES — confirmed genuinely different strings across the two arms, not a static/hardcoded pair | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Glyph tofu bug still present in source | `grep -n "fromCharCode(128273)" src-tauri/src/main.rs` | Line 1499: `glyph.textContent = String.fromCharCode(128273);` — no `fromCodePoint` variant found anywhere in the file | ✓ CONFIRMS F-34.4.2-08 unfixed, consistent with the gate's own "deliberately deferred" note |
| Kill switch still wired in source | `grep -n "GAMELIB_AUTOFILL_GLYPH" src-tauri/src/main.rs` | Line 3919: `if std::env::var("GAMELIB_AUTOFILL_GLYPH").as_deref() == Ok("0")` present, plus a doc comment at line 1573 distinguishing the strip's own lack of a kill switch | ✓ CONFIRMS item 5's live result is backed by real, current source |
| WR-07 skip-branch log line still present verbatim | `grep -n "already key, WR-07" src-tauri/src/main.rs` | Line 1974: `"[shell] post_autofill_right_click: '{label_owned}' target is a presented sheet -- skipping makeKeyAndOrderFront (already key, WR-07)"` | ✓ CONFIRMS item 3's transcript evidence (`... target is a presented sheet -- skipping makeKeyAndOrderFront (already key, WR-07)`) traces to real, current source, not a fabricated log line |
| Full Jest + Cargo suites, as a bookkeeping cross-check (NOT a live-behavior proof) | plan 12's own preflight figures | 191/191 suites, 3740/3740 tests; cargo 135/135 (1 ignored) | ✓ PASS on bookkeeping, reiterating this project's own recorded lesson that a green suite proves nothing about live AppKit/WebKit behavior — item 3's FAIL occurred against this exact green baseline |

Step 7c Probe Execution: not applicable — this phase's verification instrument is the
hand-authored `34.4.2-LIVE-GATE-RERUN-2.md` human checkpoint contract, not a
`scripts/*/tests/probe-*.sh` script. No conventional probe files exist under `scripts/` for this
phase. The gate's own measured result (FAIL, 5/6, plan 12, 2026-08-05) is used directly above
rather than re-run, since it requires interactive macOS UI driving (right-click synthesis,
Keychain/Passwords panel interaction, minimize/restore/drag) that cannot be scripted or
re-executed by this verifier.

### Requirements Coverage

| Requirement | Source Plan(s) | Description (abbreviated) | Status | Evidence |
|---|---|---|---|---|
| REQ-34.4.2-01 | 01, 07, 11, 12 | Sheet presentation mechanism | ✓ SATISFIED | Item 1 live PASS, all sub-checks. Box `[x]` in REQUIREMENTS.md, matches. |
| REQ-34.4.2-02 | 01, 07, 11, 12 | Sheet survives minimize/restore, stays interactive | ✓ SATISFIED | Item 1 sub-checks (c)/(d)/(e) live PASS, including the first-ever measurement of the post-restore interactivity claim. Box `[x]`, matches. |
| REQ-34.4.2-03 | 01, 07, 08, 11, 12 | Mandated close affordance | ✓ SATISFIED | Item 2 live PASS, both routes, exactly-one-control sub-check holds. Box `[x]`, matches. |
| REQ-34.4.2-04 | 03, 04, 08, 09, 11, 12 | In-field glyph + kill switch | ✗ BLOCKED | Kill switch (item 5) PASSED; glyph fill (item 3) FAILED. This requirement needs BOTH, per REQUIREMENTS.md's own text. Box `[ ]`, matches. |
| REQ-34.4.2-05 | 04, 09, 11, 12 | Synthesized right-click, public API only, produces a working autofill request | ✗ BLOCKED | Item 3 FAILED — the synthesized event is correctly constructed and correctly targeted per this requirement's own text, but does not deliver a working autofill request (no fill). Box `[ ]`, matches. |
| REQ-34.4.2-06 | 03, 08, 11, 12 | Cmd+V paste preserved | ✓ SATISFIED | Item 4 live PASS on Humble; DummyStore half excluded from the bar by contract amendment (BLOCKED-BY-DESIGN, https-only shell gate). Box `[x]`, matches. |
| REQ-34.4.2-07 | 05 | No credential store; DummyStore containment | ✓ SATISFIED | `dummyStoreHarnessContainment.test.ts` passing; unaffected by this run's findings. Box `[x]`, matches. |
| REQ-34.4.2-08 | 01-05, 07, 08 | Platform gating; Electron/Windows/Linux unchanged | ✓ SATISFIED (documented wart) | Per-symbol cfg table current; glyph call-site wart pre-existing and non-blocking. Box `[x]`, matches. |
| REQ-34.4.2-09 | 05, 06, 09, 10, 11, 12 | Blocking live gate records a measured PASS result | ✗ BLOCKED | Gate reached full completion (a genuine advance) but items_passed 5/6, verdict FAIL. Box `[ ]`, matches. |
| REQ-34.4.2-10 | 01, 07, 08, 12 | Epic pristine surface byte-untouched (locked descope) | ✓ SATISFIED | Source guard unchanged + item 6(b) live re-confirmation, all four absences observed on hardware. Box `[x]`, matches. |

**No orphaned requirements found** — all 10 REQ-34.4.2 IDs are declared across the twelve plans'
frontmatter `requirements:` fields and all 10 have rows in `.planning/REQUIREMENTS.md`, each with
a checkbox state that was independently cross-checked against the gate document's item verdicts
above (not merely trusted from the requirements file's own prose).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src-tauri/src/main.rs` | 1499 | `String.fromCharCode(128273)` truncates a supplementary-plane codepoint to the BMP, rendering an unassigned PUA codepoint (tofu) instead of the key glyph (F-34.4.2-08) | ⚠️ WARNING | Confirmed present in current source, unfixed, deliberately deferred by plan 12 to avoid contaminating the gate run with a mid-run rebuild. Compounds REQ-34.4.2-04/-05's discoverability failure even setting aside the fill defect. One-line fix identified (`String.fromCodePoint(128273)`), not yet applied. |
| `.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md` | §4, Recommendation #4 | Stale project knowledge artifact recommending the exact mechanism this gate falsified (F-34.4.2-09), alongside an already-stale Recommendation #1 (child-window attachment, superseded by the sheet design) | ⚠️ WARNING | Not a code defect, but will actively mislead any future session that consults this skill reference without knowing both recommendations are now falsified/superseded. Recorded as owed work by the gate document itself, not yet actioned. |
| (none) | — | No `TBD`/`FIXME`/`XXX` debt markers found in files touched by plans 11/12 | — | Debt-marker gate: clean. |
| `src-tauri/src/main.rs` (WR-02/WR-05/WR-06/IN-01/IN-03, `deferred-items.md`) | various | Five findings the phase's own review identified and explicitly deferred (raw NSWindow address race, synchronous close inside a WebKit delegate callback, debounce eviction fairness, cap-eviction dismissal no-op, URL-scheme-blind cancel sentinel) | ℹ️ INFO | Each has a documented, reasoned "why out of scope" in `deferred-items.md`; none sits on the routes this phase's own six gate items travel. Not re-litigated here — carried forward as-is per this project's own convention. |

### Human Verification Required

None beyond what the phase's own blocking gate already attempted. The gate ran to full completion
this cycle (a first), driven by a human operator on real hardware, and every item received a
genuine, evidenced verdict — including the decisive real-vs-synthesized-right-click discriminator
for item 3, which is itself the strongest kind of human-in-the-loop evidence this methodology can
produce. The next step is a design/code decision on the glyph mechanism (see Gaps Summary), not a
further observation task.

### Gaps Summary

**Phase 34.4.2 still does not achieve its goal, but the shape of the remaining gap has changed
substantially and for the better since the last verification.** The stale prior verification
(2026-08-04) diagnosed CR-01/CR-02 (the sheet mechanism never worked at all) as the root cause
blocking 5 of 6 items from even being attempted. That diagnosis is now fully retired: a
three-round debug arc closed CR-01/CR-02 outside any plan's own execution, and this cycle's gate
(`34.4.2-LIVE-GATE-RERUN-2.md`, run by plan 34.4.2-12) reached every one of the six items for the
first time in this phase's five-gate history. Items 1, 2, 4, 5, 6 all recorded genuine, well-evidenced
PASS results, each independently cross-checked against current source in this verification (the
WR-07 skip line, the kill-switch conditional, and the glyph tofu bug were all re-confirmed present
in `src-tauri/src/main.rs` exactly as the gate document describes, not merely trusted).

**The remaining gap is item 3 alone, and it is now a genuine measured failure, not an unreached
precondition.** REQ-34.4.2-04 and REQ-34.4.2-05 — the in-field autofill affordance that gives this
phase half its title — do not deliver their stated purpose: the synthesized right-click reaches the
correct element, pops the correct menu, but never fills the field, while an identical real
right-click in the same sheet does fill. This is a well-discriminated result (F-34.4.2-09), not an
ambiguous one: three otherwise-plausible explanations (sheet context, Humble's page, the platform)
are explicitly ruled out by the operator's own real-click control test. Per D-08 (no-partial-pass),
this single FAIL is sufficient to keep the phase from closing regardless of the five PASSes
alongside it, and REQ-34.4.2-09 (the gate itself must record an all-PASS result) fails as a direct
consequence.

**Process note, not a code gap, surfaced by this run and worth carrying into the next cycle:** four
structural impossibilities were found in the gate's own contract during execution rather than
review (the DummyStore https-only block affecting items 3/4 and precondition 4; item 6(a)'s
concurrency framing against AppKit's own sheet-blocks-parent design; item 2's OAuth-runner-only log
line requirement that Humble cannot emit). None of these caused a false PASS or false FAIL — each
was caught and the item was scored on an equivalent, honestly-labeled alternate route or amendment
— but the pattern itself (four independent instances in one contract) suggests contract review
should be added as its own step before future gates run, not discovered live for a fifth time.

**On the operator's post-gate decision to drop the autofill glyph:** the task briefing for this
verification records that the operator made a binding decision, after this gate's run, to drop the
autofill glyph mechanism entirely (deleting `autofill_glyph_script`, `post_autofill_right_click`,
the autofill sentinel branch, and the `GAMELIB_AUTOFILL_GLYPH` kill switch) and rely on Cmd+V as
the sole credential-entry route. **This verification does not apply that decision retroactively** —
it verifies the phase against what REQ-34.4.2-04/-05 currently specify (a working in-field
autofill affordance), which the code does not deliver. This decision is not yet reflected in any
committed plan, `deferred-items.md` entry, or `REQUIREMENTS.md` note as of this verification. It is
recorded here explicitly so the next gap-cycle plan inherits it as a scoping decision rather than
rediscovering it: if the next plan formally descopes REQ-34.4.2-04/-05 to "kill-switch-controlled
glyph, OR its planned removal, with Cmd+V as the sole credential route," this gap closes by scope
correction rather than by a further attempt to make the synthesized click fill. If the next plan
instead attempts to fix the fill mechanism, this verification's gap stands as the acceptance bar
that attempt must clear.

**This looks intentional to a degree, but is not a case for an override.** The phase's own
artifacts (the gate document, REQUIREMENTS.md, PLATFORM-SCOPE.md) already and correctly refuse to
close and route to `/gsd-plan-phase 34.4.2 --gaps` (gap cycle 3). No override is being suggested —
FAILED is the correct and honest state for REQ-34.4.2-04/-05/-09, not a deviation to be waived,
unless and until the operator's glyph-removal decision is formally ratified into a plan that
rewrites those requirements' text.

---

_Verified: 2026-08-05T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
