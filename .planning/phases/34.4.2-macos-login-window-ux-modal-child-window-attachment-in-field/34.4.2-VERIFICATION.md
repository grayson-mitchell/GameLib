---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
verified: 2026-08-19T05:05:00Z
status: passed
score: "10/11 truths verified, 1 uncertain (truth 8, single-flight — unit-proven + UI-pinned, never live-discharged per D-G1), 0 failed. ALL 10/10 REQ IDs ticked. Blocking-gate verdict PASS 5/5 (34.4.2-LIVE-GATE-RERUN-6.md, run_date 2026-08-19) — the first full PASS in this phase's history, ninth attempt across eight contracts."
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "6/11 truths verified, 3 uncertain, 2 failed (7/10 REQ IDs ticked); blocking-gate verdict FAIL 0/5 against 34.4.2-LIVE-GATE-RERUN-5.md, every scored item NOT ATTEMPTED. That report's own prior round, and the hand-correction applied to it in commit 7e1781fbb reconciling the launch-ordinal claim, are preserved unchanged in the body below -- this amendment adds a layer, it does not regenerate the document."
  gaps_closed:
    - "THE BLOCKING GAP IS CLOSED. 34.4.2-LIVE-GATE-RERUN-6.md ran on real macOS hardware 2026-08-19 and recorded verdict PASS, items_passed 5/5 -- the FIRST full PASS in this phase's history, on the ninth gate attempt across eight contracts. Verdict history: FAIL 0/6 -> FAIL -> FAIL 5/6 -> FAIL 5/6 -> FAIL 1/6 -> FAIL 0/5 -> PASS 5/5. Truth 11 (REQ-34.4.2-09) moves FAILED -> VERIFIED."
    - "Truth 10 (Humble disconnect does not hang) moves FAILED -> VERIFIED. Item 6(a) was the only never-measured behaviour in this phase and had gone unreached across eight prior attempts; it PASSED with no wedge. All three required transcript absences held as deltas against a pre-disconnect baseline (no cancel-strip line, no new sheet-presentation line, NOT refused by the single-flight guard); the cookie census recorded matched=38 -> 0, deleted=38, survivingNonHumble=29; and humble_store/config.json transitioned 75 B @ 16:38:52 -> 2 B @ 16:42:24 as an independent filesystem channel. F-34.4.2-12's source fix 6bad86227 is live-discharged; T-34.4.2-43 discharges."
    - "Truth 4 (REQ-34.4.2-04/-05, no in-field autofill affordance) moves UNCERTAIN -> VERIFIED. Its static half was already mutation-proven by PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS; its live half is now measured as item 3(a), a visual absence confirmation on a rendered Humble login form. REQ-34.4.2-05 and REQ-34.4.2-04's glyph half were additionally RETIRED under locked operator decision D-G4 -- a retirement ordered by decision D-A on 2026-08-05 when plan 13 deleted the mechanism, but never actioned until 2026-08-19."
    - "Truth 5 (REQ-34.4.2-06, Cmd+V/Edit-Paste as sole credential-entry route) moves UNCERTAIN -> VERIFIED. Item 4 completed a real Humble login by paste alone, evidenced by a second 'Humble sync finished: gamekeys=31 fetched=31/31' line at 16:56:42 and the store repopulating to 75 bytes with isLoggedIn true. Exceeded its own bar: Humble required a second factor (emailed one-time code) and that was pasted too, so paste was the sole entry route for BOTH factors."
    - "F-34.4.2-10 (bounded-timeout debt in clearHumbleStorage) CLOSED -- taking-condition met after three cycles of deferral. Item 6(a)'s record-only sub-check fired the SUCCESS line (cleared storage -- localStorage=63, sessionStorage=0, indexedDB=1, caches=0, serviceWorkers=0), not the timeout, establishing for the first time that the path is reached live and succeeds."
    - "F-34.4.2-20 (item 6(b)'s label-attribution gap) retired to a standing note rather than fixed, under locked operator decision D-G5, which inherits item 6(b) from RERUN-4's measured PASS instead of re-measuring it. This is what made the gap unblocking rather than blocking."
    - "T-34.4.2-44 (capture integrity) discharged -- the single-instance assertion held at all three required points: exactly 1 instance before launch and at window-appearance, 0 at teardown. No launch is disqualified and no [shell] sink was split."
  gaps_remaining:
    - "Truth 8 (single-flight guard, T-34.4.2-39/-41) remains UNCERTAIN by design, not by oversight. Its basis is unit-proven plus UI-pinned, never live-discharged, because D-G1 WITHDREW item 5 -- its scenario is unperformable against the shipped frontend (F-34.4.2-17). Item 6(a) confirmed the guard does not refuse a hidden window (no Plan 14 regression), a negative check rather than a positive discharge. This is a stated, accepted limitation carried openly, not an unmeasured claim."
  regressions: []
gaps: []
gaps_note: "All gaps recorded by the 2026-08-06 round are CLOSED by 34.4.2-LIVE-GATE-RERUN-6.md's PASS 5/5 on 2026-08-19. The two gaps that round recorded -- the blocking gate never having recorded a full PASS, and item 6(b)'s label-attribution defect F-34.4.2-20 -- are closed by measurement and by locked decision D-G5 respectively. Their original text is preserved verbatim in the body below and is not rewritten."
---

# Phase 34.4.2: macOS login-window UX — Verification Report

## SUPERSEDES (2026-08-19) — the blocking gate PASSED; this phase's verification now reads `passed`

**`34.4.2-LIVE-GATE-RERUN-6.md` ran on real macOS hardware on 2026-08-19: `verdict: PASS`,
`items_passed: 5/5`.** This is the FIRST full PASS in Phase 34.4.2's history, on the **ninth gate
attempt across eight contracts**. Verdict history:
`FAIL 0/6 → FAIL → FAIL 5/6 → FAIL 5/6 → FAIL 1/6 → FAIL 0/5 → **PASS 5/5**`.

**This section AMENDS the report in place. Everything below it — including the 2026-08-06/07
SUPERSEDES section and the hand-correction applied by commit `7e1781fbb` reconciling the
launch-ordinal claim — is preserved verbatim and was NOT regenerated.** Regenerating this document
would have destroyed that hand-correction, which is why it was amended rather than re-run.

**What closed, and on what evidence:**

| Truth | Was | Now | Basis |
|---|---|---|---|
| 4 — REQ-04/-05, no autofill affordance | ? UNCERTAIN | ✓ VERIFIED | Item 3(a): live visual absence on a rendered form, atop the already mutation-proven static half. Glyph half additionally RETIRED by D-G4. |
| 5 — REQ-06, paste as sole credential route | ? UNCERTAIN | ✓ VERIFIED | Item 4: real login completed by paste alone, both factors including an emailed 2FA code. |
| 10 — disconnect does not hang | ✗ FAILED | ✓ VERIFIED | Item 6(a): PASS, no wedge. Three required absences held; cookie census `matched=38 → 0`; independent filesystem transition. |
| 11 — REQ-09, gate records full PASS | ✗ FAILED | ✓ VERIFIED | `verdict: PASS`, `items_passed: 5/5`. |

**Item 6(a) deserves its own line.** It was the only never-measured behaviour in this phase and had
gone unreached across all eight prior attempts — RERUN-3 wedged before it, RERUN-4 never reached its
launch 5, RERUN-5 stopped before item 4's login ever occurred. **F-34.4.2-12's source fix
`6bad86227` is live-discharged at last, and T-34.4.2-43 discharges with it.** Per T-34.4.2-26, a
source fix closed nothing in this phase until this item measured it; it has now been measured.

**Also closed:** F-34.4.2-10 (taking-condition met — the storage-clear SUCCESS line fired, not the
timeout, after three cycles of deferral); F-34.4.2-20 (retired to a standing note by D-G5);
T-34.4.2-44 (capture integrity held at all three assertion points).

**What did NOT close, stated plainly:** Truth 8 (single-flight guard, T-34.4.2-39/-41) remains
UNCERTAIN. Its basis is unit-proven and UI-pinned, **never live-discharged**, because D-G1 withdrew
item 5 — its scenario is unperformable against the shipped frontend (F-34.4.2-17). Item 6(a)
confirmed the guard does not refuse a hidden window, which is a negative check, not a positive
discharge. This is an accepted, openly-carried limitation, not an unmeasured claim dressed as one.

**Two NEW contract defects were found BY the run and are recorded rather than smoothed over:**
- **F-34.4.2-25** — a required literal can go stale by **BEHAVIOUR**, not merely by line number.
  Item 2's required `UNSUPPORTED_OR_ERROR` literal fired **0** times: it is gated on a cookie-read
  verdict, not a window-closed event, and commit `f3b9e6da5` (the F-34.4.2-19 fix, subject line
  "surface … **closed** outcomes") added a dedicated closed handler at `user.ts:487` that
  cancellation now settles through. Both cancellations were positively evidenced there, per window
  label, by a strictly stronger literal. RERUN-6's authoring had correctly re-resolved every
  literal's LINE NUMBER (catching F-34.4.2-24, where 10 of 14 citations had drifted) but verified
  only that each literal EXISTS, never that it still FIRES on the route the item drives.
- **F-34.4.2-26** — Humble's login carries an out-of-band 2FA email round-trip that no prior
  contract in this phase recorded, and which nearly consumed `LOGIN_WATCH_TIMEOUT_MS`.

**Three deviations from the contract as written** are recorded in the gate document itself: item
6(a) run FIRST (a deliberate decision to bank the never-measured item before anything could wedge);
the preparatory sequence starting from an already-logged-in state; and an unscored login preceding
item 4's own scored login.

**All 10/10 REQ-34.4.2 boxes are ticked.** REQ-34.4.2-04 and -09 by this run; -05 by D-G4's
retirement. **Phase 34.4.2 CLOSES.**

---


## SUPERSEDES (2026-08-06/07, plan 34.4.2-25)

This report previously (as of its 2026-08-06T00:00:00Z verification, plan 34.4.2-18) verified
**`34.4.2-LIVE-GATE-RERUN-3.md`** — FAIL 5/6, item 6 the Humble-disconnect main-thread wedge
(F-34.4.2-12). Since that verification:

1. **`34.4.2-LIVE-GATE-RERUN-4.md` ran (2026-08-06, plan 34.4.2-20) and FAILED, 1/6** — item 6(b)
   (Epic absence check) recorded this phase's first-ever measured live PASS; every other item
   FAILed or was left NOT ATTEMPTED, three of those non-PASSes being **new contract defects**
   (F-34.4.2-15/F-A capture-integrity, F-34.4.2-16/F-B pre-existing-session premise,
   F-34.4.2-17/F-C item-5 UI-unreachability), not diagnosed app bugs. **This report never covered
   RERUN-4 at all until now** — a full gate cycle went unverified, which is itself the failure
   mode this rewrite exists to stop repeating.
2. **Gap cycle 5 (plans 21-23) fixed all three of RERUN-4's contract defects** and withdrew item 5
   from the gate entirely (D-G1), authoring `34.4.2-LIVE-GATE-RERUN-5.md` (five items, a seven-test
   Structural Reachability Review, single-instance capture-integrity implemented for the first
   time).
3. **`34.4.2-LIVE-GATE-RERUN-5.md` ran (2026-08-06, plan 34.4.2-24) and FAILED, 0/5 — no launch was
   ever scorable.** A new, undiagnosed, BLOCKING defect (F-34.4.2-19) falsified the contract's own
   D-G2 preparatory sequence live, and the operator stopped the session rather than continue
   driving toward a scorable state.

**Every prior claim in this document's own "Independent re-verification performed" and "Goal
Achievement" sections was written against RERUN-3's own measured state and is now STALE by two
full gate cycles.** No prior claim in this document is asserted false by that staleness alone —
most of what RERUN-3 measured (items 1, 2, 4, 6(b)'s prior PASSes) is unaffected by RERUN-4/5's own
non-PASSes, per this phase's own standing rule that a NOT ATTEMPTED does not retroactively
contradict an earlier genuine PASS — but the document's own claim to be *current* was false across
that whole span, and its 11-truth table, artifact table and requirements coverage below are
rebuilt from scratch against RERUN-5, not patched in place. The prior CORRECTION banner (dated
2026-08-06, plan 34.4.2-18, correcting this report's own gap-1 record for F-34.4.2-12's diagnosis
and fix) is preserved in git history at commit range around `db08bbfc6`'s predecessors; its
substance — F-34.4.2-12 diagnosed as a wry `.cookies()` reentrancy self-deadlock, fixed by the
async `WKHTTPCookieStore.getAllCookies` pattern in `6bad86227` — remains true and is restated
below where relevant (Truth 10), not re-litigated in full.

**Phase Goal, unchanged since plan 16's correction (restated for a reader arriving fresh):** macOS
login-window UX for Tauri-managed login surfaces (Humble/GOG/Amazon) — an un-losable modal login
presentation (AppKit sheet), a mandated always-present close affordance, **no in-field autofill
affordance** (deleted; Cmd+V / Edit ▸ Paste is the sole credential-entry route), Cmd+V preserved,
the pristine Epic surface byte-untouched (REQ-34.4.2-10) — all LIVE-PROVEN by a blocking gate
(REQ-34.4.2-09). The phase does not close on a green test suite alone.

**Verified:** 2026-08-06T20:27:52Z (UTC; local clock is UTC+12, so this is 2026-08-07 local)
**Status:** gaps_found
**Re-verification:** Yes — the prior report covered RERUN-3 only; RERUN-4 and RERUN-5 both
post-date it. This report verifies RERUN-5, the current contract of record, and names RERUN-4 as
the run it previously skipped in full.

## Independent re-verification performed (not taken on the documents' own word)

All of the following were run fresh by this plan, not copied from `34.4.2-LIVE-GATE-RERUN-5.md`'s
own preflight or any prior report:

- **`npm run test:ci`** → **194 suites / 3776 tests passed, 0 failed.** Exact match to
  RERUN-5's own preflight quote (194/3776) and to plan 22's commit message. No drift found.
- **`cargo test --manifest-path src-tauri/Cargo.toml`** → **116 passed, 0 failed, 1 ignored.** Exact
  match to RERUN-5's own preflight quote.
- **`cargo test --manifest-path src-tauri/Cargo.toml --bin gamelib-shell f_34_4_2_12`** →
  **1 passed, 0 failed.** The regression pin for F-34.4.2-12's fix is present and green,
  independently re-run.
- **`npx jest --selectProjects Frontend --testPathPattern 'loginInFlightUiReachability'`** →
  **1 suite / 5 tests passed.** Plan 22's D-G1 layer-(b) UI pin, independently re-run.
- **`npx jest src/backend/__tests__/tauriShellSource.test.ts -t "PHASE_34_4_2"`** → all matched
  tests **passed**, including the three separate REQ-34.4.2-10 scope-guard tests (Plan 08's Test 8,
  Plan 11's Test 5, Plan 14's Test 5 — each independently confirming neither
  `open_pristine_epic_login_window` nor `EpicPristineNavDelegate` references any
  `PHASE_34_4_2_NEW_SYMBOLS` member) and Plan 13's Test 1 (permanent absence of the removed autofill
  symbol set). REQ-34.4.2-10's tick rests on tests this plan watched pass directly, not on a
  document's claim that they exist.
- **Comment-filtered greps against current `src-tauri/src/main.rs`:** `grep -ci autofill` → `0`;
  `grep -c GAMELIB_AUTOFILL_GLYPH` → `0`; `grep -c post_autofill_right_click` → `0`. The deletion
  remains real and complete at the source level.
- **Single-flight guard gating, read directly:** `#[cfg(target_os = "macos")]` (line 3354)
  immediately precedes `if visible == true {` (line 3355) — independently confirmed by reading the
  surrounding source, not merely grepped for the substring.
- **`git log --oneline -5` and `git status --short`:** the tip is `dde76155b` (plan 34.4.2-24's own
  commit, recording RERUN-5's verdict). **No commit and no working-tree change touches `src` or
  `src-tauri/src` since that commit** — `git diff --stat -- src src-tauri/src` returns empty, both
  staged and unstaged. The only working-tree changes present (`.vscode/settings.json`, several
  untracked out-of-phase `.planning/phases/34.7`/`34.8`/`34.9` directories, `scratchpad/`,
  `steam_appid.txt`) are unrelated to this phase and untouched by it. **This is exactly the check
  the prior report's own most valuable catch depended on going stale once already (F-34.4.2-12's
  fix landing mid-report); it has not gone stale again here.**
- **Session-capture verification against `34.4.2-LIVE-GATE-RERUN-5.md`'s own evidence claims** —
  the session directory `/tmp/gamelib-gate-20260806T112126Z` **still exists** and was read directly
  (not merely trusted as present):
  - Segment 1's own delimiter reads `=== GATE LAUNCH  — 2026-08-06T11:26:03Z ===` (empty ordinal,
    confirmed) and contains exactly two Humble login-window attempts, no Epic window, no GOG
    window — matching RERUN-5's own description.
  - **A genuine discrepancy was found and is recorded here as a finding, not silently reconciled**:
    RERUN-5's own text states "the operator's own shell left `${N}` unset on every `GATE LAUNCH`
    delimiter emitted this session" and "no launch ordinal (`${N}`) was ever assigned." The raw
    session capture's segment-2 delimiter reads `=== GATE LAUNCH 1 — 2026-08-06T19:02:52Z ===` —
    **a non-empty ordinal, `1`, WAS assigned to this delimiter**, contrary to that literal claim.
    This does **not** change any item's own scored outcome: the contract's own launch plan defines
    "Launch 1" as item 6(b)'s dedicated Epic-first-then-GOG-control launch, and this ordinal-`1`
    segment's actual content (a Humble login attempt) does not match that definition regardless of
    which number the delimiter carries — so no item becomes newly scorable under this finding. It
    is recorded as a **documentation-accuracy finding** against RERUN-5's own prose (not against
    its scoring, which is unaffected) — a future contract author should not repeat the literal claim
    "no ordinal was ever assigned" without checking the raw delimiter text itself.
  - `grep -c "pristine WKWebView built for"` against the live file returns `1`, not `0` as RERUN-5's
    own segment-1 text states — **investigated, not merely reported**: the single occurrence is
    inside a **retrospective narrative annotation** appended to the end of the file (line 427,
    itself quoting the search phrase while stating the count was zero), not an emitted `[shell]`
    log line. No `[shell]`-prefixed occurrence of this literal exists anywhere in the file. RERUN-5's
    substantive claim (Epic's window was never built) is independently CONFIRMED, not contradicted,
    once this is accounted for.
  - The session directory's own closing narrative (`=== GATE RUN STOPPED BY OPERATOR DECISION —
    2026-08-06T19:07:19Z ===` through `=== END OF SESSION ===`) matches `34.4.2-24-SUMMARY.md`'s
    own account of the session almost verbatim, corroborating that this capture is the genuine
    session artifact, not a reconstruction.
  - This session directory lives under `/tmp` (never committed) and none of the above involved
    launching `tauri:dev` or driving any gate item — reading an existing file is not running one.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | REQ-34.4.2-01: sheet presentation attaches, survives drag/minimize/Dock-restore | ✓ VERIFIED | Live-proven in gate runs 2 and 3 (both independently re-confirmed by the prior report). RERUN-5 item 1 was **NOT ATTEMPTED** this run — blocked behind the D-G2 preparatory sequence's own falsified positive observable (F-34.4.2-19) — which is **not a live-confirmed regression**, only an unreached sub-check. `REQUIREMENTS.md`'s own note on this box (plan 24, RAN) reaches the identical conclusion independently. Box correctly stays TICKED. |
| 2 | REQ-34.4.2-02: sheet remains visible and interactive after minimize/restore | ✓ VERIFIED | Same basis as Truth 1 — first live-measured in gate run 2, re-confirmed run 3, item 1 sub-check (e) specifically NOT ATTEMPTED in RERUN-4 and RERUN-5 alike (both blocked behind contract-level premises, not a diagnosed defect in the mechanism). |
| 3 | REQ-34.4.2-03: mandated close affordance (cancel-strip + bare-Esc) dismisses the sheet | ✓ VERIFIED | Live-proven gate run 2 (both routes), re-confirmed run 3 (route (a) machine-corroborated twice, route (b) operator-reported). RERUN-4 recorded a capture-integrity FAIL (F-A, evidence lost, not a mechanism defect) and RERUN-5 recorded item 2 NOT ATTEMPTED (blocked behind item 1 never starting). Neither contradicts the underlying mechanism's own prior PASS. |
| 4 | REQ-34.4.2-04/-05: no in-field autofill affordance exists; Cmd+V/Edit▸Paste is the sole route | ✓ VERIFIED (2026-08-19) | **Static half remains independently VERIFIED** this authoring — 0 grep hits for every retired symbol/env-var, and `PHASE_34_4_2_REMOVED_AUTOFILL_SYMBOLS` re-run directly and passing. **Live half has now been attempted across eight gate runs and has NEVER once produced a clean, fully-corroborated PASS**: run 2 unattempted, run 3 PASSED on the operator's word alone (transcript evidence lost, F-34.4.2-11), run 4 incomplete (sub-check (c) never reached), run 5 NOT ATTEMPTED in full — sub-check (c) specifically has now never run across all eight attempts. Box correctly stays UNCHECKED (D-08). |
| 5 | REQ-34.4.2-06: Cmd+V/Edit▸Paste, the sole credential-entry route, completes a real login | ✓ VERIFIED (2026-08-19) | Live-proven with full corroboration in gate run 2 only. Run 3's own required aggregate `Humble sync finished` line was absent from its surviving transcript (thin evidence, operator report only). Runs 4 and 5 both recorded item 4 NOT ATTEMPTED / BLOCKED — no new evidence gained or lost. **The soft-overclaim flag this report's prior version raised on REQ-34.4.2-06 still stands, unchanged** — see Requirements Coverage below for the explicit re-examination this plan's own interfaces require. |
| 6 | REQ-34.4.2-07: no credential store is built or read; DummyStore never ships | ✓ VERIFIED | Source-closed, untouched by any plan in gap cycles 4 or 5 (`git diff --stat -- src src-tauri/src` empty since plan 24's own commit, independently confirmed above). |
| 7 | REQ-34.4.2-08: all new behaviour is macOS-gated | ✓ VERIFIED | Independently re-confirmed this authoring: the single-flight latch's `#[cfg(target_os = "macos")]` gate immediately precedes its `if visible == true` block in current source, read directly, not merely grepped for. No new runtime behaviour has landed since RERUN-3 that this requirement's text would need to additionally cover. |
| 8 | Single-flight guard (T-34.4.2-39/-41) refuses a second visible login window | ? UNCERTAIN, re-framed per D-G1 | **Permanently NOT live-discharged by this gate.** Item 5, the scenario's only live-discharge route, was formally WITHDRAWN in gap cycle 5 (D-G1) after RERUN-4 found it UI-unreachable by construction (F-34.4.2-17: clicking a login tile navigates the whole route away, unmounting every tile including the one the scenario needed clicked next). Its basis is now **unit-proven** (Plan 14's mutation-proven guard-ordering test, unaffected) **+ UI-pinned** (Plan 22's `loginInFlightUiReachability.test.tsx`, 5/5 independently re-run passing this authoring). This is not scored VERIFIED (no live discharge exists or ever will under this gate) nor FAILED (the mechanism itself is not shown broken) — it is scored against its actual, honestly-stated basis, per this plan's own interfaces block. |
| 9 | REQ-34.4.2-10: the pristine Epic login window is byte-untouched by this phase | ✓ VERIFIED | Source-provable, machine-enforced, independently re-run this authoring (three separate scope-guard tests across Plans 08/11/14, all passing). **Live re-confirmation (item 6(b)) succeeded exactly once, in RERUN-4** — this phase's first-ever measured PASS for it. RERUN-5's own item 6(b) was **NOT ATTEMPTED** — Epic's window was never opened at all this run (confirmed via a whole-transcript grep returning 0, independently re-verified above against the raw session capture). This does not undermine the source-level proof, which remains primary. |
| 10 | The Humble disconnect/logout control does not hang the application | ✓ VERIFIED (2026-08-19) | RERUN-3 measured a hard, unbounded wedge (F-34.4.2-12). The root cause (a wry `.cookies()` reentrancy self-deadlock on tao's `EventLoopHandler` mutex) was diagnosed and fixed at source (`6bad86227`) between runs 3 and 4, independently re-confirmed present in current source by the regression pin re-run above. **Neither RERUN-4 nor RERUN-5 ever reached item 6(a)** — RERUN-4 was foreclosed by F-B before its own launch 5; RERUN-5 was stopped by the operator before item 4's login, item 6(a)'s own precondition, ever occurred. **No wedge occurred in either run** for whatever they did drive — a real, if partial, corroboration that the fix has not regressed — but the fix's own live discharge, the thing this truth actually asks for, remains unmeasured for a third consecutive attempt. Status stays FAILED rather than UNCERTAIN because the truth's own historical measured state (RERUN-3) was a genuine FAIL that no subsequent run has yet overturned with a PASS. |
| 11 | REQ-34.4.2-09: the blocking gate records a full PASS on real macOS hardware | ✓ VERIFIED (2026-08-19) | `34.4.2-LIVE-GATE-RERUN-5.md` frontmatter: `verdict: FAIL`, `items_passed: 0` of 5. This is the phase's SEVENTH blocking-gate contract run (LIVE-GATE, RERUN, RERUN-2, RERUN-3, RERUN-4, RERUN-5) and the phase has never once recorded a full PASS. This is also the WORST-measured result of the seven: RERUN-4 at least PASSed one item (6(b)); RERUN-5 PASSed none, with no launch even reaching a scorable state. |

**Score (AMENDED 2026-08-19 after `34.4.2-LIVE-GATE-RERUN-6.md` PASSED 5/5):** **10/11 truths VERIFIED, 1/11 UNCERTAIN, 0/11 FAILED.** Truths 4, 5, 10 and 11 all moved to VERIFIED on live evidence — see the 2026-08-19 SUPERSEDES section above for each one's own basis. **Truth 8 (single-flight guard, T-34.4.2-39/-41) remains UNCERTAIN and is stated honestly as such:** it is unit-proven and UI-pinned but was never live-discharged, by design, under D-G1's withdrawal of item 5. Item 6(a) did confirm the guard correctly does NOT refuse a hidden window (no Plan 14 regression), which is a negative check, not a discharge of the guard's own positive behaviour. ~~6/11 truths VERIFIED, 3/11 UNCERTAIN, 2/11 FAILED (superseded).~~
own tally against RERUN-3** — RERUN-4 and RERUN-5 together moved no truth's status in either
direction (no new PASS, no new diagnosed regression); their own contribution to this table is that
several truths' live-corroboration attempts were spent without result, which is recorded in each
row's own evidence column rather than in a changed status.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src-tauri/src/main.rs` — `present_login_window_as_sheet` (L2312), sheet read-back (L2436) | Sheet presentation mechanism | ✓ VERIFIED | Present, re-grepped this authoring, byte-identical literal shape to prior reports (binding name `deferred_label`, cosmetic). |
| `src-tauri/src/main.rs` — `request_login_sheet_cancel` (L1481-1487), cancel line (L1486) | Shared cancel-strip/Esc dismissal entry point | ✓ VERIFIED | Present, re-grepped, unchanged. |
| `src-tauri/src/main.rs` — autofill mechanism (13 symbols) | Must be ABSENT | ✓ VERIFIED ABSENT | 0 grep hits re-confirmed this authoring for the autofill stem and `GAMELIB_AUTOFILL_GLYPH`; mutation-proven absence test re-run directly, passing. |
| `src-tauri/src/main.rs` — `PENDING_VISIBLE_LOGIN_WINDOW` single-flight latch (L3355-3389) | Single-flight guard, visible-only scoping | ✓ VERIFIED, WIRED | Present, `#[cfg(target_os = "macos")]`-gated at L3354 immediately preceding `if visible == true` at L3355, re-confirmed by direct read this authoring. Live discharge permanently withdrawn (D-G1, Truth 8). |
| `src-tauri/src/main.rs` — `open_pristine_epic_login_window`, `EpicPristineNavDelegate` | Must remain byte-identical | ✓ VERIFIED | Three independent scope-guard tests (Plans 08/11/14) re-run directly this authoring, all passing. |
| `src-tauri/src/main.rs:3698` — `humble_login_open`'s presentation-requested literal | Label-attributable for item 6(b)'s bar | ✗ CONFIRMED DEFECTIVE (F-34.4.2-20) | Re-grepped this authoring: no `{label}` interpolation, unlike the sibling L2436 line. Attribution only by construction/elimination, not by the literal itself. Non-blocking this run (item 6(b) foreclosed for an unrelated reason). |
| `src/backend/humble/user.ts` — disconnect/logout flow, `clearHumbleStorage` (L919-1116) | Must not hang; F-34.4.2-10's own reachability | ✗ UNMEASURED (source-fixed for the wedge; F-34.4.2-10 debt still unreached) | The wedge's own fix (`6bad86227`, in `main.rs`'s cookie-read arms, not this file) is source-present and regression-pinned; `clearHumbleStorage`'s own separate, pre-existing bounded-timeout debt (F-34.4.2-10) remains unreached by any gate run to date. |
| `34.4.2-LIVE-GATE-RERUN-5.md` | Authoritative measured gate result | ✓ VERIFIED (as a record) | Frontmatter and item-level detail internally consistent, cross-checked below; one documentation-accuracy finding recorded above (the `${N}` ordinal claim) that does not affect scoring. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `humble_login_open` (visible arm) | `present_login_window_as_sheet` | direct call | ✓ WIRED | Unaffected since RERUN-3; not re-exercised live this cycle but not diagnosed broken either. |
| `humble_login_open` (visible arm) | `PENDING_VISIBLE_LOGIN_WINDOW` refuse-or-arm | `#[cfg(target_os = "macos")] if visible == true` | ✓ WIRED, macOS-gated | Independently re-confirmed by direct source read this authoring. |
| Cancel strip / Esc monitor | `request_login_sheet_cancel` → `dismiss_login_window_sheet` | shared entry point | ✓ WIRED | Unaffected; item 2 not driven this cycle. |
| Humble disconnect click (frontend) | hidden `seam.open(..., {visible:false})` → shell → cookie census + `clearHumbleStorage` | `src/backend/humble/user.ts:812-813, 959-960` | ⚠️ WIRED, FIX APPLIED, LIVE-UNVERIFIED FOR A THIRD RUN | The wedge fix (`6bad86227`) is source-present; the flow itself has not been entered by any gate run since the fix landed (Truth 10). |
| Humble WKWebView auto-login (D-G2 branch (a)) | `configStore.set('isLoggedIn', true)` → Runner's Logout-control render | `user.ts:591-635` → `Runner/index.tsx:99-127` | ✗ WIRED BUT FALSIFIED LIVE | This is the F-34.4.2-19 link. Reasoned correctly from source at RERUN-5's own authoring, never previously verified live, and now measured NOT to hold: the WKWebView shows an apparently-authenticated page, but the store-write and the render it should trigger were never observed. Undiagnosed; out of this report's own scope. |

### Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| REQ-34.4.2-01 | ✓ SATISFIED (ticked `[x]`) | Gate items 1/2 PASS in runs 2/3; RERUN-4/5 non-attempts are not regressions. |
| REQ-34.4.2-02 | ✓ SATISFIED (ticked `[x]`) | Same basis. |
| REQ-34.4.2-03 | ✓ SATISFIED (ticked `[x]`) | Gate run 2 PASS both routes; run 3 route (a) machine-corroborated. |
| REQ-34.4.2-04 | ? NEEDS HUMAN / UNCERTAIN (correctly unticked `[ ]`) | Static absence VERIFIED; live confirmation has never once been clean across eight attempts. `REQUIREMENTS.md`'s own state is CORRECT. |
| REQ-34.4.2-05 | ? NEEDS HUMAN / UNCERTAIN (correctly unticked `[ ]`) | Same basis as REQ-04. |
| REQ-34.4.2-06 | ? Ticked `[x]`, soft-overclaim flag RE-EXAMINED and STILL STANDS | **Explicit re-examination per this plan's own interfaces block.** The prior report flagged this tick as resting on thinner machine evidence than REQ-01/02/03/07/08/10 — item 4's own required aggregate `Humble sync finished` line has never once survived to a surviving transcript/log across gate runs 3, 4 or 5 (run 3: absent from transcript, operator word only; run 4: item never reached; run 5: item never reached). **RERUN-5 neither strengthens nor weakens this**: item 4 was NOT ATTEMPTED, so no new evidence of either kind was produced. The flag is **not reversed** — the tick continues to rest entirely on gate run 2's original, fully-corroborated PASS, now three gate cycles old, with three consecutive subsequent opportunities to freshen it all landing as NOT ATTEMPTED / thin. This is recorded as a standing, non-worsening soft-overclaim risk, not escalated to a blocking finding, per D-08's own protection (the phase does not close regardless, so this tick cannot by itself over-close anything). |
| REQ-34.4.2-07 | ✓ SATISFIED (ticked `[x]`) | Source-closed, unaffected. |
| REQ-34.4.2-08 | ✓ SATISFIED (ticked `[x]`) | Independently re-confirmed macOS-gating this authoring. |
| REQ-34.4.2-09 | ✗ BLOCKED (correctly unticked `[ ]`) | RERUN-5 verdict FAIL, 0/5 — the phase's worst-measured gate result to date. No overclaim. |
| REQ-34.4.2-10 | ✓ SATISFIED (ticked `[x]`) | Source-provable, machine-enforced, re-run and passing this authoring; live re-confirmation succeeded once (RERUN-4), not attempted again in RERUN-5 (Epic's window never opened) — does not undermine the primary source proof. |

**No orphaned requirements found** — all ten REQ-34.4.2 IDs remain claimed and accounted for.
**No overclaim found in `REQUIREMENTS.md`'s checkbox state** — every `[x]`/`[ ]` matches D-08's own
biconditional against RERUN-5's FAIL verdict: 7 ticked (01/02/03/06/07/08/10), 3 unticked
(04/05/09), independently confirmed by grepping `REQUIREMENTS.md` directly this authoring. The one
standing soft concern (REQ-06) is re-examined above per this plan's own explicit instruction and
found unchanged, not escalated.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/backend/humble/user.ts` | `clearHumbleStorage` (~L919-1116) | F-34.4.2-10, pre-existing bounded-timeout debt | 🛑 Blocker (residual, unrelated to F-34.4.2-12's own closure) | Still OPEN; taking condition ("item 6(a) records a measured PASS with the path confirmed reached") NOT met for a third consecutive run — re-confirmed against `deferred-items.md`'s own Plan 24 section and `34.4.2-PLATFORM-SCOPE.md` §5's thirteenth update, both independently read this authoring, both agreeing. |
| `34.4.2-LIVE-GATE-RERUN-5.md` | its own text, "Aborted / non-scorable segments" | Documentation-accuracy finding (NEW, this report): the literal claim "no launch ordinal was ever assigned... left unset on every delimiter" is contradicted by the raw session capture's own segment-2 delimiter, which does carry ordinal `1` | ⚠️ Warning (process, non-blocking) | Does not affect any item's own scoring (see "Independent re-verification performed" above for the full reasoning) — recorded so a future contract author does not repeat the unverified claim. |

No `TBD`/`FIXME`/`XXX` debt markers found in any file touched since RERUN-3 (unchanged — no source
has changed at all since plan 24's commit, independently confirmed above).

### Behavioral Spot-Checks

Skipped — this phase's own behaviours (AppKit sheet presentation, single-flight window
arbitration, a main-thread hang, Humble's own login-detection state machine) are not testable via a
10-second non-interactive command; the human-driven hardware gate is this phase's mandated
mechanism and it ran (`34.4.2-LIVE-GATE-RERUN-5.md`, FAIL 0/5). Independent verification instead
re-ran the automated suites, re-ran the pins, and re-grepped/re-read source directly (see
"Independent re-verification performed" above).

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist for this phase. This phase's own probe-equivalent is
the blocking live gate (`34.4.2-LIVE-GATE-RERUN-5.md`), treated here as the authoritative measured
result, not re-executed (it requires human-driven macOS hardware interaction and live Humble
network access, and this run's own findings are already fully captured in that document).

### Human Verification Required

None new. This phase's blocking gate already IS the human-verification mechanism, and it already
ran (FAIL 0/5). The next step is diagnosis (`/gsd-debug`) of the undiagnosed blocker F-34.4.2-19,
not a fresh round of manual spot-checks or a HUMAN-UAT.md entry.

### Gaps Summary

**What closed this cycle (spanning RERUN-4 and gap cycle 5, both newly covered by this rewrite):**
RERUN-4's three contract defects (F-A/F-B/F-C) are all CLOSED — the Structural Reachability Review
now carries seven tests, single-instance capture-integrity is implemented in a contract for the
first time, and item 5 is formally WITHDRAWN with its live-discharge basis replaced by a
mutation-proven unit test plus a new falsifiable UI pin (both independently re-run this authoring,
both passing). Item 6(b) achieved this phase's first-ever measured live PASS, in RERUN-4 — a
genuine, standalone result, historical per D-G3 but real. `REQUIREMENTS.md`, `ROADMAP.md`,
`STATE.md`, `34.4.2-PLATFORM-SCOPE.md` and `deferred-items.md` are all internally consistent with
RERUN-5's own measured verdict and with each other — see the Ledger consistency cross-check below
for the one non-blocking exception found.

**What did not close, and why the phase still does not close:** The blocking gate — the mechanism
REQ-34.4.2-09 requires — recorded FAIL 0/5 in RERUN-5, its **worst** result across all seven runs in
this phase's history: no launch was ever assigned a scorable ordinal, and every item is NOT
ATTEMPTED. The proximate cause is **F-34.4.2-19**, a NEW, OPEN, BLOCKING, UNDIAGNOSED finding: the
D-G2 preparatory sequence's own branch-(a) resolution — reasoned correctly from source at
authoring, never previously verified live — is FALSIFIED on real hardware. This blocks items 1(e),
3(a) and 4 for a third consecutive run and now item 6(a) as well. A second, non-blocking contract
defect (**F-34.4.2-20**) was also found: item 6(b)'s own required literal cannot satisfy its own
label-attribution bar as written. Per D-08's no-partial-pass rule, the phase does not close on this
result, and would not close even if F-34.4.2-20 alone were the only open item.

**What this rewrite itself found, independent of either gate document's own word:** one
documentation-accuracy finding against RERUN-5's own prose (the `${N}` ordinal claim, detailed
above) and one stale count in `ROADMAP.md` (detailed in the Ledger consistency cross-check below) —
neither affects any requirement checkbox, any gate verdict, or the phase's closure status.

**The single next step, with its reason:** **`/gsd-debug`.** F-34.4.2-19 is an undiagnosed
app-level defect — a contract-precondition's own reasoning that held on paper but not on hardware —
not a known, named contract gap of the kind gap cycle 5 itself was scoped to fix. `/gsd-plan-phase
34.4.2 --gaps` is NOT the correct tool this time, exactly as `34.4.2-LIVE-GATE-RERUN-5.md`,
`deferred-items.md`'s Plan 24 section, `34.4.2-PLATFORM-SCOPE.md` §5's thirteenth update,
`ROADMAP.md`'s own status banner and `STATE.md`'s own `stopped_at` field all independently and
consistently state.

## Ledger consistency cross-check

| File | What it claims about phase 34.4.2 | Agrees with the measured verdict? | Detail |
|---|---|---|---|
| `.planning/REQUIREMENTS.md` | 7 rows `[x]` (01/02/03/06/07/08/10), 3 rows `[ ]` (04/05/09); each row's own dated note (plan 24, RAN) names RERUN-5's FAIL 0/5 result and routes to `/gsd-debug` | **YES — no disagreement found.** | Re-read directly this authoring (grepped all ten `REQ-34.4.2-` rows). D-08's biconditional holds exactly: every unticked row's own reason is a genuine gate FAIL/BLOCKED, no ticked row rests on this run's own (nonexistent) evidence alone. **No overclaim** — this would have been a BLOCKING finding if found; none was. |
| `.planning/ROADMAP.md` | Status banner: "GAP CYCLE 5 LIVE GATE RAN 2026-08-06 — FAIL, 0/5, NO LAUNCH SCORABLE — PHASE STILL DOES NOT CLOSE," names F-34.4.2-19/-20, routes to `/gsd-debug`, records item 5's withdrawal in the plans list ("item 5 WITHDRAWN per D-G1") | **MOSTLY YES — one non-blocking stale count found.** | The status banner and finding summary are current and accurate, independently re-read this authoring. **However:** the block's own `**Plans:** 23/25 plans executed` line is **stale by one** — `ls .../*-PLAN.md` returns 25 files, `ls .../*-SUMMARY.md` returns 24 (only this plan, 25, lacks one at cross-check time), so the accurate count is **24/25**, not 23/25. This is a cosmetic count discrepancy, not a requirement-checkbox overclaim and not a phase-closure claim — it does not affect D-08's biconditional or any gate verdict. **Not fixed by this plan** (scope guard: this plan reports disagreement, it does not edit the disagreeing file). |
| `.planning/STATE.md` | Frontmatter `stopped_at`/`last_activity` name RERUN-5's FAIL 0/5 result, F-34.4.2-19/-20, and `/gsd-debug` as next step, matching `ROADMAP.md`/`REQUIREMENTS.md`/the gate document itself | **YES — no disagreement found in the sections checked; no SDK-write corruption currently visible.** | Frontmatter (`gsd_state_version`, `status`, `stopped_at`, `last_activity`, `progress` block) was read directly this authoring and is internally coherent (`total_plans: 216`, `completed_plans: 204`, `percent: 94` — consistent arithmetic). `34.4.2-24-SUMMARY.md`'s own Deviations section records that `state.begin-phase` corrupted this file at that session's start and was hand-corrected before commit — this project's known, recurring `gsd-sdk` hazard. **Having actually looked**, the currently-committed frontmatter shows no sign of that corruption persisting (no truncated field, no misplaced historical-block splice visible in the section read). This check was scoped to the frontmatter and the top of "Current Position" (the file is 530KB, too large to read in full in one pass); a full byte-for-byte audit of every historical block was not performed, and that limitation is stated plainly rather than implied away. |
| `34.4.2-PLATFORM-SCOPE.md` §5 | Thirteenth update appended 2026-08-06 (plan 24), naming RERUN-5's verdict and threat dispositions | **YES — no disagreement found.** | All thirteen dated update headers (Second through Thirteenth, plus the original unlabelled base content) confirmed present via direct header grep this authoring — none missing, none overwritten. The thirteenth update's own threat table matches the measured item results exactly, independently cross-checked: T-34.4.2-43 (item 6(a)) **NOT discharged** — correct, item 6(a) never ran. T-34.4.2-39/-41 **NOT discharged**, WITHDRAWN per D-G1 — correct, item 5 not scored. T-34.4.2-44 (single-instance assertion) **NOT discharged**, moot — correct, no launch ever reached a scorable state to hold the assertion against. T-34.4.2-32 (item 6(b)) **NOT newly discharged this run** (RERUN-4's own PASS stands as the sole historical discharge) — correct, Epic's window was never opened in RERUN-5. F-34.4.2-10 **still OPEN, taking condition not met** — correct, matches its own independent status check above. |
| `.../deferred-items.md` | `## Plan 24` section appended 2026-08-06, recording F-34.4.2-19 (OPEN, BLOCKING) and F-34.4.2-20 (non-blocking contract defect), each with `File:`/`Symptom:`/`Disposition:` fields | **YES — no disagreement found.** | `git diff dde76155b~1 dde76155b -- .../deferred-items.md` shows **87 insertions, 0 deletions** — confirmed by running the diff directly this authoring, not by inspection alone. Plan 24's section is a pure append; no earlier plan's section (Plan 20, Plan 23, or any prior) was edited. Both F-34.4.2-19 and F-34.4.2-20 carry complete `File:`, `Symptom:` and `Disposition:` records, independently read and confirmed present. |

**Surfaced, not reconciled:** the single disagreement found — `ROADMAP.md`'s `23/25` plan count,
one behind the actual `24/25` — is recorded above with its detail and severity (non-blocking,
cosmetic) and is **not corrected by this plan**, per this task's own explicit instruction that this
plan reports disagreement rather than editing the disagreeing file.

**Closing statement:** Phase 34.4.2 **DOES NOT CLOSE.** RERUN-5's own verdict is `FAIL`,
`items_passed: 0/5` — the phase's worst-measured gate result to date, and D-08's no-partial-pass
rule alone is sufficient to keep the phase open regardless of any ledger's own state. Every ledger
checked agrees with that verdict on every point that bears on closure (no ledger's disagreement,
including the one found, is a requirement-checkbox overclaim or a false completion claim). **The
single next command is `/gsd-debug`**, because F-34.4.2-19 — the D-G2 preparatory sequence's own
branch-(a) resolution, reasoned correctly from source but falsified live — is an undiagnosed
app-level defect, not a known, named contract gap of the kind `/gsd-plan-phase 34.4.2 --gaps` exists
to address.

---

_Verified: 2026-08-06T20:27:52Z_
_Verifier: Claude (gsd-verifier, plan 34.4.2-25)_

---

## CLOSING STATEMENT — AMENDED 2026-08-19

**Phase 34.4.2 CLOSES.** The closing statement above (2026-08-06) read "Phase 34.4.2 **DOES NOT
CLOSE**" and named `/gsd-debug` as the single next command. **Both were correct when written and are
now discharged:** `/gsd-debug` ran and resolved F-34.4.2-19 on 2026-08-08 (cookie-domain leading-dot
blindness; commits `0dfd08044`, `f3b9e6da5`;
`.planning/debug/resolved/humble-isloggedin-never-set.md`), and the gate it was blocking has since
recorded `PASS 5/5`. That prior statement is preserved above as the record of its own moment, not
deleted.

D-08's no-partial-pass rule is satisfied on its own terms for the first time: **every scored item
passed**, none was inherited except item 6(b) under the explicit locked decision D-G5, and none was
waived. The one sub-check that could have been quietly assumed — item 2's exactly-one-control visual
check — was instead put to the operator as a direct question and confirmed explicitly.

_Amended: 2026-08-19 — verification brought current against `34.4.2-LIVE-GATE-RERUN-6.md` (PASS 5/5)._
_Amended in place, never regenerated, to preserve commit `7e1781fbb`'s hand-correction._
