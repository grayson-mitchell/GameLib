---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 06
subsystem: macos-shell-ux
tags: [tauri, appkit, nswindow, live-gate, macos]

# Dependency graph
requires:
  - phase: 34.4.2-05
    provides: the six-item blocking live-gate contract (`34.4.2-LIVE-GATE.md`, `verdict: null`), the platform-scope record, and the DummyStore-containment guard
provides:
  - The measured, human-observed verdict for Phase 34.4.2's two spike-validated behaviors — FAIL, 0/6, items 3-6 NOT ATTEMPTED
  - Two blocking findings (F-34.4.2-01, F-34.4.2-02) describing an unresponsive login window after the main-window minimize/restore cycle
  - A BINDING operator design decision superseding the phase's original child-window-attachment ship choice with sheet presentation + a mandated close affordance
  - An updated threat roll-up in `34.4.2-PLATFORM-SCOPE.md` §5 distinguishing "live-confirmed-broken" (T-34.4.2-05/-07) from "still unmeasured" (T-34.4.2-15 residual/-17/-21/-22)
affects: [34.4.2-gap-cycle, phase-34-6-electron-cutover-if-any]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
    - .planning/ROADMAP.md
    - .planning/STATE.md

key-decisions:
  - "BINDING (operator, 2026-08-04): login-window presentation switches from AppKit child-window attachment to SHEET presentation. The sheet MUST have an explicit close affordance — the sole required addition. Supersedes the spike-019-021 ship choice; the gap cycle's fix direction is the sheet switch, not debugging the child-window unresponsiveness."
  - "A FAIL is recorded as a FAIL at the item level, not partially credited for sub-checks (c)/(d) of item 1 that appeared to mechanically succeed — D-08's no-partial-pass rule applies within an item, not just across items."
  - "No root-cause diagnosis is asserted for F-34.4.2-01/02 — only what the operator observed. This project's own F-10 lesson (correlation shipped as cause cost nine live runs) applies directly."

requirements-completed: []

# Metrics
duration: ~20min (this session; Task 1 preflight ran in a prior session, commit b2542a56a)
completed: 2026-08-04
---

# Phase 34.4.2 Plan 06: Blocking Live Gate — Run and Verdict Summary

**The six-item blocking live gate ran on real macOS hardware and FAILED 0/6: the AppKit child-window login mechanism becomes unresponsive after the main window minimizes and restores, so the operator could neither interact with the password field nor close the window — items 3-6 were never reached, and the operator made a binding decision to replace child-window attachment with sheet presentation plus a mandated close affordance.**

## Performance

- **Duration:** ~20min this session (Task 1 preflight was a prior session, commit `b2542a56a`)
- **Completed:** 2026-08-04
- **Tasks:** 3 (Task 1 auto — preflight, prior session; Task 2 checkpoint:human-verify — operator drove the gate; Task 3 auto — this session, transcribe + propagate)
- **Files modified:** 4 (`34.4.2-LIVE-GATE.md`, `34.4.2-PLATFORM-SCOPE.md`, `ROADMAP.md`, `STATE.md`) plus this SUMMARY

## Accomplishments

- Recorded a measured, non-fabricated verdict for the two behaviors this phase built: **FAIL, items_passed: 0, items_total: 6**. Item 1 (child-window attachment) and item 2 (dismissability) both FAILED on direct observation; items 3-6 are NOT ATTEMPTED, not passed and not failed — the gate was aborted at the item-1/2 blocking failure, matching the project's own "an honest BLOCKED/NOT-ATTEMPTED is worth more than a guessed PASS" discipline.
- Filed two blocking findings without asserting a root cause: **F-34.4.2-01** (the attached login window becomes unresponsive — cannot close, password field will not accept input — after the main window minimizes and is restored from the Dock) and **F-34.4.2-02** (the same broken window cannot be closed via its own close button). Both are recorded strictly from the operator's verbatim report; no diagnosis is claimed, consistent with the project's own F-10 lesson about shipping correlation as cause.
- Transcribed a **BINDING operator design decision** that changes this phase's own "Locked, do not re-litigate" ship choice: login-window presentation moves from AppKit child-window attachment to **sheet presentation**, with an explicit close affordance as the sole mandated addition. This is recorded in three places — the gate document itself, `STATE.md`'s Decisions section, and `ROADMAP.md`'s Phase 34.4.2 block (whose original sheet-rejection clause is marked superseded, not deleted, preserving the historical record of why sheets were rejected in the first place).
- Updated the threat roll-up in `34.4.2-PLATFORM-SCOPE.md` §5: T-34.4.2-05 (spoofing via z-order) and T-34.4.2-07 (DoS via a trapped modal) moved from "OPEN-pending-gate" (unproven) to **"OPEN — CONFIRMED"** (live-observed broken) — a materially different and worse state than the pre-gate disposition. T-34.4.2-15 (residual, kill-switch), T-34.4.2-17, T-34.4.2-21 and T-34.4.2-22 remain OPEN-pending-gate exactly as before, since items 3-5 were never reached. T-34.4.2-32 (Epic scope-boundary erosion) is unaffected — its disposition was always source-provable, never gate-dependent.
- Stopped the DummyStore harness (`kill 23847`) and confirmed port 17940 is released (`lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing).
- Propagated the result to `ROADMAP.md` (Phase 34.4.2 status line: LIVE GATE FAILED, phase does not close, `/gsd-plan-phase 34.4.2 --gaps` named as the next step; plan 06's checkbox ticked with the verdict inline, matching this project's own established convention for a plan that executed but whose gate FAILED — see `34.5-41-PLAN.md`'s identical treatment) and to `STATE.md` (frontmatter `stopped_at`/`completed_plans`, a new Session Continuity entry, two new Decisions entries, a new Performance Metrics row). `STATE.md` was hand-edited, not written via `gsd-sdk query state.*`, because this repo has a recorded defect where those SDK calls revert `stopped_at`, mangle counters and splice status into historical blocks.

## Task Commits

1. **Task 1: Preflight** — `b2542a56a` (prior session; docs) — harness started/evidenced, binary symbol-verified, suite baselines captured
2. **Task 2: BLOCKING live gate** — no commit (checkpoint:human-verify; the operator drove the gate on real hardware, no file changes)
3. **Task 3: Record the measured verdict and propagate it** — `e69817bf5` (docs) — gate doc filled, threat roll-up updated, ROADMAP/STATE propagated, harness stopped

**Plan metadata:** this SUMMARY's own commit (below)

## Files Created/Modified

- `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md` — frontmatter verdict/run_date/items_passed filled; items 1/2 Observed+Verdict filled from the operator's report; items 3-6 marked NOT ATTEMPTED; ITEM VERDICT SUMMARY table filled; new "Binding Design Decision", "Findings" and "Harness Shutdown" sections added; the closing "nothing has been executed" paragraph corrected to reflect the actual result
- `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md` — §5 threat roll-up given a post-gate update table distinguishing live-confirmed-broken threats from still-unmeasured ones; original acceptance-bar text retained below it for the record
- `.planning/ROADMAP.md` — Phase 34.4.2 block: new Status line (LIVE GATE FAILED 0/6, does not close), binding design decision summary, plan 06 checkbox ticked with verdict, "Locked, do not re-litigate" clause marked superseded
- `.planning/STATE.md` — frontmatter (`stopped_at`, `last_updated`, `completed_plans` 185→186), new Performance Metrics row, two new Decisions entries (gate result + binding design decision), new top-of-history Session Continuity entry (prior entry pushed under "Prior session context, retained for history")

## Deviations from Plan

### Auto-fixed Issues

None — this plan only transcribes a measured result and propagates it; no code was touched, matching its own acceptance criterion (`git diff --stat -- src src-tauri/src` is empty, unchanged by this plan).

### Scope note (not a deviation, a deliberate scope boundary)

`.planning/REQUIREMENTS.md` is **not** in this plan's `files_modified` frontmatter and was left untouched, even though its own Plan-05-authored `deferred-items.md` entry already flags that REQ-34.4.2-01/02/03/06 carry premature `[x]` checkmarks that should be corrected once this gate's own result is known (REQ-34.4.2-06 explicitly names "plan 06's live gate" as the box's own precondition, and that precondition just returned NOT ATTEMPTED for item 4, not PASS). This is recorded here and in `STATE.md`'s new session note as a fix-alongside item for the gap-cycle plan (`/gsd-plan-phase 34.4.2 --gaps`), not fixed in this plan, to respect this plan's own declared scope.

## Auth Gates

None encountered.

## Known Stubs

None — this plan produces no runtime code.

## Threat Flags

None — this plan modifies only planning documents; no new network endpoints, auth paths, file access patterns, or schema changes were introduced.

## What Happens Next

Per D-08's no-partial-pass rule, **Phase 34.4.2 does not close.** The next step is `/gsd-plan-phase 34.4.2 --gaps`, scoped around:
1. Implementing the operator's binding design decision — sheet presentation replacing AppKit child-window attachment, with a mandated explicit close affordance.
2. Re-running the full 6-item gate once that lands, since items 3-6 (the glyph/AutoFill menu, Cmd+V, kill-switch efficacy, hidden-window/Epic non-interference) were never reached this run and remain exactly as unproven as before this gate.
3. Correcting `.planning/REQUIREMENTS.md`'s REQ-34.4.2-01/02/03/06 premature `[x]` rows (carried forward from `deferred-items.md`'s Plan 05 entry, not fixed here).

## Self-Check: PASSED

- `FOUND: .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md` (verified below)
- `FOUND: .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md`
- `FOUND: .planning/ROADMAP.md`
- `FOUND: .planning/STATE.md`
- Commit `b2542a56a` (Task 1, prior session) — present in `git log`
- Commit `e69817bf5` (Task 3, this session) — present in `git log`
- Port 17940 released — `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing (exit 1)
