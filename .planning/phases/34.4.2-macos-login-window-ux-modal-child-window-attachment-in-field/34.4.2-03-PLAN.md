---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 03
type: execute
wave: 3
depends_on: ["34.4.2-02"]
files_modified:
  - src-tauri/src/main.rs
  - src/backend/__tests__/tauriShellSource.test.ts
autonomous: true
requirements:
  - REQ-34.4.2-04
  - REQ-34.4.2-06
  - REQ-34.4.2-07
  - REQ-34.4.2-08
  - REQ-34.4.2-10

must_haves:
  truths:
    - "A key glyph is rendered inside the password field of a login page in the Tauri-managed login window."
    - "Clicking the glyph delivers the field's rect to the Rust shell without navigating the login page."
    - "The pristine Epic login window receives no injected script and no sentinel interception."
    - "The autofill request payload can never carry the password field's value."
    - "The glyph never consumes a keyboard event, so Cmd+V paste is unaffected."
    - "Hidden reveal/clear windows never receive the glyph script."
  artifacts:
    - path: "src-tauri/src/main.rs"
      provides: "autofill_glyph_script() builder, AutofillRequest struct, parse_autofill_request() pure parser, sentinel interception on the Tauri-managed surface"
      contains: "fn parse_autofill_request"
    - path: "src/backend/__tests__/tauriShellSource.test.ts"
      provides: "retightened on_navigation negative + glyph/sentinel source assertions"
  key_links:
    - from: "injected glyph script (Tauri-managed login window)"
      to: "src-tauri/src/main.rs sentinel interception"
      via: "hidden iframe navigation to REVEAL_EXFIL_HOST /autofill-request?data="
      pattern: "AUTOFILL_EXFIL_PATH"
    - from: "humble_login_open builder"
      to: "autofill_glyph_script"
      via: ".initialization_script(...) on the visible builder"
      pattern: "initialization_script"
---

<objective>
Put the in-field autofill affordance on screen and give it a way to reach Rust. Spikes 020/022 proved
the shape: inline Password AutoFill and save-prompts are platform-blocked on loopback HTTP *and* real
HTTPS, but a **synthesized right-click at the password field pops the real system context menu with
`AutoFill ›` in it**. This plan builds everything up to (but not including) the synthesized click:
the injected glyph, and the page→Rust request channel carrying the field's geometry.

**SCOPE (locked user decision, 2026-08-04): the Tauri-managed `WebviewWindow` surface ONLY** — Humble
plus the GOG/Amazon OAuth runners. **Nothing is injected into the pristine Epic `WKWebView`.** That
surface exists specifically to give Epic a zero-Tauri-injection page environment, Epic's pre-auth 403
is a parked deterministic anti-bot blocker, and the standing hCaptcha/UA constraint is unresolved —
so the user has taken the "scope the pristine arm out" option outright. No `WKUserScript`, no
`WKUserContentController`, no sentinel arm in `EpicPristineNavDelegate`, and consequently **no
Cargo.toml change in this plan at all** (the three `objc2-web-kit` features that arm would have needed
are not added). Epic ships last, after all other runners are proven.

Purpose: without an in-field affordance the AutoFill route is undiscoverable — the user has to
somehow know to right-click a password box. The glyph is the whole shippable win.

Output: one JS glyph script generated by a pure Rust builder and injected via
`initialization_script()` on the Tauri-managed login window, plus one cancelled-navigation sentinel
handler that parses the request and — for now — logs it. Plan 34.4.2-04 replaces that log with the
actual `postEvent:` pair.

**Explicitly NOT in scope, and not a deferral:** opening the AutoFill panel directly. Spike 022
proved it impossible — macOS injects the AutoFill item at menu-display time and it is absent from the
`NSMenu` object graph even when the screen shows it (`willOpenMenu:` hands over a 3-item
Cut/Copy/Paste menu; a post-display re-dump of the same retained menu is still 3 items). No task
here may attempt it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md
@.claude/skills/spike-findings-gamelib/references/tauri-login-webview-cookies.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-02-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase at plan time. -->

**The established page→Rust channel in this file is cancelled-navigation exfil.** Remote pages
CANNOT invoke app commands — `tauri-login-webview-cookies.md` § "What to Avoid" records that with
`windows: ["login-*"]` and `remote.urls: ["https://*.humblebundle.com"]` declared, an injected script
still got `rejected: report_from_page not allowed. Plugin not found`. Do not design around
`window.__TAURI__`.

The existing exfil constants and both existing consumers (`src-tauri/src/main.rs`):
```
const REVEAL_EXFIL_HOST: &str = "gamelib.invalid";           // ~L1033, RFC 2606 reserved, never resolves
fn reveal_post_script(path, body, csrf_token, exfil_host) -> String   // ~L1178
fn clear_storage_script(exfil_host) -> String                         // ~L1278, uses an @@EXFIL_HOST@@ placeholder + .replace()
```
and the interception shape both hidden-window arms use (`humble_reveal_post` ~L3206,
`humble_login_clear_storage` ~L3273):
```rust
.on_navigation(move |url| {
    if url.host_str() == Some(REVEAL_EXFIL_HOST) {
        if let Some((_, payload)) = url.query_pairs().find(|(k, _)| k == "data") {
            let _ = tx.send(payload.into_owned());
        }
        return false;   // cancel
    }
    true
})
```

**OUT OF SCOPE — do not touch.** `EpicPristineNavDelegate::navigation_policy` (~L1400) is the
pristine surface's equivalent hook and `open_pristine_epic_login_window`'s
`WKWebViewConfiguration::new(mtm)` (~L1721) is where a `WKUserScript` would attach. Both are excluded
by the locked user decision (REQ-34.4.2-10) and both must be byte-unchanged. Plan 01's
`PHASE_34_4_2_NEW_SYMBOLS` scope guard fails if this plan references any of its symbols from that
function.

**The negative test this plan must RETIGHTEN, not delete**
(`src/backend/__tests__/tauriShellSource.test.ts:636`):
```
test('NEGATIVE: the humble_login_open arm contains no .on_navigation( call anywhere in its own body', () => {
  const armBody = extractHumbleLoginOpenArmBody(loadMainRsCode())
  expect(armBody).not.toContain('.on_navigation(')
})
```
Its `extractHumbleLoginOpenArmBody` helper slices `'"humble_login_open" => {'` →
`'"humble_login_cookies" => {'`. The invariant it protects is that **no navigation event drives the
login-watch relay** (spike 013: 5 of 8 `on_navigation` events on Humble's real login page were
third-party iframes; letting an ad frame re-arm `notifyLoginNavigated()`'s deadline defeats WR-03's
timeout). A sentinel-only `on_navigation` arm does not violate that invariant — but the test must be
rewritten to enforce the invariant precisely rather than deleted.

Cargo pins (`src-tauri/Cargo.toml`): **unchanged by this plan.** `initialization_script()` is a plain
Tauri builder method needing no objc2 surface at all. The `WKUserContentController` / `WKUserScript` /
`WKUserScriptInjectionTime` features an Epic arm would have required are deliberately NOT added —
`git diff --exit-code src-tauri/Cargo.toml` returning 0 is an acceptance criterion on every task here.

Dev-gate precedent to copy for the kill switch (`humble_login_open` ~L2714):
`#[cfg(debug_assertions)] if visible && std::env::var("GAMELIB_LOGIN_DIAG").as_deref() == Ok("1")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The glyph script builder, the request struct, and the pure sentinel parser</name>
  <files>src-tauri/src/main.rs</files>
  <read_first>
    - src-tauri/src/main.rs (L1020–L1300: `REVEAL_EXFIL_HOST`, `reveal_post_script`, `clear_storage_script` and its `@@EXFIL_HOST@@` placeholder technique — copy the escaping discipline exactly; L900–L1010: `login_window_url_arg`/`login_window_title`/`login_event_value` for the pure-helper + `#[cfg(test)]` convention; L4094–L4110: the existing `#[test]` block style)
    - .claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md (§4 "The in-field affordance" — the coordinate contract and the MANDATORY `document.elementFromPoint` logging at the click centre)
    - .claude/skills/spike-findings-gamelib/sources/022-programmatic-autofill-trigger/app/src/main.rs (L356–L420 `password_field_point` — the exact JS that locates the field and reports the rect plus `hitId`/`hitTag`/`hitType`)
  </read_first>
  <behavior>
    - `parse_autofill_request` returns `None` for a URL on any host other than `REVEAL_EXFIL_HOST`.
    - `parse_autofill_request` returns `None` for the right host but the wrong path (e.g. the existing `/reveal` and `/clear-storage` paths must NOT be mistaken for autofill requests).
    - `parse_autofill_request` returns `None` when `data` is absent, when it is not valid JSON, and when any of `x`/`y`/`w`/`h` is missing or non-numeric.
    - `parse_autofill_request` returns `Some(AutofillRequest)` with the four numeric fields for a well-formed request, and carries `hit_id`/`hit_tag`/`hit_type` as `Option<String>` truncated to a small fixed length.
    - `parse_autofill_request` returns `None` when `w` or `h` is <= 0.0, or when any of the four numbers is non-finite (NaN/inf) — a degenerate rect must never reach the coordinate math in plan 34.4.2-04.
    - `autofill_glyph_script` output contains the exfil host exactly once as a JSON-escaped literal, and contains no unescaped interpolation of any caller-supplied value.
  </behavior>
  <action>
Add three pure, `#[cfg(target_os = "macos")]`-free-where-possible items to `src-tauri/src/main.rs`,
each with `#[cfg(test)]` unit tests in the file's existing `#[test]` block — the same "pure helper,
AppHandle-free, cargo-testable" pattern `login_window_url_arg`/`login_window_title`/`reveal_post_args`
already establish.

**(a) `const AUTOFILL_EXFIL_PATH: &str = "/autofill-request";`** — the sentinel path on the existing
`REVEAL_EXFIL_HOST`. Reuse the host; do NOT introduce a second host. Document that path
discrimination is what keeps this channel from colliding with `/reveal` and `/clear-storage`.

**(b) `struct AutofillRequest { x: f64, y: f64, w: f64, h: f64, hit_id: Option<String>, hit_tag:
Option<String>, hit_type: Option<String> }`** plus
**`fn parse_autofill_request(url: &tauri::Url) -> Option<AutofillRequest>`** — pure, no AppHandle, no
logging, no I/O. Validates in this order: host equals `REVEAL_EXFIL_HOST`; path equals
`AUTOFILL_EXFIL_PATH`; a `data` query pair exists; it parses as JSON; `x`/`y`/`w`/`h` are all
present, numeric and finite; `w > 0.0 && h > 0.0`. Any failure returns `None` — never a partial
struct, never a panic, never an `unwrap()`. The three `hit_*` fields are advisory diagnostics only:
accept them as strings, truncate each to at most 64 chars, and never let their absence fail the
parse. Mirror `reveal_post_args`'s "this arm's entire ASVS V5 input validation lives here" doc-comment
discipline.

**(c) `fn autofill_glyph_script(exfil_host: &str) -> String`** — builds the injected JS. Use
`clear_storage_script`'s `@@EXFIL_HOST@@` placeholder + `serde_json::to_string` + `.replace()`
technique verbatim; the host is the ONLY interpolated value.

The script's required behavior, stated as a contract the executor implements (write the JS itself —
do not paste a snippet from this plan):
- Idempotent: bail immediately if `window.__GAMELIB_AUTOFILL_GLYPH__` is already set; set it first.
- Runs in every frame (login forms are frequently in iframes) and works when the form renders late:
  do an initial scan on `DOMContentLoaded`/immediately, plus a `MutationObserver` on
  `document.documentElement` with `{childList:true, subtree:true}`, debounced.
- For each `input[type="password"]` not already decorated, render one glyph element positioned over
  the field's right-hand inside edge. Position it from `getBoundingClientRect()` with
  `position:fixed` and a maximal `z-index`; reposition on `scroll` (capture) and `resize`. Do NOT
  wrap or re-parent the input, do NOT set any attribute on the input other than a single namespaced
  data marker, and do NOT alter the input's own styles — a login form's validation logic must see an
  untouched field.
- Accessibility: `role="button"`, `aria-label="AutoFill password"`, `title` the same, `tabindex="-1"`
  so the glyph never enters the form's tab order.
- **Keyboard-transparent (REQ-34.4.2-06):** the glyph registers NO `keydown`/`keyup`/`keypress`
  listener anywhere, and calls `preventDefault()` on nothing but its own `mousedown`. Cmd+V into the
  password field must keep working — it is the universal password-manager fallback and the route a
  user takes when the AutoFill panel has no entry for the site.
- On glyph activation: `field.focus()`, then read `getBoundingClientRect()`, compute the centre, call
  `document.elementFromPoint(cx, cy)` and read `id`/`tagName`/`type` off the result (the skill makes
  this MANDATORY — a title-bar-sized coordinate error otherwise silently probes the wrong field and
  its menu is misread as evidence). Build `{x,y,w,h,hitId,hitTag,hitType}` — **the field's `value` is
  never read and must not appear anywhere in the script.**
- Deliver via a **hidden `<iframe>`**, not `location.href`: create (or reuse) a `display:none`,
  1×1 iframe and set its `src` to
  `'https://' + <host> + '/autofill-request?data=' + encodeURIComponent(JSON.stringify(payload))`.
  The iframe is what makes this safe on a LIVE login form — `location.href` would navigate the main
  document (fine for the existing hidden reveal/clear windows, destructive here: it fires
  `beforeunload` and can drop half-entered form state). Both surfaces' navigation hooks fire for
  subframes, so an iframe navigation is observed and cancelled identically.
- Rate-limit itself: ignore a second activation within 750 ms.

Everything the script does must be wrapped in a single top-level `try { } catch (e) { }` so a
throwing glyph can never break a login page.
  </action>
  <acceptance_criteria>
    - `cargo test --manifest-path src-tauri/Cargo.toml` exits 0 and the run includes new named tests covering all six behaviors above (name them so the behavior is readable from the test name, e.g. `autofill_request_rejects_reveal_path`).
    - Negative test present and passing: `parse_autofill_request` on a URL built from `REVEAL_EXFIL_HOST` + `/reveal?data=...` returns `None`.
    - Negative test present and passing: `w = 0.0`, `h = -1.0`, and `x = f64::NAN` each return `None`.
    - `autofill_glyph_script("gamelib.invalid")` output does NOT contain the substrings `.value`, `keydown`, `keypress` or `keyup` (assert this in a cargo test — it is the machine-checkable form of REQ-34.4.2-06 and REQ-34.4.2-07).
    - `autofill_glyph_script` output contains `iframe` and does NOT contain `location.href`.
    - `git diff --exit-code src-tauri/Cargo.toml` returns 0 for this task (feature additions belong to Task 2).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -20 && git diff --exit-code src-tauri/Cargo.toml && echo GLYPH_HELPERS_OK</automated>
  </verify>
  <done>Three pure helpers exist with cargo tests proving the parser rejects every malformed and degenerate input, and proving the generated script reads no field value and binds no keyboard listener.</done>
</task>

<task type="auto">
  <name>Task 2: Inject the glyph and intercept the sentinel on the Tauri-managed login surface</name>
  <files>src-tauri/src/main.rs, src/backend/__tests__/tauriShellSource.test.ts</files>
  <read_first>
    - src-tauri/src/main.rs (L2596–L2812: the `humble_login_open` arm — its `visible` gating, the existing `#[cfg(debug_assertions)] ... initialization_script(DEV_LOGIN_DIAGNOSTIC_INIT_SCRIPT)` block and the `.on_page_load(...).build()` chain; L3195–L3250: `humble_reveal_post`'s `.on_navigation(...)` interception for the exact cancel shape)
    - src/backend/__tests__/tauriShellSource.test.ts (plan 01's `PHASE_34_4_2_NEW_SYMBOLS` array — this task appends to it)
    - .claude/skills/spike-findings-gamelib/references/tauri-login-webview-cookies.md (§ "What to Avoid" — why `window.__TAURI__` is not a channel; and the `on_page_load` vs `on_navigation` distinction)
  </read_first>
  <action>
Wire the Task 1 script into the Tauri-managed login surface and intercept its sentinel.

**The only surface in scope (`humble_login_open` arm — Humble/GOG/Amazon):**
- Inside the existing `if visible` block, add
  `builder = builder.initialization_script(&autofill_glyph_script(REVEAL_EXFIL_HOST));`.
  `initialization_script` runs before any page script and in every frame, which is what the glyph
  needs. Gate it on `visible` so the hidden reveal/clear windows this same arm builds never receive
  it — those windows run their own exfil scripts and a second injected script there is pure risk.
- Add a **kill switch** mirroring the existing `GAMELIB_LOGIN_DIAG` precedent, but DEFAULTING ON:
  skip injection when `std::env::var("GAMELIB_AUTOFILL_GLYPH").as_deref() == Ok("0")`. Unlike
  `GAMELIB_LOGIN_DIAG` this is NOT `#[cfg(debug_assertions)]`-gated — it must work in a packaged
  build.
  **Why it survives the Epic descope** (its original justification was the Epic arm, which is now
  gone, so it is re-earned here on the remaining surface alone): `initialization_script()` runs
  before any page script, in every frame, on live third-party login pages that sit behind real bot
  management — Humble is behind Cloudflare, and this repo has already had an injected
  `initialization_script` become the *prime suspect* for causing a deterministic 403
  (`DEV_LOGIN_DIAGNOSTIC_INIT_SCRIPT`, which is why that one now defaults OFF). If injection ever
  trips a store's detection, the failure mode is a user locked out of their own library with no
  workaround short of a rebuild. Three lines of Rust against that is a trade worth making. It also
  gives the live gate a real item — proving the escape hatch actually works — rather than a
  hypothetical. Log which arm was taken, once, at open.
- Add an `.on_navigation(...)` hook to the builder that handles **only** the autofill sentinel:
  call `parse_autofill_request(url)`; on `Some(req)`, log one line carrying ONLY
  `label`, `req.hit_id`, `req.hit_tag`, `req.hit_type` and the four numbers, then `return false`
  (cancel); on `None`, `return true` unchanged. This hook must NOT call
  `push_login_window_event`, must NOT touch `current_origin`, and must NOT call `set_title` — the
  nav relay stays exclusively on `on_page_load`.

**Retighten, do not delete, the guard at `tauriShellSource.test.ts:636.`** Replace the blanket
`expect(armBody).not.toContain('.on_navigation(')` with assertions that enforce the invariant the
original was protecting:
  1. the arm contains at most one `.on_navigation(` call;
  2. the sliced body of that `.on_navigation(` closure contains `parse_autofill_request`;
  3. that same closure body contains none of `push_login_window_event`, `set_title`, `current_origin`,
     `login_event_value`;
  4. `.on_page_load(` remains present and still owns the `set_title(` call (the existing POSITIVE
     test at L626 must keep passing untouched).
Rewrite the describe-block comment to say plainly WHY the invariant exists (spike 013: 5 of 8
`on_navigation` events on Humble's real login page were third-party iframes; a subframe-driven relay
lets an ad frame re-arm the login watch's deadline forever) and why a sentinel-only, cancel-only arm
does not violate it.

**Pristine Epic surface — DO NOT TOUCH (REQ-34.4.2-10).** Do not add a `WKUserScript`, do not touch
`WKWebViewConfiguration::new(mtm)`'s `userContentController`, do not add a sentinel arm to
`EpicPristineNavDelegate::navigation_policy`, and do not add the `WKUserContentController` /
`WKUserScript` / `WKUserScriptInjectionTime` features to `objc2-web-kit`. `open_pristine_epic_login_window`
and `EpicPristineNavDelegate` must both be byte-unchanged.

This is the user's locked scope decision, and it also happens to retire the largest risk this phase
carried: injecting a document-start script into the one surface built specifically to have none, on a
page whose deterministic pre-auth 403 already has a previously-added document-start script as its
leading suspect. That risk is not being managed here — it is being *removed*. A future Epic phase can
re-open it with its own evidence.

Finally, APPEND `AUTOFILL_EXFIL_PATH`, `AutofillRequest`, `parse_autofill_request` and
`autofill_glyph_script` to `PHASE_34_4_2_NEW_SYMBOLS` in
`src/backend/__tests__/tauriShellSource.test.ts`, so plan 01's scope guard covers this plan's symbols.

Non-macOS: keep the glyph injection and the sentinel handling `#[cfg(target_os = "macos")]`-gated —
the affordance's whole purpose is the macOS AutoFill menu, and Windows/Linux behaviour for both
autofill and this event path is UNVERIFIED (skill § Constraints). On those platforms the login window
is byte-identical to today.
  </action>
  <acceptance_criteria>
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds.
    - `git diff --exit-code src-tauri/Cargo.toml` and `git diff --exit-code src-tauri/Cargo.lock` both return 0 — this plan needs no objc2 surface at all, so `cargoFeatures.test.ts`'s `EXPECTED_LOCKFILE_CRATE_NAMES` pin is untouched.
    - `npx jest src/backend/__tests__/cargoFeatures.test.ts` exits 0.
    - `autofill_glyph_script(` is CALLED exactly ONCE — in the `humble_login_open` arm's `if visible` block.
    - `parse_autofill_request(` is CALLED exactly ONCE — in that arm's `.on_navigation(` closure.
    - `git diff src-tauri/src/main.rs` shows ZERO changed lines inside `open_pristine_epic_login_window` AND inside `EpicPristineNavDelegate` (REQ-34.4.2-10).
    - The retightened tests at `tauriShellSource.test.ts` pass, and the pre-existing POSITIVE test `'POSITIVE: the title-refresh call (set_title) lives inside the arm\'s .on_page_load( hook body'` is unmodified and still passing.
    - `GAMELIB_AUTOFILL_GLYPH` appears exactly ONCE in main.rs, and not inside a `#[cfg(debug_assertions)]` block.
    - `PHASE_34_4_2_NEW_SYMBOLS` now contains this plan's four new symbol names.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && git diff --exit-code src-tauri/Cargo.toml && git diff --exit-code src-tauri/Cargo.lock && npx jest src/backend/__tests__/cargoFeatures.test.ts 2>&1 | tail -8</automated>
  </verify>
  <done>The Tauri-managed surface injects the glyph (gated on visible + the kill switch) and cancel-intercepts the sentinel; the pristine Epic surface is byte-unchanged; the on_navigation negative was retightened rather than deleted; no Cargo.toml change at all.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Source assertions for injection, interception, the never-read-a-credential guarantees and the Epic scope guard</name>
  <files>src/backend/__tests__/tauriShellSource.test.ts</files>
  <read_first>
    - src/backend/__tests__/tauriShellSource.test.ts (the comment-stripped loader, `extractHumbleLoginOpenArmBody`, and the Plan 01/02 describe blocks added by 34.4.2-01/02)
    - src-tauri/src/main.rs (everything shipped in Tasks 1 and 2)
    - .claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md (§ "What to Avoid" — the four dead ends these negatives permanently fence off)
  </read_first>
  <behavior>
    - Test 1: `autofill_glyph_script(` is called exactly ONCE in the comment-stripped source, inside the `humble_login_open` arm slice.
    - Test 2: within that arm slice, the `autofill_glyph_script(` call site lies inside an `if visible` guard.
    - Test 3: the arm contains exactly one `.on_navigation(` call, its closure body contains `parse_autofill_request` and `return false`, and contains none of `push_login_window_event`, `set_title`, `current_origin`, `login_event_value`.
    - Test 4 (SCOPE GUARD, REQ-34.4.2-10): plan 01's array-driven guard is GENERALIZED — every member of `PHASE_34_4_2_NEW_SYMBOLS` (current and future appends) is asserted absent from BOTH sliced bodies: `open_pristine_epic_login_window`'s AND `EpicPristineNavDelegate`'s. Additionally, `open_pristine_epic_login_window`'s slice contains no `WKUserScript`, no `addUserScript` and no `userContentController` — the pristine surface receives nothing from this phase.
    - Test 5 (NEGATIVE, permanent): the comment-stripped source contains none of `_showDigitalCredentialsPicker`, `_setCanUseCredentialStorage`, `_canUseCredentialStorage`, `menuForEvent` — spike 022 proved the first three are the W3C Digital Credentials API (identity documents, not passwords) or private, and that `menuForEvent:` returns nil because WebKit builds context menus asynchronously.
    - Test 6 (NEGATIVE, permanent): the source contains no `willOpenMenu` and no `NSMenu` import — the AutoFill item is injected by macOS at menu-display time and is absent from the NSMenu object graph; capturing or re-firing it is impossible and no future change may try.
    - Test 7: `GAMELIB_AUTOFILL_GLYPH` appears exactly ONCE and does not sit inside a `#[cfg(debug_assertions)]` block — the kill switch must work in a packaged build, because the failure it protects against is a user locked out of their library by an injection-triggered bot block, with no workaround short of a rebuild.
    - Test 8 (NEGATIVE, REQ-34.4.2-10): `src-tauri/Cargo.toml`'s `objc2-web-kit` feature array contains none of `WKUserScript`, `WKUserContentController`, `WKUserScriptInjectionTime` — the features only a pristine-surface injection would need. Read the manifest directly for this one; it is a config-shape assertion, not a main.rs assertion.
  </behavior>
  <action>
Add a `describe('Phase 34.4.2 Plan 03 — in-field autofill glyph and its cancelled-navigation
channel', ...)` block to `src/backend/__tests__/tauriShellSource.test.ts` implementing the eight
behaviors above, and place the retightened `on_navigation` assertions from Task 2 in the original
`F-34.5-G6-04 (Plan 27)` describe block where they belong (that block owns the invariant).

Every assertion runs through the comment-stripped loader. Reuse the file's existing
`extractHumbleLoginOpenArmBody` helper for the arm slice and plan 01's `extractPristineLoginFnBody`
helper; add an equivalent extractor for `EpicPristineNavDelegate`'s `define_class!` block, bounding
it explicitly and asserting the bounds were found before slicing. Then GENERALIZE plan 01's
array-iterating scope-guard test itself (do not write a separate hardcoded 2-symbol check): the
existing `PHASE_34_4_2_NEW_SYMBOLS` loop must run every array member against BOTH extractors'
slices. This is what makes plan 04's Test 7 claim ("the array-driven guard covers both bodies for
the appended symbols") true by mechanism rather than by assumption — any symbol a later plan
appends to the array is automatically checked against both Epic regions with no further test edits.
Append this plan's four symbols to the array as already specified.

Assert exact counts. For tests 5 and 6, write the test names as the reason: a future reader must be
able to see that these are not stylistic bans but records of measured platform impossibility, so
nobody spends another spike rediscovering them.
  </action>
  <acceptance_criteria>
    - `npx jest src/backend/__tests__/tauriShellSource.test.ts` exits 0 with every pre-existing test still passing.
    - Mutation check performed and recorded in the SUMMARY: temporarily add `push_login_window_event(&event_label, login_event_value("nav", ""));` inside the new `.on_navigation(` closure and confirm Test 3 fails; revert.
    - Mutation check performed and recorded in the SUMMARY: temporarily move the `autofill_glyph_script(` call outside the `if visible` guard and confirm Test 2 fails; revert.
    - Mutation check performed and recorded in the SUMMARY: temporarily add a `parse_autofill_request` call inside `EpicPristineNavDelegate::navigation_policy` and confirm Test 4 AND plan 01's scope guard both fail; revert.
    - `npm run codecheck` exits 0.
    - `npm run test:ci` exits 0 with no regression against the baseline recorded at plan start (record both numbers).
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/backend/__tests__/tauriShellSource.test.ts 2>&1 | tail -15 && npm run codecheck && npm run test:ci 2>&1 | tail -8</automated>
  </verify>
  <done>Eight assertions pass, all three mutation checks were run and recorded, the four platform-impossibility dead ends are permanently fenced, and the pristine Epic surface is provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote login page → injected glyph script | Our script runs INSIDE an untrusted origin's document and can be observed, wrapped or spoofed by that page. |
| injected script → Rust shell | A new page-driven request channel (cancelled iframe navigation to a reserved host). This is the first time a VISIBLE, credential-bearing login window gets an exfil channel — the two existing ones serve hidden throwaway windows. |
| Rust shell → log file | Request payload fields reach `eprintln!`, and `gamelib.log` is user-uploadable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-10 | Information disclosure | credential exfiltration via the new channel | mitigate | The generated script never reads `field.value` — asserted by a cargo test on `autofill_glyph_script`'s output (`.value` must not appear) and by the payload schema, which `parse_autofill_request` validates as four numbers plus three 64-char-truncated diagnostic strings. Anything else in `data` is dropped by the parser, not passed through. |
| T-34.4.2-11 | Information disclosure | request data reaching an uploadable log | mitigate | The interception logs only the label, the four numbers and `hit_id`/`hit_tag`/`hit_type`. It never logs the full URL, the raw `data` payload, the page origin or the page title — the T-34.4.1-21 / T-28-04 convention this file already enforces for cookies and reveal payloads. |
| T-34.4.2-12 | Spoofing | a malicious page forging autofill requests | accept, bounded | A hostile login page can already do anything it likes inside its own document. The bounded consequence is that it can make Rust post a right-click at coordinates IT chose — which is why plan 34.4.2-04 clamps to the webview's own bounds, debounces, and posts only `RightMouseDown`/`RightMouseUp`. A forged request cannot read anything back: the navigation is CANCELLED and nothing is returned to the page. |
| T-34.4.2-13 | Tampering | sentinel collision with the existing reveal/clear channels | mitigate | `parse_autofill_request` discriminates on PATH (`/autofill-request`) as well as host, and a cargo test asserts a `/reveal` URL on the same host returns `None`. The reveal/clear arms match on host only, but they run on their own hidden windows whose scripts never emit the autofill path. |
| T-34.4.2-14 | Denial of service | request flooding from the page | mitigate | The script self-rate-limits at 750 ms; plan 34.4.2-04 adds the authoritative server-side debounce in Rust, because a page-side limit is advisory only. |
| T-34.4.2-15 | Tampering | injection tripping a store's bot management and locking a user out of login | mitigate | Scope removal first: the pristine Epic surface — the one whose anti-bot posture is a known, parked, deterministic blocker — receives no injection at all (REQ-34.4.2-10, Test 4). For the remaining surface the residual risk is handled by the `GAMELIB_AUTOFILL_GLYPH=0` kill switch, which works in packaged builds (Test 7), plus a script that patches no prototype, wraps no network primitive and adds one namespaced global. Live-gate item 5 proves the escape hatch actually works rather than assuming it. |
| T-34.4.2-32 | Tampering | scope-boundary erosion (Epic re-entering by accident) | mitigate | Exact-count assertions (`autofill_glyph_script` once, `parse_autofill_request` once), Test 4's pristine-slice negatives, Test 8's Cargo.toml feature negative, a zero-line diff requirement on both `open_pristine_epic_login_window` and `EpicPristineNavDelegate`, and plan 01's `PHASE_34_4_2_NEW_SYMBOLS` guard extended with this plan's four symbols. The third mutation check proves the guards fire. |
| T-34.4.2-16 | Denial of service | a throwing glyph breaking a login page | mitigate | The whole script is wrapped in one top-level try/catch; it never re-parents the input, never mutates the input's styles, and sets one namespaced data marker. |
| T-34.4.2-SC | Tampering | dependency surface | mitigate | No npm/pip/cargo install, and — after the Epic descope — no Cargo feature change either. `git diff --exit-code src-tauri/Cargo.toml` and `git diff --exit-code src-tauri/Cargo.lock` both returning 0 are acceptance criteria, and Test 8 asserts the three pristine-only `objc2-web-kit` features are absent. `cargoFeatures.test.ts`'s `EXPECTED_LOCKFILE_CRATE_NAMES` pin is provably untouched. No Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `cargo build --manifest-path src-tauri/Cargo.toml` and `cargo test --manifest-path src-tauri/Cargo.toml` exit 0.
- `npx jest src/backend/__tests__/tauriShellSource.test.ts src/backend/__tests__/cargoFeatures.test.ts` exits 0.
- `npm run codecheck` and `npm run test:ci` exit 0, no regression vs baseline.
- `git diff --exit-code src-tauri/Cargo.toml` and `git diff --exit-code src-tauri/Cargo.lock` both return 0.
- `git diff src-tauri/src/main.rs` shows zero changed lines inside `open_pristine_epic_login_window` and `EpicPristineNavDelegate`.
- Electron build unaffected — no `src/backend/**` runtime source touched.
- DECLARED UNVERIFIED and recorded in the SUMMARY: that the glyph actually RENDERS on a real login
  form, and that the sentinel actually reaches Rust. Neither is provable by source assertion. Both
  are live-gate items 3 and 5 in plan 34.4.2-06.
</verification>

<success_criteria>
- One glyph script, generated by one pure builder, injected into the Tauri-managed login window only,
  gated on `visible` and on a packaged-build-capable kill switch.
- A path-discriminated, cancelled-navigation request channel on that one surface, parsed by a pure
  function that rejects every malformed and degenerate input.
- The pristine Epic surface receives nothing: no script, no sentinel, no Cargo feature (REQ-34.4.2-10).
- The script provably reads no field value and binds no keyboard listener.
- The `on_navigation` invariant is enforced more precisely than before, not weakened.
- Four platform-impossible approaches are permanently fenced by named negative tests.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-03-SUMMARY.md` when done.
</output>
</content>
