---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 24
completed: 2026-08-06
verdict: FAIL
items_passed: 0
items_total: 5
phase_closes: false
next_command: /gsd-debug
---

# Plan 34.4.2-24 — Summary

Ran the blocking live gate against `34.4.2-LIVE-GATE-RERUN-5.md` on real macOS hardware and
recorded the measured result. **Verdict FAIL, `items_passed: 0` of 5. No launch in the session was
ever scorable.** Phase 34.4.2 DOES NOT CLOSE.

## Tasks

| Task | Name | Commit |
|------|------|--------|
| 1 | Preflight — machine-satisfiable preconditions, capture staging, baselines | `f4ce1142b` |
| 2 | BLOCKING live gate (checkpoint:human-verify) | n/a — writes no file |
| 3 | Record the measured verdict and propagate it | this commit |

## Preflight (Task 1) — all green, quoted in the contract

- `pgrep -f 'target/debug/gamelib-shell'` → empty. The NEW single-instance check RERUN-4 lacked.
- `npm run test:ci` → **194 suites / 3776 tests passed**. Delta vs RERUN-4's 191/3748 is
  **+3 suites / +28 tests, FULLY ATTRIBUTED** (plan 22's own commit `59fc8e982` contributed
  +1 suite/+5 tests; the remaining +2/+23 come from out-of-phase quick task `260806-teb`, confirmed
  via `git log --diff-filter=A`). **No unexplained drift → no finding minted**, unlike plan 20's
  preflight which flagged one and never chased it.
- `cargo test --manifest-path src-tauri/Cargo.toml` → 116 passed, 0 failed, 1 ignored.
- `f_34_4_2_12` regression pin → 1 passed. Plan 22's `loginInFlightUiReachability` pin → 5 passed.
- `lsof -nP -iTCP:17940 -sTCP:LISTEN` → nothing (T-34.4.2-27 / REQ-34.4.2-07).
- Session dir `/tmp/gamelib-gate-20260806T112126Z` created UTC-timestamped, confirmed empty,
  launch-zero `gamelib.log`/`.old` archived as baselines.
- `humble_store/config.json` pre-run: **2 bytes, mtime Aug 6 13:52** (point 1 of the comparison).

## Operator's report (Task 2), verbatim in substance

**No launch was scorable, so no launch ordinal exists for any item.** The operator's shell left
`${N}` unset on every delimiter (`=== GATE LAUNCH  — 2026-08-06T11:26:03Z ===`).

**Segment 1 — ABORTED.** Contained two Humble login-window attempts and nothing else:
`loginwin-0-18c94b0e3c019f88-0fc493df` and `loginwin-1-18c94c2d34fc65e0-0f124be5`, both confirmed
Humble by `gamelib.log`'s own `Humble login-window cookie read …` watcher lines naming both labels.
**Epic's window was never built** — `grep -c "pristine WKWebView built for"` over the whole
transcript returns `0`. **No GOG control window was built either.** Item 6(b)'s Epic-first ordering
was therefore not held; the segment was aborted by operator decision and nothing from it is cited.

Both Humble attempts aborted the watch identically (`gamelib.log`):
```
Humble login-window cookie read UNSUPPORTED_OR_ERROR for window loginwin-0-… — aborting watch
Humble login-window cookie read failed: Error: humble_login:no-window:loginwin-0-…
```
The transcript shows `login-sheet: cancel requested for 'loginwin-0-…' via strip` — the sheet was
closed before the watcher read. These aborts are a **consequence of closing**, not the root problem.

**Segment 2 — the decisive observation.** Fresh instance booted 07:03:07 local, captured in the same
appended transcript. The operator opened Humble's login window, was carried straight into Humble by
the live WKWebView cookie jar (D-G2 branch (a)), and **held the sheet OPEN**:
```
present_login_window_as_sheet resolved attached=true for 'loginwin-0-18c94caba4d246b8-f9e0a603'
humble_login_open: presentation requested visible=true … sheet_presented=true
```
No `cancel requested` line for that label. With the sheet open the Humble watcher emitted
**nothing**, and `humble_store/config.json` remained **2 bytes at its original Aug 6 13:52 mtime**.
Operator: *"same result as all previous times"*, *"no logout option"*.

**No wedge occurred at any point.** The app stayed responsive; no `sample` was required or captured.
This is explicitly **NOT** an F-34.4.2-12 regression.

**Operator decision:** stop the run and route to `/gsd-debug` rather than harvest the still-
measurable items. Items 6(b), 1, 2 and 3 are therefore NOT ATTEMPTED **by explicit decision**, not
by foreclosure — recorded as such.

## Per-item result

| Item | Verdict | Reason |
|------|---------|--------|
| 6(b) Epic absences + GOG control | NOT ATTEMPTED | Epic window never built; session stopped by operator decision |
| 1 Sheet presentation / minimize-restore | NOT ATTEMPTED | Stopped by operator decision; 1(e) additionally blocked by the falsified premise |
| 2 Cancel-strip / Esc dismissal | NOT ATTEMPTED | Stopped by operator decision |
| 3 Autofill-affordance absence | NOT ATTEMPTED | Stopped by operator decision; 3(c)'s dedicated relaunch has now never run across EIGHT gate attempts |
| 4 Cmd+V paste, login completes | NOT ATTEMPTED | Additionally BLOCKED — no rendered login form obtainable |
| 6(a) Humble disconnect | NOT ATTEMPTED | Additionally BLOCKED — no disconnect control renders. Third consecutive NOT ATTEMPTED |

## Findings minted

**F-34.4.2-19 (NEW, OPEN, BLOCKING, UNDIAGNOSED) — the D-G2 branch-(a) resolution is FALSIFIED
live.** RERUN-5's Structural Reachability Review resolved the "no disconnect control when the store
is empty" Test 6 hazard by reasoning from source that an auto-login runs the validate path,
`getGamekeys` returns `ok`, and `configStore.set('isLoggedIn', true)` (`user.ts:635`) fires. That
reasoning is internally sound and **false on hardware**. No root cause asserted — diagnosis routed
to `/gsd-debug`. Blast radius: items 1(e)/3(a)/4 blocked on the login-form premise for the **third
consecutive run** (the F-34.4.2-16 class), plus item 6(a) blocked for the first time by this cause.

**F-34.4.2-20 (NEW, contract defect) — a required literal cannot satisfy its own attribution bar.**
`main.rs:2436` interpolates the window label; **`main.rs:3698` emits no label at all**, yet item
6(b) requires that literal to be label-attributed both as a GOG presence and an Epic absence. The
absence half still holds structurally (that line lives inside `humble_login_open`; Epic uses
`open_pristine_epic_login_window`), but that is attribution **by elimination, not by label**. Per
the plan's interfaces block this is recorded as a finding — **the bar was not edited or softened**.
Test 4 (absence-observability) should have caught it: it confirmed the emitters exist but never
checked each carries the label its bar attributes it by. Candidate **eighth** test class named:
*label-attribution completeness*.

**F-34.4.2-10 status:** item 6(a) did NOT run and `clearHumbleStorage` was NOT shown reached, so
its taking-condition (set by plan 23) is **NOT met**. It stays OPEN and deferred on the same
reasons. Recorded in both `PLATFORM-SCOPE.md` §5 and `deferred-items.md`, as required.

**T-34.4.2-42 scorecard, measured: 2** (F-34.4.2-19, F-34.4.2-20) — against a measurable clause
expecting zero now the review carries seven tests.

## Threat dispositions

T-34.4.2-43 does **not** discharge (item 6(a) never ran). T-34.4.2-44 does **not** discharge (no
scored launch existed to hold the single-instance assertion). T-34.4.2-32 does **not** discharge
(item 6(b) never measured). T-34.4.2-39/-41 do **not** discharge under any outcome (D-G1).
T-34.4.2-25/-26/-27/-SC hold as closed by process.

## Propagation

| Target | Change |
|--------|--------|
| `34.4.2-LIVE-GATE-RERUN-5.md` | `verdict: FAIL`, `items_passed: 0`, `run_date: 2026-08-06`; all 6 `Verdict:` fields filled; Preparatory-sequence "Measured outcome", Aborted-segments and Findings subsections; T-42 scorecard; ITEM VERDICT SUMMARY |
| `34.4.2-PLATFORM-SCOPE.md` §5 | **Thirteenth update** appended; the twelve earlier survive |
| `.planning/REQUIREMENTS.md` | Dated notes on REQ-34.4.2-04/-05/-09. **Zero boxes ticked** (D-08, mechanically asserted) |
| `.planning/ROADMAP.md` | Status banner superseded; plan 24 checked |
| `.planning/STATE.md` | frontmatter, Current Position, Session Continuity, counters 203→204 |
| `deferred-items.md` | One appended `## Plan 24` section; no earlier section edited |

## Deviations

- **`run_date: 2026-08-06`** is correct in UTC. Local clock is UTC+12, so segment 2's 07:03 local
  on Aug 7 is 19:03Z on Aug 6 — the whole session falls inside one UTC date.
- **No per-launch `gamelib-launch-N.log` was archived.** No launch was scorable, so no ordinal was
  ever assigned. Recorded as the honest state, not as a capture defect.
- **Task 3 was executed across two dispatches.** The first was cut off mid-propagation by a network
  error (`ENOTFOUND`) after writing the contract, PLATFORM-SCOPE, deferred-items and REQUIREMENTS
  but before ROADMAP/STATE/SUMMARY or any commit. Work was resumed and completed inline; nothing
  was double-applied.
- **`state.begin-phase` corrupted `STATE.md`** at session start (`stopped_at` truncated,
  `last_activity` clobbered, `percent` 94→65, plan counter 23→1, plus a splice into the historical
  Phase-34 block). All hand-corrected against a pre-session snapshot, with a NOTE added to the
  file's own note cluster. The known SDK hazard, recurring.

## Scope guards held

`git diff --stat -- src src-tauri/src` is **EMPTY** — no source changed. No PASS bar, sub-check or
required literal was altered. Item 5 was not re-instated. All prior gate documents
(`-LIVE-GATE.md`, `-RERUN.md`, `-RERUN-2.md`, `-RERUN-3.md`, `-RERUN-4.md`) are byte-unchanged. No
cookie value, gamekey, order identifier, email or account id appears in any committed change —
window labels, counts and domains only.

## Next step

**`/gsd-debug`** — exactly one command, with its reason. The blocker F-34.4.2-19 is an
**undiagnosed app-level defect**, not a known contract gap. This is materially different from gap
cycle 5's own three contract defects (F-A/F-B/F-C), which were named with stated fix directions and
correctly routed to `/gsd-plan-phase --gaps`; that command is **not** correct here.

Plan 34.4.2-25 (`34.4.2-VERIFICATION.md` refresh) remains unstarted — it is wave 4 and depends on
this plan's recorded verdict, which now exists.

## Self-Check: PASSED
