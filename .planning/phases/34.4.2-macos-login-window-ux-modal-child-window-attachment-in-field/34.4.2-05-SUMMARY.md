---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 05
subsystem: infra
tags: [test-containment, threat-register, platform-scope, live-gate-contract, epic-deferred]

# Dependency graph
requires:
  - phase: 34.4.2-01
    provides: PHASE_34_4_2_NEW_SYMBOLS scope-guard array, login-window handle resolvers
  - phase: 34.4.2-02
    provides: AppKit child-window attach/detach, DECLARED UNVERIFIED live z-order note
  - phase: 34.4.2-03
    provides: in-field autofill glyph + sentinel channel, DECLARED UNVERIFIED glyph-render/sentinel-reaches-Rust notes
  - phase: 34.4.2-04
    provides: synthesized right-click poster, DECLARED UNVERIFIED menu-pops/AutoFill-item notes
provides:
  - dummyStoreHarnessContainment.test.ts -- mutation-checked machine guard the DummyStore harness can never ship (REQ-34.4.2-07)
  - 34.4.2-PLATFORM-SCOPE.md -- per-symbol cfg-gate table (source-verified, not assumed), EPIC-DEFERRED record, real Electron-unchanged evidence, threat roll-up, package-legitimacy declaration
  - 34.4.2-LIVE-GATE.md -- six-item blocking gate contract, verdict: null, nothing executed
  - phase-local deferred-items.md (first ledger entries for this phase)
affects: [34.4.2-06-PLAN.md (live gate)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Positive-anchor containment test: a suite of only 'X is absent' assertions passes vacuously if X is renamed or deleted -- Test 5 anchors the guard to a real, existing target file before trusting any of the negatives."
    - "Base-commit-scoped git diff, not main...HEAD: this branch has diverged from main by 382 unrelated files across many prior phases, making a main-relative diff meaningless for a single-phase 'did runtime source change' check. Diffed against the commit immediately preceding plan 01's own first commit instead."
    - "Gate contract authored in a plan explicitly forbidden from executing any item -- verdict/run_date/items_passed and every item's Observed/Verdict field stay null/empty by construction, so a result cannot be back-filled from expectation."

key-files:
  created:
    - src/backend/__tests__/dummyStoreHarnessContainment.test.ts
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
  modified: []

key-decisions:
  - "src/backend/sidecar/__tests__/testContainment.test.ts's STRUCTURALLY_CONTAINED_SUITES was NOT extended: that gate's Block C tripwire scans only files in its own directory (src/backend/sidecar/__tests__/) via readdirSync(__dirname); the new suite lives in a different directory (src/backend/__tests__/), the same directory tauriShellSource.test.ts and cargoFeatures.test.ts already occupy without being registered there. Confirmed by re-running testContainment.test.ts unmodified: 52/52 still pass."
  - "git diff --stat was scoped against b692de26c^ (the commit immediately preceding plan 01's own Task 1 commit), not main...HEAD -- main...HEAD would have reported 382 files / 95,340 insertions from prior, unrelated phases, making the 'zero runtime source change' check meaningless. The base-commit diff reports exactly the three test files this phase's plans 01 and 05 added, nothing else."
  - "REQ-34.4.2-09 was deliberately withheld from this plan's end-of-execution requirements mark-complete call, despite appearing in this plan's own frontmatter requirements: list -- REQUIREMENTS.md's own row text for REQ-34.4.2-09 states plainly it is 'Plan 06's deliverable -- box stays UNCHECKED until then,' and this plan writes only the gate CONTRACT, not a gate RESULT. Marking it complete here would repeat the exact 'claim not matched by its artifact' defect this project's own history already flagged (34.4.1's D-29-09)."

requirements-completed: [REQ-34.4.2-07]
# REQ-34.4.2-08 and REQ-34.4.2-10 were already closed by plan 01 and are unaffected by this plan.
# REQ-34.4.2-09 is explicitly NOT completed by this plan (see key-decisions) -- it stays owned by
# plan 06, whose live gate run is the only thing that can close it.

# Metrics
duration: 23min
completed: 2026-08-04
---

# Phase 34.4.2 Plan 05: Pre-Gate Record — Containment Guard, Platform Scope, EPIC-DEFERRED, Live-Gate Contract Summary

**Closes the record obligations that must exist BEFORE the live gate runs and cannot honestly be written after it: a mutation-checked guard that the DummyStore OAuth test harness can never ship, a per-symbol platform-scope table read from source (not assumed), the EPIC-DEFERRED record of the locked user decision, and a six-item gate contract with `verdict: null` and nothing executed.**

## Performance

- **Duration:** ~23 min
- **Started:** 2026-08-04T03:21:00Z (approx, from prior plan's completion)
- **Completed:** 2026-08-04T03:44:00Z
- **Tasks:** 3 completed
- **Files created:** 4

## Accomplishments

- `src/backend/__tests__/dummyStoreHarnessContainment.test.ts` — six-test guard (REQ-34.4.2-07,
  T-34.4.2-23/-24): no file under `src/`, `src-tauri/src/`, `package.json` or
  `src-tauri/tauri.conf.json` may reference the harness path (`019-dummy-oauth-store`) or its port
  literal (`17940`); the bundle's `resources`/`externalBin` lists carry no `.claude/` path. Test 5
  is a POSITIVE anchor proving the harness file exists and binds loopback only (`listen(PORT,
  '127.0.0.1'`), so the negatives cannot pass vacuously against a renamed or deleted target. Test 6
  bans `0.0.0.0` and any bare single-argument `.listen(PORT)` form in the harness's own source.
  Mutation-checked live: a `17940` literal temporarily added under `src/backend/` correctly failed
  Test 2 and named the offending path in the failure message; reverted, `git diff` confirmed clean.
- `testContainment.test.ts` was checked, not modified: its `STRUCTURALLY_CONTAINED_SUITES` gate
  scans only its own directory (`src/backend/sidecar/__tests__/`) via `readdirSync(__dirname)`,
  and the new suite lives in `src/backend/__tests__/` — a directory `tauriShellSource.test.ts` and
  `cargoFeatures.test.ts` already occupy without registration. Confirmed 52/52 still pass unchanged.
- `34.4.2-PLATFORM-SCOPE.md` — six required sections. The per-symbol table covers all 17 new
  symbols from plans 01-04 (`PHASE_34_4_2_NEW_SYMBOLS`'s 16 named entries plus the deminiaturize
  observer), with every cfg gate read directly from `src-tauri/src/main.rs` this session (spot-checked
  well beyond the required three rows — all 17 were individually grepped and, for the ambiguous
  ones, read in full context). This surfaced a genuine, previously-unrecorded finding: **the
  autofill glyph injection and its sentinel parser are NOT `#[cfg(target_os = "macos")]`-gated** —
  they compile and run identically on Windows/Linux, rendering an inert key icon in every password
  field, while only `post_autofill_right_click` (the half that produces any visible effect) is
  macOS-only. Recorded plainly per the plan's own instruction not to paper over a non-gated symbol.
- The EPIC-DEFERRED section records the locked 2026-08-04 user decision verbatim (Epic ships last),
  the six things consequently not built (one per contributing plan), what stays true for Epic today
  (byte-unchanged, machine-enforced), and what a future Epic phase inherits.
- Electron-unchanged evidence section contains real, measured command output — not a claim a
  command "was run": `npm run test:ci` (189/190 suites clean, one full-suite-only flake in
  `helperProcess.test.ts` isolation-confirmed as pre-existing and unrelated — a later full re-run
  after Task 1's own commit came back 191/191 suites, 3722/3722 tests, zero failures), `npm run
  codecheck` (exit 0), the three named targeted jest suites (41/41), `seam-parity-sweep.py` (real
  `GATE FAILED` exit 1 on a stale, unrelated site, with its own `--self-test` passing 13/13 clean),
  a base-commit-scoped `git diff --stat` (zero runtime source changes outside
  `src/backend/__tests__/`), and both `Cargo.lock`/`pnpm-lock.yaml` confirmed byte-unchanged.
- The threat roll-up consolidates T-34.4.2-01 through -22 plus -32 and -SC across all four
  contributing plans into one table. Six threats (T-34.4.2-05, -07, -15-residual, -17, -21, -22)
  are marked OPEN-pending-gate with no automated discharge; T-34.4.2-15 is recorded as
  retired-by-scope-removal (Epic descope) with only its kill-switch-efficacy residual still open;
  T-34.4.2-32 (the Epic scope-boundary guard) is recorded CLOSED by source — proven four times over,
  once per contributing plan, with no live-gate dependency.
- `34.4.2-LIVE-GATE.md` — six numbered items (child-window attachment, dismissability, the glyph
  and AutoFill menu, Cmd+V paste, kill-switch efficacy, hidden-window non-interference +
  Epic-untouched absence check), each with an explicit `PASS =` condition, exact log lines to
  capture, and blank `Observed:`/`Verdict:` fields. Six preconditions, including the
  seeded-Passwords-app-entry and logout-preamble requirements in bold. A mapping table shows every
  `DECLARED UNVERIFIED` note from plans 02/03/04's own SUMMARYs is either covered by a numbered item
  or named as deliberately out of scope (Windows/Linux, no hardware exists for this gate to test
  against). Frontmatter `verdict: null`, `run_date: null`, `items_passed: null`. **No item in this
  document was executed by this plan** — stated explicitly in the document's own closing section.
- `deferred-items.md` (new for this phase) — the EPIC-DEFERRED cross-reference as its first entry,
  plus three new logged-not-fixed entries discovered during Task 2's evidence-gathering: the
  `helperProcess.test.ts` full-suite-only flake reproducing again (same one 34.4.1 already
  recorded), `seam-parity-sweep.py`'s real-tree run failing on a site (`humbleLoginFlowRegistration.ts:449`)
  this phase's own diff never touches (its own `--self-test` stays clean), and two pre-existing
  `REQUIREMENTS.md` checkbox/prose mismatches (REQ-34.4.2-06 and REQ-34.4.2-07 both marked `[x]`
  before their own stated preconditions were met) — the same class of defect 34.4.1 logged as
  D-29-09, found here but out of this plan's declared `files_modified` scope to fix.

## Task Commits

Each task was committed atomically:

1. **Task 1: Machine guard that the DummyStore harness can never reach a shipped build** - `8311d4c6d` (test)
2. **Task 2: Per-platform scope declaration, the EPIC-DEFERRED record, and Electron-unchanged evidence** - `b60daee0d` (docs)
3. **Task 3: Author the blocking live-gate contract with verdict: null** - `123b66600` (docs)

**Plan metadata:** (this document + STATE.md/ROADMAP.md updates, committed separately per the executor's final-commit step)

## Files Created/Modified

- `src/backend/__tests__/dummyStoreHarnessContainment.test.ts` — six-test containment guard, 162 lines
- `.planning/phases/34.4.2-.../34.4.2-PLATFORM-SCOPE.md` — six-section platform-scope record
- `.planning/phases/34.4.2-.../34.4.2-LIVE-GATE.md` — six-item gate contract, verdict: null
- `.planning/phases/34.4.2-.../deferred-items.md` — new phase-local ledger, four entries

## Decisions Made

See `key-decisions` in frontmatter for the full rationale on: why `testContainment.test.ts`'s
`STRUCTURALLY_CONTAINED_SUITES` was not extended (directory-scoped gate, different directory), why
the Electron-unchanged `git diff --stat` was scoped against the phase's own base commit rather than
`main...HEAD` (382 unrelated files would have made the check meaningless), and why REQ-34.4.2-09 was
deliberately excluded from this plan's own `requirements mark-complete` call despite appearing in
this plan's frontmatter (its own REQUIREMENTS.md row text says it is plan 06's deliverable).

## Deviations from Plan

### Auto-fixed Issues

None — no bugs, missing critical functionality, or blocking issues encountered in this plan's own
deliverables. All three tasks executed exactly as written; the plan's own acceptance criteria were
met on the first attempt for each task.

**Total deviations:** 0

### Out-of-scope discoveries (logged, not fixed — see `deferred-items.md`)

1. `helperProcess.test.ts` full-suite-only flake reproduced again during Task 2's `test:ci` run —
   isolated re-run confirmed 9/9 passing; the same pre-existing flake 34.4.1 already recorded.
2. `seam-parity-sweep.py`'s real-tree run fails (`GATE FAILED`, exit 1) on a site
   (`humbleLoginFlowRegistration.ts:449`) outside this phase's own diff; its own `--self-test`
   passes 13/13. Same class of staleness 34.4.1 logged and eventually fixed for a different pair
   of sites — this script is not in this plan's `files_modified`.
3. Two pre-existing `REQUIREMENTS.md` checkbox/prose mismatches: REQ-34.4.2-06 marked `[x]` before
   plan 06's live gate has run (its own text says it stays unchecked until then), and REQ-34.4.2-07
   was already marked `[x]` before this plan's own Task 1 had produced any artifact. Neither is in
   this plan's declared `files_modified`; both are logged for a future plan (ideally 06, which
   already touches `REQUIREMENTS.md` by convention) to correct.

None of the three above are caused by this plan's own changes — each was found while gathering
evidence this plan's own tasks required, and none is fixed here per the scope-boundary rule.

## Issues Encountered

None beyond the three out-of-scope discoveries above, all logged in `deferred-items.md`.

## User Setup Required

None — no external service configuration required. This plan wrote records and one test file only.

## Threat Flags

None. No file created or modified by this plan introduces new network endpoints, auth paths, file
access patterns, or schema changes at a trust boundary — `dummyStoreHarnessContainment.test.ts` is
a read-only source-assertion guard, and the other three files are planning documents.

## Next Phase Readiness

- Plan 06 (the live gate) can run directly against `34.4.2-LIVE-GATE.md`: all six items, six
  preconditions, and the PASS-condition/log-line/Observed-Verdict structure are in place, with
  every `DECLARED UNVERIFIED` note from plans 02-04 mapped to a covering item.
- `34.4.2-PLATFORM-SCOPE.md`'s per-symbol table and its glyph-not-platform-gated finding are ready
  for whoever next scopes Windows/Linux support for this feature — the inert-icon UX cost is
  flagged, not fixed, and is not this phase's job to close.
- `deferred-items.md` now exists for this phase with four entries; a future plan should reconcile
  the two `REQUIREMENTS.md` checkbox mismatches and, separately, decide who owns
  `seam-parity-sweep.py`'s stale `SITE_PROFILES` entry.
- No blockers. `npx jest dummyStoreHarnessContainment.test.ts testContainment.test.ts` (58/58),
  `npm run codecheck` (exit 0), a full `npm run test:ci` re-run after all three commits (191/191
  suites, 3722/3722 tests, zero failures — the earlier flake did not reproduce), and
  `git diff --exit-code src-tauri/Cargo.lock` (exit 0) all confirmed clean.

---
*Phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field*
*Completed: 2026-08-04*

## Self-Check: PASSED

- FOUND: `src/backend/__tests__/dummyStoreHarnessContainment.test.ts`
- FOUND: `.planning/phases/34.4.2-.../34.4.2-PLATFORM-SCOPE.md`
- FOUND: `.planning/phases/34.4.2-.../34.4.2-LIVE-GATE.md`
- FOUND: `.planning/phases/34.4.2-.../deferred-items.md`
- FOUND: this `34.4.2-05-SUMMARY.md` file
- FOUND: commit `8311d4c6d` (Task 1)
- FOUND: commit `b60daee0d` (Task 2)
- FOUND: commit `123b66600` (Task 3)
