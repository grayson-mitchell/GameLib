---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 12
type: execute
wave: 2
depends_on: ["34.4.2-11"]
gap_closure: true
files_modified:
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [REQ-34.4.2-01, REQ-34.4.2-02, REQ-34.4.2-03, REQ-34.4.2-04, REQ-34.4.2-05, REQ-34.4.2-06, REQ-34.4.2-09, REQ-34.4.2-10]

must_haves:
  truths:
    - "A human drives all six gate items on real macOS hardware, with the tauri:dev transcript captured"
    - "Gate items 2, 3, 4, 5 and 6 get a measured result for the first time in this phase's history, or an honest NOT ATTEMPTED with a stated reason"
    - "The recorded verdict propagates to the threat roll-up, requirement checkboxes, ROADMAP and STATE — whatever it is"
    - "The phase closes only on a genuine 6/6; anything less routes to another gap cycle"
  artifacts:
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md"
      provides: "The measured verdict, per-item Observed/Verdict, findings, transcript excerpts, and harness shutdown record"
      contains: "ITEM VERDICT SUMMARY"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 34.4.2 status line reflecting the measured result"
      contains: "34.4.2"
    - path: ".planning/STATE.md"
      provides: "Decisions entry, Session Continuity entry, and counters for this run"
      contains: "34.4.2"
  key_links:
    - from: "operator's measured observations"
      to: "34.4.2-LIVE-GATE-RERUN-2.md Observed/Verdict fields"
      via: "verbatim transcription, no interpretation added"
      pattern: "Observed:"
    - from: "captured tauri:dev transcript"
      to: "every [shell]-line assertion in the contract"
      via: "tee /tmp/gamelib-dev.log — gamelib.log carries no [shell] lines"
      pattern: "\\[shell\\]"
    - from: "34.4.2-LIVE-GATE-RERUN-2.md verdict"
      to: "REQUIREMENTS.md / PLATFORM-SCOPE.md §5 / ROADMAP.md / STATE.md"
      via: "propagation task"
      pattern: "items_passed"
---

<objective>
Run the fresh six-item blocking live gate on real macOS hardware and record the measured result.

Purpose: this project has now been shown four times that a fully green automated suite proves
nothing about live login-window behaviour — 3279/3279, 3387/3387, 3447/3447 and 3735/3735 were all
green while blocking defects were live, and **every blocking defect in phases 34.4.1 and 34.4.2 was
found by a human driving the UI, none by automation.** This plan is the only source of truth about
whether the login sheet actually works end to end.

**What is genuinely new this run:** gate item 1's ATTACHMENT sub-check is already live-proven
(`read-back attached=true`, commit `8b2fdb315`), so for the first time in this phase's history the
run is not expected to abort at item 1. **Items 2, 3, 4, 5 and 6 have never been attempted in any of
the four prior gate runs.** Reaching them is this run's whole point — the un-losability and
post-restore interactivity sub-checks of item 1 are likewise still unmeasured.

Discipline inherited from plans 06 and 10 and not relaxed: transcribe what the operator observed,
verbatim; assert no root cause; record an honest NOT ATTEMPTED rather than a guessed PASS; apply the
no-partial-pass rule at the item level; and propagate whatever result arrives, including a bad one.

Output: a filled gate document, an updated threat roll-up, correct requirement checkboxes, and
ROADMAP/STATE propagation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-10-SUMMARY.md
@.claude/skills/spike-findings-gamelib/references/oauth-login-test-harness.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — satisfy every machine-satisfiable precondition and capture the baselines</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-2.md` Preconditions section — each precondition's
      satisfier attribution (machine-satisfiable here vs operator-only), including the NEW seventh
      precondition (the `tauri:dev` transcript capture).
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN.md` Preconditions section — the worked example:
      the exact `lsof`/`curl`/`strings` command shapes and the evidence-quoting style to reproduce.
      **Note its own lesson:** its symbol check passed against the on-disk binary and the run still
      failed, so the check proves only "not stale on disk", never "the running process is correct".
    - `.claude/skills/spike-findings-gamelib/references/oauth-login-test-harness.md` — the harness
      start command, port 17940, and the `/events` oracle.
    - `.planning/phases/.../34.4.2-11-SUMMARY.md` — the exact new log-line literals plan 11 added,
      which this preflight symbol-checks for.
  </read_first>
  <action>
    Fill each precondition's satisfaction status in `34.4.2-LIVE-GATE-RERUN-2.md` with real, quoted
    command output — never an assertion without evidence.

    1. **Build freshness.** Run `cargo build --manifest-path src-tauri/Cargo.toml` and quote its
       final line. Then symbol-check `src-tauri/target/debug/gamelib-shell` with
       `strings ... | grep -F` and confirm each of these literals is PRESENT, quoting every result:
       - `read-back attached=` (the debug arc's falsifiable signal — the single most important one)
       - `deferred beginSheet closure entered`
       - `sheet_presented=`
       - `login cancel strip injected for '`
       - `[shell] login-sheet: cancel requested for '`
       - `skipping makeKeyAndOrderFront` (plan 11's WR-07 branch line)
       - `re-registered as still presented` (plan 11's WR-01 line)
       - `/login-cancel`
       Confirm the RETIRED literal `parent deminiaturized, re-raising` is **ABSENT** (grep returns
       nothing, exit 1) — quote that too. State plainly, as the prior contract's own lesson
       requires, that this proves only that the on-disk debug binary is not stale; `tauri dev` runs
       its own build step at launch and the operator still launches it themselves.
    2. **DummyStore harness.** Confirm port 17940 is free with `lsof -nP -iTCP:17940 -sTCP:LISTEN`,
       start
       `node .claude/skills/spike-findings-gamelib/sources/019-dummy-oauth-store/store-server.mjs`
       in the background, confirm liveness with `curl -s --max-time 5 http://127.0.0.1:17940/events`,
       and quote the raw response. **Record the PID — Task 3 stops it.**
    3. **Transcript capture (new precondition).** Verify `/tmp/gamelib-dev.log` does not already
       exist from a prior run (move or delete it if it does, and say so), and write into the
       document the exact launch command the operator must use: `npm run tauri:dev` with stderr
       merged into stdout and piped through `tee /tmp/gamelib-dev.log`. State the reason inline:
       `gamelib.log` at `~/Library/Logs/GameLib/` carries no `[shell]`-prefixed Rust lines in any
       archived file, so without this transcript every `[shell]` assertion in the contract is
       INCONCLUSIVE rather than negative — and it is what made rounds 2 and 3 of the debug arc
       diagnosable at all.
    4. **Operator-only preconditions.** Seeded Passwords-app entry, the sticky-jar logout preamble
       plus the `/events` baseline taken AFTER it, a real Humble login, and `GAMELIB_AUTOFILL_GLYPH`
       unset for the default arm: record each as OPERATOR-AT-GATE-TIME with the staged instruction
       carried verbatim from the contract. **Do not fake any of them.** Carry the rule that an
       unseeded Passwords entry makes item 3 BLOCKED-UNOBSERVABLE, not FAILED.
    5. **Suite baselines, captured BEFORE the live run.** Run `npm run test:ci` and
       `cargo test --manifest-path src-tauri/Cargo.toml`, record both figures verbatim, alongside
       the sentence that these are context, not evidence of live behaviour (T-34.4.2-26) — four
       green suites have now coexisted with four failed gates.

    Write nothing into `verdict`, `run_date`, `items_passed`, or any `Observed:`/`Verdict:` field.
  </action>
  <verify>
    <automated>GATE=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md; curl -s --max-time 5 http://127.0.0.1:17940/events | grep -q 'eventCount' && test "$(grep -c 'verdict: null' "$GATE")" -ge 1 && grep -q '^run_date: null' "$GATE" && grep -q '^items_passed: null' "$GATE" && test "$(grep -cE '^\*\*Verdict:\*\* *(PASS|FAIL|NOT ATTEMPTED|BLOCKED)' "$GATE")" = "0" && echo PREFLIGHT_OK_RESULTS_STILL_NULL</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 and its final line is quoted in the document.
    - All eight literals listed above are FOUND by `strings | grep -F` against `src-tauri/target/debug/gamelib-shell`, each quoted individually.
    - `parent deminiaturized, re-raising` is confirmed ABSENT (grep exit 1) — quoted.
    - The document states explicitly that the symbol check proves only on-disk non-staleness, not that the running process is correct.
    - The harness responds on 17940 with a JSON body containing `eventCount`, quoted raw; its PID is recorded in the document.
    - The exact transcript-capture launch command appears in the document, with the reason (`gamelib.log` has no `[shell]` lines) stated beside it.
    - `npm run test:ci` and `cargo test` figures are recorded verbatim with the "context, not evidence" caveat.
    - Frontmatter still reads `verdict: null`, `run_date: null`, `items_passed: null`, and no `Observed:` field is filled — the verify command asserts this and echoes `PREFLIGHT_OK_RESULTS_STILL_NULL`.
  </acceptance_criteria>
  <done>Every machine-satisfiable precondition is satisfied with quoted evidence, the transcript capture is staged, the operator-only preconditions carry their instructions, and no result field has been touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING live gate — the operator drives all six items on real macOS hardware</name>
  <what-built>
    Since the last gate run, three things changed.

    **1. The sheet now genuinely attaches — live-proven.** A three-round debug arc closed
    F-34.4.2-03/-04/-05: the sheet-candidate window is built hidden so `beginSheet:` can perform the
    reveal (CR-01); attachment is read back from `attachedSheet()`/`isSheet()` rather than inferred
    from "the dispatch didn't time out" (CR-02); and `beginSheet_completionHandler` is deferred
    250ms via GCD because it wedges the OS main thread when called in the same run-loop turn as a
    just-created WKWebView window (F-34.4.2-05). Your own last run produced
    `read-back attached=true` after `deferred_elapsed=260.3ms`.

    **2. Plan 11 fixed five reviewed defects that sit on the routes items 2/3/5 must travel:** the
    autofill poster no longer re-orders a presented sheet (which could have detached it on the first
    glyph click); a failed `endSheet:` hop now re-registers the label so the cancel routes stay
    reachable; the Cancel sign-in strip renders in the top frame only (no duplicates inside Humble's
    third-party iframes); its retry listener and MutationObserver are installed before anything that
    can throw; and it no longer advertises keyboard activation it cannot deliver.

    **3. Task 1's preflight** has started the DummyStore harness on port 17940, symbol-checked the
    built binary for all eight current log literals (and confirmed the retired one is gone), and
    captured the suite baselines.

    **What is still completely unmeasured:** whether the sheet is un-losable, whether it stays
    INTERACTIVE after minimize/restore, and every single one of items 2, 3, 4, 5 and 6 — none of
    which has been attempted in any of the four gate runs to date.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md`
    and work through items 1-6 in order, following each item's own written instructions. Summary:

    1. **Launch with the transcript captured:** run `npm run tauri:dev` with stderr merged into
       stdout and piped through `tee /tmp/gamelib-dev.log`, with `GAMELIB_AUTOFILL_GLYPH` unset.
       **This capture is mandatory** — `gamelib.log` carries no `[shell]` lines, and without the
       transcript every log assertion below is unscoreable.
    2. **Before anything else**, run the sticky-jar logout preamble: open a Tauri-managed login
       window, navigate it to `http://127.0.0.1:17940/logout`, then record the `/events` baseline
       count from `curl -s http://127.0.0.1:17940/events`. Skipping this silently skips the login
       form later, and a "the form was served" assertion then fails for the wrong reason.
    3. **Item 1** — open a GOG/Humble login sheet, then: click the main window (the sheet must stay
       in front; the main window declining clicks is CORRECT sheet behaviour, not a failure), drag
       the main window, minimize it to the Dock, restore it, and **immediately type into the
       password field and click the Cancel sign-in strip.** That last step is the one that matters
       most — it is F-34.4.2-01, the defect that started this phase.
    4. **Item 2** — dismiss a fresh sheet via the Cancel strip, another via bare Esc, and record what
       Cmd+W does. After each dismissal, confirm the main window is clickable again. Also confirm
       exactly ONE "Cancel sign-in" control is visible, including on Humble's real login form.
    5. **Items 3-6** — the glyph and AutoFill menu (DummyStore form and real Humble form), Cmd+V
       paste, the `GAMELIB_AUTOFILL_GLYPH=0` kill-switch arm (including that the Cancel strip
       SURVIVES it), and the hidden-window / Epic-untouched absence checks. **These five items have
       never been reached — getting to them is the point of this run.**

    Capture `/tmp/gamelib-dev.log` excerpts for the log lines each item names. If an item cannot be
    reached, say so plainly — an honest NOT ATTEMPTED is worth more than a guessed PASS, and it is
    what made all four previous runs' records usable.

    If a blocking defect appears, stop and report it. Do not attempt to work around it.
  </how-to-verify>
  <resume-signal>Report the outcome of each item you reached, verbatim, including any log lines captured and any item you did not get to. Type "approved" only if all six items passed.</resume-signal>
  <files>none — the operator drives the UI; no file is written by this task</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-2.md` in full — every item's own instructions are
      the authority; the `<how-to-verify>` summary above does not replace them.
  </read_first>
  <action>Pause execution and hand control to the operator. Claude runs NOTHING in this task: the entire value of this gate is that a human drives the real UI, which is how every blocking defect in phases 34.4.1 and 34.4.2 was actually found. Present the `<how-to-verify>` steps, wait for the operator's report, and carry that report into Task 3 verbatim without editing, summarising, or scoring it here.</action>
  <verify>
    <automated>MISSING — deliberately human-only. This item is unautomatable by construction: it measures on-screen AppKit sheet behaviour, system context-menu contents, and keyboard delivery, none of which the test suite can observe. See T-34.4.2-26 and the four-times-repeated green-suite-with-live-defect record.</automated>
    <human-check>The operator reports a per-item outcome for every item they reached, plus an explicit NOT ATTEMPTED for any they did not, and attaches or quotes `/tmp/gamelib-dev.log` excerpts for the named log lines.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator has reported a measured outcome, or an explicit NOT ATTEMPTED with a reason, for each of the six items.
    - The `/tmp/gamelib-dev.log` transcript exists and its relevant excerpts are available to Task 3; if it does not exist, that is itself recorded as a process finding and every `[shell]` assertion is scored INCONCLUSIVE rather than negative.
    - No file was written by this task.
  </acceptance_criteria>
  <done>The operator has reported a measured outcome for each of the six items, or an explicit NOT ATTEMPTED with a reason, and that report plus the transcript is available verbatim to Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measured verdict and propagate it, whatever it is</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-2.md` — every item's PASS condition, so each
      verdict is scored against the bar fixed before the run, not against what happened.
    - `.planning/phases/.../34.4.2-10-SUMMARY.md` — the propagation worked example: which files,
      which sections, and the hand-edit rule for STATE.md.
    - `.planning/ROADMAP.md` Phase 34.4.2 block — the status banner to supersede (never delete) and
      the gap-cycle-2 plan list to tick.
    - `.planning/STATE.md` frontmatter, Decisions, Performance Metrics and Session Continuity
      sections.
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` §5 — the four existing roll-up tables to
      append a FIFTH to.
  </read_first>
  <action>
    Transcribe the operator's report into the gate document and propagate the result.

    1. Fill each item's `Observed:` field with the operator's report **verbatim**, then an
       interpretation against that item's own named sub-checks — clearly separated, as
       `34.4.2-LIVE-GATE-RERUN.md`'s item 1 does. Quote the relevant `/tmp/gamelib-dev.log` lines as
       captured machine evidence. Fill each `Verdict:` with PASS, FAIL, NOT ATTEMPTED (with the
       reason), or BLOCKED-UNOBSERVABLE (with the reason). Apply the no-partial-pass rule at the
       ITEM level: an item with one failing sub-check is a FAIL, not partial credit. Assert **no
       root cause** for any defect — that diagnosis belongs to whatever cycle comes next, and this
       project has already paid nine live runs plus a three-round debug arc for shipping correlation
       as cause.
    2. Fill the frontmatter `verdict`, `run_date`, `items_passed`, `status: complete`, and the ITEM
       VERDICT SUMMARY table. `items_passed` counts only items that actually passed; NOT ATTEMPTED
       items are neither passed nor failed and must be visible as such.
    3. File any new findings as `F-34.4.2-NN` entries (the next free number after `-05`) with
       symptom, the item that surfaced them, "suspected layer: NOT diagnosed", and whether they
       block the phase.
    4. Append a fifth table to `34.4.2-PLATFORM-SCOPE.md` §5 giving every threat its post-gate
       disposition: T-34.4.2-05 by item 1, -07/-33 by item 2, -17/-21 by item 3, -22 by item 4, -15
       residual by item 5, -32 re-confirmed by item 6, -34/-35 by their source guards, and the three
       new plan-11 threats **T-34.4.2-36** (poster detaching a sheet) by item 1(e), **T-34.4.2-37**
       (permanent lock-out from a failed `endSheet:` hop) by item 2, **T-34.4.2-38** (duplicate
       strips in iframes) by item 2. A threat whose item was NOT ATTEMPTED stays OPEN with that
       stated reason — never quietly closed. Retain all four existing tables.
    5. Tick `.planning/REQUIREMENTS.md` boxes for exactly the requirements the run actually proved
       (REQ-34.4.2-01/-02 by item 1, -03 by item 2, -04 by items 3 and 5, -05 by item 3, -06 by item
       4, -10 by item 6(b), -09 by a genuine measured PASS across the contract). Leave every other
       box unchecked with its reason. **No box may be ticked on the strength of a NOT ATTEMPTED
       item**, and none on the strength of a source assertion alone.
    6. Update `.planning/ROADMAP.md`'s Phase 34.4.2 block: supersede the current
       "SHEET-DESIGN LIVE GATE RE-RUN FAILED 2026-08-04" banner with the new measured status, tick
       plans 11-12's checkboxes with their verdicts inline, and state plainly whether the phase
       CLOSES. Preserve the historical record of both failed runs, the binding sheet decision, and
       the debug arc — mark superseded text, never delete it.
    7. Update `.planning/STATE.md` by **hand-editing** — do not use `gsd-sdk query state.*` writes;
       this repo has a recorded defect where those calls revert `stopped_at`, mangle counters and
       splice status into historical blocks. Update the frontmatter counters and `stopped_at`, add a
       Performance Metrics row, add a Decisions entry for the measured result, and add a
       top-of-history Session Continuity entry. Also correct the stale "Next action" line if it
       still points at Phase 34.5 (the standing `deferred-items.md` entry from plan 07).
    8. Stop the DummyStore harness using the PID recorded in Task 1 and confirm the port is released
       with `lsof -nP -iTCP:17940 -sTCP:LISTEN` returning nothing. Record both in the gate document.

    **On anything short of 6/6:** state that the phase does NOT close, name
    `/gsd-plan-phase 34.4.2 --gaps` as the next step, and scope what the next cycle must address —
    without proposing a design change unless the operator made one. Do not re-propose child windows
    under any circumstances (binding operator decision). **On a genuine 6/6:** state that the phase
    closes, and note what a PASS still does not cover (Windows, Linux, and Epic's login UX — item
    6(b) is an absence check, not a statement that Epic's login works).
  </action>
  <verify>
    <automated>GATE=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-2.md; test "$(grep -c 'verdict: null' "$GATE")" = "0" && grep -qE '^items_passed: [0-9]+' "$GATE" && grep -q 'ITEM VERDICT SUMMARY' "$GATE" && test "$(lsof -nP -iTCP:17940 -sTCP:LISTEN | wc -l | tr -d ' ')" = "0" && test -z "$(git diff --stat -- src src-tauri/src)" && test -z "$(git diff -- .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE.md)" && echo GATE_RECORDED_AND_PROPAGATED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'verdict: null'` on the gate document returns 0 — the frontmatter now carries a real verdict (asserted by the verify command).
    - `items_passed` is an integer, and the ITEM VERDICT SUMMARY table has a non-empty verdict cell for all six items.
    - Every `Observed:` field is non-empty; every item marked NOT ATTEMPTED or BLOCKED-UNOBSERVABLE states a reason.
    - Items 2-6 each carry either a measured result or an explicitly reasoned NOT ATTEMPTED — a fifth consecutive silent skip is itself recorded as a finding.
    - No item's `Observed:` text contains a root-cause claim for a defect (findings say "suspected layer: NOT diagnosed").
    - Port 17940 has no listener at task end and the shutdown is recorded in the document (asserted by the verify command).
    - `git diff --stat -- src src-tauri/src` is EMPTY for this plan — it records a result, it does not fix code (asserted by the verify command).
    - `.planning/ROADMAP.md`'s Phase 34.4.2 block states explicitly whether the phase closes; both failed runs, the binding decision, and the debug arc remain present as history.
    - `.planning/STATE.md` was edited by hand — no `gsd-sdk query state.*` write command appears in this plan's command history.
    - Requirement boxes ticked correspond one-to-one with items that PASSED; `34.4.2-LIVE-GATE-RERUN.md` and `34.4.2-LIVE-GATE.md` are both byte-unchanged (asserted by the verify command, which echoes `GATE_RECORDED_AND_PROPAGATED_OK` only when all of the above hold).
  </acceptance_criteria>
  <done>The measured verdict is recorded with per-item evidence and no invented diagnosis, propagated to the threat roll-up, requirements, ROADMAP and STATE, the harness is stopped, and the phase's close/does-not-close status is stated unambiguously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator report → recorded verdict | Transcription is where an inconvenient observation can quietly become a pass |
| test fixture → production | The DummyStore harness is a loopback test fixture that must never ship |
| process stdout → recorded evidence | `[shell]` lines exist only in the `tauri:dev` transcript; reading `gamelib.log` instead silently downgrades every assertion to inconclusive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-25 | Repudiation (a verdict back-filled from expectation) | Task 3 transcription | mitigate — CLOSED by process | PASS conditions were fixed by plan 11 before this plan ran; Task 3 scores each item against the pre-written bar and transcribes the operator's words verbatim before interpreting them; NOT ATTEMPTED is an allowed and expected outcome. |
| T-34.4.2-26 | Repudiation (a green suite mistaken for live proof) | Task 1 baselines | mitigate — CLOSED by process | Suite figures are recorded explicitly as context, not evidence; four green suites have now coexisted with four failed gates. |
| T-34.4.2-05 | Spoofing (login surface presentation) | item 1 | mitigate — discharged or not BY THIS RUN | Attachment is already live-proven; this run measures only what remains (un-losability, ordering, post-restore interactivity). A regression here would mean the plan-11 fixes disturbed a proven mechanism. |
| T-34.4.2-07 / T-34.4.2-33 | Denial of service (user trapped behind a modal login) | items 1(d) and 2 | mitigate — discharged or not BY THIS RUN | The operator holds the escape hatch: quitting the app always works, and `captureOAuthLogin`'s five-minute deadline bounds any trap during the run itself. If item 2 does not PASS on both required routes, the threat stays OPEN and the phase does not close. |
| T-34.4.2-36 | Tampering (a presented sheet detached mid-session by the poster) | item 1(e), item 3 | mitigate — discharged or not BY THIS RUN | Item 1's post-glyph-click re-check of un-losability is the live measurement; item 3 records which WR-07 branch ran. |
| T-34.4.2-37 | Denial of service (permanent parent lock-out from a failed `endSheet:` hop) | item 2 | mitigate — discharged or not BY THIS RUN | Item 2's record-only sub-check looks for the re-registration WARN and whether a second dismissal attempt then succeeds. |
| T-34.4.2-38 | Spoofing (duplicate cancel controls inside third-party iframes) | item 2, route (a) | mitigate — discharged or not BY THIS RUN | Item 2 requires exactly ONE visible "Cancel sign-in" control, measured on Humble's real iframe-heavy form, not only on the DummyStore fixture. |
| T-34.4.2-27 | Tampering (a test fixture reaching production) | `store-server.mjs` | mitigate — CLOSED | The harness is started only for this run, binds loopback only, and is stopped in Task 3 with port release confirmed; plan 05's containment guard already asserts it never ships. |
| T-34.4.2-SC | Tampering (dependency surface) | none | mitigate — CLOSED | This plan installs nothing and changes no code — `git diff --stat -- src src-tauri/src` empty is an acceptance criterion asserted by Task 3's verify command. |
</threat_model>

<verification>
- Every item in `34.4.2-LIVE-GATE-RERUN-2.md` has a filled `Observed:` and `Verdict:`; the frontmatter carries a real verdict, run_date and items_passed.
- The `/tmp/gamelib-dev.log` transcript was captured, and its relevant `[shell]` lines are quoted in the document (or its absence is recorded as a process finding).
- Port 17940 is released.
- `git diff --stat -- src src-tauri/src` is empty for this plan.
- ROADMAP and STATE both state the same close/does-not-close conclusion as the gate document.
- Requirement checkboxes match the per-item results exactly; no box is ticked on a NOT ATTEMPTED item.
- Both prior gate documents (`34.4.2-LIVE-GATE.md`, `34.4.2-LIVE-GATE-RERUN.md`) are byte-unchanged.
</verification>

<success_criteria>
A human drove all six items on real macOS hardware with the `tauri:dev` transcript captured; items
2-6 got a measured result for the first time in this phase's history, or an honest and reasoned NOT
ATTEMPTED; the result — whatever it is — is recorded with per-item evidence, no invented root cause,
and full propagation; the phase closes only on a genuine 6/6, and anything less names the next gap
cycle explicitly without re-proposing a retired design.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-12-SUMMARY.md` when done.
Record the verdict, the per-item table, any findings, the transcript-capture status, the harness
shutdown evidence, and the close/does-not-close conclusion. State explicitly, per item, whether this
run was the FIRST time that item was ever attempted.
</output>
