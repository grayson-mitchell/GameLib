---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 21
subsystem: testing
tags: [live-gate, contract-authoring, structural-reachability-review, documentation, macos, tauri]

# Dependency graph
requires:
  - phase: 34.4.2 (plans 18, 20)
    provides: the five-test Structural Reachability Review + dual-sink evidence-capture standard (plan 18), and the two capture-integrity Section 3 bullets landed by commit db08bbfc6 (plan 20)
provides:
  - Test 6 (pre-existing external-state reachability) and Test 7 (UI-level reachability, distinct from backend-logic reachability) added to the standing live-gate-contract-authoring.md reference
  - A finding-to-test coverage map at the end of Section 2, distinguishing authoring-time tests from run-time capture-integrity rules
  - Section 1's defect tally and Section 2's heading/preamble/standing-rule updated from five to seven tests (six to eight defects)
affects: [34.4.2-22, 34.4.2-23, 34.4.2-24, 34.4.2-25, any future phase authoring a blocking live-gate contract]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Structural Reachability Review now carries seven defect-class tests instead of five; new authors of live-gate contracts must apply all seven."
    - "Coverage map convention: every measured contract-authoring defect maps to exactly one named test (Section 2) or capture rule (Section 3), never both, with the category stated explicitly."

key-files:
  created: []
  modified:
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md

key-decisions:
  - "Test 6 and Test 7 worked examples cite F-34.4.2-16 and F-34.4.2-17 by finding ID, matching the existing five-part shape (what it asks / general defect shape / worked example)."
  - "Section 1's defect tally updated to eight (six prior + F-16 + F-17); F-34.4.2-15 and F-34.4.2-18 are explicitly named as two ADDITIONAL RERUN-4 findings but excluded from the eight-count and from Section 2 entirely, since they are run-time capture-integrity defects already fixed in Section 3, not authoring-time reachability misses."
  - "Test 7's worked example corrects deferred-items.md's approximate characterisation ('the frontend disables/clears the other login buttons') after re-confirming current source: clicking a login tile calls navigate(props.loginUrl), a full React Router route change to loginweb/:runner that unmounts the entire runnerGroup container (every other tile), not a disabled/cleared button state."

patterns-established:
  - "Pattern: when a prior document's finding characterisation cannot be trusted without re-confirmation (deferred-items.md's own wording, written before this plan derived the source-level mechanism), the worked example must re-derive the citation from current source and say plainly when the earlier wording was approximate."

requirements-completed: [REQ-34.4.2-09]

# Metrics
duration: 20min
completed: 2026-08-06
---

# Phase 34.4.2 Plan 21: Test 6 + Test 7 + Coverage Map Summary

**Added two new Structural Reachability Review tests (pre-existing external-state reachability; UI-level reachability distinct from backend-logic reachability) plus a finding-to-coverage map to the standing live-gate-contract-authoring.md reference, closing the T-34.4.2-42 completeness gap RERUN-4 re-opened — without touching any gate contract, running any gate item, or changing any source file.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-08-06
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- `### Test 6 — Pre-existing external-state reachability` added to Section 2, reviewing an item's PREMISE (not its evidence) against external state a preflight cannot see (a live WKWebView cookie jar, a keychain entry, a stray process, a browser session, an OS permission grant). States the general rule: an emptied app-side store does not prove a logged-out session; every "clean state" premise needs a POSITIVE observable.
- `### Test 7 — UI-level reachability, distinct from backend-logic reachability` added to Section 2, reviewing the operator's GESTURE SEQUENCE against the shipped frontend, explicitly distinguished from Test 2 (which reasons about backend timing/modality, never about whether the UI exposes the needed controls at all).
- `### Coverage map — which test catches which measured defect` added at the end of Section 2 (before Section 3 begins), one row per measured contract-authoring defect (gate-run-2's four, F-11, F-14, F-15, F-16, F-17, F-18), each attributed to the test or Section 3 rule that now catches it and its category (authoring-time test vs. run-time capture-integrity rule). States its own limits: derived from measured defects only, nothing proves completeness, and Tests 6/7 have caught nothing yet.
- Section 1's tally raised from six to eight distinct contract-authoring defects (two new bullets for F-34.4.2-16/-17, plus an explicit note that F-34.4.2-15/-18 are additional RERUN-4 findings excluded from this count as a different category, already fixed in Section 3).
- Section 2's heading (`The seven defect-class tests`), preamble ("Apply all seven tests..."), and Section 1's standing rule ("...run this review — all seven tests...") all updated to agree with the new count; grep confirmed zero remaining literal claims of "five tests" outside a heading rename artifact.
- Section 3's two capture-integrity bullets from commit `db08bbfc6` (the exactly-ONE-app-instance rule and its preflight clause) confirmed present exactly once each — not duplicated by this plan.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add Test 6 and Test 7, and make the heading and defect tally agree with the count** - `bac0eef53` (docs)
2. **Task 2: Map every RERUN-4 finding to the test or capture rule that now catches it** - `224be7730` (docs)

**Plan metadata:** committed alongside this SUMMARY (see final commit below).

## Files Created/Modified
- `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` - Two new defect-class tests, a coverage map, and updated tally/heading/preamble/standing-rule text.

## Verbatim text of Test 6 and Test 7 as authored

(Recorded here per the plan's `<output>` instruction, since plan 34.4.2-23 must apply them literally. Full text, unmodified from the committed file.)

### Test 6 — Pre-existing external-state reachability

> **Its unit of review is the item's PREMISE, not its evidence.** Does this item's premise assume a
> fresh or clean state that some state living OUTSIDE what the contract's own preflight checks can
> silently invalidate? Name the concrete external-state surfaces this project has: a live WKWebView
> cookie jar, a system keychain entry, an already-running external process, a browser session, an
> OS-level permission grant.
>
> **General rule the finding produced: an emptied app-side store does not prove a logged-out
> session.** For every premise of the form "the user is logged out / the field is empty / no session
> exists," the contract must name a POSITIVE observable that confirms it (a rendered login form),
> never merely the absence of app-side data. Require the review to record, per item, which external
> state its premise depends on and what preflight check or in-run confirmation covers it.
>
> **Worked example (Phase 34.4.2, F-34.4.2-16):** RERUN-4's Preflight Results recorded
> `~/Library/Application Support/gamelib/humble_store/config.json`
> (`34.4.2-LIVE-GATE-RERUN-4.md`, Preflight Results section) at 2 bytes with an mtime from a prior
> session and treated it as a clean starting point. Item 4's premise — a credential-entry event to
> observe — was nevertheless false: the WKWebView cookie jar independently held a live Humble
> session that auto-completed the login with no form to paste into. Items 1(e) and 3(a) were
> invalidated by the identical mechanism in the same run. The contract had no step that cleared the
> cookie jar and no observable that would have shown the premise was already broken.

### Test 7 — UI-level reachability, distinct from backend-logic reachability

> **Its unit of review is the operator GESTURE SEQUENCE.** Can the described sequence of clicks,
> keystrokes and window interactions actually be performed against the shipped frontend as built?
>
> **Why this is not covered by Test 2:** Test 2 reasons about platform modality and backend timing
> (is the code path reachable at that moment?) and never inspects whether the UI exposes the
> controls the gesture needs. A backend path can be perfectly reachable while the frontend never
> offers the operator a way to reach it. Require the test to trace each gesture to the component
> that renders the control it names, and to record what the frontend does with that control in the
> state the item assumes — including whether navigation, conditional rendering, or a disabled state
> removes it.
>
> **General rule: a scenario whose gesture sequence the UI forbids can never PASS, no matter how
> correct the backend guard is.** The correct dispositions are to drive the path below the UI,
> restate the assertion at unit level, or withdraw the item with its reasoning recorded.
>
> **Worked example (Phase 34.4.2, F-34.4.2-17):** RERUN-4's item 5 asked the operator to click
> Amazon sign-in and then click Humble sign-in during nile's ~7-8s spawn delay, exercising the Rust
> `PENDING_VISIBLE_LOGIN_WINDOW` single-flight guard. The Structural Reachability Review's Test 2 row
> for that item verified — correctly — that the second initiation would land in the first's
> PRE-presentation window, when no sheet is up and the parent is still interactive. The item was
> still unperformable, for a reason Test 2 never asked about: clicking a login tile navigates the
> whole route away from `/login` (`Runner`'s `handleLogin()`,
> `src/frontend/screens/Login/components/Runner/index.tsx:72`, `navigate(props.loginUrl)`) to
> `loginweb/:runner` (`src/frontend/App.tsx:200-201`), unmounting the entire `runnerGroup` container
> — every OTHER runner's tile, including Humble's — that held it
> (`src/frontend/screens/Login/index.tsx:151`). There is no second store's sign-in control left
> mounted to click during the delay. **`deferred-items.md`'s own characterisation ("the frontend
> disables/clears the other login buttons") was approximate — re-confirmed at this authoring against
> current source, the mechanism is a full route navigation that unmounts the login screen outright,
> not a disabled/cleared button state.**

## Coverage map as authored (table)

| Finding | What it was | Caught by (test or Section 3 rule) | Category |
|---|---|---|---|
| Gate run 2's four single-requirement impossibilities | A `http://` origin against an `https`-only gate; a concurrency framing forbidden by AppKit sheet modality; an OAuth-only log line demanded of a non-OAuth runner | Test 1 / Test 2 / Test 3 | Authoring-time test |
| F-34.4.2-11 | A mandatory truncating `tee` colliding with a mandatory mid-run relaunch, destroying prior items' transcript evidence | Test 5 | Authoring-time test |
| F-34.4.2-14 | Three required log lines demanded of a transcript sink that structurally cannot carry sidecar-emitted output | Test 3's SINK clause | Authoring-time test |
| F-34.4.2-15 | A concurrent second `gamelib-shell` instance split the `[shell]` sink while `gamelib.log` stayed shared, so a partially-captured run looked successfully measured | Section 3's exactly-ONE-app-instance bullet | Run-time capture-integrity rule, NOT a Section 2 test |
| F-34.4.2-16 | Item 4's premise (a credential-entry event to observe) invalidated by a live WKWebView cookie jar the preflight never checked, though the app-side store it did check was correctly empty | Test 6 | Authoring-time test |
| F-34.4.2-17 | Item 5's gesture sequence was backend-reachable by every test applied to it, but the shipped frontend never offered the control the sequence needed | Test 7 | Authoring-time test |
| F-34.4.2-18 | Preflight checked for the DummyStore harness on its port but not for a pre-existing `gamelib-shell` instance | Section 3's preflight-must-check-for-a-pre-existing-instance clause | Run-time capture-integrity rule, NOT a Section 2 test |

## File:line anchors re-confirmed at this authoring

All three of Test 7's worked-example citations were re-confirmed against current source at this authoring, not copied from `deferred-items.md`'s own (now-superseded) prose:

- `src/frontend/screens/Login/index.tsx:151` — `<div className="runnerGroup">`, the single container holding all runner tiles. **Unchanged from any prior document's assumption.**
- `src/frontend/screens/Login/components/Runner/index.tsx:72` — `navigate(props.loginUrl)`, `handleLogin()`'s fallback branch (taken by every runner without a `primaryLoginAction`, which includes Humble and Amazon). **This is the actual mechanism** — a full route navigation, not a per-button disable — which is why `deferred-items.md`'s "disables/clears the other login buttons" wording is marked approximate in Test 7's worked example rather than repeated as fact.
- `src/frontend/App.tsx:200-201` — `path: 'loginweb/:runner'` routing to `screens/WebView`, confirming the navigation target is a distinct top-level route that replaces (unmounts) the entire Login screen, including `runnerGroup` and every tile inside it.

No anchor had moved since a prior document quoted it — `deferred-items.md` never cited a file:line for this mechanism in the first place, only the prose characterisation this plan replaces.

## Confirmation: Section 3's two capture-integrity rules are present and un-duplicated

- `grep -c '^- \*\*Exactly ONE app instance' live-gate-contract-authoring.md` → `1`.
- `grep -c 'preflight must check for a' live-gate-contract-authoring.md` → `1`.

Both bullets (landed by plan 20's commit `db08bbfc6`) are confirmed present exactly once each. Neither was rewritten, duplicated, or restated as a new Section 2 test — the coverage map attributes F-34.4.2-15/-18 to these Section 3 rules explicitly, in the "Run-time capture-integrity rule, NOT a Section 2 test" category.

## Count of stale "five tests" references found and fixed

Three literal references to the old five-test count existed before this plan:
1. Section 1's standing rule ("...run this review — all five tests — before publishing it.") → fixed to "seven tests".
2. Section 2's own heading ("The five defect-class tests") → fixed to "The seven defect-class tests".
3. Section 2's preamble ("Apply all five tests to every item...") → fixed to "seven tests".

A whole-file grep for `all five tests|the five defect-class tests|five tests` (excluding heading lines, which the verify command itself filters) returns zero matches after this plan's edits.

## Decisions Made
- Section 1's tally reads "eight distinct contract-authoring defects total" (six prior + F-16 + F-17), with an explicit paragraph naming F-34.4.2-15 and F-34.4.2-18 as two more RERUN-4 findings deliberately excluded from that count — they are capture-integrity defects, a different category, already fixed in Section 3. This keeps the tally honest (RERUN-4 found four things total) without collapsing the category distinction the coverage map exists to preserve.
- Test 7's worked example re-derives the UI-unreachability mechanism from current source rather than repeating `deferred-items.md`'s approximate wording, per the plan's own explicit instruction to do so if the two disagree. The re-confirmed mechanism (a full route navigation unmounting the whole login screen) is a more precise and slightly different claim than "disables/clears the other login buttons" — both support the same PASS/FAIL conclusion for item 5, so this is a correction to the mechanism's own description, not a reversal of RERUN-4's verdict.

## Deviations from Plan

None — plan executed exactly as written. No auto-fixes were needed; the only judgment call was Section 1's tally treatment of F-34.4.2-15/-18 (documented above under Decisions Made, not a deviation from the plan's own `<action>` text, which explicitly required "add the two new defect classes" — i.e. Tests 6 and 7's classes — while separately mandating the category distinction the tally note preserves).

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. This plan touches only a documentation reference file.

## Next Phase Readiness

This plan authored no gate contract, ran no gate item, launched nothing, and changed no source code (`git diff --stat -- src src-tauri/src` confirmed empty at both task commits and at plan end). Tests 6 and 7 are derived from RERUN-4's own measured findings but have not been validated by any run applying them — that validation is owed to whichever plan next authors a live-gate contract (per gap cycle 5's own plan sequence, expected to be plan 34.4.2-23, which this plan's own `<output>` instruction flags as the consumer of Test 6/Test 7's verbatim text, recorded above). Plan 34.4.2-22 is separately deriving the precise frontend mechanism in parallel per this plan's own interfaces note; if its finding differs from what this plan's Test 7 worked example states, that plan's own SUMMARY should reconcile the two, since both were authored close together against the same source.

---
*Phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field*
*Completed: 2026-08-06*

## Self-Check: PASSED

- FOUND: `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md`
- FOUND: `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-21-SUMMARY.md`
- FOUND: commit `bac0eef53` (Task 1)
- FOUND: commit `224be7730` (Task 2)
- FOUND: commit `bb7ec13f4` (this SUMMARY)
