---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 09
subsystem: infra
tags: [tauri, appkit, macos, login-window, sheet-presentation, live-gate, requirements, threat-model]

# Dependency graph
requires:
  - phase: 34.4.2 (plans 07-08)
    provides: present_login_window_as_sheet/dismiss_login_window_sheet (AppKit sheet presentation), the cancel strip + Esc backstop dismissal routes, PRESENTED_LOGIN_SHEETS registry
provides:
  - 34.4.2-LIVE-GATE-RERUN.md -- the rewritten six-item blocking gate contract for the sheet design, verdict null, items 1-2 rewritten for sheet semantics with a mandatory post-minimize/restore interactivity sub-check, items 3-6 carried over intact
  - 34.4.2-PLATFORM-SCOPE.md updated -- retired-symbol history subsection, nine new sheet/cancel-strip/Esc-monitor symbol rows with source-verified cfg gates, post-gap-cycle threat table resetting T-34.4.2-05/-07 and entering T-34.4.2-33/-34/-35
  - REQUIREMENTS.md corrected -- REQ-34.4.2-04/05/06/09 reset to [ ], each naming its specific rerun-gate-item precondition
  - deferred-items.md -- Plan 05's premature-[x] entry CLOSED; new entry for the glyph's still-unfixed un-gated cross-platform injection
affects: [34.4.2-10 (the only plan permitted to run this contract and record a result)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Author/runner separation for live gates, applied a second time after a failed run: this plan writes the contract with verdict null and is structurally forbidden from running any item in it (T-34.4.2-25)"
    - "A new mechanism does not inherit an old mechanism's fix -- T-34.4.2-05/-07 were reset from live-CONFIRMED-broken to OPEN-pending-gate rather than carried forward as though the sheet swap already proved anything"

key-files:
  created:
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md
  modified:
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
    - .planning/REQUIREMENTS.md

key-decisions:
  - "Item 1's PASS bar folds F-34.4.2-01 (the child window's post-restore unresponsive state) directly into a sub-check: type into the password field AND activate the cancel strip immediately after the Dock restore. This is the one observation the failed gate never got to make and the whole reason a rerun exists, not a generic re-test of z-order."
  - "Item 2 does not reuse the failed gate's close-button/Cmd+W wording -- a sheet has no titlebar close button. The four routes (cancel strip REQUIRED PASS, Esc REQUIRED PASS, Cmd+W RECORD ONLY, titlebar NOT A ROUTE) are enumerated fresh, each explicitly labelled so plan 10 cannot mis-score an unscoreable route."
  - "Items 3-6 carry over with additive sub-checks only (Esc-monitor-doesn't-swallow-Cmd+V in item 4; cancel-strip-survives-kill-switch in item 5; no-cancel-strip-on-hidden-or-Epic-windows in item 6), never a substance rewrite -- the four threats they discharge (T-34.4.2-17/-21/-22, -15 residual) were never attempted and stay exactly as unmeasured as the failed gate left them."
  - "T-34.4.2-05 and T-34.4.2-07 are explicitly RESET to OPEN-pending-gate rather than left at their post-gate 'CONFIRMED broken' disposition -- the child-window mechanism they were confirmed broken against no longer exists in this codebase, and describing the sheet mechanism as anything but unproven would be an unearned claim (34.4.2-PLATFORM-SCOPE.md's own repeated 'a new mechanism does not inherit a fix' rule)."

requirements-completed: []  # No requirement is newly completed by this docs-only plan -- REQ-34.4.2-04/05/06/09 stay [ ], each now naming the rerun gate item that will complete it; REQ-34.4.2-08 was already [x] before this plan and is unaffected in checkbox state (a clarifying note was added, not a completion).

# Metrics
duration: ~40min
completed: 2026-08-04
---

# Phase 34.4.2 Plan 09: Rewritten Live-Gate Contract + Platform/Requirement Reconciliation Summary

**Wrote a rewritten six-item blocking live-gate contract (`34.4.2-LIVE-GATE-RERUN.md`, verdict null) that tests AppKit sheet semantics instead of the retired child-window mechanism, folding F-34.4.2-01's exact failure mode into item 1's own PASS bar; reconciled the platform-scope record and every REQ-34.4.2 checkbox to match what plans 07-08 actually proved, not what a failed gate or a stale precondition once implied.**

## Performance

- **Duration:** ~40 min (session start not separately timestamped; estimated from commit sequence)
- **Completed:** 2026-08-04
- **Tasks:** 3/3 completed
- **Files modified:** 4 (1 created: `34.4.2-LIVE-GATE-RERUN.md`; 3 modified: `34.4.2-PLATFORM-SCOPE.md`, `deferred-items.md`, `REQUIREMENTS.md`)

## Accomplishments

- **`34.4.2-LIVE-GATE-RERUN.md`** created: six items, `verdict: null`, `run_date: null`, `items_passed: null`, `items_total: 6`, `blocking: true`. Items 1-2 rewritten entirely for AppKit sheet semantics (`present_login_window_as_sheet`/`dismiss_login_window_sheet`, not `addChildWindow:ordered:`/`removeChildWindow:`); item 1's sub-check (e) — type into the password field then activate the cancel strip, immediately after the Dock restore — is F-34.4.2-01 turned directly into a PASS condition. Item 2 enumerates four sheet-appropriate dismissal routes (cancel strip REQUIRED PASS, bare Esc REQUIRED PASS, Cmd+W RECORD ONLY, titlebar close NOT A ROUTE) rather than reusing the failed contract's close-button wording, which does not apply to a sheet at all. Items 3-6 carried over substantially unchanged, each gaining exactly the additive sub-check the plan specified (Esc-monitor/Cmd+V non-interference in item 4; cancel-strip-survives-`GAMELIB_AUTOFILL_GLYPH=0` in item 5; no-cancel-strip on hidden/Epic windows in item 6) — every `Observed:`/`Verdict:` field left blank.
- **`34.4.2-PLATFORM-SCOPE.md` § 1** updated: the four retired child-window symbols (`attach_login_window_as_child`, `detach_login_window_from_parent`, `ATTACHED_LOGIN_CHILDREN`, the deminiaturize observer) removed from the live per-symbol table and preserved instead in a new **RETIRED** history subsection naming the retiring plan and the binding decision. Nine new rows added for the sheet/cancel-strip/Esc-monitor symbols, each cfg gate read directly from the current `src-tauri/src/main.rs` this session (not assumed) — including the source-verified finding that `login_cancel_strip_script` itself is as ungated as `autofill_glyph_script`, but its CALL SITE (`main.rs:3668`) is deliberately macOS-gated, unlike the glyph's, which plan 08 left untouched on purpose.
- **`34.4.2-PLATFORM-SCOPE.md` § 5** gained a post-gap-cycle threat table (append-and-supersede, all prior tables retained): T-34.4.2-05 and T-34.4.2-07 explicitly RESET from "live-CONFIRMED broken" to "OPEN-pending-gate against the NEW mechanism"; T-34.4.2-33/-34/-35 (carried from plan 08's own threat register) entered for the first time, each mapped to a discharging gate item; T-34.4.2-15 residual/-17/-21/-22 recorded as unchanged (still NOT ATTEMPTED, no new evidence); T-34.4.2-32 recorded unchanged-CLOSED with its symbol array extended twice this cycle; T-34.4.2-SC CLOSED (no install across plans 07-09, both lockfiles byte-identical, confirmed this session).
- **`34.4.2-PLATFORM-SCOPE.md` § 4** gained a gap-cycle re-run subsection: `git diff --stat fc38d229c^..95f631fe3 -- src/backend src/frontend src/common` shows only `tauriShellSource.test.ts` changed (zero runtime source), and both lockfile `git diff --exit-code` checks exit 0 — real, measured output quoted verbatim, not asserted.
- **`REQUIREMENTS.md`** corrected: REQ-34.4.2-04, -05, -06 and -09 reset to `[ ]`, each naming the specific item in `34.4.2-LIVE-GATE-RERUN.md` it now depends on (04: items 3 and 5; 05: item 3; 06: item 4; 09: the rerun contract as a whole, run by plan 10). REQ-34.4.2-08 gained a clarifying note about the cancel-strip-vs-glyph gating asymmetry without a checkbox change. REQ-34.4.2-01/-02/-03 verified **byte-identical** to plan 07's own output via `git diff` (confirmed no accidental re-edit). All ten REQ-34.4.2 IDs remain present exactly once as row headers — none renumbered or deleted.
- **`deferred-items.md`** updated: the Plan 05 entry about premature `[x]` marks marked **CLOSED 2026-08-04 by plan 09, Task 3**, naming exactly which rows were corrected and which sub-question (REQ-34.4.2-07's own earlier-than-its-artifact checkbox) was deliberately left open as a distinct, narrower question outside this plan's scope. The `helperProcess.test.ts` and `seam-parity-sweep.py` entries remain untouched (still OPEN). A new entry records the autofill glyph's still-unfixed un-gated cross-platform injection, now contrasted against plan 08's deliberately macOS-gated cancel strip.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the rewritten six-item blocking gate contract with verdict null** - `da95c5d59` (docs)
2. **Task 2: Update the platform-scope record and threat roll-up for the sheet design** - `65be7059c` (docs)
3. **Task 3: Correct the premature requirement checkmarks and close the carried-forward deferred entry** - `d7e929469` (docs)

_This SUMMARY.md and the STATE.md/ROADMAP.md updates are committed together as the final step below._

## Files Created/Modified

- `.planning/phases/34.4.2-.../34.4.2-LIVE-GATE-RERUN.md` (created) - the rewritten six-item blocking live-gate contract, verdict null, items 1-2 for sheet semantics, items 3-6 carried over
- `.planning/phases/34.4.2-.../34.4.2-PLATFORM-SCOPE.md` (modified) - retired-symbol history subsection, nine new symbol rows with source-verified cfg gates, updated cross-platform-injection finding, post-gap-cycle threat table, gap-cycle Electron-unchanged re-run evidence
- `.planning/phases/34.4.2-.../deferred-items.md` (modified) - Plan 05 premature-`[x]` entry CLOSED; new entry for the glyph's un-gated injection
- `.planning/REQUIREMENTS.md` (modified) - REQ-34.4.2-04/05/06/09 checkboxes corrected to `[ ]` with rerun-item preconditions named; REQ-34.4.2-08 gained a clarifying note; phase-foot provenance note updated to record the gap cycle

## Decisions Made

- Item 1's interactivity sub-check (type + cancel strip, immediately after Dock restore) is written as a PASS condition, not a separate finding-hunting step -- it is F-34.4.2-01 made falsifiable, matching the whole reason this rerun exists.
- Item 2's four routes are explicitly labelled REQUIRED PASS / RECORD ONLY / NOT A ROUTE so plan 10 cannot accidentally score an unscoreable route (Cmd+W's delivery to a sheet is genuinely unknown to this project; the titlebar route does not exist for a sheet at all).
- T-34.4.2-05/-07 are RESET rather than left at "CONFIRMED broken" -- the mechanism that was confirmed broken no longer exists, and carrying forward a "broken" label against a mechanism that never ran would be exactly the kind of unearned claim T-34.4.2-26 (green-suite-mistaken-for-parity) warns against in a different form.
- REQ-34.4.2-07's own earlier-than-its-artifact checkbox timing question (distinct from the live-proof mismatches this plan's scope covers) was deliberately left open rather than investigated further -- it is a narrower, different question than the one this plan's `files_modified` and task text scope it to answer.

## Deviations from Plan

None - plan executed exactly as written. All three tasks' acceptance criteria were verified via the exact `grep -c`/`grep -n` commands the plan specified (or a stronger equivalent, noted below) before each commit.

**One verification-method refinement (not a deviation in outcome):** Task 3's acceptance criterion "`grep -c 'REQ-34.4.2-'` returns the same count as before this task" is a loose proxy -- adding cross-referencing prose that mentions other REQ IDs (e.g. REQ-34.4.2-04's row now also cites item 5, which is titled with REQ-34.4.2-04 in the gate contract) legitimately changes the raw occurrence count without renumbering or deleting anything. I verified the actual invariant the criterion protects -- no ID added, deleted, or renumbered -- with a stronger check: `grep -o '**REQ-34.4.2-[0-9]*\*\*' .planning/REQUIREMENTS.md | sort -u` returns exactly ten unique IDs, REQ-34.4.2-01 through -10, each present exactly once as a row header. Recorded here because the letter of the plan's own grep command would have produced a false negative.

## Issues Encountered

None beyond the verification-method note above.

## User Setup Required

None - no external service configuration required. This plan wrote planning documents only.

## Threat Flags

None. This plan introduces no new code, network endpoint, auth path, file access pattern, or schema change -- it only reconciles planning documents against work already built and reviewed by plans 07-08. The three threats it newly *records* (T-34.4.2-33/-34/-35) originate in plan 08's own threat register, not from anything introduced here.

## Known Stubs

None. No UI or data-flow code was created or modified by this plan.

## DECLARED UNVERIFIED (per this plan's own `<output>` spec)

This plan ran **NO** gate item, started **NO** harness, and left every result field (`verdict`,
`run_date`, `items_passed`, every item's `Observed:`/`Verdict:`) **null/blank**. Every claim this
gap cycle makes about the sheet mechanism's live behaviour (sheet z-order, post-restore
interactivity, both dismissal routes, the AutoFill menu, Cmd+V, the kill switch, hidden-window and
Epic non-interference) remains **entirely unproven on real hardware** until plan 34.4.2-10 runs
`34.4.2-LIVE-GATE-RERUN.md` and records a measured result. **Phase 34.4.2 remains NOT CLOSED.**

## Next Phase Readiness

- The rewritten contract is ready for plan 34.4.2-10's preflight + operator-driven run. No blockers:
  plans 07-08 land the mechanism this contract tests, and this plan's own `git diff --stat -- src
  src-tauri/src` is empty (confirmed below), so nothing about the code changed underneath the
  contract between plan 08 and this plan.
- Plan 10 should start from this contract's Preconditions section, filling each precondition's
  satisfaction status from a real preflight check (mirroring plan 06's own preflight discipline for
  the failed gate) before any operator involvement.

## Self-Check: PASSED

- FOUND: `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md`
- FOUND: `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md`
- FOUND: `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md`
- FOUND: `.planning/REQUIREMENTS.md`
- FOUND: `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-09-SUMMARY.md`
- FOUND commit: `da95c5d59` (Task 1)
- FOUND commit: `65be7059c` (Task 2)
- FOUND commit: `d7e929469` (Task 3)
- CONFIRMED: `git diff --stat da95c5d59^..d7e929469 -- src src-tauri/src` is EMPTY -- this plan touched no code.
- CONFIRMED: `lsof -nP -iTCP:17940 -sTCP:LISTEN` returns no output -- no harness process was started by this plan.

---
*Phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field*
*Completed: 2026-08-04*
