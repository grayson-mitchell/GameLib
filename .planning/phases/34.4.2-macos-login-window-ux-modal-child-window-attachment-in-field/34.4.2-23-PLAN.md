---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 23
type: execute
wave: 2
depends_on: ["34.4.2-21", "34.4.2-22"]
gap_closure: true
files_modified:
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-5.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REQ-34.4.2-01, REQ-34.4.2-02, REQ-34.4.2-03, REQ-34.4.2-04, REQ-34.4.2-05, REQ-34.4.2-06, REQ-34.4.2-07, REQ-34.4.2-08, REQ-34.4.2-09, REQ-34.4.2-10]

must_haves:
  truths:
    - "A fresh blocking gate contract exists with verdict null, authored by a plan forbidden from running it"
    - "The contract asserts exactly one gamelib-shell instance per launch and aborts the launch if that ever fails, so a split evidence sink can never again masquerade as a measured item"
    - "An unscored preparatory disconnect clears the WKWebView session before any item whose premise requires a login form, and a POSITIVE observable confirms it worked"
    - "Item 5 is withdrawn with its full reasoning recorded in place, never silently deleted, and its threat coverage is restated honestly"
    - "Item 6(b)'s absences are contrasted against the same lines genuinely firing for another window in the same captured launch"
    - "Every item, sub-check, precondition and the evidence-capture instruction itself has been checked against all SEVEN defect-class tests before any operator evening is spent"
  artifacts:
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-5.md"
      provides: "The seventh blocking gate contract, results unwritten, carrying its own seven-test Structural Reachability Review"
      contains: "verdict: null"
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md"
      provides: "A twelfth §5 update recording D-G1/D-G2/D-G3 and remapping every open threat to the new contract"
      contains: "Twelfth update"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.4.2-09's row naming RERUN-5 as the contract of record, with no box ticked"
      contains: "34.4.2-LIVE-GATE-RERUN-5.md"
  key_links:
    - from: "34.4.2-LIVE-GATE-RERUN-5.md Evidence-capture instruction"
      to: "the single-instance capture-integrity rule in references/live-gate-contract-authoring.md"
      via: "a pre-launch pgrep assertion, a PID recorded at launch and teardown, and an abort-and-re-run rule"
      pattern: "gamelib-shell"
    - from: "34.4.2-LIVE-GATE-RERUN-5.md preparatory disconnect"
      to: "item 4, item 1(e) and item 3(a)'s shared login-form premise"
      via: "an unscored step ordered before them, confirmed by a rendered login form rather than an emptied store"
      pattern: "preparatory"
    - from: "34.4.2-LIVE-GATE-RERUN-5.md Structural Reachability Review"
      to: "all seven defect-class tests"
      via: "a per-row REACHABLE/IMPOSSIBLE/CONDITIONAL verdict with cited evidence, plus the pairwise pass and the two new passes"
      pattern: "Test 7"
---

<objective>
Author `34.4.2-LIVE-GATE-RERUN-5.md` — the seventh blocking live-gate contract in this phase's
history, and the first whose items can all actually be reached.

**This plan is FORBIDDEN from running any gate item, starting any harness, launching
`npm run tauri:dev`, or writing any value into `verdict` / `run_date` / `items_passed` / any
`Observed:` or `Verdict:` field.** Authoring and running are separate plans by binding decision
**D-E** — the same separation plans 05/06, 09/10, 11/12, 15/16 and 19/20 used, which is what makes
every recorded FAIL in this phase trustworthy rather than back-filled from expectation
(T-34.4.2-25). **Plan 34.4.2-24 is the only writer of results.**

**`34.4.2-LIVE-GATE-RERUN-4.md` stays byte-unchanged.** It is the historical record of the run that
made this rewrite necessary (verdict FAIL, `items_passed: 1/6`, 2026-08-06). Do not edit it.

**What RERUN-4 measured, and why a bare re-run would fail again.** Only item 6(b) PASSED — the
first-ever measured result for it in this phase's history. Items 1, 2, 3, 4 and 6(a) each failed to
reach their own bar, and item 5 was found structurally UNREACHABLE. Three of those outcomes are
**contract defects, not app bugs**:

1. **F-34.4.2-15 (F-A).** A concurrent second `gamelib-shell` process split the `[shell]` stderr sink
   away from the capture while `gamelib.log` stayed SHARED — producing a populated, plausible-looking
   log next to a transcript missing every scored `[shell]` literal. Items 1 and 2 lost their decisive
   evidence to this and have no other reason to fail.
2. **F-34.4.2-16 (F-B).** A live WKWebView Humble session auto-completed the login, so items 1(e),
   3(a) and 4 had no login form to act on. The contract checked the app-side store
   (`humble_store/config.json`, 2 bytes) and wrongly read that as logged out.
3. **F-34.4.2-17 (F-C).** Item 5's scenario cannot be driven from the shipped frontend at all.

**Three binding operator decisions govern this authoring. Cite each by ID in the section it governs.**

- **D-G1 — item 5 is WITHDRAWN from the live gate.** Its scenario is structurally unperformable. The
  withdrawal is recorded in place with its full reasoning, never silently deleted. Coverage is
  preserved by two layers: plan 14's mutation-proven guard-ordering unit test (unchanged), and plan
  34.4.2-22's new frontend pin. T-34.4.2-39/-41's discharge basis must be restated honestly as
  **unit-proven + UI-pinned, NOT live-discharged.**
- **D-G2 — an UNSCORED PREPARATORY DISCONNECT runs before the measured sequence**, purely to clear
  the WKWebView cookie jar so the login-form premise is true. It is NOT item 6(a) and scores nothing;
  item 6(a) remains a separate, later, scored item whose own before/after filesystem proof stays
  intact. If the preparatory step itself wedges, that IS a finding (an F-34.4.2-12 regression) and
  must be captured with `sample <pid>` before any force-kill, even though the step is unscored.
- **D-G3 — ALL items are re-measured; nothing is inherited, one fresh verdict.** Item 6(b)'s RERUN-4
  PASS is historical record, not evidence for this run. This continues the standing D-F1 rule.

Output: a contract with `verdict: null`, its seven-test review recorded inside it, an evidence-capture
instruction that asserts a single app instance, a launch plan whose ordering has been checked
pairwise, REQUIREMENTS.md naming it contract of record with no box ticked, and a twelfth §5 update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
@.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md

**Tooling rule for this repo (mandatory).** `graphify-out/graph.json` exists. Run
`graphify query "<question>"` / `graphify explain "<concept>"` before grepping or reading raw source.
**Every log literal this contract requires must be re-grepped against CURRENT source at authoring
time** and the line number you actually observe recorded — never the one RERUN-4 quoted.

<interfaces>
<!-- The contract's building blocks. Every literal's TEXT below is what RERUN-4 verified; every
     line number is a starting point to re-confirm, not a fact to copy. -->

**Rust `[shell]` literals → SESSION TRANSCRIPT sink only:**
- `[shell] login-window sheet: read-back attached={attached} for '{label}'` (was `main.rs:2436`)
- `[shell] humble_login_open: presentation requested visible=true width=900 height=700 center=true focus_once=true persistent_pin=false light_theme_requested=true sheet_presented={sheet_presented}` (was `main.rs:3698`)
- `[shell] login-sheet: cancel requested for '{label}' via strip` / `... via esc` (was `main.rs:1486`)
- `[shell] humble_login_open: REFUSED visible login window ...` (was `main.rs:3382`) — **item 5 is
  withdrawn; this literal may appear ONLY in the withdrawal's reasoning, never as a required positive.**
- `[shell] humble_login_open: visible login window '{label}' armed as the single pending login flow (T-34.4.2-39)` (was `main.rs:3388`)
- `[shell] WARN: login-window dismiss: '{label}' main-thread dispatch failed ...` / `... main-thread hop unconfirmed within 10s ...` (was `main.rs:2579` / `:2589`), record-only (WR-01)
- `[shell] humble_login_open: pristine WKWebView built for '{label}' (zero Tauri injection ...)` —
  observed in RERUN-4's launch-1 transcript; useful as a window-label anchor.

**MUST-NOT-APPEAR literals (their absence is a PASS signal):**
- `[shell] login-window: parent deminiaturized, re-raising {N} attached child window(s)` — observer
  deleted outright by plan 07 Task 2.
- `post_autofill_right_click`, `autofill glyph injected for`, `autofill glyph injection SKIPPED`,
  `GAMELIB_AUTOFILL_GLYPH` — deleted by plan 13 (D-A). Confirm each returns 0 from a
  comment-filtered `grep -c` against current `src-tauri/src/main.rs` at authoring time.

**Sidecar `logInfo`/`logWarning` literals → `~/Library/Logs/GameLib/gamelib.log` sink ONLY:**
- `Humble sync finished: gamekeys={N} fetched={M}/{total}` — `src/backend/humble/library.ts:954`
- `Humble disconnect: cookie census before(total={N}, matched={M}) after(total={N2}, matched={M2}) deleted={D} survivingNonHumble={S}` — `src/backend/humble/user.ts:1049`
- `Humble login-window cookie read UNSUPPORTED_OR_ERROR for window {seamLabel} — aborting watch` — `src/backend/humble/user.ts:461`
All three carry **aggregate counts only** — no gamekey, order identifier, cookie value, email or
account id may be transcribed into the contract or any `Observed:` field.

**Structural constraints that killed sub-checks in prior contracts:**
- `login_window_url_arg` (was `main.rs:926`) rejects any scheme that is not exactly `https` before
  any OS window is built. The DummyStore harness is therefore unreachable from a Tauri-managed login
  sheet and is NOT started (T-34.4.2-27; REQ-34.4.2-07 also forbids building or reading any
  credential store). Record its removal with the reason so a future author does not re-add it.
- A presented AppKit sheet is application-modal to its parent, and every login trigger lives inside
  the single `main` window.
- `status=cancelled reason=window-closed` is emitted by `oauthLoginCapture.ts:236`, gated to
  `OAuthRunner = 'legendary' | 'gog' | 'nile' | 'zoom'`. **Humble is not an OAuth runner and
  structurally cannot emit it.** Never require it of Humble.

**A REACHABILITY HAZARD for D-G2's preparatory disconnect — resolve it at authoring time, do not
assume it away.** `Runner` renders its Logout/disconnect control only inside its `props.isLoggedIn`
branch (`src/frontend/screens/Login/components/Runner/index.tsx`, ~L100-130), and the Humble tile's
`isLoggedIn` derives from `humble?.isLoggedIn` (`src/frontend/screens/Login/index.tsx` ~L67-69,
~L235), which is fed from the app-side store (`GlobalState.tsx` ~L269,
`humbleConfigStore.get_nodefault('isLoggedIn')`), whose backing file was measured at **2 bytes** in
RERUN-4's own preflight. **If the app-side store is empty, the disconnect control may not be rendered
at all** — precisely the Test 6 (pre-existing external-state reachability) class this cycle just
added. A named fallback that uses only shipped UI, if that is what you confirm:
open the Humble login window once → it auto-logs in from the live WKWebView cookie (repopulating the
store and making the disconnect control appear) → click disconnect → then confirm a real login form
renders on the next Humble window open. Record whichever path you confirm, with the evidence.

**F-34.4.2-10 (Gap 7) — the still-open bounded-timeout debt** in `clearHumbleStorage`
(`src/backend/humble/user.ts:919-1116`). Untouched by `6bad86227` and by plan 17. Never reached
live: execution deadlocked strictly before it in the run that produced the original wedge. This plan
must record an EXPLICIT disposition for it (see Task 3) — never omit it.

**Document shape to mirror** (`34.4.2-LIVE-GATE-RERUN-4.md`): frontmatter with `verdict: null`,
`run_date: null`, `items_passed: null`; then `## What changed since gate run 4`,
`## Evidence-capture instruction`, `## Preconditions`, `## Structural Reachability Review`,
`## The items`, `## Findings recorded this run` (empty at authoring), `## ITEM VERDICT SUMMARY`,
`## Harness teardown`, `## What a PASS does and does not close`, `## Threat-to-item discharge table`.
The Review goes BEFORE the items so a runner meets it first.

**Binding decisions to encode, never revisit:** D-A (the in-field autofill affordance is DELETED;
Cmd+V / Edit ▸ Paste is the sole credential-entry route), D-C (AppKit sheet presentation is
permanent; child-window attachment is off the table forever), D-D / REQ-34.4.2-10 (the pristine Epic
surface is byte-untouched; 6(b) is a read-only ABSENCE check and nothing more), D-08 (no partial
pass — one item's FAIL fails the whole gate), D-09 (non-macOS unchanged), D-E (author ≠ runner),
D-G1/D-G2/D-G3 (this cycle's three, above).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 34.4.2-LIVE-GATE-RERUN-5.md — a capture that cannot split, a premise that is actually true, and an item set that can all be reached</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-5.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-4.md` IN FULL — the document being succeeded: its
      Evidence-capture instruction, its Preconditions (including the REMOVED-with-reasons list), its
      launch plan, every item's `Observed:`/`Verdict:`, its `## Findings recorded this run`
      (F-34.4.2-15/-16/-17/-18 and the T-34.4.2-42 scorecard), and its ITEM VERDICT SUMMARY.
      **Do not edit it.**
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` as plan
      34.4.2-21 left it — Section 3's dual-sink standard INCLUDING the exactly-ONE-app-instance
      bullet and the preflight clause, and Section 2's seven tests. Implement Section 3 literally;
      this contract is the first required to carry the single-instance rule.
    - `.planning/phases/.../34.4.2-22-SUMMARY.md` — plan 22's derived mechanism for why item 5's
      scenario is unperformable, and the exact wording it recommends. Item 5's withdrawal reasoning
      must use that wording, not the approximate "disables/clears the other login buttons" phrasing
      if plan 22 corrected it.
    - `.planning/debug/resolved/humble-disconnect-main-wedge.md` — `## Resolution` (what item 6(a)
      discharges, and the `humble_store/config.json` functional proof) and `## Residual Risks`.
    - `.planning/phases/.../34.4.2-19-PLAN.md` and `.../34.4.2-19-SUMMARY.md` — the authoring shape
      this plan mirrors and the verbatim capture block RERUN-4 used, so this contract's own block is
      a deliberate revision of it rather than an independent reinvention.
  </read_first>
  <action>
    Create `34.4.2-LIVE-GATE-RERUN-5.md`. Frontmatter: `phase`, `plan: 23`, `type: live-gate`,
    `status: pending`, `blocking: true`, `created: <today>`, `verdict: null`, `run_date: null`,
    `items_total: 5`, `items_passed: null`, and a `notes:` block stating it was authored by a plan
    forbidden from running it (D-E), that plan 34.4.2-24 is the only writer of results, and that it
    succeeds `-RERUN-4.md` without erasing that document's own FAIL 1/6 record.

    **`items_total: 5`, and item numbers are NOT renumbered.** Item 5 is withdrawn (D-G1), leaving
    items 1, 2, 3, 4 and 6 (with sub-parts 6(a) and 6(b)). Keep the existing numbering so
    cross-references across seven gate documents stay stable; item 5 appears as a `### Item 5 —
    WITHDRAWN` section in its numeric position, carrying its reasoning.

    **`## What changed since gate run 4`.** State: no runtime source change landed this cycle beyond
    what RERUN-4 already ran against (confirm this yourself with `git log` and record what you find —
    if a runtime change DID land, say so, because D-G3 then matters more, not less). State the
    process changes: two new defect-class tests (plan 21), a frontend UI pin (plan 22), and the
    single-instance capture-integrity rule now implemented for the first time. Then state **D-G3** in
    the document's own voice: all items are re-measured, nothing inherited — item 6(b)'s RERUN-4 PASS
    is historical record, not evidence for this run. Restate the phase's standing finding: every
    blocking defect across Phases 34.4.1 and 34.4.2 was found by a human driving real hardware while
    `cargo test` and `npm run test:ci` were green.

    **`## Evidence-capture instruction`.** Carry forward RERUN-4's instruction, which WORKED — the
    UTC-timestamped session directory, `tee -a` (never bare `tee`, never `>`), the exact per-launch
    delimiter text, per-launch `gamelib.log` archiving before the next launch's first write (cite
    `src/backend/logger/log_writer.ts`'s `renameSync` rotation as the reason), per-item launch-ordinal
    recording, the closing inventory, and the explicit sink partition with **every required literal
    annotated `(session transcript)` or `(gamelib.log)`**. Resolve and quote the current
    `ls -la ~/Library/Logs/GameLib/` output rather than guessing filenames.

    **ADD, for the first time, the single-instance capture-integrity assertions (F-34.4.2-15/F-A,
    F-34.4.2-18/F-D), as literal copy-able commands:**
    - **Before every launch:** assert zero pre-existing instances —
      `pgrep -f 'target/debug/gamelib-shell'` must return nothing. State that a non-empty result means
      STOP and resolve it before launching, not "proceed and hope".
    - **After the window appears:** record the single PID into the transcript, and state the exact
      command that does so, so the PID lands in the captured file rather than only on screen.
    - **At teardown:** record it again.
    - **The abort rule, stated as a rule and not a suggestion:** if the instance count is ever not
      exactly 1 at any of those three points, the launch is ABORTED and re-run, and its evidence is
      **not scorable** — nothing from an aborted attempt is archived or cited. Note that RERUN-4
      already aborted and cleanly re-ran two launches for exactly these reasons and that the practice
      is being promoted from improvisation to contract.
    - State plainly WHY: a second instance splits the `[shell]` sink while `gamelib.log` stays
      SHARED, producing the most dangerous evidence shape this standard can make — a populated log
      that looks like proof next to a transcript missing every scored literal.

    **`## Preconditions`.** Carry RERUN-4's, each labelled OPERATOR-AT-GATE-TIME or
    machine-satisfiable-in-preflight, with satisfaction status left BLANK for plan 24's preflight.
    **Add two:**
    - **No pre-existing `gamelib-shell` instance** (machine-satisfiable — `pgrep`, F-34.4.2-18).
    - **A WKWebView Humble session that is either already clear, or clearable by the preparatory
      step below** (see D-G2). State that this precondition is NOT satisfied by an emptied
      `humble_store/config.json` — that exact inference is what F-34.4.2-16 recorded as insufficient.
    Carry forward RERUN-4's REMOVED-with-reasons list verbatim in substance (the DummyStore harness,
    the seeded Passwords entry, the `GAMELIB_AUTOFILL_GLYPH` default arm).

    **`## Preparatory sequence (UNSCORED) — D-G2`.** A section of its own, before `## The items`,
    stating unambiguously that it scores nothing and is not item 6(a):
    - Click Humble's disconnect control ONCE, to clear the WKWebView cookie jar.
    - **Resolve the reachability hazard named in this plan's interfaces block first:** confirm at
      authoring time whether the disconnect control is even rendered when the app-side store is empty.
      If it is not, write the fallback sequence into the contract (open the Humble login window so it
      auto-logs in and repopulates the store, making the control appear; then disconnect). Record
      which path you confirmed and the source evidence for it.
    - **Require a POSITIVE observable that the jar is actually clear before any item that depends on
      it begins: a logged-out Humble page presenting a real login form.** State explicitly that an
      emptied `humble_store/config.json` alone is NOT sufficient — that is precisely the insufficiency
      F-34.4.2-16 recorded.
    - **State that a wedge here IS a finding** — an F-34.4.2-12 regression — and that
      `pgrep -f gamelib-shell` then `sample <pid> 5` must be captured and saved under
      `.planning/debug/evidence/` BEFORE any force-kill, unscored step or not.
    - State which items depend on this step: **item 1(e), item 3(a) and item 4** all require a real
      login form. RERUN-4 recorded all three as unmeasurable for the same reason; do not scope this
      step to item 4 alone.

    **`## The items`** — author each with a measurable PASS bar and sink-annotated decisive literals.
    Base them on RERUN-4's, with these mandatory changes:

    - **Item 6(b) — runs FIRST, in launch 1, and now WITH an in-session positive control (D-G3).**
      RERUN-4's own caveat #1 recorded that its PASS had no same-session confirmation that the three
      absent lines fire correctly for other windows. Fix that inside launch 1: open Epic's login
      window FIRST and record the absences, then — in the SAME launch — open one non-Epic
      Tauri-managed login window as an UNSCORED positive control and confirm those same literals DO
      fire for that label. **Consequence the contract must state: 6(b)'s check becomes
      label-attributed, not a raw zero-count** — zero occurrences attributable to Epic's window label,
      alongside at least one occurrence attributable to the control window's label, in the same
      transcript segment. Name which store the control uses and justify the choice in the review
      (Test 5): it must not alter Humble session state in a way the preparatory step or item 6(a)
      depends on. Keep RERUN-4's binding clause: a later item's failure cannot retroactively
      un-measure launch 1. Keep the D-D framing: an ABSENCE check only, never a statement that Epic's
      login UX works.
    - **Item 1** (REQ-01/-02): unchanged in substance — sub-checks (a)-(e), both required transcript
      lines in order (`read-back attached=` before `presentation requested ... sheet_presented=`), the
      retired re-raise line absent across ALL launches. Note in the item's own text that (e)'s
      premise (an empty password field) is supplied by the preparatory sequence, and that this is why
      it was unmeasurable in RERUN-4.
    - **Item 2** (REQ-03): unchanged — routes (a) strip and (b) bare Esc both REQUIRED with their own
      `via strip` / `via esc` transcript lines, the exactly-one-cancel-control sub-check, the
      main-window-interactive-after-dismissal sub-check, (c) Cmd+W and (d) titlebar-absence
      record-only, WR-01 record-only. Humble's cancellation signal is the `UNSUPPORTED_OR_ERROR`
      line — **gamelib.log** sink. Never require `status=cancelled reason=window-closed` of Humble.
      Note that RERUN-4's FAIL on this item was a capture-integrity loss (F-A), not an observed
      defect, and that the single-instance rule is what is expected to change the outcome.
    - **Item 3** (REQ-04/-05): (a) visual absence — now premised on a rendered login form; (b) zero
      occurrences of the three deleted literals across ALL launches in the session transcript;
      (c) the `GAMELIB_AUTOFILL_GLYPH=0` relaunch changes nothing — **note in the item's text that
      (c) has now never run across seven gate attempts**; (d) record-only real right-click.
    - **Item 4** (REQ-06): Cmd+V and Edit ▸ Paste, the Esc-monitor sub-check, and the raised bar that
      a real Humble login must COMPLETE using paste alone, evidenced by the aggregate
      `Humble sync finished: gamekeys={N} fetched={M}/{total}` line — **gamelib.log** sink, aggregate
      counts only. Its `gamelib.log` MUST be archived before item 6(a)'s launch begins.
    - **`### Item 5 — WITHDRAWN (D-G1)`**, in its numeric position, scoring nothing and counting
      toward neither `items_total` nor `items_passed`. Record: what the item used to ask; the measured
      finding that withdrew it (F-34.4.2-17, RERUN-4); **the mechanism as plan 34.4.2-22 derived it
      from source**, in that plan's own recommended wording; the two coverage layers that remain
      (plan 14's mutation-proven guard-ordering test, plan 22's frontend pin); and — stated plainly —
      that T-34.4.2-39/-41 are therefore **unit-proven + UI-pinned, NOT live-discharged**, and will
      not be live-discharged by this gate. State that withdrawal is not deletion: a future contract
      that wants a live discharge must drive the refusal below the UI, and the reasoning is preserved
      here so that decision is made deliberately rather than by rediscovery.
    - **Item 6(a)** (REQ-04, T-34.4.2-43): the Humble disconnect route, in its own launch, AFTER item
      4. Required absences (no cancel-strip line, no sheet-presentation line for the hidden window's
      label, NOT refused by the single-flight guard). Both positive channels required: the
      **gamelib.log** cookie-census aggregate line, AND the independent `humble_store/config.json`
      size+mtime filesystem proof. **State the ordering guarantee D-G2 buys explicitly:** the
      preparatory disconnect empties the store early, item 4's completed login repopulates it past 2
      bytes, and item 6(a)'s disconnect returns it to 2 bytes with a NEW mtime — so the before/after
      proof is genuinely three-point and is NOT destroyed by the preparatory step, which a bare
      reorder would have cost. Keep the `sample <pid>` before force-kill rule.
      **Add ONE record-only sub-check for Gap 7 / F-34.4.2-10:** determine at authoring time whether
      `clearHumbleStorage` (`src/backend/humble/user.ts:919-1116`) emits any observable line on entry
      or completion. If it does, require it RECORD-ONLY, so this run finally establishes whether that
      code path is reached at all. **If it emits nothing, say so explicitly and record that no
      observable exists** — do not invent one, and do not let its absence be mistaken for the path not
      running (Section 4 rule (i): never use a transcript's silence to prove a backend path did not
      run). This does not gate PASS/FAIL either way.
    - **Item 6 scoring stays D-08:** item 6 is ONE item; both 6(a) and 6(b) must satisfy their own
      conditions for it to PASS.

    **`## Findings recorded this run`** — present but empty, noting plan 34.4.2-24 is its only writer.
    **`## ITEM VERDICT SUMMARY`** — the five-row table with every Verdict cell EMPTY, plus a
    withdrawn-item row clearly marked as unscored.
    **`## Harness teardown`** — a "no harness is started" statement with the reason.
    **`## What a PASS does and does not close`** — a full 5/5 PASS discharges REQ-34.4.2-09 and ticks
    REQ-34.4.2-04 and -05 (re-confirming 01/02/03/06). It does **NOT** close F-34.4.2-10, does NOT
    verify Windows or Linux (D-09), does NOT make any statement about Epic's login UX beyond item
    6(b)'s named absences, does NOT live-discharge T-34.4.2-39/-41 (withdrawn item, D-G1), and does
    NOT prove the system AutoFill panel works.

    **Write NO result of any kind.** Every `Observed:` and `Verdict:` field present and empty;
    frontmatter `verdict` stays `null`.
  </action>
  <verify>
    <automated>G=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-5.md && test -f "$G" && grep -q '^verdict: null' "$G" && grep -q '^items_passed: null' "$G" && grep -q '^items_total: 5' "$G" && test "$(grep -cE '^\*\*Verdict:\*\* *(PASS|FAIL|NOT ATTEMPTED|BLOCKED|UNREACHABLE)' "$G")" = "0" && grep -q 'tee -a' "$G" && test "$(grep -cE 'tee /tmp|\| tee "' "$G")" = "0" && grep -q "pgrep -f 'target/debug/gamelib-shell'" "$G" && grep -qi 'abort' "$G" && grep -q 'D-G1' "$G" && grep -q 'D-G2' "$G" && grep -q 'D-G3' "$G" && grep -qi 'WITHDRAWN' "$G" && grep -qi 'preparatory' "$G" && grep -q 'F-34.4.2-10' "$G" && grep -q 'humble_store/config.json' "$G" && for r in 01 02 03 04 05 06 07 08 09 10; do grep -q "REQ-34.4.2-$r" "$G" || { echo "REQ-34.4.2-$r NOT COVERED"; exit 1; }; done && git diff --quiet -- .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md && test "$(lsof -nP -iTCP:17940 -sTCP:LISTEN | wc -l | tr -d ' ')" = "0" && test "$(pgrep -f 'target/debug/gamelib-shell' | wc -l | tr -d ' ')" = "0" && echo CONTRACT_AUTHORED_NO_RESULTS</automated>
  </verify>
  <acceptance_criteria>
    - `34.4.2-LIVE-GATE-RERUN-5.md` exists with `verdict: null`, `run_date: null`, `items_total: 5`,
      `items_passed: null`, and zero filled `Observed:`/`Verdict:` fields (asserted by the verify
      command).
    - The Evidence-capture instruction contains `tee -a`, no bare `tee` into a fixed path, AND the
      literal `pgrep -f 'target/debug/gamelib-shell'` pre-launch assertion plus a PID recorded at
      launch and at teardown, plus an explicit ABORT-and-re-run rule when the count is not 1
      (asserted in part by the verify command).
    - The exact current `ls -la ~/Library/Logs/GameLib/` output is quoted in the document, resolved by
      running it — not guessed.
    - Every required log literal is annotated with its sink; no sidecar-emitted literal is required of
      the session transcript.
    - A `Preparatory sequence (UNSCORED)` section exists before the items, cites D-G2, requires a
      RENDERED LOGIN FORM as the positive confirmation (explicitly stating an emptied
      `humble_store/config.json` is insufficient), names items 1(e)/3(a)/4 as its dependants, and
      mandates `sample <pid>` before any force-kill if it wedges.
    - The disconnect-control reachability hazard (the control renders only when `isLoggedIn`) was
      resolved against current source at authoring time, and whichever path was confirmed is written
      into the contract with its evidence.
    - Item 5 appears as a WITHDRAWN section in its numeric position with full reasoning, cites D-G1,
      uses plan 34.4.2-22's derived wording for the mechanism, names both surviving coverage layers,
      and states T-34.4.2-39/-41 are unit-proven + UI-pinned and NOT live-discharged (asserted in
      part by the verify command).
    - Item 6(b) runs first in launch 1 AND requires an in-session unscored positive control; its
      absences are label-attributed rather than raw zero-counts.
    - Item 6(a) requires BOTH the `gamelib.log` cookie-census line and the `humble_store/config.json`
      size+mtime proof, and states the three-point before/after guarantee D-G2's ordering preserves.
    - Item 6(a) carries a record-only F-34.4.2-10 observability sub-check, OR the contract states
      explicitly that no observable exists for `clearHumbleStorage` (asserted in part by the verify
      command's `F-34.4.2-10` check).
    - All ten REQ-34.4.2 IDs appear, each mapped to a named item or recorded source-closed (asserted
      by the verify command).
    - Every log literal quoted was re-grepped against CURRENT source at authoring time and its
      observed line number recorded — not copied from RERUN-4.
    - `34.4.2-LIVE-GATE-RERUN-4.md` is byte-unchanged (asserted by the verify command); `-RERUN-3.md`,
      `-RERUN-2.md`, `-RERUN.md` and `-LIVE-GATE.md` are byte-unchanged (check by hand).
    - `lsof -nP -iTCP:17940 -sTCP:LISTEN` and `pgrep -f 'target/debug/gamelib-shell'` both return
      nothing; no `tauri:dev` was launched (asserted by the verify command).
  </acceptance_criteria>
  <done>A blocking contract exists whose PASS conditions were fixed before the outcome was known, whose capture asserts a single app instance at three points and aborts otherwise, whose login-form premise is made true by an unscored preparatory step confirmed by a positive observable, whose withdrawn item carries its full reasoning, and which holds no result of any kind.</done>
</task>

<task type="auto">
  <name>Task 2: Run the SEVEN-test Structural Reachability Review against the contract, record it inside the contract, and fix what it finds</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-5.md</files>
  <read_first>
    - The contract Task 1 just wrote, in full — this task audits it and may send it back for edits.
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` as plan
      34.4.2-21 left it — all SEVEN tests, including Test 3's SINK clause, Test 5's pair-scoping
      reduction rule, and the two new tests. Apply them as written; do not paraphrase them weaker.
      Also read its coverage map, so this review's categories match the reference's.
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-4.md`'s own `## Structural Reachability Review`
      — the table shapes, and the honest account of what a five-test review did and did not catch.
      Your review must be at least as candid, and it must not repeat that review's own mistake of
      reasoning about backend timing while never inspecting the UI.
    - `src-tauri/src/main.rs` around `login_window_url_arg` and the `if visible == true` guard —
      locate current line numbers via graphify then a targeted grep; they may have moved.
  </read_first>
  <action>
    Add a `## Structural Reachability Review` section to `34.4.2-LIVE-GATE-RERUN-5.md`, positioned
    BEFORE `## The items` so a runner meets it first.

    **A. Per-requirement pass (Tests 1-4, 6, 7).** One table row per item, sub-check, precondition,
    the preparatory sequence AND the evidence-capture instruction itself, with columns
    `Item/sub-check | Surface it names | What it asks for | REACHABLE / IMPOSSIBLE / CONDITIONAL | Evidence (file:line or command output)`.
    **Evidence must be a source citation or real command output — never an assertion.**
    - **Test 1, origin/scheme.** Does anything require a URL `login_window_url_arg` rejects?
    - **Test 2, concurrency.** Does anything require two flows driven at once through a UI where a
      presented sheet blocks its parent? With item 5 withdrawn, the answer should be "nothing" —
      say so, and say what was checked, so "found nothing" stays distinguishable from "did not look".
    - **Test 3, emitter AND SINK.** For every required literal: grep it in current source, record the
      file:line you actually observed, and confirm the mandated capture reads the sink that emitter
      writes to. A `logInfo` literal required of the transcript is IMPOSSIBLE (F-34.4.2-14).
    - **Test 4, absence-observability.** For every required ABSENCE, confirm the PRESENCE case would
      produce an observable signal, and state per row which KIND of absence it is. **Item 6(b)'s new
      in-session positive control is exactly this test doing its job — record it as the control it
      is,** and note which of 6(b)'s three absences remain structurally guaranteed rather than newly
      exercised, as RERUN-4 honestly did.
    - **Test 6, pre-existing external-state reachability (NEW).** For EVERY item, name the external
      state its premise depends on and what confirms that state — a live WKWebView cookie jar, a
      system keychain entry, a running external process, the app-side store's own contents. **The
      rows that matter most:** items 1(e), 3(a) and 4's login-form premise (covered by the
      preparatory sequence and its rendered-form observable), and the preparatory sequence's OWN
      premise that the disconnect control is rendered at all (the `isLoggedIn` hazard). Mark any row
      whose premise rests on state nothing checks as CONDITIONAL at best, and fix it.
    - **Test 7, UI-level reachability (NEW).** For every operator gesture any item names, trace it to
      the component that renders the control, and confirm the shipped frontend permits it in the
      state the item assumes. Cover at minimum: opening each store's login window; the cancel strip
      and Esc routes; typing into the password field; Cmd+V and Edit ▸ Paste; opening Epic's login
      window; **clicking Humble's disconnect control** (the preparatory sequence AND item 6(a) both
      depend on it, and it is conditionally rendered). Cite plan 34.4.2-22's derived mechanism where
      it applies.

    **B. Pairwise pass (Test 5) — a separate, explicitly-labelled table.** Apply the reference's
    scoping reduction and STATE which reduction you applied: cross every STATE-MUTATING requirement
    against every EVIDENCE-BEARING one, and record the count considered and the count flagged. The
    state-mutating set in this contract includes at least: **the preparatory disconnect (clears the
    cookie jar AND empties the app-side store)**; item 3(c)'s `GAMELIB_AUTOFILL_GLYPH=0` relaunch;
    each dedicated launch; item 6(a)'s disconnect; item 2's repeated dismissals; item 4's consumption
    of the staged clipboard string; **item 6(b)'s unscored positive-control window** (it deliberately
    EMITS lines whose absence 6(b) asserts).

    **Five pairs are known in advance and must appear with resolutions**, so a reader sees the test
    doing real work:
    1. *(item 3(c)'s relaunch × every prior item's transcript evidence)* — the original F-34.4.2-11;
       resolved by `tee -a` plus per-launch delimiters. Confirm still resolved.
    2. *(item 3(c)'s relaunch × every prior item's `gamelib.log` evidence)* — the rotation hazard;
       resolved by per-launch archiving before the next launch's first write. Confirm still resolved.
    3. *(item 6(a)'s disconnect × item 4's completed-login state)* — resolved by ordering item 4
       strictly before item 6(a) and archiving item 4's `gamelib.log` first.
    4. **NEW — *(the preparatory disconnect × item 6(a)'s filesystem before/after proof)*.** The
       preparatory step empties `humble_store/config.json` early. Confirm the contract's ordering
       makes this HARMLESS: item 4's login repopulates it past 2 bytes before item 6(a) runs, so
       6(a)'s own >2-byte → 2-byte-with-new-mtime transition is intact. If the ordering does not
       guarantee that, fix the ordering here.
    5. **NEW — *(item 6(b)'s positive-control window × item 6(b)'s own absence checks)*.** The control
       deliberately fires the exact literals 6(b) asserts are absent. Confirm the contract resolves
       this by requiring LABEL-ATTRIBUTED absence rather than a raw zero-count, and that Epic's
       window is opened FIRST within the launch. If either is missing, fix it here.
    Also consider, and record the outcome for: *(the preparatory disconnect × items 1/2/3's evidence
    in the same launch, if the contract places them together)* — does a wedge or a state clear in the
    preparatory step foreclose them? Record the accepted risk explicitly rather than leaving it
    unstated.

    **C. Fix what the review finds, in the contract, now.** Any row marked IMPOSSIBLE and any flagged
    pair must be rewritten, re-ordered, replaced with an equivalent-route alternative, or withdrawn —
    before this plan ends. **Do not leave a known-impossible item or an unresolved interaction for the
    runner to discover.** Record every change the review caused, in the review section itself. Where
    a category found nothing, say so together with what was checked.

    **D. Requirement-coverage cross-check.** Confirm every REQ-34.4.2-01..10 is either claimed by a
    named item or explicitly recorded as source-closed and not gate-dependent (as -07 and -08 are).
    **REQ-34.4.2-05's mapping needs care:** it was claimed by item 3 alongside REQ-34.4.2-04. Confirm
    it still is after item 5's withdrawal and that no requirement lost its only claiming item.

    **E. This plan writes no result.** Confirm at task end: no `Observed:` filled, no `Verdict:`
    filled, frontmatter still `verdict: null`, no `tauri:dev` launched, no harness started, no REQ box
    ticked, no `gamelib-shell` process running.
  </action>
  <verify>
    <automated>G=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-5.md && grep -q 'Structural Reachability Review' "$G" && for t in "Test 1" "Test 2" "Test 3" "Test 4" "Test 5" "Test 6" "Test 7"; do grep -q "$t" "$G" || { echo "MISSING $t"; exit 1; }; done && grep -qi 'pairs considered' "$G" && grep -q 'F-34.4.2-11' "$G" && grep -q 'F-34.4.2-14' "$G" && grep -q 'F-34.4.2-16' "$G" && grep -q 'F-34.4.2-17' "$G" && grep -q '^verdict: null' "$G" && test "$(grep -cE '^\*\*Verdict:\*\* *(PASS|FAIL|NOT ATTEMPTED|BLOCKED|UNREACHABLE)' "$G")" = "0" && test "$(awk '/## Structural Reachability Review/{r=NR} /## The items/{i=NR} END{print (r>0 && i>r) ? "OK" : "BAD"}' "$G")" = "OK" && test "$(lsof -nP -iTCP:17940 -sTCP:LISTEN | wc -l | tr -d ' ')" = "0" && test "$(pgrep -f 'target/debug/gamelib-shell' | wc -l | tr -d ' ')" = "0" && echo REVIEW_COMPLETE_NO_RESULTS_WRITTEN</automated>
  </verify>
  <acceptance_criteria>
    - `## Structural Reachability Review` exists and is positioned BEFORE `## The items` (asserted by
      the verify command's line-order check).
    - All SEVEN defect-class tests are named and their application is visible in the tables (asserted
      by the verify command).
    - The per-requirement table has one row per item, sub-check, precondition, the preparatory
      sequence and the evidence-capture instruction, each with a REACHABLE / IMPOSSIBLE / CONDITIONAL
      verdict and a source citation or real command output — no row's evidence column is a bare
      assertion.
    - Test 3's SINK clause is visibly applied: every required literal's row names its sink and states
      whether the mandated capture reads it.
    - Test 6 has a row for EVERY item naming the external state its premise depends on and what
      confirms it, including the preparatory sequence's own `isLoggedIn` rendering hazard.
    - Test 7 has a row for every operator gesture any item names, including clicking Humble's
      conditionally-rendered disconnect control, each traced to the component that renders it.
    - The Test 5 pairwise pass is its OWN labelled table, records the count of pairs considered and
      the count flagged, names the scoping reduction applied, and carries all five named pairs
      (the three carried forward plus the two new ones) with explicit resolutions.
    - The preparatory-disconnect × item-6(a)-filesystem-proof pair is confirmed harmless by the
      contract's own ordering, or the ordering was fixed in this task.
    - The 6(b)-positive-control × 6(b)-absence pair is resolved by label-attributed absence with
      Epic's window opened first within the launch, or was fixed in this task.
    - Every change the review caused is listed; where it found nothing, that is stated together with
      what was checked.
    - All ten REQ-34.4.2 IDs are mapped to an item or recorded source-closed, and no requirement lost
      its only claiming item to item 5's withdrawal.
    - Frontmatter still reads `verdict: null`; no `Observed:`/`Verdict:` field is filled; no REQ box
      was ticked (asserted by the verify command and Task 3's).
    - `lsof -nP -iTCP:17940 -sTCP:LISTEN` and `pgrep -f 'target/debug/gamelib-shell'` both return
      nothing; no `tauri:dev` was launched (asserted by the verify command).
  </acceptance_criteria>
  <done>Every item, sub-check, precondition, the preparatory sequence and the evidence-capture instruction carries a reachability verdict backed by a source citation under all seven tests; every state-mutating requirement has been crossed against every evidence-bearing one with all five named pairs resolved; and the contract still holds no result of any kind.</done>
</task>

<task type="auto">
  <name>Task 3: Record D-G1/D-G2/D-G3 and the F-34.4.2-10 disposition, name RERUN-5 as contract of record — ticking nothing</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` §5 — the threat roll-up and its ELEVEN existing
      dated update tables, ending with the "Eleventh update (2026-08-06, plan 34.4.2-20 ...)". All
      eleven must survive; you append a twelfth. Read the eleventh in full — it is the disposition
      baseline every row of the twelfth is a delta against.
    - `.planning/REQUIREMENTS.md`'s REQ-34.4.2-01..10 rows. Confirm the current state before editing:
      **7 ticked (01/02/03/06/07/08/10), 3 unticked (04/05/09)**. Verify this yourself rather than
      trusting this plan's statement of it.
    - `.planning/phases/.../deferred-items.md`'s `## Plan 20` section — F-34.4.2-15/-16/-17/-18 and
      the T-34.4.2-42 scorecard entry, each currently recorded as "logged, not fixed, no owning plan
      yet". This task assigns owners. **Append a new section; never edit an earlier one.**
    - `.planning/phases/.../34.4.2-21-SUMMARY.md` and `.../34.4.2-22-SUMMARY.md` — what actually
      landed in wave 1, so the ownership assignments describe reality rather than intent.
  </read_first>
  <action>
    **A. `34.4.2-PLATFORM-SCOPE.md` §5 — append a TWELFTH update table**, retaining all eleven earlier
    ones. Head it `**Twelfth update (<date>, plan 34.4.2-23 — authoring
    34.4.2-LIVE-GATE-RERUN-5.md, its seven-test Structural Reachability Review, and recording the
    gap-cycle-5 operator decisions D-G1/D-G2/D-G3)**`.

    **First, record the three binding operator decisions in full, before the table**, as
    **D-G1**, **D-G2** and **D-G3**, each with the finding it resolves, what it decides, and what it
    explicitly does NOT decide. These are operator decisions of the same standing as D-A..D-F3 and
    belong in this section's own decision record, not only in the gate contract.

    Then map each open threat to its discharging item in the NEW contract, so no threat's discharge
    still points at the superseded `-RERUN-4.md`:
    - **T-34.4.2-43** (DoS, the main-thread wedge) — source-fixed (`6bad86227`), live discharge is
      item 6(a). **Third consecutive cycle undischarged**; state that plainly.
    - **T-34.4.2-39 / -41** — **disposition CHANGES under D-G1**: their live-discharge path is
      WITHDRAWN, not merely blocked. Their basis is now unit-proven (plan 14) + UI-pinned (plan 22).
      Record explicitly that this gate will never live-discharge them, and what a future contract
      would have to do (drive the refusal below the UI) if a live discharge is ever wanted.
    - **T-34.4.2-40** — static half source-closed; live half is item 3, whose sub-check (c) has now
      never run across seven gate attempts.
    - **T-34.4.2-32** — source-closed AND live-discharged once (RERUN-4's item 6(b)); D-G3 re-measures
      it, so state that the new run is a re-measurement, not an inheritance, and that RERUN-4's PASS
      remains the historical record either way.
    - **T-34.4.2-42** — completeness gap re-opened by RERUN-4's non-zero scorecard of 3, addressed by
      plan 21's two new tests. Keep the measurable clause: the count encountered during the next run
      is recorded whatever it is. **Do not record the gap as closed** — the new tests are derived,
      not validated.
    - **T-34.4.2-44** (the capture-integrity threat minted by F-34.4.2-15) — its mitigation is now
      IMPLEMENTED for the first time in a contract, not merely written into the standing reference.
      Discharged only by a run that actually holds the single-instance assertion.
    - **T-34.4.2-25 / -26** — held by D-E and D-G3.
    - **T-34.4.2-27** — the DummyStore harness is not started.
    - **F-34.4.2-10 — the Gap 7 disposition, recorded explicitly and never omitted.** Record the
      decision: **DEFERRED again this cycle, with reasons stated, and instrumented rather than
      silently carried.** The reasons, stated plainly: (i) the code path has never been reached live —
      execution deadlocked strictly before it in the run that produced the original wedge, so any fix
      would be written blind against unobserved behaviour; (ii) it sits inside the disconnect flow
      that item 6(a) must measure, so changing it this cycle risks a fresh defect becoming the reason
      item 6(a) fails for a third consecutive time; (iii) this cycle's own purpose is to get a clean,
      reachable gate run, and adding an unmeasured source change to the one flow that has never been
      exercised works directly against that. What is added instead of a fix: item 6(a)'s record-only
      observability sub-check (Task 1), so the next cycle knows whether the path is reachable at all.
      **State the condition for taking it:** once item 6(a) records a measured PASS and the path is
      shown to be reached, F-34.4.2-10 becomes a normal source-fix candidate with observed behaviour
      to fix against.

    **B. `deferred-items.md` — append a new section** `## Plan 23 — gap cycle 5 authoring: owners
    assigned to RERUN-4's four findings, F-34.4.2-10 deferred with reasons (<date>)`. Never edit an
    earlier section. Record, for each of F-34.4.2-15/-16/-17/-18 and the T-34.4.2-42 scorecard entry,
    the owning plan and what it did — F-34.4.2-15 and -18 by `db08bbfc6` (plan 20) in the standing
    reference and now IMPLEMENTED by this contract; F-34.4.2-16 by D-G2's preparatory sequence;
    F-34.4.2-17 by D-G1's withdrawal plus plan 22's pin; the scorecard by plan 21's Tests 6 and 7.
    Record F-34.4.2-10's deferral with the same three reasons and the same taking-condition as (A).
    **Note honestly that a finding with an owning plan is not a closed finding** — none of these is
    closed until the run measures the behaviour the fix is supposed to enable.

    **C. `.planning/REQUIREMENTS.md`.** Append a dated note to REQ-34.4.2-09's row stating that
    `34.4.2-LIVE-GATE-RERUN-5.md` is now the contract of record, that it was structurally reviewed
    against SEVEN defect-class tests before any run, that it covers five items freshly per D-G3 (item
    5 withdrawn per D-G1), and that its `verdict` stays `null` until plan 34.4.2-24 records a measured
    result. Append short dated notes to REQ-34.4.2-04 and -05 naming item 3 of the new contract as
    their live discharge and noting that its own premise is now supplied by D-G2's preparatory
    sequence; and to REQ-34.4.2-06 noting item 4 is likewise premised on that step. **Tick no box.**
    REQ-34.4.2-04, -05 and -09 stay `[ ]`; every currently-ticked box stays as it is.

    **D.** Confirm at task end that no gate result exists anywhere, that
    `34.4.2-LIVE-GATE-RERUN-5.md`'s frontmatter still reads `verdict: null`, that no requirement box
    changed state, and that no source file changed.
  </action>
  <verify>
    <automated>P=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md && D=.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md && grep -q 'Twelfth update' "$P" && for u in "Second update" "Third update" "Fourth update" "Fifth update" "Sixth update" "Seventh update" "Eighth update" "Ninth update" "Tenth update" "Eleventh update"; do grep -q "$u" "$P" || { echo "LOST $u"; exit 1; }; done && grep -q 'D-G1' "$P" && grep -q 'D-G2' "$P" && grep -q 'D-G3' "$P" && grep -q 'F-34.4.2-10' "$P" && grep -q '## Plan 23' "$D" && grep -q 'F-34.4.2-10' "$D" && grep -q '34.4.2-LIVE-GATE-RERUN-5.md' .planning/REQUIREMENTS.md && test "$(grep -cE '^- \[x\] \*\*REQ-34.4.2-(04|05|09)' .planning/REQUIREMENTS.md)" = "0" && test "$(grep -cE '^- \[x\] \*\*REQ-34.4.2-(01|02|03|06|07|08|10)' .planning/REQUIREMENTS.md)" = "7" && grep -q '^verdict: null' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-5.md && git diff --quiet -- src src-tauri/src && git diff --cached --quiet -- src src-tauri/src && echo PROPAGATED_NO_TICKS</automated>
  </verify>
  <acceptance_criteria>
    - `34.4.2-PLATFORM-SCOPE.md` §5 gains a `Twelfth update`; all eleven earlier named updates survive
      (asserted by the verify command).
    - D-G1, D-G2 and D-G3 are each recorded in §5 in full — the finding each resolves, what it
      decides, and what it explicitly does not decide (asserted in part by the verify command).
    - The twelfth update maps T-34.4.2-43, -39, -41, -40, -32, -42, -44, -25, -26 and -27 to a
      discharging item or a source-closure, and T-34.4.2-39/-41's disposition explicitly changes to
      "live-discharge path WITHDRAWN; basis is unit-proven + UI-pinned".
    - T-34.4.2-42 is NOT recorded as closed — the two new tests are recorded as derived, not
      validated.
    - **F-34.4.2-10 carries an explicit DEFERRED disposition with all three stated reasons and a named
      taking-condition, in BOTH `34.4.2-PLATFORM-SCOPE.md` §5 and `deferred-items.md`** (asserted by
      the verify command's checks on both files). It is not omitted from either.
    - `deferred-items.md` gains exactly one new appended `## Plan 23 — ...` section; no earlier
      section is edited (verify by `git diff` that only additions appear below the prior last line).
    - Each of F-34.4.2-15/-16/-17/-18 and the T-34.4.2-42 scorecard entry is assigned an owning plan,
      with the explicit note that an owned finding is not a closed finding.
    - `.planning/REQUIREMENTS.md`'s REQ-34.4.2-09 row names `34.4.2-LIVE-GATE-RERUN-5.md` as the
      contract of record (asserted by the verify command).
    - REQ-34.4.2-04, -05 and -09 remain `[ ]`, and exactly seven REQ-34.4.2 boxes remain `[x]`
      (01/02/03/06/07/08/10) — both asserted by the verify command.
    - `34.4.2-LIVE-GATE-RERUN-5.md` still reads `verdict: null` (asserted by the verify command).
    - `git diff --stat -- src src-tauri/src` is EMPTY — this plan changes no code (asserted by the
      verify command).
  </acceptance_criteria>
  <done>The threat roll-up carries D-G1/D-G2/D-G3 as first-class recorded decisions, every open threat has a named discharging item in the current contract, T-34.4.2-39/-41's basis is restated honestly as never-live-discharged, F-34.4.2-10 is explicitly deferred with reasons and a taking-condition in both the roll-up and the ledger rather than silently omitted, and not one requirement box moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan author → gate result | An author who can also record results can back-fill an outcome from expectation. Held apart by D-E; plan 34.4.2-24 is the only writer. |
| contract text → the surface it names | A contract can demand evidence a surface cannot produce. Seven measured instances across this phase's history. |
| contract text → the SINK its evidence lands in | An emitter can exist and be reachable while the mandated capture reads a different sink (F-34.4.2-14). Test 3's sink clause polices this. |
| requirement A → requirement B's evidence | Two individually-correct requirements can destroy each other's evidence (F-34.4.2-11). Test 5 polices this. |
| an item's premise → state outside the contract's preflight | A live WKWebView cookie jar invalidated three items while the checked app-side store looked clean (F-34.4.2-16). Test 6 and D-G2's preparatory step police this. |
| the contract's gestures → the shipped frontend | A backend-reachable path can be UI-unreachable (F-34.4.2-17). Test 7 and D-G1's withdrawal police this. |
| one app instance → the evidence sink | A second concurrent instance splits `[shell]` while `gamelib.log` stays shared, so a partial run looks measured (F-34.4.2-15). The single-instance assertion polices this. |
| the contract → the operator's evening | Every impossibility surviving review costs live-run time. Six evenings so far. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-25 | Repudiation (a result back-filled from expectation) | `34.4.2-LIVE-GATE-RERUN-5.md` | mitigate — CLOSED by process | PASS conditions are fixed here, before the outcome is known. `verdict`/`run_date`/`items_passed` stay `null`; this plan is forbidden from running any item, starting any harness, or launching `tauri:dev`; the verify commands assert every `Verdict:` field is empty, that nothing listens on port 17940, and that no `gamelib-shell` process is running. |
| T-34.4.2-26 | Repudiation (a green suite mistaken for live proof) | the whole contract | mitigate — CLOSED by process | D-G3: all items are RE-MEASURED, none inherited, stated in the contract's own text. Item 6(b)'s RERUN-4 PASS is explicitly historical record rather than evidence for the next run. |
| T-34.4.2-42 | Repudiation (a contract demanding evidence its surface cannot produce) | contract authoring | mitigate — gap re-opened by RERUN-4's scorecard of 3, addressed not closed | Task 2 applies SEVEN defect-class tests: Test 6 (external-state premises) and Test 7 (UI-level gestures) are the two classes RERUN-4 measured and the five-test review missed. Every row carries a source-cited verdict; every IMPOSSIBLE row and every flagged pair must be fixed before this plan ends. The two new tests are recorded as derived, not validated. |
| T-34.4.2-44 | Repudiation (a partially-captured run looks successfully measured) | the evidence-capture instruction | mitigate — IMPLEMENTED here for the first time | The single-instance rule moves from the standing reference into a contract: a `pgrep` assertion before every launch, a PID recorded at launch and teardown, and an ABORT-and-re-run rule when the count is not 1. RERUN-4 aborted two launches by improvisation; this contract makes it a rule. |
| T-34.4.2-43 | Denial of service (a user-triggered action wedges the whole app) | Humble disconnect, item 6(a) and the preparatory step | mitigate — source-fixed, live discharge item 6(a), undischarged for a third cycle | Item 6(a) requires dual-channel evidence that shares no failure mode. D-G2's preparatory disconnect additionally exercises the same code path EARLIER in the run, so a regression surfaces before four more launches are spent — and the contract requires `sample <pid>` before any force-kill even for that unscored step. |
| T-34.4.2-39 / T-34.4.2-41 | Spoofing (origin confusion) / DoS (a latch that never clears) | withdrawn item 5 | mitigate — source-fixed; live-discharge path WITHDRAWN (D-G1) | Basis restated honestly as unit-proven (plan 14's mutation-proven ordering test) + UI-pinned (plan 22's frontend gate). The contract must state they are NOT live-discharged and will not be by this gate. Withdrawal is recorded with reasoning so a future contract can choose deliberately to drive the refusal below the UI. |
| T-34.4.2-32 | Tampering (Epic scope-boundary erosion) | item 6(b) | mitigate — CLOSED by source, live-discharged once | D-D / REQ-34.4.2-10. 6(b) stays a read-only ABSENCE check; the contract must state it cannot be read as a claim that Epic's login UX works. This plan writes no code and touches no Epic region; the mutation-tested guards in `tauriShellSource.test.ts` remain the enforcement. Epic is Phase 34.7's work. |
| T-34.4.2-40 | Tampering (a deleted mechanism silently reintroduced) | item 3 | mitigate — source-closed, live corroboration item 3 | The three deleted literals appear ONLY in the MUST-NOT-APPEAR list, never as required positives; item 3(b)'s zero-occurrence check spans all launches. Sub-check (c) has never run across seven attempts — recorded as such. |
| T-34.4.2-27 | Tampering (a test fixture reaching production) | DummyStore harness | mitigate — CLOSED, and moot | Not started: the https-only gate makes it unreachable from a Tauri-managed login sheet, and REQ-34.4.2-07 forbids building or reading any credential store. Asserted by the `lsof` check in every verify command. |
| T-34.4.2-49 | Information disclosure (a cookie value, gamekey, order id, email or account identifier transcribed into a committed gate document) | every `Observed:` field the runner will later fill, and every literal quoted here | mitigate | Every required literal in this contract is an aggregate-count or label-only shape. The Evidence-capture instruction must state that cookie values, individual gamekeys, order identifiers, emails and account ids are never transcribed — counts, domains and window labels only. This phase already has a recorded incident of an unredacted account identifier reaching committed files. The session directory lives under `/tmp` and is never committed. |
| T-34.4.2-SC | Tampering (dependency surface) | none | mitigate — CLOSED | Documents-only: no `npm`/`pip`/`cargo` install, no source change. `git diff --stat -- src src-tauri/src` must be EMPTY at plan end. |
</threat_model>

<verification>
- `34.4.2-LIVE-GATE-RERUN-5.md` exists with `items_total: 5`, `verdict: null`, `run_date: null`, `items_passed: null`, and zero filled `Observed:`/`Verdict:` fields.
- The Evidence-capture instruction carries `tee -a`, a timestamped session directory, per-launch delimiters, per-launch `gamelib.log` archiving, per-item launch ordinals, a closing inventory, AND the `pgrep`-based single-instance assertion at three points with an abort-and-re-run rule.
- A `Preparatory sequence (UNSCORED)` section precedes the items, cites D-G2, requires a rendered login form as its positive observable, names items 1(e)/3(a)/4 as dependants, and mandates `sample` before any force-kill.
- Item 5 is WITHDRAWN in its numeric position with full reasoning, citing D-G1 and plan 22's derived mechanism; T-34.4.2-39/-41 are stated as unit-proven + UI-pinned, NOT live-discharged.
- Item 6(b) runs first with an in-session unscored positive control and label-attributed absences.
- Item 6(a) requires both evidence channels and carries a record-only F-34.4.2-10 observability sub-check, or states explicitly that no observable exists.
- The `## Structural Reachability Review` precedes `## The items`, names all seven tests, records the pairwise pass with counts, the scoping reduction and all five named pairs, and lists every change it caused.
- All ten REQ-34.4.2 IDs are accounted for; REQ-34.4.2-04/-05/-09 remain `[ ]` and exactly seven boxes remain `[x]`.
- `34.4.2-PLATFORM-SCOPE.md` §5 gains a twelfth update recording D-G1/D-G2/D-G3; the eleven earlier updates survive.
- F-34.4.2-10 is explicitly deferred with reasons and a taking-condition in both §5 and `deferred-items.md`.
- All prior gate documents are byte-unchanged.
- `git diff --stat -- src src-tauri/src` is EMPTY.
- `lsof -nP -iTCP:17940 -sTCP:LISTEN` and `pgrep -f 'target/debug/gamelib-shell'` both return nothing at plan end; `tauri:dev` was never launched.
</verification>

<success_criteria>
A blocking contract exists whose PASS conditions were fixed before the outcome was known; whose
capture cannot silently split across two app instances; whose login-form premise is made true by an
unscored preparatory step confirmed by a positive observable rather than by an inference from an
empty store; whose one unperformable item is withdrawn with its reasoning and its threat coverage
restated honestly; whose Epic absence check finally has an in-session positive control; and every one
of whose items, sub-checks, preconditions and requirement PAIRS has been proven askable of the
surface it names under all seven tests — so the next live run spends its evening measuring behaviour
instead of discovering, for a seventh time, that the contract asked for something it could never get.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-23-SUMMARY.md` when done.
Record: the exact Evidence-capture command block as authored, verbatim, including the delimiter text,
the resolved `gamelib.log` filename and the three single-instance assertion commands, since plan
34.4.2-24's runner must execute them literally; the preparatory sequence as authored, including which
disconnect-control reachability path was confirmed and the source evidence for it; the full
seven-test Structural Reachability Review outcome — every IMPOSSIBLE row, every flagged pair, and
what was done about each, plus what was checked in each category that found nothing; the count of
pairs considered and the scoping reduction applied; item 5's withdrawal reasoning as written; the
REQ-to-item coverage mapping; the log literals re-grepped against current source with the line
numbers actually observed; and the F-34.4.2-10 deferral with its three reasons and taking-condition.
State explicitly that this plan ran NO gate item, started NO harness, never launched `tauri:dev`,
left every result field null, ticked no requirement box, changed no source — and that the phase
remains NOT CLOSED until plan 34.4.2-24 records a measured verdict.
</output>
