---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 19
subject: Author the sixth blocking live-gate contract (RERUN-4) implementing the dual-sink evidence-capture standard and the five-test Structural Reachability Review, reorder item 6(b) to run first, and name it the contract of record -- ticking nothing
executed: 2026-08-06
status: complete
gap_closure: true
requirements: [REQ-34.4.2-01, REQ-34.4.2-02, REQ-34.4.2-03, REQ-34.4.2-04, REQ-34.4.2-05, REQ-34.4.2-06, REQ-34.4.2-07, REQ-34.4.2-08, REQ-34.4.2-09, REQ-34.4.2-10]
tasks_total: 3
tasks_complete: 3
deviations: 0
---

# Phase 34.4.2 — Plan 19 Summary

Authored `34.4.2-LIVE-GATE-RERUN-4.md` — the sixth blocking live-gate contract in this phase's
history and the first whose evidence-capture instruction and item ordering are structurally
designed to survive their own mandated relaunches. Ran the five-test Structural Reachability
Review against it (Tests 1-4 individually, Test 5 as a dedicated pairwise pass), and named it the
contract of record in `REQUIREMENTS.md` and `34.4.2-PLATFORM-SCOPE.md` §5's tenth update. **This
plan ran NO gate item, started NO harness, never launched `tauri:dev`, left every `Observed:`/
`Verdict:` field and the frontmatter's `verdict`/`run_date`/`items_passed` at `null`, ticked no
requirement box, and changed no source file.** The phase remains **NOT CLOSED** — the sole
remaining closure blocker is plan 34.4.2-20's own measured six-item run.

## Task 1 — `34.4.2-LIVE-GATE-RERUN-4.md` authored (`8effe6f7b`)

Created the six-item contract (frontmatter `verdict: null`, `run_date: null`, `items_total: 6`,
`items_passed: null`), superseding `34.4.2-LIVE-GATE-RERUN-3.md` (byte-unchanged, confirmed by
`git diff --quiet`) without erasing its FAIL 5/6 history.

**Exact Evidence-capture command block, as authored (for plan 34.4.2-20 to execute literally):**

Session setup, once, before the first launch:
```
export GATE_SESSION_DIR="/tmp/gamelib-gate-$(date -u +%Y%m%dT%H%M%SZ)"
mkdir -p "$GATE_SESSION_DIR"
[ -f ~/Library/Logs/GameLib/gamelib.log.old ] && cp ~/Library/Logs/GameLib/gamelib.log.old \
  "$GATE_SESSION_DIR/gamelib-launch-0-preexisting-old.log"
```

Per-launch delimiter, exact literal text, written BEFORE each launch:
```
echo "=== GATE LAUNCH ${N} — $(date -u +%Y-%m-%dT%H:%M:%SZ) ===" | tee -a "$GATE_SESSION_DIR/terminal.log"
```

The launch command itself (`tee -a`, never bare `tee`, never `>`):
```
npm run tauri:dev 2>&1 | tee -a "$GATE_SESSION_DIR/terminal.log"
```
(optionally prefixed `GAMELIB_DEV_SECRET_VAULT=1`, carried unchanged from RERUN-3's own amendment).

Per-launch `gamelib.log` archive, AFTER that launch's app quits, BEFORE the next launch starts:
```
cp ~/Library/Logs/GameLib/gamelib.log "$GATE_SESSION_DIR/gamelib-launch-${N}.log"
```

Closing inventory, at session end:
```
ls -la "$GATE_SESSION_DIR/"
wc -l "$GATE_SESSION_DIR"/*
```

**Resolved `gamelib.log` filename at authoring time** (`ls -la ~/Library/Logs/GameLib/`, quoted
verbatim, not guessed):
```
-rw-r--r--@  1 graysonmitchell  staff     2191 Aug  6 18:39 gamelib.log
-rw-r--r--@  1 graysonmitchell  staff     2191 Aug  6 18:38 gamelib.log.old
```
The active file is exactly `~/Library/Logs/GameLib/gamelib.log`, no suffix — several dated backup
files from unrelated prior phase sessions also exist in the same directory and are untouched.

**The five-launch plan this contract's own text specifies** (fixing the two known
relaunch/evidence pairs, detailed in Task 2 below): launch 1 = item 6(b) alone (Epic absence
check, FIRST); launch 2 = items 1, 2, 3(a)/(b)/(d), 5; launch 3 = item 3(c)'s
`GAMELIB_AUTOFILL_GLYPH=0` relaunch; launch 4 = item 4 (Cmd+V, real Humble login completes);
launch 5 = item 6(a) (Humble disconnect), scheduled after launch 4 so item 4's `gamelib.log`
evidence is archived before the disconnect clears the credential store it just populated.

**Sink partition, applied to every required literal in every item (fixes F-34.4.2-14's class):**
Rust `[shell]`-prefixed lines → session transcript only. Sidecar `logInfo`/`logWarning` → 
`gamelib.log` only. Sidecar stderr → transcript, prefixed `[sidecar:err]`. Sidecar `console.*` →
nothing. Every literal below is annotated accordingly; **no sidecar-emitted literal is required
of the transcript anywhere in this document** — the exact defect that cost items 2/4/6(a) their
own decisive evidence in RERUN-3 is corrected at authoring time, not discovered live a third time.

**Log literals re-grepped against CURRENT source at authoring time (line numbers actually
observed, not copied from RERUN-3):**

| Literal | File:line (as observed) | Sink |
|---|---|---|
| `read-back attached=` | `main.rs:2436` | session transcript |
| `presentation requested visible=true ... sheet_presented=` | `main.rs:3698` | session transcript |
| `cancel requested for '{label}' via {route}` | `main.rs:1486` | session transcript |
| `REFUSED visible login window` | `main.rs:3382` | session transcript |
| `armed as the single pending login flow` | `main.rs:3388` | session transcript |
| `main-thread dispatch failed` (WR-01) | `main.rs:2579` | session transcript |
| `main-thread hop unconfirmed within 10s` (WR-01) | `main.rs:2589` | session transcript |
| `login_window_url_arg` (https-only gate) | `main.rs:926` | n/a (structural constraint) |
| `if visible == true` (single-flight guard scope) | `main.rs:3355` | n/a (structural constraint) |
| `UNSUPPORTED_OR_ERROR for window {seamLabel} — aborting watch` | `user.ts:461` | `gamelib.log` |
| `Humble sync finished: gamekeys=${...}` | `library.ts:954` | `gamelib.log` |
| `cookie census before(...)` | `user.ts:1049` | `gamelib.log` |
| `parent deminiaturized, re-raising` (MUST NOT APPEAR) | 0 occurrences (confirmed) | n/a |
| `post_autofill_right_click` / `autofill glyph injected for` / `autofill glyph injection SKIPPED` / `GAMELIB_AUTOFILL_GLYPH` (MUST NOT APPEAR) | 0 combined occurrences (confirmed) | n/a |
| `log_writer.ts` rotation (`renameSync`) | `log_writer.ts:1` (import), `:74` (rename call) | n/a (rotation mechanism) |

Two literals drifted 1-2 lines from RERUN-3's own citations (`library.ts:952`→`954`,
`user.ts:1048/460`→`1049/461`), consistent with plan 18's own already-documented drift note —
re-confirmed directly, not trusted from the prior document.

**Deviation from the initial draft:** two item-header REQ citations (`REQ-34.4.2-01/-02`,
`REQ-34.4.2-04/-05`) were written in a compressed slash form that does not contain the literal
substring `REQ-34.4.2-02` or `REQ-34.4.2-05` — this broke Task 1's own verify command in isolation
(before Task 2's requirement-coverage table existed to compensate). Expanded to
`REQ-34.4.2-01, REQ-34.4.2-02` / `REQ-34.4.2-04, REQ-34.4.2-05` throughout (headers and the "What
a PASS does and does not close" section) so each task's own verify command is self-sufficient.
Also added a short paragraph naming REQ-34.4.2-07/-08 as source-closed, not gate-dependent,
directly in the Preconditions section — so Task 1's REQ-coverage bar does not depend on Task 2's
own (structurally later) coverage table. Neither change altered any item's PASS bar or any
literal's sink.

## Task 2 — Five-test Structural Reachability Review run and recorded (`f3a15e22c`)

Added `## Structural Reachability Review`, positioned before `## The six items` (confirmed by
line-order check: review section header at line 228, items section at line 328).

**Per-requirement pass (Tests 1-4):** one row per item/sub-check/precondition/the evidence-capture
instruction itself. **Zero rows marked IMPOSSIBLE.** Two rows carry a corrected SINK annotation
Test 3 specifically exists to catch — item 2's `UNSUPPORTED_OR_ERROR` line and item 4's
`Humble sync finished` line are both now explicitly `gamelib.log`-sourced, not transcript-sourced
(RERUN-3's own item text never made this explicit, which is exactly how F-34.4.2-14 slipped
through once already). Item 5's concurrency reasoning (Test 2) is stated explicitly, not assumed:
the second (Humble) initiation lands inside the first's (Amazon's) PRE-presentation window — the
nile spawn delay — confirmed via `if visible == true` (`main.rs:3355`), entered only once
`humble_login_open` actually runs.

**Pairwise pass (Test 5), its own labelled table.** Scoping reduction stated explicitly: **every
state-mutating requirement (M=7: item 3(c)'s relaunch; item 6(b)'s launch; item 6(a)'s launch;
item 6(a)'s disconnect itself, which clears the credential store and cookie jar; item 2's
dismissals; item 4's clipboard consumption; item 5's refusal) against every evidence-bearing
requirement (N=8: item 1's transcript lines; item 2's transcript+gamelib.log lines; item 3's
across-all-launches zero-occurrence check; item 4's gamelib.log line; item 5's transcript lines;
item 6(a)'s gamelib.log+filesystem evidence; item 6(b)'s transcript absences)** — at most M×N = 56
pairs considered.

**The three known pairs, each recorded with its resolution:**
1. Item 3(c)'s relaunch × every prior item's transcript evidence (the ORIGINAL F-34.4.2-11) —
   resolved by `tee -a` + per-launch delimiters.
2. Item 3(c)'s relaunch × every prior item's `gamelib.log` evidence (the newly-found rotation
   hazard, F-34.4.2-14's compounding clause) — resolved by per-launch archiving before the next
   launch's first write.
3. Item 6(a)'s disconnect × item 4's completed-login state — resolved by the launch plan's own
   ordering (item 4 at launch 4, item 6(a) at launch 5; item 4's `gamelib.log` is archived before
   launch 5 begins).

Everything else in the M×N space was checked against the hunting list (RESTARTS /
TRUNCATES-OVERWRITES / CLEARS / RESETS / env-var changes / one-shot-resource consumption) and none
flagged — stated explicitly in the document, distinguishing "found nothing" from "did not look."

**Requirement-coverage cross-check:** all ten REQ-34.4.2 IDs mapped — REQ-01/-02→item 1, REQ-03→
item 2, REQ-04→items 3 (primary)/6(a) (corroborating), REQ-05→item 3, REQ-06→item 4, REQ-07→
source-closed, REQ-08→source-closed, REQ-09→this document as a whole, REQ-10→item 6(b). No
coverage hole.

## Task 3 — RERUN-4 named contract of record, threats remapped, no ticks (`7a2d99399`)

**`REQUIREMENTS.md`:** REQ-34.4.2-09's row gains a dated note naming `34.4.2-LIVE-GATE-RERUN-4.md`
as the contract of record, structurally reviewed, all six items re-measured per D-F1. REQ-04/-05
gain notes naming item 3 as their live discharge; REQ-06 gains a note that its previously-thin
evidence is now a named, sink-annotated required line of item 4. **No box ticked** — confirmed:
REQ-34.4.2-04/-05/-09 remain `[ ]` (0 matches for `- [x]` on those three IDs) and exactly seven
other REQ-34.4.2 boxes remain `[x]` (01/02/03/06/07/08/10).

**`34.4.2-PLATFORM-SCOPE.md` §5:** appended a **Tenth update** table (all nine prior update tables
confirmed present by name). Maps T-34.4.2-43 (source-fixed, item 6(a), not discharged until
measured), T-34.4.2-39/-41 (item 5, first transcript-corroborated measurement attempt),
T-34.4.2-40 (unchanged, item 3), T-34.4.2-32 (item 6(b), first attempt in five gate runs),
T-34.4.2-42 (completeness gap CLOSED by process — the fifth review test), T-34.4.2-25/-26 (held by
D-E/D-F1), T-34.4.2-27 (unchanged, still moot). **F-34.4.2-10 explicitly recorded as still OPEN and
NOT discharged by anything in the new contract.** F-34.4.2-13 and F-34.4.2-14 cross-referenced to
`deferred-items.md`'s "Plans 17-18" section, not duplicated.

**Confirmed at task end:** `git diff --stat -- src src-tauri/src` empty (`PROPAGATED_NO_TICKS`,
not `UNEXPECTED_SOURCE_CHANGE`) — this plan changes no source.

## Deviations

**1. [Task 1 self-correction, no rule category — a verify-command self-sufficiency fix, not a
bug in behaviour]** Two item-header REQ citations were expanded from a compressed slash form
(`REQ-34.4.2-01/-02`) to explicit comma-separated IDs (`REQ-34.4.2-01, REQ-34.4.2-02`) so each
task's own verify command could confirm full REQ-ID coverage independently of later tasks' content.
A short paragraph naming REQ-34.4.2-07/-08 as source-closed was added to the Preconditions section
for the same reason. No item's PASS bar, sink annotation, or launch assignment changed. Documented
above under Task 1.

No other deviations. All three tasks' own `<verify>` automated commands and `<acceptance_criteria>`
re-run and confirmed passing below.

## Verification

| Check | Required | Measured | Verdict |
|---|---|---|---|
| `34.4.2-LIVE-GATE-RERUN-4.md` exists, `verdict: null`/`run_date: null`/`items_total: 6`/`items_passed: null` | present | present | PASS |
| Zero filled `Observed:`/`Verdict:` fields | 0 | 0 | PASS |
| `tee -a` present, no bare `tee /tmp` or `tee "$` | present / 0 | present / 0 | PASS |
| `gamelib.log` and `humble_store/config.json` both mentioned | present | present | PASS |
| All ten REQ-34.4.2 IDs appear in the contract | 10/10 | 10/10 | PASS |
| `34.4.2-LIVE-GATE-RERUN-3.md`, `-RERUN-2.md`, `-RERUN.md`, `-LIVE-GATE.md` byte-unchanged | unchanged | unchanged (confirmed via `git diff --quiet`, all four) | PASS |
| `Structural Reachability Review` precedes `## The six items` | before | before (line 228 < line 328) | PASS |
| All five tests named, pairwise pass with counts + reduction stated | present | present | PASS |
| F-34.4.2-11 and F-34.4.2-14 both named | present | present | PASS |
| Item 6(b) precedes 6(a), own launch, survives-6(a)-failure clause present | present | present | PASS |
| Item 6(a) requires both `gamelib.log` census line AND `config.json` filesystem proof | both required | both required | PASS |
| `REQUIREMENTS.md` names RERUN-4 as contract of record | present | present | PASS |
| REQ-34.4.2-04/-05/-09 remain `[ ]`; exactly 7 other boxes `[x]` | 0 ticked / 7 ticked | 0 ticked / 7 ticked | PASS |
| `34.4.2-PLATFORM-SCOPE.md` §5 gains Tenth update; nine earlier updates survive | present | present | PASS |
| `git diff --stat -- src src-tauri/src` | empty | empty | PASS |
| `lsof -nP -iTCP:17940 -sTCP:LISTEN` | empty | empty | PASS |

**This plan ran NO gate item, launched NO `tauri:dev`, started NO harness, changed NO source, and
ticked NO requirement box. The phase remains NOT CLOSED** — the sole remaining closure blocker is
plan 34.4.2-20's own full six-item live run, recording a genuine result into
`34.4.2-LIVE-GATE-RERUN-4.md`.

## Key files

- `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-4.md` — new six-item contract, `verdict: null`
- `.planning/REQUIREMENTS.md` — REQ-34.4.2-04/-05/-06/-09 dated notes added, no box ticked
- `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md` — Tenth §5 update appended

## Commits

| Commit | What |
|---|---|
| `8effe6f7b` | docs(34.4.2-19): author RERUN-4 six-item live-gate contract, verdict null |
| `f3a15e22c` | docs(34.4.2-19): run five-test Structural Reachability Review against RERUN-4 |
| `7a2d99399` | docs(34.4.2-19): name RERUN-4 as contract of record, remap every open threat -- ticking nothing |

## Carry-forward for plan 20

1. Plan 20 is the sole runner (D-E) — it must execute the five-launch plan `34.4.2-LIVE-GATE-RERUN-4.md`
   itself specifies (launch 1 = item 6(b) alone; launch 2 = items 1/2/3(a)(b)(d)/5; launch 3 =
   item 3(c)'s relaunch; launch 4 = item 4; launch 5 = item 6(a)), implementing the dual-sink
   evidence-capture instruction literally.
2. Item 6(b) MUST be attempted first, in launch 1, before anything else — this is the sixth
   attempt at measuring it in this phase's history and the first structured to survive a 6(a)
   regression.
3. `sample <pid>` BEFORE any force-kill if a wedge recurs despite the fix — this collapsed a
   three-candidate field to one the last time it was captured.

## Self-Check: PASSED

All three tasks executed and committed atomically (`8effe6f7b`, `f3a15e22c`, `7a2d99399`). All
per-task `<verify>` automated commands and the plan-level `<verification>` block commands re-run
directly above and confirmed passing. One documented deviation (Task 1 self-correction, no rule
category applies — a verify-self-sufficiency fix affecting no item's substance).

All files this plan claims to have created/modified confirmed present on disk (`test -f`), and all
three commit hashes confirmed present in `git log --oneline --all`.
