---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 10
type: execute
wave: 4
depends_on: ["34.4.2-09"]
gap_closure: true
files_modified:
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [REQ-34.4.2-01, REQ-34.4.2-02, REQ-34.4.2-03, REQ-34.4.2-04, REQ-34.4.2-05, REQ-34.4.2-06, REQ-34.4.2-09, REQ-34.4.2-10]

must_haves:
  truths:
    - "The six-item blocking gate is run by a human driving the real macOS UI, not inferred from a green suite"
    - "Every item carries a measured Observed and Verdict, or an honest NOT ATTEMPTED / BLOCKED-UNOBSERVABLE with a stated reason"
    - "The recorded verdict propagates to the threat roll-up, the requirement checkboxes, ROADMAP and STATE — whatever it is"
    - "The phase closes only on a genuine 6/6; anything less routes to another gap cycle"
  artifacts:
    - path: ".planning/phases/34.4.2-.../34.4.2-LIVE-GATE-RERUN.md"
      provides: "The measured verdict, per-item Observed/Verdict, findings, and harness shutdown record"
      contains: "ITEM VERDICT SUMMARY"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 34.4.2 status line reflecting the measured result"
      contains: "34.4.2"
    - path: ".planning/STATE.md"
      provides: "Decisions entry, Session Continuity entry, and counters for this run"
      contains: "34.4.2"
  key_links:
    - from: "operator's measured observations"
      to: "34.4.2-LIVE-GATE-RERUN.md Observed/Verdict fields"
      via: "verbatim transcription, no interpretation added"
      pattern: "Observed:"
    - from: "34.4.2-LIVE-GATE-RERUN.md verdict"
      to: "REQUIREMENTS.md / PLATFORM-SCOPE.md §5 / ROADMAP.md / STATE.md"
      via: "propagation task"
      pattern: "items_passed"
---

<objective>
Run the rewritten six-item blocking live gate on real macOS hardware and record the measured result.

Purpose: this project has been shown three times that a fully green automated suite proves nothing
about live login-window behaviour — 3279/3279, 3387/3387 and 3447/3447 all passed while blocking
defects were live, and **every blocking defect was found by a human driving the UI, none by
automation.** This phase's own 3722/3722 suite coexisted with F-34.4.2-01. This plan is the only
source of truth about whether the sheet design actually works.

Discipline this plan inherits from plan 06 and must not relax: transcribe what the operator
observed, verbatim; assert no root cause; record an honest NOT ATTEMPTED rather than a guessed PASS;
apply the no-partial-pass rule at the item level; and propagate whatever result arrives, including
a bad one.

Output: a filled gate document, an updated threat roll-up, correct requirement checkboxes, and
ROADMAP/STATE propagation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-06-SUMMARY.md
@.claude/skills/spike-findings-gamelib/references/oauth-login-test-harness.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — satisfy every machine-satisfiable precondition and capture the baselines</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN.md` Preconditions section (each precondition's
      satisfier attribution — which are machine-satisfiable here and which are operator-only)
    - `.planning/phases/.../34.4.2-LIVE-GATE.md` Preconditions section (the failed gate's own preflight
      is the worked example: the exact `lsof`/`curl`/`strings` command shapes and the evidence-quoting
      style to reproduce)
    - `.claude/skills/spike-findings-gamelib/references/oauth-login-test-harness.md` (harness start
      command and the `/events` oracle)
    - `.planning/phases/.../34.4.2-08-SUMMARY.md` (the new log-line literals to symbol-check for)
  </read_first>
  <action>
    Fill each precondition's satisfaction status in `34.4.2-LIVE-GATE-RERUN.md` with real, quoted
    command output — never an assertion without evidence.

    1. **Build freshness.** Run `cargo build --manifest-path src-tauri/Cargo.toml` and quote its final
       line. Then symbol-check the built debug binary `src-tauri/target/debug/gamelib-shell` with
       `strings ... | grep -F` for the literals this gap cycle introduced, and confirm all of them are
       present: `sheet_presented=`, `login cancel strip injected for '`,
       `[shell] login-sheet: cancel requested for '`, `/login-cancel`. Also confirm the RETIRED literal
       `parent deminiaturized, re-raising` is **ABSENT** — a stale binary is the exact failure mode
       that would make item 1 unscoreable. Quote every grep result. Note that the operator still runs
       `npm run tauri:dev` themselves; this only proves the on-disk binary is not stale.
    2. **DummyStore harness.** Confirm port 17940 is free with
       `lsof -nP -iTCP:17940 -sTCP:LISTEN`, start
       `node .claude/skills/spike-findings-gamelib/sources/019-dummy-oauth-store/store-server.mjs` in
       the background, confirm liveness with `curl -s --max-time 5 http://127.0.0.1:17940/events`, and
       quote the raw response. Record the PID — Task 3 stops it.
    3. **Seeded Passwords entry, logout preamble, real Humble login, `GAMELIB_AUTOFILL_GLYPH` unset**:
       these are operator-only. Do not fake them. Record each as OPERATOR-AT-GATE-TIME with the staged
       instruction the operator must follow, carried verbatim from the contract — in particular the
       sticky-jar preamble (navigate a login window to `http://127.0.0.1:17940/logout`, THEN read
       `/events` for the baseline count) and the rule that an unseeded Passwords entry makes item 3
       BLOCKED-UNOBSERVABLE rather than FAILED.
    4. **Suite baselines, captured BEFORE the live run.** Run `npm run test:ci` and
       `cargo test --manifest-path src-tauri/Cargo.toml` and record both figures verbatim, alongside
       the sentence that these are context, not evidence of live behaviour (T-34.4.2-26).

    Write nothing into `verdict`, `run_date`, `items_passed`, or any `Observed:`/`Verdict:` field.
  </action>
  <verify>
    <automated>lsof -nP -iTCP:17940 -sTCP:LISTEN | tail -2 && curl -s --max-time 5 http://127.0.0.1:17940/events | head -c 200 && grep -c 'verdict: null' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 and its final line is quoted in the document.
    - All four new literals are FOUND by `strings | grep -F` against `src-tauri/target/debug/gamelib-shell`, each quoted.
    - `parent deminiaturized, re-raising` is confirmed ABSENT from the binary (grep returns nothing / exit 1) — quoted.
    - The harness responds on 17940 with a JSON body containing `eventCount`, quoted raw; its PID is recorded in the document.
    - `npm run test:ci` and `cargo test` figures are recorded verbatim with the "context, not evidence" caveat.
    - Frontmatter still reads `verdict: null`, `run_date: null`, `items_passed: null`, and no `Observed:` field is filled.
  </acceptance_criteria>
  <done>Every machine-satisfiable precondition is satisfied with quoted evidence, the operator-only ones are staged with instructions, and no result field has been touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING live gate — the operator drives all six items on real macOS hardware</name>
  <what-built>
    Plans 34.4.2-07 and -08 replaced AppKit child-window attachment with SHEET presentation for
    visible Tauri-managed login windows (Humble/GOG/Amazon), retired the deminiaturize re-raise
    observer, and added two independent dismissal routes: a visible in-page "Cancel sign-in" strip
    that no kill switch can remove, and a page-independent bare-Esc handler. The glyph, the pure
    autofill parser and the synthesized right-click poster are unchanged in design; the poster's
    authorization gate was re-homed onto the sheet registry. Epic's pristine login window is
    byte-untouched.

    Task 1's preflight has started the DummyStore harness on port 17940, verified the built binary
    contains this cycle's new log literals and no longer contains the retired one, and captured the
    suite baselines.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md`
    and work through items 1-6 in order, following each item's own written instructions. Summary of
    what to do:

    1. Run `npm run tauri:dev` with `GAMELIB_AUTOFILL_GLYPH` unset.
    2. **Before anything else**, run the sticky-jar logout preamble: open a Tauri-managed login window,
       navigate it to `http://127.0.0.1:17940/logout`, then record the `/events` baseline count from
       `curl -s http://127.0.0.1:17940/events`. Skipping this silently skips the login form later.
    3. **Item 1** — open a GOG/Humble login sheet, then: click the main window, drag the main window,
       minimize it to the Dock, restore it, and **immediately type into the password field and click
       the Cancel sign-in strip.** The last step is the one that matters most — it is the exact defect
       (F-34.4.2-01) that failed the last gate.
    4. **Item 2** — dismiss a fresh sheet via the Cancel strip, another via bare Esc, and record what
       Cmd+W does. After each dismissal, confirm the main window is clickable again.
    5. **Items 3-6** — the glyph and AutoFill menu (DummyStore form and real Humble form), Cmd+V paste,
       the `GAMELIB_AUTOFILL_GLYPH=0` kill-switch arm (including that the Cancel strip SURVIVES it),
       and the hidden-window / Epic-untouched absence checks.

    Capture `gamelib.log` excerpts for the log lines each item names. If an item cannot be reached,
    say so plainly — an honest NOT ATTEMPTED is worth more than a guessed PASS, and it is what made
    the previous run's record usable.

    If a blocking defect appears, stop and report it. Do not attempt to work around it.
  </how-to-verify>
  <resume-signal>Report the outcome of each item you reached, verbatim, including any log lines captured and any item you did not get to. Type "approved" only if all six items passed.</resume-signal>
  <files>none — the operator drives the UI; no file is written by this task</files>
  <action>Pause execution and hand control to the operator. Claude runs NOTHING in this task: the entire value of this gate is that a human drives the real UI, which is how every blocking defect in phases 34.4.1 and 34.4.2 was actually found. Present the `<how-to-verify>` steps, wait for the operator's report, and carry that report into Task 3 verbatim without editing, summarising, or scoring it here.</action>
  <verify>
    <automated>MISSING — deliberately human-only. This item is unautomatable by construction: it measures on-screen AppKit sheet behaviour, system context-menu contents, and keyboard delivery, none of which the test suite can observe. See T-34.4.2-26.</automated>
    <human-check>The operator reports a per-item outcome for every item they reached, plus an explicit NOT ATTEMPTED for any they did not.</human-check>
  </verify>
  <done>The operator has reported a measured outcome for each of the six items, or an explicit NOT ATTEMPTED with a reason, and that report is available verbatim to Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measured verdict and propagate it, whatever it is</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md, .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN.md` (every item's PASS condition, so each verdict is
      scored against the bar fixed before the run, not against what happened)
    - `.planning/phases/.../34.4.2-06-SUMMARY.md` (the propagation worked example: which files, which
      sections, and the hand-edit rule for STATE.md)
    - `.planning/ROADMAP.md` Phase 34.4.2 block (the status block to replace)
    - `.planning/STATE.md` frontmatter, Decisions, Performance Metrics and Session Continuity sections
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` §5 (the roll-up tables to append to)
  </read_first>
  <action>
    Transcribe the operator's report into the gate document and propagate the result.

    1. Fill each item's `Observed:` field with the operator's report **verbatim**, then an
       interpretation against that item's own named sub-checks — clearly separated, as the failed
       gate's item 1 does. Fill each `Verdict:` with PASS, FAIL, NOT ATTEMPTED (with the reason), or
       BLOCKED-UNOBSERVABLE (with the reason). Apply the no-partial-pass rule at the ITEM level: an
       item with one failing sub-check is a FAIL, not a partial credit. Assert **no root cause** for
       any defect — that diagnosis belongs to whatever cycle comes next, and this project has already
       paid nine live runs for shipping correlation as cause.
    2. Fill the frontmatter `verdict`, `run_date`, `items_passed`, `status: complete`, and the ITEM
       VERDICT SUMMARY table. `items_passed` counts only items that actually passed; NOT ATTEMPTED
       items are neither passed nor failed and must be visible as such.
    3. File any new findings as `F-34.4.2-NN` entries with symptom, the item that surfaced them,
       "suspected layer: NOT diagnosed", and whether they block the phase.
    4. Append a post-rerun table to `34.4.2-PLATFORM-SCOPE.md` §5 giving every threat its post-gate
       disposition: T-34.4.2-05, -07, -33 discharged (or not) by items 1/2; -17/-21 by item 3; -22 by
       item 4; -15 residual by item 5; -32 re-confirmed by item 6; -34/-35 by their source guards plus
       whatever the run showed. A threat whose item was NOT ATTEMPTED stays OPEN with that stated
       reason — never quietly closed.
    5. Tick `.planning/REQUIREMENTS.md` boxes for exactly the requirements the run actually proved
       (REQ-34.4.2-01/-02 by item 1, -03 by item 2, -04 by items 3/5, -05 by item 3, -06 by item 4,
       -10 by item 6(b), -09 by the gate having run at all). Leave every other box unchecked with its
       reason.
    6. Update `.planning/ROADMAP.md`'s Phase 34.4.2 block: replace the "LIVE GATE FAILED 2026-08-04"
       status with the new measured status, tick plans 07-10's checkboxes with their verdicts inline,
       and state plainly whether the phase CLOSES. Preserve the historical record of the failed run and
       the binding decision — mark superseded text, never delete it.
    7. Update `.planning/STATE.md` by **hand-editing** — do not use `gsd-sdk query state.*` writes;
       this repo has a recorded defect where those calls revert `stopped_at`, mangle counters and
       splice status into historical blocks. Update the frontmatter counters, add a Performance
       Metrics row, add a Decisions entry for the measured result, and add a top-of-history Session
       Continuity entry.
    8. Stop the DummyStore harness using the PID recorded in Task 1 and confirm the port is released
       with `lsof -nP -iTCP:17940 -sTCP:LISTEN` returning nothing. Record both in the gate document.

    **On anything short of 6/6:** state that the phase does NOT close, name
    `/gsd-plan-phase 34.4.2 --gaps` as the next step, and scope what the next cycle must address —
    without proposing a design change unless the operator made one. **On a genuine 6/6:** state that
    the phase closes, and note what a PASS still does not cover (Windows, Linux, and Epic's login UX).
  </action>
  <verify>
    <automated>grep -c 'verdict: null' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md ; grep -n 'items_passed' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN.md ; lsof -nP -iTCP:17940 -sTCP:LISTEN | wc -l ; git diff --stat -- src src-tauri/src</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'verdict: null'` returns 0 — the frontmatter now carries a real verdict.
    - `items_passed` is an integer, and the ITEM VERDICT SUMMARY table has a non-empty verdict cell for all six items.
    - Every `Observed:` field is non-empty; every item marked NOT ATTEMPTED or BLOCKED-UNOBSERVABLE states a reason.
    - No item's `Observed:` text contains a root-cause claim for a defect (verifiable by reading — findings say "suspected layer: NOT diagnosed").
    - `lsof -nP -iTCP:17940 -sTCP:LISTEN | wc -l` returns 0 and the shutdown is recorded in the document.
    - `git diff --stat -- src src-tauri/src` is EMPTY for this plan — it records a result, it does not fix code.
    - `.planning/ROADMAP.md`'s Phase 34.4.2 block states explicitly whether the phase closes, and the failed-run history and binding decision are still present.
    - `.planning/STATE.md` was edited by hand (no `gsd-sdk query state.*` write command appears in this plan's command history).
    - Requirement boxes ticked correspond one-to-one with items that PASSED; no box is ticked on the strength of a NOT ATTEMPTED item.
  </acceptance_criteria>
  <done>The measured verdict is recorded with per-item evidence and no invented diagnosis, propagated to the threat roll-up, requirements, ROADMAP and STATE, the harness is stopped, and the phase's close/does-not-close status is stated unambiguously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator report → recorded verdict | Transcription is where an inconvenient observation can quietly become a pass |
| test fixture → production | The DummyStore harness is a loopback test fixture that must never ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-25 | Repudiation (a verdict back-filled from expectation) | Task 3 transcription | mitigate — CLOSED by process | PASS conditions were fixed by plan 09 before this plan ran; Task 3 scores each item against the pre-written bar and transcribes the operator's words verbatim before interpreting them; NOT ATTEMPTED is an allowed and expected outcome. |
| T-34.4.2-26 | Repudiation (a green suite mistaken for live proof) | Task 1 baselines | mitigate — CLOSED by process | Suite figures are recorded explicitly as context, not evidence; the plan's own objective restates the three prior gate runs where a green suite coexisted with live blocking defects. |
| T-34.4.2-07 | Denial of service (user trapped by a modal login) | items 1 and 2 | mitigate — discharged or not BY THIS RUN | If item 2 does not PASS on both required routes, the threat stays OPEN and the phase does not close — a sheet that cannot be dismissed is strictly worse than the free-floating window this design replaced. |
| T-34.4.2-33 | Denial of service (hard lock-out behind a sheet) | items 1(e) and 2 | mitigate — discharged or not BY THIS RUN | The operator holds the escape hatch: quitting the app always works, and `captureOAuthLogin`'s five-minute deadline bounds any trap during the run itself. |
| T-34.4.2-27 (DummyStore containment) | Tampering (a test fixture reaching production) | `store-server.mjs` | mitigate — CLOSED | The harness is started only for this run, binds loopback only, and is stopped in Task 3 with port release confirmed; plan 05's containment guard already asserts it never ships. |
| T-34.4.2-SC | Tampering (dependency surface) | none | mitigate — CLOSED | This plan installs nothing and changes no code — `git diff --stat -- src src-tauri/src` empty is an acceptance criterion. |
</threat_model>

<verification>
- Every item in `34.4.2-LIVE-GATE-RERUN.md` has a filled `Observed:` and `Verdict:`; the frontmatter carries a real verdict, run_date and items_passed.
- Port 17940 is released.
- `git diff --stat -- src src-tauri/src` is empty for this plan.
- ROADMAP and STATE both state the same close/does-not-close conclusion as the gate document.
- Requirement checkboxes match the per-item results exactly.
</verification>

<success_criteria>
A human drove all six items on real macOS hardware; the result — whatever it is — is recorded with
per-item evidence, no invented root cause, and full propagation; the phase closes only on a genuine
6/6, and anything less names the next gap cycle explicitly.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-10-SUMMARY.md` when done.
Record the verdict, the per-item table, any findings, the harness shutdown evidence, and the
close/does-not-close conclusion.
</output>
