---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 21
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md
autonomous: true
requirements: [REQ-34.4.2-09]

must_haves:
  truths:
    - "The Structural Reachability Review has a defect-class test for premises that depend on state living OUTSIDE what the contract's own preflight checks"
    - "The Structural Reachability Review has a defect-class test for whether the shipped frontend UI actually permits the operator gesture sequence an item describes"
    - "Every finding RERUN-4 minted maps to a named test or a named Section 3 bullet, and any that maps to NEITHER is stated as such rather than left implied"
    - "The reference's own defect tally and section heading agree with the number of tests it actually carries"
  artifacts:
    - path: ".claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md"
      provides: "Seven defect-class tests, each with what-it-asks / why-it-exists / the finding that paid for it / a worked example, plus a finding-to-test coverage map"
      contains: "Test 7"
  key_links:
    - from: "Test 6 (pre-existing external-state reachability)"
      to: "F-34.4.2-16 (item 4's premise invalidated by a live WKWebView cookie jar)"
      via: "the worked example naming the finding that paid for the test"
      pattern: "F-34.4.2-16"
    - from: "Test 7 (UI-level reachability)"
      to: "F-34.4.2-17 (item 5's scenario is UI-unreachable)"
      via: "the worked example naming the finding that paid for the test"
      pattern: "F-34.4.2-17"
    - from: "the finding-to-test coverage map"
      to: "F-34.4.2-15 / -16 / -17 / -18"
      via: "one row per finding naming the test or Section 3 bullet that now catches it"
      pattern: "F-34.4.2-18"
---

<objective>
Close the T-34.4.2-42 completeness gap that RERUN-4 re-opened with a measured, non-zero scorecard.

RERUN-4 (`34.4.2-LIVE-GATE-RERUN-4.md`, run 2026-08-06, verdict FAIL 1/6) recorded **3** structural
impossibilities and unresolved requirement interactions actually encountered during the run, against
T-34.4.2-42's own measurable clause of "should be zero". None of the five existing defect-class
tests — including the pairwise Test 5 added by plan 34.4.2-18 specifically to close the previous
completeness gap — anticipated either of the two classes that cost this run its evening:

1. **F-34.4.2-16 (F-B):** item 4's premise (a credential-entry event to observe) was false because a
   live WKWebView Humble session auto-completed the login. The contract's own preflight checked the
   app-side store (`humble_store/config.json`, confirmed 2 bytes) and concluded a logged-out state.
   The state that actually mattered lived OUTSIDE anything the preflight touched.
2. **F-34.4.2-17 (F-C):** item 5's scenario (click Amazon, then click Humble during nile's spawn
   delay) cannot be driven from the shipped frontend at all. Test 2 reasoned correctly about BACKEND
   timing — the `if visible == true` scoping, when `humble_login_open` actually runs — and never
   asked whether the UI permits the described clicks in the first place.

`deferred-items.md` already names these two candidate test classes; this plan builds them. It writes
NO gate contract, runs NO gate item, and launches nothing.

**What already landed and must NOT be re-added.** Commit `db08bbfc6` (plan 34.4.2-20) already added
to Section 3 the exactly-one-app-instance capture-integrity rule (F-34.4.2-15/F-A) and the
preflight-must-check-for-a-pre-existing-instance clause (F-34.4.2-18/F-D). Verify both are present
before writing anything; do not duplicate them, and do not restate them as new tests in Section 2 —
they are capture-integrity requirements, a different category from the defect-class tests, and the
coverage map in Task 2 is where that distinction gets stated out loud.

Output: a reference carrying seven defect-class tests, a heading and defect tally that agree with
that count, and a coverage map proving each of RERUN-4's four findings is now caught by something
named — or honestly recorded as caught by nothing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md
@.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md

**Tooling rule for this repo (mandatory).** `graphify-out/graph.json` exists. Run
`graphify query "<question>"` / `graphify explain "<concept>"` before grepping or reading raw source.
This plan touches no source, but any file:line anchor you quote into a worked example must be
re-confirmed against current source rather than copied from a prior document.

<interfaces>
<!-- The reference's existing shape, so the two new tests match it rather than diverging. -->

**Current structure of `live-gate-contract-authoring.md`:**
- `## Section 1 — Why this file exists` — a bulleted tally of the contract-authoring defects found so
  far ("**six distinct contract-authoring defects**" at the time of writing). This count is now stale.
- `## Section 2 — The five defect-class tests` — heading is now stale. Carries a preamble ("Apply all
  five tests to every item, sub-check, and precondition...") plus the row-shape table header, then
  `### Test 1 — Origin/scheme reachability` through `### Test 5 — Requirement-interaction
  reachability`.
- `## Section 3 — The dual-sink evidence-capture standard` — session directory, `tee -a`, delimiter,
  per-launch `gamelib.log` archiving, per-item launch ordinal, closing inventory, **and the
  exactly-ONE-app-instance bullet already added by `db08bbfc6`**.
- `## Section 4 — Two rules the project has paid for twice`.

**The shape every existing test follows — match it exactly:**
1. A one-or-two-sentence statement of what the test asks.
2. The general shape of the defect it catches (not phase-specific).
3. `**Worked example (Phase 34.4.2, F-NN):**` — the finding that paid for the test, with a file:line
   or command citation.

**Facts the two new worked examples rest on, all re-confirmable:**
- Test 6's paying finding: the app-side Humble credential store
  (`~/Library/Application Support/gamelib/humble_store/config.json`) was 2 bytes with a stale mtime
  in RERUN-4's own Preflight Results, while the WKWebView cookie jar independently held a live
  session that auto-completed the login. An emptied app-side store is NOT evidence of a logged-out
  session.
- Test 7's paying finding: the login tiles live in a single `runnerGroup` container in
  `src/frontend/screens/Login/index.tsx`, and `Runner`'s primary click handler calls
  `navigate(props.loginUrl)` (`src/frontend/screens/Login/components/Runner/index.tsx`), routing to
  `loginweb/:runner` (`src/frontend/App.tsx`). Re-confirm the current line numbers before quoting
  them. **Plan 34.4.2-22 is deriving the precise mechanism in parallel; if its finding contradicts
  the characterisation in `deferred-items.md` ("the frontend disables/clears the other login
  buttons"), Test 7's worked example must describe what the source actually does, and say plainly
  that the original characterisation was approximate.**

**Category distinction the coverage map must preserve:** Section 2's tests are *authoring-time
reachability* tests, applied to a contract's items before it is run. Section 3's bullets are
*run-time capture-integrity* requirements, applied to the evidence pipeline while it runs.
F-34.4.2-15 and F-34.4.2-18 are Section 3 material and are already there; F-34.4.2-16 and
F-34.4.2-17 are Section 2 material and are what this plan adds. Do not blur the two.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Test 6 and Test 7, and make the heading and defect tally agree with the count</name>
  <files>.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md</files>
  <read_first>
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` in full —
      the file being modified. Note in particular Section 2's preamble ("Apply all five tests..."),
      the shape of Tests 1-5, and the exactly-ONE-app-instance bullet already present in Section 3
      (do not duplicate it).
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-4.md`'s `## Findings recorded this run` section —
      F-34.4.2-15/-16/-17/-18 and the T-34.4.2-42 scorecard paragraph naming the two candidate test
      classes. This is the primary source for both worked examples.
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-4.md`'s item 4 and item 5 `Observed:`/`Verdict:`
      fields — the measured text each new test exists to prevent recurring.
    - `.planning/phases/.../deferred-items.md`'s `## Plan 20` section — the ledger's own wording for
      the two candidate classes, so the tests agree with the ledger instead of diverging from it.
  </read_first>
  <action>
    Add two new tests to Section 2 after `### Test 5 — Requirement-interaction reachability`, each
    matching the existing five-part shape (what it asks; the general defect shape; the worked example
    naming the finding that paid for it, with a file:line or command citation).

    **`### Test 6 — Pre-existing external-state reachability.** Its unit is the item's PREMISE, not
    its evidence. Ask: does this item's premise assume a fresh or clean state that some state living
    OUTSIDE what the contract's own preflight checks can silently invalidate? Name the concrete
    external-state surfaces this project has: a live WKWebView cookie jar, a system keychain entry,
    an already-running external process, a browser session, an OS-level permission grant. State the
    general rule the finding produced: **an emptied app-side store does not prove a logged-out
    session** — for every premise of the form "the user is logged out / the field is empty / no
    session exists", the contract must name a POSITIVE observable that confirms it (a rendered login
    form), never merely the absence of app-side data. Require the test to record, per item, which
    external state its premise depends on and what preflight check or in-run confirmation covers it.

    Worked example: F-34.4.2-16 (F-B). RERUN-4's Preflight Results recorded
    `~/Library/Application Support/gamelib/humble_store/config.json` at 2 bytes with an mtime from a
    prior session and treated it as a clean starting point. Item 4's premise — a credential-entry
    event to observe — was nevertheless false, because the WKWebView cookie jar independently held a
    live Humble session that auto-completed the login with no form to paste into. Items 1(e) and 3(a)
    were invalidated by the identical mechanism in the same run. The contract had no step that
    cleared the cookie jar and no observable that would have shown the premise was already broken.

    **`### Test 7 — UI-level reachability, distinct from backend-logic reachability.** Its unit is
    the operator GESTURE SEQUENCE. Ask: can the described sequence of clicks, keystrokes and window
    interactions actually be performed against the shipped frontend as built? State explicitly why
    this is not covered by Test 2: Test 2 reasons about platform modality and backend timing (is the
    code path reachable at that moment?) and never inspects whether the UI exposes the controls the
    gesture needs. A backend path can be perfectly reachable while the frontend never offers the
    operator a way to reach it. Require the test to trace each gesture to the component that renders
    the control it names, and to record what the frontend does with that control in the state the
    item assumes — including whether navigation, conditional rendering, or a disabled state removes
    it. Note the general rule: **a scenario whose gesture sequence the UI forbids can never PASS, no
    matter how correct the backend guard is** — the correct dispositions are to drive the path below
    the UI, restate the assertion at unit level, or withdraw the item with its reasoning recorded.

    Worked example: F-34.4.2-17 (F-C). RERUN-4's item 5 asked the operator to click Amazon sign-in
    and then click Humble sign-in during nile's ~7-8s spawn delay, exercising the Rust
    `PENDING_VISIBLE_LOGIN_WINDOW` single-flight guard. The Structural Reachability Review's Test 2
    row for that item verified — correctly — that the second initiation would land in the first's
    PRE-presentation window, when no sheet is up and the parent is still interactive. The item was
    still unperformable: no second store's sign-in control is reachable at that moment. Cite the
    actual mechanism as re-confirmed against current source at this authoring (see the interfaces
    note above); if it differs from the "disables/clears the other login buttons" characterisation in
    `deferred-items.md`, describe the source behaviour and mark the earlier characterisation as
    approximate rather than repeating it.

    **Then make the file's own arithmetic agree with itself:**
    - Rename the Section 2 heading from `The five defect-class tests` to `The seven defect-class tests`.
    - Update Section 2's preamble ("Apply all five tests to every item, sub-check, and precondition")
      to seven.
    - Update Section 1's tally of distinct contract-authoring defects to include RERUN-4's, and add
      the two new defect classes to Section 1's bullet list in the same register as the existing
      entries (a one-line statement of the class and its finding ID). Do not rewrite Section 1's
      existing bullets.
    - Update the standing-rule sentence in Section 1 ("The plan that authors the contract must run
      this review — all five tests — before publishing it") to seven.
    - Grep the whole file for any other literal claim of a test count and fix each; there must be no
      surviving reference to five tests as the current total.
  </action>
  <verify>
    <automated>R=.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md && grep -q '^### Test 6 ' "$R" && grep -q '^### Test 7 ' "$R" && grep -q '^## Section 2 — The seven defect-class tests' "$R" && test "$(grep -v '^#' "$R" | grep -ciE 'all five tests|the five defect-class tests|five tests')" = "0" && grep -q 'F-34.4.2-16' "$R" && grep -q 'F-34.4.2-17' "$R" && test "$(grep -c '^- \*\*Exactly ONE app instance' "$R")" = "1" && test "$(grep -c 'preflight must check for a' "$R")" = "1" && for t in 'Test 1' 'Test 2' 'Test 3' 'Test 4' 'Test 5'; do grep -q "### $t " "$R" || { echo "LOST $t"; exit 1; }; done && test "$(grep -c 'Worked example' "$R")" -ge 7 && git diff --quiet -- src src-tauri/src && git diff --cached --quiet -- src src-tauri/src && echo SEVEN_TESTS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `### Test 6 — Pre-existing external-state reachability` and `### Test 7 — UI-level reachability,
      distinct from backend-logic reachability` both exist as Section 2 subsections (asserted by the
      verify command).
    - The Section 2 heading reads `The seven defect-class tests`, and no line outside a heading
      claims five tests as the current total (asserted by the verify command's comment-filtered
      count of `all five tests` / `the five defect-class tests` / `five tests` → 0).
    - Tests 1-5 all survive unmodified in substance (asserted by the verify command's presence check).
    - Each new test carries: what it asks, the general defect shape, and a `Worked example` naming
      its paying finding ID with a file:line or command citation. At least seven `Worked example`
      blocks exist in the file (asserted by the verify command).
    - Test 6 states the rule that an emptied app-side store does not prove a logged-out session, and
      requires a POSITIVE observable for every "clean state" premise.
    - Test 7 states in its own text why Test 2 does not cover it, and names the three legitimate
      dispositions for a UI-forbidden scenario (drive below the UI / restate at unit level / withdraw
      with reasoning).
    - Section 3's exactly-ONE-app-instance bullet and its preflight clause each appear exactly ONCE —
      not duplicated by this plan (asserted by the verify command).
    - Section 1's defect tally and its "all five tests" standing-rule sentence are both updated.
    - `git diff --stat -- src src-tauri/src` is EMPTY — this plan changes no code (asserted by the
      verify command).
    - Every file:line anchor quoted in either worked example was re-confirmed against current source
      at this authoring, not copied from a prior document.
  </acceptance_criteria>
  <done>The standing reference carries seven defect-class tests, each in the same shape, each paid for by a named finding; its heading, preamble, standing rule and defect tally all agree with that count; and the two capture-integrity rules that landed in `db08bbfc6` are intact and un-duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Map every RERUN-4 finding to the test or capture rule that now catches it — and name any that nothing catches</name>
  <files>.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md</files>
  <read_first>
    - The file as Task 1 left it — this task audits whether the additions actually cover what they
      claim to cover.
    - `.planning/phases/.../34.4.2-LIVE-GATE-RERUN-4.md`'s `## Findings recorded this run` and its
      `## ITEM VERDICT SUMMARY` — the four findings and the seven item outcomes, which are the
      population this map is drawn over.
    - `.planning/phases/.../34.4.2-PLATFORM-SCOPE.md` §5's Eleventh update — T-34.4.2-42's
      re-opened disposition and T-34.4.2-44's entry, so the map agrees with the threat roll-up.
  </read_first>
  <action>
    Add a short subsection at the END of Section 2, headed
    `### Coverage map — which test catches which measured defect`.

    Build a table with columns
    `Finding | What it was | Caught by (test or Section 3 rule) | Category`. Populate one row per
    contract-authoring defect this project has actually measured, not a hypothetical list:
    the four gate-run-2 single-requirement impossibilities (collectively, Test 1 / Test 2 / Test 3),
    F-34.4.2-11 (Test 5), F-34.4.2-14 (Test 3's SINK clause), F-34.4.2-15 (Section 3's
    exactly-ONE-app-instance bullet — capture-integrity, NOT a Section 2 test), F-34.4.2-16 (Test 6),
    F-34.4.2-17 (Test 7), F-34.4.2-18 (Section 3's preflight clause — capture-integrity).

    **State the category distinction explicitly above the table**, in the file's own voice: Section
    2's tests are authoring-time reachability tests applied to a contract's items before it runs;
    Section 3's bullets are run-time capture-integrity requirements applied to the evidence pipeline
    while it runs. A defect belongs to exactly one category, and putting a capture-integrity failure
    in the test list would make the test list look more complete than it is.

    **Then state the map's own limits, in the same register the rest of this file uses.** Record
    plainly: this list is drawn from defects this project has MEASURED, and every entry was added
    after a live run had already paid for it. Nothing here demonstrates the list is complete — the
    five-test list looked complete before RERUN-4 measured three misses against it. Name the
    measurable clause the review is held to (T-34.4.2-42: the count of structural impossibilities and
    unresolved interactions encountered during a run should be zero) and state that the count is
    recorded per run whatever it is, so the next completeness gap surfaces as a number rather than
    as a surprise.

    **Do not claim any test is now proven effective.** The two new tests have caught nothing yet —
    they are derived from findings, not validated against a run. Say so.
  </action>
  <verify>
    <automated>R=.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md && grep -q '^### Coverage map' "$R" && for f in 'F-34.4.2-11' 'F-34.4.2-14' 'F-34.4.2-15' 'F-34.4.2-16' 'F-34.4.2-17' 'F-34.4.2-18'; do awk '/^### Coverage map/,/^## Section 3/' "$R" | grep -q "$f" || { echo "MISSING $f"; exit 1; }; done && grep -q 'T-34.4.2-42' "$R" && test "$(awk '/^### Coverage map/{c=NR} /^## Section 3/{s=NR} END{print (c>0 && s>c) ? "OK" : "BAD"}' "$R")" = "OK" && git diff --quiet -- src src-tauri/src && echo COVERAGE_MAP_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `### Coverage map — which test catches which measured defect` exists at the end of Section 2,
      before Section 3 begins (asserted by the verify command's line-order check).
    - The table has one row per MEASURED contract-authoring defect and names, for each, the test or
      Section 3 rule that catches it and its category (authoring-time test vs. run-time
      capture-integrity rule).
    - F-34.4.2-11, -14, -15, -16, -17 and -18 all appear in the file (asserted by the verify command).
    - F-34.4.2-15 and F-34.4.2-18 are attributed to Section 3 rules, NOT to Section 2 tests, and the
      category distinction is stated in prose above the table.
    - The map's own limits are stated: the list is derived from measured defects only, nothing here
      shows it is complete, and the five-test list looked complete before RERUN-4 measured three
      misses against it.
    - T-34.4.2-42's measurable clause is named and the per-run scorecard requirement restated
      (asserted by the verify command).
    - The file states that Tests 6 and 7 have caught nothing yet and are derived rather than
      validated.
    - `git diff --stat -- src src-tauri/src` is EMPTY.
  </acceptance_criteria>
  <done>Every contract-authoring defect this project has measured maps to exactly one named test or capture rule in the right category; the two categories are distinguished in prose so a capture-integrity failure is never counted as review coverage; and the map states its own limits instead of implying completeness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a review's test list → the defect space it claims to cover | A list derived from measured defects always looks complete until the next run finds a class it never had. Held by the coverage map's own stated limits and by T-34.4.2-42's per-run scorecard. |
| an authoring-time test → a run-time capture rule | Counting a capture-integrity failure as review coverage inflates the review's apparent completeness. Held by the explicit category column. |
| this reference → every future gate contract in this repo | It is project-standing, not phase-scoped: a wrong or vague test here costs every future gate run, not just this phase's. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.2-42 | Repudiation (a contract demanding evidence its surface cannot produce, wasting a live run) | the Structural Reachability Review's own completeness | mitigate — gap re-opened by RERUN-4, addressed here | Two new defect-class tests derived directly from the two classes RERUN-4 measured (F-34.4.2-16, F-34.4.2-17), each in the same five-part shape as Tests 1-5. Measurable clause unchanged and restated: the count encountered during the next run is recorded whatever it is. The plan explicitly does NOT claim the new tests are validated. |
| T-34.4.2-44 | Repudiation (a partially-captured run looks successfully measured) | Section 3's capture-integrity rules | mitigate — already landed (`db08bbfc6`), verified un-duplicated here | This plan's verify command asserts the exactly-ONE-app-instance bullet and the preflight clause each appear exactly once. Re-adding them would not be harmless: a duplicated rule invites divergent edits later. |
| T-34.4.2-25 / T-34.4.2-26 | Repudiation (a result back-filled from expectation; a green suite mistaken for live proof) | this plan | mitigate — CLOSED by scope | This plan writes no gate contract, runs no gate item, launches nothing, and fills no result field anywhere. It cannot back-fill a result it has no document to write into. |
| T-34.4.2-SC | Tampering (dependency surface) | none | mitigate — CLOSED | Documents-only: no `npm`/`pip`/`cargo` install, no source change. `git diff --stat -- src src-tauri/src` must be EMPTY at plan end, asserted by both tasks' verify commands. |
</threat_model>

<verification>
- Section 2 carries seven tests; the heading, preamble, Section 1 standing rule and Section 1 defect tally all say seven.
- Tests 1-5 survive; Tests 6 and 7 each carry what-it-asks, the general defect shape, and a worked example naming its paying finding with a source or command citation.
- Section 3's exactly-ONE-app-instance bullet and preflight clause each appear exactly once.
- The coverage map exists at the end of Section 2, distinguishes authoring-time tests from run-time capture rules, and states its own limits.
- No claim is made that Tests 6 or 7 have been validated by a run.
- `git diff --stat -- src src-tauri/src` is EMPTY.
</verification>

<success_criteria>
The next gate contract's author applies a review that would have caught two of the three defects that
cost RERUN-4 its evening — a premise resting on state the preflight never checked, and a gesture
sequence the shipped UI forbids — and can see, from the reference itself, which measured defect each
test exists for, which category it belongs to, and that the list's completeness is an open question
measured per run rather than an assumption.
</success_criteria>

<output>
Create `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-21-SUMMARY.md` when done.
Record: the verbatim text of Test 6 and Test 7 as authored, since plan 34.4.2-23 must apply them
literally; the coverage map as a table; every file:line anchor re-confirmed at this authoring with
the value actually observed (and any that had moved since the source document quoted it); the
confirmation that Section 3's two capture-integrity rules from `db08bbfc6` are present and
un-duplicated; and the count of stale "five tests" references found and fixed. State explicitly that
this plan authored no gate contract, ran no gate item, launched nothing, changed no source, and that
Tests 6 and 7 are derived from measured findings but not yet validated by any run.
</output>
