---
phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field
plan: 15
subsystem: docs/gate-authoring
tags: [documentation, live-gate, threat-model, macos, tauri, gap-cycle-3]

# Dependency graph
requires:
  - phase: 34.4.2 (plans 13-14)
    provides: the in-field autofill glyph deletion (D-A) and the single-flight guard (T-34.4.2-39/-41) this plan's contract is built around
provides:
  - "login-window-ux-macos.md corrected at the point of use: Recommendation #4/§4 marked FALSIFIED (F-34.4.2-09), Recommendation #1/§§1-2 marked SUPERSEDED (D-C), a Current-status block at the top, the menu-shape explanation labelled HYPOTHESIS and cross-referenced to the file's own pre-existing warning bullet, §3's AutoFill-search-box claim corrected to order-dependent"
  - "deferred-items.md folded in: F-34.4.2-07/-08/-09/-10, T-34.4.2-39 cross-reference, and the four-instance contract-authoring-defect pattern, each with an explicit disposition"
  - "34.4.2-LIVE-GATE-RERUN-3.md: a fresh six-item blocking gate contract (verdict null), rebuilt around what plans 13/14 actually changed rather than inheriting gate run 2's PASSes, carrying its own Structural Reachability Review section before the Items"
  - "34.4.2-PLATFORM-SCOPE.md's eighth §5 threat-register update mapping every open threat to its discharging item in the new contract"
  - "A dated REQUIREMENTS.md note on REQ-34.4.2-09 naming the new contract as the record of truth, box left unticked"
affects: [34.4.2-16 (the only plan permitted to run this contract and write its results)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Contract-authoring-as-a-defect-surface: a dedicated Structural Reachability Review section, applying four defect-class tests (origin/scheme, concurrency, log-line emitter, absence-observability) to every item/sub-check/precondition BEFORE any live run, rather than discovering impossibilities during execution"
    - "Fix-before-ship in a review, not just a finding: the review is required to rewrite or remove any IMPOSSIBLE row it finds within the SAME plan, not defer the fix to the runner"

key-files:
  created:
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-LIVE-GATE-RERUN-3.md
  modified:
    - .claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/deferred-items.md
    - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
    - .planning/REQUIREMENTS.md

key-decisions:
  - "Item 5's originally-planned 'Arm 2' (a second login attempted via UI click while a sheet is already presented) was found IMPOSSIBLE during the Structural Reachability Review and fixed in this same plan, before any live use -- a presented sheet is application-modal to the ONLY window (`main`) that hosts every login trigger in this app (confirmed: no tray/menu-bar/secondary-window login entry point exists), so a human operator cannot click a second sign-in control while one is up. Replaced with a record-only observation that the item's single remaining scenario (Amazon-then-Humble during the nile spawn delay) very likely exercises both branches of the refuse-or-arm check's incumbent lookup anyway, given the sub-second sheet-attach timing vs. the ~7-8s nile delay."
  - "Item 6(a)'s 'not refused by the single-flight guard' and item 6(b)'s Epic-exemption sub-checks were found to be STRUCTURALLY GUARANTEED BY CONSTRUCTION (both sit behind an `if visible == true` gate / Epic's own early return that a hidden window or Epic can never reach), not genuinely falsifiable live outcomes -- left in the contract as corroboration, but the review records this nuance so a live PASS is read correctly as 'no regression removed the gate,' not as a novel discovery."
  - "DummyStore harness and its precondition are REMOVED from the new contract, not deferred or started-and-unused -- `login_window_url_arg`'s https-only gate (main.rs:926, test at main.rs:5725) makes it unreachable from any Tauri-managed login sheet, exactly the cause behind two of gate run 2's own four contract-authoring defects."

requirements-completed: []

# Metrics
duration: ~12min
completed: 2026-08-05
---

# Phase 34.4.2 Plan 15: Correct the Falsified Skill Reference, Fold the Ledger, Author and Review the Next Gate Contract Summary

**Corrected `login-window-ux-macos.md`'s two stale recommendations at the point of use, folded gate
run 2's five orphaned findings into `deferred-items.md`, and authored `34.4.2-LIVE-GATE-RERUN-3.md`
(a fresh six-item contract, `verdict: null`) whose own Structural Reachability Review found and
fixed one genuinely IMPOSSIBLE sub-check (Item 5's UI-driven "Arm 2") before any live run could
have discovered it the hard way.**

## Performance

- **Duration:** ~12 min (22:18:05 -> 22:29:42, per commit timestamps)
- **Tasks:** 3/3 completed
- **Files modified:** 4 (1 new, 3 modified)

## Accomplishments

- **Task 1.** Rewrote `login-window-ux-macos.md` so a session reading it meets the correction
  BEFORE it reaches either stale recommendation: a new "Current status (2026-08-05)" block near the
  top; Requirement #4 and §4 marked FALSIFIED (F-34.4.2-09), with the real-vs-synthesized
  discriminator stated explicitly and the menu-appearance-vs-fill distinction named; Requirement #1
  and §§1-2 marked SUPERSEDED (D-C), noting the child-window mechanism's own live-broken measurement
  (F-34.4.2-01/-02) so it is not read as a safe fallback; the synthesized-vs-real menu-shape
  explanation labelled a HYPOTHESIS and cross-referenced to the file's own pre-existing "Trusting a
  synthesized-event negative" bullet; and §3's AutoFill-search-box claim corrected from "reproduced
  in both surfaces" to order-dependent (first field searched works, second beeps), reproduced in
  both orderings. Folded F-34.4.2-07/-08/-09/-10, the T-34.4.2-39 cross-reference, and the
  four-instance contract-authoring-defect pattern into `deferred-items.md`'s established
  found/symptom/scope/disposition format; F-34.4.2-06 cross-referenced to plan 14's existing entry,
  not duplicated.
- **Task 2.** Authored `34.4.2-LIVE-GATE-RERUN-3.md` from scratch: six items rebuilt around what
  plans 13 (glyph deletion) and 14 (single-flight guard) actually changed. Item 3 replaces the
  deleted glyph mechanism with an absence proof (zero log lines, an env var with no reader, a
  visual check); item 4 raises the bar (a real Humble login must COMPLETE using paste alone, since
  it is now the sole credential-entry route); item 5 is entirely new (single-flight initiation,
  T-34.4.2-39/-41's first-ever assigned discharge); items 1/2/6 carry forward with their evidence
  re-derived against current source. The DummyStore harness and its precondition are removed
  outright (not started-and-unused, as gate run 2 left it) since the shell's own https-only gate
  makes it unreachable. Every log literal any item requires was grep-confirmed in current
  `src-tauri/src/main.rs`/`src/backend` source with its line number recorded inline.
- **Task 3.** Added a `## Structural Reachability Review` section, positioned before the Items,
  applying four defect-class tests (origin/scheme, concurrency, log-line emitter,
  absence-observability) to every item, sub-check and precondition in a source-cited table. Found
  and fixed ONE row marked IMPOSSIBLE in the same plan (Item 5's originally-planned "Arm 2" — see
  Key Decisions) and flagged two further rows as structurally-guaranteed-rather-than-live-falsifiable
  (also Key Decisions) without changing them. Cross-checked all ten REQ-34.4.2 IDs for coverage (no
  hole found; REQ-04/-05/-09 remain unticked). Propagated to `.planning/REQUIREMENTS.md` (a dated
  note naming the new contract as the record of truth, no box ticked) and appended an EIGHTH
  `34.4.2-PLATFORM-SCOPE.md` §5 update table mapping every open threat to its discharging item in
  the new contract (all seven prior tables retained).

## Task Commits

Each task was committed atomically:

1. **Task 1: Correct the falsified skill reference, fold gate run 2's findings into the ledger** -
   `89552fecc` (docs)
2. **Task 2: Author `34.4.2-LIVE-GATE-RERUN-3.md`, six items, results null** - `c09e1f4dd` (docs)
3. **Task 3: Structural Reachability Review, propagate to REQUIREMENTS/PLATFORM-SCOPE** -
   `e11e33ce8` (docs)

## Files Created/Modified

- `.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md` - Current-status
  block, Requirement #4/§4 FALSIFIED, Requirement #1/§§1-2 SUPERSEDED, HYPOTHESIS labelling,
  order-dependent AutoFill-search-box correction
- `.planning/phases/34.4.2-.../deferred-items.md` - new "Plan 15" section: F-34.4.2-07/-08/-09/-10,
  T-34.4.2-39 cross-reference, the contract-authoring-defect-pattern entry
- `.planning/phases/34.4.2-.../34.4.2-LIVE-GATE-RERUN-3.md` - NEW: six-item contract, `verdict:
  null`, Structural Reachability Review before the Items
- `.planning/phases/34.4.2-.../34.4.2-PLATFORM-SCOPE.md` - eighth §5 threat-register update table
- `.planning/REQUIREMENTS.md` - dated note on REQ-34.4.2-09's row; REQ-04/-05/-09 remain `[ ]`

## Decisions Made

See `key-decisions` in frontmatter for the full reasoning on all three: Item 5's Arm 2 removal,
the two structurally-guaranteed sub-checks left in place as corroboration, and the DummyStore
harness's outright removal rather than deferral.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] A concurrent session's commit/amend on this shared (non-worktree) tree
dropped Task 3's staged changes from git history between staging and commit**
- **Found during:** Task 3, attempting to commit `34.4.2-LIVE-GATE-RERUN-3.md`/
  `34.4.2-PLATFORM-SCOPE.md`/`.planning/REQUIREMENTS.md`
- **Issue:** This plan executed on the main working tree (sequential mode, no worktree isolation),
  which another concurrent agent session (`quick-260805-v1f`, a Discord/GitHub-Sponsors sidebar
  removal, unrelated to this phase) was also committing to at the same time. That session's own
  `git add -A` swept my staged Task 3 files into one of its own commits (`def0e6838`), then
  immediately amended/replaced that commit with a new one (`d53358218`, same parent, same message,
  23 seconds later) whose tree did NOT include my Task 3 files — orphaning `def0e6838` and silently
  dropping my staged changes from the branch tip, though the content remained correct on disk in
  my own working tree (confirmed via `git diff HEAD` immediately after).
- **Fix:** Did NOT attempt any destructive git recovery (no reset, no rebase, no touching the other
  session's commits). Re-ran `git add` on exactly the three files this task owns and created a
  fresh, new commit (`e11e33ce8`) on top of the current tip — per the git safety protocol's own
  "always create NEW commits" rule. Verified the re-committed diff was byte-identical to what I had
  authored (`git diff --cached --stat` matched exactly: 163 insertions across the three files) before
  committing.
- **Files affected:** none of my own files were lost or corrupted; only the commit history sequence
  was affected (Tasks 1 and 2's commits, `89552fecc` and `c09e1f4dd`, were unaffected and remain
  intact in history).
- **Verification:** `git log --oneline` confirms all three of this plan's task commits present;
  `git diff HEAD -- <each file>` empty after the recovery commit; Task 3's own verify command
  re-run and passed (`REVIEW_COMPLETE_NO_RESULTS_WRITTEN`).
- **Committed in:** `e11e33ce8`

---

**Total deviations:** 1 auto-fixed (Rule 3 - blocking, a git-concurrency race on the shared
working tree, not a plan-content defect)
**Impact on plan:** None on the substance of this plan's own three deliverables — all task content
is exactly as authored, verified against the current tip after recovery. Worth flagging for the
orchestrator: this session ran on the main tree (`isolation` was not `worktree` for this executor),
so a second concurrent agent's commits could interleave with this one's; no data was lost this
time, but a future concurrent session amending its own commits mid-flight is a real risk on a
shared tree.

## Issues Encountered

None beyond the git-concurrency deviation above. All three tasks' own verify commands passed on
first execution.

## User Setup Required

None - no external service configuration required.

## Explicit Statement Per This Plan's Own `<output>` Requirement

**This plan ran NO gate item, started NO harness, never launched `npm run tauri:dev`
(`lsof -nP -iTCP:17940 -sTCP:LISTEN` returns nothing), left every result field in
`34.4.2-LIVE-GATE-RERUN-3.md` `null`/blank, ticked NO requirement box (REQ-34.4.2-04/-05/-09 all
remain `[ ]`), and changed NO source (`git diff --stat -- src src-tauri/src` is empty).**

**The Structural Reachability Review's own outcome, restated:** one row (Item 5's originally-planned
"Arm 2") was found IMPOSSIBLE and fixed in this same plan before any live use — a presented sheet is
application-modal to the only window hosting every login trigger in this app, confirmed by grep
(`humbleStartLogin`/`captureOAuthLogin` call sites live only inside `src/frontend/screens/WebView`
and `GlobalState.tsx`; no tray-menu login trigger exists). Two further rows (item 6(a)'s
not-refused-by-guard sub-check, item 6(b)'s Epic-exemption sub-check) were found to be structurally
guaranteed by construction rather than genuinely live-falsifiable, and are left in place as
corroboration with that nuance recorded explicitly. Everything else reviewed REACHABLE, with every
required log literal grep-confirmed against current source and its line number recorded inline in
the contract itself.

**REQ-to-item coverage mapping** (see Task 3's own Requirement-coverage cross-check table in
`34.4.2-LIVE-GATE-RERUN-3.md` for the full version): REQ-01/-02 -> Item 1; REQ-03 -> Item 2;
REQ-04 -> Item 3 (primary) + Item 6(a) (corroborating); REQ-05 -> Item 3; REQ-06 -> Item 4; REQ-07
-> source-closed, not gate-dependent, already `[x]`; REQ-08 -> source-closed, not gate-dependent,
already `[x]`; REQ-09 -> this document as a whole, gate-dependent, unticked pending plan 16's
measured result; REQ-10 -> Item 6(b), already `[x]` (source-closed), live re-confirmation only.

**Phase 34.4.2 remains NOT CLOSED.** The phase does not close on this plan's own work — a contract,
however carefully reviewed, is not a measured result. Plan 34.4.2-16 is the only plan permitted to
run `34.4.2-LIVE-GATE-RERUN-3.md` and write any value into its `Observed:`/`Verdict:` fields or its
frontmatter's `verdict`/`run_date`/`items_passed`.

## Next Phase Readiness

- `34.4.2-LIVE-GATE-RERUN-3.md` is ready to run: six items, every log literal grep-confirmed against
  current source, no known-impossible sub-check left in it, DummyStore harness correctly excluded.
- Plan 16's own preflight must independently confirm build freshness against Plans 13/14/15's
  commits before launching `npm run tauri:dev` — this cycle changed `src-tauri/src/main.rs`
  materially (Plans 13/14), so a stale-binary check is not optional.
- The corrected `login-window-ux-macos.md` is now safe for any future session to consult without
  being misled back toward either dead mechanism.

---
*Phase: 34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field*
*Completed: 2026-08-05*

## Self-Check: PASSED

`34.4.2-LIVE-GATE-RERUN-3.md` confirmed present on disk. All three task commits (`89552fecc`,
`c09e1f4dd`, `e11e33ce8`) confirmed present in `git log`. No claims found missing.
