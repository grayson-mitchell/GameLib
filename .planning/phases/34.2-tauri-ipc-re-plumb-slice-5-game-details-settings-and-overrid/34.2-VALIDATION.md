---
phase: 34.2
slug: tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-25
updated: 2026-07-25
---

# Phase 34.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `34.2-RESEARCH.md` § Validation Architecture and the seven plans' own
> `<verify><automated>` commands. Reconciled post-execution by plan **34.2-07 Task 2**
> against all seven SUMMARY.md files — every row below reflects what actually ran, not
> what was planned. Where the shipped command drifted from the plan's own text, the drift
> is recorded in a note rather than silently corrected.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29.x (project-wide, config via `package.json`) — unchanged by this phase |
| **Config file** | existing project Jest config — unchanged by this phase |
| **Quick run command** | `npx jest src/backend/sidecar/__tests__/gameDetailsFlows.test.ts` |
| **Full suite command** | `pnpm test:ci` (`jest --runInBand --silent`) |
| **Rust-side gate** | `cd src-tauri && cargo check --quiet` — compile gate for the D-10 `LONG_RUNNING_CHANNELS` string, AND a NEGATIVE gate (plan 34.2-05) proving the `metadataChanged` push added no Rust arm |
| **Type gate** | `npx tsc --noEmit` — every extraction and registration task runs it |
| **Structural test containment (gap cycle 3, plan 34.2-19)** | `src/backend/jest.setupContainment.ts`, wired via a `setupFiles` entry in `src/backend/jest.config.js`. Applies to every suite under `src/backend` — the whole backend jest project, not only `sidecar/__tests__` — because the defect it closes (CR-01 of `34.2-REVIEW-GAP-CYCLE-2.md`: a suite that omits a per-suite containment mock destroys real developer data) is a property of ANY suite that reaches `os.homedir()`/`getBaseLogPath()`/`resolveAppDataDir()` transitively, not just the sidecar directory; a narrower, directory-scoped registration would have left the exact opt-out-by-omission gap this mechanism exists to close open for any future non-sidecar backend suite. Redirects `os.homedir()` via a narrow `jest.mock('os', ...)` (only `homedir` overridden) plus eight HOME/XDG/APPDATA/LOCALAPPDATA env vars, before any suite's own imports run. Self-proven by `structuralContainment.test.ts` (6 tests, fails 5/6 with the `setupFiles` entry disabled) and a local tripwire in `bootstrap.test.ts`. |
| **Rust behavioral gate (gap cycle 3, plan 34.2-22)** | `cd src-tauri && cargo test` — proves `timeout_for()` genuinely consults `LONG_RUNNING_CHANNELS` (6 tests, bidirectionally falsifiable). This project's CI runs NO `cargo test`/`cargo check` step at all, so this command is MANUAL; its existence and the presence of genuine behavioral coverage (not merely the `#[cfg(test)]` attribute) are pinned from the jest side by `src/backend/__tests__/longRunningChannels.test.ts`, which fails if the module, its `timeout_for` references, or its `LONG_RUNNING_CHANNELS` iteration disappear. |
| **Structural test containment — dual specifier (gap cycle 4, plan 34.2-25)** | Extends `src/backend/jest.setupContainment.ts` to register a single hoisted `mockOsFactory` against BOTH `jest.mock('os', ...)` and `jest.mock('node:os', ...)`, closing the specifier-dependent hole CR-02 found (the bare `'os'` specifier was contained, `'node:os'` was not). Also replaces the predictable pid-derived containment root with `mkdtempSync(...) + chmodSync(0o700)` (WR-07) and adds a `setupFiles`-time precondition that THROWS before any suite's own module-scope imports run if either specifier resolves outside `containmentRoot` (WR-10 setup half) — a real preventer, not a per-suite tripwire that only detects after the fact. Self-proven by `structuralContainment.test.ts` Tests 7-9 (node:os identity, userInfo identity, a source gate forbidding future `node:os` homedir/userInfo imports outside a 3-entry declared allowlist). |
| **Stub-free call-site proof convention (gap cycle 4, plan 34.2-26)** | `src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts` replaces the four gap-cycle-3 WR-02 tests, which had `jest.spyOn`-replaced `logger.logError` with a promise-returning shape that never occurs at runtime (CR-01: `logError()` is actually a block-body arrow returning `undefined`). The new suite never spies on or mocks the logger module under test (`grep -cE "jest\.(spyOn\|mock)\(.*logger'" ... → 0`); Test A reproduces a REAL `ENOTDIR` rejection via a regular file written where a directory is expected, Test B reproduces a REAL synchronous throw (the "heroicLogWriter unset until bootstrap init" gotcha), both proving the new `logErrorSettled` sibling export's `.catch` guard is reachable in production. |
| **Rust presence gate — comment-aware (gap cycle 4, plan 34.2-28)** | `hasBehavioralRustTestModule` in `src/backend/__tests__/longRunningChannels.test.ts` now requires a real `assert_eq!(timeout_for(` match on a Rust-comment-stripped `#[cfg(test)]` region, not a bare `timeout_for` identifier count on raw source (WR-04's comment-only counter-example previously satisfied the gate with zero real assertions). Also adds an even-quote-count guard over `loadMainRsCode()`'s stripped output, self-tested with a synthetic `steam://`-shaped string literal, guarding against the stripper silently truncating a future code line containing an embedded `//` (WR-08). |
| **Cross-project containment scope gate — Block E (gap cycle 4, plan 34.2-29)** | New Block E in `testContainment.test.ts` recursively walks `src/frontend`, `src/common`, `src/preload`, and `meta`, asserting no `*.test.ts(x)` file there imports a containment-relevant backend module (`backend/logger`, `backend/logger/paths`, `backend/constants/paths`, `backend/sidecar/pathShim`) — converting `jest.config.js`'s hand-maintained "reachable only from `src/backend`" scope claim (CR-02 secondary instance) into an enforced invariant. Also closes Block D's two vacuity defects: WR-01 (the `HOME`-assignment gate now matches comment-stripped, not raw, source) and WR-02 (a new `hasContainmentOsMock` predicate gates the `os`/`node:os` `jest.mock` mechanism itself, not only the eight defence-in-depth env-var assignments); and WR-05 (Block C's two self-tests now build fully synthetic input instead of the live directory listing). |
| **Estimated runtime** | plan durations: 34.2-01 ~75min, 34.2-02 9min, 34.2-03 5min, 34.2-04 ~50min, 34.2-05 ~25min, 34.2-06 ~35min (commit-to-commit; targeted suites complete in low single-digit seconds each); gap cycle 3: 34.2-19 ~100min, 34.2-20 8min, 34.2-21 ~15min, 34.2-22 ~10min, 34.2-23 ~50min; gap cycle 4: 34.2-25 ~90min, 34.2-26 45min, 34.2-27 ~15min, 34.2-28 ~28min (+ resumed-session verification), 34.2-29 ~90min |

**No new test framework, no new dependency.** Every suite extends an existing, proven
harness (`appShellFlows.test.ts`'s black-box RPC loop, `appshellModules.test.ts`'s
direct-call shape, `appShellImportGate.test.ts`'s comment-stripped source gates).

**Full-suite tally after gap cycle 3 (plan 34.2-23's own reconciliation, `npx jest
--testPathPattern=src/backend`):** 111/112 suites passed, 2304/2305 tests passed — sole
failure the pre-existing, already-documented 34.1-era `rustInvokeChannel.test.ts`
(unchanged, see `deferred-items.md`). This is the documented baseline this plan's own
currency gate and Task 3 reconcile against; it grew from the 108/109-suite baseline
`34.2-15-SUMMARY.md` reported at the start of gap cycle 2 via three plans' worth of new
suites/tests (`structuralContainment.test.ts`, extensions to `loggerFlows.test.ts` and
`repairFailure.test.ts`, the `longRunningChannels.test.ts`/`testContainment.test.ts`
extensions), not via any change to the pre-existing failure.

---

## Sampling Rate

- **After every task commit:** the task's own targeted `npx jest <file>` (plus
  `npx tsc --noEmit` for the extraction/registration tasks).
- **After every plan wave:** the cross-file sweep
  `npx jest gameDetailsFlows|enrichmentFlows|bootstrapWirings|gameDetailsImportGate|gameDetailsModules|knownFixes|longRunningChannels`,
  plus the regression sweep
  `npx jest appShellFlows|settingsFlows|downloadQueueFlows|bootstrap|electronUntouched`.
- **Before `/gsd-verify-work`:** `pnpm test:ci` full suite green **plus**
  `cd src-tauri && cargo check --quiet` green **plus** `npx tsc --noEmit` clean.
- **Max feedback latency:** ~15 seconds for targeted runs — confirmed by the plans' own
  recorded suite sizes (largest targeted suite this phase, `enrichmentFlows.test.ts`, is
  28 tests).

---

## Per-Task Verification Map

*One row per task; 20 tasks across the original seven plans (3+3+3+3+2+3+3), plus 11 more
across gap cycle 3's five plans (3+2+2+2+2) — 31 tasks total across twelve plans, plus 13 more
across gap cycle 4's five code-changing plans (3+3+2+2+3, plans 34.2-25..34.2-29) — 44 tasks
total across seventeen plans. Plan 34.2-30 (this doc-only reconciliation plan) does not add its
own rows, matching the precedent set by plan 34.2-24 (gap cycle 3's own doc-only closing plan,
which likewise added zero rows for itself). Reconciled against all seventeen SUMMARY.md files.
Status legend: ✅ green · ❌ red · ⚠️ flaky.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 34.2-01-01 | 01 | 1 | REQ-34.2-02 | T-34.2-02 / T-34.2-03 | Locale path is constant-derived, never IPC input; a missing locales dir logs loudly; the init promise cannot reject unhandled and kill the sidecar | source gate + typecheck | `npx tsc --noEmit` + `node -e` bootstrap source assertions | ✅ modifies existing (`bootstrap.ts`) | ✅ green (commit `a8e7c809`) |
| 34.2-01-02 | 01 | 1 | REQ-34.2-07, REQ-34.2-04 | T-34.2-01 / T-34.2-04 | Listener registered exactly once; the underlying module is imported, never `anticheat/ipc_handler.ts` | source gate + typecheck | `npx tsc --noEmit` + `node -e` bootstrap source assertions | ✅ modifies existing (`bootstrap.ts`) | ✅ green (commit `910e8b40`) |
| 34.2-01-03 | 01 | 1 | REQ-34.2-02, REQ-34.2-07, REQ-34.2-04 | T-34.2-05 | Test writes only under `os.tmpdir()`; i18next/backendEvents/releases/anticheat are NOT mocked | unit, **non-mocked singletons** | `npx jest src/backend/sidecar/__tests__/bootstrapWirings.test.ts` | ✅ found | ✅ green (commit `8ad8f5e5`, 7 tests) |
| 34.2-02-01 | 02 | 1 | REQ-34.2-01, REQ-34.2-03, REQ-34.2-09 | T-34.2-08 / T-34.2-09 | No `electron`/`backend/ipc` import reaches the new directory; bodies relocated verbatim so no validation is dropped in transit | source gate + typecheck | `npx tsc --noEmit` + `node -e` 15-export/forbidden-import assertions | ✅ found | ✅ green (commit `bcc0921a`) |
| 34.2-02-02 | 02 | 1 | REQ-34.2-03, REQ-34.2-08, REQ-34.2-10 | T-34.2-07 / T-34.2-09 | `requestGameSettings`'s bottle fix untouched; the push seam avoids importing `backend/ipc` | source gate + typecheck | `npx tsc --noEmit` + `node -e` delegation/kind/D-09 assertions | ✅ modifies existing (`main.ts`) | ✅ green (commit `3f04b72e`) — drift: an unrelated pre-existing unused `backendEvents` import in `main.ts` was removed (Rule 3, blocking on this task's own eslint acceptance criterion), not planned |
| 34.2-02-03 | 02 | 1 | REQ-34.2-01, REQ-34.2-03, REQ-34.2-08, REQ-34.2-09, REQ-34.2-14 | T-34.2-08 | Directory-wide no-electron gate with a self-test so it cannot pass vacuously | unit + source gate | `npx jest src/backend/gamedetails/__tests__/gameDetailsModules.test.ts` | ✅ found | ✅ green (commit `27319e28`, 28 tests) |
| 34.2-03-01 | 03 | 2 | REQ-34.2-05 | T-34.2-10 | Extraction does not become a circular re-export; `launcher.ts` stays out of the sidecar graph | source gate + typecheck | `npx tsc --noEmit` + `node -e` extraction/import-back assertions | ✅ found (`knownFixes.ts`) | ✅ green (commit `f03f95d3`) |
| 34.2-03-02 | 03 | 2 | REQ-34.2-06 | T-34.2-12 | `ipc_handler.ts` neither defines nor re-exports the function; the D-16 three-state contract survives | unit + source gate | `npx jest src/backend/crossover_index/__tests__/ratingMap.test.ts` + `node -e` re-export/no-addHandler assertions | ✅ retargets existing | ✅ green (commit `137a522d`) — drift: `main.ts`'s second, plan-undocumented `buildCrossoverRatingMap` import site (`refreshCrossoverRatingMap()`) also required redirecting (Rule 3, blocking compile fix) |
| 34.2-03-03 | 03 | 2 | REQ-34.2-05 | T-34.2-11 / T-34.2-13 | Malformed JSON degrades to `null`+warn; suite writes only under `os.tmpdir()` | unit | `npx jest src/backend/__tests__/knownFixes.test.ts` | ✅ found | ✅ green (commit `99cd1450`, 5 tests) |
| 34.2-04-01 | 04 | 2 | REQ-34.2-01, REQ-34.2-09, REQ-34.2-10 | T-34.2-17 / T-34.2-18 | Exactly 15 `handle` and 0 `on`; no `ipc_handler`/`backend/ipc` import; `settingsFlowRegistration.ts` untouched | source gate + typecheck | `npx tsc --noEmit` + `node -e` kind-count and forbidden-import assertions | ✅ found (`gameDetailsFlowRegistration.ts`) | ✅ green (commit `cd115f98`) |
| 34.2-04-02 | 04 | 2 | REQ-34.2-01, REQ-34.2-02, REQ-34.2-08, REQ-34.2-09, REQ-34.2-14 | T-34.2-14 / T-34.2-16 | Object-argument channels receive their object intact; unported channels still return the marker (Invariant B) | unit (black-box sidecar RPC), **i18next NOT mocked** | `npx jest src/backend/sidecar/__tests__/gameDetailsFlows.test.ts` | ✅ found | ✅ green (commit `45ecaf6c`, 22 tests) — drift: 3 Rule-1 test-infrastructure bugs found and fixed during this task's own mandated RED spot-check and cross-plan regression sweep (`settingsFlows.test.ts` Invariant B guard narrowed; `jest.unmock('i18next')` added after the automock made the first D-02 RED check vacuous; `isOnline()` mock re-armed under `resetMocks: true`) — see `34.2-04-SUMMARY.md` |
| 34.2-04-03 | 04 | 2 | REQ-34.2-03, REQ-34.2-10, REQ-34.2-14 | T-34.2-17 / T-34.2-18 | Curated-import, delegation-shape and transport-kind gates; two do-not-touch files pinned byte-identical to HEAD | by-construction source gate | `npx jest src/backend/sidecar/__tests__/gameDetailsImportGate.test.ts` | ✅ found | ✅ green (commit `b35b31a8`, 47 tests) |
| 34.2-05-01 | 05 | 3 | REQ-34.2-01, REQ-34.2-08, REQ-34.2-09 | T-34.2-20 / T-34.2-21 | All 3 send bodies contained against process-killing rejections; zero new Rust arms | source gate + **negative** Rust gate | `npx tsc --noEmit` + `node -e` 3-on/15-handle assertions + `cd src-tauri && cargo check --quiet` | ✅ modifies existing | ✅ green (commit `51fb141d`) |
| 34.2-05-02 | 05 | 3 | REQ-34.2-01, REQ-34.2-08, REQ-34.2-09 | T-34.2-20 / T-34.2-21 / T-34.2-22 | **Positive side-effect proof only** — store read-back, manager call, transport frame; forced throw leaves the sidecar answering `health`; one listener per channel | unit (black-box sidecar RPC) | `npx jest src/backend/sidecar/__tests__/gameDetailsFlows.test.ts` | ✅ extends 34.2-04-02's file | ✅ green (commit `07c026bf`, 9 new tests, 31 total in file) — no deviation: "plan executed exactly as written" per `34.2-05-SUMMARY.md` |
| 34.2-06-01 | 06 | 3 | REQ-34.2-04, REQ-34.2-11 | T-34.2-27 / T-34.2-28 | storeSearch rethrow contract reproduced 3×; no `ipc_handler` import; no duplicate `releasesInfoReady` listener | source gate + typecheck | `npx tsc --noEmit` + `node -e` 8-handle/3-rethrow/forbidden-import assertions | ✅ found (`enrichmentFlowRegistration.ts`) | ✅ green (commit `3b17962c`) |
| 34.2-06-02 | 06 | 3 | REQ-34.2-12 | T-34.2-25 | Exemption applied for a stated structural reason; `INVOKE_TIMEOUT` unchanged; set-equality pins both widening and narrowing | config-shape + **recorded manual measurement** | `cd src-tauri && cargo check --quiet` + `npx jest src/backend/__tests__/longRunningChannels.test.ts` | ✅ found | ✅ green (commit `bee6c66c`, 6 tests) — measurement recorded: `getWikiGameInfo` 1190/957/702ms live-network, NOT exempted |
| 34.2-06-03 | 06 | 3 | REQ-34.2-04, REQ-34.2-11, REQ-34.2-14 | T-34.2-23 / T-34.2-27 / T-34.2-28 | Provider failure reaches the renderer as a rejection, not an empty result; the two anticheat riders are asserted, not just documented | unit (black-box sidecar RPC) + source gate | `npx jest src/backend/sidecar/__tests__/enrichmentFlows.test.ts` | ✅ found | ✅ green (commit `0bb157fb`, 28 tests) — drift: a separate fixup commit `de1623d9` (Rule 3) resolved 2 `no-require-imports` eslint errors and a `WikiInfo` type error in the test suite, post-commit; not part of the plan's own Task 3 text but required for the plan's own lint/typecheck acceptance criteria |
| 34.2-07-01 | 07 | 4 | REQ-34.2-13 | T-34.2-29 / T-34.2-30 | Set equality against `IPC-PORT-INVENTORY.md`; 34.1's sign-off sentence cannot be pasted | doc-shape assertion | `python3` set-equality + required-declaration script (in-plan) | ✅ found (`34.2-PORTED-CHANNELS.md`) | ✅ green — script exits 0, 26/26 set equality confirmed |
| 34.2-07-02 | 07 | 4 | REQ-34.2-13 | T-34.2-29 | Exactly two named UAT items; the sign-off sentence byte-identical across both documents; no template placeholders survive | doc-shape assertion | `python3` UAT/VALIDATION structure script (in-plan) | ✅ found (`34.2-HUMAN-UAT.md`, this file) | ✅ green — script exits 0 (see Self-Check below) |
| 34.2-07-03 | 07 | 4 | REQ-34.2-13 | T-34.2-31 / T-34.2-32 | Tally reconciles at 87; the stale `libraryManagerMap` claim corrected not deleted; Accepted Constraints/Invariants byte-unchanged | doc-shape assertion | `python3` SEAM section/tally script + `git diff --stat` (in-plan) | ✅ modifies existing (`SEAM.md`) | ✅ green — executed after this row was written, per this plan's own Task 3 |
| 34.2-19-01 | 19 (gap cycle 3) | 1 | REQ-34.2-07, REQ-34.2-14 | T-34.2-60 / T-34.2-61 / T-34.2-63 / T-34.2-65 | `os.homedir()`/`getBaseLogPath()`/`resolveAppDataDir()` redirected structurally for every `src/backend` suite via `setupFiles`; per-pid tmp root, `{recursive:true}` mkdir, no teardown race | source gate + typecheck, then live evidence | `npx tsc --noEmit` + `stat` before/after real `~/Library/Logs/GameLib/gamelib.log` across a full sidecar run | ✅ found (`jest.setupContainment.ts`) | ✅ green (commit `ec800227`, superseded by correction `752f6096` — env-var-only did not redirect `os.homedir()`; `jest.mock('os', ...)` required and coordinator-approved, `a3963ed3`) |
| 34.2-19-02 | 19 (gap cycle 3) | 1 | REQ-34.2-07, REQ-34.2-14 | T-34.2-62 / T-34.2-64 | Zero per-suite mocks required; `process.platform` sweep restored in a `finally` inside the test body, never `afterAll` | unit, hand RED-proofed | `npx jest src/backend/sidecar/__tests__/structuralContainment.test.ts` | ✅ found | ✅ green (commit `8c7ff195`, 6 tests — RED-proofed 5/6 failing with `setupFiles` disabled, recorded verbatim in `34.2-19-SUMMARY.md`) |
| 34.2-19-03 | 19 (gap cycle 3) | 1 | REQ-34.2-07, REQ-34.2-14 | T-34.2-60 / T-34.2-62 | The suite independently reproduced destroying real developer data three times now carries its own local containment tripwire, independent of the distant gate files | unit, live-verified | `npx jest src/backend/sidecar/__tests__/bootstrap.test.ts` + full-project `stat` check | ✅ modifies existing (`bootstrap.test.ts`) | ✅ green (commit `6ba9e532`) — live destruction check byte-identical before/after, directly refuting the verification's three-times-reproduced finding |
| 34.2-20-01 | 20 (gap cycle 3) | 1 | REQ-34.2-12, REQ-34.2-14 | T-34.2-66 / T-34.2-68 / T-34.2-70 | `logError` send listener guards its own call with `Promise.resolve(...).catch(...)`, stderr-only diagnostic, dropped over-narrow `as string` cast in favor of the declared `unknown` contract | source gate + typecheck | `npx tsc --noEmit` + `npx eslint src/backend/sidecar/loggerFlowRegistration.ts` | ✅ modifies existing (`loggerFlowRegistration.ts`) | ✅ green (commit `f4c29094`) |
| 34.2-20-02 | 20 (gap cycle 3) | 1 | REQ-34.2-12, REQ-34.2-14 | T-34.2-67 / T-34.2-69 (accept) | A rejecting `logError` write is caught at the call site, not by `processGuards.ts`'s generic absorption; NEGATIVE assertion (must not contain the process guard's generic text) | unit (black-box sidecar RPC), hand RED-proofed | `npx jest --testPathPattern="src/backend/sidecar/__tests__/loggerFlows\.test\.ts"` | ✅ extends existing (`loggerFlows.test.ts`) | ✅ green (commit `d82f43ae`, 5→9 tests — RED-proofed 4/9 failing against pre-fix code, recorded verbatim in `34.2-20-SUMMARY.md`) |
| 34.2-21-01 | 21 (gap cycle 3) | 1 | REQ-34.2-12, REQ-34.2-14 | T-34.2-71 / T-34.2-73 | Three hostile-value regression blocks (null-prototype, throwing `toString`, throwing `Symbol.toPrimitive`) plus a T-34.2-52 hostile-value dialog-message re-proof | unit, hand RED-proofed | `npx jest --testPathPattern="src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure.test.ts"` | ✅ extends existing (`repairFailure.test.ts`) | ✅ green (commit `1404250b`) — RED-proofed 10/14 failing against pre-fix code (`TypeError: Cannot convert object to primitive value`), recorded verbatim in `34.2-21-SUMMARY.md` |
| 34.2-21-02 | 21 (gap cycle 3) | 1 | REQ-34.2-12, REQ-34.2-14 | T-34.2-71 / T-34.2-72 / T-34.2-74 / T-34.2-75 | `reportRepairFailure` never throws for any error value; `errorText` precomputed via a `let`-fallback-before-try; each of the three signals independently try/catch-wrapped; `index.tsx` byte-unchanged | unit + source gate | `npx jest --testPathPattern="src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure.test.ts"` + `git diff --exit-code index.tsx` | ✅ modifies existing (`repairFailure.ts`) | ✅ green (commit `afa9bf61`, 14/14) |
| 34.2-22-01 | 22 (gap cycle 3) | 1 | REQ-34.2-12, REQ-34.2-14 | T-34.2-76 / T-34.2-77 / T-34.2-78 / T-34.2-80 | `timeout_for()` behaviorally proven to consult `LONG_RUNNING_CHANNELS` in both directions; `#[cfg(test)]` compiled out of the shipped binary | Rust behavioral unit test, hand RED-proofed both directions | `cd src-tauri && cargo test` | ✅ modifies existing (`main.rs`) | ✅ green (commit `c7c292e4`, 6 tests) — two mutations (`Some(INVOKE_TIMEOUT)` unconditional, `None` unconditional) fail DIFFERENT test sets (4 vs. 2), recorded verbatim in `34.2-22-SUMMARY.md` |
| 34.2-22-02 | 22 (gap cycle 3) | 1 | REQ-34.2-14 | T-34.2-79 | The Rust test module's existence, its `timeout_for` references, and its `LONG_RUNNING_CHANNELS` iteration are pinned from jest, since CI runs no cargo step | source gate over raw `main.rs`, with 2 self-tests | `npx jest --testPathPattern="src/backend/__tests__/longRunningChannels\.test\.ts"` | ✅ extends existing (`longRunningChannels.test.ts`) | ✅ green (commit `5598093e`, 8→14 tests) — RED-proofed by reverting `main.rs` to `HEAD~1`, 4/14 failing, recorded verbatim in `34.2-22-SUMMARY.md` |
| 34.2-23-01 | 23 (gap cycle 3) | 2 | REQ-34.2-07, REQ-34.2-14 | T-34.2-81 / T-34.2-85 / T-34.2-86 | Anti-claim gate matches RAW source (not comment-stripped); `backend/constants/environment` mock comment corrected to LOAD-BEARING; `process.platform`/env mutation moved into a per-test `finally` | unit, hand RED-proofed | `npx tsc --noEmit && npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment\.test\.ts"` | ✅ modifies existing (`testContainment.test.ts`) | ✅ green (commit `9e8d26e3`) — RED-PROOF 1 recorded verbatim in `34.2-23-SUMMARY.md` (injected claim into a docblock, gate caught it) |
| 34.2-23-02 | 23 (gap cycle 3) | 2 | REQ-34.2-07, REQ-34.2-14 | T-34.2-82 / T-34.2-83 / T-34.2-84 | `KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES` deleted; `readdirSync`-derived set-equality tripwire over the whole directory; structural-containment gate over `jest.config.js`/`jest.setupContainment.ts` | unit, hand RED-proofed (3 proofs) | `npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment\.test\.ts"` | ✅ extends existing (`testContainment.test.ts`) | ✅ green (commit `b1c1476c`, 29→42 tests) — RED-PROOFs 2 and 3 recorded verbatim in `34.2-23-SUMMARY.md` (probe file caught by the tripwire; `setupFiles` entry disabled caught by the structural gate) |
| 34.2-25-01 | 25 (gap cycle 4) | 1 | REQ-34.2-07, REQ-34.2-14 | CR-02 | Write the CR-02 detection gates (node:os identity, userInfo identity, a node:os-homedir/userInfo import source gate) FIRST and observe them RED against the unmodified containment module | unit, hand RED-proofed | `npx jest src/backend/sidecar/__tests__/structuralContainment.test.ts` | ✅ extends existing | ✅ green (commit `f05a1cbb`) — RED-proofed 2/12 (Tests 7,8) failing against HEAD, recorded verbatim in `34.2-25-SUMMARY.md` |
| 34.2-25-02 | 25 (gap cycle 4) | 1 | REQ-34.2-07, REQ-34.2-14 | CR-02, WR-07, WR-09, WR-10, WR-12 | Containment specifier-independent (`os` + `node:os`), atomic mode-0700 root, `setupFiles`-time precondition that REFUSES TO RUN on drift, `realHomeAtSetup`/`containmentRoot` exported and memoized on `globalThis` | source gate + typecheck | `npx tsc --noEmit` | ✅ modifies existing (`jest.setupContainment.ts`) | ✅ green (commit `935e316a`) — deliberate-break proof (node:os pointed at real home): whole suite REFUSED TO RUN (0 tests), recorded verbatim in `34.2-25-SUMMARY.md` |
| 34.2-25-03 | 25 (gap cycle 4) | 1 | REQ-34.2-07, REQ-34.2-14 | WR-09, WR-11, WR-12 | Test 6's anti-vacuity check anchored on `mkdtemp` root + pre-mock `realHomeAtSetup`, platform-independent (no macOS-hardcoded path, no `userInfo()` dependency); Test 4 sweeps `flatpakHome`; `assertNotContained` dead code removed | unit, hand RED-re-derived | `npx jest src/backend/sidecar/__tests__/structuralContainment.test.ts` | ✅ modifies existing | ✅ green (commit `447b17c9`, 12/12) — RED re-derivation with `setupFiles` disabled: 1/12 failing (Test 1), recorded verbatim in `34.2-25-SUMMARY.md` |
| 34.2-26-01 | 26 (gap cycle 4) | 1 | REQ-34.2-12, REQ-34.2-14 | CR-01 | Write a stub-free proof suite (never spies on/mocks `logger`) for the call-site guard and observe it RED against HEAD | unit, hand RED-proofed | `npx jest src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts` | ✅ found (new) | ✅ green (commit `2cfe5efe`) — RED-proofed 4/5 failing against HEAD, recorded verbatim in `34.2-26-SUMMARY.md` |
| 34.2-26-02 | 26 (gap cycle 4) | 1 | REQ-34.2-12, REQ-34.2-14 | CR-01 | `logErrorSettled` sibling export forwards the writer's promise; call site settles both async rejection and synchronous-throw paths via the same `.catch` | unit (real module, no stubs) | `npx jest src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts` | ✅ modifies existing (`logger/index.ts`, `loggerFlowRegistration.ts`) | ✅ green (commit `9608111a`, 5/5) — deliberate-break proof (block-body revert): whole node process crashed on uncaught ENOTDIR, a STRONGER signal than a clean test failure, recorded verbatim in `34.2-26-SUMMARY.md` |
| 34.2-26-03 | 26 (gap cycle 4) | 1 | REQ-34.2-12, REQ-34.2-14 | CR-01, WR-10 | Delete the four spy-fabricated WR-02 tests; correct the WR-10 tripwire comment (detector, not preventer); reconcile the backend baseline | unit + full-project sweep | `npx jest --testPathPattern="src/backend/sidecar/__tests__/loggerFlows\.test\.ts"` | ✅ modifies existing (`loggerFlows.test.ts`) | ✅ green (commit `8f846c3a`, 10/10) — failing-suite set exactly `{testContainment.test.ts (Block C, EXPECTED, closer=34.2-29), rustInvokeChannel.test.ts (baseline)}` |
| 34.2-27-01 | 27 (gap cycle 4) | 1 | REQ-34.2-12, REQ-34.2-14 | WR-06 | Add 5 hostile-dependency tests (logError throws, window.api absent, t throws, showDialogModal throws, T-34.2-52-under-hostility) and observe them RED against HEAD | unit, hand RED-proofed | `npx jest --testPathPattern="src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure\.test\.ts"` | ✅ modifies existing (`repairFailure.test.ts`) | ✅ green (commit `2f6c2dcb`) — RED-proofed 4/5 new tests failing against HEAD (Tests 3/4 via the throw ESCAPING the function), recorded verbatim in `34.2-27-SUMMARY.md` |
| 34.2-27-02 | 27 (gap cycle 4) | 1 | REQ-34.2-12, REQ-34.2-14 | WR-06, WR-03 (renderer half) | All three signals individually diagnosed (named `console.error` on failure, never silent); `t`/`showDialogModal` degrade to hardcoded English fallback rather than escaping; both files prettier-clean | unit + prettier | `npx jest --testPathPattern="src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure\.test\.ts"` | ✅ modifies existing (`repairFailure.ts`) | ✅ green (commit `78f9ad13`, 19/19) — deliberate-break proof (signal 2 reverted to empty catch): exactly Tests 1/2 failed, all others (incl. 3/4/5) stayed green, recorded verbatim in `34.2-27-SUMMARY.md` |
| 34.2-28-01 | 28 (gap cycle 4) | 1 | REQ-34.2-12, REQ-34.2-14 | WR-04 | `hasBehavioralRustTestModule` requires a real `assert_eq!(timeout_for(` match on comment-stripped source, not a bare identifier count on raw source | unit, hand RED-proofed (this session, no prior recorded RED) | `npx jest --testPathPattern="src/backend/__tests__/longRunningChannels\.test\.ts"` | ✅ modifies existing (`longRunningChannels.test.ts`) | ✅ green (commit `c4a22118`, 18/18) — two independent RED reproductions recorded verbatim in `34.2-28-SUMMARY.md` (pre-fix predicate counter-example; real `assert_eq!` lines deleted from `main.rs`) |
| 34.2-28-02 | 28 (gap cycle 4) | 1 | REQ-34.2-12, REQ-34.2-14 | WR-08 | Even-quote-count guard (whole-file + per-line) over `loadMainRsCode()`'s stripped output, self-tested with a synthetic `steam://` literal proving the guard can genuinely fail | unit, hand RED-proofed | `npx jest --testPathPattern="src/backend/__tests__/longRunningChannels\.test\.ts"` | ✅ modifies existing (`longRunningChannels.test.ts`) | ✅ green (commit `269b5cce`, 18/18) — self-test flips to FAILING when the fixture's embedded `//` is removed, recorded verbatim in `34.2-28-SUMMARY.md` |
| 34.2-29-01 | 29 (gap cycle 4) | 2 | REQ-34.2-07, REQ-34.2-14 | WR-01, WR-02 | Block D's env-var gate matches comment-stripped source (WR-01); a new `hasContainmentOsMock` predicate gates the `os`/`node:os` `jest.mock` mechanism itself (WR-02), each with a self-test | unit, hand RED-proofed | `npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment\.test\.ts"` | ✅ modifies existing (`testContainment.test.ts`) | ✅ green (commit `df156d56`) — WR-01 self-test RED recorded verbatim; WR-02 deliberate-break produced a STRONGER failure (whole suite refused to run via the WR-10 `setupFiles` precondition), both in `34.2-29-SUMMARY.md` |
| 34.2-29-02 | 29 (gap cycle 4) | 2 | REQ-34.2-07, REQ-34.2-14 | WR-05 | Block C's two self-tests decoupled from the live directory listing (fully synthetic input arrays); `loggerCallSiteGuard.test.ts` classified in `STRUCTURALLY_CONTAINED_SUITES` | unit, live-proofed | `npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment\.test\.ts"` | ✅ modifies existing (`testContainment.test.ts`) | ✅ green (commit `db4b58df`) — live proof (real `zzTripwireProbe2.test.ts` dropped in): exactly ONE failure, correctly named, recorded verbatim in `34.2-29-SUMMARY.md` |
| 34.2-29-03 | 29 (gap cycle 4) | 2 | REQ-34.2-07, REQ-34.2-14 | CR-02 (secondary), WR-10 | New Block E enforces `jest.config.js`'s backend-only containment scope claim against frontend/common/preload/meta; `bootstrap.test.ts`'s tripwire comment corrected (detector, not preventer) | unit, hand RED-proofed | `npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment\.test\.ts"` | ✅ modifies existing (`testContainment.test.ts`, `bootstrap.test.ts`) | ✅ green (commit `1e1a27aa`) — deliberate-break proof (added a `backend/logger/paths` import to a frontend-adjacent test file): Block E caught it naming the file/specifier, recorded verbatim in `34.2-29-SUMMARY.md` |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky. Every row above carries a concrete
`<automated>` command; no task closes on a Wave-0 dependency alone.*

---

## Wave 0 Requirements

*Checked against what actually exists on disk (`test -f`), confirmed 2026-07-25.*

- [x] `src/backend/sidecar/__tests__/bootstrapWirings.test.ts` — REQ-34.2-02/04/07, the
      **non-mocked** proof of both bootstrap wirings.
- [x] `src/backend/gamedetails/__tests__/gameDetailsModules.test.ts` — REQ-34.2-01/03/08/09,
      direct-call behavior tests for the extracted bodies + the directory-wide
      no-electron gate with its self-test.
- [x] `src/backend/__tests__/knownFixes.test.ts` — REQ-34.2-05, the relocated fs-read body.
- [x] `src/backend/sidecar/__tests__/gameDetailsFlows.test.ts` — REQ-34.2-01/08/09, the
      black-box RPC-loop suite for the 15 invoke channels (plan 34.2-04) and the 3 send
      channels (plan 34.2-05, extending the same file).
- [x] `src/backend/sidecar/__tests__/gameDetailsImportGate.test.ts` — REQ-34.2-03/10/14,
      curated-import + delegation-shape + transport-kind + do-not-touch gates. A
      genuinely new file; `electronUntouched.test.ts` remains Phase 28's narrow
      keyring/`configStore` byte-identity proof, untouched.
- [x] `src/backend/sidecar/__tests__/enrichmentFlows.test.ts` — REQ-34.2-04/11/14, the 8
      enrichment channels, the storeSearch rethrow contract, and the two anticheat riders.
- [x] `src/backend/__tests__/longRunningChannels.test.ts` — REQ-34.2-12, set-equality
      assertion over `main.rs`'s `LONG_RUNNING_CHANNELS` plus an `INVOKE_TIMEOUT`
      unchanged-at-60s assertion.
- [x] `src/backend/crossover_index/__tests__/ratingMap.test.ts` — **existing**, retargeted
      at `../crossoverRatingMap` with its `jest.mock('backend/ipc')` block deleted; test
      count grew from 6 to 7 (an anti-remerge source-gate test was added per
      `34.2-03-SUMMARY.md`) — no pre-existing assertion was dropped.
- [x] `34.2-PORTED-CHANNELS.md` / `34.2-HUMAN-UAT.md` — doc deliverables, load-bearing for
      REQ-34.2-13's honest sign-off (created by this plan, 34.2-07).

No planning-time path drifted from what shipped — all nine Wave 0 items above landed at
exactly the file paths this table originally sketched (unlike Phase 34.1, which had two
path drifts).

---

## Manual-Only Verifications

Exactly **two** items, and that is the point. Per D-11 this slice's unit claim is
genuinely strong — 26 channels of data-in/data-out with assertable return shapes,
exercised over the real sidecar RPC loop — unlike 34.1, whose headline deliverable was
visual and which therefore deferred ten items. Both are **deferred**; neither gates phase
closure (D-11). Both are recorded, with concrete reproduction steps and pass conditions,
in `34.2-HUMAN-UAT.md` as `UAT-34.2-01` and `UAT-34.2-02`.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| A translated notification string actually renders under a live Tauri boot | REQ-34.2-02 (D-02) | Jest can assert `i18next.t()` returns a real string after `init()`; it cannot see a notification, and it cannot exercise a **packaged** build's locale-path resolution — jest runs from the repo root, where `process.cwd()` happens to make `publicDir` resolve (the `publicdir-getapppath-chunking` gotcha family) | See `34.2-HUMAN-UAT.md` UAT-34.2-01 |
| The anticheat data file actually appears on disk after a live boot | REQ-34.2-07 (D-07) | Jest asserts the event fires and the file is written under a tmp home with a **mocked** axios; it cannot prove a real GitHub fetch completes in a live process across a real network after a real connectivity transition | See `34.2-HUMAN-UAT.md` UAT-34.2-02 |

**The honest boundary, stated once here and verbatim in `34.2-PORTED-CHANNELS.md` §6 and
`34.2-HUMAN-UAT.md`:** for the 26 channels, the automated tests exercise the real
extracted bodies over the real transport and assert real return values — so "wired and
unit-proven" is genuinely close to "works" for this slice, which is a stronger claim than
34.1 could make. For the two bootstrap wirings it is not: those tests prove the wiring
under mocked network and a tmp home, and nobody has booted the Tauri build and watched
either effect. Do not read "wired and unit-proven" as "seen working" — that conflation is
exactly how G-30-02 was declared fixed twice while the live build hung.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — all 20 tasks above
      declare a concrete `<automated>` command, confirmed against the seven SUMMARY.md
      files at execution.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — holds; every
      task has one.
- [x] Wave 0 covers all MISSING references — the seven new files plus the two
      retargeted/extended ones are listed above and confirmed present on disk.
- [x] No watch-mode flags — every command listed is a one-shot run (`npx jest <path>`,
      `pnpm test:ci`, `cargo check`, `npx tsc --noEmit`, targeted `node -e`/`python3`
      assertions); none passes `--watch`/`--watchAll`.
- [x] Feedback latency < 15s for targeted runs — confirmed by the individual plans'
      own recorded suite sizes and durations.
- [x] `cargo check` green, AND `git diff src-tauri/` shows only the one-string
      `LONG_RUNNING_CHANNELS` addition plus its doc-comment extension (34.2-06-SUMMARY.md
      confirms via `git diff src-tauri/ | grep -c dispatch_rust_channel` = 0) — the
      zero-new-Rust-arms expectation held.
- [x] The two deferred-UAT items are recorded in `34.2-HUMAN-UAT.md` naming D-02 and D-07
      specifically, with the sign-off sentence byte-identical to
      `34.2-PORTED-CHANNELS.md` §6.
- [x] `nyquist_compliant: true` set in frontmatter — justification: each of the 20 tasks
      across the seven plans (including this plan's own three) declares a concrete
      `<automated>` command; there is no gap of 3 or more consecutive tasks lacking one,
      and no task closes on a deferred or manual check alone. Re-verified post-execution
      against what actually ran, not just planned.

**Approval:** Signed off. This phase closes **unit-proven, with the two D-02/D-07 live
checks deliberately deferred** per D-11 — a recorded decision, not an oversight. All 20
tasks across the seven plans carry a green automated result (251+ tests across the
touched suites per `34.2-06-SUMMARY.md`'s own cross-plan regression count, extended by
this plan's own doc-shape scripts); `cd src-tauri && cargo check --quiet` and
`npx tsc --noEmit` were both reported clean throughout execution. The two named
deferred-UAT items (`UAT-34.2-01`, `UAT-34.2-02`) have NOT been run against a live Tauri
build by anyone — that is the honest, deliberately-deferred state of the evidence per
D-11, not a gap in this validation pass.

---

## Gap Cycle 3 Sign-Off Addendum (2026-07-26)

*Beneath, not in place of, the original Validation Sign-Off above — the original block's
provenance (what was approved, and when, for the original seven-plan shipment) stays
legible and unedited.*

- [x] **Every gap-cycle-3 task carries a concrete automated command.** All 11 tasks across
      plans 34.2-19..34.2-23 (rows `34.2-19-01` through `34.2-23-02` in the Per-Task
      Verification Map above) declare a one-shot `<automated>` command — `npx jest <path>`,
      `npx tsc --noEmit`, `npx eslint <file>`, `cd src-tauri && cargo test`, or a `stat`
      before/after live check — reconciled against all five SUMMARY.md files. No task
      closes on a Wave-0 dependency or a manual check alone.
- [x] **Every new test was hand-proven RED against pre-fix code**, recorded verbatim in its
      plan's own SUMMARY:
      - `34.2-19-02` (`structuralContainment.test.ts`) — 5 of 6 tests failed with the
        `setupFiles` entry disabled (`34.2-19-SUMMARY.md`, "Task 2 RED-PROOF output").
      - `34.2-19-03` (`bootstrap.test.ts` tripwire) — live `stat` proof: the env-var-only
        precursor left the real `gamelib.log`/`.log.old` mtimes changed; the corrected
        `jest.mock('os', ...)` fix left them byte-identical (`34.2-19-SUMMARY.md`, "Live
        Destruction Check").
      - `34.2-20-02` (`loggerFlows.test.ts` WR-02 tests) — 4 of 9 tests failed against
        pre-fix `loggerFlowRegistration.ts`, restored via `git show HEAD~1:...`
        (`34.2-20-SUMMARY.md`, "RED-PROOF").
      - `34.2-21-01` (`repairFailure.test.ts` hostile-value tests) — 10 of 14 tests failed
        against unmodified `repairFailure.ts` with `TypeError: Cannot convert object to
        primitive value` (`34.2-21-SUMMARY.md`, "RED Proof").
      - `34.2-22-01` (`main.rs` `#[cfg(test)]` module) — two opposite mutations of
        `timeout_for` (`Some(INVOKE_TIMEOUT)` unconditional, `None` unconditional) each
        failed a DIFFERENT, non-identical subset of the 6 tests (`34.2-22-SUMMARY.md`,
        "RED Proofs").
      - `34.2-22-02` (`longRunningChannels.test.ts` existence gate) — 4 of 14 tests failed
        when `main.rs` was reverted to `HEAD~1` (`34.2-22-SUMMARY.md`, "Task 2 — jest gate
        RED proof").
      - `34.2-23-01`/`34.2-23-02` (`testContainment.test.ts` hardening) — 4 independent RED
        proofs: the anti-claim gate caught an injected docblock claim; the `readdirSync`
        tripwire caught a real probe file; the structural-containment gate caught a
        commented-out `setupFiles` entry; `jest.replaceProperty` was empirically rejected
        for `process.platform` restoration (`34.2-23-SUMMARY.md`, "REQUIRED EVIDENCE").
- [x] **Every new gate carries a self-test.** `structuralContainment.test.ts`'s own
      anti-vacuity Test 6; `longRunningChannels.test.ts`'s two self-tests (attribute absent,
      attribute present but weakly-referencing); `testContainment.test.ts`'s WR-01 anti-claim
      self-test, two Block C tripwire self-tests (unclassified + phantom directions), and
      the Block D structural-gate self-test.
- [x] **The two deferred HUMAN-UAT items remain untouched and pending.** `UAT-34.2-01` and
      `UAT-34.2-02` are not named in any of the five gap-cycle-3 plans' `files_modified`
      lists, and `34.2-HUMAN-UAT.md` itself carries no commit from this cycle — both items
      keep `result: pending` and `status: deferred`, confirmed by the Manual-Only
      Verifications table above (byte-unchanged, per this plan's own acceptance criterion).
- [x] Full backend project baseline at the end of gap cycle 3 (`34.2-23-SUMMARY.md`,
      "Observed full-backend suite/test tallies"): 111/112 suites passed, 2304/2305 tests
      passed — sole failure the pre-existing, already-documented `rustInvokeChannel.test.ts`.
      `npx tsc --noEmit` clean throughout.

**Approval:** Signed off. Gap cycle 3 (34.2-19..34.2-23) closes the one new blocker and
seven of the eight warnings `34.2-VERIFICATION.md`'s third round found in gap cycle 2's
own closure work, with every new test hand-proven capable of failing and every new gate
carrying a self-test — the anti-recurrence discipline this cycle was run under, after two
prior cycles each shipped a new defect while closing the named one. The five findings NOT
fixed this cycle (WR-05, WR-06, IN-01, IN-03, IN-06) and the one accepted risk (IN-04) are
recorded with reasons in `deferred-items.md`, not silently dropped.

---

## Gap Cycle 4 Sign-Off Addendum (2026-07-26)

*Beneath, not in place of, the original Validation Sign-Off and the Gap Cycle 3 Sign-Off
Addendum above — both stay legible and unedited.*

- [x] **Every gap-cycle-4 task carries a concrete automated command.** All 13 tasks across
      plans 34.2-25..34.2-29 (rows `34.2-25-01` through `34.2-29-03` in the Per-Task
      Verification Map above) declare a one-shot `<automated>` command — `npx jest <path>`,
      `npx tsc --noEmit`, or a hand-run deliberate-break check recorded verbatim in its
      plan's own SUMMARY — reconciled against all five SUMMARY.md files. No task closes on
      a Wave-0 dependency or a manual check alone.
- [x] **The cycle's proof standard, stated once here:** every fix was preceded by a proof
      observed RED against the then-current code; no proof stubs the function under test;
      every corrected gate carries a self-test proving it rejects the input it previously
      accepted. Applied to CR-01, CR-02 (both instances), WR-01, WR-02, WR-04, WR-05, and
      WR-06 — each with a verbatim RED or deliberate-break transcript quoted in
      `34.2-PORTED-CHANNELS.md` §7's "Gap cycle 4 reconciliation" subsection, reproduced from
      the closing plan's own SUMMARY.md. WR-07, WR-09, WR-11, and WR-12 are direct
      hardening/documentation fixes with no distinguishable pre-fix failure state — recorded
      as such rather than retrofitted with a RED transcript that never ran. WR-03 and the two
      halves of WR-10's comment corrections are formatting/documentation-only changes with no
      RED/GREEN pair by nature.
- [x] **Deliberate-break checks performed this cycle** (each restored clean, `git diff
      --exit-code` confirmed after every one, per the closing plan's own SUMMARY):
      - `34.2-25` (CR-02 primary): pointed the `node:os` registration's `homedir` override at
        the real home instead of `containmentRoot` — the whole suite REFUSED TO RUN (0 tests)
        via the new `setupFiles`-time precondition, not merely one assertion failing.
      - `34.2-26` (CR-01): reverted `logErrorSettled` to a block body — the whole node process
        crashed on an uncaught `ENOTDIR` rejection, a STRONGER failure signal than a clean
        test failure, confirming the fix load-bearing.
      - `34.2-27` (WR-06): reverted signal 2's diagnostic back to an empty catch — exactly
        Tests 1 and 2 failed, all 17 others (including 3/4/5) stayed green, proving the
        break's blast radius was correctly scoped.
      - `34.2-28` (WR-04): deleted all 8 `assert_eq!(` lines from `main.rs`'s real
        `#[cfg(test)]` module, loop/comments intact — 2 of 18 jest-side gate tests failed
        naming the missing real assertion.
      - `34.2-29` (WR-02): commented out `jest.mock('node:os', mockOsFactory)` in
        `jest.setupContainment.ts` — again the whole suite refused to run via the same
        precondition, an even stronger result than the single Block D assertion it targets.
      - `34.2-29` (CR-02 secondary / Block E): temporarily added a `backend/logger/paths`
        import to a `src/common` test file — Block E caught it, naming the file and
        specifier.
      - `34.2-25` (WR-11 falsifiability, informational): this is a portability fix to an
        already-passing macOS-host test; no pre-fix failure state exists to reproduce from
        within a single-platform session — recorded as a limit of this cycle's own proof
        standard, not glossed over.
- [x] **The two deferred HUMAN-UAT items remain untouched and pending.** `UAT-34.2-01` and
      `UAT-34.2-02` are not named in any of the five gap-cycle-4 plans' `files_modified`
      lists, and `34.2-HUMAN-UAT.md` itself carries no commit from this cycle — both items
      keep `result: pending` and `status: deferred`.
- [x] **Final consistency sweep** (run by plan 34.2-30 itself, this addendum's own writer):
      `npx tsc --noEmit` exits 0; `cd src-tauri && cargo check --quiet` exits 0;
      `npx prettier --check` on every source/test file touched by plans 34.2-25..34.2-29
      exits 0 (11 files checked); the full backend jest project
      (`npx jest --config src/backend/jest.config.js`) was run twice — the first run
      additionally hit the pre-existing, already-documented non-deterministic `library.ts`
      leaked-timer flake on `withTimeout.test.ts` (2 failed, 2324 passed, 2326 total), the
      second run reproduced the clean, documented baseline exactly:
      `Test Suites: 1 failed, 112 passed, 113 total` / `Tests: 1 failed, 2325 passed, 2326
      total` — sole failure `src/backend/sidecar/__tests__/rustInvokeChannel.test.ts`, the
      pre-existing Phase 34.1-era baseline, unchanged by this cycle.

**Approval:** Signed off. Gap cycle 4 (34.2-25..34.2-30) closes all 14 findings from
`34.2-REVIEW-GAP-CYCLE-3.md` (2 critical + 12 warnings) — either fixed with a verbatim RED
proof and a self-testing gate, or, where no RED/GREEN pair exists by the finding's own
nature (WR-03, WR-07, WR-09, WR-10's two comment halves, WR-11, WR-12), fixed directly and
recorded honestly as such rather than dressed up with a fabricated transcript. The two
deliberate deferrals (`D4-DEF-01`, `D4-DEF-02`) are recorded with reasons and natural homes
in `deferred-items.md`, not silently dropped, and are pinned by `currency-gate.py`'s
cycle-4 deferred-token assertion so a future edit cannot quietly drop the record that they
were considered.
