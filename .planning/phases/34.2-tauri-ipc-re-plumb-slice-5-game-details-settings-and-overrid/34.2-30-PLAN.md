---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 30
type: execute
wave: 3
depends_on: [34.2-25, 34.2-26, 34.2-27, 34.2-28, 34.2-29]
files_modified:
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/currency-gate.py
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VALIDATION.md
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/deferred-items.md
autonomous: true
requirements: [REQ-34.2-13, REQ-34.2-14]

must_haves:
  truths:
    - "34.2-PORTED-CHANNELS.md names every one of the cycle-3 review's 14 findings with its closing plan and its verbatim RED proof, or with a recorded reason for deferral"
    - "currency-gate.py exits 0 against the updated document and still enforces gap cycle 3's own section"
    - "34.2-VALIDATION.md carries a row for every new proof this cycle added"
    - "No finding is silently dropped: everything not fixed appears in deferred-items.md with a reason and a natural home"
  artifacts:
    - path: ".planning/phases/34.2-.../34.2-PORTED-CHANNELS.md"
      provides: "Gap cycle 4 reconciliation subsection under §7 plus a §6 honesty addendum"
      contains: "Gap cycle 4 reconciliation"
    - path: ".planning/phases/34.2-.../currency-gate.py"
      provides: "Cycle-4 token lists and a newest-section-last ordering check that preserves cycle 3's assertions"
      contains: "Gap cycle 4 reconciliation"
    - path: ".planning/phases/34.2-.../deferred-items.md"
      provides: "From gap cycle 4 section with reasoned deferrals"
      contains: "From gap cycle 4"
  key_links:
    - from: ".planning/phases/34.2-.../currency-gate.py"
      to: ".planning/phases/34.2-.../34.2-PORTED-CHANNELS.md"
      via: "token presence + subsection ordering assertions"
      pattern: "PORTED_CHANNELS_PATH"
---

<objective>
Close **REQ-34.2-13** for gap cycle 4: bring `34.2-PORTED-CHANNELS.md` current, extend
`34.2-VALIDATION.md` with this cycle's new proofs, record every deliberate deferral in
`deferred-items.md`, and update `currency-gate.py` so staleness stays a non-zero exit.

Purpose: every one of this phase's verification rounds cited the SAME currency-gap class — the
declaration artifact reflecting the previous gap cycle's closures while predating the review that
triggered the current one. Gap cycle 3 committed `currency-gate.py` specifically to convert that
observation into an automated failure. This plan performs the maintenance action that script's own
docstring prescribes for a fourth cycle: EXTEND, never delete or weaken.

Output: a declaration artifact a reader can trust for gap cycle 4, and a gate that still fails if it
goes stale again.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW-GAP-CYCLE-3.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-25-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-26-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-27-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-28-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-29-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not re-derive these from the artifacts. -->

`34.2-PORTED-CHANNELS.md` section layout (line numbers as of gap cycle 3):
  §1 Port kinds (20) · §2 The 26 channels (43) · §3 Declaration riders (127) ·
  §4 Declared divergences (156) · §5 Re-deferrals (169) · §6 Sign-off — the honest statement (220)
  with `### What gap cycles 2 and 3 changed about the honesty of this sign-off` (303) ·
  §7 Reconciliation (335) with `### Gap cycle 1 reconciliation (plans 34.2-08..34.2-14)` (376),
  `### Gap cycle 2 reconciliation (plan 34.2-16 — CR-01 sidecar half)` (414) and
  `### Gap cycle 3 reconciliation (plans 34.2-19..34.2-23)` (466, currently last).

`currency-gate.py` current structure:
  `RECONCILIATION_HEADING = "### Gap cycle 3 reconciliation"`;
  `CLOSED_FINDING_TOKENS = [CR-01, WR-01, WR-02, WR-03, WR-04, WR-07, WR-08, IN-02, IN-05, timeout_for]`;
  `DEFERRED_FINDING_TOKENS = [WR-05, WR-06, IN-01, IN-03, IN-06]`;
  `PLACEHOLDER_PATTERN = TBD|TODO|FIXME|XXX` applied to the WHOLE document;
  five assertions: heading exists exactly once · body non-empty · every closed token in the section ·
  every deferred token in the section · **assertion 4** requires the cycle-3 heading to be the LAST
  `###` subsection under `## 7. Reconciliation` · assertion 5 forbids placeholders document-wide.
  Its own docstring states the correct fourth-cycle maintenance action: extend the token lists and
  add a new numbered section, never delete or weaken an assertion.
  **Note the collision:** appending a cycle-4 subsection breaks assertion 4 as written. Resolving it
  is part of this plan, not a reason to delete the assertion.

`34.2-VALIDATION.md` sections: Test Infrastructure (22) · Sampling Rate (52) ·
  Per-Task Verification Map (68, 31 rows after gap cycle 3) · Wave 0 Requirements (113) ·
  Manual-Only Verifications (148) · Validation Sign-Off (173, ORIGINAL — do not modify) ·
  Gap Cycle 3 Sign-Off Addendum (212).

`deferred-items.md` sections end with `## From gap cycle 3 (plans 34.2-19..34.2-23)` (line 154),
whose entries use the shape: bolded finding id — file:line — description — **Reason for deferring:**
— **Natural home:**.

Cycle-3 review findings to account for, all 14: CR-01, CR-02, WR-01 … WR-12. (The review's
frontmatter says `warning: 11`; the body labels WR-01 through WR-12 and the body is authoritative.)
Cycle-4 closing plans: 34.2-25 (CR-02, WR-07, WR-09, WR-10 setup half, WR-11, WR-12),
34.2-26 (CR-01, WR-03 sidecar half, WR-10 loggerFlows comment), 34.2-27 (WR-06, WR-03 renderer half),
34.2-28 (WR-04, WR-08), 34.2-29 (WR-01, WR-02, WR-05, CR-02 secondary, WR-10 bootstrap comment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the gap cycle 4 reconciliation into 34.2-PORTED-CHANNELS.md</name>
  <files>.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/34.2-.../34.2-PORTED-CHANNELS.md (§6 from line 220 and §7 from line 335 in full — mirror the existing subsection shape exactly)
    - .planning/phases/34.2-.../34.2-REVIEW-GAP-CYCLE-3.md (all 14 findings plus the "Notes on what was verified as sound" section)
    - The five SUMMARYs 34.2-25 through 34.2-29 (the verbatim RED proofs and deliberate-break outputs each recorded)
  </read_first>
  <action>
    Append `### Gap cycle 4 reconciliation (plans 34.2-25..34.2-29)` as the LAST `###` subsection
    under `## 7. Reconciliation`, after the gap-cycle-3 subsection. Do not modify §2's channel table,
    §3's three declaration riders, §5's re-deferrals, or any earlier reconciliation subsection.

    The subsection must contain, one row or bullet per finding, all fourteen: **CR-01**, **CR-02**,
    **WR-01** … **WR-12**. For each: the closing plan number, the file(s) touched, and the specific
    evidence — quote the RED proof or deliberate-break output from that plan's SUMMARY rather than
    asserting "closed". Where a review claim was empirically disproven or refined during execution,
    say so.

    Three points that must be stated explicitly because they are where a future reader is most likely
    to be misled:
    (a) **CR-01's remedy is narrower than the review's literal prescription.** The review suggested
        expression-bodying `logError` in `backend/logger/index.ts`; plan 34.2-26 instead added a new
        `logErrorSettled` export and left the four existing wrappers untouched. Record the measured
        reason (309 statement-position `logError(...)` call sites under `src/backend`, none awaited,
        against `@typescript-eslint/no-floating-promises: 'warn'` at `eslint.config.mjs:39`) and
        that the four-wrapper change remains deferred with a natural home. This IS the "audit with
        no-floating-promises" the review asked for; record its result, not just its conclusion.
    (b) **CR-02 had a secondary instance** (`jest.config.js`'s unenforced backend-only scope claim),
        closed separately by plan 34.2-29's Block E rather than by the containment module itself.
    (c) **WR-11's fix diverges from the review's suggestion.** The review proposed guarding
        `userInfo()` and deriving the platform branch; plan 34.2-25 instead removed the `userInfo()`
        call from the test entirely (because the same plan now redirects `userInfo`, which the review
        itself flags as a follow-on) and anchored the anti-vacuity check on the per-run `mkdtemp`
        containment root plus a pre-mock `realHomeAtSetup` capture. State why that is stronger:
        it holds on all three platforms, does not care whether `TMPDIR` lives under `$HOME`, and
        cannot be satisfied by the mechanism under test.

    Also record the deliberate deferrals with tokens `D4-DEF-01` and `D4-DEF-02` (defined in Task 3),
    each with its reason, so the currency gate can pin them.

    Add a matching honesty addendum to §6 titled for gap cycle 4, in the same style as the existing
    `### What gap cycles 2 and 3 changed about the honesty of this sign-off`. It must state the
    review's meta-finding plainly — each of the last three cycles fixed its named gap and introduced
    a new one, and in each case the cycle's own new tests were structurally incapable of detecting
    the shortfall — and state what cycle 4 did differently: every fix was preceded by a proof
    observed RED against the then-current code, no proof stubs the function under test, and every
    corrected gate carries a self-test proving it rejects the input it previously accepted. Name the
    two remaining deferred HUMAN-UAT items (D-11) as still deferred and still not gating closure.

    Do not introduce the tokens `TBD`, `TODO`, `FIXME` or `XXX` anywhere in the document — the
    currency gate rejects them document-wide.
  </action>
  <verify>
    <automated>cd .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid && grep -c "Gap cycle 4 reconciliation" 34.2-PORTED-CHANNELS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Gap cycle 4 reconciliation" 34.2-PORTED-CHANNELS.md` returns exactly 1.
    - All fourteen finding ids appear inside the new subsection. Verify by extracting the subsection body and checking each of `CR-01`, `CR-02`, `WR-01`..`WR-12` is present; record the extraction command and its output in the SUMMARY.
    - `D4-DEF-01` and `D4-DEF-02` both appear in the new subsection.
    - The new subsection is the LAST `###` under `## 7. Reconciliation` — verify with `grep -n "^### " 34.2-PORTED-CHANNELS.md | tail -1`.
    - `grep -cE "\bTBD\b|\bTODO\b|\bFIXME\b|\bXXX\b" 34.2-PORTED-CHANNELS.md` returns 0.
    - `git diff` on this file shows ONLY additions in §6 and §7 — §2's channel table, §3's riders and §5 are byte-unchanged. Confirm with a targeted `git diff` and record it.
  </acceptance_criteria>
  <done>The declaration artifact names all 14 cycle-3 findings with their closing plan and evidence, flags the three places the remedy diverged from the review's literal prescription, and carries a gap-cycle-4 honesty addendum.</done>
</task>

<task type="auto">
  <name>Task 2: Extend currency-gate.py for cycle 4 without weakening cycle 3's assertions</name>
  <files>.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/currency-gate.py</files>
  <read_first>
    - .planning/phases/34.2-.../currency-gate.py (all 200 lines — especially the docstring's stated maintenance policy and assertion 4's ordering logic)
    - .planning/phases/34.2-.../34.2-PORTED-CHANNELS.md (Task 1's output — the document the gate runs against)
  </read_first>
  <action>
    Extend `currency-gate.py`. Add, do not replace:

    - `CYCLE4_RECONCILIATION_HEADING = "### Gap cycle 4 reconciliation"`, kept alongside the existing
      cycle-3 constant (renaming the existing one to `CYCLE3_RECONCILIATION_HEADING` for symmetry is
      permitted; deleting it is not).
    - `CYCLE4_CLOSED_FINDING_TOKENS` = the fourteen ids `CR-01`, `CR-02`, `WR-01` … `WR-12`.
    - `CYCLE4_DEFERRED_FINDING_TOKENS` = `["D4-DEF-01", "D4-DEF-02"]`.
    - Assertions 1-3 duplicated for the cycle-4 section: heading present exactly once, body non-empty,
      every cycle-4 closed and deferred token present within that section.
    - Keep assertions 1-3 for the cycle-3 section EXACTLY as they are, with cycle 3's own token lists
      unchanged. Cycle 3's section and its guarantees must remain enforced.

    **Resolve the assertion-4 collision explicitly.** Assertion 4 currently requires the cycle-3
    heading to be the last `###` subsection under `## 7. Reconciliation`; a cycle-4 subsection
    appended after it makes that assertion fail by design. Change the ordering check to require the
    NEWEST heading (`CYCLE4_RECONCILIATION_HEADING`) to be last, and ADD a companion assertion that
    the cycle-3 heading still appears and appears BEFORE the cycle-4 heading (compare their
    `str.find` indices). Record in a comment that the ordering rule is "newest last, history
    preserved in reading order", which is what the original assertion's own failure message already
    described, and that this is an extension rather than a weakening: the cycle-3 section is now
    pinned in two ways (presence + relative position) instead of one.

    Update the module docstring's fifth-cycle instructions to reference the now-established
    per-cycle constant pattern, so the next maintainer repeats it mechanically.
    Update the final success `print(...)` to report both cycles' token counts.
  </action>
  <verify>
    <automated>cd .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid && python3 currency-gate.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 currency-gate.py` exits 0 and its success line reports both cycle-3 and cycle-4 token counts.
    - `grep -c "CYCLE3_" currency-gate.py` and `grep -c "CYCLE4_" currency-gate.py` both return >= 3.
    - Cycle-3's own token lists are unchanged in content: `CR-01, WR-01, WR-02, WR-03, WR-04, WR-07, WR-08, IN-02, IN-05, timeout_for` for closed and `WR-05, WR-06, IN-01, IN-03, IN-06` for deferred. Verify by diffing the list literals and recording the result.
    - RED proof 1, recorded verbatim: temporarily redact one cycle-4 token (e.g. `WR-11`) from the gap-cycle-4 subsection of `34.2-PORTED-CHANNELS.md`; `python3 currency-gate.py` exits NON-ZERO naming that token; restore and confirm `git diff --exit-code` on the markdown returns 0.
    - RED proof 2, recorded verbatim: temporarily move the gap-cycle-4 subsection ABOVE the gap-cycle-3 subsection; the gate exits NON-ZERO on the ordering assertion; restore and confirm the diff is clean.
    - RED proof 3, recorded verbatim: temporarily redact a cycle-3 token (e.g. `IN-05`) from the gap-cycle-3 subsection; the gate exits NON-ZERO — proving cycle 3's assertions were preserved, not weakened; restore.
    - `python3 -c "import ast,sys; ast.parse(open('currency-gate.py').read())"` exits 0.
  </acceptance_criteria>
  <done>The currency gate enforces both cycles' sections, requires the newest to be last, and is proven able to fail in three independent ways.</done>
</task>

<task type="auto">
  <name>Task 3: Extend 34.2-VALIDATION.md with this cycle's proofs and record every deferral in deferred-items.md</name>
  <files>.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VALIDATION.md, .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/deferred-items.md</files>
  <read_first>
    - .planning/phases/34.2-.../34.2-VALIDATION.md (Test Infrastructure at 22, Per-Task Verification Map at 68, Validation Sign-Off at 173 — DO NOT modify — and the Gap Cycle 3 Sign-Off Addendum at 212 whose shape the new addendum mirrors)
    - .planning/phases/34.2-.../deferred-items.md (the `## From gap cycle 3 (plans 34.2-19..34.2-23)` section at line 154 and the entry shape it uses; also the `## From plan 34.2-20` entry at line 106, which already predicted CR-01)
    - The five SUMMARYs 34.2-25 through 34.2-29
  </read_first>
  <action>
    **`34.2-VALIDATION.md`** — extend, never rewrite. Add:
    - Test Infrastructure rows for the new/changed proof surfaces: the new
      `src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts` suite (naming its
      `ENOTDIR`-via-blocked-parent rejection mechanism and its no-stubbing-the-function-under-test
      rule), the dual-specifier containment mechanism and its `setupFiles`-time precondition, the
      new `structuralContainment.test.ts` gates, `testContainment.test.ts`'s Block E cross-project
      scope gate, and the corrected Rust presence gate.
    - Per-Task Verification Map rows for every task in plans 34.2-25..34.2-30 (a row per task,
      continuing the existing numbering — the map stood at 31 rows after gap cycle 3).
    - A `## Gap Cycle 4 Sign-Off Addendum (2026-07-26)` section beneath the untouched original
      Validation Sign-Off block and the gap-cycle-3 addendum, stating the cycle's proof standard
      (every fix preceded by an observed RED; no proof stubs the function under test; every
      corrected gate carries a self-test) and listing the deliberate-break checks performed.
    The original `## Validation Sign-Off` block, the `## Manual-Only Verifications` section, the two
    deferred-UAT rows and the `rustInvokeChannel.test.ts` baseline entry must all remain
    byte-unchanged.

    **`deferred-items.md`** — add `## From gap cycle 4 (plans 34.2-25..34.2-29)` at the end, using
    the existing entry shape, with at minimum:
    - **D4-DEF-01** — `src/backend/logger/index.ts:16-27`. The four exported wrappers
      (`logDebug`/`logInfo`/`logWarning`/`logError`) still discard the `Promise<void>` their
      `LogWriter` method returns. Plan 34.2-26 added a promise-returning sibling for the one call
      site that must settle, rather than changing the shared wrappers. **Reason for deferring:** the
      measured blast radius — 309 statement-position `logError(...)` call sites under `src/backend`,
      none awaited, against `@typescript-eslint/no-floating-promises: 'warn'` — would add roughly 309
      new lint warnings for zero runtime change (the promise is dropped either way; only the drop
      site moves). **Natural home:** a standalone plan changing all four wrappers together with a
      call-site audit, the home the `## From plan 34.2-20` entry already named; this entry supersedes
      and updates that one rather than duplicating it — cross-reference it explicitly.
    - **D4-DEF-02** — the carried-forward gap-cycle-2 residuals that cycle 4 did not touch. Re-state
      each with its current status: the per-suite triple tmp-root constant hoist and the
      hoisted-factory TDZ rename (still open, now further demoted to cosmetic by the dual-specifier
      `setupFiles` mechanism), the unrestored `jest.spyOn` calls in
      `sidecarRejectionGuard.test.ts:319,376` (still open), the project-wide `prettier --check`
      baseline (still red for pre-existing files — cycle 4 makes its OWN touched files clean, which
      is the part CI attributes to this work), and the pre-existing non-deterministic
      `storeManagers/steam/library.ts` leaked-timer flake (explicitly out of scope for this cycle).
    Also mark the `## From plan 34.2-20 (gap cycle 3, WR-02 closure)` entry with a dated resolution
    note pointing at D4-DEF-01 and plan 34.2-26 — preserve its original text, do not delete it
    (the convention gap cycle 3 used when it closed the 34.2-18 entry).
  </action>
  <verify>
    <automated>cd .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid && grep -c "From gap cycle 4" deferred-items.md && grep -c "Gap Cycle 4 Sign-Off Addendum" 34.2-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## From gap cycle 4" deferred-items.md` returns 1; `grep -c "D4-DEF-01" deferred-items.md` and `grep -c "D4-DEF-02" deferred-items.md` each return >= 1.
    - `grep -c "## Gap Cycle 4 Sign-Off Addendum" 34.2-VALIDATION.md` returns 1.
    - The Per-Task Verification Map has grown by exactly the number of tasks in plans 34.2-25..34.2-30 (13). Record the before and after row counts in the SUMMARY.
    - The original `## Validation Sign-Off` block is byte-unchanged: `git diff` on `34.2-VALIDATION.md` shows no modification between its heading and the Gap Cycle 3 addendum heading. Record the confirming diff.
    - The `## From plan 34.2-20` entry retains its original text and gains a dated resolution note naming plan 34.2-26.
    - `python3 currency-gate.py` still exits 0 after these edits.
    - Final consistency sweep, recorded in the SUMMARY: `npx tsc --noEmit` exits 0, `cd src-tauri && cargo check --quiet` exits 0, `npx prettier --check` exits 0 on every source file touched by plans 34.2-25..34.2-29, and the full backend jest project's failing-suite set is exactly `{rustInvokeChannel.test.ts}`.
  </acceptance_criteria>
  <done>Every new proof has a validation row, every unfixed item has a reason and a natural home, and the phase's build/test gates are green apart from the one documented pre-existing red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declaration artifact → future reader / verifier | `34.2-PORTED-CHANNELS.md` is the phase's honest-statement of record; an overclaim here is read as evidence by the next verification round. |
| currency gate → declaration artifact | The gate is the only automated thing preventing the artifact from silently going stale, a failure this phase has already suffered three times. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-114 | Repudiation | `34.2-PORTED-CHANNELS.md` gap cycle 4 subsection | mitigate | Every one of the 14 findings is named with its closing plan and quoted evidence; the three places the remedy diverges from the review's literal prescription (CR-01's narrower fix, CR-02's secondary instance, WR-11's different anti-vacuity anchor) are called out rather than glossed. |
| T-34.2-115 | Tampering | `currency-gate.py` assertion set | mitigate | Task 2 extends rather than replaces: cycle-3's token lists and section assertions stay, the ordering rule becomes "newest last, cycle 3 still present and before it", and three independent RED proofs demonstrate the gate can still fail. The script's docstring already names deleting an assertion to make the gate pass as the anti-pattern (`T-34.2-92`). |
| T-34.2-92 (existing) | Denial of service | maintainability of the currency gate | mitigate | The per-cycle constant pattern is documented in the updated docstring so the fifth cycle is mechanical. |
| T-34.2-116 | Repudiation | deferred findings | mitigate | `D4-DEF-01`/`D4-DEF-02` are pinned by the currency gate's deferred-token assertion, so a future edit cannot quietly drop the record that they were considered and not fixed. |
| T-34.2-SC | Tampering | npm/pip/cargo installs | mitigate | Documentation-only plan; `git diff` on `package.json`/`pnpm-lock.yaml`/`Cargo.toml` must be empty. No legitimacy gate required. |
</threat_model>

<verification>
- `python3 currency-gate.py` exits 0, and exits non-zero when a cycle-4 token is redacted, when the
  subsection order is reversed, or when a cycle-3 token is redacted.
- All 14 cycle-3 review findings appear in the gap cycle 4 reconciliation subsection.
- No `TBD`/`TODO`/`FIXME`/`XXX` token anywhere in `34.2-PORTED-CHANNELS.md`.
- The original Validation Sign-Off block and the §2 channel table are byte-unchanged.
- `npx tsc --noEmit`, `cargo check --quiet` and `npx prettier --check` on this cycle's touched files
  all exit 0; the backend failing-suite set is exactly `{rustInvokeChannel.test.ts}`.
</verification>

<success_criteria>
REQ-34.2-13 is satisfied for gap cycle 4: the declaration artifact is current and honest about where
the remedy diverged from the review, the validation strategy covers every new proof, every deferral
is recorded with a reason and a natural home, and the currency gate still turns staleness into a
non-zero exit.
</success_criteria>

<output>
Create `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-30-SUMMARY.md` when done, including the three currency-gate RED proofs verbatim, the before/after Per-Task Verification Map row counts, and the final consistency sweep results.
</output>
