---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
verified: 2026-07-26T11:00:00Z
status: gaps_found
score: 14/14 requirement-level truths verified; 1 NEW blocker + 3 warnings found in gap-cycle-2's own closure work
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 14/14 (3 blockers in gap-cycle-1 closure work)
  gaps_closed:
    - "GAP-CYCLE-1 CR-01 (logError silent no-op under Tauri): registerLoggerFlows() is now reached from a real production path (src/sidecar/index.ts -> bootstrap.ts -> handlers.ts:123 -> loggerFlowRegistration.ts), registers via ipcMain.on (listener, matching send-kind), and loggerFlows.test.ts proves a POSITIVE side effect (a real marker string is read back out of the real log file after a real send frame), not merely absence-of-throw. Confirmed by direct read of handlers.ts/loggerFlowRegistration.ts and by running the suite."
    - "GAP-CYCLE-1 CR-02 (String(reason) outside its own try, crash risk): processGuards.ts:61-83 now initializes `message` to a hardcoded non-interpolated fallback literal BEFORE its own try, and reassigns it inside that try; the logging call has its own nested try with a process.stderr fallback. Confirmed by direct read and by observing '[sidecar] unhandled promise rejection: <unstringifiable reason>' printed (not a crash) during three independent full-suite test runs performed in this verification session."
    - "GAP-CYCLE-1 CR-03 (sidecarRejectionGuard.test.ts missing pathShim mock): jest.mock('../pathShim', ...) is present at sidecarRejectionGuard.test.ts:103, matching the same containment kit as the other three in-scope suites. Confirmed by direct grep/read."
  gaps_remaining: []
  regressions:
    - "NEW (gap-cycle-2's own testContainment.test.ts, plan 34.2-18): the cycle's new containment gate explicitly enumerates 11 sidecar test suites (KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES) as 'declared debt' rather than closing them, and one of those 11 -- bootstrap.test.ts -- drives the REAL, unmocked bootstrap.init() three times with no os/pathShim/logger-paths mock. This is not a hypothetical: I independently reproduced live destruction of the real developer log file THREE separate times in this verification session (timestamps 10:49, 10:56, 10:57 -- each matching a jest run I had just executed), confirming the code review's own reproduction. Electron-store JSON was not observed to be touched, but the same code path that clobbers the log file also constructs real electron-store instances, so nothing structurally prevents the config/token-wipe variant of this repo's own previously-recorded tests-clobbering-real-steam-store incident."
    - "WR-01 (gap-cycle-2 review): testContainment.test.ts's 'no longer claims NO FILESYSTEM WRITES' gate (lines 368-376) matches against COMMENT-STRIPPED source, but that exact claim can only ever appear inside a docblock (a `*`-prefixed line), which stripComments always removes before the assertion runs. The gate cannot fail regardless of whether any of the four suites carries the false claim. Confirmed by direct read of stripComments (lines 148-153) and the assertion (lines 368-376)."
    - "WR-02 (gap-cycle-2 review): the new logError send listener (loggerFlowRegistration.ts:56-58) calls logError(...) without awaiting or .catch()ing it; LogWriter#logBase is `private async` (log_writer.ts:131), so a real log-write failure (EACCES/ENOSPC/deleted dir) becomes a process-level unhandledRejection that dispatchSend's synchronous try/catch cannot catch. Only survives today because processGuards.ts (fixed by this same cycle) absorbs it as a generic warning -- but this violates processGuards.ts's own documented invariant that it is 'explicitly NOT a substitute for call-site catch/try/catch handling.' Confirmed by direct read of both files."
    - "WR-03 (gap-cycle-2 review): repairFailure.ts:45 interpolates `${error}` where error is typed `unknown`, with no try/catch -- the identical defect class plan 34.2-15 was created specifically to remove from processGuards.ts. For a null-prototype or throwing-toString error value, this throws before reaching the showDialogModal call two lines later, so the ERROR dialog (the user-visible signal REQ-34.2-12 exists to guarantee) never renders, and the throw escapes into an unhandled renderer-side rejection. The new test suite's 4th case (repairFailure.test.ts:105-117) claims to cover 'the kind of hostile reason' this gap cycle hardened against elsewhere, but only passes a plain string, which never exercises the throw path -- confirmed by direct read, an Object.create(null) case would fail today. Narrower in practical severity than gap-cycle-1's CR-01/CR-02 (real repair() rejections are near-universally Error instances, not null-prototype objects), kept at warning severity rather than blocker."
gaps:
  - truth: "The phase's own test suite does not destroy real developer data when run -- the containment discipline this gap cycle exists to enforce (CR-03/WR-01, REQ-34.2-07/-14) actually holds across the sidecar test directory, not just the four suites this cycle touched"
    status: failed
    reason: "testContainment.test.ts (plan 34.2-18, this cycle's own new artifact) declares an 11-suite hole in its own docstring and Block B gate rather than closing it. bootstrap.test.ts is one of those 11: it drives the real init() three times, mocks only online_monitor, and initHeadless() -> new LogWriter(getLogFilePath({})) -> archiveOldLogFile() renameSync's the developer's REAL ~/Library/Logs/GameLib/gamelib.log over the prior .old on every run. I independently reproduced this THREE times in this verification session alone (before running any sidecar tests: gamelib.log/.old both timestamped 10:49; after running `npx jest --testPathPattern=src/backend/sidecar/__tests__`: both re-timestamped 10:56; after running the full backend suite: both re-timestamped 10:57 again). This is not merely inherited from the code review -- it is live, current, and reproduces on every invocation of this project's own designated test commands."
    artifacts:
      - path: "src/backend/sidecar/__tests__/testContainment.test.ts"
        issue: "Lines 279-307 declare KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES (11 files) as accepted debt instead of gating them; Block B's anti-vacuity check only pins the list's LENGTH (toHaveLength(11)), so it cannot detect the list growing to cover a 12th uncontained suite either"
      - path: "src/backend/sidecar/__tests__/bootstrap.test.ts"
        issue: "No os/pathShim/backend-logger-paths mock; calls the real init() three times; each call's initHeadless() rotates the developer's real gamelib.log via archiveOldLogFile()'s renameSync, destroying the prior .old file's contents"
    missing:
      - "Apply the same four-element containment kit (os mock, pathShim mock, backend/logger/paths mock, resolve+relative tripwire) to bootstrap.test.ts and the other 10 declared-uncovered suites, OR make containment structural (a jest setupFiles entry for the backend project) so a new or existing suite cannot opt out by omission -- the code review's own CR-01 fix snippet (a jest.setupContainment.ts setupFiles module) is directly usable"
      - "At minimum, close bootstrap.test.ts before this cycle is accepted -- it is empirically the one already proven to destroy real data on this host, is a ~40-line copy of a kit already written four times in this same cycle, and touches a file this cycle's own plans did not modify (lowest-risk fix available)"
      - "Add the WR-08-style derived tripwire (every *.test.ts file in the directory must be classified as either IN_SCOPE_SUITES or KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES) so a 12th new uncontained suite cannot be silently invisible to this gate"
deferred: []
human_verification:
  - test: "UAT-34.2-01 — a translated notification string actually renders in a live Tauri window (D-02 / REQ-34.2-02)"
    expected: "Triggering repair (or another i18next-backed notify path) shows a real, non-blank, non-'undefined' translated sentence in both `pnpm tauri:dev` and a packaged build"
    why_human: "Jest proves i18next.t() returns a real string after init() and that repair's notify body reflects it over the real sidecar RPC loop, but cannot see a notification actually render in a window, nor exercise a packaged build's locale-path resolution"
  - test: "UAT-34.2-02 — the anticheat data file actually appears on disk via a real network fetch (D-07 / REQ-34.2-07)"
    expected: "After deleting <appFolder>/areweanticheatyet.json and running pnpm tauri:dev through an online transition, the file reappears and the 'AreWeAntiCheatYet data downloaded' log line appears in gamelib.log"
    why_human: "Jest asserts the event fires and the file is written under a tmp home with a mocked axios; it cannot prove a real GitHub fetch completes in a live process across a real network after a real connectivity transition"
---

# Phase 34.2: Tauri IPC re-plumb slice 5 — game details, settings and overrides — Verification Report (RE-VERIFICATION after gap cycle 2)

**Phase Goal:** Port the game details, settings and overrides IPC cluster (26 channels): per-game info/settings/overrides, SDL selection, launch options, install-path changes, version pinning, repair/kill, and the enrichment surfaces (wiki game info, anticheat status, known fixes, CrossOver index, store search, recent games). Additive and reversible — the Electron build keeps working unchanged.
**Verified:** 2026-07-26T11:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — after gap cycle 2 (plans 34.2-15..18)

## Goal Achievement

This is the THIRD verification of this phase. Gap cycle 1 (34.2-08..14) closed the original 3 requirement-level gaps but introduced 3 new blockers of its own (a silent `send` channel, a crash-capable process guard, and a destructive test). Gap cycle 2 (34.2-15..18) was executed specifically to close those 3 new blockers. All three ARE genuinely closed — independently re-derived below, not inherited from either `34.2-REVIEW-GAP-CYCLE-2.md` or the SUMMARY files. But the fresh code review that followed gap cycle 2 found a new critical issue (its own CR-01, unrelated to gap-cycle-1's CR-01) plus 3 warnings, all inside the SAME cycle's own new artifacts. I independently reproduced the critical one — live, repeatable, real-data-destroying — three separate times during this verification session, which is stronger evidence than the review itself presents.

### Observable Truths

| # | Truth (REQ) | Status | Evidence |
|---|---|---|---|
| 1 | REQ-34.2-01: 9 runner-generic dispatch channels ported verbatim across all 6 managers | VERIFIED (regression) | Untouched by gap cycle 2; full suite still green (see Behavioral Spot-Checks) |
| 2 | REQ-34.2-02: i18next initialized in sidecar bootstrap | VERIFIED (regression) | Untouched by gap cycle 2 |
| 3 | REQ-34.2-03: 19 handler bodies extracted; transitive-reach invariant proven by a gate, not overclaimed | VERIFIED (regression) | Untouched by gap cycle 2; `electronReachLedger.test.ts` still passes in the full suite run |
| 4 | REQ-34.2-04: sidecar imports underlying feature modules directly, never ipc_handler.ts | VERIFIED (regression) | Unaffected |
| 5 | REQ-34.2-05: readKnownFixes extracted | VERIFIED (regression) | Unaffected |
| 6 | REQ-34.2-06: buildCrossoverRatingMap extracted, D-16 contract preserved | VERIFIED (regression) | Unaffected |
| 7 | REQ-34.2-07: fetchLastestReleases()/releasesInfoReady→downloadAntiCheatData wiring is safely re-homed | VERIFIED at the literal-text level; the safety-net code it required (processGuards.ts) is now genuinely fixed (gap cycle 2 closed both prior blockers), but the CONTAINMENT of the test suite proving this safety continues to have a live, reproducible defect — see the new gap below | `bootstrap.ts:244-253`'s `.catch()` confirmed present (unchanged from gap cycle 1); `processGuards.ts:61-83` confirmed fixed (message construction now inside its own try, hardcoded fallback literal); observed the guard correctly absorb a synthetic `Error: disk full` rejection into a logged warning (not a crash) during a live full-suite test run |
| 8 | REQ-34.2-08: 3 metadata-override channels ported, write proven by side effect | VERIFIED (regression) | Unaffected |
| 9 | REQ-34.2-09: 6 runner-pinned stray channels ported at parity | VERIFIED (regression) | Unaffected |
| 10 | REQ-34.2-10: requestGameSettings left at parity | VERIFIED (regression) | Unaffected |
| 11 | REQ-34.2-11: 8 enrichment channels ported with contracts intact | VERIFIED (regression) | Unaffected |
| 12 | REQ-34.2-12: getCrossoverIndex exempted from 60s timeout; repair/readConfig exemption present; a real repair failure produces a visible signal | VERIFIED at the primary-defect level (the gap-cycle-1 silent-no-op CR-01 is genuinely closed — see below), but a NARROW residual gap exists for hostile/unstringifiable error values (WR-03, see regressions) | `src-tauri/src/main.rs:132-143` confirmed unchanged and correct, `cargo check --quiet` clean (independently re-run); `loggerFlowRegistration.ts`'s `registerLoggerFlows()` confirmed reachable from `handlers.ts:123` (a real production entry point) and `loggerFlows.test.ts`'s positive side-effect test (a real marker string read back from the real log file after a real transport-level send frame) independently re-run and passing |
| 13 | REQ-34.2-13: 34.2-PORTED-CHANNELS.md declares all 26 channels honestly | VERIFIED, but again stale re: the newest review | Document reflects gap-cycle-1's CR-01/CR-02/CR-03 closures (confirmed by direct read, lines 34, 354-409) but predates `34.2-REVIEW-GAP-CYCLE-2.md`'s findings, same currency-gap class as the prior two rounds — not a dishonesty finding, it does not misrepresent anything it discusses |
| 14 | REQ-34.2-14: additive/reversible holds behaviorally; no direct electron import; cargo check/tsc stay clean | VERIFIED for build-gate cleanliness; FAILED for the safety-net/test-containment component of this requirement — see the new gap below | `npx tsc --noEmit` clean (independently re-run, exit 0); `cargo check --quiet` clean (independently re-run, exit 0); full backend suite 110/111 suites, 2272/2273 tests passing, sole failure is the pre-existing (34.1-era) `rustInvokeChannel.test.ts` (confirmed matches documented baseline). However, running this exact suite is what destroys real developer data on this host — see gap below |

**Score:** 14/14 requirement-level truths pass on their literal text — but overall status is `gaps_found` because gap-cycle-2's own closure work introduced 1 new blocker (real, currently-reproducing data loss) and 3 warnings, mirroring the exact pattern of both prior cycles (fix the named gap, introduce a new one).

### New Findings From This Verification (independently re-derived, not inherited)

| # | Finding | Independent confirmation performed | Severity |
|---|---|---|---|
| 1 | `testContainment.test.ts` declares an 11-suite real-data-loss hole as accepted debt; `bootstrap.test.ts` is proven to destroy the real developer log file | Read `bootstrap.test.ts` in full (no os/pathShim/logger-paths mock, calls real `init()` 3x); checked `~/Library/Logs/GameLib/gamelib.log`/`.old` timestamps BEFORE running any tests (10:49, matching the reviewer's own earlier reproduction), then AGAIN after my own `npx jest --testPathPattern=src/backend/sidecar/__tests__` run (10:56), then AGAIN after my own full `npx jest --testPathPattern=src/backend` run (10:57) — three independent, first-hand reproductions in this session alone, not merely trusting the review's narrative | **Blocker** |
| 2 | `installUnhandledRejectionGuard`'s `String(reason)` is now correctly inside its own try (gap-cycle-1 CR-02 closed) | Read `processGuards.ts:61-83` directly; observed the guard correctly log (not crash on) a synthetic `Error: disk full` unhandled rejection during a live full-suite run | Closed — VERIFIED |
| 3 | `logError` is now correctly registered and reachable on the sidecar (gap-cycle-1 CR-01 closed) | Read `handlers.ts:123` (`registerLoggerFlows()` called unconditionally at module scope, a real production path) and `loggerFlowRegistration.ts`; re-ran `loggerFlows.test.ts`'s positive side-effect test, passing | Closed — VERIFIED |
| 4 | `sidecarRejectionGuard.test.ts` now has the `pathShim` mock (gap-cycle-1 CR-03 closed for this suite) | Grepped the file directly for `jest.mock('../pathShim'` — present at line 103 | Closed — VERIFIED |
| 5 | The "no longer claims NO FILESYSTEM WRITES" gate in `testContainment.test.ts` is vacuous | Read `stripComments` (lines 148-153) and the assertion (lines 368-376) directly — the claim can only appear in a `*`-prefixed docblock line, which `stripComments` always strips before the check runs | Warning |
| 6 | The new `logError` listener leaks a floating promise | Read `loggerFlowRegistration.ts:56-58` (no await/catch) and confirmed `LogWriter#logBase` is `private async` (`log_writer.ts:131`) | Warning |
| 7 | `repairFailure.ts` reintroduces the `${unknown}` interpolation class CR-02 existed to remove, and its own regression test doesn't cover the throwing case | Read `repairFailure.ts:45` directly (`${error}` where `error: unknown`, no try/catch) and `repairFailure.test.ts`'s 4th test (only passes a plain string, never `Object.create(null)`) | Warning (narrower scope than blocker-class findings — real `repair()` rejections are near-universally `Error` instances) |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/backend/sidecar/processGuards.ts` | Process-level guard, never throws for any rejection reason | VERIFIED | Message construction now inside its own try with a hardcoded fallback initial value (lines 61-69); logging call in its own nested try with stderr fallback (lines 70-82) |
| `src/backend/sidecar/loggerFlowRegistration.ts` | `logError` registered and reachable on the sidecar | VERIFIED | Called from `handlers.ts:123`, a real unconditional module-scope call; positive side-effect test passes |
| `src/frontend/screens/Game/GameSubMenu/repairFailure.ts` | Repair failure produces 3 independent signals, never throws | PARTIAL | Signals exist and pass for `Error`/string reasons; throws for null-prototype/unstringifiable reasons before the dialog signal renders (WR-03) |
| `src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts` | Proves CR-02 fixed, uses same containment kit as siblings | VERIFIED | `pathShim` mock present (line 103), `backend/logger/paths` mock present |
| `src/backend/sidecar/__tests__/testContainment.test.ts` | Proves containment holds; no suite escapes it undetected | **DEFECTIVE** | Proves containment for exactly 4 suites; explicitly documents an 11-suite hole as accepted debt rather than closing or gating it; one member of that hole (`bootstrap.test.ts`) is proven, live, to destroy real developer data; the "NO FILESYSTEM WRITES" anti-claim gate is vacuous (WR-01) |
| `src-tauri/src/main.rs` `LONG_RUNNING_CHANNELS` | `repair`/`readConfig` present | VERIFIED (regression) | Lines 132-143, unchanged since gap cycle 1, `cargo check` clean |
| `34.2-PORTED-CHANNELS.md` | Current, honest declaration | VERIFIED (stale re: newest review) | Reflects gap-cycle-1 closures; predates this verification's findings, same currency-gap class as prior rounds |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `handlers.ts:123` | `loggerFlowRegistration.registerLoggerFlows()` | direct call at module scope | WIRED | Confirmed: unconditional, runs on every sidecar boot |
| `GameSubMenu/index.tsx onRepairYesClick` catch | `reportRepairFailure()` | direct function call | WIRED | Confirmed; all 3 signals fire for `Error`/string reasons (repairFailure.test.ts, 4/4 passing) |
| `reportRepairFailure()` | `window.api.logError` | template-literal interpolation of `error: unknown` | **PARTIALLY WIRED** | Works for stringifiable reasons; throws (and skips the dialog signal) for null-prototype/throwing-toString reasons — no test exercises this path (WR-03) |
| `bootstrap.test.ts` | real `bootstrap.init()` | 3 direct calls, no containment mocks | **UNCONTAINED** | Confirmed by direct read and by 3 independent live reproductions of real log-file rotation in this session |
| `testContainment.test.ts` Block B | the 4 in-scope suites' source text | comment-stripped regex match | WIRED but incomplete scope | Correctly gates 4 files; does not gate (and explicitly disclaims covering) the other 11 sidecar suites that also drive real `init()` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `npx tsc --noEmit` stays clean | `npx tsc --noEmit` | exit 0, no output | PASS |
| `cargo check` stays clean | `cd src-tauri && cargo check --quiet` | exit 0, no output | PASS |
| Phase-scoped sidecar suite passes | `npx jest --testPathPattern="src/backend/sidecar/__tests__"` | 23/24 suites, 382/383 tests; sole failure is pre-existing `rustInvokeChannel.test.ts` | PASS (matches documented baseline) |
| Full backend suite matches documented baseline | `npx jest --testPathPattern=src/backend` | 110/111 suites, 2272/2273 tests; sole failure `rustInvokeChannel.test.ts` (confirmed pre-existing per `deferred-items.md`'s bisection) | PASS (matches documented baseline) |
| `processGuards.ts` no longer crashes on a synthetic rejection | observed during full-suite run | `[sidecar] unhandled promise rejection: Error: disk full` printed, process did not crash, run completed | CONFIRMS gap-cycle-1 CR-02 closed |
| Real developer log file destroyed by running the sidecar test suite | `stat gamelib.log{,.old}` before/after 3 separate test invocations in this session | Timestamps moved from 10:49 -> 10:56 -> 10:57, each matching a test run I had just executed; log content matches `bootstrap.ts`'s real boot sequence | CONFIRMS new gap-cycle-2 CR-01 (test containment hole), independently reproduced 3x, not merely inherited from the review |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| REQ-34.2-01 | 34.2-01/02/04/05 | Runner-generic + runner-pinned dispatch channels | SATISFIED | Unaffected by gap cycle 2 |
| REQ-34.2-02 | 34.2-01 | i18next init in sidecar bootstrap | SATISFIED | Unaffected |
| REQ-34.2-03 | 34.2-02, 34.2-11, 34.2-12, 34.2-14 | 19 handler bodies extracted, electron-freedom claim corrected + gate-proven | SATISFIED | Unaffected by gap cycle 2 |
| REQ-34.2-04 | 34.2-02/03/06 | sidecar imports underlying modules directly | SATISFIED | Unaffected |
| REQ-34.2-05 | 34.2-03 | readKnownFixes extracted | SATISFIED | Unaffected |
| REQ-34.2-06 | 34.2-03 | buildCrossoverRatingMap extracted | SATISFIED | Unaffected |
| REQ-34.2-07 | 34.2-01/06, 34.2-09, 34.2-15, 34.2-18 | anticheat listener wired safely, defence-in-depth guard fixed | SATISFIED at the literal-text level; the test infrastructure meant to prove this safety has its own confirmed, currently-reproducing containment defect | `.catch()`/`processGuards.ts` both confirmed fixed; `testContainment.test.ts`/`bootstrap.test.ts` confirmed defective (see gap) |
| REQ-34.2-08 | 34.2-02/05 | 3 metadata-override channels | SATISFIED | Unaffected |
| REQ-34.2-09 | 34.2-02/04/05 | 6 runner-pinned stray channels | SATISFIED | Unaffected |
| REQ-34.2-10 | 34.2-04 | requestGameSettings parity | SATISFIED | Unaffected |
| REQ-34.2-11 | 34.2-06, 34.2-13 | 8 enrichment channels, contracts intact | SATISFIED | Unaffected |
| REQ-34.2-12 | 34.2-06, 34.2-08, 34.2-14, 34.2-16, 34.2-17 | getCrossoverIndex/repair/readConfig timeout audit; real repair-failure signal | SATISFIED for the general case; a narrow residual gap for hostile error values (WR-03, warning) | `logError` registration + positive side-effect test confirmed; `repairFailure.ts`'s edge-case gap confirmed by direct read |
| REQ-34.2-13 | 34.2-07, 34.2-14 | Honest PORTED-CHANNELS.md declaration | SATISFIED | Updated for gap cycle 1; predates this review's findings, same currency-gap class as before |
| REQ-34.2-14 | all | Additive/reversible, build gates green | SATISFIED for tsc/cargo check; **BLOCKED** for the test-safety component | tsc/cargo check clean, confirmed; but the phase's own designated test command destroys real developer data (see gap) |

No orphaned requirements — all 14 IDs from REQUIREMENTS.md (lines 491-504) are declared across the 18 plans' frontmatter (originals 34.2-01..07 plus gap-closure plans 34.2-08..18).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/backend/sidecar/__tests__/testContainment.test.ts` | 279-307 | Declares 11 known-uncontained, destructive test suites as accepted debt rather than closing or gating them | **Blocker** | Real developer data (log files, and structurally, potentially electron-store data) is destroyed every time the sidecar test suite runs — independently reproduced 3x in this session |
| `src/backend/sidecar/__tests__/testContainment.test.ts` | 368-376 | "NO FILESYSTEM WRITES" anti-claim gate matches against comment-stripped source, but the claim can only ever live in a stripped-out docblock line | Warning | The gate can never fail regardless of whether the false claim is present (WR-01) |
| `src/backend/sidecar/loggerFlowRegistration.ts` | 56-58 | `logError(...)` call is neither awaited nor `.catch()`ed; `LogWriter#logBase` is async | Warning | A real log-write failure becomes an unhandled rejection, absorbed only by the (now-fixed) process guard as a generic warning — violates that guard's own documented "not a substitute for call-site handling" invariant (WR-02) |
| `src/frontend/screens/Game/GameSubMenu/repairFailure.ts` | 45 | `${error}` interpolation of an `unknown`-typed value with no try/catch, same defect class CR-02 (processGuards.ts) was hardened against | Warning | Throws for null-prototype/throwing-toString rejection reasons, silently skipping the user-visible ERROR dialog; the suite's own "hostile reason" test doesn't exercise this path (WR-03) |
| `src-tauri/src/main.rs` / `longRunningChannels.test.ts` | 146-152 / 88-129 | No Rust-side test proves `timeout_for()` actually consults `LONG_RUNNING_CHANNELS` | Warning (carried forward, not re-verified as blocker) | If `timeout_for` reverted to unconditional, every existing test still passes |

Debt-marker gate: no unreferenced `TBD`/`FIXME`/`XXX` found in files touched by this gap cycle.

### Human Verification Required

### 1. UAT-34.2-01 — Translated notification renders in a live window

**Test:** Boot `pnpm tauri:dev`, trigger a repair (or another i18next-backed notify path) on a non-Steam-runner game, confirm the notification body is a real sentence.
**Expected:** Real, non-blank, translated text in both dev and a packaged build.
**Why human:** Jest proves `i18next.t()` returns a real string post-init but cannot observe an actual rendered notification or packaged-build locale resolution.

### 2. UAT-34.2-02 — Anticheat data file appears via real network fetch

**Test:** Delete `<appFolder>/areweanticheatyet.json`, boot `pnpm tauri:dev`, wait for the online transition, confirm the file reappears and the download log line appears.
**Expected:** File reappears; `AreWeAntiCheatYet data downloaded` logged.
**Why human:** Jest asserts this under a mocked axios and a tmp home; a real GitHub fetch across a real network transition is unproven by construction.

Both items remain declared and tracked in `34.2-HUMAN-UAT.md` (status: deferred, both `result: pending`) — unaffected by this gap cycle.

### Gaps Summary

**Gap cycle 2 genuinely closed all 3 blockers gap cycle 1 introduced.** The silent `logError` channel is now registered and reachable from a real production path, proven with a positive side-effect test (a real log line read back after a real transport-level frame) rather than absence-of-error. The process-level rejection guard no longer throws for non-stringifiable reasons — I personally observed it correctly absorb a synthetic `Error: disk full` rejection into a logged warning during a live full-suite test run, rather than crashing the process. And `sidecarRejectionGuard.test.ts` now carries the same containment kit as its sibling suites.

**But this cycle repeats the exact pattern of both prior cycles: closing the named gaps while shipping a new defect in the same breath.** The headline new defect is not theoretical: `testContainment.test.ts`, the cycle's own artifact built specifically to prove test containment holds, explicitly documents an 11-suite hole in that containment as "accepted debt" — and one of those 11 suites, `bootstrap.test.ts`, is proven, right now, on this machine, to destroy the real developer's log file. I did not take the code review's word for this. I reproduced it myself, independently, three separate times over the course of this verification session, simply by running this project's own designated test commands and checking file timestamps before and after. This matches a failure class this repository has already suffered and recorded once before (`tests-clobbering-real-steam-store`), and the fact that this cycle's own new test-safety infrastructure catalogs a live instance of it as acceptable, rather than closing it, is the reason status is `gaps_found` rather than `passed`.

Three additional warning-level defects were confirmed by direct code read: a vacuous "no filesystem writes" anti-claim gate that can never fail (WR-01), a new `logError` listener that leaks an unawaited async promise (WR-02, currently harmless only because of the very guard this cycle fixed), and a renderer-side interpolation of an `unknown` error value that reintroduces the CR-02 defect class in a new location, with a test suite that claims to cover the hostile-reason case but does not (WR-03).

**Net assessment:** the phase's core objective — porting the 26 IPC channels — remains genuinely, cleanly done, unaffected across all three verification rounds. All 14 requirement-level truths pass on their literal text. The blocking issue is narrower than gap cycle 1's: it is not in the ported IPC surface itself, but in this cycle's own new test-safety tooling, which documents rather than closes a live, currently-reproducing data-loss defect. This is a well-scoped, mechanically fixable gap (apply the existing four-part containment kit — already written four times this cycle — to the 11 named files, or make containment structural via a jest setupFiles entry), not a wholesale regression in the porting work.

---

_Verified: 2026-07-26T11:00:00Z_
_Verifier: Claude (gsd-verifier)_
