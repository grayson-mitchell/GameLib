---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
verified: 2026-07-26T00:00:00Z
status: gaps_found
score: 14/14 requirement-level truths verified, 3 NEW blocker-severity defects found in the closure work itself
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 11/14
  gaps_closed:
    - "REQ-34.2-12: repair/readConfig added to LONG_RUNNING_CHANNELS in src-tauri/src/main.rs — confirmed present at lines 132-143, cargo check clean"
    - "REQ-34.2-07: void downloadAntiCheatData(...) now has a .catch() at the call site (bootstrap.ts:248-253) — confirmed by direct read and by sidecarRejectionGuard.test.ts's Group 1 RED-PROOF test"
    - "REQ-34.2-03: REQUIREMENTS.md text and dispatch.ts/enrichmentFlowRegistration.ts docstrings corrected 2026-07-26 to state the TRUE invariant (no direct import; transitive reach exists and is rescued by electronStub); electronReachLedger.test.ts now measures and pins the real transitive graph"
  gaps_remaining: []
  regressions:
    - "GAP-CYCLE-1 CR-01: the new renderer try/catch around `repair()` (GameSubMenu/index.tsx:143-149), added to close REQ-34.2-12's 'no unhandled rejection' item, logs via `window.api.logError` — an unregistered `send` channel on the sidecar. Under Tauri this is a total no-op (this repo's own recorded `sidecar-send-channels-fail-silently` class): a real repair failure now produces ZERO signal anywhere (no console line, no log file entry, no dialog), which is worse for observability than the pre-fix unhandled rejection."
    - "GAP-CYCLE-1 CR-02: `installUnhandledRejectionGuard` (processGuards.ts:48-49), the defence-in-depth guard built specifically to close REQ-34.2-07 gap #2, builds `String(reason)` OUTSIDE its own try/catch. Independently reproduced: for a non-stringifiable rejection reason (e.g. `Object.create(null)`), this throws inside the `unhandledRejection` listener, which Node turns into an unhandled `uncaughtException` (no handler installed anywhere) — killing the sidecar. This is the exact `sidecar-dialog-reject-crashes` failure class the guard exists to prevent, reintroduced by the guard itself."
    - "GAP-CYCLE-1 CR-03: `sidecarRejectionGuard.test.ts` (created by plan 34.2-09 to prove CR-02 is fixed) does not apply the `pathShim` mock that the SAME cycle's plan 34.2-10 applied to the other two suites for the identical finding. It only mocks `os.homedir()`; `pathShim.resolveAppDataDir()` prefers `env.APPDATA`/`env.XDG_CONFIG_HOME` over `homedir()` on Windows/Linux, and `constants/paths.ts`'s module-scope legacy-folder migration (`renameSync`) runs against the REAL resolved appFolder before the suite's own containment tripwire can fail loudly. On Windows (APPDATA is virtually always set) this suite is unconditionally red and carries a real data-loss risk matching this repo's own recorded `tests-clobbering-real-steam-store` incident. Confirmed NOT reproducible on the current macOS environment (ran green here); confirmed by source-level static analysis of the Windows/Linux code paths."
gaps:
  - truth: "A real repair failure produces a visible signal (console line, log entry, or user-facing dialog) under both Electron and the Tauri sidecar"
    status: failed
    reason: "window.api.logError is a send-kind channel whose ONLY registration in the whole backend is src/backend/logger/ipc_handler.ts:15 (import './logger/ipc_handler' in main.ts, Electron-only). No file under src/backend/sidecar/ registers 'logError' — confirmed via grep across src/backend/sidecar/. Under Tauri, dispatchSend (sidecarRpc.ts:138-150) looks up an empty listener array for this channel and does nothing: no reject, no timeout, no UNPORTED_CHANNEL_MARKER, no console output, no log line. The catch block added by plan 34.2-08 (GameSubMenu/index.tsx:143-149) therefore converts a real repair failure into complete silence under the platform this whole phase exists to port to."
    artifacts:
      - path: "src/frontend/screens/Game/GameSubMenu/index.tsx"
        issue: "onRepairYesClick's catch only calls window.api.logError, which is a no-op under the sidecar; no dialog, toast, or in-flight-state cleanup either"
      - path: "src/backend/sidecar/handlers.ts"
        issue: "does not import or register logger/ipc_handler.ts's addListener('logError', ...)"
    missing:
      - "Register 'logError' as a curated send-kind channel on the sidecar (a one-line ipcMain.on('logError', ...) in a curated registration module, matching logger/ipc_handler.ts:15's body), OR route the renderer catch through a channel this slice already ported/proved live"
      - "Add a positive side-effect test proving the log line is actually observable through the sidecar transport for this call site — absence of a thrown error is not acceptable evidence for a send channel (this slice's own stated standard, REQ-34.2-08/09's cross-cutting send-channel proof rule)"
      - "Surface user-facing feedback (dialog/toast) on repair failure — today the repair dialog just closes with nothing else happening on either build"
  - truth: "installUnhandledRejectionGuard (the sidecar's process-level defence-in-depth guard) never itself throws, re-throws, or terminates the process, for any rejection reason"
    status: failed
    reason: "processGuards.ts:49 builds `const message = ... + String(reason)` OUTSIDE its own try (the try only wraps the logWarning call at lines 50-62). Independently reproduced with a minimal repro mirroring the exact code shape: emitting 'unhandledRejection' with a null-prototype reason (or any value with a throwing/absent Symbol.toPrimitive/toString) makes String(reason) throw, and that throw propagates out of the listener's emit() call. Node converts an exception thrown from within an 'unhandledRejection' listener into an uncaughtException; no uncaughtException handler is installed anywhere in the sidecar (confirmed via grep), so the process dies. The module's own docstring states the absolute contract 'must NEVER re-throw... Its own logging is wrapped in a try/catch' — this is false for the message-construction step, verified empirically, not merely asserted by the code reviewer."
    artifacts:
      - path: "src/backend/sidecar/processGuards.ts"
        issue: "String(reason) at line 49 sits outside the try/catch at lines 50-62, so a non-stringifiable rejection reason throws out of the unhandledRejection listener"
    missing:
      - "Move the message construction inside the try, with a hardcoded fallback message used if String()/template interpolation itself throws (see code-review 34.2-REVIEW-GAP-CYCLE-1.md's CR-02 fix snippet for a concrete implementation)"
      - "Add a Group 2 test case: expect(() => target.emit('unhandledRejection', Object.create(null))).not.toThrow()"
  - truth: "The suite created to prove CR-02 (REQ-34.2-07 gap #2) is fixed applies the same real-filesystem containment remedy the cycle applied to the other two rewritten suites, and is not itself red on any target platform"
    status: failed
    reason: "sidecarRejectionGuard.test.ts mocks only os.homedir(), not ../pathShim (unlike gameDetailsFlows.test.ts and enrichmentFlows.test.ts, fixed for the identical finding by the SAME gap cycle's plan 34.2-10). The suite's own docstring claims 'NO FILESYSTEM WRITES... this suite never calls any directory-creation, file-write, or file-removal Node fs API itself' -- this is false: every test calls the REAL (unmocked) bootstrap.init(), which calls initLogger(), which creates/rotates a real log file. On Windows, env.APPDATA is virtually always set, so pathShim.resolveAppDataDir() returns the REAL AppData path regardless of the os.homedir() mock; constants/paths.ts's module-scope legacy-folder migration (existsSync/renameSync against the real resolved appFolder) executes at require-time, BEFORE the suite's own beforeAll containment tripwire assertion can fail. The suite is therefore unconditionally red on Windows (and on Linux with XDG_CONFIG_HOME set), and poses a genuine data-loss risk matching this repo's own previously-recorded tests-clobbering-real-steam-store incident. Independently confirmed green on the current macOS verification environment (ran locally: 17 suites / 236 tests pass) -- the defect is real but platform-conditional, not observable here."
    artifacts:
      - path: "src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts"
        issue: "No jest.mock('../pathShim', ...) factory (compare gameDetailsFlows.test.ts:136-161); docstring's 'NO FILESYSTEM WRITES' claim is false given the real, unmocked init()/initLogger() call in every test"
    missing:
      - "Copy the exact jest.mock('../pathShim', ...) factory from gameDetailsFlows.test.ts into this suite (same tmp-root shape, per-suite namespacing)"
      - "Correct the docstring to state that init() writes a real log file inside the redirected tmp root, and extend the containment tripwire to also cover the log path (getLogFilePath/getBaseLogPath), which none of the three rewritten suites currently check (WR-01 in the gap-cycle review)"
deferred: []
human_verification:
  - test: "UAT-34.2-01 — a translated notification string actually renders in a live Tauri window (D-02 / REQ-34.2-02)"
    expected: "Triggering repair (or another i18next-backed notify path) shows a real, non-blank, non-'undefined' translated sentence in both `pnpm tauri:dev` and a packaged build"
    why_human: "Jest proves i18next.t() returns a real string after init() and that repair's notify body reflects it over the real sidecar RPC loop, but cannot see a notification actually render in a window, nor exercise a packaged build's locale-path resolution"
  - test: "UAT-34.2-02 — the anticheat data file actually appears on disk via a real network fetch (D-07 / REQ-34.2-07)"
    expected: "After deleting <appFolder>/areweanticheatyet.json and running pnpm tauri:dev through an online transition, the file reappears and the 'AreWeAntiCheatYet data downloaded' log line appears in gamelib.log"
    why_human: "Jest asserts the event fires and the file is written under a tmp home with a mocked axios; it cannot prove a real GitHub fetch completes in a live process across a real network after a real connectivity transition"
---

# Phase 34.2: Tauri IPC re-plumb slice 5 — game details, settings and overrides — Verification Report (RE-VERIFICATION after gap cycle 1)

**Phase Goal:** Port the game details, settings and overrides IPC cluster (26 channels): per-game info/settings/overrides, SDL selection, launch options, install-path changes, version pinning, repair/kill, and the enrichment surfaces (wiki game info, anticheat status, known fixes, CrossOver index, store search, recent games). Additive and reversible — the Electron build keeps working unchanged.
**Verified:** 2026-07-26T00:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — after gap cycle 1 (plans 34.2-08..14)

## Goal Achievement

This is a re-verification. The prior `34.2-VERIFICATION.md` (2026-07-25) found 11/14 requirement-level truths verified, with REQ-34.2-03, REQ-34.2-07, and REQ-34.2-12 FAILED. Seven gap-closure plans (34.2-08..14) executed since, and a fresh code review (`34.2-REVIEW-GAP-CYCLE-1.md`) found 3 NEW critical issues in that closure work. All three original gaps, and all three new review findings, were independently re-derived from the live source tree below — none of the conclusions below are inherited from either document without direct confirmation.

### Observable Truths

| # | Truth (REQ) | Status | Evidence |
|---|---|---|---|
| 1 | REQ-34.2-01: 9 runner-generic dispatch channels ported verbatim across all 6 managers | VERIFIED (regression) | Untouched by gap cycle; `dispatch.ts` unchanged in scope, full suite still green |
| 2 | REQ-34.2-02: i18next initialized in sidecar bootstrap | VERIFIED (regression) | Untouched by gap cycle; `bootstrapWirings.test.ts` still green |
| 3 | REQ-34.2-03: 19 handler bodies extracted; the STATED invariant (no direct electron/backend-ipc import; transitive reach exists and is rescued at runtime) is proven by a gate, not overclaimed | **VERIFIED (gap closed)** | REQUIREMENTS.md line 493 rewritten 2026-07-26 (plan 34.2-11) to state the true property; `dispatch.ts`/`enrichmentFlowRegistration.ts` docstrings corrected; `electronReachLedger.test.ts` measures the real transitive import graph (193 visited files, `'electron'` specifier tracked) and passes. Confirmed by direct read of REQUIREMENTS.md and the test file, and by running the suite (green) |
| 4 | REQ-34.2-04: sidecar imports underlying feature modules directly, never ipc_handler.ts | VERIFIED (regression) | Unaffected |
| 5 | REQ-34.2-05: readKnownFixes extracted | VERIFIED (regression) | Unaffected |
| 6 | REQ-34.2-06: buildCrossoverRatingMap extracted, D-16 contract preserved | VERIFIED (regression) | Unaffected |
| 7 | REQ-34.2-07: fetchLastestReleases()/releasesInfoReady→downloadAntiCheatData wiring is safely re-homed | **VERIFIED at the primary-defect level, but see NEW gap #2/#3 below** | The specific defect the original gap #2 named (`void downloadAntiCheatData(...)` unguarded) is fixed: `bootstrap.ts:244-253` now attaches `.catch((error) => logWarning(...))` directly at the call site, confirmed by direct read and by `sidecarRejectionGuard.test.ts` Group 1's RED-PROOF test (independently run, passes). However, the SAME closure plan (34.2-09) also introduced a NEW process-level guard (`processGuards.ts`) as declared defence-in-depth, and that guard has its own confirmed crash-causing defect (gap #2 below) — plus the test suite written to prove the fix has its own confirmed platform-conditional defect (gap #3 below). The requirement's literal text ("safely re-homed") is undermined by the safety-net code the closure itself introduced. |
| 8 | REQ-34.2-08: 3 metadata-override channels ported, write proven by side effect | VERIFIED (regression) | Unaffected |
| 9 | REQ-34.2-09: 6 runner-pinned stray channels ported at parity | VERIFIED (regression) | Unaffected |
| 10 | REQ-34.2-10: requestGameSettings left at parity | VERIFIED (regression) | Unaffected |
| 11 | REQ-34.2-11: 8 enrichment channels ported with contracts intact | VERIFIED, strengthened | Plan 34.2-13 (WR-09 closure) consolidated the Phase 20 D-14 rethrow contract for the 3 storeSearch channels into a single shared module (`storeSearch/handlers.ts`), imported by BOTH `storeSearch/index.ts` (Electron) and `enrichmentFlowRegistration.ts` (sidecar) — confirmed by direct read, no more hand-copied duplicate |
| 12 | REQ-34.2-12: getCrossoverIndex exempted from 60s timeout; getWikiGameInfo measured; newly-reachable long-running surface audited | **VERIFIED (gap closed)** | `src-tauri/src/main.rs:132-143`: `LONG_RUNNING_CHANNELS` now contains `install, updateGame, uninstall, checkGameUpdates, refreshLibrary, getCrossoverIndex, repair, readConfig` — confirmed by direct read; `cargo check --quiet` clean. The catch added at the renderer call site (GameSubMenu/index.tsx) does prevent an unhandled promise rejection in the literal sense, but see NEW gap #1 below for the silent-failure regression that fix introduces |
| 13 | REQ-34.2-13: 34.2-PORTED-CHANNELS.md declares all 26 channels honestly | VERIFIED, but again stale | Document was updated (plan 34.2-14) to record the gap-cycle-1 closures (repair/readConfig timeout rows, CR-02 fix, transitive-electron correction) — confirmed by direct read (lines 33-37, 56-58, 117-121, 354-360). It again predates the review that found the 3 NEW blockers (same currency-gap class as the prior round, not a dishonesty finding — it does not misrepresent anything it discusses) |
| 14 | REQ-34.2-14: additive/reversible holds behaviorally; no direct electron import; cargo check/tsc stay clean | VERIFIED | `npx tsc --noEmit` clean (independently re-run); `cargo check --quiet` clean (independently re-run); full backend suite 108/109 suites, 2237/2238 tests — sole failure is the previously-documented, pre-existing (34.1-era) `rustInvokeChannel.test.ts`, independently re-run and confirmed unrelated to this phase |

**Score:** 14/14 requirement-level truths verified — BUT 3 new blocker-severity defects were independently confirmed in the gap-cycle-1 closure work itself (see below), which is why overall status is `gaps_found` rather than `passed`.

### New Blocker-Severity Findings (independently confirmed, not inherited from the review)

| # | Finding | Independent confirmation performed | Severity |
|---|---|---|---|
| 1 | GAP-CYCLE-1 CR-01 — `GameSubMenu/index.tsx`'s repair-failure catch logs via `window.api.logError`, an unregistered `send` channel on the sidecar | Grepped `src/backend/sidecar/` for any `logError` registration — none found; confirmed `logger/ipc_handler.ts:15` (the only registration) is imported only by `main.ts` (Electron); read `sidecarRpc.ts`'s `dispatchSend` (empty listener array → silent no-op) | Blocker |
| 2 | GAP-CYCLE-1 CR-02 — `installUnhandledRejectionGuard` builds `String(reason)` outside its own try, can itself crash the sidecar | Independently reproduced with a minimal Node script mirroring the exact code shape: `emit('unhandledRejection', Object.create(null))` throws out of `emit()` | Blocker |
| 3 | GAP-CYCLE-1 CR-03 — `sidecarRejectionGuard.test.ts` (created by this same gap cycle) omits the `pathShim` mock its sibling suites were fixed with, is destructive and Windows-red | Read the test file in full (no `pathShim` mock present); read `pathShim.ts`/`constants/paths.ts` to confirm `APPDATA`/`XDG_CONFIG_HOME` bypass the `os.homedir()` mock; ran the suite locally (green on macOS, matching the review's platform-conditional claim) | Blocker (platform-conditional) |

All three were re-derived independently against the live source tree (not merely trusted from `34.2-REVIEW-GAP-CYCLE-1.md`), including one live empirical reproduction (#2) and one live test run (#3) beyond what the review itself documented.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src-tauri/src/main.rs` `LONG_RUNNING_CHANNELS` | `repair`/`readConfig` added | VERIFIED | Lines 132-143, confirmed present |
| `src/backend/sidecar/bootstrap.ts` Block A | `.catch()` on `downloadAntiCheatData(...)` | VERIFIED | Lines 244-253, confirmed present and correctly wired |
| `src/backend/sidecar/processGuards.ts` | Process-level `unhandledRejection` guard, never throws | **DEFECTIVE** | Exists and is wired (`src/sidecar/index.ts`), but throws for non-stringifiable rejection reasons — see gap #2 |
| `src/frontend/screens/Game/GameSubMenu/index.tsx` | Renderer catch around `repair()` with real user/log signal | **DEFECTIVE** | Catch exists, but logs through a dead channel under Tauri — see gap #1 |
| `src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts` | Proves CR-02 fixed, uses the same containment remedy as sibling suites | **DEFECTIVE** | Suite exists and passes on macOS, but omits the `pathShim` mock — see gap #3 |
| `src/backend/sidecar/__tests__/electronReachLedger.test.ts` | Measures/pins the real transitive electron-reach graph | VERIFIED | 193 files visited, `'electron'` specifier tracked, passes |
| `src/backend/storeSearch/handlers.ts` | Single source of truth for the 3 storeSearch handler bodies | VERIFIED | Both registration sites import it; confirmed by direct read |
| `34.2-PORTED-CHANNELS.md` | Current, honest declaration | VERIFIED (stale re: newest review) | Reflects gap-cycle-1 closures; predates this verification's 3 new findings |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `src-tauri/src/main.rs` `timeout_for` | `repair`/`readConfig` channels | `LONG_RUNNING_CHANNELS.contains()` | WIRED | Confirmed present; no Rust-side test proves `timeout_for` is actually consulted (WR-03, warning-level, not re-verified as blocker) |
| `bootstrap.ts` Block A listener | `downloadAntiCheatData` | direct `.catch()` at call site | WIRED | Confirmed; RED-PROOF test independently run and passes |
| `src/sidecar/index.ts` | `processGuards.installUnhandledRejectionGuard` | import + call before `init()` | WIRED, but ordering claim is weaker than documented | `bootstrap`'s import appears first in the file; ES module static-import evaluation order means `bootstrap.ts`'s own module scope runs before `processGuards.ts`'s (both before either's guard call) — benign today (no rejection can occur synchronously during either module's own load), but the docstring's precise ordering claim is not what the code achieves (WR-04, warning, not re-verified as blocker) |
| `GameSubMenu/index.tsx onRepairYesClick` | `window.api.logError` | `send`-kind IPC call | **NOT FUNCTIONALLY WIRED under Tauri** | Channel has no registered listener on the sidecar — see gap #1 |
| `storeSearch/index.ts` + `enrichmentFlowRegistration.ts` | `storeSearch/handlers.ts` | direct function imports | WIRED | Confirmed, single implementation, both call sites are bare delegations |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| `npx tsc --noEmit` stays clean | `npx tsc --noEmit` | exit 0, no output | PASS |
| `cargo check` stays clean | `cd src-tauri && cargo check --quiet` | exit 0, no output | PASS |
| Phase-scoped + gap-cycle test suites pass | `npx jest gameDetailsFlows enrichmentFlows bootstrapWirings gameDetailsImportGate gameDetailsModules knownFixes longRunningChannels ratingMap sidecarRejectionGuard electronReachLedger storeSearch` | 17 suites, 236 tests, all passing | PASS |
| Full backend suite matches documented 108/109, 2237/2238 | `npx jest --testPathPattern=src/backend` | 108 passed / 109 total suites, 2237/2238 tests; sole failure `rustInvokeChannel.test.ts` | PASS (matches documented, pre-existing, out-of-scope failure exactly) |
| `repair`/`readConfig` now IN `LONG_RUNNING_CHANNELS` | `grep -n "repair\|readConfig" src-tauri/src/main.rs` (lines 132-143) | both present | CONFIRMS gap #1 (original) closed |
| `processGuards.ts` guard throws on non-stringifiable rejection | minimal Node repro mirroring exact code shape | `listener emit THROWS out of emit(): Cannot convert object to primitive value` | CONFIRMS NEW gap #2 |
| `logError` has no sidecar registration | `grep -rn "logError" src/backend/sidecar/` | 0 registration matches (only comments/mocks) | CONFIRMS NEW gap #1 |
| `sidecarRejectionGuard.test.ts` green on current (macOS) environment | `npx jest sidecarRejectionGuard --silent` | PASS locally | Consistent with review's "platform-conditional, not observable on macOS" framing for NEW gap #3 |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| REQ-34.2-01 | 34.2-01/02/04/05 | Runner-generic + runner-pinned dispatch channels | SATISFIED | Unaffected by gap cycle |
| REQ-34.2-02 | 34.2-01 | i18next init in sidecar bootstrap | SATISFIED | Unaffected |
| REQ-34.2-03 | 34.2-02, 34.2-11, 34.2-12, 34.2-14 | 19 handler bodies extracted, electron-freedom claim corrected + gate-proven | **SATISFIED (gap closed)** | REQUIREMENTS.md text + electronReachLedger.test.ts |
| REQ-34.2-04 | 34.2-02/03/06 | sidecar imports underlying modules directly | SATISFIED | Unaffected |
| REQ-34.2-05 | 34.2-03 | readKnownFixes extracted | SATISFIED | Unaffected |
| REQ-34.2-06 | 34.2-03 | buildCrossoverRatingMap extracted | SATISFIED | Unaffected |
| REQ-34.2-07 | 34.2-01/06, 34.2-09 | anticheat listener wired safely | **SATISFIED at primary-defect level; 2 new defects in the safety-net code (see gaps #2/#3)** | bootstrap.ts .catch() confirmed; processGuards.ts + its test suite are defective |
| REQ-34.2-08 | 34.2-02/05 | 3 metadata-override channels | SATISFIED | Unaffected |
| REQ-34.2-09 | 34.2-02/04/05 | 6 runner-pinned stray channels | SATISFIED | Unaffected |
| REQ-34.2-10 | 34.2-04 | requestGameSettings parity | SATISFIED | Unaffected |
| REQ-34.2-11 | 34.2-06, 34.2-13 | 8 enrichment channels, contracts intact | SATISFIED, strengthened | storeSearch/handlers.ts single-source consolidation confirmed |
| REQ-34.2-12 | 34.2-06, 34.2-08, 34.2-14 | getCrossoverIndex/repair/readConfig timeout audit | **SATISFIED (gap closed); renderer catch introduces a new silent-failure defect (gap #1)** | main.rs confirmed; renderer catch exists but logs to a dead channel |
| REQ-34.2-13 | 34.2-07, 34.2-14 | Honest PORTED-CHANNELS.md declaration | SATISFIED | Updated for gap cycle 1; predates this review's findings (same currency-gap class as before) |
| REQ-34.2-14 | all | Additive/reversible, build gates green | SATISFIED | tsc/cargo check clean, confirmed |

No orphaned requirements — all 14 IDs accounted for across the 7 original plans plus the 7 gap-closure plans' frontmatter.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/frontend/screens/Game/GameSubMenu/index.tsx` | 143-149 | Error handler logs via an unregistered/no-op `send` channel under Tauri | **Blocker** | Real repair failures produce zero signal on the target platform (gap #1) |
| `src/backend/sidecar/processGuards.ts` | 48-49 | `String(reason)` built outside its own try/catch, contradicting the module's own stated contract | **Blocker** | A non-stringifiable rejection reason crashes the sidecar — reintroduces the exact failure class the guard exists to prevent (gap #2) |
| `src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts` | 29-76, 280-289 | Missing `pathShim` mock; docstring's "NO FILESYSTEM WRITES" claim is false (real `init()`/`initLogger()` runs) | **Blocker (platform-conditional)** | Real, unmocked log-file writes; unconditionally red on Windows and Linux-with-`XDG_CONFIG_HOME`; data-loss risk matching a previously-recorded repo incident (gap #3) |
| `src-tauri/src/main.rs` / `longRunningChannels.test.ts` | 146-152 / 88-129 | No Rust-side test proves `timeout_for()` actually consults `LONG_RUNNING_CHANNELS` (regex-over-source-text only) | Warning | If `timeout_for` is ever reverted to unconditionally bound, every existing test still passes (WR-03, not re-verified as blocker — carried forward as accepted debt) |
| `src/sidecar/index.ts` docstring | 20-22 | Claims `installUnhandledRejectionGuard()` covers `bootstrap.ts`'s module scope; ES import-evaluation order means it does not, though this is currently benign | Warning | Documentation overclaims an ordering guarantee (WR-04, not re-verified as blocker) |
| `src/backend/sidecar/__tests__/gameDetailsImportGate.test.ts` | 283-373 | sha256 pins hash raw file bytes with no CRLF normalization | Warning | Fails on any Windows checkout with `core.autocrlf=true` even with zero real changes (WR-02, not re-verified as blocker, carried forward) |
| `src/backend/sidecar/__tests__/electronReachLedger.test.ts` | 212-236, 295-301 | Vacuity guards (`> 100` vs measured 193; unresolved-specifier drop) are far weaker than the header claims | Warning | A genuinely broken resolver could silently lose up to ~48% of the graph and still pass (WR-05, carried forward) |

Debt-marker gate: no unreferenced `TBD`/`FIXME`/`XXX` found in files touched by the gap cycle.

### Human Verification Required

### 1. UAT-34.2-01 — Translated notification renders in a live window

**Test:** Boot `pnpm tauri:dev`, trigger a repair (or another i18next-backed notify path) on a non-Steam-runner game, confirm the notification body is a real sentence.
**Expected:** Real, non-blank, translated text in both dev and a packaged build.
**Why human:** Jest proves `i18next.t()` returns a real string post-init but cannot observe an actual rendered notification or packaged-build locale resolution.

### 2. UAT-34.2-02 — Anticheat data file appears via real network fetch

**Test:** Delete `<appFolder>/areweanticheatyet.json`, boot `pnpm tauri:dev`, wait for the online transition, confirm the file reappears and the download log line appears.
**Expected:** File reappears; `AreWeAntiCheatYet data downloaded` logged.
**Why human:** Jest asserts this under a mocked axios and a tmp home; a real GitHub fetch across a real network transition is unproven by construction.

Both items remain declared and tracked in `34.2-HUMAN-UAT.md` (status: deferred, both `result: pending`) — unaffected by the gap cycle.

### Gaps Summary

**The three original requirement-level gaps are genuinely closed.** REQ-34.2-12's `repair`/`readConfig` timeout exemption is present in `src-tauri/src/main.rs` and `cargo check` stays green. REQ-34.2-07's specific unguarded-rejection defect is fixed with a direct `.catch()` at the `downloadAntiCheatData` call site, independently proven by a RED-PROOF test that fails if the fix is reverted. REQ-34.2-03's over-claimed transitive-electron-freedom property has been corrected in both the requirement text and the code docstrings, and is now backed by a real measurement of the transitive import graph rather than a depth-1 regex.

**But the closure work introduced three new, independently confirmed blocker-severity defects, all inside the SAME plans that closed the original gaps:**

1. **GAP-CYCLE-1 CR-01 (plan 34.2-08's renderer fix, REQ-34.2-12):** the repair-failure catch handler logs through `window.api.logError`, a channel with zero registration anywhere under `src/backend/sidecar/`. Under Tauri this call is a complete, silent no-op — worse for observability than the pre-fix unhandled rejection, since at least that produced a visible renderer-console error.
2. **GAP-CYCLE-1 CR-02 (plan 34.2-09's defence-in-depth guard, REQ-34.2-07):** `installUnhandledRejectionGuard` builds its log message outside its own try/catch. Independently reproduced: a non-stringifiable rejection reason throws out of the guard's own listener, which Node escalates to an uncaught, unhandled exception — killing the sidecar. This is precisely the crash class (`sidecar-dialog-reject-crashes`) this module's extensive docstring says it exists to prevent, reintroduced by the module itself.
3. **GAP-CYCLE-1 CR-03 (plan 34.2-09's own test suite, REQ-34.2-07):** `sidecarRejectionGuard.test.ts`, written to prove CR-02 is fixed, does not apply the `pathShim` mock its two sibling suites were fixed with in the SAME gap cycle (plan 34.2-10, for the identical finding). It drives the real, unmocked `bootstrap.init()` → `initLogger()` path, and its containment tripwire cannot catch the real-filesystem interaction on Windows (or Linux with `XDG_CONFIG_HOME` set) because `APPDATA`/`XDG_CONFIG_HOME` bypass the mocked `os.homedir()` before the tripwire's own assertion runs. Confirmed green on the current macOS environment; confirmed via source-level analysis to be unconditionally red, and a real data-loss risk, on Windows.

All three findings were independently re-derived against the live source tree during this verification — including a live empirical Node reproduction of CR-02's crash and a live local test run confirming CR-03's platform-conditional (not universally observable) nature — not merely inherited from `34.2-REVIEW-GAP-CYCLE-1.md`'s narrative.

**Net assessment:** the phase's core objective — porting the 26 IPC channels — is genuinely, cleanly done, and all 14 requirement-level truths pass on their own literal text. However, `gaps_found` is the correct status because a `passed` re-verification cannot be issued while the SAME cycle's own safety-net code contains a confirmed process-crash defect (gap #2) and a confirmed silent-failure defect (gap #1) in code paths this phase's own requirements (REQ-34.2-07, REQ-34.2-12) exist specifically to make safe. These are narrow, well-scoped, mechanically fixable defects (the review's own fix snippets are directly usable), not a wholesale regression in the porting work.

---

_Verified: 2026-07-26T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
