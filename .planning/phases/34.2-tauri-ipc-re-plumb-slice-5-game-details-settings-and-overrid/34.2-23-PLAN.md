---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 23
type: execute
wave: 2
gap_closure: true
gap_cycle: 3
depends_on: [34.2-19, 34.2-20]
files_modified:
  - src/backend/sidecar/__tests__/testContainment.test.ts
autonomous: true
requirements: [REQ-34.2-07, REQ-34.2-14]

must_haves:
  truths:
    - "The 'no longer claims NO FILESYSTEM WRITES' gate can actually fail — reintroducing the claim in a docblock turns it red"
    - "Every *.test.ts file in src/backend/sidecar/__tests__/ is accounted for by the gate; a new suite cannot be invisible to it"
    - "The gate fails if the structural containment setupFiles entry is removed from the backend jest project"
    - "The file no longer declares an 11-suite containment hole as accepted debt, because the hole is closed structurally"
    - "Every gate in the file carries a self-test proving that gate can fail"
  artifacts:
    - path: "src/backend/sidecar/__tests__/testContainment.test.ts"
      provides: "Non-vacuous containment gates with derived coverage over the whole directory"
      contains: "readdirSync"
  key_links:
    - from: "src/backend/sidecar/__tests__/testContainment.test.ts"
      to: "src/backend/jest.config.js setupFiles entry"
      via: "source assertion over the backend jest project config"
      pattern: "jest\\.setupContainment"
    - from: "src/backend/sidecar/__tests__/testContainment.test.ts"
      to: "every *.test.ts in its own directory"
      via: "readdirSync-derived set-equality tripwire"
      pattern: "readdirSync"
---

<objective>
Harden the gate file that gap cycle 2 shipped defective. `testContainment.test.ts` currently
carries three problems this cycle must close:

1. **WR-01 — a gate that can never fail.** The "no longer claims NO FILESYSTEM WRITES" assertion
   (lines 368-376) matches against COMMENT-STRIPPED source, but that claim can only ever appear
   on a `*`-prefixed docblock line, which `stripComments` (lines 148-153) always removes before
   the assertion runs. The reviewer proved this empirically by injecting the claim and observing
   `claim present in raw: true / claim present after strip: false`.
2. **The 11-suite declared hole.** `KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES` (lines 295-307)
   documents an open data-loss defect as accepted debt. Plan 34.2-19 closes that hole
   structurally via a backend-project `setupFiles` entry, so this list is now a false statement
   about the tree and must be replaced rather than maintained.
3. **WR-08 — a gate blind to growth.** The anti-vacuity checks pin only list LENGTH
   (`toHaveLength(4)` / `toHaveLength(11)`), so a suite added tomorrow is invisible. The
   directory currently holds 24 `*.test.ts` files; 4 are `IN_SCOPE_SUITES` and 11 are
   `KNOWN_UNCOVERED_...`, leaving **9 classified by neither list**
   (`appShellImportGate`, `dialogStub`, `electronReachLedger`, `fileStore`,
   `gameDetailsImportGate`, `keyringTokenStore`, `lifecycleStub`, `storeLayer`, and
   `testContainment` itself). Plan 34.2-19 adds a 25th (`structuralContainment.test.ts`). A
   derived tripwire must account for ALL of them, not assume 4+11 covers the tree.

While in this file, also close WR-04 (a load-bearing mock mislabelled "parity only", which invites
an edit that makes RED-PROOF-2 vacuous) and WR-07 (process-wide `process.platform` and env
mutation restored only in `afterAll` — the pattern this same file's docstrings declare unsafe).

Purpose: this phase has produced three consecutive `gaps_found` verdicts, each shipping a fresh
defect alongside the fix. Every gate this plan touches must be demonstrated capable of failing.
Output: a `testContainment.test.ts` whose every assertion is proven non-vacuous.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW-GAP-CYCLE-2.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VERIFICATION.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-19-SUMMARY.md

<interfaces>
<!-- Current structure of the file being modified. Line numbers are pre-edit. -->

src/backend/sidecar/__tests__/testContainment.test.ts:
  - :44         `const TMP_ROOT_NAME` — referenced from a hoisted `jest.mock('os', ...)` factory at :48-55 (WR-06)
  - :61-86      `jest.mock('../pathShim', ...)`
  - :92-120     `jest.mock('backend/logger/paths', ...)`
  - :126-133    `jest.mock('backend/constants/environment', ...)` — comment above it calls this
                "included for parity"; that is FACTUALLY WRONG (WR-04): `logger/paths.ts`'s
                `getBaseLogPath()` branches on `isWindows`/`isMac` imported from THIS module and
                never reads `process.platform`. Only `pathShim.ts:31` reads `process.platform`.
                The `isMac:false` pin is what makes RED-PROOF-2 non-vacuous on a darwin host.
  - :148-153    `stripComments(source)` — drops lines matching `/^\s*(\/\/|\*|\/\*)/`
  - :155-269    Block A — `beforeAll` sets four env sentinels and forces `process.platform` to
                `'linux'`; `afterAll` restores (WR-07)
  - :279-284    `IN_SCOPE_SUITES` (4 entries) + `toHaveLength(4)` anti-vacuity pin
  - :295-307    `KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES` (11 entries) + `toHaveLength(11)` pin
  - :326-366    Three `it.each(IN_SCOPE_SUITES)` pattern-presence gates (pathShim mock,
                backend/logger/paths mock, `getLogFilePath({})` tripwire) — these are CORRECT on
                stripped source and must keep using it
  - :368-376    The vacuous anti-claim gate (WR-01) — must move to RAW source
  - :382-389    An existing gate self-test for `stripComments` — the shape to replicate

Landing from wave 1 (both complete before this plan runs):
  - `src/backend/jest.setupContainment.ts` — the structural containment module (plan 34.2-19)
  - `src/backend/jest.config.js` — now carries a `setupFiles` entry naming that module
  - `src/backend/sidecar/__tests__/structuralContainment.test.ts` — a 25th suite in this directory
  - `src/backend/sidecar/__tests__/bootstrap.test.ts` — now carries its own containment tripwire
  - `src/backend/sidecar/__tests__/loggerFlows.test.ts` — extended by plan 34.2-20; it is one of
    the four `IN_SCOPE_SUITES`, so this gate reads its source and must still pass over it

This repo's precedent for a gate that carries a self-test:
  `src/backend/sidecar/__tests__/gameDetailsImportGate.test.ts` (its Gate-2 self-test).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the anti-claim gate able to fail, correct the mislabelled load-bearing mock, and stop the worker-wide platform leak</name>
  <files>src/backend/sidecar/__tests__/testContainment.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/testContainment.test.ts (the file being modified — read it in full; every edit in this task is inside it)
    - src/backend/logger/paths.ts (the evidence for WR-04: `getBaseLogPath()` imports `isWindows`/`isMac` from `../constants/environment` and never reads `process.platform`)
    - src/backend/sidecar/pathShim.ts (the counterpart: line 31 DOES read `process.platform`, which is why both the platform force and the environment mock are needed for different reasons)
    - src/backend/sidecar/__tests__/gameDetailsImportGate.test.ts (the gate-self-test convention to mirror)
  </read_first>
  <behavior>
    - Test 1 (WR-01 fixed gate): the anti-claim gate matches RAW source, so a `NO FILESYSTEM WRITES` claim written inside a docblock in any in-scope suite turns it red.
    - Test 2 (WR-01 gate self-test): a synthetic source string carrying the claim ONLY inside a `*`-prefixed docblock line is detected by the raw matcher AND is demonstrably NOT detected by `stripComments`-then-match — proving the old assertion could never fail and the new one can.
    - Test 3 (WR-07): after Block A's tests have run, `process.platform` and the four environment variables are back to their captured originals within the same test body that changed them, not deferred to a hook.
    - Test 4: the three existing pattern-presence gates (pathShim mock, backend/logger/paths mock, `getLogFilePath({})` tripwire) still pass over all four in-scope suites, including `loggerFlows.test.ts` as extended by plan 34.2-20.
  </behavior>
  <action>
    **WR-01.** Change the "no longer claims NO FILESYSTEM WRITES" `it.each` block so it reads the
    file with `readFileSync` and matches the RAW source, not the `stripComments` output. Keep the
    three pattern-PRESENCE gates on stripped source — the stripper is correct for those, because
    each of those suites' own explanatory prose necessarily names the pattern under test and would
    self-satisfy the gate. The anti-claim gate is the opposite case: the whole point is to catch
    the claim wherever it appears, and this gate file is not itself in `IN_SCOPE_SUITES`, so there
    is no self-satisfaction risk. Record that asymmetry in a comment above the changed block so a
    future reader does not "restore consistency" by putting it back on stripped source.

    Add the WR-01 gate self-test immediately after it, following
    `gameDetailsImportGate.test.ts`'s convention: build a synthetic multi-line source string whose
    only occurrence of the claim sits on a ` * `-prefixed docblock line, then assert BOTH that the
    raw matcher finds it AND that `stripComments(synthetic)` does not — the second half is the
    executable record of why the old gate was vacuous, so nobody re-derives it from scratch.

    **WR-04.** Rewrite the comment above `jest.mock('backend/constants/environment', ...)`. It
    currently says the mock is "kept even though `pathShim.ts`/`logger/paths.ts` read
    `process.platform`/`process.env` directly rather than this module — included for parity."
    That is factually wrong for `logger/paths.ts`: `getBaseLogPath()` branches on `isWindows` /
    `isMac` imported from this exact module and never reads `process.platform`; only
    `pathShim.ts:31` reads `process.platform`. The replacement comment must state that the mock is
    LOAD-BEARING, name `getBaseLogPath()` as the consumer, explain that pinning `isMac:false`
    forces the `XDG_STATE_HOME` branch which is what makes RED-PROOF-2 non-vacuous on a darwin
    host, and say plainly that removing it silently turns that proof false. Do not delete the
    mock.

    **WR-07.** Move Block A's `process.platform` redefinition and the four environment-variable
    sentinel assignments out of `beforeAll`/`afterAll` and into the individual tests that need
    them, restoring in a `finally` inside each test body. Jest reuses a worker process across test
    files, so a force exit or `process.exit` in this suite currently leaks a fake `linux` platform
    and four bogus path roots into every subsequent file in that worker — the exact pattern this
    file's own docstrings, and the repo's `tests-clobbering-real-steam-store` lesson (commit
    92c29a5e), declare unsafe. If `jest.replaceProperty(process, 'platform', 'linux')` is
    available in the installed jest version, prefer it (it is unwound by jest's own teardown);
    otherwise use `Object.defineProperty` with a `finally`-block restore. Check the installed jest
    version before choosing, and record which was used and why in the SUMMARY.

    Update the module docstring to reflect all three changes. Do not weaken or delete the existing
    RED-PROOF notes at lines 256-268 and 392-397 — they are the executable record of the previous
    cycle's evidence; if Block A's restructuring changes what those proofs describe, amend them
    with the new description rather than removing them.
  </action>
  <acceptance_criteria>
    - The anti-claim gate reads raw source: the `it.each` block for `NO FILESYSTEM WRITES` does not
      call `stripComments`. Verify by reading; additionally
      `grep -c 'NO FILESYSTEM WRITES' src/backend/sidecar/__tests__/testContainment.test.ts`
      returns at least 2 (the gate plus its self-test).
    - GATE RED PROOF recorded verbatim in the SUMMARY: temporarily insert a
      `NO FILESYSTEM WRITES` claim into a `*`-prefixed docblock line of one in-scope suite
      (`enrichmentFlows.test.ts` is the least entangled choice), run this suite, confirm the
      anti-claim gate FAILS for that file, then remove the claim and confirm `git diff` is clean.
      This is the explicit "the gate can actually fail" requirement — the old gate could not.
    - The WR-01 self-test is present and asserts BOTH halves (raw matches, stripped does not).
    - The `backend/constants/environment` mock comment contains the words `LOAD-BEARING` and names
      `getBaseLogPath`:
      `grep -c 'LOAD-BEARING' src/backend/sidecar/__tests__/testContainment.test.ts` returns at
      least 1.
    - Block A no longer mutates `process.platform` in a `beforeAll`/`afterAll` pair — the
      restoration happens inside the test bodies. Verify by reading the restructured block; record
      the chosen mechanism (`jest.replaceProperty` vs `Object.defineProperty` + `finally`) and the
      installed jest version in the SUMMARY.
    - `npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment.test.ts"` passes.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment.test.ts"</automated>
  </verify>
  <done>The anti-claim gate is hand-proven to fail when the claim is reintroduced; the load-bearing mock is correctly labelled; the platform mutation no longer outlives its own tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the declared 11-suite hole with a derived tripwire over the whole directory plus a structural-containment gate</name>
  <files>src/backend/sidecar/__tests__/testContainment.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/testContainment.test.ts (as modified by Task 1 — the lists and anti-vacuity pins being replaced live at lines 279-324 pre-edit)
    - src/backend/jest.config.js (the setupFiles entry this task gates on — landed by plan 34.2-19)
    - src/backend/jest.setupContainment.ts (the module the gate must assert exists and still sets the eight environment variables — landed by plan 34.2-19)
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/deferred-items.md (the "From plan 34.2-18" entry declaring the 11-suite hole, which this task makes obsolete — plan 34.2-24 rewrites that entry; do not edit it here)
    - src/backend/sidecar/__tests__/gameDetailsImportGate.test.ts (the gate-self-test convention)
  </read_first>
  <behavior>
    - Test 1 (derived tripwire): the set of `*.test.ts` files found by `readdirSync` in this directory is EXACTLY equal to the union of the declared classification lists — failing both on an unclassified new suite and on a phantom entry naming a file that no longer exists.
    - Test 2 (tripwire self-test): running the same set-equality logic against a synthetic directory listing containing one extra unclassified name FAILS, proving the tripwire can detect growth.
    - Test 3 (structural gate): `src/backend/jest.config.js` contains a `setupFiles` entry naming `jest.setupContainment`, matched on comment-stripped source.
    - Test 4 (structural gate): `src/backend/jest.setupContainment.ts` exists and assigns all eight environment variables (`HOME`, `USERPROFILE`, `APPDATA`, `LOCALAPPDATA`, `XDG_CONFIG_HOME`, `XDG_STATE_HOME`, `XDG_DATA_HOME`, `XDG_CACHE_HOME`).
    - Test 5 (structural gate self-test): the same matching logic run against a synthetic config source lacking the `setupFiles` entry FAILS.
    - Test 6 (anti-vacuity, replacing the length pins): the derived directory listing contains more than 20 entries and every classified name exists on disk — so a mistake that empties the listing cannot make Test 1 pass trivially.
  </behavior>
  <action>
    Delete `KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES` and its `toHaveLength(11)` anti-vacuity pin.
    The list asserts a property that is no longer true: after plan 34.2-19 those suites are
    contained by the backend project's `setupFiles` entry, so describing them as an uncovered hole
    would now be actively misleading — the same over-claim-by-stale-declaration failure this phase
    has been cited for three rounds running.

    Replace it with a two-list classification that describes what is actually true:
    - Keep `IN_SCOPE_SUITES` (the four suites carrying the full per-suite kit) as the declared
      floor, but reframe its docstring: the per-suite kit is now DEFENCE IN DEPTH over structural
      containment, not the sole containment mechanism. Keep its `toHaveLength(4)` pin.
    - Add a second declared list naming every OTHER `*.test.ts` in the directory — the suites
      relying on structural containment alone. Its docstring must state that membership carries no
      obligation beyond being consciously classified, and that structural containment is what
      makes membership safe.

    Then add the derived tripwire: `readdirSync(__dirname)` filtered to `*.test.ts`, compared for
    SET EQUALITY against the union of the two lists (sort both sides and compare, so the failure
    message names the offending files). Set equality — not merely "no unclassified files" — so the
    gate also catches a phantom entry left behind when a suite is renamed or deleted. Note in a
    comment that the directory currently holds 25 `*.test.ts` files (24 before this gap cycle,
    plus `structuralContainment.test.ts` from plan 34.2-19), and that 9 of the original 24 were
    classified by NEITHER of the old lists — which is exactly why a length pin was never
    sufficient.

    Decide explicitly whether `testContainment.test.ts` classifies itself. The gap-cycle-2
    reviewer's suggested snippet excluded it; excluding it means the gate has a permanent blind
    spot of exactly one file. Prefer including it in the structurally-contained list, and if it is
    excluded instead, say why in a comment. Record the decision in the SUMMARY either way.

    Add the tripwire self-test: run the same set-equality helper against a synthetic file list
    containing one name not present in either declared list, and assert it reports that name.
    Factor the comparison into a small named helper so the gate and its self-test exercise the
    SAME code path — a self-test that reimplements the comparison proves nothing about the gate.

    Add the structural-containment gate: read `src/backend/jest.config.js` (resolve it relative to
    `__dirname`, mirroring `longRunningChannels.test.ts`'s `MAIN_RS_PATH` idiom rather than
    hardcoding an absolute path) and assert on comment-stripped source that it contains a
    `setupFiles` entry naming `jest.setupContainment`. Read `src/backend/jest.setupContainment.ts`
    and assert all eight environment-variable assignments are present. Add the accompanying
    self-test against a synthetic config source lacking the entry.

    Replace the length-only anti-vacuity checks with the stronger form: assert the derived listing
    has more than 20 entries (a floor, not an exact pin, so adding a suite does not require
    editing this assertion) and that every classified name exists on disk.

    Update the module docstring: remove the paragraph stating this gate "does NOT cover the whole
    `src/backend/sidecar/__tests__/` directory", since after this task it does; state that
    containment is now structural (naming `src/backend/jest.setupContainment.ts`) with the
    four-suite kit retained as defence in depth; and state that every gate in the file carries a
    self-test.
  </action>
  <acceptance_criteria>
    - `KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES` no longer appears in the file:
      `grep -c 'KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES' src/backend/sidecar/__tests__/testContainment.test.ts`
      returns 0.
    - The derived tripwire is present:
      `grep -c 'readdirSync' src/backend/sidecar/__tests__/testContainment.test.ts` returns at
      least 1.
    - The structural gate is present:
      `grep -c 'jest.setupContainment' src/backend/sidecar/__tests__/testContainment.test.ts`
      returns at least 1.
    - TRIPWIRE RED PROOF recorded verbatim in the SUMMARY: create an empty placeholder file
      `src/backend/sidecar/__tests__/zzTripwireProbe.test.ts` containing a single trivially-passing
      test, run this suite, confirm the derived tripwire FAILS naming `zzTripwireProbe.test.ts`,
      then delete the probe and confirm the suite is green and `git status` is clean. This is the
      explicit "fails against pre-fix code" requirement for the WR-08 finding.
    - STRUCTURAL GATE RED PROOF recorded verbatim in the SUMMARY: comment out the `setupFiles`
      entry in `src/backend/jest.config.js`, run this suite, confirm the structural gate FAILS,
      then restore and confirm `git diff --exit-code src/backend/jest.config.js` returns 0.
    - Every gate in the file has a self-test. Count them and list them in the SUMMARY: the
      `stripComments` self-test (pre-existing), the WR-01 anti-claim self-test (Task 1), the
      tripwire self-test, and the structural-gate self-test.
    - `npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment.test.ts"` passes.
    - `npx jest --testPathPattern=src/backend` reports the failing-suite set as exactly
      `{rustInvokeChannel.test.ts}` (the documented pre-existing 34.1-era baseline). Record the
      observed suite/test tallies in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment.test.ts"</automated>
  </verify>
  <done>Every `*.test.ts` in the directory is classified by a derived set-equality tripwire; the structural containment entry is gated; both new gates are hand-proven to fail; the stale 11-suite debt declaration is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate assertions → the property they claim to enforce | A gate is only a boundary if it can fail. A vacuous assertion is indistinguishable from no assertion, but reads to a future maintainer as proof. |
| declared list → actual directory contents | A hand-maintained list drifts silently from the tree it describes; the drift is invisible until something breaks. |
| this suite's `process`-level mutations → subsequent test files in the same jest worker | Jest reuses worker processes across files, so `process.platform` and `process.env` mutations are shared state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-81 | Repudiation (false assurance) | The WR-01 anti-claim gate matching comment-stripped source, where the claim can only ever live in a stripped-out docblock | mitigate | Move the anti-claim gate to RAW source and add a self-test asserting both that raw matches and that stripped does not. Hand-proven by reintroducing the claim into a real in-scope suite's docblock and observing the gate go red. |
| T-34.2-82 | Repudiation (stale declaration) | `KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES` asserting an 11-suite containment hole that plan 34.2-19 closed | mitigate | Delete the list. A declaration that no longer matches the tree is worse than none: three verification rounds have now cited this phase for exactly this class of over-claim. |
| T-34.2-83 | Tampering (undetected growth) | A 26th suite added with no containment and no classification | mitigate | `readdirSync`-derived SET EQUALITY against the union of the declared lists, with a self-test proving the comparison detects an extra name. Hand-proven with a throwaway probe suite. Set equality also catches phantom entries after a rename. |
| T-34.2-84 | Tampering (removal of the containment mechanism) | Deletion of the `setupFiles` entry from `src/backend/jest.config.js`, or gutting `jest.setupContainment.ts` | mitigate | A structural gate reads both files and asserts the entry and all eight environment assignments, with its own self-test. This is the third independent failure site for that removal (plan 34.2-19 added `structuralContainment.test.ts` and the `bootstrap.test.ts` tripwire). |
| T-34.2-85 | Denial of service (worker state leak) | Block A's `beforeAll`/`afterAll` mutation of `process.platform` and four environment variables leaking a fake `linux` platform into subsequent files in the same worker on force exit | mitigate | Move the mutation into the test bodies with `finally`-block restoration, or `jest.replaceProperty` if available. WR-07; the pattern this file's own docstrings declare unsafe. |
| T-34.2-86 | Repudiation | A load-bearing mock documented as decorative ("included for parity"), inviting a removal that silently makes RED-PROOF-2 vacuous | mitigate | Correct the comment to state the mock is LOAD-BEARING and name `getBaseLogPath()` as the consumer that branches on `isMac`. WR-04. |

No package-manager installs in this plan; the `T-{phase}-SC` supply-chain threat does not apply.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx jest --testPathPattern="src/backend/sidecar/__tests__/testContainment.test.ts"` green.
- `npx jest --testPathPattern=src/backend` — failing-suite set is exactly `{rustInvokeChannel.test.ts}`.
- Four hand-performed RED proofs recorded: anti-claim gate, derived tripwire, structural gate, and (from Task 1) the restructured Block A still passing.
</verification>

<success_criteria>
- No gate in `testContainment.test.ts` is vacuous; each carries a self-test and at least the two
  new ones are hand-proven to fail.
- Every `*.test.ts` in `src/backend/sidecar/__tests__/` is classified, enforced by set equality
  against a `readdirSync` listing.
- The file no longer declares an open data-loss defect as accepted debt.
- Removing the structural containment entry turns this suite red.
</success_criteria>

<output>
Create `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-23-SUMMARY.md` when done.
Record verbatim: all RED-proof outputs (anti-claim gate, derived tripwire, structural gate), the self-test inventory, the `testContainment.test.ts` self-classification decision, the jest-version/platform-mechanism choice, and the observed full-backend suite/test tallies.
</output>
