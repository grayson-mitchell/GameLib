---
status: partial
blocked_on: >-
  R-34.5-G1-PKG — the packaged Tauri asset root. HOMED TO PHASE 35 on 2026-08-23 (see
  that phase's ROADMAP block); previously parked to unnamed "packaging work" since
  2026-08-07. This is the ONLY reason status is not pass — UAT-34.2-01's dev half PASSED
  and UAT-34.2-02 PASSED outright, and the failing packaged half is not a Phase 34.2
  defect. Per D-11 neither item ever gated closure, so 34.2 itself is CLOSED; do not read
  this partial as unfinished 34.2 work.
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
source: [34.2-CONTEXT.md D-11, 34.2-VALIDATION.md, 34.2-PORTED-CHANNELS.md]
started: 2026-07-25
updated: 2026-08-23
---

## Current Test

[both items RUN 2026-08-22 on real hardware. UAT-34.2-02 PASSED outright.
UAT-34.2-01 PASSED its dev-build half; its packaged-build half (step 4) is the
only thing still outstanding — and it is the half that structurally matters most,
see Gaps.]

Both items below are **deferred** per this phase's own CONTEXT decision D-11. Exactly TWO items,
not ten: D-11's whole point is that this slice's unit claim is strong — 26 channels of
data-in/data-out with assertable return shapes over the real sidecar RPC loop — and its exceptions
are narrow. Padding this list would dilute the two that actually matter. Neither item gates phase
closure.

## Tests

1. **UAT-34.2-01 — a translated notification string actually renders (D-02 / REQ-34.2-02).**

   Why manual: jest can assert `i18next.t()` returns a real string after `init()`, and
   `gameDetailsFlows.test.ts` proves `repair`'s notify body reflects that real string over the real
   sidecar RPC loop. It cannot see the app actually show a notification, and it cannot exercise a
   **packaged** build's locale-path resolution — jest runs from the repo root, where `process.cwd()`
   happens to resolve `publicDir` correctly (the `publicdir-getapppath-chunking` gotcha family).

   Steps:
   1. `pnpm tauri:dev`.
   2. Trigger a repair that **SUCCEEDS**, on a **non-Steam** runner (§3 rider 1 of
      `34.2-PORTED-CHANNELS.md`: Steam's `repair` is itself an upstream no-op). A GOG title works
      — the 2026-08-22 run used Endless Sky (GOG `1829678475`).

      **AMENDED 2026-08-23 (G-34.2-UAT-01). Do NOT drive the error path.** This step used to
      say "trigger a repair whose repair path errors", which made it a test that CANNOT FAIL:
      the error path is `i18next.t('notify.error.reparing', 'Error Repairing')`
      (`dispatch.ts:208`), and `notify.error.reparing` in
      `public/locales/en/translation.json` is *also* `"Error Repairing"` — so the hardcoded
      default and the locale value are byte-identical and the toast reads perfectly even if
      D-02's entire bootstrap init were deleted. Zero discriminating power against the exact
      defect this item exists to catch. The SUCCESS path is the real gate:
      `i18next.t('notify.finished.reparing')` (`dispatch.ts:214`) carries **no default**, so a
      dead i18next has nothing to fall back to.
   3. Confirm the notification BODY is a real, translated sentence — **specifically, that it is
      NOT the raw key `notify.finished.reparing`** (the failure rendering when i18next is
      initialised but the locale never loaded), and not blank or literally `undefined`.

      Read the **toast**, not the terminal. `dispatch.ts:215` logs the UNTRANSLATED string
      `Finished repairing`, one capital letter away from the translated `Finished Repairing` —
      a reader checking the log instead of the toast can pass this step against a dead i18next.
   4. Repeat against a **packaged** build (`pnpm tauri build` or the project's packaging command),
      which is the half jest structurally cannot reach.

   Expected: the notification body renders a real sentence in both the dev and packaged builds.

   Note: sidecar logs go to `~/Library/Logs/GameLib/gamelib.log`, **NOT** the `tauri:dev` terminal —
   a reader looking in the terminal will wrongly conclude nothing happened.

   **Line numbers re-anchored 2026-08-23.** Every `dispatch.ts:NNN` citation in this document
   was written on 2026-08-22 against 183/187/188 and went stale hours later, when the round-1
   IN-02 fix (`6a2a6e2f0`, gating the success notification behind `repairFailed` so a rejecting
   `repair()` no longer reports success) inserted lines above all three landmarks. They now read
   208/214/215. The SYMBOLS are the durable anchor — `i18next.t('notify.error.reparing', ...)`,
   `i18next.t('notify.finished.reparing')`, and `logInfo('Finished repairing', ...)`. Re-grep
   those before trusting any number here.

   result: partial — dev build PASSED 2026-08-22; packaged build FAILED 2026-08-22
           (failure attributed to R-34.5-G1-PKG, a known Phase 34.5 residual, NOT a 34.2 defect)
   evidence: |
     Driven on real hardware against a live `pnpm tauri:dev` (binary
     `target/debug/gamelib-shell`, window titled "GameLib"). Target was **Endless Sky**
     (GOG `1829678475`, macOS native) — a non-Steam runner, per this item's own note that
     Steam's `repair` is an upstream no-op.

     **The prescribed step was CORRECTED before running, because as written it could not
     fail.** Step 2 says to trigger a repair that ERRORS. That path is
     `i18next.t('notify.error.reparing', 'Error Repairing')` (`dispatch.ts:208`) — the
     hardcoded second argument is byte-identical to the locale value, so it renders a
     perfect sentence even if D-02's entire bootstrap init were deleted. Zero
     discriminating power. The SUCCESS path is the real gate:
     `i18next.t('notify.finished.reparing')` (`dispatch.ts:214`) carries **no default**, so
     a dead i18next renders the raw key.

     Ran the success path instead. Operator observed a real macOS notification **toast**
     (explicitly confirmed as the toast, not the terminal — the two are one capital letter
     apart, since `dispatch.ts:215` logs the untranslated string `Finished repairing`)
     reading a real translated sentence, NOT `notify.finished.reparing`. Backend
     corroboration: `Finished repairing` at 20:01:00 and 20:02:02, each following a real
     `gogdl repair` invocation; `repair.log` ends `[V2] INFO: All files look good`
     (Deleted 0 / Changed 0), so the repair genuinely completed.

     Incidental, not a failure: macOS raised its App Management privacy prompt
     ("GOG Galaxy was prevented from modifying apps"). gogdl needed to change 0 files, so
     nothing was blocked — but a repair that DID need to rewrite `Endless Sky.app` could be
     blocked unless GameLib is allowed under Privacy & Security → App Management.
   packaged_build: |
     RUN 2026-08-22. `electron-vite build` -> `pnpm build:sidecar-sea` -> `tauri build`,
     all three stages exit 0. **macOS arm64 packaging itself WORKS** — it produced
     `GameLib.app` and `GameLib_0.7.0_aarch64.dmg` (514,459,661 bytes). That disposes of the
     competing hypothesis that packaging is simply broken on this platform.

     **The UAT step FAILS, and the cause was proven at the artifact level rather than
     inferred.** Mounted the DMG read-only and inspected the bundle:

     - `GameLib.app/Contents/Resources/` contains only `_up_` and `icon.icns`.
     - No `locales/` directory, no `translation.json`, no `public/` directory anywhere in
       the bundle.
     - The only staged resources are `_up_/build/bin/` — exactly what
       `tauri.conf.json`'s `bundle.resources: ["../build/bin/"]` declares. `build/locales/`
       is not in that list.
     - The locale data is not compiled into the SEA sidecar either. Probed
       `Contents/MacOS/gamelib-sidecar` with `strings`: the KEY
       `notify.finished.reparing` IS present (so the `dispatch.ts:214` code path shipped,
       and the probe demonstrably works on this binary), while the locale VALUE
       `Finished Repairing` is ABSENT. Both directions checked, so the absence is real and
       not a broken grep.

     Therefore `i18next-fs-backend` has nothing to load and `t('notify.finished.reparing')`
     must render the raw key in a packaged build.

     **Honesty limit, stated rather than buried:** this proves the CAUSE conclusively — the
     locale files are physically absent from the artifact. It does NOT record an observation
     of the SYMPTOM; nobody launched the packaged app and watched a toast render
     `notify.finished.reparing`. The artifact evidence is arguably the stronger of the two
     (it shows the mechanism, not just the outcome), but the two are not the same claim.

     **Attribution: NOT a Phase 34.2 defect.** This is `R-34.5-G1-PKG`, named and
     deliberately left open by Phase 34.5 (`34.5-APP-ROOT-SWEEP.md` §3). `paths.ts:73-76` defines `publicDir` as
     `resolve(app.getAppPath(), app.isPackaged || process.env.CI === 'e2e' ? 'build' :
     'public')`, and `electronStub.ts:207` hardcodes `isPackaged: false`, so packaged
     resolution appends `'public'` and never `'build'` — as `main.rs:5281-5285` states in its own words. Phase
     34.5 fixed the adjacent half (the shell now hands down `GAMELIB_APP_ROOT` from
     `resource_dir()` on both spawn paths, so `getAppPath()` no longer returns `src-tauri/`),
     but explicitly did not close the asset-root half.

     Fixing it needs BOTH: add `../build/locales/` to `bundle.resources`, AND make the
     packaged branch of `publicDir` reachable (the `isPackaged: false` constant defeats it
     even once the files ship). One without the other is a no-op.

2. **UAT-34.2-02 — the anticheat data file actually appears on disk (D-07 / REQ-34.2-07).**

   Why manual: jest asserts the event fires and the file is written, under a tmp home with a
   **mocked** axios (`bootstrapWirings.test.ts`). It cannot prove a real GitHub fetch completes in a
   live process across a real network after a real connectivity transition.

   Steps:
   1. Delete `<appFolder>/areweanticheatyet.json`.
   2. `pnpm tauri:dev`.
   3. Wait for `initOnlineMonitor()`'s online transition.
   4. Confirm the file reappears at `<appFolder>/areweanticheatyet.json`.
   5. Check `~/Library/Logs/GameLib/gamelib.log` for the `AreWeAntiCheatYet data downloaded` line.

   Expected: the file reappears and the log line is present. **The file appearing IS the pass
   condition** — it is not a visible badge.

   **Rider (recorded inline so a correct result is not misread as a failure):** even with the file
   present, `getAnticheatInfo` returns `null` for Steam titles on every platform and `null` for
   everything on Windows (§3 rider 2 of `34.2-PORTED-CHANNELS.md`). Do not treat a `null`
   `getAnticheatInfo` result, or the absence of a visible anticheat badge on a Steam game, as a
   failure of this test — that is the FOUR-of-five expected outcome even after this fix.

   result: pass — 2026-08-22, real hardware, real network
   evidence: |
     `areweanticheatyet.json` was moved aside (not deleted) and the app cold-restarted.
     All three pass conditions met:

     1. File reappeared at `<appFolder>/areweanticheatyet.json`, mtime 20:08:00.
     2. **310412 bytes, md5 `9848ab1e845afdbccb6eec3846e2ba10`** — byte-identical to a
        direct `curl` of the upstream Mac URL taken during the test (HTTP 200, same size,
        same md5). This is what proves a REAL completed fetch rather than an empty or
        truncated write; "the file exists" alone would not.
     3. `gamelib.log` 20:08:00: `[INFO]: [Backend]: AreWeAntiCheatYet data downloaded` —
        the DOWNLOAD branch, not `AreWeAnticheatYet data did not change. Skipping.`

     Two independent corroborations that this was a genuine cold boot and not a reused
     artifact: the log ROTATED (`gamelib.log.old` retains the prior session's 11803 bytes),
     and the preceding session's log shows the skip branch taken at 19:28:04 while the file
     was present — so both branches of `downloadAntiCheatData` are now observed live, in
     the right order, on the right inputs.

     Chain proven end to end: `fetchLastestReleases()` (real GitHub) -> `releasesInfoReady`
     -> the re-homed anticheat listener -> `downloadAntiCheatData` -> real GitHub fetch ->
     `writeFile`. D-07's wiring is confirmed on a live Tauri build.

     Rider held as documented: no visible badge appeared, and none was expected.

## Manual-Only Verifications

None beyond the two items above — this document's whole purpose is the deferred-live-UAT list; per
D-11 there is no separate "manual, not deferred" category for this slice.

## Summary

total: 2
passed: 1
partial: 1
issues: 1
pending: 0
skipped: 0
blocked: 0

**Both items have now been RUN (2026-08-22, real hardware).** This paragraph previously read
"Neither item has been run yet" — true when written, stale from the moment the run landed, and
corrected 2026-08-23. UAT-34.2-02 (D-07) PASSED outright. UAT-34.2-01 (D-02) is `partial`: dev
PASSED, packaged FAILED, with the packaged failure attributed to `R-34.5-G1-PKG` (a Phase 34.5
residual) rather than to a 34.2 defect. Full evidence, including the both-directions packaged
probe, is in each item's own `evidence:`/`packaged_build:` block above. Per D-11 neither item
ever gated phase closure.

## Gaps

**G-34.2-UAT-01 — UAT-34.2-01 step 2 was a test that could not fail (test-design defect,
found 2026-08-22, corrected before running).** The step directs the operator to trigger a
repair that ERRORS. `dispatch.ts:208` is
`i18next.t('notify.error.reparing', 'Error Repairing')`, and `notify.error.reparing` in
`public/locales/en/translation.json` is *also* `"Error Repairing"` — so the rendered text is
identical whether i18next is fully initialised or completely dead. The step would have
passed against the exact defect D-02 exists to prevent. Corrected by driving the SUCCESS
path (`t('notify.finished.reparing')`, no default) instead. Same class as this phase's
recurring "gate measures the wrong property" finding.

**CLOSED 2026-08-23 — the written step is now amended in place**, so a re-run cannot repeat
the defect: step 2 above directs the success path and states why the error path is
disqualified, and step 3 now names the property to check (the raw key
`notify.finished.reparing` must NOT appear) plus the toast-vs-log trap. The correction was
previously applied only to the 2026-08-22 RUN, leaving the instruction itself defective.

**G-34.2-UAT-02 — the packaged-build half of UAT-34.2-01 FAILS (run 2026-08-22).** The
suspicion that motivated this step was correct: a dev pass carried no information about
packaging. Proven at the artifact level — the packaged bundle ships no locale files on disk
and none compiled into the SEA sidecar (see the item's `packaged_build:` block for the
both-directions probe). **REQ-34.2-02 is therefore proven for dev only, and is FALSE for a
packaged build.**

Attribution matters here: this is `R-34.5-G1-PKG`, a residual that Phase 34.5 named and
deliberately routed OUT of itself, not a defect introduced by 34.2. 34.2's own deliverable — the
sidecar i18next bootstrap — works, as the dev half proves. The fix needs `../build/locales/` added
to `bundle.resources` AND the `isPackaged: false` constant in `electronStub.ts:207` addressed,
since either alone is inert.

**ROUTED 2026-08-23 — the fix now has a named owner: Phase 35 (Electron cutover).** This item had
been parked to unnamed "packaging work" for 16 days across three phases that each declined it
(34.6: not an IPC channel port; 34.9: **DESCOPED** 2026-08-07; 34.5: closed 2026-08-20). Phase 35
satisfies the residual's own routing criterion — *"whichever plan first exercises a packaged
(non-dev) build"* (`34.5-deferred-items.md` item 12) — and is where `electronStub.ts` is deleted,
taking the `isPackaged: false` constant with it. Recorded as an inherited scope item in Phase 35's
ROADMAP block, to be minted as a requirement at `/gsd-plan-phase 35`. **This document's
`status: partial` and its `blocked_on:` frontmatter field both point there**, so the `partial`
reads as owned-elsewhere rather than as unfinished 34.2 work.

**Also corrected 2026-08-23: this document quoted `paths.ts` inaccurately.** The evidence block
above rendered the ternary as a bare `app.isPackaged ? 'build' : 'public'`. The live line has read
`app.isPackaged || process.env.CI === 'e2e' ? 'build' : 'public'` since `87c0ef823` (**2026-07-21**)
— a month before this evidence was written, so the omission was a transcription error, not drift.
Corrected in place. **The verdict is unaffected** (a shipped `.app` sets no `CI=e2e`, so the
packaged branch stays unreachable), but the dropped clause is useful to whoever fixes this: the
`'build'` branch IS reachable under one condition, which may supply a cheap test harness instead of
a full packaging run per iteration.

Also settled by this run, worth recording because it was an open question: **macOS arm64
Tauri packaging works.** The build produced a valid `.app` and a 514MB signed-less DMG with
all three stages exiting 0.

**Nothing here blocks phase closure** — per D-11 neither item ever gated it.

## The unavoidable gap, restated

For the 26 channels declared in `34.2-PORTED-CHANNELS.md`, the automated tests exercise the real
extracted bodies over the real transport and assert real return values — so "wired and
unit-proven" is genuinely close to "works" for this slice, a stronger claim than Phase 34.1 could
make. For the two bootstrap wirings above, it is not: those tests prove the wiring under mocked
network and a tmp home, and nobody has booted the Tauri build and watched either effect.

**Do not read "wired and unit-proven" as "seen working" — that is exactly how G-30-02 was declared fixed twice while the live build hung.**

Read this document together with `34.2-PORTED-CHANNELS.md` §6 — neither artifact should be read in
isolation, because either one alone could leave a later reader with the wrong impression.
