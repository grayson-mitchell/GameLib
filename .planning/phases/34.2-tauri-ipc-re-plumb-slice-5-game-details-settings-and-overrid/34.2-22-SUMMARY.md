---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 22
subsystem: testing
tags: [rust, cargo-test, tauri-shell, jest, source-gate, timeout, invoke-guardrail]

# Dependency graph
requires:
  - phase: 34.2 (gap cycle 3, plans 19-21)
    provides: structural jest containment (setupFiles + jest.mock('os')) that all backend suites now inherit, including longRunningChannels.test.ts
provides:
  - A #[cfg(test)] behavioral test module in src-tauri/src/main.rs proving timeout_for() actually consults LONG_RUNNING_CHANNELS, falsifiable in both directions
  - A jest gate (src/backend/__tests__/longRunningChannels.test.ts) pinning the Rust test module's existence, with a self-test proving the gate itself can fail
affects: [phase-34.2-verification, phase-34.3-planning]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Rust #[cfg(test)] behavioral module pinned from a JS-side jest gate because this project's CI runs no cargo step at all"
    - "Gate self-test convention (mirrors gameDetailsImportGate.test.ts's Gate-2): construct a synthetic source lacking the pattern, assert the matcher returns false, to prove the gate can fail"

key-files:
  created: []
  modified:
    - src-tauri/src/main.rs (appended #[cfg(test)] mod tests, 6 tests over timeout_for())
    - src/backend/__tests__/longRunningChannels.test.ts (new describe block, 8 -> 14 tests)

key-decisions:
  - "Test 5's negative half (non-vacuity check) uses getGameSettings, a real non-exempt channel this phase ported (src/backend/gamedetails/dispatch.ts), per the plan's explicit instruction to avoid a made-up string"
  - "The jest gate matches RAW source (not the file's own comment-stripped loadMainRsCode() helper) because #[cfg(test)] is an attribute adjacent to doc comments in the appended module, and the plan explicitly called out this ordering to avoid stripper interaction"
  - "Added a second self-test beyond the plan's minimum: a #[cfg(test)] module present but only weakly referencing timeout_for/not iterating LONG_RUNNING_CHANNELS also fails the gate — proves the gate is not satisfied by the attribute's mere presence"

patterns-established: []

requirements-completed: [REQ-34.2-12, REQ-34.2-14]

# Metrics
duration: 10min
completed: 2026-07-26
---

# Phase 34.2 Plan 22: Rust `timeout_for()` behavioral test + jest existence gate Summary

**A `#[cfg(test)]` module in `src-tauri/src/main.rs` proves `timeout_for()` genuinely consults `LONG_RUNNING_CHANNELS` (falsifiable both directions), pinned against silent deletion by a jest gate carrying its own self-test — closing a warning carried forward across all three of this phase's verification rounds.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-07-26T00:24:00Z (approx, following 34.2-21 completion)
- **Completed:** 2026-07-26T00:33:58Z
- **Tasks:** 2 (both `type="auto" tdd="true"` / `type="auto"`)
- **Files modified:** 2

## Accomplishments

- `src-tauri/src/main.rs` now has 6 behavioral tests over `timeout_for()`, replacing the previous source-shaped-only coverage. `cargo test` runs 6 tests in 0.00s (release-build unaffected, `cargo check --quiet` stays green).
- `src/backend/__tests__/longRunningChannels.test.ts` grew from 8 to 14 tests: 6 new tests (4 real assertions + 2 self-tests) pin the Rust test module's existence and genuine behavioral coverage, since this project's CI runs no `cargo test`/`cargo check` step at all.
- Both the Rust-side mutation RED proof (Task 1) and the JS-side deletion RED proof (Task 2) were hand-verified and are recorded verbatim below.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add a #[cfg(test)] module in main.rs with behavioral tests over timeout_for()** - `c7c292e4` (test)
2. **Task 2: Pin the Rust test module's existence from the jest side, with a gate self-test** - `5598093e` (test)

**Plan metadata:** (this commit, docs: complete plan)

_Note: This plan's tasks are both TDD-flavored proof work over an existing, already-correct `timeout_for()` implementation — no `feat`/GREEN commit was needed because the function under test was not changed, only tested. Both commits are typed `test` per the actual diff content._

## Files Created/Modified

- `src-tauri/src/main.rs` - Appended `#[cfg(test)] mod tests` (6 tests: exempt-channel waits indefinitely, non-exempt channel bounded, repair/readConfig exempt, getCrossoverIndex exempt, full-array iteration + non-vacuity, INVOKE_TIMEOUT pinned at 60s)
- `src/backend/__tests__/longRunningChannels.test.ts` - New describe block (`REQ-34.2-14 main.rs #[cfg(test)] behavioral test module is present...`) with `loadMainRsRaw()`, `hasBehavioralRustTestModule()`, 4 real assertions, 2 self-tests

## Decisions Made

- Chose `getGameSettings` (a real, currently non-exempt game-details channel this slice ported) for Test 5's negative-half assertion, per the plan's explicit instruction to use a real channel rather than a made-up string.
- Matched RAW `main.rs` source (not the file's existing comment-stripped `loadMainRsCode()` helper) for the new jest gate, since `#[cfg(test)]` is an attribute sitting adjacent to doc comments in the appended module — matching raw avoids any stripper interaction, as the plan's `<action>` explicitly called out.
- Added a second self-test beyond the plan's stated minimum (a `#[cfg(test)]` module present but weakly-referencing `timeout_for` and not iterating `LONG_RUNNING_CHANNELS`) to prove the gate requires genuine behavioral coverage, not merely the attribute's presence.

## RED Proofs (recorded verbatim)

### Task 1, mutation 1 — `timeout_for` stubbed to unconditional `Some(INVOKE_TIMEOUT)`

```
running 6 tests
test tests::invoke_timeout_is_60_seconds ... ok
test tests::non_exempt_channel_is_bounded_at_invoke_timeout ... ok

failures:

---- tests::get_crossover_index_is_exempt stdout ----
thread 'tests::get_crossover_index_is_exempt' panicked at src/main.rs:1083:9:
assertion `left == right` failed
  left: Some(60s)
 right: None

---- tests::exempt_channel_waits_indefinitely stdout ----
thread 'tests::exempt_channel_waits_indefinitely' panicked at src/main.rs:1064:9:
assertion `left == right` failed
  left: Some(60s)
 right: None

---- tests::repair_and_read_config_are_exempt stdout ----
thread 'tests::repair_and_read_config_are_exempt' panicked at src/main.rs:1076:9:
assertion `left == right` failed
  left: Some(60s)
 right: None

---- tests::every_long_running_channel_is_exempt_and_a_non_member_is_bounded stdout ----
thread 'tests::every_long_running_channel_is_exempt_and_a_non_member_is_bounded' panicked at src/main.rs:1093:13:
assertion `left == right` failed: expected install (a LONG_RUNNING_CHANNELS member) to wait indefinitely
  left: Some(60s)
 right: None

test result: FAILED. 2 passed; 4 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.00s
```

### Task 1, mutation 2 — `timeout_for` stubbed to unconditional `None`

```
running 6 tests
test tests::exempt_channel_waits_indefinitely ... ok
test tests::repair_and_read_config_are_exempt ... ok
test tests::invoke_timeout_is_60_seconds ... ok
test tests::get_crossover_index_is_exempt ... ok
test tests::every_long_running_channel_is_exempt_and_a_non_member_is_bounded ... FAILED
test tests::non_exempt_channel_is_bounded_at_invoke_timeout ... FAILED

failures:

---- tests::every_long_running_channel_is_exempt_and_a_non_member_is_bounded stdout ----
thread 'tests::every_long_running_channel_is_exempt_and_a_non_member_is_bounded' panicked at src/main.rs:1103:9:
assertion `left == right` failed
  left: None
 right: Some(60s)

---- tests::non_exempt_channel_is_bounded_at_invoke_timeout stdout ----
thread 'tests::non_exempt_channel_is_bounded_at_invoke_timeout' panicked at src/main.rs:1070:9:
assertion `left == right` failed
  left: None
 right: Some(60s)

test result: FAILED. 4 passed; 2 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.00s
```

Confirms the two mutations fail DIFFERENT test sets (4 vs. 2, with only partial overlap), proving the test module is falsifiable in both directions. After restoring the original body, `git diff --stat src-tauri/src/main.rs` reported 0 changes relative to the committed Task 1 state (`69 insertions(+), 0 deletions(-)` when diffed against the pre-plan baseline — purely additive).

### Task 2 — jest gate RED proof (Rust test module deleted)

Reverted `src-tauri/src/main.rs` to its pre-Task-1 content (`git show HEAD~1:src-tauri/src/main.rs`) and re-ran the jest suite:

```
FAIL Backend src/backend/__tests__/longRunningChannels.test.ts
  ● ... the #[cfg(test)] region iterates LONG_RUNNING_CHANNELS rather than hardcoding a second duplicate list
    expect(received).toBeGreaterThan(expected)
    Expected: > -1
    Received:   -1
      143 |     expect(cfgTestIndex).toBeGreaterThan(-1)

  ● ... the full gate (all three conditions together) matches the real main.rs
    expect(received).toBe(expected) // Object.is equality
    Expected: true
    Received: false

Test Suites: 1 failed, 1 total
Tests:       4 failed, 10 passed, 14 total
```

(2 additional failures not shown above in full: "main.rs contains a #[cfg(test)] attribute" and "the #[cfg(test)] region references timeout_for at least twice" — both fail with the same `-1`/"not found" pattern.) Restored `src-tauri/src/main.rs` from the pre-deletion copy; `git diff --stat src-tauri/src/main.rs` reported no output (byte-identical to Task 1's committed state). Re-ran jest: 14/14 passing again.

## Test Counts

| Suite | Before | After | Delta |
|---|---|---|---|
| `cargo test` (src-tauri) | 0 tests (no `cfg(test)` anywhere in `src-tauri/src`) | 6 tests, 0.00s | +6 |
| `longRunningChannels.test.ts` (jest) | 8 tests | 14 tests | +6 |

## Verification

- `cd src-tauri && cargo test` — exit 0, 6 passed, 0 failed, 0.00s.
- `cd src-tauri && cargo check --quiet` — exit 0.
- `npx jest --testPathPattern="src/backend/__tests__/longRunningChannels.test.ts"` — exit 0, 14/14 passing.
- `npx tsc --noEmit` — exit 0.
- `git diff --exit-code src-tauri/Cargo.toml` — exit 0 (no new dependency).
- `git diff src-tauri/src/main.rs | grep -c 'dispatch_rust_channel'` — 0 (zero new Rust dispatch arms, unchanged throughout this phase).
- `git diff src/backend/__tests__/longRunningChannels.test.ts` — purely additive (0 removed lines); pre-existing set-equality/`INVOKE_TIMEOUT` assertions untouched.

## Deviations from Plan

None - plan executed exactly as written. Both tasks' RED proofs matched the plan's expectations (bidirectional falsifiability for Task 1, deletion-detectability for Task 2), and no scope beyond the two named files was touched.

## Issues Encountered

None. `cargo test` ran against the already-warm `src-tauri/target` build cache (no cold rebuild needed, consistent with the prior-wave note that the crate is ~28GB and warm).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `timeout_for()` now has genuine behavioral coverage on the Rust side, and that coverage cannot be silently deleted without a jest failure — the carried-forward verification warning this plan targeted is closed.
- Two gap-cycle-3 plans remain: 34.2-23 (wave 2, WR-01 raw-source anti-claim gate + `readdirSync` set-equality tripwire) and 34.2-24 (wave 3, REQ-34.2-13 PORTED-CHANNELS.md currency).
- No blockers.

---
*Phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid*
*Completed: 2026-07-26*

## Self-Check: PASSED

- FOUND: src-tauri/src/main.rs
- FOUND: src/backend/__tests__/longRunningChannels.test.ts
- FOUND: .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-22-SUMMARY.md
- FOUND commit c7c292e4 (Task 1: behavioral Rust tests)
- FOUND commit 5598093e (Task 2: jest existence gate)
