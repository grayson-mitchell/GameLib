---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 27
subsystem: renderer-error-reporting
tags: [react, error-handling, jest, tdd, repair-failure, wr-06, wr-03]

requires:
  - phase: 34.2 (gap cycle 3, plan 34.2-21)
    provides: repairFailure.ts's WR-03 defensive stringification of the `unknown` error binding
provides:
  - Every one of repairFailure.ts's three signals (console.error, window.api.logError, showDialogModal/t) is now individually diagnosed on failure and none can suppress a later one
  - A throwing or absent window.api.logError leaves a named `repair-failure log signal unavailable` console diagnostic instead of vanishing
  - A throwing t()/showDialogModal no longer escapes reportRepairFailure into the un-awaited dialog-button handler at index.tsx:158 -- the dialog still renders (English fallback title/message) or a `repair-failure dialog signal unavailable` diagnostic is emitted
  - repairFailure.ts and its test file are prettier-clean (closes WR-03's CI regression)
affects: [34.2-VALIDATION.md, 34.2-VERIFICATION (fourth round), gap-cycle-4-closure]

tech-stack:
  added: []
  patterns:
    - "Precompute-then-guard: hardcode a safe fallback into a `let` binding before a try that may reassign it, so a throwing dependency degrades to the fallback instead of aborting the function (mirrors src/backend/sidecar/processGuards.ts:61-69, applied here to both errorText and the dialog title/message)"
    - "Named diagnostic over empty catch: every catch that discards a caller-supplied failure calls console.error with a literal, grep-able tag string (`repair-failure log signal unavailable`, `repair-failure dialog signal unavailable`) and passes the failure object as a second argument, never interpolated"

key-files:
  created: []
  modified:
    - src/frontend/screens/Game/GameSubMenu/repairFailure.ts
    - src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure.test.ts

key-decisions:
  - "showDialogModal is wrapped in its own try (not left as the unwrapped 'payoff' statement) per the plan's explicit action -- a throwing dialog renderer now diagnoses to console.error rather than escaping into index.tsx:158's un-awaited handler"
  - "title/message are precomputed into `let` bindings pre-initialised to the hardcoded English literals inside their own try, separate from the showDialogModal try -- a throwing t() degrades to the English fallback and the dialog still renders"

requirements-completed: [REQ-34.2-12, REQ-34.2-14]

duration: 15min
completed: 2026-07-26
---

# Phase 34.2 Plan 27: WR-06 signal-2/3 diagnosis + WR-03 prettier regression Summary

**Signals 2 and 3 of `reportRepairFailure` now each leave a named, grep-able `console.error` diagnostic on failure instead of vanishing or escaping the function — closing the same silent-void class this phase was convened to fix, reproduced inside its own remediation.**

## Performance

- **Duration:** ~15 min (commit-to-commit)
- **Started:** 2026-07-26T15:40:51+12:00 (Task 1 commit)
- **Completed:** 2026-07-26T15:42:53+12:00 (Task 2 commit)
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments

- Closed WR-06: `window.api.logError` throwing or entirely absent (the Tauri preload-factory-did-not-attach failure mode) now emits `console.error('repair-failure log signal unavailable:', logErr)` instead of an empty catch.
- Closed the T-34.2-53 unenforced claim: `showDialogModal`/`t` are no longer unguarded. A throwing `t` degrades to hardcoded English fallback strings and the dialog still renders; a throwing `showDialogModal` is caught and diagnosed via `console.error('repair-failure dialog signal unavailable:', dlgErr)` rather than propagating out of `reportRepairFailure`.
- Closed WR-03's prettier regression on `repairFailure.ts` (`npx prettier --write` applied to both files in scope).
- T-34.2-52 (information disclosure) re-verified under hostility: the dialog message never carries the raw error text, even when `window.api.logError` simultaneously throws (Test 5).

## Task Commits

Each task was committed atomically:

1. **Task 1: Add hostile-dependency tests for signals 2 and 3 and observe them RED against HEAD** - `2f6c2dcb` (test)
2. **Task 2: Make all three signals individually diagnosed and keep the dialog renderable when translation throws** - `78f9ad13` (fix)

_Note: this plan follows the RED-then-fix TDD shape at the plan level (not the `tdd="true"` frontmatter gate), matching Task 1's explicit "writes the failing tests first" objective._

## Files Created/Modified

- `src/frontend/screens/Game/GameSubMenu/repairFailure.ts` - signal 2's catch now diagnoses via `console.error`; title/message are precomputed with hardcoded English fallbacks and reassigned inside their own try; `showDialogModal` is wrapped in its own try/catch that diagnoses failures; docstring corrected (signal independence now genuinely covers all three signals, T-34.2-53 wording updated).
- `src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure.test.ts` - added a `WR-06` describe block with 5 new tests (hostile `window.api.logError`, absent `window.api`, throwing `t`, throwing `showDialogModal`, and a T-34.2-52-under-hostility regression guard).

## Verbatim Task 1 RED Output

Confirmed against unmodified `repairFailure.ts` (production file untouched, `git diff --stat` empty before Task 2):

```
FAIL Frontend src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure.test.ts
  reportRepairFailure (gap cycle 2, CR-01 renderer half)
    ✓ calls console.error exactly once, with a message containing the appName (4 ms)
    ✓ calls window.api.logError exactly once with a string containing the appName and the error text
    ✓ calls showDialogModal exactly once with showDialog true, type ERROR, a non-empty title/message, and never includes the raw error text (T-34.2-52) (1 ms)
    ✓ still reports console.error and window.api.logError for a plain-string thrown value (non-hostile baseline)
    ✓ does not throw and still calls showDialogModal with type ERROR for a null-prototype object
    ✓ does not throw and still calls showDialogModal with type ERROR for an object whose toString throws
    ✓ does not throw and still calls showDialogModal with type ERROR for an object whose Symbol.toPrimitive throws
    ✓ still calls window.api.logError exactly once for a null-prototype object (signal 2 degrades to a fallback string, never disappears)
    ✓ still calls window.api.logError exactly once for an object whose toString throws (signal 2 degrades to a fallback string, never disappears) (1 ms)
    ✓ still calls window.api.logError exactly once for an object whose Symbol.toPrimitive throws (signal 2 degrades to a fallback string, never disappears)
    ✓ still calls console.error exactly once for a null-prototype object (signal 1 is unaffected)
    ✓ still calls console.error exactly once for an object whose toString throws (signal 1 is unaffected)
    ✓ still calls console.error exactly once for an object whose Symbol.toPrimitive throws (signal 1 is unaffected)
    ✓ showDialogModal message is still the fixed translated string only, for a hostile reason (T-34.2-52 survives the hardening)
    WR-06: individually-diagnosed signals, dialog survives a throwing t/showDialogModal
      ✕ Test 1: still calls showDialogModal once and diagnoses the log-signal failure when window.api.logError throws (1 ms)
      ✕ Test 2: still calls showDialogModal once and diagnoses the log-signal failure when window.api is entirely absent
      ✕ Test 3: still renders a non-empty dialog title/message when t throws on the title key (5 ms)
      ✕ Test 4: does not throw and diagnoses the dialog-signal failure when showDialogModal throws
      ✓ Test 5: T-34.2-52 survives even when window.api.logError throws (dialog message never carries the raw error text)

  ● Test 1: Expected number of calls: 2, Received number of calls: 1
    (assertion diff — the empty catch means console.error is called once, not twice)

  ● Test 2: expect(received).toBeDefined() -- Received: undefined
    (assertion diff — no diagnostic is ever emitted for an absent window.api)

  ● Test 3: expect(received).not.toThrow()
    Error name: "Error"
    Error message: "translation exploded"
    at throwingT (repairFailure.test.ts:307:17)
    at reportRepairFailure (repairFailure.ts:101:12)
    (the throw ESCAPES reportRepairFailure -- visible as the thrown message, not an assertion diff)

  ● Test 4: expect(received).not.toThrow()
    Error name: "Error"
    Error message: "dialog exploded"
    at repairFailure.ts:98:3
    (the throw ESCAPES reportRepairFailure -- visible as the thrown message, not an assertion diff)

Test Suites: 1 failed, 1 total
Tests:       4 failed, 15 passed, 19 total
```

This matches the plan's acceptance criteria exactly: exactly four of five new tests failed, Test 5 passed, all 14 pre-existing tests passed, and Tests 3/4 failed via the throw escaping the function (not an assertion diff) — reproducing the `index.tsx:158` escape route WR-06 names.

## Verbatim Task 2 GREEN Output

```
PASS Frontend src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure.test.ts
  reportRepairFailure (gap cycle 2, CR-01 renderer half)
    [... all 14 pre-existing tests ...] ✓
    WR-06: individually-diagnosed signals, dialog survives a throwing t/showDialogModal
      ✓ Test 1: still calls showDialogModal once and diagnoses the log-signal failure when window.api.logError throws
      ✓ Test 2: still calls showDialogModal once and diagnoses the log-signal failure when window.api is entirely absent
      ✓ Test 3: still renders a non-empty dialog title/message when t throws on the title key
      ✓ Test 4: does not throw and diagnoses the dialog-signal failure when showDialogModal throws
      ✓ Test 5: T-34.2-52 survives even when window.api.logError throws (dialog message never carries the raw error text)

Test Suites: 1 passed, 1 total
Tests:       19 passed, 19 total
```

## Deliberate-Break Check (Task 2, by hand)

Reverted signal 2's catch body from the diagnostic back to empty (`// DELIBERATE BREAK FOR VERIFICATION - DO NOT COMMIT`) and re-ran:

```
Test Suites: 1 failed, 1 total
Tests:       2 failed, 17 passed, 19 total

● Test 1: Expected number of calls: 2, Received number of calls: 1
● Test 2: expect(received).toBeDefined() -- Received: undefined
```

Exactly Tests 1 and 2 failed (the two tests exercising signal 2's diagnostic) — all other 17 tests, including Tests 3/4/5, remained green, proving the break's blast radius was correctly scoped to signal 2 only. Restored from a pre-Task-2 backup copy; `npx jest --testPathPattern=repairFailure` returned to 19/19 green.

## Line-by-Line Justification of Every Remaining Empty/Bare Catch

`grep -nE "catch \{$" src/frontend/screens/Game/GameSubMenu/repairFailure.ts` returns 5 lines. None of them discards a caller-supplied failure without a diagnostic having already fired at an outer layer:

| Line | Guards | Justification |
|------|--------|----------------|
| 86 | `errorText` stringification (`error instanceof Error ? ... : String(error)`) | Pre-existing WR-03 hardening (plan 34.2-21), explicitly marked "DO NOT change it" by this plan's own interface contract. Keeps the hardcoded `'<unstringifiable error>'` fallback; `errorText` is only ever used for `window.api.logError`'s message, never the dialog. |
| 96 | `console.error(...)` (signal 1) | Signal 1 must never suppress signals 2/3. Signal 1 is itself the outermost diagnostic layer (a raw devtools console line) -- there is nothing further to diagnose it with, and adding one would just create a second identical console statement. |
| 109 | inner `console.error('repair-failure log signal unavailable:', logErr)` (signal 2's own diagnostic) | This diagnostic must never itself become a new throw path. `logErr` (the caller-supplied failure) is already diagnosed one level up at line 107; this innermost catch only guards the diagnostic call itself against a hostile `console.error` override. |
| 120 | `t('box.error.title', ...)` / `t('box.repair.error', ...)` reassignment | Keeps the hardcoded English fallback (`title`/`message` are pre-initialised before the try) -- a throwing `t` degrades to the fallback and the dialog still renders one statement later. Not a caller-supplied failure being silently discarded: the dialog itself is the visible signal that follows. |
| 136 | inner `console.error('repair-failure dialog signal unavailable:', dlgErr)` (signal 3's own diagnostic) | Same class as line 109: `dlgErr` is already diagnosed one level up at line 132; this innermost catch only guards the diagnostic call itself. |

No empty catch in the file discards a caller-supplied failure object without a diagnostic having fired for it somewhere in the call chain.

## Full Frontend Sweep

```
Test Suites: 26 passed, 26 total
Tests:       200 passed, 200 total
```

Against the 26/26 · 195/195 baseline recorded in plan 34.2-21's SUMMARY: +5 tests (the new WR-06 describe block), 0 regressions, 0 new failing suites.

## Decisions Made

- Followed the plan's explicit instruction to wrap `showDialogModal` in its own try (rather than leaving it as the unwrapped "payoff" statement, which was 34.2-21's original design choice) -- WR-06 specifically calls out this escape route into `index.tsx:158` and the plan's Task 2 action supersedes 34.2-21's prior design note.
- Precomputed `title`/`message` into `let` bindings rather than inlining `t(...)` calls directly into the `showDialogModal({...})` object literal, so a throwing `t` cannot prevent the dialog call from being reached at all -- this is stated explicitly in the plan's Task 2 action and is "strictly better than the review's minimum ask."

## Deviations from Plan

None — plan executed exactly as written. Prettier's `--write` pass made only whitespace/line-wrap adjustments (e.g. wrapping `errorText = error instanceof Error ? ... : String(error)` across two lines); no semantic changes.

## Known Stubs

None.

## Threat Flags

None — this plan's changes are entirely within the threat model already declared in the plan's `<threat_model>` section (T-34.2-103, T-34.2-104, T-34.2-52, T-34.2-105 all addressed as specified; no new trust-boundary-crossing surface introduced).

## Self-Check

- `src/frontend/screens/Game/GameSubMenu/repairFailure.ts` — FOUND
- `src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure.test.ts` — FOUND
- Commit `2f6c2dcb` — FOUND
- Commit `78f9ad13` — FOUND

## Self-Check: PASSED
