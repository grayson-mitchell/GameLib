---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/backend/sidecar/bootstrap.ts
  - src/backend/sidecar/processGuards.ts
  - src/sidecar/index.ts
  - src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts
autonomous: true
requirements: [REQ-34.2-07]

must_haves:
  truths:
    - "A filesystem error on the anticheat cache file cannot kill the sidecar process — the rejection is caught at the call site inside the releasesInfoReady listener body and logged"
    - "A process-level unhandledRejection guard exists in the sidecar entry as defence-in-depth for the several other void-ed async calls the sidecar now owns"
    - "The guard LOGS and keeps the process alive — it never re-throws, never calls process.exit, and never writes to stdout (which carries the RPC frame stream)"
    - "A test makes downloadAntiCheatData reject and asserts no unhandledRejection fires and a warning is logged"
  artifacts:
    - path: "src/backend/sidecar/processGuards.ts"
      provides: "installUnhandledRejectionGuard() — log-only, non-fatal, stderr-fallback"
      exports: ["installUnhandledRejectionGuard"]
      min_lines: 25
    - path: "src/backend/sidecar/bootstrap.ts"
      provides: "downloadAntiCheatData(...).catch(...) inside the listener body"
      contains: ".catch("
    - path: "src/sidecar/index.ts"
      provides: "Guard installed before init()"
      contains: "installUnhandledRejectionGuard"
    - path: "src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts"
      provides: "Rejecting-downloadAntiCheatData survival proof + guard unit tests + entry-order gate"
      min_lines: 80
  key_links:
    - from: "src/backend/sidecar/bootstrap.ts releasesInfoReady listener"
      to: "downloadAntiCheatData rejection"
      via: "a .catch() attached to the call itself, not to backendEvents.on()"
      pattern: "downloadAntiCheatData\\([\\s\\S]*?\\)\\s*\\.catch"
    - from: "src/sidecar/index.ts"
      to: "src/backend/sidecar/processGuards.ts"
      via: "import + call before init()"
      pattern: "installUnhandledRejectionGuard\\(\\)"
---

<objective>
Close **verification gap #2 (REQ-34.2-07) / code-review finding CR-02**: `bootstrap.ts:236-240`
does `void downloadAntiCheatData(...)` inside the re-homed `releasesInfoReady` listener, and the
surrounding `try`/`catch` (lines ~230-247) wraps only the synchronous `backendEvents.on(...)`
registration — which cannot throw. It gives ZERO coverage to the listener body, which runs later
from the emitter outside that stack frame.

`downloadAntiCheatData` (`anticheat/utils.ts:27-28`) has a rejectable
`await createMD5(anticheatDataPath)` OUTSIDE its own internal `try`/`catch` (which only wraps the
network fetch at lines ~44-62). An `EACCES`, a TOCTOU delete between `existsSync` and the open, or
the path being a directory all reject the outer promise. No `process.on('unhandledRejection')`
handler exists anywhere in the sidecar — only comments noting its absence
(`electronStub.ts:214`, `appShellFlowRegistration.ts:90`, `gameDetailsFlowRegistration.ts:61`).
On Node >= 15 the default is `--unhandled-rejections=throw`, so a single filesystem error takes down
the entire backend process.

Purpose: a local availability defect — one unreadable cache file kills the whole backend — becomes a
logged warning.
Output: a call-site `.catch()`, a log-only process guard, and a test that proves survival.

**CRITICAL PRIOR ART — heed `.claude/skills/spike-findings-gamelib/SKILL.md` and this project's
recorded `sidecar-dialog-reject-crashes` incident.** That incident was caused by making sidecar code
REJECT where it previously resolved, which hard-crashed the process because callers `await` with no
`try`/`catch` and no `unhandledRejection` guard exists. The generalised lesson governs this plan:
**neutralise by resolving/logging a safe value, never by introducing a new rejection path.** The
guard added here MUST log and keep the process alive. It must not re-throw, must not
`process.exit`, and must not itself be able to throw.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@.claude/skills/spike-findings-gamelib/SKILL.md

Gap evidence:
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VERIFICATION.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW.md

<interfaces>
<!-- Current state the executor works against. No codebase exploration needed. -->

`src/backend/sidecar/bootstrap.ts` (Block A, lines ~228-248) — current shape:
  `if (!anticheatListenerRegistered) { anticheatListenerRegistered = true; try {
     backendEvents.on('releasesInfoReady', (releasesInfo) => {
        logDebug('Releases info ready, checking anticheat data', LogPrefix.Backend)
        void downloadAntiCheatData(isMac ? releasesInfo.anticheatFiles.shaMac
                                         : releasesInfo.anticheatFiles.shaLinux) }) }
     catch (error) { logWarning(\`[bootstrap] Failed to register releasesInfoReady anticheat
     listener: ${error}\`, LogPrefix.Backend) } }`
  Block B (lines ~254-264) calls `fetchLastestReleases()` inside its own try/catch.
  `logDebug`/`logWarning`/`LogPrefix` are already imported by this module.

`src/backend/anticheat/utils.ts`:
  `async function createMD5(filePath)` — `createReadStream(filePath)` + `await finished(rStream)`.
  `async function downloadAntiCheatData(latestFileHash?: string)`:
     early-returns on `process.env.CI === 'e2e'` and `isWindows`;
     then `if (existsSync(anticheatDataPath)) { const localFileHash = await createMD5(...) }`
     — **this await is not inside any try/catch**;
     then `runOnceWhenOnline(async () => { try { axiosClient.get(...); writeFile(...) }
     catch { logWarning(...) } })`.
  Exports `gameAnticheatInfo` and `downloadAntiCheatData`.

`src/sidecar/index.ts` — 23 lines total, currently:
  `import { init } from 'backend/sidecar/bootstrap'` then a bare `init()`.
  This is the file `pnpm build:sidecar` bundles to `build/main/sidecar.js` and the Rust shell spawns.

`src/backend/logger` exports `logWarning(message, prefix)` and `LogPrefix`.
GOTCHA (recorded project memory): `heroicLogWriter` is unset until `initLogger()` runs inside
`bootstrap.init()`, so a `logWarning()` call that happens BEFORE init can throw on the unset writer.

Transport constraint: the sidecar's `process.stdout` carries the newline-delimited JSON RPC frame
stream. Diagnostic output must never be written to stdout — `stderr` only.

Jest projects (`jest.config.js`) are `src/backend`, `src/common`, `src/frontend`, `src/preload`,
`meta`. **`src/sidecar/` is NOT a jest project root**, and importing it executes `init()` at module
scope — so any assertion about `src/sidecar/index.ts` must be a source-text (by-construction) gate,
never an import.

Existing isolated-bootstrap harness to mirror: `src/backend/sidecar/__tests__/bootstrapWirings.test.ts`
uses `jest.isolateModules()` with CommonJS `require()` (its callback must run synchronously),
`jest.unmock('i18next')`, `jest.mock('electron', () => jest.requireActual('../electronStub'))`,
a `jest.mock('os')` homedir redirect namespaced by a `mock`-prefixed counter, a `jest.mock('backend/config')`
`GlobalConfig.get` stub, a `collectLines(stream)` helper, a `flush(times)` setImmediate pump, and a
`waitFor(predicate)` real-timer poller.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attach a rejection handler to the downloadAntiCheatData call inside the listener body</name>
  <read_first>
    - `src/backend/sidecar/bootstrap.ts` lines 195-267 in full (the `initOnlineMonitor` block,
      Block A's full comment + try/catch, Block B, and the `READY_SENTINEL` write) — so the edit is
      made against current state, not the version quoted in the review
    - `src/backend/anticheat/utils.ts` lines 1-64 — confirming `createMD5`'s `await` sits outside
      `downloadAntiCheatData`'s only try/catch, which wraps the `runOnceWhenOnline` network callback
    - `.planning/phases/34.2-.../34.2-VERIFICATION.md` gap #2 (`gaps:` entry for REQ-34.2-07,
      lines 20-28) — the `missing:` list this task implements
    - `.planning/phases/34.2-.../34.2-REVIEW.md` finding CR-02 (lines 159-221)
  </read_first>
  <action>
    In `src/backend/sidecar/bootstrap.ts`'s Block A, replace the `void downloadAntiCheatData(...)`
    statement with the same call followed by a `.catch((error) => { ... })` that calls
    `logWarning` with `LogPrefix.Backend` and a message beginning with the literal prefix
    `[bootstrap] downloadAntiCheatData failed:` followed by the stringified error. Use `String(error)`
    rather than template-interpolating the raw error object, matching the review's recommended shape.

    Keep the existing `logDebug('Releases info ready, checking anticheat data', LogPrefix.Backend)`
    line and the `isMac ? releasesInfo.anticheatFiles.shaMac : releasesInfo.anticheatFiles.shaLinux`
    argument expression byte-identical — this listener reproduces `anticheat/ipc_handler.ts:12-19`'s
    body and that parity is load-bearing (D-04).

    KEEP the existing `try`/`catch` around `backendEvents.on(...)`. Do not delete it. Instead, amend
    the comment above Block A with one sentence recording WHY both exist and what each covers: the
    outer `try`/`catch` covers only the synchronous registration call, and the `.catch()` covers the
    listener body, which runs later from the emitter outside that stack frame. This is the exact
    misunderstanding CR-02 identified, so it must be written down at the site, not inferred.

    Do NOT change Block B (`fetchLastestReleases()`), the `anticheatListenerRegistered` /
    `releasesFetchInitialized` idempotency guards, the block ordering, or the `READY_SENTINEL` write
    position. WR-05 (the READY sentinel firing before i18next's async resource load settles) is
    accepted debt for this cycle and is explicitly out of scope.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit && npx jest bootstrapWirings 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/sidecar/bootstrap.ts` contains no remaining `void downloadAntiCheatData` token
      (`grep -c "void downloadAntiCheatData" src/backend/sidecar/bootstrap.ts` returns 0).
    - The `downloadAntiCheatData(` call in Block A is followed by a `.catch(` in the same expression.
    - The `.catch` body calls `logWarning` and the message contains the literal substring
      `[bootstrap] downloadAntiCheatData failed:`.
    - The `.catch` body contains no `throw` and no `process.exit`.
    - The `try { backendEvents.on('releasesInfoReady'` block and its `catch` are still present.
    - The comment above Block A contains a sentence stating that the outer try/catch covers only the
      synchronous registration and the `.catch` covers the listener body.
    - `npx tsc --noEmit` clean.
    - `npx jest bootstrapWirings` passes all 7 existing tests unchanged (this task adds none there).
  </acceptance_criteria>
  <done>
    A rejection from `downloadAntiCheatData` is caught at the call site inside the listener body and
    logged as a warning; the existing D-07 wiring proof is unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a log-only process-level unhandledRejection guard and install it in the sidecar entry</name>
  <read_first>
    - `src/sidecar/index.ts` in full (23 lines) — note the deliberate thin-entry design and the
      comment explaining that all load-bearing shim ordering lives in `bootstrap.ts`'s module scope
    - `src/backend/sidecar/electronStub.ts` around line 214, `src/backend/sidecar/appShellFlowRegistration.ts`
      around line 90, and `src/backend/sidecar/gameDetailsFlowRegistration.ts` around line 61 — the
      three existing comments that NOTE the absence of an unhandledRejection guard, so their wording
      can be updated to point at the new module instead of describing it as absent
    - `src/backend/logger/index.ts` — `logWarning`'s signature and its dependence on `heroicLogWriter`
    - `.claude/skills/spike-findings-gamelib/SKILL.md` — the `sidecar-dialog-reject-crashes` entry;
      the governing constraint is that a neutralisation must resolve/log, never introduce a new
      rejection or exit path
  </read_first>
  <action>
    Create `src/backend/sidecar/processGuards.ts` exporting a single function
    `installUnhandledRejectionGuard(target: NodeJS.EventEmitter = process): void`.

    Behavior:
    - Idempotent: a module-scope boolean guard so a second call registers no second listener
      (mirrors `bootstrap.ts`'s `anticheatListenerRegistered` / `releasesFetchInitialized` pattern).
    - Registers a listener on the `'unhandledRejection'` event of `target`.
    - The listener LOGS and RETURNS. It must NOT re-throw, must NOT call `process.exit`, and must NOT
      change the exit code.
    - The log message must contain the literal prefix `[sidecar] unhandled promise rejection:`
      followed by the stringified reason.
    - The listener's own logging must be wrapped in a `try`/`catch` whose fallback writes the same
      message to `process.stderr` via `process.stderr.write`. This is load-bearing for two reasons:
      (1) `heroicLogWriter` is unset until `initLogger()` runs inside `bootstrap.init()`, so a
      rejection during early boot would make `logWarning` itself throw — turning the guard into a
      new crash path, the precise failure `sidecar-dialog-reject-crashes` records; (2) `stdout`
      carries the RPC frame stream, so diagnostics must never go there.
    - Take the `target` parameter defaulted to `process` purely so a unit test can drive a fake
      emitter without attaching real handlers to the jest worker process.

    Give the module a header docstring stating: what it guards, why the sidecar needs it (the sidecar
    owns several `void`-ed async calls; on Node >= 15 the default is `--unhandled-rejections=throw`),
    that it is defence-in-depth and NOT a substitute for call-site `.catch()`, and that it must never
    re-throw or exit.

    Then edit `src/sidecar/index.ts` to import `installUnhandledRejectionGuard` from
    `backend/sidecar/processGuards` and call it on the line BEFORE `init()`. Ordering is load-bearing:
    the guard must be live before `bootstrap.ts`'s module scope and `init()` can produce any
    rejection. Extend that file's existing header docstring with one sentence recording that ordering
    constraint.

    Finally, update the three existing comments that currently describe the guard as ABSENT
    (`electronStub.ts` ~line 214, `appShellFlowRegistration.ts` ~line 90,
    `gameDetailsFlowRegistration.ts` ~line 61) so they instead name `sidecar/processGuards.ts` and
    state that it is defence-in-depth only — a `send`-kind body must still wrap itself in
    `try`/`catch` rather than rely on the guard. Do not change any executable line in those three
    files; comment text only.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit && npx eslint src/backend/sidecar/processGuards.ts src/sidecar/index.ts && echo LINT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/sidecar/processGuards.ts` exists and exports `installUnhandledRejectionGuard`.
    - `grep -c "process.exit" src/backend/sidecar/processGuards.ts` returns 0.
    - Stripping comment lines from `processGuards.ts` and grepping for `throw` returns 0 matches
      inside the `'unhandledRejection'` listener body.
    - `grep -c "process.stdout" src/backend/sidecar/processGuards.ts` returns 0, and
      `grep -c "process.stderr" src/backend/sidecar/processGuards.ts` returns at least 1.
    - `src/sidecar/index.ts` contains `installUnhandledRejectionGuard()` and that call appears on an
      earlier line number than the `init()` call.
    - `grep -n "from 'electron'" src/backend/sidecar/processGuards.ts` returns no match — the
      existing `gameDetailsImportGate.test.ts` Gate 1 ("no .ts file directly under
      src/backend/sidecar/ imports the real electron module") must stay green with this new file in
      that directory.
    - `npx jest gameDetailsImportGate` passes (Gate 1 now covers the new file).
    - `npx tsc --noEmit` clean; `npx eslint` on both changed files exits 0.
    - `git diff` on `electronStub.ts`, `appShellFlowRegistration.ts` and
      `gameDetailsFlowRegistration.ts` shows changes on comment lines only (every changed line begins
      with optional whitespace then `//`, `*`, or `/*`).
  </acceptance_criteria>
  <done>
    A log-only, non-fatal, stderr-fallback `unhandledRejection` guard exists and is installed in the
    sidecar entry before `init()`; the three stale "no guard exists" comments now point at it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the sidecar survives a rejecting downloadAntiCheatData and pin the guard's contract</name>
  <read_first>
    - `src/backend/sidecar/__tests__/bootstrapWirings.test.ts` in full — the harness to mirror:
      `setupIsolatedBootstrap()`, the `jest.isolateModules()` + CommonJS `require()` requirement,
      `jest.unmock('i18next')`, the `jest.mock('electron', () => jest.requireActual('../electronStub'))`
      line, the `mock`-prefixed `mockTestHomeSuffix` os redirect, the `backend/config` `GlobalConfig`
      stub, and the `collectLines`/`flush`/`waitFor` helpers
    - `src/backend/sidecar/bootstrap.ts` Block A as edited by Task 1
    - `src/backend/sidecar/processGuards.ts` as created by Task 2
    - `src/sidecar/index.ts` as edited by Task 2
    - `.planning/phases/34.2-.../34.2-VERIFICATION.md` gap #2 — specifically that no
      `process.on('unhandledRejection')` handler existed anywhere, which this test now pins
  </read_first>
  <action>
    Create `src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts` with three groups of tests.

    **Group 1 — the survival proof (REQ-34.2-07, gap #2).** Mock `../../anticheat/utils` so
    `downloadAntiCheatData` is a `jest.fn()` returning a REJECTED promise (reason: an Error whose
    message names a filesystem fault, e.g. an `EACCES` on `areweanticheatyet.json`), and
    `gameAnticheatInfo` is a plain `jest.fn()` so other registration modules still import cleanly.
    Mock `../../logger` (or spy on `logWarning`) so the warning is observable.

    Drive the REAL `bootstrap.init()` through the same `jest.isolateModules()` harness
    `bootstrapWirings.test.ts` uses, with injected `PassThrough` streams, then emit
    `backendEvents.emit('releasesInfoReady', { anticheatFiles: { shaMac: 'a', shaLinux: 'b' } })`
    and pump with the `flush()` helper.

    Assert all three:
    1. A locally-registered `process.on('unhandledRejection', spy)` listener — attached in
       `beforeEach` and removed in `afterEach` via `process.off` — is NEVER called.
    2. `logWarning` was called with a message containing `[bootstrap] downloadAntiCheatData failed:`.
    3. The listener still ran: `downloadAntiCheatData` was called exactly once with the
       platform-selected hash argument, proving the `.catch()` did not replace the call.

    Add a RED-proof note in the test name or a comment stating that reverting Task 1's `.catch()`
    makes assertion 1 fail.

    **NO FILESYSTEM WRITES.** Because `downloadAntiCheatData` is mocked, this suite must not create,
    write, or delete anything under `appFolder`. Do not add an `mkdirSync(appFolder)` or an anticheat
    fixture file. This is deliberate: `pathShim.resolveAppDataDir()` prefers `env.APPDATA` /
    `env.XDG_CONFIG_HOME` over `homedir()` on Windows/Linux, so an `os.homedir()` mock alone does NOT
    guarantee redirection there (finding CR-03). Add a `beforeAll` assertion that the resolved
    `appFolder` is contained inside `os.tmpdir()`, computed with `resolve` + `relative` (NOT
    `startsWith`, and NOT `join` — this project's recorded convention is that `path.join` is not
    containment), so a future edit that starts writing has a loud tripwire already in place.

    **Group 2 — the guard's contract (unit tests against `installUnhandledRejectionGuard`).**
    Pass a real `new EventEmitter()` as `target` so nothing attaches to the jest worker's `process`.
    Assert: exactly one `'unhandledRejection'` listener is registered; a second
    `installUnhandledRejectionGuard(target)` call does not add a second listener (idempotency);
    emitting `'unhandledRejection'` with an Error logs a message containing
    `[sidecar] unhandled promise rejection:` and does NOT throw out of the `emit(...)` call
    (assert via `expect(() => target.emit(...)).not.toThrow()`); and, with `logWarning` forced to
    throw, emitting still does not throw and `process.stderr.write` (spied) receives the message —
    the early-boot `heroicLogWriter`-unset path.

    **Group 3 — the entry-ordering gate (by-construction, source text).** Read
    `src/sidecar/index.ts` as TEXT with `readFileSync` — do NOT import it, because importing executes
    `init()` and `src/sidecar/` is not a jest project root. Strip comment lines first (mirror
    `gameDetailsImportGate.test.ts`'s own `stripComments` helper: drop lines whose trimmed form
    starts with `//`, `*`, or `/*`) so the file's explanatory header cannot self-satisfy the gate.
    Assert on the stripped text that `installUnhandledRejectionGuard(` appears and that its index is
    strictly less than the index of the `init()` call.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx jest sidecarRejectionGuard 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest sidecarRejectionGuard` reports 1 suite passed and at least 7 tests passed.
    - The suite contains a test asserting the local `process.on('unhandledRejection')` spy has
      `toHaveBeenCalledTimes(0)` after a rejecting `downloadAntiCheatData` is driven through the real
      `bootstrap.init()` + a real `backendEvents.emit('releasesInfoReady', ...)`.
    - The suite contains a test asserting `logWarning` received a message matching
      `/\[bootstrap\] downloadAntiCheatData failed:/`.
    - The suite contains an idempotency test proving a second `installUnhandledRejectionGuard(target)`
      call leaves `target.listenerCount('unhandledRejection')` at 1.
    - The suite contains a test where `logWarning` is made to throw and the emit still does not throw,
      with a `process.stderr.write` spy asserted called.
    - The suite contains a source-text gate asserting `installUnhandledRejectionGuard(` precedes
      `init()` in comment-stripped `src/sidecar/index.ts`.
    - `grep -cE "mkdirSync|writeFileSync|rmSync|writeFile\(" src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts`
      returns 0 — the suite performs no filesystem writes.
    - The suite contains a `beforeAll` containment check on `appFolder` implemented with `resolve` and
      `relative` (both identifiers present in the file), not `startsWith`.
    - RED spot-check recorded in the SUMMARY: temporarily reverting Task 1's `.catch()` makes the
      unhandledRejection-spy test fail; the tree is restored afterwards.
  </acceptance_criteria>
  <done>
    A test proves that a rejecting `downloadAntiCheatData` neither fires an unhandled rejection nor
    stops the listener from running, and that the process guard logs without throwing or exiting.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem -> sidecar process | `areweanticheatyet.json` in `appFolder` is read by `createMD5` at startup; its permissions, type (file vs directory) and existence are all attacker- or accident-controllable by anything with write access to the user's config directory |
| network (GitHub raw) -> sidecar | `downloadAntiCheatData` fetches game data over HTTPS; the fetch itself is already inside a try/catch |
| sidecar process -> Rust shell (stdout frames) | stdout is the RPC transport; any diagnostic written there corrupts framing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-38 | Denial of Service | `bootstrap.ts` Block A `void downloadAntiCheatData(...)` | mitigate | A single `EACCES`/`ENOENT`/TOCTOU/is-a-directory fault on `<appFolder>/areweanticheatyet.json` currently rejects an unguarded promise and, under Node >= 15's default `--unhandled-rejections=throw`, kills the entire backend process — a full local DoS triggerable by a stray file. Task 1 attaches a `.catch()` at the call site inside the listener body. |
| T-34.2-39 | Denial of Service | Every other `void`-ed async call the sidecar owns | mitigate | The sidecar has no process-level rejection guard at all. Task 2 adds a log-only `unhandledRejection` guard installed before `init()` as defence-in-depth. Explicitly NOT a substitute for call-site handling — the three registration-module comments are updated to say so. |
| T-34.2-40 | Denial of Service | The new guard itself (`processGuards.ts`) | mitigate | The recorded `sidecar-dialog-reject-crashes` incident shows that a "fix" which introduces a new throw/reject path is worse than the bug. The guard therefore never re-throws, never calls `process.exit`, and wraps its own `logWarning` in try/catch with a `process.stderr` fallback because `heroicLogWriter` is unset until `initLogger()` — otherwise the guard would itself throw during early boot. Pinned by a unit test. |
| T-34.2-41 | Tampering | Sidecar RPC framing via stdout | mitigate | Diagnostic output from the guard goes to `process.stderr` only. Writing to stdout would inject non-frame bytes into the newline-delimited JSON RPC stream the Rust shell parses — the same shape as the recorded IN-04 stray-`rustInvoke`-frame observation. Pinned by a `grep` acceptance criterion for zero `process.stdout` references. |
| T-34.2-42 | Tampering / Integrity | Test-induced writes to a real user config directory | mitigate | `pathShim.resolveAppDataDir()` prefers `env.APPDATA`/`env.XDG_CONFIG_HOME` over `homedir()`, so an `os.homedir()` mock alone does not redirect on Windows/Linux (finding CR-03). Task 3's suite performs ZERO filesystem writes and additionally installs a `resolve`+`relative` containment tripwire on `appFolder`. `path.join` is deliberately not used for the check — this project's recorded convention is that `join` is not containment. |
| T-34.2-43 | Information Disclosure | Rejection reason text written to the log / stderr | accept | The logged reason is a filesystem error string (path + errno) for a public game-data cache file. No credentials, tokens, or Steam refresh tokens traverse this path. Local log file only. |
| T-34.2-44 | Repudiation | Silent swallow of anticheat download failures | accept | After this change a failed anticheat refresh is a warning rather than a crash, so `getAnticheatInfo` may return `null` with only a log line to explain it. Accepted: that is the pre-existing declared behavior for a cold cache (`34.2-PORTED-CHANNELS.md` §2 rider 2), and it is logged, never silent. |
| T-34.2-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages — one new first-party module, two edited files, one new test. Package Legitimacy Gate not triggered; no `[ASSUMED]`/`[SUS]` checkpoint required. |
</threat_model>

<verification>
Build gates that must stay green:

- `npx tsc --noEmit` — clean, no output
- `cd src-tauri && cargo check --quiet` — exit 0, no output (this plan touches no Rust, so this is a
  no-regression check only)
- `npx jest gameDetailsFlows enrichmentFlows bootstrapWirings gameDetailsImportGate gameDetailsModules knownFixes longRunningChannels ratingMap` — all suites pass. `bootstrapWirings` must still pass all 7 tests unchanged, and `gameDetailsImportGate` Gate 1 must stay green with `processGuards.ts` newly present under `src/backend/sidecar/`.
- New suite: `npx jest sidecarRejectionGuard` — 1 suite, >= 7 tests, all green
- Full backend suite baseline: `npx jest --testPathPattern=src/backend` → **105/106 suites,
  2211/2212 tests**, plus this plan's new suite. The single known failure is
  `rustInvokeChannel.test.ts` "writes a single well-formed rustInvoke frame", pre-existing from
  Phase 34.1 and explicitly OUT OF SCOPE. Do NOT write an acceptance criterion demanding 106/106.
- The Electron build keeps working unchanged (REQ-34.2-14): `src/backend/sidecar/**` and
  `src/sidecar/**` are sidecar-only; `main.ts` is not touched.
</verification>

<success_criteria>
- No `void downloadAntiCheatData` remains in `bootstrap.ts`; the call carries a `.catch()` that logs
- `installUnhandledRejectionGuard` exists, is idempotent, log-only, cannot throw, and never touches stdout
- `src/sidecar/index.ts` installs the guard before `init()`
- A test drives a rejecting `downloadAntiCheatData` through the real `bootstrap.init()` and proves
  zero `unhandledRejection` events plus a logged warning
- The new suite writes nothing to the filesystem and carries a `resolve`+`relative` containment tripwire
</success_criteria>

<output>
Create `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-09-SUMMARY.md` when done
</output>
