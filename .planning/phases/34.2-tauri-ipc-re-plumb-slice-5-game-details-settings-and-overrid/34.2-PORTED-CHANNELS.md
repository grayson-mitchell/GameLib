# Phase 34.2 — Declared Ported-Channel List

**Purpose:** The enumerated set the next Tauri phase (34.3+) starts from, and the artifact the
Phase 35 cutover gate reads for this slice, mirroring how this phase itself started from
`34.1-PORTED-CHANNELS.md`. It describes what actually shipped in 34.2-01..34.2-06 — verified row
by row against all six plan SUMMARY.md files, not against this phase's original planned intent.
Reconciliation findings are in section 7; no drift was found between the plan's declared 26-channel
list and what shipped.

**Refreshed after gap cycle 1 (plans 34.2-08..34.2-14).** This document originally described only
34.2-01..34.2-06 and predated the code review that found CR-01/CR-02/CR-03 and the verification
run that returned `gaps_found` (11/14). Plan 34.2-14 (this plan) re-verified every fact below against
source — not against a plan document — and extended §1, §2, §5, §6 and §7 to describe the state at
the END of gap cycle 1. A reader can tell which snapshot they are holding: if a section names plans
34.2-08 through 34.2-14, it is gap-cycle content; everything else describes the original
34.2-01..34.2-06 shipment, unchanged.

---

## 1. Port kinds

This slice introduces **no new kind** to the vocabulary Phases 30–34.1 established. It uses exactly
two of the existing kinds:

- **`sidecar invoke`** (23 channels) — the channel is registered on the sidecar's `ipcMain.handle`
  and answered over the Tauri transport.
- **`sidecar send`** (3 channels) — registered via `ipcMain.on`, fire-and-forget, each body wrapped
  in `try`/`catch` → `logSendFailure` so a throw cannot reach the sidecar's unguarded process
  boundary.

Zero `rustInvoke` rows and zero `renderer-side (Tauri JS)` rows this slice. The Rust-adjacent
changes — adding the string `"getCrossoverIndex"` to the existing `LONG_RUNNING_CHANNELS` array in
`src-tauri/src/main.rs` (plan 34.2-06), then adding `"repair"` and `"readConfig"` to the same array
during gap cycle 1 (plan 34.2-08, closing verification gap #1 / code-review CR-01) — are
**timeout-policy edits**, not a port kind, and add no `dispatch_rust_channel` arm. `LONG_RUNNING_CHANNELS`
now has **eight members** (`install`, `updateGame`, `uninstall`, `checkGameUpdates`,
`refreshLibrary`, `getCrossoverIndex`, `repair`, `readConfig`) — three of which are attributable to
this phase. The `dispatch_rust_channel` arm count stays at 11 (last changed by Phase 34.1's
`tray_set_icon`).

---

## 2. The 26 channels

Proof level is `unit` on every row — a test exercises the behavior over the real sidecar RPC loop.
No row claims more than that; see section 6 for the honest sign-off.

| Channel | Kind | Backed by | Proof level | Notes |
|---|---|---|---|---|
| `getGameInfo` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; includes the `attachOverrides` fast-path and legendary fastpath |
| `getExtraInfo` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04 |
| `getGameSettings` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; see §4 — near-duplicate of `requestGameSettings` |
| `isGameAvailable` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; object-argument-intact proof |
| `getLaunchOptions` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; see §3 rider 1 (Steam stub, `library.ts:907`) |
| `kill` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; `callAbortController` → `.stop()` call-ordering proof |
| `repair` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; see §3 rider 1 (Steam stub, `games.ts:1610`); D-02 i18next end-to-end proof lives on this row. **Gap cycle 1 (plan 34.2-08):** exempted from the 60s bounded invoke timeout by adding `"repair"` to `LONG_RUNNING_CHANNELS` in `src-tauri/src/main.rs` — the original 34.2-06 audit that populated this array MISSED `repair`/`readConfig`, caught by code-review finding CR-01 and verification gap #1. Concrete symptom before the fix: a real `legendary repair`/`gogdl repair` on a multi-GB title routinely exceeds 60s, the Rust side drops the pending entry and returns `sidecar invoke timed out` while the repair keeps running in the background, and the renderer's `onRepairYesClick` call site had no `.catch()`, so the eventual real resolution (or a second, unrelated error) produced an unhandled promise rejection on top of the wrong answer. The renderer call site (`GameSubMenu/index.tsx`) now wraps the call in try/catch and logs via `window.api.logError`, no rethrow. |
| `changeInstallPath` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; see §3 rider 1 (Steam stub, `library.ts:790` — the fourth stub, found during planning) |
| `readConfig` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; see §3 rider 3 (Legendary-only reachability). **Gap cycle 1 (plan 34.2-08):** exempted from the same 60s bound for the same reason as `repair` above — `readConfig('library')` calls `legendary.refresh()`, the same underlying work `refreshLibrary` is already exempted for. Honest tradeoff, recorded in the Rust code comment and accepted per threat T-34.2-35: the exemption applies to the WHOLE `readConfig` channel, not just the `'library'` argument, so the fast `readConfig('user')` call is now unbounded too — a wedge on that path becomes a never-settling promise instead of a 60s rejection. |
| `getGameOverride` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; see §3 rider 3 (Legendary-only reachability) |
| `getGameSdl` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; see §3 rider 3 (Legendary-only reachability) |
| `getAvailableCyberpunkMods` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; see §3 rider 3 (GOG-only reachability) |
| `setCyberpunkModConfig` | sidecar invoke | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/04; see §3 rider 3 — zero frontend callers at all |
| `getGameMetadataOverride` | sidecar invoke | `game_overrides/index.ts` (already Electron-free, never extracted) | unit | Plan 34.2-04 |
| `getAllGameOverrides` | sidecar invoke | `game_overrides/index.ts` (already Electron-free, never extracted) | unit | Plan 34.2-04 |
| `getKnownFixes` | sidecar invoke | `knownFixes.ts` (`readKnownFixes`, extracted D-05) | unit | Plan 34.2-03/06 |
| `getCrossoverIndex` | sidecar invoke | `crossover_index/crossoverRatingMap.ts` (`buildCrossoverRatingMap`, extracted D-06) | unit | Plan 34.2-03/06; exempted from the 60s invoke timeout (D-10) — see §5 |
| `getWikiGameInfo` | sidecar invoke | `wiki_game_info/wiki_game_info.ts` (already Electron-free) | unit | Plan 34.2-06; D-10 cold-cache measurement (Hades 1190ms/Stardew 957ms/Portal2 702ms) settled the exemption question — NOT exempted, stays on the default 60s bound |
| `getAnticheatInfo` | sidecar invoke | `anticheat/utils.ts` (`gameAnticheatInfo`, already Electron-free) | unit | Plan 34.2-06; see §3 rider 2 — `null` for Steam titles on every platform, `null` for everything on Windows, even fully primed |
| `searchStores` | sidecar invoke | `storeSearch/cheapshark.ts` (`searchGames`, already Electron-free) | unit | Plan 34.2-06; Phase 20 D-14 rethrow-don't-swallow contract reproduced verbatim |
| `getStoreSearchDeals` | sidecar invoke | `storeSearch/cheapshark.ts` (`getGameDeals`, already Electron-free) | unit | Plan 34.2-06; same rethrow contract |
| `getStoreSearchStoreMap` | sidecar invoke | `storeSearch/cheapshark.ts` (`getStoreMap`, already Electron-free) | unit | Plan 34.2-06; same rethrow contract |
| `removeRecent` | sidecar invoke | `recent_games/recent_games.ts` (`removeRecentGame`, already Electron-free) | unit | Plan 34.2-06 |
| `setGameMetadataOverride` | sidecar send | `gamedetails/overrides.ts` + `sidecarRpc.pushFrontendMessage` for the `metadataChanged` push | unit | Plan 34.2-02/05; store read-back + independently-asserted push-frame proof, zero new Rust arms |
| `changeGameVersionPinnedStatus` | sidecar send | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/05; see §3 rider 1 (Steam stub, `library.ts:797`); 3-positional-argument order proven load-bearing |
| `addNewApp` | sidecar send | `gamedetails/dispatch.ts` | unit | Plan 34.2-02/05; sideload-manager-only dispatch proof |

**Total: 26.** `sidecar invoke` accounts for 23 rows, `sidecar send` for 3 (23 + 3 = 26).

### The two bootstrap wirings (not channels — do not count these in the 26)

These are the load-bearing content of this slice, not the channel table above. Both are fixes to
already-shipped defects, not new features (Phases 30/32 shipped the underlying bugs; this slice is
the first to expose them because its channels are the first to depend on either).

- **i18next initialization (D-02, REQ-34.2-02).** `i18next.use(Backend).init({...})` — previously
  present only inside Electron's `app.whenReady()` in `main.ts` — now also runs at sidecar boot in
  `bootstrap.ts`, mirroring `main.ts:460-472` field-for-field behind an idempotent guard. Before this
  fix, `i18next.t()` returned `undefined` (not a throw, not the inline default) at every backend call
  site reachable from the sidecar, including `repair`'s notify body. **What the tests prove:**
  `i18next.t()` returns a real, non-`undefined` string after `init()`, against the real (unmocked)
  `i18next` singleton (`bootstrapWirings.test.ts`), and `repair`'s own notify body reflects a real
  translated string over the real sidecar RPC loop (`gameDetailsFlows.test.ts`, RED-confirmed by
  temporarily removing the `init()` call). **What the tests do NOT prove:** that a notification
  actually renders in a running Tauri window, or that the packaged build's locale-path resolution
  (the `publicdir-getapppath-chunking` gotcha family) works — jest runs from the repo root, where
  `process.cwd()` happens to resolve correctly. See §6 and `34.2-HUMAN-UAT.md` UAT-34.2-01.
- **`fetchLastestReleases()` + the re-homed `releasesInfoReady` → `downloadAntiCheatData` listener
  (D-07, REQ-34.2-07).** `fetchLastestReleases()` — the sole emitter of `releasesInfoReady` — was
  previously called only from `main.ts`, never in the sidecar bootstrap, so the event never fired
  under Tauri and `getAnticheatInfo` structurally could not return data. Both are now wired into
  `bootstrap.ts`'s `init()`, after `initOnlineMonitor()` per its `runOnceWhenOnline` precondition.
  The listener body is reproduced verbatim from `anticheat/ipc_handler.ts:12-19` (not imported — that
  file's module scope calls `addHandler` from `backend/ipc`, forbidden under `src/backend/sidecar/`).
  **What the tests prove:** the event fires and `downloadAntiCheatData` writes the data file, against
  a tmp home with a mocked axios (`bootstrapWirings.test.ts`); the anticheat riders (see §3 rider 2)
  are asserted, not just documented (`enrichmentFlows.test.ts`). **What the tests do NOT prove:** that
  a real GitHub fetch completes in a live process across a real network after a real connectivity
  transition, or that the file lands under a real `appFolder` in a packaged build. See §6 and
  `34.2-HUMAN-UAT.md` UAT-34.2-02.
  **Gap cycle 1 (plan 34.2-09) — the wiring was crash-unsafe.** This entry was originally declared
  "wired" when it shipped, but the `void downloadAntiCheatData(...)` call inside the
  `releasesInfoReady` listener body was crash-unsafe: a single `EACCES`/`ENOENT`/TOCTOU/is-a-directory
  fault reading or writing `areweanticheatyet.json` would have rejected an unguarded promise and, with
  no process-level `unhandledRejection` handler anywhere in the sidecar, killed the whole sidecar
  process. The surrounding `try`/`catch` covered only the synchronous `backendEvents.on(...)`
  registration call — zero coverage for the listener body, which runs later from the emitter, outside
  that stack frame. Closed by code-review finding CR-02 / verification gap #2: plan 34.2-09 attached a
  `.catch()` directly to the `downloadAntiCheatData(...)` call inside the listener (logging via
  `logWarning`, never rethrowing), and added `sidecar/processGuards.ts`'s log-only, idempotent,
  never-throw/never-exit `installUnhandledRejectionGuard()` as process-wide defence-in-depth,
  installed from `src/sidecar/index.ts` before `init()`. Proven live: `sidecarRejectionGuard.test.ts`
  drives the real, unmocked `bootstrap.init()` with a rejecting `downloadAntiCheatData` and asserts
  zero `unhandledRejection` events fire.

---

## 3. Declaration riders

Three riders, stated so a reader cannot over-read the table above.

**1. Steam-runner upstream stubs.** For Steam titles, FOUR ported channels are inert **in Electron
already** — porting them faithfully reproduces Electron's own current behavior, not a regression this
phase introduces, and not a gap to open a cycle against:
- `repair` — `storeManagers/steam/games.ts:1610` logs `"SteamGame.repair not implemented until Phase 2"`.
- `changeInstallPath` — `storeManagers/steam/library.ts:790` (`changeGameInstallPath`), an
  underscore-arg no-op. This is the **fourth** Steam stub; CONTEXT.md D-01 named three
  (`repair`/`changeGameVersionPinnedStatus`/`getLaunchOptions`) and this one was found during
  planning.
- `changeGameVersionPinnedStatus` — `storeManagers/steam/library.ts:797`, an underscore-arg no-op.
- `getLaunchOptions` — `storeManagers/steam/library.ts:907`, an underscore-arg no-op.

**2. `getAnticheatInfo` even when primed.** `gameAnticheatInfo` matches on
`info.storeIds.epic?.namespace` and early-returns `null` on `isWindows`, so even with plan 34.2-01's
D-07 wiring and the data file present on disk, it returns `null` for Steam titles on every platform
and `null` for everything on Windows. **"Ported and primed" must not read as "anticheat badges appear
for Steam games."**

**3. Six channels unreachable in a Steam-only workflow.** `readConfig`, `getGameOverride`,
`getGameSdl` (all three Legendary/Epic), `getAvailableCyberpunkMods`, `setCyberpunkModConfig` (both
GOG — and `setCyberpunkModConfig` has **zero** frontend callers at all), and `addNewApp` (sideload).
Ported at parity per D-08 so the Phase 35 cutover gate closes with no residue, not because they are
exercised by GameLib's current Steam-first workflow.

---

## 4. Declared divergences from the Electron build

**`getGameSettings` and the already-ported `requestGameSettings` now BOTH exist on the sidecar as
near-duplicates.** This is Electron's own duplication (both end at
`getGame(appName).getSettings()`), not introduced here. `requestGameSettings`
(`settingsFlowRegistration.ts:113`, ported Phase 31) carries a `steamLibrary.has(appName)`
bottle-launch fix `getGameSettings` does not have (D-09) — a shipped, hardware-proven fix for a real
bug, and this slice deliberately does not touch it. `getGameSettings` was ported at parity, unmodified,
alongside it. **State plainly: two settings reads must not both survive into the Electron-free build
by accident.** Deduping the two is a recorded Phase 35 cutover cleanup — see §5.

---

## 5. Re-deferrals

- **The `getGameSettings`/`requestGameSettings` dedupe** (§4, D-09) → **Phase 35 cutover.**
- **Hoisting `getCrossoverIndex`'s per-game `loadIndex`/`buildMaps` out of the loop.** A real
  inefficiency (`buildCrossoverRatingMap()` fans out over every game in every library manager and
  calls both functions per game) that D-10's timeout exemption works around rather than fixes →
  Phase 35 or a small follow-up.
- **The ~100 remaining `i18next.t()` call sites** that D-02 makes work but this slice does not
  exercise. A spot-check of the Phase 30/32 paths that were silently emitting `undefined` —
  `downloadqueue.ts`, `utils.ts`, `storeManagers/steam/library.ts`, `steam/depotErrors.ts`,
  `storeManagerCommon/games.ts` — belongs in Phase 35 or a small follow-up.
- **The `tools/dxmt.ts:148` / `wine/manager/ipc_handler.ts:61` `releasesInfoReady` consumers**, which
  D-07 un-starves as a side effect but whose channels belong to slices 6/8, not this slice.

**This slice makes NO reassignment.** The inventory needed no edit this slice, unlike 34.1's D-14
Wine-tooling reassignment — `.planning/IPC-PORT-INVENTORY.md`'s Phase 34.2 heading is unchanged.

**Findings still open after gap cycle 1** (named by review ID so a later reader can look each one
up; none of these are closed by this document — they are accepted debt for this cycle, not silently
dropped):

- **WR-03** — `knownFixes.ts`'s stated extraction rationale is factually false as of this slice (the
  docstring's justification no longer matches why the file exists post-gap-cycle).
- **WR-05** — `bootstrap.ts` signals `READY_SENTINEL` before `i18next.init()`'s async resource-load
  promise has settled; a request racing that window could still observe an uninitialized `i18next.t()`.
- **WR-06** — `readKnownFixes` builds a filesystem path from unvalidated renderer input using `join`,
  not `resolve`+`relative` containment (this project's own established convention, per the
  "join is not containment" lesson) — tracked accepted debt, a real path-traversal finding
  deliberately deferred, not fixed by this cycle.
- **WR-07** — `getCrossoverIndex` is now unbounded (per its `LONG_RUNNING_CHANNELS` exemption above)
  with no cancellation mechanism, on top of the already-known per-game `loadIndex`/`buildMaps`
  rebuild inefficiency this section already re-defers.
- **WR-10** — test-harness boilerplate (the `pathShim` mock + containment guard shape from plan
  34.2-10) is duplicated across four suites rather than hoisted into a shared helper, and the
  gamedetails electron import gate is effectively implemented twice (depth-1 regex gate plus the
  transitive `electronReachLedger` walk) with no single canonical entry point documented.
- **IN-01** — a comment in the sidecar `handlers.ts` registration wiring undercounts the channels
  `registerGameDetailsFlows()` actually registers.
- **IN-02** — a pre-existing `repair` bug (the Steam-runner stub behavior, §3 rider 1) is now pinned
  as "correct" by a new test, which could mislead a future reader into treating the stub as intended
  behavior rather than an upstream gap.
- **IN-03** — `src-tauri/src/main.rs`'s `LONG_RUNNING_CHANNELS` doc comment has grown to 28 lines of
  accumulated phase history and is due for a trim/restructure, not a correctness issue.
- **IN-04** — `enrichmentFlows.test.ts` leaves a real `rustInvoke` frame on the process stdout after
  running, a test-hygiene loose end rather than a production defect.

All nine are explicitly out of scope for this gap cycle and already logged in this phase's
`deferred-items.md`.

---

## 6. Sign-off — the honest statement

**State the difference plainly rather than copy 34.1's wording.** 34.1's headline deliverable was
visual and interactive — the frameless toggle, the titlebar buttons, the tray appearing and changing
colour, gamepad navigation, macOS traffic lights vs custom buttons — and jest structurally could not
see any of it, so "wired and unit-proven" was a weak claim there. **This slice is almost entirely
data-in/data-out: 26 channels with assertable return shapes, exercised over the real sidecar RPC
loop, so "wired and unit-proven" is genuinely close to "works" here.** Every row above is backed by a
test that drives the real extracted body (or already-clean module) over the real transport and
asserts a real return value — not a mock's echo.

That stronger claim has **exactly two honest exceptions**, and they are not softened:

- **D-02.** Tests prove `i18next.t()` returns a real string after `init()`, and that `repair`'s notify
  body reflects it over the real sidecar RPC loop. Nobody has booted the Tauri build and seen a
  translated notification render, and the packaged build's locale-path resolution (the
  `publicdir-getapppath-chunking` gotcha family) is unproven by construction — jest runs from the
  repo root, where `process.cwd()` happens to resolve `publicDir` correctly.
- **D-07.** Tests prove the event fires and the anticheat data file is written, under a tmp home with
  a mocked axios. Nobody has booted the Tauri build and confirmed a real GitHub fetch lands
  `areweanticheatyet.json` in the real `appFolder` across a real network after a real connectivity
  transition.

**Do not read "wired and unit-proven" as "seen working" — that is exactly how G-30-02 was declared fixed twice while the live build hung.**

Wherever a row's automated proof covers only wiring and not
observable behavior, that is stated on the row (see §3 riders 1–2, and the two bootstrap-wiring
entries in §2). Read this document together with `34.2-HUMAN-UAT.md` — neither artifact should be
read in isolation, because either one alone could leave a later reader with the wrong impression.

### What gap cycle 1 changed about the honesty of this sign-off

- **The curated-import discipline proves DIRECT-import freedom, not transitive.** Earlier wording in
  `dispatch.ts`'s docstring, `enrichmentFlowRegistration.ts`'s docstring, and `REQUIREMENTS.md`
  REQ-34.2-03 claimed a transitive electron-freedom property the code does not have — real two-hop
  edges exist (`dispatch.ts` → `dialog/dialog.ts` → `electron`; `enrichmentFlowRegistration.ts` →
  `storeSearch/cheapshark.ts` → `electron`) that the existing depth-1 `gameDetailsImportGate.test.ts`
  gates cannot see. Plan 34.2-11 corrected all three sites to state the true, enforced invariant: no
  DIRECT import of `electron`/`backend/ipc`/`../ipc`/`../launcher`/`main_window` from this slice's
  gated modules. Transitive reach to `electron` **exists, is expected, and is rescued at runtime** by
  `electronStub.ts`'s `Module._load` interception — the sidecar's real safety mechanism, not the
  import discipline itself.
- **The measured, enforced set now lives in `src/backend/sidecar/__tests__/electronReachLedger.test.ts`.**
  Built with the TypeScript compiler API (`ts.resolveModuleName` against this repo's own
  `tsconfig.json`), it walks the real transitive import graph from this slice's four gated entry
  points and commits a growth-only baseline: **29 electron-importing modules**, out of **192**
  first-party files reachable in total. The committed baseline IS the Phase 35 cutover work-list —
  every module Phase 35 must decouple from `electron` (or leave behind an `electronStub` rescue for)
  is named in that file, not estimated in prose here.
- **Two proof surfaces this document implicitly leaned on asserted nothing, and were replaced.**
  WR-01: `gameDetailsImportGate.test.ts`'s Gate 7/8 do-not-touch checks compared the working tree to
  `git show HEAD:<same path>` — unconditionally true on any clean checkout, so they had protected
  nothing since the moment 34.2 was committed; replaced with committed sha256 digest pins plus
  semantic Layer-2 pins. WR-04: `gameDetailsModules.test.ts`'s `getLaunchOptions` default-label test
  compared two calls to the same uninitialized `i18next.t()` (both resolving `undefined`, satisfying
  `toEqual` vacuously); replaced with an assertion against a really-initialized i18next singleton read
  from `public/locales/en/gamepage.json`, explicitly rejecting both `undefined` and the raw key. A
  later reader should not over-trust proof levels this document recorded before gap cycle 1 found
  these two — the `unit` proof-level label in §2 was true of the extraction/registration, not
  necessarily of every assertion's ability to fail.

### How gap cycle 1 touches the two deferred HUMAN-UAT items (recorded, not resolved)

Neither deferred live-UAT item in `34.2-HUMAN-UAT.md` is closed by this gap cycle. `34.2-HUMAN-UAT.md`
itself is unmodified by this plan — both items keep `result: pending` and `status: deferred`.

- **UAT-34.2-01** (a translated notification actually renders in a live Tauri window): the *unit*
  proof is now STRONGER — plan 34.2-12 replaced the vacuous default-label assertion with one that
  compares against the real on-disk `en/gamepage.json` value and explicitly rejects both `undefined`
  and the raw `'launch.default'` key. This closes the specific blind spot where a previously-passing
  test could not have caught a regression. It does NOT touch what the UAT item actually tests: a live
  render in a running Tauri window, and the packaged build's locale-path resolution (the
  `publicdir-getapppath-chunking` gotcha family) remain unproven by construction — jest runs from the
  repo root, where `process.cwd()` happens to resolve correctly. Still deferred.
- **UAT-34.2-02** (the anticheat data file actually appears on disk via a real network fetch): plan
  34.2-09 changed the FAILURE behavior on this path — a filesystem fault on the local anticheat cache
  file now logs a warning (`sidecarRejectionGuard.test.ts` proves zero `unhandledRejection` events)
  instead of, in the worst case, taking down the whole sidecar process. The SUCCESS path — a real
  GitHub fetch landing `areweanticheatyet.json` under a real `appFolder` across a real network after a
  real connectivity transition — is unchanged, so the UAT item's expected observation is unchanged.
  A tester should know that a silent absence of the file is now a logged warning rather than a dead
  backend if the fetch fails. Still deferred.

### What gap cycles 2 and 3 changed about the honesty of this sign-off

- **Gap cycle 2** genuinely closed the three blockers gap cycle 1's own closure work had
  introduced (CR-01/CR-02/CR-03 of `34.2-REVIEW-GAP-CYCLE-2.md`'s predecessor round): `logError` is
  registered and reachable from a real production path with a positive side-effect proof (a marker
  string read back from the real log file, not absence-of-throw); `processGuards.ts`'s message
  construction moved inside its own `try` with a hardcoded fallback, so a hostile rejection reason
  can no longer crash the sidecar; `sidecarRejectionGuard.test.ts` regained the `pathShim`
  containment mock its siblings had. **But gap cycle 2's own new artifact — `testContainment.test.ts`
  — shipped a defect at the same time it fixed those three:** it declared an 11-suite containment
  hole as "accepted debt" rather than closing it, and one of those 11 (`bootstrap.test.ts`) was
  independently reproduced, three separate times across two independent reviewers, destroying the
  real developer's `~/Library/Logs/GameLib/gamelib.log`/`.log.old` on every invocation of this
  project's own designated test commands. That defect is what gap cycle 3 exists to close.
- **Gap cycle 3** closes it structurally rather than by enumeration: `src/backend/jest.setupContainment.ts`,
  wired into the backend jest project's `setupFiles`, redirects `os.homedir()` (via a narrow
  `jest.mock('os', ...)` overriding only `homedir`) plus eight HOME/XDG/APPDATA/LOCALAPPDATA
  environment variables for every suite under `src/backend`, before any suite's own imports run — a
  new suite added tomorrow with zero containment code of its own is contained by construction, not
  by opting in. `KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES` — the 11-suite declared-debt list — no
  longer exists in `testContainment.test.ts`; it was replaced with a `readdirSync`-derived
  set-equality tripwire proving every `*.test.ts` file in the directory is classified. The live
  `stat` evidence in §7's gap-cycle-3 subsection below is the direct refutation of the destruction
  finding, not a restatement of it.
- **Net honesty change:** the sign-off can now state, without a carve-out, that running this
  project's own designated backend test commands does not destroy real developer data — a claim
  that was FALSE at the end of gap cycle 2 despite that cycle's own new test-safety tooling
  existing specifically to prove it. REQ-34.2-07/-14's "additive/reversible, tests don't clobber the
  developer's machine" component is now genuinely true, not merely wired-and-documented-as-debt.

### What gap cycle 4 changed about the honesty of this sign-off

Each of the last three cycles fixed its named gap and introduced a new one, and in each case the
cycle's own new tests were structurally incapable of detecting the shortfall — cycle 1 introduced
CR-01/CR-02/CR-03 of `34.2-REVIEW-GAP-CYCLE-2.md`'s predecessor round; cycle 2's own new artifact
(`testContainment.test.ts`) shipped an 11-suite accepted-debt hole that let `bootstrap.test.ts`
destroy the real developer's log three times; cycle 3's own new artifacts
(`loggerFlowRegistration.ts`'s WR-02 fix, `structuralContainment.test.ts`'s `os` mock,
`testContainment.test.ts`'s Block D/Block C gates, `longRunningChannels.test.ts`'s presence gate,
`repairFailure.ts`'s signal wrapping) shipped CR-01 (an inert `.catch` guard, "proven" by tests
that stubbed the function under test), CR-02 (a specifier-dependent containment claiming to be
structural), and ten warnings — three of them (WR-01, WR-02, WR-05) gate-quality defects inside
the very gates written to prevent gate-quality defects.

Cycle 4 did the following differently, and this is the discipline a fifth cycle should repeat
mechanically rather than re-derive:

1. **Every fix was preceded by a proof observed RED against the then-current code.** CR-01, CR-02
   (both instances), WR-01, WR-02, WR-04, WR-05 and WR-06 each have a verbatim RED transcript
   quoted in §7 below, reproduced from the closing plan's own SUMMARY.md, not asserted from memory
   of what the review said.
2. **No proof stubs the function under test.** `loggerCallSiteGuard.test.ts` (CR-01's replacement
   for the four gap-cycle-3 WR-02 tests, which had `jest.spyOn`-replaced `logger.logError` with a
   promise-returning shape that never occurs at runtime) drives the real, unmocked
   `backend/logger` module through a real `ENOTDIR` failure and a real synchronous throw — the
   same standard the review's own CR-01 finding demanded ("A test that must stub the function
   under test to observe the behaviour under test is not evidence").
3. **Every corrected gate carries a self-test proving it rejects the input it previously
   accepted.** WR-01's `assignsContainmentEnvVar` is self-tested against a synthetic docblock-only
   occurrence; WR-02's `hasContainmentOsMock` is self-tested by commenting out the real
   `jest.mock('node:os', ...)` registration (which, as a bonus, produced an even stronger failure
   — the whole suite refusing to run via WR-10's precondition, not merely one gate assertion);
   WR-04's `hasBehavioralRustTestModule` is self-tested against the review's own literal
   comment-only counter-example; WR-05's two Block C self-tests were rebuilt on fully synthetic
   input arrays instead of the live directory listing they previously depended on.

Three deliberate, narrower-than-literal remedies are called out explicitly in §7 below rather than
glossed as "closed per the review's suggestion": CR-01's `logErrorSettled` sibling export (not a
conversion of the shared `logError` wrapper — see D4-DEF-01), CR-02's secondary `jest.config.js`
instance (closed separately from the containment module itself, by plan 34.2-29's Block E), and
WR-11's anti-vacuity anchor (the per-run `mkdtemp` containment root plus a pre-mock
`realHomeAtSetup` capture, not a guarded `userInfo()` call as the review proposed).

Not every finding produced a distinguishable RED/GREEN pair. WR-07 (predictable containment root),
WR-09 (dead `containmentRoot` export) and WR-12 (the `XDG_DATA_HOME` comment's understated blast
radius) are direct hardening/documentation fixes with no prior test that could fail on the pre-fix
state — recorded honestly as such in §7, not dressed up with a RED transcript that does not exist.

The two remaining deferred HUMAN-UAT items (D-11: `UAT-34.2-01`, `UAT-34.2-02`) remain deferred and
still do not gate closure; cycle 4 did not touch either.

---

## 7. Reconciliation

The channel count is **26**, matching `.planning/IPC-PORT-INVENTORY.md`'s
`## Phase 34.2 — Slice 5 — game details, settings and overrides (26 channels)` heading exactly — set
equality, not just count equality: every name in that heading's channel list appears exactly once in
section 2's table above, and no row in section 2 names a channel absent from it. **No inventory edit
was needed** (D-08: port all 26 at parity, no reassignments) — unlike 34.1's D-14 reassignment, which
required a two-list correction.

No drift was found between the plans' declared intent and what the six SUMMARY.md files record as
actually shipped. Consulted: `34.2-01-SUMMARY.md`, `34.2-02-SUMMARY.md`, `34.2-03-SUMMARY.md`,
`34.2-04-SUMMARY.md`, `34.2-05-SUMMARY.md`, `34.2-06-SUMMARY.md`.

- **34.2-01** shipped both bootstrap wirings (i18next init, `fetchLastestReleases()` +
  re-homed `releasesInfoReady` listener) exactly as CONTEXT.md D-02/D-07 specified, proven by a
  7-test non-mocked suite (`bootstrapWirings.test.ts`) — no divergence.
- **34.2-02** extracted 15 handler bodies into `gamedetails/dispatch.ts` (including
  `changeGameVersionPinnedStatus` and `addNewApp`, which ship as `send`-kind channels registered by
  plan 34.2-05 — dispatch.ts's own body count is 15, not the same 15 as the 34.2-04 invoke
  registration list) plus `setGameMetadataOverride` into `gamedetails/overrides.ts` — exactly as
  planned. `getGameMetadataOverride`/`getAllGameOverrides` were deliberately left unextracted
  (already-clean `game_overrides/index.ts` pass-throughs) — a documented, non-drift decision, not a
  gap.
- **34.2-03** extracted `readKnownFixes`→`knownFixes.ts` (D-05) and
  `buildCrossoverRatingMap`→`crossoverRatingMap.ts` (D-06) exactly as planned, with one auto-fixed
  blocking issue (a second, plan-undocumented `main.ts` import site for
  `buildCrossoverRatingMap`) — a build-correctness fix, not a scope or behavior divergence.
- **34.2-04** registered all 15 invoke-kind channels exactly as the plan's `<interfaces>` table
  specified, with `requestGameSettings` deliberately left unregistered a second time (D-09) — no
  divergence.
- **34.2-05** registered all 3 send-kind channels exactly as planned, explicitly reporting "None -
  plan executed exactly as written" in its own SUMMARY — no divergence.
- **34.2-06** registered all 8 enrichment channels exactly as planned; the D-10 timeout-exemption
  question was settled by a real measurement (`getWikiGameInfo` NOT exempted, `getCrossoverIndex`
  exempted) exactly as CONTEXT.md's "measure first" instruction required — no divergence.

The only channel named anywhere in this document but not among the 26 ported rows in §2 is none —
every channel named in §3/§4/§5 (the Steam stubs, `requestGameSettings`, the re-deferred
`releasesInfoReady` consumers) is either already one of the 26 rows or explicitly named as belonging
to a different slice/phase, never presented as part of this slice's 26.

### Gap cycle 1 reconciliation (plans 34.2-08..34.2-14)

The 34.2-01..06 no-drift statements above remain accurate for what those six plans shipped and are
unchanged by this subsection. Gap cycle 1 ran seven further plans against verification gaps and
code-review findings found afterward; none of them added, removed, or re-kinded a channel, so the
26-channel set equality against `.planning/IPC-PORT-INVENTORY.md` still holds and no inventory edit
was needed by this cycle either.

- **34.2-08** — closed verification gap #1 / code-review CR-01 (REQ-34.2-12): exempted `repair` and
  `readConfig` from the sidecar's 60s bounded invoke timeout and caught the renderer's floating
  `repair()` call site.
- **34.2-09** — closed verification gap #2 / code-review CR-02 (REQ-34.2-07): attached a `.catch()` to
  the `downloadAntiCheatData` call inside `bootstrap.ts`'s `releasesInfoReady` listener and added a
  log-only process-level `unhandledRejection` guard (`processGuards.ts`).
- **34.2-10** — closed code-review CR-03 and WR-08: redirected `enrichmentFlows.test.ts` and
  `gameDetailsFlows.test.ts`'s destructive test config paths via a `pathShim.getPath()` mock plus a
  `resolve`+`relative` containment guard, and re-armed a dead `online_monitor` mock. No REQ ID claimed
  by this plan, by design — a test-safety fix, not new requirement coverage.
- **34.2-11** — closed verification gap #3 / code-review WR-02 (REQ-34.2-03): corrected the false
  transitive-electron-freedom claim at three sites and committed `electronReachLedger.test.ts`'s
  measured, growth-only baseline.
- **34.2-12** — closed code-review WR-01 and WR-04: replaced two assertions that could never fail
  (a tautological HEAD-comparison do-not-touch gate and a vacuous uninitialized-`i18next.t()`
  comparison) with a committed sha256 digest pin and a real-i18next-backed assertion.
- **34.2-13** — closed code-review WR-09 (REQ-34.2-11): extracted the three `storeSearch` D-14
  rethrow-contract handler bodies into `storeSearch/handlers.ts`, imported by both the Electron and
  Tauri sidecar registration sites, replacing a hand-copied duplicate.
- **34.2-14** (this plan) — closed the currency gap named in `34.2-VERIFICATION.md` truth row 13:
  brought this document current as of the end of gap cycle 1, recording what plans 34.2-08..13
  actually shipped (verified against source and each plan's SUMMARY, not against plan intent) and
  naming the findings gap cycle 1 deliberately left open (§5: WR-03, WR-05, WR-06, WR-07, WR-10,
  IN-01..04).

**Still true after this cycle:** the 26-channel set equality against `.planning/IPC-PORT-INVENTORY.md`
holds, and no inventory edit was needed — gap cycle 1's changes were entirely to timeout policy,
crash-safety, test quality, docstring accuracy, and shared-code extraction, never to the channel
count, a channel's kind, or a channel's name.

### Gap cycle 2 reconciliation (plan 34.2-16 — CR-01 sidecar half)

Gap cycle 1's plan 34.2-08 (§7 above) fixed the `repair`/`readConfig` timeout exemption and gave
`onRepairYesClick`'s renderer catch a destination: `window.api.logError`. That destination was, at
the time, completely unregistered on the sidecar — `logError` is an
`.planning/IPC-PORT-INVENTORY.md` **Phase 34.3 / slice 6** channel (`## Phase 34.3 — Slice 6 —
shell, files, logs and diagnostics`), not one of this slice's 26, and nothing under
`src/backend/sidecar/` registered it before this plan. Under Tauri, an unregistered `send` channel
is a total, silent no-op (`dispatchSend`, `sidecarRpc.ts:138-150`, looks up an empty listener array
and does nothing — no reject, no timeout, no `UNPORTED_CHANNEL_MARKER`, no console output, no log
line; this project's own `sidecar-send-channels-fail-silently` / G-30-01 failure class). Gap cycle
1's own fix therefore routed a real repair failure into exactly that void — worse than the
unhandled rejection it replaced.

Plan 34.2-16 closes this (verification gap #1 / code-review CR-01 / REQ-34.2-12):

- **What was ported:** `logError` only, via a new curated module,
  `src/backend/sidecar/loggerFlowRegistration.ts`, wired into `handlers.ts` alongside the slice's
  other curated registrations, before `ensureStoresRegistered()`. Port kind: **`sidecar send`**
  (`ipcMain.on`, fire-and-forget) — mirrors `logger/ipc_handler.ts:15`'s own
  `addListener('logError', (e, message) => logError(message, LogPrefix.Frontend))` exactly.
- **This is an out-of-slice early port, not a scope grab, and does NOT change the headline
  26-channel tally for slice 5.** The 26-channel set equality against
  `.planning/IPC-PORT-INVENTORY.md`'s `## Phase 34.2 — Slice 5` heading (§7 above) is unaffected —
  `logError` is not, and has never been, one of those 26 rows. It is now recorded in the inventory's
  "Already ported" list instead, annotated "ported early by Phase 34.2 gap cycle 2 (plan 34.2-16)"
  (see the inventory edit itself for the reconciled counts).
- **Proof level:** `unit`, but of the honest kind REQ-34.2-08/09 requires for a `send` channel — a
  positive side effect, not an absence-of-throw. `loggerFlows.test.ts` writes a real `logError` send
  frame carrying a unique marker string over the real, unmocked sidecar RPC transport, then reads
  the marker back out of the real log file the real, unmocked `backend/logger`/`LogWriter` code
  wrote it to (redirected under a disposable `os.tmpdir()` root by a four-part containment kit —
  `os`, `pathShim`, and `backend/logger/paths` mocks plus a `resolve`/`relative` tripwire). It also
  asserts the real `backend/logger` `logError` export (via `jest.spyOn`, never `jest.mock` — the
  `logger`/`log_writer.ts` circular-require precedent `sidecarRejectionGuard.test.ts` already
  documented) was called with the frame's message and `LogPrefix.Frontend`, and that no response
  frame and no `UNPORTED_CHANNEL_MARKER` ever appear for a `send`-kind id. RED spot-checked by hand:
  temporarily removing the `registerLoggerFlows()` call from `handlers.ts` made the positive test
  fail by TIMEOUT (marker never appears), never by an exception — reproducing the silent-`send`
  failure class directly, then restored (`git diff` against the Task 1 commit confirmed empty).
- **Phase 34.3 must NOT register `logError` a second time.** `electronStub.ts`'s `listenerRegistry`
  holds an ARRAY per channel and `dispatchSend` iterates every listener in it — a second
  `ipcMain.on('logError', ...)` registration would duplicate every frontend log line, not merely be
  redundant. This is declared in three places: `loggerFlowRegistration.ts`'s own module docstring,
  `handlers.ts`'s registration-site comment, and this subsection — and enforced at test time by
  `loggerFlows.test.ts`'s `listenerRegistry.get('logError')?.length === 1` assertion.
- **Logging is still NOT ported for slice 6.** The other five channels
  `src/backend/logger/ipc_handler.ts` also registers — `logInfo`, `getLogContent`,
  `showLogFileInFolder`, `uploadLogFile`, `deleteUploadedLogFile`, `getUploadedLogFiles` — remain
  unported, Phase 34.3 work, unaffected by this plan. No claim in this document or in
  `.planning/IPC-PORT-INVENTORY.md` states or implies that logging as a whole is ported.

### Gap cycle 3 reconciliation (plans 34.2-19..34.2-23)

`34.2-REVIEW-GAP-CYCLE-2.md`'s re-verification (`34.2-VERIFICATION.md`, third verification of this
phase) found gap cycle 2 genuinely closed its own three named blockers but shipped a new one plus
three warnings inside its own new artifacts. Gap cycle 3 (five plans, 34.2-19..34.2-23) closed the
new blocker and seven of the eight warnings carried into this cycle's scope, then this plan
(34.2-24) reconciles the result. Consulted: `34.2-19-SUMMARY.md` through `34.2-23-SUMMARY.md`.

**What this cycle fixed, and where:**

- **CR-01** (the re-verification's own new critical: `testContainment.test.ts` declared an
  11-suite containment hole as accepted debt; `bootstrap.test.ts` independently reproduced
  destroying the real developer's `gamelib.log`/`.log.old` three times) — **closed by plan
  34.2-19**, `src/backend/jest.setupContainment.ts` (a `setupFiles` module) plus the deletion of
  the 11-suite declared-debt list in plan 34.2-23. RED proof (34.2-19 Task 2, `setupFiles` entry
  disabled): `structuralContainment.test.ts` failed 5 of 6 tests, e.g. `Test 1: os.homedir()
  resolves inside os.tmpdir() with zero jest.mock calls` — `expect(received).toBe(expected) //
  Object.is equality — Expected: false, Received: true (assertContained: rel.startsWith('..') was
  true — os.homedir() resolved OUTSIDE os.tmpdir())`, and the anti-vacuity Test 6 received the
  literal real path `/Users/graysonmitchell/Library/Logs/GameLib/gamelib.log`. Restored, all green.
- **WR-01** (the "no longer claims NO FILESYSTEM WRITES" gate matched comment-stripped source, so
  the claim — which can only ever live in a stripped docblock line — could never be caught) —
  **closed by plan 34.2-23 Task 1**, `testContainment.test.ts` now matches RAW source. RED proof
  (RED-PROOF 1, `34.2-23-SUMMARY.md`): a synthetic claim injected into `enrichmentFlows.test.ts`'s
  docblock made `REQ-34.2-07/-14: enrichmentFlows.test.ts no longer claims "NO FILESYSTEM WRITES",
  matched on RAW source (WR-01)` fail with `expect(received).not.toContain(expected) — Expected
  substring: not "NO FILESYSTEM WRITES"`. Reverted, `git diff --exit-code` clean.
- **WR-02** (the new `logError` send listener leaked a floating promise; a real log-write failure
  would become an unhandled rejection absorbed only by the process guard, violating that guard's
  own "not a substitute for call-site handling" invariant) — **closed by plan 34.2-20**,
  `loggerFlowRegistration.ts` now wraps its `logError(...)` call in `Promise.resolve(...).catch(...)`
  with a module-attributed stderr diagnostic. RED proof (34.2-20 Task 2, pre-fix code restored via
  `git show HEAD~1:...`): `Tests: 4 failed, 5 passed, 9 total` — Test A `expect(received).toBeDefined()
  — Received: undefined` (no call-site diagnostic ever appeared because pre-fix code had no
  `.catch()` at all); Tests B/C/D the hostile/marker rejections themselves escaped as uncaught
  values inside the test process. Restored, 9/9 green.
- **WR-03** (`repairFailure.ts` interpolated a raw `error: unknown` into a template literal — the
  identical `${unknown}` defect class plan 34.2-15 removed from `processGuards.ts` — throwing for
  null-prototype/throwing-`toString` reasons and skipping the user-visible ERROR dialog) — **closed
  by plan 34.2-21**, `reportRepairFailure` now precomputes `errorText` via a `let`-fallback-before-try
  and wraps each of its three signals independently. RED proof (34.2-21 Task 1, against unmodified
  `repairFailure.ts`): `Tests: 10 failed, 4 passed, 14 total` —
  `TypeError: Cannot convert object to primitive value` at
  `window.api.logError(\`repair failed for ${appName}: ${error}\`)`, `repairFailure.ts:45:56`, for
  all three hostile shapes (`Object.create(null)`, throwing `toString`, throwing
  `Symbol.toPrimitive`). Fixed, 14/14 green.
- **WR-04** (`testContainment.test.ts` mislabelled the `backend/constants/environment` mock as
  "included for parity" when it is actually LOAD-BEARING for `getBaseLogPath()`'s `isMac`/`isWindows`
  branch — a future reader trusting the comment could remove the mock and make RED-PROOF-2 silently
  vacuous) — **closed by plan 34.2-23 Task 1**, the comment now states plainly that pinning
  `isMac:false` is what forces the XDG_STATE_HOME branch and makes RED-PROOF-2 non-vacuous on a
  darwin host.
- **WR-07** (Block A's `process.platform`/env-var adversarial mutation lived in `beforeAll`/`afterAll`
  — the exact worker-leak-under-force-exit pattern this repo's own `tests-clobbering-real-steam-store`
  lesson and the file's own docstrings warn against) — **closed by plan 34.2-23 Task 1**, moved into
  a per-test `withAdversarialPlatformAndEnv()` helper restoring in its own `finally`.
- **WR-08** (`IN_SCOPE_SUITES` was a fixed 4-entry declared list — a new uncontained suite would be
  invisible to the gate, the exact failure mode CR-01 above already demonstrated) — **closed by
  plan 34.2-23 Task 2**, a `readdirSync`-derived set-equality tripwire (`diffSuiteClassification`)
  now proves every `*.test.ts` file in the directory is classified by exactly one of
  `STRUCTURALLY_CONTAINED_SUITES` or the (now much shorter) declared-uncovered list. RED proof
  (RED-PROOF 2, `34.2-23-SUMMARY.md`): a real placeholder file `zzTripwireProbe.test.ts` made the
  set-equality assertion fail — `"unclassified": Array ["zzTripwireProbe.test.ts"]`. Deleted, 42/42
  green.
- **IN-02** (`ReportRepairFailureOptions` exported but used only within its own module) —
  **addressed by plan 34.2-21 Task 2**, dropped `export` after confirming zero external consumers
  via grep.
- **IN-05** (unchecked `args[0] as string` cast in the `logError` listener, narrower than the
  declared `unknown` transport contract) — **closed by plan 34.2-20 Task 1**, the cast dropped in
  favor of passing `args[0]` through as `unknown`.
- **Carried-forward: no Rust test proved `timeout_for()` consults `LONG_RUNNING_CHANNELS`** —
  **closed by plan 34.2-22**, a `#[cfg(test)]` module in `src-tauri/src/main.rs` (6 tests),
  bidirectionally falsifiable and pinned from the jest side (`longRunningChannels.test.ts`, since
  this project's CI runs no `cargo test` step). RED proof (34.2-22 Task 1, `timeout_for` stubbed to
  unconditional `Some(INVOKE_TIMEOUT)`): `test result: FAILED. 2 passed; 4 failed` — e.g.
  `tests::get_crossover_index_is_exempt` panicked, `left: Some(60s) right: None`. Stubbed to
  unconditional `None` instead: a DIFFERENT `2 passed; 4 failed` split. Restored, `git diff --stat`
  purely additive against the pre-plan baseline.

**Containment is now structural, not a per-suite opt-in.** `src/backend/jest.setupContainment.ts`,
registered via the backend jest project's `setupFiles` entry in `src/backend/jest.config.js`,
redirects `os.homedir()` (a narrow `jest.mock('os', ...)` overriding only `homedir`, everything else
real via `jest.requireActual`) plus `HOME`/`USERPROFILE`/`APPDATA`/`LOCALAPPDATA`/`XDG_CONFIG_HOME`/
`XDG_STATE_HOME`/`XDG_DATA_HOME`/`XDG_CACHE_HOME`, for every suite under `src/backend`, before any
suite's own imports run. The previous cycle's `KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES` declaration
is gone from `testContainment.test.ts` because the hole it described is closed by construction, not
because the declaration was inconvenient to carry — a new suite added tomorrow with zero containment
code of its own is contained the same way every existing suite now is, and `testContainment.test.ts`'s
own `readdirSync` tripwire (WR-08 above) proves no suite is invisible to that classification.

**The live evidence, verbatim (`34.2-19-SUMMARY.md`, "Live Destruction Check — final, with the
corrected `jest.mock('os', ...)` fix in place"):**

```
BEFORE (real ~/Library/Logs/GameLib):
Jul 26 11:42:53 2026 /Users/graysonmitchell/Library/Logs/GameLib/gamelib.log
Jul 26 11:42:53 2026 /Users/graysonmitchell/Library/Logs/GameLib/gamelib.log.old

npx jest --testPathPattern="src/backend/sidecar/__tests__"
Test Suites: 1 failed, 24 passed, 25 total
Tests:       1 failed, 389 passed, 390 total
(sole failure: rustInvokeChannel.test.ts, pre-existing 34.1-era baseline)

AFTER (real ~/Library/Logs/GameLib):
Jul 26 11:42:53 2026 /Users/graysonmitchell/Library/Logs/GameLib/gamelib.log
Jul 26 11:42:53 2026 /Users/graysonmitchell/Library/Logs/GameLib/gamelib.log.old
```

Both mtimes byte-identical before and after — the direct refutation of the verification's own
three-times-reproduced finding (10:49 → 10:56 → 10:57 rotation, `34.2-VERIFICATION.md` line 23).
This is the single most load-bearing statement in this section: the phase's own designated test
commands no longer destroy real developer data, independently confirmed, not merely asserted.

**What this cycle did NOT fix, and why:**

- **WR-05** (the containment tmp-root string duplicated three times per suite, inconsistently,
  across five files, with nothing asserting the three roots agree) — not fixed this cycle.
  Structural containment (the `setupFiles` mechanism above) demoted these per-suite mocks to
  defence in depth: a desync between them can no longer produce a write outside `os.tmpdir()`,
  because the project-wide `setupFiles` entry already redirects `os.homedir()` and the relevant
  env vars regardless of what any individual suite's own mocks say. Still worth fixing for
  legibility — tracked in `deferred-items.md`.
- **WR-06** (hoisted `jest.mock` factories referencing an out-of-scope `const`, surviving only
  because nothing calls `os.homedir()` during module evaluation) — not fixed this cycle. Same
  reasoning as WR-05; the fix is naturally bundled with it. Tracked in `deferred-items.md`.
- **IN-01** (prettier violations in five gap-cycle-2 files) — not fixed this cycle. `prettier
  --check` is red at baseline across dozens of pre-existing files in this repository, so this
  cycle neither regresses nor greens that gate; fixing only five changed files would not change
  the gate's overall outcome. Gap cycle 3's OWN new/modified files (`jest.setupContainment.ts`,
  `structuralContainment.test.ts`, `loggerFlowRegistration.ts`, `loggerFlows.test.ts`,
  `repairFailure.ts`, `repairFailure.test.ts`, `main.rs`, `longRunningChannels.test.ts`,
  `testContainment.test.ts`) are this cycle's own responsibility and were not flagged by any
  plan's own `prettier`/eslint acceptance criteria as newly red.
- **IN-03** (unrestored `jest.spyOn` in two `sidecarRejectionGuard.test.ts` harness helpers,
  benign because each runs inside a fresh `jest.isolateModules()` registry) — not fixed this
  cycle; out of scope for the five plans this cycle ran. Tracked in `deferred-items.md`.
- **IN-06** (`MaxListenersExceededWarning` from repeated `init()` calls attaching process-level
  listeners never removed) — not fixed this cycle; cosmetic in tests today, but the real fix
  belongs with `bootstrap.init()`'s own idempotency, not with a test file. Tracked in
  `deferred-items.md`.

**IN-04** (renderer-controlled log injection through the newly-ported `logError` channel) is
recorded as an ACCEPTED risk, not a deferral — see plan 34.2-20's threat model entry T-34.2-69 and
`deferred-items.md`: exact parity with the Electron handler (`src/backend/logger/ipc_handler.ts:15`),
so changing it here would be an undeclared behavioural divergence between the two builds, which
REQ-34.2-14 forbids.

**What this cycle did NOT touch:** the 26 ported channels. The set equality against
`.planning/IPC-PORT-INVENTORY.md`'s slice-5 heading is unchanged from the two prior gap cycles — no
channel was added, removed or re-kinded by any of plans 34.2-19..34.2-23, and all 14
requirement-level truths passed on their literal text in the third verification
(`34.2-VERIFICATION.md`, "Score: 14/14 requirement-level truths pass on their literal text"). This
cycle's work is entirely test-safety and error-path hardening — five plans, zero channel-table
edits, zero inventory edits.

**Still true after this cycle:** the 26-channel set equality against
`.planning/IPC-PORT-INVENTORY.md`'s slice-5 heading still holds — `logError` was never one of the
26 and is not counted as one now. The only inventory edit this cycle made was moving `logError` from
the Phase 34.3/slice-6 list to the "Already ported" list, with both affected counts adjusted in the
same edit (see the inventory document itself for the reconciled arithmetic).

### Gap cycle 4 reconciliation (plans 34.2-25..34.2-29)

`34.2-REVIEW-GAP-CYCLE-3.md` (reviewing what gap cycle 3 shipped) found 2 critical + 12 warning
findings — the review's frontmatter undercounts them as `warning: 11`; the body labels WR-01
through WR-12 and the body is authoritative, per this document's own established convention of
trusting body over frontmatter. All fourteen are accounted for below, each with its closing plan,
the file(s) touched, and the specific evidence — quoted verbatim from the closing plan's own
SUMMARY.md, not asserted.

**CR-01 — the WR-02 call-site rejection guard was inert; `logError()` returns `undefined`.**
Closed by **plan 34.2-26**. Files: `src/backend/logger/index.ts` (new `logErrorSettled` sibling
export), `src/backend/sidecar/loggerFlowRegistration.ts` (call site now imports
`logErrorSettled`, wrapped in `try`/`catch` for the synchronous-throw path),
`src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts` (new, stub-free proof suite),
`src/backend/sidecar/__tests__/loggerFlows.test.ts` (the four spy-fabricated gap-cycle-2 WR-02
tests deleted). RED proof (Task 1, verbatim, all four contracts failing against HEAD):

```
FAIL Backend src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts
  loggerCallSiteGuard (Phase 34.2 gap cycle 4, plan 34.2-26 — CR-01 / REQ-34.2-12 / REQ-34.2-14)
    ✕ Test A (async rejection, real module): a real ENOTDIR log-write rejection produces a call-site-attributed stderr diagnostic (280 ms)
    ✕ Test B (synchronous throw): an unassigned writer does not escape the listener, and still writes a diagnostic (209 ms)
    ✕ Test C (contract, runtime): backend/logger exports a promise-returning error-logging entry point (1 ms)
    ✕ Test D (contract, source gate): logger/index.ts declares the wrapper with an EXPRESSION body (1 ms)
Test Suites: 1 failed, 1 total
Tests:       4 failed, 1 passed, 5 total
```

Deliberate-break confirmation (Task 2, reverting `logErrorSettled` to a block body): the whole node
process crashed with an uncaught `ENOTDIR` rejection rather than a clean test failure — a STRONGER
failure signal than the pre-fix state, confirming the fix is load-bearing. **See point (a) below
for why the remedy is narrower than the review's literal prescription.**

**CR-02 — structural containment is specifier-dependent (`jest.mock('os')` misses `node:os`) — two
instances.** Primary instance closed by **plan 34.2-25**; secondary instance closed by **plan
34.2-29** (Block E). Files: `src/backend/jest.setupContainment.ts`,
`src/backend/sidecar/__tests__/structuralContainment.test.ts`,
`src/backend/sidecar/__tests__/testContainment.test.ts`. RED proof (34.2-25 Task 1, against
unmodified `jest.setupContainment.ts`):

```
FAIL Backend src/backend/sidecar/__tests__/structuralContainment.test.ts
    ✕ Test 7: require('node:os').homedir() resolves identically to require('os').homedir()
    ✕ Test 8: os.userInfo().homedir is redirected identically to os.homedir()
  ● Test 7: Expected: ".../T/gamelib-jest-home-49492"  Received: "/Users/graysonmitchell"
  ● Test 8: Expected: ".../T/gamelib-jest-home-49492"  Received: "/Users/graysonmitchell"
Test Suites: 1 failed, 1 total
Tests:       2 failed, 10 passed, 12 total
```

Fix: a hoisted `mockOsFactory` registered against BOTH `jest.mock('os', ...)` and
`jest.mock('node:os', ...)`. Secondary instance deliberate-break proof (34.2-29 Block E):
temporarily added `import { getLogFilePath } from 'backend/logger/paths'` to
`src/common/types/__tests__/storePolicy.test.ts` (outside `src/backend`):

```
✕ Block E ... no frontend/common/preload/meta *.test.ts(x) file imports a containment-relevant backend module
  + Array [ Object { "file": "src/common/types/__tests__/storePolicy.test.ts", "specifier": "backend/logger/paths" } ]
Tests:       1 failed, 51 passed, 52 total
```

**See point (b) below.**

**WR-01 — Block D's `HOME` env-var gate matched raw source; a docblock comment alone satisfied
it.** Closed by **plan 34.2-29** (Task 1). File:
`src/backend/sidecar/__tests__/testContainment.test.ts`. RED proof (self-test against the
not-yet-fixed predicate):

```
● Block D ... WR-01 self-test: a synthetic source whose only process.env.HOME = occurrence sits on a docblock line is rejected
  Expected: false
  Received: true
```

Fix: `assignsContainmentEnvVar` now matches `stripComments(source)`. Deliberate-break confirmation
(commented out the real `process.env.HOME = containmentRoot` assignment in
`jest.setupContainment.ts`): the gate correctly went red, naming the missing assignment.

**WR-02 — Block D did not gate the `os`/`node:os` `jest.mock` mechanism itself, only the eight
defence-in-depth env-var assignments.** Closed by **plan 34.2-29** (Task 1). File:
`src/backend/sidecar/__tests__/testContainment.test.ts`. Deliberate-break proof (commented out
`jest.mock('node:os', mockOsFactory)` in `jest.setupContainment.ts`): the result was STRONGER than
a single gate assertion failing — the whole suite failed to run, via the WR-10 `setupFiles`-time
precondition (added by 34.2-25) throwing first:

```
FAIL Backend src/backend/sidecar/__tests__/testContainment.test.ts
  ● Test suite failed to run
    [jest.setupContainment] REFUSING TO RUN: containment precondition failed --
    require('os').homedir()=.../gamelib-jest-home-fyVoM0, require('node:os').homedir()=/Users/graysonmitchell, ...
Test Suites: 1 failed, 1 total
Tests:       0 total
```

Fix: new `hasContainmentOsMock` predicate gates the `jest.mock('os'/'node:os', ...)` registration
plus the `homedir`/`userInfo` overrides directly.

**WR-03 — both production files (`loggerFlowRegistration.ts`, `repairFailure.ts`) regressed from
prettier-clean to prettier-dirty; CI runs `pnpm prettier`.** Closed in two halves: sidecar half by
**plan 34.2-26**, renderer half by **plan 34.2-27**. Fix: `npx prettier --write` on both files;
both plans' SUMMARYs record `npx prettier --check` exiting 0 afterward
(`loggerFlowRegistration.ts`/`logger/index.ts` per 34.2-26; `repairFailure.ts`/
`repairFailure.test.ts` per 34.2-27). No RED/GREEN test pair — this is a formatting-gate fix, not a
behavioral one; recorded honestly as such.

**WR-04 — the Rust-test presence gate counted `timeout_for` occurrences in COMMENTS, so a
zero-assertion test module could satisfy it.** Closed by **plan 34.2-28** (Task 1). File:
`src/backend/__tests__/longRunningChannels.test.ts`. RED proof (reverted
`hasBehavioralRustTestModule` to the pre-fix raw-text-count predicate):

```
● self-test: WR-04 counter-example — comment-only timeout_for mentions plus a zero-assertion loop does NOT match the gate
  Expected: false
  Received: true
Test Suites: 1 failed, 1 total
Tests:       1 failed, 17 skipped, 18 total
```

Second, live proof (deleted all 8 `assert_eq!(` lines from `main.rs`'s real `#[cfg(test)]` module,
loop/comments intact):

```
● the #[cfg(test)] region contains a real assert_eq! against timeout_for, not merely the identifier
    Expected substring: /assert_eq!\(\s*timeout_for\(/
● the full gate (all three conditions together) matches the real main.rs
    Expected: true  Received: false
Tests: 2 failed, 16 passed, 18 total
```

Fix: `hasBehavioralRustTestModule` now requires a real `assert_eq!(timeout_for(` match on
comment-stripped source, not a bare identifier count.

**WR-05 — Block C's two self-tests built their input from the LIVE `listActualTestSuites()`
directory listing, so a genuine failure produced two additional misleading self-test failures.**
Closed by **plan 34.2-29** (Task 2). File: `src/backend/sidecar/__tests__/testContainment.test.ts`.
Live proof (dropped a real `zzTripwireProbe2.test.ts` into the directory): pre-fix produced 3
failures (1 correct + 2 misleading self-test noise, per the review's own reproduction); post-fix:

```
Tests:       1 failed, 47 passed, 48 total
  ● Block C ... T-34.2-83: every *.test.ts file ... classified ...
    unclassified: Array [ "zzTripwireProbe2.test.ts" ]
```

Exactly ONE failure, correctly naming the probe. Fix: both self-tests now build `actual`/`declared`
from fully synthetic literal arrays instead of the live directory.

**WR-06 — `repairFailure.ts` silently swallowed a missing/throwing `window.api.logError` — the
exact silent-void class this phase exists to close.** Closed by **plan 34.2-27**. Files:
`src/frontend/screens/Game/GameSubMenu/repairFailure.ts`,
`src/frontend/screens/Game/GameSubMenu/__tests__/repairFailure.test.ts`. RED proof (Task 1, against
unmodified `repairFailure.ts`):

```
WR-06: individually-diagnosed signals, dialog survives a throwing t/showDialogModal
  ✕ Test 1: still calls showDialogModal once and diagnoses the log-signal failure when window.api.logError throws
  ✕ Test 2: still calls showDialogModal once and diagnoses the log-signal failure when window.api is entirely absent
  ✕ Test 3: still renders a non-empty dialog title/message when t throws on the title key
  ✕ Test 4: does not throw and diagnoses the dialog-signal failure when showDialogModal throws
  ✓ Test 5: T-34.2-52 survives even when window.api.logError throws
Tests:       4 failed, 15 passed, 19 total
```

Tests 3/4 failed via the throw ESCAPING `reportRepairFailure` itself (not an assertion diff),
reproducing the `index.tsx:158` un-awaited-handler escape route WR-06 names. Fix: signal 2's catch
now emits `console.error('repair-failure log signal unavailable:', logErr)`; `showDialogModal` and
the `t(...)` calls are each individually try/catch-wrapped with hardcoded English fallbacks.

**WR-07 — the containment root was predictable (pid-derived), never cleaned up, with a
check-then-act (`existsSync`/`mkdirSync`) TOCTOU window.** Closed by **plan 34.2-25** (Task 2).
File: `src/backend/jest.setupContainment.ts`. Fix:
`mkdtempSync(join(realTmpRoot, 'gamelib-jest-home-'))` + `chmodSync(root, 0o700)`, replacing the
predictable path and the race. **No RED test exists for this one — it is a direct hardening fix
with no prior assertion that could have failed on the pre-fix code; recorded honestly rather than
retrofitted with a RED transcript that was never run.**

**WR-08 — `loadMainRsCode()`'s `//`-to-end-of-line stripper has no string-literal awareness and
will silently truncate a future `"steam://..."`-shaped literal.** Closed by **plan 34.2-28**
(Task 2). File: `src/backend/__tests__/longRunningChannels.test.ts`. Fix: an even-quote-count guard
(whole-file and per-line) over the stripped output, self-tested with a synthetic `steam://` literal
fed through the same `loadMainRsCode(source?)` code path. Genuine falsifiability was hand-verified
both ways: removing the fixture's embedded `//` (inside the `steam://` literal itself) flips the
self-test to failing —

```
● stripper integrity (WR-08) › self-test: ... produces an ODD "-count on that line (proves the guard can fail)
  Expected: 1  Received: 0
Tests:       1 failed, 17 skipped, 18 total
```

— while removing only the fixture's separately-appended trailing comment does NOT (documented in
`34.2-28-SUMMARY.md` as an honesty correction to the plan's own stated falsifiability check, which
was itself vacuous for this fixture).

**WR-09 — `containmentRoot` was exported with zero consumers, the exact dead-export class this
same cycle removed from `repairFailure.ts` elsewhere.** Closed by **plan 34.2-25** (Task 3). File:
`src/backend/sidecar/__tests__/structuralContainment.test.ts`. Fix: Test 1 strengthened to exact
identity (`homedir() === containmentRoot`), giving the export a genuine consuming assertion instead
of dropping the `export` keyword. **No RED test — this is a dead-code-to-live-code fix; the "proof"
is that the export now has a real importer, not a failing/passing test pair.**

**WR-10 — the containment tripwires DETECT, they cannot PREVENT — module-scope imports already
write to disk before any tripwire test body runs.** Closed across three plans. Setup half (the real
fix — a `setupFiles`-time precondition that throws before any suite's own imports run) closed by
**plan 34.2-25** (Task 2); the `loggerFlows.test.ts` tripwire comment corrected by **plan 34.2-26**;
the `bootstrap.test.ts` tripwire comment corrected by **plan 34.2-29** (Task 3). File (setup half):
`src/backend/jest.setupContainment.ts`. Deliberate-break proof (34.2-25 Task 2, pointing the
`node:os` registration's `homedir` override at the real home instead of `containmentRoot`):

```
FAIL Backend src/backend/sidecar/__tests__/structuralContainment.test.ts
  ● Test suite failed to run
    [jest.setupContainment] REFUSING TO RUN: containment precondition failed -- ...
Test Suites: 1 failed, 1 total
Tests:       0 total
```

The entire suite failed to run (0 tests executed) BEFORE any test body — proving the precondition
is a real preventer, not a post-hoc per-suite detector. The two comment corrections (loggerFlows,
bootstrap) are textual only — no RED/GREEN pair, since they change documentation about which
mechanism does the preventing, not behavior.

**WR-11 — `structuralContainment.test.ts` Test 6's anti-vacuity check was macOS-specific and could
hard-fail in containerised CI (`os.userInfo()` throwing, `TMPDIR` under `$HOME`).** Closed by
**plan 34.2-25** (Task 3). File: `src/backend/sidecar/__tests__/structuralContainment.test.ts`.
Fix: dropped `userInfo()` as an independent reference and the hardcoded macOS log path; replaced
with two platform-independent assertions anchored on the per-run `mkdtemp` containment root plus a
pre-mock `realHomeAtSetup` capture. **See point (c) below for why this diverges from the review's
literal suggestion and why it is stronger. No RED test — this is a portability fix to an existing
passing-on-this-host test; there is no failure state to reproduce pre-fix on a non-macOS host from
within this session.**

**WR-12 — the module's comment claimed `XDG_DATA_HOME`/`XDG_CACHE_HOME` are "not read by either of
the two resolvers this module exists to contain," but `constants/paths.ts:21`'s `flatpakHome` is a
first-party reader.** Closed by **plan 34.2-25** (Task 2 comment fix, Task 3 test addition). Files:
`src/backend/jest.setupContainment.ts` (comment corrected to name `constants/paths.ts:21` as a
first-party consumer), `src/backend/sidecar/__tests__/structuralContainment.test.ts` (Test 4
extended to sweep `flatpakHome` into the containment check). **No RED test — the comment was
factually wrong but the underlying containment already held (measured: the value stays contained
because it derives from the already-redirected `homedir()`); this is a documentation-accuracy fix
plus an added regression pin, not a behavior fix.**

Deliberate deferrals from this cycle, tokens `D4-DEF-01` and `D4-DEF-02` (full text in
`deferred-items.md`'s `## From gap cycle 4` section):

- **D4-DEF-01** — `backend/logger/index.ts`'s four exported wrappers still discard the promise
  their `LogWriter` method returns; CR-01's fix added a sibling export rather than converting them.
- **D4-DEF-02** — the carried-forward gap-cycle-3 residuals this cycle did not touch (the
  per-suite tmp-root string triplication, the hoisted-factory TDZ naming issue, the two unrestored
  `jest.spyOn` calls in `sidecarRejectionGuard.test.ts`, the project-wide `prettier --check`
  baseline, and the pre-existing `library.ts` leaked-timer flake).

**(a) CR-01's remedy is narrower than the review's literal prescription.** The review suggested
converting `logError` itself to an expression body so it forwards `LogWriter#logError`'s promise.
Plan 34.2-26 instead added `logErrorSettled`, a new sibling export, and left the four existing
wrappers (`logDebug`/`logInfo`/`logWarning`/`logError`) byte-unchanged. Measured reason (the "audit
with `@typescript-eslint/no-floating-promises`" the review itself asked for): 309 statement-position
`logError(...)` call sites exist under `src/backend`, none awaited, against
`@typescript-eslint/no-floating-promises: 'warn'` in this project's eslint config — converting the
shared wrapper's inferred return type from `void` to `Promise<void>` would add roughly 309 new lint
warnings for zero runtime behavior change (the promise is dropped either way; only the drop site
moves from inside the wrapper to each unawaited call site). This audit's result — the count, not
just the conclusion — is recorded here and in D4-DEF-01/`deferred-items.md`, which also
cross-references and supersedes the pre-existing "From plan 34.2-20" entry rather than duplicating
it.

**(b) CR-02 had a secondary instance closed separately from the containment module itself.** Beyond
the `os`/`node:os` specifier gap in `jest.setupContainment.ts` (closed by plan 34.2-25),
`jest.config.js`'s comment asserted "the destruction path is reachable only from `src/backend`, so
the frontend/common/preload/meta jest projects are deliberately untouched" as a hand-maintained,
unenforced assumption — true today, but nothing would fail if a `meta` or `preload` suite started
importing `backend/logger` tomorrow. Plan 34.2-29's Block E converts this into an enforced
invariant (deliberate-break proof under WR-02/CR-02 secondary above), closing it separately from,
not as part of, the containment module's own fix.

**(c) WR-11's fix diverges from the review's suggestion, and is stronger.** The review proposed
guarding the `userInfo()` call and deriving the platform branch from `isMac`/`isWindows`. Plan
34.2-25 instead removed `userInfo()` from the test entirely — because the same plan's CR-02 fix now
redirects `userInfo()` too (the review's own CR-02 fix section flags this as a follow-on: "If
`userInfo` is redirected, `structuralContainment.test.ts` Test 6 needs a different independent
reference") — and anchored the anti-vacuity check on the per-run `mkdtemp` containment root plus a
`realHomeAtSetup` value captured before any mock is installed. This is stronger than the review's
proposed guard-and-branch approach for three reasons: it holds identically on all three platforms
(no `isMac`/`isWindows` branch to keep in sync with `getBaseLogPath()`'s own branch), it does not
care whether `TMPDIR` happens to live under `$HOME` (the review's own point 2), and it cannot be
satisfied by the mechanism under test — `realHomeAtSetup` is captured via `jest.requireActual`
before `jest.setupContainment.ts` installs its own mock, so the reference value is structurally
independent of whatever the mock later returns.

### Gap cycle 5 reconciliation (quick tasks 260823-9ds .. 260823-c2w)

`34.2-REVIEW-GAP-CYCLE-4.md` (reviewing what gap cycle 4 shipped) found 1 critical + 11 warning +
8 info = **20** findings: CR-01, WR-01..WR-11, IN-01..IN-08. Its frontmatter has always said
`total: 20` and the body agrees, so unlike cycle 3 there is no count discrepancy here.

**Correction to an earlier record.** `deferred-items.md` and three STATE.md quick-task rows
written on 2026-08-23 reported this cycle's scope as **17**. That figure is wrong and was never
supported by the review; it appears to have been an early miscount that then propagated. The
work was complete either way — all twenty are discharged — but the arithmetic in those documents
is corrected in the same commit as this section.

Unlike cycles 1-4, these closures were NOT made by a numbered gap-cycle plan. They were made by
five `/gsd-quick` tasks on 2026-08-23, which is why this section is keyed by quick-task id and
commit rather than by plan number. Every row below names a commit; the per-finding detail,
including each RED proof, lives in `deferred-items.md`'s "Gap-cycle-4 findings CLOSED 2026-08-23"
sections and is not duplicated here.

**Closed (20 of 20).**

- **CR-01** (the shared comment stripper) — found **already closed** before any work started.
  `src/backend/testUtils/stripSourceComments.ts` exists, strips block comments before the
  line-prefix filter, and both originally-defective files import it. Recorded as closed rather
  than re-fixed; the review's own text had it as a re-scoped blocker.
- **WR-01** (containment roots minted per test FILE, never reaped — 6,057 directories, ~500 MB)
  — `2a121ac90`, quick task `260823-9ds`. `jest.globalSetup.js` now mints one reapable run root
  per invocation; per-file roots nest inside it. Growth went from +174 per run to +1.
- **WR-11** (six planning gates existed and nothing ran them; two were silently red) —
  `e33dc2744`, `22d7895c4`, same task. `meta/runPlanningGates.py` + `pnpm planning-gates`, wired
  into `.github/workflows/codecheck.yml`.
- **WR-04, WR-06, WR-09, WR-10, IN-01** (five gates that could not fail) — `11b5212ed`, quick
  task `260823-a4o`. WR-09's new precondition caught a live vacuity introduced hours earlier by
  this phase's own IN-03 doc-comment fix.
- **WR-03** (`repairFailure.ts` swallowed a broken translation catalogue) — `cade8d757`, quick
  task `260823-wr3`. The only PRODUCTION-code finding of the twenty. Two of the review's three
  prescribed diagnostics were declined as self-defeating or redundant, taking the alternative the
  review itself offers.
- **WR-05, WR-07, WR-08** (gates measuring the wrong property) — `9c8a29b48`, `13b394ece`,
  `2fd9308c2`, quick task `260823-amg`.
- **IN-03, IN-04, IN-05, IN-06, IN-07** (hygiene) — `9175d11e2`, `ab040b355`, `b79c03a3f`, quick
  task `260823-bo0`.
- **WR-02, IN-02, IN-08** (narrower gate correctness) — `1835726a3`, `ae265a0c7`, `c576a77cd`,
  quick task `260823-c2w`.

**Deferred: none.** This cycle carried nothing forward of its own. The two items in
`deferred-items.md`'s "From gap cycle 4" section — `D4-DEF-01` and `D4-DEF-02` — predate it and
remain tracked there; `D4-DEF-02` is itself the carried-forward gap-cycle-2 residual set, and two
of its members are discharged below.

**Two long-deferred gap-cycle-2 findings closed as a side effect.** Both were named in the
gap-cycle-3 reconciliation's deferred list above and had been carried ever since:

- **gap-cycle-2 WR-05** (the containment tmp-root string duplicated three times per suite,
  inconsistently, in `loggerFlows.test.ts` and `testContainment.test.ts`) — closed by
  `1835726a3` / `ae265a0c7`. Both files now derive one `mkdtempSync` root that all three mock
  factories read.
- **gap-cycle-2 WR-06** (hoisted `jest.mock` factories referencing an out-of-scope `const`,
  surviving only by accident of naming) — closed by the same commits: the shared root is
  `mock`-prefixed, which is what `babel-plugin-jest-hoist`'s out-of-scope-variable rule actually
  requires.

**Five of the review's own claims did not survive checking**, which is worth recording because
this cycle's findings were largely about gates that pass while measuring the wrong thing:

1. **WR-08's prescribed regex carries the defect it reports.** Its lazy `[\s\S]*?` crosses the
   loop's closing brace, so it accepts the same counter-example reversed. Measured.
2. **WR-08's literal counter-example does not reproduce** — one `timeout_for` reference, so it
   trips the `>= 2` count rather than the structural hole. Pasted in verbatim it would have been
   a RED proof proving nothing.
3. **IN-05's premise is false.** `chmodSync(root, 0o700)` is not a no-op: `mkdtemp`'s mode is
   subject to umask, and `umask 0277` yields **0500**. Measured. The call stays.
4. **IN-08's remedy is impossible as written.** "Fix the shared stripper per CR-01" — CR-01's fix
   had already landed and that util deliberately does not strip trailing comments, because a
   naive pass is the WR-08 regression class that cut six `main.rs` lines in half.
5. **CR-01 was already closed** when the review recorded it as an open blocker.

**One finding was surfaced by this cycle's own work and is NOT part of the twenty.** A per-suite
`jest.mock('os', ...)` in a backend suite is **inert** — `jest.setupContainment.ts` runs from
`setupFiles` and requires `'os'` in its own precondition, so the mocked module is already
instantiated in jest's registry before a test file's hoisted `jest.mock` registers a new factory.
Measured with a scratch suite; pinned by a test in `loggerFlows.test.ts`. Containment is not
weakened, but ~30 backend suites document a mock that never takes effect. Tracked at
`.planning/todos/pending/2026-08-23-per-suite-jest-mock-os-is-inert-in-backend-suites.md`.

### Late closures (2026-08-23)

Findings closed AFTER their own cycle's reconciliation section was written. A cycle's section is
a record of what that cycle did; it is not amended years later to claim credit for work done
outside it. Late work lands here instead, keyed by `(round, ID)` because bare IDs collide across
rounds — round 1's `IN-04` and round 3's `IN-04`, where they exist, are unrelated findings that
happen to share a string.

**Closed (1)**

- **gap cycle 2 IN-04** — *Renderer-controlled log injection through the newly-ported `logError`
  channel* (`34.2-REVIEW-GAP-CYCLE-2.md`). This finding was in NEITHER of the gap-cycle-3 gate's
  closed or deferred token lists — it was not deferred with a reason, it was simply lost. Surfaced
  by quick task `260823-d7j`, which had to declare a `disposition:` per cycle review and found
  cycle 2 unaccountable for exactly one finding.

  **Fixed** by `src/backend/logger/sanitizeRendererLogMessage.ts`, called at the two
  renderer-facing log channels in `sidecar/loggerFlowRegistration.ts`.

  **Scope grew between the review and the fix.** The review named `logError` alone; `logInfo` has
  since been registered in the same file and is the identical vector. Both are sanitised.

  **The review's prescribed remedy was NOT followed, and would have been a regression.** It read:
  *"do it once in `LogWriter#writeString` (escape `\n` → `\\n` for non-forced messages)"*. Two
  independent defects in that one sentence:

  1. `forceLog` is not a trust signal — it is the logs-disabled override
     (`log_writer.ts:115`, `if (this.#logsDisabled && !forceLog)`). Keying the escape to it
     would escape trusted backend messages while leaving renderer text unescaped on any forced
     path. A gate on the wrong property, which is the defect class this phase's reviews kept
     finding.
  2. `writeString` sees output that is legitimately multi-line. Every log call reaches it via
     `formatLogMessage` → `convertUnknownToString` (`formatter.ts:32-48`), which returns
     `message.stack` for an `Error` and `JSON.stringify(message, null, 2)` for everything else.
     Escaping there would flatten **every stack trace and every pretty-printed object in the
     application's logs** onto one line — a permanent, repo-wide readability loss traded for an
     info-severity finding.

  The escape therefore sits at the trust boundary. The review's actual intent — one
  implementation, not an escape open-coded per channel — is preserved.

  **Proof.** 6 unit assertions in `logger/__tests__/sanitizeRendererLogMessage.test.ts` plus one
  behavioural test in `sidecar/__tests__/loggerFlows.test.ts` that drives a forged frame through
  the real transport and reads `gamelib.log` back. That test asserts on whole LINES, not
  substring presence: the forged text is legitimately present in escaped form, so a
  `not.toContain(forgedLine)` would measure the wrong property and fail even against a correct
  fix. RED-proven twice against the reverted call site — once as written (3 forged lines found
  where 0 are allowed) and once with the escape-presence assertions disabled, to confirm the
  load-bearing line assertion goes red on its own rather than riding on a neighbour.

  Backend suite 176/176, 4062 passed (from 175/4055): +1 suite, +7 tests, 0 regressions.

- **gap cycle 2 IN-03** — *Unrestored `jest.spyOn` in two harness helpers*
  (`sidecarRejectionGuard.test.ts:311,372`). Real and still present when checked. Closed with the
  review's own remedy, `afterEach(() => jest.restoreAllMocks())` at describe level — the restore
  cannot go inside the helpers, because both RETURN the spy as `logWarningMock` and their callers
  assert on it. Proven across a test PAIR, since a hook that runs between tests cannot be observed
  from inside one: the first installs a spy and stashes the module object, the second asserts it is
  no longer a mock function. RED-proven by deleting the `afterEach` (`Expected: false, Received:
  true`). The assertion deliberately targets the object captured INSIDE `isolateModules`; asserting
  on a top-level `require('backend/logger')` would pass vacuously against a different registry's
  instance that was never spied on.

- **gap cycle 2 IN-06** — *`MaxListenersExceededWarning` from repeated `init()` calls*. Closed, but
  **the review's diagnosis was wrong and its principled remedy was impossible.** The review says
  "each `init()` attaches process-level listeners that are never removed" and proposes a
  module-scope idempotency flag in `bootstrap.init()`. `bootstrap.ts` registers **no process
  listeners at all**. Traced with `--trace-warnings`, the real source is third-party:
  `node_modules/tmp/lib/tmp.js:732` runs `process.addListener(EXIT, _garbageCollector)`
  unconditionally at module scope, and `constants/paths.ts:11` imports `tmp`. This suite calls
  `jest.isolateModules()` **nine** times; each fresh registry re-executes `paths.ts`, re-loads
  `tmp`, and adds one more listener to the one real `process`. An idempotency flag could not have
  fixed it even in our own code — the flag would live in the registry `isolateModules` just
  replaced.

  So the count is a bounded artifact of module isolation, not a leak. The review's other option,
  `setMaxListeners(0)`, disables leak detection outright and forever; instead the ceiling is an
  explicit 20 against a measured 12, with a test asserting the bound so it is a *measured* limit
  rather than a silenced alarm. RED-proven at ceiling 3 (`Expected: < 3, Received: 12`).

**Deferred: none.**

**A correction to this section's own first version.** When IN-04 was closed earlier the same day,
this section stated that three of cycle 2's deferred findings — `IN-01`, `IN-03`, `IN-06` —
"remain genuinely deferred". Checking them against the live tree rather than against the ledger
showed that was wrong for one: **`IN-01` (prettier violations in five files) was already CLOSED** —
all five pass `prettier --check`, measured in place. It had been fixed at some point and never
recorded, the same bookkeeping failure that lost IN-04, in the opposite direction. So cycle 2 was
**two** findings from complete, not three.

**What this section does NOT close.** Nothing of gap cycle 2 remains open: 15 of 15 are now
accounted for (9 in the gap-cycle-3 reconciliation, `WR-05`/`WR-06` in gap cycle 5, `IN-01` found
already closed, and `IN-04`/`IN-03`/`IN-06` here). `34.2-REVIEW-GAP-CYCLE-2.md` is therefore moved
to `disposition: closed`.

### Late closures — gap cycle 1 (2026-08-23)

`34.2-REVIEW-GAP-CYCLE-1.md`'s 14 findings. Its three criticals were closed by gap cycle 2; the
remaining eleven were carried forward and never dispositioned as a set, which is what quick task
`260823-d7j` recorded as `disposition: partial`. All eleven were checked against the LIVE TREE
rather than against the ledger — the method that has now caught a mislabelled finding in both
cycles.

**Already closed, never recorded (5).** No work needed; each carries its own in-source evidence.

- **gap cycle 1 WR-01** — log path absent from the containment tripwire. Closed by plan 34.2-18;
  `gameDetailsFlows.test.ts:368` names both the finding and the plan in source.
- **gap cycle 1 WR-03** — CR-01's Rust half had no Rust-side test. Closed by gap cycle 3 plan 22:
  `main.rs:6141` carries behavioural `timeout_for()` tests asserting real channel outcomes.
- **gap cycle 1 WR-05** — `electronReachLedger`'s vacuity guards weaker than its header claimed.
  Now guarded by a named `requiredModules` list of load-bearing electron-reaching edges, not a
  bare `size > 0`.
- **gap cycle 1 WR-07** — `loadMainRsCode()` corrupting Rust string literals containing `//`.
  Closed by `backend/testUtils/stripSourceComments.ts`, which is string-literal aware.
- **gap cycle 1 IN-03** — the containment check built an entire isolated bootstrap graph to read
  three constants. Closed by `loadConstantsPaths()`.

**Accepted by design (1).**

- **gap cycle 1 IN-01** — `timeout_for` is argument-blind, so `readConfig('user')` is unbounded
  along with `readConfig('library')`. The trade is documented at the entry itself
  (`main.rs:205`): `readConfig('library')` calls `legendary.refresh()`, the same work
  `refreshLibrary` is exempted for, while `readConfig('user')` is fast — so this trades a 60s
  rejection for a never-settling promise only if that fast path ever wedges. A recorded decision,
  not an oversight.

**Closed (3)** — WR-04 was attempted, shipped, and reverted; see its entry under Deferred.

- **gap cycle 1 WR-04** — **REVERTED. Still OPEN, and now better understood.** The finding is
  real: `src/sidecar/index.ts` states the guard "must be live before `bootstrap.ts`'s module
  scope", and it is not, because ES evaluation runs every import before any statement in the
  importing body.

  **The review's prescribed fix — "make the install a module-scope side effect of a module
  imported first" — BREAKS THE SIDECAR, and it was shipped before that was caught.** Attempted
  2026-08-23 as `src/sidecar/installRejectionGuardFirst.ts`; reverted the same day after the Tauri
  build failed with a broken pipe. Root cause: **two valid ordering invariants are in conflict**,
  and the review names only one of them.

  1. `index.ts:12` (pre-existing): *"the load-bearing shim ordering (the `Module._load` hook, then
     the backend registration import) lives in `bootstrap.ts`'s module scope"*. The electron
     `Module._load` hook MUST be installed before any backend module is evaluated.
  2. WR-04's invariant: the rejection guard should be live before bootstrap's module scope.

  These cannot both hold while `processGuards.ts` imports `logWarning` from `backend/logger`.
  Importing the guard first drags `backend/logger` → `backend/utils` → `legendary/electronStores`
  → `new ElectronStore` into evaluation **before the electron hook exists**, so `app.getPath()`
  returns `undefined` and the sidecar dies at startup with
  `ERR_INVALID_ARG_TYPE: The "path" argument must be of type string`.

  Isolated by bisecting the entry file against its own parent commit: `HEAD~1` exits 0, the
  WR-04 commit exits 1.

  **What the unit suite could not tell us.** All 176 backend suites stayed green across the
  breakage, and `pnpm build:sidecar` exits 0 — the bundle builds fine, it just cannot RUN. The
  bundle-offset check performed at the time confirmed the guard was emitted before `bootstrap.ts`
  and was read as confirmation the fix worked; it measured emission order and said nothing about
  whether the process survives. A gate measuring the wrong property, committed inside a fix for a
  gate measuring the wrong property.

  **Viable routes, none attempted yet:** (a) make the guard logger-free so its import graph
  touches no backend module — it would then genuinely be safe to import first; (b) install the
  guard inside `bootstrap.ts`'s module scope immediately AFTER the electron hook and before the
  backend registration import, which respects invariant 1 while covering most of invariant 2.
  Route (b) looks best. Any attempt MUST gate on actually starting the built sidecar, not on the
  unit suite and not on bundle offsets.

- **gap cycle 1 WR-06** — the i18next assertion could not distinguish "resources loaded" from
  "inline English default". Still true when measured: `gamepage.json`'s `launch.default` is
  `"Default"`, byte-identical to the inline default in
  `i18next.t('launch.default', 'Default', { ns: 'gamepage' })`, so both paths render the same
  string. **This is the third instance of this exact shape in Phase 34.2**, after round-1 WR-04
  and `G-34.2-UAT-01`. Fixed by asserting the discriminating property directly, with a sentinel
  default that appears nowhere in the locale file: a live i18next returns the locale value, a dead
  one returns the sentinel. RED-proven by repointing it at an unresolvable key
  (`Expected: "Default", Received: "WR-06-SENTINEL-NOT-A-TRANSLATION"`).

- **gap cycle 1 WR-02** — the two sha256 gates hashed raw bytes with no line-ending
  normalisation. `.gitattributes` carries no `text=auto eol=lf`, so a Windows checkout under
  Git's default `core.autocrlf=true` fails both gates for a reason unrelated to their content.
  CI cannot catch it — `test.yml` runs on `ubuntu-latest` only — so it would surface as a
  confusing local red for a Windows contributor on a repo that ships a Windows build. Both sites
  now normalise. Proven in both directions: raw CRLF digest `a4736e7b…` differs from the pin,
  normalised CRLF digest `43afa3fb…` matches it, and **the committed pins are unchanged** because
  normalising an already-LF file is a no-op.

- **gap cycle 1 IN-02** — dead `if (current === undefined) break` beneath
  `while (queue.length > 0)`: unreachable code inside a file whose stated purpose is proving the
  walk does not silently short-circuit. Rewritten so the undefined check IS the loop condition —
  same guarantee, no dead branch, no non-null assertion.

**Deferred (2)**

- **gap cycle 1 IN-04** — documentation drift. Part (a), the `192-vs-194` file-count note, is now
  stale in a third way (the walk measures 228) but the comment does date its own regeneration and
  points at a SUMMARY for the historical explanation, so it is context rather than a live claim.
  Left as-is: rewriting historical provenance to match a current measurement is how provenance
  gets lost. No behavioural or gate consequence.

**Result.** `34.2-REVIEW-GAP-CYCLE-1.md` stays `disposition: partial`. Twelve of fourteen are
closed or accepted; **WR-04 and IN-04 remain open**, WR-04 because its only known fix breaks the
sidecar and needs a different approach.
