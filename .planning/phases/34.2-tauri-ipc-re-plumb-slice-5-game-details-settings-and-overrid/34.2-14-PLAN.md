---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 14
type: execute
wave: 3
depends_on: [34.2-08, 34.2-09, 34.2-10, 34.2-11, 34.2-12, 34.2-13]
gap_closure: true
files_modified:
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md
autonomous: true
requirements: [REQ-34.2-12, REQ-34.2-03]

must_haves:
  truths:
    - "34.2-PORTED-CHANNELS.md is current as of the end of the gap cycle — it no longer predates the code review that found CR-01"
    - "The repair timeout gap is recorded on the repair and readConfig rows, including that the original 34.2-06 audit missed them"
    - "The sign-off no longer rests on a transitive electron-freedom property the code does not have"
    - "The two deferred HUMAN-UAT items keep their deferred status, and any interaction with this cycle's changes is recorded rather than used to claim closure"
    - "Findings that remain open after this cycle are named by review ID, so nothing reads as closed that is not"
  artifacts:
    - path: ".planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md"
      provides: "Current, honest declaration reflecting gap-cycle plans 34.2-08..13"
      contains: "LONG_RUNNING_CHANNELS"
  key_links:
    - from: "34.2-PORTED-CHANNELS.md repair row"
      to: "src-tauri/src/main.rs LONG_RUNNING_CHANNELS"
      via: "recorded timeout-policy state after plan 34.2-08"
      pattern: "repair"
    - from: "34.2-PORTED-CHANNELS.md sign-off section"
      to: "src/backend/sidecar/__tests__/electronReachLedger.test.ts"
      via: "named as where the measured electron-reach set now lives"
      pattern: "electronReachLedger"
---

<objective>
Close the **currency gap** `34.2-VERIFICATION.md` records against `34.2-PORTED-CHANNELS.md`
(truth row 13): *"The artifact predates the code review that found CR-01, so it does not yet record
the repair-timeout gap — this is a currency gap in a since-superseded snapshot."*

The document was written by plan 34.2-07 and was verified accurate for what it claimed at the time.
Three things have since changed underneath it:

1. `repair` and `readConfig` were never audited against `LONG_RUNNING_CHANNELS` when it was written;
   plan 34.2-08 added both. §1's timeout narrative and the `repair` / `readConfig` rows describe the
   pre-fix state.
2. The `releasesInfoReady` -> `downloadAntiCheatData` bootstrap wiring was declared "wired" but was
   crash-unsafe; plan 34.2-09 added a call-site `.catch()` and a process-level rejection guard.
3. §6's sign-off rests on a curated-import discipline whose prose overclaimed transitive
   electron-freedom; plan 34.2-11 corrected the claim and committed a measured reach ledger.

This plan runs last in the gap cycle precisely so it can describe the FINAL state, not an
intermediate one.

Purpose: the artifact the Phase 35 cutover gate reads for this slice tells the truth as of the end of
this cycle, including what is still open.
Output: an updated declaration plus a gap-cycle reconciliation section.

**This plan is documentation-only.** It modifies exactly one Markdown file and zero source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VERIFICATION.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-HUMAN-UAT.md

<interfaces>
<!-- The document's existing structure. Preserve it; do not restructure. -->

`34.2-PORTED-CHANNELS.md` sections:
  §1 Port kinds — `sidecar invoke` (23) / `sidecar send` (3); zero `rustInvoke` and zero
     `renderer-side (Tauri JS)` rows; states that the one Rust-adjacent change (adding
     `"getCrossoverIndex"` to `LONG_RUNNING_CHANNELS`) is a timeout-policy edit, not a port kind, and
     that the `dispatch_rust_channel` arm count stays at 11.
  §2 The 26 channels — table `| Channel | Kind | Backed by | Proof level | Notes |`, proof level
     `unit` on every row. Relevant rows:
       `repair` — "Plan 34.2-02/04; see §3 rider 1 (Steam stub, `games.ts:1610`); D-02 i18next
                   end-to-end proof lives on this row"
       `readConfig` — "Plan 34.2-02/04; see §3 rider 3 (Legendary-only reachability)"
       `getCrossoverIndex` — "...exempted from the 60s invoke timeout (D-10) — see §5"
       `getWikiGameInfo` — the cold-cache measurement, NOT exempted
       `getAnticheatInfo` — the §3 rider 2 Epic-namespace / Windows-null behaviour
     Followed by the two bootstrap-wiring entries (D-02 i18next init, D-07 `fetchLastestReleases()`).
  §3 Declaration riders 1-3 (Steam upstream stubs; anticheat Epic-namespace/Windows-null; six
     channels unreachable in a Steam-only workflow).
  §4 The `getGameSettings` / `requestGameSettings` near-duplicate note (D-09).
  §5 Re-deferrals — the D-09 dedupe, the `getCrossoverIndex` per-game `loadIndex`/`buildMaps` hoist,
     the ~100 remaining `i18next.t()` call sites, the `tools/dxmt.ts` / `wine/manager/ipc_handler.ts`
     `releasesInfoReady` consumers. Ends "**This slice makes NO reassignment.**"
  §6 Sign-off — this slice is data-in/data-out so "wired and unit-proven" is genuinely close to
     "works", with **exactly two honest exceptions** (D-02 and D-07); closing line: "Do not read
     'wired and unit-proven' as 'seen working' — that is exactly how G-30-02 was declared fixed twice
     while the live build hung."
  §7 Reconciliation — 26-channel set equality against `.planning/IPC-PORT-INVENTORY.md`, no inventory
     edit needed (D-08), and a per-plan (34.2-01..06) no-drift statement.

`34.2-HUMAN-UAT.md` holds two items, both deferred per D-11 with `result: pending`:
  UAT-34.2-01 — a translated notification string actually renders in a live Tauri window
  UAT-34.2-02 — the anticheat data file actually appears on disk via a real network fetch

**Gap-cycle outcomes to record** (read each plan's SUMMARY for what actually shipped, not what was
planned — a SUMMARY may record a deviation):
  34.2-08 — `repair` + `readConfig` added to `LONG_RUNNING_CHANNELS` (8 members); exact-set pin
            updated; try/catch + `window.api.logError` at the `GameSubMenu` repair call site.
            Closes gap #1 / CR-01.
  34.2-09 — `.catch()` on `downloadAntiCheatData` inside the `releasesInfoReady` listener;
            `installUnhandledRejectionGuard()` in `sidecar/processGuards.ts`, installed from
            `src/sidecar/index.ts` before `init()`. Closes gap #2 / CR-02.
  34.2-10 — `enrichmentFlows.test.ts` and `gameDetailsFlows.test.ts` redirected via a `pathShim` mock
            with a `resolve`+`relative` containment guard; `online_monitor` mock re-armed.
            Closes CR-03 + WR-08.
  34.2-11 — transitive-electron-freedom claim corrected in two docstrings and in REQUIREMENTS.md;
            `electronReachLedger.test.ts` commits the measured set (194 first-party files reachable
            from the four gated entry points, 29 of which import `electron` directly).
            Closes gap #3 / WR-02.
  34.2-12 — tautological HEAD-comparison gates replaced by committed sha256 digests plus a
            ten-channel semantic pin; the vacuous i18next assertion replaced by a real initialized
            translation. Closes WR-01 + WR-04.
  34.2-13 — `storeSearch/handlers.ts` extracted; both registration sites delegate to it. Closes WR-09.

**Explicitly still open after this cycle** (record as such; do not imply otherwise):
  WR-03, WR-05, WR-06, WR-07, WR-10, IN-01, IN-02, IN-03, IN-04 — accepted debt for this cycle.
  The pre-existing `rustInvokeChannel.test.ts` failure (Phase 34.1 tray work), the `library.ts`
  `pollInstallOnce` leaked-timer jest exit-1, and the pre-existing eslint error in
  `crossover_index/__tests__/index.test.ts:29` — all out of scope, all already logged in
  `deferred-items.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring the port-kinds section, the affected channel rows and the re-deferral list current</name>
  <read_first>
    - `.planning/phases/34.2-.../34.2-PORTED-CHANNELS.md` in full — all seven sections
    - `.planning/phases/34.2-.../34.2-08-SUMMARY.md` and `34.2-09-SUMMARY.md` — what actually shipped
      for the two code gaps, including any recorded deviation from plan
    - `src-tauri/src/main.rs` lines 100-150 — the FINAL `LONG_RUNNING_CHANNELS` membership, read from
      source rather than from a plan, so the document records the real state
    - `src/backend/sidecar/bootstrap.ts` Block A as edited by 34.2-09
    - `.planning/phases/34.2-.../34.2-VERIFICATION.md` truth row 13 and gap #1 — the specific
      staleness this task closes
  </read_first>
  <action>
    Edit §1, §2 and §5 in place. Preserve the existing section numbering, the table column order
    (`Channel | Kind | Backed by | Proof level | Notes`), and the document's voice.

    **§1 Port kinds.** The sentence stating that the one Rust-adjacent change is adding the string
    `"getCrossoverIndex"` to `LONG_RUNNING_CHANNELS` is now incomplete. Record that the gap cycle
    (plan 34.2-08) added two further strings, `"repair"` and `"readConfig"`, bringing the list to
    eight members. Re-affirm the two claims that remain true: this is still a timeout-policy edit and
    not a port kind, and the `dispatch_rust_channel` arm count is still 11.

    **§2 `repair` row.** Extend the Notes cell to record: exempted from the 60s invoke bound as of
    plan 34.2-08; the original 34.2-06 audit MISSED it, caught by code review CR-01 and verification
    gap #1; the concrete symptom was that a real `legendary repair` / `gogdl repair` on a multi-GB
    title exceeds 60s, the Rust side drops the pending entry and returns `sidecar invoke timed out`
    while the repair keeps running, and the renderer call site had no `.catch()` so it also produced
    an unhandled rejection. Record that the renderer call site now catches and logs.

    **§2 `readConfig` row.** Extend the Notes cell to record that it is exempted for the same reason
    (`readConfig('library')` calls `legendary.refresh()`, the operation `refreshLibrary` is already
    exempted for), plus the honest tradeoff carried in the code comment: the exemption is per-channel,
    so the fast `readConfig('user')` argument is unbounded too.

    **§2 bootstrap-wiring entries.** Update the D-07 `fetchLastestReleases()` / re-homed
    `releasesInfoReady` entry: it was declared "wired" but the `void downloadAntiCheatData(...)` call
    was crash-unsafe — a single filesystem error on `areweanticheatyet.json` would have killed the
    whole sidecar process, because the surrounding try/catch covered only the synchronous
    `backendEvents.on(...)` registration and no `unhandledRejection` handler existed anywhere in the
    sidecar. Record that plan 34.2-09 attached a `.catch()` at the call site and added
    `sidecar/processGuards.ts`'s log-only `installUnhandledRejectionGuard()`, installed from
    `src/sidecar/index.ts` before `init()`.

    **§5 Re-deferrals.** Keep every existing entry. Append the findings that remain genuinely open
    after this gap cycle, each named by its review ID so a later reader can look it up: WR-03 (the
    `knownFixes.ts` docstring's factually false extraction rationale), WR-05 (the READY sentinel fires
    before i18next's async resource load settles), WR-06 (the `readKnownFixes` path traversal — record
    as tracked accepted debt, noting this project's convention that containment is `resolve` +
    `relative`, never `join`), WR-07 (`getCrossoverIndex` unbounded with no cancellation on top of the
    known per-game rebuild), WR-10 (duplicated test-harness boilerplate and the twice-implemented
    gamedetails electron gate), and IN-01 through IN-04.

    Do NOT change the 26-channel count, any channel name, any `Kind` cell, or any `Proof level` cell.
    No channel was added, removed, or re-kinded by this gap cycle, and §7's set-equality
    reconciliation against `.planning/IPC-PORT-INVENTORY.md` must remain true.
    Do NOT edit `.planning/IPC-PORT-INVENTORY.md` or `SEAM.md` — both are out of scope.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib/.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid && grep -cE '^\| `[A-Za-z]+` \| sidecar (invoke|send) \|' 34.2-PORTED-CHANNELS.md && for k in LONG_RUNNING_CHANNELS processGuards CR-01 WR-06 WR-10 IN-04; do grep -qF "$k" 34.2-PORTED-CHANNELS.md || { echo "FAIL missing $k"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The automated check prints `26` for the channel-row count and then `OK` — the §2 table still has
      exactly 26 `sidecar invoke`/`sidecar send` rows, and every required marker string is present.
    - §1 names all three timeout-exemption additions attributable to phase 34.2 (`getCrossoverIndex`,
      `repair`, `readConfig`), states the list now has eight members, and still states the
      `dispatch_rust_channel` arm count is 11.
    - The `repair` row's Notes cell contains the literal substrings `CR-01` and `LONG_RUNNING_CHANNELS`.
    - The `readConfig` row's Notes cell records both the `legendary.refresh()` rationale and the
      per-channel `readConfig('user')` tradeoff.
    - The D-07 bootstrap-wiring entry names `processGuards` and states the pre-fix crash risk.
    - §5 contains entries naming each of `WR-03`, `WR-05`, `WR-06`, `WR-07`, `WR-10`, `IN-01`,
      `IN-02`, `IN-03`, `IN-04`, and every pre-existing §5 entry is still present.
    - The eight members recorded in §1 match the actual array in `src-tauri/src/main.rs` exactly —
      cross-checked by reading the Rust source, not a plan document.
    - `git status --porcelain .planning/IPC-PORT-INVENTORY.md` is empty, and `git status --porcelain`
      lists no modified file under `src/` or `src-tauri/` — this plan is documentation-only.
  </acceptance_criteria>
  <done>
    §1, the `repair`/`readConfig` rows, the D-07 wiring entry and §5 describe the post-gap-cycle state,
    and the still-open findings are named rather than implied closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the sign-off's electron-freedom basis and add a gap-cycle reconciliation section</name>
  <read_first>
    - `.planning/phases/34.2-.../34.2-PORTED-CHANNELS.md` §6 and §7 as edited by Task 1
    - `.planning/phases/34.2-.../34.2-10-SUMMARY.md`, `34.2-11-SUMMARY.md`, `34.2-12-SUMMARY.md`,
      `34.2-13-SUMMARY.md` — what actually shipped for the four test-and-honesty plans, including
      recorded deviations and negative-control results
    - `src/backend/sidecar/__tests__/electronReachLedger.test.ts` — the committed baseline's actual
      size, so §6 records the measured numbers rather than this plan's quoted ones
    - `.planning/phases/34.2-.../34.2-HUMAN-UAT.md` — both items' current `result` and `status`
      fields, which must NOT change
    - `.planning/phases/34.2-.../34.2-VERIFICATION.md` gap #3 and the Anti-Patterns table
  </read_first>
  <action>
    **§6 Sign-off.** Preserve the two things that make this section valuable: the plainly-stated
    difference from 34.1 (this slice is data-in/data-out, so "wired and unit-proven" is genuinely
    close to "works") and the closing G-30-02 warning line. Do not soften either.

    Add to §6 a short subsection recording what the gap cycle changed about the honesty of this
    declaration:
    - The curated-import discipline this slice relies on proves **direct**-import freedom, not
      transitive. The earlier wording in `dispatch.ts`, `enrichmentFlowRegistration.ts` and
      REQUIREMENTS.md REQ-34.2-03 claimed a transitive property the code does not have; plan 34.2-11
      corrected all three. State the true invariant here too: transitive reach to `electron` exists,
      is expected, and is rescued at runtime by `electronStub`'s `Module._load` interception.
    - Name `src/backend/sidecar/__tests__/electronReachLedger.test.ts` as the artifact that now holds
      the measured, enforced set, and record the counts read from that file — not from this plan.
      State explicitly that the committed baseline is the Phase 35 cutover work-list.
    - Record that two proof surfaces this document implicitly leaned on were found to assert nothing
      and were replaced (WR-01's tautological HEAD-comparison gates, WR-04's vacuous i18next
      assertion), so a later reader does not over-trust the pre-gap-cycle proof levels.

    **HUMAN-UAT interaction — record, do not resolve.** Add a short paragraph to §6 stating how this
    gap cycle touches the two deferred live-UAT items, and that neither is closed by it:
    - UAT-34.2-01 (a translated notification actually renders): the unit proof is now STRONGER —
      plan 34.2-12 replaced the vacuous assertion with one that compares against the on-disk
      `en/gamepage.json` value and explicitly rejects both `undefined` and the raw key. A live render
      in a Tauri window, and the packaged build's locale-path resolution, remain unproven.
      Still deferred.
    - UAT-34.2-02 (the anticheat file actually appears via a real network fetch): plan 34.2-09 changed
      the FAILURE behaviour of that path — a filesystem error now logs a warning instead of killing
      the sidecar. The success path is unchanged, so the UAT's expected observation is unchanged, but
      a tester should know that a silent absence of the file is now a logged warning rather than a
      dead backend. Still deferred.
    Do NOT edit `34.2-HUMAN-UAT.md`, and do NOT change either item's `result: pending` or deferred
    status. This plan records an interaction; it does not close a UAT item.

    **§7 Reconciliation.** Keep the existing 34.2-01..06 no-drift statements verbatim — they remain
    accurate for what those plans shipped. Append a clearly-labelled gap-cycle subsection covering
    plans 34.2-08 through 34.2-14: one line each naming the plan, the verification gap or review
    finding ID it closes, and what shipped. Restate that no channel was added, removed, or re-kinded,
    so the 26-channel set equality against `.planning/IPC-PORT-INVENTORY.md` still holds and no
    inventory edit was needed.

    Finally, update the document's header paragraph, which currently says it "describes what actually
    shipped in 34.2-01..34.2-06 — verified row by row against all six plan SUMMARY.md files". Extend
    the range to include the gap-cycle plans and note that the document was refreshed after the gap
    cycle, so a reader can tell at a glance which snapshot they are holding.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib/.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid && for k in electronReachLedger Module._load 34.2-08 34.2-13 UAT-34.2-01 UAT-34.2-02 G-30-02 WR-01 WR-04; do grep -qF "$k" 34.2-PORTED-CHANNELS.md || { echo "FAIL missing $k"; exit 1; }; done; cd /Users/graysonmitchell/Projects/GameLib && git diff --quiet -- .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The automated check prints `OK`: all nine marker strings are present and `34.2-HUMAN-UAT.md` has
      no uncommitted modification.
    - §6 still contains the plainly-stated difference from 34.1 and the closing G-30-02 warning line,
      neither softened.
    - §6 contains a subsection stating the true invariant — direct-import freedom is what the curated
      import discipline proves; transitive reach exists, is expected, and is rescued by
      `electronStub`'s `Module._load` interception — and the literal string `Module._load` is present.
    - §6 names `electronReachLedger`, records the counts read from that test file, and states the
      baseline is the Phase 35 cutover work-list.
    - §6 records that WR-01's HEAD-comparison gates and WR-04's i18next assertion asserted nothing and
      were replaced.
    - §6 contains a paragraph naming both `UAT-34.2-01` and `UAT-34.2-02`, stating for each how this
      gap cycle interacts with it and that it remains deferred. Neither is described as closed,
      passed, or verified.
    - §7 retains its existing 34.2-01..06 no-drift statements verbatim and gains a labelled gap-cycle
      subsection with one line per plan for `34.2-08` through `34.2-14`, each naming the gap or review
      finding ID it closes.
    - §7 still asserts 26-channel set equality against `.planning/IPC-PORT-INVENTORY.md` and that no
      inventory edit was needed.
    - The document header paragraph names the gap-cycle plan range and states the document was
      refreshed after the gap cycle.
    - `git status --porcelain` lists no modified file under `src/` or `src-tauri/`, and
      `.planning/IPC-PORT-INVENTORY.md` is unmodified.
  </acceptance_criteria>
  <done>
    §6 no longer rests on a false transitive-electron-freedom property, names the ledger and the two
    replaced assert-nothing proofs, records both deferred UAT interactions without claiming closure,
    and §7 reconciles the full gap cycle.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declaration artifact -> Phase 35 cutover gate | `34.2-PORTED-CHANNELS.md` is the artifact the Phase 35 completion gate reads for this slice. Anything it asserts is treated downstream as established, without re-derivation |
| declaration artifact -> human tester | `34.2-HUMAN-UAT.md` and this document are explicitly meant to be read together; the document itself states that either one alone can leave a reader with the wrong impression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-69 | Repudiation / Integrity of claims | §1, the `repair`/`readConfig` rows, the D-07 wiring entry | mitigate | The document describes a pre-fix state: `repair` un-exempted, the anticheat wiring "wired" but crash-unsafe. A Phase 35 reader would conclude the timeout audit was complete and the bootstrap wiring safe. Task 1 records the real post-cycle state, including that the original audit missed two channels. |
| T-34.2-70 | Repudiation / Integrity of claims | §6 sign-off's reliance on the curated-import discipline | mitigate | The sign-off's strength rests partly on an electron-freedom property the code does not have transitively. Task 2 states the true invariant, names `Module._load` as the actual mechanism, and points at the committed reach ledger — converting an implicit overclaim into a measured, enforced fact. |
| T-34.2-71 | Spoofing | Gap-closure work presented as UAT closure | mitigate | The most likely dishonesty in a gap cycle is quietly upgrading a deferred live-UAT item because adjacent code improved. Task 2 records the interaction with both UAT items explicitly and forbids editing `34.2-HUMAN-UAT.md`; the automated check fails the task if that file is modified at all. |
| T-34.2-72 | Repudiation | Findings still open after the cycle | mitigate | A refreshed document listing only what was fixed reads as "everything was fixed". Task 1 requires WR-03/05/06/07/10 and IN-01..04 to be named by ID in §5, so residual debt stays visible — including WR-06's path traversal, a real security finding deliberately deferred. |
| T-34.2-73 | Tampering | The 26-channel set equality with `.planning/IPC-PORT-INVENTORY.md` | mitigate | An edit that accidentally adds, removes, or renames a table row would silently break §7's set-equality claim and the Phase 35 completion gate depending on it. Task 1's automated check counts the channel rows and fails unless exactly 26 remain. |
| T-34.2-74 | Information Disclosure | Content added to the declaration | accept | All added content describes first-party module paths, review finding IDs, and test names already present in the repository. No secrets, credentials, endpoints, or user data. |
| T-34.2-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages and modifies NO source files — one Markdown edit. Package Legitimacy Gate not triggered; no `[ASSUMED]`/`[SUS]` checkpoint required. |
</threat_model>

<verification>
This plan is documentation-only, so the source build gates are no-regression checks rather than
targets of change. All must still be green when the plan closes:

- `npx tsc --noEmit` — clean, no output
- `cd src-tauri && cargo check --quiet` — exit 0, no output
- `npx jest gameDetailsFlows enrichmentFlows bootstrapWirings gameDetailsImportGate gameDetailsModules knownFixes longRunningChannels ratingMap` — all suites pass, plus the gap cycle's new suites (`sidecarRejectionGuard`, `electronReachLedger`, `storeSearch`)
- Full backend suite baseline: `npx jest --testPathPattern=src/backend` → **105/106 suites,
  2211/2212 tests**, plus the gap cycle's new suites. The single known failure is
  `rustInvokeChannel.test.ts` "writes a single well-formed rustInvoke frame", pre-existing from
  Phase 34.1 and explicitly OUT OF SCOPE for this cycle. Do NOT write an acceptance criterion
  demanding 106/106 or 2212/2212 — state the baseline.
- `git status --porcelain` shows exactly one modified file: `34.2-PORTED-CHANNELS.md`. Any source
  file appearing in that output means this plan exceeded its scope.
- The Electron build keeps working unchanged (REQ-34.2-14) trivially — zero source files touched.

Content cross-check (do not skip): every factual claim added to the document must be verified against
SOURCE, not against a plan document. The eight `LONG_RUNNING_CHANNELS` members come from
`src-tauri/src/main.rs`; the reach-ledger counts come from
`src/backend/sidecar/__tests__/electronReachLedger.test.ts`; the shipped-vs-planned details come from
each plan's SUMMARY, which may record a deviation. A document that restates plan intent rather than
shipped reality would reproduce exactly the staleness this plan exists to remove.
</verification>

<success_criteria>
- `34.2-PORTED-CHANNELS.md` describes the state at the END of the gap cycle, not before it
- The `repair` and `readConfig` rows record the timeout exemption, the missed audit, and the CR-01
  provenance
- The D-07 bootstrap-wiring entry records the crash risk and its fix
- §6 states the true (direct-import) invariant, names `Module._load` and `electronReachLedger`, and
  records the two replaced assert-nothing proofs
- Both deferred UAT items' interactions are recorded and neither is claimed closed;
  `34.2-HUMAN-UAT.md` is unmodified
- Still-open findings are named by review ID in §5
- §7 reconciles plans 34.2-08..14; the 26-channel set equality is preserved
- Exactly one file is modified
</success_criteria>

<output>
Create `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-14-SUMMARY.md` when done
</output>
