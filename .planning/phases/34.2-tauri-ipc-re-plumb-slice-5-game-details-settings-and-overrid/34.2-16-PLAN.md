---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 16
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_cycle: 2
files_modified:
  - src/backend/sidecar/loggerFlowRegistration.ts
  - src/backend/sidecar/handlers.ts
  - src/backend/sidecar/__tests__/loggerFlows.test.ts
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md
  - .planning/IPC-PORT-INVENTORY.md
autonomous: true
requirements: [REQ-34.2-12, REQ-34.2-08, REQ-34.2-09, REQ-34.2-13, REQ-34.2-14]

must_haves:
  truths:
    - "A `logError` send frame arriving over the real sidecar RPC transport produces an actual log line in the sidecar's log file — not merely 'no error was thrown'"
    - "`logError` has exactly one registered listener on the sidecar; it is never registered twice"
    - "The sidecar registration body is behaviorally identical to Electron's `logger/ipc_handler.ts:15` — same LogPrefix, same argument"
    - "The early port of a Phase 34.3 (slice 6) channel is declared in BOTH the phase ledger and the cross-phase inventory, so slice 6 cannot double-register it"
    - "The new test suite writes nothing outside os.tmpdir() — it ships with the containment kit from day one rather than acquiring it in a later gap cycle"
  artifacts:
    - path: "src/backend/sidecar/loggerFlowRegistration.ts"
      provides: "registerLoggerFlows() — curated send-kind registration of the logError channel"
      exports: ["registerLoggerFlows"]
      min_lines: 25
    - path: "src/backend/sidecar/handlers.ts"
      provides: "registerLoggerFlows() call alongside the other curated registrations"
      contains: "registerLoggerFlows"
    - path: "src/backend/sidecar/__tests__/loggerFlows.test.ts"
      provides: "Positive side-effect proof over the real transport + containment kit"
      min_lines: 120
    - path: ".planning/IPC-PORT-INVENTORY.md"
      provides: "logError recorded as ported early by 34.2 gap cycle 2, removed from the slice-6 outstanding count"
      contains: "logError"
  key_links:
    - from: "src/backend/sidecar/handlers.ts"
      to: "src/backend/sidecar/loggerFlowRegistration.ts"
      via: "import + module-scope call alongside registerAppShellFlows/registerGameDetailsFlows"
      pattern: "registerLoggerFlows\\(\\)"
    - from: "a `send`-kind logError RPC frame"
      to: "the sidecar log file"
      via: "listenerRegistry → registerLoggerFlows body → backend/logger logError → LogWriter"
      pattern: "logError\\("
---

<objective>
Close the sidecar half of **verification gap #1 (GAP-CYCLE-1 CR-01, REQ-34.2-12)**:
`window.api.logError` is a `send`-kind channel whose ONLY registration in the entire backend is
`src/backend/logger/ipc_handler.ts:15`, a file imported only by `main.ts` (Electron). Nothing under
`src/backend/sidecar/` registers it. Under Tauri, `dispatchSend` (`sidecarRpc.ts:138-150`) looks up
an empty listener array and does nothing: no reject, no timeout, no `UNPORTED_CHANNEL_MARKER`, no
console output, no log line. This repo has already recorded that failure class
(`sidecar-send-channels-fail-silently`, G-30-01).

Gap cycle 1's renderer fix routed real repair failures into exactly that void, which is strictly
worse than the unhandled rejection it replaced. This plan makes the channel live and PROVES it with
a positive side effect — the phase's own stated standard (REQ-34.2-08/09) says absence of a thrown
error is not acceptable evidence for a `send` channel.

`logError` is assigned to Phase 34.3 (slice 6, `.planning/IPC-PORT-INVENTORY.md` line 49). Porting
it here is an early port, not a scope grab — but it MUST be declared in both ledgers so slice 6 does
not register it a second time (a duplicate listener would double every frontend log line).

Purpose: an error channel that actually reaches an operator under the platform this phase exists to
port to. Output: a curated `loggerFlowRegistration.ts`, its wiring, a transport-level side-effect
proof, and honest bookkeeping in both ledgers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VERIFICATION.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW-GAP-CYCLE-1.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live tree — no codebase exploration required. -->

Electron's only registration (src/backend/logger/ipc_handler.ts:14-15):
  addListener('logInfo',  (e, message) => logInfo(message, LogPrefix.Frontend))
  addListener('logError', (e, message) => logError(message, LogPrefix.Frontend))
This file also registers getLogContent / showLogFileInFolder / uploadLogFile /
deleteUploadedLogFile / getUploadedLogFiles — all Phase 34.3 (slice 6) channels. It must NOT be
side-effect-imported by the sidecar (D-04): it imports `backend/ipc`, which imports real electron.

Preload (src/preload/api/misc.ts:127):  export const logError = makeListenerCaller('logError')
  → send-kind, one string argument, no response frame.

Sidecar curated-registration shape (src/backend/sidecar/appShellFlowRegistration.ts:192-200):
  ipcMain.on('changeLanguage', (_event: unknown, ...args: unknown[]) => { ... })
where `ipcMain` is imported from './electronStub' (never from 'electron', never `backend/ipc`).

Registration site (src/backend/sidecar/handlers.ts:73-107): a flat list of module-scope calls —
registerSteamFlows(); registerSteamAuthFlows(); registerInstallFlows(); registerSettingsFlows();
registerDialogFlows(); registerDownloadQueueFlows(); registerAppShellFlows();
registerGameDetailsFlows(); registerEnrichmentFlows(); then ensureStoresRegistered(); then
registerStoreWriteHandlers().

Logging entry point (src/backend/logger/index.ts): exports logError(input, prefix?, options?) and
LogPrefix (from ./constants). log_writer.ts's writeString() is async and not awaited by logError —
a file-content assertion must poll, not assume a single flush turn.

Test transport helpers to mirror (src/backend/sidecar/__tests__/gameDetailsFlows.test.ts):
  startSidecar() → { input, frames } via init(input, output) over PassThrough streams
  writeSend(input, id, channel, args) → input.write(JSON.stringify({id, kind:'send', channel, args}) + '\n')
  listenerRegistry / handlerRegistry imported from '../electronStub'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loggerFlowRegistration.ts and wire it into handlers.ts</name>
  <files>src/backend/sidecar/loggerFlowRegistration.ts, src/backend/sidecar/handlers.ts</files>
  <read_first>
    - src/backend/logger/ipc_handler.ts (the source-of-truth body being mirrored — line 15)
    - src/backend/sidecar/appShellFlowRegistration.ts (the curated send-kind registration shape: module docstring conventions, `ipcMain.on(channel, (_event: unknown, ...args: unknown[]) => ...)`, the invoke-vs-send warning block)
    - src/backend/sidecar/handlers.ts (the file being modified — read the full module docstring and the registration call list at lines 71-114)
    - .planning/IPC-PORT-INVENTORY.md (confirm with your own eyes that `logError` currently sits in the Phase 34.3 slice-6 list before you port it early)
  </read_first>
  <action>
Create `src/backend/sidecar/loggerFlowRegistration.ts` exporting a single function
`registerLoggerFlows(): void` that registers exactly ONE channel:

- `ipcMain.on('logError', ...)` — send-kind — whose body calls `logError(<first arg>, LogPrefix.Frontend)`
  using the imports `{ logError, LogPrefix }` from `'backend/logger'`. Behavior must be identical to
  `logger/ipc_handler.ts:15`. Follow `appShellFlowRegistration.ts`'s `(_event: unknown, ...args: unknown[])`
  parameter shape and cast the first argument the same way its send bodies do; do not widen or
  reformat the message.

Import `ipcMain` from `'./electronStub'` — never from `'electron'`, never `backend/ipc`, and never
side-effect-import `backend/logger/ipc_handler.ts` (D-04: it registers five OTHER slice-6 channels
and pulls `backend/ipc` → real electron into the sidecar graph). `backend/logger` itself is already
in the sidecar graph (`processGuards.ts` imports `logWarning` from it), so this adds no new reach.

Register ONLY `logError`. Do not opportunistically add `logInfo`, `getLogContent`,
`showLogFileInFolder`, `uploadLogFile`, `deleteUploadedLogFile`, or `getUploadedLogFiles` — those
remain Phase 34.3 work and porting them here would be an undeclared scope grab.

The module docstring must state, in this order: (a) that this closes 34.2 gap cycle 2 / CR-01 /
REQ-34.2-12; (b) that `logError` is an IPC-PORT-INVENTORY **slice 6 (Phase 34.3)** channel ported
early because gap cycle 1's renderer repair-failure handler routes into it and would otherwise be a
silent no-op under Tauri; (c) an explicit instruction that Phase 34.3 must NOT register `logError`
again — a second listener would duplicate every frontend log line, since `dispatchSend` iterates all
listeners; (d) the send-vs-invoke warning carried by the sibling registration modules.

In `src/backend/sidecar/handlers.ts`: add the `registerLoggerFlows` import alongside the other
registration imports and call it in the module-scope list, placed with the other curated
registrations BEFORE `ensureStoresRegistered()`. Add a comment above the call recording the
early-port and pointing at the ledger rows Task 3 writes. Extend the file's module docstring to name
the new module, matching how it already names `registerAppShellFlows`/`registerGameDetailsFlows`/
`registerEnrichmentFlows`.
  </action>
  <acceptance_criteria>
    - `grep -c "ipcMain.on('logError'" src/backend/sidecar/loggerFlowRegistration.ts` returns exactly 1
    - `grep -v '^\s*\*' src/backend/sidecar/loggerFlowRegistration.ts | grep -cE "ipcMain\.(on|handle)\("` returns exactly 1 (no other channel was registered)
    - `grep -c "from 'electron'" src/backend/sidecar/loggerFlowRegistration.ts` returns 0, and `grep -c "logger/ipc_handler" src/backend/sidecar/loggerFlowRegistration.ts` returns 0
    - `grep -c 'registerLoggerFlows()' src/backend/sidecar/handlers.ts` returns exactly 1
    - The docstring contains the literal strings `34.3` and `must NOT register` (or equivalent explicit prohibition) — verifiable by grep
    - `npx tsc --noEmit` exits 0
    - `npx jest --testPathPattern='src/backend/sidecar/__tests__/(electronUntouched|gameDetailsImportGate)'` still passes — the new module must not trip the electron-freedom or import gates
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx jest --testPathPattern='src/backend/sidecar/__tests__/(electronUntouched|gameDetailsImportGate|electronReachLedger)'</automated>
  </verify>
  <done>`logError` is registered exactly once on the sidecar through a curated module, with the early-port prohibition recorded in code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the log line is observable through the real sidecar transport</name>
  <files>src/backend/sidecar/__tests__/loggerFlows.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/gameDetailsFlows.test.ts — lines 100-350 are the harness being copied: the `jest.mock('os', ...)` redirect, the `jest.mock('../pathShim', ...)` factory (lines 136-161), the electron/electron-store/axios/environment/config/online_monitor mocks, and the `beforeAll` containment tripwire (lines 318-331). Also lines 400-450 for `startSidecar`/`writeSend`/`flush`.
    - src/backend/logger/paths.ts — `getBaseLogPath()` reads `process.env.LOCALAPPDATA`/`XDG_STATE_HOME` and `homedir()`; it is NOT covered by the pathShim mock, which is why this suite must mock it (WR-01)
    - src/backend/logger/log_writer.ts — `writeString()` is async and `#archiveOldLogFile()` does a real `renameSync`; this is the destructive write the containment kit exists to contain
    - src/backend/sidecar/loggerFlowRegistration.ts (Task 1's output — the body under test)
    - src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts — lines 158-166 explain why `backend/logger` must be wrapped with `jest.spyOn`, never `jest.mock` (the logger/log_writer circular require)
  </read_first>
  <behavior>
    - `listenerRegistry.get('logError')?.length` is exactly 1 after `init()` — never 0 (unported) and never 2 (double registration)
    - Writing a well-formed `send` frame for `logError` with a unique marker string causes that marker to appear in the sidecar's log file within a bounded poll
    - The real `backend/logger` `logError` export was called with the frame's message and `LogPrefix.Frontend`
    - No response frame is produced for the send frame's id, and no `UNPORTED_CHANNEL_MARKER` appears anywhere in the collected frames
  </behavior>
  <action>
Create `src/backend/sidecar/__tests__/loggerFlows.test.ts`. It must ship with the FULL containment
kit from day one — this cycle exists because gap cycle 1 created a new suite without it.

Containment kit (all four elements are mandatory):
1. `jest.mock('os', ...)` redirecting `homedir()` to a per-suite tmp root named
   `gamelib-loggerflows-test-home-${process.pid}` under `os.tmpdir()`.
2. `jest.mock('../pathShim', ...)` — copy the factory shape from `gameDetailsFlows.test.ts:136-161`
   verbatim except for the tmp-root name, with NO platform branch and NO env-var reference. This is
   the real choke point: `resolveAppDataDir()` prefers `env.APPDATA`/`env.XDG_CONFIG_HOME` over
   `homedir()` on Windows/Linux.
3. `jest.mock('backend/logger/paths', ...)` — the log path is computed independently of `pathShim`
   from `process.env.LOCALAPPDATA`/`XDG_STATE_HOME` plus `homedir()`, so it bypasses both mocks
   above. Mock `getLogFilePath` to derive its path under the SAME per-suite tmp root, mirroring the
   real relative-path derivation (heroic log vs runner log vs game log) so distinct arguments still
   yield distinct paths. The factory must NOT call `jest.requireActual('backend/logger')` — that
   re-enters the logger/log_writer circular require and throws inside `LogWriter`'s constructor.
4. A top-level `beforeAll` containment tripwire using the `resolve`+`relative` idiom (never `join`,
   never a bare `startsWith` on the raw string) asserting that `appFolder`, `userDataPath`,
   `fixesPath` AND `getLogFilePath({})` all resolve inside `os.tmpdir()`, throwing a loud
   "REFUSING TO RUN" error otherwise. Copy the message shape from `gameDetailsFlows.test.ts:318-331`.

Suite body — drive the REAL, unmocked transport (`init(input, output)` over `PassThrough` streams,
as `gameDetailsFlows.test.ts` does; mock only the boundary modules that would do network I/O:
axios, online_monitor, backend/config, backend/constants/environment, electron→electronStub,
electron-store→fileStore). Ensure the mocked `GlobalConfig.get()` settings object does not disable
logging, or the writer will drop the line and the file assertion will never satisfy.

Tests:
- Registration count: `expect(listenerRegistry.get('logError')?.length).toBe(1)`.
- POSITIVE SIDE EFFECT (the load-bearing test): write a `send` frame for `logError` carrying a
  message containing a unique marker (embed `process.pid` and a random suffix so a stale file can
  never satisfy it), then poll — bounded, e.g. up to 50 iterations alternating `setImmediate` and a
  short timer — reading `getLogFilePath({})` until it exists and contains the marker. Assert the
  marker is present. Do NOT assert merely that nothing threw; that is the exact evidence standard
  REQ-34.2-08/09 rejects for send channels.
- Secondary: `jest.spyOn` the real `backend/logger` module's `logError` (spyOn, never jest.mock) and
  assert it was called with the frame's message and `LogPrefix.Frontend`.
- Negative: assert the collected output frames contain no entry for the send frame's id and no
  `UNPORTED_CHANNEL_MARKER` anywhere.

Add a RED-PROOF comment naming the exact line that makes the positive test fail if reverted — the
`registerLoggerFlows()` call in `handlers.ts`. Perform that revert once, confirm the positive test
goes red (and that it goes red SILENTLY at the transport, i.e. by the marker never appearing rather
than by an exception), restore, and record the observation in the SUMMARY.

Do not write a "NO FILESYSTEM WRITES" claim anywhere in this file — `init()` demonstrably writes a
real log file; say so accurately in the docstring, naming the redirected tmp root.
  </action>
  <acceptance_criteria>
    - `npx jest --testPathPattern=loggerFlows` passes
    - `grep -c "jest.mock('../pathShim'" src/backend/sidecar/__tests__/loggerFlows.test.ts` returns 1
    - `grep -c "backend/logger/paths" src/backend/sidecar/__tests__/loggerFlows.test.ts` returns 1 or more (the log-path mock is present)
    - `grep -c 'getLogFilePath' src/backend/sidecar/__tests__/loggerFlows.test.ts` returns 2 or more, and the containment `beforeAll` loop includes it
    - `grep -ci 'NO FILESYSTEM WRITES' src/backend/sidecar/__tests__/loggerFlows.test.ts` returns 0
    - The positive test reads real file content: `grep -cE 'readFileSync|readFile\(' src/backend/sidecar/__tests__/loggerFlows.test.ts` returns 1 or more
    - RED spot-check recorded in the SUMMARY: with `registerLoggerFlows()` removed from `handlers.ts`, the positive side-effect test fails by timeout/marker-absence (not by an exception), demonstrating the silent-send failure mode first-hand
    - Running the suite leaves no new files or directories outside `os.tmpdir()`: after the run, `ls ~/Library/Application\ Support/GameLib` (macOS) shows no modification timestamps from this run, and `git status --porcelain` shows no unexpected files
  </acceptance_criteria>
  <verify>
    <automated>npx jest --testPathPattern=loggerFlows</automated>
  </verify>
  <done>A `logError` frame over the real transport is proven to land in the sidecar log file, and the suite is contained inside os.tmpdir() by construction.</done>
</task>

<task type="auto">
  <name>Task 3: Declare the early port in both ledgers</name>
  <files>.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md, .planning/IPC-PORT-INVENTORY.md</files>
  <read_first>
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md (the file being modified — read sections 1 "Port kinds", 2 "The 26 channels", 5 "Re-deferrals", 6 "Sign-off", and 7 "Reconciliation" incl. the gap-cycle-1 reconciliation subsection at line 346, whose format the new subsection mirrors)
    - .planning/IPC-PORT-INVENTORY.md (the file being modified — read the "Totals", "Already ported (27)" and "Phase 34.3 — Slice 6" sections; `logError` currently appears on line 49 in the slice-6 list)
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VERIFICATION.md (gap #1 — the finding being closed)
  </read_first>
  <action>
Update `34.2-PORTED-CHANNELS.md` with a new subsection under section 7 titled for gap cycle 2
(mirroring the existing "Gap cycle 1 reconciliation" subsection's format). It must record:
- That `logError` — an IPC-PORT-INVENTORY **slice 6 / Phase 34.3** channel, NOT one of this slice's
  26 — is now registered on the sidecar by `loggerFlowRegistration.ts`, and why: gap cycle 1's
  renderer repair-failure handler logs through it, and an unregistered `send` channel is a total
  silent no-op under Tauri (`sidecar-send-channels-fail-silently` / G-30-01).
- That the headline 26-channel tally for this slice is UNCHANGED — `logError` is declared as an
  out-of-slice early port and must not be counted toward slice 5.
- Its port kind (`sidecar send`) and its honest proof level: proven by a positive side effect (the
  message appears in the sidecar log file over the real transport), not by absence of a marker.
- The explicit instruction that Phase 34.3 must not register `logError` a second time, and that the
  other five `logger/ipc_handler.ts` channels (`logInfo`, `getLogContent`, `showLogFileInFolder`,
  `uploadLogFile`, `deleteUploadedLogFile`, `getUploadedLogFiles`) remain slice-6 work and are still
  unported — so no reader can take this row for "logging is ported".

Update `.planning/IPC-PORT-INVENTORY.md`: remove `logError` from the Phase 34.3 slice-6 channel list
and add it to the "Already ported" list, adjusting BOTH affected counts in the same edit (the
already-ported total and the slice-6 total, plus the "Totals" section if it carries a derived
number). Annotate the moved entry with "ported early by Phase 34.2 gap cycle 2 (plan 34.2-16)" so
the Phase 35 completion gate reconciles. If any count in the document cannot be made to add up,
STOP and report the discrepancy rather than writing a number that does not reconcile.
  </action>
  <acceptance_criteria>
    - `grep -c 'logError' .planning/IPC-PORT-INVENTORY.md` returns 1 or more, and `logError` no longer appears in the Phase 34.3 slice-6 channel list (verify by reading that section)
    - The inventory's already-ported count and slice-6 count each changed by exactly 1 in the correct direction, and every total in the document still adds up (state the arithmetic in the SUMMARY)
    - `34.2-PORTED-CHANNELS.md` contains a gap-cycle-2 subsection naming `logError`, its `sidecar send` port kind, and the "not counted toward the 26" statement
    - `34.2-PORTED-CHANNELS.md` explicitly names the five still-unported sibling logging channels
    - No claim anywhere in either file states or implies that logging is ported for slice 6
  </acceptance_criteria>
  <verify>
    <automated>grep -n "logError" .planning/IPC-PORT-INVENTORY.md .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md</automated>
  </verify>
  <done>Both ledgers record the early port honestly, with reconciling counts and an explicit no-double-registration instruction for Phase 34.3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar `send` frame | Untrusted, renderer-controlled string arrives over the RPC transport and is written into the user's log file |
| sidecar → operator (log file) | The only channel by which a backend failure becomes visible to a human; a silent channel here is an observability failure, not a cosmetic one |
| test process → real user app-data / log directories | The new suite drives real `init()`/`initLogger()`; without containment it rotates the developer's real `gamelib.log` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-46 | Repudiation | `logError` send channel under Tauri | mitigate | Register the channel on the sidecar via `loggerFlowRegistration.ts`; prove observability with a positive side-effect test that reads the real log file over the real transport, not an absence-of-throw assertion |
| T-34.2-47 | Tampering | Renderer-supplied message written verbatim into `gamelib.log` (log-injection via embedded newlines) | accept | Exact parity with Electron's `logger/ipc_handler.ts:15`, which has always written the renderer string verbatim; the sidecar introduces no new exposure. Changing the log-escaping policy is a codebase-wide decision, out of scope for a gap-closure cycle — recorded here so it is not lost |
| T-34.2-48 | Denial of Service | Unbounded/high-frequency `logError` frames bloating the log file | accept | Parity with Electron; `LogWriter` already rotates `gamelib.log` → `.old`. No new amplification is introduced by registering the channel |
| T-34.2-49 | Elevation of privilege / integrity | Phase 34.3 registering `logError` a second time (duplicate listener → duplicated log lines, and any future non-idempotent body running twice) | mitigate | Declared in the module docstring, in `34.2-PORTED-CHANNELS.md`, and in `.planning/IPC-PORT-INVENTORY.md`; enforced by the `listenerRegistry.get('logError')?.length === 1` assertion in `loggerFlows.test.ts` |
| T-34.2-50 | Tampering | The NEW test suite writing to the real user config/log directory (recorded `tests-clobbering-real-steam-store` incident) | mitigate | Four-part containment kit (`os`, `pathShim`, `backend/logger/paths` mocks + a `resolve`/`relative` tripwire covering the log path) shipped with the suite from day one, not retrofitted in a later cycle |
| T-34.2-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages. Any apparent need for a new dependency must STOP the plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `cd src-tauri && cargo check --quiet` exits 0 (unchanged — no Rust touched; `logError` is a
  `send` channel and needs no `LONG_RUNNING_CHANNELS` entry or new Tauri arm)
- `npx jest --testPathPattern=src/backend` shows no NEW failures against the baseline (108/109
  suites, 2237/2238 tests; sole failure remains `rustInvokeChannel.test.ts`); suite count rises by
  exactly 1 for the new file
- `electronUntouched.test.ts`, `gameDetailsImportGate.test.ts` and `electronReachLedger.test.ts`
  remain green — the new sidecar module must not import real electron or widen the reach graph in a
  way those gates reject
- Electron behavior is unchanged: `logger/ipc_handler.ts` and `main.ts` are not modified, so
  `window.api.logError` under Electron still lands in the same place (REQ-34.2-14)
</verification>

<success_criteria>
- A `logError` frame over the real sidecar transport produces a real log line, proven by reading the
  file — the silent-`send` failure class is closed for this call site.
- Exactly one listener is registered for the channel, and the early port is declared in both ledgers
  with reconciling counts.
- The new test suite is contained inside `os.tmpdir()` by construction — no fourth gap.
</success_criteria>

<output>
Create `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-16-SUMMARY.md` when done, including the RED spot-check observation and the inventory count arithmetic.
</output>
