---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 24
type: execute
wave: 3
gap_closure: true
gap_cycle: 3
depends_on: [34.2-19, 34.2-20, 34.2-21, 34.2-22, 34.2-23]
files_modified:
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/deferred-items.md
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VALIDATION.md
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/currency-gate.py
autonomous: true
requirements: [REQ-34.2-13, REQ-34.2-07, REQ-34.2-12, REQ-34.2-14]

must_haves:
  truths:
    - "34.2-PORTED-CHANNELS.md is current with respect to the gap-cycle-2 review and the third verification, closing the currency gap flagged in all three rounds"
    - "deferred-items.md no longer describes the 11-suite containment hole as open, because it is closed structurally"
    - "Every gap-cycle-2 finding NOT fixed by this cycle is recorded as explicitly deferred with a stated reason and a natural home"
    - "34.2-VALIDATION.md's per-task verification map covers gap cycle 3's tasks, and its Test Infrastructure section records the two new gates"
    - "A re-runnable currency gate fails when the newest cycle's reconciliation is missing or incomplete"
  artifacts:
    - path: ".planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md"
      provides: "Gap cycle 3 reconciliation section and a refreshed honest sign-off"
      contains: "Gap cycle 3 reconciliation"
    - path: ".planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/deferred-items.md"
      provides: "Corrected containment-debt entry plus the explicitly-deferred residual findings"
    - path: ".planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/currency-gate.py"
      provides: "Re-runnable doc-currency check over 34.2-PORTED-CHANNELS.md"
  key_links:
    - from: "34.2-PORTED-CHANNELS.md gap cycle 3 section"
      to: "34.2-REVIEW-GAP-CYCLE-2.md findings CR-01/WR-01/WR-02/WR-03"
      via: "a per-finding disposition list"
      pattern: "WR-0[123]"
    - from: "currency-gate.py"
      to: "34.2-PORTED-CHANNELS.md"
      via: "section-presence and finding-ID coverage assertions"
      pattern: "Gap cycle 3 reconciliation"
---

<objective>
Close the recurring currency gap. REQ-34.2-13 (`34.2-PORTED-CHANNELS.md` declares all 26 channels
honestly) has been marked **VERIFIED but stale re: the newest review** in all three verification
rounds — the document reflects the previous cycle's closures and predates the review that
triggered the current one. The verifier is explicit that this is a currency-gap class, not a
dishonesty finding, but it recurs every round because nothing makes the document catch up.

This plan brings all three of this phase's living documents current with gap cycle 3, and adds a
committed doc-shape script — mirroring plan 34.2-07's own `python3` doc-shape precedent — that
fails when the newest cycle's reconciliation is missing or incomplete, so "stale" becomes a red
gate rather than a verifier's observation.

It also records, explicitly and with reasons, every gap-cycle-2 finding this cycle did NOT fix.
Silently dropping them is exactly how the last two cycles left residue.

Purpose: stop the currency gap from recurring a fourth time, and leave an honest record of what
was closed versus deferred.
Output: updated `34.2-PORTED-CHANNELS.md`, `deferred-items.md`, `34.2-VALIDATION.md`, and a
committed `currency-gate.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VERIFICATION.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW-GAP-CYCLE-2.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-19-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-20-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-21-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-22-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-23-SUMMARY.md

<interfaces>
<!-- Structure of the documents being edited. -->

34.2-PORTED-CHANNELS.md section headings (current):
  `## 1. Port kinds` · `## 2. The 26 channels`
  (+ `### The two bootstrap wirings (not channels — do not count these in the 26)`)
  `## 3. Declaration riders` · `## 4. Declared divergences from the Electron build`
  `## 5. Re-deferrals` · `## 6. Sign-off — the honest statement`
     (+ `### What gap cycle 1 changed about the honesty of this sign-off`
      + `### How gap cycle 1 touches the two deferred HUMAN-UAT items (recorded, not resolved)`)
  `## 7. Reconciliation`
     (+ `### Gap cycle 1 reconciliation (plans 34.2-08..34.2-14)`
      + `### Gap cycle 2 reconciliation (plan 34.2-16 — CR-01 sidecar half)`)
  → Gap cycle 3's section belongs as a new `###` subsection at the END of §7, matching that shape.

deferred-items.md sections (current):
  `## From plan 34.2-03` · `## From plan 34.2-07`
  `## From the phase-level post-merge test gate (orchestrator, /gsd-execute-phase 34.2)`
     — contains the rustInvokeChannel.test.ts bisection; this is the BASELINE, preserve verbatim
  `## From plan 34.2-18 (gap cycle 2, CR-03/WR-01 closure)`
     — the 11-suite containment-hole entry that gap cycle 3 makes obsolete

34.2-VALIDATION.md sections: Test Infrastructure · Sampling Rate · Per-Task Verification Map
  (20 rows across plans 01-07; columns: Task ID | Plan | Wave | Requirement | Threat Ref |
  Secure Behavior | Test Type | Automated Command | File Exists | Status) · Wave 0 Requirements ·
  Manual-Only Verifications · Validation Sign-Off. Gap cycles 1 and 2 did not extend the map;
  gap cycle 3 does.

Gap-cycle-2 review findings and their disposition in THIS cycle:
  CR-01 (11-suite hole / bootstrap.test.ts real-log destruction)      → FIXED, plan 34.2-19
  WR-01 (vacuous NO FILESYSTEM WRITES gate)                           → FIXED, plan 34.2-23 Task 1
  WR-02 (floating promise in the logError listener)                   → FIXED, plan 34.2-20
  WR-03 (${unknown} interpolation in repairFailure.ts)                → FIXED, plan 34.2-21
  WR-04 (load-bearing mock mislabelled "parity only")                 → FIXED, plan 34.2-23 Task 1
  WR-07 (beforeAll/afterAll process.platform leak)                    → FIXED, plan 34.2-23 Task 1
  WR-08 (fixed 4-entry list blind to growth)                          → FIXED, plan 34.2-23 Task 2
  IN-02 (ts-prune: ReportRepairFailureOptions)                        → addressed, plan 34.2-21 Task 2
  IN-05 (unchecked `args[0] as string` cast)                          → FIXED, plan 34.2-20
  Carried-forward: no Rust test proves timeout_for() consults
                   LONG_RUNNING_CHANNELS                              → FIXED, plan 34.2-22
  WR-05 (tmp-root string duplicated 3x per suite, inconsistently)     → NOT fixed this cycle
  WR-06 (hoisted jest.mock factory referencing an out-of-scope const) → NOT fixed this cycle
  IN-01 (prettier violations; gate already red at baseline)           → NOT fixed this cycle
  IN-03 (unrestored jest.spyOn in two harness helpers)                → NOT fixed this cycle
  IN-06 (MaxListenersExceededWarning from repeated init())            → NOT fixed this cycle
  IN-04 (renderer-controlled log injection, Electron parity)          → ACCEPTED, see 34.2-20 T-34.2-69
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the gap cycle 3 reconciliation to 34.2-PORTED-CHANNELS.md and refresh the sign-off</name>
  <files>.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md (the file being modified — read §6 and §7 in full, including both existing gap-cycle reconciliation subsections, so the new one matches their shape and level of specificity)
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VERIFICATION.md (the findings being reconciled — the `regressions:` block and the "New Findings From This Verification" table)
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW-GAP-CYCLE-2.md (CR-01, WR-01..WR-08, IN-01..IN-06 in full)
    - The five wave-1/wave-2 SUMMARY files 34.2-19-SUMMARY.md through 34.2-23-SUMMARY.md — the recorded RED proofs, stat timestamps and decisions this section reports
    - .planning/IPC-PORT-INVENTORY.md (section "Phase 34.2 — Slice 5" — to confirm the 26-channel set is untouched by this cycle)
  </read_first>
  <action>
    Add a `### Gap cycle 3 reconciliation (plans 34.2-19..34.2-23)` subsection at the END of §7,
    matching the depth and tone of the two existing reconciliation subsections.

    It must state, concretely:
    - **What this cycle fixed and where** — one bullet per finding, naming the finding ID, the
      plan that closed it, the file changed, and the RED proof that demonstrates the fix is real.
      Pull the verbatim evidence out of the SUMMARY files rather than paraphrasing it. Cover
      CR-01, WR-01, WR-02, WR-03, WR-04, WR-07, WR-08, IN-02, IN-05, and the carried-forward
      `timeout_for` item.
    - **That containment is now structural** — name `src/backend/jest.setupContainment.ts` and the
      backend project's `setupFiles` entry, and state plainly that the previous cycle's
      `KNOWN_UNCOVERED_BOOTSTRAP_DRIVING_SUITES` declaration is gone because the hole it described
      is closed, not because the declaration was inconvenient.
    - **The live evidence, verbatim** — the `stat` before/after timestamps from plan 34.2-19's
      Task 3 showing the developer's real `~/Library/Logs/GameLib/gamelib.log` and its `.old`
      sibling are untouched across a full sidecar suite run. This is the direct refutation of the
      verification's own three-times-reproduced destruction finding and is the single most
      load-bearing statement in the section.
    - **What this cycle did NOT fix** — one bullet each, with a reason: WR-05, WR-06, IN-01,
      IN-03, IN-06. Do not soften them into "minor". State the finding and the reason (for
      WR-05/WR-06, that structural containment demoted the per-suite tmp-root mocks to defence in
      depth, so a desync between them can no longer produce an uncontained write; for IN-01, that
      `prettier --check` is red at baseline across dozens of pre-existing files, so this cycle
      neither regresses nor greens that gate).
    - **What this cycle did NOT touch** — the 26 ported channels. State that the set equality
      against `.planning/IPC-PORT-INVENTORY.md`'s slice-5 heading is unchanged, that no channel
      was added, removed or re-kinded, and that all 14 requirement-level truths passed on their
      literal text in the third verification. This cycle's work is entirely test-safety and
      error-path hardening.

    Then update §6's sign-off so it is current. Add a short block matching the shape of the
    existing "What gap cycle 1 changed about the honesty of this sign-off", recording what gap
    cycles 2 and 3 changed. Do NOT weaken or delete the existing honest-boundary paragraph (the
    "do not read 'wired and unit-proven' as 'seen working'" statement) or the two named
    deferred-UAT items; they are unaffected by this cycle and their `pending` status is unchanged.

    Do not alter §2's 26-channel table, §3's three declaration riders, or gap cycle 2's `logError`
    early-port reconciliation — none of them changed.
  </action>
  <acceptance_criteria>
    - `34.2-PORTED-CHANNELS.md` contains exactly one `Gap cycle 3 reconciliation` heading:
      `grep -c 'Gap cycle 3 reconciliation' <file>` returns 1.
    - Every finding ID this cycle closed appears in that section: `CR-01`, `WR-01`, `WR-02`,
      `WR-03`, `WR-04`, `WR-07`, `WR-08`, `IN-02`, `IN-05`, and the string `timeout_for`.
    - Every finding NOT fixed appears with a reason: `WR-05`, `WR-06`, `IN-01`, `IN-03`, `IN-06`.
    - The verbatim `stat` timestamps from plan 34.2-19's Task 3 appear in the section.
    - §2's channel table is byte-unchanged: `git diff` on the file shows no `+`/`-` lines between
      the `## 2. The 26 channels` heading and the `## 3. Declaration riders` heading.
    - The two deferred HUMAN-UAT items are still recorded as pending: `grep -c 'UAT-34.2-01'` and
      `grep -c 'UAT-34.2-02'` each return at least 1, and neither is marked resolved.
    - No new placeholders introduced: `grep -cE 'TBD|TODO|FIXME|XXX' <file>` returns the same count
      as before the edit. Record both counts in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Gap cycle 3 reconciliation' .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md</automated>
  </verify>
  <done>The declaration document reports gap cycle 3's closures, its deferrals with reasons, and the live evidence that the data-loss blocker is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct deferred-items.md and record the residual findings as explicit, reasoned deferrals</name>
  <files>.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/deferred-items.md</files>
  <read_first>
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/deferred-items.md (the file being modified — read all four existing sections; the rustInvokeChannel.test.ts baseline bisection must be preserved verbatim)
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW-GAP-CYCLE-2.md (WR-05, WR-06, IN-01, IN-03, IN-04, IN-06 — the findings being deferred, with their own reproduction detail)
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-23-SUMMARY.md (what the derived tripwire now enforces, which determines how much of the old debt entry is genuinely obsolete)
    - src/backend/jest.setupContainment.ts (the mechanism that made the old entry obsolete)
  </read_first>
  <action>
    Rewrite the `## From plan 34.2-18 (gap cycle 2, CR-03/WR-01 closure)` section. Its current text
    states that 11 named sidecar suites "also drive the REAL, unmocked `bootstrap.init()` and
    therefore carry the SAME pre-existing risk class", and nominates "a small follow-up plan
    applying the same kit to each of the 11 files" as the natural home. That is no longer true:
    plan 34.2-19 closed it structurally for the whole backend project.

    Do NOT delete the entry. Rewrite it as a CLOSED entry: keep the original description of what
    the debt was (it is the historical record of a real, reproduced data-loss defect), then append
    a dated resolution block naming plan 34.2-19, `src/backend/jest.setupContainment.ts`, the
    `setupFiles` mechanism, and the live `stat` evidence. Append a `— CLOSED by gap cycle 3` suffix
    to the section heading so a reader scanning for open debt does not stop on it. An entry
    silently deleted looks like it never happened; an entry left open looks like the defect is
    still live. Neither is honest.

    Add a new section `## From gap cycle 3 (plans 34.2-19..34.2-23)` recording the findings this
    cycle deliberately did not fix. For each: the finding ID, the file and line region, a
    one-paragraph statement of what the defect is (a reader of this file must not have to open
    another document), the reason for deferring, and a natural home. Cover:
    - **WR-05** — the containment tmp-root string duplicated three times per suite across five
      files, inconsistently, with nothing asserting the three roots agree. Reason: structural
      containment demoted these per-suite mocks to defence in depth, so a desync between them can
      no longer produce a write outside `os.tmpdir()`; the tripwires pass either way because all
      three roots remain under tmpdir. Still worth fixing for legibility.
    - **WR-06** — hoisted `jest.mock` factories referencing an out-of-scope `const`
      (`TMP_ROOT_NAME`), surviving only because nothing calls `os.homedir()` during module
      evaluation, and which `babel-jest` would reject outright. Reason: same as WR-05; the fix is
      naturally bundled with it (rename to a `mock`-prefixed identifier).
    - **IN-01** — prettier violations in five changed files. Reason: `prettier --check` is red at
      baseline across dozens of pre-existing files, so the gate cannot distinguish this cycle's
      contribution and fixing only these five changes no CI outcome. Record separately whether gap
      cycle 3's OWN new files are prettier-clean, since those are this cycle's responsibility.
    - **IN-03** — unrestored `jest.spyOn` in two `sidecarRejectionGuard.test.ts` harness helpers,
      benign because each runs inside a fresh `jest.isolateModules()` registry, but the stated
      rationale for narrowing `loadConstantsPaths()` was only half-applied.
    - **IN-06** — `MaxListenersExceededWarning` from repeated `init()` calls attaching
      process-level listeners that are never removed. Reason: cosmetic in tests, but the same
      accumulate-without-cleanup shape that would matter if `init()` were ever called twice in
      production; the real fix belongs with `bootstrap.init()`'s idempotency, not with a test.

    Also record **IN-04** (renderer-controlled log injection through the newly-ported `logError`
    channel) as an ACCEPTED risk rather than a deferral, cross-referencing plan 34.2-20's threat
    model entry T-34.2-69, with the reason: exact parity with the Electron handler
    (`src/backend/logger/ipc_handler.ts:15`), so changing it here would be an undeclared
    behavioural divergence between the two builds, which REQ-34.2-14 forbids. Natural home: a
    single change in `LogWriter#writeString`.

    Leave the `## From the phase-level post-merge test gate` section byte-unchanged — the
    `rustInvokeChannel.test.ts` bisection is the documented baseline this cycle's acceptance
    criteria depend on, and it remains open 34.1-era debt.
  </action>
  <acceptance_criteria>
    - The plan-34.2-18 section heading carries a closure marker:
      `grep -c 'CLOSED by gap cycle 3' <file>` returns at least 1, and the section body still
      contains the original 11 suite names (historical record preserved, not deleted).
    - A `From gap cycle 3` section exists and names all six residual findings:
      `grep -oE 'WR-05|WR-06|IN-01|IN-03|IN-04|IN-06' <file> | sort -u | wc -l` returns 6.
    - The `rustInvokeChannel.test.ts` baseline section is byte-unchanged: `git diff <file>` shows
      no `+`/`-` lines between the `## From the phase-level post-merge test gate` heading and the
      next `##` heading.
    - Each deferred entry states both a reason and a natural home — verified by reading; no entry
      consists solely of a finding ID and a citation.
    - `grep -c 'setupFiles' <file>` returns at least 1 (the resolution names the mechanism).
  </acceptance_criteria>
  <verify>
    <automated>grep -oE 'WR-05|WR-06|IN-01|IN-03|IN-04|IN-06' .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/deferred-items.md | sort -u | wc -l</automated>
  </verify>
  <done>The obsolete containment-debt entry is marked closed with its evidence; every unfixed gap-cycle-2 finding is recorded with a reason and a natural home; the pre-existing baseline entry is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Extend 34.2-VALIDATION.md and commit a currency gate so "stale" becomes a failing check</name>
  <files>.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VALIDATION.md, .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/currency-gate.py</files>
  <read_first>
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VALIDATION.md (the file being modified — read the Test Infrastructure table, the Per-Task Verification Map's exact column shape, and the Validation Sign-Off checklist)
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-07-PLAN.md (the precedent for python3 doc-shape scripts used as automated verification — mirror it rather than inventing a new approach)
    - The five SUMMARY files 34.2-19-SUMMARY.md through 34.2-23-SUMMARY.md — the actual commands, test counts and statuses each new row must report
    - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md (as updated by Task 1 — the document the currency gate checks)
  </read_first>
  <action>
    Extend `34.2-VALIDATION.md` in three places, then commit the currency gate as a file.

    **Test Infrastructure table.** Add two rows for the gates gap cycle 3 introduced. The first
    names `src/backend/jest.setupContainment.ts` and the backend project's `setupFiles` entry as
    the structural containment mechanism, stating that it applies to every `src/backend` suite
    rather than only the sidecar directory, and why that blast radius was chosen. The second names
    `cd src-tauri && cargo test` as the Rust-side behavioral gate, recording explicitly that this
    project's CI runs NO cargo step, so the command is manual and its existence is pinned from the
    jest side by `src/backend/__tests__/longRunningChannels.test.ts`. Update the Quick-run /
    Full-suite rows if plan 34.2-19's Task 3 found the documented baseline tallies changed.

    **Per-Task Verification Map.** Append one row per task across plans 34.2-19..34.2-23, using the
    existing ten-column shape. Populate Threat Ref from each plan's own `<threat_model>` IDs
    (the T-34.2-60..T-34.2-86 range) and Status from what the SUMMARY files actually record —
    green only where a green run was observed, with any drift noted in the row rather than
    silently normalised, exactly as the existing 20 rows do.

    **Validation Sign-Off.** Add a dated gap-cycle-3 addendum BENEATH the original approval block
    (do not edit the original — its provenance must stay legible). The addendum asserts: every
    gap-cycle-3 task carries a concrete automated command; every new test was hand-proven RED
    against pre-fix code, with a list naming each RED proof and the SUMMARY it is recorded in;
    every new gate carries a self-test; and the two deferred HUMAN-UAT items remain untouched and
    pending.

    **The currency gate.** Create
    `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/currency-gate.py`
    as a committed, re-runnable script rather than an inline one-liner, so a fourth gap cycle can
    extend it instead of re-deriving what "current" means. It must read
    `34.2-PORTED-CHANNELS.md` and assert:
    - a `Gap cycle 3 reconciliation` heading exists, exactly once;
    - every closed-finding token appears within that section: `CR-01`, `WR-01`, `WR-02`, `WR-03`,
      `WR-04`, `WR-07`, `WR-08`, `IN-02`, `IN-05`, `timeout_for`;
    - every deferred-finding token appears within it: `WR-05`, `WR-06`, `IN-01`, `IN-03`, `IN-06`;
    - the section is the LAST `###` subsection under `## 7. Reconciliation`, so a fourth cycle
      appending its own section keeps the ordering meaningful;
    - no `TBD`/`TODO`/`FIXME`/`XXX` placeholder appears anywhere in the document.
    On any failure it must exit non-zero and print the specific missing item — a gate whose failure
    message does not name the offender gets ignored. Keep the token lists in named module-level
    constants at the top of the file so extending them for a fourth cycle is a one-line edit, and
    add a header comment stating that purpose.
  </action>
  <acceptance_criteria>
    - `python3 .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/currency-gate.py`
      exits 0 against the Task 1 output. Record the full source and the exit code in the SUMMARY.
    - CURRENCY GATE RED PROOF recorded verbatim in the SUMMARY: temporarily remove the `WR-05`
      mention from `34.2-PORTED-CHANNELS.md`, re-run the script, confirm a non-zero exit whose
      message names `WR-05`, then restore and confirm `git diff` on that file is clean. This is the
      explicit "the gate can actually fail" requirement.
    - The Per-Task Verification Map contains a row for every task in plans 34.2-19..34.2-23 —
      count them and state the before/after row totals in the SUMMARY.
    - The Test Infrastructure table names both new gates:
      `grep -c 'jest.setupContainment' <file>` and `grep -c 'cargo test' <file>` each return at
      least 1.
    - Every appended row carries a non-empty Automated Command cell and a Threat Ref cell drawn
      from the T-34.2-60..T-34.2-86 range — verified by reading; no row uses a placeholder.
    - The original Validation Sign-Off approval block is unmodified — the gap-cycle-3 items are an
      addendum beneath it. Verify with `git diff` on that region.
    - The two Manual-Only Verifications rows (UAT-34.2-01, UAT-34.2-02) are byte-unchanged.
  </acceptance_criteria>
  <verify>
    <automated>python3 .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/currency-gate.py</automated>
  </verify>
  <done>The validation strategy covers gap cycle 3's tasks with real threat refs and observed statuses, and a committed script turns document staleness into a non-zero exit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declaration documents → downstream readers and Phase 35 | `34.2-PORTED-CHANNELS.md` is the artifact Phase 35's cutover gate reads to decide what is ported and at what proof level. A stale or over-claiming statement here propagates into a build decision. |
| deferred-items.md → future planners | An entry describing a closed defect as open, or a real defect as absent, misdirects the next gap cycle's scope. |
| this cycle's SUMMARY evidence → the reconciliation text | The reconciliation restates evidence produced elsewhere; a paraphrase can drift from what was actually observed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-87 | Repudiation (stale declaration) | `34.2-PORTED-CHANNELS.md` lagging the newest review — flagged in all three verification rounds | mitigate | Task 1 adds the gap-cycle-3 reconciliation; Task 3 commits `currency-gate.py`, which exits non-zero when the newest cycle's section is missing or incomplete. Hand-proven to fail by removing a required token. |
| T-34.2-88 | Repudiation (false closure claim) | The reconciliation asserting the data-loss blocker is closed | mitigate | The claim must carry the verbatim `stat` before/after timestamps from plan 34.2-19's Task 3, not a paraphrase. An acceptance criterion requires the literal timestamps to be present. |
| T-34.2-89 | Repudiation (silently dropped findings) | WR-05, WR-06, IN-01, IN-03, IN-06 disappearing between cycles | mitigate | Task 2 records each with a reason and a natural home; Task 3's gate asserts each token is present in the reconciliation section, so a future edit cannot quietly drop them. |
| T-34.2-90 | Tampering | An edit to the reconciliation that also alters the 26-channel table or the two deferred-UAT items | mitigate | Acceptance criteria pin §2's table byte-unchanged between its own heading and §3's, and pin the UAT rows byte-unchanged. This cycle ported no channel and resolved no UAT item; the documents must not imply otherwise. |
| T-34.2-91 | Information disclosure | Verbatim evidence pasted into a public planning document — `stat` output and log paths include the developer's home directory | accept | The paths are already present throughout this phase's verification and review documents, and the repository is a public fork whose `.planning/` tree already contains them. No credentials, tokens or file CONTENTS are reproduced — only paths and timestamps. Recorded rather than silently accepted. |
| T-34.2-92 | Denial of service | The currency gate itself becoming unmaintainable and being deleted by a fourth cycle | mitigate | Token lists live in named module-level constants with a header comment stating that extending them is the intended maintenance action, so the cheap path is to extend rather than delete. |

No package-manager installs and no source-code changes in this plan; the `T-{phase}-SC`
supply-chain threat does not apply. The only executable artifact is a standard-library-only
`python3` script.
</threat_model>

<verification>
- `grep -c 'Gap cycle 3 reconciliation' 34.2-PORTED-CHANNELS.md` returns 1.
- `grep -oE 'WR-05|WR-06|IN-01|IN-03|IN-04|IN-06' deferred-items.md | sort -u | wc -l` returns 6.
- `python3 .../currency-gate.py` exits 0, and is hand-proven to exit non-zero naming `WR-05` when
  that token is removed.
- `git diff` confirms §2's channel table, the UAT rows, the original Validation Sign-Off block and
  the `rustInvokeChannel.test.ts` baseline entry are all byte-unchanged.
</verification>

<success_criteria>
- All three living documents are current with gap cycle 3.
- Every gap-cycle-2 finding is accounted for: fixed (with the plan and its RED proof named),
  deferred (with a reason and a natural home), or accepted (with the reason).
- Document staleness is now a non-zero exit rather than a verifier's observation, and the gate is
  hand-proven to fail.
- Nothing about the 26 ported channels, the three declaration riders, or the two deferred-UAT
  items was changed by this cycle, and the documents say so.
</success_criteria>

<output>
Create `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-24-SUMMARY.md` when done.
Record verbatim: the currency-gate source and its exit code, the currency-gate RED proof output, the before/after Per-Task Verification Map row totals, and the before/after placeholder counts for 34.2-PORTED-CHANNELS.md.
</output>
