---
phase: 34.2
slug: tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
reviews_covered: [34.2-REVIEW.md]
findings_in_scope: 17
findings_closed: 14
findings_accepted: 1
findings_open: 2
status: partial
created: 2026-08-22
---

# Phase 34.2 — Review Fix Ledger (round 1)

Disposition ledger for the **round-1** code review, `34.2-REVIEW.md`
(3 critical / 10 warning / 4 info = 17). That file still reads `status: issues_found` and always
will — it is a historical record and is never rewritten when fixes land. This sibling is the
current disposition.

**Status is `partial`, deliberately, and it will not turn this phase green.** Of 17: 14 closed,
1 accepted by design, 2 open. `all_fixed` would turn the folder green and would be a false
claim over two live findings — the exact failure mode recorded against Phase 34.9, where
`all_fixed` over a deferred finding reproduced a four-time defect. The true signal survives;
the false one ("three unaddressed criticals exist") does not.

## Scope warning — review-round IDs COLLIDE

This phase ran **five** reviews (round 1 plus `34.2-REVIEW-GAP-CYCLE-{1,2,3,4}.md`), and **each
round restarts numbering at `CR-01`/`WR-01`**. Round 1's `WR-06` (path traversal in
`readKnownFixes`) and round 3's `WR-06` (a hoisted-factory TDZ naming nit) are unrelated findings
sharing a string. **Every row below is keyed by `(round, ID)`.** Anything that keys on the bare ID
merges findings that have nothing to do with each other.

**This ledger covers round 1 only.** The four gap-cycle reviews are NOT dispositioned here. Two of
them still read `issues_found`, and none of the four carries an explorer badge at all —
`artifactKind()` matches `name === kind || name.endsWith('-' + kind)`, so `-REVIEW-GAP-CYCLE-N.md`
matches nothing. They are invisible to tooling and remain outstanding.

## Closed (14)

| Round | ID | Finding | Closed by | Evidence (verified 2026-08-22, current code) |
|-------|----|---------|-----------|----------------------------------------------|
| 1 | CR-01 | `repair` served over the 60s invoke bound — spurious timeout + unhandled renderer rejection | 34.2-08 | `src-tauri/src/main.rs:117-142` — `repair` and `readConfig` present in `LONG_RUNNING_CHANNELS`, tradeoff recorded inline at `:198-199`; `GameSubMenu/index.tsx:149-155` wraps `repair()` and delegates to `reportRepairFailure()` without rethrowing |
| 1 | CR-02 | `void downloadAntiCheatData(...)` can reject and crash the sidecar | 34.2-09 | `sidecar/bootstrap.ts:517-524` — `.catch()` attached directly to the call, logs via `logWarning`, no rethrow. Independently corroborated live 2026-08-22: both branches of `downloadAntiCheatData` observed on real hardware during UAT-34.2-02 |
| 1 | CR-03 | New test suites destructively `rmSync`/overwrite paths not guaranteed redirected | 34.2-10, strengthened by 34.2-19 | Originally per-suite `jest.mock('../pathShim')` + `beforeAll` `resolve`+`relative` tripwires. Now **structurally** closed: `jest.config.js:23` registers `jest.setupContainment.ts` as a project-wide `setupFiles` entry, and its precondition (`:339-387`) throws `REFUSING TO RUN` across all backend suites if containment fails |
| 1 | WR-01 | Gates 7/8 ("byte-identical to HEAD") are tautologies that can never fail in CI | 34.2-12 | `gameDetailsImportGate.test.ts:283-297` and `:363-374` — real sha256 pins. Both digests **re-computed with `shasum` during the 2026-08-22 security audit and matched**; later re-pinned by Phase 34.5 with a stated in-file reason |
| 1 | WR-02 | Import gates are depth-1 textual matches while docstrings claim transitive electron-freedom | 34.2-11 | False claim replaced with the true direct-import-only invariant (`dispatch.ts:9-19`, `enrichmentFlowRegistration.ts:90-107`, `REQUIREMENTS.md:594`), plus a growth-only ledger: `electronReachLedger.test.ts:460-473` asserts `toEqual([])` on new modules vs baseline |
| 1 | WR-04 | Vacuous i18next assertion — the test computed its expectation from the same broken call | 34.2-12 | `gameDetailsModules.test.ts:189-198,407-436` — real `i18next` init backed by the on-disk `public/locales/en/gamepage.json`, asserting the result is neither the key nor `undefined` |
| 1 | WR-08 | `enrichmentFlows.test.ts`'s `online_monitor` mock never re-armed under `resetMocks: true` | 34.2-10 | `enrichmentFlows.test.ts:478-494` — `mockReset().mockReturnValue(true)` and `mockReset().mockImplementation(cb => cb())` in `beforeEach` |
| 1 | WR-09 | `enrichmentFlowRegistration.ts` clones `storeSearch/index.ts`'s three handler bodies with nothing pinning them equal | 34.2-13 | `storeSearch/handlers.ts` is the single implementation; `storeSearch/index.ts:3-6,18-24` and `enrichmentFlowRegistration.ts:129-132,182-192` both delegate to it. Re-inlining gated by `storeSearch/__tests__/handlers.test.ts:149-179`, which uses the shared `stripSourceComments` util and self-tests that a comment-only occurrence is rejected |
| 1 | WR-03 | `knownFixes.ts`'s stated extraction rationale is factually false | quick 260822-tpn | `knownFixes.ts:5-21` — the false "deliberately excluded from the Node sidecar's import graph" claim is replaced with the true, narrower invariant (this module avoids a DIRECT import), and the real transitive chain is named inline so the next reader can re-check it. Same correction shape 34.2-11 applied to WR-02 |
| 1 | WR-06 | `readKnownFixes` builds a filesystem path from unvalidated renderer input with `join` | quick 260822-tpn | `knownFixes.ts:40-66` — `resolve`+`relative` containment matching `sidecar/fileStore.ts:121-133` (`startsWith('..') \|\| isAbsolute \|\| === ''`), logging and returning `null` rather than throwing, since every existing failure mode of this function yields null and all three call sites null-check. **RED-proved:** two traversal tests fail against the pre-fix code — one of which was rewritten mid-task after it passed for the wrong reason (absent file, not containment) — plus an anti-vacuity test proving the guard does not reject ordinary input. 8/8 green after; full backend suite 170/170 suites, 3927 tests |
| 1 | WR-07 | `getCrossoverIndex` unbounded with no cancellation, on top of a per-game index rebuild | quick 260822-wr07 | **Both halves fixed.** (1) `crossoverRatingMap.ts:48-70` loads the index once per pass instead of once per game, via a new `buildIndexResolver()` (`crossover_index/index.ts`). LAZY-once, not eager-once: `ratingMap.test.ts` asserts a library with nothing eligible must not consult the index AT ALL, and an unconditional hoist would have silently broken that. (2) `fetcher.ts` adds a 5-minute per-descriptor failure back-off, so a stale cache plus a failing network makes ONE network attempt instead of one per game — previously `loadIndex` returned `cached.data` on failure without refreshing `fetchedAt`, so every caller retried. `fetchedAt` is deliberately NOT faked forward; it stays an honest record of the last success. **Measured, not assumed:** the per-game cost was `crossoverIndexStore.get()` at **1.41ms** against the real 239KB/2421-entry store (`conf@10.2.0` re-reads and re-parses the backing file on every access); `buildMaps` was already memoized on `generatedAt`, so map construction was never the cost. Worst case before: 10s axios timeout × N games, serially, on a channel with `timeout_for() == None`. **RED-proved** in both places: the load-once gate failed at 50 loads and 2 loads pre-fix; the back-off gate failed on the second suppression assertion. New suite `ratingMapIndexLoads.test.ts` deliberately leaves `../index` REAL so the genuine `loadIndex` call chain is measurable — `ratingMap.test.ts` mocks it wholesale and is structurally blind to this property. 171/171 suites, 3934 tests |
| 1 | WR-10 | Test-harness boilerplate duplicated across suites; the gamedetails electron gate implemented twice | quick 260822-wr10 (two parts) | **All three sub-claims disposed.** (a) The duplicated `stripComments` copies were already gone — six suites import the shared `backend/testUtils/stripSourceComments`, which carries its own self-test. Landed during the WR-09 work, never recorded here. (b) The gamedetails electron gate existed twice over the same directory and same six patterns; the `gameDetailsModules.test.ts` copy is deleted, `gameDetailsImportGate.test.ts` Gate 3 survives, and the survivor was **RED-proved** by planting `import { app } from 'electron'` in `gamedetails/overrides.ts`. (c) The harness boilerplate is now extracted to `sidecar/__tests__/helpers/sidecarHarness.ts` — **13 files changed, 693 deletions, 38 insertions.** `collectFrames`/`startSidecar`/`writeInvoke`/`writeSend`/`findResponse` were 49 copies across 13 suites; every copy was verified **unanimous** by normalized-body sha1 (comments stripped, normalizer self-checked in both directions on a known comment-only-different pair) before removal, so this was mechanical, not a judgement call. Real scope was 5× the review's estimate: 20 suites / 643 duplicated lines, not "four new suites". **Two deliberate non-extractions, recorded in the harness docstring:** `flush()` stays local — 7 syntactically and ~3 semantically distinct implementations (`setImmediate`×N vs `setTimeout(0)` vs `setTimeout(25)`; different event-loop phases, not interchangeable), plus 4 more suites with differently-named variants = 11 flush-family implementations; unifying them would change settling behaviour and surface as flake. And the `jest.mock('os')` (28 suites) / `jest.mock('../pathShim')` (8 suites) blocks **cannot** be shared at all — `jest.mock` is hoisted per-file and its factory cannot close over non-`mock`-prefixed bindings, so WR-10's suggestion to hoist them is unachievable as written, a runner constraint rather than a preference. `humbleLoginFlows.test.ts` keeps its identical `collectFrames` on purpose: importing the harness would pull `bootstrap` (GlobalConfig/i18next) in at module load, which is exactly the cost its `startTransport` exists to avoid — reason recorded in-file. Verified: 171/171 suites, 3931 tests, unchanged before and after; tsc clean; eslint 0 errors; prettier clean |
| 1 | IN-02 | A pre-existing `repair` bug is now pinned as correct by a new test | **FIXED 2026-08-22 — code, not the test title** | **The previous row in this ledger misidentified the finding.** It read "(the Steam-runner stub)" and went looking in `storeManagers/steam/games.ts`; IN-02 is not about that stub. The stub is named in a `D-01 declaration rider` comment sitting immediately above the function (`dispatch.ts:154-158`), which is what that pass matched on. The actual finding is `dispatch.ts`'s `repair()`: `catch` notified "Error Repairing", then **fell through** to the unguarded `notify('finished')` + `logInfo('Finished repairing')` + `status: 'done'`, so a failed repair reported success.<br><br>**Not cosmetic.** The `catch` swallows without rethrowing, so the invoke RESOLVES — which means `GameSubMenu/index.tsx:150-155`'s `catch` → `reportRepairFailure()` could never fire for a runner-level repair failure, only a transport one. That handler is **gap cycle 2's CR-01 renderer half**, with three RED-proofed failure signals and the T-34.2-52 disclosure guard. A whole audited surface was unreachable for the case it was built for — the `a-pass-can-cover-an-unreachable-surface` shape.<br><br>**Fix:** `repairFailed` flag gating the success notify+log; `status: 'done'` stays UNCONDITIONAL (it clears the renderer's `repairing` spinner). Shape mirrors `moveInstall` (`main.ts:1113-1156`), the correct sibling in this same codebase — an in-repo precedent, not a new design.<br><br>The review's own proposed fix (retitle the test, file it in `UPSTREAM-DEFECTS.md`) was **unusable**: no `UPSTREAM-DEFECTS.md` exists anywhere in the repo, and its rationale — upstream byte-fidelity — is a constraint CLAUDE.md records as dead.<br><br>`gameDetailsModules.test.ts:349` now pins the FIX. Body text cannot distinguish the two notifications (this suite runs real uninitialized i18next via `jest.unmock`; `t()` returns `undefined` for both keys, with or without a defaultValue — probed, not assumed), so the success path is identified by `logInfo('Finished repairing')`. **RED-proved in both directions:** pre-fix body → notify count 2 ≠ 1 fails; inverted matcher + pre-fix body → passes, so the `.not` assertion is non-vacuous.<br><br>**Residual, deliberately NOT done here:** `repair()` still does not rethrow, so `reportRepairFailure()` remains unreachable for runner-level failures. Fixing the false success and rewiring the renderer path are separate decisions; the second changes the channel contract. |
| 1 | IN-01 | `handlers.ts` comment undercounts the channels `registerGameDetailsFlows()` registers | **FIXED 2026-08-22 — the whole class, plus a gate** | **The review's own prescribed number was wrong.** It said write "the 18 channels (15 invoke + 3 send)"; counted from source the module registers **19 (16 invoke + 3 send)**. The review copied "18" out of `gameDetailsFlowRegistration.ts`'s then-docstring instead of counting — applying its fix verbatim would have planted a fresh wrong number contradicting the module it describes. (That docstring has since been corrected independently and now reads 19/16+3.)<br><br>**The finding was one of a class of five.** Censusing every numbered call-site claim in `handlers.ts` against the actual registration count inside each `register*Flows()` body: appShell 18→**19**; gameDetails 15 invoke→**16 + 3**; shellFiles 15 send→**17 send + 3 invoke = 20**; logger "the single `logError` send channel"→**6**; humble 10 (enumerated by name)→**16**. Four others (downloadQueue 5, enrichment 8, clipboard 3, humbleLogin 6) matched. Also stale: three `register*Flows()` docstrings (`appShell` 18 vs 19, `logger` "the single `logError` channel" vs 6, `settings` "the two settings-read invoke handlers" vs 12), `appShellFlowRegistration.ts`'s module docstring, and `handlers.ts`'s own module docstring — which still claimed the four Phase 34.5 seams "register 0 channels today" when all four are full.<br><br>**Fix is structural, not a re-count.** Counts were REMOVED from `handlers.ts` entirely — every call-site comment and its module docstring — because that file owned none of them and carried ten copies of facts defined elsewhere. Each count now lives in exactly ONE place, its own module's `register*Flows()` docstring. Rationale (ordering constraints, the Jest tray guard, the duplicate-`logError` warning) was preserved verbatim.<br><br>**Gate:** new `sidecar/__tests__/flowRegistrationCensus.test.ts`, 65 tests. Gate 1 = hand-maintained per-module `{invoke, send}` table vs actual `ipcMain.handle`/`ipcMain.on` counts, plus file-set equality so a new module cannot appear unlisted. Gate 2 derives each docstring's claimed total from one specific position — the token after `Registers` — and compares it to the actual count; modules that deliberately state no total are listed explicitly so "no claim" is a visible choice. Gate 3 asserts `handlers.ts` states no count at all. **All three RED-proved independently**, each failing exactly one test: reverting appShell's docstring to 18 reds Gate 2; restoring IN-01's own stale call-site comment reds Gate 3; perturbing the table by one channel reds Gate 1.<br><br>Deliberately NOT attempted: parsing counts out of module-level prose. Those docstrings cite phase numbers, `main.ts` line numbers and REQ IDs; a generic "number near the word channels" parser matches dozens per file and would be a noise generator, not a gate.<br><br>Two gates elsewhere reacted, both correctly: `testContainment.test.ts` Block C required the new suite be classified (added to `STRUCTURALLY_CONTAINED_SUITES` with its rationale), and `gameDetailsImportGate.test.ts` Gate 7's sha256 pin on `settingsFlowRegistration.ts` fired. That digest was recomputed only after verifying the change is comment-only — HEAD and worktree are byte-identical after stripping comments — and the D-09 bottle-launch property it protects is proven independently by the semantic-pin test, which stayed green. Verified: backend 173/173, 4001 passed; frontend 120/120, 1967 passed; tsc clean; eslint 0 errors; prettier clean |

## Accepted by design (1)

Not debt. A named decision was made, the decision is recorded in a phase artifact, and the residual
is stated. Distinguished from the Open table below so the phase is not misread as carrying work it
deliberately chose not to do.

| Round | ID | Finding | Decision | Why it is a choice, and the residual it leaves |
|-------|----|---------|----------|------------------------------------------------|
| 1 | WR-05 | `bootstrap.ts` signals `READY_SENTINEL` before `i18next.init()`'s resource load settles | **T-34.2-03** (`34.2-01-PLAN.md:372`) | The floating promise is the mitigation, not an oversight. T-34.2-03 is a DoS threat: the sidecar has no `process.on('unhandledRejection')` guard (`sidecar-dialog-reject-crashes`), so awaiting `init()` would let a locale-load failure prevent `init()` from ever writing `READY_SENTINEL` — trading a transient wrong string for a sidecar that never boots. `.catch()` + synchronous `init()` is the deliberate trade. Verified closed as a mitigation in `34.2-SECURITY.md:56`. **Residual, accepted:** between the sentinel and resource load, `t()` at the ported call sites returns the key or `undefined`. D-02 narrows that window to the first few hundred ms of startup; it does not close it, and no test covers the window (`bootstrapWirings.test.ts:309` waits for `isInitialized === true` first). Re-opening this means finding a way to signal readiness without awaiting — not simply adding an `await` |

## Open (2) — real debt, not silently dropped

Each was explicitly named in `34.2-PORTED-CHANNELS.md` §5 as out of scope for gap cycle 1. Status
re-verified against current code on 2026-08-22 except where the row says otherwise.

| Round | ID | Finding | Verified status 2026-08-22 | Evidence / note |
|-------|----|---------|----------------------------|-----------------|
| 1 | IN-03 | `main.rs`'s `LONG_RUNNING_CHANNELS` doc comment has grown to 28 lines of phase history | STILL OPEN | Measured 2026-08-22: still **28** comment lines immediately above the constant. Cosmetic |
| 1 | IN-04 | `enrichmentFlows.test.ts` leaves a real `rustInvoke` frame on process stdout | **STILL OPEN — named symptom gone, condition persists** | SETTLED 2026-08-22 by running the suite with real stdout captured (an earlier probe — counting `rustInvoke` in the test file — was discarded as invalid, since the frame originates from `bootstrap.init()`, not a literal in the test). Result: the frame IN-04 NAMED (`rustInvoke`/`tray_set_icon`) is **gone**, but the CONDITION it described persists in a different shape. The suite still emits one real transport frame to real `process.stdout`, escaping the injected `PassThrough`: `{"kind":"frontendMessage","channel":"storeChanged","args":[{"store":"configStore","key":"games.recent","value":[]}]}`. **A gate written on the literal `rustInvoke` would report this CLOSED while the hazard stands** — the same stale-by-behaviour trap this project has hit before. Cosmetic inside jest; the reason it is not nothing is that stdout **is** the RPC pipe under Tauri, so the leak proves the harness does not fully isolate writes |

## Reconciliation

14 closed + 1 accepted by design + 2 open = **17**, matching `34.2-REVIEW.md`'s `findings.total: 17`
(3 critical + 10 warning + 4 info). All 3 criticals are closed. No finding is unaccounted for.

## What this ledger does NOT claim

- **"Accepted by design" is not "closed."** No code changed for WR-05 and none is planned; the
  behaviour the review described is still present. The bucket asserts only that it is present
  *on purpose*, that the purpose is written down somewhere checkable, and that the residual is
  stated. If T-34.2-03 is ever re-mitigated some other way, WR-05 goes back to open.
- It does not disposition the four gap-cycle reviews (see the scope warning above).
- Three rows (IN-01, IN-02, IN-04) are recorded as **carried or undetermined**, not verified.
  They are marked that way rather than asserted closed or asserted open on the strength of the
  prior ledger alone.
- `reviewStatus()` still returns a non-complete verdict for `34.2-REVIEW.md` on its own; the
  change in this phase's badge is attributable entirely to this sibling file, which is the
  intended mechanism.
