---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 13
type: execute
wave: 2
depends_on: [34.2-11]
gap_closure: true
files_modified:
  - src/backend/storeSearch/handlers.ts
  - src/backend/storeSearch/index.ts
  - src/backend/sidecar/enrichmentFlowRegistration.ts
  - src/backend/storeSearch/__tests__/handlers.test.ts
autonomous: true
requirements: [REQ-34.2-03]

must_haves:
  truths:
    - "The Phase 20 D-14 rethrow-don't-swallow error contract for the three storeSearch channels exists in exactly ONE implementation, imported by both the Electron registration and the sidecar registration"
    - "An Electron-side change to the log message, the wrapping, or the rethrow cannot silently diverge on Tauri, because there is no second copy to diverge from"
    - "The extracted module stays Electron-free and does not import backend/ipc, preserving the curated-import discipline the sidecar depends on"
    - "The existing sidecar rethrow-contract tests still pass unchanged, proving the extraction is behaviour-preserving"
  artifacts:
    - path: "src/backend/storeSearch/handlers.ts"
      provides: "The three shared try/logError/rethrow handler bodies"
      exports: ["handleSearchStores", "handleGetStoreSearchDeals", "handleGetStoreSearchStoreMap"]
      min_lines: 30
    - path: "src/backend/storeSearch/index.ts"
      provides: "addHandler registrations delegating to the shared bodies"
    - path: "src/backend/sidecar/enrichmentFlowRegistration.ts"
      provides: "ipcMain.handle registrations delegating to the same shared bodies"
    - path: "src/backend/storeSearch/__tests__/handlers.test.ts"
      provides: "Behavioural proof of the rethrow contract plus a single-source-of-truth gate"
      min_lines: 60
  key_links:
    - from: "src/backend/storeSearch/index.ts"
      to: "src/backend/storeSearch/handlers.ts"
      via: "import of the three shared bodies"
      pattern: "from\\s+['\"]\\./handlers['\"]"
    - from: "src/backend/sidecar/enrichmentFlowRegistration.ts"
      to: "src/backend/storeSearch/handlers.ts"
      via: "import of the same three shared bodies"
      pattern: "storeSearch/handlers"
---

<objective>
Close **code-review finding WR-09**: `enrichmentFlowRegistration.ts:165-207` hand-copies
`storeSearch/index.ts:14-44`'s three `try` / `logError('<channel> handler failed: ...')` / `throw err`
blocks. This is the **one place in the slice where behavior is duplicated rather than extracted** —
every other channel imports a single shared body.

Phase 20's D-14 error contract now lives in two files with no test asserting they agree. The frontend
container is exclusively responsible for turning a rejected promise into the "provider failed" state,
so an Electron-side change to the log message, the wrapping, or the rethrow would silently diverge on
Tauri — and a swallowed error is indistinguishable from "no results".

The same problem was already solved correctly elsewhere in this slice: `getKnownFixes` was extracted
to `knownFixes.ts` (D-05) and `getCrossoverIndex` to `crossoverRatingMap.ts` (D-06) in plan 34.2-03.
This applies the identical treatment to the last duplicated surface.

Purpose: one source of truth for the D-14 error contract, so the two builds cannot drift.
Output: `storeSearch/handlers.ts`, imported by both registration sites, with the duplication gated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Gap evidence:
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW.md

<interfaces>
<!-- Current state. Both implementations are reproduced here so the executor can verify byte-parity
     before extracting, without exploring. -->

`src/backend/storeSearch/index.ts` (44 lines, Electron side) — imports
`addHandler` from `backend/ipc`, `logError`/`LogPrefix` from `backend/logger`, and
`searchGames`/`getGameDeals`/`getStoreMap` from `./cheapshark`. Registers three handlers, each shaped
`try { return await <fn>(<arg>) } catch (err) { logError('<channel> handler failed: ' + String(err), LogPrefix.Backend); throw err }`:
  - `addHandler('searchStores', async (_event, query) => ...)`         -> `searchGames(query)`,
    log message `searchStores handler failed: ${String(err)}`
  - `addHandler('getStoreSearchDeals', async (_event, gameId) => ...)` -> `getGameDeals(gameId)`,
    log message `getStoreSearchDeals handler failed: ${String(err)}`
  - `addHandler('getStoreSearchStoreMap', async () => ...)`            -> `getStoreMap()`,
    log message `getStoreSearchStoreMap handler failed: ${String(err)}`
  Its module docstring records that this mirrors `backend/discounts/index.ts`'s throw-on-failure
  contract verbatim, and that handlers never swallow errors into an empty result.

`src/backend/sidecar/enrichmentFlowRegistration.ts` lines ~162-207 — the hand-copied clones, each
`ipcMain.handle('<channel>', async (_event: unknown, ...args: unknown[]) => { const x = args[0] as
<type>; try { return await <fn>(x) } catch (err) { logError(...same message...); throw err } })`.
  Its imports include `searchGames`, `getGameDeals`, `getStoreMap` from `'../storeSearch/cheapshark'`
  (line ~114) and `logError`, `LogPrefix` from `'../logger'`.

`src/backend/storeSearch/cheapshark.ts` — exports `getStoreMap` (line ~121), `searchGames` (line ~157),
`getGameDeals` (line ~182). **It imports `{ app } from 'electron'` at line 2** — this is one of the
29 modules in `electronReachLedger.test.ts`'s committed baseline (plan 34.2-11), reached at runtime
through `electronStub`'s `Module._load` hook. The new `handlers.ts` therefore inherits that same
transitive reach and must NOT claim otherwise in its docstring.

**Gates that constrain this change (all currently green — keep them green):**
  - `enrichmentFlows.test.ts` (~line 902): asserts comment-stripped `enrichmentFlowRegistration.ts`
    matches NONE of `/ipc_handler/`, `/backend\/ipc/`, `/from ['"]\.\.\/ipc['"]/`, `/storeSearch\/index/`.
    A `../storeSearch/handlers` import satisfies all four — but `../storeSearch/index` would not, so
    the sidecar must import the new module, never `index.ts`.
  - `enrichmentFlows.test.ts` (~line 914) and `gameDetailsImportGate.test.ts` Gate 1: no `.ts` file
    directly under `src/backend/sidecar/` imports the real `electron` module.
  - `enrichmentFlows.test.ts`'s "storeSearch error contract (load-bearing trio, run first)" describe
    block asserts a rejecting provider surfaces an ERROR response frame over the real sidecar RPC
    loop, not an empty array. It runs FIRST in the file deliberately, so a later happy-path test's
    `storeMapCache` hit cannot mask the reject scenario. **This block must pass unchanged after the
    extraction — it is the behaviour-preservation proof.**

`src/backend/storeSearch/__tests__/` does not exist yet. `jest.config.js` lists `src/backend` as a
project root, so a new `__tests__` directory there is collected automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the three handler bodies into a shared Electron-free storeSearch/handlers.ts and delegate from the Electron registration</name>
  <read_first>
    - `src/backend/storeSearch/index.ts` in full (44 lines) — the three registrations, the exact log
      message strings, and the module docstring's D-14 rationale
    - `src/backend/storeSearch/cheapshark.ts` lines 1-10 and the three exported function signatures
      at lines ~121, ~157, ~182 — including its `import { app } from 'electron'` at line 2
    - `src/backend/knownFixes.ts` and `src/backend/crossover_index/crossoverRatingMap.ts` — the two
      extraction precedents from plan 34.2-03, for header style and module shape to mirror
    - `.planning/phases/34.2-.../34.2-REVIEW.md` finding WR-09 (lines 606-631), and WR-03
      (lines 380-406) which records how an extraction docstring can state a benefit that is not true
      — the mistake this module's header must not repeat
  </read_first>
  <action>
    Create `src/backend/storeSearch/handlers.ts` exporting exactly three functions with these names
    and shapes:
      - `handleSearchStores(query: string)` -> `await searchGames(query)`
      - `handleGetStoreSearchDeals(gameId: string)` -> `await getGameDeals(gameId)`
      - `handleGetStoreSearchStoreMap()` -> `await getStoreMap()`
    Each wraps its call in `try` / `catch (err)`, calls
    `logError(\`<channel> handler failed: ${String(err)}\`, LogPrefix.Backend)` with the channel name
    matching the existing message EXACTLY (`searchStores`, `getStoreSearchDeals`,
    `getStoreSearchStoreMap` — these strings are the observable contract and must not be reworded),
    then `throw err`. Return types must match `cheapshark.ts`'s own return types; do not widen to
    `unknown` or `any`.

    Imports: only `./cheapshark` and `../logger`. It must NOT import `backend/ipc`, `./index`, or
    `electron`.

    Header docstring must state: why the module exists (WR-09 — the D-14 rethrow contract had two
    copies with nothing pinning them equal, so an Electron-side edit could silently diverge on Tauri);
    that both `storeSearch/index.ts` (Electron, via `addHandler`) and
    `sidecar/enrichmentFlowRegistration.ts` (Tauri, via `ipcMain.handle`) import these same bodies;
    that the rethrow is deliberate and load-bearing (Phase 20 D-14 — the frontend container owns the
    "provider failed" state, and a swallowed error is indistinguishable from "no results"); and that
    the module imports no `backend/ipc` and no `electron` DIRECTLY, while noting honestly that it
    reaches `electron` transitively through `cheapshark.ts:2`'s `import { app } from 'electron'`,
    rescued at runtime by `electronStub`'s `Module._load` hook. Do NOT claim transitive
    electron-freedom — that is the exact overclaim verification gap #3 / WR-02 found, corrected by
    plan 34.2-11, and WR-03 records a sibling case of an extraction docstring stating a benefit that
    was not true.

    Then rewrite `src/backend/storeSearch/index.ts`'s three `addHandler` registrations as one-line
    delegations to the imported functions, mirroring the delegation shape D-03 established for
    `main.ts`. Remove the now-unused `searchGames`/`getGameDeals`/`getStoreMap` and
    `logError`/`LogPrefix` imports if nothing else in the file uses them. Keep `addHandler`'s import,
    the channel names, the handler arity, and the `_event` parameter positions exactly as they are —
    the typed-IPC signatures must not change.

    Behaviour must be byte-equivalent: same call, same log string, same rethrow, same return value.
    This is an extraction, not a redesign. Do not add validation, do not change the error type, do not
    add a fallback return.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit && npx eslint src/backend/storeSearch/handlers.ts src/backend/storeSearch/index.ts && echo LINT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/storeSearch/handlers.ts` exists and exports exactly `handleSearchStores`,
      `handleGetStoreSearchDeals`, `handleGetStoreSearchStoreMap`.
    - `grep -cE "from 'electron'|backend/ipc|from './index'" src/backend/storeSearch/handlers.ts`
      returns 0.
    - The three log-message templates in `handlers.ts` contain the literal substrings
      `searchStores handler failed:`, `getStoreSearchDeals handler failed:`, and
      `getStoreSearchStoreMap handler failed:`.
    - Each of the three functions contains a `throw err` statement inside its `catch`.
    - `src/backend/storeSearch/index.ts` imports from `./handlers` and each of its three
      `addHandler` bodies is a single delegating call — `grep -c "logError" src/backend/storeSearch/index.ts`
      returns 0.
    - `src/backend/storeSearch/index.ts` still registers exactly the three channel names
      `searchStores`, `getStoreSearchDeals`, `getStoreSearchStoreMap` via `addHandler`.
    - `npx tsc --noEmit` clean; `npx eslint` on both files exits 0.
  </acceptance_criteria>
  <done>
    One implementation of the D-14 rethrow contract exists, in an Electron-free module, and the
    Electron registration delegates to it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Point the sidecar registration at the same shared bodies and pin the single-source-of-truth property</name>
  <read_first>
    - `src/backend/sidecar/enrichmentFlowRegistration.ts` in full — the module docstring (especially
      the storeSearch error-contract paragraph and the curated-import paragraph naming
      `storeSearch/index.ts` as forbidden), the `../storeSearch/cheapshark` import at line ~114, and
      the three cloned handler bodies at lines ~162-207. **Read the version as edited by plan
      34.2-11**, which corrected this docstring's transitive-electron-freedom claim — do not revert
      that correction.
    - `src/backend/storeSearch/handlers.ts` as created by Task 1
    - `src/backend/sidecar/__tests__/enrichmentFlows.test.ts` lines 895-928 — the import gate that
      forbids `storeSearch/index` (which `storeSearch/handlers` satisfies) — and the
      "storeSearch error contract (load-bearing trio, run first)" describe block that must keep
      passing unchanged
    - `.planning/phases/34.2-.../34.2-REVIEW.md` finding WR-09
  </read_first>
  <action>
    In `src/backend/sidecar/enrichmentFlowRegistration.ts`, replace the three cloned handler bodies
    with one-line delegations to `handleSearchStores`, `handleGetStoreSearchDeals` and
    `handleGetStoreSearchStoreMap`, imported from `'../storeSearch/handlers'`.

    Preserve exactly: the channel names, the `ipcMain.handle` registration kind, the
    `async (_event: unknown, ...args: unknown[])` signature shape, and the
    `args[0] as string` argument extraction for the two channels that take one
    (`searchStores` -> `query`, `getStoreSearchDeals` -> `gameId`).

    Remove `searchGames`, `getGameDeals` and `getStoreMap` from the `'../storeSearch/cheapshark'`
    import. If no other channel in the file uses that module, remove the import entirely. Remove
    `logError`/`LogPrefix` only if no other handler in the file still uses them — check before
    deleting.

    Update this module's docstring in two places:
    - The storeSearch error-contract paragraph currently says the try/logError/throw shape "is
      reproduced VERBATIM for all three". That is no longer the mechanism. Change it to record that
      the three bodies are now IMPORTED from `storeSearch/handlers.ts`, that this closes WR-09 (the
      contract previously existed in two hand-copied places with nothing pinning them equal), and
      that the rethrow remains deliberate for the Phase 20 D-14 reason.
    - The curated-import paragraph currently says "NEVER `storeSearch/index.ts` (which itself calls
      `addHandler`)". Keep that prohibition and add that `storeSearch/handlers.ts` is the correct
      import target precisely because it contains no `addHandler` call — this is the same
      underlying-module-not-ipc_handler rule D-04 already states.

    Then create `src/backend/storeSearch/__tests__/handlers.test.ts` with two groups:

    **Group 1 — behavioural (the contract itself).** Mock `../cheapshark` so each of `searchGames`,
    `getGameDeals`, `getStoreMap` can be scripted. For each of the three handlers assert: on success
    the resolved value is passed through unchanged; on a rejecting provider the handler REJECTS
    (never resolves to `[]`, `null`, or `undefined`) and `logError` was called with a message
    containing the channel's exact `<channel> handler failed:` prefix. Mock `../../logger` to observe
    the call. The "never resolves to an empty result" assertion is the D-14 contract and must be
    explicit, not implied by the rejection assertion alone.

    **Group 2 — single-source-of-truth gate (this is what makes the drift impossible to reintroduce).**
    Read `src/backend/storeSearch/index.ts` and `src/backend/sidecar/enrichmentFlowRegistration.ts`
    as text, strip comments with the same helper shape the other gates in this repo use (drop lines
    whose trimmed form starts with `//`, `*`, or `/*`), and assert:
      - both stripped sources reference the shared module (`./handlers` and `storeSearch/handlers`
        respectively);
      - NEITHER stripped source contains the literal `handler failed:` any more — the log strings now
        live in exactly one file. This is the assertion that fails if someone re-inlines a copy.
    Comment-stripping is load-bearing here: this test file and both docstrings discuss the phrase
    `handler failed`, and an unfiltered match would be self-invalidating.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx jest storeSearch enrichmentFlows 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "storeSearch/handlers" src/backend/sidecar/enrichmentFlowRegistration.ts` returns at
      least 1.
    - Comment-stripped `src/backend/sidecar/enrichmentFlowRegistration.ts` contains no occurrence of
      `handler failed:`, and neither does comment-stripped `src/backend/storeSearch/index.ts`.
    - `grep -cE "storeSearch/index" src/backend/sidecar/enrichmentFlowRegistration.ts` returns 0
      outside comment lines — the existing `enrichmentFlows.test.ts` import gate stays green.
    - `npx jest storeSearch` reports 1 suite passed with at least 8 tests (3 pass-through, 3 reject +
      log, 2 source gates).
    - `npx jest enrichmentFlows` passes with the SAME test count as before this plan, including the
      "storeSearch error contract (load-bearing trio, run first)" describe block — this is the
      behaviour-preservation proof for the extraction.
    - `npx jest gameDetailsImportGate` passes (Gate 1 unaffected).
    - Negative control, run and recorded in the SUMMARY: temporarily re-inlining one try/logError/throw
      block into `enrichmentFlowRegistration.ts` makes the Group 2 gate fail; reverted afterwards.
    - `npx tsc --noEmit` clean; `npx eslint src/backend/sidecar/enrichmentFlowRegistration.ts` exits 0.
    - Plan 34.2-11's docstring correction is still present: comment-stripped
      `enrichmentFlowRegistration.ts` still contains `Module._load` and does not reassert
      `either directly or transitively`.
  </acceptance_criteria>
  <done>
    Both registration sites import the same three bodies, the log strings exist in exactly one file,
    and a gate fails if a copy is ever re-inlined.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CheapShark (third-party HTTP API) -> backend | `searchGames`/`getGameDeals`/`getStoreMap` fetch and parse untrusted third-party JSON. Failure handling at this boundary is the contract being unified |
| backend -> renderer (both Electron `addHandler` and sidecar `ipcMain.handle`) | The rejected-vs-resolved distinction is what tells the frontend "provider failed" instead of "no results". Divergence between the two builds is a correctness boundary |
| renderer -> `getKnownFixes` -> filesystem | An adjacent, pre-existing path on the same enrichment registration surface (see accepted threat below) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-64 | Tampering / behavioural divergence | Duplicated D-14 error contract in `storeSearch/index.ts` and `enrichmentFlowRegistration.ts` | mitigate | Two hand-copied implementations with nothing pinning them equal. An Electron-side change to the wrapping or the rethrow silently diverges on Tauri, where the frontend would then read a swallowed error as "no results" instead of "provider failed". Task 1 extracts one shared body; Task 2 points both sites at it and adds a gate asserting the log strings exist in exactly one file. |
| T-34.2-65 | Information Disclosure | `logError(\`... ${String(err)}\`)` on a CheapShark failure | accept | The logged string is a third-party HTTP error. No credentials traverse this path — CheapShark is an unauthenticated public API. Local log file only. Pre-existing behaviour, preserved byte-identically by the extraction. |
| T-34.2-66 | Denial of Service | Unvalidated `query`/`gameId` forwarded to CheapShark | accept | Pre-existing upstream behaviour, unchanged by this extraction. The values are used as HTTP query parameters by `cheapshark.ts`, not as filesystem or shell input. Out of scope for a de-duplication plan; noted so the extraction is not misread as having reviewed the inputs. |
| **T-34.2-67 (WR-06)** | **Tampering / Path Traversal** | **`src/backend/knownFixes.ts:29-35`, reached via `enrichmentFlowRegistration.ts`'s `getKnownFixes` channel** | **accept (KNOWN, OUT OF SCOPE this cycle)** | `readKnownFixes(appName, runner)` builds `join(fixesPath, \`${appName}-${storeMap[runner]}.json\`)` from an `appName` that arrives straight off the wire as `args[0] as string` with no validation. `join` NORMALISES `..` segments rather than rejecting them, so `appName = '../../../../etc/hosts'` escapes `fixesPath`, and the parsed contents are returned to the renderer. Secondary: `storeMap[runner]` is `undefined` for an unrecognised runner, silently producing `<appName>-undefined.json`. This is a faithful port of a pre-existing `launcher.ts` defect — NOT a regression introduced by phase 34.2 — but this slice newly exposed it on the sidecar RPC surface with no typed-IPC layer in front of it. **Explicitly deferred by the developer for this gap cycle; recorded here so it is tracked, not forgotten.** The remediation, when it is scheduled, must follow this project's recorded Phase 18 convention: `path.join` is NOT containment — use `resolve` + `relative` + an `isAbsolute`/`..` escape check, and reject an unknown `runner` rather than producing an `undefined` filename. |
| T-34.2-68 | Spoofing / vacuous assertion | The new single-source-of-truth gate | mitigate | A gate asserting the absence of `handler failed:` would be self-invalidating, because this test file and both module docstrings discuss that exact phrase. Both source gates therefore strip comments before matching, and a mandatory negative control (re-inline one block, watch the gate fail) proves the gate is live. |
| T-34.2-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages — one new first-party module, two edited source files, one new test. Package Legitimacy Gate not triggered; no `[ASSUMED]`/`[SUS]` checkpoint required. |
</threat_model>

<verification>
Build gates that must stay green:

- `npx tsc --noEmit` — clean, no output
- `cd src-tauri && cargo check --quiet` — exit 0 (no Rust touched; no-regression check only)
- `npx jest gameDetailsFlows enrichmentFlows bootstrapWirings gameDetailsImportGate gameDetailsModules knownFixes longRunningChannels ratingMap` — all suites pass. `enrichmentFlows` must pass with the SAME test count as before, including its first-running storeSearch error-contract describe block — that is the behaviour-preservation proof for this extraction.
- New suite: `npx jest storeSearch` — 1 suite, >= 8 tests, all green
- Full backend suite baseline: `npx jest --testPathPattern=src/backend` → **105/106 suites,
  2211/2212 tests**, plus this plan's new suite. The single known failure is
  `rustInvokeChannel.test.ts` "writes a single well-formed rustInvoke frame", pre-existing from
  Phase 34.1 and explicitly OUT OF SCOPE. Do NOT demand 106/106 or 2212/2212.
- **The Electron build keeps working unchanged (REQ-34.2-14).** This plan modifies an Electron-side
  file (`storeSearch/index.ts`), so the invariant is behavioural, not textual — exactly as D-03/D-05/D-06
  already established for `main.ts`, `launcher.ts` and `crossover_index/ipc_handler.ts`. The three
  channels must register the same names with the same arity and produce the same success and failure
  behaviour.
- `electronReachLedger.test.ts` (from plan 34.2-11) must stay green: `storeSearch/handlers.ts` adds a
  hop to `cheapshark.ts`, which is ALREADY in the committed baseline, so the reachable
  electron-importing set must not grow. If it does, that is a finding to report, not a baseline to
  silently widen.
</verification>

<success_criteria>
- `src/backend/storeSearch/handlers.ts` holds the only implementation of the three handler bodies
- Both `storeSearch/index.ts` and `enrichmentFlowRegistration.ts` delegate to it
- The literal `handler failed:` log strings exist in exactly one file, enforced by a comment-stripped gate
- `enrichmentFlows.test.ts`'s storeSearch error-contract block passes unchanged
- `electronReachLedger.test.ts`'s baseline does not grow
- The WR-06 path-traversal defect is recorded as a tracked, accepted threat with an ID and a stated
  remediation approach, not silently dropped
</success_criteria>

<output>
Create `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-13-SUMMARY.md` when done
</output>
