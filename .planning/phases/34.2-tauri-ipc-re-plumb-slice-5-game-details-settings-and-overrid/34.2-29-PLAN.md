---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 29
type: execute
wave: 2
depends_on: [34.2-25, 34.2-26]
files_modified:
  - src/backend/sidecar/__tests__/testContainment.test.ts
  - src/backend/sidecar/__tests__/bootstrap.test.ts
autonomous: true
requirements: [REQ-34.2-07, REQ-34.2-14]

must_haves:
  truths:
    - "Deleting the real HOME assignment from the containment module fails Block D, and a docblock mentioning it cannot satisfy the gate"
    - "Deleting the os / node:os homedir mock from the containment module fails Block D — the mechanism the module itself calls load-bearing is gated"
    - "Dropping one unclassified *.test.ts into the sidecar test directory produces exactly ONE failure, naming the file"
    - "A frontend, common, preload or meta test file that imports a containment-relevant backend module fails a gate, so the backend-only scope claim in jest.config.js is enforced rather than assumed"
  artifacts:
    - path: "src/backend/sidecar/__tests__/testContainment.test.ts"
      provides: "Stripped-source env gate, os-mock gate, synthetic Block C self-tests, cross-project scope gate, registration of the new suite"
      contains: "loggerCallSiteGuard.test.ts"
    - path: "src/backend/sidecar/__tests__/bootstrap.test.ts"
      provides: "Corrected tripwire comment naming the real preventer"
      contains: "jest.setupContainment"
  key_links:
    - from: "src/backend/sidecar/__tests__/testContainment.test.ts"
      to: "src/backend/jest.setupContainment.ts"
      via: "stripped-source assertions over the containment module"
      pattern: "JEST_SETUP_CONTAINMENT_PATH"
    - from: "src/backend/sidecar/__tests__/testContainment.test.ts"
      to: "src/frontend, src/common, src/preload, meta"
      via: "cross-project import scope gate"
      pattern: "backend/logger"
---

<objective>
Close the four gate-quality defects that live in `testContainment.test.ts`, plus CR-02's secondary
instance and WR-10's remaining comment:

- **WR-01** — Block D's `HOME` env-var gate matches RAW source, and `jest.setupContainment.ts`'s own
  docblock contains a line matching `process\.env\.HOME\s*=`, so deleting the real assignment leaves
  the gate green. This is the identical stripped-vs-raw vacuity defect gap cycle 2's WR-01 was raised
  to fix, reintroduced in the gate written to fix it.
- **WR-02** — Block D gates only the eight `process.env` assignments, which the containment module
  itself labels "defense-in-depth", and never gates the `jest.mock` `homedir` override the module
  labels "THE load-bearing fix". Deleting the mock leaves Block D fully green.
- **WR-05** — Block C's two self-tests build their input from the live directory listing, so the
  moment the real tripwire fires they emit two additional, actively misleading failures. Reproduced
  live by the reviewer: 3 failures instead of 1.
- **CR-02 secondary** — `jest.config.js`'s comment asserts the destruction path "is reachable only
  from `src/backend`, so the frontend/common/preload/meta jest projects are deliberately untouched".
  True today, hand-maintained, and unenforced — nothing fails if a `meta` or `preload` suite starts
  importing `backend/logger` tomorrow.
- **WR-10 (remaining half)** — `bootstrap.test.ts:100-117`'s comment claims its first-test tripwire
  "runs before any of the real `init()` calls below", presenting a post-hoc detector as a preventer.

This plan also **registers `loggerCallSiteGuard.test.ts`** (created by plan 34.2-26) in
`STRUCTURALLY_CONTAINED_SUITES`, closing the Block C failure that plan deliberately left open as
evidence the tripwire is live.

Purpose: three of gap cycle 3's new gates can pass while the invariant they name is broken. A gate
that cannot fail is worse than no gate, because it is read as evidence.

Output: gates that are each proven falsifiable, and a scope claim that is enforced instead of
asserted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW-GAP-CYCLE-3.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-25-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-26-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not re-derive these from the codebase. -->

`src/backend/sidecar/__tests__/testContainment.test.ts` existing structure:
- `stripComments(source)` at line 198 — drops lines whose trimmed-left form starts with `//`, `*`
  or `/*`. Already used by Block B. THIS is the helper WR-01's fix routes the env gate through.
- `IN_SCOPE_SUITES` (413, 4 entries) and `STRUCTURALLY_CONTAINED_SUITES` (448, 21 entries).
- Block C (617-675): `listActualTestSuites()`, `diffSuiteClassification(actualFiles, declaredNames)`
  returning `{ unclassified, phantom }`, the set-equality gate, and the two coupled self-tests at
  643-674 that WR-05 flags.
- Block D (684-743): `JEST_CONFIG_PATH`, `JEST_SETUP_CONTAINMENT_PATH`,
  `hasSetupContainmentEntry(source)` (stripped-source, with a self-test), and
  `REQUIRED_CONTAINMENT_ENV_VARS` (8 keys) driving an `it.each` that matches RAW source at line 740.

Upstream contracts this plan depends on (read the two SUMMARYs before asserting on them):
- Plan 34.2-25 makes `jest.setupContainment.ts` register the containment factory for BOTH `'os'` and
  `'node:os'` via a hoisted function declaration named `mockOsFactory`, overriding `homedir` and
  `userInfo`, and creating the root with `mkdtempSync` + `chmodSync(0o700)`. Block D's new mechanism
  gate must assert against that shape, not the pre-25 single-specifier inline factory.
- Plan 34.2-26 adds `src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts`, which is currently
  unclassified and therefore failing Block C's set-equality test.

`src/backend/jest.config.js` lines 18-21 carry the backend-only scope claim.
`src/backend/sidecar/__tests__/bootstrap.test.ts` imports `../bootstrap` and `../../logger/paths` at
module scope (lines 66-75); its tripwire is the first `it` inside the describe at line 111.
`src/backend/constants/paths.ts` performs module-scope work (an `app.getPath('appData')` read at
line 9 and a `mkdirSync` under the `CI === 'e2e'` branch at line 18) — i.e. before any test body.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Block D gate the load-bearing mechanism, on stripped source, with self-tests that prove each gate can fail (WR-01, WR-02)</name>
  <files>src/backend/sidecar/__tests__/testContainment.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/testContainment.test.ts (the file being modified — read lines 190-210 for `stripComments`, and 684-750 for Block D in full)
    - src/backend/jest.setupContainment.ts (post-plan-34.2-25 state — the mechanism this gate must pin: the `mockOsFactory` function declaration, the two `jest.mock` registrations, the `homedir`/`userInfo` overrides, `mkdtempSync`/`chmodSync`, and the eight env assignments)
    - .planning/phases/34.2-.../34.2-25-SUMMARY.md (what plan 34.2-25 actually shipped — assert against the real shape, not this plan's expectation of it)
    - .planning/phases/34.2-.../34.2-REVIEW-GAP-CYCLE-3.md (WR-01 lines 270-299, WR-02 lines 301-321)
  </read_first>
  <behavior>
    - The env-var gate runs on `stripComments(source)`. A synthetic source whose ONLY occurrence of `process.env.HOME =` sits inside a `*`-prefixed docblock line is REJECTED. RED against the current raw-source form, which accepts it.
    - A new mechanism gate rejects a synthetic containment module that registers the factory for `'os'` only (the exact pre-plan-34.2-25 state), and rejects one with both registrations but no `homedir` override.
    - The mechanism gate accepts the real, post-34.2-25 `jest.setupContainment.ts`.
    - A `userInfo` override gate rejects a synthetic factory that overrides `homedir` but not `userInfo`.
  </behavior>
  <action>
    In Block D of `src/backend/sidecar/__tests__/testContainment.test.ts`:

    **WR-01.** Change the `it.each(REQUIRED_CONTAINMENT_ENV_VARS)` body to read the file and match
    against `stripComments(source)` rather than the raw string. Add a comment stating the asymmetry
    deliberately, since Block A's "NO FILESYSTEM WRITES" anti-claim gate correctly needs RAW source
    (it hunts a *comment*) while this gate hunts a *code statement* — the two are not inconsistent,
    and a future reader must not "harmonise" them. Add a self-test feeding the shared matcher a
    synthetic source whose only `process.env.HOME =` occurrence is on a ` * ...` docblock line and
    asserting the stripped matcher does NOT match. Factor the matcher into a shared predicate
    (`assignsContainmentEnvVar(source, key): boolean`) so the gate and the self-test exercise the
    same code path — the discipline this file already applies to `hasSetupContainmentEntry` and
    `diffSuiteClassification`.

    **WR-02.** Add a shared predicate `hasContainmentOsMock(source: string): boolean` over
    `stripComments(source)` requiring ALL of: a `jest.mock` registration naming the bare `'os'`
    specifier; a `jest.mock` registration naming the `'node:os'` specifier; a `homedir` override
    whose value is an arrow function; and a `userInfo` override. Drive it against the real
    `JEST_SETUP_CONTAINMENT_PATH` in one gate, and add three self-tests: (a) a synthetic source with
    only the bare-`'os'` registration is rejected — this is the pre-34.2-25 state, so the self-test
    documents exactly what CR-02 was; (b) a synthetic source with both registrations but no
    `homedir` override is rejected; (c) a synthetic source with both registrations, a `homedir`
    override and a `userInfo` override is accepted.

    Update Block D's describe title and its docstring to state that it now gates BOTH the
    load-bearing module-registry substitution and the defence-in-depth env assignments — the review's
    complaint was that the block "advertises itself as *the* structural gate" while covering only the
    half the module itself calls defence-in-depth.

    Write the WR-01 self-test and the WR-02 gate FIRST and run them before touching the existing
    `it.each`, so the WR-01 RED is observed intrinsically.
  </action>
  <verify>
    <automated>npx jest --config src/backend/jest.config.js --testPathPattern=testContainment 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Intrinsic RED recorded verbatim in the SUMMARY: with the new WR-01 self-test added but the `it.each` still on raw source, the self-test fails. Then, after routing through `stripComments`, it passes.
    - Deliberate-break check 1 (WR-01), recorded verbatim: comment out `process.env.HOME = containmentRoot` in `src/backend/jest.setupContainment.ts`; re-run; the `HOME` env-var gate FAILS (it did not before this task); restore and confirm `git diff --exit-code src/backend/jest.setupContainment.ts` returns 0.
    - Deliberate-break check 2 (WR-02), recorded verbatim: comment out the `jest.mock('node:os', mockOsFactory)` registration; re-run; the new mechanism gate FAILS; restore and confirm `git diff --exit-code` returns 0.
    - All three `hasContainmentOsMock` self-tests pass, including the one documenting the pre-34.2-25 single-specifier state as rejected.
    - `grep -c "node:os" src/backend/sidecar/__tests__/testContainment.test.ts` returns >= 3.
    - `npx jest --config src/backend/jest.config.js --testPathPattern=testContainment` exits 0 EXCEPT for the Block C set-equality failure naming `loggerCallSiteGuard.test.ts`, which Task 2 closes. Record that this is the only remaining failure.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Block D gates the mechanism the containment module itself calls load-bearing, on stripped source, and both new gates are proven able to fail against the real file.</done>
</task>

<task type="auto">
  <name>Task 2: Decouple Block C's self-tests from the live directory and classify the new suite (WR-05)</name>
  <files>src/backend/sidecar/__tests__/testContainment.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/testContainment.test.ts (Block C — lines 583-682, including `listActualTestSuites`, `diffSuiteClassification`, and the two coupled self-tests at 643-674)
    - src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts (created by plan 34.2-26 — the suite this task classifies)
    - .planning/phases/34.2-.../34.2-26-SUMMARY.md (the recorded Block C failure this task closes)
    - .planning/phases/34.2-.../34.2-REVIEW-GAP-CYCLE-3.md (WR-05 lines 378-405 — the reproduced 3-failures-instead-of-1 output)
  </read_first>
  <action>
    **WR-05.** Rewrite both Block C self-tests to use fully synthetic inputs. Neither may call
    `listActualTestSuites()` — a self-test whose job is to prove the comparator can fail must not
    depend on the state the comparator is comparing. Use small literal arrays, e.g. actual
    `['a.test.ts','b.test.ts','zzSynthetic.test.ts']` against declared `['a.test.ts','b.test.ts']`
    for the unclassified direction, and the mirror for the phantom direction. Both must still call
    `diffSuiteClassification` — the SAME function the real gate calls — so the self-test proves the
    gate's own logic rather than a reimplementation. Add a comment recording the reproduced symptom
    (dropping one unclassified file produced 3 failures, two of them with misleading `expected
    ['zzSyntheticUnclassifiedSuite.test.ts']` / `expected []` messages that actively misdirect the
    person diagnosing the real failure).

    **Registration.** Add `'loggerCallSiteGuard.test.ts'` to `STRUCTURALLY_CONTAINED_SUITES`, keeping
    the array alphabetically sorted, and update the array's docstring entry count (it currently says
    the directory holds 25 files, 4 in-scope + 21 structurally contained). Recompute both numbers
    from `readdirSync` rather than trusting the prose, and correct the docstring to the observed
    values. Add a one-line note that this suite is classified as structurally contained because it
    carries no `os`/`pathShim` mock of its own and relies entirely on the `setupFiles` mechanism
    (mocking only `backend/logger/paths`, which is below the code it tests).
  </action>
  <verify>
    <automated>npx jest --config src/backend/jest.config.js --testPathPattern=testContainment 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --config src/backend/jest.config.js --testPathPattern=testContainment` exits 0 — the Block C failure plan 34.2-26 left open is closed.
    - `grep -c "listActualTestSuites()" src/backend/sidecar/__tests__/testContainment.test.ts` returns exactly 2 (the anti-vacuity test and the real set-equality gate); neither self-test calls it.
    - `grep -c "loggerCallSiteGuard.test.ts" src/backend/sidecar/__tests__/testContainment.test.ts` returns >= 1.
    - `STRUCTURALLY_CONTAINED_SUITES` is alphabetically sorted (verify with a shell sort comparison, recorded in the SUMMARY) and the docstring's file counts match `readdirSync`'s actual output.
    - WR-05 live proof, recorded verbatim: create a real `zzTripwireProbe2.test.ts` in `src/backend/sidecar/__tests__/` containing one trivially-passing test; run the suite; observe EXACTLY ONE failure (the set-equality gate, naming the probe) — the review measured three before this task; delete the probe and confirm `git status --porcelain src/backend/sidecar/__tests__` is clean.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>An unclassified new suite produces exactly one, correctly-named failure, and the new proof suite from plan 34.2-26 is classified.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce the backend-only containment scope claim and correct the bootstrap tripwire comment (CR-02 secondary, WR-10)</name>
  <files>src/backend/sidecar/__tests__/testContainment.test.ts, src/backend/sidecar/__tests__/bootstrap.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/testContainment.test.ts (Blocks A-D as they stand after Tasks 1-2; the `stripComments` helper this task reuses)
    - src/backend/jest.config.js (lines 12-24 — the backend-only scope claim being enforced)
    - src/backend/sidecar/__tests__/bootstrap.test.ts (lines 60-120 — the module-scope imports and the tripwire comment being corrected)
    - src/backend/constants/paths.ts (lines 1-25 — the module-scope work that proves a first-test tripwire cannot be a preventer)
    - .planning/phases/34.2-.../34.2-REVIEW-GAP-CYCLE-3.md (CR-02 secondary at lines 220-227, WR-10 at lines 510-527)
  </read_first>
  <action>
    **New Block E — cross-project containment scope gate.** `src/backend/jest.config.js` justifies
    registering `setupFiles` on the Backend project ONLY by asserting the destruction path is
    reachable only from `src/backend`. Turn that hand-maintained assumption into an enforced one.
    Add a block that recursively walks `src/frontend`, `src/common`, `src/preload` and `meta`
    (skipping `node_modules`), collects every `*.test.ts` / `*.test.tsx` file, strips comments from
    each, and asserts none of them references any of the containment-relevant module specifiers:
    `backend/logger`, `backend/logger/paths`, `backend/constants/paths`, `backend/sidecar/pathShim`
    (match both the bare-specifier and relative `../../backend/...` forms). Assert the collected
    violation list `toEqual([])` so a failure names the offending file and specifier.
    Add two self-tests over the shared predicate: a synthetic source importing `backend/logger` is
    REJECTED; a synthetic source importing an unrelated backend module (e.g. `common/types`) is
    ACCEPTED. Add a comment stating explicitly what this gate is for: if a `meta` or `preload` suite
    starts importing one of these tomorrow, it would run WITHOUT the containment `setupFiles` entry,
    reopening the destruction path in a project nobody is watching — and `meta/__tests__/buildSidecarSea.test.ts`
    reads backend source as TEXT only, which this gate must continue to allow.

    **WR-10 comment correction in `bootstrap.test.ts`.** Rewrite the comment at lines 100-110. It
    currently claims the tripwire being the FIRST test in the describe block means it "runs before
    any of the real `init()` calls below". Under a broken containment that is not protective: this
    file imports `../bootstrap` and `../../logger/paths` at module scope, and `constants/paths.ts`
    does work at module scope, so those imports have already touched the filesystem before any test
    body runs. State that plainly, name `src/backend/jest.setupContainment.ts`'s `setupFiles`-time
    precondition (added by plan 34.2-25) as the actual preventer, and keep this test as localised
    documentation at the site of the historical defect — which is genuine value, since it fails
    locally rather than only in a distant gate file. Do NOT weaken, move or delete the test itself.
    Change only the comment.

    Then reconcile the full backend baseline.
  </action>
  <verify>
    <automated>npx jest --config src/backend/jest.config.js --testPathPattern="testContainment|bootstrap" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --config src/backend/jest.config.js --testPathPattern="testContainment|bootstrap"` exits 0.
    - Block E's real-file gate passes and both of its self-tests pass; `grep -c "backend/logger" src/backend/sidecar/__tests__/testContainment.test.ts` returns >= 2.
    - Block E deliberate-break check, recorded verbatim: temporarily add `import { getLogFilePath } from 'backend/logger/paths'` to a `meta` or `src/common` test file; re-run; Block E FAILS naming that file; revert and confirm `git status --porcelain` is clean.
    - `grep -c "jest.setupContainment" src/backend/sidecar/__tests__/bootstrap.test.ts` returns >= 1, and the corrected comment no longer claims the tripwire runs before module-scope filesystem work.
    - `git diff src/backend/sidecar/__tests__/bootstrap.test.ts` shows comment-only changes — no `it`, `expect`, `import` or `describe` line is modified.
    - Full backend sweep `npx jest --config src/backend/jest.config.js 2>&1 | tail -40`: the failing-suite set is exactly `{rustInvokeChannel.test.ts}` (the documented Phase 34.1 baseline). Record suite/test counts in the SUMMARY; if the pre-existing non-deterministic `storeManagers/steam/library.ts` leaked-timer flake lands on another suite, re-run and record both runs — it is explicitly out of scope.
    - `npx tsc --noEmit` exits 0; `npx prettier --check src/backend/sidecar/__tests__/testContainment.test.ts src/backend/sidecar/__tests__/bootstrap.test.ts` exits 0.
    - `cd src-tauri && cargo check --quiet` exits 0.
  </acceptance_criteria>
  <done>The backend-only containment scope is enforced by a falsifiable gate instead of a comment, and the bootstrap tripwire is described as what it is.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jest worker (any project) → host filesystem | Only the Backend project registers the containment `setupFiles` entry; a test file in another project that reaches the destruction path writes to the real `$HOME`. |
| gate assertion → source text | The gates read source as untrusted text; a comment that satisfies a code-statement gate is a false-green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-109 | Repudiation | Block D env-var gate | mitigate | Task 1 routes it through `stripComments` and adds a self-test proving a docblock-only occurrence is rejected, so deleting the real assignment can no longer leave the gate green. |
| T-34.2-110 | Tampering | the `jest.mock('os')`/`jest.mock('node:os')` containment mechanism | mitigate | Task 1's `hasContainmentOsMock` gate pins both registrations plus the `homedir` and `userInfo` overrides, so silently deleting the load-bearing fix fails a test. Proven by the deliberate-break check. |
| T-34.2-111 | Tampering | real `~/Library/Logs/GameLib/gamelib.log` via a non-Backend jest project | mitigate | Task 3's Block E forbids frontend/common/preload/meta test files from importing `backend/logger`, `backend/logger/paths`, `backend/constants/paths` or `backend/sidecar/pathShim`, converting `jest.config.js`'s hand-maintained scope claim into an enforced invariant. |
| T-34.2-112 | Denial of service | diagnosability of a real Block C tripwire failure | mitigate | Task 2 decouples the two self-tests from the live directory listing, so the real failure is reported once with a correct message instead of being buried under two misleading ones. |
| T-34.2-113 | Repudiation | `bootstrap.test.ts` tripwire comment | mitigate | Task 3 corrects the preventer/detector claim and names the module that actually prevents; the comment previously overstated the protection the test provides. |
| T-34.2-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks in this plan (`git diff` on `package.json`/`pnpm-lock.yaml` must be empty); no legitimacy gate required. |
</threat_model>

<verification>
- Commenting out `process.env.HOME = containmentRoot` fails Block D's `HOME` gate (it did not before).
- Commenting out the `node:os` registration fails the new mechanism gate.
- A synthetic containment module with only the bare-`'os'` registration is rejected by the predicate.
- A real unclassified `*.test.ts` in the sidecar test directory produces exactly one failure.
- A frontend/common/preload/meta test file importing `backend/logger/paths` fails Block E.
- `bootstrap.test.ts`'s change is comment-only.
- Full backend project: only `rustInvokeChannel.test.ts` fails.
- `npx tsc --noEmit`, `npx prettier --check` on both files, and `cargo check --quiet` exit 0.
</verification>

<success_criteria>
WR-01, WR-02 and WR-05 are closed with self-tests proving each corrected gate rejects the input it
previously accepted; CR-02's secondary hand-maintained scope claim is enforced by Block E; WR-10's
remaining comment is corrected; `loggerCallSiteGuard.test.ts` is classified and the backend baseline
is back to its single documented red.
</success_criteria>

<output>
Create `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-29-SUMMARY.md` when done, including all four deliberate-break outputs verbatim (WR-01 HOME, WR-02 node:os, WR-05 one-failure probe, Block E cross-project import) and the reconciled backend sweep counts.
</output>
