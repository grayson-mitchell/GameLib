---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 07
type: execute
wave: 4
depends_on: [34.2-01, 34.2-02, 34.2-03, 34.2-04, 34.2-05, 34.2-06]
files_modified:
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-HUMAN-UAT.md
  - .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VALIDATION.md
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
autonomous: true
requirements: [REQ-34.2-13]
must_haves:
  truths:
    - "A reader of 34.2-PORTED-CHANNELS.md can see all 26 channels with their port kind and honest proof level, and reconcile the set against IPC-PORT-INVENTORY.md exactly."
    - "All three declaration riders live in the artifact, not in a reader's head."
    - "The two deferred-UAT items name D-02 and D-07 specifically with reproduction steps, never a generic live-UAT placeholder."
    - "SEAM.md's slice-5 rows have moved from the deferred backlog to the ported section, and the running wired/re-routed tally reconciles."
  artifacts:
    - path: ".planning/phases/34.2-.../34.2-PORTED-CHANNELS.md"
      provides: "The declared 26-channel list, riders, and the honest sign-off"
      contains: "Total: 26"
    - path: ".planning/phases/34.2-.../34.2-HUMAN-UAT.md"
      provides: "The two named deferred live-UAT items with reproduction steps"
    - path: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      provides: "Slice-5 rows moved §3 -> §1, tally updated to 87"
  key_links:
    - from: ".planning/phases/34.2-.../34.2-PORTED-CHANNELS.md"
      to: ".planning/IPC-PORT-INVENTORY.md"
      via: "set equality against the Phase 34.2 26-channel heading list"
      pattern: "26 channels"
    - from: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      to: "34.2-PORTED-CHANNELS.md"
      via: "§1 subsection cross-reference by filename"
      pattern: "34.2-PORTED-CHANNELS.md"
---

<objective>
Close the slice: declare all 26 channels with their honest proof level, record the three
declaration riders, name the two genuinely-deferred live-UAT items, fill out the
validation contract, and move SEAM.md's slice-5 rows from the deferred backlog into the
ported section.

Purpose: the declared list is the artifact the Phase 35 cutover gate reads, and it is the
only place where "what we actually proved" is separated from "what a reader might assume
we proved". This slice can make a genuinely stronger claim than 34.1 did — 26 channels
of data-in/data-out with assertable return shapes, not a visual deliverable jest cannot
see — and that difference must be STATED, not copied from 34.1's wording. It has exactly
two honest exceptions, and they get named.

Output: `34.2-PORTED-CHANNELS.md`, `34.2-HUMAN-UAT.md`, a completed `34.2-VALIDATION.md`,
and a reconciled SEAM.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/IPC-PORT-INVENTORY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-CONTEXT.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-01-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-02-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-03-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-04-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-05-SUMMARY.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-06-SUMMARY.md

<codebase_exploration_rule>
`graphify-out/graph.json` exists. Orient with `graphify query "<question>"` /
`graphify explain "<concept>"` / `graphify path "<A>" "<B>"` BEFORE reading or
grepping raw source files. Read raw files only after graphify has oriented you, or to
confirm specific lines/signatures listed in `<read_first>`.
</codebase_exploration_rule>

<interfaces>
<!-- The declared kind assignments this plan must reconcile against what the six prior
     SUMMARYs record as actually shipped. Verify row by row against the SUMMARYs, NOT
     against this table on faith — if they disagree, the SUMMARYs win and the divergence
     is recorded in the reconciliation section. -->

sidecar invoke (23): getGameInfo · getExtraInfo · getGameSettings · isGameAvailable ·
  getLaunchOptions · kill · repair · changeInstallPath · readConfig · getGameOverride ·
  getGameSdl · getAvailableCyberpunkMods · setCyberpunkModConfig ·
  getGameMetadataOverride · getAllGameOverrides   [plan 34.2-04]
  getKnownFixes · getCrossoverIndex · getWikiGameInfo · getAnticheatInfo · searchStores ·
  getStoreSearchDeals · getStoreSearchStoreMap · removeRecent   [plan 34.2-06]

sidecar send (3): setGameMetadataOverride (+ the `metadataChanged` push riding the
  existing frontend_message relay) · changeGameVersionPinnedStatus · addNewApp
  [plan 34.2-05]

rustInvoke: none. renderer-side (Tauri JS): none. New Rust arms: ZERO — the only Rust
change is one string added to the existing `LONG_RUNNING_CHANNELS` array.

Not channels, but part of the slice and must appear in the artifact: the two bootstrap
wirings (i18next init; `fetchLastestReleases()` + the re-homed `releasesInfoReady` →
`downloadAntiCheatData` listener) from plan 34.2-01.

SEAM.md structure (`.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md`):
  §1 "Ported (real, wired, hardware-provable)" — the last subsection is
    `### App shell + window chrome cluster (real, Phase 34.1) — CLOSED, moved out of §3`
    at line ~322, ending at the `---` before `## 2.` (~line 379). The new subsection goes
    immediately after it, following the same heading convention and bullet style.
  §3 "Deferred (out of scope this phase, the incremental-port backlog)" at line ~406 —
    the ranked table (lines ~413-423), then "**The IPC re-plumb (the headline cost):**"
    paragraph (~425-448) carrying the running tally, currently **61 wired/re-routed
    total**, and an "Also deferred" list (~465-473).
  SEAM.md line ~473 currently reads: "`launcher.ts`'s full Wine/GameConfig/DownloadManager
  pipeline and the other 5 store managers (GOG/Legendary/Nile/Zoom/Sideload) —
  `steamFlowRegistration.ts` deliberately imports only `SteamLibraryManager`/`SteamGame`,
  not `storeManagers/index.ts`'s `libraryManagerMap`." **This claim is now partially
  stale** and Task 3 must correct it honestly rather than leave it.

`.planning/IPC-PORT-INVENTORY.md:43-45` — `## Phase 34.2 — Slice 5 — game details,
settings and overrides (26 channels)` plus its alphabetical backtick-quoted list. D-08
means NO inventory edit is needed this slice (unlike 34.1's D-14 reassignment); the
artifact reconciles against this list by SET EQUALITY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 34.2-PORTED-CHANNELS.md</name>
  <files>.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-PORTED-CHANNELS.md (the whole file — the section layout (§1 port kinds, §2 the table, §3 logged no-ops/declared-empty, §4 divergences, §5 re-deferrals, §6 sign-off, §7 reconciliation), the row format, and the honesty conventions to follow)
    - .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md (the earlier precedent, for the declared-list discipline)
    - all six 34.2-0N-SUMMARY.md files (the source of truth for what SHIPPED — verify every row against these, never against the plan's intent)
    - .planning/IPC-PORT-INVENTORY.md lines 43-45 (the 26-channel list to reconcile against by set equality)
    - .planning/phases/34.2-.../34.2-CONTEXT.md (D-01's, D-07's and D-08's declaration riders, and D-11's sign-off instruction with its "state the difference plainly rather than copying 34.1's wording" clause)
  </read_first>
  <action>
Create `34.2-PORTED-CHANNELS.md` following `34.1-PORTED-CHANNELS.md`'s section layout.

§1 Port kinds — state that this slice introduces NO new kind; it uses `sidecar invoke`
(23) and `sidecar send` (3) only, with zero `rustInvoke` and zero `renderer-side (Tauri
JS)` rows. Note the one Rust-adjacent change (a string added to
`LONG_RUNNING_CHANNELS`) is a timeout-policy edit, not a port kind, and adds no
`dispatch_rust_channel` arm.

§2 The 26 channels — one row per channel: `| Channel | Kind | Backed by | Proof level |
Notes |`. Backed-by names the actual module and function shipped (e.g.
`gamedetails/dispatch.ts`, `anticheat/utils.ts`'s `gameAnticheatInfo`). Proof level is
`unit` where a test exercises the behavior over the real RPC loop — do not invent a
higher level for any row. End with a totals line reconciling 23 + 3 = 26.

Add a short subsection for **the two bootstrap wirings**, which are not channels and must
not be counted in the 26, but are the load-bearing content of the slice: i18next
initialization, and `fetchLastestReleases()` + the re-homed `releasesInfoReady` →
`downloadAntiCheatData` listener. State for each what it fixes (a defect shipped in
Phases 30/32, not a new feature) and what the automated tests do and do not prove.

§3 Declaration riders — all three, stated so a reader cannot over-read the table:
  1. **Steam-runner upstream stubs.** For Steam titles, FOUR ported channels are inert in
     Electron ALREADY: `repair` (`storeManagers/steam/games.ts:1610` logs "not
     implemented until Phase 2"), `changeInstallPath` (`library.ts:790`),
     `changeVersionPinnedStatus` (`library.ts:797`) and `getLaunchOptions`
     (`library.ts:907`) are underscore-arg no-ops. CONTEXT.md D-01 named three; the fourth
     (`changeGameInstallPath`) was found during planning. Porting them faithfully is
     correct — this is Electron's own current behavior, not a regression this phase
     introduces, and not a gap to open a cycle against.
  2. **`getAnticheatInfo` even when primed.** `gameAnticheatInfo` matches on
     `info.storeIds.epic?.namespace` and early-returns `null` on `isWindows`, so even
     with plan 34.2-01's wiring and the data file on disk it returns `null` for Steam
     titles on every platform and `null` for everything on Windows. "Ported and primed"
     must not read as "anticheat badges appear for Steam games."
  3. **Six channels unreachable in a Steam-only workflow.** `readConfig`,
     `getGameOverride`, `getGameSdl` (Legendary/Epic), `getAvailableCyberpunkMods`,
     `setCyberpunkModConfig` (GOG — and `setCyberpunkModConfig` has ZERO frontend callers
     at all), and `addNewApp` (sideload). Ported at parity per D-08 so the Phase 35 gate
     closes with no residue, not because they are exercised.

§4 Declared divergences from the Electron build — record that `getGameSettings` and the
already-ported `requestGameSettings` now BOTH exist on the sidecar as near-duplicates.
This is Electron's own duplication, not introduced here; `requestGameSettings` carries a
`steamLibrary.has(appName)` bottle-launch fix `getGameSettings` does not have (D-09), it
was deliberately not touched, and deduping the two is a recorded Phase 35 cutover
cleanup. State plainly that two settings reads must not both survive into the
Electron-free build by accident.

§5 Re-deferrals — the `getGameSettings`/`requestGameSettings` dedupe (Phase 35); hoisting
`getCrossoverIndex`'s per-game `loadIndex`/`buildMaps` out of the loop (a real
inefficiency the D-10 timeout exemption works around rather than fixes); the ~100
remaining `i18next.t()` call sites that plan 34.2-01 makes work but this slice does not
exercise (a spot-check of the Phase 30/32 paths that were silently emitting `undefined` —
`downloadqueue.ts`, `utils.ts`, `steam/library.ts`, `depotErrors.ts`,
`storeManagerCommon/games.ts` — belongs in Phase 35 or a small follow-up); and the
`tools/dxmt.ts:148` / `wine/manager/ipc_handler.ts:61` `releasesInfoReady` consumers,
which plan 34.2-01 un-starves as a side effect but whose channels belong to slices 6/8.
State explicitly that this slice makes NO reassignment — the inventory needed no edit,
unlike 34.1's D-14.

§6 Sign-off — **write this section fresh; do not paste 34.1's.** State the difference
plainly: 34.1's headline deliverable was visual and interactive and jest structurally
could not see any of it, so "wired and unit-proven" was a weak claim there. This slice is
almost entirely data-in/data-out — 26 channels with assertable return shapes exercised
over the real sidecar RPC loop — so "wired and unit-proven" is genuinely close to
"works", and say so. Then name the TWO honest exceptions and do not soften them:
  - **D-02.** Tests prove `i18next.t()` returns a real string after `init()`. Nobody has
    booted the Tauri build and seen a translated notification render, and the packaged
    build's locale-path resolution (the `publicdir-getapppath-chunking` family) is
    unproven by construction — jest runs from the repo root, where `process.cwd()`
    happens to work.
  - **D-07.** Tests prove the event fires and the data file is written under a tmp home
    with a mocked axios. Nobody has booted the Tauri build and confirmed the real GitHub
    fetch lands `areweanticheatyet.json` in the real `appFolder` across a real network.
End with the standing caution in this project's own words: do not read "wired and
unit-proven" as "seen working" — that is exactly how G-30-02 was declared fixed twice
while the live build hung. Wherever a row's automated proof covers only wiring and not
observable behavior, say so on the row.

§7 Reconciliation — assert SET EQUALITY against
`.planning/IPC-PORT-INVENTORY.md`'s `## Phase 34.2 — Slice 5 ... (26 channels)` list:
every name in that heading's list appears exactly once in §2, and no §2 row names a
channel absent from it. Record that no inventory edit was needed (D-08, no
reassignments). Then reconcile row-by-row against the six SUMMARY files and record any
divergence between what the plans declared and what shipped — if there is none, say so
explicitly, as 34.1's §7 does.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=".planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md"; I=".planning/IPC-PORT-INVENTORY.md";
python3 - "$F" "$I" <<PY
import re,sys
f,i=open(sys.argv[1]).read(),open(sys.argv[2]).read()
inv=re.search(r"## Phase 34\.2 .*?\(26 channels\)\n\n(.+?)\n",i,re.S).group(1)
inv_set={n.strip() for n in re.findall(r"\x60([^\x60]+)\x60",inv)}
assert len(inv_set)==26, f"inventory list is {len(inv_set)}, expected 26"
rows=[l for l in f.split("\n") if l.startswith("| \x60")]
doc_set={re.match(r"\|\s*\x60([^\x60]+)\x60",l).group(1) for l in rows}
missing=inv_set-doc_set; extra=doc_set-inv_set
assert not missing, f"channels missing from the declared table: {sorted(missing)}"
assert not extra, f"declared table names channels not in slice 5: {sorted(extra)}"
assert len(rows)==26, f"expected 26 table rows, found {len(rows)}"
for s in ["Total: 26","games.ts:1610","library.ts:790","library.ts:797","library.ts:907","storeIds.epic","isWindows","requestGameSettings","G-30-02","D-02","D-07"]:
    assert s in f, f"missing required declaration: {s}"
assert "None proves the window on screen" not in f, "34.1 sign-off text was pasted verbatim; D-11 requires a fresh statement"
for h in ["## 1.","## 2.","## 3.","## 4.","## 5.","## 6.","## 7."]:
    assert h in f, f"missing section {h}"
print("OK 26/26 set equality")
PY'</automated>
  </verify>
  <acceptance_criteria>
    - The script above exits 0: the §2 table has exactly 26 rows and set-equals `IPC-PORT-INVENTORY.md`'s slice-5 list.
    - Every row carries a Kind of exactly `sidecar invoke` or `sidecar send`; the kind counts are 23 and 3.
    - All seven sections (§1..§7) are present, plus the bootstrap-wirings subsection under §2.
    - All three riders are present with their file:line citations, including the FOURTH Steam stub (`library.ts:790`).
    - §6 does not reuse 34.1's `None proves the window on screen actually did the thing.` sentence and instead states this slice's stronger claim plus the two named exceptions.
    - §7 states whether any divergence was found between the plans and the SUMMARYs — a bare "no drift" without having checked does not satisfy this; name the SUMMARY files consulted.
  </acceptance_criteria>
  <done>The Phase 35 cutover gate has a declared, reconcilable 26-channel list, and every over-readable claim in it is bounded by a rider.</done>
</task>

<task type="auto">
  <name>Task 2: Record the two deferred live-UAT items and complete 34.2-VALIDATION.md</name>
  <files>.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-HUMAN-UAT.md, .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-VALIDATION.md</files>
  <read_first>
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md (the item format: reproduction steps, expected result, why jest cannot cover it)
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VALIDATION.md (a fully filled-out validation contract to model the completed file on — the per-task map, wave-0 list, manual-only table and sign-off block)
    - .planning/phases/34.2-.../34.2-VALIDATION.md (the current template-placeholder state this task replaces)
    - all six 34.2-0N-SUMMARY.md files (the actual `<automated>` commands run per task, and their results — the map must reconcile against what happened, not what was planned)
    - .planning/phases/34.2-.../34.2-CONTEXT.md D-11 (the instruction that the deferred item must name D-02 and D-07 specifically, "rather than a generic live-UAT placeholder")
  </read_first>
  <action>
Part A — create `34.2-HUMAN-UAT.md` with exactly TWO items, in
`34.1-HUMAN-UAT.md`'s format. Two, not ten: D-11's whole point is that this slice's
unit claim is strong and its exceptions are narrow. A padded list would dilute the two
that matter.

  **UAT-34.2-01 — a translated notification string actually renders (D-02 / REQ-34.2-02).**
  Why manual: jest can assert `i18next.t()` returns a real string; it cannot see the app
  show a notification, and it cannot exercise a PACKAGED build's locale-path resolution
  (jest runs from the repo root where `process.cwd()` happens to resolve `publicDir`
  correctly — the `publicdir-getapppath-chunking` gotcha family). Steps: `pnpm tauri:dev`,
  trigger a repair on a game whose repair path errors, confirm the notification BODY is
  a real sentence and not blank/`undefined`. Then repeat against a PACKAGED build, which
  is the half jest structurally cannot reach. Note that sidecar logs go to
  `~/Library/Logs/GameLib/gamelib.log`, NOT the `tauri:dev` terminal — a reader looking
  in the terminal will wrongly conclude nothing happened.

  **UAT-34.2-02 — the anticheat data file actually appears on disk (D-07 / REQ-34.2-07).**
  Why manual: jest asserts the event fires and the file is written under a tmp home with
  a mocked axios; it cannot prove a real GitHub fetch completes in a live process across
  a real network after a real connectivity transition. Steps: delete
  `<appFolder>/areweanticheatyet.json`, `pnpm tauri:dev`, wait for
  `initOnlineMonitor()`'s online transition, confirm the file reappears; check
  `~/Library/Logs/GameLib/gamelib.log` for the `AreWeAntiCheatYet data downloaded` line.
  Record the rider inline so the tester does not misread a correct result as a failure:
  even with the file present, `getAnticheatInfo` returns `null` for Steam titles on every
  platform and `null` for everything on Windows — the file appearing IS the pass
  condition, not a visible badge.

Close the file with the same honesty sentence used in `34.2-PORTED-CHANNELS.md` §6,
VERBATIM, so neither document can be read in isolation and leave a reader with a
different impression.

Part B — replace `34.2-VALIDATION.md`'s template placeholders with the real contract:
  - Test Infrastructure: Jest 29.x, existing project config; quick command
    `npx jest src/backend/sidecar/__tests__/gameDetailsFlows.test.ts`; full suite
    `pnpm test:ci`; Rust gate `cd src-tauri && cargo check`; estimated runtimes from the
    SUMMARYs' recorded durations.
  - Sampling Rate: per task commit, the task's own targeted `npx jest <file>`; per wave,
    the cross-file sweep
    `npx jest gameDetailsFlows|enrichmentFlows|bootstrapWirings|gameDetailsImportGate|gameDetailsModules|knownFixes|longRunningChannels`;
    before `/gsd-verify-work`, `pnpm test:ci` green plus `cargo check` green.
  - Per-Task Verification Map: ONE ROW PER TASK across all seven plans (20 tasks: 3+3+3+3+2+3+3), with
    the plan, task name, the `<automated>` command actually run, the REQ id(s), the
    threat ref where the task carries one, and the observed status. Reconcile against the
    SUMMARYs — if a plan's shipped command differed from its plan text, record the drift
    in a note rather than the planned command, exactly as 34.1's map does.
  - Wave 0 Requirements: the seven new test files this phase creates, checked against
    disk with `test -f`, not against the planning-time guesses.
  - Manual-Only Verifications: the two items from Part A, cross-referenced to
    `34.2-HUMAN-UAT.md`.
  - Validation Sign-Off: work the checklist honestly. Set `nyquist_compliant: true` in
    the frontmatter ONLY if every one of the 20 tasks carries a concrete `<automated>`
    command and there is no run of 3+ consecutive tasks without one; otherwise set it
    `false` and say which tasks fail the check. Set `status: complete`, `wave_0_complete:
    true`, and the `updated` date. No watch-mode flags anywhere.
  </action>
  <verify>
    <automated>bash -c 'set -e; D=".planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid";
python3 - "$D" <<PY
import re,sys,os
d=sys.argv[1]
u=open(os.path.join(d,"34.2-HUMAN-UAT.md")).read()
v=open(os.path.join(d,"34.2-VALIDATION.md")).read()
p=open(os.path.join(d,"34.2-PORTED-CHANNELS.md")).read()
assert "UAT-34.2-01" in u and "UAT-34.2-02" in u, "both named UAT items required"
assert "UAT-34.2-03" not in u, "D-11 requires exactly two named items"
for s in ["D-02","D-07","tauri:dev","areweanticheatyet.json","gamelib.log"]:
    assert s in u, f"HUMAN-UAT missing {s}"
for ph in ["{pytest 7.x","{quick command}","{full command}","REQ-{XX}","T-34.2-01 / —","{behavior}","{N}"]:
    assert ph not in v, f"VALIDATION.md still carries template placeholder: {ph}"
assert "nyquist_compliant:" in v
assert re.search(r"status:\s*complete",v), "VALIDATION frontmatter status not set to complete"
rows=[l for l in v.split("\n") if re.match(r"\|\s*34\.2-0\d",l) or re.match(r"\|\s*0?\d+\s*\|",l)]
assert len(rows)>=20, f"per-task map has {len(rows)} rows, expected >=20 (one per task)"
# the honesty sentence must be byte-identical across both documents
sign=[s for s in p.split("\n") if "wired and unit-proven" in s]
assert sign, "PORTED-CHANNELS is missing the sign-off sentence"
assert any(s.strip() in u for s in sign), "the sign-off sentence is not reproduced verbatim in HUMAN-UAT"
print("OK")
PY'</automated>
  </verify>
  <acceptance_criteria>
    - `34.2-HUMAN-UAT.md` contains exactly two items, `UAT-34.2-01` and `UAT-34.2-02`, each naming its decision (D-02 / D-07) and its REQ id, with concrete reproduction steps and an explicit pass condition.
    - Both items state why jest cannot cover the behavior, and both name `~/Library/Logs/GameLib/gamelib.log` as the place to look.
    - UAT-34.2-02 carries the anticheat rider inline so a `null` badge is not misread as a failure.
    - `34.2-VALIDATION.md` contains no `{...}` template placeholder; frontmatter has `status: complete`, `wave_0_complete: true`, an `updated` date, and a `nyquist_compliant` value justified in the sign-off section.
    - The per-task map has at least 20 rows (one per task across the seven plans), each with a real `<automated>` command.
    - The Wave 0 list is checked against disk, and any planning-time path that drifted is recorded as a drift note rather than silently corrected.
    - The sign-off sentence appears byte-identically in both `34.2-PORTED-CHANNELS.md` §6 and `34.2-HUMAN-UAT.md`.
  </acceptance_criteria>
  <done>The two genuine gaps are named with steps, and the validation contract records what was actually run rather than what was planned.</done>
</task>

<task type="auto">
  <name>Task 3: Move SEAM.md's slice-5 rows from §3 to §1 and reconcile the tally</name>
  <files>.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</files>
  <read_first>
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md lines 322-380 (the Phase 34.1 §1 subsection — the heading convention, bullet style and level of detail to match)
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md lines 406-475 (§3: the ranked table, the "IPC re-plumb (the headline cost)" tally paragraph, and the "Also deferred" list — in particular the `launcher.ts` / five-store-managers bullet at ~line 473, which this task must correct)
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md lines 565-600 (the Incremental-Port Checklist steps 5/6 this task discharges, and the Load-Bearing Invariants section)
    - .planning/phases/34.2-.../34.2-PORTED-CHANNELS.md (post-Task-1 state — the source for the new subsection's content)
  </read_first>
  <action>
Edit `.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md`.

1. Add a new §1 subsection immediately after the Phase 34.1 one, headed
   `### Game details, settings and overrides cluster (real, Phase 34.2) — CLOSED, moved out of §3`,
   in the same voice and bullet style. It must cross-reference
   `34.2-PORTED-CHANNELS.md` by filename (SEAM's own convention) and cover:
   - 26 channels, 23 `sidecar invoke` + 3 `sidecar send`, NO new port kind, ZERO new
     `dispatch_rust_channel` arms (the arm count stays at 11) — the only Rust change is
     `"getCrossoverIndex"` added to the existing `LONG_RUNNING_CHANNELS` array, a
     timeout-policy edit extending Phase 30's CR-03 mechanism.
   - **The slice's actual content: two startup wirings, not 26 delegations.** i18next was
     never initialized in the sidecar, so `i18next.t()` returned `undefined` (not a
     throw, not the inline default) at every backend call site reachable from the
     sidecar since Phase 30; and `fetchLastestReleases()` — the sole emitter of
     `releasesInfoReady` — was never called, so `getAnticheatInfo` structurally could not
     return data. Both are now wired in `bootstrap.ts`, alongside the re-homed
     `releasesInfoReady` → `downloadAntiCheatData` listener that `anticheat/ipc_handler.ts`
     cannot supply to the sidecar. Generalize the lesson for later slices: Electron
     `app.whenReady()` inits are not auto-run in the sidecar, and the ones that fail
     QUIETLY are the dangerous ones — 34.1's CR-01 caught the loud half of the same bug.
   - Three more Electron-side extractions (`main.ts` → `gamedetails/{dispatch,overrides}.ts`,
     `launcher.ts` → `knownFixes.ts`, `crossover_index/ipc_handler.ts` →
     `crossoverRatingMap.ts`), continuing the behavioral-not-textual invariant 34.1
     established; `electronUntouched.test.ts` again left byte-unchanged, with a
     genuinely new `gameDetailsImportGate.test.ts` carrying this slice's gates.
   - Claim level: unit-proven, with the honest note that this slice's claim is STRONGER
     than 34.1's (data-in/data-out with assertable return shapes over the real RPC loop)
     and that it has exactly two named live-UAT exceptions (D-02, D-07), recorded in
     `34.2-HUMAN-UAT.md`. Do not read "wired and unit-proven" as "seen working".

2. Update the "IPC re-plumb (the headline cost)" paragraph in §3: append this slice to
   the running enumeration in the established style and change the running total from
   **61 wired/re-routed total** to **87**. Do the arithmetic against
   `34.2-PORTED-CHANNELS.md`'s totals line and state the 23/3 split.

3. Correct the now-stale "Also deferred" bullet that claims `steamFlowRegistration.ts`
   imports only `SteamLibraryManager`/`SteamGame` and not `libraryManagerMap`. As of this
   slice, `gameDetailsFlowRegistration.ts` dispatches runner-generically through
   `libraryManagerMap` for all six managers — a deliberate decision (D-01), resting on the
   Phase 32 D-02 finding that `storeManagers/index.ts` force-constructs all six in the
   sidecar regardless, so narrowing would buy nothing and would mean shipping a runner
   guard Electron does not have. Rewrite the bullet to say what is STILL deferred:
   `launcher.ts`'s full Wine/GameConfig/DownloadManager pipeline (only `readKnownFixes`
   was extracted out of it, precisely to avoid importing the module). Do not delete the
   bullet — correct it, and note the date and the slice that made it stale.

4. Update the ranked §3 table only where this slice genuinely changes a row's status.
   Do NOT mark a row closed that this slice did not close.

Leave §2, the Accepted Constraints, the Incremental-Port Checklist and the Load-Bearing
Invariants byte-unchanged. Do not renumber or reflow any section this task does not
name.
  </action>
  <verify>
    <automated>bash -c 'set -e; S=".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md";
python3 - "$S" <<PY
import re,sys
s=open(sys.argv[1]).read()
assert "### Game details, settings and overrides cluster (real, Phase 34.2) — CLOSED, moved out of §3" in s, "new §1 subsection missing or misnamed"
i1=s.index("## 1. Ported"); i2=s.index("## 2. Stubbed"); i3=s.index("## 3. Deferred")
new=s.index("### Game details, settings and overrides cluster")
assert i1<new<i2, "the new subsection is not inside §1"
assert "34.2-PORTED-CHANNELS.md" in s[i1:i2], "§1 subsection does not cross-reference the declared list"
assert "87" in s[i3:], "running tally not updated to 87"
assert "61 wired/re-routed total" not in s, "stale 61 tally still present"
assert "deliberately imports only" not in s or "libraryManagerMap" in s[i3:], "stale steamFlowRegistration claim not corrected"
for s_ in ["Accepted Constraints","Incremental-Port Checklist","Load-Bearing Invariants","## 2. Stubbed"]:
    assert s_ in s, f"section lost: {s_}"
print("OK")
PY
git diff --stat .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md'</automated>
  </verify>
  <acceptance_criteria>
    - SEAM.md §1 contains the new `### Game details, settings and overrides cluster (real, Phase 34.2) — CLOSED, moved out of §3` subsection, positioned after the Phase 34.1 one and before `## 2.`, cross-referencing `34.2-PORTED-CHANNELS.md` by filename.
    - The subsection states 26 channels / 23 invoke + 3 send / zero new port kinds / zero new `dispatch_rust_channel` arms (count stays 11), and names the two bootstrap wirings as the slice's real content.
    - The §3 headline-cost tally reads 87 and enumerates this slice in the established style; the string `61 wired/re-routed total` no longer appears.
    - The stale `launcher.ts` / five-store-managers bullet is corrected — not deleted — and names D-01, the Phase 32 D-02 basis, and what remains deferred.
    - `## 2. Stubbed / Minimal`, `## Accepted Constraints`, `## Incremental-Port Checklist` and `## Load-Bearing Invariants` are byte-unchanged (`git diff` shows no hunk inside them).
    - No §3 ranked-table row is marked closed that this slice did not close.
  </acceptance_criteria>
  <done>SEAM.md tells the truth about this slice: 26 channels moved to ported, the tally reconciles at 87, and a claim this slice invalidated was corrected rather than left standing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → future reader/agent | `34.2-PORTED-CHANNELS.md` and SEAM.md are the inputs the Phase 35 cutover gate and every later slice reason from. An overclaim here becomes a wrong decision later. |
| automated proof → claimed proof level | Every row asserts a proof level that a reader will act on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.2-29 | Repudiation | "Wired and unit-proven" being read later as "seen working" | mitigate | §6 is written fresh (Task 1's verify fails if 34.1's sentence is pasted), states the stronger claim this slice can honestly make, and names D-02/D-07 as the two exceptions. The identical sentence appears in `34.2-HUMAN-UAT.md`, asserted byte-identically by Task 2's verify, so neither document can be read in isolation and mislead. This is the exact conflation that let G-30-02 be declared fixed twice while the live build hung. |
| T-34.2-30 | Tampering | A channel silently dropping out of the declared list, breaking the Phase 35 completion gate | mitigate | Task 1's verify performs SET EQUALITY (not count equality) between the §2 table and `IPC-PORT-INVENTORY.md`'s slice-5 list, and fails on both a missing and an extra name. §7 additionally reconciles row-by-row against the six SUMMARYs, with the plans' declared intent explicitly subordinate to what shipped. |
| T-34.2-31 | Repudiation | A stale SEAM.md claim (the `libraryManagerMap` bullet) surviving and misleading a later slice's import-graph reasoning | mitigate | Task 3 requires the bullet be CORRECTED rather than deleted, naming the date, the slice, D-01 and the Phase 32 D-02 basis, so the change of fact is auditable rather than a silent rewrite. Its verify asserts the stale phrasing is gone. |
| T-34.2-32 | Tampering | Collateral edits to SEAM.md's Accepted Constraints / Invariants while moving rows | mitigate | Task 3's acceptance requires those four sections be byte-unchanged, checked by `git diff` hunk inspection. |
| T-34.2-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan is documentation only; it touches no source file, no manifest, and installs nothing. |
</threat_model>

<verification>
- All three tasks' automated scripts exit 0.
- `pnpm test:ci` full suite green and `cd src-tauri && cargo check --quiet` green — the phase gate, run before `/gsd-verify-work`.
- `git diff --stat` lists only the four declared documents; no source file is modified by this plan.
- `.planning/IPC-PORT-INVENTORY.md` is byte-unchanged (D-08: no reassignment this slice, unlike 34.1's D-14).
</verification>

<success_criteria>
- 26 channels declared with kind and honest proof level, set-equal to the inventory.
- Three declaration riders and the `getGameSettings`/`requestGameSettings` divergence recorded in the artifact rather than in a reader's head.
- Exactly two deferred live-UAT items, naming D-02 and D-07 with reproduction steps and pass conditions.
- `34.2-VALIDATION.md` completed against what actually ran; SEAM.md reconciled at 87 with its stale claim corrected.
</success_criteria>

<output>
Create `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-07-SUMMARY.md` when done
</output>
