---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 03
subsystem: ipc
tags: [electron, tauri, sidecar, known-fixes, crossover-index, ipc-extraction, jest]

# Dependency graph
requires:
  - phase: 34.2-02
    provides: the D-01/D-03/D-08 extraction pattern (Electron-free module + main.ts one-line delegation + direct-call test suite) this plan mirrors for launcher.ts and crossover_index/ipc_handler.ts
provides:
  - "src/backend/knownFixes.ts: readKnownFixes, extracted out of launcher.ts (D-05), importable without pulling launcher.ts's Wine pipeline into the sidecar's import graph"
  - "src/backend/crossover_index/crossoverRatingMap.ts: buildCrossoverRatingMap + its D-11/D-16 three-state docstring, extracted out of crossover_index/ipc_handler.ts (D-06), importable without triggering an addHandler side effect"
  - "5-test direct-call proof suite for readKnownFixes (knownFixes.test.ts)"
  - "ratingMap.test.ts retargeted at crossoverRatingMap.ts, jest.mock('backend/ipc') block removed, anti-remerge source-gate test added"
affects: [34.2-04, 34.2-05, 34.2-06, 34.2-07]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "D-05/D-06 extraction pattern (mirrors 34.2-02's D-01/D-03/D-08): move the reusable function verbatim into a new Electron-free sibling module when the function's ONLY host file is either deliberately excluded from the sidecar graph (launcher.ts) or co-locates the function with its own addHandler side-effect registration (crossover_index/ipc_handler.ts); the host file imports the function back for its unchanged consumer/registration"

key-files:
  created:
    - src/backend/knownFixes.ts
    - src/backend/crossover_index/crossoverRatingMap.ts
    - src/backend/__tests__/knownFixes.test.ts
  modified:
    - src/backend/launcher.ts
    - src/backend/main.ts
    - src/backend/crossover_index/ipc_handler.ts
    - src/backend/crossover_index/__tests__/ratingMap.test.ts

key-decisions:
  - "knownFixes.ts imports readFileSync from 'fs' (not 'graceful-fs') to match launcher.ts's actual import source for that symbol -- the plan's <interfaces> block described both existsSync and readFileSync as coming from graceful-fs, but launcher.ts's own import block splits them across two modules (existsSync from graceful-fs, readFileSync from fs); verified against the live source before writing the new module rather than trusting the plan's paraphrase"
  - "main.ts's refreshCrossoverRatingMap() import redirected from './crossover_index/ipc_handler' to './crossover_index/crossoverRatingMap' -- not in the plan's Task 2 <files> list, but ipc_handler.ts no longer exports buildCrossoverRatingMap after the extraction, so this was a blocking compile fix (Rule 3), not scope creep"

requirements-completed: [REQ-34.2-05, REQ-34.2-06, REQ-34.2-14]

# Metrics
duration: 5min
completed: 2026-07-25
---

# Phase 34.2 Plan 03: Known-Fixes and CrossOver Rating Map Extraction Summary

**Extracted `readKnownFixes` out of `launcher.ts` into `knownFixes.ts` (D-05) and `buildCrossoverRatingMap` out of `crossover_index/ipc_handler.ts` into `crossoverRatingMap.ts` (D-06), closing the two remaining "the function I need lives inside a file the sidecar can never import" traps in this slice.**

## Performance

- **Duration:** 5 min (commit-to-commit; investigation/read time not included)
- **Started:** 2026-07-25T21:02:36+12:00
- **Completed:** 2026-07-25T21:07:17+12:00
- **Tasks:** 3
- **Files modified:** 7 (3 created, 4 modified)

## Accomplishments
- `knownFixes.ts` holds `readKnownFixes`, moved verbatim from `launcher.ts:1042-1059` (same `try`/`catch`, same `logWarning` interpolation, same `KnowFixesInfo` cast, same `null` returns); `launcher.ts`'s `installFixes` imports it back unchanged, and the now-dead `fixesPath`/`storeMap`/`KnowFixesInfo` imports were removed from `launcher.ts`
- `crossoverRatingMap.ts` holds `buildCrossoverRatingMap` with its D-11/D-16 three-state docstring moved verbatim; `ipc_handler.ts` is reduced to two imports plus the single `addHandler('getCrossoverIndex', ...)` line, with no re-export
- `ratingMap.test.ts` retargeted at `../crossoverRatingMap`, its `jest.mock('backend/ipc', ...)` block removed (no longer needed -- the module under test has no `addHandler` call of its own), and a new anti-remerge test added that fails if `addHandler` is ever pasted back into `crossoverRatingMap.ts` (test count 6 -> 7)
- `knownFixes.test.ts` adds 5 direct-call tests (all tagged `REQ-34.2-05`): no-file returns null, well-formed file parses, malformed JSON returns null AND logs a warning, two runners with the same appName resolve two different filenames (`storeMap[runner]` proven load-bearing), and a guard asserting `fixesPath` resolves under `os.tmpdir()`
- Both RED conditions spot-checked and reverted before committing: removing `storeMap[runner]` from the path construction fails 3/5 tests (including test 4); replacing the `try`/`catch` with a bare `JSON.parse` fails test 3 on the malformed-JSON case with an uncaught `SyntaxError`

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract readKnownFixes into src/backend/knownFixes.ts (D-05)** - `f03f95d3` (feat)
2. **Task 2: Extract buildCrossoverRatingMap into crossover_index/crossoverRatingMap.ts (D-06)** - `137a522d` (feat)
3. **Task 3: Direct-call tests for the extracted known-fixes reader** - `99cd1450` (test)

**Plan metadata:** (this commit) `docs(34.2-03): complete known-fixes and crossover rating map extraction plan`

## Files Created/Modified
- `src/backend/knownFixes.ts` - `readKnownFixes`, verbatim from `launcher.ts`
- `src/backend/launcher.ts` - deletes `readKnownFixes`, imports it back from `./knownFixes`; drops now-unused `fixesPath`/`storeMap`/`KnowFixesInfo` imports
- `src/backend/main.ts` - `getKnownFixes` registration's import source redirected to `./knownFixes`; `refreshCrossoverRatingMap()`'s import source redirected to `./crossover_index/crossoverRatingMap` (blocking fix, see Decisions)
- `src/backend/crossover_index/crossoverRatingMap.ts` - `buildCrossoverRatingMap` + its D-11/D-16 docstring, verbatim from `ipc_handler.ts`
- `src/backend/crossover_index/ipc_handler.ts` - reduced to two imports and the single `addHandler('getCrossoverIndex', ...)` registration line
- `src/backend/crossover_index/__tests__/ratingMap.test.ts` - retargeted at `../crossoverRatingMap`, `jest.mock('backend/ipc')` block removed, anti-remerge source-gate test added
- `src/backend/__tests__/knownFixes.test.ts` - new 5-test direct-call proof suite for `readKnownFixes`

## Decisions Made
- `knownFixes.ts` sources `readFileSync` from `'fs'` and `existsSync` from `'graceful-fs'`, matching `launcher.ts`'s own split import (not the plan's `<interfaces>` paraphrase, which described both as coming from `graceful-fs`) -- verified against the live import block before writing the new module
- `main.ts`'s `refreshCrossoverRatingMap()` import redirected alongside the `getCrossoverIndex` registration's import, since `ipc_handler.ts` no longer exports `buildCrossoverRatingMap` after the D-06 extraction and `main.ts` was not in Task 2's declared `<files>` list for this specific call site

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Redirected main.ts's second buildCrossoverRatingMap import site**
- **Found during:** Task 2 (`npx tsc --noEmit` after reducing `ipc_handler.ts`)
- **Issue:** `main.ts` imports `buildCrossoverRatingMap` from `./crossover_index/ipc_handler` at TWO call sites: the interfaces-documented `getCrossoverIndex` registration AND `refreshCrossoverRatingMap()` (a WR-05 fix that re-invokes the map builder on library-membership changes, `main.ts:371-380`), which the plan's `<interfaces>` block did not mention. After Task 2 removed the export from `ipc_handler.ts`, this second call site broke the build (`TS2459: Module declares 'buildCrossoverRatingMap' locally, but it is not exported`).
- **Fix:** Redirected the single `import { buildCrossoverRatingMap } from './crossover_index/ipc_handler'` line in `main.ts` to `./crossover_index/crossoverRatingMap`, which now correctly backs both call sites.
- **Files modified:** src/backend/main.ts
- **Verification:** `npx tsc --noEmit` clean; `npx jest src/backend/crossover_index/` 52/52 green.
- **Committed in:** `137a522d` (Task 2 commit)

### Deferred (out of scope, logged not fixed)

**1. Pre-existing eslint error in `src/backend/crossover_index/__tests__/index.test.ts:29`**
- `@typescript-eslint/no-unnecessary-type-assertion`, confirmed pre-existing via `git stash` + re-run against the unmodified tree. This plan never touches `index.test.ts`. Logged to `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/deferred-items.md` per the scope-boundary rule rather than fixed inline.

---
**Total deviations:** 1 auto-fixed (1 blocking), 1 deferred (pre-existing, out of scope)
**Impact on plan:** The main.ts fix was a one-line import-source redirect required for the build to compile after Task 2's extraction; no scope creep, no behavior change. The deferred eslint error is unrelated to this plan's files.

## Issues Encountered
None beyond the deviation documented above.

## User Setup Required
None - no external service configuration required.

## Electron Regression Check (manual, not a gate)

Not run live this session (no interactive `npm start` session available in this
executor context). Confidence is high without it: both extractions are byte-identical
verbatim moves (confirmed via the automated verify scripts' comment-stripped source
diffs), `launcher.ts`'s `installFixes` and `main.ts`'s `getKnownFixes`/`refreshCrossoverRatingMap`
call sites are unchanged apart from their import source, `npx tsc --noEmit` is clean
across the whole project, and the full backend Jest suite (`src/backend/__tests__/`,
`src/backend/crossover_index/`, `src/backend/gamedetails/__tests__/`) is green
(324 tests across the touched suites). Recommended follow-up: a human should confirm
`npm start` still launches and a game's Settings page still resolves known fixes before
this branch ships, per the plan's own verification note that `launcher.ts` is on the
launch path.

## Next Phase Readiness
- `readKnownFixes` and `buildCrossoverRatingMap` are both now importable by the Node
  sidecar with no Electron dependency and no side-effect registration, unblocking a
  future plan's sidecar registration of `getKnownFixes`/`getCrossoverIndex`
- `installFixes` and the Electron `getCrossoverIndex`/`refreshCrossoverRatingMap` paths
  are behaviorally unchanged (verbatim extraction)
- No blockers identified for 34.2-04 through 34.2-07

---
*Phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid*
*Completed: 2026-07-25*

## Self-Check: PASSED

All created files confirmed on disk (`src/backend/knownFixes.ts`,
`src/backend/crossover_index/crossoverRatingMap.ts`,
`src/backend/__tests__/knownFixes.test.ts`, this SUMMARY). All three task commit
hashes (`f03f95d3`, `137a522d`, `99cd1450`) confirmed present in `git log`.
