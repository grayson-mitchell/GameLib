---
phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid
plan: 08
subsystem: infra
tags: [tauri, sidecar, ipc-timeout, rust, react, error-handling]

# Dependency graph
requires:
  - phase: 34.2-06
    provides: "the LONG_RUNNING_CHANNELS exemption mechanism and its longRunningChannels.test.ts pin (getCrossoverIndex precedent)"
provides:
  - "repair and readConfig exempted from the sidecar's 60s bounded invoke timeout"
  - "an eight-member LONG_RUNNING_CHANNELS exact-set pin with named per-channel tests"
  - "a non-rethrowing try/catch around the renderer's repair() call site, logged via window.api.logError"
affects: [34.2-VERIFICATION, 34.2-REVIEW, phase-35-electron-cutover]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-channel timeout exemption via LONG_RUNNING_CHANNELS array membership (Rust), guarded by a comment-stripped exact-set test pin (TS)"
    - "Renderer floating-promise call sites caught with try/catch + window.api.logError, never rethrown, matching GamePage/index.tsx:288 convention"

key-files:
  created: []
  modified:
    - "src-tauri/src/main.rs"
    - "src/backend/__tests__/longRunningChannels.test.ts"
    - "src/frontend/screens/Game/GameSubMenu/index.tsx"

key-decisions:
  - "readConfig is exempted per-channel, not per-argument, because timeout_for() only sees the channel name — a wedged readConfig('user') (normally fast) becomes a never-settling promise instead of a 60s rejection; this tradeoff is recorded inline in the one-line code comment (T-34.2-35, accepted)."
  - "The renderer catch logs and does not rethrow, since onClick: async () => onRepairYesClick(appName) is a floating promise the dialog framework never awaits — rethrowing would recreate the exact unhandled-rejection problem one frame up."
  - "No new user-facing dialog/toast was added for repair failures; scope was strictly 'cannot become an unhandled rejection', matching the plan's explicit non-goal."

patterns-established: []

requirements-completed: [REQ-34.2-12]

# Metrics
duration: 12min
completed: 2026-07-25
---

# Phase 34.2 Plan 08: Long-running repair/readConfig timeout exemption + renderer catch Summary

**Exempted `repair` and `readConfig` from the sidecar's 60s invoke timeout (Rust `LONG_RUNNING_CHANNELS`) and added a non-rethrowing catch + `logError` around the renderer's floating `repair()` call, closing verification gap REQ-34.2-12 / review finding CR-01.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-25T11:52:17Z
- **Completed:** 2026-07-25T11:56:14Z
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- `repair` and `readConfig` now resolve to `None` (unbounded) in `timeout_for()`, matching the same honest-failure-mode contract the six pre-existing exemptions already have
- `longRunningChannels.test.ts`'s exact-set pin extended to the new eight-member array, with two new named per-channel tests and a widened six-member pre-existing-survivor loop
- The renderer's `onRepairYesClick` can no longer produce an unhandled promise rejection — a failed `repair()` invoke is caught and logged via `window.api.logError`, matching the established `GamePage/index.tsx:288` convention

## Task Commits

Each task was committed atomically:

1. **Task 1: Add repair and readConfig to LONG_RUNNING_CHANNELS and update the exact-set pin in the same commit** - `3f43fa8b` (fix)
2. **Task 2: Catch and log the repair rejection at the renderer call site** - `e0c5da86` (fix)

**Plan metadata:** (pending — this commit)

## Files Created/Modified
- `src-tauri/src/main.rs` - Added `"repair"` and `"readConfig"` to `LONG_RUNNING_CHANNELS`, each with a one-line rationale comment; `INVOKE_TIMEOUT`, `timeout_for()`, and all `dispatch_rust_channel` arms left byte-unchanged
- `src/backend/__tests__/longRunningChannels.test.ts` - Extended `EXPECTED_LONG_RUNNING_CHANNELS` to eight members, widened the pre-existing-survivor test to six members, added two named per-channel tests for `repair` and `readConfig`
- `src/frontend/screens/Game/GameSubMenu/index.tsx` - Wrapped `await repair(appName, runner)` in `onRepairYesClick` with try/catch, logging via `window.api.logError` on rejection, no rethrow

## Decisions Made
- readConfig exemption applies to the whole channel (not just `readConfig('library')`), an accepted tradeoff recorded in-code per threat T-34.2-35 — see key-decisions above.
- Renderer catch logs and swallows rather than rethrowing, to avoid recreating the floating-promise unhandled-rejection problem at the `onClick` call site one frame up.

## Deviations from Plan

None - plan executed exactly as written. Both tasks matched their `<action>` and `<acceptance_criteria>` blocks with no Rule 1-4 triggers.

## Issues Encountered

One benign eslint warning was introduced at `GameSubMenu/index.tsx:147` (`Invalid type "unknown" of template literal expression` on the caught `error` in the new `logError` call) — this is the same warning class already present at the established convention site `GamePage/index.tsx:288` for the identical `${error}` pattern on a caught, implicitly-`unknown`-typed variable. `npx eslint` still exits 0 with 0 errors (18 warnings total, up from the pre-existing 17), consistent with the acceptance criterion "exits 0 with no errors" — not a regression, just the same accepted warning class appearing at a second call site.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- REQ-34.2-12 is complete; gap #1 (CR-01) is closed.
- Remaining gap plans 34.2-09..14 in this gap cycle are unaffected by this plan's file set (`src-tauri/src/main.rs`, `longRunningChannels.test.ts`, `GameSubMenu/index.tsx` — none overlap with other gap plans per the phase's wave-safety analysis).
- No blockers for the next gap plan in sequence.

---
*Phase: 34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid*
*Completed: 2026-07-25*
