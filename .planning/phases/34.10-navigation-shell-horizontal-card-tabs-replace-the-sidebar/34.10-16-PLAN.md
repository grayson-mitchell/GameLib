---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 16
type: execute
wave: 3
depends_on: ["34.10-13", "34.10-14", "34.10-15"]
files_modified:
  - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [REQ-34.10-05, REQ-34.10-08, REQ-34.10-09, REQ-34.10-16]

must_haves:
  truths:
    - "Every sub-check recorded NOT ATTEMPTED in run 1 is measured in run 2 — Games tab stays lit on a game page, tier-2 collapses with no empty 204px band, and all four Electron tabs navigate"
    - "The two fixed defects are re-measured on a real build, in more than one theme"
    - "Run 1's recorded verdicts survive verbatim as historical evidence; run 2 is additive"
    - "The preflight results block is filled this time, including the exactly-one-process check"
    - "The phase closes only on 5/5, and any unreached sub-check is recorded NOT ATTEMPTED rather than inferred"
  artifacts:
    - path: ".planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md"
      provides: "The run-2 contract, its re-derived reachability and pairwise review, and the recorded run-2 verdicts"
      contains: "Run 2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.10-05/-08/-09/-16 resolved against a measured run rather than a unit test"
      contains: "REQ-34.10-16"
  key_links:
    - from: "34.10-LIVE-GATE.md run 2"
      to: ".planning/REQUIREMENTS.md"
      via: "each run-2 item names the REQ id whose checkbox its verdict decides"
      pattern: "REQ-34.10"
---

<objective>
Re-run the blocking live gate against the fixed build and close phase 34.10 — or record honestly that
it still does not close.

Purpose: run 1 scored 2/5 with two blocking defects and three sub-checks never reached. Plans 34.10-13,
-14 and -15 fixed the two defects. Nothing in this repository can confirm that: the `Frontend` jest
project is `testEnvironment: 'node'` with no jsdom, no CSS engine and no download queue. A live gate
has beaten a green suite on this project three times.
Output: run-2 verdicts recorded item by item, and the four Pending 34.10 requirements resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-13-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-14-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-15-SUMMARY.md

<interfaces>
<!-- What changed since run 1, and therefore which run-1 verdicts are now stale -->
  34.10-13  Dropdown/index.tsx, Dropdown/index.scss, CategoryFilter, LibraryFilters,
            NavShell/index.scss (.NavShell__tier2Portal gains overflow-y)
            -> stales Item 3 entirely
  34.10-14  DownloadsRing/index.scss (mask, token chain, idle opacity, 16px)
            -> stales Item 4; touches navbar colour tokens, so it also touches Item 1's surface
            -> the ring is one of Item 2's three no-drag surfaces
  34.10-15  DownloadsRing/index.tsx (+ its test file)
            -> stales Item 4

<!-- Run 1's recorded state, to be preserved, not overwritten -->
  frontmatter: verdict: fail, items_total: 5, items_passed: 2, run_date: 2026-08-08,
               run_order: [1, 3, 4, 2, 5], findings: F-34.10-01, F-34.10-02
  Item 1 PASS · Item 2 PASS · Item 3 FAIL · Item 4 FAIL · Item 5 NOT ATTEMPTED
  §2 Preflight Results block: ALL FOUR CHECKBOXES LEFT UNFILLED — flagged by 34.10-VERIFICATION.md
  as a paperwork gap in the gate's own contract. Closing it is part of this plan.

<!-- Themes, for item 1 (never evaluate the nav in midnightMirage alone) -->
  midnightMirage  navbar #161c1e over body #070a0b
  gruvbox_dark    navbar #32302f over body #282828
  dracula         navbar #44475a over body #282a35   (navbar MUCH lighter — worst case)
  dracula-classic (non-blocking spot-check)

<!-- Suite baseline: DO NOT copy run 1's numbers -->
  Run 1's preflight expected 216 suites / 4214 tests with exactly 1 failure. Plans 34.10-13/-14/-15
  ADD test files and cases, so that baseline is now stale. This project has already shipped one gate
  whose own baseline was stale (recorded in the 34.4.2 cycle). Derive the current expected numbers
  from the three SUMMARYs before running, and record both expected and observed.
  The one legitimate failure remains meta/__tests__/hardcodedStringGate.test.ts on
  src/frontend/screens/WebView/index.tsx:347, owned by a concurrent session's uncommitted tree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the run-2 contract, with its own re-derived reachability and pairwise review</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md (the whole file — §2's unfilled preflight block, §3's run-1 review, §4's recorded verdicts, §5's rules)
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md (the seven defect-class tests and the pairwise requirement-interaction pass; the unit of the pairwise pass is the PAIR, not the item)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-13-SUMMARY.md, 34.10-14-SUMMARY.md, 34.10-15-SUMMARY.md (what actually shipped, so each item's expectations match the code rather than this plan's intent)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md (the three gaps, and the anti-pattern row about the unfilled preflight block)
  </read_first>
  <action>
    Edit `34.10-LIVE-GATE.md` ADDITIVELY. Do not delete, reword or renumber anything in §0-§5: run 1's
    item tables, its findings and its review are historical evidence and a future reader must be able
    to see what was measured when. Make exactly these changes:

    1. Add a one-line pointer at the top of §4 stating that the verdicts recorded in §4 are **run 1's**
       (2026-08-08) and that run 2 is recorded in §7 below.
    2. Fill nothing in §2's Preflight Results block — that block belongs to run 1 and its moment has
       passed. Instead, §6 (below) carries a fresh preflight block for run 2, and §6 states plainly
       that run 1's block was left unfilled, which `34.10-VERIFICATION.md` recorded as a paperwork gap.
    3. Append `## 6. Run 2 — scope, preflight, and re-derived review`.
    4. Append `## 7. Run 2 — the items`.
    5. Update the frontmatter: keep `items_total: 5`; add `run_number: 2`; add a `runs:` list whose
       first entry records run 1 (`run: 1, date: 2026-08-08, verdict: fail, items_passed: 2`); reset
       each entry in `items[]` to `status: pending` for run 2 and reset `items_passed: 0` and
       `verdict: null`. Leave `findings:` intact and append run-2 findings to it if any arise.

    **§6 must contain, written out, not referenced:**

    *Scope, with per-item justification.* Every item is in scope for run 2, but for different reasons,
    and item 2 is deliberately partial:

    | Item | Run-2 scope | Why |
    |---|---|---|
    | 1 | FULL re-run, all 3 scored themes + `dracula-classic` spot-check, PLUS a new per-theme observation of the idle Downloads ring | 34.10-14 changed navbar colour tokens (`--divider` -> `--navbar-inactive`/`--navbar-accent`) and 34.10-13 changed `NavShell/index.scss`. Run 1 observed the idle ring in one theme only; `dracula`'s navbar is the lightest shipped and is the worst case for the new track token. |
    | 3 | FULL re-run, all 5 sub-checks, including the two recorded NOT ATTEMPTED | 34.10-13 rewrote `Dropdown`'s trigger and geometry and added a scroll container to `.NavShell__tier2Portal`. The two unreached sub-checks (Games tab stays lit on a game page; tier-2 collapses with no empty 204px band) must be measured, never inferred. |
    | 4 | FULL re-run, all 5 sub-checks, PLUS a cross-surface discriminator sub-check | 34.10-14 and 34.10-15 both changed this component. The discriminator is added so a second failure is diagnosable in the same session instead of costing another gap cycle. |
    | 2 | PARTIAL: re-run ONLY "drag by the Downloads ring does NOT move the window" | 34.10-14 changed the ring's stylesheet, and the ring is one of item 2's three no-drag surfaces. `resolveDragRegion` (`tauriWindowChrome.ts:301-334`) and `.NavShell__navbar` were not touched by any of the three fix plans, so the other four sub-checks stand on run 1's measurement. State this reasoning inline so the partial scope is a recorded judgement, not an omission. Item 2's run-2 verdict is run 1's four sub-checks plus run 2's re-measured fifth. |
    | 5 | Re-run "shell renders" and the NOT ATTEMPTED "all 4 tabs navigate" | The tab-navigation sub-check has never been measured. The traffic-light observation from run 1 (NO overlap) stands and is not re-taken. |

    *Preflight for run 2*, as a table plus a results block with slots that must be filled BEFORE item 1:
    - P1 — build matches the committed tree. Run `pnpm jest --runInBand --silent --forceExit` (this
      phase's documented `test:ci` hang workaround, **not** bare `pnpm test:ci`). **Do not copy run 1's
      216/4214 figure** — derive the current expected suite and test counts from the three fix plans'
      SUMMARYs and write both the expected and the observed numbers into the results block. Exactly one
      failure is legitimate: `meta/__tests__/hardcodedStringGate.test.ts` on
      `src/frontend/screens/WebView/index.tsx:347`, from a concurrent session's uncommitted tree. More
      than one is a real regression and stops the gate.
    - P2 — `pnpm codecheck` clean.
    - P3 — exactly one app process before the FIRST launch. `pgrep -f 'tauri dev'` and
      `pgrep -f 'target/debug/gamelib'` must both be empty. A concurrent second instance is this
      project's own recorded capture-integrity trap (F-34.4.2-15/-18): it does not announce itself, it
      just makes a half-observed run look measured.
    - P4 — the library contains at least one game with a distinctive title (item 3's search sub-check
      is vacuous against an empty grid) and at least one game whose **page** can be opened (item 3's
      two previously-unreached sub-checks both need a game page to exist).
    - P5 — at least one game available to start a download (item 4's active sub-checks and the
      discriminator).
    - P6 — `settings.framelessWindow` is OFF at the start of the run.
    - P7 — **the download queue is EMPTY at the start of the run.** New for run 2, and load-bearing:
      item 1 now includes a per-theme *idle* ring observation, which item 4's download destroys. See
      the pairwise table below.

    *Structural reachability review for run 2*, applying the seven defect-class tests from the skill
    reference to every run-2 sub-check, with a REACHABLE / NOT REACHABLE verdict and evidence per row.
    Give particular attention to the sub-checks that have never been measured, since an unreachable
    scenario is exactly how run 1's item-3 sub-checks went unrecorded and how an earlier phase shipped
    an item whose scenario the UI forbids:
    - "Games tab stays lit on a game page" — confirm in source that `resolveActiveTab` returns `games`
      for a `/gamepage/*` path, so the scenario the operator is asked to observe is producible at all.
    - "Tier-2 column collapses, no empty 204px band" — confirm `NavShell/index.tsx:50-53`'s
      `tier2Collapsed` condition (`activeTab === 'games' && !filled`) is actually reachable from a game
      page, i.e. that `Library` (and therefore its portalled `<Header>`) is unmounted there.
    - "All 4 tabs navigate under Electron" — confirm `npm start` still builds and that no fix plan
      touched an Electron-only path.
    - The new item-4 discriminator — confirm the `/download-manager` screen renders its own percent
      readout (`DownloadManagerItem/index.tsx:91`), so there is something to compare against.

    *Pairwise review for run 2*, re-derived rather than copied from §3. Reduce over
    {state-mutating action} × {evidence-bearing item}. The state-mutating actions in run 2 are:
    (a) theme switching, (b) expanding a tier-2 disclosure panel — NEW, introduced by 34.10-13,
    (c) starting a download, (d) toggling `framelessWindow`, (e) quitting Tauri to launch Electron.
    Hunt specifically for anything that restarts, truncates, clears or destroys evidence another item
    depends on, and order destructive actions last. At minimum these must appear as rows with a verdict:
    - **(c) against item 1 — DESTROYS.** Item 1's new per-theme idle-ring observation requires an empty
      queue. Once item 4 starts a download the idle state is gone for the rest of the session. This is
      the reason for preflight P7 and for item 1 running first; record it, and record that re-running
      item 1 for any reason requires emptying the queue first.
    - **(b) against item 3's own later sub-checks.** An expanded disclosure left open changes the
      tier-2 column's scroll state before the game-page collapse is observed. Resolve it by requiring
      both disclosures be closed before the collapse sub-check, and say so in the item's steps.
    - **(d) against items 1, 3, 4** — run 1's finding stands: the frameless toggle changes window
      chrome and must run after every by-eye observation. Re-state it rather than cross-referencing.
    - **(e) against items 1-4's recorded verdicts** — verdicts live in this markdown file, written as
      measured, so a process restart cannot erase them. This is the direct application of the
      F-34.4.2-11 lesson.
    State the pairs considered and the pairs flagged, and fix `run_order` accordingly. The expected
    outcome is `[1, 3, 4, 2, 5]` again, but derive it; do not assert it.

    **§7 must contain** one sub-section per item, each with a table of sub-checks and a blank verdict
    slot accepting exactly `PASS`, `FAIL` or `NOT ATTEMPTED`, plus a notes column required on any FAIL
    or NOT ATTEMPTED. The sub-checks are:

    - **Item 1** — per-theme rows for `midnightMirage`, `gruvbox_dark`, `dracula` (scored) and
      `dracula-classic` (non-blocking spot-check), each covering BOTH the tab-merge seam and the idle
      Downloads ring being visible-but-dimmed. Keep the ring observation in its own column so a ring
      failure cannot be mistaken for a seam failure.
    - **Item 3** — (i) search text filters the grid; (ii) the Categories control opens on CLICK, not on
      mouseover, and its panel renders wholly inside the tier-2 column with every checkbox reachable;
      (iii) clicking the Categories trigger a second time closes it; (iv) toggling a category visibly
      changes the grid; (v) the Filters control behaves identically and a filter visibly changes the
      grid; (vi) with both disclosures closed, on a game page the Games tab stays lit; (vii) on that
      same game page the tier-2 column collapses to nothing rather than showing an empty 204px band.
      Sub-checks (vi) and (vii) are the two recorded NOT ATTEMPTED in run 1 — they must be measured.
    - **Item 4** — (i) idle: the ring is visible and dimmed, and reads as a RING, not a filled disc;
      (ii) hover at idle: still a ring, not a solid disc (this is the exact run-1 symptom); (iii)
      active: the arc fills and advances as the download progresses; (iv) the count badge appears and
      is correct; (v) clicking reaches `/download-manager` with no tier-1 tab lit and the ring showing
      the active state; (vi) **discriminator** — while the same download runs, the `/download-manager`
      screen's own per-item percent readout advances. Record (vi) even when (iii) passes: if (iii)
      fails while (vi) passes the fault is ring-local, and if both fail the fault is the app-wide
      progress feed. That single extra observation is what stops a third gap cycle.
    - **Item 2** — one row only: frameless ON, dragging by the Downloads ring does NOT move the window.
      State inline that the other four sub-checks stand on run 1 and why.
    - **Item 5** — (i) the Electron shell renders; (ii) all four tier-1 tabs navigate. Note that the
      run-1 traffic-light observation (NO overlap) stands and is not re-taken.

    Re-state §5's two rules verbatim in §7's preamble so the operator does not have to scroll: the
    phase closes only on 5/5, and any unreached sub-check is recorded NOT ATTEMPTED with a reason,
    never inferred as a PASS from a green suite and never inferred as a FAIL from silence.

    Do not run the gate in this task. Decision D-E: the task that authors a gate contract may never
    also run it.

    Stage with explicit per-file `git add` paths only — never `git add -A`, `git add .` or
    `git commit -a`. Another session is editing this repository;
    `src-tauri/src/main.rs`, `src/backend/humble/*`, `src/frontend/screens/WebView/*`,
    `.vscode/settings.json`, `scratchpad/`, `steam_appid.txt` and `.planning/phases/34.7-*` are
    off-limits.
  </action>
  <acceptance_criteria>
    - `34.10-LIVE-GATE.md` contains `## 6.` and `## 7.` sections, and every heading and table from §0 through §5 is byte-identical to before except the single run-1 pointer line added at the top of §4 (verify with `git diff` and state the diff's shape in the SUMMARY)
    - Run 1's five item verdicts (PASS, PASS, FAIL, FAIL, NOT ATTEMPTED) and both findings are still present in the file
    - Frontmatter carries `run_number: 2`, a `runs:` list containing run 1's `items_passed: 2` and `verdict: fail`, `items_passed: 0`, `verdict: null`, and all five `items[].status` reset to `pending`
    - §6 contains a preflight table with rows P1..P7 and an unfilled results block with a slot per row
    - §6's P1 row does NOT contain the numbers 216 or 4214, and instructs deriving the expected counts from the three fix SUMMARYs
    - §6 contains a reachability review with a REACHABLE / NOT REACHABLE verdict per run-2 sub-check, including explicit rows for the three sub-checks never previously measured
    - §6 contains a pairwise table with at least the four rows named in the action, one of which is verdict DESTROYS for (download start) against (item 1's idle-ring observation), and states pairs considered and pairs flagged
    - §7's item 3 has seven sub-check rows and item 4 has six, each with an empty verdict slot
    - §7's item 4 includes the `/download-manager` percent-readout discriminator row
    - §7 restates the no-partial-pass rule and the NOT ATTEMPTED rule
    - The file's YAML frontmatter still parses
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md';const s=fs.readFileSync(p,'utf8');const fm=s.split('---')[1];for(const k of ['run_number: 2','items_passed: 0','verdict: null'])if(!fm.includes(k))throw new Error('frontmatter missing '+k);if(!/runs:/.test(fm))throw new Error('no runs: history list');if(!/##\s*6\./.test(s)||!/##\s*7\./.test(s))throw new Error('missing section 6 or 7');const s6=s.slice(s.indexOf('## 6.'));for(const p7 of ['P7'])if(!s6.includes(p7))throw new Error('missing preflight '+p7);if(/216|4214/.test(s6))throw new Error('stale suite baseline copied into run-2 preflight');if(!/DESTROYS/.test(s6))throw new Error('pairwise review records no destructive pair');if(!/NOT ATTEMPTED/.test(s6))throw new Error('NOT ATTEMPTED rule not restated');if(!/F-34.10-01/.test(s)||!/F-34.10-02/.test(s))throw new Error('run 1 findings were destroyed');console.log('run-2 contract authored, run-1 evidence intact')"</automated>
  </verify>
  <done>Sections 6 and 7 exist with a scoped re-run, a P1..P7 preflight carrying a freshly-derived suite baseline, a reachability review covering the three never-measured sub-checks, a re-derived pairwise review that flags the download-vs-idle-ring destruction, and blank per-sub-check verdict slots — with run 1's record untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the gate on real hardware and resolve the four Pending requirements</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md §6 and §7 (task 1's contract — the scope, the preflight, and the run order the pairwise review fixed)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-13-SUMMARY.md, 34.10-14-SUMMARY.md, 34.10-15-SUMMARY.md (what shipped, and the current expected suite/test counts for preflight P1)
    - .planning/REQUIREMENTS.md lines 365-380 and the REQ-34.10-05/-08/-09/-16 rows (the four checkboxes this run decides)
  </read_first>
  <action>
    Run the preflight, record P1..P7 into §6's results block, then walk §7's items in the contract's
    fixed order, recording each sub-check verdict into the file **as it is measured**, never batched at
    the end. Use exactly `PASS`, `FAIL` or `NOT ATTEMPTED` — no other value, no blanks.

    Then update the frontmatter's `items[].status`, `items_passed` and `verdict` alongside the body so
    the two never drift. Set `verdict: pass` only if `items_passed == 5`.

    Then update `.planning/REQUIREMENTS.md`:
    - On 5/5: tick REQ-34.10-05, REQ-34.10-08, REQ-34.10-09 and REQ-34.10-16, each with a one-line
      citation naming `34.10-LIVE-GATE.md` run 2 and its date, and update the traceability table rows
      at lines 365-380 from `Pending` to `Complete`. Also re-confirm REQ-34.10-07, whose "becomes an
      ambient ring" framing `34.10-VERIFICATION.md` flagged as entangled with F-34.10-02 — if item 4
      passed, append a line to its entry recording that the entanglement is resolved by run 2.
    - On anything less than 5/5: change nothing to Complete. Append the run-2 result to each affected
      requirement's entry, mint any new finding as `F-34.10-NN` in the gate file with what was observed
      (not merely that it failed), and report that the phase does not close.

    Do not edit `.planning/STATE.md` in this task. This project has a recorded defect where
    `gsd-sdk query state.*` writes corrupt `progress.percent` via a `min(planFraction, phaseFraction)`
    resync; leave state updates to the orchestrator and hand-check them afterwards.

    Stage with explicit per-file `git add` paths only.
  </action>
  <what-built>
    Three fix plans landed since run 1:
    - **34.10-13** turned `Dropdown` from a hover-triggered, right-anchored, 250px-minimum popup into a
      click-toggled in-flow disclosure sized to the 204px column, dropped `popUpOnHover` from both
      consumers, and gave `.NavShell__tier2Portal` a scroll container. This closes F-34.10-01.
    - **34.10-14** replaced the Downloads ring's opaque `::after` "hole" — which was a guess about the
      backdrop that the Link's own `:hover` falsified — with a CSS mask, so the centre is genuinely
      transparent in every state; repainted the idle track in the same token chain as the count badge
      the operator confirmed visible; and raised idle opacity from 0.5 to 0.65. This closes
      F-34.10-02 cause (a).
    - **34.10-15** fixed the arc's data binding per the root cause diagnosed live in 34.10-12, and gave
      `RingProgress` its first test coverage. This closes F-34.10-02 cause (b).

    None of that is confirmed. Every automated check in this phase is a `testEnvironment: 'node'`
    structural test — no jsdom, no CSS engine, no compositor, no download queue. Nothing in the repo
    can see whether the panel fits, whether the ring is a ring, or whether the arc moves.
  </what-built>
  <how-to-verify>
    Work through `34.10-LIVE-GATE.md` §6 then §7, writing each verdict into that file as you go.

    **Preflight — fill §6's results block before touching item 1.**
    1. `pnpm jest --runInBand --silent --forceExit` (NOT bare `pnpm test:ci`, which is known to hang
       under some shells this phase). Record the expected and observed suite/test counts. Exactly one
       failure is legitimate — `meta/__tests__/hardcodedStringGate.test.ts` on
       `src/frontend/screens/WebView/index.tsx:347`, which belongs to another session's uncommitted
       tree. More than one failure: stop, that is a regression.
    2. `pnpm codecheck`.
    3. `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'` — both must print nothing. If
       either prints a PID, kill it or identify whose it is before launching. A second instance makes a
       half-observed run look measured.
    4. Confirm the library has a game with a distinctive title AND a game whose page you can open.
    5. Confirm a game is available to download.
    6. Confirm Settings -> General -> "Frameless window" is OFF.
    7. **Confirm the download queue is EMPTY.** Item 1 now includes an idle-ring observation that item
       4's download permanently destroys for this session.

    **Item 1 — theme survival + idle ring (run first).** `pnpm tauri:dev`. In `midnightMirage`, then
    `gruvbox_dark`, then `dracula`, then spot-check `dracula-classic`: click all four tier-1 tabs and
    judge (a) whether the active tab reads as continuous with the content surface below — no visible
    seam, no band boundary cutting across it — and (b) whether the Downloads ring in the navbar's
    right cluster is visible, dimmed, and shaped like a RING rather than a filled dot or nothing at
    all. Record both per theme. A tab or ring that works in the default theme and fails in `dracula`
    is a FAIL: `dracula`'s navbar is the lightest shipped and is the whole reason this item exists.

    **Item 3 — tier-2 filter controls (run second).** On the Games tab:
    1. Type a game name into the tier-2 search box — the grid must filter.
    2. **Click** (do not hover) the Categories control. It must open on the click, not on mouseover,
       and the panel must render entirely inside the 204px column with every checkbox visible and
       clickable. Nothing may extend past the left edge of the window.
    3. Click the Categories trigger again — it must close.
    4. Re-open it and toggle a category — the grid must respond.
    5. Repeat 2-4 for the Filters control.
    6. **Close both controls**, then open a game page. The Games tab must stay lit.
    7. On that same game page, the tier-2 column must collapse away entirely — not leave an empty
       204px band. Watch for a stray scrollbar appearing during the collapse; report it if you see one.
    Sub-checks 6 and 7 were never reached in run 1. If you cannot reach them, record NOT ATTEMPTED with
    the reason — do not guess.

    **Item 4 — Downloads ring (run third).** With the queue still empty:
    1. Look at the ring: visible, dimmed, ring-shaped.
    2. Hover it: it must still read as a ring. A solid filled circle here is the exact run-1 symptom
       and is a FAIL.
    3. Start a download. The arc must fill and keep advancing as the download progresses.
    4. The count badge must appear with the right number.
    5. Click the ring: you land on `/download-manager`, no tier-1 tab is lit, and the ring shows its
       active state.
    6. **While the same download is still running**, look at the `/download-manager` screen's own
       per-item percent readout. Does it advance past 0%? Record this even if step 3 passed — it is
       what tells us, in one session, whether any future failure is ring-local or app-wide.

    **Item 2 — one sub-check only (run fourth).** Turn Settings -> General -> "Frameless window" ON.
    Try to drag the window by the Downloads ring — it must NOT move. Turn frameless back OFF. The
    other four drag sub-checks stand on run 1 and are not re-taken; only the ring's stylesheet changed.

    **Item 5 — Electron (run last).** Quit the Tauri build entirely and re-check that
    `pgrep -f 'tauri dev'` is empty, so the two shells never run at once. Run `npm start`. Confirm the
    shell renders and **click all four tier-1 tabs, confirming each navigates**. That tab-navigation
    sub-check has never been measured. Do not fix anything cosmetic you see here — Phase 35 deletes
    Electron.

    Record every verdict in `34.10-LIVE-GATE.md` §7 as you measure it. Set the frontmatter `verdict` to
    `pass` only on 5/5. Mark anything unreached NOT ATTEMPTED with a reason. If any item FAILs, the
    phase does not close: write up what was observed and report it.
  </how-to-verify>
  <acceptance_criteria>
    - §6's preflight results block has every slot P1..P7 filled, with P1 carrying both expected and observed suite/test counts and P3 carrying the `pgrep` outcome
    - Every sub-check row in §7 carries exactly one of `PASS`, `FAIL`, `NOT ATTEMPTED` — no blanks, no `TBD`
    - Item 3's sub-checks (vi) and (vii) and item 5's "all 4 tabs navigate" each carry a recorded verdict — these three were NOT ATTEMPTED in run 1 and a blank here is itself a defect in the run
    - Item 1 carries four per-theme rows, each with both a seam verdict and a ring verdict
    - Item 4 carries the `/download-manager` percent-readout discriminator verdict
    - Frontmatter `items_passed` equals the count of items whose sub-checks all PASS, and `verdict: pass` appears only if that count is 5
    - Every FAIL carries a note describing what was observed, not only that it failed
    - Run 1's §0-§5 content is still present and unmodified apart from task 1's pointer line
    - `.planning/REQUIREMENTS.md` is updated consistently with the recorded verdict — four checkboxes ticked only on 5/5, otherwise none of them
    - `.planning/STATE.md` is unmodified by this task
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md';const s=fs.readFileSync(p,'utf8');const fm=s.split('---')[1];if(/verdict:\s*null/.test(fm))throw new Error('run 2 verdict still null');const m=fm.match(/items_passed:\s*(\d)/);if(!m)throw new Error('items_passed not recorded');const passed=Number(m[1]);const isPass=/verdict:\s*pass/.test(fm);if(isPass&&passed!==5)throw new Error('verdict pass with items_passed='+passed);const s7=s.slice(s.indexOf('## 7.'));if(/\bTBD\b/.test(s7))throw new Error('unfilled slot in run 2');const rows=s7.split(/\r?\n/).filter(l=>l.trim().startsWith('|')&&/\|\s*(PASS|FAIL|NOT ATTEMPTED)\s*\|/.test(l));if(rows.length<18)throw new Error('too few recorded sub-check verdicts: '+rows.length);const s6=s.slice(s.indexOf('## 6.'),s.indexOf('## 7.'));if(/\[ \]/.test(s6))throw new Error('preflight results block still has unfilled checkboxes');const req=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const ticked=['REQ-34.10-05','REQ-34.10-08','REQ-34.10-09','REQ-34.10-16'].filter(r=>new RegExp('\\\\[x\\\\]\\\\s*\\\\*\\\\*'+r).test(req)).length;if(isPass&&ticked!==4)throw new Error('gate passed but '+ticked+'/4 requirements ticked');if(!isPass&&ticked!==0)throw new Error('gate did not pass but '+ticked+' requirements were ticked');console.log('run 2 recorded, requirements consistent')"</automated>
    <human-check>All five items measured on real hardware in the contract's order, with the three previously-NOT-ATTEMPTED sub-checks measured, the idle ring judged in all four themes, and every verdict written into 34.10-LIVE-GATE.md as it was taken</human-check>
  </verify>
  <done>Run 2 is fully recorded with no blank slots, the preflight block is filled including the single-process check, the three never-measured sub-checks now carry verdicts, and REQUIREMENTS.md matches the measured outcome exactly.</done>
  <resume-signal>Type "approved" once every §7 sub-check carries a verdict and §6's preflight block is filled, or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → native window operations | Item 2 exercises the real `startDragging` path that unit tests only mock |
| operator observation → recorded verdict | An inferred or remembered verdict is indistinguishable in the file from a measured one |
| concurrent session → this repository | Another agent is editing files this run must not stage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-42 | Repudiation | a PASS recorded for a sub-check that was never reached | mitigate | Every sub-check has its own slot accepting only PASS/FAIL/NOT ATTEMPTED; the automated check rejects blanks and `TBD` and requires at least 18 recorded rows; the three previously-unreached sub-checks are named individually in the acceptance criteria |
| T-34.10-43 | Tampering | a stale suite baseline making a regression look expected | mitigate | P1 forbids copying run 1's 216/4214 figure and requires deriving expected counts from the three fix SUMMARYs, recording expected AND observed |
| T-34.10-44 | Denial of service | item 4's download destroying item 1's idle-ring observation | mitigate | Preflight P7 requires an empty queue; the pairwise review records the pair as DESTROYS; item 1 runs first |
| T-34.10-45 | Tampering | a concurrent second app instance splitting the observation | mitigate | P3's `pgrep` check is recorded in the results block, and item 5 re-checks it before launching Electron |
| T-34.10-46 | Repudiation | overwriting run 1's evidence while recording run 2 | mitigate | Run 2 is appended as §6/§7; task 1's acceptance criteria require §0-§5 and both findings to survive, verified by `git diff` shape |
| T-34.10-47 | Tampering | staging another session's uncommitted work | mitigate | Explicit per-file `git add` only; the off-limits path list is restated in both tasks |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists in this plan |
</threat_model>

<verification>
- `34.10-LIVE-GATE.md` frontmatter carries a non-null run-2 `verdict`, an `items_passed` count matching §7's recorded verdicts, and a `runs:` history preserving run 1
- §6's preflight results block is fully filled, including expected-vs-observed suite counts and the `pgrep` outcome
- No slot in §7 is blank or `TBD`
- The three sub-checks recorded NOT ATTEMPTED in run 1 all carry a run-2 verdict
- `.planning/REQUIREMENTS.md`'s four Pending 34.10 checkboxes are ticked if and only if `items_passed == 5`
- `.planning/STATE.md` untouched by this plan
</verification>

<success_criteria>
- All five items measured on the fixed build, in the order the re-derived pairwise review fixed
- The idle Downloads ring judged in all four themes, never in `midnightMirage` alone
- The `/download-manager` percent discriminator recorded regardless of whether the arc passed, so a
  further failure is diagnosable without another gap cycle
- Phase 34.10 closes on 5/5, or does not close and says exactly why
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-16-SUMMARY.md` when done
</output>
