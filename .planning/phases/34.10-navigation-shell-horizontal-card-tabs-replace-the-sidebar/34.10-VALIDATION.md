---
phase: 34.10
slug: navigation-shell-horizontal-card-tabs-replace-the-sidebar
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-07
---

# Phase 34.10 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `34.10-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29 via `ts-jest`, 5-project `jest.config.js` |
| **Config file** | `src/frontend/jest.config.js` (Frontend project) · `src/preload/jest.config.js` (Preload project) |
| **Quick run command** | `npx jest --config src/frontend/jest.config.js <path-to-single-test-file>` |
| **Full suite command** | `pnpm test:ci` (all 5 projects, `--runInBand --silent`) |
| **Estimated runtime** | ~77 seconds (full suite) |
| **Baseline (re-verified this session)** | 204/204 suites · 4069/4069 tests · green |

> ⚠ **HARD CONSTRAINT — no DOM.** The `Frontend` jest project is `testEnvironment: 'node'`.
> There is **no jsdom and no component rendering**. Every frontend assertion in this phase
> must use the repo's existing *structural* idiom: direct function invocation, element-tree
> inspection of returned React elements, or source-text gates over SCSS/TSX. Any plan task
> whose acceptance criteria assume `render()`, `screen.getBy*`, or `userEvent` is
> **unexecutable as written** and must be rewritten or reclassified as a manual checkpoint.

---

## Sampling Rate

- **After every task commit:** `npx jest --config src/frontend/jest.config.js <touched-test-file>`
  (targeted — the full suite is too slow for per-task on a phase that touches many small files)
- **After every plan wave:** `pnpm test:ci` — must stay at 204+N suites / 4069+N tests, zero regressions
- **Before `/gsd-verify-work`:** full suite green **AND** both live-app checkpoints below executed and recorded
- **Max feedback latency:** ~15s targeted · ~77s full suite

---

## Per-Task Verification Map

> Synced against the 11 committed plans (`ed356aa07`). 23 tasks across 7 waves.
> The authoritative automated command for each task is that task's own
> `<verify><automated>` block inside its PLAN.md — this table records the mapping and
> the sampling obligation, not a second copy of the commands.

| Task ID | Plan | Wave | Requirement | Threat Ref | Verified Behavior | Test Type | File Exists | Status |
|---------|------|------|-------------|------------|-------------------|-----------|-------------|--------|
| 34.10-01-01 | 01 | 1 | REQ-34.10-01, -11 | — | Tab identity, route resolution, default-store cascade; `resolveActiveTab` returns `null` for `/download-manager` | unit (structural) | ❌ W0 | ⬜ pending |
| 34.10-01-02 | 01 | 1 | REQ-34.10-09 | — | `Tier2PortalContext` seam exists and the Games portal target consumes it | unit (structural) | ❌ W0 | ⬜ pending |
| 34.10-02-01 | 02 | 1 | REQ-34.10-03 | — | `NavItem` row primitive re-parented off `.Sidebar`; zero `.Sidebar` refs remain in its CSS | unit (source gate) | ❌ W0 | ⬜ pending |
| 34.10-02-02 | 02 | 1 | REQ-34.10-01, -10 | — | `--tier2-width` (204px) and `--traffic-light-inset` (0px) each declared exactly once repo-wide; no hard-coded `78px` | unit (source gate, `shellTokens.test.ts`) | ❌ W0 | ⬜ pending |
| 34.10-03-01 | 03 | 1 | REQ-34.10-07, -08 | — | `DownloadsRing` keeps an always-mounted queue subscription and reads keyed progress | unit (structural) | ❌ W0 | ⬜ pending |
| 34.10-03-02 | 03 | 1 | REQ-34.10-08 | — | Conic-gradient fill, theme-following inset, idle state (D-07 inverts the idle guard) | unit (source gate) | ❌ W0 | ⬜ pending |
| 34.10-04-01 | 04 | 2 | REQ-34.10-02, -11 | — | Four route-driven tabs on reskinned MUI `<Tabs>`; `NavLink`-driven, not state-driven | unit (structural) | ❌ W0 | ⬜ pending |
| 34.10-04-02 | 04 | 2 | REQ-34.10-06, -14 | — | Relational seam renders; MUI default indicator suppressed; `nav.tabs.games` key carries no `gamelib:` prefix | unit (source gate) | ❌ W0 | ⬜ pending |
| 34.10-05-01 | 05 | 2 | REQ-34.10-11, -14 | — | Stores tier-2 panel — 8 destinations with their guards preserved | unit (structural) | ❌ W0 | ⬜ pending |
| 34.10-05-02 | 05 | 2 | REQ-34.10-11, -14 | — | Settings tier-2 panel incl. the 3 **reparented** destinations (Wine Manager, Accessibility, Console Mode) + relocated `QuitButton` | unit (structural) | ❌ W0 | ⬜ pending |
| 34.10-06-01 | 06 | 2 | REQ-34.10-07 | — | `Header` becomes a vertical stack and drops the frameless padding hack | unit (source gate) | ❌ W0 | ⬜ pending |
| 34.10-06-02 | 06 | 2 | REQ-34.10-09 | — | `Library` **portals** `Header` into the tier-2 target from inside its own provider — filter controls stay wired to real `LibraryContext` setters, not the no-op `initialContext` | unit (structural) | ❌ W0 | ⬜ pending |
| 34.10-07-01 | 07 | 3 | REQ-34.10-12, -13 | — | `HeroicVersion` relocated + CSS re-parented off `.Sidebar`; the tour's **second** entry point (`TourButton tourId={SIDEBAR_TOUR_ID}`) removed | unit (source gate) | ❌ W0 | ⬜ pending |
| 34.10-07-02 | 07 | 3 | REQ-34.10-01, -11, -12 | — | `NavShell` root; **unconditional** `HeroicVersion` mount (hidden, never unmounted); `handleGoToScreen` subscription **and** its unsubscribe carried over | unit (structural, all 5 tab states) | ❌ W0 | ⬜ pending |
| 34.10-08-01 | 08 | 4 | REQ-34.10-01 | — | Shell mounted in `App.tsx` behind the tier-2 portal provider; console-mode branch deliberately excluded | unit (structural) | ❌ W0 | ⬜ pending |
| 34.10-08-02 | 08 | 4 | REQ-34.10-04 | — | `.App` grid restructured **without** renaming the F-10 gate's selectors | unit (source gate) | ✅ `Login/__tests__/index.test.tsx` re-run as gate | ⬜ pending |
| 34.10-08-03 | 08 | 4 | REQ-34.10-05, -09 | — | Drag region migrated off `.Sidebar` with **all three** `FakeElement` fixtures updated in the SAME commit as `tauriWindowChrome.ts:333` | unit (mocked `closest()`) | ✅ `src/preload/__tests__/framelessRuntime.test.ts:235,243,256` — UPDATE, do not create | ⬜ pending |
| 34.10-09-01 | 09 | 5 | REQ-34.10-11 | — | Destination-coverage suite — full replacement for the deleted `SidebarLinks/__tests__/index.test.tsx` | unit (structural) | ❌ W0 | ⬜ pending |
| 34.10-09-02 | 09 | 5 | REQ-34.10-13 | — | Tour-disabled gate: both entry points unreachable; `SidebarTour.tsx` preserved on disk with zero importers | unit (source gate) | ❌ W0 | ⬜ pending |
| 34.10-09-03 | 09 | 5 | REQ-34.10-15 | — | Retired `Sidebar` tree deleted; no dangling importers | unit (source gate) + build | ❌ W0 | ⬜ pending |
| 34.10-10-01 | 10 | 6 | REQ-34.10-14 | — | The one new translation key synced **without** touching unrelated catalog drift (`pnpm i18n` is recorded as churning the three upstream `en/` catalogs) | automated | ✅ existing catalogs | ⬜ pending |
| 34.10-10-02 | 10 | 6 | REQ-34.10-15 | — | `pnpm gen-i18n-gate-scope` re-run, `meta/i18nGateScope.json` committed, and the gate **proven to have scanned the new files** | automated, **blocking** | ✅ gate exists — snapshot regenerated | ⬜ pending |
| 34.10-11-01 | 11 | 7 | REQ-34.10-16 | — | Live-gate contract authored | doc | ❌ W0 | ⬜ pending |
| 34.10-11-02 | 11 | 7 | REQ-34.10-04, -05, -06, -09, -16 | — | **BLOCKING human checkpoint** — live gate on real hardware; see Manual-Only Verifications below | live-app | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Replacement for `src/frontend/components/UI/SidebarLinks/__tests__/index.test.tsx` — structural tests against the new tier-1/tier-2 shell components (the existing file pins exact label order/adjacency and will not compile once `SidebarLinks` is deleted; it needs full replacement, not a patch)
- [ ] `HeroicVersion` mount + side-effect survival test
- [ ] `SidebarTour` non-mount / disabled-state test
- [ ] `--tier2-width` single-definition source-gate test
- [ ] **`pnpm gen-i18n-gate-scope` re-run and the regenerated `meta/i18nGateScope.json` committed** — the gate scope is a static human-regenerated snapshot, NOT a live glob. New nav component files are invisible to the blocking i18n gate until this runs. This is a *task*, not a convention.
- [ ] Framework/config install: **none needed** — the `Frontend` and `Preload` jest projects already exist and suit this phase's no-DOM, direct-invoke idiom

---

## Manual-Only Verifications

> Project lesson on the record: **"live gate beat a green suite three times."** A green
> `pnpm test:ci` built from this phase's own mocked tests is NOT proof that dragging or
> theme-merging works on a real build. Both rows below are blocking phase-gate items.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Card-tab "merge into content surface" illusion survives every shipped theme | D-04 / REQ-34.10-06 | No jsdom, no rendering, no screenshot capability in the jest setup. This is a pixel-level figure/ground effect. | Per plan 11's gate contract: launch `pnpm tauri:dev` and cycle all four tier-1 tabs in `midnightMirage`, `gruvbox_dark`, and `dracula`, spot-checking `dracula-classic` (the dracula variants invert the navbar/body relationship). Confirm the active tab reads as continuous with the content surface in each. **Never evaluate nav in `midnightMirage` alone.** Record a separate verdict per theme. |
| Real window dragging under WKWebView after `.Sidebar` retirement | D-02 / REQ-34.10-05 | WKWebView does not support `-webkit-app-region`; the `closest('.Sidebar')` fallback at `tauriWindowChrome.ts:333` is the ACTIVE runtime path. The unit test mocks `closest()` and therefore proves only the JS branch logic, never the real webview behavior. | On a live Tauri build, drag the window by the new tier-1 tab strip and by the empty chrome around it. Confirm the window moves. Then confirm dragging still works with `framelessWindow` toggled both ways. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all ❌ MISSING references above
- [ ] No watch-mode flags in any command
- [ ] Feedback latency < 77s
- [ ] Both Manual-Only rows executed and recorded (not waived)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
