---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
verified: 2026-08-09T00:00:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 6/9 (run-3-based; superseded run-4 has since closed)
  gaps_closed:
    - "F-34.10-03 (navbar seam) — CLOSED, live gate run 4 item 1 Step 1 PASS in the scored theme (midnightMirage) AND all three scored themes in Step 2 (midnightMirage, gruvbox_dark, dracula)"
    - "F-34.10-04 (wordmark/strip/ring one line) — CLOSED, same run-4 measurements, first time either finding was measured in a theme this contract scores"
    - "Gamepad focus-scroll regression — still NOT ATTEMPTED (no controller hardware), but no longer an open gap: run 4 recorded it as a named permanent residual risk per its own escape hatch (P9), and ROADMAP.md/REQUIREMENTS.md carry it forward explicitly rather than leaving it a pending cell"
    - "REQ-34.10-06 and REQ-34.10-16 — now Complete in REQUIREMENTS.md, closed by run 4's 5/5 verdict"
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "Gamepad focus-scroll regression (scrollCardIntoView) survives the scroll-container relocation"
    addressed_in: "Named permanent residual risk on REQ-34.10-06 / ROADMAP.md; not scheduled to a numbered future phase, but explicitly non-blocking per the gate's own P9 escape hatch and not silently dropped"
    evidence: "34.10-LIVE-GATE.md §10.5 item 1: 'NOT ATTEMPTED... carried forward by this plan into ROADMAP.md and REQUIREMENTS.md as a named, permanent residual risk'; ROADMAP.md confirms this language is present"
  - truth: "Pill-tab restyle (operator's run-3-time election) supersedes the card/folder framing"
    addressed_in: "Explicit operator decision recorded in ROADMAP.md 'Deferred from this phase — pill-tab restyle election'"
    evidence: "ROADMAP.md: 'REQ-34.10-06's card/folder framing STANDS unchanged... The pill-tab restyle is DEFERRED to separate follow-up work'"
  - truth: "CR-01 (undefined --navbar-active token, no fallback, in gruvbox_dark) and CR-02 (missing -webkit-app-region: drag on .NavShell__navbar, breaking Electron frameless dragging) are fixed"
    addressed_in: "Explicit operator decision to defer, recorded in 34.10-REVIEW.md and carried forward in ROADMAP.md, rather than patch after the gate already passed"
    evidence: "ROADMAP.md 'Carried forward from the phase-34.10 code review... deliberately NOT fixed in this phase... deferred on the operator's explicit decision, because patching CSS after the gate would mean HEAD no longer matches the bundle that passed'"
human_verification: []
---

# Phase 34.10: Navigation shell — horizontal card tabs replace the sidebar Verification Report

**Phase Goal:** Replace the left sidebar with a two-tier navigation shell. Tier 1 is a horizontal
row of card/folder tabs (Manage Accounts, Games, Stores, Settings) where the active tab merges into
the content surface below it. Tier 2 is a vertical contextual panel scoped to the selected tab.
`Sidebar` and `SidebarLinks` are retired and their ~14 destinations redistributed. The Games panel
carries today's filter controls across unchanged.

**Verified:** 2026-08-09
**Status:** passed
**Re-verification:** Yes — this file REPLACES the stale run-3-based `34.10-VERIFICATION.md`
(`gaps_found`, 6/9, written before commit `220211230`/`1c7a3359d` and before live gate run 4). This
report is derived from live gate run 4 (`34.10-LIVE-GATE.md` §10, verdict PASS 5/5, 2026-08-09), the
`/gsd-debug` session that fixed F-34.10-03/-04 (`.planning/debug/navbar-seam-and-logo-offset.md`),
the code review (`34.10-REVIEW.md`), and independent re-checks performed directly against the
current tree by this verification pass (not copied from any SUMMARY.md claim).

## Methodology note

This phase is gated by a live, human-driven visual/behavioral test — the project's `Frontend` jest
project has no jsdom/CSS engine and cannot see a pixel. F-34.10-03/-04 each survived two prior
"fixed" declarations (34.10-18's seam border, 34.10-19's tab min-height) before a live gate
measurement contradicted the fix, with the jest suite fully green through both failures. This
verification therefore does NOT accept "a fix shipped" as equivalent to "a fix measured to work,"
and independently re-derives evidence rather than trusting `34.10-LIVE-GATE.md`'s own narration:
this pass re-read every cited source line, independently ran the contract checker script, and
independently ran the relevant jest suites rather than accepting the SUMMARY/ROADMAP counts as-is.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Sidebar and SidebarLinks are retired; ~14 destinations are redistributed into the two-tier NavShell (tier-1 tabs + tier-2 panels) | VERIFIED | `src/frontend/App.tsx:102` mounts `<NavShell />`; no `<Sidebar` reference remains in `App.tsx` (only `SidebarTour.tsx` survives, deliberately disabled per D-13, confirmed by `tourDisabled.test.ts` PASS). `NavShell/index.tsx`, `navTabs.ts`, and `components/{NavTabs,NavItem,GamesPanel,StoresPanel,SettingsPanel,DownloadsRing,HeroicVersion}/` all exist. `destinationCoverage.test.tsx` structurally enumerates former Sidebar destinations and PASSED when independently re-run by this verification (`npx jest ... destinationCoverage.test.tsx` → PASS). |
| 2 | Tier-1 card/folder tabs merge into the content surface with no visible seam, and the wordmark/tab-strip/ring share one line, in every shipped theme (REQ-34.10-06) | VERIFIED | Live gate run 4, item 1: F-34.10-03 and F-34.10-04 both PASS in `midnightMirage` (Step 1) — **first-ever measurement of either finding in a scored theme** — plus the seam column PASSes in all three scored themes (`midnightMirage`, `gruvbox_dark`, `dracula`) in Step 2. Fix independently re-read at `NavTabs/index.scss:124-142` (`&.MuiTabs-root { padding-bottom: 0; min-height: 0; }`) and root cause independently re-read at `GamesSettings/index.scss:62` (rescoped under `.settingsDialogContent, .settingsWrapper`, no longer a bare top-level selector). |
| 3 | Tier-2 vertical contextual panel is scoped to the selected tab, functionally live (search/category/filter actually drive the grid), and collapses cleanly on a game page (REQ-34.10-09) | VERIFIED | Live gate run 4, item 3: all 7 sub-checks PASS plus both F-34.10-05 theme cells PASS. `NavShell/index.tsx:50-53` (`tier2Collapsed`) and `navTabs.ts:37` (`resolveActiveTab`) independently re-read and confirmed present. |
| 4 | Downloads is not a tab — it renders as an ambient top-right progress ring with idle/active/count states (REQ-34.10-08) | VERIFIED | Live gate run 4, item 4: all sub-checks PASS including the `ProgressHeader` discriminator (advancing past 0%, ruling out the previously-shipped frozen-arc defect). `DownloadsRing.test.tsx` and `downloadsRingStyles.test.ts` independently re-run by this verification → PASS. |
| 5 | Real window dragging works via the new navbar under WKWebView, in both frameless states (REQ-34.10-04) | VERIFIED | Live gate run 4, item 2: all 5 sub-checks PASS, re-driven live. `resolveDragRegion` independently re-read at `src/preload/api/tauriWindowChrome.ts:301-334`. |
| 6 | Electron cosmetic divergence is recorded, not fixed (REQ-34.10-05) | VERIFIED | Live gate run 4, item 5: shell renders, 4 tabs navigate, PASS. Traffic-light non-overlap recorded per D-03, not re-taken. |
| 7 | The navbar stays fixed to the top during scroll, and the scrollbar starts below the navbar rather than overdrawing it | VERIFIED | Live gate run 4, item 1 Step 1: F-34.10-06(a)/(b) both PASS, re-run live rather than carried forward. |
| 8 | Mouse/keyboard scroll-position restore (back-to-top) survives the scroll-container relocation | VERIFIED | Live gate run 4, item 1 Step 1: PASS, re-run live. |
| 9 | The requirement-mandated blocking live gate itself closes the phase (REQ-34.10-16) | VERIFIED | `34.10-LIVE-GATE.md` §10.5 verdict: **PASS, 5/5**. Independently re-run: `node 34.10-run4-contract-check.mjs 34.10-LIVE-GATE.md` → `Total verdict slots found: 41 / resolved: 41 / unfilled: 0 / invalid: 0 / RESULT: COMPLETE`. REQUIREMENTS.md confirms REQ-34.10-06 and REQ-34.10-16 both marked Complete. |

**Score:** 9/9 truths independently VERIFIED.

**Named, deliberately-open residual (non-blocking, does not fail this truth or this phase):** the
gamepad focus-scroll regression sub-check (`GamesList/index.tsx:46-76`'s `scrollCardIntoView`) is
NOT ATTEMPTED for the fourth consecutive live gate run — no controller hardware has ever been
available. This is explicitly covered by the gate contract's own P9 escape hatch (non-blocking,
never inferred from the mouse/keyboard PASS since it is a different code path with different
container-rect-relative arithmetic) and is recorded as a named, permanent residual risk in
ROADMAP.md and REQUIREMENTS.md rather than left as a silent gap. Confirmed present in both files by
direct read.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/frontend/components/UI/NavShell/index.tsx` | NavShell root, mounts tier-1 + tier-2 | VERIFIED | Exists, wired into `App.tsx:102`, exercised live by all 5 gate items |
| `src/frontend/components/UI/NavShell/components/NavTabs/{index.tsx,index.scss}` | Tier-1 card/folder tab strip, seam-free | VERIFIED | Component wired; fix (`padding-bottom: 0; min-height: 0;` nested under `&.MuiTabs-root`) confirmed present at lines 124-142 by direct read AND confirmed live-measured PASS in all 3 scored themes |
| `src/frontend/screens/Settings/sections/GamesSettings/index.scss` | Root-cause rule rescoped, no longer leaking app-wide | VERIFIED | Confirmed at lines 22-81: `.MuiTabs-root { padding-bottom: var(--space-xs); ... }` now nested under `.settingsDialogContent, .settingsWrapper`, no bare top-level `.MuiTabs-root` selector remains |
| `src/frontend/__tests__/muiTabsSelectorScoping.test.ts` | Repo-wide guard against a future unscoped `.MuiTabs-root` leak | VERIFIED | File exists, independently re-run → PASS (part of the 14/14 suite run below) |
| `src/frontend/components/UI/NavShell/components/DownloadsRing/{index.tsx,index.scss}` | Ambient progress ring | VERIFIED | Live-measured PASS on all sub-checks, run 4; jest suites re-run and PASS |
| `src/frontend/components/UI/NavShell/components/{GamesPanel,StoresPanel,SettingsPanel,NavItem}/index.tsx` | Tier-2 panels per tab | VERIFIED | All exist; each has a passing jest suite, independently re-run |
| `src/frontend/components/UI/Sidebar/` | Retired | PARTIAL RETIREMENT (expected) | Directory still exists but only `components/SidebarTour.tsx` survives, deliberately disabled (not deleted) per decision D-13, pending a future onboarding-tour rework phase — not wired into `App.tsx`. Confirmed by `tourDisabled.test.ts` PASS and by `App.tsx` containing no `<Sidebar` render. This matches the phase's own explicitly stated scope (D-13), not an incomplete retirement. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `App.tsx` | `NavShell` | direct import + render at line 102 | WIRED | Confirmed by direct read |
| `NavTabs` (tier-1 click) | `navTabs.ts` `resolveActiveTab`/route table | click handlers drive `createHashRouter` navigation | WIRED | Live gate item 3(vi), item 5 (Electron) both PASS |
| Tier-2 search/category/filter controls | Games grid state | live gate item 3(i)/(iv)/(v) | WIRED | PASS — "selection reaches the real grid state, not a decorative control" |
| `DownloadsRing` | `/download-manager` route + queue state | live gate item 4 | WIRED | PASS, including the `ProgressHeader` percent-readout discriminator confirming the ring is not decoratively frozen |
| `.NavShell__navbar` | `resolveDragRegion` (Tauri drag) | `tauriWindowChrome.ts:301-334` | WIRED | Live gate item 2, all 5 sub-checks PASS |
| `.NavShell__navbar` | Electron native drag region | `-webkit-app-region: drag` | **NOT WIRED (CR-02, carried forward, deferred)** | Confirmed by direct grep: no `-webkit-app-region: drag` anywhere in `NavShell/index.scss`, though the old `.Sidebar/index.scss` had it (`git show 0559bc0d0~1`) and the `no-drag` exclusion children WERE ported (`HeroicVersion/index.scss:35`, `WindowControls/index.scss:6`). This is a real, confirmed gap in the Electron-only frameless path. It is explicitly out of this phase's scope by operator decision (Electron is deprecated, deleted entirely in Phase 35) and does not affect the Tauri path the live gate scores — recorded as carried-forward, not a phase-blocking gap. |

### Independent Re-Verification (not derived from SUMMARY.md narration)

| Check | Command | Result |
|-------|---------|--------|
| Live-gate contract checker | `node 34.10-run4-contract-check.mjs 34.10-LIVE-GATE.md` | `41/41 resolved, 0 unfilled, 0 invalid, RESULT: COMPLETE` |
| NavShell + guard test suites | `npx jest --silent --forceExit src/frontend/components/UI/NavShell src/frontend/__tests__/muiTabsSelectorScoping.test.ts src/frontend/__tests__/appShellLayout.test.ts` | `14 suites / 159 tests, all PASS` |
| CR-01 real (undefined token) | `grep -n "\-\-navbar-active:" src/frontend/themes.scss` vs `--navbar-active-background:` | Confirmed: `--navbar-active` declared in only 3 theme blocks (lines 92, 281, 340); `gruvbox_dark` (block starting line 192) has `--navbar-active-background` but NOT `--navbar-active` — `NavTabs/index.scss`'s `.Mui-selected { color: var(--navbar-active); }` has no fallback, confirmed at the cited line. Real defect, confirmed independently, correctly recorded as deferred/carried-forward rather than fixed. |
| CR-02 real (missing Electron drag region) | `grep -n "app-region" src/frontend/components/UI/NavShell/index.scss` (empty) vs `git show 0559bc0d0~1:.../Sidebar/index.scss` (`-webkit-app-region: drag` present) | Confirmed real; correctly recorded as deferred. |
| Sidebar retirement | `grep -n "Sidebar" src/frontend/App.tsx` | No render of `<Sidebar` remains |
| REQUIREMENTS.md traceability | `grep -n "REQ-34.10-" REQUIREMENTS.md` | REQ-34.10-01 through -16, all marked `Complete` |

### Requirements Coverage

| Requirement | Source Plan(s) | Status | Evidence |
|-------------|-----------------|--------|----------|
| REQ-34.10-01 | 34.10-01, -02, -07, -08 | SATISFIED | NavShell mounted at App.tsx's single former Sidebar site; grid areas confirmed |
| REQ-34.10-02 | 34.10-04 | SATISFIED | Covered by wiring above |
| REQ-34.10-03 | 34.10-02 | SATISFIED | Covered |
| REQ-34.10-04 | 34.10-04, -08, -11, -22 | SATISFIED | Live gate item 2, 5/5 PASS |
| REQ-34.10-05 | 34.10-08, -11, -16, -22 | SATISFIED | Live gate item 5 PASS, D-03 recorded |
| REQ-34.10-06 | 34.10-02, -04, -11, -17, -18, -19, -23, -25, -26, -27 | SATISFIED | Live gate run 4 item 1, 5/5 sub-checks + 3-theme sweep, now Complete in REQUIREMENTS.md |
| REQ-34.10-07 | 34.10-03, -06 | SATISFIED | Covered |
| REQ-34.10-08 | 34.10-03, -12, -14, -15, -16, -22 | SATISFIED | Live gate item 4 PASS |
| REQ-34.10-09 | 34.10-01, -06, -08, -11, -13, -16, -17, -20, -22 | SATISFIED | Live gate item 3 PASS, all 7 sub-checks |
| REQ-34.10-10 | 34.10-02 | SATISFIED | Covered |
| REQ-34.10-11 | 34.10-05, -07, -09 | SATISFIED | Covered |
| REQ-34.10-12 | 34.10-07 | SATISFIED | Covered |
| REQ-34.10-13 | 34.10-09 | SATISFIED | Covered |
| REQ-34.10-14 | 34.10-04, -05, -10 | SATISFIED | Covered |
| REQ-34.10-15 | 34.10-09, -10 | SATISFIED | Covered |
| REQ-34.10-16 | 34.10-11, -16, -21, -22, -24, -25, -26, -27 | SATISFIED | Live gate run 4 verdict PASS 5/5, now Complete in REQUIREMENTS.md |

No orphaned requirements found: all 16 IDs (REQ-34.10-01 through REQ-34.10-16) appear in at least
one plan's `requirements:` frontmatter and all are marked Complete in REQUIREMENTS.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `NavTabs/index.scss` | ~229 | `color: var(--navbar-active)` with no fallback (CR-01) | ⚠️ Warning (carried forward, deferred) | Confirmed real: `gruvbox_dark` (a scored gate theme) lacks `--navbar-active`, only `--navbar-active-background`. Not observed as visibly broken during run 4's `gruvbox_dark` sweep (which judged seam/idle-ring, not label legibility). Operator-deferred rather than patched post-gate, to avoid HEAD diverging from the bundle that passed. |
| `NavShell/index.scss` | n/a (absence) | Missing `-webkit-app-region: drag` on `.NavShell__navbar` (CR-02) | ⚠️ Warning (carried forward, deferred) | Confirmed real: old `.Sidebar` had it, new navbar doesn't; breaks Electron-frameless dragging only. Electron is fully deleted in Phase 35; explicitly deferred by operator decision, not a Tauri-path defect. |
| `NavShell/index.tsx` / `SidebarTour.tsx` | n/a | `SidebarTour` disabled, not deleted | ℹ️ Info | Deliberate per D-13; tour rework is explicitly a separate future phase, confirmed by `tourDisabled.test.ts` |
| `muiTabsSelectorScoping.test.ts` guard | n/a | Guard is bypassable by wrapping an unscoped `.MuiTabs-root` in `@media`/`@supports` (WR-01) | ⚠️ Warning (carried forward, low severity) | Verified empirically per ROADMAP.md; dormant today, does not affect current correctness |

No `TBD`/`FIXME`/`XXX` unresolved debt markers found in the files touched by this phase's scope
(NavShell tree, GamesSettings/index.scss, guard tests) during this verification's direct reads.

### Human Verification Required

None. The phase's own blocking live gate (REQ-34.10-16) already performed and recorded the
human-driven visual/behavioral verification this phase requires (5/5 PASS, run 4,
`34.10-LIVE-GATE.md` §10.5), and this verification independently confirmed the gate's checker script
result, the underlying source fixes, and the relevant jest suites rather than re-requesting the same
human judgment calls. The one remaining unmeasured item (gamepad focus-scroll regression) cannot be
human-verified without controller hardware that has not been available across four gate runs, and is
correctly carried forward as a named permanent residual risk rather than blocking this phase.

### Gaps Summary

No blocking gaps found. All 9 derived observable truths for the phase goal are VERIFIED against the
current codebase, independently re-checked rather than taken from SUMMARY.md or ROADMAP.md
narration alone:

- Sidebar retirement and destination redistribution: confirmed by direct file/grep read and a
  passing `destinationCoverage.test.tsx`.
- The two findings (F-34.10-03 seam, F-34.10-04 one-line navbar) that had failed every prior gate
  run are now closed by a live measurement taken in all three scored themes (run 4), not merely a
  shipped fix in an unscored theme — this is the first time this phase's own repeatedly-proven
  "shipped ≠ measured" caution has been satisfied for these two findings.
- The live-gate contract's own checker script was independently re-run and confirms 41/41 verdict
  slots resolved with 0 invalid, matching the claimed PASS 5/5.
- Two Critical code-review findings (CR-01, CR-02) were independently re-confirmed as real defects
  in the codebase, but both are explicitly and knowingly deferred by operator decision (recorded in
  `34.10-REVIEW.md` and `ROADMAP.md`), on the sound reasoning that patching CSS after the gate
  already passed would decouple HEAD from the measured bundle. Neither affects the Tauri path this
  phase's own live gate scores. These are reported here as carried-forward findings, not as phase
  failures, per the explicit instruction accompanying this verification.
- The gamepad focus-scroll regression remains genuinely unmeasured (no controller hardware in four
  runs) but is correctly and explicitly recorded as a named permanent residual risk rather than
  silently dropped, satisfying the gate's own escape-hatch contract.
- The pill-tab restyle and onboarding-tour rework are confirmed, explicit, out-of-scope deferrals
  with binding operator decisions on record — not stale requirement wording or missed work.

---

_Verified: 2026-08-09_
_Verifier: Claude (gsd-verifier)_
