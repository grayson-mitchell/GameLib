---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
verified: 2026-08-08T00:00:00Z
status: gaps_found
score: 3/9 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Tier-2 Categories/Filters controls (the relocated LibraryFilters/CategoryFilter dropdowns) are usable and drive the games grid"
    status: failed
    reason: "F-34.10-01 (live gate item 3, measured on real hardware 2026-08-08): the search sub-check PASSED (proving the createPortal seam into LibraryContext is sound), but the two Dropdown-based controls open on mouseover instead of click and render a >250px popup anchored right:0 inside the 204px tier-2 column, pushing more than half the popup -- including every checkbox -- off the left edge of the window and making it unusable."
    artifacts:
      - path: "src/frontend/components/UI/Dropdown/index.scss"
        issue: ".dropdown { position: absolute; top: 100%; right: 0px; min-width: 250px; } anchors a 250px-min popup to the right edge of a 204px-wide ancestor (--tier2-width, NavShell/index.scss), producing ~46px+ of left-edge overflow that exits the viewport because the tier-2 column is flush to the window's left edge."
      - path: "src/frontend/components/UI/CategoryFilter/index.tsx"
        issue: "Line 78 passes popUpOnHover to Dropdown, opening the panel on mouseover rather than click -- unusable via keyboard/touch and disorienting in a narrow column."
      - path: "src/frontend/components/UI/LibraryFilters/index.tsx"
        issue: "Line 265 passes the same popUpOnHover prop to the same Dropdown component -- identical defect, identical root cause."
    missing:
      - "Dropdown's popup geometry must be adapted for a 204px-wide ancestor: either clamp/reposition the popup to stay within the tier-2 column's bounds, or replace the absolutely-positioned popup with an in-panel (non-detached) expand pattern for this call site."
      - "popUpOnHover must be removed (or made conditional) for CategoryFilter/LibraryFilters so the controls open on click, matching every other tier-2 interaction pattern in this shell."
      - "A live re-run of Item 3's two remaining sub-checks once the above is fixed: Games tab stays lit on a game page, and the tier-2 column collapses (no empty 204px band) rather than an unmount -- both were never reached in the failed run and are recorded NOT ATTEMPTED, not inferred."
  - truth: "The ambient Downloads ring is visible and dimmed at idle (never absent) and fills with the head download's real percent during an active download"
    status: failed
    reason: "F-34.10-02 (live gate item 4, measured on real hardware 2026-08-08): idle state is invisible (not dimmed-visible as D-07 required), hover reveals a solid filled disc instead of a ring, and the arc never fills during a real, observed download -- only the count badge and the /download-manager click-through work."
    artifacts:
      - path: "src/frontend/components/UI/NavShell/components/DownloadsRing/index.scss"
        issue: "The ring's &::after 'hole' paints var(--navbar-background) unconditionally, but the parent Link's own &:hover repaints its background to var(--navbar-active-background) -- so on hover the hole stops matching its backdrop and reads as a solid disc. At idle, the track colour (--divider at opacity: 0.5) against --navbar-background carries near-zero contrast, so the ring is technically painted but visually indistinguishable from empty."
      - path: "src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx"
        issue: "RingProgress derives percent from hasProgress(appName, runner) and writes it to --dl-progress, but --dl-progress never leaves 0turn against a live download queue -- hasProgress is not yielding a percent for the head element in this mount context, even though the same queue subscription correctly drives the count badge. Count and arc read from two different data paths."
    missing:
      - "Fix the &::after hole to track the Link's actual computed background in both rest and :hover states (e.g. inherit/currentColor-based hole, or an explicit --navbar-active-background rule under &:hover), and raise idle-state contrast so the ring reads as present-but-dimmed rather than invisible."
      - "Root-cause why hasProgress(appName, runner) yields no percent for the head element in DownloadsRing's mount context specifically (CurrentDownload's equivalent call site works) -- likely a keying/timing difference in how RingProgress is remounted vs. the retired component -- and fix so --dl-progress tracks the real percent."
      - "A live re-run of Item 4 in full (idle + active + badge + click-through + active-state-on-/download-manager) once both causes are fixed."
  - truth: "Electron renders the new shell and all four tier-1 tabs navigate correctly (REQ-34.10-05's 'navigates correctly' claim)"
    status: partial
    reason: "Live gate item 5: shell-renders and the traffic-light-overlap observation (no overlap) both PASSED, but the all-4-tabs-navigate sub-check was not reached in the operator's session and is recorded NOT ATTEMPTED per the gate's own rule -- never inferred as a pass from the shell rendering. The gate's own no-partial-pass rule (phase closes only on 5/5) makes this technically still an open item, even though it is a re-measurement, not a known code defect: no Electron-specific tab-routing change shipped in this phase that would plausibly break navigation (NavTabs is route-driven via NavLink, shell-agnostic)."
    artifacts: []
    missing:
      - "Re-run Electron (npm start), click all four tier-1 tabs, and record PASS/FAIL into 34.10-LIVE-GATE.md item 5's remaining sub-check row. This is a measurement gap, not a diagnosed defect -- treat as low-risk unless the re-run actually surfaces a failure."
deferred: []
human_verification: []
---

# Phase 34.10: Navigation shell — horizontal card tabs replace the sidebar Verification Report

**Phase Goal:** Replace the left sidebar with a two-tier navigation shell — horizontal card/folder
tabs (tier 1) merging into the content surface, plus a vertical contextual panel (tier 2) scoped to
the active tab.

**Verified:** 2026-08-08
**Status:** gaps_found
**Re-verification:** No — initial verification

## Governing evidence

`34.10-LIVE-GATE.md` is the single most authoritative artifact in this phase: it is the only check
capable of observing a pixel, a hover trigger, a CSS custom property's computed value, or a real
download queue against a running Tauri build. Its recorded, human-measured verdict is treated here
as fact, not as a claim to re-derive:

| Item | Requirement | Verdict | Finding |
|---|---|---|---|
| 1 — Theme survival (3 themes + spot-check) | REQ-34.10-06 | **PASS** | — |
| 2 — Window dragging, both frameless states, 5 sub-checks | REQ-34.10-04 | **PASS** | — |
| 3 — Tier-2 portal is live, not decorative | REQ-34.10-09 | **FAIL** | F-34.10-01 |
| 4 — Ambient Downloads ring | REQ-34.10-08 | **FAIL** | F-34.10-02 |
| 5 — Electron divergence recorded | REQ-34.10-05 | **NOT ATTEMPTED** | tab-navigation sub-check unmeasured |

`items_passed: 2` / `items_total: 5`. The gate's own no-partial-pass rule ("phase closes only on
5/5") means the phase does not close, independent of any other evidence gathered below.

This verification report does not re-derive what the live gate already measured. It confirms the
gate's structural preconditions in source, cross-references the ~120 automated tests this phase
added (all `testEnvironment: 'node'`, no jsdom — incapable of contradicting or corroborating any
visual/behavioral claim), checks requirement traceability, and scans for anti-patterns and
under-evidenced requirements the gate did not directly name.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Two-tier navigation shell (tier-1 tabs + tier-2 panel) structurally replaces the sidebar, wired into `App.tsx`, with `App.css`'s grid restructured and the F-10 regression gate's `.App`/`.App .content` selectors preserved verbatim | ✓ VERIFIED | `src/frontend/App.tsx:14-15,102` imports and mounts `<NavShell />`; `src/frontend/App.css:27-31` shows the four-area grid (`offline`/`navbar`/`tier2 content`/`tier2 controller`); `src/frontend/components/UI/Sidebar` retired from the render tree (zero importers of `SidebarTour` outside its own file, confirming D-13's "preserved on disk, unmounted"); live gate items 1-3 all observed the shell rendering as a precondition of their own checks |
| 2 | Tier-1 card/folder tabs visually merge into the content surface across every shipped theme, including the navbar-lighter-than-body inversion (`dracula`) | ✓ VERIFIED | Live gate Item 1, measured on real hardware, PASS across `midnightMirage`, `gruvbox_dark`, `dracula`, spot-checked `dracula-classic` |
| 3 | A frameless window can be dragged by empty tier-1 navbar space under WKWebView, and cannot be dragged by a tab, the Downloads ring, or window controls; native titlebar dragging still works when not frameless | ✓ VERIFIED | Live gate Item 2, measured on real hardware, PASS on all 5 sub-checks; corroborated in source — `src/preload/api/tauriWindowChrome.ts:333` now reads `start.closest('.NavShell__navbar') !== null`, matching the class rendered by the shell |
| 4 | Tier-2 Games panel controls (search, categories, filters) are wired to real `LibraryContext` state (not the no-op `initialContext` trap) AND are actually operable in the 204px column | ✗ FAILED | Live gate Item 3: search sub-check PASSED (portal seam confirmed sound), but Categories/Filters dropdowns FAIL — see F-34.10-01 |
| 5 | On a game page, the Games tab stays lit and the tier-2 column collapses (width transition, no unmount) rather than showing an empty 204px band | ? UNCERTAIN | Live gate Item 3's remaining two sub-checks were never reached (run halted at the dropdown failure) — recorded NOT ATTEMPTED, not inferred either way. Blocked behind the same F-34.10-01 gap-cycle re-run. |
| 6 | The ambient Downloads ring is visible and dimmed at rest (never absent) and fills with the head download's real percent during an active download, with a correct count badge and `/download-manager` click-through | ✗ FAILED | Live gate Item 4: idle FAIL (invisible), active-fill FAIL (arc never leaves 0turn), active-state-on-`/download-manager` FAIL; count badge and click-through PASS — see F-34.10-02 |
| 7 | All 14 sidebar destinations (Stores + Settings tier-2 panels, including the 3 reparented items — Wine Manager, Accessibility, Console Mode — and the relocated `QuitButton`) are reachable with their original visibility guards intact | ✓ VERIFIED | `src/frontend/components/UI/NavShell/components/StoresPanel/index.tsx`, `SettingsPanel/index.tsx`, `QuitButton/index.tsx` exist; `destinationCoverage.test.tsx` (structural, direct-invoke) asserts labels/guards/element-kinds for every destination and replaces the deleted `SidebarLinks` suite. This class of claim (element-tree shape, prop-driven conditional rendering) is legitimately provable by the repo's no-DOM structural idiom, unlike the two failed items above — no live contradiction was raised against it. |
| 8 | Electron still renders the new shell and all four tabs navigate; the macOS traffic-light/tab-bar cosmetic divergence is recorded, not fixed | ⚠️ PARTIAL | Live gate Item 5: shell-renders PASS, traffic-light-overlap observation PASS (no overlap recorded), but all-4-tabs-navigate sub-check NOT ATTEMPTED — see gap 3 below |
| 9 | A blocking live gate on a real build closes the phase (REQ-34.10-16's own completion criterion) | ✗ FAILED | `34.10-LIVE-GATE.md` frontmatter: `verdict: fail`, `items_passed: 2` of 5. Per the gate's own stated no-partial-pass rule, the phase cannot close on this result. |

**Score:** 3/9 truths verified (2 failed with diagnosed causes, 1 partial/gap, 1 uncertain-and-blocked, 1 failed-by-definition on the gate's own closure rule; truths 1-3/7 stand on solid, independently-checked evidence)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/frontend/components/UI/NavShell/index.tsx` | Shell root: navbar + tier-2 column | ✓ VERIFIED | Exists, imported and mounted at `App.tsx:102` |
| `src/frontend/components/UI/NavShell/components/NavTabs/index.tsx` | Tier-1 route-driven tab strip | ✓ VERIFIED | Exists; live-confirmed merge behavior (Item 1) |
| `src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx` + `.scss` | Ambient Downloads indicator | ⚠️ HOLLOW | Exists, mounted, structurally tested, but live gate shows it renders no visible ring — see F-34.10-02 |
| `src/frontend/screens/Library/index.tsx` (portal render) | `createPortal(<Header />, target)` into tier-2 | ✓ VERIFIED (seam only) | Live-confirmed the seam is correct (search filters the grid); the two `Dropdown`-based children it hosts are broken — see F-34.10-01. The portal artifact itself is not implicated. |
| `src/frontend/components/UI/Dropdown/index.scss`, `CategoryFilter/index.tsx`, `LibraryFilters/index.tsx` | Usable filter controls inside the 204px column | ✗ DEFECTIVE | Confirmed in source: `right: 0; min-width: 250px` popup geometry + `popUpOnHover` triggers, unmodified from the pre-portal, full-width-header context they were designed for |
| `src/frontend/App.css` | Four-area grid | ✓ VERIFIED | `grid-template-areas: 'offline offline' 'navbar navbar' 'tier2 content' 'tier2 controller'` present |
| `src/preload/api/tauriWindowChrome.ts` | Drag region keyed on `.NavShell__navbar` | ✓ VERIFIED | Line 333 confirmed; live-confirmed behavior (Item 2) |
| `.planning/phases/.../34.10-LIVE-GATE.md` | Gate contract + recorded run | ✓ VERIFIED (as a document) | Exists, well-formed frontmatter, `verdict: fail`, `items_passed: 2`. Note: the file's own §2 "Preflight Results" block (`P1`/`P2`/`P3`/`P6`) was left with all checkboxes unfilled (`[ ]`) despite the contract requiring them recorded before Item 1 — a documentation-completeness gap in the gate's own paperwork, not something that changes any item's measured verdict (informational, see Anti-Patterns). |
| `meta/i18nGateScope.json` | Regenerated scope including NavShell files | ✓ VERIFIED | 11 references to `NavShell` found in the committed snapshot |
| `src/frontend/components/UI/Sidebar/components/SidebarTour.tsx` | Preserved on disk, unmounted | ✓ VERIFIED | File exists; zero importers outside itself found repo-wide |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `App.tsx` | `NavShell` | single import + render site | ✓ WIRED | Confirmed at `App.tsx:14-15,102` |
| `tauriWindowChrome.ts` (`resolveDragRegion`) | `NavShell/index.scss`'s `.NavShell__navbar` | shared class name | ✓ WIRED, live-confirmed | Item 2 PASS |
| `Library/index.tsx` | `Tier2PortalContext` | `useContext` + `createPortal` | ✓ WIRED, live-confirmed (seam) | Item 3's search sub-check PASS |
| `CategoryFilter`/`LibraryFilters` | `Dropdown` (popup geometry) | `popUpOnHover` prop + `.dropdown` CSS | ✗ WIRED BUT BROKEN | Live-confirmed unusable — F-34.10-01 |
| `DownloadsRing` | `window.api.getDMQueueInformation`/`handleDMQueueInformation` | mount-time subscription | ✓ WIRED (count only) | Count badge live-confirmed correct; the count and ring-fill paths are NOT the same wiring — the ring's `hasProgress` path is disconnected from a real percent, per F-34.10-02 |
| `DownloadsRing`/`RingProgress` | `hasProgress(appName, runner)` | hook call, `--dl-progress` custom property | ✗ HOLLOW | Wired (the property is set) but the value it receives never reflects real progress against a live queue — see F-34.10-02, cause (b) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `DownloadsRing` (count badge) | `elements.length` | `window.api.getDMQueueInformation()` subscription | Yes — live-confirmed (`2` shown with two queued) | ✓ FLOWING |
| `DownloadsRing`/`RingProgress` (arc fill) | `--dl-progress` from `hasProgress(appName, runner).percent` | `hasProgress` hook, keyed by `head.params.appName` | No — arc never left `0turn` against a real, running download | ✗ DISCONNECTED |
| `CategoryFilter`/`LibraryFilters` (grid filtering) | `LibraryContext` setters via portalled `<Header />` | `Library`'s own provider, portal target | Yes for search (live-confirmed grid filters) — unmeasured for category/filter toggles because the controls could not be operated to test the data flow | ⚠️ PARTIALLY FLOWING / UNTESTABLE due to F-34.10-01 |

### Behavioral Spot-Checks

Not run independently. The phase's own live gate (`34.10-LIVE-GATE.md`) is the authoritative
behavioral check for this phase and has already been executed on real hardware; re-running
equivalent checks here would not add information and the workflow instructs treating that gate's
verdicts as measured fact.

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention or PLAN/SUMMARY-declared probe found for this phase. The
phase's probe-equivalent is `34.10-LIVE-GATE.md`, a `checkpoint:human-verify` gate rather than a
scripted probe — already covered above.

### Requirements Coverage

| Requirement | Source Plan | Description (abridged) | Status | Evidence |
|---|---|---|---|---|
| REQ-34.10-01 | 01, 07, 08 | Two-tier shell structurally replaces the sidebar; `.App`/`.App .content` selectors preserved | ✓ SATISFIED | `App.css` grid, `App.tsx` mount site, F-10 gate untouched by name. One sub-claim (tier-2 collapse on a game page, no empty band) is unmeasured — see Truth 5 above; does not invalidate the broader structural claim. |
| REQ-34.10-02 | 04 | Tier-1 tabs on reskinned MUI `<Tabs>`, route-driven, relational active-tab seam | ✓ SATISFIED | Structural (`NavTabs/index.tsx`) + corroborated live by Item 1's merge-seam PASS (same visual mechanism REQ-34.10-06 tests) |
| REQ-34.10-03 | 02 | Navbar inset is a conditional 0px token, not hardcoded 78px | ✓ SATISFIED | Source-gate provable (`shellTokens.test.ts`); no live/visual claim in this requirement's text |
| REQ-34.10-04 | 02, 08 | Drag region migrates off `.Sidebar` onto the navbar, 3 fixtures updated in lockstep | ✓ SATISFIED | Live gate Item 2, PASS, all 5 sub-checks, on real hardware |
| REQ-34.10-05 | 08 (D-03), 11 | Electron keeps working (not perfect); traffic-light divergence recorded, not fixed | ⚠️ PARTIAL — Pending in REQUIREMENTS.md | Shell-renders + traffic-light observation both PASS; "navigates correctly" clause's own sub-check NOT ATTEMPTED. Correctly left Pending, not Complete. |
| REQ-34.10-06 | 04 | Theme survival, 3 themes + spot-check, blocking live gate | ✓ SATISFIED | Live gate Item 1, PASS, all themes, on real hardware |
| REQ-34.10-07 | 03 | Downloads indicator becomes an ambient ring in `nav-right`; `WindowControls` untouched; frameless hack removed; `aria-label` present | ⚠️ SATISFIED (structural only) — see note | Structural/positional claims (mount location, `WindowControls` unmoved, `Header/index.css:14-18` hack removed, `aria-label` present) are source-gate provable and not contradicted live. **However**, the requirement's own framing — "becomes an ambient ring" — is undercut in practice by F-34.10-02: what actually renders is not perceivable as a ring at all. REQUIREMENTS.md leaves this Complete while its sibling REQ-34.10-08 (the ring's fill/idle *behavior*) was correctly reverted to Pending; the *visibility* half of "ambient ring" arguably belongs to -07 as much as -08 and is equally undermined by the same live finding. Flagged, not force-failed, because -07's own checklist text is about position/wiring/aria, not appearance — but this is exactly the class of requirement the task asked to surface: partly evidenced only by a structural unit test while a sibling clause depends on a visual outcome the live gate falsified. |
| REQ-34.10-08 | 03 | Ring fill from real percent, idle-dimmed-not-absent, count badge, keyboard/AT reachable | ✗ FAILED — Pending in REQUIREMENTS.md (correct) | Live gate Item 4, FAIL — F-34.10-02 |
| REQ-34.10-09 | 01, 06 | Games tier-2 panel carries filter controls across via portal, unchanged, still wired to `LibraryContext` | ✗ FAILED — Pending in REQUIREMENTS.md (correct) | Live gate Item 3, FAIL — F-34.10-01. Portal seam itself confirmed correct; "carries today's filter controls across unchanged" is exactly what failed, since the controls' own geometry breaks in the new context. |
| REQ-34.10-10 | 02 | Single `--tier2-width` token, 204px, source-gated | ✓ SATISFIED | `shellTokens.test.ts` asserts single declaration; no visual claim requiring live verification |
| REQ-34.10-11 | 05, 09 | All 14 destinations redistributed, guards intact | ✓ SATISFIED | `destinationCoverage.test.tsx` (structural), no live contradiction; this class of claim (conditional rendering, element kind, label text) is legitimately provable without jsdom |
| REQ-34.10-12 | 07 | `HeroicVersion` moved, mount-timing/cache-clear/changelog side effects preserved unconditionally | ✓ SATISFIED | Structural (mount-timing is pure JS logic, testable via direct invocation without rendering); not contradicted live |
| REQ-34.10-13 | 07, 09 | Onboarding tour disabled, both entry points removed, `SidebarTour.tsx` preserved unmounted | ✓ SATISFIED | Confirmed by direct repo scan: zero importers of `SidebarTour` outside its own file; `tourDisabled.test.ts` exists |
| REQ-34.10-14 | 10 | i18n gate scans new files; only `nav.tabs.games` is a new key; no upstream catalog edits | ✓ SATISFIED | `meta/i18nGateScope.json` contains 11 `NavShell` references (regenerated); automated, not a visual claim |
| REQ-34.10-15 | 09 | Retired `SidebarLinks` test coverage replaced, not deleted; baseline suite green | ✓ SATISFIED | `destinationCoverage.test.tsx` exists as the stated replacement; by definition a test-existence claim, no visual/runtime component |
| REQ-34.10-16 | 11 | A blocking live gate on a real build closes the phase | ✗ NOT SATISFIED — Pending in REQUIREMENTS.md (correct) | `34.10-LIVE-GATE.md`: `verdict: fail`, 2/5. This requirement's own text ties completion directly to the gate closing the phase, which it has not. |

**Orphaned requirements:** none. All 16 IDs (REQ-34.10-01..16) appear in at least one plan's
`requirements:` frontmatter and are present in `.planning/REQUIREMENTS.md`'s Phase 34.10 rows —
full 1:1 traceability, no gaps in the mapping itself.

**REQUIREMENTS.md accuracy check:** the traceability table (lines 365-380) already correctly shows
REQ-34.10-05/-08/-09/-16 as `Pending` and the remaining twelve as `Complete`, matching this
verification's own independent findings exactly. This is the plan-11 executor's own self-correction
(see `34.10-11-SUMMARY.md`), and it holds up under this separate re-check.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `34.10-LIVE-GATE.md` | 102-109 | Preflight Results block (`P1`/`P2`/`P3`/`P6`) left with all checkboxes unfilled | ℹ️ Info | The gate's own contract required these recorded before Item 1; they were not. Does not change any item's measured verdict (the five item tables themselves are fully recorded), but is a paperwork gap in the gate document itself — worth closing in the gap-cycle plan alongside the two code fixes, since a future reader can't confirm the build-freshness/process-hygiene preconditions were actually checked. |
| `src/frontend/components/UI/Header/index.css` | 38 | `/* TODO move following styles to the components which use them */` | ℹ️ Info, out of scope | Pre-existing since 2022 (`git blame`: commit `a679ce9b39`), not introduced or touched by this phase's `6914e48fe` edit to the same file. Not attributable to phase 34.10; not a blocker. |

No `FIXME`/`XXX`/`HACK`/`PLACEHOLDER` markers, no empty stub returns (`return null`/`{}`/`[]`
outside test files), and no hardcoded-empty props were found in any of the 26 files newly created
under `src/frontend/components/UI/NavShell/`.

### Human Verification Required

None new. All items requiring human/live verification for this phase were already routed through
and executed by `34.10-LIVE-GATE.md` (a `checkpoint:human-verify` gate) — that is the authoritative
mechanism this project uses for exactly this class of check, and it has already run. The remaining
open items (F-34.10-01, F-34.10-02, Item 5's tab-navigation sub-check) require **fixes**, then a
**live re-run of the same gate**, not a new/separate human verification pass invented by this
report.

### Gaps Summary

The phase's own blocking live gate — the sole check in this phase capable of observing pixels,
hover triggers, CSS custom-property values, or a real download queue — FAILED at 2 of 5 items on
2026-08-08. Per the gate's own no-partial-pass rule, the phase does not close.

**Two diagnosed, code-level defects block closure:**

1. **F-34.10-01 (REQ-34.10-09):** the relocated `Dropdown`-based Categories/Filters controls in the
   Games tier-2 panel are unusable — they open on hover instead of click and render a 250px-min
   popup that overflows off the left edge of the 204px column. The portal seam that carries them
   into `LibraryContext` is independently confirmed sound (search works); only the two `Dropdown`
   consumers' geometry/trigger need fixing.

2. **F-34.10-02 (REQ-34.10-08):** the ambient `DownloadsRing` renders no visible ring in any state —
   invisible at idle (contrast/backdrop-token bug), a solid disc on hover (the same backdrop-token
   bug), and no progress arc during a real, observed download (`hasProgress()` yields no percent for
   the head element in this mount context, despite the same queue subscription correctly driving the
   count badge). Two independent root causes, both diagnosed to file/line in the frontmatter above.

**One unmeasured item, classified as a low-risk re-measurement gap rather than a known defect:**

3. Electron's all-four-tabs-navigate sub-check (REQ-34.10-05) was never reached in the live-gate
   session and is recorded NOT ATTEMPTED. No Electron-specific navigation logic changed in this
   phase (`NavTabs` is shell-agnostic, route-driven via `NavLink`), so this is judged low-risk, but
   the gate's own no-partial-pass rule still requires it measured before the phase can close — the
   gap-cycle plan should fold in a quick re-run (`npm start`, click all four tabs) alongside the two
   code fixes above.

**One requirement flagged for the same defect class the task asked about, beyond -05/-08/-09:**
REQ-34.10-07's "becomes an ambient ring" framing is undercut by the same F-34.10-02 finding that
falsified its sibling REQ-34.10-08, even though -07's own checklist text is mostly positional/wiring
and remains independently true. REQUIREMENTS.md currently leaves -07 Complete; this verification
does not force it to Pending (its own text doesn't assert the visual-fill behavior -08 owns), but
flags it so the gap-cycle plan is aware the two requirements' visual claims are entangled and should
both be re-confirmed by the same live re-run.

**The sketch's full Games filter panel (search, views, collections, cross-store facets, filter
chips — `sketch-findings-gamelib`'s `library-filtering.md`) was deliberately NOT delivered in this
phase.** What shipped is the pre-existing header dropdowns (`LibrarySearchBar`, `CategoryFilter`,
`LibraryFilters`) relocated into the tier-2 column via a React portal, exactly as REQ-34.10-09's own
text specifies ("carries today's filter controls across unchanged... restyling them to fit the
column is 34.11's job, deliberately not done twice") and as the ROADMAP's Phase 34.10 section states
explicitly ("this phase: relocate existing controls; 34.11 redesigns"). This is correct scoping, not
a gap — Phase 34.11 owns the actual filter-panel redesign. However, F-34.10-01 is **not** a scope
question for 34.11: it is a functional break (the relocated controls cannot be operated at all) in
controls this phase itself claims to have carried across "unchanged" and "usable app on its own." It
must be fixed in 34.10's own gap cycle. The 34.10-11-SUMMARY.md's own next-steps note suggests the
gap-cycle planner re-read `library-filtering.md` when deciding *how* to fix it (a minimal
geometry/trigger fix vs. folding into 34.11's redesign), but the decision of *whether* to fix it in
34.10 is not open — REQ-34.10-09 requires it.

---

*Verified: 2026-08-08*
*Verifier: Claude (gsd-verifier)*
