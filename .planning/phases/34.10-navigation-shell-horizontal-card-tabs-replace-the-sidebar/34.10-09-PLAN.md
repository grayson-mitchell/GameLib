---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 09
type: execute
wave: 5
depends_on: ["34.10-08"]
files_modified:
  - src/frontend/components/UI/NavShell/__tests__/destinationCoverage.test.tsx
  - src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts
  - src/frontend/components/UI/Sidebar/components/SidebarTour.tsx
  - src/frontend/components/UI/Sidebar/index.tsx
  - src/frontend/components/UI/Sidebar/index.scss
  - src/frontend/components/UI/Sidebar/components/CurrentDownload/index.tsx
  - src/frontend/components/UI/Sidebar/components/CurrentDownload/index.css
  - src/frontend/components/UI/Sidebar/components/HeroicVersion/index.tsx
  - src/frontend/components/UI/Sidebar/components/HeroicVersion/index.scss
  - src/frontend/components/UI/Sidebar/components/QuitButton/index.tsx
  - src/frontend/components/UI/Sidebar/components/SidebarItem/index.tsx
  - src/frontend/components/UI/Sidebar/components/SidebarItem/index.css
  - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx
  - src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx
autonomous: true
requirements: [REQ-34.10-11, REQ-34.10-13, REQ-34.10-15]

must_haves:
  truths:
    - "Every behavioural claim the deleted SidebarLinks test made is still asserted somewhere, against the new components"
    - "A future change that drops a destination, a guard, or the version mount fails a test rather than shipping"
    - "The onboarding tour cannot start: nothing mounts it and nothing renders its button"
    - "The retired sidebar tree is gone from the codebase apart from the tour file the deferred phase needs"
  artifacts:
    - path: "src/frontend/components/UI/NavShell/__tests__/destinationCoverage.test.tsx"
      provides: "The replacement for the deleted SidebarLinks suite: all destinations, all guards, all element kinds"
    - path: "src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts"
      provides: "Proof that no shell file references SidebarTour, TourButton or data-tour"
  key_links:
    - from: "src/frontend/components/UI/NavShell/__tests__/destinationCoverage.test.tsx"
      to: "StoresPanel / SettingsPanel / NavTabs"
      via: "direct invocation and element-graph label collection"
      pattern: "collectElements"
---

<objective>
Replace the retired suite's coverage, prove the tour is off, and delete the `Sidebar` tree.

Purpose: `SidebarLinks/__tests__/index.test.tsx` directly imports the module being deleted, so it
is a compile error, not a failing assertion — and its assertions carry real behavioural claims from
earlier phases (Steam-key gating, the Manage Accounts entry point, the absence of Discord and GitHub
Sponsors). Deleting it without a replacement silently drops that coverage.
Output: two new test suites, then the deletion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-CONTEXT.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VALIDATION.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-PATTERNS.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-08-SUMMARY.md

<interfaces>
<!-- Behavioural claims in the suite being deleted that MUST survive, and where each now lives -->
"Redeem a Steam key" hidden without a Steam session / shown with one, onClick fires
    handleRedeemKeyDialog(true)                       -> StoresPanel  (plan 05 already asserts this)
"Manage Accounts" present, never "Login", url '/login' in both auth states
                                                      -> NavTabs      (assert here)
No "Discord", no "GitHub Sponsors", but "Ko-fi" present
                                                      -> SettingsPanel (assert here)
Label ordering/adjacency of the flat list                -> obsolete: the flat list no longer exists

<!-- Files to delete (the whole tree EXCEPT SidebarTour.tsx) -->
src/frontend/components/UI/Sidebar/index.tsx, index.scss
src/frontend/components/UI/Sidebar/components/CurrentDownload/{index.tsx,index.css}
src/frontend/components/UI/Sidebar/components/HeroicVersion/{index.tsx,index.scss}
src/frontend/components/UI/Sidebar/components/QuitButton/index.tsx
src/frontend/components/UI/Sidebar/components/SidebarItem/{index.tsx,index.css}
src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx
src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Destination-coverage suite — the replacement for the deleted SidebarLinks tests</name>
  <files>src/frontend/components/UI/NavShell/__tests__/destinationCoverage.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx (all 248 lines — every claim in it must be re-homed or consciously declared obsolete)
    - src/frontend/components/UI/NavShell/components/StoresPanel/index.tsx (plan 05)
    - src/frontend/components/UI/NavShell/components/SettingsPanel/index.tsx (plan 05)
    - src/frontend/components/UI/NavShell/components/NavTabs/index.tsx (plan 04)
    - src/frontend/components/UI/NavShell/index.tsx (plan 07 — the version-slot mount this suite also guards)
  </read_first>
  <behavior>
    - Across the four tabs plus the two tier-2 panels, the union of destinations exactly equals the settled tree: no destination the sidebar carried is missing and none was invented
    - Manage Accounts is a tab whose target is /login in both logged-out and logged-in context states, and no label anywhere is "Login"
    - Labels never include "Discord" or "GitHub Sponsors"; "Ko-fi" is present in the Settings panel
    - HeroicVersion is present in NavShell's tree for every active-tab value
    - Nothing in the shell renders an element whose label is "Zoom Store"
  </behavior>
  <action>
    Create `src/frontend/components/UI/NavShell/__tests__/destinationCoverage.test.tsx` as the
    single suite that answers "did every destination survive". Use the structural direct-invoke
    idiom throughout — mock `react`'s `useContext`, `react-router-dom`, `react-i18next` (returning
    each key's default value as its translation) and the CSS-importing child modules, then call
    `NavTabs()`, `StoresPanel()`, `SettingsPanel()` and `NavShell()` as plain functions and walk the
    returned element graphs with the `collectElements` helper copied from the suite being deleted.

    Assert an **exact expected-destination set**, not a subset: build the union of tab labels plus
    both panels' labels for a permissive context (every store logged in, non-Windows) and compare it
    with a literal expected array. A subset assertion would let a silently dropped row pass; the
    whole point of this suite is that the redistribution was lossless.

    Re-home the deleted suite's three surviving claims explicitly, each with a test name that says
    which claim it carries: the Manage Accounts entry point (target `/login` in both auth states,
    label never "Login"); the community links (no "Discord", no "GitHub Sponsors", "Ko-fi" present);
    and the version mount (`HeroicVersion` present in `NavShell`'s tree for all five active-tab
    values, including null). The Redeem-a-Steam-key gating claim is already asserted by plan 05's
    `StoresPanel.test.tsx` — reference that in a comment rather than duplicating it.

    Declare the obsolete claims in a comment at the top of the file: `labels[0] === 'GameLib'` and
    "Manage Accounts immediately before Accessibility" described the flat single-column list this
    phase removes; they have no successor and are deliberately not re-homed. Writing that down stops
    a later reader treating the missing assertions as an oversight.
  </action>
  <acceptance_criteria>
    - The suite compares the collected destination union against a literal expected array with `toEqual`, not `toContain`
    - A named test asserts the Manage Accounts tab target is `/login` for both a logged-out and a logged-in context, and that no collected label equals 'Login'
    - A named test asserts labels contain neither 'Discord' nor 'GitHub Sponsors' and do contain 'Ko-fi'
    - A named test iterates all five active-tab values and asserts HeroicVersion is in `NavShell()`'s returned tree each time
    - A comment at the top of the file names the two obsolete assertions and why they have no successor
    - Removing any single row from `SettingsPanel` makes this suite fail (demonstrate with one row, then revert)
    - No occurrence of `render(`, `screen.` or `userEvent` in the file
  </acceptance_criteria>
  <verify>
    <automated>npx jest --config src/frontend/jest.config.js src/frontend/components/UI/NavShell/__tests__/destinationCoverage.test.tsx</automated>
  </verify>
  <done>An exact-set destination gate exists, the three surviving claims from the deleted suite are re-homed with named tests, and the two obsolete claims are recorded as deliberately dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Tour-disabled gate</name>
  <files>src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts, src/frontend/components/UI/Sidebar/components/SidebarTour.tsx</files>
  <read_first>
    - src/frontend/components/UI/Sidebar/components/SidebarTour.tsx (the whole file — the twelve `data-tour="sidebar-*"` step selectors that are the deferred phase's work-list)
    - src/frontend/components/UI/NavShell/index.tsx (plan 07 — confirm nothing references the tour)
    - src/frontend/screens/Login/__tests__/index.test.tsx (lines 34-90 — the source-gate idiom this suite uses)
  </read_first>
  <action>
    Add a header comment to `SidebarTour.tsx` recording its state: disabled by Phase 34.10 (D-13),
    intentionally left on disk because this file plus its `data-tour="sidebar-*"` selector set is the
    deferred tour-rework phase's work-list, and not mounted by anything. Note in the same comment
    the pre-existing duplicate `data-tour="sidebar-downloads"` value that used to be set on two
    different elements, so the rework phase does not rediscover it. Change nothing else in the file —
    its step text and i18n keys are the rework phase's input.

    Create `src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts` as a source gate in
    the `Login/__tests__/index.test.tsx` style: read every `.tsx` file under
    `src/frontend/components/UI/NavShell/` with `readFileSync` + `stripSourceComments` and assert
    that none of them contains `SidebarTour`, `SIDEBAR_TOUR_ID`, `TourButton` or `data-tour`. Also
    assert that `SidebarTour.tsx` has no importer anywhere under `src/frontend` outside itself —
    a repo-wide grep-equivalent over source text — which is what actually proves the tour cannot
    start. A mount-absence assertion on a single component would not: the tour has two entry points,
    and it was the second one (the help button inside `HeroicVersion`) that would have shipped a
    broken tour.
  </action>
  <acceptance_criteria>
    - `grep -rn "SidebarTour\|SIDEBAR_TOUR_ID" src/frontend --include=*.tsx --include=*.ts` matches only `SidebarTour.tsx` itself
    - `grep -rc "data-tour" src/frontend/components/UI/NavShell` totals 0
    - `SidebarTour.tsx`'s header comment names D-13, the deferred phase, and the duplicate `sidebar-downloads` value
    - `SidebarTour.tsx`'s exported component body and step array are unchanged apart from the added comment
    - The gate fails if a `data-tour` attribute is added to any NavShell file (demonstrate, then revert)
  </acceptance_criteria>
  <verify>
    <automated>npx jest --config src/frontend/jest.config.js src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts</automated>
  </verify>
  <done>The tour has no reachable entry point, proven by a source gate over both of them, and its file is preserved with its status recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the retired Sidebar tree</name>
  <files>src/frontend/components/UI/Sidebar/index.tsx, src/frontend/components/UI/Sidebar/index.scss, src/frontend/components/UI/Sidebar/components/CurrentDownload/index.tsx, src/frontend/components/UI/Sidebar/components/CurrentDownload/index.css, src/frontend/components/UI/Sidebar/components/HeroicVersion/index.tsx, src/frontend/components/UI/Sidebar/components/HeroicVersion/index.scss, src/frontend/components/UI/Sidebar/components/QuitButton/index.tsx, src/frontend/components/UI/Sidebar/components/SidebarItem/index.tsx, src/frontend/components/UI/Sidebar/components/SidebarItem/index.css, src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx, src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/Sidebar/index.tsx (confirm every behaviour it held has a successor: the DM queue subscription is in DownloadsRing, handleGoToScreen is in NavShell, the drag-resize is deliberately dropped)
    - src/frontend/App.tsx (confirm plan 08 already removed the import and render site)
    - src/frontend/components/UI/index.tsx (the UI barrel — check whether it re-exports anything from the Sidebar tree before deleting)
  </read_first>
  <action>
    Delete the eleven files listed in `<files>` — the entire `Sidebar` tree **except**
    `components/SidebarTour.tsx`, which task 2 preserved. Delete the now-empty
    `SidebarLinks/__tests__/` and `SidebarLinks/`, `SidebarItem/`, `QuitButton/`,
    `HeroicVersion/` and `CurrentDownload/` directories along with their files, leaving
    `Sidebar/components/SidebarTour.tsx` in place.

    Before deleting, verify the successor of each capability actually landed, since after this task
    there is nothing to compare against: the DM queue subscription lives in `DownloadsRing` (plan 03),
    `handleGoToScreen` in `NavShell` (plan 07), `QuitButton` and the two panels under `NavShell`
    (plan 05), `HeroicVersion` under `NavShell` (plan 07), and the row primitive as `NavItem`
    (plan 02). The `localStorage['sidebar-width']` drag-resize has no successor **by design** — tier 2
    is a fixed-width column.

    If `src/frontend/components/UI/index.tsx` re-exports anything from this tree, remove that export
    line too. Do not add a re-export for the new shell to the barrel: `App.tsx` imports it directly.

    This deletion also removes the last `.Sidebar` markup from the app, which is why plan 08's
    drag-region change had to land first.
  </action>
  <acceptance_criteria>
    - `ls src/frontend/components/UI/Sidebar` lists only the `components` directory, and `ls src/frontend/components/UI/Sidebar/components` lists only `SidebarTour.tsx`
    - `grep -rn "components/UI/Sidebar" src/ --include=*.tsx --include=*.ts` matches nothing except `SidebarTour.tsx`'s own path
    - `grep -rn "SidebarLinks\|SidebarItem\|CurrentDownload" src/frontend` matches nothing
    - `npx tsc --noEmit` (or the repo's typecheck script) reports no unresolved-import errors
    - `pnpm test:ci` runs to completion with no suite failing to compile
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:ci</automated>
  </verify>
  <done>The Sidebar tree is gone except the preserved tour file, nothing imports it, and the full suite compiles and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan adds tests and deletes files; it introduces no new data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-22 | Repudiation | coverage silently lost with the deleted suite | mitigate | Task 1 re-homes each surviving claim under a test name that states which claim it carries, and records the two obsolete claims in a comment, so the coverage delta is auditable rather than implicit. |
| T-34.10-23 | Elevation of privilege | a guarded destination surviving deletion only in the old tree | mitigate | Task 1's exact-set assertion runs before task 3 deletes anything, so a destination present only in the retired tree fails the suite rather than disappearing. |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists to audit. |
</threat_model>

<verification>
- `pnpm test:ci` — full suite green; suite/test counts equal the 204/4069 baseline minus the deleted `SidebarLinks` suite plus this phase's new suites, with zero failures
- `grep -rn "\.Sidebar" src/frontend src/preload` returns nothing outside `SidebarTour.tsx`
</verification>

<success_criteria>
- The destination union is asserted as an exact set, and dropping any row fails the suite
- The tour has no reachable entry point, proven over both of them
- The Sidebar tree is deleted except the preserved tour file
- The full suite compiles and passes
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-09-SUMMARY.md` when done
</output>
