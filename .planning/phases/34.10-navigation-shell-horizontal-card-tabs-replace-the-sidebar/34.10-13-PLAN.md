---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/components/UI/Dropdown/index.tsx
  - src/frontend/components/UI/Dropdown/index.scss
  - src/frontend/components/UI/CategoryFilter/index.tsx
  - src/frontend/components/UI/LibraryFilters/index.tsx
  - src/frontend/components/UI/NavShell/index.scss
  - src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx
autonomous: true
gap_closure: true
requirements: [REQ-34.10-09]

must_haves:
  truths:
    - "The Categories and Filters controls open on click, not on mouseover"
    - "Clicking an open control's own trigger closes it again"
    - "The expanded panel renders inside the 204px tier-2 column's flow rather than as a detached popup anchored to the column's right edge"
    - "A panel taller than the tier-2 column is reachable by scrolling, not clipped away by the column's overflow:hidden"
    - "The gamepad helper's `.dropdownContainer` / `.dropdown` / `.dropdownButton` selector contract still resolves, and `closeDropdown` now actually closes"
  artifacts:
    - path: "src/frontend/components/UI/Dropdown/index.tsx"
      provides: "A click-toggled disclosure with aria-expanded and containment-checked blur close"
      contains: "aria-expanded"
    - path: "src/frontend/components/UI/Dropdown/index.scss"
      provides: "In-flow panel geometry that fits a 204px column"
      contains: "dropdownContainer"
    - path: "src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx"
      provides: "Behavioural (not literal-grep) proof of the click toggle and the absence of hover handlers"
      contains: "onMouseEnter"
  key_links:
    - from: "src/frontend/components/UI/Dropdown/index.tsx"
      to: "src/frontend/helpers/gamepad.ts"
      via: "the .dropdownContainer / .dropdown / .dropdownButton class-name contract that closeDropdown() clicks through"
      pattern: "dropdownButton"
    - from: "src/frontend/components/UI/NavShell/index.scss"
      to: "src/frontend/components/UI/Dropdown/index.scss"
      via: ".NavShell__tier2Portal becomes the scroll container the now-in-flow panel needs"
      pattern: "NavShell__tier2Portal"
---

<objective>
Close F-34.10-01: make the two relocated `Dropdown`-based filter controls usable inside the 204px
tier-2 column by turning the detached, right-anchored, 250px-min-width popup into an in-flow
disclosure that opens on click.

Purpose: live gate item 3 FAILED. The portal seam is confirmed sound (search filters the grid), so
this is not a portal problem — `Dropdown`'s trigger and popup geometry were never adapted to a narrow
column, and more than half the panel, including every checkbox, renders off the left edge of the
window.
Output: click-toggled, in-column filter controls that Phase 34.11 can restyle in place.
</objective>

<scope_fence>
This plan fixes a functional break. It does NOT implement `sketch-findings-gamelib`'s
`references/library-filtering.md` panel redesign (search, views, collections, cross-store facets,
removable chips, facet counts) — that is Phase 34.11's work, and REQ-34.10-09's own text says this
phase "carries today's filter controls across unchanged ... restyling them to fit the column is
34.11's job, deliberately not done twice".

Read `library-filtering.md` §1 for direction only, so the fix does not contradict 34.11. It will not:
variant C's own structure puts facets in a *collapsed, expandable, in-panel* section, which is
exactly the shape this fix produces. Do not add facet counts, chips, view switchers, collections, or
persistence. Do not change which controls exist or their order.
</scope_fence>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-CONTEXT.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-06-SUMMARY.md

<interfaces>
<!-- Consumer census, done at planning time. `Dropdown` has exactly TWO consumers repo-wide and BOTH
     now live inside the 204px tier-2 column, so a global geometry change has no other call site to
     break. Re-confirm this with the grep in Task 1's acceptance criteria before changing anything. -->

  src/frontend/components/UI/CategoryFilter/index.tsx:6,73   imports and renders <Dropdown>
  src/frontend/components/UI/LibraryFilters/index.tsx:8,260  imports and renders <Dropdown>
  -> both are rendered ONLY by src/frontend/components/UI/Header/index.tsx:13,14
  -> <Header> has exactly one render site: Library's createPortal into .NavShell__tier2Portal
  (There is no third consumer. `frontend/helpers/gamepad.ts` is not a consumer — it is a
   class-name-contract dependant, see below.)

<!-- The class-name contract gamepad.ts depends on. These three literals MUST survive verbatim. -->
  src/frontend/helpers/gamepad.ts:390  insideDropdown()  -> el.closest('.dropdown')
  src/frontend/helpers/gamepad.ts:397  closeDropdown()   -> el.closest('.dropdownContainer'),
                                        then dropdown.querySelector('.dropdownButton').click()
  Note: closeDropdown() is ALREADY broken today, because the button's onClick does
  `setIsExpanded(true)` unconditionally and never toggles. The toggle in Task 1 repairs it.

<!-- Current Dropdown/index.tsx shape (55 lines) -->
  Props: { title?, children, className?, buttonClass?, popUpOnHover? }
  handlePopup('enter'|'leave') early-returns unless popUpOnHover
  button:  onMouseEnter / onMouseLeave / onClick -> setIsExpanded(true)  [never false]
  panel:   onMouseEnter / onMouseLeave / onBlur -> false / onFocus -> true
           className={`dropdown ${isExpanded ? 'expanded' : 'collapsed'}`}

<!-- Geometry that must change, and the box it must fit inside -->
  Dropdown/index.scss:16-20   .dropdown { position: absolute; top: 100%; right: 0px; min-width: 250px }
  Dropdown/index.scss:8-14    .dropdown.expanded { max-height: 70vh; box-shadow: 4px 5px 5px 7px rgba(0,0,0,.2); overflow-y: auto }
  Dropdown/index.scss:94-97   body.isRTL .dropdownContainer .dropdown { right: auto; left: 0px }
  NavShell/index.scss:17      --tier2-width: 204px
  NavShell/index.scss:74-88   .NavShell__tier2 { width: var(--tier2-width); overflow: hidden; transition: width .22s cubic-bezier(.4,0,.2,1) ... }
                              ^ overflow:hidden is LOAD-BEARING for the collapse transition — do not change it
  NavShell/index.scss:102-104 .NavShell__tier2Portal { height: 100% }
  Header/index.css:1-9        .Header { display:flex; flex-direction:column; padding: var(--space-lg) var(--space-sm) }

<!-- React version, confirmed: ^18.3.1 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Turn Dropdown into a click-toggled disclosure and delete the hover trigger</name>
  <files>src/frontend/components/UI/Dropdown/index.tsx, src/frontend/components/UI/CategoryFilter/index.tsx, src/frontend/components/UI/LibraryFilters/index.tsx</files>
  <read_first>
    - src/frontend/components/UI/Dropdown/index.tsx (all 55 lines — the whole component is being reworked)
    - src/frontend/helpers/gamepad.ts lines 385-410 (`insideDropdown` / `closeDropdown` — the class-name contract and the click-to-close path the toggle repairs)
    - src/frontend/components/UI/CategoryFilter/index.tsx lines 68-80 (the `<Dropdown>` call site, including the `data-tour` prop that is not in `Props` and is legal only because TS exempts hyphenated JSX attributes — leave it alone)
    - src/frontend/components/UI/LibraryFilters/index.tsx lines 255-270 (the second call site)
  </read_first>
  <action>
    **Dropdown/index.tsx.** Delete `popUpOnHover` from the `Props` type, delete the `handlePopup`
    function, and delete all four `onMouseEnter`/`onMouseLeave` handlers (two on the button, two on
    the panel). Removing the prop rather than merely not passing it is deliberate: it makes the hover
    trigger unreachable by construction rather than by convention.

    Replace the button's `onClick` with a real toggle. Current behaviour is
    `setIsExpanded(true)` unconditionally, which is why `gamepad.ts:397`'s `closeDropdown()` — which
    closes by clicking `.dropdownButton` — has never worked. Target behaviour:

      const toggle = () => {
        if (!isExpanded) window.api.gamepadAction({ action: 'tab' })
        setIsExpanded((prev) => !prev)
      }

    Keep the `gamepadAction({ action: 'tab' })` call on the expand transition only, exactly as today.
    Do not put the side effect inside the state updater.

    Add `aria-expanded={isExpanded}` to the button. Do not add `aria-controls`/`id` — that needs a
    generated id and is not required to close this gap.

    Move the blur-close from the panel to the container, with a containment check. The panel keeps
    `onFocus={() => setIsExpanded(true)}` (removing it would let Tab move focus into a collapsed,
    invisible panel, since the collapsed state is `max-height: 0; overflow: hidden`, not
    `display: none`). The panel LOSES its `onBlur`. The container gains:

      onBlur={(e) => {
        if (!e.currentTarget.contains(e.relatedTarget as Node | null)) setIsExpanded(false)
      }}

    This is load-bearing, not tidying: with a naive panel-level `onBlur`, clicking the trigger while
    the panel holds focus fires `focusout` -> `setIsExpanded(false)` and the subsequent toggle then
    flips `false -> true`, so the control would visibly refuse to close. The containment check makes
    the trigger click a same-container focus move, which does not close, so the toggle is the only
    thing that acts.

    Preserve verbatim, byte for byte, the three class-name literals `dropdownContainer`, `dropdown`
    and `dropdownButton`, and the `expanded`/`collapsed` modifier strings. `gamepad.ts` string-anchors
    all of them and a rename fails silently.

    **CategoryFilter/index.tsx and LibraryFilters/index.tsx.** Delete the `popUpOnHover` line from
    each `<Dropdown>` call site (`CategoryFilter` line 78, `LibraryFilters` line 265). Change nothing
    else in either file — not the `data-tour` props, not the children, not the ordering.

    Stage with explicit per-file `git add` paths only. Never `git add -A`, `git add .` or
    `git commit -a`: another session is editing this repository, and
    `src-tauri/src/main.rs`, `src/backend/humble/*`, `src/frontend/screens/WebView/*`,
    `.vscode/settings.json`, `scratchpad/`, `steam_appid.txt` and `.planning/phases/34.7-*` are
    off-limits.
  </action>
  <acceptance_criteria>
    - `Dropdown`'s `Props` type no longer declares `popUpOnHover`, and neither call site passes it
    - The consumer census still holds: `grep -rn "from '../Dropdown'\|from 'frontend/components/UI/Dropdown'" src --include="*.tsx"` returns exactly the two known consumers. If a third has appeared, stop and report before proceeding — a global geometry change is only safe because there is no third call site.
    - The three class-name literals `dropdownContainer`, `dropdown` and `dropdownButton` all still appear in `Dropdown/index.tsx`
    - `pnpm codecheck` exits 0
    - `pnpm lint` reports no new errors in the three modified files
    - Behavioural proof is Task 3's job — do not add a literal-grep assertion for `popUpOnHover` here; see Task 3's note on why
  </acceptance_criteria>
  <verify>
    <automated>pnpm codecheck && node -e "const fs=require('fs');const s=fs.readFileSync('src/frontend/components/UI/Dropdown/index.tsx','utf8');for(const c of ['dropdownContainer','dropdownButton','aria-expanded'])if(!s.includes(c))throw new Error('missing '+c);if(/onMouseEnter|onMouseLeave/.test(s))throw new Error('hover handlers still present');const cf=fs.readFileSync('src/frontend/components/UI/CategoryFilter/index.tsx','utf8');const lf=fs.readFileSync('src/frontend/components/UI/LibraryFilters/index.tsx','utf8');if(/popUpOnHover/.test(cf)||/popUpOnHover/.test(lf))throw new Error('a consumer still passes popUpOnHover');console.log('dropdown trigger reworked')"</automated>
  </verify>
  <done>`Dropdown` opens and closes on click, exposes `aria-expanded`, has no mouse-enter/leave handlers anywhere, closes only when focus leaves the whole container, and both consumers have dropped the prop.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-lay the panel in the tier-2 column's flow and give the column a scroll container</name>
  <files>src/frontend/components/UI/Dropdown/index.scss, src/frontend/components/UI/NavShell/index.scss</files>
  <read_first>
    - src/frontend/components/UI/Dropdown/index.scss (all 97 lines — the geometry block at 16-20, the expanded block at 8-14, and the RTL block at 94-97 all change)
    - src/frontend/components/UI/NavShell/index.scss lines 74-105 (`.NavShell__tier2`'s load-bearing `overflow: hidden` + width transition, and `.NavShell__tier2Portal`)
    - src/frontend/components/UI/Header/index.css lines 1-25 (the vertical stack the panel now sits inside, and its `var(--space-sm)` inline padding, which is what leaves the panel less than 204px of usable width)
    - src/frontend/components/UI/NavShell/__tests__/shellTokens.test.ts (the source-text gate idiom this repo uses for stylesheets, and its comment-stripping rationale)
  </read_first>
  <action>
    **Dropdown/index.scss — `.dropdown` block (currently lines 16-20).** Remove the four declarations
    that make it a detached, right-anchored, minimum-250px popup: the absolute positioning, the
    `top` offset, the `right` offset, and the `min-width`. Replace them with in-flow geometry:

      width: 100%;
      min-width: 0;
      box-sizing: border-box;

    Keep everything else in the block unchanged — `display: flex`, `flex-direction: column`,
    `border-radius: var(--space-xs)`, `background: var(--background)`, `opacity: 0`,
    `transition: all 200ms`, `pointer-events: none`, `max-height: 0`, `overflow: hidden`,
    `gap: 0.3rem`, `padding: var(--space-sm)`. The collapsed `max-height: 0` + `overflow: hidden`
    pair is what keeps the disclosure animating and what keeps `onFocus` able to re-expand it, so it
    must survive.

    **Dropdown/index.scss — `.dropdown.expanded` block (currently lines 8-14).** Replace
    `box-shadow: 4px 5px 5px 7px rgba(0, 0, 0, 0.2)` with `box-shadow: inset 0 0 0 1px var(--divider)`.
    A 7px-spread drop shadow is popup styling; inside a 204px column it bleeds over the neighbouring
    controls. An inset hairline gives the expanded panel an edge with zero layout shift. Change
    `max-height: 70vh` to `max-height: 50vh` so an expanded panel cannot consume the whole column.
    Keep `pointer-events: all`, `opacity: 1` and `overflow-y: auto`.

    **Dropdown/index.scss — RTL block (currently lines 94-97).** Delete it. It only overrides `right`
    and `left`, both of which no longer exist on the element. Leaving a dead directional override in
    a stylesheet is how the next reader concludes the panel is still absolutely positioned.

    **NavShell/index.scss — `.NavShell__tier2Portal` (currently lines 102-104).** Add:

      overflow-y: auto;
      overflow-x: hidden;

    keeping `height: 100%`. This is required, not cosmetic: once the panels expand in flow, the Games
    stack (search + two disclosures) can exceed the column height, and `.NavShell__tier2`'s own
    `overflow: hidden` would clip the excess into unreachability — trading one unusable-control bug
    for another. Do NOT change `.NavShell__tier2`'s `overflow: hidden`: it is what keeps content from
    spilling during the `width` collapse transition to 0.

    **Comment discipline in these two files.** The Task 3 source gate strips block comments but, per
    `stripSourceComments`'s own documented limitation, does NOT strip trailing `//` comments. Do not
    write the strings `position: absolute`, `right: 0px`, or `min-width: 250px` in any trailing `//`
    comment in `Dropdown/index.scss`. Describe the change in prose instead. Five plans in this phase
    tripped their own acceptance criteria on exactly this.

    Stage with explicit per-file `git add` paths only.
  </action>
  <acceptance_criteria>
    - `.NavShell__tier2` still declares `overflow: hidden` and its `width: var(--tier2-width)` + cubic-bezier width transition are unchanged
    - `.NavShell__tier2Portal` declares `overflow-y: auto`
    - `src/frontend/components/UI/NavShell/__tests__/shellTokens.test.ts` still passes unchanged — in particular `--tier2-width:` is still declared exactly once and `204px` still appears exactly once under `src/frontend`
    - `pnpm codecheck` exits 0
    - Whether the panel actually fits and is actually reachable by eye CANNOT be proven here: this jest project is `testEnvironment: 'node'` with no jsdom and no CSS engine, so nothing in this repo can compute a box, resolve a custom property, or observe an overflow. That proof is routed to plan 34.10-16's gate item 3.
  </acceptance_criteria>
  <verify>
    <automated>pnpm codecheck && pnpm jest --runInBand --silent --forceExit src/frontend/components/UI/NavShell/__tests__/shellTokens.test.ts</automated>
  </verify>
  <done>The panel is an in-flow, full-width, hairline-edged disclosure capped at 50vh, the tier-2 portal scrolls, the tier-2 column's collapse transition is untouched, and the dead RTL override is gone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove the disclosure behaviour by direct invocation, and gate the geometry in source</name>
  <files>src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx (the hand-rolled useState/useEffect harness + direct-invocation idiom to copy; note `__beginRender` / `__resetMount` and the "imported after the mocks, textual order, this project's ts-jest does not hoist jest.mock" note)
    - src/frontend/components/UI/NavShell/__tests__/shellTokens.test.ts (the `stripSourceComments` + `readFileSync` source-gate idiom for stylesheets)
    - src/backend/testUtils/stripSourceComments.ts (both exports, and the documented limitation that trailing `//` comments survive `stripSourceComments` and need `stripTrailingLineComment` layered on)
    - src/frontend/components/UI/Dropdown/index.tsx (the reworked component under test)
  </read_first>
  <action>
    Create `src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx`. Before
    writing it, run `ls src/frontend/components/UI/Dropdown/` and confirm no file differing from this
    one only by letter case already exists — on this macOS case-insensitive filesystem such a file is
    dropped from the tsc/eslint program silently, with no diagnostic. This already bit plan 34.10-04.

    Mock `../index.scss` to `{}` (no CSS transform is configured for this jest project) and mock
    `window.api.gamepadAction`. Invoke `Dropdown({ title: 'Filters', children: null })` directly
    against the harness — there is no jsdom and no renderer, so nothing is mounted.

    Two describe blocks.

    **`describe('Dropdown disclosure behaviour')` — behavioural assertions, deliberately NOT literal
    greps.** The point is to assert the thing rather than a string: five plans in this phase tripped
    literal-based criteria on incidental prose. Assert:
    - the returned root element's `className` starts with `dropdownContainer`, and the root carries
      an `onBlur` function prop
    - the button child's props contain no `onMouseEnter` and no `onMouseLeave` key
      (`expect(button.props.onMouseEnter).toBeUndefined()`), and the panel child's props contain
      neither either
    - the panel's `className` is `dropdown collapsed` on first invocation
    - invoking `button.props.onClick()` and re-invoking the component yields a panel `className` of
      `dropdown expanded` and `aria-expanded === true` on the button
    - invoking `onClick()` a second time and re-invoking yields `dropdown collapsed` again and
      `aria-expanded === false` — this is the assertion that pins the toggle, and therefore the
      assertion that keeps `gamepad.ts`'s `closeDropdown()` working
    - expanding calls `window.api.gamepadAction` with `{ action: 'tab' }`; collapsing does not call
      it again
    - the root's `onBlur`, invoked with `{ currentTarget: { contains: () => true }, relatedTarget: {} }`,
      leaves the panel expanded; invoked with `contains: () => false`, it collapses. This is the
      click-the-trigger-while-the-panel-has-focus case that would otherwise refuse to close.
    - the button's `className` contains `dropdownButton` — the literal `gamepad.ts:397` queries for

    **`describe('Dropdown panel geometry (source gate)')` — source-text assertions on
    `Dropdown/index.scss`.** Read it with `readFileSync`, pass it through `stripSourceComments`, then
    apply `stripTrailingLineComment` per line before matching, so neither a block comment nor a
    trailing `//` comment can satisfy or break these. Assert on the stripped text that:
    - `/position:\s*absolute/` does not match
    - `/min-width:\s*250px/` does not match
    - `/right:\s*0px/` does not match
    - `/width:\s*100%/` matches
    - `/box-sizing:\s*border-box/` matches
    - `body.isRTL` does not appear
    State in the file's docstring that this block proves the declarations are gone from source and
    proves nothing whatsoever about the rendered result.

    Run the full suite once (`pnpm jest --runInBand --silent --forceExit`) and confirm the count is
    216 suites / 4214 tests plus this file's additions, with **exactly one** pre-existing failure:
    `meta/__tests__/hardcodedStringGate.test.ts` on `src/frontend/screens/WebView/index.tsx:347`.
    That failure belongs to a concurrent session's uncommitted tree. Do not fix it, do not allowlist
    it, do not attribute it to phase 34.10. More than one failure is a real regression.

    Then run `pnpm gen-i18n-gate-scope` and confirm `git diff --quiet -- meta/i18nGateScope.json`
    exits 0. It should: `meta/genI18nGateScope.ts:125` skips any path containing `__tests__/`, so
    this new file is legitimately out of the blocking i18n gate's scope rather than silently escaping
    it. If the file DOES change, run `git checkout -- meta/i18nGateScope.json` and report — something
    other than a test file entered the tree.

    Stage with explicit per-file `git add` paths only.
  </action>
  <acceptance_criteria>
    - `pnpm jest --runInBand --silent --forceExit src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx` passes
    - The behavioural block asserts a full open -> close -> open cycle driven by `onClick`, not merely that an `onClick` exists
    - The source-gate block runs its input through both `stripSourceComments` and `stripTrailingLineComment`
    - `pnpm jest --runInBand --silent --forceExit` reports exactly one failing suite, and it is `meta/__tests__/hardcodedStringGate.test.ts`
    - `pnpm gen-i18n-gate-scope` leaves `meta/i18nGateScope.json` byte-identical (`git diff --quiet -- meta/i18nGateScope.json` exits 0)
    - `ls src/frontend/components/UI/Dropdown/` shows no case-variant duplicate of the new test file
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest --runInBand --silent --forceExit src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx && pnpm gen-i18n-gate-scope >/dev/null && git diff --quiet -- meta/i18nGateScope.json && echo "dropdown disclosure proven, i18n scope unchanged"</automated>
  </verify>
  <done>A direct-invocation test pins the click toggle, the absence of hover handlers, the aria-expanded state and the containment-checked blur close; a comment-immune source gate pins the geometry; the suite is at its known one-failure baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared UI component → all call sites | `Dropdown` is shared; a geometry change made for one context reaches every consumer |
| stylesheet source text → gate assertion | A comment containing a forbidden literal can vacuously satisfy or break a source gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-32 | Denial of service | a third `Dropdown` consumer outside the 204px column | mitigate | Task 1's acceptance criteria re-run the consumer census and require a stop-and-report if a third call site has appeared since planning |
| T-34.10-33 | Denial of service | tier-2 content becoming unreachable behind `.NavShell__tier2`'s `overflow: hidden` | mitigate | `.NavShell__tier2Portal` gains `overflow-y: auto`; the expanded panel is capped at `50vh` |
| T-34.10-34 | Tampering | silent breakage of `gamepad.ts`'s string-anchored selectors | mitigate | The three class-name literals are pinned by behavioural assertions in Task 3, and the toggle assertion is what makes `closeDropdown()` functional for the first time |
| T-34.10-35 | Repudiation | a vacuous source gate satisfied by a comment | mitigate | Task 3 layers `stripTrailingLineComment` over `stripSourceComments`, and Task 2 forbids the forbidden literals in trailing comments |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists in this plan |
</threat_model>

<verification>
- `pnpm codecheck` exits 0
- `pnpm jest --runInBand --silent --forceExit` shows exactly the one known pre-existing failure
- `shellTokens.test.ts` still passes: `--tier2-width:` declared once, `204px` present once
- `meta/i18nGateScope.json` unchanged after regeneration
- Usability in the running 204px column is NOT proven by anything above — it is routed to plan 34.10-16, gate item 3
</verification>

<success_criteria>
- Both filter controls open on click and close on a second click
- The expanded panel is laid out inside the tier-2 column rather than anchored to its right edge at a 250px minimum
- Nothing in `sketch-findings-gamelib`'s 34.11 filter-panel redesign was implemented
- `.NavShell__tier2`'s collapse transition and `overflow: hidden` are untouched
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-13-SUMMARY.md` when done
</output>
