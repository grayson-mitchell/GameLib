---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 20
type: execute
wave: 2
depends_on: ["34.10-17"]
files_modified:
  - src/frontend/components/UI/Dropdown/index.scss
  - src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx
autonomous: true
gap_closure: true
requirements: [REQ-34.10-09]

must_haves:
  truths:
    - "The expanded Categories/Filters disclosure no longer paints a surface that reads as a black block against the tier-2 column, in any shipped theme"
    - "The fix implements the reading (W1 wrong token / W2 broken inheritance) that 34.10-17's live capture settled, not the first one that fits"
    - "Plan 34.10-13's in-flow panel geometry survives intact — the F-34.10-01 fix is not partially undone while fixing its successor"
    - "The panel's surface is expressed relationally and cannot go wrong again in a theme where the navbar is lighter than the body"
  artifacts:
    - path: "src/frontend/components/UI/Dropdown/index.scss"
      provides: "A disclosure panel surface that agrees with the tier-2 column it now lives in"
      contains: "dropdown"
    - path: "src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx"
      provides: "Comment-immune source gates on the new surface declaration and on 34.10-13's surviving geometry"
  key_links:
    - from: "src/frontend/components/UI/Dropdown/index.scss (.dropdown surface)"
      to: "src/frontend/components/UI/NavShell/index.scss (.NavShell__tier2 background)"
      via: "the surface the panel is composited against, named relationally rather than as --background"
      pattern: "background"
---

<objective>
Close **F-34.10-05** — the tier-2 Categories/Filters disclosure panel rendering a background that
breaks its styling — by implementing whichever of the two readings plan 34.10-17's live capture
settled.

Purpose: the verifier could not conclusively diagnose this from source. `.dropdown` declares
`background: var(--background)`, which IS a legitimate per-theme token, so the operator's
"does not track the active theme" is an inference, not a measurement. Two very different defects
produce the same complaint: a token that resolves correctly but names the wrong surface for the
panel's new home, or a custom property that stops resolving in the portalled context. They need
different fixes, so 34.10-17 measured the computed colour in two themes first.

Output: a disclosure panel whose surface agrees with the 204px column it was relocated into, with
plan 34.10-13's geometry untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F04-DIAGNOSIS.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-13-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-CONTEXT.md

Required skills — invoke both before writing any CSS:
- `Skill("sketch-findings-gamelib")` — `references/navigation-shell.md` § 3 (why an absolute
  light/dark assumption breaks) and § 4 (what tier 2 is). `references/library-filtering.md` is
  **Phase 34.11's scope** — read it only to confirm you are not doing its work.
- `Skill("spike-findings-gamelib")` — Tauri/WKWebView gotchas.

<interfaces>
<!-- Already extracted; do not re-explore. -->

`src/frontend/components/UI/Dropdown/index.scss` — the surface declaration under suspicion and the
geometry plan 34.10-13 landed (all of which must survive):

  .dropdownContainer { position: relative; .button { padding-inline: 2rem; } }
  .dropdownContainer .dropdown.expanded { pointer-events: all; opacity: 1; max-height: 50vh;
                                          box-shadow: inset 0 0 0 1px var(--divider);
                                          overflow-y: auto; }
  .dropdownContainer .dropdown { width: 100%; min-width: 0; box-sizing: border-box;
                                 display: flex; border-radius: var(--space-xs);
                                 flex-direction: column;
                                 background: var(--background);      <-- the suspect
                                 opacity: 0; transition: all 200ms; pointer-events: none;
                                 max-height: 0; overflow: hidden; gap: 0.3rem;
                                 padding: var(--space-sm); }

`src/frontend/components/UI/NavShell/index.scss` — the surface the panel is now composited against:
  `.NavShell__tier2 { background: var(--navbar-background); ... }`
  `.NavShell__tier2Portal { height: 100%; overflow-y: auto; overflow-x: hidden; }`

Per-theme token values (`src/frontend/themes.scss`, already read):

| theme | --background | --navbar-background | --navbar-active-background |
|---|---|---|---|
| midnightMirage | `var(--neutral-02)` #161c1e | same as --background | `var(--background-darker)` |
| gruvbox_dark | #1d2021 | #32302f | #282828 |
| dracula | #090b1c | #44475a | #282a36 |

  -> In `midnightMirage` the panel and the column are the SAME colour, so the defect is invisible
     there. In `dracula` the panel is #090b1c on #44475a. Never judge this in the default theme.

**Consumer census, taken at planning time:** `Dropdown` has exactly TWO call sites in the whole
repository — `src/frontend/components/UI/CategoryFilter/index.tsx` and
`src/frontend/components/UI/LibraryFilters/index.tsx` — and after plan 34.10-06's portal both are
rendered exclusively inside `.NavShell__tier2Portal`. There is therefore no third context whose
surface a change here could break. **Re-run this census before editing** (`grep -rln "UI/Dropdown"
src/frontend --include="*.tsx"`); if a third consumer has appeared since, stop and report rather
than scoping the change by guesswork.

`src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx` already exists
(plan 34.10-13) and already contains a comment-immune stylesheet source gate using the
`stripSourceComments` + `stripTrailingLineComment` layering. Extend it; do not create a second file.

`meta/i18nGateScope.json` already registers `Dropdown/index.tsx` and `CategoryFilter/index.tsx`
(plan 34.10-13's deviation). This plan touches only `.scss` and a test, so no regeneration is
expected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give the disclosure panel a surface that agrees with the column it lives in</name>
  <files>src/frontend/components/UI/Dropdown/index.scss</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F04-DIAGNOSIS.md § "F-34.10-05 verdict" — this is the specification; it records which of W1 and W2 survived, in which themes, from a measured computed colour
    - src/frontend/components/UI/Dropdown/index.scss (the whole file — the geometry from 34.10-13 must survive)
    - src/frontend/components/UI/NavShell/index.scss (`.NavShell__tier2`'s own background, the surface being composited against)
    - src/frontend/components/UI/NavShell/components/DownloadsRing/index.scss (its header comment — the precedent for "remove the class of bug rather than chase the symptom", which is why 34.10-14 replaced a guessed backdrop patch with a real transparent hole)
  </read_first>
  <action>
    **Gate before any edit.** Open `34.10-F04-DIAGNOSIS.md` § "F-34.10-05 verdict". If it records
    neither W1 nor W2 as surviving — or records `NOT ATTEMPTED` because the capture was never taken
    — STOP: write no CSS and produce a SUMMARY stating the plan is blocked on an unobtained
    measurement. Do not fall back to the most plausible reading.

    Re-run the consumer census first: `grep -rln "UI/Dropdown" src/frontend --include="*.tsx"`.
    Expect exactly `CategoryFilter/index.tsx` and `LibraryFilters/index.tsx`. If a third call site
    exists, stop and report — a global surface change would then reach a context nobody measured.

    **If W1 survived (the token resolves, but names the wrong surface):** the panel is an in-flow
    disclosure inside a column already painted `var(--navbar-background)`, and `var(--background)`
    is the app's deep body surface — correct when this control lived in the full-width header,
    wrong now. Prefer removing the class of defect over swapping one absolute token for another:
    make the panel composite directly onto the column's own surface (`background: transparent`),
    keeping `.expanded`'s existing `box-shadow: inset 0 0 0 1px var(--divider)` as the affordance
    that delimits it. That leaves nothing to mismatch in any theme or any future surface change —
    the same reasoning `DownloadsRing/index.scss`'s header comment records for the mask fix. If you
    judge a distinct raised surface is required for legibility instead, you may use a relational
    token, but you must then state in the comment which token, what it resolves to in
    `midnightMirage`, `gruvbox_dark` and `dracula`, and why each of the three is legible against
    `--navbar-background`. A hex literal, or any value that is only checked in the default theme,
    is not acceptable — `midnightMirage` is precisely the theme where this defect is invisible.

    **If W2 survived (the custom property does not resolve in the portalled context):** do NOT
    apply the W1 fix. The defect is then a custom-property inheritance break, and the fix is to
    restore resolution (or to declare the needed property on an ancestor that is inside the portal
    target's inheritance chain). Implement what the diagnosis names, and record in the SUMMARY that
    the W1 surface question is separate and remains open or moot, as applicable.

    In both cases: do not weaken or re-litigate plan 34.10-13's geometry. `width: 100%`,
    `min-width: 0`, `box-sizing: border-box`, `max-height: 50vh` on `.expanded`, the inset hairline,
    and the absence of `right: 0` / `min-width: 250px` are the F-34.10-01 fix that live gate run 2
    confirmed. Touch none of them.

    Do not restyle the controls to fit the 204px column beyond this background fix. Restyling the
    filter controls for the column, and the search/views/collections/facets panel the sketch
    describes, are **Phase 34.11's** work (CONTEXT D-09, and the Deferred Ideas list). Fixing a
    broken surface is in scope; redesigning the panel is not.

    Write a comment above the change naming F-34.10-05, citing `34.10-F04-DIAGNOSIS.md` as the
    measurement, quoting the measured computed colour and the theme it was taken in, and stating
    that this panel is composited against `.NavShell__tier2`'s `--navbar-background` — so a future
    reader who moves the control again knows what the declaration depends on.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const strip=(s)=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,'');const d=strip(fs.readFileSync('src/frontend/components/UI/Dropdown/index.scss','utf8'));if(/background:\s*var\(--background\)/.test(d))throw new Error('F-34.10-05: .dropdown still paints var(--background)');if(/#[0-9a-fA-F]{3,8}/.test(d))throw new Error('F-34.10-05: a hex colour literal was introduced into Dropdown/index.scss');for(const g of ['width:\\\\s*100%','min-width:\\\\s*0','box-sizing:\\\\s*border-box','max-height:\\\\s*50vh','inset 0 0 0 1px var\\\\(--divider\\\\)'])if(!new RegExp(g).test(d))throw new Error('34.10-13 geometry lost: '+g);if(/right:\\s*0/.test(d)||/min-width:\\s*250px/.test(d))throw new Error('the F-34.10-01 popup geometry came back');console.log('surface changed, 34.10-13 geometry intact')" && pnpm codecheck</automated>
  </verify>
  <done>`.dropdown` no longer paints `var(--background)`, the new surface is expressed relationally with a comment quoting the measured colour and theme, every 34.10-13 geometry declaration survives, and the F-34.10-01 popup geometry has not returned.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the new surface and the surviving 34.10-13 geometry together</name>
  <files>src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/Dropdown/__tests__/dropdownDisclosure.test.tsx (plan 34.10-13's existing behavioural assertions and its comment-immune stylesheet gate — extend that gate's describe block, follow its `stripSourceComments` + `stripTrailingLineComment` layering exactly)
    - src/frontend/components/UI/Dropdown/index.scss as edited by task 1
  </read_first>
  <action>
    Extend the existing stylesheet-gate describe block in `dropdownDisclosure.test.tsx` with a
    `F-34.10-05` group asserting, against comment-stripped source only:

    1. `Dropdown/index.scss` contains no `background: var(--background)` declaration. Name
       F-34.10-05 and the measured theme in the failure message.
    2. `Dropdown/index.scss` contains no hex colour literal at all. Cite
       `navigation-shell.md` § 3's navbar-lighter-than-body table in the message — an absolute
       colour here is invisible in `midnightMirage` and wrong in `dracula`.
    3. The exact surface declaration task 1 wrote is present.
    4. Plan 34.10-13's geometry survives: `width: 100%`, `min-width: 0`, `box-sizing: border-box`,
       `max-height: 50vh` on `.expanded`, and `box-shadow: inset 0 0 0 1px var(--divider)`. State
       in the message that these are the F-34.10-01 fix confirmed by live gate run 2, so a failure
       here means a closed finding is being reopened.
    5. The F-34.10-01 popup geometry has not returned: no `right: 0` and no `min-width: 250px`
       anywhere in the file.

    Every assertion runs on comment-stripped text; this task's own comments quote the asserted
    declarations, so an unstripped scan would be self-satisfying. Do not add a bare occurrence
    count against unfiltered source.

    Leave every behavioural assertion plan 34.10-13 wrote (the open→close→open cycle, the absence
    of hover handlers, `aria-expanded`, the containment-checked blur, the `dropdownButton` literal
    that `gamepad.ts:397` depends on) untouched — they pin the click-toggle contract live gate run 2
    confirmed.
  </action>
  <verify>
    <automated>pnpm jest --runInBand --silent --forceExit src/frontend/components/UI/Dropdown</automated>
  </verify>
  <done>`dropdownDisclosure.test.tsx` carries a five-gate `F-34.10-05` group alongside plan 34.10-13's untouched behavioural assertions, all passing, all comment-immune.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared UI component → all call sites | `Dropdown` is shared; a surface change made for the tier-2 column reaches every consumer |
| a closed finding's fix → its successor's fix | F-34.10-01's geometry and F-34.10-05's surface live in the same 90-line stylesheet |
| default theme → shipped themes | This defect is invisible in `midnightMirage`, where the panel and the column are the same colour |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-80 | Repudiation | a fix chosen between W1 and W2 without the measurement | mitigate | Task 1 stops hard if `34.10-F04-DIAGNOSIS.md` § "F-34.10-05 verdict" records neither reading as surviving |
| T-34.10-81 | Tampering | a third `Dropdown` consumer outside the 204px column | mitigate | Task 1 re-runs the consumer census and requires a stop-and-report if the count is not exactly two |
| T-34.10-82 | Tampering | F-34.10-01's confirmed fix partially undone while fixing F-34.10-05 | mitigate | Task 1's automated check and gates 4-5 both assert every 34.10-13 geometry declaration and forbid the return of `right: 0` / `min-width: 250px` |
| T-34.10-83 | Information disclosure | a surface only verified in `midnightMirage`, where the defect is invisible | mitigate | A relational-token choice must state its resolved value in all three scored themes; gate 2 forbids hex literals; plan 34.10-22's item 3 re-measures live |
| T-34.10-84 | Elevation of privilege | this plan drifting into Phase 34.11's filter-panel redesign | mitigate | Task 1 fences the change to the background declaration and names D-09 and the Deferred Ideas entry that own the restyle |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists in this plan |
</threat_model>

<verification>
- `pnpm codecheck` clean.
- `pnpm jest --runInBand --silent --forceExit` — record observed suite/test counts in the SUMMARY
  against the most recent baseline recorded in this phase's SUMMARYs; state both numbers.
- `git status` shows `meta/i18nGateScope.json` clean — this plan touches only a stylesheet and a
  test, and adds no user-facing string.
- **Nothing here is proven by the suite.** No jsdom, no CSS engine, no way to resolve a custom
  property or compute a colour. The adjudicator is plan 34.10-22's live gate item 3. Do not tick
  REQ-34.10-09 from anything in this plan.
</verification>

<success_criteria>
- The reading recorded in `34.10-F04-DIAGNOSIS.md` § "F-34.10-05 verdict" is the one implemented
- `.dropdown` no longer paints `var(--background)`; no hex literal exists in the file
- Every plan 34.10-13 geometry declaration survives and the F-34.10-01 popup geometry has not returned
- `dropdownDisclosure.test.tsx` passes with 34.10-13's behavioural assertions plus five new comment-immune gates
- `pnpm codecheck` clean and the suite at or above its recorded baseline
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-20-SUMMARY.md` when done
</output>
