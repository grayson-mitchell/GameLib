---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 10
type: execute
wave: 6
depends_on: ["34.10-09"]
files_modified:
  - meta/i18nGateScope.json
  - public/locales/en/translation.json
autonomous: true
requirements: [REQ-34.10-14, REQ-34.10-15]

must_haves:
  truths:
    - "The blocking hardcoded-string gate actually scans every new navigation file, not just the files that existed before this phase"
    - "The one new nav string has a real catalog key, not only an inline default"
    - "The full test suite is green with the regenerated scope in place"
    - "No upstream translation catalog is edited to make any of the above pass"
  artifacts:
    - path: "meta/i18nGateScope.json"
      provides: "Regenerated gate scope including every NavShell file"
      contains: "NavShell"
  key_links:
    - from: "meta/i18nGateScope.json"
      to: "meta/hardcodedStringGate.ts"
      via: "scanScope() reads only the paths listed in this committed snapshot"
      pattern: "NavShell"
---

<objective>
Register the new navigation files with the blocking i18n gate, sync the single new translation key,
and prove the full suite green against the regenerated scope.

Purpose: `meta/i18nGateScope.json` is a **static committed snapshot**, not a live glob. Until it is
regenerated, every file this phase created is invisible to the blocking hardcoded-string gate — a
raw string literal in the new navbar would pass `pnpm test:ci` today and surface months later at
someone else's regeneration. A green suite is not evidence the gate scanned this phase's code.
Output: regenerated scope, one catalog key, a verified full-suite run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-RESEARCH.md
@.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-I18N-CONTRACT.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-09-SUMMARY.md

<interfaces>
<!-- Commands (from package.json) -->
pnpm gen-i18n-gate-scope   — esbuild-bundles meta/genI18nGateScope.ts and runs it; rewrites meta/i18nGateScope.json
pnpm i18n                  — i18next-parser; extracts t() calls into public/locales/*/
pnpm i18n-churn-guard      — asserts a pnpm i18n run touched nothing under public/locales except gamelib.json
pnpm test:ci               — all 5 jest projects, includes the blocking meta/hardcodedStringGate suite

<!-- The one new key introduced by this phase (plan 04) -->
t('nav.tabs.games', 'Games')   — default namespace -> public/locales/<locale>/translation.json

<!-- Known state carried in from 34.8 (do not try to fix here) -->
`pnpm i18n` currently churns the three upstream en/ catalogs on PRE-EXISTING unrelated source
drift that predates this phase. The churn guard is built and correct; the tree is not clean enough
for it to pass. Recorded in 34.8-I18N-CONTRACT.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync the one new translation key without touching unrelated catalog drift</name>
  <files>public/locales/en/translation.json</files>
  <read_first>
    - .planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/34.8-I18N-CONTRACT.md (the churn contract, the `keepRemoved: true` requirement, and the pre-existing-drift caveat)
    - i18next-parser.config.js (the lexer `functions` arrays and `keepRemoved` — confirm `keepRemoved` is still true before running anything; a run with it false deleted 36 real fork keys once)
    - src/frontend/components/UI/NavShell/components/NavTabs/index.tsx (the single `nav.tabs.games` call site)
    - public/locales/en/translation.json (confirm no `nav.tabs.games` key exists yet)
  </read_first>
  <action>
    Confirm `i18next-parser.config.js` still has `keepRemoved: true` before doing anything — a run
    with it false previously deleted 36 real fork keys that the static lexer cannot see through
    dynamic key construction and aliased `t`. If it is false, stop and report; do not run the
    extractor.

    Record `git status --porcelain public/locales` before and after everything in this task, so the
    diff is attributable.

    Add the `nav.tabs.games` key with the English value "Games" to
    `public/locales/en/translation.json`. Prefer running `pnpm i18n` and keeping only that one
    added key; if the run also churns the three upstream `en/` catalogs on the pre-existing drift
    described in `<interfaces>`, revert everything except the single `nav.tabs.games` addition with
    a targeted checkout of the other catalogs, and record in the summary that the churn was
    attributable to that pre-existing drift rather than to this phase. **Never hand-edit an upstream
    catalog to make the churn guard pass**, and never "fix" the unrelated drift here — it is
    explicitly out of scope per the 34.8 contract.

    Do not machine-fill the other 48 locales: every locale falls back to the inline English default
    `t('nav.tabs.games', 'Games')` until translated, which is the designed behaviour, and bulk
    filling is a separate, deliberately opt-in operation.

    Leave the working tree clean apart from the intended additions.
  </action>
  <acceptance_criteria>
    - `grep -c "nav.tabs.games" public/locales/en/translation.json` returns 1
    - `git diff --name-only public/locales` lists at most `public/locales/en/translation.json`
    - `git diff public/locales/en/translation.json` shows exactly one added key
    - `i18next-parser.config.js` still has `keepRemoved: true` (unchanged by this task)
    - The summary records the before/after `git status --porcelain public/locales` output and, if a churn occurred, names the pre-existing drift as its cause
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:ci</automated>
  </verify>
  <done>The single new key exists in the English catalog, no upstream catalog was edited, and the extractor config is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the i18n gate scope and prove the gate actually scanned the new files</name>
  <files>meta/i18nGateScope.json</files>
  <read_first>
    - meta/genI18nGateScope.ts (the whole generator — how the merge-base diff produces the file list, and why it must run locally rather than in CI)
    - meta/i18nGateScope.json (the current 136-entry snapshot — note it lists every retired Sidebar file individually)
    - meta/hardcodedStringGate.ts (how `scanScope()` consumes the snapshot and why it refuses an empty scope)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-RESEARCH.md (§ i18n Migration and Traps and Corrections §3 — the full description of this trap)
  </read_first>
  <action>
    Run `pnpm gen-i18n-gate-scope` and commit the regenerated `meta/i18nGateScope.json`.

    Then verify the regeneration actually did its job rather than assuming it did — this is the
    whole point of the task. Confirm that every `.tsx`/`.ts` file created by this phase under
    `src/frontend/components/UI/NavShell/` appears in the snapshot's `files` array, and that the
    deleted `Sidebar` tree files no longer appear. Confirm the snapshot's recorded `baseCommit`
    still matches `package.json`'s `upstream.baseCommit`; if it does not, stop and report rather
    than committing a snapshot generated against the wrong base.

    Then run `pnpm test:ci` in full and confirm the `meta` project's gate reports a `scannedFiles`
    count consistent with the regenerated snapshot and `violations: 0`. **A green run against the
    old snapshot proves nothing about this phase's code** — that is exactly the failure mode this
    task exists to close, so record the `scannedFiles` number in the summary rather than only
    "green".

    If the gate reports a violation in a new nav file, fix the source (wrap the literal in `t()`
    with an existing key where one exists) — do **not** widen the allowlist. Adding an allowlist
    entry to silence a fresh violation is explicitly forbidden by the 34.8 contract.
  </action>
  <acceptance_criteria>
    - `grep -c "NavShell" meta/i18nGateScope.json` is at least the number of `.tsx`/`.ts` source files this phase created under `NavShell/` (tests excluded if the generator excludes them — state which in the summary)
    - `grep -c "Sidebar/components/SidebarLinks/index.tsx" meta/i18nGateScope.json` returns 0
    - `grep -c "Sidebar/components/SidebarTour.tsx" meta/i18nGateScope.json` returns 1 (the preserved file is still fork-owned and still in scope)
    - The snapshot's `baseCommit` equals `package.json`'s `upstream.baseCommit`
    - `pnpm test:ci` is green and the gate's reported `scannedFiles` count is recorded in the summary
    - `meta/i18nGateAllowlist.json` is unchanged — `git diff --exit-code meta/i18nGateAllowlist.json` succeeds
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:ci</automated>
  </verify>
  <done>The gate scope includes every new nav file and excludes the deleted ones, the full suite is green, and the scanned-file count is recorded rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling → committed artifact | A locally-run generator writes a snapshot that CI then trusts as the gate's entire scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-24 | Repudiation | a green `pnpm test:ci` implying coverage it does not have | mitigate | The scope is regenerated first, its contents are asserted file-by-file, and the gate's `scannedFiles` count is recorded in the summary — so "green" is backed by a number rather than by assumption. |
| T-34.10-25 | Tampering | silencing a fresh violation via the allowlist | mitigate | `meta/i18nGateAllowlist.json` must be byte-unchanged, asserted with `git diff --exit-code`; a new violation must be fixed in source. |
| T-34.10-26 | Tampering | `pnpm i18n` deleting fork keys | mitigate | `keepRemoved: true` is verified before the extractor is run at all, and the run is aborted if it is false — this exact configuration previously deleted 36 real fork keys. |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists to audit. |
</threat_model>

<verification>
- `pnpm test:ci` — full suite green, gate `violations: 0`, `scannedFiles` consistent with the regenerated snapshot
- `git diff --exit-code meta/i18nGateAllowlist.json` — unchanged
- `git status --porcelain public/locales` — at most the one intended English catalog change
</verification>

<success_criteria>
- Every new NavShell source file is listed in `meta/i18nGateScope.json`
- The deleted Sidebar files are gone from the snapshot, `SidebarTour.tsx` remains
- `nav.tabs.games` exists in the English catalog; no upstream catalog was hand-edited
- The full suite is green and the gate's scanned-file count is recorded
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-10-SUMMARY.md` when done
</output>
