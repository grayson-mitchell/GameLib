---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 21
subsystem: docs
tags: [live-gate, contract-authoring, verification, navigation-shell, decision-D-E]

# Dependency graph
requires:
  - phase: 34.10-18
    provides: "Navbar seam border (F-34.10-03 fix) + scroll-container relocation (F-34.10-06 fix), both source-only, unverified live"
  - phase: 34.10-19
    provides: "NavTabs min-height fix (F-34.10-04 fix), source-only, unverified live"
  - phase: 34.10-20
    provides: "Dropdown panel surface fix (F-34.10-05 fix), source-only, unverified live"
provides:
  - "34.10-LIVE-GATE.md §9: the run-3 contract -- scope with per-item justification (9.1), preflight P1-P9 with an empty results block (9.2), structural reachability review (9.3), pairwise requirement-interaction review with a derived run order (9.4), and item bodies for all 5 items with every verdict slot at the literal placeholder 'PENDING — run 3' (9.5)"
affects: ["34.10-22 (the only invocation permitted to run this contract and fill its verdict slots)"]

tech-stack:
  added: []
  patterns:
    - "Grounding a preflight's expected baseline in a live authoring-time measurement rather than reconciling two conflicting historical SUMMARY counts by inference"
    - "Explicit reopen conditions on a reduced-scope item (item 4), so a scope-reduction decision cannot silently paper over a regression surfaced by the item it was folded into (item 1)"

key-files:
  created: []
  modified:
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md

key-decisions:
  - "Item 2 (window dragging) gets a FULL 5-sub-check re-run for run 3, not carried forward, even though source inspection alone (resolveDragRegion and .NavShell__navbar's markup untouched by 34.10-18/-19/-20) would justify skipping it. Chose the conservative re-run specifically because 34.10-18's own Task 1 committed a defect to this SAME element (a seam-border token reasoned as theme-safe by identical source-level logic) that a 100%-passing unit-gate suite did not catch, and only a coordinator's live re-verification found broken in 9 of 11 themes. Re-testing costs the operator well under a minute; an unfounded carry-forward on the one element already proven wrong once this cycle was judged not worth the risk."
  - "Item 4's idle sub-check is folded into item 1's new per-theme idle-ring sweep rather than independently re-scored, to avoid double-counting the identical DownloadsRing observation under two item numbers -- with an explicit, written reopen condition: if item 1's per-theme sweep surfaces ANY FAIL, item 4 must be reopened in full regardless of what its own carried-forward sub-checks (ii-vi) show."
  - "P1's expected jest baseline (219 suites / 4269 tests) is grounded in a live suite run performed at this plan's own authoring time, not copied from either of two conflicting historical counts: 34.10-19-SUMMARY.md states 219/4269 after its own fix, while 34.10-20-SUMMARY.md's own reconciliation math computes 4264 without accounting for 34.10-19's claimed +10 tests. Rather than resolving this by inference, the discrepancy is flagged explicitly in §9.2 for the run-3 operator to note if it resurfaces."
  - "A new DESTROYS pair, absent from run 1 and run 2's own pairwise reviews, was found during authoring: item 3's category/filter toggling shortens the Games grid, which can retroactively invalidate item 1's new P8 precondition (a scrollable grid) for its F-34.10-06(b) and back-to-top regression sub-checks. Resolved by run order (item 1 before item 3), which happens to already match run 1/run 2's own order derived for an unrelated reason -- stated explicitly as a coincidence, not silently assumed."
  - "Item 1's own per-theme theme-switch loop is sequenced to run strictly AFTER its four theme-independent finding sub-checks and two scroll-regression sub-checks, all taken once in the default theme -- a new intra-item DESTROYS pair this run's longer item 1 introduces (interleaving a theme switch mid-observation of a scroll/seam state), which run 2's shorter item 1 never had to consider."

requirements-completed: []

duration: ~35min
completed: 2026-08-08
---

# Phase 34.10 Plan 21: Author run-3 live-gate contract (§9, empty verdict slots) Summary

**Appended `## 9. Run 3` to `34.10-LIVE-GATE.md`: a from-the-current-tree re-derived scope, a 9-row preflight, a source-cited structural reachability review, a 12-pair pairwise requirement-interaction review that finds one new DESTROYS pair this run's longer item 1 introduces, and item bodies for all 5 items carrying 42 empty `PENDING — run 3` verdict slots — zero verdicts filled, per decision D-E.**

## Performance

- **Tasks:** 3/3 completed (combined into one atomic commit — see Task Commits below)
- **Files modified:** 1
- **Duration:** ~35 min

## Accomplishments

- Read run 1's (§0-§5) and run 2's (§6-§7) full recorded history, `34.10-VERIFICATION.md`'s five open gaps, and all three fix-plan SUMMARYs (34.10-18/-19/-20) to re-derive run 3's scope from the CURRENT tree rather than copying run 2's — which predates all four fixes (34.10-17's diagnosis plus -18/-19/-20's implementations).
- **§9.1** scopes item 1 as a FULL re-run larger than run 2's ever reached: the 4-theme seam+idle-ring sweep (never scored in either prior run), 4 theme-independent finding sub-checks (one per F-34.10-03/04/06a/06b, each cited to its exact fix line), and 2 new scroll-relocation regression sub-checks (mouse/keyboard back-to-top, gamepad focus-scroll) with an explicit NOT ATTEMPTED escape hatch when no controller is available. Items 2, 4 and 5 each carry a written, evidence-based scope decision rather than a default "closed, skip."
- **§9.2** preflight adds two new rows (P8: a scrollable Games grid; P9: controller presence) load-bearing for item 1's new sub-checks, and grounds P1's expected jest baseline in a live suite run performed during this plan's own authoring (219 suites / 4269 tests, 0 failing) rather than trusting either of two conflicting historical SUMMARY counts.
- **§9.3** gives every run-3 sub-check a REACHABLE/CONDITIONAL verdict with file:line evidence re-confirmed against the CURRENT source tree (not inferred from a plan number), including two explicit Test 4 (absence-observability) cases for the new F-34.10-06(b) scrollbar-position and gamepad focus-scroll sub-checks.
- **§9.4** runs the pairwise review with the reduction stated explicitly (8 state-mutating actions × relevant evidence-bearing items/sub-checks, 12 pairs considered, 5 flagged), finds one genuinely new DESTROYS pair (item 3's filtering vs. item 1's new P8 precondition) plus a new intra-item pair (item 1's own theme-switch loop vs. its own regression sub-checks), and derives the run order `[1, 3, 4, 2, 5]` from those findings — explicitly noting it coincides with, rather than silently reuses, run 1/run 2's own order.
- **§9.5** writes item bodies for all 5 items with 42 `PENDING — run 3` slots (verified automatically: none is `PASS`/`FAIL`), numbered operator steps naming exact routes/controls/thresholds (e.g. the 450px back-to-top trigger cited to `Library/index.tsx:324`), and an explicit warning against generalized "looks fine" per-theme reports — the exact failure mode that cost the per-theme sweep three runs running.
- Frontmatter updated per the plan's exact field list: `run_number: 3`, `items_passed: 0`, all five `items[].status` reset to `pending`, a new `runs:` entry (`authored_by: "34.10-21"`, `run_by: "34.10-22"`) with a note preserving run 1's/run 2's own `items_passed` history, and an `items_reset_note` field stating explicitly what was reset and what was preserved. `findings:` list left open on F-34.10-03/-04/-05/-06 (closure requires a measurement, not a landed fix).
- **Incidental correctness fix (Rule 1):** item 1's frontmatter `findings:` array was `["F-34.10-03", "F-34.10-04"]`, omitting `F-34.10-06` even though the top-level `findings:` list (and item 1's own §7 body) clearly attribute F-34.10-06 to item 1 too. Corrected to `["F-34.10-03", "F-34.10-04", "F-34.10-06"]` while editing the same block for the status reset.

## Task Commits

All three tasks (scope+preflight, reachability+pairwise review, item bodies+frontmatter) were authored together as one internally-consistent document — §9.5's item bodies and step sequencing depend directly on §9.4's derived run order, and splitting the single-file, single-purpose markdown edit across three commits would have been an artificial boundary with no independent verification value. Committed as one atomic unit:

1. **Tasks 1-3: author run-3 live-gate contract (§9)** - `419421775` (docs)

## Files Created/Modified

- `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md` - Appended `## 9. Run 3` (§9.1-§9.5); frontmatter `run_number`, `items_passed`, `items[].status`, and a new `runs:` entry updated per the plan's exact field list. §0-§7 (run 1's and run 2's recorded history) left byte-identical.

## Decisions Made

See `key-decisions` in frontmatter above for the four decisions with the most reasoning behind them (item 2's conservative full re-run, item 4's fold-with-reopen-condition, P1's live-grounded baseline, and the new item-1-before-item-3 DESTROYS pair). One additional decision not listed there: item 5 (Electron) is scoped as a full re-run judged "cheap, not necessary" — no Electron-specific code changed, but the shared `App.css`/`NavShell/index.scss` did, and re-confirming both sub-checks costs the operator under two minutes against a deprecated shell (Phase 35 deletes Electron entirely).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Item 1's frontmatter `findings:` array was missing F-34.10-06**
- **Found during:** Task 3's frontmatter edit (resetting `items[].status` to `pending`)
- **Issue:** `items: - id: 1 ... findings: ["F-34.10-03", "F-34.10-04"]` omitted `F-34.10-06`, even though the file's own top-level `findings:` list attributes F-34.10-06 to `item: 1`, and item 1's own §7 run-2 body discusses all three findings together as the item's own recorded defects.
- **Fix:** Added `"F-34.10-06"` to item 1's `findings:` array.
- **Files modified:** `34.10-LIVE-GATE.md` (same edit that reset `status:` fields)
- **Verification:** Re-read the edited block; confirmed all three IDs now present and consistent with the top-level `findings:` list.
- **Committed in:** `419421775`

---

**Total deviations:** 1 auto-fixed (1 Rule 1 — a pre-existing data-consistency gap in the file this plan was already editing, not newly introduced).
**Impact on plan:** Trivial, no scope creep — a one-array-element correction discovered while making an already-scheduled edit to the same block.

## Issues Encountered

None blocking. One editorial choice worth recording: this plan's own authoring-time jest run (219 suites / 4269 tests, 0 failing) resolved an ambiguity between 34.10-19-SUMMARY.md's stated count (219/4269) and 34.10-20-SUMMARY.md's own reconciliation math (which computes 4264 without accounting for 34.10-19's claimed +10 tests) — the live run matches 34.10-19's number exactly, suggesting 34.10-20's reconciliation text under-counted rather than the tree having regressed. This plan does not correct 34.10-20-SUMMARY.md's own text (out of this plan's scope — it is a different plan's committed artifact); it flags the discrepancy in §9.2 for the run-3 operator instead.

## Live-Gate Caveat (not a deviation — this plan's own explicit charter)

Per decision D-E, this plan **authors** a contract and **does not run it**. Nothing in `34.10-LIVE-GATE.md` §9 constitutes a measurement of anything — every verdict slot is the literal placeholder `PENDING — run 3`, verified automatically (42 occurrences, zero `| PASS |`/`| FAIL |` cells anywhere in §9). Plan `34.10-22` is the only invocation permitted to run this contract and fill its verdict slots. REQ-34.10-06/-08/-09 remain unticked from this plan.

## User Setup Required

None — no external service configuration required. This plan touches only a planning-document markdown file.

## Next Phase Readiness

- `34.10-22` can proceed immediately: the contract is complete, self-consistent, and every precondition (P1-P9) is stated with an explicit reason tying it to the item(s) that depend on it.
- The run-3 operator should be told, ahead of time, about the two open items this contract itself flags as unresolved-by-design: (a) the 34.10-19/34.10-20 jest-count discrepancy in P1 (informational, not blocking), and (b) the gamepad focus-scroll sub-check's NOT ATTEMPTED escape hatch if no controller is available at run time (does not block item 1's own PASS, but must be carried forward as unmeasured).
- No blockers.

## Self-Check: PASSED

- FOUND: `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md` (§9 confirmed present via automated grep-equivalent checks above)
- FOUND commit `419421775` in `git log --oneline`
- All three plan-level automated `<verify>` checks (Task 1, Task 2, Task 3) re-run and PASSED against the committed file state
- `git diff --name-only` against the parent commit shows exactly one path: `34.10-LIVE-GATE.md` (plan-level `<verification>` requirement)
- `meta/i18nGateScope.json` unchanged (`git status --short` shows nothing)
- `pnpm jest --runInBand --silent --forceExit` (full repo, run at this plan's authoring time, before any edits): 219 suites passed / 219 total, 4269 tests passed / 4269 total, 0 failing — unaffected by this plan's doc-only edit

---
*Phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar*
*Completed: 2026-08-08*
