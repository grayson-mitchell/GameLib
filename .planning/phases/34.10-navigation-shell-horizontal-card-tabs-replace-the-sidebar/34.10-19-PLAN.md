---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 19
type: execute
wave: 3
depends_on: ["34.10-17", "34.10-18"]
files_modified:
  - src/frontend/components/UI/NavShell/components/NavTabs/index.scss
  - src/frontend/components/UI/NavShell/index.scss
  - src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts
autonomous: true
gap_closure: true
requirements: [REQ-34.10-06]

must_haves:
  truths:
    - "The wordmark, the tier-1 tab strip and the Downloads ring read as one line — their vertical mid-points agree, and no element is pushed onto or below a second row"
    - "The fix implements the single surviving cause written in 34.10-F04-DIAGNOSIS.md, not a guess — and if that document records UNDECIDED, no CSS is written at all"
    - "The card/folder seam recipe still holds after the fix: the active tab still overlaps the navbar's bottom border by 1px and still paints its own bottom edge in the content colour"
    - "Any MUI override is scoped under `.NavTabs` and cannot reach the other `<Tabs>` instance in the app"
  artifacts:
    - path: "src/frontend/components/UI/NavShell/components/NavTabs/index.scss"
      provides: "The tab strip's own box model, sized so it shares the navbar's line rather than overflowing it"
      contains: "NavTabs"
    - path: "src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts"
      provides: "Source gates pinning the fix and forbidding the regression shapes"
  key_links:
    - from: "34.10-F04-DIAGNOSIS.md § Surviving hypotheses"
      to: "src/frontend/components/UI/NavShell/components/NavTabs/index.scss"
      via: "the exact selector and declaration the diagnosis named"
      pattern: "MuiTab-root"
---

<objective>
Close **F-34.10-04** — the wordmark and the Downloads ring reading as a second row instead of
sharing one line with the tab strip — by implementing the single surviving cause recorded in
`34.10-F04-DIAGNOSIS.md`.

Purpose: `34.10-VERIFICATION.md` established that `flex-wrap` is `nowrap` everywhere in the shell,
so nothing in static source explains this symptom and a fix written from source alone would be a
guess. Plan 34.10-17 measured the live box model precisely so this plan does not have to guess.
This project has already paid once for a fix authored against a falsified premise.

Output: a single-line navbar whose card/folder seam recipe still holds, plus source gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F04-DIAGNOSIS.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-18-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-CONTEXT.md

Required skills — invoke both before writing any CSS:
- `Skill("sketch-findings-gamelib")` — `references/navigation-shell.md` § "CSS Patterns → Card /
  folder tab" (the recipe this fix must not break) and § 3 (relational, never absolute, colours).
- `Skill("spike-findings-gamelib")` — Tauri/WKWebView gotchas.

<interfaces>
<!-- Already extracted; do not re-explore. -->

`.../NavTabs/index.tsx` — every `<Tab>` passes BOTH `icon={<FontAwesomeIcon .../>}` AND
`label={t(...)}`, with `iconPosition="start"` and `disableRipple`. The four labels come from
existing translation keys (`userselector.manageaccounts`, `nav.tabs.games`, `stores`, `Settings`).
MUI is used here ONLY as the accessibility primitive (roving tabIndex, `role="tablist"`,
`aria-selected`, `id="tab-{index}"` matching `TabPanel`'s `aria-labelledby`).

`.../NavTabs/index.scss` currently styles `.NavTabs .MuiTabs-indicator { display: none }` and
`.NavTabs .MuiTab-root { border: 1px solid transparent; border-bottom: none; background:
transparent; color: var(--navbar-inactive); padding: var(--space-xs) var(--space-md);
border-radius: var(--space-2xs) var(--space-2xs) 0 0; position: relative; top: 1px;
font-size: var(--text-md); font-weight: var(--regular); text-transform: none; ... }` plus `:hover`,
`:focus-visible` and `.Mui-selected` rules. It declares **no** `min-height` and **no** `flex-wrap`.

MUI v5 defaults that apply here (verify against the installed version, do not trust this quote):
  `Tab.js`  — when a Tab has BOTH `icon` and `label`: `minHeight: 72; paddingTop: 9; paddingBottom: 9`
  `Tabs.js` — root: `overflow: hidden; minHeight: 48; display: flex`

`.../NavShell/index.scss` declares `--navbar-height: 56px` at `:root` and
`.NavShell__navbar { height: var(--navbar-height); display: flex; align-items: flex-end; ... }`
plus (after plan 34.10-18) `border-bottom: 1px solid var(--divider)`.

**There is a SECOND MUI `<Tabs>` instance in this app**: `src/frontend/screens/WineManager/index.tsx:222`.
Any override written as a bare `.MuiTab-root { ... }` would reach it. Every rule in this plan must
stay nested under `.NavTabs`.

`src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts` was created by plan 34.10-18
with eight comment-immune source gates (seam recipe both halves, scroll ownership, F-10 percentage
prohibition, dead-sticky prohibition, Library migration). Extend it; do not create a second file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the diagnosis's surviving cause so the navbar's three children share one line</name>
  <files>src/frontend/components/UI/NavShell/components/NavTabs/index.scss, src/frontend/components/UI/NavShell/index.scss</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F04-DIAGNOSIS.md § "Surviving hypotheses" — this is the specification for this task; the exact file, selector and declaration are named there
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F04-DIAGNOSIS.md § "F-34.10-04 hypothesis table" — read the rejected rows too, so you do not re-introduce a rejected reading
    - src/frontend/components/UI/NavShell/components/NavTabs/index.scss (the file most likely to change)
    - src/frontend/components/UI/NavShell/index.scss (as edited by plan 34.10-18 — do not disturb its `border-bottom`)
    - node_modules/@mui/material/Tab/Tab.js (confirm the installed version's icon+label `minHeight` rather than trusting a quoted number)
  </read_first>
  <action>
    **Gate before any edit.** Open `34.10-F04-DIAGNOSIS.md`. If its `## Surviving hypotheses`
    section records `UNDECIDED — measurement not obtained` for F-34.10-04, or names no single cause,
    STOP: write no CSS, produce a SUMMARY stating the plan is blocked on an unobtained measurement,
    and say that plan 34.10-17's checkpoint must be re-run. Implementing a plausible cause without
    the measurement is precisely the failure this gap cycle exists to avoid, and a green suite
    cannot tell the difference.

    Otherwise implement exactly the declaration the diagnosis names, in exactly the file and
    selector it names. The following constraints bind regardless of which cause survived:

    - **Scope every MUI override under `.NavTabs`.** A bare `.MuiTab-root` or `.MuiTabs-root`
      selector would also restyle `WineManager/index.tsx:222`'s tab strip, which is not this
      phase's surface and has its own height expectations.
    - **Do not remove the icons and do not remove the labels.** Both are load-bearing: the labels
      are the four existing translation keys and the accessible names (`<Tabs>` deliberately carries
      no `aria-label` because each `<Tab>`'s `label` supplies it), and the icons are the four
      already-imported glyphs the design specifies. Dropping either to dodge a MUI default would be
      a scope reduction, not a fix.
    - **Preserve the card/folder seam recipe.** `.NavTabs .MuiTab-root` must keep
      `position: relative; top: 1px` and `border-bottom: none`, and `.Mui-selected` must keep
      `background: var(--body-background)`, `border-color: var(--divider)` and
      `border-bottom: 1px solid var(--body-background)`. Plan 34.10-18's gates assert both halves;
      if your change makes them fail, the change is wrong, not the gate.
    - **The tab must never be shorter than its own content.** If the fix reduces a height, state in
      a comment what the tab's content height actually is (icon glyph + label line-box + the
      vertical padding in `--space-xs`) and why the chosen value clears it. A clipped label is a
      new defect, not a closed one.
    - **Relational tokens only.** No hex literal, no theme-conditional value. Every scored theme
      ships a navbar LIGHTER than the body (`navigation-shell.md` § 3).
    - If the diagnosis's cause requires changing `--navbar-height`, change it at its single `:root`
      declaration in `NavShell/index.scss` and nowhere else, and check `shellTokens.test.ts` still
      passes — that file pins single-declaration-ness for the shell tokens.

    Write a comment above the change that names F-34.10-04, cites `34.10-F04-DIAGNOSIS.md` as the
    measurement it implements, and states the measured numbers (the tab box height, the navbar
    height, and the mid-point delta between the tab labels and the wordmark/ring) that make it
    necessary. A future reader must be able to tell this apart from an arbitrary style preference.

    Do not touch `App.css`, `Library/index.tsx`, `Dropdown/index.scss`, or `NavTabs/index.tsx` —
    they belong to other plans in this cycle or to no plan at all.
  </action>
  <verify>
    <automated>pnpm codecheck && pnpm jest --runInBand --silent --forceExit src/frontend/components/UI/NavShell</automated>
  </verify>
  <done>The declaration named in `34.10-F04-DIAGNOSIS.md` § Surviving hypotheses is implemented, scoped under `.NavTabs`, with icons and labels intact, the seam recipe's declarations untouched, and a comment recording the measured numbers — or, if the diagnosis is UNDECIDED, no CSS was written and the block is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the single-line navbar and the regression shapes that would undo it</name>
  <files>src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts</files>
  <read_first>
    - src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts (plan 34.10-18's eight gates and its docstring's explanation of why comment-stripping is load-bearing)
    - src/frontend/components/UI/NavShell/components/NavTabs/index.scss as edited by task 1
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F04-DIAGNOSIS.md § Surviving hypotheses (the gate must pin the specific declaration the diagnosis named, not a generic property)
  </read_first>
  <action>
    Extend `appShellLayout.test.ts` — do not create a second file, and do not rename it (this
    project has a recorded macOS case-collision trap where two paths differing only in case silently
    drop one from `tsc` and `eslint` with no diagnostic).

    Add a `describe('F-34.10-04 — the navbar is one line', ...)` block containing, at minimum:

    1. A gate asserting the exact declaration task 1 added, read from the file the diagnosis named,
       scanned after comment-stripping. Quote the finding ID in the failure message.
    2. A gate asserting every `MuiTab`/`MuiTabs` selector in `NavTabs/index.scss` is nested under
       `.NavTabs`, so no rule can leak to `WineManager`'s tab strip. Write the assertion against
       the comment-stripped text, and make the failure message name `WineManager/index.tsx:222` as
       the surface that would be collaterally restyled.
    3. A gate asserting `.NavTabs .MuiTab-root` still declares `position: relative` and `top: 1px`,
       and `.Mui-selected` still declares `background: var(--body-background)` — the seam recipe
       must survive this fix, and a height change is the most plausible way to break it silently.
    4. A gate asserting neither `NavShell/index.scss` nor `NavTabs/index.scss` declares
       `flex-wrap` anywhere. `nowrap` is the default and is what the shell relies on; an explicit
       `wrap` added later would reproduce F-34.10-04's reported symptom for real.
    5. A gate asserting no hex colour literal was introduced into `NavTabs/index.scss` by this
       change — cite `navigation-shell.md` § 3's inversion table in the message.

    Every assertion runs against comment-stripped source. Do not add any gate that counts
    occurrences in unfiltered text: this plan's own comments quote the declarations being asserted,
    so an unstripped scan would be satisfied by prose.

    This plan adds no user-facing string, so `meta/i18nGateScope.json` needs no regeneration —
    confirm with `git status` after this task and, if it is dirty, localise the literal and commit
    the regenerated scope rather than reverting it.
  </action>
  <verify>
    <automated>pnpm jest --runInBand --silent --forceExit src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts</automated>
  </verify>
  <done>`appShellLayout.test.ts` carries a five-gate `F-34.10-04` block covering the implemented declaration, MUI scoping, seam-recipe survival, the `flex-wrap` prohibition and the literal-colour prohibition — all passing, all comment-immune.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnosis document → CSS change | An UNDECIDED or hedged diagnosis silently becomes a guessed fix if the plan does not stop |
| `.NavTabs` stylesheet → the app's other MUI `<Tabs>` instance | An unscoped MUI class selector reaches `WineManager/index.tsx:222` |
| a height change → the card/folder seam recipe | The recipe's `top: 1px` overlap is exactly the kind of thing a height fix breaks without any error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-70 | Repudiation | a fix written against an unobtained measurement | mitigate | Task 1 opens with a hard stop on `UNDECIDED — measurement not obtained`, requiring a re-run of plan 34.10-17's checkpoint rather than a plausible guess |
| T-34.10-71 | Tampering | `WineManager`'s tab strip collaterally restyled | mitigate | Task 1 requires every MUI override nested under `.NavTabs`; gate 2 asserts it and names the collateral surface |
| T-34.10-72 | Tampering | the seam recipe broken by a height change | mitigate | Gate 3 pins `position: relative`, `top: 1px` and the selected tab's content-coloured background; plan 34.10-18's gates 1-3 also still run |
| T-34.10-73 | Denial of service | a tab label clipped by a too-small height | mitigate | Task 1 requires the tab's content height to be stated in a comment and cleared by the chosen value; the live gate in plan 34.10-22 re-reads all four labels in all four themes |
| T-34.10-74 | Spoofing | a scope reduction disguised as a fix (dropping icons or labels) | mitigate | Task 1 forbids removing either and states why both are load-bearing (accessible names, existing i18n keys, the design's own icon set) |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists in this plan |
</threat_model>

<verification>
- `pnpm codecheck` clean.
- `pnpm jest --runInBand --silent --forceExit` — record observed suite/test counts in the SUMMARY,
  against the baseline recorded in `34.10-18-SUMMARY.md`, not an older quote.
- `git status` shows `meta/i18nGateScope.json` clean.
- **Nothing here is proven by the suite.** No jsdom, no CSS engine — no test in this repo can see
  whether three elements share a line. The adjudicator is plan 34.10-22's live gate item 1. Do not
  tick REQ-34.10-06 from anything in this plan.
</verification>

<success_criteria>
- The declaration named in `34.10-F04-DIAGNOSIS.md` § Surviving hypotheses is implemented in the named file and selector, scoped under `.NavTabs`
- Icons, labels, `position: relative`, `top: 1px`, and the `.Mui-selected` content-coloured background all survive
- `appShellLayout.test.ts` passes with plan 34.10-18's eight gates plus this plan's five
- `pnpm codecheck` clean and the suite at or above `34.10-18-SUMMARY.md`'s recorded baseline
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-19-SUMMARY.md` when done
</output>
