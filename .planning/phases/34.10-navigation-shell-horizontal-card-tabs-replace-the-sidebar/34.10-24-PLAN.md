---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 24
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [REQ-34.10-16]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md

must_haves:
  truths:
    - "A reader arriving at ROADMAP.md or STATE.md is told the /gsd-debug session is CLOSED, not owed"
    - "The pill-tab election is recorded as a DEFERRED decision with an owner phase, not as an oversight or an open question"
    - "REQ-34.10-06's card/folder framing is stated as STANDING for run 4, so the gate is scored against the tab shape actually shipped"
    - "The next command reads /gsd-execute-phase 34.10, and the gap-cycle-3 plan list is visible"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 34.10 section corrected: debug closed, gap cycle 3 listed, pill deferral recorded"
      contains: "gap cycle 3"
    - path: ".planning/STATE.md"
      provides: "Frontmatter stopped_at and the Phase 34.10 position block corrected"
      contains: "34.10-23"
  key_links:
    - from: ".planning/ROADMAP.md Phase 34.10"
      to: ".planning/debug/navbar-seam-and-logo-offset.md"
      via: "explicit citation of the resolved debug session and its two commits"
      pattern: "220211230"
---

<objective>
Correct two planning artifacts that are provably stale, and record one operator decision that
currently exists only inside a live-gate appendix.

Three specific staleness facts, established after those files were written:

1. `/gsd-debug` HAS RUN and is CLOSED. `.planning/debug/navbar-seam-and-logo-offset.md` carries
   `status: resolved`. Its root cause (an unscoped `.MuiTabs-root { padding-bottom: var(--space-xs) }`
   in `GamesSettings/index.scss:40`) was fixed in commit `220211230` and operator-confirmed live in
   commit `1c7a3359d` ("the download and wordmark read level now"). ROADMAP.md still says
   **Next: `/gsd-debug`**; STATE.md's `stopped_at` still says the same. Both are wrong.
2. F-34.10-03 and F-34.10-04 are FIXED. What remains owed is a run-4 gate that MEASURES the fix
   against the three-theme contract — the fix was only ever confirmed in the app's ambient teal
   theme, not `midnightMirage`/`gruvbox_dark`/`dracula`.
3. The operator's run-3-time election to replace the card/folder tab with a **pill tab** is DEFERRED
   to separate follow-up work and is OUT OF SCOPE for this gap cycle. Right now it is recorded only
   in `34.10-LIVE-GATE.md` §9.6, where `34.10-VERIFICATION.md` reads it as an unresolved open design
   question that blocks authoring a run-4 contract. It does not block anything; it is a decision.

Purpose: the run-4 contract author (plan 34.10-25) must not have to re-derive any of this, and must
not be told by ROADMAP.md to run `/gsd-debug` or by VERIFICATION.md that the tab shape is undecided.
Output: two corrected planning documents.

**Do NOT reword REQ-34.10-06.** Its "the active tab merges into the content surface" framing STANDS
unchanged, and run 4 scores the card/folder tab AS IT STANDS. Do not plan, scope, or hint at a
pill-tab restyle anywhere in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/debug/navbar-seam-and-logo-offset.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct ROADMAP.md's Phase 34.10 section and record the pill-tab deferral</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 2352-2430 — the Phase 34.10 section, its `**Plans:**` paragraph, and the gap-cycle-2 checklist
    - `.planning/debug/navbar-seam-and-logo-offset.md` § Resolution — root cause, fix, verification, `STILL OWED`, and `follow_up`
    - `.planning/phases/34.10-.../34.10-VERIFICATION.md` frontmatter `gaps_remaining` — the authoritative gap list this cycle closes
  </read_first>
  <action>
    Edit the `### Phase 34.10:` section only. Leave every other phase untouched.

    (a) Replace the sentence **`Next: /gsd-debug`** (and the two lines after it that justify it, ending
        at the `34.10-22-SUMMARY.md` citation) with a current-state paragraph stating:
        - the `/gsd-debug` session ran and is CLOSED (`.planning/debug/navbar-seam-and-logo-offset.md`,
          `status: resolved`);
        - its root cause: `src/frontend/screens/Settings/sections/GamesSettings/index.scss:40`
          declared `.MuiTabs-root { padding-bottom: var(--space-xs) }` with no scoping ancestor, so
          the 8px leaked app-wide into the nav shell's `<Tabs>`; `.NavShell__navbar`'s
          `align-items: flex-end` bottom-aligns the BORDER BOX, so the padding held the tab strip
          8px above the real navbar/content boundary — one cause, both findings;
        - fixed in commit `220211230` (a nested `&.MuiTabs-root { padding-bottom: 0; min-height: 0 }`
          in `NavTabs/index.scss`), operator-confirmed live in commit `1c7a3359d`;
        - five earlier attempts targeted the wrong property and are enumerated in the debug file so
          none is retried — explicitly: do not re-litigate `min-height` or `align-items` on that
          element;
        - what is STILL OWED is a run-4 live gate that re-measures item 1 IN FULL, because the fix
          was confirmed in ONE non-scored theme (the ambient teal scheme) and this exact truth has
          already been wrongly declared fixed twice in this phase;
        - **Next: `/gsd-execute-phase 34.10`**.

    (b) Update the `**Plans:**` line to `26 plans` and append the gap-cycle-3 checklist below the
        existing gap-cycle-2 one, using the same `- [ ]` format:
            Gap cycle 3 plans, closing the run-3 gaps recorded in `34.10-VERIFICATION.md`:
            - [ ] 34.10-23-PLAN.md — rescope the leaking `.MuiTabs-root` rule + repo-wide guard test (wave 1)
            - [ ] 34.10-24-PLAN.md — correct stale ROADMAP/STATE records; record the pill-tab deferral (wave 1)
            - [ ] 34.10-25-PLAN.md — author the run-4 gate contract §10.1-§10.4 (scope, preflight, reviews) (wave 2)
            - [ ] 34.10-26-PLAN.md — author §10.5 item bodies + prove every automated check against a filled specimen (wave 3)
            - [ ] 34.10-27-PLAN.md — RUN the run-4 gate; reconcile REQUIREMENTS/ROADMAP/STATE (wave 4, blocking checkpoint)

    (c) Add a **Deferred from this phase** entry recording the pill-tab election, verbatim-quoting the
        operator from `34.10-LIVE-GATE.md` §9.6: *"i going to drop the tab and let make a pill, that
        what it looks like anyway! just needs rounded bottom corners."* State that it is DEFERRED to
        separate follow-up work, is explicitly OUT OF SCOPE for gap cycle 3, and that
        **REQ-34.10-06's card/folder framing STANDS unchanged** — run 4 scores the tab shape as it
        currently ships. State that this is a recorded decision, not an oversight and not an open
        question, so `34.10-VERIFICATION.md`'s third `human_verification` item is answered.
        Note the phase already owes a deferred onboarding-tour rework phase (CONTEXT.md D-13); the
        pill restyle is a second deferred item, not a replacement for it.

    (d) Add a line recording the follow-up finding the debug session filed and plan 34.10-23 closes:
        the unscoped-`.MuiTabs-root` leak class had no regression guard; 34.10-23 rescopes the rule
        and adds one.

    Preserve the "--- run 2's status, preserved as history ---" and "--- run 1's original entry ---"
    blocks byte-for-byte. This section is append-and-correct, not rewrite.
  </action>
  <acceptance_criteria>
    - `grep -c '/gsd-debug' .planning/ROADMAP.md` returns `0` within the Phase 34.10 section
      (verify by extracting lines from `### Phase 34.10:` to the next `### Phase ` heading and
      grepping that extract, not the whole file)
    - That same extract contains all of: `220211230`, `1c7a3359d`, `gap cycle 3`, `34.10-27-PLAN.md`,
      `pill`, and `/gsd-execute-phase 34.10`
    - That extract contains the string `REQ-34.10-06` in a sentence stating the card/folder framing
      stands
    - The extract still contains `--- run 2's status, preserved as history ---` and
      `--- run 1's original entry, preserved as history ---`
    - `git diff --stat .planning/ROADMAP.md` shows changes confined to the Phase 34.10 section
      (no hunk header outside the 2352-2500 line neighbourhood)
  </acceptance_criteria>
  <verify>
    <automated>awk '/^### Phase 34\.10:/,/^### Phase 34\.11/' .planning/ROADMAP.md > /tmp/p3410.txt && ! grep -q '/gsd-debug' /tmp/p3410.txt && grep -q '220211230' /tmp/p3410.txt && grep -q '1c7a3359d' /tmp/p3410.txt && grep -q 'gap cycle 3' /tmp/p3410.txt && grep -q '34.10-27-PLAN.md' /tmp/p3410.txt && grep -qi 'pill' /tmp/p3410.txt && grep -q '/gsd-execute-phase 34.10' /tmp/p3410.txt && echo ROADMAP_OK</automated>
  </verify>
  <done>
    ROADMAP.md's Phase 34.10 section describes the closed debug session, lists gap cycle 3's five
    plans, records the pill-tab deferral with REQ-34.10-06 explicitly unchanged, and points at
    `/gsd-execute-phase 34.10`. Runs 1 and 2's history blocks are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct STATE.md's stopped_at and Phase 34.10 position block</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` lines 1-30 (frontmatter and Current Position header)
    - `.planning/STATE.md` lines 478-522 — the Phase 34.10 position block, including the "NEXT COMMAND: /gsd-debug" line at ~518 and the "34.10-VERIFICATION.md IS STALE" warning at ~515
  </read_first>
  <action>
    **Edit STATE.md by hand with the Edit tool. Do NOT use `gsd-sdk query state.*` write handlers
    for this file** — this project has a recorded, repeatedly-confirmed defect where state writes
    corrupt STATE.md (misplacing frontmatter fields and relocating entries). If any tooling touches
    it, re-read the file afterward and hand-correct.

    (a) Frontmatter line 6 — replace `stopped_at:` with a current value naming this cycle:
        gap cycle 3 planned (plans 34.10-23..27); the `/gsd-debug` session is CLOSED and its fix is
        live-confirmed; what remains is a run-4 live gate re-measuring item 1 in full; next command
        `/gsd-execute-phase 34.10`.
        Update `last_updated` to the current ISO timestamp and `last_activity` to today's date.
        Update `progress.total_plans` and `progress.completed_plans` to account for the five new,
        not-yet-executed plans (total +5, completed unchanged). State both the old and new numbers
        in the SUMMARY so the arithmetic is auditable.

    (b) In the Phase 34.10 position block (~lines 481-520), make FOUR corrections and leave the rest
        of the block as historical record:
        - Change `**NEXT COMMAND: /gsd-debug**` (~line 518) and its two-line justification to
          `**NEXT COMMAND: /gsd-execute-phase 34.10**` — gap cycle 3 is planned as plans
          34.10-23..27.
        - Replace the `⚠ 34.10-VERIFICATION.md IS STALE` warning (~lines 515-516) with a note that
          VERIFICATION.md was REGENERATED 2026-08-09 from run 3 plus the resolved debug session and
          is now AUTHORITATIVE.
        - Amend the "STILL OPEN" paragraph (~lines 491-495): F-34.10-03 and F-34.10-04 are now FIXED
          (commit `220211230`) and operator-confirmed (commit `1c7a3359d`), but confirmed in ONE
          non-scored theme only, so they are `fixed-but-unmeasured-by-the-gate`, and REQ-34.10-06 /
          REQ-34.10-16 correctly stay Pending until a run-4 gate scores a genuine 5/5.
        - Amend the pill-tab line (~line 513): it is DEFERRED to separate follow-up work, out of
          scope for gap cycle 3, and REQ-34.10-06's card/folder framing stands for run 4.
        Add one new line recording that the gamepad focus-scroll sub-check has been NOT ATTEMPTED in
        every run of this phase (P9 unmet, no controller) and is either measured in run 4 or
        permanently carried forward as a named residual risk — never inferred from the
        mouse/keyboard result.

    Do not touch the Phase 34.5 ACTIVE BLOCKER banner or any other phase's entry.
  </action>
  <acceptance_criteria>
    - `sed -n '6p' .planning/STATE.md` contains `34.10-23` and does NOT contain `/gsd-debug`
    - `grep -c 'NEXT COMMAND: /gsd-debug' .planning/STATE.md` returns `0`
    - `grep -c '/gsd-execute-phase 34.10' .planning/STATE.md` returns at least `1`
    - `grep -c '220211230' .planning/STATE.md` returns at least `1`
    - The frontmatter still parses: `head -15 .planning/STATE.md` shows a well-formed YAML block
      opening and closing with `---`, with `gsd_state_version`, `milestone`, `status`, `stopped_at`,
      `last_updated`, `last_activity` and `progress` all present and each appearing exactly once
    - `grep -c '⛔ ACTIVE BLOCKER — Phase 34.5' .planning/STATE.md` is unchanged from its pre-edit value
  </acceptance_criteria>
  <verify>
    <automated>head -15 .planning/STATE.md && sed -n '6p' .planning/STATE.md | grep -q '34.10-23' && ! grep -q 'NEXT COMMAND: /gsd-debug' .planning/STATE.md && grep -q '220211230' .planning/STATE.md && echo STATE_OK</automated>
  </verify>
  <done>
    STATE.md's frontmatter and Phase 34.10 block describe the closed debug session, the fixed-but-
    unmeasured findings, the deferred pill tab, the permanently-unmeasured gamepad sub-check, and
    `/gsd-execute-phase 34.10` as the next command. The YAML frontmatter is intact and every field
    appears exactly once.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan. No code, no runtime surface, no input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-24-01 | Tampering | STATE.md corruption by GSD state-write tooling | mitigate | Task 2 forbids `gsd-sdk query state.*` writes for this file and requires a post-edit YAML-integrity assertion checking each frontmatter field appears exactly once |
| T-34.10-24-02 | Repudiation | A deferral recorded so vaguely it reads later as an oversight | mitigate | The pill-tab entry must carry the operator's verbatim quote, an explicit OUT OF SCOPE statement, and an explicit "REQ-34.10-06 stands" statement |
| T-34.10-24-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task exists in this plan; it modifies two markdown files. Any install attempt here is out of scope and must stop the plan. |
</threat_model>

<verification>
- `git diff --name-only` lists exactly `.planning/ROADMAP.md` and `.planning/STATE.md`. Another
  session is active in this repository: explicit per-file `git add` only, never `git add -A`/`.`/`-a`.
- No `src/` file is touched, so no test or typecheck baseline can change. Do not run the full suite
  for this plan; it proves nothing here.
- Nothing in this plan closes a requirement. REQ-34.10-06 and REQ-34.10-16 stay Pending; only a
  genuine 5/5 run-4 verdict may change them (plan 34.10-27).
</verification>

<success_criteria>
- Neither ROADMAP.md's Phase 34.10 section nor STATE.md tells a reader to run `/gsd-debug`.
- The pill-tab election is discoverable as a deferred decision with its scope boundary stated.
- REQ-34.10-06's wording is byte-identical to before this plan (verify: `git diff .planning/REQUIREMENTS.md` is empty — this plan does not touch that file).
- The gap-cycle-3 plan list is visible in ROADMAP.md.
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-24-SUMMARY.md` when done.
</output>
