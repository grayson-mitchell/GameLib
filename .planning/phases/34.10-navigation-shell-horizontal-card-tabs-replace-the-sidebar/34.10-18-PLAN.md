---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 18
type: execute
wave: 2
depends_on: ["34.10-17"]
files_modified:
  - src/frontend/App.css
  - src/frontend/components/UI/NavShell/index.scss
  - src/frontend/screens/Library/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts
autonomous: true
gap_closure: true
requirements: [REQ-34.10-06]

must_haves:
  truths:
    - "`.NavShell__navbar` carries the content-boundary border the card/folder recipe's erasure mechanic needs, so the active tab's `border-bottom: 1px solid var(--body-background)` has something to erase"
    - "The navbar row is structurally outside the scrolling region — it cannot scroll away, without relying on a `position: sticky` that a grid item's own grid area would defeat"
    - "An active scrollbar is drawn inside the content region, starting below the navbar's bottom edge, not at the window's full-height right edge"
    - "Library's scroll-position restore and back-to-top button still fire, against the new scroll container, rather than silently listening to an element that no longer scrolls"
    - "No percentage height or min-height is introduced on `.App` or `.App .content` — the F-10 WKWebView trap that produced a 23323.0625px row is not re-entered"
  artifacts:
    - path: "src/frontend/App.css"
      provides: "The shell grid with a non-scrolling navbar row and `.App .content` as the scroll container"
      contains: "overflow"
    - path: "src/frontend/components/UI/NavShell/index.scss"
      provides: "The navbar's content-boundary border completing the card/folder seam recipe"
      contains: "border-bottom"
    - path: "src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts"
      provides: "Comment-immune source gates pinning both halves of the seam recipe, the scroll-container ownership, and the absence of percentage heights"
      min_lines: 60
  key_links:
    - from: "src/frontend/components/UI/NavShell/index.scss (.NavShell__navbar border-bottom)"
      to: "src/frontend/components/UI/NavShell/components/NavTabs/index.scss (.Mui-selected border-bottom)"
      via: "the seam-erasure pair from navigation-shell.md § Card / folder tab"
      pattern: "border-bottom"
    - from: "src/frontend/screens/Library/index.tsx"
      to: "src/frontend/App.css (.App .content)"
      via: "the scroll listener and scrollTo target resolved from the DOM, not hardcoded to document.body"
      pattern: "main\\.content"
---

<objective>
Close **F-34.10-03** (a visible ~10px seam between the tier-1 tab strip and the content region) and
**F-34.10-06** (the navbar scrolls away with page content; an active scrollbar draws over it) —
the two findings the verifier root-caused in source, with the offending commit and the missing
declarations named.

Purpose: both are confirmed regressions with known causes, so they get a fix, not a diagnosis.
F-34.10-03 is half a recipe: `navigation-shell.md`'s card/folder tab pattern requires a
`border-bottom` on the navbar for the active tab's own `border-bottom` — already present in
`NavTabs/index.scss` — to erase. F-34.10-06 is a relocation that never happened: commit
`0000baf94` deleted `.App .Sidebar { position: sticky; top: 0px; max-height: 100vh }` stating that
layout ownership moves into `NavShell/index.scss`, and no positioning rule was ever added there.

Output: a navbar that is structurally un-scrollable, a scrollbar that starts below it, a completed
seam recipe, and source gates that pin all three so a future refactor cannot silently undo them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-CONTEXT.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F04-DIAGNOSIS.md

Required skills — invoke both before writing any CSS:
- `Skill("sketch-findings-gamelib")` — `references/navigation-shell.md` § "CSS Patterns → Card /
  folder tab" is the exact recipe whose missing half IS F-34.10-03. Do not re-derive the design;
  port it. § 3's navbar-lighter-than-body table is why the border token must be relational.
- `Skill("spike-findings-gamelib")` — Tauri/WKWebView gotchas.

<interfaces>
<!-- Current state of every rule this plan touches, already extracted. Do not go looking. -->

`src/frontend/App.css` — the shell grid as it stands:

  .App { text-align: center; display: grid;
         grid-template-columns: min-content 1fr;
         grid-template-areas: 'offline offline' 'navbar navbar'
                              'tier2 content' 'tier2 controller';
         grid-template-rows: min-content min-content 1fr min-content;
         min-height: 100vh; }

  .App .content { container: main-content / inline-size; display: flex;
                  grid-area: content; flex-direction: column;
                  /* a long F-10 comment block lives here — PRESERVE IT VERBATIM */
                  &:not(:has(.gameConfigContainer img.backgroundImage)) {
                    background: var(--gradient-body-background, var(--body-background)); } }

  .App .controller { grid-area: controller; position: sticky; bottom: 0px;
                     background: var(--background); z-index: 1400; }

`src/frontend/index.scss` — who actually scrolls today:

  :root { overflow-y: overlay; }
  html  { width: 100vw; overflow-x: hidden; }
  body  { height: 100vh; overflow: auto !important; padding: 0 !important; }
  ::-webkit-scrollbar { width: 10px; height: 10px;
                        background: var(--current-background, var(--body-background)); }

  -> `body` is the scroll container today (height 100vh + overflow auto), `.App` is min-height
     100vh and grows past it. The scrollbar is body's, drawn at the window's full-height right
     edge — which is precisely why it draws over the navbar. It is a real 10px-wide scrollbar,
     not an overlay one.

`src/frontend/components/UI/NavShell/index.scss`:

  .NavShell__navbar { grid-area: navbar; height: var(--navbar-height);
                      background: var(--navbar-background); display: flex;
                      align-items: flex-end; padding-inline-start: var(--traffic-light-inset); }
  -> No `border-bottom`. No `position`. `--navbar-height: 56px`, declared at `:root` in this file.

`src/frontend/components/UI/NavShell/components/NavTabs/index.scss` — the OTHER half of the recipe,
already shipped, do NOT change it:

  .NavTabs .MuiTab-root { border: 1px solid transparent; border-bottom: none;
                          position: relative; top: 1px; ... }
  .NavTabs .MuiTab-root.Mui-selected { background: var(--body-background);
                                       border-color: var(--divider);
                                       border-bottom: 1px solid var(--body-background); }

`src/frontend/screens/Library/index.tsx` — the two effects that hardcode the scroll container:

  279  useLayoutEffect(() => {
  281    const scrollPosition = parseInt(storage?.getItem('scrollPosition') || '0')
  283    const storeScrollPosition = () => {
  285      storage?.setItem('scrollPosition', document.body.scrollTop.toString() || '0')
  287    }
  290    document.body.addEventListener('scroll', storeScrollPosition)
  291    document.body.scrollTo(0, scrollPosition || 0)
  293    return () => { document.body.removeEventListener('scroll', storeScrollPosition) }
  296  }, [])

  298  useEffect(() => {              // back-to-top button visibility
  303    const scrollCallback = () => {
  305      btn.style.visibility = document.body.scrollTop > 450 ? 'visible' : 'hidden'
  308    }
  310    document.body.addEventListener('scroll', scrollCallback)
  311    return () => document.body.removeEventListener('scroll', scrollCallback)
  312  }, [])

  315  const backToTop = () => { anchor.scrollIntoView({ behavior:'smooth', block:'center' }) }
  -> `scrollIntoView` walks up to whatever scroll container actually exists; it needs NO change.

`src/frontend/components/UI/NavShell/components/NavTabs/index.tsx:67` also writes
`localStorage.setItem('scrollPosition', '0')` — a plain storage write with no DOM target. No change.

`src/frontend/App.tsx` renders `<main className="content">` (and, in console mode only,
`<main className="content consoleContent">`) as a direct grid child of `.App`.

Other `position: sticky` rules in the tree, for blast-radius awareness:
  App.css:68                     `.App .controller` (grid-area controller, bottom: 0)
  Library/index.css:47           `.libraryHeader` (top: 0) — lives INSIDE `.content`
  WindowControls/index.scss:7    `.windowControls` (grid-area content, align-self: start, top: 0)
  Dialog/index.css:71, WineManager/index.css:114 — inside their own scrollers, unaffected

Test helper already used by this phase's source gates:
  `import { stripSourceComments } from 'backend/testUtils/stripSourceComments'`
  `src/frontend/components/UI/NavShell/__tests__/shellTokens.test.ts` is the existing idiom —
  `readFileSync` + `stripSourceComments` + regex counts. Follow it; do not invent a new one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the card/folder seam recipe by giving the navbar the border the active tab erases</name>
  <files>src/frontend/components/UI/NavShell/index.scss</files>
  <read_first>
    - .claude/skills/sketch-findings-gamelib/references/navigation-shell.md § "CSS Patterns → Card / folder tab" (the `.navbar { border-bottom: 1px solid var(--color-border) }` line is the missing half)
    - src/frontend/components/UI/NavShell/components/NavTabs/index.scss (the shipped half — read it, do not modify it)
    - src/frontend/components/UI/NavShell/index.scss (the `.NavShell__navbar` rule)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F04-DIAGNOSIS.md § "F-34.10-04 hypothesis table" H5 call-out — if H5 reported a measured non-zero gap between `navbar.bottom` and `content.top`, record that fact in this task's own comment and in the SUMMARY; it means a SECOND contributor to F-34.10-03 exists and plan 34.10-19 owns it
  </read_first>
  <action>
    Add a `border-bottom` to `.NavShell__navbar` in `src/frontend/components/UI/NavShell/index.scss`,
    using `var(--divider)` — the same relational token `.NavTabs .MuiTab-root.Mui-selected` already
    uses for `border-color`, so the two halves of the recipe cannot drift apart per-theme. Do not
    use a literal colour and do not introduce a new token: `navigation-shell.md` § 3's inversion
    table (navbar LIGHTER than body in all three scored themes, `dracula` most severely) is exactly
    what a fixed colour breaks.

    Above the declaration, write a comment stating what it is for in the recipe's own terms: the
    active tab's `border-bottom: 1px solid var(--body-background)` plus its `top: 1px` offset exist
    to ERASE this border precisely where the two overlap, producing the merged-into-content
    illusion REQ-34.10-06 requires. Say plainly that removing either half silently reverts the
    illusion, since one half alone produces no visible error.

    Note in the comment, and confirm before committing, that the global `* { box-sizing: border-box }`
    in `App.css` means this 1px border is drawn INSIDE the existing `height: var(--navbar-height)` —
    the navbar's outer box does not grow and no row re-flow occurs. Do not change
    `--navbar-height` here; the tab strip's own height is plan 34.10-19's subject.

    Do not touch `NavTabs/index.scss` in this plan at all — it already ships its half correctly, and
    a needless edit there would collide with plan 34.10-19's file ownership.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const strip=(s)=>s.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,'');const nav=strip(fs.readFileSync('src/frontend/components/UI/NavShell/index.scss','utf8'));const m=nav.match(/\.NavShell__navbar\s*\{[^}]*\}/);if(!m)throw new Error('.NavShell__navbar rule not found');if(!/border-bottom:\s*1px\s+solid\s+var\(--divider\)/.test(m[0]))throw new Error('F-34.10-03: navbar border-bottom missing or not var(--divider)');if(/#[0-9a-fA-F]{3,8}/.test(m[0].match(/border-bottom:[^;]*/)[0]))throw new Error('F-34.10-03: navbar border-bottom uses a literal colour');const tabs=strip(fs.readFileSync('src/frontend/components/UI/NavShell/components/NavTabs/index.scss','utf8'));if(!/border-bottom:\s*1px\s+solid\s+var\(--body-background\)/.test(tabs))throw new Error('F-34.10-03: the erasure half in NavTabs is gone — the recipe is broken again');console.log('both halves of the seam recipe present')" && pnpm jest --runInBand --silent --forceExit src/frontend/components/UI/NavShell/__tests__/shellTokens.test.ts</automated>
  </verify>
  <done>`.NavShell__navbar` declares `border-bottom: 1px solid var(--divider)` with a comment naming the erasure mechanic; `NavTabs/index.scss` is unmodified; `shellTokens.test.ts` still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Move the scrolling region out of the navbar row and migrate Library's scroll listeners onto it</name>
  <files>src/frontend/App.css, src/frontend/screens/Library/index.tsx</files>
  <read_first>
    - src/frontend/App.css (the whole `.App` and `.App .content` rules, including the long F-10 comment block that must survive verbatim)
    - src/frontend/index.scss lines 1-50 (`:root { overflow-y: overlay }`, `html`, `body { height: 100vh; overflow: auto !important }`, and the 10px `::-webkit-scrollbar` — this is the scrollbar that currently draws over the navbar)
    - src/frontend/screens/Library/index.tsx lines 276-320 (the two effects and `backToTop`)
    - .planning/phases/34.4.1-*/34.4.1-F10-DIAGNOSIS.md if present, otherwise the F-10 comment block in App.css itself
  </read_first>
  <action>
    **The obvious fix is wrong and must not be attempted.** `position: sticky; top: 0` on
    `.NavShell__navbar` cannot work: a sticky-positioned grid item is constrained to its own grid
    area, and the navbar's grid area is the single `min-content` row that is exactly as tall as the
    navbar itself — zero sticky travel, no pinning, and a change that looks correct in review while
    doing nothing. The old `.App .Sidebar` rule worked only because its grid area spanned the tall
    `1fr` row. Do not port that rule shape onto the navbar. State this reasoning in a comment so the
    next reader does not "simplify" it back.

    Instead, take the navbar row out of the scrolling region entirely, which fixes both halves of
    F-34.10-06 at once — the navbar cannot scroll away, and the scrollbar is drawn by the content
    region so it starts below the navbar's bottom edge instead of at the window's full-height right
    edge. Two edits in `src/frontend/App.css`:

    1. On `.App`, replace `min-height: 100vh` with `height: 100vh` and add `overflow: hidden`. Use
       the viewport unit, never a percentage.
    2. On `.App .content`, add `overflow-y: auto` and `min-height: 0`. `min-height: 0` is what lets
       a grid item in a `1fr` row shrink below its content size so the row stops growing and the
       element scrolls instead; without it the row grows and nothing scrolls.

    **F-10 trap, hardware-confirmed on this project, directly in this blast radius:** a WKWebView
    resolving a percentage `height`/`min-height` against a `1fr` grid row produced a garbage
    `23323.0625px` row (the fractional `.0625` was the fingerprint of an iterative solve) and a
    blank window. Every value you add here must be a viewport unit or `0` — no `100%`, no `auto`
    percentage, on either `.App` or `.App .content`. Preserve the existing F-10 comment block in
    `.App .content` verbatim and add your `min-height: 0` reasoning beneath it, explaining that `0`
    is a length, not a percentage, and does not re-enter the cycle. This fix must be judged live
    under Tauri/WKWebView — plan 34.10-22's gate is the adjudicator, not a desktop browser.

    Do not change `src/frontend/index.scss`. `body { height: 100vh; overflow: auto !important }`
    stays as it is; with `.App` now exactly `100vh` there is nothing for body to scroll, so its
    scrollbar disappears on its own. Editing that file would widen the blast radius past this
    finding for no gain.

    Then migrate `src/frontend/screens/Library/index.tsx`, whose two effects address
    `document.body` directly and would otherwise keep listening to an element that no longer
    scrolls — a silent break of both scroll-position restore and the back-to-top button, with no
    error and no failing test. In each effect, resolve the scroll container once, inside the effect
    body, into a local const — `document.querySelector('main.content') ?? document.body` — and use
    that same local for `addEventListener`, `removeEventListener`, `scrollTop` and `scrollTo`, so
    the cleanup can never detach from a different element than it attached to. Do not resolve it at
    module scope: `main.content` does not exist until App has mounted. Keep the `?? document.body`
    fallback and comment why (console mode and any future shell that does not render the nav shell).
    Leave `backToTop`'s `scrollIntoView` untouched — it walks up to whatever scroll container
    exists and needs no change; say so in a comment so a later reader does not "fix" it too.

    Explicitly OUT OF SCOPE, do not touch, but record in the SUMMARY:
    `src/frontend/screens/Discounts/index.tsx:549`'s `window.scrollTo({ top: 0 })` is already inert
    today (the window has never been the scroll container — `body` is), so it is a pre-existing
    latent defect, not a regression this plan introduces. Do not fix it here.
  </action>
  <verify>
    <automated>pnpm codecheck && pnpm jest --runInBand --silent --forceExit src/frontend/screens/Library</automated>
  </verify>
  <done>`.App` is `height: 100vh; overflow: hidden`, `.App .content` is `overflow-y: auto; min-height: 0`, no percentage height exists on either, the F-10 comment block survives verbatim, and both Library effects attach to and detach from the same resolved `main.content` element with a `document.body` fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the seam recipe, the scroll-container ownership and the F-10 prohibition in source gates</name>
  <files>src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts</files>
  <read_first>
    - src/frontend/components/UI/NavShell/__tests__/shellTokens.test.ts (the exact `readFileSync` + `stripSourceComments` + regex-count idiom to follow — including its docstring's explanation of why source-text gates are the only option in a `testEnvironment: 'node'` project with no jsdom and no CSS engine)
    - src/backend/testUtils/stripSourceComments.ts (the helper's actual signature and behaviour)
    - src/frontend/App.css and src/frontend/components/UI/NavShell/index.scss as edited by tasks 1 and 2
  </read_first>
  <action>
    Before creating the file, run `ls src/frontend/components/UI/NavShell/__tests__/` and confirm no
    entry differing only in case from `appShellLayout.test.ts` exists. This project has a recorded
    trap: on macOS's case-insensitive filesystem two paths differing only in case silently collide
    and one is DROPPED from `tsc` and `eslint` with no diagnostic, so the dropped file becomes
    coverage theatre. If a collision exists, stop and report rather than picking a near-name.

    Create `src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts` following
    `shellTokens.test.ts`'s idiom exactly: `readFileSync` the specific stylesheets, run
    `stripSourceComments` over every one before asserting, and assert on the stripped text only.
    The comment-stripping is load-bearing, not hygiene — this plan's own task 1 and task 2 both
    write explanatory comments that mention the very declarations being asserted, so an unstripped
    scan could be satisfied by prose alone. Write that reason into the file's docstring.

    Assert exactly these, each as its own `it`, each with a message naming the finding it guards:

    1. **F-34.10-03, navbar half.** The `.NavShell__navbar` rule in `NavShell/index.scss` contains
       `border-bottom: 1px solid var(--divider)`.
    2. **F-34.10-03, tab half.** `NavTabs/index.scss` still contains
       `border-bottom: 1px solid var(--body-background)` inside a `.Mui-selected` rule. Both halves
       are asserted together in one file on purpose: the recipe is a pair, and deleting either half
       produces no visible error, which is exactly how this shipped broken.
    3. **F-34.10-03, no literal colour.** Neither the navbar's `border-bottom` nor the selected
       tab's names a hex literal — both resolve through a `var(--...)` token, because
       `navigation-shell.md` § 3 shows the navbar is lighter than the body in every scored theme.
    4. **F-34.10-06, scroll ownership.** The `.App` rule in `App.css` contains `overflow: hidden`
       and `height: 100vh`, and does NOT contain `min-height: 100vh`.
    5. **F-34.10-06, the scrolling element.** The `.App .content` rule contains `overflow-y: auto`
       and `min-height: 0`.
    6. **F-10 prohibition.** Neither the `.App` rule nor the `.App .content` rule contains any
       percentage `height:` or `min-height:` value. Write the assertion against a pattern that
       matches any digits-then-`%`, and cite the measured `23323.0625px` row in the failure message
       so a future author who trips it knows immediately what they re-entered.
    7. **F-34.10-06, no dead sticky.** `NavShell/index.scss` does not declare `position: sticky` on
       `.NavShell__navbar`. State in the message that a sticky grid item is constrained to its own
       grid area and would have zero travel in the `min-content` navbar row — this gate exists so
       the wrong-but-plausible fix cannot be added later as an apparent improvement.
    8. **Library scroll-container migration.** `src/frontend/screens/Library/index.tsx`, stripped of
       comments, contains no `document.body.addEventListener`, no `document.body.removeEventListener`,
       no `document.body.scrollTop` and no `document.body.scrollTo`, and does contain the
       `main.content` selector. Name the silent-failure mode in the message: a listener on a
       no-longer-scrolling element throws nothing and fails no test.

    Every count-style assertion must run against the comment-stripped text, never the raw file. Do
    not add a gate that asserts a count of `0` against unfiltered source.

    No user-facing string is added by this plan, so `meta/i18nGateScope.json` needs no
    regeneration. Confirm that by checking `git status` after this task: if the file is dirty,
    something in this plan introduced a translatable literal and it must be localised and the file
    committed — a green jest run does not mean the blocking i18n gate is satisfied.
  </action>
  <verify>
    <automated>pnpm jest --runInBand --silent --forceExit src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts</automated>
  </verify>
  <done>`appShellLayout.test.ts` exists with eight comment-immune source gates covering both halves of the seam recipe, the scroll-container relocation, the F-10 percentage prohibition, the dead-sticky prohibition and Library's migration — all passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document/body scroll → `.App .content` scroll | Every consumer that hardcoded `document.body` silently stops working; nothing throws |
| stylesheet source text → gate assertion | This plan's own comments mention the asserted declarations, so an unstripped scan is self-satisfying |
| desktop-browser reasoning → WKWebView behaviour | Grid + percentage-height interactions have already produced a garbage row on this project once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-60 | Denial of service | `.App` / `.App .content` under WKWebView | mitigate | Only viewport units and `0` are permitted; gate 6 forbids any percentage height on either rule and cites the measured `23323.0625px` row; the live gate in plan 34.10-22 re-measures under Tauri, not a browser |
| T-34.10-61 | Tampering | Library's scroll-position restore and back-to-top button | mitigate | Task 2 resolves the container once per effect into a local used for both attach and detach; gate 8 forbids any surviving `document.body` scroll reference |
| T-34.10-62 | Repudiation | a source gate satisfied by an explanatory comment | mitigate | `stripSourceComments` runs before every assertion, and the docstring records that this plan's own comments name the asserted declarations |
| T-34.10-63 | Tampering | the seam recipe half-reverted by a later refactor | mitigate | Gates 1-3 assert BOTH halves in one file, so deleting either fails the same suite |
| T-34.10-64 | Tampering | a later author adding `position: sticky` to the navbar as an apparent improvement | mitigate | Gate 7 forbids it and states why it would have zero travel |
| T-34.10-65 | Spoofing | a case-colliding test filename silently dropped from tsc/eslint | mitigate | Task 3 requires an `ls` of the `__tests__` directory and a stop-and-report on any case-differing entry |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists in this plan |
</threat_model>

<verification>
- `pnpm codecheck` clean.
- `pnpm jest --runInBand --silent --forceExit` — record the observed suite/test counts in the
  SUMMARY. Derive the expected baseline from the most recent full-suite run recorded in this
  phase's SUMMARYs, not from an older plan's quoted number, and state both.
- `git status` shows `meta/i18nGateScope.json` clean. This plan adds no user-facing string; if that
  file is dirty, a translatable literal was introduced and must be localised and the file
  committed before the plan closes.
- **Nothing in this plan is proven by the test suite.** The `Frontend` jest project is
  `testEnvironment: 'node'` with no jsdom and no CSS engine — no test here can see a seam, a
  scrollbar, or a pinned navbar. Every gate above pins source text only. The adjudicator is plan
  34.10-22's live gate item 1. Do not tick REQ-34.10-06 from anything in this plan.
</verification>

<success_criteria>
- `.NavShell__navbar` carries `border-bottom: 1px solid var(--divider)`; `NavTabs/index.scss` is unmodified
- `.App` is `height: 100vh; overflow: hidden`; `.App .content` is `overflow-y: auto; min-height: 0`; no percentage height on either
- No `position: sticky` was added to the navbar
- Library's two scroll effects attach and detach against a single resolved `main.content` element with a `document.body` fallback
- `appShellLayout.test.ts` passes with all eight comment-immune gates
- `pnpm codecheck` clean and the full suite at or above its recorded baseline
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-18-SUMMARY.md` when done
</output>
