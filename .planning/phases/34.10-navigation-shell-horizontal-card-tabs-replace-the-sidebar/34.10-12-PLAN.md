---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md
autonomous: false
gap_closure: true
requirements: [REQ-34.10-08]

must_haves:
  truths:
    - "It is recorded, from a live observation on real hardware, whether ANY surface in the app displays an advancing install percent during a real download — not just whether the ring does"
    - "The DM queue head element is confirmed (or falsified) to be the element actually downloading, by direct observation of the /download-manager list order"
    - "Exactly one hypothesis for F-34.10-02 cause (b) survives, and every rejected hypothesis carries the file:line or observation that rejects it"
    - "No code fix is written in this plan — a fix authored before the discriminator runs is the failure mode this plan exists to prevent"
  artifacts:
    - path: ".planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md"
      provides: "The live measurement, the hypothesis table with verdicts, and the single surviving root cause that plan 34.10-15 implements against"
      contains: "Surviving hypothesis"
  key_links:
    - from: "34.10-F02-DIAGNOSIS.md"
      to: "src/frontend/hooks/hasProgress.ts / src/frontend/state/InstallProgress.ts"
      via: "the traced progressUpdate -> store -> hook -> --dl-progress path, with the break point named"
      pattern: "hasProgress"
---

<objective>
Root-cause F-34.10-02 cause (b) — `--dl-progress` never leaving `0turn` against a live download —
by running a three-question live discriminator FIRST and only then reading source, producing a
written diagnosis that plan 34.10-15 implements against.

Purpose: this project's recorded lesson is explicit — "when two readings both fit, build the
discriminator" and "one `sample <pid>` collapsed a 3-candidate field instantly". The gate author's
own diagnosis names a *likely* cause ("a keying/remount or timing difference vs. the retired
`CurrentDownload`") that is already falsified by source: the retired `Sidebar/index.tsx` used the
identical `elements[0]` source AND the identical `key={...params.appName}`. Writing a fix against a
falsified premise is exactly how a phase burns a gap cycle.
Output: `34.10-F02-DIAGNOSIS.md` with a live measurement, a hypothesis table, and one surviving cause.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-03-SUMMARY.md

<interfaces>
<!-- The full progress data path, already traced at planning time. The executor does NOT need to
     re-discover this; it needs to find which link in it is broken. -->

Backend emitters (all call the same channel):
  src/backend/utils.ts:1212, 1318, 1475        sendFrontendMessage('progressUpdate', payload)
  src/backend/storeManagers/steam/depot.ts:1698
  src/backend/storeManagers/steam/library.ts:1727

Transport (documented as needing NO ipcMain registration):
  src/backend/sidecar/downloadQueueFlowRegistration.ts:42-52 — "Push channels (`progressUpdate`,
  `changedDMQueueInformation`) are NOT registered here ... Both already flow through the existing,
  unmodified sendFrontendMessage -> getMainWindow().webContents.send -> electronStub's
  fakeWebContents.send -> pushFrontendMessage chain".
  => progressUpdate and changedDMQueueInformation share ONE transport chain. The count badge proves
     that chain is alive. Any hypothesis that says "the transport is dead" must explain that.

Preload slot:
  src/preload/api/library.ts:10   export const onProgressUpdate = frontendListenerSlot('progressUpdate')

Renderer store (registers its listener at MODULE LOAD, top level, not inside a hook):
  src/frontend/state/InstallProgress.ts:10
    window.api.onProgressUpdate((e, { appName, progress, runner }) => {
      useInstallProgressRaw.setState({ [`${appName}_${runner}`]: progress })
    })

Hook:
  src/frontend/hooks/hasProgress.ts:38
    const installationProgress = useInstallProgress((state) => state[`${appName}_${runner}`])
  returns [currentProgress, previousProgress]; currentProgress is seeded from
  localStorage[`${appName}_${runner}_progress`] via JSON.parse(... || '{}').

All SIX consumers of the same hook, with the same key shape:
  src/frontend/screens/DownloadManager/components/ProgressHeader/index.tsx:23
  src/frontend/screens/DownloadManager/components/DownloadManagerItem/index.tsx:91
  src/frontend/screens/Library/components/GameCard/index.tsx:147
  src/frontend/screens/Game/GamePage/index.tsx:135
  src/frontend/components/UI/DLCList/DLC/index.tsx:32
  src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx:26   <-- the broken one

<!-- FALSIFIED AT PLANNING TIME — do not spend effort re-testing these -->
The retired call site was NOT structurally different. `git show 0559bc0d0^:src/frontend/components/
UI/Sidebar/index.tsx` shows it subscribed to the same channel, took the same `elements[0]`, and
rendered `<CurrentDownload key={currentDMElement.params.appName} appName={...} runner={...} />`.
The only behavioural difference is that `CurrentDownload` returned `null` unless
`libraryStatus.length` was non-zero, so it was only ever ON SCREEN during an active install.
Therefore: "the new component lost the appName key" and "the new component remounts differently"
are both already dead. The gate's own suggested cause is wrong.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the three-question live discriminator and record the answers</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md (§ Item 4 and F-34.10-02 — the exact symptom already measured, so this diagnostic does not re-measure it)
    - src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx (the component under diagnosis)
    - src/frontend/screens/DownloadManager/components/DownloadManagerItem/index.tsx (the comparison surface — line 91 calls the identical hook with the identical key shape)
  </read_first>
  <action>
    Create `34.10-F02-DIAGNOSIS.md` with a `## Live measurement (run 1)` section containing the three
    questions below as a table with an explicit answer slot each, then present them to the developer,
    then write their answers back into that table before this task is done.

    This is a DIAGNOSTIC, not a live gate — decision D-E ("the plan that authors a gate may never
    also run it") governs `34.10-LIVE-GATE.md` and does not apply here. Do not touch
    `34.10-LIVE-GATE.md` in this plan at all.

    Preconditions to state in the file and confirm before asking:
    - Exactly one app process. Run `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'`;
      both must be empty before launch. A second concurrent instance is this project's own recorded
      capture-integrity trap (F-34.4.2-15/-18).
    - At least one game available to install. If none is, record every answer as NOT ATTEMPTED with
      that reason and say so explicitly — never infer an answer from an absence.

    The three questions (the whole point: Q1 discriminates an app-wide progress-plumbing failure
    from a `DownloadsRing`-local failure; Q3 discriminates "the head element is not the downloading
    element" from both):

    | # | Question | Why it discriminates |
    |---|---|---|
    | Q1 | During the SAME active download, does the `/download-manager` screen's own per-item percent readout advance past 0%? | `DownloadManagerItem/index.tsx:91` calls `hasProgress(appName, runner)` with the identical key shape. Advancing => the `progressUpdate` -> `InstallProgress` -> `hasProgress` path is ALIVE and the fault is local to `DownloadsRing`. Stuck at 0% => the fault is upstream and app-wide, and is NOT a 34.10 regression at all. |
    | Q2 | During the same download, does the game's card in the Games grid show advancing install progress? | `GameCard/index.tsx:147` is a third, independent consumer of the same hook. Two agreeing surfaces beat one. |
    | Q3 | On `/download-manager`, is the FIRST item in the queue list the game that is actually downloading, or is a different (queued, not started) item first? | `DownloadsRing` binds to `elements[0]`. If the actively-downloading element is not `elements[0]`, the ring is correctly reading a genuinely-zero-percent element and the bug is the binding, not the data. |

    Ask the developer for a plain yes/no plus any detail on each. Do NOT ask them to open a web
    inspector or read computed styles — this project has recorded that a Tauri webview may expose no
    inspector, and no answer here needs one.
  </action>
  <what-built>
    Nothing new is built by this task. `DownloadsRing` shipped in plan 34.10-03 and was mounted in
    34.10-07. The live gate already measured that its progress arc never fills. This task does not
    re-measure that — it measures three OTHER surfaces during the same download, so the next plan
    fixes the actual break instead of the one the gate's author guessed at.
  </what-built>
  <how-to-verify>
    Roughly five minutes, one download.

    1. Confirm no app is already running: `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'`
       must both print nothing. If either prints a PID, kill it or identify whose it is first.
    2. Run `pnpm tauri:dev`.
    3. Start any real download from any store.
    4. **Q1** — go to `/download-manager` (click the Downloads indicator in the navbar). Watch the
       downloading item's own percent readout for ~20 seconds. Report: does the number advance past
       0%, or does it sit at 0%?
    5. **Q3** — while still on `/download-manager`, report whether the item at the TOP of the queue
       list is the one actually downloading, or whether a different, not-yet-started item is listed
       first.
    6. **Q2** — go to the Games tab. Find the downloading game's card in the grid. Report whether it
       shows advancing install progress.

    Answer each question yes/no. If you could not reach a question, say "not attempted" and why —
    do not guess.
  </how-to-verify>
  <acceptance_criteria>
    - `34.10-F02-DIAGNOSIS.md` exists and contains a `## Live measurement (run 1)` section
    - Q1, Q2 and Q3 each carry a recorded answer of exactly `YES`, `NO`, or `NOT ATTEMPTED` — no blank, no `TBD`, no `[ ]` placeholder
    - Any `NOT ATTEMPTED` answer carries a stated reason on the same row
    - The single-process precondition result (`pgrep` output empty / not empty) is recorded in the file
    - `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md` is unmodified by this plan (`git diff --quiet -- <that path>` exits 0)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md';const s=fs.readFileSync(p,'utf8');if(!/##\s*Live measurement/.test(s))throw new Error('no Live measurement section');for(const q of ['Q1','Q2','Q3']){const row=s.split(/\r?\n/).find(l=>l.includes('| '+q+' '));if(!row)throw new Error('missing row for '+q);if(!/(YES|NO|NOT ATTEMPTED)/.test(row))throw new Error(q+' has no recorded answer');}if(/TBD|\[ \]/.test(s))throw new Error('unfilled placeholder left in diagnosis');console.log('diagnostic answers recorded')"</automated>
    <human-check>Three answers recorded from one real download on real hardware, with the single-process precondition confirmed</human-check>
  </verify>
  <done>`34.10-F02-DIAGNOSIS.md` carries a YES/NO/NOT ATTEMPTED answer for each of Q1, Q2, Q3, measured live, with no placeholders left.</done>
  <resume-signal>Type your three answers (e.g. "Q1 no, Q2 no, Q3 the downloading one is first"), or describe what blocked the measurement</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Trace the surviving break in source and write the root cause</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md (task 1's recorded answers — these decide which branch of the hypothesis table you are on)
    - src/frontend/hooks/hasProgress.ts (the hook, all 59 lines — the localStorage seed and `calculatePercent` are both live suspects)
    - src/frontend/state/InstallProgress.ts (the zustand store and its module-load listener registration)
    - src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx (`RingProgress` and the `elements[0]` binding)
    - src/backend/sidecar/downloadQueueFlowRegistration.ts (lines 42-52 — the documented shared transport chain for both push channels)
    - src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx (note it MOCKS `hasProgress` wholesale and never invokes `RingProgress` — it is structurally incapable of catching this class of defect, and that fact belongs in the writeup)
  </read_first>
  <action>
    Append a `## Hypothesis table` and a `## Surviving hypothesis` section to `34.10-F02-DIAGNOSIS.md`.

    Evaluate exactly these six hypotheses, each with a VERDICT of `SURVIVES`, `REJECTED` or
    `UNTESTABLE HERE`, and for every `REJECTED` cite the file:line or the recorded Q-answer that
    rejects it. Do not add a seventh without stating why it was needed.

    - **H1 — the `progressUpdate` push never reaches the renderer under Tauri.** Rejected if Q1 or Q2
      is YES. If Q1 and Q2 are both NO, this survives and the defect is app-wide, not a 34.10
      regression: `downloadQueueFlowRegistration.ts:42-52` claims both push channels share one
      chain, so a live count badge with a dead progress feed contradicts that claim and the
      contradiction must be resolved in source, not assumed away.
    - **H2 — key mismatch: the emitted `${appName}_${runner}` does not equal the DM element's
      `params.appName`/`params.runner`.** Compare the payload literal at `utils.ts:1475` and
      `steam/depot.ts:1698` against `DMQueueElement['params']` in `common/types`. Note this
      hypothesis predicts the SAME observable as H1 on Q1/Q2, so those two must be separated in
      source, not by the live answers.
    - **H3 — `hasProgress`'s own state machine suppresses the value.** Specifically: `previousProgress`
      is `JSON.parse('{}')`, which is truthy, so `currentProgress` seeds as `{}`; and
      `calculatePercent` returns `storedPercent` when a stale `${appName}_${runner}_progress`
      localStorage entry holds `percent: 100`. Work out what each of those produces and whether it
      can produce a persistent 0.
    - **H4 — `elements[0]` is not the downloading element.** Decided by Q3.
    - **H5 — the value is correct and the arc was simply invisible for the same reason as cause (a).**
      Note that `.DownloadsRing--idle` (`opacity: 0.5`) is NOT applied while the queue is non-empty,
      so an accent-coloured arc at full opacity would have been visible; state whether that reasoning
      holds or fails.
    - **H6 — React drops the `--dl-progress` custom property in the built bundle.** Check whether the
      built renderer output under `build/` (or `dist/`) still carries the property name; a custom
      property in a `style` object is passed through by React 18, so this is expected to be rejected
      cheaply.

    Then write `## Surviving hypothesis` naming exactly ONE cause, the exact file and line where the
    chain breaks, and — in one paragraph — the fix plan 34.10-15 should implement, stated as concrete
    identifiers and values, not as a direction.

    If the surviving cause lies OUTSIDE `src/frontend` (i.e. it is a backend/sidecar emitter or
    transport defect), do not invent a frontend workaround and do not derive the percent from a
    different source to make the arc move: mint it as a new finding `F-34.10-03` in the diagnosis
    file, state plainly that plan 34.10-15's scope changes, and say whether the fix is reachable
    inside `src/frontend` or not.

    Write no production code in this plan. `git add` only the diagnosis file by explicit path — never
    `git add -A`, `git add .` or `git commit -a`; another session is editing this repository.
  </action>
  <acceptance_criteria>
    - The file contains a `## Hypothesis table` with exactly six rows labelled H1..H6, each carrying `SURVIVES`, `REJECTED` or `UNTESTABLE HERE`
    - Exactly one hypothesis carries `SURVIVES` (if two would survive, the task is not done — find the source-level discriminator that separates them, per H1 vs H2)
    - Every `REJECTED` row cites either a `path:line` reference or one of `Q1`/`Q2`/`Q3`
    - A `## Surviving hypothesis` section names one file and line where the chain breaks
    - The writeup states that `DownloadsRing.test.tsx` mocks `hasProgress` and never invokes `RingProgress`, and is therefore incapable of catching this defect class
    - No file under `src/` is modified by this plan (`git status --porcelain src` prints nothing)
    - `pnpm codecheck` is clean (unchanged tree, so this is a no-drift check, not a claim about the fix)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'),cp=require('child_process');const p='.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md';const s=fs.readFileSync(p,'utf8');if(!/##\s*Hypothesis table/.test(s))throw new Error('no hypothesis table');const lines=s.split(/\r?\n/);for(const h of ['H1','H2','H3','H4','H5','H6']){if(!lines.some(l=>l.includes('| '+h+' ')||l.includes('**'+h+' ')))throw new Error('missing '+h);}const survives=(s.match(/SURVIVES/g)||[]).length;if(survives!==1)throw new Error('expected exactly 1 SURVIVES, got '+survives);if(!/##\s*Surviving hypothesis/.test(s))throw new Error('no surviving-hypothesis section');const dirty=cp.execSync('git status --porcelain src',{encoding:'utf8'}).trim();if(dirty)throw new Error('this plan must not modify src/: '+dirty);console.log('diagnosis complete, src untouched')"</automated>
  </verify>
  <done>One named root cause with a file:line break point, five rejected hypotheses each carrying its rejecting evidence, and zero source changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar/backend → renderer | `progressUpdate` crosses the push-channel transport this diagnosis is testing |
| operator report → recorded evidence | A remembered or inferred answer is indistinguishable in the file from a measured one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-29 | Repudiation | recording an inferred answer as a measured one | mitigate | Q1/Q2/Q3 each accept only `YES`/`NO`/`NOT ATTEMPTED`, every `NOT ATTEMPTED` requires a stated reason, and the automated check rejects blanks and `TBD` |
| T-34.10-30 | Information disclosure | a game/library title appearing in the diagnosis file | accept | Titles are already present throughout `.planning/`; no credential, token or account id is requested by any of the three questions |
| T-34.10-31 | Tampering | a concurrent second app instance producing a half-observed run | mitigate | The single-process `pgrep` precondition is recorded in the file, per this project's F-34.4.2-15/-18 lesson |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists in this plan |
</threat_model>

<verification>
- `34.10-F02-DIAGNOSIS.md` exists with a filled live-measurement table and a six-row hypothesis table
- Exactly one hypothesis survives, with a `path:line` break point
- `git status --porcelain src` is empty — this plan diagnoses, it does not fix
- `.planning/phases/.../34.10-LIVE-GATE.md` is untouched
</verification>

<success_criteria>
- The F-34.10-02 cause (b) hypothesis field is collapsed from six candidates to one, using a live
  measurement taken before any code was written
- The gate author's suggested cause (a keying/remount difference) is explicitly recorded as falsified,
  with the `git show 0559bc0d0^` evidence
- Plan 34.10-15 has a concrete, named fix target rather than a direction
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-12-SUMMARY.md` when done
</output>
