---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 10
subsystem: testing
tags: [i18n, jest, gate-scope, translation, meta]

# Dependency graph
requires:
  - phase: 34.10-09
    provides: "The retired Sidebar tree deleted (11 files, SidebarTour.tsx preserved) and the two new NavShell-scoped test gates -- the exact stale-scope condition this plan closes"
provides:
  - "meta/i18nGateScope.json regenerated: 136 -> 144 files, all 11 non-test NavShell source files registered, 4 deleted Sidebar paths removed, SidebarTour.tsx preserved"
  - "nav.tabs.games English catalog key, synced without touching unrelated upstream catalog drift"
  - "Proof (not assumption) that the blocking hardcoded-string gate genuinely scanned this phase's new files: scannedFiles(144) verified to match the committed snapshot exactly"
affects: [34.10-11, verifier]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Targeted git checkout to isolate one intended catalog addition from pnpm i18n's incidental re-sort/re-pluralize churn on unrelated pre-existing keys, rather than hand-editing the extractor's own output"

key-files:
  created: []
  modified:
    - public/locales/en/translation.json
    - meta/i18nGateScope.json

key-decisions:
  - "pnpm i18n's run touched translation.json, gamepage.json, and login.json far beyond the single intended key (re-sorted whitespace/plural-key entries, new steam_logout_failed_* and redeemSteamKey.* keys with no source relationship to this plan). Per the 34.8-I18N-CONTRACT's documented pre-existing-drift caveat, gamepage.json and login.json were reverted to HEAD entirely via targeted `git checkout`, and translation.json was reverted then hand-edited to add only the nav.tabs.games key -- never hand-editing the extractor's actual output, never touching the unrelated drift."
  - "The plan's own acceptance criterion `grep -c \"nav.tabs.games\" public/locales/en/translation.json` returns 1 is unsatisfiable as a literal flat-string grep given the catalog's established convention: i18next-parser.config.js sets keySeparator: '.', so every dotted key (including every other nested key already in this file, e.g. message.sync, setting.adddesktopshortcuts) nests as JSON objects, never as a literal dotted string. Verified correctness instead via `JSON.parse(...).nav.tabs.games === 'Games'` and via pnpm i18n's own extractor output (which independently produced the identical nested shape before being reverted for the unrelated-churn reasons above)."
  - "DECISION (orchestrator, 2026-08-08, verified independently before being given): finalize this plan now treating the one remaining hardcodedStringGate.test.ts failure as an external blocker out of phase 34.10's scope, rather than holding the plan open pending a concurrent session. Confirmed via diff that src/frontend/screens/WebView/index.tsx was ALREADY in the gate scope snapshot before this plan's regeneration (present in both the 136-entry pre-regen and 144-entry post-regen files) -- this plan's own regeneration added zero concurrent-session files, only NavShell files (+ Header/index.tsx, already committed by plan 34.10-06) and the 4 deleted Sidebar paths. The violating string itself is confirmed absent from HEAD (git diff shows it only in the uncommitted working tree), so it is entirely the concurrent Humble debug session's WIP, not anything this plan touched, produced, or is capable of committing to per its explicit isolation constraints (no allowlist widening, no editing that file)."

requirements-completed: [REQ-34.10-14, REQ-34.10-15]

# Metrics
duration: ~25min
completed: 2026-08-08
---

# Phase 34.10 Plan 10: i18n gate scope regeneration + translation key sync Summary

**Regenerated the committed i18n gate scope snapshot (136 -> 144 files) to register all 11 new NavShell source files and drop the 4 deleted Sidebar paths, synced the one new `nav.tabs.games` catalog key without touching unrelated upstream drift, and proved via the gate's own `scannedFiles` self-check that the blocking hardcoded-string gate genuinely scanned this phase's code rather than a stale snapshot.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-08-08T09:00:00+12:00 (approx.)
- **Completed:** 2026-08-08T09:35:00+12:00
- **Tasks:** 2 completed
- **Files modified:** 2 (`public/locales/en/translation.json`, `meta/i18nGateScope.json`)

## Accomplishments

- **Task 1:** Ran `pnpm i18n` and kept only the single intended addition. The run also churned `gamepage.json`, `login.json`, and unrelated parts of `translation.json` on pre-existing upstream drift (re-sorted plural-key entries, new `steam_logout_failed_*`/`redeemSteamKey.*` keys with no relation to this phase) -- exactly the pre-existing-drift caveat `34.8-I18N-CONTRACT.md` documents. Reverted `gamepage.json`/`login.json` entirely via targeted `git checkout`, reverted `translation.json` then hand-added only `"nav": { "tabs": { "games": "Games" } }` (alphabetically positioned between `message` and `next`, matching the extractor's own output before revert). `i18next-parser.config.js`'s `keepRemoved: true` confirmed unchanged throughout -- no run was attempted with it false. `git diff --name-only public/locales` shows only `translation.json`. `pnpm codecheck` (tsc) and `pnpm lint-translations` both exit 0.
- **Task 2:** Ran `pnpm gen-i18n-gate-scope`. Snapshot grew 136 -> 144 files. Diffed old vs. new file lists directly: **added** all 11 non-test `.ts`/`.tsx` files under `NavShell/` (`Tier2PortalContext.tsx`, `components/{DownloadsRing,GamesPanel,HeroicVersion,NavItem,NavTabs,QuitButton,SettingsPanel,StoresPanel}/index.tsx`, `index.tsx`, `navTabs.ts`) plus `Header/index.tsx` (already committed by plan 34.10-06, correctly picked up by the merge-base diff, unrelated to any concurrent session); **removed** the 4 now-deleted Sidebar paths (`HeroicVersion/index.tsx`, `SidebarItem/index.tsx`, `SidebarLinks/index.tsx`, `Sidebar/index.tsx`). `grep -c "Sidebar/components/SidebarTour.tsx"` = 1 (preserved). `baseCommit` (`b5b5cad3fa2e822602d320b70788d87240fc056e`) verified to equal `package.json`'s `upstream.baseCommit`. `git diff --exit-code meta/i18nGateAllowlist.json` succeeds (byte-unchanged).
- **Full-suite verification, run as `pnpm jest --runInBand --silent --forceExit` per this plan's baseline note:** went from the inherited **2 failing suites / 5 failing tests** to **1 failing suite / 1 failing test**. Specifically fixed: `genI18nGateScope.test.ts` (1 test, now fully green 9/9) and 3 of `hardcodedStringGate.test.ts`'s 4 failures -- those 4 were previously blocked by a `ScopeLoadError` on the stale snapshot's dangling paths, which masked whether the gate had ever actually completed a real scan. With the regenerated scope, 123/124 of `hardcodedStringGate.test.ts`'s tests now pass, including its own meta-assertion `scannedFiles matches the committed scope snapshot exactly` (144) -- direct proof, not assumption, that the gate scanned every file this phase registered.
- **The 1 remaining failure is a confirmed external blocker, not phase 34.10's to fix:** `hardcodedStringGate.test.ts`'s top-level "scans the whole committed scope and finds zero violations" assertion reports one violation at `src/frontend/screens/WebView/index.tsx:347` (`'the Humble sign-in window closed or could not be reached'`). Verified via `git diff` that this string exists **only in the uncommitted working tree** -- it is not present at HEAD. `src/frontend/screens/WebView/index.tsx` was already present in the gate scope snapshot before this plan ran (confirmed by diffing the pre- and post-regeneration file lists: identical entry, zero delta), so this plan's regeneration did not introduce it or any other concurrent-session path into scope. The violation is entirely attributable to the concurrent Humble debug session's uncommitted work (`.planning/debug/humble-isloggedin-never-set.md`, F-34.4.2-19), which this plan is explicitly forbidden from touching, reverting, or silencing via the allowlist (T-34.10-25).

## Task Commits

1. **Task 1: Sync `nav.tabs.games` translation key** - `3202f2ed6` (feat)
2. **Task 2: Regenerate the i18n gate scope** - `f7fe85ad4` (feat)
3. **[Interstitial] Record scope-regen resolution + new blocker in STATE.md** - `86a784639` (docs)

**Plan metadata:** (this commit, following SUMMARY creation)

## Files Created/Modified

- `public/locales/en/translation.json` -- added `nav.tabs.games: "Games"` only; all other extractor churn reverted as pre-existing drift
- `meta/i18nGateScope.json` -- regenerated: 144 files (was 136), +11 NavShell files, +1 already-committed Header/index.tsx, -4 deleted Sidebar paths

## Decisions Made

See `key-decisions` in the frontmatter: (1) reverted unrelated `pnpm i18n` churn per the 34.8 contract rather than hand-editing the extractor's output or the unrelated catalogs; (2) the plan's literal `grep -c "nav.tabs.games"` acceptance criterion is unsatisfiable given the catalog's established nested-key convention, verified via JSON path instead; (3) orchestrator decision to finalize this plan now with the one remaining gate failure recorded as an external blocker, independently confirmed that this plan's own regeneration introduced zero concurrent-session scope entries.

## Deviations from Plan

### Auto-fixed Issues

None -- both tasks' actions matched the plan as written. The `git checkout` revert of `gamepage.json`/`login.json`/parts of `translation.json` is not a deviation; it is the plan's own `<action>` text for Task 1, explicitly anticipating and prescribing this exact recovery for pre-existing catalog drift.

---

**Total deviations:** 0 auto-fixed.
**Impact on plan:** No scope change. Both tasks' `<action>`/acceptance criteria are satisfied as written, with two narrowing clarifications documented above (the literal `grep` criterion vs. the catalog's nested convention; confirming zero concurrent-session leakage into the regenerated scope) and one orchestrator-directed finalization decision.

## Issues Encountered

- **Full-suite verification surfaced a real violation outside this plan's own scope.** After Task 2's regeneration, `pnpm jest --runInBand --silent --forceExit` reported 1 failing suite / 1 failing test (`hardcodedStringGate.test.ts`) rather than fully green. Root-caused to a hardcoded string in `src/frontend/screens/WebView/index.tsx:347`, confirmed via `git diff` to be uncommitted WIP from the concurrent Humble debug session (F-34.4.2-19), not anything committed or touched by phase 34.10, and confirmed via a before/after scope-file diff that this plan's own regeneration did not pull that file into scope (it was already present, unchanged, in the pre-regeneration 136-entry snapshot). Per explicit isolation constraints (no editing/reverting concurrent-session files, no allowlist widening), this plan does not fix it. **Executor initially treated this as a blocking checkpoint and stopped for a decision; the orchestrator, after independently verifying the zero-concurrent-session-leakage claim, directed finalizing the plan with this recorded as a known external blocker.** No source, test, or config file belonging to phase 34.10 requires further changes to close this out -- once the concurrent session commits (or itself wraps that string in `t()`), the suite will read fully green with no further action from this plan.

## User Setup Required

None -- no external service configuration required.

## Next Phase Readiness

- `meta/i18nGateScope.json` is current and correct for the entire NavShell surface; plan 34.10-11 (this phase's final plan) can proceed without re-touching the scope snapshot.
- **Known external blocker carried forward, not owned by 34.10-11 either:** `hardcodedStringGate.test.ts` will continue to report exactly 1 violation at `src/frontend/screens/WebView/index.tsx:347` until the concurrent Humble debug session (`.planning/debug/humble-isloggedin-never-set.md`, F-34.4.2-19) commits its own fix. The phase verifier should treat this as a pre-existing external condition, not a phase 34.10 regression -- the gate scope and allowlist are both correct and byte-verified as of this plan.
- No other blocker from this plan's own scope.

---
*Phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar*
*Completed: 2026-08-08*

## Self-Check: PASSED

`public/locales/en/translation.json` verified to contain `nav.tabs.games: "Games"` via `JSON.parse`.
`meta/i18nGateScope.json` verified to contain all 11 NavShell source files, exclude the 4 deleted
Sidebar paths, and retain `SidebarTour.tsx`. All 3 commits (`3202f2ed6`, `f7fe85ad4`, `86a784639`)
verified present in `git log --oneline --all`.
