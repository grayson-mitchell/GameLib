---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 15
type: execute
wave: 2
depends_on: ["34.10-12", "34.10-14"]
files_modified:
  - src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx
autonomous: true
gap_closure: true
requirements: [REQ-34.10-08]

must_haves:
  truths:
    - "The ring's --dl-progress tracks the real percent of the download that is actually running"
    - "The fix implements the single surviving hypothesis from 34.10-F02-DIAGNOSIS.md, not a guess"
    - "A regression test exercises RingProgress itself, not only the outer DownloadsRing, so the arc's data path is covered for the first time"
    - "No percent is synthesised, averaged, or derived from a substitute source to make the arc move"
  artifacts:
    - path: "src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx"
      provides: "The corrected progress binding for the ring's arc"
      contains: "dl-progress"
    - path: "src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx"
      provides: "A RingProgress-level regression test covering the diagnosed break"
      contains: "RingProgress"
  key_links:
    - from: "src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx"
      to: "src/frontend/hooks/hasProgress.ts"
      via: "the appName/runner pair the ring binds the arc to"
      pattern: "hasProgress"
---

<objective>
Close F-34.10-02 cause (b): make `--dl-progress` track a real percent during a real download, by
implementing the single surviving root cause recorded in `34.10-F02-DIAGNOSIS.md`.

Purpose: the arc never left `0turn` against a live queue while the count badge — reading the same
subscription — was correct. Plan 34.10-12 collapsed six candidate causes to one using a live
discriminator taken before any code was written. This plan implements that one.
Output: a correctly-bound progress arc plus the first test in this repo that exercises `RingProgress`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-12-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-14-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md

<interfaces>
<!-- The component as it stands. RingProgress is an inner function component; the existing test file
     never invokes it, which is why the arc's data path has zero coverage today. -->
  function RingProgress({ appName, runner }: { appName: string; runner: Runner }) {
    const [progress] = hasProgress(appName, runner)
    const percent = progress.percent ?? 0
    return <span className="DownloadsRing__ring"
                 style={{ '--dl-progress': `${percent / 100}turn` } as CSSProperties} />
  }

  export default function DownloadsRing() {
    const [elements, setElements] = useState<DMQueueElement[]>([])   // from getDMQueueInformation
    const head = elements[0]                                          // <- the arc's binding
    const isIdle = elements.length === 0
    const isActive = pathname.startsWith('/download-manager')
    ...
    {head ? <RingProgress key={head.params.appName} appName={head.params.appName}
                          runner={head.params.runner} />
          : <span className="DownloadsRing__ring" style={{'--dl-progress':'0turn'}} />}
    {elements.length > 0 && <span className="DownloadsRing__count">{elements.length}</span>}
  }

<!-- The hook, unchanged -->
  src/frontend/hooks/hasProgress.ts:38
    useInstallProgress((state) => state[`${appName}_${runner}`])
  seeds currentProgress from localStorage[`${appName}_${runner}_progress`] via JSON.parse(... || '{}')

<!-- ALREADY FALSIFIED at planning time — do not "fix" these -->
  The retired `Sidebar/index.tsx` used the SAME `elements[0]` source and the SAME
  `key={currentDMElement.params.appName}` (`git show 0559bc0d0^:src/frontend/components/UI/Sidebar/index.tsx`).
  The keying/remount theory the live gate suggested is dead. Do not reintroduce it.

<!-- Existing test coverage and its blind spot -->
  DownloadsRing.test.tsx mocks `frontend/hooks/hasProgress` wholesale and asserts only on the OUTER
  component's element graph (idle modifier, count badge, key, aria-label, aria-current, cleanup).
  `RingProgress` is never called, so `--dl-progress` has never been computed by any test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement the diagnosed fix for the arc's data path</name>
  <files>src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md (§ Surviving hypothesis — the file:line break point and the stated fix; this task implements THAT, not a re-derivation)
    - src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx (all 87 lines)
    - src/frontend/hooks/hasProgress.ts (all 59 lines)
    - src/frontend/state/InstallProgress.ts (the store and its key shape)
    - common/types (the `DMQueueElement` shape, specifically `params.appName` / `params.runner` and any status field that distinguishes a running element from a merely queued one)
  </read_first>
  <action>
    Implement the fix named in `34.10-F02-DIAGNOSIS.md`'s `## Surviving hypothesis` section. That
    document is the authority for WHAT to change; this action constrains HOW.

    Hard constraints, all of which come from D-06, REQ-34.10-08 or this project's recorded lessons:

    - **Do not invent an aggregate progress source.** D-06 rejects it explicitly: per-item sizes vary
      enough that an aggregate moves unevenly and sometimes backwards. The arc shows one download's
      percent.
    - **Do not synthesise, interpolate, smooth or floor the percent** to make the arc appear to move.
      If the real value is 0, the arc stays at `0turn`.
    - **Do not switch to a different data source** (a timer, a byte count, a status string) to route
      around the diagnosed break. The percent comes from `hasProgress(appName, runner)`, per
      REQ-34.10-08's own text.
    - **Keep the count badge's data path untouched.** It works; it is the control in this experiment.
    - **Keep `--dl-progress` as the transport** into the stylesheet, and keep the property written on
      `.DownloadsRing__ring`. Plan 34.10-14's `conic-gradient` reads it there.
    - **Preserve the idle branch's always-mounted `.DownloadsRing__ring` span.** D-07 requires the
      slot be held at all times; a `return null` anywhere in this component is a regression.

    If the diagnosis's surviving cause is that the arc binds to the wrong queue element (i.e.
    `elements[0]` is not the element actually downloading), select the running element explicitly from
    the queue rather than by index, and state in the SUMMARY exactly which field of `DMQueueElement`
    identifies it. Keep the `key` on `RingProgress` equal to whichever element the arc binds to, so
    per-download progress state still resets on handover — that behaviour is named in REQ-34.10-08
    and must survive.

    If the diagnosis's surviving cause is `F-34.10-03` (a break outside `src/frontend`), do not
    attempt a frontend workaround. Record in the SUMMARY that this task is NOT ATTEMPTED, state why,
    and leave the component unchanged so plan 34.10-16's gate measures the honest state rather than a
    cosmetic patch. A phase that does not close is a better outcome than one that closes on a
    disguised defect.

    Stage with explicit per-file `git add` paths only — never `git add -A`, `git add .` or
    `git commit -a`. Another session is editing this repository;
    `src-tauri/src/main.rs`, `src/backend/humble/*`, `src/frontend/screens/WebView/*`,
    `.vscode/settings.json`, `scratchpad/`, `steam_appid.txt` and `.planning/phases/34.7-*` are
    off-limits.
  </action>
  <acceptance_criteria>
    - The change implements the hypothesis recorded as `SURVIVES` in `34.10-F02-DIAGNOSIS.md`, and the SUMMARY names that hypothesis by its H-number
    - `hasProgress` is still the sole source of the arc's percent — no timer, byte-count, status-string or aggregate substitute appears in the component
    - The component contains no `return null` on any path (D-07's always-mounted slot)
    - `--dl-progress` is still written on the element carrying `className="DownloadsRing__ring"`
    - The count badge's `elements.length` binding is byte-identical to before
    - `pnpm codecheck` exits 0
    - Whether the arc actually fills against a real download CANNOT be proven here: there is no jsdom, no live IPC and no download queue in this jest project. That proof is routed to plan 34.10-16's gate item 4.
  </acceptance_criteria>
  <verify>
    <automated>pnpm codecheck && node -e "const fs=require('fs');const p='src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx';const s=fs.readFileSync(p,'utf8');if(!/hasProgress\(/.test(s))throw new Error('hasProgress no longer sourced');if(/return null/.test(s))throw new Error('D-07 violated: a return null path exists');if(!/--dl-progress/.test(s))throw new Error('--dl-progress transport removed');if(!/elements\.length/.test(s))throw new Error('count badge binding changed');console.log('arc binding fix shape ok')"</automated>
  </verify>
  <done>The arc's percent comes from `hasProgress` applied to the element the diagnosis identified, with the always-mounted idle slot, the count badge path and the `--dl-progress` transport all intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cover RingProgress itself, closing the hole that let this ship</name>
  <files>src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx (all of it — the hand-rolled useState/useEffect harness, `collectElements`, `makeElement`, and the wholesale `jest.mock('frontend/hooks/hasProgress')` that is the blind spot being closed)
    - src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx (task 1's output)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F02-DIAGNOSIS.md (the surviving hypothesis — the regression test must fail against the pre-fix behaviour it describes)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VALIDATION.md (§ Test Infrastructure — the no-jsdom constraint that bounds what may be asserted)
  </read_first>
  <action>
    Extend `DownloadsRing.test.tsx` with a new `describe('RingProgress arc binding (F-34.10-02)')`
    block. Do not rewrite the existing describe block; its eight tests all still apply.

    Export `RingProgress` from the component module as a named export so it can be invoked directly
    — it is currently module-private, which is the mechanical reason no test has ever computed
    `--dl-progress`. Keep `DownloadsRing` as the default export.

    In the new block, drive the mocked `hasProgress` per test rather than through the file-level
    `progressPercent` variable alone, and assert:
    - invoking `RingProgress({ appName: 'x', runner: 'legendary' })` with a mocked percent of 42
      returns an element whose `style['--dl-progress']` is `'0.42turn'`
    - with a percent of 0 it is `'0turn'`, and with 100 it is `'1turn'`
    - with `percent` absent (`undefined`) it is `'0turn'` and does not throw — the `?? 0` path
    - the returned element's `className` is exactly `DownloadsRing__ring`, matching the selector
      plan 34.10-14's stylesheet targets
    - a regression assertion aimed squarely at the diagnosed cause: construct the queue shape that
      `34.10-F02-DIAGNOSIS.md` identifies as the break (for example, a queue whose running element is
      not at index 0, if that is the surviving hypothesis) and assert that the element `RingProgress`
      is invoked with is the one carrying real progress. Write this assertion so it FAILS against the
      pre-fix code — if it would have passed before task 1, it is not a regression test and must be
      rewritten. Record in the SUMMARY that you confirmed this, and how.

    Assert nothing about colour, contrast, visibility or geometry. This jest project is
    `testEnvironment: 'node'` with no jsdom; this phase already shipped ~120 green structural tests
    while two user-visible features were broken, and a test that appears to prove the arc is visible
    would repeat exactly that. Add a comment in the new describe block naming plan 34.10-16 item 4 as
    where visual proof lives.

    Run the full suite once (`pnpm jest --runInBand --silent --forceExit`). Expect **exactly one**
    pre-existing failure: `meta/__tests__/hardcodedStringGate.test.ts` on
    `src/frontend/screens/WebView/index.tsx:347`, from a concurrent session's uncommitted tree. Do not
    fix it, allowlist it, or attribute it to phase 34.10. More than one failure is a real regression
    and must stop the plan.

    Stage with explicit per-file `git add` paths only.
  </action>
  <acceptance_criteria>
    - `RingProgress` is a named export of the component module and `DownloadsRing` remains the default export
    - The new describe block asserts `--dl-progress` values for percents 42, 0, 100 and `undefined`
    - The regression assertion fails when run against the pre-fix component — verified by the executor and stated in the SUMMARY with the method used (for example `git stash` of task 1's change, or a temporary local revert)
    - The eight pre-existing tests in this file still pass unmodified
    - No assertion in the new block mentions colour, contrast, opacity or pixel geometry
    - `pnpm jest --runInBand --silent --forceExit` reports exactly one failing suite and it is `meta/__tests__/hardcodedStringGate.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest --runInBand --silent --forceExit src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx src/frontend/components/UI/NavShell/__tests__/downloadsRingStyles.test.ts && pnpm codecheck</automated>
  </verify>
  <done>`RingProgress` is directly covered for the first time, `--dl-progress` is asserted across four percent inputs, and a regression test that provably failed before the fix now passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar/backend → renderer | `progressUpdate` crosses the push-channel transport that feeds the arc |
| DM queue ordering → arc binding | The queue's element order is backend-controlled; the arc must not assume index 0 is the running element unless the diagnosis proves it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-39 | Repudiation | a cosmetic patch making the arc move without the real percent | mitigate | Task 1 forbids timers, byte counts, status strings, aggregates and interpolation by name, and the automated check asserts `hasProgress(` is still the source |
| T-34.10-40 | Repudiation | a regression test that would have passed before the fix | mitigate | Task 2 requires the executor to run the new assertion against the pre-fix code and record the method in the SUMMARY |
| T-34.10-41 | Denial of service | reintroducing a `return null` path and breaking D-07's always-mounted slot | mitigate | Automated check rejects any `return null` in the component |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists in this plan |
</threat_model>

<verification>
- `pnpm codecheck` exits 0
- `DownloadsRing.test.tsx` and `downloadsRingStyles.test.ts` both pass
- `pnpm jest --runInBand --silent --forceExit` shows exactly the one known pre-existing failure
- The SUMMARY names the implemented hypothesis by H-number and records how the regression assertion was proven to fail pre-fix
- The arc filling against a real download is NOT proven here — routed to plan 34.10-16 gate item 4
</verification>

<success_criteria>
- The arc's percent is bound to the download that is actually running, per the diagnosis
- No substitute data source, aggregate or synthetic value was introduced
- `RingProgress` has test coverage for the first time since it shipped
- If the surviving cause proved to be outside `src/frontend`, that is recorded honestly as F-34.10-03 and the component was left alone rather than patched cosmetically
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-15-SUMMARY.md` when done
</output>
