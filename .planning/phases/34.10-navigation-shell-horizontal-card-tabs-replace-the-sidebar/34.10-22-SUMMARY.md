---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 22
status: complete
gap_closure: true
completed: 2026-08-08
requirements: [REQ-34.10-04, REQ-34.10-05, REQ-34.10-06, REQ-34.10-08, REQ-34.10-09, REQ-34.10-16]
outcome: "Live gate run 3 executed and recorded: 4/5. Phase 34.10 does NOT close."
key-files:
  modified:
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
    - .planning/REQUIREMENTS.md
commits:
  - "8abca9f2c test(34.10-22): record live gate run 3 — 4/5, item 1 FAIL, phase stays open"
  - "111a3abd7 docs(34.10-22): reconcile REQUIREMENTS.md to live gate run 3"
---

# Plan 34.10-22 — Run the run-3 live gate and record what happened

## Outcome

**4/5. Items 2, 3, 4 and 5 PASS; item 1 FAILS. Phase 34.10 does not close.**

The phase closes only on 5/5. 4/5 has now failed to close it twice.

| Item | Requirement | Run 2 | Run 3 |
|---|---|---|---|
| 1 — Theme survival / seam / scroll | REQ-34.10-06 | FAIL | **FAIL** |
| 2 — Window dragging under WKWebView | REQ-34.10-04 | PASS | **PASS** |
| 3 — Tier-2 portal + F-34.10-05 | REQ-34.10-09 | FAIL | **PASS** |
| 4 — Ambient Downloads ring | REQ-34.10-08 | PASS | **PASS** |
| 5 — Electron divergence | REQ-34.10-05 | PASS | **PASS** |

## What was built

Nothing. This plan is a measurement, per decision D-E: the plan that authors a gate contract may
never also run it. 34.10-21 authored §9 and filled nothing; this plan filled it.

## Three columns measured for the first time in this phase

All three had been outstanding since run 1 and were the specific losses run 3's contract existed
to prevent:

1. **Item 1's four-theme sweep.** All eight cells resolved — three scored themes with a distinct
   seam and idle-ring verdict each, and `dracula-classic` NOT ATTEMPTED for a concrete reason.
2. **Item 3's sub-checks (vi) and (vii)** — NOT ATTEMPTED in both prior runs. "Still reachable"
   was never allowed to stand in for "already measured."
3. **A fully recorded preflight, P1-P9.** Run 2's block was left entirely blank and its own
   recorder flagged that as a capture gap.

## Findings closed by measurement

- **F-34.10-05** (34.10-20) — disclosure panel background correct in `gruvbox_dark`, `dracula`,
  and a third theme (`cyberspace oasis`) the operator volunteered. The third theme carries weight:
  `midnightMirage` was excluded from scoring because `--background` and `--navbar-background`
  coincide there, so three independent themes passing is what rules out a fix that only holds
  where those tokens agree.
- **F-34.10-06** (34.10-18) — navbar stayed pinned through a full scroll; scrollbar sat below it
  throughout; the mouse/keyboard back-to-top consumer migration held. This is the strongest result
  in the run: relocating the scroll container off `document.body` was the largest and riskiest
  change of the gap cycle, it fails silently by construction, and no test on this project can
  observe it.

## Findings left open, because they were measured failing

- **F-34.10-03** — operator: *"still a gap"*. 34.10-18's seam border did not work.
- **F-34.10-04** — operator: *"gamelib icon and download sit lower"*. 34.10-19's `min-height` did
  not work. The failure **shape changed**, from run 2's full wrap onto a second row to a vertical
  misalignment. Recorded as an observation with no cause assigned — a cause guessed into a gate
  becomes the next plan's falsified premise.

Both were measured against a bundle verified **before scoring** to contain their fixes.

## Two things the run caught that the verdict table does not show

1. **A pre-fix bundle.** The preflight found the app was serving a stale build — `frontendDist`
   points at a static directory, so a bare `tauri dev` serves whatever `electron-vite build` last
   wrote, and `build/` predated all five fixes. Scoring against it would have recorded FAIL on all
   four findings and attributed it to fixes the running app never contained. Caught before any
   item was scored; the operator relaunched via full `pnpm tauri:dev`.
2. **Six unsatisfiable checks in run 3's own contract**, minted as **F-34.10-07** (process, not
   product). §9.1 scoped a `dracula-classic` theme that does not exist in the picker — a Test 7
   (UI-level reachability) miss. The other five are a **new defect class**, verification/prose
   collisions: the checks grep §9 for the pending-placeholder literal and so trip on the prose
   describing it; reject any `[ ]` in a preflight block whose own multiple-choice format guarantees
   one; count verdict cells by the bare literal `| PASS |`, so Markdown emphasis hid 32 resolved
   cells as 4; scan a fixed 600-character window for each finding's status, which verbose
   summaries overflow; and locate findings by `indexOf`, which matches cross-references earlier in
   the frontmatter. All resolved without altering a value, with a `js-yaml` parse before and after
   proving the reordering changed nothing. **Rule for the next contract: run a gate check against
   a filled specimen at authoring time, not only against the empty template.**

## Carried forward as unmeasured

- **Gamepad focus-scroll regression** — no controller available (P9). Never inferred from the
  mouse/keyboard result: different code path, different container-rect-relative arithmetic, and
  it is exactly the path 34.10-18's relocation affects.
- **`dracula-classic`** — theme absent from the shipped picker.

## Operator design decision recorded during the run

The developer elected to **abandon the card/folder tab for a pill tab with rounded bottom
corners** — *"i going to drop the tab and let make a pill, that what it looks like anyway! just
needs rounded bottom corners."*

Recorded as a decision, **not** as a closure. F-34.10-03 and F-34.10-04 keep their FAIL verdicts
as measured against the contract as written. It does mean the next cycle is a design change rather
than a third attempt at seam erasure — defensible on this run's own evidence, since the card tab's
whole premise is a merge with the content surface that has now failed twice against two different
fixes, whereas a pill has no seam to erase. Scoping it belongs to a later plan.

## Requirements reconciled

| Requirement | Before | After | Basis |
|---|---|---|---|
| REQ-34.10-04 | Complete | Complete | Item 2 PASS, re-measured not assumed |
| REQ-34.10-05 | Complete | Complete | Item 5 PASS |
| REQ-34.10-06 | Pending | **Pending** | Item 1 FAIL |
| REQ-34.10-08 | Complete | Complete | Item 4 PASS, both gating conditions checked |
| REQ-34.10-09 | Pending | **Complete** | Item 3 PASS incl. (vi), (vii), F-34.10-05 |
| REQ-34.10-16 | Pending | **Pending** | Closes only at 5/5; run 3 scored 4/5 |

Nothing ticked from a test suite. `STATE.md` was not touched via `gsd-sdk query state.*` mutation
verbs, which corrupt the file on this project.

## Deviations

- **Resumed session.** Run 3 was already partly recorded in the working tree (P1, P2, and an
  escalated P3) from before a context reset. Rather than trust it, P3 and the bundle staleness were
  re-measured from scratch before any scoring.
- **P4-P8 answered as a batch.** Flagged in the gate file rather than smoothed over: they are
  yes/no preconditions, not scored cells, so a batch answer is legitimate — but the distinction
  from the per-theme cells (where batching is forbidden) is stated explicitly. P4's distinctive
  title was deferred and filled live at item 3(i) as `king` / *Kingdom Come*.
- **Item 5's P3 re-check ran after the launch, not before it.** The contract's sequencing was not
  achieved; recorded plainly. The check confirmed both probes empty, so the two shells never ran
  concurrently and the evidence is scorable.
- **Item 4 sub-check (vi) re-asked** rather than accepting an obvious operator typo, because it is
  the single cell discriminating a genuinely-tracking ring from the frozen-`0turn` failure that
  already shipped once in this phase behind a green suite.

## Self-Check: PASSED

All three tasks' automated verifications pass:

- Task 1 — `run 3 recorded: 32 resolved cells`
- Task 2 — `frontmatter reconciled: 4/5, verdict fail`
- Task 3 — `REQUIREMENTS.md reconciled against items_passed=4, 6 notes cite run 3`

`git diff --name-only` shows exactly the two declared paths. §0-§8 of the gate file are unmodified;
only §9 and the frontmatter changed. No user-facing string introduced.

## Next command

**`/gsd-debug`** — not `/gsd-plan-phase 34.10 --gaps`.

F-34.10-03 and F-34.10-04 have each survived a targeted fix that its author's reasoning and unit
gates both endorsed. That is an undiagnosed defect, not a known gap, and this phase's own record
shows what happens when a cause is assumed: 34.10-18's first seam fix was reasoned theme-safe and
was broken in 9 of 11 themes. The pill decision changes the eventual shape of the fix but does not
supply a diagnosis, and the run-3 evidence that the seam is **identical across all three themes**
is a real discriminator a debug session should start from.

Also owed, tracked so it does not evaporate:
- the pill-tab design change needs its own plan
- the gamepad focus-scroll path remains unmeasured
- **F-34.10-07** — `live-gate-contract-authoring.md` should gain the verification/prose collision
  class and the "execute the check against a filled specimen" rule
