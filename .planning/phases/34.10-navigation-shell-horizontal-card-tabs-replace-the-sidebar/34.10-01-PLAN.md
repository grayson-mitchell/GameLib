---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/components/UI/NavShell/navTabs.ts
  - src/frontend/components/UI/NavShell/Tier2PortalContext.tsx
  - src/frontend/components/UI/NavShell/components/GamesPanel/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/navTabs.test.ts
  - src/frontend/components/UI/NavShell/__tests__/GamesPanel.test.tsx
autonomous: true
requirements: [REQ-34.10-01, REQ-34.10-09, REQ-34.10-11]

must_haves:
  truths:
    - "Every route in the app's flat router table resolves to exactly one tier-1 tab, or to no tab for /download-manager (Downloads is ambient, not a destination)"
    - "The Stores tab's target is computed by the same default-store cascade the sidebar used, including the sessionStorage last-store override"
    - "A consumer rendered inside Library's LibraryContext provider can place UI into the shell's tier-2 column without leaving that provider's React tree"
    - "The Games tier-2 panel reports whether anything has been portalled into it, so the shell can collapse an empty panel instead of showing a 204px hole"
  artifacts:
    - path: "src/frontend/components/UI/NavShell/navTabs.ts"
      provides: "NavTabId union, tab index map, resolveActiveTab(), resolveDefaultStore()"
      exports: ["NAV_TAB_IDS", "NavTabId", "NAV_TAB_INDEX", "resolveActiveTab", "resolveDefaultStore"]
    - path: "src/frontend/components/UI/NavShell/Tier2PortalContext.tsx"
      provides: "Tier2PortalProvider + context carrying the portal target node and its filled flag"
      exports: ["Tier2PortalContext", "Tier2PortalProvider"]
    - path: "src/frontend/components/UI/NavShell/components/GamesPanel/index.tsx"
      provides: "The Games tier-2 portal target element"
  key_links:
    - from: "src/frontend/components/UI/NavShell/components/GamesPanel/index.tsx"
      to: "src/frontend/components/UI/NavShell/Tier2PortalContext.tsx"
      via: "useContext + callback ref registering the target node"
      pattern: "Tier2PortalContext"
---

<objective>
Create the three contracts every later plan in this phase builds against: tab identity
(`navTabs.ts`), the tier-2 portal seam (`Tier2PortalContext.tsx`), and the Games tier-2 portal
target (`GamesPanel`). Nothing is mounted yet — this plan is pure contract, so waves 2 and 3 can
build tabs, panels and the Library side against fixed signatures instead of exploring for them.

Purpose: the tier-1/tier-2 shell is assembled by five different plans. Without a shared tab-id
type and a shared portal seam they would each invent one.
Output: two contract modules, one portal-target component, and unit tests for the pure functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-CONTEXT.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-PATTERNS.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VALIDATION.md

<interfaces>
<!-- Contracts this plan CREATES. Later plans consume these exact names. -->

navTabs.ts:
  NAV_TAB_IDS: readonly ['accounts', 'games', 'stores', 'settings']
  type NavTabId = 'accounts' | 'games' | 'stores' | 'settings'
  NAV_TAB_INDEX: Record<NavTabId, number>   // accounts 0, games 1, stores 2, settings 3
  resolveActiveTab(pathname: string): NavTabId | null
  resolveDefaultStore(sessions: NavStoreSessions): string
  interface NavStoreSessions {
    epic: { username?: string }
    gog: { username?: string }
    amazon: { user_id?: string }
    steam: { username?: string }
    zoom: { username?: string; enabled: boolean }
  }

Tier2PortalContext.tsx:
  interface Tier2PortalValue {
    target: HTMLElement | null
    setTarget: (el: HTMLElement | null) => void
    filled: boolean
    setFilled: (filled: boolean) => void
  }
  Tier2PortalContext: React.Context<Tier2PortalValue>
  Tier2PortalProvider: React.FC<{ children: React.ReactNode }>

<!-- Existing contract this plan must match (source of the cascade being ported): -->
SidebarLinks/index.tsx:82-113 computes `defaultStore` then overrides it from
sessionStorage.getItem('last-store').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: navTabs contract — tab identity, route resolution, default-store cascade</name>
  <files>src/frontend/components/UI/NavShell/navTabs.ts, src/frontend/components/UI/NavShell/__tests__/navTabs.test.ts</files>
  <read_first>
    - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx (lines 45-113 — `inWebviewScreen`, `isSettings`, and the full `defaultStore` cascade being ported verbatim)
    - src/frontend/App.tsx (lines 160-260 — the `createHashRouter` table; every `path:` entry must resolve)
    - src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx (the repo's structural test idiom; this task's test is a plain-function test so it needs none of the hook mocking, but read it for the file layout convention)
  </read_first>
  <behavior>
    - resolveActiveTab('/login') and resolveActiveTab('/loginweb/steam') return 'accounts'
    - resolveActiveTab('/') and resolveActiveTab('/gamepage/legendary/abc') return 'games'
    - resolveActiveTab('/store/gog'), '/store-page', '/discounts', '/store-search', '/humble-keys', '/humble-keys/waiting' all return 'stores'
    - resolveActiveTab('/settings/general'), '/wine-manager', '/accessibility', '/console', '/wiki' all return 'settings'
    - resolveActiveTab('/download-manager') returns null — Downloads is ambient state, not a tab
    - resolveActiveTab of an unrouted path returns null
    - resolveDefaultStore returns 'epic' when nothing is logged in
    - resolveDefaultStore returns 'zoom' only when zoom.enabled and zoom.username and none of epic/gog/amazon are logged in
    - resolveDefaultStore returns 'amazon' when only amazon.user_id is set, 'gog' when gog.username is set and epic is not, 'steam' when only steam.username is set
    - a non-null sessionStorage 'last-store' value overrides every branch above
  </behavior>
  <action>
    Create `src/frontend/components/UI/NavShell/navTabs.ts` exporting `NAV_TAB_IDS`, the `NavTabId`
    union, `NAV_TAB_INDEX` (accounts 0, games 1, stores 2, settings 3 — this index feeds the
    `id="tab-{index}"` attribute that `components/UI/TabPanel/index.tsx:14`'s existing
    `aria-labelledby="tab-{index}"` contract expects), `resolveActiveTab(pathname)` and
    `resolveDefaultStore(sessions)` plus the `NavStoreSessions` interface, exactly as named in the
    `<interfaces>` block.

    `resolveActiveTab` returns `NavTabId | null`. Returning `null` for `/download-manager` is
    deliberate and is the whole reason the return type is nullable: per the settled design
    (`navigation-shell.md` §6) Downloads is ambient state with its own persistent indicator, not a
    destination, so the tab strip legitimately shows nothing lit there and the ring carries the
    active signal instead (plan 03 gives the ring `aria-current="page"` on that route). Match on:
    `/login` and any `/loginweb` prefix → accounts; exact `/` and any `/gamepage` prefix → games;
    any `/store/` segment, `store-page`, `last-url`, `/discounts`, `/store-search`, `/humble-keys`
    → stores; `/settings`, `/wine-manager`, `/accessibility`, `/console`, `/wiki` → settings;
    everything else including `/download-manager` → null. `last-url` appears as a `store/:store`
    param value in the code being retired, so the `/store/` prefix already covers it — keep an
    explicit `includes('last-url')` clause anyway to mirror `SidebarLinks/index.tsx:45-48`.

    `resolveDefaultStore` ports the cascade at `SidebarLinks/index.tsx:82-113` unchanged, in the
    same branch order, including the trailing `sessionStorage.getItem('last-store')` override that
    wins over every branch. Read `sessionStorage` inside the function, not at module scope, so a
    test can stub it (per REQ-34.10-11 the cascade carries over unchanged — do not "simplify" the
    Zoom branch even though ZOOM Platform is absent from the Stores tier-2 list; the branch decides
    a route target, not a rendered row).

    Write `__tests__/navTabs.test.ts` covering every `<behavior>` bullet. These are plain exported
    functions, so no hook mocking is needed — but stub `globalThis.sessionStorage` the way
    `SidebarLinks/__tests__/index.test.tsx:81-88` does. Do not use `render()`, `screen.getBy*` or
    `userEvent` anywhere: the `Frontend` jest project is `testEnvironment: 'node'` with no jsdom.
  </action>
  <acceptance_criteria>
    - `src/frontend/components/UI/NavShell/navTabs.ts` exports all five of `NAV_TAB_IDS`, `NavTabId`, `NAV_TAB_INDEX`, `resolveActiveTab`, `resolveDefaultStore`
    - `NAV_TAB_INDEX` maps accounts→0, games→1, stores→2, settings→3
    - `resolveActiveTab('/download-manager')` is `null`, asserted by a named test
    - Every `path:` value in `App.tsx`'s router table other than `download-manager` and `*` has a test asserting its resolved tab
    - The default-store test asserts the `last-store` override wins over an otherwise-'epic' result
    - `grep -n "sessionStorage" src/frontend/components/UI/NavShell/navTabs.ts` shows the read inside `resolveDefaultStore`, not at module scope
    - No occurrence of `render(`, `screen.` or `userEvent` in the new test file
  </acceptance_criteria>
  <verify>
    <automated>npx jest --config src/frontend/jest.config.js src/frontend/components/UI/NavShell/__tests__/navTabs.test.ts</automated>
  </verify>
  <done>navTabs.ts exports the full contract; every router path resolves to the documented tab or to null; the default-store cascade including the sessionStorage override is reproduced and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tier-2 portal seam + the Games portal-target panel</name>
  <files>src/frontend/components/UI/NavShell/Tier2PortalContext.tsx, src/frontend/components/UI/NavShell/components/GamesPanel/index.tsx, src/frontend/components/UI/NavShell/__tests__/GamesPanel.test.tsx</files>
  <read_first>
    - src/frontend/screens/Library/LibraryContext.tsx (the `initialContext` no-op setters — this is WHY a portal is required instead of moving `<Header>`)
    - src/frontend/components/UI/LibrarySearchBar/index.tsx (lines 1-30 — `useContext(LibraryContext)` consumption)
    - src/frontend/components/UI/CategoryFilter/index.tsx (lines 1-20 — same)
    - src/frontend/components/UI/LibraryFilters/index.tsx (lines 1-30 — same)
    - src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx (the structural direct-invoke test idiom to copy)
  </read_first>
  <behavior>
    - Tier2PortalContext's default value has `target: null`, `filled: false` and no-throw setters
    - GamesPanel returns an element carrying the className `NavShell__tier2Portal` and a ref callback
    - GamesPanel's ref callback forwards the node to the context's `setTarget`
    - GamesPanel renders no children of its own — it is a target, not a panel with content
  </behavior>
  <action>
    Create `src/frontend/components/UI/NavShell/Tier2PortalContext.tsx` exporting
    `Tier2PortalContext` and `Tier2PortalProvider` with the `Tier2PortalValue` shape in the
    `<interfaces>` block. The provider holds `target` and `filled` in `useState` and memoises the
    value object. `setTarget` is passed as a callback ref by `GamesPanel`, so React calls it with
    the node on mount and with `null` on unmount — no `document.getElementById` polling anywhere.

    Document at the top of the file, in a comment, the reason this seam exists, because it is not
    obvious and a future reader will otherwise "simplify" it into a component move: per REQ-34.10-09,
    `LibrarySearchBar`, `CategoryFilter` and `LibraryFilters` all call
    `useContext(LibraryContext)`, and `LibraryContext.tsx`'s `initialContext` supplies a full set of
    silent no-op setters — so rendering `<Header>` in the app-level shell instead of inside
    `Library`'s provider produces filter controls that look correct and do nothing, with no error,
    no warning and no failing test. React context follows the React tree, not the DOM tree, so a
    portal keeps the controls inside `Library`'s provider while placing them in the shell's tier-2
    column.

    Create `src/frontend/components/UI/NavShell/components/GamesPanel/index.tsx`: a component that
    consumes `Tier2PortalContext` and returns a single `div` with className
    `NavShell__tier2Portal` whose `ref` is the context's `setTarget`. It renders no children.
    Plan 06 supplies the content from the `Library` side; plan 07's shell reads `filled` to decide
    whether to collapse the column.

    Write `__tests__/GamesPanel.test.tsx` using the structural idiom: mock `react`'s `useContext`
    to return a stub context value with a jest.fn `setTarget`, call `GamesPanel()` as a plain
    function, and assert on the returned element's `props.className` and that `props.ref` is the
    stubbed `setTarget`. No DOM, no `render()`.
  </action>
  <acceptance_criteria>
    - `Tier2PortalContext.tsx` exports both `Tier2PortalContext` and `Tier2PortalProvider`
    - The file contains a comment naming `LibraryContext`'s no-op `initialContext` as the reason the portal exists
    - `GamesPanel()` returns an element whose `props.className` is exactly `NavShell__tier2Portal`
    - `GamesPanel()`'s returned element has `props.ref` identical to the context's `setTarget`
    - `GamesPanel()`'s returned element has no `children` prop
    - No occurrence of `render(`, `screen.` or `userEvent` in the new test file
  </acceptance_criteria>
  <verify>
    <automated>npx jest --config src/frontend/jest.config.js src/frontend/components/UI/NavShell/__tests__/GamesPanel.test.tsx</automated>
  </verify>
  <done>The portal seam exists with its rationale recorded in-source, and the Games tier-2 target registers itself with the context via a callback ref.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route pathname → tab resolution | `location.pathname` is app-internal (hash router), not user-supplied network input; no untrusted data crosses here |
| sessionStorage `last-store` → route target | A value read from the renderer's own sessionStorage is interpolated into a client-side route path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-01 | Tampering | `resolveDefaultStore` reading `sessionStorage['last-store']` | accept | Pre-existing behaviour ported verbatim from `SidebarLinks/index.tsx:110-113`; the value only selects a client-side hash route rendered by the existing `store/:store` route, which already handles unknown store values. Adding validation here would change shipped behaviour inside a relocation phase. |
| T-34.10-02 | Information disclosure | `navTabs.ts` | accept | Pure route-string functions; no credentials, no PII, no IPC. |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | This phase installs **zero** new dependencies — MUI, react-router, i18next and FontAwesome are all already in `package.json` (34.10-RESEARCH.md § Metadata). No package-manager install task exists in any plan, so the legitimacy gate has nothing to audit. |
</threat_model>

<verification>
- `npx jest --config src/frontend/jest.config.js src/frontend/components/UI/NavShell` — both new suites green
- `npx tsc --noEmit -p tsconfig.json` type-checks the new modules (or the project's existing typecheck script)
- No file outside `src/frontend/components/UI/NavShell/` is modified by this plan
</verification>

<success_criteria>
- `navTabs.ts`, `Tier2PortalContext.tsx` and `GamesPanel/index.tsx` exist with the exact export names in `<interfaces>`
- Every router path resolves to its documented tab; `/download-manager` resolves to `null`
- Both new test suites pass, neither uses jsdom-dependent APIs
- Nothing is mounted yet — `App.tsx` is untouched
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-01-SUMMARY.md` when done
</output>
