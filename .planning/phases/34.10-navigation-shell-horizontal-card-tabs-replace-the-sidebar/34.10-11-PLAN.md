---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 11
type: execute
wave: 7
depends_on: ["34.10-10"]
files_modified:
  - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
autonomous: false
requirements: [REQ-34.10-04, REQ-34.10-05, REQ-34.10-06, REQ-34.10-09, REQ-34.10-16]

must_haves:
  truths:
    - "The active tab visually merges into the content surface in midnightMirage, gruvbox_dark and dracula — verified by eye, on a running build"
    - "A frameless window can still be dragged by empty navbar space under WKWebView, and cannot be dragged by a tab or the Downloads ring"
    - "Typing in the tier-2 search box actually filters the games grid, proving the portal preserved LibraryContext"
    - "Electron still renders and navigates; its traffic-light overlap is recorded, not fixed"
    - "Any item not measured is recorded NOT ATTEMPTED, never inferred from a green suite"
  artifacts:
    - path: ".planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md"
      provides: "The gate contract and its recorded per-item verdicts"
      contains: "verdict"
  key_links:
    - from: "34.10-LIVE-GATE.md"
      to: "REQ-34.10-04 / REQ-34.10-06 / REQ-34.10-09"
      via: "each gate item names the requirement whose box it ticks"
      pattern: "REQ-34.10"
---

<objective>
Author and then run the blocking live gate that closes this phase: theme survival on three themes,
real window dragging under WKWebView, live proof the portalled filters still drive the library, and
a recorded observation of Electron's accepted cosmetic divergence.

Purpose: this project has the lesson on the record — **a live gate beat a green suite three times.**
Every automated check in this phase is a mocked, no-DOM structural test. None of them can see a
pixel or move a window.
Output: a gate contract with per-item verdicts recorded from an actual run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-CONTEXT.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VALIDATION.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-10-SUMMARY.md

<interfaces>
<!-- Themes to switch between (Settings → Accessibility/Appearance), exact names from themes.scss -->
midnightMirage (default) · gruvbox_dark · dracula · dracula-classic

<!-- Verified navbar-vs-body relationship, all three INVERTED relative to the naive assumption -->
midnightMirage: navbar #161c1e over body #070a0b   (navbar lighter)
gruvbox_dark:   navbar #32302f over body #282828   (navbar lighter)
dracula:        navbar #44475a over body #282a35   (navbar MUCH lighter — highest inversion)

<!-- The frameless setting toggled during item 2 -->
settings.framelessWindow — defaults false; toggling it calls Tauri setDecorations() at runtime
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the gate contract</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VALIDATION.md (§ Manual-Only Verifications — the two rows this gate expands into items)
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md (the five defect-class tests for reviewing a gate contract BEFORE its first run, including Test 5 whose unit of review is the requirement PAIR)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-08-SUMMARY.md (what actually shipped in the drag-region change)
  </read_first>
  <action>
    Write `34.10-LIVE-GATE.md` with YAML frontmatter carrying `verdict: null`, `items_total`,
    `items_passed` and a per-item status list, then one section per item. Five items:

    **Item 1 — theme survival.** Launch `pnpm tauri:dev`. In each of `midnightMirage`,
    `gruvbox_dark` and `dracula` (and spot-check `dracula-classic`), click all four tier-1 tabs and
    confirm the active tab reads as continuous with the content surface below it — no visible seam,
    no band boundary cutting across the active tab. Record a per-theme PASS/FAIL, not one aggregate.
    **Never evaluate in `midnightMirage` alone**: all three ship a navbar *lighter* than the body,
    with `dracula` the most inverted, and the card-tab style was chosen specifically because it
    survives that. Ticks REQ-34.10-06 (**D-04**).

    **Item 2 — real window dragging.** With `settings.framelessWindow` ON, drag the window by empty
    space in the tier-1 navbar and confirm it moves. Then confirm dragging by a tab, by the
    Downloads ring, and inside the window-controls area does **not** move it. Then toggle
    `framelessWindow` OFF and confirm the native titlebar still drags and nothing is broken.
    The unit test mocks `closest()` and therefore proves only the JS branch — WKWebView's actual
    non-support of `-webkit-app-region` is what makes this fallback live-load-bearing. Ticks
    REQ-34.10-04.

    **Item 3 — the portal is live, not decorative.** On the Games tab, type into the tier-2 search
    box and confirm the games grid filters; toggle a category and a filter and confirm the grid
    responds. This is the observable proof that `<Header>` still sits inside `LibraryContext` —
    `initialContext`'s no-op setters would produce controls that look identical and do nothing.
    Then navigate to a game page and confirm the Games tab stays lit while the tier-2 column
    collapses rather than showing an empty 204px band. Ticks REQ-34.10-09.

    **Item 4 — ambient Downloads ring.** With no download running, confirm the ring is visible and
    dimmed rather than absent. Start a download and confirm the ring fills and the count badge
    appears; confirm clicking it reaches `/download-manager` and that no tier-1 tab is lit there
    while the ring shows the active state. Ticks REQ-34.10-08's live half.

    **Item 5 — Electron divergence recorded.** Launch the Electron build (`npm start`), confirm the
    shell renders and all four tabs navigate, and **record** whether macOS traffic lights overlap
    the tab bar in frameless mode. This is an observation, not a defect to fix: Electron's
    `titleBarStyle: 'hidden'` + `titleBarOverlay: true` keeps the traffic lights, unlike Tauri's
    `setDecorations(false)`, and Phase 35 deletes Electron. Ticks REQ-34.10-05 (**D-03**).

    State the no-partial-pass rule explicitly: the phase closes only on 5/5, and an item that could
    not be reached is recorded **NOT ATTEMPTED**, never inferred from a green `pnpm test:ci`.

    Before finalising, run the contract-authoring review from the skill reference, including the
    pairwise test: check whether satisfying any item destroys another item's evidence. Item 2's
    `framelessWindow` toggle is the obvious candidate — it changes window chrome mid-run, so
    sequence it **after** items 1 and 3, whose observations are taken in the default decorated
    state. Record the review's outcome in the contract.
  </action>
  <acceptance_criteria>
    - The file has frontmatter with `verdict: null`, `items_total: 5` and a per-item status list initialised to `pending`
    - Each of the five items names the REQ id whose box it ticks
    - Item 1 has three separate per-theme result slots, not one aggregate slot
    - Item 2 has separate result slots for frameless-on drag, frameless-on no-drag (tab, ring, controls) and frameless-off
    - The no-partial-pass rule and the NOT ATTEMPTED rule are both stated in the contract
    - A "Contract review" section records the pairwise check and the resulting item ordering
    - `gsd-sdk query frontmatter.validate` style YAML parse succeeds (the frontmatter is well-formed)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const m=require('fs').readFileSync('.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md','utf8'); const fm=m.split('---')[1]; if(!/verdict:\s*null/.test(fm)||!/items_total:\s*5/.test(fm)) { throw new Error('gate frontmatter missing verdict/items_total') } console.log('gate contract frontmatter ok')"</automated>
  </verify>
  <done>A five-item gate contract exists with per-item result slots, requirement mapping, the no-partial-pass rule, and a recorded pairwise review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the live gate on real hardware</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md (task 1's contract — the five items, in the order the contract's pairwise review fixed)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-08-SUMMARY.md (what shipped in the drag-region change, so item 2's expectations match the code)
  </read_first>
  <action>
    Run `pnpm test:ci` once first and confirm it is green, so the build being measured matches the
    committed tree — a gate run against a stale build proves nothing. Then present the gate to the
    developer and walk the five items in the contract's fixed order (1, 3, 4, 2, 5), recording each
    verdict into `34.10-LIVE-GATE.md` as it is measured rather than at the end. Set the frontmatter
    `verdict` to `pass` only on 5/5; mark any unreached item NOT ATTEMPTED. On any FAIL, write up
    what was observed and route the phase to `/gsd-plan-phase 34.10 --gaps` instead of closing it.
  </action>
  <what-built>
    The two-tier navigation shell is fully built, mounted and green on the automated suite:
    tier-1 card/folder tabs, tier-2 Stores/Settings nav lists, the Games filter column fed by a
    portal from the Library screen, the ambient Downloads ring, the relocated always-mounted version
    block, the disabled tour, the migrated window drag region, and a regenerated i18n gate scope.
    Every automated check in this phase is a mocked, no-DOM structural test — none of them can see a
    pixel or move a window, which is exactly what these five items measure.
  </what-built>
  <how-to-verify>
    Work through `34.10-LIVE-GATE.md` item by item and record each verdict in that file as you go.

    1. Run `pnpm tauri:dev`.
    2. **Item 1 — theme survival.** Switch theme to `midnightMirage`, click all four tabs, and note
       whether the active tab merges into the content surface. Repeat in `gruvbox_dark`, then
       `dracula`, then spot-check `dracula-classic`. Record one verdict per theme. A tab that reads
       as a separate raised chip in `dracula` is a FAIL even if it looks perfect in the default
       theme — that inversion is the whole reason this item exists.
    3. **Item 3 — portal is live.** On the Games tab, type a game name into the tier-2 search box:
       the grid must filter. Toggle a category and a filter: the grid must respond. Then open a game
       page and confirm the Games tab stays lit while the tier-2 column collapses instead of showing
       an empty 204px band.
    4. **Item 4 — Downloads ring.** With nothing downloading, confirm the ring is present and
       dimmed. Start any download; confirm it fills and shows a count. Click it: you should land on
       `/download-manager`, with no tab lit and the ring showing the active state.
    5. **Item 2 — dragging.** Turn `settings.framelessWindow` ON. Drag the window by empty navbar
       space — it must move. Try dragging by a tab, by the Downloads ring, and inside the window
       controls — none of them may move the window. Toggle `framelessWindow` OFF and confirm the
       native titlebar still drags. (This item runs after 1/3/4 because toggling frameless changes
       window chrome mid-run.)
    6. **Item 5 — Electron.** Quit Tauri, run `npm start`, confirm the shell renders and all four
       tabs navigate, and record whether traffic lights overlap the tab bar in frameless mode. Do
       not fix anything you see here — record it.

    Record every verdict in `34.10-LIVE-GATE.md`, set its frontmatter `verdict` to `pass` only on
    5/5, and mark any unreached item **NOT ATTEMPTED** rather than inferring it. If any item FAILs,
    the phase does not close: report the failure and route to `/gsd-plan-phase 34.10 --gaps`.
  </how-to-verify>
  <acceptance_criteria>
    - Every one of the five items has a recorded PASS, FAIL or NOT ATTEMPTED in `34.10-LIVE-GATE.md`
    - Item 1 carries three separate per-theme verdicts, `dracula` among them
    - Item 2 carries separate verdicts for frameless-on drag, the three no-drag surfaces, and frameless-off
    - The frontmatter `verdict` is `pass` only if `items_passed` equals 5
    - Any FAIL is written up with what was observed, not only that it failed
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md','utf8');const fm=m.split('---')[1];if(/verdict:\s*null/.test(fm)){throw new Error('gate verdict still null - run not recorded')}if(!/items_passed:\s*\d/.test(fm)){throw new Error('items_passed not recorded')}console.log('gate verdict recorded')"</automated>
    <human-check>All five items measured on real hardware with per-theme and per-frameless-state verdicts written into 34.10-LIVE-GATE.md</human-check>
  </verify>
  <done>Every gate item carries a recorded PASS, FAIL or NOT ATTEMPTED; the frontmatter verdict reflects the measured result and is `pass` only on 5/5.</done>
  <resume-signal>Type "approved" once every item's verdict is recorded in 34.10-LIVE-GATE.md, or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → native window operations | The gate exercises the real `startDragging` path that plans 08's unit tests only mock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-27 | Repudiation | recording a PASS that was never measured | mitigate | Per-item verdict slots with an explicit NOT ATTEMPTED value, a no-partial-pass rule, and per-theme rather than aggregate slots for item 1 — the project has a recorded history of aggregate slots hiding unmeasured sub-items. |
| T-34.10-28 | Denial of service | frameless toggle wedging the window mid-run | mitigate | Item 2 is sequenced last so a chrome problem cannot destroy items 1/3/4's evidence; the contract's pairwise review records that ordering and its reason. |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists to audit. |
</threat_model>

<verification>
- `34.10-LIVE-GATE.md` frontmatter carries a non-null `verdict` and an `items_passed` count matching the recorded per-item verdicts
- `pnpm test:ci` is still green at the moment of the run (a stale-build gate proves nothing about the current tree)
</verification>

<success_criteria>
- All five items measured and recorded on real hardware
- Theme survival verified in three themes, never in `midnightMirage` alone
- Dragging verified in both frameless states, with the three no-drag surfaces checked
- Electron's cosmetic divergence recorded rather than fixed
- Phase closes only on 5/5
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-11-SUMMARY.md` when done
</output>
