---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 16
subsystem: ui
tags: [live-gate, requirements, gap-cycle, navshell, downloads-ring, tier2-filters]

requires:
  - phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
    provides: "Run 1's live gate (34.10-11), scoring 2/5 with F-34.10-01 and F-34.10-02"
  - phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
    provides: "F-34.10-01 fix (34.10-13, Dropdown click-toggle + in-flow geometry)"
  - phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
    provides: "F-34.10-02 cause (a) fix (34.10-14, DownloadsRing mask + idle-track token)"
  - phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
    provides: "F-34.10-02 cause (b) fix (34.10-15, RingProgress unconditional mount)"
provides:
  - "Live gate run 2, scored 4/5: items 2 (drag), 3 (filter controls' functional contract), 4 (Downloads ring), 5 (Electron) all PASS; item 1 (theme/seam survival) FAIL"
  - "F-34.10-02 CONFIRMED CLOSED by live measurement -- the two unresolved risks 34.10-15-SUMMARY.md flagged are FALSIFIED"
  - "F-34.10-01 CONFIRMED CLOSED by live measurement"
  - "Four new findings minted (F-34.10-03..06) from a genuinely new defect class: layout/seam regressions in the navbar itself, not present in run 1's own findings"
  - "REQ-34.10-05 and REQ-34.10-08 ticked Complete against a measured run; REQ-34.10-09 and REQ-34.10-16 correctly left Pending with reasons tied to specific new findings"
affects: ["a future 34.10 gap-cycle plan (item 1's 3 new findings)", "REQ-34.10-06 (theme/seam survival, not itself re-verified per-theme this run)"]

tech-stack:
  added: []
  patterns:
    - "Additive live-gate re-run: §6/§7 appended to an existing gate file, run 1's §0-§5 preserved byte-identical, frontmatter carries a runs: history list"
    - "Per-requirement (not per-gate) completion ticking: a requirement is ticked Complete when its own specific gate item passes, even while the overall gate verdict is fail -- deviates from this plan's own originally-authored all-or-nothing framing, see Deviations"

key-files:
  created:
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-16-SUMMARY.md
  modified:
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
    - .planning/REQUIREMENTS.md

key-decisions:
  - "Task 1 (contract authoring) and task 2 (running/recording) were executed by different invocations of this executor, honoring decision D-E, even though both happened within the same overall session -- task 1 committed and returned a checkpoint before any run-2 result was known."
  - "Item 4's overall PASS is recorded even though its discriminator sub-check (vi) was not itemized separately in the operator's terse raw report ('#4 pass') -- the coordinator's relay explicitly stated 'all sub-checks including the arc filling and advancing' PASS, which is treated as the authoritative synthesis of the human's session. Recorded with an explicit honesty note in the gate file rather than silently smoothed over."
  - "Item 1's per-theme rows are recorded NOT ATTEMPTED, not FAIL, because the operator's report of the two layout defects (gap, wrapping) was general, not scoped to any one theme -- per this project's own NOT-ATTEMPTED-never-inferred rule, no per-theme verdict is invented from a general report."
  - "REQ-34.10-09 left Pending despite item 3's own gate-item PASS: F-34.10-05 (black background) is judged, by the same precedent run 1 itself set (F-34.10-01 was a geometry/rendering defect in the identical component, and it gated this requirement), to fall inside REQ-34.10-09's scope rather than 34.11's deferred restyling scope."
  - "REQ-34.10-16 left Pending: its own text names the card-tab merge illusion as the first behaviour it exists to verify, and that behaviour (item 1) FAILED this run -- the gate mechanism performed correctly, but the requirement's own text is that the gate closes the phase, and it has not."

requirements-completed: [REQ-34.10-05, REQ-34.10-08]

duration: ~1h10min (task 1 authoring + two coordinator-relayed result messages, including one correction)
completed: 2026-08-08
---

# Phase 34.10 Plan 16: Live gate run 2 — 4/5, item 1 (theme/seam) newly FAILS Summary

**Run 2 confirmed both of run 1's blocking defects (F-34.10-01, F-34.10-02) are genuinely fixed by live measurement — closing REQ-34.10-08 and falsifying the two risks 34.10-15-SUMMARY.md left open — but a previously-passing item (theme/seam survival) newly FAILED on layout regressions never seen in run 1, so the phase does not close and a gap cycle is owed.**

## Performance

- **Duration:** ~1h10min (task 1 contract authoring, checkpoint, then two coordinator-relayed operator-report messages — the second correcting item 5 from NOT ATTEMPTED to PASS)
- **Completed:** 2026-08-08
- **Tasks:** 2 (task 1: author run-2 contract; task 2: record the human-run gate)
- **Files modified:** 2 (`34.10-LIVE-GATE.md`, `REQUIREMENTS.md`) + this SUMMARY

## Accomplishments

- Authored `34.10-LIVE-GATE.md` §6 (scope, a fresh P1-P7 preflight deriving the suite baseline from `34.10-15-SUMMARY.md` instead of run 1's stale 216/4214, a re-derived Structural Reachability Review covering the three sub-checks never reached in run 1, and a pairwise review flagging download-start as DESTROYS against item 1's new idle-ring observation) and §7 (blank per-sub-check verdict tables) — committed (`b6ab173ef`) before any run-2 result was known, per decision D-E.
- Recorded run 2's results into §7 and the frontmatter as the operator's report was relayed: **items_passed: 4/5**, `verdict: fail`.
  - **Item 1 — FAIL.** Two new blocking layout defects (visible ~10px seam gap between tab strip and content; logo/Downloads-ring wrapping to a second row) plus a related defect (navbar not fixed to the top of the window; an active scrollbar draws over it). Per-theme rows recorded NOT ATTEMPTED, not inferred, since the operator's report was general rather than per-theme.
  - **Item 2 — PASS.** Frameless-ON drag by the Downloads ring does not move the window.
  - **Item 3 — PASS** (functional contract: click-toggle, containment, close-on-reclick, filter application, both controls). Confirms F-34.10-01 CLOSED. A new, unrelated defect (F-34.10-05, black background breaking the panel's styling) was minted separately, not folded into this item's verdict, per the operator's own framing.
  - **Item 4 — PASS**, all sub-checks including the arc filling and advancing. Confirms F-34.10-02 CLOSED in full (both causes). **The two risks 34.10-15-SUMMARY.md explicitly left open (key-driven remount; H3-mechanism doubt) are FALSIFIED by this measurement** — recorded prominently per the coordinator's explicit instruction that this correction "matters and must not be lost."
  - **Item 5 — PASS** (corrected mid-session from an initial NOT ATTEMPTED report). Shell renders and all four tier-1 tabs navigate — the first time this sub-check has ever been measured, closing a gap that survived run 1 unmeasured.
- Minted four new findings, **F-34.10-03** through **F-34.10-06**, into the gate file's frontmatter `findings:` list and §7's item-1 writeup (F-34.10-03/04/06) and item-3 writeup (F-34.10-05).
- Updated `.planning/REQUIREMENTS.md`: ticked **REQ-34.10-05** and **REQ-34.10-08** Complete against this measured run; left **REQ-34.10-09** and **REQ-34.10-16** Pending with reasons tied to the specific new findings that keep them open. Also fixed a pre-existing inconsistency found in the process: REQ-34.10-09's checkbox had drifted to `[x]` even though its own prose already said "REVERTED to Pending" after run 1 — corrected to `[ ]` independent of run 2's own (also-Pending) result.

## Task Commits

1. **Task 1: Author the run-2 contract, with its own re-derived reachability and pairwise review** — `b6ab173ef` (docs) — §6/§7 appended, run 1's §0-§5 left byte-identical apart from one pointer line; committed and checkpoint returned before any run-2 result existed.
2. **Task 2: Record the human-run gate results** — committed with this SUMMARY (see below) — §7 verdicts, frontmatter (`items_passed: 4`, `verdict: fail`, `runs:` history), four new findings, `REQUIREMENTS.md` updates.

## Files Created/Modified

- `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md` — §7 verdicts recorded item-by-item as relayed; frontmatter `verdict: fail`, `items_passed: 4`, `runs:` history entry for run 2 completed; four new findings (`F-34.10-03..06`) added to `findings:`; `F-34.10-01`/`F-34.10-02` marked `status: closed` in-place (their original entries preserved verbatim, a `status`/`closed_by` field added rather than rewritten)
- `.planning/REQUIREMENTS.md` — REQ-34.10-05 and REQ-34.10-08 ticked Complete with run-2 citations; REQ-34.10-09 and REQ-34.10-16 left Pending with run-2 addenda explaining why; REQ-34.10-09's stale `[x]`/"REVERTED to Pending" inconsistency corrected to `[ ]`; traceability table (lines 365-381) updated to match

## Decisions Made

- **Per-requirement, not per-gate, completion ticking.** This plan's own task 2 action text originally framed REQUIREMENTS.md ticking as all-or-nothing ("On 5/5: tick... On anything less than 5/5: change nothing to Complete"). The coordinator's explicit run-2 instructions overrode this: REQ-34.10-05 and REQ-34.10-08 were ticked even though the overall gate is `fail`, because each requirement's own text is satisfied by its own specific item (5 and 4 respectively) passing, independent of item 1's unrelated seam/layout failure. This is a deliberate, explicitly-instructed deviation from the plan's originally-authored framing, not a self-directed shortcut — recorded here so a future reader understands why the "5/5 or nothing" rule as literally written in the plan text was not applied.
- **Preflight P1-P7 left unfilled, honestly.** The operator's relayed report covered item verdicts only, never the preflight measurements (jest counts, codecheck, pgrep, library state). Rather than infer "pass" for these from the fact that a detailed live run clearly happened, the Preflight Results block records each row as NOT REPORTED, with an explicit note that this is a gap in this run's own capture, separate from and not affecting the five items' own verdicts.
- **Item 4's discriminator sub-check (vi) recorded PASS despite not being separately itemized in the operator's raw ("#4 pass") report** — the coordinator's synthesis explicitly said "all sub-checks... PASS," treated here as the authoritative account of the full session, with an inline honesty note in the gate file distinguishing what the raw quote said from what is recorded.
- **Item 5's initial NOT ATTEMPTED verdict was recorded, then corrected in place** when the coordinator relayed that the operator had in fact measured it. Both the initial and corrected states are visible in this file's own git history (two edits to the same section) rather than being silently rewritten as if the correction had always been the first draft.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected the item-4 discriminator's source citation during contract authoring (task 1)**
- **Found during:** Task 1, §6.3's reachability review
- **Issue:** The plan's own task text cited `DownloadManagerItem/index.tsx:91` as the location of the `/download-manager` screen's per-item percent readout. Source verification showed line 91 is `const [progress] = hasProgress(appName, runner)` — that value is only ever passed into a cancel-dialog helper, never rendered as percent text. The actual on-screen readout is `ProgressHeader` (`DownloadManager/index.tsx:111-115`, `ProgressHeader/index.tsx:113,122`), which matches `34.10-F02-DIAGNOSIS.md`'s own Q1 discriminator (already used once in this phase for the identical purpose).
- **Fix:** §6.3 and §7's item-4 discriminator row cite `ProgressHeader`, not `DownloadManagerItem`, with the correction stated explicitly inline so a future reader isn't left to rediscover the same mismatch.
- **Files modified:** `34.10-LIVE-GATE.md` (authoring, no source code touched)
- **Committed in:** `b6ab173ef` (task 1)

**2. [Rule 1 - Bug] Fixed a pre-existing REQUIREMENTS.md checkbox/prose inconsistency for REQ-34.10-09**
- **Found during:** Task 2, while deciding REQ-34.10-09's final state
- **Issue:** REQ-34.10-09's bullet checkbox read `[x]` (Complete) while the very next sentence in the same bullet said "**REVERTED to Pending 2026-08-08**" — the checkbox had never actually been flipped when run 1's plan (34.10-11) recorded the revert. The traceability table (line 373) inherited the same wrong "Complete" value.
- **Fix:** Checkbox corrected to `[ ]` and the traceability table row corrected to `Pending`, independent of and prior to applying run 2's own (also-Pending) result.
- **Files modified:** `.planning/REQUIREMENTS.md`
- **Committed in:** this plan's task 2 commit (below)

**Total deviations:** 2 auto-fixed (1 documentation-citation correction, 1 pre-existing inconsistency fix). No code was touched by this plan — it is a documentation/verification-recording plan only.

## Known Stubs

None. This plan produces no application code.

## Threat Flags

None beyond those already disposed in this plan's own `<threat_model>` (T-34.10-42 through -47, all `mitigate`, all satisfied by the structure of §7's recorded verdicts and the frontmatter's `runs:` history).

## Issues Encountered

- **Preflight capture gap.** The operator's session, as relayed to this executor, covered only item-by-item verdicts — no P1-P7 preflight measurements were reported. This is recorded honestly in the gate file (NOT REPORTED, not inferred) and is flagged as a real, if minor, gap in this run's own evidence trail, distinct from the five items' own results.
- **Mid-session correction.** Item 5 was first reported as NOT ATTEMPTED, then corrected by the coordinator to PASS after the operator had in fact measured it. Handled by editing the same section in place with the corrected values and an explicit note describing the correction, rather than silently discarding the intermediate state.

## User Setup Required

None.

## Next Phase Readiness

**Phase 34.10 does NOT close.** Run 2 scored 4/5. Item 1 (theme/seam survival) is the sole blocker, on three new findings never seen in run 1:

- **F-34.10-03** — a visible ~10px gap between the tier-1 tab strip and the content region, breaking the card/folder tab merge illusion REQ-34.10-06 exists to verify.
- **F-34.10-04** — the wordmark/logo and the Downloads ring wrap onto a second row instead of sharing one line with the tab strip.
- **F-34.10-05** — the tier-2 disclosure panel (from 34.10-13) renders a black background that breaks its styling; keeps REQ-34.10-09 Pending.
- **F-34.10-06** — the navbar scrolls away with page content instead of staying fixed to the top of the window, and an active scrollbar draws over it instead of starting below it.

None of these four findings have a diagnosis yet — this plan only records what the operator observed, per its own scope (task 1 forbids the authoring task from also running the gate; task 2 records, it does not diagnose). **The next command is `/gsd-plan-phase 34.10 --gaps`**, to diagnose and fix F-34.10-03 through F-34.10-06, then run a third live-gate pass scoped to item 1 (and item 3's F-34.10-05) specifically — items 2, 4 and 5 are now closed and should not need re-measurement unless a gap-cycle fix touches their surfaces.

REQ-34.10-08 is genuinely closed — the Downloads ring works, confirmed live, and the two open risks from 34.10-15 are resolved. REQ-34.10-05 is genuinely closed — Electron renders and navigates, confirmed live for the first time. REQ-34.10-09 and REQ-34.10-16 remain open, tied directly to the four new findings above.

---
*Phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar*
*Completed: 2026-08-08*

## Self-Check: PASSED

`34.10-LIVE-GATE.md` and `REQUIREMENTS.md` verified present and modified on disk; task 1 commit hash (`b6ab173ef`) verified present in `git log --oneline --all`; YAML frontmatter of `34.10-LIVE-GATE.md` verified parseable with `verdict: fail`, `items_passed: 4`, and a 2-entry `runs:` history.
