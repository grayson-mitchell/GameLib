---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 27
type: execute
wave: 4
depends_on: [34.10-26]
gap_closure: true
autonomous: false
requirements: [REQ-34.10-06, REQ-34.10-16]
files_modified:
  - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md

must_haves:
  truths:
    - "Every verdict slot in §10.5 carries a measured value — PASS, FAIL, or NOT ATTEMPTED with its reason"
    - "The F-34.10-03 seam and F-34.10-04 single-line sub-checks are measured in midnightMirage, and the sweep covers midnightMirage, gruvbox_dark and dracula"
    - "`items_passed` and `verdict` are filled from what was measured, never from what was expected"
    - "REQ-34.10-06 and REQ-34.10-16 are ticked if and only if the verdict is a genuine 5/5"
    - "The gamepad focus-scroll sub-check ends this cycle either measured or permanently recorded as a named residual risk"
  artifacts:
    - path: ".planning/phases/34.10-.../34.10-LIVE-GATE.md"
      provides: "Run 4's completed contract: filled preflight block, filled §10.5, reconciled frontmatter"
      contains: "run_number: 4"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.10-06 / REQ-34.10-16 status reconciled against the measured verdict"
      contains: "REQ-34.10-16"
  key_links:
    - from: "the operator's measured observations"
      to: "34.10-LIVE-GATE.md §10.5 verdict cells"
      via: "recorded as each is measured, never batched at session end"
      pattern: "10\\.5"
    - from: "34.10-run4-contract-check.mjs"
      to: "frontmatter items_passed / verdict"
      via: "consistency assertion run at close-out"
      pattern: "items_passed"
---

<objective>
RUN the run-4 live gate and record its verdict. This is the blocking checkpoint that decides
whether phase 34.10 closes.

This plan is the RUNNER. It did not author the contract (plans 34.10-25 and 34.10-26 did) and it
may not reshape it. Decision D-E exists for exactly this reason: a plan that both writes and scores
a contract can quietly bend the contract to fit what it saw. If a step turns out to be unreachable
or defective at run time, record that as a finding against the contract — do not rewrite the step
and then score it.

Purpose: F-34.10-03 and F-34.10-04 have been root-caused and fixed (commit `220211230`,
operator-confirmed `1c7a3359d`), but confirmed in ONE theme the gate does not score. This phase has
already declared this exact truth fixed TWICE before, and a live measurement contradicted the fix
both times. A shipped fix is never evidence. Only this run can close them.

Output: a completed §10 in `34.10-LIVE-GATE.md`, and REQUIREMENTS/ROADMAP/STATE reconciled to what
was measured.

**House rule D-08, restated because it is the whole point:** the phase closes only on 5/5.
`verdict: pass` may be set only if `items_passed == 5`. Requirement boxes tick if and only if the
verdict is a genuine 5/5. Any FAIL, or any unresolved slot, means the phase stays open and routes
to another gap cycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md
@.planning/debug/navbar-seam-and-logo-offset.md
@.planning/ROADMAP.md

Read `Skill("spike-findings-gamelib")` and its `references/live-gate-contract-authoring.md` §4 —
the two rules this project has paid for twice: never use a transcript's silence to prove a path did
not run, and a green test suite never closes a live gate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the automatable preflight rows and fill §10.2's Preflight Results block</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - `34.10-LIVE-GATE.md` §10.2 — every preflight row, its exact command, and the item premise that depends on it
    - `34.10-LIVE-GATE.md` §10.1 — which items are in scope, so a preflight row whose only dependent sub-check was carried forward can be recorded N/A rather than run pointlessly
    - `.planning/debug/navbar-seam-and-logo-offset.md` § session_constraints_learned — the five environment traps (stale bundle, single instance, dead console, screencapture failure modes, Keychain timeout)
    - `.planning/phases/34.10-.../34.10-23-SUMMARY.md` — the exact test-count delta plan 34.10-23 introduced, so P1's expected/observed comparison is arithmetic rather than a guess
  </read_first>
  <action>
    Execute every preflight row §10.2 defines that does not require the app to be running, and write
    the literal results into §10.2's Preflight Results block. Fill the block by stating each row's
    selected value in prose alongside the checkbox form — a correctly-filled multiple-choice row
    still leaves unticked `[ ]` options behind, and run 3's own checker was broken by exactly that;
    plan 34.10-26's checker deliberately ignores this block, so write it for a human reader.

    At minimum:
    - Full suite: `pnpm jest --runInBand --silent --forceExit` (this phase's documented `test:ci`
      hang workaround — NOT bare `pnpm test:ci`, which is known to hang under some shells here).
      Write BOTH §10.2's expected numbers and the observed numbers. If they differ, state by how
      much and why BEFORE proceeding; do not proceed on an unexplained delta. If a failure appears,
      name it explicitly rather than assuming it is one of the previously-recorded flakes
      (`bootstrapWirings`, `hardcodedStringGate`) — and if you do attribute it to a flake, run the
      single-file repro first, per this project's own `flake-baselines-can-be-undiagnosed-bugs` rule.
    - `pnpm codecheck` — record the exit code.
    - Single instance: `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'` must BOTH be
      empty before the first launch. If either returns a PID, kill it or identify whose it is, then
      re-check and record both the before and after readings. This trap has fired at least three
      times in this phase and does not announce itself — it splits the `[shell]` sink while
      `gamelib.log` stays shared, so a half-captured run looks fully measured.
    - Bundle staleness, BEFORE any scored observation: the launch must be `pnpm tauri:dev`, never
      bare `tauri dev`. `src-tauri/tauri.conf.json` sets `frontendDist: "../build"` — a static
      directory, not a dev server. Run §10.2's bundle-content assertions against `build/assets/`
      and confirm the served CSS chunk contains the compiled `.NavTabs.MuiTabs-root` rule with
      `padding-bottom:0` AND that plan 34.10-23's rescoped Settings selector is present in its
      ancestor-scoped form. Record the actual grep output. **No scored observation may be taken
      until this row passes.** Run 3 caught a pre-fix bundle exactly here, which would otherwise
      have recorded four confident FAILs against fixes the running app never contained.

    Hand the launch itself to Task 2. Do not launch the app in this task, and do not record any
    item verdict here.
  </action>
  <acceptance_criteria>
    - §10.2's Preflight Results block contains a recorded value for every row §10.2 defines — no
      blank rows. A blank slot at gate close-out is itself a defect in the run, not an acceptable outcome
    - The block records both the expected and the observed jest suite/test counts as explicit numbers
    - The block records the literal `pgrep` output (PIDs or "empty") for both process patterns, and
      if any PID was found, both the before and after readings
    - The block records the verbatim output of the bundle-content grep commands, showing
      `padding-bottom:0` present in the served chunk
    - `git diff --numstat` for `34.10-LIVE-GATE.md` shows changes confined to §10.2's results block
      (no verdict cell in §10.5 is touched by this task)
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest --runInBand --silent --forceExit; pnpm codecheck; pgrep -f 'tauri dev'; pgrep -f 'target/debug/gamelib'; grep -o '\.NavTabs\.MuiTabs-root{[^}]*}' build/assets/*.css</automated>
  </verify>
  <done>
    Every automatable preflight row has a recorded result in §10.2, including the expected-vs-observed
    test counts, the single-instance readings, and proof that the built bundle contains commit
    `220211230`'s fix. No item verdict has been written.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive the run-4 gate live and record every verdict as it is measured</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <action>
    Drive the operator through §10.5's items in §10.4's derived run order, exactly as written, and
    transcribe each verdict into §10.5's own tables in `34.10-LIVE-GATE.md` AS IT IS MEASURED.
    This task writes verdict cells and nothing else: it may not edit §10.1-§10.4 (decision D-E — the
    runner may not reshape the contract), and it may not touch the frontmatter `verdict` or
    `items_passed` fields, which Task 3 owns as sole writer.

    Ask one sub-check at a time. Do not batch questions and do not batch answers: run 3 recorded
    three per-theme cells from a single cross-theme generalisation and had to flag the weaker
    evidence provenance in the document itself. If an answer is ambiguous, contradictory, or looks
    like a typo, RE-ASK rather than taking the obvious reading — run 3 caught a duplicated
    sub-check label exactly this way, on the one cell that discriminated a real defect from a
    frozen-at-zero one.

    The full operator-facing procedure — launch command, presence signals, per-theme recording
    rules, the gamepad escape hatch and the capture hygiene rules — is in `<how-to-verify>` below.
    Follow it verbatim rather than paraphrasing it to the operator.
  </action>
  <read_first>
    - `34.10-LIVE-GATE.md` §10.5 — the item bodies, in the run order §10.4 derived; follow that order exactly, it encodes DESTROYS pairs
    - `34.10-LIVE-GATE.md` §10.4 — why the order is what it is, so you can tell the operator what NOT to do early (e.g. applying a filter shortens the Games grid; starting a download destroys the idle-ring state irreversibly for the session)
    - `34.10-LIVE-GATE.md` §5 — the no-partial-pass rule and the NOT ATTEMPTED rule
  </read_first>
  <what-built>
    Commit `220211230` fixed the confirmed root cause of F-34.10-03 (the ~8px seam between the
    tier-1 tab strip and the content surface) and F-34.10-04 (the wordmark and Downloads ring
    reading lower than the tab strip): an unscoped `.MuiTabs-root { padding-bottom: var(--space-xs) }`
    in `GamesSettings/index.scss` leaking 8px into the nav shell's `<Tabs>`. Plan 34.10-23 then
    rescoped that rule at source and added a regression guard. The fix was pixel-verified and
    operator-confirmed — but in ONE theme the gate does not score.
  </what-built>
  <how-to-verify>
    1. Launch with `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev`. **Never bare `tauri dev`** —
       `frontendDist: "../build"` is a static directory, so a bare launch serves whatever
       `electron-vite build` last wrote, and this phase has already nearly scored five fixes against
       a bundle that predated them all. `GAMELIB_DEV_SECRET_VAULT=1` is required: without it an
       unanswered Keychain prompt makes `keyring_get` time out after 45s and takes the whole app down.
    2. Confirm exactly one app process is running (`pgrep`), then work through §10.5's items in
       §10.4's derived run order. Do not reorder.
    3. For item 1 Step 1, in `midnightMirage`: judge the seam and the single-line sub-checks. The
       presence signal for a FAIL is a strip of navbar-coloured background between the selected
       tab's bottom edge and the content surface — it measured exactly 8px before the fix. "It looks
       fine" is not the answer being sought; ask whether that strip is there.
    4. For item 1 Step 2, sweep `midnightMirage`, `gruvbox_dark` and `dracula`, recording BOTH the
       seam cell AND the idle-ring cell for each theme before moving to the next. **A general report
       ("looks the same across themes") is NOT a per-theme measurement** — record NOT ATTEMPTED for
       whichever cells it does not distinctly address. This cost the sweep three runs running.
       `dracula-classic` is NOT in scope: it does not exist in the shipped picker.
    5. For the gamepad focus-scroll sub-check: ask whether a controller is connected and recognised.
       If not, record NOT ATTEMPTED with "no controller available" as the reason. It does not by
       itself fail item 1, but it must NOT be inferred from the mouse/keyboard result — different
       code path, different container-rect-relative arithmetic. Task 3 then records it as a
       permanent named residual risk.
    6. Take the non-scoring observation §10.5 defines for plan 34.10-23's rescope collateral
       (Settings → Games, Download Manager, Wine Manager tab strips).
    7. If any step needs a computed style: **the Web Inspector console does not evaluate in this
       build** — input echoes, `1+1` returns nothing, Enter is inert, while the app's own logging
       still reaches the console. Use a `window.api.logInfo` probe to `gamelib.log`, or
       `screencapture` + pixel decode. If capturing: wrap it in `caffeinate -d -i -u -t 60`, size-check
       the PNG (>400KB — a sleeping display silently yields an all-black ~107KB file instead of an
       error), and re-locate the navbar each launch rather than reusing coordinates.

    **Recording rules, non-negotiable:** write each verdict into `34.10-LIVE-GATE.md` AS IT IS
    MEASURED, not batched at session end — verdicts live in the markdown file, which no process
    restart can erase. Use exactly `PASS`, `FAIL`, or `NOT ATTEMPTED`, with a reason inline for any
    FAIL or NOT ATTEMPTED. Never infer a PASS from a green test suite, from a prior run's source
    reasoning, or from a landed fix; never infer a FAIL from silence. If the operator's answer is
    ambiguous or looks like a typo, RE-ASK rather than taking the obvious reading — run 3 caught a
    duplicated sub-check label this way on the one cell that discriminated a real defect.
  </how-to-verify>
  <acceptance_criteria>
    - Every verdict cell in §10.5 is resolved to exactly one of `PASS`, `FAIL`, `NOT ATTEMPTED`
      (markdown emphasis permitted); zero cells left empty
    - Every `FAIL` and every `NOT ATTEMPTED` carries an inline reason; every FAIL that reports the
      operator's own words quotes them verbatim
    - Item 1's per-theme sweep table has a distinct recorded value in BOTH columns for each of
      `midnightMirage`, `gruvbox_dark` and `dracula` — nine cells minimum, none inferred from a
      cross-theme generalisation without that being stated as the evidence
    - `dracula-classic` appears nowhere as a scored or attempted cell
    - `node .planning/phases/34.10-*/34.10-run4-contract-check.mjs .planning/phases/34.10-*/34.10-LIVE-GATE.md`
      reports `unfilled: 0` and `invalid: 0` for the §10.5 slots (the frontmatter consistency half
      may still fail here — Task 3 reconciles it)
  </acceptance_criteria>
  <resume-signal>
    Report each item's result as it is measured. When all items are done, type "gate complete" —
    or describe any step that could not be reached and why, so it is recorded NOT ATTEMPTED rather
    than guessed.
  </resume-signal>
  <verify>
    <automated>node .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-run4-contract-check.mjs .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md 2>&1 | grep -E 'unfilled: 0' && echo ALL_SLOTS_RESOLVED</automated>
  </verify>
  <done>
    Every §10.5 verdict slot carries a measured value with reasons on every FAIL and NOT ATTEMPTED.
    Item 1's three-theme sweep is complete in both columns. The contract checker reports zero
    unfilled and zero invalid slots.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the frontmatter verdict and propagate to REQUIREMENTS, ROADMAP and STATE</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `34.10-LIVE-GATE.md` §10.5 as filled by Task 2 — the measured verdicts, which are the ONLY input to this task
    - `34.10-LIVE-GATE.md` §5 — `verdict: pass` may be set only if `items_passed == 5`
    - `34.10-LIVE-GATE.md` §9.6 — the "what this run settled and what it did not" shape to mirror for run 4
    - `.planning/REQUIREMENTS.md` lines 365-380 (the status table) and the REQ-34.10-06 / REQ-34.10-16 entries around line 804
    - `.planning/ROADMAP.md` § Phase 34.10 as corrected by plan 34.10-24
  </read_first>
  <action>
    **This task is the SOLE writer of `verdict` and `items_passed`, and it fills them from what was
    measured, never from what was expected.**

    (a) Set each frontmatter `items[].status` from its own measured item verdict in §10.5. Set
        `items_passed` to the count of `pass` statuses. Set `verdict: pass` if and only if
        `items_passed == 5`; otherwise `verdict: fail`. Update the run-4 `runs:` entry to match.
        Run `34.10-run4-contract-check.mjs` against the file and require exit `0` — it asserts this
        consistency for you.

    (b) Write `### 10.6 Run 4 — what this run settled, and what it did not`, stating in BOTH
        directions: what closed by measurement, what did not, what was measured for the first time,
        and what is carried forward as unmeasured. Name F-34.10-03 and F-34.10-04 explicitly and
        state whether each is now `closed` (by measurement in a scored theme) or still open. Do not
        write a cause for any new failure — a cause guessed into a gate becomes the next plan's
        falsified premise; record the observation and let a later diagnosis own it.

    (c) **REQUIREMENTS.md — tick if and only if 5/5.** On a genuine 5/5: set REQ-34.10-06 and
        REQ-34.10-16 to `Complete` in the status table AND check their `- [ ]` boxes, appending a
        line to each recording the measuring run, its date, and the three themes swept. On anything
        less than 5/5: change NOTHING in REQUIREMENTS.md except an appended note stating run 4's
        outcome and that both requirements remain Pending. Do not reword REQ-34.10-06 in either case.

    (d) ROADMAP.md — update the Phase 34.10 `**Plans:**` paragraph and tick the gap-cycle-3
        checklist entries. State the run-4 verdict, whether the phase CLOSES, and the next command:
        `/gsd-verify-work` on a 5/5, or `/gsd-plan-phase 34.10 --gaps` (gap cycle 4) on anything less.
        If any item FAILED, note that `34.10-VERIFICATION.md` must be REGENERATED before that
        `--gaps` run — this phase has already nearly planned a cycle against a stale verification file.

    (e) STATE.md — update the frontmatter `stopped_at`, `last_updated`, `last_activity`, and
        `progress.completed_plans`, and add a Phase 34.10 position entry recording the run-4
        outcome. **Edit by hand with the Edit tool; do NOT use `gsd-sdk query state.*` write
        handlers** — this project has a recorded, repeatedly-confirmed defect where those writes
        corrupt STATE.md. Re-read the file after editing and hand-correct anything displaced.

    (f) **The gamepad focus-scroll sub-check's terminal disposition.** If it was measured, record the
        result and close it. If it was again NOT ATTEMPTED, record it as a PERMANENT named residual
        risk — in ROADMAP.md's Phase 34.10 section and as an appended note on the relevant
        requirement — stating that `scrollCardIntoView`'s container-rect-relative arithmetic
        (`GamesList/index.tsx`, exercised only when `activeController` is truthy) has never been
        measured in any run of this phase, that plan 34.10-18's scroll-container relocation directly
        affects it, and that it is never inferred from the mouse/keyboard sub-check's PASS. It must
        not survive as a perpetual pending cell that a fifth run inherits unchanged.

    (g) If any contract defect surfaced during the run (a step that could not be reached, a control
        that does not exist, an instruction that contradicts another), record it as a new finding in
        the frontmatter `findings:` list, typed `process-not-product` if no user-facing behaviour is
        affected — the same way F-34.10-07 was recorded rather than folded into a product finding.
  </action>
  <acceptance_criteria>
    - `node .planning/phases/34.10-*/34.10-run4-contract-check.mjs .planning/phases/34.10-*/34.10-LIVE-GATE.md`
      exits `0` and reports `COMPLETE`, `unfilled: 0`, `invalid: 0`, and a consistent frontmatter
    - Frontmatter `items_passed` equals the number of `items[]` entries with `status: pass`, verified
      by the checker; `verdict: pass` present only when that count is `5`
    - REQUIREMENTS.md's REQ-34.10-06 and REQ-34.10-16 rows read `Complete` if and only if
      `items_passed == 5`; otherwise both still read `Pending` and
      `git diff .planning/REQUIREMENTS.md` shows only appended note text, no status-cell change
    - REQ-34.10-06's requirement text is unchanged: `git diff .planning/REQUIREMENTS.md` contains no
      deletion of any line belonging to the REQ-34.10-06 entry
    - `grep -c '^### 10\.6' …/34.10-LIVE-GATE.md` returns `1`
    - ROADMAP.md's Phase 34.10 section names the next command, and if the phase did not close it
      also contains the string `REGENERATE` (or `regenerated`) applied to `34.10-VERIFICATION.md`
    - `head -15 .planning/STATE.md` shows well-formed YAML with each of `stopped_at`,
      `last_updated`, `last_activity`, `progress` appearing exactly once
    - If the gamepad sub-check is NOT ATTEMPTED, `grep -c 'scrollCardIntoView' .planning/ROADMAP.md`
      returns at least `1`
  </acceptance_criteria>
  <verify>
    <automated>D=.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar; node $D/34.10-run4-contract-check.mjs $D/34.10-LIVE-GATE.md && grep -c '^### 10\.6' $D/34.10-LIVE-GATE.md | grep -q '^1$' && head -15 .planning/STATE.md && echo RECONCILE_OK</automated>
  </verify>
  <done>
    The frontmatter verdict and `items_passed` reflect the measured run and pass the contract
    checker. §10.6 states what closed and what did not, in both directions, with no guessed causes.
    REQUIREMENTS.md moved only on a genuine 5/5. ROADMAP.md and STATE.md name the correct next
    command. The gamepad sub-check has a terminal disposition rather than another deferral.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| running app → operator's eye → recorded verdict | The only channel in this phase that can observe a rendered pixel. Every automated check in the tree is `testEnvironment: 'node'` with no jsdom and no CSS engine. |
| recorded verdict → requirement status | A verdict written from expectation rather than measurement propagates into REQUIREMENTS.md and closes a phase that is not done. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-27-01 | Spoofing | A stale bundle presenting as the fixed build, producing four confident FAILs against code the app never ran | mitigate | Task 1's bundle-content assertion must pass before any scored observation; `pnpm tauri:dev` is mandatory and bare `tauri dev` is forbidden |
| T-34.10-27-02 | Tampering | A second concurrent app instance splitting the `[shell]` sink while `gamelib.log` stays shared, making a half-captured run look measured | mitigate | Task 1 asserts both `pgrep` patterns empty before launch and records the literal readings; re-checked before any second shell launch |
| T-34.10-27-03 | Repudiation | A verdict inferred from a landed fix or a green suite rather than measured | mitigate | §5's NOT-ATTEMPTED-never-inferred rule is restated in Task 2's instructions; every FAIL quotes the operator verbatim; Task 3 is the sole writer of `verdict`/`items_passed` and takes §10.5 as its only input |
| T-34.10-27-04 | Elevation of Privilege | The runner amending the contract to fit what it observed | mitigate | Decision D-E: this plan may not edit §10.1-§10.4; a defective step is recorded as a new finding, not rewritten |
| T-34.10-27-05 | Denial of Service | An unanswered Keychain prompt killing the app 45s in, mid-measurement | mitigate | `GAMELIB_DEV_SECRET_VAULT=1` is in the launch command in Task 2's step 1 |
| T-34.10-27-06 | Information Disclosure | A screenshot silently returning an all-black ~107KB PNG instead of erroring, on a sleeping display | mitigate | Task 2 step 7 requires `caffeinate -d -i -u -t 60` and a >400KB size check before any capture is trusted |
| T-34.10-27-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task exists in this plan; it runs an existing app and edits markdown. Any install attempt must stop the run — an install mid-gate would invalidate the bundle assertion Task 1 just made. |
</threat_model>

<verification>
- `git diff --name-only` lists exactly the four files in `files_modified`. Another session is active
  in this repository: explicit per-file `git add` only, never `git add -A`, `git add .`, or
  `git commit -a`; do not stage `src-tauri/src/main.rs`, `src/backend/humble/*`,
  `src/frontend/screens/WebView/*`, `.vscode/settings.json`, `scratchpad/`, `steam_appid.txt`, or
  `.planning/phases/34.7-*`.
- `34.10-run4-contract-check.mjs` exits `0` against the completed contract.
- §0 through §10.4 of `34.10-LIVE-GATE.md` are byte-identical apart from §10.2's results block —
  runs 1-3's recorded history and run 4's own scope/reviews are evidence, not drafts.
- A green `pnpm jest` run closes NOTHING here. The `Frontend` jest project is
  `testEnvironment: 'node'` with no jsdom and no CSS engine; it has been fully green through every
  failing run of this gate. That is the entire reason this plan exists.
</verification>

<success_criteria>
- Every §10.5 verdict slot carries a measured value; the contract checker exits `0`.
- `items_passed` and `verdict` are consistent with the recorded item statuses.
- REQ-34.10-06 and REQ-34.10-16 moved to Complete if and only if the verdict is a genuine 5/5.
- §10.6 records what closed and what did not, in both directions, with no guessed causes.
- ROADMAP.md and STATE.md name the correct next command, and on a non-close they say
  `34.10-VERIFICATION.md` must be regenerated first.
- The gamepad focus-scroll sub-check is measured or permanently recorded as a named residual risk.
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-27-SUMMARY.md` when done.
</output>
