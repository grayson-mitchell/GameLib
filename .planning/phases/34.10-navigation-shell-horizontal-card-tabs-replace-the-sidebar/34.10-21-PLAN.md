---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 21
type: execute
wave: 4
depends_on: ["34.10-18", "34.10-19", "34.10-20"]
files_modified:
  - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
autonomous: true
gap_closure: true
requirements: [REQ-34.10-16]

must_haves:
  truths:
    - "A run-3 contract exists that re-measures item 1 IN FULL, including the per-theme sweep across midnightMirage, gruvbox_dark, dracula and the dracula-classic spot-check that has never been reached in two runs"
    - "Every one of F-34.10-03, -04, -05 and -06 has a named sub-check that would observably fail if its fix did not work"
    - "Items 2, 4 and 5's re-measurement scope is stated with a per-item justification, and anything deliberately not re-taken says why in the document"
    - "A pairwise requirement-interaction review was run, its unit of review was the PAIR, and the run order is DERIVED from its findings rather than asserted"
    - "A preflight requires and records exactly one app process, and names every precondition an item's premise depends on"
    - "This plan authors the contract and does not run it — no verdict slot is filled by the author"
  artifacts:
    - path: ".planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md"
      provides: "§9 — the run-3 scope table, preflight, structural reachability review, pairwise review, derived run order, and item bodies with empty verdict slots"
      contains: "Run 3"
  key_links:
    - from: "34.10-LIVE-GATE.md § 9 item 1"
      to: "F-34.10-03 / F-34.10-04 / F-34.10-06"
      via: "named sub-checks whose failure would be observable, one per finding"
      pattern: "F-34\\.10-0[346]"
    - from: "34.10-LIVE-GATE.md § 9 pairwise review"
      to: "the run_order frontmatter field"
      via: "an ordering derived from the DESTROYS rows, not asserted"
      pattern: "run_order"
---

<objective>
Author the **run-3** live-gate contract as a new `## 9.` section of `34.10-LIVE-GATE.md`: scope
with per-item justification, a preflight, a structural reachability review, a pairwise
requirement-interaction review, a derived run order, and item bodies with empty verdict slots.

Purpose: run 2 scored 4/5 and the phase does not close. Four fixes have since landed
(34.10-18, -19, -20) plus a diagnosis. Item 1's per-theme sweep — the specific check REQ-34.10-06's
own text demands — has **never been reached in either run**. A contract copied from run 2 would
re-inherit run 2's scope, which was written before any of these fixes existed.

Decision D-E is binding: **the plan or task that authors a gate contract may never also run it.**
This plan authors. Plan 34.10-22 runs. Every verdict slot this plan writes must be left empty.

Output: `34.10-LIVE-GATE.md` § 9, ready to hand to a separate invocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-F04-DIAGNOSIS.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-18-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-19-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-20-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-CONTEXT.md

Required skills — invoke all three before authoring:
- `Skill("spike-findings-gamelib")` — `references/live-gate-contract-authoring.md` is the
  authoring standard: the seven defect-class tests, Test 5's pairwise rule (the unit of review is
  the PAIR), and the dual-sink capture standard.
- `Skill("sketch-findings-gamelib")` — `references/navigation-shell.md` § 3 supplies the exact
  per-theme navbar/body values item 1's sweep is judged against.
- `Skill("spike-findings-gamelib")` again for the Tauri/WKWebView gotchas the operator will hit.

<interfaces>
<!-- The gate file's existing structure, so the new section is appended and nothing is rewritten. -->

`34.10-LIVE-GATE.md` currently holds:
  frontmatter — `verdict`, `items_total: 5`, `items_passed: 4`, `run_number: 2`, a `runs:` history
    list, an `items:` list carrying run 2's statuses, a `findings:` list (F-34.10-01..06 with
    `status: closed` on -01 and -02), and `run_order: [1, 3, 4, 2, 5]`
  §0-§5  — run 1's preamble, preflight, contract review, five items, and the no-partial-pass rule
  §6     — run 2's scope, preflight (recorded honestly as NOT FILLED), and re-derived reviews
  §7     — run 2's item bodies and verdicts

Run 2's own recorded gaps, which run 3 must not repeat:
  - §6.2's Preflight Results block was never filled; the run 2 recorder flagged that as a gap in
    its own capture and stated it "should be closed by whichever gap-cycle plan re-runs this gate"
  - Item 1's four per-theme rows are ALL `NOT ATTEMPTED`, for BOTH the seam and the idle-ring
    columns — the sweep has never been measured in any run
  - Item 3's sub-checks (vi) and (vii) are `NOT ATTEMPTED` in both runs

The five items and their requirements:
  1 — theme survival / card-tab merge         REQ-34.10-06 (D-04)   run 1 PASS, run 2 FAIL
  2 — real window dragging under WKWebView    REQ-34.10-04          run 1 PASS, run 2 PASS (partial)
  3 — tier-2 portal is live, not decorative   REQ-34.10-09          run 1 FAIL, run 2 PASS (i-v)
  4 — ambient Downloads ring                  REQ-34.10-08          run 1 FAIL, run 2 PASS
  5 — Electron divergence recorded            REQ-34.10-05 (D-03)   run 2 PASS

What the three fix plans changed (read their SUMMARYs for the authoritative list; this is the
planning-time expectation the author must verify against those SUMMARYs, not substitute for them):
  34.10-18 — `App.css` (`.App` becomes `height: 100vh; overflow: hidden`; `.App .content` becomes
             the scroll container), `NavShell/index.scss` (navbar `border-bottom`),
             `Library/index.tsx` (scroll listeners move off `document.body`)
  34.10-19 — `NavTabs/index.scss` and/or `NavShell/index.scss` (the tab strip's box model)
  34.10-20 — `Dropdown/index.scss` (the disclosure panel's surface)

Standing project rules that bind this contract:
  - **A live gate has beaten a green test suite three times.** The `Frontend` jest project is
    `testEnvironment: 'node'` with no jsdom and no CSS engine; no test in this repo can see a
    pixel, a scrollbar, a hover trigger or a pinned navbar.
  - **NOT ATTEMPTED is never inferred.** An unreached sub-check is recorded with its reason, never
    inferred as PASS from a green suite and never as FAIL from silence.
  - **The phase closes only on 5/5.**
  - **Exactly one app process**, or the `[shell]` sink splits and a half-captured run looks measured
    (F-34.4.2-15/-18). This failed live on this project today.
  - GameLib is an independent project, not a fork tracking upstream Heroic — never raise upstream
    deviation as a concern anywhere in the contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive run 3's scope and preflight from the current tree, not from run 2's</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md § 6.1 and § 6.2 (run 2's scope table and preflight — the shape to follow, NOT the content to copy; §6.2's own results block was never filled and that is one of run 3's owed corrections)
    - 34.10-18-SUMMARY.md, 34.10-19-SUMMARY.md, 34.10-20-SUMMARY.md (each SUMMARY's `key-files.modified` list — the authoritative record of what changed and therefore of what must be re-measured)
    - 34.10-VERIFICATION.md (the six recorded gaps this run must close or honestly leave open)
  </read_first>
  <action>
    Append a new `## 9. Run 3 — scope, preflight, and re-derived review` section to
    `34.10-LIVE-GATE.md`. Do not modify §0-§7; they are run 1's and run 2's recorded history and
    must stay byte-identical apart from the new section being appended after them.

    Write `### 9.1 Scope, with per-item justification` as a table with one row per item, each
    carrying the run-3 scope and the reason:

    - **Item 1 — FULL re-run, and larger than run 2's.** It must cover, per theme, across
      `midnightMirage`, `gruvbox_dark`, `dracula` and the non-blocking `dracula-classic`
      spot-check: (a) the tab/content merge seam, (b) the idle Downloads ring's visibility and
      ring-shape. **This sweep has never been reached in either run** — all eight run-2 cells are
      NOT ATTEMPTED. It must additionally carry four theme-independent sub-checks, one per fix,
      each phrased so that a failure is observable rather than inferable:
        · F-34.10-03 — no visible band or gap between the tier-1 tab strip and the content region
        · F-34.10-04 — the wordmark, the tab strip and the Downloads ring read as ONE line
        · F-34.10-06(a) — the navbar stays fixed at the top of the window while a long route is
          scrolled to its end and back
        · F-34.10-06(b) — with a scrollbar active, it starts BELOW the navbar's bottom edge and
          does not draw over the navbar at any scroll position
      Plus one regression sub-check for the fix itself, since plan 34.10-18 moved the scrolling
      element out from under `document.body`: on the Games grid, scroll down far enough for the
      back-to-top button to appear, click it, leave the tab and return, and confirm the grid's
      scroll position behaves as before. State plainly in the contract that this sub-check exists
      because the migration fails silently — a scroll listener attached to a no-longer-scrolling
      element throws nothing and fails no test.
    - **Item 3 — FULL re-run, all 7 sub-checks, plus F-34.10-05.** Sub-checks (vi) and (vii) have
      never been reached in either run and must be measured, never inferred. Add an F-34.10-05
      sub-check: with the Categories panel expanded, its background reads as a deliberate surface
      belonging to the tier-2 column rather than a black block — judged in at least
      `gruvbox_dark` AND `dracula`, and explicitly NOT in `midnightMirage` alone, where the panel
      and the column are the same colour and the defect is invisible by construction.
    - **Items 2, 4 and 5 — state a scope and justify it.** Do not default to "closed, skip". Derive
      each from the three SUMMARYs' actual file lists, and write the reasoning inline so a partial
      scope is a recorded judgement rather than an omission. Specifically consider, and answer in
      the document: item 2's drag surfaces are defined by where empty navbar space now is, and plan
      34.10-19 changed the tab strip's box model — do the "drag by empty navbar space moves" and
      "drag by a tab does not" sub-checks still stand on run 1/2's measurement, or not? Item 4's
      idle sub-checks are now also scored per-theme inside item 1 — say how the two relate so the
      same observation is not double-counted or silently dropped. Item 5 runs the same CSS under a
      different shell, and plan 34.10-18 changed the shell grid's overflow — say whether that
      makes Electron's re-measurement necessary or merely cheap.

    Then write `### 9.2 Preflight for run 3` as a table, and immediately below it a **Preflight
    Results** block with one literal slot per row. Every row must name the item premise that
    depends on it. Include at minimum:
      P1 — the build matches the committed tree. `pnpm jest --runInBand --silent --forceExit`
           (this phase's documented `test:ci` hang workaround; **not** bare `pnpm test:ci`).
           **Derive the expected suite/test counts from 34.10-18/-19/-20's own SUMMARYs, and write
           both the expected and the observed numbers into the results block.** Do not copy run 2's
           218/4246 — three plans have added tests since.
      P2 — `pnpm codecheck` clean.
      P3 — **exactly one app process** before the FIRST launch: `pgrep -f 'tauri dev'` and
           `pgrep -f 'target/debug/gamelib'` both empty. State that this is a recorded
           capture-integrity trap (F-34.4.2-15/-18) that failed live on this project today, and
           that a found PID must be killed or attributed before launching.
      P4 — the library contains a game with a distinctive, unique-substring title AND a game whose
           page can be opened (item 3's search sub-check and sub-checks vi/vii).
      P5 — at least one game available to start a download (item 4's active sub-checks).
      P6 — `settings.framelessWindow` is OFF at the start (items 1, 3, 4 are judged in the default
           decorated state).
      P7 — the download queue is EMPTY at the start (item 1's per-theme idle-ring sweep).
      P8 — **new for run 3, and load-bearing**: the Games grid is long enough to produce an active
           scrollbar and to scroll past the back-to-top threshold, with no category or filter left
           applied from a previous session. Item 1's F-34.10-06(b) and the back-to-top regression
           sub-check are both unmeasurable without it. If the grid does not scroll, those
           sub-checks are recorded NOT ATTEMPTED with this as the reason — never inferred from "the
           navbar looked fine".

    Leave every results slot empty. Filling one here would make this plan both author and runner.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md';const s=fs.readFileSync(p,'utf8');if(!/##\s*9\.\s*Run 3/.test(s))throw new Error('§9 not appended');const nine=s.slice(s.indexOf('## 9.'));for(const h of ['### 9.1','### 9.2'])if(!nine.includes(h))throw new Error('missing '+h);for(const t of ['midnightMirage','gruvbox_dark','dracula','dracula-classic'])if(!nine.includes(t))throw new Error('scope omits theme '+t);for(const f of ['F-34.10-03','F-34.10-04','F-34.10-05','F-34.10-06'])if(!nine.includes(f))throw new Error('scope omits finding '+f);for(const p8 of ['P1','P2','P3','P4','P5','P6','P7','P8'])if(!nine.includes(p8))throw new Error('preflight omits '+p8);if(!/pgrep -f 'tauri dev'/.test(nine)&&!nine.includes('pgrep'))throw new Error('single-process preflight not present');if(/218 suites|4246 tests/.test(nine))throw new Error('run 2 jest baseline was copied instead of derived');const six=s.indexOf('## 6.');if(six<0||six>s.indexOf('## 9.'))throw new Error('run 2 section was disturbed');console.log('run-3 scope and preflight authored')"</automated>
  </verify>
  <done>§9.1 carries a five-row scope table with per-item justification covering all four themes and all four findings; §9.2 carries P1-P8 with an empty results block; §0-§7 are unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Run the structural reachability review and the pairwise review, and derive the run order from them</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` (the seven defect-class tests and Test 5's pairwise rule — the unit of review is the PAIR, and the hunt is for anything that restarts, truncates, clears, resets, consumes a one-shot resource or sets an env var)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md § 6.3 and § 6.4 (run 2's own reviews — re-derive, do not copy; run 2's pairwise pass found a real defect, which is the evidence that this pass earns its cost)
    - src/frontend/components/UI/NavShell/index.tsx and .../navTabs.ts (`resolveActiveTab`, `tier2Collapsed`) — the source that decides whether item 3's sub-checks vi/vii are producible at all
    - src/preload/api/tauriWindowChrome.ts (`resolveDragRegion`, ~lines 301-342) — the source that decides whether item 2's no-drag assertions are meaningful absences
  </read_first>
  <action>
    Append `### 9.3 Structural reachability review for run 3` and
    `### 9.4 Pairwise review for run 3 (Test 5)` to §9.

    For §9.3, apply all seven defect-class tests to **every** run-3 sub-check, with a verdict of
    `REACHABLE`, `REACHABLE, CONDITIONAL on <preflight row>`, or `NOT REACHABLE` plus the reason.
    Cite source file:line as evidence, not intuition. Give particular attention to the sub-checks
    that have never been measured, because an unreachable scenario is exactly how run 1's and run
    2's item-3 sub-checks went unrecorded, and how phase 34.4.2 shipped an item whose own gesture
    sequence the UI forbade (F-34.4.2-17). At minimum, decide and evidence:
      · item 3(vi)/(vii) — is a game page reachable and does `tier2Collapsed` actually evaluate
        true there? (`NavShell/index.tsx`'s `tier2Collapsed` expression and `navTabs.ts`'s
        `resolveActiveTab`)
      · item 1's F-34.10-06(b) — is a scrollbar producible at all after plan 34.10-18 moved the
        scrolling element? Name which element now owns the scrollbar and on which route it appears,
        because "no scrollbar was visible" must be distinguishable from "the scrollbar is correctly
        below the navbar". This is a Test 4 (absence-observability) case: state what the PRESENCE
        case would look like.
      · item 1's back-to-top regression sub-check — is the 450px threshold reachable on the
        available library size? Tie it to P8.
      · item 1's per-theme idle-ring cells — is the idle state producible for the whole sweep?
      · item 2's three no-drag sub-checks — restate the Test 4 argument that each is a meaningful
        absence (the interactive-element exclusion is a real branch that would fire and move the
        window if removed), not one disproven by construction.

    For §9.4, apply Test 5 with the reduction "every {state-mutating action} against every
    {evidence-bearing item}". Enumerate the state-mutating actions explicitly, then produce a table
    with a `Destroys it?` column and a reasoning column. Run 2's pass found a real DESTROYS pair;
    at minimum re-evaluate that one and hunt for the new ones this run introduces:
      · starting a download (item 4) versus item 1's per-theme IDLE ring sweep — run 2 found this
        DESTROYS and resolved it by run order plus P7; item 1 is now much longer, so restate it
      · switching theme (item 1) — the theme picker lives in Settings, while the seam, the
        single-line navbar, the scroll-pinning and the scrollbar-position observations are all best
        taken on a long, scrollable route. Does navigating away and back per theme disturb any
        other item's evidence, and does any per-theme cell require a route change the contract does
        not currently ask for?
      · expanding a tier-2 disclosure (item 3) versus item 1's scroll observations AND versus item
        3's own later sub-checks — an applied category or filter shortens the Games grid, which is
        exactly the precondition (P8) item 1's scrollbar and back-to-top sub-checks depend on
      · toggling `framelessWindow` (item 2) versus items 1, 3, 4 — restate run 1/2's finding
      · quitting Tauri to launch Electron (item 5) versus items 1-4's recorded verdicts
      · **and one class this project has paid for specifically**: any requirement in this contract
        that restarts, truncates or clears something another item's evidence lives in. State
        explicitly whether Test 3 (log-line emitter reachability) is N/A for this contract as it was
        for runs 1 and 2, and if any sub-check now depends on a terminal transcript or
        `gamelib.log`, apply the dual-sink append-and-archive standard rather than a truncating
        `tee`.

    Record the count of pairs considered and the count flagged. Then write a
    **Run order, derived (not asserted)** paragraph that follows from the DESTROYS rows, and update
    the frontmatter `run_order` field only if the derivation actually changes it — if it does not,
    say so explicitly rather than silently leaving it.

    Do not fill any verdict slot. This section is review, not measurement.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md';const s=fs.readFileSync(p,'utf8');const nine=s.slice(s.indexOf('## 9.'));for(const h of ['### 9.3','### 9.4'])if(!nine.includes(h))throw new Error('missing '+h);if(!/Pairs considered/i.test(nine))throw new Error('pairwise review records no pair count');if(!/Pairs flagged/i.test(nine))throw new Error('pairwise review records no flagged count');if(!/DESTROYS/.test(nine))throw new Error('no DESTROYS column outcome recorded');if(!/derived/i.test(nine))throw new Error('run order is asserted, not derived');if(!/Test 3/.test(nine))throw new Error('Test 3 applicability not stated');if(!/Test 4/.test(nine))throw new Error('Test 4 absence-observability not applied');console.log('reachability and pairwise reviews authored')"</automated>
  </verify>
  <done>§9.3 gives every run-3 sub-check a reachability verdict with source evidence; §9.4 records the state-mutating actions, a DESTROYS-column table, pair counts considered and flagged, an explicit Test 3 applicability statement, and a run order derived from the findings.</done>
</task>

<task type="auto">
  <name>Task 3: Write run 3's item bodies with empty verdict slots and register the run in frontmatter</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md § 7 (run 2's item bodies — the table shapes and verdict-line conventions to follow)
    - the §9.1-§9.4 sections written by tasks 1 and 2 (the scope and run order these bodies must implement exactly)
  </read_first>
  <action>
    Append `### 9.5 Run 3 — the items` to §9, with one sub-section per item, ordered by the run
    order §9.4 derived. Restate at the top of §9.5, so the operator does not have to scroll: the
    phase closes only on 5/5; `verdict: pass` may be set only if `items_passed == 5`; NOT ATTEMPTED
    is recorded with its reason and never inferred in either direction; every verdict is written
    into this file **as it is measured**, not batched at the end.

    Every sub-check gets a verdict cell containing exactly the literal placeholder
    `PENDING — run 3` and a notes cell. Use exactly `PASS`, `FAIL` or `NOT ATTEMPTED` as the only
    permitted final values, and say so. Item 1's per-theme table needs four theme rows × two
    columns (seam, idle ring), plus a separate table for its four finding sub-checks
    (F-34.10-03, F-34.10-04, F-34.10-06(a), F-34.10-06(b)) and the back-to-top regression
    sub-check. Item 3 needs all seven original sub-checks plus the F-34.10-05 background sub-check
    with its own per-theme cells for `gruvbox_dark` and `dracula`.

    Write each item's steps as numbered operator instructions with the exact route, the exact
    control and the expected observation — not a description of intent. For item 1 in particular,
    spell out the per-theme loop concretely enough that the operator cannot accidentally judge the
    seam in one theme and generalise: for each of the four themes, switch the theme in
    Settings -> Accessibility, navigate to the route the reachability review named, click all four
    tier-1 tabs, and record BOTH cells before moving to the next theme. State that a general,
    unscoped report ("looks fine") is not a per-theme measurement and must be recorded NOT
    ATTEMPTED — this is exactly what happened in run 2 and it cost the sweep a third time.

    Then update the frontmatter, and only these fields:
      - append a `runs:` entry for run 3 with `run: 3`, today's date, `verdict: null`,
        `authored_by: "34.10-21"`, `run_by: "34.10-22 (a separate, later invocation — decision D-E)"`,
        and a note saying the results are not yet recorded
      - set `run_number: 3`
      - reset the `items:` list's `status` fields to `pending` and `items_passed: 0`, leaving
        `verdict: fail` unresolved until run 3 is scored — and add an inline note that runs 1 and 2's
        headline numbers are preserved in the `runs:` history so nothing is lost by the reset
      - leave the `findings:` list alone except to confirm F-34.10-03..06 remain open; do not mark
        any of them closed here. **A finding is closed by a measurement, not by a fix landing.**

    Do not write a verdict anywhere. Do not fill the §9.2 preflight results block. Do not touch
    §0-§7. When finished, state in the SUMMARY that the contract is authored and unrun, and that
    plan 34.10-22 — a separate, later invocation — is the only thing permitted to fill it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md';const s=fs.readFileSync(p,'utf8');const nine=s.slice(s.indexOf('## 9.'));if(!nine.includes('### 9.5'))throw new Error('missing §9.5 item bodies');const slots=(nine.match(/PENDING — run 3/g)||[]).length;if(slots<20)throw new Error('only '+slots+' empty verdict slots — item 1 alone needs 8 per-theme cells plus 5 sub-checks');if(/\|\s*(PASS|FAIL)\s*\|/.test(nine))throw new Error('a verdict was filled by the contract author — D-E violation');const fm=s.slice(0,s.indexOf('---',3));if(!/run_number:\s*3/.test(fm))throw new Error('run_number not advanced to 3');if(!/items_passed:\s*0/.test(fm))throw new Error('items_passed not reset for run 3');if(!/34\.10-22/.test(fm))throw new Error('run_by does not name the separate runner plan');for(const f of ['F-34.10-03','F-34.10-04','F-34.10-05','F-34.10-06'])if(!fm.includes(f))throw new Error('finding '+f+' missing from frontmatter');console.log('run-3 item bodies authored, all slots empty')"</automated>
  </verify>
  <done>§9.5 holds every run-3 item body with at least 20 `PENDING — run 3` slots and no filled verdict; frontmatter carries `run_number: 3`, `items_passed: 0`, a run-3 `runs:` entry naming 34.10-22 as the runner, and F-34.10-03..06 still open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract author → contract runner | An author who also runs can rationalise an unreachable item into a PASS; D-E exists because this project has paid for it |
| run 2's contract → run 3's contract | A copied scope silently re-inherits assumptions written before four fixes existed |
| individually-valid requirements → each other | A contract can be defective through the INTERACTION of two valid requirements; item-by-item review is structurally incapable of catching it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-90 | Elevation of privilege | this plan filling its own verdict slots | mitigate | Task 3's automated check fails if any `\| PASS \|` or `\| FAIL \|` cell exists in §9, and requires ≥20 `PENDING — run 3` slots |
| T-34.10-91 | Repudiation | a per-theme sweep "measured" from one general report | mitigate | Item 1's steps require both cells recorded before moving to the next theme, and the contract states that an unscoped report is recorded NOT ATTEMPTED — the exact failure of run 2 |
| T-34.10-92 | Tampering | a contract defective through requirement interaction | mitigate | §9.4 is mandatory, its unit of review is the pair, it must record pairs considered and flagged, and the run order must be derived from the DESTROYS rows |
| T-34.10-93 | Spoofing | a half-captured run that looks measured because a second instance split the sink | mitigate | P3 requires and records `pgrep` on both process names before the first launch and again before the Electron launch |
| T-34.10-94 | Information disclosure | a stale jest baseline making a real regression look expected | mitigate | P1 requires the expected counts be DERIVED from 34.10-18/-19/-20's SUMMARYs; task 1's check fails if run 2's 218/4246 is copied |
| T-34.10-95 | Repudiation | a finding marked closed because a fix landed rather than because it was measured | mitigate | Task 3 forbids closing any of F-34.10-03..06 in this plan and states that closure requires a measurement |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists in this plan |
</threat_model>

<verification>
- `git diff --name-only` shows exactly one path: `34.10-LIVE-GATE.md`.
- §0 through §7 are byte-identical to their pre-plan state (`git diff` shows additions only, after
  the frontmatter and at the end of the file).
- No source file is touched, so `pnpm jest --runInBand --silent --forceExit` must be unchanged;
  record the observed counts in the SUMMARY because P1's expected baseline is derived from them.
- No user-facing string is introduced; `meta/i18nGateScope.json` stays clean.
</verification>

<success_criteria>
- §9 exists with 9.1 scope, 9.2 preflight (P1-P8, results block empty), 9.3 reachability review, 9.4 pairwise review, 9.5 item bodies
- Item 1's scope covers all four themes on both the seam and the idle ring, plus one observable sub-check per finding and the back-to-top regression check
- Items 2, 4 and 5 carry a stated, justified scope — nothing is skipped without a written reason
- The pairwise review records pairs considered and flagged, and the run order is derived from it
- Every verdict slot reads `PENDING — run 3`; frontmatter carries `run_number: 3`, `items_passed: 0`, and names 34.10-22 as the runner
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-21-SUMMARY.md` when done
</output>
