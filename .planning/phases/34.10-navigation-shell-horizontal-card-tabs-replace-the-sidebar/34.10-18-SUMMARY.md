---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 18
subsystem: ui
tags: [css, grid, scroll, tauri, wkwebview, navigation-shell]

# Dependency graph
requires:
  - phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
    provides: "34.10-17's live box-model measurement (34.10-F04-DIAGNOSIS.md) confirming F-34.10-03 and F-34.10-06 as source-level, not layout-row, defects"
provides:
  - "Completed card/folder seam recipe: .NavShell__navbar now carries border-bottom: 1px solid var(--divider) for the active tab's erasure border to erase"
  - ".App .content is now the scroll container (overflow-y: auto; min-height: 0) instead of document.body; .App is height: 100vh; overflow: hidden so it can never grow past the viewport"
  - "Navbar is structurally outside the scrolling region (own min-content grid row above the scroll container), not via position: sticky"
  - "Both active document.body scroll consumers under src/frontend migrated: Library's scroll-position-restore/back-to-top effects and GamesList's gamepad scrollCardIntoView"
  - "appShellLayout.test.ts: 9 comment-immune source gates (10 tests) pinning the seam recipe, the scroll relocation, the F-10 percentage prohibition, the dead-sticky prohibition, and document.body migration per-file AND whole-tree"
affects: ["34.10-19 (tab geometry fix)", "34.10-20 (dropdown panel token)", "34.10-21 (verification run 3)", "34.10-22 (live gate)"]

tech-stack:
  added: []
  patterns:
    - "Nested-brace-aware CSS block extraction (extractBlock helper) for source-text gates on rules containing nested selectors, rather than a naive [^}]* regex that truncates at the first inner brace"
    - "Scroll container resolved inside effect bodies / at call time via document.querySelector('main.content') ?? document.body, never at module scope"

key-files:
  created:
    - src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts
  modified:
    - src/frontend/components/UI/NavShell/index.scss
    - src/frontend/App.css
    - src/frontend/screens/Library/index.tsx
    - src/frontend/screens/Library/components/GamesList/index.tsx
    - src/frontend/screens/Login/__tests__/index.test.tsx

key-decisions:
  - "GamesList's scrollCardIntoView proximity thresholds now compare against the container's own getBoundingClientRect() (containerRect.top / containerRect.bottom) instead of window.innerHeight/0, because the scroll container no longer spans the full window (navbar above, controller row below)"
  - "Library's two scroll effects resolve main.content once per effect run, inside the effect body (not module scope), with a document.body fallback for console mode / any future navless shell"
  - "position: sticky on the navbar was deliberately NOT attempted -- documented in an App.css comment as a dead end (zero travel in a min-content grid row)"

patterns-established:
  - "Container-relative scroll arithmetic: destination = container.scrollTop + (element rect delta from container rect), replacing offsetTop-based math that silently breaks when the offsetParent chain no longer matches the actual scroll container"

requirements-completed: [REQ-34.10-06]

duration: ~20min
completed: 2026-08-08
---

# Phase 34.10 Plan 18: Navbar seam border + scroll-container relocation Summary

**Completed the card/folder tab seam recipe with a navbar border-bottom, and relocated the page's scroll container from `document.body` to `.App .content` so the navbar can no longer scroll away or be covered by body's scrollbar — migrating both active `document.body` scroll consumers (Library's restore/back-to-top effects, GamesList's gamepad focus-scroll) in the process.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 3 completed
- **Files modified:** 5 (1 created, 4 modified) + 1 pre-existing test updated as a direct consequence

## Accomplishments

- `.NavShell__navbar` now declares `border-bottom: 1px solid var(--divider)`, completing F-34.10-03's missing half of the card/folder seam recipe (`NavTabs/index.scss`'s `.Mui-selected` erasure border was already shipped and untouched by this plan).
- `.App` changed from `min-height: 100vh` (auto, unbounded height) to `height: 100vh; overflow: hidden`; `.App .content` changed to `overflow-y: auto; min-height: 0`, making it the real scroll container. `document.body`'s own scrollbar has nothing left to scroll once `.App` is fixed at `100vh`.
- Both real `document.body` scroll consumers under `src/frontend` migrated: Library's scroll-position-restore and back-to-top-visibility effects, and GamesList's gamepad-only `scrollCardIntoView` — the latter is the consumer a `Library/index.tsx`-scoped review would have missed, per the plan's own warning.
- `appShellLayout.test.ts` created with 9 `it`/`it.each` blocks (10 tests total) pinning: both halves of the seam recipe, no literal colours, `.App`/`.App .content` ownership, the F-10 percentage-height prohibition, the dead-sticky prohibition, and `document.body` migration both per-file (Library, GamesList) and whole-tree (every `.ts`/`.tsx` under `src/frontend` outside `__tests__`).
- A pre-existing F-10 regression test (`Login/__tests__/index.test.tsx`) that asserted the now-removed `.App { min-height: 100vh }` was updated to assert the new, strictly stronger `.App { height: 100vh }` guarantee instead — auto-fixed as a direct consequence of Task 2's change (Rule 1).
- Full suite green: **219/219 suites, 4256/4256 tests** (baseline was 218/218 suites, 4246/4246 tests per `34.10-15-SUMMARY.md`, the most recent full-suite record in this phase; the delta is exactly the 1 new suite / 10 new tests this plan adds).
- `pnpm codecheck` (`tsc --noEmit`) clean. `meta/i18nGateScope.json` stayed clean (no user-facing string added).

## Task Commits

1. **Task 1: Complete the card/folder seam recipe** - `1139bf1db` (fix)
2. **Task 2: Move the scrolling region and migrate document.body consumers** - `0ff4fe6fd` (fix)
3. **Task 3: Pin the seam recipe, scroll ownership and F-10 prohibition in source gates** - `a821ed545` (test)

## Files Created/Modified

- `src/frontend/components/UI/NavShell/index.scss` - `.NavShell__navbar` gains `border-bottom: 1px solid var(--divider)` with a comment naming the erasure mechanic
- `src/frontend/App.css` - `.App` becomes `height: 100vh; overflow: hidden`; `.App .content` becomes `overflow-y: auto; min-height: 0`; F-10 comment block preserved verbatim with new reasoning appended
- `src/frontend/screens/Library/index.tsx` - both scroll effects resolve `main.content` (with `document.body` fallback) inside the effect body instead of hardcoding `document.body`
- `src/frontend/screens/Library/components/GamesList/index.tsx` - `scrollCardIntoView` resolves the container at call time and computes both scroll destinations relative to the container's own rect instead of `window.innerHeight`/`offsetTop`
- `src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts` - new, 9 source-text gates / 10 tests
- `src/frontend/screens/Login/__tests__/index.test.tsx` - one assertion updated from the removed `min-height: 100vh` to the new `height: 100vh`

## Decisions Made

- **Container-relative arithmetic for `scrollCardIntoView`, not a target swap alone.** The plan explicitly warned that swapping only the scroll target while keeping `offsetTop`-based math would convert a silent no-op into a silent wrong-offset scroll. Both destinations are now computed as `container.scrollTop + (element rect delta from container rect)`, using the `rect`/`containerRect` already available in the function.
- **Proximity thresholds compare against the container's own rect, not `window.innerHeight`.** The scroll container no longer spans the full window (navbar above it, controller row below it), so a window-relative "too close to the edge" test would fire at the wrong point. Documented in a comment at the call site.
- **`position: sticky` on the navbar was not attempted**, per the plan's explicit prohibition — documented as a dead end directly in `App.css` so a future "improvement" doesn't re-add it.
- **`--divider` reused as the navbar border's token**, matching the token `.Mui-selected` already uses for `border-color`, so the two recipe halves cannot drift apart per-theme (navigation-shell.md § 3's navbar-lighter-than-body inversion table was the reason a fixed colour was rejected).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated Login's F-10 regression test that asserted the now-removed `.App { min-height: 100vh }`**
- **Found during:** Task 2 verification (full-suite run before Task 3)
- **Issue:** `src/frontend/screens/Login/__tests__/index.test.tsx` (34.4.1's F-10 regression suite) asserted `cssBlock(read(APP_CSS), '.App')` matches `/min-height:\s*100vh/`. Task 2 replaced `.App`'s `min-height: 100vh` with `height: 100vh; overflow: hidden`, so the assertion started failing against the new (correct) stylesheet.
- **Fix:** Updated the assertion to `/height:\s*100vh/` and rewrote the test's comment to explain the new mechanism: `height: 100vh` (fixed) is a strictly stronger viewport-tall guarantee than the old `min-height: 100vh` (which allowed unbounded growth) was, so the test's own stated intent — guard against the shell collapsing below viewport height — still holds under the new declaration.
- **Files modified:** `src/frontend/screens/Login/__tests__/index.test.tsx`
- **Verification:** `pnpm jest --runInBand --silent --forceExit src/frontend/screens/Login/__tests__/index.test.tsx` — 18/18 passing. Full suite re-run afterward: 219/219 suites, 4256/4256 tests.
- **Committed in:** `a821ed545` (Task 3 commit, alongside the new `appShellLayout.test.ts` since both are test-only changes)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug in a pre-existing test's stale assumption)
**Impact on plan:** Necessary to keep the full suite green after Task 2's intended, plan-mandated `.App` change; no scope creep — the fix touches only the one stale assertion, not the test file's other 17 tests.

## Issues Encountered

None beyond the deviation above.

## Verification (plan-level, per the plan's own `<verification>` block)

- `pnpm codecheck` — clean.
- `grep -rn "document\.body\." src/frontend --include="*.ts" --include="*.tsx" | grep -v __tests__` — 21 hits, all `classList`/`className`/`insertAdjacentElement`; zero scroll usage. Confirmed both before and after the migration (before: 2 files had the scroll-related subset; after: those same 2 files' scroll hits are gone).
- `pnpm jest --runInBand --silent --forceExit` (full repo) — **219/219 suites, 4256/4256 tests**, all green. Baseline was 218/218 suites, 4246/4246 tests (`34.10-15-SUMMARY.md`, the most recent full-suite record in this phase — `34.10-17-SUMMARY.md` was diagnosis-only and recorded no full-suite run). The delta (+1 suite, +10 tests) is exactly `appShellLayout.test.ts`'s own new suite/tests.
- `git status` shows `meta/i18nGateScope.json` clean — no user-facing string was added by this plan.
- **Per the plan's own instruction: nothing above proves F-34.10-06 or F-34.10-03 fixed.** The `Frontend` jest project is `testEnvironment: 'node'` with no jsdom/CSS engine — every gate in `appShellLayout.test.ts` pins source text only. REQ-34.10-06 is NOT ticked from this plan; the live adjudicator is plan 34.10-22's live gate.

## Known Stubs

None introduced by this plan.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries were introduced. All edits are CSS layout/scroll-container relocation and their pinning tests, matching the plan's own threat register (all `mitigate` dispositions already addressed by the tasks above).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 34.10-19 (tab geometry) can now proceed without colliding with this plan's navbar border edit — `NavTabs/index.scss` was read but not modified here.
- Plan 34.10-22's live gate is the required adjudicator for both F-34.10-03 and F-34.10-06; this plan's fixes are source-only and unverified live.
- H5 in `34.10-F04-DIAGNOSIS.md` found the 8px seam gap and F-34.10-04's vertical offset share the same underlying cause (the 72px tab sitting flush to the top of its 80px container) — plan 34.10-19 owns that geometry; this plan's navbar-border half of the seam recipe is independent of it and does not need to change once 34.10-19 lands.

---
*Phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar*
*Completed: 2026-08-08*

## Self-Check: PASSED

All created/modified files verified present on disk; all 4 commit hashes
(`1139bf1db`, `0ff4fe6fd`, `a821ed545`, `5cd31d8a1`) verified present in
`git log --oneline --all`.
