---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 09
subsystem: ui
tags: [react, typescript, testing, deletion, tour, i18n]

# Dependency graph
requires:
  - phase: 34.10-05
    provides: "StoresPanel / SettingsPanel destination content this plan's exact-set gate collects labels from"
  - phase: 34.10-07
    provides: "NavShell/index.tsx (HeroicVersion version-slot mount), handleGoToScreen carry-over"
  - phase: 34.10-08
    provides: "App.tsx no longer renders <Sidebar />, App.css has no .App .Sidebar rule -- the last live .Sidebar markup this plan's deletion depends on already being gone"
provides:
  - "destinationCoverage.test.tsx: exact-set destination gate over NavTabs + StoresPanel + SettingsPanel, re-homing the three surviving SidebarLinks claims"
  - "tourDisabled.test.ts: source gate proving both tour entry points (SidebarTour's own mount, HeroicVersion's former TourButton) are unreachable"
  - "The retired Sidebar tree deleted (11 files) except Sidebar/components/SidebarTour.tsx, preserved for the deferred tour-rework phase"
affects: [34.10-10, 34.10-11]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Multi-component destination-union test file: mock every hook a REAL (not mocked) component needs (react useContext/useEffect, react-router-dom, react-i18next, @mui/material, @fortawesome/react-fontawesome) so several sibling components can be invoked directly in the same suite and their real label output compared against a literal exact-set array, rather than each component only being tested in isolation"
    - "Recursive fs.readdirSync({ recursive: true }) source-text walk (Node >=20) for a forward-looking absence gate that must catch files added AFTER the plan that wrote it, not just the files that existed when it was written"

key-files:
  created:
    - src/frontend/components/UI/NavShell/__tests__/destinationCoverage.test.tsx
    - src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts
  modified:
    - src/frontend/components/UI/Sidebar/components/SidebarTour.tsx
    - src/frontend/themes.scss

key-decisions:
  - "destinationCoverage.test.tsx's exact-set union deliberately excludes QuitButton's own 'Quit' label -- Quit is an action, not a navigable destination (no url), and the plan's own <action> text scopes the union to NavTabs()/StoresPanel()/SettingsPanel()'s collected element graphs, which never invoke QuitButton() (JSX creates an unexecuted element reference, not a call)"
  - "tourDisabled.test.ts's first check (no NavShell source file references the tour) scopes its target file list to NavShell's non-test .tsx files, excluding __tests__/ -- a check asserting 'no file contains this string' is unsatisfiable by construction if its own target set includes the file doing the asserting (or NavShell.test.tsx, which already names SidebarTour in its own absence-check from plan 07)"
  - "tourDisabled.test.ts's second check (no live importer) excludes the retired Sidebar/ tree from its target set -- those files are dead code already unreachable from any render path (34.10-08 replaced <Sidebar/> with <NavShell/> in App.tsx) and this same plan's Task 3 deletes them; scanning a file this plan is about to delete would make Task 2's own verification depend on Task 3 having already run, a plan-internal ordering contradiction rather than a real reachability gap"
  - "Removed themes.scss's dracula-theme .Sidebar .tour-button override (not in this plan's files_modified list) as a Rule 1 fix: Task 3 removes the last .Sidebar markup from the DOM, so this nested selector -- which could only ever match a .tour-button rendered under a .Sidebar ancestor -- became permanently unreachable dead CSS, caught directly by the plan's own verification (grep -rn \"\\.Sidebar\" src/frontend src/preload). The general .tour-button rule two lines above was left untouched since it still styles the unrelated, still-live Library tour's TourButton (ActionIcons/index.tsx)"

requirements-completed: [REQ-34.10-11, REQ-34.10-13, REQ-34.10-15]

# Metrics
duration: ~35min
completed: 2026-08-08
---

# Phase 34.10 Plan 09: Destination-coverage gate, tour-disabled gate, delete the retired Sidebar tree Summary

**Two new test suites (`destinationCoverage.test.tsx`, `tourDisabled.test.ts`) replace the deleted `SidebarLinks` suite's coverage and prove the onboarding tour has no reachable entry point, then the entire retired `Sidebar` tree (11 files) is deleted except the preserved `SidebarTour.tsx`.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-08T08:41:00+12:00
- **Completed:** 2026-08-08T08:55:00+12:00
- **Tasks:** 3 completed
- **Files modified:** 4 (2 created, 1 modified for the tour comment, 1 unrelated-but-caught themes.scss fix) + 11 deleted

## Accomplishments

- `destinationCoverage.test.tsx` (9 tests): invokes `NavTabs()`, `StoresPanel()`, `SettingsPanel()` and `NavShell()` directly (no jsdom) and asserts the union of their collected labels equals a literal 22-entry array exactly (`toEqual`, not `toContain`) -- 4 tab labels + 8 Stores rows + 10 Settings rows, in a permissive context (Humble logged in, Steam session, non-Windows) so every guarded row is forced to appear. Re-homes the three surviving `SidebarLinks` claims with named tests (Manage Accounts targets `/login` in both auth states with no label ever "Login"; no "Discord"/"GitHub Sponsors" with "Ko-fi" present; `HeroicVersion` present in `NavShell()`'s tree for all five active-tab values including `null`), references `StoresPanel.test.tsx` for the already-covered Redeem-a-Steam-key claim instead of duplicating it, and records `labels[0] === 'GameLib'` and the Manage-Accounts-before-Accessibility adjacency claim as deliberately obsolete (no successor -- the flat list they described no longer exists). Live-verified the gate's bite: temporarily removed `SettingsPanel`'s "Advanced" row, confirmed the suite failed with a precise diff, then reverted.
- `tourDisabled.test.ts` (12 tests): a source-text gate (`stripSourceComments` + `readFileSync`, following `Login/__tests__/index.test.tsx`'s idiom) proving both of the retired navigation's tour entry points are gone -- `SidebarTour`'s own mount (`Sidebar/index.tsx`, deleted by Task 3) and the second, easy-to-miss one, `HeroicVersion`'s former `TourButton` (already removed by plan 34.10-07). Recursively walks every non-test `.tsx` file under `NavShell/` asserting none contains `SidebarTour`, `SIDEBAR_TOUR_ID`, `TourButton` or `data-tour`, and separately scans all of `src/frontend` (excluding the retired `Sidebar/` tree, deleted in this same plan's Task 3) confirming no file imports `SidebarTour.tsx`. Live-verified: added a temporary `data-tour` attribute to `GamesPanel`, confirmed the suite failed, reverted.
- `SidebarTour.tsx` gains a header comment recording its disabled status (D-13), naming the deferred tour-rework phase's work-list, and flagging the pre-existing duplicate `data-tour="sidebar-downloads"` value (both `Sidebar/index.tsx`'s `currentDownloads` wrapper and `SidebarLinks`' "Downloads" row carried the same selector) so the rework phase doesn't rediscover it. No other change to the file -- its step text and i18n keys are untouched.
- Deleted the entire retired `Sidebar` tree except `components/SidebarTour.tsx`: `index.tsx`, `index.scss`, `CurrentDownload/`, `HeroicVersion/`, `QuitButton/`, `SidebarItem/`, `SidebarLinks/` (11 files, 1444 lines). Verified each capability's successor before deleting: DM queue subscription lives in `DownloadsRing` (34.10-03), `handleGoToScreen` in `NavShell` (34.10-07, with the leak fixed), Quit and both tier-2 panels under `NavShell` (34.10-05), `HeroicVersion` under `NavShell` (34.10-07), the row primitive as `NavItem` (34.10-02). The `sidebar-width` drag-resize has no successor by design (tier 2 is a fixed-width column). `App.tsx` and the UI barrel already had zero references (34.10-08).
- Zero regressions from this plan's own scope: `pnpm codecheck` (tsc) clean, `npx eslint` clean on every touched file, `pnpm lint-translations` exits 0 (only the pre-existing unrelated empty-translation warnings). The two new suites pass in full (21/21 tests).

## Task Commits

1. **Task 1: Destination-coverage suite** - `d687272f4` (test)
2. **Task 2: Tour-disabled gate** - `23af7a3e5` (test)
3. **Task 3: Delete the retired Sidebar tree** - `0559bc0d0` (feat)
4. **[Deviation] Remove orphaned dracula `.Sidebar .tour-button` CSS** - `70db444ee` (fix)

## Files Created/Modified

- `src/frontend/components/UI/NavShell/__tests__/destinationCoverage.test.tsx` -- 9 structural tests, direct-invoke, no jsdom
- `src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts` -- 12 structural/source-text tests, direct-invoke, no jsdom
- `src/frontend/components/UI/Sidebar/components/SidebarTour.tsx` -- header comment only (status recorded); step array and i18n keys unchanged
- `src/frontend/themes.scss` -- removed the now-unreachable `.Sidebar .tour-button` nested override
- Deleted: `Sidebar/index.tsx`, `Sidebar/index.scss`, `Sidebar/components/{CurrentDownload,HeroicVersion,QuitButton,SidebarItem,SidebarLinks}/*`

## Decisions Made

See `key-decisions` in the frontmatter -- four narrowing/scoping decisions, all resolving plan-internal contradictions inherent to writing an absence-gate about itself or about files being deleted in the same plan, plus one out-of-plan CSS cleanup the plan's own verification directly caught.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Two ESLint errors in the new test files, found during full-repo verification**
- **Found during:** Task 3's `pnpm jest --runInBand` + `npx eslint` pass
- **Issue:** `destinationCoverage.test.tsx` declared `effectCleanup` but never read it (no test in that file exercises the effect's cleanup return -- that behavior is already covered by `NavShell.test.tsx`). `tourDisabled.test.ts` had a redundant `as string` type assertion on an already-inferred `string` parameter.
- **Fix:** Removed the unused `effectCleanup` variable and its assignment (the mock `useEffect` now just invokes the effect, discarding the result); removed the redundant assertion.
- **Files modified:** Both new test files (folded into their Task 1/2 commits was not possible since the lint pass ran after Task 3's full-suite check discovered them -- committed instead as part of Task 3's commit).
- **Verification:** `npx eslint` clean on both files; both suites re-run green (21/21).
- **Committed in:** `0559bc0d0` (Task 3 commit)

**2. [Rule 1 - Bug] Orphaned `.Sidebar .tour-button` CSS override in `themes.scss`, outside this plan's `files_modified` list**
- **Found during:** Task 3's own verification grep (`grep -rn "\.Sidebar" src/frontend src/preload`, from the plan's `<verification>` section)
- **Issue:** The dracula theme block's `.Sidebar .tour-button { --tour-button-color: var(--primary-active); }` rule could only ever match a `.tour-button`-classed element nested under a `.Sidebar`-classed ancestor. Task 3 removes the last `.Sidebar` markup from the live DOM (34.10-08 had already removed the render call; this plan removes the source file), making the selector permanently unreachable.
- **Fix:** Deleted the three-line dead rule. The general `.tour-button { --tour-button-color: var(--accent); }` rule immediately above was left untouched -- confirmed still live, styling `ActionIcons/index.tsx`'s `library-tour-button` (the unrelated, still-active Library tour).
- **Files modified:** `src/frontend/themes.scss`
- **Verification:** `grep -rn "\.Sidebar" src/frontend src/preload` now returns only historical doc-comment mentions in already-committed prior-plan files (see Issues Encountered); brace balance re-checked programmatically; `npx tsc --noEmit` clean.
- **Committed in:** `70db444ee` (separate commit, this plan's fourth)

---

**Total deviations:** 2 auto-fixed (both Rule 1). Neither changes any component's behavior; both are cleanup caught by this plan's own verification commands, not scope creep.
**Impact on plan:** No scope change. All three tasks' `<behavior>`/`<action>`/acceptance criteria are satisfied as written, with the narrowing decisions documented above where the plan's literal text would otherwise be self-contradictory.

## Issues Encountered

- **Plan-internal contradiction (resolved by narrowing, not by asking):** Task 2's acceptance criterion `grep -rn "SidebarTour\|SIDEBAR_TOUR_ID" src/frontend --include=*.tsx --include=*.ts matches only SidebarTour.tsx itself` cannot be literally satisfied by any test file that implements the gate (the gate's own source must contain the forbidden tokens to search for them), nor by `NavShell.test.tsx` (already shipped by plan 34.10-07, which names `'SidebarTour'` in its own absence-check). Resolution: scoped `tourDisabled.test.ts`'s content-scan target set to exclude `__tests__/`, consistent with this phase's established "own text trips its own literal grep" pattern (documented identically in plans 02, 04, 05, 07, 08's summaries) -- the *intent* (no live source reference) is satisfied; the literal repo-wide grep still matches test files that describe the absence, which is expected and not a defect.
- **Same class, second instance:** `grep -rn "components/UI/Sidebar" src/ ... matches nothing except SidebarTour.tsx's own path` and `grep -rn "SidebarLinks|SidebarItem|CurrentDownload" src/frontend matches nothing` (Task 3's acceptance criteria) also match historical doc-comment attributions in already-committed prior-plan files (`NavTabs/index.tsx`, `navTabs.ts`, `NavItem/index.scss`, `NavItem.test.tsx`, `NavTabsComponent.test.tsx`, `GamesPanel.test.tsx`, `StoresPanel.test.tsx`, `DownloadsRing.test.tsx`, `navTabs.test.ts`, plus this plan's own `destinationCoverage.test.tsx`/`tourDisabled.test.ts`), all referencing the old component names for porting-history context, not live imports. Confirmed via a live-import-pattern grep (`from\s+['"].*Sidebar/...`) that zero actual `import` statements reference any deleted path. Editing 8+ already-committed files' doc comments from prior plans is out of this plan's scope (`files_modified` boundary) for zero functional benefit.
- **`meta/i18nGateScope.json` is now stale, as the plan's own `<known_traps>` anticipated:** two suites fail after Task 3 -- `meta/__tests__/hardcodedStringGate.test.ts` (4 tests) and `meta/__tests__/genI18nGateScope.test.ts` (1 test) -- because the committed scope snapshot still lists four now-deleted paths: `src/frontend/components/UI/Sidebar/components/HeroicVersion/index.tsx`, `src/frontend/components/UI/Sidebar/components/SidebarItem/index.tsx`, `src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx`, `src/frontend/components/UI/Sidebar/index.tsx`. This is explicitly out of this plan's scope (`meta/i18nGateScope.json` is not in `files_modified`, and the executor prompt names this exact trap as owned by plan 34.10-10). **Plan 34.10-10 must regenerate the scope snapshot and remove these four entries** before its own gate work can proceed cleanly.
- **Full-suite accounting is contaminated by unrelated, pre-existing uncommitted work already present in this working tree** (not touched, not committed by this plan): modified `src-tauri/src/main.rs`, `src/backend/humble/{user.ts,loginWindowSeam.ts,__tests__/user.test.ts}`, `src/frontend/screens/WebView/{index.tsx,components/TauriLoginPanel.tsx,components/__tests__/TauriLoginPanel.test.tsx}`, plus an untracked `src/frontend/screens/WebView/__tests__/HumbleLoginWatchErrorHandling.test.ts` and `.planning/debug/humble-isloggedin-never-set.md` (matching the open Humble `isLoggedIn` investigation tracked separately). Because of this, this plan's own accounting is reported as a **relative delta**, not an absolute global count: this plan removed 1 suite/8 tests (`SidebarLinks/__tests__/index.test.tsx`) and added 2 suites/21 tests (`destinationCoverage.test.tsx` 9, `tourDisabled.test.ts` 12) -- net **+1 suite / +13 tests** attributable to this plan. `pnpm jest --runInBand --silent --forceExit` run at Task 3's completion (including the unrelated contamination) reports 216 suites / 4214 tests, 214 suites / 4209 tests passing, with the 5 failures confined entirely to the two known-stale-scope suites above (verified identical failure set across two consecutive runs).

## User Setup Required

None -- no external service configuration required.

## Next Phase Readiness

- **Plan 34.10-10 has a concrete, pre-diagnosed blocker to open with:** regenerate `meta/i18nGateScope.json` to drop the four now-deleted `Sidebar` paths listed above before attempting any gate-scope work of its own -- both `hardcodedStringGate.test.ts` and `genI18nGateScope.test.ts` will otherwise fail immediately on `ScopeLoadError`/missing-file assertions, unrelated to whatever 34.10-10 itself changes.
- The retired `Sidebar` tree is gone except `components/SidebarTour.tsx`, confirmed via `find`, three scoped greps, and a live-import-pattern check -- no functional regression, only expected historical doc-comment mentions remain in already-committed prior-plan files (out of scope to edit).
- `destinationCoverage.test.tsx` and `tourDisabled.test.ts` are both forward-looking gates (recursive directory walks, not hardcoded file lists) -- a future plan adding a new NavShell source file with a `data-tour` attribute, or dropping a settled destination row, fails the suite automatically rather than shipping silently.
- Plan 34.10-11 (this phase's final plan, per the phase's own dependency graph) can proceed once 34.10-10 clears the stale scope snapshot; no other blocker from this plan's scope.

---
*Phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar*
*Completed: 2026-08-08*

## Self-Check: PASSED

Both created files verified present on disk (`destinationCoverage.test.tsx`, `tourDisabled.test.ts`);
`Sidebar/index.tsx` verified deleted; `Sidebar/components/SidebarTour.tsx` verified preserved;
all 4 commits (`d687272f4`, `23af7a3e5`, `0559bc0d0`, `70db444ee`) verified present in
`git log --oneline --all`.
