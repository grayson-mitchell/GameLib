---
findings:
  - id: "F-34.10-01"
    item: 3
    severity: blocking
    summary: "Tier-2 Categories/Filters dropdowns open on hover and overflow off the left edge of the window, unusable in the 204px column"
    cause: "Dropdown/index.scss `.dropdown{position:absolute;right:0;min-width:250px}` inside --tier2-width:204px, plus popUpOnHover at CategoryFilter:78 and LibraryFilters:265"
    status: closed
    closed_by: "34.10-13, confirmed by run-2 live measurement"
  - id: "F-34.10-02"
    item: 4
    severity: blocking
    summary: "DownloadsRing shows no ring at all — invisible at rest, a solid disc on hover, and no progress fill during an active download; only the count number renders"
    cause: "(a) ::after hole paints var(--navbar-background) while :hover sets the parent to var(--navbar-active-background), and idle track (--divider) has near-zero contrast at opacity 0.5; (b) --dl-progress never leaves 0turn because hasProgress() yields no percent for the head element, though the queue subscription driving the count is live"
    status: closed
    closed_by: "34.10-14 (cause a) + 34.10-15 (cause b), confirmed by run-2 live measurement -- the two risks 34.10-15-SUMMARY.md flagged as unresolved are FALSIFIED"
  - id: "F-34.10-03"
    item: 1
    severity: blocking
    status: "fixed, awaiting gate measurement"
    summary: "Visible ~10px gap/seam between the tier-1 tab strip and the content region below it -- the card/folder tab merge illusion REQ-34.10-06 requires depends on there being no seam at all. RUN 3 UPDATE: STILL PRESENT. Operator's verbatim words, 2026-08-08: 'still a gap'. Measured against a bundle verified to contain 1139bf1db (the seam border) and 4a3a3d830 (its token correction), so this is NOT a stale-bundle artifact. Run 3's completed four-theme sweep additionally establishes the seam is IDENTICAL across midnightMirage, gruvbox_dark and dracula -- including dracula, whose navbar/body inversion makes it the likeliest to diverge -- so the residual seam does not vary by theme. No cause is assigned here by design. RUN 4 PRE-MEASUREMENT UPDATE (2026-08-09): root cause found (GamesSettings/index.scss:40's unscoped `.MuiTabs-root{padding-bottom:var(--space-xs)}` leaking app-wide) and fix landed in `220211230` (`NavTabs/index.scss:140`'s `padding-bottom: 0` inside the nested `&.MuiTabs-root` block). Live-verified by screencapture + pngscan.py pixel decode, `navbar-seam-and-logo-offset.md` § Resolution -- but that confirmation was taken in ONE unscored theme only (a teal scheme, navbar #1c3d4a / body #091a21), NOT midnightMirage, gruvbox_dark, or dracula. NOT set to closed here."
    source: "Run 2, operator report, 2026-08-08; re-measured and still failing in run 3"
    not_closed_because: "Run-3 item 1's F-34.10-03 sub-check recorded FAIL. 34.10-18's fix landed but did not work. This finding is NOT closed on the strength of the fix having shipped -- three of this phase's findings were previously 'fixed' before a measurement contradicted the fix. RUN 4 UPDATE: the fifth fix attempt (220211230) IS confirmed live, but only in a theme this contract does not score. Closure requires run 4's own midnightMirage measurement (§10.5, item 1 Step 1). This finding has already been wrongly declared fixed by 34.10-18 once before a measurement contradicted it -- do not repeat that mistake by inferring closure from the debug session's off-theme confirmation."
  - id: "F-34.10-04"
    item: 1
    severity: blocking
    status: "fixed, awaiting gate measurement"
    summary: "The wordmark/logo and the Downloads ring wrap onto a second row instead of sharing one line with the tab strip. RUN 3 UPDATE: STILL FAILING, AND THE SHAPE HAS CHANGED. Operator's verbatim words, 2026-08-08: 'gamelib icon and download sit lower'. This describes a VERTICAL MISALIGNMENT of the wordmark and ring relative to the tab strip, not run 2's full wrap onto a second row. The change of shape is recorded as an observation only; whether 34.10-19's min-height partially improved it or altered it by another mechanism is NOT diagnosed here. Measured against a bundle verified to contain df351c087. RUN 4 PRE-MEASUREMENT UPDATE (2026-08-09): same root cause and fix as F-34.10-03 (`220211230`, `NavTabs/index.scss:140`). Operator-confirmed in `1c7a3359d`, verbatim: 'the download and wordmark read level now' -- but that confirmation was given in the same unscored teal theme, NOT midnightMirage, gruvbox_dark, or dracula. NOT set to closed here."
    source: "Run 2, operator report, 2026-08-08; re-measured, still failing with a changed shape, in run 3"
    not_closed_because: "Run-3 item 1's F-34.10-04 sub-check recorded FAIL. 34.10-19's .MuiTab-root min-height fix landed and is present in the measured bundle, but the wordmark and ring still do not share the tab strip's line. RUN 4 UPDATE: 220211230 is confirmed live and operator-confirmed by eye, but only in a theme this contract does not score. Closure requires run 4's own midnightMirage measurement (§10.5, item 1 Step 1). This finding has already been wrongly declared fixed by 34.10-19 once before a measurement contradicted it -- do not repeat that mistake by inferring closure from the debug session's off-theme confirmation."
  - id: "F-34.10-05"
    item: 3
    severity: non-blocking-to-item-3-but-real
    status: closed
    summary: "The tier-2 disclosure panel (Categories/Filters, from 34.10-13) renders a black background that breaks its styling -- almost certainly a hardcoded or wrong colour token rather than the theme chain, since it does not track the active theme. RUN 3 UPDATE: RESOLVED. Operator's verbatim words, 2026-08-08: 'panel's background is now working', confirmed on follow-up as covering 'both themes plus cyberspace oasis for good measure'."
    source: "Run 2, operator report, 2026-08-08"
    closed_by: "34.10-20 (Dropdown/index.scss `background: transparent`), CLOSED BY MEASUREMENT in run-3 item 3's F-34.10-05 sub-check -- both scored theme cells (gruvbox_dark, dracula) PASS, plus an unscored third theme (cyberspace oasis) volunteered by the operator. The third theme matters: §9.1 excluded midnightMirage from scoring precisely because --background and --navbar-background coincide there, making the original defect invisible by construction; three independent themes passing rules out the fix holding only where those tokens happen to agree."
  - id: "F-34.10-06"
    item: 1
    severity: blocking
    status: closed
    summary: "The navbar scrolls away with page content instead of staying fixed to the top of the window; when a scrollbar is active it draws OVER the navbar instead of starting below it. RUN 3 UPDATE: RESOLVED on both halves. (a) The navbar stayed fixed at the top through a full scroll of the Games grid to its end and back. (b) The scrollbar was observed active and correctly positioned below the navbar's bottom edge -- the PRESENCE case §9.3's Test-4 row required, not the 'no scrollbar was visible at all' case, which P8's confirmation independently rules out."
    source: "Run 2, operator report, 2026-08-08"
    closed_by: "34.10-18 (App.css scroll-container relocation from document.body to .App .content, 0ff4fe6fd), CLOSED BY MEASUREMENT in run-3 item 1's F-34.10-06(a) and F-34.10-06(b) sub-checks. The associated mouse/keyboard back-to-top regression sub-check ALSO passed, confirming the document.body consumer migration -- a change that fails silently by construction (a scrollTo() on a no-longer-scrolling element throws nothing, logs nothing, and fails no test on this project, whose Frontend jest project is testEnvironment: 'node' with no jsdom). The GAMEPAD half of that regression check remains NOT ATTEMPTED (P9, no controller available) and is carried forward as unmeasured; scrollCardIntoView's container-rect-relative arithmetic is exercised only by the gamepad path and is never inferred from the mouse/keyboard result."
  - id: "F-34.10-07"
    item: null
    severity: process-not-product
    summary: "Run 3's own contract (§9, authored by 34.10-21) carried four defects that its authoring-time review did not catch. (1) SCOPE/UI-REACHABILITY: §9.1 and §9.5 scope a `dracula-classic` theme spot-check, but no such option exists in the shipped theme picker -- a Test 7 (UI-level reachability) miss, the very test `live-gate-contract-authoring.md` added after F-34.4.2-17 and which had, until now, caught nothing. (2)(3)(4) VERIFICATION/PROSE COLLISIONS, a NEW defect class not in that reference's Section 2: Task 1's automated check greps all of §9 for the literal pending-placeholder string and so was tripped by the authoring prose DESCRIBING the placeholder; the same check rejects any '[ ]' in §9.2, which the preflight block's own '[x] pass / [ ] fail' multiple-choice format guarantees will always be present; and it counts verdict cells by the bare literal '| PASS |', so any Markdown emphasis around a verdict makes resolved cells invisible to it (32 resolved cells were detected as 4)."
    cause: "Plan 34.10-21 authored three automated checks and never executed any of them against a filled specimen -- only against the empty template it had just written, where they trivially pass."
    further_collisions: "Task 2's check added two more of the same class, found during reconciliation. (5) It asserts each finding's status by scanning a fixed 600-CHARACTER WINDOW after the finding's ID -- so any finding whose summary is verbose enough (as all four became once run-3 observations were appended to them, exactly as task 2 instructs) pushes its own status field out of the window and reads as missing. (6) It locates each finding by `indexOf(id)`, which matches the FIRST occurrence of that string anywhere in the frontmatter -- and finding IDs legitimately appear earlier, both in items[].findings cross-reference arrays and in the runs[] note this plan was instructed to write. Both were resolved WITHOUT altering any value: status keys were hoisted above the long summaries, and the top-level findings: block was moved ahead of runs: and items: so each finding's own entry is the first match. A js-yaml parse was run before and after to prove the reordering changed nothing semantically -- verdict fail, items_passed 4, item statuses 1:fail 2:pass 3:pass 4:pass 5:pass, finding statuses unchanged."
    status: open
    source: "Run 3, recorder observation, 2026-08-08"
    note: "Recorded as its own finding rather than folded into an existing one, per this plan's own task-1 acceptance criteria. It is typed process-not-product: no user-facing behaviour is affected, and none of the four altered any measured verdict in this run. Generalisable rule for the next contract, and a candidate addition to `live-gate-contract-authoring.md`: an automated gate check MUST be run against a filled specimen at authoring time, not merely reasoned about."
    run4_note: "34.10-26 addresses this finding's own generalisable rule directly: its `34.10-run4-contract-check.mjs` is executed against BOTH the real, unfilled `34.10-LIVE-GATE.md` (expect INCOMPLETE) and a FILLED specimen (`scratchpad/34.10-run4-filled-specimen.md`, not committed) constructed to contain all six of this finding's own recorded collision shapes (expect COMPLETE). Both outputs are recorded in `34.10-26-SUMMARY.md`. Left `open` rather than `closed`: this closes the LOOP for run 4's own checker, but the generalisable rule it names is a standing process lesson for every future contract, not a one-time fix."
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 11
type: live-gate
verdict: null
items_total: 5
items_passed: 0
run_number: 4
authored: 2026-08-08
authored_by: "34.10-11 Task 1"
run_by: "34.10-11 Task 2 (a separate, later invocation -- decision D-E: the plan/task that authors a gate contract may never also run it)"
items_reset_note: "items_passed and every items[] entry's status field below were reset to 0/pending by 34.10-26 Task 1 to track run 4. Nothing is lost by this reset: run 1's items_passed: 2, run 2's items_passed: 4, and run 3's items_passed: 4 all remain preserved, unchanged, in the runs: history list below (the run:1, run:2 and run:3 entries), and §4/§7/§9.5's own historical item bodies are untouched byte-for-byte apart from this file's append-only history. Preserved unchanged alongside the status reset: every items[] entry's existing note/run3_note field (historical record of run 3's own outcome) and findings array -- only the status scalar was touched."
runs:
  - run: 1
    date: 2026-08-08
    verdict: fail
    items_passed: 2
    authored_by: "34.10-11 Task 1"
    run_by: "34.10-11 Task 2"
    note: "Historical record. See §4 below for the full item-by-item body -- unmodified except for the pointer line task 1 of 34.10-16 added at its top. This runs: entry exists so the frontmatter itself preserves run 1's headline numbers even though items[] below has been reset to track run 2."
  - run: 2
    date: 2026-08-08
    verdict: fail
    items_passed: 4
    authored_by: "34.10-16 Task 1"
    run_by: "34.10-16 Task 2 (a separate, later invocation -- decision D-E)"
    note: "Recorded from the operator's live report, relayed by the coordinator in two messages (the second amending item 5 from NOT ATTEMPTED to PASS after it was in fact measured). See §7 below for the run-2 item-by-item body. Preflight P1-P7 results were NOT relayed in the operator's report -- §6's Preflight Results block is left unfilled and flagged as a gap in THIS run's own capture, not inferred or fabricated."
  - run: 3
    date: 2026-08-08
    verdict: fail
    items_passed: 4
    authored_by: "34.10-21"
    run_by: "34.10-22 (a separate, later invocation -- decision D-E: the plan/task that authors a gate contract may never also run it)"
    note: "RUN COMPLETE. 4/5 -- items 2, 3, 4 and 5 PASS; item 1 FAILS. Phase does NOT close (5/5 required). What changed versus run 2, stated in both directions: IMPROVED -- item 3 now PASSES in full, including sub-checks (vi) and (vii), which were NOT ATTEMPTED in BOTH prior runs and are measured here for the first time; F-34.10-05 is closed by measurement across gruvbox_dark, dracula and a volunteered third theme; F-34.10-06 is closed on both halves (navbar stays pinned, scrollbar sits below it), validating 34.10-18's scroll-container relocation -- the largest and riskiest change of the gap cycle -- including the silent-failure shapes no node-environment test on this project can observe. ALSO FIRST-EVER: item 1's four-theme sweep was completed, having been NOT ATTEMPTED in runs 1 and 2, and the full preflight P1-P9 was recorded, where run 2's was left entirely unfilled. REGRESSED/UNCHANGED -- F-34.10-03 (seam) and F-34.10-04 (wordmark and ring not on the tab strip's line) BOTH still FAIL against a bundle verified to contain their fixes, so 34.10-18's seam border and 34.10-19's tab min-height did NOT work; F-34.10-04's shape has CHANGED from run 2's full second-row wrap to a vertical misalignment, recorded as an observation without a cause. UNMEASURED -- the gamepad focus-scroll regression (P9, no controller available) and dracula-classic (the theme does not exist in the picker). A stale pre-fix bundle was caught and corrected at preflight BEFORE any scoring, which would otherwise have failed all four findings against fixes the running app never contained."
  - run: 4
    date: null
    verdict: null
    items_passed: 0
    authored_by: "34.10-25 + 34.10-26"
    run_by: "34.10-27"
    note: "AUTHORING ONLY, NOT YET RUN (decision D-E: the plan that authors a gate contract may never also run it -- 34.10-25 authored §10.1-10.4's scope/preflight/reachability/pairwise review, 34.10-26 authored §10.5's item bodies plus 34.10-run4-contract-check.mjs, and only 34.10-27 may execute the gate and fill this entry's date/verdict/items_passed and every §10.5 verdict slot). Two fixes landed since run 3: 220211230 (the fifth attempt on F-34.10-03/-04, confirmed live but only in an unscored teal theme) and 34.10-23 (rescoped the true root cause in GamesSettings/index.scss plus a repo-wide jest guard against the unscoped-selector defect class recurring). Scope carries forward items 2/3/4/5 as RE-RUN (not carried forward, per this contract's own established convention) and item 1 as the only item that can still fail the phase closed; dracula-classic is DROPPED from scope permanently (F-34.10-07, confirmed absent from the shipped theme picker)."
items:
  - id: 1
    name: "Theme survival -- card/folder tab merges into the content surface"
    requirement: "REQ-34.10-06 (D-04)"
    status: pending
    findings: ["F-34.10-03", "F-34.10-04", "F-34.10-06"]
    note: "Run 3: FAILS on two independent grounds. (1) Step 1's F-34.10-03 sub-check FAILED (operator verbatim: 'still a gap') and F-34.10-04 FAILED (operator verbatim: 'gamelib icon and download sit lower'), both against a bundle confirmed to contain their fixes. (2) All three scored themes FAILED the Step-2 seam column. PASSING within this item: F-34.10-06(a), F-34.10-06(b) and the mouse/keyboard back-to-top regression. The four-theme sweep was COMPLETED for the first time in this phase and shows the seam is identical across themes including dracula -- recorded as an observation, not a diagnosis. The gamepad focus-scroll regression is NOT ATTEMPTED (P9, no controller) and is carried forward as unmeasured, never inferred from the mouse/keyboard result."
  - id: 2
    name: "Real window dragging under WKWebView, both frameless states"
    requirement: "REQ-34.10-04"
    status: pending
    note: "Run 3: all 5 sub-checks PASS, re-driven live rather than carried forward. §9.1 deliberately refused the source-level shortcut because 34.10-18 had already committed a fix to this same .NavShell__navbar element on exactly that reasoning and been wrong in 9 of 11 themes. Notably the drag-by-a-tab exclusion held despite 34.10-19 changing .MuiTab-root's min-height, confirming live what §9.1 argued at source level."
  - id: 3
    name: "Tier-2 portal is live, not decorative"
    requirement: "REQ-34.10-09"
    status: pending
    run3_note: "Run 3: PASS. All 7 functional sub-checks passed, INCLUDING (vi) 'Games tab stays lit on a game page' and (vii) 'tier-2 column collapses with no empty 204px band' -- both measured for the FIRST time in this phase after being NOT ATTEMPTED in runs 1 and 2. Both F-34.10-05 scored theme cells (gruvbox_dark, dracula) PASS, plus a third theme (cyberspace oasis) the operator volunteered unprompted, which strengthens the generalisation §9.1 was specifically worried about. P4's deferred distinctive-title slot was filled live: 'king' / Kingdom Come."
    note: "Run 2's own note preserved below for history: F-34.10-01 CLOSED (34.10-13) -- functional contract (open on click, contained, closes, filters apply) confirmed live for both Categories and Filters. F-34.10-05 (black background), fixed source-only by 34.10-20, is now IN SCOPE for run 3 -- see §9.1/§9.5."
  - id: 4
    name: "Ambient Downloads ring"
    requirement: "REQ-34.10-08"
    status: pending
    run3_note: "Run 3: PASS. Sub-checks ii-vi all PASS, each re-driven live against a real download rather than silently inherited from run 2. The cross-referenced stand-in for sub-check (i) -- item 1's midnightMirage idle-ring cell -- also PASSED, satisfying this item's second gating condition. §9.1's reopening trigger ('if item 1's per-theme idle-ring sweep surfaces ANY FAIL') did NOT fire: all three scored idle-ring cells PASS. Sub-check (vi), the ProgressHeader discriminator, was re-asked explicitly rather than accepting an obvious typo, because it is the one cell distinguishing a genuinely-tracking ring from the --dl-progress frozen-at-0turn failure that already shipped once in this phase behind a green suite; confirmed 'YES', advancing past 0%."
    note: "Run 2's own note preserved below for history: F-34.10-02 CLOSED -- both causes (a) via 34.10-14, (b) via 34.10-15 -- confirmed by live measurement. Run 3's own scope for this item is reduced -- see §9.1/§9.5, since none of 34.10-18/-19/-20 touched DownloadsRing's own files."
  - id: 5
    name: "Electron cosmetic divergence recorded"
    requirement: "REQ-34.10-05 (D-03)"
    status: pending
    run3_note: "Run 3: PASS. Shell renders and all 4 tier-1 tabs navigate under Electron; operator verbatim: 'pass renders, navigates'. The shared CSS that DID change this cycle (App.css's scroll-container relocation, NavShell/index.scss's seam border) renders acceptably under Chromium as well as WKWebView. P3's required pre-launch re-check was in practice executed immediately AFTER the operator's report rather than before it -- the contract's sequencing was NOT achieved and this is recorded rather than glossed; the check nonetheless confirmed both pgrep probes EMPTY, so the two shells never ran concurrently and the evidence is scorable. Run 1's traffic-light observation (NO overlap, D-03 'record, don't fix') stands and was NOT re-taken."
    note: "Run 2's own note preserved below for history: shell renders AND all 4 tabs navigate, both confirmed live in run 2. Re-scoped cheap-but-optional for run 3 -- see §9.1/§9.5, since App.css (shared by both shells) changed in 34.10-18."
run_date: 2026-08-08
run_order: [1, 3, 4, 2, 5]
---

# Phase 34.10 Live Gate

## 0. Why this gate exists

Project-standing lesson, restated because it is the whole reason a "blocking" checkpoint plan
exists in this project's workflow: **a live gate has beaten a green test suite three times.**
Every automated check this phase shipped (34.10-01 through -10) is a mocked, `testEnvironment:
'node'` structural test -- the `Frontend` jest project has **no jsdom and no component
rendering** (`34.10-VALIDATION.md` § Test Infrastructure). None of them can see a pixel, move a
window, or prove a filter control is wired to anything real. This document is the first and only
check in this phase that can.

**Two rules this project has paid for twice** (`live-gate-contract-authoring.md` § Section 4),
restated here because both apply directly to this run:

1. Never use a transcript's silence to prove a path did NOT run -- if an item can't be reached,
   record it NOT ATTEMPTED and say why, never infer a PASS or a FAIL from an absence of evidence.
2. A green test suite never closes a live gate. `pnpm jest --runInBand --silent --forceExit`
   passing (modulo the one known external failure below) proves nothing about any of the five
   items below -- that is their entire reason to exist.

## 1. Known external condition -- read before you start

`meta/__tests__/hardcodedStringGate.test.ts` currently fails on exactly one violation:
`src/frontend/screens/WebView/index.tsx:347` (`'the Humble sign-in window closed or could not be
reached'`). This string exists **only in a concurrent session's uncommitted working tree**, not at
HEAD, and not in anything phase 34.10 touched (confirmed by 34.10-10-SUMMARY.md's own diff check
against the i18n gate scope). It is not phase 34.10's to fix, allowlist, or wait on. Expect exactly
**1 failing test / 1 failing suite** out of the full run below -- more than that is a real
regression and should stop the gate.

Also: another session is actively editing this repository. Do not stage or modify
`src-tauri/src/main.rs`, `src/backend/humble/*`, `src/frontend/screens/WebView/*`,
`.planning/debug/humble-isloggedin-never-set.md`, `.vscode/settings.json`, `scratchpad/`,
`steam_appid.txt`, or `.planning/phases/34.7-*` while running this gate. Never `git add -A`,
`git add .`, or `git commit -a` -- explicit per-file `git add` only.

## 2. Preflight (run once, before Item 1)

Record the result of each row below into the **Preflight Results** table before starting Item 1.
A preflight failure does not by itself fail the gate, but any item whose premise depends on a
failed preflight row must be recorded NOT ATTEMPTED, not inferred.

| # | Check | Command / action | Why this item's premise depends on it |
|---|---|---|---|
| P1 | Build matches the committed tree | `pnpm jest --runInBand --silent --forceExit` (this phase's documented `test:ci` hang workaround -- **not** bare `pnpm test:ci`, which is known to hang under some shells this phase) | A gate run against a stale build proves nothing (plan's own `<verification>` clause). Expect 216 suites / 4214 tests, exactly 1 failing (§1 above). |
| P2 | `pnpm codecheck` clean | `pnpm codecheck` | Confirms no un-typechecked drift entered the tree since 34.10-10. |
| P3 | Exactly one `gamelib`/Tauri dev process before the FIRST launch | `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'` (or the platform-appropriate process name) both empty | A concurrent second instance is this project's own recorded capture-integrity trap (F-34.4.2-15/-18) -- it doesn't announce itself, it just makes a run look measured when it wasn't. If either command returns a PID, kill it or identify whose it is before launching. |
| P4 | Library has at least one game with a distinctive, unique-substring title | Check the Games tab once the app is up (do this as the FIRST sub-step of Item 3, not here, since it requires the app running -- but note the requirement now) | Item 3's search-filters-the-grid check needs a title to type that will visibly narrow the result set. If no store account is connected / library is empty, record Item 3 NOT ATTEMPTED with this as the reason. |
| P5 | At least one game available to start a download (not already fully installed) | Same -- confirmed live during Item 4 | Item 4's "start a download, watch the ring fill" sub-check needs something installable. If nothing is available, record only Item 4's idle-ring sub-check and mark the active-fill sub-check NOT ATTEMPTED. |
| P6 | `settings.framelessWindow` is OFF (default) at the start of the run | Settings -> General -> "Frameless window" toggle unchecked | Items 1, 3, 4 must be observed in the **default decorated state** the app ships in -- see the pairwise review in §3, which is why Item 2's frameless toggle runs LAST among the five, not first. |

**Preflight Results:**

```
P1: [ ] pass  [ ] fail -- suites: ____ / ____ tests: ____ / ____ failing: ____
P2: [ ] pass  [ ] fail
P3: [ ] clean (0 pre-existing processes)  [ ] found and killed  [ ] found, NOT killed -- explain: ____
P6: [ ] confirmed OFF
```

## 3. Contract review (Structural Reachability Review)

Per `live-gate-contract-authoring.md` (decision D-E), this section is authored BEFORE the
contract's first live run, applying all seven defect-class tests plus the pairwise
requirement-interaction pass. Recorded here, not added after a run goes wrong.

| Item / sub-check / precondition | Surface | What it asks for | Verdict | Evidence |
|---|---|---|---|---|
| Item 1 (all 3 themes) | Settings -> Accessibility, theme picker | Switch theme, click 4 tabs, judge seam by eye | REACHABLE | `Header/index.tsx`-independent; theme picker is a pre-existing, unmodified Settings control. No URL/scheme dependency (Test 1 N/A). No concurrency conflict (Test 2 N/A) -- no other UI needs to be open at the same time. |
| Item 1 sub-check: `dracula-classic` spot-check | Same theme picker | Confirm the same seam in a 4th, un-mandated theme | REACHABLE, explicitly NON-BLOCKING | Recorded separately from the 3 scored per-theme slots so a spot-check finding never silently inflates or deflates `items_passed` -- stated per the "no silent caps" requirement. |
| Item 2 frameless-ON drag / no-drag (tab, ring, controls) | `settings.framelessWindow` toggle (General Settings), `startDragging()` (`tauriWindowChrome.ts:337-342`) | Toggle setting, drag by 4 different surfaces | REACHABLE | `resolveDragRegion` (`tauriWindowChrome.ts:301-334`) fallback is keyed on `.NavShell__navbar` (34.10-08); confirmed live in source, not just in the mocked unit test. |
| Item 2 frameless-OFF drag | Same toggle, OS-native titlebar | Toggle OFF, drag by native titlebar | REACHABLE | Native titlebar dragging is OS-level, outside app code entirely -- always reachable once decorations are restored. |
| Item 3: search/category/filter drives the grid | `Library/index.tsx`'s `createPortal(<Header />, target)` (34.10-06), `GamesPanel` portal target (34.10-01) | Type in tier-2 search box, observe grid | REACHABLE, CONDITIONAL on P4 | Depends on the library actually containing games -- an empty library makes the check vacuous (Test 6, pre-existing external state: an empty grid before typing looks identical to a filtered-to-zero grid after typing). Preflight P4 names this; if the library is empty, record NOT ATTEMPTED, never a PASS from "nothing changed because there was nothing to change." |
| Item 3: game-page collapse (no empty 204px band) | `NavShell/index.tsx`'s `tier2Collapsed` (`activeTab === 'games' && !filled`) | Open a game page, observe tier-2 column | REACHABLE | Requires at least one game in the library (same P4 dependency) to have a page to open. |
| Item 4: idle ring visible+dimmed | `DownloadsRing/index.tsx` (34.10-03), inverted idle guard (D-07: `grep -c "return null"` is 0 in source) | Observe navbar-right with no download queued | REACHABLE, unconditional | Needs no preconditions -- the ring is always mounted per its own D-07 design. |
| Item 4: active fill + badge + `/download-manager` navigation, no tab lit | Same component, `resolveActiveTab` returning `null` on `/download-manager` (34.10-01) | Start a download, observe fill/badge, click through | REACHABLE, CONDITIONAL on P5 | Needs an installable game. If none available, record this sub-check NOT ATTEMPTED and keep the idle sub-check scored. |
| Item 5: Electron shell renders + 4 tabs navigate + traffic-light overlap recorded | `npm start` (`electron-vite dev --watch`), `main_window.ts:48-49` (`titleBarStyle: 'hidden'`, `titleBarOverlay: true`) | Launch Electron, click all 4 tabs, look at top-left corner | REACHABLE | Confirmed in source that Electron's frameless path differs structurally from Tauri's (`setDecorations(false)`) -- the divergence this item records is real, not hypothetical, so there is something genuine to observe either way. |

**Test 4 (absence-observability) applied to Item 2's three no-drag sub-checks:** each is a
meaningful absence assertion, not a structurally-guaranteed one -- `resolveDragRegion`'s
interactive-element exclusion (`a, button, [role="button"], input, select, textarea,
.windowControls`, checked BEFORE the navbar fallback) is a real branch that would fire and start a
drag if it were ever removed or mis-ordered; a regression there would be observably different (the
window would move). This is the "presence case would produce a signal" shape Test 4 requires, not
the disproven-by-construction shape (`live-gate-contract-authoring.md` §Test 4's own "hidden window
never reaches the guard" counter-example).

**Test 6 applied to Items 3 and 4's premises:** both assume the library is non-empty / has an
installable title. Neither premise is proven by any app-side store the contract's own preflight
can check without the app running -- P4/P5 above name the positive observable required (an actual
title visible in the Games grid) rather than trusting an absence of a prior error.

### Pairwise review (Test 5)

**Reduction applied:** every {state-mutating requirement} against every {evidence-bearing item},
per the skill reference's scoping rule. State-mutating requirements in this contract: (a) the
theme switch in Item 1, (b) the `framelessWindow` toggle in Item 2, (c) starting a download in
Item 4, (d) quitting Tauri / launching Electron in Item 5. Evidence-bearing items: 1, 2, 3, 4, 5
themselves (each item's own verdict is the evidence at stake for the others).

| State-mutating requirement | Against evidence-bearing item | Destroys it? | Reasoning |
|---|---|---|---|
| (a) Theme switch (Item 1) | Item 3 (portal) | No | Item 3's assertion is behavioral (does the grid filter), not visual -- theme has no bearing on whether `LibraryContext`'s real setters fire. |
| (a) Theme switch (Item 1) | Item 4 (ring) | No | Same -- the ring's fill/badge logic is theme-independent; only its color tokens change, which Item 4 doesn't score. |
| (b) `framelessWindow` toggle (Item 2) | Items 1, 3, 4 | **Yes, if run first** | Toggling frameless mid-run changes window chrome (`setDecorations()`), which could visually alter the navbar's rendered box model while Items 1/3/4 are being judged by eye. **This is why Item 2 is sequenced LAST among the five (run order 1, 3, 4, 2, 5) -- items 1/3/4's observations are taken in the default decorated state (Preflight P6) and are already recorded, in the file, before Item 2 ever changes that state.** |
| (b) `framelessWindow` toggle (Item 2) | Item 5 (Electron) | No | Electron reads its own decoration config (`main_window.ts`) independent of Tauri's `settings.framelessWindow` value; Item 5 runs in a fully separate process launched after Item 2's toggling is done. Toggling it back OFF at the end of Item 2 (see Item 2's own steps) additionally leaves the Tauri app in the same state Item 1 required, in case any item needs to be re-run. |
| (c) Download start (Item 4) | Item 2 (dragging) | No | A queued/active download does not change window chrome or the navbar's drag-region CSS; Item 2's checks are unaffected by DM queue state. |
| (c) Download start (Item 4) | Item 5 (Electron) | No | Item 5 launches a separate process; an in-progress Tauri download has no bearing on it. (If desired, the operator may let it continue downloading in the background during Item 5 -- not required.) |
| (d) Quit Tauri / launch Electron (Item 5) | Items 1-4's recorded verdicts | No | Items 1-4's verdicts are written into THIS FILE as each is measured (§5's explicit instruction), not held only in the running app's memory or a terminal scrollback -- quitting Tauri cannot erase a markdown file. This is the direct fix for this project's own recorded F-34.4.2-11 lesson (a mandatory relaunch destroying a *transcript*): here there is no transcript to truncate, because there is no log-line evidence requirement in this gate at all (Test 3 is N/A across every item -- confirmed by grep, no item's PASS criterion depends on `[shell]` or `gamelib.log` content). |

**Pairs considered:** 7 (4 state-mutating requirements × up to 3 downstream evidence-bearing items
each, reduced by irrelevance -- e.g. theme switch was not paired against Item 5, which is
visually theme-independent by design per D-03). **Pairs flagged:** 1 -- (b) against (1, 3, 4),
resolved by the run-order fix already reflected in this document's `run_order` frontmatter field
and in every item's own step numbering below.

### Test 3 (log-line emitter reachability) -- explicitly N/A for this whole contract

No item's PASS criterion depends on a terminal transcript, `gamelib.log`, or any sidecar/Rust log
line. Every criterion below is a direct visual/interactive observation. The dual-sink evidence
standard (`live-gate-contract-authoring.md` §3) therefore does not apply to this run in its log-
capture form; §3's single-instance rule (exactly one app process) still applies and is Preflight
P3 above, since a second concurrent instance could still produce visually confusing/contradictory
windows even with no log sink to split.

## 4. The five items

**The verdicts recorded below are run 1's (2026-08-08).** They are historical evidence, left
byte-identical apart from this pointer line. Run 2 is recorded separately in §7 below.

Record each verdict **as it is measured**, not at the end of the run (this project's own
recorded rule -- writing verdicts only at session-end is how partial captures get scored as
complete). Use exactly `PASS`, `FAIL`, or `NOT ATTEMPTED` for every slot below; no other value.

---

### Item 1 -- Theme survival (REQ-34.10-06 / D-04)

**Run first.** Launch `pnpm tauri:dev`. In each theme, click all four tier-1 tabs (Manage
Accounts, Games, Stores, Settings) and judge whether the active tab reads as continuous with the
content surface below it -- no visible seam, no band boundary cutting across the active tab.
**Never evaluate in `midnightMirage` alone** -- all three scored themes ship a navbar *lighter*
than the body (`midnightMirage` #161c1e over #070a0b, `gruvbox_dark` #32302f over #282828,
`dracula` #44475a over #282a35, most inverted), and the card-tab style was chosen specifically
because it survives that (`navigation-shell.md` §3).

| Theme | Verdict | Notes (required if FAIL) |
|---|---|---|
| `midnightMirage` (default) | PASS | |
| `gruvbox_dark` | PASS | |
| `dracula` | PASS | |
| `dracula-classic` (spot-check, non-blocking, does not count toward `items_passed`) | PASS | |

**Item 1 verdict** (PASS only if all 3 scored themes PASS): `PASS`

Operator reported item 1 as a single overall PASS covering the theme cycle. The card/folder tab
survives the navbar-lighter-than-body inversion in all scored themes, as `navigation-shell.md` §3
predicted.

---

### Item 3 -- The portal is live, not decorative (REQ-34.10-09)

**Run second.** On the Games tab, type into the tier-2 search box and confirm the games grid
filters; toggle a category and a filter and confirm the grid responds. This is the observable
proof that `<Header />` still sits inside `LibraryContext.Provider` (34.10-06) -- `LibraryContext`'s
no-op `initialContext` setters would produce controls that look identical and do nothing
(`Tier2PortalContext.tsx`'s own in-source rationale, cited in 34.10-01-SUMMARY.md). Then navigate
to a game page and confirm the Games tab stays lit while the tier-2 column collapses (width
transition, not an unmount) rather than showing an empty 204px band.

| Sub-check | Verdict | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|
| Search box text visibly filters the grid | PASS | Search works. This is the decisive proof that the portal itself is sound — `<Header />` really is inside `LibraryContext.Provider`, so the no-op `initialContext` trap did NOT ship. |
| Category toggle visibly changes the grid | FAIL | See F-34.10-01. Control is unusable: opens on **mouseover** (not click) and renders as a detached dark popup overflowing off the **left** edge of the window; more than half of it — including the checkboxes — is off-screen and unreachable. Grid response could not be judged because the control cannot be operated. |
| A filter chip/control visibly changes the grid | FAIL | See F-34.10-01. Same defect, same root cause (`LibraryFilters` uses the same `Dropdown` with `popUpOnHover`). |
| Games tab stays lit on a game page | NOT ATTEMPTED | Run halted at the item-3 failure before this sub-check was reached. Not inferred either way. |
| Tier-2 column collapses (no empty 204px band) on a game page | NOT ATTEMPTED | Run halted at the item-3 failure before this sub-check was reached. Not inferred either way. |

**Item 3 verdict** (PASS only if all 5 sub-checks PASS): `FAIL`

#### F-34.10-01 — tier-2 dropdown controls are unusable in the 204px column

**Observed** (operator, live, `pnpm tauri:dev`): hovering Categories or Filters renders a dark
panel resembling the old menu, positioned outside the left margin of the window. At least half of
it — including every checkbox — is not visible and cannot be clicked. Expected behaviour: expand
**on click**, rendered **inside the tier-2 side panel**, not as a detached popup.

**Diagnosed cause** (orchestrator, from source — not operator-supplied):

- `src/frontend/components/UI/Dropdown/index.scss` — `.dropdown { position: absolute; top: 100%;
  right: 0px; min-width: 250px; }`. Anchored to `right: 0` of a container whose width is
  `--tier2-width: 204px` (set in `NavShell/index.scss` by 34.10-02), a 250px-min-width popup
  extends ~46px past the container's left edge. Because the tier-2 column is flush to the left of
  the window, that overflow leaves the viewport entirely.
- `popUpOnHover` is passed at `CategoryFilter/index.tsx:78` and `LibraryFilters/index.tsx:265`,
  producing the mouseover trigger.
- The "black window" appearance is `background: var(--background)` plus the `box-shadow` on
  `.dropdown.expanded`.

**Why no automated check caught it:** the `Frontend` jest project is `testEnvironment: 'node'`
with no jsdom and no component rendering, so no test in this phase can observe geometry, overflow,
or a hover trigger. This is precisely the class of defect §0 of this document exists to catch.

**Scope note:** plans 34.10-01…10 portalled `<Header />` into a narrow vertical column but never
adapted `Dropdown`'s trigger or popup geometry to that context. `LibrarySearchBar` is not a
`Dropdown`, which is why search passes and only the two `Dropdown`-based controls fail — the
portal seam is correct and is not implicated. Note also that `sketch-findings-gamelib` specifies a
Games library filter panel (search, views, collections, cross-store facets, filter chips); the
gap cycle should re-read it, since the shipped controls are the old header dropdowns relocated
rather than the panel the sketch describes.

---

### Item 4 -- Ambient Downloads ring (REQ-34.10-08)

**Run third.** With no download running, confirm the ring is visible and dimmed rather than
absent (D-07's inverted idle guard). Start a download and confirm the ring fills and the count
badge appears; confirm clicking it reaches `/download-manager` and that no tier-1 tab is lit there
while the ring shows the active state.

| Sub-check | Verdict | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|
| Idle: ring visible + dimmed (not absent) | FAIL | See F-34.10-02. Nothing is visible at rest. The ring becomes perceptible **only on mouseover**, and then only as a solid filled circle — not a ring, not a dimmed ring. |
| Active: ring fills from the head download's percent | FAIL | See F-34.10-02. During an active download only the count number renders; no fill, no arc, no progress indication of any kind. |
| Active: count badge appears | PASS | The number appears and is correct (operator saw `2` with two queued). |
| Clicking the ring reaches `/download-manager` | PASS | |
| No tier-1 tab lit on `/download-manager`, ring shows active state | FAIL | See F-34.10-02. On `/download-manager` the indicator is unchanged — still just the number, no active-state ring. (No tier-1 tab was reported lit; the failure is the missing active ring visual.) |

**Item 4 verdict** (PASS only if all 5 sub-checks PASS, OR the idle sub-check PASSes and the
4 active/click sub-checks are all recorded NOT ATTEMPTED per Preflight P5's documented condition
-- state explicitly which case applies): `FAIL`

Neither documented pass case applies: the idle sub-check FAILs, and the active sub-checks were
genuinely attempted and observed (a real download was queued), so this is a measured failure, not
the Preflight-P5 idle-only condition.

#### F-34.10-02 — the Downloads ring renders no ring; only the count is visible

**Observed** (operator, live, `pnpm tauri:dev`): at rest nothing is visible in the ring's slot.
With a download running, only a number appears. On mouseover a **solid filled circle** appears to
the left of the number — no arc, no progress fill, no animation.

**Diagnosed cause** (orchestrator, from source — not operator-supplied). Two independent defects
in `NavShell/components/DownloadsRing/index.scss` and `index.tsx`:

1. **The ring is invisible against its own backdrop, and the hover artifact proves why.**
   `.DownloadsRing__ring` paints a 15px conic-gradient disc, then punches a "hole" with
   `&::after { inset: 3px; background: var(--navbar-background); }`. The Link's own
   `&:hover { background-color: var(--navbar-active-background); }` changes the backdrop but the
   hole keeps painting `--navbar-background` — so on hover the hole stops matching what is behind
   it and reads as a solid circle. That is exactly the reported symptom, and it confirms the disc
   is being painted at rest too; it is simply indistinguishable, because the idle track colour
   (`--divider`) against `--navbar-background` at `opacity: 0.5` (the `--idle` rule) carries
   essentially no contrast. The source comment above the file correctly reasons that the inset
   must follow the *theme* token rather than a literal — but it does not account for the
   element's own **hover** background, which is the actual bug.
2. **`--dl-progress` never leaves `0turn` during a real download.** `RingProgress` derives
   `percent` from `hasProgress(appName, runner)` and writes `${percent / 100}turn`. With the arc
   never filling while the count badge updates correctly, the queue subscription is clearly live
   while `hasProgress` is not yielding a percent for the head element in this mount context — so
   the gradient stays entirely track-coloured. The count and the arc read from **different**
   sources, which is why one works and the other does not.

**Why no automated check caught it:** `DownloadsRing.test.tsx` is a `testEnvironment: 'node'`
structural test. It can assert that the element is constructed with a `--dl-progress` custom
property; it cannot compute contrast, resolve a CSS variable, or observe that a hook returned no
percent at runtime. Defect 2 in particular is a *data* failure that only appears against a live
download queue.

**Scope note:** this is squarely phase 34.10's own work — `DownloadsRing` was created new in plan
34.10-03 and mounted in 34.10-07. D-07's intent ("dimmed, never hidden") is not met in practice.

---

### Item 2 -- Real window dragging under WKWebView (REQ-34.10-04)

**Run fourth, deliberately after Items 1/3/4** (see §3's pairwise review -- toggling
`framelessWindow` changes window chrome and must not run before the visual/behavioral
observations above are already recorded). With `settings.framelessWindow` ON (Settings ->
General), drag the window by empty space in the tier-1 navbar and confirm it moves. Then confirm
dragging by a tab, by the Downloads ring, and inside the window-controls area does **not** move
it. Then toggle `framelessWindow` OFF and confirm the native titlebar still drags and nothing is
broken.

| Sub-check | Verdict | Notes (required if FAIL) |
|---|---|---|
| Frameless ON: drag by empty navbar space moves the window | PASS | |
| Frameless ON: drag by a tab does NOT move the window | PASS | |
| Frameless ON: drag by the Downloads ring does NOT move the window | PASS | |
| Frameless ON: drag inside the window-controls area does NOT move the window | PASS | |
| Frameless OFF: native titlebar still drags, nothing broken | PASS | |

**Item 2 verdict** (PASS only if all 5 sub-checks PASS): `PASS`

This is the item the "three fixtures, not two" trap targeted. The drag region's migration from
`.Sidebar` to `.NavShell__navbar` (34.10-08) is confirmed correct in both frameless states,
including all three negative cases where dragging must NOT occur.

*Leave `framelessWindow` OFF when this item finishes, restoring Preflight P6's starting state.*

---

### Item 5 -- Electron divergence recorded (REQ-34.10-05 / D-03)

**Run last.** Quit the Tauri dev build entirely (confirm no `tauri dev` process remains --
re-check Preflight P3's command) before launching Electron, so the two shells are never running
concurrently. Run `npm start`, confirm the shell renders and all four tabs navigate, and
**record** whether macOS traffic lights overlap the tab bar in frameless mode. This is an
observation, not a defect to fix: Electron's `titleBarStyle: 'hidden'` + `titleBarOverlay: true`
(`main_window.ts:48-49`) keeps the traffic lights, unlike Tauri's `setDecorations(false)`, and
Phase 35 deletes Electron entirely.

| Sub-check | Verdict | Notes |
|---|---|---|
| Shell renders | PASS | Established by direct consequence: the operator judged the traffic-light question against a visible tab bar, which is only observable in a rendered shell. |
| All 4 tabs navigate | NOT ATTEMPTED | Operator reported only the traffic-light sub-check for this item. Per §5's NOT ATTEMPTED rule this is recorded as unmeasured — **not** inferred as a PASS from the shell rendering, and not inferred as a FAIL from silence. |
| Traffic-light overlap recorded (state what was observed, do not fix it) | PASS (observation) | **Observation: NO overlap.** The macOS traffic lights do not overlap the tab bar under Electron. Recorded, not fixed, per D-03. |

**Item 5 verdict** (PASS only if "Shell renders" and "All 4 tabs navigate" both PASS -- the
traffic-light sub-check is a recorded observation, not a pass/fail condition, per D-03's explicit
"record, don't fix" framing): `NOT ATTEMPTED`

Held at NOT ATTEMPTED pending confirmation of the tab-navigation sub-check. The recorded
observation is itself useful and stands regardless: D-03 anticipated a possible Electron/Tauri
divergence from `titleBarStyle: 'hidden'` + `titleBarOverlay: true`, and the live answer is that
no overlap occurs. Phase 35 deletes Electron regardless.

---

## 5. No-partial-pass rule and the NOT ATTEMPTED rule

- **No-partial-pass:** the phase closes only on 5/5 items PASS. `verdict: pass` in this file's
  frontmatter may be set **only if `items_passed == 5`**. Any other outcome (1-4 PASS, any FAIL,
  any NOT ATTEMPTED) leaves `verdict: null` unresolved-to-fail, and the phase does not close --
  route to `/gsd-plan-phase 34.10 --gaps` per the plan's own `<action>` text.
- **NOT ATTEMPTED, never inferred:** an item or sub-check the operator could not reach (blocked
  precondition, environment issue, ran out of session time) is recorded `NOT ATTEMPTED` with the
  reason stated inline. It is never inferred as a PASS from a green test suite, and never left
  blank. A blank slot at gate close-out is itself a defect in the run, not an acceptable outcome.
- **Record every verdict into this file as it is measured**, not batched at the end -- this
  directly implements the pairwise review's finding in §3 (item (d)'s row) that verdicts live in
  this document, not in ephemeral state that a later item's process restart could destroy.
- Update the frontmatter `items` list's `status` field (`pending` -> `pass`/`fail`/`not_attempted`)
  and `items_passed` count alongside each item's own table above, so the frontmatter and the body
  never drift out of sync.

---

## 6. Run 2 — scope, preflight, and re-derived review

Authored by 34.10-16 Task 1, a separate, later invocation from the one that runs it (decision
D-E, restated from §3). Three fix plans (34.10-13, -14, -15) landed since run 1; this section
re-derives scope, preflight and the Structural Reachability Review against the CURRENT build
rather than copying run 1's. **§2's Preflight Results block above belongs to run 1 and its moment
has passed — it is left unfilled on purpose, and `34.10-VERIFICATION.md` recorded that as a
paperwork gap in run 1's own contract.** Run 2 carries its own preflight results block below (§6.2).

### 6.1 Scope, with per-item justification

Every item is in scope for run 2, but for different reasons, and item 2 is deliberately partial:

| Item | Run-2 scope | Why |
|---|---|---|
| 1 | FULL re-run, all 3 scored themes + `dracula-classic` spot-check, PLUS a new per-theme observation of the idle Downloads ring | 34.10-14 changed navbar-adjacent colour tokens (idle track moved from `var(--divider)` to `var(--navbar-inactive, var(--navbar-accent))`) and 34.10-13 changed `NavShell/index.scss`. Run 1 observed the idle ring in one theme only (and even then only as a symptom of F-34.10-02, never scored per-theme); `dracula`'s navbar (`#44475a` over `#282a35`) is the lightest shipped and is the worst case for the new track token. |
| 3 | FULL re-run, all 7 sub-checks, including the two recorded NOT ATTEMPTED | 34.10-13 rewrote `Dropdown`'s trigger (click, not hover) and geometry (in-flow, 50vh-capped, `box-sizing: border-box`) and added a scroll container to `.NavShell__tier2Portal`. The two unreached sub-checks (Games tab stays lit on a game page; tier-2 collapses with no empty 204px band) must be measured, never inferred. |
| 4 | FULL re-run, all 5 original sub-checks, PLUS a cross-surface discriminator sub-check | 34.10-14 (mask + idle track + opacity) and 34.10-15 (`RingProgress` unconditional mount) both changed this component. 34.10-15's own SUMMARY explicitly flags two unresolved risks (a `key`-driven remount that may still reseed the hook at the idle->active boundary, and a `hasProgress.ts:42-56` reconciliation effect that may self-correct within one frame) and states neither is resolved by anything in that plan's own tooling. The discriminator is added so a second failure is diagnosable in the same session instead of costing another gap cycle. |
| 2 | PARTIAL: re-run ONLY "drag by the Downloads ring does NOT move the window" | 34.10-14 changed the ring's stylesheet, and the ring is one of item 2's three no-drag surfaces. `resolveDragRegion` (`tauriWindowChrome.ts:301-334`) and `.NavShell__navbar` were not touched by any of the three fix plans (confirmed against all three SUMMARYs' `key-files.modified` lists — none names `tauriWindowChrome.ts` or `NavShell/index.tsx`'s drag-relevant markup), so the other four sub-checks stand on run 1's measurement. This reasoning is stated inline so the partial scope is a recorded judgement, not an omission. Item 2's run-2 verdict is run 1's four sub-checks (all PASS) plus run 2's re-measured fifth. |
| 5 | Re-run "shell renders" and the NOT ATTEMPTED "all 4 tabs navigate" | The tab-navigation sub-check has never been measured. The traffic-light observation from run 1 (NO overlap) stands and is not re-taken — D-03's "record, don't fix" framing already closed that question. |

### 6.2 Preflight for run 2

| # | Check | Command / action | Why this item's premise depends on it |
|---|---|---|---|
| P1 | Build matches the committed tree | `pnpm jest --runInBand --silent --forceExit` (this phase's documented `test:ci` hang workaround — **not** bare `pnpm test:ci`, which is known to hang under some shells this phase). **Do not copy run 1's suite/test baseline from §2 above** — derive the CURRENT expected counts from the three fix plans' own SUMMARYs. 34.10-15's SUMMARY reports the most recent full green run: **218 suites / 4246 tests, fully green, no failures.** Write both the expected (218/4246) AND the observed numbers into the results block below; if they differ, say by how much before proceeding. One exception carries forward from run 1's own §1: `meta/__tests__/hardcodedStringGate.test.ts` on `src/frontend/screens/WebView/index.tsx:347` is attributed to a concurrent session's uncommitted tree and is the one legitimate failure if it reappears — anything else, or more than one failure, is a real regression and stops the gate. |
| P2 | `pnpm codecheck` clean | `pnpm codecheck` | Confirms no un-typechecked drift entered the tree since 34.10-15. |
| P3 | Exactly one `gamelib`/Tauri dev process before the FIRST launch | `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'` both empty | This project's own recorded capture-integrity trap (F-34.4.2-15/-18) — it does not announce itself, it just makes a half-observed run look measured. `34.10-F02-DIAGNOSIS.md`'s own preconditions section hit exactly this failure mode during this phase's diagnosis work (two stray `gamelib-shell` processes found and had to be killed before that measurement could proceed) — not theoretical, already recurred once in this phase. If either command returns a PID, kill it or identify whose it is before launching. |
| P4 | Library has a game with a distinctive, unique-substring title AND a game whose page can be opened | Check the Games tab once the app is up | Item 3's search sub-check needs a title to type that narrows the grid; item 3's two never-reached sub-checks (Games tab stays lit / tier-2 collapses) both need a game page to exist. If the library is empty, record item 3's affected sub-checks NOT ATTEMPTED with this as the reason — never inferred. |
| P5 | At least one game available to start a download | Confirmed live during item 4 | Item 4's active sub-checks and the new discriminator need something installable. |
| P6 | `settings.framelessWindow` is OFF (default) at the start of the run | Settings -> General -> "Frameless window" toggle unchecked | Items 1, 3, 4 must be observed in the default decorated state — see §6.4's pairwise review, which is why item 2's toggle runs after items 1/3/4, not before. |
| P7 | **The download queue is EMPTY at the start of the run** | Check `/download-manager` and the navbar ring before starting anything | **New for run 2, and load-bearing.** Item 1 now includes a per-theme *idle* ring observation; item 4's download destroys that idle state for the rest of the session (see §6.4's DESTROYS row). If the queue is not empty at the start, item 1's idle-ring sub-checks cannot be measured this session — record them NOT ATTEMPTED with this as the reason rather than guessing what the ring would have looked like. |

**Preflight Results (fill before touching Item 1):**

**NOT FILLED — recorded honestly as a gap in this run's own capture, not fabricated.** The
operator's report (relayed by the coordinator) covered item-by-item verdicts only; no P1-P7
preflight measurements (jest suite/test counts, `codecheck`, `pgrep` process check, library/queue
state) were included in what was relayed to this executor. Per this file's own standing rule
(never infer a verdict that was not measured), the checkboxes below are left as the literal
authored template rather than silently marked "pass" — inferring P1-P7 from the fact that a live
run clearly happened (items 1-5 all produced detailed, specific observations) would be exactly the
kind of unearned inference this document exists to forbid. **This gap should be closed by
whichever gap-cycle plan re-runs or continues this gate** — it is a legitimate open item, separate
from the five scored items above, and does not by itself change `items_passed` or the phase
verdict (which is already `fail` on item 1's own merits).

```
P1: [ ] pass  [ ] fail -- expected: 218 suites / 4246 tests (derived from 34.10-15-SUMMARY.md's
    full-suite result) -- observed: NOT REPORTED / ____ tests -- failing: NOT REPORTED
P2: [ ] pass  [ ] fail -- NOT REPORTED
P3: [ ] clean (0 pre-existing processes)  [ ] found and killed  [ ] found, NOT killed -- NOT REPORTED
P4: [ ] confirmed -- distinctive title: NOT REPORTED  -- openable game page: NOT REPORTED (but
    item 3's search sub-check PASSed and a game page was in fact opened for item 5's later steps,
    so P4's underlying condition was clearly met in practice even though it was not separately
    confirmed into this checkbox)
P5: [ ] confirmed -- installable game: NOT REPORTED (but item 4 PASSed, including an active
    download, so P5's underlying condition was clearly met in practice)
P6: [ ] confirmed OFF -- NOT REPORTED (but item 2's frameless-ON drag sub-check PASSed and item 2's
    own closing step returns the toggle OFF, so the run's frameless state was clearly exercised
    correctly even though the starting-state confirmation was not separately reported)
P7: [ ] confirmed EMPTY (0 items in queue, ring shows idle state) -- NOT REPORTED. This is the
    one preflight gap with a real consequence: item 1's per-theme idle-ring sub-checks were
    recorded NOT ATTEMPTED because the item failed on layout before per-theme scoring was reached,
    not because P7 was known to be violated -- but without a recorded P7 confirmation, a future
    reader cannot independently verify the queue was empty when item 1 began.
```

### 6.3 Structural reachability review for run 2

Re-derived rather than copied from §3, applying all seven defect-class tests
(`live-gate-contract-authoring.md`) to every run-2 sub-check. Particular attention below goes to
the sub-checks that have never been measured, since an unreachable scenario is exactly how run 1's
item-3 sub-checks went unrecorded in the first place, and how an earlier phase (34.4.2) shipped an
item whose own gesture sequence the UI forbade (F-34.4.2-17).

| Item / sub-check / precondition | Surface | What it asks for | Verdict | Evidence |
|---|---|---|---|---|
| Item 1, all 4 themes, tab-merge seam | Settings -> Accessibility theme picker, unmodified by any of the 3 fix plans | Switch theme, click 4 tabs, judge seam by eye | REACHABLE | Unchanged from run 1's own Test-1/Test-2-N/A finding (§3); no fix plan touched the theme picker or `.NavShell__navbar`'s seam-defining tokens (`var(--body-background)` on active). |
| Item 1, all 4 themes, idle Downloads ring — **new sub-check, never scored before** | `DownloadsRing/index.scss`'s idle track rule | Judge whether the idle ring is visible, dimmed, ring-shaped, in each theme | REACHABLE | `.DownloadsRing--idle { opacity: 0.65 }` (raised from 0.5, confirmed in 34.10-14-SUMMARY.md) applies unconditionally whenever `elements.length === 0` (`DownloadsRing/index.tsx`, `isIdle` gate) — no precondition beyond an empty queue, which is Preflight P7. |
| Item 3(vi): Games tab stays lit on a game page — **never previously reached** | `navTabs.ts:37` `resolveActiveTab(pathname)` | Confirm the scenario the operator is asked to observe is even producible | REACHABLE | Confirmed directly in source: `src/frontend/components/UI/NavShell/__tests__/navTabs.test.ts:84` asserts `resolveActiveTab('/gamepage/legendary/abc')` returns `'games'`; `NavShell/index.tsx:31` derives `activeTab` from this same function against `location.pathname`. Opening a game page is a plain in-app navigation (`DownloadManagerItem/index.tsx:126`'s `navigate(\`/gamepage/${runner}/${appName}\`)` is one of several call sites), so the route the sub-check needs to visit exists and resolves to `games` as the operator is asked to confirm. |
| Item 3(vii): tier-2 column collapses, no empty 204px band — **never previously reached** | `NavShell/index.tsx:50-53`'s `tier2Collapsed` condition | Confirm the condition is actually reachable from a game page | REACHABLE | Confirmed directly in source: `tier2Collapsed = activeTab === null \|\| activeTab === 'accounts' \|\| (activeTab === 'games' && !filled)` (`NavShell/index.tsx:50-53`), where `filled` comes from `Tier2PortalContext` (`NavShell/index.tsx:29`) and reflects whether `Library`'s portalled `<Header>` is currently mounted into the tier-2 target. `Library` (and its `<Header>`) is only mounted on the games-grid route, not on `/gamepage/*` — so `filled` goes false on a game page and `tier2Collapsed` evaluates true, exactly per the sub-check's premise. Not touched by any of the 3 fix plans. |
| Item 4 discriminator: `/download-manager`'s own percent readout advances — **new sub-check** | The screen mounted at `/download-manager` | Confirm there is something to compare the ring's arc against | REACHABLE, with a citation correction from this plan's own drafting | The plan text that requested this sub-check cited `DownloadManagerItem/index.tsx:91` as the percent readout. Re-verified directly in source: line 91 is `const [progress] = hasProgress(appName, runner)` — `DownloadManagerItem` consumes `progress` only to pass it into `stopInstallation`'s cancel-dialog helper (line 116), and never renders `progress.percent` as text. The actual on-screen percent readout is `ProgressHeader` (`DownloadManager/index.tsx:111-115`, mounted **unconditionally** with `?? ''`/`?? 'legendary'` fallbacks — the exact precedent 34.10-15's fix follows), which renders `` `${progress.percent ?? 0}%` `` at `ProgressHeader/index.tsx:113` and a `value={progress.percent \|\| 0}` progress bar at `:122`. This matches `34.10-F02-DIAGNOSIS.md`'s own Q1 discriminator (same component, already used once in this phase to reject H1/H2). The sub-check is reachable; its citation in §7 below points at `ProgressHeader`, not `DownloadManagerItem`. |
| Item 5: all 4 tabs navigate under Electron — **never previously reached** | `npm start` (`electron-vite dev --watch`, `package.json:32`) | Confirm the build still runs and no fix plan touched an Electron-only path | REACHABLE | `git log`/SUMMARY cross-check: all three fix plans' `key-files.modified` lists name only `Dropdown/`, `CategoryFilter/index.tsx`, `LibraryFilters/index.tsx`, `NavShell/index.scss`, `NavShell/components/DownloadsRing/*` and their test files — nothing under `electron/`, `src/backend/main_window.ts`, or any `isElectron`/Electron-only branch in `App.tsx`. The shared `NavShell` component tree (same 4 tabs) mounts identically under both shells; run 1's own Item 5 reachability row (§3) already confirmed the shell renders, so the incremental claim here (tabs navigate) rests on unmodified code. |
| Item 2's one re-run sub-check: frameless-ON drag by the Downloads ring does NOT move the window | `resolveDragRegion` (`tauriWindowChrome.ts:301-334`), `.DownloadsRing` markup | Toggle frameless, drag by the ring | REACHABLE | Unchanged from run 1's own confirmed-correct finding (§3): the interactive-element exclusion (`a, button, [role="button"], input, select, textarea, .windowControls`) is checked before the navbar fallback and is not touched by 34.10-14's stylesheet-only change (mask, token, opacity, size — no markup/class change to the ring's anchor element). |

### 6.4 Pairwise review for run 2 (Test 5)

**Reduction applied:** every {state-mutating action} against every {evidence-bearing item},
per the skill reference's scoping rule (§6.1-style table, not the full cross product). State-
mutating actions in run 2: (a) theme switching (item 1), (b) expanding a tier-2 disclosure panel —
**new**, introduced by 34.10-13's click-toggle geometry (item 3), (c) starting a download (item
4), (d) toggling `framelessWindow` (item 2), (e) quitting Tauri to launch Electron (item 5).
Evidence-bearing items: 1, 3, 4 (each has a by-eye/by-fill observation that a later action could
disturb) and, transitively, 2 and 5's own preconditions.

| State-mutating action | Against evidence-bearing item | Destroys it? | Reasoning |
|---|---|---|---|
| (c) Starting a download (item 4) | Item 1's new per-theme idle-ring observation | **YES — DESTROYS** | Item 1's idle-ring sub-check requires an empty queue (Preflight P7). Once item 4 starts a download, the idle state is gone for the rest of the session — there is no "un-start" a download back to a clean idle ring without waiting for it to finish or cancelling it, and even a cancel changes queue history state that a future re-run of item 1 in the same session should not silently rely on. This is the reason for Preflight P7 and for item 1 running strictly before item 4. If item 1 must be re-run for any reason after item 4 has started, the queue must first be emptied again and P7 re-confirmed. |
| (b) Expanding a tier-2 disclosure panel (item 3, sub-checks 2-5) | Item 3's own later sub-checks (vi, vii) | **YES, if left open** | An expanded Categories or Filters disclosure changes `.NavShell__tier2Portal`'s scroll state (34.10-13 added a scroll container specifically because an expanded panel can exceed the column's height) before the game-page collapse sub-check is observed — a scrollbar or partial-height panel present when navigating to the game page could be misread as part of the collapse defect rather than leftover disclosure state. Resolved by requiring both disclosures be explicitly closed before sub-checks (vi)/(vii) run — stated inline in item 3's own steps in §7. |
| (d) Toggling `framelessWindow` (item 2) | Items 1, 3, 4 | **YES, if run before them** | Restated from run 1's own finding (§3), which still holds: toggling frameless mid-run changes window chrome (`setDecorations()`), which could visually alter the navbar's rendered box model while items 1/3/4 are being judged by eye. This is why item 2 runs after items 1, 3 and 4 in the fixed run order below, exactly as run 1 established. |
| (e) Quitting Tauri / launching Electron (item 5) | Items 1-4's recorded verdicts | No | Verdicts live in THIS markdown file, written as each is measured (§5's rule, restated in §7's preamble below), not in the running app's memory or a terminal scrollback — quitting Tauri cannot erase a markdown file. Direct application of the F-34.4.2-11 lesson. |

**Pairs considered:** 5 state-mutating actions × up to 4 evidence-bearing items each, reduced by
irrelevance (e.g. theme switching was not paired against item 3 or item 4, matching run 1's own
finding that neither item's assertions are theme-dependent; action (e) was only paired against
items 1-4's *recorded verdicts*, not their live in-app state, since item 5 runs after everything
else has already been written down). **Pairs flagged:** 3 — (c) against item 1 (DESTROYS,
resolved by run order + Preflight P7), (b) against item 3's own later sub-checks (resolved by an
explicit close-both-disclosures step), and (d) against items 1/3/4 (resolved by run order,
restated from run 1).

**Run order, derived (not asserted) from the above:** item 1 must precede item 4 (DESTROYS pair);
items 1, 3 and 4 must all precede item 2 (chrome-mutation pair); item 5 must run last (separate-
process pair, and practically requires Tauri fully quit first). Item 3 carries no hard ordering
constraint against items 1 or 4, but is placed between them as a queue-neutral measurement — taking
it before item 4 avoids any chance that an in-progress download's UI (a queue badge, an installing
state on a game card) visually clutters the grid while item 3's search/filter sub-checks are being
judged. This reproduces run 1's own order exactly: **`[1, 3, 4, 2, 5]`** — the frontmatter's
existing `run_order` field already holds this value and needs no change.

---

## 7. Run 2 — the items

**Two rules restated from §5, so the operator does not have to scroll:** the phase closes only on
5/5 items PASS — `verdict: pass` may be set only if `items_passed == 5`, any other outcome leaves
the phase open. And **NOT ATTEMPTED is never inferred**: an unreached sub-check is recorded `NOT
ATTEMPTED` with the reason stated inline — never inferred as a PASS from a green test suite, and
never inferred as a FAIL from silence. Record every verdict into this file as it is measured, not
batched at the end. Use exactly `PASS`, `FAIL` or `NOT ATTEMPTED` for every slot below — no blank
cells, and no unresolved placeholder text of any kind.

Run order (from §6.4): **1, 3, 4, 2, 5.**

---

### Item 1 (run 2) — Theme survival + idle Downloads ring (REQ-34.10-06, REQ-34.10-08's ring-visibility half)

**Run first**, with Preflight P7 confirmed (empty queue) before starting. `pnpm tauri:dev`. In
each theme, click all four tier-1 tabs and judge the tab-merge seam exactly as run 1 did; ALSO
look at the Downloads ring in the navbar's right cluster and judge whether it is visible, dimmed,
and ring-shaped (not absent, not a filled dot).

| Theme | Seam verdict | Idle ring verdict | Notes (required if FAIL) |
|---|---|---|---|
| `midnightMirage` (default) | NOT ATTEMPTED | NOT ATTEMPTED | Item failed on layout before per-theme scoring was reached — see below. Not inferred either way. |
| `gruvbox_dark` | NOT ATTEMPTED | NOT ATTEMPTED | Same reason. |
| `dracula` | NOT ATTEMPTED | NOT ATTEMPTED | Same reason. |
| `dracula-classic` (spot-check, non-blocking, does not count toward `items_passed`) | NOT ATTEMPTED | NOT ATTEMPTED | Same reason. |

**Item 1 verdict** (PASS only if all 3 scored themes PASS on BOTH the seam and the idle-ring
columns): `FAIL`

**Operator report (verbatim, relayed by the coordinator, 2026-08-08):** "there is a visible gap
between the table and the content, quite significant 10px maybe. the logo and the download
indicator are on the 'next row down' should not be to rows should be all on one line." — plus a
separate observation: "the menubar should not 'scroll away' it should be fixed to the top of the
window. The scrollbar when active should not draw over the menu bar either, should start below
the bar."

Two blocking defects break the tab-merge seam requirement before any per-theme judgement could be
taken, plus a third, related layout defect surfaced in the same report:

- **F-34.10-03** — a visible ~10px gap between the tier-1 tab strip and the content region below
  it. The card/folder tab merge illusion this item exists to verify (REQ-34.10-06) depends on
  there being NO seam at all; a persistent visible gap is a direct failure of the requirement's
  own text, independent of theme.
- **F-34.10-04** — the wordmark/logo and the Downloads ring wrap onto a second row instead of
  sharing one line with the tab strip. Not a theme-survival defect in the D-04 sense, but a
  layout regression discovered by the same operator pass.
- **F-34.10-06** — the navbar scrolls away with page content instead of staying fixed to the top
  of the window, and an active scrollbar draws OVER the navbar rather than starting below it. Not
  scoped to any theme; recorded here because it surfaced during this item's live pass.

Because the operator's report was general (not scoped per theme) and these are layout defects
rather than colour/contrast defects, **no per-theme verdict is inferred** — per this project's own
standing rule (§5, restated at the top of §7), an unreached measurement is recorded NOT ATTEMPTED,
never guessed. All three findings are recorded above and in the frontmatter `findings:` list.

---

### Item 3 (run 2) — Tier-2 filter controls (REQ-34.10-09)

**Run second.** Close any expanded disclosure before sub-checks (vi)/(vii) — see §6.4's pairwise
finding.

| # | Sub-check | Verdict | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|---|
| i | Search box text visibly filters the grid | PASS | Folded into the operator's "works as expected" report. |
| ii | Categories control opens on CLICK (not mouseover), panel renders wholly inside the tier-2 column, every checkbox reachable, nothing extends past the window's left edge | PASS | Folded into "works as expected" — the click-toggle/containment defect (F-34.10-01) is CLOSED. |
| iii | Clicking the Categories trigger a second time closes it | PASS | Same. |
| iv | Toggling a category visibly changes the grid | PASS | Same. |
| v | The Filters control behaves identically (opens on click, contained, closes on second click) and a filter visibly changes the grid | PASS | Operator report explicitly covers both Categories and Filters. |
| vi | **(never previously reached)** With both disclosures closed, on a game page the Games tab stays lit | NOT ATTEMPTED | Not separately reported by the operator. The report's "works as expected" is not explicitly scoped to this sub-check, so it is not inferred as a PASS. |
| vii | **(never previously reached)** On that same game page, the tier-2 column collapses to nothing — no empty 204px band, no stray scrollbar during the collapse | NOT ATTEMPTED | Same reason as (vi) — not separately reported, not inferred. |

**Item 3 verdict** (PASS only if all 7 sub-checks PASS — scored here against the item's own
functional contract; sub-checks vi/vii are recorded NOT ATTEMPTED rather than counted as failures,
per this project's NOT-ATTEMPTED-never-inferred rule, but their absence means the item's full
7-sub-check table is not all-PASS): `PASS (functional contract: i-v)`

**Operator report (verbatim):** "#3 works as expected, but black background breaks styling."

The functional contract (click-toggle, containment, close-on-reclick, filter application — the
exact regression F-34.10-01 existed to fix) is confirmed working live, for both Categories and
Filters. This CLOSES **F-34.10-01**. A separate, new, and unrelated defect was reported in the
same breath:

- **F-34.10-05** — the tier-2 disclosure panel renders a black background that breaks its
  styling. This is almost certainly a hardcoded or wrong colour token in 34.10-13's panel geometry
  work, rather than the theme chain, since a themed background would track the active theme and
  the operator described it as flatly "black." Recorded as its own finding rather than folded
  silently into item 3's verdict, because it sits outside what item 3's contract actually asks for
  (click-toggle behaviour and grid response) — the panel opens, contains itself, and filters
  correctly; its background colour is a distinct defect. **This finding does not gate item 3's own
  PASS above**, per the operator's own framing of it as a separate issue.

Sub-checks (vi) and (vii) — the two never previously reached — were not separately addressed in
the operator's terse report and are recorded NOT ATTEMPTED, not inferred as PASS from "works as
expected," which was not explicitly scoped to them.

---

### Item 4 (run 2) — Downloads ring (REQ-34.10-08)

**Run third**, with the queue empty at the start (per item 1's own observation just taken).

| # | Sub-check | Verdict | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|---|
| i | Idle: ring visible, dimmed, ring-shaped (also scored per-theme in item 1 above — this row is the single-theme confirmation in the default theme) | PASS | Folded into the operator's "#4 pass." |
| ii | Hover at idle: still reads as a ring, not a solid filled disc (the exact run-1 F-34.10-02 symptom) | PASS | Same. |
| iii | Active: the arc fills and advances as the download progresses | PASS | Explicitly named by the coordinator's relay ("all sub-checks including the arc filling and advancing"). This is the exact sub-check F-34.10-02 cause (b) broke in run 1. |
| iv | Active: the count badge appears and is correct | PASS | Folded into "#4 pass." |
| v | Clicking the ring reaches `/download-manager`, no tier-1 tab lit, ring shows active state | PASS | Folded into "#4 pass." |
| vi | **Discriminator (new).** While the same download runs, does `ProgressHeader`'s own percent readout on `/download-manager` (`ProgressHeader/index.tsx:113,122`, mounted unconditionally at `DownloadManager/index.tsx:111-115` — see §6.3's citation correction) advance past 0%? | PASS | Recorded per the coordinator's explicit "all sub-checks" framing. Honesty note: the operator's own raw report ("#4 pass") did not itemize this discriminator separately from the ring's own arc (iii) — since (iii) itself PASSed this run, the discriminator's diagnostic purpose (telling ring-local from app-wide on a FAIL) was not exercised. |

**Item 4 verdict** (PASS only if sub-checks i-v all PASS; vi is recorded regardless and does not
by itself gate the verdict, but a FAIL on vi combined with a FAIL on iii changes where the finding
is attributed — see the note column): `PASS`

**Operator report (verbatim):** "#4 pass."

This **CLOSES F-34.10-02** in full — both previously diagnosed causes: (a) the mask/idle-track
fix (34.10-14) and (b) the `RingProgress` unconditional-mount fix (34.10-15). Recorded explicitly,
because it matters and must not be lost: **34.10-15-SUMMARY.md flagged two unresolved risks that
this live measurement now FALSIFIES.**

- **Risk 1** (`key={head?.params.appName ?? 'idle'}` forcing a remount at the idle->active
  boundary, potentially undercutting the unconditional-mount fix's own premise) — FALSIFIED. The
  arc genuinely fills and advances during a real download, which could not happen if the
  `RingProgress` hook instance were still being torn down and reseeded at that boundary the way
  34.10-15's own SUMMARY worried it might be.
- **Risk 2** (H3's reconciliation-effect mechanism not surviving close re-reading of
  `hasProgress.ts:42-56` — the worry that the fix would produce at most one transient 0%-percent
  frame rather than a sustained fix) — FALSIFIED. The count badge, the arc's continued advancement,
  and the click-through to `/download-manager` all confirm the fix holds for the whole download,
  not just a single frame.

Both risks were explicitly flagged as open, unresolved questions by 34.10-15's own author, routed
to this exact gate item for adjudication ("Plan 34.10-16's live gate item 4 is the only
adjudicator, and it must be treated as a genuinely open question, not a formality" —
34.10-15-SUMMARY.md). This run answers that question: the fix works.

---

### Item 2 (run 2) — Real window dragging under WKWebView, ring surface only (REQ-34.10-04)

**Run fourth**, deliberately after items 1/3/4 (see §6.4 — toggling `framelessWindow` changes
window chrome and must not run before the visual observations above are recorded).

| Sub-check | Verdict | Notes (required if FAIL) |
|---|---|---|
| Frameless ON: drag by the Downloads ring does NOT move the window | PASS | Operator report: "#2 pass." |

The other four sub-checks (drag by empty navbar space DOES move the window; drag by a tab does
NOT; drag inside the window-controls area does NOT; frameless OFF native titlebar still drags)
stand on run 1's own measurement (all PASS, §4 above) and are not re-taken — only the ring's
stylesheet changed since run 1, and `resolveDragRegion`/`.NavShell__navbar` were untouched by any
of the three fix plans (§6.1).

**Item 2 verdict** (run 1's four sub-checks, all PASS, PLUS this run's re-measured fifth, also
PASS): `PASS`

*Leave `framelessWindow` OFF when this item finishes, restoring Preflight P6's starting state.*

---

### Item 5 (run 2) — Electron divergence (REQ-34.10-05)

**Run last.** Quit the Tauri dev build entirely and re-confirm `pgrep -f 'tauri dev'` is empty
before launching Electron (§6.4's action (e), and Preflight P3's own check re-applied), so the two
shells never run concurrently. Run `npm start`.

| Sub-check | Verdict | Notes |
|---|---|---|
| Shell renders | PASS | Operator report: "confirm the shell renders." |
| **(never previously reached before this run)** All 4 tier-1 tabs navigate | PASS | Operator report: "checked all four tabs." **This is the first time this sub-check has ever been measured** — NOT ATTEMPTED in run 1 (§4 above); a data gap now closed, not a routine pass. |

The traffic-light observation from run 1 (NO overlap, recorded per D-03's "record, don't fix"
framing) stands and is not re-taken.

**Item 5 verdict** (PASS only if both sub-checks PASS): `PASS`

**Operator report (verbatim, correction relayed after an initial "not reached" report proved
premature):** "confirm the shell renders, checked all four tabs."

Both sub-checks are measured and PASS. **Correction note, recorded for the honest record:** this
item was first reported to this executor as NOT ATTEMPTED (Electron run not yet performed at that
point in the session), then the coordinator relayed a correction that the operator had in fact
run it. Both states are recorded here in the file's own history rather than silently overwritten —
the git history for this file shows the intermediate NOT ATTEMPTED state; this is the corrected,
final one.

---

## 9. Run 3 — scope, preflight, and re-derived review

Authored by 34.10-21 (three tasks: scope+preflight, reachability+pairwise review, item bodies), a
separate, later invocation from the one that runs it (decision D-E, restated from §3 and §6).
**This section authored the contract; it did not run it.** At authoring time every verdict slot
below carried a literal pending placeholder, and plan 34.10-22 was the only invocation permitted to
fill them. **Run 3 has since been executed by 34.10-22 and every slot is now resolved** — see §9.5.

> *Recorder's note (34.10-22).* This paragraph and §9.5's preamble originally spelled the pending
> placeholder out verbatim. Task 1's own automated check greps the whole of §9 for that literal
> string and fails if it appears anywhere, so the check was **unsatisfiable by construction**: the
> authoring prose describing the placeholder tripped the same assertion as a genuinely unfilled
> slot. This is an eighth contract-authoring defect of the kind Section 2 of
> `live-gate-contract-authoring.md` catalogues, and a new class — a **verification/prose collision**,
> where the check cannot distinguish a scored cell from a sentence about scored cells. Resolved by
> restating both passages in the past tense without quoting the literal, which changes no verdict.
>
> The same collision class fired **twice more** in this run, and all three are recorded together so
> the pattern is visible rather than filed as three papercuts: (2) §9.2's Preflight Results block is
> authored as `[x] pass  [ ] fail` multiple-choice rows, while Task 1's check rejects **any** `[ ]`
> in that block — so a correctly-filled preflight could never satisfy it, since every selected row
> necessarily leaves its unchosen options unticked. Resolved by stating each selected value and
> naming the rejected options in prose instead of leaving empty boxes. (3) The check counts verdict
> cells by the literal pattern `| PASS |`, so **Markdown emphasis around a verdict breaks it** —
> this recorder's initial `**PASS**` formatting reduced 32 resolved cells to 4 detected ones. All
> three are cosmetic-to-fix and changed no measurement, but each would have read to a later reader
> as "the gate did not complete." The generalisable lesson for the next contract: **an automated
> gate check must be run against a filled specimen at authoring time**, not only reasoned about —
> plan 34.10-21 authored three checks it never once executed against a completed example.

Four fixes landed since run 2, all read in full to author this section: 34.10-18 (navbar
`border-bottom: 1px solid var(--body-background)` completing the seam-erasure recipe, plus a
mid-plan coordinator correction of that same token from an initially-defective `var(--divider)`
choice; relocation of the scroll container from `document.body` to `.App .content`; migration of
both `document.body` scroll consumers), 34.10-19 (`.NavTabs .MuiTab-root`'s content-derived
`min-height` fixing the second-row wrap, plus a handed-off `.Mui-selected` border-color fix),
34.10-20 (`.dropdown`'s background changed from `var(--background)` to `transparent`), and
34.10-17 (the diagnosis both 19 and 20 implement). §9.1-§9.4 below re-derive scope, preflight and
review against what these four artifacts actually changed — confirmed against each SUMMARY's own
`key-files.modified` list, not assumed from plan numbers — rather than copying run 2's §6, which
predates all four.

**A live gate has beaten a green test suite three times in this project, and this exact gap cycle
produced a fourth, closer-to-home instance of the same lesson:** 34.10-18's own Task 1 committed a
seam-border fix (`var(--divider)`) that its own unit gate suite passed 100% on, reasoned as "the
same relational token `.Mui-selected` already uses" and therefore theme-safe — and was wrong, in 9
of 11 themes, caught only by a coordinator's manual re-verification against `themes.scss`'s actual
structure. Nothing in §9.3 below infers a PASS from "the code looks unaffected"; every sub-check
this section marks REACHABLE still requires a live, human-driven observation in §9.5.

### 9.1 Scope, with per-item justification

| Item | Run-3 scope | Why |
|---|---|---|
| 1 | **FULL re-run, larger than run 2's.** Per theme, across `midnightMirage`, `gruvbox_dark`, `dracula` and the non-blocking `dracula-classic` spot-check: (a) the tab/content merge seam, (b) the idle Downloads ring's visibility and ring-shape. **Never reached in either run** — all eight run-2 cells are NOT ATTEMPTED (§7 above). PLUS four theme-independent sub-checks, one per fix, each phrased so a failure is observable: F-34.10-03 (no visible band/gap between the tier-1 strip and the content region — `NavShell/index.scss:116`'s `border-bottom: 1px solid var(--body-background)`), F-34.10-04 (wordmark + tab strip + Downloads ring read as ONE line — `NavTabs/index.scss:51`'s `min-height: calc(...)`), F-34.10-06(a) (navbar stays fixed at the top while a long route scrolls to its end and back — `App.css:56`'s `height: 100vh; overflow: hidden` plus `App.css:96-97`'s `.App .content { overflow-y: auto; min-height: 0 }`), F-34.10-06(b) (a scrollbar starts BELOW the navbar's bottom edge, never draws over it — same citation, since `.App .content` at `App.tsx:103`'s `<main className="content">` is now the sole scroll region and the navbar's own grid row, per `App.css`'s `grid-template-areas`, sits structurally above it). PLUS **two** regression sub-checks for the scroll relocation itself (34.10-18 moved the scrolling element out from under `document.body` and migrated every consumer): mouse/keyboard back-to-top behaviour (`Library/index.tsx:277-332`) and gamepad focus-scroll (`GamesList/index.tsx`'s `scrollCardIntoView`, lines 46-90 and its gating `useEffect` at lines 137-151). Both exist because the migration fails silently — a scroll listener or a `scrollTo()` on a no-longer-scrolling element throws nothing, logs nothing, and fails no test (confirmed: `Frontend` jest is `testEnvironment: 'node'`, no jsdom, cannot observe a real scroll). **Escape hatch, explicit:** if no game controller is available, the gamepad sub-check is recorded `NOT ATTEMPTED` with "no controller available" as the reason; it does not by itself fail item 1, but must be carried forward into the phase's closing record as unmeasured rather than dropped — it is never inferred from the mouse/keyboard sub-check passing, since `scrollCardIntoView`'s arithmetic (container-rect-relative, `containerRect.top`/`containerRect.bottom`) is exercised only by the gamepad path; the mouse/keyboard `backToTop()` handler is a separate code path entirely. |
| 2 | **FULL re-run, all 5 sub-checks — NOT carried forward, despite the source-level case for doing so.** `resolveDragRegion` (`tauriWindowChrome.ts:301-334`) and `.NavShell__navbar`'s class/markup are untouched by any of 34.10-18/-19/-20 (confirmed against all three SUMMARYs' `key-files.modified` lists); 34.10-19's `.MuiTab-root` `min-height` change alters tab height only, not the `button`/`[role="button"]` exclusion `resolveDragRegion` checks BEFORE the navbar fallback (`tauriWindowChrome.ts:322-329`), since a MUI `<Tab>` renders as a native `<button>` regardless of its CSS height. By that reasoning alone, all 5 sub-checks could stand on run 1's/run 2's own measurement, exactly as run 2 did for 4 of 5. **This plan deliberately does NOT take that shortcut**, for a reason specific to this gap cycle: 34.10-18's own Task 1 committed a seam-border fix to this SAME element (`.NavShell__navbar`) reasoned as theme-safe by source inspection alone, and a coordinator's live re-verification found it broken in 9 of 11 themes despite full unit-gate coverage. The identical failure mode — "the source reasoning says this element is unaffected, therefore skip re-measuring it" — is exactly what produced that defect. Re-testing five quick drag/no-drag gestures costs the operator well under a minute; an unfounded carry-forward on the one element in this contract that has ALREADY been wrong once this gap cycle is not a trade worth making. |
| 3 | **FULL re-run, all 7 sub-checks, plus F-34.10-05.** Sub-checks (vi) and (vii) have never been reached in either run and must be measured, never inferred — `NavShell/index.tsx`'s `tier2Collapsed` (lines 50-53) and `navTabs.ts`'s `resolveActiveTab` (line 37) are both untouched by 34.10-18/-19/-20, so run 2's own REACHABLE verdict for these sub-checks (§6.3) still applies unchanged, but "still reachable" is not "already measured." Add an F-34.10-05 sub-check: with the Categories panel expanded, its background reads as a deliberate surface belonging to the tier-2 column (`Dropdown/index.scss:43`'s new `background: transparent`, compositing onto `.NavShell__tier2`'s own `var(--navbar-background)`) rather than a black block — judged in `gruvbox_dark` AND `dracula` (the two themes `34.10-F04-DIAGNOSIS.md`'s own W1-verdict evidence was measured against), and explicitly NOT in `midnightMirage` alone, where `--background` and `--navbar-background` happened to coincide, making the pre-fix defect invisible by construction in that one theme. |
| 4 | **REDUCED.** DownloadsRing's own files (`NavShell/components/DownloadsRing/index.tsx`, `index.scss`) were touched by NEITHER 34.10-18, -19, nor -20 (confirmed against all three SUMMARYs' `key-files.modified` lists — none names `DownloadsRing/`). Sub-check (i) (idle: ring visible + dimmed + ring-shaped) is now ALSO scored per-theme inside item 1's own per-theme sweep (same visual judgement, same element, same code); it is NOT independently re-scored here, to avoid double-counting the identical observation under two different item numbers — item 1's per-theme idle-ring column is the sole record of it for run 3. Sub-checks (ii)-(vi) (hover-doesn't-degrade-to-a-solid-disc, active arc fill, count badge, click-through, the `ProgressHeader` discriminator) are eligible to stand entirely on run 2's own PASS measurement, since nothing in their code path changed — but per this contract's own convention (§9.5), the runner still writes an explicit verdict for each rather than silently inheriting run 2's cell. **Explicit condition, so this scope decision cannot silently paper over a regression:** if item 1's per-theme idle-ring sweep surfaces ANY FAIL, item 4 must be reopened in full at the next gap cycle regardless of what its own carried-forward sub-checks show, since a FAIL there would mean the shared component's idle rendering has regressed since run 2 by a mechanism this contract's source review did not anticipate. |
| 5 | **FULL re-run, both sub-checks — judged cheap, not judged necessary.** Neither `electron/`, `src/backend/main_window.ts`, nor any Electron-only branch is touched by 34.10-18/-19/-20 (confirmed against all three SUMMARYs), so nothing Electron-specific changed. But `App.css` and `NavShell/index.scss` — both shared, unconditionally, by whichever shell renders `<NavShell />` and `.App` — DID change (the scroll-container relocation and the seam border). Electron is deprecated (Phase 35 deletes it entirely) and this item's own D-03 framing is "record, don't fix," not a blocking correctness gate on the same footing as items 1/3/4 — so re-measurement is not judged NECESSARY. It is judged CHEAP: launching `npm start` and clicking 4 tabs costs under two minutes, and doing so removes any question about whether the shared-CSS changes rendered oddly under Chromium's engine (a different question from whether they render correctly under WKWebView, which items 1/3 already cover). The run-1 traffic-light observation (NO overlap, D-03's "record, don't fix" framing) stands and is not re-taken. |

### 9.2 Preflight for run 3

| # | Check | Command / action | Why this item's premise depends on it |
|---|---|---|---|
| P1 | Build matches the committed tree | `pnpm jest --runInBand --silent --forceExit` (this phase's documented `test:ci` hang workaround — **not** bare `pnpm test:ci`). **Derived from the current tree at this plan's own authoring time (2026-08-08), not copied from any prior SUMMARY** — a live run at authoring time observed **219 suites / 4269 tests, 0 failing, fully green.** Note for the runner: 34.10-19-SUMMARY.md's own text also states 219/4269 after its own fix, while 34.10-20-SUMMARY.md's own reconciliation math computes 4264 (4259 + its own +5) without accounting for 34.10-19's claimed +10 — an unresolved 5-test discrepancy between those two SUMMARYs that this plan does not attempt to settle by inference. The authoring-time live run above (219/4269, zero failures) is the most concrete evidence available and is what run 3 should expect if the tree is unchanged between now and the live run; write BOTH the expected (219 suites / 4269 tests) and the observed numbers into the results block below, and if they differ, say by how much before proceeding. Two previously-flagged conditions were NOT observed at authoring time (both suites fully green, no `hardcodedStringGate` failure, no `bootstrapWirings` flake) — if either reappears by run time, name it explicitly rather than treating an unexplained new failure as either of these known ones by assumption. |
| P2 | `pnpm codecheck` clean | `pnpm codecheck` | Confirms no un-typechecked drift entered the tree since 34.10-20. |
| P3 | **Exactly one app process** before the FIRST launch | `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'` (or the platform-appropriate process name) both empty | This project's own recorded capture-integrity trap (F-34.4.2-15/-18) — a concurrent second instance does not announce itself, it splits the `[shell]` sink while `gamelib.log` stays shared, producing a half-captured run that LOOKS fully measured. This has already recurred at least twice in this phase (`34.10-F02-DIAGNOSIS.md`'s own precondition census, and again per §6.2's P3 row). If either command returns a PID, kill it or identify whose it is before launching, and re-check before the Electron launch (item 5) too. |
| P4 | Library has a game with a distinctive, unique-substring title AND a game whose page can be opened | Check the Games tab once the app is up | Item 3's search sub-check (i) needs a title to type; sub-checks (vi)/(vii) need an openable game page. If the library is empty, record the affected item-3 sub-checks NOT ATTEMPTED with this as the reason — never inferred. |
| P5 | At least one game available to start a download | Confirmed live during item 4 | Item 4's active sub-checks (ii)-(vi) need something installable, even though their run-3 disposition per §9.1 is "eligible to carry forward" — the runner may still choose to re-drive them, and P5 is what makes that choice possible. |
| P6 | `settings.framelessWindow` is OFF (default) at the start | Settings -> General -> "Frameless window" toggle unchecked | Items 1, 3, 4 must be observed in the default decorated state — see §9.4's pairwise review, which is why item 2's toggle runs after items 1/3/4, not before. |
| P7 | **The download queue is EMPTY at the start** | Check `/download-manager` and the navbar ring before starting anything | Item 1's per-theme idle-ring sub-checks and its default-theme F-34.10-0x finding sub-checks all need an idle ring; item 4's decision to fold sub-check (i) into item 1 depends on item 1 actually observing the idle state, not an already-active queue. Starting item 4's download destroys this for the rest of the session (§9.4's DESTROYS row) — if the queue is not empty at the start, record the affected item-1 sub-checks NOT ATTEMPTED with this as the reason. |
| P8 | **New for run 3, and load-bearing.** The Games grid is long enough to produce an active scrollbar and to scroll past the 450px back-to-top threshold (`Library/index.tsx:324`), with no category or filter left applied from a previous session | Check the Games tab's card count / scroll behaviour before starting | Item 1's F-34.10-06(b) sub-check and the mouse/keyboard back-to-top regression sub-check are both unmeasurable without it. If the grid does not scroll, those sub-checks are recorded NOT ATTEMPTED with this named as the reason — never inferred from "the navbar looked fine." §9.4 also ties this row to item 3's own filtering action, since applying a category/filter SHORTENS the grid and can retroactively invalidate this precondition for anything measured after that action — this is why item 1 (which needs P8 intact) runs before item 3 (which can violate it) in the derived run order below. |
| P9 | **New for run 3.** Whether a game controller is connected and recognised by the app (`activeController` in `ContextProvider`, gating `GamesList/index.tsx:137-151`'s focus listener) | Check the app's controller-detection state (e.g. Settings, or any existing in-app indicator) once the app is up | Item 1's gamepad focus-scroll sub-check is unmeasurable without it — the listener is not even attached when `activeController` is falsy. Record the literal answer; if no controller is available, that sub-check is recorded NOT ATTEMPTED with this row named, never inferred from the mouse/keyboard sub-check passing (different code path, different arithmetic — see §9.1's item-1 row). |

**Preflight Results (fill before touching Item 1 — left empty by this plan; filling this in would make 34.10-21 both author and runner, violating decision D-E):**

```
P1: [x] PASS (with a named known flake; the `fail` option is NOT selected)
    -- expected: 219 suites / 4269 tests, 0 failing
    (34.10-21's authoring-time live run, 2026-08-08) -- observed: 219 suites / 4269 tests
    -- failing: 1 suite / 1 test.
    Suite and test counts match the expected numbers EXACTLY (219/4269), so the unresolved
    34.10-19/34.10-20 SUMMARY 5-test discrepancy did NOT resurface and is not implicated here.
    The single failure is `src/backend/sidecar/__tests__/bootstrapWirings.test.ts:368`
    (`expect(contents).toBe(mockGamesJsonText)`, received ""), which is the `bootstrapWirings`
    flake §9.2's own P1 row named in advance as a previously-flagged condition. It is NAMED here
    explicitly rather than assumed: per this project's own `flake-baselines-can-be-undiagnosed-bugs`
    lesson, the single-file repro was run before accepting the baseline --
    `pnpm jest src/backend/sidecar/__tests__/bootstrapWirings.test.ts --runInBand --silent
    --forceExit` => 1 suite / 7 tests, ALL PASS, 1.579 s. Passing in isolation and failing in the
    full run makes it order-dependent, matching the recorded flake shape. The other previously
    flagged condition (`hardcodedStringGate`) did NOT appear.
    Bearing on this gate: NONE. The failing test is a sidecar backend wiring test; no item in §9.5
    depends on it, and per §5 and Section 4(ii) of `live-gate-contract-authoring.md` a green suite
    could not have closed any item here anyway (`Frontend` jest is `testEnvironment: 'node'`,
    no jsdom, no CSS engine).
P2: [x] PASS (the `fail` option is NOT selected) -- `pnpm codecheck` (`tsc --noEmit`) exit 0,
    no diagnostics emitted.
P3: [x] FOUND AND KILLED (neither `clean (0 pre-existing processes)` nor `found, NOT killed`
    is selected)
    -- FIRST CHECK, run by the executor rather than relayed (2026-08-08, before any launch):
       `pgrep -f 'tauri dev'`            => 8881, 8931   (NOT empty)
       `pgrep -f 'target/debug/gamelib'` => 9024  (`target/debug/gamelib-shell`, NOT empty)
    All three started 18:25:45. This is the P3 trap firing for the third recorded time in this
    phase.
    -- ESCALATION, found while identifying those PIDs: the pre-existing instance was serving a
       PRE-FIX bundle, which would have made the run unscorable in a way the process count alone
       does not reveal. `src-tauri/tauri.conf.json` sets `frontendDist: "../build"` -- a static
       directory, NOT a dev server -- so `tauri dev` serves whatever `electron-vite build` last
       wrote. `find build -type f -newermt "2026-08-08 19:40"` returned NOTHING, while the four
       fixes this run exists to measure landed at 19:44 (1139bf1db seam border), 19:46 (0ff4fe6fd
       scroll relocation), 20:06 (4a3a3d830 seam-token correction), 20:10 (3744163f3 panel
       surface) and 20:21 (df351c087 tab min-height). Scoring against that instance would have
       recorded FAIL on all four of F-34.10-03/-04/-05/-06 and attributed it to fixes the running
       bundle never contained.
    -- RESOLUTION: processes killed. RE-CHECK by the executor (2026-08-08, resumed session,
       before the first scored launch):
       `pgrep -f 'tauri dev'`            => no output (EMPTY)
       `pgrep -f 'target/debug/gamelib'` => no output (EMPTY)
       `ps -Ao pid,lstart,command | grep -Ei 'tauri|gamelib'` => no app process of any kind;
       the only matches are two long-running log readers on `gamelib.log` (PIDs 82623, 85359,
       `tail -n 0 -F` / `tail -f`), which are not app instances and do not split the sink.
       P3 therefore resolves as `found and killed` -> CLEAN at the moment of this record.
    -- BUNDLE-STALENESS RE-CHECK (the escalation above, RE-MEASURED rather than assumed to be
       resolved by the kill): `find build -type f -newermt "2026-08-08 19:40"` => 0 files;
       `build/` dir mtime is Aug 8 19:28. The five fixes this run exists to measure
       (19:44 1139bf1db, 19:46 0ff4fe6fd, 20:06 4a3a3d830, 20:10 3744163f3, 20:21 df351c087)
       are STILL ABSENT from the served bundle. `frontendDist: "../build"` is a static dir,
       so the launch MUST be `pnpm tauri:dev`
       (= `electron-vite build && pnpm build:sidecar && tauri dev`) and never a bare
       `tauri dev`, or run 3 scores a pre-fix bundle exactly as run 2's instance would have.
       [AWAITING OPERATOR LAUNCH — no scored observation may be taken before this is done]
LAUNCH: operator launched via `pnpm tauri:dev` (the full command, so `electron-vite build`
    regenerated `build/` and the five fixes are present in the served bundle — this is the
    condition the escalation above established as mandatory).

P4: [x] confirmed -- distinctive title: `king` / *Kingdom Come* (deferred at preflight, RECORDED
    at item 3(i) when it was actually typed, as promised rather than left blank)
    -- openable game page: confirmed available, and exercised live by item 3(vi)/(vii)
P5: [x] confirmed -- installable game: confirmed available
P6: [x] confirmed OFF
P7: [x] confirmed EMPTY (0 items in queue, ring shows idle state)
P8: [x] confirmed -- grid scrolls past 450px, no leftover category/filter applied: confirmed
P9: controller connected and recognised: [x] NO (the `yes` option is NOT selected)
    -- gamepad sub-check recorded NOT ATTEMPTED
    -- operator's own words: "dont have a controller to test right now". Per §9.1's explicit
       escape hatch this does NOT by itself fail item 1, but it is carried forward into the
       phase's closing record as UNMEASURED, and is never inferred from the mouse/keyboard
       sub-check (step 5) passing -- different code path, different (container-rect-relative)
       arithmetic.

RECORDING NOTE on P4-P8, so a reader can judge the evidence rather than trust it: these five
    were confirmed by the operator as a single batch response ("done steps 1-5"), not elicited
    as five separately-answered rows. They are yes/no preconditions, not scored verdict cells,
    so a batch confirmation is legitimate here -- but it is flagged explicitly because this
    contract's own §9.5 rule forbids batch answers for the scored per-theme cells, and the
    distinction should not be blurred by silence. P4's distinctive-title string in particular
    was NOT captured at preflight and is deferred to item 3(i).
```

### 9.3 Structural reachability review for run 3

Re-derived, not copied, applying all seven defect-class tests (`live-gate-contract-authoring.md`)
to every run-3 sub-check. Particular attention below goes to sub-checks never previously measured,
since an unreachable scenario is exactly how run 1's item-3 sub-checks went unrecorded in the first
place (§3), and how phase 34.4.2 shipped an item whose own gesture sequence the UI forbade
(F-34.4.2-17).

| Item / sub-check / precondition | Surface | What it asks for | Verdict | Evidence |
|---|---|---|---|---|
| Item 1, all 4 themes, tab-merge seam | Settings -> Accessibility theme picker, unmodified by any of 18/19/20/17 | Switch theme, click 4 tabs, judge seam by eye | REACHABLE | Unchanged from run 1's/run 2's own Test-1/Test-2-N/A finding; no fix plan touched the theme picker. |
| Item 1, all 4 themes, idle Downloads ring | `DownloadsRing/index.tsx:72`'s `isIdle = elements.length === 0` (unconditional, unchanged since run 2) | Judge whether the idle ring is visible, dimmed, ring-shaped, in each theme | REACHABLE, CONDITIONAL on P7 | Same as run 2's own finding (§6.3); code path untouched by 18/19/20. |
| Item 1, F-34.10-03 (seam gap) | `NavShell/index.scss:116` | Confirm no visible band/gap | REACHABLE | Declaration confirmed present in current source at the cited line; this IS the fix's own location, not an inference from a plan number. |
| Item 1, F-34.10-04 (single line) | `NavTabs/index.scss:51` | Confirm wordmark/strip/ring share one line | REACHABLE | Declaration confirmed present in current source at the cited line. |
| Item 1, F-34.10-06(a) (navbar stays pinned) | `App.css:56` (`.App { height: 100vh; overflow: hidden }`), grid-template-areas placing `navbar navbar` in its own row above `content` | Scroll a long route to its end and back, confirm the navbar never moves | REACHABLE, CONDITIONAL on P8 | `.App`'s fixed height + `.App .content`'s own `overflow-y: auto` (line 96) means the navbar's own grid row structurally cannot scroll — but "structurally cannot scroll" is a source-level claim, and this exact class of claim (source reasoning standing in for a live measurement) is what 34.10-18's own coordinator-caught defect falsified once already this cycle for the sibling border-bottom fix. Kept REACHABLE, not asserted PASS. |
| Item 1, F-34.10-06(b) (scrollbar below navbar) | `App.tsx:103`'s `<main className="content">`, the sole scroll region per the above | With a scrollbar active, confirm it starts below the navbar's bottom edge at every scroll position | REACHABLE, CONDITIONAL on P8 | **Test 4 (absence-observability) case.** The PRESENCE signal this sub-check is watching for: if `.App .content` were NOT the true scroll owner (e.g. a regression re-introduced `document.body` scrolling), the OS/webview scrollbar would draw against the full window height, starting at the very top and visibly overlapping the navbar's own row — an observably different, wrong picture. "No scrollbar was visible at all" must be recorded distinctly from "the scrollbar is correctly positioned below the navbar" (the former is P8 unmet, not a PASS). |
| Item 1, mouse/keyboard back-to-top regression | `Library/index.tsx:277-332`, `main.content` query at line 291, 450px threshold at line 324 | Scroll down past 450px, click "back to top", leave the tab and return, confirm scroll behaviour holds | REACHABLE, CONDITIONAL on P8 | Confirmed directly in source: the effect resolves `main.content` (App.tsx:103's element) with a `document.body` fallback, and the 450px visibility threshold is a literal in the cited line, not inferred. |
| Item 1, gamepad focus-scroll regression | `GamesList/index.tsx:137-151`'s `useEffect(..., [listRef.current, activeController])`, `scrollCardIntoView` at lines 46-90 | Navigate the grid with a controller far enough that focus moves past the visible rows, confirm auto-scroll keeps the focused card in view, both directions | **REACHABLE, CONDITIONAL on P9** (and P8, since a grid too short to scroll makes the scenario vacuous even with a controller present) | The listener is attached ONLY when `activeController` is truthy (confirmed at the cited `useEffect` dependency array) — this is a Test 4 case as well: the PRESENCE signal is the grid visibly auto-scrolling AND the focused card actually ending up inside the container's own `getBoundingClientRect()` bounds (per the container-relative arithmetic at lines 46-90), so "nothing moved" cannot be misread as a pass, and a vacuously-short grid (P8 unmet) makes the whole scenario untestable regardless of P9. |
| Item 3(vi): Games tab stays lit on a game page | `navTabs.ts:37`'s `resolveActiveTab` (`pathname === '/' \|\| pathname.includes('/gamepage')` returns `'games'`) | Confirm the scenario is producible | REACHABLE | Unchanged from run 2's own finding (§6.3) — this function is untouched by 18/19/20. Re-confirmed present at the cited line during this authoring pass. |
| Item 3(vii): tier-2 column collapses, no empty 204px band | `NavShell/index.tsx:50-53`'s `tier2Collapsed` | Confirm the condition is reachable from a game page | REACHABLE | Unchanged from run 2's own finding (§6.3) — confirmed present, same lines, untouched by 18/19/20. |
| Item 3: F-34.10-05 background sub-check, `gruvbox_dark` + `dracula` | `Dropdown/index.scss:43`'s `background: transparent` | Expand a disclosure panel, judge its surface against the tier-2 column's own backdrop | REACHABLE | Declaration confirmed present in current source. `midnightMirage` is deliberately excluded from scoring per §9.1's own reasoning — the pre-fix defect coincided invisibly in that one theme (`34.10-F04-DIAGNOSIS.md`'s W1 verdict), so a PASS there would prove nothing about whether the fix generalises. |
| Item 2, all 5 sub-checks | `resolveDragRegion` (`tauriWindowChrome.ts:301-334`), `.NavShell__navbar`, `.MuiTab-root`, `DownloadsRing`, `.windowControls` | Toggle frameless, drag by 4 surfaces + confirm native titlebar drag OFF | REACHABLE | `resolveDragRegion`'s interactive-element exclusion (`start.closest('a, button, [role="button"], input, select, textarea, .windowControls')`, lines 322-329) is checked BEFORE the `.NavShell__navbar` fallback (line 331) — a real branch that would fire and move the window if ever removed or mis-ordered (Test 4: presence case = the window moving). None of `tauriWindowChrome.ts`, `.NavShell__navbar`'s class/markup, or `.windowControls`'s markup were touched by 18/19/20 — only `.MuiTab-root`'s CSS `min-height` changed, which does not affect its `button` tag or `role` attribute. |
| Item 4, sub-checks (ii)-(vi) | `DownloadsRing/index.tsx`, `RingProgress`, `ProgressHeader` (`DownloadManager/index.tsx:111-115`) | Confirm active-fill, badge, click-through, discriminator | REACHABLE | Unchanged from run 2's own finding (§6.3) — none of these files were touched by 18/19/20. |
| Item 5: shell renders + 4 tabs navigate | `npm start` (`electron-vite dev --watch`), shared `NavShell`/`.App` tree | Launch Electron, click 4 tabs | REACHABLE | `git`/SUMMARY cross-check unchanged from run 2's own finding: none of 18/19/20 touch `electron/`, `main_window.ts`, or any Electron-only branch. `App.css`/`NavShell/index.scss` DID change, but both are shared CSS rendered by whichever shell mounts the same React tree — no Electron-specific code path is newly at risk. |

### 9.4 Pairwise review for run 3 (Test 5)

**Reduction applied:** every {state-mutating action} against every {evidence-bearing item or
sub-check}, per the skill reference's scoping rule — not the full cross product. State-mutating
actions in run 3: (a) theme switching (item 1's per-theme loop), (b) expanding a tier-2 disclosure
panel (item 3), (c) starting a download (item 4), (d) toggling `framelessWindow` (item 2), (e)
quitting Tauri to launch Electron (item 5), (f) the mouse/keyboard back-to-top scroll action (item
1's own regression sub-check), (g) the gamepad-driven scroll action (item 1's own regression
sub-check), (h) running `pnpm jest`/`pnpm codecheck` as preflight CI processes (P1/P2).
Evidence-bearing items/sub-checks: items 1-5's own verdicts, plus item 1's internal
finding-sub-check evidence and item 3's internal (vi)/(vii) evidence.

| State-mutating action | Against evidence-bearing item/sub-check | Destroys it? | Reasoning |
|---|---|---|---|
| (c) Starting a download (item 4) | Item 1's per-theme idle-ring sweep AND its default-theme idle observation | **YES — DESTROYS** | Carried forward from run 2's own finding, restated because item 1 is now longer: once a download starts, the idle state is gone for the rest of the session — there is no "un-start" without waiting it out or cancelling (which changes queue-history state a re-run should not silently rely on). Resolved by run order (item 1 before item 4) plus P7. |
| (b) Expanding a tier-2 disclosure panel (item 3) | Item 3's own later sub-checks (vi)/(vii) | **YES, if left open** | Carried forward from run 2's own finding (§6.4): an expanded panel changes `.NavShell__tier2Portal`'s scroll/height state before the game-page collapse sub-check is observed. Resolved by requiring both disclosures explicitly closed before (vi)/(vii) run — restated in item 3's own steps in §9.5. |
| (b) Expanding a tier-2 disclosure panel, or applying a category/filter (item 3) | **NEW for run 3.** Item 1's P8-dependent sub-checks: F-34.10-06(b) (scrollbar position) and the mouse/keyboard back-to-top regression | **YES, if item 3 runs before item 1's scroll sub-checks are recorded** | Applying a category or filter SHORTENS the Games grid — exactly the precondition (P8) item 1's scrollbar and back-to-top sub-checks depend on to be non-vacuous. If item 3's filtering runs first and is left applied, item 1's scroll sub-checks become unmeasurable (grid too short to scroll) without any error or signal — a silent precondition violation, not a crash. **Resolved by run order: item 1 (which needs P8 intact) runs BEFORE item 3 (which can violate it).** This is a new constraint this run's own longer item 1 introduces; run 2's contract never depended on P8 at all. |
| (d) Toggling `framelessWindow` (item 2) | Items 1, 3, 4 | **YES, if run before them** | Carried forward from run 1's/run 2's own finding (§3/§6.4), restated because it still holds: toggling frameless mid-run changes window chrome (`setDecorations()`), which could visually alter the navbar's rendered box model while items 1/3/4 are being judged by eye. Resolved by run order — item 2 runs after items 1, 3, 4. |
| (a) Theme switching, item 1's own per-theme loop | Item 1's own theme-independent finding sub-checks (F-34.10-03/-04/-06a/-06b) AND its own regression sub-checks (f, g) | **YES, if interleaved** | **New pair this run introduces, since run 2 never reached per-theme scoring at all.** These finding/regression sub-checks are, by their own design, theme-independent (their fixes are CSS declarations that do not vary by theme). Interleaving the theme-switch loop with them risks a theme change landing mid-observation of a scroll/seam state, and adds no evidence value since the outcome should not vary by theme. **Resolved by sequencing:** run the four finding sub-checks and both regression sub-checks ONCE, in the default theme (`midnightMirage`), BEFORE beginning the four-theme sweep — stated explicitly in item 1's own step list in §9.5, not left to operator discretion. |
| (f) Mouse/keyboard back-to-top action | (g) Gamepad focus-scroll action | No, but noted | Different code paths, different arithmetic (§9.1's item-1 row) — neither invalidates the other's own PASS/FAIL evidence. The mouse/keyboard action may leave a non-zero scroll position persisted via `Library/index.tsx`'s session-storage restore effect (line ~298), which becomes the gamepad sub-check's own starting point; this does not destroy either sub-check's evidence, but the runner should record the starting scroll state if it seems to matter to what "moved" means for the gamepad check. |
| (h) Running `pnpm jest`/`pnpm codecheck` (P1/P2) | The single Tauri dev instance (P3) | No | Both run as separate `node`/`tsc` CI processes against the source tree; neither restarts, touches, or otherwise interferes with a running `tauri dev`/`gamelib` process. Explicitly considered because "anything that RESTARTS the app or a process" is on the hunting list this project's own reference names (`live-gate-contract-authoring.md` §Test 5) — this pair was checked and found NOT to apply. |
| (d) `framelessWindow` toggle (item 2) | Item 5 (Electron) | No | Carried forward: Electron reads its own decoration config independent of Tauri's `settings.framelessWindow`, and item 2's own closing step restores the toggle OFF before item 5 runs. |
| (c) Download start (item 4) | Item 2 (dragging) | No | Carried forward: a queued/active download does not change window chrome or drag-region CSS. |
| (c) Download start (item 4) | Item 5 (Electron) | No | Carried forward: separate process, no bearing. |
| (e) Quit Tauri / launch Electron (item 5) | Items 1-4's recorded verdicts | No | Carried forward: verdicts are written into THIS FILE as each is measured (§5's rule, restated at the top of §9.5), not held only in the running app's memory — quitting Tauri cannot erase a markdown file. |

**Pairs considered:** 12 (8 distinct state-mutating actions × the evidence-bearing items/sub-checks
each is plausibly relevant to, reduced by irrelevance — e.g. theme switching was not paired against
item 5, which is visually theme-independent by design per D-03, and (h) was only paired against P3
since P1/P2 have no plausible bearing on any visual item). **Pairs flagged:** 5 — (c) against item
1's idle evidence, (b) against item 3's own (vi)/(vii), (b)/filtering against item 1's P8-dependent
sub-checks (new this run), (d) against items 1/3/4, and (a) against item 1's own
finding/regression sub-checks (new this run) — all five resolved by run order or explicit
within-item sequencing stated in §9.5, none left open.

### Test 3 (log-line emitter reachability) — explicitly N/A for this whole contract

Restated from §3/§6.3: no item's PASS criterion in run 3 depends on a terminal transcript,
`gamelib.log`, or any sidecar/Rust log line — every criterion is a direct visual/interactive
observation, including all of run 3's new sub-checks (the four F-34.10-0x finding checks, the two
scroll-regression checks, and the F-34.10-05 per-theme check). The dual-sink evidence standard
therefore does not apply to this run in its log-capture form; §3's single-instance rule (P3 above)
still applies regardless, since a second concurrent instance could still produce visually confusing
or contradictory windows even with no log sink to split.

### Run order, derived (not asserted)

From the DESTROYS rows above: item 1 must precede item 4 (idle-ring DESTROYS pair); item 1 must
also now precede item 3 (the new P8-dependent DESTROYS pair — item 3's filtering can shorten the
grid before item 1's scroll sub-checks are recorded); items 1, 3 and 4 must all precede item 2
(chrome-mutation DESTROYS pair); item 5 must run last (separate-process pair, and practically
requires Tauri fully quit first, per P3 re-checked before that launch). Within item 1 itself, the
theme-independent finding and regression sub-checks must run before the four-theme sweep begins
(interleaving DESTROYS pair). Within item 3, both disclosures must be closed before sub-checks
(vi)/(vii) run.

**Applying these constraints in order produces exactly `[1, 3, 4, 2, 5]` — unchanged from run 1's
and run 2's own derived order.** This is stated explicitly, not silently: the new pairs this run
introduces (item 1 before item 3; item 1's internal sequencing) both happen to already fall out of
the SAME ordering run 1/run 2 established for different reasons (item 3 was already placed after
item 1 to avoid an in-progress download's UI cluttering item 3's own grid judgement — see §6.4's own
reasoning for item 3's placement between items 1 and 4). No change to the frontmatter `run_order`
field is needed; it already reads `[1, 3, 4, 2, 5]`.

---

### 9.5 Run 3 — the items

**Restated so the operator does not have to scroll:** the phase closes only on 5/5 — `verdict:
pass` may be set only if `items_passed == 5`. **NOT ATTEMPTED is never inferred** — an unreached
sub-check is recorded `NOT ATTEMPTED` with its reason, never inferred as a PASS from a green test
suite or a prior run's source-level reasoning, and never inferred as a FAIL from silence. **Every
verdict is written into this file as it is measured, not batched at the end.** Use exactly `PASS`,
`FAIL` or `NOT ATTEMPTED` as the only permitted final values in every slot below — at authoring
time every slot carried a pending placeholder, and no slot in this section could be filled by
anything other than plan 34.10-22, which filled them during run 3.

Run order (from §9.4's derivation): **1, 3, 4, 2, 5.**

---

#### Item 1 (run 3) — Theme survival, seam/single-line/scroll-pinning findings, and scroll-regression checks (REQ-34.10-06)

**Run first**, with Preflight P7 (empty queue), P8 (scrollable grid) and P9 (controller presence)
confirmed. Launch `pnpm tauri:dev` — this item is Tauri/WKWebView-only; `pnpm start` (Electron)
cannot exercise it.

**Step 1 — in the default theme (`midnightMirage`), before switching any theme** (per §9.4's
own derived sequencing, so a theme change never interleaves with these):

1. Judge F-34.10-03: on the Games tab, look for any visible band or gap between the tier-1 tab
   strip and the content region below the active tab.
2. Judge F-34.10-04: confirm the wordmark, the tab strip and the Downloads ring all sit on ONE
   visual line in the navbar, not two rows.
3. Judge F-34.10-06(a): on a route with enough content to scroll (the Games grid, per P8), scroll
   to the very end and back to the top; confirm the navbar never moves and stays fixed at the top
   of the window throughout.
4. Judge F-34.10-06(b): with the scrollbar active during that same scroll, confirm it starts BELOW
   the navbar's bottom edge at every scroll position and never draws over the navbar.
5. Mouse/keyboard regression: on the Games grid, scroll down past the point where the "back to top"
   button appears (450px, per `Library/index.tsx:324`); click it; leave the Games tab (e.g. to
   Settings) and return; confirm the grid's scroll position behaves sensibly (does not silently
   fail to restore, does not throw, does not leave the button permanently stuck visible/hidden).
6. Gamepad regression (P9-conditional): with a controller connected and recognised, navigate the
   Games grid with the D-pad/stick far enough that focus moves past the visible rows; confirm the
   grid auto-scrolls to keep the focused card in view, both downward (focus near the bottom edge)
   and upward (focus near the top edge). **If no controller is available, record this sub-check
   `NOT ATTEMPTED` with "no controller available" as the reason — this does not by itself fail item
   1, but must be carried forward into the phase's closing record as unmeasured, and must never be
   inferred from step 5's own result.**

| Sub-check | Verdict | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|
| F-34.10-03 — no visible seam/gap | FAIL | Operator's own words, verbatim: **"still a gap"**. Measured in `midnightMirage` against a bundle rebuilt by `pnpm tauri:dev` (so `1139bf1db` + `4a3a3d830`, the seam-border fix and its token correction, ARE present in the served build — this is not a stale-bundle artifact). **F-34.10-03 is NOT closed by 34.10-18.** No cause recorded here by design: diagnosis belongs to a later plan, and a guess written into a gate becomes tomorrow's falsified premise. |
| F-34.10-04 — wordmark/strip/ring share one line | FAIL | Operator's own words, verbatim: **"gamelib icon and download sit lower"**. The wordmark and the Downloads ring are vertically offset from the tab strip rather than sharing one line. Note this is a *different shape* from run 2's recorded observation (a full wrap to a second row) — it is now a vertical misalignment on what may be the same row. That distinction is recorded as observed, NOT diagnosed. **F-34.10-04 is NOT closed by 34.10-19** (`df351c087`'s `.MuiTab-root` `min-height`, confirmed present in the rebuilt bundle). |
| F-34.10-06(a) — navbar stays fixed while scrolling | PASS | Navbar stayed fixed at the top of the window through a full scroll of the Games grid to its end and back. 34.10-18's scroll-container relocation (`0ff4fe6fd`) holds. |
| F-34.10-06(b) — scrollbar starts below the navbar, never over it | PASS | Scrollbar observed active and correctly positioned below the navbar's bottom edge — the PRESENCE case per §9.3's Test-4 row, not the "no scrollbar was visible at all" case, which P8's confirmation independently rules out. |
| Mouse/keyboard back-to-top regression | PASS | Scrolled past the 450px threshold, back-to-top button appeared and worked; leaving the Games tab and returning behaved sensibly, with no stuck-visible/stuck-hidden button. `Library/index.tsx`'s migration off `document.body` survives. |
| Gamepad focus-scroll regression (P9-conditional; escape hatch applies) | NOT ATTEMPTED | **P9 unmet.** Operator's own words: "dont have a controller to test right now". `GamesList/index.tsx:137-151` attaches the focus listener only when `activeController` is truthy, so the scenario is structurally unreachable without hardware. Per §9.1's explicit escape hatch this does not by itself fail item 1, but `scrollCardIntoView`'s container-rect-relative arithmetic (lines 46-90) — which plan 34.10-18's scroll-container relocation directly affects — remains **UNMEASURED** and is carried into the phase's closing record as such. Never inferred from step 5's mouse/keyboard result: separate code path, separate arithmetic. |

**Step 2 — the four-theme sweep**, only after Step 1's six sub-checks are all recorded. For EACH
of the four themes in turn (`midnightMirage`, `gruvbox_dark`, `dracula`, `dracula-classic`): switch
the theme in Settings -> Accessibility, navigate to the Games route, click all four tier-1 tabs,
and record BOTH the seam cell AND the idle-ring cell for that theme before moving to the next one.
**A general, unscoped report ("looks fine across themes") is NOT a per-theme measurement and must
be recorded `NOT ATTEMPTED`** for whichever cells it does not distinctly address — this is exactly
what happened in run 2 (§7 above) and cost this sweep a third time; do not let it happen a fourth.

| Theme | Seam verdict | Idle ring verdict (visible, dimmed, ring-shaped) | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|---|
| `midnightMirage` (default) | FAIL | PASS | Seam carried from Step 1's own F-34.10-03 observation in this same theme: operator's verbatim words **"still a gap"**. Ring: see the provenance note below. |
| `gruvbox_dark` | FAIL | PASS | Operator switched the theme and re-judged; verbatim: **"other 2 have EXACTLY same results"** (i.e. identical to `midnightMirage`). Scored FAIL on that basis — a distinct per-theme judgement was made and reported, not inferred by this recorder. |
| `dracula` | FAIL | PASS | Same measurement as above, same verbatim quote. Notable because `dracula` is the theme this project's own sketch findings record as INVERTING navbar vs body backgrounds — the one most likely to diverge. It did not. |
| `dracula-classic` (spot-check, non-blocking, does not count toward `items_passed`) | NOT ATTEMPTED | NOT ATTEMPTED | **The theme does not exist in the shipped picker.** Operator's verbatim words: **"There is no dracula-classic option"**. This is a defect in §9.1's own scope, not an operator omission: the contract named a theme the UI does not offer — a Test-7 (UI-level reachability) miss made at authoring time by 34.10-21, which listed the theme without confirming the picker offers it. Non-blocking by its own declared status. |

**Idle-ring evidence provenance, stated so a reader can judge it rather than trust it.** The three
scored idle-ring cells rest on TWO operator statements taken together, not on three separately
elicited per-theme observations: (1) the cross-theme equivalence statement **"other 2 have EXACTLY
same results"**, made after switching themes and re-judging, which by its own wording covers both
scored questions and not only the seam; and (2) the ring's actual state, supplied when this recorder
noticed it had never been pinned down in any theme — operator's verbatim words: **"the ring i
dimmed, visible and ring shaped"**, which is exactly the three-part criterion this cell asks for
(visible + dimmed + ring-shaped, not a solid disc). Combining them gives PASS in all three scored
themes. This is weaker evidence than three independently narrated judgements would be, and is
flagged rather than smoothed over — but it is a real measurement of a real state, not an inference
from a green suite or from a prior run, and it is the FIRST time this column has carried any value
at all in three runs.

**Cross-theme observation (recorded as an observation, NOT a diagnosis — cause assignment belongs
to a later plan):** the seam FAILs **identically** across all three scored themes, including
`dracula`, whose navbar/body inversion makes it the most likely of the three to diverge. The first
completed per-theme sweep in this phase therefore establishes that the residual seam does **not**
vary by theme. What that implies about its cause is deliberately not written here.

**Item 1 verdict** (PASS only if: all 6 Step-1 sub-checks PASS — where the gamepad regression's
`NOT ATTEMPTED`-with-stated-reason is treated as non-blocking per its own escape hatch, but an
actual reachable FAIL on it DOES block — AND all 3 scored themes PASS on BOTH the seam and
idle-ring columns in Step 2): **FAIL**

Item 1 fails on two independent grounds, either of which alone is sufficient: (1) Step 1's
F-34.10-03 and F-34.10-04 sub-checks both FAILED against a bundle confirmed to contain their
fixes, and (2) all three scored themes FAILED the Step-2 seam column. What DID pass is recorded
just as plainly: F-34.10-06(a), F-34.10-06(b) and the mouse/keyboard back-to-top regression all
PASSED, so 34.10-18's scroll-container relocation — the largest and riskiest change in this gap
cycle — is measured as working, including the silent-failure shapes (`document.body` consumer
migration) that no unit test on this project can observe. The gamepad half of that regression
check remains UNMEASURED (P9, no controller). **The four-theme sweep was completed for the first
time in this phase**, resolving a column that had been NOT ATTEMPTED in both prior runs.

---

#### Item 3 (run 3) — Tier-2 portal, full functional contract + F-34.10-05 (REQ-34.10-09)

**Run second.** Before starting sub-checks (vi)/(vii), close any expanded disclosure (Categories or
Filters) — see §9.4's own pairwise finding.

| # | Sub-check | Verdict | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|---|
| i | Search box text visibly filters the grid | PASS | Operator typed the substring `king` against *Kingdom Come*; grid filtered visibly. This also retroactively fills P4's deferred distinctive-title slot with a real value. |
| ii | Categories control opens on CLICK (not mouseover), panel renders wholly inside the tier-2 column, every checkbox reachable, nothing extends past the window's left edge | PASS | Operator's verbatim words: **"on click, wholey renders"** — both halves of this sub-check (click-not-hover, and full containment inside the 204px column) confirmed. This is F-34.10-01's own subject matter re-confirmed still closed. |
| iii | Clicking the Categories trigger a second time closes it | PASS | Operator's verbatim words: **"closes on click"**. |
| iv | Toggling a category visibly changes the grid | PASS | |
| v | The Filters control behaves identically (click-toggle, contained, closes on second click) and a filter visibly changes the grid | PASS | |
| vi | With both disclosures closed, on a game page the Games tab stays lit | PASS | **First measurement of this sub-check in this phase** — NOT ATTEMPTED in both run 1 and run 2. `navTabs.ts:37`'s `resolveActiveTab` confirmed working live, not merely reachable. |
| vii | On that same game page, the tier-2 column collapses to nothing — no empty 204px band, no stray scrollbar during the collapse | PASS | **First measurement of this sub-check in this phase** — NOT ATTEMPTED in both run 1 and run 2. `NavShell/index.tsx:50-53`'s `tier2Collapsed` confirmed working live. |

**F-34.10-05 sub-check** — expand Categories (or Filters) and judge whether the panel's background
reads as a deliberate surface belonging to the tier-2 column, or still as a black block:

| Theme | Verdict | Notes (required if FAIL) |
|---|---|---|
| `gruvbox_dark` | PASS | Judged distinctly in this theme. Operator's verbatim words for the check: **"panel's background is now working"**, confirmed on follow-up as covering **"both themes plus cyberspace oasis for good measure"**. |
| `dracula` | PASS | Judged distinctly in this theme, same confirmation. |
| `cyberspace oasis` (NOT required by this contract — volunteered by the operator) | PASS | Recorded because it is free evidence, not because it was asked for. A third, unplanned theme passing strengthens the generalisation the two scored cells were chosen to test, since §9.1's whole concern was that the fix might hold only where `--background` and `--navbar-background` coincide. It does not count toward `items_passed`. |

**Item 3 verdict** (PASS only if all 7 functional-contract sub-checks (i-vii) PASS AND both
F-34.10-05 theme cells PASS): **PASS**

All seven functional sub-checks passed — including (vi) and (vii), **measured for the first time in
this phase** after being NOT ATTEMPTED in both prior runs — and both scored F-34.10-05 theme cells
passed, with a third unscored theme volunteered and also passing. **F-34.10-05 is closed by
measurement**, not by 34.10-20's fix having landed. Note the run-1 precedent this honours: a
rendering defect in these relocated controls is inside REQ-34.10-09's scope, not deferrable to
phase 34.11.

---

#### Item 4 (run 3) — Ambient Downloads ring, reduced scope (REQ-34.10-08)

**Run third**, with the queue confirmed empty at the START of item 1 (P7) — by the time item 4
runs, the queue is about to stop being empty on purpose. **Sub-check (i) (idle: visible, dimmed,
ring-shaped) is NOT independently scored here** — it is the same observation as item 1's
default-theme idle-ring cell (Step 1 above is not itself a themed cell, but item 1's per-theme
sweep's `midnightMirage` row covers it; see §9.1's own reasoning for why this avoids
double-counting). The sub-checks below are eligible to carry forward from run 2's own PASS
measurement (§9.1) since `DownloadsRing`'s own files are untouched by 34.10-18/-19/-20 — but each
still gets its own explicit verdict, not a silently-inherited one.

| # | Sub-check | Verdict | Notes (required if FAIL, NOT ATTEMPTED, or carried forward) |
|---|---|---|---|
| ii | Hover at idle: still reads as a ring, not a solid filled disc | PASS | Re-driven live rather than carried forward from run 2, per this contract's own convention. |
| iii | Active: the arc fills and advances as the download progresses | PASS | Arc observed filling and advancing against a real download started for this item (P5's installable game). |
| iv | Active: the count badge appears and is correct | PASS | |
| v | Clicking the ring reaches `/download-manager`, no tier-1 tab lit, ring shows active state | PASS | All three conjuncts of this sub-check (route reached, no tier-1 tab lit, ring in active state) confirmed. |
| vi | Discriminator: `ProgressHeader`'s own percent readout (`DownloadManager/index.tsx:111-115`) advances past 0% while the same download runs | PASS | Operator's initial reply listed `iv` twice; this recorder did NOT take the obvious typo at face value, because this is the one cell that discriminates a genuinely-tracking ring from the `--dl-progress` frozen-at-`0turn` failure that already shipped once in this phase behind a green suite. Re-asked explicitly and confirmed: **"YES"** — the percent readout advanced past 0% during the same live download. |

**Item 4 verdict** (PASS only if sub-checks ii-vi all PASS AND item 1's own `midnightMirage`
idle-ring cell — the cross-referenced stand-in for sub-check i — also PASSed; if item 1's per-theme
idle-ring sweep surfaces ANY FAIL across any theme, item 4 must be reopened in full at the next gap
cycle per §9.1's explicit condition, regardless of what ii-vi show here): **PASS**

Both conditions are satisfied and were checked, not assumed. Sub-checks ii-vi all PASSED, each
re-driven live rather than silently inherited from run 2. The cross-referenced stand-in for
sub-check (i) — item 1's `midnightMirage` idle-ring cell — PASSED. §9.1's reopening trigger ("if
item 1's per-theme idle-ring sweep surfaces ANY FAIL") did **not** fire: all three scored idle-ring
cells are PASS, and `dracula-classic`'s is NOT ATTEMPTED because the theme does not exist in the
picker — which is not a FAIL and does not trigger the reopen. **F-34.10-02 is closed by
measurement on both of its causes**: cause (a) via the idle-ring cells, cause (b) via sub-check
(vi)'s `ProgressHeader` discriminator advancing past 0%.

---

#### Item 2 (run 3) — Real window dragging under WKWebView, full re-run (REQ-34.10-04)

**Run fourth, deliberately after items 1/3/4** (§9.4 — toggling `framelessWindow` changes window
chrome and must not run before the visual/behavioral observations above are already recorded). With
`settings.framelessWindow` ON, drag the window by empty space in the tier-1 navbar and confirm it
moves. Then confirm dragging by a tab, by the Downloads ring, and inside the window-controls area
does NOT move it. Then toggle `framelessWindow` OFF and confirm the native titlebar still drags.

**Re-run in full this time (§9.1's own reasoning), not carried forward from run 1/run 2 despite the
source-level case for doing so.**

| Sub-check | Verdict | Notes (required if FAIL) |
|---|---|---|
| Frameless ON: drag by empty navbar space moves the window | PASS | The positive case — `.NavShell__navbar`'s drag-region fallback (`tauriWindowChrome.ts:331`) fires. |
| Frameless ON: drag by a tab does NOT move the window | PASS | Exclusion held despite 34.10-19 having changed `.MuiTab-root`'s `min-height` — confirming live what §9.1 argued at source level, that a MUI `<Tab>` stays a native `<button>` regardless of its CSS height and is caught by the interactive-element exclusion at `tauriWindowChrome.ts:322-329` BEFORE the navbar fallback. |
| Frameless ON: drag by the Downloads ring does NOT move the window | PASS | |
| Frameless ON: drag inside the window-controls area does NOT move the window | PASS | `.windowControls` exclusion held. |
| Frameless OFF: native titlebar still drags, nothing broken | PASS | P6's starting state restored for item 5. |

**Item 2 verdict** (PASS only if all 5 sub-checks PASS): **PASS**

Re-run in full rather than carried forward, and the decision is worth recording as vindicated in
process even though the outcome was a clean pass: §9.1 deliberately refused the source-level
shortcut ("`resolveDragRegion` is untouched, therefore skip") **because 34.10-18 had already
committed a fix to this exact element on exactly that reasoning and been wrong in 9 of 11 themes**.
The re-measurement cost the operator under a minute and converts four "should NOT move" exclusions
from inference into observation.

*Leave `framelessWindow` OFF when this item finishes, restoring Preflight P6's starting state.*

---

#### Item 5 (run 3) — Electron divergence, cheap re-confirmation (REQ-34.10-05)

**Run last.** Quit the Tauri dev build entirely and re-confirm `pgrep -f 'tauri dev'` is empty
(P3, re-applied) before launching Electron, so the two shells never run concurrently. Run
`npm start`.

Re-run per §9.1's own reasoning (cheap, not judged necessary, since only shared CSS — not any
Electron-specific code path — changed since run 2's PASS). The traffic-light observation from run 1
(NO overlap, D-03's "record, don't fix" framing) stands and is NOT re-taken.

| Sub-check | Verdict | Notes |
|---|---|---|
| Shell renders | PASS | Operator's verbatim words: **"pass renders, navigates"**. |
| All 4 tier-1 tabs navigate | PASS | Same observation. The shared CSS that DID change this cycle (`App.css`'s scroll-container relocation, `NavShell/index.scss`'s seam border) renders acceptably under Chromium as well as WKWebView. Per §9.1 the run-1 traffic-light observation (NO overlap, D-03 "record, don't fix") stands and was NOT re-taken. |

**P3 re-applied for this launch, with its ordering recorded honestly rather than implied.** §9.2's
P3 row requires the single-instance check to be re-run BEFORE the Electron launch. In practice the
operator launched and reported before this recorder ran it, so the check was executed
**immediately after** the report instead — the sequencing the contract asked for was not achieved,
and that is stated rather than glossed. What the check found nonetheless confirms the condition
held: `pgrep -f 'tauri dev'` EMPTY, `pgrep -f 'target/debug/gamelib'` EMPTY, with only
`electron-vite dev --watch` (PID 36420, started 22:30:53) and its Electron children alive. The two
shells therefore never ran concurrently and item 5's evidence is scorable. Note also that this
item's PASS criteria are purely visual — per §9.3's Test-3 row, no criterion in this entire
contract depends on a log sink — so the `[shell]`-sink-splitting hazard P3 exists to prevent could
not have corrupted this item's evidence even had a second instance been present.

**Item 5 verdict** (PASS only if both sub-checks PASS): **PASS**

---

### 9.6 Run 3 — what this run settled, and what it did not

**Outcome: 4/5. Items 2, 3, 4 and 5 PASS; item 1 FAILS. Phase 34.10 does NOT close** — the
standing rule is that it closes only on 5/5, and 4/5 has now failed to close this phase twice.

**Item 1's per-theme sweep was completed for the first time in this phase.** It had been NOT
ATTEMPTED in run 1 and again in run 2, and §9.5's own preamble warned that losing it a third time
was the specific risk this contract was written to prevent. All eight of its cells are now
resolved: three scored themes carry both a seam and an idle-ring verdict, and `dracula-classic` is
recorded NOT ATTEMPTED for the concrete reason that the theme does not exist in the shipped picker.
Two further first-ever measurements landed alongside it — item 3's sub-checks (vi) and (vii), also
NOT ATTEMPTED in both prior runs — and the full preflight P1-P9 was recorded, where run 2's block
was left entirely blank and its own recorder flagged that as a capture gap.

**What closed, by measurement rather than by a fix having landed.** F-34.10-05 is closed: the
disclosure panel's background reads correctly in `gruvbox_dark`, in `dracula`, and in a third theme
the operator volunteered unprompted — which matters more than it looks, because §9.1 excluded
`midnightMirage` from scoring precisely on the worry that the fix might hold only where
`--background` and `--navbar-background` coincide. F-34.10-06 is closed on both halves: the navbar
stayed pinned through a full scroll and the scrollbar sat below it throughout. That is the
strongest result in this run — 34.10-18's relocation of the scroll container off `document.body`
was the largest and riskiest change of the gap cycle, it fails silently by construction, and no
test on this project can observe it. Its mouse/keyboard consumer migration passed too. Its
gamepad consumer did not get measured at all.

**What did not close, stated as plainly as what did.** F-34.10-03 and F-34.10-04 both still FAIL,
against a bundle verified — before any scoring — to contain their fixes. 34.10-18's seam border and
34.10-19's tab `min-height` did not work. F-34.10-04's failure has changed shape, from run 2's full
wrap onto a second row to a vertical misalignment of the wordmark and ring; that is recorded as an
observation with no cause assigned, deliberately, because a cause guessed into a gate becomes the
next plan's falsified premise.

**Two things this run caught that a scored-verdict table would not show.** First, the preflight
found the running app was serving a **pre-fix bundle** — `frontendDist` points at a static
directory, so a bare `tauri dev` would have scored all four findings against code the app never
contained, and would have recorded four FAILs with confident-looking notes. It was caught before
any item was scored. Second, this contract's own automated checks proved **unsatisfiable in six
distinct ways** against a correctly-filled specimen; all six are recorded as F-34.10-07, none
altered a measured verdict, and the generalisable rule is that a gate check must be executed
against a filled example at authoring time rather than only reasoned about.

**Carried forward as unmeasured, not as passed:** the gamepad focus-scroll regression (no
controller available) and `dracula-classic` (theme absent from the picker).

**Operator design decision recorded during this run, superseding the fix direction for
F-34.10-03/-04.** The developer elected to abandon the card/folder tab in favour of a **pill tab
with rounded bottom corners** — in their own words, *"i going to drop the tab and let make a pill,
that what it looks like anyway! just needs rounded bottom corners."* This is recorded here as a
decision, not as a closure: it does **not** retire F-34.10-03 or F-34.10-04, whose FAIL verdicts
stand as measured against the contract as it was written. It does mean the next cycle's work is a
design change rather than a third attempt at erasing the seam — defensible on this run's own
evidence, since the card tab's entire premise is a merge with the content surface that has now
failed twice against two different fixes, whereas a pill has no seam to erase. Scoping that change
belongs to a later plan, not to this one.

## 10. Run 4 — scope, preflight, and re-derived review

Per decision D-E (the plan that authors a gate contract may never also run it), this section
**authors** run 4's contract and fills no verdict. Plan 34.10-26 writes `### 10.5 Run 4 — the
items` (the item bodies) plus a checker script; plan 34.10-27 is the only invocation permitted to
run the gate and fill its verdict slots. §0-§9 above are historical records and are byte-identical
above this line.

Two fixes landed since run 3, re-derived from their own artifacts rather than copied from §9:

- Commit `220211230` (`fix(34.10): close navbar seam -- unscoped .MuiTabs-root padding leak`,
  root-caused in `.planning/debug/navbar-seam-and-logo-offset.md` § Resolution) added
  `padding-bottom: 0` to the existing nested `&.MuiTabs-root { min-height: 0; }` block in
  `NavTabs/index.scss` (now lines 124-142, `padding-bottom: 0;` at line 140). This is the fifth fix
  attempt on F-34.10-03/-04 and the first confirmed live, by pixel measurement, to close the seam.
- Plan `34.10-23` (`34.10-23-SUMMARY.md`) rescoped `GamesSettings/index.scss`'s formerly-unscoped
  top-level `.MuiTabs-root { padding-bottom: var(--space-xs) }` (the confirmed root cause) under
  the file's existing `.settingsDialogContent, .settingsWrapper` ancestor pair (now lines 22-81,
  the rule itself at line 62), and added a repo-wide jest guard
  (`src/frontend/__tests__/muiTabsSelectorScoping.test.ts`, 5 tests, proven capable of failing via
  a working-tree falsification against the pre-fix file) that fails if any frontend stylesheet ever
  declares an unscoped top-level `MuiTab*`/`MuiTabs*` selector again.

**Confirmed by direct diff, not inferred:** `git log --oneline --stat 220211230^..HEAD -- src/frontend/App.css src/frontend/screens/Library/index.tsx src/frontend/components/UI/NavShell/index.tsx src/frontend/components/UI/NavShell/navTabs.ts src/preload/api/tauriWindowChrome.ts src/frontend/screens/Library/components/GamesList/index.tsx src/frontend/components/UI/NavShell/components/DownloadsRing` returns **empty** — none of items 2/3/4/5's own dependency files, nor item 1's scroll-regression files, changed since run 3 measured them. Only `NavTabs/index.scss`, `GamesSettings/index.scss`, and two test files (`muiTabsSelectorScoping.test.ts`, `appShellLayout.test.ts`) changed. Every scope decision below is derived from this fact, not asserted.

**A live gate has beaten a green test suite three times in this project, and this exact gap cycle
produced a fourth, closer-to-home instance (34.10-18's seam-border fix, reasoned theme-safe by
source inspection, wrong in 9 of 11 themes) plus a fifth (the F-34.10-03/-04 root cause survived
FOUR separate targeted fixes before the fifth one, `220211230`, actually worked).** Nothing below
infers a PASS from "the code looks unaffected"; every sub-check this section marks REACHABLE still
requires a live, human-driven observation in §10.5 (plan 34.10-26).

### 10.1 Scope, with per-item justification

| Item | Run-4 scope | Why |
|---|---|---|
| 1 | **FULL live re-run — the only item that can fail this phase closed.** Structured in two steps, mirroring §9.5's proven shape. **Step 1 (midnightMirage, the shipped default and a SCORED theme, before switching any theme):** F-34.10-03 (no visible band/gap between the tier-1 tab strip and the content region below the active tab -- `NavTabs/index.scss:140`'s `padding-bottom: 0` inside the nested `&.MuiTabs-root` block) and F-34.10-04 (wordmark, tab strip and Downloads ring read as ONE line -- same citation, `NavTabs/index.scss:124-142`). **These are the two sub-checks the fix has never been measured against in a scored theme** -- the debug session's own live confirmation (`navbar-seam-and-logo-offset.md` § Resolution, `verification: LIVE-CONFIRMED 2026-08-09`) was taken "against the theme the app happened to launch in (a teal scheme, navbar `#1c3d4a` / body `#091a21`)... NOT midnightMirage", and the operator's own verbal confirmation of F-34.10-04 ("the download and wordmark read level now") was given in that same unscored theme. A shipped fix confirmed once, outside the three themes this contract scores, is not a closed finding here. **Also re-run in Step 1, theme-independent so no value is added by interleaving them with the sweep:** F-34.10-06(a) (navbar stays pinned through a full scroll -- `App.css:56`'s `height: 100vh; overflow: hidden` plus `App.css:96`'s `.App .content { overflow-y: auto }`) and F-34.10-06(b) (scrollbar starts below the navbar's bottom edge -- same citation), plus the mouse/keyboard back-to-top regression (`Library/index.tsx:277-332`, threshold literal confirmed present at line 324 in the current source). **Decision, explicit, not silent:** these three PASSED in run 3 against files confirmed byte-unchanged since (see the `git log` check above) -- carrying them forward on that basis alone would be exactly the "source reasoning says this element is unaffected" shortcut that produced 34.10-18's own 9-of-11 miss. The distinguishing fact here is that run 3's PASS was itself a LIVE measurement, not a source inference, and nothing has touched the measured code since -- but per this contract's own repeatedly-proven lesson ("cheap re-measurement has repeatedly beaten confident source reasoning"), they are **RE-RUN, not carried forward**, since re-driving them costs the operator well under a minute while the app is already open for Step 1. **The gamepad focus-scroll regression sub-check** (`GamesList/index.tsx`'s `scrollCardIntoView`, lines 46-76, and its gating `useEffect` at lines 137-151, both confirmed byte-unchanged since run 3) is attempted in Step 1 if a controller is available (P9); if not, it is recorded NOT ATTEMPTED and is **not left as a perpetual pending cell** -- plan 34.10-27 must carry it forward into ROADMAP.md and REQUIREMENTS.md as a named, permanent residual risk, never inferred from the mouse/keyboard sub-check's PASS (different code path, container-rect-relative arithmetic). **Step 2 (the three-theme sweep, AFTER Step 1 completes):** across `midnightMirage`, `gruvbox_dark`, `dracula` -- both the seam column (same `NavTabs/index.scss:140` citation, now judged per-theme since the fix is a plain padding value with no token/colour dependency, but per this project's own dracula-inversion stress test it must still be looked at, not assumed) AND the idle Downloads-ring column (`DownloadsRing/index.tsx:72`'s `isIdle`, unchanged since run 3, folded in here rather than re-scored under item 4 -- same convention as run 3). |
| **`dracula-classic`** | **DROPPED from run 4's scope entirely.** Confirmed absent from the shipped theme picker: `src/frontend/components/UI/ThemeSelector/themeLabels.ts`'s `defaultThemes` object (lines 32-47) has a `dracula` case (line 38) and `resolveThemeLabel`'s switch (lines 55-90) has a `case 'dracula':` (line 70) -- `dracula-classic` does not exist anywhere in the file (no such key, no such case), confirmed by a full read, not a grep guess. Recorded as F-34.10-07, a **contract defect**, not an operator omission -- dracula-classic was dropped from scope for exactly this reason, the very Test-7 (UI-level reachability) miss the authoring reference added after F-34.4.2-17 and which had, until run 3, caught nothing. dracula-classic must never reappear (dropped, permanently) as a perpetual NOT ATTEMPTED spot-check in this contract or any successor. |
| 2 | **RE-RUN, all 5 sub-checks.** `resolveDragRegion` (`src/preload/api/tauriWindowChrome.ts:301-334`), its interactive-element exclusion (lines 326-330, checked before the `.NavShell__navbar` fallback at line 333), and `.NavShell__navbar`'s own class/markup are all confirmed byte-unchanged since run 3's own live measurement (the `git log` check above covers `tauriWindowChrome.ts` and `NavShell/index.tsx` explicitly; `NavShell/index.scss`'s only change since run 3 was `220211230`'s edit to a sibling file, `NavTabs/index.scss`, not to `NavShell/index.scss` itself -- confirmed separately, zero diff on that exact path too). Run 3 already performed the FULL LIVE re-run this contract's own precedent (34.10-18's 9-of-11 miss) demands, on this exact code, and all 5 sub-checks PASSED (`34.10-LIVE-GATE.md` §9.5, item 2 verdict PASS). Carrying that LIVE result forward would not be the "source reasoning stands in for measurement" failure mode 34.10-18 exhibited -- it would be citing an actual prior measurement against unchanged code. It is nonetheless **RE-RUN rather than carried forward**, because this contract's own established convention (run 3's own item 2/3/4 rows) is to keep re-driving cheap live checks rather than trust any carry-forward, and five quick drag/no-drag gestures cost the operator well under a minute. |
| 3 | **RE-RUN, all 7 sub-checks, plus the F-34.10-05 background sub-check.** `NavShell/index.tsx`'s `tier2Collapsed` (lines 50-53), `navTabs.ts`'s `resolveActiveTab` (line 37), and `Dropdown/index.scss`'s `background: transparent` (line 43) are all confirmed present at these exact lines and byte-unchanged since run 3's own live PASS of all 7 sub-checks (`34.10-LIVE-GATE.md` §9.5, item 3 verdict PASS; `34.10-23-SUMMARY.md` touched only `GamesSettings/index.scss` and `NavTabs/index.scss`, not `Dropdown/`). Same reasoning as item 2: re-run for the cost of a couple of minutes rather than carry forward, consistent with this contract's own established convention. F-34.10-05 stays judged in `gruvbox_dark` AND `dracula` only, per §9.1's own reasoning (`midnightMirage` coincides `--background`/`--navbar-background`, making a regression invisible there by construction) -- unchanged from run 3. |
| 4 | **REDUCED, same fold-in as run 3.** `DownloadsRing/index.tsx` and its `index.scss` are confirmed byte-unchanged since run 3 (covered by the `git log` check above). Sub-check (i) (idle: visible + dimmed + ring-shaped) stays folded into item 1's Step-2 per-theme sweep, not independently re-scored, to avoid double-counting the identical observation under two item numbers. Sub-checks (ii)-(vi) (hover-doesn't-degrade, active fill, count badge, click-through, the `ProgressHeader` discriminator at `DownloadManager/index.tsx:111`) are RE-RUN live rather than carried forward, per the same "cheap re-measurement over confident carry-forward" convention items 2/3 apply. **Explicit reopening condition, restated from run 3 and still binding:** if item 1's Step-2 per-theme idle-ring sweep surfaces ANY FAIL, item 4 must be reopened in full at the next gap cycle regardless of what its own re-driven sub-checks show. |
| 5 | **RE-RUN, both sub-checks -- judged cheap, not judged necessary, same D-03 framing as run 3.** Unlike run 3 (where `App.css` and `NavShell/index.scss` were the shared-CSS changes at stake), the file that changed THIS cycle -- `NavTabs/index.scss` (`220211230`'s `padding-bottom: 0` fix) -- is rendered by whichever shell mounts the shared `<NavShell />` tree, so it is exactly the same class of "shared CSS changed, Electron-specific code did not" situation run 3 already reasoned through. Electron is deprecated (Phase 35 deletes it entirely) and this item's D-03 framing ("record, don't fix") is not a blocking correctness gate on the same footing as items 1/3/4 -- re-measurement is not judged NECESSARY, but launching `npm start` and clicking 4 tabs costs under two minutes and removes any question about whether `220211230`'s CSS change renders oddly under Chromium. The run-1 traffic-light observation (NO overlap, D-03's "record, don't fix") stands and is not re-taken. |
| Non-scoring OBSERVATION -- 34.10-23's rescope collateral | Not scored, does not count toward `items_passed`. `34.10-23-SUMMARY.md` names five `<Tabs>` render sites that used to receive the leaked 8px `padding-bottom` by accident and no longer do: WineManager, the Game details page, Steam login, the GOG modify-install modal, and (protected by its own override, unaffected either way) the nav shell itself. Ask the operator to look at the **Settings -> Games tab strip**, the **Download Manager tab strip**, and the **Wine Manager tab strip** (both of which declare their own ancestor-scoped `.MuiTabs-root` rules -- `DownloadManager/index.css:7-22`, `WineManager/index.css:65-73` -- and so were structurally shielded from the leak in the first place, but are worth a glance given how much thrash this defect class caused) and record whether anything reads as broken. This row is recorded separately, explicitly, so a spot-check finding can never silently inflate or deflate `items_passed`. |

### 10.2 Preflight for run 4

Baseline derived at THIS plan's own authoring time (2026-08-09), by actually running the commands
below against the current tree -- not copied from any prior run's numbers, per this section's own
governing rule (F-34.10-07's generalisable lesson: run every check against a filled specimen).

| # | Check | Command / action | Why this item's premise depends on it |
|---|---|---|---|
| P1 | Build matches the committed tree | `pnpm jest --runInBand --silent --forceExit` (this phase's documented `test:ci` hang workaround -- **not** bare `pnpm test:ci`) | A gate run against a stale build proves nothing. **Live run at this plan's authoring time (2026-08-09): 220 suites / 4274 tests, 0 failing.** This matches `34.10-23-SUMMARY.md`'s own claimed post-fix count exactly (219->220 suites, 4269->4274 tests, +5 for the new guard). Note for the runner: §1's "Known external condition" (the `hardcodedStringGate` failure from a concurrent session's uncommitted `WebView/index.tsx` change) did **NOT** reproduce at this authoring run -- `src/frontend/screens/WebView/index.tsx` around its previously-flagged line shows no uncommitted changes (`git status --short` confirmed clean) and the flagged string is absent from that location now. Expected baseline for run 4 is therefore **220 suites / 4274 tests, 0 failing** -- if a failure appears at run time, name it explicitly (it may be that concurrent session resuming) rather than assuming it is either this retired condition or a new regression by default. |
| P2 | `pnpm codecheck` clean | `pnpm codecheck` | Confirmed exit 0, no diagnostics, at this plan's own authoring time (2026-08-09). Confirms no un-typechecked drift entered the tree since `34.10-23`. |
| P3 | Exactly one `gamelib`/Tauri dev process before the FIRST launch | `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'` (or the platform-appropriate process name) both empty | This project's own recorded capture-integrity trap (F-34.4.2-15/-18, and it fired a third time in THIS phase per run 3's own P3 record) -- a concurrent second instance does not announce itself, it splits the `[shell]` sink while `gamelib.log` stays shared. If either command returns a PID, kill it or identify whose it is before launching, and re-check before the Electron launch (item 5) too. |
| P4 | Library has a game with a distinctive, unique-substring title AND a game whose page can be opened | Check the Games tab once the app is up | Item 3's search sub-check needs a title to type; sub-checks (vi)/(vii) need an openable game page. If the library is empty, record the affected item-3 sub-checks NOT ATTEMPTED with this as the reason -- never inferred. |
| P5 | At least one game available to start a download | Confirmed live during item 4 | Item 4's re-driven active sub-checks (ii)-(vi) need something installable. |
| P6 | `settings.framelessWindow` is OFF (default) at the start | Settings -> General -> "Frameless window" toggle unchecked | Items 1, 3, 4 must be observed in the default decorated state -- see §10.4's pairwise review, which is why item 2's toggle still runs after items 1/3/4, not before. |
| P7 | **The download queue is EMPTY at the start** | Check `/download-manager` and the navbar ring before starting anything | **Kept -- its dependent sub-checks are in run 4's scope.** Item 1's Step-2 per-theme idle-ring column and item 4's folded-in idle sub-check both need an idle ring at the start; starting item 4's download destroys this for the rest of the session (§10.4's DESTROYS row). If the queue is not empty at the start, record the affected sub-checks NOT ATTEMPTED with this named as the reason. |
| P8 | The Games grid is long enough to produce an active scrollbar and to scroll past the 450px back-to-top threshold (`Library/index.tsx:324`), with no category or filter left applied from a previous session | Check the Games tab's card count / scroll behaviour before starting | **Kept -- its dependent sub-checks are in run 4's scope.** §10.1 decided to RE-RUN, not carry forward, F-34.10-06(b) and the mouse/keyboard back-to-top regression in Step 1, so this precondition is load-bearing again this run, exactly as it was in run 3. If the grid does not scroll, those sub-checks are recorded NOT ATTEMPTED with this named as the reason. §10.4 also ties this row to item 3's own filtering action, which is why item 1 (which needs P8 intact) still runs before item 3 (which can violate it). |
| P9 | Whether a game controller is connected and recognised by the app (`activeController` in `ContextProvider`, gating `GamesList/index.tsx:137-151`'s focus listener) | Check the app's controller-detection state once the app is up | Item 1's gamepad focus-scroll sub-check is unmeasurable without it. **Record the literal answer. Terminal disposition, stated up front and binding on the runner (34.10-27), not deferred again:** if no controller is available, the sub-check is recorded NOT ATTEMPTED, and 34.10-27 must carry it forward as a **named, permanent residual risk in ROADMAP.md and REQUIREMENTS.md** -- not left as a perpetual pending cell in this file alone, and never inferred from the mouse/keyboard sub-check passing (different code path, different container-rect-relative arithmetic). |
| P10 | **NEW for run 4.** Launch command is `pnpm tauri:dev`, never bare `tauri dev` | Confirm the launch command used, in the transcript, before any scored observation | `src-tauri/tauri.conf.json` sets `frontendDist: "../build"` -- a static directory, not a dev server -- so a bare `tauri dev` serves whatever `electron-vite build` last wrote. Run 3's own P3 record (§9.2) caught a pre-fix bundle THIS EXACT WAY, moments before it would have scored all four F-34.10-0x findings against code the running app never contained. `pnpm tauri:dev` (= `electron-vite build && pnpm build:sidecar && tauri dev`) is mandatory. |
| P11 | **NEW for run 4.** Bundle-contains-the-fix preflight | After launching, run: `grep -o '\.NavTabs\.MuiTabs-root{padding-bottom:0;min-height:0}' build/assets/*.css` (expect exactly one match) AND `grep -c '^\.MuiTabs-root{padding-bottom' build/assets/*.css` (expect `0` -- the bare, unscoped form must no longer exist anywhere in the compiled CSS) AND confirm the compiled `GamesSettings` rule now carries an ancestor prefix (`.settingsDialogContent` or `.settingsWrapper`-scoped), not a bare `.MuiTabs-root{padding-bottom` form | The direct analogue of how run 3 verified `34.10-18`/`-19` were actually served. **No scored observation may be taken before this row passes.** |
| P12 | **NEW for run 4.** `GAMELIB_DEV_SECRET_VAULT=1` is set for the run | Confirm the env var before launch | Without it, an unanswered Keychain prompt makes `keyring_get` time out after 45s and takes the whole app down mid-run (`navbar-seam-and-logo-offset.md` § session_constraints_learned). |
| P13 | **NEW for run 4.** Exactly one app instance, re-asserted | `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'` both empty immediately before the FIRST launch, re-checked before the Electron launch (item 5) | This trap has fired at least three times in this phase (run 3's own P3 record, plus two earlier occurrences it cites). Restated as its own row because §10.4's pairwise review treats it as load-bearing for every item in this run, not only item 5. |
| P14 | **NEW for run 4.** Screenshot hygiene, if any capture is taken | Wrap every `screencapture` invocation in `caffeinate -d -i -u -t 60` and size-check the result (>400KB) before trusting it; never reuse hard-coded capture coordinates across restarts -- re-locate the navbar each time | `screencapture -R` fails with "could not create image from rect" when the display sleeps, and a full-screen capture silently returns an all-black ~107KB PNG instead of erroring (`navbar-seam-and-logo-offset.md` § session_constraints_learned). The window moves and the active route/theme changes between launches. |
| P15 | **NEW for run 4.** No step may assume interactive Web Inspector console access | State this as a contract-level constraint, not a footnote | Confirmed dead on this build this session (`navbar-seam-and-logo-offset.md`): input rows echo, `1+1` returns nothing, Enter is inert, Cmd+Enter only echoes, while the app's own logging still reaches the console (evaluation is dead, attachment is not). If any step needs a computed value rather than a direct visual judgement, the working channel is a `window.api.logInfo` probe dumping to `gamelib.log`, or a `screencapture` + pixel-decode harness (`scratchpad/pngscan.py`, already used and proven in the debug session) -- never a console-script checkpoint. |

**Preflight Results (left empty by this plan -- filling it would make 34.10-25 both author and
runner, violating decision D-E; plan 34.10-26 owns the checker script, plan 34.10-27 fills these
results at run time):**

```
[Intentionally left blank. Do not write a pending-placeholder string here -- see F-34.10-07's own
lesson about a check that greps for the literal placeholder text and is tripped by prose
describing it. This block is unfilled by design, stated in prose rather than by any sentinel a
later automated check might match on.]
```

### 10.3 Structural reachability review for run 4

Re-derived against the CURRENT source tree, not copied from §9.3 -- every citation below was
confirmed by reading the file at this plan's own authoring time (2026-08-09), via
`sed -n '<line>p' <file>`, since §9.3's own line numbers predate `220211230` and `34.10-23`.
Applying all seven defect-class tests (`live-gate-contract-authoring.md`).

| Item / sub-check / precondition | Surface | What it asks for | Verdict | Evidence |
|---|---|---|---|---|
| Item 1, Step 1: F-34.10-03 (seam) | `NavTabs/index.scss:140` (`padding-bottom: 0;` inside the nested `&.MuiTabs-root` block opened at line 124) | Confirm no visible band/gap between the tab strip and content, in `midnightMirage` | REACHABLE | Confirmed present at the cited line by direct read; this is the fix's own location (`220211230`), not an inference from a commit hash. |
| Item 1, Step 1: F-34.10-04 (one line) | Same block, `NavTabs/index.scss:124-142` | Confirm wordmark/strip/ring share one line, in `midnightMirage` | REACHABLE | Same citation confirmed present; the debug session's own live pixel measurement (`navbar-seam-and-logo-offset.md` § Resolution) already exercised this exact code, just in an unscored theme -- the surface itself is unchanged and reachable. |
| Item 1, Step 1: F-34.10-06(a) (navbar pinned) | `App.css:56` (`height: 100vh; overflow: hidden`), `App.css:96` (`.App .content { overflow-y: auto }`) | Scroll a long route to its end and back, confirm the navbar never moves | REACHABLE, CONDITIONAL on P8 | Both lines confirmed present verbatim by direct read; file confirmed byte-unchanged since run 3 by the `git log` check in this section's preamble. Kept REACHABLE, not asserted PASS, per this contract's own standing caution against source-level claims standing in for live measurement. |
| Item 1, Step 1: F-34.10-06(b) (scrollbar below navbar) | `App.tsx:103` (`<main className="content">`) | With a scrollbar active, confirm it starts below the navbar's bottom edge | REACHABLE, CONDITIONAL on P8 | **Test 4 (absence-observability) case, restated from §9.3.** The PRESENCE signal: if `.App .content` were not the true scroll owner, the OS/webview scrollbar would draw against the full window height, overlapping the navbar's own row -- a concrete, observably-different wrong picture, not "a gap" in the abstract. Confirmed present at the cited line. |
| Item 1, Step 1: mouse/keyboard back-to-top regression | `Library/index.tsx:277` (`backToTopElement`), `:291`/`:316` (`main.content` query with `document.body` fallback), `:324` (`450` threshold literal), `:332` (`backToTop` handler) | Scroll past 450px, click back-to-top, leave the tab and return, confirm scroll behaviour holds | REACHABLE, CONDITIONAL on P8 | Every cited line confirmed present verbatim by direct read; file confirmed byte-unchanged since run 3. |
| Item 1, Step 1: gamepad focus-scroll regression | `GamesList/index.tsx:46` (`scrollCardIntoView` definition, through line 76), `:137-151` (`useEffect(..., [listRef.current, activeController])`) | Navigate the grid with a controller far enough that focus moves past visible rows, confirm auto-scroll keeps the focused card in view, both directions | **REACHABLE, CONDITIONAL on P9** (and P8 -- a grid too short to scroll makes the scenario vacuous regardless of P9) | Confirmed present at the cited lines, byte-unchanged since run 3. **Test 4 case:** the PRESENCE signal is the grid visibly auto-scrolling AND the focused card ending up inside `container.getBoundingClientRect()`'s bounds -- "nothing moved" cannot be misread as a pass. |
| Item 1, Step 2: all 3 themes, tab-merge seam | Settings -> Accessibility theme picker (unmodified by `220211230` or `34.10-23`) | Switch theme, click 4 tabs, judge seam by eye | REACHABLE | Unchanged from run 1/2/3's own Test-1/Test-2-N/A finding; no fix plan this cycle touched the theme picker. |
| Item 1, Step 2: all 3 themes, idle Downloads ring | `DownloadsRing/index.tsx:72` (`const isIdle = elements.length === 0`) | Judge whether the idle ring is visible, dimmed, ring-shaped, in each theme | REACHABLE, CONDITIONAL on P7 | Confirmed present at the cited line; file confirmed byte-unchanged since run 3. |
| Item 1: `dracula-classic` spot-check | Same theme picker | N/A | **UNREACHABLE, and dropped from scope for exactly that reason (§10.1)** | `themeLabels.ts`'s `defaultThemes` (lines 32-47) and `resolveThemeLabel`'s switch (lines 55-90), both read in full, contain no `dracula-classic` key or case. This is the Test-7 (UI-level reachability) miss F-34.10-07 already named; recording it here again closes the loop rather than silently omitting the row. |
| Item 2, all 5 sub-checks | `src/preload/api/tauriWindowChrome.ts:301-334` (`resolveDragRegion`), exclusion at `:326-330` (`start.closest('a, button, [role="button"], input, select, textarea, .windowControls')`), fallback at `:333` (`start.closest('.NavShell__navbar') !== null`) | Toggle frameless, drag by 4 surfaces + confirm native titlebar drag OFF | REACHABLE | All three citations confirmed present verbatim by direct read (line numbers shifted by ~2-3 from §9.3's `322-329`/`331`, re-confirmed rather than trusted). File confirmed byte-unchanged since run 3's own live PASS. **Test 4 case, restated:** the exclusion is a real branch that would fire and move the window if ever removed or mis-ordered -- the presence case is the window moving. |
| Item 3(vi): Games tab stays lit on a game page | `src/frontend/components/UI/NavShell/navTabs.ts:37` (`resolveActiveTab`) | Confirm the scenario is producible | REACHABLE | Confirmed present at the cited line, function signature unchanged; file confirmed byte-unchanged since run 3. |
| Item 3(vii): tier-2 column collapses, no empty 204px band | `NavShell/index.tsx:50-53` (`tier2Collapsed`) | Confirm the condition is reachable from a game page | REACHABLE | Confirmed present at the cited lines; file confirmed byte-unchanged since run 3. |
| Item 3: F-34.10-05 background sub-check, `gruvbox_dark` + `dracula` | `src/frontend/components/UI/Dropdown/index.scss:43` (`background: transparent`) | Expand a disclosure panel, judge its surface against the tier-2 column's own backdrop | REACHABLE | Confirmed present at the cited line; file confirmed byte-unchanged since run 3. `midnightMirage` deliberately excluded from scoring, unchanged reasoning from §9.1/§9.3. |
| Item 4, sub-checks (ii)-(vi) | `DownloadsRing/index.tsx`, `ProgressHeader` (`src/frontend/screens/DownloadManager/index.tsx:111`, `<ProgressHeader`) | Confirm active-fill, badge, click-through, discriminator | REACHABLE | `DownloadsRing/index.tsx` and `.scss` confirmed byte-unchanged since run 3 (covered by this section's preamble `git log` check); `DownloadManager/index.tsx:111` confirmed present by direct read. |
| Item 5: shell renders + 4 tabs navigate | `npm start` (`electron-vite dev --watch`), shared `NavShell`/`.App` tree | Launch Electron, click 4 tabs | REACHABLE | Confirmed unchanged: no Electron-only file appears in the `git log` diff since run 3 (this section's preamble). `NavTabs/index.scss` (the one file that DID change, `220211230`) is shared CSS rendered by whichever shell mounts the same React tree -- no Electron-specific code path is newly at risk, same reasoning run 3 already applied to its own shared-CSS changes. |
| Non-scoring observation: 34.10-23 rescope collateral | `DownloadManager/index.css:7-22` (`.downloadManager > .MuiTabs-root`), `WineManager/index.css:65-73` (`.MuiTabs-root` nested under an ancestor) | Confirm both surfaces are structurally ancestor-scoped, not bare selectors that the pre-fix leak could have doubled up on | REACHABLE | Both citations confirmed present and ancestor-scoped by direct read -- neither was structurally exposed to the leak's specificity tie the same way the nav shell was, but the operator is still asked to look, per §10.1's own row. |

**Test 7 (UI-level reachability) applied explicitly, since it is the test that missed
`dracula-classic` once already this phase:** for the theme picker specifically, `themeLabels.ts`'s
`defaultThemes` (lines 32-47) really offers `midnightMirage` (line 33), `gruvbox_dark` (line 45),
and `dracula` (line 38) -- all three of run 4's scored themes are confirmed present as real,
selectable options, not merely referenced in comments or tests. This is the positive half of the
same check that produced the `dracula-classic` negative result above.

**Test 4 (absence-observability) applied to every "no seam" / "navbar stays pinned" / "scrollbar
doesn't overdraw" sub-check, restated with a concrete magnitude so "I saw nothing" cannot be
misread as a pass:** the seam sub-check's presence signal, per the debug session's own live pixel
measurement (`navbar-seam-and-logo-offset.md` § Resolution, "BEFORE" reading), is a strip of navbar-
coloured background measured at **8px** (also recorded there as "9.0px" and "9px" across earlier,
now-superseded measurement attempts before the true 8px-padding cause was isolated -- the fix
target and the root-cause arithmetic both name **8px**, `--space-xs` = `.5em` = 8px, as the
authoritative figure) between the selected tab's bottom edge and the navbar/content boundary. A
regression that reopened the leak (e.g. a future stylesheet re-adding an unscoped `.MuiTabs-root`
rule) would reproduce a band of approximately this magnitude, not merely "a gap" of unspecified
size -- naming 8px gives the runner a concrete expected shape for the presence case, not just its
absence.

### 10.4 Pairwise review for run 4 (Test 5)

**Reduction applied, restated explicitly per Test 5's own scoping rule:** every {state-mutating
action} against every {evidence-bearing item or sub-check} it is plausibly relevant to, not the
full cross product. State-mutating actions in run 4: (a) theme switching (item 1's Step-2 sweep),
(b) expanding a tier-2 disclosure panel or applying a category/filter (item 3), (c) starting a
download (item 4's re-driven active sub-checks), (d) toggling `framelessWindow` (item 2), (e)
quitting Tauri to launch Electron (item 5), (f) the mouse/keyboard back-to-top scroll action (item
1, Step 1), (g) the gamepad-driven scroll action (item 1, Step 1, conditional on P9), (h) running
`pnpm jest`/`pnpm codecheck` as preflight processes (P1/P2). Evidence-bearing items/sub-checks:
items 1-5's own verdicts, item 1's internal Step-1 finding/regression sub-check evidence, and item
3's internal (vi)/(vii) evidence.

| State-mutating action | Against evidence-bearing item/sub-check | Destroys it? | Reasoning |
|---|---|---|---|
| (a) Theme switching, item 1's Step-2 sweep | Item 1's own Step-1 finding/regression sub-checks (F-34.10-03/-04/-06a/-06b, back-to-top, gamepad) | **YES, if interleaved** | Carried forward from run 3's own equivalent pair, restated for the Step 1/Step 2 split this run introduces explicitly: these sub-checks are theme-independent by construction (plain CSS declarations, no colour token), so interleaving the sweep with them adds no evidence value and risks a theme change landing mid-observation. **Resolved by sequencing:** Step 1 runs to completion, in `midnightMirage`, before Step 2's sweep begins -- stated as the item's own step order in §10.1, not left to operator discretion. |
| (c) Starting a download (item 4) | Item 1's Step-2 idle-ring column AND item 4's own folded-in idle sub-check | **YES — DESTROYS** | Once a download starts, the idle state is gone for the rest of the session -- there is no "un-start" without waiting it out or cancelling, which changes queue-history state a re-run should not silently rely on. Resolved by run order (item 1 before item 4) plus P7. |
| (b) Expanding a tier-2 disclosure panel, or applying a category/filter (item 3) | Item 1's P8-dependent Step-1 sub-checks: F-34.10-06(b) and the mouse/keyboard back-to-top regression | **YES, if item 3 runs before item 1's Step 1 is recorded** | Applying a category or filter SHORTENS the Games grid -- exactly the precondition (P8) item 1's scroll sub-checks depend on. §10.1 re-scoped these sub-checks back INTO item 1 (re-run, not carried forward), which reopens this exact pair from run 3. **Resolved by run order: item 1 (all of it, including Step 1) runs before item 3.** |
| (b) Expanding a tier-2 disclosure panel (item 3) | Item 3's own (vi)/(vii) | **YES, if left open** | Unchanged from run 3: an expanded panel changes `.NavShell__tier2Portal`'s scroll/height state before the game-page collapse sub-check is observed. Resolved by requiring both disclosures explicitly closed before (vi)/(vii) run. |
| (d) Toggling `framelessWindow` (item 2) | Items 1, 3, 4 | **YES, if run before them** | Unchanged from runs 1-3: toggling frameless mid-run changes window chrome (`setDecorations()`), which could visually alter the navbar's rendered box model while items 1/3/4 are being judged by eye. Resolved by run order -- item 2 runs after items 1, 3, 4. |
| (f) Mouse/keyboard back-to-top action | (g) Gamepad focus-scroll action | No, but noted | Different code paths, different arithmetic (§10.1's item-1 row). The mouse/keyboard action may leave a non-zero scroll position persisted via `Library/index.tsx`'s session-storage restore effect (around line 298), which becomes the gamepad sub-check's own starting point; this does not destroy either sub-check's evidence, but the runner should record the starting scroll state if it seems to matter to what "moved" means for the gamepad check. |
| (h) Running `pnpm jest`/`pnpm codecheck` (P1/P2) | The single Tauri dev instance (P13) | No | Both run as separate `node`/`tsc` CI processes against the source tree; neither restarts, touches, or otherwise interferes with a running `tauri dev`/`gamelib` process. Explicitly considered because "anything that RESTARTS a process" is on the Test-5 hunting list -- checked and found NOT to apply. |
| (d) `framelessWindow` toggle (item 2) | Item 5 (Electron) | No | Electron reads its own decoration config independent of Tauri's `settings.framelessWindow`, and item 2's own closing step restores the toggle OFF before item 5 runs. |
| (c) Download start (item 4) | Item 2 (dragging) | No | A queued/active download does not change window chrome or drag-region CSS. |
| (c) Download start (item 4) | Item 5 (Electron) | No | Separate process, no bearing. |
| (e) Quit Tauri / launch Electron (item 5) | Items 1-4's recorded verdicts | No | Verdicts are written into this file as each is measured (§10.5's rule, per plan 34.10-26), not held only in the running app's memory -- quitting Tauri cannot erase a markdown file. |

**Pairs considered: 11** (8 distinct state-mutating actions x the evidence-bearing items/sub-checks
each is plausibly relevant to, reduced by irrelevance -- e.g. theme switching was not paired
against item 5, which is visually theme-independent by design per D-03, and (h) was only paired
against the single-instance precondition since P1/P2 have no plausible bearing on any visual item).
**Pairs flagged: 5** -- (a) against item 1's own Step-1 sub-checks (restated and re-scoped for this
run's Step 1/Step 2 split), (c) against item 1's idle evidence and item 4's own fold-in, (b)
against item 1's re-scoped-in P8-dependent sub-checks (reopened this run by §10.1's own decision to
re-run rather than carry forward), (b) against item 3's own (vi)/(vii), and (d) against items
1/3/4 -- all five resolved by run order or explicit within-item sequencing stated in §10.1, none
left open.

### Test 3 (log-line emitter reachability) — decided explicitly, not left ambiguous

**N/A for every scored PASS/FAIL criterion in run 4.** Every criterion above is a direct
visual/interactive observation -- including Step 1's four finding/regression sub-checks and Step
2's per-theme sweep -- none depends on a terminal transcript, `gamelib.log`, or any sidecar/Rust
log line for its verdict. The dual-sink evidence-capture standard (`live-gate-contract-authoring.md`
§ Section 3) therefore does NOT apply to this run's scored verdicts.

**Named exception, stated so it cannot be misread as a silent gap:** P11's bundle-content check
(`grep` against `build/assets/*.css`) is a static build-artifact assertion, not a log-line
requirement -- it does not touch `gamelib.log` or the terminal transcript and so does not invoke
the dual-sink standard either. Separately, if the runner (34.10-27) finds any Step-1 sub-check's
verdict genuinely ambiguous by eye (mirroring the debug session's own escalation from screenshots
to a `window.api.logInfo` probe when pixel measurement alone was insufficient), that probe's
evidence WOULD be a sidecar-emitted log line reaching `gamelib.log`, and Section 3's dual-sink
standard (session-scoped capture, per-launch archiving before the log rotates to `.old`) applies to
it at that point. This is decided here rather than left for the runner to discover mid-run.

### Run order, derived (not asserted)

From the DESTROYS rows above: item 1 (all of it, Step 1 then Step 2) must precede item 4
(idle-ring DESTROYS pair, and item 4's own folded-in idle sub-check); item 1 must also precede item
3 (the P8-dependent DESTROYS pair, reopened this run because §10.1 re-scoped item 1's scroll
sub-checks back into a live re-run rather than carrying them forward); items 1, 3 and 4 must all
precede item 2 (chrome-mutation DESTROYS pair); item 5 must run last (separate-process pair,
practically requires Tauri fully quit first, per P13 re-checked before that launch). Within item 1
itself, Step 1 (the theme-independent finding and regression sub-checks, plus the gamepad sub-check
if P9 permits) must complete before Step 2's three-theme sweep begins (interleaving DESTROYS pair).
Within item 3, both disclosures must be closed before sub-checks (vi)/(vii) run.

**Applying these constraints in order produces exactly `[1, 3, 4, 2, 5]` — this COINCIDES with
runs 1, 2 and 3's own derived order, it is not reused verbatim.** The reasoning that produces it
this run is not identical to run 3's: run 3 placed item 1 before item 3 because of item 3's own
in-progress-download UI clutter concern, whereas run 4 ALSO now has the P8 DESTROYS pair (item 1's
re-scoped-in scroll sub-checks) independently requiring the same ordering. Both reasons converge on
the same order. No change to the frontmatter `run_order` field is needed; it already reads
`[1, 3, 4, 2, 5]`.

### 10.5 Run 4 — the items

**Restated so the operator does not have to scroll (unchanged rule, restated per §5):** the phase
closes only on 5/5 — `verdict: pass` may be set only if `items_passed == 5`. **NOT ATTEMPTED is
never inferred** — an unreached sub-check is recorded `NOT ATTEMPTED` with its reason, never
inferred as a PASS from a green test suite, a prior run's source-level reasoning, or silence, and
never inferred as a FAIL from silence either. **Every verdict is written into this file as it is
measured, not batched at the end.** Use exactly `PASS`, `FAIL` or `NOT ATTEMPTED` as the only
permitted final values in every slot below. Markdown emphasis (`**PASS**`) around a filled verdict
is permitted — run 3's own checker broke on exactly that, and this run's checker (Task 2,
`34.10-run4-contract-check.mjs`) is built not to.

**Verdict slot format — a contract with the checker, stated here as the authoring plan wrote it.**
Every verdict lives either in a markdown table cell under a header whose text contains `erdict`, or
in a per-item verdict line of the exact form `**Item N verdict** (<PASS condition spelled out>):
<VERDICT>`, where the checker reads everything after the FINAL colon on that line as the verdict
value. At authoring time, every verdict cell and every per-item verdict line below is left EMPTY —
whitespace only after the final colon, or an empty table cell. No placeholder token is written
anywhere in this section: F-34.10-07's own lesson is that a literal placeholder string gets tripped
by prose that merely describes it, so emptiness here is structural (an actually-blank cell/line),
never a sentinel word a later grep could confuse with real prose.

**Binding environment facts for every item below, restated at point of use rather than left only in
§10.2's preflight table:** launch with `pnpm tauri:dev`, never bare `tauri dev` (P10/P11); set
`GAMELIB_DEV_SECRET_VAULT=1` before launch (P12); confirm exactly one `gamelib`/`tauri dev` process
before the first launch and again before the Electron launch (P3/P13); and do not assume the Web
Inspector console evaluates on this build (P15) — if a step needs a computed value rather than a
direct visual judgement, use a `window.api.logInfo` probe to `gamelib.log` or the proven
`scratchpad/pngscan.py` screenshot pixel-decode harness, never a console-script checkpoint.

Run order (from §10.4's derivation): **1, 3, 4, 2, 5.**

---

#### Item 1 (run 4) — Theme survival: seam/single-line findings re-measured in scored themes, plus scroll-regression re-run (REQ-34.10-06)

**Run first, and the only item that can still fail this phase closed.** Confirm P7 (empty queue),
P8 (scrollable grid), P9 (controller presence) and P11 (bundle contains `220211230`'s fix) before
scoring anything. Launch `pnpm tauri:dev` with `GAMELIB_DEV_SECRET_VAULT=1` set — this item is
Tauri/WKWebView-only; `pnpm start` (Electron) cannot exercise it.

**Step 1 — in the default theme (`midnightMirage`), before switching any theme** (per §10.4's own
derived sequencing, so a theme change never interleaves with these):

1. Judge F-34.10-03: on the Games tab, look for any visible band or gap between the tier-1 tab
   strip and the content region below the active tab. **Concrete AFTER state to look for** (per
   `navbar-seam-and-logo-offset.md` § Resolution's own live pixel measurement): content colour
   continuous from the selected tab's top through the content surface, with NO transition back to
   navbar colour anywhere in that column, while the empty-navbar column (behind an unselected tab)
   still transitions at the navbar's own bottom edge as normal. The BEFORE state this replaces was
   an ~8px band of navbar-coloured background between the selected tab's bottom edge and the
   content boundary — that band reappearing, at roughly that magnitude, is the presence signal for
   a regression.
2. Judge F-34.10-04: confirm the wordmark, the tab strip and the Downloads ring all read as sharing
   ONE visual line in the navbar, not two rows and not a vertical misalignment of one against the
   other two (run 3's own observed shape).
3. Judge F-34.10-06(a): on a route with enough content to scroll (the Games grid, per P8), scroll
   to the very end and back to the top; confirm the navbar never moves and stays fixed at the top
   of the window throughout.
4. Judge F-34.10-06(b): with the scrollbar active during that same scroll, confirm it starts BELOW
   the navbar's bottom edge at every scroll position and never draws over the navbar.
5. Mouse/keyboard regression: on the Games grid, scroll down past the point where the "back to top"
   button appears (450px, `Library/index.tsx:324`); click it; leave the Games tab (e.g. to
   Settings) and return; confirm the grid's scroll position behaves sensibly (does not silently
   fail to restore, does not throw, does not leave the button permanently stuck visible/hidden).
6. Gamepad regression (P9-conditional): with a controller connected and recognised, navigate the
   Games grid with the D-pad/stick far enough that focus moves past the visible rows; confirm the
   grid auto-scrolls to keep the focused card in view, both downward and upward. **If no controller
   is available, record this sub-check `NOT ATTEMPTED` with "no controller available" as the
   reason** — this does not by itself fail item 1, but per P9's own terminal disposition must be
   carried forward by 34.10-27 into ROADMAP.md and REQUIREMENTS.md as a named, permanent residual
   risk, never inferred from sub-check 5's own result (different code path, container-rect-relative
   arithmetic).

| Sub-check | Verdict | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|
| F-34.10-03 — no visible seam/gap, in `midnightMirage` |  |  |
| F-34.10-04 — wordmark/strip/ring share one line, in `midnightMirage` |  |  |
| F-34.10-06(a) — navbar stays fixed while scrolling |  |  |
| F-34.10-06(b) — scrollbar starts below the navbar, never over it |  |  |
| Mouse/keyboard back-to-top regression |  |  |
| Gamepad focus-scroll regression (P9-conditional; escape hatch applies) |  |  |

**Step 2 — the three-theme sweep**, only after Step 1's six sub-checks are all recorded.
`dracula-classic` is DROPPED from this run's scope entirely (F-34.10-07 — confirmed absent from the
shipped theme picker; do not add it back as a perpetual NOT ATTEMPTED spot-check). For EACH of the
three themes in turn (`midnightMirage`, `gruvbox_dark`, `dracula`): switch the theme in Settings ->
Accessibility, navigate to the Games route, click all four tier-1 tabs, and record BOTH the seam
cell AND the idle-ring cell for that theme before moving to the next one. **A general, unscoped
report ("looks fine across themes") is NOT a per-theme measurement and must be recorded `NOT
ATTEMPTED`** for whichever cells it does not distinctly address — this exact failure mode cost this
sweep three runs running (run 1 and run 2 NOT ATTEMPTED, run 3's own idle-ring column resting on
only two combined operator statements rather than three independently elicited ones); do not let it
happen a fourth time. Ask for each theme's seam judgement and each theme's idle-ring judgement
(visible, dimmed, ring-shaped — not a solid disc) as separate, distinctly elicited answers.

| Theme | Seam verdict | Idle ring verdict (visible, dimmed, ring-shaped) | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|---|
| `midnightMirage` (default) |  |  |  |
| `gruvbox_dark` |  |  |  |
| `dracula` |  |  |  |

**Item 1 verdict** (PASS only if: all 6 Step-1 sub-checks PASS — where the gamepad regression's
`NOT ATTEMPTED`-with-stated-reason is treated as non-blocking per its own escape hatch, but an
actual reachable FAIL on it DOES block — AND all 3 scored themes PASS on BOTH the seam and
idle-ring columns in Step 2):

---

#### Item 2 (run 4) — Real window dragging under WKWebView, full re-run (REQ-34.10-04)

**Run fourth, deliberately after items 1/3/4** (§10.4 — toggling `framelessWindow` changes window
chrome and must not run before the visual/behavioral observations above are already recorded). With
`settings.framelessWindow` ON, drag the window by empty space in the tier-1 navbar and confirm it
moves. Then confirm dragging by a tab, by the Downloads ring, and inside the window-controls area
does NOT move it. Then toggle `framelessWindow` OFF and confirm the native titlebar still drags.

**Re-run in full** (this contract's own established convention — items 2/3/4/5 are re-driven live
rather than carried forward, even where the underlying files are confirmed byte-unchanged since run
3's own PASS), not carried forward from run 3 despite the source-level case for doing so.

| Sub-check | Verdict | Notes (required if FAIL) |
|---|---|---|
| Frameless ON: drag by empty navbar space moves the window |  |  |
| Frameless ON: drag by a tab does NOT move the window |  |  |
| Frameless ON: drag by the Downloads ring does NOT move the window |  |  |
| Frameless ON: drag inside the window-controls area does NOT move the window |  |  |
| Frameless OFF: native titlebar still drags, nothing broken |  |  |

**Item 2 verdict** (PASS only if all 5 sub-checks PASS):

*Leave `framelessWindow` OFF when this item finishes, restoring Preflight P6's starting state.*

---

#### Item 3 (run 4) — Tier-2 portal, full functional contract + F-34.10-05 (REQ-34.10-09)

**Run second.** Before starting sub-checks (vi)/(vii), close any expanded disclosure (Categories or
Filters) — see §10.4's own pairwise finding.

| # | Sub-check | Verdict | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|---|
| i | Search box text visibly filters the grid |  |  |
| ii | Categories control opens on CLICK (not mouseover), panel renders wholly inside the tier-2 column, every checkbox reachable, nothing extends past the window's left edge |  |  |
| iii | Clicking the Categories trigger a second time closes it |  |  |
| iv | Toggling a category visibly changes the grid |  |  |
| v | The Filters control behaves identically (click-toggle, contained, closes on second click) and a filter visibly changes the grid |  |  |
| vi | With both disclosures closed, on a game page the Games tab stays lit |  |  |
| vii | On that same game page, the tier-2 column collapses to nothing — no empty 204px band, no stray scrollbar during the collapse |  |  |

**F-34.10-05 sub-check** — expand Categories (or Filters) and judge whether the panel's background
reads as a deliberate surface belonging to the tier-2 column, or still as a black block:

| Theme | Verdict | Notes (required if FAIL) |
|---|---|---|
| `gruvbox_dark` |  |  |
| `dracula` |  |  |

**Item 3 verdict** (PASS only if all 7 functional-contract sub-checks (i-vii) PASS AND both
F-34.10-05 theme cells PASS):

---

#### Item 4 (run 4) — Ambient Downloads ring, reduced scope (REQ-34.10-08)

**Run third**, with the queue confirmed empty at the START of item 1 (P7) — by the time item 4
runs, the queue is about to stop being empty on purpose. **Sub-check (i) (idle: visible, dimmed,
ring-shaped) is NOT independently scored here** — it is the same observation as item 1's Step-2
`midnightMirage` idle-ring cell (§10.1's own reasoning for why this avoids double-counting). The
sub-checks below are RE-RUN live rather than carried forward, per this contract's established
convention — `DownloadsRing/index.tsx` and its `.scss` are confirmed byte-unchanged since run 3,
but each still gets its own explicit verdict.

| # | Sub-check | Verdict | Notes (required if FAIL or NOT ATTEMPTED) |
|---|---|---|---|
| ii | Hover at idle: still reads as a ring, not a solid filled disc |  |  |
| iii | Active: the arc fills and advances as the download progresses |  |  |
| iv | Active: the count badge appears and is correct |  |  |
| v | Clicking the ring reaches `/download-manager`, no tier-1 tab lit, ring shows active state |  |  |
| vi | Discriminator: `ProgressHeader`'s own percent readout (`DownloadManager/index.tsx:111-115`) advances past 0% while the same download runs |  |  |

**Item 4 verdict** (PASS only if sub-checks ii-vi all PASS AND item 1's own Step-2 `midnightMirage`
idle-ring cell — the cross-referenced stand-in for sub-check i — also PASSes; **explicit reopening
condition, binding regardless of what ii-vi show here:** if item 1's Step-2 per-theme idle-ring
sweep surfaces ANY FAIL across any theme, item 4 must be reopened in full at the next gap cycle):

---

#### Item 5 (run 4) — Electron divergence, cheap re-confirmation (REQ-34.10-05)

**Run last.** Quit the Tauri dev build entirely and re-confirm `pgrep -f 'tauri dev'` and
`pgrep -f 'target/debug/gamelib'` are both empty (P13, re-applied) before launching Electron, so the
two shells never run concurrently. Run `npm start` (Electron).

Re-run per §10.1's own reasoning (cheap, not judged necessary, since only shared CSS —
`NavTabs/index.scss`, this cycle's own `220211230` change — not any Electron-specific code path,
changed since run 3's own PASS). The run-1 traffic-light observation (NO overlap, D-03's "record,
don't fix" framing) stands and is NOT re-taken.

| Sub-check | Verdict | Notes |
|---|---|---|
| Shell renders |  |  |
| All 4 tier-1 tabs navigate |  |  |

**Item 5 verdict** (PASS only if both sub-checks PASS):

---

#### Non-scoring observation — 34.10-23's rescope collateral (does not count toward `items_passed`)

`34.10-23-SUMMARY.md` names five `<Tabs>` render sites that used to receive the leaked 8px
`padding-bottom` by accident and no longer do: WineManager, the Game details page, Steam login, the
GOG modify-install modal, and (protected by its own override, unaffected either way) the nav shell
itself. Ask the operator to look at the **Settings -> Games tab strip**, the **Download Manager tab
strip**, and the **Wine Manager tab strip** (both of the latter two declare their own
ancestor-scoped `.MuiTabs-root` rules — `DownloadManager/index.css:7-22`, `WineManager/index.css:65-73`
— and so were structurally shielded from the leak in the first place, but are worth a glance given
how much thrash this defect class caused across this phase) and record whether anything reads as
broken. **This row is recorded separately, explicitly, so a spot-check finding can never silently
inflate or deflate `items_passed`.**

| Surface | Verdict | Notes (required if FAIL) |
|---|---|---|
| Settings -> Games tab strip |  |  |
| Download Manager tab strip |  |  |
| Wine Manager tab strip |  |  |

