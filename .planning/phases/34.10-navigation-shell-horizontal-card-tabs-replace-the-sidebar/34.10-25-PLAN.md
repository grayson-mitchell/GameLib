---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 25
type: execute
wave: 2
depends_on: [34.10-23, 34.10-24]
gap_closure: true
autonomous: true
requirements: [REQ-34.10-06, REQ-34.10-16]
files_modified:
  - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md

must_haves:
  truths:
    - "Run 4's scope names, per item, whether it is re-run or carried forward, with evidence — never a bare 'unchanged, skip'"
    - "`dracula-classic` does not appear anywhere in run 4's scope"
    - "No step in run 4's contract assumes interactive Web Inspector console access"
    - "The contract mandates `pnpm tauri:dev` and a preflight that proves the served bundle contains the fixes being measured"
    - "The gamepad focus-scroll sub-check has a stated terminal disposition: measured, or permanently carried forward as a named residual risk"
  artifacts:
    - path: ".planning/phases/34.10-.../34.10-LIVE-GATE.md"
      provides: "§10.1 scope, §10.2 preflight, §10.3 structural reachability review, §10.4 pairwise review + derived run order"
      contains: "## 10. Run 4"
  key_links:
    - from: "§10.2 preflight"
      to: "commit 220211230's fix in the served bundle"
      via: "a bundle-content assertion run before any scored observation"
      pattern: "padding-bottom:0"
---

<objective>
Author the SCOPE half of the run-4 live-gate contract: `## 10. Run 4` in `34.10-LIVE-GATE.md`,
sections §10.1 (scope with per-item justification), §10.2 (preflight), §10.3 (structural
reachability review) and §10.4 (pairwise requirement-interaction review + derived run order).

This plan authors. It does NOT run anything and does not fill a single verdict. Plan 34.10-26
writes the item bodies; plan 34.10-27 is the only invocation permitted to run the gate and record
verdicts. That author/runner separation (decision D-E) exists so the runner cannot quietly reshape
the contract to fit what it observed.

Purpose: run 3 scored 4/5. Item 1 failed on F-34.10-03 and F-34.10-04, which have since been
root-caused and fixed (commit `220211230`, operator-confirmed `1c7a3359d`) — but confirmed in ONE
theme the gate does not score. What this phase owes is a measurement, not another fix.
Output: §10.1-§10.4 of `34.10-LIVE-GATE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md
@.planning/debug/navbar-seam-and-logo-offset.md
@.planning/ROADMAP.md

Read `Skill("spike-findings-gamelib")` and then its
`references/live-gate-contract-authoring.md` — the seven defect-class tests, Test 5's pairwise
rule (the unit of review is the requirement PAIR), and the dual-sink capture standard.

Read `Skill("sketch-findings-gamelib")` and then its `references/navigation-shell.md` §3 — the
navbar-lighter-than-body inversion table that makes "never evaluate in `midnightMirage` alone"
a design rule and not a preference. These decisions reach you ONLY through this skill; treat them
as settled input.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author §10.1 (scope, per item) and §10.2 (preflight)</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - `34.10-LIVE-GATE.md` §9.1 (line ~837) and §9.2 (line ~847) — run 3's own scope and preflight, the structure this section mirrors and re-derives rather than copies
    - `34.10-LIVE-GATE.md` frontmatter `findings:` (lines 1-53) — especially F-34.10-07's `cause` and `further_collisions` fields
    - `34.10-LIVE-GATE.md` §9.6 (line ~1277) — what run 3 settled and what it did not
    - `.planning/debug/navbar-seam-and-logo-offset.md` § Resolution and § session_constraints_learned — the fix, its single-theme scope limit, and the five environment traps
    - `.planning/phases/34.10-.../34.10-23-SUMMARY.md` — what plan 34.10-23 changed and which surfaces it could affect
    - `src/frontend/components/UI/ThemeSelector/themeLabels.ts` — confirm for yourself that a `dracula` case exists and no `dracula-classic` case does
  </read_first>
  <action>
    Append `## 10. Run 4 — scope, preflight, and re-derived review` to `34.10-LIVE-GATE.md`. Leave
    §0-§9 byte-identical; this file is append-and-preserve.

    Open with a short preamble stating: this section authors the contract and does not run it
    (decision D-E); plan 34.10-27 is the only invocation permitted to fill its verdict slots; and
    two fixes landed since run 3 — commit `220211230` (the F-34.10-03/-04 root-cause fix in
    `NavTabs/index.scss`) and plan 34.10-23 (rescoping the leaking `GamesSettings` rule plus a guard
    test). Read both SUMMARY/debug artifacts to re-derive scope; do not copy §9.

    **§10.1 Scope, with per-item justification** — one table row per item. Every row must state a
    decision AND its evidence. A bare "unchanged, skip" is forbidden; name the file, commit, or
    SUMMARY that supports the call.

    - **Item 1 — FULL re-run, and the only item that can fail this phase closed.** Structure it in
      two steps, mirroring §9.5's proven shape:
      * Step 1, in `midnightMirage` (the shipped default and a SCORED theme), before switching any
        theme: F-34.10-03 (no visible band or gap between the tier-1 tab strip and the content
        region below the active tab) and F-34.10-04 (wordmark, tab strip and Downloads ring read as
        ONE line). These are the two sub-checks the fix has never been measured against in a scored
        theme — the debug session's own confirmation was taken in the ambient teal scheme
        (navbar `#1c3d4a` / body `#091a21`), which is NOT one of the three the contract scores.
        Say so explicitly in the row so a reader knows why a "shipped fix" is not a closed finding.
      * Step 2, the sweep across `midnightMirage`, `gruvbox_dark`, `dracula` — both the seam column
        AND the idle Downloads-ring column, recorded per theme before moving to the next.
      * The gamepad focus-scroll regression sub-check (`GamesList/index.tsx`'s `scrollCardIntoView`,
        exercised only when `activeController` is truthy).
      * F-34.10-06(a)/(b) and the mouse/keyboard back-to-top regression PASSED in run 3 against the
        same code; state the carry-forward decision for each explicitly with its evidence, or
        re-scope them in — either is acceptable, silence is not.
    - **`dracula-classic` is DROPPED from run 4's scope entirely.** State the reason in the table:
      confirmed absent from the shipped theme picker (`src/frontend/components/UI/ThemeSelector/themeLabels.ts`
      has a `dracula` case and no `dracula-classic` case), recorded as F-34.10-07, a contract defect
      rather than an operator omission. It must not reappear as a perpetual NOT ATTEMPTED spot-check.
    - **Items 2, 3, 4 and 5 — decide and JUSTIFY each.** All four PASSED in run 3. For each, state
      whether it is re-run or carried forward and why, citing what did or did not change since run 3.
      Relevant facts to weigh, stated so you do not have to hunt them: plan 34.10-23 touched
      `GamesSettings/index.scss`, `NavTabs/index.scss` and one new test file; it changed no
      component markup, no `resolveDragRegion`, no `DownloadsRing` file, and no Electron-only path.
      Also weigh this project's own recorded precedent, which §9.1 acted on and was vindicated by:
      34.10-18 committed a fix to `.NavShell__navbar` reasoned as safe by source inspection alone
      and was wrong in 9 of 11 themes. Cheap re-measurement has repeatedly beaten confident source
      reasoning in this phase.
    - **Add one non-scoring OBSERVATION row for plan 34.10-23's collateral.** Rescoping the
      `GamesSettings` rule removed an 8px `padding-bottom` that had been leaking onto four other
      `<Tabs>` surfaces. Ask the operator to look at the Settings → Games tab strip, the Download
      Manager tab strip, and the Wine Manager tab strip and record whether anything reads as broken.
      Mark it explicitly **non-blocking, does not count toward `items_passed`** — recorded separately
      so a spot-check finding can never silently inflate or deflate the item count.

    **§10.2 Preflight for run 4** — a table of numbered rows, each with a check, an exact command or
    action, and the item premise that depends on it. Carry forward P1-P9's shape from §9.2 and add
    the rows below. Do not copy §9.2's expected jest baseline: derive the CURRENT expected suite and
    test counts by running `pnpm jest --runInBand --silent --forceExit` at THIS plan's authoring
    time (this phase's documented `test:ci` hang workaround — not bare `pnpm test:ci`) and write the
    observed numbers in as the expected numbers, with the date. Plan 34.10-23 added exactly 5 tests;
    account for that rather than inferring it.

    New or hardened rows this run requires, each stated as a command the operator can run:
    - **Launch command is `pnpm tauri:dev`, never bare `tauri dev`.** `src-tauri/tauri.conf.json`
      sets `frontendDist: "../build"` — a static directory, not a dev server — so a bare `tauri dev`
      serves whatever `electron-vite build` last wrote. Run 3's own preflight caught a pre-fix bundle
      this exact way, and this phase has already nearly scored five fixes against a bundle that
      predated them all.
    - **Bundle-contains-the-fix preflight**, the direct analogue of how run 3 verified 34.10-18/-19:
      after launching, assert the served CSS chunk under `build/assets/` contains the compiled
      `.NavTabs.MuiTabs-root` rule with `padding-bottom:0`, AND that the compiled `GamesSettings`
      tab rule now carries an ancestor (a `.settingsWrapper`-prefixed or `.settingsDialogContent`-
      prefixed form) rather than a bare `.MuiTabs-root{padding-bottom`. Give the exact `grep -o`
      commands. No scored observation may be taken before this row passes.
    - **`GAMELIB_DEV_SECRET_VAULT=1` is required.** Without it an unanswered Keychain prompt makes
      `keyring_get` time out after 45s and takes the whole app down mid-run.
    - **Exactly one app instance** (`pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'`
      both empty before the first launch, re-checked before the Electron launch if item 5 is in
      scope). This trap has fired at least three times in this phase.
    - **Screenshot hygiene, if any capture is taken.** `screencapture -R` fails with "could not
      create image from rect" when the display sleeps, and a full-screen capture silently returns an
      all-black ~107KB PNG instead of erroring. Wrap every capture in `caffeinate -d -i -u -t 60`
      and size-check the result (>400KB) before trusting it. The window moves and the active
      route/theme change between launches — re-locate the navbar each time; never reuse hard-coded
      coordinates across restarts.
    - **No step may assume interactive Web Inspector console access.** Confirmed dead on this build:
      input rows echo, `1+1` returns nothing, Enter is inert, Cmd+Enter only echoes, while the app's
      own logging still reaches the console (so this is evaluation, not attachment). If any step
      needs a computed value, the working channel is a `window.api.logInfo` probe dumping to
      `gamelib.log`, or a `screencapture` + pixel-decode harness. State this as a contract-level
      constraint, not a footnote.
    - **Controller presence (the P9 successor).** Record the literal answer. State the terminal
      disposition up front: if no controller is available, the gamepad focus-scroll sub-check is
      recorded NOT ATTEMPTED and **permanently carried forward as a named residual risk** in
      ROADMAP.md and REQUIREMENTS.md by plan 34.10-27 — not left as a perpetual pending cell, and
      never inferred from the mouse/keyboard sub-check's PASS, which exercises a different code path
      with different (container-rect-relative) arithmetic.
    - Carry forward the queue-empty and scrollable-grid rows (§9.2's P7/P8) if and only if the
      sub-checks that depend on them are in run 4's scope; if a row's dependent sub-check is carried
      forward rather than re-run, drop the row and say why.

    Leave §10.2's Preflight Results block EMPTY. Filling it would make this plan both author and
    runner, violating D-E. Do NOT write the literal placeholder string that a later automated check
    might grep for — see plan 34.10-26, which owns the checks; state in prose that the block is
    intentionally unfilled.
  </action>
  <acceptance_criteria>
    - `grep -c '^## 10\. Run 4' .planning/phases/34.10-*/34.10-LIVE-GATE.md` returns `1`
    - `grep -c '^### 10\.1' …LIVE-GATE.md` returns `1` and `grep -c '^### 10\.2' …LIVE-GATE.md` returns `1`
    - Extracting §10 (from `## 10. Run 4` to end of file) and stripping nothing: the extract contains
      `pnpm tauri:dev`, `GAMELIB_DEV_SECRET_VAULT=1`, `caffeinate`, `padding-bottom:0`,
      `window.api.logInfo`, and `themeLabels.ts`
    - The §10 extract mentions `dracula-classic` at most in a DROPPED/out-of-scope statement: every
      occurrence must be within 200 characters of the word `dropped` or `does not exist`
    - The §10 extract contains a distinct scope row for each of items 1, 2, 3, 4 and 5, each with a
      stated re-run/carry-forward decision and at least one cited file, commit, or SUMMARY
    - The §10 extract contains the non-scoring observation row for 34.10-23's collateral, with the
      literal words `does not count toward` in its text
    - §10.2's expected jest baseline is a number pair written into the document, and the SUMMARY
      records the authoring-time `pnpm jest --runInBand --silent --forceExit` run those numbers came from
    - `git diff …/34.10-LIVE-GATE.md` shows ONLY additions after the previous final line — zero
      deletions and zero modifications to §0-§9 (`git diff --numstat` deletion count for this file is `0`)
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md; awk '/^## 10\. Run 4/,0' $F > /tmp/s10.txt && grep -q 'pnpm tauri:dev' /tmp/s10.txt && grep -q 'GAMELIB_DEV_SECRET_VAULT=1' /tmp/s10.txt && grep -q 'caffeinate' /tmp/s10.txt && grep -q 'window.api.logInfo' /tmp/s10.txt && grep -q 'themeLabels.ts' /tmp/s10.txt && grep -q 'does not count toward' /tmp/s10.txt && [ "$(git diff --numstat -- $F | cut -f2)" = "0" ] && echo S10_SCOPE_OK</automated>
  </verify>
  <done>
    §10.1 and §10.2 exist. Every item carries a justified scope decision, `dracula-classic` is
    dropped with its reason, the preflight mandates `pnpm tauri:dev` plus a bundle-content
    assertion, and no step assumes console access. §0-§9 are byte-identical.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author §10.3 (structural reachability review) and §10.4 (pairwise review + derived run order)</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - `34.10-LIVE-GATE.md` §9.3 (line ~947) and §9.4 (line ~972) — run 3's reviews, including its two explicit Test-4 (absence-observability) cases and its 12-pair reduction
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` — all seven defect-class tests and Test 5's pairwise scoping rule
    - `34.10-LIVE-GATE.md` frontmatter finding `F-34.10-07` — the six verification/prose collisions and the Test-7 (UI-level reachability) miss that put a non-existent theme in run 3's scope
    - The §10.1 scope table this plan's Task 1 just wrote — the review must cover exactly the sub-checks that scope defines, no more and no fewer
  </read_first>
  <action>
    Append `### 10.3 Structural reachability review for run 4` and
    `### 10.4 Pairwise review for run 4 (Test 5)` to `34.10-LIVE-GATE.md`.

    **§10.3** — one table row per sub-check §10.1 puts in scope. Columns: sub-check / surface /
    what it asks for / verdict (`REACHABLE`, `REACHABLE, CONDITIONAL on <preflight row>`, or
    `UNREACHABLE`) / evidence. Re-derive against the CURRENT source tree; cite `file:line` and
    confirm each citation by reading the file, not by trusting §9.3's line numbers, which predate
    two commits.

    Apply all seven defect-class tests. Two in particular, because each has already failed once in
    this phase:
    - **Test 7 (UI-level reachability):** for every UI control a step asks the operator to operate,
      confirm the control actually exists in the shipped build. This is the test that missed
      `dracula-classic`. For the theme picker specifically, enumerate the theme options
      `themeLabels.ts` really offers and confirm all three scored themes are among them.
    - **Test 4 (absence-observability):** for every sub-check whose PASS is the ABSENCE of something
      (no seam, no band, no second row, scrollbar does NOT overdraw), state what the PRESENCE case
      would look like, so "I saw nothing" cannot be misread as a pass. The seam sub-check's presence
      signal is a strip of navbar-coloured background between the selected tab's bottom edge and the
      content surface — the debug session measured that strip at exactly 8px, so name a concrete
      expected magnitude rather than "a gap".

    **§10.4** — apply Test 5 with the reduction stated explicitly: every {state-mutating action}
    against every {evidence-bearing item or sub-check}, not the full cross product. State the count
    of pairs considered and the count flagged. State-mutating actions to enumerate at minimum:
    theme switching; expanding a tier-2 disclosure or applying a category/filter (it SHORTENS the
    Games grid and can retroactively invalidate a scrollable-grid precondition); starting a download
    (it destroys the idle-ring state irreversibly for the session); toggling `framelessWindow`;
    quitting Tauri to launch Electron; running the preflight CI commands.

    Hunt specifically for anything that RESTARTS, TRUNCATES, CLEARS, or RESETS a source of evidence
    — the pair class that destroyed three of six items' transcripts in phase 34.4.2. Note in the
    review whether run 4 has any log-line or transcript dependency at all: if every criterion is a
    direct visual/interactive observation, say so explicitly and mark Test 3 N/A, as runs 1-3 did;
    if any step depends on `gamelib.log` (a `window.api.logInfo` probe would), then Test 3 is NOT
    N/A and the dual-sink append-and-archive capture standard applies to that step — decide and
    state which, do not leave it ambiguous.

    Close §10.4 with a **Run order, derived (not asserted)** subsection: list the ordering
    constraints each DESTROYS pair imposes, then state the order those constraints produce. If the
    derived order matches run 3's `[1, 3, 4, 2, 5]`, say that it coincides rather than that it was
    reused, and update the frontmatter `run_order` field only if the derivation actually differs.
  </action>
  <acceptance_criteria>
    - `grep -c '^### 10\.3' …LIVE-GATE.md` returns `1` and `grep -c '^### 10\.4' …LIVE-GATE.md` returns `1`
    - The §10 extract contains all seven defect-class test names or numbers, with `Test 4` and
      `Test 7` each appearing in a sentence that applies them to a specific named sub-check
    - The §10 extract contains a `Pairs considered:` count and a `Pairs flagged:` count, both integers
    - The §10 extract contains a `Run order` subsection whose text derives the order from named
      DESTROYS pairs rather than asserting it
    - Every `file:line` citation in §10.3 resolves: for each cited path, the file exists and the
      cited line contains what the row claims. Verify by spot-checking every citation with
      `sed -n '<line>p' <file>` and record the checks in the SUMMARY — run 3's own §9.3 rows were
      correct only because this was actually done
    - Test 4's seam row names a concrete expected magnitude (the string `8px` or `8 px` appears in it)
    - `git diff --numstat` deletion count for `34.10-LIVE-GATE.md` is `0`
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md; awk '/^## 10\. Run 4/,0' $F > /tmp/s10.txt && grep -q '^### 10.3' /tmp/s10.txt && grep -q '^### 10.4' /tmp/s10.txt && grep -q 'Pairs considered' /tmp/s10.txt && grep -q 'Pairs flagged' /tmp/s10.txt && grep -q 'Test 7' /tmp/s10.txt && grep -qE '8 ?px' /tmp/s10.txt && [ "$(git diff --numstat -- $F | cut -f2)" = "0" ] && echo S10_REVIEW_OK</automated>
  </verify>
  <done>
    §10.3 gives every in-scope sub-check a reachability verdict with a source citation verified by
    reading the file. §10.4 states its pairwise reduction, its considered/flagged counts, and a run
    order derived from named DESTROYS pairs. Test 3's applicability is decided explicitly rather
    than assumed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → gate contract | The contract is the sole instrument that decides whether this phase ships. A defective contract produces a confident-looking but unfounded verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-25-01 | Spoofing | A stale bundle masquerading as the fixed build | mitigate | §10.2 mandates `pnpm tauri:dev` and a bundle-content `grep` asserting the compiled `padding-bottom:0` rule is present before any scored observation |
| T-34.10-25-02 | Repudiation | A scope decision recorded as "unchanged, skip" with no evidence, unfalsifiable later | mitigate | §10.1's acceptance criteria require every item row to cite a file, commit, or SUMMARY |
| T-34.10-25-03 | Denial of Service | An unanswered Keychain prompt killing the app 45s into an unattended run | mitigate | `GAMELIB_DEV_SECRET_VAULT=1` is a mandatory preflight row |
| T-34.10-25-04 | Information Disclosure | A step assuming a dead Web Inspector console, silently producing no evidence | mitigate | Contract-level constraint forbidding console-dependent steps; the `window.api.logInfo` / screencapture channels are named as the working substitutes |
| T-34.10-25-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task exists in this plan; it edits one markdown file. Any install attempt must stop the plan. |
</threat_model>

<verification>
- `git diff --name-only` lists exactly `34.10-LIVE-GATE.md`. Explicit per-file `git add` only —
  another session is active in this repository.
- `git diff --numstat` for that file must show `0` deletions. §0-§9 are historical evidence and are
  append-only.
- This plan fills NO verdict and runs NO gate. Nothing here ticks REQ-34.10-06 or REQ-34.10-16.
- A `js-yaml` parse of the file's frontmatter must succeed before and after, with `verdict`,
  `items_passed` and every `items[].status` value UNCHANGED — this plan does not touch them.
  Plan 34.10-26 owns the frontmatter reset; plan 34.10-27 owns the verdicts.
</verification>

<success_criteria>
- `## 10. Run 4` exists with §10.1-§10.4 complete.
- Every item has a justified scope decision; `dracula-classic` is dropped with its reason recorded.
- The preflight mandates `pnpm tauri:dev`, a bundle-content assertion,
  `GAMELIB_DEV_SECRET_VAULT=1`, single-instance, and screenshot hygiene; and forbids console-dependent steps.
- The gamepad sub-check's terminal disposition (measure, or permanently carry forward as a named
  residual risk) is stated in the contract rather than deferred again.
- §0-§9 byte-identical.
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-25-SUMMARY.md` when done.
</output>
