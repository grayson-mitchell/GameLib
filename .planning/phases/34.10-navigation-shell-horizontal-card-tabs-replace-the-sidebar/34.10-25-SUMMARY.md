---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 25
subsystem: process
tags: [live-gate, contract-authoring, gap-cycle-3]

# Dependency graph
requires:
  - phase: 34.10 (debug session navbar-seam-and-logo-offset.md, commit 220211230)
    provides: the F-34.10-03/F-34.10-04 root-cause fix (padding-bottom:0 in NavTabs/index.scss), live-confirmed but only in an unscored teal theme
  - phase: 34.10-23
    provides: rescoped GamesSettings/index.scss + repo-wide muiTabsSelectorScoping.test.ts guard
provides:
  - "## 10. Run 4" scope + preflight + structural reachability + pairwise review (§10.1-§10.4) in 34.10-LIVE-GATE.md
affects: [34.10-26 (authors §10.5 item bodies + checker script), 34.10-27 (runs the gate, fills verdicts)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Author/runner separation for a live-gate contract's SCOPE half vs BODY half, split across two plans in the same gap cycle (34.10-25 scope, 34.10-26 body+checker), both still separate from the runner (34.10-27), per decision D-E"
    - "Scope decisions justified by a direct `git log --stat <commit>^..HEAD -- <paths>` check proving zero diff, rather than by source-level reasoning alone -- distinguishes a defensible carry-forward-eligible decision from the exact 'source reasoning stands in for measurement' failure mode that produced 34.10-18's 9-of-11 miss"

key-files:
  modified:
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md

key-decisions:
  - "Item 1 (the only item that can still fail this phase) is structured in two steps: Step 1 re-measures F-34.10-03/F-34.10-04 in midnightMirage (a SCORED theme) for the first time -- the debug session's own live confirmation was taken in an unscored teal theme (navbar #1c3d4a / body #091a21) and does not, by itself, close the finding against this contract. Step 2 sweeps all three scored themes for the seam + idle-ring columns."
  - "F-34.10-06(a)/(b) and the mouse/keyboard back-to-top regression, which PASSED in run 3 against files confirmed byte-unchanged since, are RE-RUN rather than carried forward -- citing this contract's own repeatedly-proven lesson that cheap re-measurement beats confident carry-forward, even when the carry-forward would cite a live PASS rather than source inference."
  - "Items 2, 3, 4, 5 are likewise RE-RUN rather than carried forward, for the same reason, keeping this run's per-item scope structurally identical to run 3's own established convention rather than trimming it opportunistically."
  - "dracula-classic is dropped from run 4's scope entirely -- confirmed absent from themeLabels.ts's defaultThemes/resolveThemeLabel (no key, no case) -- recorded as the Test-7 UI-level-reachability outcome of F-34.10-07, not merely restated as a known issue."
  - "A non-scoring OBSERVATION row (does not count toward items_passed) covers 34.10-23's rescope collateral on WineManager/GamePage/SteamLogin/GOG-modify-install-modal/Settings-Games/Download-Manager tab strips."

patterns-established:
  - "Test 4 (absence-observability) rows should name a concrete expected magnitude for the presence case wherever one is known (here: 8px, from --space-xs), not just 'a gap' -- gives the runner a falsifiable shape for a future regression, not merely a vague absence."
  - "Test 3 (log-line/sink reachability) decided explicitly per contract even when N/A, including a named conditional exception (an ambiguity-driven window.api.logInfo fallback DOES invoke the dual-sink standard) rather than leaving the boundary case implicit."

requirements-completed: []

# Metrics
duration: ~95min
completed: 2026-08-09
---

# Phase 34.10 Plan 25: Author run-4 live-gate scope, preflight, and structural/pairwise review Summary

**Authored `## 10. Run 4` (§10.1-§10.4) in `34.10-LIVE-GATE.md`: item 1's re-measurement is split into a midnightMirage-only step (closing the "confirmed only in an unscored teal theme" gap left by the debug session) and a three-theme sweep, `dracula-classic` is dropped from scope with its Test-7 evidence, and every other item's re-run-vs-carry-forward decision is justified by a direct `git log --stat` diff rather than source reasoning alone.**

## Performance

- **Duration:** ~95 min (context loading + source verification + two-task authoring)
- **Started:** 2026-08-09 (session resumed after an API interruption with zero prior writes)
- **Completed:** 2026-08-09
- **Tasks:** 2 completed
- **Files modified:** 1 (append-only)

## Accomplishments

- §10.1: per-item run-4 scope table. Item 1 (the only item that can fail the phase) gets a mandatory two-step FULL re-run — Step 1 in `midnightMirage` re-measures F-34.10-03/F-34.10-04 against commit `220211230`'s fix in a SCORED theme for the first time (the debug session's own live confirmation was in an unscored teal ambient theme); Step 2 sweeps `midnightMirage`/`gruvbox_dark`/`dracula` for the seam and idle-ring columns. `dracula-classic` is dropped from scope entirely, confirmed absent from `themeLabels.ts`. Items 2/3/4/5 are re-run (not silently carried forward) with each decision backed by a direct `git log --oneline --stat 220211230^..HEAD -- <dependency files>` check returning empty (zero diff since run 3's own live PASS). A non-scoring observation row covers 34.10-23's rescope collateral on five other `<Tabs>` render sites.
- §10.2: preflight P1-P9 re-derived (jest baseline re-measured live at authoring time: 220 suites / 4274 tests, 0 failing, exactly matching 34.10-23-SUMMARY.md's claimed post-fix count; the previously-flagged `hardcodedStringGate` failure did not reproduce — confirmed via `git status` showing the flagged file clean). Five new rows (P10-P14): mandatory `pnpm tauri:dev`, a bundle-content grep proving `padding-bottom:0` reached the served CSS, `GAMELIB_DEV_SECRET_VAULT=1`, single-instance re-assertion, and screenshot hygiene. Preflight Results block left intentionally empty per decision D-E, with an explicit instruction not to write a placeholder string (F-34.10-07's own lesson).
- §10.3: structural reachability review, every citation re-confirmed by direct `sed -n`-equivalent read against current source (not trusted from §9.3). Test 7 applied explicitly to the theme picker (confirms `midnightMirage`/`gruvbox_dark`/`dracula` are real options; confirms `dracula-classic` is UNREACHABLE). Test 4 applied with a concrete magnitude (8px) for the seam's presence case.
- §10.4: pairwise review — 11 pairs considered, 5 flagged, all resolved by run order or explicit sequencing. Two pairs are new relative to run 3, both consequences of re-scoping item 1's scroll sub-checks back into a live re-run. Derived run order `[1, 3, 4, 2, 5]` coincides with runs 1-3 via a partially different reasoning chain, stated as a coincidence rather than a reuse. Test 3 decided explicitly (N/A for scored verdicts; a named exception for an ambiguity-driven `window.api.logInfo` fallback).

## Task Commits

1. **Task 1: Author §10.1 (scope) and §10.2 (preflight)** — `0d339e4ab` (docs)
2. **Task 2: Author §10.3 (structural reachability) and §10.4 (pairwise review)** — `380251e68` (docs)

**Plan metadata:** (this commit, following this summary)

## Files Created/Modified

- `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md` — appended `## 10. Run 4` (§10.1-§10.4), 201 insertions across both commits, 0 deletions. §0-§9 confirmed byte-identical (`git diff --numstat` against the pre-plan commit `e3bb1aff1` shows exactly one changed file, 0 deletions).

## Decisions Made

See `key-decisions` in frontmatter above. The most consequential one: every re-run-vs-carry-forward call for items 2/3/4/5 was made the same way run 3 made it — re-run despite a defensible case for carrying forward — because this contract's own history (34.10-18's 9-of-11 miss, five failed fix attempts on F-34.10-03/-04) has repeatedly shown that cheap re-measurement beats confident reasoning, even reasoning built on a genuine prior live measurement rather than source inspection alone.

## Deviations from Plan

None. Both tasks matched their `<action>`/`<acceptance_criteria>` blocks. One self-correction during Task 1: the first draft of the `dracula-classic` scope-drop sentence had `dracula-classic` more than 200 characters from the words "dropped" or "does not exist" in one place — the acceptance criterion's own automated check (a `js-yaml`-style windowed proximity scan, run by hand via Python) caught this before commit, and the sentence was rewritten so every occurrence sits inside a 200-character window of one of those two phrases. Re-verified clean before staging.

## Authoring-Time Verification Run (for the SUMMARY, per plan instruction)

- `pnpm jest --runInBand --silent --forceExit` (2026-08-09): **220 suites / 4274 tests, 0 failing.** This is the number written into §10.2's P1 row as run 4's expected baseline.
- `pnpm codecheck` (2026-08-09): exit 0, no diagnostics.
- `git log --oneline --stat 220211230^..HEAD -- src/frontend/App.css src/frontend/screens/Library/index.tsx src/frontend/components/UI/NavShell/index.tsx src/frontend/components/UI/NavShell/navTabs.ts src/preload/api/tauriWindowChrome.ts src/frontend/screens/Library/components/GamesList/index.tsx src/frontend/components/UI/NavShell/components/DownloadsRing`: empty — the fact underpinning every "re-run because unchanged, not because untested" decision in §10.1.
- Every `file:line` citation in §10.3 was independently re-read against the current tree via the `Read`/`Bash grep -n` tool before being written into the contract (`NavTabs/index.scss:124-142`, `GamesSettings/index.scss:22-81`/`62`, `App.css:56`/`96`, `App.tsx:103`, `Library/index.tsx:277/291/316/324/332`, `GamesList/index.tsx:46-76`/`137-151`, `tauriWindowChrome.ts:301-334`/`326-330`/`333`, `NavShell/index.tsx:50-53`, `navTabs.ts:37`, `Dropdown/index.scss:43`, `DownloadsRing/index.tsx:72`, `DownloadManager/index.tsx:111`, `DownloadManager/index.css:7-22`, `WineManager/index.css:65-73`, `themeLabels.ts:32-47`/`38`/`55-90`/`70`). None were trusted from §9.3's own line numbers.

## Issues Encountered

None beyond the `dracula-classic` proximity self-correction noted above.

## Requirements Note

`requirements: [REQ-34.10-06, REQ-34.10-16]` is this plan's own frontmatter, but per this plan's `<verification>` section: this plan authors a contract and fills no verdict, so it ticks neither requirement. `requirements-completed` above is left empty and `requirements.mark-complete` was not run. Both requirements remain open pending the run-4 live gate itself (34.10-27).

## User Setup Required

None — no external service configuration required. This plan edits one markdown document.

## Next Phase Readiness

- Plan 34.10-26 authors `### 10.5 Run 4 — the items` (the item bodies) plus a checker script, validated against a filled specimen per this phase's own F-34.10-07 lesson, and resets the frontmatter's `items[]`/`items_passed`/`verdict` to pending for run 4.
- Plan 34.10-27 is the only invocation permitted to run the gate and fill §10.5's verdict slots — a blocking, `autonomous: false` live gate.
- No blockers for continuing to plan 34.10-26.

---
*Phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar*
*Completed: 2026-08-09*

## Self-Check: PENDING
