---
phase: 34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar
plan: 22
type: execute
wave: 5
depends_on: ["34.10-21"]
files_modified:
  - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [REQ-34.10-04, REQ-34.10-05, REQ-34.10-06, REQ-34.10-08, REQ-34.10-09, REQ-34.10-16]

must_haves:
  truths:
    - "Run 3's preflight results are RECORDED as measured — including the exactly-one-process check — not left as an unfilled template the way run 2's were"
    - "Item 1's per-theme sweep across midnightMirage, gruvbox_dark, dracula and the dracula-classic spot-check carries a real per-cell verdict for the first time in this phase, or an explicit NOT ATTEMPTED with its reason"
    - "Every one of F-34.10-03, -04, -05, -06 is either closed by a measurement or left open with what was observed — none is closed because a fix landed"
    - "REQUIREMENTS.md reflects only what was measured; no requirement is ticked from a green test suite"
    - "The phase's outcome is stated honestly: closed on 5/5, or open with the next command named"
  artifacts:
    - path: ".planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md"
      provides: "§9's filled preflight results, per-item verdicts, and reconciled frontmatter for run 3"
      contains: "Run 3"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement states matching run 3's measured outcome, with notes citing the run"
  key_links:
    - from: "34.10-LIVE-GATE.md § 9.5 item verdicts"
      to: ".planning/REQUIREMENTS.md"
      via: "per-requirement completion ticked only from its own gate item's measured verdict"
      pattern: "run 3"
---

<objective>
**Run** the run-3 live gate contract that plan 34.10-21 authored, on real hardware, and record what
actually happened — then reconcile `REQUIREMENTS.md` to the measured result and state plainly
whether phase 34.10 closes.

Purpose: decision D-E — the plan or task that authors a gate contract may never also run it. Plan
34.10-21 authored §9 and filled nothing. This plan is that separate, later invocation. Its job is
measurement and honest recording, not persuasion: a live gate has beaten a green test suite three
times on this project, and this phase alone has shipped two visibly broken features behind a fully
green 218-suite run.

Output: §9 filled in, `REQUIREMENTS.md` reconciled, and either a closed phase or a named next step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-21-SUMMARY.md
@.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-CONTEXT.md

Required skill:
- `Skill("spike-findings-gamelib")` — `references/live-gate-contract-authoring.md` for the
  recording rules (NOT ATTEMPTED is never inferred; verdicts are written as they are measured, not
  batched; the single-instance rule).

<interfaces>
<!-- The contract this plan executes. Do not re-derive its scope; run it. -->

`34.10-LIVE-GATE.md` § 9, authored by plan 34.10-21, contains:
  §9.1  scope with per-item justification
  §9.2  preflight P1-P8 with an EMPTY results block
  §9.3  structural reachability review
  §9.4  pairwise review and the derived run order
  §9.5  item bodies, every verdict cell reading `PENDING — run 3`

Frontmatter fields this plan reconciles: `verdict`, `items_passed`, `run_number`, the run-3 entry
in `runs:`, the `items:` list's `status` fields, and the `findings:` list's `status`/`closed_by`
for F-34.10-03..06.

Requirement states at the start of this plan:
  REQ-34.10-04  Complete   (item 2 — window dragging)
  REQ-34.10-05  Complete   (item 5 — Electron)
  REQ-34.10-06  Pending    (item 1 — theme survival; reverted by plan 34.10-17)
  REQ-34.10-08  Complete   (item 4 — Downloads ring)
  REQ-34.10-09  Pending    (item 3 — tier-2 filter controls; held open by F-34.10-05)
  REQ-34.10-16  Pending    (the gate itself closes the phase)

Standing rules, restated because they decide this plan's outcome:
  - The phase closes only on **5/5**. `verdict: pass` may be set only if `items_passed == 5`.
  - **NOT ATTEMPTED is never inferred** — not as PASS from a green suite, not as FAIL from silence.
  - **No requirement may be ticked from a unit test.** `testEnvironment: 'node'`, no jsdom, no CSS
    engine. Visual truths are settled by this gate alone.
  - **Exactly one app process**, or the `[shell]` sink splits and a half-observed run looks
    measured (F-34.4.2-15/-18). This failed live on this project today.
  - A finding is closed by a measurement, never by a fix having landed.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the run-3 gate on real hardware and record every verdict as it is measured</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md § 9 in full (this is the script; do not improvise around it, and do not re-scope it mid-run)
    - § 9.4's derived run order (the order the items must be taken in, and why)
    - § 6.2's unfilled Preflight Results block (run 2's own recorded capture gap — the thing this run must not repeat)
  </read_first>
  <action>
    Execute §9's preflight and items in §9.4's derived run order, relaying each step to the
    developer and writing their answers into §9's own tables **as each is measured**, not batched at
    the end. Verdicts live in this markdown file; that is deliberate, and it is why quitting the app
    for item 5 cannot destroy them.

    Run P1 and P2 yourself (`pnpm jest --runInBand --silent --forceExit`, then `pnpm codecheck`) and
    write the observed numbers into §9.2's results block alongside the expected numbers §9.1/§9.2
    derived from the 34.10-18/-19/-20 SUMMARYs. If they differ, say by how much before proceeding
    rather than after.

    Ask the developer for P3 through P8 and record their literal answers. **P3 is not optional and
    not inferable**: `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'` must both be run
    and their result written down as `clean` / `found and killed` / `found, NOT killed` with the
    explanation. Run 2's preflight block was left entirely unfilled and the recorder correctly
    flagged that as a gap in its own capture; do not reproduce it. If a preflight row fails, every
    item whose premise depends on it is recorded NOT ATTEMPTED with that row named — never inferred.

    Recording rules, applied to every cell:
    - Only `PASS`, `FAIL` or `NOT ATTEMPTED`. No blanks, no `PENDING — run 3` left behind, no
      "probably", no "looks fine" standing in for a per-theme cell.
    - A general, unscoped report does **not** satisfy a per-theme cell. Item 1's sweep is four
      themes × two columns; if the developer reports "the seam looks fine" without naming the
      theme, record the unnamed themes NOT ATTEMPTED and ask again. This is precisely how the
      sweep was lost in run 2, for the second time.
    - For any FAIL, capture the operator's own words verbatim before paraphrasing, and mint a new
      finding ID (`F-34.10-07` onward) in the frontmatter `findings:` list rather than folding it
      into an existing one.
    - Do not diagnose a cause during the run. Record the observation; diagnosis belongs to a
      later plan and a guessed cause written here becomes tomorrow's falsified premise.

    Present the run to the developer as one continuous session with the preflight first, then the
    items in the derived order. Tell them up front roughly how long it takes and that item 1 is the
    long one because it has never been completed.
  </action>
  <what-built>
    Nothing new is built by this task. Plans 34.10-18, -19 and -20 landed fixes for F-34.10-03,
    -04, -05 and -06; plan 34.10-21 authored the contract that measures whether they worked. This
    task is the measurement. Every fix in this cycle is unproven until it runs — the test suite is
    `testEnvironment: 'node'` with no jsdom and cannot see a seam, a line, a scrollbar or a colour.
  </what-built>
  <how-to-verify>
    Allow a decent block of time — item 1 alone is four themes with two judgements each plus five
    layout sub-checks, and it has never been completed in two attempts.

    1. **Preflight.** I will run the test suite and `pnpm codecheck` and report the numbers. You
       run `pgrep -f 'tauri dev'` and `pgrep -f 'target/debug/gamelib'` and tell me exactly what
       they print — if either prints a PID, kill it or tell me whose it is before launching
       anything. Then tell me: is the download queue empty, is `framelessWindow` OFF in
       Settings -> General, does your library have a game with a distinctive title and an openable
       game page, is at least one game installable, is the Games grid long enough to actually
       produce a scrollbar with no category or filter left applied, and do you have a game
       controller connected and recognised. The controller question is not optional politeness —
       one of item 1's sub-checks exercises a gamepad-only code path that plan 34.10-18 rewrote,
       and if you have no controller I record that sub-check NOT ATTEMPTED with the reason rather
       than skipping it.
    2. **Then work through `34.10-LIVE-GATE.md` § 9.5 in the order § 9.4 derived.** I will read you
       each item's steps. Answer each sub-check with `PASS`, `FAIL` or `not attempted` plus a
       sentence — and for anything you could not reach, say why rather than guessing.
    3. **Item 1 is per-theme and must be answered per-theme.** For each of `midnightMirage`,
       `gruvbox_dark`, `dracula` and `dracula-classic`, switch the theme, then answer both
       questions for that theme before moving on: does the active tab read as continuous with the
       content surface (no seam, no band, no gap), and is the idle Downloads ring visible, dimmed
       and ring-shaped. "They all looked fine" is not an answer I can record — I will have to write
       down NOT ATTEMPTED for every theme you did not name.
    4. If anything looks wrong, describe what you SEE, not what you think causes it. I will record
       your words verbatim.

    Everything gets written into the gate file as you say it, so nothing is lost if the session is
    interrupted or the app has to be restarted.
  </how-to-verify>
  <acceptance_criteria>
    - §9.2's Preflight Results block is filled for P1-P8, with the `pgrep` outcome written literally
    - No cell anywhere in §9 still reads `PENDING — run 3`
    - Every cell reads exactly `PASS`, `FAIL` or `NOT ATTEMPTED`, and every `NOT ATTEMPTED` and
      every `FAIL` carries a reason or the operator's verbatim words in its notes cell
    - Item 1's per-theme table has all eight cells resolved (four themes × seam + idle ring)
    - Any new defect observed is minted as its own finding ID in the frontmatter `findings:` list
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md';const s=fs.readFileSync(p,'utf8');const nine=s.slice(s.indexOf('## 9.'));if(/PENDING — run 3/.test(nine))throw new Error('unfilled verdict slots remain in §9');const body=nine.slice(nine.indexOf('### 9.5'));const cells=[...body.matchAll(/\|\s*(PASS|FAIL|NOT ATTEMPTED)\s*\|/g)];if(cells.length<20)throw new Error('only '+cells.length+' resolved verdict cells in §9.5');const pre=nine.slice(nine.indexOf('### 9.2'),nine.indexOf('### 9.3'));if(/\[ \]/.test(pre))throw new Error('§9.2 preflight results still hold empty checkboxes');if(!/pgrep/.test(pre))throw new Error('§9.2 results do not record the single-process check');for(const t of ['midnightMirage','gruvbox_dark','dracula'])if(!body.includes(t))throw new Error('item 1 per-theme row missing for '+t);console.log('run 3 recorded: '+cells.length+' resolved cells')"</automated>
    <human-check>The full run-3 contract executed on real hardware in the derived order, with the preflight recorded and item 1's per-theme sweep completed for the first time in this phase</human-check>
  </verify>
  <done>§9's preflight results and every verdict cell are filled with measured values, item 1's eight per-theme cells are resolved, and any new defect has its own finding ID.</done>
  <resume-signal>Work through the gate with me item by item; say "done" when every sub-check has an answer, or tell me what blocked the run</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reconcile the gate's frontmatter and finding states with what was measured</name>
  <files>.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md § 9.5 (task 1's recorded verdicts — the only input to this task)
    - the same file's frontmatter `items:`, `findings:` and `runs:` lists
    - § 5's no-partial-pass and NOT ATTEMPTED rules
  </read_first>
  <action>
    Bring the frontmatter into agreement with §9.5's body, so the two can never drift:

    - Set each `items:` entry's `status` to `pass`, `fail` or `not_attempted` matching its recorded
      item verdict, and set `items_passed` to the count of `pass`.
    - Set `verdict: pass` **only if `items_passed == 5`**. Any other outcome leaves `verdict: fail`.
      An item containing an unresolved NOT ATTEMPTED sub-check does not pass, whatever the rest of
      its table says, unless that item's own body documents a pass case that permits it and you
      state explicitly which case applies.
    - For each of F-34.10-03, -04, -05 and -06: set `status: closed` with `closed_by` naming both
      the fix plan and the run-3 item that measured it — **only if** that finding's own sub-check
      recorded PASS. If the sub-check recorded FAIL, leave the finding open and update its
      `summary` with what run 3 actually observed. If it recorded NOT ATTEMPTED, leave it open and
      say so. Do not close a finding because its fix landed; three of this phase's own findings
      were "fixed" before a measurement contradicted the fix.
    - Fill the run-3 `runs:` entry's `verdict` and `items_passed`, and add a note naming what
      changed versus run 2 — including anything that regressed, stated as plainly as anything that
      improved.
    - Add a short prose paragraph at the end of §9.5 summarising the run: what closed, what did
      not, and — if item 1's per-theme sweep was completed for the first time — say so explicitly,
      because it has been outstanding since run 1.

    Do not adjust any verdict to make the arithmetic tidier. Do not re-run anything.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md';const s=fs.readFileSync(p,'utf8');const fm=s.slice(0,s.indexOf('\n---',4));if(!/run_number:\s*3/.test(fm))throw new Error('this is not run 3 — frontmatter run_number is not 3');const nine=s.slice(s.indexOf('## 9.'));if(/PENDING — run 3/.test(nine))throw new Error('§9 still holds unfilled verdict slots — task 1 is not complete');const st=[...fm.matchAll(/status:\s*(pass|fail|not_attempted)/g)].map(m=>m[1]);const passed=(fm.match(/items_passed:\s*(\d+)/)||[])[1];if(passed===undefined)throw new Error('items_passed missing');const itemPass=st.slice(0,5).filter(v=>v==='pass').length;if(String(itemPass)!==passed)throw new Error('items_passed ('+passed+') disagrees with the items list ('+itemPass+' pass)');const verdict=(fm.match(/^verdict:\s*(\w+)/m)||[])[1];if(verdict==='pass'&&passed!=='5')throw new Error('verdict: pass set with items_passed='+passed+' — violates the no-partial-pass rule');if(/status:\s*pending/.test(fm))throw new Error('an item is still pending after the run');const runs=fm.slice(fm.indexOf('run: 3'));if(!/verdict:\s*(pass|fail)/.test(runs))throw new Error('the run-3 runs: entry carries no verdict');for(const f of ['F-34.10-03','F-34.10-04','F-34.10-05','F-34.10-06']){const i=fm.indexOf(f);if(i<0)throw new Error('finding '+f+' missing');if(!/status:\s*(open|closed)/.test(fm.slice(i,i+600)))throw new Error(f+' carries no status after the run');}console.log('frontmatter reconciled: '+passed+'/5, verdict '+verdict)"</automated>
  </verify>
  <done>`items:` statuses, `items_passed`, `verdict`, the run-3 `runs:` entry and every F-34.10-03..06 finding state agree with §9.5's recorded body, and no finding is closed without a measured PASS.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS.md to the measured result and state the phase's outcome</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md § 9.5 and its reconciled frontmatter (task 2's output)
    - .planning/REQUIREMENTS.md lines ~369-380 (the traceability table) and ~803-814 (REQ-34.10-04/-05/-06/-08/-09/-16's checklist items and their existing REVERTED / RE-COMPLETED note style)
    - .planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-VERIFICATION.md (the gap list this run either closes or leaves open)
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md` so every Phase 34.10 row states what run 3 measured — both
    the table cell and the checklist tick, kept in sync, since this phase has already shipped a
    drift between the two.

    Per-requirement, ticked only from its own gate item's measured verdict:
    - **REQ-34.10-06** (item 1) — Complete only if item 1 PASSED, which requires the per-theme
      sweep to have been completed, not merely the layout sub-checks. If any theme cell is NOT
      ATTEMPTED, this stays Pending and the note says which cells were unreached.
    - **REQ-34.10-09** (item 3) — Complete only if item 3 PASSED including sub-checks (vi), (vii)
      and the F-34.10-05 background sub-check. Note that run 1 set the precedent that a rendering
      defect in these relocated controls is inside this requirement's scope, not deferred to 34.11.
    - **REQ-34.10-16** — Complete only if `items_passed == 5`. Its own text is that the gate closes
      the phase.
    - **REQ-34.10-04, -05, -08** — currently Complete. If run 3's re-measurement of items 2, 4 or 5
      recorded a FAIL or a NOT ATTEMPTED on a sub-check those requirements depend on, REVERT the
      affected one to Pending with a note citing run 3, exactly as this file already does for
      earlier reverts. A previously-passing requirement is not immune; item 1 itself regressed
      between run 1 and run 2.

    Every change carries an appended note in the file's existing style, citing
    `34.10-LIVE-GATE.md` run 3 and the specific item and sub-check. **Do not tick anything from a
    green test suite** — the `Frontend` jest project has no jsdom and no CSS engine, and this phase
    has already had four requirements ticked from unit tests before their own live checks ran.

    Finally, state the phase outcome plainly in the SUMMARY:
    - If `items_passed == 5`: the phase closes; name `/gsd-verify-work 34.10` as the next command.
    - Otherwise: the phase stays open; say which items failed and which findings remain, and name
      the next command honestly — `/gsd-debug` if the blocker is an undiagnosed defect rather than
      a known gap, `/gsd-plan-phase 34.10 --gaps` if the remaining work is known gap closure. Do
      not present a partial result as progress toward closure; 4/5 did not close this phase twice.

    Do not update `.planning/STATE.md` through `gsd-sdk query state.*` mutation verbs — those
    corrupt the file on this project. If STATE.md needs a line, hand-apply it with Edit and re-read
    the result.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const g=fs.readFileSync('.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-LIVE-GATE.md','utf8');const fm=g.slice(0,g.indexOf('\n---',4));if(!/run_number:\s*3/.test(fm))throw new Error('gate file is not at run 3');const passed=Number((fm.match(/items_passed:\s*(\d+)/)||[])[1]);const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const L=r.split(/\r?\n/);const rows={};for(const n of ['04','05','06','08','09','16']){const row=L.find(l=>new RegExp('^\\\\|\\\\s*REQ-34\\\\.10-'+n+'\\\\s*\\\\|').test(l));if(!row)throw new Error('no traceability row for REQ-34.10-'+n);rows[n]=/Complete/.test(row)?'Complete':'Pending';}if(passed!==5&&rows['16']==='Complete')throw new Error('REQ-34.10-16 marked Complete with items_passed='+passed);let cited=0;for(const n of ['04','05','06','08','09','16']){const item=L.find(l=>new RegExp('^-\\\\s*\\\\[.\\\\]\\\\s*\\\\*\\\\*REQ-34\\\\.10-'+n+'\\\\*\\\\*').test(l));if(!item)throw new Error('no checklist item for REQ-34.10-'+n);const ticked=/^-\s*\[x\]/.test(item);if(ticked!==(rows[n]==='Complete'))throw new Error('REQ-34.10-'+n+' checklist tick disagrees with its traceability row');if(/run 3/.test(item))cited++;}if(cited===0)throw new Error('no Phase 34.10 requirement note cites live gate run 3');console.log('REQUIREMENTS.md reconciled against items_passed='+passed+', '+cited+' notes cite run 3')"</automated>
  </verify>
  <done>Every Phase 34.10 requirement's table cell and checklist tick agree with each other and with run 3's measured items, at least one note cites run 3, REQ-34.10-16 is Complete only at 5/5, and the SUMMARY states the phase outcome and the honest next command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human operator report → recorded verdict | An unscoped or paraphrased report is indistinguishable from a measurement once written into the file |
| gate body → gate frontmatter | The two can drift, and a reader who checks only one gets a different answer |
| gate verdicts → REQUIREMENTS.md | A requirement ticked from a green suite outlives the run that should have contradicted it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.10-100 | Spoofing | a half-observed run made to look measured by a second concurrent instance | mitigate | P3's `pgrep` result is written literally into §9.2 and asserted by task 1's automated check; run 2's entirely unfilled preflight is the recorded counter-example |
| T-34.10-101 | Repudiation | a per-theme sweep inferred from one general "looks fine" | mitigate | Task 1 requires all eight item-1 cells resolved and instructs the executor to record NOT ATTEMPTED for any theme the operator did not name; the automated check asserts all three scored theme rows exist and no `PENDING` remains |
| T-34.10-102 | Tampering | frontmatter arithmetic massaged to reach 5/5 | mitigate | Task 2's check recomputes `items_passed` from the `items:` list and rejects `verdict: pass` below 5 |
| T-34.10-103 | Repudiation | a finding closed because its fix landed rather than because it was measured | mitigate | Task 2 permits `status: closed` only on a measured PASS for that finding's own sub-check, and requires `closed_by` to name the run-3 item |
| T-34.10-104 | Elevation of privilege | a requirement ticked from a green test suite | mitigate | Task 3 forbids it explicitly and its check cross-references `items_passed` from the gate file; the table cell and the checklist tick must agree |
| T-34.10-105 | Denial of service | `gsd-sdk query state.*` mutation corrupting STATE.md | mitigate | Task 3 forbids the mutation verbs and requires hand-applied Edit plus a re-read |
| T-34.10-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; no install task exists in this plan |
</threat_model>

<verification>
- `pnpm jest --runInBand --silent --forceExit` and `pnpm codecheck` are run as P1/P2 and their
  observed values are written into §9.2 alongside the expected values. A green suite proves nothing
  about any of §9's five items — that is their entire reason to exist.
- `git diff --name-only` shows exactly two paths: `34.10-LIVE-GATE.md` and `.planning/REQUIREMENTS.md`.
- §0 through §8 of the gate file are unmodified; only §9 and the frontmatter change.
- No user-facing string is introduced; `meta/i18nGateScope.json` stays clean.
</verification>

<success_criteria>
- §9.2's preflight results are filled, including the literal `pgrep` outcome
- No `PENDING — run 3` slot survives; every cell is `PASS`, `FAIL` or `NOT ATTEMPTED` with a reason where required
- Item 1's four theme rows carry resolved seam and idle-ring verdicts — the first completed sweep in this phase, or an explicit record of why not
- Frontmatter `items:`, `items_passed` and `verdict` agree with the body, and `verdict: pass` appears only at 5/5
- F-34.10-03..06 are closed only where measured, and any new defect has its own finding ID
- REQUIREMENTS.md's table cells and checklist ticks agree with each other and with the run
- The SUMMARY states whether phase 34.10 closes and names the honest next command
</success_criteria>

<output>
Create `.planning/phases/34.10-navigation-shell-horizontal-card-tabs-replace-the-sidebar/34.10-22-SUMMARY.md` when done
</output>
