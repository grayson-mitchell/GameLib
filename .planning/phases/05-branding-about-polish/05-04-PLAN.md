---
phase: 05-branding-about-polish
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - scripts/verify-branding.cjs
autonomous: true
requirements: [BRAND-02, BRAND-03, APP-01]

must_haves:
  truths:
    - "verify-branding.cjs asserts the Phase 5 rebranded surfaces (tray, log paths, install/config paths, presence, changelog) in a new Section 5"
    - "The script still asserts the legacy join(configFolder, 'heroic') migration path is UNCHANGED"
    - "node scripts/verify-branding.cjs exits 0 across all sections"
  artifacts:
    - path: "scripts/verify-branding.cjs"
      provides: "Phase 5 branding verification section"
      contains: "Phase 5"
  key_links:
    - from: "scripts/verify-branding.cjs Section 5"
      to: "public/changelog.json"
      via: "fs.existsSync check"
      pattern: "changelog.json"
---

<objective>
Extend the existing `scripts/verify-branding.cjs` (from Phase 4) with a Section 5 block asserting all Phase 5 rebranded surfaces, and run it as the phase branding gate.

Purpose: Give BRAND-02/BRAND-03/APP-01 a durable machine check that regressions (or a future upstream merge) would catch. This plan runs in Wave 2 because its assertions depend on the completed edits from plans 05-01 and 05-02.
Output: An extended verification script that exits 0 against the rebranded codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-branding-about-polish/05-CONTEXT.md
@.planning/phases/05-branding-about-polish/05-RESEARCH.md
@.planning/phases/05-branding-about-polish/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 5 branding checks + run the gate</name>
  <files>scripts/verify-branding.cjs</files>
  <read_first>
    - scripts/verify-branding.cjs (L21-29 check() helper; L86-96 existing readFileSync+includes pattern; note utilsTs at L86 and pathsTs at L92 are already loaded; the summary block near L105 that the new section precedes; L93-96 legacy join(configFolder,'heroic') UNCHANGED assertion)
    - .planning/phases/05-branding-about-polish/05-PATTERNS.md (verify-branding.cjs Section 5 block — exact checks to append)
    - .planning/phases/05-branding-about-polish/05-RESEARCH.md (verify-branding.cjs Extension section; Pitfall #3)
  </read_first>
  <action>
    Append a Section 5 block before the final summary block, following the existing `check(name, result)` and `fs.readFileSync(...)` + `.includes(...)` pattern. REUSE the already-loaded `utilsTs` (L86) and `pathsTs` (L92) variables — do NOT re-read those two files. Add these assertions (per PATTERNS Section 5 block):
    - tray_icon.ts: includes `setToolTip('GameLib')` AND NOT `setToolTip('Heroic')`
    - logger/paths.ts: NOT includes `'Heroic'` AND NOT `'Heroic Games Launcher'`
    - constants/paths.ts (pathsTs): includes `join(userHome, 'Games', 'GameLib')` AND NOT `'Games', 'Heroic'`
    - config.ts: includes `wineCrossoverBottle: 'GameLib'`
    - presence.ts: NOT includes `'Heroic Games Launcher'`
    - utilsTs: NOT includes `'via Heroic on '` AND includes the GameLib Discord version template literal `` `GameLib ${app.getVersion()}` ``
    - utilsTs: NOT includes the D-05 dialog/log strings `'Heroic will maybe not work probably!'` AND NOT `restart Heroic.` (Rosetta message, L1349) AND NOT `? 'Heroic' :` (writeConfig template, L1638) — these three utils.ts D-05 strings otherwise have no automated gate
    - public/changelog.json: `fs.existsSync` true
    - utilsTs: includes `readFileSync` AND NOT `GITHUB_API/tags/`
    Do NOT weaken or remove the existing L93-96 check that asserts `pathsTs` STILL contains `join(configFolder, 'heroic')` (legacy migration — Pitfall #3). Then run the script and ensure all sections pass.
  </action>
  <verify>
    <automated>node scripts/verify-branding.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-branding.cjs` exits 0 with the new Section 5 checks reported PASS
    - The script output still shows the legacy `join(configFolder, 'heroic') UNCHANGED` check passing
    - Section 5 covers tray, logger paths, install path, config bottle, presence, Discord version, the three utils.ts D-05 dialog/log strings, and changelog existence + local-read
  </acceptance_criteria>
  <done>verify-branding.cjs asserts every Phase 5 rebranded surface and exits 0; legacy migration assertion preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Build-time verification script; runs against source files, no runtime path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | (n/a) | scripts/verify-branding.cjs | accept | Dev-time verification script reading local source with fs.readFileSync; no external input, no runtime deployment. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. Nothing to verify. |
</threat_model>

<verification>
- `node scripts/verify-branding.cjs` exits 0 (phase branding gate)
- Phase gate: `pnpm codecheck` + `pnpm test` + `node scripts/verify-branding.cjs` all green before `/gsd:verify-work`
</verification>

<success_criteria>
- Section 5 asserts all Phase 5 surfaces and the full script passes
- Legacy migration assertion untouched
</success_criteria>

<output>
Create `.planning/phases/05-branding-about-polish/05-04-SUMMARY.md` when done
</output>
