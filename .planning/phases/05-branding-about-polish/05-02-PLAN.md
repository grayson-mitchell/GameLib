---
phase: 05-branding-about-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/updater.ts
  - src/backend/launcher.ts
  - src/backend/storeManagers/gog/setup.ts
  - src/backend/storeManagers/zoom/games.ts
  - src/backend/constants/paths.ts
  - src/backend/logger/paths.ts
  - src/backend/config.ts
autonomous: true
requirements: [BRAND-03]

must_haves:
  truths:
    - "The restart dialogs, Mangohud/Rosetta errors, and GOG/Zoom error strings read GameLib, not Heroic (D-05)"
    - "New installs default to ~/Games/GameLib and log to a GameLib-labelled directory — clean cutover, no migration (D-08)"
    - "The default CrossOver bottle name is GameLib (D-08)"
  artifacts:
    - path: "src/backend/constants/paths.ts"
      provides: "GameLib default install path and wine prefix dir"
      contains: "join(userHome, 'Games', 'GameLib')"
    - path: "src/backend/logger/paths.ts"
      provides: "GameLib-labelled log directories and log filename"
      contains: "GameLib"
    - path: "src/backend/config.ts"
      provides: "GameLib default CrossOver bottle"
      contains: "wineCrossoverBottle: 'GameLib'"
  key_links:
    - from: "src/backend/config.ts defaultInstallPath"
      to: "heroicInstallPath constant"
      via: "config default references the paths.ts constant"
      pattern: "defaultInstallPath: heroicInstallPath"
---

<objective>
Rebrand the remaining user-facing backend strings (D-05) and all three filesystem path constants (D-08) that live outside `utils.ts`.

Purpose: Complete BRAND-03 across the restart dialogs, store-manager errors, log directories, install path, and CrossOver bottle default — via clean cutover, no migration (D-08 rationale: no published-GameLib userbase to migrate).
Output: Rebranded strings and path constants across 7 backend files. Every edit is a single-string replacement with an exact known location.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-branding-about-polish/05-CONTEXT.md
@.planning/phases/05-branding-about-polish/05-RESEARCH.md
@.planning/phases/05-branding-about-polish/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand remaining user-facing backend strings (D-05)</name>
  <files>src/backend/updater.ts, src/backend/launcher.ts, src/backend/storeManagers/gog/setup.ts, src/backend/storeManagers/zoom/games.ts</files>
  <read_first>
    - src/backend/updater.ts (L65 restart dialog)
    - src/backend/launcher.ts (L594 Mangohud flatpak error)
    - src/backend/storeManagers/gog/setup.ts (L237 'to try again restart Heroic and')
    - src/backend/storeManagers/zoom/games.ts (L510 'Heroic could not find the executable...')
    - .planning/phases/05-branding-about-polish/05-RESEARCH.md (D-05 string inventory table)
  </read_first>
  <action>
    Apply the four D-05 single-string replacements exactly per the RESEARCH inventory:
    - `updater.ts:L65` `'Do you want to restart Heroic now?'` → `'Do you want to restart GameLib now?'`
    - `launcher.ts:L594` `...restart Heroic.` (Mangohud flatpak error) → `...restart GameLib.`
    - `gog/setup.ts:L237` `'to try again restart Heroic and'` → `'to try again restart GameLib and'`
    - `zoom/games.ts:L510` `'Heroic could not find the executable...'` → `'GameLib could not find the executable...'`
    Do not touch any internal identifiers, i18n KEY paths, or third-party contracts in these files.
  </action>
  <verify>
    <automated>grep -rn "restart Heroic\|Heroic could not find" src/backend/updater.ts src/backend/launcher.ts src/backend/storeManagers/gog/setup.ts src/backend/storeManagers/zoom/games.ts; test $(grep -rc "restart Heroic\|Heroic could not find" src/backend/updater.ts src/backend/launcher.ts src/backend/storeManagers/gog/setup.ts src/backend/storeManagers/zoom/games.ts | grep -v ':0$' | wc -l) -eq 0 && pnpm codecheck</automated>
  </verify>
  <acceptance_criteria>
    - None of the four files contain "restart Heroic" or "Heroic could not find"
    - The four target strings now read GameLib
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
  <done>All four D-05 dialog/error strings outside utils.ts read GameLib; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rebrand filesystem path constants via clean cutover (D-08)</name>
  <files>src/backend/constants/paths.ts, src/backend/logger/paths.ts, src/backend/config.ts</files>
  <read_first>
    - src/backend/constants/paths.ts (L52-58 heroicInstallPath + defaultWinePrefixDir; L33 legacyAppFolder DO NOT TOUCH; L63-67 publicDir)
    - src/backend/logger/paths.ts (L16 Windows, L19 macOS, L23 Linux/XDG, L49 relativeFilePath)
    - src/backend/config.ts (L355 wineCrossoverBottle; L341/L344 constant references — do not change)
    - .planning/phases/05-branding-about-polish/05-RESEARCH.md (D-08 string inventory; downstream reads confirm no migration; Pitfalls #1 and #3)
    - .planning/phases/05-branding-about-polish/05-PATTERNS.md (paths.ts / logger/paths.ts / config.ts patterns)
  </read_first>
  <action>
    D-08 clean cutover, no migration. Apply exactly per RESEARCH inventory:
    - `constants/paths.ts`: `heroicInstallPath = join(userHome, 'Games', 'Heroic')` → `'GameLib'`; AND `defaultWinePrefixDir = join(userHome, 'Games', 'Heroic', 'Prefixes')` → `'GameLib'`. Per Pitfall #1 these are two independent hardcoded constants — BOTH must change. Grep `'Games', 'Heroic'` in this file finds two matches; both flip to GameLib.
    - Per Pitfall #3, DO NOT change `paths.ts` `legacyAppFolder = join(configFolder, 'heroic')` — the verify-branding script asserts it is UNCHANGED (first-launch migration for upgrading Heroic users).
    - `logger/paths.ts`: L16 `join(localAppData, 'Heroic', 'logs')` → `'GameLib'`; L19 `join(homedir(), 'Library', 'Logs', 'Heroic Games Launcher')` → `join(homedir(), 'Library', 'Logs', 'GameLib')`; L23 `join(stateHome, 'Heroic', 'logs')` → `'GameLib'`; L49 `relativeFilePath = 'heroic'` → `'gamelib'` (per RESEARCH open-question #2, in-scope for D-08 completeness — the main app log filename becomes `gamelib.log`).
    - `config.ts:L355`: `wineCrossoverBottle: 'Heroic'` → `wineCrossoverBottle: 'GameLib'`. Leave L341 `defaultInstallPath: heroicInstallPath` and L344 references untouched — they use the constants, which now resolve to GameLib.
    No migration code — existing data at old paths is not moved (D-08).
  </action>
  <verify>
    <automated>test $(grep -c "'Games', 'Heroic'" src/backend/constants/paths.ts) -eq 0 && grep -q "join(configFolder, 'heroic')" src/backend/constants/paths.ts && test $(grep -c "'Heroic'" src/backend/logger/paths.ts) -eq 0 && grep -q "wineCrossoverBottle: 'GameLib'" src/backend/config.ts && pnpm codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `constants/paths.ts` has zero `'Games', 'Heroic'` matches and STILL contains `join(configFolder, 'heroic')` (legacy migration preserved)
    - `logger/paths.ts` contains no quoted `'Heroic'` string (paths + relativeFilePath rebranded)
    - `config.ts` contains `wineCrossoverBottle: 'GameLib'`
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
  <done>New installs default to ~/Games/GameLib, logs write to a GameLib directory as gamelib.log, CrossOver default bottle is GameLib; legacy migration path preserved; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes only string literals and path constants consumed exclusively by backend code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | D-08 clean cutover of log/install/prefix paths | accept | No published-GameLib userbase to migrate (D-08 rationale). Old data at prior paths is left in place, not deleted; new defaults apply to new installs only. Legacy Heroic-config migration path is explicitly preserved (Pitfall #3). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new npm packages). Nothing to verify. |
</threat_model>

<verification>
- `pnpm codecheck` exits 0 (primary gate)
- No "restart Heroic" / "Heroic could not find" strings remain in the four D-05 files
- `constants/paths.ts` has no `'Games', 'Heroic'` and retains `join(configFolder, 'heroic')`
- `logger/paths.ts` has no quoted `'Heroic'`; `config.ts` bottle default is GameLib
</verification>

<success_criteria>
- All D-05 strings outside utils.ts and all three D-08 path constants read GameLib
- Legacy migration path untouched; tsc clean
</success_criteria>

<output>
Create `.planning/phases/05-branding-about-polish/05-02-SUMMARY.md` when done
</output>
