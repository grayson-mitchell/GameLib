---
phase: 05-branding-about-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .vscode/launch.json
autonomous: true
requirements: [BRAND-04]

must_haves:
  truths:
    - "The README intro is typo-free (derivative, Differentiators, macOS, GameLib capitalization)"
    - "Instructional in-README Heroic mentions read GameLib (build/run/debug steps)"
    - "The fork-of-Heroic attribution and upstream links are retained"
    - "The VS Code Run & Debug config name reads 'Launch GameLib (HMR & HR)' and matches the README reference"
  artifacts:
    - path: "README.md"
      provides: "Accurate GameLib-branded docs with fork attribution"
      contains: "GameLib"
    - path: ".vscode/launch.json"
      provides: "GameLib-named launch configuration"
      contains: "Launch GameLib (HMR & HR)"
  key_links:
    - from: "README.md L220 task reference"
      to: ".vscode/launch.json config name"
      via: "shared launch-config name string"
      pattern: "Launch GameLib \\(HMR"
---

<objective>
README accuracy + fork-clarity pass (D-09/D-10) and the matching VS Code launch-config rename.

Purpose: Complete BRAND-04 — fix the four typos, rebrand instructional "Heroic" mentions to GameLib, keep the fork attribution and upstream/sponsor links, and update `.vscode/launch.json` (which the README references by name) so the Run & Debug panel matches.
Output: A typo-free, GameLib-branded README that still credits Heroic as upstream, plus a synchronized launch-config name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-branding-about-polish/05-CONTEXT.md
@.planning/phases/05-branding-about-polish/05-RESEARCH.md
@.planning/phases/05-branding-about-polish/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README typo fixes + instructional rebrand (D-09/D-10)</name>
  <files>README.md</files>
  <read_first>
    - README.md (L3 intro typos; L5 unclosed paren; L14 gameLib; L56/L76/L87/L214/L217/L220 instructional; L77/L131/L228-234 attribution to KEEP)
    - .planning/phases/05-branding-about-polish/05-RESEARCH.md (README Changes tables: typo fixes, instructional rebrand, keep-as-attribution)
    - .planning/phases/05-branding-about-polish/05-PATTERNS.md (README section: exact current→fix table)
  </read_first>
  <action>
    D-09 typo fixes: L3 `derivitive` → `derivative`; L3 `Differntiators` → `Differentiators`; L5 `(Playing Games on MacOS` → `(Playing Games on macOS)` (close the paren, fix MacOS→macOS); L14 `gameLib is built with` → `GameLib is built with`.
    D-10 instructional rebrand: L56 `from Heroic` → `from GameLib`; L76 `Heroic will still _work_ on most distros` → `GameLib will still _work_...`; L87 `Heroic was translated to almost 40 different languages` → `GameLib has been translated to almost 40 different languages`; L214 `build Heroic` → `build GameLib`; L217 heading `testing/debugging Heroic on your own system` → `...GameLib...`; L220 `Launch Heroic (HMR & HR)` → `Launch GameLib (HMR & HR)` AND `Heroic will start up after a short while` → `GameLib will start up after a short while`.
    KEEP as attribution (D-10 exception — do NOT change): L3 `derivative of Heroic Games Launcher`, L77 the Heroic Discord link, L131 the Weblate link, L228-234 Weblate/Signpath sponsor mentions. Do not perform a blanket Heroic→GameLib sweep; only the enumerated instructional lines change.
  </action>
  <verify>
    <automated>! grep -qE "derivitive|Differntiators|gameLib is built|Playing Games on MacOS" README.md && ! grep -qE "restart Heroic|build Heroic|from Heroic|Launch Heroic \(HMR" README.md && grep -q "derivative of Heroic Games Launcher" README.md && grep -q "Launch GameLib (HMR & HR)" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains none of: `derivitive`, `Differntiators`, `gameLib is built`, `Playing Games on MacOS`
    - README contains no instructional `build Heroic` / `from Heroic` / `Launch Heroic (HMR`
    - README still contains `derivative of Heroic Games Launcher` (attribution kept)
    - README contains `Launch GameLib (HMR & HR)`
  </acceptance_criteria>
  <done>README intro is typo-free, instructional mentions say GameLib, fork attribution + upstream/sponsor links retained.</done>
</task>

<task type="auto">
  <name>Task 2: Rename VS Code launch config to match README (D-10)</name>
  <files>.vscode/launch.json</files>
  <read_first>
    - .vscode/launch.json (L5 configuration name; L30 compounds[].configurations reference)
    - .planning/phases/05-branding-about-polish/05-PATTERNS.md (.vscode/launch.json section — both occurrences must change)
    - .planning/phases/05-branding-about-polish/05-RESEARCH.md (Pitfall #4: launch.json name must match README L220)
  </read_first>
  <action>
    Change BOTH occurrences of `Launch Heroic (HMR & HR)` → `Launch GameLib (HMR & HR)`: the `name` field (L5) AND the reference inside the `compounds[0].configurations` array (L30). Per Pitfall #4, both must change or the Run & Debug panel shows a stale name that mismatches README L220.
  </action>
  <verify>
    <automated>! grep -q "Launch Heroic (HMR & HR)" .vscode/launch.json && test $(grep -c "Launch GameLib (HMR & HR)" .vscode/launch.json) -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `.vscode/launch.json` contains no `Launch Heroic (HMR & HR)`
    - `.vscode/launch.json` contains exactly two occurrences of `Launch GameLib (HMR & HR)` (name + compounds reference)
  </acceptance_criteria>
  <done>VS Code Run & Debug config name reads GameLib in both the definition and the compound reference, matching README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation and editor-config changes only; no runtime code path affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | (n/a) | README.md, .vscode/launch.json | accept | Static docs/editor config; no trust boundary, no runtime execution, no threat surface. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. Nothing to verify. |
</threat_model>

<verification>
- Automated grep gates in each task pass
- Manual (phase gate, per VALIDATION Manual-Only): read README top-to-bottom — intro typo-free, fork-of-Heroic attribution + upstream links retained, instructional steps say GameLib
</verification>

<success_criteria>
- README typos fixed, instructional mentions rebranded, attribution/links kept
- launch.json name matches README reference in both places
</success_criteria>

<output>
Create `.planning/phases/05-branding-about-polish/05-03-SUMMARY.md` when done
</output>
