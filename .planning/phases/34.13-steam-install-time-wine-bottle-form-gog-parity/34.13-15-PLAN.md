---
phase: 34.13-steam-install-time-wine-bottle-form-gog-parity
plan: 15
type: execute
wave: 3
depends_on: ['34.13-08']
files_modified:
  - src/frontend/helpers/steamInstallOptionsEntry.ts
  - src/frontend/helpers/__tests__/steamInstallOptionsEntry.test.ts
  - src/frontend/screens/Library/components/GameCard/index.tsx
  - src/frontend/screens/Game/GameSubMenu/index.tsx
  - src/frontend/screens/ConsoleMode/InstallOverlay/consoleSteamTarget.ts
  - src/frontend/screens/ConsoleMode/InstallOverlay/__tests__/consoleSteamTarget.test.ts
  - src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx
  - src/frontend/screens/ConsoleMode/InstallOverlay/index.scss
autonomous: true
requirements: [D-24, D-27, D-28, D-29]
user_setup: []

must_haves:
  truths:
    - "D-27 row 3 ships as a CONTEXT-MENU ITEM, not a caret. `GameCard` gets one new entry in its existing `items: Item[]` array (`GameCard/index.tsx:338-393`), beside the untouched plain install `Item` (`:368-374`). RESEARCH Q4 and plan 08 both verified the `:322` line D-27 cites is dead code inside `handleEdit()` behind `if (isSideloaded)` — nothing at that line is edited, and an executor who edits it has edited the Edit-Game action by mistake."
    - "D-27 row 5 ships as a NEW CONTROL, not a relabel. RESEARCH Q4's full-file read and plan 08's `<call_site_map>` both found `GameSubMenu` has NO install affordance for an uninstalled game of ANY runner: `:176` is dead code behind `if (isSideloaded)`, and the file's entire button cluster sits inside `{isInstalled && …}` (`:312`). There is nothing to relabel. A future reader who 'corrects' this back to a relabel will be relabelling a control that does not exist."
    - "Reachability of the new GameSubMenu control is PROVEN, not assumed: `DotsMenu.tsx:41-61` mounts `GameSubMenu` with `isInstalled={is_installed}` and NO installed-gate of its own, and `GamePage/index.tsx:482` mounts `DotsMenu` unconditionally inside the `title ? …` branch. So the submenu renders for an uninstalled game and a `!isInstalled`-gated child is reachable. Without this chain the whole item would be dead code — the exact failure mode D-27 row 5 already walked into once."
    - "D-28 is enforced by a SINGLE evaluation site, not by two copies of a conjunct. `runner === 'steam'` is evaluated for these two entries in exactly one place — `steamInstallOptionsEntry.ts` — and both `.tsx` files call it rather than inlining the test. Proven by a unit test parameterised over `gog`/`legendary`/`nile`/`sideload` across every state combination (both predicates false for all of them) PLUS comment-stripped source gates asserting each `.tsx` calls the shared predicate."
    - "GOG/Epic/Amazon are proven unchanged by ADDITION, not by inspection: every existing entry is untouched. Source gates assert GameCard's plain install `Item` still carries `label: t('button.install')` and its exact `show:` expression, and that GameSubMenu's `{isInstalled && (` block opener is still present verbatim. The new affordances are pure additions whose visibility predicate returns false for every non-Steam runner."
    - "D-29: Console Mode gets D-24's check with ONE definition of the check, not a second one. `consoleSteamTarget.ts` imports 34.13-08's `resolveQuickInstallTarget`, `evaluateQuickInstallTarget` and `LOW_SPACE_FLOOR_BYTES` and adds only IPC orchestration. It must NOT call `startSteamQuickInstall`: that function's degrade branch flips `useInstallGameModal.isOpen`, which would open the DESKTOP options dialog underneath a full-screen gamepad overlay — the exact thing D-29 forbids."
    - "D-29's open question is RESOLVED: on a failed check Console Mode shows the failure IN PLACE, in the overlay's own existing Steam branch. The `consoleModal` that today reads 'Opening Steam to install…' + game title becomes a `--status-danger` failure title + a reason line + a single `consoleChip` dismiss button, built entirely from classes already in this file. No platform row, no wine row, no library picker, no install control — it is a terminal message, not an options surface."
    - "The 1500ms auto-dismiss timer is armed ONLY in the ok branch. Today it is armed at mount, in parallel with `install()`. If it stayed armed the failure message would vanish after 1.5 seconds — a silent failure, which is precisely what D-29 exists to prevent. Asserted by a spec that observes `setTimeout` NOT called on the blocked path, and by the repo's own ledgered lesson that a timer armed outside the branch that owns it is a no-op waiting to happen."
    - "D-24 fail-open carries over verbatim: an UNKNOWN verdict is not a failed one. A rejected `listSteamLibraryTargets` yields `[]` (delegated `steam://install`, no probe) and a rejected `checkDiskSpace` yields `undefined` (install proceeds). Console Mode is a TV/couch surface with no keyboard — a probe misbehaving must never be able to wedge the only install path it has. Discriminator spec: rejected `checkDiskSpace` → verdict ok."
    - "The A button cannot fire the install the check just refused. `focused` defaults to `'install'` and `useGamepadButtonPress(BTN_ACTION, …)` (`:264-267`) plus the Enter/Space keydown branch (`:250-258`) both call `installGame()` with no runner test — so today, and in any naive implementation of D-29, one A press bypasses the entire check. `resolveConsoleActionIntent()` is a pure function both input paths route through, returning `'dismiss'` for `runner === 'steam'` unconditionally. Unit-tested, because a source gate alone cannot prove a handler routes through it."
    - "No filesystem path reaches a Console Mode screen. `SteamQuickInstallDegrade.libraryPath` is carried in state (it is what plan 08 produces) but the copy interpolates NOTHING — the reason strings are static. Source gate: `libraryPath` appears zero times in the overlay's comment-stripped source. A couch/TV/streamed surface is the worst place in the app to print a home-directory path."
    - "This plan mints NO catalog keys itself and writes NO catalog file. `34.13-13` is the phase's single catalog writer; the four keys are listed for it to absorb, one of which (`steam.install.withOptionsLabel`) is SHARED with plan 08 and must not be minted twice."
    - "VACUITY BOUNDARY, stated not assumed: there is no jsdom in this repo, and all three files this plan modifies import a stylesheet at line 1/line 5, so NONE of them can be imported into a Jest test at all — not even by plan 08's element-graph idiom. Every automated assertion here is either a pure-function spec or a comment-stripped source-text gate. A green suite is NOT evidence that any menu item renders, that a gamepad reaches it, or that the Console Mode failure state is legible. That boundary belongs to 34.13-13's manual gate and is enumerated in `<verification>`."
  artifacts:
    - path: "src/frontend/helpers/steamInstallOptionsEntry.ts"
      provides: "The single evaluation site of D-28 for the two menu surfaces — showSteamCardInstallOptions and showSteamSubMenuInstallOptions, both pure, both Steam-gated"
      contains: "showSteamCardInstallOptions"
      min_lines: 40
    - path: "src/frontend/helpers/__tests__/steamInstallOptionsEntry.test.ts"
      provides: "Unit specs for both predicates parameterised over every runner, plus comment-stripped source gates proving both .tsx files call the shared predicate and left every pre-existing entry untouched"
      contains: "stripSourceComments"
      min_lines: 120
    - path: "src/frontend/screens/Library/components/GameCard/index.tsx"
      provides: "D-27 row 3 — one new 'Install with options…' Item in the existing items array, Steam-only, calling openSteamInstallOptions directly"
      contains: "showSteamCardInstallOptions"
    - path: "src/frontend/screens/Game/GameSubMenu/index.tsx"
      provides: "D-27 row 5 — a NEW not-installed Steam-only 'Install with options…' button in a new block preceding the file's {isInstalled && …} cluster"
      contains: "showSteamSubMenuInstallOptions"
    - path: "src/frontend/screens/ConsoleMode/InstallOverlay/consoleSteamTarget.ts"
      provides: "D-29 — the Console Mode half of D-24: probeSteamQuickInstallTarget (composing 34.13-08's pure helpers), steamBlockedMessageKey, and resolveConsoleActionIntent (the A-button bypass gate)"
      contains: "resolveConsoleActionIntent"
      min_lines: 70
    - path: "src/frontend/screens/ConsoleMode/InstallOverlay/__tests__/consoleSteamTarget.test.ts"
      provides: "The D-29 verdict matrix with its fail-open discriminator, the A-button intent matrix, and source gates over the overlay (timer placement, no install on the blocked path, no libraryPath in copy, non-Steam path untouched)"
      contains: "DISCRIMINATOR"
      min_lines: 150
    - path: "src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx"
      provides: "The gated Steam branch — probe before install, in-place failure state, timer armed only on the ok path, both input handlers routed through resolveConsoleActionIntent"
      contains: "probeSteamQuickInstallTarget"
  key_links:
    - from: "src/frontend/screens/Library/components/GameCard/index.tsx"
      to: "src/frontend/state/InstallGameModal.ts openSteamInstallOptions()"
      via: "a direct store import added to the file's EXISTING import from 'frontend/state/InstallGameModal' (:58) — RESEARCH Q4 offered prop-threading through GamesList/Library/RecentlyPlayed as the alternative; it is not needed"
      pattern: "openSteamInstallOptions"
    - from: "src/frontend/screens/Game/GameSubMenu/index.tsx"
      to: "src/frontend/state/InstallGameModal.ts openSteamInstallOptions()"
      via: "the same existing import (:15); gameInfo and appName are already props"
      pattern: "openSteamInstallOptions"
    - from: "src/frontend/screens/ConsoleMode/InstallOverlay/consoleSteamTarget.ts"
      to: "src/frontend/state/InstallGameModal.ts evaluateQuickInstallTarget()"
      via: "a pure-helper import — the reason there is ONE definition of D-24's check across two degrade shapes"
      pattern: "evaluateQuickInstallTarget"
    - from: "src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx"
      to: "window.api.install (via frontend/helpers install())"
      via: "the UNCHANGED existing call, now preceded by the D-29 gate — the literal stays here, keeping plan 08's two-line platformToInstall count true"
      pattern: "installPath: 'default'"
---

<objective>
Take the three pieces of locked scope plan 08 froze out and named in its `<handoffs>`, and give them
an owner: the two "Install with options…" menu entries D-27 asks for on `GameCard` and `GameSubMenu`,
and D-29's extension of D-24's local check to Console Mode.

Plan 08 built both doors (`startSteamQuickInstall`, `openSteamInstallOptions`) and routed every
entry point it owned. It deliberately did not touch `GameCard/index.tsx`, `GameSubMenu/index.tsx` or
`ConsoleMode/InstallOverlay/index.tsx`. **No other plan in the set lists any of those three files.**
Without this plan they are silently dropped and the phase closes believing it shipped D-27 in full.

Purpose:
- **D-27 row 3** — `GameCard`'s quick half already works via plan 08 (site 4, zero edits). Its
  options half does not exist. One new `ContextMenu` `Item`.
- **D-27 row 5** — `GameSubMenu` has no install affordance at all for an uninstalled game. One new
  button. This is a NEW CONTROL, not the relabel D-27's table describes.
- **D-29** — Console Mode is a ninth install entry point outside D-27's map. D-23 holds there by
  accident (containment); D-24 does not hold there at all. This plan makes it hold, in the only shape
  a gamepad UI with no approved options contract can support: an in-place terminal failure message.

Output: one shared pure predicate module and its test, two additive menu entries, one Console Mode
decision module and its test, one gated overlay, two SCSS rules. No IPC channels, no dependencies,
no catalog writes.

## ⚠ Three things a reader will get wrong — read before writing a line of code

1. **`GameCard/index.tsx:322` and `GameSubMenu/index.tsx:176` are NOT the lines you edit.** Both are
   inside `handleEdit()` behind `if (isSideloaded)`. D-27 cites them; RESEARCH Q4 and plan 08's
   `<call_site_map>` both falsified them. Editing either one edits the Edit-Game action.
2. **`GameSubMenu`'s entry is a NEW control.** D-27 says "item relabels to 'Install with options…'".
   There is no item. Say it in the code comment you write, because this correction has now had to be
   made three times (RESEARCH Q4, UI-SPEC §GameSubMenu, plan 08 `<handoffs>` item 2).
3. **Console Mode must NOT call `startSteamQuickInstall`.** Its degrade branch opens the desktop
   `InstallModal` by flipping `isOpen`. Under a full-screen gamepad overlay that is either invisible
   or a second modal the user cannot navigate. D-29 forbids an options path in Console Mode; compose
   the PURE helpers instead.

**Scope fences (all hard):**
- Do NOT edit `src/frontend/state/InstallGameModal.ts`. Plan 08 is its only editor and its exports
  are FINAL. This plan consumes them and adds none.
- Do NOT edit `public/locales/en/gamelib.json`. Ship every new string as an inline
  `t('gamelib:…', 'English default')` default (the `WineVersionSelector.tsx:28,33` precedent).
  34.13-13 is the phase's single catalog writer; the keys are listed in `<catalog_keys>` for it.
- Do NOT edit `MainButton.tsx`, `GamePage/index.tsx`, anything under `InstallModal/`, anything under
  `src/backend/`, `src/common/`, `package.json` or `pnpm-lock.yaml`.
- Do NOT modify `ConsoleMode/InstallOverlay/index.tsx`'s non-Steam branch, `installGame()`, or the
  existing `install({… installPath: 'default' …})` call's arguments. Plan 08's structural gate
  (`platformToInstall: 'Windows'` appears exactly twice in non-test `src/frontend`) counts this file
  as one of the two survivors — changing that literal breaks a gate two plans rely on.
- Do NOT add a `disabled` prop to any shared primitive, and do NOT touch `UI/Dropdown/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-CONTEXT.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UI-SPEC.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VALIDATION.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-RESEARCH.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-08-PLAN.md

Knowledge graph is authoritative for orientation — run `graphify query "…"` before broad source
exploration. It is thin on these three surfaces (Console Mode resolves to a 5-node community;
`GameSubMenu` has no node at all), so every `file:line` anchor below was verified by direct read on
2026-08-15 and is the primary reference.

**`34.13-PATTERNS.md` carries a ⚠ banner — read it first.** Its `AlwaysShowSteamInstallForm` section
is a retired trap (D-26). Its banner explicitly states that the GameCard context-menu item and the
GameSubMenu entry have **no pattern assignment**, and Console Mode is not in the map at all (it
predates D-29). The authoritative sources for these three surfaces are `34.13-UI-SPEC.md`
§"Split-Button Control Contract" (§GameCard, §GameSubMenu) and this plan.

<interfaces>
<!-- Extracted from the codebase 2026-08-15. Use these directly — no exploration needed. -->

**Consumed from 34.13-08 (`src/frontend/state/InstallGameModal.ts`) — READ ONLY, do not edit:**
- `openSteamInstallOptions(appName: string, gameInfo: GameInfo, degrade?: SteamQuickInstallDegrade)`
  — synchronous, zero IPC. Both new menu entries call it with two arguments (a deliberate click
  carries no degrade — plan 08 spec C3).
- `resolveQuickInstallTarget(libraries) => target | undefined` — `find(isPrimary) ?? libraries[0]`.
- `evaluateQuickInstallTarget(target, disk, libraryCount) => { ok: true } | { ok: false; degrade }`.
  Rules: `disk === undefined` → ok (fail-open); `!disk.validPath` → `library-missing`;
  `disk.free < LOW_SPACE_FLOOR_BYTES` → `library-full`.
- `LOW_SPACE_FLOOR_BYTES` (`1024 ** 3`), `SteamQuickInstallDegrade`
  (`{ reason: 'library-missing' | 'library-full'; libraryPath: string; freeBytes: number; onlyLibrary: boolean }`),
  `SteamInstallLibraryTarget`.
- **`startSteamQuickInstall` is explicitly NOT consumed by this plan** — see `<objective>` item 3.
- **Read `34.13-08-SUMMARY.md` first for the FINAL signatures.** If any name differs from the list
  above, follow the SUMMARY and report the delta; do not "fix" plan 08's shipped module.

**IPC — both already dual-ported, NO new channel:**
- `listSteamLibraryTargets: () => Promise<{ path: string; steamappsDir: string; isPrimary: boolean }[]>`
  — server-gated on `isSteamNativeInstallEnabled()` in BOTH handlers, so `[]` means "GameLib controls
  no local target" (native install OFF → delegated `steam://install`). That empty array IS the
  signal; do not add a settings read.
- `checkDiskSpace: (folder: string) => Promise<DiskSpaceData>` — `DiskSpaceData` has
  `{ free, diskSize, message, validPath, validFlatpakPath }`; `validPath` is an `isWritable()` probe
  and `getDiskInfo`'s doc comment states the path need not exist, so calling it on a missing library
  is safe and yields `validPath: false`.

**`GameCard/index.tsx` (verified 2026-08-15):**
- `:1` `import './index.css'` — **so this file can never be imported into a Jest test.**
- `:58` already imports `{ openInstallGameModal } from 'frontend/state/InstallGameModal'` — add
  `openSteamInstallOptions` to this existing import, do not create a second one.
- `:39-56` imports `Settings`, `Download`, `Cancel`, `PlayArrow`, `Upgrade`, `OpenInNew`,
  `Description` … from `@mui/icons-material`. **`Settings` is already imported** (used at `:395`),
  so the UI-SPEC's suggested icon needs zero new imports.
- `:100` `const [gameInfo, setGameInfo] = useState<GameInfo>(gameInfoFromProps)` — in scope.
- `:144-145` `const isInstallable = gameInfo.installable === undefined || gameInfo.installable`.
- `:316-318` `isSideloaded` / `isSteam` / `isDelisted`. `isSteam` already exists — but the new entry
  must still route through the shared predicate (see Task 1's D-28 containment argument).
- `:320-336` `handleEdit()` — contains the `:322` line D-27 cites. **Do not touch.**
- `:338-393` `const items: Item[] = [ … ]`. The plain install entry is `:368-374`:
  `label: t('button.install')`, `onclick: () => buttonClick()`,
  `show: !isInstalled && !isQueued && isInstallable && !isDelisted`, `icon: <Download />`.
  **Unchanged** — it continues to serve every non-Steam runner, and for Steam it is the quick half
  (plan 08 site 4, zero edits).
- `t` is `useTranslation('gamepage')`; `t2` is `useTranslation()`. A `gamelib:`-prefixed key resolves
  through either (the `ns:key` form overrides the default namespace) — the file has no `gamelib`
  hook today, so add `const { t: tGamelib } = useTranslation('gamelib')` mirroring
  `ConsoleMode/InstallOverlay/index.tsx:374`'s own precedent rather than relying on prefix-only.

**`GameSubMenu/index.tsx` (verified 2026-08-15):**
- `:1` `import './index.css'` — **not importable in Jest either.**
- `:15` already imports `{ openInstallGameModal } from 'frontend/state/InstallGameModal'`.
- `:20-34` the `@mui/icons-material` import block uses the aliased `X as XIcon` convention
  throughout. **`Download` is NOT imported.** The UI-SPEC snippet writes `<Download />`; follow the
  file's own convention and import `Download as DownloadIcon`. Same icon, file's naming.
- `:89-90` `isSideloaded` / `isSteam`. `isInstalled` is a prop (`:41`), `gameInfo` a prop (`:52`),
  `appName` a prop (`:56`).
- `:174-178` `handleEdit()` — contains the `:176` line D-27 cites. **Do not touch.**
- `:300-311` the render opens `<div className="gameTools subMenuContainer">` → `<div className="submenu">`.
- `:312` `{isInstalled && (` — the opener of the file's entire button cluster. **The new block goes
  immediately BEFORE this line**, as the first child of `.submenu`: for an uninstalled Steam game it
  is the only control in the menu, and document order puts it first for keyboard/gamepad traversal.
- Every button in the file uses `className="link button is-text is-link buttonWithIcon"`. Match it.
- `t` is `useTranslation('gamepage')` — add a `gamelib` hook as above.

**Reachability chain for the GameSubMenu entry (verified — this is why the item is not dead code):**
`GamePage/index.tsx:482` renders `<DotsMenu …>` unconditionally inside its `title ? …` branch →
`DotsMenu.tsx:41-61` renders `<GameSubMenu … isInstalled={is_installed} …>` with no gate of its own.
So `GameSubMenu` mounts for uninstalled games and a `!isInstalled`-gated child renders.

**`ConsoleMode/InstallOverlay/index.tsx` (411 lines, verified 2026-08-15):**
- `:5` `import './index.scss'` — **not importable in Jest.**
- `:21` `type FocusKey = 'platform' | 'wine' | 'cancel' | 'install'`.
- `:71` `const [focused, setFocused] = useState<FocusKey>('install')` — **defaults to `'install'`.**
- `:92-117` the Steam branch effect. Today: `if (game.runner !== 'steam') return`, then
  `void install({ gameInfo: game, previousProgress: null, progress, installPath: 'default',
  isInstalling: false, platformToInstall: 'Windows', t, showDialogModal: () => null })`, then
  `setTimeout(onDismiss, 1500)`, with a `cancelled` flag + `clearTimeout` cleanup. **This is the
  effect this plan gates.**
- `:164-187` `installGame()` — the NON-Steam install path. **Untouched.**
- `:250-258` the Enter/Space branch of the keydown handler: `if (h.focused === 'install') void
  h.installGame()`. No runner test.
- `:264-268` `useGamepadButtonPress(BTN_ACTION, () => { if (focused === 'install') void installGame()
  … })` and `useGamepadButtonPress(BTN_BACK, onDismiss)`. No runner test. **These two are the A-button
  bypass.**
- `:276-355` the render. `:279` `{game.runner === 'steam' ? ( … ) : ( … )}` — the Steam branch is
  already a bespoke two-line status surface: `consoleModalTitle` reading
  `t('console.steam.installing', 'Opening Steam to install…')` plus `consoleModalGameTitle`.
  **This is the surface D-29's failure state extends.**
- `:277` the container is already `role="dialog" aria-live="polite"` — swapped text is announced.
- `:374` `const { t: tGamelib } = useTranslation('gamelib')` inside `SelectorRow`, used as
  `tGamelib('gamelib:consoleMode.installPrevious', 'Previous')`. **The file's own established idiom
  for gamelib strings — mirror it in the main component.**
- Existing classes available with zero new CSS: `consoleModal`, `consoleModalTitle`,
  `consoleModalGameTitle`, `consoleInstallButtons` (`InstallOverlay/index.scss:151`), `consoleChip`
  (`ConsoleMode/index.scss:94`, already used at `:328,339` of this file).
- `InstallOverlay/index.scss` uses `var(--space-*)`, `var(--text-secondary, …)`,
  `var(--text-default, …)`, `var(--accent, …)`. `--status-danger` is a real token
  (`styles/_colors.scss:28`). **No hex literals, no new scale.**

**`install()` — what actually reaches the backend (a refinement of plan 08's note):**
`frontend/helpers/library.ts:34-99`. With `installPath: 'default'` it does NOT forward the string
`'default'` — it awaits `requestAppSettings()` and substitutes `defaultInstallPath`, then calls
`window.api.install({ path: <defaultInstallPath>, … })`. That is GameLib's default install directory,
**not a registered Steam library**, so `resolveOverride()` (`installLocation.ts:201-219`) logs and
falls back to the primary. Plan 08's conclusion (D-23 holds by containment) is CORRECT; its stated
mechanism ("'default' is not a registered library path") is right by accident. Record the correction
in the SUMMARY. Nothing about this call changes in this plan.

**Test infrastructure — Frontend project (`src/frontend/jest.config.js`):**
`rootDir: '../..'`, `roots: ['<rootDir>/src/frontend']`,
`testMatch: ['**/__tests__/**/*.test.tsx', '**/__tests__/**/*.test.ts']`, `resetMocks: true`,
`modulePaths: ['./src/']`, `testEnvironment: 'node'` (no jsdom, repo-wide).
**All three `.tsx` files this plan modifies import a stylesheet, so plan 08's element-graph idiom is
NOT available here** — a direct import dies at the stylesheet before any assertion runs
(`34.13-VALIDATION.md` hard constraint; `SideloadDialog/__tests__/index.test.tsx`'s own doc comment).

**Source-gate idiom — the analog to copy:**
`src/frontend/screens/Settings/components/__tests__/framelessWindowCopy.test.ts` (read its header in
full). `readFileSync` + `stripSourceComments` from `backend/testUtils/stripSourceComments` (resolves
via `modulePaths: ['./src/']`), then assert on the stripped text. Mandatory here: this plan's own
comments legitimately contain the words its gates forbid in executable code.

**Window stub idiom (node env):** `src/frontend/hooks/__tests__/hasStatus.reconcile.test.ts:99-110` —
`;(global as unknown as { window: unknown }).window = { … }` in `beforeEach`, `delete (global as
unknown as { window?: unknown }).window` in `afterAll`. Use this rather than dependency injection:
the ledgered lesson is that a test which reconstructs the call site is a replica that drifts.
</interfaces>

<d29_resolution>
## D-29's OPEN FOR PLANNING item, resolved: what Console Mode shows when the check fails

**Decision: the failure is shown IN PLACE, in the overlay's existing Steam branch, as a terminal
three-element state — a `--status-danger` failure title, a static reason line, and a single focused
`consoleChip` dismiss button — and the 1500ms auto-dismiss timer does not arm.**

Justification, against the existing Console Mode UI rather than in the abstract:

1. **The surface already exists and is already the right shape.** `InstallOverlay`'s Steam branch
   (`:279-285`) is not an install form — it is a bespoke, non-interactive status card
   ("Opening Steam to install…" + game title) that dismisses itself. A failure state is the same card
   with different copy and one button. It reuses `consoleModal`, `consoleModalTitle`,
   `consoleModalGameTitle`, `consoleInstallButtons` and `consoleChip` — all already in this file.
   **This is why no `/gsd-ui-phase` pass is needed**, which is the condition D-29 attached to its
   rejection of a gamepad options surface.
2. **It is provably not an options path.** No platform selector, no wine selector, no library picker,
   no install control, no `FocusKey` row added. There is nothing to choose. D-29's prohibition holds
   structurally, not by promise.
3. **No other channel is available.** The overlay deliberately passes `showDialogModal: () => null`
   (`:107`, `:181`) and adds `document.body.classList.add('console-modal-open')` (`:88`) to block the
   global back action. Console Mode is intentionally sealed off from the desktop dialog/toast stack;
   routing a failure there would surface it on a screen the user is not looking at, or nowhere.
4. **Auto-dismiss must not arm.** Today the timer starts at mount. If it stayed armed, the failure
   message would disappear after 1.5 seconds and the user would see a flash and no install — a
   silent failure, the exact outcome D-29 exists to prevent. The timer moves inside the ok branch.
5. **It is announced.** The container is already `role="dialog" aria-live="polite"` (`:277`), so
   replacing the title text announces the failure without new ARIA work.
6. **Dismissal already works and gets one visible affordance.** `BTN_BACK` (`:268`) and
   Escape/Backspace (`:213-218`) are runner-agnostic and already dismiss. A gamepad UI should not
   rely on an invisible affordance, so the state adds one real `consoleChip` — a non-zero-area,
   enabled `<button>`, which is the whole gamepad-focus mechanism on both runtimes (plan 08: Electron
   simulates real keypresses; Tauri walks `FOCUSABLE_SELECTOR` in document order dropping
   `disabled` / `aria-hidden` / zero-area elements). `consoleChip` carries real padding, so the
   Tauri-only zero-area drop cannot bite.

**Rejected alternatives, recorded so they are not re-litigated:**
- *Let it fail as it does today* — rejected by D-29 explicitly.
- *Open the desktop options dialog* (`startSteamQuickInstall`'s degrade) — rejected by D-29
  explicitly, and mechanically wrong: a `z-index: 10` full-screen overlay with `console-modal-open`
  set is not a place a second modal can be navigated with a gamepad.
- *A toast / `showDialogModal`* — unavailable; see point 3.
- *Retry / "change library" affordance* — that is an options path. Out by D-29.

**The hazard this resolution creates, and its control.** With no auto-dismiss, the failure state is
on screen indefinitely, and `focused` is still `'install'` (`:71`) while both `BTN_ACTION` (`:264`)
and Enter/Space (`:250`) call `installGame()` with no runner test. One A press would fire the very
install the check just refused. **Both input paths must route through `resolveConsoleActionIntent()`,
which returns `'dismiss'` for `runner === 'steam'` unconditionally.** Note this also changes the
transient "Opening Steam to install…" state, where A currently fires a *second* install — a
pre-existing latent defect, fixed here as a consequence rather than a separate errand, and recorded
as an intentional behavior change in the SUMMARY.
</d29_resolution>

<catalog_keys>
## New `gamelib:` keys for 34.13-13 to absorb (this plan writes NO catalog file)

| Key | English default | Used by |
|-----|-----------------|---------|
| `steam.install.withOptionsLabel` | `Install with options…` | **SHARED with 34.13-08** (MainButton caret). GameCard's `Item` label and GameSubMenu's button label reuse it. **Do not mint a second key** — one label, three surfaces, per the UI-SPEC's own snippets. |
| `consoleMode.steamInstallBlockedTitle` | `Can't start this install` | Console Mode failure title |
| `consoleMode.steamInstallLibraryMissing` | `Your Steam library folder isn't available right now. Reconnect the drive and try again.` | Console Mode reason line, `reason === 'library-missing'` |
| `consoleMode.steamInstallLibraryFull` | `There isn't enough free space in your Steam library folder. Free up space and try again.` | Console Mode reason line, `reason === 'library-full'` |

The three `consoleMode.*` keys follow the FLAT shape already in `public/locales/en/gamelib.json`
(`consoleMode.installNext`, `consoleMode.installPrevious`) — not a nested `consoleMode.steamInstall.*`
object, which would fork the file's own convention.

**Reused, no new key:** the dismiss chip uses `t('button.dismiss', 'Dismiss')`, which already exists
in `public/locales/en/translation.json` under `button`.
</catalog_keys>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the two "Install with options…" menu entries, gated by one shared Steam-only predicate</name>

  <files>src/frontend/helpers/steamInstallOptionsEntry.ts, src/frontend/helpers/__tests__/steamInstallOptionsEntry.test.ts, src/frontend/screens/Library/components/GameCard/index.tsx, src/frontend/screens/Game/GameSubMenu/index.tsx</files>

  <read_first>
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-08-SUMMARY.md` —
      the FINAL exported signature of `openSteamInstallOptions`. If it differs from
      `(appName, gameInfo, degrade?)`, follow the SUMMARY.
    - `34.13-UI-SPEC.md` §"Split-Button Control Contract" → the `### GameCard` and `### GameSubMenu`
      subsections, in full. They are the binding composition contracts and both carry the
      "NOT a caret" / "NOT a relabel" corrections.
    - `src/frontend/screens/Library/components/GameCard/index.tsx` — lines 1-60 (imports),
      100, 144-145, 316-336 (the `handleEdit` trap), and 338-393 (the whole `items` array).
      **Do not read the whole 700-line file; those ranges are sufficient and were verified.**
    - `src/frontend/screens/Game/GameSubMenu/index.tsx` — lines 1-40 (imports), 60-95 (locals and
      props), 174-178 (the `handleEdit` trap), and 300-345 (the render opening and the
      `{isInstalled && (` block).
    - `src/frontend/screens/Library/components/ContextMenu/index.tsx` — the `Item` type, to confirm
      the exact field names (`label` / `onclick` / `show` / `icon`) before writing an entry.
    - `src/frontend/screens/Settings/components/__tests__/framelessWindowCopy.test.ts` — the
      source-gate idiom in full, including its header explaining why it is a source gate and not a
      render test.
    - `src/backend/testUtils/stripSourceComments.ts` — its doc comment explains why a bare regex over
      raw source is a rotted gate.
    - `src/frontend/jest.config.js` — confirm `roots`, `testMatch`, `modulePaths`, `resetMocks`.
  </read_first>

  <behavior>
Write `src/frontend/helpers/__tests__/steamInstallOptionsEntry.test.ts` FIRST and observe it RED
against the unmodified tree before touching either `.tsx`. The RED must name the not-yet-existing
module.

**File header comment** must record: (a) neither `.tsx` under test can be imported here — both
`import './index.css'` at line 1 and there is no jsdom in this repo — so every assertion is either a
pure-function spec or a comment-stripped source gate; (b) the vacuity boundary: these specs prove a
predicate's value and a source file's wiring, and prove NOTHING about whether a menu item renders,
is visible, or is reachable by gamepad — that is 34.13-13's manual gate; (c) that the source gates
run over `stripSourceComments`-stripped text specifically so the explanatory comments this task adds
to both `.tsx` files can neither satisfy nor break a gate.

**Group A — `showSteamCardInstallOptions` (D-27 row 3, D-28).**
  - A1: Steam + not installed + not queued + installable + not delisted → `true`.
  - A2: **`DISCRIMINATOR: D-28 — no non-Steam runner ever sees this entry`** — parameterised over
    `'gog'`, `'legendary'`, `'nile'`, `'sideload'` with the otherwise-passing state from A1 → `false`
    for every one. An implementation that drops the runner conjunct passes A1 and A3-A6 and fails
    only this. Name it as the discriminator in the title.
  - A3: Steam + `isInstalled: true` → `false`.
  - A4: Steam + `isQueued: true` → `false`.
  - A5: Steam + `isInstallable: false` → `false`.
  - A6: Steam + `isDelisted: true` → `false`.
  - A7: the predicate is a pure function of its argument — calling it twice with the same input
    yields the same result and it touches no `window` (assert with NO window stub installed, so a
    stray `window.api` read throws rather than passing silently).

**Group B — `showSteamSubMenuInstallOptions` (D-27 row 5, D-28).**
  - B1: Steam + `isInstalled: false` → `true`.
  - B2: **`DISCRIMINATOR: D-28`** — same four non-Steam runners with `isInstalled: false` → `false`.
  - B3: Steam + `isInstalled: true` → `false` (the submenu's install entry is for uninstalled games
    only; the file's own `{isInstalled && …}` cluster owns everything else).
  - B4: the two predicates are NOT the same function — with a state that is
    `{runner:'steam', isInstalled:false, isQueued:true, isInstallable:true, isDelisted:false}`,
    the card predicate is `false` while the submenu predicate is `true`. This pins the deliberate
    asymmetry (a card can be in the download queue; the submenu has no queue concept) so a later
    "simplification" that collapses them into one is a test failure.

**Group C — comment-stripped source gates over `GameCard/index.tsx`.**
  - C1: the stripped source contains `showSteamCardInstallOptions` — the file uses the shared
    predicate rather than an inline `isSteam && …` expression. **Prove the gate non-vacuous in the
    same spec** by asserting the same matcher does NOT match a small inline known-good-but-wrong
    specimen string that inlines the conjunct instead.
  - C2: the stripped source contains `openSteamInstallOptions`.
  - C3: **the pre-existing plain install entry is untouched** — the stripped source still contains
    `label: t('button.install')` AND the exact string
    `show: !isInstalled && !isQueued && isInstallable && !isDelisted`. This is the D-28 proof by
    absence-of-change: every non-Steam runner's install affordance is byte-identical.
  - C4: `openInstallGameModal` still appears (the `handleEdit` sideload path at `:322` was not
    collaterally removed).
  - C5: the stripped source contains ZERO occurrences of `startSteamQuickInstall` — the card's quick
    half routes through `buttonClick` → `Library/index.tsx` → plan 08's chokepoint, and a direct call
    here would be a second route to the same behavior.

**Group D — comment-stripped source gates over `GameSubMenu/index.tsx`.**
  - D1: contains `showSteamSubMenuInstallOptions`.
  - D2: contains `openSteamInstallOptions`.
  - D3: **the file's existing cluster is untouched** — the stripped source still contains
    `{isInstalled && (` and still contains `t('button.edit-game', 'Edit Game')`.
  - D4: the new button carries the file's own class string — the stripped source contains at least
    one more occurrence of `link button is-text is-link buttonWithIcon` than the pre-edit count.
    **Record the pre-edit count by running the count against `git show HEAD:<path>` at authoring
    time and pin the expected post-edit number as a literal**, so the gate cannot pass on a file
    where the button was added with a different class string.
  - D5: contains ZERO occurrences of `Dropdown` and ZERO of `ContextMenu` — the UI-SPEC requires a
    third, distinct idiom here; importing either primitive would be the "homogenize them into one
    component" failure it explicitly forbids.
  </behavior>

  <action>
**1. Create `src/frontend/helpers/steamInstallOptionsEntry.ts`.** A dependency-free module importing
only `Runner` from `common/types`. It exports two pure predicates and their two input types:

- `showSteamCardInstallOptions({ runner, isInstalled, isQueued, isInstallable, isDelisted })` →
  `runner === 'steam'` AND the plain install entry's own visibility expression
  (`!isInstalled && !isQueued && isInstallable && !isDelisted`).
- `showSteamSubMenuInstallOptions({ runner, isInstalled })` → `runner === 'steam' && !isInstalled`.

The module doc comment is load-bearing and must state three things:
  (a) **why this module exists at all** — D-28 makes quick install and its options path Steam-only,
      and this is the SINGLE site where `runner === 'steam'` is evaluated for these two entries, so
      a future runner extension is one edit and one test, not a grep;
  (b) **why the card predicate mirrors the existing install entry's expression exactly** — the new
      entry may appear only where the plain install entry already appears, which is what makes
      "GOG/Epic/Amazon unchanged" a structural fact rather than a claim;
  (c) **that these two predicates are deliberately NOT the same function** (spec B4) — the card has
      a queue/installable/delisted vocabulary the submenu does not.

Do NOT re-export it through `src/frontend/helpers/index.ts`; import it by full path
(`frontend/helpers/steamInstallOptionsEntry`) so this plan does not touch a barrel file it does not
own.

**2. Edit `src/frontend/screens/Library/components/GameCard/index.tsx`.** Four edits, nothing else:
  a. Extend the existing `frontend/state/InstallGameModal` import (`:58`) with
     `openSteamInstallOptions`.
  b. Add the `showSteamCardInstallOptions` import.
  c. Add `const { t: tGamelib } = useTranslation('gamelib')` beside the existing `t`/`t2` hooks.
  d. Add ONE new object to the `items: Item[]` array (`:338-393`), placed IMMEDIATELY AFTER the
     existing plain install entry (`:368-374`) so the two install affordances are adjacent in the
     menu, per the UI-SPEC. Its fields: `label` = `tGamelib('gamelib:steam.install.withOptionsLabel',
     'Install with options…')`; `onclick` = `() => openSteamInstallOptions(appName, gameInfo)` (two
     arguments — a deliberate click carries no degrade, plan 08 spec C3); `show` =
     `showSteamCardInstallOptions({ runner, isInstalled, isQueued, isInstallable, isDelisted })`;
     `icon` = `<Settings />`, already imported at `:39-56` and visually distinct from the quick
     entry's `<Download />` as the UI-SPEC requires.
  Write a one-line comment on the new entry recording that D-27's cited line `:322` is dead
  sideload-only code and was deliberately not touched.
  **Do not** edit `handleEdit()`, the `downIcon` `SvgButton` (`:288-296`), the plain install entry,
  or the `buttonClick` prop chain. The card's quick half is already correct via plan 08's site 4.

**3. Edit `src/frontend/screens/Game/GameSubMenu/index.tsx`.** Four edits, nothing else:
  a. Extend the existing `frontend/state/InstallGameModal` import (`:15`) with
     `openSteamInstallOptions`.
  b. Add `showSteamSubMenuInstallOptions` and add `Download as DownloadIcon` to the aliased
     `@mui/icons-material` block (`:20-34`) — the UI-SPEC names `<Download />`; the file's convention
     is the alias.
  c. Add `const { t: tGamelib } = useTranslation('gamelib')` beside the existing `t`.
  d. Insert a new block as the FIRST child of `<div className="submenu">`, immediately BEFORE
     `{isInstalled && (` at `:312`:
     `{showSteamSubMenuInstallOptions({ runner, isInstalled }) && (<button …>)}` — a plain
     `<button className="link button is-text is-link buttonWithIcon">` whose `onClick` is
     `() => openSteamInstallOptions(appName, gameInfo)`, containing `<DownloadIcon />` and
     `tGamelib('gamelib:steam.install.withOptionsLabel', 'Install with options…')`.
  Write a comment on the block stating, in these terms, that this is a **NEW control and not a
  relabel**: D-27's table claims an existing Install item is relabelled here, RESEARCH Q4's full-file
  read found none, `:176` is sideload-only dead code, and the file's whole button cluster is
  `isInstalled`-gated. Without that note the next reader will "correct" it back.
  **Do not** use `Dropdown` or `ContextMenu`, and do not touch `handleEdit()` or any existing entry.

**4. Verify no collateral.** `npx tsc --noEmit` must pass, and the diff must touch exactly the four
files in this task — no catalog, no `InstallGameModal.ts`, no barrel, no `package.json`.
  </action>

  <verify>
    <automated>npx jest src/frontend/helpers/__tests__/steamInstallOptionsEntry.test.ts --silent && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `npx jest src/frontend/helpers/__tests__/steamInstallOptionsEntry.test.ts --silent` exits 0,
      and the test file was observed RED first with the RED output naming the missing module.
    - `npx tsc --noEmit` exits 0.
    - `grep -c "openSteamInstallOptions" src/frontend/screens/Library/components/GameCard/index.tsx`
      returns 2 (the import and the one call site); the same command against
      `src/frontend/screens/Game/GameSubMenu/index.tsx` returns 2.
    - `grep -v '^\s*//' src/frontend/screens/Library/components/GameCard/index.tsx | grep -c "runner === 'steam'"`
      is unchanged from its pre-edit value (the pre-existing `isSteam` local at `:317` is the only
      occurrence; the new entry adds none) — the D-28 test lives in the shared module, not inlined.
    - `git diff --name-only` lists exactly the four files in `<files>`.
    - `git diff src/frontend/screens/Library/components/GameCard/index.tsx | grep '^-' | grep -v '^---'`
      shows ONLY the two import lines being extended — no existing `Item`, no `handleEdit` line, and
      no `downIcon` line appears as a removal.
    - `git diff src/frontend/screens/Game/GameSubMenu/index.tsx | grep '^-' | grep -v '^---'` shows
      ONLY the import lines being extended — `{isInstalled && (` does not appear as a removal.
    - `pnpm test:ci` exits 0 (no existing suite regressed).
  </acceptance_criteria>

  <done>
GameCard's context menu and GameSubMenu both offer "Install with options…" for an uninstalled Steam
game and for nothing else, both routed to plan 08's `openSteamInstallOptions` with no prop threading,
with D-28 enforced at one evaluation site and proven by a named discriminator over four non-Steam
runners plus source gates showing every pre-existing entry is byte-unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend D-24's local check to Console Mode with an in-place failure state and no options path (D-29)</name>

  <files>src/frontend/screens/ConsoleMode/InstallOverlay/consoleSteamTarget.ts, src/frontend/screens/ConsoleMode/InstallOverlay/__tests__/consoleSteamTarget.test.ts, src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx, src/frontend/screens/ConsoleMode/InstallOverlay/index.scss</files>

  <read_first>
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-08-SUMMARY.md` —
      the FINAL signatures of `resolveQuickInstallTarget`, `evaluateQuickInstallTarget`,
      `LOW_SPACE_FLOOR_BYTES` and `SteamQuickInstallDegrade`'s four fields, plus plan 08's recorded
      fail-open rule. Write against what shipped.
    - `src/frontend/state/InstallGameModal.ts` — READ ONLY. Confirm the two pure helpers are exported
      and confirm you are NOT importing `startSteamQuickInstall`. If the pure helpers are not
      exported, stop: the Wave-2 precondition is unmet, and duplicating the decision here would
      create the second definition this plan exists to avoid.
    - `src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx` (411 lines, full file) — the effect
      at `:92-117`, the `focused` default at `:71`, both input handlers at `:250-258` and `:264-268`,
      and the Steam render branch at `:276-290`.
    - `src/frontend/screens/ConsoleMode/InstallOverlay/index.scss` (full, 157 lines) — the existing
      token vocabulary. Every value you add must already appear in this file or be a real theme token.
    - `src/frontend/helpers/library.ts` lines 34-99 — `install()`, and specifically the
      `installPath === 'default'` → `requestAppSettings()` substitution. Read it so the SUMMARY's
      correction to plan 08's stated mechanism is accurate rather than repeated from this plan.
    - `src/frontend/hooks/__tests__/hasStatus.reconcile.test.ts` lines 94-112 — the `global.window`
      stub with its `delete` teardown.
    - `src/frontend/screens/Settings/components/__tests__/framelessWindowCopy.test.ts` — the source
      gate idiom (also read for Task 1; re-read only if not already in context).
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VALIDATION.md`
      §"Named Vacuous-Assertion Risks" — risks 1 and 4 apply directly to this task.
  </read_first>

  <behavior>
Write `src/frontend/screens/ConsoleMode/InstallOverlay/__tests__/consoleSteamTarget.test.ts` FIRST
and observe it RED before touching the overlay. The RED must name all three not-yet-existing exports.

**File header comment** must record: (a) `InstallOverlay/index.tsx` imports `./index.scss` at line 5
and there is no jsdom, so the component cannot be rendered or imported here — group D is a
source-text gate by necessity, not by preference; (b) the vacuity boundary — a green run proves the
decision functions and the overlay's wiring shape, and proves NOTHING about whether the failure card
is legible on a TV or reachable with a controller (34.13-13's manual gate); (c) that D-29's failure
shape is an in-place terminal message by decision, so a future reader finding no dialog here is
looking at the design, not an omission.

**Harness.** A `stubWindowApi(overrides)` installing
`(global as unknown as { window: unknown }).window = { api: { listSteamLibraryTargets, checkDiskSpace } }`
with `jest.fn()`s plus a `delete` teardown; a `makeTargets(n)` factory producing
`{ path, steamappsDir, isPrimary: i === 0 }`; a `makeDisk(overrides)` factory over `DiskSpaceData`'s
five fields. `resetMocks: true` is load-bearing — re-arm in `beforeEach`.

**Group A — `probeSteamQuickInstallTarget()` verdicts (D-29 inheriting D-24).**
  - A1: `0 libraries → ok, and checkDiskSpace was NOT called` — native install OFF returns `[]`
    server-side, which means GameLib controls no local target and there is nothing to check. This is
    the delegated `steam://install` path; probing it would be a check with no subject.
  - A2: 1 valid library (`validPath: true`, `free` at or above `LOW_SPACE_FLOOR_BYTES`) → ok.
  - A3: `validPath: false` → not ok, `degrade.reason === 'library-missing'`, and `degrade.libraryPath`
    equals the resolved primary's `path`.
  - A4: `free` exactly `LOW_SPACE_FLOOR_BYTES - 1` → not ok, `'library-full'`; `free` exactly
    `LOW_SPACE_FLOOR_BYTES` → ok. **Boundary asserted on both sides.**
  - A5: `probes the RESOLVED primary's steamappsDir` — with two targets, `checkDiskSpace` called
    exactly once with `targets[0].steamappsDir`, never with `.path`.
  - A6: `onlyLibrary` is carried through from `evaluateQuickInstallTarget` — `true` with one library,
    `false` with two (parameterised), even though Console Mode renders neither. It is part of plan
    08's record shape and dropping it here would fork the type.
  - A7: **`DISCRIMINATOR: an UNKNOWN disk verdict is not a failed one`** — `checkDiskSpace` rejects →
    verdict ok. An implementation that treats a rejected probe as a failure passes A1-A6 and fails
    only this. Console Mode is a couch surface with no keyboard; a misbehaving probe must never be
    able to wedge its only install path.
  - A8: `listSteamLibraryTargets` rejects → ok (the A1 shape, reached by failure rather than by
    configuration).
  - A9: `probeSteamQuickInstallTarget never rejects` —
    `await expect(probeSteamQuickInstallTarget()).resolves.toBeDefined()` with both channels
    rejecting.

**Group B — `resolveConsoleActionIntent()` (the A-button bypass gate).**
  - B1: `{ runner: 'steam', focused: 'install' }` → `'dismiss'`. **This is the whole point:** the
    Steam branch renders no install control on ANY path, so an A press can only mean "get me out".
  - B2: parameterised over all four `FocusKey` values with `runner: 'steam'` → `'dismiss'` for every
    one. The Steam branch has no focus rows at all, so no `focused` value may reach an install.
  - B3: **`DISCRIMINATOR: D-28 — non-Steam runners keep today's behavior exactly`** — parameterised
    over `'gog'`, `'legendary'`, `'nile'`, `'sideload'`: `focused: 'install'` → `'install'`,
    `focused: 'cancel'` → `'dismiss'`, `focused: 'platform'` → `'none'`, `focused: 'wine'` → `'none'`.
    An implementation that gates on "is the failure showing" instead of "is this Steam" passes B1/B2
    and fails only this.
  - B4: the function takes NO "blocked" input, and a spec comment records why: the answer is
    `'dismiss'` for Steam in both the transient "Opening Steam…" state and the failure state, so an
    input the result does not depend on would invite a future reader to treat it as load-bearing.

**Group C — `steamBlockedMessageKey()`.**
  - C1: `'library-missing'` → `'consoleMode.steamInstallLibraryMissing'`.
  - C2: `'library-full'` → `'consoleMode.steamInstallLibraryFull'`.
  - C3: the mapping is exhaustive over `SteamQuickInstallDegrade['reason']` — assert the returned key
    for every member of the union, enumerated from the type rather than hand-listed, so a third
    reason added upstream is a compile error here rather than a silently missing string.

**Group D — comment-stripped source gates over `InstallOverlay/index.tsx`.**
  - D1: contains `probeSteamQuickInstallTarget` and `resolveConsoleActionIntent`.
  - D2: contains ZERO occurrences of `startSteamQuickInstall` and ZERO of `openSteamInstallOptions` —
    **the D-29 "no options path" proof.** Prove the gate non-vacuous in the same spec against a
    known-bad inline specimen containing `openSteamInstallOptions(`.
  - D3: contains ZERO occurrences of `libraryPath` — no filesystem path may reach a TV screen
    (T-34.13-15-04). Non-vacuity proven the same way.
  - D4: **the non-Steam path is untouched** — the stripped source still contains
    `installPath: installPath || 'default'` (inside `installGame()`) and still contains
    `platformToInstall: 'Windows'`. The second is load-bearing beyond this plan: plan 08's phase gate
    expects exactly two non-test occurrences of that literal in `src/frontend`, and this file is one
    of them.
  - D5: **the timer is armed inside the ok branch, not at effect top level** — assert that in the
    stripped source the first index of `setTimeout(` is GREATER than the first index of
    `probeSteamQuickInstallTarget(`. A textual ordering gate is weak on its own, so pair it with the
    manual-gate row in `<verification>` and say so in the spec's own comment. Record in the SUMMARY
    what the pre-edit ordering was (today `setTimeout` follows `install(` at effect top level).
  - D6: contains exactly ONE occurrence of `install({` — the single unchanged install call. More than
    one means a second install path was introduced on the Steam branch.
  </behavior>

  <action>
**1. Create `src/frontend/screens/ConsoleMode/InstallOverlay/consoleSteamTarget.ts`.** It imports
`resolveQuickInstallTarget`, `evaluateQuickInstallTarget` and the `SteamQuickInstallDegrade` /
`SteamInstallLibraryTarget` types from `frontend/state/InstallGameModal`, and `Runner` from
`common/types`. It exports:

  a. `type ConsoleSteamVerdict = { ok: true } | { ok: false; degrade: SteamQuickInstallDegrade }`.
  b. `probeSteamQuickInstallTarget(): Promise<ConsoleSteamVerdict>` — sequence exactly:
     fetch `window.api.listSteamLibraryTargets()` inside a `try/catch` leaving the list `[]` on
     rejection; if the list is empty return `{ ok: true }` WITHOUT calling `checkDiskSpace`; resolve
     the target with `resolveQuickInstallTarget`; if it is `undefined` return `{ ok: true }`; call
     `window.api.checkDiskSpace(target.steamappsDir)` inside a `try/catch` yielding `undefined` on
     rejection; return `evaluateQuickInstallTarget(target, disk, libraries.length)` mapped into the
     verdict shape. Doc-comment the fail-open direction and name the failure it prevents, and
     doc-comment why `steamappsDir` and never `path` is probed (it is the directory the backend
     writes to, and it is backend-enumerated, never renderer-composed — see the threat model).
  c. `resolveConsoleActionIntent({ runner, focused }): 'install' | 'dismiss' | 'none'` — returns
     `'dismiss'` when `runner === 'steam'`; otherwise `'install'` for `focused === 'install'`,
     `'dismiss'` for `focused === 'cancel'`, `'none'` otherwise. Its doc comment must state the
     defect it closes: `focused` defaults to `'install'` and both input handlers called
     `installGame()` with no runner test, so before this function one A press could fire the install
     the D-29 check had just refused — and, on the transient path, a second concurrent install.
  d. `steamBlockedMessageKey(reason)` — an exhaustive switch over
     `SteamQuickInstallDegrade['reason']` returning the two `consoleMode.*` catalog key strings.

  The module doc comment must state, in these terms: **this is the Console Mode half of D-24 and it
  deliberately does not call `startSteamQuickInstall`**, because that function's degrade branch opens
  the desktop options dialog, which D-29 forbids in a gamepad UI. One definition of the check (the
  imported pure helpers), two degrade shapes.

**2. Edit `src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx`.** Five edits, nothing else:
  a. Import the module's three functions and the verdict/degrade types; add
     `const { t: tGamelib } = useTranslation('gamelib')` in the main component, mirroring
     `SelectorRow`'s existing hook at `:374`.
  b. Add `const [steamBlocked, setSteamBlocked] = useState<SteamQuickInstallDegrade | null>(null)`.
  c. Rewrite the Steam effect (`:92-117`) to: keep `if (game.runner !== 'steam') return` as the FIRST
     statement; keep the `cancelled` flag; `await`/`.then()` `probeSteamQuickInstallTarget()`; on a
     not-ok verdict call `setSteamBlocked(verdict.degrade)` and return **without calling `install()`
     and without arming any timer**; on an ok verdict fire the EXISTING `install({…})` call with its
     arguments byte-identical to today and only then arm the 1500ms `setTimeout(onDismiss)`. The
     cleanup must clear a timer that may never have been created. Update the effect's existing
     comment block (`:92-95`) so its description matches the new shape — the current text describes
     an unconditional install.
  d. Route BOTH input paths through `resolveConsoleActionIntent`: the `BTN_ACTION` handler
     (`:264-267`) and the Enter/Space branch of the keydown handler (`:250-258`) each compute the
     intent from `{ runner: game.runner, focused }` and switch on it — `'install'` → `void
     installGame()`, `'dismiss'` → `onDismiss()`, `'none'` → nothing. The keydown handler reads live
     values through `handlersRef` (`:192-207`); add `runner` to that ref's payload rather than
     capturing it in the listener closure, matching the file's own stated reason for the ref.
     `BTN_BACK` (`:268`) is unchanged.
  e. Extend the Steam render branch (`:279-285`) so that when `steamBlocked` is non-null it renders
     the failure card instead of the "Opening Steam to install…" card: a `consoleModalTitle` with an
     added `blocked` class carrying
     `tGamelib('gamelib:consoleMode.steamInstallBlockedTitle', "Can't start this install")`; the
     existing `consoleModalGameTitle` with `game.title`; a `<p className="consoleModalReason">`
     carrying `tGamelib('gamelib:' + steamBlockedMessageKey(steamBlocked.reason), <default>)` with
     the English defaults from `<catalog_keys>`; and a `consoleInstallButtons` wrapper holding ONE
     `<button className="consoleChip active" onClick={onDismiss}>` labelled
     `t('button.dismiss', 'Dismiss')`, focused on mount via the existing `cancelButtonRef` so a
     controller lands on it. **No platform row, no wine row, no path row, no install control, no new
     `FocusKey`.** Interpolate NO path into any string.

**3. Edit `src/frontend/screens/ConsoleMode/InstallOverlay/index.scss`.** Exactly two rules, using
only values that already appear in this file or are real theme tokens:
  - `.consoleModalTitle.blocked { color: var(--status-danger); }` — the token
    `styles/_colors.scss:28` already defines, and the same shade the UI-SPEC reserves for the D-24
    notice, so the desktop degrade and the Console Mode degrade read as the same condition.
  - `.consoleModalReason { font-size: 1rem; color: var(--text-default, #eae8e5); max-width: 80vw;
    text-align: center; }` — every value copied from a sibling rule in this same file
    (`.consoleModalTitle`, `.consoleInstallPath`, `.consoleModalGameTitle`). No new scale, no new
    hex beyond the file's existing fallback idiom.

**4. Verify no collateral.** `npx tsc --noEmit` and `pnpm test:ci` pass; the diff touches exactly the
four files in this task; `ConsoleMode/index.scss` and `ConsoleMode/index.tsx` are untouched.
  </action>

  <verify>
    <automated>npx jest src/frontend/screens/ConsoleMode/InstallOverlay/__tests__/consoleSteamTarget.test.ts --silent && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `npx jest src/frontend/screens/ConsoleMode/InstallOverlay/__tests__/consoleSteamTarget.test.ts --silent`
      exits 0, and the file was observed RED first with the RED naming all three missing exports.
    - `npx tsc --noEmit` exits 0 and `pnpm test:ci` exits 0.
    - `grep -rn "platformToInstall: 'Windows'" src/frontend --include='*.ts' --include='*.tsx' | grep -v __tests__`
      returns exactly TWO lines — `state/InstallGameModal.ts` and
      `ConsoleMode/InstallOverlay/index.tsx` — i.e. plan 08's phase gate still holds unchanged.
    - `grep -c "startSteamQuickInstall\|openSteamInstallOptions" src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx`
      returns 0 (D-29: no options path in the gamepad UI).
    - `grep -c "libraryPath" src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx` returns 0.
    - `grep -c "install({" src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx` returns 1.
    - `grep -c -- "--status-danger" src/frontend/screens/ConsoleMode/InstallOverlay/index.scss`
      returns 1, and
      `grep -cE "#[0-9a-fA-F]{6}" src/frontend/screens/ConsoleMode/InstallOverlay/index.scss` is
      unchanged from its pre-edit value plus at most 1 (the one `var(--text-default, #eae8e5)`
      fallback copied verbatim from `.consoleInstallPath`).
    - `git diff src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx | grep '^-' | grep -v '^---'`
      contains NO line from `installGame()` (`:164-187`) and no line from the non-Steam render
      branch (`:286-351`).
    - `git diff --name-only` lists exactly the four files in `<files>`.
  </acceptance_criteria>

  <done>
A Console Mode Steam install runs D-24's local check first, using the same two pure decision
functions the desktop path uses; a missing or full primary library stops the install and shows a
terminal, dismissible, `--status-danger` failure card in place — with no auto-dismiss, no options
surface, no path printed, and no way for the A button to fire the install the check just refused —
while every non-Steam runner's Console Mode install path is byte-unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → backend, `window.api.listSteamLibraryTargets()` | **Newly crossed from Console Mode.** A pre-existing read, already gated server-side on `isSteamNativeInstallEnabled()`. This plan adds a consumer, not a channel. |
| renderer → backend, `window.api.checkDiskSpace(folder)` | **Newly crossed from Console Mode.** The argument is `listSteamLibraryTargets()`'s own `steamappsDir` — backend-enumerated and round-tripped, never renderer-composed. |
| renderer → backend, `window.api.install(InstallArgs)` from Console Mode | **Crossed today and NARROWED by this plan.** The call's arguments are unchanged; the change is that a failing local check now prevents it. |
| renderer-internal, `GameCard` / `GameSubMenu` → `openSteamInstallOptions` | **No boundary crossed.** D-25 makes the options path synchronous and zero-IPC; these two entries perform no IPC at all. |
| renderer-internal, `steamBlocked` degrade record → Console Mode copy | A filesystem path enters component state and sits one interpolation away from a full-screen, potentially TV/streamed surface. In-process only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.13-15-01 | Elevation of Privilege | The two new menu entries reaching a Steam-only install surface from a non-Steam game | **mitigate** | D-28's `runner === 'steam'` test is evaluated at exactly ONE site (`steamInstallOptionsEntry.ts`) which both `.tsx` files call rather than inlining, so the two surfaces cannot drift apart. Proven by a named discriminator parameterised over `gog`/`legendary`/`nile`/`sideload` across every state combination (Task 1 specs A2, B2) plus source gates asserting each file calls the shared predicate (C1, D1) and that every pre-existing entry is byte-unchanged (C3, D3). The worst outcome of a bypass is a dialog opening for a runner whose backend has no Steam branch — an inert dialog, not an install — but the containment is structural regardless. |
| T-34.13-15-02 | Tampering | `checkDiskSpace(target.steamappsDir)` from Console Mode — a renderer-supplied path reaching a backend filesystem probe | accept | The argument is not renderer-composed: it is the `steamappsDir` field of an element the BACKEND enumerated moments earlier, returned unmodified, and the plan forbids constructing or joining a path in the renderer. The handler is read-only (`getDiskInfo` + `isWritable`, no write, no exec) and its own doc comment states the path need not exist, so a stale value yields `validPath: false` rather than an error — which is D-24's intended behavior. Identical in shape and rationale to plan 08's accepted T-34.13-08-03; this plan adds a second caller, not a new capability. |
| T-34.13-15-03 | Denial of Service | Two new awaits in front of Console Mode's only install path — a wedged or rejecting channel turning the gamepad install into a dead overlay | **mitigate** | Both awaits are individually wrapped: a rejected `listSteamLibraryTargets` yields `[]` (delegated install, no probe at all) and a rejected `checkDiskSpace` yields `undefined` (fail-open, install proceeds). Specs A7, A8 and A9 assert an ok verdict in each case and that the probe never rejects; A7 is named as the discriminator. The severity floor matters here more than on the desktop: Console Mode is a couch surface with no keyboard and no address bar, so a wedged install path has no workaround. Note the path was ALREADY async before this plan — `install()` awaits `requestAppSettings()` — so this is an added await on an existing async path, not a new latency class. |
| T-34.13-15-04 | Information Disclosure | `steamBlocked.libraryPath` — a local filesystem path in component state, one interpolation away from a full-screen surface | **mitigate** | The D-29 copy is static: three fixed strings selected by `steamBlockedMessageKey(reason)`, interpolating nothing. **Control:** a source gate asserts `libraryPath` appears ZERO times in the overlay's comment-stripped source, proven non-vacuous against a known-bad specimen. This is deliberately stricter than plan 08's accepted T-34.13-08-05 (which reasons that a Steam library path carries no credential and stays in-process): a Console Mode screen is routinely a living-room TV or a stream, and a home-directory path there is disclosed to everyone in the room. The record still travels — dropping the field would fork plan 08's type — it is simply never rendered and never logged. |
| T-34.13-15-05 | Spoofing | The A/Enter press firing the install the D-24 check just refused, on a screen that says the install cannot start | **mitigate** | `focused` defaults to `'install'` and both `useGamepadButtonPress(BTN_ACTION, …)` and the Enter/Space keydown branch called `installGame()` with no runner test — a pre-existing latent defect whose window today is the 1500ms auto-dismiss, and which D-29's non-dismissing failure state would widen to unbounded. Both paths now route through the pure `resolveConsoleActionIntent()`, which returns `'dismiss'` for `runner === 'steam'` unconditionally (specs B1, B2), with B3 as the discriminator proving all four non-Steam runners keep today's behavior exactly. A source gate cannot prove a handler routes through a function, so this is unit-tested at the decision and gated textually at the wiring — and carried to the manual gate as an explicit controller step. |
| T-34.13-15-06 | Repudiation | — | accept | Not applicable. This plan adds no audited action and no authorization decision; it adds two menu affordances that open an existing dialog and one pre-install check that can only reduce what fires. |
| T-34.13-15-SC | Tampering | npm/pip/cargo installs | **n/a — the Package Legitimacy Gate is NOT APPLICABLE to this plan or this phase.** `34.13-RESEARCH.md` §"Standard Stack" records "No new libraries. This phase composes existing in-repo primitives only," and marks `## Package Legitimacy Audit` not applicable. This plan adds ZERO dependencies: `zustand`, `common/types`, `@mui/icons-material` (`Settings`, `Download`) and both IPC channels already ship. `package.json` / `pnpm-lock.yaml` are absent from `files_modified` by design and are covered by the diff-scope acceptance criterion in both tasks. **Especially load-bearing here:** the obvious "fix" for "I cannot import these `.tsx` files in a test" is `pnpm add -D jest-environment-jsdom`, which `34.13-VALIDATION.md` forbids as a hard constraint. An executor running a package-manager install has escaped scope — stop and return to planning. | The standing control remains armed: any install task would require the RESEARCH.md audit table plus a blocking human-verify checkpoint for every `[ASSUMED]`/`[SUS]` package; `[SLOP]` packages are forbidden and such checkpoints are never auto-approvable. |

**ASVS L1 posture for this plan:** V5 (Input Validation) is the relevant category. The controls are
(a) no path is composed, joined or synthesized in the renderer on any path this plan adds — the only
value crossing to `checkDiskSpace` is a backend-enumerated field round-tripped verbatim
(T-34.13-15-02), and (b) no path is rendered (T-34.13-15-04, mitigated by an executable gate). V2
(auth), V3 (session), V4 (access control) and V6 (cryptography) are untouched, matching
`34.13-RESEARCH.md` §"Security Domain" ("No new authentication, session, network-facing, or
cryptographic surface is introduced by this phase"). This plan additionally introduces no new IPC
channel and no new persisted state.
</threat_model>

<verification>
## Per-task verification map (for `34.13-VALIDATION.md`, folded in by 34.13-13)

| Task ID | Wave | Decision | Threat Ref | Test Type | Automated Command | File Exists |
|---------|------|----------|------------|-----------|-------------------|-------------|
| 34.13-15-01 | 3 | D-27 (rows 3, 5), D-28 | T-34.13-15-01 | unit (pure predicates, RED-proven) + static (comment-stripped source gates) | `npx jest src/frontend/helpers/__tests__/steamInstallOptionsEntry.test.ts --silent && npx tsc --noEmit` | ❌ Wave 0 — created by this task |
| 34.13-15-02 | 3 | D-24, D-28, D-29 | T-34.13-15-02, -03, -04, -05 | unit (verdict + intent matrices, RED-proven) + static (source gates) | `npx jest src/frontend/screens/ConsoleMode/InstallOverlay/__tests__/consoleSteamTarget.test.ts --silent && npx tsc --noEmit` | ❌ Wave 0 — created by this task |

## Phase-level checks owed by this plan

- `npx tsc --noEmit` exits 0; `pnpm test:ci` exits 0.
- `grep -rn "platformToInstall: 'Windows'" src/frontend --include='*.ts' --include='*.tsx' | grep -v __tests__`
  still returns exactly TWO lines. **This plan must not change plan 08's number.** If it returns
  anything else after this plan, a surface bypassing D-17's marshalling was introduced here.
- Both new suites were observed RED before GREEN, and each RED named the not-yet-existing exports.
  A gate that only ever passed is not evidence — this repo carries multiple ledgered instances of a
  passing test guarding nothing.
- Three deliberate-breakage observations recorded verbatim in the SUMMARY: dropping the `runner`
  conjunct from the shared predicate failing A2/B2 only; treating a rejected `checkDiskSpace` as a
  failure failing A7 only; and gating `resolveConsoleActionIntent` on "is the failure showing"
  instead of "is this Steam" failing B3 only.

## Manual-gate rows this plan owes `34.13-VALIDATION.md` (34.13-13 folds them in)

- **`GameCard` context menu, Steam, uninstalled** — open the card's context menu on an uninstalled
  Steam game: an "Install with options…" entry exists beside the plain Install entry, with a
  visually distinct icon, and opens the same dialog the `MainButton` caret opens. Then open the
  context menu on an uninstalled **GOG** game and confirm the entry is **absent**.
- **`GameSubMenu`, Steam, uninstalled — CORRECTS AN EXISTING ROW.** `34.13-VALIDATION.md`'s current
  row reads *"the install entry reads 'Install with options…'"*, which presumes a relabel. There is
  no entry to relabel. The correct step is: open the dots menu on an **uninstalled** Steam game's
  game page and confirm an "Install with options…" button **now exists** (it did not before), and
  that it is **absent** for an uninstalled GOG game and for an **installed** Steam game.
- **Gamepad into `GameCard`'s context menu** — with a controller, confirm the new entry is reachable
  and that reaching it does not make the existing Install entry unreachable. Both runtimes; the
  Tauri leg is not redundant (its focus collector drops zero-area elements, a filter Electron's
  native traversal does not apply).
- **D-29 Console Mode degrade — NEW ROW, no equivalent exists today.** With native install ON, make
  the primary Steam library unavailable (detach the drive or rename the folder), then launch the
  game from **Console Mode** with a controller. Confirm: the overlay shows the failure card, it does
  **not** auto-dismiss after ~1.5s, no install starts, **no filesystem path appears anywhere on
  screen**, the Dismiss chip is reachable, and pressing **A** dismisses rather than starting an
  install. Repeat under `pnpm tauri:dev` (never `tauri dev` — serves a stale bundle).
- **D-29 Console Mode happy path — regression guard.** With a healthy primary library, launch a Steam
  game from Console Mode: it still shows "Opening Steam to install…", still installs, and still
  auto-dismisses. The probe must not have introduced a visible stall.
- **D-29 Console Mode, non-Steam regression** — install a GOG game from Console Mode: the platform
  row, wine row, path row, Cancel and Install chips all behave exactly as before, and A on the
  Install chip still installs.
- **Failure card legibility at couch distance (OBSERVE, do not pass/fail)** — Console Mode is a TV
  surface. Judge whether the `--status-danger` title and the reason line read at distance. If they do
  not, that is a copy/size amendment for a follow-up, not a defect in this plan's logic.

## Explicitly NOT verified here (owned by the manual gate, and NOT provable in Jest)

- **That any of the three surfaces RENDERS anything.** All three modified files import a stylesheet
  at line 1/line 5, so none can be imported into a Jest test at all — not even by plan 08's
  element-graph idiom. A green suite and a completely broken menu are indistinguishable here.
  `34.13-VALIDATION.md` vacuous-assertion risk 1 applies to this plan even more sharply than to
  plan 08, and the SUMMARY must say so.
- **That the two new menu entries are gamepad-reachable.** The mechanism is established (plan 08:
  document order + real, enabled, non-`aria-hidden`, non-zero-area elements) and both entries satisfy
  it by construction, but neither runtime's focus ring is observable from Jest.
- **That the Console Mode failure card is legible, or that the Dismiss chip receives focus on mount.**
  Structure is gated; pixels and focus are the manual gate's.
- **That `resolveConsoleActionIntent` is actually wired into both handlers.** The decision is
  unit-tested and the presence of the call is source-gated, but "this handler routes through that
  function" is not provable without a DOM. Carried to the manual gate as an explicit A-press step.
- That the Steam options dialog itself renders correctly when opened from either new entry — that is
  34.13-10 / 34.13-12's surface, reached by an entry point this plan only supplies.
</verification>

<handoffs>
## Work this plan deliberately does NOT contain

1. **`34.13-VALIDATION.md`'s GameSubMenu row is wrong** — it describes a relabel of an entry that
   does not exist. The corrected row is in `<verification>` above. **34.13-13 must fold in the
   correction, not the original**, or the gate will be scored against a control the phase never
   built. Also note `34.13-VALIDATION.md` has **no Console Mode row at all** — it predates D-29 —
   so all three D-29 rows above are additions, not edits.
2. **`34.13-13`'s `depends_on` still lists `34.13-04`, which is deleted** (already flagged by plan 08
   `<handoffs>` item 5). It must ALSO gain a dependency on this plan, since this plan mints three
   catalog keys 34.13-13 is the sole writer for. Orchestrator fix — this plan does not edit other
   plans' frontmatter.
3. **`ROADMAP.md`'s plan count for phase 34.13 is now 14 plans (04 deleted, 15 added)** and its plan
   list needs a `34.13-15-PLAN.md` row. Orchestrator fix; this plan was scoped to write exactly one
   new file and touched nothing else.
4. **The `install()` helper's `'default'` substitution** (`library.ts:69-74` replaces `'default'`
   with `defaultInstallPath` before marshalling) means the path reaching `InstallArgs` from Console
   Mode is GameLib's default install directory, not the literal `'default'`. Plan 08's conclusion is
   unaffected — it is still not a registered Steam library, so `resolveOverride()` falls back to the
   primary — but plan 08's stated MECHANISM is imprecise. Recorded in this plan's `<interfaces>` and
   owed to the SUMMARY; no code change follows.
5. **Console Mode's pre-existing A-button double-install on the transient "Opening Steam…" state** is
   fixed here as a consequence of T-34.13-15-05, not as a separate errand. It is a behavior change on
   a Steam surface and must be recorded as intentional in the SUMMARY so a later reader does not read
   it as scope creep.
6. **A gamepad-navigable Steam options surface** remains out of scope by D-29's explicit decision. If
   the manual gate finds users want to change the library from Console Mode, that is a new
   `/gsd-ui-phase` pass and a new decision — not an executor's judgement call, and not a change to
   this plan.
</handoffs>

<success_criteria>
- `GameCard` offers "Install with options…" as a **context-menu `Item`** (never a caret) for an
  uninstalled, non-delisted, non-queued, installable **Steam** game and for nothing else, calling
  `openSteamInstallOptions(appName, gameInfo)` directly with no prop threading through
  `GamesList`/`Library`/`RecentlyPlayed`, and the card's quick half is unchanged because plan 08
  already routed it via site 4.
- `GameSubMenu` offers a **NEW** "Install with options…" button — not a relabel, because RESEARCH Q4
  and plan 08 both verified there is nothing there to relabel — gated `!isInstalled && isSteam`,
  placed as the first child of `.submenu`, using the file's own
  `link button is-text is-link buttonWithIcon` idiom rather than `Dropdown` or `ContextMenu`, and its
  reachability is proven by the `GamePage:482 → DotsMenu:41 → GameSubMenu` mount chain rather than
  assumed.
- D-28 holds by construction: `runner === 'steam'` is evaluated for these two entries at exactly one
  site, proven by a named discriminator over four non-Steam runners observed failing when the
  conjunct is dropped, plus source gates showing GameCard's plain install `Item` and GameSubMenu's
  `{isInstalled && (` cluster are byte-unchanged.
- D-29 is closed with its open question answered and justified: Console Mode runs D-24's local check
  before installing, using the **same two pure decision functions** the desktop path uses (never
  `startSteamQuickInstall`, whose degrade would open the desktop dialog under a gamepad overlay), and
  a failed check produces a **terminal in-place failure card** built from classes already in the file
  — failure title in `--status-danger`, one static reason line, one Dismiss chip — with the 1500ms
  auto-dismiss timer armed only on the ok branch.
- The check cannot be bypassed: both the `BTN_ACTION` handler and the Enter/Space keydown branch
  route through `resolveConsoleActionIntent`, which returns `'dismiss'` for Steam unconditionally,
  with a discriminator proving all four non-Steam runners keep today's behavior exactly.
- D-24's fail-open rule carries over verbatim — an unknown verdict is not a failed one — proven by a
  named discriminator (rejected `checkDiskSpace` → install proceeds) observed failing when a
  rejection is treated as a failure.
- No filesystem path reaches a Console Mode screen: `libraryPath` matches zero times in the overlay's
  comment-stripped source, gate proven non-vacuous against a known-bad specimen.
- Plan 08's structural gate is preserved: `platformToInstall: 'Windows'` still appears exactly twice
  in non-test `src/frontend`.
- Zero new dependencies, zero new IPC channels, zero new persisted state, zero catalog writes; the
  four new `gamelib:` keys ship as inline `t()` defaults and are listed for 34.13-13, with
  `steam.install.withOptionsLabel` shared with plan 08 rather than duplicated.
- `npx tsc --noEmit` and `pnpm test:ci` both exit 0, and the diff touches exactly the eight paths in
  `files_modified` — no `common/`, no `backend/`, no `InstallGameModal.ts`, no `InstallModal/`, no
  `MainButton.tsx`, no locale catalog, no `package.json`.
</success_criteria>

<source_coverage>
## Source Coverage (this plan)

| Source | Item | Status |
|--------|------|--------|
| CONTEXT | **D-27 row 3** — GameCard: "button quick, context menu gains 'Install with options…'" | COVERED — Task 1. Quick half already shipped by plan 08 (site 4, zero edits); this plan adds the context-menu `Item`. D-27's cited line `:322` is dead sideload code and is deliberately not touched (RESEARCH Q4) |
| CONTEXT | **D-27 row 5** — GameSubMenu: "Fixed — options; item relabels" | COVERED **and CORRECTED** — Task 1 adds a NEW control. D-27's premise that an Install item exists to relabel is falsified by RESEARCH Q4's full-file read, by the UI-SPEC §GameSubMenu, and by plan 08's `<call_site_map>` row 5. The correction is written into the code comment so it is not re-reversed |
| CONTEXT | **D-28** — quick install and its options path are STEAM-ONLY; GOG/Epic/Amazon unchanged | COVERED — one evaluation site, two discriminator specs over four runners, source gates proving every pre-existing entry byte-unchanged, and `resolveConsoleActionIntent` spec B3 for the Console Mode input paths |
| CONTEXT | **D-29** — Console Mode gets D-24's check, no options path | COVERED — Task 2. The check is extended by composing plan 08's pure helpers (one definition, two degrade shapes); the "no options path" half is proven by a source gate returning zero for both `startSteamQuickInstall` and `openSteamInstallOptions` |
| CONTEXT | **D-29's OPEN FOR PLANNING** — "what Console Mode shows when the check fails" | **RESOLVED** — `<d29_resolution>`: an in-place terminal failure card in the overlay's existing Steam branch, no auto-dismiss, one dismiss chip, no path rendered. Justified against the existing Console Mode UI (six numbered points), with four rejected alternatives recorded and one created hazard (the A-button bypass) controlled by a unit-tested pure function |
| CONTEXT | **D-24** — a missing/full target degrades with the problem surfaced | COVERED for the Console Mode surface — Task 2 specs A3/A4 plus the failure card. The desktop degrade-into-dialog half is plan 08's + 34.13-10's and is correctly not duplicated here |
| CONTEXT | **D-25** — the options dialog opens instantly, zero IPC | HONOURED — both new menu entries call `openSteamInstallOptions` with two arguments and perform no IPC of their own |
| CONTEXT | Deferred Ideas (always-show setting, per-game override, "remember last used" library, Linux Proton path, download sizes) | CORRECTLY ABSENT — this plan reads no setting, adds no persisted state, calls no `getInstallInfo`, and makes no platform offer |
| RESEARCH | **Q4** — call-site reachability | HONOURED and EXTENDED. Q4's rows 2 and 4 are the two entries this plan builds. Q4's point 3 offered "thread a NEW second callback prop through `GamesList`/`Library`/`RecentlyPlayed`" as an alternative for GameCard — **not needed**: `GameCard/index.tsx:58` already imports the store module, so a direct import costs zero prop threading. Same for `GameSubMenu/index.tsx:15`. Recorded so the heavier option is not revived |
| RESEARCH | **Q2** — `Dropdown` is a caret primitive, not a split button | HONOURED BY EXCLUSION — neither surface uses `Dropdown`. GameCard uses its own older `ContextMenu`/`Item` mechanism; GameSubMenu uses a plain button. A source gate asserts `Dropdown` appears zero times in `GameSubMenu` |
| RESEARCH | **Q3** — `checkDiskSpace` is the local check, no new IPC | HONOURED — Console Mode reuses the same already-dual-ported channel with the same `steamappsDir` argument shape plan 08 uses |
| UI-SPEC | §"Split-Button Control Contract" → §GameCard, §GameSubMenu | COVERED — Task 1 follows both compositions. Two documented deviations, both following the target file's own convention over the snippet's literal text: GameSubMenu imports `Download as DownloadIcon` (the file's aliasing convention) and both files add a `useTranslation('gamelib')` hook rather than relying on prefix-only resolution (`InstallOverlay:374`'s precedent) |
| UI-SPEC | §"D-24 Degrade-into-Options Contract" | PARTIALLY APPLICABLE — its contract is written for the desktop dialog (34.13-10's). Console Mode inherits only the trigger conditions (`validPath === false`, free space below the floor) and the `--status-danger` shade, so the two degrades read as the same condition. Its dialog placement/`.infoBox` rules do not apply where there is no dialog, per D-29 |
| UI-SPEC | ⚠ CORRECTED cells (Q6 notice uses `.infoBox`, not `ThirdPartyDialog`'s slot) | NOT APPLICABLE to this plan — it renders no `InstallModal` content. Noted so the correction is not assumed missed |
| VALIDATION | Wave 0: both test files are new; no test covers any of these three surfaces today | CLOSED — Tasks 1 and 2, both RED-proven |
| VALIDATION | Hard constraint: no jsdom, no react-test-renderer | HONOURED — all three modified `.tsx` files import a stylesheet and cannot be imported at all, so every assertion is a pure-function spec or a comment-stripped source gate; the constraint is restated in both test headers and in `must_haves` |
| VALIDATION | Named vacuous-assertion risks 1 ("store-flip ≠ dialog renders") and 4 ("one click-through ≠ coverage") | ADDRESSED BY EXPLICIT SCOPING — stated in both test headers, in `must_haves`, in `<verification>`, and required in the SUMMARY. Risk 4 is answered by enumerating each surface separately in the manual gate rather than assuming a `MainButton` pass covers them |
| VALIDATION | Its GameSubMenu manual row presumes a relabel | **CORRECTED** — the replacement row is in `<verification>` and handed to 34.13-13 (`<handoffs>` item 1) |
| PATTERNS | ⚠ banner: these surfaces have NO pattern assignment; the `AlwaysShowSteamInstallForm` section is a retired trap | HONOURED — nothing here reads an always-show setting or builds those files; the two menu entries are built from the UI-SPEC + each target file's own idiom, and Console Mode (absent from the map entirely, predating D-29) from its own existing Steam branch |
| GOAL | ROADMAP phase goal (per RESEARCH Q7's rewrite) — an explicit "Install with options…" path alongside a one-click quick install, with a safe degrade when local checks fail | COVERED — this plan supplies the options path on the two surfaces plan 08 left, and extends the degrade to the one surface that had neither |
| REQ | — | No `REQ-` IDs minted for this phase (ROADMAP marks them TBD); coverage tracked against `D-NN`. Not a gap. |

**No unplanned items remain in this plan's boundary, and no item in plan 08's `<handoffs>` list
addressed to a sibling plan is left unowned by this one** — items 1, 2 and 6 are Tasks 1 and 2;
items 3, 4 and 5 were orchestrator/sibling-plan concerns already resolved or re-raised in
`<handoffs>` above. No phase split needed: 2 tasks, 8 files, four of them new and small.
</source_coverage>

<output>
Create `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-15-SUMMARY.md`
when done. It MUST include:
- the verbatim RED output from both tasks, each naming the not-yet-existing exports;
- the three deliberate-breakage asymmetries (dropped `runner` conjunct → A2/B2 only; rejected
  `checkDiskSpace` treated as failure → A7 only; intent gated on "blocked" instead of "steam" →
  B3 only), each with the specs that failed and the ones that stayed green;
- the FINAL exported signatures of both new modules — `showSteamCardInstallOptions`,
  `showSteamSubMenuInstallOptions`, `probeSteamQuickInstallTarget`, `ConsoleSteamVerdict`,
  `resolveConsoleActionIntent`, `steamBlockedMessageKey` — so a later reader need not re-read them;
- **the D-29 resolution as implemented**, restated in one paragraph with its justification, so the
  decision travels with the code rather than only with this plan;
- the four `gamelib:` catalog keys with their English defaults, flagged as owed to 34.13-13, with
  `steam.install.withOptionsLabel` explicitly marked SHARED with plan 08 and not to be minted twice;
- confirmation that `platformToInstall: 'Windows'` still returns exactly TWO non-test lines in
  `src/frontend`, with both named — plan 08's gate must survive this plan unchanged;
- the before/after counts for every structural gate in both tasks' acceptance criteria, including the
  pre-edit `link button is-text is-link buttonWithIcon` count and the pre-edit `setTimeout(`/
  `install(` ordering in the overlay;
- **the intentional behavior change** on Console Mode's transient "Opening Steam to install…" state:
  A now dismisses where it previously fired a second install. Record it as a consequence of
  T-34.13-15-05, not as scope creep;
- the correction to plan 08's stated mechanism for Console Mode's install path (`install()`
  substitutes `defaultInstallPath` for `'default'` before marshalling; the conclusion holds, the
  mechanism was imprecise);
- the corrected GameSubMenu manual-gate row and the three new Console Mode rows, flagged as
  obligations on 34.13-13;
- an explicit statement of the vacuity boundary: all three modified `.tsx` files import a stylesheet
  and cannot be imported into a Jest test, so nothing in this plan's green suite is evidence that any
  menu entry or failure card renders, is visible, or is reachable with a controller.
</output>
