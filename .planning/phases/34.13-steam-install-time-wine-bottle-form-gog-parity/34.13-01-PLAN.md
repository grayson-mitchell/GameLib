---
phase: 34.13-steam-install-time-wine-bottle-form-gog-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/types.ts
  - src/common/types/steam.ts
  - src/backend/storeManagers/steam/electronStores.ts
  - src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts
autonomous: true
requirements: [D-17, D-26]
user_setup: []

must_haves:
  truths:
    - "D-26 (RETIREMENT, not work): `AppSettings.alwaysShowSteamInstallForm` is NOT declared, and `GlobalConfig.getFactoryDefaults()` gains no such default. D-13's always-show setting is CUT — the caret's `Install with options…` affordance (D-21) replaces it, plan `34.13-04` is deleted, and its number is a permanent gap. The contract gate asserts the key's ABSENCE from both `src/common/types.ts` and `src/backend/config.ts`, so a future executor working from the pre-amendment PATTERNS map cannot silently reintroduce it."
    - "D-17: `InstallArgs` carries an explicit `steamForceWindowsViaBottle?: boolean` that is a SEPARATE field from `platformToInstall` — a Windows-via-bottle choice is expressible without reinterpreting the `platformToInstall: 'Windows'` value `installSteamGame()` already hardcodes for every Steam install."
    - "D-17: the Windows-depot-availability signal has a declared home on both sides of the cache boundary — `SteamMetadataCacheEntry.is_windows_native` (backend persistence) and `GameInfo.is_windows_native` (frontend hydration) — with `=== true` documented as the ONLY value that permits offering a Windows install."
    - "D-17 durability: `SteamMetadataCacheEntry.forcedWindowsViaBottle?: boolean` is declared as the PERSISTED verdict of a completed forced Windows-via-bottle install, under the SAME `=== true` / absent-means-never-forced contract as `is_windows_native` — so 34.13-14 can make `getSettings()`, `isNative()`, `launch()` and `uninstall()` agree with where the bits actually live. Declared only; nothing reads or writes it in this plan."
    - "The `isSteamBottleEligible` verdict object has exactly one declared shape (`SteamBottleEligibilityVerdict`), so plans 34.13-07 (IPC producer) and 34.13-09 (SteamBottleSetup consumer) compile against the same contract instead of two hand-rolled inline shapes."
    - "No behavior changes ship in this plan: `pnpm codecheck` exits 0 and `pnpm test:ci` stays green."
  artifacts:
    - path: "src/common/types.ts"
      provides: "GameInfo.is_windows_native (D-17), InstallArgs.steamForceWindowsViaBottle (D-17). NO always-show key — D-26 retired D-13."
      contains: "steamForceWindowsViaBottle"
    - path: "src/common/types/steam.ts"
      provides: "SteamBottleEligibilityVerdict — the isSteamBottleEligible IPC return shape"
      contains: "SteamBottleEligibilityVerdict"
    - path: "src/backend/storeManagers/steam/electronStores.ts"
      provides: "SteamMetadataCacheEntry.is_windows_native (the persisted D-17 depot-availability signal) and SteamMetadataCacheEntry.forcedWindowsViaBottle (the persisted D-17 forced-install verdict consumed by 34.13-14)"
      contains: "forcedWindowsViaBottle"
    - path: "src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts"
      provides: "RED-proven contract gate for all five declarations, plus the D-26 absence gate for the retired always-show key"
      min_lines: 60
  key_links:
    - from: "src/common/types/steam.ts SteamBottleEligibilityVerdict"
      to: "src/common/types.ts WineInstallation"
      via: "existing `import type { WineInstallation } from '../types'` at the top of steam.ts — already present, do not add a second import"
      pattern: "wineVersion\\?: WineInstallation"
---

<objective>
Declare every new cross-cutting type this phase needs, in one Wave-1 plan, so waves 2+ never
collide on `src/common/types.ts`.

Per the outline's multi-plan file table, **this is the only plan in phase 34.13 that edits
`src/common/types.ts`.** Every field any later plan needs must be declared here — 34.13-02
(backend read-side), 34.13-06 (`install()` override), 34.13-07 (IPC) and 34.13-08 (the frontend
marshalling seam) all run in later waves and consume these declarations without re-editing the file.

Purpose: 34.13-02 and 34.13-07 sit in Wave 2/3 and would otherwise both need to add a field to
`src/common/types.ts` in parallel. Consolidating the declarations into Wave 1 removes that
collision entirely and gives every downstream executor a fixed contract to compile against instead
of a scavenger hunt.

Output: five declarations (four fields + one interface) and one RED-proven contract test.
**Zero behavior change** — nothing reads or writes these fields in this plan.

## ⚠ AMENDED 2026-08-15 by D-26 — read before writing a line of code

The previous version of this plan ALSO declared `AppSettings.alwaysShowSteamInstallForm` and seeded
a `false` factory default in `src/backend/config.ts`, as D-13's half of the always-show setting.
**D-26 RETIRES D-13 and CUTS that setting outright. Plan `34.13-04`, which would have built its UI
and backend accessor, is deleted from disk; its number is a permanent gap.**

Consequently this plan:
- declares **no** `alwaysShowSteamInstallForm` key anywhere,
- edits `src/backend/config.ts` not at all — the file is out of `files_modified` entirely,
- and turns D-13 into an ABSENCE gate rather than a declaration. `34.13-PATTERNS.md` still carries
  an `AlwaysShowSteamInstallForm.tsx` / `alwaysShowInstallFormSetting.ts` section behind a 🛑
  banner, and this plan's contract test is what makes a future executor's accidental revival fail
  loudly instead of silently landing a dead `GlobalConfig` key.

Everything D-17 in this plan is UNCHANGED. In particular `InstallArgs.steamForceWindowsViaBottle` is
the contract 34.13-08 (now Wave 2) is frozen against — do not rename, widen or defer it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-CONTEXT.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-PLAN-OUTLINE.md

<interfaces>
<!-- Extracted from the codebase 2026-08-14. The executor should use these directly. -->

`src/common/types.ts` — the three insertion points, verified at these exact lines:

- `AppSettings` (interface opens L96). The mirror key is `enableSteamNativeInstall: boolean`
  at **L127** — note it is REQUIRED, not optional. Insert immediately after it.
- `GameInfo` (interface opens L194). The platform-flag cluster is
  `is_mac_native?: boolean` (L221), the `mac_arch` doc block (L222-226),
  `mac_arch?: '32' | '64' | 'unknown'` (L227), `is_linux_native?: boolean` (L228),
  `is_delisted?: boolean` (L231), `steamPlatformsCaptured?: boolean` (L234).
  Insert immediately after `is_linux_native` (L228).
- `InstallArgs` (L402-411) in full:
    path: string
    platformToInstall: InstallPlatform
    installDlcs?: Array<string>
    sdlList?: string[]
    installLanguage?: string
    branch?: string
    build?: string
    dependencies?: string[]
  `InstallParams extends InstallArgs` (L413) adds `appName` / `gameInfo` / `runner` / `size`.
  `SteamGame.install(args: InstallArgs)` is `games.ts:679` — the new field reaches the Steam
  backend through the existing parameter, no signature change anywhere.

`src/common/types/steam.ts` — already opens with `import type { WineInstallation } from '../types'`
and already exports `SteamBottleConfig` (`bottleName` / `wineVersion?` / `wineCrossoverBottle?` /
`provisioned`) plus `SteamUserData`, `RedeemKeyResult`. This is the established home for
Steam-specific shared types; the new verdict interface belongs here, NOT in `src/common/types.ts`.

`src/backend/storeManagers/steam/electronStores.ts` — `SteamMetadataCacheEntry` opens at L35.
Existing platform cluster: `is_mac_native?: boolean` (L42), `is_linux_native?: boolean` (L43),
`is_delisted?: boolean` (L46), `platformsCaptured?: boolean` (L51), `mac_arch?` (L58),
`mac_arch_verified?` (L61), `mac_arch_source?` (L68). The L39-41 comment currently reads
"Windows is the implicit baseline (no flag)" — that premise is what D-17 retires.
**Two fields land here, not one:** `is_windows_native` (the pre-install depot-availability signal,
captured from appdetails by 34.13-02) and `forcedWindowsViaBottle` (the post-install verdict written
by 34.13-14). They are different facts about different moments — "this game HAS a Windows build"
versus "this install IS the Windows build, in the bottle" — and collapsing them into one field would
make every mac-native game with a Windows depot read as already-forced.

`src/backend/config.ts` — `GlobalConfigV0.getFactoryDefaults(): AppSettings` opens at **L323**.
Its body is `const settings: Partial<AppSettings> = { ... }` (L327) returned under a
`// @ts-expect-error` (L381), and `enableSteamNativeInstall: false` sits at **L374**.
**This plan does NOT edit this file.** It is listed here only because the contract gate READS it
(via `readFileSync`) to prove D-26's retirement: no `alwaysShowSteamInstallForm` default exists.
Note why the read is necessary rather than relying on tsc: because the literal is typed
`Partial<AppSettings>`, neither a missing NOR a spurious key is a compile error here.

`src/backend/testUtils/stripSourceComments.ts` — exports `stripSourceComments(source: string)`
(block comments removed first, then whole comment-prefixed lines) and `stripTrailingLineComment`.
This is the shared, mandated comment-stripper for every source-text gate in this repo.

Jest: 5 projects; the Backend project (`src/backend/jest.config.js`) has
`roots: ['<rootDir>/src/backend']`, `testMatch: ['**/__tests__/**/*.test.ts']`, `resetMocks: true`,
and `modulePaths: ['./src/']` (tsconfig `baseUrl`). Single-file run: `npx jest <path> --silent`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the contract gate and RED-prove it against the current tree</name>

  <files>src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts</files>

  <read_first>
    - `src/backend/storeManagers/steam/__tests__/nativeInstallSetting.test.ts` — the in-repo
      precedent for a Steam backend test that asserts on `src/common/types.ts` SOURCE TEXT via
      `readFileSync(join(__dirname, '../../../../common/types.ts'), 'utf8')`. Copy its path
      arithmetic and its describe-block style verbatim.
    - `src/backend/testUtils/stripSourceComments.ts` — read the doc comment in full. It explains
      why a bare `grep`-style regex over raw source is a rotted gate, and that the shared stripper
      is mandatory rather than optional.
    - `src/backend/jest.config.js` — confirm `roots`, `testMatch` and `modulePaths` so the new file
      is picked up by the Backend project without any config edit.
    - `src/common/types.ts` lines 96-147 (`AppSettings`), 194-240 (`GameInfo`), 393-418
      (`WineInstallation` / `InstallArgs` / `InstallParams`) — the current, pre-edit state the gate
      must fail against.
    - `src/common/types/steam.ts` (full file, ~50 lines) — the current, pre-edit state.
    - `src/backend/config.ts` lines 323-383 — `getFactoryDefaults()`, and specifically that
      `const settings: Partial<AppSettings>` makes a missing default type-invisible.
    - `src/backend/storeManagers/steam/electronStores.ts` lines 25-78 — `SteamMetadataCacheEntry`,
      including the `mac_arch_verified` / `mac_arch_source` doc comments, which are the house style
      for a persisted verdict whose provenance matters.
  </read_first>

  <action>
Create `src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts` in the Backend
jest project. Write it BEFORE any declaration exists, and run it to prove it fails. This file is the
only automated evidence for a plan whose entire output is type declarations — a gate that has never
been observed failing is not evidence.

Import discipline: use `import type` for everything from `common/types` and `common/types/steam` so
no runtime module load occurs. Import `stripSourceComments` from `backend/testUtils/stripSourceComments`
(a value import — it is a pure function with no side effects). Do NOT import `backend/config`,
`../electronStores`, or anything that pulls `electron-store`; every assertion against those two files
is a source-text assertion via `readFileSync`.

Write four describe blocks:

1. `describe('D-17: Windows-via-bottle install override')` — a compile-time assertion. Declare a
   `const args: InstallArgs` literal that sets `path`, `platformToInstall: 'Windows'` and
   `steamForceWindowsViaBottle: true`, then `expect(args.steamForceWindowsViaBottle).toBe(true)`.
   Add a second case asserting `undefined` is a legal value (the legacy/no-override shape). Add a
   source assertion, over the comment-stripped `src/common/types.ts`, that `platformToInstall:
   InstallPlatform` is STILL declared unchanged — this is the guard against a future executor
   "simplifying" the override by folding it back into `platformToInstall`, which outline note 2
   proves would route every mac-native install into the bottle.

2. `describe('D-17: Windows depot-availability signal and forced-install verdict')` — a
   compile-time assertion that `GameInfo` accepts `is_windows_native: true` (declare a
   minimally-populated `Partial<GameInfo>` typed literal and read the field back), plus TWO source
   assertions over the comment-stripped `src/backend/storeManagers/steam/electronStores.ts`:
   (a) that `SteamMetadataCacheEntry` declares `is_windows_native?: boolean`; and
   (b) that it declares `forcedWindowsViaBottle?: boolean` — the persisted forced-install verdict
   34.13-14 consumes. Use `stripSourceComments` on that file before matching, because its existing
   L39-41 block comment already contains the words "Windows" and "native" and would otherwise
   satisfy a naive regex vacuously — and because the doc comment you write in Task 2 will itself
   name both identifiers in prose. Add a compile-time assertion that a
   `SteamMetadataCacheEntry`-typed literal accepts `forcedWindowsViaBottle: true` **and** compiles
   with the key entirely absent, pinning that it is optional (absent === never forced).

3. `describe('D-26: the always-show setting is RETIRED, not declared')` — an ABSENCE gate, not a
   declaration. Two source assertions, both over comment-stripped sources, both asserting ZERO
   matches for `alwaysShowSteamInstallForm`:
   (a) over `src/common/types.ts` — no such key on `AppSettings`;
   (b) over `src/backend/config.ts` — no such key in `getFactoryDefaults()`'s literal.
   Both assertions are load-bearing and neither can be replaced by a type check: `AppSettings` is
   constructed everywhere via `<AppSettings>{}` assertions and `as AppSettings` casts, and
   `getFactoryDefaults()`'s literal is typed `Partial<AppSettings>` — so tsc is structurally blind
   to a spurious key in either place.
   Title the `it()`s with the reason, not the mechanic: D-26 cut the setting because D-21's caret
   puts the options dialog one click away on every surface, so a global `GlobalConfig` toggle for
   the same capability is dead weight. `34.13-PATTERNS.md`'s `AlwaysShowSteamInstallForm.tsx`
   section is a retired trap kept only as a settings-toggle reference; this gate is what stops an
   executor reading it as a work item.
   **Note this block is the one describe that is expected to be GREEN on the pre-edit tree** — it
   asserts an absence that already holds. That is correct and must be stated in the test file's own
   header: it is a REGRESSION gate against revival, not a RED-proven declaration gate, so it is
   deliberately excluded from the four-identifier RED proof below.

4. `describe('isSteamBottleEligible verdict shape')` — a compile-time assertion that
   `SteamBottleEligibilityVerdict` is importable from `common/types/steam` and accepts
   `{ eligible: true }` alone as well as `{ eligible, wineVersion, bottleName }` fully populated.
   Assert `eligible` is required by constructing the minimal literal and reading it back.

Every source assertion must run its file through `stripSourceComments` first and must be written so
that the pre-edit tree fails it. Do not use a bare `grep -c` style count over unstripped text.

Then RED-prove the gate: run the test against the untouched tree and record the failure. The
expected failure mode is ts-jest type errors (TS2353 "Object literal may only specify known
properties" / TS2339) naming `steamForceWindowsViaBottle`, `is_windows_native` and
`forcedWindowsViaBottle`, plus a module-resolution/type error for `SteamBottleEligibilityVerdict` —
plus the two `electronStores.ts` source assertions failing. Capture that output in the plan SUMMARY.
Do NOT proceed to Task 2 until all FOUR identifiers have been observed in a real failure.
Describe block 3 (D-26) is expected to PASS in this run — it gates an absence that already holds.
Record that explicitly rather than letting it read as an under-counted RED.
  </action>

  <verify>
    <automated>! npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts --silent</automated>
  </verify>

  <acceptance_criteria>
    - The file `src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts` exists
      and contains exactly four `describe(` blocks.
    - `npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts --silent`
      exits NON-ZERO against the current (pre-Task-2) tree.
    - The captured failure output literally contains all FOUR strings:
      `steamForceWindowsViaBottle`, `is_windows_native`, `forcedWindowsViaBottle`,
      `SteamBottleEligibilityVerdict`. An observed failure that names fewer than four is NOT a valid
      RED proof — fix the gate, do not proceed.
    - `alwaysShowSteamInstallForm` does NOT appear in the RED failure output. If it does, describe
      block 3 was written as a presence gate rather than D-26's absence gate — fix it before
      continuing, because the "fix" a green-seeking executor would reach for is declaring the
      retired key.
    - The file imports `stripSourceComments` from `backend/testUtils/stripSourceComments` and does
      NOT import `backend/config` or `../electronStores` (assert by reading the import block: a
      runtime import of either would pull `electron-store` into the Backend project).
    - Every one of the five source-text assertions — the `platformToInstall` survival check, the two
      `electronStores.ts` declaration checks, and the two D-26 absence checks — applies
      `stripSourceComments` to its input before matching. Zero assertions match against raw,
      unstripped file contents. Stripping is load-bearing for the D-26 pair specifically: this plan's
      own doc comments and the retired-trap note in `34.13-PATTERNS.md`-derived prose both name the
      identifier, so an unstripped absence gate would fail on its own explanation.
    - The RED failure output is recorded verbatim in the plan SUMMARY.
  </acceptance_criteria>

  <done>The contract gate exists, has been executed against the unmodified tree, and has been observed failing while naming all four new identifiers — with the D-26 absence block passing, as designed.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the four fields and the verdict interface</name>

  <files>src/common/types.ts, src/common/types/steam.ts, src/backend/storeManagers/steam/electronStores.ts</files>

  <read_first>
    - `src/common/types.ts` lines 96-147, 194-240, 393-418 — the three insertion points and the
      surrounding doc-comment style (`GameInfo.mac_arch`'s block comment at L222-226 is the house
      style for a field that carries a non-obvious semantic contract; match its density).
    - `src/common/types/steam.ts` (full file) — `SteamBottleConfig`'s doc comment is the placement
      and commenting precedent, and its `import type { WineInstallation } from '../types'` at line 1
      is already present.
    - `src/backend/storeManagers/steam/electronStores.ts` lines 35-69 — `SteamMetadataCacheEntry`,
      including the L39-41 comment whose "Windows is the implicit baseline (no flag)" premise this
      task retires.
    - `src/backend/config.ts` lines 323-383 — `getFactoryDefaults()`. **Read-only.** This task does
      not edit it; Task 1's D-26 gate asserts no `alwaysShowSteamInstallForm` default appears there.
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-CONTEXT.md`
      §"Amendments — quick-install model", **D-26** in full — the retirement of D-13, the deletion of
      plan `34.13-04`, and why the caret replaces the setting. Do not declare anything it retired.
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-PLAN-OUTLINE.md`
      notes 2 and 3 — the binding rationale for why the override is a separate field and where the
      Windows signal comes from. Both must be paraphrased into the doc comments you write.
  </read_first>

  <action>
Make five declarations. Nothing reads or writes any of them in this plan — if you find yourself
editing `games.ts`, `library.ts`, `InstallGameModal.ts`, `main.ts`, `steamAuthFlowRegistration.ts`,
`src/preload/api/steam.ts`, `src/common/types/ipc.ts` **or `src/backend/config.ts`**, stop: those
belong to plans 34.13-02, 34.13-06, 34.13-07 and 34.13-08 — and `config.ts` belongs to nobody in
this phase, because D-26 cut the only reason to touch it.

1. `src/common/types.ts`, `GameInfo` — insert `is_windows_native?: boolean` immediately after
   `is_linux_native?: boolean` (L228). Doc comment must record: this is D-17's Windows-depot
   availability signal, mirroring `SteamMetadataCacheEntry.is_windows_native`; it is sourced from
   `data.platforms.windows` on the SAME `appdetails` response that already yields
   `data.platforms.mac` / `data.platforms.linux` (persisted by 34.13-02, hydrated onto `GameInfo`
   in `library.ts`); and — the load-bearing contract — **only `=== true` permits offering a Windows
   install.** `undefined` means "never captured" (pre-34.13 cache entries) and MUST NOT be coerced
   to available, because forcing a Windows install of a game with no Windows depot is an explicitly
   Deferred Idea. Reference the UI-SPEC rule that the Windows `MenuItem` is ABSENT (never
   present-but-disabled) when unavailable.

2. `AppSettings` — **NOTHING IS ADDED.** D-26 retired D-13, so no `alwaysShowSteamInstallForm` key
   is declared here or anywhere. `enableSteamNativeInstall: boolean` (L127) stays exactly as it is,
   with no new sibling. This item exists so the omission reads as a decision rather than an
   oversight; Task 1's describe block 3 is the gate that keeps it that way.

3. `src/common/types.ts`, `InstallArgs` — insert `steamForceWindowsViaBottle?: boolean` after
   `dependencies?: string[]` (L410). This is D-17's override signal. The doc comment MUST state the
   reason it is a separate field, because a future reader will otherwise "simplify" it away:
   `installSteamGame()` hardcodes `platformToInstall: 'Windows'` for EVERY Steam install and the
   Steam backend reads that field nowhere, so `platformToInstall === 'Windows'` cannot distinguish
   a deliberate user choice from the legacy default — reinterpreting it would route every
   mac-native install into the bottle. Also state the value contract: only `=== true` overrides;
   `undefined` and `false` are both "legacy routing, byte-identical to today" (the invariant
   34.13-06's regression guard pins). 34.13-06 is the only reader; 34.13-08 is the only writer.
   The field is a plain `boolean` by design — it carries no path, name or command, so it cannot
   widen the install surface beyond selecting among `SteamGame.install()`'s four existing branches.

4. `src/backend/storeManagers/steam/electronStores.ts`, `SteamMetadataCacheEntry` — insert
   `is_windows_native?: boolean` immediately after `is_linux_native?: boolean` (L43). Update the
   existing L39-41 comment so it no longer asserts "Windows is the implicit baseline (no flag)" as
   an unqualified truth — amend it to record that D-17 makes Windows availability an EXPLICIT
   captured flag, because a mac-native game may or may not have a Windows depot and the platform
   row's Windows option is gated on the answer. Repeat the `=== true` contract from item 1.

4b. `src/backend/storeManagers/steam/electronStores.ts`, `SteamMetadataCacheEntry` — also insert
   `forcedWindowsViaBottle?: boolean`, placed immediately after `is_windows_native` so the two D-17
   fields read as a pair. Give it its own doc block, in the density of the existing `mac_arch`
   comment, recording all five of:
   - **What it means:** this appId's CURRENT install was placed in the Phase 17 `GameLibSteam`
     bottle by a D-17 forced Windows-via-bottle install. It is a fact about *where the bits are*,
     not a preference and not a capability.
   - **How it differs from `is_windows_native`:** that field says the game HAS a Windows build
     (pre-install, from appdetails); this one says the installed copy IS the Windows build, in the
     bottle (post-install, written by GameLib). They are never interchangeable.
   - **The value contract, identical to `is_windows_native`'s:** only `=== true` has any effect.
     `undefined` and `false` both mean "never forced." **Absent is the shape of EVERY pre-34.13
     cache entry**, so the fail-safe direction is one-way: an unreadable or missing flag must route
     exactly as today, never into the bottle.
   - **Who writes it:** 34.13-14 only, and only after a forced install actually committed bits to
     the bottle — never on a deferral, a rejected dispatch or a failed download. It is cleared by
     34.13-14 when the bottle's appmanifest is confirmed absent (uninstall complete), so a later
     native re-install does not inherit a stale verdict.
   - **Who reads it:** 34.13-14's `isBottleEligible()` seam only — which is why `getSettings()`,
     `isNative()`, `launch()` and `uninstall()` all stay consistent with one another. Note
     explicitly that the Phase 24 bridge predicates deliberately do NOT read it (the bridge bottle
     is a different filesystem root that never held these bits).
   ⚠ **Carry-forward warning to record in the comment:** `steamMetadataStore.set` REPLACES the whole
   entry (T-18-02-04), so this field must be explicitly carried forward by any write that enumerates
   its payload — exactly as `mac_arch_verified` / `mac_arch_source` already are. 34.13-14 owns that
   edit; the warning belongs next to the declaration so a future writer sees it.

5. `src/common/types/steam.ts` — add `export interface SteamBottleEligibilityVerdict` with
   `eligible: boolean`, `wineVersion?: WineInstallation`, `bottleName?: string`. Place it after
   `SteamBottleConfig`. Reuse the existing top-of-file `import type { WineInstallation } from
   '../types'` — do not add a second import. Doc comment: this is the return shape of the
   `isSteamBottleEligible` IPC channel (registered by 34.13-07 in BOTH `main.ts` and
   `steamAuthFlowRegistration.ts`); `wineVersion` / `bottleName` are shaped after
   `getSteamBottleSettings()`'s own fields and exist so ONE round-trip also closes D-15's exposure
   half — `SteamBottleSetup.tsx` (34.13-09) reads the persisted `wineVersion` from here instead of
   re-deriving it via `resolveSteamBottleEngine`. Note explicitly that the `AsyncIPCFunctions`
   signature for the channel is 34.13-07's edit to `src/common/types/ipc.ts`, not this plan's.

6. `src/backend/config.ts` — **NO EDIT.** `getFactoryDefaults()` gains no new default. Adding one
   would create a persisted `GlobalConfig` key that nothing reads, on a code path D-26 deleted.

Then run the gate GREEN and run the full suite to confirm zero behavior change.
  </action>

  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts --silent && pnpm codecheck && pnpm test:ci</automated>
  </verify>

  <acceptance_criteria>
    - `npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts --silent`
      exits 0 with all four describe blocks passing (the same command that exited non-zero in Task 1).
    - `pnpm codecheck` (`tsc --noEmit`) exits 0.
    - `pnpm test:ci` exits 0 — no pre-existing suite regressed. In particular
      `src/backend/sidecar/__tests__/settingsFlows.test.ts` and
      `src/backend/storeManagers/steam/__tests__/nativeInstallSetting.test.ts` (both of which
      construct `Partial<AppSettings>` casts) still pass.
    - `src/common/types.ts` declares, in the comment-stripped source, both of:
      `is_windows_native?: boolean` inside `GameInfo` and `steamForceWindowsViaBottle?: boolean`
      inside `InstallArgs`.
    - **D-26 absence, re-asserted after the edit:** `alwaysShowSteamInstallForm` matches ZERO times
      in the comment-stripped `src/common/types.ts` AND ZERO times in the comment-stripped
      `src/backend/config.ts`. A single match in either is a FAILED acceptance — it means the
      retired D-13 setting was revived from `34.13-PATTERNS.md`'s trap section.
    - `src/common/types.ts` still declares `platformToInstall: InstallPlatform` on `InstallArgs`,
      unchanged — the override is additive, not a replacement.
    - `src/common/types/steam.ts` exports `SteamBottleEligibilityVerdict` with `eligible: boolean`
      required and `wineVersion?: WineInstallation` / `bottleName?: string` optional, and contains
      exactly ONE `import type { WineInstallation }` statement.
    - `src/backend/storeManagers/steam/electronStores.ts` comment-stripped source contains BOTH
      `is_windows_native?: boolean` and `forcedWindowsViaBottle?: boolean` inside
      `SteamMetadataCacheEntry`, and its pre-existing platform comment no longer states
      "Windows is the implicit baseline (no flag)" without the D-17 qualification.
    - `forcedWindowsViaBottle` is declared OPTIONAL (`?:`). A required declaration is a FAILED
      acceptance — every pre-34.13 cache entry on disk lacks the key, and a required field would
      make the persisted shape unrepresentable rather than fail-safe.
    - `forcedWindowsViaBottle`'s doc comment names `34.13-14` as its sole reader/writer, states the
      `=== true`-only contract, states that absent means never-forced, and carries the T-18-02-04
      carry-forward warning. A declaration without the carry-forward warning is a FAILED acceptance:
      the silent-drop failure it prevents is the field reverting a forced game to native routing on
      the next metadata fetch.
    - `git diff --name-only` lists ONLY the four files in this plan's `files_modified`. Zero edits to
      `src/backend/config.ts`, `games.ts`, `library.ts`, `InstallGameModal.ts`, `main.ts`,
      `steamAuthFlowRegistration.ts`, `src/preload/api/steam.ts`, or `src/common/types/ipc.ts`.
      `git status --porcelain src/backend/config.ts` returns empty.
    - Every doc comment written names the decision it implements (`D-17`, or `D-26` for the two
      "deliberately absent" notes) and, for the two `=== true` contracts and the `platformToInstall`
      rationale, states the failure mode it prevents.
  </acceptance_criteria>

  <done>All five declarations exist, no always-show key was declared anywhere, the RED-proven contract gate is GREEN, `tsc --noEmit` and the full suite are clean, and the diff touches only this plan's four files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → backend IPC (`window.api.install`) | `InstallArgs` crosses here. This plan DECLARES `steamForceWindowsViaBottle` on that payload; no code sets or reads it until 34.13-08 / 34.13-06. |
| backend → renderer IPC (`isSteamBottleEligible`) | `SteamBottleEligibilityVerdict` crosses here. Declared only; the channel is registered by 34.13-07. |
| local filesystem config store (`steam_metadata` CacheStore) | `SteamMetadataCacheEntry.is_windows_native` and `forcedWindowsViaBottle` persist here. Already-trusted local, single-user store. **`GlobalConfig` is NOT crossed by this plan** — D-26 cut the only key that would have landed there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.13-01 | Tampering | `InstallArgs.steamForceWindowsViaBottle` — a renderer-controlled value that will influence backend install routing in 34.13-06 | mitigate | Type-level containment, landed in this plan: the field is declared `boolean`, not `string`/path/`InstallPlatform`. It can therefore only SELECT among `SteamGame.install()`'s four pre-existing branches; it cannot express a filesystem path, a bottle name, or a command. Every branch it can select still passes through the untouched `sanitizeBottleName` (`bottle.ts:166-179`) and `SAFE_INSTALLDIR` (`installLocation.ts:91-128`) guards. The doc comment pins `undefined`/`false` as byte-identical-to-today, and 34.13-06 owns the RED-proven regression guard for that invariant. |
| T-34.13-02 | Information Disclosure | `SteamBottleEligibilityVerdict.wineVersion` carries a `WineInstallation`, whose `bin` field is an absolute host filesystem path, toward the renderer | accept | No new class of data crosses the boundary. `WineInstallation` objects — including `bin`, `lib`, `lib32`, `wineserver` paths — are already returned to the renderer wholesale by the existing `getAlternativeWine()` and `requestAppSettings()` channels, which `InstallModal` and `SteamBottleSetup.tsx` both already consume. The renderer is a first-party, non-remote surface in this app. Declaration-only in this plan; nothing transmits until 34.13-07. |
| T-34.13-03 | Elevation of Privilege | ~~New `AppSettings.alwaysShowSteamInstallForm` GlobalConfig key~~ — **RETIRED by D-26; no such key is declared.** The residual threat is its silent REVIVAL: a future executor reading `34.13-PATTERNS.md`'s trap section lands a persisted `GlobalConfig` key that gates a user-visible install behaviour with no owning decision and no reader. | mitigate | Task 1 describe block 3 asserts ZERO comment-stripped matches for `alwaysShowSteamInstallForm` in BOTH `src/common/types.ts` and `src/backend/config.ts`, and Task 2's acceptance re-asserts the same absence after the edit. `src/backend/config.ts` is out of `files_modified` and its cleanliness is gated by `git status --porcelain`. This is a contract-integrity control rather than a classical security one, registered here because the retired model routed a user-visible behaviour change through a persisted config key. |
| T-34.13-05 | Tampering | `SteamMetadataCacheEntry.forcedWindowsViaBottle` — a persisted flag that 34.13-14 will use to re-route `launch()` and, critically, `uninstall()` | accept | **Declaration-only in this plan; the operative mitigation is 34.13-14's (T-34.13-14-01/02/03), which treats it as a real integrity surface rather than an accepted local-cache risk.** What lands here is the type-level containment: the field is `boolean`, optional, and `=== true`-gated by contract, so it can only SELECT among `SteamGame`'s pre-existing bottle/native branches — it carries no path, bottle name or command, and every branch it can select still passes through the untouched `sanitizeBottleName` / `getBottleSteamappsDir` guards. Nothing reads or writes it in this plan. |
| T-34.13-04 | Spoofing / Tampering | `SteamMetadataCacheEntry.is_windows_native` — a cached flag that will gate whether a Windows install option is offered for a mac-native game | accept | Local cache poisoning requires filesystem write access to the user's own app-support directory, at which point the attacker already controls the install path outright. The `=== true`-only contract this plan writes into the doc comment is a correctness guard (fail-closed on an uncaptured/legacy entry), not a security control. Consumed by 34.13-12; nothing reads it in this plan. |
| T-34.13-SC | Tampering | npm/pip/cargo installs | n/a — **Package Legitimacy Gate is NOT APPLICABLE to this phase.** RESEARCH.md §"Standard Stack" states "No new libraries. This phase composes existing in-repo primitives only," and explicitly records "No package installs, so `## Package Legitimacy Audit` is not applicable to this phase." This plan installs nothing and adds no dependency to `package.json`. If any executor finds itself running a package-manager install, that is out-of-scope work: stop and return to planning. | The standing control remains armed for any future plan in this phase: an install task would require the RESEARCH.md audit table plus a blocking human-verify checkpoint for every `[ASSUMED]`/`[SUS]` package. |

**ASVS L1 posture for this plan:** V5 (Input Validation) is the only marginally-relevant category,
and this plan adds no validation surface — it declares a boolean and two optional booleans. V2/V3/V4/V6
(auth, session, access control, cryptography) are untouched, matching RESEARCH.md §"Security Domain".
</threat_model>

<verification>
## Per-task verification map (for `34.13-VALIDATION.md`, filled by 34.13-13)

| Task ID | Wave | Decision | Threat Ref | Test Type | Automated Command | File Exists |
|---------|------|----------|------------|-----------|-------------------|-------------|
| 34.13-01-01 | 1 | D-17, D-26 | T-34.13-01, T-34.13-03 | unit (contract gate, RED-proven) | `! npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts --silent` | ❌ Wave 0 — created by this task |
| 34.13-01-02 | 1 | D-17, D-26 | T-34.13-01, T-34.13-03 | unit + static | `npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts --silent && pnpm codecheck && pnpm test:ci` | ✅ (created in task 1) |

## Phase-level checks owed by this plan

- `pnpm codecheck` exits 0.
- `pnpm test:ci` exits 0 — this plan is a no-behavior-change plan, so any suite that flips red is a
  real regression, not an expected update.
- The contract gate has been observed RED before it was observed GREEN. A gate that only ever passed
  is not evidence (this repo has three ledgered instances of a passing test guarding nothing).

## Explicitly NOT verified here (owned by later plans)

- That anything WRITES `is_windows_native` (34.13-02) or READS it (34.13-12).
- That anything WRITES, READS or CLEARS `forcedWindowsViaBottle` (34.13-14). Declaring the field is
  not evidence that a forced install is durable.
- That anything SETS `steamForceWindowsViaBottle` (34.13-08) or HONORS it (34.13-06).
- Nothing about an always-show setting is verified here **because there is nothing to verify**:
  D-26 retired D-13 and deleted plan `34.13-04`. What IS verified is its absence.
- That `isSteamBottleEligible` is registered on either runtime or exposed through the preload seam
  (34.13-07). Declaring the verdict TYPE is not evidence of a wired channel.
</verification>

<success_criteria>
- `src/common/types.ts` declares `GameInfo.is_windows_native?: boolean` and
  `InstallArgs.steamForceWindowsViaBottle?: boolean`, each with a doc comment naming D-17 and the
  failure mode it prevents — and declares NO `alwaysShowSteamInstallForm` key (D-26).
- `src/common/types/steam.ts` exports `SteamBottleEligibilityVerdict`.
- `src/backend/storeManagers/steam/electronStores.ts` declares
  `SteamMetadataCacheEntry.is_windows_native?: boolean` and
  `SteamMetadataCacheEntry.forcedWindowsViaBottle?: boolean` (both optional), and its stale
  "Windows is the implicit baseline" comment is amended.
- `src/backend/config.ts` is UNTOUCHED and contains no `alwaysShowSteamInstallForm` default (D-26).
- `src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts` exists, was observed
  FAILING against the pre-edit tree while naming all four new identifiers, and now passes — with its
  D-26 absence block green in BOTH runs, recorded as such.
- `pnpm codecheck` and `pnpm test:ci` both exit 0.
- The diff touches exactly the four files in `files_modified` — no source file that reads or writes
  these fields is modified in this plan.
</success_criteria>

<handoffs>
Facts later plans need, recorded here so they are not re-derived.

**→ EVERY later plan (D-26):** `AppSettings.alwaysShowSteamInstallForm` **does not exist and must
not be created.** If a downstream plan's `<upstream_contract>` tells you to grep for it and confirm
it landed, that plan is stale against D-26 — report the delta, do not declare the key.

**→ 34.13-08 (Wave 2, the only writer of the override):** `InstallArgs.steamForceWindowsViaBottle`
is FINAL as declared here — a plain optional `boolean`, `=== true`-only. 34.13-08's
`installSteamGame(appName, gameInfo, path = '', forceWindowsViaBottle = false)` marshals into it and
is this phase's sole write site.

**→ 34.13-06 (Wave 3):** `InstallArgs.steamForceWindowsViaBottle` is the ONLY field to read for the
install-time override. `platformToInstall` stays hardcoded `'Windows'` and stays unread by the Steam
backend.

**→ 34.13-14 (Wave 4, the sole consumer of `forcedWindowsViaBottle`):**
- The persisted field is `SteamMetadataCacheEntry.forcedWindowsViaBottle?: boolean`, declared in
  `src/backend/storeManagers/steam/electronStores.ts`. This plan is the only plan in the phase that
  edits that file — 34.13-14 must NOT re-declare it, widen it, or move it.
- Value contract, identical to `is_windows_native`'s: **only `=== true` has any effect; `undefined`
  and `false` both mean "never forced."** Absent is the shape of every pre-34.13 cache entry, so the
  fail-safe direction is one-way — an absent flag must route exactly as today.
- The field is deliberately SEPARATE from `is_windows_native`. That one is a capability captured
  pre-install from appdetails; this one is a fact about where the current install physically landed.
  A single merged field would read every Windows-capable mac-native game as already-forced.
- ⚠ The declaration's doc comment carries the T-18-02-04 carry-forward warning:
  `steamMetadataStore.set` replaces the whole entry, so 34.13-14 owes an explicit carry-forward in
  `fetchMetadataIfNeeded`'s enumerated payload. Without it the verdict is silently dropped on the
  next appdetails fetch and the forced game reverts to native routing intermittently.
</handoffs>

<output>
Create `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-01-SUMMARY.md`
when done. It MUST include:
- the verbatim RED output from Task 1 (the four identifiers named in a real failure), plus an
  explicit note that describe block 3 (D-26 absence) was GREEN in both the RED and the GREEN run,
- the exact final field names and their declaring file:line, so 34.13-02/06/07/08/09/12/14 can
  be written against a fixed contract without re-reading `src/common/types.ts` or
  `electronStores.ts`,
- **an explicit statement that `AppSettings.alwaysShowSteamInstallForm` was NOT created and
  `src/backend/config.ts` was not edited (D-26)** — 34.13-05's Task 1 `read_first` instructs its
  executor to confirm exactly this from this SUMMARY and to treat its presence as a blocking finding,
- the verbatim doc comment written for `forcedWindowsViaBottle`, so 34.13-14 can implement against
  the contract it states rather than re-deriving it,
- confirmation that `src/common/types.ts` is now closed to further edits for this phase.
</output>
