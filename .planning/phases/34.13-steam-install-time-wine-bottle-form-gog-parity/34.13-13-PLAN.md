---
phase: 34.13-steam-install-time-wine-bottle-form-gog-parity
plan: 13
type: execute
wave: 8
depends_on:
  - 34.13-01
  - 34.13-02
  - 34.13-03
  - 34.13-05
  - 34.13-06
  - 34.13-07
  - 34.13-08
  - 34.13-09
  - 34.13-10
  - 34.13-11
  - 34.13-12
  - 34.13-14
  - 34.13-15
files_modified:
  - public/locales/en/gamelib.json
  - .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VALIDATION.md
  - .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md
autonomous: false
requirements:
  - D-06
  - D-20

must_haves:
  truths:
    - "D-06 is observable: on every opens=YES row of the Section-Gating Matrix the Steam install dialog shows NO download-size and NO install-size row anywhere, and its Install button is enabled and clickable in every state — never disabled, greyed, or gated on diskSize or on free space."
    - "D-20 is observable: on a Windows host with no library choice (Matrix row 5 — native install OFF, or ON with ≤1 library) the dialog renders exactly one read-only 'Windows' platform row, the content-light notice, and Cancel/Install — nothing else, no empty-state illustration or heading. D-20's expectation-setting copy is carried by the `gamelib:steam.install.contentLightNotice` line in an `.infoBox` inside the dialog (rows 5 and 7), NOT by a settings tooltip — D-26 cut the always-show setting that previously hosted it."
    - "Every one of the six new gamelib: catalog values is byte-identical to the inline English default shipped at its call site in src/ — a catalog value that differs from the inline default is a defect, not a variant."
    - "All nine opens=NO Matrix rows (2, 3, 3a, 6, 6a, 6b, 10, 10a, 10b) are each individually observed to open NOTHING — Phase 21 D-09 zero-friction survives, and no row is marked PASS by inference from another row."
    - "All seven opens=YES Matrix rows (1, 4, 5, 7, 8, 11, 12) are each individually observed to render exactly the four region columns the Matrix specifies for that row."
    - "The phase is exercised on BOTH runtimes — Electron and `pnpm tauri:dev` (never `tauri dev`, which serves a stale static bundle) — and a channel registered on only one runtime is caught rather than passed over."
    - "A forced Windows-via-bottle install of a mac-native game IS launchable, and its settings screen resolves the bottle's wine version — a real PASS/FAIL gate, because 34.13-14 closed T-34.13-06-06. The 'record it either way, a FAIL is expected' framing 34.13-06 shipped no longer applies."
    - "Uninstalling a forced Windows-via-bottle install actually removes the bottle copy — verified ON DISK under the Phase 17 GameLibSteam bottle's steamapps, not inferred from the library badge flipping."
    - "Reversibility holds: after uninstalling a forced install, re-installing the same game with NO override installs and launches NATIVELY — a stale forced verdict is the single most likely new bug 34.13-14 could ship, so it is observed rather than assumed."
    - "The full Jest suite is green AND the manual gate is executed — a green suite alone does not close this phase."
  artifacts:
    - path: "public/locales/en/gamelib.json"
      provides: "The six new gamelib: keys for this phase, nested and alphabetised, values byte-identical to their inline source defaults"
      contains: "sharedBottleNotice"
    - path: ".planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md"
      provides: "The manual gate ledger — one row per enumerated gate item with an explicit PASS / FAIL / NOT ATTEMPTED disposition and the observation that produced it"
      contains: "G-ROW-2/3-noWin"
    - path: ".planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VALIDATION.md"
      provides: "Per-Task Verification Map filled from every sibling plan's own map block"
      contains: "nyquist_compliant"
  key_links:
    - from: "public/locales/en/gamelib.json"
      to: "src/frontend/**/*.tsx call sites"
      via: "catalog value === inline default of the t('gamelib:<key>', '<default>') call"
      pattern: "gamelib:(steam\\.install|settings|help|status)\\."
    - from: ".planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md"
      to: "34.13-UI-SPEC.md Section-Gating Matrix"
      via: "one ledger row per Matrix row id, all sixteen present"
      pattern: "G-ROW-(1|2|3|3a|4|5|6|6a|6b|7|8|10|10a|10b|11|12)\\b"
---

<objective>
Close Phase 34.13. Two halves:

1. **Localisation.** Write every new `gamelib:` key from the UI-SPEC Copywriting Contract into
   `public/locales/en/gamelib.json`. This plan is the phase's **single catalog writer** — every
   feature plan deliberately shipped its string as an inline English default
   (`t('gamelib:key', 'English default')`, the live `WineVersionSelector.tsx:28,33` precedent) and
   deliberately left the catalog untouched. This plan transcribes those defaults, and proves each
   catalog value is byte-identical to the default actually shipped in source.

2. **The blocking manual phase gate.** The primary deliverable of this phase — the form opening for
   the right games, staying shut for the nine zero-friction cases, and the chosen wine version
   reaching the bottle store — is structurally unprovable in Jest (`34.13-VALIDATION.md`: no jsdom,
   no react-test-renderer). A green suite is a precondition, not the gate.

Purpose: the phase cannot be marked complete on automated evidence, because the automated evidence
is known to be blind to the phase's actual subject. Every sibling plan explicitly deferred its
rendered behaviour here. This plan is where those deferrals are collected and discharged.

Output: an updated `en/gamelib.json`, a filled `34.13-VALIDATION.md` Per-Task Verification Map, and
`34.13-UAT.md` carrying an explicit disposition for every enumerated gate item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UI-SPEC.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-CONTEXT.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VALIDATION.md
</context>

<verified_findings>
Measured against the working tree on 2026-08-14, before this plan was written. Each of these is a
fact the executor would otherwise have to rediscover, and two of them change what a gate here can
legitimately claim.

1. **`pnpm lint-translations:gamelib` NEVER exits non-zero, and it is noisy today.** Measured:
   exit code `0`, **856 lines** of output, of which **53 are `Error: ENOENT`** stack traces. Cause:
   `meta/lintTranslations.ts` has no `process.exit` anywhere, and its `readFile()` catches every
   error and `console.log`s it (`lintTranslations.ts:58-67`). `gamelib.json` exists in **exactly one
   locale** — `public/locales/en/` — so all 48 other locale directories produce an ENOENT per run.
   **Consequence: the gate as briefed ("`pnpm lint-translations:gamelib` green") is vacuous if
   "green" is read as "exit 0" — it is 0 today, it will be 0 after this plan, and it would be 0 with
   a deliberately corrupt catalog.** Task 1 therefore gates on a **before/after output diff**, not
   on the exit code. This repo already carries multiple ledgered instances of a passing check that
   guarded nothing; do not add another.
2. **The linter cannot currently report a real gamelib finding at all.** `checkLanguage()` skips
   `en` (`lintTranslations.ts:177`) and iterates the *translation* file's keys, so with no non-`en`
   `gamelib.json` in the tree there is no file for it to check. Measured: **0** `Empty translation`
   lines and **0** `Missing content in translation` lines. The D-14 "one flat string, no `<Trans>`
   indexed tag" decision (34.13-10) is therefore **future-proofing, not a live failure** — it stays
   binding (it is frozen by 34.13-10 and this plan must not renegotiate it), but do not cite it in
   the SUMMARY as a measured avoided failure.
3. **Handoff 6 confirmed exactly.** In `public/locales/en/gamepage.json`: `button.install` =
   `"Install"`, `install.disk-space-left` = `"Space Available"`, `game.platform` = `"Select
   Platform Version to Install"` all already exist; `install.steam-location.title` and
   `install.steam-location.primary` are **`undefined`** — inline-default-only, reused as-is. None of
   these five are `gamelib:` keys and **none of them may be added to `gamelib.json`**.
4. **Catalog shape.** `public/locales/en/gamelib.json` is 102 lines, **4-space indented**
   (`i18next-parser.config.js: indentation: 4`), with top-level and nested keys in **alphabetical
   order**. `settings.wineDefaultLabel` = `"Wine Default"` is the exact precedent this plan follows:
   the catalog value equals the inline default at `WineVersionSelector.tsx:28` verbatim. Some keys
   (`redeemKey.*`) are legitimately empty strings — that is an i18next-parser artefact
   (`defaultValue: ''`), **not** the convention for keys with real inline defaults.
5. **`SteamInstallLocationPicker` is still referenced.** Measured: `grep -rn
   "SteamInstallLocationPicker\|useSteamInstallLocation" src/` returns **10** matches today.
   34.13-08 (Wave 4) retires it. This plan re-runs that grep as a phase gate and expects **0**.
6. **Available scripts:** `pnpm codecheck` = `tsc --noEmit`; `pnpm test:ci` = `jest --runInBand
   --silent`; `pnpm lint` = `eslint --cache .`.
8. **⚠ Task 3's `<automated>` ledger check is currently UNPROVEN — the prior RED-proof is STALE and
   must NOT be inherited.** The earlier proof was taken against a filled **32-row** specimen using
   the retired id set (`G-ROW-3a`, `G-ROW-5-noWin`, `G-HANDOFF-D12`, `G-SETTING`, …). The
   2026-08-15 amendment (D-21..D-29) changed **both** the count (32 → **36**) and the alternation
   (new `G-QUICK-*`, `G-D24-*`, `G-D29-CONSOLE`, `G-GAMEPAD-CARET`, `G-D25-INDIALOG` ids; the
   `G-ROW-[0-9]*[ab]*` branch replaced by `G-ROW-[1-8]` plus `G-ROW-2/3-noWin`). **The shipped
   command has never been executed against a specimen.**
   This is exactly the condition the prior version of this note warned about in its own last
   sentence — *"if any future plan adds an item id, this proof is stale again — re-take it, do not
   inherit it"* — and it now applies to the note itself.
   **Re-taking the proof is part of Task 3**, not a prerequisite someone else discharged. Required
   discriminators, each measured independently against a filled 36-row specimen: exit `0` on a
   complete ledger; exit `1` when a row is deleted; exit `1` when an unredacted 17-digit SteamID64
   is appended; exit `1` when a **retired** id (`G-SETTING`, `G-HANDOFF-D12`, `G-ROW-11`) is
   appended — this branch is new and has never been exercised; and `grep -c "G-ROW-2/3-noWin"`
   returning `1`, confirming the alternation still resolves leftmost-longest so the
   `G-ROW-[1-8]` branch does not swallow it. Record every observation in the SUMMARY.
   It remains a **well-formedness** check only — it can never show that any gate item passed.
7. **The Steam metadata cache is a `CacheStore` named `steam_metadata`**
   (`src/backend/storeManagers/steam/electronStores.ts:27-30`) — i.e. `steam_metadata.json` in
   GameLib's app-support directory. That is the file the **D-25** cold-cache gate item
   (`G-D25-INDIALOG`) clears.
</verified_findings>

<falsified_briefs>
Briefs this plan received that are wrong, and what replaces them.

1. **"all 14" Section-Gating Matrix rows** (`34.13-PLAN-OUTLINE.md` row 34.13-05, and the outline's
   description of this plan). **FALSIFIED** — 34.13-05 already found this and the orchestrator
   carried it forward. The UI-SPEC is source of truth and has **16** data rows:
   `1, 2, 3, 3a, 4, 5, 6, 6a, 6b, 7, 8, 10, 10a, 10b, 11, 12`. **There is no row 9.** Nine are
   `opens=NO`, seven are `opens=YES`. This plan enumerates all sixteen individually.
2. **"`pnpm lint-translations:gamelib` green" as a gate.** **FALSIFIED as written** — see
   `<verified_findings>` item 1. Replaced by an output-diff gate.
3. **"read ALL sibling plans 34.13-01 … 34.13-12".** When this plan's drafting began, only
   **34.13-01 … 34.13-10** existed on disk; 34.13-11 and 34.13-12 were written concurrently and
   landed before this plan was finalised. The two catalog keys they own were therefore initially
   transcribed from the UI-SPEC Copywriting Contract / D-12 Busy State Contract, and then
   **re-checked against the owner plans once they appeared. Both match verbatim:**
   - `gamelib:steam.install.forceWindowsHelp` — 34.13-12 `<action>` (~L590) freezes the identical
     string, em dash included.
   - `gamelib:status.checkingSteamInstallOptions` — 34.13-11 (~L313) freezes
     `Checking install options…` and explicitly names the **U+2026** ellipsis codepoint.

   This raises confidence but does **not** discharge the check: a frozen plan string is still not
   the string the executor shipped. **Task 1 must reconcile all six keys against the inline default
   actually present in source and treat any divergence as a defect to report, not to silently
   absorb.**
</falsified_briefs>

<phase_notes>
Facts carried in from sibling plans that change how this gate is read. Do not compress these away.

- **The mid-phase blank window is expected (34.13-08 handoff).** Between Wave 4 and Wave 6,
  `useInstallGameModal` flips `isOpen` and **nothing renders**, because `InstallModal/index.tsx`
  has no Steam branch until 34.13-12 (Wave 6). If a mid-phase manual check observed a dead click
  in that window, that was **not** a regression and must not be re-litigated here. This gate runs
  after Wave 6; at that point a dead click **is** a failure.
- **Wine-version handoff crosses two independent code paths.** 34.13-07/34.13-10 own the write
  (`persistBottleWineVersion`), 34.13-09 owns the read (`SteamBottleSetup` seeding). A green suite
  covers each half separately and **neither end to end**. Gate item G-HANDOFF-D15 is the only
  evidence they meet.
- **The D-17 override IS persisted — `T-34.13-06-06` was CLOSED by 34.13-14 (Wave 4).** 34.13-06
  shipped the override as install-time-only and escalated the post-install divergence with
  disposition `transfer`; the developer chose to close it. 34.13-14 persists the forced verdict as
  `SteamMetadataCacheEntry.forcedWindowsViaBottle` (declared by 34.13-01) and consults it inside
  `isBottleEligible()` itself, so `getSettings()`, `isNative()`, `launch()`, `uninstall()` and
  `install()` are all consistent from one seam. Two consequences for this gate:
  1. **G-HANDOFF-LAUNCH is now a real PASS/FAIL item**, not a "record it either way" observation. A
     FAIL is a defect in 34.13-14, not an already-escalated design gap. The old framing must not be
     carried into the ledger.
  2. **Two new items exist because the fix has its own failure modes**: `G-HANDOFF-UNINSTALL` (the
     bottle copy is genuinely removed) and `G-HANDOFF-REINSTALL` (the verdict is cleared, so a later
     native re-install is not mis-routed). A green Jest suite proves the routing *decision*; only
     these items prove the game runs and the disk is clean.
  ⚠ **One behaviour is specified, not a bug:** re-installing a forced game **without uninstalling
  first** re-routes to the bottle even if the platform row is left on macOS — the bits are already
  there, so that is a repair/update. The revert path is uninstall → re-install, which is exactly
  what `G-HANDOFF-REINSTALL` exercises. Record it if observed; do NOT mark it FAIL.
- **`pnpm tauri:dev`, never `tauri dev`.** `tauri dev` serves a **stale static bundle**
  (`frontendDist` is a directory, not a dev server) — a UAT run against it observes the previous
  build and reports it as this phase's behaviour. Any gate row run under `tauri dev` is void.
- **No jsdom escape hatch.** Installing `jest-environment-jsdom` or `react-test-renderer` to make
  the dialog testable is **out of scope for this phase** and is an explicit non-goal (see
  `T-34.13-13-SC`). The manual gate is the design, not a workaround.
</phase_notes>

<tasks>

<task type="auto">
  <name>Task 1: Write the ten live gamelib: keys, prove each equals its inline source default, and prove the three retired keys are absent</name>

  <files>public/locales/en/gamelib.json</files>

  <read_first>
    - `public/locales/en/gamelib.json` — the whole file (102 lines). Note the 4-space indentation,
      the alphabetical ordering of both top-level and nested keys, and that `settings` already
      exists as a top-level object.
    - `meta/lintTranslations.ts` lines 1-70 and 139-181 — the header docstring explaining the
      `LINT_TRANSLATIONS_NAMESPACES` scoping, `readFile()`'s catch-and-log (why the script never
      fails), and the `if (dir === 'en') return` that excludes the file you are editing from the
      checks.
    - `src/frontend/screens/Settings/components/WineVersionSelector.tsx` lines 20-40 — the live
      precedent: `tGamelib('gamelib:settings.wineDefaultLabel', 'Wine Default')` at the call site,
      `"wineDefaultLabel": "Wine Default"` in the catalog. Byte-identical. Match this.
    - `.planning/phases/34.13-.../34.13-UI-SPEC.md` §"Copywriting Contract" (lines 93-119) and
      §"D-12 Busy State Contract" (lines 349-386) — the authoritative English for all six strings.
    - The `<action>` blocks that froze each default in the sibling plans, to confirm the shipped
      source matches: `34.13-03-PLAN.md` (bottle-name helper), `34.13-10-PLAN.md` (the D-14 notice,
      the D-24 library-missing/full notices, the Q6 content-light notice), `34.13-12-PLAN.md`
      (`forceWindowsHelp`), `34.13-08-PLAN.md` and `34.13-15-PLAN.md` (`withOptionsLabel`, shared),
      and `34.13-15-PLAN.md`'s `<catalog_keys>` block (the three `consoleMode.*` keys).
      **`34.13-04-PLAN.md` no longer exists** — it was DELETED by D-26 and owned only the two
      retired always-show strings.
    - The SUMMARY files of 34.13-03, 34.13-10, 34.13-15, and — if they exist by the time this runs —
      34.13-08, 34.13-11 and 34.13-12. Each was required to record its shipped English default
      verbatim precisely so this task does not have to re-derive it.
  </read_first>

  <action>
    **Step 1 — capture the lint baseline BEFORE touching anything.** Run
    `pnpm lint-translations:gamelib` and save stdout+stderr to a scratch file. This baseline is the
    only thing that makes the post-edit check non-vacuous; the exit code is `0` unconditionally and
    proves nothing (see `<verified_findings>` item 1).

    **Step 2 — harvest the actually-shipped inline defaults from source.** For each of the six keys
    below, locate its `t(...)` / `tGamelib(...)` call site under `src/` and extract the second
    argument verbatim. The source is authoritative over this plan's transcription for the four keys
    already shipped by closed plans; this plan's transcription is authoritative only as the value
    the source was contractually required to ship.

    | # | Key (under the `gamelib` namespace) | English default | Owner plan |
    |---|---|---|---|
    | K1 | `steam.install.sharedBottleNotice` | `The wine version you choose here is used for every Steam game that needs a bottle — not just this one. Changing it later will change how those games run too.` | 34.13-10 |
    | K2 | `steam.install.bottleNameReadonlyHelper` | `GameLib manages one shared CrossOver bottle for Steam. Naming a separate bottle per game isn't supported yet.` | 34.13-03 |
    | K3 | `steam.install.forceWindowsHelp` | `This game has a native macOS version, but you can install the Windows version instead through a Wine bottle — useful if the native build is missing features or performs worse.` | 34.13-12 |
    | K4 | `steam.install.withOptionsLabel` | `Install with options…` | 34.13-08 (**shared** — 34.13-15 reuses it for `GameCard`'s menu Item and `GameSubMenu`'s button. **One label, three surfaces — do not mint a second key.**) |
    | K5 | `steam.install.contentLightNotice` | `This installs through Steam's own client, so there's nothing to choose here. Turn on native Steam installs in Settings to manage install location and Windows compatibility from GameLib.` | 34.13-10 |
    | K6 | `steam.install.libraryMissingNotice` | `Your Steam library couldn't be reached — choose another below.` | 34.13-10 |
    | K7 | `steam.install.libraryFullNotice` | `Not enough space in your Steam library — choose another below or free up space.` | 34.13-10 |
    | K8 | `consoleMode.steamInstallBlockedTitle` | `Can't start this install` | 34.13-15 |
    | K9 | `consoleMode.steamInstallLibraryMissing` | `Your Steam library folder isn't available right now. Reconnect the drive and try again.` | 34.13-15 |
    | K10 | `consoleMode.steamInstallLibraryFull` | `There isn't enough free space in your Steam library folder. Free up space and try again.` | 34.13-15 |

    **The three `consoleMode.*` keys use the FLAT shape** already shipped in
    `public/locales/en/gamelib.json` (`consoleMode.installNext`, `consoleMode.installPrevious`) — not
    a nested `consoleMode.steamInstall.*` object.

    **⚠ THREE KEYS WERE DELETED FROM THIS TABLE — do NOT re-add them.** A prior revision of this plan
    required `settings.alwaysShowSteamInstallForm` (K4), `help.alwaysShowSteamInstallForm` (K5) and
    `status.checkingSteamInstallOptions` (K6), and its automated checks *asserted they existed*.
    All three are now retired and defining any of them contradicts a locked decision:
    - the two `alwaysShowSteamInstallForm` keys belonged to **`34.13-04`, a plan DELETED from disk**
      (D-26 cut the always-show setting; D-13 is RETIRED);
    - `status.checkingSteamInstallOptions` was **D-12's origin-side busy state, which D-25 inverted**
      — rewritten `34.13-11` now carries a gate proving that identifier is ABSENT from
      `MainButton.tsx`, `GameCard/index.tsx` and `GameSubMenu/index.tsx`.

    Consequently this phase creates **no `status` and no `help` top-level object** in the catalog
    (neither exists today — verified), and adds nothing under `settings`. It creates exactly one new
    top-level object, `steam`, plus three flat additions to the existing `consoleMode`.

    **⚠ COPY DEFECT to resolve before writing K6/K7.** Both strings end in "choose another below",
    but D-24's **≤1-library corner** is precisely the case where `34.13-10` deliberately fabricates
    **no picker** ("no picker is fabricated with nothing in it"). In that corner the copy promises a
    control that is not on screen. `steamDegrade.onlyLibrary` is already available to the dialog and
    tells it exactly when it is in that corner. **Resolve this** — either mint `onlyLibrary` variants
    of K6/K7 that drop the "choose another below" clause, or reword K6/K7 so they are true in both
    cases — and record which you chose and why in the SUMMARY. Do not ship copy that points at an
    absent control.

    Character notes, because these are the failure modes: K1, K3, K6 and K7 each contain an **em
    dash** `—` (U+2014, not a hyphen and not an en dash). K2, K5, K8, K9 and K10 contain **straight
    apostrophes** (U+0027, not U+2019) in `isn't` / `there's` / `Can't` / `couldn't`. K4 ends with a
    **single-character horizontal ellipsis** `…` (U+2026, not three periods). Transcribe from the
    shipped source, do not retype from memory.

    **Step 3 — reconcile, do not absorb.** If the inline default found in source differs from the
    table above by even one character, **stop and report it in the SUMMARY as a defect** naming the
    key, the plan that owns it, the shipped string and the contracted string. Write the *shipped*
    string into the catalog so the catalog and source agree, and flag the divergence for the phase
    checker. If a key has **no** call site in `src/` at all, that means its owner plan did not ship
    it — record it as a **BLOCKER** and do not invent a catalog entry for a string nothing renders.

    **Step 4 — write the catalog.** Insert the six keys into `public/locales/en/gamelib.json`
    preserving the file's existing conventions exactly:
    - 4-space indentation, matching `i18next-parser.config.js`'s `indentation: 4`.
    - Alphabetical order at every level. K4 goes **inside the existing `settings` object**, before
      `framelessWindowDescription`. K1/K2/K3 go into a **new** top-level `steam` object containing a
      nested `install` object, with the three keys alphabetised as
      `bottleNameReadonlyHelper`, `forceWindowsHelp`, `sharedBottleNotice`. K5 goes into a **new**
      top-level `help` object. K6 goes into a **new** top-level `status` object.
    - Resulting top-level key order: `app, consoleMode, discounts, downloadManager, gamepage, help,
      humble, library, login, redeemKey, settings, sideload, status, steam, themeSelector`.
    - Trailing newline preserved; no reordering, rewording, or removal of any existing key.

    **Step 5 — do NOT run `pnpm i18n`.** The i18next-parser run rewrites all four namespaces across
    all locales and is this repo's known catalog-drift hazard. Hand-edit only.

    **Step 6 — scope fence.** The only file this task modifies is
    `public/locales/en/gamelib.json`. In particular `public/locales/en/gamepage.json` stays
    untouched: `button.install`, `install.disk-space-left` and `game.platform` already exist there,
    and `install.steam-location.title` / `install.steam-location.primary` are deliberately
    inline-default-only and are reused as-is (upstream handoff 6). None of those five belong in
    `gamelib.json`.

    **Step 7 — re-run the linter and diff against the Step 1 baseline.**
  </action>

  <verify>
    <automated>pnpm lint-translations:gamelib > /tmp/34.13-lint-after.txt 2>&1; node -e "const j=require('./public/locales/en/gamelib.json'); const need=[['steam','install','sharedBottleNotice'],['steam','install','bottleNameReadonlyHelper'],['steam','install','forceWindowsHelp'],['steam','install','withOptionsLabel'],['steam','install','contentLightNotice'],['steam','install','libraryMissingNotice'],['steam','install','libraryFullNotice'],['consoleMode','steamInstallBlockedTitle'],['consoleMode','steamInstallLibraryMissing'],['consoleMode','steamInstallLibraryFull']]; let bad=0; for(const p of need){const v=p.reduce((a,k)=>a&&a[k],j); if(typeof v!=='string'||v.length===0){console.error('MISSING/EMPTY',p.join('.'));bad++}} const banned=[['settings','alwaysShowSteamInstallForm'],['help','alwaysShowSteamInstallForm'],['status','checkingSteamInstallOptions']]; for(const p of banned){const v=p.reduce((a,k)=>a&&a[k],j); if(v!==undefined){console.error('RETIRED KEY PRESENT (D-26/D-25 violation)',p.join('.'));bad++}} process.exit(bad)"</automated>
  </verify>

  <acceptance_criteria>
    - `node -e "require('./public/locales/en/gamelib.json')"` exits 0 — the file is still valid JSON.
    - All six keys resolve to a **non-empty string** via the `<verify>` command above (exit `0`).
      An empty-string value is a FAIL here: `redeemKey.*`'s empty values are an i18next-parser
      artefact and are **not** the convention for a key with a real inline default.
    - **Catalog value === inline source default, for all six keys.** For each key, the value read
      from `public/locales/en/gamelib.json` is byte-identical to the second argument of its
      `t('gamelib:<key>', '<default>')` call site under `src/`. Prove this mechanically, and prove
      the check is not vacuous by first altering one catalog value by a single character and
      observing the check FAIL, then reverting. Record both observations in the SUMMARY. A check
      that has only ever passed is not evidence.
    - **Every key has at least one call site.** For each of the **ten** live keys K1-K10,
      `grep -rn "gamelib:<key>" src/ | wc -l` returns **≥ 1**. A key with zero call sites is a
      BLOCKER, not a pass — it means its owner plan did not ship the string it contracted to ship.
    - **No retired key has a call site.** `grep -rn "alwaysShowSteamInstallForm\|checkingSteamInstallOptions" src/ | wc -l`
      returns **0**. A non-zero result means a plan revived work D-26 or D-25 retired, and is a
      BLOCKER. Prove this check non-vacuous by temporarily inserting one such identifier into a
      scratch file under `src/`, observing the check FAIL, then removing it.
    - **Em dash / apostrophe / ellipsis integrity.**
      `grep -c "—" public/locales/en/gamelib.json` returns **≥ 4** (K1, K3, K6, K7), and
      `node -e "const j=require('./public/locales/en/gamelib.json'); process.exit(j.steam.install.withOptionsLabel.endsWith('…')?0:1)"` exits `0`.
      No key contains ` -- `, ` - ` in place of an em dash, or `...` in place of `…`.
    - **Lint output is unchanged in substance.** Comparing `/tmp/34.13-lint-after.txt` to the Step 1
      baseline, ignoring only the two nondeterministic lines (the esbuild temp-path line matching
      `gamelib-runts-` and the `⚡ Done in Nms` line):
      - the count of lines matching `^Error: ENOENT` is **identical** to the baseline (53 at the
        time of planning — compare to the measured baseline, do not hardcode);
      - the count of lines matching `^Empty translation` is **0**;
      - the count of lines matching `^Missing content in translation` is **0**;
      - no line present in the after-output is absent from the baseline.
      State explicitly in the SUMMARY that the command's **exit code was not used as the gate**,
      and why (`meta/lintTranslations.ts` has no `process.exit`; measured exit `0` with 53 ENOENT
      lines before any edit).
    - **Scope fence holds.** `git diff --name-only` lists exactly
      `public/locales/en/gamelib.json` for this task. `public/locales/en/gamepage.json` is NOT
      listed, and no file under `public/locales/<non-en>/` is listed.
    - **No key removed or reworded.** `node -e` comparison confirms every key present in the
      pre-edit `gamelib.json` is still present with an unchanged value.
  </acceptance_criteria>

  <done>
    `public/locales/en/gamelib.json` carries all six new keys, correctly nested and alphabetised,
    each value byte-identical to the inline default its call site ships; the equality check has been
    observed failing against a deliberately altered value and then passing; the linter's substantive
    output is unchanged from the pre-edit baseline; and the diff touches that one file only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the phase-wide automated gate and fill the Per-Task Verification Map</name>

  <files>.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VALIDATION.md</files>

  <read_first>
    - `.planning/phases/34.13-.../34.13-VALIDATION.md` in full — the Per-Task Verification Map
      skeleton (lines 51-59), the Wave 0 requirements (lines 77-85), the four named
      vacuous-assertion risks (lines 104-118) and the Validation Sign-Off checklist (lines 120-130).
    - The `## Per-task verification map (for 34.13-VALIDATION.md, filled by 34.13-13)` block in
      **every** sibling plan — each one pre-computed its own rows. Confirmed present in 34.13-01
      (~L387), 34.13-05 (~L663), 34.13-06 (~L630), 34.13-08 (~L793), 34.13-10 (~L804), and the
      equivalent block in the remaining plans. Lift them; do not re-derive them.
    - Every `34.13-NN-SUMMARY.md` present in the phase directory — for the actual observed status of
      each row (`✅ green` vs `⚠️ flaky` vs a recorded deviation), and for the RED-before-GREEN
      observations several plans were required to record.
    - `package.json` lines 37-63 — `codecheck`, `test:ci`, `lint`, `lint-translations:gamelib`.
  </read_first>

  <action>
    Run the phase's automated gate and record it, then fill `34.13-VALIDATION.md`.

    **Gate A — type check.** `pnpm codecheck` exits 0.

    **Gate B — full suite.** `pnpm test:ci` exits 0. Record the total test count. This is a
    precondition for the manual gate, explicitly **not** a substitute for it.

    **Gate C — picker retirement (D-04).**
    `grep -rn "SteamInstallLocationPicker\|useSteamInstallLocation" src/` returns **nothing**.
    Measured at planning time: 10 matches. 34.13-08 retires them. If this still matches, the phase
    is not closeable — report which files.

    **Gate D — lint.** `pnpm lint` exits 0.

    **Gate E — the D-06 static half.** The new Steam dialog never gates Install on size: in
    `src/frontend/screens/Library/components/InstallModal/SteamDialog/`, the Install button carries
    no `disabled` prop and `getInstallInfo` is neither imported nor called. Run these over
    comment-stripped source so a doc-comment mentioning `getInstallInfo` cannot self-invalidate the
    check — strip `//` and `/* */` lines before counting, per this repo's grep-gate hygiene rule.

    **Then fill `34.13-VALIDATION.md`:**
    - Replace the placeholder row in the **Per-Task Verification Map** with one row per task across
      all thirteen plans, lifted from each plan's own map block. `Requirement` carries the `D-NN`
      decision id (no `REQ-` ids are minted this phase). `Threat Ref` carries the plan's own
      `T-34.13-NN-MM` id or `—`.
    - Set `nyquist_compliant: true` in the frontmatter **only if** every row has an `<automated>`
      command or an explicit Wave 0 dependency; set `wave_0_complete: true` **only if** all five
      Wave 0 checkboxes are satisfiable from the SUMMARYs. If either is not true, leave it `false`
      and record why — a frontmatter flag flipped without evidence is the failure mode this repo
      has ledgered repeatedly.
    - Tick the Validation Sign-Off checkboxes that Gates A-E discharge. Leave
      "Manual gate executed and recorded" **unticked** — Task 3 owns it.
    - Fill the `Requirement` column of the **Manual-Only Verifications** table (currently seven
      `TBD` cells) with the `D-NN` each row actually exercises.

    Do not edit any file under `src/`. If a gate fails, that is a finding to report to the
    orchestrator, not something to fix here — Wave 7 is a gate, not a repair bay.
  </action>

  <verify>
    <automated>pnpm codecheck && pnpm test:ci && ! grep -rq "SteamInstallLocationPicker\|useSteamInstallLocation" src/ && pnpm lint</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm codecheck` exits 0.
    - `pnpm test:ci` exits 0; the SUMMARY records the total test count and names any test file that
      was skipped.
    - `grep -rn "SteamInstallLocationPicker\|useSteamInstallLocation" src/` produces **zero** lines
      (`grep -rc ... | awk -F: '{s+=$2} END{print s}'` → `0`). The SUMMARY records that this was 10
      before the phase and 0 after, so the assertion is shown to have moved rather than to have
      always held.
    - `pnpm lint` exits 0.
    - Gate E: over comment-stripped `SteamDialog/index.tsx`,
      `grep -c "getInstallInfo"` returns `0` and `grep -c "disabled"` returns `0`.
    - `34.13-VALIDATION.md`'s Per-Task Verification Map contains **at least one row per plan
      34.13-01 … 34.13-12**, and every row's `Automated Command` cell is a runnable command (not
      `{command}`). `grep -c "{PP}\|{W}\|{XX}\|{NN}\|{command}" 34.13-VALIDATION.md` returns `0` —
      no placeholder survives.
    - Every `TBD` in the Manual-Only Verifications table's `Requirement` column is replaced:
      `grep -c "TBD" 34.13-VALIDATION.md` returns `0`.
    - The frontmatter flags match the evidence: if `nyquist_compliant: true`, then every map row has
      a non-empty `Automated Command` or `❌ W0`; if either flag is left `false`, the SUMMARY names
      the specific unmet condition.
    - "Manual gate executed and recorded" is still **unticked** at the end of this task.
    - `git diff --name-only` for this task lists only
      `.planning/phases/34.13-.../34.13-VALIDATION.md`. No file under `src/` or `public/` is listed.
  </acceptance_criteria>

  <done>
    All five automated gates pass and are recorded with their measured before/after values;
    `34.13-VALIDATION.md` carries a complete, placeholder-free Per-Task Verification Map and honest
    frontmatter flags; and the manual sign-off line is untouched, awaiting Task 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING manual UAT gate — enumerate the Section-Gating Matrix on both runtimes</name>

  <files>.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md</files>

  <read_first>
    - `.planning/phases/34.13-.../34.13-UI-SPEC.md` §"Section-Gating Matrix" (lines 132-231),
      §"Interaction Contract: D-17 Platform-Switch" (lines 235-289) and §"D-12 Busy State Contract"
      (lines 349-386). The Matrix is the source of truth for the sixteen rows below.
    - `34.13-09-PLAN.md` `<manual_gate_handoff>` (lines 606-632) — the eight D-15 steps, reproduced
      verbatim in G-HANDOFF-D15 below.
    - `34.13-06-PLAN.md` `<handoffs>` (lines 705-757) — the T-34.13-06-06 escalation and its
      five-surface table. **Read it for the failure it describes, not for its disposition** —
      34.13-14 closed it.
    - `34.13-14-PLAN.md` in full, and `34.13-14-SUMMARY.md` if present — the durability fix whose
      real-hardware behaviour G-HANDOFF-LAUNCH / G-HANDOFF-UNINSTALL / G-HANDOFF-REINSTALL are the
      only evidence for. Its `<handoffs>` names the one behaviour that is specified rather than
      defective (an in-place re-install re-routes to the bottle) — do not ledger that as a FAIL.
    - *(The former pointer to `34.13-04-PLAN.md`'s deferred `<human-check>` is REMOVED — that plan
      is DELETED from disk (D-26) and its check became `G-NOSETTING`, which asserts the setting's
      ABSENCE. Do not go looking for the file.)*
    - Every `34.13-NN-SUMMARY.md` — for any behaviour a plan recorded as deviating from its own
      contract, which must be re-observed here rather than trusted.
  </read_first>

  <action>
    Drive the blocking gate. This is an orchestration task for the executor, not an implementation
    task — **no file under `src/` or `public/` may be modified by it.**

    1. Confirm Task 2's gates are green. A red suite means the gate runs against a build that is
       already known-broken; stop and report instead.
    2. Create `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md`
       **pre-populated** with all thirty-six item ids from the "Recording" section below, each with an
       empty Disposition and Observation cell, one row per runtime (`electron`, `tauri`). Creating
       the skeleton up front is what forces enumeration; a ledger written after the fact from memory
       is how rows get sampled instead of enumerated.
    3. Present the `<how-to-verify>` script to the developer and **block**. Do not self-approve, do
       not infer any disposition, and do not fill a cell the developer did not report — even one
       that "must" be true given another row. `workflow.auto_advance` does not apply: this gate is
       the phase's only evidence for its primary deliverable.
    4. Transcribe the developer's reported observations into `34.13-UAT.md` verbatim. Where a
       disposition is `NOT ATTEMPTED`, record the named blocker; where it is `FAIL`, record what was
       seen rather than what was expected.
    5. Run the ledger-completeness check in `<verify>` and fix only **transcription** defects it
       finds (a missing row, a malformed disposition) — never by inventing an observation.
    6. Tick "Manual gate executed and recorded" in `34.13-VALIDATION.md` and set its `Approval:`
       line to the outcome.
    7. Report the roll-up to the orchestrator. If any item is `FAIL` or `NOT ATTEMPTED`, recommend
       phase status `human_needed` with each open item named individually — never `gaps_found`,
       which drops the phase to zero items in `audit-uat` and hides the open work.
  </action>

  <verify>
    <automated>cd .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity && test $(grep -o "G-QUICK-MAC-NATIVE\|G-QUICK-MAC-MULTILIB\|G-QUICK-MAC-BOTTLE\|G-QUICK-WIN\|G-QUICK-LINUX\|G-ROW-2/3-noWin\|G-ROW-[1-8]\|G-D06-NOSIZE\|G-D20-CONTENTLIGHT\|G-D20-Q6-COPY\|G-D05-BOTTLENAME\|G-D16-CROSSOVER\|G-D14-NOTICE\|G-D02-TARGET\|G-D08-FREESPACE\|G-NOSETTING\|G-SPLIT-SURFACES\|G-QUICK-DEFAULT\|G-QUICK-NOPROBE\|G-D24-DEGRADE\|G-D24-ONLYLIB\|G-D29-CONSOLE\|G-GAMEPAD-CARET\|G-D25-INDIALOG\|G-HANDOFF-D17\|G-HANDOFF-D15\|G-HANDOFF-LAUNCH\|G-HANDOFF-UNINSTALL\|G-HANDOFF-REINSTALL" 34.13-UAT.md | sort -u | wc -l) -eq 36 && grep -q "G-ROW-2/3-noWin" 34.13-UAT.md && grep -q "G-HANDOFF-REINSTALL" 34.13-UAT.md && ! grep -qE "G-SETTING|G-D20-TOOLTIP|G-HANDOFF-D12|G-ROW-9|G-ROW-1[0-2]|G-ROW-[36]a|G-ROW-6b" 34.13-UAT.md && ! grep -qE "[0-9]{17}" 34.13-UAT.md && grep -qE "\bPASS\b|\bFAIL\b|NOT ATTEMPTED" 34.13-UAT.md</automated>
    <human-check>The thirty-six-item script in `<how-to-verify>` below, run by the developer on Electron and `pnpm tauri:dev`. This is the blocking half; the automated command above only proves the ledger is well-formed, never that any item passed.</human-check>
  </verify>

  <what-built>
    Waves 1-6 delivered: the shared contracts and the `is_windows_native` signal; the
    backend-authoritative `isSteamBottleEligible` verdict and its dual-runtime IPC registration; the
    `persistBottleWineVersion` write and the `SteamBottleSetup` read that consumes it; the
    CrossOver-only engine filter and read-only bottle name; the global always-show setting; the pure
    section-gating predicate; the rewritten `startSteamInstall` trigger with `SteamInstallLocationPicker`
    retired; the `SteamDialog` sibling; the D-12 busy state on the origin controls; and the
    `InstallModal` fifth branch with the reworked platform row.

    Task 1 and Task 2 of this plan delivered the catalog entries and a green
    `codecheck` / `test:ci` / `lint` / picker-retirement gate.

    **None of that is evidence the form renders.** With no jsdom and no react-test-renderer in this
    repo, a green store test and a completely broken `InstallModal` are indistinguishable to Jest
    (`34.13-VALIDATION.md` vacuous-assertion risk 1). Every frontend plan in this phase explicitly
    deferred its rendered behaviour to this gate. This is where the phase is actually verified.
  </what-built>

  <how-to-verify>

### Ground rules — read before starting

1. **Two runtimes, always.** Every item is run under **Electron** (the normal dev build) and under
   **`pnpm tauri:dev`**. Never `tauri dev` — it serves a stale static bundle, so a run against it
   observes the *previous* build and silently reports it as this phase's behaviour. Any row run
   under `tauri dev` is **void** and must be re-run. A channel registered on only one runtime
   passes every Electron item and fails only under Tauri; that asymmetry is the entire reason both
   columns exist.
2. **Enumerate, never sample.** "One click-through ≠ coverage" is this phase's named
   vacuous-assertion risk. **No row may be marked PASS by inference from another row**, including
   rows that look mechanically identical (rows 8 and 12 each merge two mechanically-different
   sub-cases that render the same — both sub-cases still get observed).
3. **Three dispositions only: PASS / FAIL / NOT ATTEMPTED.** "Looks fine", "seems right", "N/A" and
   a blank cell are all rejected. Every row records the **observation**, not a verdict.
   **NOT ATTEMPTED** is acceptable **only** when the required host OS is unavailable; it must name
   the host needed, and it stays an open item — it does not close.
4. **The phase does not close on a partial gate.** Any FAIL or NOT ATTEMPTED keeps the phase
   `human_needed` with the item ledgered individually in `34.13-UAT.md`. Do not roll open items up
   into a phase-level status that hides them.
5. **Redact before committing.** Screenshots, log excerpts and paths pasted into `34.13-UAT.md`
   must not carry a SteamID64, account name or auth token. This repo has previously committed an
   unredacted Steam account id across four files.
6. **Historical note, not a bug:** during Waves 4-5 the store flipped `isOpen` with nothing
   rendering, because `InstallModal` had no Steam branch until Wave 6. That is expected and already
   recorded. **At this gate, after Wave 6, a dead click IS a failure.**

### Setup

| Item | Value |
|---|---|
| Always-show toggle | Settings → General → "Always show install options for Steam games" |
| Native-install toggle | Settings → General → the existing Steam native-install toggle |
| Library count | number of Steam libraries registered on disk (`libraryfolders.vdf`) |
| Metadata cache | `steam_metadata.json` in GameLib's app-support dir (`CacheStore('steam_metadata')`) |
| Bottle-required game | a **Windows-only** Steam game (mac-native `false`) — macOS host only |
| Mac-native game | a Steam game with a **native macOS build**, ideally one that also has a Windows depot |

---

### PART A — the quick-install path (D-22 / D-23)

> **⚠ REPLACED 2026-08-15 (D-21..D-29). Do NOT reconcile against an older ledger.**
> The previous PART A was **"the nine `opens = NO` rows"** — a matrix of host × native-install ×
> always-show × library-count combinations, each verifying the form did **not** auto-open.
> **That premise is gone: D-22 retired every auto-open trigger, so nothing auto-opens in any
> combination and "opens = NO" is no longer a distinguishing property.** Old IDs `G-ROW-3a`,
> `G-ROW-6a/6b`, `G-ROW-10/10a/10b` have **no successor**. What survives is the substance — the
> quick path must install without ceremony — re-expressed below against the split button.

For each: click the **primary** half of the split button (never the caret).
**PASS = no dialog, no modal, no overlay, no picker appears at any point between the click and the
install starting — including one that flashes and closes — the install proceeds, and the files land
in Steam's primary library.**
**FAIL = anything appears, OR the click does nothing at all (a dead click is a failure, not a pass),
OR the files land somewhere other than the primary library.**

| Item | Host | Game | Native install | Libraries | Electron | Tauri |
|---|---|---|---|---|---|---|
| **G-QUICK-MAC-NATIVE** | macOS | mac-native | OFF | 0 or 1 | ⬜ | ⬜ |
| **G-QUICK-MAC-MULTILIB** | macOS | mac-native | **ON** | **>1** | ⬜ | ⬜ |
| **G-QUICK-MAC-BOTTLE** | macOS | **bottle-required** (Windows-only) | any | any | ⬜ | ⬜ |
| **G-QUICK-WIN** | Windows | any | **ON** | **>1** | ⬜ | ⬜ |
| **G-QUICK-LINUX** | Linux | any | OFF | 0 or 1 | ⬜ | ⬜ |

**`G-QUICK-MAC-BOTTLE` is the sharpest inversion in this gate.** Under the retired model a
bottle-required game **force-opened** the form — that was D-10's first trigger and the phase's
original headline behavior. It must now install **silently** from the primary half, with bottle
consent still asked at the right moment by the existing `steamBottleSetupRequired` push. A tester
who remembers the old behavior will read the correct result as a regression.

**`G-QUICK-MAC-MULTILIB` and `G-QUICK-WIN` are the other inversion:** >1 library used to force the
override picker open. It must now go to the primary library with no prompt — the caret is how a
user asks for a choice.

---

### PART B — the eight already-open dialog rows

> **⚠ RE-DERIVED 2026-08-15 (D-21..D-29). Do NOT reconcile this against an older ledger.**
> The previous version of PART B had **seven `opens = YES` rows** keyed on
> `opens = bottleRequired OR (nativeInstallOn AND libraryCount > 1) OR alwaysShowOn`, and every
> row carried an always-show column. **D-22 retired all three triggers and D-26 cut the
> always-show setting.** There is now **no `opens` column and no always-show setting** — the
> dialog is open because the tester clicked **"Install with options…"**. Row numbers are the
> AMENDED 8-row matrix's and **do not correspond to the old row numbers**: old G-ROW-4/5/7/8/11/12
> have no successor with the same number. Score from this table only.

**Reaching every row below:** open the dialog via **"Install with options…"** (the `MainButton`
caret, `GameCard`'s context-menu Item, or `GameSubMenu`'s entry). No setting toggles it open.

Each row states five region columns. **PASS = all five observed as specified.**
**FAIL = any region present that should be absent, absent that should be present, or in the wrong
read-only/selectable state.** Record which region was wrong.
`hasChoice` := native install **ON** AND library count **> 1**.

| Item | Host / Case / native+libs | Platform row | Library dropdown | Wine section | Free-space line | Content-light notice | Electron | Tauri |
|---|---|---|---|---|---|---|---|---|
| **G-ROW-1** | macOS, **bottle-required** (Windows-only / 32-bit) game, any settings | read-only **"Windows"** | **absent** — destination is the bottle's `steamapps`, never a host library | **present**: CrossOver engine picker + read-only `GameLibSteam` name + D-14 notice | **absent** | absent | ⬜ | ⬜ |
| **G-ROW-2** | macOS, mac-native, **macOS selected**, native OFF **or** ON with ≤1 library | **selectable** if the game has a Windows depot, else read-only **"macOS"** | **absent** | **absent** | **absent** | absent | ⬜ | ⬜ |
| **G-ROW-3** | macOS, mac-native, **macOS selected**, `hasChoice` | **selectable** if Windows depot, else read-only **"macOS"** | **present** | **absent** | **present** | absent | ⬜ | ⬜ |
| **G-ROW-4** | macOS, mac-native, **Windows selected** (reached by flipping the selector from row 2/3) | **selectable**, Windows chosen | **absent even when `hasChoice`** — destination flipped to the bottle, so a host-library dropdown would lie about where the game goes | **present** | **absent** | absent | ⬜ | ⬜ |
| **G-ROW-2/3-noWin** | macOS, mac-native game **with NO Windows depot** | read-only **"macOS"**; the Windows `MenuItem` is **absent from the list entirely** — FAIL if present-but-disabled or greyed with a tooltip. Row 4 is structurally **unreachable** for this game | per row 2/3 | absent | per row 2/3 | absent | ⬜ | ⬜ |
| **G-ROW-5** | **Windows host**, native OFF **or** ON with ≤1 library | read-only **"Windows"** (D-19) | **absent** | **absent** | **absent** | **PRESENT** (D-20/Q6) | ⬜ | ⬜ |
| **G-ROW-6** | **Windows host**, `hasChoice` | read-only **"Windows"** (D-19) | **present** | **absent** | **present** | absent | ⬜ | ⬜ |
| **G-ROW-7** | **Linux host**, native OFF **or** ON with ≤1 library | **does not render at all** (D-18) | **absent** | **absent** | **absent** | **PRESENT** (D-20/Q6) | ⬜ | ⬜ |
| **G-ROW-8** | **Linux host**, `hasChoice` | **does not render** (D-18) | **present** | **absent** | **present** | absent | ⬜ | ⬜ |

**Row 4 is reached from rows 2/3 by flipping the platform selector**, not by a separate entry point.
Flipping back re-reveals the dropdown — that is row 3 again.

**G-ROW-2/3-noWin** is a distinct observation of rows 2/3's one depot-dependent cell, not a ninth
matrix row. It exists because "the Windows option is absent, not disabled" is the one thing a
depot-carrying test game cannot show.

**Rows 5 and 7 are the content-light case** D-20 accepts as-is. On a non-mac host the identity
`contentLightNotice === !libraryDropdown` holds; observe the notice per-row as a literal rather than
deriving it, so a wrong implementation cannot make the gate agree with itself.

---

### PART C — decision-specific observations

| Item | Decision | PASS | FAIL | Electron | Tauri |
|---|---|---|---|---|---|
| **G-D06-NOSIZE** | **D-06** | On **every** opens=YES row above, the dialog shows **no download-size row and no install-size row anywhere**, and the Install button is **enabled and clickable in every state** | Any size/"Getting download size…" text appears, OR the Install button is ever disabled/greyed, OR it is gated on `diskSize` or on free space | ⬜ | ⬜ |
| **G-D20-CONTENTLIGHT** | **D-20** | On a **Windows host with no library choice** (Matrix row 5 — run **both** sub-cases: (i) native install OFF, any library count; (ii) native ON with ≤1 library): the dialog renders one read-only "Windows" platform row, the **content-light notice**, and Cancel + Install — **and nothing else**. No empty-state illustration, heading, or "nothing to configure" message. Install completes normally from it | An empty state was built, OR the two sub-cases render differently from each other, OR the content-light notice is absent | ⬜ | ⬜ |
| **G-D20-Q6-COPY** | **D-20 / Q6** | The content-light notice renders in an **`.infoBox`** (NOT `ThirdPartyDialog`'s `.noticeIcon`/`.noticeInfo`, which are nested inside `.thirdPartyNotice` and would render unstyled here) and reads exactly the `gamelib:steam.install.contentLightNotice` value Task 1 wrote. Observe on **both** row 5 and row 7 | Notice absent, showing a raw key, visibly unstyled, or appearing on any row other than 5 and 7 | ⬜ | ⬜ |
| **G-D05-BOTTLENAME** | D-05 | In the wine section, the bottle-name field is **prefilled `GameLibSteam`** and **not editable**; a `.smallInputInfo` helper line beneath reads "GameLib manages one shared CrossOver bottle for Steam. Naming a separate bottle per game isn't supported yet." | Field is editable, empty, or the helper line is absent / shows a raw key | ⬜ | ⬜ |
| **G-D16-CROSSOVER** | D-16 | The engine dropdown lists **only CrossOver engines** — no GPTK, no plain wine builds, no Proton | Any non-CrossOver entry is offerable | ⬜ | ⬜ |
| **G-D14-NOTICE** | D-14 | The shared-bottle notice renders in an `.infoBox` with a `faWarning` icon in the **default text colour**, and reads exactly: "The wine version you choose here is used for every Steam game that needs a bottle — not just this one. Changing it later will change how those games run too." It renders **whenever the wine section is visible**, not behind a toggle | The icon is red (`--status-danger`, reserved for the `isKnownNotToWork` warning — the two must not look identical), the copy differs by any character from the catalog value Task 1 wrote, or it is hidden behind a disclosure | ⬜ | ⬜ |
| **G-D02-TARGET** | D-02 | With >1 library, pick the **non-default** library in the dropdown, install, then confirm on disk that the game's files landed **under that chosen library path** | Files landed in the primary/default library instead — the dropdown is decorative | ⬜ | ⬜ |
| **G-D08-FREESPACE** | D-08 | The free-space line reads "Space Available: {value}" with **no** "After Install" segment and **no** not-enough-space warning branch | A projection or blocking warning was built | ⬜ | ⬜ |
| **G-NOSETTING** | **D-26** *(replaces the retired `G-SETTING`)* | In Settings → General there is **NO** "Always show install options for Steam games" row, on the global screen **and** on every per-game settings screen. The Steam native-install row is unchanged and still present | Any always-show row exists anywhere. **⚠ This item asserts an ABSENCE and inverts its predecessor** — the retired `G-SETTING` required that row to exist, position correctly, and survive a restart. A tester reconciling against an older ledger will score this backwards. D-26 CUT the setting and deleted the plan that built it | ⬜ | ⬜ |
| **G-SPLIT-SURFACES** | **D-21 / D-27** | The install control offers both intents on every surface: (1) `MainButton` shows a primary Install **and** a caret opening "Install with options…"; (2) `GameCard`'s context menu has an "Install with options…" **Item** (not a caret); (3) `GameSubMenu` has an "Install with options…" entry. All three use the **same** label string | Any surface missing its affordance, a caret built on `GameCard`, or a second label key minted | ⬜ | ⬜ |
| **G-QUICK-DEFAULT** | **D-22 / D-23** | Clicking the **primary** half installs with **no dialog at all**, and the game lands in Steam's primary library. Nothing auto-opens the form — confirm on a bottle-required game too, where the old model force-opened it | Any dialog opens from the primary half. **⚠ INVERTS the retired model**, which auto-opened the form for bottle-required games | ⬜ | ⬜ |
| **G-QUICK-NOPROBE** | **D-25** | With a **cold** Steam metadata cache, the **primary** half starts installing without a multi-second stall. Proxy for an absence: time from click to visible install activity is comparable to a warm-cache click, not ~15 s | A multi-second dead click on the primary half — the eligibility probe leaked back onto the quick path | ⬜ | ⬜ |
| **G-D25-INDIALOG** | **D-25** | With a **cold** cache, the **caret** opens the dialog **immediately**; the **wine section** shows the loading state; the **dialog's own** Install button is disabled until it resolves; and the **library dropdown and free-space line are SUPPRESSED while pending**, appearing only once the verdict lands | Dialog withheld until the verdict; loading state on the origin button; dropdown/free-space visible then vanishing. **⚠ INVERTS the retired `G-HANDOFF-D12`**, which required "no half-rendered modal" and put the spinner on the origin control. **A half-rendered dialog is now CORRECT.** Scoring this from memory of the old gate will get it exactly backwards | ⬜ | ⬜ |
| **G-D24-DEGRADE** | **D-24** | Make the primary library unavailable (detach the drive) or full, then click the **primary** half: quick install **degrades into the options dialog** with the reason surfaced as an `.infoBox` at the **top** of the dialog content, and the Install button stays **enabled** | It errors out, silently picks another drive, or opens with no reason shown | ⬜ | ⬜ |
| **G-D24-ONLYLIB** | **D-24** | **OBSERVE AND RECORD — not pass/fail.** Repeat G-D24-DEGRADE with the failing library as the user's **only** library. The dialog opens with the notice but **no dropdown to choose a replacement**. Record verbatim what the notice says and what Install does. The behavior here is **deliberately undecided**; this row exists to capture ground truth, and its result feeds the copy question flagged in Task 1 | *(no FAIL condition — recording nothing is the only way to fail)* | ⬜ | ⬜ |
| **G-D29-CONSOLE** | **D-29** | In **Console Mode**, with the primary library unavailable, starting a Steam install shows the failure **in place** (a `--status-danger` title, a reason line, and a single Dismiss chip) — **no** desktop dialog opens underneath. Then: (1) pressing **A** does **not** start the install anyway; (2) the message **does not auto-dismiss after 1.5 s** | The desktop options dialog appears under the overlay, A bypasses the check, or the failure message vanishes on the 1500 ms timer | ⬜ | ⬜ |
| **G-GAMEPAD-CARET** | **D-21** | With a **controller**, the caret is reachable, reaching it does not make the primary half unreachable, and focus order is stable — on `MainButton` **and** the game card. **Must be observed on BOTH runtimes independently** | Caret unreachable, primary half lost, or focus order unstable. **⚠ An Electron pass is NOT evidence for Tauri:** Tauri filters focusables by zero bounding-box area, so a caret can pass every Electron check and be silently dropped from Tauri's focus ring | ⬜ | ⬜ |

---

### PART D — the four upstream handoff items

**G-HANDOFF-D17 — the D-17 platform-switch interaction (UI-SPEC steps 1→5).**
Preconditions: macOS host, a **mac-native** Steam game that **also has a Windows depot**,
always-show **ON**.

1. Open the install form. The platform `SelectField` is selectable with **macOS pre-selected**.
2. Flip it to **Windows**. Observe, in this top-to-bottom order inside `DialogContent`: the
   CrossOver engine `SelectField` **appears** below the platform row; the read-only `GameLibSteam`
   bottle-name field appears; the D-14 notice appears immediately after it, inside the same block.
3. **Simultaneously**, if a library dropdown was showing, it **disappears** — the destination is no
   longer a host library.
4. The reveal is a **snap, with no height animation**. FAIL if the section eases/slides in — no
   sibling dialog animates this mount and this phase must not special-case Steam into motion.
5. The dialog grows within `max-height: 90vh` and scrolls internally; it does not overflow the
   window or clip the footer.
6. Flip **back** to macOS: the wine section and the D-14 notice **unmount together**, and the
   library dropdown **reappears** if the >1-library condition still holds. Any engine/bottle-name
   selection made while on Windows is **discarded, not preserved** — that is the specified
   behaviour, not a bug.

PASS = all six observed. FAIL = any region appears/disappears out of the stated order, an animation
is present, state is preserved across the toggle, or the dialog clips.

**G-HANDOFF-D15 — the no-re-ask handoff (34.13-09's eight steps, verbatim).**
Preconditions: macOS host, a Windows-only (bottle-eligible) Steam game, the bottle **not yet
provisioned**, and **at least two CrossOver engines detected** so the choice is observable.

1. Open the install form for the bottle-eligible game and pick a CrossOver engine that is **not
   first in the list**. *(If `resolveSteamBottleEngine`'s derivation would pick it anyway, the test
   cannot distinguish a working handoff from a coincidence — this is the whole point of picking a
   non-first engine. A run that used the first engine is **void**.)*
2. Confirm the install. `SteamBottleSetup` opens via the `steamBottleSetupRequired` push.
3. **Assert:** the engine selector shows the engine chosen in step 1, **already selected, with no
   second question asked**. This is D-15.
4. **Assert:** the bottle-name field still reads `GameLibSteam` and is read-only (D-05).
5. **Assert:** the consent step still renders and still requires an explicit "Set up Steam" click —
   the form **feeds** the wizard, it does not replace or auto-confirm it.
6. Change the engine in the wizard to a different one and confirm the change sticks — the seed is a
   **default, not a lock**.
7. Close the wizard without confirming, trigger the install again, and repeat step 3. This exercises
   the session-reset path; **a stale seed from the previous session fails here and nowhere else**.
8. Repeat steps 1-3 under **`pnpm tauri:dev`**. A channel registered on only one runtime surfaces
   here as a wizard that falls back to the derived engine.

PASS = steps 3, 4, 5, 6, 7 and 8 all observed as stated. FAIL = the wizard re-asks, seeds the
derived default instead of the chosen engine, auto-confirms, locks the engine, carries a stale seed
after step 7, or behaves differently under Tauri.

**G-HANDOFF-LAUNCH — is a forced install actually launchable? (T-34.13-06-06, CLOSED by 34.13-14).**
34.13-06 shipped the override as install-time-only and escalated the post-install divergence
(disposition `transfer`). **34.13-14 closed it**: the forced verdict is persisted as
`forcedWindowsViaBottle` and consulted inside `isBottleEligible()`, so `getSettings()`, `isNative()`,
`launch()`, `uninstall()` and `install()` all agree about where the bits live. **This is therefore a
real PASS/FAIL gate now — a FAIL is a defect in 34.13-14, not an expected outcome.** The unit suite
proves the routing decision; only this item proves the game runs.

Preconditions: a completed forced Windows-via-bottle install from G-HANDOFF-D17, macOS host.

1. The game shows as **installed** in the library, and its install size/path reflect the bottle copy.
2. **Launch** starts the game. Confirm the route, not just the outcome: the Steam log records
   `launching appId <id> via the bottled Steam client`, and the host Steam client is **not**
   brought up with a "game not installed" error. A `steam://rungameid` dispatch to the host client
   is a FAIL even if something appears to happen.
3. The game's **settings screen** shows the **bottle's** wine version and the `GameLibSteam` bottle
   name — not an empty per-game `GameConfig`.
4. `isNative()` is observably `false` for this game: the pre-launch wine check runs (a wine
   version/prefix step is exercised) rather than being skipped.

PASS = all four observed. FAIL = any of the four takes the native path. Record the observed
behaviour of each of the four surfaces individually — "it launched" is not an observation for this
item, because three of the four surfaces can be wrong while the game still starts.

**G-HANDOFF-UNINSTALL — does uninstalling a forced install actually remove the bottle copy?**
(34.13-14, `T-34.13-14-03`.) The pre-fix defect leaked the bottle copy forever, so a badge flip is
not evidence — check the disk.

Preconditions: the forced install from G-HANDOFF-LAUNCH, still installed. **Run this after
G-HANDOFF-LAUNCH and before G-HANDOFF-REINSTALL.**

1. **Before uninstalling**, record the on-disk path of the game's directory under the Phase 17
   `GameLibSteam` bottle's `steamapps/common/`, and confirm the bottle's
   `appmanifest_<appId>.acf` exists. Redact any account identifier from the path before pasting it
   into the ledger.
2. Uninstall from GameLib. Observe that the Steam log records `delegating uninstall for appId <id>
   via the bottled Steam client` — **not** a `steam://uninstall` dispatch to the host client.
3. **Assert on disk:** the directory recorded in step 1 is gone, and the bottle's
   `appmanifest_<appId>.acf` is gone. A library badge flipping to not-installed while the files
   remain is a **FAIL** — that is exactly the pre-fix behaviour.
4. **Assert the badge** also flipped to not-installed without needing an app restart (the bottle ACF
   poller owns this).

PASS = 2, 3 and 4 observed, with the disk check in 3 done by looking at the filesystem. FAIL = a
native `steam://uninstall` dispatch, files left behind, or a badge that never flips.

**G-HANDOFF-REINSTALL — reversibility: is a later NATIVE re-install mis-routed?**
(34.13-14, `T-34.13-14-03` — the most likely way the durability fix ships a new bug.) The persisted
verdict must have been cleared by the confirmed uninstall; if it was not, the game is permanently
stuck in the bottle.

Preconditions: G-HANDOFF-UNINSTALL completed (the forced install is uninstalled), same mac-native
game, always-show **ON**.

1. Open the install form again. The platform row is **selectable** with **macOS pre-selected** —
   not read-only "Windows", and no wine section is showing while macOS is selected.
2. Leave macOS selected and install. Observe the Steam log: the install takes the **native** path
   (`steam://install` handoff, or the native depot path if native-install is ON) — **not** the
   bottled Steam client. A bottle dispatch here means the verdict was never cleared: **FAIL**.
3. **Launch** the game. It launches natively via the host Steam client.
4. The settings screen shows a per-game config, not the bottle's wine version.

PASS = all four observed. FAIL = any step routes through the bottle.

⚠ **Specified behaviour, NOT a defect:** if you instead re-install *without* uninstalling first, the
install re-routes to the bottle even with macOS selected — the bits are already there, so that is a
repair/update (34.13-14 `<handoffs>` item 4). If you observe it, record it as a note on this item;
do **not** mark the item FAIL for it. The revert path is uninstall → re-install, which is what
steps 1-4 above exercise.

**G-D25-INDIALOG — the cold-cache in-dialog loading state.** *(This is the full procedure for the
`G-D25-INDIALOG` item summarised in PART C; it is ONE item, recorded once. It replaces the retired
`G-HANDOFF-D12`.)*
> **⚠ THIS ITEM IS INVERTED FROM ITS PREDECESSOR — read before scoring.**
> The retired `G-HANDOFF-D12` required the busy state on the **origin control** and asserted
> **"no half-rendered modal"** (the dialog must not mount until the verdict resolved).
> **D-25 reverses both.** The dialog now mounts **immediately** and the loading state lives
> **inside** it. **A half-rendered dialog is the CORRECT result**, and a busy origin button is now
> a FAILURE. Anyone scoring this from memory of the old gate will invert every line below.

1. Quit GameLib. Delete `steam_metadata.json` from GameLib's app-support directory so the metadata
   cache is genuinely cold. Relaunch.
2. Click the **caret** ("Install with options…") on a Steam game.
3. **Assert:** the dialog opens **immediately** — not after a wait. The platform row is present
   right away.
4. **Assert:** the **wine section** carries the loading state (spinning `faSyncAlt`), the
   **dialog's own** Install button is **disabled**, and the **library dropdown and free-space line
   are absent** while pending — they appear only once the verdict lands. Nothing appears and then
   vanishes.
5. **Assert:** the **origin control shows NO busy state** — no spinner, no disabled Install button
   on `MainButton` / `GameCard` / `GameSubMenu`. Origin-side busy state is a D-25 violation.
6. **Assert:** on the **primary** half with the same cold cache, there is **no probe at all** — the
   install starts without the multi-second wait (see `G-QUICK-NOPROBE`).
7. **Worst case is 15 seconds** (`METADATA_FETCH_TIMEOUT_MS` = 15000ms) — now time-in-dialog with a
   disabled Install button, not time on an origin button. Up to 15s is a PASS. A state that never
   resolves is a FAIL — under Tauri specifically, that is the signature of a channel registered on
   only one runtime.

PASS = 3, 4, 5, 6 and 7 observed on both runtimes. FAIL = a withheld dialog, any origin-side busy
state, a dropdown/free-space line that flashes in then disappears, a probe on the quick path, or an
unbounded pending state.

---

### Recording

Create `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md` with
one row per item id above. **The manifest is 36 items** = 5 + 9 + 16 + 6, re-derived 2026-08-15.
(The retired manifest was 32 and shares only some ids — do not reuse it.)

**PART A — quick install (5):** `G-QUICK-MAC-NATIVE`, `G-QUICK-MAC-MULTILIB`,
`G-QUICK-MAC-BOTTLE`, `G-QUICK-WIN`, `G-QUICK-LINUX`

**PART B — the 8-row matrix (9):** `G-ROW-1`, `G-ROW-2`, `G-ROW-3`, `G-ROW-4`, `G-ROW-2/3-noWin`,
`G-ROW-5`, `G-ROW-6`, `G-ROW-7`, `G-ROW-8`

**PART C — decision + amendment observations (16):** `G-D06-NOSIZE`, `G-D20-CONTENTLIGHT`,
`G-D20-Q6-COPY`, `G-D05-BOTTLENAME`, `G-D16-CROSSOVER`, `G-D14-NOTICE`, `G-D02-TARGET`,
`G-D08-FREESPACE`, `G-NOSETTING`, `G-SPLIT-SURFACES`, `G-QUICK-DEFAULT`, `G-QUICK-NOPROBE`,
`G-D24-DEGRADE`, `G-D24-ONLYLIB`, `G-D29-CONSOLE`, `G-GAMEPAD-CARET`

**PART D — procedures (6):** `G-HANDOFF-D17`, `G-HANDOFF-D15`, `G-HANDOFF-LAUNCH`,
`G-HANDOFF-UNINSTALL`, `G-HANDOFF-REINSTALL`, `G-D25-INDIALOG`

`G-D25-INDIALOG` is summarised in PART C's table **and** given its full procedure in PART D. It is
**ONE** item, counted once under PART D and recorded once in the ledger.

**RETIRED — must NOT appear:** `G-SETTING` and `G-D20-TOOLTIP` (D-26 cut the setting they test),
`G-HANDOFF-D12` (D-25 inverted it — its successor is `G-D25-INDIALOG`), and the auto-open row ids
`G-ROW-3a`, `G-ROW-6a`, `G-ROW-6b`, `G-ROW-9`, `G-ROW-10`, `G-ROW-10a`, `G-ROW-10b`, `G-ROW-11`,
`G-ROW-12`, `G-ROW-5-noWin` (D-22 retired the triggers; the matrix is 8 rows).

**Before writing the ledger, re-derive the count from the actual tables** and confirm it is 36. A
manifest whose count disagrees with its own tables is exactly the defect this gate exists to catch;
if they disagree, the tables win — fix the manifest and say so in the SUMMARY.

Each row carries:

| Column | Content |
|---|---|
| Item | the id |
| Runtime | `electron` / `tauri` (one row per runtime, or one row with two verdict cells) |
| Disposition | `PASS` / `FAIL` / `NOT ATTEMPTED` |
| Observation | **what was actually seen** — not a restatement of the expectation |
| Blocker | for NOT ATTEMPTED: the host OS or fixture needed, and the phase it should be re-run in |

Name the file `*-UAT.md` exactly — this project's `audit-uat` tooling globs
`*-UAT.md` / `*-VERIFICATION.md` and silently misses any other filename. Keep the phase status
`human_needed` (not `gaps_found`) while any item is open; `gaps_found` drops a phase to zero items
in the audit and hides the open work.
  </how-to-verify>

  <acceptance_criteria>
    - `34.13-UAT.md` exists at the exact path in `<files>` and contains **all thirty-six** item ids
      listed under "Recording", using the `<automated>` command above, which returns **36**.
      **This command was re-authored 2026-08-15 for the amended manifest and has NOT yet been
      RED-proven — proving it is part of this task, not an inherited result.** Build a filled
      36-row specimen, confirm the command returns `36`, then delete one row at a time and confirm
      the count drops. Record both the green and at least two red observations in the SUMMARY; a
      count check that has only ever passed is not evidence.
      **Caveat that must be re-checked, not assumed:** it relies on the shell's `grep` resolving
      alternation leftmost-**longest**, so `G-ROW-2/3-noWin` wins over the `G-ROW-[1-8]` branch at
      the same position. Guard it rather than trusting it — additionally assert
      `grep -c "G-ROW-2/3-noWin" 34.13-UAT.md` and `grep -c "G-HANDOFF-REINSTALL" 34.13-UAT.md` are
      each **≥ 1**. If those pass while the count reads `35`, the alternation collapsed and the
      count is wrong, not the ledger.
    - **All eight Matrix rows are present individually**, plus the depot observation. The ids
      `G-ROW-1, G-ROW-2, G-ROW-3, G-ROW-4, G-ROW-5, G-ROW-6, G-ROW-7, G-ROW-8` and
      `G-ROW-2/3-noWin` each appear at least once. **The matrix has exactly 8 rows** — a ledger
      containing `G-ROW-9` or higher, or any of the retired `G-ROW-3a` / `G-ROW-6a` / `G-ROW-6b` /
      `G-ROW-10*` / `G-ROW-11` / `G-ROW-12` ids, is reconciling against the retired 16-row model and
      is a transcription error, not a pass.
    - **Every item has a disposition from the closed set.** No row's Disposition cell is empty or
      holds anything other than `PASS`, `FAIL`, or `NOT ATTEMPTED`. Verify: the count of rows whose
      Disposition matches `^(PASS|FAIL|NOT ATTEMPTED)$` equals the total item-row count.
    - **Every item has a non-empty Observation** describing what was seen. A row whose Observation
      restates the expectation verbatim, or reads "looks fine" / "as expected" / "ok", is rejected.
    - **Both runtimes are represented.** Every item id appears with both an `electron` and a `tauri`
      verdict (either as two rows or two cells). `grep -c "tauri dev" 34.13-UAT.md` — if any run was
      performed with bare `tauri dev` rather than `pnpm tauri:dev`, that row is void and must be
      re-run.
    - **Every NOT ATTEMPTED names its blocker.** Each such row's Blocker cell names the host OS or
      fixture required. A NOT ATTEMPTED with an empty Blocker cell is rejected — an unrecorded
      blocker rots silently, which this project has ledgered happening across four phases.
    - **No PASS by inference.** No Observation cell references another item id as its evidence
      (e.g. "same as G-ROW-6"). Each row's evidence is its own observation.
    - **D-06 and D-20 are explicitly discharged.** `G-D06-NOSIZE`, `G-D20-CONTENTLIGHT` and
      `G-D20-Q6-COPY` each carry a disposition and an observation. These are the two decisions this
      plan owns. *(The retired `G-D20-TOOLTIP` is NOT among them — D-26 cut the setting whose
      tooltip it inspected, and D-20's copy now lives in the dialog itself.)*
    - **The amendment items are explicitly discharged.** `G-QUICK-DEFAULT`, `G-QUICK-NOPROBE`,
      `G-D25-INDIALOG`, `G-D24-DEGRADE`, `G-D24-ONLYLIB`, `G-D29-CONSOLE`, `G-SPLIT-SURFACES`,
      `G-NOSETTING` and `G-GAMEPAD-CARET` each carry a disposition and an observation. These cover
      D-21..D-29, which no prior version of this gate touched.
    - **`G-D24-ONLYLIB` is observe-only and cannot be `PASS`.** Its Disposition must be
      `NOT ATTEMPTED` or a recorded observation — never `PASS`/`FAIL` — because the behavior it
      documents is deliberately undecided. Its Observation must quote the notice copy verbatim and
      state what Install did. An empty Observation is the only way this row fails.
    - **`G-GAMEPAD-CARET` requires two independent runtime observations.** An Electron PASS may not
      be cited as evidence for the Tauri cell: Tauri filters focusables by zero bounding-box area,
      so a caret can pass every Electron check and be silently dropped from Tauri's focus ring. A
      Tauri cell whose Observation references the Electron run is rejected.
    - **The three durability items each carry a real disposition.** `G-HANDOFF-LAUNCH`'s
      Observation records all four of its surfaces individually (install-state, launch route,
      settings resolution, `isNative()`/pre-launch wine check) — "it launched" is rejected, because
      three surfaces can be wrong while the game still starts. `G-HANDOFF-UNINSTALL`'s Observation
      records the **on-disk** state of the bottle's `steamapps/common/` directory and its
      `appmanifest_<appId>.acf` after the uninstall, not just the badge. `G-HANDOFF-REINSTALL`'s
      Observation names the route the re-install actually took, read from the Steam log. A
      disposition of `NOT ATTEMPTED` on any of the three is acceptable only if a forced
      Windows-via-bottle install could not be completed at all, and must say so — and in that case
      all three are NOT ATTEMPTED together, since each depends on the previous one.
    - **G-HANDOFF-REINSTALL is not marked FAIL for the specified in-place behaviour.** Re-installing
      without uninstalling first re-routes to the bottle by design (34.13-14 `<handoffs>` item 4);
      if observed, it is a note on the row, not a failure.
    - **Redaction.** `34.13-UAT.md` contains no 17-digit SteamID64, no Steam account name and no
      token: `grep -cE "[0-9]{17}" 34.13-UAT.md` returns `0`.
    - `34.13-VALIDATION.md`'s "Manual gate executed and recorded" checkbox is now ticked, and its
      `Approval:` line records the outcome.
    - `git diff --name-only` for this task lists only the two `.planning/` files. **No file under
      `src/` or `public/` is modified by this task** — this is a gate, not a repair bay. A defect
      found here is reported to the orchestrator for a gap-closure cycle.
  </acceptance_criteria>

  <resume-signal>
    Reply `approved` only if **every** item is `PASS`. Otherwise reply with the item ids that are
    `FAIL` or `NOT ATTEMPTED` and their observations — those become the gap-closure input, and the
    phase stays `human_needed` with each open item ledgered individually in `34.13-UAT.md`.
  </resume-signal>

  <done>
    `34.13-UAT.md` carries an explicit disposition and a real observation for all thirty-six items
    across both runtimes; the eight Matrix rows are each individually recorded with no
    inference-based passes; all six upstream handoff items are discharged — including the three
    forced-install durability items (launchability, on-disk uninstall, and reversibility) that
    34.13-14 made provable; and `34.13-VALIDATION.md`'s manual sign-off is ticked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → shipped bundle | `public/locales/en/gamelib.json` is bundled and rendered into the UI; its values reach the DOM through i18next |
| dev machine → `.planning/` (committed) | The manual gate runs a real, authenticated Steam session; its observations are transcribed into a file that gets committed |
| package registry → repo | No package installs this phase — stated explicitly below rather than omitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.13-13-01 | Tampering | `public/locales/en/gamelib.json` | mitigate | Task 1 modifies exactly one file and proves it: `git diff --name-only` must list only `public/locales/en/gamelib.json`, `gamepage.json` and all non-`en` locales must be untouched, and every pre-existing key must survive with an unchanged value. `pnpm i18n` is explicitly forbidden — it rewrites all four namespaces across all 49 locales and is this repo's known catalog-drift hazard. |
| T-34.13-13-02 | Information disclosure | The six new English strings | accept | All six are static English copy with **no interpolation** — no `{{...}}` placeholder, no path, no account identifier, no bottle name, no appId. Nothing user-derived is rendered. Verified against the UI-SPEC Copywriting Contract, which specifies no variable in any of the six. |
| T-34.13-13-03 | Information disclosure | `34.13-UAT.md` (committed) | mitigate | The gate runs against a real authenticated Steam library. Task 3 forbids pasting a SteamID64, account name or token, and gates on `grep -cE "[0-9]{17}"` returning `0`. This repo has previously committed an unredacted Steam account id across four files — this is a recurrence guard, not a hypothetical. |
| T-34.13-13-04 | Repudiation | The manual gate record | mitigate | A gate item a tester can mark "looks fine" is not a gate. Every item states its exact PASS and FAIL observations; the ledger's Disposition cell is restricted to `PASS` / `FAIL` / `NOT ATTEMPTED`; every row must carry an Observation of what was *seen*; and no row may cite another row as its evidence. NOT ATTEMPTED must name its blocker so the record cannot rot silently. |
| T-34.13-13-05 | Spoofing | `pnpm tauri:dev` vs `tauri dev` | mitigate | `tauri dev` serves a **stale static bundle**, so a gate run against it observes the previous build and reports it as this phase's behaviour — a passing result standing in for a build that was never exercised. Task 3 mandates `pnpm tauri:dev`, and any row run under bare `tauri dev` is declared void and must be re-run. |
| T-34.13-13-06 | Denial of service | `G-D25-INDIALOG` cold-cache wait | accept | The in-dialog loading state wraps `checkBottleEligibility()` → `ensurePlatformsCaptured()`, a poll already bounded at `METADATA_FETCH_TIMEOUT_MS` = 15000ms (`state.ts:30`). The bound is reused unchanged; this plan adds no retry and no second await. 15s is the worst case the gate item cites verbatim, and a bounded wait is what makes the pending state finite rather than a hang. Under D-25 the wait is **behind an explicit user request** for the dialog and no longer blocks the default install path at all, which strictly reduces exposure versus the retired D-12 model. Local-only, single-user, no remote amplification path. |
| T-34.13-13-07 | Elevation of privilege | `G-HANDOFF-LAUNCH` uninstall step | accept | Step 4 of that item runs a real uninstall against the tester's own library, deliberately last. `SAFE_INSTALLDIR` / `sanitizeBottleName` are untouched by this phase; no path this plan introduces reaches a delete. The risk is to the tester's own test fixture, which is the point of the observation. |
| T-34.13-13-SC | Tampering | npm/pnpm supply chain | mitigate | **No package installs this phase — the Package Legitimacy Gate is not applicable.** Stated explicitly rather than omitted. `package.json` and `pnpm-lock.yaml` are absent from `files_modified` and any change to either is an out-of-scope escape. Specifically: installing **`jest-environment-jsdom`** or **`react-test-renderer`** to make the dialog renderable in Jest — thereby converting this manual gate into an automated one — is an out-of-scope escape a sibling plan already flagged (`34.13-VALIDATION.md`'s hard constraint, outline note 4). The no-jsdom constraint is the design premise of this entire gate; adding a dependency to route around it is a scope change requiring its own discussion, not an implementation detail. |
</threat_model>

<verification>
Phase-level checks this plan owes, in order:

1. `pnpm codecheck` exits 0.
2. `pnpm test:ci` exits 0.
3. `pnpm lint` exits 0.
4. `grep -rn "SteamInstallLocationPicker\|useSteamInstallLocation" src/` returns **zero** lines
   (was 10 before this phase — the assertion is shown to have moved, not to have always held).
5. All six `gamelib:` keys resolve to non-empty strings in `public/locales/en/gamelib.json`, each
   byte-identical to the inline default at its call site, with the equality check observed FAILING
   against a one-character alteration before it was observed passing.
6. `pnpm lint-translations:gamelib`'s substantive output is unchanged from the pre-edit baseline —
   `Empty translation` count `0`, `Missing content in translation` count `0`, `Error: ENOENT` count
   identical to baseline. **The exit code is explicitly not used as the gate**; the script has no
   `process.exit` and returns 0 unconditionally.
7. `34.13-VALIDATION.md` carries a placeholder-free Per-Task Verification Map covering every plan.
8. `34.13-UAT.md` carries a disposition and an observation for all thirty-six gate items across
   both runtimes, with all eight Matrix rows individually present.

**A green suite alone does not close this phase.** Checks 1-3 are preconditions. The phase closes on
check 8.
</verification>

<success_criteria>
- Six new `gamelib:` keys in `public/locales/en/gamelib.json`, correctly nested and alphabetised,
  4-space indented, each value byte-identical to the inline English default shipped at its call
  site — with the equality check RED-proven before it was GREEN.
- No non-`en` locale file, and no `gamepage.json`, modified. `pnpm i18n` not run.
- `codecheck`, `test:ci` and `lint` all exit 0; the picker-retirement grep returns zero.
- The lint-translations gate is stated non-vacuously: the SUMMARY records that exit code 0 is
  unconditional for this script, gives the measured baseline (856 lines / 53 ENOENT / 0 findings),
  and compares against it.
- `34.13-VALIDATION.md` has a complete Per-Task Verification Map, honest frontmatter flags, and its
  manual sign-off ticked.
- `34.13-UAT.md` records all thirty-six items with `PASS` / `FAIL` / `NOT ATTEMPTED` and a real
  observation each, on Electron and `pnpm tauri:dev` (except `G-D24-ONLYLIB`, which is observe-only
  and carries an observation rather than a verdict).
- All five quick-install rows individually observed to open nothing and land in the primary library;
  all eight matrix rows plus the depot observation individually observed to render exactly their
  five specified regions; no row passed by inference.
- The nine amendment items (D-21..D-29) discharged: the split button on all three surfaces, quick
  install as the default, no probe on the quick path, D-25's in-dialog loading state, D-24's degrade
  and its ≤1-library corner, Console Mode, the absent always-show setting, and gamepad caret
  reachability observed **independently on both runtimes**.
- All upstream handoffs discharged as distinct items: the 8-row enumeration; the three
  forced-install durability items (launchability, on-disk uninstall, native-re-install
  reversibility) that 34.13-14 made provable; the eight D-15 steps; the mid-phase blank-window note;
  the D-14 verbatim flat string; and the reuse-don't-re-add rule for the existing `gamepage:` keys.
- D-06 and D-20 explicitly observed and recorded.
</success_criteria>

<output>
Create `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-13-SUMMARY.md`
when done. It MUST include:

- The **measured** `pnpm lint-translations:gamelib` baseline (line count, `Error: ENOENT` count,
  `Empty translation` count, `Missing content` count) taken BEFORE the catalog edit, the same four
  numbers AFTER, and an explicit statement that the exit code was not used as the gate and why.
- The final English value written for each of the six keys, verbatim, **and** the inline default
  found at each call site — so the byte-identity claim is auditable without re-reading the tree.
  Any divergence between the two, named as a defect with its owning plan.
- The RED observation for the catalog-vs-source equality check: the single-character alteration
  made, the failure it produced, and the pass after reverting.
- The before/after counts for
  `grep -rn "SteamInstallLocationPicker\|useSteamInstallLocation" src/` (expected 10 → 0).
- The `pnpm test:ci` total test count, and any skipped file.
- The full disposition roll-up from `34.13-UAT.md`: counts of `PASS` / `FAIL` / `NOT ATTEMPTED`,
  and every non-PASS item id with its observation and blocker.
- **The observed results of the three forced-install durability items in full**, since they are the
  only real-hardware evidence that 34.13-14 closed `T-34.13-06-06`:
  - `G-HANDOFF-LAUNCH` — all four surfaces individually (install-state, launch route read from the
    Steam log, settings resolution, pre-launch wine check).
  - `G-HANDOFF-UNINSTALL` — the recorded on-disk path before the uninstall (account identifiers
    redacted) and its state after, plus the `appmanifest_<appId>.acf` state.
  - `G-HANDOFF-REINSTALL` — the route the native re-install actually took, and whether the
    in-place (no-uninstall) case was observed and recorded as a note rather than a FAIL.
  A FAIL on any of the three is now a defect in 34.13-14, not an escalated design gap — report it as
  gap-closure input naming the surface that took the wrong path.
- Whether `nyquist_compliant` and `wave_0_complete` were set `true` in `34.13-VALIDATION.md`, and if
  not, the specific unmet condition.
- The phase status recommendation: `complete` only if all thirty-six items are `PASS`; otherwise
  `human_needed` (never `gaps_found`, which hides open items from `audit-uat`) with each open item
  listed individually.
</output>
