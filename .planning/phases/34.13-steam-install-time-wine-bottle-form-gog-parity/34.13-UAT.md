---
phase: 34.13
slug: steam-install-time-wine-bottle-form-gog-parity
status: partial
manifest_count: 36
created: 2026-08-15
human_verification_relocated:
  - origin_item: "G-GAMEPAD-CARET / electron"
    to_phase: "38"
    to_item: "38-C07"
    blocked_by: "a game controller"
    relocated: 2026-08-27
    decided_by: "operator, during the 2026-08-27 gate session"
    outcome: "open — not yet run in phase 38"
  - origin_item: "G-GAMEPAD-CARET / tauri"
    to_phase: "38"
    to_item: "38-C08"
    blocked_by: "a game controller"
    relocated: 2026-08-27
    decided_by: "operator, during the 2026-08-27 gate session"
    outcome: "open — not yet run in phase 38"
relocation_note: >
  Two-way by construction: `38-VERIFICATION.md`'s `38-C07`/`38-C08` each carry
  `origin_phase: "34.13"` and an `origin_item` naming the row above. Phase 38's own closing
  procedure step 4 requires whoever discharges them to come back and update the `outcome:` field
  here, so this receipt does not rot. If it still reads "open" long after a controller was
  available, the walk-back did not happen — check `38-VERIFICATION.md`'s
  `human_verification_discharged` array, which is where a discharged item is MOVED to (annotating
  in place is invisible to `gsd-sdk query audit-uat`).
---

# Phase 34.13 — Manual UAT Ledger

> Prepared by 34.13-13, Task 3 (preparation half only — this executor does not perform, simulate, or
> infer any of the observations below; that is BLOCKING and reserved for the human developer).
> Source of truth: `34.13-13-PLAN.md`'s `<how-to-verify>` (36-item manifest, amended 2026-08-15 for
> D-21..D-29). Do NOT reconcile this ledger against any older 32-row version — the id set changed.

## Ground rules (see the plan's `<how-to-verify>` for full text)

1. **Two runtimes, always** — Electron (normal dev build) and `pnpm tauri:dev` (**never** bare
   `tauri dev`, which serves a stale static bundle; any row run under it is void and must be re-run).
2. **Enumerate, never sample.** No row may be marked PASS by inference from another row, even rows
   that render identically.
3. **Four dispositions: `PASS` / `FAIL` / `NOT ATTEMPTED` / `RELOCATED`.** No "looks fine", no
   blank cells. `NOT ATTEMPTED` must name the blocking host OS or fixture and stays an OPEN item.
   **`RELOCATED` was added 2026-08-27 by operator decision** and is the ONLY disposition that is not
   a score: it means the item has been moved, with a two-way receipt, to a phase that owns its
   blocker, and is no longer 34.13's to run. It requires a destination phase + item id in the
   Observation cell and a matching `human_verification_relocated` receipt in this file's
   frontmatter. Recording a relocated item as `NOT ATTEMPTED` instead is WRONG and self-defeating:
   ground rule 4 would keep 34.13 open on a row it can never run, which is precisely the outcome
   Phase 38 exists to prevent ("so that individual phases can close on their runnable items instead
   of each carrying a permanently-blocked row").
4. **The phase does not close on a partial gate.** Any `FAIL` or `NOT ATTEMPTED` keeps the phase
   `human_needed`, itemized individually here — never rolled up into `gaps_found`. `RELOCATED` does
   NOT hold this phase open; the destination phase does, and the receipt is how that stays true. A
   park is a promise with no receipt unless someone walks back.
5. **Redact before committing.** No SteamID64 (17 digits), account name, or token in any cell.
6. Historical note: a dead click between Wave 4 and Wave 6 was expected and is not re-litigated here.
   At this gate (post Wave 6), a dead click **is** a failure.

Every row below currently carries `pending` — this executor has not run any of them. Replace `pending`
with the real disposition and a real Observation (what was actually seen, not the expectation restated)
before this ledger can close the phase. `Blocker` is filled only for `NOT ATTEMPTED`.

---

## Current Test

<!-- Added 2026-08-19 at gate start so an interrupted session can resume. OVERWRITE, never append. -->

item: (none in flight — pick from the runnable set below)
runtime: n/a
part: n/a
row: 43 of 72 resolved (36 PASS / 0 FAIL / 5 NOT ATTEMPTED / 2 RELOCATED); 29 pending
awaiting: operator choice of next item

**PART D's four-item handoff chain is CLOSED as of 2026-08-27 (tauri halves).** `G-HANDOFF-D17` ->
`G-HANDOFF-LAUNCH` -> `G-HANDOFF-UNINSTALL` -> `G-HANDOFF-REINSTALL`, all four run in strict order on
ONE genuinely-forced fixture — `Machinarium` (40700), a mac-native title forced to Windows-via-bottle
so D-17's forced arm was actually exercised. **Fixture choice was the load-bearing decision:**
`isBottleEligible()` is an OR (`isBottleEligibleFromPlatforms() || isForcedWindowsViaBottle()`,
`games.ts:1887`), so a merely-Windows-only title would have produced four identical-looking PASSes
while never touching the forced arm. The chain ends with 40700 RE-INSTALLED NATIVELY under host Steam
(301 MB at `~/Library/Application Support/Steam/steamapps/common/Machinarium`,
`install.platform:"Mac"`, `GamesConfig/40700.json == {}`), the forced verdict cleared, and the
`GameLibSteam` bottle holding nothing for 40700. The B-04 uninstall-vanish defect REPRODUCED during
the uninstall step and is logged to `.planning/debug/uninstall-game-vanishes.md` — still parked, not
investigated here.

**Fixture state left behind, for whoever runs the electron halves:**
- 40700 `Machinarium` — installed NATIVELY (host Steam), NOT forced, NOT in the bottle. Re-arming the
  chain for electron means uninstalling and re-forcing from scratch; the electron halves also need
  their own Steam login, a separate credential from the tauri run's.
- 2706020 `All Will Fall` — still installed IN the bottle from `G-QUICK-DEFAULT`.
- `/etc/hosts` is CLEAN — the store-API blackhole was removed 2026-08-27.

**Runnable on this host (macOS) with no new fixtures — 10 rows:** `G-D02-TARGET` (x2),
`G-D24-DEGRADE` (x2), `G-D24-ONLYLIB` (x2 — OBSERVE-ONLY, never scored PASS/FAIL), `G-D29-CONSOLE`
(x2), `G-QUICK-DEFAULT`/electron, `G-QUICK-NOPROBE`/electron. `G-HANDOFF-D15` (x2, currently NOT
ATTEMPTED) is re-runnable by moving the bottled `steam.exe` (5.8 MB) aside — NEVER `cxbottle.conf`.
**Blocked on another host — 16 rows:** `G-QUICK-WIN`, `G-ROW-5/6/7/8`, `G-QUICK-LINUX`,
`G-D20-CONTENTLIGHT`, `G-D20-Q6-COPY`.

**The G-D25-INDIALOG banked-observations block that stood here is GONE ON PURPOSE** — both its rows
are now scored PASS and every region was transcribed verbatim into their Observation cells. The
arming procedure below is NOT superseded and applies to any future row needing an unprobed title.

**⚠ THIS ITEM'S STATED PRECONDITION IS STALE AND COST THREE ATTEMPTS. READ BEFORE RE-RUNNING.**
The row says "cold cache (delete `steam_metadata.json`, relaunch)". **That no longer arms anything.**
Deleting the metadata cache does NOT make the probe pend, for two independent reasons:
1. **Wrong store.** The probe short-circuits on `steamPlatformsCaptured`, which lives in
   `steam_library.json`, NOT in `steam_metadata.json`. 34.14/34.15's bulk PICS capture set it TRUE
   on **380 of 381** titles. `hasSteamDepotSignalCaptured()` (`steamPlatformRow.ts:237-241`) just
   reads that flag. `index.tsx:484` already predicted this: unresolved-at-open stays reachable via
   "fail-soft sync, absent `oslist`, cached-library early returns, cold start".
2. **The file refills itself.** On launch the app repopulates `steam_metadata.json` 0 -> 224 entries
   within ~6s of Frontend Ready, then STOPS. So "the file is absent" is never the state a human
   clicks in, and racing the fill is not reproducible.

Attempt 1 (title `22380`, metadata deleted) — **no spinner, and NO FETCH AT ALL**: cache sat at 224
before and after, `22380` still cold. Nothing to spin for. Attempt 2 (flag flipped false, metadata
still cold) — fetch DID fire but returned fast, so the spinner rendered for ~1 frame and was
invisible; scoring either attempt FAIL would have recorded a defect over a branch that never ran.

**THE ONLY UNCAPTURED TITLE IN THE LIBRARY IS A DEAD END.** `28050` (Deus Ex: Human Revolution) is
the sole `steamPlatformsCaptured: false` entry — and it is uncaptured BECAUSE `is_delisted: true`:
no store page ⇒ `appdetails` 404 ⇒ platforms never captured. Circular. Do not spend time on it.

**HOW TO ACTUALLY ARM IT (worked, attempt 3):** the fetch must HANG, not fail.
(a) quit the app (it clobbers the stores on write); (b) set the fixture title's
`steamPlatformsCaptured` to `false` in `steam_library.json` and DELETE its `steam_metadata.json`
entry; (c) blackhole the store API — `198.51.100.1 store.steampowered.com` in `/etc/hosts` +
`dscacheutil -flushcache`. **`127.0.0.1` DOES NOT WORK** — it REFUSES in ~1ms, so the fetch fails
instantly and the timeout path never executes; 198.51.100.1 (TEST-NET-2) is unrouted, so connect
hangs. Verify with `curl --max-time 8`: **exit 28 (timeout) = armed; exit 7 (refused) = not armed.**
(d) relaunch and re-verify BOTH flags survived startup. This holds the pending window open for the
full 15s — no flash-catching. **Remove the hosts line afterwards.**

**SELF-INFLICTED, recorded so it is not misread as a product defect:** deleting the whole metadata
cache made the app re-fetch 224 titles in a burst and Steam rate-limited it — one
`403` on `22380` at 17:25:40, while plain `curl` returned `200` throughout. It cleared by itself and
corrupted nothing. Same class as the earlier `G-QUICK-MAC-MULTILIB` run that disqualified its own
fixture: prefer flipping ONE title's flag over clearing a whole store.

session: 2026-08-27. **Running from an ISOLATED WORKTREE** `~/Projects/GameLib-wt/uat3413`
(branch `wt/uat3413`, based at `1ff9cfaff`) because a CONCURRENT session is executing phase 34.18
in the main tree — it moved HEAD 4 commits and held 3 backend files dirty mid-session. Rows run
against a tree another agent is editing are void. **The app runs from the worktree; THIS LEDGER
stays the record of truth in the main tree** so results land on `fix/steam-native-install-stability`,
not on `wt/uat3413`.

worktree setup gotcha, will recur: `pnpm start` died with `Error: Electron uninstall` — a fresh
worktree gets a **240 KB stub** `node_modules/electron/dist` and no `path.txt`. Repaired by
`cp -Rc` of the main tree's 282 MB `dist` + `path.txt` (APFS clone, near-free, no re-download).

**COLD-CACHE FIXTURE — measured 2026-08-27, changes how D-25 and `G-QUICK-NOPROBE` are armed.**
Deleting `store_cache/steam_metadata.json` does NOT give a cold cache for long: on launch the app
repopulates it in the background. Measured: 0 -> 132 entries within ~6s of Frontend Ready, up to
**224**, then it STOPS and stays there. So "cold cache" cannot mean "the file is absent" — by the
time a human clicks anything it is partly warm, and racing the fill is not reproducible.
**It must mean cold FOR THE TITLE UNDER TEST.** 381 library entries minus 224 cached leaves
**157 stably-uncached titles**, 149 of them uninstalled + installable, and they stay uncached
until something asks for them individually. Pick the fixture from that set and the precondition
holds indefinitely with no timing pressure. Derive the set with:
`node -e "const S=process.env.HOME+'/Library/Application Support/gamelib/store_cache';
const mk=new Set(Object.keys(require(S+'/steam_metadata.json')));
console.log(require(S+'/steam_library.json').filter(g=>!mk.has(String(g.app_name))).length)"`

**PART B is COMPLETE for everything THIS machine can reach.** `G-ROW-1` (FAIL, owned by
`G-D05-BOTTLENAME`), `G-ROW-2`/`G-ROW-3`/`G-ROW-4` PASS both runtimes, `G-ROW-2/3-noWin` NOT
ATTEMPTED (no mac-native title without a Windows depot exists in this library).

**OTHER-HOST BATCH — 16 rows, deliberately left `pending`, NOT `NOT ATTEMPTED`.** The developer has
access to Windows and Linux hardware and will run these in ONE block on another computer
(confirmed 2026-08-21). `NOT ATTEMPTED` is reserved for rows that CANNOT be reached; closing these
would falsely retire reachable work, and `audit-uat` reporting them as outstanding is CORRECT here.

| host | items | rows |
|---|---|---|
| **Windows** | `G-QUICK-WIN`, `G-ROW-5`, `G-ROW-6`, `G-D20-CONTENTLIGHT` | 8 |
| **Linux** | `G-QUICK-LINUX`, `G-ROW-7`, `G-ROW-8` | 6 |
| **both** | `G-D20-Q6-COPY` (spec says observe on row 5 AND row 7) | 2 |

Take the WHOLE list on the trip — `G-D20-CONTENTLIGHT` and `G-D20-Q6-COPY` sit in PART C, not PART B,
and are easy to leave behind when packing a "finish the matrix" batch. Every item must be run on
BOTH runtimes (`pnpm tauri:dev`, never bare `tauri dev`). `G-D20-CONTENTLIGHT` additionally requires
BOTH of its sub-cases — (i) native install OFF at any library count, and (ii) native ON with
<=1 library — and they must render identically; a difference between them is the FAIL.

**Next runnable work is PART C**, starting at `G-D06-NOSIZE`. Most of PART C is observation on an
already-open dialog, so the `/Volumes/blank` fixture can stay ejected for now — but note
`G-D02-TARGET` REQUIRES >1 library and `G-D24-DEGRADE` requires making the primary library
unavailable, so those two need the volume re-attached and should be batched together.

**Session ended 2026-08-21 with PART A closed except the two host-blocked rows.** All tauri
rows in this session ran under **`GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev`** — the Keychain
slot `com.gamelib.launcher/steam-refresh-token` was EMPTY (`keyring_get present=false len=0
elapsed=7ms`, a fast NoEntry, not a timeout), so the Tauri runtime was logged OUT of Steam while
Electron stayed logged in. **The two runtimes do NOT share a Steam login** — Electron keeps a
`steam:v1:` safeStorage blob in `steam_store/config.json`, Tauri reads the Keychain. Note a failed
keyring read is memoised for 120s, so clicking Retry inside that window is a guaranteed no-op
(`served from cache, no keyring_get issued`).

**Before resuming, read the six todos filed 2026-08-21** — the gate found six defects, none of
them 34.13's, and two of them will block fixture selection again:
`2026-08-21-steam-library-22-games-never-reach-the-rendered-library.md` (pick a fixture you can
actually SEE) — **CLOSED 2026-08-22, operator-confirmed on a cold restart; no longer constrains
fixture choice** and `2026-08-21-steam-depot-chunks-fail-to-decode-z-data-error.md` (pick one whose
depots decode). Prefer an ALREADY-INSTALLED 64-bit mac-native title and use the ACF-aside resume
trick, which decodes few or no chunks.

**All tauri rows: `pnpm tauri:dev`, never bare `tauri dev`** (stale static bundle; any row run
under it is void). **Do NOT re-arm `G-QUICK-MAC-MULTILIB` with Amnesia (57300)** — it is a
32-bit mac title and now carries a verified `mac_arch:"32"`, so it routes to the bottle BY
DESIGN and can never satisfy that row. See the row's own Observation for the full reasoning.

**G-ROW-2/3-noWin closed as NOT ATTEMPTED** (both runtimes) — no fixture available, no mac-native
Steam title without a Windows depot in the developer's library.

**Fixture is CURRENTLY RUNNABLE for this row** — native installs still ON and a second library is
still registered, so `hasChoice` is TRUE, matching this row's precondition (native ON, >1 library).

**FIXTURE REPLACED 2026-08-21 — the FAT32 USB is GONE, superseded by an HFS+ volume.** The second
library is now `/Volumes/blank/SteamLibrary` (HFS+ journaled, 29 GiB, writable, created 2026-08-21).
The `/Volumes/NO NAME` FAT32 entry from 2026-08-19 is still listed in `libraryfolders.vdf` but is
UNMOUNTED, and `getSteamLibraries()` drops it via `existsSync` (`src/backend/utils.ts:679-680`), so
`libraryCount` is 2, not 3. **The 2026-08-19 "USB fixture hazard" paragraph below is therefore
RETIRED for new runs** — HFS+ has no 4 GB file cap. `noowners` still applies to the external mount,
so a LAUNCH from that volume may still fail for filesystem reasons unrelated to GameLib.

**Scoring rule for the rest of PART B — do NOT double-count the bottle-name defect.** `G-ROW-4`
and any other row whose spec says only "wine section PRESENT" is satisfied by the section being
present. The blank bottle-name field is already itemised once in `## Gaps` against `G-ROW-1` and
is owned by `G-D05-BOTTLENAME`. Re-failing every wine-bearing row on the same single frontend
defect would inflate the gap count and misrepresent the blast radius to the fix planner.

**1 defect, 2 rows FAILED so far** (`G-ROW-1` electron + tauri) — see `## Gaps`. Frontend-only:
one fix closes both runtimes. Expect `G-D05-BOTTLENAME` (2 more rows) to fail on the same cause.

**FIXTURE CHANGED MID-SESSION 2026-08-19 ~19:00 — the `hasChoice` deferral is LIFTED.** A USB drive
was attached and registered, so `libraryfolders.vdf` now holds TWO entries
(`~/Library/Application Support/Steam` and `/Volumes/NO NAME/SteamLibrary`) and
`defaultSettings.enableSteamNativeInstall` is `true` in `~/Library/Application Support/gamelib/config.json`.
`hasChoice` is therefore now **TRUE**.

Consequences — **the two matrix rows have SWAPPED which one needs a special fixture**:
- NOW RUNNABLE with no setup: rows 3/4 (`G-QUICK-MAC-MULTILIB`), `G-ROW-3`, `G-ROW-4`, `G-D02-TARGET`.
- NOW NEEDS A FIXTURE: `G-ROW-2` (the ≤1-library / native-OFF case). Reach it by ejecting the USB
  **or** toggling native Steam installs OFF — do NOT delete the library entry, that wastes the fixture.
- A first attempt at `G-ROW-2` was made just before this was discovered; the developer saw a platform
  row + a "Choose Steam library" dropdown. That observation is **re-attributed to `G-ROW-3`**, where a
  present dropdown is CORRECT. It was NOT scored as a G-ROW-2 failure. Recording this explicitly
  because scoring it under the intended row would have produced a false FAIL.

**USB fixture hazard (`/Volumes/NO NAME`, `msdos`/FAT32, 114 GiB free, writable):** 4 GB max file
size, and `noowners` means the volume CANNOT store POSIX permission bits (no executable flag). Fine
for `G-D02-TARGET`'s "files landed here" assertion; a LAUNCH from this volume may fail for
filesystem reasons unrelated to GameLib. Do not log such a failure as a GameLib defect.

**PART A closed except the two host-blocked items** (`G-QUICK-WIN`, `G-QUICK-LINUX`) and the two
fixture-deferred `G-QUICK-MAC-MULTILIB` rows.

**Redaction reminder (ground rule 5):** bottle manifests carry a `LastOwner` SteamID64. It has been
read during verification but deliberately NOT transcribed into any cell in this ledger.

**Carry-forward for `G-HANDOFF-D15`:** the `GameLibSteam` bottle is ALREADY PROVISIONED on this
machine, so the no-re-ask consent flow cannot be observed without first removing/renaming that
bottle. Any G-HANDOFF-D15 run that skips that teardown is scoring an unreachable path.

**DEFERRED — awaiting fixture, NOT skipped.** The `hasChoice` set (native install ON AND library
count > 1) has no fixture on this machine yet and stays `pending`: rows 3/4 (`G-QUICK-MAC-MULTILIB`),
`G-ROW-3`, `G-ROW-4`, `G-D02-TARGET`. Developer is attaching a USB drive as the second library.

Diagnosed 2026-08-19 — **Steam adds libraries per DRIVE, not per folder.** `libraryfolders.vdf` holds
exactly one entry (`"0"` → `~/Library/Application Support/Steam`) and the only writable volume is the
internal disk; `/Volumes/Bloom` and `/Volumes/VS Code` are read-only mounted DMGs. Pointing Steam's
"Add a Drive" at a folder on the volume that already hosts a library resolves to the same drive and
silently no-ops — `content_log.txt` recorded ZERO entries for the attempt (last line remains
`Triggered async write of Steam library folders configuration (1 libraries)`), so the request never
reached the content system. This is a Steam client behaviour, NOT a GameLib defect — do not file it
as a gap.

**Session run order** (host is macOS, so host-blocked rows are deferred to the end, not skipped):

1. PART A macOS rows → 2. PART B macOS rows (1, 2, 3, 4, 2/3-noWin) → 3. PART C macOS-runnable rows
   → 4. PART D handoff procedures → 5. host-blocked rows (Windows/Linux) resolved last.

**Mount verified 2026-08-19** on branch `fix/steam-native-install-stability` @ `2b72818af`:
`<SteamDialog` renders at `InstallModal/index.tsx:600`, `<EligibilityLoadingRow />` at `:624`.
(Line numbers moved from the 335/392 recorded at Wave 6 — 34.15 edited the file — but the phase IS
reachable in the running app, so these rows are not testing a dead surface.)

---

## PART A — the quick-install path (D-22 / D-23) — 5 items

PASS = no dialog/modal/overlay/picker appears at any point (including one that flashes and closes),
install proceeds, files land in the primary library. FAIL = anything appears, OR the click does
nothing (dead click), OR files land elsewhere.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-QUICK-MAC-NATIVE | electron | PASS | Developer-confirmed 2026-08-19. Criterion run: primary-half click on a macOS-native game (native install OFF, or ON with ≤1 library) — no dialog/modal/overlay/picker appeared (including flash-and-close), install proceeded, files landed in the primary library. Confirmed as a whole; no per-region narrative was dictated | |
| G-QUICK-MAC-NATIVE | tauri | PASS | Developer-confirmed 2026-08-19 under `pnpm tauri:dev`. Criterion run: primary-half click on the same macOS-native game — no dialog/modal/overlay/picker appeared, install proceeded to the primary library. Confirmed as a whole; no per-region narrative was dictated | |
| G-QUICK-MAC-MULTILIB | electron | PASS | 2026-08-21, developer-observed + disk-confirmed. Game: `Amnesia: The Dark Descent` (appid 57300), mac-native. **Fixture:** native install ON and TWO libraries resolve — primary plus `/Volumes/blank/SteamLibrary` (HFS+, 29 GiB). The older `/Volumes/NO NAME` entry is still in `libraryfolders.vdf` but is unmounted, and `getSteamLibraries()` filters on `existsSync` (`src/backend/utils.ts:679-680`), so it does not count — `libraryCount` = 2, `hasChoice` TRUE, matching this row’s precondition. **Method:** Steam client quit, `appmanifest_57300.acf` moved aside so GameLib saw the title uninstalled while 2.4 GB of content stayed on disk; the install therefore resumed over existing content rather than re-downloading. **Observed:** primary-half click → **nothing opened** — no dialog, modal, overlay or library picker, no flash-and-close — install started immediately and ran to completion. **Disk evidence:** `appmanifest_57300.acf` re-written 19:44 under the PRIMARY library’s `steamapps/`, `StateFlags "4"`, `BytesDownloaded` == `BytesToDownload` == `SizeOnDisk` == 2527362577, and `InstalledDepots` (57301 + 57303) byte-identical to the Steam-written manifest that was moved aside — a COMPLETE manifest, no stub/clobber signature. Both non-primary targets are excluded by EVIDENCE, not inference: `/Volumes/blank/SteamLibrary/steamapps/` is EMPTY, and no 57300 manifest exists under the `GameLibSteam` bottle. **NOTE — observed, not a failure of this row:** the mac depot is 32-bit (`file` reports `Mach-O executable i386`), so the title cannot run natively on this macOS, and GameLib correctly surfaced the Phase 18 CrossOver reinstall option AFTER the install completed. That offer is post-install and did not force anything open at click time, so the no-force-open criterion this row guards is unaffected. This row asserts install ROUTING, not launchability — launch was NOT attempted and is not claimed | |
| G-QUICK-MAC-MULTILIB | tauri | NOT ATTEMPTED | 2026-08-21, attempted THREE times under `pnpm tauri:dev` (dev vault arm), never completed. No fixture on this machine satisfies the row precondition. **(1) Amnesia: The Dark Descent** (57300) - nothing opened on the primary-half click, but routed to the CrossOver bottle (`os=windows`, depots 57301+57302, `source bottle`), NOT the primary library. Correct behaviour, not a defect: the electron half of this item installed the mac depot at 19:43, the Phase 18 Mach-O probe then read `i386` and wrote `mac_arch:"32"` / `mac_arch_verified:true`, and `isBottleEligibleFromPlatforms()` (`games.ts:1746`) routes to the bottle on exactly that verdict. **The electron run manufactured the evidence that disqualified the fixture for the tauri run.** Scoring this FAIL would record a defect that does not exist. **(2) Wasteland 1 - The Original Classic** (259130), chosen because `mac_arch:"unknown"` provably routes native - depot selection was CORRECT (`os=macos`, depots 259132+259134, windows/linux skipped) and `Wasteland.app` landed in the PRIMARY library, but the install died after 211s on 142 `Z_DATA_ERROR` chunk decode failures, deterministic across both attempts and all six CDN hosts at HTTP 200. Depot-download defect, filed separately, NOT 34.13. **(3) Wasteland 3** (719040) and **(4) Len's Island** (1335830), both 64-bit mac-native and installed - NOT VISIBLE in the library at all. Header reads `356 of 356` with no filters while the backend logs 378 games; 22 owned games never reach the screen. Filed separately. **What WAS observed, twice, on two different titles: NOTHING OPENED on the primary-half click** - no dialog, modal, overlay or picker, no flash-and-close - with `libraryCount=2` and native install ON, which is the retired force-open behaviour this row exists to catch. That is recorded as an observation only; the row stays NOT ATTEMPTED because "install proceeds, files land in primary" was never completed end-to-end | No usable fixture: 32-bit mac titles route to the bottle by design; Wasteland 1 depots fail to decode deterministically; Wasteland 3 and Len's Island are invisible (22-game library gap). Needs a 64-bit mac-native title that is BOTH visible in the library AND has decodable depots |
| G-QUICK-MAC-BOTTLE | electron | PASS (partial coverage — see note) | 2026-08-19, developer-observed + disk-confirmed. Game: `All Will Fall` (appid 2706020), Windows-only. Primary-half click → **nothing opened**, install started immediately, ran to completion, game launched successfully. Disk evidence: `appmanifest_2706020.acf` written 17:39 under `CrossOver/Bottles/GameLibSteam/drive_c/Program Files (x86)/Steam/steamapps/` (the BOTTLE path, not native), `StateFlags "4"`, `SizeOnDisk 4228928616`. The no-force-open criterion — the actual D-22/D-23 regression this row guards — is PASSED. **CONSENT HALF NOT EXERCISED:** no `steamBottleSetupRequired` prompt appeared, which is CORRECT here because the `GameLibSteam` bottle was already provisioned (it also holds 1771300, 552500, 228980, 226840, 206020/206060, 256290, 2457220, 2825840). Consent is not re-asked for an existing bottle, so this run could not demonstrate that it is still asked at the right moment — that assertion is structurally unreachable with a pre-provisioned bottle and is NOT claimed verified by this row. It is owned by `G-HANDOFF-D15`, which preconditions on "bottle NOT yet provisioned" | |
| G-QUICK-MAC-BOTTLE | tauri | PASS (partial coverage — same caveat as the electron row) | 2026-08-19, developer-observed + disk-confirmed under `pnpm tauri:dev`. Same Windows-only title re-run (`All Will Fall`, appid 2706020 — the row does not require a distinct game). Primary-half click → nothing opened; install proceeded into the bottle. Disk evidence: `appmanifest_2706020.acf` re-written 18:48 under the `GameLibSteam` bottle path, `StateFlags "4"`, `buildid 24654363`, `InstalledDepots` populated (depot 2706021, manifest 7563…9358 — middle digits elided 2026-08-21: a 19-digit depot manifest id is NOT a SteamID64, but it tripped ground rule 5’s `[0-9]{17}` guard and held this ledger’s own automated check RED, size 4228928616), `BytesDownloaded` == `BytesToDownload` == `SizeOnDisk` == 4228928616 — a COMPLETE manifest, no stub/clobber signature (noted because phase 23.2 is concurrently fixing a manifest-clobber defect; the 615→483 byte drop vs the 17:39 write is whitespace only, not lost keys). **CONSENT HALF NOT EXERCISED** for the same reason as the electron row — bottle pre-provisioned, so `steamBottleSetupRequired` correctly did not fire and this run cannot evidence that consent is still asked. Owned by `G-HANDOFF-D15` | |
| G-QUICK-WIN | electron | pending | Click primary half on Windows host, native install ON, >1 library — confirm nothing opens, install goes to primary library | |
| G-QUICK-WIN | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-QUICK-LINUX | electron | pending | Click primary half on Linux host, native install OFF or ON with ≤1 library — confirm nothing opens | |
| G-QUICK-LINUX | tauri | pending | Same, via `pnpm tauri:dev` | |

---

## PART B — the 8-row Section-Gating Matrix + depot observation — 9 items

Reach every row via **"Install with options…"** (MainButton caret / GameCard context-menu Item /
GameSubMenu entry). PASS = all five region columns (Platform row, Library dropdown, Wine section,
Free-space line, Content-light notice) observed exactly as specified for that row. FAIL = any region
present that should be absent, absent that should be present, or in the wrong read-only/selectable
state — record which region was wrong. `hasChoice` := native install ON AND library count > 1.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-ROW-1 | electron | PASS | 2026-08-22, developer-observed on Electron, `All Will Fall` (2706020), a Windows-only bottle-required title. **ALL FIVE REGIONS CONFIRMED on a build carrying the bottle-name fix:** (1) platform row read-only "Windows"; (2) library dropdown ABSENT, correct because the destination is the bottle steamapps and never a host library; (3) wine section PRESENT with all three of its required parts — CrossOver-only engine picker (also scored as `G-D16-CROSSOVER`), bottle-name field read-only and prefilled `GameLibSteam` (also scored as `G-D05-BOTTLENAME`), and the D-14 shared-bottle notice (also scored as `G-D14-NOTICE`); (4) free-space line ABSENT; (5) content-light notice ABSENT, correct on macOS since that notice belongs only to matrix rows 5 and 7. **SUPERSEDES THE 2026-08-19 FAIL, which is retained in this ledger's history above.** That FAIL was real when taken — the bottle-name field genuinely rendered blank — and was fixed by `83866f847` later the same day. This row was deliberately NOT flipped on the strength of the fix alone; it was held at FAIL until every one of the five regions had been re-observed, because only ONE of the five had ever failed and crediting the other four unlooked-at would have been the exact defect this gate exists to catch | |
| G-ROW-1 | tauri | PASS | 2026-08-22, developer-confirmed under `pnpm tauri:dev`, `All Will Fall` (2706020). All five regions reported matching the electron half: platform row read-only "Windows", library dropdown ABSENT, wine section PRESENT with its three parts, free-space line ABSENT, content-light notice ABSENT. **CLEARS THE PHASE'S LAST OUTSTANDING FAIL.** Supersedes the 2026-08-19 FAIL, retained above, whose sole cause (blank bottle name) was fixed by `83866f847` on 2026-08-19 20:27. As with the electron half, this was NOT flipped on the strength of the fix - it was held at FAIL until re-observed on a build carrying it. Environment verified from the log before recording, not assumed: `[bootstrap] secret stores: dev-vault` and `DevVaultTokenStore` at 07:28:18, `SteamUser.loggedOn` at 07:28:36 (cold-connect 7801ms), `All Will Fall` still `is_installed: false` in the library cache, and no dialog-side errors in the window. Confirmed whole by the developer; no per-region narrative was dictated - the same evidence grade as rows 1-2 of this ledger, recorded plainly rather than dressed up as a region-by-region reading. | |
| G-ROW-2 | electron | PASS | 2026-08-21, developer-confirmed on Electron. Game: `Wasteland 2: Director's Cut` (404730), mac-native, NOT installed, dialog opened via the "Install options..." route (not the primary half). **Fixture:** `/Volumes/blank` was ejected, so `libraryCount` = 1 and `hasChoice` is FALSE - this is the row's SECOND sub-case (native install left ON, <=1 library), not the native-OFF sub-case, which remains unexercised. **Observed:** platform row showed **macOS selected with Windows SELECTABLE**; the other four regions (library dropdown, wine section, free-space line, content-light notice) all ABSENT. Confirmed whole; no per-region narrative was dictated. **Why the selectable platform row is the CORRECT result and not a defect:** `steam_metadata` has `is_windows_native: true` for 404730, and this row's platform cell is the one depot-dependent column - selectable is required when a Windows depot exists, read-only "macOS" only when it does not (that no-Windows-depot case is the separate `G-ROW-2/3-noWin` item, closed NOT ATTEMPTED for want of a fixture). **The present, correctly-populated platform row also distinguishes "the four regions are correctly absent" from "the dialog rendered nothing at all" - an all-absent observation alone could not tell those apart.** Contrast the 2026-08-19 attempt recorded above, which saw a library dropdown and was re-attributed to `G-ROW-3` because `hasChoice` was TRUE at the time; the fixture is now genuinely FALSE, so this observation scores against G-ROW-2 ||
| G-ROW-2 | tauri | PASS | 2026-08-21, developer-confirmed under `pnpm tauri:dev` **after a deliberate app restart**, so the ejected-volume fixture was read at boot rather than inherited from a session that started while `/Volumes/blank` was still mounted - the stale-state reading is ruled out, not assumed away. Same game and fixture as the electron half: `Wasteland 2: Director's Cut` (404730), mac-native, NOT installed, `libraryCount` = 1 so `hasChoice` FALSE, native install left ON (the <=1-library sub-case). **Observed:** the dialog rendered **ONE line only** - the platform row, macOS selected, changeable to Windows - and nothing else. That is a positive observation of what WAS present, which is stronger evidence than four separate absence claims: it confirms library dropdown, wine section, free-space line and content-light notice are all absent while proving the dialog did render. Selectable Windows is CORRECT here because `is_windows_native: true` for 404730; read-only "macOS" would have been the FAIL. Matches the electron half exactly, so no runtime divergence on this row | |
| G-ROW-3 | electron | PASS | 2026-08-19, developer-observed. Fixture verified from disk at observation time, not assumed: `libraryfolders.vdf` = 2 entries and `enableSteamNativeInstall: true`, so `hasChoice` was genuinely TRUE. All four regions correct — platform row PRESENT, library dropdown PRESENT, free-space line PRESENT, wine section ABSENT. Free-space was re-checked specifically after the first report of "only two rows", because it renders as `afterSelect` small text UNDER the dropdown rather than as its own row. **Variant not distinguished:** the row's spec allows the platform row to be either selectable (game has a Windows depot) or read-only "macOS" (it doesn't) — both PASS — and which one was on screen was not captured. That distinction is a PRECONDITION for `G-ROW-4`, which requires a selectable row to flip. **Provenance note:** this observation was first taken while the row was believed to be `G-ROW-2`; the USB fixture landed mid-session and inverted `hasChoice`. Re-attributed to `G-ROW-3` after confirming library count from disk — under the intended row a present dropdown would have been scored a false FAIL | |
| G-ROW-3 | tauri | PASS | 2026-08-19, developer-observed under `pnpm tauri:dev`, independently of the electron row. All four regions correct — platform row PRESENT, library dropdown PRESENT, free-space line PRESENT, wine section ABSENT. **Variant captured here (it was not on the electron run): the platform row is SELECTABLE**, so this game carries a Windows depot. That discharges `G-ROW-4`'s precondition — a flip to Windows is reachable — and means `G-ROW-2/3-noWin` needs a DIFFERENT game (one with no Windows depot), not this one | |
| G-ROW-4 | electron | PASS | Developer-confirmed 2026-08-21. Criterion run: flipped platform selector from macOS to Windows on the `G-ROW-3` game (Windows-depot-bearing mac-native title) — platform row stayed selectable with Windows chosen; library dropdown ABSENT (even though `hasChoice` is true); wine section PRESENT; free-space line ABSENT. Confirmed as a whole; no per-region narrative was dictated | |
| G-ROW-4 | tauri | PASS | Developer-confirmed 2026-08-21 under `pnpm tauri:dev`, independently of the electron row — same result: platform row stayed selectable with Windows chosen; library dropdown ABSENT; wine section PRESENT; free-space line ABSENT. Confirmed as a whole; no per-region narrative was dictated | |
| G-ROW-2/3-noWin | electron | NOT ATTEMPTED | | Developer reports no mac-native Steam title without a Windows depot in their library — no fixture available on this machine |
| G-ROW-2/3-noWin | tauri | NOT ATTEMPTED | | Same — no fixture available on this machine |
| G-ROW-5 | electron | pending | Windows host, native OFF or ON with ≤1 library: read-only "Windows" (D-19); dropdown/wine/free-space ABSENT; content-light notice PRESENT (D-20/Q6) | |
| G-ROW-5 | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-ROW-6 | electron | pending | Windows host, hasChoice: read-only "Windows" (D-19); dropdown PRESENT; wine ABSENT; free-space PRESENT | |
| G-ROW-6 | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-ROW-7 | electron | pending | Linux host, native OFF or ON with ≤1 library: platform row does not render at all (D-18); dropdown/wine/free-space ABSENT; content-light notice PRESENT (D-20/Q6) | |
| G-ROW-7 | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-ROW-8 | electron | pending | Linux host, hasChoice: platform row does not render (D-18); dropdown PRESENT; wine ABSENT; free-space PRESENT | |
| G-ROW-8 | tauri | pending | Same, via `pnpm tauri:dev` | |

---

## PART C — decision + amendment observations — 16 items

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-D06-NOSIZE | electron | PASS | 2026-08-22, developer-confirmed on Electron. Observed across TWO structurally different dialogs, deliberately, because a size row could plausibly render in one shape and not the other: (a) `Wasteland 2: Director's Cut` (404730), mac-native, no wine section - dialog rendered the platform row and nothing else; (b) `All Will Fall` (2706020), Windows-only, wine section PRESENT (platform row read-only "Windows", bottle-name field read-only, engine dropdown rendered). **Neither dialog showed any download-size row, any install-size row, or any transient "Getting download size..." text**, and the **Install button was enabled and clickable in both cases** - the developer was asked about the button state specifically rather than it being inferred from silence, since the row FAILs on the button being greyed even momentarily during load. Both halves of the criterion therefore observed, not one half assumed |  |
| G-D06-NOSIZE | tauri | PASS | 2026-08-22, developer-confirmed under `pnpm tauri:dev`. Environment verified from the log and process table before recording, not assumed: `pnpm tauri:dev` live as PID 54464, `build/assets` rebuilt 13:48 from an EMPTIED directory (the 483 accumulated chunks spanning Aug 18-22 were removed at preflight, so this bundle cannot be a discarded chunk — F-34.10-08's trap, closed by construction rather than by grep). Confirmed as a whole by the developer; no per-clause narrative was dictated — the same evidence grade as the `G-QUICK-MAC-NATIVE` and `G-D16-CROSSOVER` rows above, recorded plainly rather than dressed up as a clause-by-clause reading. The clauses put to the developer are listed verbatim so the SCOPE of the attestation is on the record: the instruction put to the developer named **two structurally different dialogs deliberately**, mirroring how the electron half was scored, so that a size row that renders in one shape but not the other could not hide: `Wasteland 2: Director's Cut` (404730, mac-native — no wine section) and `All Will Fall` (2706020, Windows-only — wine section present). In both: no download-size row, no install-size row, no transient "Getting download size…" text, and the Install button enabled and clickable throughout. **Evidence-grade caveat, recorded rather than smoothed over:** the button-never-greyed clause is a TIMING observation — the row FAILs if the button is greyed even momentarily during load — and the developer confirmed the item as a whole without narrating the first frame separately. That clause is therefore attested, not independently narrated. **Arm differs from every prior 34.13 row: `[bootstrap] secret stores: keyring` at 13:45:08, NOT the `dev-vault` arm all the 2026-08-19/22 rows recorded.** Immaterial to this row, which is a UI-shape assertion indifferent to the secret store, but recorded so a future reader sees the difference here rather than discovering it. The library was CACHE-HYDRATED, not freshly synced (`steam_library.json`, 533 KB / 378 entries): the run logged `library refresh deferred until a deliberate Steam action`, which defers the REFRESH, not the list. **The scoring assistant initially misread that line as predicting an empty library and warned the developer so; the developer corrected it before any row was scored.** Recorded because the mistaken warning could have caused a correct row to be read as a failure. | |
| G-D20-CONTENTLIGHT | electron | pending | Windows host with no library choice (Matrix row 5 — run BOTH sub-cases: native OFF any library count; native ON with ≤1 library): one read-only "Windows" row, content-light notice, Cancel+Install, NOTHING else (no empty-state illustration/heading). Install completes normally. **CORRECTED by 34.13 review A-08 (was: "Both sub-cases render identically"):** review WR-04 deliberately made the two sub-cases render DIFFERENT COPY, so identical rendering is now a FAIL. Native OFF must show `gamelib:steam.install.contentLightNotice`; native ON with ≤1 library must show `gamelib:steam.install.contentLightSingleLibraryNotice`. The section LAYOUT is identical; only the sentence differs. **Also (review B-WR-02):** on the very first frame, before the Steam library fetch settles, NEITHER notice may appear — a flash of the "Turn on native Steam installs in Settings" sentence on a host where the setting is already ON is a FAIL | |
| G-D20-CONTENTLIGHT | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D20-Q6-COPY | electron | pending | Content-light notice renders in an `.infoBox` (NOT `ThirdPartyDialog`'s `.noticeIcon`/`.noticeInfo`). **CORRECTED by 34.13 review A-08:** the copy is now a TWO-BRANCH split (review WR-04), so this row must be scored per branch, on BOTH row 5 and row 7. (a) native installs OFF → exactly `gamelib:steam.install.contentLightNotice`: "This installs through Steam's own client, so there's nothing to choose here. Turn on native Steam installs in Settings to manage install location and Windows compatibility from GameLib." (b) native installs ON with ≤1 Steam library → exactly `gamelib:steam.install.contentLightSingleLibraryNotice`: "There's only one Steam library on this system, so there's nothing to choose here — GameLib will install this game into it." Seeing sentence (a) in case (b) is the exact defect WR-04 fixed and is a FAIL | |
| G-D20-Q6-COPY | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D05-BOTTLENAME | electron | PASS | 2026-08-22, developer-observed on Electron, `All Will Fall` (2706020). **All three parts PASS.** (1) bottle-name field is **prefilled `GameLibSteam`**; (2) **not editable** (read-only); (3) helper line present beneath it reading exactly "GameLib manages one shared CrossOver bottle for Steam. Naming a separate bottle per game isn't supported yet." **THIS REVERSES THE PREDICTED FAILURE.** The `## Gaps` entry below predicted a blank field from a CODE READ taken 2026-08-19, asserting `DEFAULT_STEAM_BOTTLE_NAME` had "zero frontend references" so "no code path can ever populate it". Both claims were true when written and are **now false**: commit `83866f847` (`fix(quick-260819-s8p)`, 2026-08-19 20:27) added `steamBottleDefaults.ts` as a frontend mirror and seeds `crossoverBottle` through a functional updater that falls back to `DEFAULT_STEAM_BOTTLE_NAME` when the value is unset (never clobbering an already-set name), at `InstallModal/index.tsx:527`, gated on `isSteamManagedApp`. The fix was already in HEAD before this session began - the gap record simply rotted. Verified in source before recording, not taken on the observation alone | |
| G-D05-BOTTLENAME | tauri | PASS | 2026-08-22, developer-confirmed under `pnpm tauri:dev`, `All Will Fall` (2706020): bottle-name field prefilled `GameLibSteam`, read-only, helper line present. Matches the electron half, so no runtime divergence on the fix delivered by `83866f847`. Environment verified from the log before recording, not assumed: `[bootstrap] secret stores: dev-vault` and `DevVaultTokenStore` at 07:28:18, `SteamUser.loggedOn` at 07:28:36 (cold-connect 7801ms), `All Will Fall` still `is_installed: false` in the library cache, and no dialog-side errors in the window. Confirmed whole by the developer; no per-region narrative was dictated - the same evidence grade as rows 1-2 of this ledger, recorded plainly rather than dressed up as a region-by-region reading. | |
| G-D16-CROSSOVER | electron | PASS | 2026-08-22, developer-confirmed on Electron, observed on `All Will Fall` (2706020) - a Windows-only title, chosen because the wine section only renders when a bottle is required. The wine-version/engine dropdown was **selectable and listed ONLY CrossOver engines - no GPTK, no plain wine builds, no Proton**. The developer confirmed those three exclusions EXPLICITLY when asked, rather than the absence being inferred from a general impression: "only CrossOver" is the whole assertion here, and a single GPTK or Proton entry buried in the list is the FAIL |  |
| G-D16-CROSSOVER | tauri | PASS | 2026-08-22, developer-confirmed under `pnpm tauri:dev`, `All Will Fall` (2706020): engine dropdown lists only CrossOver engines - no GPTK, no plain wine builds, no Proton. Matches the electron half. Environment verified from the log before recording, not assumed: `[bootstrap] secret stores: dev-vault` and `DevVaultTokenStore` at 07:28:18, `SteamUser.loggedOn` at 07:28:36 (cold-connect 7801ms), `All Will Fall` still `is_installed: false` in the library cache, and no dialog-side errors in the window. Confirmed whole by the developer; no per-region narrative was dictated - the same evidence grade as rows 1-2 of this ledger, recorded plainly rather than dressed up as a region-by-region reading. | |
| G-D14-NOTICE | electron | PASS | 2026-08-22, developer-observed on Electron, `All Will Fall` (2706020). Notice PRESENT, rendered alongside the wine section (not behind a toggle or disclosure), with the warning icon in **DEFAULT TEXT COLOUR, not red** — the developer was asked about hue specifically, because red (`--status-danger`) is reserved for the isKnownNotToWork warning and the two must not be confusable. **Copy verified against the catalogue rather than by eye-diffing a sentence:** `gamelib:steam.install.sharedBottleNotice` resolves to "The wine version you choose here is used for every Steam game that needs a bottle - not just this one. Changing it later will change how those games run too.", and the inline default at `SteamDialog/index.tsx:479` is BYTE-IDENTICAL to it, so the rendered string cannot drift from the catalogue. **NOTE — PLAN PROSE DRIFT, NOT AN APP DEFECT:** plan 13's PART C table quotes this string WITHOUT the word "here" ("The wine version you choose is used..."). The catalogue and the source agree with each other and disagree with the plan's prose. The row's own FAIL condition defers to "the catalog value Task 1 wrote", so the catalogue is authoritative and this is a PASS. A tester character-diffing against the plan text would have raised a FALSE FAIL here | |
| G-D14-NOTICE | tauri | PASS | 2026-08-22, developer-confirmed under `pnpm tauri:dev`, `All Will Fall` (2706020): shared-bottle notice present with the warning icon in default text colour, not red. Copy is guaranteed identical to the electron half by construction - both runtimes serve the same React bundle and the same `gamelib:steam.install.sharedBottleNotice` catalogue value, whose inline default at `SteamDialog/index.tsx:479` is byte-identical. Note plan 13 PART C quotes this string WITHOUT the word "here"; the catalogue is authoritative per the row's own FAIL condition. Environment verified from the log before recording, not assumed: `[bootstrap] secret stores: dev-vault` and `DevVaultTokenStore` at 07:28:18, `SteamUser.loggedOn` at 07:28:36 (cold-connect 7801ms), `All Will Fall` still `is_installed: false` in the library cache, and no dialog-side errors in the window. Confirmed whole by the developer; no per-region narrative was dictated - the same evidence grade as rows 1-2 of this ledger, recorded plainly rather than dressed up as a region-by-region reading. | |
| G-D02-TARGET | electron | pending | With >1 library, pick the NON-default library in the dropdown, install, confirm ON DISK the files landed under that chosen library path (not the primary/default) | |
| G-D02-TARGET | tauri | PASS | 2026-08-27, developer-driven + disk-confirmed under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev` from the isolated worktree `~/Projects/GameLib-wt/uat3413`. Fixture: `Roots of Pacha` (1245560), developer's choice from a mac-native shortlist. **Mac-native was REQUIRED, not incidental** — `libraryDropdown = !wineSection && hasChoice` (`steamSectionGating.ts:279`), so a Windows-only title renders the wine section and SUPPRESSES the very dropdown this row scores; picking one would have produced a false FAIL, the same trap `G-D08-FREESPACE` records. **BEFORE-state captured independently first, so "landed there" is provable rather than asserted:** `/Volumes/blank/SteamLibrary` held ONLY `libraryfolder.vdf` and an EMPTY `steamapps/` — 4.0K total, volume at 111 Mi used of 29 Gi. **AFTER, all four checked separately:** (a) content at `/Volumes/blank/SteamLibrary/steamapps/common/Roots of Pacha` (`Roots of Pacha.app`), 1.1 G, volume now 1.2 Gi used; (b) `appmanifest_1245560.acf` written INTO THE CHOSEN LIBRARY (485 bytes, `StateFlags "4"`, `installdir "Roots of Pacha"`, `SizeOnDisk 1206921083`, `buildid 22520504`) — not into the primary; (c) the PRIMARY library was checked explicitly for absence rather than left unexamined: `~/Library/Application Support/Steam/steamapps/common/` still holds 23 entries and contains no Roots of Pacha; (d) backend record agrees — `install.install_path` is the chosen path, `install.platform: "Mac"`. The dropdown necessarily OFFERED and HONOURED the non-default entry: with the primary resolving to host Steam (`isPrimary = index === 0`, `installLocation.ts:74`), content could not reach `/Volumes/blank` by any other route. **Library topology verified at run time, not assumed:** `libraryfolders.vdf` lists three entries but `getSteamLibraries()` filters on `existsSync` (`utils.ts:671-684`), and `/Volumes/NO NAME` is unmounted, so the live count is 2 (host Steam + `/Volumes/blank`) and `hasChoice` is genuinely true. **Not a defect, noted for reuse:** `/Volumes/blank` is HFS+ mounted `noowners`, so it cannot carry executable bits — this row asserts only WHERE the files went, and a launch failure from that volume would be a filesystem limitation, not a GameLib fault | |
| G-D08-FREESPACE | electron | PASS | 2026-08-22, developer-observed on Electron (`electron-vite dev`, PID 74317), independently of the tauri half — not carried across from it. Fixture: `Wasteland 2: Director's Cut` (404730), mac-native, macOS selected, chosen for the same structural reason as the tauri half — `libraryDropdown = !wineSection && hasChoice` (`steamSectionGating.ts:279`) and `freeSpaceLine = libraryDropdown` (`:284`), so a Windows-only title would suppress the very line this row scores and produce a false FAIL. Fixture state re-checked on disk at observation time: `/Volumes/blank/SteamLibrary` mounted, empty, 29 GiB free; `/Volumes/NO NAME` still absent though listed in `libraryfolders.vdf`; libraryCount = 2 via `getSteamLibraries()`'s `existsSync` filter (`src/backend/utils.ts:680`). **Observed:** (1) library dropdown present, offering both the primary library and the USB library; (2) free-space line reading "Space Available: {value}" beneath it; (3) both named FAIL branches ABSENT — no "After Install" segment, no not-enough-space warning. The two absences were put to the developer explicitly rather than inferred from silence, since a row whose whole content is "these two things were never built" fails by omission if absence is assumed. **Same evidentiary limit as the tauri half, restated rather than dropped:** with 29 GiB free against an install that was never started, the not-enough-space branch had no occasion to fire, so its absence here is weaker evidence than its presence would have been. The "After Install" absence is the stronger half — that segment was never built at all. **Runtime agreement is real but cheap:** both runtimes serve the same React bundle, so matching results confirm the observation was repeated, not that two independent implementations agree | |
| G-D08-FREESPACE | tauri | PASS | 2026-08-22, developer-observed under `pnpm tauri:dev` (PID 54464, `build/assets` rebuilt 13:48 from an emptied directory; arm `[bootstrap] secret stores: keyring` 13:45:08). Fixture: `Wasteland 2: Director's Cut` (404730), mac-native, macOS selected — **chosen deliberately over a Windows-only title because `libraryDropdown = !wineSection && hasChoice` (`steamSectionGating.ts:279`), so a bottle-required game SUPPRESSES the dropdown this row's free-space line hangs off (`freeSpaceLine = libraryDropdown`, `:284`). A Windows-only fixture would have produced a false FAIL.** **Fixture state verified on disk at observation time, not assumed:** `/Volumes/blank` mounted, `/Volumes/blank/SteamLibrary` present and EMPTY, 29 GiB free; `/Volumes/NO NAME` still absent though listed in `libraryfolders.vdf`; so `getSteamLibraries()`'s `existsSync` filter (`src/backend/utils.ts:680`) yields libraryCount = 2 and `hasChoice` TRUE. **Observed:** (1) the library dropdown rendered, offering both the primary library and the USB library; (2) the free-space line read "Space Available: {value}" beneath it; (3) **both named FAIL branches confirmed ABSENT** — no "After Install" segment and no not-enough-space warning. The developer was asked about the two absences explicitly rather than being allowed to report silence, since a row whose whole content is "these two things were never built" fails by omission if absence is inferred. With 29 GiB free against an unstarted install, the warning branch also had no reason to fire, so its absence here is weaker evidence than its presence would have been — recorded as a limit, not smoothed over. **Incidental but load-bearing finding:** the dropdown appeared with NO app restart, in a session that booted at 13:45 while `/Volumes/blank` was still unmounted. That empirically confirms the read is per-modal-mount, not per-boot — `InstallModal/index.tsx:344` re-enters `pending` and calls `window.api.listSteamLibraryTargets()` in a `useEffect` keyed on the game. The scorer traced this in source BEFORE the observation and predicted no restart was needed; the observation confirms the prediction rather than the prediction excusing the observation. This retires the stale-state doubt that forced the deliberate restart recorded on `G-ROW-2`/tauri — in the MOUNT direction only; the eject direction is untested here | |
| G-NOSETTING | electron | PASS | 2026-08-22, developer-observed on Electron. **Both clauses observed separately — the presence half was NOT credited on the strength of the absence half.** (1) ABSENCE: no "Always show install options for Steam games" row in Settings → General on the global screen, and none on the per-game settings screen either; the developer checked per-game specifically rather than generalising from the global screen. (2) PRESENCE: the Steam native-install row is still there, reported by the developer as the checkbox **"Download Steam games directly in GameLib"**. That string was then verified in source rather than taken as a label match by eye: it is the inline default at `src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx:25` and the catalog value of `steam-native-install` (`public/locales/en/translation.json:1076`), so the row the developer saw IS this row and not a neighbouring control — worth pinning because the developer did not recognise the row from the item's own wording, which says "Steam native-install row" and never quotes the shipped label. **This row asserts an ABSENCE and inverts its retired predecessor** — the pre-amendment item for the always-show row, which required that row to exist, position correctly and survive a restart; scored forwards here against D-26's cut, not against the older ledger. (That predecessor's literal id is deliberately NOT written out: this ledger's own automated gate greps the file for retired ids to prove they are absent, and cannot tell a retired ROW from prose that merely NAMES one. Writing it turned the gate RED on a correct ledger — the same check-versus-prose collision recorded as F-34.10-07 in phase 34.10, reproduced here. The row's original pending text phrased around it the same way, so this is the authors' intended handling, not a workaround invented at scoring time.) | |
| G-NOSETTING | tauri | PASS | 2026-08-22, developer-confirmed under `pnpm tauri:dev`. Environment verified from the log and process table before recording, not assumed: `pnpm tauri:dev` live as PID 54464, `build/assets` rebuilt 13:48 from an EMPTIED directory (the 483 accumulated chunks spanning Aug 18-22 were removed at preflight, so this bundle cannot be a discarded chunk — F-34.10-08's trap, closed by construction rather than by grep). Confirmed as a whole by the developer; no per-clause narrative was dictated — the same evidence grade as the `G-QUICK-MAC-NATIVE` and `G-D16-CROSSOVER` rows above, recorded plainly rather than dressed up as a clause-by-clause reading. The clauses put to the developer are listed verbatim so the SCOPE of the attestation is on the record: (1) no "Always show install options for Steam games" row in Settings → General on the global screen; (2) none on a per-game settings screen either; (3) the Steam native-install row still present as the "Download Steam games directly in GameLib" checkbox. Matches the electron half, so no runtime divergence on D-26's cut. **Arm differs from every prior 34.13 row: `[bootstrap] secret stores: keyring` at 13:45:08, NOT the `dev-vault` arm all the 2026-08-19/22 rows recorded.** Immaterial to this row, which is a UI-shape assertion indifferent to the secret store, but recorded so a future reader sees the difference here rather than discovering it. The library was CACHE-HYDRATED, not freshly synced (`steam_library.json`, 533 KB / 378 entries): the run logged `library refresh deferred until a deliberate Steam action`, which defers the REFRESH, not the list. **The scoring assistant initially misread that line as predicting an empty library and warned the developer so; the developer corrected it before any row was scored.** Recorded because the mistaken warning could have caused a correct row to be read as a failure. | |
| G-SPLIT-SURFACES | electron | PASS | 2026-08-22, developer-observed on Electron on an uninstalled Steam title (**the specific appid was not dictated by the developer and is deliberately NOT recorded here** — the row asserts affordance presence on three surfaces, which is title-independent, and inventing a fixture id would be exactly the inference this gate forbids). **All three surfaces confirmed individually, none inferred from another.** (1) `MainButton` on the game page — primary Install plus a caret opening "Install with options…". (2) `GameCard` context menu — an "Install with options…" **Item**, NOT a caret, which is the row's named FAIL shape for this surface. (3) `GameSubMenu` — reached via the three-vertical-dots control on the game detail page (`GamePage/index.tsx:485` → `DotsMenu.tsx`'s `faEllipsisV` button → `GameSubMenu`); the entry is present. Worth recording because the developer could not initially find this surface: the submenu is a DIFFERENT control from the GameCard context menu already scored above, and for an uninstalled Steam game its install-options entry is deliberately the FIRST and ONLY item in the menu (`GameSubMenu/index.tsx:344-374`, placed ahead of the `isInstalled`-gated cluster so document order favours keyboard/gamepad traversal). **The same-label clause was verified against the catalogue rather than by eye-diffing three rendered strings**, per the method `G-D14-NOTICE` used: all four call sites resolve the single key `gamelib:steam.install.withOptionsLabel` — `GameCard/index.tsx:388`, `MainButton.tsx:367` and `:393`, `GameSubMenu/index.tsx:371` — so no second label key was minted and the three surfaces cannot drift from each other by construction. **Session evidence, recovered from the Electron dev log after the fact rather than dictated:** the run spanned 13:25-13:41 on 2026-08-22 and opened exactly three game pages — `All Will Fall` (2706020, uninstalled), `Wasteland 2: Director's Cut` (404730, uninstalled) and `Wasteland 3` (719040, INSTALLED). So the observation was taken on one of the two uninstalled titles; **which one is still not claimed**, because the log proves a page was opened, not which page the affordances were counted on. Recorded this way deliberately: narrowing a fixture to a set the log can support is evidence, naming one of the two would be inference. `Wasteland 3` is excluded as the fixture by its own install state, not by assumption — an installed title cannot render an install control at all | |
| G-SPLIT-SURFACES | tauri | PASS | 2026-08-22, developer-confirmed under `pnpm tauri:dev`. Environment verified from the log and process table before recording, not assumed: `pnpm tauri:dev` live as PID 54464, `build/assets` rebuilt 13:48 from an EMPTIED directory (the 483 accumulated chunks spanning Aug 18-22 were removed at preflight, so this bundle cannot be a discarded chunk — F-34.10-08's trap, closed by construction rather than by grep). Confirmed as a whole by the developer; no per-clause narrative was dictated — the same evidence grade as the `G-QUICK-MAC-NATIVE` and `G-D16-CROSSOVER` rows above, recorded plainly rather than dressed up as a clause-by-clause reading. The clauses put to the developer are listed verbatim so the SCOPE of the attestation is on the record: (1) `MainButton` primary Install plus a caret opening "Install with options…"; (2) `GameCard` context menu carries an "Install with options…" **Item**, not a caret; (3) the three-vertical-dots control on the game page (`DotsMenu` → `GameSubMenu`) carries the same entry. **The uninstalled title used was again not dictated and is NOT recorded** — same handling as the electron half; the row asserts affordance presence on three surfaces, which is title-independent. Label identity is guaranteed by construction, not by eye: all four call sites resolve the single key `gamelib:steam.install.withOptionsLabel`, and both runtimes serve the SAME React bundle, so the three labels cannot differ across runtimes either. **Arm differs from every prior 34.13 row: `[bootstrap] secret stores: keyring` at 13:45:08, NOT the `dev-vault` arm all the 2026-08-19/22 rows recorded.** Immaterial to this row, which is a UI-shape assertion indifferent to the secret store, but recorded so a future reader sees the difference here rather than discovering it. The library was CACHE-HYDRATED, not freshly synced (`steam_library.json`, 533 KB / 378 entries): the run logged `library refresh deferred until a deliberate Steam action`, which defers the REFRESH, not the list. **The scoring assistant initially misread that line as predicting an empty library and warned the developer so; the developer corrected it before any row was scored.** Recorded because the mistaken warning could have caused a correct row to be read as a failure. | |
| G-QUICK-DEFAULT | electron | pending | Clicking primary half installs with NO dialog at all, lands in primary library — including on a bottle-required game (old model force-opened it; that is now wrong) | |
| G-QUICK-DEFAULT | tauri | PASS (partial coverage — the "primary library" clause is NOT claimed, see note) | 2026-08-27, developer-observed + disk-confirmed under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev` from the isolated worktree. Game: `All Will Fall` (2706020), Windows-only, bottle-required. **Fixture was fully clean and UNMODIFIED** — verified before the click: not installed, no ACF in the bottle, the native library or `/Volumes/blank`, no content on disk, `steamPlatformsCaptured` true, not delisted. A genuine first install, nothing to resume from. **Observed:** primary-half click -> **NOTHING OPENED** — no dialog, modal, overlay or picker, no flash-and-close — install started and ran to completion (~4.2 GB). This is the D-22/D-23 regression the row guards: the retired model force-opened the options dialog for a bottle-required game and that is now wrong. **Disk evidence:** `appmanifest_2706020.acf` written 18:25 under the BOTTLE path (`CrossOver/Bottles/GameLibSteam/drive_c/Program Files (x86)/Steam/steamapps/`), `StateFlags "4"`, `installdir "All Will Fall"`, `buildid 24838791`, and `BytesDownloaded` == `BytesToDownload` == `SizeOnDisk` == 4222299944 — a COMPLETE manifest with no stub/clobber signature; 3.9 GB of content present under `steamapps/common/All Will Fall`. Both non-bottle targets are excluded by EVIDENCE, not inference: no `appmanifest_2706020.acf` exists in the primary native library or under `/Volumes/blank/SteamLibrary`. **No `steamBottleSetupRequired` prompt appeared, which is CORRECT here** — the `GameLibSteam` bottle was already provisioned with `steam.exe` present, so `isBottleReady()` was true and consent is not re-asked. That path stays unexercised and is owned by `G-HANDOFF-D15`. **⚠ PARTIAL COVERAGE — the row's "lands in primary library" clause is NOT satisfied by this run and is NOT claimed.** A Windows-only title correctly lands in the BOTTLE, so this fixture is structurally incapable of evidencing the primary-library destination; that clause needs a mac-native title and remains open | |
| G-QUICK-NOPROBE | electron | pending | With a COLD Steam metadata cache, primary half starts installing without a multi-second stall (comparable timing to a warm-cache click, not ~15s) | |
| G-QUICK-NOPROBE | tauri | PASS | 2026-08-27, developer-observed + log-timed under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev` from the isolated worktree. Fixture: `Fallout 2` (38410), deliberately armed cold — metadata entry deleted AND `steamPlatformsCaptured` set false — so a probe on the quick path would have required a genuine round trip and could not resolve from local data. **This row asserts STALL TIMING at click, nothing else.** Observed: primary-half click started immediately, no stall. Timing from the log, not recollection: `buildDepotPlan/fetchDepotPlanEntry:38414 attempt 1 succeeded in 611ms`, `:38415 ... 305ms`, `buildDepotPlan: total 1731ms`, then `getContentServerHosts ... hosts=6`, then `steam-flags-census stage=download-entry appId=38410 totalFiles=65` -> `stage=download-complete`, with `runNativeDepotDownload ... total since click 5062ms`. The install pipeline was therefore entered and running within the first second and did real network work throughout — nowhere near the ~15s a metadata probe costs on a cold cache. Criterion met. **SEPARATE DEFECT, NOT THIS ITEM'S AND NOT SCORED HERE:** the download then FAILED at 5062ms — `SteamGame: depot install failed for appId 38410: The Steam download failed.` That string is the GENERIC fallback in `depotErrors.ts:277` (`steam.download.error.genericV2`), reached only when the error text matches none of the known signatures — so the cause is unclassified, and no `fetchChunk`/`reason=` detail was logged. Note `lzmaLoader: native lzma-native decode is explicitly DISABLED by this build (NATIVE_LZMA_DECODE_ENABLED=false)` fired repeatedly during the attempt. **Caveat before anyone files this as confirmed:** this fixture had its platform capture deliberately wiped, so re-test on an UNMODIFIED title first to rule the fixture out. Filed as a todo; it is a depot-install concern, not a 34.13 install-form concern | |
| G-D24-DEGRADE | electron | pending | Make primary library unavailable (detach drive) or full, click primary half: quick install degrades into the options dialog, reason surfaced as `.infoBox` at TOP of dialog content, Install button stays ENABLED | |
| G-D24-DEGRADE | tauri | PASS | 2026-08-27, developer-observed under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev` from the isolated worktree. **⚠ THE ROW'S SUGGESTED ARMING ("detach drive") CANNOT WORK — recorded so the next runner does not burn time on it.** `getSteamLibraries()` (`utils.ts:671-684`) filters every candidate through `existsSync` BEFORE `listSteamLibraryTargets()` indexes them, and `isPrimary` is `index === 0` (`installLocation.ts:74`). A detached drive therefore never becomes a MISSING primary — it is filtered out and the list simply shortens, promoting the next library. The reachable arming is a library root that EXISTS but has NO `steamapps/` subdirectory: it survives the filter, then `checkDiskSpace(root/steamapps)` returns `validPath:false` (from `isWritable`, `main.ts:684`) and `library-missing` fires — the case `installTarget.ts:95` already describes. **Fixture built accordingly, without touching either real library:** host Steam quit first (it rewrites `libraryfolders.vdf` and would clobber the edit; quit by the developer, not killed), the vdf backed up byte-for-byte (`shasum ccd1980e5b28f2f893c144115901b3c94f1ccde3`), then an empty decoy root inserted at index 0 with the three real entries renumbered down. Live resolution order became: `/usr/share/steam` (filtered, absent) -> decoy (PRIMARY, no `steamapps/`) -> host Steam -> `/Volumes/NO NAME` (filtered, unmounted) -> `/Volumes/blank`. `Roots of Pacha` and both real libraries were left untouched throughout. **All three clauses observed separately:** (1) the PRIMARY-half click — the button face, not the caret — DEGRADED into the options dialog instead of installing silently, which is the whole assertion: this is the one path that normally opens nothing at all. (2) an `.infoBox` notice rendered at the TOP of dialog content, above the platform row. Developer reported it as "Your steam library couldn't be reached - choose another below"; the authoritative catalogue value is `gamelib:steam.install.libraryMissingNotice` = "Your Steam library couldn't be reached — choose another below." The differences (lowercase "steam", ASCII hyphen for the em dash) are consistent with HAND TRANSCRIPTION and are NOT claimed here as a rendering difference — no byte-exact copy match is asserted, only that the notice was present, at the top, and said this in substance. (3) the Install button stayed ENABLED — the named FAIL shape (a degrade that dead-ends) did not occur. **Incidental but load-bearing evidence for review B-WR-01:** the copy selector at `SteamDialog/index.tsx:452` is keyed on `gating.libraryDropdown`, NOT on `steamDegrade.onlyLibrary` — B-WR-01 re-keyed it precisely because the two predicates diverge whenever a wine section renders, which would promise "choose another below" with nothing below. The variant observed was the "choose another below" one, so `gating.libraryDropdown` was genuinely TRUE at render and the selector picked correctly in a reachable state. The complementary `libraryMissingOnlyNotice` branch is `G-D24-ONLYLIB`'s to observe. Install was cancelled, not completed | |
| G-D24-ONLYLIB | electron | pending | **OBSERVE-ONLY — never PASS/FAIL.** Repeat G-D24-DEGRADE with the failing library as the user's ONLY library. Record verbatim what the notice says and what Install does — this behaviour is deliberately undecided | |
| G-D24-ONLYLIB | tauri | OBSERVED (not scored — this row is OBSERVE-ONLY by design) | 2026-08-27, developer-observed under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev` from the isolated worktree. Fixture: `libraryfolders.vdf` trimmed to a SINGLE entry, the empty decoy root from `G-D24-DEGRADE` (exists, no `steamapps/`), so `libraryCount == 1` and the failing library is the user's only one. Original vdf held at `shasum ccd1980e...` for restore. **RECORDED VERBATIM, as the row requires — no verdict is offered.** (1) NOTICE: "Your Steam library couldn't be reached. Reconnect the drive, then try again." — byte-exact against `gamelib:steam.install.libraryMissingOnlyNotice`. (2) NO library dropdown rendered, so `gating.libraryDropdown` was false and the copy selector at `SteamDialog/index.tsx:452` correctly took the "Only" branch — the complement of the `G-D24-DEGRADE` observation, confirming both arms of review B-WR-01's re-keying from opposite sides. (3) INSTALL WAS ENABLED, AND CLICKING IT INSTALLED. It did not error, no-op, or refuse: it CREATED the missing `steamapps/` directory in the decoy root and began downloading `Baldur's Gate: Enhanced Edition` into `decoy-steam-library/steamapps/common/`, reaching 747 MB. Both real libraries were untouched (host `common/` still 23 entries; `/Volumes/blank` still only `Roots of Pacha`). **⚠ CORRECTED SAME DAY — the original wording said the install was "cancelled by the developer"; it was NOT, and the correction matters because the run's ENDING must not be read as product behaviour.** The log shows it ran to completion and then FAILED: `(20:09:18) runNativeDepotDownload: ensureSteamClientReady`, `(20:09:19) resolveSteamInstallTarget took 230ms` (so the target resolved cleanly against the decoy and `installNative` genuinely ran — itself the proof that Install proceeded NATIVELY rather than delegating), then `(20:15:11) steam-flags-census stage=download-complete appId=228280 totalFiles=2455` immediately followed by `SteamGame: depot install failed for appId 228280: The Steam download failed.` after 352138ms. **The failure is attributable to the VERIFIER, not to GameLib:** while that download was live, the verifier ran `rm -rf` over its output directory TWICE (on the mistaken assumption it had already been stopped), with the sidecar holding open write handles on files inside it — `lsof` confirmed handles on `lang/pl_PL/sounds/Male3c.wav` and `music/bf1/bf1j2.acm` at the time. A terminal failure arriving exactly at the `download-complete` census, which expects all 2455 files present, is the expected consequence of deleting a download mid-stream. **THIS RUN THEREFORE CARRIES NO EVIDENCE ABOUT INSTALL-FAILURE BEHAVIOUR and must not be cited as such.** What it does still evidence, unaffected by the interference, is the part observed BEFORE the deletion: Install was enabled, clicking it started a native depot download, and it created the missing `steamapps/` directory in the library the notice had called unreachable. **The observation, stated plainly: the notice tells the user to reconnect a drive and retry, while the button succeeds by fabricating the path the notice called unreachable.** Whether that is right is exactly what this row leaves undecided. **⚠ SEPARATE FINDING, NOT THIS ROW'S — the "Reconnect the drive" copy may name an UNREACHABLE state.** If a sole library's drive is genuinely detached, `getSteamLibraries()` filters the path on `existsSync` (`utils.ts:684`), the list is EMPTY, `resolveQuickInstallTarget` returns `undefined`, and `startSteamQuickInstall` takes the delegated `steam://install` branch — D-24 cannot fire at all. The only routes into `libraryMissingOnlyNotice` are a root that exists with a missing `steamapps/` (this fixture) or a root that is not writable (`validPath = isWritable`, `main.ts:684`); neither is a detached drive. Raised here as an observation for whoever decides this behaviour, deliberately NOT scored and NOT filed as a defect from an observe-only row | |
| G-D29-CONSOLE | electron | pending | Console Mode, primary library unavailable: install shows failure IN PLACE (`--status-danger` title, reason line, single Dismiss chip), no desktop dialog underneath. (1) pressing A does NOT start install anyway; (2) message does NOT auto-dismiss after 1.5s | |
| G-D29-CONSOLE | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-GAMEPAD-CARET | electron | RELOCATED | **Moved to Phase 38 as `38-C07` on 2026-08-27 by operator decision** ("gamepad test should goto 38"). Not a score — never run here, and no observation is claimed. Phase 38 ("Deferred hardware and environment UAT gates") owns every item blocked on hardware this project does not have; it already held `38-C01`..`38-C06`, all blocked on the same missing controller, so this row was a permanently-blocked row in the wrong ledger. Two research facts were carried into `38-C07` so the eventual run does not mis-score: (a) the two surfaces do NOT look alike — MainButton has a real caret (`className="SteamInstallCaret"`, `MainButton.tsx:356`) but the GameCard has NO caret, its entry is the "Install with options…" CONTEXT-MENU item (`GameCard/index.tsx:386-397`), so an operator hunting for a caret on the card would file a false FAIL; (b) the caret leaves the tab order by UNMOUNTING, not by `disabled` (`MainButton.tsx:334-339`), so its correct absence looks nothing like a focus failure. Receipt in this file's frontmatter under `human_verification_relocated` | |
| G-GAMEPAD-CARET | tauri | RELOCATED | **Moved to Phase 38 as `38-C08` on 2026-08-27, same decision.** Split from the electron half rather than relocated as one compound item, per Phase 38 relocation rule (4) — a compound item resolves on a single pass/fail and the un-run half disappears. The split is load-bearing here, not bookkeeping: this row's own pre-existing text already said an Electron PASS is NOT evidence for this cell, and the source reason was pinned for `38-C08` — Tauri builds its own focusable list in `getFocusableElements()` (`src/preload/api/tauriGamepadInput.ts:50-62`) and DROPS any element failing `hasZeroArea(el.getBoundingClientRect())` (`:57`, defined `:46-48`), a filter Electron's native traversal never applies. An icon-only split-button half inside a `flex-wrap: wrap` container is exactly the shape that can measure zero on one runtime and not the other. Receipt in frontmatter | |

---

## PART D — the six upstream handoff / procedure items — 6 items

### G-HANDOFF-D17 — D-17 platform-switch interaction (UI-SPEC steps 1→5)

Preconditions: macOS host, mac-native Steam game with a Windows depot, always-show ON (n/a — no
always-show setting exists per D-26; reach via the caret). PASS = all six sub-steps observed
(selectable macOS-preselected row; flip-to-Windows reveal order engine→bottle-name→D-14 notice;
simultaneous dropdown disappearance; snap with NO height animation; dialog stays within
`max-height: 90vh` scrolling internally; flip-back unmounts wine+notice together and dropdown
reappears if condition holds, discarding any Windows-side selection).

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-D17 | electron | PASS (broader coverage than the tauri half — see asymmetry note) | 2026-08-22, developer-observed on Electron (`electron-vite dev`, PID 74317), independently of the tauri half — **no step was carried across from it.** Fixture: `Wasteland 2: Director's Cut` (404730), mac-native with a Windows depot, not installed, with `/Volumes/blank/SteamLibrary` MOUNTED (29 GiB free, empty) so libraryCount = 2 and `hasChoice` TRUE — the condition the tauri half did NOT have. **Step 1 PASS** — platform row selectable, macOS pre-selected. **Step 2 PASS on substance** — engine dropdown, read-only `GameLibSteam` bottle field and the D-14 notice all appeared. Vertical order is bottle-then-engine here as well, which is `F-34.13-D17-ORDER`, already filed as a CONTRACT defect by operator decision; not re-litigated and not scored as a failure. That the same order appears on both runtimes is expected, not corroborating — one React bundle serves both. **Step 3 PASS — and this clause was INERT on the tauri half.** With the USB library mounted, the library dropdown was on screen before the flip and DISAPPEARED on flipping to Windows, the destination no longer being a host library. **Step 4 PASS** — wine section snapped in, no ease/slide/height animation. **Step 5 PASS** — dialog stayed within the window, no footer clipping, no overflow. **Step 6 PASS on both reachable halves — the dropdown half was likewise INERT on tauri.** Flipping back to macOS unmounted the wine section and D-14 notice together, and the library dropdown REAPPEARED, the >1-library condition still holding. Discard half remains **UNEXERCISABLE** on this runtime too: only `Wine-Crossover-latest` is installed under `tools/wine/`, so the enabled engine dropdown offers exactly one entry and no alternative selection exists to make and then discard — the same single-engine hardware gap that blocks `G-HANDOFF-D15`. **Recording-integrity note:** the four remaining steps were put to the developer as a numbered list labelled 1/2/4/5 and answered as 1/2/3/4. The renumbering is sequential and the content mapping is unambiguous — exactly four questions were asked and four affirmatives returned — but it is recorded rather than silently normalised, because a mis-mapped answer is exactly the kind of thing a ledger should surface instead of resolving on the scorer's confidence. Steps 3 and 6 were reported in the developer's preceding message, before these four. **RUNTIME ASYMMETRY, carried forward as a real residual:** the dropdown disappear/reappear behaviour (step 3, step 6's second half) is now proven on **Electron only**. The tauri half was run at libraryCount = 1 and those clauses were recorded INERT there; they are NOT retroactively credited, since that observation could not have seen them. Closing the asymmetry needs one re-run of `G-HANDOFF-D17`/tauri with the drive mounted — cheap, and it does not block this row | single CrossOver engine installed (discard clause only) |
| G-HANDOFF-D17 | tauri | PASS (one contract defect remains; the two inert clauses are now PROVEN — see the 2026-08-27 addendum) | 2026-08-22, developer-observed under `pnpm tauri:dev` (PID 54464, `build/assets` rebuilt 13:48 from an emptied directory; arm `[bootstrap] secret stores: keyring` at 13:45:08, NOT the `dev-vault` arm the 2026-08-19/22 rows used). Fixture: `Wasteland 2: Director's Cut` (404730), mac-native WITH a Windows depot, not installed. **Step 1 PASS** — platform row selectable, macOS pre-selected. **Step 2 — all three regions APPEARED, but NOT in the plan's stated top-to-bottom order.** Observed: CrossOver bottle-name field FIRST, wine-version/engine dropdown SECOND. The plan's step 2 enumerates "in top-to-bottom order … CrossOver engine `SelectField` … ; read-only bottle-name field … ; D-14 notice", i.e. engine before bottle-name. **Scored as a CONTRACT DEFECT, not an app defect, under the operator's explicit decision on 2026-08-22** — see the `F-34.13-D17-ORDER` gap entry below for the three grounds. Verified in source before scoring rather than taken on the observation alone: `WineSelector/index.tsx:224` renders the `CrossOver Bottle` `TextInputField` and `:242` the `install.wineversion` `SelectField`, so the shipped order genuinely is bottle-then-engine and the developer's report is exact. **Step 3 INERT** — `libraryCount = 1` at observation time (only `Macintosh HD` mounted; `/Volumes/NO NAME` and `/Volumes/blank` both absent from disk though present in `libraryfolders.vdf`, and `getSteamLibraries()` filters on `existsSync` at `src/backend/utils.ts:680`), so `hasChoice` was false and no library dropdown was ever on screen to disappear. Not a pass — there was nothing to observe. **Step 4 PASS** — the wine section SNAPPED in; no ease, no slide, no height animation. This is the clause most likely to be missed, because an animation reads as polish rather than as the named FAIL it is. **Step 5 PASS** — dialog stayed within the window, no footer clipping, no overflow. **Step 6 — SPLIT.** Unmount half **PASS**: flipping back to macOS unmounted the wine section and the D-14 notice TOGETHER. Dropdown-reappear half **INERT** for the same `libraryCount = 1` reason as step 3. Discard half **UNEXERCISABLE**: the wine-version dropdown is editable (not greyed — confirmed with the developer specifically, since greyed would contradict `G-D16-CROSSOVER`'s own PASS) but offers **exactly one entry**, only `Wine-Crossover-latest` being installed under GameLib's `tools/wine/`, so no alternative selection exists to make and then discard. **This is the SAME single-engine blocker that stops `G-HANDOFF-D15`** — one hardware-state gap disables clauses in two separate rows, and neither is an operator failure. **What this row does NOT prove:** the ordering assertion as the plan wrote it, the dropdown's disappear/reappear behaviour, and the engine-choice-discarded-across-toggle behaviour. Four of six steps are discharged whole | single CrossOver engine installed (discard clause); `libraryCount = 1` (steps 3, 6-dropdown)  **RESIDUAL CLOSED 2026-08-27.** The dropdown disappear/reappear clauses were previously INERT on this runtime because the tauri half ran at `libraryCount = 1`, where a library dropdown has nothing to show and its absence proves nothing. Re-run today at **`libraryCount = 2`** — primary plus `/Volumes/blank/SteamLibrary` (29 GiB free); the stale `/Volumes/NO NAME` entry is still in `libraryfolders.vdf` but is correctly dropped by `getSteamLibraries()`'s `existsSync`, so the count is 2, not 3. Fixture: `Machinarium` (40700), a mac+windows title, uninstalled and captured. Under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev` from the isolated worktree, developer-observed, each clause reported separately: (1) platform row SELECTABLE with **macOS pre-selected**; (2) switching to Windows -> wine section APPEARS **and the "Choose Steam library" dropdown DISAPPEARS**; (3) switching back to macOS -> dropdown **REAPPEARS** and the wine section goes. Clause (2)'s dropdown half was confirmed EXPLICITLY rather than inferred from clause (3)'s "reappear" — inferring it is what left this residual open in the first place. The contract defect `F-34.13-D17-ORDER` (shipped wine section renders bottle-name before engine, the plan's step 2 says the reverse) is UNAFFECTED by this run and still stands |

### G-HANDOFF-D15 — the no-re-ask handoff (34.13-09's eight steps, verbatim)

Preconditions: macOS host, Windows-only bottle-eligible game, bottle NOT yet provisioned, ≥2 CrossOver
engines detected. PASS = steps 3, 4, 5, 6, 7, 8 all observed as stated (wizard shows the non-first
engine chosen in step 1 already selected with no re-ask; bottle-name still `GameLibSteam` read-only;
consent step still requires explicit "Set up Steam" click; changing the engine in the wizard sticks;
closing without confirming then repeating does NOT carry a stale seed; step 8 repeats 1-3 under
`pnpm tauri:dev`).

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-D15 | electron | NOT ATTEMPTED | 2026-08-22. **NOT ATTEMPTED — operator decision to park 34.13 testing, taken after the setup was fully armed, not before.** Developer's stated reason, recorded close to verbatim rather than paraphrased: cannot change the bottle (it is the shared Steam bottle) and therefore cannot change the wine version. **Setup that WAS completed and then reversed, so a future run knows it is reachable:** a second CrossOver engine (`Wine-23.7.1-1`) was installed through the Wine Manager alongside `Wine-Crossover-latest`, satisfying the two-engine precondition; and the bottled `drive_c/Program Files (x86)/Steam/steam.exe` was moved aside so `isBottleReady()` (`steam/bottle.ts:240`) returned false and the `steamBottleSetupRequired` wizard would fire from all three emit sites (`steam/games.ts:1023`, `:1990`, `:2221`). **`steam.exe` has been RESTORED** — verified present at 5,774,488 bytes with `cxbottle.conf` intact, so the bottle is back to normal and no cleanup is owed. **A CORRECTION worth carrying forward:** this row was described six times earlier in the session as requiring the 173 GB bottle to be DELETED. That was WRONG. Readiness is two `existsSync` calls — `isBottleProvisioned()` (cxbottle.conf) AND the bottled steam.exe — so hiding one 5.8 MB file arms it, and hiding steam.exe rather than the conf keeps `isBottleProvisioned()` TRUE, which is `provisionBottle()`'s re-entrancy guard against `cxbottle --create` re-running. The claim was repeated before it was checked. **SCORER'S INFERENCE, flagged as inference and NOT as the developer's reason:** step 6 requires changing the engine inside the wizard, and D-14 makes the chosen wine version apply to EVERY Steam game needing a bottle — with ~173 GB across 10 games already provisioned against `Wine-Crossover-latest`, that is a global change, not a test-local one. If that reading is right, this row is structurally expensive on ANY machine with a populated shared bottle and cheap only on a fresh one, which is a property of the item rather than of this hardware. A future planner should confirm or reject that before assuming a re-run is cheap | Shared-bottle engine change (operator); 34.13 testing parked 2026-08-22 |
| G-HANDOFF-D15 | tauri | NOT ATTEMPTED | 2026-08-22. **NOT ATTEMPTED — operator decision to park 34.13 testing, taken after the setup was fully armed, not before.** Developer's stated reason, recorded close to verbatim rather than paraphrased: cannot change the bottle (it is the shared Steam bottle) and therefore cannot change the wine version. **Setup that WAS completed and then reversed, so a future run knows it is reachable:** a second CrossOver engine (`Wine-23.7.1-1`) was installed through the Wine Manager alongside `Wine-Crossover-latest`, satisfying the two-engine precondition; and the bottled `drive_c/Program Files (x86)/Steam/steam.exe` was moved aside so `isBottleReady()` (`steam/bottle.ts:240`) returned false and the `steamBottleSetupRequired` wizard would fire from all three emit sites (`steam/games.ts:1023`, `:1990`, `:2221`). **`steam.exe` has been RESTORED** — verified present at 5,774,488 bytes with `cxbottle.conf` intact, so the bottle is back to normal and no cleanup is owed. **A CORRECTION worth carrying forward:** this row was described six times earlier in the session as requiring the 173 GB bottle to be DELETED. That was WRONG. Readiness is two `existsSync` calls — `isBottleProvisioned()` (cxbottle.conf) AND the bottled steam.exe — so hiding one 5.8 MB file arms it, and hiding steam.exe rather than the conf keeps `isBottleProvisioned()` TRUE, which is `provisionBottle()`'s re-entrancy guard against `cxbottle --create` re-running. The claim was repeated before it was checked. **SCORER'S INFERENCE, flagged as inference and NOT as the developer's reason:** step 6 requires changing the engine inside the wizard, and D-14 makes the chosen wine version apply to EVERY Steam game needing a bottle — with ~173 GB across 10 games already provisioned against `Wine-Crossover-latest`, that is a global change, not a test-local one. If that reading is right, this row is structurally expensive on ANY machine with a populated shared bottle and cheap only on a fresh one, which is a property of the item rather than of this hardware. A future planner should confirm or reject that before assuming a re-run is cheap. This row is step 8 ONLY (a repeat of steps 1-3), and step 8 is downstream of the electron half's steps 1-7, so it is blocked transitively rather than independently | Blocked transitively by the electron half; 34.13 testing parked 2026-08-22 |

### G-HANDOFF-LAUNCH — is a forced install actually launchable? (T-34.13-06-06, CLOSED by 34.13-14)

Preconditions: a completed forced Windows-via-bottle install from G-HANDOFF-D17, macOS host. **Real
PASS/FAIL gate now** — a FAIL is a defect in 34.13-14, not an expected/escalated outcome. Record all
FOUR surfaces individually — "it launched" alone is not an acceptable observation.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-LAUNCH | electron | pending | Record individually: (1) shows installed, size/path reflect bottle copy; (2) Launch route — Steam log records launching via the bottled Steam client, NOT a `steam://rungameid` dispatch to host client; (3) settings screen shows bottle's wine version + `GameLibSteam` name; (4) `isNative()` observably false — pre-launch wine check runs | |
| G-HANDOFF-LAUNCH | tauri | PASS | 2026-08-27, developer-observed + log/process-confirmed under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev` from the isolated worktree. **PRECONDITION SATISFIED PROPERLY — this is a GENUINELY FORCED install, not a lookalike.** Fixture built for this row: `Machinarium` (40700), a mac+windows title, installed through the D-17 platform-switch flow with Windows deliberately selected over the pre-selected macOS. Verified persisted: `SteamGame: appId 40700 — persisted forced Windows-via-bottle verdict after a committed install` (18:33:38) and `forcedWindowsViaBottle: true` in the metadata store. **This matters: `isBottleEligible()` is an OR — `isBottleEligibleFromPlatforms() || isForcedWindowsViaBottle()` (`games.ts:1887-1891`) — so a WINDOWS-ONLY title reaches the bottle through the PLATFORM arm and would exercise none of D-17's forced path.** `All Will Fall` (2706020) was rejected as a fixture for exactly that reason; it carries no forced flag at all, correctly. Scoring this row on such a title would have been a confident PASS over an untouched arm. **All FOUR surfaces recorded individually:** **(1)** shows installed; size and path reflect the BOTTLE copy — `install_size "296.22 MiB"`, `platform "Windows"`, `install_path .../CrossOver/Bottles/GameLibSteam/drive_c/Program Files (x86)/Steam/steamapps/common/Machinarium`; bottle ACF `StateFlags "4"`, `installdir "Machinarium"`, `BytesDownloaded == SizeOnDisk == 310605728`. **(2)** LAUNCH ROUTE — stated outright in the log, not inferred: `SteamGame: launching appId 40700 via the bottled Steam client` (18:39:12), then `Running Wine command: .../GameLibSteam/drive_c/Program Files (x86)/Steam/steam.exe -applaunch`. Process table confirms it independently: `winewrapper.exe --run -- .../GameLibSteam/.../steam.exe -applaunch 40700`, and `Machinarium.exe` running from the bottle. **NO `steam://rungameid` dispatch to the host client** — the host Steam was NOT running before the launch and was STILL not running after, so the host path is excluded by evidence rather than by inference. **(3)** settings screen showed the bottle's wine version and the `GameLibSteam` bottle name, developer-observed. **(4)** `isNative()` observably false — the pre-launch wine check RAN: `Checking if wine version exists: CrossOver - 26.2` (18:39:12) immediately followed by the Wine command above. **Recorded honestly: the developer reported seeing NO pre-launch wine check, and that report is ACCURATE — the check is silent and log-only, not a visible UI step.** The row's wording invites a false FAIL here; a native title would never check a wine version nor run a Wine command, so the assertion is satisfied by the log. **The forced install is genuinely launchable — the game ran.** **Incidental, not scored and not a defect of this row:** `raiseFrontmostBottledProcess [launch]: no matching process within ~18s — falling back to app.hide()` (18:39:33) — GameLib could not find the bottled process to bring frontmost and fell back. Consistent with the known CrossOver off-screen-window behaviour; the game still launched and ran | |

### G-HANDOFF-UNINSTALL — does uninstalling a forced install actually remove the bottle copy?

Preconditions: the forced install from G-HANDOFF-LAUNCH, still installed. Run AFTER G-HANDOFF-LAUNCH,
BEFORE G-HANDOFF-REINSTALL.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-UNINSTALL | electron | pending | Record: (1) on-disk path under Phase 17 `GameLibSteam` bottle's `steamapps/common/` before uninstall (redact account identifiers), `appmanifest_<appId>.acf` exists; (2) Steam log records delegating uninstall via bottled client, not `steam://uninstall` to host; (3) ON DISK: directory AND `appmanifest_<appId>.acf` both gone; (4) badge flips to not-installed without app restart | |
| G-HANDOFF-UNINSTALL | tauri | PASS | 2026-08-27, developer-observed + disk/log-confirmed under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev`. Run AFTER `G-HANDOFF-LAUNCH`, BEFORE `G-HANDOFF-REINSTALL`, on the same genuinely-forced `Machinarium` (40700) fixture. Game process quit first; the host Steam CLIENT (`steam_osx`) was not running before or after, so a `steam://uninstall` dispatch to the host is excluded by EVIDENCE. **⚠ WORDING CORRECTED 2026-08-27** — this originally read "host Steam NOT running before or after", which claimed more than was true: a host `ipcserver` HELPER (`Steam.AppBundle/.../MacOS/ipcserver`, pid 74469) had in fact been up since 06:31 that morning, predating the entire session. The exclusion SURVIVES the correction on two independent grounds — `ipcserver` alone cannot perform an uninstall, and the log names the bottle route explicitly — but the original phrasing would have been falsified by a single `ps`, so it is tightened here rather than left to be discovered. **All four recorded individually:** **(1)** BEFORE-state captured independently rather than recalled: 296 MB / 36 files at `.../CrossOver/Bottles/GameLibSteam/drive_c/Program Files (x86)/Steam/steamapps/common/Machinarium`, and `appmanifest_40700.acf` present (605 bytes, `StateFlags "4"`, `installdir "Machinarium"`, `buildid 24417709`). Note the ACF had been REWRITTEN by the bottled client at 18:44 — the Phase 17 bottle's Steam adopted GameLib's manifest on its startup scan, confirming the handoff. **(2)** UNINSTALL ROUTE — delegated via the bottled client: `SteamGame: removed bottle install for appId 40700 at ".../CrossOver/Bottles/GameLibSteam/drive_c/..."` (18:49:04). No host dispatch; host Steam never started. D-17 reversibility also fired: `SteamGame: appId 40700 — cleared forced Windows-via-bottle verdict (bottle ACF confirmed absent)`. **(3)** ON DISK: the content directory is GONE and `appmanifest_40700.acf` is GONE — both verified by direct filesystem check, not by the badge. Residue that REMAINS is client-side and correctly retained, not the install: `steamapps/shadercache/40700`, `Steam/userdata/<accountId>/40700`, `Steam/appcache/librarycache/40700`, and the Amanita-Design save folders under `drive_c/users/crossover/AppData/{Local,Roaming}`. Deleting user SAVE data on uninstall would be the defect; the row asks for the directory and the ACF, and both are gone. **(4)** badge flipped to not-installed with NO app restart — developer-observed, corroborated by `Steam: uninstall polling complete appId 40700 — badge flipped not-installed` (18:49:04). **⚠ SEPARATE DEFECT REPRODUCED IN THIS RUN — NOT THIS ROW'S ASSERTION, NOT SCORED HERE.** 50 seconds after the badge flip the game VANISHED from the library grid: `(18:49:54) [ERROR]: [Frontend]: Library: 1 owned Steam game(s) silently excluded from library grid by stale nonAvailableGames entry: 40700`. That line is the blind-spot guard belonging to the PARKED debug session `.planning/debug/uninstall-game-vanishes.md`, and it fires only when a game is still excluded AFTER the reconciliation pass (`reconcileTick` is in its dependency array). The session was reparked 2026-08-19 as "UNREPRODUCED across 2 instrumented attempts" — **this is the instrumented reproduction it was awaiting**, logged with the mechanism named. Recorded to that debug file the same day | |

### G-HANDOFF-REINSTALL — reversibility: is a later NATIVE re-install mis-routed?

Preconditions: G-HANDOFF-UNINSTALL completed, same mac-native game, reached via the caret ("always-show
ON" language from the plan is stale — there is no such setting; use the caret).
⚠ Specified behaviour, NOT a defect: re-installing WITHOUT uninstalling first re-routes to the bottle —
if observed, record as a note, do not mark FAIL for it.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-REINSTALL | electron | pending | Record: (1) platform row selectable, macOS pre-selected, no wine section while macOS selected; (2) Steam log shows NATIVE install path, not bottled client; (3) launches natively via host Steam client; (4) settings screen shows per-game config, not bottle's wine version | |
| G-HANDOFF-REINSTALL | tauri | PASS | 2026-08-27, developer-observed + disk/process-confirmed under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev` from the isolated worktree `~/Projects/GameLib-wt/uat3413`. LAST of the strict handoff chain, run immediately after `G-HANDOFF-UNINSTALL` on the same `Machinarium` (40700) fixture. **Preconditions verified on disk before starting, not assumed from the uninstall row:** `is_installed:false` with `install:{}`; `forcedWindowsViaBottle:false` in BOTH `steam_library.json` and `steam_metadata.json`; `appmanifest_40700.acf` absent from the `GameLibSteam` bottle AND from host Steam; `is_mac_native:true`, `platformsCaptured:true`; `enableSteamNativeInstall = true` in settings. That last flag decides which native path is under test: `install()` falls past `routeThroughBottle` (now false) into `installNative()` — GameLib's OWN depot orchestrator — NOT the Phase 24 bridge, since `isBridgeEligible()` requires `isBottleEligibleFromPlatforms()`, false for a mac-native title (`games.ts:1913`). **All four recorded individually.** **(1)** reached via the CARET (options dialog), not the button face: platform row selectable with macOS PRE-SELECTED — developer-observed. **(2)** with macOS selected the wine section was ABSENT entirely (no bottle-name field, no wine version) — developer-observed; and the install route was NATIVE, evidenced three independent ways rather than by a log line: content landed in the HOST Steam library at `~/Library/Application Support/Steam/steamapps/common/Machinarium` (created 19:02, 301 MB / `SizeOnDisk 315720790`); GameLib's OWN sidecar (pid 62205) held a live open WRITE handle inside `Machinarium.app/Contents/Resources/arch_mac/` mid-download, so GameLib performed the transfer itself; and the `GameLibSteam` bottle gained NOTHING (`appmanifest_40700.acf` still absent there afterwards). Host `appmanifest_40700.acf` written at 19:04, 543 bytes, `StateFlags "4"`, `installdir "Machinarium"`, `buildid 24417709`. **(3)** launched natively via the HOST Steam client, captured live in `ps` while running: game process `76577 = .../Steam/steamapps/common/Machinarium/Machinarium.app/Contents/MacOS/Machinarium` (the native mac binary at the host path), with host `steam_osx` pid 76420 STARTED 19:03:44 — i.e. brought up to service this launch; only the `ipcserver` helper had been up before. The bottled `steam.exe` (pid 74102) remained parked on `-applaunch 2706020` throughout and never touched 40700, so the bottle route is excluded by PROCESS EVIDENCE, not by absence of a log line. Developer quit the game deliberately (not a crash). **(4)** settings screen showed ORDINARY per-game settings — developer-observed; corroborated by `GamesConfig/40700.json` being `{}` (no wine version pinned) and the library entry reading `install.platform: "Mac"` with `install_path` under host Steam and no `forcedWindowsViaBottle` key. **The 32-bit risk flagged before the run did NOT materialise and the stale signal is worth recording:** `steam_metadata.json` still carries `mac_arch:"unknown"` (`mac_arch_source:"minos"`), but the shipped binary is a universal `x86_64 + arm64` Mach-O (`lipo -archs`), so it runs natively on Apple Silicon. The metadata field was never resolved for this title; it is not evidence of a 32-bit build. **⚠ The row's "always-show ON" precondition language is stale** — there is no such setting; the caret is the route, as the section header above already notes. The specified-behaviour carve-out (re-install WITHOUT uninstalling first re-routes to the bottle) was NOT exercised here and remains untested — this run uninstalled first, as the chain requires | |

### G-D25-INDIALOG — the cold-cache in-dialog loading state (full procedure; PART C's row is the same item, recorded once here)

> ⚠ INVERTED from this item's pre-amendment predecessor: the dialog now mounts IMMEDIATELY and the loading state
> lives INSIDE it. A half-rendered dialog is CORRECT; a busy origin button is now a FAILURE.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-D25-INDIALOG | electron | PASS | 2026-08-27, developer-observed + log-confirmed, run from the isolated worktree `~/Projects/GameLib-wt/uat3413` @ `1ff9cfaff`. Fixture: `Fallout: New Vegas` (22380), Windows-only, uninstalled. **All five regions observed individually; none inferred from another.** **(3)** dialog opened IMMEDIATELY with the platform row present at first paint — observed three separate times (twice on the fast path, once with the probe armed). The row correctly offered a single platform option, this title being Windows-only. **(4)** with the probe genuinely armed: spinning refresh icon PRESENT in the wine section, the dialog's OWN Install button DISABLED, and the library dropdown + free-space line ABSENT for the entire pending window — absent throughout, not flash-then-vanish. Developer confirmed each of the four clauses separately. **(5)** the origin control showed NO busy state — observed on all three runs. This is the D-25 inversion of the retired D-12 contract and is the half most likely to regress silently. **(6)** primary-half click on the SAME title under the SAME armed conditions started installing immediately with no stall. Under the arming setup this is a binary result rather than a timing judgement: the store API was hanging for a full 15s, so any probe on the quick path would have produced an unmistakable 15-second wait. It did not. Install was cancelled at once — only start behaviour is claimed, not a completed download. **(7)** BOUNDED, evidenced by log line rather than recollection: `(17:34:04) [WARNING]: [Steam]: Steam metadata fetch failed for appId 22380: AxiosError: timeout of 15000ms exceeded` — exactly `METADATA_FETCH_TIMEOUT_MS = 15000` (`src/backend/storeManagers/steam/state.ts:30`), hit on the fixture title itself; 70 bounded timeouts across the session, all terminating. Not unbounded ⇒ PASS. **⚠ The row's stated precondition ("delete `steam_metadata.json`") arms NOTHING and cost three attempts** — the probe short-circuits on `steamPlatformsCaptured` in a DIFFERENT store, and the metadata file refills itself to 224 entries on launch. Full arming procedure, the two failed attempts, and why the one naturally-uncaptured title is a dead end are recorded in the `## Current Test` block above; read it before running the tauri half | |
| G-D25-INDIALOG | tauri | PASS | 2026-08-27, developer-observed + log-confirmed under `GAMELIB_DEV_SECRET_VAULT=1 pnpm tauri:dev` (never bare `tauri dev`) from the isolated worktree `~/Projects/GameLib-wt/uat3413` @ `1ff9cfaff`, `build/` emptied first. Same fixture as the electron row — `Fallout: New Vegas` (22380), still uncaptured with metadata still cold, both re-verified after the tauri process started. **Observed independently; NOTHING carried across from the electron half.** **(3)** dialog opened immediately, platform row present. **(4)** spinning refresh icon PRESENT, dialog's own Install button DISABLED, dropdown + free-space line ABSENT for the whole pending window. **(5)** origin control showed NO busy state. **(7)** developer reported it settling after ~15s, corroborated in-runtime by **75** bounded `timeout of 15000ms exceeded` lines in the tauri window, all terminating, plus `getSteamInstallSize: store API fetch failed for appId 22380: Error: connect ETIMEDOUT` against the blackholed store host. Bounded, not unbounded ⇒ PASS. This is the clause the row calls out as signalling a channel registered on only one runtime; it resolved on BOTH, so no such gap exists. **(6)** primary-half click was ACCEPTED immediately with no stall — the game entered the download queue at once (`17:52:27 Fallout: New Vegas was added to the download queue`). Binary under the arming setup: a blocking probe would have produced a 15s wait before anything happened, and none occurred. **⚠ WORDING CORRECTED 2026-08-27 — this region originally read "started installing immediately", which claimed more than was observed.** THIS RUNTIME IS SIGNED OUT OF STEAM and no install can actually proceed here: `Steam: logged in but no stored refresh token — cannot reconnect` / `downloadSteamDepots: no authenticated Steam CM connection` / `depot install failed for appId 22380: You are not signed in to Steam, so this download could not start` (17:52:43, ~16s after the queue entry). **The region-6 claim SURVIVES the correction** because what it asserts is the ABSENCE OF A BLOCKING PROBE at click time, and a probe would have stalled the click for 15s whatever the auth state — the auth failure lands strictly AFTER the queue entry, on a separate code path (`ensureSteamClientReady` -> `downloadSteamDepots`). It does NOT survive as evidence that an install runs; that is `G-QUICK-NOPROBE`'s and `G-QUICK-DEFAULT`'s clause and is recorded there, unscored. **Incidental, not a defect:** on the repeat click the button offered RESUME rather than Install, because the earlier cancelled attempts left partial content on disk — the documented "interrupted install detected on startup — surfacing as resumable, NOT auto-resuming" behaviour. It is a side-effect of this gate's own repeated clicking, not a product finding | |

---

## Gaps

<!-- Structured for /gsd-plan-phase --gaps. APPEND only. -->

```yaml
- truth: "The Steam install-options wine section shows a bottle-name field prefilled with `GameLibSteam`, read-only (D-05, G-ROW-1 + G-D05-BOTTLENAME)."
  status: resolved
  resolved_by: "83866f847 fix(quick-260819-s8p) — 2026-08-19 20:27, ALREADY IN HEAD before the
    2026-08-22 observation. Added src/frontend/screens/Game/GamePage/components/steamBottleDefaults.ts
    (frontend mirror of DEFAULT_STEAM_BOTTLE_NAME) and seeds crossoverBottle at
    InstallModal/index.tsx:527 behind isSteamManagedApp."
  amended_2026_08_22: "AMENDED IN PLACE, NOT DELETED. The two artefact claims below are now FALSE:
    DEFAULT_STEAM_BOTTLE_NAME no longer has zero frontend references, and a code path CAN now
    populate it. Field observed prefilled GameLibSteam on 2026-08-22 (G-D05-BOTTLENAME/electron
    PASS). Left in place because the reasoning records how a CODE-READ prediction went stale
    against a fix that landed the same day — see also G-ROW-1, whose FAIL rests on this cause."
  reason: "User reported: 'crossover bottle - blank, read only'. The field renders read-only but with no value."
  severity: major
  test: G-ROW-1 (electron AND tauri — both observed independently, both FAIL)
  runtimes_affected: both
  scope: "Frontend-only. Both runtimes serve the same React bundle, so this is one defect, not two."
  root_cause: "`InstallModal/index.tsx:127` initialises `useState('')` for `crossoverBottle` and never seeds it for the Steam path. That empty string is passed to `<WineSelector ... bottleNameReadOnly>` at `:634`. `DEFAULT_STEAM_BOTTLE_NAME` ('GameLibSteam') is defined at `src/backend/storeManagers/steam/constants.ts:21` and has zero frontend references, so no code path can ever populate it. Read-only + unseeded = permanently blank."
  artifacts:
    - src/frontend/screens/Library/components/InstallModal/index.tsx:127
    - src/frontend/screens/Library/components/InstallModal/index.tsx:634
    - src/backend/storeManagers/steam/constants.ts:21
  missing:
    - "A frontend-reachable source for the default Steam bottle name. `DEFAULT_STEAM_BOTTLE_NAME` currently lives in a backend module; the fix needs it in a shared location or delivered over the existing preload seam. NOTE for the planner: 34.13 declared `src/common/types.ts` CLOSED to edits — confirm whether that fence is still in force before siting the constant there."
    - "Seeding of `crossoverBottle` when the install target is Steam and `gating.wineSection` is true."
  also_predicts:
    - "G-D05-BOTTLENAME (both runtimes) — same field, same expectation, not yet run."
```

```yaml
- truth: "Flipping the platform selector to Windows reveals the wine section's regions in the plan's
    stated top-to-bottom order: CrossOver engine SelectField, then the read-only bottle-name field,
    then the D-14 notice (34.13-13-PLAN.md PART D, G-HANDOFF-D17 step 2)."
  id: F-34.13-D17-ORDER
  status: contract-defect
  severity: process-not-product
  found: "2026-08-22, G-HANDOFF-D17/tauri, live run on Wasteland 2: Director's Cut (404730)."
  observed: "Shipped order is bottle-name FIRST, engine SECOND — the reverse of the plan's first two
    entries. All three regions DO appear, and appear together inside the same block."
  source_evidence: "src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx:224
    (CrossOver Bottle TextInputField) precedes :242 (install.wineversion SelectField). The D-14 notice
    renders after {children} at SteamDialog/index.tsx:474-482. Confirmed by reading the components, not
    inferred from the observation."
  disposition: "Scored as a CONTRACT DEFECT — plan prose drift — and G-HANDOFF-D17 PASSes on substance.
    OPERATOR DECISION, 2026-08-22, taken explicitly rather than assumed by the scorer, who put both
    readings and a recommendation to the developer before writing anything."
  grounds:
    - "34.13 never touched field ORDER. It made the bottle field read-only (D-05) and filtered engines
      to CrossOver-only (D-16); both already PASS. The developer reports the order has always been this
      way, i.e. it is pre-existing upstream WineSelector layout."
    - "D-17 itself says nothing about field order. Read at scoring time: it decides that the macOS
      platform row is SELECTABLE and that SteamGame.install() must honour platformToInstall over
      isBottleEligible()'s verdict. The top-to-bottom list is the plan author's prose, not the
      decision's content."
    - "This ledger already carries the same shape: G-D14-NOTICE records PLAN PROSE DRIFT, NOT AN APP
      DEFECT, where a tester character-diffing the plan text would have raised a FALSE FAIL."
  generalisable_rule: "A gate step that enumerates regions AND asserts their order in one sentence
    conflates two independent requirements, and its FAIL condition then fires on either. The
    substantive assertion (all three regions appear, together, on the flip) and the cosmetic one (this
    exact vertical order) should have been separate rows. Cf. the standing lesson that a contract can
    be defective through the INTERACTION of two valid requirements — review pairs, not items."
  action_required: "None for the app. If the vertical order is genuinely wanted engine-first, that is a
    NEW design decision and a new plan — not a 34.13 regression, and not something this gate can compel."
```

## Recording integrity

- Manifest re-derived from the plan's own tables at ledger-creation time: **5 (Part A) + 9 (Part B) +
  16 (Part C) + 6 (Part D) = 36**, matching `34.13-13-PLAN.md`'s stated count.
- The entire pre-amendment id family (the always-show-setting item, the busy-state item this ledger's
  D-25 procedure supersedes, the retired auto-open-trigger matrix rows 3a/6a/6b/9/10/10a/10b/11/12, and
  the retired no-Windows-depot row-5 variant) is confirmed absent from this ledger's actual rows —
  verified by the phase's own ledger-completeness gate, which independently re-derives this same
  36-item count and asserts each one specifically.
- No 17-digit SteamID64, account name, or token appears anywhere in this file (nothing has been
  transcribed from a real session yet — this is the pre-populated skeleton).
- Every row's Disposition is `pending`. **None of the 72 rows above have been executed, simulated, or
  inferred by this executor.** This is the enumeration required before the blocking gate runs, not the
  gate result.

## Roll-up (as of ledger creation)

- PASS: 0
- FAIL: 0
- NOT ATTEMPTED: 0
- pending: 72 (36 items × 2 runtimes)

**Phase status recommendation: `human_needed`.** The blocking manual gate (Task 3) has not been
executed — that is reserved for the human developer per this session's explicit instruction. This
ledger is the enumerated skeleton the developer fills in; it is not evidence of any item passing.
