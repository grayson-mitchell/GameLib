---
phase: 34.13
slug: steam-install-time-wine-bottle-form-gog-parity
status: partial
manifest_count: 36
created: 2026-08-15
---

# Phase 34.13 — Manual UAT Ledger

> Prepared by 34.13-13, Task 3 (preparation half only — this executor does not perform, simulate, or
> infer any of the observations below; that is BLOCKING and reserved for the human developer).
> Source of truth: `34.13-13-PLAN.md`'s `<how-to-verify>` (36-item manifest, amended 2026-08-15 for
> D-21..D-29). Do NOT reconcile this ledger against any older 32-row version — the id set changed.

## Ground rules (see the plan's `<how-to-verify>` for full text)

1. **Two runtimes, always** — Electron (normal dev build) and `pnpm tauri:dev` (**never** bare
   `tauri dev`, which serves a stale static bundle; any row run under it is void and must be re-run).
2. **Enumerate, never sample.** No row may be marked PASS by inference from another row, even rows
   that render identically.
3. **Three dispositions only: `PASS` / `FAIL` / `NOT ATTEMPTED`.** No "looks fine", no blank cells.
   `NOT ATTEMPTED` must name the blocking host OS or fixture and stays an OPEN item.
4. **The phase does not close on a partial gate.** Any `FAIL` or `NOT ATTEMPTED` keeps the phase
   `human_needed`, itemized individually here — never rolled up into `gaps_found`.
5. **Redact before committing.** No SteamID64 (17 digits), account name, or token in any cell.
6. Historical note: a dead click between Wave 4 and Wave 6 was expected and is not re-litigated here.
   At this gate (post Wave 6), a dead click **is** a failure.

Every row below currently carries `pending` — this executor has not run any of them. Replace `pending`
with the real disposition and a real Observation (what was actually seen, not the expectation restated)
before this ledger can close the phase. `Blocker` is filled only for `NOT ATTEMPTED`.

---

## Current Test

<!-- Added 2026-08-19 at gate start so an interrupted session can resume. OVERWRITE, never append. -->

item: G-ROW-2/3-noWin
runtime: electron
part: B
row: 19 of 72 (canonical ledger position; 10 of 72 resolved)
awaiting: user response

**Fixture note:** this row needs a DIFFERENT game than G-ROW-3/G-ROW-4 — one with NO Windows depot
(per `G-ROW-3` tauri's finding that the row-3/4 game does carry one).

**Scoring rule for the rest of PART B — do NOT double-count the bottle-name defect.** `G-ROW-4`
and any other row whose spec says only "wine section PRESENT" is satisfied by the section being
present. The blank bottle-name field is already itemised once in `## Gaps` against `G-ROW-1` and
is owned by `G-D05-BOTTLENAME`. Re-failing every wine-bearing row on the same single frontend
defect would inflate the gap count and misrepresent the blast radius to the fix planner.

**1 defect, 2 rows FAILED so far** (`G-ROW-1` electron + tauri) — see `## Gaps`. Frontend-only:
one fix closes both runtimes. Expect `G-D05-BOTTLENAME` (2 more rows) to fail on the same cause.

**FIXTURE CHANGED MID-SESSION 2026-08-19 ~19:00 — the `hasChoice` deferral is LIFTED.** A USB drive
was attached and registered, so `libraryfolders.vdf` now holds TWO entries
(`~/Library/Application Support/Steam` and `/Volumes/NO NAME/SteamLibrary`) and
`defaultSettings.enableSteamNativeInstall` is `true` in `~/Library/Application Support/gamelib/config.json`.
`hasChoice` is therefore now **TRUE**.

Consequences — **the two matrix rows have SWAPPED which one needs a special fixture**:
- NOW RUNNABLE with no setup: rows 3/4 (`G-QUICK-MAC-MULTILIB`), `G-ROW-3`, `G-ROW-4`, `G-D02-TARGET`.
- NOW NEEDS A FIXTURE: `G-ROW-2` (the ≤1-library / native-OFF case). Reach it by ejecting the USB
  **or** toggling native Steam installs OFF — do NOT delete the library entry, that wastes the fixture.
- A first attempt at `G-ROW-2` was made just before this was discovered; the developer saw a platform
  row + a "Choose Steam library" dropdown. That observation is **re-attributed to `G-ROW-3`**, where a
  present dropdown is CORRECT. It was NOT scored as a G-ROW-2 failure. Recording this explicitly
  because scoring it under the intended row would have produced a false FAIL.

**USB fixture hazard (`/Volumes/NO NAME`, `msdos`/FAT32, 114 GiB free, writable):** 4 GB max file
size, and `noowners` means the volume CANNOT store POSIX permission bits (no executable flag). Fine
for `G-D02-TARGET`'s "files landed here" assertion; a LAUNCH from this volume may fail for
filesystem reasons unrelated to GameLib. Do not log such a failure as a GameLib defect.

**PART A closed except the two host-blocked items** (`G-QUICK-WIN`, `G-QUICK-LINUX`) and the two
fixture-deferred `G-QUICK-MAC-MULTILIB` rows.

**Redaction reminder (ground rule 5):** bottle manifests carry a `LastOwner` SteamID64. It has been
read during verification but deliberately NOT transcribed into any cell in this ledger.

**Carry-forward for `G-HANDOFF-D15`:** the `GameLibSteam` bottle is ALREADY PROVISIONED on this
machine, so the no-re-ask consent flow cannot be observed without first removing/renaming that
bottle. Any G-HANDOFF-D15 run that skips that teardown is scoring an unreachable path.

**DEFERRED — awaiting fixture, NOT skipped.** The `hasChoice` set (native install ON AND library
count > 1) has no fixture on this machine yet and stays `pending`: rows 3/4 (`G-QUICK-MAC-MULTILIB`),
`G-ROW-3`, `G-ROW-4`, `G-D02-TARGET`. Developer is attaching a USB drive as the second library.

Diagnosed 2026-08-19 — **Steam adds libraries per DRIVE, not per folder.** `libraryfolders.vdf` holds
exactly one entry (`"0"` → `~/Library/Application Support/Steam`) and the only writable volume is the
internal disk; `/Volumes/Bloom` and `/Volumes/VS Code` are read-only mounted DMGs. Pointing Steam's
"Add a Drive" at a folder on the volume that already hosts a library resolves to the same drive and
silently no-ops — `content_log.txt` recorded ZERO entries for the attempt (last line remains
`Triggered async write of Steam library folders configuration (1 libraries)`), so the request never
reached the content system. This is a Steam client behaviour, NOT a GameLib defect — do not file it
as a gap.

**Session run order** (host is macOS, so host-blocked rows are deferred to the end, not skipped):

1. PART A macOS rows → 2. PART B macOS rows (1, 2, 3, 4, 2/3-noWin) → 3. PART C macOS-runnable rows
   → 4. PART D handoff procedures → 5. host-blocked rows (Windows/Linux) resolved last.

**Mount verified 2026-08-19** on branch `fix/steam-native-install-stability` @ `2b72818af`:
`<SteamDialog` renders at `InstallModal/index.tsx:600`, `<EligibilityLoadingRow />` at `:624`.
(Line numbers moved from the 335/392 recorded at Wave 6 — 34.15 edited the file — but the phase IS
reachable in the running app, so these rows are not testing a dead surface.)

---

## PART A — the quick-install path (D-22 / D-23) — 5 items

PASS = no dialog/modal/overlay/picker appears at any point (including one that flashes and closes),
install proceeds, files land in the primary library. FAIL = anything appears, OR the click does
nothing (dead click), OR files land elsewhere.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-QUICK-MAC-NATIVE | electron | PASS | Developer-confirmed 2026-08-19. Criterion run: primary-half click on a macOS-native game (native install OFF, or ON with ≤1 library) — no dialog/modal/overlay/picker appeared (including flash-and-close), install proceeded, files landed in the primary library. Confirmed as a whole; no per-region narrative was dictated | |
| G-QUICK-MAC-NATIVE | tauri | PASS | Developer-confirmed 2026-08-19 under `pnpm tauri:dev`. Criterion run: primary-half click on the same macOS-native game — no dialog/modal/overlay/picker appeared, install proceeded to the primary library. Confirmed as a whole; no per-region narrative was dictated | |
| G-QUICK-MAC-MULTILIB | electron | pending | Click primary half on a macOS-native game with native install ON and >1 library — confirm nothing opens (>1 library used to force the picker; it must not now) | |
| G-QUICK-MAC-MULTILIB | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-QUICK-MAC-BOTTLE | electron | PASS (partial coverage — see note) | 2026-08-19, developer-observed + disk-confirmed. Game: `All Will Fall` (appid 2706020), Windows-only. Primary-half click → **nothing opened**, install started immediately, ran to completion, game launched successfully. Disk evidence: `appmanifest_2706020.acf` written 17:39 under `CrossOver/Bottles/GameLibSteam/drive_c/Program Files (x86)/Steam/steamapps/` (the BOTTLE path, not native), `StateFlags "4"`, `SizeOnDisk 4228928616`. The no-force-open criterion — the actual D-22/D-23 regression this row guards — is PASSED. **CONSENT HALF NOT EXERCISED:** no `steamBottleSetupRequired` prompt appeared, which is CORRECT here because the `GameLibSteam` bottle was already provisioned (it also holds 1771300, 552500, 228980, 226840, 206020/206060, 256290, 2457220, 2825840). Consent is not re-asked for an existing bottle, so this run could not demonstrate that it is still asked at the right moment — that assertion is structurally unreachable with a pre-provisioned bottle and is NOT claimed verified by this row. It is owned by `G-HANDOFF-D15`, which preconditions on "bottle NOT yet provisioned" | |
| G-QUICK-MAC-BOTTLE | tauri | PASS (partial coverage — same caveat as the electron row) | 2026-08-19, developer-observed + disk-confirmed under `pnpm tauri:dev`. Same Windows-only title re-run (`All Will Fall`, appid 2706020 — the row does not require a distinct game). Primary-half click → nothing opened; install proceeded into the bottle. Disk evidence: `appmanifest_2706020.acf` re-written 18:48 under the `GameLibSteam` bottle path, `StateFlags "4"`, `buildid 24654363`, `InstalledDepots` populated (depot 2706021, manifest 7563175421706049358, size 4228928616), `BytesDownloaded` == `BytesToDownload` == `SizeOnDisk` == 4228928616 — a COMPLETE manifest, no stub/clobber signature (noted because phase 23.2 is concurrently fixing a manifest-clobber defect; the 615→483 byte drop vs the 17:39 write is whitespace only, not lost keys). **CONSENT HALF NOT EXERCISED** for the same reason as the electron row — bottle pre-provisioned, so `steamBottleSetupRequired` correctly did not fire and this run cannot evidence that consent is still asked. Owned by `G-HANDOFF-D15` | |
| G-QUICK-WIN | electron | pending | Click primary half on Windows host, native install ON, >1 library — confirm nothing opens, install goes to primary library | |
| G-QUICK-WIN | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-QUICK-LINUX | electron | pending | Click primary half on Linux host, native install OFF or ON with ≤1 library — confirm nothing opens | |
| G-QUICK-LINUX | tauri | pending | Same, via `pnpm tauri:dev` | |

---

## PART B — the 8-row Section-Gating Matrix + depot observation — 9 items

Reach every row via **"Install with options…"** (MainButton caret / GameCard context-menu Item /
GameSubMenu entry). PASS = all five region columns (Platform row, Library dropdown, Wine section,
Free-space line, Content-light notice) observed exactly as specified for that row. FAIL = any region
present that should be absent, absent that should be present, or in the wrong read-only/selectable
state — record which region was wrong. `hasChoice` := native install ON AND library count > 1.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-ROW-1 | electron | **FAIL** | 2026-08-19, developer-observed. Wrong region: **wine section — bottle-name field**. Observed: platform row read-only (correct); CrossOver bottle-name field read-only but **BLANK** (expected prefilled `GameLibSteam`); wine-version/engine picker editable with exactly one option (acceptable — only one CrossOver engine is installed on this host, and D-16 only requires that no non-CrossOver engine be offered). Library dropdown **ABSENT** (correct) and free-space line **ABSENT** (correct), both confirmed on follow-up questioning. D-14 shared-bottle notice not separately reported — deferred to its own item `G-D14-NOTICE`, which owns that assertion. **Damage is confined to ONE region (bottle name); the other three observed regions are correct.** **Root cause identified during this gate (code-confirmed, not inferred):** `InstallModal/index.tsx:127` initialises `const [crossoverBottle, setCrossoverBottle] = useState('')` and that empty string flows unchanged into `<WineSelector crossoverBottle={crossoverBottle} bottleNameReadOnly>` at `:634`. `DEFAULT_STEAM_BOTTLE_NAME` (= `'GameLibSteam'`) is defined at `src/backend/storeManagers/steam/constants.ts:21` and has ZERO frontend references, so nothing ever seeds the field. Read-only + never-seeded = permanently blank. Predicts a matching failure of `G-D05-BOTTLENAME` **FIX LANDED `83866f847` (quick 260819-s8p) — code change, NOT a runtime observation:** `crossoverBottle` is now seeded with `DEFAULT_STEAM_BOTTLE_NAME` on the Steam branch of `InstallModal/index.tsx`; the field remains read-only by design. **Mid-gate integrity warning:** this row's observation was recorded against pre-fix code — the wine-section surface changed under a gate that is mid-run, so this row needs re-observation on a rebuilt app before its Disposition can be trusted going forward. | |
| G-ROW-1 | tauri | **FAIL** | 2026-08-19, developer-observed under `pnpm tauri:dev`, independently (NOT inferred from the electron row). Same single wrong region: CrossOver bottle-name field read-only but **BLANK**. Same root cause — the defect is frontend-only, so both runtimes serve the identical React bundle. **Additionally confirmed on this run: the D-14 shared-bottle notice IS PRESENT** in the wine section. That settles the presence half of the wine-section spec for this row; it does NOT discharge `G-D14-NOTICE`, which additionally asserts `.infoBox` container, `faWarning` icon in DEFAULT text colour (not `--status-danger`), exact copy, and not-behind-a-toggle — none of those were inspected here **FIX LANDED `83866f847` (quick 260819-s8p) — code change, NOT a runtime observation:** `crossoverBottle` is now seeded with `DEFAULT_STEAM_BOTTLE_NAME` on the Steam branch of `InstallModal/index.tsx`; the field remains read-only by design. **Mid-gate integrity warning:** this row's observation was recorded against pre-fix code and needs re-observation on a rebuilt app. Ground rule 2 restated: the seam is runtime-independent React state, which is exactly the shape that invites an inferred PASS from the electron row above — an independent tauri observation is still required. | |
| G-ROW-2 | electron | pending | macOS, mac-native, macOS selected, native OFF or ON with ≤1 library: platform selectable if Windows depot else read-only "macOS"; dropdown/wine/free-space all ABSENT | |
| G-ROW-2 | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-ROW-3 | electron | PASS | 2026-08-19, developer-observed. Fixture verified from disk at observation time, not assumed: `libraryfolders.vdf` = 2 entries and `enableSteamNativeInstall: true`, so `hasChoice` was genuinely TRUE. All four regions correct — platform row PRESENT, library dropdown PRESENT, free-space line PRESENT, wine section ABSENT. Free-space was re-checked specifically after the first report of "only two rows", because it renders as `afterSelect` small text UNDER the dropdown rather than as its own row. **Variant not distinguished:** the row's spec allows the platform row to be either selectable (game has a Windows depot) or read-only "macOS" (it doesn't) — both PASS — and which one was on screen was not captured. That distinction is a PRECONDITION for `G-ROW-4`, which requires a selectable row to flip. **Provenance note:** this observation was first taken while the row was believed to be `G-ROW-2`; the USB fixture landed mid-session and inverted `hasChoice`. Re-attributed to `G-ROW-3` after confirming library count from disk — under the intended row a present dropdown would have been scored a false FAIL | |
| G-ROW-3 | tauri | PASS | 2026-08-19, developer-observed under `pnpm tauri:dev`, independently of the electron row. All four regions correct — platform row PRESENT, library dropdown PRESENT, free-space line PRESENT, wine section ABSENT. **Variant captured here (it was not on the electron run): the platform row is SELECTABLE**, so this game carries a Windows depot. That discharges `G-ROW-4`'s precondition — a flip to Windows is reachable — and means `G-ROW-2/3-noWin` needs a DIFFERENT game (one with no Windows depot), not this one | |
| G-ROW-4 | electron | PASS | Developer-confirmed 2026-08-21. Criterion run: flipped platform selector from macOS to Windows on the `G-ROW-3` game (Windows-depot-bearing mac-native title) — platform row stayed selectable with Windows chosen; library dropdown ABSENT (even though `hasChoice` is true); wine section PRESENT; free-space line ABSENT. Confirmed as a whole; no per-region narrative was dictated | |
| G-ROW-4 | tauri | PASS | Developer-confirmed 2026-08-21 under `pnpm tauri:dev`, independently of the electron row — same result: platform row stayed selectable with Windows chosen; library dropdown ABSENT; wine section PRESENT; free-space line ABSENT. Confirmed as a whole; no per-region narrative was dictated | |
| G-ROW-2/3-noWin | electron | pending | macOS, mac-native game with NO Windows depot: read-only "macOS"; the Windows MenuItem is ABSENT from the list entirely (FAIL if present-but-disabled/greyed with tooltip); row 4 structurally unreachable for this game | |
| G-ROW-2/3-noWin | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-ROW-5 | electron | pending | Windows host, native OFF or ON with ≤1 library: read-only "Windows" (D-19); dropdown/wine/free-space ABSENT; content-light notice PRESENT (D-20/Q6) | |
| G-ROW-5 | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-ROW-6 | electron | pending | Windows host, hasChoice: read-only "Windows" (D-19); dropdown PRESENT; wine ABSENT; free-space PRESENT | |
| G-ROW-6 | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-ROW-7 | electron | pending | Linux host, native OFF or ON with ≤1 library: platform row does not render at all (D-18); dropdown/wine/free-space ABSENT; content-light notice PRESENT (D-20/Q6) | |
| G-ROW-7 | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-ROW-8 | electron | pending | Linux host, hasChoice: platform row does not render (D-18); dropdown PRESENT; wine ABSENT; free-space PRESENT | |
| G-ROW-8 | tauri | pending | Same, via `pnpm tauri:dev` | |

---

## PART C — decision + amendment observations — 16 items

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-D06-NOSIZE | electron | pending | On every opens=YES row above: no download-size/install-size row anywhere; Install button enabled and clickable in every state, never gated on diskSize or free space. **Note (Gate E finding, 34.13-13 Task 2):** the footer Install button DOES carry `disabled={eligibilityPending}` — a documented, planned D-25 exception unrelated to size — do not score that alone as a FAIL; it is a FAIL only if the button is EVER disabled for a size/free-space reason | |
| G-D06-NOSIZE | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D20-CONTENTLIGHT | electron | pending | Windows host with no library choice (Matrix row 5 — run BOTH sub-cases: native OFF any library count; native ON with ≤1 library): one read-only "Windows" row, content-light notice, Cancel+Install, NOTHING else (no empty-state illustration/heading). Install completes normally. **CORRECTED by 34.13 review A-08 (was: "Both sub-cases render identically"):** review WR-04 deliberately made the two sub-cases render DIFFERENT COPY, so identical rendering is now a FAIL. Native OFF must show `gamelib:steam.install.contentLightNotice`; native ON with ≤1 library must show `gamelib:steam.install.contentLightSingleLibraryNotice`. The section LAYOUT is identical; only the sentence differs. **Also (review B-WR-02):** on the very first frame, before the Steam library fetch settles, NEITHER notice may appear — a flash of the "Turn on native Steam installs in Settings" sentence on a host where the setting is already ON is a FAIL | |
| G-D20-CONTENTLIGHT | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D20-Q6-COPY | electron | pending | Content-light notice renders in an `.infoBox` (NOT `ThirdPartyDialog`'s `.noticeIcon`/`.noticeInfo`). **CORRECTED by 34.13 review A-08:** the copy is now a TWO-BRANCH split (review WR-04), so this row must be scored per branch, on BOTH row 5 and row 7. (a) native installs OFF → exactly `gamelib:steam.install.contentLightNotice`: "This installs through Steam's own client, so there's nothing to choose here. Turn on native Steam installs in Settings to manage install location and Windows compatibility from GameLib." (b) native installs ON with ≤1 Steam library → exactly `gamelib:steam.install.contentLightSingleLibraryNotice`: "There's only one Steam library on this system, so there's nothing to choose here — GameLib will install this game into it." Seeing sentence (a) in case (b) is the exact defect WR-04 fixed and is a FAIL | |
| G-D20-Q6-COPY | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D05-BOTTLENAME | electron | pending | Wine section: bottle-name field prefilled `GameLibSteam`, not editable; `.smallInputInfo` helper line reads "GameLib manages one shared CrossOver bottle for Steam. Naming a separate bottle per game isn't supported yet." **CODE-READ FINDING 2026-08-19 — NOT a runtime observation; this row stays `pending` until actually observed:** the PREFILL half is expected to FAIL — the field renders EMPTY, not `GameLibSteam`. `InstallModal/index.tsx:127` initialises `crossoverBottle` to `''` and the Steam branch passes it down at `:634`, but nothing ever seeds it. `WineSelector`'s only two writers are both unreachable on the Steam path: `WineSelector/index.tsx:116` (`setCrossoverBottle(globalConfig.wineCrossoverBottle)`) is gated on `useSharedPrefix`, which only the toggle at `:301` can flip, and that toggle sits inside `!hideSharedPrefixToggle` at `:292` — a prop the Steam branch sets; and the field's own `onChange` at `:228` is disabled by `bottleNameReadOnly`. The guided wizard does seed it (`SteamBottleSetup.tsx:213` → `DEFAULT_STEAM_BOTTLE_NAME`); this dialog never got the equivalent. **Score the three halves separately** — `not editable` and the `.smallInputInfo` helper line are expected to PASS, so a whole-row PASS on the strength of those two would hide the defect. **FIX LANDED `83866f847` (quick 260819-s8p) — code change, NOT a runtime observation:** `crossoverBottle` is now seeded with `DEFAULT_STEAM_BOTTLE_NAME` on the Steam branch of `InstallModal/index.tsx`; the field remains read-only by design. **Mid-gate integrity warning:** any wine-section observation recorded BEFORE this commit was made against pre-fix code and needs re-observation on a rebuilt app. | |
| G-D05-BOTTLENAME | tauri | pending | Same, via `pnpm tauri:dev` **Same CODE-READ FINDING as the electron row above** (empty bottle-name field; the prefill half is expected to FAIL) — the seam is runtime-independent React state, but per ground rule 2 this row still requires its own independent observation and is NOT inferred from the electron row. **FIX LANDED `83866f847` (quick 260819-s8p) — code change, NOT a runtime observation:** `crossoverBottle` is now seeded with `DEFAULT_STEAM_BOTTLE_NAME` on the Steam branch of `InstallModal/index.tsx`; the field remains read-only by design. **Mid-gate integrity warning:** any wine-section observation recorded BEFORE this commit was made against pre-fix code and needs re-observation on a rebuilt app. Ground rule 2 restated: the seam is runtime-independent React state, which is exactly the shape that invites an inferred PASS — an independent tauri observation is still required. | |
| G-D16-CROSSOVER | electron | pending | Engine dropdown lists ONLY CrossOver engines — no GPTK, no plain wine, no Proton | |
| G-D16-CROSSOVER | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D14-NOTICE | electron | pending | Shared-bottle notice renders in `.infoBox` with `faWarning` icon in DEFAULT text colour (not red `--status-danger`), reads exactly "The wine version you choose here is used for every Steam game that needs a bottle — not just this one. Changing it later will change how those games run too.", renders whenever wine section is visible (not behind a toggle) | |
| G-D14-NOTICE | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D02-TARGET | electron | pending | With >1 library, pick the NON-default library in the dropdown, install, confirm ON DISK the files landed under that chosen library path (not the primary/default) | |
| G-D02-TARGET | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D08-FREESPACE | electron | pending | Free-space line reads "Space Available: {value}" — no "After Install" segment, no not-enough-space warning branch | |
| G-D08-FREESPACE | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-NOSETTING | electron | pending | Settings → General has NO "Always show install options for Steam games" row — global screen AND every per-game settings screen. Steam native-install row unchanged/still present. (D-26 CUT this setting — this item asserts an ABSENCE; the pre-amendment gate item for the always-show row required the opposite, so do not reconcile against it) | |
| G-NOSETTING | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-SPLIT-SURFACES | electron | pending | Install control offers both intents on every surface: (1) MainButton primary Install + caret "Install with options…"; (2) GameCard context menu has an "Install with options…" Item (not a caret); (3) GameSubMenu has an "Install with options…" entry. All three use the SAME label string | |
| G-SPLIT-SURFACES | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-QUICK-DEFAULT | electron | pending | Clicking primary half installs with NO dialog at all, lands in primary library — including on a bottle-required game (old model force-opened it; that is now wrong) | |
| G-QUICK-DEFAULT | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-QUICK-NOPROBE | electron | pending | With a COLD Steam metadata cache, primary half starts installing without a multi-second stall (comparable timing to a warm-cache click, not ~15s) | |
| G-QUICK-NOPROBE | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D24-DEGRADE | electron | pending | Make primary library unavailable (detach drive) or full, click primary half: quick install degrades into the options dialog, reason surfaced as `.infoBox` at TOP of dialog content, Install button stays ENABLED | |
| G-D24-DEGRADE | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-D24-ONLYLIB | electron | pending | **OBSERVE-ONLY — never PASS/FAIL.** Repeat G-D24-DEGRADE with the failing library as the user's ONLY library. Record verbatim what the notice says and what Install does — this behaviour is deliberately undecided | |
| G-D24-ONLYLIB | tauri | pending | Same, via `pnpm tauri:dev` — OBSERVE-ONLY | |
| G-D29-CONSOLE | electron | pending | Console Mode, primary library unavailable: install shows failure IN PLACE (`--status-danger` title, reason line, single Dismiss chip), no desktop dialog underneath. (1) pressing A does NOT start install anyway; (2) message does NOT auto-dismiss after 1.5s | |
| G-D29-CONSOLE | tauri | pending | Same, via `pnpm tauri:dev` | |
| G-GAMEPAD-CARET | electron | pending | With a controller: caret reachable, reaching it does not lose the primary half, focus order stable — on MainButton AND the game card. Must be observed independently per runtime, not inferred | |
| G-GAMEPAD-CARET | tauri | pending | Same, via `pnpm tauri:dev` — **independent observation required; an Electron PASS is NOT evidence for this cell** (Tauri's focus collector filters zero-bounding-box elements, a filter Electron's native traversal does not apply) | |

---

## PART D — the six upstream handoff / procedure items — 6 items

### G-HANDOFF-D17 — D-17 platform-switch interaction (UI-SPEC steps 1→5)

Preconditions: macOS host, mac-native Steam game with a Windows depot, always-show ON (n/a — no
always-show setting exists per D-26; reach via the caret). PASS = all six sub-steps observed
(selectable macOS-preselected row; flip-to-Windows reveal order engine→bottle-name→D-14 notice;
simultaneous dropdown disappearance; snap with NO height animation; dialog stays within
`max-height: 90vh` scrolling internally; flip-back unmounts wine+notice together and dropdown
reappears if condition holds, discarding any Windows-side selection).

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-D17 | electron | pending | Run all six sub-steps in order; record each individually | |
| G-HANDOFF-D17 | tauri | pending | Same, via `pnpm tauri:dev` | |

### G-HANDOFF-D15 — the no-re-ask handoff (34.13-09's eight steps, verbatim)

Preconditions: macOS host, Windows-only bottle-eligible game, bottle NOT yet provisioned, ≥2 CrossOver
engines detected. PASS = steps 3, 4, 5, 6, 7, 8 all observed as stated (wizard shows the non-first
engine chosen in step 1 already selected with no re-ask; bottle-name still `GameLibSteam` read-only;
consent step still requires explicit "Set up Steam" click; changing the engine in the wizard sticks;
closing without confirming then repeating does NOT carry a stale seed; step 8 repeats 1-3 under
`pnpm tauri:dev`).

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-D15 | electron | pending | Run steps 1-7; record steps 3,4,5,6,7 individually | |
| G-HANDOFF-D15 | tauri | pending | Run step 8 (repeat 1-3) via `pnpm tauri:dev` | |

### G-HANDOFF-LAUNCH — is a forced install actually launchable? (T-34.13-06-06, CLOSED by 34.13-14)

Preconditions: a completed forced Windows-via-bottle install from G-HANDOFF-D17, macOS host. **Real
PASS/FAIL gate now** — a FAIL is a defect in 34.13-14, not an expected/escalated outcome. Record all
FOUR surfaces individually — "it launched" alone is not an acceptable observation.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-LAUNCH | electron | pending | Record individually: (1) shows installed, size/path reflect bottle copy; (2) Launch route — Steam log records launching via the bottled Steam client, NOT a `steam://rungameid` dispatch to host client; (3) settings screen shows bottle's wine version + `GameLibSteam` name; (4) `isNative()` observably false — pre-launch wine check runs | |
| G-HANDOFF-LAUNCH | tauri | pending | Same, via `pnpm tauri:dev`, if a second forced install is available for this runtime — else NOT ATTEMPTED naming the reason | |

### G-HANDOFF-UNINSTALL — does uninstalling a forced install actually remove the bottle copy?

Preconditions: the forced install from G-HANDOFF-LAUNCH, still installed. Run AFTER G-HANDOFF-LAUNCH,
BEFORE G-HANDOFF-REINSTALL.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-UNINSTALL | electron | pending | Record: (1) on-disk path under Phase 17 `GameLibSteam` bottle's `steamapps/common/` before uninstall (redact account identifiers), `appmanifest_<appId>.acf` exists; (2) Steam log records delegating uninstall via bottled client, not `steam://uninstall` to host; (3) ON DISK: directory AND `appmanifest_<appId>.acf` both gone; (4) badge flips to not-installed without app restart | |
| G-HANDOFF-UNINSTALL | tauri | pending | Same, via `pnpm tauri:dev`, if applicable — else NOT ATTEMPTED naming the reason | |

### G-HANDOFF-REINSTALL — reversibility: is a later NATIVE re-install mis-routed?

Preconditions: G-HANDOFF-UNINSTALL completed, same mac-native game, reached via the caret ("always-show
ON" language from the plan is stale — there is no such setting; use the caret).
⚠ Specified behaviour, NOT a defect: re-installing WITHOUT uninstalling first re-routes to the bottle —
if observed, record as a note, do not mark FAIL for it.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-HANDOFF-REINSTALL | electron | pending | Record: (1) platform row selectable, macOS pre-selected, no wine section while macOS selected; (2) Steam log shows NATIVE install path, not bottled client; (3) launches natively via host Steam client; (4) settings screen shows per-game config, not bottle's wine version | |
| G-HANDOFF-REINSTALL | tauri | pending | Same, via `pnpm tauri:dev`, if applicable — else NOT ATTEMPTED naming the reason | |

### G-D25-INDIALOG — the cold-cache in-dialog loading state (full procedure; PART C's row is the same item, recorded once here)

> ⚠ INVERTED from this item's pre-amendment predecessor: the dialog now mounts IMMEDIATELY and the loading state
> lives INSIDE it. A half-rendered dialog is CORRECT; a busy origin button is now a FAILURE.

| Item | Runtime | Disposition | Observation | Blocker |
|---|---|---|---|---|
| G-D25-INDIALOG | electron | pending | Cold cache (delete `steam_metadata.json`, relaunch), click caret: (3) dialog opens immediately with platform row present right away; (4) wine section shows spinning `faSyncAlt`, dialog's own Install button disabled, dropdown/free-space line ABSENT while pending (not flash-then-vanish); (5) origin control (MainButton/GameCard/GameSubMenu) shows NO busy state; (6) primary half with same cold cache has NO probe at all; (7) worst case 15s (`METADATA_FETCH_TIMEOUT_MS`) is a PASS, unbounded pending is a FAIL | |
| G-D25-INDIALOG | tauri | pending | Same, via `pnpm tauri:dev` — an unresolved pending state here specifically signals a channel registered on only one runtime | |

---

## Gaps

<!-- Structured for /gsd-plan-phase --gaps. APPEND only. -->

```yaml
- truth: "The Steam install-options wine section shows a bottle-name field prefilled with `GameLibSteam`, read-only (D-05, G-ROW-1 + G-D05-BOTTLENAME)."
  status: failed
  reason: "User reported: 'crossover bottle - blank, read only'. The field renders read-only but with no value."
  severity: major
  test: G-ROW-1 (electron AND tauri — both observed independently, both FAIL)
  runtimes_affected: both
  scope: "Frontend-only. Both runtimes serve the same React bundle, so this is one defect, not two."
  root_cause: "`InstallModal/index.tsx:127` initialises `useState('')` for `crossoverBottle` and never seeds it for the Steam path. That empty string is passed to `<WineSelector ... bottleNameReadOnly>` at `:634`. `DEFAULT_STEAM_BOTTLE_NAME` ('GameLibSteam') is defined at `src/backend/storeManagers/steam/constants.ts:21` and has zero frontend references, so no code path can ever populate it. Read-only + unseeded = permanently blank."
  artifacts:
    - src/frontend/screens/Library/components/InstallModal/index.tsx:127
    - src/frontend/screens/Library/components/InstallModal/index.tsx:634
    - src/backend/storeManagers/steam/constants.ts:21
  missing:
    - "A frontend-reachable source for the default Steam bottle name. `DEFAULT_STEAM_BOTTLE_NAME` currently lives in a backend module; the fix needs it in a shared location or delivered over the existing preload seam. NOTE for the planner: 34.13 declared `src/common/types.ts` CLOSED to edits — confirm whether that fence is still in force before siting the constant there."
    - "Seeding of `crossoverBottle` when the install target is Steam and `gating.wineSection` is true."
  also_predicts:
    - "G-D05-BOTTLENAME (both runtimes) — same field, same expectation, not yet run."
```

## Recording integrity

- Manifest re-derived from the plan's own tables at ledger-creation time: **5 (Part A) + 9 (Part B) +
  16 (Part C) + 6 (Part D) = 36**, matching `34.13-13-PLAN.md`'s stated count.
- The entire pre-amendment id family (the always-show-setting item, the busy-state item this ledger's
  D-25 procedure supersedes, the retired auto-open-trigger matrix rows 3a/6a/6b/9/10/10a/10b/11/12, and
  the retired no-Windows-depot row-5 variant) is confirmed absent from this ledger's actual rows —
  verified by the phase's own ledger-completeness gate, which independently re-derives this same
  36-item count and asserts each one specifically.
- No 17-digit SteamID64, account name, or token appears anywhere in this file (nothing has been
  transcribed from a real session yet — this is the pre-populated skeleton).
- Every row's Disposition is `pending`. **None of the 72 rows above have been executed, simulated, or
  inferred by this executor.** This is the enumeration required before the blocking gate runs, not the
  gate result.

## Roll-up (as of ledger creation)

- PASS: 0
- FAIL: 0
- NOT ATTEMPTED: 0
- pending: 72 (36 items × 2 runtimes)

**Phase status recommendation: `human_needed`.** The blocking manual gate (Task 3) has not been
executed — that is reserved for the human developer per this session's explicit instruction. This
ledger is the enumerated skeleton the developer fills in; it is not evidence of any item passing.
