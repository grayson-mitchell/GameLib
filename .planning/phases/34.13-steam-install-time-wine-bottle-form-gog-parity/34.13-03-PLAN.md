---
phase: 34.13-steam-install-time-wine-bottle-form-gog-parity
plan: 03
type: execute
wave: 2
depends_on: ['34.13-01']
files_modified:
  - src/frontend/screens/Library/components/InstallModal/WineSelector/engineFilter.ts
  - src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts
  - src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx
  - .planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md
autonomous: true
requirements: ['D-05', 'D-16']
user_setup: []

must_haves:
  truths:
    - "D-16: the wine-engine dropdown offers ONLY CrossOver-type engines on the Steam path, and the auto-seeded default engine is a CrossOver engine — never a toolkit/wine/proton engine that would silently yield a broken bottle."
    - "D-16: SteamBottleSetup.tsx inherits the CrossOver-only filter WITHOUT being edited, because the filter lives inside WineSelector and defaults on from the runner=\"steam\" prop it already passes (RESEARCH.md Pitfall 3)."
    - "D-05: on the Steam path the CrossOver bottle-name field is prefilled and NOT editable, with a helper line explaining that one shared bottle is managed for Steam."
    - "D-05: the pre-existing shared-prefix disable does NOT show the read-only helper copy — the two reasons a field is disabled stay distinguishable."
    - "Every non-Steam caller (GOG / Epic / Amazon / Zoom / sideload install modals) renders a byte-for-byte unchanged WineSelector — the new props are optional with undefined defaults."
    - "The folded GPTK todo is re-scoped, not closed: its provisionBottle-rejection and resolveSteamBottleEngine-silent-override halves remain open and visible in .planning/todos/pending/."
  artifacts:
    - path: "src/frontend/screens/Library/components/InstallModal/WineSelector/engineFilter.ts"
      provides: "The four pure decision functions for D-16 and D-05, importable without evaluating any .css/.scss"
      exports: ["resolveCrossoverOnly", "filterWineEngines", "selectDefaultEngine", "resolveBottleNameState"]
    - path: "src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts"
      provides: "Exhaustive coverage of both decisions, incl. the RED-provable seeding guard"
      min_lines: 60
    - path: "src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx"
      provides: "crossoverOnly + bottleNameReadOnly optional props wired to engineFilter.ts at all four engine-list consumption points"
      contains: "crossoverOnly"
    - path: ".planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md"
      provides: "Re-scoped todo recording that only D-16's frontend-filter half shipped"
      contains: "34.13"
  key_links:
    - from: "src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx"
      to: "./engineFilter"
      via: "named import of all four decision functions"
      pattern: "from '\\./engineFilter'"
    - from: "src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx"
      to: "the engine SelectField options, its disabled guard, its onChange lookup, and the auto-seed effect"
      via: "engineOptions replacing every wineVersionList read on those four paths"
      pattern: "engineOptions"
    - from: "src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx"
      to: "the CrossOver-only filter"
      via: "its existing runner=\"steam\" prop, through resolveCrossoverOnly's default — no edit to that file"
      pattern: "resolveCrossoverOnly"
---

<objective>
Give `WineSelector` two additive optional props — `crossoverOnly` (D-16) and `bottleNameReadOnly`
(D-05) — with the actual decision logic extracted into a pure, SCSS-free `engineFilter.ts` sibling
module so Jest can prove it.

Purpose: D-16 removes a misleading affordance — bottle creation is hardcoded to CrossOver's
`cxbottle`, so any other engine silently produces a broken bottle. D-05 stops the form promising a
bottle name this phase cannot provision. Both must land INSIDE `WineSelector` (not in the parent, not
in the new Steam dialog) so `SteamBottleSetup.tsx`'s independent `getAlternativeWine()` call inherits
them for free — RESEARCH.md Pitfall 3 names "a diff that touches `InstallModal/index.tsx` or a new
dialog file but not `WineSelector/index.tsx`" as the warning sign of getting this wrong.

Output: one new pure module, one new test file, `WineSelector/index.tsx` wired to it, and the folded
GPTK todo re-scoped (NOT closed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-CONTEXT.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-PATTERNS.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

From `src/common/types.ts:23-29`:
  export type Runner = 'legendary' | 'gog' | 'sideload' | 'nile' | 'zoom' | 'steam'
  — six members, exhaustive. `WineSelector`'s `runner` prop is `Runner | undefined`.

From `src/common/types.ts:393-400`:
  export interface WineInstallation {
    bin: string
    name: string
    type: 'wine' | 'proton' | 'crossover' | 'toolkit'
    lib?: string
    lib32?: string
    wineserver?: string
  }
  — `type` is the D-16 discriminator. `'crossover'` is the only value `cxbottle` can provision.

From `src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx` (235 lines,
the file this plan modifies — current state of the four seams):
  L19-47  `type Props` — optional props `runner?`, `initiallyOpen?`, `sharedPrefixNote?`,
          `hideSharedPrefixToggle?`, each carrying a multi-line comment naming the phase that
          added it and stating "optional/default-undefined so every existing caller is
          byte-for-byte unchanged". This is the convention the two new props must follow.
  L64     `const { t, i18n } = useTranslation('gamepage')`
  L108-113 auto-seed effect: `const firstAvailableVersion = wineVersionList.at(0)` — SEAM 1
  L116    `const showBottle = wineVersion?.type === 'crossover'` — reused unchanged
  L150-158 the bottle-name `TextInputField`, `disabled={useSharedPrefix}` — the D-05 seam
  L164    `disabled={useSharedPrefix || wineVersionList.length === 0}` — SEAM 2
  L167-169 `wineVersionList.find((version) => version.name === e.target.value)!` — SEAM 3
  L173-178 `wineVersionList && wineVersionList.map((version, i) => <MenuItem .../>)` — SEAM 4

From `src/frontend/screens/Settings/components/WineVersionSelector.tsx:28,33` — the live
inline-English-default precedent for a `gamelib:` string in a component whose primary namespace is
something else:
  const { t: tGamelib } = useTranslation('gamelib')
  tGamelib('gamelib:settings.wineDefaultLabel', 'Wine Default')

From `src/frontend/screens/Library/components/InstallModal/SideloadDialog/filters.ts` — the exact
"pure module extracted out of a .tsx purely so Jest can import it" precedent, including the
explanatory doc-comment convention its header uses. Its test at
`SideloadDialog/__tests__/index.test.tsx` imports `../filters`, never `../index`.

From `src/frontend/jest.config.js`:
  testEnvironment: node (jsdom NOT installed) · roots: ['<rootDir>/src/frontend']
  testMatch: ['**/__tests__/**/*.test.tsx', '**/__tests__/**/*.test.ts']
  — a new test therefore MUST live in a `__tests__/` directory to be discovered.
</interfaces>
</context>

<constraints>
**Why the pure module is mandatory (do not attempt to test the component).**
`WineSelector/index.tsx` has no `.scss` import of its own, but it imports `SelectField`,
`ToggleSwitch`, `TextInputField` and `PathSelectionBox` from `frontend/components/UI`, and
`frontend/components/UI/SelectField/index.tsx` line 5 is `import './index.css'`. This project's Jest
config has no CSS transform or `moduleNameMapper`, so importing `WineSelector/index.tsx` from a test
throws "Jest encountered an unexpected token" while evaluating that transitive `.css` import, before
any assertion runs. There is no jsdom and no react-test-renderer to fall back to. The pure module IS
the testable surface; the `.tsx` wiring is proven by 34.13-13's manual gate.

**Scope prohibitions — each of these is a Deferred Idea that must stay absent from the diff:**
- Do NOT add a corrective override that force-switches an already-set non-CrossOver `wineVersion` to
  a CrossOver one. That is `resolveSteamBottleEngine`'s silent-override half, explicitly Deferred.
  The new logic only governs which engines are OFFERED and which is SEEDED when none is set.
- Do NOT touch `src/backend/storeManagers/steam/bottle.ts`. The `provisionBottle` engine-rejection
  half is explicitly Deferred; D-16 is frontend-filter only.
- Do NOT make the bottle name editable-but-per-game, and do NOT add a second bottle name. Multi-bottle
  is Phase 22.
- Do NOT add the D-14 shared-bottle notice here — that is 34.13-10's task, in the new Steam dialog.
- Do NOT edit `src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx`. Inheriting the
  filter with zero edits to that file is the D-16 acceptance property, not an oversight.
- Do NOT edit `src/frontend/screens/Library/components/InstallModal/index.tsx` — 34.13-12 owns it.
- Do NOT edit `public/locales/en/gamelib.json` — 34.13-13 is the single catalog writer for this
  phase. Ship the inline English default via the `tGamelib('gamelib:key', 'English default')`
  precedent above.

**`depends_on: 34.13-01` is ordering-only.** 34.13-01 consolidates new `src/common/types.ts` fields
so waves 2+ do not collide on that file. This plan consumes only the pre-existing `Runner` and
`WineInstallation` types and adds no field to `types.ts`. Do not go looking for a new type to import.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the D-16 / D-05 decisions into a pure engineFilter.ts module, with tests</name>
  <files>src/frontend/screens/Library/components/InstallModal/WineSelector/engineFilter.ts, src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts</files>

  <read_first>
    - src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx (the file whose
      logic is being extracted — read L19-47 for the Props convention, L108-116 for the auto-seed
      effect and `showBottle`, and L150-179 for the bottle-name field and engine SelectField)
    - src/frontend/screens/Library/components/InstallModal/SideloadDialog/filters.ts (the pure-module
      extraction precedent, including its header doc-comment explaining WHY the module exists)
    - src/frontend/screens/Library/components/InstallModal/SideloadDialog/__tests__/index.test.tsx
      (the matching test precedent — note it imports `../filters`, never `../index`)
    - src/common/types.ts lines 23-29 and 393-400 (`Runner`, `WineInstallation`)
    - src/frontend/jest.config.js (confirms `__tests__/` placement is required for discovery)
  </read_first>

  <behavior>
    - `resolveCrossoverOnly('steam', undefined)` is `true` — SteamBottleSetup, which passes
      `runner="steam"` and no new prop, is filtered automatically (RESEARCH.md Pitfall 3).
    - `resolveCrossoverOnly(r, undefined)` is `false` for every other `Runner` member
      ('legendary', 'gog', 'sideload', 'nile', 'zoom') and for `undefined`.
    - `resolveCrossoverOnly('steam', false)` is `false` — an explicit prop always beats the
      runner-derived default, in both directions.
    - `filterWineEngines(list, false)` returns the SAME array reference it was given (identity),
      so non-Steam callers do no work and allocate nothing.
    - `filterWineEngines(list, true)` keeps only entries whose `type` is `'crossover'`, dropping
      `'wine'`, `'proton'` and `'toolkit'`.
    - `selectDefaultEngine(list, true)` returns a `'crossover'` engine even when a `'toolkit'`
      engine occupies index 0 of the unfiltered list. This is the regression guard: today's
      `wineVersionList.at(0)` returns the toolkit engine, which then makes `showBottle` false, hides
      the bottle-name field entirely, and leaves the SelectField holding a value absent from its own
      options — the exact silent breakage the folded GPTK todo describes.
    - `selectDefaultEngine([], true)` and `selectDefaultEngine(onlyNonCrossoverList, true)` return
      `undefined` rather than throwing or returning a non-CrossOver engine.
    - `resolveBottleNameState(true, undefined)` is `{ disabled: true, showReadOnlyHelper: false }` —
      the pre-existing shared-prefix disable must NOT surface the new D-05 copy.
    - `resolveBottleNameState(false, true)` is `{ disabled: true, showReadOnlyHelper: true }`.
    - `resolveBottleNameState(false, undefined)` is `{ disabled: false, showReadOnlyHelper: false }`.
  </behavior>

  <action>
    Create `src/frontend/screens/Library/components/InstallModal/WineSelector/engineFilter.ts`
    exporting exactly four pure functions, with no React import, no `.css`/`.scss` import, and no
    dependency on anything under `frontend/components/`:

    1. `resolveCrossoverOnly(runner: Runner | undefined, crossoverOnly: boolean | undefined): boolean`
       — returns `crossoverOnly ?? runner === 'steam'`. Use `??` specifically, NOT `||`, so an
       explicit `crossoverOnly={false}` can override the Steam default.
    2. `filterWineEngines(wineVersionList: WineInstallation[], crossoverOnly: boolean): WineInstallation[]`
       — when `crossoverOnly` is false, return `wineVersionList` unchanged (the same reference, not a
       copy); otherwise return `wineVersionList.filter((version) => version.type === 'crossover')`.
    3. `selectDefaultEngine(wineVersionList: WineInstallation[], crossoverOnly: boolean): WineInstallation | undefined`
       — defined in terms of `filterWineEngines(wineVersionList, crossoverOnly).at(0)` so the offered
       list and the seeded default can never disagree.
    4. `resolveBottleNameState(useSharedPrefix: boolean, bottleNameReadOnly: boolean | undefined): { disabled: boolean; showReadOnlyHelper: boolean }`
       — `disabled` is `useSharedPrefix || bottleNameReadOnly === true`; `showReadOnlyHelper` is
       `bottleNameReadOnly === true` and is deliberately independent of `useSharedPrefix`.

    Give the file a header doc-comment in the shape `SideloadDialog/filters.ts` uses: name Phase
    34.13-03 and decisions D-16 and D-05, state that the module exists because `WineSelector`'s
    transitive `frontend/components/UI` → `SelectField/index.css` import makes the `.tsx` unimportable
    under this project's jsdom-less Jest config, and state that `resolveCrossoverOnly` defaults on for
    `runner === 'steam'` specifically so `SteamBottleSetup.tsx` inherits the filter without being
    edited (RESEARCH.md Pitfall 3).

    Create `src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts`
    importing from `../engineFilter`. Build `WineInstallation` fixtures covering all four `type`
    values, with the `'toolkit'` fixture placed at index 0 of the mixed list so the
    `selectDefaultEngine` assertion is genuinely load-bearing rather than incidentally true. Cover
    every bullet in `<behavior>`. For `resolveCrossoverOnly`'s non-Steam cases, enumerate all five
    remaining `Runner` members plus `undefined` explicitly — the union is closed at six members, so
    enumerate rather than sample.

    RED-prove the seeding assertion before moving on: temporarily change `selectDefaultEngine`'s body
    to `wineVersionList.at(0)` (ignoring `crossoverOnly`), confirm that single test fails, then
    restore the real body. Record the observed failure message in the SUMMARY. Do not commit the
    temporary edit.
  </action>

  <verify>
    <automated>npx jest src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts --silent</automated>
  </verify>

  <acceptance_criteria>
    - `src/frontend/screens/Library/components/InstallModal/WineSelector/engineFilter.ts` exists and
      `grep -c "^export function" <file>` returns exactly `4`.
    - `grep -E "\.css|\.scss|from 'react'|frontend/components" <file>` returns no matches — the
      module must be importable in isolation.
    - `grep -n "crossoverOnly ?? runner === 'steam'" <file>` matches — `??` not `||`, so
      `crossoverOnly={false}` can override.
    - `npx jest src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts --silent`
      exits 0 with at least 9 passing tests.
    - The test file contains an assertion using `toBe` (reference identity, not `toEqual`) for
      `filterWineEngines(list, false)`.
    - The test file's mixed-engine fixture list has an entry with `type: 'toolkit'` at index 0, and a
      test asserting `selectDefaultEngine(mixedList, true)?.type === 'crossover'`.
    - The test file asserts `resolveCrossoverOnly` for all six `Runner` members plus `undefined`
      (7 cases).
    - The test file asserts `resolveBottleNameState(true, undefined)` yields
      `showReadOnlyHelper: false` while `disabled` is `true`.
    - SUMMARY records the RED-proof failure message observed when `selectDefaultEngine` was
      temporarily reduced to `wineVersionList.at(0)`, and `git diff` shows no trace of that temporary
      edit.
  </acceptance_criteria>

  <done>
    `engineFilter.ts` exists with four pure exports and no UI/CSS coupling; its test file passes;
    the `selectDefaultEngine` CrossOver-seeding assertion has been RED-proven against the naive
    `.at(0)` implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire crossoverOnly (D-16) and bottleNameReadOnly (D-05) into WineSelector</name>
  <files>src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx</files>

  <read_first>
    - src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx (the file being
      modified, in full — 235 lines)
    - src/frontend/screens/Library/components/InstallModal/WineSelector/engineFilter.ts (created in
      Task 1 — the four functions being consumed)
    - src/frontend/screens/Settings/components/WineVersionSelector.tsx lines 20-40 (the live
      `useTranslation('gamelib')` + inline-English-default precedent)
    - .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UI-SPEC.md lines
      73 and 105-113 (the D-05 helper-text copy, its exact `gamelib:` key, and its `.smallInputInfo`
      placement contract)
    - src/frontend/screens/Library/components/InstallModal/DownloadDialog/index.tsx line 675 (the
      only existing `.smallInputInfo` usage in `InstallModal/`, for markup shape)
  </read_first>

  <behavior>
    - Non-Steam callers are unaffected: with `runner` unset or set to any non-`'steam'` value and
      neither new prop passed, every engine in `wineVersionList` remains offered, the seeded default
      is still `wineVersionList.at(0)`, the bottle-name field's `disabled` is still exactly
      `useSharedPrefix`, and no helper line renders.
    - On the Steam path the engine `SelectField` offers only CrossOver engines, its
      `disabled`-when-empty guard counts only CrossOver engines, its `onChange` resolves the picked
      name against the filtered list, and the auto-seed effect seeds from the filtered list.
    - With `bottleNameReadOnly`, the bottle-name `TextInputField` is not editable and a helper line
      renders directly beneath it. With only `useSharedPrefix` true, the field is disabled and no
      helper line renders.
  </behavior>

  <action>
    Edit `src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx` only.

    Add two optional props to `type Props` (L19-47): `crossoverOnly?: boolean` and
    `bottleNameReadOnly?: boolean`. Precede each with a multi-line comment in the same shape the
    existing `runner` / `sharedPrefixNote` / `hideSharedPrefixToggle` comments use: name Phase
    34.13-03 and the decision (D-16 / D-05), state what it does, and state that it is
    optional/default-undefined so every existing caller is byte-for-byte unchanged. For
    `crossoverOnly`, additionally record that leaving it undefined on a `runner="steam"` caller still
    filters, which is how `SteamBottleSetup.tsx` inherits D-16 without being edited. Destructure both
    in the component signature (L49-63).

    Import `resolveCrossoverOnly`, `filterWineEngines`, `selectDefaultEngine` and
    `resolveBottleNameState` from `'./engineFilter'`.

    Derive, above the existing effects: an `effectiveCrossoverOnly` from `resolveCrossoverOnly(runner,
    crossoverOnly)`, and an `engineOptions` from `filterWineEngines(wineVersionList,
    effectiveCrossoverOnly)` wrapped in the already-imported `useMemo` keyed on `wineVersionList`,
    `runner` and `crossoverOnly`.

    Replace the `wineVersionList` read at ALL FOUR engine-list consumption points — missing any one
    of them leaves a half-applied filter:
      SEAM 1 (L108-113, the auto-seed effect): replace `wineVersionList.at(0)` with
        `selectDefaultEngine(wineVersionList, effectiveCrossoverOnly)` and update the dependency
        array accordingly. Leave the `if (wineVersion) return` early-exit untouched — an
        already-chosen engine is never force-corrected (see the Deferred prohibition above).
      SEAM 2 (L164): `disabled={useSharedPrefix || engineOptions.length === 0}`.
      SEAM 3 (L167-169): resolve the picked name against `engineOptions`, not `wineVersionList`.
      SEAM 4 (L173-178): map over `engineOptions`.

    For D-05, at the bottle-name field (L150-158): compute the bottle-name state once via
    `resolveBottleNameState(useSharedPrefix, bottleNameReadOnly)` and drive the `TextInputField`'s
    `disabled` from its `disabled` field. Convert the `{showBottle && (...)}` body to a fragment and
    render, immediately after the `TextInputField` and gated on the state's `showReadOnlyHelper`
    field, a `<span className="smallInputInfo">` carrying the UI-SPEC copy under key
    `gamelib:steam.install.bottleNameReadonlyHelper` with the English text
    "GameLib manages one shared CrossOver bottle for Steam. Naming a separate bottle per game isn't
    supported yet." as the inline default. Obtain it via a second translation hook
    `const { t: tGamelib } = useTranslation('gamelib')` alongside the existing
    `useTranslation('gamepage')`, exactly as `WineVersionSelector.tsx` does. Do not add the key to
    `public/locales/en/gamelib.json`.

    Leave `showPrefix`, `showBottle`, the `isKnownNotToWork` advisory and the
    `hideSharedPrefixToggle` block structurally unchanged.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx eslint src/frontend/screens/Library/components/InstallModal/WineSelector/ && npx jest src/frontend/screens/Library/components/InstallModal/WineSelector --silent</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - In `WineSelector/index.tsx`: `grep -c "crossoverOnly?: boolean" <file>` is `1` and
      `grep -c "bottleNameReadOnly?: boolean" <file>` is `1`.
    - `grep -n "from './engineFilter'" <file>` matches exactly once and names all four functions.
    - `grep -c "engineOptions" <file>` is at least `4` (the `useMemo` binding plus SEAMs 2, 3, 4).
    - `grep -n "wineVersionList.at(0)" <file>` returns NO matches — SEAM 1 is converted.
    - Excluding the `useMemo` argument and the `Props`/destructuring lines, no remaining
      `wineVersionList` read feeds the engine `SelectField`: verify by inspection that every
      occurrence of `wineVersionList` in the JSX return block has been replaced by `engineOptions`.
    - `grep -n "gamelib:steam.install.bottleNameReadonlyHelper" <file>` matches, on a line also
      containing the English default string, and `grep -n "useTranslation('gamelib')" <file>` matches.
    - `git diff --name-only` shows `public/locales/en/gamelib.json` is NOT modified.
    - `grep -n "smallInputInfo" <file>` matches inside the `showBottle` block.
    - `git diff --stat` lists only `WineSelector/index.tsx` for this task — no change to
      `SteamBottleSetup.tsx`, `InstallModal/index.tsx`, or `backend/storeManagers/steam/bottle.ts`.
  </acceptance_criteria>

  <done>
    Both props exist as optional/undefined-default additions, all four engine-list seams read
    `engineOptions`, the bottle-name field and its helper line are driven by
    `resolveBottleNameState`, `tsc --noEmit` and eslint are clean, and the diff is confined to this
    one source file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-scope the GPTK todo and prove existing callers are untouched</name>
  <files>.planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md</files>

  <read_first>
    - .planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md (the file being
      modified — read in full, note its YAML frontmatter shape and its "Solution" options (a)/(b))
    - .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-CONTEXT.md
      "Folded Todos" section (states the todo is "re-scoped, not closed") and "Deferred Ideas"
      (names the two halves that must remain open)
    - src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx (the completed
      Task 2 diff, to confirm the caller census below)
  </read_first>

  <action>
    Append a dated "## Update 2026-08-14 — Phase 34.13-03 (partial)" section to
    `.planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md`. Record precisely:
    that option (a)'s FRONTEND-FILTER half shipped as D-16 — `WineSelector` now offers only
    `type === 'crossover'` engines and seeds its default from that filtered list whenever
    `runner === 'steam'`, covering both the new install form and the guided `SteamBottleSetup`
    surface; that option (a)'s `provisionBottle` engine-rejection half did NOT ship and remains open;
    that the `resolveSteamBottleEngine` silent-override half did NOT ship and remains open; and that
    option (b), the prefix-based provisioning path, remains out of scope. State explicitly that the
    todo stays in `pending/` — the affordance is hidden, the underlying engine mismatch is not fixed,
    so a user who has already persisted a `toolkit` engine into the bottle store is still exposed.

    Keep the file in `.planning/todos/pending/`. Do NOT move it to a `done/`/`resolved/` directory,
    do not delete it, and do not remove or rewrite the original Problem/Solution sections — append
    only, so the untaken halves stay legible.

    Then run the caller census that proves the additive-props claim: confirm every existing
    `<WineSelector` call site in `src/` passes neither new prop and is therefore unchanged in
    behavior, and record the enumerated list of call sites with their `runner` value (or absence) in
    the SUMMARY. Any `runner="steam"` call site found is a site that now inherits D-16 — that is the
    intended outcome, not a defect.
  </action>

  <verify>
    <automated>pnpm test:ci</automated>
  </verify>

  <acceptance_criteria>
    - `ls .planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md` succeeds — the
      todo is still pending.
    - `grep -c "34.13" .planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md`
      is at least `1`.
    - `grep -n "provisionBottle" <todo>` and `grep -n "resolveSteamBottleEngine" <todo>` both still
      match, in text stating those halves remain open.
    - `git diff .planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md` shows
      only added lines — zero deletions.
    - `pnpm test:ci` exits 0 with no newly failing tests relative to the pre-plan baseline.
    - SUMMARY lists every `<WineSelector` call site found in `src/`, each annotated with its `runner`
      prop value or "no runner prop", and confirms none passes `crossoverOnly` or
      `bottleNameReadOnly`.
    - SUMMARY explicitly names `SteamBottleSetup.tsx` as a `runner="steam"` call site that gains the
      D-16 filter with zero edits to that file (the RESEARCH.md Pitfall 3 acceptance property).
  </acceptance_criteria>

  <done>
    The GPTK todo is appended-to and still pending with both untaken halves legible; the full suite
    is green; the WineSelector caller census is recorded and confirms zero behavior change for
    non-Steam callers plus automatic D-16 inheritance for `SteamBottleSetup.tsx`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → renderer form controls | The only boundary this plan touches. Both new props are compile-time booleans supplied by GameLib's own components; no user-supplied string, path, or identifier crosses any boundary in this plan. |
| renderer → backend (`provisionBottle`) | Explicitly NOT crossed by this plan — no IPC channel is added, called, or modified. Named here only to record that the filter is a display-layer control sitting in front of an unchanged backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.13-03-01 | Tampering | `engineFilter.ts` → `provisionBottle` | accept | The CrossOver-only filter is a UI affordance, NOT an enforcement boundary. A `toolkit`/`wine` engine already persisted into `steamBottleConfigStore`, or set via global config, still reaches `provisionBottle` unrejected. D-16 explicitly scopes this phase to the frontend half; the backend-rejection half is a Deferred Idea and stays open on the GPTK todo (Task 3). Accepted because the failure mode is a broken bottle for the affected user, not privilege escalation or data exposure — impact "medium, macOS only, workaround is the default" per the todo's own assessment. |
| T-34.13-03-02 | Information disclosure | D-05 helper string | accept | The new `gamelib:steam.install.bottleNameReadonlyHelper` line is static English copy with no interpolation of a path, account identifier, or bottle name. Nothing user-derived is rendered. |
| T-34.13-03-03 | Denial of service | `filterWineEngines` on the auto-seed effect | accept | `filterWineEngines` returns the input array by reference when `crossoverOnly` is false, and `engineOptions` is `useMemo`-keyed, so no new allocation or render loop is introduced for the non-Steam callers that dominate this component's usage. `selectDefaultEngine` returning `undefined` on an all-non-CrossOver list is handled by the effect's existing truthiness guard, so an empty filtered list cannot wedge the form. |
| T-34.13-03-04 | Elevation of privilege | new `Props` members | accept | Both props are optional booleans with undefined defaults, consumed only for render decisions inside one component. They gate no capability, no filesystem path, and no IPC call. |
| T-34.13-SC | Tampering | npm/pip/cargo installs | accept | **No package installs in this plan** — no dependency is added, upgraded, or removed, and no lockfile is touched. The Package Legitimacy Gate protocol is therefore NOT APPLICABLE to 34.13-03. If an executor finds itself needing a new package to satisfy any task here, that is a deviation requiring a blocking human checkpoint before install. |
</threat_model>

<verification>
1. `npx jest src/frontend/screens/Library/components/InstallModal/WineSelector --silent` — the new
   pure-module tests pass.
2. `npx tsc --noEmit` — clean; proves the additive props broke no existing `<WineSelector>` call site.
3. `npx eslint src/frontend/screens/Library/components/InstallModal/WineSelector/` — clean.
4. `pnpm test:ci` — full suite green, no regression in any existing frontend/backend project.
5. `git diff --name-only` for the whole plan lists exactly the four files in `files_modified` —
   in particular NOT `public/locales/en/gamelib.json`, NOT `SteamBottleSetup.tsx`, NOT
   `InstallModal/index.tsx`, and NOT `backend/storeManagers/steam/bottle.ts`.
6. `ls .planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md` — still pending.

**Deferred to 34.13-13's manual gate (structurally unprovable here — no jsdom):** that the rendered
engine dropdown actually shows only CrossOver entries, that the bottle-name field actually resists
typing, and that the helper line actually appears beneath it. A green suite for this plan proves the
decision functions and the absence of type/lint breakage — it does not prove the render.

### Validation-map rows contributed by this plan
(For 34.13-13 to lift into `34.13-VALIDATION.md`'s Per-Task Verification Map.)

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|
| 34.13-03-01 | 03 | 2 | D-16, D-05 | T-34.13-03-01 | Filter is display-only; backend unchanged and todo stays open | unit | `npx jest .../WineSelector/__tests__/engineFilter.test.ts --silent` |
| 34.13-03-02 | 03 | 2 | D-16, D-05 | T-34.13-03-04 | Optional props gate render only, no capability | static | `npx tsc --noEmit && npx eslint .../WineSelector/` |
| 34.13-03-03 | 03 | 2 | D-16 | T-34.13-SC | No package installs | integration | `pnpm test:ci` |
</verification>

<success_criteria>
- `engineFilter.ts` exists with exactly four pure exports and zero UI/CSS/React coupling.
- `engineFilter.test.ts` passes with ≥9 tests, including the RED-proven `selectDefaultEngine`
  CrossOver-seeding guard and the 7-case exhaustive `resolveCrossoverOnly` enumeration.
- **D-16:** all four engine-list consumption points in `WineSelector/index.tsx` read `engineOptions`;
  `wineVersionList.at(0)` no longer appears in the file.
- **D-16:** `SteamBottleSetup.tsx` inherits the filter with zero edits to that file, via
  `resolveCrossoverOnly`'s `runner === 'steam'` default.
- **D-05:** the bottle-name field's `disabled` and the helper line's visibility both come from
  `resolveBottleNameState`, and the shared-prefix disable does not surface the helper copy.
- **D-05:** the helper string ships as `tGamelib('gamelib:steam.install.bottleNameReadonlyHelper',
  '…')` with an inline English default and no edit to `public/locales/en/gamelib.json`.
- Every existing non-Steam `<WineSelector>` caller is behaviorally unchanged; the census is recorded
  in the SUMMARY.
- The GPTK todo is appended-to, still in `pending/`, with both untaken halves legible.
- `npx tsc --noEmit`, eslint on the WineSelector directory, and `pnpm test:ci` all green.
</success_criteria>

<output>
Create `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-03-SUMMARY.md`
when done. It must record: the RED-proof failure message from Task 1, the full `<WineSelector>`
caller census from Task 3 with each site's `runner` value, and an explicit statement that the
`provisionBottle`-rejection and `resolveSteamBottleEngine`-override halves of the GPTK todo remain
open.
</output>
