---
phase: 34.13
slug: steam-install-time-wine-bottle-form-gog-parity
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-14
---

# Phase 34.13 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `34.13-RESEARCH.md` §"Validation Architecture". The Per-Task Verification Map is
> filled by the planner once plan/task IDs exist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29.x (`ts-jest` preset), 5 projects (`backend` / `common` / `frontend` / `preload` / `meta`) |
| **Config file** | `jest.config.js` (root) — `testEnvironment: 'node'` project-wide |
| **Quick run command** | `npx jest <path-to-file> --silent` |
| **Full suite command** | `pnpm test:ci` (`jest --runInBand --silent`) |
| **Estimated runtime** | Full suite ~several minutes (4400+ tests); single file ~5–20s |

**HARD CONSTRAINT — no jsdom, no react-test-renderer anywhere in this repo.**
Confirmed by `SideloadDialog/__tests__/index.test.tsx`'s own doc-comment. A test that imports a
`.tsx` dialog directly fails at `import './index.scss'` before any assertion runs. Therefore **no
test in this phase may prove a behavior by rendering a component and simulating a click.** Every
frontend deliverable must be proven either at the pure-function / zustand-store level (the
`SteamBottleSetup.test.ts` and `SideloadDialog/filters.ts` precedent — logic extracted OUT of the
`.tsx` specifically so it is testable) or at the manual UAT gate below.

---

## Sampling Rate

- **After every task commit:** Run `npx jest <the task's test file> --silent`
- **After every plan wave:** Run `pnpm test:ci`
- **Before `/gsd-verify-work`:** Full suite green **AND** the manual gate below executed
- **Max feedback latency:** ~20s (single file), full suite per wave

⚠ **A green Jest suite alone does NOT satisfy this phase's gate.** The primary deliverable — the
form actually opening and its choice actually reaching the bottle store — is structurally
unprovable in Jest here. The manual gate is load-bearing, not a formality.

---

## Per-Task Verification Map

*Filled by 34.13-13, Task 2, lifted from each sibling plan's own "Per-task verification map" block
where present, and synthesized from the plan's own `<verification>` block where it lacked one
(34.13-02, 34.13-07, 34.13-09 — three plans that verified per-plan rather than per-task).
All rows below were re-confirmed live: `pnpm codecheck` exit 0, `pnpm test:ci` exit 0
(273 suites / 5398 passed / 1 pre-existing skip / 5399 total), so every row's cited test file is
independently confirmed green as of this fill, not merely trusted from the owning plan's SUMMARY.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 34.13-01-01 | 01 | 1 | D-17, D-26 | T-34.13-01, T-34.13-03 | Contract gate pins the two new optional fields and the absent always-show key | unit (contract gate, RED-proven) | `! npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts --silent` | ❌ W0 (created by this task) | ✅ green |
| 34.13-01-02 | 01 | 1 | D-17, D-26 | T-34.13-01, T-34.13-03 | Types declared; verdict type exported; no always-show key anywhere | unit + static | `npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts --silent && pnpm codecheck && pnpm test:ci` | ✅ | ✅ green |
| 34.13-02-01 | 02 | 2 | D-17 | — | Captures `is_windows_native` from the already-fetched appdetails response; no new network call | unit + static | `npx tsc --noEmit && npx jest src/backend/storeManagers/steam/__tests__/games.test.ts --silent` | ✅ | ✅ green |
| 34.13-02-02 | 02 | 2 | D-17, D-07 | — | Hydrates `is_windows_native` onto the synced `GameInfo`; `getInstallInfo()` stub documented, still returns `undefined` | unit + static | `npx tsc --noEmit && npx jest src/backend/storeManagers/steam/__tests__/library.test.ts --silent` | ✅ | ✅ green |
| 34.13-02-03 | 02 | 2 | D-09 | — | `checkBottleEligibility()` public wrapper added without widening any private method | unit + static | `npx tsc --noEmit && npx jest src/backend/storeManagers/steam/__tests__/games.test.ts --silent` | ✅ | ✅ green |
| 34.13-03-01 | 03 | 2 | D-16, D-05 | T-34.13-03-01 | Filter is display-only; backend unchanged, D-07 todo stays open | unit | `npx jest src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts --silent` | ✅ | ✅ green |
| 34.13-03-02 | 03 | 2 | D-16, D-05 | T-34.13-03-04 | Optional props gate render only, no new capability | static | `npx tsc --noEmit && npx eslint src/frontend/screens/Library/components/InstallModal/WineSelector/` | ✅ | ✅ green |
| 34.13-03-03 | 03 | 2 | D-16 | T-34.13-SC | No package installs | integration | `pnpm test:ci` | ✅ | ✅ green |
| 34.13-05-01 | 05 | 2 | D-03, D-11, D-18, D-19, D-20, D-22, D-26 | T-34.13-05-06 | Matrix harness enumerates all 8 gating rows | unit (matrix harness, RED-proven) | `! npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamSectionGating.test.ts --silent` | ❌ W0 (created by this task) | ✅ green |
| 34.13-05-02 | 05 | 2 | D-02, D-03, D-08, D-11, D-17, D-18, D-19, D-20 | T-34.13-05-01 | Pure `resolveSteamSectionGating()` gating function ships | unit | `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamSectionGating.test.ts --silent && pnpm codecheck` | ✅ | ✅ green |
| 34.13-05-03 | 05 | 2 | D-09 (source gate), D-18, D-20, D-22, D-26 | T-34.13-05-03, T-34.13-05-04, T-34.13-05-06 | Saboteur-proven harness rejects 10 named broken gating functions | unit + static | `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamSectionGating.test.ts --silent && pnpm codecheck && pnpm test:ci` | ✅ | ✅ green |
| 34.13-06-01 | 06 | 3 | D-17 | T-34.13-06-02 | Legacy routing is byte-identical when the override is absent | unit (characterization, RED-proven vs. 2 deliberate breaks) | `npx jest src/backend/storeManagers/steam/__tests__/games.test.ts -t "D-17 Windows-via-bottle override" --silent` | ✅ (extends existing suite) | ✅ green |
| 34.13-06-02 | 06 | 3 | D-17 | T-34.13-06-01, T-34.13-06-04 | A gated override selects among existing branches only | unit | same command | ✅ | ✅ green |
| 34.13-06-03 | 06 | 3 | D-17 | T-34.13-06-01, T-34.13-06-04 | Malformed/hostile override falls through, logged | unit | `npx jest src/backend/storeManagers/steam/__tests__/games.test.ts --silent && pnpm codecheck` | ✅ | ✅ green |
| 34.13-07-01 | 07 | 3 | D-17 | T-34.13-07 | Shared, guarded, dual-runtime handler seam + typed contract built | unit + static | `npx tsc --noEmit && npx jest src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts --silent` | ✅ | ✅ green |
| 34.13-07-02 | 07 | 3 | D-17 | T-34.13-07 | Registered on BOTH runtimes (Electron `main.ts` + sidecar) and preload seam wired, all proven by execution | unit + static | `npx jest src/backend/sidecar/__tests__/steamAuthFlows.test.ts src/preload/__tests__/steamInstallFormApi.test.ts --silent && npx tsc --noEmit` | ✅ | ✅ green |
| 34.13-07-03 | 07 | 3 | D-17 | T-34.13-07 | `IPC-PORT-INVENTORY.md` records both channels; preload-surface gate reconciled | static + integration | `python3 .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py && pnpm test:ci` | ✅ | ✅ green |
| 34.13-08-01 | 08 | 2 | D-22, D-23, D-24, D-25, D-27, D-28, D-17 | T-34.13-08-01, -03, -04, -05 | `InstallGameModal` store gains quick-vs-options intent split, RED-proven | unit (store-level + pure fns) | `npx jest src/frontend/state/__tests__/InstallGameModal.test.ts --silent && npx tsc --noEmit` | ❌ W0 (created by this task) | ✅ green |
| 34.13-08-02 | 08 | 2 | D-21, D-27, D-28 | T-34.13-08-01, -06 | `MainButton` split button (primary + caret) wired to the intent split | unit (element graph) + static | `npx jest src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamSplitButton.test.tsx --silent && npx jest src/frontend/components/UI/Dropdown --silent && npx tsc --noEmit` | ❌ W0 (created by this task) | ✅ green |
| 34.13-08-03 | 08 | 2 | D-04 | T-34.13-08-01 | `SteamInstallLocationPicker` retired | static (absence) + full regression | `! grep -rn "SteamInstallLocationPicker\|useSteamInstallLocation\|SteamLibraryOption" src/ && npx tsc --noEmit && pnpm test:ci` | ✅ | ✅ green |
| 34.13-09-01 | 09 | 4 | D-15 | — | "persisted value vs. derived default" decision extracted into a pure, tested function | unit | `npx jest src/frontend/screens/Game/GamePage/components/__tests__/steamBottleDefaults.test.ts --silent` | ✅ | ✅ green |
| 34.13-09-02 | 09 | 4 | D-15 | — | Seeding effect wired to the persisted value behind a settle gate, gated by source assertion | unit + static | `npx jest src/frontend/screens/Game/GamePage/components/__tests__/steamBottleSetupSeeding.test.ts --silent && npx tsc --noEmit` | ✅ | ✅ green |
| 34.13-10-01 | 10 | 5 | D-02, D-08 | T-34.13-10-01 | Pure install-target module, RED-proven, saboteur-checked | unit (pure module) | `npx jest src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/installTarget.test.ts --silent && npx tsc --noEmit` | ❌ W0 (created by this task) | ✅ green |
| 34.13-10-02 | 10 | 5 | D-01, D-02, D-06, D-08, D-14 | T-34.13-10-02, T-34.13-10-04, T-34.13-10-05 | `SteamDialog` skeleton built to `ThirdPartyDialog` shape, D-06 never gates on size | static (tsc + eslint) | `npx tsc --noEmit && npx eslint src/frontend/screens/Library/components/InstallModal/SteamDialog --ext .ts,.tsx` | ✅ | ✅ green |
| 34.13-10-03 | 10 | 5 | D-01, D-06, D-14, and 34.13-05's review obligation | T-34.13-10-06 | Comment-stripped absence gates (`getInstallInfo`, `writeConfig`, generic `install()`) specimen-proven | static + integration | `npx jest src/frontend/screens/Library/components/InstallModal/SteamDialog --silent && npx tsc --noEmit && pnpm test:ci` | ❌ W0 (created by this task) | ✅ green |
| 34.13-11-01 | 11 | 7 | D-25 | T-34.13-11-01, -02, -03, -05 | Cold-cache eligibility probe pure functions, 3 named discriminators, RED-proven | unit | `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts --silent` | ❌ (created by this task) | ✅ green |
| 34.13-11-02 | 11 | 7 | D-25 | — | `EligibilityLoadingRow` component, element-graph inspection, RED-proven | unit (direct component invocation) | `npx jest src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/EligibilityLoadingRow.test.tsx --silent` | ❌ (created by this task) | ✅ green |
| 34.13-11-03 | 11 | 7 | D-25, D-11, D-06 | T-34.13-11-04, -06 | Wired into `SteamDialog`; D-06's "never disabled" claim narrowed to the one documented `eligibilityPending` exception | static (comment-stripped source + recursive census) + regression | `npx jest src/frontend/screens/Library/components/InstallModal --silent && pnpm test:ci` | ❌ (created by this task) | ✅ green |
| 34.13-12-01 | 12 | 6 | D-17, D-18, D-19, D-03 | T-34.13-12-01 | Pure `steamPlatformRow` module, RED-proven + 3 saboteurs | unit | `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamPlatformRow.test.ts --silent && npx tsc --noEmit` | ❌ W0 (created by this task) | ✅ green |
| 34.13-12-02 | 12 | 6 | D-01, D-03, D-05, D-16, D-17, D-18, D-19 | T-34.13-12-01, T-34.13-12-04 | `InstallModal`'s fifth (Steam) branch mounts `SteamDialog` | static (tsc + eslint) + regression | `npx tsc --noEmit && npx eslint src/frontend/screens/Library/components/InstallModal --ext .ts,.tsx && npx jest src/frontend/screens/Library/components/InstallModal --silent` | ✅ | ✅ green |
| 34.13-12-03 | 12 | 6 | D-01, D-03, D-16, D-17 | T-34.13-12-03, T-34.13-12-04 | Comment-stripped source gates on the reworked platform row, specimen-proven | static + integration | `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/installModalSource.test.ts --silent && npx tsc --noEmit && pnpm test:ci` | ❌ W0 (created by this task) | ✅ green |
| 34.13-14-01 | 14 | 4 | D-17 | T-34.13-14-01, T-34.13-14-05 | Durable forced verdict re-routes all six `games.ts` consumers except the bridge | unit (RED-proven against the un-persisted tree) | `npx jest src/backend/storeManagers/steam/__tests__/games.test.ts -t "D-17 forced-verdict durability" --silent` | ✅ (extends existing suite) | ✅ green |
| 34.13-14-02 | 14 | 4 | D-17 | T-34.13-14-01, -02, -04, -05 | Verdict written only by a committed forced install and survives re-fetch | unit | same command, plus `-t "D-17 Windows-via-bottle override"` for the 34.13-06 baseline | ✅ | ✅ green |
| 34.13-14-03 | 14 | 4 | D-17 | T-34.13-14-03, T-34.13-14-05 | Confirmed bottle removal clears the verdict; install-state reconciliation unchanged | unit | `npx jest src/backend/storeManagers/steam/__tests__/library.test.ts --silent` | ✅ | ✅ green |
| 34.13-15-01 | 15 | 3 | D-27 (rows 3, 5), D-28 | T-34.13-15-01 | `GameCard`/`GameSubMenu` "Install with options…" entry predicates, RED-proven | unit + static | `npx jest src/frontend/helpers/__tests__/steamInstallOptionsEntry.test.ts --silent && npx tsc --noEmit` | ❌ W0 (created by this task) | ✅ green |
| 34.13-15-02 | 15 | 3 | D-24, D-28, D-29 | T-34.13-15-02, -03, -04, -05 | Console Mode Steam-blocked target verdict + intent matrices, RED-proven | unit + static | `npx jest src/frontend/screens/ConsoleMode/InstallOverlay/__tests__/consoleSteamTarget.test.ts --silent && npx tsc --noEmit` | ❌ W0 (created by this task) | ✅ green |
| 34.13-13-01 | 13 | 8 | D-06, D-20 | T-34.13-13-01 | 11 `gamelib:` catalog keys written, byte-identical to inline source defaults, mechanically extracted and RED-proven | static (mechanical byte-identity + retired-key absence, RED-proven) | `node -e "<see 34.13-13-SUMMARY.md for the full byte-identity script>"` | ✅ | ✅ green |
| 34.13-13-02 | 13 | 8 | — | — | Phase-wide automated gate run; this map filled | static + integration | `pnpm codecheck && pnpm test:ci && ! grep -rq "SteamInstallLocationPicker\|useSteamInstallLocation" src/ && pnpm lint` | ✅ | ❌ red — `pnpm lint` exits 1 (see finding below; codecheck/test:ci/picker-retirement all green individually) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Gate D finding — `pnpm lint` does not exit 0 (Task 2, Gate D)

Measured: 54 errors / 3623 warnings, `exit 1`. Two categories:

1. **52 errors are pre-existing repository-wide lint debt, unrelated to any 34.13 plan's files**
   (`meta/lintTranslations.ts`, `meta/buildSidecarSea.ts`, several `src/backend/sidecar/__tests__/*`
   and `src/backend/sidecar/*.ts` files, `src/backend/storeManagers/gog/redist.ts`,
   `src/backend/humble/dedup.ts`, `src/backend/downloadmanager/downloadqueue.ts`,
   `src/backend/crossover_index/__tests__/index.test.ts`, `meta/__tests__/i18nParserConfig.test.ts`,
   `meta/__tests__/preserveRunnerSymlinks.test.ts`, `src/backend/__tests__/longRunningChannels.test.ts`,
   `src/backend/__tests__/tauriShellSource.test.ts`, `src/frontend/screens/Game/GamePage/components/SteamBridgeSetup.tsx`
   — mostly `@typescript-eslint/no-require-imports`, `no-unnecessary-type-assertion` and
   `no-unused-vars` in files this phase never touches).
2. **2 errors are inside a file this phase created:** `src/backend/storeManagers/steam/installFormIpc.ts:78`
   and `:82`, both `@typescript-eslint/no-unnecessary-type-assertion`. That file was created by
   34.13-07 (Task 1), whose own `<verification>` block ran `npx tsc --noEmit` and three Jest files but
   never `pnpm lint` — a gap in that plan's own verification list, not a regression introduced here.

Per this task's own scope fence ("Do not edit any file under `src/`... Wave 7 is a gate, not a repair
bay"), neither category was fixed. Both are reported as gap-closure input; see SUMMARY.

### Gate C finding — picker-retirement grep returns 1, not 0

`grep -rn "SteamInstallLocationPicker\|useSteamInstallLocation" src/` returns exactly one line:
`src/frontend/screens/Library/components/InstallModal/SteamDialog/installTarget.ts:41`, a doc comment
*documenting* D-04's retirement of the component ("...retiring `SteamInstallLocationPicker`'s own
default-selection effect..."), not a live reference to it. `grep -rn ... | grep -v "^\s*\*"` (comment
line stripped) returns `0`. The substance of D-04 — no code path imports or calls the retired
component/hook — holds; the plan's literal "returns nothing" acceptance criterion is stale against a
one-line explanatory comment 34.13-10 wrote when retiring it. Recorded as a finding, not fixed (no
`src/` edits this task).

### Gate E finding — `disabled` count is 1, not 0 (comment-stripped `SteamDialog/index.tsx`)

`getInstallInfo` (comment-stripped): `0` — matches. `disabled` (comment-stripped): `1`, not the `0`
this plan's Task 2 acceptance criteria states. The one match is `disabled={eligibilityPending}` on the
footer Install button, added by **34.13-11 (D-25, Wave 7)** — a plan that landed *after* this Gate E
text was drafted. The file's own header doc comment (lines 30-38) names this explicitly as "a PLANNED
narrowing of 34.13-10's original 'no disabled attribute in any state' claim, not a regression," tied to
D-25's in-dialog loading state rather than to D-06's size/free-space gating (`getInstallInfo`,
`diskSize`, `spaceLeftAfter`, `notEnoughDiskSpace` all measure `0`). D-06's actual substance — never
gated on size — holds. Flagged for the human UAT tester on `G-D06-NOSIZE` (see `34.13-UAT.md`) so the
interaction with D-25's `eligibilityPending` disable is not scored as a D-06 violation, and so a tester
working from this plan's literal Gate E text does not reconcile against a stale expectation.

### Deliverable → test-type map (from RESEARCH.md, pre-plan)

| Deliverable | Test Type | Target file |
|---|---|---|
| `isSteamBottleEligible` backend verdict (awaits `ensurePlatformsCaptured()`) | unit | `src/backend/storeManagers/steam/__tests__/games.test.ts` (extend) |
| IPC dual-registration (Electron `main.ts` **and** sidecar) + preload exposure | unit | `src/backend/sidecar/__tests__/steamAuthFlows.test.ts` (extend) |
| Trigger logic in `startSteamInstall` / `openInstallGameModal` | unit (store-level) | `src/frontend/state/__tests__/InstallGameModal.test.ts` (**new file**) |
| `WineSelector` CrossOver-only filter (D-16) | unit (extracted pure fn) | new — extract filter out of `.tsx` first |
| `WineSelector` read-only bottle name (D-05) | unit (extracted prop logic) | same as above |
| Wine choice → `persistBottleWineVersion` call site (D-14) | unit | new call-site test (store round-trip already covered in `bottle.test.ts`) |
| `SteamBottleSetup` **reads** the persisted version (D-15) | unit (extracted "which default wins" fn) | alongside `steamBottleDefaults.ts` |
| `SteamInstallLocationPicker` retirement leaves no dangling refs | static | `tsc --noEmit` + `grep -rn "SteamInstallLocationPicker\|useSteamInstallLocation" src/` → empty |
| New user-facing strings localised | static | `pnpm lint-translations:gamelib` green |

---

## Wave 0 Requirements

- [x] `src/frontend/state/__tests__/InstallGameModal.test.ts` — created by 34.13-08 Task 1; confirmed
      PASS in the full suite run for this fill
- [x] Extract `WineSelector`'s CrossOver filter + read-only-name logic into a pure module, plus its
      test — `engineFilter.test.ts`, created by 34.13-03 Task 1; confirmed PASS
- [x] Extract `SteamBottleSetup`'s "persisted value vs. derived default" decision into a pure fn, plus
      its test — `steamBottleDefaults.test.ts`, created by 34.13-09 Task 1; confirmed PASS
- [x] Extend `src/backend/storeManagers/steam/__tests__/games.test.ts` with `checkBottleEligibility()`
      coverage — 34.13-02 Task 3 / 34.13-06; confirmed PASS
- [x] Extend `src/backend/sidecar/__tests__/steamAuthFlows.test.ts` with the new channel's sidecar
      registration — 34.13-07 Task 2; confirmed PASS

---

## Manual-Only Verifications

**⚠ AMENDED 2026-08-15 by D-21..D-28.** The prior version of this table was indexed by D-10's
auto-open trigger combinations. **D-22 retired every trigger and D-26 cut the always-show setting**,
so a gate that enumerates trigger rows now measures a contract that does not exist — the exact
"non-vacuous gate that guards nothing" failure this document's own Vacuous-Assertion section warns
about. The axis is replaced: **the user is the trigger**, so the gate must enumerate the *two user
intents* (quick install vs. "Install with options…") crossed with the section-gating conditions,
and it must cover the split control on every surface that carries one.

**One successful install is still not proof of the feature** — the reason has changed, not the rule.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| **Quick install (the default path)** installs with no dialog | D-22, D-23 | Regression guard on Phase 21 D-09, now reached via D-23 | Click the split button's **primary** half on a native game → **nothing opens**, install proceeds silently, and the game lands in Steam's primary library |
| **Caret opens the options dialog** — `MainButton` | D-21, D-25 | No jsdom — Jest cannot render `InstallModal` | Click the caret → dialog opens **instantly** (D-25), not after an eligibility wait |
| **Context menu opens the options dialog** — `GameCard` | D-27 | Per-surface shape differs (D-27); a `MainButton` pass does not cover this | Right-click / open the card's context menu → an "Install with options…" item exists and opens the same dialog |
| **`GameSubMenu`'s relabelled entry** | D-27, D-28 | D-27 marks it fixed-to-options; RESEARCH Q4 found no existing item to relabel, so this is new UI | Open the game submenu → the install entry reads "Install with options…" and opens the dialog |
| Options dialog renders correctly for a bottle-eligible game | D-01, D-05, D-06 | No jsdom | macOS + a Windows-only Steam game, **via the caret**: read-only "Windows" platform row, wine section, read-only `GameLibSteam` bottle name |
| Chosen wine version survives into the bottle store | D-15 | Cross-process write→read across two independent code paths | Pick a non-default CrossOver engine → Install → confirm `SteamBottleSetup` opens **already carrying that engine** rather than re-asking |
| Library dropdown appears when a real choice exists | D-02, D-03 | Requires a real second Steam library on disk | `enableSteamNativeInstall` ON, 2+ libraries, **via the caret** on a non-bottle game → dropdown appears; chosen target is where the game lands |
| **D-25 busy state — INVERTED from the retired D-12 contract** | D-25 | Requires a cold cache; timing-dependent | Clear Steam metadata cache, click the **caret** → the dialog opens **immediately** with the wine section showing its loading state and the dialog's own Install button disabled until eligibility resolves. **A half-rendered dialog is now the CORRECT result** — the old gate asserted the opposite ("no half-rendered modal"), so a tester working from memory will score this backwards |
| **Quick path fires NO eligibility probe** | D-25 | Absence-of-latency is unobservable in a unit test | Cold cache, click the **primary** half → install starts without the multi-second wait. D-25 removes the dead-click risk from the default path entirely |
| **D-24 degrade — target library missing/full** | D-24 | Requires physically detaching or filling a library | Make the primary library unavailable, click the **primary** half → quick install **degrades into the options dialog with the problem surfaced**, rather than erroring or silently picking another drive |
| **D-24 ≤1-library corner (OPEN — observe, do not assume)** | D-24 | Known unresolved gap, see UI-SPEC | Same as above but with the failing library as the **only** library → the dialog has no dropdown offering a replacement. **Record what Install actually does**; the behavior is undecided, so this row's job is to observe, not to pass/fail |
| **Gamepad reachability of the caret (COULD NOT VERIFY)** | D-21 | No repo precedent for a two-target split button's gamepad focus | With a gamepad: confirm the caret is reachable, that reaching it does not make the primary half unreachable, and that focus order is stable on both `MainButton` and the game card |
| Both runtimes | D-21, D-25 (cross-cutting) | Phase must work on Electron **and** Tauri/sidecar | Repeat the quick-install, caret-opens, and D-25 busy rows under `pnpm tauri:dev` (never `tauri dev` — serves a stale bundle) |

---

## Named Vacuous-Assertion Risks (must be designed against, not discovered later)

1. **Store-flip ≠ dialog renders.** A test asserting `useInstallGameModal.setState({isOpen: true})`
   proves the store flipped, not that `InstallModal/index.tsx` renders a Steam branch. A green
   store test and a broken dialog are indistinguishable to Jest here. Mitigation is the manual
   gate — not a cleverer unit test.
2. **Write ≠ read.** Asserting `persistBottleWineVersion` was called proves the write. It is not
   evidence `SteamBottleSetup` reads it — two independent paths, two independent tests.
3. **Registration ≠ wired.** Asserting the handler is registered in both files proves
   registration, not that the preload seam exposes it. Verify `src/preload/` exposure in the *same*
   task, not a follow-up.
4. **One click-through ≠ coverage.** See the Manual-Only table — enumerate the **two user intents**
   (quick install vs. "Install with options…") crossed with the section-gating conditions, on
   **each** surface that carries a split control. *(Amended by D-22/D-27: this item previously said
   "enumerate D-10's trigger rows crossed with native-install ON/OFF"; those triggers are retired.)*
5. **A pass recorded against the retired contract is worse than no pass.** Two rows in the table
   above now assert the **opposite** of what the pre-amendment gate asserted — D-25's half-rendered
   dialog is correct where D-12 forbade it, and the always-show row is gone entirely because the
   control no longer exists. Anyone re-running this gate from memory, or reconciling an older UAT
   ledger against it, will score those backwards. Re-derive from this table, never from a prior run.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or a Wave 0 dependency
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references above
- [x] No watch-mode flags
- [x] Every frontend deliverable is either pure-function-tested or listed in the manual gate — none silently assumed
- [ ] Manual gate executed and recorded (a green suite alone does not close this phase) — **Task 3 owns this row; left unticked**
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — Gates A (`codecheck`), B (`test:ci`), C (picker retirement, substance confirmed)
discharged green; Gate D (`pnpm lint`) is red (54 pre-existing/out-of-scope errors + 2 in-phase errors
in `installFormIpc.ts`, see findings above); Gate E (`getInstallInfo`/`disabled` static half) discharged
with one documented, planned deviation (D-25's `eligibilityPending` disable). The blocking manual gate
(Task 3) has not yet run.
