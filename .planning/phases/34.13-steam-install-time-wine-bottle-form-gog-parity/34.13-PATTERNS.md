# Phase 34.13: Steam install-time wine/bottle form (GOG parity) - Pattern Map

**Mapped:** 2026-08-14
**Files analyzed:** 13 (new + modified)
**Analogs found:** 13 / 13

> ## ⚠ PARTIALLY SUPERSEDED 2026-08-15 by D-21..D-28 — read this before emitting any task
>
> This map was produced against the retired auto-open trigger model. It is **still accurate for
> every file it covers except one**, but it is **no longer complete**. Two specific hazards:
>
> **1. One entry is a trap.** The `AlwaysShowSteamInstallForm.tsx` / `alwaysShowInstallFormSetting.ts`
> section is **RETIRED — do not build those files** (D-26 cut the setting; plan `34.13-04` is
> deleted). It is kept only as a settings-toggle pattern reference. See the 🛑 banner on that section.
>
> **2. Four NEW surfaces have NO pattern assignment in this document.** D-21/D-24/D-27 introduced
> them after this map was written, so "Analogs found: 13/13" does **not** mean full coverage of the
> amended phase:
> - the **split button** on `GamePage/components/MainButton.tsx` (primary + caret)
> - the **"Install with options…" context-menu item** on `Library/components/GameCard/index.tsx`
> - the **new install entry** on `Game/GameSubMenu/index.tsx` (RESEARCH Q4: nothing exists to
>   relabel, so this is a new control, not an edit)
> - the **D-24 degrade notice** surfaced when quick install's local check fails
>
> For those four, the authoritative sources are **`34.13-UI-SPEC.md`** §"Split-Button Control
> Contract" and §"D-24 Degrade-into-Options Contract" (both checker-verified against real source:
> the caret idiom against `FilterFacetGroup/index.scss:9-15`, tokens against `_colors.scss:4,28`),
> plus **`34.13-RESEARCH.md`** §"Amendment research" **Q2** — which establishes that
> `UI/Dropdown/` is a gamepad-aware caret primitive but **is NOT a split button**, so the split
> must be composed rather than assumed.
>
> **3. The routing target is not the current wiring.** RESEARCH **Q4** found that four of D-27's six
> call sites do not fire for `runner === 'steam'` today, and that a seventh, uncounted site
> (`GamePage/index.tsx:663-688`) is a third hardcoded `window.api.install({...})` bypassing both
> `openInstallGameModal` and `startSteamInstall`. The `InstallGameModal.ts` entry below describes
> the chokepoint as it exists; do not assume editing it reaches every surface.

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx` (NEW) | component (sibling dialog) | request-response (form → install IPC) | `src/frontend/screens/Library/components/InstallModal/ThirdPartyDialog/index.tsx` | exact — same prop contract, same lean shape (146 lines), explicitly named by RESEARCH.md as the model over `DownloadDialog` |
| `src/frontend/screens/Library/components/InstallModal/SteamDialog/index.scss` (NEW) | style | — | `src/frontend/screens/Library/components/InstallModal/ThirdPartyDialog/index.css` + `InstallModal/index.scss` (shared classes) | exact — reuse `.infoBox`, `.InstallModal__sectionHeader`, `.InstallModal__toggle`, `.smallInputInfo`; do not fork new tokens |
| `src/frontend/screens/Library/components/InstallModal/index.tsx` (MODIFY — add 5th branch) | component (dialog router) | request-response | itself (existing 4-branch pattern at lines 173–281) | exact — this is the established pattern, not a new one (D-01) |
| `src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx` (MODIFY — add `crossoverOnly`/`bottleNameReadOnly` props) | component (form fragment) | request-response | itself | exact — additive optional props on an existing component, same convention as `runner`/`hideSharedPrefixToggle`/`sharedPrefixNote` already added by Phase 17 |
| `src/frontend/state/InstallGameModal.ts` (MODIFY — `startSteamInstall` gains eligibility check + always-show read) | store / trigger logic | event-driven (zustand state transition) | itself (existing `startSteamInstall`/`installSteamGame` functions) | exact — same file, same pattern, extend not replace |
| `src/frontend/state/SteamInstallLocation.ts` (RETIRE) | store | — | n/a — deleted, folded into new SteamDialog state | n/a |
| `src/frontend/screens/Game/GamePage/components/SteamInstallLocationPicker.tsx` (RETIRE) | component | — | n/a — deleted, folded into new SteamDialog | n/a |
| `src/frontend/App.tsx` (MODIFY — remove `<SteamInstallLocationPicker />` mount) | provider/mount root | — | itself (lines 34, 142) | exact |
| `src/backend/storeManagers/steam/games.ts` (MODIFY — add `checkBottleEligibility()` public wrapper) | service (backend class method) | request-response | `isBottleEligible()` (private, line 1328) + `ensurePlatformsCaptured()` (private, line 1383) — same class, same file | exact — new public method composes two existing private methods, same pattern `isBridgeEligible()` (line 1353) already uses to compose `isBottleEligible()` |
| `src/backend/main.ts` (MODIFY — register `isSteamBottleEligible` handler) | route (Electron IPC) | request-response | the "macOS CrossOver bottle trio" block, lines 922–934 (`steamBottleProvision`/`isSteamBottleProvisioned`/`steamBottleStatus`) | exact — same file, same physical grouping, same `addHandler` shape |
| `src/backend/sidecar/steamAuthFlowRegistration.ts` (MODIFY — register `isSteamBottleEligible` handler) | route (Tauri sidecar IPC) | request-response | `isSteamBottleProvisioned`/`steamBottleStatus` handlers, lines 225–241 | exact — same file (NOT `installFlowRegistration.ts` — see Shared Patterns / research contradiction note), same `ipcMain.handle` shape |
| `src/frontend/screens/Settings/components/AlwaysShowSteamInstallForm.tsx` (NEW, D-13) | component (settings toggle) | request-response | `src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx` | exact — CONTEXT.md D-13 explicitly says "follow `EnableSteamNativeInstall.tsx` exactly" |
| `src/backend/storeManagers/steam/alwaysShowInstallFormSetting.ts` (NEW, D-13) | config / backend accessor | request-response | `src/backend/storeManagers/steam/nativeInstallSetting.ts` | exact — CONTEXT.md D-13 explicitly says "follow `nativeInstallSetting.ts` exactly" |
| `src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx` (MODIFY — seeding effect prefers persisted wineVersion, Pattern 4 gap) | component (guided setup) | request-response | itself (existing seeding `useEffect`, lines 78–92) | exact — same file, same effect, add one branch |

## Pattern Assignments

### `src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx` (component, request-response)

**Analog:** `src/frontend/screens/Library/components/InstallModal/ThirdPartyDialog/index.tsx` (146 lines total — read in full)

**Imports pattern** (lines 1-22):
```typescript
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import {
  GameInfo,
  InstallPlatform,
  Runner,
  WineInstallation
} from 'common/types'
import Anticheat from 'frontend/components/UI/Anticheat'
import {
  DialogFooter,
  DialogHeader,
  DialogContent
} from 'frontend/components/UI/Dialog'
import { install, writeConfig } from 'frontend/helpers'
import ContextProvider from 'frontend/state/ContextProvider'
import { InstallProgress } from 'frontend/types'
import React, { useCallback, useContext } from 'react'
import { useTranslation } from 'react-i18next'
import { AvailablePlatforms } from '..'
import './index.css'
```
For the Steam dialog: drop `writeConfig` (D-14 forbids it — use `persistBottleWineVersion` IPC instead), drop `Anticheat`/`hasAnticheatInfo` (no anticheat concept for Steam here), keep everything else. Change `'./index.css'` to `'./index.scss'` per this dialog's own SCSS needs (D-02 dropdown, D-08 free-space row).

**Prop contract — copy verbatim** (lines 24-35):
```typescript
interface Props {
  backdropClick: () => void
  appName: string
  runner: Runner
  platformToInstall: InstallPlatform
  availablePlatforms: AvailablePlatforms
  winePrefix: string
  crossoverBottle: string
  wineVersion: WineInstallation | undefined
  children: React.ReactNode
  gameInfo: GameInfo
}
```
This is the identical shape every sibling receives from `InstallModal/index.tsx`. The Steam dialog takes the SAME shape (RESEARCH.md Pattern 1) plus whatever new local state it needs for D-02's library dropdown and D-08's free-space line (not passed down from the parent — these are Steam-only concerns, fetched inside the new dialog itself).

**Core install-confirm pattern to adapt (NOT copy verbatim — D-14 diverges)** (lines 56-86):
```typescript
const handleInstall = useCallback(async () => {
  // Write Default game config with prefix on linux
  if (!isWin) {
    const gameSettings = await window.api.requestGameSettings(appName)
    if (wineVersion) {
      writeConfig({
        appName,
        config: {
          ...gameSettings,
          winePrefix,
          wineVersion,
          wineCrossoverBottle: crossoverBottle
        }
      })
    }
  }
  backdropClick()
  return install({
    gameInfo,
    previousProgress: progress,
    progress,
    installPath: 'thirdParty',
    isInstalling: false,
    t,
    platformToInstall,
    showDialogModal: () => backdropClick()
  })
}, [appName, t, winePrefix, wineVersion, crossoverBottle, platformToInstall])
```
**Divergence required (D-14):** replace the `writeConfig` call with `window.api.persistBottleWineVersion(wineVersion)` (new IPC, or reuse a bottle-adjacent handler per RESEARCH.md's Open Question #1) — write to `steamBottleConfigStore`, NOT per-game `GameConfig`. Also replace the generic `install()` helper call with `installSteamGame(appName, gameInfo, selectedLibraryPath)` from `src/frontend/state/InstallGameModal.ts` (NOT `frontend/helpers/library.ts`'s `install()` — that lacks Steam's hardcoded `platformToInstall`/DLC/SDL defaults). Only call `persistBottleWineVersion` when the wine section was actually shown (bottle-required or D-17 Windows-selected path).

**Header + footer chrome pattern — copy verbatim structure** (lines 88-144):
```typescript
return (
  <>
    <DialogHeader onClose={backdropClick}>
      {gameInfo.overrides?.title || gameInfo.title}
      {availablePlatforms.map((p) => (
        <FontAwesomeIcon className="InstallModal__platformIcon" icon={p.icon} key={p.value} />
      ))}
    </DialogHeader>
    <DialogContent>
      {/* Steam-specific notice/sections replace ThirdPartyDialog's anticheat+notice block */}
      {children}
    </DialogContent>
    <DialogFooter>
      <button className="button is-secondary" onClick={handleInstall} disabled={/* D-12 busy state condition */}>
        {t('button.install')}
      </button>
    </DialogFooter>
  </>
)
```
Note `disabled={runner !== 'legendary'}` in the analog is ThirdPartyDialog-specific (dead-guard artifact) — do NOT copy that condition; the Steam dialog's Install button disables only per D-12's busy-state contract (which per UI-SPEC lives on the ORIGIN button before the dialog even opens, not inside this footer — verify against UI-SPEC's "D-12 Busy State Contract" section before wiring any `disabled` here).

---

### `src/frontend/screens/Library/components/InstallModal/index.tsx` (component, request-response) — add 5th branch

**Analog:** itself — the existing 4-branch pattern

**Imports pattern already in file** (lines 1-29): includes `ThirdPartyDialog`, `ImportDialog`, `SideloadDialog`, `DownloadDialog`, `WineSelector`, `SelectField`, `closeInstallGameModal`/`useInstallGameModal` from `frontend/state/InstallGameModal`. Add `import SteamDialog from './SteamDialog'`.

**Shared state owned by the parent** (lines 49-52, 104-105):
```typescript
const [winePrefix, setWinePrefix] = useState('...')
const [wineVersion, setWineVersion] = useState<WineInstallation>()
const [wineVersionList, setWineVersionList] = useState<WineInstallation[]>([])
const [crossoverBottle, setCrossoverBottle] = useState('')
...
const [platformToInstall, setPlatformToInstall] = useState<InstallPlatform>(getDefaultplatform())
```
The Steam dialog reuses all of these unchanged — no new top-level state needed in `InstallModal/index.tsx` itself for D-02/D-08 (those live inside `SteamDialog`).

**`platformSelection()` — the exact D-03/D-17/D-18/D-19 guard to modify** (lines 129-158):
```typescript
function platformSelection() {
  const showPlatformSelection = availablePlatforms.length > 1
  if (!showPlatformSelection) {
    return null
  }
  const disabledPlatformSelection = Boolean(runner === 'sideload' && appName)
  return (
    <SelectField
      label={`${t('game.platform', 'Select Platform Version to Install')}:`}
      htmlId="platformPick"
      value={platformToInstall}
      disabled={disabledPlatformSelection}
      onChange={(e) => setPlatformToInstall(e.target.value as InstallPlatform)}
    >
      {availablePlatforms.map((p, i) => (
        <MenuItem value={p.value} key={i}>
          <Box sx={{ display: 'flex', placeItems: 'center' }}>
            <SvgIcon sx={{ marginInlineEnd: 1 }}>
              <FontAwesomeIcon icon={p.icon} />
            </SvgIcon>
            {p.name}
          </Box>
        </MenuItem>
      ))}
    </SelectField>
  )
}
```
`showPlatformSelection` must change for Steam per D-03/D-17/D-18/D-19 (Section-Gating Matrix): render read-only on Windows hosts (D-19) even with 1 platform, render SELECTABLE on macOS for a mac-native game with Windows-depot available (D-17), never render on Linux (D-18). This is the single highest-value line to get right — UI-SPEC's Interaction Contract section (D-17 Platform-Switch) governs the selectable case's wiring, which reuses `setPlatformToInstall` verbatim (no new event plumbing per UI-SPEC line 241).

**`hasWine` gate and `<WineSelector>` mount pattern** (line 107, and every sibling branch e.g. lines 186-199):
```typescript
const hasWine = platformToInstall === 'Windows' && !isWin
...
{hasWine ? (
  <WineSelector
    appName={appName}
    winePrefix={winePrefix}
    wineVersion={wineVersion}
    wineVersionList={wineVersionList}
    title={gameInfo?.title}
    setWinePrefix={setWinePrefix}
    setWineVersion={setWineVersion}
    crossoverBottle={crossoverBottle}
    setCrossoverBottle={setCrossoverBottle}
    initiallyOpen
  />
) : null}
```
For Steam: add `runner="steam"` (already an optional prop, drives both the existing `isKnownNotToWork` advisory AND the new D-16 CrossOver filter) plus the two new D-16/D-05 props (`crossoverOnly`/`bottleNameReadOnly` — naming per Claude's Discretion). Per UI-SPEC's Interaction Contract §3: this stays a plain conditional mount, **no height-transition animation** — snap only, matching every other sibling's existing `hasWine` toggle behavior.

**Sibling dialog branch pattern to replicate as the 5th branch** (lines 166-200, structure repeats for each of the 4 existing branches):
```typescript
<Dialog onClose={closeModal} showCloseButton className="InstallModal__dialog">
  {isThirdPartyManagedApp ? (
    <ThirdPartyDialog {...sharedProps}>
      {platformSelection()}
      {hasWine ? <WineSelector {...wineSelectorProps} /> : null}
    </ThirdPartyDialog>
  ) : isImportMode && showDownloadDialog ? (
    <ImportDialog {...sharedProps}>...</ImportDialog>
  ) : (
    /* ... DownloadDialog / SideloadDialog branches ... */
  )}
</Dialog>
```
Add `isSteamManagedApp` computed the same way as `isThirdPartyManagedApp` (line 161: `const isThirdPartyManagedApp = gameInfo && !!gameInfo.thirdPartyManagedApp`), e.g. `const isSteamManagedApp = runner === 'steam'`, and branch on it FIRST (before `isThirdPartyManagedApp`, since Steam games never carry `thirdPartyManagedApp`, ordering is defensive not load-bearing) or immediately after — RESEARCH.md confirms no branch-order conflict since Steam never reaches `isImportMode`/`showDownloadDialog` today via the `InstallGameModal.ts` chokepoint.

---

### `src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx` (component, request-response) — add D-16/D-05 props

**Analog:** itself, full file already read (235 lines)

**Existing optional-prop convention to extend (D-16/D-05 follow this exact shape)** (lines 19-47):
```typescript
type Props = {
  setWineVersion: (newVersion: WineInstallation) => void
  setWinePrefix: (newPrefix: string) => void
  setCrossoverBottle: React.Dispatch<React.SetStateAction<string>>
  winePrefix: string
  crossoverBottle: string
  wineVersionList: WineInstallation[]
  wineVersion: WineInstallation | undefined
  title?: string
  appName: string
  runner?: Runner
  initiallyOpen?: boolean
  sharedPrefixNote?: string
  hideSharedPrefixToggle?: boolean
}
```
Add e.g. `crossoverOnly?: boolean` and `bottleNameReadOnly?: boolean`, both optional/undefined-default so every existing caller (GOG/Epic/Amazon/sideload/guided-Steam-setup) is byte-for-byte unchanged — this is the SAME pattern `runner`/`hideSharedPrefixToggle`/`sharedPrefixNote` already used when Phase 17/19 added them.

**D-16 filter seam — the exact engine list consumption points to modify** (lines 160-179):
```typescript
<SelectField
  label={`${t('install.wineversion')}:`}
  htmlId="wineVersion"
  value={wineVersion?.name || ''}
  disabled={useSharedPrefix || wineVersionList.length === 0}
  onChange={(e) =>
    setWineVersion(
      wineVersionList.find((version) => version.name === e.target.value)!
    )
  }
>
  {wineVersionList &&
    wineVersionList.map((version, i) => (
      <MenuItem key={i} value={version.name}>
        <WineVersionListItem version={version} />
      </MenuItem>
    ))}
</SelectField>
```
Compute `const engineOptions = crossoverOnly ? wineVersionList.filter((w) => w.type === 'crossover') : wineVersionList` near the top of the component and substitute `engineOptions` for `wineVersionList` at BOTH the `disabled={... || wineVersionList.length === 0}` check and the `.map()` call. **Filter inside this component, gated on the existing `runner` prop or the new `crossoverOnly` prop — not in the parent** (`InstallModal/index.tsx`'s shared `useEffect`) — because `SteamBottleSetup.tsx` has its own independent `getAlternativeWine()` call and would not otherwise benefit (RESEARCH.md Pitfall 3).

**D-05 read-only bottle-name seam — the exact field to modify** (lines 150-158):
```typescript
{showBottle && (
  <TextInputField
    label={t('setting.winecrossoverbottle', 'CrossOver Bottle')}
    htmlId="crossoverBottle"
    value={crossoverBottle}
    onChange={(newValue) => setCrossoverBottle(newValue)}
    disabled={useSharedPrefix}
  />
)}
```
Change `disabled={useSharedPrefix}` to `disabled={useSharedPrefix || bottleNameReadOnly}`. Add the D-05 helper text (`gamelib:steam.install.bottleNameReadonlyHelper`) directly below, in the `.smallInputInfo` shape (see D-08 pattern below), rendered only `{bottleNameReadOnly && <span className="smallInputInfo">...</span>}`.

**`showBottle` derivation — reuse unchanged** (line 116):
```typescript
const showBottle = wineVersion?.type === 'crossover'
```
This already correctly hides the whole bottle-name block whenever CrossOver isn't the selected engine — with D-16's filter applied, for Steam this becomes always-true once an engine is selected (since only CrossOver engines are offered), so no additional gating logic needed here.

**`runner`-gated existing advisory — do not disturb, reuse as the placement precedent for new Steam-only branches** (lines 118-130, 180-196):
```typescript
const isKnownNotToWork =
  platform === 'darwin' &&
  runner === 'steam' &&
  showBottle &&
  typeof crossoverRating === 'number' &&
  crossoverRating <= 2
...
{isKnownNotToWork && (
  <div className="infoBox">
    <FontAwesomeIcon icon={faWarning} style={{ color: 'var(--status-danger)' }} />
    <Trans i18n={i18n} i18nKey="install.warn-crossover-wont-run" ns="gamepage">
      This game is known not to run well under CrossOver on macOS...
    </Trans>
  </div>
)}
```
This is the exact `.infoBox` shape UI-SPEC's D-14 shared-bottle notice must reuse — **but with `faWarning`'s DEFAULT/unstyled color, not `var(--status-danger)`** (UI-SPEC explicitly reserves the red for this pre-existing `isKnownNotToWork` warning only, so the two visually differ). Render the D-14 notice unconditionally whenever the wine section is visible (not behind a rating check like this one).

---

### `src/frontend/state/InstallGameModal.ts` (store/trigger logic, event-driven) — extend `startSteamInstall`

**Analog:** itself, full file already read (90 lines)

**Current trigger function to extend (D-10's unified predicate replaces this)** (lines 45-58):
```typescript
export const startSteamInstall = async (appName: string, gameInfo: GameInfo) => {
  const libraries = await window.api.listSteamLibraryTargets()
  if (libraries.length > 1) {
    useSteamInstallLocation.getState().open(appName, gameInfo, libraries)
    return
  }
  installSteamGame(appName, gameInfo)
}
```
Rewrite per UI-SPEC's trigger predicate (`opens = bottleRequired OR (nativeInstallOn AND libraryCount > 1) OR alwaysShowOn`): call `window.api.isSteamBottleEligible(appName)` (new IPC) alongside the existing `listSteamLibraryTargets()` call and a new always-show settings read, then either call `installSteamGame(appName, gameInfo)` directly (zero-friction, D-09 preserved) or open the NEW Steam dialog's own store/state (replacing `useSteamInstallLocation.getState().open(...)`) — not `useSteamInstallLocation`, which retires. Keep the function's `async` signature and standalone-testable shape (the existing doc comment at lines 45-50 explains exactly why it's extracted this way — preserve that structure).

**`installSteamGame` — reuse verbatim, unchanged** (lines 28-43):
```typescript
export const installSteamGame = (
  appName: string,
  gameInfo: GameInfo,
  path = ''
) => {
  void window.api.install({
    appName,
    path,
    runner: 'steam',
    installDlcs: [],
    sdlList: [],
    installLanguage: 'en-US',
    platformToInstall: 'Windows',
    gameInfo
  })
}
```
D-17 requires this to accept the user's platform override eventually (macOS→Windows), but CONTEXT.md's amendment scopes that as backend work on `SteamGame.install()` honoring `InstallArgs.platformToInstall` — verify with planning whether `installSteamGame`'s hardcoded `platformToInstall: 'Windows'` needs to become a parameter, since today it is Steam-only-ever-Windows.

**`openInstallGameModal` chokepoint — read-only reference, do not change signature** (lines 60-83):
```typescript
export const openInstallGameModal = ({ appName, runner, gameInfo, action = 'install' }: OpenInstallGameModalParams) => {
  if (runner === 'steam' && action === 'install' && gameInfo) {
    void startSteamInstall(appName, gameInfo)
    return
  }
  useInstallGameModal.setState({ isOpen: true, appName, runner, gameInfo, action })
}
```
All 7 call sites are unaffected (RESEARCH.md confirms) — only `startSteamInstall`'s internal body changes.

---

### `src/backend/storeManagers/steam/games.ts` (service, request-response) — add `checkBottleEligibility()`

**Analog:** the existing private predicate methods in the same class/file

**`isBottleEligible()` — the private predicate to wrap, do not widen visibility** (lines 1328-1340):
```typescript
private isBottleEligible(): boolean {
  if (!isMac) return false
  const meta = steamMetadataStore.get(this.appId)
  if (meta?.mac_arch === '32') return true
  return meta?.platformsCaptured === true && meta?.is_mac_native === false
}
```

**`ensurePlatformsCaptured()` — the private cold-cache guard to call first** (lines 1383-1391, truncated):
```typescript
private async ensurePlatformsCaptured(): Promise<void> {
  if (!isMac) return
  const alreadyCaptured = (): boolean =>
    steamMetadataStore.get(this.appId)?.platformsCaptured === true
  if (alreadyCaptured()) return
  await this.fetchMetadataIfNeeded(this.getGameInfo())
  // ... bounded poll up to METADATA_FETCH_TIMEOUT_MS (state.ts:30), 100ms increments
}
```

**`isBridgeEligible()` — the established compose-not-widen pattern to copy exactly** (lines 1353-1359):
```typescript
private isBridgeEligible(): boolean {
  return (
    this.isBottleEligible() &&
    bridgeAllowlist.has(this.appId) &&
    !bridgeFailedThisSession.has(this.appId)
  )
}
```

**`install()`'s own call sequence — the exact precedent for the new wrapper's body** (lines 701-702):
```typescript
await this.ensurePlatformsCaptured()
if (this.isBottleEligible()) {
```
New public method, alongside these, following the identical sequence:
```typescript
async checkBottleEligibility(): Promise<boolean> {
  await this.ensurePlatformsCaptured()
  return this.isBottleEligible()
}
```
This is the exact seam RESEARCH.md Pattern 3 recommends — avoid a third re-derivation (D-09's core anti-pattern) and avoid widening the two private methods' visibility.

---

### `src/backend/main.ts` / `src/backend/sidecar/steamAuthFlowRegistration.ts` (route, request-response) — register `isSteamBottleEligible`

**Analog (Electron):** the "macOS CrossOver bottle trio" block, `src/backend/main.ts` lines 922-934
```typescript
// Phase 17 (17-04): dedicated Steam CrossOver bottle provisioning + status.
// D-04: bottled-Steam auth stays opaque...
addHandler('steamBottleProvision', async (event, args) => provisionBottle(args))
addHandler('isSteamBottleProvisioned', async () => isBottleProvisioned())
addHandler('steamBottleStatus', async () => ({
  provisioned: steamBottleConfigStore.get_nodefault('provisioned') ?? false,
  bottleName:
    steamBottleConfigStore.get_nodefault('bottleName') ?? DEFAULT_STEAM_BOTTLE_NAME
}))
```
Add `addHandler('isSteamBottleEligible', async (event, appName) => new SteamGame(appName).checkBottleEligibility())` in this same block — matches `SteamLibraryManager.getGame(id)`'s own established `new SteamGame(id)` instantiation pattern (`library.ts:535-536`).

**Analog (Tauri sidecar):** the same trio, `src/backend/sidecar/steamAuthFlowRegistration.ts` lines 225-241
```typescript
ipcMain.handle('isSteamBottleProvisioned', async () => {
  return isBottleProvisioned()
})
ipcMain.handle('steamBottleStatus', async () => {
  return {
    provisioned: steamBottleConfigStore.get_nodefault('provisioned') ?? false,
    bottleName: steamBottleConfigStore.get_nodefault('bottleName') ?? DEFAULT_STEAM_BOTTLE_NAME
  }
})
```
Add the mirrored `ipcMain.handle('isSteamBottleEligible', async (_event, appName) => new SteamGame(appName).checkBottleEligibility())` here.

**IMPORTANT — placement correction (RESEARCH.md CONTRADICTION note):** CONTEXT.md's canonical_refs says this channel "must land on both Electron `main.ts` and the Tauri sidecar's `installFlowRegistration.ts`." That is corrected by research: `installFlowRegistration.ts` is scoped to install/uninstall/update-check (5 handlers, none bottle-related — confirmed by its own doc-comment). The bottle trio lives in `steamAuthFlowRegistration.ts`. Register the new channel there, matching `main.ts`'s own physical grouping — not in `installFlowRegistration.ts`.

**Dual-port precedent already in this codebase to confirm the mechanics against** (`listSteamLibraryTargets`, `installFlowRegistration.ts:189-190` vs `main.ts:913-914`):
```typescript
// Electron, main.ts:913-914
addHandler('listSteamLibraryTargets', async () =>
  isSteamNativeInstallEnabled() ? listSteamLibraryTargets() : []
)
// Tauri sidecar, installFlowRegistration.ts:189-190 — byte-identical shape
ipcMain.handle('listSteamLibraryTargets', async () =>
  isSteamNativeInstallEnabled() ? listSteamLibraryTargets() : []
)
```
Confirms the "one task, two file edits, byte-identical handler body" convention this repo already follows for every dual-ported Steam channel.

---

### ~~`src/frontend/screens/Settings/components/AlwaysShowSteamInstallForm.tsx` (NEW) + `src/backend/storeManagers/steam/alwaysShowInstallFormSetting.ts` (NEW) (D-13)~~ — **RETIRED, DO NOT BUILD**

> **🛑 SUPERSEDED by D-26 (2026-08-15). Neither file may be created.**
>
> **D-13 is RETIRED** — the global always-show setting is CUT, and plan `34.13-04`, which owned
> these two files, is **DELETED** (its number is a deliberate gap; nothing was renumbered). The
> caret's "Install with options…" puts the form one click away on every surface, which is what the
> setting existed to guarantee.
>
> The section below is **preserved only as a pattern reference** — the `EnableSteamNativeInstall`
> two-file idiom (`isDefault` gate → `useSetting` → `InfoIcon` → single backend read seam) is still
> the correct shape for *any* future Steam settings toggle. It is **not** an instruction to build
> one this phase. A planner that emits tasks from this section resurrects the exact work D-26 cut.
>
> Note also that D-20's expectation-setting copy, which had been parked in this setting's tooltip,
> lost its home when the setting was cut. It is **not** homeless — `34.13-UI-SPEC.md` resolves it
> onto the dialog itself via `ThirdPartyDialog`'s existing `.noticeIcon`/`.noticeInfo` slot.

**Analog (frontend):** `src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx` (44 lines, full file)
```typescript
import { useContext } from 'react'
import { useTranslation } from 'react-i18next'
import { ToggleSwitch } from 'frontend/components/UI'
import useSetting from 'frontend/hooks/useSetting'
import SettingsContext from '../SettingsContext'
import InfoIcon from 'frontend/components/UI/InfoIcon'

const EnableSteamNativeInstall = () => {
  const { t } = useTranslation()
  const { isDefault } = useContext(SettingsContext)
  const [enableSteamNativeInstall, setEnableSteamNativeInstall] = useSetting(
    'enableSteamNativeInstall',
    false
  )
  if (!isDefault) {
    return <></>
  }
  return (
    <div className="toggleRow">
      <ToggleSwitch
        title={t('setting.steam-native-install', 'Download Steam games directly in GameLib')}
        htmlId="enable-steam-native-install"
        handleChange={() => setEnableSteamNativeInstall(!enableSteamNativeInstall)}
        value={enableSteamNativeInstall}
      />
      <InfoIcon text={t('help.steam_native_install', '...')} />
    </div>
  )
}
export default EnableSteamNativeInstall
```
Copy this shape exactly for the new component, swapping the setting key to `alwaysShowSteamInstallForm`, the `htmlId`, and the copy per UI-SPEC's Copywriting Contract (`gamelib:settings.alwaysShowSteamInstallForm` / `gamelib:help.alwaysShowSteamInstallForm` — note the NEW namespace `gamelib:` vs. this analog's unnamespaced `useTranslation()`/`setting.*` keys; follow the standing localisation requirement, not this analog's older namespace convention).

**Analog (backend):** `src/backend/storeManagers/steam/nativeInstallSetting.ts` (17 lines, full file)
```typescript
/**
 * D-13 opt-in setting — the single backend read seam for whether GameLib is
 * allowed to depot-download Steam games in-process...
 */
import { GlobalConfig } from 'backend/config'

export function isSteamNativeInstallEnabled(): boolean {
  return GlobalConfig.get().getSettings().enableSteamNativeInstall ?? false
}
```
Copy verbatim shape for `alwaysShowInstallFormSetting.ts`, e.g. `export function shouldAlwaysShowSteamInstallForm(): boolean { return GlobalConfig.get().getSettings().alwaysShowSteamInstallForm ?? false }`. Per D-13, this is called from the RENDERER via `window.api.requestAppSettings()` (already used by `useSetting`), so no new IPC channel is needed purely for the setting read itself (RESEARCH.md confirms) — but note it needs a new `AppSettings` key added to the shared settings type/schema wherever `enableSteamNativeInstall` is declared (not located in the excerpts read this pass — planner must grep the `AppSettings`/`GlobalConfig` schema for that key's declaration site).

---

### `src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx` (component, request-response) — D-15 handoff wiring

**Analog:** itself, existing seeding effect

**Current seeding effect — the exact branch to extend** (lines 78-92):
```typescript
useEffect(() => {
  if (!globalConfig || wineVersion || !enginesFetched) return
  // Prefer CrossOver — the Steam bottle is created via CrossOver's cxbottle
  // (17-01/17-04), so CrossOver is the correct default even when the user's
  // global engine is plain Wine (17-06 UAT finding).
  setWineVersion(
    resolveSteamBottleEngine(globalConfig.wineVersion, wineVersionList)
  )
  setWinePrefix(globalConfig.defaultWinePrefixDir)
  // ALWAYS the dedicated Steam bottle, never the user's shared GOG/Epic
  // bottle (globalConfig.wineCrossoverBottle) — the two must stay distinct.
  setCrossoverBottle(DEFAULT_STEAM_BOTTLE_NAME)
}, [globalConfig, wineVersion, enginesFetched, wineVersionList])
```
Per RESEARCH.md Pattern 4 (the confirmed D-15 gap): this effect must be extended to prefer a persisted `wineVersion` (surfaced via the new `isSteamBottleEligible` verdict object or an extended `steamBottleStatus` shape — RESEARCH.md's Open Question #1) over `resolveSteamBottleEngine`'s derivation, when one exists. This is its own explicit task per the Common Pitfalls section — do not fold it into the D-14 store-write task.

**Engine-fetch effect — unaffected, reused as-is, but gains the D-16 filter automatically** (lines 65-76):
```typescript
useEffect(() => {
  if (!isOpen) return
  let cancelled = false
  void window.api.getAlternativeWine().then((list: WineInstallation[]) => {
    if (cancelled) return
    setWineVersionList(list)
    setEnginesFetched(true)
  })
  return () => { cancelled = true }
}, [isOpen])
```
This calls `getAlternativeWine()` independently of `InstallModal`'s own call — confirms why D-16's CrossOver filter MUST live inside `WineSelector` itself (this component already passes `runner="steam"` to `WineSelector` per RESEARCH.md, so the filter applies here automatically once implemented there, with zero changes needed in this file for D-16 specifically).

---

### `src/frontend/screens/Game/GamePage/components/SteamInstallLocationPicker.tsx` (RETIRE) — reference for the D-02 dropdown's markup shape

**Analog:** itself, full file already read (90 lines) — being retired, but its markup is the direct source for the D-02 library dropdown's content (re-skinned as `SelectField`/`MenuItem` per UI-SPEC's Reuse Boundaries, not the raw `<select>` below):
```typescript
<select
  id="steamInstallLocationSelect"
  className="steamInstallLocationSelect"
  value={selectedPath}
  onChange={(e) => setSelectedPath(e.target.value)}
>
  {libraries.map((lib) => (
    <option key={lib.path} value={lib.path}>
      {lib.isPrimary
        ? `${lib.path} (${t('install.steam-location.primary', 'default')})`
        : lib.path}
    </option>
  ))}
</select>
```
```typescript
const handleConfirm = () => {
  close()
  installSteamGame(appName, gameInfo, selectedPath)
}
```
The confirm-handler shape (`installSteamGame(appName, gameInfo, selectedPath)`) is the exact call the new SteamDialog's library-dropdown confirm path must reuse unchanged (RESEARCH.md Open Question #2 — direct reuse, not a redesign).

**Full reference census for retirement (4 files, RESEARCH.md "Retiring SteamInstallLocationPicker" section):**
1. `SteamInstallLocationPicker.tsx` itself — delete.
2. `src/frontend/state/SteamInstallLocation.ts` (its zustand store `useSteamInstallLocation`, type `SteamLibraryOption`) — delete.
3. `src/frontend/state/InstallGameModal.ts:3,51-58` — remove the import, rewrite `startSteamInstall` (see above).
4. `src/frontend/App.tsx:34,142` — remove the `<SteamInstallLocationPicker />` mount and its import; no replacement mount needed (the new dialog is already reachable via `InstallModal`).

---

### D-08 Free-space line pattern

**Analog:** `src/frontend/screens/Library/components/InstallModal/DownloadDialog/index.tsx` lines 663-684 (`.smallInputInfo` shape, `checkDiskSpace` call at line 459)
```typescript
afterInput={
  downloadSize ? (
    <span className="smallInputInfo">
      {validPath && validFlatpakPath && (
        <>
          <span>{`${t('install.disk-space-left', 'Space Available')}: `}</span>
          <span><strong>{`${message}`}</strong></span>
          {!notEnoughDiskSpace && (
            <span>{` - ${t('install.space-after-install', 'After Install')}...`}</span>
          )}
        </>
      )}
    </span>
  ) : null
}
```
For Steam's D-08 (free-space ONLY, no "after install" projection, no not-enough-space blocking): strip to just the first `<span>`/`<strong>{message}</strong>` pair, drop the `!notEnoughDiskSpace` branch entirely. Source the value from `window.api.checkDiskSpace(path)` (`DiskSpaceData` shape, `common/types.ts:710-716`, only `.free`/`.message` consumed) called on the currently-selected library path from the D-02 dropdown.

---

## Shared Patterns

### Dual-runtime IPC registration (Electron + Tauri sidecar)
**Source:** `src/backend/main.ts` lines 922-934 ↔ `src/backend/sidecar/steamAuthFlowRegistration.ts` lines 225-241 (identical handler bodies, different registration mechanism: `addHandler(...)` vs `ipcMain.handle(...)`)
**Apply to:** the new `isSteamBottleEligible` channel — MUST land in both files as one atomic change (RESEARCH.md Pitfall 2 — a channel registered on only one runtime silently hangs the D-12 busy state under Tauri specifically). Verify against `.planning/IPC-PORT-INVENTORY.md` and the preload surface (`src/preload/api/steam.ts` or equivalent) as part of the same task.

### Settings toggle two-file pattern (frontend component + backend accessor)
**Source:** `EnableSteamNativeInstall.tsx` ↔ `nativeInstallSetting.ts`
**Apply to:** the new always-show setting (D-13) — `isDefault`-gated UI, single `useSetting` hook, single `GlobalConfig`-reading backend accessor, ONE read seam (`startSteamInstall` in `InstallGameModal.ts`).

### Sibling-dialog prop contract
**Source:** `ThirdPartyDialog/index.tsx` lines 24-35 (shared by all 4 existing siblings)
**Apply to:** `SteamDialog` — identical prop shape received from `InstallModal/index.tsx`; do not invent a divergent contract.

### `.infoBox` warning/notice shape
**Source:** `WineSelector/index.tsx` lines 180-196 (danger-red `isKnownNotToWork` advisory) and lines 214-228 (default-color shared-prefix caution) — TWO existing severities already coexist here
**Apply to:** the D-14 shared-bottle notice — MUST use the default/unstyled `faWarning` color (matching the shared-prefix caution's shape, not the red `isKnownNotToWork` advisory's), per UI-SPEC's explicit instruction that these must never look identical.

### Optional-prop additive extension (never break existing callers)
**Source:** `WineSelector`'s own `runner`/`initiallyOpen`/`sharedPrefixNote`/`hideSharedPrefixToggle` props — each added at a different phase, always optional/undefined-default
**Apply to:** the new `crossoverOnly`/`bottleNameReadOnly` props (D-16/D-05) — same convention, zero impact on GOG/Epic/Amazon/sideload/guided-Steam-setup callers.

### Compose-a-private-predicate-via-one-public-wrapper (never widen visibility)
**Source:** `SteamGame.isBridgeEligible()` (`games.ts:1353-1359`) composing `isBottleEligible()`
**Apply to:** the new `checkBottleEligibility()` wrapper composing `ensurePlatformsCaptured()` + `isBottleEligible()` — same class, same file, same pattern, third instance not a new one.

## No Analog Found

None — every file this phase touches has a strong, verified in-repo analog. This phase is explicitly scoped by CONTEXT.md/RESEARCH.md as pure composition of existing primitives (see RESEARCH.md "Don't Hand-Roll" table); no new architectural pattern is introduced.

## Metadata

**Analog search scope:** `src/frontend/screens/Library/components/InstallModal/**`, `src/frontend/screens/Settings/components/**`, `src/frontend/screens/Game/GamePage/components/**`, `src/frontend/state/**`, `src/backend/storeManagers/steam/**`, `src/backend/main.ts`, `src/backend/sidecar/*FlowRegistration.ts`
**Files scanned:** 13 direct reads (full or targeted-range) this session, plus RESEARCH.md's own 20 primary-source reads (reused, not re-read, per anti-duplication rule) — combined ~30 source files
**Pattern extraction date:** 2026-08-14
**Note on RESEARCH.md overlap:** RESEARCH.md already performed exhaustive `file:line`-level verification for this phase (its own confidence rating: HIGH, every claim checked against source). This PATTERNS.md file adds the concrete, copy-pasteable code excerpts RESEARCH.md's prose describes, re-verified against direct reads in this session (imports blocks, exact prop interfaces, exact IPC handler bodies) rather than re-deriving RESEARCH.md's architectural conclusions.
