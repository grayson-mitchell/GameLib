---
phase: 34.13-steam-install-time-wine-bottle-form-gog-parity
verified: 2026-08-28T00:00:00Z
status: human_needed
score: 27/27 code-level D-NN truths verified; 8 manual UAT rows NOT ATTEMPTED, 2 OBSERVE-only (undecided by design) remain open for this phase
overrides_applied: 0
human_verification:
  - test: "G-QUICK-MAC-MULTILIB (tauri) — quick-install primary-half click on a 64-bit mac-native Steam title, visible in the library, with a Windows-depot-having sibling that must NOT be force-routed to the bottle"
    expected: "Install proceeds end-to-end, files land in the primary Steam library, no dialog/picker interrupts the click"
    why_human: "No fixture on the verifying host satisfies the precondition: 32-bit mac titles route to the CrossOver bottle by design (correct, not a defect), one candidate title's depots fail to decode deterministically (filed separately, not 34.13), and two other 64-bit candidates are invisible in the library due to an unrelated 22-game filter gap. NOT ATTEMPTED per 34.13-UAT.md line 421."
  - test: "G-ROW-2/3-noWin (electron and tauri) — options-dialog platform row for a mac-native Steam title that has NO Windows depot at all (distinct from 'depot signal not yet fetched')"
    expected: "Platform row renders read-only 'macOS' (not selectable), per D-19/D-17's no-Windows-depot branch"
    why_human: "No such title exists in the developer's Steam library — fixture cannot be constructed without owning a qualifying game. NOT ATTEMPTED per 34.13-UAT.md lines 449-450."
  - test: "G-HANDOFF-D15 (electron and tauri) — after a D-17 forced Windows-via-bottle install, open the guided wine-version setup and confirm it seeds from the persisted `persistBottleWineVersion()` choice rather than re-deriving a default"
    expected: "Guided setup's wine-version field pre-fills with the value written by the install-time form, not a fresh default"
    why_human: "Requires selecting a NON-FIRST CrossOver engine, which requires >=2 installed CrossOver.app bundles. Only ONE CrossOver.app bundle exists on the verifying host (Wine Manager 'wine'-typed builds do not qualify — `getCrossover()` in `utils/compatibility_layers.ts:308` only enumerates `com.codeweavers.CrossOver` app bundles). Re-diagnosed 2026-08-28; the previously recorded blocker text (shared-bottle deletion) was wrong twice over. Needs a second CrossOver.app installed at a distinct path. NOT ATTEMPTED per 34.13-UAT.md lines 528-529."
  - test: "G-HANDOFF-LAUNCH (electron) — after a D-17 forced install, launch the game and confirm the Steam log shows dispatch via the bottled Steam client, not a host `steam://rungameid` call"
    expected: "Launch routes through the bottle; settings screen shows the bottle's wine version and `GameLibSteam` name; pre-launch native-check runs false"
    why_human: "Operator decision at 34.13 close-out (2026-08-28) to skip the three electron handoff rows. The tauri half of this exact chain is PASS and both runtimes share one backend (`storeManagers/steam/games.ts`) for install/launch/uninstall/isBottleEligible, so what remains unverified is specifically transport-level divergence between the Electron and Tauri IPC layers on this chain — not the underlying business logic, which is source-verified. NOT ATTEMPTED per 34.13-UAT.md line 539."
  - test: "G-HANDOFF-UNINSTALL (electron) — uninstall a D-17-forced bottle install and confirm both the on-disk bottle directory and its `appmanifest_<appId>.acf` are removed, and the badge flips without app restart"
    expected: "Uninstall delegates to the bottled client, on-disk artifacts are both removed, UI updates live"
    why_human: "Same operator close-out decision and same transport-level-divergence-only risk as G-HANDOFF-LAUNCH. NOT ATTEMPTED per 34.13-UAT.md line 549."
  - test: "G-HANDOFF-REINSTALL (electron) — after uninstalling a D-17-forced bottle install, reinstall and confirm the platform row now offers macOS pre-selected/native path rather than re-forcing the bottle"
    expected: "Platform row is selectable, macOS pre-selected, no wine section while macOS is selected; launches natively via host Steam client"
    why_human: "Same operator close-out decision and same transport-level-divergence-only risk as G-HANDOFF-LAUNCH/UNINSTALL. NOT ATTEMPTED per 34.13-UAT.md line 561."
  - test: "G-D24-ONLYLIB (electron and tauri) — quick install when the user's ONLY Steam library is unreachable (root exists, no `steamapps/`, libraryCount == 1)"
    expected: "Undecided by design — recorded as an OBSERVE-only row, no pass/fail verdict is being requested. What was observed: notice text renders correctly and matches the byte-exact catalog string on the tauri half; on both runtimes Install remained ENABLED and clicking it proceeded to create the missing `steamapps/` directory and start a download rather than blocking, which may or may not be the intended behavior for this corner (the notice says 'reconnect and retry' while the button silently succeeds by fabricating the path)."
    why_human: "This is a genuine product-behavior decision (should Install stay enabled when the notice says the library is unreachable?), not a code-correctness question a grep/read can resolve. 34.13-UAT.md explicitly declines to score it and defers the decision. Lines 492-493."
gaps: []
deferred:
  - truth: "D-21/D-29 gamepad reachability of the caret ('Install with options…') and the Console Mode A-button listener, physically pressed on a real controller"
    addressed_in: "Phase 38"
    evidence: "34.13-UAT.md's human_verification_relocated frontmatter lists 18 rows moved to Phase 38 with two-way receipts; 38-VERIFICATION.md contains matching origin_phase: \"34.13\" / origin_item entries (including 38-C07/38-C08 for the gamepad rows) confirming the relocation receipt is intact, not rotted. Phase 38's own status is human_needed — it has not yet closed these rows, but that is Phase 38's open item, not a 34.13 gap, per 34.13-UAT.md's own ground rule 3 ('RELOCATED does NOT hold this phase open')."
  - truth: "16 Windows/Linux-host UAT rows (native platform behavior on non-macOS hosts)"
    addressed_in: "Phase 38"
    evidence: "Same relocation mechanism as above — these are the 'OTHER-HOST BATCH' rows explicitly relocated because no Windows/Linux host was available to the 34.13 verifier/developer."
---

# Phase 34.13: Steam install-time wine/bottle form (GOG parity) Verification Report

**Phase Goal (verbatim, ROADMAP.md:1663):** "A Steam install offers a one-click quick install by default, targeting Steam's primary library, with an explicit 'Install with options…' path — reachable via a caret or context-menu item — that opens GameLib's install modal for platform selection and wine/bottle choice. This gives Steam more control than GOG's always-modal default, not parity with it, while a bottle install still degrades safely into the options path when quick install's local checks fail."

**Verified:** 2026-08-28
**Status:** human_needed
**Re-verification:** No — initial verification

**Requirement-ID namespace:** `.planning/REQUIREMENTS.md` has zero entries mapped to this phase. Per ROADMAP.md:1665 ("Requirements: none minted — coverage is tracked against the `D-NN` decisions in `34.13-CONTEXT.md`"), this report treats **D-01 through D-29** (with D-12 superseded by D-25 and D-13 explicitly RETIRED by D-26) as the requirement-traceability namespace for this phase.

## Note on scope: title/goal staleness (informational, not a gap)

`34.13-CONTEXT.md:1665-1667` flags, in the phase's own words, that the "(GOG parity)" framing in both the ROADMAP heading and this phase's directory slug is **superseded by D-28**: the phase does not reach GOG parity, it *overshoots* it (Steam gets more automatic control than GOG's always-modal default, in the opposite direction). The document explicitly records that the heading/slug were left unchanged on purpose (renaming one without the other would desync phase lookup) and that "the phase title and goal in ROADMAP.md carry the superseded framing and must be rewritten." I verified the goal text quoted above is byte-identical to what's still live in ROADMAP.md today — the staleness is acknowledged and tracked in-project, not hidden. Scored as informational, not a BLOCKER or WARNING against this phase's own delivery.

## Goal Achievement

### Observable Truths (D-NN decision coverage)

All 27 scoreable decisions were verified by reading the actual implementation source (file:line evidence below), not accepted from any SUMMARY.md. D-12 and D-13 are formally superseded/retired and are not independently scored (D-12's substance is carried by D-25; D-13 is confirmed absent from source).

| # | Truth (D-NN) | Status | Evidence |
|---|---|---|---|
| D-01 | Steam gets a new, lean sibling dialog inside `InstallModal` (5th branch) | VERIFIED | `src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx` exists, substantive (500+ lines), wired as the 5th branch in `InstallModal/index.tsx` |
| D-02 | Steam library location folds into the form as a dropdown | VERIFIED | `installTarget.ts` + `steamSectionGating.ts` drive `gating.libraryDropdown`; `SteamDialog/index.tsx:452` keys the dropdown render on it |
| D-03 | Platform row renders even with only one platform available | VERIFIED | `steamPlatformRow.ts` / `steamSectionGating.ts`, amended in substance by D-17/D-18/D-19 |
| D-04 | `SteamInstallLocationPicker` retires; form becomes the single Steam install surface | VERIFIED | `find src -iname "*SteamInstallLocationPicker*"` returns zero files; deletion commit `a2ba207ff` confirmed in git log |
| D-05 | CrossOver bottle name field shown prefilled, read-only | VERIFIED | `WineSelector` Steam-aware branch, `engineFilter.ts` tests |
| D-06 | No download/install-size row for Steam; never gated on disk size | VERIFIED | `SteamDialog/index.tsx:545` has exactly one JSX `disabled=` term (`eligibilityPending \|\| submitting`); `__tests__/steamDialogSource.test.ts` (91 tests, all PASS) asserts no SIZE token (diskSize/spaceLeftAfter/notEnoughDiskSpace/getInstallInfo) appears anywhere in the file. The `submitting` addition (from D-25) is orthogonal to disk-size gating — confirmed not a violation |
| D-07 | `SteamLibraryManager.getInstallInfo()` stays a stub | VERIFIED | Never called from `SteamDialog/index.tsx` per the same source-text gate |
| D-08 | Disk info is free-space-only, no "after install" math | VERIFIED | `installTarget.ts` free-space line |
| D-09 | New backend-authoritative IPC `isSteamBottleEligible(appName)`, dual-registered | VERIFIED | `src/backend/main.ts:944` (Electron), `src/backend/sidecar/steamAuthFlowRegistration.ts:302` (Tauri sidecar), typed in `src/common/types/ipc.ts:301`, exposed via `src/preload/api/steam.ts:23` |
| D-10 | Two-condition trigger table (bottle required / >1 library) — amended by D-22; insight absorbed into section-relevance gating | VERIFIED (absorbed) | `steamSectionGating.ts` lines 102, 114, 185-199, 259-297 implement `resolveSteamSectionGating()` using exactly the bottleRequired/libraryCount>1 predicates D-10 defined; not separately cited in any plan's `requirements:` field but genuinely implemented, not orphaned |
| D-11 | Section relevance (dropdown/wine section only render when relevant) survives in substance, reached by user intent instead of a setting | VERIFIED | Same `steamSectionGating.ts` module |
| D-12 | Superseded by D-25 ("This amends D-12 and replaces `34.13-UI-SPEC.md`'s 'D-12 Busy State Contract'") | SUPERSEDED — not independently scored | `34.13-CONTEXT.md:277` |
| D-13 | RETIRED by D-26 — no always-show global setting is built | VERIFIED absent | `settings`/`help.alwaysShowSteamInstallForm` identifiers confirmed absent from source; plan `34.13-04` is a permanent, deliberate gap on disk |
| D-14 | Chosen wine version written via `persistBottleWineVersion()` into `steamBottleConfigStore` | VERIFIED | `SteamDialog/index.tsx:323`: `const persisted = window.api.persistBottleWineVersion(wineVersion)` |
| D-15 | Form feeds the guided post-install setup; does not replace it | VERIFIED (code) / UNVERIFIED (live cross-process handoff) | `steamBottleDefaults.ts`: `resolveSteamBottleSeedEngine` checks `isUsablePersistedEngine` and returns the persisted value before calling `resolveSteamBottleEngine`. **Live end-to-end handoff is a human-verification item** — see below |
| D-16 | Steam's `WineSelector` filters to CrossOver-type engines only | VERIFIED | `engineFilter.ts` + dedicated tests |
| D-17 | macOS platform row selectable (not read-only); mac-native game can be forced to Windows-via-bottle | VERIFIED | `src/backend/storeManagers/steam/games.ts` lines 191, 225, 250-253, 795, 835-836, 931-997, 1874, 2234-2235, 2491-2492, 2683 wire `forcedWindowsViaBottle` / `steamForceWindowsViaBottle` end to end — substantive, not a stub |
| D-18 | On Linux, platform row does not render | VERIFIED | `steamSectionGating.ts` / `steamPlatformRow.ts` |
| D-19 | On Windows, platform row is read-only | VERIFIED | same module |
| D-20 | "Content-light" form (no size/no picker) accepted as-is | VERIFIED | `contentLightNotice` / `contentLightSingleLibraryNotice` catalog keys wired |
| D-21 | Install control becomes a split button (primary = quick install, caret = "Install with options…") | VERIFIED (code) / UNVERIFIED (physical gamepad reach) | `MainButton.tsx` (Dropdown primitive + `openSteamInstallOptions`), `GameCard/index.tsx` (context-menu item). Gamepad reachability of the caret is a Phase-38-relocated item (38-C07/C08), receipts confirmed intact |
| D-22 | Auto-open triggers retired; the user is the trigger | VERIFIED | `steamSectionGating.ts` has no "opens" computation; `InstallGameModal.ts` no longer auto-routes Steam installs through the form |
| D-23 | Quick install always targets Steam's primary library | VERIFIED | `resolveQuickInstallTarget` |
| D-24 | Quick install degrades into the options dialog on missing/full local library check | VERIFIED (code + live) | `evaluateQuickInstallTarget` / degrade logic; **live-tested PASS** in UAT (`G-D24-DEGRADE`, both runtimes). The ≤1-library corner (`G-D24-ONLYLIB`) is OBSERVE-only, undecided by design — see Human Verification |
| D-25 | Options dialog opens instantly; wine section carries eligibility loading state; Install button disabled until resolved | VERIFIED | `EligibilityLoadingRow.tsx`; `SteamDialog/index.tsx:545` `disabled={eligibilityPending \|\| submitting}` |
| D-26 | D-13's always-show setting cut; plan `34.13-04` deleted | VERIFIED | No `AppSettings.alwaysShowSteamInstallForm` anywhere in source; plan 04 absent from disk, permanent gap documented |
| D-27 | Six-call-site entry-point map (2 split, 4 fixed) | VERIFIED | All six sites independently grepped and confirmed: `MainButton.tsx` (split), `GameCard/index.tsx` (split), `Library/index.tsx:504` (fixed quick), `Game/GamePage/index.tsx:345` (fixed quick), `GameSubMenu/index.tsx:209,371` (fixed options, relabeled), `DownloadDialog/index.tsx:305` (fixed options) |
| D-28 | Quick install is Steam-only this phase; GOG/Epic/Amazon unaffected | VERIFIED | No changes found to GOG/Epic/Amazon install-modal call paths; scope confirmed narrow |
| D-29 | Console Mode gets D-24's local-validity check, but no options path; gamepad UI is a 9th entry point outside D-27's map | VERIFIED (code) / UNVERIFIED (physical A-button press) | `src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx:323`: `useGamepadButtonPress(getActionButtonIndex(layout), ...)` calls `installGame()` through the same `resolveConsoleActionIntent` logic as the Enter-key handler — genuinely wired, not a stub. Physical controller press untested on this host (no controller attached); same coverage class as D-21, relocated to Phase 38 |

**Score:** 27/27 scoreable D-NN decisions VERIFIED at the source-code level. Two decisions (D-12, D-13) are formally superseded/retired and correctly absent, not separately scored.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx` | Steam sibling dialog (D-01) | VERIFIED | Substantive, wired as 5th InstallModal branch, single legitimate `disabled=` term confirmed by source-text gate |
| `src/frontend/screens/Library/components/InstallModal/steamSectionGating.ts` | Pure section-visibility computation (D-10/D-11 absorption) | VERIFIED | `resolveSteamSectionGating()` implements the bottleRequired/libraryCount predicates |
| `src/backend/storeManagers/steam/games.ts` | D-17 forced-Windows-via-bottle override, install/launch/uninstall handoff | VERIFIED | `forcedWindowsViaBottle`/`steamForceWindowsViaBottle` wired at 10 call sites |
| `src/backend/main.ts`, `src/backend/sidecar/steamAuthFlowRegistration.ts` | Dual-runtime `isSteamBottleEligible` IPC registration | VERIFIED | Registered on both Electron and Tauri sidecar |
| `src/preload/api/steam.ts`, `src/common/types/ipc.ts` | Typed preload exposure of the new IPC | VERIFIED | Line 23 / line 301 respectively |
| `src/frontend/screens/Game/GamePage/components/steamBottleDefaults.ts` | D-15 read side (guided setup seeds from persisted choice) | VERIFIED | `resolveSteamBottleSeedEngine` returns persisted value verbatim when usable |
| `src/frontend/screens/ConsoleMode/InstallOverlay/index.tsx` | D-29 gamepad A-button listener + D-24 check | VERIFIED | `useGamepadButtonPress` wired to the same intent-resolution logic as Enter |
| `public/locales/en/gamelib.json` | 11-key localisation catalog (D-06/D-20) | VERIFIED | Independently dumped via `python3 -c "import json..."`; all 11 K-keys present byte-identical to plan's table, plus 3 extra Notice variant keys resolving a documented copy defect |
| `src/frontend/screens/Library/components/GameCard/index.tsx`, `.../Game/GamePage/components/MainButton.tsx` | D-21/D-27 split-button entry points | VERIFIED | Both use the `Dropdown` primitive; caret routes to `openSteamInstallOptions` |
| `find src -iname "*SteamInstallLocationPicker*"` (should not exist) | D-04 retirement | VERIFIED | Zero results; deletion commit `a2ba207ff` confirmed |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `SteamDialog` submit | `persistBottleWineVersion` IPC | `window.api.persistBottleWineVersion(wineVersion)` | WIRED | `SteamDialog/index.tsx:323` |
| `steamBottleDefaults.ts` | persisted `steamBottleConfigStore` value | `resolveSteamBottleSeedEngine` → `isUsablePersistedEngine` | WIRED (code) | Live cross-process confirmation is a human-verification item (G-HANDOFF-D15, NOT ATTEMPTED both runtimes) |
| `games.ts` install/launch/uninstall | `forcedWindowsViaBottle` verdict | Read at 10 call sites incl. `install()`, launch dispatch, uninstall dispatch | WIRED | Confirmed by direct source read, not SUMMARY |
| `MainButton.tsx` caret | `SteamDialog` (options path) | `Dropdown` primitive → `openSteamInstallOptions` | WIRED | Electron/Tauri renderer path confirmed; gamepad-driven activation of the same caret is UNVERIFIED live (relocated to Phase 38) |
| `InstallOverlay` A-button listener | `installGame()` | `useGamepadButtonPress` → `resolveConsoleActionIntent` (same function the Enter-key handler calls) | WIRED (code) | Enter-equivalent path live-tested PASS (`G-D29-CONSOLE`); physical gamepad press untested, no controller on host |
| `main.ts` / `steamAuthFlowRegistration.ts` | `isSteamBottleEligible` handler | IPC channel registration, both runtimes | WIRED | Confirmed dual-registered by direct grep of both files |

### Requirements Coverage

All 29 D-NN decisions accounted for above (27 scored VERIFIED, D-12 superseded by D-25, D-13 retired by D-26 and confirmed absent). No orphaned decisions found — D-10, which is not cited in any plan's `requirements:` frontmatter field, was independently confirmed to be substantively implemented (absorbed into D-11/D-22's user-intent-driven gating) rather than dropped.

### Anti-Patterns Found

None. `pnpm eslint` run against the full 57-file phase-touched-file list (compiled from all 14 plans' SUMMARY.md `key-files` frontmatter) returns **zero errors** (`--quiet` scope), including the two errors previously known in `installFormIpc.ts` — confirmed fixed by commit `10cc05e76`. Only pre-existing repo-wide warnings remain, none introduced by this phase. Debt-marker scan (TBD/FIXME/XXX) on the same file set: none found.

### Requirements/Documentation Consistency Notes (informational, not blockers)

1. **"Six" vs "eleven" localisation keys.** `34.13-13-PLAN.md`'s Task 1 heading correctly says "the eleven live gamelib: keys," but its `must_haves.truths` (line 33) and several Step-4/acceptance-criteria lines say "six" — stale text carried over from an earlier plan revision written before D-21..D-29 expanded scope from 6 to 11 keys. Independently confirmed all 11 keys are genuinely present and byte-identical in `gamelib.json` and at every call site. This is a documentation drift inside the plan file, not a functional gap.
2. **Phase title/slug staleness (D-28).** Covered above — acknowledged in-project, not hidden, informational only.

### Human Verification Required

Eight UAT rows are formally `NOT ATTEMPTED` and two are `OBSERVED (not scored — undecided by design)`. None of these represent a code-level defect — every underlying code path they would exercise was independently confirmed wired and substantive by direct source read (see Observable Truths above for D-15, D-17-handoff, D-21, D-24, D-29). They represent **manual-verification-coverage gaps**: fixtures the verifying host cannot construct (only one CrossOver.app bundle, no qualifying no-Windows-depot title, no gamepad controller) or explicit operator decisions to stop paying for redundant transport-only coverage at close-out. Per `34.13-UAT.md`'s own ground rule 4 ("Any FAIL or NOT ATTEMPTED keeps the phase `human_needed`... never rolled up into `gaps_found`"), and because no formal override record (`accepted_by`/`accepted_at`) exists for the operator's 2026-08-28 close-out decision — only a git/ledger note — this phase's status is `human_needed`, not `passed`. Full detail and reproduction instructions for each of the following items are in `34.13-UAT.md`; a condensed version is in this report's YAML frontmatter `human_verification` block.

1. G-QUICK-MAC-MULTILIB (tauri) — 64-bit mac-native quick-install fixture unavailable on this host.
2. G-ROW-2/3-noWin (electron + tauri) — no owned title without a Windows depot to construct the fixture.
3. G-HANDOFF-D15 (electron + tauri) — needs a second CrossOver.app bundle at a distinct path; only one exists on this host.
4. G-HANDOFF-LAUNCH (electron) — operator close-out decision; tauri half PASS, electron-transport divergence risk accepted not overlooked.
5. G-HANDOFF-UNINSTALL (electron) — same as above.
6. G-HANDOFF-REINSTALL (electron) — same as above.
7. G-D24-ONLYLIB (electron + tauri) — genuinely undecided product behavior (should Install stay enabled when the sole library is unreachable?), not a code-correctness question.

Two related items were relocated (not left open) to Phase 38 with an intact two-way receipt, confirmed by grepping matching `origin_phase: "34.13"` entries in `38-VERIFICATION.md` against `34.13-UAT.md`'s `human_verification_relocated` frontmatter — these do **not** hold this phase open per its own ground rule 3, and are listed under `deferred` in this report's frontmatter rather than `human_verification`:
- D-21/D-29 gamepad-controller reachability (38-C07/38-C08).
- 16 Windows/Linux-host rows requiring a non-macOS host.

### Gaps Summary

No code-level gaps were found. Every artifact, key link, and D-NN decision that can be verified by reading source was confirmed genuinely implemented — including the two decisions (D-12, D-13) that were deliberately superseded/retired, which are confirmed absent from source rather than silently missing. The 8 NOT ATTEMPTED + 2 OBSERVE-only UAT rows are manual-verification-coverage gaps caused by host fixture limitations (single CrossOver bundle, no qualifying game titles, no gamepad controller) and one explicit operator scope-closure decision, not implementation defects. Because the UAT ledger's own closure rule keeps the phase `human_needed` under these conditions, and no formal VERIFICATION-level override exists to supersede that rule, this report holds `status: human_needed` rather than `passed`.

---

_Verified: 2026-08-28_
_Verifier: Claude (gsd-verifier)_
