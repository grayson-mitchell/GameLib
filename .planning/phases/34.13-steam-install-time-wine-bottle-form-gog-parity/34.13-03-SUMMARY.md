---
phase: 34.13-steam-install-time-wine-bottle-form-gog-parity
plan: 03
subsystem: frontend
tags: [react, wine-selector, steam, install-form, D-16, D-05]

# Dependency graph
requires: ["34.13-01 (ordering-only -- no new common/types.ts field consumed)"]
provides:
  - "engineFilter.ts's four pure exports (resolveCrossoverOnly, filterWineEngines, selectDefaultEngine, resolveBottleNameState) -- src/frontend/screens/Library/components/InstallModal/WineSelector/engineFilter.ts"
  - "WineSelector's crossoverOnly?: boolean and bottleNameReadOnly?: boolean optional props"
affects: ["34.13-10 (new Steam dialog owns the D-14 shared-bottle notice, separate from this plan's D-05 read-only helper)", "34.13-12 (InstallModal/index.tsx owner)", "34.13-13 (manual gate proves the actual render)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure-decision-module extraction for components whose transitive UI imports pull in a stylesheet at import time -- WineSelector's SelectField import chain reaches SelectField/index.css, which this project's jsdom-less jest config cannot parse. Same posture as SideloadDialog/filters.ts (Phase 34.8-08a)."
    - "Grep-gate self-check: a doc comment discussing the reason a module avoids CSS/React coupling can itself contain the banned literal substrings (\".css\", \"frontend/components\") that a strict grep acceptance gate searches the WHOLE file for -- rephrase the prose to avoid the literal substrings rather than loosen the gate."

key-files:
  created:
    - src/frontend/screens/Library/components/InstallModal/WineSelector/engineFilter.ts
    - src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts
  modified:
    - src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx
    - .planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md

key-decisions:
  - "D-16 and D-05 both land entirely inside WineSelector (not the parent InstallModal, not a new Steam dialog) so SteamBottleSetup.tsx's independent WineSelector call inherits both for free via its existing runner=\"steam\" prop -- zero edits to that file, matching RESEARCH.md Pitfall 3's warning."
  - "selectDefaultEngine and filterWineEngines share one code path (selectDefaultEngine calls filterWineEngines(...).at(0)) so the offered engine list and the seeded default can never disagree -- this is what the RED-proof below empirically confirms matters."
  - "Kept effectiveCrossoverOnly as a plain per-render derivation (not memoized) and fed only that single derived value into engineOptions's useMemo deps, satisfying react-hooks/exhaustive-deps without duplicating runner/crossoverOnly in the dependency array."

patterns-established:
  - "A strict grep acceptance gate (`grep -E \"pattern1|pattern2\" <file>` expected to return zero matches) must be checked against the file's OWN prose, not just its code -- a doc comment explaining why a module avoids X can accidentally contain the literal string \"X\" and self-trip the gate it is meant to satisfy."

requirements-completed: [D-05, D-16]

duration: 8min
completed: 2026-08-15
---

# Phase 34.13 Plan 03: WineSelector CrossOver-Only Filter (D-16) + Read-Only Bottle Name (D-05) Summary

**Extracted the D-16 CrossOver-only engine filter and D-05 read-only bottle-name state into a pure, RED-proven `engineFilter.ts` module, then wired both as additive optional props into `WineSelector`, so `SteamBottleSetup.tsx` inherits the filter automatically without any edit to that file.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-15T01:00:55Z
- **Completed:** 2026-08-15T01:07:59Z
- **Tasks:** 3
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments

- Wrote `engineFilter.ts` with four pure exports (`resolveCrossoverOnly`, `filterWineEngines`, `selectDefaultEngine`, `resolveBottleNameState`), zero React/CSS/UI-component coupling, and a 19-test suite covering every `<behavior>` bullet including the 7-case exhaustive `Runner` enumeration.
- RED-proved the `selectDefaultEngine` CrossOver-seeding guard against the naive `wineVersionList.at(0)` implementation before restoring the real body -- see verbatim failure output below.
- Wired `crossoverOnly` (D-16) and `bottleNameReadOnly` (D-05) into `WineSelector/index.tsx` as additive optional props at all four engine-list consumption points (auto-seed effect, disabled guard, onChange lookup, SelectField options) plus the bottle-name field's disabled state and new read-only helper line.
- Re-scoped (not closed) the folded GPTK todo, recording precisely which half of option (a) shipped and which two halves remain open.
- Full caller census confirms zero behavior change for the four non-Steam `WineSelector` call sites and automatic D-16 inheritance for `SteamBottleSetup.tsx`.
- `pnpm test:ci`: 255/255 suites, 4921 passed / 1 skipped (baseline from 34.13-01 was 254/254 suites, 4891 passed / 1 skipped) -- one new suite added, zero regressions.

## Task Commits

1. **Task 1: Extract the D-16 / D-05 decisions into a pure engineFilter.ts module, with tests** - `5238b1a8e` (test)
2. **Task 2: Wire crossoverOnly (D-16) and bottleNameReadOnly (D-05) into WineSelector** - `d00b117e5` (feat)
3. **Task 3: Re-scope the GPTK todo and prove existing callers are untouched** - `5391ec025` (docs)

_No separate plan-metadata commit is issued by this executor; this SUMMARY + STATE.md/ROADMAP.md tracking land in the final docs commit below._

## RED Proof (Task 1, verbatim failure output)

Command: `npx jest src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts --silent`, run against `selectDefaultEngine` temporarily reduced to `return wineVersionList.at(0)` (ignoring `crossoverOnly`, not committed):

```
FAIL Frontend src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts
  ● selectDefaultEngine › returns a crossover engine even when a toolkit engine sits at index 0 (regression guard)

    expect(received).toBe(expected) // Object.is equality

    Expected: "crossover"
    Received: "toolkit"

      82 |   it('returns a crossover engine even when a toolkit engine sits at index 0 (regression guard)', () => {
      83 |     const result = selectDefaultEngine(mixedList, true)
    > 84 |     expect(result?.type).toBe('crossover')
         |                          ^
      85 |     expect(result?.name).toBe('CrossOver 24')

  ● selectDefaultEngine › returns undefined when no engine in the list is crossover-type

    expect(received).toBeUndefined()

    Received: {"bin": "/bin/Game Porting Toolkit", "name": "Game Porting Toolkit", "type": "toolkit"}

      100 |       makeEngine('Proton Experimental', 'proton')
      101 |     ]
    > 102 |     expect(selectDefaultEngine(onlyNonCrossover, true)).toBeUndefined()
          |                                                         ^

Test Suites: 1 failed, 1 total
Tests:       2 failed, 17 passed, 19 total
```

This confirms the folded GPTK todo's exact failure mode: with `mixedList`'s `'toolkit'` engine at index 0, the naive `.at(0)` implementation seeds a non-CrossOver engine -- exactly the "silently produces a broken bottle" defect D-16 exists to close. After restoring the real `filterWineEngines(...).at(0)`-based body, the suite passed 19/19. Git diff after Task 1's commit shows no trace of the temporary reduction.

## WineSelector Caller Census (Task 3)

Every existing `<WineSelector` call site in `src/`, confirmed via `grep -rn "<WineSelector" src/`:

| # | File:Line | `runner` prop | New props passed? |
|---|-----------|----------------|--------------------|
| 1 | `InstallModal/index.tsx:187` (inside `ThirdPartyDialog` branch) | no runner prop | none |
| 2 | `InstallModal/index.tsx:215` (inside `ImportDialog` branch) | no runner prop | none |
| 3 | `InstallModal/index.tsx:242` (inside `DownloadDialog` branch) | no runner prop | none |
| 4 | `InstallModal/index.tsx:268` (inside `SideloadDialog` branch) | no runner prop | none |
| 5 | `SteamBottleSetup.tsx:166` | `runner="steam"` | none |

Confirmed by `grep -rn "crossoverOnly\|bottleNameReadOnly" InstallModal/index.tsx SteamBottleSetup.tsx` returning zero matches (exit 1).

- Call sites 1-4 pass no `runner` prop at all, so `resolveCrossoverOnly(undefined, undefined)` evaluates to `false` -- these callers (GOG/Epic/Amazon/sideload install modals reached through `InstallModal`) are behaviorally byte-for-byte unchanged: every engine in `wineVersionList` remains offered, the seeded default is still `wineVersionList.at(0)` (via `selectDefaultEngine` with `crossoverOnly=false`, identity-preserving), and the bottle-name field's `disabled` is still exactly `useSharedPrefix`.
- **Call site 5, `SteamBottleSetup.tsx`, is the intended D-16 inheritance case, not a defect:** it already passes `runner="steam"` and no new prop, so `resolveCrossoverOnly('steam', undefined)` evaluates to `true` and the guided Steam bottle setup dialog now offers only CrossOver engines and seeds a CrossOver default -- achieved with **zero edits** to `SteamBottleSetup.tsx` itself, per RESEARCH.md Pitfall 3 and this plan's `<constraints>` prohibition against editing that file.

## GPTK Todo Status (D-16 partial closure)

`.planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md` is **appended-to, still `pending/`**. Recorded explicitly:

- **Shipped:** option (a)'s frontend-filter half -- `WineSelector` now offers/seeds only `type === 'crossover'` engines whenever `runner === 'steam'`, covering both the new Steam install form and `SteamBottleSetup.tsx`.
- **Remains open:** the `provisionBottle` engine-rejection half (`src/backend/storeManagers/steam/bottle.ts` still accepts any `WineInstallation` unrejected -- a `toolkit`/`wine` engine already persisted into the Steam bottle config store before this filter existed still reaches `provisionBottle` unrejected).
- **Remains open:** the `resolveSteamBottleEngine` silent-override half (`steamBottleDefaults.ts` untouched -- this plan's filter only governs OFFER/SEED, never force-corrects an already-set non-CrossOver `wineVersion`, per this plan's own scope prohibition).
- **Remains fully out of scope:** option (b), the prefix-based GPTK provisioning path.

The diff to the todo file is append-only (zero deletions, confirmed via `git diff` showing no `^-` lines).

## Files Created/Modified

- `src/frontend/screens/Library/components/InstallModal/WineSelector/engineFilter.ts` (created) -- four pure exports, 4-function/0-CSS/0-React contract
- `src/frontend/screens/Library/components/InstallModal/WineSelector/__tests__/engineFilter.test.ts` (created) -- 19 tests
- `src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx` (modified) -- `crossoverOnly`/`bottleNameReadOnly` props, `engineOptions` at all four seams, `resolveBottleNameState`-driven bottle-name field + helper line
- `.planning/todos/pending/steam-bottle-gptk-engine-produces-broken-bottle.md` (modified) -- append-only re-scope section

## Decisions Made

- No architectural changes; both props landed exactly as additive/optional per the plan's frozen contract.
- Fixed a self-defeating grep acceptance gate before it could fail: `engineFilter.ts`'s header doc-comment initially explained the CSS-coupling rationale using the literal substrings `.css`, `.scss`, `frontend/components` -- exactly the substrings the plan's own acceptance criterion `grep -E "\.css|\.scss|from 'react'|frontend/components" <file>` checks for zero matches across the WHOLE file (not just the code). Rephrased the prose to describe the same rationale without those literal substrings; re-ran the grep gate to confirm zero matches while keeping the doc-comment's informational value. Not logged as a numbered deviation below since it was fixed before any commit and produced no functional change -- purely a prose adjustment to satisfy the plan's own stated acceptance criterion.

## Deviations from Plan

None - plan executed exactly as written. (See the grep-gate prose fix above, addressed inline before Task 1's commit, not a deviation from behavior/scope.)

## Issues Encountered

- `npx eslint` flagged `react-hooks/exhaustive-deps` on the `engineOptions` `useMemo` when its dependency array was `[wineVersionList, runner, crossoverOnly]` as the plan's `<action>` specified literally -- because the memo callback references `effectiveCrossoverOnly`, not `runner`/`crossoverOnly` directly. [Rule 1 - Bug] Fixed by depending on `effectiveCrossoverOnly` itself (the value the callback actually closes over, fully derived from `runner`+`crossoverOnly` each render) instead of its two source values -- functionally identical memoization behavior, lint-clean. Verified `npx eslint` returns zero warnings/errors after the change; `npx tsc --noEmit` and the full `WineSelector` jest suite stayed green throughout.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `WineSelector`'s two new optional props (`crossoverOnly`, `bottleNameReadOnly`) are frozen, additive contracts for 34.13-13's manual gate to verify by rendering.
- `SteamBottleSetup.tsx` inherits D-16 automatically; any downstream plan should NOT re-edit that file to "add" the filter -- it is already inherited.
- The new Steam install dialog (34.13-10) still owns the separate D-14 shared-bottle notice -- not duplicated here, and this plan's D-05 helper line is a different, narrower piece of copy (bottle-name-field-specific, not the general shared-bottle consequence notice).
- `public/locales/en/gamelib.json` is untouched by this plan; the new `gamelib:steam.install.bottleNameReadonlyHelper` key exists only as an inline English default until 34.13-13 (the phase's single catalog writer) adds it.
- No blockers for the remaining wave 2 plans.

---
*Phase: 34.13-steam-install-time-wine-bottle-form-gog-parity*
*Completed: 2026-08-15*

## Self-Check: PASSED

All 4 created/modified source and doc files confirmed present on disk (`engineFilter.ts`, `engineFilter.test.ts`, `WineSelector/index.tsx`, the re-scoped GPTK todo); all three task commits (`5238b1a8e`, `d00b117e5`, `5391ec025`) confirmed present in `git log --oneline --all`.
