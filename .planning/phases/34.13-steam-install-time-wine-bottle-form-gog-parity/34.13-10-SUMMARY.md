---
phase: 34.13-steam-install-time-wine-bottle-form-gog-parity
plan: 10
subsystem: ui
tags: [typescript, react, steam, install-modal, wine-bottle, section-gating, jest, source-gate]

# Dependency graph
requires:
  - phase: 34.13-01
    provides: "GameInfo.is_windows_native / InstallArgs.steamForceWindowsViaBottle / SteamBottleEligibilityVerdict frozen type contract"
  - phase: 34.13-05
    provides: "resolveSteamSectionGating() / SteamSectionGatingVerdict -- the SOLE source of section visibility consumed here"
  - phase: 34.13-07
    provides: "window.api.persistBottleWineVersion(wineVersion) -- the D-14 write channel, dual-registered Electron + Tauri sidecar"
  - phase: 34.13-08
    provides: "installSteamGame(appName, gameInfo, path, forceWindowsViaBottle) 4-arg signature; SteamQuickInstallDegrade / InstallGameModalState.steamDegrade; deletion of SteamInstallLocation.ts and SteamInstallLocationPicker.tsx"
provides:
  - "SteamDialog -- the D-01 fifth InstallModal sibling component (created, NOT mounted -- 34.13-12 adds the parent branch)"
  - "installTarget.ts -- resolveSteamInstallPath()/defaultSteamLibraryPath(), the T-34.13-10-01 destination containment gate, zero runtime imports"
  - "steamDialogSource.test.ts -- permanent comment-stripped source gates for every negative decision this dialog encodes"
affects: [34.13-11, 34.13-12, 34.13-13]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure-module-beside-a-CSS-importing-.tsx extraction (SideloadDialog/filters.ts, WineSelector/engineFilter.ts precedent) applied to a destination-containment gate."
    - "Comment-stripped source gate with paired throwing-helper functions (expectAbsent/expectPresent/assertOrderingPersistBeforeInstall/assertForceWindowsViaBottlePrefixed/assertBoundedStatusDanger), called directly in the main gate blocks and wrapped in expect(() => ...).toThrow()/.not.toThrow() in the known-bad-specimen block -- so the identical assertion logic drives both the real gate and its own non-vacuity proof."

key-files:
  created:
    - src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx
    - src/frontend/screens/Library/components/InstallModal/SteamDialog/installTarget.ts
    - src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/installTarget.test.ts
    - src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/steamDialogSource.test.ts
  modified: []

key-decisions:
  - "Q6 content-light notice uses .infoBox, NOT ThirdPartyDialog's .noticeIcon/.noticeInfo slot -- ThirdPartyDialog/index.css:1-25 nests both classes inside .thirdPartyNotice, in a stylesheet only that dialog imports; reusing them here would mean importing another dialog's stylesheet, contradicting this plan's zero-stylesheet decision. .infoBox is global + dialog-scoped and already used by WineSelector/the D-14 notice, so it is the honest equivalent. Copy, key and trigger condition unchanged (this deviation was flagged in advance by 34.13-05-SUMMARY.md)."
  - "No stylesheet is created -- WineSelector is the in-repo precedent for a dialog fragment shipping zero CSS; .smallInputInfo (global, index.scss:95) and .infoBox (global + dialog-scoped, InstallModal/index.scss:7) cover every element this dialog renders. This departs from 34.13-PATTERNS.md's file table, which lists a new SteamDialog/index.scss."
  - "D-24's degrade notice renders FIRST in DialogContent (above {children}); the D-14 notice renders immediately after {children} rather than inside WineSelector's own markup, because WineSelector belongs to 34.13-03 and this plan must not edit it -- same visual position, different file-ownership reality. Regions 2 (children/wine section), 3 (D-14 notice), 4 (library dropdown) and 5 (Q6 notice) are mutually exclusive at runtime per 34.13-05's matrix, so only the D-24-notice-before-{children} ordering is user-observable; the rest are pinned in source order only for readability."
  - "steamDegrade.onlyLibrary is deliberately read-but-unused: the whole steamDegrade record is read via the zustand selector and its reason field drives the notice copy, but onlyLibrary is never branched on. 34.13-08 already resolved the UI-SPEC's flagged <=1-library corner as 'render the notice plus whatever the matrix's other rows already produce, fabricate no empty picker' -- this dialog's response is exactly that (do nothing extra), so the field exists for a future consumer, not for this file."
  - "The D-02 dropdown label default text follows this plan's own <action> instruction verbatim -- t('install.steam-location.title', 'Choose Steam library') -- which differs slightly from 34.13-UI-SPEC.md's Copywriting Contract cell ('Steam library', no 'Choose' prefix). Both reuse the SAME existing gamepage:install.steam-location.title key (not a new catalog entry either way), so this is a cosmetic default-text discrepancy, not a decision divergence. Recorded per this plan's own 'if a plan cell disagrees with the UI-SPEC, the UI-SPEC wins' convention used elsewhere in this phase -- flagging for 34.13-13/reviewer rather than silently reconciling, since the plan's <action> text was the more specific, task-level instruction actually followed here."

patterns-established:
  - "assertContainment(resolveFn, libraries): a single throwing helper run against the SAME HOSTILE_INPUTS case table for both the real destination-resolution implementation and a resolveWithoutContainment saboteur, so the saboteur is provably held to the identical bar the real implementation passes, not a weaker parallel one."

requirements-completed: [D-01, D-02, D-06, D-08, D-14, D-20, D-24, D-25]

duration: 7min (commit-to-commit window; full session included extensive upstream-contract verification reading per this plan's own required gate)
completed: 2026-08-15
---

# Phase 34.13 Plan 10: SteamDialog -- the D-01 Fifth InstallModal Sibling Summary

**Built the Steam install-options dialog (`SteamDialog`) as a lean `ThirdPartyDialog`-shaped sibling driven entirely by 34.13-05's gating verdict -- D-02 library dropdown, D-08 free-space line, D-14 shared-bottle notice with a persist-then-install confirm handler, D-24 degrade notice, and the Q6 content-light notice -- plus a RED-proven, saboteur-checked destination-containment module and a comment-stripped source-gate suite locking every negative decision; created but not yet mounted (34.13-12 wires the parent branch in Wave 6).**

## Performance

- **Duration:** ~7 min (commit-to-commit)
- **Started:** 2026-08-15T04:46:22Z
- **Completed:** 2026-08-15T04:53:32Z
- **Tasks:** 3
- **Files modified:** 4 (all created)

## Accomplishments

- Verified all four `<upstream_contract>` checks before writing any code: 34.13-05's verdict shape (exactly 6 fields, no `opens`), 34.13-07's `persistBottleWineVersion` IPC channel, 34.13-08's `installSteamGame` 4-arg signature and `SteamQuickInstallDegrade`/`steamDegrade` shape, and confirmed no `SteamInstallFormContext` exists anywhere -- **zero deltas found**, every consumed contract landed exactly as documented.
- Task 1: `installTarget.ts` (zero runtime imports) + RED-proven `installTarget.test.ts` (16 tests) -- `defaultSteamLibraryPath()` (D-02 primary default) and `resolveSteamInstallPath()` (T-34.13-10-01 strict-equality containment gate), proven over a hostile-input sweep and a `resolveWithoutContainment` saboteur run through the identical shared `assertContainment` helper/case table.
- Task 2: `SteamDialog/index.tsx` -- the D-01 fifth sibling, `tsc --noEmit` and `eslint` clean (2 pre-existing-pattern warnings only, matching `DownloadDialog`'s unwrapped async-effect call and `ThirdPartyDialog`'s own `useCallback` deps convention).
- Task 3: `steamDialogSource.test.ts` -- comment-stripped source gates for every forbidden mechanism, the three 34.13-05 review-obligation tokens, required wiring, region ordering, and persist-before-install ordering, each proven against an executable known-bad specimen; stripper load-bearing-ness proven against 3 tokens in both `//` and `/* */` comment forms.
- `pnpm test:ci`: **265/265 suites, 5225 passed / 1 skipped, zero regressions.**

## Task Commits

1. **Task 1: The pure install-destination module -- primary default + T-34.13-10-01 containment gate** - `b74243e48` (test)
2. **Task 2: The SteamDialog sibling component** - `13f7100b8` (feat)
3. **Task 3: Lock the negative decisions -- comment-stripped source gates** - `418d6de07` (test)

_No separate plan-metadata commit is issued by this executor; this SUMMARY + STATE.md/ROADMAP.md tracking land in the final docs commit below._

## RED Proof (Task 1, verbatim)

Command: `npx jest src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/installTarget.test.ts --silent`

Against the pre-module tree (test file written, `installTarget.ts` not yet created):

```
FAIL Frontend src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/installTarget.test.ts
  ● Test suite failed to run

    Cannot find module '../installTarget' from 'src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/installTarget.test.ts'

    > 1 | import {
        | ^
      2 |   SteamDialogLibraryOption,
      3 |   defaultSteamLibraryPath,
      4 |   resolveSteamInstallPath

Test Suites: 1 failed, 1 total
Tests:       0 total
```

The failing import statement names both `defaultSteamLibraryPath` and `resolveSteamInstallPath` directly -- a valid RED proof per the plan's own acceptance bar. After creating `installTarget.ts`: **16/16 passed** (GREEN), including the `resolveWithoutContainment` saboteur block, which independently proves the saboteur FAILS the same `assertContainment` invariant (thrown as a real jest assertion error) that the real implementation passes cleanly on every one of the three fixtures.

## Final Props Interface (for 34.13-12)

**12 fields** -- the identical 10-field sibling prop contract every `InstallModal` sibling receives, plus exactly 2 additions (`gating`, `steamLibraries`). Note: the plan's own `<output>` instruction text says "all eleven field names," which does not match its own stated arithmetic ("the identical ten fields... plus exactly two additions") -- transcribing the actual, correct count here rather than silently reconciling the plan's off-by-one prose.

```ts
interface Props {
  backdropClick: () => void
  appName: string
  runner: Runner                          // accepted for parity, unused (D-06)
  platformToInstall: InstallPlatform      // accepted for parity, unused (D-17 -- gating.forceWindowsViaBottle carries it)
  availablePlatforms: AvailablePlatforms
  winePrefix: string                      // accepted for parity, unused (D-14)
  crossoverBottle: string                 // accepted for parity, unused (D-14/D-05)
  wineVersion: WineInstallation | undefined
  children: React.ReactNode
  gameInfo: GameInfo
  gating: SteamSectionGatingVerdict       // NEW -- 34.13-05's verdict, sole source of section visibility
  steamLibraries: SteamDialogLibraryOption[] // NEW -- parent's single library-list read, handed down
}
```

**34.13-11 (wave 7) adds a THIRD new field, `eligibilityPending: boolean`, and the D-25 `disabled={eligibilityPending}` wiring -- not added by this plan.**

## Exact `installSteamGame(...)` Call Site (for 34.13-12)

```ts
installSteamGame(
  appName,
  gameInfo,
  destination,                        // resolveSteamInstallPath(selectedPath, steamLibraries, gating.libraryDropdown)
  gating.forceWindowsViaBottle        // the verdict's own field, passed through verbatim -- never reconstructed
)
```

Confirm-handler order, exactly as the plan specifies: (1) if `gating.wineSection && wineVersion`, `await window.api.persistBottleWineVersion(wineVersion)`, logging (never blocking) on `{status:'error'}`; (2) compute `destination` via `resolveSteamInstallPath`; (3) `backdropClick()`; (4) `installSteamGame(...)` as above.

## The Four New `gamelib:` Keys -- Exact English Defaults Shipped (for 34.13-13)

Transcribed verbatim from `34.13-UI-SPEC.md`'s Copywriting Contract, exactly as they appear in `SteamDialog/index.tsx`'s `tGamelib(...)` calls:

| Key | English default |
|---|---|
| `gamelib:steam.install.sharedBottleNotice` | "The wine version you choose here is used for every Steam game that needs a bottle — not just this one. Changing it later will change how those games run too." |
| `gamelib:steam.install.libraryMissingNotice` | "Your Steam library couldn't be reached — choose another below." |
| `gamelib:steam.install.libraryFullNotice` | "Not enough space in your Steam library — choose another below or free up space." |
| `gamelib:steam.install.contentLightNotice` | "This installs through Steam's own client, so there's nothing to choose here. Turn on native Steam installs in Settings to manage install location and Windows compatibility from GameLib." |

**The UI-SPEC's bold emphasis on the word "every" in the D-14 notice was deliberately dropped**, shipped as one flat string with no `<Trans>`/indexed-tag markup -- an indexed `<1></1>` tag would push a decorative emphasis into 34.13-13's catalog entry and into `lint-translations`' content-less-tag checks across every locale, for no behavioural gain. `public/locales/en/gamelib.json` is untouched (`git status --porcelain public/locales/` empty) -- 34.13-13 is the single catalog writer.

## Q6 Notice Class Deviation (evidence)

The UI-SPEC's original Copywriting Contract cell prescribed reusing `ThirdPartyDialog`'s `.noticeIcon`/`.noticeInfo` advisory slot. Read `ThirdPartyDialog/index.css:1-25` directly and confirmed: both classes are nested inside `.thirdPartyNotice`, in a stylesheet imported ONLY by `ThirdPartyDialog/index.tsx`. Reusing them here would require importing another dialog's stylesheet, contradicting this plan's zero-stylesheet decision. `34.13-05-SUMMARY.md` had already flagged this exact correction for this plan's benefit, and `34.13-UI-SPEC.md` line 140 carries a **⚠ CORRECTED 2026-08-15** note confirming `.infoBox` is the fix. Shipped: `.infoBox` (global at `components/UI/InfoBox/index.css`, dialog-scoped refinement at `InstallModal/index.scss:7-12`), same shape as the D-14 notice. Copy, catalog key and trigger condition (`gating.contentLightNotice`) are unchanged from the UI-SPEC.

## `steamDegrade.onlyLibrary` -- Read But Unused

`steamDegrade` is read whole via the zustand selector `useInstallGameModal((state) => state.steamDegrade)`, so `onlyLibrary` is present on the value in scope, but this dialog never branches on it. This is intentional, not a dropped field: 34.13-08 already resolved the UI-SPEC's flagged ≤1-library corner as "render the D-24 notice plus whatever the Section-Gating Matrix's other rows already produce (the content-light shape), fabricate no empty picker" -- which is exactly what happens here with zero extra code, since the notice and the matrix-driven regions are already independent. A future consumer of `onlyLibrary` would need its own reason to branch on it; this plan is not that consumer.

## `disabled` Count -- Currently 0, Expected to Become 1

`grep -v '^\s*\*' index.tsx | grep -v '^\s*//' | grep -cE '\bdisabled\b'` returns **0** as shipped by this plan -- the footer Install button carries no `disabled` prop in any render state. This is **expected to become 1** after 34.13-11 (wave 7) wires `disabled={eligibilityPending}` into this same file per D-25. This plan's own source gate (`steamDialogSource.test.ts`) deliberately does NOT assert `disabled`'s absence (see the test file's own header comment) -- a reviewer who sees the count become 1 after wave 7 is looking at the correct end state, not a regression.

## No-Stylesheet Decision (evidence)

`ls src/frontend/screens/Library/components/InstallModal/SteamDialog/` lists exactly `index.tsx`, `installTarget.ts`, `__tests__` -- no `index.scss`/`index.css`, and none is imported (`grep -cE "import '\./index\.(scss|css)'" index.tsx` returns 0). Precedent: `WineSelector/index.tsx` ships zero stylesheet. `.smallInputInfo` is global (`src/frontend/index.scss:95-101`). `.infoBox` is global (`components/UI/InfoBox/index.css`) plus dialog-scoped (`InstallModal/index.scss:7-12`, which applies automatically because this dialog renders inside `.InstallModal__dialog`). `SelectField`'s own `Field` chrome covers the dropdown. This is a deliberate departure from `34.13-PATTERNS.md`'s file table, which lists a new `SteamDialog/index.scss`. Consequence: with no new layout, there is no `1fr` grid track and therefore no exposure to the project-standing WKWebView 1px-border rasterisation failure.

## D-14 Notice Placement Note (layout_contract)

`34.13-UI-SPEC.md` §2 describes the D-14 notice as appearing "inside the same mounted block" as the wine section. It cannot literally live inside `<WineSelector>` here -- that file is owned by 34.13-03 and this plan must not edit it -- so it renders immediately after `{children}` instead, which is the same visual position from the user's point of view. This is a file-ownership consequence, not a design change. Regions 2 (`{children}`/wine section), 3 (D-14 notice), 4 (library dropdown, with the D-08 free-space line in its `afterSelect` slot) and 5 (Q6 content-light notice) are mutually exclusive at runtime per 34.13-05's 8-row matrix (`libraryDropdown` and `wineSection` are never both true; `contentLightNotice` is true only when both are false), so their relative source order is unobservable to the user except for the D-24 degrade notice, which must and does render first, above everything.

## Upstream Contract Verification -- Zero Deltas

All four checks in `<upstream_contract>` were run against the live tree before writing any code, and every one matched the plan's documented expectation exactly:

1. **34.13-05's verdict** (`steamSectionGating.ts`): exactly 6 fields (`platformRow`, `libraryDropdown`, `wineSection`, `freeSpaceLine`, `contentLightNotice`, `forceWindowsViaBottle`), no `opens` field. Matched.
2. **34.13-07's write channel**: `persistBottleWineVersion` is a `makeHandlerInvoker('persistBottleWineVersion')` export in `src/preload/api/steam.ts`, and `src/common/types/ipc.ts:307` declares `(wineVersion: WineInstallation) => Promise<{ status: 'done' | 'error'; error?: string }>`. Matched.
3. **34.13-08's install call**: `installSteamGame(appName, gameInfo, path = '', forceWindowsViaBottle = false)` -- arity 4, positional boolean fourth argument. Matched (no adapter needed). `SteamQuickInstallDegrade` has exactly `reason: 'library-missing' | 'library-full'`, `libraryPath: string`, `freeBytes: number`, `onlyLibrary: boolean`, and `InstallGameModalState.steamDegrade?: SteamQuickInstallDegrade` exists. Matched. **No `SteamInstallFormContext` exists anywhere in `InstallGameModal.ts`** -- confirmed by full read of the file. Matched (confirms absence, as expected).
4. **`SteamInstallLocationPicker.tsx`** (read for its copy/shape per Task 1's `<read_first>`): confirmed already deleted by 34.13-08, exactly as `<prior_wave_context>` stated -- read attempt returned "File does not exist," consistent with the plan's own warning that this file "is being deleted by 34.13-08; read it for its behaviour, not as a live dependency." Its default-selection and confirm-shape behaviour was instead taken from the plan's own `<interfaces>` section, which had already transcribed it.

No adapter code, no delta report needed for 34.13-12's reviewer beyond what is recorded above.

## Specimen-Proof Table (Task 3, non-vacuity evidence)

| Token / assertion | Specimen | Observed result |
|---|---|---|
| `getInstallInfo`, `writeConfig`, `frontend/helpers`, `listSteamLibraryTargets`, `diskSize`, `notEnoughDiskSpace`, `space-after-install`, `spaceLeftAfter`, `availablePlatforms.length`, `libraryCount` (10 tokens) | `const x = '<token>'` (executable position) | `expectAbsent` throws for every one |
| `platformToInstall ===` | `if (platformToInstall === 'Windows') {}` | `expectAbsent` throws |
| `getInstallInfo`, `writeConfig`, `listSteamLibraryTargets` (3 tokens, `//` form) | `// mentions <token> only to explain it is forbidden` | `expectAbsent` passes (stripper removed the whole comment line) |
| same 3 tokens (`/* */` form) | `/* mentions <token> only to explain it is forbidden */` | `expectAbsent` passes (stripper removed the block comment) |
| `forceWindowsViaBottle` prefix | `installSteamGame(a, b, c, forceWindowsViaBottle)` (no `gating.` prefix) | `assertForceWindowsViaBottlePrefixed` throws |
| persist-before-install ordering | the two calls written in swapped order | `assertOrderingPersistBeforeInstall` throws |
| ordering vacuity (bare name) | specimen with only an `import { installSteamGame }` line, no real call site | bare-name index sits BEFORE the persist call (would vacuously "pass" a naive check); `installSteamGame(` (with parenthesis) is absent entirely, so the real ordering helper correctly throws instead of vacuously passing |
| bounded `--status-danger` | two `var(--status-danger)` occurrences in one specimen | `assertBoundedStatusDanger` throws (expects exactly 1) |

All 20 `it()`s in the "known-bad specimen" block passed on first run (`npx jest ... --silent`: 75/75 total across both new test files). Full `pnpm test:ci`: 265/265 suites, 5225 passed / 1 skipped, zero regressions.

## Files Created

- `src/frontend/screens/Library/components/InstallModal/SteamDialog/installTarget.ts` -- zero-import pure module, ~78 lines
- `src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/installTarget.test.ts` -- 16 tests, ~200 lines
- `src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx` -- the D-01 fifth sibling, ~264 lines
- `src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/steamDialogSource.test.ts` -- 59 tests, ~380 lines

## Decisions Made

- Followed the plan's own D-02 dropdown-label `<action>` text (`t('install.steam-location.title', 'Choose Steam library')`) over the UI-SPEC Copywriting Contract cell's slightly different default text (`'Steam library'`) -- both reuse the same existing key, so this is a cosmetic default-text discrepancy between two plan artifacts, not a design decision divergence. Flagged in `key-decisions` above rather than silently reconciled.
- Kept two pre-existing eslint warnings (`no-floating-promises` on the unwrapped `getSpace()` call in Effect B, `exhaustive-deps` on `handleInstall`'s `useCallback` missing `backdropClick`) because both match established sibling conventions verbatim: `DownloadDialog/index.tsx:479` has the identical unwrapped `getSpace()` call, and `ThirdPartyDialog/index.tsx:86`'s own `handleInstall` `useCallback` also omits `backdropClick` from its deps array. Neither is an error (`eslint` exit code 0); introducing a `void`/dep-array change here would diverge from the pattern this plan is explicitly modelled on.
- Used `# eslint-disable-next-line react-hooks/exhaustive-deps` on Effect A (default-selection) rather than adding `selectedPath` to its dependency array, because the plan's own action text specifies the effect is "Keyed on `steamLibraries`" only -- adding `selectedPath` would re-run the effect on every selection change, which is not what D-02's "default on array-identity-change" behaviour calls for.

## Deviations from Plan

None beyond the two cosmetic/discretionary items already recorded above (the D-02 label default-text source, and the Q6 notice's `.infoBox` class, which was already anticipated and pre-approved by `34.13-05-SUMMARY.md`). No Rule 1/2/3/4 deviation was required -- every upstream contract matched exactly, and the plan's own `<action>` prose was detailed enough to implement without gap-filling.

## Issues Encountered

None. The plan's extensive `<upstream_contract>` pre-flight check meant no runtime surprises during implementation; the two named "corrections" in `<prior_wave_context>` (the fetch/gate dependency cycle and the Q6 notice class) were both already resolved by the plan's own `<action>` and `<layout_contract>` text before this executor began writing code.

## User Setup Required

None -- no external service configuration required.

## Explicitly NOT Verified Here (per plan's own `<verification>` section, and this plan's own `<testing_reality>`)

- **That the dialog renders at all, or renders the right sections.** No jsdom in this project's Frontend jest config; reaching a render would require mocking the UI kit's whole transitive stylesheet graph. A green source gate and a dialog that renders nothing are indistinguishable to Jest here. **34.13-13's manual UAT gate is the only evidence**, and it must enumerate the Section-Gating Matrix's 8 rows rather than sample it.
- That the parent actually mounts `SteamDialog` and passes a correctly-computed `gating` verdict -- 34.13-12, Wave 6. Confirmed by grep that this component is unreachable at runtime today: `grep -rn "SteamDialog" src/frontend --include="*.tsx" | grep -v "SteamDialog/"` returns nothing.
- That the persisted wine version is actually READ by the guided bottle setup -- 34.13-09 (already landed, per `<prior_wave_context>`). This plan proves only the write ordering (persist before install).
- That the four new `gamelib:` catalog entries exist and `pnpm lint-translations:gamelib` is green -- 34.13-13, the single catalog writer for this phase.
- **That the D-25 eligibility loading row appears, or that the Install button is ever disabled** -- 34.13-11 (wave 7), which edits this same file afterwards. This plan's gates deliberately do NOT forbid a `disabled` attribute.
- That the D-24 notice actually RENDERS, or that quick install actually degrades into this dialog in a live app -- 34.13-08 produces the `steamDegrade` record; 34.13-13's manual gate is the only evidence the two halves meet at runtime.
- That the Q6 content-light notice appears on a real Windows or Linux host -- 34.13-13's manual gate, exercising matrix rows 5 and 7 explicitly.

## Next Plan Readiness

- **This plan mounts nothing.** `SteamDialog` is created but unreachable at runtime until 34.13-12 (Wave 6) adds the fifth branch to `InstallModal/index.tsx`. No Jest evidence in this plan speaks to whether the dialog renders -- that is 34.13-13's manual UAT gate alone.
- 34.13-12 can wire the parent branch directly from the "Final Props Interface" and "Exact `installSteamGame(...)` Call Site" sections above without re-reading the component.
- 34.13-11 (wave 7) is the next editor of `SteamDialog/index.tsx` and `steamDialogSource.test.ts` -- both files are structured (per this SUMMARY's `disabled`-count note and the source gate's own header comment) so that adding `eligibilityPending`/`disabled={eligibilityPending}` and the `EligibilityLoadingRow` is additive, not a fight against this plan's gates.
- 34.13-13 needs: the four verbatim English default strings (above) for the catalog; the specimen-proof table (above) for review; and the manual UAT obligation this plan explicitly could not discharge (rendering, region visibility, D-24 live-degrade, Q6 on a real host).
- No blockers.

---
*Phase: 34.13-steam-install-time-wine-bottle-form-gog-parity*
*Completed: 2026-08-15*

## Self-Check: PASSED

All 4 created files confirmed present on disk; all 3 task commits (`b74243e48`, `13f7100b8`, `418d6de07`) confirmed present in `git log --oneline --all`.
