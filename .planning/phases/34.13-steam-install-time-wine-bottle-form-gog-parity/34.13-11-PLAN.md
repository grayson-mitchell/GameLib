---
phase: 34.13-steam-install-time-wine-bottle-form-gog-parity
plan: 11
type: execute
wave: 7
depends_on: ['34.13-07', '34.13-08', '34.13-10', '34.13-12']
files_modified:
  - src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts
  - src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts
  - src/frontend/screens/Library/components/InstallModal/SteamDialog/EligibilityLoadingRow.tsx
  - src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/EligibilityLoadingRow.test.tsx
  - src/frontend/screens/Library/components/InstallModal/index.tsx
  - src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx
  - src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/steamDialogSource.test.ts
  - src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts
autonomous: true
requirements: ['D-25', 'D-11', 'D-06']
user_setup: []

must_haves:
  truths:
    - "D-25 (the inversion): the eligibility loading state lives INSIDE the options dialog, in the physical slot the Steam `<WineSelector>` would otherwise occupy, and the dialog's OWN footer Install button is the thing that is disabled while it pends. NO origin install control (`MainButton.tsx`, `GameCard/index.tsx`, `GameSubMenu/index.tsx`) gains any busy state, spinner, or `disabled` term from this plan — proven by a proof-of-absence gate over all three files, not by silence."
    - "D-25 (the quick path fires NO probe) — guaranteed STRUCTURALLY, not by promise. The probe lives in a hook that only `InstallModal/index.tsx` calls, and `InstallGameWrapper` returns `<></>` when `useInstallGameModal().isOpen` is false (`InstallModal/index.tsx:287-292`, verified 2026-08-15). The quick-install path never flips `isOpen`, so the component that owns the probe never mounts. Backed by three gates: the `isOpen` early-return still exists; `src/frontend/state/InstallGameModal.ts` contains ZERO `isSteamBottleEligible`; and a recursive `src/frontend` census confirms every file referencing `isSteamBottleEligible` is on an explicitly enumerated allowlist."
    - "D-25 (opens INSTANTLY): nothing in this plan awaits anything before render. `useSteamBottleEligibility` returns a state synchronously on first call — `pending` on macOS, already-resolved on Windows/Linux — and the IPC round trip happens in a `useEffect` AFTER the first render. A half-rendered dialog is the CORRECT, designed result; `34.13-VALIDATION.md`'s Manual-Only table records that a tester working from memory of the retired D-12 contract will score this backwards."
    - "macOS-only scope, written fail-SAFE not fail-fast: the probe is skipped only when `platform` is EXPLICITLY `'win32'` or `'linux'` — never as `!== 'darwin'` — so an absent or unexpected `platform` still asks the backend rather than silently answering 'not eligible'. This mirrors 34.13-08's `skipEligibility` convention exactly and is pinned by a named DISCRIMINATOR spec that the inverted form fails and every other spec passes."
    - "D-09 preserved: `bottleRequired` still originates ONLY from the backend verdict `window.api.isSteamBottleEligible`. The probe module contains none of `is_mac_native`, `mac_arch`, `steamPlatformsCaptured` — no renderer re-derivation, which is the exact defect `.planning/debug/steam-bottle-guided-setup-never-fires.md` records (a cold metadata cache reading as 'not eligible')."
    - "D-11 / section gating stays DELEGATED, never re-derived: pending-time suppression is a single pure function `applyEligibilityPending(verdict, pending)` applied to 34.13-05's verdict in `InstallModal/index.tsx`. `SteamDialog` receives an already-gated verdict and its own section conditions are untouched, so 34.13-10's and 34.13-12's 'delegate, never re-derive' rule survives byte-for-byte."
    - "FALSIFIED BRIEF (UI-SPEC): the D-25 contract asserts the library dropdown and free-space line 'depend only on `nativeInstallOn`/`libraryCount`, both known synchronously' and 'can render immediately alongside the loading row'. FALSE — 34.13-05's shipped formula is `libraryDropdown = !wineSection && nativeInstallOn && libraryCount > 1`, so both DO depend on the pending verdict. Rendering them immediately produces a dropdown that appears and then vanishes when eligibility resolves true. Resolved by suppressing both while pending; `platformRow` is preserved untouched because it genuinely IS synchronous. Recorded as a finding with evidence, not worked around."
    - "SUPERSEDED UPSTREAM GATE, narrowed not deleted: 34.13-10's D-06 truth ('the footer Install button carries no `disabled` attribute at all, in any render state') was written under the retired D-12 model — 34.13-10's own objective says the spinner 'belongs on the origin control (34.13-11), never on this footer button'. D-25 inverts that. The gate is narrowed to D-06's actual substance — the Install button is NEVER gated on size (`diskSize`, `free`, `spaceLeftAfter`, `notEnoughDiskSpace`, `getInstallInfo` remain zero) — and re-gated positively so `disabled` may ONLY be the D-25 pending flag."
    - "T-34.13-11-01 (no permanent wedge): every exit path resolves. A rejected or timed-out `isSteamBottleEligible` resolves fail-CLOSED to `bottleRequired: false` (UI-SPEC: 'a timeout is not modeled as its own error state (falls back to not eligible)'), so the footer Install button can never stay disabled forever. Proven by a spec that RED-fails when the `.catch` is removed."
    - "T-34.13-11-02 (no cross-game bleed): a late response for a DIFFERENT `appName` is discarded by the pure reducer, not merely by an effect cleanup flag. Proven by a two-deferred interleaving spec where the stale verdict says `bottleRequired: true` and the state must stay on the current game's answer."
    - "VACUITY BOUNDARY, stated not assumed. `34.13-VALIDATION.md` risk 1 ('store-flip ≠ dialog renders') applies in full. Three independent layers of evidence are shipped — (1) BEHAVIORAL: `EligibilityLoadingRow` is invoked as a plain function and its returned React-element graph is inspected, the repo's sanctioned no-jsdom idiom; (2) PURE: the probe state machine and `applyEligibilityPending` are exhaustively unit-tested; (3) STRUCTURAL: comment-stripped source gates pin the mount site, its guard and its ordering. NONE of the three is a pixel. That `InstallModal → SteamDialog → screen` actually paints the row is 34.13-13's blocking manual gate and must be recorded as such in the SUMMARY."
  artifacts:
    - path: "src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts"
      provides: "The D-25 eligibility state machine: the pure platform gate, the pure request/response/failure reducers with the stale-appName guard, `applyEligibilityPending`, and the thin `useSteamBottleEligibility` hook that composes them"
      contains: "applyEligibilityPending"
      min_lines: 90
    - path: "src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts"
      provides: "RED-proven unit coverage of the pure core: the fail-safe platform DISCRIMINATOR, the fail-closed rejection path, the stale-appName interleaving, and `applyEligibilityPending`'s exact field footprint with a saboteur that clears `platformRow`"
      contains: "DISCRIMINATOR"
      min_lines: 140
    - path: "src/frontend/screens/Library/components/InstallModal/SteamDialog/EligibilityLoadingRow.tsx"
      provides: "The D-25 loading row — the existing `buttonWithIcon` + `faSyncAlt`/`fa-spin` idiom relocated into the dialog. No stylesheet, no new token, no new spinner component."
      contains: "fa-spin"
      min_lines: 15
    - path: "src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/EligibilityLoadingRow.test.tsx"
      provides: "BEHAVIORAL evidence by direct component invocation — the returned element graph carries faSyncAlt + fa-spin + the reused copy — plus a DISCRIMINATOR that a text-only implementation fails"
      contains: "Checking install options"
      min_lines: 70
    - path: "src/frontend/screens/Library/components/InstallModal/index.tsx"
      provides: "The hook call, the `applyEligibilityPending` wrap around 34.13-05's verdict, the loading row mounted in the Steam wine-section slot, and `eligibilityPending` passed to SteamDialog"
      contains: "useSteamBottleEligibility"
    - path: "src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx"
      provides: "The one D-25 change to the dialog: an `eligibilityPending` prop driving `disabled` on the footer Install button"
      contains: "eligibilityPending"
    - path: "src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/steamDialogSource.test.ts"
      provides: "34.13-10's blanket 'no disabled Install button' gate narrowed to D-06's substance (never size-gated) plus the D-25 positive gate — the inversion recorded in the test file itself, not just in a SUMMARY"
      contains: "D-25"
    - path: "src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts"
      provides: "The quick-path no-probe census (recursive src/frontend allowlist + the InstallGameModal.ts zero-gate + the isOpen mount-gate), the loading-row mount/order/guard gates, and the origin-control proof-of-absence — each proven non-vacuous against a known-bad specimen"
      contains: "allowlist"
      min_lines: 150
  key_links:
    - from: "src/frontend/screens/Library/components/InstallModal/index.tsx"
      to: "src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts useSteamBottleEligibility()"
      via: "the ONLY non-test call site in the frontend — the probe cannot fire unless this component mounts, and this component only mounts when the modal is open"
      pattern: "useSteamBottleEligibility\\("
    - from: "src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts"
      to: "window.api.isSteamBottleEligible (34.13-07's channel)"
      via: "a single call inside the hook's useEffect, after first render — never before, never on the quick path"
      pattern: "window\\.api\\.isSteamBottleEligible\\("
    - from: "src/frontend/screens/Library/components/InstallModal/index.tsx"
      to: "src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts applyEligibilityPending()"
      via: "wraps 34.13-05's resolveSteamSectionGating() verdict before it becomes SteamDialog's `gating` prop — one pure function, not three inline `&& !pending` terms"
      pattern: "applyEligibilityPending\\("
    - from: "src/frontend/screens/Library/components/InstallModal/index.tsx"
      to: "src/frontend/screens/Library/components/InstallModal/SteamDialog/EligibilityLoadingRow.tsx"
      via: "mounted as the FIRST arm of the Steam wine-section conditional, so it occupies the exact DOM slot <WineSelector> would otherwise take (UI-SPEC D-25)"
      pattern: "<EligibilityLoadingRow"
    - from: "src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx"
      to: "the footer Install button's disabled attribute"
      via: "the `eligibilityPending` prop — the single piece of the retired D-12 busy contract that survives, relocated from the origin control to the dialog's own footer"
      pattern: "disabled=\\{eligibilityPending\\}"
---

<objective>
**This plan is a REWRITE. Its previous content planned the exact opposite of what it now delivers.**

Deliver D-25's in-dialog eligibility loading state: the options dialog opens instantly, the wine
section carries the loading state, and the dialog's own Install button stays disabled until the
verdict resolves.

Purpose:
- **D-25** — with an explicit "Install with options…" click the user has already asked for the
  dialog. Withholding it for up to `METADATA_FETCH_TIMEOUT_MS` (15000ms) is a dead click, not a
  safety measure. The dialog already reveals sections dynamically (D-17's macOS platform switch), so
  a loading state extends an existing mechanic rather than inventing one.
- **Remove the latency from the default path entirely.** D-25: *"The quick path needs no eligibility
  probe at all — the backend already resolves it inside `install()`."* This plan's job includes
  making sure no probe is ever reintroduced there, and it does that structurally (see
  `<the_inversion>`), not by asking future authors nicely.
- **Reuse, do not invent.** The `buttonWithIcon` + `faSyncAlt`/`fa-spin` idiom already exists twice
  in this repo (`MainButton.tsx:193-198`, `GameCard/index.tsx:253-256`, verified 2026-08-15). D-25
  RELOCATES it; it does not redesign it. The i18n key `gamelib:status.checkingSteamInstallOptions`
  is likewise reused verbatim — only its render location changes.

Output: two new source files (a pure-cored probe module + a stylesheet-free loading row), two new
test files, one narrowed upstream gate, one new cross-file gate file, and two surgical edits to
`InstallModal/index.tsx` and `SteamDialog/index.tsx`.

**Scope fences (all hard):**
1. Do NOT add a busy state, spinner or `disabled` term to `MainButton.tsx`, `GameCard/index.tsx` or
   `GameSubMenu/index.tsx`. See `<deliberate_exclusions>` for why zero residual origin-side state is
   needed, and Task 3's proof-of-absence gate which enforces it.
2. Do NOT edit `src/frontend/state/InstallGameModal.ts`. 34.13-08 is its sole editor. This plan only
   READS it, to assert it contains no eligibility probe.
3. Do NOT edit `steamSectionGating.ts` (34.13-05), `WineSelector/` (34.13-03), `steamPlatformRow.ts`
   (34.13-12), `public/locales/` (34.13-13), `src/common/`, `src/preload/`, or anything under
   `src/backend/`.
4. Do NOT add a new IPC channel. 34.13-07 already ships `isSteamBottleEligible` on BOTH runtimes.
5. Do NOT create a new stylesheet, CSS class, design token or hex value. The loading row composes
   `buttonWithIcon` and `fa-spin`, both of which already exist globally.
6. Do NOT write a rendering test. No jsdom, no `react-test-renderer`, no `@testing-library/*`, no
   simulated clicks. `EligibilityLoadingRow.tsx` is INVOKED as a plain function and its returned
   element graph is searched — the repo's own documented pattern.
</objective>

<the_inversion>
**Read this before anything else. The old version of this file, the outline's row for plan 11, and
`34.13-UI-SPEC.md`'s prior "D-12 Busy State Contract" all assert the OPPOSITE of what you are
building. So do 34.13-08's and 34.13-10's own comments and truths.**

| | Retired D-12 model | **Locked D-25 model (build this)** |
|---|---|---|
| When the probe runs | BEFORE the decision to open the dialog | AFTER the dialog has already opened |
| Where the busy state lives | on the ORIGIN control (`MainButton` / `GameCard`) | INSIDE the dialog, in the wine-section slot |
| What is `disabled` | the origin Install control | the dialog's OWN footer Install button |
| Half-rendered modal | forbidden — "no half-rendered modal" | **required** — the partial state is the design |
| Quick path | probed, up to 15s of spinning button | **no probe at all** — backend resolves in `install()` |
| The 15s worst case | still exists | still exists, but RELOCATED to time-in-dialog |

`34.13-UI-SPEC.md` §"D-25 Busy State Contract — SUPERSEDES the prior 'D-12 Busy State Contract'
(EXACTLY INVERTED)" is the binding contract (UI-SPEC approved 6/6, 2026-08-15). `34.13-CONTEXT.md`
D-25 (lines 277-286) is the locked decision. **D-13 is RETIRED and plan 34.13-04 is DELETED — its
number is a permanent gap. Nothing here may reference or depend on it.**

**If you find yourself editing an origin install control, you have reverted the inversion. Stop.**
</the_inversion>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-CONTEXT.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UI-SPEC.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VALIDATION.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-PATTERNS.md

`34.13-PATTERNS.md` is **PARTIALLY SUPERSEDED** — read its ⚠ banner first. Its
`AlwaysShowSteamInstallForm.tsx` / `alwaysShowInstallFormSetting.ts` section is a RETIRED TRAP
(D-26 cut the setting). Nothing in this plan touches either.

Knowledge graph is authoritative for orientation. Run
`graphify query "InstallModal dialog gating verdict steam bottle eligibility"` or
`graphify explain "InstallModal()"` before any broad source exploration. Every `file:line` anchor
below was verified by direct read on **2026-08-15** — note that `MainButton.tsx`'s line numbers have
drifted since the pre-amendment draft of this plan, so trust these, not the old ones.

<interfaces>
<!-- Extracted from the codebase and from upstream plans on 2026-08-15. Use these directly. -->

**The mount gate that makes the quick-path guarantee structural** —
`src/frontend/screens/Library/components/InstallModal/index.tsx:287-300`:
`export function InstallGameWrapper()` reads `useInstallGameModal()` and returns `<></>` when
`!installGameModalState.isOpen`, otherwise renders `<InstallModal appName runner gameInfo />`.
**The probe hook is called inside `InstallModal`, below this gate.** The quick path never sets
`isOpen: true`, so it cannot reach the probe. This is a load-bearing invariant, and Task 3 gates it.

**Host platform inside `InstallModal/index.tsx`** — `const { platform } = useContext(ContextProvider)`
at L45; `isMac`/`isWin`/`isLinux` are derived at L60-62 as `platform === 'darwin' | 'win32' | 'linux'`.
`platform` originates from `window.platform` (`src/preload/index.ts:26`,
`contextBridge.exposeInMainWorld('platform', process.platform)`) via `GlobalState.tsx:292`. Its
runtime type is `NodeJS.Platform | string`, so treat an unexpected value as UNKNOWN, never as
"not macOS".

**The busy idiom to relocate, verbatim** (`MainButton.tsx:193-198`, verified 2026-08-15):
a `<span className="buttonWithIcon">` containing `<FontAwesomeIcon icon={faSyncAlt} className="fa-spin" />`
followed by the `t(...)` call. `GameCard/index.tsx:253-256` is the same shape inside a
`<button className="svg-button iconDisabled" disabled>`. **Copy the `MainButton` shape** — the
UI-SPEC's D-25 code block reproduces exactly that span form. `faSyncAlt` comes from
`@fortawesome/free-solid-svg-icons` and `FontAwesomeIcon` from `@fortawesome/react-fontawesome`;
neither imports a stylesheet, which is why `MainButton.tsx` is invocable in Jest and why
`EligibilityLoadingRow.tsx` will be too.

**Frozen by 34.13-07 (`isSteamBottleEligible`) — CONSUME, DO NOT EDIT:**
- `window.api.isSteamBottleEligible(appName: string) => Promise<SteamBottleEligibilityVerdict>`
  (`src/common/types/ipc.ts`, preload export `makeHandlerInvoker('isSteamBottleEligible')` in
  `src/preload/api/steam.ts`). Registered on BOTH runtimes — `addHandler` in `src/backend/main.ts`
  and `ipcMain.handle` in `src/backend/sidecar/steamAuthFlowRegistration.ts`.
- The verdict shape is 34.13-01's `SteamBottleEligibilityVerdict` (`src/common/types.ts`). **Read
  `34.13-01-SUMMARY.md` and `34.13-07-SUMMARY.md` for the shipped field names** and write against
  those. The field this plan consumes is the eligibility boolean; if the shipped name differs from
  what you expect, follow the SUMMARY and record the delta.
- The channel name and the preload API-METHOD name are IDENTICAL here (34.13-07 records this
  explicitly). That is what makes Task 3's frontend census sound — a repo lesson exists about a
  census that greps the CHANNEL name and therefore misses every frontend caller. Not a hazard here,
  but the wiring test must state why.
- 34.13-07's seam guards `appName` with `/^\d+$/` BEFORE constructing a `SteamGame`, and fails
  closed to a not-eligible verdict. **Do not add a second renderer-side copy of that guard** — the
  repo's `redeemSteamKey` precedent (two copies of one trust boundary) is the pattern to avoid.

**Frozen by 34.13-05 (`steamSectionGating.ts`) — CONSUME, DO NOT EDIT:**
`SteamSectionGatingVerdict` has exactly six fields: `opens`, `platformRow`, `libraryDropdown`,
`wineSection`, `freeSpaceLine`, `forceWindowsViaBottle`. The formula that matters here is
`libraryDropdown = !wineSection && nativeInstallOn && libraryCount > 1` — this is what falsifies the
UI-SPEC's "the dropdown depends only on `nativeInstallOn`/`libraryCount`" claim (see
`<falsified_briefs>`). `resolveSteamSectionGating()` is the producer; `platformRow` is a
`SteamPlatformRowMode` string union, not a boolean.

**Shipped by 34.13-12 (`InstallModal/index.tsx`, Wave 6) — the surface this plan extends:**
`resolveSteamSectionGating(...)` is called in a `useMemo`; its result is the `gating` prop handed to
`<SteamDialog>`; the Steam `<WineSelector>` mount is gated on `steamGating.wineSection` (explicitly
NOT on `hasWine`). **Read `34.13-12-SUMMARY.md` before editing** — the identifier names
(`steamGating`, `isSteamManagedApp`, the fail-closed fallback path) are its to define, and this plan
must adopt whatever shipped rather than reintroducing its own.

**Shipped by 34.13-10 (`SteamDialog/index.tsx`, Wave 5) — the dialog this plan adds a state to:**
Structurally a `ThirdPartyDialog` clone: `DialogHeader` → `DialogContent` (`{children}`, then the
D-14 shared-bottle notice, then the D-02 library dropdown with the D-08 free-space line in its
`afterSelect` slot) → `DialogFooter` with a single `className="button is-secondary"` button labelled
`t('button.install')`. **That footer button currently carries no `disabled` attribute at all**, by
34.13-10's design under the retired model — see `<superseded_upstream_gate>`.

**Test infrastructure — Frontend project (`src/frontend/jest.config.js`):**
`rootDir: '../..'`, `roots: ['<rootDir>/src/frontend']`,
`testMatch: ['**/__tests__/**/*.test.tsx', '**/__tests__/**/*.test.ts']`, `resetMocks: true`,
`modulePaths: [compilerOptions.baseUrl]` (= `./src/`), `testEnvironment` at the Jest default
(`node`). **Read its docstring in full** — it is the authority for the direct-invocation idiom and
it records that adding `jest-environment-jsdom` is excluded from auto-fix. Single-file run:
`npx jest <path> --silent`.

**The direct-invocation idiom to copy** —
`src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamIncomplete.test.tsx`
(read in full; `MainButton.steamWaitingRestart.test.tsx` is its twin): module-level
`jest.mock('react-i18next', () => ({ useTranslation: () => ({ t: (_key, defaultValue) => defaultValue }) }))`,
the component imported AFTER the mocks, invoked as a plain function, and a local recursive
`collectText(node)` helper walking `el.props.children`. **Consequence to plan for: the mocked `t`
returns the ENGLISH DEFAULT, so assertions see `Checking install options…`, never the key.** Assert
the key itself by source gate.

**Comment stripper for source gates:** `src/backend/testUtils/stripSourceComments.ts` exports
`stripSourceComments(source: string)`; it resolves from the Frontend project via `modulePaths` as
`backend/testUtils/stripSourceComments` (live cross-project precedent:
`src/frontend/state/__tests__/GlobalStateRefreshCacheGuard.test.ts`). **Mandatory for every
source-text gate in this plan** — this plan's own comments legitimately contain
`isSteamBottleEligible` and `checkingSteamInstallOptions` while explaining the rules, and an
unstripped regex would be satisfied by the comment alone.

**i18n:** `gamelib:status.checkingSteamInstallOptions` → `Checking install options…` (U+2026, matching
`'Setting up Steam…'`). It IS present in the UI-SPEC's Copywriting Contract table (L138, marked
"relocated, not renamed"). Ship it as an inline default —
`t('gamelib:status.checkingSteamInstallOptions', 'Checking install options…')` — and do NOT touch
`public/locales/en/gamelib.json`; 34.13-13 is this phase's single catalog writer. `meta/lintTranslations.ts`
compares non-English catalogs against the English one; it does not scan source for used-but-unlisted
keys, so `pnpm lint-translations:gamelib` stays green with an inline-only default. Same precedent as
34.13-10's `gamelib:steam.install.sharedBottleNotice`.
</interfaces>
</context>

<falsified_briefs>
Three upstream claims were checked against source and upstream plans this session. Record all three
in the SUMMARY as findings — the repo carries a ledgered lesson that a stale cross-reference rots
silently and that a plan can invent a constraint that does not exist.

1. **UI-SPEC D-25 Busy State Contract: "The library dropdown / free-space line are NOT gated by this
   loading state — those depend only on `nativeInstallOn`/`libraryCount`, both known synchronously at
   open time; they can render immediately alongside the loading row." FALSE.**
   34.13-05's shipped formula is `libraryDropdown = !wineSection && nativeInstallOn && libraryCount > 1`
   (quoted verbatim in 34.13-10's own `<layout_contract>`), and `wineSection` depends entirely on the
   pending `bottleRequired`. So on a macOS host with native install ON and 2+ libraries, following the
   UI-SPEC literally renders a dropdown that VANISHES when eligibility resolves true — a section that
   appears and then disappears, which is worse than the flicker-free snap D-25 asks for.
   **Resolution:** suppress `libraryDropdown` and `freeSpaceLine` (alongside `wineSection`) while
   pending; preserve `platformRow` untouched, because it genuinely is synchronous. This is NOT a
   decision reversal — D-25 says nothing about the dropdown, and the UI-SPEC's own *intent* ("don't
   gate what is already known") is honoured, only its factually-wrong premise is corrected. Task 1
   pins the exact field footprint with a saboteur. Hand the UI-SPEC amendment forward.

2. **34.13-10 asserts "This dialog's Install button is never disabled" and gates it as an absolute.**
   True under the model it was written against, and 34.13-10 says so in its own words: *"the
   eligibility check runs before the dialog opens, so the spinner belongs on the origin control
   (34.13-11), never on this footer button."* D-25 inverts the premise, so the gate is a casualty of
   the same inversion. See `<superseded_upstream_gate>` for the exact narrowing — D-06's substance
   (never size-gated) is preserved and strengthened, not dropped.

3. **The plan outline's row for 34.13-11 ("D-12 busy state on the origin controls … `GameCard`,
   `MainButton`, `GameSubMenu`") is fully retired**, as is the prior content of this very file. The
   outline was written pre-amendment; `34.13-CONTEXT.md`'s plan-set impact table already records
   "34.13-11 | Rewritten — D-12 busy state moves inside the dialog and off the default path (D-25)".
   Do not reconcile against the outline.
</falsified_briefs>

<superseded_upstream_gate>
**This plan is authorised to narrow exactly ONE upstream gate, and only this one.**

`SteamDialog/__tests__/steamDialogSource.test.ts` (34.13-10) asserts the footer Install button
carries **no `disabled` attribute at all, in any render state**. D-25 requires
`disabled={eligibilityPending}`. That blanket form is a proxy that over-reaches D-06's actual
substance.

- **D-06's substance — PRESERVED and re-gated positively:** the Install button is never gated on
  *size*. `diskSize`, `free`, `spaceLeftAfter`, `notEnoughDiskSpace`, `getInstallInfo` must all still
  be ZERO in `SteamDialog/index.tsx`'s comment-stripped source. `SteamLibraryManager.getInstallInfo()`'s
  stub can still never hang this dialog.
- **What replaces the blanket:** `disabled` may appear on the footer Install button ONLY as the D-25
  pending flag. Task 3 asserts both directions — the positive form is present exactly once, and no
  other expression feeds it.
- **Do NOT delete the spec.** Rewrite it in place, keep its `describe` block, and add a comment
  recording that D-25 inverted its premise, with the CONTEXT.md line reference. A deleted gate is
  indistinguishable from a gate that never existed.
- **Do NOT edit any other assertion in that file**, and do not touch `34.13-10-PLAN.md` or
  `34.13-10-SUMMARY.md`. The SUMMARY of THIS plan carries the reconciliation note so a verifier does
  not score the narrowing as a regression against 34.13-10's `must_haves`.
</superseded_upstream_gate>

<deliberate_exclusions>
Named so an executor does not "helpfully" widen scope. Each is a decision, not an oversight.

- **Any busy state on `MainButton.tsx`, `GameCard/index.tsx` or `GameSubMenu/index.tsx` — EXCLUDED,
  and justified against D-25 rather than assumed.** There are exactly two user intents. The *quick*
  intent fires no probe at all (D-25: "the backend already resolves it inside `install()`"), so there
  is no latency for an origin spinner to cover. The *options* intent opens the dialog synchronously
  on the same tick as the click, so the origin control's job is finished before any await begins —
  a spinner there would render for zero frames. **No residual origin-side state is needed.** Task 3
  enforces this by proof of absence, so a future edit that reintroduces it fails a gate rather than
  passing silently.
- **The split button itself, the "Install with options…" context-menu item, and `GameSubMenu`'s
  relabelled entry (D-21/D-27)** — owned by 34.13-08's rewrite. This plan renders what happens AFTER
  the caret is clicked, never the caret.
- **The D-24 degrade-into-options notice** — a separate contract (`UI-SPEC` §"D-24 Degrade-into-Options
  Contract") driven by a *local* validity check (`window.api.checkDiskSpace`), explicitly not a Steam
  CM round trip. Different mechanism, different owner.
- **A retry affordance on eligibility failure.** The UI-SPEC is explicit: "a timeout is not modeled as
  its own error state (falls back to 'not eligible')". Fail-closed and silent is the specified
  behavior; inventing a retry button would be new UI the spec did not ask for.
- **`SteamBottleSetup.tsx`'s own `isSteamBottleEligible` call (34.13-09)** — a post-install consumer
  on a different surface. It stays, and Task 3's census allowlist names it explicitly so it is
  covered rather than silently tolerated.
- **Deleting any now-unused D-12 remnant in `state/InstallGameModal.ts`** (e.g. a
  `useSteamInstallOptionsCheck` store left over from the retired model). That file is 34.13-08's.
  If Task 3's census finds such a store with zero consumers, **record it in the SUMMARY as dead code
  from the retired model and hand it to 34.13-13** — do not delete it here.
</deliberate_exclusions>

<upstream_contract>
**Confirm all four before writing any code. If a check fails, STOP and report a dependency failure —
do not declare the missing piece here and do not work around it.**

1. **34.13-07 (Wave 3) — the channel.** `grep -n "isSteamBottleEligible" src/preload/api/steam.ts`
   returns a `makeHandlerInvoker('isSteamBottleEligible')` export, and the same symbol appears in
   `src/common/types/ipc.ts` with an `(appName: string) => Promise<SteamBottleEligibilityVerdict>`
   signature.
2. **34.13-05 (Wave 2) — the verdict type.**
   `grep -n "export interface SteamSectionGatingVerdict" -A 10 src/frontend/screens/Library/components/InstallModal/steamSectionGating.ts`
   shows the six fields. If the shipped names differ, follow the shipped names and record the delta.
3. **34.13-10 (Wave 5) — the dialog exists** at
   `src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx` with a `gating` prop
   and a single footer Install button.
4. **34.13-12 (Wave 6) — the dialog is MOUNTED and the verdict is COMPUTED.**
   `grep -n "resolveSteamSectionGating(\|<SteamDialog" src/frontend/screens/Library/components/InstallModal/index.tsx`
   returns both. If `<SteamDialog` is absent, 34.13-12 has not landed and this plan has nothing to
   attach to — **blocking dependency failure, stop.**

**BLOCKING CONTRADICTION CHECK (do this before Task 1, not at the end):**
`grep -c "isSteamBottleEligible" src/frontend/state/InstallGameModal.ts` must return `0`.
A non-zero count means 34.13-08 shipped an eligibility probe on the install chokepoint, which
contradicts D-25 ("the quick path needs no eligibility probe at all" + "the dialog opens INSTANTLY").
**Do not delete it and do not work around it — STOP and report the contradiction to planning**, with
the matching lines quoted. That file is 34.13-08's and the conflict is a decision-level one.
</upstream_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The eligibility probe — a pure state machine with a razor-thin hook over it</name>

  <files>src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts, src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts</files>

  <read_first>
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-CONTEXT.md`
      lines 277-286 — **D-25 verbatim**, the locked decision this task implements.
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UI-SPEC.md`
      lines 479-531 — the **D-25 Busy State Contract**, including the macOS-only scope, the
      fail-closed timeout rule, and the 15s worst case.
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-01-SUMMARY.md`
      and `34.13-07-SUMMARY.md` — the SHIPPED `SteamBottleEligibilityVerdict` field names and the
      shipped preload export. Write against these, not against a predicted shape.
    - `src/frontend/screens/Library/components/InstallModal/steamSectionGating.ts` — READ ONLY. Read
      `SteamSectionGatingVerdict` in full and confirm the `libraryDropdown` formula includes
      `!wineSection`; that fact is the evidence behind falsified brief 1. Do not edit.
    - `src/frontend/screens/Library/components/InstallModal/index.tsx` lines 40-70 — how `platform`
      arrives (`useContext(ContextProvider)`, L45) and how `isMac`/`isWin`/`isLinux` are derived
      (L60-62). Your platform gate must consume the same `platform` value, not a second source.
    - `src/frontend/state/SteamBottleSetup.ts` (~51 lines, full file) — the repo's precedent for a
      small state module whose predicate is ONE exported pure function, and the docstring reason it
      exists as one.
    - `src/frontend/jest.config.js` — the docstring in full. It is the authority for this phase's
      no-jsdom testing posture.
    - `src/backend/testUtils/stripSourceComments.ts` — the doc comment in full.
  </read_first>

  <behavior>
    - `shouldProbeEligibility({ platform, runner })` returns `false` for `runner !== 'steam'`;
      returns `false` when `platform` is exactly `'win32'` or exactly `'linux'`; returns `true` for
      `'darwin'`, and ALSO `true` for `undefined`, `''` and any unrecognised string.
    - `initialEligibilityState({ platform, runner, appName })` yields a PENDING state when
      `shouldProbeEligibility` is true, and an already-RESOLVED state with the not-eligible verdict
      otherwise — synchronously, with no promise involved.
    - `applyEligibilityResponse(state, { appName, verdict })` resolves the state when `appName`
      matches the state's; returns the state UNCHANGED when it does not.
    - `applyEligibilityFailure(state, { appName })` resolves fail-closed to not-eligible on a match,
      and is a no-op on a mismatch.
    - `isEligibilityPending(state)` is `true` only in the pending phase.
    - `applyEligibilityPending(verdict, pending)` returns `verdict` unchanged when `pending` is
      false; when true, returns a copy with `wineSection`, `libraryDropdown` and `freeSpaceLine`
      forced `false` and every other field — `opens`, `platformRow`, `forceWindowsViaBottle` —
      byte-identical to the input.
  </behavior>

  <action>
Create the module and its spec. **Write the spec first and observe it RED.**

**Part A — `__tests__/steamEligibilityProbe.test.ts`, RED first.**

Header comment records: this is Phase 34.13 **D-25**, it is the EXACT INVERSION of the retired D-12
contract, the worst case is **15 seconds on a cold metadata cache (`METADATA_FETCH_TIMEOUT_MS` =
15000ms)** and under D-25 that time is spent with the dialog OPEN and its own Install button
disabled, not with an origin button spinning. Also record the vacuity boundary: these are pure
functions; nothing here proves anything renders.

Specs:
  1. `probes on macOS` — `platform: 'darwin'`, `runner: 'steam'` → `shouldProbeEligibility` true and
     `initialEligibilityState` is pending.
  2. `skips on Windows and on Linux` — `'win32'` and `'linux'` → false, and the initial state is
     already resolved not-eligible, so `isEligibilityPending` is false. This is the row that keeps
     the dialog fully-resolved on Windows/Linux (UI-SPEC: matrix rows 5-8 render immediately).
  3. **`DISCRIMINATOR: an unknown platform still asks the backend — the gate is written as an
     explicit win32/linux skip, never as !== darwin`** — `undefined`, `''` and `'freebsd'` all
     return `true`. Name the discriminator in the title. An implementation written as
     `platform === 'darwin'` or `platform !== 'darwin'` passes specs 1 and 2 and fails ONLY this
     one, and the inverted form is the historical shape of a cold-cache "not eligible" false
     negative (`.planning/debug/steam-bottle-guided-setup-never-fires.md`).
  4. `a non-steam runner never probes` — `runner: 'gog'` with `platform: 'darwin'` → false.
  5. `a resolved verdict for the current game ends the pending phase` — `applyEligibilityResponse`
     with a matching `appName` → not pending, and `bottleRequired` equals the verdict's value, for
     both `true` and `false`.
  6. **`DISCRIMINATOR: a late verdict for a DIFFERENT game is discarded`** — start pending for
     `'440'`, apply a response for `'570'` carrying bottle-required `true`, assert the state is
     STILL pending and still carries no verdict; then apply `'440'`'s own response and assert it
     lands. An implementation that ignores `appName` passes every other spec and fails only this
     one. This is T-34.13-11-02.
  7. `a rejected probe resolves fail-closed, it does not stay pending forever` —
     `applyEligibilityFailure` with a matching `appName` → not pending, bottle-required `false`.
     Comment it against the UI-SPEC line ("a timeout is not modeled as its own error state").
     This is T-34.13-11-01.
  8. `applyEligibilityPending is identity when not pending` — a verdict with every field set to a
     distinctive value round-trips deep-equal.
  9. `applyEligibilityPending suppresses exactly three fields while pending` — assert
     `wineSection`/`libraryDropdown`/`freeSpaceLine` are `false` AND assert `platformRow`,
     `opens` and `forceWindowsViaBottle` are strictly equal to the input's values.
 10. **`DISCRIMINATOR: the platform row is NOT suppressed while pending`** — with `platformRow` set
     to a selectable mode, assert it survives. A saboteur that clears the whole verdict passes spec
     9's first half and fails here; the platform row is synchronously known (D-03/D-17/D-19) and
     hiding it would make the dialog look empty on open, which is the D-20 corner the phase already
     decided not to create.
 11. `source gates` over `stripSourceComments(readFileSync(<the module>))`:
     - ZERO matches of `is_mac_native`, `mac_arch`, `steamPlatformsCaptured` — the D-09
       no-renderer-re-derivation rule. **Prove non-vacuous in the same spec** by asserting the same
       three searches DO hit an inline known-bad specimen string.
     - `window.api.isSteamBottleEligible(` appears exactly once.
     - ZERO matches of `/^\\\\d\+\$/`-style appId validation — the guard is 34.13-07's, and a second
       renderer copy would drift (T-34.13-11-05).
     - the effect's cleanup exists: the stripped source contains a `return () =>` inside the
       `useEffect`, and a `cancelled` (or equivalently-named) flag. Belt-and-braces over spec 6's
       reducer guard, which is the primary control.

Run the spec against the empty tree and **capture the RED output verbatim**. A RED that does not
name specs 3, 6 and 10 means the discriminators are not wired — fix the spec, do not proceed.

**Part B — `steamEligibilityProbe.ts`.**

Export the pure functions named in `<behavior>` plus a small discriminated state type with a phase
field (`'pending' | 'resolved'`) carrying `appName` and, when resolved, `bottleRequired`. Then export
one thin hook, `useSteamBottleEligibility({ platform, runner, appName })`, which:
  - holds the state in `useState`, seeded by `initialEligibilityState(...)` — so the FIRST render is
    already correct and nothing is awaited before paint (this is what "opens INSTANTLY" means
    mechanically);
  - re-seeds when `appName`, `runner` or `platform` changes;
  - in a `useEffect` keyed on those three, returns early unless the state is pending, otherwise calls
    `window.api.isSteamBottleEligible(appName)` and funnels resolve → `applyEligibilityResponse`,
    reject → `applyEligibilityFailure`, both guarded by a cleanup flag so an unmounted dialog never
    calls `setState`;
  - returns `{ pending: isEligibilityPending(state), bottleRequired }`.

Comment requirements, all load-bearing:
  - the file header states this is **D-25** and that it is the EXACT INVERSION of the retired D-12
    contract, citing `34.13-CONTEXT.md` D-25 and the UI-SPEC's superseding section by name;
  - the platform gate carries a comment that it is written as an explicit `'win32' || 'linux'` skip
    and **never** as `!== 'darwin'`, so an unknown platform asks the backend — the same convention
    34.13-08 uses, for the same reason;
  - the `.catch` carries the UI-SPEC's fail-closed rule and the 15000ms bound verbatim;
  - the reducer's `appName` check states it is the PRIMARY stale-response control and the effect
    cleanup is secondary.

Do NOT edit the test file in Part B. Do NOT import anything from `state/InstallGameModal.ts`.
  </action>

  <verify>
    <automated>npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts --silent && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - Part A: the spec file run against the pre-implementation tree **exits non-zero**, and the
      captured RED output names specs 3, 6 and 10 by title. Recorded verbatim in the SUMMARY.
    - Part B: `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts --silent`
      exits 0.
    - These spec-title substrings appear verbatim in the file: `DISCRIMINATOR: an unknown platform
      still asks the backend`, `DISCRIMINATOR: a late verdict for a DIFFERENT game is discarded`,
      `DISCRIMINATOR: the platform row is NOT suppressed while pending`.
    - The platform gate is written positively. Over comment-stripped source of
      `steamEligibilityProbe.ts`: `grep -c "!== 'darwin'"` → `0` and
      `grep -c "'win32'"` → `≥ 1` and `grep -c "'linux'"` → `≥ 1`.
    - No renderer re-derivation. Over comment-stripped source:
      `grep -cE "is_mac_native|mac_arch|steamPlatformsCaptured"` → `0`.
    - The 15s worst case is verbatim, not paraphrased:
      `grep -c 'METADATA_FETCH_TIMEOUT_MS' src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts` → `≥ 1`
      and `grep -c '15000' <same file>` → `≥ 1`.
    - Exactly one IPC call site: over comment-stripped source,
      `grep -c 'window.api.isSteamBottleEligible('` → `1`.
    - No cross-plan file was touched:
      `git status --porcelain src/frontend/state/ src/frontend/screens/Library/components/InstallModal/steamSectionGating.ts src/frontend/screens/Library/components/InstallModal/WineSelector/ src/common/ src/backend/ src/preload/ public/locales/ package.json pnpm-lock.yaml` → empty.
    - `git diff --name-only --diff-filter=A src/frontend` lists exactly the two files of this task.
    - `npx tsc --noEmit` exits 0. `npx eslint src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts` exits 0.
  </acceptance_criteria>

  <done>The D-25 eligibility state machine exists as pure, RED-proven functions with a razor-thin hook over them: it is pending only on macOS (fail-safe on an unknown platform), resolves fail-closed on rejection so the dialog can never wedge, discards a late verdict aimed at a different game, and suppresses exactly three verdict fields while pending while leaving the synchronously-known platform row alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The loading row — the relocated spin idiom, proven by direct component invocation</name>

  <files>src/frontend/screens/Library/components/InstallModal/SteamDialog/EligibilityLoadingRow.tsx, src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/EligibilityLoadingRow.test.tsx</files>

  <read_first>
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UI-SPEC.md`
      lines 497-508 — the D-25 markup block. It is the literal shape to reproduce: a
      `<span className="buttonWithIcon">` wrapping `<FontAwesomeIcon icon={faSyncAlt} className="fa-spin" />`
      and the `t('gamelib:status.checkingSteamInstallOptions', 'Checking install options…')` call.
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UI-SPEC.md`
      lines 535-594 — Reuse Boundaries. Confirm for yourself that this row is listed as
      "relocated existing idiom, new placement only" and NOT as a new spinner component.
    - `src/frontend/screens/Game/GamePage/components/MainButton.tsx` lines 180-200 — the idiom being
      relocated, verified 2026-08-15 (`buttonWithIcon` span at L193, `fa-spin` at L195). Copy this
      shape, not `GameCard`'s `svg-button iconDisabled` button form — the UI-SPEC reproduces the
      span form.
    - `src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamIncomplete.test.tsx`
      (full file) — the harness to copy: the module-level `jest.mock('react-i18next', ...)` whose
      `t` returns the DEFAULT VALUE, the import-after-mocks ordering, and the recursive
      `collectText(node)` helper.
    - `src/frontend/screens/Library/components/InstallModal/WineSelector/index.tsx` lines 1-30 —
      the zero-stylesheet precedent: a component in this tree that ships no CSS file at all and
      consumes only global/parent-scoped classes. This row does the same.
    - `src/frontend/screens/Humble/Keys/index.tsx` lines 20-30 — the live
      `t('gamelib:key', 'English default')` namespace-prefix precedent.
  </read_first>

  <behavior>
    - Invoking `EligibilityLoadingRow` as a plain function returns an element graph whose collected
      text contains `Checking install options…`.
    - That same graph contains a `FontAwesomeIcon` element whose `props.icon` is `faSyncAlt` and
      whose `props.className` is `fa-spin`.
    - The outermost returned element's `props.className` is `buttonWithIcon`.
    - The component takes no props that alter any of the above.
  </behavior>

  <action>
**Part A — write `__tests__/EligibilityLoadingRow.test.tsx` first and observe it RED.**

Copy the harness from `MainButton.steamIncomplete.test.tsx`: mock `react-i18next` at module level so
`t` returns its default-value argument, import the component AFTER the mocks, add a local recursive
`collectText(node)` that walks `props.children`, and add a `findIcon(tree)` helper that recursively
locates the element whose `props.className` is `fa-spin` and returns it so `props.icon` can be
inspected directly. **`collectText` only descends `children`, so `className` and `icon` are NOT text
— read them as props, never by string-matching the collected text.**

Header comment records three things: (a) this is Phase 34.13 **D-25** — the loading state lives
INSIDE the dialog, the exact inversion of the retired D-12 contract that put it on the origin button;
(b) the component is INVOKED as a plain function and only its returned React-element object graph is
inspected — nothing is rendered, there is no DOM, no click is simulated — quoting
`src/frontend/jest.config.js`'s docstring as the authority; (c) the vacuity boundary — this proves
the element graph, not pixels, and it does not prove the dialog MOUNTS this component; that is
Task 3's structural gate plus 34.13-13's blocking manual gate.

Specs:
  1. `renders the reused copy` — collected text contains `Checking install options…` (the English
     default; the mocked `t` never surfaces the key).
  2. **`DISCRIMINATOR: the spinner is part of the row — a text-only loading row fails`** — assert
     `findIcon(tree)` is non-null, that its `props.icon` is the imported `faSyncAlt` (compare against
     the imported symbol, not a string), and that its `props.className` is exactly `fa-spin`. Name
     it as the discriminator: an implementation that returns only the sentence passes spec 1 and
     fails only here, and a static sentence is not a loading state.
  3. `uses the shared buttonWithIcon wrapper, not a bespoke class` — the returned element's
     `props.className` is exactly `buttonWithIcon`.
  4. `source gates` over `stripSourceComments(readFileSync(<the component>))`:
     - `gamelib:status.checkingSteamInstallOptions` appears exactly once — the key survives even
       though the mocked `t` hides it from every behavioral assertion above.
     - ZERO matches of `CircularProgress`, `<Spinner`, `@mui/material` — no alternative spinner and
       no new dependency. **Prove non-vacuous** by asserting the same searches DO hit an inline
       known-bad specimen string.
     - ZERO matches of `import './` and ZERO of `.scss` / `.css` — the component imports no
       stylesheet, which is both the zero-stylesheet rule AND the reason this test can invoke it.
     - ZERO matches of `/#[0-9a-fA-F]{3,8}\b/` — no hardcoded hex.

RED-prove against the current tree and capture the output verbatim. A RED that does not name spec 2
means `findIcon` is not reading props — fix the harness, do not proceed.

**Part B — write `EligibilityLoadingRow.tsx`.**

A single default-exported function component, no props, importing only `FontAwesomeIcon`,
`faSyncAlt` and `useTranslation`. Return the UI-SPEC's markup verbatim. Use the `gamelib:` prefix on
the key with the inline English default `Checking install options…` (U+2026, matching
`'Setting up Steam…'`), and comment that the prefix is deliberate and overrides whatever namespace
the surrounding `useTranslation` is bound to.

File header comment must state: this is the D-25 relocation of an EXISTING idiom
(`MainButton.tsx:193-198` / `GameCard/index.tsx:253-256`), it is **not** a new spinner component,
it ships no stylesheet by design (the `WineSelector` precedent), and it renders in the DOM slot the
Steam `<WineSelector>` would otherwise occupy — mounted by `InstallModal/index.tsx`, never by
`SteamDialog` itself, because the wine section arrives through `{children}`.

Do NOT edit the test file in Part B. Do NOT create a `.css`/`.scss` file. Do NOT touch
`public/locales/`.
  </action>

  <verify>
    <automated>npx jest src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/EligibilityLoadingRow.test.tsx --silent && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - Part A: the spec run against the pre-implementation tree **exits non-zero** and the captured
      RED output names spec 2 (`DISCRIMINATOR: the spinner is part of the row`). Recorded verbatim
      in the SUMMARY.
    - Part B: the same command exits 0.
    - The spec-title substring `DISCRIMINATOR: the spinner is part of the row` appears verbatim in
      the test file.
    - The icon assertion reads props, not text: the file contains `props.icon` and `props.className`
      and does NOT assert `fa-spin` via `collectText`.
    - No rendering was introduced:
      `grep -rniE 'jsdom|react-test-renderer|@testing-library|ReactDOM|\.render\(|fireEvent|simulate' src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/EligibilityLoadingRow.test.tsx`
      → no matches.
    - Idiom reuse, over comment-stripped `EligibilityLoadingRow.tsx`: `fa-spin` count → `1`,
      `faSyncAlt` count → `2` (the import and the usage), `buttonWithIcon` count → `1`,
      `CircularProgress` count → `0`.
    - No stylesheet and no token invented: `ls src/frontend/screens/Library/components/InstallModal/SteamDialog/`
      contains no `.css` or `.scss` entry, and
      `grep -cE '#[0-9a-fA-F]{3,8}' src/frontend/screens/Library/components/InstallModal/SteamDialog/EligibilityLoadingRow.tsx` → `0`.
    - The key ships inline and the catalog is untouched:
      `grep -c "gamelib:status.checkingSteamInstallOptions" src/frontend/screens/Library/components/InstallModal/SteamDialog/EligibilityLoadingRow.tsx` → `1`,
      `git status --porcelain public/locales/` → empty, and `pnpm lint-translations:gamelib` exits 0.
    - `git diff --name-only --diff-filter=A src/frontend` lists exactly the two files of this task
      plus Task 1's two.
    - `npx tsc --noEmit` exits 0. `npx eslint src/frontend/screens/Library/components/InstallModal/SteamDialog/EligibilityLoadingRow.tsx` exits 0.
  </acceptance_criteria>

  <done>`EligibilityLoadingRow` exists as a stylesheet-free component that returns the relocated `buttonWithIcon` + `faSyncAlt`/`fa-spin` idiom with the reused `gamelib:status.checkingSteamInstallOptions` copy — proven BEHAVIORALLY by direct invocation of the component and inspection of its returned element graph, with a discriminator that a text-only loading row fails.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the dialog — mount the row, disable the footer Install, and gate the quick path shut</name>

  <files>src/frontend/screens/Library/components/InstallModal/index.tsx, src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx, src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/steamDialogSource.test.ts, src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts</files>

  <read_first>
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-12-SUMMARY.md`
      — **read this first.** 34.13-12 owns `InstallModal/index.tsx`; adopt its shipped identifier
      names (`steamGating`, `isSteamManagedApp`, the fail-closed fallback) rather than inventing
      parallel ones. If the SUMMARY and the source disagree, follow the source and record it.
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-10-SUMMARY.md`
      — the shipped `SteamDialog` prop contract and the exact form of its footer Install button.
    - `src/frontend/screens/Library/components/InstallModal/index.tsx` — lines 40-70 (`platform`,
      `isMac`/`isWin`/`isLinux`), the `resolveSteamSectionGating` `useMemo`, the Steam
      `<WineSelector>` mount inside the Steam `{children}` block, the `<SteamDialog>` mount, and
      **lines 287-300 (`InstallGameWrapper`'s `if (!installGameModalState.isOpen) return <></>`)** —
      that early return is the structural guarantee this plan rests on and Task 3 gates it.
    - `src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx` — the full
      component, in particular the props interface and the `DialogFooter` Install button.
    - `src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/steamDialogSource.test.ts`
      — the whole file, and specifically the spec asserting the Install button carries no `disabled`
      attribute. That is the ONE gate this plan is authorised to narrow; see
      `<superseded_upstream_gate>`.
    - `src/frontend/state/InstallGameModal.ts` — READ ONLY. Confirm the
      `grep -c "isSteamBottleEligible"` → `0` contradiction check from `<upstream_contract>` before
      writing anything. Do not edit.
    - `src/frontend/screens/Game/GamePage/components/MainButton.tsx`,
      `src/frontend/screens/Library/components/GameCard/index.tsx`,
      `src/frontend/screens/Game/GameSubMenu/index.tsx` — READ ONLY, enough to confirm for yourself
      that none of them carries a Steam eligibility busy state. If one does, STOP and report: the
      retired D-12 wiring was built somewhere and D-25 is being violated.
    - `src/frontend/state/__tests__/GlobalStateRefreshCacheGuard.test.ts` lines 30-90 — the live
      cross-project `stripSourceComments` + `readFileSync` source-gate idiom and its import path
      `backend/testUtils/stripSourceComments`.
  </read_first>

  <action>
**Part A — `InstallModal/index.tsx` (three small edits, adopting 34.13-12's shipped names).**

  1. **Call the hook, unconditionally, at the top level with the other hooks.** React forbids
     conditional hook calls, so `useSteamBottleEligibility({ platform, runner, appName })` is called
     for every runner; the *IPC* is gated inside by `shouldProbeEligibility`, which returns `false`
     for any non-Steam runner (Task 1 spec 4). Comment this explicitly so a reader does not "fix" it
     into a conditional call. `platform` is the L45 context value — do not introduce a second source.
  2. **Wrap the verdict, do not add inline `&& !pending` terms.** Where 34.13-12 computes
     `resolveSteamSectionGating(...)`, pass its result through
     `applyEligibilityPending(verdict, eligibility.pending)` and hand the WRAPPED value onward as the
     `gating` prop. One pure function, one call site. Feed `bottleRequired` into
     `resolveSteamSectionGating` from the hook's resolved value (fail-closed `false` while pending —
     Task 1 makes this the module's job, not this file's). Comment the falsified brief: the UI-SPEC
     claims the dropdown and free-space line can render immediately, but 34.13-05's formula
     `libraryDropdown = !wineSection && nativeInstallOn && libraryCount > 1` makes both depend on the
     pending verdict, so rendering them early produces a section that appears and then vanishes.
     Cite `<falsified_briefs>` item 1 by name.
  3. **Mount the loading row in the wine-section slot.** In the Steam `{children}` block, make the
     wine region a conditional whose FIRST arm is `eligibility.pending && <EligibilityLoadingRow />`
     and whose second arm is the existing `steamGating.wineSection && <WineSelector ... />` mount.
     Order matters and is gated: the loading row occupies the exact DOM position `<WineSelector>`
     would otherwise take (UI-SPEC D-25). Do not change any prop on the existing `<WineSelector>`
     mount. Comment that the two are mutually exclusive twice over — by this ternary AND by
     `applyEligibilityPending` forcing `wineSection: false` — belt and braces, deliberately.
  4. Pass `eligibilityPending={eligibility.pending}` to `<SteamDialog>`.

Change NOTHING on the four non-Steam dialog branches, and do not touch `platformSelection()`,
`steamPlatformRow.ts` or the `InstallGameWrapper` early return.

**Part B — `SteamDialog/index.tsx` (one change only).**

Add `eligibilityPending: boolean` to the props interface and set `disabled={eligibilityPending}` on
the single footer Install button. **Nothing else in this file changes** — no section condition, no
new import, no state, no effect. Comment the button with: this is D-25's one surviving piece of the
retired D-12 busy contract, relocated from the origin control to this footer; and D-06 still holds —
this button is never gated on size, and `getInstallInfo` is still never called.

**Part C — narrow the superseded gate in `SteamDialog/__tests__/steamDialogSource.test.ts`.**

Rewrite IN PLACE the spec asserting the Install button carries no `disabled` attribute. Keep its
`describe` block. The narrowed spec asserts, over comment-stripped `SteamDialog/index.tsx`:
  - `disabled=` appears exactly once in the file, and the character immediately following it begins
    `{eligibilityPending}` — the pending flag is the ONLY thing that can disable this button;
  - ZERO matches of `diskSize`, `free`, `spaceLeftAfter`, `notEnoughDiskSpace`, `getInstallInfo` —
    D-06's substance, preserved and now asserted positively rather than by proxy. **Prove
    non-vacuous** against an inline known-bad specimen containing `disabled={notEnoughDiskSpace}`.
Add a comment above the spec recording that its predecessor asserted the OPPOSITE, that D-25
(`34.13-CONTEXT.md` lines 277-286) inverted the premise, and that 34.13-10's objective itself
attributed the spinner to the origin control under the retired model. **Touch no other spec in this
file.**

**Part D — the new cross-file gate file `__tests__/steamEligibilityWiring.test.ts`.**

Header comment records: `InstallModal/index.tsx` and `SteamDialog/index.tsx` both reach the UI kit's
transitive stylesheet imports and can never be imported by a test in this project, so these are
comment-stripped SOURCE gates. Their vacuity boundary is explicit — they prove the code says what it
must, not that anything paints. Every target is read with `readFileSync` and passed through
`stripSourceComments` before assertion.

  **Group A — the quick path can never fire a probe.** This is D-25's load-bearing negative.
    A1. `src/frontend/state/InstallGameModal.ts` contains ZERO matches of `isSteamBottleEligible`.
        Comment it: a non-zero count is a decision-level contradiction with D-25, not a bug to patch
        here — that file is 34.13-08's.
    A2. **The mount gate still exists**: `InstallModal/index.tsx` contains
        `if (!installGameModalState.isOpen)` (or whatever 34.13-12 shipped as the equivalent early
        return in `InstallGameWrapper`) followed by an empty-fragment return. Comment that this early
        return is WHY the quick path cannot probe: the component that owns the hook does not mount
        unless the modal is open, and quick install never opens it. If a future edit removes it,
        this gate fails rather than the guarantee silently evaporating.
    A3. **Recursive frontend census.** Walk `src/frontend` with `readdirSync`, collect every `.ts`
        and `.tsx` file whose comment-stripped source matches `isSteamBottleEligible`, and assert the
        set is a subset of an explicitly declared `allowlist` array: this plan's probe module, this
        plan's two test files that mention it, 34.13-09's `SteamBottleSetup.tsx` and its test.
        Assert separately that the allowlist does NOT contain `state/InstallGameModal.ts`. Comment
        why the census is sound: for this channel the IPC channel name and the preload API-METHOD
        name are IDENTICAL (34.13-07 records this), so a single-token search cannot miss a frontend
        caller — the repo carries a lesson about a census that greps the channel name and misses
        every call site because they use the method name. **Prove non-vacuous** by asserting the
        walker DOES find the probe module (a known-present file) and by asserting the same matcher
        hits an inline known-bad specimen.
    A4. `steamEligibilityProbe` is imported by exactly ONE non-test file under `src/frontend`, and
        that file is `InstallModal/index.tsx`. Same walker, same non-vacuity backstop.

  **Group B — the loading row is actually mounted, in the right slot, behind the right guard.**
    B1. `InstallModal/index.tsx` contains `<EligibilityLoadingRow` exactly once, and imports it from
        the `SteamDialog/` directory.
    B2. `eligibility.pending` (or the shipped identifier) appears within the 150 characters
        immediately preceding that mount — the row is guarded by the pending flag and nothing else.
    B3. **Ordering:** the index of `<EligibilityLoadingRow` is LESS than the index of the Steam
        `<WineSelector` mount. **Prove non-vacuous** by running the same index comparison against an
        inline known-bad specimen with the order inverted and asserting it fails there.
    B4. `useSteamBottleEligibility(` appears exactly once and `applyEligibilityPending(` appears
        exactly once — one hook call, one wrap, no second re-derivation.
    B5. ZERO matches of a hand-rolled suppression: no `&& !pending`, no `&& !eligibility.pending`
        appended to any `gating.` field read. The suppression is `applyEligibilityPending`'s job.
        **Prove non-vacuous** against an inline specimen containing `gating.wineSection && !pending`.

  **Group C — the origin controls are deliberately UNTOUCHED (the inversion holds).**
    C1. Over comment-stripped `MainButton.tsx`, `GameCard/index.tsx` and `GameSubMenu/index.tsx`:
        ZERO matches of `checkingSteamInstallOptions`, `useSteamInstallOptionsCheck`,
        `isSteamInstallOptionsCheckActiveFor` and `isSteamBottleEligible` in all three.
        Title the spec so it reads as a DELIBERATE non-edit, e.g.
        `D-25 inversion: no origin install control carries an eligibility busy state`.
        **Prove non-vacuous** by asserting the same four searches DO hit an inline known-bad
        specimen. Comment that under the retired D-12 contract these three files were the busy
        state's home, that D-25 moved it inside the dialog, and that the quick path fires no probe
        so there is no latency here to cover.
    C2. `fa-spin` counts in `MainButton.tsx` and `GameCard/index.tsx` are unchanged from their
        pre-plan values (`2` and `1` respectively, verified 2026-08-15) — this plan added no spinner
        to either. Record pre/post in the SUMMARY.

  **Group D — no new spinner component, no new stylesheet.**
    D1. `readdirSync` of `SteamDialog/` yields no entry matching `/\.(css|scss)$/` and no entry
        matching `/spinner/i`. **Prove non-vacuous** against a hard-coded specimen array containing
        `'MySpinner.tsx'` and `'index.scss'`.

**RED-prove Groups A2, B and C** before Part A/B land, or — if the wiring is written first — verify
by temporarily reverting the Part A mount and confirming B1/B2/B3 fail while Groups A, C and D stay
green. Either ordering is acceptable; record which was used and the observed asymmetry in the
SUMMARY. **A gate that only ever passed is not evidence** — this repo carries multiple ledgered
instances of a correctly-written, passing test that guarded nothing.

Then run the full suite: this task edits the dialog router that every install flow mounts.
  </action>

  <verify>
    <automated>npx jest src/frontend/screens/Library/components/InstallModal --silent && npx tsc --noEmit && pnpm test:ci</automated>
  </verify>

  <acceptance_criteria>
    - `npx jest src/frontend/screens/Library/components/InstallModal --silent` exits 0 with Groups
      A, B, C and D all passing, and with 34.13-10's and 34.13-12's pre-existing suites still green.
    - The RED/deliberate-breakage asymmetry was OBSERVED and is recorded verbatim in the SUMMARY:
      with the Part A mount reverted, B1/B2/B3 FAIL while Groups A, C and D stay GREEN.
    - Every non-vacuity backstop is present: the A3, A4, B3, B5, C1 and D1 specs each contain a
      positive assertion against a known-bad specimen, so an unmatchable pattern cannot pass silently.
    - **The quick path is provably probe-free:**
      `grep -c 'isSteamBottleEligible' src/frontend/state/InstallGameModal.ts` → `0`, and the Group A3
      census passes with `state/InstallGameModal.ts` absent from the allowlist.
    - **The mount gate survives:**
      `grep -c 'installGameModalState.isOpen' src/frontend/screens/Library/components/InstallModal/index.tsx` → `≥ 1`.
    - **The dialog's own Install button is the disabled one:** over comment-stripped
      `SteamDialog/index.tsx`, `disabled=` count → `1` and
      `grep -c 'disabled={eligibilityPending}'` → `1`; and
      `grep -cE 'diskSize|spaceLeftAfter|notEnoughDiskSpace|getInstallInfo'` → `0` (D-06 preserved).
      **Proven non-vacuous at planning time:** pre-edit the file matches `disabled=` zero times, so
      `0 → 1` is a real transition. Record pre/post in the SUMMARY.
    - **No origin control changed:**
      `git status --porcelain src/frontend/screens/Game/GamePage/components/MainButton.tsx src/frontend/screens/Library/components/GameCard/index.tsx src/frontend/screens/Game/GameSubMenu/index.tsx` → empty.
    - The narrowed gate is NARROWED, not deleted: `steamDialogSource.test.ts`'s spec count is
      unchanged from pre-edit (record both numbers), and the file contains `D-25` at least once.
    - `git diff --name-only --diff-filter=A src/frontend` lists exactly FIVE paths across this whole
      plan — the probe module, the loading row, and three test files. Any sixth added path is a
      blocking deviation.
    - The catalog fence held: `git status --porcelain public/locales/` → empty and
      `pnpm lint-translations:gamelib` exits 0.
    - No rendering was introduced:
      `grep -rniE 'jsdom|react-test-renderer|@testing-library|ReactDOM|fireEvent|simulate' src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts`
      → no matches, and the file imports no `.tsx` module.
    - `npx tsc --noEmit` exits 0. `npx eslint` exits 0 on `InstallModal/index.tsx` and
      `SteamDialog/index.tsx`.
    - `pnpm test:ci` exits 0 with no pre-existing suite regressed. The ONLY expected test change in
      the whole repo is the narrowed spec in `steamDialogSource.test.ts`; any other suite requiring
      an edit is a real regression, not a fixture to update.
    - Ownership gate — `git status --porcelain src/common/ src/backend/ src/preload/ public/locales/ src/frontend/state/ src/frontend/screens/Library/components/InstallModal/steamSectionGating.ts src/frontend/screens/Library/components/InstallModal/WineSelector/ src/frontend/screens/Library/components/InstallModal/steamPlatformRow.ts package.json pnpm-lock.yaml` → empty.
  </acceptance_criteria>

  <done>The options dialog mounts `EligibilityLoadingRow` in the exact slot the Steam `<WineSelector>` would occupy, guarded solely by the pending flag; its own footer Install button is `disabled={eligibilityPending}` with D-06's never-size-gated rule preserved and re-gated positively; section suppression is one pure function rather than inline terms; and the quick path is proven probe-free by a mount gate, a zero-count on the install chokepoint and a non-vacuous recursive frontend census — with the three origin controls proven untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → backend, `window.api.isSteamBottleEligible(appName)` | **Crossed by this plan, but not newly opened.** 34.13-07 registered the channel on both runtimes and 34.13-09 already calls it. This plan adds a second renderer caller, sending only an already-trusted internal `appName` sourced from `GameInfo`. 34.13-07's seam guards it with `/^\d+$/` before constructing a `SteamGame` and fails closed. |
| backend → renderer, the `SteamBottleEligibilityVerdict` response | A boolean-bearing object that decides whether a wine section renders and whether the Install button is enabled. It never reaches a command-executing call in this plan — the install itself still goes through 34.13-08's marshaller and the backend re-resolves eligibility inside `install()` (D-25). |
| user intent → dialog render | The dialog now mounts with an unresolved section. The question this plan answers is what the user can DO during that window, and the answer is: read the platform row, and nothing else that could be wrong. |
| renderer → i18next, the reused `gamelib:` key | A static English default compiled into the bundle. No interpolation, no user data, no `appName`. |
| **NOT crossed:** renderer install dispatch | This plan adds no install call, no `InstallArgs` construction site, and no path input. `installSteamGame` and `window.api.install` appear nowhere in any file it writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.13-11-01 | Denial of Service | A permanently pending eligibility state leaving the dialog's Install button permanently `disabled` — the user opens the options dialog and can never install, on any game, without restarting | **mitigate** | There is exactly ONE latch and TWO resolution paths, both in `steamEligibilityProbe.ts`. `applyEligibilityResponse` resolves on success; `applyEligibilityFailure` resolves fail-CLOSED to `bottleRequired: false` on rejection, matching the UI-SPEC's rule that a timeout "is not modeled as its own error state (falls back to 'not eligible')". The upper bound is the backend's own `METADATA_FETCH_TIMEOUT_MS` (15000ms), not an unbounded await. Task 1 spec 7 pins the failure path and is RED-proven; the state is a pure projection with no second `useState` mirror, so no surface can outlive the reducer's resolution. Additionally, the pending state cannot survive the dialog: `InstallGameWrapper` unmounts the whole tree on close, discarding it. |
| T-34.13-11-02 | Spoofing | A late `isSteamBottleEligible` response for game A applied to game B's dialog — the user sees the wrong verdict, and a bottle-requiring game renders as native (or vice versa) | **mitigate** | The PRIMARY control is in the pure reducer, not in an effect flag: `applyEligibilityResponse` and `applyEligibilityFailure` both compare the response's `appName` against the state's and return the state unchanged on mismatch. Task 1 spec 6 is a named DISCRIMINATOR built from two interleaved deferreds where the stale verdict asserts the OPPOSITE value, so an implementation that ignores `appName` passes every other spec and fails only that one. The effect's cleanup flag is a secondary control preventing `setState` after unmount, and its presence is a source gate. Residual: two dialogs for the same `appName` cannot exist simultaneously — `InstallGameWrapper` renders at most one `InstallModal`. |
| T-34.13-11-03 | Spoofing | The renderer re-deriving bottle eligibility locally instead of trusting the backend verdict — D-09's named anti-pattern, and the historical shape of `.planning/debug/steam-bottle-guided-setup-never-fires.md` where a cold metadata cache read as "not eligible" | **mitigate** | `bottleRequired` originates exclusively from `window.api.isSteamBottleEligible`, whose backend seam awaits `ensurePlatformsCaptured()` first. Task 1's source gate asserts the probe module's comment-stripped source contains ZERO matches of `is_mac_native`, `mac_arch`, `steamPlatformsCaptured`, proven non-vacuous against a known-bad specimen. The one platform test the module DOES make is a call-avoidance optimisation, not a verdict — it is written as an explicit `'win32' || 'linux'` skip, never `!== 'darwin'`, so an absent or unexpected `window.platform` still asks the backend. Task 1 spec 3 is a named DISCRIMINATOR precisely because the inverted form passes every other spec. |
| T-34.13-11-04 | Tampering | An install dispatched against a STALE `bottleRequired: false` during the pending window — a bottle-requiring macOS game installed as if native, or a wine choice submitted before the wine section resolved | **mitigate** | Three independent layers. (a) The dialog's own Install button is `disabled={eligibilityPending}` — Task 3 gates both that it is present exactly once and that nothing else feeds `disabled`. (b) `applyEligibilityPending` forces `wineSection: false` while pending, so no wine selector is mounted and no half-configured engine choice can exist to submit. (c) **The backend remains authoritative regardless** — D-25's own text records that `install()` resolves bottle eligibility itself, so even a hypothetically-dispatched early install is corrected server-side; the frontend gate is a UX control, not the security boundary. Residual, stated not hidden: the `disabled` attribute is a render-time control, so a click landing in the same frame as the mount is theoretically unblocked; the consequence is bounded by (c) to a correctly-bottled install. |
| T-34.13-11-05 | Spoofing | A renderer-side caller invoking `isSteamBottleEligible` with an arbitrary `appName` | **transfer** | Owned by 34.13-07's `T-34.13-07-01` numeric-appId guard (`/^\d+$/`, applied at the shared seam before a `SteamGame` is constructed, failing closed). This plan deliberately adds NO second, duplicated guard in the renderer — a frontend copy would be unenforceable and would drift, and the repo's `redeemSteamKey` precedent (two copies of one trust boundary) is the pattern to avoid. Task 1 gates the absence of a duplicated appId regex in the probe module. |
| T-34.13-11-06 | Denial of Service | An eligibility probe fired on the QUICK install path, reintroducing the multi-second dead click D-25 exists to eliminate | **mitigate** | Structural, not procedural. The probe lives in a hook called only by `InstallModal`, and `InstallGameWrapper` returns `<></>` unless `useInstallGameModal().isOpen` — the quick path never flips `isOpen`, so the owning component never mounts. Three gates back this: A2 asserts the `isOpen` early return still exists (so the guarantee cannot silently evaporate), A1 asserts `state/InstallGameModal.ts` contains zero `isSteamBottleEligible`, and A3 is a recursive `src/frontend` census against an explicit allowlist, proven non-vacuous. The census is sound because for this channel the IPC channel name and the preload API-method name are identical — 34.13-07 records this, which is what defuses the repo's ledgered "census by wrong namespace misses call sites" failure mode. |
| T-34.13-11-07 | Information Disclosure | The loading row's copy rendering into the UI | accept | A static, localised, non-interpolated English default — `Checking install options…`. It carries no `appName`, no Steam AppID, no library path, no wine engine path, no account identifier. Nothing is logged, no telemetry sink is touched, nothing is written to disk. Consistent with the standing Steam-identity redact-forward decision by carrying nothing that would need redacting. |
| T-34.13-11-08 | Repudiation | — | accept | Not applicable. This plan adds no audited action, no authorization decision and no persistent state mutation. It projects one transient boolean onto one dialog. |
| T-34.13-11-09 | Elevation of Privilege | — | accept | Not applicable. No privilege, capability, permission or trust level is introduced, widened or evaluated. No new IPC channel is registered; the one channel used was already dual-registered and already had a renderer caller. |
| T-34.13-SC | Tampering | npm/pip/cargo installs | **n/a — the Package Legitimacy Gate is NOT APPLICABLE to this plan or this phase.** `34.13-RESEARCH.md` §"Standard Stack" states "No new libraries. This phase composes existing in-repo primitives only," and explicitly records that a `## Package Legitimacy Audit` table is not applicable. This plan adds zero dependencies: `FontAwesomeIcon`, `faSyncAlt`, `react` and `react-i18next` are all already imported by files in this tree. `package.json` / `pnpm-lock.yaml` are absent from `files_modified` by design and are covered by an explicit ownership gate in all three tasks. **Especially load-bearing here:** the obvious "fix" for the dialog being untestable is `pnpm add -D jest-environment-jsdom`, and `src/frontend/jest.config.js`'s own docstring records that adding it is excluded from auto-fix and would require a human package-legitimacy checkpoint. An executor running a package-manager install has escaped scope — stop and return to planning. | The standing control remains armed: any install task in this phase would require the RESEARCH.md audit table plus a blocking human-verify checkpoint for every `[ASSUMED]`/`[SUS]` package; `[SLOP]` packages are forbidden and such checkpoints are never auto-approvable regardless of `workflow.auto_advance`. |

**ASVS L1 posture for this plan:** V5 (Input Validation) is the only category with a live surface —
the `appName` crossing to `isSteamBottleEligible` — and it is **transferred** to 34.13-07's
fail-closed `/^\d+$/` seam rather than duplicated (T-34.13-11-05). The availability concerns
(T-34.13-11-01, T-34.13-11-06) are UI controls rather than ASVS categories and are treated as
`mitigate` with their residual frames stated explicitly rather than defaulted to `accept`. V2
(authentication), V3 (session management), V4 (access control) and V6 (cryptography) are untouched,
matching `34.13-RESEARCH.md` §"Security Domain" ("No new authentication, session, network-facing, or
cryptographic surface is introduced by this phase").
</threat_model>

<verification>
## Per-task verification map (for `34.13-VALIDATION.md`, filled by 34.13-13)

| Task ID | Wave | Decision | Threat Ref | Test Type | Automated Command | File Exists |
|---------|------|----------|------------|-----------|-------------------|-------------|
| 34.13-11-01 | 7 | D-25 | T-34.13-11-01, -02, -03, -05 | unit (pure functions, RED-proven, 3 named discriminators) | `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts --silent` | ❌ created by this task |
| 34.13-11-02 | 7 | D-25 | — | unit (direct component invocation, element-graph inspection, RED-proven) | `npx jest src/frontend/screens/Library/components/InstallModal/SteamDialog/__tests__/EligibilityLoadingRow.test.tsx --silent` | ❌ created by this task |
| 34.13-11-03 | 7 | D-25, D-11, D-06 | T-34.13-11-04, -06 | static (comment-stripped source + recursive census + filesystem gates) + full regression | `npx jest src/frontend/screens/Library/components/InstallModal --silent && pnpm test:ci` | ❌ created by this task |

## Phase-level checks owed by this plan

- `npx tsc --noEmit` exits 0.
- `pnpm test:ci` exits 0 after Task 3. The ONLY expected test change in the repo is the narrowed
  spec in `steamDialogSource.test.ts`; any other suite needing an edit is a real regression.
- Both new spec files were observed RED before GREEN, and each RED named its discriminators
  (Task 1: specs 3, 6, 10; Task 2: spec 2). A gate that only ever passed is not evidence.
- The Task 3 deliberate-breakage asymmetry (reverted mount → B1/B2/B3 fail, Groups A/C/D stay green)
  was observed and recorded verbatim.
- Pre/post counts recorded for every count-based gate: `disabled=` in `SteamDialog/index.tsx`
  (0→1), `fa-spin` in `MainButton.tsx` (2→2, unchanged) and in `GameCard/index.tsx` (1→1,
  unchanged), `isSteamBottleEligible` in `state/InstallGameModal.ts` (0→0), and
  `steamDialogSource.test.ts`'s spec count (unchanged).
- `git diff --name-only --diff-filter=A src/frontend` lists exactly five paths.
- `pnpm lint-translations:gamelib` exits 0 and `public/locales/` is untouched.

## Explicitly NOT verified here (owned by 34.13-13's blocking manual gate)

- **That the loading row is visible on any screen.** Task 2 proves a React-element object graph;
  Task 3 proves source text and a mount site. Neither is a pixel. `34.13-VALIDATION.md`
  vacuous-assertion risk 1 ("store-flip ≠ dialog renders") applies in its general form: an
  element-graph assertion and a dialog that never mounts are indistinguishable to Jest here.
- **That the dialog opens INSTANTLY.** The mechanical claim (nothing is awaited before the first
  render) is provable; the perceptual claim is not. Requires a cold Steam metadata cache and a
  stopwatch — enumerated in `34.13-VALIDATION.md`'s Manual-Only table.
- **That the quick path has no perceptible wait.** Absence-of-latency is unobservable in a unit
  test; the VALIDATION table has a dedicated row for it.
- **The 15-second cold-cache worst case**, and that the footer Install button is observably disabled
  for its duration.
- **That the suppressed library dropdown then appears correctly on resolution** (falsified brief 1's
  resolution). Source-and-unit-gated only here.
- **Both runtimes.** The busy state depends on 34.13-07's channel being registered on Electron AND
  the Tauri sidecar; a channel registered on Electron only would leave the dialog pending forever
  under Tauri specifically. Hence the manual gate's "both runtimes" row and `pnpm tauri:dev`, never
  `tauri dev` (which serves a stale static bundle).

## Standing rules honoured

- **No 1px border on a `1fr` grid-track boundary** — this plan introduces no CSS at all and no grid
  layout; it composes `buttonWithIcon` and `fa-spin`, both of which already exist globally.
- **No hardcoded hex, no parallel spacing/type scale** — gated to zero in both new source files.
- `pnpm tauri:dev`, never `tauri dev` — carried into the handoff for 34.13-13's manual gate.
- **Localisation:** one key, reused not renamed, shipped as an inline English default; the catalog is
  34.13-13's and is fenced by an ownership gate in all three tasks.
</verification>

<handoffs>
- **To 34.13-13 (single catalog writer):** `gamelib:status.checkingSteamInstallOptions` →
  `Checking install options…` still needs its entry in `public/locales/en/gamelib.json`. Unlike the
  pre-amendment draft, this key IS present in the UI-SPEC Copywriting Contract table (L138, marked
  "relocated, not renamed"), so it should be picked up — confirm it is.
- **To 34.13-13's manual gate — the highest-risk row in the phase.** `34.13-VALIDATION.md` risk 5
  states it plainly: *"A pass recorded against the retired contract is worse than no pass."* The D-25
  row asserts the OPPOSITE of the pre-amendment gate. **A half-rendered dialog is the CORRECT
  result.** Re-derive the row from the VALIDATION table, never from a prior UAT run or from memory.
  The row must be exercised on **macOS specifically** (the loading state cannot appear on Windows or
  Linux — `isSteamBottleEligible`'s backend predicate is `if (!isMac) return false`), on **both
  runtimes**, and it must observe THREE things, not one: the dialog appears immediately, the wine
  section slot shows the spinner, and the dialog's own Install button is disabled until it resolves.
- **To 34.13-13's phase gate:** add `grep -rn "isSteamBottleEligible" src/frontend` to the sweep. It
  must return hits ONLY in this plan's probe module, its tests, and 34.13-09's `SteamBottleSetup.tsx`
  and test — never in `state/InstallGameModal.ts` and never in an origin install control.
- **To whoever verifies 34.13-10:** its `must_haves` truth "the footer Install button carries no
  `disabled` attribute at all, in any render state" is **deliberately falsified by this plan** under
  D-25. D-06's substance (never size-gated) is preserved and re-gated positively. This is a planned
  narrowing, not a regression. See `<superseded_upstream_gate>`.
- **UI-SPEC amendment owed:** the D-25 Busy State Contract's claim that the library dropdown and
  free-space line "can render immediately alongside the loading row" is falsified by 34.13-05's
  `libraryDropdown = !wineSection && …` formula. The SUMMARY must state the correction so the spec is
  amended rather than the code being "fixed" back to the broken behaviour later.
- **Possible dead code to report, not delete:** if 34.13-08's rewrite left a
  `useSteamInstallOptionsCheck` store (or equivalent D-12 remnant) in `state/InstallGameModal.ts`
  with zero consumers, record it in the SUMMARY and hand it to 34.13-13. That file is 34.13-08's.
</handoffs>

<success_criteria>
- **The inversion is real and complete.** The loading state renders INSIDE the options dialog, in the
  slot the Steam `<WineSelector>` would occupy, and the DIALOG'S OWN footer Install button is the
  disabled one. No origin install control gained a busy state, a spinner or a `disabled` term —
  enforced by a proof-of-absence gate over all three, proven non-vacuous.
- **The quick path fires no eligibility probe, guaranteed structurally.** The probe lives in a hook
  that only `InstallModal` calls, and `InstallGameWrapper` does not mount `InstallModal` unless the
  modal is open. Three gates back it: the `isOpen` early return still exists,
  `state/InstallGameModal.ts` contains zero `isSteamBottleEligible`, and a recursive `src/frontend`
  census matches an explicit allowlist that excludes it.
- **The dialog opens instantly, mechanically provable.** `initialEligibilityState` is synchronous and
  seeds `useState`; the IPC happens in a `useEffect` after first render. Nothing is awaited before
  paint.
- **macOS-only, fail-safe.** The probe is skipped only for an explicit `'win32'`/`'linux'`; an
  unknown platform still asks the backend. Pinned by a named DISCRIMINATOR the inverted form fails.
- **It can never wedge, and it can never bleed across games.** Rejection resolves fail-closed to
  not-eligible; a late verdict for a different `appName` is discarded by the pure reducer. Both are
  named discriminators, both RED-proven.
- **Section gating stays delegated.** Pending-time suppression is one pure function
  (`applyEligibilityPending`) with an exact, saboteur-pinned field footprint: `wineSection`,
  `libraryDropdown` and `freeSpaceLine` forced false; `platformRow`, `opens` and
  `forceWindowsViaBottle` byte-identical. No inline `&& !pending` term exists anywhere.
- **D-06 survives its own gate's narrowing.** `diskSize`, `free`, `spaceLeftAfter`,
  `notEnoughDiskSpace` and `getInstallInfo` stay at zero in `SteamDialog/index.tsx`; `disabled=`
  appears exactly once and is exactly the D-25 pending flag; the superseded spec is rewritten in
  place with its inversion recorded, never deleted.
- **Nothing new was invented.** No new spinner component, no stylesheet, no CSS class, no design
  token, no hex, no dependency, no IPC channel. `git diff --name-only --diff-filter=A src/frontend`
  yields exactly five paths, three of them tests.
- **Both falsified briefs are recorded as findings with evidence** — the UI-SPEC's dropdown claim
  (contradicted by 34.13-05's formula) and 34.13-10's absolute no-`disabled` gate (a casualty of the
  same inversion).
- **Evidence discipline held.** Both new spec files were observed RED naming their discriminators
  before GREEN, and Task 3's deliberate-breakage asymmetry was observed and recorded.
- **The vacuity boundary is stated, not assumed.** An element-graph assertion, a pure unit test and a
  source gate are not a pixel. Nothing here proves the row is visible; 34.13-13's blocking manual
  gate is the only evidence for that, and the SUMMARY says so.
- `npx tsc --noEmit`, `npx eslint` on every edited source file, `pnpm lint-translations:gamelib` and
  `pnpm test:ci` all exit 0; the diff touches exactly the eight paths in `files_modified` — no
  `common/`, no `backend/`, no `preload/`, no `state/`, no `steamSectionGating.ts`, no
  `WineSelector/`, no locale catalog, no `package.json`.
</success_criteria>

<source_coverage>
## Source Coverage (this plan)

The phase-wide multi-source audit lives in `34.13-PLAN-OUTLINE.md` §Coverage — **but note that
outline predates the D-21..D-28 amendment and its row for plan 11 is fully retired.** This plan's
slice:

| Source | Item | Status |
|--------|------|--------|
| CONTEXT | **D-25** — "The options dialog opens INSTANTLY; the wine section carries the eligibility loading state; the dialog's own Install button stays disabled until it resolves"; and "The quick path needs no eligibility probe at all" | COVERED in full — Task 1 (the state machine and the fail-safe macOS scope), Task 2 (the loading row, behaviorally proven), Task 3 (the mount in the wine-section slot, the disabled footer button, and the three gates that keep the quick path probe-free). |
| CONTEXT | **D-25 amends D-12** — the busy state relocates from origin control to dialog | HONORED AS AN INVERSION, not a relocation-by-halves. The origin controls are gated to ZERO eligibility state (Task 3 group C), so the retired contract cannot survive by accident. `<the_inversion>` states the old/new table explicitly so an executor working from the outline or from 34.13-08/10's comments does not rebuild the retired shape. |
| CONTEXT | **D-26 — D-13 RETIRED, plan 34.13-04 DELETED** | HONORED — `depends_on` contains no `34.13-04`, no always-show setting is read anywhere, and `34.13-PATTERNS.md`'s `AlwaysShowSteamInstallForm` section is flagged as a trap in `<context>`. |
| CONTEXT | D-11 — sections render only when relevant | COVERED — `applyEligibilityPending` keeps an unresolved section from rendering a stale or vanishing answer, and Task 1 spec 10 keeps the synchronously-known platform row visible so the dialog is never empty on open (the D-20 corner). |
| UI-SPEC | **D-25 Busy State Contract** — macOS-only scope; the relocated `buttonWithIcon`+`faSyncAlt.fa-spin` idiom in the `<WineSelector>` DOM slot; the key REUSED verbatim not renamed; snap-not-animate resolution; the dialog's own Install button disabled; "no half-rendered modal" REVERSED; 15s worst case relocated | HONORED item by item — Task 2 reproduces the markup block literally, Task 1's platform gate implements the macOS scope fail-safely, Task 3 pins the mount slot and the disabled button, and no transition/animation is added anywhere. |
| UI-SPEC | "The library dropdown / free-space line … can render immediately alongside the loading row" | **FALSIFIED AND CORRECTED** — 34.13-05's `libraryDropdown = !wineSection && …` makes both depend on the pending verdict. Suppressed while pending; `platformRow` preserved. Recorded as a finding with evidence and handed forward as a UI-SPEC amendment, not silently absorbed. |
| UI-SPEC | Reuse Boundaries — the busy idiom is "reused, relocated inside the dialog per D-25, not a new spinner component"; §"NEW — the D-25 in-dialog loading row (relocated existing idiom, new placement only)" | HONORED — Task 2 gates `CircularProgress`/`<Spinner`/`@mui/material` to zero and Task 3 group D gates the directory against any `Spinner`-named or stylesheet file. |
| UI-SPEC | Copywriting L138 — `gamelib:status.checkingSteamInstallOptions` → "Checking install options…" | SHIPPED as an inline English default; catalog handed to 34.13-13 (the single writer) and confirmed present in the Copywriting table. |
| VALIDATION | Hard constraint: no jsdom, no react-test-renderer; every frontend deliverable is either pure-function-tested or listed in the manual gate — none silently assumed | HONORED, and improved on: the state machine is pure-function-tested, the loading row is proven by the repo's sanctioned direct-invocation idiom (real behavioral evidence, not a source gate), the wiring is proven by comment-stripped source gates, and everything visual is explicitly assigned to 34.13-13. |
| VALIDATION | Vacuous-assertion risk 1: "store-flip ≠ dialog renders" | ADDRESSED BY EXPLICIT SCOPING and by raising the evidence floor — the loading row's element graph is inspected directly rather than asserting a flag flipped. The residual gap (that `InstallModal → SteamDialog → screen` paints) is stated in `must_haves`, in both new test headers, in `<verification>` and is required in the SUMMARY. |
| VALIDATION | Vacuous-assertion risk 5: "a pass recorded against the retired contract is worse than no pass" | ADDRESSED — `<the_inversion>` gives the old/new table, the narrowed upstream gate carries the inversion in a code comment, and `<handoffs>` requires 34.13-13's D-25 row to be re-derived from the VALIDATION table rather than from memory or a prior ledger. |
| VALIDATION | Manual-Only rows: "D-25 busy state — INVERTED", "Quick path fires NO eligibility probe", "Both runtimes" | ALL THREE claimed as manual and named in `<verification>`'s not-verified-here list — none silently assumed green. |
| PATTERNS | ⚠ banner: the `AlwaysShowSteamInstallForm` entry is a retired trap; four amendment surfaces have no pattern assignment | HONORED — none of the four unassigned surfaces (split button, context-menu item, GameSubMenu entry, D-24 notice) belongs to this plan; each is named in `<deliberate_exclusions>` with its real owner. |
| RESEARCH | Amendment research D-21..D-28; "No new libraries. This phase composes existing in-repo primitives only" | HONORED — zero dependencies added, gated by an ownership check on `package.json`/`pnpm-lock.yaml` in all three tasks. |
| GOAL | ROADMAP phase goal — the user must never face a dead click while GameLib works out whether the install form is needed | COVERED, and by a stronger mechanism than the retired model: the dead click is not merely made visible, it is REMOVED from the default path entirely (no probe), and on the deliberate path the dialog appears immediately with the wait made legible inside it. |
| REQ | — | No `REQ-` IDs minted for this phase (ROADMAP marks them TBD); coverage is tracked against `D-NN` per the outline's decision-coverage gate. Not a gap. |
| DEFERRED | Editable/per-game bottle names; backend engine rejection; per-game override of the (now-cut) always-show setting; `getInstallInfo()` / download sizes; a Linux Proton path; extending quick install to GOG/Epic/Amazon (D-28) | CORRECTLY ABSENT — this plan renders one boolean's pending phase inside one dialog. It reads no setting, no depot flag, no size, and calls exactly one pre-existing backend function. |

No unplanned items. No phase split needed: 3 tasks, 8 files (5 new — two of them small source files
and three tests — plus 3 surgical edits), comfortably inside a single executor's context budget.
</source_coverage>

<output>
Create `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-11-SUMMARY.md`
when done. It MUST include:
- the verbatim RED output from Task 1 Part A (naming specs 3, 6 and 10) and from Task 2 Part A
  (naming spec 2);
- the Task 3 deliberate-breakage asymmetry (reverted mount → B1/B2/B3 fail while Groups A, C and D
  stay green), with the spec names on both sides;
- the pre/post counts for every count-based gate: `disabled=` in `SteamDialog/index.tsx` (0→1),
  `isSteamBottleEligible` in `state/InstallGameModal.ts` (0→0), `fa-spin` in `MainButton.tsx` (2→2)
  and `GameCard/index.tsx` (1→1), and `steamDialogSource.test.ts`'s spec count before and after;
- **both falsified briefs, stated as findings for the phase record** — (1) the UI-SPEC's claim that
  the library dropdown and free-space line can render immediately, falsified by 34.13-05's
  `libraryDropdown = !wineSection && …` formula, with the correction applied and the UI-SPEC
  amendment owed; (2) 34.13-10's absolute "no `disabled` Install button" gate, narrowed to D-06's
  actual substance because D-25 inverted its premise — explicitly flagged as a PLANNED narrowing so
  a verifier does not score it as a regression against 34.13-10's `must_haves`;
- **the D-25 inversion stated in the SUMMARY's own words**, so the phase record cannot be read back
  against the retired D-12 contract: the busy state is in the dialog, the disabled button is the
  dialog's own, the half-rendered dialog is CORRECT, and the quick path fires no probe;
- **how the quick-path guarantee is structural**: the `InstallGameWrapper` `isOpen` early return, the
  zero-count on `state/InstallGameModal.ts`, and the recursive `src/frontend` census with its
  allowlist reproduced verbatim;
- the 34.13-13 handoffs called out explicitly — the catalog entry, the manual D-25 row's
  score-backwards hazard (macOS only, both runtimes, `pnpm tauri:dev` never `tauri dev`), the phase
  gate's `isSteamBottleEligible` sweep, and any D-12 remnant found in `state/InstallGameModal.ts`;
- an explicit statement of the vacuity boundary: an element-graph assertion, a pure unit test and a
  source gate are not a pixel. Nothing here proves the loading row is visible or that the dialog
  opens instantly in perceptual terms; 34.13-13's blocking manual gate is the only evidence for that.
</output>
