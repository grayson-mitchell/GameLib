---
phase: 34.13-steam-install-time-wine-bottle-form-gog-parity
plan: 11
type: execute
wave: 5
depends_on: ['34.13-08']
files_modified:
  - src/frontend/types.ts
  - src/frontend/screens/Game/GameContext.tsx
  - src/frontend/screens/Game/GamePage/index.tsx
  - src/frontend/screens/Game/GamePage/components/SteamBridgeSetup.tsx
  - src/frontend/screens/Game/GamePage/components/MainButton.tsx
  - src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamInstallOptions.test.tsx
  - src/frontend/screens/Library/components/GameCard/index.tsx
  - src/frontend/screens/Library/components/GameCard/__tests__/steamInstallOptionsBusy.test.ts
autonomous: true
requirements: [D-12]
user_setup: []

must_haves:
  truths:
    - "D-12: while the Steam eligibility verdict is pending, the install control the user actually clicked is BOTH visibly busy AND `disabled` — never a silent multi-second dead click. On the game page this is proven behaviorally: `MainButton` invoked with `is.checkingSteamInstallOptions === true` returns an element tree whose install button carries `disabled === true` and whose label text is `Checking install options…`."
    - "D-12: the worst case is stated verbatim in the source, not paraphrased — the doc comment on the new `is.checkingSteamInstallOptions` field records `15 seconds on a cold metadata cache (METADATA_FETCH_TIMEOUT_MS = 15000ms)`, matching 34.13-08's module doc comment and the UI-SPEC D-12 Busy State Contract."
    - "D-12: there is exactly ONE source of truth. Every one of the three surfaces derives its busy flag from 34.13-08's exported `isSteamInstallOptionsCheckActiveFor(state, appName, runner)` — no surface hand-rolls `isChecking && appName === …`, and no surface introduces a local `useState` mirror that could latch independently of 34.13-08's `finally` release."
    - "D-12: NO new spinner component is created. The busy markup reuses the two existing in-repo idioms byte-for-byte — `buttonWithIcon` + `<FontAwesomeIcon icon={faSyncAlt} className=\"fa-spin\" />` in `MainButton.tsx` (mirroring its own `is.settingUpBottle` branch at L192-199) and the `svg-button iconDisabled` disabled-spinner button in `GameCard/index.tsx` (mirroring its own `isInstalling && isSteam` branch at L251-258). Proven by an added-file gate: `git diff --name-only --diff-filter=A src/frontend` yields ONLY the two new `__tests__` files."
    - "D-12 re-entrancy: a second click cannot dispatch a second `startSteamInstall`. Every install-dispatching affordance on the two real origin surfaces is gated — `MainButton`'s install button via `disabledInstallButtons`, `GameCard`'s download icon via a `renderIcon()` branch that returns a `disabled` button instead of the clickable `SvgButton`, and `GameCard`'s context-menu Install item via its `show` flag. `GamePage.handleInstall` additionally early-returns, mirroring its existing `if (settingUpBottle) return` guard — defense in depth, not the primary control."
    - "FALSIFIED BRIEF REPAIRED — the game page did NOT share the chokepoint. `GamePage/index.tsx:676-688` dispatched `window.api.install({... platformToInstall: 'Windows' ...})` DIRECTLY for `runner === 'steam' && !is_installed`, bypassing `openInstallGameModal` → `startSteamInstall` entirely. Without this repair `MainButton`'s D-12 wiring is dead code by construction AND the whole phase (the form, D-10's triggers, D-17's platform override) never fires from the game page. After the repair that branch dispatches through the chokepoint, and the zero-friction outcome is byte-identical because `installSteamGame(appName, gameInfo)` marshals the identical `InstallArgs`."
    - "FALSIFIED BRIEF RECORDED — `GameSubMenu/index.tsx` is NOT an install origin and is correctly left unmodified. Its only `openInstallGameModal` call (L174-178) is inside `handleEdit()` behind `if (isSideloaded)`, unreachable for `runner === 'steam'`; the component renders no Install control at all. This is covered by a proof-of-absence gate, not silently dropped — if a future edit adds one, the gate fails."
    - "VACUITY BOUNDARY, stated not assumed: `MainButton` is proven by DIRECT FUNCTION INVOCATION of the component and inspection of the returned React-element object graph — the repo's own sanctioned no-jsdom idiom (`src/frontend/jest.config.js` docstring; live precedent `MainButton.steamIncomplete.test.tsx`). Nothing is rendered, no DOM exists, and no click is simulated. `GameCard` and `GameSubMenu` both `import './index.css'` on line 1 and therefore CANNOT be invoked this way at all — they are proven only by comment-stripped source gates. That the spinner is actually VISIBLE on any surface is 34.13-13's blocking manual gate and must be recorded as such in the SUMMARY."
  artifacts:
    - path: "src/frontend/screens/Game/GamePage/components/MainButton.tsx"
      provides: "The D-12 busy label branch and the `disabledInstallButtons` term, mirroring the file's own `is.settingUpBottle` treatment"
      contains: "checkingSteamInstallOptions"
    - path: "src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamInstallOptions.test.tsx"
      provides: "RED-proven behavioral coverage of the D-12 label + disabled state by direct component invocation, plus the discriminator that a busy-but-clickable button fails"
      contains: "Checking install options"
      min_lines: 120
    - path: "src/frontend/screens/Library/components/GameCard/index.tsx"
      provides: "The D-12 disabled-spinner `renderIcon()` branch and the context-menu Install `show` gate"
      contains: "isSteamInstallOptionsCheckActiveFor"
    - path: "src/frontend/screens/Library/components/GameCard/__tests__/steamInstallOptionsBusy.test.ts"
      provides: "Cross-surface comment-stripped source gates: GameCard's two gated affordances, the GamePage chokepoint repair, the GameSubMenu proof-of-absence, the single-source-of-truth predicate rule, and the no-new-spinner-component negative gate"
      contains: "GameSubMenu"
      min_lines: 130
    - path: "src/frontend/screens/Game/GamePage/index.tsx"
      provides: "The `is.checkingSteamInstallOptions` computation feeding GameContext, the re-entrancy early return, and the chokepoint-bypass repair"
      contains: "openInstallGameModal"
    - path: "src/frontend/types.ts"
      provides: "`GameContextType['is'].checkingSteamInstallOptions` declared beside `settingUpBottle`, with the 15s worst case in its doc comment"
      contains: "checkingSteamInstallOptions"
  key_links:
    - from: "src/frontend/screens/Game/GamePage/index.tsx"
      to: "src/frontend/state/InstallGameModal.ts isSteamInstallOptionsCheckActiveFor"
      via: "the single exported pure predicate, computed once and fed into GameContext's `is` — the exact shape `settingUpBottle` already uses at GamePage/index.tsx:174-179"
      pattern: "isSteamInstallOptionsCheckActiveFor\\("
    - from: "src/frontend/screens/Library/components/GameCard/index.tsx"
      to: "src/frontend/state/InstallGameModal.ts useSteamInstallOptionsCheck"
      via: "direct zustand subscription (GameCard has no GameContext) plus the same exported predicate"
      pattern: "useSteamInstallOptionsCheck\\(\\)"
    - from: "src/frontend/screens/Game/GamePage/index.tsx handleInstall()"
      to: "src/frontend/state/InstallGameModal.ts openInstallGameModal"
      via: "the repaired Steam branch — the game page now shares the chokepoint instead of hand-marshalling InstallArgs"
      pattern: "openInstallGameModal\\("
    - from: "src/frontend/screens/Game/GamePage/components/MainButton.tsx"
      to: "GameContext is.checkingSteamInstallOptions"
      via: "context read — both the `disabledInstallButtons` term and the `getButtonLabel()` branch"
      pattern: "is\\.checkingSteamInstallOptions"
---

<objective>
Make D-12 real on the surface the user actually touched.

34.13-08 (Wave 4) landed the transient store and its exported predicate, and it deliberately stopped
there: it records that "the D-12 busy state is VISIBLE on any button" is NOT verified by that plan
and hands the wiring to this one. The eligibility check runs *before* the dialog exists — up to
**15 seconds on a cold metadata cache (`METADATA_FETCH_TIMEOUT_MS` = 15000ms)** — so there is no
dialog footer to put a spinner in. The spinner belongs on whichever install control was clicked.

Purpose:
- **D-12** — "no half-rendered modal, and no silent multi-second dead click either." The modal half
  is 34.13-08's (`isOpen` flips strictly after the verdict). The dead-click half is this plan's: the
  clicked control must go visibly busy AND `disabled` for the duration.
- Reuse, do not invent. Both surfaces already contain the exact idiom — `MainButton.tsx:192-199`
  spins for `is.settingUpBottle`, `GameCard/index.tsx:251-258` spins for `isInstalling && isSteam`.
  This is state plumbing, not a new visual pattern.
- One source of truth. 34.13-08 froze `isSteamInstallOptionsCheckActiveFor(state, appName, runner)`
  specifically to prevent the button/status desync its sibling `isSteamBottleSetupActiveFor`
  docstring names. Every surface calls it; none re-derives it.

Output: one new `GameContextType['is']` field plumbed the same way `settingUpBottle` is, two wired
origin surfaces, one repaired chokepoint bypass, one repaired stale cross-reference comment, and two
new test files — one behavioral, one source-gate.

**Scope fences (all hard):**
1. Do NOT edit `src/frontend/state/InstallGameModal.ts`. 34.13-08 is its sole editor and its
   contract is FINAL. Consume `useSteamInstallOptionsCheck` and
   `isSteamInstallOptionsCheckActiveFor`; add neither a second store nor a local `useState` mirror.
2. Do NOT edit `public/locales/en/gamelib.json`. Ship the inline English default via
   `t('gamelib:key', 'English default')` (live precedent: `Humble/Keys/index.tsx:25`). 34.13-13 is
   this phase's single catalog writer. See `<handoffs>` — the key this plan ships is **missing from
   the UI-SPEC Copywriting Contract table** and must be handed forward explicitly.
3. Do NOT edit `src/common/`, anything under `src/backend/`, `InstallModal/` (34.13-03/05/10/12), or
   `SteamBottleSetup.tsx` / `steamBottleDefaults.ts` (34.13-09).
4. Do NOT write a rendering test. No jsdom, no react-test-renderer, no `ReactDOM.render`, no
   `@testing-library/*`, no simulated clicks. `MainButton` is INVOKED as a plain function and its
   returned element graph is searched — the repo's own documented pattern. `GameCard` and
   `GameSubMenu` cannot even be imported (`import './index.css'` on line 1) and are proven only by
   comment-stripped source gates plus 34.13-13's manual gate.
5. Do NOT rewire `SteamClientSetup.tsx:78` or `SteamBridgeSetup.tsx:79-88`. Those are post-consent
   retries, deliberately zero-friction — re-asking the install-options question there would undo the
   consent the user just gave. `SteamBridgeSetup.tsx` gets a **comment-only** cross-reference repair
   and nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-CONTEXT.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UI-SPEC.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VALIDATION.md
@.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-PATTERNS.md

Knowledge graph is authoritative for orientation. Run
`graphify explain "MainButton.tsx"` / `graphify query "GameCard install button spinner"` before any
broad source exploration. Every `file:line` anchor below was verified by direct read on 2026-08-14.

<interfaces>
<!-- Extracted from the codebase and from 34.13-08 on 2026-08-14. Use these directly; no exploration needed. -->

**Frozen by 34.13-08 (`src/frontend/state/InstallGameModal.ts`) — CONSUME, DO NOT EDIT:**
- `useSteamInstallOptionsCheck` — a zustand store with `isChecking: boolean`, `appName?: string`,
  `begin(appName)`, `end()`. `begin()` is the first statement of `startSteamInstall`, before any
  `await`; `end()` runs in a `finally` so it releases on the form-opens, zero-friction and
  IPC-rejection paths alike.
- `isSteamInstallOptionsCheckActiveFor(state: { isChecking: boolean; appName?: string }, appName: string, runner: Runner): boolean`
  — returns `state.isChecking && state.appName === appName && runner === 'steam'`. Modelled
  field-for-field on `isSteamBottleSetupActiveFor` (`SteamBottleSetup.ts:45-51`).
- `installSteamGame(appName, gameInfo, path = '', forceWindowsViaBottle = false)` and
  `openInstallGameModal({ appName, runner, gameInfo, action })` — both exported, both already
  imported by files this plan edits.
**Read `34.13-08-SUMMARY.md` first and write against the signatures it records as FINAL.** If they
differ from the above, follow the SUMMARY and report the difference.

**The precedent this plan copies, end to end — `settingUpBottle`.** Trace it once and every edit
below is a mechanical mirror:
- `src/frontend/types.ts:392` — `settingUpBottle: boolean` inside `GameContextType['is']`
  (alphabetical-ish block: `sideloaded` L391, `settingUpBottle` L392, `syncing` L393).
- `src/frontend/screens/Game/GameContext.tsx:31` — `settingUpBottle: false,` in `initialContext.is`.
- `src/frontend/screens/Game/GamePage/index.tsx:171-179` — the comment block plus
  `const steamBottleSetup = useSteamBottleSetup()` and
  `const settingUpBottle = isSteamBottleSetupActiveFor(steamBottleSetup, appName, runner)`.
- `src/frontend/screens/Game/GamePage/index.tsx:413` — `settingUpBottle,` inside
  `contextValues.is`.
- `src/frontend/screens/Game/GamePage/index.tsx:674` — `if (settingUpBottle) return` at the top of
  `handleInstall`.
- `src/frontend/screens/Game/GamePage/components/MainButton.tsx:60` — `is.settingUpBottle` as the
  last term of `disabledInstallButtons` (L46-60).
- `src/frontend/screens/Game/GamePage/components/MainButton.tsx:190-199` — the `getButtonLabel()`
  branch with the `buttonWithIcon` / `faSyncAlt` / `fa-spin` markup.
- `src/frontend/screens/Game/GamePage/components/GameStatus.tsx:135-136` — a third consumer, out of
  scope here (see `<deliberate_exclusions>`).

**`MainButton.tsx` anchors (334 lines):**
- L16-17 already imports `FontAwesomeIcon` and `faSyncAlt`. **No new import is needed.**
- L46-60 `const disabledInstallButtons = …` — an `||` chain ending in `is.settingUpBottle`.
- L129-238 `getButtonLabel()` — an ordered `if` cascade; L192-199 is the `is.settingUpBottle`
  branch, L201-208 the `is.installing` branch.
- L277-309 the install `<button>`: `onClick` at L280-291 (`runner !== 'steam'` →
  `openInstallGameModal`, otherwise `handleInstall(is_installed)`), `disabled={disabledInstallButtons}`
  at L292, `{getButtonLabel()}` at L308.
- **This file imports no `.css`/`.scss`** — which is exactly why it is invocable in Jest.

**`GameCard/index.tsx` anchors (652 lines):**
- L1 `import './index.css'` — **this file can never be imported by a Jest test.**
- L5-6 already imports `FontAwesomeIcon` and `faSyncAlt`. **No new icon import is needed.**
- L121-126 the `useGlobalState.keys(...)` zustand read — house style for a direct store hook here.
- L193-298 `renderIcon()`; L251-258 is the existing Steam spinner branch, verbatim:
  a `<button className="svg-button iconDisabled" disabled>` wrapping
  `<FontAwesomeIcon icon={faSyncAlt} className="fa-spin" />`.
- L285-297 the `else` branch — the clickable `SvgButton className="downIcon" onClick={() => buttonClick()}`
  that is the real library-grid install affordance.
- L338-456 `items: Item[]`; L368-374 is the Install entry
  (`show: !isInstalled && !isQueued && isInstallable && !isDelisted`).
- L316-318 `const isSideloaded/isSteam/isDelisted` — note `isSteam` is declared at L317 yet
  referenced inside `renderIcon` at L240/L252; that is safe because `renderIcon` is only *called* at
  L603. Follow the same shape, but declare the new busy flag with the hooks near the top (React hooks
  must be unconditional and top-level).
- L104-105 two translators: `const { t } = useTranslation('gamepage')` and `const { t: t2 } = useTranslation()`.
  Use `t` with an explicit `gamelib:` prefix — the prefix wins over the bound namespace.
- `Item` (`../ContextMenu`, `index.tsx:7-12`) has exactly `{ icon, label, onclick, show }` — **there
  is no `disabled` field**, so the Install entry is gated via `show`, not via a disabled style.
- `SvgButton` accepts `disabled` (already used at L280).

**`GameSubMenu/index.tsx` anchors (497 lines) — NOT EDITED:**
- L1 `import './index.css'` — not importable by Jest either.
- L15 imports `openInstallGameModal`; its ONLY call is L174-178 inside `handleEdit()`, behind
  `if (isSideloaded)`. `isSideloaded` is `runner === 'sideload'` (L89) and can never be `'steam'`.
- The component renders no Install control: its buttons are Edit / Add shortcut / Uninstall /
  Force Update / Move / Change Install Location / Verify / EOS Overlay / Add to Steam / Categories /
  Store / Changelog / ProtonDB / Requirements / Modify Installation / Browse Files / Browse Prefix.
  Everything install-adjacent sits behind `isInstalled &&` (L312).

**`GamePage/index.tsx` anchors (711 lines):**
- L81 already imports `openInstallGameModal`. **No new import is needed for the repair.**
- L171-179 the `settingUpBottle` computation (the mirror site).
- L387-424 `const contextValues: GameContextType = { … is: { … } … }`; L413 `settingUpBottle,`.
- L338-340 `function handleModal() { openInstallGameModal({ appName, runner, gameInfo }) }`.
- L663-709 `async function handleInstall(is_installed: boolean)`:
  L664-667 the `isQueued` early return, L669-674 the `settingUpBottle` early return, **L676-688 the
  Steam bypass block**, L690-692 `if (!is_installed && !isInstalling) return handleModal()`.
- L527 `handleInstall={handleInstall}` — the prop passed to `MainButton`.

**Test infrastructure — Frontend project (`src/frontend/jest.config.js`):**
`rootDir: '../..'`, `roots: ['<rootDir>/src/frontend']`,
`testMatch: ['**/__tests__/**/*.test.tsx', '**/__tests__/**/*.test.ts']`, `resetMocks: true`,
`modulePaths: [compilerOptions.baseUrl]` (= `./src/`), `testEnvironment` at the Jest default (`node`).
Its docstring is the authority for this plan's testing approach and should be quoted in the new test
file's header: *"Component tests in this project therefore call function components directly (no
ReactDOM/render tree) and mock 'react' (useContext) / 'react-i18next' (useTranslation) at the module
level — this only inspects the returned React-element object graph, which needs no DOM."*

**The live invocation idiom to copy verbatim** —
`src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamIncomplete.test.tsx`
(read it in full; `MainButton.steamWaitingRestart.test.tsx` is its twin):
- `let mockIs: Record<string, boolean>` + `jest.mock('react', () => ({ ...jest.requireActual('react'), useContext: () => ({ is: mockIs, statusContext: mockStatusContext }) }))`
- `jest.mock('react-i18next', () => ({ useTranslation: () => ({ t: (_key, defaultValue) => defaultValue }) }))`
  — **note the consequence: the assertion sees the ENGLISH DEFAULT, not the key.** Assert on
  `Checking install options…`, and separately assert the key by source gate.
- `jest.mock('frontend/hooks/useSetting', …)` and `jest.mock('frontend/state/InstallGameModal', () => ({ openInstallGameModal: jest.fn() }))`
- `import MainButton from '../MainButton'` AFTER the mocks.
- A local `collectText(node)` helper that walks `el.props.children` recursively.
- `mockIs` is typed `Record<string, boolean>`, so adding a required field to `GameContextType['is']`
  does **not** break these two existing files at compile time, and the new field reads `undefined`
  (falsy) in them — they must stay green untouched.
- `collectText` as written only descends `children`. The `disabled` prop is NOT text; assert it by
  locating the button node in the returned tree and reading `props.disabled` directly.

**Comment stripper for source gates:** `src/backend/testUtils/stripSourceComments.ts` exports
`stripSourceComments(source: string)`. It resolves from the Frontend project via `modulePaths` as
`backend/testUtils/stripSourceComments`; live cross-project precedent
`src/frontend/state/__tests__/GlobalStateRefreshCacheGuard.test.ts:40,74`. Mandatory for every
source-text gate in this plan — Task 1 and Task 3 both write comments that legitimately contain
`checkingSteamInstallOptions` and `window.api.install` while explaining the rules.
</interfaces>

<falsified_briefs>
Four upstream claims were checked against source this session. Two are wrong and are repaired here;
one changes this plan's testing strategy for the better; one is a forward handoff.

1. **RESEARCH.md L107/L508-517 and the outline both assert that "7 call sites all funnel through one
   chokepoint" (`startSteamInstall`). FALSE for the game page.**
   `GamePage/index.tsx:676-688` short-circuits `runner === 'steam' && !is_installed` into a DIRECT
   `window.api.install({ appName, path: '', runner: 'steam', installDlcs: [], sdlList: [],
   installLanguage: 'en-US', platformToInstall: 'Windows', gameInfo })` — a hand-copied duplicate of
   `installSteamGame`, carrying a Phase 21 `(D-04)` comment. `MainButton`'s Steam click routes to
   `handleInstall`, so it reaches this block and never touches `openInstallGameModal`.
   **Consequence if unrepaired:** `startSteamInstall` never runs from the game page, so `begin()`
   never fires, so `is.checkingSteamInstallOptions` is permanently `false` there — this plan's
   MainButton deliverable would be unreachable dead code that still passes an invocation test. And
   more broadly the entire phase — the form, D-10's dual trigger, D-17's platform override — would
   silently not exist on the game page. Task 1 repairs it.

2. **RESEARCH.md L515 lists `GameSubMenu/index.tsx:176` as an install call site. Technically true,
   materially false.** That call is `handleEdit()`'s sideload branch (`if (isSideloaded)`),
   unreachable for `runner === 'steam'`. `GameSubMenu` renders no Install control whatsoever.
   Do **not** invent one and do **not** add a spinner there. Task 3 covers it by proof of absence.
   The same applies to `GameCard/index.tsx:322` (also `handleEdit()`, also sideload-only) — GameCard
   IS an origin, but via `buttonClick()` (→ `GamesList/index.tsx:194-197` →
   `Library/index.tsx:469-475 handleModal` → `openInstallGameModal`), never via L322.

3. **The outline's note 4 says these three `.tsx` call sites "cannot be rendered in Jest" and that
   only source gates are available. Half wrong — in this plan's favour.** Rendering is indeed
   impossible, but `MainButton.tsx` imports no stylesheet and is therefore *invocable*: two live
   tests (`MainButton.steamIncomplete.test.tsx`, `MainButton.steamWaitingRestart.test.tsx`) already
   call it as a function and search the returned element graph, and `src/frontend/jest.config.js`'s
   docstring sanctions the technique by name. **Task 2 therefore delivers real behavioral evidence,
   not a source gate.** `GameCard` and `GameSubMenu` genuinely are source-gate-only — both open with
   `import './index.css'`.

4. **The UI-SPEC's own Copywriting Contract table (L93-125) omits
   `gamelib:status.checkingSteamInstallOptions`.** The key appears only inside the D-12 Busy State
   Contract code block at L365. 34.13-13 sources the catalog from that table, so the key will be
   missed unless handed forward. This plan ships the inline English default (so the UI is correct
   either way) and records the obligation in `<handoffs>`.
</falsified_briefs>

<deliberate_exclusions>
Named so an executor does not "helpfully" widen scope. Each is a decision, not an oversight.

- **`GameStatus.tsx:135-136`** — the third `is.settingUpBottle` consumer. Not wired here: the
  UI-SPEC's D-12 contract names exactly three surfaces, and the busy state is bounded at 15s and is
  already legible on the button the user just clicked. Mirroring it into the status line is a second
  surface the spec did not ask for. Record it in the SUMMARY as a considered exclusion.
- **`SteamClientSetup.tsx:78`** (`installSteamGame(appName, gameInfoRef.current)`) and
  **`SteamBridgeSetup.tsx:79-88`** — post-consent retries. Routing them through the trigger would
  re-ask a question the user just answered. `SteamBridgeSetup` gets a comment-only cross-reference
  repair (Task 1) because its comment cites the block Task 1 rewrites.
- **A re-entrancy latch inside `startSteamInstall`.** Tempting, but it belongs to 34.13-08, which is
  frozen. The disabled control plus `handleInstall`'s early return are this plan's controls; see
  `<threat_model>` T-34.13-11-01 for why that is sufficient and where the residual risk sits.
- **`RecentlyPlayed/index.tsx:104`** — another `GameCard` host passing the same `handleModal`. It
  needs no edit: the busy state lives inside `GameCard`, so every host inherits it.
</deliberate_exclusions>

<handoffs>
- **To 34.13-13 (single catalog writer):** this plan ships exactly ONE new user-facing string,
  `gamelib:status.checkingSteamInstallOptions` → `Checking install options…` (U+2026 ellipsis,
  matching `'Setting up Steam…'`). It is **absent from the UI-SPEC Copywriting Contract table** and
  must still be added to `public/locales/en/gamelib.json` for `pnpm lint-translations:gamelib`.
  The SUMMARY must state this explicitly.
- **To 34.13-13's manual gate:** the D-12 row must be exercised on BOTH origin surfaces — the
  library grid card AND the game page primary button — because they are wired through two different
  mechanisms (direct zustand subscription vs. GameContext). One click-through of one surface is not
  coverage. Worst case to observe: **15 seconds on a cold metadata cache**.
- **To 34.13-13's phase gate:** add `grep -rn "platformToInstall: 'Windows'" src/frontend` to the
  sweep. After Task 1 it must return exactly TWO hits — `state/InstallGameModal.ts` (the sanctioned
  marshaller) and `GamePage/components/SteamBridgeSetup.tsx` (the sanctioned post-consent retry) —
  and never a third.
</handoffs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plumb the D-12 predicate into GameContext and repair the game-page chokepoint bypass</name>

  <files>src/frontend/types.ts, src/frontend/screens/Game/GameContext.tsx, src/frontend/screens/Game/GamePage/index.tsx, src/frontend/screens/Game/GamePage/components/SteamBridgeSetup.tsx</files>

  <read_first>
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-08-SUMMARY.md` —
      the FINAL exported names and signatures for `useSteamInstallOptionsCheck` and
      `isSteamInstallOptionsCheckActiveFor`, plus the recorded `METADATA_FETCH_TIMEOUT_MS` value.
      Write against what shipped, not against this plan's prediction. If they differ, follow the
      SUMMARY and report it.
    - `src/frontend/state/InstallGameModal.ts` — READ ONLY. Confirm the two exports exist and read
      the predicate's doc comment (it names this plan's three consumers by file). Do not edit.
    - `src/frontend/state/SteamBottleSetup.ts` (51 lines, full file) — the sibling predicate and the
      reason its docstring gives for existing as ONE exported pure function.
    - `src/frontend/types.ts` lines 375-400 — the `GameContextType['is']` block, to place the new
      field beside `settingUpBottle` (L392).
    - `src/frontend/screens/Game/GameContext.tsx` (full file, ~42 lines) — `initialContext.is` at
      L14-36. Every field in `GameContextType['is']` is required, so this file MUST gain the new key
      or `tsc` fails.
    - `src/frontend/screens/Game/GamePage/index.tsx` lines 165-200 (the `settingUpBottle` mirror
      site), 385-425 (`contextValues`), and 663-709 (`handleInstall` in full, including the Steam
      bypass block at 676-688 and the `handleModal` fall-through at 690-692).
    - `src/frontend/screens/Game/GamePage/index.tsx` lines 75-90 — confirm `openInstallGameModal` is
      already imported (L81) so the repair adds no import.
    - `src/frontend/screens/Game/GamePage/components/SteamBridgeSetup.tsx` lines 60-95 — the
      comment at L76-78 that cites "GamePage's handleInstall … (D-04)" as its precedent. That
      citation goes stale the moment this task lands.
  </read_first>

  <action>
Four edits. The first three are a mechanical mirror of `settingUpBottle`; the fourth is a
falsified-brief repair without which this plan's own deliverable is unreachable.

**1. `src/frontend/types.ts`** — inside `GameContextType['is']`, immediately after
`settingUpBottle: boolean` (L392), add `checkingSteamInstallOptions: boolean`. Doc-comment it with
three facts: it is Phase 34.13 D-12; it reflects 34.13-08's transient
`useSteamInstallOptionsCheck` store via the exported `isSteamInstallOptionsCheckActiveFor`
predicate; and its worst-case duration is **15 seconds on a cold metadata cache
(`METADATA_FETCH_TIMEOUT_MS` = 15000ms)**. Write that number and that constant name verbatim — a
downstream reader must not have to guess whether the state is a 200ms flicker or a quarter-minute
wait.

**2. `src/frontend/screens/Game/GameContext.tsx`** — add `checkingSteamInstallOptions: false,` to
`initialContext.is`, beside `settingUpBottle: false,` (L31). Required, not optional: the object is
typed `GameContextType`.

**3. `src/frontend/screens/Game/GamePage/index.tsx`** — three edits, all mirrors:
   a. Beside the existing `useSteamBottleSetup()` block (L171-179), add
      `const steamInstallOptionsCheck = useSteamInstallOptionsCheck()` and
      `const checkingSteamInstallOptions = isSteamInstallOptionsCheckActiveFor(steamInstallOptionsCheck, appName, runner)`.
      Import both from `frontend/state/InstallGameModal` (the file already imports
      `openInstallGameModal` from there at L81 — extend that import, do not add a second one).
      Comment it as the D-12 mirror of the Phase 17 `settingUpBottle` block directly above, and
      state that the predicate is called rather than re-derived so the card, the button and the
      status can never disagree.
   b. In `contextValues.is` (L394-419), add `checkingSteamInstallOptions,` immediately after
      `settingUpBottle,` (L413).
   c. In `handleInstall` (L663), add `if (checkingSteamInstallOptions) return` immediately after the
      existing `if (settingUpBottle) return` (L674). Comment it as **defense in depth, not the
      primary control** — the primary control is `MainButton`'s `disabled` attribute (Task 2); this
      guard exists because `handleInstall` is a prop and a future caller might not be disabled.

**4. `src/frontend/screens/Game/GamePage/index.tsx` — THE REPAIR.** Replace the BODY of the
`gameInfo.runner === 'steam' && !is_installed` block (L676-688). Keep the guard condition exactly as
it is. Replace the 10-line hand-marshalled `window.api.install({...})` call with a single dispatch
through the chokepoint: `openInstallGameModal({ appName, runner: 'steam', gameInfo, action: 'install' })`.

Requirements on this edit:
- `window.api.install` must no longer appear anywhere in this file.
- The literal `platformToInstall: 'Windows'` must no longer appear anywhere in this file.
- Replace the stale `// Steam: bypass install-location modal — delegate straight to
  SteamGame.install() (D-04)` comment. The new comment must record: the Phase 21 `(D-04)` bypass
  duplicated `installSteamGame`'s `InstallArgs` by hand and is retired; the game page now shares the
  single chokepoint with the library grid, so Phase 34.13's D-10 trigger, D-12 busy state and D-17
  platform override reach the game page at all; and the **zero-friction outcome is unchanged** —
  when `steamInstallFormOpens()` returns `false`, `startSteamInstall` calls
  `installSteamGame(appName, gameInfo)`, which marshals byte-identical `InstallArgs`
  (`path: ''`, `installDlcs: []`, `sdlList: []`, `installLanguage: 'en-US'`,
  `platformToInstall: 'Windows'`). Phase 21 D-09 is preserved, not reversed.
- Do NOT touch the `isQueued` early return, the `settingUpBottle` early return, the
  `!is_installed && !isInstalling → handleModal()` fall-through, the `!folder` guard, the sideload
  guard, or the trailing `install({...})` call.

**5. `src/frontend/screens/Game/GamePage/components/SteamBridgeSetup.tsx` — COMMENT ONLY.** Its
L76-78 comment claims "Same direct Steam-install invocation GamePage's handleInstall uses for
`runner === 'steam'` (D-04)". That is false after edit 4. Rewrite those lines to state that this is
a deliberate **post-consent bridge-fallback retry** which stays zero-friction on purpose — the user
has already consented, so re-opening the Phase 34.13 install form here would re-ask an answered
question — and that GamePage's `handleInstall` now dispatches through `openInstallGameModal`
instead. **Change no executable line in this file.** This repo carries a ledgered lesson that stale
cross-references rot silently; repair it in the commit that falsifies it.

Then run the full suite: this task changes a required context field and the game page's install
dispatch, both of which many suites import transitively.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx jest src/frontend/screens/Game/GamePage/components/__tests__ --silent && pnpm test:ci</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with zero output.
    - The field is declared and defaulted:
      `grep -c 'checkingSteamInstallOptions: boolean' src/frontend/types.ts` → `1`;
      `grep -c 'checkingSteamInstallOptions: false' src/frontend/screens/Game/GameContext.tsx` → `1`.
    - The worst case is verbatim, not paraphrased:
      `grep -c 'METADATA_FETCH_TIMEOUT_MS' src/frontend/types.ts` → `1` and
      `grep -c '15000' src/frontend/types.ts` → `1`.
    - The predicate is called, not re-derived. Over comment-stripped source
      (`grep -vE '^\s*(//|\*|/\*)' src/frontend/screens/Game/GamePage/index.tsx`):
      `grep -c 'isSteamInstallOptionsCheckActiveFor('` → `1`, and
      `grep -cE 'isChecking\s*&&'` → `0`.
    - **The bypass is gone** — `grep -c 'window.api.install' src/frontend/screens/Game/GamePage/index.tsx` → `0`
      and `grep -c "platformToInstall" src/frontend/screens/Game/GamePage/index.tsx` → `0`.
      **Proven non-vacuous at planning time:** the pre-edit file matches `window.api.install` once
      (L678) and `platformToInstall` once (L685). Record both pre/post counts in the SUMMARY.
    - **The dispatch is present** — over comment-stripped source of the same file,
      `grep -c 'openInstallGameModal('` → `2` (the pre-existing `handleModal` at L339 plus the new
      Steam branch). A count of `1` means the repair was not applied; a count of `3+` means the
      surrounding flow was restructured. Pre-edit the same command returns `1`.
    - The re-entrancy guard exists and sits with its sibling: over comment-stripped source,
      `grep -c 'if (checkingSteamInstallOptions) return'` → `1`, and its line index is greater than
      that of `if (settingUpBottle) return`.
    - The context wiring is complete: `grep -c 'checkingSteamInstallOptions,' src/frontend/screens/Game/GamePage/index.tsx` → `1`.
    - The `SteamBridgeSetup.tsx` edit is comment-only: `git diff -U0 src/frontend/screens/Game/GamePage/components/SteamBridgeSetup.tsx`
      shows every added/removed line beginning with optional whitespace then `//` or `*`. No
      `window.api`, no `import`, no JSX line changed. Independently:
      `grep -c 'window.api.install' src/frontend/screens/Game/GamePage/components/SteamBridgeSetup.tsx` → `1` (unchanged).
    - The stale citation is gone:
      `grep -c "Same direct Steam-install invocation" src/frontend/screens/Game/GamePage/components/SteamBridgeSetup.tsx` → `0`.
    - Both pre-existing MainButton tests stay green **and untouched**:
      `npx jest src/frontend/screens/Game/GamePage/components/__tests__ --silent` exits 0 and
      `git status --porcelain src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamIncomplete.test.tsx src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamWaitingRestart.test.tsx` → empty.
    - `pnpm test:ci` exits 0. Any suite that flips red here is a real regression — nothing in this
      task is expected to require a test update.
    - No new file was created: `git diff --name-only --diff-filter=A` → empty.
    - Ownership gate — `git status --porcelain src/common/ src/backend/ public/locales/ src/frontend/state/ src/frontend/screens/Library/ package.json pnpm-lock.yaml` → empty.
  </acceptance_criteria>

  <done>`GameContextType['is'].checkingSteamInstallOptions` exists, defaults to `false`, and is computed on the game page from 34.13-08's single exported predicate; the Phase 21 direct-`window.api.install` bypass is retired so the game page shares the install chokepoint (pre/post grep counts recorded); and `SteamBridgeSetup.tsx`'s now-false cross-reference is repaired comment-only in the same commit that falsified it.</done>
</task>

<task type="auto">
  <name>Task 2: MainButton goes busy and disabled — RED-proven by direct component invocation</name>

  <files>src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamInstallOptions.test.tsx, src/frontend/screens/Game/GamePage/components/MainButton.tsx</files>

  <read_first>
    - `src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamIncomplete.test.tsx`
      (full file) — the exact harness to copy: the four `jest.mock` calls, the `mockIs` /
      `mockStatusContext` module-scoped lets, `collectText`, the `steamGameInfo` fixture builder,
      and its header docstring explaining the no-jsdom pattern.
    - `src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamWaitingRestart.test.tsx`
      (full file) — its twin; note how the two differ in what they assert so the new file's naming
      and structure are consistent with both.
    - `src/frontend/screens/Game/GamePage/components/MainButton.tsx` (334 lines, full file) — in
      particular `disabledInstallButtons` (L46-60), the whole `getButtonLabel()` cascade (L129-238)
      with its `is.settingUpBottle` branch (L192-199), and the install `<button>` (L277-309) where
      `disabled={disabledInstallButtons}` sits at L292.
    - `src/frontend/jest.config.js` — read the docstring in full. It is the authority for why this
      test invokes rather than renders, and it must be cited in the new file's header.
    - `src/frontend/screens/Humble/Keys/index.tsx` lines 20-30 — the live
      `t('gamelib:key', 'English default')` precedent, for the namespace-prefix form.
  </read_first>

  <action>
**Part A — write the spec file first and observe it RED.**

Create `src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamInstallOptions.test.tsx`,
copying the harness from `MainButton.steamIncomplete.test.tsx` verbatim (same four `jest.mock`
calls, same `collectText`, same fixture-builder shape).

**Header comment** must record three things: (a) this is Phase 34.13 D-12 — the install control the
user clicked must be visibly busy AND disabled while `isSteamBottleEligible` resolves, worst case
**15 seconds on a cold metadata cache (`METADATA_FETCH_TIMEOUT_MS` = 15000ms)**; (b) the component
is INVOKED as a plain function and its returned React-element object graph is inspected — nothing
is rendered, there is no DOM, no click is simulated, quoting `src/frontend/jest.config.js`'s
docstring as the authority; (c) the vacuity boundary — this proves the element graph, not pixels;
that the spinner is actually visible on screen is 34.13-13's blocking manual gate.

**Harness addition.** `collectText` only walks `children`, so add a `findInstallButton(tree)` helper
that recursively locates the element whose `props.className` (a `classNames(...)` result string)
contains `mainBtn` and whose subtree text is the install-side label — then assert on its
`props.disabled` directly. Do NOT assert `disabled` by text.

**Specs.**
  1. `renders the busy label while the Steam install-options check is active` — `mockIs` with
     `checkingSteamInstallOptions: true`, an uninstalled Steam `gameInfo`. Assert the collected text
     contains `Checking install options…` (the English default — `react-i18next` is mocked to return
     the default value, so the key never appears).
  2. **`DISCRIMINATOR: the busy button is also disabled — a spinning but clickable button fails`** —
     same input; assert `findInstallButton(tree).props.disabled === true`. Name it as the
     discriminator in the title: a label-only implementation passes spec 1 and fails only this one,
     and a label-only implementation is exactly the dead-click D-12 forbids.
  3. `does not render the busy label when the check is inactive` — `checkingSteamInstallOptions:
     false`, everything else identical. Assert the text does NOT contain `Checking install options`
     and DOES contain `Install`, and that `props.disabled` is falsy.
  4. **`DISCRIMINATOR: the busy state is not conflated with the guided bottle setup`** — with
     `checkingSteamInstallOptions: true` and `settingUpBottle: false`, assert the text does NOT
     contain `Setting up Steam`. Then invert (`settingUpBottle: true`,
     `checkingSteamInstallOptions: false`) and assert it does NOT contain `Checking install options`.
     The two adjacent branches must not swallow each other.
  5. `the guided bottle setup wins when both are somehow true` — both flags `true`; assert the text
     contains `Setting up Steam` and NOT `Checking install options`, and `props.disabled === true`.
     This pins the branch ORDER deterministically so a later reader cannot reorder the cascade
     without a failing test.
  6. `a non-steam runner is unaffected` — `gameInfo.runner: 'gog'` with
     `checkingSteamInstallOptions: false` (the predicate can only be true for `'steam'`, so the
     context value is what varies, not the runner). Assert the label is `Install` and
     `props.disabled` is falsy. Guards against a term accidentally added to `disabledPlayButtons` or
     to a runner-agnostic condition.
  7. **Source gates** over `stripSourceComments(readFileSync('.../MainButton.tsx'))`:
     - contains `is.checkingSteamInstallOptions` at least twice (the `disabledInstallButtons` term
       and the label branch).
     - contains `gamelib:status.checkingSteamInstallOptions` exactly once — the key survives even
       though the mocked `t` hides it from every behavioral assertion above.
     - contains ZERO matches of `useSteamInstallOptionsCheck` — `MainButton` reads context, never
       the store directly. **Prove this gate non-vacuous in the same spec**: assert the same
       substring search DOES hit against a small inline known-bad specimen string.
     - `fa-spin` count is exactly `3` and `faSyncAlt` count is exactly `3` — the two pre-existing
       Steam branches (L184, L195) plus the one added here, and they stay paired. Pre-edit both are
       `2`; assert the numbers, and record pre/post in the SUMMARY.
     - contains ZERO matches of `CircularProgress` and ZERO of `<Spinner` — no alternative spinner
       was introduced.

**RED-prove the whole file** against the current tree. Expected failures: specs 1, 2, 5 and the
source gates. Capture the output verbatim. **A RED that does not name spec 2 (the disabled
discriminator) means the harness is not actually reading `props.disabled` — fix the harness, do not
proceed.**

**Part B — implement in `MainButton.tsx`.** Two edits, no new imports (`FontAwesomeIcon` and
`faSyncAlt` are already imported at L16-17):
  - Append `is.checkingSteamInstallOptions` as a new final term of the `disabledInstallButtons`
    `||` chain (L46-60), directly after `is.settingUpBottle`. Comment it as Phase 34.13 D-12 and
    state plainly that **the disabled attribute IS the re-entrancy control** — the eligibility await
    runs up to 15s and a second click would dispatch a second `startSteamInstall`.
  - Add a `getButtonLabel()` branch immediately AFTER the `is.settingUpBottle` branch (L192-199) and
    BEFORE the `is.installing` branch (L201). Copy the settingUpBottle branch's markup shape
    verbatim — `<span className="buttonWithIcon">`, `<FontAwesomeIcon icon={faSyncAlt} className="fa-spin" />`,
    then `{t('gamelib:status.checkingSteamInstallOptions', 'Checking install options…')}` (U+2026
    ellipsis, matching `'Setting up Steam…'`). Comment the ORDER decision: bottle setup is a
    longer-lived state and keeps precedence; the check is transient and must not mask it. Note in
    the comment that the `gamelib:` prefix is deliberate — this component's `useTranslation` is
    bound to `'gamepage'`, and the prefix overrides it (precedent `Humble/Keys/index.tsx:25`).

Do NOT edit the test file in Part B. Do NOT edit `public/locales/`.
  </action>

  <verify>
    <automated>npx jest src/frontend/screens/Game/GamePage/components/__tests__ --silent && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - Part A: `npx jest src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamInstallOptions.test.tsx --silent`
      **exits non-zero** against the unmodified `MainButton.tsx`, and the captured RED output names
      spec 2 (the disabled discriminator). Recorded verbatim in the SUMMARY.
    - Part B: the same command **exits 0**, and
      `npx jest src/frontend/screens/Game/GamePage/components/__tests__ --silent` exits 0 — the two
      pre-existing MainButton suites included, unmodified
      (`git status --porcelain` shows no change to either).
    - These spec-title substrings appear verbatim in the new file: `DISCRIMINATOR: the busy button is
      also disabled`, `DISCRIMINATOR: the busy state is not conflated`, `the guided bottle setup wins
      when both are somehow true`.
    - The disabled assertion reads a prop, not text: the file contains `props.disabled` and does NOT
      assert the word "disabled" via `collectText`.
    - No rendering was introduced anywhere:
      `grep -rniE 'jsdom|react-test-renderer|@testing-library|ReactDOM|\.render\(|fireEvent|simulate' src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamInstallOptions.test.tsx` → no matches.
    - Idiom reuse, over comment-stripped `MainButton.tsx`: `fa-spin` count → `3`, `faSyncAlt` count →
      `3`, `buttonWithIcon` count unchanged from pre-edit +1, `CircularProgress` count → `0`.
      (Pre-edit `fa-spin` and `faSyncAlt` are both `2`.)
    - The key ships inline and the catalog is untouched:
      `grep -c "gamelib:status.checkingSteamInstallOptions" src/frontend/screens/Game/GamePage/components/MainButton.tsx` → `1`,
      and `git status --porcelain public/locales/` → empty.
    - The disable term landed in the install chain, not the play chain: over comment-stripped source,
      the index of `is.checkingSteamInstallOptions` inside `disabledInstallButtons` is greater than
      the index of `const disabledInstallButtons` and less than the index of `function getPlayLabel`;
      and `disabledPlayButtons`'s own `||` chain does NOT contain it.
    - `npx tsc --noEmit` exits 0. `npx eslint src/frontend/screens/Game/GamePage/components/MainButton.tsx` exits 0.
    - No new source file: `git diff --name-only --diff-filter=A src/frontend` lists exactly one path,
      the new test file.
    - Ownership gate — `git status --porcelain src/common/ src/backend/ public/locales/ src/frontend/state/ src/frontend/screens/Library/ package.json pnpm-lock.yaml` → empty.
  </acceptance_criteria>

  <done>The game page's primary install button renders `Checking install options…` with the existing `faSyncAlt`/`fa-spin` markup and is simultaneously `disabled` while the D-12 check is active — proven by direct component invocation, with a discriminator that a spinning-but-clickable button fails and a branch-order spec that pins it against the adjacent bottle-setup state.</done>
</task>

<task type="auto">
  <name>Task 3: GameCard goes busy, and the remaining two surfaces are gated by comment-stripped source proofs</name>

  <files>src/frontend/screens/Library/components/GameCard/index.tsx, src/frontend/screens/Library/components/GameCard/__tests__/steamInstallOptionsBusy.test.ts</files>

  <read_first>
    - `src/frontend/screens/Library/components/GameCard/index.tsx` (652 lines) — specifically L1-60
      (imports; note `import './index.css'` on L1 and that `FontAwesomeIcon`/`faSyncAlt` are already
      imported at L5-6), L100-155 (hooks and destructuring), L193-298 (`renderIcon()` in full,
      especially the Steam spinner branch at L251-258 and the clickable install `else` at L285-297),
      L316-318 (`isSideloaded`/`isSteam`/`isDelisted`), and L338-456 (`items`, especially the Install
      entry at L368-374).
    - `src/frontend/screens/Library/components/ContextMenu/index.tsx` lines 1-20 — the `Item`
      interface. Confirm for yourself that it has `{ icon, label, onclick, show }` and **no**
      `disabled` field, which is why the Install entry is gated via `show`.
    - `src/frontend/screens/Game/GameSubMenu/index.tsx` — read enough to confirm for yourself that
      its only `openInstallGameModal` call (L174-178) is inside `handleEdit()` behind
      `if (isSideloaded)`, and that no Install control is rendered anywhere in the component. If that
      turns out to be false, STOP and report it: this task's proof-of-absence gate would be wrong.
    - `src/frontend/state/__tests__/GlobalStateRefreshCacheGuard.test.ts` lines 30-90 — the live
      cross-project `stripSourceComments` + `readFileSync` source-gate idiom, including its import
      path `backend/testUtils/stripSourceComments`.
    - `src/backend/testUtils/stripSourceComments.ts` — read the doc comment in full; it explains why
      a bare regex over raw source is a rotted gate. Mandatory here, because this task's own comments
      will legitimately contain `checkingSteamInstallOptions` and `buttonClick` while explaining the
      rules.
    - `src/frontend/screens/Library/components/GamesList/index.tsx` lines 190-205 and
      `src/frontend/screens/Library/index.tsx` lines 469-476 — confirm the real GameCard install
      route (`buttonClick` → `handleGameCardClick` → `handleModal` → `openInstallGameModal`), so the
      gate is written against the affordance that actually dispatches.
  </read_first>

  <action>
**Part A — wire `GameCard/index.tsx`.** Three edits, no new icon import needed:

  1. **Subscribe.** With the other top-level hooks (beside the `useGlobalState.keys(...)` block at
     L121-126), add `const steamInstallOptionsCheck = useSteamInstallOptionsCheck()` and
     `const checkingSteamInstallOptions = isSteamInstallOptionsCheckActiveFor(steamInstallOptionsCheck, appName, runner)`,
     importing both from `frontend/state/InstallGameModal` — extend the existing L58 import of
     `openInstallGameModal`, do not add a second import statement. Comment that `GameCard` sits in
     the library tree with no `GameContext`, so it subscribes to the store directly while still
     calling the SAME exported predicate the game page uses — one definition, two subscription
     mechanisms. `appName` and `runner` are already destructured at L130-136.

  2. **`renderIcon()` busy branch.** Insert a new branch immediately BEFORE the existing
     `if (isInstalling && isSteam)` branch (L251-258), gated on `checkingSteamInstallOptions`. Copy
     that branch's markup verbatim — `<button className="svg-button iconDisabled" disabled>` wrapping
     `<FontAwesomeIcon icon={faSyncAlt} className="fa-spin" />` — and add
     `title={t('gamelib:status.checkingSteamInstallOptions', 'Checking install options…')}` to the
     button, because unlike `MainButton` this affordance is icon-only and would otherwise be a
     wordless spinner. Comment the placement: the check strictly precedes any install dispatch, so
     it must be evaluated before the installing branch; and returning a `disabled` `<button>`
     instead of the clickable `SvgButton` from L285-297 is what makes the dead click impossible —
     **the disabled control IS the re-entrancy control.**

  3. **Context-menu Install entry.** Append `&& !checkingSteamInstallOptions` to the `show`
     expression of the Install item (L368-374). Comment that `Item` carries no `disabled` field, so
     hiding is the only available gate, and that leaving it shown would be a second, ungated
     dispatch path straight past the spinner on the card.

Do NOT touch the `handleEdit`/L322 sideload path, the `handlePlay` install branch (L611-622, reached
only for non-Steam runners from the play/cancel icon), or any other `items` entry.

**Part B — the cross-surface gate file.** Create
`src/frontend/screens/Library/components/GameCard/__tests__/steamInstallOptionsBusy.test.ts`
(a `.ts` file with no JSX and no component import).

**Header comment** must record: `GameCard/index.tsx` and `GameSubMenu/index.tsx` both open with
`import './index.css'`, so — unlike `MainButton` — they cannot be imported by any test in this
project at all (no jsdom, and ts-jest has no CSS transform). These are therefore comment-stripped
SOURCE gates, and their vacuity boundary is explicit: they prove the code says what it must, not
that anything renders. 34.13-13's blocking manual gate is the only evidence for the latter.

Read each target with `readFileSync` and run it through `stripSourceComments` before asserting.

  **Group A — `GameCard/index.tsx` is wired.**
    A1. contains `isSteamInstallOptionsCheckActiveFor(` exactly once, and `useSteamInstallOptionsCheck()`
        exactly once.
    A2. contains ZERO matches of `/isChecking\s*&&/` — the condition is never hand-rolled here.
        **Prove non-vacuous in the same spec** against a small inline known-bad specimen string.
    A3. `fa-spin` count is exactly `2` and `faSyncAlt` count is exactly `2` (the pre-existing
        installing branch plus the new checking branch). Pre-edit both are `1`.
    A4. contains `gamelib:status.checkingSteamInstallOptions` exactly once.
    A5. contains ZERO matches of `useState` **initialised from** `checkingSteamInstallOptions` — no
        local mirror that could latch independently of 34.13-08's `finally`. Implement as: the
        stripped source contains no line matching `/useState\([^)]*checking/i`.
    A6. **Ordering, structurally:** the index of `checkingSteamInstallOptions` inside `renderIcon`'s
        body precedes the index of `isInstalling && isSteam`. Locate `renderIcon`'s body by slicing
        the stripped source between `const renderIcon` and `const isHiddenGame`.
    A7. The context-menu Install entry is gated: the stripped source contains
        `!checkingSteamInstallOptions` at least twice (the `renderIcon` branch condition and the
        `show` expression).

  **Group B — `GameSubMenu/index.tsx` proof of absence (falsified brief 2).**
    B1. `openInstallGameModal` appears exactly once in the stripped source, and the slice of source
        containing it also contains `isSideloaded` — i.e. the only call is still the sideload edit
        path, not a new install affordance.
    B2. contains ZERO matches of `checkingSteamInstallOptions`, ZERO of `useSteamInstallOptionsCheck`
        and ZERO of `fa-spin`. Title the spec so it reads as a DELIBERATE non-edit, e.g.
        `GameSubMenu renders no install control, so it is deliberately not wired (falsified brief 2)`.
        **Prove non-vacuous** by asserting the same three searches DO hit against the stripped
        `GameCard/index.tsx` source, which this task just wired.
    B3. contains ZERO matches of `t('button.install'` — GameSubMenu offers no Install label.

  **Group C — the Task 1 chokepoint repair stays repaired** (a regression guard on the
  falsified-brief fix, over stripped `GamePage/index.tsx`):
    C1. ZERO matches of `window.api.install` and ZERO of `platformToInstall`.
    C2. `openInstallGameModal(` appears exactly twice.
    C3. `isSteamInstallOptionsCheckActiveFor(` appears exactly once.

  **Group D — the no-new-spinner-component negative gate.** Assert with `existsSync` that none of
  these exist: `src/frontend/components/UI/SteamInstallOptionsSpinner.tsx`,
  `src/frontend/components/UI/InstallOptionsSpinner.tsx`, and any `Spinner`-named file directly
  under `src/frontend/screens/Library/components/GameCard/` or
  `src/frontend/screens/Game/GamePage/components/`. Enumerate each directory with `readdirSync` and
  assert no entry matches `/spinner/i`. **Prove non-vacuous** by asserting the same `/spinner/i`
  match DOES hit a hard-coded specimen array containing `'MySpinner.tsx'`.

**RED-prove Group A and Group C** before Part A lands, or — if Part A is written first — verify by
temporarily reverting the `renderIcon` branch and confirming A3/A6/A7 fail while Groups B and D stay
green. Either ordering is acceptable; record which was used and the observed asymmetry in the
SUMMARY. A gate that only ever passed is not evidence.

Then run the full suite — this task edits a component nearly every library view mounts.
  </action>

  <verify>
    <automated>npx jest src/frontend/screens/Library/components/GameCard/__tests__/steamInstallOptionsBusy.test.ts --silent && npx tsc --noEmit && pnpm test:ci</automated>
  </verify>

  <acceptance_criteria>
    - `npx jest src/frontend/screens/Library/components/GameCard/__tests__/steamInstallOptionsBusy.test.ts --silent`
      exits 0 with Groups A, B, C and D all passing.
    - The RED/deliberate-breakage asymmetry was OBSERVED and is recorded verbatim in the SUMMARY:
      with the `renderIcon` busy branch reverted, A3/A6/A7 FAIL while Groups B and D stay GREEN.
    - Every non-vacuity backstop is present in the file — the A2, B2 and D specs each contain a
      positive assertion against a known-bad specimen, so an unmatchable pattern cannot pass silently.
    - `GameSubMenu/index.tsx` is byte-identical:
      `git status --porcelain src/frontend/screens/Game/GameSubMenu/index.tsx` → empty. It is
      COVERED by Group B, not silently dropped.
    - Idiom reuse in `GameCard/index.tsx`, over comment-stripped source: `fa-spin` count → `2`,
      `faSyncAlt` count → `2`, `svg-button iconDisabled` count → `2`, `CircularProgress` count → `0`.
      (Pre-edit: `fa-spin` `1`, `faSyncAlt` `1`, `svg-button iconDisabled` `1`.) Record pre/post.
    - **No new spinner component anywhere:** `git diff --name-only --diff-filter=A src/frontend`
      lists exactly TWO paths across this whole plan, both under `__tests__`. Any third added path
      is a blocking deviation.
    - The catalog fence held: `git status --porcelain public/locales/` → empty, and
      `grep -c "gamelib:status.checkingSteamInstallOptions" src/frontend/screens/Library/components/GameCard/index.tsx` → `1`.
    - No rendering was introduced:
      `grep -rniE 'jsdom|react-test-renderer|@testing-library|ReactDOM|fireEvent|simulate' src/frontend/screens/Library/components/GameCard/__tests__/steamInstallOptionsBusy.test.ts` → no matches,
      and the file contains no `import` of any `.tsx` module.
    - `npx tsc --noEmit` exits 0. `npx eslint src/frontend/screens/Library/components/GameCard/index.tsx` exits 0.
    - `pnpm test:ci` exits 0 with no pre-existing suite regressed.
    - Ownership gate — `git status --porcelain src/common/ src/backend/ public/locales/ src/frontend/state/ src/frontend/screens/Library/components/InstallModal/ package.json pnpm-lock.yaml` → empty.
  </acceptance_criteria>

  <done>The library card's install affordance returns a disabled spinner button with a titled tooltip while the D-12 check runs and its context-menu Install entry disappears with it; `GameSubMenu` is proven by absence to need no wiring; the Task 1 chokepoint repair has a standing regression guard; and the "no new spinner component" rule is enforced by a filesystem gate proven to fail against a known-bad specimen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → renderer dispatch (`GameCard.buttonClick`, `MainButton.handleInstall`, `GameCard` context-menu Install) | **The boundary this plan exists to control.** A human click is the untrusted event; the question is what happens when it arrives twice, or arrives while a 15-second async decision is already in flight. |
| renderer → backend, `window.api.install(InstallArgs)` | **Crossed differently after Task 1.** The game page stops hand-marshalling `InstallArgs` and delegates to `installSteamGame` via the chokepoint. Strictly fewer construction sites, not more. |
| renderer-internal, `useSteamInstallOptionsCheck` → three surfaces | A zustand store read. No boundary — in-process, no serialization, no persistence, no log sink. |
| renderer → i18next, the new `gamelib:` key | Static English default compiled into the bundle. No interpolation, no user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.13-11-01 | Denial of Service (re-entrancy) | Double-click / repeat-click on an install control during the up-to-15s eligibility await, dispatching `startSteamInstall` two or more times — two concurrent IPC round-trips, two `installSteamGame` calls, and a `begin()`/`end()` pair whose `end()` from the first invocation clears the busy state while the second is still pending | **mitigate** | **A disabled control is the control, and it is applied to every dispatching affordance, not just the obvious one.** (a) `MainButton`'s install `<button>` gains `is.checkingSteamInstallOptions` as a term of `disabledInstallButtons`, so `disabled` is set on the same element that carries the spinner — Task 2 spec 2 is a named DISCRIMINATOR that fails a label-only implementation. (b) `GameCard`'s `renderIcon()` returns a `disabled` `<button>` INSTEAD of the clickable `SvgButton`, so the `onClick={() => buttonClick()}` handler is not merely inert, it is absent from the tree. (c) `GameCard`'s context-menu Install entry — a second, independent dispatch path that would otherwise route straight past the visibly-busy card — is removed via `show`, since `Item` has no `disabled` field. (d) `GamePage.handleInstall` early-returns on the flag, mirroring its existing `if (settingUpBottle) return`; this is defense in depth because `handleInstall` is passed as a prop and a future caller might not be disabled. **Residual risk, stated not hidden:** `begin()` is synchronous and is the first statement of `startSteamInstall`, so the store flips before any `await` and React re-renders the disabled control on the next commit; a second click landing inside that same frame is theoretically unblocked. It is not mitigated further here because the fix would be a re-entrancy latch inside `startSteamInstall`, which 34.13-08 owns and has frozen. The consequence is bounded — a duplicate `window.api.install` for the same `appName`, which the Steam backend already treats idempotently (the install is delegated to the Steam client or the bottled client for the same AppID) — and no security boundary is crossed. |
| T-34.13-11-02 | Tampering | **Install-path and install-argument integrity across the Task 1 chokepoint repair** — the game page stops constructing `InstallArgs` itself | **mitigate** | The repair strictly REDUCES the number of `InstallArgs` construction sites in the renderer. Post-repair the game page dispatches `openInstallGameModal({ appName, runner: 'steam', gameInfo, action: 'install' })` and every field is marshalled by `installSteamGame` (34.13-08), whose own gates require `path` to be forwarded verbatim with no synthesis and `platformToInstall` to stay hardcoded `'Windows'`. Backend containment is untouched and remains authoritative: `resolveOverride()` (`installLocation.ts:201-219`, T-21-17) honours an `args.path` override only when `resolve()`'d it equals exactly one registered library's `resolve()`'d path, otherwise falling back to the primary — comparison by `resolve()`, never `join()`/prefix (the Phase 18 "`path.join` is not containment" lesson). `sanitizeInstalldir`/`SAFE_INSTALLDIR` gate the leaf segment. This plan adds no path input, no free-text field and no new IPC call. Proven by Task 1's `window.api.install` → `0` / `platformToInstall` → `0` gates (pre-edit `1` each) plus Task 3's Group C standing regression guard, and by the handoff instructing 34.13-13 to hold `platformToInstall: 'Windows'` at exactly two repo-wide frontend sites. |
| T-34.13-11-03 | Denial of Service | A permanently spinning, permanently disabled install control — the user can never install anything again without restarting the app | **mitigate** | There is exactly ONE latch and ONE release. The release is 34.13-08's `finally` (asserted by its spec C5, which was observed failing when `end()` was moved out of the `finally`). This plan is forbidden from adding a second: Task 3 gate A5 asserts `GameCard` holds no `useState` initialised from the busy flag, and `MainButton` reads context only (Task 2 gate: zero `useSteamInstallOptionsCheck` matches in `MainButton.tsx`). Every surface is therefore a pure projection of one store field, so no surface can outlive the store's own release. The upper bound on the wait is the backend's `METADATA_FETCH_TIMEOUT_MS` (15000ms), not an unbounded await, and 34.13-08's `catch` falls back to a zero-friction install rather than returning silently. |
| T-34.13-11-04 | Spoofing | A surface re-deriving the busy condition locally (e.g. `store.isChecking && appName === x`) and drifting from the other two — the button/status desync `isSteamBottleSetupActiveFor`'s own docstring says it exists to prevent | **mitigate** | All three surfaces call the single exported `isSteamInstallOptionsCheckActiveFor(state, appName, runner)`. Gated in both directions: Task 1 asserts exactly one call in `GamePage/index.tsx` with zero `isChecking &&` matches; Task 3 group A2 asserts the same for `GameCard` and proves the pattern non-vacuous against a known-bad specimen. The predicate's own `runner === 'steam'` term means a non-Steam game can never enter the busy state, and its `appName` term means one game's check cannot disable another game's card — both inherited from the frozen predicate rather than re-implemented per surface. |
| T-34.13-11-05 | Information Disclosure | The new busy label and `title` tooltip rendering into the UI | accept | The string is a static, localised, non-interpolated English default — `Checking install options…`. It carries no `appName`, no Steam AppID, no library path, no wine engine binary path, no account identifier. Nothing is logged, no telemetry sink is touched, and nothing is written to disk. This is consistent with the standing Steam-identity redact-forward decision by carrying nothing that would need redacting. |
| T-34.13-11-06 | Repudiation | — | accept | Not applicable. This plan adds no audited action, no authorization decision and no state mutation of its own — it projects one existing transient boolean onto three read-only surfaces and moves one dispatch from a duplicated call site to the shared one. |
| T-34.13-11-07 | Elevation of Privilege | — | accept | Not applicable. No privilege, capability, permission or trust level is introduced, widened or evaluated. The only behavioural delta at the renderer→backend boundary is Task 1's repair, which removes a hand-rolled construction of arguments the shared marshaller already builds identically. |
| T-34.13-SC | Tampering | npm/pip/cargo installs | **n/a — the Package Legitimacy Gate is NOT APPLICABLE to this plan or this phase.** `34.13-RESEARCH.md` §"Standard Stack" states "No new libraries. This phase composes existing in-repo primitives only," and explicitly records that a `## Package Legitimacy Audit` table is not applicable. This plan adds zero dependencies: `FontAwesomeIcon`, `faSyncAlt`, `classNames`, `zustand` and `react-i18next` are all already imported by the very files being edited. `package.json` / `pnpm-lock.yaml` are absent from `files_modified` by design and are covered by an explicit ownership gate in all three tasks. **Especially load-bearing here:** the obvious "fix" for `GameCard`/`GameSubMenu` being untestable is `pnpm add -D jest-environment-jsdom`, and `src/frontend/jest.config.js`'s own docstring records that adding it is excluded from auto-fix and would require a human package-legitimacy checkpoint. If an executor finds itself running a package-manager install, that is a scope escape — stop and return to planning. | The standing control remains armed: any install task in this phase would require the RESEARCH.md audit table plus a blocking human-verify checkpoint for every `[ASSUMED]`/`[SUS]` package; `[SLOP]` packages are forbidden and such checkpoints are never auto-approvable regardless of `workflow.auto_advance`. |

**ASVS L1 posture for this plan:** V5 (Input Validation) is the only category with a live surface,
and its relevant control is argument integrity at the renderer→backend install boundary — **mitigated**
by T-34.13-11-02 (fewer construction sites, backend `resolveOverride`/`sanitizeInstalldir` chain
untouched and still authoritative), not accepted. The re-entrancy work in T-34.13-11-01 is a UI
availability control rather than an ASVS category, and is treated as `mitigate` with its residual
frame stated explicitly rather than defaulted to `accept`. V2 (authentication), V3 (session
management), V4 (access control) and V6 (cryptography) are untouched, matching `34.13-RESEARCH.md`
§"Security Domain" ("No new authentication, session, network-facing, or cryptographic surface is
introduced by this phase").
</threat_model>

<verification>
## Per-task verification map (for `34.13-VALIDATION.md`, filled by 34.13-13)

| Task ID | Wave | Decision | Threat Ref | Test Type | Automated Command | File Exists |
|---------|------|----------|------------|-----------|-------------------|-------------|
| 34.13-11-01 | 5 | D-12 | T-34.13-11-02, T-34.13-11-04 | static (comment-stripped source gates) + full regression | `npx tsc --noEmit && pnpm test:ci` | ✅ (existing suites) |
| 34.13-11-02 | 5 | D-12 | T-34.13-11-01, T-34.13-11-03 | unit (direct component invocation, element-graph inspection, RED-proven) | `npx jest src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamInstallOptions.test.tsx --silent` | ❌ created by this task |
| 34.13-11-03 | 5 | D-12 | T-34.13-11-01, T-34.13-11-03, T-34.13-11-04 | static (comment-stripped source + filesystem gates) + full regression | `npx jest src/frontend/screens/Library/components/GameCard/__tests__/steamInstallOptionsBusy.test.ts --silent && pnpm test:ci` | ❌ created by this task |

## Phase-level checks owed by this plan

- `npx tsc --noEmit` exits 0.
- `pnpm test:ci` exits 0 after Task 1 and again after Task 3 — this plan adds a required
  `GameContextType` field and edits two components nearly every screen mounts.
- The two pre-existing MainButton suites are green and byte-identical.
- The new MainButton suite was observed RED before GREEN, and the RED named the disabled
  discriminator. A gate that only ever passed is not evidence — this repo carries multiple ledgered
  instances of a correctly-written, passing test guarding nothing.
- The Task 3 deliberate-breakage asymmetry (reverted `renderIcon` branch → A3/A6/A7 fail, Groups B
  and D stay green) was observed and is recorded verbatim.
- Pre/post grep counts recorded for every count-based gate: `window.api.install` in
  `GamePage/index.tsx` (1→0), `platformToInstall` there (1→0), `openInstallGameModal(` there (1→2),
  `fa-spin` in `MainButton.tsx` (2→3) and in `GameCard/index.tsx` (1→2).
- `git diff --name-only --diff-filter=A src/frontend` lists exactly two paths, both under
  `__tests__`.

## Explicitly NOT verified here (owned by 34.13-13's blocking manual gate)

- **That the spinner is visible on any screen.** Task 2 proves a React-element object graph; Task 3
  proves source text. Neither is a pixel. `34.13-VALIDATION.md` vacuous-assertion risk 1 applies in
  its general form: an element-graph assertion and a broken stylesheet are indistinguishable to Jest.
- **That the busy state is reachable at all in a running app.** That requires `startSteamInstall` to
  actually fire, which depends on 34.13-07's channel being registered on BOTH runtimes — a
  channel registered on Electron only would hang the D-12 busy state under Tauri specifically
  (RESEARCH.md Pitfall 2). Hence the manual gate's "both runtimes" row and `pnpm tauri:dev`, never
  `tauri dev`.
- **The 15-second cold-cache worst case.** Timing-dependent and requires a genuinely cold Steam
  metadata cache. Enumerated in `34.13-VALIDATION.md`'s Manual-Only table.
- **That the context-menu Install entry actually disappears**, and that the card's spinner tooltip
  reads correctly on hover. Source-gate-only here.
- **That the Task 1 chokepoint repair produces the same install on disk** as the retired bypass.
  Byte-identical `InstallArgs` is argued from `installSteamGame`'s frozen contract and guarded by
  Task 3 Group C; end-to-end confirmation belongs to 34.13-13's game-page install row.
- Anything about the dialog itself — its sections, its gating, its wine selector. 34.13-10 and
  34.13-12 own that; this plan never opens it.

## Standing rules honoured

- No 1px border on a `1fr` grid-track boundary — this plan introduces **no new CSS at all** and no
  new grid layout; it reuses `buttonWithIcon`, `svg-button iconDisabled`, `fa-spin` and `mainBtn`,
  every one of which already exists.
- `pnpm tauri:dev`, never `tauri dev` — carried into the handoff for 34.13-13's manual gate.
- Localisation: one new `gamelib:` key, shipped as an inline English default; the catalog is
  34.13-13's and is fenced by an ownership gate in all three tasks.
</verification>

<success_criteria>
- **D-12 is real on both origin surfaces.** While the eligibility verdict is pending, `MainButton`'s
  install button renders `Checking install options…` with `faSyncAlt`/`fa-spin` inside
  `buttonWithIcon` AND carries `disabled === true`; `GameCard`'s install affordance is a `disabled`
  `svg-button iconDisabled` spinner with a matching `title`, and its context-menu Install entry is
  gone. No half-rendered modal (34.13-08's `isOpen` ordering) and no silent multi-second dead click.
- **The worst case is cited verbatim**, not paraphrased: `15 seconds on a cold metadata cache
  (METADATA_FETCH_TIMEOUT_MS = 15000ms)` appears in `src/frontend/types.ts`'s doc comment and in the
  new MainButton test's header.
- **One source of truth.** All three surfaces call `isSteamInstallOptionsCheckActiveFor`; no surface
  hand-rolls `isChecking &&`; `GameCard` holds no local `useState` mirror; `MainButton` never touches
  the store directly. Each of those is a gate, and the hand-roll gate is proven non-vacuous against a
  known-bad specimen.
- **No new spinner component.** `git diff --name-only --diff-filter=A src/frontend` yields exactly
  two paths, both under `__tests__`; `fa-spin`/`faSyncAlt` counts move 2→3 in `MainButton.tsx` and
  1→2 in `GameCard/index.tsx`; `CircularProgress` count stays `0` in both; and a filesystem gate
  proven against a `'MySpinner.tsx'` specimen confirms no `Spinner`-named file was added.
- **Re-entrancy is closed on every dispatching affordance**, not just the visible one — the button,
  the card icon, the context-menu entry, and `handleInstall`'s early return — with the residual
  same-frame window stated explicitly in the threat model rather than papered over.
- **The falsified brief is repaired and provable:** `GamePage/index.tsx` contains zero
  `window.api.install` and zero `platformToInstall` (one each before), and dispatches
  `openInstallGameModal` twice (once before), so the game page shares the chokepoint and Phase
  34.13's trigger, form and D-17 override reach it at all. Phase 21 D-09 zero-friction is preserved
  because `installSteamGame` marshals byte-identical `InstallArgs`.
- **The second falsified brief is covered, not dropped:** `GameSubMenu/index.tsx` is byte-identical
  and is proven by absence — no install control, no busy wiring needed — with the proof shown
  non-vacuous against the just-wired `GameCard` source.
- **Stale cross-references are repaired in the commit that falsifies them:** `SteamBridgeSetup.tsx`'s
  "Same direct Steam-install invocation GamePage's handleInstall uses" comment is rewritten,
  comment-only, with no executable line changed.
- Evidence discipline held: the MainButton suite was observed RED (naming the disabled discriminator)
  before GREEN, and the Task 3 deliberate-breakage asymmetry was observed and recorded.
- `npx tsc --noEmit`, `npx eslint` on both edited components, and `pnpm test:ci` all exit 0; the diff
  touches exactly the eight paths in `files_modified` — no `common/`, no `backend/`, no
  `state/InstallGameModal.ts`, no `InstallModal/`, no locale catalog, no `package.json`.
</success_criteria>

<source_coverage>
## Source Coverage (this plan)

The phase-wide multi-source audit lives in `34.13-PLAN-OUTLINE.md` §Coverage. This plan's slice:

| Source | Item | Status |
|--------|------|--------|
| CONTEXT | **D-12** — busy/disabled state while `isSteamBottleEligible` resolves; a cold cache can wait up to `METADATA_FETCH_TIMEOUT_MS`; no half-rendered modal and no silent multi-second dead click | COVERED — Task 2 (MainButton label + `disabled`, RED-proven behaviorally), Task 3 (GameCard icon + context-menu gate), Task 1 (the GameContext plumbing that makes both reachable). The no-half-rendered-modal half is 34.13-08's `isOpen` ordering and is correctly not duplicated here. |
| CONTEXT | D-12 Claude's Discretion — "How the busy state in D-12 is rendered (existing per-game status vs. a local spinner)" | EXERCISED — existing per-game state, projected from one store field through the `settingUpBottle` precedent. No local spinner, no new component. Recorded as a discretion call in the SUMMARY. |
| UI-SPEC | D-12 Busy State Contract — the state belongs on the ORIGIN control, not in the dialog; reuse `buttonWithIcon` + `faSyncAlt.fa-spin` + `disabled` from `MainButton.tsx:182-186` / `GameCard/index.tsx:252-258`; **not** a new spinner component; gate on a single new transient value read like `is.installing`/`is.settingUpBottle`; 15s worst case verbatim | HONORED in full — Task 2 and Task 3 copy those exact two markup shapes; the single value is `is.checkingSteamInstallOptions`; the 15s figure is a gated string. |
| UI-SPEC | Copy — `t('gamelib:status.checkingSteamInstallOptions', 'Checking install options…')` | SHIPPED as an inline English default on both surfaces. **GAP SURFACED, NOT ABSORBED:** this key is missing from the UI-SPEC's own Copywriting Contract table (L93-125) and appears only in the D-12 code block at L365, so 34.13-13 will miss it unless told. Handed forward in `<handoffs>` and required in the SUMMARY. |
| GOAL | ROADMAP phase goal — "the user must never face a dead click or a half-rendered modal while GameLib works out whether the install form is needed" | COVERED — the dead-click half is this plan's entire deliverable, on both surfaces where a click can originate. |
| CONTEXT | D-10 — the trigger must actually reach every install entry point | PRESERVED AND REPAIRED — falsified brief 1: the game page bypassed the chokepoint, so D-10 reached only the library grid. Task 1's repair is what makes D-10 true on the game page. Recorded as a finding, not silently absorbed. |
| VALIDATION | Hard constraint: no jsdom, no react-test-renderer; every frontend deliverable is either pure-function-tested or listed in the manual gate — none silently assumed | HONORED, and improved on: `MainButton` is proven by the repo's sanctioned direct-invocation idiom (`jest.config.js` docstring + two live precedents), `GameCard`/`GameSubMenu` by comment-stripped source gates, and everything visual is explicitly assigned to 34.13-13's manual gate. |
| VALIDATION | Vacuous-assertion risk 1: "store-flip ≠ dialog renders" (general form: element graph ≠ pixels) | ADDRESSED BY EXPLICIT SCOPING — stated in `must_haves`, in the new test file's header, in `<verification>`, and required in the SUMMARY. Not assumed away. |
| VALIDATION | Vacuous-assertion risk 4: "one click-through ≠ coverage" | ADDRESSED — `<handoffs>` requires 34.13-13's D-12 row to exercise BOTH surfaces, because they are wired through two different mechanisms (GameContext vs. direct zustand subscription). |
| PATTERNS | The `SteamBottleSetup.ts` store + exported-pure-predicate shape and its three-consumer projection | HONORED — mirrored field-for-field through `types.ts` → `GameContext.tsx` → `GamePage/index.tsx` → `MainButton.tsx`, plus a direct subscription for `GameCard` which has no `GameContext`. |
| RESEARCH | "7 call sites funnel through one chokepoint" | **FALSIFIED AND REPAIRED** (falsified brief 1) — the game page did not; `GameSubMenu` is not an install site at all (falsified brief 2). Both recorded with evidence rather than worked around. |
| REQ | — | No `REQ-` IDs minted for this phase (ROADMAP marks them TBD); coverage is tracked against `D-NN` per the outline's decision-coverage gate. Not a gap. |
| DEFERRED | Editable/per-game bottle names; backend engine rejection; a per-game override of the always-show setting; `getInstallInfo()` / download sizes; a Linux Proton path | CORRECTLY ABSENT — this plan renders one boolean on two buttons; it reads no setting, no depot flag, no size, and calls no backend function. |

No unplanned items. No phase split needed: 3 tasks, 8 files (6 edits — four of them a handful of
lines, one comment-only — and 2 new test files), comfortably inside a single executor's context
budget.
</source_coverage>

<output>
Create `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-11-SUMMARY.md`
when done. It MUST include:
- the verbatim RED output from Task 2 Part A, showing the disabled discriminator failing;
- the Task 3 deliberate-breakage asymmetry (reverted `renderIcon` branch → A3/A6/A7 fail while
  Groups B and D stay green), with the spec names on both sides;
- the pre/post counts for every count-based gate: `window.api.install` and `platformToInstall` in
  `GamePage/index.tsx` (1→0 each), `openInstallGameModal(` there (1→2), `fa-spin`/`faSyncAlt` in
  `MainButton.tsx` (2→3) and in `GameCard/index.tsx` (1→2);
- **both falsified briefs, stated as findings for the phase record** — (1) the game page did NOT
  share the install chokepoint and was repaired here, with the consequence that this plan's own
  deliverable would otherwise have been unreachable dead code AND the whole phase would have been
  absent from the game page; (2) `GameSubMenu` is not an install origin and is covered by proof of
  absence rather than by an invented spinner;
- **the 34.13-13 handoff, called out explicitly**: `gamelib:status.checkingSteamInstallOptions` →
  `Checking install options…` is missing from the UI-SPEC Copywriting Contract table and still needs
  a catalog entry for `pnpm lint-translations:gamelib`; the manual D-12 row must exercise BOTH origin
  surfaces on BOTH runtimes (`pnpm tauri:dev`, never `tauri dev`); and the phase gate should hold
  `platformToInstall: 'Windows'` at exactly two frontend sites;
- the Claude's-Discretion call recorded: existing per-game state projected through GameContext rather
  than a local spinner, and `GameStatus.tsx` deliberately NOT wired (with the reasoning);
- an explicit statement of the vacuity boundary: an element-graph assertion and a source gate are not
  a pixel; nothing here proves the spinner is visible, and 34.13-13's blocking manual gate is the
  only evidence for that.
</output>
