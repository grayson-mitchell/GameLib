---
phase: 26-steam-key-redemption
plan: 04
type: execute
wave: 3
depends_on: [26-01, 26-02, 26-03]
files_modified:
  - src/frontend/types.ts
  - src/frontend/state/ContextProvider.tsx
  - src/frontend/state/GlobalState.tsx
  - src/frontend/components/UI/RedeemSteamKeyDialog/copy.ts
  - src/frontend/components/UI/RedeemSteamKeyDialog/copy.test.ts
  - src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx
  - src/frontend/App.tsx
autonomous: true
requirements: [REQ-26-04, REQ-26-05, REQ-26-06]

must_haves:
  truths:
    - "A boolean context toggle (showRedeemKeyDialog/handleRedeemKeyDialog) opens/closes the redeem modal"
    - "The modal validates format client-side and does NOT call redeemSteamKey on malformed input"
    - "On success the modal shows the redeemed game/package name and refreshes the Steam library (no restart)"
    - "The four outcomes (success/already-owned/invalid/rate-limited) each render a distinct message; the modal stays open"
    - "The redeem call carries store:'steam' (store-aware-ready)"
    - "The raw key is never logged from the modal"
  artifacts:
    - path: "src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx"
      provides: "Dialog-based redeem modal with key input + inline outcome + View-in-library jump"
      contains: "redeemSteamKey"
    - path: "src/frontend/components/UI/RedeemSteamKeyDialog/copy.ts"
      provides: "Pure outcome->copy mapping (4 distinct buckets)"
      contains: "RedeemKeyOutcome"
    - path: "src/frontend/state/ContextProvider.tsx"
      provides: "showRedeemKeyDialog/handleRedeemKeyDialog defaults"
      contains: "handleRedeemKeyDialog"
    - path: "src/frontend/App.tsx"
      provides: "always-mounted <RedeemSteamKeyDialog />"
      contains: "RedeemSteamKeyDialog"
  key_links:
    - from: "RedeemSteamKeyDialog"
      to: "window.api.redeemSteamKey"
      via: "onRedeem after isObviouslyMalformed passes"
      pattern: "redeemSteamKey"
    - from: "RedeemSteamKeyDialog success path"
      to: "window.api.refreshLibrary({ library: 'steam' })"
      via: "post-success ownership refresh (NOT recomputeOwnership directly)"
      pattern: "refreshLibrary"
    - from: "RedeemSteamKeyDialog View-in-library"
      to: "common/matching/titleMatch"
      via: "title-match packageList name against refreshed Steam GameInfo, degrade gracefully"
      pattern: "titleMatch|fuzzyMatch|normalizeTitle"
---

<objective>
Build the redeem modal and its context toggle. Add a simple boolean `showRedeemKeyDialog` /
`handleRedeemKeyDialog` toggle across the `ContextProvider`/`GlobalState`/`types.ts` triad (mirroring
`externalLinkDialogOptions`), then build `RedeemSteamKeyDialog` — a `Dialog`-based modal (NOT the
generic `DialogHandler` message-box) with a single key input, client-side format validation (26-02),
the `redeemSteamKey` IPC call (26-03), inline distinct outcome copy for all four SPEC buckets, a
success game-name display, a post-success `refreshLibrary({ library: 'steam' })`, and a
graceful "View in library" jump — and mount it once in `App.tsx`.

Purpose: The user-facing redemption surface (REQ4/REQ5/REQ6). Outcome copy is extracted into a pure,
unit-tested `copy.ts` so the four-distinct-messages requirement has automated coverage.
Output: Context toggle + redeem modal + pure copy map + App mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-steam-key-redemption/26-SPEC.md
@.planning/phases/26-steam-key-redemption/26-RESEARCH.md
@.planning/phases/26-steam-key-redemption/26-PATTERNS.md
@.planning/phases/26-steam-key-redemption/26-01-SUMMARY.md
@.planning/phases/26-steam-key-redemption/26-02-SUMMARY.md
@.planning/phases/26-steam-key-redemption/26-03-SUMMARY.md

<interfaces>
<!-- Consumes 26-01 types, 26-02 validator, 26-03 IPC. -->
Types (26-01): RedeemKeyResult { store:'steam'; outcome: RedeemKeyOutcome; packageList?: Record<string,string>; message? }
  RedeemKeyOutcome = 'success' | 'already-owned' | 'invalid' | 'rate-limited' | 'error'
Validator (26-02, src/frontend/helpers/steamKeyValidation.ts): normalizeKey(raw), isObviouslyMalformed(raw)
IPC (26-03): window.api.redeemSteamKey({ store: 'steam', key }): Promise<RedeemKeyResult>
Existing refresh IPC (unchanged): window.api.refreshLibrary({ library: 'steam' }) — fires recomputeOwnership side effect
Title matcher (existing): common/matching/titleMatch.ts -> normalizeTitle, fuzzyMatch (0.85 threshold)

Modal analog (RESEARCH Pattern 5 — corrects CONTEXT.md's DialogHandler ref):
src/frontend/components/UI/ExternalLinkDialog/index.tsx — Dialog/DialogContent/DialogFooter + local useState,
gated on a ContextProvider boolean, mounted once in App.tsx. DO NOT build on DialogHandler (a fixed
backend-driven message-box with no slot for a stateful form).

Context triad analog: externalLinkDialogOptions / handleExternalLinkDialog across
  types.ts :164-167 (+ context-shape :134-135), ContextProvider.tsx :104-105, GlobalState.tsx :562-564
App mount analog: App.tsx <ExternalLinkDialog /> at :103 (always-mounted, self-gating dialog list :96-108)

Pitfall 3: packageList gives packageID->name, NOT AppIDs. Resolve the View-in-library target by
title-matching the packageList name against the freshly-refreshed Steam GameInfo[]; if no confident
match, show the success name WITHOUT a jump link (graceful degrade — never a dead link).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add showRedeemKeyDialog / handleRedeemKeyDialog boolean toggle to the context triad</name>
  <files>src/frontend/types.ts, src/frontend/state/ContextProvider.tsx, src/frontend/state/GlobalState.tsx</files>
  <read_first>
    - src/frontend/types.ts (the file being modified; ExternalLinkDialogOptions + context-shape entries :134-135, :164-167)
    - src/frontend/state/ContextProvider.tsx (the file being modified; default values :104-105)
    - src/frontend/state/GlobalState.tsx (the file being modified; handleExternalLinkDialog setter :562-564)
    - .planning/phases/26-steam-key-redemption/26-PATTERNS.md (ContextProvider/GlobalState/types.ts section)
  </read_first>
  <action>
    Mirror the `externalLinkDialogOptions`/`handleExternalLinkDialog` triad but as a plain boolean (the modal owns its own input/outcome state, so context only needs open/closed). In `types.ts` add to the context-shape interface: `showRedeemKeyDialog: boolean` and `handleRedeemKeyDialog: (show: boolean) => void`. In `ContextProvider.tsx` add defaults `showRedeemKeyDialog: false` and `handleRedeemKeyDialog: () => null`. In `GlobalState.tsx` add the setter `handleRedeemKeyDialog = (show: boolean) => { this.setState({ showRedeemKeyDialog: show }) }`, add `showRedeemKeyDialog: false` to initial state, and include both in the value object passed to `ContextProvider.Provider` (same place `handleExternalLinkDialog` is provided).
  </action>
  <verify>
    <automated>grep -q "handleRedeemKeyDialog" src/frontend/types.ts && grep -q "showRedeemKeyDialog" src/frontend/state/ContextProvider.tsx && grep -q "handleRedeemKeyDialog = (show" src/frontend/state/GlobalState.tsx && npx tsc --noEmit -p . 2>&1 | grep -iE "frontend/types.ts|ContextProvider.tsx|GlobalState.tsx"; test $? -ne 0 && echo TRIAD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` declares `showRedeemKeyDialog: boolean` and `handleRedeemKeyDialog: (show: boolean) => void`.
    - `ContextProvider.tsx` provides both defaults; `GlobalState.tsx` defines the setter, seeds initial state, and passes both into the provider value.
    - `npx tsc --noEmit` reports no new type errors in the three files.
  </acceptance_criteria>
  <done>The boolean toggle exists across all three files and type-checks; consumers (dialog + sidebar) can read/set it.</done>
</task>

<task type="auto">
  <name>Task 2: RedeemSteamKeyDialog modal + pure outcome-copy map + App mount</name>
  <files>src/frontend/components/UI/RedeemSteamKeyDialog/copy.ts, src/frontend/components/UI/RedeemSteamKeyDialog/copy.test.ts, src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx, src/frontend/App.tsx</files>
  <read_first>
    - src/frontend/components/UI/ExternalLinkDialog/index.tsx (full file — the Dialog reuse pattern to copy)
    - src/frontend/components/UI/Dialog (Dialog/DialogContent/DialogFooter exports)
    - src/frontend/App.tsx (the file being modified; always-mounted dialog list :96-108, imports :14-15)
    - src/frontend/helpers/steamKeyValidation.ts (26-02: normalizeKey, isObviouslyMalformed)
    - src/common/matching/titleMatch.ts (normalizeTitle, fuzzyMatch — for the View-in-library jump)
    - .planning/phases/26-steam-key-redemption/26-RESEARCH.md (Pattern 5, Pitfall 3, Open Question 1 copy guidance)
    - .planning/phases/26-steam-key-redemption/26-PATTERNS.md (RedeemSteamKeyDialog + App.tsx sections)
  </read_first>
  <action>
    Create `copy.ts` exporting a pure `redeemOutcomeCopy(outcome: RedeemKeyOutcome, packageName?: string): { message: string; tone: 'success' | 'error' }` returning a DISTINCT message per bucket (success names the game via packageName; already-owned = "already owned" copy; invalid = "this key doesn't look right"; rate-limited = "wait / cooldown" copy; error/not-connected = a distinct connectivity message). Never a generic "failed" shared across buckets (SPEC REQ5 / D-08). Create `copy.test.ts` asserting all four buckets (+ error) yield mutually-distinct messages and that success interpolates packageName. Create `RedeemSteamKeyDialog/index.tsx` following the `ExternalLinkDialog` shape: gate render on `showRedeemKeyDialog` from `ContextProvider`; hold local `useState` for the key text, a busy flag, the current `RedeemKeyOutcome | null`, and the resolved success package name + optional jump target. In `onRedeem`: if `isObviouslyMalformed(key)` set a local malformed message and RETURN without any IPC call (SPEC REQ3); else call `window.api.redeemSteamKey({ store: 'steam', key: normalizeKey(key) })`, set the outcome, and render `redeemOutcomeCopy` inline (modal STAYS OPEN on any result, D-06/D-08). On `outcome==='success'`: read the first `packageList` value as the game name, call `window.api.refreshLibrary({ library: 'steam' })` (NOT recomputeOwnership directly — correction #3), then resolve a "View in library" jump by title-matching the package name against the refreshed Steam library via `fuzzyMatch`/`normalizeTitle`; if a confident match is found render a jump link/button, otherwise render the name with NO link (graceful degrade, Pitfall 3). Never log the raw `key`/`normalizeKey(key)` anywhere. In `App.tsx`, import `RedeemSteamKeyDialog` and mount `<RedeemSteamKeyDialog />` in the always-mounted dialog list beside `<ExternalLinkDialog />`.
  </action>
  <verify>
    <automated>npx jest src/frontend/components/UI/RedeemSteamKeyDialog/copy.test.ts && grep -q "redeemSteamKey" src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx && grep -q "refreshLibrary" src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx && grep -q "isObviouslyMalformed" src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx && grep -q "RedeemSteamKeyDialog" src/frontend/App.tsx && ! grep -nE "console\.(log|info|warn|error)\(.*\bkey\b" src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx && npx tsc --noEmit -p . 2>&1 | grep -iE "RedeemSteamKeyDialog|App.tsx"; test $? -ne 0 && echo DIALOG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `copy.ts` exports `redeemOutcomeCopy`; `copy.test.ts` proves the four buckets (+ error) return mutually-distinct strings and success interpolates the game name; `npx jest .../copy.test.ts` exits 0.
    - `index.tsx` calls `isObviouslyMalformed` and RETURNS before `redeemSteamKey` on malformed input (no IPC call).
    - `index.tsx` calls `window.api.redeemSteamKey({ store: 'steam', ... })` and, on success, `window.api.refreshLibrary({ library: 'steam' })`.
    - The View-in-library jump uses `fuzzyMatch`/`normalizeTitle` and degrades to name-without-link when no confident match.
    - No `console.*` (or logger) call in the modal interpolates the raw key (grep returns no match).
    - `App.tsx` mounts `<RedeemSteamKeyDialog />`.
    - `npx tsc --noEmit` reports no new type errors in the edited/created files.
  </acceptance_criteria>
  <done>The modal validates, redeems, shows distinct inline outcomes, names the game, refreshes the library, offers a graceful jump, never logs the key, and is mounted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → renderer → IPC | User-typed key enters the modal and is sent over IPC |
| modal → console/log | The raw key is in modal memory and must not be logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | RedeemSteamKeyDialog logging | mitigate | No `console.*`/logger call interpolates the raw key; asserted by grep in verify. |
| T-26-02 | Tampering / Input Validation | client-side format gate | mitigate | isObviouslyMalformed runs before any IPC call (SPEC REQ3); the backend still re-validates (defense-in-depth), so a bypass only costs an extra rejected round-trip. |
| T-26-03 | Denial of Service | repeated invalid redeems | accept | No batch/auto-retry loop; one redeem per explicit user click, and rate-limited outcome is surfaced so the user is not led to keep retrying (SPEC). |
| T-26-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A — zero new packages this phase. |
</threat_model>

<verification>
- `npx jest src/frontend/components/UI/RedeemSteamKeyDialog/copy.test.ts` green (distinct outcome copy).
- Grep confirms the malformed-input early return, the redeemSteamKey + refreshLibrary calls, the App mount, and no key logging.
- `npx tsc --noEmit` clean for edited/created files.
- Manual UAT (tracked in 26-VALIDATION.md, run at verify-work): live redeem of the user's spare test keys (success/already-owned/invalid), game appears after refresh with no restart, and log inspection confirms no raw key.
</verification>

<success_criteria>
- Redeem modal opens from the context toggle, validates client-side, redeems via IPC, renders four distinct inline outcomes, names the redeemed game, refreshes the Steam library, offers a graceful View-in-library jump, and never logs the key.
</success_criteria>

<output>
Create `.planning/phases/26-steam-key-redemption/26-04-SUMMARY.md` when done.
</output>
