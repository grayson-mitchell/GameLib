---
phase: 26-steam-key-redemption
plan: 03
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - src/common/types/ipc.ts
  - src/preload/api/steam.ts
  - src/backend/main.ts
autonomous: true
requirements: [REQ-26-02, REQ-26-06]

must_haves:
  truths:
    - "The renderer can invoke redeemSteamKey over IPC and receive a classified RedeemKeyResult"
    - "The IPC payload carries a store field ('steam') alongside the key (store-aware-ready)"
    - "The handler delegates to SteamUser.redeemKey and never throws across the boundary"
  artifacts:
    - path: "src/common/types/ipc.ts"
      provides: "redeemSteamKey entry in AsyncIPCFunctions typed with store+key payload -> RedeemKeyResult"
      contains: "redeemSteamKey"
    - path: "src/preload/api/steam.ts"
      provides: "redeemSteamKey makeHandlerInvoker export"
      contains: "redeemSteamKey"
    - path: "src/backend/main.ts"
      provides: "addHandler('redeemSteamKey') delegating to SteamUser.redeemKey"
      contains: "redeemSteamKey"
  key_links:
    - from: "src/backend/main.ts addHandler('redeemSteamKey')"
      to: "SteamUser.redeemKey"
      via: "destructures { store, key } and calls SteamUser.redeemKey(store, key)"
      pattern: "SteamUser.redeemKey"
    - from: "src/common/types/ipc.ts redeemSteamKey"
      to: "RedeemKeyResult"
      via: "return type imported from common/types/steam"
      pattern: "RedeemKeyResult"
---

<objective>
Wire the `redeemSteamKey` IPC method across the three-file lockstep — the shared type contract
(`ipc.ts`), the preload bridge (`steam.ts`), and the backend handler (`main.ts`) — delegating to the
`SteamUser.redeemKey` wrapper from 26-01. The payload carries `{ store, key }` so the store dimension
exists in the IPC surface (REQ-26-06), Steam-only wired.

Purpose: Exposes the backend primitive to the renderer so the dialog (26-04) can call it. Planning the
three files together avoids a broken partial wiring (a preload export with no handler, or a handler with
no type entry).
Output: A fully-typed, registered `redeemSteamKey` IPC method.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-steam-key-redemption/26-SPEC.md
@.planning/phases/26-steam-key-redemption/26-PATTERNS.md
@.planning/phases/26-steam-key-redemption/26-01-SUMMARY.md

<interfaces>
<!-- Consumes 26-01's SteamUser.redeemKey(store,key) and RedeemKeyResult type. -->
Backend method (from 26-01): SteamUser.redeemKey(store: 'steam', key: string): Promise<RedeemKeyResult>
Type to import in ipc.ts: RedeemKeyResult from common/types/steam

Three-file pattern (exact analog = steamSubmitGuard):
- src/preload/api/steam.ts:7  export const steamSubmitGuard = makeHandlerInvoker('steamSubmitGuard')
- src/backend/main.ts:926-928 addHandler('steamSubmitGuard', async (event, code) => SteamUser.submitSteamGuardCode(code))
- src/common/types/ipc.ts:253 steamSubmitGuard: (code: string) => Promise<{ status: 'done' | 'error' }>

The existing refreshLibrary handler (main.ts:1065-1101) already fires HumbleLibrary.recomputeOwnership()
as a side effect when called with library:'steam' — DO NOT add any new refresh/recompute plumbing here;
the frontend (26-04) calls the existing refreshLibrary({ library: 'steam' }) on success.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add redeemSteamKey to the three-file IPC lockstep</name>
  <files>src/common/types/ipc.ts, src/preload/api/steam.ts, src/backend/main.ts</files>
  <read_first>
    - src/common/types/ipc.ts (the file being modified; AsyncIPCFunctions steam entries :237-256, steamSubmitGuard :253)
    - src/preload/api/steam.ts (the file being modified; makeHandlerInvoker exports :1-10)
    - src/backend/main.ts (the file being modified; steam addHandler block :920-933; refreshLibrary handler :1065-1101 for context only — do not modify)
    - src/backend/storeManagers/steam/user.ts (SteamUser.redeemKey from 26-01)
    - .planning/phases/26-steam-key-redemption/26-PATTERNS.md (ipc.ts / steam.ts / main.ts sections)
  </read_first>
  <action>
    In `src/common/types/ipc.ts`, import `RedeemKeyResult` from `common/types/steam` and add to `AsyncIPCFunctions` (in the steam block near :253): `redeemSteamKey: (payload: { store: 'steam'; key: string }) => Promise<RedeemKeyResult>`. In `src/preload/api/steam.ts`, add `export const redeemSteamKey = makeHandlerInvoker('redeemSteamKey')` alongside the other steam invokers. In `src/backend/main.ts`, add `addHandler('redeemSteamKey', async (event, { store, key }) => SteamUser.redeemKey(store, key))` inside the existing steam handler block (near :928), matching the `steamSubmitGuard` registration. Do NOT modify the `refreshLibrary` handler and do NOT add any recomputeOwnership call here (correction #3 — the frontend reuses the existing refreshLibrary IPC path).
  </action>
  <verify>
    <automated>grep -q "redeemSteamKey" src/common/types/ipc.ts && grep -q "makeHandlerInvoker('redeemSteamKey')" src/preload/api/steam.ts && grep -q "addHandler('redeemSteamKey'" src/backend/main.ts && grep -q "SteamUser.redeemKey" src/backend/main.ts && npx tsc --noEmit -p . 2>&1 | grep -iE "ipc.ts|preload/api/steam.ts|main.ts" ; test $? -ne 0 && echo WIRING_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ipc.ts` has a `redeemSteamKey` entry in `AsyncIPCFunctions` returning `Promise<RedeemKeyResult>` with a `{ store: 'steam'; key: string }` payload.
    - `steam.ts` exports `redeemSteamKey = makeHandlerInvoker('redeemSteamKey')`.
    - `main.ts` registers `addHandler('redeemSteamKey', ...)` delegating to `SteamUser.redeemKey(store, key)`.
    - No new `recomputeOwnership`/`refreshLibrary` call was added in `main.ts` for redeem.
    - `npx tsc --noEmit` reports no new type errors in the three edited files.
  </acceptance_criteria>
  <done>The redeemSteamKey IPC method type-checks end-to-end and is registered in all three files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | A compromised/malicious renderer could send an arbitrary string as the key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Tampering | redeemSteamKey IPC payload trust | accept | Not a new risk — `redeemKey` is Steam's authoritative validator; worst case of a malformed payload is a rejected InvalidKey/OnCooldown result, no privilege escalation. Standing mitigation = the project-wide Electron context-isolation + contextBridge-only IPC surface, unchanged here. |
| T-26-01 | Information Disclosure | handler logging | mitigate | The handler passes the key straight to SteamUser.redeemKey and does not log it; redaction is enforced in 26-01's wrapper. Do not add any key-logging in the handler. |
| T-26-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A — zero new packages this phase. |
</threat_model>

<verification>
- Grep confirms all three files reference `redeemSteamKey` and the handler delegates to `SteamUser.redeemKey`.
- `npx tsc --noEmit` clean for the three files.
</verification>

<success_criteria>
- `redeemSteamKey` is invokable from the renderer, typed `{ store, key } -> RedeemKeyResult`, and registered as a backend handler delegating to the 26-01 wrapper.
- No redeem-specific refresh plumbing added (reuses existing refreshLibrary).
</success_criteria>

<output>
Create `.planning/phases/26-steam-key-redemption/26-03-SUMMARY.md` when done.
</output>
