---
phase: 26-steam-key-redemption
plan: 05
type: execute
wave: 4
depends_on: [26-04]
files_modified:
  - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx
  - src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx
autonomous: true
requirements: [REQ-26-01]

must_haves:
  truths:
    - "A 'Redeem a Steam key' sidebar item appears only when logged into Steam"
    - "With no Steam session the item is absent"
    - "Clicking the item opens the redeem modal (does not navigate to a route)"
  artifacts:
    - path: "src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx"
      provides: "Login-gated Redeem-a-Steam-key SidebarItem placed under Settings"
      contains: "handleRedeemKeyDialog"
    - path: "src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx"
      provides: "Login-gating test (hidden logged-out, shown logged-in)"
      contains: "Redeem"
  key_links:
    - from: "SidebarLinks Redeem item onClick"
      to: "handleRedeemKeyDialog(true)"
      via: "opens modal (D-03), not a route navigation"
      pattern: "handleRedeemKeyDialog"
    - from: "SidebarLinks Redeem item render gate"
      to: "steam.username"
      via: "conditional render on Steam login (D-02 / SPEC REQ1)"
      pattern: "steam.username|steam\\?\\."
---

<objective>
Add the entry point: a login-gated "Redeem a Steam key" sidebar item under the Settings link that opens
the redeem modal (from 26-04) on click. The item is present only when a Steam session exists
(`steam.username` truthy) and absent otherwise (SPEC REQ1 / D-01/D-02/D-03). Add the SidebarLinks
login-gating test (Wave 0 gap from 26-VALIDATION — no such test exists today).

Purpose: The discoverable, correctly-gated surface that lets the user reach the redeem modal — the last
wiring that makes the phase end-to-end.
Output: Gated sidebar item + login-gating test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-steam-key-redemption/26-SPEC.md
@.planning/phases/26-steam-key-redemption/26-PATTERNS.md
@.planning/phases/26-steam-key-redemption/26-04-SUMMARY.md

<interfaces>
<!-- Consumes 26-04's handleRedeemKeyDialog context toggle. -->
Context toggle (26-04): handleRedeemKeyDialog(show: boolean) from ContextProvider
Steam login gate: steam.username (truthy after Steam login; same gate the file's `loggedIn` uses at :58-63)

Analogs in the file being modified (SidebarLinks/index.tsx):
- Login-gated item: `{humble?.isLoggedIn && (<SidebarItem url="/humble-keys" icon={faKey} .../>)}` (:192-199)
- Settings block (D-01: place the new item AFTER this): SidebarItemWithSubmenu (~:267)
- Button-style (not url) SidebarItem: Discord/Patreon/Ko-fi usage (:316-344) — elementType="button" + onClick
- faKey icon already imported (:14)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add login-gated Redeem-a-Steam-key sidebar item under Settings</name>
  <files>src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx</files>
  <read_first>
    - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx (the file being modified; humble-gated item :192-199, Settings block ~:267, button-style SidebarItem :316-344, useContext destructure ~:16-30, steam gate :58-63)
    - .planning/phases/26-steam-key-redemption/26-PATTERNS.md (SidebarLinks section)
  </read_first>
  <action>
    Destructure `steam` and `handleRedeemKeyDialog` from `useContext(ContextProvider)` (add to the existing destructure). After the Settings `SidebarItemWithSubmenu` block (D-01, ~:267), add a conditional `{steam.username && (<SidebarItem elementType="button" onClick={() => handleRedeemKeyDialog(true)} icon={faKey} label={t('sidebar.redeemSteamKey', 'Redeem a Steam key')} dataTour="sidebar-redeem-steam-key" />)}`. Use `elementType="button"` + `onClick` (not `url`) so clicking opens the modal rather than navigating (D-03), mirroring the Discord/Patreon button-style items. Gate on `steam.username` (D-02 / SPEC REQ1 — the renderer's truthy proxy for `SteamUser.isLoggedIn()`).
  </action>
  <verify>
    <automated>grep -q "handleRedeemKeyDialog(true)" src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx && grep -q "sidebar.redeemSteamKey" src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx && grep -q "steam.username" src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx && npx tsc --noEmit -p . 2>&1 | grep -i "SidebarLinks/index.tsx"; test $? -ne 0 && echo SIDEBAR_OK</automated>
  </verify>
  <acceptance_criteria>
    - The new `SidebarItem` is gated on `steam.username` and placed after the Settings block.
    - It uses `elementType="button"` + `onClick={() => handleRedeemKeyDialog(true)}` (opens modal, no route navigation).
    - `npx tsc --noEmit` reports no new type errors in the file.
  </acceptance_criteria>
  <done>The gated sidebar item exists, opens the modal on click, and type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: SidebarLinks login-gating test (hidden logged-out, shown logged-in)</name>
  <files>src/frontend/components/UI/Sidebar/components/SidebarLinks/__tests__/index.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx (the component under test)
    - .planning/phases/26-steam-key-redemption/26-VALIDATION.md (Wave 0 gap — no SidebarLinks test exists today)
    - .planning/phases/26-steam-key-redemption/26-PATTERNS.md (test conventions; ContextProvider-driven render)
  </read_first>
  <action>
    Create the new test file (Wave 0 infrastructure — none exists). Render `SidebarLinks` inside a mocked `ContextProvider` value in a router context (React Testing Library, matching the frontend jest project's conventions — look at a sibling component test for the render/wrap helper). Case 1: provide `steam: { username: undefined }` and assert the "Redeem a Steam key" item is NOT in the document. Case 2: provide `steam: { username: 'TestUser' }` and a `handleRedeemKeyDialog` jest.fn, assert the item IS present, and (optionally) that clicking it calls `handleRedeemKeyDialog(true)`. Mock only what the component needs to mount; keep it a focused gating test.
  </action>
  <verify>
    <automated>npx jest src/frontend/components/UI/Sidebar/components/SidebarLinks -x</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and asserts the redeem item is absent when `steam.username` is falsy and present when truthy.
    - `npx jest src/frontend/components/UI/Sidebar/components/SidebarLinks -x` exits 0.
  </acceptance_criteria>
  <done>REQ-26-01's login-gating behavior has automated coverage and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Steam session state → sidebar visibility | The entry point must not be reachable without a live Steam session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Elevation / Access Control | redeem entry-point visibility | mitigate | Item gated on `steam.username`; the backend wrapper additionally requires `ensureConnected()` (26-01), so even a bypassed UI gate cannot redeem without a live session (defense-in-depth). Gating asserted by the SidebarLinks test. |
| T-26-01 | Information Disclosure | sidebar item | accept | The sidebar item handles no key value — the key is only entered inside the modal (26-04). No new disclosure surface. |
| T-26-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A — zero new packages this phase. |
</threat_model>

<verification>
- `npx jest src/frontend/components/UI/Sidebar/components/SidebarLinks -x` green.
- Grep confirms the gated item wires onClick to `handleRedeemKeyDialog(true)`.
- Manual UAT (26-VALIDATION): with no Steam session the item is absent; after Steam login it appears and opens the modal.
</verification>

<success_criteria>
- A login-gated "Redeem a Steam key" sidebar item opens the redeem modal, absent when logged out, with automated gating coverage.
</success_criteria>

<output>
Create `.planning/phases/26-steam-key-redemption/26-05-SUMMARY.md` when done.
</output>
