---
phase: 34.18-retire-the-macos-x64-intel-ci-leg
plan: 05
subsystem: infra
tags: [macos, arm64, ipc, tauri, electron, environment-probe, dead-code-removal]

# Dependency graph
requires:
  - phase: 34.18 (plans 01-04)
    provides: README disclosure, x64 non-goal survivor gate, and prior 34.18 groundwork retiring the macOS x64/Intel CI leg
provides:
  - Zero `isIntelMac` references anywhere under src/ — constant, both IPC registrations (Electron ipc_handler.ts + Tauri sidecar appShellFlowRegistration.ts), common type, preload invoker, frontend mirror, all comment mentions
  - Eight (in practice nine) behaviour-preserving conditional-branch collapses across backend Wine/GPTK/DXMT/PATH logic
  - A static zero-match completeness gate (meta/__tests__/isIntelMacRemoved.test.ts) that fails CI if the token ever regresses
  - A D-14 RED-proof (removeCopies.test.ts) rewritten to survive the constant's removal, still catching an unsafe `!isIntelMac`-equivalent negation
affects: [34.18 remaining plans, any future macOS host-probe work touching backend/constants/environment.ts]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Static completeness gate via spawnSync('grep', ...) in jest, paired with a vacuity-control assertion proving the grep reached a populated tree"
    - "RED-proof-of-the-RED-proof: before trusting a rewritten regression test, temporarily reintroduce the exact defect shape via a scratch mutation (restored by cp + shasum, never git checkout --) and confirm the test fails"

key-files:
  created:
    - meta/__tests__/isIntelMacRemoved.test.ts
  modified:
    - src/backend/constants/environment.ts
    - src/backend/utils/ipc_handler.ts
    - src/backend/sidecar/appShellFlowRegistration.ts
    - src/backend/sidecar/clipboardFlowRegistration.ts
    - src/backend/sidecar/shellFilesFlowRegistration.ts
    - src/backend/storeManagers/steam/games.ts
    - src/backend/utils.ts
    - src/backend/launcher.ts
    - src/backend/main.ts
    - src/backend/tools/dxmt.ts
    - src/backend/tools/index.ts
    - src/common/types/ipc.ts
    - src/preload/api/misc.ts
    - src/frontend/types.ts
    - src/frontend/state/ContextProvider.tsx
    - src/frontend/state/GlobalState.tsx
    - src/frontend/screens/WineManager/index.tsx
    - 9 backend/sidecar test fixture files (jest.mock reconciliation)
    - src/backend/storeManagers/steam/__tests__/removeCopies.test.ts (D-14 rewrite)
    - src/backend/sidecar/__tests__/flowRegistrationCensus.test.ts (deviation)

key-decisions:
  - "D-10: removal lands as one atomic unit across all four tasks — no intermediate state ships a dangling isIntelMac reference on one side of the IPC boundary"
  - "D-11: isMacOSUpToDate remains live in the collapsed utils.ts branch; pre-Sonoma Apple Silicon still selects Wine-Crossover"
  - "D-12: isAppleSiliconMac stays a positive probe — never rewritten as a negated-Intel-probe equivalent, since an empty/unknown CPU model would wrongly evaluate true under negation"
  - "D-14: the RED-proof is now self-contained via a local negatedIntelProbe() helper, since there is no real isIntelMac field left to read off the module"

requirements-completed:
  - REQ-34.18-07
  - REQ-34.18-08

# Metrics
duration: ~55min
completed: 2026-08-27
---

# Phase 34.18 Plan 05: Remove isIntelMac Summary

**Removed `isIntelMac` entirely from the codebase (constant, both IPC registrations, type, preload, frontend mirror, all comments), collapsing nine behaviour-preserving conditional branches, while `isAppleSiliconMac` and its destructive-action gate survive untouched and the D-14 RED-proof outlives the constant it was written against.**

## Performance

- **Duration:** ~55 min
- **Tasks:** 4
- **Files modified:** 30 (17 non-test source/config files, 13 test files)

## Accomplishments

- `grep -rn isIntelMac src/ | wc -l` == **0** (target achieved and verified twice: once at HEAD after all edits, once again after the RED-proof-of-the-RED-proof restore)
- Collapsed all conditional branches gated by `isIntelMac` — the plan's enumerated eight plus one additional site discovered during execution (`checkRosettaInstall()`'s dead early-return guard in `utils.ts`)
- `isAppleSiliconMac` preserved as a positive probe, byte-identical expression, doc comment reworded only to drop cross-references to the removed constant
- Static zero-match completeness gate (`meta/__tests__/isIntelMacRemoved.test.ts`) created, RED-proven against the pre-removal baseline (61 matches / 28 files), now green and permanently guarding against regression
- D-14 RED-proof in `removeCopies.test.ts` rewritten to be self-contained (a local `negatedIntelProbe()` helper reproducing the exact unsafe-negation shape) and demonstrated, via a scratch mutation of `isAppleSiliconMac`, to still fail against the defect it guards against

## Task Commits

1. **Task 1: static zero-match completeness gate (RED-proven against baseline)** - `649905885` (test)
2. **Task 2: remove constant, both IPC registrations, collapse backend branches** - `8e7af620c` (feat)
3. **Task 3: remove type, preload invoker, frontend mirror** - `9ffe52920` (feat)
4. **Task 4: reconcile test fixtures/census + rewrite D-14 RED-proof** - `375e3b113` (test)

_Note: no separate plan-metadata commit is issued for this plan — per this execution's explicit constraint, STATE.md/ROADMAP.md/REQUIREMENTS.md are the orchestrator's sole responsibility and are not touched by this executor. This SUMMARY.md is committed separately below._

## Files Created/Modified

- `meta/__tests__/isIntelMacRemoved.test.ts` - static zero-match completeness gate over `src/`, with a vacuity control proving the grep reaches a populated tree
- `src/backend/constants/environment.ts` - deleted `isIntelMac`; reworded `isAppleSiliconMac`'s doc comment (expression untouched)
- `src/backend/utils/ipc_handler.ts` - removed the Electron `addHandler('isIntelMac', ...)` registration and its import
- `src/backend/sidecar/appShellFlowRegistration.ts` - removed the Tauri sidecar `ipcMain.handle('isIntelMac', ...)` registration; updated channel-count doc comments (20→19 total, 8→7 invoke)
- `src/backend/sidecar/clipboardFlowRegistration.ts`, `shellFilesFlowRegistration.ts` - reworded already-ported-channel comment lists (four→three)
- `src/backend/storeManagers/steam/games.ts` - reworded a comment warning against widening `isAppleSiliconMac` to a negated-Intel-probe equivalent
- `src/backend/utils.ts` - collapsed `isIntelMac || !isMacOSUpToDate` → `!isMacOSUpToDate`; removed `checkRosettaInstall()`'s dead `if (isIntelMac) return` guard (extra collapse, not in plan's enumerated 8)
- `src/backend/launcher.ts` - `isMacOSUpToDate && !isIntelMac` → `isMacOSUpToDate`
- `src/backend/tools/dxmt.ts` - collapsed two `isMac && !isIntelMac` conditions and one `!isMac || isIntelMac` guard
- `src/backend/tools/index.ts` - collapsed PATH ternary to its else-arm literal
- `src/backend/main.ts` - `isMac && !isIntelMac` → `isMac`
- `src/common/types/ipc.ts` - deleted `isIntelMac: () => boolean` interface member
- `src/preload/api/misc.ts` - deleted the `isIntelMac` handler invoker export
- `src/frontend/types.ts`, `ContextProvider.tsx` - deleted the `isIntelMac` field/default
- `src/frontend/state/GlobalState.tsx` - deleted the `if (platform === 'darwin') { this.setState({ isIntelMac: ... }) }` block; later removed the now-unused `platform` destructure (deviation)
- `src/frontend/screens/WineManager/index.tsx` - removed `isIntelMac` from context destructure; collapsed `getDefaultRepository()`'s Intel-specific branch
- 9 backend/sidecar test files - removed `isIntelMac: false` from `jest.mock('backend/constants/environment', ...)` fixtures
- `src/backend/sidecar/__tests__/appShellFlows.test.ts` - removed the dedicated isIntelMac invoke test; reworded doc comment
- `src/backend/sidecar/__tests__/clipboardFlows.test.ts` - removed `isIntelMac` from `FOREIGN_CHANNELS`; updated count comment
- `src/backend/storeManagers/steam/__tests__/removeCopies.test.ts` - D-14 RED-proof rewrite: self-contained `negatedIntelProbe()` helper, `loadWithHost` return type dropped `isIntelMac`, all comment mentions reworded
- `src/backend/sidecar/__tests__/flowRegistrationCensus.test.ts` - updated hardcoded EXPECTED census entry (8→7 invoke) (deviation)

## Decisions Made

- D-10/D-11/D-12/D-14 applied exactly as specified in the plan frontmatter; no deviation from the decision set itself
- Interpreted D-12's "checkRosettaInstall() body untouched" as referring to the substantive Rosetta-check logic, not the dead `if (isIntelMac) return` early-return guard preceding it — removed the guard since it never fires in a shipped arm64 build and its literal token blocks the zero-match gate

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug/blocking] Ninth branch-collapse site not in the plan's enumerated eight**
- **Found during:** Task 2
- **Issue:** `checkRosettaInstall()` in `utils.ts` contains `if (isIntelMac) { return }`, a dead early-return guard not among the plan's eight named collapse sites, yet its literal token blocks the D-10 zero-match gate
- **Fix:** Removed the guard; behaviour-preserving because it never fires in a shipped arm64-only build (the substantive Rosetta-check logic below it is untouched, per D-12)
- **Files modified:** `src/backend/utils.ts`
- **Committed in:** `8e7af620c` (documented in the commit message)

**2. [Rule 1 - Bug] Stale test census after invoke-channel count changed**
- **Found during:** Task 4 verification (Backend jest run)
- **Issue:** `flowRegistrationCensus.test.ts`'s hardcoded EXPECTED table still asserted `{invoke: 8, send: 12}` for `appShellFlowRegistration.ts` after Task 2 dropped the real count to 7
- **Fix:** Updated the expected entry to `{invoke: 7, send: 12}`
- **Files modified:** `src/backend/sidecar/__tests__/flowRegistrationCensus.test.ts`
- **Verification:** Backend jest project re-run, test passes
- **Committed in:** `375e3b113`

**3. [Rule 1 - Bug] Unused-variable lint error after frontend mirror removal**
- **Found during:** Task 4 verification (`pnpm lint`)
- **Issue:** Removing the `if (platform === 'darwin') {...}` block in `GlobalState.tsx` (Task 3) left `platform` destructured from `this.state` in `componentDidMount()` with no remaining use, tripping `no-unused-vars`
- **Fix:** Removed `platform` from the destructure; confirmed via grep no other use in scope; re-ran `pnpm codecheck` (clean) and `pnpm lint` (0 errors)
- **Files modified:** `src/frontend/state/GlobalState.tsx`
- **Committed in:** `375e3b113`

---

**Total deviations:** 3 auto-fixed (all Rule 1 — bugs/blocking issues directly caused by this task's own edits)
**Impact on plan:** All auto-fixes were necessary for correctness and to reach the zero-match/clean-lint targets the plan requires. No scope creep beyond what was needed to make this plan's own edits internally consistent.

## Issues Encountered

- **False-positive skip-test grep:** the acceptance criteria's literal `xit(\|it\.skip(\|describe\.skip(` pattern matched 3 times in `appShellFlows.test.ts`, all substring matches inside `app.exit(`/`handleExit()`, not real skipped tests — confirmed via a refined grep excluding `.exit(`.
- **RED-proof-of-the-RED-proof (mandatory verification step):** backed up `environment.ts` (shasum `46bd3be5...`), temporarily mutated `isAppleSiliconMac` to `isMac && !/intel/i.test(cpus()[0]?.model ?? '')` (the exact unsafe-negation shape D-14 exists to catch), ran `removeCopies.test.ts` and observed exactly 3 predicted failures (empty-model case, `cpus()` returning `[]` case, and the dedicated RED-proof test itself — all expected `false`, received `true`). Restored the file via `cp` from the backup (never `git checkout --`), verified the restored file's shasum matched the backup exactly, then re-ran the suite and confirmed green (33/33 passed).
- **Pre-existing baseline failures, confirmed out of scope:** `decompressPool.test.ts` (3 failures, native LZMA decode unavailable in this dev environment) and `genI18nGateScope.test.ts` (1 failure, i18n fork-touched-files snapshot drift from unrelated concurrent frontend work) — both already logged in `deferred-items.md` from plan 34.18-01, reproduced identically here, neither file touched by this plan's edits.

## Verification Results

- `grep -rn isIntelMac src/ | wc -l` == 0
- `grep -rln isAppleSiliconMac src/ | wc -l` == 3 (survives, unchanged)
- `git diff --name-only public/locales/` — empty (no locale drift)
- `pnpm codecheck` — clean
- `pnpm lint` — 0 errors (4131 pre-existing warnings, unrelated)
- `pnpm prettier` — `GlobalState.tsx` reformatted (trailing-comma cleanup from this task's own edit); 3 unrelated pre-existing files flagged (games.test.ts, filterEngine.test.ts, installFlows.test.ts) left untouched, out of scope
- Backend: 4269/4274 passed, 2 skipped (3 pre-existing/unrelated decompressPool.test.ts failures — documented baseline)
- Common: 41/41 passed
- Frontend: 2059/2059 passed
- Preload: 140/140 passed
- Meta: 607/609 passed, 1 skipped (1 pre-existing/unrelated genI18nGateScope.test.ts failure — documented baseline)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`isIntelMac` is fully retired from the codebase; the zero-match gate (`meta/__tests__/isIntelMacRemoved.test.ts`) will catch any regression in CI. `isAppleSiliconMac` and its destructive-action gate in `games.ts` remain fully functional and untouched. No blockers for subsequent 34.18 plans.

---
*Phase: 34.18-retire-the-macos-x64-intel-ci-leg*
*Completed: 2026-08-27*

## Self-Check: PASSED

- FOUND: `meta/__tests__/isIntelMacRemoved.test.ts`
- FOUND: `.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-05-SUMMARY.md`
- FOUND: commit `649905885` (Task 1)
- FOUND: commit `8e7af620c` (Task 2)
- FOUND: commit `9ffe52920` (Task 3)
- FOUND: commit `375e3b113` (Task 4)
