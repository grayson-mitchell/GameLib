---
phase: 34.18-retire-the-macos-x64-intel-ci-leg
plan: 07
type: execute
wave: 5
depends_on:
  - 34.18-02
  - 34.18-03
  - 34.18-04
  - 34.18-05
  - 34.18-06
files_modified:
  - .planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-LIVE-GATE.md
  - meta/runnersOnedirDigests.json
  - electron-builder.yml
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - REQ-34.18-10
decisions: [D-05, D-06]

must_haves:
  truths:
    - "D-05: THE PHASE DOES NOT CLOSE ON SENTINELS. All six digest slots are `PENDING-CI-PUBLISH` today with `runId: null` — including the three arm64 ones — so retiring x64 alone does not fix the post-checkout hook throwing. The phase closes with three REAL arm64 digests and a `pnpm download-helper-binaries` that exits 0 on darwin. Code-ready is explicitly not done"
    - "D-05: The remote gestures are the DEVELOPER's, not the executor's (34.16 D-03). The executor hands over the gate criteria as WRITTEN and cited by number, and STOPS. It does not dispatch the workflow, does not run `pnpm pin:runner-digests` against the live release, and does not paraphrase the normative text"
    - "At least one pinned digest is independently re-derived locally with `shasum -a 256` against the downloaded archive. A mutating command's own success report is not proof of its effect (F-34.9-02) — the pin script read those digests from the same publication a green fetch would verify against, so a green-only result is near-tautological"
    - "D-06 follow-through: `electron-builder.yml`'s `[REMOVE WHEN DIGESTS ARE PINNED ...]` marker and the prose it tags are removed IN THE SAME COMMIT that pins the digests. That prose becomes stale-by-behaviour on exactly that commit; deferring it is what this project's constraint forbids, and 34.16-06's §9 criterion 7 said the same thing about the same marker"
    - "Ledger item 13 (`the guard has never been exercised in CI`) closes HERE and not earlier. Its mechanism — `install-deps`'s `pnpm download-helper-binaries` throwing on sentinels so CI never reaches the guard — is discharged only once the sentinels are gone. Plan 34.18-06 deliberately carried it forward rather than pre-closing it"
    - "The gate criteria live in a normative `34.18-LIVE-GATE.md` with numbered items and `Observed:` fields, so the developer's report is recorded against a citable criterion rather than summarised into prose that cannot be audited"
  artifacts:
    - path: ".planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-LIVE-GATE.md"
      provides: "the normative, numbered developer gate with Observed: fields"
      contains: "Observed:"
    - path: "meta/runnersOnedirDigests.json"
      provides: "three real arm64 sha256 digests and a non-null runId"
      contains: "runId"
    - path: "electron-builder.yml"
      provides: "mac: prose with the pinned-digests marker removed"
      contains: "mac:"
  key_links:
    - from: ".github/workflows/build-runners-onedir-macos.yml (dispatched on the default branch)"
      to: "meta/runnersOnedirDigests.json"
      via: "pnpm pin:runner-digests fetching SHA256SUMS-arm64 and BUILD-MANIFEST-arm64.json from the rolling release"
      pattern: "pin:runner-digests"
    - from: "meta/runnersOnedirDigests.json"
      to: "public/bin/arm64/darwin"
      via: "pnpm download-helper-binaries verifying each archive against the committed digest before extracting"
      pattern: "download-helper-binaries"
---

<objective>
Discharge D-05: hand the developer the two remote gestures that turn three `PENDING-CI-PUBLISH`
sentinels into three real arm64 digests, prove the result independently, and close the ledger item
that could only ever close here.

Purpose: the phase does not close on sentinels. Everything code-side is merged and green by this
wave; what remains is a workflow dispatch on the default branch and a `pnpm pin:runner-digests` run,
both of which are the developer's by 34.16's D-03. The post-checkout hook has been throwing on those
sentinels since Phase 34.9, and retiring x64 alone did not fix it.

Output: a normative gate document with recorded observations, three real digests committed together
with `electron-builder.yml`'s marker removal, a green `pnpm download-helper-binaries` on darwin, and
ledger item 13 closed on evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-CONTEXT.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-VALIDATION.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-PATTERNS.md

<interfaces>
<!-- State as of plan time (2026-08-27). Waves 1-4 change some of this; re-measure before Task 1. -->

`meta/runnersOnedirDigests.json` after plan 34.18-02: three keys
(`legendary_macOS_arm64_onedir.tar.gz`, `gogdl_macOS_arm64_onedir.tar.gz`,
`nile_macOS_arm64_onedir.tar.gz`), all `"PENDING-CI-PUBLISH"`, `runId: null`,
`layout: "onedir-v1"`.

`meta/downloadHelperBinaries.ts` throws on a sentinel before any fetch, and throws on a digest
mismatch before writing anything under `public/bin`, naming the pinned run id.

`meta/pinRunnerDigests.ts` after plan 34.18-03: fetches `${BASE_URL}/SHA256SUMS-arm64` and
`${BASE_URL}/BUILD-MANIFEST-arm64.json` from the `runners-onedir-macos` rolling release over
hardcoded repo/tag constants; validates digest format, known filenames and full key coverage;
asserts a non-null `runId`; writes once at the end or not at all.

`electron-builder.yml` after plan 34.18-04: the `mac:` prose block carries
`[REMOVE WHEN DIGESTS ARE PINNED -- Phase 34.18 plan 34.18-07's developer gate]` plus the sentence
about `pnpm download-helper-binaries` being unable to supply the runners (three sentinels).

`.planning/phases/34.9-.../deferred-items.md` item 13 (`:501`) after plan 34.18-06 carries
`**CARRIED FORWARD 2026-08-27 (Phase 34.18):**` pointing at this plan.

**The exact analog for this gate's shape** is `.planning/phases/34.16-.../34.16-06-PLAN.md:244-286`
(Task 3), an executed `checkpoint:human-verify` gating a `pin:runner-digests`-adjacent developer
hand-off. Its two conventions are load-bearing and must be reproduced:
  1. `<action>` says the normative doc is to be CITED, never paraphrased;
  2. `<how-to-verify>` names, in writing, the criteria a rushed human most often skips — and forbids
     skipping them — rather than merely listing the correct steps.
Its own §9 criterion 7 already required removing the `[REMOVE WHEN DIGESTS ARE PINNED ...]` marker
in the same commit as the pin. That gate never ran; this one inherits the obligation.

**Post-checkout hazard, standing project practice:** `git checkout -- <path>` fires
`.husky/post-checkout`, whose `pnpm download-helper-binaries` step throws on sentinel digests — so
during this gate a `git checkout` is doubly dangerous (it can also fire a full runner re-download at
an arbitrary moment). Back up by `cp` first; restore by `cp`; re-verify by `shasum -a 256`. Never
`git stash`, never `git reset`.

`.husky/pre-push` runs `pnpm codecheck && pnpm lint && pnpm prettier && pnpm i18n --fail-on-update`.
Prettier and `i18n --fail-on-update` have been RED repo-wide; verify the CURRENT state before
assuming a push needs `--no-verify`, and record what you found either way.
</interfaces>
</context>

<constraints>
- **The executor performs NO remote gesture.** No `gh workflow run`, no `gh release` command, no
  `pnpm pin:runner-digests` against the live release. Those are the developer's (34.16 D-03).
- **Never fabricate a digest.** If the developer's report is incomplete, the gate is not discharged.
  A hand-typed digest that happens to be 64 hex characters is worse than a sentinel, because the
  sentinel is honest.
- **The pin and the `electron-builder.yml` marker removal are ONE commit.** Not two, not deferred.
- **Do not close ledger item 13 before the gate returns a pass.** Task 3 runs only after Task 2's
  observations are recorded.
- **Do not edit any code, test or workflow file.** Waves 1-4 own those. This plan's only non-doc
  edits are the digest values in `meta/runnersOnedirDigests.json` and the marker/prose removal in
  `electron-builder.yml`.
- Restore by `cp` + `shasum -a 256`; never `git checkout -- <path>`, never `git stash`, never
  `git reset`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Author the normative 34.18-LIVE-GATE.md the checkpoint will cite</name>
  <read_first>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md — its §5 output-capture form, §9 (pin criteria) and §10 (corruption criteria), including their `Observed:` fields; .planning/phases/34.16-.../34.16-06-PLAN.md:244-286 (the checkpoint that consumed them); meta/runnersOnedirDigests.json and meta/pinRunnerDigests.ts and electron-builder.yml as left by waves 2-3; .planning/phases/34.18-.../34.18-VALIDATION.md § Manual-Only Verifications</read_first>
  <files>.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-LIVE-GATE.md</files>
  <action>
Before writing, RE-MEASURE the current state rather than trusting this plan's `<interfaces>` block:
the digest key set and values, `runId`, whether `electron-builder.yml` still carries the marker, and
whether `pnpm pin:runner-digests` is single-arch. Record those as numbered PRECONDITIONS at the top
of the document, each with the command that established it. A gate whose preconditions were assumed
rather than measured can decay between waves — this project has shipped exactly that.

Write `34.18-LIVE-GATE.md` with three numbered sections, each criterion individually numbered and
each carrying an empty `Observed:` field:

  **§1 — Dispatch (developer).** Confirm `.github/workflows/build-runners-onedir-macos.yml` on the
  DEFAULT BRANCH is byte-identical to the reviewed copy (`workflow_dispatch` only ever runs the
  default branch's copy — this is what cost Phase 34.16 an attempt). Dispatch it. Record the run id,
  the single leg's runner label and its conclusion. Criterion: exactly ONE leg ran, it was assigned
  a runner (non-empty `runner` AND non-empty `group` — an empty group is the signature of a label
  nothing serves, which is `F-34.16-G`'s own evidence), and it concluded `success`. Record that three
  `*_macOS_arm64_onedir.tar.gz` archives plus `SHA256SUMS-arm64` and `BUILD-MANIFEST-arm64.json` are
  present on the `runners-onedir-macos` release.

  **§2 — Pin (developer).** Run `pnpm pin:runner-digests`. Criteria: it exits 0; all three digest
  slots become 64-character LOWERCASE hex; `runId` is non-null and equals §1's recorded run id;
  `layout` is still `onedir-v1`; the `digests` key set is exactly the three arm64 names and is
  otherwise unchanged. **And the criterion most often skipped under time pressure, stated so it is
  not:** independently re-derive AT LEAST ONE digest locally — download that archive from the release
  and run `shasum -a 256` on it — and confirm it matches the pinned value. The pin script read those
  digests from the same publication a green fetch verifies against, so a green-only result is
  near-tautological; a mutating command's own success report is not proof of its effect (F-34.9-02).

  **§3 — Prove the consumer (developer).** On an arm64 Mac run `pnpm download-helper-binaries`.
  Criteria: it exits 0 with NO sentinel throw; `public/bin/arm64/darwin` afterwards contains all
  three runner trees; and each of `legendary`, `gogdl`, `nile` prints a version string and exits 0
  when invoked from that tree. Record `find public/bin/arm64/darwin | wc -l` and
  `find public/bin/arm64/darwin -type l | wc -l`. **Second most-skipped criterion, stated so it is
  not:** also confirm the FAIL-CLOSED path still works — back up `meta/runnersOnedirDigests.json` by
  `cp`, corrupt ONE character of one digest, re-run `pnpm download-helper-binaries`, and confirm two
  SEPARATE claims: (a) it exits non-zero with a message naming the archive and the pinned run id, and
  (b) it wrote NOTHING under `public/bin` (prove with `find public/bin -newer <backup-file>` listing
  nothing). Restore by `cp` and re-verify with `shasum -a 256`. **Never `git checkout -- <path>`** —
  it fires `.husky/post-checkout`, whose `pnpm download-helper-binaries` step would then fire a full
  runner re-download at an arbitrary moment. Never `git stash`, never `git reset`.

Add a closing section listing what the executor does after the developer reports: fill every
`Observed:` field, then commit the pinned `meta/runnersOnedirDigests.json` TOGETHER WITH
`electron-builder.yml`'s marker removal as ONE commit.
  </action>
  <acceptance_criteria>
- `34.18-LIVE-GATE.md` exists with three numbered sections and a numbered PRECONDITIONS block, every precondition citing the command that established it.
- Every criterion in §1, §2 and §3 has its own number and an empty `Observed:` field: `grep -c "Observed:" <file>` equals the total criterion count stated in the document's own header.
- §2 contains the independent `shasum -a 256` re-derivation criterion, explicitly flagged as most-often-skipped.
- §3 contains the corruption criterion split into TWO separate claims (non-zero exit naming the run id; nothing written under `public/bin`), and the `cp`-restore instruction with an explicit prohibition on `git checkout --`.
- The document nowhere instructs the executor to dispatch the workflow or run `pin:runner-digests`.
- The preconditions were re-measured in this task, not copied from the plan: the summary quotes the commands and their output.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=".planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-LIVE-GATE.md"; test -s "$F" && grep -q "Observed:" "$F" && grep -q "shasum -a 256" "$F" && grep -q "git checkout" "$F" && echo OK'</automated>
  </verify>
  <done>A normative, numbered gate document exists whose preconditions were measured against the tree as it now stands, and which the checkpoint can cite rather than paraphrase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN GATE — dispatch the arm64 workflow, pin real digests, prove the consumer (D-05)</name>
  <read_first>.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-LIVE-GATE.md §PRECONDITIONS, §1, §2 and §3 — the normative source for this gate</read_first>
  <files>meta/runnersOnedirDigests.json, electron-builder.yml, .planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-LIVE-GATE.md</files>
  <action>
Hand the developer §1, §2 and §3's criteria AS WRITTEN, cited by section and number, and STOP.
`34.18-LIVE-GATE.md` is normative — cite it, never paraphrase it. Do not dispatch the workflow, do
not run `pnpm pin:runner-digests`, do not run any `gh` command: those are the developer's gestures
under 34.16's D-03.

After the developer reports back, fill every `Observed:` field in `34.18-LIVE-GATE.md` with what they
actually reported (not with what the criterion expected), then commit the pinned
`meta/runnersOnedirDigests.json` TOGETHER WITH `electron-builder.yml`'s marker removal as ONE commit.
The `electron-builder.yml` edit is: delete the
`[REMOVE WHEN DIGESTS ARE PINNED -- Phase 34.18 plan 34.18-07's developer gate]` marker and rewrite
the sentence it tags — the claim that `pnpm download-helper-binaries` cannot supply the runners
becomes false on exactly this commit.

If any criterion fails, record the failure in its `Observed:` field and STOP. Do not pin partial
results, do not hand-type a digest, and do not proceed to Task 3.
  </action>
  <what-built>
All code-side consumers are retired and merged: one arm64 matrix leg, three digest slots, an
arm64-only downloader with a 34.18-current sentinel message, a single-arch `pin:runner-digests`, one
guard invocation per macOS packaging script (RED-proven strict against a known-bad tree), six
repaired upload globs, and `isIntelMac` removed from all 28 files. **Three `PENDING-CI-PUBLISH`
sentinels remain**, which is why `pnpm download-helper-binaries` still throws on darwin and why the
post-checkout hook has been failing since Phase 34.9. Nothing has ever been pinned: `runId` is still
`null`. Retiring x64 did not fix that and was never going to.
  </what-built>
  <how-to-verify>
Work `34.18-LIVE-GATE.md` §1, then §2, then §3, in order, recording an observation against every
numbered criterion. Capture command output to files and read exit codes back explicitly rather than
trusting a scrollback.

The three criteria most often skipped under time pressure, called out so they are not:

- **§1's runner-assignment check.** A leg that is queued is not a leg that ran. Record BOTH the
  `runner` and the `group` fields. An empty `runner` AND an empty `group` after 25+ minutes is a
  label nothing serves — that is exactly `F-34.16-G`'s evidence, and mistaking it for congestion cost
  Phase 34.16 three attempts.
- **§2's independent digest re-derivation.** Download at least one published archive and run
  `shasum -a 256` on it yourself. The pin script read its digests from the same publication a green
  fetch verifies against, so a green pin plus a green fetch is close to tautological. A mutating
  command's own success report is not proof of its effect (F-34.9-02). Without this, §2 is a
  self-report.
- **§3's corruption test, as TWO claims.** "It threw" and "it wrote nothing" are separate
  measurements needing separate evidence: the non-zero exit with the run id named, and a
  `find public/bin -newer <backup>` listing nothing. Back up by `cp` first; restore by `cp` and
  re-verify with `shasum -a 256`. **Never `git checkout -- <path>`** — it fires
  `.husky/post-checkout`, whose `pnpm download-helper-binaries` step would fire a full runner
  re-download at an arbitrary moment. Never `git stash`, never `git reset`.

Before pushing, check `.husky/pre-push`'s four gates (`pnpm codecheck && pnpm lint && pnpm prettier
&& pnpm i18n --fail-on-update`) against their CURRENT state rather than assuming the recorded
repo-wide red still holds, and record what you found either way.
  </how-to-verify>
  <acceptance_criteria>
- All three digest slots in `meta/runnersOnedirDigests.json` are 64-character lowercase hex; none is `PENDING-CI-PUBLISH`.
- `runId` is non-null and equals the run id recorded in §1; `layout` is still `onedir-v1`; the `digests` key set is exactly the three `_macOS_arm64_onedir.tar.gz` names.
- At least one digest was independently re-derived locally with `shasum -a 256` and matched; the archive name and the first 8 hex characters are recorded.
- `pnpm download-helper-binaries` exits 0 on darwin with no sentinel throw, and all three runners print a version string and exit 0 from `public/bin/arm64/darwin`.
- The corruption test produced BOTH: a non-zero exit whose message named the archive and the pinned run id, AND a `find public/bin -newer <backup>` that listed nothing. The file was restored by `cp` and re-verified by `shasum -a 256`.
- `grep -c "REMOVE WHEN DIGESTS ARE PINNED" electron-builder.yml` prints `0`, in the SAME commit as the pin: `git show --name-only HEAD` lists both `meta/runnersOnedirDigests.json` and `electron-builder.yml`.
- Every `Observed:` field in `34.18-LIVE-GATE.md` is filled with the developer's actual report.
- No `gh` command, no `pnpm pin:runner-digests` run and no `git checkout --`/`stash`/`reset` appears in the executor's own shell history.
  </acceptance_criteria>
  <resume-signal>Type "approved" plus the three pinned digests' first 8 hex characters and the run id — or name the numbered criterion that failed.</resume-signal>
  <verify>
    <automated>python3 -c "import json,re; d=json.load(open('meta/runnersOnedirDigests.json')); g=d['digests']; assert len(g)==3, g; assert all(re.fullmatch(r'[0-9a-f]{64}', v) for v in g.values()), g; assert all(k.endswith('_macOS_arm64_onedir.tar.gz') for k in g), g; assert d['runId'] is not None and d['layout']=='onedir-v1'; assert 'REMOVE WHEN DIGESTS ARE PINNED' not in open('electron-builder.yml').read(); print('OK', d['runId'])"</automated>
  </verify>
  <done>Three real arm64 digests and a non-null runId are committed alongside the marker removal in one commit; `pnpm download-helper-binaries` exits 0 on darwin; the fail-closed path was proven as two separate claims; every gate criterion has a recorded observation.</done>
</task>

<task type="auto">
  <name>Task 3: Close ledger item 13 and the requirement clauses that only this gate could discharge</name>
  <read_first>.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-LIVE-GATE.md with its Observed: fields filled; .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md item 13 (:501-518) including plan 34.18-06's CARRIED FORWARD paragraph; .planning/REQUIREMENTS.md rows and bullets for REQ-34.9-03, REQ-34.9-04, REQ-34.16-02 and REQ-34.18-10</read_first>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md, .planning/REQUIREMENTS.md</files>
  <action>
Run ONLY after Task 2 returned a pass with every `Observed:` field filled. If any criterion failed,
this task does not run.

**Ledger item 13** — append `**CLOSED 2026-08-27 (Phase 34.18, plan 34.18-07):**` beneath plan
34.18-06's `CARRIED FORWARD` paragraph, without deleting it. Record the evidence, not the intent:
the run id from §1, that `pnpm download-helper-binaries` exited 0 on darwin with no sentinel throw
(§3), and that the fail-closed path was separately proven by the corruption test (non-zero exit
naming the run id AND nothing written under `public/bin`). Item 13's mechanism was that
`install-deps`'s `pnpm download-helper-binaries` step threw on the sentinels so CI never reached the
guard; state that the sentinels are gone, which is what removes the blockage. **Do not claim the
guard has now run in a CI job unless §1's recorded evidence actually shows that** — if the dispatch
only exercised the runner-onedir workflow and not a `build-base.yml` macOS leg, say precisely that
and scope the closure to the sentinel blockage it actually removed. A closure that overstates what
was observed is the failure this phase has been guarding against throughout.

**Requirement clauses.** Append dated `2026-08-27` clauses recording the live proof to:
  - `REQ-34.9-03` (downloader sources darwin archives) — the live network sourcing path is now
    PROVEN, not merely unit-tested; cite §3.
  - `REQ-34.9-04` (sha256 digest verification) — now exercised against a real published digest, with
    at least one independently re-derived locally; cite §2's re-derivation and §3's corruption test.
  - `REQ-34.16-02` (guard actually exercised in CI) — update its PARTIAL disposition from plan
    34.18-06 with what §1/§3 actually established, using the same do-not-overstate rule as item 13.
  - `REQ-34.18-10` — mark complete, citing the run id and the three pinned digests' first 8 hex
    characters.
Update each ID's status-table row AND its detail bullet. Append; never rewrite prior text. Never a
`### <ID>` heading.

**Concurrent-edit hazard:** `.planning/REQUIREMENTS.md` is edited by other sessions. `git diff` the
path before committing and commit with explicit paths only.
  </action>
  <acceptance_criteria>
- `deferred-items.md` item 13 carries BOTH plan 34.18-06's `CARRIED FORWARD 2026-08-27` paragraph and a new `CLOSED 2026-08-27 (Phase 34.18, plan 34.18-07)` paragraph; the earlier paragraph is not deleted.
- Item 13's closure paragraph names the run id and states what was and was NOT observed about a CI-run guard.
- `REQ-34.9-03`, `REQ-34.9-04`, `REQ-34.16-02` and `REQ-34.18-10` each carry a dated `2026-08-27` live-proof clause in both the status-table row and the detail bullet.
- `REQ-34.18-10`'s status cell reads Complete and names the run id.
- `grep -c "^### " .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md` is unchanged from its pre-edit value.
- `git diff --name-only` for this task lists only the two files in this task's file list.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md; awk "/^### 13\./,/^### 14\./" $F | grep -q "CLOSED 2026-08-27" && awk "/^### 13\./,/^### 14\./" $F | grep -q "CARRIED FORWARD 2026-08-27" && grep -q "REQ-34.18-10.*2026-08-27" .planning/REQUIREMENTS.md && echo OK'</automated>
  </verify>
  <done>Item 13 is closed on recorded evidence rather than on the intent of the phase, the three requirement clauses that only this gate could discharge are updated, and no closure overstates what was actually observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions run -> rolling release -> every build machine | The archives and digests published by the dispatched run become what every developer and CI checkout downloads and executes. |
| committed digest -> extracted binary | `meta/runnersOnedirDigests.json` is the in-repository statement of correct bytes. Everything downstream trusts it without re-deriving it. |
| developer report -> planning record | The executor cannot observe the remote gestures; it records what the developer reports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.18-25 | Tampering | `pnpm pin:runner-digests` self-report | mitigate | The pin script reads its digests from the same publication a subsequent fetch verifies against, so "pin succeeded, fetch succeeded" is near-tautological and would not detect a tampered release asset. §2 requires at least one digest to be independently re-derived locally with `shasum -a 256` against the downloaded archive — a mutating command's own success report is not proof of its effect (F-34.9-02). |
| T-34.18-26 | Tampering | `downloadOnedirAsset()` fail-closed path after pinning | mitigate | Once real digests are pinned, the sentinel throw stops firing and the mismatch throw becomes the only remaining supply-chain gate. §3's corruption test proves it as TWO separate claims — a non-zero exit naming the pinned run id, AND nothing written under `public/bin` — because "it threw" and "it wrote nothing" are different properties and a partial write would still poison the tree. |
| T-34.18-27 | Spoofing | `workflow_dispatch` against a non-default branch | mitigate | `workflow_dispatch` only ever runs the DEFAULT BRANCH's copy of a workflow, so dispatching while the reviewed change sits on a feature branch silently exercises different YAML. §1 requires confirming the default-branch copy is byte-identical to the reviewed one before dispatch — the same trap that cost Phase 34.16 an attempt. |
| T-34.18-28 | Repudiation | `electron-builder.yml`'s pinned-digests marker | mitigate | The prose claiming `download-helper-binaries` cannot supply the runners becomes false on exactly the pin commit. Splitting the two into separate commits leaves a commit whose config prose contradicts its own repository state — 34.16-06's §9 criterion 7 said the same, and that gate never ran. Enforced by a `git show --name-only HEAD` criterion listing both files. |
| T-34.18-29 | Repudiation | ledger item 13's closure | mitigate | Closing item 13 with "the guard now runs in CI" when the dispatch exercised only the runner-onedir workflow would be a false record forced by a closure gate. Task 3 explicitly scopes the closure to the sentinel blockage actually removed and forbids overstating §1's evidence. |
| T-34.18-30 | Denial of Service | `.husky/post-checkout` during the gate | mitigate | `git checkout -- <path>` fires `download-helper-binaries`, which during this gate can trigger a full runner re-download at an arbitrary moment or throw on a half-restored manifest. Backup-by-`cp`, restore-by-`cp`, verify-by-`shasum` is stated in the gate document AND in the checkpoint's own how-to-verify, and the prohibition is an acceptance criterion. |
| T-34.18-SC | Tampering | npm/pip/cargo installs | accept | **This plan installs no packages.** The only non-documentation writes are three digest values and a comment-block removal. |
</threat_model>

<verification>
1. `meta/runnersOnedirDigests.json`: exactly 3 keys, all `_macOS_arm64_onedir.tar.gz`, each a 64-char lowercase hex digest; `runId` non-null; `layout` `onedir-v1`.
2. `grep -c "REMOVE WHEN DIGESTS ARE PINNED" electron-builder.yml` prints `0`, and `git show --name-only <pin-commit>` lists both that file and the digests file.
3. `pnpm download-helper-binaries` exits 0 on darwin; all three runners print a version and exit 0 from `public/bin/arm64/darwin`.
4. The corruption test's two claims are separately recorded, and the manifest was restored by `cp` with a matching `shasum -a 256`.
5. At least one digest independently re-derived locally; archive name and first 8 hex characters recorded.
6. Every `Observed:` field in `34.18-LIVE-GATE.md` is filled with the developer's actual report.
7. Ledger item 13 closed with evidence and without overstating what §1 showed; REQ-34.9-03, REQ-34.9-04, REQ-34.16-02 and REQ-34.18-10 updated.
8. `.husky/pre-push`'s four gates checked against their current state and the result recorded, whichever way it went.
9. No `gh` command, no `pin:runner-digests` run, no `git checkout --`/`stash`/`reset` in the executor's shell history.
</verification>

<success_criteria>
- Three real arm64 digests and a non-null `runId` are committed together with the `electron-builder.yml` marker removal in ONE commit.
- `pnpm download-helper-binaries` exits 0 on darwin — the post-checkout hook stops throwing.
- The fail-closed path is proven after pinning, as two separate claims.
- At least one digest independently re-derived; no digest hand-typed.
- Ledger item 13 closed on recorded evidence, scoped to what was actually observed.
- **The phase closes here, not on sentinels.**
</success_criteria>

<output>
Create `.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-07-SUMMARY.md` when done. It MUST record the run id, the three pinned digests' first 8 hex characters, the independently re-derived digest, both claims of the corruption test, and the single commit SHA carrying the pin plus the marker removal.
</output>
