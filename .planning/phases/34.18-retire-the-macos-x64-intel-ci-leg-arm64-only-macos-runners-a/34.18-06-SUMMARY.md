---
phase: 34.18-retire-the-macos-x64-intel-ci-leg
plan: 06
subsystem: docs
tags: [record-closure, requirements, roadmap, deferred-items, ipc-inventory, no-source-changes]

# Dependency graph
requires:
  - phase: 34.18 (plans 01-05)
    provides: the arm64-only CI workflow, guard reduction, digest-pin reduction, and README/isIntelMac removal that this plan's dispositions cite as their discharge mechanisms
provides:
  - REQ-34.1-05 amended (not rewritten) with a dated 2026-08-27 sub-bullet recording the 19->18 sidecar-registered-channel count and the deliberate non-propagation of that count into closed-phase historical records
  - IPC-PORT-INVENTORY.md's Phase 34.1 slice count moved 33->32 with `isIntelMac` dropped from its channel list
  - 34.1-PORTED-CHANNELS.md's `isIntelMac` row annotated retired (row kept, not deleted)
  - Dated 2026-08-27 dispositions for F-34.16-D (x64 half only; arm64 half's prior FIXED/proven-live status untouched) and F-34.16-G (closed resolved-by-retirement) in ROADMAP.md
  - Dated 2026-08-27 dispositions, in both status-table row and detail bullet, for REQ-34.9-02/-03/-04 and REQ-34.16-01/-02/-04 in REQUIREMENTS.md
  - deferred-items.md items 1, 2, 3, 12 CLOSED 2026-08-27; item 13 CARRIED FORWARD (explicitly not closed); item 18 (C2-05) given a CROSS-REFERENCE addendum recording that its 2026-08-25 closure rationale is now stale-by-behaviour, without re-closing it
affects: [34.18-07 (item 13's actual discharge mechanism), any future audit of Phase 34.1/34.9/34.16 closed-phase records]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Amend, never rewrite: closed-phase requirement/finding text stays byte-unchanged; new information lands as a dated, clearly-labelled appended bullet or paragraph"
    - "Cross-reference vs. re-closure: an already-CLOSED ledger item whose cited rationale goes stale gets a dated CROSS-REFERENCE addendum naming the staleness and its cause, never a second CLOSED label"
    - "Propagation boundary recorded, not silently applied: which closed-phase historical documents are deliberately left untouched is written down as a decision, not left as an undetectable omission"
    - "Carried-forward vs. closed: a ledger item whose discharge mechanism is a later plan that has not yet run stays open, dated, with the mechanism named — closing it early would be a false record"

key-files:
  modified:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/IPC-PORT-INVENTORY.md
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-PORTED-CHANNELS.md
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md

decisions:
  - "D-13 (34.18-CONTEXT.md): REQ-34.1-05's 19-channel count is amended with a dated sub-bullet, original text stays byte-unchanged; 34.1-VERIFICATION.md, 34.2-02/-04 PLAN/SUMMARY, and 34.5-UAT.md are deliberately NOT touched — recorded as a propagation boundary in the new sub-bullet itself, not left as a silent omission"
  - "Item 18/C2-05 gets a CROSS-REFERENCE addendum, not a second CLOSED label, per the plan's explicit must-have; the addendum also records the NEW_FINDING surfaced during execution (34.18-04 removed the exact --arch=x64 guard step item 18's 2026-08-25 closure cited), stating the finding is now moot-by-construction rather than reverted"
  - "Item 13 stays CARRIED FORWARD rather than CLOSED, because its discharge mechanism (plan 34.18-07's developer gate) has not run yet at this plan's execution time — closing it now would be a false record"

# Metrics
duration: ~50min
completed: 2026-08-27
---

# Phase 34.18 Plan 06: Retirement record-closure — dated dispositions, zero re-closures Summary

Record-closure documentation pass giving every superseded finding, requirement, and ledger item touched by the macOS x64/Intel retirement a dated 2026-08-27 disposition — without re-closing anything already closed, without a silent omission where propagation was deliberately withheld, and with zero source-code changes.

## What Was Done

**Task 1 — REQ-34.1-05 amendment and channel-count propagation** (commit `fb6543eea`)
- Appended a dated sub-bullet to REQ-34.1-05 (never touching its original text) recording that `isIntelMac` was removed from the sidecar registry by plan 34.18-05, dropping the sidecar-routed channel count from 19 to 18, and explicitly naming the propagation boundary: `34.1-VERIFICATION.md`'s two "19 channels" mentions and the "19 channels" mentions in `34.2-02-PLAN.md`, `34.2-02-SUMMARY.md`, `34.2-04-PLAN.md`, `34.2-04-SUMMARY.md` are deliberately left untouched as as-of-close historical records; `34.5-UAT.md:507`'s "19 channels" is a different count (never-ported channels) and was never in scope regardless.
- `.planning/IPC-PORT-INVENTORY.md`: Phase 34.1 slice heading moved from "(33 channels)" to "(32 channels)", `isIntelMac` removed from the alphabetical channel list, with a dated note distinguishing this slice-total count from REQ-34.1-05's narrower sidecar-registration count.
- `34.1-PORTED-CHANNELS.md`: the `isIntelMac` row is annotated retired (channel and constant removed entirely, arm64-only build cannot run on Intel) — the row is kept, not deleted, as the as-of-close record of what Phase 34.1 shipped.
- Verified: `git diff` on this commit shows only additions to REQ-34.1-05's block (no modification to its original bullet); "33 channels" count drops to 0, "32 channels" count is 1; `isIntelMac` appears only in the new dated note, not in the channel list itself.

**Task 2 — Dated dispositions for two findings and six requirements** (commit `9f64b73c7`)
- `ROADMAP.md`: `F-34.16-D` disposed on its **x64 half only** — the arm64 half's prior FIXED/proven-live status (plans 34.16-07/-08/-09) is explicitly preserved, not restated as closed; the x64 half is recorded moot-by-retirement (no x64 tree exists to apply the fix to). `F-34.16-G` closed resolved-by-retirement — its own content (macos-13 retirement, three failed runner-assignment attempts) is recorded as the evidence that drove the retirement decision, not a mistaken finding.
- `REQUIREMENTS.md`: REQ-34.9-02/-03/-04 and REQ-34.16-01/-02/-04 each received a dated 2026-08-27 disposition in **both** their status-table row and detail bullet (verified independently — see below), naming which half is achieved (arm64) versus non-goal (x64), and closing or explicitly carrying forward the corresponding `deferred-items.md` ledger item. REQ-34.16-03 was left untouched (out of scope for this plan). REQ-34.16-02's disposition correctly stops short of claiming completion — its x64-adjacent CI-exercise half stays open pending item 13's plan 34.18-07 discharge.
- Verified: all six IDs carry `2026-08-27` in both locations (lines 395-397/405-408 for status rows, 905-907/1040-1043 for detail bullets); `grep -c "^### F-34.16"` on ROADMAP.md = 0 (no headings introduced); F-34.16-D's disposition contains "arm64 half" and "FIXED"/"proven live"; REQ-34.16-04's disposition contains "REQ-34.18-03" and "superseded".

**Task 3 — Dated dispositions across six deferred-items.md ledger items** (commit `02a9c7ca6`)
- Items 1, 2, 3, 12: each given a `**CLOSED 2026-08-27 (Phase 34.18, plan 34.18-06)**` paragraph. Items 1-3 record the x64 clause as a non-goal (citing `F-34.16-G`'s runner-unschedulability evidence and the operator's retirement decision) and the arm64 clause as satisfied by Phase 34.18's CI leg. Item 12 records the arm64 half as ACHIEVED and REMAINING achieved (plan 34.18-04 removed `--arch=x64` and RED-proved the surviving guard against two known-bad trees) while the x64 half's unresolved coverage question is recorded MOOT-BY-CONSTRUCTION, not answered.
- Item 13: given a `**CARRIED FORWARD, dated 2026-08-27 ... — NOT closed**` paragraph naming plan 34.18-07's developer gate as the actual discharge mechanism, which has not yet run.
- Item 18 (C2-05): given a `**CROSS-REFERENCE, dated 2026-08-27 ...**` paragraph — explicitly not a second CLOSED label. It records that the 2026-08-25 closure's cited mechanism (`dist:mac`/`release:mac`'s hardcoded `--arch=x64` guard step) was accurate on 2026-08-25 but is now stale-by-behaviour because plan 34.18-04 removed that exact step. Per the NEW_FINDING surfaced mid-execution, the addendum states the finding is now **moot-by-construction** (x64 packaging removed entirely, so there is no x64 build left to reach the auto-publish channel C2-05 was about) rather than reverted, and names plan 34.18-04 explicitly.
- Verified: `git diff --stat` on this commit shows 12 insertions, 0 deletions (strictly append-only); the item-18 line range (591-680) contains zero `CLOSED 2026-08-27` occurrences and one unchanged `CLOSED 2026-08-25`; item 13's actual block (lines 509-537) contains zero `**CLOSED` labels, one `CARRIED FORWARD` label, and one `34.18-07` mention; total `### ` heading count is unchanged (39, same as before this plan); all six pre-existing `**ROUTED 2026-08-22:**` lines are unchanged and present.

## Overall Plan Verification

- `git diff --name-only` across all three task commits (`fb6543eea`, `9f64b73c7`, `02a9c7ca6`) touches only: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/IPC-PORT-INVENTORY.md`, `.planning/phases/34.1-.../34.1-PORTED-CHANNELS.md`, `.planning/phases/34.9-.../deferred-items.md` — nothing outside `.planning/`.
- Zero `^\+### ` (new markdown `### <ID>` headings) introduced across all three commits — every disposition landed as an appended bullet or paragraph, per the plan's explicit constraint.
- `src/backend/sidecar/appShellFlowRegistration.ts` was read but not edited (out of this plan's `files_modified` scope): its doc comments still read "19 ... (7 invoke + 12 send)", which is numerically coincidental (later additions — `frontendReady` and a tray-color registration — offset `isIntelMac`'s removal) and does not conflict with REQ-34.1-05's own at-close count; the plan's internal safety gate (doc comments must not have moved ahead of code) passed.
- `npx jest --selectProjects Meta`: 605 total, 603 passed, 1 failed, 1 skipped — unchanged from the pre-plan baseline. The 1 failure is `meta/__tests__/genI18nGateScope.test.ts`'s pre-existing "A-17 ANTI-ROT" drift, not owned by this plan.
- `.planning/STATE.md` was never opened or edited, per the critical constraint (1.16MB, off limits). No `gsd-sdk query state.*`, `roadmap.update-plan-progress`, or `phase.complete` verb was ever invoked.
- No `git stash`, `git reset --hard`, or `git checkout -- <file>` was run at any point.
- Every commit was preceded by `git status --porcelain` to confirm only this plan's own target paths were staged, per the concurrent-session safety constraint. No collision was observed at any commit point (one unrelated pre-existing modification, `34.13-UAT.md`, was present in the working tree throughout from before this session started and was never staged or touched by any of this plan's commits).

## Deviations from Plan

### Auto-fixed Issues

None — the plan's three tasks were executed as written.

### NEW_FINDING handled (not a deviation from acceptance criteria, but a discovery during execution)

**Item 18/C2-05's 2026-08-25 closure rationale is now stale-by-behaviour.** Its `FIXED` justification cited that `dist:mac`/`release:mac` run `pnpm verify:runner-bundle build --arch=x64` as a hardcoded step — plan 34.18-04 removed that exact step. Per the plan's own must-have, item 18 was **not** re-closed. Instead, a `CROSS-REFERENCE` addendum dated 2026-08-27 was appended, naming plan 34.18-04 explicitly, and stating the finding is now moot-by-construction (x64 packaging removed entirely) rather than reverted. See Task 3 above.

## Known Stubs

None — this plan makes no source changes and introduces no UI-facing code.

## Threat Flags

None — this plan is documentation-only; no new network endpoints, auth paths, file access patterns, or schema changes were introduced. The plan's own threat_model entries (T-34.18-21 through T-34.18-24, T-34.18-SC) concerning re-closure/false-record/collision risks were all addressed by construction (see Deviations and Overall Plan Verification above) rather than surfacing new surface.

## Self-Check: PASSED

- `.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-06-SUMMARY.md` — FOUND (this file, written before commit per required order).
- Commit `fb6543eea` — FOUND in git log.
- Commit `9f64b73c7` — FOUND in git log.
- Commit `02a9c7ca6` — FOUND in git log.
