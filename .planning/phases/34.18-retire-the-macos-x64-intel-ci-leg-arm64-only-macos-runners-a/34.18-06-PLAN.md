---
phase: 34.18-retire-the-macos-x64-intel-ci-leg
plan: 06
type: execute
wave: 4
depends_on:
  - 34.18-03
  - 34.18-04
  - 34.18-05
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/IPC-PORT-INVENTORY.md
  - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-PORTED-CHANNELS.md
  - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
autonomous: true
requirements:
  - REQ-34.18-09
decisions: [D-13]

must_haves:
  truths:
    - "D-13: `REQ-34.1-05` is AMENDED with a dated superseding note, never rewritten. Its text records 19 sidecar-routed channels including `isIntelMac`; that is what Phase 34.1 actually verified at close, and rewriting it would falsify a closed phase's record. The note says 34.18 retired the channel and the live count is now 18"
    - "D-13: `.planning/IPC-PORT-INVENTORY.md`'s Phase 34.1 slice heading goes 33 -> 32 and drops `isIntelMac` from its channel list; `34.1-PORTED-CHANNELS.md`'s `isIntelMac` row is annotated as retired by 34.18"
    - "D-13 scope is bounded ON PURPOSE, and the boundary is RECORDED rather than left as a silent omission. `34.1-VERIFICATION.md`'s two `19 channels` mentions and the 34.2 PLAN/SUMMARY mentions are as-of-close historical records and stay untouched — a propagation that 'fixes' them would falsify executed history. This project has repeatedly been bitten by a status doc lagging undetected, so the decision NOT to propagate is written down where the next reader will find it"
    - "Every superseded finding and requirement gets a DATED disposition: `F-34.16-D` (its x64 half ONLY — the arm64 half is FIXED and proven live), `F-34.16-G`, REQ-34.9-02/-03/-04, and REQ-34.16-01/-02/-04. For REQ-34.16-01 and -02 the disposition must say WHICH PART became a non-goal, because both have arm64 halves that were achieved"
    - "Ledger items 1, 2 and 3 stop pointing at a closed phase. They are ROUTED to Phase 34.16, which closed PARTIAL on 2026-08-27, and none has been touched since — they are closed directly with a dated note citing both 34.16's partial achievement and 34.18's completion of the arm64-only path, avoiding a needless intermediate routing hop"
    - "Item 18 / C2-05 is CROSS-REFERENCED, never re-closed. It already carries `CLOSED 2026-08-25` (quick task `260825-alv`, commit `adc648eb6`) with its residual explicitly re-homed to items 12 and 13. A second closure would create a contradictory record; the ROADMAP's disposition list treating it as open is stale"
    - "Item 13 is NOT pre-closed here. Its mechanism — `install-deps`'s `pnpm download-helper-binaries` throwing on sentinels so CI never reaches the guard — is discharged only by plan 34.18-07's developer gate. It gets a dated cross-reference to that gate and stays open until the gate runs"
    - "Every disposition is a table row or a bullet append. NONE is a `### <ID>` heading — a `-REVIEW.*\\.md$` closure glob harvests `^### <ID>` headings, and the convention holds across these files independently of whether that glob can fire on them"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "dated dispositions for REQ-34.9-02/-03/-04 and REQ-34.16-01/-02/-04, plus REQ-34.1-05's D-13 amendment"
      contains: "Phase 34.18"
    - path: ".planning/IPC-PORT-INVENTORY.md"
      provides: "Phase 34.1 slice count of 32 with isIntelMac removed from the channel list"
      contains: "32 channels"
    - path: ".planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md"
      provides: "dated dispositions for items 1, 2, 3, 12; a carry-forward note on 13; a cross-reference note on 18"
      contains: "2026-08-27"
  key_links:
    - from: ".planning/REQUIREMENTS.md REQ-34.1-05"
      to: "src/backend/sidecar/appShellFlowRegistration.ts"
      via: "a dated amendment sub-bullet recording 19 -> 18 registered channels after 34.18 removed isIntelMac"
      pattern: "REQ-34.1-05"
    - from: ".planning/phases/34.9-.../deferred-items.md item 18"
      to: "items 12 and 13"
      via: "a cross-reference note pointing at where the residual was re-homed, NOT a second closure"
      pattern: "C2-05"
---

<objective>
Close the record. Every finding, requirement and ledger item the retirement supersedes gets a dated
disposition; the IPC channel-count docs record 19 -> 18; and the deliberate boundary of that
propagation is written down rather than left as a silent omission.

Purpose: the ROADMAP's own scope section says these must "each get a dated disposition rather than
being silently abandoned". Two traps sit inside that instruction — item 18/C2-05 is ALREADY closed
(re-closing it manufactures a contradictory record), and items 1/2/3 are ROUTED to a phase that has
since closed (leaving them pointing at it is the "blocker records rot silently" shape this project
has hit three times).

Output: no superseded item left open by omission, nothing closed twice, and the not-propagated docs
named with a reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-CONTEXT.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-RESEARCH.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-PATTERNS.md

<interfaces>
<!-- Anchors verified at plan time (2026-08-27, HEAD). Line numbers may shift by a few lines if a
     concurrent session edited these files — locate by the quoted text, not by the number alone. -->

`.planning/REQUIREMENTS.md`
  `:395-397` status-table rows `| REQ-34.9-02 | Phase 34.9 | Partial (2026-08-11 — ...) |` and the
    same shape for `-03`, `-04`.
  `:405-408` status-table rows for `REQ-34.16-01`, `-02`, `-03`, `-04`.
  `:588`  `- [x] **REQ-34.1-05**: Curated sidecar registration for the 19 sidecar-routed channels.
          A new `src/backend/sidecar/appShellFlowRegistration.ts` registers
          `getCustomThemes`/.../`isIntelMac`/... *(D-03, D-09)*`
    The D-13 amendment goes as a NEW INDENTED SUB-BULLET directly beneath this row, matching the
    existing amendment style used by `REQ-34.1-02`/`REQ-34.1-03` immediately above in the same file.
  `:894-896` detail bullets `- **REQ-34.9-02**: ...`, `-03`, `-04`.
  `:1029-1032` detail bullets `- [ ] **REQ-34.16-01**: ...`, `-02`, `-04`.

`.planning/ROADMAP.md`
  `### Phase 34.16` block at `:1361`, whose closure section (`:1448-1476`) already carries the
  bullets `- **Achieved.** ...`, `- **Not achieved, and not achievable as specified.** ...`,
  `- **F-34.16-D and F-34.16-G remain open/blocking against the phase, by design.** ...` and
  `**Superseded by a retirement decision (operator, 2026-08-27).** ...`. Dispositions here are
  BULLETS, matching `- [x] ... — **RAN 2026-08-27** ...` elsewhere in the file. Never headings.

`.planning/IPC-PORT-INVENTORY.md:125` — `## Phase 34.1 — Slice 4 — app shell and window chrome (33 channels)`
  `:132` — the alphabetical channel list containing `isIntelMac` between `isFullscreen` and
  `isMaximized`.

`.planning/phases/34.1-.../34.1-PORTED-CHANNELS.md:56` — the table row
  `| `isIntelMac` | sidecar invoke | `backend/constants/environment`'s `isIntelMac` const | unit | Plan 34.1-04 |`

`.planning/phases/34.9-.../deferred-items.md` — item headings and their existing append lines:
  `:16` `### 1. REQ-34.9-02 — CI x64 leg` ... `:40` `**ROUTED 2026-08-22:** now owned by **Phase 34.16** ...`
  `:42` `### 2. REQ-34.9-03 — the downloader sources darwin archives` ... `:59` same ROUTED line
  `:61` `### 3. REQ-34.9-04 — sha256 digest verification` ... `:75` same ROUTED line
  `:480` `### 12. The wired guard covers arm64 only` ... `:499` same ROUTED line
  `:501` `### 13. The guard has never been exercised in CI` ... `:517` same ROUTED line
  `:581` `### 18. C2-05 — the arm64-only guard is live and active in real CI, and gates an auto-publishing release`
        ... `:616` ROUTED line ... `:641` `**CLOSED 2026-08-25** (quick task `260825-alv`), by commit
        `adc648eb6`` with "Three qualifications, all of which survive closure", qualification 2
        stating the residual was "re-homed to items 12 and 13" (NOT dropped).
  The append template, verbatim in shape, is the existing `**ROUTED 2026-08-22:**` paragraph: a
  bold label, a date, then prose that explicitly says it supersedes only the routing and leaves the
  prior text standing.

**Docs deliberately NOT propagated (record this, do not edit them):**
  `.planning/phases/34.1-.../34.1-VERIFICATION.md:255` and `:325` — two "19 sidecar-routed channels"
    mentions in a CLOSED phase's verification record.
  `.planning/phases/34.2-.../34.2-02-PLAN.md`, `34.2-02-SUMMARY.md`, `34.2-04-PLAN.md`,
    `34.2-04-SUMMARY.md` — multiple "19 channels" mentions in executed plan/summary records.
  `.planning/phases/34.5-.../34.5-UAT.md:507` — "19 channels intentionally not ported: 3 DROPPED...
    16 DEFERRED..." — a DIFFERENT 19 (channels never ported at all), a false positive; do not
    conflate it with REQ-34.1-05's registered-channel count.
</interfaces>
</context>

<constraints>
- **Amend, never rewrite.** `REQ-34.1-05`'s existing text stays byte-identical; the amendment is a
  new sub-bullet beneath it. Same for every `**ROUTED 2026-08-22:**` line in `deferred-items.md` —
  never delete a prior line.
- **Never a `### <ID>` heading.** Every disposition is a table row or a bullet/paragraph append. A
  `-REVIEW.*\.md$` closure glob harvests `^### <ID>` headings project-wide; the convention holds
  across these files regardless.
- **Do NOT renumber `deferred-items.md`'s items.** Its numbering is its own internal cross-reference
  scheme.
- **Item 18 / C2-05: cross-reference ONLY.** It is already `CLOSED 2026-08-25`. Do not add a second
  `**CLOSED ...**` line. Do not restate its closure. The ROADMAP's disposition list is stale on this
  point and must not be inherited.
- **Item 13 is NOT closed here.** Closing it now would be a false record — the guard has still never
  run in CI at the time this plan executes. Plan 34.18-07 closes it.
- **Do NOT edit `34.1-VERIFICATION.md`, any `34.2-*` PLAN/SUMMARY, or `34.5-UAT.md`.** They are not
  in `files_modified`. Their exclusion is recorded, not acted on.
- **Do not touch any code, test or workflow file.** This plan is documentation only; `git diff
  --name-only` must contain nothing outside `.planning/`.
- Restore by `cp` + `shasum -a 256`, never `git checkout -- <path>`. Never `git stash`/`git reset` —
  `.planning/` is exactly where a concurrent session's work gets stranded.
- **Concurrent-edit hazard:** `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` are edited by
  other sessions. Commit with `git commit --only <explicit paths>` and, before committing, `git diff`
  each path to confirm you are not absorbing someone else's row.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: D-13 — amend REQ-34.1-05 and propagate the channel count, recording the propagation boundary</name>
  <read_first>.planning/REQUIREMENTS.md around :585-595 (REQ-34.1-05 and the REQ-34.1-02/-03 amendment style immediately above it); .planning/IPC-PORT-INVENTORY.md :120-140; .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-PORTED-CHANNELS.md :45-65; src/backend/sidecar/appShellFlowRegistration.ts:1-10 and :180-190 as left by plan 34.18-05 (to confirm the registration's own count is now 19 / 7 invoke + 12 send)</read_first>
  <files>.planning/REQUIREMENTS.md, .planning/IPC-PORT-INVENTORY.md, .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-PORTED-CHANNELS.md</files>
  <action>
**REQ-34.1-05 amendment.** Leave the requirement's own text untouched. Add ONE new indented
sub-bullet directly beneath it, matching the amendment style of `REQ-34.1-02`/`REQ-34.1-03` above,
labelled `**Amendment 2026-08-27 (Phase 34.18):**`. It must say:
  - `isIntelMac` was removed from the sidecar registry because an arm64-only macOS build cannot run
    on an Intel Mac, so the constant is permanently `false` in every shipped build and exposing it
    claimed a capability the app does not ship;
  - the 19-channel count in the requirement's own text is now HISTORICAL — it records what Phase 34.1
    verified at close — and 18 channels remain registered;
  - this does not reopen or invalidate Phase 34.1's verified close; it records a change made
    afterwards;
  - **and, explicitly, the propagation boundary**: `34.1-VERIFICATION.md`'s two "19 channels"
    mentions and the 34.2 PLAN/SUMMARY mentions are deliberately left as as-of-close historical
    records; `34.5-UAT.md:507`'s "19 channels" is a different count (channels never ported at all)
    and is not in scope. Name those files. This project has been bitten by a propagation plan missing
    a status doc undetected, and the antidote is to write the decision down where the next reader
    checking for drift will find it — not to leave it inferable from an absence.

**IPC-PORT-INVENTORY.md.** Change the Phase 34.1 slice heading from `(33 channels)` to
`(32 channels)`, remove `isIntelMac` from the alphabetical channel list at `:132` (it sits between
`isFullscreen` and `isMaximized` — remove the entry and its comma, leaving the list well-formed), and
add a short dated note under that heading recording that Phase 34.18 retired the channel. **Do not
conflate the two numbers:** 33/32 is this slice's total channel count; 19/18 is REQ-34.1-05's
narrower sidecar-routed-registration count. Both drop by exactly one, for the same reason, and the
note should say so, because a later reader seeing two different numbers move together will otherwise
suspect one of them is wrong.

**34.1-PORTED-CHANNELS.md.** Annotate the `isIntelMac` table row rather than deleting it — it is a
record of what 34.1 ported. Append to the row's last cell (or add a dated marker in the row) that
Phase 34.18 retired the channel on 2026-08-27. Keep the table's column count intact.

Verify against the code as it now stands, not against this plan's prose: confirm
`appShellFlowRegistration.ts`'s doc comments read 19 (7 invoke + 12 send) after plan 34.18-05. If
they do not, STOP — the docs must not be moved ahead of the code.
  </action>
  <acceptance_criteria>
- `grep -c "Amendment 2026-08-27 (Phase 34.18)" .planning/REQUIREMENTS.md` prints at least `1`, and that bullet names `34.1-VERIFICATION.md`, the 34.2 records and `34.5-UAT.md` as deliberately-not-propagated.
- `REQ-34.1-05`'s original bullet text is byte-unchanged: `git diff .planning/REQUIREMENTS.md` shows only ADDED lines in that region, no modified line beginning `- [x] **REQ-34.1-05**`.
- `grep -c "33 channels" .planning/IPC-PORT-INVENTORY.md` prints `0`; `grep -c "32 channels" .planning/IPC-PORT-INVENTORY.md` prints `1`.
- `grep -c "isIntelMac" .planning/IPC-PORT-INVENTORY.md` prints `0` in the Phase 34.1 channel list (a dated note mentioning the retired channel name by way of explanation is acceptable — verify by reading which line matched).
- `34.1-PORTED-CHANNELS.md`'s `isIntelMac` row still exists and now carries a `2026-08-27` / `34.18` retirement marker; the row's pipe count is unchanged.
- `grep -rn "isIntelMac" src/ | wc -l` prints `0` — the code change this documents actually landed (plan 34.18-05).
- `grep -c "7 invoke + 12 send" src/backend/sidecar/appShellFlowRegistration.ts` is at least `1`.
- `git diff --name-only` lists only files under `.planning/`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -q "Amendment 2026-08-27 (Phase 34.18)" .planning/REQUIREMENTS.md && grep -q "32 channels" .planning/IPC-PORT-INVENTORY.md && test "$(grep -rn isIntelMac src/ | wc -l | tr -d " ")" = "0" && echo OK'</automated>
  </verify>
  <done>REQ-34.1-05 is amended not rewritten, both channel counts moved by one for a stated reason, and the docs deliberately left alone are named in the amendment.</done>
</task>

<task type="auto">
  <name>Task 2: Dated dispositions for the two findings and the six requirements</name>
  <read_first>.planning/ROADMAP.md § Phase 34.16 closure section (:1448-1476) in full; .planning/REQUIREMENTS.md status-table rows :395-397 and :405-408, and detail bullets :894-896 and :1029-1032; .planning/phases/34.16-.../34.16-LIVE-GATE.md around the F-34.16-D and F-34.16-G entries (search for `F-34.16-D` and `F-34.16-G`) so you state each finding's CURRENT status correctly before disposing of it</read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
**Findings, as bullets appended to the ROADMAP's Phase 34.16 closure section** (never as headings):
  - `F-34.16-D` — dispose of its **x64 half ONLY**, dated 2026-08-27, as moot-by-retirement. State
    plainly that the arm64 half was already FIXED and proven live in CI (34.16-07/-08/-09) and is not
    what is being closed here. A disposition that reads as closing the whole finding would erase
    real evidence.
  - `F-34.16-G` — close as resolved-by-retirement, dated 2026-08-27. Its content (`macos-13` retired
    by GitHub 2025-12-04; the x64 leg never assigned a runner across three attempts; an empty
    `runner` AND an empty `group` after 25+ minutes is a label nothing serves) is the EVIDENCE for
    the retirement, so the disposition is "the leg this finding is about no longer exists", not "the
    finding was wrong".
Also amend the existing `- **F-34.16-D and F-34.16-G remain open/blocking against the phase, by
design.**` bullet with a dated follow-on sentence pointing at these dispositions, so a reader who
stops at that bullet is not left with a superseded statement.

**Requirements — update BOTH the status-table row and the detail bullet for each ID.** The table
rows are `| REQ-... | Phase ... | <status> |`; append a dated clause to the status cell. The detail
bullets get an appended sentence. Dispositions:
  - `REQ-34.9-02` (CI builds x64 and arm64) — the x64 clause becomes a NON-GOAL as of 2026-08-27;
    the arm64 clause is satisfied by Phase 34.18's arm64-only workflow. Cite ledger item 1.
  - `REQ-34.9-03` (downloader sources darwin archives) — becomes arm64-only; the code path exists
    and its live proof is plan 34.18-07's gate. Cite ledger item 2.
  - `REQ-34.9-04` (sha256 digest verification) — becomes arm64-only, same live proof. Cite item 3.
  - `REQ-34.16-01` (guard covers x64 as well as arm64) — **PARTIAL**: the arm64 half was ACHIEVED and
    remains in force; the x64 half is a non-goal as of 2026-08-27. Say which part, in those terms.
    Note that Phase 34.18 plan 34.18-04 removed the `--arch=x64` invocation and RED-proved the
    surviving one-arch guard strict against a known-bad tree, so the achieved half is not merely
    assumed to have survived the reduction.
  - `REQ-34.16-02` (guard actually exercised in CI) — **PARTIAL**, same shape. Its remaining half
    depends on ledger item 13, which plan 34.18-07's gate discharges; do not mark it complete here.
  - `REQ-34.16-04` (`pin:runner-digests` fail-closed over both arches) — **SUPERSEDED by
    REQ-34.18-03**, dated 2026-08-27. State the mechanism: it fetched `SHA256SUMS-x64`, which was
    never published, so the tool was un-runnable; 34.18 reduced it to one arch and replaced the
    now-vacuous two-manifest `runId` equality check with an explicit non-null assertion rather than
    dropping it.

Every disposition carries the date `2026-08-27` and names Phase 34.18. None is written as a heading.
  </action>
  <acceptance_criteria>
- Each of the six requirement IDs has a `2026-08-27` dated disposition in BOTH its status-table row and its detail bullet: verify per ID with `grep -n "REQ-34.9-02" .planning/REQUIREMENTS.md` etc. and read both matches.
- The REQ-34.16-01 and REQ-34.16-02 dispositions each contain an explicit which-part statement (the words "arm64 half" and "x64" both appear in each).
- The REQ-34.16-04 disposition contains `REQ-34.18-03` and the word `superseded` (any case).
- `.planning/ROADMAP.md` § Phase 34.16 contains dated `2026-08-27` dispositions for `F-34.16-D` and `F-34.16-G`, and the pre-existing "remain open/blocking ... by design" bullet carries a follow-on pointer.
- The F-34.16-D disposition explicitly limits itself to the x64 half: the words "arm64 half" and one of "FIXED"/"proven live" both appear in it.
- `grep -c "^### F-34.16" .planning/ROADMAP.md` prints `0` — no disposition became a heading.
- `git diff --name-only` lists only `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.
- `git diff .planning/REQUIREMENTS.md | grep -c "^-"` accounts only for lines you intentionally rewrote (status-cell edits); no requirement's core text was deleted.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'for id in REQ-34.9-02 REQ-34.9-03 REQ-34.9-04 REQ-34.16-01 REQ-34.16-02 REQ-34.16-04; do grep -q "$id.*2026-08-27" .planning/REQUIREMENTS.md || { echo "MISSING dated disposition: $id"; exit 1; }; done; test "$(grep -c "^### F-34.16" .planning/ROADMAP.md)" = "0" && echo OK'</automated>
  </verify>
  <done>Both findings and all six requirements carry dated 2026-08-27 dispositions that say which part became a non-goal, none written as a heading, and the ROADMAP's superseded "remain open by design" bullet points forward.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger dispositions — close 1, 2, 3 and 12; carry 13 forward; cross-reference 18 without re-closing it</name>
  <read_first>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md items 1 (:16-41), 2 (:42-60), 3 (:61-76), 12 (:480-500), 13 (:501-518) and 18 (:581-668) IN FULL — including item 18's existing CLOSED 2026-08-25 paragraph and its three qualifications, because qualification 2 is what makes this a cross-reference and not a closure</read_first>
  <files>.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md</files>
  <action>
Append ONE bold-labelled paragraph beneath each item's existing text, in the exact shape of that
file's own `**ROUTED 2026-08-22:**` paragraphs (bold label, date, prose, and an explicit statement of
what the new line supersedes). Never delete a prior line. Never convert an item's `### N. Title`
heading into anything else, and never renumber.

  - **Items 1, 2 and 3** — `**CLOSED 2026-08-27 (Phase 34.18):**`. Each currently reads
    `ROUTED 2026-08-22: now owned by Phase 34.16`, and Phase 34.16 closed PARTIAL on 2026-08-27
    without any of the three being updated — the exact "a routing record rots silently once its
    owner closes" shape. Close them directly rather than adding a second routing hop: cite 34.16's
    partial achievement (the arm64 half landed and passed live) and 34.18's completion of the
    arm64-only path, and state that the x64 half is a non-goal as of 2026-08-27, not an unmet
    obligation. Each paragraph must say it supersedes only the routing, leaving the OWNER text and
    the item's original body standing.
  - **Item 12** ("The wired guard covers arm64 only") — `**CLOSED 2026-08-27 (Phase 34.18):**`
    resolved-by-retirement. arm64-only is now the WHOLE scope, so "covers arm64 only" has stopped
    being a gap and become the specification. Cite plan 34.18-04's RED-proof that the surviving
    one-arch guard is strict against a known-bad tree — otherwise this closure reads as "we
    redefined the problem away" rather than "we measured the remaining half".
  - **Item 13** ("The guard has never been exercised in CI") — `**CARRIED FORWARD 2026-08-27
    (Phase 34.18):**`, NOT closed. Its mechanism is unchanged: `.github/actions/install-deps`'s last
    step is `pnpm download-helper-binaries`, which throws on the `PENDING-CI-PUBLISH` sentinels, so
    CI never reaches the guard. Three arm64 sentinels still stand when this plan runs. Point at plan
    34.18-07's blocking developer gate as the discharge, and state that item 13 closes there and not
    before. Closing it here would be a false record.
  - **Item 18 / C2-05** — `**CROSS-REFERENCE 2026-08-27 (Phase 34.18):**`, and NOTHING that reads as
    a closure. Note that Phase 34.18 removed the x64 macOS artifact entirely, so the
    `release:mac -p always` -> `electron-updater` path this item names is closed by construction; and
    then state the important part: **this item is already `CLOSED 2026-08-25` (quick task
    `260825-alv`, commit `adc648eb6`), and its residual was re-homed to items 12 and 13 by
    qualification 2 of that closure — so 34.18's disposition of the residual is items 12's and 13's
    entries above, not a second closure of item 18.** Also record that the ROADMAP's Phase 34.18
    disposition list, which names item 18 as an open item, is STALE on that point, so a later reader
    comparing the two does not conclude the ledger was missed.

Do not add a `**CLOSED**` label to item 18. Do not add any `### <ID>` heading anywhere.
  </action>
  <acceptance_criteria>
- `grep -c "2026-08-27 (Phase 34.18)" .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md` prints `6`.
- Items 1, 2, 3 and 12 each carry a `**CLOSED 2026-08-27 (Phase 34.18):**` label; item 13 carries `**CARRIED FORWARD 2026-08-27 (Phase 34.18):**`; item 18 carries `**CROSS-REFERENCE 2026-08-27 (Phase 34.18):**`. Verify each by reading its item block, not by a global count alone.
- Item 18's block contains NO second `**CLOSED 2026-08-27` label: `awk '/^### 18\./,/^### 19\./' <file> | grep -c "CLOSED 2026-08-27"` prints `0`, while `grep -c "CLOSED 2026-08-25" ` over the same range still prints at least `1` (the original closure survives).
- Item 18's new paragraph names both `items 12` and `13` and the words `already` and `stale`.
- Item 13's new paragraph names `34.18-07` and does NOT contain the word `CLOSED`.
- All six pre-existing `**ROUTED 2026-08-22:**` lines still present: `grep -c "ROUTED 2026-08-22" <file>` is unchanged from its pre-edit value.
- `grep -c "^### " <file>` is unchanged from its pre-edit value — no heading added, removed or renumbered.
- `git diff <file> | grep -c "^-"` prints `1` (the diff header only) — this task is append-only.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'F=.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md; test "$(grep -c "2026-08-27 (Phase 34.18)" $F)" = "6" || { echo "expected 6 dispositions"; exit 1; }; test "$(awk "/^### 18\./,/^### 19\./" $F | grep -c "CLOSED 2026-08-27")" = "0" || { echo "item 18 was re-closed"; exit 1; }; echo OK'</automated>
  </verify>
  <done>Four items closed, one carried forward to the gate that actually discharges it, and the already-closed item cross-referenced without a contradictory second closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record -> future planner/verifier | `.planning/` is the input every later phase, gap cycle and verification run reads. A false or stale record here silently misdirects work that has no other source of truth. |
| closed-phase record -> audit | A closed phase's verification/summary documents are evidence of what was measured at close. Editing them retroactively destroys that evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.18-21 | Repudiation | `deferred-items.md` item 18 / C2-05 | mitigate | The ROADMAP lists item 18 as needing disposition, but the item is already `CLOSED 2026-08-25` with its residual re-homed to items 12/13. A second closure produces two contradictory closure records for one item, and a later auditor cannot tell which is authoritative. Mitigated by a cross-reference label with a machine-checkable criterion (`CLOSED 2026-08-27` must NOT appear inside item 18's range) and by recording that the ROADMAP is stale. |
| T-34.18-22 | Tampering | `REQ-34.1-05` and `34.1-VERIFICATION.md` | mitigate | Rewriting a closed phase's requirement text, or "fixing" its verification record's counts, falsifies what was actually verified at close. Mitigated by amend-never-rewrite (asserted as ADDED-lines-only in the diff) and by an explicit not-propagated list written INTO the amendment, so the omission is a recorded decision rather than an undetectable gap. |
| T-34.18-23 | Repudiation | ledger item 13 | mitigate | Closing item 13 in the same sweep as items 1/2/3/12 would record "the guard has been exercised in CI" while three sentinels still block `install-deps`. That is the "a closure gate can FORCE a false record" shape. Mitigated by a CARRIED FORWARD label pointing at plan 34.18-07, with a criterion asserting the word `CLOSED` does not appear in it. |
| T-34.18-24 | Tampering | `.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` under concurrent sessions | mitigate | These two files are edited by other sessions; `gsd-sdk` commit helpers stage the whole tree and `git commit --only` takes the working tree rather than the index, which has already absorbed a concurrent session's row once in this project. Mitigated by `git diff`-ing each path before committing and committing with explicit paths only. |
| T-34.18-SC | Tampering | npm/pip/cargo installs | accept | **This plan installs no packages and touches no code.** `git diff --name-only` containing nothing outside `.planning/` is an acceptance criterion. |
</threat_model>

<verification>
1. Six dated `2026-08-27 (Phase 34.18)` dispositions in `deferred-items.md`, with the correct label on each of items 1, 2, 3, 12, 13, 18.
2. Item 18's range contains no `CLOSED 2026-08-27` and still contains its original `CLOSED 2026-08-25`.
3. All six requirement IDs carry dated dispositions in both the status table and the detail bullet; REQ-34.16-01/-02 each name their arm64 half explicitly; REQ-34.16-04 names REQ-34.18-03 as its successor.
4. `F-34.16-D`'s disposition is limited to its x64 half in so many words; `F-34.16-G` is closed resolved-by-retirement.
5. `grep -c "^### F-34.16" .planning/ROADMAP.md` prints `0`; `grep -c "^### " deferred-items.md` unchanged.
6. `REQ-34.1-05`'s original text byte-unchanged; the amendment names `34.1-VERIFICATION.md`, the 34.2 records and `34.5-UAT.md` as deliberately not propagated.
7. IPC-PORT-INVENTORY 33 -> 32 with `isIntelMac` gone from the list; `34.1-PORTED-CHANNELS.md`'s row annotated, not deleted.
8. `git diff --name-only` contains nothing outside `.planning/`.
9. `grep -rn "isIntelMac" src/ | wc -l` prints `0` — this plan documents a change that actually landed.
</verification>

<success_criteria>
- Nothing superseded is left open by omission, and nothing already closed is closed twice.
- Item 13 is carried forward to plan 34.18-07 rather than pre-closed.
- Every disposition is a table row or bullet append, dated 2026-08-27, naming Phase 34.18.
- The propagation boundary is recorded in the amendment, naming the four documents deliberately left alone.
- No code, test or workflow file touched.
</success_criteria>

<output>
Create `.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-06-SUMMARY.md` when done. It MUST list, ID by ID, where each of the two findings, six requirements and six ledger items received its disposition, and state explicitly that item 18 was cross-referenced rather than re-closed.
</output>
