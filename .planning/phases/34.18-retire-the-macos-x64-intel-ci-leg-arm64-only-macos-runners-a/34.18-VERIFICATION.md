---
phase: 34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a
verified: 2026-08-27T19:50:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
---

# Phase 34.18: Retire the macOS x64 (Intel) CI leg — arm64-only macOS runners Verification Report

**Phase Goal:** Make GameLib's macOS support arm64-only, in every place that currently assumes two
architectures, and dispose of the requirements and findings that assumption left open. Phase 34.16
closed PARTIAL on 2026-08-27 with its x64 half proven unachievable; this phase acts on that result.

**Verified:** 2026-08-27T19:50Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | README discloses Intel Mac non-support, fact-only, no rationale leakage | ✓ VERIFIED | `README.md:82` contains the exact literal `GameLib will not support Intel Macs on macOS.` `grep -n "2027\|macos-13\|F-34.16-G" README.md` returns nothing. |
| 2 | macOS runner-onedir pipeline is single-arch (arm64), three archives not six | ✓ VERIFIED | `.github/workflows/build-runners-onedir-macos.yml` matrix has exactly one leg (`os: macos-14`, `arch: arm64`, lines 99-100). `meta/runnersOnedirDigests.json` has exactly 3 `digests` keys, all `_macOS_arm64_onedir.tar.gz`. `grep -rn '_macOS_x86_64_onedir' meta/` returns nothing. |
| 3 | `pin:runner-digests` is runnable single-arch and still fail-closed | ✓ VERIFIED | `meta/runnersOnedirDigests.json` shows real pinned data: `runId: "33047720816"`, three 64-char lowercase-hex digests (regex-confirmed `^[0-9a-f]{64}$` for all three), no `PENDING-CI-PUBLISH` remaining. |
| 4 | macOS build compiles/verifies/packages arm64 only, no `--x64` | ✓ VERIFIED | `package.json` `dist:mac`/`release:mac` each run exactly one `pnpm verify:runner-bundle build --arch=arm64` invocation; no `--arch=x64` anywhere in either script. `meta/verifyRunnerBundle.ts` remains arch-parameterised, `findDarwinBinRoot()` unchanged (as predicted, needing no functional edit). |
| 5 | The three `x64` non-goal categories survive the sweep, pinned by an automated test | ✓ VERIFIED | `meta/__tests__/x64NonGoalSurvivor.test.ts` run live: 3/3 pass — `electron-builder.yml` keeps all 7 `build/bin/x64/win32/` refs (2 inside `mac:`), `downloadHelperBinaries.ts` keeps all 6 linux/windows keys, `utils.ts` keeps the `x64Path` fallback. |
| 6 | `build-base.yml` upload-artifact globs match real electron-builder output | ✓ VERIFIED | All 5 `path:`/glob references in `.github/workflows/build-base.yml` now read `dist/GameLib*...`; zero `dist/Heroic*` remain (grep returned nothing). |
| 7 | `isIntelMac` does not exist anywhere in `src/`, all 8 branches collapsed | ✓ VERIFIED | `grep -rn "isIntelMac" src/` returns 0 matches across backend, frontend, preload, common. `isAppleSiliconMac` still exists in `src/backend/constants/environment.ts:36`. |
| 8 | Apple-Silicon/Rosetta surfaces (non-Intel) survive untouched; D-14 RED-proof outlives the constant | ✓ VERIFIED | `isAppleSiliconMac` retained as a positive probe (not `!isIntelMac`). `removeCopies.test.ts` contains a rewritten unsafe-negation RED-proof (`isAppleSiliconMac (host gate)` describe block, lines ~302-466) covering both the Intel-model and empty-cpu-model host cases. |
| 9 | Superseded requirements/findings/ledger items get dated dispositions, no double-closures | ✓ VERIFIED | `REQUIREMENTS.md` carries dated 2026-08-27 dispositions for REQ-34.9-02/-03/-04, REQ-34.16-01/-02/-04. Ledger item 18/C2-05 confirmed to carry only a `CROSS-REFERENCE, dated 2026-08-27` addendum (34.18-06-SUMMARY.md:67,88) — not a second `CLOSED` label. Items 1/2/3/12 closed, item 13 explicitly carried forward to REQ-34.18-10's gate. |
| 10 | Phase closes on real arm64 digests, not sentinels, developer-gated | ✓ VERIFIED | `34.18-LIVE-GATE.md` frontmatter `verdict: PASS`. 21 of 21 `**Observed:**` fields filled with substantive dated evidence (none empty/TBD). Digests independently re-derived (`shasum -a 256` for `legendary` matched `ac9fc18d…`). `electron-builder.yml`'s `REMOVE WHEN DIGESTS ARE PINNED` marker is gone (`grep` returns nothing). |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `README.md` | Verbatim Intel-drop disclosure | ✓ VERIFIED | Line 82, exact string, no rationale tokens |
| `.github/workflows/build-runners-onedir-macos.yml` | Single arm64 matrix leg | ✓ VERIFIED | One leg, macos-14/arm64 |
| `meta/runnersOnedirDigests.json` | 3 real digest keys, non-null runId | ✓ VERIFIED | 3 keys, `runId: "33047720816"`, all 64-char lowercase hex |
| `meta/downloadHelperBinaries.ts` | arm64-only onedir downloader, updated sentinel message | ✓ VERIFIED (via live gate + survivor test) | `pnpm download-helper-binaries` exited 0 on darwin per LIVE-GATE §3 |
| `meta/pinRunnerDigests.ts` | Single-arch, non-null runId assertion | ✓ VERIFIED (via digests output) | Real pinned data present, run recorded as `33047720816` |
| `package.json` | `dist:mac`/`release:mac` one guard invocation, no `--x64` | ✓ VERIFIED | Confirmed by direct read of scripts block |
| `electron-builder.yml` | One-arch `mac:` prose, marker removed | ✓ VERIFIED | No `REMOVE WHEN DIGESTS ARE PINNED` marker; 7 legitimate `x64/win32` refs remain |
| `.github/workflows/build-base.yml` | macOS leg `--arm64` only; all upload globs fixed | ✓ VERIFIED | `dist/GameLib*` throughout, no `dist/Heroic*` |
| `src/backend/constants/environment.ts` | `isIntelMac` removed, `isAppleSiliconMac` retained | ✓ VERIFIED | Direct read |
| `meta/__tests__/x64NonGoalSurvivor.test.ts` | Passes, pins the 3 non-goal categories | ✓ VERIFIED | Ran live: 3/3 pass |
| `src/backend/storeManagers/steam/__tests__/removeCopies.test.ts` | D-14 RED-proof rewritten inline, not deleted | ✓ VERIFIED | `isAppleSiliconMac (host gate)` describe block present with unsafe-negation cases |
| `34.18-LIVE-GATE.md` | 21/21 criteria, verdict PASS | ✓ VERIFIED | Frontmatter + all `Observed:` fields checked |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `package.json` scripts | `meta/verifyRunnerBundle.ts` | `pnpm verify:runner-bundle build --arch=arm64` | WIRED | Single invocation confirmed in both `dist:mac`/`release:mac` |
| `build-runners-onedir-macos.yml` | `meta/runnersOnedirDigests.json` | `pnpm pin:runner-digests` (developer-run) | WIRED | `runId` in digests file matches the dispatched run id recorded in `34.18-LIVE-GATE.md` §1 criterion 2 |
| `download-helper-binaries` | `public/bin/arm64/darwin` | live download against pinned digests | WIRED | LIVE-GATE §3: exit 0, all 3 runner trees present and each executable |
| `x64NonGoalSurvivor.test.ts` | `electron-builder.yml`/`downloadHelperBinaries.ts`/`utils.ts` | grep-based pin, mutation-proven | WIRED | Test passes live; plan text records it as RED-proven by mutation at authoring time |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|---|---|---|---|
| REQ-34.18-01 | 34.18-01 | ✓ SATISFIED | README literal string, no rationale tokens |
| REQ-34.18-02 | 34.18-02 | ✓ SATISFIED | Single matrix leg, 3 digest keys, arm64-only downloader |
| REQ-34.18-03 | 34.18-03 | ✓ SATISFIED | Real pinned digests + non-null runId in `meta/runnersOnedirDigests.json` |
| REQ-34.18-04 | 34.18-04 | ✓ SATISFIED | One guard invocation, no `--x64`, in both mac scripts |
| REQ-34.18-05 | 34.18-01 | ✓ SATISFIED | `x64NonGoalSurvivor.test.ts` 3/3 pass, live-run confirmed |
| REQ-34.18-06 | 34.18-04 | ✓ SATISFIED | All `build-base.yml` upload globs now `dist/GameLib*` |
| REQ-34.18-07 | 34.18-05 | ✓ SATISFIED | `isIntelMac` zero matches in `src/` |
| REQ-34.18-08 | 34.18-05 | ✓ SATISFIED | `isAppleSiliconMac` retained, RED-proof rewritten in `removeCopies.test.ts` |
| REQ-34.18-09 | 34.18-06 | ✓ SATISFIED | Dated dispositions present in REQUIREMENTS.md; item 18/C2-05 cross-referenced not re-closed |
| REQ-34.18-10 | 34.18-07 | ✓ SATISFIED | `34.18-LIVE-GATE.md` verdict PASS 21/21, real digests pinned, download-helper-binaries exits 0 |

All 10 declared REQ IDs (REQ-34.18-01..10) are present in `.planning/REQUIREMENTS.md` and traced to a
supporting plan. No orphaned requirement IDs found for this phase.

**Documentation-lag note (not a code gap):** `.planning/REQUIREMENTS.md`'s summary status column
(around line 413-421) still labels REQ-34.18-01 through -09 as `Planned` rather than `Complete`,
while the fuller checklist text below (lines ~1088-1096) and the codebase both show these are done.
Only REQ-34.18-10 carries `Complete` in the status column. This is bookkeeping drift in the
requirements table's summary line, not a gap in the underlying work — every piece of code evidence
above independently confirms REQ-34.18-01..09 are implemented and match their described shape. Flagged
for correction, not scored as a failure.

### Anti-Patterns Found

No debt markers (`TBD`/`FIXME`/`XXX`) were introduced by this phase's commits. Three files touched by
plan 34.18-05 (`src/backend/utils.ts`, `src/backend/tools/index.ts`, `src/backend/main.ts`) do contain
pre-existing `FIXME` comments (robocopy progress reporting, DXVK/VKD3D version handling, error
propagation) — confirmed via `git show 8e7af620c -- <file> | grep FIXME` to be absent from this
phase's diffs, i.e. inherited from upstream Heroic content, unrelated to the `isIntelMac` removal.
Not attributable to this phase.

### Test Suite Regression Check

Re-ran all five jest projects live against the current HEAD and compared to the VALIDATION.md-recorded
measured baseline:

| Project | Baseline | Measured (this verification) | Match |
|---|---|---|---|
| Backend | 4271/4266 pass/3 fail/2 skip | 4271/4266/3 fail/2 skip | ✓ exact match |
| Meta | 605/603 pass/1 fail/1 skip | 605/603/1 fail/1 skip | ✓ exact match |
| Common | 41/41 | 41/41 | ✓ exact match |
| Preload | 140/140 | 140/140 | ✓ exact match |
| Frontend | 2059/2059 | 2059/2059 | ✓ exact match |

The 3 Backend failures (`decompressPool.test.ts` native-LZMA) and 1 Meta failure
(`genI18nGateScope.test.ts` A-17 snapshot drift) are the same pre-existing, documented,
phase-independent failures recorded in `deferred-items.md` — reproduced by this verification with
matching failure messages, not new regressions introduced by 34.18.

Also specifically re-ran the cross-project reader `src/backend/__tests__/runnersOnedirWorkflow.test.ts`
(the file the VALIDATION.md correction flagged as a fail-open blind spot for plan 34.18-02) —
40/40 pass, confirming the fix landed in commit `ba9964d44` holds at current HEAD.

### Human Verification Required

None. All ten must-haves are grep/file/test verifiable, and REQ-34.18-10 (the only manual-only
requirement by contract) was already discharged by the developer's live gate run — `34.18-LIVE-GATE.md`
records the developer's own dispatch of `build-runners-onedir-macos.yml` and manual digest pin, with
21/21 `Observed:` fields filled. Verifying this phase requires no additional human action.

### Gaps Summary

No gaps found. All 10 must-haves (REQ-34.18-01..10, corresponding to every ROADMAP success condition
for arm64-only macOS support) are verified directly against the codebase, not against SUMMARY claims:
grep counts, live jest runs, and direct file reads all corroborate the phase's own record. The two
live-gate criteria recorded as "written-vs-property mismatches" (§1/4 `runnerName`/`runnerGroupName`
null-for-both-jobs landmark; §3/6 `find public/bin` over-scoped) were independently reviewed and judged
honestly handled — both are PASS on the underlying property, both are explicitly flagged in
`34.18-LIVE-GATE.md` for rewording rather than silently re-scoped or hidden, and neither affects the
truth they exist to support. The `REQUIREMENTS.md` status-column lag for REQ-34.18-01..09 (labelled
`Planned` while the checklist text and code both show `Complete`) is noted as a documentation
correction item, not a phase-goal gap.

---

_Verified: 2026-08-27T19:50Z_
_Verifier: Claude (gsd-verifier)_
