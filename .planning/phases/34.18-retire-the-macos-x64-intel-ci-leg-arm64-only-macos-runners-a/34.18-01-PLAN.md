---
phase: 34.18-retire-the-macos-x64-intel-ci-leg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - meta/__tests__/readmeDisclosure.test.ts
  - meta/__tests__/x64NonGoalSurvivor.test.ts
autonomous: true
requirements:
  - REQ-34.18-01
  - REQ-34.18-05
decisions: [D-01, D-02, D-03, D-07, D-08]

must_haves:
  truths:
    - "D-01, D-02: A reader of `README.md` § `## Supported Operating Systems` encounters the verbatim sentence `GameLib will not support Intel Macs on macOS`. The literal is the requirement, not a paraphrase of it — the gate is a literal-string assertion, which is the whole reason D-02 specified a sentence instead of an intent"
    - "D-03: That text states the fact and nothing else. GitHub's Fall 2027 x86_64 EOL, `macos-13`'s retirement and `F-34.16-G` appear NOWHERE in `README.md` — the runner situation is the project's problem, not the reader's, and rationale belongs in the phase record and the commit body"
    - "D-01: `README.md` is the only surface touched. In-app Settings, `CONTRIBUTING.md` and `CHANGELOG.md` are NOT edited — an Intel user cannot run an arm64 build at all, so an in-app notice reaches nobody who needs it, and `CHANGELOG.md` is stale upstream Heroic content"
    - "D-02: The disclosure gate was proven RED against the pre-fix `README.md` BEFORE the edit landed, and green after. A gate written after the edit proves only that the file contains what was just written"
    - "D-07, D-08: The three `x64` categories that are explicit NON-goals still exist after this phase's sweeps — 7 `build/bin/x64/win32/` references in `electron-builder.yml`, all 6 linux/windows download keys in `meta/downloadHelperBinaries.ts`, and the `x64Path` runtime fallback in `src/backend/utils.ts`. A bare `x64` grep hit is NOT evidence of an incomplete sweep"
    - "D-08: That survivor gate is load-bearing, not coincidental — proven by mutation, because unlike every other gate in this phase its known-bad input is not HEAD. Deleting one reference from a scratch copy makes it FAIL"
  artifacts:
    - path: "README.md"
      provides: "the user-facing Intel-drop disclosure in the OS-support list"
      contains: "GameLib will not support Intel Macs on macOS"
    - path: "meta/__tests__/readmeDisclosure.test.ts"
      provides: "literal-string gate on the D-02 disclosure sentence"
      contains: "GameLib will not support Intel Macs on macOS"
    - path: "meta/__tests__/x64NonGoalSurvivor.test.ts"
      provides: "the over-reach direction of the x64 boundary — pins the 3 non-goal categories"
      contains: "build/bin/x64/win32/"
  key_links:
    - from: "meta/__tests__/readmeDisclosure.test.ts"
      to: "README.md"
      via: "readFileSync of the repo-root README, asserted with toContain on the literal"
      pattern: "GameLib will not support Intel Macs on macOS"
    - from: "meta/__tests__/x64NonGoalSurvivor.test.ts"
      to: "electron-builder.yml, meta/downloadHelperBinaries.ts, src/backend/utils.ts"
      via: "readFileSync + exact-count / exact-literal assertions over the three non-goal categories"
      pattern: "toHaveLength\\(7\\)"
---

<objective>
Land the ROADMAP's named non-negotiable — the user-facing statement that GameLib will not support
Intel Macs — and the survivor gate that bounds every later sweep in this phase from the over-reach
direction.

Purpose: the ROADMAP says the phase "is not complete until that sentence exists somewhere a user or
contributor will actually encounter it", and no inherited requirement covered it. Separately, this
phase deletes `x64` in three later plans; `x64` is overloaded four ways in this repo and only ONE
of them retires. This project's record is that a sweep audited only for *under*-reach misses
*over*-reach entirely, so the survivor gate must exist BEFORE the sweeps, not after.

Output: `README.md` carries the verbatim disclosure; two new `meta/` jest gates exist, each proven
against its own known-bad input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-CONTEXT.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-PATTERNS.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-VALIDATION.md

<interfaces>
<!-- Extracted at plan time (2026-08-27, HEAD). The executor needs no exploration for these. -->

`README.md:76-82` at HEAD, verbatim:

    - SteamOS (downloading using Discover only)
    - Windows 10 & 11
    - macOS 14 or newer

Line 82 is `- macOS 14 or newer`. `README.md` contains NO other mention of Intel, x64 or arm64
macOS support — the disclosure has exactly one home.

jest project layout (root `jest.config.js`): five projects, `displayName`s are `Backend`,
`Frontend`, `Common`, `Preload` and **`Meta`**. `meta/jest.config.js` sets `rootDir: '..'`,
`roots: ['<rootDir>/meta']`, `testMatch: ['**/__tests__/**/*.test.ts']`. `--selectProjects` is
CASE-SENSITIVE: `--selectProjects meta` matches nothing and EXITS 0. Use `Meta`.

Path depth: `meta/__tests__/*.test.ts` sits two levels below the repo root, so
`join(__dirname, '..', '..')` is the repo root. `meta/__tests__/buildRunnersOnedir.test.ts` reads a
sibling with `readFileSync(join(__dirname, '..', 'downloadHelperBinaries.ts'), ...)` — same idiom,
one hop shallower.

Non-goal counts measured at HEAD (2026-08-27):
  `grep -c "build/bin/x64/win32/" electron-builder.yml`  -> 7
    (lines 40, 41, 42 in `win.files`; 84, 85 inside the `mac:` `files:` block; 121, 122 in
     `linux.files`. The two inside the `mac:` block are the point — the macOS package legitimately
     bundles win32 x64 GOG Galaxy / Epic helpers under an `x64` path key.)
  `meta/downloadHelperBinaries.ts` linux/windows keys, 6 exact literals:
    'legendary_linux_x64' (:311), 'legendary_windows_x64.exe' (:312),
    'gogdl_linux_x86_64' (:336), 'gogdl_windows_x86_64.exe' (:337),
    'nile_linux_x86_64' (:354), 'nile_windows_x86_64.exe' (:355)
  `src/backend/utils.ts:553` -> `const x64Path = join(publicDir, 'bin', 'x64', process.platform, ...segments)`

Pre-existing PARTIAL pin, so you do not mistake this test for a duplicate:
`meta/__tests__/downloadHelperBinaries.test.ts` already has an `it.each([...])('still contains the
literal %s')` block covering the six linux/win32 download keys against its own source read. That
covers ONE of D-08's three categories, against ONE file, and it lives in a test file plan 34.18-02
edits heavily. The new file is the consolidated D-08 gate across all three categories and is
deliberately owned by a different plan than the sweeps it bounds. Do NOT remove or relax the
existing block; do NOT skip category 2 here because that block exists.

Existing vacuity idioms to imitate (do not invent a new one):
  `meta/__tests__/verifyRunnerBundle.test.ts:364-394` — "vacuity control"
  `meta/__tests__/buildRunnersOnedir.test.ts:765-774` — "vacuity guard: different archiveSha256
  produces different output" (mutate one input, assert the output actually changes)
</interfaces>
</context>

<constraints>
- **D-02's literal is exact.** `GameLib will not support Intel Macs on macOS` — that capitalisation,
  that wording, no leading "Note:", no substitution of "macOS" for "Mac OS". The gate is a
  `toContain` on this string; any drift fails it, which is the intent.
- **D-03 is a prohibition.** No rationale in `README.md`. Do not add "because GitHub retired
  `macos-13`", do not link the phase, do not mention Fall 2027. The commit body is where the
  rationale goes.
- **D-01 is a scope fence.** Do not edit in-app Settings strings, `CONTRIBUTING.md` or
  `CHANGELOG.md`. They were considered and declined.
- **Do not touch `public/locales/en/gamelib.json` or `translation.json`.** This disclosure is a
  README change, not a UI string. A new key in `translation.json` fails the i18n churn guard in CI.
- **The survivor test asserts NON-goals.** It must never be "fixed" by deleting a reference it
  reports. If it goes red in a later plan, the later plan over-reached — that is the gate working.
- `prettier` uses `proseWrap: preserve`, so a long README line is not rewrapped. Run
  `pnpm prettier` **in place** (never against a temp copy — a copy resolves a different config).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the D-02 disclosure gate and prove it RED against the pre-fix README</name>
  <read_first>README.md lines 70-90; meta/__tests__/verifyRunnerBundle.test.ts:364-394 (the "vacuity control" idiom); meta/__tests__/buildRunnersOnedir.test.ts top-of-file (the `readFileSync(join(__dirname, '..', ...))` path idiom); meta/jest.config.js</read_first>
  <files>meta/__tests__/readmeDisclosure.test.ts</files>
  <action>
Create `meta/__tests__/readmeDisclosure.test.ts` with a single `describe` block named for D-02. It
reads the repo-root `README.md` with `readFileSync(join(__dirname, '..', '..', 'README.md'), 'utf8')`
and asserts, with `toContain`, the exact literal `GameLib will not support Intel Macs on macOS`.

Add a second assertion in the same file enforcing D-03's prohibition: the README must NOT match
`/macos-13|Fall 2027|F-34\.16-G/i`. This is the fact-only clause made mechanical — a later
well-meaning edit that "explains why" fails here rather than shipping.

Include a top-of-file docstring stating that the string is normative per D-02, that the reason it is
a literal rather than an intent is so a grep can close the requirement, and that the assertion was
RED-proven against HEAD's `README.md` (which lacks it) before the edit in Task 2.

**Run the test BEFORE Task 2's edit and record the RED output in the summary.** The disclosure
assertion must fail; the D-03 prohibition assertion must already pass (HEAD's README mentions none
of those three tokens). A gate written after the edit proves only that the file contains what was
just written.
  </action>
  <acceptance_criteria>
- `meta/__tests__/readmeDisclosure.test.ts` exists and contains the literal `GameLib will not support Intel Macs on macOS` exactly once as the asserted string.
- Run before Task 2: `pnpm test --selectProjects Meta -t "disclosure"` exits NON-ZERO, and its output names the missing disclosure assertion. This RED output is quoted verbatim in the plan summary.
- The D-03 prohibition assertion passes in that same RED run (it is asserting an absence that already holds).
- The command uses `Meta`, not `meta` — a lowercase project name matches nothing and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --selectProjects Meta -t "disclosure"</automated>
  </verify>
  <done>The gate file exists and has been observed FAILING against HEAD's README for the reason it is meant to fail.</done>
</task>

<task type="auto">
  <name>Task 2: Amend README.md's Supported Operating Systems list with the verbatim disclosure</name>
  <read_first>README.md lines 70-90 (the `## Supported Operating Systems` list); .planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-CONTEXT.md D-01/D-02/D-03</read_first>
  <files>README.md</files>
  <action>
Replace `README.md:82`, currently the single line `- macOS 14 or newer`, with:

    - macOS 14 or newer (Apple Silicon only). GameLib will not support Intel Macs on macOS.

That is the whole edit. The parenthetical states the positive requirement; the sentence is D-02's
literal, unmodified. No rationale clause, no link, no footnote (D-03). No other line of `README.md`
changes, and no other file changes.

Then re-run Task 1's gate and observe GREEN. Then run `pnpm prettier` **in place** on the repo and
confirm `README.md` is not reported — a markdown change is the one kind of edit most likely to trip
the prettier gate, and `pnpm codecheck` (tsc) says nothing about it.

Commit body (not the README) carries the rationale per D-03: `macos-13` was retired by GitHub on
2025-12-04, the x64 leg was never assigned a runner across three attempts (`F-34.16-G`), and GitHub
ends x86_64 macOS support in Fall 2027 — so this is a product decision, not a temporary workaround.
  </action>
  <acceptance_criteria>
- `grep -F -c "GameLib will not support Intel Macs on macOS" README.md` prints `1`.
- `grep -n "macOS 14 or newer" README.md` shows the amended line inside the `## Supported Operating Systems` list, and `git diff --stat README.md` shows exactly 1 file, 1 insertion, 1 deletion.
- `grep -Eic "macos-13|Fall 2027|F-34\.16-G" README.md` prints `0`.
- `pnpm test --selectProjects Meta -t "disclosure"` exits 0.
- `pnpm prettier` does not list `README.md` among its findings.
- `git diff --name-only` for this task lists `README.md` and nothing else.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --selectProjects Meta -t "disclosure" && grep -F -c "GameLib will not support Intel Macs on macOS" README.md</automated>
  </verify>
  <done>The verbatim disclosure is in the OS-support list, the gate that was red in Task 1 is green, and no rationale leaked into user-facing text.</done>
</task>

<task type="auto">
  <name>Task 3: Write the D-08 non-goal survivor gate and prove it load-bearing by mutation</name>
  <read_first>electron-builder.yml (all 7 `build/bin/x64/win32/` sites — lines 40-42, 84-85, 121-122); meta/downloadHelperBinaries.ts:300-365 (the linux/win32 key tables); src/backend/utils.ts:545-560 (the `x64Path` fallback); meta/__tests__/buildRunnersOnedir.test.ts:765-774 (the "vacuity guard" idiom to imitate); 34.18-CONTEXT.md D-07's four-category table</read_first>
  <files>meta/__tests__/x64NonGoalSurvivor.test.ts</files>
  <action>
Create `meta/__tests__/x64NonGoalSurvivor.test.ts`. One `describe` block, three `it` blocks, one per
D-07 non-goal category. All three read files from the repo root via
`readFileSync(join(__dirname, '..', '..', <path>), 'utf8')`.

1. **win32 helper path key.** Assert `electron-builder.yml` still contains exactly **7** occurrences
   of the literal `build/bin/x64/win32/`. In the same `it`, assert that at least one of them is
   inside the `mac:` `files:` block — slice the file from the `mac:` key to the `dmg:` key and
   assert the slice contains `build/bin/x64/win32/GalaxyCommunication.exe` and
   `build/bin/x64/win32/EpicGamesLauncher.exe`. This second assertion is the one that matters: the
   macOS package legitimately bundles win32 x64 helpers, and a sweeper who "cleaned the mac block"
   would pass a bare count while breaking Windows-guest helper resolution on macOS.

2. **linux/windows download keys.** Assert `meta/downloadHelperBinaries.ts` still contains all six
   exact literals: `legendary_linux_x64`, `legendary_windows_x64.exe`, `gogdl_linux_x86_64`,
   `gogdl_windows_x86_64.exe`, `nile_linux_x86_64`, `nile_windows_x86_64.exe`. Assert each literal
   individually, not a regex count — a count over `_x64|_x86_64` also matches the `comet-*` keys and
   the darwin keys this phase removes, so it would drift with unrelated edits and stop measuring the
   property it names.

3. **runtime x64 fallback.** Assert `src/backend/utils.ts` still contains the identifier `x64Path`
   and the expression `join(publicDir, 'bin', 'x64', process.platform`. This is the existence-checked
   box64 affordance for Linux ARM; CONTEXT's Deferred Ideas records that removing its darwin arm was
   offered and DECLINED — the branch is inert, not harmful.

Top-of-file docstring: state that this file asserts NON-goals, that it exists because a sweep
audited only for under-reach misses over-reach entirely, and that if it goes red the correct
response is to revert the over-reaching edit, never to relax the assertion.

**RED-proof (mandatory, and different in kind from the other gates in this phase).** The known-bad
input here is NOT HEAD — HEAD already has the right counts. Prove the assertions are load-bearing by
mutation, following `buildRunnersOnedir.test.ts:765`'s vacuity-guard technique: copy
`electron-builder.yml` to a scratch path, delete ONE `build/bin/x64/win32/` line from the copy, point
the test at the copy (a temporary local edit), confirm the count assertion FAILS, then restore and
re-confirm GREEN. Repeat for one of the six download keys. Record both mutation observations in the
summary. Do the mutation as a plan-time verification step — do NOT commit a second permanent test.

**Restore the mutated file by `cp` from a backup you took first, then confirm with `shasum -a 256`.
NEVER `git checkout -- <path>`** — it fires `.husky/post-checkout`, whose `pnpm
download-helper-binaries` step throws on the sentinel digests. Never `git stash`, never `git reset`.
  </action>
  <acceptance_criteria>
- `meta/__tests__/x64NonGoalSurvivor.test.ts` exists with exactly three `it` blocks, one per D-07 non-goal category.
- `pnpm test --selectProjects Meta -t "non-goal"` exits 0 against HEAD.
- Mutation proof 1 recorded: with one `build/bin/x64/win32/` line deleted from a scratch copy, the count assertion FAILS naming 6 instead of 7.
- Mutation proof 2 recorded: with `gogdl_windows_x86_64.exe` removed from a scratch copy, that literal's assertion FAILS.
- After both mutations, `shasum -a 256 electron-builder.yml meta/downloadHelperBinaries.ts` matches the pre-mutation values recorded at the start of the task.
- `git status --porcelain` shows only the new test file as changed by this task.
- No `git checkout --`, `git stash` or `git reset` appears anywhere in the task's shell history.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --selectProjects Meta -t "non-goal"</automated>
  </verify>
  <done>The three non-goal categories are pinned by an automated gate that has been demonstrated to fail when any of them is eroded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> packaged macOS `.app` | `electron-builder.yml`'s `mac:` `files:` block decides which binaries are signed into and shipped inside the app bundle. An edit here changes what executes on a user's machine. |
| repo -> developer/CI checkout | `meta/downloadHelperBinaries.ts`'s key tables decide which remote assets are fetched onto build machines. |
| project -> reader | `README.md` is the only surface in this plan that a non-contributor reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.18-01 | Denial of Service | `electron-builder.yml` `mac:` `files:` — `build/bin/x64/win32/*.exe` | mitigate | Over-reach in a later plan's `x64` sweep would drop the win32 GOG Galaxy / Epic helpers from the macOS bundle, silently breaking Windows-guest helper resolution at runtime with a green build. Task 3 pins the count at 7 AND pins both `mac:`-block literals by name, and is mutation-proven. |
| T-34.18-02 | Tampering | `meta/downloadHelperBinaries.ts` linux/windows key table | mitigate | An over-reaching sweep that removed a linux/windows key would make CI silently fetch nothing for that platform. Task 3 pins all six literals individually rather than by regex count, so the assertion cannot drift with unrelated `x64` edits. |
| T-34.18-03 | Repudiation | `README.md` disclosure | accept | A documentation string crosses no trust boundary and grants no capability. The residual risk is a later edit paraphrasing it away; Task 1's literal gate plus the D-03 prohibition assertion cover both directions. |
| T-34.18-SC | Tampering | npm/pip/cargo installs | accept | **This plan installs no packages.** No `package.json` dependency block is touched and no lockfile changes, so the Package Legitimacy Gate has an empty population. `git diff --name-only` listing only the three files in `files_modified` is the check. |
</threat_model>

<verification>
1. `pnpm test --selectProjects Meta` — full Meta project green (note the capital M; `meta` matches nothing and exits 0).
2. `grep -F -c "GameLib will not support Intel Macs on macOS" README.md` prints `1`.
3. `grep -Eic "macos-13|Fall 2027|F-34\.16-G" README.md` prints `0`.
4. `pnpm prettier` does not list `README.md`.
5. Both RED/mutation observations (Task 1's failing run, Task 3's two mutations) are quoted in the plan summary. A summary that reports only green runs has not discharged this plan.
6. `git diff --name-only <base>..HEAD` lists exactly `README.md`, `meta/__tests__/readmeDisclosure.test.ts`, `meta/__tests__/x64NonGoalSurvivor.test.ts`.
</verification>

<success_criteria>
- `README.md` carries the verbatim D-02 sentence in `## Supported Operating Systems`, with no rationale (D-03) and no other surface touched (D-01).
- The disclosure gate was observed RED before the edit and GREEN after.
- The D-08 survivor gate is committed, green, and mutation-proven in both of the two categories with countable references.
- No package installs, no i18n catalog changes, no `git checkout --`/`stash`/`reset`.
</success_criteria>

<output>
Create `.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-01-SUMMARY.md` when done. It MUST quote the Task 1 RED run and both Task 3 mutation failures verbatim.
</output>
