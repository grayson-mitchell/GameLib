---
phase: 34.18-retire-the-macos-x64-intel-ci-leg
plan: 04
type: execute
wave: 3
depends_on:
  - 34.18-02
files_modified:
  - package.json
  - electron-builder.yml
  - .github/workflows/build-base.yml
autonomous: true
requirements:
  - REQ-34.18-04
  - REQ-34.18-06
decisions: [D-07, D-09]

must_haves:
  truths:
    - "D-07: `pnpm dist:mac` and `pnpm release:mac` each run the runner-bundle guard exactly ONCE, for arm64, and pass no `--x64` flag to `electron-builder`. `release:mac`'s `-p always` publishes to the GitHub releases feed that `electron-updater` consumes, so removing the x64 artifact closes that exposure by construction rather than by a guard that has to keep working"
    - "The one-arch guard is proven STRICT, not merely green. `meta/verifyRunnerBundle.ts` needs no functional change — `findDarwinBinRoot()` throws when `build/bin/${arch}/darwin` is absent and `main()` returns `summary.ok ? 0 : 1` over a fixed three-runner list — but that conclusion is discharged by RED-proving the single-arch invocation against a known-bad tree, not by reading the source"
    - "D-07: `electron-builder.yml`'s `mac:` coverage prose describes one arch and three sentinels. That prose is stale-by-behaviour on exactly this commit; deferring it is what this project's own constraint forbids"
    - "D-09: Every upload glob in `.github/workflows/build-base.yml` matches a file `electron-builder` actually produces. `productName` is `GameLib`, so all six `dist/Heroic*` globs match nothing today under `if-no-files-found: error` — on Windows and Linux too, not only macOS"
    - "D-09 attribution isolation: the four non-macOS glob fixes land in their OWN commit, separate from the macOS-arch commit, so a `build-base.yml` breakage can be reverted on one side without touching the other. The operator accepted this caveat when overriding the recommendation to defer Windows/Linux"
    - "D-07 boundary: the `--x64` flags in `release:win`, `sign:win`, `build-base.yml`'s `dist:win portable --x64` and `dist:linux AppImage --x64 --arm64` all SURVIVE. They are Windows/Linux build arches, not the macOS build arch, and are explicit non-goals"
  artifacts:
    - path: "package.json"
      provides: "arm64-only macOS packaging scripts with a single guard invocation each"
      contains: "verify:runner-bundle build --arch=arm64 && electron-builder"
    - path: "electron-builder.yml"
      provides: "mac: coverage prose matching package.json's actual arch coverage"
      contains: "arm64"
    - path: ".github/workflows/build-base.yml"
      provides: "arm64-only macOS build leg and six product-name-correct upload globs"
      contains: "dist/GameLib"
  key_links:
    - from: "package.json:release:mac"
      to: "meta/verifyRunnerBundle.ts"
      via: "single pnpm verify:runner-bundle build --arch=arm64, &&-chained before electron-builder -p always"
      pattern: "verify:runner-bundle build --arch=arm64 && electron-builder -p always"
    - from: ".github/workflows/build-base.yml macOS leg"
      to: "package.json:dist:mac"
      via: "pnpm dist:mac --arm64 --publish=never"
      pattern: "dist:mac --arm64 --publish=never"
    - from: ".github/workflows/build-base.yml upload steps"
      to: "electron-builder.yml artifactName templates"
      via: "dist/GameLib* globs matching ${productName}-${version}-... outputs"
      pattern: "dist/GameLib\\*"
---

<objective>
Make a macOS build compile, verify and package arm64 only, make `electron-builder.yml`'s prose say
what is now true, and repair every upload glob in `build-base.yml` — with the non-macOS half
isolated in its own commit.

Purpose: `release:mac` chains `-p always` into the releases feed `electron-updater` consumes.
Removing `--x64` closes the "unverified x64 macOS build reaches real users' auto-update channel"
path by construction. Note the ROADMAP's stated mechanism for this is STALE — it says the guard is
"hardcoded to `--arch=arm64`", but `package.json:46,51` have run the guard for BOTH arches since
Phase 34.16. The exposure is real; its mechanism is not what the ROADMAP describes.

Output: two arm64-only packaging scripts, a truthful `mac:` comment block, a macOS CI leg that
builds one arch, and six globs that match real filenames — the last four in a separate, revertible
commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-CONTEXT.md
@.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-RESEARCH.md

<interfaces>
<!-- Extracted at plan time (2026-08-27, HEAD). -->

`package.json:46` and `:51`, verbatim:
  "release:mac": "pnpm clean:dist-mac && pnpm build-steam-bridge && electron-vite build && pnpm verify:runner-bundle build --arch=arm64 && pnpm verify:runner-bundle build --arch=x64 && electron-builder -p always --mac --x64 --arm64"
  "dist:mac":    "export CSC_IDENTITY_AUTO_DISCOVERY=false && pnpm clean:dist-mac && pnpm build-steam-bridge && electron-vite build && pnpm verify:runner-bundle build --arch=arm64 && pnpm verify:runner-bundle build --arch=x64 && electron-builder --mac"

`meta/verifyRunnerBundle.ts` has ZERO arch-specific logic. `grep -n "x64\|x86_64\|lipo" meta/verifyRunnerBundle.ts` matches only the two CLI usage strings (`:35`, `:521`). `findDarwinBinRoot()` (`:235-263`) THROWS naming root and arch when `build/bin/${arch}/darwin` is absent; `inspectRunnerTree()` iterates a fixed `RUNNERS = ['legendary','gogdl','nile']`; `main()` (`:561-600`) returns `summary.ok ? 0 : 1`. `meta/__tests__/verifyRunnerBundle.test.ts` is 626 lines with ZERO `x64` references — it is already arch-agnostic and needs no edit.

`meta/__tests__/verifyRunnerBundle.test.ts:605-622` — the C2-04 wiring pin. It reads
`scripts['dist:mac']`/`scripts['release:mac']` via `JSON.parse` (never raw-text grep, deliberately)
and asserts `indexOf('pnpm verify:runner-bundle') < indexOf('electron-builder')`. `indexOf` finds
the FIRST occurrence, so removing the second (x64) invocation leaves both tests green. Do not modify
this block.

`electron-builder.yml` — `productName: GameLib` (`:2`); mac `artifactName:
${productName}-${version}-macOS-${arch}.${ext}` (`:48`); the `mac:` prose comment block spans
`:55-80` and currently claims BOTH arches are guarded, describes `--arch=x64` as running
automatically, says "six PENDING-CI-PUBLISH sentinels", and carries the marker
`[REMOVE WHEN DIGESTS ARE PINNED -- 34.16-LIVE-GATE.md Scored Item 3]` at `:80`. The `mac:` `files:`
block at `:82-85` includes `build/bin/x64/win32/GalaxyCommunication.exe` and
`build/bin/x64/win32/EpicGamesLauncher.exe` — **D-07 NON-GOALS, do not touch.**
Linux artifactName `${productName}-${version}-linux-${arch}.${ext}` (`:102`); Windows portable
`${productName}-${version}-Portable-${arch}.${ext}` (`:45`).

`.github/workflows/build-base.yml` (95 lines) — matrix `:19-30`, four legs:
`windows-2025`/x64, `windows-11-arm`/arm64, `ubuntu-24.04`/x64, `macos-15`/x64. `matrix.arch` is
referenced NOWHERE in the steps — only `matrix.os` and `runner.os` are. Steps:
  `:41-43` `pnpm dist:win portable --x64` (**non-goal, keep**)
  `:44-46` `pnpm dist:win portable --arm64`
  `:47-53` `pnpm dist:mac --x64 --arm64 --publish=never`, `if: runner.os == 'macOS'`
  `:61-63` `pnpm dist:linux AppImage --x64 --arm64 --publish=never` (**non-goal, keep**)
  `:64-78` combined "Upload built version" step — `name:` and `path:` are `||`-chained ternaries over
    `matrix.os`, with a `macos-15` arm producing `'mac-x64'` / `'dist/Heroic*x64.dmg'`. NO `if:`.
  `:79-86` "Upload linux ARM version", `path: dist/Heroic*arm64.AppImage`, `if: runner.os == 'Linux'`
  `:87-95` "Upload macOS ARM version", `name: mac-arm64`, `path: dist/Heroic*arm64.dmg`,
    `if: runner.os == 'macOS'`
All upload steps carry `if-no-files-found: error`. `grep -rn "mac-x64" .github/` shows NO consumer
outside this file — the artifact name is not referenced by any other workflow.

The six `Heroic*` globs are at `:71`, `:72`, `:73`, `:74`, `:82`, `:91`.
</interfaces>
</context>

<constraints>
- **D-07 boundary — these `x64` occurrences MUST SURVIVE this plan:**
  `package.json` `release:win ... --x64` (`:47`) and `sign:win ... --x64 --arm64` (`:49`);
  `build-base.yml` `pnpm dist:win portable --x64` (`:42`) and
  `pnpm dist:linux AppImage --x64 --arm64` (`:62`);
  `electron-builder.yml`'s 7 `build/bin/x64/win32/` references, including the 2 inside the `mac:`
  `files:` block. Those two are the point: the macOS package legitimately bundles win32 x64 GOG
  Galaxy / Epic helpers. Plan 34.18-01's `x64NonGoalSurvivor` gate fails if you touch them.
- **Do NOT edit `meta/verifyRunnerBundle.ts` or `meta/__tests__/verifyRunnerBundle.test.ts`.** They
  are not in `files_modified`. If the RED-proof in Task 1 shows they genuinely need a change, STOP
  and report it rather than editing out of scope.
- **Do NOT edit `meta/runnersOnedirDigests.json`.** Plan 34.18-02 owns it; plan 34.18-07 pins it.
- **The `[REMOVE WHEN DIGESTS ARE PINNED ...]` marker stays** (retargeted). It is still true —
  three arm64 sentinels remain. Plan 34.18-07's developer gate removes it in the same commit that
  pins the digests.
- **Tasks 2 and 3 are SEPARATE COMMITS.** This is D-09's operator-acknowledged attribution caveat,
  and it is the whole reason those two tasks are not one. Do not squash them.
- Restore any file by `cp` + `shasum -a 256`, never `git checkout -- <path>`. Never `git stash`,
  never `git reset`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Drop the x64 guard call and electron-builder flag; RED-prove the one-arch guard; rewrite the mac: prose</name>
  <read_first>package.json scripts block (lines 40-80); electron-builder.yml lines 44-90 (the `mac:` block including its prose comment and `files:` list); meta/verifyRunnerBundle.ts:220-270 (findDarwinBinRoot) and :510-600 (parseCliArgs and main); meta/__tests__/verifyRunnerBundle.test.ts:595-626 (the C2-04 wiring pin — read it so you can confirm it stays green rather than editing it)</read_first>
  <files>package.json, electron-builder.yml</files>
  <action>
**package.json.** In BOTH `release:mac` and `dist:mac`, delete the `&& pnpm verify:runner-bundle
build --arch=x64` step and delete `--x64` from the `electron-builder` invocation. Target strings:

  "release:mac": `pnpm clean:dist-mac && pnpm build-steam-bridge && electron-vite build && pnpm verify:runner-bundle build --arch=arm64 && electron-builder -p always --mac --arm64`
  "dist:mac": `export CSC_IDENTITY_AUTO_DISCOVERY=false && pnpm clean:dist-mac && pnpm build-steam-bridge && electron-vite build && pnpm verify:runner-bundle build --arch=arm64 && electron-builder --mac`

`-p always` STAYS on `release:mac` — the release ritual is not this phase's to redesign, and it was
explicitly left alone in 34.16. Nothing else in `package.json` changes: no key added or removed, no
reordering, and `release:win`/`sign:win`'s `--x64` untouched.

**RED-proof the one-arch guard (this is a deliverable, not a check).** CONTEXT's Claude's-Discretion
list asks whether the guard needs any change beyond callers dropping `--arch=x64`. Answer it by
measurement, in this order, and record all three observations in the summary:
  1. `pnpm verify:runner-bundle "$(mktemp -d)" --arch=arm64` — MUST exit non-zero and the message
     MUST name both the root and `arch="arm64"`. This is the vacuity question: an arch-parameterised
     guard given one arch must fail loudly on a missing tree, not pass over an empty one.
  2. Build a scratch root that DOES contain `build/bin/arm64/darwin` but is missing one of the three
     runners (`legendary`, `gogdl`, `nile`). `pnpm verify:runner-bundle <root> --arch=arm64` MUST
     exit non-zero, because `main()` returns `summary.ok ? 0 : 1` over a fixed three-runner list —
     one arch does not shrink the runner list.
  3. `pnpm test --selectProjects Meta -t "verifyRunnerBundle"` passes with `meta/verifyRunnerBundle.ts`
     and its test file BYTE-UNCHANGED (`git diff --name-only` must not list either).
If all three hold, state the conclusion explicitly in the summary: the guard needed no code change,
and that was proven against a known-bad tree rather than inferred from reading it.

**electron-builder.yml `mac:` prose (`:55-80`).** Rewrite so it is true after this edit:
  - one guard invocation (`--arch=arm64`), not two; delete the "arm64 then x64" and "Both arches are
    covered by this guard" claims;
  - "six PENDING-CI-PUBLISH sentinels" becomes three;
  - state that Phase 34.18 retired the macOS x64 leg (`F-34.16-G`: `macos-13` retired by GitHub
    2025-12-04, never assigned a runner across three attempts) so macOS is arm64-only;
  - KEEP the still-true reasoning: the onedir trees are macOS-only because the amfid per-file
    Gatekeeper tax is macOS-specific; a nested tree in `app.asar.unpacked` must not be trusted just
    because the build succeeded (electron-builder issue #3940); the guard inspects the STAGED
    `build/` tree, not the packaged `.app`; and the deliberate consequence that `dist:mac` fails fast
    on a checkout with no staged `build/bin/arm64/darwin`;
  - RETARGET the marker: `[REMOVE WHEN DIGESTS ARE PINNED -- 34.16-LIVE-GATE.md Scored Item 3]`
    becomes `[REMOVE WHEN DIGESTS ARE PINNED -- Phase 34.18 plan 34.18-07's developer gate]`. Keep
    the marker; it is still true. `34.16-LIVE-GATE.md`'s Scored Item 3 can never run now.
  - Do NOT touch the `files:` list below the comment. The two `build/bin/x64/win32/*.exe` entries are
    D-07 non-goals.
  </action>
  <acceptance_criteria>
- `python3 -c "import json; s=json.load(open('package.json'))['scripts']; assert s['dist:mac'].count('verify:runner-bundle')==1 and s['release:mac'].count('verify:runner-bundle')==1, s; assert '--arch=x64' not in s['dist:mac'] and '--arch=x64' not in s['release:mac']; assert '--x64' not in s['dist:mac'] and '--x64' not in s['release:mac']; assert '-p always' in s['release:mac']; assert '--x64' in s['release:win'] and '--x64' in s['sign:win']; print('OK')"` exits 0.
- `pnpm test --selectProjects Meta -t "verifyRunnerBundle"` exits 0 with `git diff --name-only` NOT listing `meta/verifyRunnerBundle.ts` or `meta/__tests__/verifyRunnerBundle.test.ts`.
- RED-proof observation 1 recorded: guard against an empty scratch root exits non-zero and the message contains `arch="arm64"`.
- RED-proof observation 2 recorded: guard against a root missing one runner exits non-zero.
- `grep -c "build/bin/x64/win32/" electron-builder.yml` prints `7`.
- `grep -c -- "--arch=x64" electron-builder.yml` prints `0`; `grep -c "six" electron-builder.yml` prints `0`.
- `grep -c "REMOVE WHEN DIGESTS ARE PINNED" electron-builder.yml` prints `1`, and `grep -c "34.16-LIVE-GATE.md" electron-builder.yml` prints `0`.
- `pnpm test --selectProjects Meta -t "non-goal"` exits 0 (plan 34.18-01's survivor gate).
- `python3 -c "import yaml; yaml.safe_load(open('electron-builder.yml')); print('OK')"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --selectProjects Meta && python3 -c "import json; s=json.load(open('package.json'))['scripts']; assert s['dist:mac'].count('verify:runner-bundle')==1 and '--x64' not in s['dist:mac']; assert s['release:mac'].count('verify:runner-bundle')==1 and '--x64' not in s['release:mac']; print('OK')"</automated>
  </verify>
  <done>Both macOS packaging scripts guard and build one arch; the guard's strictness under one arch is measured, not assumed; the `mac:` prose states what is now true and its stale-plan marker points at a gate that can actually run.</done>
</task>

<task type="auto">
  <name>Task 2 (OWN COMMIT — macOS half): build-base.yml builds and uploads arm64 only</name>
  <read_first>.github/workflows/build-base.yml in full (95 lines — matrix, all build steps, all three upload steps and their `if:` conditions); electron-builder.yml:44-50 and :100-105 (the mac and linux `artifactName` templates, so you can confirm which suffix each glob must match)</read_first>
  <files>.github/workflows/build-base.yml</files>
  <action>
Four edits, all macOS, landing as ONE commit whose message states it is the macOS-arch half of
D-09 (see Task 3 for why the split exists).

1. **Matrix.** Change the `macos-15` leg's `arch: x64` to `arch: arm64`. `macos-15` is an Apple
   Silicon image; the `x64` label was always describing the artifact, not the host, and it is now
   wrong on both counts. `matrix.arch` is referenced nowhere in the steps, so this is a label
   correction — say so in the commit body rather than implying it changes behaviour.

2. **Build step (`:47-53`).** `pnpm dist:mac --x64 --arm64 --publish=never` becomes
   `pnpm dist:mac --arm64 --publish=never`. Rename the step from "Build macOS x64 & arm64 versions"
   to "Build macOS arm64 version". Leave the `env:` block and `if: runner.os == 'macOS'` alone.

3. **Combined "Upload built version" step (`:64-78`).** Remove the `macos-15` arm from BOTH the
   `name:` ternary (`'mac-x64'`) and the `path:` ternary (`'dist/Heroic*x64.dmg'`), and add
   `if: runner.os != 'macOS'` to the step. **The `if:` is load-bearing, not tidiness:** with the arm
   removed the `||` chain evaluates to `false` on macOS, which yields an empty `name:`/`path:` and
   makes the step fail under `if-no-files-found: error`. macOS is then served solely by the dedicated
   "Upload macOS ARM version" step below. `grep -rn "mac-x64" .github/` confirms no other workflow
   consumes that artifact name, so dropping it breaks no consumer.

4. **"Upload macOS ARM version" step (`:87-95`).** `path: dist/Heroic*arm64.dmg` becomes
   `path: dist/GameLib*arm64.dmg`. `productName` is `GameLib` and the mac `artifactName` template is
   `${productName}-${version}-macOS-${arch}.${ext}`, so the produced file is
   `GameLib-<version>-macOS-arm64.dmg` — which `dist/Heroic*arm64.dmg` has never matched. Leave
   `name: mac-arm64` as-is.

Do NOT touch `pnpm dist:win portable --x64` (`:42`) or
`pnpm dist:linux AppImage --x64 --arm64` (`:62`) — Windows/Linux build arches, D-07 non-goals.
Do NOT touch the four non-macOS `Heroic*` globs in this commit; they are Task 3's.
  </action>
  <acceptance_criteria>
- `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/build-base.yml')); m=d['jobs']['build']['strategy']['matrix']['include']; mac=[e for e in m if e['os']=='macos-15']; assert len(mac)==1 and mac[0]['arch']=='arm64', m; print('OK')"` exits 0.
- `grep -c -- "dist:mac --arm64 --publish=never" .github/workflows/build-base.yml` prints `1`; `grep -c -- "dist:mac --x64" .github/workflows/build-base.yml` prints `0`.
- `grep -c "mac-x64" .github/workflows/build-base.yml` prints `0`; `grep -c "Heroic\*x64.dmg" .github/workflows/build-base.yml` prints `0`.
- `grep -c "dist/GameLib\*arm64.dmg" .github/workflows/build-base.yml` prints `1`.
- The combined upload step carries `if: runner.os != 'macOS'` — verify by reading the step, and confirm structurally: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/build-base.yml')); s=[x for x in d['jobs']['build']['steps'] if x.get('name')=='Upload built version'][0]; assert \"runner.os != 'macOS'\" in s['if'], s; print('OK')"` exits 0.
- Non-goals intact: `grep -c -- "dist:win portable --x64" .github/workflows/build-base.yml` prints `1` and `grep -c -- "dist:linux AppImage --x64 --arm64" .github/workflows/build-base.yml` prints `1`.
- The four non-macOS `Heroic*` globs are STILL `Heroic*` after this commit: `grep -c "dist/Heroic\*" .github/workflows/build-base.yml` prints `4`.
- This task is committed on its own: `git log -1 --name-only` lists only `.github/workflows/build-base.yml`, and the message identifies it as the macOS-arch half.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/build-base.yml')); m=d['jobs']['build']['strategy']['matrix']['include']; assert [e for e in m if e['os']=='macos-15'][0]['arch']=='arm64'; s=[x for x in d['jobs']['build']['steps'] if x.get('name')=='Upload built version'][0]; assert \"runner.os != 'macOS'\" in s['if']; print('OK')"</automated>
  </verify>
  <done>The macOS CI leg builds one arch and uploads one dmg through a glob that matches a real filename; the four non-macOS globs are untouched so Task 3's commit is independently revertible.</done>
</task>

<task type="auto">
  <name>Task 3 (OWN COMMIT — non-macOS half): repair the four Windows/Linux upload globs</name>
  <read_first>.github/workflows/build-base.yml as left by Task 2 (the two upload steps and their `path:` expressions); electron-builder.yml:34, :45, :102 (the nsis, portable and linux `artifactName` templates) and :2 (`productName`)</read_first>
  <files>.github/workflows/build-base.yml</files>
  <action>
Change the remaining four `dist/Heroic*` globs to `dist/GameLib*`, changing nothing else:

  - `:71` `'dist/Heroic*.exe'`            (windows-2025)   -> `'dist/GameLib*.exe'`
  - `:72` `'dist/Heroic*.exe'`            (windows-11-arm) -> `'dist/GameLib*.exe'`
  - `:74` `'dist/Heroic*x86_64.AppImage'` (ubuntu-24.04)   -> `'dist/GameLib*x86_64.AppImage'`
  - `:82` `dist/Heroic*arm64.AppImage`    (linux ARM)      -> `dist/GameLib*arm64.AppImage`

Only the product-name prefix changes. The arch suffixes are already correct: electron-builder
resolves `${arch}` to `x86_64` for AppImage and leaves it literal for nsis/portable, and the
templates are `${productName}-${version}-Portable-${arch}.${ext}` and
`${productName}-${version}-linux-${arch}.${ext}`.

**Commit this separately from Task 2, with a message that states the attribution reason:** these are
Windows and Linux CI fixes riding inside a macOS-arch retirement per D-09, an operator decision that
overrode the recommendation to defer them to their own owner, and the separate commit is what makes
an independent revert possible if `build-base.yml` breaks. Do not squash with Task 2.

Do not re-touch anything Task 2 changed, and do not touch `pnpm dist:win portable --x64` or
`pnpm dist:linux AppImage --x64 --arm64`.
  </action>
  <acceptance_criteria>
- `grep -c "dist/Heroic" .github/workflows/build-base.yml` prints `0`.
- `grep -c "dist/GameLib" .github/workflows/build-base.yml` prints `6` (2 `.exe`, 1 `x86_64.AppImage`, 1 `arm64.AppImage`, 1 `arm64.dmg` from Task 2 — and no sixth unless Task 2's count is re-derived; verify the exact set by reading the four `path:` expressions rather than trusting the total).
- Each of the four globs is verified individually: `dist/GameLib*.exe` appears twice, `dist/GameLib*x86_64.AppImage` once, `dist/GameLib*arm64.AppImage` once.
- Non-goals intact: `grep -c -- "--x64" .github/workflows/build-base.yml` prints `2` (the `dist:win portable --x64` and `dist:linux AppImage --x64 --arm64` steps).
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build-base.yml')); print('OK')"` exits 0.
- `git log -1 --name-only` lists only `.github/workflows/build-base.yml`, and `git log -2 --oneline` shows this commit and Task 2's as two distinct commits.
- `git show HEAD --stat` shows a diff of exactly 4 changed lines.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import re; s=open('.github/workflows/build-base.yml').read(); assert 'dist/Heroic' not in s; assert s.count('dist/GameLib*.exe')==2 and s.count('dist/GameLib*x86_64.AppImage')==1 and s.count('dist/GameLib*arm64.AppImage')==1 and s.count('dist/GameLib*arm64.dmg')==1; import yaml; yaml.safe_load(s); print('OK')"</automated>
  </verify>
  <done>All six upload globs match filenames `electron-builder` actually produces, and the Windows/Linux half sits in its own revertible commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `release:mac` -> GitHub releases feed -> end users | `release:mac` chains `-p always` into the feed `electron-updater` consumes. Whatever it publishes auto-updates onto real users' machines. |
| staged `build/` tree -> packaged `.app` | `verify:runner-bundle` is the only automated inspection between the staged runner trees and the signed, shipped bundle. |
| CI job -> uploaded artifact | `if-no-files-found: error` is the only thing distinguishing "built nothing" from "built something" in `build-base.yml`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.18-12 | Tampering | `package.json:release:mac` `-p always` + `--x64` | mitigate | Ledger item 18 / C2-05's real user-facing exposure: an x64 macOS build reaching the auto-update channel. **The ROADMAP's stated mechanism is stale** — `package.json:46,51` have guarded BOTH arches since 34.16, so the exposure is not "an unguarded arch". It is that the x64 artifact is built at all from a tree `download-helper-binaries` cannot populate (its three x64 digest slots never had a publication behind them), then published. Dropping `--x64` removes the artifact, closing the path by construction rather than by a guard that must keep working. |
| T-34.18-13 | Elevation of Privilege | `meta/verifyRunnerBundle.ts` under a single arch | mitigate | The ROADMAP warns an arch-parameterised guard given one arch "can go vacuous rather than strict" — a vacuous guard would sign and ship an unverified runner tree. Task 1 does not accept the source-reading conclusion: it RED-proves against a missing tree AND a tree missing one runner, both required to exit non-zero, with the guard's own source byte-unchanged. |
| T-34.18-14 | Denial of Service | `build-base.yml` combined upload step | mitigate | Removing the `macos-15` arm from the `||` ternary without adding `if: runner.os != 'macOS'` yields an empty `name:`/`path:` on macOS and fails the job under `if-no-files-found: error`. The `if:` is asserted structurally (parsed YAML), not by eyeballing the diff. |
| T-34.18-15 | Repudiation | D-09 attribution across platforms | mitigate | Windows/Linux CI changes riding inside a macOS-arch phase mean a `build-base.yml` breakage cannot be attributed to one half from the phase boundary alone. Mitigated by two separate commits with explicit messages, asserted by `git log -1 --name-only` and a 4-line diff on the second. |
| T-34.18-16 | Tampering | `electron-builder.yml` `mac:` `files:` `build/bin/x64/win32/*.exe` | mitigate | The prose being rewritten sits directly above the `files:` list. An over-reaching edit here silently drops the win32 GOG/Epic helpers from the macOS bundle with a green build. Pinned at 7 by acceptance criteria and by plan 34.18-01's survivor gate. |
| T-34.18-SC | Tampering | npm/pip/cargo installs | accept | **This plan installs no packages.** `package.json`'s `scripts` block is edited; no `dependencies`/`devDependencies` entry and no lockfile is touched. Verify with `git diff package.json` showing only `scripts` lines. |
</threat_model>

<verification>
1. `pnpm test --selectProjects Meta` green, with `meta/verifyRunnerBundle.ts` and its test byte-unchanged.
2. `pnpm test --selectProjects Meta -t "non-goal"` green — no over-reach into the win32/linux `x64` categories.
3. The summary records all three guard RED-proof observations and states the explicit conclusion about whether `verifyRunnerBundle.ts` needed a change.
4. `git log -3 --oneline` shows Task 2's macOS commit and Task 3's non-macOS commit as two distinct commits, in that order.
5. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build-base.yml'))"` and the same for `electron-builder.yml` both exit 0.
6. Non-goal survivors: `--x64` still present in `release:win`, `sign:win`, `dist:win portable`, `dist:linux AppImage`; 7 `build/bin/x64/win32/` refs in `electron-builder.yml`.
7. `pnpm codecheck`, `pnpm lint`, `pnpm prettier` each run in place and reported.
</verification>

<success_criteria>
- One guard invocation and no `--x64` in each of `dist:mac`/`release:mac`; `-p always` preserved.
- The one-arch guard is proven strict against two distinct known-bad trees, with its own source untouched.
- `electron-builder.yml`'s `mac:` prose is true and its marker points at a gate that can run.
- All six `build-base.yml` globs match real filenames, macOS builds arm64 only, and the non-macOS half is a separate commit.
- Every D-07 non-goal `x64` occurrence survives.
</success_criteria>

<output>
Create `.planning/phases/34.18-retire-the-macos-x64-intel-ci-leg-arm64-only-macos-runners-a/34.18-04-SUMMARY.md` when done. It MUST record the three guard RED-proof observations and the two commit SHAs proving the D-09 split.
</output>
