---
phase: 01-steam-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/common/types.ts
  - src/common/types/steam.ts
  - src/common/types/electron_store.ts
  - src/common/types/ipc.ts
  - src/backend/logger/constants.ts
  - src/backend/storeManagers/index.ts
  - src/backend/storeManagers/steam/library.ts
  - src/backend/storeManagers/steam/games.ts
  - src/backend/wiki_game_info/umu/utils.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
user_setup: []

must_haves:
  truths:
    - "Project type-checks with 'steam' as a recognized Runner (tsc --noEmit exits 0)"
    - "steam-user and react-qr-code are runtime deps; @types/steam-user is a devDependency"
    - "'steamConfigStore' is a valid store name accepted by TypeCheckedStoreBackend"
    - "libraryManagerMap has a 'steam' entry satisfying Record<Runner, LibraryManager>"
  artifacts:
    - path: "src/common/types/steam.ts"
      provides: "SteamCredentials and SteamUserData types"
      contains: "export interface SteamUserData"
    - path: "src/backend/storeManagers/steam/library.ts"
      provides: "Phase-1 stub SteamLibraryManager implementing LibraryManager"
      contains: "implements LibraryManager"
    - path: "src/backend/storeManagers/steam/games.ts"
      provides: "Phase-1 stub SteamGame implementing Game"
      contains: "implements Game"
    - path: "package.json"
      provides: "Steam auth dependencies"
      contains: "react-qr-code"
  key_links:
    - from: "src/backend/storeManagers/index.ts"
      to: "src/backend/storeManagers/steam/library.ts"
      via: "libraryManagerMap.steam = new SteamLibraryManager()"
      pattern: "steam: new SteamLibraryManager\\(\\)"
    - from: "src/common/types/electron_store.ts"
      to: "src/common/types/steam.ts"
      via: "import SteamUserData for steamConfigStore.userData"
      pattern: "steamConfigStore"
---

<objective>
Lay the type + dependency foundation that makes `'steam'` a first-class `Runner` across GamerLib, and add a no-op `LibraryManager`/`Game` stub so the `satisfies Record<Runner, LibraryManager>` compile gate passes. This is the base of the Walking Skeleton: nothing in the backend or frontend slices compiles until this plan lands.

Purpose: A single, low-risk compile-gate plan. Every Phase 1 requirement (AUTH-01..05) depends on these types existing; isolating them keeps later waves green and parallelizable.
Output: Installed packages, new `src/common/types/steam.ts`, `'steam'` added to every exhaustive `Runner` map/union, `steamConfigStore` registered in `StoreStructure`, Steam IPC type signatures declared, and a compiling Steam library/game stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-steam-authentication/01-RESEARCH.md
@.planning/phases/01-steam-authentication/01-PATTERNS.md
@.planning/phases/01-steam-authentication/SKELETON.md

<interfaces>
<!-- Contracts the executor must satisfy. Extracted from codebase 2026-06-26. -->

Runner union — src/common/types.ts line 23:
  export type Runner = 'legendary' | 'gog' | 'sideload' | 'nile' | 'zoom'
GameInfo.runner inline union — src/common/types.ts line 183:
  runner: 'legendary' | 'gog' | 'sideload' | 'nile' | 'zoom'

Exhaustive Record<Runner, ...> maps that the union change forces (all must gain a 'steam' key):
  - src/backend/storeManagers/index.ts:13 libraryManagerMap ... satisfies Record<Runner, LibraryManager>
  - src/backend/logger/constants.ts:31 RunnerToLogPrefixMap: Record<Runner, LogPrefix>
  - src/backend/wiki_game_info/umu/utils.ts:9 storeMapping: Record<Runner, string>
  (src/frontend/components/UI/LibrarySearchBar uses Partial<Record<Runner,...>> — no change needed)

LibraryManager interface — src/common/types/game_manager.ts:66 (methods the stub must implement):
  init, getGame, refresh, getGameInfo, getInstallInfo, listUpdateableGames,
  changeGameInstallPath, changeVersionPinnedStatus, installState, getLaunchOptions

Game interface — src/common/types/game_manager.ts:25 (returned by getGame; the SteamGame stub must implement):
  getSettings, getGameInfo, getExtraInfo, importGame, onInstallOrUpdateOutput, install,
  isNative, addShortcuts, removeShortcuts, launch, moveInstall, repair, syncSaves,
  uninstall, update, forceUninstall, stop, isGameAvailable (getAchievements is optional)

electron-store name constraint — src/common/types/electron_store.ts:
  TypeCheckedStoreBackend<Name extends ValidStoreName>, ValidStoreName = keyof StoreStructure.
  Existing zoom block to copy shape from:
    zoomConfigStore: { credentials?: ZoomCredentials; isLoggedIn: boolean; username?: string }

IPC type files — src/common/types/ipc.ts:
  SyncIPCFunctions has `logoutZoom: () => void` (line 126)
  AsyncIPCFunctions has `getZoomUserInfo` (197) and `authZoom` (211)

Package Legitimacy Audit (RESEARCH.md §Package Legitimacy Audit): steam-user, steam-session,
@types/steam-user, react-qr-code all dispositioned "Approved". slopcheck was UNAVAILABLE at
research time → fallback policy treats them as [ASSUMED] → human verification required (Task 1).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before install</name>
  <what-built>Nothing yet — this is a pre-install security gate. RESEARCH.md §Package Legitimacy Audit dispositioned all four packages "Approved", but slopcheck was unavailable, so the fallback policy treats them as [ASSUMED]. Threat T-01-SC requires human confirmation before any package-manager install.</what-built>
  <read_first>
    - .planning/phases/01-steam-authentication/01-RESEARCH.md (§Package Legitimacy Audit)
  </read_first>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/steam-user — confirm latest is 5.3.0, maintainer DoctorMcKay, repo github.com/DoctorMcKay/node-steam-user, no postinstall script.
    2. Open https://www.npmjs.com/package/steam-session — confirm 1.9.4, maintainer DoctorMcKay.
    3. Open https://www.npmjs.com/package/react-qr-code — confirm 2.2.0, repo github.com/rosskhanas/react-qr-code.
    4. Open https://www.npmjs.com/package/@types/steam-user — confirm 5.1.1, DefinitelyTyped.
    Confirm none were unpublished/deprecated and none show a postinstall script.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all four packages exist at the stated versions with the stated maintainers and no postinstall scripts.
    - This checkpoint is NEVER auto-approved (workflow.auto_advance is ignored for legitimacy gates).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install packages and add the Steam type foundation</name>
  <files>package.json, src/common/types.ts, src/common/types/steam.ts, src/common/types/electron_store.ts, src/common/types/ipc.ts, src/backend/logger/constants.ts, src/backend/wiki_game_info/umu/utils.ts</files>
  <read_first>
    - package.json (dependencies / devDependencies blocks)
    - src/common/types.ts (Runner union line 23; GameInfo.runner line 183)
    - src/common/types/electron_store.ts (StoreStructure interface; zoomConfigStore block; ZoomCredentials import)
    - src/common/types/ipc.ts (SyncIPCFunctions ~line 126; AsyncIPCFunctions ~lines 197, 211)
    - src/backend/logger/constants.ts (LogPrefix object lines 3-24; RunnerToLogPrefixMap line 31)
    - src/backend/wiki_game_info/umu/utils.ts (storeMapping line 9)
    - src/common/types/zoom.ts (analog for the new steam.ts types file)
  </read_first>
  <action>
    Run `npm install steam-user@5.3.0 react-qr-code@2.2.0` and `npm install --save-dev @types/steam-user@5.1.1`. steam-session installs transitively via steam-user — do not add it explicitly. Confirm package.json `dependencies` gains `steam-user` and `react-qr-code`, and `devDependencies` gains `@types/steam-user`.

    Create `src/common/types/steam.ts` exporting two interfaces: `SteamCredentials` with `refreshToken: string` (decrypted before use), and `SteamUserData` with `username: string` and optional `steamId?: string`. Keep the file minimal — no LaunchOption/install types (those are Phase 2).

    In `src/common/types.ts`: append `| 'steam'` to the `Runner` union at line 23, and add `| 'steam'` to the inline `GameInfo.runner` union at line 183. Search the file for any other inline Runner-shaped union and update consistently.

    In `src/common/types/electron_store.ts`: add a `steamConfigStore` member to `StoreStructure` shaped `{ isLoggedIn: boolean; refreshToken?: string; userData?: SteamUserData }` (refreshToken is the safeStorage-encrypted base64 string with the `steam:v1:` prefix). Add `import { SteamUserData } from './steam'` alongside the existing `ZoomCredentials` import.

    In `src/common/types/ipc.ts`: add `logoutSteam: () => void` to `SyncIPCFunctions` (next to `logoutZoom`); add to `AsyncIPCFunctions`: `steamStartQR: () => Promise<{ status: 'done' | 'error'; challengeUrl?: string }>`, `steamPollQR: () => Promise<{ status: 'done' | 'waiting' | 'error'; username?: string }>`, `steamStartCredentials: (credentials: { username: string; password: string }) => Promise<{ status: 'done' | 'guard_required' | 'error' }>`, `steamSubmitGuard: (code: string) => Promise<{ status: 'done' | 'error' }>`, `getSteamUserInfo: () => Promise<SteamUserData | undefined>`, `checkSteamInstalled: () => Promise<boolean>`. Add `import type { SteamUserData } from './steam'`.

    In `src/backend/logger/constants.ts`: add `Steam: 'Steam'` to the `LogPrefix` object (after `Zoom: 'Zoom'`) and `steam: LogPrefix.Steam` to `RunnerToLogPrefixMap`. Do not touch `MaxLogPrefixLength` (computed automatically).

    In `src/backend/wiki_game_info/umu/utils.ts`: add `steam: 'steam'` to the `storeMapping` Record<Runner, string>.
  </action>
  <verify>
    <automated>grep -q "react-qr-code" package.json &amp;&amp; grep -q "steam-user" package.json &amp;&amp; grep -q "@types/steam-user" package.json &amp;&amp; test -f src/common/types/steam.ts &amp;&amp; grep -q "| 'steam'" src/common/types.ts &amp;&amp; grep -q "steamConfigStore" src/common/types/electron_store.ts &amp;&amp; grep -q "checkSteamInstalled" src/common/types/ipc.ts &amp;&amp; grep -q "Steam: 'Steam'" src/backend/logger/constants.ts &amp;&amp; grep -q "steam: 'steam'" src/backend/wiki_game_info/umu/utils.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/steam-user`, `node_modules/steam-session`, `node_modules/react-qr-code`, `node_modules/@types/steam-user` all exist after install.
    - `src/common/types/steam.ts` exports `SteamUserData` and `SteamCredentials`.
    - `src/common/types/ipc.ts` declares all 7 steam IPC functions (logoutSteam, steamStartQR, steamPollQR, steamStartCredentials, steamSubmitGuard, getSteamUserInfo, checkSteamInstalled).
    - `RunnerToLogPrefixMap` contains a `steam:` key.
    - `storeMapping` in umu/utils.ts contains `steam: 'steam'`.
  </acceptance_criteria>
  <done>All four packages installed; `'steam'` present in the Runner union, GameInfo.runner, StoreStructure, ipc.ts, LogPrefix, RunnerToLogPrefixMap, and umu storeMapping.</done>
</task>

<task type="auto">
  <name>Task 3: Add stub Steam LibraryManager + Game and register in libraryManagerMap</name>
  <files>src/backend/storeManagers/steam/library.ts, src/backend/storeManagers/steam/games.ts, src/backend/storeManagers/index.ts</files>
  <read_first>
    - src/common/types/game_manager.ts (LibraryManager interface line 66; Game interface line 25)
    - src/backend/storeManagers/zoom/library.ts (analog ZoomLibraryManager implements LibraryManager)
    - src/backend/storeManagers/zoom/games.ts (analog for the Game class shape)
    - src/backend/storeManagers/index.ts (libraryManagerMap, lines 1-19)
  </read_first>
  <action>
    Create `src/backend/storeManagers/steam/games.ts` exporting `class SteamGame implements Game` whose constructor takes the app id string. Implement every method of the `Game` interface (getSettings, getGameInfo, getExtraInfo, importGame, onInstallOrUpdateOutput, install, isNative, addShortcuts, removeShortcuts, launch, moveInstall, repair, syncSaves, uninstall, update, forceUninstall, stop, isGameAvailable) as Phase-1 no-ops: synchronous methods return harmless defaults (e.g. `isNative` returns true, `getGameInfo` throws `new Error('Steam library not implemented until Phase 2')`), async methods reject or resolve with a clear "not implemented until Phase 2" error/value. Match exact method signatures and return types from the Game interface so tsc is satisfied. Do NOT implement getAchievements (optional).

    Create `src/backend/storeManagers/steam/library.ts` exporting `export default class SteamLibraryManager implements LibraryManager`. Implement all interface methods (init, getGame, refresh, getGameInfo, getInstallInfo, listUpdateableGames, changeGameInstallPath, changeVersionPinnedStatus, installState, getLaunchOptions) as Phase-1 stubs: `init` resolves immediately, `getGame(id)` returns `new SteamGame(id)`, `refresh` resolves to `null`, `getGameInfo` returns `undefined`, `getInstallInfo` resolves `undefined`, `listUpdateableGames` resolves `[]`, `getLaunchOptions` returns `[]`, void methods are empty. Use `LogPrefix.Steam` if logging. Match exact signatures so `satisfies Record<Runner, LibraryManager>` passes.

    In `src/backend/storeManagers/index.ts`: add `import SteamLibraryManager from 'backend/storeManagers/steam/library'` next to the ZoomLibraryManager import, and add `steam: new SteamLibraryManager()` to the `libraryManagerMap` object literal. The `satisfies Record<Runner, LibraryManager>` constraint must now require and accept the steam entry.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run codecheck` (tsc --noEmit) exits 0 with no errors.
    - `src/backend/storeManagers/steam/library.ts` contains `implements LibraryManager` and `export default class SteamLibraryManager`.
    - `src/backend/storeManagers/steam/games.ts` contains `implements Game`.
    - `src/backend/storeManagers/index.ts` contains `steam: new SteamLibraryManager()` inside the object that has `satisfies Record<Runner, LibraryManager>`.
  </acceptance_criteria>
  <done>Whole project type-checks clean with 'steam' as a registered Runner; Steam library/game stubs exist and are wired into libraryManagerMap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine | Package installs execute code (postinstall) and ship into the app bundle |
| (future) renderer → main | Established by later plans; no IPC handlers added here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm install of steam-user, react-qr-code, @types/steam-user | mitigate | Blocking human-verify checkpoint (Task 1) confirms packages on npmjs.com before install; RESEARCH.md audit confirms no postinstall scripts. Never auto-approved. |
| T-01-TYPE | Tampering | Runner union / Record<Runner> maps left partially updated | mitigate | Task 3 gate is `npm run codecheck`; tsc fails if any exhaustive map/union is missing 'steam' |
</threat_model>

<verification>
- `npm run codecheck` exits 0 (full project type-checks with 'steam' as a Runner).
- All four packages resolvable under `node_modules/`.
- No frontend `ContextType` changes here (deferred to plan 03) so the renderer still type-checks.
</verification>

<success_criteria>
- `'steam'` is a recognized `Runner` everywhere tsc enforces it; `npm run codecheck` is green.
- Steam dependencies installed at pinned versions.
- `steamConfigStore` is a valid `ValidStoreName`; Steam IPC types declared; Steam library/game stubs registered.
</success_criteria>

<output>
Create `.planning/phases/01-steam-authentication/01-01-SUMMARY.md` when done.
</output>
