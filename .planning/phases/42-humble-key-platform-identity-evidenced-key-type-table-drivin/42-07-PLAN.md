---
phase: 42-humble-key-platform-identity-evidenced-key-type-table-drivin
plan: 07
type: execute
wave: 5
depends_on: ["42-03", "42-04", "42-05", "42-06"]
files_modified:
  - .planning/todos/pending/2026-08-23-humble-integrated-activation-reconcile-key-state-with-steam-.md
autonomous: false
requirements: [REQ-42-07, REQ-42-08]

must_haves:
  truths:
    - "The owning todo no longer asserts that owned keys appear in Keys-waiting with an Activate button."
    - "The todo records HOW that claim was measured false, with file:line citations, so it cannot be re-derived by the next reader."
    - "The A1 evidenced assumption (key_type === 'gog') is either confirmed against a live sync or explicitly recorded as still unconfirmed."
    - "The four no-logo display names are operator-approved, not planner-invented."
  artifacts:
    - path: ".planning/todos/pending/2026-08-23-humble-integrated-activation-reconcile-key-state-with-steam-.md"
      provides: "Corrected problem statement + measurement record + resolves_phase"
      contains: "resolves_phase"
  key_links:
    - from: "the owning todo"
      to: "src/common/humble/viewFilters.ts:62"
      via: "cited as the evidence that owned keys are excluded from Keys-waiting"
      pattern: "viewFilters.ts"
---

<objective>
Close the phase's two record-keeping obligations: strike the owning todo's false
premise with the measurement that disproved it (CONTEXT.md scope item 5,
success criterion 6), and get live/operator confirmation of the one evidenced
assumption the whole GOG half rests on (A1).

Purpose: the todo's central claim — that owned keys sit in Keys-waiting offering
an actionable Activate button, spending real Steam activation attempts and
risking a rate-limit across 18 keys — is FALSE and has been false since
`5bfc2cb3d` (2026-07-08), six weeks before the todo was written. Left as-is, the
next reader re-derives the same wrong requirement and plans batch-activation and
rate-limit-serialization work that has no reachable code path.

Output: a corrected todo carrying its own disproof, and a recorded operator
verdict on `key_type === 'gog'` plus the four unverifiable display names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-CONTEXT.md
@.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-01-SUMMARY.md
@.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-03-SUMMARY.md
@.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-04-SUMMARY.md
@.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-05-SUMMARY.md
@.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-06-SUMMARY.md
@.planning/todos/pending/2026-08-23-humble-integrated-activation-reconcile-key-state-with-steam-.md
</context>

<interfaces>
<!-- The four measurements that disprove the todo. All re-verified 2026-09-07
     against fix/steam-native-install-stability @ 7bf39e3b5. Cite these
     verbatim; do not re-derive them. -->

| Todo claim | Verdict | Evidence |
|---|---|---|
| Owned keys appear in Keys-waiting | FALSE | `selectKeysWaiting` returns `false` for any `k.ownedElsewhere` — `src/common/humble/viewFilters.ts:62`, landed `5bfc2cb3d` on 2026-07-08, six weeks BEFORE the todo was written |
| Owned keys offer an Activate button | FALSE | The button rides entirely on the optional `claimAction` prop (`HumbleKeyRow/index.tsx:29`, rendered at `:114`). Exactly one caller supplies it: `Keys/Waiting/index.tsx:222`. `Spares/index.tsx:79` passes `giftAction` only; `HumbleKeyGroup/index.tsx:81` (the All tab) passes `urgencyTier` only |
| Reconciling spends a Steam activation attempt | UNREACHABLE for owned keys | No Activate button ⇒ no attempt |
| Rate-limit hazard across 18 keys | NO PATH TO OCCUR | Same as above |

THE REAL DEFECT the todo was circling: owned + REVEALED keys have no tab at all.
`selectGiftableSpares` takes `state === 'UNREVEALED'` only
(`src/common/humble/viewFilters.ts:76`), so those keys appear ONLY under the All
tab's `Revealed` heading, permanently, with no settle path. Phase 42 plans 03 and
06 close that.

TODO FRONTMATTER CONVENTION — `resolves_phase` is a quoted string naming the
plan(s) or phase, e.g. `resolves_phase: "41-01, 41-05"`. Per project memory, an
ABSENT `resolves_phase` is silently missed by the autoclose tooling — this todo
currently has no such field and must gain one.

A1 — THE ASSUMPTION UNDER TEST. `key_type === 'gog'` is EVIDENCED, not observed.
Basis: Playnite's `keyTypeWhitelist` plus GOG Galaxy's `KEY_TYPE` enum, recorded
at `src/backend/humble/__tests__/fixtures/tpks.ts:459`. The operator's
`~/Library/Application Support/gamelib/store_cache/humble_library.json` was
clobbered to `{}` on 2026-09-07 12:29 (the known tests-clobber-real-stores
defect), so no GOG key has ever been read live. The operator bought a GOG Humble
game on 2026-09-07, satisfying the todo's own revisit gate. A sync must be run
before this phase closes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Strike the false premise and record how it was measured</name>
  <files>.planning/todos/pending/2026-08-23-humble-integrated-activation-reconcile-key-state-with-steam-.md</files>
  <action>
    Edit the todo in place. Do NOT delete it, do NOT move it to `completed/` in
    this task — its status is decided in Task 2 once the live check lands.

    1. Replace the two false paragraphs in `## Problem` — the opening sentence
       "...so GameLib keeps presenting it in Keys-waiting with an actionable
       **Activate** button" and the whole numbered cost (1) about spending real
       Steam activation attempts / rate limits / "Any batch or 'activate all'
       affordance MUST serialize and stop on the first `rate-limited`" — with a
       new subsection titled exactly:

           ### CORRECTION 2026-09-07: the original Activate-button premise was FALSE

       Reproduce the four-row verdict table from this plan's `<interfaces>`
       block verbatim, including every file:line citation and the `5bfc2cb3d`
       commit sha and date. Add one sentence stating that the claim was already
       false when the todo was written, and one stating that no batch-activation
       or rate-limit-serialization requirement follows from it.

    2. PRESERVE, do not delete:
       - the 2026-08-23 census table (33 orders / 32 keys; 31 steam, 1 generic,
         0 gog; 20 REVEALED steam keys as 13 exact / 5 fuzzy / 2 unowned). Add a
         note that it is the ONLY measurement that exists and cannot be re-taken,
         because the live cache was clobbered to `{}` on 2026-09-07 12:29.
       - the "Current behaviour is SAFE, just noisy" heading and its
         `EPurchaseResult.AlreadyOwned` self-heal description — that part is
         still true and is why this was never a data-loss bug.
       - the entire "Also in scope: per-platform redeem deep links" section and
         the "Watch out for" generic-platform warning. Both were correct and both
         were implemented.

    3. Add a new subsection recording the real defect and what shipped:

           ### The real defect, and what Phase 42 shipped

       State that owned + REVEALED keys had no tab at all
       (`viewFilters.ts:62` excludes them from Keys-waiting; `:76` takes
       `UNREVEALED` only for Giftable Spares), so they sat under All-keys'
       `Revealed` heading permanently with no settle path. Then list what landed,
       one line each with its plan number: the pure `key_type` table (42-01), the
       provenance schema (42-02), the exact-match auto-settle with durable undo
       (42-03), the row store indicator (42-04), the GOG deep link (42-05), the
       reachable Undo in the All tab (42-06). Note that the Undo affordance did
       NOT already render in the All tab — that was found during planning and
       closed by 42-06 — so a future reader does not assume D-42-01 shipped for
       free.

    4. Frontmatter: add `resolves_phase: "42"`. Per project memory an absent
       `resolves_phase` is silently missed by the autoclose tooling. Leave
       `status: OPEN` for now; Task 2 sets the final value.

    Do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md`. The orchestrator
    owns those files and the `gsd-sdk state.*` / `roadmap.*` / `phase.*` verbs are
    a recorded corruption vector in this repo — do not run any of them.
  </action>
  <verify>
    <automated>F=.planning/todos/pending/2026-08-23-humble-integrated-activation-reconcile-key-state-with-steam-.md; grep -q "CORRECTION 2026-09-07" "$F" &amp;&amp; grep -q "viewFilters.ts:62" "$F" &amp;&amp; grep -q "5bfc2cb3d" "$F" &amp;&amp; grep -q 'resolves_phase' "$F" &amp;&amp; grep -q "Watch out for" "$F" &amp;&amp; grep -q "AlreadyOwned" "$F" &amp;&amp; ! grep -q "MUST serialize and stop on the first" "$F" &amp;&amp; echo OK</automated>
  </verify>
  <done>The correction subsection exists with all four citations; the census, self-heal and deep-link sections survive; the rate-limit-serialization requirement sentence is gone; `resolves_phase` is present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live confirmation — GOG key_type, both themes, no layout shift</name>
  <files>(none — verification only; no file is modified by this task)</files>
  <action>
    STOP. Present the checkpoint below to the operator and WAIT for their reply.
    Do not answer any of the six items yourself, do not infer an outcome from the
    test suite, and do not auto-advance. Per project memory, auto-mode has
    previously answered its own human-verify gate and fabricated the outcome; this
    plan is `autonomous: false` and this gate is `blocking` precisely for that
    reason. `workflow.auto_advance` is `false` in `.planning/config.json` and must
    not be overridden here.

    Before presenting, run the <verify> command so the operator is not asked to
    inspect a red build.

    After the operator replies, record their six answers verbatim in the summary
    and set the todo's `status:` per the <done> criteria. If A1 came back with a
    string other than `gog`, do NOT edit the table in this task — record the exact
    one-key follow-up edit needed in `src/common/humble/keyTypePresentation.ts`
    and let the orchestrator decide whether it lands as a gap-closure plan or a
    quick task.
  </action>
  <what-built>
    Phase 42 shipped: a pure `key_type` presentation table, a store logo + proper
    name on every Humble key row, a GOG redeem deep link, automatic settling of
    exact-match owned+revealed keys with a reachable Undo, and a corrected owning
    todo.

    Four things cannot be proved by any automated test in this repo and need your
    eyes. Two of them are assumptions the whole GOG half rests on.
  </what-built>
  <how-to-verify>
    PREREQUISITE — run a Humble sync first. Your
    `~/Library/Application Support/gamelib/store_cache/humble_library.json` was
    clobbered to `{}` on 2026-09-07 12:29, so GameLib currently has no key data at
    all. Nothing below is meaningful until a sync has repopulated it. The mode-600
    overlays (`humble_local_redeemed.json`, `humble_revealed.json`) survived and
    will re-apply.

    1. A1 — CONFIRM `key_type === 'gog'` IS WHAT HUMBLE ACTUALLY SENDS.
       This has never been observed live; it was assumed from Playnite's
       `keyTypeWhitelist` and GOG Galaxy's `KEY_TYPE` enum. You bought a GOG
       Humble game on 2026-09-07, so it should now be in the sync.
       - After the sync, run:
         `python3 -c "import json;d=json.load(open('$HOME/Library/Application Support/gamelib/store_cache/humble_library.json'));print(sorted({k['platform'] for e in d.values() for k in e.get('keys',[])}))"`
       - Report the exact list of platform strings it prints.
       - If `gog` is present: A1 is CONFIRMED. Say so.
       - If a DIFFERENT string appears for your GOG game (e.g. `gog_keyless`,
         `gogcom`): report the exact string. Only ONE key in
         `src/common/humble/keyTypePresentation.ts`'s map changes — the structure
         does not. That is a one-line follow-up, not a redesign.
       - If the sync produced no GOG key at all: say so, and A1 stays
         UNCONFIRMED. That is an acceptable outcome; it must be recorded, not
         guessed at.

    2. THE FOUR DISPLAY NAMES YOU HAVE NOT SEEN. The planner chose these for
       platforms with no logo asset in the repo. They are display-only — each is a
       one-key edit and no behaviour depends on the string. Approve or correct:
       - `uplay` -> "Ubisoft Connect"
       - `battlenet` -> "Battle.net"
       - `origin` / `origin_keyless` -> "Origin"
       - `nintendo_direct` -> "Nintendo"

    3. BOTH THEMES — do not eyeball only one. Open the Humble Keys screen (All
       tab). Switch between a LIGHT and a DARK GameLib theme. For each:
       - Is the Steam / GOG / Epic logo visible and correctly coloured in the row
         caption? The store SVGs are imported `?react` and inherit `currentColor`
         only if the SVG does not hard-code its fill — a logo that vanishes or
         goes invisible on one theme is the expected failure mode here.
       - Report per theme, not as one verdict.

    4. NO LAYOUT SHIFT — MEASURE, do not judge by eye. The repo has a recorded
       lesson that "no layout shift" claims from screenshots were wrong twice.
       In the Tauri web inspector, compare a row WITH a logo (a Steam key) against
       a row WITHOUT one (a `generic` or `uplay` key):
       - `getComputedStyle(row).height` for both — must be equal.
       - `document.querySelector('.humbleKeyRowCaption').getBoundingClientRect().left`
         for both — must be equal (the reserved glyph box is doing its job).
       - Report the four numbers.

    5. THE GOG DEEP LINK, END TO END — only if step 1 found a GOG key.
       Open the claim wizard on it, reveal, and click the activation button. It
       should open `https://www.gog.com/redeem/<your code>` in your browser with
       the code prefilled. Report whether GOG's page accepted the prefill.
       Do NOT paste the key value into this session — just say yes or no.

    6. THE AUTO-SETTLE AND ITS UNDO — the reason this phase exists.
       - After the sync, check All-keys' `Redeemed` heading. Any of your
         exact-match owned+revealed Steam keys should now be there, each showing
         "Already in your Steam library" and an Undo button. How many settled?
       - Click Undo on one. It should return to the `Revealed` heading.
       - Now sync AGAIN. It must STAY in `Revealed` — it must not re-settle. This
         is the durability guard from plan 42-03; if it re-settles, that guard is
         broken and the Undo is cosmetic.
       - Confirm none of your FUZZY-matched keys settled (the 5 from the
         2026-08-23 census). They must still be REVEALED and must still offer the
         "Not the same game" override.
  </how-to-verify>
  <resume-signal>
    Reply with: the platform-string list from step 1; approve/correct for the four
    names in step 2; a per-theme verdict for step 3; the four measured numbers for
    step 4; yes/no for step 5; and the settled count plus the re-sync durability
    result for step 6. Then type "approved" — or describe what is wrong.
  </resume-signal>
  <verify>
    <automated>npx jest --selectProjects Backend src/backend/humble &amp;&amp; npx jest --selectProjects Frontend src/frontend &amp;&amp; npx tsc --noEmit &amp;&amp; echo SUITES-GREEN-PENDING-HUMAN</automated>
  </verify>
  <done>The operator has answered all six items. The answers are recorded verbatim in the summary, and `status:` on the todo is set from the outcome (`completed` if steps 1, 5 and 6 all passed; otherwise left OPEN with the specific residue named).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator terminal -> session transcript | The live-verification steps read the operator's real Humble cache, which contains revealed key values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-27 | Information disclosure | a real key value pasted into the session transcript during live verification | mitigate | Step 1's command projects ONLY the `platform` field via a set comprehension — it never prints a key value, a title, or a `redeemed_key_val`. Step 5 explicitly instructs the operator to report yes/no rather than paste the key. No step asks for the contents of `humble_revealed.json` or `humble_local_redeemed.json`. |
| T-42-28 | Repudiation | the A1 assumption silently promoted to fact | mitigate | Step 1 has three enumerated outcomes — CONFIRMED, DIFFERENT-STRING, or UNCONFIRMED — and the third is explicitly declared an acceptable answer that must be recorded. The gate is `blocking`; the executor cannot answer it on the operator's behalf. |
| T-42-29 | Tampering | the todo's disproof itself being wrong | mitigate | Every claim in the correction carries a file:line plus, for the decisive one, a commit sha and date, so the next reader can re-run the measurement rather than trust the prose. Task 1's verify greps for each citation. |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install in this plan. No new dependency. No `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<verification>
- `autonomous: false` — this plan contains a `checkpoint:human-verify` with
  `gate="blocking"`. Per project memory, auto-mode has previously ANSWERED its own
  human-verify gate and fabricated the outcome. The executor must stop and wait.
  Do not synthesise operator answers.
- Full suite green before the checkpoint is presented: Backend Humble slice,
  whole Frontend project, `tsc --noEmit`.
- `pnpm lint-translations:gamelib`, `pnpm i18n-churn-guard` and
  `pnpm i18n --fail-on-update` pass across the phase's two new catalog keys
  (`humbleKeys.platformOther`, `humbleKeys.openStore`) plus 42-06's
  (`humbleKeys.settledFromOwnership`).
- `git diff --quiet -- public/locales/en/translation.json` across the whole phase.
- Do NOT run `gsd-sdk state.*`, `roadmap.*` or `phase.*`. STATE.md and ROADMAP.md
  are the orchestrator's to write.
</verification>

<success_criteria>
- The owning todo no longer asserts owned keys appear in Keys-waiting with an
  Activate button, and carries the four-row verdict table with citations.
- The census, the AlreadyOwned self-heal note, the deep-link section and the
  generic "Watch out for" warning all survive.
- `resolves_phase: "42"` is present.
- The operator has answered all six live-verification items and the answers are
  recorded verbatim.
- A1 is recorded as CONFIRMED, CORRECTED (with the real string), or UNCONFIRMED —
  never assumed.
</success_criteria>

<output>
Create `.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-07-SUMMARY.md` when done.
Record the operator's six answers verbatim, the final `status:` set on the todo,
and — if A1 came back with a different string — the exact one-key follow-up edit
required in `src/common/humble/keyTypePresentation.ts`.
</output>
