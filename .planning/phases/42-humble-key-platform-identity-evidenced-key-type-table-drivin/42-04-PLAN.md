---
phase: 42-humble-key-platform-identity-evidenced-key-type-table-drivin
plan: 04
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/frontend/jest.config.js
  - src/frontend/__mocks__/svgReactStub.tsx
  - src/frontend/__tests__/svgReactResolution.test.tsx
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx
  - public/locales/en/gamelib.json
autonomous: true
requirements: [REQ-42-02]

must_haves:
  truths:
    - "A Steam key's row caption shows the Steam logo and the word 'Steam', not the lowercase token 'steam'."
    - "A GOG key's row caption shows the GOG logo and 'GOG'."
    - "A uplay/battlenet/origin/nintendo_direct key's row shows its proper name with NO logo element."
    - "A generic or unrecognised key_type shows a neutral 'Other' label with NO logo and NO fabricated name — and never the GameLib icon."
    - "The row's `{{origin}}` half (the bundle name) is unchanged in every case."
    - "The existing Waiting suite still passes with zero edits — the new SVG import did not break module resolution for it."
  artifacts:
    - path: "src/frontend/__mocks__/svgReactStub.tsx"
      provides: "Jest stand-in for vite's `?react` SVG-as-component imports"
    - path: "src/frontend/jest.config.js"
      provides: "moduleNameMapper routing *.svg?react to the stub"
      contains: "moduleNameMapper"
    - path: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      provides: "Table-driven store indicator replacing the raw key_type token"
      contains: "getKeyTypePresentation"
    - path: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx"
      provides: "First-ever HumbleKeyRow render coverage, pinning all four indicator branches"
      min_lines: 120
  key_links:
    - from: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      to: "common/humble/keyTypePresentation"
      via: "import alongside the existing getExpirationDisplay / UrgencyTier siblings"
      pattern: "from 'common/humble/keyTypePresentation'"
    - from: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      to: "public/locales/en/gamelib.json"
      via: "tGamelib('gamelib:humbleKeys.platformOther', 'Other') for the unknown branch"
      pattern: "humbleKeys.platformOther"
---

<objective>
Replace `HumbleKeyRow`'s raw lowercase `key_type` token with a table-driven store
indicator: logo (when one exists) plus proper display name (D-42-03, CONTEXT.md
scope item 2). Today the row literally reads "steam · Humble RPG Bundle".

Purpose: the store a key belongs to is never presented as such. This is the
user-visible half of the phase, and it is the first ever render coverage for
`HumbleKeyRow` — no test currently pins its caption at all.

Output: a jest-resolvable SVG import path, the rewritten caption, one new i18n
string in the fork-owned catalog, and a new suite pinning all four branches
including the explicit unknown case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-CONTEXT.md
@.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-PATTERNS.md
@.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-01-SUMMARY.md
@src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
@src/frontend/jest.config.js
</context>

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

FROM PLAN 42-01 (already landed):

    // src/common/humble/keyTypePresentation.ts
    export type HumbleStoreLogoId = 'steam' | 'gog' | 'epic'
    export type HumbleKeyTypePresentation =
      | { kind: 'branded'; name: string; logo: HumbleStoreLogoId }
      | { kind: 'named'; name: string }
      | { kind: 'unknown' }
    export function getKeyTypePresentation(keyType: string): HumbleKeyTypePresentation

THE EXACT CAPTION TO REPLACE — `HumbleKeyRow/index.tsx:228-235`:

    {!isUnpicked && (
      <span className="humbleKeyRowCaption">
        {t('humbleKeys.rowCaption', '{{platform}} · {{origin}}', {
          platform: humbleKey.platform,
          origin: humbleKey.origin
        })}
      </span>
    )}

`humbleKey.platform` is the raw lowercase `key_type` (`classify.ts:440`).
`humbleKey.origin` is the BUNDLE NAME (e.g. "Humble RPG Bundle") — it is NOT the
EA store. Beware: `origin` is simultaneously a `key_type` value AND a HumbleKey
field name. D-42-03 changes only how `platform` renders; `{{origin}}` is untouched.

THE i18n BINDING ALREADY PRESENT — `HumbleKeyRow/index.tsx:64-68`:

    const { t } = useTranslation()
    const { t: tGamelib } = useTranslation('gamelib')

`tGamelib` is declared but the caption at `:230` currently calls plain `t`.
Namespace-prefixed call style used elsewhere in this feature
(`HumbleClaimWizard/index.tsx:388`): `tGamelib('gamelib:humbleKeys.activatedTitle', 'Activated')`.

CATALOG FACTS, MEASURED — `humbleKeys.rowCaption` currently lives in
`public/locales/en/translation.json`, NOT `gamelib.json`. `translation.json` is
upstream-owned and CI fails on any write to it.

THE SVG-AS-COMPONENT CONVENTION — `src/frontend/components/UI/StoreLogos/index.tsx:2-7`:

    import EpicLogo from 'frontend/assets/epic-logo.svg?react'
    import GOGLogo from 'frontend/assets/gog-logo.svg?react'
    import SteamLogo from 'frontend/assets/steam-logo.svg?react'
    // ... then <SteamLogo className={className} />

Do NOT route through `StoreLogos`. Its `default` branch returns the GameLib icon
(`<img src={GameLibIcon} ... alt="GameLib" />`) and its prop is `Runner`
(`legendary | gog | sideload | nile | zoom | steam`), which has no member for
`origin`/`uplay`/`battlenet`/`nintendo_direct`/`epic_keyless`. D-42-03 names that
`default` branch as "the trap". Do not extend `Runner`.

ASSET INVENTORY, MEASURED (`ls src/frontend/assets/*logo*`): `epic-logo.svg`,
`gog-logo.svg`, `steam-logo.svg`, `amazon-logo.svg`, `zoom-logo.svg`. There is NO
`uplay-logo.svg`, `battlenet-logo.svg`, `origin-logo.svg` or `nintendo-logo.svg`.
The no-logo branch is the only correct rendering for those, not a shortcut.

THE D-22 READ-ONLY CONTRACT — `HumbleKeyRow/index.tsx:43-56`, enumerating exactly
three sanctioned interactive exceptions (`undoOverride`, `giftAction`,
`claimAction`). Per CONTEXT.md, the store indicator is PRESENTATIONAL and needs
NO new exception: it adds no click handler, no button, no link, no cursor:pointer,
and no callback-bearing prop.

FRONTEND JEST HARNESS FACTS, MEASURED:
- `src/frontend/jest.config.js` has `displayName: 'Frontend'`,
  `testEnvironment` defaults to `node`, and has NO `moduleNameMapper` at all.
- No jsdom / react-test-renderer is installed. Component tests mock `'react'` and
  `'react-i18next'` at module level and invoke the component as a plain function,
  inspecting the returned element graph. See
  `HumbleClaimWizard/__tests__/index.test.tsx:1-60` for the harness, and
  `Waiting/__tests__/index.test.tsx` for the `textContent`/`findByClassNamePart`
  helpers.
- `Waiting/__tests__/index.test.tsx:22` imports the REAL `HumbleKeyRow` module
  (`import HumbleKeyRow from '../../components/HumbleKeyRow'`) — it is not
  `jest.mock`ed. So any unresolvable import added to `HumbleKeyRow` breaks that
  currently-green suite.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make `*.svg?react` imports resolvable under the Frontend jest project (RED first)</name>
  <files>src/frontend/__tests__/svgReactResolution.test.tsx, src/frontend/__mocks__/svgReactStub.tsx, src/frontend/jest.config.js</files>
  <behavior>
    - A test file can `import SteamLogo from 'frontend/assets/steam-logo.svg?react'`
      and the import resolves to a callable React component under the Frontend
      jest project.
    - Rendering it as `<SteamLogo className="x" />` yields an element whose
      `props.className === 'x'`.
  </behavior>
  <action>
    THIS IS A HARD PREREQUISITE, not housekeeping. `HumbleKeyRow` is about to gain
    `*.svg?react` imports, and `Waiting/__tests__/index.test.tsx:22` imports the
    real `HumbleKeyRow` module. The Frontend jest project has no
    `moduleNameMapper`, `moduleFileExtensions` does not include `svg`, and the
    `?react` query suffix makes the specifier unresolvable to ts-jest. Without
    this task, adding the import turns a currently-green suite red.

    RED FIRST. Create `src/frontend/__tests__/svgReactResolution.test.tsx` with
    the two assertions above. Run it BEFORE touching the config and CAPTURE the
    failure — it must be a "Cannot find module 'frontend/assets/steam-logo.svg?react'"
    resolution error. Record that output in the summary; it is the proof that the
    mapper is load-bearing rather than defensive.

    THEN create `src/frontend/__mocks__/svgReactStub.tsx`: a default-exported
    function component taking `{ className?: string }` and returning a `<span>`
    carrying that className plus a stable `data-testid` (e.g. `svg-stub`) so tests
    can find it in the element graph. Document at the top of the file WHY it
    exists — vite's `vite-plugin-svgr` `?react` suffix (declared for tsc via
    `src/common/typedefs/vite.d.ts`'s `/// <reference types="vite-plugin-svgr/client" />`)
    has no jest equivalent, and the project deliberately installs no jsdom or SVG
    transformer.

    THEN add to `src/frontend/jest.config.js`:

        moduleNameMapper: {
          '\\.svg\\?react$': '<rootDir>/src/frontend/__mocks__/svgReactStub.tsx'
        }

    Add a comment above it in the same explanatory style as that file's existing
    header docstring, citing Phase 42 and naming the consumer
    (`HumbleKeyRow`'s D-42-03 store indicator).

    Two things to check and report:
    - Confirm `<rootDir>` in this config is the REPO root (`rootDir: '../..'`), so
      the mapper target path must be `<rootDir>/src/frontend/__mocks__/...`, not
      `<rootDir>/__mocks__/...`.
    - Confirm the new stub file does not itself get collected as a test. The
      project's `testMatch` is `['**/__tests__/**/*.test.tsx', '**/__tests__/**/*.test.ts']`,
      and `__mocks__/` is a different directory, so it will not — but verify the
      run's test count did not change unexpectedly.

    Do NOT install any package. Do NOT add jsdom, `jest-svg-transformer`, or
    `jest-transformer-svg`. A moduleNameMapper needs no dependency.
  </action>
  <verify>
    <automated>npx jest --selectProjects Frontend src/frontend/__tests__/svgReactResolution.test.tsx &amp;&amp; npx jest --selectProjects Frontend src/frontend/screens/Humble &amp;&amp; grep -q "moduleNameMapper" src/frontend/jest.config.js &amp;&amp; echo OK</automated>
  </verify>
  <done>The resolution test passes; the pre-existing Frontend Humble suites still pass with zero edits; the captured RED resolution error is in the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Render the table-driven store indicator in the row caption</name>
  <files>src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx, public/locales/en/gamelib.json</files>
  <action>
    Import the three logos the same way `StoreLogos` does, directly in this file:

        import SteamLogo from 'frontend/assets/steam-logo.svg?react'
        import GOGLogo from 'frontend/assets/gog-logo.svg?react'
        import EpicLogo from 'frontend/assets/epic-logo.svg?react'

    and import the table alongside the existing `common/humble` siblings at
    `:6-7`:

        import { getKeyTypePresentation } from 'common/humble/keyTypePresentation'

    Follow the file's existing import grouping (external libs, then `common/`,
    then local relative).

    Resolve once, near the other derived locals around `:88-100`:

        const presentation = getKeyTypePresentation(humbleKey.platform)

    Then rewrite the caption block at `:228-235`. Requirements:

    - Keep `t('humbleKeys.rowCaption', '{{platform}} · {{origin}}', { platform, origin })`
      as the TEXT, passing the resolved DISPLAY NAME as `platform` and
      `humbleKey.origin` unchanged as `origin`. Do not edit
      `public/locales/en/translation.json` — the key already exists there, the
      English template is unchanged, and CI fails on any write to that file.
      Only the interpolated value changes.

    - Resolve the display name with an EXHAUSTIVE branch over
      `presentation.kind`. All three members must be handled explicitly and there
      must be NO `default:` clause and no `??`/`||` fall-through supplying a name:
        * `'branded'` -> `presentation.name`, plus the matching logo element
        * `'named'`   -> `presentation.name`, NO logo element
        * `'unknown'` -> `tGamelib('gamelib:humbleKeys.platformOther', 'Other')`,
                         NO logo element
      Add a compile-time exhaustiveness guard (assign the fall-through value to a
      `const _exhaustive: never`) so a future fourth member fails `pnpm codecheck`
      rather than silently rendering nothing.

    - Map `presentation.logo` to a component with an explicit three-case lookup
      over `HumbleStoreLogoId` ('steam' | 'gog' | 'epic'). Never fall back to the
      GameLib icon; never route through `StoreLogos`.

    - Render the logo INLINE inside the existing `humbleKeyRowCaption` span, in a
      dedicated wrapper element with its own className (e.g.
      `humbleKeyRowStoreLogo`), placed before the text. Per CONTEXT.md's deferred
      UI notes: reserve the glyph box so the caption text does not shift
      horizontally between logo and no-logo rows, and do not change the row
      height — the caption line already carries `{{platform}} · {{origin}}` and
      must not reflow the urgency badge or expiration columns.

      There is no colocated stylesheet for `HumbleKeyRow`. Find where
      `.humbleKeyRowCaption` is defined and add the sibling rules there, next to
      it — do not create a new stylesheet for this component and do not inline
      styles. If reserving the box requires a fixed dimension, derive it from the
      caption's existing line-height rather than inventing a magic number.

    - Give the logo element an accessible name. The SVGs are decorative next to
      the adjacent text name, so mark them `aria-hidden` and let the display name
      carry the meaning — do NOT duplicate the store name into an `alt`/`title`
      that a screen reader would then read twice.

    - `isUnpicked` rows keep rendering no caption at all (`!isUnpicked` guard
      unchanged) — a Choice-month pseudo-entry has no key_type to present.

    Add the ONE new string to `public/locales/en/gamelib.json` under
    `humbleKeys.platformOther` with value `"Other"`. Nothing else goes in a
    catalog: the store brand names live as plain TS literals in
    `common/humble/keyTypePresentation.ts` (they are do-not-translate proper
    nouns per `meta/i18nGlossary.json`, and that file is not in
    `meta/i18nGateScope.json`).

    D-22 NOTE — write this as a short comment directly above the new indicator
    markup so a reviewer is not left guessing: this indicator is PRESENTATIONAL
    and therefore needs NO fourth sanctioned exception in the `:43-56` contract
    block. It adds no click handler, no button or link element, no
    cursor:pointer, and no callback-bearing prop. The contract block itself is
    NOT edited by this plan. (Plan 42-06 does add a fourth exception, for an
    interactive affordance — that is a separate, enumerated change.)
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx jest --selectProjects Frontend src/frontend/screens/Humble &amp;&amp; python3 -c "import json;d=json.load(open('public/locales/en/gamelib.json'));assert d['humbleKeys']['platformOther']=='Other'" &amp;&amp; git diff --quiet -- public/locales/en/translation.json &amp;&amp; grep -v '^\s*[/*]' src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx | grep -qv "StoreLogos" &amp;&amp; echo OK</automated>
  </verify>
  <done>The row renders logo + proper name; `translation.json` is untouched (`git diff --quiet` passes); `gamelib.json` has exactly one new key; no `StoreLogos` import; `tsc` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin all four indicator branches with the first HumbleKeyRow suite (RED first)</name>
  <files>src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx</files>
  <behavior>
    Table-driven over `platform` (`it.each`), asserting on the returned element graph:
    - 'steam'           -> caption text contains 'Steam · Humble RPG Bundle'; a logo element is present
    - 'gog'             -> contains 'GOG · Humble RPG Bundle'; a logo element is present
    - 'epic'            -> contains 'Epic Games · '; a logo element is present
    - 'epic_keyless'    -> contains 'Epic Games · '; a logo element is present
    - 'uplay'           -> contains 'Ubisoft Connect · '; NO logo element
    - 'battlenet'       -> contains 'Battle.net · '; NO logo element
    - 'origin'          -> contains 'Origin · Humble RPG Bundle'; NO logo element
                           (also asserts the `{{origin}}` bundle-name half is
                           still the bundle name and was not overwritten by the
                           same-named key_type — the field-name collision trap)
    - 'nintendo_direct' -> contains 'Nintendo · '; NO logo element
    - 'generic'         -> contains 'Other · '; NO logo element
    - 'wibble' (unrecognised) -> contains 'Other · '; NO logo element

    Direct negative pins (D-42-03's named trap), asserted for EVERY platform above:
    - The rendered graph contains no element whose props reference `gamelib-icon`
      or an `alt` of `'GameLib'`.
    - The raw lowercase token never appears in the caption for a mapped platform:
      for 'steam', assert `!caption.includes('steam ·')` (lowercase s) while
      `caption.includes('Steam ·')` holds. This is the actual defect and it needs
      a direct pin, not an implied one.
    - For the two unknown cases, assert the caption contains NO platform-looking
      proper noun — neither 'Generic' nor 'Wibble' nor the raw input string.

    Structural pins:
    - `state: 'UNPICKED'` renders no caption span at all.
    - With no `claimAction`, `giftAction` or `undoOverride` prop, the rendered
      graph contains ZERO elements of type `'button'`. This pins the D-22
      read-only contract against the new indicator: the store indicator adds no
      interactivity.
  </behavior>
  <action>
    Create `src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx`
    — the first test file this component has ever had.

    Use the established no-DOM harness: mock `'react-i18next'`'s `useTranslation`
    to return a `t` that interpolates `{{...}}` into the supplied default string
    (copy the implementation from `HumbleClaimWizard/__tests__/index.test.tsx:39-51`),
    and make it handle BOTH `useTranslation()` and `useTranslation('gamelib')` —
    `HumbleKeyRow` calls both. Reuse the `textContent` / `findByClassNamePart`
    element-graph walkers from the sibling suites rather than writing new ones;
    if they are not exported, copy them with a comment naming the source file.

    `HumbleKeyRow` has no `useState`/`useEffect`, so no `'react'` mock and no
    slot harness is needed — invoke the component directly as a plain function
    and inspect its returned element graph. Say so in the file docblock, and also
    state why the suite lives here rather than in the backend project (unlike the
    pure `common/humble` helpers, this is a frontend component and belongs to the
    `Frontend` jest project).

    Detect logo presence via the `data-testid` on the Task 1 SVG stub, not by
    counting elements — the mapper substitutes the stub for every `?react` import.

    RED FIRST is not possible for the whole suite (Task 2 already landed the
    implementation), so instead prove the pins BITE: after the suite is green,
    temporarily revert the caption's `platform` argument to `humbleKey.platform`
    (the raw token), re-run, CAPTURE the failures, then restore. Record that
    captured output in the summary. A test that would pass against the old code
    proves nothing about this phase.
  </action>
  <verify>
    <automated>npx jest --selectProjects Frontend src/frontend/screens/Humble/Keys/components/HumbleKeyRow &amp;&amp; npx jest --selectProjects Frontend src/frontend/screens/Humble 2>&1 | tee /tmp/gsd-42-04.txt; grep -qE "Tests:.*[1-9][0-9]* passed" /tmp/gsd-42-04.txt &amp;&amp; echo OK</automated>
  </verify>
  <done>All eleven platform rows plus the negative and structural pins pass. The bite-proof revert produced real failures and was captured before restoring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Humble API -> renderer | `humbleKey.platform` is an attacker-influenceable remote string rendered directly into the UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-13 | Spoofing | a hostile `key_type` impersonating a store in the caption | mitigate | The rendered name comes exclusively from the closed lookup table in `keyTypePresentation.ts`; an unmapped value renders the neutral `'Other'` label and never the raw string. Pinned by the `'wibble'` row asserting the input string does not appear. |
| T-42-14 | Spoofing | a hostile `key_type` acquiring a store LOGO it is not entitled to | mitigate | `presentation.logo` is typed `HumbleStoreLogoId` ('steam' \| 'gog' \| 'epic') and is only present on the `'branded'` union member, which only the closed table can produce. The GameLib-icon `default` fall-through from `StoreLogos` is structurally unreachable because `StoreLogos` is not imported — pinned by a grep and by the no-`gamelib-icon` assertion on every row. |
| T-42-15 | Tampering | React injection through the interpolated display name | accept | React escapes text children by default and neither the table nor this component uses `dangerouslySetInnerHTML`. The interpolated value is a compile-time literal from the table, never the remote string. |
| T-42-16 | Elevation of privilege | the presentational change quietly widening the D-22 read-only contract | mitigate | The indicator adds no handler, element or callback prop. Pinned structurally: with no action props supplied, the rendered graph contains zero `'button'` elements. The `:43-56` contract block is not edited by this plan. |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install in this plan. The jest SVG problem is solved with a `moduleNameMapper` and a local stub — explicitly NOT by adding `jest-svg-transformer`, `jest-transformer-svg` or jsdom. No `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<verification>
- `npx jest --selectProjects Frontend src/frontend` — the whole Frontend project
  is green. Confirm the total test count went UP (new suites) and no previously
  passing suite was edited.
- `npx tsc --noEmit` clean; `pnpm lint` no new warnings.
- `pnpm prettier` DRY-RUN before assuming formatting is clean (the repo's
  pre-push runs it and it has been red repo-wide before).
- `git diff --quiet -- public/locales/en/translation.json` must pass — any write
  to the upstream-owned catalog fails CI.
- `pnpm lint-translations:gamelib` and `pnpm i18n-churn-guard` both pass with the
  new `humbleKeys.platformOther` key. `pnpm i18n --fail-on-update` (the pre-push
  gate) must not want to add or remove anything.
- `HumbleKeyRow/index.tsx` is already listed in `meta/i18nGateScope.json:99`;
  confirm the i18n gate still passes for it after the `tGamelib` call is added.
</verification>

<success_criteria>
- Steam and GOG rows show a logo plus 'Steam'/'GOG'.
- Epic and epic_keyless show the Epic logo plus 'Epic Games'.
- origin, origin_keyless, uplay, battlenet, nintendo_direct show proper names with NO logo.
- generic and unrecognised values show 'Other' with NO logo and no fabricated name.
- The GameLib icon appears nowhere in any branch.
- `{{origin}}` (bundle name) is unchanged in every case.
- `translation.json` untouched; exactly one new key in `gamelib.json`.
- The pre-existing `Waiting` and `HumbleClaimWizard` suites pass with zero edits.
- The bite-proof and the SVG-resolution RED were both captured.
</success_criteria>

<output>
Create `.planning/phases/42-humble-key-platform-identity-evidenced-key-type-table-drivin/42-04-SUMMARY.md` when done.
Record: the captured SVG-resolution RED; the captured bite-proof failures; where
`.humbleKeyRowCaption` is defined and what CSS you added beside it; and an
explicit statement that the D-22 contract block was NOT edited and why none was
needed.
</output>
