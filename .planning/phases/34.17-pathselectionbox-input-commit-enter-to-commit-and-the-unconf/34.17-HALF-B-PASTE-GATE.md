---
phase: 34.17
gate: half-b-paste
requirement: REQ-34.17-04
status: authored
authored: 2026-08-25
run_at:
host:
verdict:
session_dir:
---

# 34.17 Half B — Paste Reproduction Gate

Author/runner separation is deliberate house practice here. This file was authored by an agent
that has NOT run it and does not know the answer. Every field under `## RUN RECORD` below is a
literal `TBD-RUN` placeholder. A human runs the protocol on a real macOS Tauri host and fills those
fields from that run; nothing above the `## RUN RECORD` heading is touched by the runner.

## 1. What is being tested, and what is NOT

This gate settles one question only: **does pasting into `PathSelectionBox` reproduce the
2026-08-11 ledger report of a repeating unrenderable glyph replacing the clipboard text?** It does
not settle *why*, and it does not fix anything. Both a clean reproduction attempt (`VERIFIED-ABSENT`)
and an actual reproduction (`REPRODUCED-HOST-LEVEL` / `REPRODUCED-LOCAL`) are complete, passing
outcomes of this gate. Nothing in this document can be satisfied only by the defect existing.

The **Enter-to-commit half is separate work**, owned by `34.17-01` (already landed) and `34.17-02`.
Nothing observed by this gate may be used to score the Enter half, and nothing recorded here makes
any claim about it.

## 2. Candidate causes — ranked, unconfirmed hypotheses, not conclusions

Every candidate below is an unconfirmed hypothesis. None of them is asserted as a conclusion by
this document. The protocol's job is to discriminate between them, not to pick a winner in advance.

**Candidate A — `navigator.clipboard` silently no-opping under Tauri/WKWebView (the ledger's own
suspected cause).** This candidate is mechanically implausible for a native Cmd+V into a plain
`<input>`. Phase 34.3 (commit `44232d3e`) migrated every programmatic clipboard call site in this
codebase onto `window.api.clipboardWriteText` / `clipboardReadText`; zero `navigator.clipboard`
call sites remain anywhere in the tree. A native OS-level keyboard paste into a DOM `<input>` never
touches that API on any code path that exists today. **Falsifier:** none needed to rule this in —
it is already ruled out by absence of a connecting mechanism. It is recorded here only so a later
reader cannot resurrect it as the explanation without re-deriving this absence for themselves.

**Candidate B — the tao/wry Cmd+V key-equivalent delivery gap, already fixed once, but only for the
Epic login child window.** `src-tauri/src/main.rs` (function `open_pristine_epic_login_window`,
beginning at line 2210) carries a live-confirmed fix at lines 2363-2454. Its own comment records
the diagnostic: choosing Edit ▸ Paste from the macOS menu bar pastes correctly (proving the webview
is first responder and does handle `paste:`), but pressing Cmd+V does nothing, because tao's
`NSWindow` subclass consumes the raw Cmd+V `NSEvent` before AppKit's `performKeyEquivalent:`
main-menu traversal ever sees it. The installed fix is an `NSEvent` local monitor scoped, by its
own explicit comment, to that one window only — "every other event -- including these same
shortcuts in every OTHER window -- fall[s] through completely unchanged." The primary Tauri window,
where `PathSelectionBox` renders, is NOT covered by that fix. **Falsifier:** if Cmd+V does nothing
at all in the target field (no glyph, no text, field unchanged) AND Edit ▸ Paste from the menu bar
succeeds, that symptom shape matches Candidate B exactly. **This candidate predicts silence, not a
glyph** — the 2026-08-11 report describes a repeating unrenderable glyph, a different symptom
shape, which is itself evidence against a clean match to Candidate B.

**Candidate C — no defect, or a transient one-off from the original gate run.** Evidence pulling
this direction already exists: `Skill("spike-findings-gamelib")` records, for the Epic/Humble
login-window UX spikes (019-022), that Cmd+V paste from the Passwords app is one of the two working
Keychain-autofill channels observed in a WKWebView surface in this app. That does not settle the
primary window, but it does mean "Cmd+V is broken everywhere under wry" is not a safe prior.
**Falsifier:** the probe string appears correctly, character for character, in the target field
after Cmd+V.

## 3. Preconditions

Run each of the following, in order, capturing stdout/stderr and exit status into files under a
session directory rather than trusting terminal scrollback. Do not use a `tee` plus shell-array
exit-status idiom to capture the exit code — that idiom silently no-ops under zsh's default
options. Use instead:

```
<command> > "$SESSION_DIR/<name>.log" 2>&1; echo $? > "$SESSION_DIR/<name>.exit"
```

1. `mktemp -d /tmp/gamelib-34.17-halfb-XXXXXX` — the resulting absolute path is `$SESSION_DIR`, and
   is copied verbatim into this file's `session_dir` frontmatter field once run.
2. `git rev-parse HEAD` and `git status --porcelain`, both recorded — the record must state exactly
   what code was under test, because `34.17-01`/`34.17-02` may or may not be in the tree yet when
   this runs, and this plan does not wait for either.
3. `pgrep -fl gamelib-shell` — expected result: **no output**. `pgrep GameLib` does not match this
   process; do not substitute it. If a stale instance is running, fully quit it before continuing —
   `pnpm tauri:dev` exits 0 without replacing an already-running instance, so a stale build would
   silently pass this gate against unmodified code.
4. Launch `pnpm tauri:dev` (not `tauri dev`, which serves a stale static bundle), then re-run
   `pgrep -fl gamelib-shell` — expected result: exactly one process line.
5. `echo -n "/tmp/gamelib-paste-probe-12345" | pbcopy` then `pbpaste | xxd | head -3` — recorded, so
   the record proves what was actually on the clipboard rather than assuming it.
6. Navigate to Settings → Advanced, read `#setting-alt-legendary`'s current value, and record it so
   it can be restored afterward.

## RUN RECORD

Run at: TBD-RUN
Host: TBD-RUN
Session dir: TBD-RUN
Code under test: TBD-RUN

### Item 1 — target, Cmd+V

Focus `#setting-alt-legendary` (Settings → Advanced → Alternative Legendary Binary) by a single
click directly in the text field — not the folder icon. Press **Cmd+V**. This is the primary
observation; it matches the original 2026-08-11 report's gesture exactly and is not substitutable
by any other paste route. Record what appeared in the field, character for character. Do not
paraphrase as "garbled" — copy the actual characters, or capture them in the evidence screenshot,
or both.

Observed: TBD-RUN
Result: TBD-RUN

### Item 2 — target, DOM reading

Right-click the same field ▸ Inspect Element, and read the `<input>` node's `value` property
directly in the Elements panel (never paste into the Tauri DevTools console — it cannot be pasted
into). Record the value exactly. This separates "the characters are actually wrong" from "the
characters are right but render as an unrenderable glyph" — state explicitly which of those two it
is.

Observed: TBD-RUN
Result: TBD-RUN

### Item 3 — control C1, uncontrolled input (host-level discriminator)

Cmd+F to focus the library search bar (`#search`), press **Cmd+V**, record what appears. This
input has no `value` prop and no React `onChange` — it is fully uncontrolled, driven only by a
`ref` (`SearchBar/index.tsx:77-85`). If this control is ALSO wrong, no React-controlled-input
theory can explain it, and the defect is attributable to a host/shell-level mechanism, out of this
phase's scope fence.

Observed: TBD-RUN
Result: TBD-RUN

### Item 4 — control C2, TextInputField but not PathSelectionBox (component-layer discriminator)

Focus `#steamgriddb-api-key` (Settings → Advanced → SteamGridDB API Key), press **Cmd+V**, record
what appears. This field shares `TextInputField`'s hand-wired `element.value = value` plus
`addEventListener('input', ...)` architecture (`TextInputField/index.tsx:37-50`) but is not a
`PathSelectionBox`. Item 3 clean + this item wrong ⇒ the layer is `TextInputField`-wide. Item 3
clean + this item clean + item 1 wrong ⇒ the layer is `PathSelectionBox`-specific.

Observed: TBD-RUN
Result: TBD-RUN

### Item 5 — menu-bar discriminator (run LAST, only after items 1-4 are recorded)

Return to the target field (`#setting-alt-legendary`) and paste via **Edit ▸ Paste** from the
macOS menu bar. Record what appeared. This is the exact discriminator
`src-tauri/src/main.rs:2363` used to identify the tao key-equivalent gap on 2026-08-03: menu route
works while Cmd+V does not ⇒ key-equivalent delivery, which is host-level. **This item does not replace item 1** — item 1's keyboard gesture is what the original report describes and remains the
primary observation regardless of what this item shows.

Observed: TBD-RUN
Result: TBD-RUN

## 5. Evidence capture (mandatory, not optional)

- A screenshot of the target field immediately after item 1, saved into `$SESSION_DIR` and then
  copied into
  `.planning/phases/34.17-pathselectionbox-input-commit-enter-to-commit-and-the-unconf/evidence/34.17-halfb-target-cmdv.png`.
- If any anomalous rendering appears, do not attempt to describe its shape in prose — rely on the
  screenshot plus the item 2 DOM `value` reading, which is the exact byte-for-byte record of what
  the field actually holds.
- **Redaction rule, binding:** crop the screenshot to the field only. This is a public fork; do not
  let a wider capture leak a home-directory path, account name, or unrelated Settings content.

## 6. Verdict table

Exactly one row applies. If the observed combination matches none of the rows below, the verdict is
`INVALID` — record the combination verbatim and stop. Do not force the nearest-looking row.

| Item 1 (target, Cmd+V) | Item 3 (control C1) | Item 4 (control C2) | Item 5 (menu route) | Verdict |
|---|---|---|---|---|
| clean | — | — | — | `VERIFIED-ABSENT` |
| wrong | wrong | — | — | `REPRODUCED-HOST-LEVEL` |
| wrong | clean | — | works | `REPRODUCED-HOST-LEVEL` (key-equivalent delivery) |
| wrong | clean | wrong | wrong | `REPRODUCED-LOCAL` (TextInputField layer) |
| wrong | clean | clean | wrong | `REPRODUCED-LOCAL` (PathSelectionBox layer) |
| anything else | | | | `INVALID` — record the combination and stop; do not force a verdict |

## 7. Terminal actions — one per verdict, stated here so the runner cannot improvise

- **`VERIFIED-ABSENT`** ⇒ close. Evidence attached, no fix, no new pending item beyond the ledger
  disposition line.
- **`REPRODUCED-HOST-LEVEL`** ⇒ mint a new pending todo and **STOP**. The fix is out of this
  phase's **scope fence** — Phase 34.17 owns `PathSelectionBox` and the input-commit chain beneath
  it, not a general host/shell clipboard shim — and no file under `src-tauri/` or `src/frontend/`
  may be touched by this phase for it.
- **`REPRODUCED-LOCAL`** ⇒ mint a new pending todo carrying the now-known layer, record that a fix
  is in scope for a follow-up plan, and **STOP** — this plan still writes no fix, because the
  mechanism was unknown when it was planned.
- **`INVALID`** ⇒ record the observation combination verbatim, do not re-run, and do not force the
  nearest-looking verdict. A second attempt is a new decision for the developer to make, not an
  automatic retry.

## 8. Restore steps

1. Put `#setting-alt-legendary` back to the pre-run value recorded in precondition 6.
2. Clear the probe string from the clipboard (copy something innocuous, or clear it).
3. Fully quit the `pnpm tauri:dev` instance launched for this run.
