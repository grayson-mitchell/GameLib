---
phase: 34.17-pathselectionbox-input-commit-enter-to-commit-and-the-unconf
plan: 03
subsystem: ui
tags: [clipboard, paste, tauri, wkwebview, reproduction-gate, evidence]

# Dependency graph
requires: []
provides:
  - "A run, verdict-bearing reproduction contract for the 2026-08-11 paste report (34.17-HALF-B-PASTE-GATE.md)"
  - "A dated PASTE-HALF-ONLY disposition against 34.9 deferred-items.md item 8"
affects: []

# Tech tracking
tech-stack:
  added: []
patterns:
  - "Author the reproduction contract with RUN RECORD fields empty BEFORE running it, so the runner cannot back-fill a protocol around the answer"
key-files:
  created:
    - .planning/phases/34.17-pathselectionbox-input-commit-enter-to-commit-and-the-unconf/evidence/34.17-halfb-target-cmdv.png
  modified:
    - .planning/phases/34.17-pathselectionbox-input-commit-enter-to-commit-and-the-unconf/34.17-HALF-B-PASTE-GATE.md
    - .planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
key-decisions:
  - "Recorded VERIFIED-ABSENT as a complete passing closure, per the contract, rather than as a gap or an unproven item."
  - "Item 2 (DOM reading) recorded as NOT TAKEN with its reason rather than silently omitted; it is moot because item 1 was clean, and its only job was to adjudicate a discrepancy that did not arise."
  - "Item 5's first reading was DISCARDED and re-run, not recorded: the operator reported a masked/asterisk reading against #setting-alt-legendary, which is an unmasked text input, so the paste had gone to the wrong field."
requirements-completed: [REQ-34.17-04]

# Metrics
duration: ~20min
completed: 2026-08-26
---

# Phase 34.17 Plan 03: Half B Paste Reproduction Gate Summary

**The 2026-08-11 paste defect did not reproduce. Verdict `VERIFIED-ABSENT`, closed on evidence,
no fix written and none needed.**

## Verdict

`VERIFIED-ABSENT` — verdict-table row 1 (`item 1 clean`, items 3/4/5 don't-care).

| Item | Route | Observed | Result |
| --- | --- | --- | --- |
| 1 | target `#setting-alt-legendary`, **Cmd+V** | `/tmp/gamelib-paste-probe-12345` | **clean** |
| 2 | DOM `value` reading | Inspect Element unavailable | **not taken — moot** |
| 3 | control C1, uncontrolled `#search`, Cmd+V | `/tmp/gamelib-paste-probe-12345` | **clean** |
| 4 | control C2, `#steamgriddb-api-key`, Cmd+V | masked (`type="password"`) | **clean, with stated limit** |
| 5 | target, **Edit ▸ Paste** menu route | `/tmp/gamelib-paste-probe-12345` | **works** |

Item 1 alone decides row 1. Items 3–5 were taken anyway, are consistent with it, and none was used
to override it.

## Both candidates excluded by evidence, not assumption

- **Candidate A (`navigator.clipboard` no-opping under WKWebView)** — no mechanism connects it to a
  native Cmd+V into a DOM `<input>`. Phase 34.3 (`44232d3e`) had already migrated every
  programmatic call site onto `window.api.clipboardWriteText` / `clipboardReadText`.
- **Candidate B (tao consuming the Cmd+V key-equivalent, as fixed for the Epic login child window
  at `src-tauri/src/main.rs:2363`)** — predicts **silence**, i.e. nothing arriving. All 30 bytes
  arrived. Item 5 additionally shows the menu route and the keyboard route behaving identically,
  which is precisely the symmetry Candidate B says should not exist.

## What this does NOT establish

It does not establish that the 2026-08-11 report was mistaken. That report was never independently
re-confirmed — item 8's own text says so — and the tree has changed substantially since. **A defect
that existed and was incidentally fixed is indistinguishable, from this run alone, from one that
never reproduced.** The record says exactly this, so a later reader cannot upgrade "did not
reproduce" into "was never real".

## Rigour notes

- **Contract authored empty first.** Every RUN RECORD field was a literal `TBD-RUN` placeholder
  before the run (commit `b053d244e`), so the protocol could not be shaped around a known answer.
- **A stale instance was found and killed.** `pgrep -fl gamelib-shell` before the run returned PID
  49913, left from `34.17-02`. Recorded rather than overwritten — `pnpm tauri:dev` exits 0 without
  replacing a running instance, so a stale build silently passing is the exact failure this
  precondition exists to catch. Fresh instance was PID 53657.
- **The clipboard was verified at byte level** (`xxd`: 30 bytes, pure ASCII, no trailing newline)
  before the run and re-verified intact before item 5 — so a clean reading cannot be an artefact of
  a clipboard that was never loaded.
- **Item 5's first reading was discarded, not recorded.** The operator reported asterisks against an
  unmasked text input, which is impossible; that indicated the paste had gone into the masked
  SteamGridDB field. Re-run in the correct field. Recording the anomalous reading would have put a
  false observation into an evidence document.
- **Item 4's asterisks were checked against the source** (`SteamGridDbApiKey.tsx:75`,
  `type="password"`) rather than assumed to be either fine or anomalous.

## Terminal Action Taken

`VERIFIED-ABSENT` ⇒ **close** (section 7, one branch only):

1. Evidence attached: `evidence/34.17-halfb-target-cmdv.png`, cropped to the field per the binding
   redaction rule.
2. No fix written; no file under `src-tauri/` or `src/frontend/` touched by this plan.
3. No new pending todo minted — section 7 specifies none for this verdict.
4. Ledger disposition appended to `34.9` `deferred-items.md` item 8, scoped in its own wording to
   the **PASTE HALF ONLY**, explicitly claiming nothing about the Enter half.

## Restore Steps — verified performed, not merely asserted

1. `#setting-alt-legendary` at its pre-run value: `config.json` → `altLegendaryBin = ''`.
2. Clipboard cleared: `pbpaste | wc -c` → `0`.
3. Instance quit: `pgrep -fl gamelib-shell` → empty, exit 1.

## Self-Check: PASSED
