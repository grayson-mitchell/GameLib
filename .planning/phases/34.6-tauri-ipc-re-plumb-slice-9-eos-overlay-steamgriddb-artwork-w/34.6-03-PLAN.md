---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
autonomous: true
requirements: [REQ-34.6-11, REQ-34.6-12]
must_haves:
  truths:
    - "D-12: ONE blocking live-gate contract exists, with a fixed sequence — step 0 pre-port marker capture, Epic login FIRST, Epic logout LAST"
    - "The contract is written with EMPTY result slots BEFORE any live work, so no item can be back-filled to match an outcome"
    - "Every scored item names the observation that would make it FAIL, not only what a PASS looks like"
    - "D-13: the Epic logout item scores an IDENTITY assertion against a PLANTED foreign cookie, not a count"
    - "D-15: the Epic login item names the embedded/primary tile and explicitly refuses SIDLogin as a substitute"
    - "A-01: no step expects a decline message from a ported winetricks channel on macOS"
    - "A-02: the EOS dialog is expected at remove(), and 'no dialog at enable' is recorded in advance as the CORRECT outcome"
  artifacts:
    - path: ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md"
      provides: "the blocking live-gate contract, steps 0-8 plus the optional Humble rider, all result slots empty"
      min_lines: 200
      contains: "Written with EMPTY result slots BEFORE any live work"
  key_links:
    - from: "34.6-LIVE-GATE.md"
      to: ".planning/phases/34.6-.../34.6-VALIDATION.md § Manual-Only Verifications"
      via: "the step table this contract expands, step for step"
      pattern: "Step 0|Step 8"
---

<objective>
Author `34.6-LIVE-GATE.md` — the ONE blocking live gate this phase closes on (D-12) — with EMPTY
result slots, BEFORE any live work happens and before any port lands.

Writing the contract first is not ceremony. This project has a recorded incident
(`34.4.1-35-PLAN.md` precondition 6, still UNSTRUCK) where a gate item's PASS condition was
satisfiable by an empty starting state, and the number that satisfied it was arithmetically
forced. A contract written after the run cannot exhibit that defect and cannot be checked for it
either. `34.4.2-VERIFICATION.md` records nine gate attempts amended IN PLACE for the same reason.

Purpose: fix the sequence, the preconditions, the scoring bars and the FAIL conditions while no
result is known.
Output: one document, `verdict: null`, every result slot blank.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VALIDATION.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md

Read `Skill("spike-findings-gamelib")` — specifically
`references/live-gate-contract-authoring.md`, which carries five defect-class tests for reviewing
a blocking live-gate contract BEFORE its first run, including Test 5 (does satisfying requirement
A destroy requirement B's evidence) and the dual-sink append-and-archive capture standard
(`tee -a`, per-launch delimiters, `gamelib.log` archived before it rotates).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the contract frontmatter, preconditions and capture standard</name>
  <read_first>
    - `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-4.md` first 100 lines (frontmatter schema and precondition shape)
    - `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-35-PLAN.md` lines 30-70 (precondition 6, UNSTRUCK — the arithmetically-forced `survivingNonHumble=0` this contract must not repeat)
    - `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-VERIFICATION.md` (the amend-in-place / SUPERSEDES discipline across nine attempts)
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md`
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VALIDATION.md` § "Manual-Only Verifications"
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md`.

Frontmatter, copying `34.4.1-LIVE-GATE-RERUN-4.md`'s schema:
`phase: 34.6`, `plan: 12` (the plan that RUNS it), `type: live-gate`, `status: pending`,
`blocking: true`, `run: 1`, `supersedes_items: []`, `regression_reruns: []`,
`new_observations: []`, `created: <today>`, `completed: null`, `verdict: null`.

The body's first line after the H1 must be, verbatim:
`> Written with EMPTY result slots BEFORE any live work.`

Write a "## Why this gate exists" section stating: this is the LAST IPC re-plumb slice; the gate
covers 24 newly-ported channels AND four inherited verification legs re-homed from the parked
Phase 34.7 (34.5 UAT test 11 Epic half, test 12 `egsSync`, test 13 legendary save sync, and Epic
logout's `clearEpicCookies`). D-12: one document, one verdict, no partial-pass mode.

Write "## Preconditions" as a NUMBERED list, each stating EXACT MEASURED state with a named
evidence source and a blank `Measured:` slot. At minimum:
  1. **Build freshness.** `pnpm tauri:dev` exits 0 WITHOUT replacing an already-running instance,
     and `pgrep GameLib` misses the `gamelib-shell` process name — a whole UAT here once ran on an
     8-hour-old build. Record the exact command used to prove the running binary was built from
     the commit under test (e.g. `pgrep -fl gamelib-shell` plus the binary's mtime against
     `git log -1 --format=%cI`), and record the commit SHA.
  2. **Log capture standard.** Dual-sink append-and-archive per the spike reference: shell
     scrollback captured with `tee -a` including per-launch delimiters, AND
     `~/Library/Logs/GameLib/gamelib.log` archived to a run-scoped copy BEFORE the run, because
     `eprintln!` in the Rust shell goes to stderr and NEVER reaches `gamelib.log` (folded todo 2's
     own root cause). A clean grep of the wrong sink is not evidence of absence.
  3. **Secret-store arm.** `grep -c '\[bootstrap\] secret stores: keyring' <archived log>` and
     `grep -c '\[bootstrap\] steamgrid secret store: keyring'` both return at least 1 for the run
     under test. A `GAMELIB_DEV_SECRET_VAULT=1` boot invalidates step 3's Keychain assertion.
  4. **Epic account state.** Logged OUT at run start (the jar is sticky across restarts — a
     scripted login test needs a logout preamble). Name the account.
  5. **SteamGridDB API key.** The operator holds a real key at gate time (D-06's stated
     precondition). Record only that a key was held, never the key.
  6. **EOS overlay restore point.** Record the operator's legendary/EOS overlay state BEFORE the
     round-trip (installed? enabled? for which appName?) and the exact commands that restore it,
     because D-05's round-trip mutates it.
  7. **Save-sync fixture.** A small owned Epic title WITH cloud saves, named, and its save
     directory BACKED UP to a run-scoped copy before step 7. D-14's upload half can DESTROY a save.
  8. **Move/import fixture.** A small owned title named, plus the target directory, for step 5's
     `moveInstall`/`importGame`. Resume/move over EXISTING content — this project has measured a
     "90 GB" gate at 71.5 seconds when run that way.
  9. **Cookie jar starting census.** `before total` and the count of Epic-domain cookies, MEASURED
     and recorded, plus the name and domain of the foreign cookie planted for step 8.

Add a "## Scoring rules" section stating: no partial pass (34.5 D-08); every item is PASS, FAIL or
NOT ATTEMPTED; a NOT ATTEMPTED item is not a pass; the verdict is `PASS n/n` only when every
scored item passed; if a rerun is needed the document is AMENDED IN PLACE with a marked SUPERSEDES
section, never regenerated (regenerating destroys hand-corrections layered onto prior amendments).
  </action>
  <verify>
    <automated>test -f .planning/phases/34.6-*/34.6-LIVE-GATE.md && head -30 .planning/phases/34.6-*/34.6-LIVE-GATE.md | grep -c 'Written with EMPTY result slots BEFORE any live work'</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and its frontmatter contains `verdict: null`, `status: pending`, `blocking: true`.
    - `grep -c 'Written with EMPTY result slots BEFORE any live work' <file>` returns 1, and the line appears within the first 20 lines.
    - The preconditions section contains at least 9 numbered items, each with a blank `Measured:` slot.
    - `grep -c 'Measured:' <file>` is at least 9 and every occurrence is followed by an empty value (no precondition is pre-filled).
    - The build-freshness precondition names both `pgrep` misses `gamelib-shell` and the `pnpm tauri:dev` no-op-against-a-running-instance hazard.
    - The capture standard names BOTH sinks and states that `eprintln!` never reaches `gamelib.log`.
  </acceptance_criteria>
  <done>The contract's frame exists, unfilled, with preconditions that state measured state rather than attestation.</done>
</task>

<task type="auto">
  <name>Task 2: Write the nine scored steps (0-8) plus the optional Humble rider, each with an explicit FAIL condition and an empty result slot</name>
  <read_first>
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VALIDATION.md` § "Manual-Only Verifications" (the step table this expands — steps 0 through 8 plus the rider)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md` § Decisions D-04 through D-15 and § Amendments A-01/A-02
    - `src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts` lines 155-200 and 245-300 (`remove()`'s unconditional dialog at :162, `enable()`'s `if (!isInstalled())`-gated dialog at :197, `isInstalled()`'s `existsSync`, `getWinePrefixFolder()`'s `!isLinux` return at :295)
    - `src/frontend/helpers/declaredUnavailable.ts` (`DECLARED_UNAVAILABLE_MARKER` literal, for step 0)
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Add a "## Scored items" section with one subsection per step. Each subsection has: **What it
proves**, **How to drive it** (exact UI path or command), **PASS condition**, **FAIL condition**
(an explicit named observation, per `34.4.1-35-PLAN.md`'s own anti-vacuity rule), **Evidence to
capture**, and an empty `**Result:**` slot.

  - **Step 0 — pre-port `DECLARED_UNAVAILABLE_MARKER` capture (D-10 as corrected by A-01).** RUN
    BY PLAN 34.6-04, on a build where the winetricks registration has NOT landed. Drive the
    Winetricks panel (`src/frontend/components/UI/Winetricks/index.tsx`, whose two
    `callOrDeclare` sites are at lines 37 and 59) and capture the literal
    `[GAMELIB_DECLARED_UNAVAILABLE]` in a real `gamelib.log`, naming a `winetricks*` channel. This
    is the FIRST live observation of that literal in this project's history and CANNOT be
    recovered once the port lands. PASS: `grep -c "GAMELIB_DECLARED_UNAVAILABLE" <archived log>`
    returns at least 1 AND at least one matching line names a `winetricks` channel. FAIL: zero
    matches, OR matches that name only `steamgriddb.*` channels (the SteamGridDB half retires by
    port per D-10 and does not discharge the winetricks half). Record the commit SHA the capture
    ran against and confirm `winetricksAvailable` is absent from `handlerRegistry` on that build.
  - **Step 1 — Epic embedded login (D-15).** Log in via the EMBEDDED/PRIMARY tile. State
    explicitly: **SIDLogin device-auth does NOT satisfy this item** — item 11 means the primary
    path specifically, and accepting the alternative would let the primary path ship unobserved
    under a document reading "Epic login PASSED". PASS: session established and the Library
    renders at least one named Epic title. FAIL: login completes only via SIDLogin; or the library
    renders zero Epic tiles (record which, do not fold them together). Rider (folded todo 1,
    `REQ-34.4.1-GAP-11`): watch for a `keyring_get` timeout and MEASURE elapsed time — inferred
    time is not a measurement. A non-firing rider is NOT a discharge; record it as unexercised and
    leave the todo open.
  - **Step 2 — EOS overlay round-trip (D-05 as corrected by A-02).** install -> enable -> disable
    -> remove. State in the step body: the confirmation dialog fires at **`remove()`**
    (`eos_overlay.ts:162`, called unconditionally), NOT at `enable()` (`:197`, gated
    `if (!isInstalled())` and therefore unreachable in this order because install already ran).
    "Expected a dialog at enable, none appeared" is the CORRECT outcome and must be recorded as
    such, not as a defect. PASS: all four operations complete through the sidecar AND the remove
    dialog appears AND dismissing it does not crash the app (the 34.5 D-15 hazard). FAIL: any
    operation returns `UNPORTED_CHANNEL_MARKER`; or the app crashes on dialog dismissal. Include
    the precondition-6 restore commands as a post-step instruction.
  - **Step 3 — SteamGridDB full chain with a REAL key (D-06).** `setApiKey` -> `hasApiKey` ->
    `searchGame` -> `getGrids` -> `getHeroes`, with artwork actually RENDERING in
    `SteamGridDBPicker`. PASS: artwork renders AND the key is verified present in the Keychain
    slot `steamgrid-api-key` AND absent from `config.json`. FAIL: artwork does not render; or
    `grep -c steamGridDbApiKey <config.json>` finds the key in the clear (an A-03 regression).
    Precondition: plans 34.6-01 and 34.6-02 landed — otherwise this step is the moment of exposure.
  - **Step 4 — winetricks (D-04 as corrected by A-01).** Assert the channel RESOLVES WITH REAL
    DATA through the sidecar and that the D-11 observable shows the handler was reached. State
    explicitly: **an empty list is an acceptable RESULT, not a decline** —
    `Winetricks.listInstalled()` returns `[]` when `winetricks.log` is absent at the configured
    Wine prefix, which is DATA-dependent, not platform-gated. Winetricks has genuine macOS support
    (`macEnvs` at `tools/index.ts:628`, selected at `:642`); there is NO `!isLinux` guard in the
    `Winetricks` object. **Do not write a check expecting a decline message** — none will appear
    and "gate never fired" would be a false negative. PASS: no `UNPORTED_CHANNEL_MARKER` for any
    of the three channels AND the D-11 observable line appears for `winetricksInstall`. FAIL:
    `UNPORTED_CHANNEL_MARKER` appears; or the observable line is absent after driving the install
    action.
  - **Step 5 — the 8 late-discovered on the live session (D-07).** `getAchievements`,
    `getPlaytimeFromRunner`, `getGogDiscounts` ride the session from step 1. `frontendReady` fires
    automatically at app start — score it from its D-11 observable line, not from a UI symptom
    (it is send-kind and fails SILENTLY under Tauri). `getDefaultSavePath` is exercised by step 7.
    `moveInstall`/`importGame` use the small fixture from precondition 8. `runWineCommandForGame`
    is driven from the per-game Wine-command action. PASS: all 8 named channels produce a real
    result or observable, with zero `UNPORTED_CHANNEL_MARKER` lines. FAIL: any one of the 8 is not
    exercised (record it NOT ATTEMPTED, which is not a pass) or produces the marker.
  - **Step 6 — `egsSync` (34.5 UAT test 12).** Drive `egsSync` through the UI on the live session.
    PASS: the command completes and the Library reflects the sync. FAIL: it errors, or "the
    command was called" is the only evidence available — a call proves wiring, not behaviour.
  - **Step 7 — legendary save sync round-trip (D-14).** Back the save up first (precondition 7).
    Mutate a save locally -> sync UP -> alter or delete locally -> sync DOWN -> assert the
    mutation CAME BACK, by content. PASS: the specific mutated bytes/string are present again
    after the down-sync. FAIL: exit code 0 with unchanged or empty content — "the command exited
    0" is explicitly NOT proof here, because a sync that silently syncs nothing would pass that.
    Precondition: `getDefaultSavePath` is ported (D-14's wave-ordering constraint, satisfied by
    plan 34.6-10).
  - **Step 8 — Epic logout, LAST (D-12, D-13).** Plant a NAMED non-Epic cookie with a known name
    and domain in the jar BEFORE logging out (precondition 9). Log out. Then, by an INDEPENDENT
    re-read of the jar, assert (a) all Epic-domain cookies are cleared and (b) **that exact
    planted cookie survives BY NAME**. Record `before total` and `matched` in the result so a
    future reader can distinguish a real pass from an arithmetically-forced one. State explicitly:
    **a count alone is NOT sufficient — this AMENDS the ROADMAP's stated discharge bar**, because
    34.4.1's precondition 6 is UNSTRUCK precisely for accepting `survivingNonHumble=0` when
    `before total=34` equalled `matched=34`. FAIL: the planted cookie is absent afterwards; or the
    result records only a count. Rider (folded todo 2, `D-29-06`): if a generic RPC timeout
    co-occurs, capture the FULL shell scrollback and correlate `id=` values against the cookie
    operation — `gamelib.log` alone cannot settle this.
  - **Optional rider — Humble login (folded todo 4, `D-29-02`, REQ-34.6-12).** Not required by
    this phase's own leg: this phase logs into EPIC, and the 404 is a HUMBLE surface. Dischargeable
    ONLY if the operator additionally opens a Humble login and observes whether
    `getAccountIdentity` 404s. State in the document: **if no Humble login is performed, record
    NOT DISCHARGED and return the todo to pending — it must not be closed by association with the
    phase that folded it.**

Close with a "## Verdict" section holding `**Verdict:** _(pending)_` and an empty
"## Closing statement" section.
  </action>
  <verify>
    <automated>F=$(ls .planning/phases/34.6-*/34.6-LIVE-GATE.md); grep -c '^\*\*Result:\*\*' "$F"; grep -c 'FAIL condition' "$F"; grep -vc x /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - The document contains exactly 10 `**Result:**` slots (steps 0-8 plus the Humble rider), and every one of them is EMPTY.
    - The document contains at least 10 explicit FAIL conditions, one per scored item.
    - `grep -n 'SIDLogin' <file>` returns a match that says SIDLogin does NOT satisfy step 1.
    - `grep -n 'remove()' <file>` returns a match placing the dialog at remove, and `grep -n 'enable' <file>` returns a match recording "no dialog at enable" as the correct outcome.
    - `grep -in 'decline' <file>` in step 4 returns text saying an empty list is a RESULT, not a decline, and no step-4 text asks for a decline message.
    - `grep -n 'by name\|BY NAME' <file>` returns a match inside step 8, and step 8 states that a count alone is not sufficient.
    - `grep -n 'NOT DISCHARGED' <file>` returns a match inside the Humble rider.
    - `grep -c 'verdict: null' <file>` returns 1.
    - Anti-vacuity self-check, recorded in the SUMMARY: for each of the 10 items, name a concrete starting state that would make its PASS condition satisfiable with NO work done. If any item has one, rewrite that item before finishing.
    - Requirement-pair check (spike reference Test 5), recorded in the SUMMARY: for each ordered pair of steps, state whether satisfying the earlier one destroys the later one's evidence. Step 0 vs step 4 and step 1 vs step 8 are known instances and must be named explicitly.
  </acceptance_criteria>
  <done>All ten scored items exist with empty results, explicit FAIL conditions, and a recorded pair-interaction review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator credentials → the gate document | a real Epic password, a real SteamGridDB API key and real cookie values are handled during the run |
| `gamelib.log` → a public fork's issue tracker | users of this fork paste log output; anything the gate instructs to capture may be published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-08 | Information Disclosure | the contract instructing capture of raw log/scrollback containing credentials | mitigate | Precondition 5 records only THAT a key was held, never the key. The evidence instructions must say to redact cookie VALUES (names and domains only) and to never paste a `Running command:` line containing `--secret`, a PKCE verifier or a private-branch password (the `F-34.5-G6-22` class, ledger row `U-34.5-33`). |
| T-34.6-09 | Repudiation | a result slot back-filled after the run to match the outcome | mitigate | The whole plan: empty slots authored before any live work, `verdict: null`, amend-in-place-with-SUPERSEDES on rerun. The anti-vacuity and pair-interaction reviews are recorded in the SUMMARY, dated before the run. |
| T-34.6-10 | Information Disclosure | step 3 writing a real API key to `config.json` | transfer | Owned by plans 34.6-01/02 (A-03). This contract's step-3 FAIL condition asserts the negative directly against `config.json`, so a regression is caught by the gate rather than assumed absent. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | Documentation-only plan. No packages installed. |
</threat_model>

<verification>
- The document exists, `verdict: null`, ten empty `**Result:**` slots.
- No source file is modified by this plan.
- The anti-vacuity and requirement-pair reviews are written into `34.6-03-SUMMARY.md`, dated before any live run.
</verification>

<success_criteria>
- D-12's single sequenced gate is fully specified: step 0 pre-port, login first, logout last.
- D-13's identity-not-count bar, D-15's SIDLogin refusal, A-01's no-decline correction and A-02's remove-not-enable correction are each stated in the step that carries them.
- D-04, D-05, D-06, D-07, D-10 and D-14 each have a named scored item.
- Folded todos 1, 2 and 4 have explicit, honestly-conditioned discharge rules.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-03-SUMMARY.md` when done, including the anti-vacuity and requirement-pair reviews.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
</output>
