---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 10
type: execute
wave: 6
depends_on: ["34.6-09"]
files_modified:
  - src/backend/sidecar/runnerMiscFlowRegistration.ts
  - src/backend/sidecar/__tests__/runnerMiscFlows.test.ts
  - src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts
  - src/frontend/screens/Settings/sections/AdvancedSettings/__tests__/EosDeclineCallSiteGuard.test.ts
autonomous: true
requirements: [REQ-34.6-04, REQ-34.6-08, REQ-34.6-09]
must_haves:
  truths:
    - "D-14: `getDefaultSavePath` is registered on the sidecar BEFORE the legendary save-sync live leg runs, or path resolution fails"
    - "All 24 in-scope channels are registered — a census, provable by one command, not a sample"
    - "D-09 is complete: Describe 7 asserts absence for exactly Zoom's 3 and presence for exactly the 16 deferred, arithmetic re-derived from measurement and RED-proven in BOTH directions"
    - "D-08: `EosDeclineCallSiteGuard.test.ts` is still green and UNMODIFIED — all `callOrDeclare` wrappers stay, and the 11-vs-10 count is reconciled to the measured 20 sites across 6 files"
    - "The sidecar BUNDLE starts, not only the jest suites"
  artifacts:
    - path: "src/backend/sidecar/runnerMiscFlowRegistration.ts"
      provides: "getAchievements, getDefaultSavePath, getPlaytimeFromRunner registered invoke-kind"
      contains: "getDefaultSavePath"
  key_links:
    - from: "src/backend/sidecar/runnerMiscFlowRegistration.ts"
      to: "src/backend/save_sync.ts getDefaultSavePath"
      via: "direct import of the pure function, not main.ts's handler"
      pattern: "getDefaultSavePath"
---

<objective>
Register the last three of the 24 channels, complete D-09's Describe 6/7 re-derivation, verify
D-08's guard is still green unmodified, and prove the sidecar BUNDLE starts.

`getDefaultSavePath` carries D-14's hard ordering constraint: it must be ported BEFORE the
legendary save-sync leg, or path resolution fails at live-gate step 7. This plan is where that
constraint is satisfied, and it precedes plan 34.6-12's gate run.

This is also the census point. The phase's scope is 24 channels (D-01), and a sample is not a
census — this plan asserts all 24 by name in one command.

Purpose: close the port half of the phase with a provable, complete registration.
Output: 3 channels, the final gate arithmetic, and a bundle-level smoke assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-09-SUMMARY.md

<interfaces>
Electron originals:
  main.ts:822-827   addHandler('getAchievements', async (event, appName, runner, lang = 'en-US') =>
                      getGame(appName, runner).getAchievements?.(lang) ?? [])
  main.ts:1286-1290 addHandler('getDefaultSavePath', async (event, appName, runner, alreadyDefinedGogSaves) =>
                      getDefaultSavePath(appName, runner, alreadyDefinedGogSaves))
                      // delegates to save_sync.ts's pure function; that function reaches
                      // app.getPath('documents') at save_sync.ts:146, inside
                      // getDefaultGogSavePaths's gogVariableMap.DOCUMENTS branch, only when
                      // game.isNative(). pathShim.ts already implements `documents` (REQ-34.5-01),
                      // so there is no additional hazard here.
  main.ts:1495-1503 addHandler('getPlaytimeFromRunner', async (e, runner, appName) => {
                      const { disablePlaytimeSync } = GlobalConfig.get().getSettings()
                      if (disablePlaytimeSync) { return }
                      if (runner === 'gog') { return libraryManagerMap[runner].getGame(appName).getGOGPlaytime() }
                      return })

Target module already hosts `egsSync`, `syncSaves`, `callTool` and the four runner CLI version
channels (`src/backend/sidecar/runnerMiscFlowRegistration.ts`).

D-08's guard (`.../AdvancedSettings/__tests__/EosDeclineCallSiteGuard.test.ts`):
  EXPECTED_EOS_CALL_SITES = 11, CALL_SITE_WINDOW = 200 collapsed chars
  It is a SOURCE-TEXT structural gate, not registry-shaped — deliberately, because
  AdvancedSettings/index.tsx cannot be imported under the frontend jest project's node environment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register `getAchievements`, `getDefaultSavePath` and `getPlaytimeFromRunner` in `runnerMiscFlowRegistration.ts`</name>
  <read_first>
    - `src/backend/main.ts` lines 818-830, 1280-1295 and 1490-1510 (the three handler bodies)
    - `src/backend/save_sync.ts` lines 1-60 and 130-160 (`getDefaultSavePath`'s signature and the `app.getPath('documents')` call at :146 inside the `game.isNative()` branch)
    - `src/backend/sidecar/runnerMiscFlowRegistration.ts` in full (its docstring, curated-import list and `ipcMain.handle` idiom; it already hosts `egsSync` and `syncSaves`)
    - `src/backend/sidecar/pathShim.ts` (confirm `documents` is implemented and does not throw — REQ-34.5-01 added it)
  </read_first>
  <files>src/backend/sidecar/runnerMiscFlowRegistration.ts, src/backend/sidecar/__tests__/runnerMiscFlows.test.ts</files>
  <behavior>
    - `registerRunnerMiscFlows()` puts all three names in `handlerRegistry` and none in `listenerRegistry`.
    - `getAchievements` defaults `lang` to `'en-US'` when the argument is absent, and resolves `[]` when the game has no `getAchievements` method.
    - `getDefaultSavePath` forwards `appName`, `runner` and `alreadyDefinedGogSaves` unchanged to `save_sync.ts`'s function.
    - `getPlaytimeFromRunner` resolves `undefined` when `disablePlaytimeSync` is set, without touching `libraryManagerMap`.
    - `getPlaytimeFromRunner` resolves `undefined` for a non-`gog` runner.
  </behavior>
  <action>
Add three `ipcMain.handle(...)` registrations to `registerRunnerMiscFlows()` in
`src/backend/sidecar/runnerMiscFlowRegistration.ts`, bodies copied from `main.ts` with no semantic
change, adapting only the wrapper to this file's `(_event: unknown, ...args: unknown[])` idiom.

Import `getDefaultSavePath` DIRECTLY from `../save_sync` (the pure function), `getGame` from
`../utils`, and `GlobalConfig`/`libraryManagerMap` as the file already does. Never import
`src/backend/main.ts`.

Record in an inline comment at the `getDefaultSavePath` registration: D-14's ordering constraint
(this port must precede the legendary save-sync live leg, live-gate step 7, or path resolution
fails), and that `save_sync.ts:146`'s `app.getPath('documents')` is already covered by
`pathShim.ts` (REQ-34.5-01) so no new shim work is required.

Preserve `getAchievements`'s optional-chaining and `?? []` fallback exactly; preserve
`getPlaytimeFromRunner`'s two bare `return` statements (returning `undefined`, not `null` and not
`0`) exactly — a caller distinguishing "sync disabled" from "zero playtime" would break silently.

Extend `src/backend/sidecar/__tests__/runnerMiscFlows.test.ts` with the five behaviours above.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "runnerMiscFlows" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All three names registered: `for c in getAchievements getDefaultSavePath getPlaytimeFromRunner; do grep -q "'$c'" src/backend/sidecar/runnerMiscFlowRegistration.ts || echo "MISSING $c"; done` prints nothing.
    - `grep -v '^ *[/*]' src/backend/sidecar/runnerMiscFlowRegistration.ts | grep -c "from '../main'\|ipc_handler"` returns 0.
    - The `disablePlaytimeSync` test asserts `undefined`, not falsiness — RED-proven by changing the handler to `return 0`, which must make it fail. Revert after proving.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The last three channels are registered, and D-14's ordering constraint is satisfied before the gate.</done>
</task>

<task type="auto">
  <name>Task 2: Final Describe 6 re-derivation, the 24-channel census, and D-09's both-directions RED proof</name>
  <read_first>
    - `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` in full as left by plan 34.6-09 (module scope, Describe 6, Describe 7)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-07-SUMMARY.md`, `34.6-08-SUMMARY.md`, `34.6-09-SUMMARY.md` (the measured values each prior plan left)
    - `.planning/IPC-PORT-INVENTORY.md` § "Phase 34.6 — Slice 9" and § "Late-discovered — owner Phase 34.6" (the authoritative 16 + 8 = 24 work-list)
  </read_first>
  <files>src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts</files>
  <action>
`registerRunnerMiscFlows()` is already called at this file's module scope, so the three new channels
are visible. Re-derive Describe 6's `handleChannels.length`, `listenChannels.length`, the per-module
`counts` array and its total by MEASUREMENT: run the suite, read the actual values from the failing
expectations, then independently re-derive each and confirm agreement. Stop and report on any
disagreement — it would mean a channel registered somewhere unexpected. Update
`runnerMiscFlowRegistration`'s `declaredChannels` entry in the `MODULES` table with the three NAMES,
not only a bumped count.

Add a NEW test — the **24-channel census** (D-01). Declare the 24 names as four labelled arrays
(EOS 8, SteamGridDB 5, winetricks 3, late-discovered 8), assert `TWENTY_FOUR.length === 24` and
assert per channel that it is registered with the CORRECT KIND: 22 in `handlerRegistry`, and exactly
`winetricksInstall` and `frontendReady` in `listenerRegistry` with `handlerRegistry.has()` false.
This test must import and call every registration module that owns one of the 24 — including
`registerAppShellFlows` and `registerInstallFlows`, which this file does not currently call — or add
them at module scope alongside the others, before the canonical snapshot. A census that silently
omits a module is a sample.

Complete **D-09's both-directions RED proof** and record BOTH failure messages verbatim in the
SUMMARY:
  - Presence direction: comment out one registration from EACH of the five registration modules
    that own part of the 24, one at a time, and confirm the census test names the exact missing
    channel. Revert each.
  - Absence direction: register a fake handler for each of `authZoom`, `getZoomUserInfo` and
    `logoutZoom` in turn and confirm Describe 7's absence assertion FAILS naming that channel.
    Revert each.

`expect(DROPPED_OR_DEFERRED.length).toBe(3)` must hold, with the array containing exactly the three
Zoom names. Do not delete Describe 7 — it is the only assertion in the repo keeping Zoom dropped
(34.5 D-02).
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "runnerSliceRegistration" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The suite passes with 0 failures.
    - The census test exists and asserts exactly 24 names with per-channel kind. `grep -c "'steamgriddb.getGrids'\|'frontendReady'\|'moveInstall'\|'getDefaultSavePath'" src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` shows the late-discovered and cluster names are all present in the same file.
    - The 24 names in the census match `.planning/IPC-PORT-INVENTORY.md`'s two Phase-34.6 bucket lines exactly — verify by extracting both lists and diffing them, and record the diff (empty) in the SUMMARY.
    - `expect(DROPPED_OR_DEFERRED.length).toBe(3)` is present and the array holds only the three Zoom names.
    - Ten RED proofs are recorded in the SUMMARY (five presence, three absence, plus the two send-kind channels' kind assertions), each with its verbatim failure message.
    - The SUMMARY records the measured Describe 6 values alongside the independently re-derived values, and states they agreed.
  </acceptance_criteria>
  <done>All 24 channels are proven registered with the correct kind by a census, and D-09's split is complete and RED-proven both ways.</done>
</task>

<task type="auto">
  <name>Task 3: Verify D-08's guard is green and UNMODIFIED, reconcile the 11-vs-10 count, and prove the sidecar bundle starts</name>
  <read_first>
    - `src/frontend/screens/Settings/sections/AdvancedSettings/__tests__/EosDeclineCallSiteGuard.test.ts` in full (`EXPECTED_EOS_CALL_SITES = 11`, `CALL_SITE_WINDOW = 200`, and the two `eosOverlayUnavailable` wrapper assertions)
    - `src/frontend/helpers/declaredUnavailable.ts` in full (`callOrDeclare`'s contract — once a channel is ported, `spec.call()` resolves and the wrapper returns `{ ok: true, value }` with no call-site change needed)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md` § 7 (the 20-site, 6-file table resolving the 11-vs-10 discrepancy)
  </read_first>
  <files>src/frontend/screens/Settings/sections/AdvancedSettings/__tests__/EosDeclineCallSiteGuard.test.ts (READ-ONLY — verify green and unmodified; do not edit)</files>
  <action>
**Verify, do not modify.** D-08 keeps every `callOrDeclare` wrapper after the port: its contract —
never throws, never returns a rejected promise — is a genuine safety property regardless of
porting, and it is the ONLY renderer-to-`gamelib.log` path in the app
(`frontend/index.tsx:41` bridges `window.error` only, never `unhandledrejection`). Ripping the
wrappers out would re-create the exact defect plan 34.5-48 diagnosed: 8 EOS rejections in one UAT
session with ZERO log lines.

Run the guard suite and confirm it passes UNMODIFIED. Confirm by
`git diff HEAD -- <guard test path>` that this phase has not touched it.

**Reconcile the count, in the SUMMARY, not by editing the test.** D-08's cited "10 call sites"
naming four files is STALE: a project-wide grep of `callOrDeclare(` (excluding test files and
`declaredUnavailable.ts` itself) finds **20 sites across 6 files** —
`AdvancedSettings/index.tsx` 11, `SteamGridDBPicker/index.tsx` 3, `Winetricks/index.tsx` 2,
`SteamGridDbApiKey.tsx` 2, `SideloadDialog/index.tsx` 1, `EditGameDialog/index.tsx` 1. D-08's
figure omitted `Winetricks/index.tsx` and `SteamGridDbApiKey.tsx`. The guard's own
`EXPECTED_EOS_CALL_SITES = 11` is EXACTLY correct — all 11 EOS sites live in
`AdvancedSettings/index.tsx` alone — so the guard needs no change. Re-run the census grep yourself
and record the measured per-file table in the SUMMARY; do not transcribe the numbers above without
re-measuring, because that is exactly how a stale count propagates.

**Bundle smoke.** Run `pnpm build:sidecar` then `node build/main/sidecar.js` and confirm it starts
and prints the ready sentinel. This phase added imports to four registration modules and one new
module; a bundle-only defect (any `src/sidecar/index.ts` import before `bootstrap`) kills the
sidecar while BOTH a green jest run and a clean bundle pass. Also run `npx tsc --noEmit` —
`ts-jest` is transpile-only, so a green suite says nothing about types.
  </action>
  <verify>
    <automated>npx jest --selectProjects Frontend --testPathPattern "EosDeclineCallSiteGuard" 2>&1 | tail -15 && npx tsc --noEmit && pnpm build:sidecar && timeout 20 node build/main/sidecar.js 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --selectProjects Frontend --testPathPattern "EosDeclineCallSiteGuard"` passes with 0 failures and does NOT print "no projects found matching selection".
    - `git diff HEAD~N -- src/frontend/screens/Settings/sections/AdvancedSettings/__tests__/EosDeclineCallSiteGuard.test.ts` (N spanning this phase) is EMPTY.
    - `git diff HEAD~N -- src/frontend/helpers/declaredUnavailable.ts` is EMPTY — D-08 keeps it unchanged.
    - The measured `callOrDeclare(` census is recorded in the SUMMARY as a per-file table with a total, obtained by re-running the grep, and the total is stated against D-08's "10" with the discrepancy explained.
    - `pnpm build:sidecar` exits 0 and `node build/main/sidecar.js` prints the ready sentinel within 20 seconds.
    - `npx tsc --noEmit` exits 0.
    - Full project runs are green: `npx jest --selectProjects Backend` and `npx jest --selectProjects Frontend`, run separately. Do NOT gate on a full `pnpm test` — it manufactures a different failure set under load.
  </acceptance_criteria>
  <done>D-08's guard is proven green and untouched, the count is reconciled by measurement, and the bundle boots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar (`getDefaultSavePath`) | an appName and a GOG saves descriptor cross here and influence a filesystem path |
| sidecar → `app.getPath('documents')` via `pathShim` | a path name that throws loudly on an unknown key, by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-27 | Tampering | `getDefaultSavePath`'s `alreadyDefinedGogSaves` argument influencing a resolved save path | accept | Byte-equivalent port of an existing Electron path; the argument comes from the app's own config, and `pathShim`'s `documents` throws loudly on an unknown key rather than returning an empty string. Not in `T-34.5-C6-49-03`'s named scope. |
| T-34.6-28 | Repudiation | a ported channel silently failing with no log line, re-creating plan 34.5-48's defect | mitigate | D-08 keeps every `callOrDeclare` wrapper, and task 3 proves the guard green and untouched. The wrapper remains the only renderer-to-`gamelib.log` path in the app. |
| T-34.6-29 | Denial of Service | a bundle-only import defect killing the sidecar while every suite is green | mitigate | Task 3's `node build/main/sidecar.js` smoke assertion — jest is structurally blind to this class. |
| T-34.6-30 | Repudiation | claiming "all 24 ported" from a sample | mitigate | Task 2's census asserts all 24 by name AND diffs the list against `IPC-PORT-INVENTORY.md`'s own bucket lines. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed. |
</threat_model>

<verification>
- `npx jest --selectProjects Backend` — green (full project run).
- `npx jest --selectProjects Frontend` — green (full project run).
- `npx tsc --noEmit` — exits 0.
- `cd src-tauri && cargo check` — exits 0 (this phase touched `main.rs` in plan 34.6-01).
- `pnpm build:sidecar && node build/main/sidecar.js` — starts.
- `npm start` (Electron) unchanged.
</verification>

<success_criteria>
- All 24 in-scope channels are registered with the correct kind, proven by a census diffed against the inventory.
- D-14's ordering constraint is satisfied: `getDefaultSavePath` is ported before the save-sync leg.
- D-09 is complete and RED-proven in both directions; only Zoom's 3 assert absence.
- D-08's guard is green and unmodified; the 11-vs-10 count is reconciled to a measured 20 across 6 files.
- The sidecar bundle boots.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-10-SUMMARY.md` when done, recording the ten RED-proof failure messages, the measured Describe 6 values, the census-vs-inventory diff and the `callOrDeclare` per-file table.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
Run `graphify update .` after the source changes land.
</output>
