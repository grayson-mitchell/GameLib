---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 14
subsystem: infra
tags: [ipc-inventory, documentation, gate-closure, todo-hygiene, d-01, d-10, d-13]

requires:
  - phase: 34.6-12
    provides: "the live gate run (Steps 0-8), whose recorded results this plan cites"
  - phase: 34.6-13
    provides: "the D-03 close-out re-derive and inventory reconciliation this plan's channel list checks against"
  - phase: 34.6-15
    provides: "the six in-place SUPERSEDES amendments to the live gate's own contract defects"
  - phase: 34.6-17
    provides: "the FINAL ADJUDICATION scored tally and closed verdict (FAIL 7/9) this plan treats as ground truth"
provides:
  - "34.6-PORTED-CHANNELS.md: a 24-row honest, measured proof-level ledger for every channel Phase 34.6 ported"
  - "U-34.5-32 closed in place with a two-part disposition (SteamGridDB retired by port; winetricks discharged pre-port)"
  - "4 folded todos (Epic logout, keyring_get, F-9, Humble 404) disposed against their own stricter bars — none close"
  - "2 new todos filed for gate findings the plan itself could not fix (documentation-only): EOS overlay callOrDeclare bypass, installed.json watcher never ported"
affects: ["35"]

tech-stack:
  added: []
  patterns:
    - "Cite LIVE proof only against a gate item whose own recorded outcome is PASS — never against an item scored FAIL as a whole, even when a sub-channel resolved real data"
    - "A todo's discharge condition being met by observation does not mean the todo closes if the observation itself is a confirmed FAIL — the confirmed defect gets its own new todo, and the original stays pending as the record of when/why ownership moved"
    - "Recurrence of a failure CLASS (same shell-timeout shape, different id, different diagnosed cause) is not a discharge of a specific still-open CASE"

key-files:
  created:
    - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md
    - .planning/todos/pending/2026-08-25-eos-overlay-gamesubmenu-bypasses-callordeclare-on-linux.md
    - .planning/todos/pending/2026-08-25-installed-json-watcher-not-ported-to-tauri.md
  modified:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
    - .planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md
    - .planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md
    - .planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md
    - .planning/todos/pending/2026-08-23-humble-user-info-404-two-candidates-undiscriminated.md

key-decisions:
  - "winetricksAvailable/winetricksInstalled/winetricksInstall use a 'ported with a declared caveat' proof level, not a LIVE(item 4) citation, because item 4's own scored outcome is FAIL as a whole — citing PASS against a FAIL item would violate the document's own citation discipline"
  - "EOS overlay's enable/disable operations get UNREACHABLE-ON-PLATFORM caveats distinct from install/remove's LIVE(item 2) PASS, and the 4 status/support channels get plain 'unit' with an explicit no-isolated-citation note — no channel is over-claimed to inherit its siblings' proof level"
  - "The two live-gate findings this plan could not fix (documentation-only) are filed as new todos with resolves_phase: unassigned, rather than fixed in source or silently dropped"
  - "All 4 folded todos stay pending — none close, even where a discharge condition's literal wording was technically satisfied by observation, because the observation itself confirmed a defect rather than clearing one"

requirements-completed: [REQ-34.6-12, REQ-34.6-14]

duration: ~9min (this session's commit span; work spanned a context-compaction boundary across two sessions)
completed: 2026-08-25
---

# Phase 34.6 Plan 14: EOS Overlay/SteamGridDB/Artwork Closing Artefacts Summary

**Declared a 24-channel, per-channel honest proof-level ledger reconciling to the phase's final FAIL 7/9 live-gate verdict; closed `U-34.5-32` with a two-part disposition; and disposed of 4 folded todos against their own stricter discharge bars, none of which close.**

## Performance

- **Duration:** ~9 min (commit span `d0908c325` → `1e14a668e`; the underlying research and drafting spanned a prior session across a context-compaction boundary)
- **Tasks:** 3
- **Files modified:** 8 (3 created, 5 modified)

## Accomplishments

- **`34.6-PORTED-CHANNELS.md` created** — 24 rows verified to reconcile empty-diff against `IPC-PORT-INVENTORY.md`'s two Phase-34.6 bucket sections; every `LIVE (item N) — PASS` citation checked to point only at items 2, 3, 5, or 7 (all PASS in the FINAL ADJUDICATION tally), never at item 4 or 8 (the two FAILs).
- **`U-34.5-32` closed in place**, two-part: SteamGridDB half retired by port (live-gate item 3's PASS-on-property, real key, real artwork rendered); winetricks half discharged pre-port (Step 0's verbatim `GAMELIB_DECLARED_UNAVAILABLE` capture, commit `9245195b66e21c9b3f3f1d6b1235e03a5bb81698`, A-01's outcome preserved / reasoning corrected). Prior row history (2026-08-14 opening, 2026-08-19 GATE 5 note) preserved, not deleted — confirmed by `git diff` showing a single in-place line replacement.
- **4 folded todos disposed, none closed.** Each checked against the live gate's FINAL ADJUDICATION, and each note explains the *distinction* between the todo's literal discharge wording and its actually-applicable stricter bar:
  - Epic logout: observed live (Step 8), but the count-only cross-check that would read as a discharge PASSED falsely (8=8) while D-13's stricter identity re-read confirmed 0-of-7 cookies actually cleared — a real, now separately-owned defect, not a discharge.
  - `keyring_get`: neither gate observation (Step 1's "unreported" rider; Step 3's `keyring:unavailable` failure) is the bounded, measured timeout this todo needs.
  - F-9: the gate reproduced the same failure *class* (a different id, root-caused to a missing `LONG_RUNNING_CHANNELS` entry) but not the same *case* — id=1575's cookie-op co-occurrence remains undetermined.
  - Humble 404: the gate's own optional rider was never exercised (Epic-only leg) and its own text says `NOT DISCHARGED`, returned to pending.
- **2 new todos filed** (deviation Rule 2) for live-gate findings explicitly assigned to this plan for disposition but requiring a source change out of scope for a documentation-only plan: `GameSubMenu`'s three EOS call sites bypassing `callOrDeclare` on Linux, and `main.ts`'s `installed.json` watcher never being ported to the sidecar.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create `34.6-PORTED-CHANNELS.md`** — `d0908c325` (docs)
   - Rule 2 deviation folded in: filed 2 new todos for gate findings this plan could not fix directly — `92d01702a` (docs)
2. **Task 2: Amend `U-34.5-32` in place in `34.5-UNTESTED-ITEMS.md`** — `bb58654b6` (docs)
3. **Task 3: Dispose of 4 folded todos** — `1e14a668e` (docs)

**Plan metadata:** this SUMMARY's own commit (see below)

## Files Created/Modified

- `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md` — 24-row honest per-channel proof-level ledger, plus live-gate narrative and corrections section
- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md` — `U-34.5-32` row closed in place, two-part disposition appended
- `.planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md` — dated disposition note appended, stays pending
- `.planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md` — dated disposition note appended, stays pending
- `.planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md` — dated disposition note appended, stays pending
- `.planning/todos/pending/2026-08-23-humble-user-info-404-two-candidates-undiscriminated.md` — dated disposition note appended, stays pending
- `.planning/todos/pending/2026-08-25-eos-overlay-gamesubmenu-bypasses-callordeclare-on-linux.md` — new, files the FINDING 2 gap from `34.6-LIVE-GATE.md` Step 2
- `.planning/todos/pending/2026-08-25-installed-json-watcher-not-ported-to-tauri.md` — new, files the Step 7 watcher gap

## Decisions Made

- The winetricks channels' proof level is stated as "ported with a declared caveat" rather than a `LIVE(item 4)` citation, since item 4's own scored outcome is FAIL as a whole (conjunctive PASS condition across all three channels) — citing PASS against a FAIL-scored item would violate the document's own citation discipline. `winetricksInstall` itself is explicitly marked FAIL, not miscited.
- EOS overlay's four operations are NOT given a uniform proof level: `installEosOverlay`/`removeEosOverlay` get `LIVE(item 2) — PASS (macOS-scoped)`; `enableEosOverlay`/`disableEosOverlay` get an explicit UNREACHABLE-ON-PLATFORM caveat (no macOS UI reaches them); the four status/support channels get plain "unit" with a note that no isolated live citation exists for them.
- The 4 folded todos are disposed against their own *stricter* bar, not their weaker literal wording, per the plan's own instruction — most visibly for Epic logout, where a weak count-only reading would have suggested discharge while the project's own established stricter bar (D-13's identity re-read) confirms an actual defect.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Filed 2 new todos for live-gate findings explicitly assigned to this plan for disposition, beyond the plan's declared 6-file scope**

- **Found during:** Task 1 (drafting `34.6-PORTED-CHANNELS.md`'s "Corrections this declaration must not paper over" section)
- **Issue:** `34.6-LIVE-GATE.md` contains two findings whose own text explicitly defers disposition to this plan: Step 2's FINDING 2 (`GameSubMenu/index.tsx`'s three EOS call sites bypass `callOrDeclare`, silently swallowing Linux-side rejections — "for plan 34.6-14 to pick up and dispose of") and Step 7's second, larger finding (the `installed.json` file-watcher never ported to Tauri — "a todo is owed for this but not filed by this task, per instruction"). Neither had been filed anywhere. Since this plan is documentation-only and modifies no source file, fixing them directly was out of scope; leaving them undisposed would let the gate's own explicit instruction go unfulfilled.
- **Fix:** Filed two new todos, `resolves_phase: "unassigned"` (no live phase currently owns either porting/hardening gap), each documenting the gap, tracing it to a concrete symptom (the `getDefaultSavePath` staleness for the watcher; the two silent error-listener layers for the EOS bypass), and stating a concrete discharge condition.
- **Files modified:** `.planning/todos/pending/2026-08-25-eos-overlay-gamesubmenu-bypasses-callordeclare-on-linux.md`, `.planning/todos/pending/2026-08-25-installed-json-watcher-not-ported-to-tauri.md`
- **Verification:** `grep -L 'resolves_phase'` against both files prints nothing; both referenced explicitly and cross-linked from `34.6-PORTED-CHANNELS.md`'s corrections section.
- **Committed in:** `92d01702a`

---

**Total deviations:** 1 (2 new todo files filed under Rule 2)
**Impact on plan:** Additive only — no source file touched, no scope narrowed. Ensures the live gate's own explicit disposition instructions do not silently go unfulfilled, consistent with this project's repeated `blocked_by`/`resolves_phase` rot lesson.

## Issues Encountered

None beyond the deviation above. All verification steps (24-row count, inventory reconciliation, LIVE-citation-only-against-PASS check, `git diff` history-preservation checks, `resolves_phase` presence, no-deletion checks) passed on first measurement.

## Threat Flags

None. This plan modified no source file, added no network endpoint, auth path, file-access pattern, or schema change at a trust boundary — all changes are `.planning/` documentation.

## Threat Model Disposition (from plan frontmatter)

| Threat | Disposition | How mitigated |
|---|---|---|
| T-34.6-41 (Repudiation: LIVE citation against a NOT ATTEMPTED item) | mitigate | Verified directly: every `LIVE (item N) — PASS` citation in the 24-channel table points only to items 2, 3, 5, or 7, all recorded PASS in the FINAL ADJUDICATION tally. |
| T-34.6-42 (Repudiation: "unit-proven" claimed from a mere call) | mitigate | EOS status/support channels are stated as plain "unit" with an explicit note that no isolated live citation exists — "the function is called" is never conflated with proof of behaviour. |
| T-34.6-43 (Repudiation: folded todo 4 closed by association with the phase that folded it) | mitigate | Humble todo's disposition note quotes the gate's literal `NOT DISCHARGED` and confirms it returns to pending. |
| T-34.6-44 (Repudiation: `U-34.5-32` retired wholesale, asserting something untrue about a shipping surface) | mitigate | Two-part disposition applied; A-01's falsified premise recorded on the row explicitly (outcome preserved, reasoning corrected) so no later reader inherits the false premise. |
| T-34.6-45 (Repudiation: a `resolves_phase:`-less todo silently escapes auto-close) | mitigate | `resolves_phase` set on both newly-filed todos; `grep -L 'resolves_phase'` against all touched todo files confirmed empty (asserted, not merely assumed). |
| T-34.6-SC (Tampering: npm/pip/cargo installs) | mitigate | Documentation-only plan. No packages installed — confirmed by `git diff --stat` across all commits showing only `.md` files. |

## Self-Check: PASSED

All 4 created files found on disk; all 5 modified files found on disk; all 4 task commit hashes
(`d0908c325`, `92d01702a`, `bb58654b6`, `1e14a668e`) confirmed present via `git log --oneline --all`.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Phase 34.6's closing-artefacts obligations are now complete.** `34.6-PORTED-CHANNELS.md` gives Phase 35 (and any future consumer) a single, honest, per-channel proof-level reference reconciled against both the inventory and the closed live gate.
- **4 residual items remain genuinely open, correctly**, not silently closed: Epic logout's confirmed cookie-clear defect (owned by `2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md`), `keyring_get`'s unmeasured bounded-timeout path, F-9's undetermined cookie-op co-occurrence, and Humble's undiscriminated 404 cause. None block Phase 35; all are recorded honestly as unresolved.
- **2 new gaps are now tracked** (EOS overlay `callOrDeclare` bypass on Linux; the `installed.json` watcher gap) with `resolves_phase: "unassigned"` — no phase currently owns fixing either, and a future phase-planning pass should consider claiming them explicitly rather than letting them rot.

---
*Phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w*
*Completed: 2026-08-25*
