---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 20
type: execute
wave: 3
depends_on: ["34.6-18", "34.6-19"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VALIDATION.md
  - .planning/todos/pending/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md
  - .planning/todos/completed/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md
  - .planning/todos/pending/2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-34.6-05, REQ-34.6-10, REQ-34.6-11, REQ-34.6-12, REQ-34.6-14]

must_haves:
  truths:
    - "REQUIREMENTS.md's REQ-34.6-05 no longer reads `REQUIREMENT NOW FULLY COMPLETE` — it records the CR-01 regression, the landed fix, and the one item still outstanding, and its checkbox stays `[ ]` until the live re-drive in 34.6-21 returns a result"
    - "REQ-34.6-10/-11/-12/-14 checkboxes are `[x]`, matching their SUMMARY frontmatter and the verifier's independent confirmation, so an automated sweep does not misreport this phase as 3/14 short"
    - "`34.6-PORTED-CHANNELS.md`'s `moveInstall` and `importGame` rows no longer claim a containment hardening the code does not have"
    - "The CR-01 todo is CLOSED — moved to `.planning/todos/completed/` with a `resolves_phase: 34.6` field it never had, since auto-close cannot see a todo without one"
    - "The adjacent `winePrefix`/`wineVersion` todo stays PENDING and gains one sentence recording the new `path` policy as context for its own open design question"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "an honest REQ-34.6-05 entry plus four corrected checkboxes"
      contains: "REQ-34.6-05"
    - path: ".planning/todos/completed/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md"
      provides: "the closed CR-01 todo, carrying resolves_phase: 34.6"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "34.6-VERIFICATION.md"
      via: "the REQ-34.6-05 correction citing the gap that found it"
      pattern: "34\\.6-VERIFICATION\\.md"
    - from: ".planning/phases/34.6-*/34.6-PORTED-CHANNELS.md"
      to: "assertPlausibleAbsolutePath"
      via: "the corrected moveInstall / importGame proof-level rows"
      pattern: "assertPlausibleAbsolutePath"
---

<objective>
Bring this phase's written record back into agreement with its code. Three of
`34.6-VERIFICATION.md`'s own `missing` bullets and its Warning-level anti-pattern are
documentation, not code: REQ-34.6-05 still reads "REQUIREMENT NOW FULLY COMPLETE" for a
requirement the verification marks BLOCKED; four checkboxes are stale at `[ ]`; and
`34.6-PORTED-CHANNELS.md` — the phase's honest proof ledger — states a containment hardening
that plan 34.6-18 removed.

Purpose: this project has a recorded failure mode where a SUMMARY or status doc is wrong while
the record it summarises is right, and another where a closure gate FORCES a false record. Both
are avoided here by writing the honest intermediate state rather than a premature completion.
Output: corrected requirements, a corrected ledger, an extended validation map, and two correctly
dispositioned todos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VERIFICATION.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-18-PLAN.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md
@.planning/todos/pending/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md
@.planning/todos/pending/2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct REQ-34.6-05 and flip the four stale checkboxes in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 840-856 — the whole Phase 34.6 block. Each requirement is ONE very long line; edit in place with a targeted replacement, never by rewriting the block.
    - Line 846 in particular — REQ-34.6-05's current tail, which ends with `**REQUIREMENT NOW FULLY COMPLETE.**` followed by the `winePrefix`/`wineVersion` residual sentence.
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VERIFICATION.md` — the `gaps:` frontmatter block and the Requirements Coverage table's REQ-34.6-05 row and its `Note:` on REQ-34.6-10/-11/-12/-14.
    - `.planning/phases/34.6-.../34.6-13-SUMMARY.md`, `34.6-14-SUMMARY.md`, `34.6-15-SUMMARY.md`, `34.6-17-SUMMARY.md` — their `requirements-completed` frontmatter, which is the independent evidence that the four checkbox flips are correct rather than assumed.
    - `.planning/phases/34.6-.../34.6-18-SUMMARY.md` and `34.6-19-SUMMARY.md` — for the actual landed commit hashes to cite.
  </read_first>

  <action>
    In `.planning/REQUIREMENTS.md`:

    1. **REQ-34.6-05 (line 846).** Replace the literal string `**REQUIREMENT NOW FULLY COMPLETE.**`
       with an honest record. It must state, in order: that the 34.6-11 hardening was found
       DEFECTIVE by `34.6-REVIEW.md` CR-01 and independently re-verified in
       `34.6-VERIFICATION.md` (status `gaps_found`, this requirement BLOCKED); that containment
       against `defaultInstallPath` rejected the cross-drive move and out-of-tree import each
       feature exists to perform, AND was circular because that root is renderer-writable via
       `setSetting`; that gap plans 34.6-18/34.6-19 replaced it with
       `assertPlausibleAbsolutePath` (shape-only: absolute, no `..` segment, no NUL, non-empty,
       string) plus a user-visible rejection dialog, RED-proven with the pre-fix run captured to
       `evidence/34.6-18-RED-crossroot.txt`; and that `T-34.5-C6-49-03` is RE-DISPOSITIONED, not
       discharged-and-forgotten, with the accepted residual named (a compromised renderer can
       still call the channel directly with any absolute traversal-free path — accepted at ASVS
       L1 for a local desktop launcher). Cite the landing commits from the 34.6-18 / 34.6-19
       SUMMARYs.
       **LEAVE THE CHECKBOX AS `- [ ]`.** Add the sentence: closure of this requirement is
       pending the live re-drive contracted in `34.6-21-PLAN.md` and appended to
       `34.6-LIVE-GATE.md`; plan 34.6-21 flips it. Writing `[x]` here would be a gate forcing a
       false record — the same failure this correction exists to undo.
       Keep the existing `winePrefix`/`wineVersion`/`wineCrossoverBottle` residual sentence and
       its todo citation intact — that residual is unchanged and still open.

    2. **REQ-34.6-10 (line 851), REQ-34.6-11 (852), REQ-34.6-12 (853), REQ-34.6-14 (855).**
       Change each line's leading `- [ ]` to `- [x]` and append to each a short note of the form:
       confirmed complete by `34.6-VERIFICATION.md` (2026-08-25, Requirements Coverage table) and
       by the corresponding SUMMARY's `requirements-completed` frontmatter — checkbox flipped in
       gap plan 34.6-20; the checkbox, not the substance, was the gap. Name the specific SUMMARY
       per requirement: REQ-34.6-10 → `34.6-13-SUMMARY.md`; REQ-34.6-11 → `34.6-15-SUMMARY.md`
       and `34.6-17-SUMMARY.md`; REQ-34.6-12 → `34.6-14-SUMMARY.md`; REQ-34.6-14 →
       `34.6-13-SUMMARY.md` and `34.6-14-SUMMARY.md`.
       Do NOT flip any other checkbox in the file, and do not mint a new REQ ID — the fix
       delivers no property the existing 14 do not already cover; REQ-34.6-05's own text is the
       correct home.

    3. Update the ROADMAP's Phase 34.6 entry: change the `**Plans:**` line (line ~3036) from
       17 to the new total, and append a clearly-labelled second gap-cycle block after the
       existing `**Gap cycle — 3 plans authored 2026-08-25 ...**` block, headed for the
       verification-driven cycle, listing 34.6-18 through 34.6-21 with one line each. Do not
       edit or re-tick any existing plan row.
  </action>

  <verify>
    <automated>bash -c "grep -c 'REQUIREMENT NOW FULLY COMPLETE' <(sed -n '846p' .planning/REQUIREMENTS.md)"</automated>
  </verify>

  <acceptance_criteria>
    - The false claim is gone from REQ-34.6-05: `sed -n '846p' .planning/REQUIREMENTS.md | grep -c 'REQUIREMENT NOW FULLY COMPLETE'` returns `0`.
    - The correction cites its source and the new control: `sed -n '846p' .planning/REQUIREMENTS.md | grep -c '34.6-VERIFICATION.md'` returns at least `1`, and the same line `| grep -c 'assertPlausibleAbsolutePath'` returns at least `1`.
    - REQ-34.6-05's checkbox is deliberately NOT ticked yet: `grep -c '^- \[ \] \*\*REQ-34.6-05\*\*' .planning/REQUIREMENTS.md` returns `1`.
    - All four stale checkboxes flipped, none missed and none over-reached: `grep -cE '^- \[x\] \*\*REQ-34\.6-(10|11|12|14)\*\*' .planning/REQUIREMENTS.md` returns `4`, and `grep -cE '^- \[ \] \*\*REQ-34\.6-(10|11|12|14)\*\*' .planning/REQUIREMENTS.md` returns `0`.
    - No other requirement changed state: `git diff -U0 .planning/REQUIREMENTS.md | grep -E '^[+-]- \[' | grep -vcE 'REQ-34\.6-(05|10|11|12|14)'` returns `0`.
    - The ROADMAP entry names all four new plans: `grep -cE '34\.6-(18|19|20|21)-PLAN\.md' .planning/ROADMAP.md` returns at least `4`, and no pre-existing `34.6-01..17` row was modified: `git diff -U0 .planning/ROADMAP.md | grep -E '^-' | grep -cE '34\.6-(0[1-9]|1[0-7])-PLAN\.md'` returns `0`.
  </acceptance_criteria>

  <done>REQUIREMENTS.md tells the truth about REQ-34.6-05, four checkboxes match their evidence, and the ROADMAP lists this gap cycle without disturbing the previous one.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the `34.6-PORTED-CHANNELS.md` proof rows and extend the `34.6-VALIDATION.md` per-task map</name>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md, .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VALIDATION.md</files>

  <read_first>
    - `.planning/phases/34.6-.../34.6-PORTED-CHANNELS.md` lines 90-115 — the `importGame` row (line 97) and the `moveInstall` row (line 98) in full. Both currently end with "Hardened by plan 34.6-11 (T-34.5-C6-49-03) via ... `assertContainedPath`". That is the sentence the code now contradicts. Note also that both rows carry SEPARATE, still-valid product defects (the GOG identity-validation gap on import; the macOS `openrsync` flag rejection on move) — those must survive the edit untouched.
    - `.planning/phases/34.6-.../34.6-VALIDATION.md` lines 53-82 — the Per-Task Verification Map table and its column shape (`Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status`). EXTEND it; do not replace it.
    - `.planning/phases/34.6-.../34.6-VALIDATION.md` lines 18-43 — the Test Infrastructure block and the two command-shape hazards. Every command you add to the map must obey both (capitalised `--selectProjects Backend`; no full `pnpm test`).
  </read_first>

  <action>
    1. In `34.6-PORTED-CHANNELS.md`, rewrite ONLY the trailing hardening clause of the
       `importGame` and `moveInstall` rows. Each replacement clause must say: hardened by plan
       34.6-11, then CORRECTED by gap plan 34.6-18 after `34.6-REVIEW.md` CR-01 /
       `34.6-VERIFICATION.md` found the containment root rejected the feature's own primary
       input; the live control is now `rendererPathGuard.assertPlausibleAbsolutePath` (shape
       only), with `defaultInstallPath` containment removed; and the honest proof level for that
       control — `unit` at the time of writing, since the appended live-gate items in
       `34.6-LIVE-GATE.md`'s gap-cycle-2 section had not yet been driven when this row was
       written. Plan 34.6-21 upgrades the proof level after the drive; do not pre-write a LIVE
       claim here.
       Leave every other clause of both rows byte-identical, including the two product-defect
       descriptions and their committed-todo cross-references.

    2. In `34.6-VALIDATION.md`, append four rows to the Per-Task Verification Map, one per gap
       task, using the existing column shape:
       - `18-T1 | 34.6-18 | 1 | REQ-34.6-05 | T-34.6-46, T-34.6-47 | shape validator proven in BOTH directions at the primitive | unit | npx jest --selectProjects Backend --testPathPattern "rendererPathGuard" | yes | (status)`
       - `18-T2 | 34.6-18 | 1 | REQ-34.6-05 | T-34.5-C6-49-03, T-34.6-48 | cross-root ACCEPT RED-proven before the fix; header comments re-dispositioned same commit | unit | npx jest --selectProjects Backend --testPathPattern "installFlows|rendererPathGuard" | yes | (status)`
       - `19-T1/T2 | 34.6-19 | 2 | REQ-34.6-05 | T-34.6-32, T-34.6-50, T-34.6-51 | rejection is user-visible; fork strings confined to gamelib.json; namespace actually loaded | unit | npx jest --selectProjects Backend --testPathPattern "installFlows|i18nCatalogChurnGuard" | yes | (status)`
       - `21-T2 | 34.6-21 | 4 | REQ-34.6-05 | T-34.5-C6-49-03 | the cross-root case driven LIVE, with the macOS openrsync confound distinguishable from a containment rejection | manual (live gate) | MANUAL — see 34.6-LIVE-GATE.md gap-cycle-2 section | yes | (status)`
       Add one sentence under the table noting that `21-T2` is the phase's only remaining
       manual item and that it is contracted with empty result slots before any run.

    3. Add a line to `34.6-VALIDATION.md`'s Validation Sign-Off list recording that REQ-34.6-05's
       replacement validator is RED-proven in BOTH directions — the original sign-off line only
       required RED-proof "against known-bad input", which is precisely the one-directional bar
       that let CR-01 through. Do not delete the original line; add the corrected one beneath it
       so the widening is legible.
  </action>

  <verify>
    <automated>bash -c "grep -c 'assertPlausibleAbsolutePath' .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md"</automated>
  </verify>

  <acceptance_criteria>
    - Both rows corrected, neither missed: `grep -c 'assertPlausibleAbsolutePath' .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md` returns exactly `2`.
    - Neither row still asserts the removed control as live: `grep -n 'assertContainedPath' .planning/phases/34.6-.../34.6-PORTED-CHANNELS.md` shows every remaining occurrence inside a clause that explicitly says it was CORRECTED or REMOVED — check by eye and quote each surviving occurrence in the SUMMARY.
    - The two product defects survived the edit: `grep -c 'openrsync' .planning/phases/34.6-.../34.6-PORTED-CHANNELS.md` returns at least `1`, and `grep -c 'goggame-' .planning/phases/34.6-.../34.6-PORTED-CHANNELS.md` returns at least `1`.
    - The validation map gained exactly four rows and lost none: `git diff -U0 .planning/phases/34.6-.../34.6-VALIDATION.md | grep -cE '^\+\| 1[89]-T|^\+\| 21-T'` returns `4`, and `git diff -U0 .planning/phases/34.6-.../34.6-VALIDATION.md | grep -cE '^-\|'` returns `0`.
    - Every added command uses the capitalised project name: `git diff -U0 .planning/phases/34.6-.../34.6-VALIDATION.md | grep '^+' | grep -c 'selectProjects backend'` returns `0` (the lowercase form matches nothing and exits 0 — a fail-open gate).
  </acceptance_criteria>

  <done>The proof ledger states the control the code actually has, and the validation map covers all four gap tasks with runnable, correctly-cased commands.</done>
</task>

<task type="auto">
  <name>Task 3: Close the CR-01 todo with a `resolves_phase` field, and annotate the adjacent residual todo</name>
  <files>.planning/todos/pending/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md, .planning/todos/completed/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md, .planning/todos/pending/2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md</files>

  <read_first>
    - `.planning/todos/pending/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md` — the whole file. Note its frontmatter has `created`, `source`, `severity`, `status`, `introduced_by` and NO `resolves_phase`. Note also its "Fix direction (not yet decided)" section, which this gap cycle decides.
    - `.planning/todos/pending/2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md` — the whole file, in particular its "Suggested resolution" section, which asks what containment policy is correct for a Wine prefix. Plan 34.6-18's decision on `path` is directly relevant context for that question and must be recorded there.
    - `ls .planning/todos/completed/ | tail -5` — the naming convention for closed todos, so the moved file matches it.
  </read_first>

  <action>
    1. In the CR-01 todo, before moving it:
       - Add `resolves_phase: 34.6` to the frontmatter. It has none, and this project's todo
         auto-close reads that field — without it the todo is invisible to every automated
         sweep, which is why this closure is an explicit task rather than a side effect.
       - Add `resolved: 2026-08-25` and change `status: pending` to `status: RESOLVED`.
       - Replace the section heading `## Fix direction (not yet decided)` with
         `## Fix direction — DECIDED (gap plans 34.6-18 / 34.6-19)` and, beneath it, keep the
         original candidate list verbatim but add which candidate was taken and why, plus the
         two rejected alternatives with their reasons (mirror `34.6-18-PLAN.md`'s `<decision>`
         block — do not paraphrase it into something weaker). Explicitly record the argument the
         todo itself did not have: the containment root was renderer-writable via `setSetting`,
         so the control was circular against its own named adversary.
       - Add a `## Closure evidence` section naming: the landing commits from
         `34.6-18-SUMMARY.md` / `34.6-19-SUMMARY.md`; the RED-proof file
         `evidence/34.6-18-RED-crossroot.txt`; and the fact that closure of REQ-34.6-05 itself
         additionally awaits `34.6-21`'s live re-drive, so this todo closing is not the same
         event as the requirement closing.
    2. Move the file to `.planning/todos/completed/` with `git mv`. Do NOT combine the move with
       a large content append in a single commit — a `git mv` plus a big append defeats rename
       detection; make the content edits, verify, then move, and check the result with
       `git diff --cached -M --summary` showing a rename.
    3. In the ADJACENT todo
       `2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md`, which STAYS
       PENDING and is NOT fixed here, add exactly one sentence to its "Suggested resolution"
       section: that gap plan 34.6-18 resolved the sibling question for `importGame`'s `path`
       argument by dropping `defaultInstallPath` containment in favour of a shape-only
       validator, on the grounds that the root was renderer-writable and that the feature's own
       input is out-of-root — so "containment against `defaultInstallPath`" is no longer
       available as a default answer for `winePrefix`/`wineVersion`/`wineCrossoverBottle`
       either, and the open design question is now narrower: existence/type validation versus a
       different root entirely. Change nothing else in that file; its `status: OPEN` stays.
       Note its `planned_as: 34.6-14` field is now stale (34.6-14 closed without disposing of
       it) — say so in the same sentence rather than silently leaving a rotted pointer.
  </action>

  <verify>
    <automated>bash -c "test -f .planning/todos/completed/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md && ! test -f .planning/todos/pending/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md && grep -q 'resolves_phase: 34.6' .planning/todos/completed/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md && echo OK"</automated>
  </verify>

  <acceptance_criteria>
    - The todo moved, not copied: `test -f .planning/todos/completed/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md` succeeds AND `test ! -f .planning/todos/pending/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md` succeeds.
    - It now carries the field auto-close needs: `grep -c '^resolves_phase: 34.6$' <the completed file>` returns `1`, and `grep -c '^status: RESOLVED$' <the completed file>` returns `1`.
    - The decision is recorded, not just referenced: `grep -c 'renderer-writable' <the completed file>` returns at least `1` and `grep -c 'not yet decided' <the completed file>` returns `0`.
    - Git sees a rename, not a delete-plus-add: `git diff --cached -M --summary | grep -c 'rename .*path-containment-guard-rejects'` returns `1`.
    - The adjacent todo stayed pending and open: `test -f .planning/todos/pending/2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md` succeeds, `grep -c '^status: OPEN$' <that file>` returns `1`, and `git diff -U0 <that file> | grep -cE '^\+[^+]'` returns between `1` and `4` — one sentence added, nothing rewritten.
    - The macOS rsync todo is untouched: `git status --porcelain .planning/todos/pending/2026-08-24-move-game-is-broken-on-macos-rsync-flags-openrsync-rejects.md` produces NO output. That is a separate defect on the same feature and is explicitly out of this cycle's scope.
  </acceptance_criteria>

  <done>CR-01 is closed with the field auto-close needs, its decision is recorded where a future reader will find it, and the adjacent residual is annotated without being absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → future automated sweep | a checkbox or a ledger row is read by tooling that never reads the prose around it |
| planning record → future human reader | a stated rationale is trusted as fact unless it is diffed against the code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-53 | Repudiation | REQUIREMENTS.md continuing to assert `REQUIREMENT NOW FULLY COMPLETE` for a requirement the verification marks BLOCKED | mitigate | The literal is removed and replaced with the regression record; the acceptance criterion greps line 846 for it and requires `0`. |
| T-34.6-54 | Repudiation | the correction over-reaching into a premature `[x]`, forcing a false record because a closure gate wants a tick | mitigate | REQ-34.6-05's checkbox stays `- [ ]` by explicit acceptance criterion, with the outstanding live item named; plan 34.6-21 flips it only after the drive returns a result. |
| T-34.6-55 | Repudiation | flipping the four stale checkboxes on the strength of SUMMARY frontmatter alone (a SUMMARY can be wrong while the record it summarises is right) | mitigate | Each flip cites BOTH the SUMMARY's `requirements-completed` frontmatter AND `34.6-VERIFICATION.md`'s independent Requirements Coverage confirmation — two sources, one of which re-derived the substance rather than restating it. |
| T-34.6-56 | Repudiation | `34.6-PORTED-CHANNELS.md` carrying a hardening claim the code contradicts, in the phase's designated honest proof ledger | mitigate | Both rows are rewritten and pinned by an exact-count grep (`assertPlausibleAbsolutePath` returns 2); every surviving `assertContainedPath` mention must be quoted in the SUMMARY with its corrected framing. |
| T-34.6-57 | Repudiation | the closed todo becoming invisible to automated sweeps because it never had `resolves_phase` | mitigate | The field is added BEFORE the move and pinned by an exact-match grep. This is the third recorded instance of this failure in this project; the closure is an explicit task, not a side effect. |
| T-34.6-58 | Tampering | the scope fence leaking — absorbing the adjacent `winePrefix`/`wineVersion` under-restriction todo or the macOS `openrsync` todo into this cycle | mitigate | The adjacent todo gains exactly one sentence, bounded by an added-lines-between-1-and-4 criterion and a `status: OPEN` check; the rsync todo is required to show NO git status output at all. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | **No packages are installed by this plan.** Documentation edits and `git mv` only. |
</threat_model>

<verification>
- `sed -n '846p' .planning/REQUIREMENTS.md | grep -c 'REQUIREMENT NOW FULLY COMPLETE'` returns `0`.
- `grep -cE '^- \[x\] \*\*REQ-34\.6-(10|11|12|14)\*\*' .planning/REQUIREMENTS.md` returns `4`.
- `grep -c 'assertPlausibleAbsolutePath' .planning/phases/34.6-.../34.6-PORTED-CHANNELS.md` returns `2`.
- The CR-01 todo is in `completed/` with `resolves_phase: 34.6`; the two fenced-out todos are unchanged / one-sentence-annotated as specified.
- `pnpm planning-gates` shows NO NEW failure. It is already red at HEAD on ONE pre-existing, unrelated gate — `.planning/phases/34.4.1-*/seam-parity-sweep-gate.py`, whose committed `34.4.1-SEAM-PARITY-SWEEP.md` went stale when plan 34.6-09 wired `steamgrid` into a sidecar registration module (the sweep's own prose predicted exactly that trigger). It is OUT of this gap cycle's scope. The bar here: the run must report `6/7` with that same gate as the only failure and its diff unchanged from HEAD's. If a SECOND gate goes red, or that diff changes, stop — this cycle caused it.
</verification>

<success_criteria>
- Nothing in this phase's written record asserts a property its code does not have.
- No requirement is ticked ahead of its evidence, and none is left untied to evidence it already has.
- Two todos end this plan in exactly the state their own scope warrants: one closed with the field auto-close needs, one still open with new context.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-20-SUMMARY.md` when done.
Quote every surviving `assertContainedPath` occurrence in `34.6-PORTED-CHANNELS.md` with its corrected framing, so a reader can confirm none of them is a live claim.
Commit with `git commit --only <explicit paths>`; make the todo content edits and the `git mv` in separate commits so rename detection survives. Never use `git stash`, `git reset`, or `git checkout -- <file>`.
</output>
