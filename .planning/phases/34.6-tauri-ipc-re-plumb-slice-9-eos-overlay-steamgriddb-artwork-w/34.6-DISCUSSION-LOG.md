# Phase 34.6: Tauri IPC re-plumb slice 9 — EOS overlay, SteamGridDB artwork, winetricks + the Epic/save-sync verification inherited from 34.7 - Discussion Log

> **Audit trail only.** Do not use as input for planning, research, or execution agents.
> Decisions are captured in `34.6-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-08-23
**Phase:** 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
**Areas discussed:** Port scope (the 8 late-discovered), What "ported" can be PROVEN to mean on a macOS gate, Fate of the honest-decline machinery, The inherited Epic/save-sync/logout verification leg

---

## Area selection

All four offered gray areas were selected (multiSelect).

Two facts measured before the discussion and presented up front, neither carried by the ROADMAP goal
text:

1. Port scope is **24** channels, not 16 — `IPC-PORT-INVENTORY.md` § "Late-discovered — owner
   Phase 34.6" assigns 8 more. All 8 confirmed absent from every `src/backend/sidecar/*Registration.ts`.
2. Porting turns a shipped, tested, **gated** decline path into a live path — two gates go red by
   construction (`runnerSliceRegistration.test.ts` Describe 7, `EosDeclineCallSiteGuard.test.ts`).

---

## Port scope — the 8 late-discovered

### Q1: Does Phase 34.6 own all 24, or only the 16 the ROADMAP names?

| Option | Description | Selected |
|--------|-------------|----------|
| All 24 — 16 + the 8 late-discovered | Inventory already names 34.6 as owner; Phase 35's gate is "COMPLETE". Cost: phase ~50% bigger than ROADMAP implies; goal text needs amending | ✓ |
| 16 only — the 8 get their own phase | Matches the ROADMAP definition. Cost: needs a 34.6.1 before 35; untracked deferral is how UAT debt accumulates here | |
| 16 + `frontendReady`, defer the other 7 | Ports the only one with a live failing caller. Cost: still needs a follow-up phase; splits the 8 on a criterion the inventory doesn't use | |

**User's choice:** All 24. → **D-01**

### Q2: How are the three security-flagged channels ported? (`importGame`, `moveInstall`, `runWineCommandForGame` — `T-34.5-C6-49-03`)

| Option | Description | Selected |
|--------|-------------|----------|
| Port as-is, harden in a dedicated plan in the same phase | Preserves additive/reversible; separately-committed hardening discharges the finding | ✓ |
| Port as-is only — hardening is Phase 35's problem | Cheapest. Cost: forwards a security finding across the cutover, where it stops being reversible | |
| Harden inline at the port | No separate pass. Cost: a live-gate FAIL can't be bisected to "port broke it" vs "validation rejected it" | |

**User's choice:** Port as-is, dedicated hardening plan. → **D-02**

### Q3: Who re-runs `preload-surface-gate.py`, and what happens to new findings?

| Option | Description | Selected |
|--------|-------------|----------|
| 34.6 re-runs at close; findings ABSORBED into this phase | Only a re-derive that runs AFTER the ports land can assert "COMPLETE". Cost: open-ended tail | ✓ |
| 34.6 re-runs and RECORDS; Phase 35 absorbs | Bounded here. Cost: hands the riskiest phase an unbounded list — 34.5 D-05's rejected alternative | |
| Don't re-run — the 2026-08-12 audit stands | Audit was thorough. Cost: eight phases of source changes since | |

**User's choice:** Absorb into this phase. → **D-03**

### Q4: What's the ceiling on absorbing?

| Option | Description | Selected |
|--------|-------------|----------|
| Absorb up to ~10; beyond that halt and insert a phase | A number the executor can act on; ~10 ≈ one wave at 3–4 channels/plan | ✓ |
| Absorb any count — no ceiling | Guarantees the precondition literally. Cost: unbounded; 34.5's seven gap cycles | |
| Absorb only channels with a live caller | Prioritises real breakage. Cost: "no live caller" is a grep claim; census-by-wrong-namespace is a recorded failure here | |

**User's choice:** ~10 ceiling. → **D-03**

**Notes:** No scope creep raised in this area.

---

## What "ported" can be PROVEN to mean on a macOS gate

### Q1: Passing bar for the winetricks cluster? (`tools/index.ts:807,843` return early on `!isLinux`)

| Option | Description | Selected |
|--------|-------------|----------|
| Prove the guard fired, not the feature | Assert the handler was REACHED and returned the guard's own result. Cost: needs an observable at the guard that doesn't exist today | ✓ |
| Unit-prove on macOS, live-prove on Linux in Phase 38 | Declared exception. Cost: 34.5 D-08 says no partial pass | |
| Run the leg on a Linux box | Only bar that proves the feature. Cost: no Linux CI leg exists | |

**User's choice:** Prove the guard fired. → **D-04**

### Q2: What must the macOS gate exercise for EOS overlay?

| Option | Description | Selected |
|--------|-------------|----------|
| Full round-trip: install → enable → disable → remove | Only path proving the legendary shell-out through the sidecar; hits `eos_overlay.ts:194`'s dialog — the D-15 crash hazard. Cost: mutates legendary state; needs a restore step | ✓ |
| Reads only — status, version, isEnabled, updateInfo | Non-mutating. Cost: leaves the crash-capable dialog path unobserved | |
| Reads + a deliberate uninstalled `enable()` | Cheapest way to hit the dialog. Cost: install/remove unobserved | |

**User's choice:** Full round-trip. → **D-05**

### Q3: What does the gate use for SteamGridDB's API key?

| Option | Description | Selected |
|--------|-------------|----------|
| A real key, entered live through the settings field | Full chain incl. artwork rendering; the only cluster macOS can fully prove. Cost: operator must hold a key at gate time | ✓ |
| Two legs: no-key decline, then a real key | Also proves the `hasApiKey=false` branch. Cost: one extra item and a key add/remove cycle | |
| `hasApiKey`/`setApiKey` only — no network | No third-party dependency at gate time. Cost: the three that do real network work stay unobserved | |

**User's choice:** Real key, live. → **D-06**

### Q4: How are the 8 late-discovered gated on macOS?

| Option | Description | Selected |
|--------|-------------|----------|
| Ride the Epic/GOG session the inherited leg establishes | Near-free once the session exists; fixture game for `moveInstall`/`importGame`. Cost: sequences the gate | ✓ |
| Port + unit-prove; live-observe only `frontendReady` | Cheapest. Cost: three security-flagged channels ship a hardening change no live run exercises | |
| Live-observe all 8, `moveInstall` on a throwaway fixture | Highest bar. Cost: largest gate this project has run | |

**User's choice:** Ride the session. → **D-07**

---

## Fate of the honest-decline machinery

### Q1: What happens to `callOrDeclare()`'s 10 wrapped call sites once the channels register?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep the wrapper, unwind nothing | Its never-throws contract is a real safety property; it's the ONLY renderer→`gamelib.log` path. Cost: dead-looking code, a decline branch no live test exercises | ✓ |
| Rip the wrappers out at port | Removes 10 sites of indirection. Cost: re-creates the defect 34.5-48 diagnosed — 8 EOS rejections, zero log lines | |
| Keep only where a platform can't run it | Wrapper survives where a decline is a real product state. Cost: two idioms for one channel class | |

**User's choice:** Keep everywhere. → **D-08**

### Q2: How is `runnerSliceRegistration.test.ts` Describe 7 amended?

| Option | Description | Selected |
|--------|-------------|----------|
| Split it: Zoom-3 stays absence, the 16 flip to presence | Invariant preserved both ways; RED-proof required in both directions; the 19-name arithmetic must be re-derived | ✓ |
| Delete Describe 7 | Simplest. Cost: silently drops the only assertion keeping Zoom dropped | |
| Leave it; let the port make it red, fix at the end | Red = "ports landed". Cost: a red HEAD shipped for a day here once | |

**User's choice:** Split it. → **D-09**

### Q3: What discharges `U-34.5-32`?

| Option | Description | Selected |
|--------|-------------|----------|
| SteamGridDB half retires by port; winetricks half discharged live | winetricks still declines on macOS, so the gate captures `DECLARED_UNAVAILABLE_MARKER` in a real log — a first. Cost: needs a two-part ledger disposition | ✓ |
| Retire wholesale — porting supersedes the question | Cost: winetricks still declines on macOS; the marker would have zero live observations ever | |
| Keep it open — hand to Phase 38 | Cost: forwards a residual across the cutover; blocker records rot silently here (3×) | |

**User's choice:** Split it. → **D-10**

### Q4: What does 34.6 do about send-kind `winetricksInstall`?

| Option | Description | Selected |
|--------|-------------|----------|
| Port as a send channel, add an observable at the sidecar handler | Keeps Electron-identical semantics; one observable also serves D-04's guard-fired bar. Cost: a log line is not a return value | ✓ |
| Convert to invoke-kind at the port | Removes the silent-failure class. Cost: a behaviour change smuggled into a port, right before the cutover | |
| Port as-is; carry 34.5-55's exemption forward | Purely mechanical. Cost: no observable at all, contradicting D-04 | |

**User's choice:** Send-kind + observable. → **D-11**

**Post-discussion measurement that widened this decision:** `frontendReady` is ALSO send-kind
(`src/preload/api/misc.ts:93`, `makeListenerCaller`) — a fact `IPC-PORT-INVENTORY.md` does not
record. D-11 was written to cover **two** send channels, not one. The inventory's line reference
`GlobalState.tsx:1586` is also stale; the caller is at `:1609`.

---

## The inherited Epic / save-sync / logout verification leg

### Q1: One gate or two?

| Option | Description | Selected |
|--------|-------------|----------|
| One blocking gate, sequenced: login leg first | Session is a precondition for everything downstream; logout must run last. Cost: a FAIL anywhere fails the phase under D-08 | ✓ |
| Two gates: a port gate and a verification gate | FAIL localises. Cost: a second document is where items go missing from `audit-uat` here | |
| One gate, logout its own re-run | Cost: a third artifact; re-runs multiply (34.4.2 took nine attempts) | |

**User's choice:** One sequenced gate. → **D-12**

### Q2: How is the Epic logout item specified?

| Option | Description | Selected |
|--------|-------------|----------|
| Assert the SURVIVOR's identity against a planted foreign cookie | The correction 34.4.1's UNSTRUCK precondition 6 demands. Cost: the gate must plant a cookie | ✓ |
| Count cross-check as the ROADMAP words it | Matches inherited wording. Cost: reproduces the forced-arithmetic failure exactly | |
| Both — identity plus count | Count also catches over-deletion. Cost: a count mismatch with a correct identity is ambiguous | |

**User's choice:** Identity against a planted cookie. → **D-13** *(this AMENDS the ROADMAP's stated
discharge bar)*

### Q3: What fixture and pass bar for legendary save sync?

| Option | Description | Selected |
|--------|-------------|----------|
| Small owned Epic game with cloud saves; round-trip a mutated save | Proves both directions. Cost: needs such a title installed; `getDefaultSavePath` must be ported first | ✓ |
| Download-only | Non-destructive. Cost: the upload half — the one that can destroy a save — unobserved | |
| Command-exit proof only | Cheapest. Cost: "the function is CALLED" proves wiring, not behaviour | |

**User's choice:** Mutated-save round-trip. → **D-14**

### Q4: Does SIDLogin satisfy UAT item 11 if the embedded login fails?

| Option | Description | Selected |
|--------|-------------|----------|
| No — item 11 means the embedded login specifically | It's the primary path and has never been gated live. Cost: a 403 recurrence blocks the phase | ✓ |
| Yes — either path establishes the session | Downstream items only need a session. Cost: the primary path could pass the phase unexercised | |
| Embedded required; SIDLogin a pre-authorized downgrade | Unblocks the rest of the leg. Cost: D-08 has no partial-pass mode | |

**User's choice:** No — embedded specifically. → **D-15**

---

## Todo folding

Four presented; **all four folded**.

| Todo | Folded | Note |
|---|---|---|
| `2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md` | ✓ | Is the fourth inherited item; ROADMAP names this file as its tracker |
| `2026-08-23-keyring-get-bounded-timeout-unverified-live.md` | ✓ | Observation rider — its revisit trigger literally names 34.6's live gate |
| `2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md` | ✓ | Observation rider — same parked set, same trigger |
| `2026-08-23-humble-user-info-404-two-candidates-undiscriminated.md` | ✓ | **Adjacency concern was raised in the option text and the operator selected it anyway.** It is a Humble surface; this phase's gate logs into Epic. CONTEXT.md records the condition: dischargeable only if the operator additionally opens a Humble login, otherwise it returns to pending rather than closing by association |

---

## Claude's Discretion

- Wave composition and plan count (~6–9 port plans + hardening + gate contract + gate run + re-derive)
- Whether any of the 24 is a wave-1 cross-cutting seam (`runWineCommandForGame` is the candidate)
- The exact shape of the D-11/D-04 sidecar-handler observable
- Which Epic title serves as the D-14 save-sync and D-07 `moveInstall`/`importGame` fixture
- How EOS/legendary state is restored after D-05's round-trip

## Deferred Ideas

- Phase 38 — the real Linux observation of winetricks and the Linux-prefix EOS overlay path
- Re-homing `declaredUnavailable.ts`'s constant tables now that D-08 keeps the wrappers permanently
- The `ElectronTokenStore` plaintext fallback (34.5 D-12) — still open on the Steam token path
