---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 04
type: execute
wave: 2
depends_on: ["34.6-03"]
files_modified:
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
autonomous: false
requirements: [REQ-34.6-11]
must_haves:
  truths:
    - "D-10 as corrected by A-01: the `DECLARED_UNAVAILABLE_MARKER` literal is observed live, in a real `gamelib.log`, for a winetricks channel — the first such observation in this project's history"
    - "The capture happened on a build where the winetricks registration had NOT landed, proven by the commit SHA and by the channel's absence from `handlerRegistry`"
    - "Step 0's result slot in `34.6-LIVE-GATE.md` is filled from the run, not from intent"
  artifacts:
    - path: ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md"
      provides: "step 0 result recorded, with the archived log excerpt and the commit SHA"
      contains: "GAMELIB_DECLARED_UNAVAILABLE"
  key_links:
    - from: "src/frontend/components/UI/Winetricks/index.tsx"
      to: "callOrDeclare"
      via: "the two wrapped winetricks call sites (lines 37, 59) that emit the marker while the channel is unported"
      pattern: "callOrDeclare\\("
---

<objective>
Run STEP 0 of the live gate, and ONLY step 0: capture `DECLARED_UNAVAILABLE_MARKER` in a real
`gamelib.log` for a winetricks channel, on a build where the winetricks registration has not
landed.

This is a hard ordering constraint, not a preference. A-01 corrected D-10's reasoning: winetricks
does NOT decline on macOS (there is no `!isLinux` guard in the `Winetricks` object; it has genuine
macOS support via `macEnvs`). It is the UNPORTED channel that produces the marker. Once plan
34.6-07 registers `winetricksAvailable`/`winetricksInstalled`/`winetricksInstall`, the channel
resolves and there is nothing left to observe — the literal would end this project's history with
ZERO live observations ever, which is exactly what D-10 was written to prevent.

Purpose: capture a one-shot observation before the port destroys its own evidence.
Output: step 0's result slot in `34.6-LIVE-GATE.md`, filled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md

<interfaces>
`src/frontend/helpers/declaredUnavailable.ts`:
  export const DECLARED_UNAVAILABLE_MARKER = '[GAMELIB_DECLARED_UNAVAILABLE]' as const
  export const WINETRICKS_API_METHODS = ['winetricksListInstalled', 'winetricksListAvailable', 'winetricksInstall'] as const
  export const WINETRICKS_CHANNELS = ['winetricksAvailable', 'winetricksInstall', 'winetricksInstalled'] as const
  // callOrDeclare logs ONE line via window.api.logError, deduped per channel per session

Call sites that emit it for winetricks: `src/frontend/components/UI/Winetricks/index.tsx` lines 37 and 59.
Note the preload API-method names differ from the channel names — the frontend calls
`winetricksListAvailable`/`winetricksListInstalled`, not the channel names. A grep written against
the channel names alone will match nothing at the call sites (this exact mistake is recorded as
`F-34.5-G6-21`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the build under test genuinely has winetricks unported, before driving anything</name>
  <read_first>
    - `src/backend/sidecar/wineToolsFlowRegistration.ts` (confirm no `winetricks*` channel is registered on this commit)
    - `src/backend/sidecar/__tests__/wineToolsFlows.test.ts` lines 120-145 (the `T-34.5-15` absence assertion that is still green on this commit)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md` § Preconditions 1 and 2
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Establish and record, before the app is driven:
  - The commit SHA under test (`git rev-parse HEAD`).
  - That `winetricksAvailable`, `winetricksInstalled` and `winetricksInstall` are absent from both
    registries on this commit, by running
    `npx jest --selectProjects Backend --testPathPattern "wineToolsFlows" -t "T-34.5-15"` and
    confirming the absence assertion passes.
  - Build freshness per precondition 1: run `pnpm tauri:dev`, then confirm the RUNNING process is
    the one just built. `pnpm tauri:dev` exits 0 WITHOUT replacing an already-running instance and
    `pgrep GameLib` misses the `gamelib-shell` process name — so use `pgrep -fl gamelib-shell` and
    compare the binary's mtime against the commit timestamp. If a stale instance is running, quit
    it first. A whole UAT in this project once ran on an 8-hour-old build.
  - Archive `~/Library/Logs/GameLib/gamelib.log` to a run-scoped copy BEFORE driving anything, and
    start shell-scrollback capture with `tee -a` and a per-launch delimiter.

Fill precondition slots 1, 2 and the step-0 build-provenance fields in `34.6-LIVE-GATE.md` with
these MEASURED values. Do not fill any other precondition or step.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "wineToolsFlows" -t "T-34.5-15" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - The `T-34.5-15` absence test passes on the commit under test, and the jest output does NOT contain "no projects found matching selection".
    - `grep -c 'winetricks' src/backend/sidecar/wineToolsFlowRegistration.ts` returns 0 on this commit.
    - `34.6-LIVE-GATE.md` precondition 1 records a commit SHA and the `pgrep -fl gamelib-shell` output that proves the running binary matches it.
    - The archived pre-run copy of `gamelib.log` exists at a recorded path.
  </acceptance_criteria>
  <done>The build under test is proven to be one where the marker is still producible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive the Winetricks panel and capture the marker live</name>
  <read_first>
    - `src/frontend/components/UI/Winetricks/index.tsx` lines 30-70 (the two `callOrDeclare` sites and what UI action reaches each)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md` § Step 0
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Present the how-to-verify steps below to the operator and BLOCK. Do not proceed to task 3, and do not permit plan 34.6-07 to run, until the operator reports either the captured marker lines or an explicit FAIL. This capture is one-shot: the winetricks port destroys its own evidence, so a skipped or assumed result cannot be recovered later.
  </action>
  <what-built>
    Nothing was built. The `callOrDeclare` wrappers and the `DECLARED_UNAVAILABLE_MARKER` literal
    already shipped in Phase 34.5 plan 55. What has never happened is a live session opening one
    of the wrapped surfaces, so "declines honestly" is a source-level claim only
    (`34.5-UNTESTED-ITEMS.md` row `U-34.5-32`, and gate 5 on 2026-08-19 returned
    `grep -c "GAMELIB_DECLARED_UNAVAILABLE"` = 0 — an absence of OPPORTUNITY, not an absence of
    decline).
  </what-built>
  <how-to-verify>
    1. With the app running from the build proven in task 1, open a game's settings and reach the
       **Winetricks** panel (`src/frontend/components/UI/Winetricks/index.tsx`).
    2. Let the panel attempt to list installed and available components — this drives both
       `callOrDeclare` sites (lines 37 and 59).
    3. Confirm the panel renders an UNAVAILABLE state: no empty component list presented as
       success, and no reachable install action.
    4. In a terminal, run against the LIVE log (not the pre-run archive):
       `grep -n "GAMELIB_DECLARED_UNAVAILABLE" ~/Library/Logs/GameLib/gamelib.log`
    5. Expected: at least one line, and at least one of those lines names a `winetricks` channel.
       Copy the matching lines verbatim.
    6. If ZERO lines appear: this is a FAIL for step 0, NOT a reason to proceed. Report it —
       plan 34.6-07 must not run until step 0 is resolved, because the port destroys the evidence.
    7. If lines appear naming only `steamgriddb.*` channels: also a FAIL for step 0. The
       SteamGridDB half of `U-34.5-32` retires by PORT (D-10); it does not discharge the
       winetricks half.
  </how-to-verify>
  <acceptance_criteria>
    - At least one `[GAMELIB_DECLARED_UNAVAILABLE]` line exists in the live `gamelib.log`, naming a winetricks channel or API method, captured verbatim.
    - The Winetricks panel visibly showed an unavailable state — recorded in words, not inferred.
    - The captured lines contain NO game appName argument and no other user data — `callOrDeclare`'s contract deliberately excludes `args` because this is a public fork whose users paste log output. If an argument IS present, record it as a finding.
  </acceptance_criteria>
  <resume-signal>Paste the matching `gamelib.log` lines and describe the panel state, or report the FAIL.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record step 0's result in the gate contract and note what it does and does not discharge</name>
  <read_first>
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md` § Step 0
    - `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md` (the `U-34.5-32` row — read it, do not edit it here; plan 34.6-14 owns its disposition)
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Fill step 0's `**Result:**` slot in `34.6-LIVE-GATE.md` with: the verbatim matched log lines, the
commit SHA, the archived-log path, the panel state as observed by the operator, and a PASS or FAIL.

Add, in the same result slot, an explicit scope statement: this observation discharges the
WINETRICKS half of `U-34.5-32` only. It makes NO claim about the SteamGridDB half, which D-10
retires by PORT (plan 34.6-09) rather than by decline. Do not edit `34.5-UNTESTED-ITEMS.md` here —
plan 34.6-14 writes the two-part disposition once both halves are settled.

Leave `verdict:` as `null` and `status:` as `pending`. Step 0 passing is not a gate verdict; the
gate has ten scored items and no partial-pass mode.
  </action>
  <verify>
    <automated>F=$(ls .planning/phases/34.6-*/34.6-LIVE-GATE.md); grep -A6 'Step 0' "$F" | grep -c 'GAMELIB_DECLARED_UNAVAILABLE'</automated>
  </verify>
  <acceptance_criteria>
    - Step 0's `**Result:**` slot is no longer empty and contains a verbatim log line, a commit SHA and a PASS/FAIL word.
    - The frontmatter still reads `verdict: null` and `status: pending`.
    - The result slot states in words that this discharges the winetricks half of `U-34.5-32` only.
    - Every other `**Result:**` slot in the document is still empty (`grep -c '^\*\*Result:\*\* *$'` returns 9).
  </acceptance_criteria>
  <done>The one-shot observation is recorded, scoped honestly, and plan 34.6-07 is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `gamelib.log` → the gate document → a public repository | captured log lines are committed to a public fork |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-11 | Information Disclosure | a captured `[GAMELIB_DECLARED_UNAVAILABLE]` line carrying an appName argument | mitigate | Task 2's acceptance criteria require checking the captured line for arguments; `declaredUnavailable.ts`'s contract excludes `args` deliberately, citing `enableEosOverlay('')`/`disableEosOverlay('')` app-name arguments as the reason. An argument present is recorded as a FINDING, not pasted silently. |
| T-34.6-12 | Repudiation | claiming the capture happened on an unported build when it did not | mitigate | Task 1 records the commit SHA and RE-RUNS the `T-34.5-15` absence assertion on that commit rather than trusting the plan order. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed. |
</threat_model>

<verification>
- `npx jest --selectProjects Backend --testPathPattern "wineToolsFlows" -t "T-34.5-15"` passes on the commit under test.
- Step 0's result slot is filled; the other nine remain empty.
- No source file is modified by this plan.
</verification>

<success_criteria>
- `DECLARED_UNAVAILABLE_MARKER` has its first live observation in this project's history, for a winetricks channel.
- The observation is provenanced to a commit where the channel was genuinely unported.
- Plan 34.6-07 (the winetricks port) is unblocked; it MUST NOT have run before this point.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-04-SUMMARY.md` when done.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
</output>
