---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 10
subsystem: ipc
tags: [tauri, sidecar, ipc-registration, census, red-proof, electron-reach-ledger]

requires:
  - phase: 34.6-01/34.6-09
    provides: 21 of the phase's 24 in-scope channels already registered on the sidecar
      (runnerAuth, wineTools, shortcuts, appShell, install, eosOverlay, enrichment/SteamGridDB)
provides:
  - the last 3 of 8 late-discovered channels (`getAchievements`, `getDefaultSavePath`,
    `getPlaytimeFromRunner`) registered invoke-kind in `runnerMiscFlowRegistration.ts`
    (module now 14/14)
  - a full 24-channel census (`runnerSliceRegistration.test.ts` Describe 11), diffed at test
    runtime against `IPC-PORT-INVENTORY.md`
  - Describe 6/7 re-derived to 62 handle-kind / 5 listen-kind, per-module `[11,13,7,14,8,14]`=67,
    both directions RED-proven across all six modules
  - verification (not modification) that `EosDeclineCallSiteGuard.test.ts` (D-08 tripwire) stays
    green and byte-identical, plus an independently re-measured `callOrDeclare` census
  - proof the sidecar bundle boots post-all-changes
affects: [34.6-12, 34.6-13, 34.6-14]

tech-stack:
  added: []
  patterns:
    - "Isolated before/after measurement via `git show <commit>^:<path>` swap-in, to attribute an
       electron-reach-ledger delta to exactly one commit's change rather than accumulated state"

key-files:
  created: []
  modified:
    - src/backend/sidecar/runnerMiscFlowRegistration.ts
    - src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts
    - src/backend/sidecar/__tests__/flowRegistrationCensus.test.ts
    - src/backend/sidecar/__tests__/invokeReturnValueSweep.test.ts
    - src/backend/sidecar/__tests__/electronReachLedger.test.ts

key-decisions:
  - "D-14's ordering constraint satisfied: getDefaultSavePath lands on the sidecar before the
     phase's inherited legendary save-sync live-gate leg runs"
  - "electronReachLedger.test.ts's BASELINE_ELECTRON_REACHING_MODULES baseline update
     (save_sync.ts) is a MEASURED finding, not the plan's own <interfaces> prediction (which named
     a pre-existing syncGOGSaves edge) — the actual edge is the new getDefaultSavePath handler's
     own direct import"
  - "D-09's RED-proof was run across six modules (runnerAuth, wineTools, shortcuts, appShell,
     install, eosOverlay+enrichment folded, runnerMisc), one more than the plan's own
     <interfaces> section named, because this plan's own Task 1 changed runnerMisc's count"

requirements-completed: [REQ-34.6-04, REQ-34.6-08, REQ-34.6-09]

metrics:
  duration: ~110min
  completed: 2026-08-24
---

# Phase 34.6 Plan 10: Last 3 late-discovered channels, 24-channel census, D-09 both-directions RED-proof Summary

**Ported `getAchievements`/`getDefaultSavePath`/`getPlaytimeFromRunner` byte-equivalently into `runnerMiscFlowRegistration.ts`, built the phase's full 24-channel census, re-derived Describe 6/7 to 62/5/67 across six modules, and discovered via direct measurement (not the plan's own prediction) that `getDefaultSavePath`'s port pulls a new module — `save_sync.ts` — into the sidecar's electron-reach ledger.**

## Performance

- **Duration:** ~110 min
- **Tasks:** 3/3 completed (Task 3 verify-only, no code change)
- **Files modified:** 5

## Accomplishments

- `runnerMiscFlowRegistration.ts` is now complete at 14/14 invoke-kind channels: the four
  runner-CLI-version probes and two runtime channels (34.5-07), three "other" + two saves-sync
  channels (34.5-12), plus the three late-discovered channels this plan ported
  (`getAchievements`, `getDefaultSavePath`, `getPlaytimeFromRunner`), all three preserving
  `main.ts`'s exact behaviour byte-for-byte — including `getPlaytimeFromRunner`'s two bare
  `return` statements in its `disablePlaytimeSync` branch (resolving `undefined`, never `null`
  or `0`).
- Built the phase's full 24-channel census: a new Describe 11 in `runnerSliceRegistration.test.ts`
  asserts all 24 in-scope channels present by exact name and kind, diffed at test runtime against
  `IPC-PORT-INVENTORY.md` — the diff is empty.
- Re-derived Describe 6/7 by measurement (never prediction) to 62 handle-kind / 5 listen-kind
  channels, per-module tally `[11, 13, 7, 14, 8, 14]` totalling 67. Both directions of D-09
  (presence and absence) were RED-proven across all six modules this test spans — one more than
  the plan's own `<interfaces>` section named, because Task 1's channel-count change on
  `runnerMisc` itself required inclusion.
- Fixed a stale `register*Flows()` docstring on `runnerMiscFlowRegistration.ts` (still claimed
  "11 invoke-kind channels" / "complete at 11/11" despite Task 1 already landing the 3 new
  channels) — a Rule 1 bug found during the pin-maintenance sweep, required for
  `flowRegistrationCensus.test.ts`'s docstring Gate 2 to be non-vacuous.
- Updated and individually RED-proved three anti-rot pin-maintenance files:
  `flowRegistrationCensus.test.ts` (EXPECTED table 11→14), `invokeReturnValueSweep.test.ts`
  (total 37→40, `runnerMisc` module spec 11→14, all 3 new channels confirmed to prove a return
  value), and `electronReachLedger.test.ts` (see Deviations below for the unplanned baseline
  finding).
- Verified (did not modify) `EosDeclineCallSiteGuard.test.ts`: `git diff HEAD` against it is
  empty, and it is 9/9 green. Independently re-measured the `callOrDeclare(` census — 20 genuine
  call sites across 6 files (2 raw grep matches excluded as prose-comment false positives) — which
  agrees exactly with the plan's own prior research, reconciling the historical "11 vs 10 call
  sites" question by fresh measurement rather than transcription.
- Proved the sidecar bundle boots cleanly with all changes in place: `pnpm build:sidecar && node
  build/main/sidecar.js` prints `__GAMELIB_SIDECAR_READY__` with no errors.

## Task Commits

Each task committed atomically:

1. **Task 1: Port `getAchievements`/`getDefaultSavePath`/`getPlaytimeFromRunner`** — `5d3bf721d` (feat)
2. **Task 2: 24-channel census, D-09 both-directions RED-proof, pin maintenance** — `21b55e303` (test)
3. **Task 3: verify `EosDeclineCallSiteGuard.test.ts` unmodified, re-measure `callOrDeclare` census, prove bundle boots** — verify-only, no commit (no files changed)

**Plan metadata:** (this commit, see below)

## Files Created/Modified

- `src/backend/sidecar/runnerMiscFlowRegistration.ts` — added 3 invoke-kind channels
  (`getAchievements`, `getDefaultSavePath`, `getPlaytimeFromRunner`), fixed a stale docstring
- `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` — new Describe 11 (24-channel
  census vs `IPC-PORT-INVENTORY.md`), Describe 6/7 re-derived to 62/5/67
- `src/backend/sidecar/__tests__/flowRegistrationCensus.test.ts` — EXPECTED table entry for
  `runnerMiscFlowRegistration.ts` updated 11→14
- `src/backend/sidecar/__tests__/invokeReturnValueSweep.test.ts` — total 37→40, `runnerMisc`
  module spec 11→14
- `src/backend/sidecar/__tests__/electronReachLedger.test.ts` — `BASELINE_ELECTRON_REACHING_MODULES`
  gained `save_sync.ts`; forward-pointing UPDATE note added after the pre-existing Plan 34.5-13
  comment block

## RED-Proof Verbatim Failure Messages

All three pin-maintenance updates were individually RED-proven (temporarily reverted, confirmed
the exact failure, restored, confirmed green, confirmed a clean `git diff --stat` revert) before
being folded into the Task 2 commit.

**`flowRegistrationCensus.test.ts`** (EXPECTED reverted to `{ invoke: 11, send: 0 }`):
```
Expected: 11
Received: 14
```
Restored to 14: 68/68 green. `git diff --stat` after revert: `+3 -1` (clean).

**`invokeReturnValueSweep.test.ts`** (`expectedCount: 14`→`11`, `toHaveLength(40)`→`toHaveLength(37)`):
Two failures, both exact count mismatches on the reverted values (`runnerMisc has exactly 11
channels` and `extracts exactly 40 channels total`, each failing against the actual (still-14/40)
extracted counts). Restored from scratchpad backup: 7/7 green. `git diff --stat` after revert:
`+12 -6` (clean).

**`electronReachLedger.test.ts`** (`save_sync.ts` baseline entry replaced with a placeholder string):
```
A NEW electron-importing module has entered the sidecar's reach graph and is NOT in the
committed baseline: src/backend/save_sync.ts.
```
Restored from scratchpad backup: 4/4 green. `git diff --stat` after revert: `+22 -1` (clean).

**D-09 both-directions RED-proof** (`runnerSliceRegistration.test.ts` Describe 6/7/11): ran the
standard temporarily-break/confirm-exact-failure/restore/confirm-green cycle across all six
modules this test spans; every mutation produced the expected exact assertion-failure message on
first attempt and every revert produced a clean diff.

## Describe 6/7 Re-derivation

Independently re-derived (not transcribed from the plan): 62 handle-kind / 5 listen-kind channels,
per-module `[11, 13, 7, 14, 8, 14]` = 67. The `runnerMisc` module's contribution rose from 11 to
14 (this plan's own Task 1 delta), moving the handle-kind total from 59 (plan 34.6-09's
re-derivation) to 62. Listen-kind count (5) is unchanged — all three channels this plan ported are
invoke-kind.

## `callOrDeclare(` Census (independently re-measured)

| File | Genuine call sites |
|---|---|
| `src/frontend/components/UI/EditGameDialog/index.tsx` | 1 |
| `src/frontend/components/UI/SteamGridDBPicker/index.tsx` | 3 |
| `src/frontend/components/UI/Winetricks/index.tsx` | 2 |
| `src/frontend/screens/Library/components/InstallModal/SideloadDialog/index.tsx` | 1 |
| `src/frontend/screens/Settings/components/SteamGridDbApiKey.tsx` | 2 |
| `src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx` | 11 |
| **Total** | **20** |

Two comment-only false positives were excluded from the raw grep count: `AdvancedSettings/index.tsx:38`
(a prose comment referencing the pattern by name) and `eosOverlayFlowRegistration.ts:37` (a
docstring reference, D-08's own KEEP-callOrDeclare note). This 20-across-6-files figure agrees
exactly with the plan's own prior research and with REQ-34.6-09's text.

## electronReachLedger.test.ts: Measured Baseline Update (unplanned finding)

The plan's own `<interfaces>` note predicted `save_sync.ts` would enter the measured baseline via
a pre-existing `syncGOGSaves` import edge. That prediction did NOT hold on direct measurement. The
edge that actually fires is the new `getDefaultSavePath` handler's own direct import from
`../save_sync`.

Measured via an isolated `git show`-based swap of only `runnerMiscFlowRegistration.ts`'s
pre-Task-1 content (backed up to scratchpad first, restored after):

- **BEFORE** (pre-Task-1 `runnerMiscFlowRegistration.ts` swapped in): `electronImportingFiles.size`
  38, `visitedFiles.size` 252
- **AFTER** (post-Task-1, current committed content): `electronImportingFiles.size` 39,
  `visitedFiles.size` 253

A clean +1/+1, exactly `save_sync.ts` itself — everything it imports was already reached via other
entry points in the same baseline. This is a growth-only tripwire (not a strict-equality one), so
adding this one name is the correct, minimal update; no other module needed adding.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Stale `register*Flows()` docstring on `runnerMiscFlowRegistration.ts`**
- **Found during:** Task 2, pin-maintenance sweep
- **Issue:** The docstring immediately preceding `export function registerRunnerMiscFlows` still
  said "11 invoke-kind channels" / "complete at 11/11" despite Task 1 already having ported the 3
  new channels earlier in the same plan.
- **Fix:** Updated to "14 invoke-kind channels" / "complete at 14/14", with a note attributing the
  three new channels to Phase 34.6 Plan 10 (D-14).
- **Files modified:** `src/backend/sidecar/runnerMiscFlowRegistration.ts`
- **Commit:** `21b55e303`

**2. [Rule 1 - Bug, structural finding] `electronReachLedger.test.ts` baseline gap**
- **Found during:** Task 2, growth-tripwire re-run after Task 1's port
- **Issue:** `save_sync.ts` was not yet in `BASELINE_ELECTRON_REACHING_MODULES`, and the plan's own
  `<interfaces>` prediction for why it would enter (a pre-existing `syncGOGSaves` edge) did not
  hold on measurement — the actual new edge is `getDefaultSavePath`'s own direct import.
- **Fix:** Added `save_sync.ts` to the baseline array with a full measured before/after note;
  appended a forward-pointing UPDATE comment after the pre-existing Plan 34.5-13 note (which had
  correctly ruled out this reachability via a *different* edge at that earlier time) rather than
  deleting or rewriting that historical record.
- **Files modified:** `src/backend/sidecar/__tests__/electronReachLedger.test.ts`
- **Commit:** `21b55e303`

**3. [Rule 1 - not a bug, scope note] D-09 RED-proof spans six modules, not five**
- **Found during:** Task 2
- **Issue:** The plan's own `<interfaces>` section named five modules for the D-09 both-directions
  RED-proof; Task 1's channel-count change on `runnerMisc` made a sixth module's inclusion
  necessary for the proof to be non-vacuous against the plan's own change.
- **Fix:** Ran the standard RED-proof cycle across all six modules `runnerSliceRegistration.test.ts`
  spans, not just the five the plan named.
- **Files modified:** `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts`
- **Commit:** `21b55e303`

### None Other

No architectural changes, no auth gates, no blocked tasks. Task 3 required no code changes
(verify-only), as instructed by the plan's own conflict-resolution note (this plan's
`files_modified` frontmatter lists `EosDeclineCallSiteGuard.test.ts`, but REQ-34.6-09 requires it
stay unmodified — resolved by verifying, not editing).

## Verification

- `npx jest --selectProjects Backend`: 180/181 suites, 4229/4234 tests passed, 2 skipped, 3 failed
  — all 3 failures confined to `src/backend/storeManagers/steam/__tests__/decompressPool.test.ts`
  (pre-existing, Phase 23.1 lzmaLoader native-vs-pure-js decoder mismatch; not in scope for this
  plan, unchanged, untouched).
- `npx jest --selectProjects Frontend`: 122/122 suites, 2023/2023 tests, fully green.
- `npx tsc --noEmit -p .`: clean, no errors (run twice this session).
- Sidecar bundle boot: `pnpm build:sidecar` (1.3mb, "Done in 34ms") then `node
  build/main/sidecar.js` — `__GAMELIB_SIDECAR_READY__` observed cleanly in stdout, no errors.

## Known Stubs

None.

## Threat Flags

None. This plan ports 3 already-existing IPC handlers byte-equivalently (no new network endpoints,
auth paths, or trust-boundary changes) and otherwise only updates test/census files.

## Self-Check: PASSED

- `src/backend/sidecar/runnerMiscFlowRegistration.ts`: FOUND
- `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts`: FOUND
- `src/backend/sidecar/__tests__/flowRegistrationCensus.test.ts`: FOUND
- `src/backend/sidecar/__tests__/invokeReturnValueSweep.test.ts`: FOUND
- `src/backend/sidecar/__tests__/electronReachLedger.test.ts`: FOUND
- Commit `5d3bf721d`: FOUND in `git log`
- Commit `21b55e303`: FOUND in `git log`
