---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 01
subsystem: auth
tags: [rust, tauri, keyring, safeStorage, steamgriddb, secret-store, sidecar]

# Dependency graph
requires:
  - phase: 34.4.1
    provides: >-
      The HumbleSecretStore / SidecarKeyringSlotStore precedent (plans 12/13) this plan mirrors
      exactly, and the keyring_account() Rust allowlist this plan extends.
provides:
  - "steamgrid-api-key Rust keyring allowlist arm (keyring_account()), with 3 new unit tests"
  - "KEYRING_SLOT_STEAMGRID_API_KEY constant in src/backend/sidecar/keyringTokenStore.ts"
  - "Swappable SteamGridDbSecretStore seam (src/backend/steamgrid/secretStore.ts) with an Electron implementation behaviourally identical to today, including the ONE surviving legacy-plaintext migration codepath"
  - "secureKey.ts header amended in place: the TRIGGER CONDITION it stated is now discharged"
affects: [34.6-02, 34.6-09]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "SteamGridDbSecretStore mirrors HumbleSecretStore's swappable-singleton shape byte-for-byte (interface + Electron impl + setX/getX registry, no env-var auto-selection)"
    - "isAvailable() probes secureKey.ts's crypto primitives indirectly (encryptApiKey('probe') + isEncryptedValue()) rather than exporting a fourth function from secureKey.ts, since the seam module must never import electron directly"

key-files:
  created:
    - src/backend/steamgrid/secretStore.ts
    - src/backend/steamgrid/__tests__/secretStore.test.ts
  modified:
    - src-tauri/src/main.rs
    - src/backend/sidecar/keyringTokenStore.ts
    - src/backend/steamgrid/secureKey.ts

key-decisions:
  - "isAvailable() is implemented as a side-effect-free probe (encrypt a throwaway value, check the resulting prefix) rather than exporting secureKey.ts's private encryptionAvailable() — keeps secureKey.ts's three-export surface unchanged per the plan's explicit instruction, while still reflecting real safeStorage availability rather than key presence."
  - "Did NOT mint a bespoke SidecarSteamGridDbSecretStore subclass or wire keyring reads/writes in this plan — per the plan's explicit scope, that is plan 34.6-02's job, parameterising the generic SidecarKeyringSlotStore with the steamgrid-api-key slot this plan added to the Rust allowlist."

patterns-established:
  - "Pattern: a swappable secret-store seam's Electron implementation absorbs its feature's pre-existing inline legacy-plaintext migration branch (from ipc_handler.ts's getDecryptedApiKey()), rather than leaving two migration codepaths to reconcile later."

requirements-completed: [REQ-34.6-06]

# Metrics
duration: 45min
completed: 2026-08-23
---

# Phase 34.6 Plan 01: SteamGridDB API-key hardening seam (A-03, first half) Summary

**Added the `steamgrid-api-key` Rust keyring allowlist arm and a swappable `SteamGridDbSecretStore` TypeScript seam, landing BEFORE the SteamGridDB channels are ported (plan 34.6-09) per amendment A-03 — closing the plaintext-on-disk hazard at its source.**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-08-23T09:30:00Z (approx, first read)
- **Completed:** 2026-08-23T10:15:00Z (approx)
- **Tasks:** 3/3 completed
- **Files modified:** 5 (2 created, 3 modified)

## Accomplishments
- `keyring_account()` in `src-tauri/src/main.rs` now has a fourth allowlist arm for slot
  `steamgrid-api-key`, mapping to its own distinct account string, proven both directions
  (maps correctly AND rejects near-misses) by 3 new Rust unit tests.
- `KEYRING_SLOT_STEAMGRID_API_KEY` constant exported from `keyringTokenStore.ts`, ready for
  plan 34.6-02 to parameterise the generic `SidecarKeyringSlotStore` with.
- `src/backend/steamgrid/secretStore.ts` created: a `SteamGridDbSecretStore` interface (total-method
  contract: never throws, never rejects) plus `ElectronSteamGridDbSecretStore`, whose observable
  behaviour is byte-identical to today's `ipc_handler.ts` call shape — including the one-time
  legacy-plaintext-read-then-re-encrypt migration that used to live inline in `getDecryptedApiKey()`.
- `secureKey.ts`'s header comment amended in place: the TRIGGER CONDITION it named is now recorded
  as discharged by this exact plan, not left standing as a stale claim.
- 14 new backend unit tests covering the `sgdb:v1:` round-trip, the legacy-plaintext migration
  (RED-proven), the empty/absent cases, the encryption-unavailable fallback, and the total-method
  contract for all four interface methods (RED-proven).

## Task Commits

Each task was committed atomically, staging only its declared paths (`git commit --only`) to keep
concurrent-session work in the shared tree untouched:

1. **Task 1: Add the `steamgrid-api-key` arm to the Rust keyring allowlist, with collision and near-miss tests** - `d50583a29` (feat)
2. **Task 2: Create the swappable `SteamGridDbSecretStore` seam with an Electron implementation that is behaviourally identical to today** - `0c1d7d2d7` (feat)
3. **Task 3: Backend suite for the seam and the Electron implementation, including the legacy-plaintext and never-throws directions** - `d3c7420ed` (test)

_Plan metadata commit (this SUMMARY.md + STATE.md/ROADMAP.md hand-edits): committed separately, see below._

## Files Created/Modified
- `src-tauri/src/main.rs` - Added `"steamgrid-api-key" => Ok("steamgrid-api-key")` match arm to `keyring_account()`; rewrote the doc comment that previously claimed the slot "deliberately has NO slot here"; added 3 unit tests (mapping, collision-guard, near-miss rejection).
- `src/backend/sidecar/keyringTokenStore.ts` - Added `export const KEYRING_SLOT_STEAMGRID_API_KEY = 'steamgrid-api-key'`.
- `src/backend/steamgrid/secretStore.ts` (new) - `SteamGridDbSecretStore` interface, `ElectronSteamGridDbSecretStore`, `setSteamGridDbSecretStore`/`getSteamGridDbSecretStore` swappable singleton.
- `src/backend/steamgrid/secureKey.ts` - Amended header comment in place (F-1b provenance kept, TRIGGER CONDITION recorded as discharged); crypto primitive bodies (`isEncryptedValue`/`encryptApiKey`/`decryptApiKey`) unchanged.
- `src/backend/steamgrid/__tests__/secretStore.test.ts` (new) - 14 unit tests for the seam.

## Decisions Made
- `isAvailable()` probes `secureKey.ts`'s existing crypto primitives indirectly (`isEncryptedValue(encryptApiKey('probe'))`) rather than exporting a new `encryptionAvailable()` from `secureKey.ts` — satisfies both the plan's "keep the three exports unchanged" instruction and the acceptance criterion that `secretStore.ts` never imports `electron` directly, while still reflecting genuine `safeStorage` readiness rather than key presence.
- Scope held exactly to the plan: no `SidecarSteamGridDbSecretStore` subclass, no keyring read/write wiring, and `ipc_handler.ts` untouched — all explicitly deferred to plan 34.6-02 / 34.6-09 per the plan's own instructions.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Acceptance-criteria greps initially failed on the literal strings they were checking for**
- **Found during:** Task 2 verification
- **Issue:** The first drafts of `secretStore.ts`'s header comment and `secureKey.ts`'s rewritten header comment happened to contain the literal substrings `from 'electron'` and `TRIGGER CONDITION` respectively — inside prose describing the hazard, not as actual code — which made the plan's own acceptance-criteria greps (`grep -n "from 'electron'" secretStore.ts` should return no matches; `grep -c 'TRIGGER CONDITION' secureKey.ts` should return 0) fail even though the underlying code was correct.
- **Fix:** Reworded both comments to describe the same facts without using those exact literal strings (e.g. "its `safeStorage` import (sourced from the `electron` package)" instead of "`import { safeStorage } from 'electron'`"; "The condition that section said would trigger..." instead of "TRIGGER CONDITION").
- **Files modified:** `src/backend/steamgrid/secretStore.ts`, `src/backend/steamgrid/secureKey.ts`
- **Verification:** Both greps re-run and now pass as specified.
- **Committed in:** `0c1d7d2d7` (Task 2 commit)

**2. [Rule 1 - Bug] `git commit` (without `--only`) on Task 1 absorbed two unrelated concurrent-session renames**
- **Found during:** Task 1 commit
- **Issue:** Two files (`.planning/debug/steam-library-22-games-missing.md` → `resolved/...` and a `.planning/todos/pending/...` → `completed/...` rename) were already staged in the git index by a concurrent session before this executor started. The first `git commit -m ...` for Task 1 committed the entire index, not just the two files this executor had just `git add`ed, landing those two unrelated renames in this plan's commit.
- **Fix:** `git reset --soft HEAD~1` (moves HEAD only, does not touch the index or working tree) followed by `git commit --only src-tauri/src/main.rs src/backend/sidecar/keyringTokenStore.ts` to commit exactly the two declared files, leaving the concurrent renames staged in the index exactly as they were before. Every subsequent commit in this plan used `git commit --only <paths>`.
- **Files modified:** none (git history correction only; no source files touched)
- **Verification:** `git show --stat HEAD` confirmed exactly 2 files in the corrected commit; `git status --porcelain` confirmed the two concurrent renames remained staged (`RM ...`) afterward, untouched.
- **Committed in:** `d50583a29` (the corrected Task 1 commit)

**3. [Rule 1 - Bug] Test-file TypeScript error: spread argument into a zero-arity mock**
- **Found during:** Task 3 `tsc --noEmit` verification
- **Issue:** `jest.mock('backend/config', () => ({ GlobalConfig: { get: (...args) => mockGlobalConfigGet(...args) } }))` failed `tsc --noEmit` with "A spread argument must either have a tuple type or be passed to a rest parameter" — `mockGlobalConfigGet`'s inferred type (a zero-arg `jest.fn`) didn't accept a spread of `unknown[]`.
- **Fix:** Removed the unnecessary arg-forwarding; `GlobalConfig.get()` is always called with no arguments in the real code, so the mock's `get` is now `() => mockGlobalConfigGet()`.
- **Files modified:** `src/backend/steamgrid/__tests__/secretStore.test.ts`
- **Verification:** `npx tsc --noEmit` exits 0; test suite still 14/14 passing.
- **Committed in:** `d3c7420ed` (Task 3 commit)

---

**Total deviations:** 3 auto-fixed (2 Rule 1 comment-wording bugs against the plan's own acceptance greps, 1 Rule 1 git-history correction, 1 Rule 1 TypeScript type bug — grouped as 3 entries above since two of the comment fixes shared one root cause and are documented together in entry 1). All three are corrections to this plan's own artifacts (test file, comments, git history); none touch out-of-scope files. No scope creep.

## Issues Encountered
None beyond the deviations documented above.

## User Setup Required
None - no external service configuration required. No new dependencies were installed (`keyring` was already present in `src-tauri/Cargo.toml`, confirmed via `grep -n '^keyring'` before Task 1 per the threat model's T-34.6-SC mitigation).

## Verification Summary
- `cd src-tauri && cargo check` — clean, no new warnings.
- `cd src-tauri && cargo test keyring_account` — 10 passed, 0 failed (7 pre-existing + 3 new).
- `npx jest --selectProjects Backend --testPathPattern "steamgrid"` — 20 passed, 0 failed (14 new + 6 pre-existing in `utils.test.ts`), no "no projects found matching selection" printed.
- `npx tsc --noEmit` — exits 0.
- RED-proofs performed and reverted (not committed): (1) mutating the Rust match arm to a `starts_with("steamgrid")` wildcard made `keyring_account_rejects_steamgrid_near_miss_slots` fail; (2) deleting the re-encrypt branch from `getApiKey()` made exactly the legacy-plaintext test fail; (3) removing `clearApiKey()`'s catch made exactly the total-method `clearApiKey` test fail.
- `graphify update .` run after source changes landed (34,027 nodes, 40,455 edges) — `graphify-out/` is gitignored, no additional commit needed.

## Next Phase Readiness
- Plan 34.6-02 can now parameterise the generic `SidecarKeyringSlotStore` with `KEYRING_SLOT_STEAMGRID_API_KEY` to build the sidecar-side `SteamGridDbSecretStore` implementation and install it via `setSteamGridDbSecretStore()`.
- Plan 34.6-09 (the SteamGridDB channel port) is now unblocked from a hazard-sequencing standpoint: the keyring slot and seam both exist BEFORE any SteamGridDB channel reaches the sidecar's curated import graph, satisfying A-03's ordering requirement. Plan 34.6-09 still needs plan 34.6-02's sidecar-side implementation to land first, and separately owns deleting `ipc_handler.ts`'s now-redundant inline legacy-plaintext migration branch and reconciling it onto this seam.
- No blockers identified.

---
*Phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w*
*Completed: 2026-08-23*
