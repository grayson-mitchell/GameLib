---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 08
type: execute
wave: 4
depends_on: ["34.6-06", "34.6-07"]
files_modified:
  - src/backend/sidecar/eosOverlayFlowRegistration.ts
  - src/backend/sidecar/handlers.ts
  - src/backend/sidecar/__tests__/eosOverlayFlows.test.ts
  - src/backend/sidecar/__tests__/electronReachLedger.test.ts
  - src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts
autonomous: true
requirements: [REQ-34.6-01, REQ-34.6-08, REQ-34.6-13]
must_haves:
  truths:
    - "All 8 EOS overlay channels are registered invoke-kind on the sidecar via a new dedicated registration module"
    - "A-02: the dialog the D-05 round-trip actually hits is `remove()`'s at `eos_overlay.ts:162`, called unconditionally — `enable()`'s at `:197` is gated `if (!isInstalled())` and cannot fire in install->enable->disable->remove order"
    - "D-09: the EOS 8 flip from ABSENCE to PRESENCE in `runnerSliceRegistration.test.ts`, which requires a NEW module-scope import and call in that file, not merely an array edit"
    - "The electron-reach ledger gains the new module as an entry point and `eos_overlay.ts` as a reaching module"
  artifacts:
    - path: "src/backend/sidecar/eosOverlayFlowRegistration.ts"
      provides: "registerEosOverlayFlows() — 8 invoke handlers"
      exports: ["registerEosOverlayFlows"]
      min_lines: 40
    - path: "src/backend/sidecar/handlers.ts"
      provides: "the composition-root call"
      contains: "registerEosOverlayFlows()"
  key_links:
    - from: "src/backend/sidecar/eosOverlayFlowRegistration.ts"
      to: "src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts"
      via: "direct import of the logic module, never its sibling ipc_handler.ts"
      pattern: "eos_overlay/eos_overlay"
    - from: "src/backend/sidecar/handlers.ts"
      to: "registerEosOverlayFlows"
      via: "called alongside the existing register*Flows block, before ensureStoresRegistered()"
      pattern: "registerEosOverlayFlows\\(\\)"
---

<objective>
Port the 8 EOS overlay channels onto a new sidecar registration module and wire it into
`handlers.ts`.

This is the cluster D-05's live-gate step exercises with a FULL round-trip — install -> enable ->
disable -> remove — because that is the only path proving the legendary shell-out works through
the sidecar, and it is the path that traverses the 34.5 D-15 crash hazard (a dialog REJECTION
crashes the app under the sidecar).

**A-02 corrects D-05's citation and every plan and gate step must follow it.** The
`dialog.showMessageBox` inside `enable()` is at `eos_overlay.ts:197` (not `:194`) and is gated
`if (!isInstalled())`, so in the round-trip's order the preceding install has already made
`isInstalled()` true and it CANNOT fire. The dialog that DOES fire is `remove()`'s at `:162`,
called unconditionally, at the round-trip's LAST step. D-05's decision is unchanged; only the step
it fires at moves.

D-08 is also load-bearing here: all `callOrDeclare` wrappers STAY after the port. Once a channel is
ported, `spec.call()` resolves and `callOrDeclare` returns `{ ok: true, value }` — the wrapper
becomes a harmless pass-through needing no call-site change. `EosDeclineCallSiteGuard.test.ts` must
therefore stay green UNMODIFIED; plan 34.6-10 verifies that.

Purpose: the last unported Epic-adjacent cluster, registered.
Output: a new module, the composition-root wiring, and three gates amended in the same commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-07-SUMMARY.md

<interfaces>
Electron original (`src/backend/storeManagers/legendary/eos_overlay/ipc_handler.ts` — the file this
new module REPLACES as the wiring path, and never imports):
  import { getStatus, getLatestVersion, updateInfo, install, remove, enable, disable, isEnabled } from './eos_overlay'
  addHandler('getEosOverlayStatus', getStatus)
  addHandler('getLatestEosOverlayVersion', getLatestVersion)
  addHandler('updateEosOverlayInfo', updateInfo)
  addHandler('installEosOverlay', install)
  addHandler('removeEosOverlay', remove)
  addHandler('enableEosOverlay', async (e, appName) => enable(appName))
  addHandler('disableEosOverlay', async (e, appName) => disable(appName))
  addHandler('isEosOverlayEnabled', async (e, appName?) => isEnabled(appName))

`src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts` (318 lines):
  line 1     import { dialog } from 'electron'          // the new electron-reach edge
  :161-190   remove()      — dialog.showMessageBox at :162, UNCONDITIONAL
  :193-230   enable()      — dialog.showMessageBox at :197, gated `if (!isInstalled())`
  :249-251   isInstalled() — existsSync-based
  :295       getWinePrefixFolder() — `if (!isLinux || !appName) return null`, so enable() runs
             prefix-less on macOS. This one IS genuinely platform-gated, unlike the winetricks case.

Structural template: `src/backend/sidecar/wineToolsFlowRegistration.ts` — single feature surface,
invoke-heavy, one dialog round-trip buried in one call.

`handlers.ts` wiring idiom, lines 205-214: an inline comment in the form
`// Phase 34.X Plan NN (REQ-...): <what> — no ordering constraint relative to the other calls
above.` immediately preceding each `register*Flows()` call, all placed before
`ensureStoresRegistered()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `eosOverlayFlowRegistration.ts` with all 8 handlers, and wire it into `handlers.ts`</name>
  <read_first>
    - `src/backend/storeManagers/legendary/eos_overlay/ipc_handler.ts` in full (the 8 registrations and their exact argument shapes — note that five take no arguments and three take an appName, one of them optional)
    - `src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts` lines 1-10, 155-235 and 245-300 (the electron import, both dialogs and their guard conditions, `isInstalled`, `getWinePrefixFolder`)
    - `src/backend/sidecar/wineToolsFlowRegistration.ts` lines 1-90 (the module docstring convention and the curated-import rule stated there verbatim)
    - `src/backend/sidecar/handlers.ts` lines 200-220 (the existing `register*Flows()` block and its comment convention)
  </read_first>
  <files>src/backend/sidecar/eosOverlayFlowRegistration.ts, src/backend/sidecar/handlers.ts, src/backend/sidecar/__tests__/eosOverlayFlows.test.ts</files>
  <behavior>
    - `registerEosOverlayFlows()` puts all 8 channel names in `handlerRegistry` and none in `listenerRegistry`.
    - `enableEosOverlay` forwards its first argument to `enable(appName)` unchanged.
    - `isEosOverlayEnabled` works when called with NO argument (the Electron signature has `appName?` optional) and does not throw.
    - Calling `registerEosOverlayFlows()` twice does not double-register (matching the idempotence property the other registration modules' suites assert).
    - The module contains no `ipcMain.on` call.
  </behavior>
  <action>
Create `src/backend/sidecar/eosOverlayFlowRegistration.ts`, structurally mirroring
`wineToolsFlowRegistration.ts`: a top-of-file docstring naming the cluster, the phase and plan, and
restating the curated-import rule; then a single
`export function registerEosOverlayFlows(): void` containing 8 `ipcMain.handle(...)` calls, one
per channel, delegating to the eight named exports of `eos_overlay.ts`.

Import `ipcMain` from `./electronStub` and the eight functions DIRECTLY from
`../storeManagers/legendary/eos_overlay/eos_overlay`. **Never import
`eos_overlay/ipc_handler.ts`** — it calls `addHandler` from `backend/ipc`, an Electron-only path.
Add the same comment-stripped no-`ipc_handler` guard assertion the other suites carry.

Preserve each channel's argument shape exactly: five take none (`getEosOverlayStatus`,
`getLatestEosOverlayVersion`, `updateEosOverlayInfo`, `installEosOverlay`, `removeEosOverlay`),
`enableEosOverlay` and `disableEosOverlay` take an appName, and `isEosOverlayEnabled` takes an
OPTIONAL appName.

Record in the module docstring, citing A-02 so no later reader re-derives it wrong: the
`dialog.showMessageBox` this cluster reaches in the D-05 round-trip is `remove()`'s at
`eos_overlay.ts:162`, unconditional; `enable()`'s at `:197` is gated `if (!isInstalled())` and
cannot fire once install has run. Also record that `getWinePrefixFolder()` at `:295` returns `null`
on `!isLinux`, so `enable()` runs prefix-less on macOS — that is genuine upstream behaviour, not a
port artefact.

In `src/backend/sidecar/handlers.ts`, add the import and the call. Place
`registerEosOverlayFlows()` alongside the existing block that calls `registerRunnerAuthFlows()` /
`registerWineToolsFlows()` / `registerShortcutsFlows()` / `registerRunnerMiscFlows()` (lines
211-214), before `ensureStoresRegistered()`, with an inline comment in the file's own convention:
`// Phase 34.6 Plan 08 (REQ-34.6-01): the 8 EOS overlay channels — no ordering constraint relative
to the other calls above.` Extend the file's top docstring the same way each prior slice did.

Create `src/backend/sidecar/__tests__/eosOverlayFlows.test.ts` covering the five behaviours above
plus a curated-import guard test copied from `wineToolsFlows.test.ts`'s `T-34.5-15` shape.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "eosOverlayFlows|handlers" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle(" src/backend/sidecar/eosOverlayFlowRegistration.ts` returns exactly 8, and `grep -c "ipcMain.on(" ...` returns 0.
    - All 8 channel names are present: `for c in getEosOverlayStatus getLatestEosOverlayVersion updateEosOverlayInfo installEosOverlay removeEosOverlay enableEosOverlay disableEosOverlay isEosOverlayEnabled; do grep -q "'$c'" src/backend/sidecar/eosOverlayFlowRegistration.ts || echo "MISSING $c"; done` prints nothing.
    - `grep -v '^ *[/*]' src/backend/sidecar/eosOverlayFlowRegistration.ts | grep -c 'ipc_handler'` returns 0.
    - `grep -c "registerEosOverlayFlows" src/backend/sidecar/handlers.ts` returns 2 (the import and the call), and the call line number is LESS than the `ensureStoresRegistered()` line number.
    - The `isEosOverlayEnabled`-with-no-argument test passes; it is RED-proven by making the parameter required. Revert after proving.
    - `pnpm build:sidecar && node build/main/sidecar.js` starts and prints the ready sentinel — a new import into the registration chain can kill the sidecar in a way both a green suite and a clean build miss.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All 8 EOS channels are registered from a new module wired into the composition root, and the sidecar bundle still boots.</done>
</task>

<task type="auto">
  <name>Task 2: Grow the electron-reach ledger for the new module and its `dialog` edge</name>
  <read_first>
    - `src/backend/sidecar/__tests__/electronReachLedger.test.ts` lines 1-100 and 234-300
    - `src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts` line 1 (`import { dialog } from 'electron'`)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md` § 10
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-06-SUMMARY.md` (the ledger state plans 34.6-05/06 left)
  </read_first>
  <files>src/backend/sidecar/__tests__/electronReachLedger.test.ts</files>
  <action>
Add `join(REPO_ROOT, 'src/backend/sidecar/eosOverlayFlowRegistration.ts')` to `ENTRY_POINTS` with a
`// Phase 34.6 Plan 08 (REQ-34.6-13): ...` comment noting it is a brand-new registration module and
that `eos_overlay.ts` reaches `electron` directly at its own line 1.

Then RUN the test and read its failure output to DISCOVER the full set of modules to add to
`BASELINE_ELECTRON_REACHING_MODULES`. `eos_overlay.ts` is one of them by direct grep, but do NOT
stop at the predicted one — legendary's own command and library modules are transitively reachable
from it and a planning-time prediction of ledger entries has been wrong in this project before
(34.4 predicted two, needed three). Add exactly what the measurement names, each with a one-line
comment naming the electron symbol it imports.

The ledger is growth-only. Extend the array; never loosen the assertion.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "electronReachLedger" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "eosOverlayFlowRegistration.ts" src/backend/sidecar/__tests__/electronReachLedger.test.ts` returns at least 1 inside `ENTRY_POINTS`.
    - `grep -c "eos_overlay/eos_overlay.ts" src/backend/sidecar/__tests__/electronReachLedger.test.ts` returns at least 1 inside `BASELINE_ELECTRON_REACHING_MODULES`.
    - The suite passes with 0 failures.
    - The SUMMARY records the MEASURED list of newly-added baseline modules and states whether it exceeded the single predicted entry.
    - `git diff HEAD -- src/backend/sidecar/__tests__/electronReachLedger.test.ts` shows only array additions and comments.
  </acceptance_criteria>
  <done>The EOS module and its electron reach are on the committed baseline that IS the Phase 35 cutover work-list.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the EOS 8 to PRESENCE in `runnerSliceRegistration.test.ts`, including the module-scope wiring that flip requires</name>
  <read_first>
    - `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` lines 75-100 (the module-scope block — it clears both registries and calls ONLY the four Phase-34.5 modules; this is the scope finding PATTERNS delta 3 records) and lines 400-541 (Describe 6 and Describe 7 as left by plan 34.6-07)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-07-SUMMARY.md` (the measured Describe 6 values plan 34.6-07 left, so this plan's deltas are additive to that state)
    - `src/backend/sidecar/eosOverlayFlowRegistration.ts` (created in task 1)
  </read_first>
  <files>src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts</files>
  <action>
**The module-scope wiring is mandatory and is the part an array-only edit would miss.** This file's
module scope calls exactly four registration functions. `registerEosOverlayFlows()` is not among
them, so the EOS channels' registries are EMPTY here — flipping the 8 names to a presence assertion
without first adding the import and the call would produce 8 failing tests, and "fixing" that by
softening the assertion would produce a gate that measures nothing.

Add `import { registerEosOverlayFlows } from '../eosOverlayFlowRegistration'` and call it in the
module-scope block alongside the existing four, BEFORE the `CANONICAL_HANDLER_ENTRIES` /
`CANONICAL_LISTENER_ENTRIES` snapshot is taken — the snapshot is what
`restoreCanonicalRegistrations()` replays, and a registration added after it would be invisible to
every describe.

**Describe 7 (D-09).** Remove the 8 names from `DEFERRED_EOS_OVERLAY` so that array is empty (or
delete it and its spread), and re-derive the self-containment literal by REMOVING the names, not by
retyping it: after this task it is `8` (16 - 8), and plan 34.6-09 takes it to 3. `DROPPED_ZOOM`'s 3
names stay, asserting absence.

Add the 8 EOS names to the PRESENCE assertion plan 34.6-07 introduced (or a parallel `it.each`
following this file's own `MODULES`-driven idiom at lines 174-204), asserting
`handlerRegistry.has(channel) === true` and `(listenerRegistry.get(channel) ?? []).length === 0`
for each — all 8 are invoke-kind.

**Describe 6.** Decide and record explicitly whether `eosOverlayFlowRegistration` joins the
`MODULES` table. It is not one of the four Phase-34.5 modules that describe's title names, so the
honest choice is to WIDEN the describe's own title and the `MODULES` table together — a count that
silently absorbs a fifth module under a title saying "all four modules" is a stale claim of exactly
the kind this project has been bitten by. Whichever you choose, the `handleChannels.length` and
`listenChannels.length` pins must be re-derived by MEASUREMENT (run the suite, read the actual),
then independently re-derived and confirmed to agree. If they disagree, stop and report.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "runnerSliceRegistration" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "registerEosOverlayFlows()" src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` returns a match whose line number is LESS than the `CANONICAL_HANDLER_ENTRIES` declaration line.
    - The whole suite passes with 0 failures.
    - `grep -n "toBe(8)" src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` returns a match inside Describe 7's self-containment test.
    - All 8 EOS names appear in a presence assertion and none appear in the absence array: `for c in getEosOverlayStatus getLatestEosOverlayVersion updateEosOverlayInfo installEosOverlay removeEosOverlay enableEosOverlay disableEosOverlay isEosOverlayEnabled; do ...; done` confirms each.
    - Both directions are RED-proven: (a) commenting out one `ipcMain.handle` in `eosOverlayFlowRegistration.ts` makes exactly that channel's presence assertion FAIL; (b) the Zoom absence assertion still fails when a fake `authZoom` handler is registered. Revert both after proving; record both failure messages in the SUMMARY.
    - The SUMMARY records the measured Describe 6 values and the explicit decision about whether `MODULES` was widened, with the describe title updated to match.
  </acceptance_criteria>
  <done>The EOS 8 assert presence from a genuinely-registered module scope, Zoom's 3 still assert absence, and the arithmetic came from measurement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar (8 EOS channels) | an appName crosses on three of them and reaches a legendary shell-out |
| sidecar → Rust shell (`dialog.showMessageBox`) | `remove()` blocks on an operator decision; a dialog REJECTION crashes the app under the sidecar (34.5 D-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-21 | Denial of Service | `eos_overlay.ts:162`'s unconditional `dialog.showMessageBox` under the sidecar — the 34.5 D-15 crash hazard | mitigate | Ported byte-equivalently (no new dialog is introduced), and the hazard is EXERCISED deliberately at live-gate step 2's remove step rather than left unobserved. A-02 fixes which step to watch: a report of "expected a dialog at enable, none appeared" is the CORRECT outcome. The module docstring records this so no later reader re-derives it wrong. |
| T-34.6-22 | Tampering | an appName argument reaching a legendary shell-out via `enableEosOverlay`/`disableEosOverlay` | accept | Byte-equivalent port of an existing Electron path; the argument comes from the app's own game list, not free-form user input, and the surface is unchanged by this phase. Not in `T-34.5-C6-49-03`'s scope, which named only `importGame`, `moveInstall` and `runWineCommandForGame`. |
| T-34.6-23 | Information Disclosure | a log line echoing an appName from an EOS handler | mitigate | No new log line is added by this plan. `declaredUnavailable.ts`'s own doc comment cites `enableEosOverlay('')`/`disableEosOverlay('')` app-name arguments as the reason it excludes `args` — do not reintroduce them here. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed. |
</threat_model>

<verification>
- `npx jest --selectProjects Backend --testPathPattern "eosOverlayFlows|handlers|electronReachLedger|runnerSliceRegistration"` — green, 0 failures.
- `npx tsc --noEmit` — exits 0.
- `pnpm build:sidecar && node build/main/sidecar.js` — starts and prints the ready sentinel.
- `npx jest --selectProjects Frontend --testPathPattern "EosDeclineCallSiteGuard"` — still green and UNMODIFIED (D-08). Plan 34.6-10 owns the formal verification; run it here as an early tripwire.
- `npm start` (Electron) unchanged — `eos_overlay/ipc_handler.ts` is untouched.
</verification>

<success_criteria>
- 8 EOS channels registered invoke-kind from a new module wired into `handlers.ts`.
- A-02's dialog correction is recorded at the code, not only in planning documents.
- The electron-reach ledger measures the new module and its `dialog` edge, from a measurement.
- Describe 7's absence array is down to Zoom's 3 plus SteamGridDB's 5; the EOS 8 assert presence.
- HEAD is green at the end of this plan.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-08-SUMMARY.md` when done, recording the RED-proof failure messages, the measured ledger additions and the `MODULES`-table decision.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
Run `graphify update .` after the source changes land.
</output>
