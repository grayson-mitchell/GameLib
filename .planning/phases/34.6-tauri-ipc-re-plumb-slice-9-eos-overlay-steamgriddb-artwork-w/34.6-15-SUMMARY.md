---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 15
subsystem: testing
tags: [live-gate, contract-amendment, supersedes, uat, gap-closure]

requires:
  - phase: 34.6-12
    provides: "run 1's measured results and the six contract defects it found in the gate's own text, recorded as prose"
provides:
  - "six dated, marked SUPERSEDES sections amending 34.6-LIVE-GATE.md in place"
  - "two rescores from run 1's own evidence: Step 2 BLOCKED -> PASS (macOS-scoped), Step 5's runWineCommandForGame BLOCKED -> UNREACHABLE-BY-CONSTRUCTION (unscored)"
  - "a corrected contract for run 2 to be scored against"
affects: ["34.6-17"]

tech-stack:
  added: []
  patterns:
    - "A corrected gate condition must name the PROPERTY, never a landmark, literal or count a correct installation can fail"
    - "Amend in place with a marked SUPERSEDES section plus a pointer AT the original clause; never delete the original, never regenerate the document"
    - "A mechanical assertion is proven in BOTH directions — cleared against the real input, failed against a known-bad one — or it is marked operator-driven with no fabricated proof"

key-files:
  created: []
  modified:
    - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md

key-decisions:
  - "Step 5's runWineCommandForGame is removed from the scored DENOMINATOR rather than passed — an unexercised channel is not a working one, and an amendment must not read as if it were"
  - "Precondition 9's console recipe is REMOVED, not demoted to a fallback: a route the operator cannot execute is not a fallback, and keeping it invites a future run to attempt it and record a false BLOCKED"
  - "Steps 4 and 8 deliberately untouched — amending a step so its own FAIL stops counting is the one move this plan must not make"
  - "verdict: left at FAIL; re-adjudication is plan 34.6-17's, after run 2"

patterns-established:
  - "Scope discipline stated ONCE at the head of an amendment block and declared binding on all its sections, rather than repeated per section where it can drift"

requirements-completed: [REQ-34.6-11, REQ-34.6-12]

duration: ~35min
completed: 2026-08-25
---

# Phase 34.6 Plan 15: Supersede the Gate's Six Contract Defects Summary

**Gave all six contract defects run 1 found in `34.6-LIVE-GATE.md`'s own text the formal in-place SUPERSEDES treatment D-12 requires — 308 insertions, 1 deletion — rescoring two items from evidence run 1 already captured and leaving both FAILs untouched.**

## Performance

- **Duration:** ~35 min
- **Tasks:** 3
- **Files modified:** 1

## Accomplishments

- **Six marked, dated SUPERSEDES sections**, each quoting its original clause verbatim, stating why it is defective from a *re-measured* fact, and giving the corrected clause as the property the step exists to protect.
- **Six pointers inserted at the original clauses**, so a reader arriving at a superseded clause is told so in place rather than having to know the amendment exists.
- **Every underlying fact re-measured against today's source before amending** — not carried forward from run 1.
- **Two mechanical assertions proven in both directions**; the four operator-driven ones state a falsifying observation and explicitly offer no fabricated proof.
- **Run 1's record intact:** `git diff --numstat` reports 308 insertions and exactly 1 deletion, that deletion being the single `runWineCommandForGame` line deliberately replaced in place, whose text is preserved inside the replacement.

## Task Commits

1. **Tasks 1–3 (re-measure, amend, prove)** — `a6a676e21` (docs)

Tasks 1–3 landed as one commit rather than three. The amendment text *cites* the Task 1 measurements and *contains* the Task 3 non-vacuity proofs, so splitting them would have produced two intermediate commits carrying a document in a knowingly incomplete state — an amendment block asserting facts it had not yet recorded. Recorded as a deviation below.

## Files Created/Modified

- `.planning/phases/34.6-.../34.6-LIVE-GATE.md` — six SUPERSEDES sections and an amendment tally appended; six pointers inserted at the superseded clauses

## Re-measurements (Task 1)

Every fact confirmed still true, against today's source:

| # | Fact | Measured |
|---|------|----------|
| 1 | EOS platform gates | `AdvancedSettings/index.tsx:66` `isWindows`, `:401` gate · `GameSubMenu/index.tsx:81` `isLinux`, `:461` gate — still opposite, still neither covering macOS |
| 2 | `config.json` key state | `.defaultSettings.steamGridDbApiKey` = `''`; `grep -c` returns **1** on a correct install |
| 3 | `runWineCommandForGame` census | `grep -rn ... src/frontend/ \| wc -l` → **0** |
| 4 | EGL emptiness | run 1's four on-disk checks, named individually rather than summarised |
| 5 | Fixture/runner mismatch | precondition 8 named World of Goo (Steam 22000); the channels were driven against GOG titles Endless Sky/Balrum |
| 6 | DevTools console limitation | confirmed twice before this gate was authored — Phase 34.1 UAT item 5b (2026-08-22), Phase 34.5 gate 4 (2026-08-12) |

**One measurement came back STRONGER than this plan's own prescription.** The plan predicted #2's grep "fires on a correct installation". It is worse than that, and structurally so: `src/backend/config.ts:380` ships `steamGridDbApiKey: ''` as a default, and `src/backend/sidecar/steamgridSecretStore.ts:198` explicitly writes `''` back *after* migrating a key into the keyring. A correct, post-migration install is therefore **guaranteed** to be in the exact state the original clause scored as a FAIL. The amendment records the structural reason, not just the observation.

## Non-vacuity proofs (Task 3)

**Mechanical, both directions:**

| Assertion | Real input | Known-bad input |
|-----------|-----------|-----------------|
| #2 — `.defaultSettings.steamGridDbApiKey` is empty | **PASS** (`''`) | **FAIL** (copy carrying `'deadbeefcafe1234'`) |
| #3 — zero `runWineCommandForGame` renderer call sites | **0** over `src/frontend/` | **1** over a scratch file containing a call site |

**Operator-driven (#1, #4, #5, #6):** each states its falsifying observation in one sentence and is explicitly marked as having no mechanical proof. No proof was fabricated for a condition that has none.

## Decisions Made

- **`runWineCommandForGame` leaves the scored denominator; it does not become a PASS.** The channel is declared, exposed and registered with zero callers. Recording it as passed would claim a working channel from a census that only proves nobody calls it — the amendment says "unexercised", and says it ships that way.
- **Precondition 9's console recipe is removed outright.** Demoting it to a fallback would leave a route in the document that the operator cannot execute, inviting a future run to attempt it and record a false BLOCKED. D-13's identity bar — the part that actually caught Step 8's FAIL — is unchanged; only the fixture's provenance moves from planted to app-placed.
- **Step 2's rescore is taken from run 1's evidence, with no re-drive.** Run 1 recorded both in-scope operations completing a genuine sidecar round-trip and the dialog appearing at `remove()`. Re-driving a settled observation is how a UAT run manufactures evidence, which this project has on record.
- **Scope discipline is stated once, at the head of the amendment block, and declared binding on all six sections** rather than repeated per section, where a restatement can silently drop a qualifier.

## Deviations from Plan

**1. [Commit granularity] Tasks 1–3 committed together rather than atomically**

- **Found during:** Task 2
- **Issue:** The plan implies three commits. But the amendment text cites Task 1's measurements inline and embeds Task 3's both-directions proof tables, so a Task-2-only commit would have published an amendment block asserting measurements it had not yet recorded and claiming non-vacuity it had not yet proven.
- **Fix:** One commit covering all three, with the task boundaries preserved in this summary and in the document's own structure.
- **Verification:** All three tasks' acceptance criteria independently checked against the committed file.
- **Committed in:** `a6a676e21`

**2. [Tooling] First pointer-insertion pass aborted on an ambiguous anchor**

- **Found during:** Task 2
- **Issue:** The insertion script anchored on clause text that, after the amendment block was appended, appeared **twice** — once in the original clause and once inside the SUPERSEDES section quoting it verbatim. The script's own uniqueness guard caught this and aborted before writing.
- **Fix:** Split the document at the amendment marker and restricted pointer insertion to the original body.
- **Verification:** All 6 pointers present; `git diff` confirms 1 deletion, the intended one.
- **Note:** Worth keeping in mind for plan 34.6-17 — quoting an original clause verbatim inside an amendment makes that clause's text non-unique for any later scripted edit. Anchor on the pre-amendment region, or on line position.

---

**Total deviations:** 2 (1 commit-granularity, 1 tooling)
**Impact on plan:** Neither changes any amendment's content. No scope creep.

## Issues Encountered

None beyond the anchor ambiguity above, which the script's own guard caught before any write.

## User Setup Required

None.

## Next Phase Readiness

**The contract is now correct and run 2 can be scored against it.** Plan `34.6-17` is unblocked on this side.

**It is still blocked on plan `34.6-16`** — `winetricksInstall`'s fix — because Step 4 is run 2's only live leg and there is nothing to re-drive until the fix lands.

**And it still cannot reach `PASS n/n`.** Step 8 remains FAIL, untouched by design. Plan `34.6-17` Task 3 blocks on that as an operator decision: fix it, re-scope it explicitly via a seventh SUPERSEDES, or close the phase FAIL.

---
*Phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w*
*Completed: 2026-08-25*
