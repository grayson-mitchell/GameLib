---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 12
type: execute
wave: 8
depends_on: ["34.6-03", "34.6-04", "34.6-10", "34.6-11"]
files_modified:
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
autonomous: false
requirements: [REQ-34.6-11, REQ-34.6-12]
must_haves:
  truths:
    - "D-12: ONE blocking live gate runs, sequenced — Epic embedded login FIRST (step 1), Epic logout LAST (step 8)"
    - "D-15: the Epic login leg used the EMBEDDED/primary tile; SIDLogin did not substitute for it"
    - "D-05 as corrected by A-02: the EOS round-trip completed and the confirmation dialog was observed at remove(), not enable()"
    - "D-06: a REAL SteamGridDB API key drove the full chain and artwork rendered, with the key verified in the Keychain and absent from config.json"
    - "D-04 as corrected by A-01: the winetricks channels resolved with real data and the D-11 observable proved the handler was reached — no decline was expected or required"
    - "D-07: the 8 late-discovered channels were exercised on the live Epic/GOG session"
    - "D-14: the legendary save-sync round-trip proved BOTH directions by content, not by exit code"
    - "D-13: Epic logout was scored by the survival of a PLANTED, NAMED foreign cookie, not by a count"
  artifacts:
    - path: ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md"
      provides: "all ten result slots filled, a verdict, and a closing statement"
      contains: "verdict:"
  key_links:
    - from: "34.6-LIVE-GATE.md preconditions"
      to: "the running build"
      via: "a commit SHA and a pgrep -fl gamelib-shell check proving the binary under test"
      pattern: "gamelib-shell"
---

<objective>
Run the ONE blocking live gate (D-12) and fill its remaining nine result slots.

Step 0 already ran (plan 34.6-04) and its slot is filled. This plan runs steps 1 through 8 plus the
optional Humble rider, IN THE CONTRACT'S FIXED ORDER. The order is not a convenience: the Epic
session step 1 establishes is a precondition for steps 5, 6 and 7, and step 8's
`clearEpicCookies` DESTROYS it.

There is no partial-pass mode (34.5 D-08). An item is PASS, FAIL or NOT ATTEMPTED, and NOT
ATTEMPTED is not a pass. A FAIL is a BLOCKING finding for this phase, resolved by a gap cycle
inside 34.6 — not an advisory note, and not a pre-authorized override.

Purpose: exercise 24 newly-ported channels and four never-run inherited verification legs on real
macOS hardware.
Output: `34.6-LIVE-GATE.md` with ten filled slots, a verdict, and a closing statement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VALIDATION.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-10-SUMMARY.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-11-SUMMARY.md

Read `Skill("spike-findings-gamelib")` § "Live-gate contract authoring" for the dual-sink
append-and-archive capture standard, and § "Tauri gotchas" for the build-freshness and
DevTools-console constraints.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Satisfy and record every precondition BEFORE driving anything</name>
  <read_first>
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md` § Preconditions (all nine)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-04-SUMMARY.md` (what step 0 already recorded, so preconditions 1 and 2 are re-measured for THIS run, not copied)
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Work through preconditions 1-9 in order and fill each `Measured:` slot with an EXACT value and its
evidence command. Do not attest; measure. An unstated precondition is how a gate gets skipped and
becomes UAT debt (Phases 21/30/34.2 each record this).

Specific hazards to defeat, each of which has bitten this project:
  - **Build freshness (precondition 1).** `pnpm tauri:dev` exits 0 WITHOUT replacing an already-
    running instance, and `pgrep GameLib` misses the process name `gamelib-shell`. Quit any running
    instance first, then start, then prove the running binary matches `git rev-parse HEAD` via
    `pgrep -fl gamelib-shell` plus the binary's mtime. A whole UAT here once ran on an 8-hour-old
    build. Also: `tauri dev` serves a STALE bundle — use `pnpm tauri:dev`.
  - **Log capture (precondition 2).** Archive `~/Library/Logs/GameLib/gamelib.log` to a run-scoped
    copy BEFORE launching, and capture shell scrollback with `tee -a` and per-launch delimiters.
    The sidecar's own console and logger are invisible because stdout IS the RPC pipe, and
    `eprintln!` in the Rust shell goes to stderr and never reaches `gamelib.log`. Both sinks are
    required; a clean grep of the wrong sink is not evidence of absence.
  - **Secret-store arm (precondition 3).** Confirm `[bootstrap] secret stores: keyring` AND
    `[bootstrap] steamgrid secret store: keyring` both appear for this launch. If
    `GAMELIB_DEV_SECRET_VAULT=1` is set, unset it — a dev-vault boot invalidates step 3's Keychain
    assertion entirely.
  - **Epic logged OUT at start (precondition 4).** The cookie jar is sticky across restarts, so a
    scripted or manual login test needs a logout preamble. Record how logged-out state was
    confirmed.
  - **Backups (preconditions 6, 7).** The EOS/legendary restore point and the save-directory backup
    must exist BEFORE step 2 and step 7 respectively. D-14's upload half can DESTROY a save.
  - **Cookie census (precondition 9).** Record `before total` and the Epic-domain count, and plant
    the named foreign cookie NOW, recording its exact name and domain. Planting it after step 1
    risks the login flow clearing it.

If any precondition cannot be satisfied, STOP and report which one. Running a gate on an
unsatisfied precondition is how `34.4.1-35-PLAN.md`'s precondition 6 ended up UNSTRUCK.
  </action>
  <verify>
    <automated>F=$(ls .planning/phases/34.6-*/34.6-LIVE-GATE.md); grep -c '^\*\*Measured:\*\* *$' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the nine `Measured:` slots is non-empty (`grep -c '^\*\*Measured:\*\* *$'` returns 0).
    - Precondition 1 records a commit SHA and the `pgrep -fl gamelib-shell` output for THIS run.
    - Precondition 3 records both `[bootstrap]` receipt greps with counts of at least 1.
    - Precondition 9 records `before total`, the Epic-domain count, and the planted cookie's name and domain — the domain WITHOUT a leading-dot ambiguity, since a leading dot has produced a false cookie match in this project before.
    - The archived pre-run `gamelib.log` and the scrollback capture file both exist at recorded paths.
  </acceptance_criteria>
  <done>Every precondition is measured and recorded; the run may proceed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive steps 1 through 8 in order, plus the optional Humble rider</name>
  <read_first>
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md` § Scored items (all ten, including their FAIL conditions)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-11-SUMMARY.md` (what the hardening changed, so a step-5 rejection can be read as validation working rather than the port broken)
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Present the how-to-verify steps below to the operator and BLOCK. Drive nothing yourself and infer nothing. Record the operator's report verbatim into task 3's slots without widening it — a "seemed to work" is not a PASS. If the operator stops partway, record the reached step and mark every later item NOT ATTEMPTED rather than leaving its slot empty.
  </action>
  <what-built>
    24 IPC channels ported onto the Tauri sidecar across plans 34.6-05 through 34.6-10; the
    SteamGridDB API-key keyring hardening (plans 34.6-01/02); and the `T-34.5-C6-49-03` containment
    hardening (plan 34.6-11). Four inherited verification legs re-homed from the parked Phase 34.7
    have NEVER been exercised live under Tauri: Epic login, `egsSync`, legendary save sync, and
    Epic logout's `clearEpicCookies`.
  </what-built>
  <how-to-verify>
    Follow `34.6-LIVE-GATE.md`'s scored items 1 through 8 IN ORDER, then the optional rider. Do not
    reorder: step 1 establishes the session steps 5/6/7 need, and step 8 destroys it.

    1. **Epic embedded login.** Use the EMBEDDED/primary tile. SIDLogin does NOT satisfy this
       (D-15). Rider: watch for a `keyring_get` timeout and MEASURE elapsed seconds; inferred time
       is not a measurement, and a non-firing rider is not a discharge.
    2. **EOS overlay round-trip:** install -> enable -> disable -> remove. Expect the confirmation
       dialog at **remove**, not enable (A-02). "No dialog at enable" is the CORRECT outcome.
       Report immediately if dismissing the dialog crashes the app — that is the 34.5 D-15 hazard.
       Then run precondition 6's restore commands.
    3. **SteamGridDB with a REAL key:** setApiKey -> hasApiKey -> searchGame -> getGrids ->
       getHeroes, with artwork RENDERING in the picker. Then verify the key is in the Keychain slot
       `steamgrid-api-key` and ABSENT from `config.json`.
    4. **Winetricks.** Expect the channels to RESOLVE WITH REAL DATA. An empty list is an
       acceptable RESULT, not a decline (A-01). Do NOT wait for a decline message — winetricks has
       genuine macOS support and none will appear. Confirm the D-11 observable line
       `[GAMELIB_SIDECAR_SEND_HANDLER] winetricksInstall` appears after driving the install action.
    5. **The 8 late-discovered.** Score `frontendReady` from its observable line
       (`[GAMELIB_SIDECAR_SEND_HANDLER] frontendReady`), not from a UI symptom — it is send-kind
       and fails silently. Drive the other seven. Use the small fixture for
       moveInstall/importGame; resume/move over EXISTING content.
    6. **`egsSync`** through the UI.
    7. **Legendary save sync round-trip.** Save backed up first. Mutate -> sync up -> alter or
       delete locally -> sync down -> assert the mutation CAME BACK by CONTENT. Exit code 0 is not
       proof.
    8. **Epic logout, LAST.** Log out, then INDEPENDENTLY re-read the jar and assert the planted
       cookie survives BY NAME. Record `before total` and `matched`. Rider: if a generic RPC
       timeout co-occurs, capture the FULL shell scrollback and correlate `id=` values.
    9. **Optional Humble rider.** Only if you also open a Humble login. If you do not, that is
       fine — it will be recorded NOT DISCHARGED.

    Notes: the Tauri DevTools console cannot be pasted into. Read sidecar output from
    `~/Library/Logs/GameLib/gamelib.log`, not the `tauri:dev` terminal.

    Report, per step: PASS / FAIL / NOT ATTEMPTED, plus the evidence the contract asks for. Do not
    smooth over a FAIL — a FAIL here is more valuable than a soft pass, and this project has a
    recorded case of a UAT run manufacturing evidence that disqualified its own fixture.
  </how-to-verify>
  <acceptance_criteria>
    - Each of steps 1-8 has a reported outcome with the evidence its contract item names.
    - Step 1's outcome states which login tile was used.
    - Step 2's outcome states at which step the dialog appeared.
    - Step 3's outcome includes the Keychain check AND the `config.json` negative check.
    - Step 4's outcome reports the resolved data (possibly an empty list) and the observable line, and does NOT report a missing decline as a failure.
    - Step 5's outcome names all 8 channels individually with an outcome each.
    - Step 7's outcome describes the mutated content that came back, by content.
    - Step 8's outcome states the planted cookie's name and whether it survived, plus `before total` and `matched`.
    - The Humble rider is reported as performed-or-not.
  </acceptance_criteria>
  <resume-signal>Report each step's outcome and evidence, or describe where the run stopped.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fill the result slots, adjudicate the verdict, and write the closing statement</name>
  <read_first>
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md` (all ten items and their FAIL conditions)
    - `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-VERIFICATION.md` (the closing-statement and amend-in-place discipline across nine attempts)
    - `.planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md`, `.../2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md`, `.../2026-08-23-humble-user-info-404-two-candidates-undiscriminated.md` (the three riders' own stated discharge conditions — read them, do not act on them here; plan 34.6-14 owns their dispositions)
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Fill every remaining `**Result:**` slot from the operator's report — the observed outcome and its
evidence, verbatim where the contract asks for verbatim. Never write a result the operator did not
report, and never widen a reported outcome (a "seemed to work" is NOT a PASS).

Adjudicate against each item's own stated FAIL condition, not against a general impression. Set
`verdict:` to `PASS n/n` only when every scored item passed; otherwise `FAIL n/m` with the failing
item numbers named in the frontmatter. Set `status:` and `completed:` accordingly.

Write a "## Closing statement" section that:
  - States what this run PROVED and, separately, what it did NOT — in particular, any item scored
    NOT ATTEMPTED, which is not a pass.
  - Records the three folded-todo riders' observations: whether `keyring_get` fired and its
    MEASURED elapsed time; whether a generic RPC timeout co-occurred with a cookie operation and
    what the scrollback correlation showed; and whether a Humble login was performed. State plainly
    for each whether it discharges the todo or leaves it open. A non-firing rider is NOT a
    discharge.
  - Records any NEW observation the run produced that no contract item anticipated, in
    `new_observations:`. A code-read prediction has outlived its own fix by three days in this
    project; an unanticipated live observation is the most valuable output a gate produces.

**If the verdict is FAIL:** do not amend the phase's other artefacts and do not proceed to plans
34.6-13/14. Report the failing items to the orchestrator as a BLOCKING finding for a gap cycle
inside this phase. If a rerun is later authored, AMEND THIS DOCUMENT IN PLACE with a marked
SUPERSEDES section — never regenerate it, which would destroy hand-corrections layered onto prior
amendments.
  </action>
  <verify>
    <automated>F=$(ls .planning/phases/34.6-*/34.6-LIVE-GATE.md); grep -c '^\*\*Result:\*\* *$' "$F"; grep -n '^verdict:' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\*\*Result:\*\* *$' <file>` returns 0 — no empty result slot remains.
    - `verdict:` is `PASS n/n` or `FAIL n/m`, never `null`, and `status:` is no longer `pending`.
    - The closing statement names every NOT ATTEMPTED item explicitly, if any.
    - The three riders each have an explicit discharge-or-open statement, and the `keyring_get` one reports a MEASURED elapsed time or states it did not fire.
    - `new_observations:` is present in the frontmatter, listing any unanticipated finding (an empty list is acceptable only if the closing statement says so explicitly).
    - No result slot contains language softer than the operator's own report.
  </acceptance_criteria>
  <done>The gate has one verdict, every slot is filled from observation, and the riders are honestly adjudicated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator credentials → the run | a real Epic password, a real SteamGridDB API key, real cookies |
| captured logs/scrollback → a public fork's committed history | anything pasted into the gate document is published |
| the running build → the recorded verdict | a stale binary would attribute a pass to the wrong commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-08 | Information Disclosure | pasting raw log or scrollback containing a credential into the gate document | mitigate | Redact cookie VALUES (record names and domains only), never paste a `Running command:` line containing `--secret`, a PKCE verifier or a private-branch password (the `F-34.5-G6-22` class, ledger row `U-34.5-33`), and never record the SteamGridDB key itself. |
| T-34.6-10 | Information Disclosure | step 3 writing the operator's real API key to `config.json` | mitigate | Step 3's own FAIL condition asserts the negative directly against `config.json`, and precondition 3 proves the keyring arm was taken. If it FAILS, the operator must rotate the key. |
| T-34.6-09 | Repudiation | a result slot softened or back-filled to produce a pass | mitigate | Slots were authored empty before any work (plan 34.6-03); task 3 forbids widening the operator's report; `verdict` is adjudicated against each item's own FAIL condition. |
| T-34.6-35 | Repudiation | scoring a pass against a stale binary | mitigate | Precondition 1 records a commit SHA and a `pgrep -fl gamelib-shell` check for THIS run; `pnpm tauri:dev` exiting 0 without replacing a running instance is the known trap. |
| T-34.6-21 | Denial of Service | the `remove()` dialog crashing the app under the sidecar (34.5 D-15) | mitigate (observed) | Step 2 exercises it deliberately and instructs an immediate report on crash. This is the first time the hazard is observed rather than reasoned about. |
| T-34.6-36 | Tampering | step 7's upload half destroying the operator's save | mitigate | Precondition 7 requires a run-scoped backup of the save directory BEFORE step 7. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed. |
</threat_model>

<verification>
- All ten result slots filled; `verdict` set; `status` no longer `pending`.
- Preconditions 1-9 all carry measured values.
- No source file is modified by this plan.
- On FAIL: the orchestrator is told, plans 34.6-13/14 do not run, and a gap cycle opens inside this phase.
</verification>

<success_criteria>
- The gate ran in the contract's fixed order: step 0 (already done), login first, logout last.
- Every scored item has an outcome adjudicated against its own FAIL condition.
- The three folded-todo riders are each explicitly discharged or explicitly left open.
- The document carries one verdict and no partial pass.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-12-SUMMARY.md` when done.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
</output>
