---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 13
type: execute
wave: 9
depends_on: ["34.6-12"]
files_modified:
  - .planning/IPC-PORT-INVENTORY.md
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PRELOAD-SURFACE-REDERIVE.md
autonomous: false
requirements: [REQ-34.6-10, REQ-34.6-14]
must_haves:
  truths:
    - "D-03: `preload-surface-gate.py` is RE-RUN at phase close, after this phase's ports landed, and its findings are absorbed up to ~10 channels"
    - "D-03: beyond ~10 findings this becomes a BLOCKING HALT and a new phase is inserted before Phase 35 — it is not silently absorbed"
    - "D-01: `IPC-PORT-INVENTORY.md` reconciles to 24 channels ported by this phase, arithmetically checkable"
    - "A-04: the stale `GlobalState.tsx:1586` citation is corrected to `:1609` in the same pass, not left standing beside new rows"
  artifacts:
    - path: ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PRELOAD-SURFACE-REDERIVE.md"
      provides: "the D-03 re-run's raw output, its finding count, and the absorb-or-halt decision"
    - path: ".planning/IPC-PORT-INVENTORY.md"
      provides: "updated totals, the 24 ported, and the corrected frontendReady citation"
      contains: "1609"
  key_links:
    - from: ".planning/phases/34.5-.../preload-surface-gate.py"
      to: ".planning/IPC-PORT-INVENTORY.md bucket lines"
      via: "the gate cross-checks the preload-derived surface against the inventory's own buckets"
      pattern: "preload-surface-gate"
---

<objective>
Run D-03's close-out re-derive and reconcile `IPC-PORT-INVENTORY.md`.

D-03 is a task, not a footnote. "The IPC re-plumb is COMPLETE" — Phase 35's own gate — can only be
asserted by a re-derive that runs AFTER this phase's ports land. The inventory has been
non-exhaustive TWICE already: `getInstallInfo` (plan 34.5-43), then 11 more (plan 34.5-49). Eight
phases of source changes have landed since that 2026-08-12 audit.

The ceiling is binding: absorb up to ~10 newly-found channels; **beyond ~10 this is a BLOCKING HALT
and a new phase is inserted before Phase 35**, in the shape 34.5's gap cycle 6 used. ~10 is roughly
one wave at this project's measured 3-4-channels-per-plan rate. The rejected alternatives were: no
ceiling (unbounded), and record-and-hand-to-Phase-35 (handing the riskiest phase an unbounded list).

Purpose: make the completeness claim derivable rather than transcribed.
Output: a re-derive record, an absorb-or-halt decision, and a reconciled inventory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-10-SUMMARY.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-12-SUMMARY.md

<interfaces>
The script (`.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py`):
  python3 preload-surface-gate.py --self-test   # prove the assertions reject bad input — run FIRST
  python3 preload-surface-gate.py               # check the live documents/source tree

It is standalone (it does not import `ported-channels-gate.py`) and re-derives the real preload
surface from `src/preload/` source at run time via a comment-stripping, multi-line-aware regex over
`makeHandlerInvoker|makeListenerCaller|frontendListenerSlot`, then cross-checks the union against
`IPC-PORT-INVENTORY.md`'s bucket lines.

Its own docstring states the interpretation rule: on failure, ADD the missing channel as a new
bucket line (creating a new "late-discovered" section if none fits) — **never** delete or weaken
the assertion to make the gate pass.

`IPC-PORT-INVENTORY.md` § "Totals" currently reads: Unique channels 225, Ported to sidecar 28,
Unported 183 — figures that predate this phase and several prior slices' own updates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the gate's self-test, then the live run, and record both verbatim</name>
  <read_first>
    - `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py` (its docstring, its `--self-test` mode and its interpretation rule)
    - `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PRELOAD-SURFACE-AUDIT.md` (plan 34.5-49's audit — the method this re-run repeats and the provenance of the 8 late-discovered)
    - `.planning/IPC-PORT-INVENTORY.md` § "Preload-surface coverage" (the standing invariant this re-derive checks)
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PRELOAD-SURFACE-REDERIVE.md</files>
  <action>
Run `python3 <path>/preload-surface-gate.py --self-test` FIRST. The self-test is what proves the
gate's assertions actually reject bad input; a live run of an unproven gate is a green that measures
nothing, and this project has recorded gates that were non-vacuous, RED-proven and still measuring
the wrong property. If the self-test fails, STOP and report — the gate itself is broken and its
live result is uninterpretable.

Then run the live check. Capture BOTH outputs verbatim into a new file,
`.planning/phases/34.6-.../34.6-PRELOAD-SURFACE-REDERIVE.md`, with:
  - the exact commands and the commit SHA they ran against;
  - the self-test output;
  - the live output;
  - a COUNT of channels found in preload source but absent from every `IPC-PORT-INVENTORY.md`
    bucket line, listed by name.

Do not interpret yet and do not edit the inventory yet — task 2 makes the absorb-or-halt decision
against the recorded count. Recording the raw output before deciding is what makes the decision
auditable.

Caution while running: this project has recorded a NUL byte in a planning document making `grep`
treat it as binary and silently report nothing. If the gate's output looks impossibly clean, check
`file` on the inventory before trusting it.
  </action>
  <verify>
    <automated>python3 .planning/phases/34.5-*/preload-surface-gate.py --self-test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The self-test runs and its verdict (pass or fail) is recorded verbatim in the new file.
    - The live run's output is recorded verbatim, with the commit SHA it ran against.
    - The count of unbucketed channels is stated as an integer and each is listed by name.
    - `file .planning/IPC-PORT-INVENTORY.md` reports text, not binary (the NUL-byte trap).
    - No inventory edit has been made yet.
  </acceptance_criteria>
  <done>The re-derive's raw result exists, self-test first, before any interpretation.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: The D-03 absorb-or-halt decision</name>
  <read_first>
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PRELOAD-SURFACE-REDERIVE.md` (task 1's recorded count and names)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md` D-03 (the ~10 ceiling and its rationale)
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PRELOAD-SURFACE-REDERIVE.md</files>
  <action>
Present the measured count and the full list of unbucketed channel names from task 1, then present the three options below and BLOCK on the operator's selection. Do not choose on the operator's behalf: D-03's ~10 ceiling is a scope decision with a ROADMAP consequence. Record the selection and its reasoning into `34.6-PRELOAD-SURFACE-REDERIVE.md` before continuing, and if `halt` is selected, stop this plan and report to the orchestrator.
  </action>
  <decision>
    How to dispose of the channels the D-03 re-derive found unbucketed.
  </decision>
  <context>
    D-03 sets a ceiling of roughly 10 channels — about one wave at this project's measured
    3-4-channels-per-plan rate. Below it, this phase absorbs them. Above it, the finding becomes a
    BLOCKING HALT and a new phase is inserted before Phase 35, in the shape 34.5's gap cycle 6 used.
    Phase 35's own roadmap entry requires the IPC re-plumb to be COMPLETE before cutover, so an
    unbucketed channel is not a documentation nit — it is a channel that ships broken.

    Report the measured count and the channel names from task 1 before presenting the options.
  </context>
  <options>
    <option id="zero-findings">
      <name>Zero unbucketed channels — nothing to decide</name>
      <pros>The completeness claim is now derived, not transcribed. Proceed to task 3.</pros>
      <cons>Confirm the gate was non-vacuous: the self-test passed, and the live run examined a non-empty channel set. A gate that found nothing because it looked at nothing is the failure mode to rule out here.</cons>
    </option>
    <option id="absorb">
      <name>Absorb (count is at or under ~10)</name>
      <pros>Keeps Phase 35's gate meaningful and does not forward an obligation. Matches D-03's stated intent.</pros>
      <cons>Adds unplanned ports to this phase after its live gate already ran — the newly-absorbed channels would be unit-proven only, with no live observation. That limitation must be recorded, not glossed.</cons>
    </option>
    <option id="halt">
      <name>Blocking halt and insert a new phase before Phase 35 (count exceeds ~10)</name>
      <pros>D-03's own stated response. Bounds this phase and gives the new work its own discuss/plan/gate cycle.</pros>
      <cons>Delays Phase 35. Requires a ROADMAP insertion and a new phase directory.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The measured count and the full list of channel names are presented before the options.
    - Whichever option is chosen, the reasoning is recorded in `34.6-PRELOAD-SURFACE-REDERIVE.md` alongside the count.
    - If `absorb` is chosen, the record states explicitly that the absorbed channels are unit-proven only and were NOT covered by this phase's live gate.
    - If `halt` is chosen, no further task in this plan runs; report to the orchestrator for a ROADMAP insertion.
    - Under no option is the gate's assertion deleted or weakened to make it pass — that is the script's own stated prohibition.
  </acceptance_criteria>
  <resume-signal>Select: zero-findings, absorb, or halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Reconcile `IPC-PORT-INVENTORY.md` to 24 ported and fix the A-04 stale citation in the same pass</name>
  <read_first>
    - `.planning/IPC-PORT-INVENTORY.md` in full (326 lines — § "Totals", § "Already ported", § "Phase 34.6 — Slice 9", § "Late-discovered — owner Phase 34.6", § "Preload-surface coverage")
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-10-SUMMARY.md` (the 24-channel census and its diff against the inventory's own bucket lines)
    - `src/frontend/state/GlobalState.tsx` line 1609 (confirm `window.api.frontendReady()` by direct read before writing the corrected citation — a code-read prediction has outlived its own fix by three days in this project)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PRELOAD-SURFACE-REDERIVE.md` (task 2's decision)
  </read_first>
  <files>.planning/IPC-PORT-INVENTORY.md</files>
  <action>
Update `.planning/IPC-PORT-INVENTORY.md`:

  - **§ "Totals".** Recompute `Ported to sidecar` and `Unported` to include this phase's 24, plus
    any channels absorbed by task 2's decision. State the arithmetic explicitly in the update note
    (previous ported + 24 + absorbed = new ported), in the style the existing "Unique channels
    raised N -> M" notes use, so a later reader can check it rather than trust it.
  - **§ "Phase 34.6 — Slice 9" and § "Late-discovered — owner Phase 34.6".** Mark both sections'
    channels as PORTED, naming the phase and the plan that ported each cluster. Keep the two
    sections separate — merging them would silently inflate the "(16 channels)" heading's
    historical count, which is exactly why the late-discovered eight got their own bucket.
  - **A-04 — the stale citation.** The "Late-discovered" section cites `GlobalState.tsx:1586` for
    `frontendReady`'s caller. The measured truth is `:1609`. FIX IT IN PLACE in this same pass
    rather than appending new rows around it, and re-verify the line number by direct read first.
    Also correct the same bullet's claim that `frontendReady` "will hit `UNPORTED_CHANNEL_MARKER`
    under Tauri today": it is send-kind (`src/preload/api/misc.ts:93`, `makeListenerCaller`), so it
    failed SILENTLY — the marker was true only inside the transport and never surfaced. That made
    it WORSE than the inventory implied.
  - **§ "Preload-surface coverage".** Record that D-03's re-derive ran at this phase's close, with
    the date, the commit SHA and the finding count, so the invariant's last verification is
    datable. A blocker or invariant record that rots silently has bitten this project three times.

Do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` — the orchestrator owns those, and a
concurrent session is active in this repo.
  </action>
  <verify>
    <automated>grep -c '1609' .planning/IPC-PORT-INVENTORY.md; grep -c '1586' .planning/IPC-PORT-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GlobalState.tsx:1586' .planning/IPC-PORT-INVENTORY.md` returns 0 and `grep -c 'GlobalState.tsx:1609' ...` returns at least 1.
    - The `frontendReady` bullet no longer claims it surfaces `UNPORTED_CHANNEL_MARKER`; it states it fails silently and names `makeListenerCaller`.
    - § "Totals" states the new arithmetic explicitly and the numbers add up when checked by hand.
    - Both Phase-34.6 bucket sections remain SEPARATE, each marked ported with its plan number.
    - § "Preload-surface coverage" records the re-derive date, commit SHA and finding count.
    - `git diff -- .planning/STATE.md .planning/ROADMAP.md` is empty.
    - `file .planning/IPC-PORT-INVENTORY.md` still reports text, not binary.
  </acceptance_criteria>
  <done>The inventory is arithmetically checkable, the stale citation is gone, and the invariant's last verification is datable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/preload/` source → the derived channel surface | the gate re-derives rather than trusting a transcribed list, because the transcription has been wrong twice |
| the inventory → Phase 35's completeness gate | Phase 35 must not run while any channel is unported; the inventory is what quantifies "any" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-37 | Repudiation | asserting "the re-plumb is COMPLETE" from a transcribed list rather than a derivation | mitigate | D-03's re-run derives the surface from source at run time; task 1 runs `--self-test` FIRST so a vacuous green is ruled out before the live result is read. |
| T-34.6-38 | Tampering | weakening or deleting the gate's assertion to make it pass | mitigate | The script's own docstring prohibits it; task 2's acceptance criteria restate the prohibition under every option. |
| T-34.6-39 | Repudiation | absorbing more than ~10 findings silently, handing Phase 35 an unbounded list | mitigate | Task 2 is a BLOCKING decision checkpoint with the count presented before the options, and `halt` stops the plan. |
| T-34.6-40 | Repudiation | a stale citation propagating because new rows were appended around it | mitigate | A-04 requires the fix IN PLACE in the same pass, with the line re-verified by direct read first. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; `preload-surface-gate.py` is an existing standalone script run with the system `python3`. |
</threat_model>

<verification>
- `python3 .planning/phases/34.5-*/preload-surface-gate.py --self-test` — passes.
- `python3 .planning/phases/34.5-*/preload-surface-gate.py` — its result is recorded verbatim, whatever it is.
- `grep -c 'GlobalState.tsx:1586' .planning/IPC-PORT-INVENTORY.md` — 0.
- `git diff -- .planning/STATE.md .planning/ROADMAP.md` — empty.
- No source file is modified by this plan unless task 2 chose `absorb`, in which case the absorbed ports are their own commit.
</verification>

<success_criteria>
- D-03's re-derive ran at close, self-test first, with its raw output recorded.
- The absorb-or-halt decision was made against a measured count, by the operator, and recorded.
- The inventory reconciles to 24 ported by this phase, arithmetically checkable.
- A-04's stale citation is corrected in place and its accompanying false claim with it.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-13-SUMMARY.md` when done.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
</output>
