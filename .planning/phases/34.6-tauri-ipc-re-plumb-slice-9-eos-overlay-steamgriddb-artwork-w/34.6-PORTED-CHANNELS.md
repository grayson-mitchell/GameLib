# Phase 34.6 — Declared Ported-Channel List

This document follows `34.5-PORTED-CHANNELS.md`'s structure section for section: a channel is
declared here only against a MEASURED proof level, never a planning-time intent. "Ported" means
registered — it is not, by itself, a claim that the channel works. Where this phase's live gate
(`34.6-LIVE-GATE.md`) actually drove a channel, its recorded RESULT is cited by item number and
carried here verbatim in effect, not softened. Where the gate did not drive a channel, the proof
level says so plainly (`unit` only) rather than implying a live observation that never happened.

**Provenance.** `34.6-LIVE-GATE.md` ran as plan `34.6-12` (run 1, 2026-08-24), was amended in place
by plan `34.6-15` (six SUPERSEDES sections, 2026-08-25) which corrected six contract defects found
in the gate's own text without converting any FAIL to a PASS, and was finally adjudicated by plan
`34.6-17` (2026-08-25), which recorded the closed, final scored tally this document cites
throughout. `IPC-PORT-INVENTORY.md` was reconciled by plan `34.6-13` (D-03 close-out re-derive,
0 unbucketed channels) the same day. This document is written after all four of those plans closed,
so every citation below is against the FINAL adjudicated record, not an intermediate one.

## Live-gate narrative, including the failures, in full

The blocking live gate scored **9 items** (0 through 8) and closed **FAIL 7/9**: 7 PASS, 2 FAIL, 0
NOT ATTEMPTED. The two failing items are **Step 4** (`winetricksInstall`) and **Step 8** (Epic
logout). Per D-12 (one document, one verdict, no partial-pass mode), this FAIL is not rounded up,
not smoothed over, and not re-scoped away — the operator was offered three options for Step 8 ((a)
fix the defect, (b) a seventh SUPERSEDES scoping the item out of the verdict, (c) close the phase
FAIL with a named, owned failure) and **chose (c)**. This document never contradicts that verdict.

**Final scored tally** (`34.6-LIVE-GATE.md`, FINAL ADJUDICATION section, plan 34.6-17):

| Item | Subject | Outcome | Basis |
|---|---|---|---|
| 0 | `DECLARED_UNAVAILABLE_MARKER` pre-port capture | PASS | run 1 (plan 34.6-04); irreversible, cannot be recaptured |
| 1 | Epic embedded login | PASS | run 1 |
| 2 | EOS overlay round-trip | PASS (macOS-scoped) | rescored by SUPERSEDES amendment #1, from run 1's own evidence — no re-drive |
| 3 | SteamGridDB, real key | PASS | run 1, on the property; FAIL clause corrected by amendment #2 |
| 4 | Winetricks | **FAIL** | run 1, CARRIED — not re-driven; cause localised and PARKED |
| 5 | 8 late-discovered channels | PASS | 6 PASS + 1 discharged via Step 7; `runWineCommandForGame` removed from the denominator by amendment #3 as UNREACHABLE-BY-CONSTRUCTION |
| 6 | `egsSync` | PASS | run 1, on the property; second PASS clause corrected by amendment #4 |
| 7 | legendary save sync | PASS | run 1; round-trip proved BY CONTENT, never by exit code |
| 8 | Epic logout | **FAIL** | run 1, CARRIED — deliberately out of the gap cycle's scope; operator chose (c) |

**Both failures, stated in full, exactly as `34.6-LIVE-GATE.md`'s own "What the two failures mean"
section states them — this document does not compress or soften either:**

- **Step 4 — `winetricksInstall` — NOT an IPC defect.** The gap cycle proved this phase's port of the
  channel is CORRECT: driving the same Install action by KEYBOARD sends the frame and runs
  winetricks end to end. On the MOUSE path no frame is ever sent, because React unmounts the
  suggestion rows between `mousedown` and `mouseup` — a renderer defect that pre-dates this phase.
  It is PARKED, with the break point measured exactly, in
  `.planning/todos/pending/2026-08-24-winetricksinstall-send-channel-is-a-live-silent-no-op.md`.
  **This phase does not own it**, but the channel's own send-kind transport (D-11's own subject) is
  what caught it — no automated check in this repo could have, because a send-kind channel that
  never fires produces zero rejection evidence to assert against.
- **Step 8 — Epic logout — a shell/wry-layer defect on a channel this phase never ported.**
  `clearEpicCookies` reports clearing 8 cookies and clears zero of 7 in-scope cookies. Owned by
  `.planning/todos/pending/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md`.
  Caught only because D-13's bar is the survival of a planted/named cookie re-read independently —
  the app's own count said 8, and a count-only cross-check (the ledger's inherited bar) would have
  PASSED a logout that cleared nothing.

**These are not the same kind of thing, and this document does not conflate them.** Step 4 is a
failure inside this phase's own deliverable (the port works; the button above it does not). Step 8
is a genuine gate failure on a pre-existing, unported surface. Neither is a regression this phase's
IPC re-plumbing introduced. What the gate proves about this phase's own 24 ported channels is that
every one of them behaves correctly everywhere it could be reached at all.

**Six contract defects were found in the gate's own text** (Steps 2, 3, 5's `runWineCommandForGame`,
6, and preconditions 8 and 9) and were each resolved by an in-place SUPERSEDES amendment (plan
34.6-15) under D-12 — none converted a FAIL into a PASS; several converted a wrongly-written PASS or
FAIL condition into one that actually measures the property it claims to. See `34.6-LIVE-GATE.md`'s
own "AMENDMENTS" section for each amendment's before/after text and proof.

## The 24 channels

| Channel | Kind | Registration module | Proof level | Riders |
|---|---|---|---|---|
| `installEosOverlay` | sidecar invoke | `eosOverlayFlowRegistration.ts` | unit + LIVE (item 2) — PASS (macOS-scoped) | Completed a genuine sidecar round-trip with zero `UNPORTED_CHANNEL_MARKER`. Item 2's original PASS condition assumed all four EOS operations were UI-reachable on any platform; SUPERSEDES amendment #1 (34.6-15) scoped it to the two macOS-reachable operations (install, remove) after run 1 already proved both worked. |
| `removeEosOverlay` | sidecar invoke | `eosOverlayFlowRegistration.ts` | unit + LIVE (item 2) — PASS (macOS-scoped) | Completed a genuine sidecar round-trip. The `remove()` confirmation dialog appeared exactly as A-02 predicted (unconditional call, `eos_overlay.ts:161-162`) — first live confirmation of that half of A-02 under the sidecar. Dismissal did not crash the app (34.5 D-15 hazard, observed live, closed). |
| `enableEosOverlay` | sidecar invoke | `eosOverlayFlowRegistration.ts` | ported with a declared caveat: UNREACHABLE-ON-PLATFORM (macOS) | NOT ATTEMPTED at item 2 — no reachable UI surface on macOS at all. `AdvancedSettings/index.tsx:401` gates the global toggle `isWindows`-only; `GameSubMenu/index.tsx:461` gates the per-game menu item `isLinux && runner === 'legendary'`-only. Neither covers macOS. Pre-existing platform/product fact, not introduced by this phase, and not a porting defect — the channel itself never returned `UNPORTED_CHANNEL_MARKER` when its sibling `installEosOverlay`/`removeEosOverlay` were driven on the same registration path. A-02's enable-side half ("no dialog fires when already installed") also remains UNTESTABLE on this platform and is not recorded as confirmed. |
| `disableEosOverlay` | sidecar invoke | `eosOverlayFlowRegistration.ts` | ported with a declared caveat: UNREACHABLE-ON-PLATFORM (macOS) | Same platform-gating fact and same NOT ATTEMPTED disposition as `enableEosOverlay`, identical root cause (FINDING 1, `34.6-LIVE-GATE.md` Step 2). |
| `getEosOverlayStatus` | sidecar invoke | `eosOverlayFlowRegistration.ts` | unit | Not individually isolated as fired during item 2's round trip in the gate's own recorded evidence — the round trip's per-channel citations name only `installEosOverlay`/`removeEosOverlay` by result. Recorded as `unit` only rather than inferring a LIVE citation this document cannot point to a specific measured line for. |
| `getLatestEosOverlayVersion` | sidecar invoke | `eosOverlayFlowRegistration.ts` | unit | Same disposition as `getEosOverlayStatus` — no isolated live citation in the gate's recorded evidence. |
| `isEosOverlayEnabled` | sidecar invoke | `eosOverlayFlowRegistration.ts` | unit | Same disposition as `getEosOverlayStatus` — no isolated live citation in the gate's recorded evidence. |
| `updateEosOverlayInfo` | sidecar invoke | `eosOverlayFlowRegistration.ts` | unit | Same disposition as `getEosOverlayStatus` — no isolated live citation in the gate's recorded evidence. |
| `steamgriddb.setApiKey` | sidecar invoke | `enrichmentFlowRegistration.ts` | unit + LIVE (item 3) — PASS | A real key was entered and verified present in the Keychain slot, never in `config.json` (value confirmed empty, `isEmptyString: true`, length 0; `grep -c steamGridDbApiKey` matches the setting NAME only, corrected by SUPERSEDES amendment #2). A-03 divergence recorded: routes exclusively through `getSteamGridDbSecretStore()`, deliberately not byte-equivalent with the Electron original. |
| `steamgriddb.hasApiKey` | sidecar invoke | `enrichmentFlowRegistration.ts` | unit + LIVE (item 3) — PASS | Resolved true through the keyring-backed store (artwork rendered, which only happens if this resolved true). A-03 divergence applies, as above. |
| `steamgriddb.searchGame` | sidecar invoke | `enrichmentFlowRegistration.ts` | unit + LIVE (item 3) — PASS | Full chain driven with real network calls for two titles never previously looked up in this installation, so the render check could not pass on cached artwork alone. A-03 divergence applies, as above. |
| `steamgriddb.getGrids` | sidecar invoke | `enrichmentFlowRegistration.ts` | unit + LIVE (item 3) — PASS | Artwork rendered in `SteamGridDBPicker`. A-03 divergence applies, as above. |
| `steamgriddb.getHeroes` | sidecar invoke | `enrichmentFlowRegistration.ts` | unit + LIVE (item 3) — PASS | Artwork rendered in `SteamGridDBPicker`. A-03 divergence applies, as above. |
| `winetricksAvailable` | sidecar invoke | `wineToolsFlowRegistration.ts` | ported with a declared caveat: exercised within item 4, whose OWN recorded outcome is FAIL | Resolved with real data, zero `UNPORTED_CHANNEL_MARKER`. This document does not cite it as "LIVE (item 4) — PASS" because item 4's own scored outcome is FAIL as a whole (its PASS condition is conjunctive across all three winetricks channels) — citing a PASS against a FAIL item would violate this document's own citation discipline. The FAIL is attributable solely to `winetricksInstall`, below, not to this channel. |
| `winetricksInstalled` | sidecar invoke | `wineToolsFlowRegistration.ts` | ported with a declared caveat: exercised within item 4, whose OWN recorded outcome is FAIL | Resolved with real data, zero `UNPORTED_CHANNEL_MARKER`. Same citation discipline as `winetricksAvailable`, immediately above. |
| `winetricksInstall` | sidecar **send** | `wineToolsFlowRegistration.ts` | ported with a declared caveat: item 4 — **FAIL**, live silent no-op | The D-11 observable (`[GAMELIB_SIDECAR_SEND_HANDLER] winetricksInstall`) never fired after a real Install click on a real result row. Registered, present in the running build, not stale, not undeclared, not filtered Rust-side, not the `declined` guard, not the wrong button — the entire exclusion list is measured in `34.6-LIVE-GATE.md` Step 4, not merely asserted. Cause LOCALISED (React unmounts the suggestion rows between `mousedown`/`mouseup` on the mouse path only; the identical action by keyboard sends the frame and runs winetricks) but NOT fixed — PARKED in `.planning/todos/pending/2026-08-24-winetricksinstall-send-channel-is-a-live-silent-no-op.md`. **NOT an IPC/port defect** — a renderer defect pre-dating this phase, caught only because D-11's observable exists for exactly this failure shape (a send-kind channel fails silently by construction; every registration/census/sweep gate in this repo passes around it). |
| `frontendReady` | sidecar **send** | `appShellFlowRegistration.ts` | unit + LIVE (item 5) — PASS | D-11 observable fired: `[GAMELIB_SIDECAR_SEND_HANDLER] frontendReady` at app start. This is the phase's OTHER send-kind channel, sharing the entire transport path with the broken `winetricksInstall` — its success is what localises item 4's defect to that one channel rather than to the send transport as a whole. Two deliberate exclusions from the curated port of `main.ts:560-601` (plan 34.6-05), both RED-proven via mock-not-called assertions: (1) `handleProtocol(...)` — already handled by `bootstrap.ts`'s `deliverStartupProtocolUrl`/`registerProtocolUrlHandler`; (2) the 5s `initQueue(true)` boot-time auto-resume — deferred to Phase 35 per Phase 33 D-04. |
| `getAchievements` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit + LIVE (item 5) — PASS | Positive write observable, twice: `store_cache/gog_achievements.json` gained real per-appId entries after opening two GOG titles, timed to the same second as a GOG token refresh. Empty result lists (`total_count=0`) are a genuine data-dependent RESULT (per A-01's principle, extended) because the account has never played these titles via Galaxy — not a decline. Implemented by exactly one store manager (`gog/games.ts:141`); driving it on a non-GOG title would have been a vacuous pass. |
| `getDefaultSavePath` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit + LIVE (item 5, discharged via item 7) — PASS on the channel, product defect found behind it | Invoked twice, reached the legendary store manager, spawned a real subprocess — the channel itself works. Returned `''` because of a defect BEHIND the channel: legendary correctly persists `save_path` to `installed.json`, but GameLib's readback (`getGameInfo(..., true)`) reads a stale in-memory `Map` that nothing repopulates after the subprocess writes the file — `main.ts`'s `installed.json` file-watcher (which would trigger the repopulate) is not in the sidecar's import graph and has fired 0 times under Tauri. This is a real, unfixed gap (recorded in the corrections section below), not a porting defect in this channel. |
| `getGogDiscounts` | sidecar invoke | `enrichmentFlowRegistration.ts` | unit + LIVE (item 5) — PASS | Stores → Deals rendered a populated grid of discounted GOG products; zero `GAMELIB_UNPORTED_CHANNEL`/`GAMELIB_DECLARED_UNAVAILABLE` lines, and the screen's own failure branch routes through `window.api.logError`, so a silent failure was structurally impossible here. |
| `getPlaytimeFromRunner` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit + LIVE (item 5) — PASS | Freshly-dated write matching the live GOG sessions endpoint's `time_sum` exactly, with the correct insert-branch empty-string shape for first/lastPlayed. Exposed in preload as `fetchPlaytimeFromServer`, not under its wire name — grepping the wire name alone would have made this channel look unreachable (the `census-by-wrong-namespace-misses-call-sites` shape this project has hit before). |
| `importGame` | sidecar invoke | `installFlowRegistration.ts` | unit + LIVE (item 5) — PASS on the channel, serious product defect found behind it | Driven as a deliberate GOG-title mismatch; the channel executed end-to-end but nothing compares the imported folder's `goggame-*.info` product id against the selected appName, so a mismatched import reports success while corrupting state on both sides (the wrong game's install record written, the right game's own `versionEtag`/`pinnedVersion` lost). Hardened by plan 34.6-11 (T-34.5-C6-49-03) via `rendererPathGuard.ts`'s `assertContainedPath` for the destination path only, then CORRECTED by gap plan 34.6-18 after `34.6-REVIEW.md` CR-01 / `34.6-VERIFICATION.md` found the containment root rejected the feature's own primary input (an out-of-tree, already-installed game — exactly what `importGame` exists to accept); the live control is now `rendererPathGuard.assertPlausibleAbsolutePath` (shape only: absolute, no `..` segment, no NUL, non-empty), with `defaultInstallPath` containment REMOVED entirely — this remains a SOURCE-LEVEL discharge of `T-34.5-C6-49-03` only, not the live identity-validation gap found here. Proof level for the corrected control is `unit` at time of writing, since the appended live-gate items in `34.6-LIVE-GATE.md`'s gap-cycle-2 section had not yet been driven when this row was written; plan 34.6-21 upgrades the proof level after the drive. `winePrefix`/`wineVersion`/`wineCrossoverBottle` remain NOT contained by 34.6-11, filed separately: `.planning/todos/pending/2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md`. Cross-referenced (not duplicated): committed todo `83890d02a`. |
| `moveInstall` | sidecar invoke | `installFlowRegistration.ts` | unit + LIVE (item 5) — PASS on the channel, product defect found behind it | Full trace proves end-to-end execution (Gog move attempt → backend rsync invocation → real error propagated and surfaced through an in-app dialog). Fails on macOS specifically because `moveOnUnix` probes `which rsync` for existence, not capability, and macOS ships Apple's `openrsync`, which rejects `--no-human-readable`/`--info=name,progress`; a working `mv -f` fallback exists in the same function and is never reached. No data lost. Hardened by plan 34.6-11 (T-34.5-C6-49-03) via `assertContainedPath`, then CORRECTED by gap plan 34.6-18 after `34.6-REVIEW.md` CR-01 / `34.6-VERIFICATION.md` found the containment root rejected the feature's own primary input (a cross-drive/external-disk move — exactly what `moveInstall` exists to perform); the live control is now `rendererPathGuard.assertPlausibleAbsolutePath` (shape only), with `defaultInstallPath` containment REMOVED entirely. Proof level for the corrected control is `unit` at time of writing, since the appended live-gate items in `34.6-LIVE-GATE.md`'s gap-cycle-2 section had not yet been driven when this row was written; plan 34.6-21 upgrades the proof level after the drive. Cross-referenced (not duplicated): committed todo `5448b7854`. |
| `runWineCommandForGame` | sidecar invoke | `wineToolsFlowRegistration.ts` | ported with a declared caveat: UNREACHABLE-BY-CONSTRUCTION | Zero renderer call sites, measured by a full-source census (`grep -rn runWineCommandForGame src/frontend/` returns nothing); the Tools panel's Winecfg/Winetricks/Run-EXE buttons all go through the separate `callTool` channel instead. Item 5's driving method ("the per-game Wine-command action") named an action that does not exist — a contract defect, corrected by SUPERSEDES amendment #3 (34.6-15), which removed this channel from item 5's scored denominator entirely rather than passing it: it is recorded as a coverage FACT (ships exercised by no gate at all), not a pass. Ported byte-equivalently (D-02) and hardened for T-34.5-C6-49-03 by plan 34.6-11 (`assertCommandParts`) despite having no live caller to exercise that hardening. |

## What shipped

**24 of 24 inventoried channels for this phase are ported**, registered across four sidecar
registration modules plus one shared module carried from Phase 34.5:
`eosOverlayFlowRegistration.ts` (8, new module), `enrichmentFlowRegistration.ts` (5 SteamGridDB +
1 late-discovered `getGogDiscounts` = 6, extending the module plan 34.5 created),
`wineToolsFlowRegistration.ts` (3 winetricks + 1 late-discovered `runWineCommandForGame` = 4,
extending the module plan 34.5 created), `installFlowRegistration.ts` (2 late-discovered:
`importGame`, `moveInstall`), `appShellFlowRegistration.ts` (1 late-discovered: `frontendReady`),
`runnerMiscFlowRegistration.ts` (3 late-discovered: `getAchievements`, `getDefaultSavePath`,
`getPlaytimeFromRunner`). 8 + 6 + 4 + 2 + 1 + 3 = **24**, matching `IPC-PORT-INVENTORY.md`'s two
Phase-34.6 bucket sections exactly: "Phase 34.6 — Slice 9" (16: EOS overlay 8 + SteamGridDB 5 +
winetricks 3) plus "Late-discovered — owner Phase 34.6" (8), 16 + 8 = 24.

**Deferred count: confirmed ZERO, not merely asserted.** Plan `34.6-13`'s close-out re-derive
(D-03) measured the full 220-channel preload surface at phase close and found `unlisted=0` —
non-vacuity established first by running the gate's own `--self-test`, which correctly rejected all
six known-bad fixtures before the zero-finding live run was trusted. No channel this phase was
responsible for was left unbucketed, dropped silently, or carried forward undeclared.

**Porting is registration, not proof of behaviour, and this document does not let the 24/24 count
imply otherwise.** The live gate closed FAIL 7/9. Every channel above that carries a LIVE citation
was actually driven; every channel that carries only `unit` was not, and this document says so
plainly rather than implying a live observation that never happened.

## Corrections this declaration must not paper over

**The six PATTERNS deltas** (`34.6-PATTERNS.md`'s own "Deltas Not Flagged in CONTEXT.md/RESEARCH.md"
section, reproduced here because a declaration document is the place a future reader looks, not a
patterns file):

1. `wineToolsFlows.test.ts:133`'s absence assertion would have FAILED the moment winetricks ported
   (it asserted the channels were absent from the registry) — rewritten to a presence assertion
   before the port landed, not after.
2. `enrichmentFlows.test.ts`'s "still unported" exemplar channel needed reselecting three times
   across this phase's plans as real ports landed under it: `checkDiskSpace` → `getLegendaryVersion`
   → `winetricksInstall` → (finally) `authZoom`, a channel dropped permanently and therefore safe to
   use as a permanent exemplar.
3. `runnerSliceRegistration.test.ts`'s module-scope setup originally called only the four Phase-34.5
   registration modules; flipping in SteamGridDB (5) and EOS (8) required ALSO adding new imports
   and calls at module scope for the new registration modules, not merely editing the census arrays
   those calls feed.
4. `electronReachLedger.test.ts` needed one new `ENTRY_POINTS` member (the new EOS module) plus two
   new `BASELINE_ELECTRON_REACHING_MODULES` entries (`eos_overlay.ts`, `steamgrid/utils.ts`) that
   neither CONTEXT.md nor RESEARCH.md predicted.
5. `steamgrid/ipc_handler.ts` carried a second, older inline legacy-plaintext-migration branch,
   distinct from `humbleSecretStore.ts`'s established pattern — flagged during research, resolved by
   deletion in plan 34.6-09 once A-03's keyring-only path made it fully dead code.
6. The `callOrDeclare` "11 vs 10" discrepancy is resolved below, in its own subsection.

**The `callOrDeclare` reconciliation — 20 sites across 6 files, not 10 or 11.** Independently
re-measured in plan 34.6-10 and confirmed against `34.6-PATTERNS.md`:

| File | Genuine `callOrDeclare(` call sites |
|---|---|
| `src/frontend/components/UI/EditGameDialog/index.tsx` | 1 |
| `src/frontend/components/UI/SteamGridDBPicker/index.tsx` | 3 |
| `src/frontend/components/UI/Winetricks/index.tsx` | 2 |
| `src/frontend/screens/Library/components/InstallModal/SideloadDialog/index.tsx` | 1 |
| `src/frontend/screens/Settings/components/SteamGridDbApiKey.tsx` | 2 |
| `src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx` | 11 |
| **Total** | **20** |

`EosDeclineCallSiteGuard.test.ts`'s `EXPECTED_EOS_CALL_SITES = 11` is exactly correct — all 11
EOS-specific sites live in `AdvancedSettings/index.tsx`. D-08's older "10" figure appears to have
simply omitted `Winetricks/index.tsx` (2) and `SteamGridDbApiKey.tsx` (2) from an original count of
the other four files (16), which is why 16 + 4 = 20 reconciles and 10 does not.

**Electron-reach ledger deltas, per plan, where separately recorded:**

- Plan 34.6-05: `visitedFiles` 239 → 244 (+5: `appShellFlowRegistration.ts`,
  `sendChannelObservable.ts`, `appshell/themes.ts`, `appshell/releases.ts`, `appshell/language.ts`);
  `electronImportingFiles` unchanged at 35.
- Plan 34.6-06: `visitedFiles` 244 → 246 (+2: `installFlowRegistration.ts`,
  `backend/utils/checkGameUpdates.ts`); `electronImportingFiles` unchanged at 35.
- Plan 34.6-08: `visitedFiles` 246 → 247 (+1: `eosOverlayFlowRegistration.ts` itself);
  `electronImportingFiles` unchanged at 35 — `eos_overlay.ts` was already transitively reachable via
  an earlier entry point.
- Plan 34.6-10: an isolated `git show`-based swap measured `getDefaultSavePath`'s OWN direct import
  of `save_sync.ts` as the cause of a ledger baseline gain — NOT the plan's own predicted edge
  (`syncGOGSaves`, which this phase's plan 34.5 had already proven does NOT reach `save_sync.ts`).
  Before: 38/252. After: 39/253. Clean +1/+1.
- Plans 34.6-07 and 34.6-09 did not separately record a `visitedFiles`/`electronImportingFiles`
  delta in their own SUMMARY documents; this document does not fabricate one for them.

**A-04, the stale citation — bigger than a line-number fix.** `IPC-PORT-INVENTORY.md`'s
`frontendReady` entry originally cited `GlobalState.tsx:1586` and claimed the channel "will hit
`UNPORTED_CHANNEL_MARKER` under Tauri today". Plan 34.6-13 fixed the line number (`:1609`, confirmed
by direct read: `window.api.frontendReady()`) — but the SAME bullet's failure-mode claim was also
false, and false in the DANGEROUS direction: `frontendReady` is send-kind, so it can never surface
`UNPORTED_CHANNEL_MARKER` at all — `dispatchSend` finds no listener and returns, with no
timeout/rejection machinery on either side. It failed SILENTLY, not loudly. The inventory described
a loud failure where the truth was a silent one; this is exactly why D-11's `logSendHandlerReached`
observable exists, and it is the same failure shape that made `winetricksInstall`'s FAIL invisible
to every automated check in this phase.

**`frontendReady`'s two deliberate body exclusions**, RED-proven via mock-not-called assertions
(plan 34.6-05), reproduced here rather than only in the channel table above: (1) `handleProtocol(...)`
— redundant with `bootstrap.ts`'s own `deliverStartupProtocolUrl`/`registerProtocolUrlHandler`; (2)
the 5s `initQueue(true)` boot-time auto-resume — deferred to Phase 35 per Phase 33's own D-04. Both
are intentional narrowings of the curated port from `main.ts:560-601`, not omissions.

**FINDING 2 (Step 2, `34.6-LIVE-GATE.md`) — `GameSubMenu`'s three EOS call sites bypass
`callOrDeclare` entirely, and this document does not paper over it.** `handleEosOverlay()`
(`src/frontend/screens/Game/GameSubMenu/index.tsx:225-241`) calls `disableEosOverlay`/
`enableEosOverlay`/`installEosOverlay` DIRECTLY — zero matches for
`callOrDeclare|declaredUnavailable` in that file. On Linux (the one platform where this menu item is
reachable, per the `isLinux`-gate above), a rejection from any of these three calls is an unhandled
promise rejection that reaches neither `gamelib.log` sink: `bootErrorSurface.ts`'s shared handler
only `console.error`s once `#root` is mounted, and `index.tsx`'s `window.api.logError` bridge
listens only for `'error'`, never `'unhandledrejection'`. `EosDeclineCallSiteGuard.test.ts`'s
`EXPECTED_EOS_CALL_SITES = 11` cannot see this — it enumerates conforming call sites, and this one
does not conform. The live gate explicitly assigned this finding's disposition to this plan
("plan 34.6-14 to pick up and dispose of"). Disposed of here, per that instruction: filed as
`.planning/todos/pending/2026-08-25-eos-overlay-gamesubmenu-bypasses-callordeclare-on-linux.md`,
not fixed directly — this plan is documentation-only and modifies no source file.

**The `installed.json` file-watcher gap (Step 7, `34.6-LIVE-GATE.md`) — recorded, not fixed.**
`main.ts:1031-1044`'s watcher on `installed.json`, which calls `refreshInstalled()` 500ms after any
change, is not in the sidecar's import graph and has fired 0 times under Tauri (measured against
both the running bundle and `gamelib.log`). This is why `getDefaultSavePath` reads back a stale
in-memory value even though legendary correctly persisted the real one to disk — and it means ANY
field legendary writes to `installed.json` (`save_path`, `save_timestamp`) is permanently invisible
to GameLib under Tauri, not just this one channel's symptom. The gate's own text records "a todo is
owed for this but not filed by this task, per instruction" without naming a plan; as the phase's
closing-artefacts plan, this document treats that as owed to it and disposes of it here: filed as
`.planning/todos/pending/2026-08-25-installed-json-watcher-not-ported-to-tauri.md`.
