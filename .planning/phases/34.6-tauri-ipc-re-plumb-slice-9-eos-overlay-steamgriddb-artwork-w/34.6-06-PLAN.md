---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 06
type: execute
wave: 2
depends_on: ["34.6-05"]
files_modified:
  - src/backend/sidecar/installFlowRegistration.ts
  - src/backend/sidecar/__tests__/installFlows.test.ts
  - src/backend/sidecar/__tests__/electronReachLedger.test.ts
autonomous: true
requirements: [REQ-34.6-04, REQ-34.6-13]
must_haves:
  truths:
    - "D-02: `importGame` and `moveInstall` are registered on the sidecar BYTE-EQUIVALENTLY, with no validation added — hardening lands in plan 34.6-11, separately committed"
    - "A live-gate FAIL on either channel is bisectable to 'the port broke it' versus 'validation rejected it', which is exactly what D-02's rule buys"
    - "The electron-reach ledger measures `installFlowRegistration.ts`"
  artifacts:
    - path: "src/backend/sidecar/installFlowRegistration.ts"
      provides: "importGame and moveInstall registered invoke-kind"
      contains: "moveInstall"
  key_links:
    - from: "src/backend/sidecar/installFlowRegistration.ts"
      to: "libraryManagerMap[runner].getGame(appName).moveInstall / importGame"
      via: "direct import of the underlying logic module, never `main.ts` and never a feature ipc_handler"
      pattern: "libraryManagerMap"
---

<objective>
Port `importGame` and `moveInstall` onto the sidecar, BYTE-EQUIVALENTLY, per D-02.

D-02 is explicit and this plan must not improve on it: these two channels (with
`runWineCommandForGame`, plan 34.6-07) carry `T-34.5-C6-49-03` — renderer-supplied filesystem paths
reaching real file operations, with no sidecar handler today. They port AS-IS, and a DEDICATED,
SEPARATELY-COMMITTED plan in this same phase (34.6-11) then hardens them. The rejected alternatives
were: hardening inline at the port (a live-gate FAIL could not be bisected to "port broke it" vs
"validation rejected it"), and forwarding to Phase 35 (the cutover is where it stops being
reversible, and this project's record is that forwarded findings go unowned — `D-29-08` sat unowned
for weeks).

A-03 is an exception to this rule for SteamGridDB ONLY. It does not extend here.

Purpose: registration without behaviour change, so plan 34.6-11's hardening has a clean baseline.
Output: two channels registered, the ledger grown, no validation added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-05-SUMMARY.md

<interfaces>
Electron originals (`src/backend/main.ts`):
  addHandler('moveInstall', async (event, { appName, path, runner }): Promise<void> => { ... })   // lines 1112-1158
  addHandler('importGame', async (event, { appName, path, runner, platform, winePrefix, wineVersion, ... }) => { ... })   // lines 1160-1245

Both bodies use, and all of these are already safe under the sidecar:
  sendGameStatusUpdate  (backend/utils)
  notify                (already an appShellFlowRegistration send channel)
  showDialogBoxModalAuto (backend/dialog/dialog.ts — Rust-forwarded, never-throwing stub)
  libraryManagerMap     (backend/storeManagers)
  i18next.t             (already used by ported channels)

Existing idiom in the target module (`src/backend/sidecar/installFlowRegistration.ts:159, 206-208`):
  ipcMain.handle('uninstall', (event: unknown, ...args: unknown[]) => ...)
  ipcMain.handle('checkGameUpdates', checkGameUpdates)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register `moveInstall` and `importGame` in `installFlowRegistration.ts`, byte-equivalently</name>
  <read_first>
    - `src/backend/main.ts` lines 1108-1245 (BOTH handler bodies in full — this is the source of truth for byte-equivalence)
    - `src/backend/sidecar/installFlowRegistration.ts` in full (its docstring, its curated-import list, and its `ipcMain.handle` idiom)
    - `src/backend/dialog/dialog.ts` (82 lines — confirm `showDialogBoxModalAuto`'s sidecar behaviour; the stub is Rust-forwarded and non-throwing, unlike the `dialog.showMessageBox` reject hazard 34.5 D-15 records)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md` § "Curated-import discipline"
  </read_first>
  <files>src/backend/sidecar/installFlowRegistration.ts, src/backend/sidecar/__tests__/installFlows.test.ts</files>
  <behavior>
    - `registerInstallFlows()` puts `moveInstall` and `importGame` in `handlerRegistry`, not `listenerRegistry`.
    - Invoking the registered `moveInstall` handler with `{ appName, path, runner }` calls `libraryManagerMap[runner].getGame(appName).moveInstall(path)` with `path` passed through UNCHANGED — no normalisation, no resolution, no rejection.
    - `moveInstall` emits `sendGameStatusUpdate` with status `moving` before the move and `done` after, in that order, on both the success and the error branch.
    - On `moveRes.status === 'error'`, `moveInstall` calls `showDialogBoxModalAuto` and still emits the trailing `done` status.
    - Invoking the registered `importGame` handler passes every field of its argument object through to the underlying import call unchanged.
  </behavior>
  <action>
Add two `ipcMain.handle(...)` registrations to `registerInstallFlows()` in
`src/backend/sidecar/installFlowRegistration.ts`, copying the bodies from `main.ts:1112-1158`
(`moveInstall`) and `main.ts:1160-1245` (`importGame`) with NO semantic change. Adapt only the
mechanical wrapper: `addHandler(name, async (event, arg) => ...)` becomes
`ipcMain.handle(name, async (_event: unknown, ...args: unknown[]) => ...)` matching the idiom
already used at `installFlowRegistration.ts:159`.

Import the underlying logic modules DIRECTLY (`backend/storeManagers`'s `libraryManagerMap`,
`backend/utils`'s `sendGameStatusUpdate`, `backend/dialog/dialog`'s `showDialogBoxModalAuto`).
Never import `src/backend/main.ts` and never import a feature's own `ipc_handler.ts` — that file
calls `addHandler`/`addListener` from `backend/ipc`, an Electron-only path unusable from the
sidecar.

**Add NO validation, NO path containment, NO argument sanitisation.** If a reviewer's instinct
during execution is to "just add a quick check on `path`", that instinct is what D-02 exists to
overrule. Record `T-34.5-C6-49-03` in an inline comment on each of the two registrations, naming
plan 34.6-11 as the owner of the hardening and citing D-02.

Extend `src/backend/sidecar/__tests__/installFlows.test.ts` with the five behaviour assertions
above. The pass-through assertion for `moveInstall` must assert argument IDENTITY (the same string
instance or a strict equality against a value containing `..` and a space), copying the
`runWineCommand` pass-through-proof idiom at `wineToolsFlows.test.ts` Describe 3 — a
byte-equivalence claim that only tests a clean input would still pass if normalisation were added.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "installFlows" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle('moveInstall'" src/backend/sidecar/installFlowRegistration.ts` returns 1 and the same for `'importGame'`.
    - `grep -n 'ipc_handler' src/backend/sidecar/installFlowRegistration.ts` returns no match.
    - `grep -n 'T-34.5-C6-49-03' src/backend/sidecar/installFlowRegistration.ts` returns 2 matches, one per registration, each naming plan 34.6-11.
    - The pass-through test uses an input containing `..` and a space and asserts it arrives UNCHANGED. This is RED-proven: inserting `path.resolve(...)` or a `..` rejection into the handler makes exactly that test fail. Revert after proving.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both channels are registered with a proven-unchanged argument path, and the security finding is recorded at the code, not only in planning documents.</done>
</task>

<task type="auto">
  <name>Task 2: Grow the electron-reach ledger to cover `installFlowRegistration.ts`</name>
  <read_first>
    - `src/backend/sidecar/__tests__/electronReachLedger.test.ts` lines 1-100 (`ENTRY_POINTS` — `installFlowRegistration.ts` is NOT currently a member) and the growth-only header
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-05-SUMMARY.md` (what plan 34.6-05 measured and added, so this plan's additions are additive to that state)
  </read_first>
  <files>src/backend/sidecar/__tests__/electronReachLedger.test.ts</files>
  <action>
Add `join(REPO_ROOT, 'src/backend/sidecar/installFlowRegistration.ts')` to `ENTRY_POINTS` with an
inline comment following the file's convention (`// Phase 34.6 Plan 06 (REQ-34.6-13): ...`) noting
that this module has never been measured, so its transitive electron reach is unknown until now.

Then RUN the test and read its failure output to DISCOVER which modules to add to
`BASELINE_ELECTRON_REACHING_MODULES`. Do not predict them here. Add exactly what the measurement
names, each with a one-line comment naming the electron symbol it imports.

Do not weaken the subset assertion. Adding an entry point without extending the baseline fails the
test BY CONSTRUCTION — that is the tripwire working, and the array is what gets extended.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "electronReachLedger" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "installFlowRegistration.ts" src/backend/sidecar/__tests__/electronReachLedger.test.ts` returns at least 1 inside `ENTRY_POINTS`.
    - The test passes with 0 failures.
    - The SUMMARY records the MEASURED newly-added baseline modules and states they were read from the test's failure output.
    - `git diff HEAD -- src/backend/sidecar/__tests__/electronReachLedger.test.ts` shows only additions to the two arrays and their comments — no assertion operator changed.
  </acceptance_criteria>
  <done>The ledger measures `installFlowRegistration.ts` and its baseline reflects a measurement, not a prediction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar (`importGame`, `moveInstall`) | renderer-supplied filesystem paths cross here and reach real file operations; there is no sidecar handler today, which is the finding |
| sidecar → filesystem | the move/import destination is whatever the renderer sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C6-49-03 | Tampering | `importGame`, `moveInstall` — renderer-supplied filesystem paths with no containment | mitigate (DEFERRED WITHIN THIS PHASE, by D-02) | Ported as-is here so a live-gate FAIL stays bisectable; hardened by plan 34.6-11, separately committed, inside this same phase. Recorded in an inline comment at each registration naming 34.6-11 as owner. NOT forwarded to Phase 35 — D-02 rejected that explicitly because forwarded findings go unowned here. |
| T-34.6-17 | Tampering | a well-meaning executor adding "just a quick path check" during the port | mitigate | Task 1's pass-through test asserts a `..`-containing path arrives UNCHANGED and is RED-proven against the addition of `path.resolve` or a traversal rejection. The port cannot silently acquire validation. |
| T-34.6-18 | Denial of Service | `showDialogBoxModalAuto` rejecting under the sidecar and crashing the app (the 34.5 D-15 hazard) | accept | `backend/dialog/dialog.ts`'s stub is Rust-forwarded and non-throwing; RESEARCH re-verified this. The genuine crash-capable path in this phase is `eos_overlay.ts:162`'s `dialog.showMessageBox`, which plan 34.6-08 and live-gate step 2 own. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed. |
</threat_model>

<verification>
- `npx jest --selectProjects Backend --testPathPattern "installFlows|electronReachLedger"` — green.
- `npx tsc --noEmit` — exits 0.
- `pnpm build:sidecar && node build/main/sidecar.js` — starts and prints the ready sentinel.
- `npm start` (Electron) unchanged — `main.ts`'s own handlers are untouched.
</verification>

<success_criteria>
- `moveInstall` and `importGame` are registered invoke-kind on the sidecar.
- No validation was added; the pass-through is RED-proven against a `..`-containing input.
- `T-34.5-C6-49-03` is recorded at both call sites with plan 34.6-11 named as owner.
- The electron-reach ledger measures `installFlowRegistration.ts`.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-06-SUMMARY.md` when done.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
Run `graphify update .` after the source changes land.
</output>
