---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 09
type: execute
wave: 5
depends_on: ["34.6-02", "34.6-08"]
files_modified:
  - src/backend/sidecar/enrichmentFlowRegistration.ts
  - src/backend/steamgrid/ipc_handler.ts
  - src/backend/sidecar/__tests__/enrichmentFlows.test.ts
  - src/backend/sidecar/__tests__/electronReachLedger.test.ts
  - src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts
autonomous: true
requirements: [REQ-34.6-02, REQ-34.6-04, REQ-34.6-08, REQ-34.6-13]
must_haves:
  truths:
    - "A-03: the 5 SteamGridDB channels are registered ONLY after the keyring hardening landed, so D-06's live gate cannot write the operator's real key to disk in plaintext"
    - "Exactly ONE API-key migration codepath survives the port — `ipc_handler.ts`'s inline `getDecryptedApiKey()` branch is gone"
    - "`getGogDiscounts` is registered invoke-kind"
    - "D-09: the SteamGridDB 5 flip from ABSENCE to PRESENCE, leaving only Zoom's 3 asserting absence"
  artifacts:
    - path: "src/backend/sidecar/enrichmentFlowRegistration.ts"
      provides: "5 steamgriddb.* channels + getGogDiscounts, registered invoke-kind"
      contains: "steamgriddb.setApiKey"
  key_links:
    - from: "src/backend/sidecar/enrichmentFlowRegistration.ts"
      to: "getSteamGridDbSecretStore()"
      via: "the API-key read/write path, replacing the direct GlobalConfig + secureKey access"
      pattern: "getSteamGridDbSecretStore"
    - from: "src/backend/sidecar/enrichmentFlowRegistration.ts"
      to: "src/backend/steamgrid/utils.ts"
      via: "direct import of searchGame/getGrids/getHeroes, never steamgrid/ipc_handler.ts"
      pattern: "steamgrid/utils|from '../steamgrid'"
---

<objective>
Port the 5 SteamGridDB channels plus `getGogDiscounts` into `enrichmentFlowRegistration.ts`, and
reconcile the two competing API-key migration paths down to one.

**This plan MUST NOT run before plans 34.6-01 and 34.6-02.** A-03 makes the keyring hardening a
CO-REQUISITE that lands BEFORE this port: registering `steamgriddb.setApiKey`/`hasApiKey` makes
`secureKey.ts`'s `safeStorage` import resolve to `electronStub.ts`'s deliberately dead stub, so
`encryptApiKey()` takes its plaintext branch and the key persists in the clear — and D-06's live
gate enters a REAL key through the settings field, so the gate step itself would be the moment of
exposure. This is an explicit, operator-approved exception to D-02's port-then-harden rule, taken
for this channel only; D-02 is unchanged for its own three channels.

PATTERNS delta 5 is also settled here: `steamgrid/ipc_handler.ts`'s `getDecryptedApiKey()` contains
an inline legacy-plaintext migration branch that is a SECOND, older path distinct from the
`humbleSecretStore.ts` pattern plan 34.6-01 adopted. Only one migration codepath may survive.

Purpose: register the cluster onto a storage path that cannot leak, with a single migration.
Output: 6 channels registered, one migration path deleted, three gates amended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-01-SUMMARY.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-02-SUMMARY.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-08-SUMMARY.md

<interfaces>
Landed by plans 34.6-01/02:
  src/backend/steamgrid/secretStore.ts
    export interface SteamGridDbSecretStore { isAvailable(): boolean
                                              getApiKey(): Promise<string | undefined>
                                              setApiKey(value: string): Promise<void>
                                              clearApiKey(): Promise<void> }
    export function getSteamGridDbSecretStore(): SteamGridDbSecretStore
  src/backend/sidecar/steamgridSecretStore.ts
    export function installSidecarSteamGridDbSecretStore(): void   // called from bootstrap.ts

Electron original (`src/backend/steamgrid/ipc_handler.ts`, 99 lines — never imported):
  addHandler('steamgriddb.hasApiKey',  () => !!readStoredApiKey())
  addHandler('steamgriddb.setApiKey',  (event, key) => { const trimmed = key.trim()
                                        const stored = trimmed ? encryptApiKey(trimmed) : ''
                                        GlobalConfig.get().setSetting('steamGridDbApiKey', stored) })
  addHandler('steamgriddb.searchGame', async (event, query) => { ...SteamGridDB.searchGame(apiKey, query)
                                        mapped to { id, name }; empty array when no key... })
  addHandler('steamgriddb.getGrids',   async (event, args) => { ...SteamGridDB.getGrids(apiKey,
                                        { gameId, dimensions, styles }) mapped to { id, url, thumb }... })
  addHandler('steamgriddb.getHeroes',  async (event, args) => { ...same shape as getGrids... })
  // plus the local helpers readStoredApiKey() and getDecryptedApiKey() — the second migration path

Electron original (`src/backend/discounts/index.ts:100-135`):
  addHandler('getGogDiscounts', async (_event, locale, hideOwned = false, wishlistOnly = false) => {...})
  // uses GOGUser.getCredentials() only; no direct electron API

`src/backend/steamgrid/utils.ts:3` — `import { app } from 'electron'` (the new reach edge).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register the 5 SteamGridDB channels and `getGogDiscounts` in `enrichmentFlowRegistration.ts`, reading the key through the seam</name>
  <read_first>
    - `src/backend/steamgrid/ipc_handler.ts` in full (99 lines — the five handler bodies, `readStoredApiKey()`, and `getDecryptedApiKey()`'s inline migration branch)
    - `src/backend/steamgrid/secretStore.ts` (landed by plan 34.6-01 — the seam this port must use instead)
    - `src/backend/discounts/index.ts` lines 95-140 (the `getGogDiscounts` body)
    - `src/backend/sidecar/enrichmentFlowRegistration.ts` in full (201 lines — its docstring, which already RESERVES these 5 channel names, its curated-import rule, and its `ipcMain.handle` idiom)
  </read_first>
  <files>src/backend/sidecar/enrichmentFlowRegistration.ts, src/backend/sidecar/__tests__/enrichmentFlows.test.ts</files>
  <behavior>
    - `registerEnrichmentFlows()` puts all 5 `steamgriddb.*` names plus `getGogDiscounts` in `handlerRegistry` and none in `listenerRegistry`.
    - `steamgriddb.setApiKey` with `'  abc  '` calls `getSteamGridDbSecretStore().setApiKey('abc')` — trimmed — and never touches `GlobalConfig`.
    - `steamgriddb.setApiKey` with `''` clears the key.
    - `steamgriddb.hasApiKey` returns `true` only when the seam resolves a non-empty key, and resolves `false` (never rejects) when the seam's read fails.
    - `steamgriddb.searchGame` with no key resolves `[]` without any network call, matching the Electron original.
    - `steamgriddb.getGrids` and `getHeroes` map results to `{ id, url, thumb }` and RETHROW a network error (the Electron original logs and rethrows — preserve that; these are invoke-kind so the rejection reaches the caller).
    - `getGogDiscounts` forwards `locale`, `hideOwned` and `wishlistOnly` with the same defaults.
  </behavior>
  <action>
Extend `registerEnrichmentFlows()` in `src/backend/sidecar/enrichmentFlowRegistration.ts` with six
`ipcMain.handle(...)` registrations, bodies copied from `steamgrid/ipc_handler.ts` and
`discounts/index.ts` with ONE deliberate change: every API-key read and write goes through
`getSteamGridDbSecretStore()` instead of `GlobalConfig.get().getSettings().steamGridDbApiKey` /
`setSetting` / `encryptApiKey` / `decryptApiKey`. That change IS A-03's whole point — a
byte-equivalent port here would re-open the plaintext hazard.

Because the seam is async, the handlers that read the key become `async` and `await` it.
`steamgriddb.hasApiKey` was synchronous in Electron; it is invoke-kind, so making it async is
transport-transparent to the renderer. Record that in the module docstring.

Import the underlying logic directly: `* as SteamGridDB from '../steamgrid/utils'` for
`searchGame`/`getGrids`/`getHeroes`, `getSteamGridDbSecretStore` from `../steamgrid/secretStore`,
and the discounts function from `../discounts`. **Never import `steamgrid/ipc_handler.ts`** — the
curated-import rule this module's own docstring states.

`getGogDiscounts` ports byte-equivalently — it touches no electron API and uses only
`GOGUser.getCredentials()`.

Extend `src/backend/sidecar/__tests__/enrichmentFlows.test.ts` with the seven behaviours above. The
never-touches-config assertion must be explicit and RED-proven: after `steamgriddb.setApiKey`,
assert the mocked `GlobalConfig` settings object has NO `steamGridDbApiKey` entry.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "enrichmentFlows" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All six names are registered: `for c in steamgriddb.hasApiKey steamgriddb.setApiKey steamgriddb.searchGame steamgriddb.getGrids steamgriddb.getHeroes getGogDiscounts; do grep -q "'$c'" src/backend/sidecar/enrichmentFlowRegistration.ts || echo "MISSING $c"; done` prints nothing.
    - `grep -v '^ *[/*]' src/backend/sidecar/enrichmentFlowRegistration.ts | grep -c 'ipc_handler'` returns 0.
    - `grep -v '^ *[/*]' src/backend/sidecar/enrichmentFlowRegistration.ts | grep -c 'encryptApiKey\|decryptApiKey\|steamGridDbApiKey'` returns 0 — the port reaches the key ONLY through the seam.
    - The never-touches-config assertion is RED-proven: adding a `setSetting('steamGridDbApiKey', ...)` call to the `setApiKey` handler makes exactly that test fail. Revert after proving.
    - `pnpm build:sidecar && node build/main/sidecar.js` starts and prints the ready sentinel.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Six channels registered, with the API key reachable only through the keyring-backed seam.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the second migration path in `steamgrid/ipc_handler.ts` and grow the electron-reach ledger</name>
  <read_first>
    - `src/backend/steamgrid/ipc_handler.ts` lines 1-30 (`readStoredApiKey()` and `getDecryptedApiKey()`, whose `if (!isEncryptedValue(stored))` branch re-encrypts and persists — the second migration path)
    - `src/backend/steamgrid/secretStore.ts` (`ElectronSteamGridDbSecretStore.getApiKey()`, which plan 34.6-01 gave the SAME legacy-plaintext responsibility)
    - `src/backend/sidecar/__tests__/electronReachLedger.test.ts` lines 1-100 and 234-300
    - `src/backend/steamgrid/utils.ts` line 3 (`import { app } from 'electron'`)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md` § Deltas 4 and 5
  </read_first>
  <files>src/backend/steamgrid/ipc_handler.ts, src/backend/sidecar/__tests__/electronReachLedger.test.ts</files>
  <action>
**PATTERNS delta 5 — collapse the two migration paths to one.** Rewrite
`src/backend/steamgrid/ipc_handler.ts`'s five handlers to call `getSteamGridDbSecretStore()`
exactly as the sidecar registration now does, and DELETE the local `readStoredApiKey()` and
`getDecryptedApiKey()` helpers together with their inline `if (!isEncryptedValue(stored))`
re-encrypt-and-persist branch. That responsibility now lives in
`ElectronSteamGridDbSecretStore.getApiKey()` (plan 34.6-01), which is the one surviving codepath.

Leaving both live is not a cosmetic duplication: two migration paths over one `configStore` key can
interleave, and only one of them is covered by plan 34.6-01's tests. After this task,
`grep -c 'isEncryptedValue' src/backend/steamgrid/ipc_handler.ts` must return 0.

`ipc_handler.ts` stays an Electron-only file and is still never imported by the sidecar — this
rewrite changes which module owns the migration, not the import graph.

**PATTERNS delta 4 — the ledger.** `enrichmentFlowRegistration.ts` is ALREADY an `ENTRY_POINTS`
member, so no entry point is added here. But it now reaches `src/backend/steamgrid/utils.ts`, which
imports `app` from `electron` at its own line 3. RUN the ledger test and add exactly the modules its
failure output names to `BASELINE_ELECTRON_REACHING_MODULES`, each with a one-line comment naming
the electron symbol. `steamgrid/utils.ts` is one of them by direct grep; do not assume it is the
only one.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "electronReachLedger|enrichmentFlows" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'isEncryptedValue\|getDecryptedApiKey\|readStoredApiKey' src/backend/steamgrid/ipc_handler.ts` returns 0.
    - `grep -c 'getSteamGridDbSecretStore' src/backend/steamgrid/ipc_handler.ts` returns at least 1.
    - `grep -c "steamgrid/utils.ts" src/backend/sidecar/__tests__/electronReachLedger.test.ts` returns at least 1 inside `BASELINE_ELECTRON_REACHING_MODULES`.
    - Repo-wide, exactly one module implements the legacy-plaintext migration: `grep -rn "isEncryptedValue" src/backend --include=*.ts` returns matches only in `secureKey.ts` (the primitive) and `secretStore.ts` (the one consumer) — no third file.
    - Both suites pass with 0 failures; `npx tsc --noEmit` exits 0.
    - `npm start` (Electron) still reads and writes a SteamGridDB API key: assert by running the plan 34.6-01 `secretStore` suite, which covers the Electron implementation this rewrite now routes through.
  </acceptance_criteria>
  <done>One migration path survives, and the ledger measures the SteamGridDB reach edge.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the SteamGridDB 5 to PRESENCE in `runnerSliceRegistration.test.ts`, leaving only Zoom's 3 asserting absence</name>
  <read_first>
    - `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` lines 75-100 (the module-scope block as left by plan 34.6-08) and Describe 6 / Describe 7 as left by plan 34.6-08
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-08-SUMMARY.md` (the measured Describe 6 values and the `MODULES`-table decision)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md` § 5 and § Deltas item 3
  </read_first>
  <files>src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts</files>
  <action>
As with the EOS 8, the array edit alone will NOT work: this file's module scope does not call
`registerEnrichmentFlows()`, so the five `steamgriddb.*` registries are empty here. Add
`import { registerEnrichmentFlows } from '../enrichmentFlowRegistration'` and call it in the
module-scope block, BEFORE the `CANONICAL_HANDLER_ENTRIES` / `CANONICAL_LISTENER_ENTRIES` snapshot
is taken.

**Describe 7 (D-09).** Remove the 5 dotted `steamgriddb.*` names from `DEFERRED_STEAMGRIDDB` so
that array is empty (or delete it and its spread), and re-derive the self-containment literal by
REMOVING the names: it becomes `3` — `DROPPED_ZOOM` alone. This is D-09's end state. `DROPPED_ZOOM`
STAYS asserting absence: 34.5 D-02 dropped Zoom permanently, and this assertion is the only thing in
the repo keeping it dropped.

Add the 5 names to the presence assertion, `handlerRegistry.has(channel) === true` and
`(listenerRegistry.get(channel) ?? []).length === 0` for each — all 5 are invoke-kind.

**Describe 6.** Re-derive its `handleChannels.length` / `listenChannels.length` / per-module counts
by MEASUREMENT, consistently with the `MODULES`-table decision plan 34.6-08 recorded. If
`enrichmentFlowRegistration` joins `MODULES`, add its full declared-channel list, not only a bumped
count — a count bumped without the names added would pass while measuring nothing. Re-derive each
number independently and confirm agreement with the measured value; stop and report on disagreement.

Note for the record in the SUMMARY: `runnerMiscFlowRegistration`'s three new channels land in plan
34.6-10, which performs the FINAL Describe 6 re-derivation and the final both-directions RED proof.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "runnerSliceRegistration" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "registerEnrichmentFlows()" src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` returns a match whose line number is LESS than the `CANONICAL_HANDLER_ENTRIES` declaration line.
    - `grep -n "toBe(3)" src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` returns a match inside Describe 7's self-containment test, and the absence array now contains only `authZoom`, `getZoomUserInfo` and `logoutZoom`.
    - All 5 `steamgriddb.*` names appear in a presence assertion and none in the absence array.
    - The suite passes with 0 failures.
    - Both directions are RED-proven: (a) commenting out `ipcMain.handle('steamgriddb.getGrids', ...)` makes exactly that presence assertion FAIL; (b) registering a fake `logoutZoom` listener makes the Zoom absence assertion FAIL. Revert both; record both failure messages in the SUMMARY.
  </acceptance_criteria>
  <done>Describe 7's absence array is down to Zoom's 3 — D-09's end state for the absence half.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar (`steamgriddb.setApiKey`) | a real user credential crosses here and is persisted |
| sidecar → steamgriddb.com | a search query and a game id leave the machine, authenticated with the user's key |
| sidecar → OS Keychain | where the key must land, per A-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-01 | Information Disclosure | the API key persisting in `config.json` in the clear because `safeStorage` resolves to a dead stub under the sidecar | mitigate | A-03's co-requisite: plans 34.6-01/02 landed the keyring seam FIRST, and this port reaches the key ONLY through `getSteamGridDbSecretStore()`. Asserted by `grep -c 'encryptApiKey\|steamGridDbApiKey'` returning 0 in the registration module, and RED-proven by task 1's never-touches-config test. Live-gate step 3 re-checks the negative against a real `config.json`. |
| T-34.6-24 | Tampering | two competing migration codepaths interleaving over one `configStore` key | mitigate | Task 2 deletes `ipc_handler.ts`'s inline branch; the repo-wide grep in its acceptance criteria proves exactly one implementation survives. |
| T-34.6-25 | Information Disclosure | a `logError` in `searchGame`/`getGrids`/`getHeroes` echoing the API key inside the error object | mitigate | The Electron originals log `['SteamGridDB search failed:', error]` — the key is a header, not part of the query. Preserve the shape and assert in the new tests that no log argument contains the seeded key value. |
| T-34.6-26 | Spoofing | a compromised or MITM'd steamgriddb response driving artwork URLs into the renderer | accept | The port introduces no new trust in the response beyond the Electron original's, which already maps to `{ id, url, thumb }`. Out of this phase's scope; not a regression. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed. |
</threat_model>

<verification>
- `npx jest --selectProjects Backend --testPathPattern "enrichmentFlows|electronReachLedger|runnerSliceRegistration|steamgrid"` — green, 0 failures.
- `npx tsc --noEmit` — exits 0.
- `pnpm build:sidecar && node build/main/sidecar.js` — starts and prints the ready sentinel.
- `npm start` (Electron) still round-trips a SteamGridDB API key, covered by the `secretStore` suite that `ipc_handler.ts` now routes through.
- `grep -rn "isEncryptedValue" src/backend --include=*.ts` names exactly two files.
</verification>

<success_criteria>
- 5 SteamGridDB channels plus `getGogDiscounts` are registered invoke-kind.
- The API key is unreachable from the registration module except through the keyring-backed seam.
- Exactly one legacy-plaintext migration codepath exists repo-wide.
- Describe 7's absence array holds only Zoom's 3.
- HEAD is green at the end of this plan.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-09-SUMMARY.md` when done, recording the RED-proof failure messages, the measured ledger additions and the Describe 6 values.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
Run `graphify update .` after the source changes land.
</output>
