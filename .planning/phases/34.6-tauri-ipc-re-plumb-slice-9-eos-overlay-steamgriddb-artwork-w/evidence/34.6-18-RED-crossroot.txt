npm warn Unknown project config "node-linker". This will stop working in the next major version of npm. See `npm help npmrc` for supported config options.
Running one project: Backend
FAIL Backend src/backend/sidecar/__tests__/installFlows.test.ts
  sidecar install-slice flows (Phase 30 Plan 02)
    ✓ Test 1: listSteamLibraryTargets resolves [] with the native-install opt-in OFF (mirrors Electron gate) (5 ms)
    ✓ Test 1b: listSteamLibraryTargets resolves the real registered-library array with the opt-in ON (1 ms)
    ✓ Test 4: uninstall resolves and delegates to the real uninstallGameCallback (2 ms)
    ✓ Test 5: checkGameUpdates resolves a string[] without rejecting (1 ms)
    ✓ WR-05: one runner rejecting must not discard the other runners’ update results (10 ms)
    ✓ Test 6 (Invariant B guard): winetricksInstall (deliberately unported, deferred to Phase 34.6) still rejects non-fatally, and the RPC loop keeps serving (1 ms)
    ✓ T-34.6 registration-kind: moveInstall and importGame are invoke-kind (handlerRegistry), never listener-kind (listenerRegistry)
    ✓ moveInstall forwards a legitimate apostrophe-and-space-containing path (inside the configured root) to SteamGame.moveInstall() unchanged (1 ms)
    ✕ moveInstall forwards an absolute destination OUTSIDE the configured defaultInstallPath to SteamGame.moveInstall() unchanged (cross-drive move, CR-01)
    ✓ moveInstall rejects an absolute path containing a ".." segment, and SteamGame.moveInstall is never reached
    ✓ moveInstall rejects a path escaping the configured install root, and SteamGame.moveInstall is never reached (T-34.5-C6-49-03)
    ✓ a rejected moveInstall still emits the terminal gameStatusUpdate "done" (never wedges in "moving") (1 ms)
    ✓ a rejected moveInstall never logs the rejected path itself
    ✓ moveInstall emits gameStatusUpdate "moving" before the move and "done" after, on the success branch
    ✓ moveInstall calls showDialogBoxModalAuto on the error branch, and STILL emits the trailing "done" status (1 ms)
    ✓ importGame forwards every field of its argument object through unchanged (path/platform to SteamGame.importGame(), winePrefix/wineVersion/wineCrossoverBottle to writeConfig())
    ✕ importGame forwards an absolute source OUTSIDE the configured defaultInstallPath to SteamGame.importGame() unchanged (out-of-tree import, CR-01) (1 ms)
    ✓ importGame rejects a path escaping the configured install root, and SteamGame.importGame is never reached (T-34.5-C6-49-03)

  ● sidecar install-slice flows (Phase 30 Plan 02) › moveInstall forwards an absolute destination OUTSIDE the configured defaultInstallPath to SteamGame.moveInstall() unchanged (cross-drive move, CR-01)

    expect(jest.fn()).toHaveBeenCalledTimes(expected)

    Expected number of calls: 1
    Received number of calls: 0

      537 |     await flush()
      538 |
    > 539 |     expect(steamGameMocks.moveInstall).toHaveBeenCalledTimes(1)
          |                                        ^
      540 |     expect(steamGameMocks.moveInstall.mock.calls[0][0]).toBe(crossRootPath)
      541 |
      542 |     const response = frames.find((f) => f.id === 'move-crossroot-1')

      at Object.<anonymous> (src/backend/sidecar/__tests__/installFlows.test.ts:539:40)

  ● sidecar install-slice flows (Phase 30 Plan 02) › importGame forwards an absolute source OUTSIDE the configured defaultInstallPath to SteamGame.importGame() unchanged (out-of-tree import, CR-01)

    expect(jest.fn()).toHaveBeenCalledTimes(expected)

    Expected number of calls: 1
    Received number of calls: 0

      724 |     await flush()
      725 |
    > 726 |     expect(steamGameMocks.importGame).toHaveBeenCalledTimes(1)
          |                                       ^
      727 |     expect(steamGameMocks.importGame.mock.calls[0][0]).toBe(crossRootPath)
      728 |     expect(steamGameMocks.importGame.mock.calls[0][1]).toBe('Windows')
      729 |

      at Object.<anonymous> (src/backend/sidecar/__tests__/installFlows.test.ts:726:39)

Test Suites: 1 failed, 1 total
Tests:       2 failed, 16 passed, 18 total
Snapshots:   0 total
Time:        0.796 s, estimated 1 s
Ran all test suites matching /installFlows/i.
