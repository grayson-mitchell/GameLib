---
phase: 34.6
plan: 13
type: rederive-record
decision: D-03
run_date: 2026-08-25
commit: d2e30868c0888073d00bb42ed3c84b28b49d9da3
findings: 0
disposition: zero-findings
status: complete
---

# D-03 close-out re-derive — Phase 34.6

**Result: ZERO unbucketed channels. `IPC-PORT-INVENTORY.md`'s bucket lines cover the full
re-derived preload surface.**

D-03 exists because the inventory has been non-exhaustive **twice**: `getInstallInfo`
(plan 34.5-43), then eleven more (plan 34.5-49). Eight phases of source changes landed after the
2026-08-12 audit, so "the IPC re-plumb is COMPLETE" — Phase 35's own gate — had to be **derived**
after this phase's ports landed, not transcribed from a list written before them.

Run at commit `d2e30868c` on 2026-08-25, at phase close, after all 24 of this phase's ports.

## Live run — verbatim

```
OK: distinct invoke=160, send=60, union=220, push (out of tally)=30, unlisted=0. IPC-PORT-INVENTORY.md's bucket lines cover the full re-derived preload surface (U-34.5-14, D-CYCLE6-C).
```

Exit code **0**.

| Measure | Value |
|---------|-------|
| distinct invoke channels | 160 |
| distinct send channels | 60 |
| **union (tallied surface)** | **220** |
| push channels (out of tally) | 30 |
| **unlisted / unbucketed** | **0** |

The union of **220** sits **above** the audited floor of 217 the gate enforces — consistent with
this phase having added surface — and every one of those channels resolves to a bucket line.

## Non-vacuity — the failure mode this record must rule out

The `zero-findings` disposition is only meaningful if the gate could have failed and was looking
at something. Both confirmed, not assumed:

**1. The gate rejects bad input.** `--self-test` exercises six independent checks against
deliberately-broken fixtures and each one correctly rejected its fixture:

```
  self-test OK: a comment-only name leaking into the extracted invoke set rejected by check_comment_blindness correctly rejected
  self-test OK: a document where bucket-line scoping makes no difference rejected by check_bucket_line_scoping correctly rejected
  self-test OK: a mismatched '## Totals' -> 'Unique channels' figure rejected by check_totals_reconciliation correctly rejected
  self-test OK: a document with the audit-filename provenance marker stripped rejected by check_provenance correctly rejected

All 6 check(s) proved capable of rejecting the input they exist to reject (6 self-test case(s), 1:1).

SELF-TEST OK: every assertion rejects its corresponding bad input.
```

Covered: a preload channel absent from every bucket; a single-line-only regex regression (caught by
the audited floor); a comment-only name leaking into the extracted set; bucket-line scoping failing
to discriminate; a `## Totals` figure disagreeing with the bucket lines; and a missing provenance
marker.

**2. The live run examined a non-empty set.** 220 distinct channels re-derived from
`src/preload/` source at run time — via a comment-stripping, multi-line-aware regex over
`makeHandlerInvoker|makeListenerCaller|frontendListenerSlot` — not read from any list. A gate that
finds nothing because it looked at nothing is the specific failure this section exists to exclude.

## D-03 absorb-or-halt disposition

**`zero-findings`.** There is nothing to absorb and nothing to halt for. The ~10 ceiling was never
approached; the count is **0**.

- **Not `absorb`** — no channels were found, so no unplanned ports were added to this phase after
  its live gate ran. The "unit-proven only, never live-observed" caveat that `absorb` would have
  required does **not** apply to anything here.
- **Not `halt`** — no phase insertion before Phase 35 is warranted on this evidence.

**No assertion in the gate was deleted or weakened to make it pass** — the script's own stated
prohibition. The gate is byte-unchanged by this plan; `git diff` against it is empty.

## What this does and does not license

**Does:** Phase 35's completeness precondition is satisfied *for the preload-derived surface* — the
claim is now derived at a known commit on a known date, and re-derivable by re-running one script.

**Does NOT:** this measures the **preload surface**, i.e. what the renderer can reach through
`window.api`. It says nothing about whether each ported channel *behaves* correctly — that is the
live gate's job, and this phase's live gate closed `FAIL 7/9`. In particular
`runWineCommandForGame` is bucketed and ported yet has **zero renderer call sites**, so it counts
here while being exercised by no gate at all. Coverage of the surface is not coverage of behaviour.
