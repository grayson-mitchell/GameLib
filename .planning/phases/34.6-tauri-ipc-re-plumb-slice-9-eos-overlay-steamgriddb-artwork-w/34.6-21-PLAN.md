---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 21
type: execute
wave: 4
depends_on: ["34.6-20"]
gap_closure: true
files_modified:
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VERIFICATION.md
autonomous: false
requirements: [REQ-34.6-05]

must_haves:
  truths:
    - "`34.6-LIVE-GATE.md` carries an APPENDED, clearly-marked gap-cycle-2 section with THREE new scored items (G2-1, G2-2 ACCEPT-direction; G2-3 REJECT-direction); its closed `verdict: FAIL 7/9` and every one of its nine original items are untouched and unre-scored"
    - "Each new item is authored with an EMPTY Result slot BEFORE any drive, per D-12's contract-first rule"
    - "The `moveInstall` item distinguishes a path-validation rejection from the SEPARATE, already-filed macOS `openrsync` flag failure — a run that dies on rsync is NOT allowed to launder this item into 'untestable'"
    - "G2-3 — the REJECT direction — carries a recorded, non-blank result of its own, so the appended section is proven in BOTH directions at the live surface and not only in the ACCEPT direction the fix widened; a `NOT ATTEMPTED` is acceptable ONLY with a named blocking reason and the named substitute evidence"
    - "REQ-34.6-05's checkbox reflects the MEASURED live outcome, whatever it is — not the outcome the gap cycle hoped for"
    - "REQ-34.6-05's checkbox is written by a BRANCH on the measured verdict: `[x]` only if G2-1 and G2-2 both PASSED, `[ ]` plus a new `resolves_phase: 34.6`-carrying todo otherwise"
  artifacts:
    - path: ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md"
      provides: "an appended gap-cycle-2 section with THREE contract-first scored items — G2-1/G2-2 ACCEPT-direction, G2-3 REJECT-direction — each with a recorded, non-blank result, plus a `gap_cycle_2_verdict` fraction that never rounds up"
      contains: "GAP CYCLE 2"
    - path: ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VERIFICATION.md"
      provides: "an appended, dated gap-cycle-2 addendum in the Gaps Summary recording that the CR-01 code fix landed and what the live re-drive returned — with `status:` and `gaps:` frontmatter byte-unchanged, since a re-verification run owns those"
  key_links:
    - from: ".planning/phases/34.6-*/34.6-LIVE-GATE.md"
      to: "the cross-root moveInstall / importGame drive"
      via: "an appended section, never an amendment to a scored item"
      pattern: "GAP CYCLE 2"
    - from: ".planning/phases/34.6-*/34.6-LIVE-GATE.md"
      to: "the REJECT-direction drive (item G2-3)"
      via: "a filled Result slot recording the visible rejection dialog, or a named NOT ATTEMPTED reason"
      pattern: "G2-3"
---

<objective>
`34.6-VERIFICATION.md`'s third `missing` bullet: "A live-gate/manual test exercising
`moveInstall` to a path outside `defaultInstallPath` and `importGame` from a path outside it, so
this class of regression is caught before the next live gate." Neither channel was EVER driven
against an out-of-root path — that absence is why the phase's live gate returned PASS on both
while the guard was rejecting their primary input.

Purpose: prove the fix against a real filesystem and a real picker, not only against jest mocks,
and leave a gate item behind so the next regression of this class is caught by a run rather than
by a code review.
Output: two appended, contract-first gate items; a recorded per-item verdict; and REQ-34.6-05
closed or re-blocked on the strength of that verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VERIFICATION.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-18-PLAN.md
@.planning/todos/pending/2026-08-24-move-game-is-broken-on-macos-rsync-flags-openrsync-rejects.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append a gap-cycle-2 section to `34.6-LIVE-GATE.md` with two contract-first items and EMPTY result slots</name>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>

  <read_first>
    - `.planning/phases/34.6-.../34.6-LIVE-GATE.md` — the frontmatter (`status: complete`, `verdict: FAIL 7/9`, `failing_items: [4, 8]`, `final_adjudication`, `step_8_decision`), the `## Scoring rules` section (~:352), the `## Scored items` preamble that mandates the What-it-proves / How-to-drive / PASS / FAIL / Evidence-capture / empty-Result shape, and the closing statement at `:1812`, whose exact text is `**This document is now closed.**` — note the `is`; a grep for `This document now closed` matches NOTHING in this file and would make any ordering check undefined. The existing document's SUPERSEDES convention is for amending an item whose OWN text changed — that is NOT what this task does.
    - `.planning/todos/pending/2026-08-24-move-game-is-broken-on-macos-rsync-flags-openrsync-rejects.md` — the exact failure signature of the macOS `openrsync` rejection (`moveOnUnix` probes `which rsync` for existence, not capability; `--no-human-readable` / `--info=name,progress` are rejected; a working `mv -f` fallback exists in the same function and is never reached). You need the exact observable so the new item can distinguish it from a path-validation rejection.
    - `.planning/phases/34.6-.../34.6-18-PLAN.md`'s `<decision>` and `<threat_model>` — so the PASS conditions test the property the fix actually delivers.
    - `.planning/phases/34.6-.../34.6-19-PLAN.md` — the three `gamelib:installFlows.*` strings, so the negative item can name the dialog text the operator should see.
  </read_first>

  <action>
    Append — never amend, never re-score, never regenerate — a new final section to
    `34.6-LIVE-GATE.md`, headed exactly `## GAP CYCLE 2 — appended items (2026-08-25, post-CR-01
    fix)`. Open it with a three-sentence preamble stating: that the document's closed
    `verdict: FAIL 7/9` and all nine original scored items are UNTOUCHED and are NOT re-scored by
    anything below; that these two items are NEW, added because `34.6-VERIFICATION.md` found the
    original gate never drove either channel against an out-of-root path; and that they carry
    their own separate mini-verdict which does not alter the 7/9. Leave the frontmatter's
    `verdict:` / `failing_items:` / `status:` fields exactly as they are; add ONE new frontmatter
    key, `gap_cycle_2_verdict:`, initialised to the empty string, so the new items' outcome is
    machine-readable without touching the closed one.

    Author two items in the document's existing shape (What it proves / How to drive it / PASS
    condition / FAIL condition / Evidence capture / `**Result:**` left EMPTY):

    **Item G2-1 — `moveInstall` to a destination OUTSIDE `defaultInstallPath`.**
    - *What it proves:* the CR-01 regression is gone at the real picker, not only at the mock —
      a destination the user picks on a different volume reaches the runner instead of being
      rejected before any filesystem call.
    - *How to drive it:* use a small fixture game (this project has measured a resume-over-
      existing-content move at seconds, not the "90 GB" a naive reading implies). Confirm
      Settings' Default Install Path first and record it verbatim. Then Game page → Move Game →
      pick a destination on a DIFFERENT volume (e.g. an external disk or a disk image mounted
      under `/Volumes/`), one that is provably not nested under the recorded default. Capture
      `gamelib.log` from the moment the picker closes.
    - *PASS condition:* the log shows the move ATTEMPTED — the runner's own move path is
      entered — and NO `moveInstall: rejected path` line appears, and no
      `gamelib:installFlows.pathRejectedTitle` dialog is shown. **The move itself completing is
      NOT required for PASS.**
    - *FAIL condition (concrete, named, and disambiguated — this is the load-bearing part):*
      - **FAIL-A (this item's own failure):** a `moveInstall: rejected path` log line, or the
        "Can't use that location" dialog, appears. That is the CR-01 class regressing.
      - **NOT this item's failure — the separate macOS blocker:** the move is attempted and then
        errors with an `openrsync`/unknown-option signature (`--no-human-readable` or
        `--info=name,progress` rejected). That outcome is **PASS for G2-1** and is recorded
        additionally as a confirmation of
        `.planning/todos/pending/2026-08-24-move-game-is-broken-on-macos-rsync-flags-openrsync-rejects.md`,
        which stays open. State explicitly, in the item text, that the rsync blocker MUST NOT be
        used to mark this item NOT ATTEMPTED or untestable: the property under test is whether
        the path was ACCEPTED, and that is decided before rsync is ever invoked.
      - **NOT ATTEMPTED is available only if** no second volume can be mounted at all — and in
        that case the operator must say so in the Result slot rather than leaving it blank.
    - *Evidence capture:* the recorded `defaultInstallPath` value; the picked destination path;
      the `gamelib.log` excerpt spanning picker-close to the first move-related line; and a note
      of which of FAIL-A / rsync-signature / clean-completion occurred.

    **Item G2-2 — `importGame` from a source OUTSIDE `defaultInstallPath`.**
    - *What it proves:* the same, source-side; and additionally that the negative direction still
      works, so the item is not a one-directional pass.
    - *How to drive it:* place (or identify) an already-installed game folder on a volume outside
      the recorded default install path. Library → Import → point `PathSelectionBox` at it →
      Import. Note the `pickFile` distinction: for a macOS target the picker is in FILE mode and
      a `.app` bundle is invisible in directory mode — choose the platform selector FIRST.
    - *PASS condition:* the import is ATTEMPTED — the runner's `importGame` is entered — with no
      `importGame: rejected path` line and no rejection dialog. As with G2-1, a downstream
      product failure (for example the already-filed GOG identity-validation gap) is NOT a
      failure of this item.
    - *FAIL condition:* an `importGame: rejected path` log line or the "Can't use that location"
      dialog appears.
    - *Evidence capture:* the source path used; the `gamelib.log` excerpt; the observed outcome.

    **Item G2-3 — the negative direction, so this appended section is not one-directional.**
    Drive ONE deliberately malformed path through the same UI surface if the picker permits it
    (for example by typing a relative path into `PathSelectionBox` on the import flow rather than
    using the browse button), and assert the rejection IS visible: the "Can't use that location"
    dialog appears AND the rejected path does not appear in `gamelib.log`. If the UI makes a
    malformed value impossible to submit, record `NOT ATTEMPTED — UI cannot express the input`
    with the reason, and note that the primitive-level and channel-level REJECT coverage from
    plan 34.6-18 stands as the substitute evidence. Do not silently omit the item.

    Do NOT run anything in this task. The section is authored with empty Result slots and
    committed BEFORE any drive — that is D-12's contract-first rule and the reason the original
    gate's evidence is legible at all.
  </action>

  <verify>
    <automated>bash -c "grep -c 'GAP CYCLE 2' .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md"</automated>
  </verify>

  <acceptance_criteria>
    - The appended section exists and is the LAST section, checked against the document's ACTUAL closing text: `grep -n 'This document is now closed' .planning/phases/34.6-.../34.6-LIVE-GATE.md` returns exactly one line (it is line 1812 at HEAD, and the literal contains `is` — `This document now closed` matches nothing and would leave this check undefined), and `grep -n '## GAP CYCLE 2' <same file>` returns exactly one line whose number is GREATER than it. Verify BOTH greps return a match before comparing; a comparison against an empty result is not a check.
    - Nothing was removed or re-scored: `git diff -U0 .planning/phases/34.6-.../34.6-LIVE-GATE.md | grep -cE '^-[^-]'` returns `0`. A single deleted line in a closed gate document is a re-score; there must be none.
    - The closed verdict is intact: `grep -c '^verdict: FAIL 7/9$' .planning/phases/34.6-.../34.6-LIVE-GATE.md` returns `1`, and `grep -c '^failing_items: \[4, 8\]$' <same file>` returns `1`.
    - The new machine-readable slot exists and is empty: `grep -cE '^gap_cycle_2_verdict: *""?$' <same file>` returns `1`.
    - All three items have EMPTY result slots before any drive: `grep -c '\*\*Result:\*\*' <same file>` increased by exactly `3`, and each of the three new occurrences is followed by no content on its own line.
    - The rsync confound is called out by name, not merely implied: `grep -c 'openrsync' <same file>` returns at least `1`, AND the G2-1 item contains the literal phrase `MUST NOT be used to mark this item NOT ATTEMPTED`.
    - The section is proven non-vacuous in both directions by construction: it contains at least one item whose PASS condition is an ACCEPT (G2-1/G2-2) and at least one whose PASS condition is a REJECT (G2-3).
  </acceptance_criteria>

  <done>The gate contract for the never-tested case exists, appended and empty, without disturbing a single scored item of the closed FAIL 7/9 run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive the three appended live-gate items and record the measured results</name>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>

  <read_first>
    - The `## GAP CYCLE 2` section authored in Task 1 — the full text of all three items, including their disambiguation rules. Read it before touching the app; the whole point of contract-first is that the bar is fixed before the run.
    - `.planning/phases/34.6-.../34.6-LIVE-GATE.md`'s `## Preconditions` section (~:74) — the build/launch preconditions the original run used.
  </read_first>

  <what-built>
    Plan 34.6-18 replaced `moveInstall`/`importGame`'s `defaultInstallPath` containment with
    `assertPlausibleAbsolutePath` (absolute, no `..` segment, no NUL, non-empty, string). Plan
    34.6-19 made any remaining rejection produce a visible "Can't use that location" dialog.
    Both are proven at the unit level, including a RED-before-fix capture. Neither has ever been
    driven against a real OS picker returning a real out-of-volume path — which is exactly the
    coverage hole that let CR-01 ship.
  </what-built>

  <how-to-verify>
    1. Build and launch the Tauri app with `pnpm tauri:dev` (NOT `tauri dev` — that serves a
       stale static bundle). Note that `pnpm tauri:dev` exits 0 WITHOUT replacing an already
       running instance, and `pgrep GameLib` misses the process name `gamelib-shell` — check for
       `gamelib-shell` before assuming a fresh instance.
    2. Open Settings and record the exact `Default Install Path` value. Every later judgement is
       relative to this string; write it down verbatim before doing anything else.
    3. Mount or identify a second volume under `/Volumes/` that is provably not nested under
       that path (an external disk, or a disk image created for the purpose).
    4. Drive **G2-1**: Game page → Move Game → pick a destination on that second volume. Watch
       for either (a) the move being attempted, (b) a "Can't use that location" dialog, or
       (c) an `openrsync` / unknown-option error AFTER the move is attempted. Outcomes (a) and
       (c) are both PASS for this item; only (b) is FAIL. Capture the `gamelib.log` excerpt.
    5. Drive **G2-2**: choose the platform in the install modal FIRST, then Library → Import →
       point at an already-installed game folder on that second volume → Import. Capture the log.
    6. Drive **G2-3**: attempt to submit a malformed path (e.g. type a relative path into the
       import path field instead of browsing). Confirm the "Can't use that location" dialog
       appears AND that `gamelib.log` does NOT contain the rejected path string. If the UI will
       not let you submit it, record `NOT ATTEMPTED` with that reason.
    7. Fill each item's `**Result:**` slot with the observed outcome, the captured log excerpt,
       and — for G2-1 — which of the three outcome classes occurred. A blank slot is not a pass.
  </how-to-verify>

  <acceptance_criteria>
    - All three `**Result:**` slots in the `## GAP CYCLE 2` section are non-empty, each naming a PASS / FAIL / NOT ATTEMPTED verdict and citing a concrete observation, not a restatement of the PASS condition.
    - G2-1's result explicitly states which outcome class occurred (move attempted cleanly / move attempted then `openrsync` error / rejection dialog). If the `openrsync` class occurred, the result must ALSO say that item G2-1 is PASS and that the rsync todo remains open — the confound must be recorded, never used to blank the item.
    - The recorded `Default Install Path` value and the actual destination/source paths used are written into the results, so a later reader can independently confirm the paths really were out-of-root.
    - `gap_cycle_2_verdict:` in the frontmatter is filled with an honest fraction in the document's own style (e.g. `PASS 3/3`, or `PASS 2/3, 1 NOT ATTEMPTED`) — never rounded up, and never `PASS n/n` unless every scored item passed.
  </acceptance_criteria>

  <action>
    Drive the three `## GAP CYCLE 2` items exactly as their contract text specifies, then fill
    each `**Result:**` slot in `34.6-LIVE-GATE.md` and set the `gap_cycle_2_verdict:` frontmatter
    key to the honest fraction. Do not edit any item's PASS/FAIL condition text while running it
    — a contract adjusted to fit an observation is not a contract. If an item cannot be driven,
    write `NOT ATTEMPTED` with the blocking reason in its slot rather than leaving it blank.
    Do not touch the closed `verdict: FAIL 7/9` frontmatter or any of the nine original items.
  </action>

  <resume-signal>Type "approved" with the three results, or describe what you observed instead.</resume-signal>

  <verify>
    <automated>bash -c "python3 -c \"import re,sys; p='.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md'; s=open(p).read(); m='## GAP CYCLE 2'; sys.exit(2) if m not in s else None; sec=s.split(m,1)[-1]; slots=re.findall(r'\\*\\*Result:\\*\\*(.*)', sec); sys.exit(0 if len(slots)==3 and all(x.strip() for x in slots) else 1)\""</automated>
  <!-- The earlier form, `s.split('GAP CYCLE 2')[1]`, took the slice BETWEEN the first and second
       occurrences of the bare string. The heading plus the preamble's own required mention of
       "GAP CYCLE 2" put both occurrences before the first item, so that slice contained ZERO
       `**Result:**` matches and the check FALSE-FAILED a correctly-filled document — on a
       BLOCKING human gate. Splitting on the heading `## GAP CYCLE 2` with `maxsplit=1` and
       taking `[-1]` takes everything from the heading to end-of-file, and the `m not in s`
       guard exits 2 rather than silently scanning the whole document. -->
  </verify>

  <done>All three appended items carry a recorded verdict and a concrete observation, `gap_cycle_2_verdict` is filled with an honest fraction, and the closed FAIL 7/9 record is byte-untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measured outcome into REQUIREMENTS.md and the ported-channels ledger</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md, .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VERIFICATION.md</files>

  <read_first>
    - The filled `## GAP CYCLE 2` results from Task 2.
    - `.planning/REQUIREMENTS.md` line 846 — REQ-34.6-05 as plan 34.6-20 left it (checkbox `- [ ]`, closure explicitly pending this drive).
    - `.planning/phases/34.6-.../34.6-PORTED-CHANNELS.md` — the `moveInstall` / `importGame` rows as plan 34.6-20 left them, currently declaring proof level `unit` for the new control.
  </read_first>

  <action>
    Branch on the MEASURED result — do not pre-decide which branch runs.

    **If G2-1 and G2-2 both PASSED:** flip REQ-34.6-05's checkbox to `- [x]` and append a
    closing sentence naming the live evidence: the `34.6-LIVE-GATE.md` gap-cycle-2 items, their
    verdict fraction, and the recorded out-of-root paths. Upgrade both
    `34.6-PORTED-CHANNELS.md` rows' proof level for the path-validation control from `unit` to
    `unit + LIVE (gap-cycle-2 item G2-1 / G2-2)`, citing the specific item — the ledger's own
    convention is that a LIVE claim names its gate item.

    **If either FAILED:** leave REQ-34.6-05 at `- [ ]`, append the measured failure and its
    observable, and file a new todo under `.planning/todos/pending/` carrying
    `resolves_phase: 34.6` from the moment it is created (this phase has already been bitten
    three times by a todo with no `resolves_phase`). Do NOT upgrade the ledger's proof level.
    Do NOT re-run the gate to get a different answer inside this plan.

    **If G2-3 was NOT ATTEMPTED:** record that in both places as a named, non-vacuous outcome
    with the substitute evidence (plan 34.6-18's primitive-level and channel-level REJECT
    coverage), rather than letting it disappear from the reckoning.

    In every branch, add one line to `34.6-VERIFICATION.md`'s Gaps Summary section — appended,
    clearly dated and marked as a gap-cycle-2 addendum — recording that the CR-01 gap's code fix
    landed and what the live re-drive returned. Do not edit that document's `gaps:` frontmatter
    or change its `status:` field; a re-verification run owns that, not this plan.
  </action>

  <verify>
    <automated>bash -c "grep -cE '^- \[(x| )\] \*\*REQ-34.6-05\*\*' .planning/REQUIREMENTS.md"</automated>
  </verify>

  <acceptance_criteria>
    - REQ-34.6-05's checkbox state matches the recorded verdict, provably: if `grep -c 'PASS' <the G2-1 Result slot>` indicates PASS for both G2-1 and G2-2, then `grep -c '^- \[x\] \*\*REQ-34.6-05\*\*' .planning/REQUIREMENTS.md` returns `1`; otherwise it returns `0` and `grep -c '^- \[ \] \*\*REQ-34.6-05\*\*'` returns `1`. State in the SUMMARY which branch ran and quote the Result text that decided it.
    - The requirement entry cites the live item by ID: `sed -n '846p' .planning/REQUIREMENTS.md | grep -cE 'G2-1|G2-2'` returns at least `1`.
    - Proof-level upgrades are gated on the verdict: `grep -c 'gap-cycle-2 item G2' .planning/phases/34.6-.../34.6-PORTED-CHANNELS.md` returns `2` on the PASS branch and `0` on the FAIL branch — never a LIVE claim without a passing item behind it.
    - On the FAIL branch only, a new todo exists under `.planning/todos/pending/` containing `resolves_phase: 34.6` in its frontmatter.
    - `34.6-VERIFICATION.md`'s `status:` and `gaps:` frontmatter are byte-unchanged: `git diff -U0 .planning/phases/34.6-.../34.6-VERIFICATION.md | grep -cE "^[+-](status|gaps|  - truth):"` returns `0`.
    - `pnpm planning-gates` shows NO NEW failure. It is already red at HEAD on ONE pre-existing, unrelated gate — `.planning/phases/34.4.1-*/seam-parity-sweep-gate.py`, whose committed `34.4.1-SEAM-PARITY-SWEEP.md` went stale when plan 34.6-09 wired `steamgrid` into a sidecar registration module (the sweep's own prose predicted exactly that trigger). It is OUT of this gap cycle's scope. The bar here: the run must report `6/7` with that same gate as the only failure and its diff unchanged from HEAD's. If a SECOND gate goes red, or that diff changes, stop — this cycle caused it.
  </acceptance_criteria>

  <done>REQ-34.6-05 and the proof ledger say exactly what the live run measured — in either direction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → closed gate document | a live run's outcome enters a document whose prior verdict is already adjudicated and must not shift |
| a confounding, separately-owned defect → this item's verdict | the macOS `openrsync` blocker sits on the same feature and can masquerade as "untestable" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-59 | Repudiation | the appended section re-scoring, re-scoping or silently amending the closed `FAIL 7/9` verdict | mitigate | Append-only, pinned by a `git diff -U0` of the file piped into `grep -cE '^-[^-]'` returning `0`, plus exact-match greps that `verdict: FAIL 7/9` and `failing_items: [4, 8]` survive. The new outcome lands in a NEW frontmatter key, `gap_cycle_2_verdict`. |
| T-34.6-60 | Repudiation | the macOS `openrsync` blocker laundering item G2-1 into NOT ATTEMPTED / untestable, so the property under test is never actually decided | mitigate | The item text names the confound, states that the rsync signature is a **PASS** for G2-1 (because path acceptance is decided before rsync is invoked), and carries the literal phrase `MUST NOT be used to mark this item NOT ATTEMPTED`. Task 2's acceptance criteria require the result to name which outcome class occurred. |
| T-34.6-61 | Repudiation | a blank `**Result:**` slot reading as a pass | mitigate | Task 2's acceptance criteria require all three slots non-empty with a named verdict and a concrete observation; `gap_cycle_2_verdict` must be an honest fraction and may not be `PASS n/n` unless every scored item passed — the original document's own scoring rule. |
| T-34.6-62 | Repudiation | REQ-34.6-05 being ticked on the strength of the unit tests alone, before the drive | mitigate | Plan 34.6-20 deliberately leaves the checkbox `- [ ]`; Task 3 here branches on the MEASURED result and its acceptance criterion requires the checkbox state to match the recorded verdict in both directions. |
| T-34.6-63 | Repudiation | the gate section being one-directional (only ACCEPT cases), the exact shape that let CR-01 through | mitigate | Item G2-3 drives the REJECT direction at the same UI surface, and if the UI cannot express the malformed input that is recorded as NOT ATTEMPTED with named substitute evidence, never omitted. |
| T-34.6-64 | Information Disclosure | a live run's captured `gamelib.log` excerpt carrying a real user path into a public-fork planning document | mitigate | Evidence capture is scoped to the specific move/import lines; G2-3's own PASS condition independently asserts the rejected path is ABSENT from the log. If a captured excerpt contains an unrelated personal path, redact it in the Result slot and say that it was redacted. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | **No packages are installed by this plan.** The only commands are `pnpm tauri:dev` (an existing script) and documentation edits. |
</threat_model>

<verification>
- `git diff -U0 .planning/phases/34.6-.../34.6-LIVE-GATE.md | grep -cE '^-[^-]'` returns `0` after BOTH Task 1 and Task 2.
- `grep -c '^verdict: FAIL 7/9$' .planning/phases/34.6-.../34.6-LIVE-GATE.md` returns `1`.
- All three `## GAP CYCLE 2` `**Result:**` slots are non-empty after Task 2, sliced with `s.split('## GAP CYCLE 2', 1)[-1]` — never `s.split('GAP CYCLE 2')[1]`, which takes the span between the heading and the preamble's own mention and therefore contains zero slots.
- G2-3's slot in particular carries a recorded REJECT-direction verdict (or a named `NOT ATTEMPTED` reason plus its substitute evidence): `grep -A3 'G2-3' <the live gate file>` shows a non-blank `**Result:**`.
- `grep -c 'This document is now closed' .planning/phases/34.6-.../34.6-LIVE-GATE.md` returns `1` and the `## GAP CYCLE 2` heading's line number is greater than that line's.
- REQ-34.6-05's checkbox state matches the recorded verdict.
- `pnpm planning-gates` shows NO NEW failure. It is already red at HEAD on ONE pre-existing, unrelated gate — `.planning/phases/34.4.1-*/seam-parity-sweep-gate.py`, whose committed `34.4.1-SEAM-PARITY-SWEEP.md` went stale when plan 34.6-09 wired `steamgrid` into a sidecar registration module (the sweep's own prose predicted exactly that trigger). It is OUT of this gap cycle's scope. The bar here: the run must report `6/7` with that same gate as the only failure and its diff unchanged from HEAD's. If a SECOND gate goes red, or that diff changes, stop — this cycle caused it.
</verification>

<success_criteria>
- The case that was never tested is now a standing, appended gate item with a fixed bar and a recorded result.
- The closed FAIL 7/9 verdict and all nine original items are byte-untouched.
- The macOS rsync blocker is distinguishable from a containment rejection by construction, and cannot blank the item.
- The appended section is scored in BOTH directions: G2-1/G2-2 record the ACCEPT case the fix widened, and G2-3 records the REJECT case that proves the widening is not a removal.
- REQ-34.6-05 closes on measurement, or stays open with a new `resolves_phase`-carrying todo.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-21-SUMMARY.md` when done.
State which Task 3 branch ran and quote the Result text that decided it. Record G2-3's outcome explicitly — including, if it was `NOT ATTEMPTED`, the named blocking reason and the substitute evidence — so the REJECT direction never disappears from the reckoning.
Commit Task 1 (the empty contract) SEPARATELY from Task 2/3 (the filled results) — a contract and its own result in one commit is indistinguishable from a contract written to fit the result. Use `git commit --only <explicit paths>`. Never use `git stash`, `git reset`, or `git checkout -- <file>`.
</output>
