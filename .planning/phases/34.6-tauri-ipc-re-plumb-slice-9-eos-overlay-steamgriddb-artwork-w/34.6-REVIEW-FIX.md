---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
reviews: 34.6-REVIEW.md
status: resolved
completed: 2026-08-26
findings_total: 4
fixed: 4
deferred: 0
---

# Phase 34.6 — Review Fix Pass

Dispositions for all four findings in `34.6-REVIEW.md` (reviewed 2026-08-25T09:32:09Z). That file
records what was **found** and is deliberately not rewritten; this sibling records what was
**done**.

| ID | Severity | Disposition | Landed | Evidence |
|---|---|---|---|---|
| CR-01 | Critical | **FIXED** | 34.6-18 / 34.6-19 | `defaultInstallPath` containment dropped from `moveInstall`/`importGame` in favour of `rendererPathGuard.assertPlausibleAbsolutePath` (shape only) + a user-visible rejection dialog. Live-proven by `34.6-LIVE-GATE.md` gap-cycle-2 items G2-1/G2-2 (accept) and G2-3 (reject), `gap_cycle_2_verdict: "PASS 3/3"`. Independently re-tested by `34.6-VERIFICATION.md` run 2 (`status: passed`), which measured that `assertContainedPath` has **zero production callers**. |
| WR-01 | Warning | **FIXED** | quick `260826-w4c` | `typeof key !== 'string'` guard added at **both** IPC boundaries — `sidecar/enrichmentFlowRegistration.ts` (the cast the review named) **and** `steamgrid/ipc_handler.ts`, which had the same hole. Rejects with a warning and a no-op before any keyring round trip. RED-proven: 4 cases (`undefined`, number, `null`, object) fail against the restored bare cast. |
| WR-02 | Warning | **FIXED** | quick `260826-w4c` | `SidecarSteamGridDbSecretStore.setApiKey()` now mirrors the Electron path exactly — trims, and treats a whitespace-only value as an explicit clear (`keyring_delete`, not a `keyring_set` of `'   '`). RED-proven: all 3 new tests fail against the restored pre-fix body. |
| WR-03 | Warning | **FIXED** | quick `260826-w4c` | `SearchBar/index.tsx` keeps the 4-step focus-race description **and** the `preventDefault` handler — the mechanism is real. The unsupported causal claim is retracted in place: the comment now records that plan 34.6-17 shipped this guard and the button stayed dead, and that the 2026-08-26 Step 4 re-drive passed by a different route. "DO NOT REMOVE" / "Proven by measurement" removed. |

## Why `status: resolved` and not `partial`

All four are genuinely closed in source — none deferred, none re-homed to a todo. The two
behavioural fixes are RED-proven against restored pre-fix code, not merely green after the change.

One test is deliberately **not** counted among the RED-proven: WR-01's fifth case (an empty string
still reaching the store) passes in both directions **by design**, because it pins that the guard
tests the TYPE rather than truthiness — a truthiness check would have silently broken the "submit
an empty value to clear the key" gesture. Counting it would inflate the proof.

## Verification at the time of this pass

`pnpm codecheck` exit 0 · `enrichmentFlows` + `steamgridSecretStore` **61/61** · SearchBar 2/2 ·
eslint **0 errors** across all six touched files · prettier clean, measured in place.

## Note on this file's existence

An earlier attempt fixed the red badge by editing `34.6-REVIEW.md`'s own `status:` to `resolved`.
That was the wrong repair: a REVIEW's `status:` is stale by design and pairs with this sibling the
way PLAN pairs with SUMMARY, so rewriting it destroys the record of what was found. The review has
been restored to its found-state (`issues_found`, `critical: 1`, `warning: 3`) and the dispositions
live here instead. Attribution proven in three directions against the real `parse.js`:
`reviewStatus(found, 'resolved')` → `complete`, `reviewStatus(found, null)` → `blocked`,
`reviewStatus(found, 'partial')` → `inprogress`.
