---
phase: 34.6
plan: 12
type: live-gate
status: pending
blocking: true
run: 1
supersedes_items: []
regression_reruns: []
new_observations: []
created: 2026-08-23
completed: null
verdict: null
---

# Phase 34.6 — Blocking Live Gate (run 1)

> Written with EMPTY result slots BEFORE any live work.

This contract is authored by plan `34.6-03`. Per this project's standing rule (decision D-E in
`Skill("spike-findings-gamelib")` § `references/live-gate-contract-authoring.md`), **the plan that
authors a gate contract may never also run it.** Steps 1-8 and the Humble rider are run by plan
`34.6-12`; step 0 is run earlier, by plan `34.6-04`, on a build that deliberately predates the
winetricks port — see Step 0 below for why that ordering is load-bearing and irreversible.

## Why this gate exists

This is the **last** IPC re-plumb slice. The gate closes two kinds of work in one document:

1. **24 newly-ported sidecar channels** — 8 EOS overlay, 5 SteamGridDB artwork, 3 winetricks, and 8
   late-discovered channels (`frontendReady`, `getAchievements`, `getDefaultSavePath`,
   `getGogDiscounts`, `getPlaytimeFromRunner`, `importGame`, `moveInstall`,
   `runWineCommandForGame`) — the full count corrected by D-01 from the ROADMAP's stale "16".
2. **Four inherited verification legs re-homed from the parked Phase 34.7** — the Epic login half of
   34.5 UAT test 11, test 12's `egsSync`, test 13's legendary save sync, and Epic logout's
   `clearEpicCookies`. These exercise already-ported code; they add no channel-porting work of their
   own.

**D-12: one document, one verdict, no partial-pass mode.** Two prior gates in this project
(`34.4.1-35-PLAN.md`, `34.4.2-VERIFICATION.md`) both went through a rerun caused by a defect in the
*contract*, not in the code the contract was measuring:

- `34.4.1-35-PLAN.md` precondition 6 (**still UNSTRUCK**) records a gate item whose PASS condition
  was satisfiable by an empty starting state — `survivingNonHumble=0` was accepted when
  `before total=34` equalled `matched=34`, so the census had no non-Humble cookie left to spare. The
  number was arithmetically forced, not passing. Nothing in that run could have failed the item, so
  the item proved nothing.
- `34.4.2-VERIFICATION.md` records **nine** separate gate attempts, each amended in place rather than
  regenerated, because a contract written after the fact cannot be checked for the defect class above
  and a regenerated document destroys hand-corrections layered onto prior amendments.

A contract with empty result slots, authored before any live work and before this phase's own port
lands, cannot exhibit either defect — there is no result to reason backward from while writing it.
That is the entire purpose of this document existing as a separate, earlier-committed artifact from
the plan that runs it.

---

## Preconditions

Each precondition states **exact measured state**, not attestation. The `Measured:` slot is blank —
filled only by the plan that actually runs the corresponding step.

1. **Build freshness.** `pnpm tauri:dev` exits 0 **without** replacing an already-running instance,
   and `pgrep GameLib` **misses** the `gamelib-shell` process name — a whole UAT in this project once
   ran on an 8-hour-old build without anyone noticing. Record the exact command used to prove the
   running binary was built from the commit under test (e.g. `pgrep -fl gamelib-shell` plus the
   binary's mtime against `git log -1 --format=%cI`), and record the commit SHA the run executed
   against.
   **Measured:** (recorded by plan `34.6-04`, task 1, 2026-08-23) The documented hazard reproduced
   live on the first attempt: `pnpm tauri:dev` (LAUNCH 1, delimiter `=== GATE LAUNCH 1 —
   2026-08-23T13:13:07Z ===` in the session scrollback) rebuilt the binary but printed
   `[shell] another GameLib instance is already running -- sending focus sentinel to it and
   exiting` and exited 0, leaving a PRE-EXISTING stale process running (`pgrep -fl gamelib-shell` →
   PID 60016, `ps -p 60016 -o lstart` → `Mon Aug 24 01:13:24 2026`, i.e. that process predated
   this launch and was running code older than the commit under test). That stale instance was
   terminated with `kill -TERM 60016` (confirmed gone: `pgrep -fl gamelib-shell` exit 1 after 5s).
   LAUNCH 2 (delimiter `=== GATE LAUNCH 2 —` in the scrollback) was then run with zero competing
   instances and started cleanly: `pgrep -fl gamelib-shell` → PID 60732, `ps -p 60732 -o lstart` →
   `Mon Aug 24 01:30:02 2026`; the binary `src-tauri/target/debug/gamelib-shell` has mtime
   `Aug 24 01:30:06 2026` (`stat -f "%Sm %N"`). Both the process start and the binary mtime
   postdate the commit under test's own commit timestamp (`2026-08-24T00:53:53+12:00` — same
   local day, 36 minutes earlier). The running instance is confirmed built from the commit under
   test, not a stale one.
   **Commit SHA:** `9245195b66e21c9b3f3f1d6b1235e03a5bb81698`

2. **Log capture standard.** Dual-sink append-and-archive, per the spike reference's Section 3
   standard: shell scrollback captured with `tee -a` into a single session-scoped file, with a
   per-launch delimiter line written before each launch (`=== GATE LAUNCH ${N} — <UTC timestamp>
   ===`), **and** `~/Library/Logs/GameLib/gamelib.log` archived to a run-scoped copy **before** the
   run — because `eprintln!` in the Rust shell goes to stderr and **never** reaches `gamelib.log`
   (this is folded todo 2's own root cause: `gamelib.log` alone cannot settle an `eprintln!`-only
   observation). A clean grep of the wrong sink is not evidence of absence — it is evidence of
   nothing. Archive `gamelib.log` before the next launch's first write renames it to `.old`
   (`src/backend/logger/log_writer.ts:72-74`).
   **Measured:** (recorded by plan `34.6-04`, task 1, 2026-08-23) Pre-run `gamelib.log` archived
   BEFORE any launch to `~/Library/Logs/GameLib/gamelib.log.pre-346-04-20260823T131307Z` (1801
   bytes at archive time). Shell scrollback captured via `tee -a` into a single session-scoped file
   for both launch attempts, with per-launch delimiter lines
   (`=== GATE LAUNCH 1 — 2026-08-23T13:13:07Z ===`, `=== GATE LAUNCH 2 — <timestamp written at
   second launch> ===`); durable copy at `~/Library/Logs/GameLib/gamelib.scrollback.346-04.log`
   (567 lines at hand-off). `GAMELIB_DEV_SECRET_VAULT` was NOT set for either launch. Note for the
   operator continuing at Task 2: `gamelib.log` renames to `.old` on the NEXT process's first
   write, so the live file to grep at Task 2 time is `~/Library/Logs/GameLib/gamelib.log` as long
   as no additional relaunch happens between this hand-off and the capture.
   **Session directory:** `/private/tmp/claude-501/-Users-graysonmitchell-Projects-GameLib/66e745bf-2e3f-4360-b001-7d5fa617a19a/scratchpad/34.6-04-gate` (session-scoped, ephemeral); durable mirror at `~/Library/Logs/GameLib/gamelib.scrollback.346-04.log`

3. **Secret-store arm.** `grep -c '\[bootstrap\] secret stores: keyring' <archived log>` and
   `grep -c '\[bootstrap\] steamgrid secret store: keyring'` both return at least 1 for the run under
   test. A `GAMELIB_DEV_SECRET_VAULT=1` boot invalidates step 3's Keychain assertion — the run must
   NOT set that env var.
   **Measured:** [pending]

4. **Epic account state.** Logged **OUT** at run start — the cookie jar is sticky across restarts, so
   a scripted login test needs an explicit logout preamble or step 1 tests nothing. **An emptied
   app-side store does not prove a logged-out session** (34.4.2's F-34.4.2-16: a live WKWebView
   cookie jar independently held a session that auto-completed a login with no form to paste into,
   even though the app-side config store was correctly measured empty). Confirm logged-out state by
   a **positive observable** — the embedded/primary Epic login **form actually renders** when the
   login surface is opened, before any credential is entered — never by the absence of app-side data
   alone. If the form does not render (an existing session auto-resumes instead), clear the cookie
   jar and WebKit's WebsiteData store, then re-check, before proceeding to step 1. Name the account
   used.
   **Measured:** [pending]
   **Account:** [pending]
   **Login form rendered before credential entry (Y/N):** [pending]

5. **SteamGridDB API key.** The operator holds a real key at gate time (D-06's stated precondition).
   Record only **that** a key was held, never the key's value — per this contract's own threat
   register, T-34.6-08.
   **Measured:** [pending]

6. **EOS overlay restore point.** Record the operator's legendary/EOS overlay state **before** the
   round-trip (installed? enabled? for which `appName`?) and the exact commands that restore it,
   because D-05's round-trip mutates it (install → enable → disable → remove, ending in an uninstalled
   state regardless of the starting one).
   **Measured:** [pending]
   **Restore commands:** [pending]

7. **Save-sync fixture.** A small owned Epic title **with** cloud saves, named, and its save directory
   **backed up** to a run-scoped copy before step 7. D-14's upload half can **destroy** a save if the
   round-trip goes wrong mid-sync.
   **Measured:** [pending]
   **Fixture title:** [pending]
   **Backup path:** [pending]

8. **Move/import fixture.** A small owned title named, plus the target directory, for step 5's
   `moveInstall`/`importGame`. Resume/move over **existing** content, not a from-scratch install —
   this project has measured a "90 GB" gate at 71.5 seconds when run that way.
   **Measured:** [pending]
   **Fixture title:** [pending]
   **Target directory:** [pending]

9. **Cookie jar starting census.** `before total` and the count of Epic-domain cookies, **measured**
   and recorded, plus the **name and domain** of the foreign (non-Epic) cookie planted for step 8's
   companion step 8 (Epic logout). This is the precondition D-13 exists to enforce — see Step 8 below
   for why a count alone is insufficient.
   **Measured:** [pending]
   **Before total:** [pending]
   **Epic-domain count:** [pending]
   **Planted cookie name/domain:** [pending]

---

## Scoring rules

- **No partial pass** (34.5 D-08). This document produces exactly one verdict.
- Every scored item is **PASS**, **FAIL**, or **NOT ATTEMPTED**.
- A **NOT ATTEMPTED** item is not a pass. It does not count toward the numerator of the verdict
  fraction, and it does not silently disappear from the reckoning either — it is recorded as its own
  outcome.
- The verdict is `PASS n/n` **only** when every scored item passed. Any FAIL or NOT ATTEMPTED item
  makes the verdict something other than `PASS n/n`, stated honestly as that fraction (e.g.
  `PASS 8/10, 1 FAIL, 1 NOT ATTEMPTED`).
- If a rerun is needed, this document is **amended in place** with a clearly marked `## SUPERSEDES`
  section carrying the date and the items it revises — never regenerated. Regenerating destroys any
  hand-corrections layered onto prior amendments, which is exactly what made
  `34.4.2-VERIFICATION.md`'s nine-attempt history legible instead of lost.

---

## Scored items

Every item states **What it proves**, **How to drive it**, **PASS condition**, **FAIL condition** (a
concrete, named observation — not merely "PASS condition not met" restated), **Evidence to
capture**, and an empty **Result:** slot.

### Step 0 — pre-port `DECLARED_UNAVAILABLE_MARKER` capture (D-10 as corrected by A-01)

**Run by plan `34.6-04`, not plan `34.6-12`.** This step must run on a build where the winetricks
sidecar registration has **not yet landed** — it is the one item in this whole document whose
evidence is destroyed by this phase's own success. Once winetricks ports (plan `34.6-07`), there is
nothing left to decline and the marker can never be observed live again.

**What it proves:** that the unported winetricks channel produces
`[GAMELIB_DECLARED_UNAVAILABLE]` — the literal exported as `DECLARED_UNAVAILABLE_MARKER`
(`src/frontend/helpers/declaredUnavailable.ts:43`) — in a real `gamelib.log`, and that this is the
**first live observation of that literal in this project's history** (D-10's own reason for existing:
without this step, the literal ships with zero live observations ever, which is exactly the gap D-10
exists to close).

**How to drive it:** open the Winetricks panel (`src/frontend/components/UI/Winetricks/index.tsx`,
whose two `callOrDeclare` sites are at lines 37 and 59) on a build without the winetricks
registration, and trigger the action that calls a `winetricks*` channel.

**PASS condition:** `grep -c "GAMELIB_DECLARED_UNAVAILABLE" <archived log>` returns at least 1 **AND**
at least one matching line names a `winetricks` channel.

**FAIL condition:** zero matches, **OR** matches that name only `steamgriddb.*` channels — the
SteamGridDB half of `U-34.5-32` retires by port per D-10 and does not discharge the winetricks half;
a SteamGridDB-only match would be a false positive for this item specifically.

**Evidence to capture:** the archived `gamelib.log` excerpt containing the matched line(s); the
commit SHA this capture ran against; confirmation that `winetricksAvailable` is **absent** from
`handlerRegistry` on that build (i.e. the channel really was unported at capture time, not merely
unexercised).

**Build provenance (task 1, plan `34.6-04`, 2026-08-23 — precedes and is separate from the Result
below, which is filled only by task 3 after task 2's live capture):**
- Commit SHA under test: `9245195b66e21c9b3f3f1d6b1235e03a5bb81698` (same commit recorded in
  precondition 1 above).
- `npx jest --selectProjects Backend --testPathPattern "wineToolsFlows" -t "T-34.5-15"` → **PASS**,
  2 passed / 21 skipped / 0 failed, output opens with `Running one project: Backend` (does NOT say
  "no projects found matching selection"). This is the authoritative absence check: both
  `T-34.5-15` assertions directly call `expect(handlerRegistry.has(channel)).toBe(false)` for
  `winetricksAvailable`, `winetricksInstall`, `winetricksInstalled` (and `runWineCommandForGame`),
  and passed.
- `grep -c 'winetricks' src/backend/sidecar/wineToolsFlowRegistration.ts` → **3**, NOT 0 as the
  plan's own acceptance criteria literally states. Recorded as a finding, not silently resolved:
  all 3 matches are in the module's header COMMENT (lines 34-35, describing the three channels as
  belonging to Phase 34.6, not this slice) — none is a channel registration. The jest absence test
  above is the authoritative source of truth for "is the channel registered"; the plan's grep
  acceptance criterion is stale by not excluding comments (this project's own recorded failure
  mode: "a gate literal can be stale by behaviour" / "an audit coarser than the defect's unit
  cannot find it"). The behavioural proof (`handlerRegistry.has() === false`) holds regardless.
- Build proven fresh and provenanced per precondition 1 above: `pgrep -fl gamelib-shell` → PID
  60732, running since `Mon Aug 24 01:30:02 2026`, binary mtime `Aug 24 01:30:06 2026`, both after
  the commit's own timestamp `2026-08-24T00:53:53+12:00`. The app is RUNNING NOW and ready for the
  operator to drive Task 2 (fresh instance, not the stale PID 60016 that LAUNCH 1 exposed and that
  was killed).
- Pre-run `gamelib.log` archived to
  `~/Library/Logs/GameLib/gamelib.log.pre-346-04-20260823T131307Z` before either launch, per
  precondition 2 above.

**Result:** **PASS.** Captured live in a real `gamelib.log`, on the build under test
(commit `9245195b66e21c9b3f3f1d6b1235e03a5bb81698`, the same commit recorded in precondition 1
and in this step's build-provenance block above). Archived by the operator's session to
`~/Library/Logs/GameLib/gamelib.log.step0-capture-20260823T191024Z` (independently confirmed
present on disk at hand-off, 22,664 bytes, `grep -c "GAMELIB_DECLARED_UNAVAILABLE"` = 5 total
lines in that archive).

Matched winetricks lines (verbatim, independently re-grepped from the archived file at task-3
time, not merely copied from the report):

```
166:(07:08:53) [ERROR]:   [Frontend]:        [GAMELIB_DECLARED_UNAVAILABLE] Winetricks unavailable under Tauri (channel=winetricksInstalled) -- tracked as its own deferral (D-03): [GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'winetricksInstalled'
167:(07:08:53) [ERROR]:   [Frontend]:        [GAMELIB_DECLARED_UNAVAILABLE] Winetricks unavailable under Tauri (channel=winetricksAvailable) -- tracked as its own deferral (D-03): [GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'winetricksAvailable'
```

Both lines name a `winetricks` channel (`winetricksInstalled`, `winetricksAvailable`) — the PASS
condition's "at least one matching line names a winetricks channel" is satisfied twice over, and
this is not a `steamgriddb.*`-only false positive (the FAIL condition's named trap).

**Panel state, as reported by the operator** (this executor cannot observe rendered UI directly;
this is the live-verify checkpoint's own designed division of labour — a human reports what
rendered):
> "Winetricks component management is unavailable on this build" / "Installed components:
> Winetricks support is deferred to a future release (D-03, Phase 34.6) and cannot be listed or
> installed from this build."

This satisfies the acceptance criteria's "the Winetricks panel visibly showed an unavailable
state — recorded in words, not inferred": no empty list was presented as a silent success, and no
install action was reachable.

**Privacy check (T-34.6-11 mitigation):** `grep -icE "appName|avernum|206020"` over the marker
lines in the archived file returns 0 — no game name or appId leaked into the captured lines.
`callOrDeclare`'s deliberately args-free contract held under a real, live call. Recorded as a
positive finding, not merely assumed from source.

**Fixture provenance — the panel was NOT reachable as-found (record honestly, per this
project's standing rule against implying default reachability):** the operator initially could
not locate the Winetricks button. Root cause, confirmed by direct source read at
`src/frontend/screens/Settings/sections/GamesSettings/index.tsx`: line 83 computes
`isCrossover = wineVersion?.type === 'crossover'`; line 184's `{!isCrossover && (...)}` suppresses
the entire block containing `<Tools />` (Winecfg / Winetricks / Run EXE, line 201) plus
AutoDXVK/Esync/Fsync/WoW64/FSR whenever the game's configured Wine version has `type: "crossover"`.
The Wine tab itself still renders (version selector, prefix, bottle) but the tools row is simply
absent. The operator switched the test game's Wine version from "CrossOver" to
"Wine CrossOver - 23.7.1-1" (`type: "wine"`) before the Winetricks button appeared — confirmed in
the archived log at the `206020: Setting wineVersion to {...,"type":"wine",...}` line. **This
capture required a config change to reach the panel; it was not reachable by default.**
**Implication for the actual codebase, not just this gate run:** any real user whose configured
Wine version has `type: "crossover"` can never see the Winetricks decline path at all — the
`callOrDeclare` machinery this gate captured is unreachable for them, a distinct fact from
"winetricks is unported."
**Also observed, not chased here:** at the moment the operator hit the guard, the on-disk config
for game 206020 (Avernum 4) read `type: "wine"` while the running app's resolved Wine-version
state behaved as `crossover` (per the archived log's `wineVersion` transitions around
`07:03`–`07:43`) — i.e. the running app's resolved setting differed momentarily from the config
file on disk. Recorded as an observation for a future debug session; out of scope for this plan.

**Scope statement (per this plan's own instruction — do not conflate with the SteamGridDB half):**
this observation discharges the **winetricks half of `U-34.5-32` only**. It makes no claim about
the SteamGridDB half, which D-10 retires by PORT (plan `34.6-09`) rather than by decline.
`34.5-UNTESTED-ITEMS.md`'s two-part disposition is written by plan `34.6-14`, not here.

---

**ADDENDUM to Step 0 (not a Step 0 PASS/FAIL condition — Step 0's own scoring is winetricks-only
by contract; this is a separate, incidental observation from the same capture run, recorded here
because no other step or plan was ever going to capture it before its own port destroys the
evidence):**

The same archived capture (`~/Library/Logs/GameLib/gamelib.log.step0-capture-20260823T191024Z`,
commit `9245195b66e21c9b3f3f1d6b1235e03a5bb81698`) incidentally contains three further
`GAMELIB_DECLARED_UNAVAILABLE` lines, at `06:53:07`, for channels this phase also ports later
(EOS overlay in plan `34.6-08`, wave 4; SteamGridDB in plan `34.6-09`, wave 5) — exactly the same
"the port destroys its own evidence" hazard that motivates Step 0's existence and ordering applies
to these, and nothing in this gate document's Steps 1-8 was ever scoped to capture them pre-port
(Step 2 proves the EOS *ported* round-trip; Step 3 proves SteamGridDB *ported*; neither one's
contract asks for a pre-port decline capture). They exist in this run's evidence only by
incidental luck of capture ordering, verified independently (re-grepped from the archive, not
copied from the report):

```
44:(06:53:07) [ERROR]:   [Frontend]:        [GAMELIB_DECLARED_UNAVAILABLE] EOS Overlay unavailable under Tauri (channel=getLatestEosOverlayVersion) -- tracked as its own deferral (D-03): [GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'getLatestEosOverlayVersion'
45:(06:53:07) [ERROR]:   [Frontend]:        [GAMELIB_DECLARED_UNAVAILABLE] EOS Overlay unavailable under Tauri (channel=getEosOverlayStatus) -- tracked as its own deferral (D-03): [GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'getEosOverlayStatus'
46:(06:53:07) [ERROR]:   [Frontend]:        [GAMELIB_DECLARED_UNAVAILABLE] SteamGridDB artwork unavailable under Tauri (channel=steamgriddb.hasApiKey) -- tracked as its own deferral (D-03): [GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'steamgriddb.hasApiKey'
```

**State plainly:** these three lines discharge the live-observation half of `U-34.5-32` for the
EOS overlay and SteamGridDB channel families as well — i.e. `DECLARED_UNAVAILABLE_MARKER` now has
a live, pre-port observation for **all three** deferred clusters (winetricks, EOS overlay,
SteamGridDB), not winetricks alone. This addendum does **not** change Step 0's own PASS/FAIL
scoring (which remains winetricks-only per its written contract), does not fill Step 2 or Step 3's
`Result:` slots (those remain `[pending]` for plan `34.6-12`, and score the *ported* behaviour, a
different property), and does not by itself close any ledger row — `34.6-14` owns
`34.5-UNTESTED-ITEMS.md`'s disposition writing.

---

### Step 1 — Epic embedded login (D-15)

**What it proves:** that the **embedded/primary** Epic login tile — not SIDLogin — establishes a
working session. Epic has had two wired tiles since 2026-08-22 (primary = embedded login, alternative
= SIDLogin device-auth). Item 11 (the ROADMAP's own naming) means the embedded path specifically. It
is the primary path and nothing in this project has ever gated it live before this run.

**State explicitly, per D-15: SIDLogin does NOT satisfy this item.** Accepting SIDLogin as a
substitute would let the primary path ship unobserved while this document reads "Epic login PASSED"
— a pass covering a surface that was never actually reached, which this project has a recorded
history of (see the Process Lessons memory index entry "a PASS can cover an unreachable surface").

**How to drive it:** log in via the embedded/primary Epic tile.

**PASS condition:** session established, **and** the Library renders at least one named Epic title.

**FAIL condition:** login completes only via SIDLogin (record this as its own distinct outcome, do
NOT fold it into a generic pass); **or** the Library renders zero Epic tiles after a session is
established (record which of the two failure shapes occurred — they are different defects and must
not be folded together).

**Evidence to capture:** the tile clicked (embedded vs. SIDLogin, stated explicitly even on a pass);
at least one rendered Epic title's name; the launch ordinal and archived-log excerpt covering login.

**Rider (folded todo 1, `REQ-34.4.1-GAP-11`, `.planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md`):**
watch for a `keyring_get` timeout during this step and **measure** elapsed time if one fires —
inferred time is not a measurement. An unbounded `keyring_get` can consume the sidecar's entire 60s
RPC budget. **A non-firing rider is NOT a discharge of the todo** — if it does not fire during this
run, record it as unexercised and leave the todo open; do not mark it resolved by absence.

**Result:** [pending]

---

### Step 2 — EOS overlay round-trip (D-05 as corrected by A-02)

**What it proves:** that the full install → enable → disable → remove cycle completes through the
sidecar, and that the confirmation-dialog crash hazard (34.5 D-15: a dialog *rejection* crashes the
app under the sidecar) is exercised deliberately, not left unobserved.

**State explicitly, per A-02: the confirmation dialog fires at `remove()`
(`src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts:161-162`, called
unconditionally), NOT at `enable()` (`:193-197`, gated `if (!isInstalled())` at line 196).** In the
install → enable → disable → remove order, `enable()`'s own dialog cannot fire — the preceding
install step already made `isInstalled()` (`:249`) true by the time `enable()` runs. **"Expected a
dialog at enable, none appeared" is the CORRECT outcome for this run and must be recorded as such,
not as a defect.** The dialog that genuinely fires, at the round-trip's last step
(`remove()`), is the one this item's crash-hazard proof depends on.

**How to drive it:** through the EOS overlay panel (`AdvancedSettings/index.tsx`'s `callOrDeclare`
sites), run install, then enable, then disable, then remove, in that exact order. Dismiss the
`remove()` confirmation dialog (accept the removal) and observe whether the app survives dismissal.

**PASS condition:** all four operations complete through the sidecar, **AND** the `remove()`
confirmation dialog appears, **AND** dismissing it does not crash the app.

**FAIL condition:** any of the four operations returns `UNPORTED_CHANNEL_MARKER`; **or** the app
crashes on dialog dismissal (the 34.5 D-15 hazard, now observed live for the first time under the
sidecar).

**Evidence to capture:** the `getEosOverlayStatus` result after each of the four steps; explicit
confirmation that no dialog appeared at `enable()` (recorded as the correct outcome, per A-02); the
archived-log excerpt spanning all four operations; the operator's legendary/EOS state after remove,
cross-checked against precondition 6's restore commands.

**Post-step instruction:** run precondition 6's restore commands to return the operator's
legendary/EOS overlay state to what it was before this round-trip.

**Result:** [pending]

---

### Step 3 — SteamGridDB full chain with a REAL key (D-06)

**What it proves:** that the full artwork chain — `setApiKey` → `hasApiKey` → `searchGame` →
`getGrids` → `getHeroes` — resolves through the sidecar with real network calls, with artwork actually
**rendering** in `SteamGridDBPicker`, and that A-03's keyring hardening genuinely keeps the key out of
plaintext config.

**Precondition:** plans `34.6-01` and `34.6-02` (A-03's keyring hardening) must have landed before
this step runs — otherwise this step is the exact moment of exposure A-03 exists to prevent (entering
a real key against an unhardened `secureKey.ts` would persist it in plaintext, per
`electronStub.ts:27-29`'s dead `safeStorage` stub).

**How to drive it:** enter a real SteamGridDB API key through the settings field
(`SteamGridDbApiKey.tsx`), then drive a search/grid/hero lookup through `SteamGridDBPicker` for a
title never previously looked up in this app installation — a title with pre-existing cached
artwork would render regardless of whether this run's network calls actually succeed, which would
make the render check pass without proving anything about this run.

**PASS condition:** artwork renders in `SteamGridDBPicker`, **AND** the key is verified present in
the Keychain slot `steamgrid-api-key`, **AND** absent from `config.json`.

**FAIL condition:** artwork does not render; **or** `grep -c steamGridDbApiKey <config.json>` finds
the key in the clear (an A-03 regression — this is the negative check the plan's own threat register,
T-34.6-10, requires this step to assert directly rather than assume absent).

**Evidence to capture:** a screenshot or description of rendered artwork; the Keychain slot lookup
command and its result (never the key value itself); the `grep -c steamGridDbApiKey <config.json>`
output (expected: 0).

**Result:** [pending]

---

### Step 4 — winetricks (D-04 as corrected by A-01)

**What it proves:** that the ported channel resolves with real data through the sidecar and that the
D-11 sidecar-handler observable fires — i.e. "the handler was reached", which is the property this
item actually measures.

**State explicitly, per A-01: an empty list is an acceptable RESULT, not a decline.**
`Winetricks.listInstalled()` (`tools/index.ts:755-765`) returns `[]` only when `winetricks.log` is
absent at the configured Wine prefix — that is **data-dependent, not platform-gated**. Winetricks has
genuine macOS support: a dedicated `macEnvs` branch (`tools/index.ts:628`), selected by
`isMac ? macEnvs : linuxEnvs` (`:642`). There is **no** `!isLinux` guard anywhere in the real
`Winetricks` object (`tools/index.ts:528`) — the `!isLinux` guards at `:807`/`:843` belong to
`getDxvkUrl()`/`getVkd3dUrl()`, unrelated DXVK/VKD3D URL-selection functions. **Do not write a check
expecting a decline message from a ported winetricks channel on macOS — none will appear, and a "gate
never fired" reading here would be a false negative**, not a passing absence.

**How to drive it:** through the Winetricks panel, trigger `winetricksAvailable`,
`winetricksInstalled`, and `winetricksInstall` (the install action) against the configured Wine
prefix.

**PASS condition:** no `UNPORTED_CHANNEL_MARKER` for any of the three channels, **AND** the D-11
observable line appears for `winetricksInstall`.

**FAIL condition:** `UNPORTED_CHANNEL_MARKER` appears for any of the three; **or** the D-11 observable
line is absent after driving the install action.

**Evidence to capture:** the raw return value of `winetricksAvailable`/`winetricksInstalled` (an empty
list is acceptable evidence of a working, data-dependent result — record it as such, not as a
failure); the archived-log excerpt containing the D-11 observable line for `winetricksInstall`.

**Result:** [pending]

---

### Step 5 — the 8 late-discovered channels, on the live session (D-07)

**What it proves:** that all 8 late-discovered channels work through the sidecar, riding the
authenticated session step 1 established.

**How to drive it, per channel:**
- `getAchievements`, `getPlaytimeFromRunner`, `getGogDiscounts` — ride the session from step 1
  directly (near-zero marginal cost once that session exists).
- `frontendReady` fires automatically at app start — **score it from its D-11 observable line, not
  from a UI symptom.** It is send-kind (`src/preload/api/misc.ts:93`, `makeListenerCaller`) and fails
  **silently** under Tauri — there is no UI symptom to observe even when it is broken.
- `getDefaultSavePath` is exercised by step 7 (do not attempt to drive it independently here — its
  proof lives in that step).
- `moveInstall`/`importGame` use the small fixture from precondition 8, moved/imported over existing
  content.
- `runWineCommandForGame` is driven from the per-game Wine-command action in the UI.

**PASS condition:** all 8 named channels produce a real result or observable, with zero
`UNPORTED_CHANNEL_MARKER` lines across any of them.

**FAIL condition:** any one of the 8 is not exercised (record it as **NOT ATTEMPTED**, which is not a
pass per the Scoring rules above — do not silently omit it); or any one produces
`UNPORTED_CHANNEL_MARKER`.

**Evidence to capture:** per-channel result or observable-line excerpt, for all 8, with explicit
NOT ATTEMPTED markers for any not exercised.

**Result:** [pending]

---

### Step 6 — `egsSync` (34.5 UAT test 12)

**What it proves:** that `egsSync` works when actually driven, not merely that it is wired.

**How to drive it:** trigger `egsSync` through the UI on the live Epic session from step 1.

**PASS condition:** the command completes, **AND** the Library reflects the sync (a visible change
attributable to the sync, not merely a non-error return).

**FAIL condition:** the command errors; **or** "the command was called" is the only evidence
available — a call proves wiring, not behaviour, per this project's own standing lesson, and is
explicitly insufficient here.

**Evidence to capture:** the pre-sync and post-sync Library state (what changed, specifically);
archived-log excerpt covering the sync call.

**Result:** [pending]

---

### Step 7 — legendary save sync round-trip (D-14)

**What it proves:** that both directions of legendary save sync work through the sidecar, by content,
not by exit code.

**Precondition:** `getDefaultSavePath` (one of the 8 late-discovered channels) must be ported before
this step runs — D-14's wave-ordering constraint, satisfied by plan `34.6-10` landing before plan
`34.6-12` runs this gate.

**How to drive it:** back the fixture save up first (precondition 7). Mutate the save locally (a
specific, identifiable change — e.g. append a known marker string to a save file) → sync **up** →
alter or delete the local copy → sync **down** → check whether the mutation came back.

**PASS condition:** the specific mutated bytes/string are present again after the down-sync,
verified by direct content comparison.

**FAIL condition:** exit code 0 with unchanged or empty content. **"The command exited 0" is
explicitly NOT proof here** — a sync that silently syncs nothing would also exit 0, which is exactly
why this item is scored by content, never by exit code alone.

**Evidence to capture:** the exact mutation string/bytes used; a diff or grep showing the mutation
present in the post-down-sync file; confirmation the pre-sync backup (precondition 7) remains intact
regardless of outcome.

**Result:** [pending]

---

### Step 8 — Epic logout, LAST (D-12, D-13)

**What it proves:** that `clearEpicCookies` removes Epic-domain cookies **without** removing cookies
belonging to other domains — scored by identity, not by count.

**Why this runs last:** the Epic session step 1 establishes is a precondition for `egsSync`, save
sync, `getAchievements`, and `getPlaytimeFromRunner`. Logging out destroys that session, so every
other item in this document must run before this one.

**State explicitly, per D-13: a count alone is NOT sufficient — this AMENDS the ROADMAP's stated
discharge bar.** The ROADMAP's inherited wording asks for "a `clearEpicCookies` count cross-checked
against an independent re-read of the jar." That is exactly the shape that produced
`34.4.1-35-PLAN.md`'s still-UNSTRUCK precondition 6: `survivingNonHumble=0` was accepted as a pass
when `before total=34` equalled `matched=34` — with every cookie in the jar belonging to the thing
being cleared, no non-cleared cookie could ever have survived to prove the clear was scoped correctly.
The count was arithmetically forced, not passing.

**How to drive it:** plant the named non-Epic cookie recorded in precondition 9, **before** logging
out. Log out via the UI. Then, by an **independent** re-read of the jar (not a value cached from
before the logout), assert both: (a) all Epic-domain cookies are cleared, and (b) the exact planted
cookie survives **by name**.

**PASS condition:** after the independent re-read, zero Epic-domain cookies remain, **AND** the
planted foreign cookie is present, matched by its exact recorded name.

**FAIL condition:** the planted cookie is absent afterwards (over-scoped clear); **or** the result
records only a count with no identity check performed (this is a FAIL on this item's own terms, not
merely an incomplete record — a count-only result repeats precisely the defect D-13 exists to avoid
inheriting).

**Evidence to capture:** `before total` and `matched` (Epic-domain count before logout), recorded
alongside the identity check — **not instead of it** — so a future reader can distinguish a real pass
from an arithmetically forced one; the planted cookie's name and domain, both before and after;
confirmation the re-read was independent (a fresh jar query, not a cached value).

**Rider (folded todo 2, `D-29-06`,
`.planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md`):** if a
generic RPC timeout co-occurs with this cookie operation, capture the **full shell scrollback**, not
just `gamelib.log` — `eprintln!` output never reaches `gamelib.log`, so `gamelib.log` alone cannot
settle whether a co-occurring timeout is related. Correlate `id=` values across both sinks if this
fires. A non-firing rider is not a discharge of the todo.

**Result:** [pending]

---

### Optional rider — Humble login (folded todo 4, `D-29-02`, REQ-34.6-12)

**Not required by this phase's own leg.** This phase's live gate logs into **Epic**; the 404 this
rider investigates is a **Humble** surface. It is dischargeable **only if** the operator additionally
opens a Humble login during this same gate session and observes whether `getAccountIdentity` 404s.

**What it proves (only if exercised):** which of the two candidate explanations for the Humble
`getAccountIdentity` 404 holds, or that neither does.

**How to drive it:** open a Humble login separately from the Epic flow this gate otherwise runs, and
observe the `getAccountIdentity` call.

**PASS condition (only meaningful if exercised):** the 404 is observed and attributed to one of the
two candidate causes, or confirmed absent, with the reasoning recorded.

**FAIL condition:** N/A in the count-toward-verdict sense — this rider does not gate the phase's own
verdict. Its own failure mode is being silently marked closed without ever having been exercised.

**State explicitly: if no Humble login is performed during this session, record NOT DISCHARGED and
return the todo to pending.** It must not be closed by association with the phase that folded it —
folding a todo into a phase's scope is not the same as the phase's own leg actually discharging it,
and this rider's entire reason for existing is to keep that distinction visible.

**Evidence to capture (if exercised):** the `getAccountIdentity` response and status code; which
candidate explanation the evidence supports, or an explicit "neither" finding.

**Result:** [pending]

---

## Verdict

**Verdict:** _(pending)_

Filled only after every scored item above has a recorded outcome. Per the Scoring rules, this reads
`PASS 10/10` only if every one of the ten items above scored PASS; any FAIL or NOT ATTEMPTED item
changes this line to the honest fraction (e.g. `PASS 8/10, 1 FAIL, 1 NOT ATTEMPTED`) rather than
being rounded up or smoothed over.

---

## Closing statement

_(pending — written after the verdict is filled, summarizing what the run found, any deviations from
this contract's own preconditions or step order, and any items amended in place under a `##
SUPERSEDES` section on a later run)_
