---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 15
type: execute
wave: 9
depends_on: ["34.6-12"]
gap_closure: true
files_modified:
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
autonomous: true
requirements: [REQ-34.6-11, REQ-34.6-12]
must_haves:
  truths:
    - "All six contract defects run 1 found in the gate's OWN text receive the formal in-place SUPERSEDES treatment D-12 requires — the closing statement records them as prose, which is NOT the same thing"
    - "The document is AMENDED IN PLACE, never regenerated: run 1's measured results, findings and closing statement survive byte-for-byte"
    - "A superseded condition is rewritten to assert the PROPERTY the step exists to protect, never a landmark, a literal, or a count that a correct installation can fail"
    - "An amendment may rescore an item ONLY from evidence run 1 already captured; it may never manufacture an outcome the operator did not report, and it may never convert a FAIL into a PASS"
    - "Steps 4 and 8 are OUT of this plan's scope — they are code defects, not contract defects, and amending a step so its own FAIL stops counting is the one move this plan must not make"
  artifacts:
    - path: ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md"
      provides: "six marked SUPERSEDES sections, dated, each naming the item it revises and the corrected condition"
      contains: "## SUPERSEDES"
  key_links:
    - from: "each SUPERSEDES section"
      to: "the scored item or precondition it revises"
      via: "an explicit item name in the section heading"
      pattern: "## SUPERSEDES"
---

<objective>
Give the six contract defects run 1 found in `34.6-LIVE-GATE.md`'s own text the formal in-place
SUPERSEDES treatment D-12 requires, so run 2 is scored against a contract that is correct.

Run 1 RECORDED all six as prose inside the steps they affect. Its own closing statement §3 says so
plainly: *"none have been amended yet — that amendment is explicitly out of this task's scope."*
Recording a defect and superseding the clause that carries it are different acts, and only the
second one changes what a rerun is scored against. Left unamended, run 2 would re-score Step 3
against a `grep` that fails on any correct installation, and re-score Step 6 against a clause that
is unsatisfiable on the operator's actual fixture — manufacturing two more false FAILs.

This plan touches ONE file and changes NO code.

Purpose: make the contract correct before it is used again.
Output: `34.6-LIVE-GATE.md` carrying six dated, marked SUPERSEDES sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-12-SUMMARY.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
</context>

<the_six_defects>

Named exactly as run 1's closing statement §3 names them. The `Corrected bar` column is this plan's
prescription; the executor must verify each against the live source before writing it, because a
review's prescribed fix can itself carry the defect it reports (this project has four recorded
instances).

| # | Item | Defect as found | Corrected bar |
|---|------|-----------------|---------------|
| 1 | **Step 2** (EOS round-trip) | PASS condition assumes all four operations are UI-reachable on any platform. macOS has ZERO reachable entry points to `enable`/`disable` — `AdvancedSettings/index.tsx:401` gates on `isWindows`, `GameSubMenu/index.tsx:461` gates on `isLinux`, and the two point in opposite directions. | Scope the condition BY PLATFORM. On macOS the scored surface is install → remove only, and the `remove()` dialog observation is what carries the 34.5 D-15 crash-hazard proof. enable/disable are recorded UNREACHABLE-ON-PLATFORM, which is neither a PASS nor a FAIL of the port. |
| 2 | **Step 3** (SteamGridDB) | FAIL condition is `grep -c steamGridDbApiKey <config.json>` finding a match. The setting NAME legitimately exists in a correct install with an empty value, so this fires on every correct installation. | Assert the JSON VALUE is empty, not that the key name is absent. Read the value with a JSON-aware check and assert it is `""`/absent-of-secret — never a `grep` for the key name. |
| 3 | **Step 5, `runWineCommandForGame`** | Its driving method names "the per-game Wine-command action", a UI action that exists nowhere in the renderer. A full-source census found ZERO call sites. | Restate as UNREACHABLE-BY-CONSTRUCTION: the channel is declared, exposed and registered but has no renderer caller, so no live drive is possible. This is a coverage FACT to carry forward, not a step an operator can fail. |
| 4 | **Step 6** (`egsSync`) | Second PASS clause ("the Library reflects the sync") is unsatisfiable whenever the linked EGL genuinely has zero installed games — proven here by four independent on-disk checks. | Make the clause CONDITIONAL on the fixture having ≥1 installed EGL title, and name the on-disk check that establishes which branch applies. A zero-title EGL scores on the command's own completion evidence alone. |
| 5 | **Precondition 8** | A Steam-only fixture (World of Goo) was chosen to support a channel Steam cannot reach. | Require the fixture to be named together with the RUNNER each scored channel needs, and require that pairing to be checked when the precondition is measured — not when the step it feeds is driven. |
| 6 | **Precondition 9** | Its cookie-planting recipe requires typing into the Tauri DevTools console — a surface this project's standing guidance already treats as non-executable by an operator (accepts neither paste nor Enter; confirmed since Phase 34.1 UAT item 5b, 2026-08-22, and Phase 34.5 gate 4, 2026-08-12). | Replace the recipe with a console-free planting route, or mark the precondition BLOCKED and say so up front. A step reachable only through the console must be written as BLOCKED, never as an executable recipe. |

</the_six_defects>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify each of the six defects against live source before amending anything</name>
  <read_first>
    - `.planning/phases/34.6-.../34.6-LIVE-GATE.md` § Scored items and § Preconditions — the exact clause text each defect lives in
    - run 1's FINDING 1 and FINDING 2 under Step 2, and the "Disposition" paragraphs under Steps 3 and 6
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Do NOT amend yet. For each of the six, re-measure the fact the defect rests on, against the source
as it stands TODAY. This is not ceremony: this project has a recorded case of a code-read PREDICTION
outliving its own fix by three days, and four recorded cases of a review's PRESCRIBED fix carrying
the same defect it reports.

Specifically re-measure:
  - **#1** — that `AdvancedSettings/index.tsx`'s EOS block is still `isWindows`-gated and
    `GameSubMenu/index.tsx`'s is still `isLinux`-gated. Grep both predicates; record line numbers.
    If either has moved or changed, the amendment text changes with it.
  - **#2** — what a correct `config.json` actually contains for the SteamGridDB key setting. Read
    the real file. Record the exact setting name and its value shape when NO key is stored, so the
    corrected assertion is written against observed data, not assumed data.
  - **#3** — that `runWineCommandForGame` still has zero renderer call sites. Re-run the census
    across the whole of `src/frontend`; a call site added since run 1 would invalidate the whole
    amendment. Record the command and its count.
  - **#4** — the four on-disk checks run 1 used to establish the EGL had zero installed titles.
    Name them, so the corrected conditional clause cites a check rather than an impression.
  - **#5** — which runner each channel scored under Step 5 actually requires. Record the mismatch
    that made World of Goo the wrong fixture.
  - **#6** — that the DevTools-console limitation still holds. Cite the two prior confirmations by
    date rather than re-deriving them.

Record all six measurements in the plan's working notes for Task 2 to cite. A measurement that
contradicts the table above WINS — amend the table's prescription, and say so.
  </action>
  <verify>
    <automated>grep -n "isWindows\|isLinux" src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx src/frontend/screens/Game/GameSubMenu/index.tsx | head; grep -rc "runWineCommandForGame" src/frontend/ 2>/dev/null | grep -v ':0' | head</automated>
  </verify>
  <acceptance_criteria>
    - All six facts are re-measured against today's source, each with the command that measured it and its output.
    - The `runWineCommandForGame` census reports a NUMBER, and that number is 0 (or the amendment is re-scoped and this is stated).
    - The `config.json` fact is read from a real file, not assumed.
    - Any measurement contradicting this plan's own prescription table is recorded as such, and the prescription is corrected before Task 2.
  </acceptance_criteria>
  <done>Every defect's underlying fact is confirmed current, or the prescription is corrected to match what is.</done>
</task>

<task type="auto">
  <name>Task 2: Write six marked SUPERSEDES sections, amending in place</name>
  <read_first>
    - `.planning/phases/34.6-.../34.6-LIVE-GATE.md` § Scoring rules — the SUPERSEDES format the contract mandates
    - `.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-VERIFICATION.md` — the amend-in-place discipline across nine attempts
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
Amend the document IN PLACE. Never regenerate it. Regenerating destroys hand-corrections layered
onto prior amendments, which is exactly what made `34.4.2-VERIFICATION.md`'s nine-attempt history
legible instead of lost.

Each of the six gets one clearly marked section carrying:
  - a heading naming the item it revises, e.g. `## SUPERSEDES — Step 3's FAIL condition (2026-08-25)`
  - the ORIGINAL clause, quoted verbatim, so a reader can see what changed
  - why it is defective — the measured fact from Task 1, not a restatement of the PASS condition
  - the CORRECTED clause, stated as the property the step exists to protect
  - whether run 1's recorded outcome is RESCORED by this amendment, and if so, from what to what,
    citing only evidence run 1 already captured

Hard constraints, each of which this project has been bitten by:
  - **A corrected condition must name the PROPERTY, never a landmark or a literal.** An appearance
    gate that names a landmark, and a gate literal that is stale by behaviour, are two of this
    project's nine recorded ways a green check proves nothing.
  - **Never convert a FAIL into a PASS.** Steps 4 and 8 are untouched by this plan. A rescore is
    only ever available where run 1 explicitly recorded "PASS on the property, condition defective"
    (Steps 3 and 6) or "BLOCKED — CONTRACT DEFECT" (Step 2, Step 5's `runWineCommandForGame`).
  - **Preserve every existing byte of run 1's results, findings and closing statement.** The
    amendments are additive sections plus, where a clause is superseded, a pointer AT the original
    clause to the section that supersedes it. Do not delete the original clause.
  - **Do not touch the `verdict:` frontmatter.** Re-adjudication belongs to plan 34.6-17.
  </action>
  <verify>
    <automated>F=$(ls .planning/phases/34.6-*/34.6-LIVE-GATE.md); echo "SUPERSEDES sections: $(grep -c '^## SUPERSEDES' "$F")"; grep -c '^\*\*Result:\*\* *$' "$F"; grep -n '^verdict:' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## SUPERSEDES'` returns exactly 6.
    - Each section names its item in the heading and carries a date.
    - Each quotes the original clause verbatim AND states the corrected one.
    - `grep -c '^\*\*Result:\*\* *$'` still returns 0 — no result slot was emptied.
    - `verdict:` is still `FAIL`, unchanged by this plan.
    - `git diff` shows only ADDITIONS plus supersede-pointers; no run-1 result, finding or closing-statement line is deleted or reworded.
    - No amendment touches Step 4 or Step 8.
  </acceptance_criteria>
  <done>The contract states six corrected conditions, and run 1's record is intact underneath them.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the corrected conditions are non-vacuous in BOTH directions</name>
  <read_first>
    - the six SUPERSEDES sections just written
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md</files>
  <action>
A corrected condition that cannot FAIL is worse than the defective one it replaced, because it
carries the authority of having been fixed. For each of the six, state in the SUPERSEDES section
what a FAILING observation would look like under the corrected clause — concretely, as an
observation an operator could make, not as "the PASS condition not met" restated.

Where the corrected clause is mechanically checkable (#2's JSON value assertion, #3's census),
RUN it against a known-bad input and record that it fails, and against the real input and record
that it passes. An assertion unproven in both directions is one of this project's recorded ways a
gate proves nothing.

For #1, #4, #5 and #6 — which are operator-driven rather than mechanical — state the falsifying
observation in one sentence each and mark them as such. Do not fabricate a mechanical proof for a
condition that has none.
  </action>
  <verify>
    <automated>F=$(ls .planning/phases/34.6-*/34.6-LIVE-GATE.md); grep -c 'falsif\|would FAIL if\|FAILING observation' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - All six SUPERSEDES sections state a concrete falsifying observation.
    - #2 and #3's corrected assertions are each run against a known-bad and a real input, with both outputs recorded.
    - #1, #4, #5, #6 are explicitly marked operator-driven, with no fabricated mechanical proof.
  </acceptance_criteria>
  <done>Every corrected condition can be failed, and the two mechanical ones are proven to fail on bad input.</done>
</task>

</tasks>

<threat_model>
Not applicable — this plan changes no code, ships no channel, and touches one planning document. The
only risk it carries is documentary: an amendment that rescores an item beyond what run 1's evidence
supports, which Task 2's constraints and Task 3's both-directions proof exist to prevent.
</threat_model>
