---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 12
subsystem: testing
tags: [live-gate, tauri, sidecar-ipc, uat, eos-overlay, steamgriddb, winetricks, epic-login, save-sync]

requires:
  - phase: 34.6-03
    provides: "34.6-LIVE-GATE.md authored with EMPTY result slots, by a plan forbidden from running it"
  - phase: 34.6-04
    provides: "Step 0's DECLARED_UNAVAILABLE_MARKER capture, taken on a build that deliberately predates the winetricks port"
  - phase: 34.6-10
    provides: "the last 3 of the 24 ported channels, completing the surface this gate exercises"
  - phase: 34.6-11
    provides: "T-34.5-C6-49-03 containment hardening, so a step-5 rejection reads as validation working rather than the port broken"
provides:
  - "34.6-LIVE-GATE.md run 1: all ten result slots filled from operator observation, verdict FAIL, closing statement"
  - "two FAILs distinguished BY KIND: winetricksInstall (this phase's own broken deliverable) vs Epic logout (a shell/wry defect on a channel 34.6 never ported)"
  - "six contract defects located in the gate's OWN text (Steps 2, 3, 5, 6, preconditions 8, 9) — recorded, NOT yet amended"
  - "three BLOCKED items, all the same shape: no reachable UI surface on macOS"
  - "six live defects no automated check in this repo could have caught, four of which present as silence or false success"
affects: ["34.6-13", "34.6-14", "34.6-15", "34.6-16", "34.6-17"]

tech-stack:
  added: []
  patterns:
    - "Identity re-read beats count cross-check: D-13 scored Epic logout by the survival of a PLANTED, NAMED foreign cookie — the app's own count said 8, so a count-only check would have PASSED a logout that cleared zero"
    - "The D-11 send-handler observable is the only instrument that can detect a send-kind no-op; registration census, invoke-return sweep and UNPORTED_CHANNEL_MARKER absence are all vacuous for send-kind by construction"
    - "A gate's own text is a reviewable artefact: six of this run's findings were defects in the contract, not in the code it scored"

key-files:
  created: []
  modified:
    - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md

key-decisions:
  - "Verdict is FAIL, not PASS-with-notes: D-12 admits no partial-pass mode, and two items scored FAIL outright"
  - "The two FAILs are recorded as different KINDS of failure so no reader concludes 34.6 broke Epic logout — 34.6 never ported that channel"
  - "The six contract defects were RECORDED but deliberately NOT amended: amendment is out of this task's scope and is owed the in-place SUPERSEDES treatment under D-12"
  - "The Humble rider is NOT DISCHARGED and returns to pending — folding a todo into a phase's scope is not the same as that phase discharging it"
  - "This document sets no phase-level status; that determination belongs to 34.6-13/14 and the operator"

requirements-completed: [REQ-34.6-11, REQ-34.6-12]

duration: 10h29m wall clock (operator-driven live gate across six recording segments)
completed: 2026-08-24
---

# Phase 34.6 Plan 12: Run the Blocking Live Gate Summary

**Ran `34.6-LIVE-GATE.md` steps 1–8 plus the Humble rider on real macOS hardware, filled all ten result slots from operator observation, and adjudicated `verdict: FAIL` on two outright FAILs — `winetricksInstall`'s silent send-kind no-op and Epic logout clearing zero of 7 cookies while reporting 8.**

## Performance

- **Duration:** 10h 29m wall clock
- **Started:** 2026-08-24T12:46:21+12:00
- **Completed:** 2026-08-24T23:15:33+12:00
- **Tasks:** 3
- **Files modified:** 1

## Accomplishments

- **All nine preconditions measured, not attested.** `grep -c '^\*\*Measured:\*\* *$'` returns 0. Build freshness proved by commit SHA plus `pgrep -fl gamelib-shell`; both log sinks archived before launch; both `[bootstrap] secret stores: keyring` receipts confirmed.
- **All ten result slots filled.** `grep -c '^\*\*Result:\*\* *$'` returns 0. Every scored item carries the evidence its contract clause names.
- **Six live defects surfaced that no automated check in this repo could have caught** — four of them presenting as silence or as false success. This phase would have closed green without the gate.
- **The two FAILs were separated by kind and that distinction was written into the record**, so the phase's failure is not misattributed.
- **Six defects were found in the gate's own contract text**, not in the ported code it scored.

## Task Commits

Six recording commits, one per gate segment rather than one per task — the human-verify task (Task 2) spans the whole run and its results were committed as they were observed, so the record could not be lost to a session boundary:

1. **Task 1: preconditions** — `2619dc39d` (docs: establish preconditions 1-3, 6, 8-9; partial 4; defer 5, 7)
2. **Task 1 (cont.) + Task 2/3 step 1** — `6db9637ba` (docs: precondition 4/5/7 closure, Step 1 PASS)
3. **Task 2/3 step 2** — `d363d20ab` (docs: Step 2 BLOCKED, A-02 split confirmation, FINDING 1/2)
4. **Task 2/3 steps 3–4** — `1b5a0ec47` (docs: Step 3 PASS-with-defective-check, Step 4 FAIL)
5. **Task 2/3 steps 5–6** — `0bbdc7761` (docs: Step 5 6 PASS/1 deferred/1 BLOCKED, Step 6 PASS-with-defective-clause)
6. **Task 2/3 steps 7–8 + verdict** — `3edacef3c` (docs: Step 7 PASS, Step 8 FAIL, verdict FAIL, closing statement)

## Files Created/Modified

- `.planning/phases/34.6-.../34.6-LIVE-GATE.md` — run 1's measured preconditions, ten filled result slots, `verdict: FAIL`, and the closing statement

## Results

| Item | Outcome |
|------|---------|
| Step 0 — `DECLARED_UNAVAILABLE_MARKER` capture | PASS (run by plan 34.6-04, pre-port build) |
| Step 1 — Epic embedded login | PASS — embedded/primary tile, D-15 satisfied |
| Step 2 — EOS overlay round-trip | **BLOCKED — CONTRACT DEFECT** (no reachable enable/disable surface on macOS) |
| Step 3 — SteamGridDB with a real key | PASS on the property; its literal FAIL condition is defective |
| Step 4 — Winetricks | **FAIL** — `winetricksInstall` is a live silent no-op |
| Step 5 — the 8 late-discovered channels | 6 PASS · 1 BLOCKED (`runWineCommandForGame`) · 1 discharged via Step 7 (`getDefaultSavePath`) |
| Step 6 — `egsSync` | PASS on the property; its second PASS clause is unsatisfiable on this fixture |
| Step 7 — legendary save sync | PASS — round-trip proved BY CONTENT, both directions |
| Step 8 — Epic logout | **FAIL** — reports 8 cleared, clears zero of 7 in-scope cookies |
| Humble rider (optional) | NOT DISCHARGED — no Humble login performed; returns to pending |

**Verdict: FAIL.** Per D-12, one document, one verdict, no partial-pass mode.

## Decisions Made

- **The two FAILs are not the same kind of thing.** Step 4 is a failure of this phase's own deliverable — 34.6 ported `winetricksInstall` and it does not work. Step 8 is a shell/wry-layer defect on a channel this phase never ported; it is a genuine gate failure but not a 34.6 port regression. Recording them undifferentiated would have libelled the phase.
- **Six contract defects recorded, none amended.** Amendment is explicitly out of Task 3's scope and is owed the in-place SUPERSEDES treatment under D-12 — never a regeneration, which would destroy hand-corrections layered onto prior amendments.
- **The Humble rider returns to pending.** Its own stated rule: folding a todo into a phase's scope is not the same as that phase's leg discharging it. A non-firing rider is not a discharge.

## Contract defects found in the gate's own text

Six, spanning Steps 2, 3, 5, 6 and preconditions 8 and 9:

1. **Step 2** — its PASS condition assumes all four EOS operations are UI-reachable on any platform; macOS has no reachable enable/disable surface at all (FINDING 1).
2. **Step 3** — its literal FAIL condition (`grep -c steamGridDbApiKey <config.json>`) fails on any *correct* installation, because the setting name legitimately exists with an empty value. It must assert the JSON value is empty, not grep for the key name.
3. **Step 5, `runWineCommandForGame`** — its driving method names a UI action that does not exist anywhere in the renderer; a full-source census found zero call sites.
4. **Step 6** — its second PASS clause ("the Library reflects the sync") is unsatisfiable whenever the linked EGL genuinely has zero installed games, as proven by four independent on-disk checks.
5. **Precondition 8** — a Steam-only fixture (World of Goo) was chosen to support a channel Steam cannot reach.
6. **Precondition 9** — its cookie-planting recipe required typing into the Tauri DevTools console, a surface this project's own standing guidance already treats as non-executable by an operator. It should have been written as BLOCKED, not as an executable recipe.

## Issues Encountered

**Six live defects, four of them silent or falsely successful.** Every one would have shipped invisibly — census, sweep, slice and unit-test gates all pass around them by construction:

- `winetricksInstall`'s silent send-kind no-op (Step 4's FAIL)
- `openDialog` missing from `LONG_RUNNING_CHANNELS`, silently dropping picker results after 60s
- `PathSelectionBox`'s `onBlur` silently unlinking EGS sync on an empty field — reported to the operator as "Unsync Complete"
- `moveOnUnix` passing rsync flags macOS's `openrsync` rejects
- `importGame` performing no identity validation between the selected game and the folder's actual contents
- Epic logout reporting a clear of 8 cookies while clearing zero of 7 in-scope cookies

Each is filed as its own pending todo under `.planning/todos/pending/2026-08-24-*`. Three further BLOCKED items share one shape — no reachable UI surface — and are contract defects rather than code defects.

**D-13 is what caught Step 8, and that is the load-bearing lesson.** The ROADMAP's inherited bar was a cookie COUNT cross-check. The app's own count was `8`, so a count-only check would have PASSED a logout that cleared nothing. Only the identity re-read of a planted, named foreign cookie caught it.

## Deviations from Plan

**1. [Recording cadence] Committed per gate segment rather than per task**

- **Found during:** Task 2 (the blocking human-verify gate)
- **Issue:** Task 2 spans the entire multi-hour operator run. Holding all results until the task closed would have risked losing the record to a session boundary.
- **Fix:** Results were committed as observed, six commits across six segments; Tasks 2 and 3 are therefore interleaved in history rather than sequential.
- **Verification:** Both of Task 3's automated checks pass — 0 empty `Measured:` slots, 0 empty `Result:` slots, `verdict:` non-null.
- **Committed in:** `6db9637ba` … `3edacef3c`

**2. [Rule 2 — Missing Critical] `new_observations: []` contradicted the closing statement**

- **Found during:** plan close-out
- **Issue:** The frontmatter carried an empty `new_observations:` list while the closing statement's §4 enumerated six unanticipated live findings. Task 3's acceptance criteria allow an empty list *only if the closing statement says so explicitly* — it says the opposite. Left as-is, the machine-readable field would have hidden this run's most valuable output from every downstream reader.
- **Fix:** Populated `new_observations:` with the six findings the closing statement already names.
- **Files modified:** `34.6-LIVE-GATE.md` (frontmatter only; no result slot or prose touched)
- **Verification:** The list matches §4's enumeration one-for-one.

---

**Total deviations:** 2 (1 recording-cadence, 1 missing-critical auto-fix)
**Impact on plan:** Neither alters a measured result. The frontmatter fix is a record-fidelity correction, not a SUPERSEDES amendment — it changes no scored outcome and is distinct from the six contract defects, which remain unamended by design.

## User Setup Required

None — no external service configuration required. The run consumed a real SteamGridDB API key and a real Epic session, both supplied by the operator during the gate.

## Next Phase Readiness

**BLOCKING — this phase cannot close.** Task 3's own rule: *"If the verdict is FAIL: do not amend the phase's other artefacts and do not proceed to plans 34.6-13/14. Report the failing items to the orchestrator as a BLOCKING finding for a gap cycle inside this phase."*

Plans `34.6-13` and `34.6-14` are therefore gated behind a gap cycle covering:

1. **`winetricksInstall`** — this phase's own ported deliverable does not work. Narrowed to case (B): the listener IS registered (`electronStub.ts:130`'s `ipcMain.on` is an unconditional `Map` insert with no failure mode, on a path proven to execute), so the frame never reaches `dispatchSend`. Needs runtime instrumentation of `tauriTransport.ts`'s `send()`, not further code reading.
2. **The six contract defects** — each owed an in-place SUPERSEDES amendment under D-12.
3. **A targeted rerun** of the affected steps as run 2, amended into this document in place.

**Epic logout is explicitly OUT of the gap cycle's scope.** It is a genuine gate FAIL but a shell/wry defect on a channel 34.6 never ported; it stays on its own todo (`2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md`) rather than being pulled into an IPC re-plumb phase.

---
*Phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w*
*Completed: 2026-08-24*
