---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 19
type: execute
wave: 2
depends_on: ["34.6-18"]
gap_closure: true
files_modified:
  - public/locales/en/gamelib.json
  - src/backend/sidecar/bootstrap.ts
  - src/backend/main.ts
  - src/backend/sidecar/installFlowRegistration.ts
  - src/backend/sidecar/__tests__/installFlows.test.ts
  - src/backend/sidecar/__tests__/gamelibNamespaceLoad.test.ts
autonomous: true
requirements: [REQ-34.6-05]

must_haves:
  truths:
    - "A rejected `moveInstall` or `importGame` path produces a user-visible dialog, not only a `logError` line plus a terminal `done` status that reads as the app doing nothing"
    - "The three new user-facing strings live in `public/locales/en/gamelib.json` under the `gamelib:` namespace — never in `translation.json`, which the D-05 churn guard fails CI on"
    - "The namespace load is proven by EFFECT — a default-free `t('gamelib:installFlows.pathRejectedTitle')` against the real catalog returns the catalog value, not the key — because with byte-identical inline defaults a grep on the `ns:` array cannot observe the difference"
    - "The `gamelib` namespace is actually LOADED by both i18next init sites, so the strings resolve from the catalog rather than silently falling back to their inline English default forever"
    - "The dialog does not regress T-34.6-32: the rejected path is not carried into the message, and it does not regress T-34.6-31: `gameStatusUpdate` still emits exactly `['done']` on the rejection path"
  artifacts:
    - path: "public/locales/en/gamelib.json"
      provides: "an `installFlows` section with `pathRejectedTitle`, `pathRejectedBodyMove`, `pathRejectedBodyImport`"
      contains: "installFlows"
    - path: "src/backend/sidecar/__tests__/gamelibNamespaceLoad.test.ts"
      provides: "the default-free resolution proof that the `gamelib` namespace actually loads — the only observation that can distinguish a loaded namespace from an unloaded one when the inline defaults are byte-identical to the catalog"
    - path: "src/backend/sidecar/installFlowRegistration.ts"
      provides: "a `showDialogBoxModalAuto` call on both rejection paths, keyed on the `gamelib:` namespace"
  key_links:
    - from: "src/backend/sidecar/installFlowRegistration.ts"
      to: "public/locales/en/gamelib.json"
      via: "i18next.t('gamelib:installFlows.*') on the rejection path"
      pattern: "gamelib:installFlows\\."
    - from: "src/backend/sidecar/bootstrap.ts"
      to: "the gamelib namespace catalog"
      via: "the i18next init `ns` array"
      pattern: "ns: \\['translation', 'gamelib'\\]"
---

<objective>
`34.6-VERIFICATION.md`'s gap records that the rejection is **silent to the user**: a terminal
`done` status is emitted with no path-specific message, so the UI just does nothing. Plan
34.6-18 corrected WHICH paths are rejected; this plan makes any remaining rejection visible.

Purpose: a user who somehow supplies a malformed path learns why, instead of watching a
no-op — and a future regression of the same class surfaces to the user rather than hiding in
`gamelib.log`.
Output: three catalog strings in the fork's own namespace, that namespace actually loaded on
both builds, and a dialog on both rejection paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VERIFICATION.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-18-PLAN.md
@src/backend/sidecar/installFlowRegistration.ts
@public/locales/en/gamelib.json
</context>

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

`src/backend/sidecar/installFlowRegistration.ts` already imports and uses, in `moveInstall`'s
error branch:
  showDialogBoxModalAuto({ title: i18next.t('box.error.title', 'Error'),
                           message: i18next.t('box.error.moving', '...', { error }),
                           type: 'ERROR' })
Those existing keys resolve against the DEFAULT namespace (`translation`) and must not be moved.

Both i18next init sites currently pass NO `ns` and NO `defaultNS`:
  - `src/backend/sidecar/bootstrap.ts` ~:480-494 — the sidecar's only init
  - `src/backend/main.ts` ~:480-492 — the Electron leg's only init
Both use `i18next-fs-backend` with `loadPath: join(publicDir, 'locales', '{{lng}}', '{{ns}}.json')`,
so adding `'gamelib'` to `ns` is sufficient for `public/locales/{{lng}}/gamelib.json` to load.

`public/locales/en/gamelib.json` is a 4-space-indented, alphabetically-ordered object whose
top-level keys today are: app, consoleMode, discounts, downloadManager, gamepage, humble,
humbleKeys, library, login, pathSelectionBox, redeemKey, settings, sideload, status, steam,
themeSelector.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the `installFlows` strings to `gamelib.json` and make both i18next inits load the `gamelib` namespace</name>
  <files>public/locales/en/gamelib.json, src/backend/sidecar/bootstrap.ts, src/backend/main.ts, src/backend/sidecar/__tests__/gamelibNamespaceLoad.test.ts</files>

  <read_first>
    - `public/locales/en/gamelib.json` — at minimum the `consoleMode` section, whose `steamInstallBlockedTitle` / `steamInstallLibrary*` strings are the closest existing tone model for a blocked-action message. Match that voice: plain, second person, tells the user what to do next.
    - `src/backend/sidecar/bootstrap.ts` lines 439-500 — the i18next init block and its placement comment. The comment is load-bearing about ordering; do not move the block.
    - `src/backend/main.ts` lines 478-493 — the Electron-leg init, which must receive the identical option change so the two builds do not diverge.
    - `i18next-parser.config.js` — `defaultNamespace: 'translation'`, `indentation: 4`, `keySeparator: '.'`, `keepRemoved: true`. The catalog edit must preserve 4-space indentation or `pnpm i18n` output will fight the hand edit.
    - `meta/i18nCatalogChurnGuard.ts` and `meta/__tests__/i18nCatalogChurnGuard.test.ts` — the guard that fails CI on ANY changed path under `public/locales/` that is not a `gamelib.json` / `gamelib.mt.json` leaf. Its `live tree` test diffs the real working tree inside `pnpm test:ci`, so it catches hand edits.
  </read_first>

  <action>
    1. Add a new top-level `installFlows` section to `public/locales/en/gamelib.json`, inserted
       in alphabetical order between `humbleKeys` and `library`, with exactly three keys:
       - `pathRejectedTitle` — "Can't use that location"
       - `pathRejectedBodyMove` — "GameLib couldn't move this game there. The destination has to be a full folder path with no \"..\" steps in it. Pick the destination folder again."
       - `pathRejectedBodyImport` — "GameLib couldn't import from that location. The source has to be a full folder path with no \"..\" steps in it. Pick the game's folder again."
       None of the three interpolates the rejected path (T-34.6-32), and none uses `count` —
       `{{count}}` is RESERVED by i18next and fails at render time, not at extraction.
       Edit the JSON with a round-trip-verified script: load with `json.load`, assert
       `json.dumps(original, indent=4, ensure_ascii=False)` re-dump equals the file's current
       content BEFORE mutating, then mutate and dump with the same options. A naive rewrite
       swamps the commit diff.
       Do NOT touch `public/locales/en/translation.json` or any other locale file, and do not
       add these keys to any non-`en` locale by hand.

    2. In BOTH `src/backend/sidecar/bootstrap.ts` (~:480) and `src/backend/main.ts` (~:480), add
       two options to the existing `i18next.use(Backend).init({ ... })` object:
       `ns: ['translation', 'gamelib']` and `defaultNS: 'translation'`.
       Set `defaultNS` EXPLICITLY even though `'translation'` is i18next's own default — passing
       an `ns` array without it is exactly the kind of implicit behaviour that would silently
       re-home the ~103 existing backend `t()` call sites. Add a one-line comment at each site
       naming the fork's namespace split (fork strings in `gamelib.json`, upstream Heroic
       strings in `translation.json`) and stating that both sites must change together.

    3. Do not add a `gamelib:`-prefixed call anywhere yet — Task 2 wires the call sites. This
       task's job is that the namespace exists and loads.

    4. Prove the namespace actually LOADS, by effect and not by configuration presence. Add one
       test to `src/backend/sidecar/__tests__/` (a new file, `gamelibNamespaceLoad.test.ts`, is
       acceptable and keeps it out of the install-flow suite's mock-heavy environment) that
       initialises a REAL i18next instance against the REAL catalog directory — `i18next-fs-backend`
       with `loadPath` pointing at `public/locales/{{lng}}/{{ns}}.json`, `lng: 'en'`,
       `ns: ['translation', 'gamelib']`, `defaultNS: 'translation'`, `initImmediate: false` (or an
       awaited `init`) so the read completes before the assertion — and then asserts
       `t('gamelib:installFlows.pathRejectedTitle')` **called with NO default argument** returns the
       catalog value from `gamelib.json`, and specifically NOT the key string
       `'gamelib:installFlows.pathRejectedTitle'` and NOT `'installFlows.pathRejectedTitle'`.
       Read the expected value from `gamelib.json` at test time rather than hardcoding it, so the
       assertion cannot drift from the catalog.
       This is the ONLY observation that distinguishes a loaded namespace from an unloaded one:
       Task 2 requires every `t()` call to carry an inline default byte-identical to the catalog
       value, so a namespace that never loads renders the SAME STRING as one that does. A
       `grep -c` on the `ns:` array is a config-presence check and can never see the difference.
       Assert the same for `pathRejectedBodyMove` and `pathRejectedBodyImport` in the same test.
  </action>

  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "i18nCatalogChurnGuard|gamelibNamespaceLoad|installFlows"</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "import json;d=json.load(open('public/locales/en/gamelib.json'));print(sorted(d['installFlows'].keys()))"` prints exactly `['pathRejectedBodyImport', 'pathRejectedBodyMove', 'pathRejectedTitle']`, and each value is a non-empty string.
    - No upstream catalog was touched: `git diff --name-only public/locales/ | grep -v 'gamelib\(\.mt\)\?\.json$'` produces NO output. `translation.json` in particular must be absent from that list — the churn guard fails CI on it, and `translation.json` already holding fork-added blocks (`humbleKeys.*`) makes "one more key next to the others" look right when it is not.
    - The catalog is still 4-space-indented and round-trips: `python3 -c "import json;s=open('public/locales/en/gamelib.json').read();d=json.loads(s);import sys;sys.exit(0 if json.dumps(d,indent=4,ensure_ascii=False)+chr(10)==s or json.dumps(d,indent=4,ensure_ascii=False)==s else 1)"` exits 0.
    - The namespace is proven LOADED BY EFFECT, not by config presence: the new default-free resolution test passes, and it is proven non-vacuous in the failing direction — temporarily removing `'gamelib'` from the test's own `ns` array makes the assertion FAIL (record the observed failure message in the SUMMARY, then restore by editing the line back; never `git checkout -- <file>`). Without this, `T-34.6-51` is mitigated only by a grep that cannot observe its own threat.
    - Both init sites changed, and identically: `grep -c "ns: \['translation', 'gamelib'\]" src/backend/sidecar/bootstrap.ts` returns `1` and the same grep against `src/backend/main.ts` returns `1`; `grep -c "defaultNS: 'translation'" src/backend/sidecar/bootstrap.ts` returns `1` and against `src/backend/main.ts` returns `1`. A one-sided change is a build divergence, which is the failure shape REQ-34.6-06's own module header calls out for the secret store.
    - `npx jest --selectProjects Backend` is green — in particular the suites that boot the real bootstrap i18next init (`enrichmentFlows.test.ts`) must not regress.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>The three fork strings exist in the fork's own catalog, the `gamelib` namespace loads on both builds, and no upstream catalog was touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface the rejection to the user on both handlers</name>
  <files>src/backend/sidecar/installFlowRegistration.ts, src/backend/sidecar/__tests__/installFlows.test.ts</files>

  <read_first>
    - `src/backend/sidecar/installFlowRegistration.ts` — the two rejection `catch` blocks as they stand after plan 34.6-18, and `moveInstall`'s existing error-branch `showDialogBoxModalAuto` call (which documents why `_event` is omitted under the sidecar; reuse that same call shape).
    - `src/backend/sidecar/__tests__/installFlows.test.ts` — the rejection tests, especially `a rejected moveInstall still emits the terminal gameStatusUpdate "done" (never wedges in "moving")` (asserts `statuses` equals `['done']`) and `a rejected moveInstall never logs the rejected path itself`. Both must stay green unchanged.
    - `src/backend/dialog/dialog.ts` — `showDialogBoxModalAuto`'s `sendFrontendMessage('showDialog', ...)` branch, which is the path taken when there is no real `event` (every sidecar invoke handler). This is why the dialog does NOT add a `gameStatusUpdate` frame and therefore cannot break the `['done']` assertion.
  </read_first>

  <behavior>
    - `moveInstall` rejecting `'../../etc/passwd'` calls `showDialogBoxModalAuto` exactly once, with `type: 'ERROR'`, `title` resolved from `gamelib:installFlows.pathRejectedTitle` and `message` from `gamelib:installFlows.pathRejectedBodyMove`.
    - `importGame` rejecting `'../../etc/passwd'` calls `showDialogBoxModalAuto` exactly once, with `message` from `gamelib:installFlows.pathRejectedBodyImport`.
    - Neither dialog's `title` nor `message` contains the rejected path string.
    - The `gameStatusUpdate` frames on both rejection paths are still exactly `['done']`.
    - `moveInstall`'s EXISTING error branch (a real move that fails, e.g. mocked `{status:'error', error:'disk full'}`) still calls `showDialogBoxModalAuto` exactly once with the pre-existing `box.error.moving` message — the new call must not double-fire there.
  </behavior>

  <action>
    Write the four new assertions in `src/backend/sidecar/__tests__/installFlows.test.ts` FIRST
    and run the suite to confirm they fail (`mockedShowDialogBoxModalAuto` receives 0 calls on
    the rejection paths). Tee that run to
    `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/evidence/34.6-19-RED-dialog.txt`.
    The `evidence/` directory does NOT exist at HEAD, so prepend `mkdir -p` to that capture:
    `mkdir -p .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/evidence && npx jest --selectProjects Backend --testPathPattern "installFlows" 2>&1 | tee <the path above>`.
    Without it `tee` fails, the RED capture is silently lost, and the "exists, non-empty"
    criterion below fails on an otherwise-correct run. Plan 34.6-18 Task 2 creates the same
    directory, but do NOT rely on that — both plans are wave 1 and may run in either order.
    Never use `git stash`, `git reset`, or `git checkout -- <file>`.

    Then, in `src/backend/sidecar/installFlowRegistration.ts`, inside each rejection `catch`
    block, between the path-free `logError` and the terminal `sendGameStatusUpdate`, add one
    `showDialogBoxModalAuto` call. Use the SAME call shape as the file's existing error-branch
    dialog (three fields: `type`, `title`, `message`; no `_event` argument under the sidecar).
    Set `type` to `ERROR`. Resolve `title` through `i18next.t` from the key
    `gamelib:installFlows.pathRejectedTitle` in both handlers; resolve `message` from
    `gamelib:installFlows.pathRejectedBodyMove` in `moveInstall` and from
    `gamelib:installFlows.pathRejectedBodyImport` in `importGame`. Keep the `gamelib:` prefix
    explicit on every key — a `t()` call missing its prefix silently retargets the upstream
    `translation` catalog. Pass each key's catalog value from Task 1 as the inline default
    second argument, BYTE-IDENTICAL to what `gamelib.json` holds; a divergent default is inert
    when the key exists and produces a silent two-strings-for-one-message split.

    Do NOT interpolate `path` into either string. Do NOT add a second `sendGameStatusUpdate`.
    Do NOT move the dialog call outside the `error instanceof PathShapeError` branch — a
    non-`PathShapeError` throw must keep its existing rethrow-only behaviour.

    Add one more assertion pinning the catalog↔default agreement: read
    `public/locales/en/gamelib.json` in the test with `readFileSync` and assert each of the
    three catalog values appears verbatim as a `t()` default in
    `src/backend/sidecar/installFlowRegistration.ts`'s source. This is the both-directions
    check — it fails if the catalog drifts from the code OR the code drifts from the catalog.
  </action>

  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "installFlows"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/evidence/34.6-19-RED-dialog.txt` exists and shows THE NEW DIALOG TESTS failing specifically — not merely that the suite was red. Pin each new test by a distinctive substring of its own title: `grep -E '✕' <that file> | grep -c '<moveInstall dialog test title substring>'` returns `1`, and the same for the `importGame` dialog test. Quote both matched lines verbatim in the SUMMARY, and record the run's failed-test count; if it exceeds the number of new tests, name every extra failure before proceeding. A glyph count alone cannot distinguish "the new assertions failed" from "the suite failed for an unrelated reason".
    - `npx jest --selectProjects Backend --testPathPattern "installFlows"` green, test count strictly greater than after plan 34.6-18.
    - The three keys are wired, comment lines excluded: `grep -vE '^\s*(//|\*|/\*)' src/backend/sidecar/installFlowRegistration.ts | grep -c 'gamelib:installFlows\.'` returns exactly `3`.
    - No fork string leaked into an upstream namespace: `grep -c "t('installFlows\." src/backend/sidecar/installFlowRegistration.ts` returns `0` (i.e. no un-prefixed form of these keys exists).
    - The no-wedge contract survives: the pre-existing tests `a rejected moveInstall still emits the terminal gameStatusUpdate "done"` and the `importGame` reject test both still assert `['done']` and both pass, with ZERO deletions anywhere in the file: `git diff -U0 src/backend/sidecar/__tests__/installFlows.test.ts | grep -cE '^-[^-]'` returns exactly `0`, with no carve-out. Plan 34.6-18's comment and test-title edits land in a PRIOR, already-committed wave, so this plan's `git diff` cannot show them — the previous caveat exempting them was inoperative and would have licensed any deletion. If this returns non-zero, this plan deleted a line; name each and justify it in the SUMMARY.
    - The redaction contract survives: the pre-existing `a rejected moveInstall never logs the rejected path itself` test still passes, and the new dialog assertions additionally check `title`/`message` do not contain the rejected path.
    - `npx jest --selectProjects Backend` full project green; `npx tsc --noEmit` exits 0; `pnpm lint` reports 0 findings at `severity === 2`.
    - `pnpm planning-gates` shows NO NEW failure, checked HERE and not deferred to plan 34.6-20. REQ-34.6-13 requires any gate turned red by construction to be amended in the SAME commit; first detection a full wave later would breach it. The suite is already red at HEAD on ONE pre-existing, unrelated gate — `.planning/phases/34.4.1-*/seam-parity-sweep-gate.py`, stale since plan 34.6-09 wired `steamgrid` into a sidecar registration module, and OUT of this cycle's scope. The bar: the run reports `6/7` with that same gate as the only failure and its diff unchanged from HEAD's. If a SECOND gate goes red, or that diff changes, stop — this plan caused it, and amend it in this commit.
  </acceptance_criteria>

  <done>A rejected path produces a visible, localisable dialog on both channels, with the terminal-status and log-redaction contracts intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar → renderer (`showDialog` frontend message) | error text crosses into the UI, where the user can screenshot or paste it |
| repo → CI (`public/locales/**`) | the D-05 i18n churn guard treats any non-`gamelib` locale write as a defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-32 | Information Disclosure | the new user-facing dialog echoing the rejected path into a screenshot or a pasted bug report | mitigate | Neither the title nor either body string interpolates `path`; no `{{...}}` placeholder exists in any of the three keys, and a test asserts the rejected path is absent from both dialog fields. Same rule the existing `logError` already follows. |
| T-34.6-31 | Denial of Service | the added dialog perturbing the terminal-status contract and re-wedging the UI | mitigate | `showDialogBoxModalAuto` under the sidecar takes `dialog.ts`'s `sendFrontendMessage('showDialog', ...)` branch, which never throws and emits no `gameStatusUpdate` frame; the pre-existing `['done']` assertions are kept unmodified and must stay green. |
| T-34.6-50 | Tampering | a fork string written into `translation.json` (or another upstream catalog), turning CI red and re-creating the D-06 split-brain | mitigate | Keys go only into `public/locales/en/gamelib.json`; the acceptance criterion greps `git diff --name-only public/locales/` for any non-`gamelib` leaf and requires empty output. `meta/i18nCatalogChurnGuard.ts`'s `live tree` test is the independent CI backstop. |
| T-34.6-51 | Repudiation | the string appearing localisable while permanently rendering its inline English default, because the `gamelib` namespace is never loaded | mitigate | Mitigated by EFFECT, not by configuration presence. `ns: ['translation', 'gamelib']` plus an explicit `defaultNS: 'translation'` are added at BOTH init sites and pinned by `grep -c` — but a grep on the `ns:` array is blind to this threat by construction: Task 2 requires every inline `t()` default to be byte-identical to the catalog value, so an unloaded namespace renders the SAME STRING as a loaded one. The observing control is a test that initialises real i18next against the real `public/locales` and asserts `t('gamelib:installFlows.pathRejectedTitle')` **with no default argument** returns the catalog value rather than the key, proven non-vacuous by removing `'gamelib'` from the test's own `ns` array and observing the failure. |
| T-34.6-52 | Repudiation | catalog value and `t()` default drifting apart, producing two different strings for one message | mitigate | A test reads `gamelib.json` and asserts each value appears verbatim as a `t()` default in the handler source — checked in both directions, since a matching default is what makes a rename a silent no-op when the key already exists. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | **No packages are installed by this plan.** Only existing project modules, `i18next` (already a dependency and already imported in the file) and Python's stdlib `json` for the catalog edit. |
</threat_model>

<verification>
- `npx jest --selectProjects Backend --testPathPattern "installFlows|i18nCatalogChurnGuard"` — green.
- `npx jest --selectProjects Backend` — full Backend project green.
- `git diff --name-only public/locales/` lists only `public/locales/en/gamelib.json`.
- `npx tsc --noEmit` exits 0; `pnpm lint` 0 findings at `severity === 2`.
- The default-free `t('gamelib:installFlows.pathRejectedTitle')` resolution test passes against the real `public/locales` catalog, and is proven to FAIL when `'gamelib'` is dropped from its own `ns` array.
- `pnpm planning-gates` reports `6/7` with `34.4.1-*/seam-parity-sweep-gate.py` as the only failure and its diff unchanged from HEAD's.
</verification>

<success_criteria>
- A rejected `moveInstall`/`importGame` path is visible to the user, not just to `gamelib.log`.
- Every new string lives in `gamelib.json` under the `gamelib:` namespace and actually loads.
- The T-34.6-31 and T-34.6-32 contracts from plan 34.6-11 are intact and still proven by their original tests.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-19-SUMMARY.md` when done.
Record the exact three catalog key paths and the two i18next init sites changed, so a future localisation sweep can find them.
Commit with `git commit --only <explicit paths>`. Never use `git stash`, `git reset`, or `git checkout -- <file>`.
Run `graphify update .` after the source changes land.
</output>
