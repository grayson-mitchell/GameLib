---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 07
type: execute
wave: 3
depends_on: ["34.6-04", "34.6-05"]
files_modified:
  - src/backend/sidecar/wineToolsFlowRegistration.ts
  - src/backend/sidecar/__tests__/wineToolsFlows.test.ts
  - src/backend/sidecar/__tests__/enrichmentFlows.test.ts
  - src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts
autonomous: true
requirements: [REQ-34.6-03, REQ-34.6-04, REQ-34.6-07, REQ-34.6-08, REQ-34.6-13]
must_haves:
  truths:
    - "The 3 winetricks channels and `runWineCommandForGame` are registered on the sidecar and resolve with real data"
    - "A-01: no macOS decline branch is ported, because the `Winetricks` object has no `!isLinux` guard — it has genuine macOS support via `macEnvs`"
    - "D-11: `winetricksInstall` stays send-kind and emits the shared observable"
    - "Three previously-green gates are amended in the SAME commit as the change that breaks them — `wineToolsFlows.test.ts`'s absence assertion, `enrichmentFlows.test.ts`'s unported exemplar, and `runnerSliceRegistration.test.ts`'s winetricks slice of Describe 6 and 7"
    - "D-09: Zoom's 3 stay an ABSENCE assertion; the winetricks 3 flip to PRESENCE"
  artifacts:
    - path: "src/backend/sidecar/wineToolsFlowRegistration.ts"
      provides: "winetricksAvailable, winetricksInstalled (invoke) + winetricksInstall (send) + runWineCommandForGame (invoke)"
      contains: "winetricksInstall"
  key_links:
    - from: "src/backend/sidecar/wineToolsFlowRegistration.ts"
      to: "Winetricks (src/backend/tools/index.ts:528)"
      via: "direct import of the logic object, never tools/ipc_handler.ts"
      pattern: "Winetricks"
    - from: "src/backend/sidecar/wineToolsFlowRegistration.ts"
      to: "logSendHandlerReached"
      via: "the first statement of the winetricksInstall listener body"
      pattern: "logSendHandlerReached\\('winetricksInstall'\\)"
---

<objective>
Port the 3 winetricks channels plus `runWineCommandForGame` into `wineToolsFlowRegistration.ts`,
and amend — in the same commit — the three green gates this port turns red by construction.

**A-01 governs this plan and OVERRIDES D-04's original text.** D-04 cited `tools/index.ts:807` and
`:843` as `!isLinux` guards inside `Winetricks` and framed the macOS bar as "prove the guard
fired". Those lines belong to `getDxvkUrl()` and `getVkd3dUrl()` — unrelated DXVK/VKD3D
URL-selection functions that happen to sit near the `Winetricks` object. The real `Winetricks`
object begins at `tools/index.ts:528` and contains **no `!isLinux` guard anywhere**; it has a
dedicated `macEnvs` branch at `:628`, selected by `isMac ? macEnvs : linuxEnvs` at `:642`.
Winetricks has genuine macOS support. `listInstalled()` (`:755-765`) returns `[]` only when
`winetricks.log` is absent at the configured Wine prefix — DATA-dependent, not platform-gated.

**Do not port a macOS decline branch. It does not exist upstream.**

Plan 34.6-04 has already captured the pre-port `DECLARED_UNAVAILABLE_MARKER` observation (A-01's
corrected D-10 disposition). This plan destroys that evidence by design; it must not run first.

Purpose: real cross-platform data resolution through the sidecar, with zero red HEAD.
Output: 4 channels registered, 3 gates amended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-05-SUMMARY.md

<interfaces>
Electron originals (`src/backend/tools/ipc_handler.ts` — do NOT import this file):
  addHandler('runWineCommandForGame', async (event, { appName, commandParts, runner }) => {
    if (isWindows) { return execAsync(commandParts.join(' ')) }
    return runWineCommandOnGame(runner, appName, { commandParts, wait: false, protonVerb: 'runinprefix' })
  })
  addListener('winetricksInstall', async (event, runner, appName, component) =>
    Winetricks.install(runner, appName, component))
  addHandler('winetricksAvailable', async (event, runner, appName) => {
    try { return await Winetricks.listAvailable(runner, appName) } catch { return [] } })
  addHandler('winetricksInstalled', async (event, runner, appName) => {
    const game = getGame(appName, runner); return Winetricks.listInstalled(game) })

Note the argument shapes differ between the three winetricks channels: `install`/`listAvailable`
take `(runner, appName, ...)`, while `listInstalled` takes a resolved GAME OBJECT from
`getGame(appName, runner)`. Preserve both exactly.

Landed by plan 34.6-05:
  src/backend/sidecar/sendChannelObservable.ts
    export const SEND_HANDLER_MARKER = '[GAMELIB_SIDECAR_SEND_HANDLER]'
    export function logSendHandlerReached(channel: string): void

Existing send-kind idiom in the sidecar (`shortcutsFlowRegistration.ts`'s addShortcut/
processShortcut/removeShortcut, `runnerAuthFlowRegistration.ts`'s logoutGOG). Note that
`wineToolsFlowRegistration.ts` itself currently has ZERO `ipcMain.on(...)` calls — all 9 of its
channels are invoke — so this is a first for THIS file, but not a new idiom in the codebase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register the 3 winetricks channels and `runWineCommandForGame` in `wineToolsFlowRegistration.ts`</name>
  <read_first>
    - `src/backend/tools/ipc_handler.ts` in full (78 lines — the four handler bodies to port; this file is the SOURCE, never an import)
    - `src/backend/tools/index.ts` lines 528-800 (the `Winetricks` object: `install`, `listAvailable`, `listInstalled`, the `macEnvs` branch at :628 and `isMac ? macEnvs : linuxEnvs` at :642 — confirm for yourself that no `!isLinux` guard exists in this object)
    - `src/backend/sidecar/wineToolsFlowRegistration.ts` in full (243 lines — its docstring naming these exact channels as Phase 34.6's, its curated-import list, its `ipcMain.handle` idiom)
    - `src/backend/sidecar/shortcutsFlowRegistration.ts` (the `ipcMain.on` send-kind idiom with a guarded body)
    - `src/backend/sidecar/sendChannelObservable.ts` (landed by plan 34.6-05)
  </read_first>
  <files>src/backend/sidecar/wineToolsFlowRegistration.ts</files>
  <behavior>
    - `registerWineToolsFlows()` puts `winetricksAvailable`, `winetricksInstalled` and `runWineCommandForGame` in `handlerRegistry`, and `winetricksInstall` in `listenerRegistry` only.
    - `winetricksAvailable` returns `[]` when the underlying `Winetricks.listAvailable` throws — matching the Electron original's `try/catch { return [] }` exactly.
    - `winetricksInstalled` resolves the game via `getGame(appName, runner)` and passes the GAME OBJECT to `Winetricks.listInstalled`, not the appName.
    - Invoking the `winetricksInstall` listener calls `logSendHandlerReached('winetricksInstall')` as its first statement, then `Winetricks.install(runner, appName, component)`.
    - A rejection from `Winetricks.install` is caught and logged; it never escapes the listener.
    - `runWineCommandForGame` on non-Windows forwards `{ commandParts, wait: false, protonVerb: 'runinprefix' }` to `runWineCommandOnGame` with `commandParts` UNCHANGED.
  </behavior>
  <action>
Extend `registerWineToolsFlows()` in `src/backend/sidecar/wineToolsFlowRegistration.ts` with four
registrations, bodies copied from `tools/ipc_handler.ts` with no semantic change:

  - `ipcMain.handle('winetricksAvailable', ...)` — invoke-kind, preserving the `catch { return [] }`.
  - `ipcMain.handle('winetricksInstalled', ...)` — invoke-kind, preserving the
    `getGame(appName, runner)` resolution before calling `Winetricks.listInstalled`.
  - `ipcMain.on('winetricksInstall', ...)` — SEND-kind (D-11 keeps it send-kind; converting to
    invoke would smuggle a behaviour change into a port immediately before the cutover). First
    statement: `logSendHandlerReached('winetricksInstall')`. Wrap the whole body so a rejection is
    caught and logged — never a bare `.catch()`, and never left unguarded: send-kind failures are
    structurally silent to the renderer, so the caught-and-logged error is the only observability
    that exists.
  - `ipcMain.handle('runWineCommandForGame', ...)` — invoke-kind, preserving the `isWindows`
    `execAsync(commandParts.join(' '))` branch verbatim. **Add NO validation to `commandParts`.**
    This channel carries `T-34.5-C6-49-03` and ports as-is under D-02; plan 34.6-11 hardens it,
    separately committed. Record `T-34.5-C6-49-03` and plan 34.6-11 in an inline comment here.

Import `Winetricks` and `runWineCommandOnGame` from `../tools`, `getGame` and `execAsync` from
`../utils`, `isWindows` from `../constants/environment`. **Never import `tools/ipc_handler.ts`** —
`wineToolsFlows.test.ts`'s `T-34.5-15` curated-import guard asserts no `ipc_handler` substring
appears in this file, and that guard must stay green.

Do NOT add any macOS branch, guard or decline path. Per A-01 the upstream object has none, and a
comment referencing "the `!isLinux` guard in Winetricks" would carry a citation that this phase has
already falsified.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "wineToolsFlows" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle('winetricksAvailable'\|ipcMain.handle('winetricksInstalled'\|ipcMain.handle('runWineCommandForGame'" src/backend/sidecar/wineToolsFlowRegistration.ts` returns 3.
    - `grep -c "ipcMain.on('winetricksInstall'" src/backend/sidecar/wineToolsFlowRegistration.ts` returns 1, and `grep -c "ipcMain.handle('winetricksInstall'" ...` returns 0.
    - `grep -n "logSendHandlerReached('winetricksInstall')" src/backend/sidecar/wineToolsFlowRegistration.ts` returns exactly one match.
    - `grep -vc '^ *[/*]' src/backend/sidecar/wineToolsFlowRegistration.ts` followed by `grep -v '^ *[/*]' src/backend/sidecar/wineToolsFlowRegistration.ts | grep -c 'ipc_handler'` returns 0 — the curated-import guard property, checked on comment-stripped source the same way the test does.
    - `grep -in 'isLinux' src/backend/sidecar/wineToolsFlowRegistration.ts` returns no match introduced by this plan.
    - `grep -n 'T-34.5-C6-49-03' src/backend/sidecar/wineToolsFlowRegistration.ts` returns a match naming plan 34.6-11.
    - The `commandParts` pass-through is RED-proven: inserting a shell-metacharacter rejection into the `runWineCommandForGame` body makes the pass-through test fail. Revert after proving.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Four channels registered, one of them send-kind with the D-11 observable, no invented platform guard, no smuggled validation.</done>
</task>

<task type="auto">
  <name>Task 2: Amend `wineToolsFlows.test.ts`'s now-false absence assertion and `enrichmentFlows.test.ts`'s unported exemplar</name>
  <read_first>
    - `src/backend/sidecar/__tests__/wineToolsFlows.test.ts` lines 120-150 (the `T-34.5-15` describe block: the curated-import guard that STAYS, and the four-channel absence assertion at :133 that becomes false)
    - `src/backend/sidecar/__tests__/enrichmentFlows.test.ts` lines 985-1015 (the `REQ-34.2-14` unported-channel test whose exemplar is currently `winetricksInstall`, its third substitution after `checkDiskSpace` and `getLegendaryVersion`)
    - `src/preload/api/zoom.ts` (`authZoom = makeHandlerInvoker('authZoom')` — invoke-kind, and permanently dropped by 34.5 D-02, therefore a channel that can never be ported)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md` § Deltas 1 and 2
  </read_first>
  <files>src/backend/sidecar/__tests__/wineToolsFlows.test.ts, src/backend/sidecar/__tests__/enrichmentFlows.test.ts</files>
  <action>
**`wineToolsFlows.test.ts:133` (PATTERNS delta 1).** The test
`'T-34.5-15 none of the three DEFERRED winetricks channels, nor runWineCommandForGame, is present
in either registry'` asserts absence of `winetricksAvailable`, `winetricksInstall`,
`winetricksInstalled` and `runWineCommandForGame`. All four are now registered. Rewrite it to
assert PRESENCE with the correct KIND per channel — `handlerRegistry.has(c) === true` and
`listenerRegistry.get(c) ?? [] === []` for the three invoke channels, and the inverse for
`winetricksInstall`. Rename the test so its title states what it now measures; leave the
`T-34.5-15` tag so the provenance chain survives. Do NOT delete it — deleting drops the only
kind-correctness assertion for these four channels, and a mis-registered send channel fails
SILENTLY under the sidecar.

Leave the curated-import guard test in the same describe UNCHANGED and green.

**`enrichmentFlows.test.ts` (PATTERNS delta 2).** The `REQ-34.2-14` test uses `winetricksInstall`
as its live "still unported" example. That exemplar has already moved twice
(`checkDiskSpace` -> `getLegendaryVersion` -> `winetricksInstall`) as each was ported in turn;
this is a recurring, intentional pattern in the file, not a one-off. Substitute it to
**`authZoom`**, and update the inline comment to say WHY this substitution is different from the
previous three: `authZoom` is not merely deferred, it is **permanently DROPPED** by 34.5 D-02, so
it cannot be ported by a future slice and this exemplar will not need substituting again. Verify
before committing that `authZoom` is invoke-kind (`src/preload/api/zoom.ts:4`,
`makeHandlerInvoker`) — a send-kind exemplar would never surface `UNPORTED_CHANNEL_MARKER` and the
test would go vacuously green.

Do not touch `runnerSliceRegistration.test.ts` in this task; task 3 owns it.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "wineToolsFlows|enrichmentFlows" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Both suites pass with 0 failures.
    - `grep -n 'winetricksInstall' src/backend/sidecar/__tests__/enrichmentFlows.test.ts` returns no match inside the `REQ-34.2-14` test, and `grep -n 'authZoom' ...` returns a match there.
    - The substituted exemplar is proven non-vacuous: the `REQ-34.2-14` test still asserts `UNPORTED_CHANNEL_MARKER` is returned, and temporarily registering `authZoom` on the sidecar makes it FAIL. Revert after proving.
    - `grep -n "T-34.5-15" src/backend/sidecar/__tests__/wineToolsFlows.test.ts` returns 2 matches (the curated-import guard and the rewritten kind assertion).
    - The rewritten presence assertion is RED-proven in the other direction: removing the `ipcMain.on('winetricksInstall', ...)` registration makes it fail with a message naming the kind mismatch, not merely "not found". Revert after proving.
  </acceptance_criteria>
  <done>Two gates that this port turns false are corrected in the same commit, neither weakened nor deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the winetricks slice of `runnerSliceRegistration.test.ts`'s Describe 6 and Describe 7, arithmetic re-derived</name>
  <read_first>
    - `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` lines 75-100 (the module-scope registration block that calls exactly the four Phase-34.5 modules) and lines 400-541 (Describe 6's `SEND_CHANNELS` array and its `34` / `4` / `[11, 9, 7, 11]` / `38` pins, and Describe 7's `DROPPED_ZOOM` / `DEFERRED_*` arrays and its `toBe(19)`)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PATTERNS.md` § 5 (the module-scope scope finding) and § "Deltas" item 3
    - `src/backend/sidecar/wineToolsFlowRegistration.ts` (as modified by task 1 — count its registrations directly, do not assume)
  </read_first>
  <files>src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts</files>
  <action>
`registerWineToolsFlows()` is ALREADY called at this file's module scope (line ~88), so the four
new channels are already visible to both describes. No new import is needed in this task — that is
the EOS and SteamGridDB problem, owned by plans 34.6-08 and 34.6-09.

**Describe 7 (D-09).** Remove the three names from `DEFERRED_WINETRICKS` so that array becomes
empty (or delete the array and its spread). `DROPPED_ZOOM`'s 3 names STAY, asserting absence —
34.5 D-02 dropped Zoom permanently and Describe 7's absence assertion is the only thing in the
repo keeping it dropped. Re-derive `expect(DROPPED_OR_DEFERRED.length).toBe(N)` by REMOVING the
names, not by retyping the literal against an unchanged array: an edited literal that no longer
matches the array contents is a vacuous self-assertion. After this task the literal is `16`
(19 - 3); plans 34.6-08 and 34.6-09 take it to 8 and then 3.

Add a NEW `it.each` (or a new describe alongside Describe 7, following this file's own existing
`MODULES`-driven presence idiom at lines 174-204) asserting PRESENCE for the three winetricks
channels with the correct kind: `winetricksAvailable`/`winetricksInstalled` in `handlerRegistry`,
`winetricksInstall` in `listenerRegistry` with length > 0 and ABSENT from `handlerRegistry`.

**Describe 6.** Its counts are scoped to the four Phase-34.5 modules and all four new channels
land in one of them, so every pin moves. Recompute each by MEASURING, not by arithmetic on this
plan's prose:
  - `expect(handleChannels.length).toBe(34)` -> the measured new total.
  - `expect(listenChannels.length).toBe(4)` -> the measured new total.
  - `SEND_CHANNELS` -> add `'winetricksInstall'`. This is a SET-EQUALITY check, not size-only,
    deliberately: a size-only check would pass if two channels swapped kinds.
  - `expect(counts).toEqual([11, 9, 7, 11])` and `expect(...).toBe(38)` -> update the
    `wineToolsFlowRegistration` element and the total. Also update that module's own
    `declaredChannels` array in the `MODULES` table so the per-module completeness `it.each` covers
    the new channels — a count bumped without the names added would pass while measuring nothing.

Run the suite and take the failing expectations' ACTUAL values as the source of truth for each
number; then re-derive each independently and confirm they agree. If they disagree, stop and report
— a disagreement means a channel was registered somewhere unexpected.
  </action>
  <verify>
    <automated>npx jest --selectProjects Backend --testPathPattern "runnerSliceRegistration" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The whole `runnerSliceRegistration` suite passes with 0 failures.
    - `grep -n "toBe(16)" src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` returns a match inside Describe 7's self-containment test, and the `DEFERRED_WINETRICKS` names no longer appear in the absence array.
    - `grep -n "'authZoom'\|'getZoomUserInfo'\|'logoutZoom'" src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` still returns matches inside `DROPPED_ZOOM`, still asserting ABSENCE.
    - `grep -n "'winetricksInstall'" src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` returns a match inside `SEND_CHANNELS`.
    - Both directions are RED-proven, per D-09: (a) deleting the `ipcMain.on('winetricksInstall', ...)` registration makes the new presence assertion FAIL; (b) adding a fake `ipcMain.handle('authZoom', ...)` to any of the four modules makes the Zoom absence assertion FAIL. Revert both after proving, and record the two failure messages in the SUMMARY.
    - The SUMMARY records the MEASURED new values for the four Describe 6 pins alongside the independently re-derived values, and states that they agreed.
  </acceptance_criteria>
  <done>Describe 6 and Describe 7 reflect the winetricks port, with the arithmetic derived from measurement and both directions RED-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar (`runWineCommandForGame`) | a renderer-supplied `commandParts` array reaches a Wine process, and on Windows reaches `execAsync(commandParts.join(' '))` — a shell |
| renderer → sidecar (`winetricksInstall`) | a component name reaches a winetricks invocation, fire-and-forget, with no response frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C6-49-03 | Tampering | `runWineCommandForGame` — a renderer-supplied command string reaching a Wine process, and a shell on the Windows branch | mitigate (DEFERRED WITHIN THIS PHASE, by D-02) | Ported as-is here to keep a live-gate FAIL bisectable; hardened by plan 34.6-11, separately committed, inside this phase. Recorded in an inline comment naming 34.6-11. |
| T-34.6-19 | Tampering | `winetricksInstall`'s `component` argument reaching a winetricks invocation with no response frame | mitigate | Ported byte-equivalently from the Electron original, so no new exposure is introduced; the D-11 observable makes the invocation visible in `gamelib.log` for the first time, which is what makes plan 34.6-11's hardening reviewable at all. |
| T-34.6-13 | Information Disclosure | the D-11 observable echoing the component name or appName | mitigate | `logSendHandlerReached` takes only a channel name — the argument cannot be passed. |
| T-34.6-20 | Repudiation | a mis-registered send channel failing silently and going unnoticed | mitigate | Task 2's rewritten `T-34.5-15` assertion and task 3's `SEND_CHANNELS` set-equality check both assert KIND, not merely presence; both are RED-proven. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed. |
</threat_model>

<verification>
- `npx jest --selectProjects Backend --testPathPattern "wineToolsFlows|enrichmentFlows|runnerSliceRegistration"` — green, 0 failures.
- `npx tsc --noEmit` — exits 0.
- `pnpm build:sidecar && node build/main/sidecar.js` — starts and prints the ready sentinel.
- `npm start` (Electron) unchanged — `tools/ipc_handler.ts` is untouched.
- HEAD is green at the end of this plan: no gate is left red for a later plan to close.
</verification>

<success_criteria>
- `winetricksAvailable`, `winetricksInstalled`, `winetricksInstall` and `runWineCommandForGame` are registered with the correct kind.
- No macOS decline branch was invented (A-01), and no validation was smuggled into the T-34.5-C6-49-03 channel (D-02).
- `wineToolsFlows.test.ts`, `enrichmentFlows.test.ts` and `runnerSliceRegistration.test.ts` are all green in the same commit as the port.
- Zoom's 3 still assert absence; the winetricks 3 assert presence; both RED-proven.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-07-SUMMARY.md` when done, recording the two RED-proof failure messages and the measured Describe 6 values.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
Run `graphify update .` after the source changes land.
</output>
