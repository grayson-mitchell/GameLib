---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
verified: 2026-08-26T08:04:09Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
re_verification:
  run: 2
  previous_verified: 2026-08-25T09:41:30Z
  previous_status: gaps_found
  previous_score: 8/9
  gaps_closed:
    - "REQ-34.6-05's T-34.5-C6-49-03 path-containment hardening protects moveInstall/importGame without breaking their primary real-world use case (CR-01) — closed by gap plans 34.6-18/19/20/21; independently re-measured 2026-08-26, see the RE-VERIFICATION section"
  gaps_remaining: []
  regressions: []
human_verification: []
deferred: # Coverage limits NOT met on this hardware, explicitly owned by a later milestone phase — not actionable gaps
  - truth: "enableEosOverlay / disableEosOverlay / isEosOverlayEnabled are behaviourally exercised (registration IS verified; behaviour is not)"
    addressed_in: "Phase 38"
    evidence: "Phase 38 goal: 'Discharge, in one deliberate sweep, every UAT item across the project that cannot be run on this machine because it needs hardware or an OS this project does not have.' All three sit behind `isWindows` at src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx:133 and :401."
  - truth: "runWineCommandForGame is behaviourally exercised from a renderer call site"
    addressed_in: "Phase 38"
    evidence: "Zero renderer call sites (`grep -rn runWineCommandForGame src/frontend/` = 0, re-confirmed 2026-08-26). Its only live surface is Windows-side; same Phase 38 hardware sweep."
open_operator_decisions: # NOT verification blockers — the phase goal is achieved without these. Recorded so they are not laundered by a `passed` status.
  - id: G-34.6-UAT-03
    decision: "Whether to re-drive closed live-gate Step 4 (winetricksInstall) as a proper gate run now that the winetricks dependencies are present"
    why_open: "34.6-UAT.md test 5 observed the Step 4 symptom NOT reproducing (first-ever `[GAMELIB_SIDECAR_SEND_HANDLER] winetricksInstall`), but the experiment is confounded — `brew install p7zip cabextract zenity` and the package list becoming selectable moved together. Two prior explanations for this defect were already disproven. A UAT observation is not a gate re-drive."
    must_not: "Do NOT flip Step 4 on the strength of the UAT. `34.6-LIVE-GATE.md` stays at `verdict: FAIL 7/9`, `failing_items: [4, 8]` until an operator decides otherwise."
    owner: operator
  - id: G-34.6-UAT-01
    decision: "Whether to file a todo for the EOS overlay 'Check for Updates' opaque failure when no Epic session exists"
    why_open: "UX gap behind a working channel (legendary status exits 1 with a 401). Explicitly NOT a port defect and explicitly must not hold the phase."
    owner: operator
# --- Preserved verbatim from verification run 1 (2026-08-25T09:41:30Z). This block is
# --- the historical record ONLY; the live status/score/gaps for this document are the
# --- keys above. Kept because run 1 is what gap plans 34.6-18..21 were written against.
superseded_run_1:
  phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
  verified: 2026-08-25T09:41:30Z
  status: gaps_found
  score: 8/9 must-haves verified
  overrides_applied: 0
  gaps:
    - truth: "REQ-34.6-05's T-34.5-C6-49-03 path-containment hardening protects moveInstall/importGame without breaking their primary real-world use case"
      status: failed
      reason: "assertContainedPath gates moveInstall/importGame against GlobalConfig.get().getSettings().defaultInstallPath as the containment root. Both features exist specifically to reference locations OUTSIDE that root: moveInstall's onMoveInstallYesClick (GameSubMenu/index.tsx:97-109) opens a native openDialog with properties:['openDirectory'] and no path constraint (the whole point is moving to another drive/external disk); importGame's ImportDialog (InstallModal/ImportDialog/index.tsx:106-116) uses PathSelectionBox unconstrained by defaultInstallPath, because its purpose is pointing at an ALREADY-installed game located elsewhere on disk. openDialog's shared implementation (src/backend/utils/openDialog.ts) is a bare native OS picker with zero root enforcement. A path returned from either picker that sits outside defaultInstallPath resolves via assertContainedPath's relative()-based check to a string starting with '..', which throws PathContainmentError and silently aborts the operation (terminal 'done' status emitted with no path-specific user message). Independently re-verified against both call sites, the shared dialog handler, and the guard implementation (src/backend/sidecar/rendererPathGuard.ts) — matches 34.6-REVIEW.md CR-01 exactly, confirmed 2026-08-25T09:32:09Z, AFTER 34.6-11-SUMMARY.md and REQUIREMENTS.md's REQ-34.6-05 both already declared 'REQUIREMENT NOW FULLY COMPLETE.' installFlows.test.ts's own coverage only exercises 'inside the configured root' (legitimate) and 'escaping the configured install root' (rejected) — it never exercises the real, intended cross-root use case, so the green suite pins the wrong behavior as correct rather than catching the regression."
      artifacts:
        - path: "src/backend/sidecar/installFlowRegistration.ts"
          issue: "moveInstall (:258-296) and importGame (:342-379) call assertContainedPath(GlobalConfig.get().getSettings().defaultInstallPath, path, ...) as their first statement, rejecting any renderer-supplied destination/source outside that single configured directory"
        - path: "src/backend/sidecar/rendererPathGuard.ts"
          issue: "assertContainedPath is a correct containment primitive in isolation, but its callers apply it against the wrong trust boundary for these two channels (a config value the same process writes, not a renderer-tamper boundary)"
      missing:
        - "Either drop containment for moveInstall/importGame specifically (already gated by an OS-native picker, a stronger trust boundary) and validate only that the path is a plausible absolute filesystem path, or widen the root to reflect 'anywhere the OS file picker can reach' and gate tamper-resistance on a different signal (e.g. the path was actually returned by a real openDialog() call this session)"
        - "A live-gate/manual test exercising moveInstall to a path outside defaultInstallPath and importGame from a path outside it, so this class of regression is caught before the next live gate"
        - "REQUIREMENTS.md's REQ-34.6-05 entry corrected — it currently reads 'REQUIREMENT NOW FULLY COMPLETE' with no record of this regression"
  human_verification: []
---

# Phase 34.6: Tauri IPC re-plumb slice 9 — EOS overlay, SteamGridDB artwork, winetricks + inherited Epic/save-sync verification

**Phase Goal:** Port the remaining deferred/late-discovered IPC channels onto the Node sidecar under the Rust/Tauri shell — EOS overlay, SteamGridDB artwork, winetricks, plus the Epic/save-sync verification legs inherited from parked Phase 34.7. Scope is 24 channels (D-01 absorbed the 8 "Late-discovered — owner Phase 34.6" entries).
**Verified:** 2026-08-25T09:41:30Z
**Status:** gaps_found
**Re-verification:** No — initial verification

> ⚠️ **THIS SECTION IS THE RUN-1 RECORD (2026-08-25) AND IS PRESERVED UNCHANGED.** The live status
> of this document is **`passed`, 9/9**, set by **verification run 2 on 2026-08-26** — see
> [`# RE-VERIFICATION — run 2`](#re-verification--run-2-2026-08-26t080409z) at the bottom of this
> file, and the frontmatter above. Run 1's own frontmatter is preserved under `superseded_run_1:`.
> The single gap run 1 found (CR-01) is **CLOSED**. What has NOT changed:
> `34.6-LIVE-GATE.md` is still closed at `verdict: FAIL 7/9`, `failing_items: [4, 8]`.

## Framing this verification does NOT re-litigate

`34.6-LIVE-GATE.md` closed **FAIL 7/9** (7 PASS, 2 FAIL, 0 NOT ATTEMPTED) by an explicit, recorded operator decision (option (c): close FAIL with both failures named and owned; `34.6-17-SUMMARY.md`). That verdict is treated here as an **owned outcome, not a fresh finding**:

- **Step 4 (`winetricksInstall`)** — independently confirmed NOT an IPC/port defect. The channel is genuinely registered (`wineToolsFlowRegistration.ts:335`, `ipcMain.on('winetricksInstall', ...)`, calls `logSendHandlerReached` first), its jest coverage is green, and `frontendReady` (the phase's *other* send-kind channel, sharing the same transport) fired live and PASSED — which is what localises the defect to a renderer-side mouse/keyboard asymmetry outside this phase's porting surface. Parked at `.planning/todos/pending/2026-08-24-winetricksinstall-send-channel-is-a-live-silent-no-op.md` with no `resolves_phase` (correctly, since 34.6 never owned the defect's actual location).
- **Step 8 (Epic logout)** — a wry-layer cookie-clear defect on `clearEpicCookies`, a channel this phase never ported (34.4.1 owns it). Out of this phase's porting scope by construction.

Neither failure is re-scored below. What this verification DOES add is one finding raised after the phase's own closing artifacts were written (`34.6-REVIEW.md`, timestamped 09:32:09Z on 2026-08-25 — after `34.6-PORTED-CHANNELS.md` at 21:11 and `REQUIREMENTS.md`'s REQ-34.6-05 entry, both already reading "REQUIREMENT NOW FULLY COMPLETE"): **CR-01**, independently re-verified below.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The 8 EOS overlay channels are registered on the sidecar, invoke-kind, from a dedicated module | ✓ VERIFIED | `src/backend/sidecar/eosOverlayFlowRegistration.ts:71-119` registers all 8 (`getEosOverlayStatus`, `getLatestEosOverlayVersion`, `updateEosOverlayInfo`, `installEosOverlay`, `removeEosOverlay`, `enableEosOverlay`, `disableEosOverlay`, `isEosOverlayEnabled`) each calling the real `eos_overlay.ts` function, not a stub; wired in `handlers.ts:95,218`. `eosOverlayFlows.test.ts` green (part of the 278/278 run below). Live-gate item 2 PASS (macOS-scoped: install/remove round-trip). `enableEosOverlay`/`disableEosOverlay` have no reachable macOS UI surface — declared honestly in `34.6-PORTED-CHANNELS.md`, not a porting defect. |
| 2 | The 5 SteamGridDB channels are registered, routed exclusively through a keyring-backed secret store (never plaintext) | ✓ VERIFIED | `src/backend/sidecar/enrichmentFlowRegistration.ts` registers all 5 `steamgriddb.*` handlers via `getSteamGridDbSecretStore()`; `src-tauri/src/main.rs:276` has the `"steamgrid-api-key"` keyring arm with unit tests (:7780-7803); `steamgridSecretStore.ts` implements `SidecarSteamGridDbSecretStore`. Live-gate item 3 PASS: real key entered, confirmed in Keychain, confirmed absent from `config.json`. Non-blocking warnings WR-01 (unvalidated key type) / WR-02 (no trim/whitespace-clear parity with Electron) noted in `34.6-REVIEW.md`, not must-have blockers. |
| 3 | The 3 winetricks channels are registered with real (not decline-branch) macOS behavior | ✓ VERIFIED | `wineToolsFlowRegistration.ts:301-338` registers `winetricksAvailable`/`winetricksInstalled` (invoke) and `winetricksInstall` (send, `logSendHandlerReached` first). A-01 confirmed in-code: no `!isLinux`/`isMac` decline branch. `winetricksAvailable`/`winetricksInstalled` resolved with real data live (item 4's evidence); `winetricksInstall` itself is the Step 4 renderer-side failure described above — not a registration defect. |
| 4 | The 8 late-discovered channels (`frontendReady`, `importGame`, `moveInstall`, `runWineCommandForGame`, `getAchievements`, `getDefaultSavePath`, `getPlaytimeFromRunner`, `getGogDiscounts`) are registered | ✓ VERIFIED | Confirmed present with real bodies across `appShellFlowRegistration.ts`, `installFlowRegistration.ts`, `wineToolsFlowRegistration.ts`, `runnerMiscFlowRegistration.ts`, `enrichmentFlowRegistration.ts`. Live-gate item 5: 6 PASS + 1 discharged via item 7 + `runWineCommandForGame` correctly removed from the scored denominator as UNREACHABLE-BY-CONSTRUCTION (zero renderer call sites, `grep -rn runWineCommandForGame src/frontend/` empty — re-confirmed). Product-level defects found BEHIND `getDefaultSavePath`, `importGame`, and `moveInstall` are documented honestly in `34.6-PORTED-CHANNELS.md` as separate, filed todos — not porting defects. |
| 5 | REQ-34.6-05's T-34.5-C6-49-03 hardening discharges the tampering threat on `moveInstall`/`importGame`/`runWineCommandForGame` without regressing legitimate use | ✗ FAILED | See Gaps below (CR-01). The guard is real, correctly shaped (containment via `resolve()`/`relative()`, not a character allowlist — REQ-37-06 respected), and correctly removes the shell from `runWineCommandForGame`'s Windows branch. But `moveInstall`/`importGame` gate against `GlobalConfig.get().getSettings().defaultInstallPath`, and both features' entire purpose is referencing a location OUTSIDE that root. Independently re-verified against both frontend call sites and the shared `openDialog` picker (zero path constraint). |
| 6 | One blocking live gate ran, sequenced, with an honest per-item verdict and no partial-pass laundering | ✓ VERIFIED | `34.6-LIVE-GATE.md` authored 2026-08-23 with empty result slots, executed 2026-08-24/25 by plan 34.6-12, amended via three explicit SUPERSEDES sections (34.6-15/16/17) that only touch items whose own text changed — Steps 4 and 8 are explicitly untouched by any amendment. Closed `status: complete`, `verdict: FAIL 7/9` (7 PASS/2 FAIL/0 NOT ATTEMPTED of 9 scored items). No amendment re-scopes a failing item out of the verdict. |
| 7 | The 4 folded todos (Epic logout cookie clear, `keyring_get` timeout, F-9 RPC timeout, Humble 404) get explicit dispositions against their own stricter bars, none falsely closed | ✓ VERIFIED | All 4 confirmed still in `.planning/todos/pending/`: `2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md`, `2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md` (the D-13 identity-based re-measurement, which found an actual defect), Humble 404 todo, and the timeout todo. `34.6-14-SUMMARY.md` states "All 4 folded todos stay pending — none close." None found in `.planning/todos/completed/`. |
| 8 | `preload-surface-gate.py` re-run at phase close, self-test proven first, findings correctly absorbed/halted | ✓ VERIFIED | Independently re-ran (not merely trusted): `python3 .../preload-surface-gate.py --self-test` → all 6 checks correctly rejected their bad-input fixtures, exit 0. `python3 .../preload-surface-gate.py` (live) → `distinct invoke=160, send=60, union=220, push=30, unlisted=0`, exit 0 — matches `34.6-13-SUMMARY.md`'s claimed 220/unlisted=0 exactly. |
| 9 | Closing artefacts (`34.6-PORTED-CHANNELS.md`, `IPC-PORT-INVENTORY.md` reconciliation, `U-34.5-32` split disposition) are written from measured results, not planning-time intent | ✓ VERIFIED | `34.6-PORTED-CHANNELS.md` declares all 24 channels with per-channel proof level (unit / unit+LIVE with a specific gate-item citation / declared caveat), cites specific gate items for every LIVE claim, states plainly where evidence is only `unit`, and does not paper over the FAIL 7/9 verdict or `runWineCommandForGame`'s zero-caller status. |

**Score:** 8/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/backend/sidecar/eosOverlayFlowRegistration.ts` | 8 EOS channels, invoke-kind | ✓ VERIFIED | Real handler bodies, wired into `handlers.ts` |
| `src/backend/sidecar/enrichmentFlowRegistration.ts` | 5 SteamGridDB + `getGogDiscounts` | ✓ VERIFIED | Routes through `getSteamGridDbSecretStore()` only |
| `src/backend/sidecar/wineToolsFlowRegistration.ts` | 3 winetricks + `runWineCommandForGame` | ✓ VERIFIED | Send-kind channel calls `logSendHandlerReached` first; `assertCommandParts` guard present |
| `src/backend/sidecar/installFlowRegistration.ts` | `importGame`, `moveInstall` | ⚠️ HARDENING DEFECTIVE | Channels themselves execute end-to-end; the containment guard added on top rejects legitimate cross-root paths (see gap) |
| `src/backend/sidecar/appShellFlowRegistration.ts` | `frontendReady` (send-kind) | ✓ VERIFIED | `logSendHandlerReached` fires live, item 5 PASS |
| `src/backend/sidecar/runnerMiscFlowRegistration.ts` | `getAchievements`, `getDefaultSavePath`, `getPlaytimeFromRunner` | ✓ VERIFIED | All 3 registered, live-driven |
| `src/backend/sidecar/rendererPathGuard.ts` | `assertContainedPath`/`assertCommandParts`, no character allowlist | ✓ VERIFIED (primitive) / ✗ MISAPPLIED (2 of 3 call sites) | Primitive itself is correctly shaped; wrong trust boundary chosen for `moveInstall`/`importGame` |
| `src-tauri/src/main.rs` | `"steamgrid-api-key"` keyring arm | ✓ VERIFIED | `:276`, with mapping/collision/near-miss unit tests |
| `.planning/phases/.../34.6-PORTED-CHANNELS.md` | Honest 24-channel proof ledger | ✓ VERIFIED | Exists, per-channel citations, does not overstate proof level |
| `.planning/IPC-PORT-INVENTORY.md` | Reconciled to 24 ported, `unlisted=0` | ✓ VERIFIED | Independently re-derived: `unlisted=0`, exit 0 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `handlers.ts` | `eosOverlayFlowRegistration.ts` | `registerEosOverlayFlows()` call | ✓ WIRED | `handlers.ts:95,218` |
| `GameSubMenu/index.tsx` (`onMoveInstallYesClick`) | `installFlowRegistration.ts`'s `moveInstall` | `window.api.moveInstall({appName, path, runner})` | ⚠️ WIRED BUT REGRESSED | Reaches the handler; handler then rejects any path the native picker returns outside `defaultInstallPath` — the picker's primary intended output |
| `ImportDialog/index.tsx` | `installFlowRegistration.ts`'s `importGame` | `window.api.importGame({...})` | ⚠️ WIRED BUT REGRESSED | Same failure mode, source-side |
| `enrichmentFlowRegistration.ts` | `steamgridSecretStore.ts` | `getSteamGridDbSecretStore()` | ✓ WIRED | No `GlobalConfig`/`secureKey` fallback path remains (import-gate test) |
| `preload-surface-gate.py` | `IPC-PORT-INVENTORY.md` | static extraction + diff | ✓ WIRED | Live run: `unlisted=0` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REQ-34.6-01 | 34.6-08 | 8 EOS overlay channels registered | ✓ SATISFIED | Code + tests confirmed above |
| REQ-34.6-02 | 34.6-09 | 5 SteamGridDB channels registered via keyring seam | ✓ SATISFIED | Code + tests confirmed above |
| REQ-34.6-03 | 34.6-07 | 3 winetricks channels registered, no false decline branch | ✓ SATISFIED | Code + A-01 confirmed in-code |
| REQ-34.6-04 | 34.6-05/06/07/09/10 | 8 late-discovered channels registered | ✓ SATISFIED | All 8 confirmed present |
| REQ-34.6-05 | 34.6-11 | T-34.5-C6-49-03 hardening discharged | ✗ BLOCKED | CR-01: hardening breaks the primary real-world use case of the two features it protects. REQUIREMENTS.md's "REQUIREMENT NOW FULLY COMPLETE" cannot honestly stand — see gap. |
| REQ-34.6-06 | 34.6-01/02 | SteamGridDB key never persists in plaintext under the sidecar | ✓ SATISFIED | Rust arm + seam + migration confirmed; WR-01/WR-02 are non-blocking warnings, not failures of this requirement's core ask |
| REQ-34.6-07 | 34.6-05/07 | Send-kind channels keep send-kind, gain observable | ✓ SATISFIED | `logSendHandlerReached` present on both `frontendReady` and `winetricksInstall` |
| REQ-34.6-08 | 34.6-07/08/09/10 | 24-channel census, both-directions RED-proven | ✓ SATISFIED | `flowRegistrationCensus.test.ts`/`runnerSliceRegistration.test.ts` green, re-ran locally |
| REQ-34.6-09 | 34.6-10 | `EosDeclineCallSiteGuard.test.ts` stays green/unmodified | ✓ SATISFIED | Confirmed green in local re-run |
| REQ-34.6-10 | 34.6-13 | `preload-surface-gate.py` re-run at phase close, self-test first | ✓ SATISFIED | Independently re-ran both self-test and live mode; matches claimed 220/unlisted=0. **Note:** REQUIREMENTS.md's checkbox is still `[ ]` despite `34.6-13-SUMMARY.md` claiming `requirements-completed: [REQ-34.6-10, REQ-34.6-14]` — a documentation-currency gap, not a substance gap (see Anti-Patterns). |
| REQ-34.6-11 | 34.6-04/12/15/17 | One blocking, sequenced live gate with no partial-pass | ✓ SATISFIED | Process requirement is about running the gate correctly and adjudicating one verdict, not about the verdict being PASS — confirmed met. REQUIREMENTS.md checkbox stale (same note as REQ-34.6-10). |
| REQ-34.6-12 | 34.6-12/14/15/17 | 4 folded todos disposed, none falsely closed | ✓ SATISFIED | Confirmed all 4 still pending. REQUIREMENTS.md checkbox stale (same note). |
| REQ-34.6-13 | 34.6-05/06/07/08 | Every red-by-construction gate amended same-commit | ✓ SATISFIED | REQUIREMENTS.md correctly shows `[x]` FULLY COMPLETE; independently confirmed green |
| REQ-34.6-14 | 34.6-13/14 | Closing artefacts written from measured results | ✓ SATISFIED | `34.6-PORTED-CHANNELS.md` confirmed honest and complete. REQUIREMENTS.md checkbox stale (same note). |

**13/14 requirements SATISFIED. 1/14 (REQ-34.6-05) BLOCKED.**

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/REQUIREMENTS.md` | 851, 852, 853, 855 | REQ-34.6-10/11/12/14 checkboxes remain `[ ]` despite `34.6-13-SUMMARY.md`/`34.6-14-SUMMARY.md` frontmatter both claiming `requirements-completed` for all four, and the substance being independently verified complete in this report | ⚠️ Warning | Documentation-currency gap only — the underlying work is done and verified above. A future automated requirements sweep reading only checkbox state (not SUMMARY frontmatter) would misreport this phase as 3/14 short. Recommend a follow-up edit flipping the 4 checkboxes with a note citing this VERIFICATION.md. |
| `src/frontend/components/UI/SearchBar/index.tsx` | 89-120 | Comment asserts a since-disproven root-cause theory ("Proven by measurement... fired the send and ran winetricks end to end") as settled fact for the Step 4 `:focus-within` mechanism, with a "DO NOT REMOVE" imperative | ℹ️ Info | Already flagged as WR-03 in `34.6-REVIEW.md`; not re-scored here to avoid double-counting. Misleads future debugging of the still-open `winetricksInstall` cause. |
| `src/backend/sidecar/enrichmentFlowRegistration.ts` | `steamgriddb.setApiKey` handler | Unvalidated `key` argument type; a non-string reaching `setToken()` throws inside a `try` that logs a misleading "failed" message even when the keyring write may have partially succeeded | ℹ️ Info | WR-01 in `34.6-REVIEW.md`, not re-scored here (low likelihood path, IPC args are string-typed at the preload boundary in practice) |
| `src/backend/sidecar/steamgridSecretStore.ts` | `setApiKey` | No trim/whitespace-as-clear parity with the Electron implementation | ℹ️ Info | WR-02 in `34.6-REVIEW.md`, not re-scored here |

No `TBD`/`FIXME`/`XXX` unreferenced debt markers found in any of the phase's modified sidecar registration files.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Registration-level test suites for the 24-channel port pass in isolation | `npx jest --selectProjects Backend --testPathPattern "eosOverlayFlows\|enrichmentFlows\|wineToolsFlows\|installFlows\|runnerSliceRegistration\|rendererPathGuard\|flowRegistrationCensus"` | 7 suites, 278 tests, all PASS | ✓ PASS |
| `preload-surface-gate.py` self-test proves the gate's own assertions reject bad input | `python3 .../preload-surface-gate.py --self-test` | All 6 checks correctly rejected fixtures, exit 0 | ✓ PASS |
| `preload-surface-gate.py` live run against current `IPC-PORT-INVENTORY.md` | `python3 .../preload-surface-gate.py` | `union=220, unlisted=0`, exit 0 | ✓ PASS |
| Referenced landing commits exist in history | `git cat-file -t <hash>` for a sample across plans 08/09/07/11/13/14/17 | All resolve to real commits | ✓ PASS |
| `moveInstall`/`importGame` containment guard rejects the features' primary (cross-root) real-world input | Code trace: `openDialog` (no root constraint) → `assertContainedPath(defaultInstallPath, path, ...)` → `relative()` starts with `..` → `PathContainmentError` | Confirmed by reading `openDialog.ts`, `GameSubMenu/index.tsx:97-109`, `ImportDialog/index.tsx:106-116`, `rendererPathGuard.ts:52-63`, `installFlowRegistration.ts:258-296,342-379` | ✗ FAIL (see gap) |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared by this phase's PLAN/SUMMARY files or found on disk. `preload-surface-gate.py` (above) functions as this phase's equivalent gate and was executed directly. SKIPPED — no additional conventional probes apply.

### Human Verification Required

None. The live-gate work (Steps 0-8, Epic login/EOS/SteamGridDB/winetricks/save-sync/logout) was already driven live by the operator and is recorded in `34.6-LIVE-GATE.md`; this verification does not need to re-drive it. CR-01 (the one gap found) was resolved to a definitive FAILED status by direct code/UI-flow tracing, not by anything requiring interactive/visual judgment.

### Gaps Summary

The phase's core stated goal — port the 24 deferred/late-discovered IPC channels onto the Node sidecar — is genuinely achieved. All 24 channels are registered with real (non-stub) handler bodies, wired into `handlers.ts`, covered by green registration-level tests, and independently confirmed present in a re-derived preload-surface census (`unlisted=0`). The phase's own honest bookkeeping around its FAIL 7/9 live-gate verdict, its two live failures (`winetricksInstall`, Epic logout — both correctly characterized as non-porting defects), and its four folded-todo dispositions (none falsely closed) all hold up under independent re-check.

**One genuine gap was found, discovered after the phase's own closing artifacts were written:** REQ-34.6-05's `T-34.5-C6-49-03` renderer-path hardening (plan 34.6-11), while correctly shaped as a containment primitive and correctly applied to remove the shell from `runWineCommandForGame`'s Windows branch, gates `moveInstall`/`importGame` against the wrong trust boundary. Both features exist specifically to let a user reference a location outside `GlobalConfig`'s `defaultInstallPath` — moving an install to another drive, or importing a game already installed elsewhere — and the hardening as shipped silently rejects that primary use case. This was independently re-verified against both frontend call sites, the shared native-picker implementation, and the guard itself; it exactly matches `34.6-REVIEW.md`'s CR-01 finding (written the same day, after `34.6-PORTED-CHANNELS.md` and REQUIREMENTS.md's REQ-34.6-05 entry had already both declared the requirement fully complete). A todo is already filed (`.planning/todos/pending/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md`) but no code fix or requirement correction has landed yet — this is what keeps overall status at `gaps_found` rather than `passed`.

A secondary, non-blocking documentation-currency issue: REQUIREMENTS.md's checkboxes for REQ-34.6-10/11/12/14 remain unchecked (`[ ]`) despite the corresponding plans' SUMMARY frontmatter (`34.6-13`, `34.6-14`, `34.6-15`, `34.6-17`) all declaring `requirements-completed` for them, and this verification independently confirming the substance of all four is done. This is recorded as a Warning-level anti-pattern, not a gap, because it does not affect the delivered code — but it should be fixed so a future automated requirements sweep does not misreport this phase.

**Recommendation:** a small gap-closure plan (or quick task, since it is a scoped, single-file fix) to correct `moveInstall`/`importGame`'s containment boundary per the review's suggested fix, plus flipping REQUIREMENTS.md's four stale checkboxes, would close this phase to `passed`.


**GAP-CYCLE-2 ADDENDUM — 2026-08-26 (appended by plan 34.6-21; this document's `status:` and
`gaps:` frontmatter are deliberately UNCHANGED, since a re-verification run owns those, not this
plan).**

The CR-01 gap recorded above — REQ-34.6-05's containment guard rejecting the very inputs
`moveInstall` and `importGame` exist to accept — now has BOTH a landed code fix and a live
re-drive.

*Code fix:* gap plans 34.6-18 and 34.6-19 removed `defaultInstallPath` containment from both
handlers, replacing it with `rendererPathGuard.assertPlausibleAbsolutePath` (shape only: absolute,
no `..` segment, no NUL, non-empty, string) and adding a user-visible rejection dialog so a refusal
can no longer be silent. `assertContainedPath` now has ZERO production callers. The decisive
argument for removal was not "the OS picker is a stronger boundary" but that the old control was
CIRCULAR: `defaultInstallPath` is renderer-writable through the `setSetting` send channel, so the
named adversary could grant itself the property the guard asserted.

*Live re-drive:* the coverage hole this document identified — that neither channel had EVER been
driven against an out-of-root path — is now closed. `34.6-LIVE-GATE.md`'s appended `## GAP CYCLE 2`
section carries three contract-first items, authored with empty Result slots before any drive, and
returned **PASS 3/3** on 2026-08-26:

- **G2-1** `moveInstall` to `/Volumes/GLFIXTURE/move-dest` (a separate volume, provably not nested
  under `/Users/graysonmitchell/GameLib`): the runner's move path was entered at `gamelib.log:83`
  with zero rejection lines. It then failed on the macOS `openrsync` flag defect — scored PASS per
  that item's own disambiguation rule, since acceptance is decided before rsync runs, and recorded
  as a live confirmation of that separate todo, which stays OPEN.
- **G2-2** `importGame` from `/Volumes/GLFIXTURE/import-src/Endless Sky.app`: reached gogdl itself
  at `gamelib.log:278`, no rejection line, no dialog. The identity-validation defect this document
  already records was re-confirmed downstream and is UNCHANGED by this addendum.
- **G2-3** the REJECT direction: `../../etc` produced the visible "Can't use that location" dialog
  (`gamelib.log:99`) and is absent from the log, so the widened ACCEPT path did not cost the
  REJECT path.

*One finding worth carrying forward, outside REQ-34.6-05's scope.* G2-1's FIRST run produced no
log lines and no UI feedback at all. The cause is not CR-01 and not any Phase 34.6 port: `openDialog`
is missing from `LONG_RUNNING_CHANNELS` (`src-tauri/src/main.rs:184`), so any file picker held open
past the 60s `INVOKE_TIMEOUT` has its result dropped and `moveInstall` is never invoked. That is the
already-filed
`.planning/todos/pending/2026-08-24-opendialog-is-missing-from-long-running-channels-so-every-file-picker-flow-dies-silently.md`,
still OPEN, and it means the live-gate evidence for ANY picker-driven channel is only trustworthy
when the pick completed inside 60s. Run 2 did, and that is why it measured anything at all.

---

_Verified: 2026-08-25T09:41:30Z_
_Verifier: Claude (gsd-verifier)_

---

# RE-VERIFICATION — run 2, 2026-08-26T08:04:09Z

**Everything above this line is the run-1 record (2026-08-25) plus plan 34.6-21's GAP-CYCLE-2
addendum. It is preserved verbatim and is NOT rewritten.** Run 1's frontmatter is preserved under
the `superseded_run_1:` key. This section supersedes run 1's `status:`, `score:` and `gaps:` — which
run 1's addendum explicitly left for a re-verification run to own.

**Result: `gaps_found` → `passed`. Score 8/9 → 9/9. 13/14 → 14/14 requirements.**

## What was independently re-measured (not read from SUMMARY.md)

### Truth 5 — the one gap run 1 found (CR-01) — now ✓ VERIFIED

Re-tested exactly as the re-verification brief demanded, by direct measurement:

| Check | Command / read | Result |
|---|---|---|
| `assertContainedPath` has ZERO production callers | `grep -rn assertContainedPath src/ src-tauri/ scripts/` | Only its own definition + doc comments in `rendererPathGuard.ts`, and `__tests__/`. **Zero production call sites.** ✓ |
| `moveInstall` uses the shape guard as its FIRST statement | read `installFlowRegistration.ts:298-336` | `assertPlausibleAbsolutePath(path, 'moveInstall')` at `:304`, inside the handler's first `try`, before `sendGameStatusUpdate({status:'moving'})` and before any filesystem call ✓ |
| `importGame` likewise | read `installFlowRegistration.ts:417-460` | `assertPlausibleAbsolutePath(path, 'importGame')` at `:431`, first statement after destructuring ✓ |
| `defaultInstallPath` containment is gone from both | `grep -rn "assertContainedPath\|defaultInstallPath" src/backend/sidecar/installFlowRegistration.ts` | No containment call, no `GlobalConfig…defaultInstallPath` read in either handler ✓ |
| Rejection is USER-VISIBLE | read `:319-330` / `:442-453`; `public/locales/en/gamelib.json:48-50` | Both branches call `showDialogBoxModalAuto({title: rejectedPathDialogTitle(), message: i18next.t('gamelib:installFlows.pathRejectedBodyMove'/`…Import`), type:'ERROR'})`; all three keys present in `gamelib.json` (fork namespace, NOT `translation.json`) ✓ |
| The `gamelib` i18next namespace actually loads at runtime | read `bootstrap.ts:499` and `main.ts` init | `ns: ['translation','gamelib'], defaultNS: 'translation'` present at **both** init sites (sidecar + Electron leg) — a one-sided change would have shipped the raw key ✓ |
| Neither string interpolates the rejected path | read both `t()` calls and both `logError` strings | No `path` interpolation anywhere (T-34.6-32 holds) ✓ |

### The removal's justification was verified, not accepted

The decisive argument for dropping containment is that it was **CIRCULAR**. I did not take that on
trust — `src/backend/sidecar/settingsFlowRegistration.ts:160` registers `setSetting` via
`ipcMain.on`, and for `appName === 'default'` it calls
`GlobalConfig.get().setSetting(key as keyof AppSettings, value)` **with no key allowlist**. So a
renderer that can call `moveInstall` can first call `setSetting` to widen `defaultInstallPath` to
`/` and then move anywhere. The old control therefore bought **nothing** against the adversary
`T-34.5-C6-49-03` names, while costing the feature its primary input. Removing it is a net
improvement, not a descope of convenience.

**Comparative check on the accepted residual.** REQUIREMENTS.md accepts, at ASVS L1, that "a
compromised renderer can still call the channel directly with any absolute, traversal-free path —
the same exposure every other filesystem-touching sidecar channel carries." That claim was
spot-checked: `install` (`installFlowRegistration.ts:234-250`) takes a renderer-supplied
`install_path` with **no path guard at all**. So the two hardened channels are now strictly
*stronger* than the surrounding baseline, and the residual statement is accurate rather than
self-serving.

### Live evidence — gap cycle 2, read at source

`34.6-LIVE-GATE.md` frontmatter re-read directly: `verdict: FAIL 7/9` and `failing_items: [4, 8]`
are **byte-unchanged**, and `gap_cycle_2_verdict: "PASS 3/3"` is a **separate key**. The appended
`## GAP CYCLE 2` section (from line 1818) states in its own opening paragraph that it re-scores
nothing. Every `## SUPERSEDES` heading in the document was enumerated — Steps 2, 3, 5, 6 and
Preconditions 8/9. **None touches Step 4 or Step 8.** The three appended items were authored with
empty Result slots before the drive (commit `5d1787a73`, Task 1) and filled afterwards (`8cc8f87d2`,
Task 2) — contract-first ordering confirmed in git, not asserted.

- **G2-1** — destination `/Volumes/GLFIXTURE/move-dest`, device id `16777240` vs `16777231`,
  non-nesting checked with `os.path.commonpath`. Runner move path entered at `gamelib.log:83`;
  zero rejection lines. FAIL-A did not occur. PASS.
- **G2-2** — source `/Volumes/GLFIXTURE/import-src/Endless Sky.app` reached gogdl at
  `gamelib.log:278`; no rejection line, no dialog. PASS.
- **G2-3** — `../../etc` produced the visible "Can't use that location" dialog (`gamelib.log:99`)
  and the rejected string is absent from the log. The REJECT direction survived the widening. PASS.

The coverage hole run 1 named — *neither channel had ever been driven against an out-of-root path*
— is the specific hole these three items close.

### RED proof is real, not claimed

Both evidence files were opened and contain genuine **failing** pre-fix runs:
`evidence/34.6-18-RED-crossroot.txt` shows the two cross-root tests `✕` failing against the
containment build; `evidence/34.6-19-RED-dialog.txt` shows the three dialog tests `✕` failing
before 34.6-19 landed. This is what makes the now-green tests non-vacuous.

## Regression checks on the eight truths that passed in run 1

All re-measured, none regressed:

- **All 24 channels re-enumerated by AST-shaped grep** (`ipcMain.handle|on` + exact channel
  literal, across `src/backend/sidecar/*.ts`): 8 EOS, 5 SteamGridDB
  (`hasApiKey`/`setApiKey`/`searchGame`/`getGrids`/`getHeroes`), 3 winetricks, 8 late-discovered.
  **24/24 present**, each in its declared module, each wired from `handlers.ts` (`:119`, `:144`,
  `:158`, `:213`, `:215`, `:218`).
- **Tests re-run:** `npx jest --selectProjects Backend --testPathPattern "installFlows|rendererPathGuard|gamelibNamespaceLoad|eosOverlayFlows|enrichmentFlows|wineToolsFlows|runnerSliceRegistration|flowRegistrationCensus"` → **8 suites, 305 tests, all PASS** (run 1 measured 278; the +27 are 34.6-18/19's new cross-root, shape, dialog and namespace-load coverage).
- **`preload-surface-gate.py` re-run, self-test first:** all 6 checks rejected their bad-input
  fixtures (exit 0), then live → `distinct invoke=160, send=60, union=220, push=30, unlisted=0`,
  exit 0. Unchanged from run 1.
- **`pnpm codecheck` (tsc --noEmit): exit 0.**
- **`pnpm i18n-churn-guard`: exit 0** — "no upstream `public/locales/` catalog changed", confirming
  the three new strings went into the fork's `gamelib.json` and not `translation.json`.
- **Four folded todos still pending, none falsely closed:** `2026-08-23-epic-logout-cookie-clear-…`,
  `2026-08-24-epic-logout-reports-clearing-cookies-…`, `2026-08-23-keyring-get-bounded-timeout-…`,
  `2026-08-23-f9-generic-rpc-timeout-…`, `2026-08-23-humble-user-info-404-…` all in
  `.planning/todos/pending/`.
- **The CR-01 todo has correctly MOVED to `completed/`:**
  `.planning/todos/completed/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md`.
- **All eight referenced commits resolve** (`git cat-file -t`): `402b48c50`, `6d30448e3`,
  `df03d47d8`, `1a549bd28`, `6af1bd4d5`, `3bf24798f`, `5448b7854`, `83890d02a`.

## Updated Observable Truths

| # | Truth | Run 1 | Run 2 | Evidence delta |
|---|---|---|---|---|
| 1 | 8 EOS overlay channels registered, invoke-kind, dedicated module | ✓ | ✓ VERIFIED | Re-enumerated 8/8. UAT tests 2/3 additionally upgraded `getEosOverlayStatus`, `getLatestEosOverlayVersion` and `updateEosOverlayInfo` from `unit` to live-observed on macOS. |
| 2 | 5 SteamGridDB channels registered, keyring-only | ✓ | ✓ VERIFIED | Re-enumerated 5/5. UAT test 1 re-confirmed the bootstrap keyring receipt on a genuine cold start. |
| 3 | 3 winetricks channels registered with real (not decline-branch) macOS behaviour | ✓ | ✓ VERIFIED | Strengthened: UAT test 4 shows the REAL binary running (`tools/winetricks fonts list`) with 0 `DECLARED_UNAVAILABLE_MARKER`; UAT test 5 shows `winetricksInstall` reaching its handler and completing a real `winetricks -q corefonts`. |
| 4 | 8 late-discovered channels registered | ✓ | ✓ VERIFIED | Re-enumerated 8/8 with exact file:line. |
| 5 | REQ-34.6-05's hardening discharges T-34.5-C6-49-03 without regressing legitimate use | ✗ FAILED | ✓ VERIFIED | The full re-measurement above. Control re-dispositioned to `assertPlausibleAbsolutePath`; ACCEPT proven live in both directions (G2-1/G2-2), REJECT proven live (G2-3), rejection now user-visible, circularity of the old control independently confirmed at `settingsFlowRegistration.ts:160`. |
| 6 | One blocking live gate, sequenced, honest per-item verdict, no partial-pass laundering | ✓ | ✓ VERIFIED | Re-confirmed at source: `FAIL 7/9` / `failing_items: [4,8]` unchanged; gap cycle 2 carries a separate key and says so; no SUPERSEDES touches item 4 or 8; the appended contract was authored empty-first in a prior commit. |
| 7 | 4 folded todos disposed, none falsely closed | ✓ | ✓ VERIFIED | All still in `pending/`. |
| 8 | `preload-surface-gate.py` re-run at close, self-test proven first | ✓ | ✓ VERIFIED | Both modes independently re-run this session; identical figures. |
| 9 | Closing artefacts written from measured results | ✓ | ✓ VERIFIED (with one bookkeeping warning) | `34.6-PORTED-CHANNELS.md` now cites G2-1/G2-2 per channel; a new `34.6-UAT.md` (complete, 5/5, 3 gaps) covers the surface the gate never reached. See WARNING W-R2-01 below. |

**Score: 9/9 truths verified.**

## Updated Requirements Coverage

**14/14 SATISFIED.** Two changes since run 1:

- **REQ-34.6-05** — `✗ BLOCKED` → **✓ SATISFIED**. Its REQUIREMENTS.md entry now carries a
  **CORRECTED 2026-08-26** clause naming this document's CR-01 as the finding, and a **CLOSED
  2026-08-26 … on MEASURED live evidence** clause citing G2-1/G2-2/G2-3 individually. The checkbox
  was flipped to `[x]` *after* the drive, and the requirement's own text had earlier been flipped
  BACK to `[ ]` with "NOT YET FULLY COMPLETE — closure is pending the live re-drive" — i.e. the
  record shows the checkbox tracking measurement rather than intent, in both directions. That is
  the shape this project's own lessons say to look for, and it holds.
- **REQ-34.6-10 / -11 / -12 / -14** — run 1's Warning-level stale-checkbox anti-pattern is
  **CLOSED**. All fourteen REQ-34.6-* checkboxes now read `[x]` (verified by direct extraction of
  the checkbox glyphs at `.planning/REQUIREMENTS.md:842-855`), and REQ-34.6-11's entry names the
  flip explicitly as "the checkbox, not the substance, was the gap".

## Anti-Patterns — run 2

| File | Locus | Pattern | Severity | Impact |
|---|---|---|---|---|
| `34.6-PORTED-CHANNELS.md` | rows 97 (`importGame`) and 98 (`moveInstall`), **Proof level** column | The gap-cycle-2 upgrade is recorded only in the **Riders** prose cell. The **Proof level** column still reads `unit + LIVE (item 5)` for both rows — it was never updated to `unit + LIVE (gap-cycle-2 item G2-1 / G2-2)`. | ⚠️ Warning (**W-R2-01**) | The substance IS present in the same row, so nothing is lost to a careful reader. But anything that reads the ledger *by column* — the obvious way to consume a proof table — will not see that these two channels gained live out-of-root evidence on 2026-08-26. This is the project's own recurring shape: a restatement dropping the qualifier while the underlying record stays right. Recommend a one-line edit to both Proof level cells. |
| `src/backend/sidecar/rendererPathGuard.ts` | `assertContainedPath` (`:75`) | Exported, fully unit-tested, **zero production call sites**. | ⚠️ Warning | Dead production code by the export-level spot check. Deliberately retained with a named future consumer (`todos/pending/2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md`) documented in the module header, so it is retention-by-decision rather than leftover — but it is dead today and a later reader should not assume it guards anything. |
| `.planning/REQUIREMENTS.md` | REQ-34.6-05, opening sentence | The requirement's headline text still reads "validate their renderer-supplied path by CONTAINMENT against the app's own configured install root" — which is now **not what ships**. The correction is appended later in the same (very long) entry. | ℹ️ Info | Append-only ledger convention, and the correction is dated, explicit and cites its evidence. Noted only because a reader who stops at the first sentence gets the pre-correction picture. |
| `src/backend/main.ts` | `:674` `// FIXME: Propagate errors` | Debt marker inside a file this phase touched. | ℹ️ Info (not a blocker) | `git blame` dates it to upstream Heroic commit `fcd30b420`, 2024-03-14. Pre-existing, outside this phase's hunks (34.6-19's only change to this file is the `ns:`/`defaultNS:` pair). Not gated. |
| Files changed since run 1 | `gamelib.json`, `main.ts`, `bootstrap.ts`, `installFlowRegistration.ts`, `rendererPathGuard.ts`, 4 test files | Debt-marker sweep | — | **No `TBD` / `FIXME` / `XXX` introduced** by any of gap plans 34.6-18..21. |

## Behavioral Spot-Checks — run 2

| Behavior | Command | Result | Status |
|---|---|---|---|
| Registration + guard + dialog + i18n suites pass | `npx jest --selectProjects Backend --testPathPattern "installFlows\|rendererPathGuard\|gamelibNamespaceLoad\|eosOverlayFlows\|enrichmentFlows\|wineToolsFlows\|runnerSliceRegistration\|flowRegistrationCensus"` | 8 suites, **305 tests**, all PASS | ✓ PASS |
| Typecheck | `pnpm codecheck` (`tsc --noEmit`) | exit 0 | ✓ PASS |
| Fork strings did not touch upstream catalogs | `pnpm i18n-churn-guard` | `clean -- no upstream public/locales/ catalog changed`, exit 0 | ✓ PASS |
| Preload-surface gate proves its own assertions first | `python3 …/preload-surface-gate.py --self-test` | all 6 checks rejected their fixtures, exit 0 | ✓ PASS |
| Preload-surface gate live | `python3 …/preload-surface-gate.py` | `invoke=160, send=60, union=220, push=30, unlisted=0`, exit 0 | ✓ PASS |
| 24/24 channels registered on the sidecar | scripted `ipcMain.(handle\|on)('<channel>'` scan over `src/backend/sidecar/*.ts` | 24/24, each with file:line | ✓ PASS |
| `assertContainedPath` is unreachable from production | `grep -rn assertContainedPath src/ src-tauri/ scripts/` | 0 production call sites | ✓ PASS |
| Referenced landing commits exist | `git cat-file -t` × 8 | all resolve | ✓ PASS |

## Probe Execution

No `scripts/*/tests/probe-*.sh` exists in this repository and no PLAN/SUMMARY in this phase
declares one. `preload-surface-gate.py` is this phase's equivalent gate and was executed directly,
self-test first — recorded above. SKIPPED, with reason.

## Coverage caveats — assessed against the verdict, not waved through

The re-verification brief asked whether the caveats the phase itself records bear on the verdict.
Assessed individually:

1. **"Porting is REGISTRATION, not behaviour."** The phase goal is *port the 24 channels onto the
   Node sidecar*, and REQ-34.6-01/-02/-03/-04 are worded as registration requirements. Registration
   is verified 24/24 with real handler bodies and `handlers.ts` wiring. Behaviour, where reachable,
   was additionally driven — live gate items 2/3/5, gap-cycle-2 G2-1/G2-2/G2-3, and now UAT tests
   1-5. **Does not bear on the verdict**, but it is the honest ceiling of what "ported" proves and
   it is stated as such in `34.6-PORTED-CHANNELS.md`.
2. **`enableEosOverlay` / `disableEosOverlay` / `isEosOverlayEnabled` have no reachable macOS UI.**
   Independently confirmed: `AdvancedSettings/index.tsx:133` (`if (isWindows)`) and `:401`
   (`{isWindows && …}`), with that file's own comment stating `isEosOverlayEnabled` "genuinely never
   fires on macOS". Registration is verified; behaviour is unprovable on this hardware.
   **Deferred to Phase 38**, whose goal is exactly this sweep. Not a gap.
3. **`runWineCommandForGame` ships with zero renderer call sites.** Re-confirmed
   (`grep -rn runWineCommandForGame src/frontend/` = 0). Run 1 already removed it from the live
   gate's scored denominator as UNREACHABLE-BY-CONSTRUCTION, via a marked SUPERSEDES. **Deferred to
   Phase 38.** Not a gap.
4. **The pre-existing 14-channel inventory discrepancy is FLAGGED, not absorbed.** Verified at
   `.planning/IPC-PORT-INVENTORY.md:291-297`: `Ported + Unported = 52 + 159 = 211` against
   `Unique channels = 225`, with the note stating the gap predates this plan, that this plan moved
   both rows by exactly 24, and that folding it in "would destroy the evidence that the gap exists
   at all". That is the correct handling — a phase that absorbed it silently would be the defect.
   **Does not bear on this phase's verdict**; it is a standing baseline task nobody owns yet.

## Why the status is `passed` and not `human_needed`

`34.6-UAT.md` is `status: complete`, 5/5 passed, and it is the human round for this phase — it ran
*after* the gate and deliberately targeted only the surface the gate never reached. Nothing among
the nine must-haves is now awaiting human judgement; every one resolved by measurement.

Two items nonetheless need an **operator decision**, and they are recorded in the
`open_operator_decisions:` frontmatter key rather than in `human_verification:` — because neither is
required to establish that the phase goal is achieved, and putting them in `human_verification`
would falsely gate the phase:

- **G-34.6-UAT-03 (significant).** UAT test 5 saw the Step 4 symptom fail to reproduce and produced
  the first-ever `[GAMELIB_SIDECAR_SEND_HANDLER] winetricksInstall` observable. **This is NOT a
  basis for flipping Step 4**, and this verification does not flip it: two variables moved together
  (`brew install p7zip cabextract zenity`, and the package list consequently becoming selectable),
  and two prior explanations for this defect have already been disproven. A properly-conditioned
  gate re-drive is now *possible*; whether to run it, and whether to amend the closed gate with a
  seventh SUPERSEDES, is the operator's call under the same option-(c) authority that closed it.
- **G-34.6-UAT-01 (minor).** EOS "Check for Updates" fails opaquely with no Epic session. UX gap
  behind a working channel; the UAT itself says it must not hold the phase.

## The two standing live failures are UNTOUCHED

Stated plainly so no later reader mistakes this `passed` for a live-gate pass:

- **`34.6-LIVE-GATE.md` remains closed at `verdict: FAIL 7/9`, `failing_items: [4, 8]`,** by the
  operator's explicit option-(c) decision. Nothing in gap cycle 2, in `34.6-UAT.md`, or in this
  re-verification re-scores it. **Any reading of this document that treats the phase as "now passing
  its live gate" is WRONG.**
- **Step 4 (`winetricksInstall`)** — still FAILED of record; cause still unestablished; parked at
  `todos/pending/2026-08-24-winetricksinstall-send-channel-is-a-live-silent-no-op.md`.
- **Step 8 (Epic logout)** — a wry-layer cookie-clear defect on `clearEpicCookies`, a channel this
  phase never ported (34.4.1 owns it). Out of scope by construction.

`passed` here means: **the phase's own goal — porting 24 IPC channels onto the Node sidecar — is
achieved and independently measured, and the one gap that blocked it is closed.** It does not mean
the live gate passed, and it does not close the two owned failures.

## Open items carried out of this phase (none blocking)

Still `pending`, correctly, and none auto-closable with this phase:

- `2026-08-24-winetricksinstall-send-channel-is-a-live-silent-no-op.md` (Step 4)
- `2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md` + `2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md` (Step 8)
- `2026-08-24-opendialog-is-missing-from-long-running-channels-…` — **raised in priority by gap
  cycle 2**: it VOIDED G2-1's first run silently, and it means live-gate evidence for *any*
  picker-driven channel is only trustworthy when the pick completed inside 60s.
- `2026-08-24-move-game-is-broken-on-macos-rsync-flags-openrsync-rejects.md` — live-confirmed again by G2-1.
- `2026-08-24-importgame-does-not-validate-the-folder-matches-the-selected-game.md` — live-confirmed again by G2-2.
- `2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md` — the declared residual, and `assertContainedPath`'s named future consumer.
- `2026-08-26-path-rejection-dialog-uses-an-oversized-large-text-window.md` — UI polish on the new dialog; no `resolves_phase:`, must not auto-close.
- `2026-08-23-keyring-get-bounded-timeout-unverified-live.md`, `2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md`, `2026-08-23-humble-user-info-404-two-candidates-undiscriminated.md` — the remaining folded todos.

---

_Re-verified: 2026-08-26T08:04:09Z (run 2)_
_Verifier: Claude (gsd-verifier) — goal-backward, adversarial stance_
