---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 14
type: execute
wave: 9
depends_on: ["34.6-12"]
files_modified:
  - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
  - .planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md
  - .planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md
  - .planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md
  - .planning/todos/pending/2026-08-23-humble-user-info-404-two-candidates-undiscriminated.md
autonomous: true
requirements: [REQ-34.6-12, REQ-34.6-14]
must_haves:
  truths:
    - "D-01: `34.6-PORTED-CHANNELS.md` declares all 24 channels with HONEST per-channel proof levels, written from measured results and never from planning-time intent"
    - "D-10: `U-34.5-32` gets a TWO-PART disposition — the SteamGridDB half retires BY PORT, the winetricks half is discharged by step 0's live marker capture. Not a tick."
    - "D-13: folded todo 1 (Epic logout) closes only if the live gate's step 8 scored an IDENTITY assertion against a planted named cookie"
    - "Folded todo 4 (Humble 404) is recorded NOT DISCHARGED and returned to pending unless a Humble login was actually performed — it must not be closed by association"
  artifacts:
    - path: ".planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md"
      provides: "24 rows: channel, kind, registration module, honest proof level, riders"
      min_lines: 80
      contains: "Proof level"
  key_links:
    - from: "34.6-PORTED-CHANNELS.md"
      to: "34.6-LIVE-GATE.md"
      via: "each row's LIVE proof level cites the gate item number that proved it"
      pattern: "item [0-8]"
---

<objective>
Write the phase's closing artefacts and dispose of every ledger row and folded todo it touched —
honestly, including the ones that do NOT close.

This is where a phase's debt is either recorded or quietly lost. This project's own record is
explicit about the failure modes: `blocked_by`/`parked_to_phase` records rot silently (three times);
a UAT `pass` can cover a surface that could not render at all; an item closed by association with
the phase that folded it is a defect, not bookkeeping.

Purpose: leave a per-channel proof ledger and four honest todo dispositions.
Output: `34.6-PORTED-CHANNELS.md`, `U-34.5-32`'s two-part disposition, four todo dispositions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-CONTEXT.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-04-SUMMARY.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-10-SUMMARY.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-11-SUMMARY.md
@.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `34.6-PORTED-CHANNELS.md` — 24 rows with honest, measured proof levels</name>
  <read_first>
    - `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md` (the exact table format and section order this document must follow: purpose/provenance paragraph, live-gate narrative INCLUDING failed attempts in full, "What shipped", and "corrections this declaration must not paper over")
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md` (the filled results — every LIVE proof level must cite a specific item number and its outcome)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-05-SUMMARY.md` (the two declared caveats for `frontendReady`)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-11-SUMMARY.md` (the three T-34.5-C6-49-03 residuals)
  </read_first>
  <files>.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md</files>
  <action>
Create `.planning/phases/34.6-.../34.6-PORTED-CHANNELS.md`, following `34.5-PORTED-CHANNELS.md`'s
structure section for section:

  1. **Purpose/provenance paragraph** — measurement wins over prediction; this document is written
     from ACTUAL `*-SUMMARY.md` and live-gate results, never from planning-time intent.
  2. **Live-gate narrative** — what the gate run did, in order, INCLUDING any failed attempt in
     full detail. Do not smooth over a FAIL: `34.5-PORTED-CHANNELS.md` records its own gate's
     failures, and that is what makes its passes readable.
  3. **The 24-row table**, columns `| Channel | Kind | Registration module | Proof level | Riders |`.
     Proof levels are one of: `unit`, `unit + LIVE (item N)`, or
     `ported with a declared caveat: <caveat>`. **Where an automated test proves only wiring, say
     so plainly** — "unit-proven" must not be written where the only evidence is that a function is
     called. "The function is CALLED" proves wiring, not behaviour.
  4. **"What shipped"** — a per-module channel-count breakdown reconciling to 24, with any channel
     NOT ported enumerated explicitly. This phase's scope is the whole remaining backlog, so the
     deferred count should be ZERO — confirm that against the final count rather than asserting it.
  5. **"Corrections this declaration must not paper over"** — with the exact grep or command that
     found each: the six PATTERNS deltas that CONTEXT.md and RESEARCH.md did not flag; the
     `callOrDeclare` 11-vs-10 reconciliation to a measured 20 sites across 6 files; the
     electron-reach ledger additions and whether any exceeded its prediction; A-04's stale
     inventory citation; and the two `frontendReady` body exclusions (`handleProtocol`,
     `initQueue`) with their reasons.

Specific rows that need a caveat rather than a clean level:
  - `frontendReady` — send-kind; proof is the D-11 observable line, not a return value; and its
    body deliberately excludes `handleProtocol` (already delivered at bootstrap) and
    `initQueue(true)` (Phase 33 D-04 deferred boot-time auto-resume to Phase 35).
  - `winetricksInstall` — send-kind; structurally silent on failure by design; guarded in
    try/catch, never a bare `.catch()`; proof is the D-11 observable.
  - `importGame`, `moveInstall`, `runWineCommandForGame` — carry `T-34.5-C6-49-03`; ported
    byte-equivalently (plans 34.6-06/07) and hardened separately (plan 34.6-11); the Windows
    non-shell branch is UNVERIFIED on Windows hardware.
  - The 5 SteamGridDB channels — their key path is keyring-backed per A-03, which is a DIVERGENCE
    from byte-equivalence and must be stated as such, with the reason.
  </action>
  <verify>
    <automated>F=.planning/phases/34.6-*/34.6-PORTED-CHANNELS.md; grep -c '^| `' $F</automated>
  </verify>
  <acceptance_criteria>
    - The table has exactly 24 channel rows, and every one of the 24 names appears exactly once: extract the channel column, sort, and confirm 24 unique entries matching `IPC-PORT-INVENTORY.md`'s two Phase-34.6 bucket lines. Record the diff (empty) in the SUMMARY.
    - Every `LIVE` proof level cites a specific gate item number, and that item's result in `34.6-LIVE-GATE.md` is a PASS. A LIVE claim citing a NOT ATTEMPTED item is a defect.
    - The five caveat rows named above each carry their caveat text.
    - The "corrections" section lists at least the six PATTERNS deltas plus the `callOrDeclare` count reconciliation, each with the command that found it.
    - "What shipped" reconciles to 24 and states the deferred count, confirmed against the final registration census rather than asserted.
  </acceptance_criteria>
  <done>Every ported channel has an honest, measured proof level, and the phase's own discoveries are recorded rather than smoothed.</done>
</task>

<task type="auto">
  <name>Task 2: `U-34.5-32`'s two-part disposition (D-10)</name>
  <read_first>
    - `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md` (the `U-34.5-32` row in full, including its stated retirement bar and its 2026-08-19 "GATE 5 STAYS OPEN" note recording `grep -c "GAMELIB_DECLARED_UNAVAILABLE"` = 0)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-04-SUMMARY.md` (step 0's captured marker — the winetricks half's evidence)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-LIVE-GATE.md` step 3 (the SteamGridDB half's working-feature observation)
  </read_first>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</files>
  <action>
Amend `U-34.5-32`'s row in `34.5-UNTESTED-ITEMS.md` IN PLACE with a TWO-PART disposition. D-10 is
explicit that this row does not retire wholesale and does not get a tick:

  - **SteamGridDB half — RETIRED BY PORT.** Once ported on macOS there is no decline left to
    observe. Live-gate step 3 observed the WORKING feature instead: `setApiKey` -> `hasApiKey` ->
    `searchGame` -> `getGrids` -> `getHeroes` with artwork rendering. Cite the item and its result.
    State plainly that this half was NOT discharged by observing a decline — retiring it by port is
    a different claim, and conflating the two is what "retiring wholesale" would have done.
  - **Winetricks half — DISCHARGED LIVE, pre-port.** Cite step 0's captured
    `[GAMELIB_DECLARED_UNAVAILABLE]` line verbatim, its commit SHA, and the fact that it is the
    FIRST live observation of that literal in this project's history. Record A-01's correction on
    the row itself: the original reasoning ("winetricks still declines on macOS") is FALSIFIED —
    the `Winetricks` object has no `!isLinux` guard and has genuine macOS support via `macEnvs`
    (`tools/index.ts:628`, selected at `:642`). The OUTCOME is preserved, the reasoning is not.
    A future reader must not inherit the false premise.

Do not delete the row's history. Amend in place, as this file's own prior updates do.
  </action>
  <verify>
    <automated>grep -c 'U-34.5-32' .planning/phases/34.5-*/34.5-UNTESTED-ITEMS.md</automated>
  </verify>
  <acceptance_criteria>
    - The `U-34.5-32` row contains BOTH a "SteamGridDB half" and a "winetricks half" disposition, each with its own evidence.
    - The winetricks half quotes the captured marker line verbatim and names the commit SHA it was captured on.
    - The row records that A-01 FALSIFIED the original reasoning while preserving the outcome, in words a later reader cannot mistake for the old premise.
    - The row's prior history (its 2026-08-14 opening and its 2026-08-19 GATE 5 note) is intact — `git diff` shows an amendment, not a rewrite.
    - No other `U-34.5-*` row is modified.
  </acceptance_criteria>
  <done>The ledger row carries a two-part disposition with a falsified premise recorded, not a tick.</done>
</task>

<task type="auto">
  <name>Task 3: Dispose of the four folded todos — including the two that do NOT close</name>
  <read_first>
    - `.planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md` (folded todo 1 — its discharge bar is D-13, NOT the todo's own wording)
    - `.planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md` (folded todo 2, `REQ-34.4.1-GAP-11`)
    - `.planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md` (folded todo 3, `D-29-06`)
    - `.planning/todos/pending/2026-08-23-humble-user-info-404-two-candidates-undiscriminated.md` (folded todo 4, `D-29-02`)
    - `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-12-SUMMARY.md` and `34.6-LIVE-GATE.md`'s closing statement (the riders' actual observed outcomes)
  </read_first>
  <files>.planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md, .planning/todos/pending/2026-08-23-keyring-get-bounded-timeout-unverified-live.md, .planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md, .planning/todos/pending/2026-08-23-humble-user-info-404-two-candidates-undiscriminated.md</files>
  <action>
Dispose of each todo against ITS OWN stated bar, using only what the gate actually observed.

  1. **Epic logout cookie clear.** Its discharge bar is **D-13**, not the todo's own wording: the
     ROADMAP's "count cross-checked against an independent re-read" is AMENDED, because a count can
     be arithmetically forced — that is why `34.4.1-35-PLAN.md`'s precondition 6 is UNSTRUCK
     (`survivingNonHumble=0` when `before total=34` equalled `matched=34`). Close it ONLY if step 8
     recorded the PLANTED, NAMED foreign cookie surviving by name. If step 8 recorded only a count,
     it does NOT close — say so and leave it pending.
  2. **`keyring_get` bounded timeout.** Close only if the gate WATCHED it fire and MEASURED elapsed
     time producing a classified, bounded error. If it stayed unexercised on the unhappy path, the
     todo STAYS OPEN, unscheduled, revisit-on-next-live-gate. A non-firing rider is not a discharge.
  3. **F-9 generic RPC timeout co-occurrence.** Close only if a timeout recurred during the gate AND
     the FULL shell scrollback was correlated by `id=` against a cookie operation. `gamelib.log`
     alone cannot settle it — `eprintln!` goes to stderr and never reaches the log file, and a clean
     grep of the wrong source is not evidence of absence. Otherwise it stays UNDETERMINED, which is
     its existing, deliberate status.
  4. **Humble user-info 404 (`D-29-02`).** This is a HUMBLE login surface and this phase's gate
     logs into EPIC. Dischargeable ONLY if the operator additionally opened a Humble login. **If no
     Humble login was performed, record it NOT DISCHARGED and return it to pending. It must not be
     closed by association with the phase that folded it.**

For each todo that CLOSES: move it to `.planning/todos/completed/` and record the evidence and the
date in the file. For each that does NOT: leave it in `pending/`, and append a dated note stating
what this phase's gate did and did not observe, plus its revisit trigger. A record that says
nothing about a gate it rode through will rot — three `blocked_by`/`parked_to_phase` records have
rotted silently in this project already.

Set or verify `resolves_phase:` on every todo file you touch. This field is ABSENT BY DEFAULT and
its absence has made todo auto-close miss items three times here.
  </action>
  <verify>
    <automated>ls .planning/todos/pending/2026-08-23-*.md 2>/dev/null | wc -l; ls .planning/todos/completed/2026-08-23-*.md 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All four todo files are accounted for: each is either in `completed/` with dated evidence, or in `pending/` with a dated note naming what this gate observed and its revisit trigger.
    - The Epic-logout todo's disposition cites step 8's PLANTED-COOKIE-BY-NAME result, not a count. If the gate produced only a count, the file is in `pending/` and says so.
    - The `keyring_get` todo's disposition states a MEASURED elapsed time or states plainly that it did not fire.
    - The Humble todo's disposition states whether a Humble login was performed; if not, the file is in `pending/` and contains the words `NOT DISCHARGED`.
    - Every touched todo file has a `resolves_phase:` field set.
    - `grep -L 'resolves_phase' .planning/todos/pending/2026-08-23-*.md .planning/todos/completed/2026-08-23-*.md 2>/dev/null` prints nothing.
  </acceptance_criteria>
  <done>Four folded todos have honest dispositions, and the two or three that do not close say why and when to revisit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the gate's observed results → the closing artefacts | a proof level written stronger than its evidence becomes a false completeness claim Phase 35 relies on |
| a folded todo → its disposition | closing by association destroys the tracker for a defect that still ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.6-41 | Repudiation | a `PORTED-CHANNELS.md` row claiming LIVE proof against a NOT ATTEMPTED gate item | mitigate | Task 1's acceptance criteria require every LIVE level to cite an item number whose recorded result is a PASS; a citation to a NOT ATTEMPTED item is called out as a defect. |
| T-34.6-42 | Repudiation | writing "unit-proven" where the only evidence is that a function is called | mitigate | Task 1 states the rule inline: "the function is CALLED" proves wiring, not behaviour, and such rows must say so plainly. |
| T-34.6-43 | Repudiation | closing folded todo 4 by association with the phase that folded it | mitigate | Task 3 requires the literal words `NOT DISCHARGED` and a return to `pending/` unless a Humble login was actually performed. |
| T-34.6-44 | Repudiation | retiring `U-34.5-32` wholesale, asserting something untrue about a surface that still ships | mitigate | D-10's two-part disposition is required, with A-01's falsified premise recorded on the row so no later reader inherits it. |
| T-34.6-45 | Repudiation | a `resolves_phase:`-less todo silently escaping auto-close | mitigate | Task 3 sets the field on every touched file and asserts it with `grep -L`. |
| T-34.6-SC | Tampering | npm/pip/cargo installs | mitigate | Documentation-only plan. No packages installed. |
</threat_model>

<verification>
- `34.6-PORTED-CHANNELS.md` has 24 rows whose channel set diffs empty against the inventory's Phase-34.6 buckets.
- `U-34.5-32` carries two dispositions and its history is intact.
- All four folded todos are accounted for, each with `resolves_phase:` set.
- No source file is modified by this plan.
- `git diff -- .planning/STATE.md .planning/ROADMAP.md` is empty — the orchestrator owns those, and a concurrent session is active.
</verification>

<success_criteria>
- Every one of the 24 channels has an honest proof level traceable to a measured result.
- `U-34.5-32` splits rather than retiring, with A-01's correction recorded.
- Two-to-four folded todos are disposed of against their own bars, and the ones that do not close say why and when to revisit.
- The phase's own undocumented discoveries are recorded with the commands that found them.
</success_criteria>

<output>
Create `.planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-14-SUMMARY.md` when done, including the 24-row-vs-inventory diff.
Commit with `git commit --only <paths>`. Never use `git stash` or `git reset`.
</output>
