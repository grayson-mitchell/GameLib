---
phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
plan: 20
subsystem: docs
tags: [planning-record, requirements, gap-closure, todo-hygiene]

requires:
  - phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w plan 18
    provides: "assertPlausibleAbsolutePath, the corrected control this plan's record edits cite"
  - phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w plan 19
    provides: "the user-visible rejection dialog, cited alongside plan 18 in the corrected record"
provides:
  - "REQUIREMENTS.md's REQ-34.6-05 entry no longer claims completion for a BLOCKED requirement; checkbox is [ ] pending plan 34.6-21's live re-drive"
  - "REQ-34.6-10/-11/-12/-14 checkboxes flipped [ ] -> [x], matching their SUMMARY frontmatter and 34.6-VERIFICATION.md's independent confirmation"
  - "34.6-PORTED-CHANNELS.md's importGame/moveInstall rows state the control the code actually has (assertPlausibleAbsolutePath), not the removed assertContainedPath containment"
  - "34.6-VALIDATION.md's Per-Task Verification Map extended with the four gap-cycle-2 tasks (18-T1, 18-T2, 19-T1/T2, 21-T2)"
  - "CR-01 todo closed with resolves_phase: 34.6, moved to completed/, decision recorded"
  - "Adjacent winePrefix/wineVersion todo annotated with the containment precedent, left OPEN"
affects: [34.6-21-PLAN.md's live re-drive and its REQ-34.6-05 checkbox flip, any future reader of REQUIREMENTS.md/34.6-PORTED-CHANNELS.md/34.6-VALIDATION.md]

tech-stack:
  added: []
  patterns:
    - "Un-ticking a checkbox is itself the correction, not an omission to avoid, when the tick asserts completion for a requirement an independent verification marks BLOCKED"

key-files:
  created: []
  modified:
    - .planning/REQUIREMENTS.md
    - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-PORTED-CHANNELS.md
    - .planning/phases/34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w/34.6-VALIDATION.md
    - .planning/todos/completed/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md
    - .planning/todos/pending/2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md

key-decisions:
  - "REQ-34.6-05's checkbox was CHANGED from [x] to [ ] rather than left ticked, because the tick itself was part of the false record this plan exists to correct — 34.6-VERIFICATION.md marks the requirement BLOCKED and the live re-drive (34.6-21) has not run"
  - "requirements-completed below lists only REQ-34.6-10/-11/-12/-14, not REQ-34.6-05 — this plan corrected REQ-34.6-05's record but did not complete it; listing it as completed here would repeat the exact failure this plan exists to fix"
  - "ROADMAP.md was verified, not edited — commit ca5e3348c already wrote the gap-cycle-2 block and all four 34.6-18..21 plan rows, confirmed present exactly once"

patterns-established:
  - "Split a todo's content edit and its git mv into separate commits so rename detection survives a large content append (git-mv-plus-big-append-defeats-rename-detection)"

requirements-completed: [REQ-34.6-10, REQ-34.6-11, REQ-34.6-12, REQ-34.6-14]

duration: ~35min
completed: 2026-08-26
---

# Phase 34.6 Plan 20: Reconcile the written record with the code Summary

**Un-ticked REQ-34.6-05's false "REQUIREMENT NOW FULLY COMPLETE" claim, flipped four stale checkboxes, corrected `34.6-PORTED-CHANNELS.md`'s hardening claim from the removed `assertContainedPath` to the live `assertPlausibleAbsolutePath`, extended the validation map, and closed the CR-01 todo with the field auto-close needs.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-26T13:30:00+12:00 (approx.)
- **Completed:** 2026-08-26T14:10:00+12:00 (approx.)
- **Tasks:** 3/3 completed
- **Files modified:** 5

## Accomplishments

- `.planning/REQUIREMENTS.md`'s REQ-34.6-05 entry no longer reads "REQUIREMENT NOW FULLY COMPLETE" — it now records the CR-01 regression (`34.6-REVIEW.md`, independently re-verified in `34.6-VERIFICATION.md`, status `gaps_found`), the landed fix (`assertPlausibleAbsolutePath`, plans 34.6-18/34.6-19), and the one item still outstanding (plan 34.6-21's live re-drive). Its checkbox is `[ ]`, not `[x]`.
- REQ-34.6-10/-11/-12/-14 checkboxes flipped `[ ]` -> `[x]`, each citing `34.6-VERIFICATION.md`'s Requirements Coverage table plus the specific originating SUMMARY's `requirements-completed` frontmatter (34.6-13, 34.6-14, 34.6-15, 34.6-17 as named per requirement in the plan).
- `.planning/ROADMAP.md` verified — not edited. Commit `ca5e3348c` already carries the `Gap cycle 2 — 4 plans authored` block and all four `34.6-18..21-PLAN.md` rows, present exactly once (`grep -c` returns `1` and `4` respectively).
- `34.6-PORTED-CHANNELS.md`'s `importGame` and `moveInstall` rows rewritten at the trailing hardening clause only: both now state the control was `assertContainedPath`, CORRECTED by gap plan 34.6-18 after CR-01/`34.6-VERIFICATION.md`, with the live control now `rendererPathGuard.assertPlausibleAbsolutePath` (proof level `unit`, pending plan 34.6-21's live drive). The two product-defect descriptions (GOG identity mismatch on import; macOS `openrsync` rejection on move) and their committed-todo cross-references are untouched.
- `34.6-VALIDATION.md`'s Per-Task Verification Map extended with four rows (`18-T1`, `18-T2`, `19-T1/T2`, `21-T2`) covering the gap-cycle-2 tasks, every command using the capitalised `--selectProjects Backend`/`Frontend` form. A corrected Validation Sign-Off line was added noting REQ-34.6-05's replacement validator is RED-proven in BOTH directions, not just against known-bad input — the one-directional bar that let CR-01 through.
- The CR-01 todo gained `resolves_phase: 34.6`, `status: RESOLVED`, a "Fix direction — DECIDED" section mirroring `34.6-18-PLAN.md`'s `<decision>` block (including the circular-root argument against `setSetting`), and a "Closure evidence" section — then moved to `.planning/todos/completed/` in a separate commit so `git diff -M` detects the rename (confirmed: `rename ... (100%)`).
- The adjacent `winePrefix`/`wineVersion` todo gained one sentence recording that gap plan 34.6-18's dropped-containment decision narrows its own open design question, and flagging its `planned_as: 34.6-14` field as stale. It stays `status: OPEN` and otherwise unchanged.

## Task Commits

Each task was committed atomically, split further where rename detection required it:

1. **Task 1: Correct REQ-34.6-05 and flip the four stale checkboxes** - `d847c06ac` (docs)
2. **Task 2: Correct `34.6-PORTED-CHANNELS.md` and extend `34.6-VALIDATION.md`** - `6a7341e28` (docs)
3. **Task 3a: Record the CR-01 todo's decision (content edit, pre-move)** - `d27bcd3cf` (docs)
3. **Task 3b: Move the CR-01 todo to `completed/`** - `b3ede8b4d` (docs)
3. **Task 3c: Annotate the adjacent winePrefix/wineVersion todo** - `e9f562710` (docs)

**Plan metadata:** committed separately below (SUMMARY.md itself).

_Note: Task 3 was split into three commits — content edit, then `git mv`, then the adjacent-file annotation — per the plan's explicit instruction that a `git mv` combined with a large content append defeats rename detection._

## Files Created/Modified

- `.planning/REQUIREMENTS.md` - REQ-34.6-05 corrected and un-ticked; REQ-34.6-10/-11/-12/-14 ticked with evidence citations
- `.planning/phases/34.6-.../34.6-PORTED-CHANNELS.md` - `importGame`/`moveInstall` rows' hardening clause corrected to name the live control
- `.planning/phases/34.6-.../34.6-VALIDATION.md` - four rows appended to the Per-Task Verification Map; sign-off line corrected
- `.planning/todos/completed/2026-08-25-path-containment-guard-rejects-moveinstall-and-importgame-primary-use-case.md` - CR-01 todo closed, decision recorded, moved from `pending/`
- `.planning/todos/pending/2026-08-24-importgame-wineprefix-wineversion-not-contained-by-34-6-11.md` - one sentence added noting the containment precedent; stays OPEN

## Decisions Made

- REQ-34.6-05's checkbox was deliberately un-ticked (`[x]` -> `[ ]`) rather than left alone, because the tick was itself part of the false record — `34.6-VERIFICATION.md` marks this requirement BLOCKED, and plan 34.6-21's live re-drive has not yet run. Leaving it ticked would have shipped the same completion-claim-on-a-blocked-requirement failure this plan exists to undo.
- This SUMMARY's `requirements-completed` frontmatter lists only REQ-34.6-10/-11/-12/-14, deliberately excluding REQ-34.6-05, even though it appears in the plan's `requirements` field. REQ-34.6-05 was corrected, not completed, by this plan — listing it as completed here would repeat the exact honesty failure this plan exists to fix.
- `.planning/ROADMAP.md` was left untouched. Verification confirmed commit `ca5e3348c` already wrote the gap-cycle-2 block and all four plan rows exactly once; editing it would have risked a duplicate append, which the plan's threat model (T-34.6-63) specifically calls out as the more likely failure mode here.

## Deviations from Plan

None - plan executed exactly as written. All acceptance-criteria greps were run and returned the exact values the plan specified before each commit.

## Quoted `assertContainedPath` occurrences after correction

Both surviving mentions in `34.6-PORTED-CHANNELS.md`, quoted per the plan's `<output>` instruction so a reader can confirm neither is a live claim:

- `importGame` row: "Hardened by plan 34.6-11 (T-34.5-C6-49-03) via `rendererPathGuard.ts`'s `assertContainedPath` for the destination path only, then CORRECTED by gap plan 34.6-18 after `34.6-REVIEW.md` CR-01 / `34.6-VERIFICATION.md` found the containment root rejected the feature's own primary input ... the live control is now `rendererPathGuard.assertPlausibleAbsolutePath` ... with `defaultInstallPath` containment REMOVED entirely."
- `moveInstall` row: "Hardened by plan 34.6-11 (T-34.5-C6-49-03) via `assertContainedPath`, then CORRECTED by gap plan 34.6-18 after `34.6-REVIEW.md` CR-01 / `34.6-VERIFICATION.md` found the containment root rejected the feature's own primary input ... the live control is now `rendererPathGuard.assertPlausibleAbsolutePath` ... with `defaultInstallPath` containment REMOVED entirely."

Both occurrences are past-tense/historical references to the removed control, immediately followed by "CORRECTED by gap plan 34.6-18" and the live control's name — neither reads as a current claim.

## Issues Encountered

None. `git status --porcelain` was checked before every commit; the concurrent session's foreign staged rename (`2026-08-21-steam-library-22-games-never-reach-the-rendered-library.md`, pending -> completed) and the unstaged `.planning/debug/` deletion/untracked pair were confirmed present but never staged or committed by this plan — `git show --stat --format="" --name-only HEAD` after every commit confirms only this plan's declared files landed.

## Verification Run

- `sed -n '846p' .planning/REQUIREMENTS.md | grep -c 'REQUIREMENT NOW FULLY COMPLETE'` -> `0`
- `grep -cE '^- \[x\] \*\*REQ-34\.6-(10|11|12|14)\*\*' .planning/REQUIREMENTS.md` -> `4`
- `grep -c 'assertPlausibleAbsolutePath' 34.6-PORTED-CHANNELS.md` -> `2`
- `grep -c '^- \[x\] \*\*REQ-34.6-05\*\*' .planning/REQUIREMENTS.md` -> `0`; `git diff -U0 ... | grep -c '^-- \[x\] \*\*REQ-34.6-05\*\*'` -> `1`
- `git status --porcelain .planning/ROADMAP.md` -> no output; `grep -c 'Gap cycle 2 — 4 plans authored' .planning/ROADMAP.md` -> `1`
- CR-01 todo confirmed in `completed/` with `resolves_phase: 34.6`; adjacent todo confirmed still `pending/`, `status: OPEN`
- `pnpm planning-gates` -> **6/7**, sole failure `.planning/phases/34.4.1-*/seam-parity-sweep-gate.py` (pre-existing, unrelated to this plan's `.planning/` edits — this plan touched no file that gate reads), matching the baseline this plan's own verification block names. No second gate went red.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The written record now agrees with the code for every item this plan's scope covered. REQ-34.6-05 remains honestly open, gated on plan 34.6-21's live re-drive, which is the next and final plan in this gap cycle.
- Plan 34.6-21 is explicitly marked NOT autonomous — it drives a live gate and re-scores `34.6-LIVE-GATE.md`'s gap-cycle-2 section, then is the only plan authorized to flip REQ-34.6-05's checkbox back to `[x]`, and only if the drive returns PASS.

---
*Phase: 34.6-tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w*
*Completed: 2026-08-26*
