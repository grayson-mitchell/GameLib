---
phase: 34.6
slug: tauri-ipc-re-plumb-slice-9-eos-overlay-steamgriddb-artwork-w
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-23
---

# Phase 34.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `34.6-RESEARCH.md` § "Validation Architecture", with the test commands
> **corrected** — see the ⚠ note under Test Infrastructure.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29 (`ts-jest` preset, **transpile-only** — type errors do NOT fail a suite; `tsc --noEmit` is a separate gate). **Rust is in scope this phase** — A-03's `keyring_account()` slot edits `src-tauri/src/main.rs`, so `cargo check` + `cargo test` are mandatory, not optional. |
| **Config file** | Root `jest.config.js` (5 projects) → `src/backend/jest.config.js` (`displayName: 'Backend'`, `rootDir: '../..'`, `roots: ['<rootDir>/src/backend']`, `setupFiles: jest.setupContainment.ts`, `globalSetup: jest.globalSetup.js`); `src/frontend/jest.config.js` (`displayName: 'Frontend'`); `src-tauri/Cargo.toml` |
| **Quick run command** | `npx jest --selectProjects Backend --testPathPattern "<suite>"` (run from repo root) |
| **Full suite command** | `npx jest --selectProjects Backend` + `npx jest --selectProjects Frontend` + `npx tsc --noEmit` + `cd src-tauri && cargo check && cargo test keyring` |
| **Estimated runtime** | Backend project ~90s; targeted `--testPathPattern` run <10s; `cargo check` ~20s warm |

⚠ **Two command-shape hazards measured during planning — do not regress them.**

1. **`--selectProjects` is CASE-SENSITIVE and fails OPEN.** The project displayNames are
   `Backend` and `Frontend`, capitalised. `npx jest --selectProjects backend --listTests`
   prints *"You provided values --selectProjects but no projects found matching selection"*
   **and exits 0**. A task or CI step written with the lowercase form is a **silently vacuous
   green** — it asserts nothing and reports success. Every automated command in this file uses
   the capitalised form. Any new one must too.
2. **Do not gate on a full `pnpm test`.** This project has a recorded lesson that a full-suite
   run manufactures a *different* failure set under load (988s in-suite vs 0.7s alone). Gate on
   the per-project runs above. Equally, do **not** substitute a module-scoped `cd src/backend &&
   npx jest` — that form has shipped a red HEAD here for a day by missing same-shape fixtures in
   other directories.

---

## Sampling Rate

- **After every task commit:** `npx jest --selectProjects Backend --testPathPattern "<the suite(s) this task touched>"`
- **After every plan wave:** full `Backend` + `Frontend` project runs, plus `npx tsc --noEmit`; on any wave touching `src-tauri/`, also `cargo check`
- **Before `/gsd:verify-work`:** both project runs green, `tsc --noEmit` clean, `cargo check` clean, `preload-surface-gate.py` green (D-03), and the blocking live gate PASS
- **Max feedback latency:** ~90 seconds (full Backend project); <10 seconds for the targeted per-task form

---

## Per-Task Verification Map

> Populated at planning (`/gsd-plan-phase 34.6`, 2026-08-23) and kept current by executors.
> **Re-clustered from `34.6-RESEARCH.md`'s 12-ID recommendation to 14** — its own assumption A2
> declared that clustering a recommendation, not a constraint. Two IDs were added:
> **REQ-34.6-13** owns the three red-by-construction gates `34.6-PATTERNS.md` found that neither
> CONTEXT.md nor RESEARCH.md named, and **REQ-34.6-14** owns the closing artefacts no other
> requirement covered. Requirement text is in `.planning/REQUIREMENTS.md`.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08-T1 | 34.6-08 | 4 | REQ-34.6-01 (8 EOS channels registered from a new module) | T-34.6-21 | Dialog hazard exercised deliberately at `remove()` (A-02), not left unobserved | unit (registry presence + curated-import guard) | `npx jest --selectProjects Backend --testPathPattern "eosOverlayFlows"` | ❌ W0 — new suite | ⬜ pending |
| 09-T1 | 34.6-09 | 5 | REQ-34.6-02 (5 SteamGridDB channels registered) | T-34.6-01 | Key reachable ONLY via `getSteamGridDbSecretStore()`; gated on REQ-34.6-06 landing first (A-03) | unit (registry presence + never-touches-config) | `npx jest --selectProjects Backend --testPathPattern "enrichmentFlows"` | ✅ extend existing | ⬜ pending |
| 07-T1 | 34.6-07 | 3 | REQ-34.6-03 (3 winetricks channels; `listInstalled` resolves with real, possibly empty, data — **not** a decline, per A-01) | — | N/A | unit (registry presence + kind) | `npx jest --selectProjects Backend --testPathPattern "wineToolsFlows"` | ✅ extend existing | ⬜ pending |
| 05-T2 / 06-T1 / 07-T1 / 09-T1 / 10-T1 | 34.6-05, -06, -07, -09, -10 | 1,2,3,5,6 | REQ-34.6-04 (8 late-discovered registered across 4 modules) | T-34.5-C6-49-03 (3 of the 8) | Ported byte-equivalently first, per D-02 | unit (registry presence + kind) | `npx jest --selectProjects Backend --testPathPattern "appShellFlows\|installFlows\|wineToolsFlows\|runnerMiscFlows"` | ✅ extend existing | ⬜ pending |
| 11-T1/T2 | 34.6-11 | 7 | REQ-34.6-05 (harden `importGame`/`moveInstall`/`runWineCommandForGame`) | **T-34.5-C6-49-03** | Containment against a NON-renderer-supplied root, mirroring `resolveContainedPath` (depot.ts:990, VERIFIED to exist); shell removed from the Windows branch | unit, RED-proven against a traversal AND a legitimate apostrophe path | `npx jest --selectProjects Backend --testPathPattern "rendererPathGuard\|installFlows\|wineToolsFlows"` | ❌ W0 — new suite | ⬜ pending |
| 01-T1/T2/T3, 02-T1/T2/T3 | 34.6-01, -02 | 1, 2 | REQ-34.6-06 (SteamGridDB key hardening — **A-03**, lands BEFORE REQ-34.6-02) | **T-34.6-01 info-disclosure: plaintext secret**, T-34.6-02 slot allowlist, T-34.6-05 dev-vault fall-through | Key persists only via OS Keychain slot `steamgrid-api-key`; SET→invalidateCache→GET→exact-compare→delete migration; total-method store; BOTH bootstrap arms wired | unit (migration round-trip + never-writes-config) + Rust unit | `npx jest --selectProjects Backend --testPathPattern "steamgrid.*[Ss]ecret"` and `cd src-tauri && cargo test keyring_account` | ❌ W0 — new suites | ⬜ pending |
| 05-T1, 07-T1 | 34.6-05, -07 | 1, 3 | REQ-34.6-07 (D-11 observable for the 2 send-kind channels) | T-34.6-13 (log hygiene) | `logSendHandlerReached(channel)` takes ONLY a channel name — the `args` parameter does not exist, so it cannot be misused | unit (exact-string log assertion) | `npx jest --selectProjects Backend --testPathPattern "sendChannelObservable"` | ❌ W0 — new suite | ⬜ pending |
| 07-T3, 08-T3, 09-T3, 10-T2 | 34.6-07, -08, -09, -10 | 3,4,5,6 | REQ-34.6-08 (Describe 6 + Describe 7 re-derived: 3 Zoom absence + 16 presence + a 24-channel census) | T-34.6-20, T-34.6-30 | Zoom's 3 stay unregistered (34.5 D-02); kind asserted, not only presence | unit, **RED-proven in BOTH directions**, arithmetic from MEASUREMENT | `npx jest --selectProjects Backend --testPathPattern "runnerSliceRegistration"` | ✅ modify existing | ⬜ pending |
| 10-T3 | 34.6-10 | 6 | REQ-34.6-09 (`EosDeclineCallSiteGuard.test.ts` green and UNMODIFIED, D-08; 11-vs-10 reconciled to a measured 20/6) | T-34.6-28 | `callOrDeclare` remains the only renderer→`gamelib.log` path in the app | unit (source-text gate) + a re-measured project-wide grep census | `npx jest --selectProjects Frontend --testPathPattern "EosDeclineCallSiteGuard"` plus `git diff` proving the file untouched | ✅ existing, verify unmodified | ⬜ pending |
| 13-T1/T2 | 34.6-13 | 9 | REQ-34.6-10 (`preload-surface-gate.py` re-run at close; absorb ≤10 or HALT) | T-34.6-37, T-34.6-38 | Self-test FIRST — a live run of an unproven gate is a green that measures nothing; never weaken the assertion to make it pass | script (not jest) + a blocking operator decision | `python3 .planning/phases/34.5-*/preload-surface-gate.py --self-test && python3 .planning/phases/34.5-*/preload-surface-gate.py` | ✅ existing script | ⬜ pending |
| 03-T1/T2, 04-T1/T2/T3, 12-T1/T2/T3 | 34.6-03, -04, -12 | 1, 2, 8 | REQ-34.6-11 (ONE blocking live gate, sequenced per D-12; contract authored with EMPTY slots FIRST) | T-34.6-08, T-34.6-09, T-34.6-35 | D-13's identity-not-count assertion; D-15's SIDLogin refusal; A-01's no-decline correction; A-02's remove-not-enable correction | **manual-only** | N/A — see Manual-Only Verifications | ⬜ pending |
| 14-T3 | 34.6-14 | 9 | REQ-34.6-12 (four folded-todo dispositions, incl. the ones that do NOT close) | T-34.6-43, T-34.6-45 | Todo 4 recorded `NOT DISCHARGED` unless a Humble login actually happened; `resolves_phase:` set on every touched file | **manual-only, opportunistic** + a `grep -L 'resolves_phase'` check | `grep -L 'resolves_phase' .planning/todos/pending/2026-08-23-*.md .planning/todos/completed/2026-08-23-*.md` prints nothing | N/A | ⬜ pending |
| 05-T3, 06-T2, 07-T2, 08-T2, 09-T2 | 34.6-05, -06, -07, -08, -09 | 1,2,3,4,5 | REQ-34.6-13 (all five red-by-construction gates amended in the SAME commit as the change that breaks them) | T-34.6-29 (bundle-only defect) | No red HEAD crosses a plan boundary; ledger additions read from the test's own failure output, never predicted | unit + a **bundle-level** smoke assertion | `npx jest --selectProjects Backend --testPathPattern "wineToolsFlows\|enrichmentFlows\|electronReachLedger"` and `pnpm build:sidecar && node build/main/sidecar.js` | ✅ modify existing | ⬜ pending |
| 13-T3, 14-T1/T2 | 34.6-13, -14 | 9 | REQ-34.6-14 (closing artefacts written from measured results; `U-34.5-32` splits; A-04's citation fixed in place) | T-34.6-41, T-34.6-42, T-34.6-44 | Every `LIVE` proof level cites a gate item whose recorded result is a PASS; "the function is CALLED" is never written as "unit-proven" | documentation, cross-checked by a diff of the 24-row channel set against `IPC-PORT-INVENTORY.md`'s own bucket lines | `grep -c 'GlobalState.tsx:1586' .planning/IPC-PORT-INVENTORY.md` returns 0 | N/A | ⬜ pending |
| 18-T1 | 34.6-18 | 1 | REQ-34.6-05 | T-34.6-46, T-34.6-47 | shape validator proven in BOTH directions at the primitive | unit | `npx jest --selectProjects Backend --testPathPattern "rendererPathGuard"` | ✅ existing | ✅ green |
| 18-T2 | 34.6-18 | 1 | REQ-34.6-05 | T-34.5-C6-49-03, T-34.6-48 | cross-root ACCEPT RED-proven before the fix; header comments re-dispositioned same commit | unit | `npx jest --selectProjects Backend --testPathPattern "installFlows\|rendererPathGuard"` | ✅ existing | ✅ green |
| 19-T1/T2 | 34.6-19 | 2 | REQ-34.6-05 | T-34.6-32, T-34.6-50, T-34.6-51 | rejection is user-visible; fork strings confined to gamelib.json; namespace actually loaded | unit | `npx jest --selectProjects Backend --testPathPattern "installFlows\|i18nCatalogChurnGuard"` | ✅ existing | ✅ green |
| 21-T2 | 34.6-21 | 4 | REQ-34.6-05 | T-34.5-C6-49-03 | the cross-root case driven LIVE, with the macOS openrsync confound distinguishable from a containment rejection | manual (live gate) | MANUAL — see `34.6-LIVE-GATE.md` gap-cycle-2 section | ✅ existing | ⬜ pending |

`21-T2` is this phase's only remaining manual item; it is contracted with empty result slots in `34.6-LIVE-GATE.md`'s gap-cycle-2 section before any run.

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] New backend suite for the SteamGridDB keyring-backed secret store, mirroring `src/backend/sidecar/__tests__/humbleSecretStore.test.ts` — covers REQ-34.6-06
- [ ] Rust `keyring_account()` test for the new `steamgrid-api-key` slot, mirroring the existing `keyring_account_maps_humble_session_to_its_own_distinct_account`-style tests in `src-tauri/src/main.rs` — covers REQ-34.6-06
- [ ] New/extended `*FlowRegistration.ts` suite coverage for the 8 EOS + 8 late-discovered channels, in whichever registration module(s) the planner assigns — covers REQ-34.6-01/04
- [ ] New suite asserting the D-11 sidecar-handler observable fires for `frontendReady` and `winetricksInstall`, and that it carries **no** `args` — covers REQ-34.6-07
- [ ] New validator suite for REQ-34.6-05, RED-proven against a known-bad traversal/command input before it is allowed to go green
- [ ] `34.6-LIVE-GATE.md` contract file, written with **empty result slots BEFORE any live run**, mirroring `34.4.1-LIVE-GATE-RERUN-4.md`'s Task 1 discipline — covers REQ-34.6-11
- [ ] A **bundle-level** smoke assertion (`node build/main/sidecar.js` starts) — a bundle-only defect is structurally invisible to jest, and this phase adds imports to the sidecar's registration chain

---

## Manual-Only Verifications

> One blocking live gate, one verdict, no partial pass (34.5 D-08). **Sequence is fixed** by
> D-12/D-14/D-15 and A-01. Build freshness must be confirmed first: `pnpm tauri:dev` exits 0
> **without replacing a running instance**, and `pgrep GameLib` misses `gamelib-shell` — a whole
> UAT here once ran on an 8-hour-old build.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| **Step 0 — pre-port marker capture** (A-01) | REQ-34.6-11 / D-10 | The `DECLARED_UNAVAILABLE_MARKER` only appears while winetricks is **unported**; after the port there is nothing to observe | On a build **without** the winetricks registration, open the surface that calls a winetricks channel and capture `DECLARED_UNAVAILABLE_MARKER` in a real `gamelib.log`. This is the literal's **first live observation in project history** — it cannot be recovered once the port lands. |
| **Step 1 — Epic embedded login** | REQ-34.6-11 / D-15 | Real Epic credentials; establishes the session every later step depends on | Log in via the **embedded/primary** tile. **SIDLogin does NOT satisfy this** — item 11 means the primary path specifically. Rider: watch for a `keyring_get` timeout and **measure** elapsed time (folded todo 1); a non-firing rider is not a discharge. |
| **Step 2 — EOS overlay round-trip** | REQ-34.6-11 / D-05 + A-02 | legendary shell-out through the sidecar; traverses the 34.5 D-15 dialog crash hazard | install → enable → disable → remove. **The dialog fires at `remove()` (`eos_overlay.ts:162`, unconditional), NOT at `enable()`** — `enable()`'s dialog at `:197` is gated `if (!isInstalled())` and cannot fire here. "No dialog at enable" is the **correct** outcome, not a defect. Record the restore step for the operator's legendary/EOS state. |
| **Step 3 — SteamGridDB full chain** | REQ-34.6-11 / D-06 | Requires a real SteamGridDB API key and real network | `setApiKey` → `hasApiKey` → `searchGame` → `getGrids` → `getHeroes`, with artwork rendering in `SteamGridDBPicker`. **Precondition: REQ-34.6-06's keyring hardening has landed** (A-03) — otherwise this step writes the operator's real key to disk in plaintext. Verify the key lands in the Keychain slot, not `config.json`. |
| **Step 4 — winetricks** | REQ-34.6-11 / D-04 as corrected by **A-01** | Needs a real Wine prefix / macOS environment | Assert the channel **resolves with real data** through the sidecar and that the **D-11 observable shows the handler was reached**. An empty list is an acceptable result (data-dependent on `winetricks.log` at the prefix), **not** a decline. **Do not write a step expecting a decline message** — winetricks has genuine macOS support (`macEnvs`, `tools/index.ts:628/642`) and none will appear. |
| **Step 5 — the 8 late-discovered on the live session** | REQ-34.6-11 / D-07 | Needs the authenticated Epic/GOG session from step 1 | `getAchievements`, `getPlaytimeFromRunner`, `getGogDiscounts` ride the session. `moveInstall`/`importGame` use a **small fixture game** — resume/move over **existing content**, which this project has measured at seconds, not the "90 GB" the naive reading implies. |
| **Step 6 — `egsSync`** | REQ-34.6-11 | Already-ported code, never exercised live | Drive `egsSync` through the UI on the live Epic session. |
| **Step 7 — legendary save sync round-trip** | REQ-34.6-11 / D-14 | Both directions must be proven; the upload half can **destroy** a save | **Back the save up first.** Mutate a save locally → sync up → alter/delete locally → sync down → assert the mutation came back. Precondition: `getDefaultSavePath` is ported (D-14's wave-ordering constraint). "The command exited 0" is **not** proof — a sync that silently syncs nothing would pass that. |
| **Step 8 — Epic logout, LAST** | REQ-34.6-11 / D-13 | Destroys the session every prior step needs | **Plant a named non-Epic cookie in the jar first**, then log out, then assert **that exact cookie survives BY NAME** via an independent re-read. Record `before total` and `matched` in the result so a future reader can distinguish a real pass from an arithmetically-forced one (34.4.1 precondition 6, UNSTRUCK). A count alone is **not** sufficient — this amends the ROADMAP's stated bar. Rider: if a generic RPC timeout co-occurs, capture the **full shell scrollback**, not just `gamelib.log` (folded todo 2 — `eprintln!` goes to stderr and never reaches the log file). |
| **Optional rider — Humble login** | REQ-34.6-12 | Not required by this phase's own leg | Folded todo 4 (`D-29-02`) is dischargeable **only if** the operator additionally opens a Humble login. **If no Humble login is performed, record it as NOT DISCHARGED and return it to pending** — it must not be closed by association with the phase that folded it. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify
- [ ] Wave 0 covers all MISSING references above
- [ ] No watch-mode flags in any committed command
- [ ] **Every `--selectProjects` value is capitalised (`Backend`/`Frontend`)** — the lowercase form matches nothing and exits 0
- [ ] REQ-34.6-08's split is RED-proven in **both** directions; REQ-34.6-05's validator is RED-proven against known-bad input
- [x] **CORRECTED (gap plan 34.6-20):** the original line above required RED-proof only "against known-bad input" — a one-directional bar that let CR-01 through undetected (the validator rejected bad input correctly but was never proven against the LEGITIMATE cross-root/out-of-tree specimen it also had to accept). REQ-34.6-05's replacement validator (`assertPlausibleAbsolutePath`, plan 34.6-18) is RED-proven in **BOTH** directions: against known-bad input, AND against the primary real-world specimen each channel exists to accept (`evidence/34.6-18-RED-crossroot.txt`).
- [ ] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-23 at `/gsd-plan-phase 34.6`. Every task in plans 34.6-01..14 carries an `<automated>` verify or an explicit Wave 0 dependency; the four manual-only items are the live gate's own steps, whose contract is authored with empty result slots BEFORE any run. Every `--selectProjects` value in this file and in all 14 plans is capitalised.
