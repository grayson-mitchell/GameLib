---
phase: 07-game-details-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/storeManagers/steam/electronStores.ts
  - src/backend/storeManagers/steam/games.ts
  - src/backend/storeManagers/steam/library.ts
  - src/frontend/screens/Game/GamePage/index.css
autonomous: true
gap_closure: true
requirements: [DETAIL-01, DETAIL-02]

must_haves:
  truths:
    - "A Mac-native Steam game whose art was cached before Phase 7 reports is_mac_native=true after one library open/refresh (self-healing re-fetch), without a full resync."
    - "The self-heal re-fetch runs once per game, not on every getGameInfo/render (no fetch loop, T-2-03 pendingFetches dedup preserved)."
    - "A Steam game installed on macOS reports its installed platform as the Mac build ('Mac'), not the literal 'Windows' constant; Linux installs report 'linux'."
    - "Supported-platform icons in the Install-info tab have a visible horizontal gap (>=5px) between glyphs."
    - "DETAIL-02 AppleGamingWiki overlay becomes re-testable because is_mac_native is now correctly populated for cached Mac games (no DETAIL-02 code change required)."
  artifacts:
    - path: "src/backend/storeManagers/steam/electronStores.ts"
      provides: "platformsCaptured sentinel on SteamMetadataCacheEntry"
      contains: "platformsCaptured"
    - path: "src/backend/storeManagers/steam/games.ts"
      provides: "Self-healing platform re-fetch guard + platformsCaptured persistence"
    - path: "src/backend/storeManagers/steam/library.ts"
      provides: "Host-derived installed platform (no hardcoded Windows)"
    - path: "src/frontend/screens/Game/GamePage/index.css"
      provides: "Visible spacing between platform-support icons"
  key_links:
    - from: "src/backend/storeManagers/steam/games.ts getGameInfo"
      to: "steamMetadataStore.platformsCaptured"
      via: "refetch guard consults the cache sentinel"
      pattern: "platformsCaptured"
    - from: "src/backend/storeManagers/steam/library.ts install construction"
      to: "InstallPlatform (Mac/linux/Windows)"
      via: "host-platform derivation replacing 'Windows' as const"
      pattern: "install.*platform"
---

<objective>
Close three UAT gaps from `07-UAT.md` for Phase 7 (Game Details Enrichment). All three
stem from Steam platform data, plus one cosmetic icon-spacing miss:

1. GAP 1 (major, DETAIL-01): Steam supported-platform flags never re-populate for games
   whose art was cached before Phase 7, so multiplatform Steam games (e.g. 7 Days to Die)
   show Windows-only and the DETAIL-02 overlay never gates on.
2. GAP 2 (major, DETAIL-01): installed platform is hardcoded to 'Windows' at every Steam
   install-info construction site, so a Mac install reads "Windows".
3. GAP 3 (cosmetic, DETAIL-01): platform-support icons crowd each other; need >=5px gap.

Purpose: DETAIL-01 must work for the ENTIRE existing Steam library (not just freshly-synced
uncached games), and DETAIL-02's Mac gate depends on GAP 1's is_mac_native being correct.
This plan does NOT re-plan the phase — it fixes only these three diagnosed gaps.

Output: self-healing platform re-fetch, correct installed-platform reporting, icon spacing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-game-details-enrichment/07-CONTEXT.md
@.planning/phases/07-game-details-enrichment/07-UAT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

SteamMetadataCacheEntry (src/backend/storeManagers/steam/electronStores.ts:25):
  art_cover: string
  art_square: string
  extra: ExtraInfo
  is_mac_native?: boolean      // optional — undefined on pre-Phase-7 entries
  is_linux_native?: boolean    // optional — undefined on pre-Phase-7 entries
  is_delisted?: boolean
  // (this plan adds) platformsCaptured?: boolean

steam/games.ts getGameInfo (L152-162): reads library.get(appId); current refetch guard is
  `if (!existing.art_cover) { void this.fetchMetadataIfNeeded(existing) }`.
steam/games.ts fetchMetadataIfNeeded (L174): pendingFetches.add BEFORE first await (T-2-03).
  Captures platforms at L230-231 (`!!data.platforms?.mac` / `.linux`); persists to
  steamMetadataStore.set at L246-253; delisted terminal branch at L186-199 (returns early).

steam/library.ts install construction (3 sites): `platform: 'Windows' as const` at L205,
  L325, L486. Imports already present (L14): `import { isWindows, isMac } from
  'backend/constants/environment'`. `isLinux` is also exported from that module.

InstallPlatform valid members (src/common/types.ts:618): 'Windows' | 'Win32' | 'Mac' (Legendary),
  'windows' | 'osx' | 'linux' (Gog), 'windows' | 'linux' (Zoom), 'Browser'.
  Frontend detects mac via `['osx','Mac'].includes(platform)` and linux via
  `['linux','Linux'].includes(platform)` (GamePage/index.tsx:334-335). InstalledInfo.tsx:29
  reads `install.platform` DIRECTLY for its "Installed Platform" display.

CSS: `.platformSupport__icons` rule lives in GamePage/index.css:769, nested under
  `.extraInfoTabs` (L722), currently `display: inline-flex; gap: var(--space-sm);`.
  `--space-sm` is defined globally (`:root`, _spacing.scss = 0.75em). PlatformSupport.tsx
  renders `<span className="platformSupport__icons">` with FontAwesome brand glyphs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-healing Steam platform-flag re-fetch (GAP 1, DETAIL-01)</name>
  <files>src/backend/storeManagers/steam/electronStores.ts, src/backend/storeManagers/steam/games.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/electronStores.ts (SteamMetadataCacheEntry, L25-38)
    - src/backend/storeManagers/steam/games.ts (getGameInfo L152-162; fetchMetadataIfNeeded L174-259)
  </read_first>
  <action>
    Fix the root cause at games.ts:157 where the platform-capture re-fetch is gated ONLY on
    `!existing.art_cover`, so every pre-Phase-7 cached game skips the ONLY code path that
    records is_mac_native/is_linux_native.

    1. In electronStores.ts, add an optional sentinel field `platformsCaptured?: boolean` to
       `SteamMetadataCacheEntry` (with a short comment: set true once a fetch has recorded the
       appdetails `platforms` object; undefined on pre-Phase-7 entries lets us distinguish
       "never captured" from a genuine Windows-only `false`).

    2. In games.ts `fetchMetadataIfNeeded`, on the SUCCESSFUL capture path add
       `platformsCaptured: true` to the `steamMetadataStore.set(this.appId, { ... })` object
       (the same object that already sets is_mac_native/is_linux_native around L246-253).
       Do NOT remove or alter the existing `!!data.platforms?.mac` / `.linux` capture at
       L230-231 — verify it is preserved.

    3. In games.ts `getGameInfo`, change the re-fetch guard (currently `if (!existing.art_cover)`)
       so it ALSO fires when the cache entry was never platform-captured. Read the cache once via
       `steamMetadataStore.get(this.appId)` and trigger `fetchMetadataIfNeeded(existing)` when
       `!existing.art_cover` OR (`!existing.is_delisted` AND the cache entry's
       `platformsCaptured !== true`). Gating the platform condition on `!existing.is_delisted`
       prevents a re-fetch loop for delisted games (whose terminal branch returns before
       capturing platforms). Because fetchMetadataIfNeeded persists `platformsCaptured: true`,
       the fetch runs at most once per game and subsequent getGameInfo calls skip it — this is
       the self-healing one-time re-fetch.

    Preserve the T-2-03 dedup: fetchMetadataIfNeeded already does `pendingFetches.add` before the
    first await and returns early if the appId is in-flight — do not change that ordering. The new
    guard may call fetchMetadataIfNeeded on repeated renders while a fetch is in-flight; the
    pendingFetches check absorbs that safely. Import steamMetadataStore in games.ts if it is not
    already imported.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && pnpm tsc --noEmit 2>&1 | tail -5; grep -q "platformsCaptured" src/backend/storeManagers/steam/electronStores.ts && grep -q "platformsCaptured" src/backend/storeManagers/steam/games.ts && echo GUARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - games.ts getGameInfo no longer gates the platform re-fetch SOLELY on `!existing.art_cover`; it also fires when the cache entry's `platformsCaptured !== true` (for non-delisted games).
    - SteamMetadataCacheEntry has a `platformsCaptured?: boolean` field; fetchMetadataIfNeeded sets it true on the successful capture path.
    - A Mac-native cached Steam game reports is_mac_native=true after one getGameInfo/refresh, without a full resync.
    - The re-fetch runs at most once per game (platformsCaptured makes it skip afterward); no per-render fetch loop.
    - The L230-231 `data.platforms?.mac`/`.linux` capture and the T-2-03 pendingFetches.add-before-await ordering are unchanged.
    - `pnpm tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Multiplatform Steam games in the pre-existing cache surface is_mac_native/is_linux_native after one library open, unblocking DETAIL-01 icons and the DETAIL-02 Mac overlay gate.</done>
</task>

<task type="auto">
  <name>Task 2: Derive installed platform instead of hardcoding Windows (GAP 2, DETAIL-01)</name>
  <files>src/backend/storeManagers/steam/library.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/library.ts (install construction at L205, L325, L486; imports L14)
    - src/frontend/screens/Game/GamePage/components/InstalledInfo.tsx (reads install.platform directly, L29)
  </read_first>
  <action>
    Replace the hardcoded `platform: 'Windows' as const` at all three install-info construction
    sites in library.ts (L205, L325, L486) with a host-derived InstallPlatform. Steam installs
    the depot for the host OS, so on macOS the install is the Mac build.

    Add a small module-level helper (e.g. `hostInstallPlatform(): InstallPlatform`) near the top
    of library.ts that returns `'Mac'` when `isMac`, `'linux'` when `isLinux`, else `'Windows'`.
    These are all valid `InstallPlatform` union members and match the frontend's mac/linux
    detection (`['osx','Mac']` and `['linux','Linux']` in GamePage/index.tsx). Add `isLinux` to the
    existing `import { isWindows, isMac } from 'backend/constants/environment'` (isLinux is exported
    from that module). Use the helper at all three sites in place of `'Windows' as const`.

    Keep it type-correct against `InstallPlatform` (do NOT use `as const` on a computed value; the
    helper's return type annotation carries the type). InstalledInfo.tsx reads `install.platform`
    directly for its "Installed Platform" display, so this backend change is what the UI shows.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && pnpm tsc --noEmit 2>&1 | tail -5; test "$(grep -c \"platform: 'Windows' as const\" src/backend/storeManagers/steam/library.ts)" = "0" && echo NO_HARDCODED_WINDOWS</automated>
  </verify>
  <acceptance_criteria>
    - No `platform: 'Windows' as const` literal remains in library.ts (all three sites use the host-derived helper).
    - On macOS the installed platform resolves to 'Mac'; on Linux to 'linux'; on Windows to 'Windows'.
    - The value is type-correct against the `InstallPlatform` union (tsc passes).
    - InstalledInfo's "Installed Platform" row reads this field (no separate frontend change needed).
  </acceptance_criteria>
  <done>A Steam game installed on macOS shows "Installed Platform: Mac" (the Mac build), not "Windows".</done>
</task>

<task type="auto">
  <name>Task 3: Space out platform-support icons (GAP 3, DETAIL-01 cosmetic)</name>
  <files>src/frontend/screens/Game/GamePage/index.css</files>
  <read_first>
    - src/frontend/screens/Game/GamePage/index.css (`.platformSupport__icons` rule at L769, under `.extraInfoTabs`)
    - src/frontend/screens/Game/GamePage/components/PlatformSupport.tsx (renders the icon span)
  </read_first>
  <action>
    UAT test 2 reports the supported-platform icons render almost on top of each other despite the
    existing `.platformSupport__icons { gap: var(--space-sm) }` rule. Ensure a visible horizontal
    gap of at least ~5px renders between the FontAwesome brand glyphs.

    In index.css, verify the `.platformSupport__icons` rule (L769) is actually producing spacing
    when the app runs; if the flex `gap` is not visibly separating the inline SVG glyphs, make the
    spacing explicit and robust — e.g. give the glyphs an inter-icon horizontal margin
    (`margin-inline: var(--space-2xs)` ≈ 6px, or bump the `gap` to a clearly visible value) so
    adjacent icons are separated by >=5px. Keep using the semantic spacing tokens from _spacing.scss
    (no hard-coded px per the CSS convention); `--space-2xs` (0.375em) or `--space-xs` (0.5em) are
    appropriate. This is the ONLY UI change — do not alter the icons, layout, or PlatformSupport.tsx
    logic.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && grep -A4 "platformSupport__icons" src/frontend/screens/Game/GamePage/index.css | grep -E "gap:|margin" && echo SPACING_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `.platformSupport__icons` (or the glyphs within it) carries an explicit horizontal spacing declaration using a semantic spacing token (not a hard-coded hex/px).
    - The rendered platform icons have >=5px visible horizontal separation between adjacent glyphs.
    - No change to PlatformSupport.tsx or other GamePage layout.
  </acceptance_criteria>
  <done>Platform-support icons in the Install-info tab are clearly separated (>=5px) rather than crowding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Steam appdetails API → backend | Public JSON already consumed by fetchMetadataIfNeeded; `data.platforms` is booleans coerced via `!!` — no new untrusted surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | appdetails `platforms` object | accept | Values coerced with `!!` to boolean; no injection path. Existing behavior, unchanged. |
| T-07-02 | Denial of Service | getGameInfo re-fetch guard | mitigate | `platformsCaptured` sentinel + pendingFetches dedup ensure at most one fetch per game; delisted-branch exclusion prevents re-fetch loops. |

No package installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
- `pnpm tsc --noEmit` passes (no type regressions from the InstallPlatform derivation or the new cache field).
- `pnpm lint` clean on the touched files.
- No `platform: 'Windows' as const` literal remains in steam/library.ts.
- `platformsCaptured` present in both electronStores.ts and games.ts; re-fetch guard no longer gates solely on `!existing.art_cover`.
- `.platformSupport__icons` carries explicit token-based spacing.
- Manual re-UAT (macOS, real Steam account) of UAT tests 1, 3, 4, 5, 6, 7 is expected after this plan — GAP 1's fix unblocks the DETAIL-02 overlay tests.
</verification>

<success_criteria>
- A pre-Phase-7-cached Mac-native Steam game reports is_mac_native=true after one library open (self-heal), showing the Apple glyph and enabling the darwin+is_mac_native DETAIL-02 overlay gate.
- A Steam game installed on macOS reports "Installed Platform: Mac".
- Platform-support icons have >=5px visible spacing.
- No DETAIL-02 code change required; its UAT tests become re-testable.
- tsc + lint clean; T-2-03 dedup preserved.
</success_criteria>

<output>
Create `.planning/phases/07-game-details-enrichment/07-02-SUMMARY.md` when done.
</output>
