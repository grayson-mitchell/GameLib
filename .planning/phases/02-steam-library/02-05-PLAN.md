---
phase: 02-steam-library
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - src/frontend/assets/steam-logo.svg
  - src/frontend/components/UI/StoreLogos/index.tsx
  - src/frontend/screens/Library/components/GameCard/index.tsx
  - src/frontend/screens/Library/components/GameCard/index.css
  - src/frontend/screens/Library/components/LibraryHeader/index.tsx
  - src/frontend/screens/Library/components/LibraryHeader/index.css
  - src/common/types/ipc.ts
  - src/preload/api/steam.ts
  - src/backend/main.ts
  - public/locales/en/translation.json
autonomous: true
requirements: [LIB-02, LIB-03, LIB-04]

must_haves:
  truths:
    - "Each Steam GameCard shows the Steam store logo"
    - "Each Steam GameCard shows playtime as 'N hours' / '1 hour' / 'Never played', and renders nothing when playtime is unknown"
    - "While metadata is loading the card shows the AppID placeholder title and a skeleton cover (existing CachedImage opacity pattern)"
    - "Steam game descriptions render as escaped React text, never raw HTML"
    - "A background-sync spinner shows in the LibraryHeader while a Steam sync is running"
    - "When offline with cached data, a 'last synced X ago' stale indicator shows"
    - "A manual Refresh button re-triggers the Steam library sync"
  artifacts:
    - path: "src/frontend/assets/steam-logo.svg"
      provides: "Steam store mark with currentColor fill"
      contains: "svg"
    - path: "src/frontend/components/UI/StoreLogos/index.tsx"
      provides: "steam case"
      contains: "SteamLogo"
    - path: "src/frontend/screens/Library/components/GameCard/index.tsx"
      provides: "playtime display"
      contains: "steamPlaytime"
  key_links:
    - from: "LibraryHeader"
      to: "steamSyncStore syncedAt"
      via: "getSteamSyncedAt IPC"
      pattern: "getSteamSyncedAt"
    - from: "GameCard"
      to: "playtime"
      via: "gameInfo.extra.steamPlaytimeMinutes"
      pattern: "steamPlaytimeMinutes"
---

<objective>
Render the Steam-specific UI per 02-UI-SPEC.md: a Steam store logo on cards, playtime display ("N hours"/"Never played"), the metadata-loading skeleton (AppID placeholder + faded cover), a background-sync spinner in the LibraryHeader, an offline "last synced X ago" stale indicator, and a manual Refresh button. Adds one request-style IPC `getSteamSyncedAt` to read the last-sync timestamp. Completes the visible surface of LIB-02 (install/grayscale already from GameInfo), LIB-03 (playtime), LIB-04 (art/title/genres).

Purpose: Turns the raw data into the contracted user experience and honors D-02 (sync spinner / first-sync state), D-08 (hours format), D-09 (stale indicator).
Output: New steam-logo.svg, modified StoreLogos/GameCard/LibraryHeader (+css), new getSteamSyncedAt IPC across ipc.ts/preload/main, and i18n strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-steam-library/02-UI-SPEC.md
@.planning/phases/02-steam-library/02-PATTERNS.md
@.planning/phases/02-steam-library/02-04-SUMMARY.md

<interfaces>
StoreLogos (src/frontend/components/UI/StoreLogos/index.tsx): switch(runner) returning <XLogo className={className} />; import logos via `frontend/assets/x-logo.svg?react`.
GameCard (src/frontend/screens/Library/components/GameCard/index.tsx): `const { t } = useTranslation('gamepage')`, `const { t: t2 } = useTranslation()`; runner span around line 531-537 uses getStoreName(runner, t2('Other')); isInstalled at line 127; gameInfo in scope; cover via CachedImage; skeleton uses img.loading/img.loaded opacity (App.css).
LibraryHeader (src/frontend/screens/Library/components/LibraryHeader/index.tsx): Props { list }, uses useContext(LibraryContext) for showFavourites, useTranslation(); structure h5.libraryHeader > div.libraryHeaderWrapper > span.libraryTitle + ActionIcons. Pull `refreshing`, `refreshingInTheBackground`, `connectivity`, `storesFilters`... from ContextProvider (ContextType has refreshing:boolean line45, refreshingInTheBackground line46, connectivity line113).
ContextProvider also exposes refreshLibrary (and window.api.refreshLibrary('steam') routes to SteamLibraryManager.refresh()).
From 02-01: steamSyncStore (CacheStore<number,'syncedAt'>) in backend electronStores.
IPC: AsyncIPCFunctions (handlers) in src/common/types/ipc.ts ~lines 214-225 (steam handlers); preload handlers via makeHandlerInvoker in src/preload/api/steam.ts; backend handlers via addHandler in src/backend/main.ts.
i18n: add keys to public/locales/en/translation.json.
faSyncAlt from '@fortawesome/free-solid-svg-icons'; spinner @keyframes refreshing defined in UpdateComponent/index.css.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Steam logo asset and StoreLogos case</name>
  <files>src/frontend/assets/steam-logo.svg, src/frontend/components/UI/StoreLogos/index.tsx</files>
  <read_first>
    - src/frontend/assets/zoom-logo.svg (compact single-path SVG format with viewBox)
    - src/frontend/components/UI/StoreLogos/index.tsx (full file, lines 1-26)
    - .planning/phases/02-steam-library/02-PATTERNS.md (steam-logo.svg + StoreLogos sections)
    - .planning/phases/02-steam-library/02-UI-SPEC.md (Steam Store Logo SVG: fill currentColor, .store-icon 35x35)
  </read_first>
  <action>
    Create `src/frontend/assets/steam-logo.svg` as a compact single-colour SVG of the Steam gauge/"S" mark using the same format as zoom-logo.svg (root `<svg xmlns viewBox=...>` with one or more `<path>`). Set `fill="currentColor"` (or no explicit fill so it inherits) so it themes via `var(--text-default)` through the `.store-icon` class. Do NOT use the `<use>/<symbol>` nesting from gog-logo.svg.

    In `StoreLogos/index.tsx`: add `import SteamLogo from 'frontend/assets/steam-logo.svg?react'` after the ZoomLogo import, and add `case 'steam': return <SteamLogo className={className} />` before the `default` case.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - File src/frontend/assets/steam-logo.svg exists and contains `<svg` and `currentColor`
    - StoreLogos/index.tsx contains `SteamLogo` import and `case 'steam'`
    - `npm run codecheck` exits 0
  </acceptance_criteria>
  <done>Steam logo renders via StoreLogos for runner 'steam'; codecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: GameCard playtime display + skeleton + i18n strings</name>
  <files>src/frontend/screens/Library/components/GameCard/index.tsx, src/frontend/screens/Library/components/GameCard/index.css, public/locales/en/translation.json</files>
  <read_first>
    - src/frontend/screens/Library/components/GameCard/index.tsx lines 100-103 (t/t2), 441-460 (img/logo classes), 499-540 (cover + runner span area)
    - .planning/phases/02-steam-library/02-PATTERNS.md (GameCard playtime addition + CachedImage fallback)
    - .planning/phases/02-steam-library/02-UI-SPEC.md (Playtime Display; Interaction States; Copywriting Contract; Color tokens; spacing)
    - public/locales/en/translation.json (existing key structure for new keys)
  </read_first>
  <action>
    In `GameCard/index.tsx`, immediately after the runner `<span className="runner">` block (around line 537, inside the Link), add a Steam-only playtime span: render only when `runner === 'steam' && gameInfo.extra?.steamPlaytimeMinutes !== undefined`. Use `<span className="steamPlaytime">` whose text is: when value === 0 → `t2('game.steam.neverPlayed', 'Never played')`; else compute `hours = Math.round(value / 60)` and render `${hours} ${hours === 1 ? t2('game.hour', 'hour') : t2('game.hours', 'hours')}`. When steamPlaytimeMinutes is undefined, render nothing (omit element). Use t2 (default namespace) for these new keys. Do NOT introduce raw English without a t() wrapper.

    Confirm the existing skeleton/placeholder path covers Steam: the title already falls back to whatever GameInfo.title holds (AppID string until metadata loads, set by backend), and CachedImage with empty art_cover already applies the img.loading opacity skeleton + heroic_card.jpg fallback — no new skeleton code needed. Do NOT render description via dangerouslySetInnerHTML anywhere (T-2-02).

    In `GameCard/index.css`: add a `.steamPlaytime` rule using semantic tokens only — `font-size: var(--text-sm)`, `color: var(--text-secondary)`, weight `var(--regular)`. No hardcoded hex/px.

    In `public/locales/en/translation.json`: add keys matching the t() calls: `game.steam.neverPlayed` = "Never played", `game.hour` = "hour", `game.hours` = "hours" (create nested objects as needed without disturbing existing keys).
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - GameCard/index.tsx contains `steamPlaytime` and `steamPlaytimeMinutes` and gates on `runner === 'steam'`
    - GameCard/index.tsx contains `neverPlayed` and uses `Math.round` for hours
    - GameCard/index.tsx contains no `dangerouslySetInnerHTML`
    - GameCard/index.css `.steamPlaytime` uses `var(--text-sm)` and `var(--text-secondary)` (no raw hex)
    - translation.json contains "neverPlayed" and "hours"
    - `npm run codecheck` exits 0
  </acceptance_criteria>
  <done>Steam cards show correctly pluralized playtime / "Never played" / nothing-when-unknown; description stays escaped text; skeleton handled by existing pattern; tokens-only CSS.</done>
</task>

<task type="auto">
  <name>Task 3: getSteamSyncedAt IPC + LibraryHeader sync spinner, stale indicator, manual refresh</name>
  <files>src/common/types/ipc.ts, src/preload/api/steam.ts, src/backend/main.ts, src/frontend/screens/Library/components/LibraryHeader/index.tsx, src/frontend/screens/Library/components/LibraryHeader/index.css, public/locales/en/translation.json</files>
  <read_first>
    - src/common/types/ipc.ts lines 214-225 (steam async handler signatures)
    - src/preload/api/steam.ts (makeHandlerInvoker bridges)
    - src/backend/main.ts (addHandler usage, e.g. lines 587-589; steam handlers block)
    - src/backend/storeManagers/steam/electronStores.ts (steamSyncStore)
    - src/frontend/screens/Library/components/LibraryHeader/index.tsx (full file)
    - .planning/phases/02-steam-library/02-UI-SPEC.md (Sync spinner, Stale indicator, Manual Refresh button specs + copy + tokens)
    - .planning/phases/02-steam-library/02-PATTERNS.md (LibraryHeader section)
  </read_first>
  <action>
    Add a request-style IPC `getSteamSyncedAt(): Promise<number | null>`: declare it in the AsyncIPCFunctions block of `src/common/types/ipc.ts` near the other steam handlers (~line 225); add `export const getSteamSyncedAt = makeHandlerInvoker('getSteamSyncedAt')` in `src/preload/api/steam.ts`; register `addHandler('getSteamSyncedAt', () => steamSyncStore.get('syncedAt', null) ?? null)` in `src/backend/main.ts` (import steamSyncStore from the steam electronStores), placed with the other steam handlers.

    In `LibraryHeader/index.tsx`: pull `refreshing`, `refreshingInTheBackground`, `connectivity`, and the steam filter signal (`category`/`storesFilters` — read whichever indicates Steam is shown; if not readily available, gate on `connectivity` + a fetched syncedAt) from `useContext(ContextProvider)`. Implement three UI elements per UI-SPEC, all strings via t():
    1. Background-sync spinner: inside `span.libraryTitle` after `numberOfgames`, render a `faSyncAlt` FontAwesome icon with the existing `refreshing` animation class when `refreshing && refreshingInTheBackground` (reuse existing background-refresh state per CONTEXT discretion — no new sync-state machine). Tooltip `t('steam.syncing', 'Syncing Steam library…')`, color var(--text-secondary).
    2. Stale indicator: a second row inside `.libraryHeaderWrapper`, shown only when `connectivity.status !== 'online'` and the fetched syncedAt is non-null. Text `t('steam.lastSynced', 'Steam library last synced {{time}} ago', { time })` where `time` is the largest-whole-unit relative string (minutes→hours→days, never seconds) computed from `Date.now() - syncedAt`. Fetch syncedAt via `window.api.getSteamSyncedAt()` in a useEffect on mount and when connectivity.status changes; store in component state.
    3. Manual Refresh button: a `faSyncAlt` icon button (title `t('steam.refresh', 'Refresh Steam Library')`) that calls `window.api.refreshLibrary('steam')`; disabled + spinning while `refreshing`.

    In `LibraryHeader/index.css`: add rules for the spinner, stale indicator (`var(--text-xs)`, `var(--text-secondary)`), and refresh button using semantic tokens only; reuse the `@keyframes refreshing` animation (do not redefine it). No raw hex/px (35px store badge exception does not apply here).

    Add i18n keys to `public/locales/en/translation.json`: `steam.syncing`, `steam.lastSynced` (with `{{time}}` interpolation), `steam.refresh`. Do not duplicate keys added in Task 2.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - ipc.ts contains `getSteamSyncedAt`; preload steam.ts contains `getSteamSyncedAt`; main.ts registers `addHandler('getSteamSyncedAt'` and references `steamSyncStore`
    - LibraryHeader/index.tsx contains `faSyncAlt`, calls `window.api.getSteamSyncedAt`, calls `window.api.refreshLibrary('steam')`, and references `connectivity`
    - LibraryHeader/index.css uses `var(--text-xs)` / `var(--text-secondary)` and reuses (not redefines) `@keyframes refreshing`
    - translation.json contains `syncing`, `lastSynced`, `refresh` steam keys with `{{time}}` in lastSynced
    - `npm run codecheck` exits 0
  </acceptance_criteria>
  <done>LibraryHeader shows a background-sync spinner, an offline stale indicator backed by getSteamSyncedAt, and a working manual Refresh button; tokens-only CSS; codecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main ↔ renderer | GameInfo text/art + syncedAt timestamp cross IPC |
| renderer rendering | store description/genres/title rendered to DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02 | Tampering | Steam store short_description/genres in GameCard | mitigate | rendered only as React JSX text (auto-escaped); no dangerouslySetInnerHTML introduced (this plan) |
| T-2-04 | Elevation of Privilege | frontend imports | mitigate | no steam-user/steam-session in renderer; only GameInfo + getSteamSyncedAt timestamp via IPC |
| T-2-SC | Tampering | npm installs | accept | zero new packages; faSyncAlt + steam-logo.svg use existing deps/assets |
</threat_model>

<verification>
- `npm run codecheck` exits 0.
- `npm test -- --testPathPattern=steam` still exits 0 (no backend regressions).
- No `dangerouslySetInnerHTML` in GameCard or LibraryHeader.
- No raw hex/px introduced in new CSS (UI-SPEC token enforcement).
</verification>

<success_criteria>
Steam cards show the store logo and correctly formatted playtime, fall back to AppID + skeleton while metadata loads, and never render raw HTML; the LibraryHeader shows a sync spinner during background sync, a stale indicator when offline, and a manual Refresh button. LIB-02/03/04 are visually complete per 02-UI-SPEC.md.
</success_criteria>

<output>
Create `.planning/phases/02-steam-library/02-05-SUMMARY.md` when done.
</output>
