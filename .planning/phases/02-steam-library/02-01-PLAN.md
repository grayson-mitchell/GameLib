---
phase: 02-steam-library
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/backend/storeManagers/steam/user.ts
  - src/backend/storeManagers/steam/electronStores.ts
  - src/common/types.ts
  - src/backend/storeManagers/steam/__tests__/library.test.ts
  - src/backend/storeManagers/steam/__tests__/games.test.ts
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]

must_haves:
  truths:
    - "SteamLibraryManager.refresh() can obtain the authenticated steam-user client from Phase 1"
    - "A persistent indefinite cache exists for the Steam library list, per-game metadata, and last-sync timestamp"
    - "GameInfo.extra can carry Steam playtime in minutes"
    - "Jest test scaffolds exist for the library sync and lazy metadata behaviors so later waves have RED targets"
  artifacts:
    - path: "src/backend/storeManagers/steam/electronStores.ts"
      provides: "steamLibraryStore, steamMetadataStore, steamSyncStore CacheStores"
      contains: "steamLibraryStore"
    - path: "src/backend/storeManagers/steam/user.ts"
      provides: "public static getClient() accessor"
      contains: "getClient"
    - path: "src/common/types.ts"
      provides: "ExtraInfo.steamPlaytimeMinutes field"
      contains: "steamPlaytimeMinutes"
    - path: "src/backend/storeManagers/steam/__tests__/library.test.ts"
      provides: "RED test scaffold for LIB-01/02/03"
      contains: "describe"
    - path: "src/backend/storeManagers/steam/__tests__/games.test.ts"
      provides: "RED test scaffold for LIB-04"
      contains: "describe"
  key_links:
    - from: "src/backend/storeManagers/steam/library.ts"
      to: "src/backend/storeManagers/steam/user.ts"
      via: "SteamUser.getClient()"
      pattern: "getClient"
---

<objective>
Lay the Phase 2 foundation: expose the authenticated steam-user client from Phase 1's `SteamUser` class, create the persistent CacheStores for library/metadata/sync-timestamp caching, extend `ExtraInfo` with the Steam playtime field, and create the Jest test scaffolds (Wave 0) that downstream implementation waves will satisfy.

Purpose: Without `SteamUser.getClient()` the library manager cannot reach the CM connection; without the CacheStores there is no place to persist data; without the test scaffolds the Nyquist sampling contract (02-VALIDATION.md) has no RED targets.
Output: Modified `user.ts`, `electronStores.ts`, `common/types.ts`, plus two new test files with mock scaffolding and `it.todo` behavior stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-steam-library/02-RESEARCH.md
@.planning/phases/02-steam-library/02-PATTERNS.md
@.planning/phases/02-steam-library/02-VALIDATION.md

<interfaces>
Phase 1 SteamUser class (src/backend/storeManagers/steam/user.ts):
- `private static client: InstanceType<typeof SteamUserLib> | null` — the authenticated steam-user instance (set in finishAuth)
- `static isLoggedIn(): boolean`
- `import SteamUserLib from 'steam-user'` (esModuleInterop, export =)

CacheStore (src/backend/cache.ts):
- `constructor(filename: string, max_value_lifespan: number | null = 60*6, options?)` — null lifespan = never expires
- generic form used by Zoom: `new CacheStore<GameInfo[], 'games'>('zoom_library', null)`

ExtraInfo (src/common/types.ts lines 155-162): about?, reqs, releaseDate?, storeUrl?, changelog?, genres?

Existing electronStores.ts exports `configStore` (TypeCheckedStoreBackend, cwd 'steam_store').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SteamUser.getClient() accessor, create CacheStores, extend ExtraInfo</name>
  <files>src/backend/storeManagers/steam/user.ts, src/backend/storeManagers/steam/electronStores.ts, src/common/types.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/user.ts (current SteamUser class; note `private static client` line 53 and `isLoggedIn()` line 69)
    - src/backend/storeManagers/steam/electronStores.ts (current file — only exports configStore)
    - src/backend/storeManagers/zoom/electronStores.ts (CacheStore usage pattern, lines 1-22)
    - src/backend/cache.ts (CacheStore constructor signature, lines 17-21)
    - src/common/types.ts lines 155-162 (ExtraInfo interface)
    - .planning/phases/02-steam-library/02-PATTERNS.md (sections for user.ts, electronStores.ts, common/types.ts)
  </read_first>
  <action>
    In `user.ts`: add a public accessor `static getClient(): InstanceType<typeof SteamUserLib> | null { return this.client }` immediately after the `isLoggedIn()` method (around line 71). This is the only change to user.ts.

    In `electronStores.ts`: import `CacheStore` from `'../../cache'` and `GameInfo`, `ExtraInfo` from `'common/types'`. Add three CacheStores with indefinite (`null`) lifespan per D-05:
    - `steamLibraryStore = new CacheStore<GameInfo[], 'games'>('steam_library', null)` — the full library list keyed by 'games'.
    - `steamMetadataStore = new CacheStore<SteamMetadataCacheEntry>('steam_metadata', null)` — per-game metadata keyed by appId string.
    - `steamSyncStore = new CacheStore<number, 'syncedAt'>('steam_sync', null)` — last successful sync epoch millis keyed by 'syncedAt' (consumed by the stale indicator in plan 05).
    Define and export `interface SteamMetadataCacheEntry { art_cover: string; art_square: string; extra: ExtraInfo }`. Do NOT register any of these in StoreStructure — CacheStore manages its own file under store_cache/ (RESEARCH anti-pattern #2). Update the export line to include configStore, steamLibraryStore, steamMetadataStore, steamSyncStore, and the SteamMetadataCacheEntry type.

    In `common/types.ts`: add one optional field `steamPlaytimeMinutes?: number` to the `ExtraInfo` interface, after `genres?: string[]`. Comment it as "total playtime in minutes from getUserOwnedApps()". Field is optional so non-Steam runners leave it undefined.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "static getClient" src/backend/storeManagers/steam/user.ts` returns 1
    - `grep -c "steamLibraryStore\|steamMetadataStore\|steamSyncStore" src/backend/storeManagers/steam/electronStores.ts` is >= 3
    - electronStores.ts exports `SteamMetadataCacheEntry` (grep `export.*SteamMetadataCacheEntry` matches)
    - `grep -c "steamPlaytimeMinutes" src/common/types.ts` returns 1
    - `npm run codecheck` exits 0
  </acceptance_criteria>
  <done>getClient() accessor exists, three CacheStores + SteamMetadataCacheEntry type exported, ExtraInfo has steamPlaytimeMinutes, codecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create library.test.ts scaffold (RED) for LIB-01/02/03</name>
  <files>src/backend/storeManagers/steam/__tests__/library.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/__tests__/user.test.ts (Phase 1 test — mock factory patterns, jest.mock('backend/logger') factory to avoid fs-extra native crash, beforeEach mock re-establishment because jest.config resetMocks:true)
    - .planning/phases/02-steam-library/02-VALIDATION.md (Wave 0 Requirements: required mocks list; Per-Task Verification Map)
    - .planning/phases/02-steam-library/02-RESEARCH.md (Pattern 1 refresh(), buildInstalledMap; Code Examples getUserOwnedApps, ACF Parsing)
  </read_first>
  <action>
    Create the test file with full mock scaffolding (do NOT yet write passing assertions against real implementation — those are filled in plan 02). Set up these mocks at top of file: `jest.mock('backend/logger', () => ({ logInfo: jest.fn(), logError: jest.fn(), logWarning: jest.fn(), LogPrefix: {} }))` (factory form to avoid the fs-extra native module crash documented in Phase 1), `jest.mock('backend/utils', () => ({ getSteamLibraries: jest.fn() }))`, `jest.mock('graceful-fs')`, `jest.mock('@node-steam/vdf')`, `jest.mock('../../../ipc', () => ({ sendFrontendMessage: jest.fn() }))`, and `jest.mock('../user')` to control `SteamUser.getClient()`. Add a `describe('SteamLibraryManager', ...)` block containing `it.todo()` placeholders that name each required behavior so the suite stays green until plan 02 implements them:
    - it.todo('LIB-01: refresh() calls getUserOwnedApps and builds a GameInfo per owned app')
    - it.todo('LIB-01: refresh() calls sendFrontendMessage pushGameToLibrary once per game')
    - it.todo('LIB-02: buildInstalledMap marks is_installed true when StateFlags bit 4 is set (e.g. 4, 6, 516)')
    - it.todo('LIB-02: buildInstalledMap marks is_installed false when StateFlags bit 4 is clear')
    - it.todo('LIB-02: a corrupt/unparseable ACF file is skipped without throwing')
    - it.todo('LIB-03: GameInfo.extra.steamPlaytimeMinutes equals app.playtime_forever')
    - it.todo('refresh() serves cached library from steamLibraryStore when getUserOwnedApps throws')
    Do not import the not-yet-existing state.ts module at top level if it would break compilation; keep the scaffold compiling (todos require no implementation imports).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=steam/library --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - File src/backend/storeManagers/steam/__tests__/library.test.ts exists
    - `grep -c "it.todo" src/backend/storeManagers/steam/__tests__/library.test.ts` is >= 7
    - File contains the string `getSteamLibraries` (mock present) and `sendFrontendMessage` (mock present)
    - `npm test -- --testPathPattern=steam/library --passWithNoTests` exits 0 (todos do not fail)
  </acceptance_criteria>
  <done>library.test.ts exists with mock scaffolding and 7+ it.todo behavior stubs covering LIB-01/02/03; suite runs green.</done>
</task>

<task type="auto">
  <name>Task 3: Create games.test.ts scaffold (RED) for LIB-04</name>
  <files>src/backend/storeManagers/steam/__tests__/games.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/__tests__/user.test.ts (mock factory patterns)
    - .planning/phases/02-steam-library/02-RESEARCH.md (Pattern 2 getGameInfo + fetchMetadataIfNeeded; Code Examples Steam Store API; Pitfall 5 pendingFetches dedup)
    - .planning/phases/02-steam-library/02-VALIDATION.md (LIB-04 test rows)
  </read_first>
  <action>
    Create the test file with mock scaffolding: `jest.mock('backend/logger', () => ({ logInfo: jest.fn(), logError: jest.fn(), logWarning: jest.fn(), LogPrefix: {} }))`, `jest.mock('axios')`, `jest.mock('../../../ipc', () => ({ sendFrontendMessage: jest.fn() }))`, and a mock for the metadata cache store. Add `describe('SteamGame.getGameInfo lazy metadata', ...)` with `it.todo()` placeholders:
    - it.todo('LIB-04: getGameInfo returns the existing library entry synchronously')
    - it.todo('LIB-04: when art_cover is empty, fetchMetadataIfNeeded calls the Steam store appdetails API')
    - it.todo('LIB-04: after fetch, art_cover/art_square/title/genres/about.description are populated from the API response')
    - it.todo('LIB-04: fetched metadata is written to steamMetadataStore for indefinite reuse')
    - it.todo('LIB-04: fetchMetadataIfNeeded calls sendFrontendMessage pushGameToLibrary with the updated GameInfo')
    - it.todo('LIB-04: concurrent getGameInfo calls for the same appId only fire one network request (pendingFetches dedup)')
    - it.todo('LIB-04: a failed appdetails request is caught and logged without throwing')
    Keep the scaffold compiling.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=steam/games --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - File src/backend/storeManagers/steam/__tests__/games.test.ts exists
    - `grep -c "it.todo" src/backend/storeManagers/steam/__tests__/games.test.ts` is >= 7
    - File contains `jest.mock('axios')`
    - `npm test -- --testPathPattern=steam/games --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>games.test.ts exists with mock scaffolding and 7+ it.todo stubs covering LIB-04; suite runs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Steam CM ↔ main process | steam-user TCP connection; authenticated session from Phase 1 |
| Steam store API ↔ main process | untrusted HTTP responses (appdetails) cross here |
| local disk (ACF) ↔ main process | untrusted/possibly corrupt VDF files |
| main ↔ renderer (IPC) | GameInfo crosses; renderer must treat as data only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Denial of Service | ACF parse in buildInstalledMap | mitigate | try/catch each parse, skip corrupt files (implemented plan 02) |
| T-2-02 | Tampering | Steam store short_description | mitigate | render as React text only, never dangerouslySetInnerHTML (plan 05) |
| T-2-03 | Tampering | concurrent metadata fetches | mitigate | module-level pendingFetches Set dedup (plan 03) |
| T-2-04 | Elevation of Privilege | steam-user import surface | mitigate | steam-user/electronStores stay in src/backend only — this plan adds no renderer imports |
| T-2-SC | Tampering | npm installs | accept | Phase 2 installs zero new packages (RESEARCH Package Legitimacy Audit) — no supply-chain surface |
</threat_model>

<verification>
- `npm run codecheck` exits 0 (no type errors from new accessor, stores, or ExtraInfo field).
- `npm test -- --testPathPattern=steam --passWithNoTests` exits 0 (both scaffolds present and green).
- Both test files exist under src/backend/storeManagers/steam/__tests__/.
</verification>

<success_criteria>
SteamUser.getClient() is callable; steamLibraryStore/steamMetadataStore/steamSyncStore + SteamMetadataCacheEntry are exported; ExtraInfo carries steamPlaytimeMinutes; library.test.ts and games.test.ts exist as green RED-target scaffolds with named it.todo behaviors for LIB-01..04.
</success_criteria>

<output>
Create `.planning/phases/02-steam-library/02-01-SUMMARY.md` when done.
</output>
