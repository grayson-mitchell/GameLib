---
phase: 30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/storeManagers/steam/withTimeout.ts
  - src/backend/storeManagers/steam/installLocation.ts
  - src/backend/storeManagers/steam/depot.ts
  - src/backend/storeManagers/steam/games.ts
  - src/backend/storeManagers/steam/__tests__/withTimeout.test.ts
  - src/backend/storeManagers/steam/__tests__/depot.test.ts
  - src/backend/storeManagers/steam/__tests__/games.test.ts
  - src/backend/sidecar/__tests__/installFlows.test.ts
autonomous: true
gap_closure: true
requirements: [REQ-30-04, REQ-30-05]
user_setup: []

must_haves:
  truths:
    - "Under Tauri, clicking Install on a Steam title whose steam-user CM socket is present-but-unresponsive reaches a terminal state within a bounded time (badge clears + an ERROR dialog surfaces) instead of spinning on 'installing' forever"
    - "A never-settling steam-user getProductInfo() PICS call REJECTS within the bound instead of parking indefinitely — the reject flows through the EXISTING withPlanBuildRetry -> throw -> {status:'error'} path that 30-05's finally/catch already converts to a terminal 'done'"
    - "Every known pre-download CM primitive is bounded — getProductInfo (fetchInstalldir/fetchAppInfo/getOwnedSets/fetchDlcInfos), getDepotDecryptionKey + getRawManifest (fetchDepotPlanEntry, once per depot), and getContentServers (getContentServerHosts) — plus the resolveSteamInstallTarget phase in runNativeDepotDownload; a socket going stale at click OR mid-buildDepotPlan converts to a settled {status:'error'} rather than a hang"
    - "A healthy (fast-resolving) CM call is unaffected — the Electron build's install path behaves EXACTLY as before, no false-trip on a legitimately slow-but-progressing PICS fetch, and the streaming download phase (which runs AFTER the bounded pre-download phase) is never bounded"
  artifacts:
    - path: "src/backend/storeManagers/steam/withTimeout.ts"
      provides: "reusable Promise.race timeout wrapper that REJECTS with a descriptive error on a never-settling await"
      contains: "withTimeout"
    - path: "src/backend/storeManagers/steam/installLocation.ts"
      provides: "fetchInstalldir's getProductInfo bounded by withTimeout"
      contains: "withTimeout"
    - path: "src/backend/storeManagers/steam/depot.ts"
      provides: "getProductInfo (fetchAppInfo/getOwnedSets/fetchDlcInfos), getDepotDecryptionKey + getRawManifest (fetchDepotPlanEntry), and getContentServers (getContentServerHosts) bounded by withTimeout"
      contains: "withTimeout"
    - path: "src/backend/storeManagers/steam/games.ts"
      provides: "runNativeDepotDownload pre-download-phase watchdog returning {status:'error'} on stall"
      contains: "withTimeout"
  key_links:
    - from: "src/backend/storeManagers/steam/depot.ts"
      to: "withTimeout"
      via: "each pre-download getProductInfo/getDepotDecryptionKey/getRawManifest/getContentServers await wrapped so a hung CM job rejects"
      pattern: "withTimeout\\("
    - from: "src/backend/storeManagers/steam/games.ts"
      to: "withTimeout"
      via: "pre-download phase (resolveSteamInstallTarget) wrapped; a stall returns {status:'error'}"
      pattern: "withTimeout\\("
    - from: "src/backend/sidecar/installFlowRegistration.ts"
      to: "sendGameStatusUpdate"
      via: "30-05's EXISTING finally (hadError) / catch converts the timeout-origin {status:'error'}/throw into terminal 'done'"
      pattern: "status: 'done'"
---

<objective>
Close **G-30-02**: under the Tauri build, clicking Install on a Steam title hangs the "installing"
badge forever (live retest Test 1; also blocks the Install->Uninstall E2E, Test 4).

Root cause (ALREADY diagnosed — do NOT re-diagnose; see
`.planning/debug/steam-install-spinner-hangs-tauri-live-g3002.md` and the
`Gaps (retest cycle)` block in `30-HUMAN-UAT.md`): the native pre-download phase makes bare,
un-timed `steam-user` `getProductInfo()` PICS calls (`fetchInstalldir` via `resolveSteamInstallTarget`,
then `fetchAppInfo`/`getOwnedSets`/`fetchDlcInfos` in `buildDepotPlan`). `steam-user`'s
`getProductInfo` neither times out nor rejects when the CM socket is present-but-unresponsive — it
queues the job and the Promise **never settles**. Under Tauri the client is rehydrated from the
persisted store and `ensureConnected`'s fast-path (`user.ts:70-81`) returns `true` on any truthy
`client.steamID` without revalidating the socket, so a click-time `getProductInfo` can park forever.
`install()` therefore never returns AND never throws -> the sidecar handler's `await install()`
(`installFlowRegistration.ts:168`) never settles -> neither 30-05's `finally` (`hadError`) nor its
`catch` fires -> no terminal 'done' -> spinner stuck.

**Why 30-05 missed it:** 30-05 only handled the two SETTLED outcomes — a returned `{status:'error'}`
(the `finally`'s `hadError`) and a thrown error (the `catch`). A never-settling await is a THIRD
outcome, so neither guard can fire. 30-05's jest tests stay green because they mock the
depot/steam-user layer and resolve `install()` instantly.

**Fix strategy (two additive layers — do NOT duplicate 30-05's terminal-clear machinery, feed it):**

1. **PRIMARY — bound the CM primitives.** A small `withTimeout` wrapper on EVERY pre-download CM call —
   the `getProductInfo` PICS calls, `fetchDepotPlanEntry`'s per-depot `getDepotDecryptionKey` +
   `getRawManifest`, and `getContentServerHosts`'s `getContentServers` — converts a never-answered CM
   job into a bounded **rejection**. That rejection is already handled by the EXISTING machinery:
   `withPlanBuildRetry` retries it (bounded by `PLAN_BUILD_MAX_ATTEMPTS`) then rethrows ->
   `buildDepotPlan`/`downloadSteamDepots` fail -> `runNativeDepotDownload` returns/throws
   `{status:'error'}` -> 30-05's `finally`/`catch` pushes the terminal 'done' + surfaces the ERROR
   dialog. Zero new terminal-status logic.

2. **BELT-AND-SUSPENDERS — bound the pre-download PHASE.** Wrap the pre-download resolution portion of
   `runNativeDepotDownload` (`resolveSteamInstallTarget`) in the same `withTimeout`, returning
   `{status:'error'}` on stall. This guards against a FUTURE un-timed pre-download await that is NOT a
   CM primitive, and guarantees the badge can never hang from the pre-download phase — WITHOUT
   bounding the streaming download (which runs AFTER this region and can legitimately take hours; see
   the placement note below).

**Coverage note (checker finding #3 — chose to EXTEND, not narrow):** the two additive layers together
bound the COMPLETE known pre-download CM surface. In addition to `getProductInfo`, this plan wraps
`fetchDepotPlanEntry`'s `getDepotDecryptionKey`/`getRawManifest` (bare callback CM calls run once per
depot via the same `withPlanBuildRetry` machinery, `depot.ts:679-683`) — so a socket that goes stale
MID-`buildDepotPlan` (after the earlier `getProductInfo` calls already succeeded) can no longer hang
either. These are the same call-class as `getProductInfo` (bare, un-timed, callback-Promise-wrapped CM
round-trips) and flow through the identical retry-then-throw recovery, so wrapping them is low-risk and
additive. This is what makes must_haves truth #3 honestly true.

**Deliberate placement deviation (recorded so a reviewer sees it is intentional, not an oversight):**
the diagnosis's belt-and-suspenders wording says "a watchdog around the handler's `await install()`."
A flat timeout on `await install()` is REJECTED here: CR-03/CR-04 deliberately REMOVED the 60s
sidecar-invoke deadline precisely because a real depot install runs far longer than any fixed bound,
and the handler cannot cheaply observe the streaming `progressUpdate` frames to disarm such a watchdog
(`ipc.ts`'s `sendFrontendMessage` is a one-way `webContents.send`, not a subscribable emitter). The
hang is specifically in the PRE-download PICS/plan-build phase (before any `progressUpdate`), so this
plan bounds THAT phase — at the CM-primitive layer and around `resolveSteamInstallTarget` — never the
streaming download. This satisfies the design constraint ("bound that phase, not the streaming
download") literally and is strictly safer for long installs.

**`ensureConnected` hardening — SCOPED OUT (documented follow-up, deferred on its own merits).** The
option of "revalidate a live socket in `ensureConnected`'s fast-path" is deferred, NOT because any
source artifact labeled it optional (it does not — do not attribute that framing to the diagnosis),
but on its own technical merits: it sits on the hot path of every steam-user caller (library sync,
launch, install), cheap socket "liveness" is not reliably detectable without an actual CM round-trip,
and it is very hard to test deterministically. The two timeout layers above fully close G-30-02
without it. Record it in the SUMMARY as a deferred hardening item; do NOT implement it here.

Purpose: make the Tauri Steam install button ALWAYS leave the "installing" state — matching Electron —
even when the sidecar's long-lived steam-user CM connection has gone stale.
Output: `withTimeout` helper + bounded pre-download CM/phase awaits + RED->GREEN tests + a both-builds
regression sweep proving the change is additive and non-regressive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-HUMAN-UAT.md
@.planning/debug/steam-install-spinner-hangs-tauri-live-g3002.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-05-SUMMARY.md
@.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
@src/backend/storeManagers/steam/installLocation.ts
@src/backend/storeManagers/steam/depot.ts
@src/backend/storeManagers/steam/games.ts
@src/backend/storeManagers/steam/user.ts
@src/backend/sidecar/installFlowRegistration.ts

<interfaces>
<!-- Exact contracts the executor edits. Line anchors verified against the codebase. -->

The pre-download CM awaits to bound (all BARE `await`s with no timeout today):

getProductInfo (PICS):
- `installLocation.ts:161`  `fetchInstalldir` -> `await client.getProductInfo([numericAppId], [], true)`
    Its try/catch (lines 159-179) already catches a THROW and returns `undefined` (installdir has a
    benign fallback). Wrapping the await makes a hung call reject -> caught -> `undefined` -> install
    proceeds to `buildDepotPlan` (also bounded below).
- `depot.ts:412`  `getOwnedSets`  -> `client.getProductInfo([], packageIds, true)`
- `depot.ts:430`  `fetchAppInfo`  -> `client.getProductInfo([numericAppId], [], true)`
- `depot.ts:447`  `fetchDlcInfos` -> `client.getProductInfo(dlcIds, [], true)`
    These three run INSIDE `withPlanBuildRetry` (see below), each as a SEPARATE sequential call
    (`buildDepotPlan` calls them at depot.ts:607/625/631).

getDepotDecryptionKey / getRawManifest (per-depot manifest fetch — checker finding #3 additions):
- `depot.ts:524-530`  `fetchDepotPlanEntry` -> `new Promise<Buffer>((resolve, reject) => client.getDepotDecryptionKey(numericOwnerAppId, numericDepotId, cb))`
- `depot.ts:532-543`  `fetchDepotPlanEntry` -> `new Promise<Buffer>((resolve, reject) => client.getRawManifest(numericOwnerAppId, numericDepotId, descriptor.manifest, 'public', cb))`
    Both bare callback-Promise CM round-trips; `fetchDepotPlanEntry` runs once per depot via
    `withPlanBuildRetry` at depot.ts:679-683.

getContentServers (content-directory fetch — checker finding #1 CORRECTED ANCHOR):
- `depot.ts:2069-2075`  `getContentServerHosts` ->
    `await new Promise<RawContentServer[]>((resolvePromise, reject) => { client.getContentServers(numericAppId, cb) })`
    Invoked from `downloadSteamDepots` at `depot.ts:2202` (`await getContentServerHosts(client, Number(appId))`),
    which runs AFTER `buildDepotPlan` and BEFORE `downloadDepotFiles`'s first byte — correctly in the
    pre-download phase. Wrap EITHER the inner `new Promise` at 2069-2075 OR the `getContentServerHosts(...)`
    call at 2202. NOTE: `depot.ts:223` is the `SteamUserDepotClient` INTERFACE method signature
    (`getContentServers(appId, callback): void`) — do NOT edit line 223 (it is a type declaration, not a
    call). This is a legitimate belt-and-suspenders addition; it is NOT cited in the debug session file.
    Do NOT bound any per-chunk streaming/download call inside `downloadDepotFiles` — those run AFTER
    plan-build+directory fetch and emit `progressUpdate`.

`withPlanBuildRetry` (`depot.ts:346-399`) — the EXISTING recovery machinery a timeout rejection feeds:
```
async function withPlanBuildRetry<T>(signal, step, label?): Promise<T>
  for attempt 1..PLAN_BUILD_MAX_ATTEMPTS:
    try { return await step(getDepotClient()) }
    catch (err) {
      if (isNonRetryableDepotError(err)) throw err
      if (attempt === PLAN_BUILD_MAX_ATTEMPTS) break
      await SteamUser.ensureConnected(); await delay(PLAN_BUILD_RETRY_DELAY_MS, signal)
    }
  throw lastErr   // <- a persistent hung CM ends here: a THROW, not a hang
```
A timeout reject is retryable (it is NOT an `isNonRetryableDepotError` EResult), so the helper burns
its bounded attempts then throws — exactly the terminal throw the handler needs.

`runNativeDepotDownload` (`games.ts:1157-1265`) — pre-download vs streaming boundary:
```
ensureSteamClientReady(this.appId)           // sync probes, returns {ready:false} -> {status:'error'}
resolveSteamInstallTarget(this.appId, args)  // line 1191  <-- PRE-DOWNLOAD: wrap THIS in withTimeout
downloadSteamDepots(this.appId, {...})       // line 1203  <-- buildDepotPlan + getContentServerHosts
                                             //             (all CM calls inside bounded above) THEN
                                             //             streaming (progressUpdate at depot.ts:2222).
                                             //             Do NOT wrap this whole call.
```
Returning `{status:'error', error}` here is the exact contract 30-05 already clears (see below).

30-05's EXISTING terminal-clear machinery in `installFlowRegistration.ts` (DO NOT re-implement):
- lines 194-216: on `result.status === 'error'` sets `hadError = true` and (unless the error starts
  with `'Steam client not ready'`) calls `showDialogBoxModalAuto({type:'ERROR', message: result.error})`.
- lines 225-235: `finally { if (deferredToSetup || wasAborted || hadError) sendGameStatusUpdate({status:'done'}) }`.
- lines 219-224: `catch (error) { sendGameStatusUpdate({status:'done'}); throw error }`.
=> A returned `{status:'error'}` OR a thrown error BOTH already clear the badge. Your job is only to
make the hung CM PRODUCE one of those two outcomes within a bound.

`ensureConnected` bounds to weigh the timeout value against (`user.ts:70-144`): cold-connect 15s
(`connectSteamUserClient`) + a 20s grace window = ~35s worst case total.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add withTimeout and bound every pre-download CM call (getProductInfo, getDepotDecryptionKey/getRawManifest, getContentServers)</name>
  <files>src/backend/storeManagers/steam/withTimeout.ts, src/backend/storeManagers/steam/installLocation.ts, src/backend/storeManagers/steam/depot.ts, src/backend/storeManagers/steam/__tests__/withTimeout.test.ts, src/backend/storeManagers/steam/__tests__/depot.test.ts</files>
  <behavior>
    Write these tests FIRST (RED — they must FAIL against current HEAD, where the bare awaits never
    settle):
    - `withTimeout.test.ts` (self-contained; touches NO DecompressPool/worker_threads, so full fake
      timers are safe here):
      - `withTimeout(p, ms, label)` RESOLVES with p's value when p resolves before `ms` (transparent
        happy path — proves no behavior change for a healthy/fast call, protecting the Electron path).
      - `withTimeout(p, ms, label)` REJECTS with an Error whose message contains `label` and the ms
        bound when p never settles past `ms` (drive with `jest.useFakeTimers()` +
        `jest.advanceTimersByTimeAsync(ms)`; call `jest.useRealTimers()` in an `afterEach`).
      - When p REJECTS before `ms`, withTimeout rejects with p's ORIGINAL error (does not mask it) and
        does not leave a dangling timer (assert `jest.getTimerCount() === 0` after settle).
    - `depot.test.ts`: a `buildDepotPlan` run whose mocked `client.getProductInfo` returns a promise
      that NEVER resolves must cause `buildDepotPlan` to REJECT within the bounded window instead of
      hanging. Assert it rejects (not that it hangs). Keep an existing happy-path `buildDepotPlan` test
      green to prove the wrapper is transparent when `getProductInfo` resolves normally.
      FAKE-TIMER DISCIPLINE (checker finding #5 — depot.test.ts:3211-3217 documents that full fake
      timers were AVOIDED suite-wide due to real DecompressPool worker_threads, and depot.test.ts:589
      relies on REAL `delay()` for retry-backoff): scope `jest.useFakeTimers()` to THIS new test (or a
      dedicated `describe` block) ONLY, and restore with `jest.useRealTimers()` in a `finally`/
      `afterEach` so the surrounding real-timer-dependent tests stay stable. This new test's path
      (buildDepotPlan, PICS only) does not touch DecompressPool, so it is safe in isolation. Because
      `withPlanBuildRetry` interleaves the `withTimeout` bound with a real `delay()` backoff and an
      `ensureConnected()` call between attempts, use `jest.advanceTimersByTimeAsync(...)` (the async
      variant, which flushes microtasks) to step through BOTH the timeout rejections and the backoff
      delays, and keep the suite's existing `SteamUser.ensureConnected` mock so it resolves instantly.
    - `depot.test.ts` (EXTENDED-coverage RED case — checker follow-up; proves the finding #3 additions,
      not just getProductInfo): add at least one dedicated case that mocks a never-settling
      `getDepotDecryptionKey` (and/or `getRawManifest`, and/or `getContentServers`) — the same
      call-class as getProductInfo but reached LATER in plan-build — and asserts the flow produces a
      bounded reject that settles to `{status:'error'}` within the timeout, NOT a hang. It MUST FAIL
      against current HEAD (bare callback-Promise never settles). One combined case exercising a
      never-settling `getDepotDecryptionKey` on the `fetchDepotPlanEntry` path is sufficient (make the
      earlier getProductInfo calls resolve normally so execution actually REACHES the depot-manifest
      fetch). Reuse the identical fake-timer discipline above (tight `jest.useFakeTimers()` scope +
      `jest.useRealTimers()` restore + `jest.advanceTimersByTimeAsync(...)` to step BOTH the withTimeout
      reject and withPlanBuildRetry's real `delay()` backoff), and reuse the suite's existing
      buildDepotPlan/fetchDepotPlanEntry fixtures/mocks for consistency.
  </behavior>
  <action>
    Create `src/backend/storeManagers/steam/withTimeout.ts` exporting
    `export async function withTimeout<T>(promise: Promise<T>, ms: number, label: string): Promise<T>`.
    Implement as a `Promise.race([promise, rejectAfter(ms)])` where the timeout branch rejects with
    `new Error(\`${label} timed out after ${ms}ms\`)`. Clear the `setTimeout` handle in a `.finally()`
    (or equivalent) on the race so a fast-resolving promise never leaves a dangling timer (the
    withTimeout.test.ts timer-count assertion enforces this). Also export a shared
    `export const STEAM_PICS_TIMEOUT_MS = 25000` constant in this file.

    Choose and justify 25s (record the justification in the SUMMARY): healthy PICS `getProductInfo`
    round-trips are sub-second to low-single-digit seconds (see the `[Timing] fetchInstalldir` /
    `buildDepotPlan/<label>` logs); 25s sits BELOW `ensureConnected`'s ~35s worst case yet FAR above
    any healthy call, so it cannot false-trip a legitimately slow-but-progressing fetch. Because
    CR-03/CR-04 removed the 60s outer sidecar-invoke deadline, this per-call bound is the install's
    only pre-download deadline.

    WORST-CASE aggregate (checker finding #4 — state this ACCURATELY; do NOT undersell it as "a couple
    of minutes"): the three PICS calls in `buildDepotPlan` (`fetchAppInfo`/`getOwnedSets`/
    `fetchDlcInfos`) are THREE SEPARATE sequential `withPlanBuildRetry` calls (depot.ts:607/625/631),
    each able to burn `PLAN_BUILD_MAX_ATTEMPTS` (confirm the constant; 3 today) x 25s = 75s if the same
    socket-level staleness hangs all three — so the theoretical worst case is `fetchInstalldir` 25s +
    3 x 75s ≈ 250s (~4.2 min), plus per-depot manifest retries if the plan ever reaches them. The COMMON
    stale-at-click case is far shorter (~100s): `fetchInstalldir` swallows its 25s to `undefined`, then
    `fetchAppInfo` throws after its ~75s and short-circuits `buildDepotPlan` before the later calls.
    Set the human-retest expectation at "up to ~4-5 minutes worst case, commonly ~1.5 min" — still
    bounded and correct; the point is the badge ALWAYS clears, not that it clears instantly. (25s is
    kept, not reduced: lowering it to shrink the aggregate would risk false-tripping a slow-but-
    progressing PICS fetch on a poor connection, which is worse than a bounded wait.)

    In `installLocation.ts:161`, wrap the getProductInfo await:
    `await withTimeout(client.getProductInfo([numericAppId], [], true), STEAM_PICS_TIMEOUT_MS,
    'fetchInstalldir getProductInfo')`. The existing try/catch (159-179) already turns the resulting
    reject into a benign `undefined` installdir — no other change to that function.

    In `depot.ts`, wrap in `withTimeout(..., STEAM_PICS_TIMEOUT_MS, '<call-name>')` (import
    `withTimeout`/`STEAM_PICS_TIMEOUT_MS` from `./withTimeout`):
    - the three getProductInfo awaits: lines 412 (`getOwnedSets`), 430 (`fetchAppInfo`), 447
      (`fetchDlcInfos`);
    - `fetchDepotPlanEntry`'s two per-depot callback-Promise awaits: `getDepotDecryptionKey`
      (lines 524-530) and `getRawManifest` (lines 532-543) — wrap the `new Promise<Buffer>(...)`
      expression itself in `withTimeout(...)`; the `wrapDepotKeyError` reject wiring inside stays
      unchanged;
    - `getContentServerHosts`'s content-directory await: wrap the `new Promise<RawContentServer[]>(...)`
      at lines 2069-2075 (equivalently, wrap the `getContentServerHosts(client, Number(appId))` call at
      line 2202). Do NOT touch the interface method signature at line 223.
    Do NOT change `withPlanBuildRetry`'s logic — a timeout reject is a normal retryable throw it already
    handles (it is not an `isNonRetryableDepotError`). Do NOT wrap any per-chunk streaming/download call.

    Leave the `[Timing]` instrumentation lines in place (out of scope to remove here). Do NOT touch
    `src-tauri`, the handler, or `ensureConnected`.
  </action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/withTimeout.test.ts src/backend/storeManagers/steam/__tests__/depot.test.ts</automated>
  </verify>
  <done>withTimeout.test.ts and depot.test.ts pass: a never-settling getProductInfo makes buildDepotPlan REJECT within the bound; a fast getProductInfo is transparent (happy path unchanged); withTimeout leaves no dangling timer; the new fake-timer test is scoped and restores real timers so the rest of depot.test.ts stays stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound runNativeDepotDownload's pre-download phase + full both-builds regression sweep</name>
  <files>src/backend/storeManagers/steam/games.ts, src/backend/storeManagers/steam/__tests__/games.test.ts, src/backend/sidecar/__tests__/installFlows.test.ts</files>
  <behavior>
    Write these tests FIRST (RED against HEAD):
    - `games.test.ts` (follow the existing SteamGame install harness): a `runNativeDepotDownload` (via
      `SteamGame.install()` on the native branch) whose `resolveSteamInstallTarget` never settles must
      RESOLVE to `{status:'error'}` within the bounded window (fake timers advanced past the phase
      bound; scope the fake timers to this test and `jest.useRealTimers()` restore as in Task 1), NOT
      hang. Keep a happy-path native install test green (fast resolveSteamInstallTarget -> normal
      `{status:'done'}`) to prove the wrapper is transparent.
    - `installFlows.test.ts` (extend the Phase 30 sidecar suite): add a test where
      `SteamGame.install()` resolves `{status:'error', error:'<pre-download timed out ...>'}` (the
      shape Task 2 now produces) and assert the handler's gameStatusUpdate sequence reaches
      `['queued','installing','done']` AND emits a `showDialog` ERROR frame — i.e. the timeout-origin
      error rides 30-05's EXISTING finally/catch with no new handler code. The existing 30-05 tests
      (success emits only `['queued','installing']`, abort/deferred/client-not-ready) must stay green.
  </behavior>
  <action>
    In `games.ts` `runNativeDepotDownload`, wrap ONLY the pre-download resolution await
    (`resolveSteamInstallTarget(this.appId, args)`, line ~1191) in
    `withTimeout(resolveSteamInstallTarget(this.appId, args), STEAM_PICS_TIMEOUT_MS,
    'resolveSteamInstallTarget')` (import from `./withTimeout`). Because `resolveSteamInstallTarget`'s
    own timeout throw would propagate out of `runNativeDepotDownload` as an unhandled reject, catch it
    and convert to the returned-error contract 30-05 clears: return
    `{status:'error', error: 'Steam pre-download timed out: ' + String(err)}` (do NOT let it throw past
    the existing `finally` cleanup at lines 1258-1264). Keep the message OUT of the
    `'Steam client not ready'` prefix so 30-05 surfaces the ERROR dialog (this is a genuine failure,
    not the client-setup case). Do NOT wrap `downloadSteamDepots` (line 1203) — the buildDepotPlan and
    getContentServerHosts CM calls inside it are already bounded by Task 1, and the streaming phase
    after plan-build must stay unbounded (it emits `progressUpdate` and can run for hours).

    No change to `installFlowRegistration.ts` is required — 30-05's `finally (hadError)` / `catch`
    already convert both the returned `{status:'error'}` (this task) and any propagated throw (Task 1)
    into a terminal 'done'. The installFlows.test.ts addition LOCKS that this holds for the
    timeout-origin error.

    Then run the FULL regression sweep covering BOTH builds' invariants:
    - `npx jest src/backend/storeManagers/steam/ src/backend/sidecar/` — the steam store-manager suite
      (install/depot/games/library) AND the sidecar suite both stay green (proves the change is
      additive and the SEAM Invariant B non-fatal-import property holds — no new fatal import dragged
      into the sidecar bootstrap).
    - `npx tsc --noEmit` — typecheck clean (proves the Electron main-process build still compiles; the
      timeout wrappers are transparent pass-throughs on the healthy path, so `npm start`'s Steam
      install behaves exactly as before — the additive/reversible SEAM invariant).
    Record in the SUMMARY: (a) the 25s value justification AND the accurate ~4-5 min worst-case /
    ~1.5 min common-case bound, (b) that the streaming download phase is deliberately NOT bounded,
    (c) that coverage was EXTENDED to fetchDepotPlanEntry's getDepotDecryptionKey/getRawManifest so no
    known un-timed pre-download await remains, (d) the `ensureConnected` fast-path hardening is a
    documented deferred follow-up (deferred on its own technical merits, out of scope), and (e) that the
    two builds' Steam-install behavior is unchanged on a healthy CM (only a stale/unresponsive CM now
    converts to a bounded terminal error).
  </action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/ src/backend/sidecar/ && npx tsc --noEmit</automated>
  </verify>
  <done>games.test.ts proves a never-settling resolveSteamInstallTarget yields {status:'error'} within the bound (happy path unchanged); installFlows.test.ts proves the timeout-origin error reaches ['queued','installing','done'] + a showDialog ERROR frame via 30-05's existing guard; the full steam + sidecar suites and tsc --noEmit all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar steam-user client -> CM network | a stale/half-open CM socket can leave a getProductInfo/getDepotDecryptionKey/getRawManifest/getContentServers job unanswered indefinitely |
| sidecar -> renderer (frontend_message relay) | the timeout error text is surfaced via showDialog (reuses 30-05's already-analyzed path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07-01 | Denial of service | never-settling getProductInfo on a stale CM socket | mitigate | withTimeout bounds each pre-download getProductInfo at 25s; a hung job becomes a bounded reject that withPlanBuildRetry's existing bounded-retry -> throw -> {status:'error'} converts into a terminal badge-clear |
| T-30-07-02 | Denial of service | a socket going stale MID-buildDepotPlan (getDepotDecryptionKey/getRawManifest/getContentServers) or a future un-timed pre-download await | mitigate | fetchDepotPlanEntry's two per-depot CM calls AND getContentServerHosts are also wrapped (finding #3 extension), and resolveSteamInstallTarget is wrapped in runNativeDepotDownload — the complete known pre-download CM surface plus the resolution phase is bounded, not just the click-time getProductInfo |
| T-30-07-03 | Availability regression | bounding the streaming download by mistake would kill legitimate multi-hour installs | accept/mitigate | scope is strictly the pre-download PICS/plan-build/content-directory phase; downloadSteamDepots' streaming phase (post plan-build, emits progressUpdate) is explicitly NOT wrapped — CR-03/CR-04's long-install invariant preserved |
| T-30-07-04 | Information disclosure | showDialog message = timeout error string | mitigate | reuses 30-05's disposition — surfaces only the store-manager-produced error text, no new sensitive data; client-not-ready prefix still suppressed |
| T-30-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — no legitimacy gate needed |
</threat_model>

<verification>
- `npx jest src/backend/storeManagers/steam/ src/backend/sidecar/` passes (withTimeout, depot, games,
  install-flow wiring, and sidecar bootstrap).
- `npx tsc --noEmit` passes (Electron main build still compiles).
- A never-settling `getProductInfo` makes `buildDepotPlan` REJECT within the bound (was: hang).
- A never-settling `getDepotDecryptionKey`/`getRawManifest`/`getContentServers` likewise rejects within
  the bound (finding #3 extension — no un-timed pre-download CM await remains).
- A never-settling `resolveSteamInstallTarget` makes `runNativeDepotDownload` return `{status:'error'}`
  within the bound (was: hang).
- The timeout-origin `{status:'error'}` reaches `['queued','installing','done']` + a `showDialog` ERROR
  frame through 30-05's existing guard (no new handler code).
- Happy-path (fast CM call / fast resolveSteamInstallTarget) install behavior is unchanged —
  the wrappers are transparent, protecting the Electron build.
</verification>

<success_criteria>
- Under Tauri, clicking Install on a Steam title with a stale/unresponsive CM socket ALWAYS leaves the
  "installing" state within a bounded time — worst case ~4-5 min (three sequential PICS retries at
  25s x 3 attempts each), commonly ~1.5 min when the socket is already stale at click and the first
  PICS call short-circuits buildDepotPlan — badge clears + ERROR dialog. The G-30-02 spinner hang can
  no longer occur, unblocking the Install->Uninstall E2E (Test 4).
- The fix reuses 30-05's terminal-clear machinery (finally `hadError` / catch) rather than duplicating
  it — the two new timeout layers only ensure `install()` PRODUCES a settled outcome.
- The COMPLETE known pre-download CM surface is bounded (getProductInfo + getDepotDecryptionKey +
  getRawManifest + getContentServers) plus the resolveSteamInstallTarget phase — a socket going stale
  at click OR mid-plan-build both convert to a bounded terminal error.
- Both builds' Steam install behaves exactly as before on a healthy CM; the streaming download phase is
  never bounded; `npm start` (Electron) is non-regressed (additive/reversible SEAM invariant).
- `ensureConnected` fast-path hardening is recorded as a deferred follow-up, not silently dropped.
</success_criteria>

<output>
Create `.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-07-SUMMARY.md` when done.
</output>
