---
phase: 30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - src-tauri/src/main.rs
  - src/common/types/sidecarTransport.ts
  - src/backend/sidecar/electronStub.ts
  - src/backend/dialog/dialog.ts
  - src/backend/sidecar/__tests__/dialogStub.test.ts
autonomous: false
requirements: [REQ-30-07, REQ-30-09]

must_haves:
  truths:
    - "A folder picker opens natively in the Tauri build when the sidecar calls dialog.showOpenDialog"
    - "The picked path comes back to the sidecar in Electron's exact {canceled, filePaths} shape"
    - "notify() logs when it skips, instead of silently doing nothing"
    - "The rustInvoke allowlist and Rust's dispatch_rust_channel match arms name the same channel string"
  artifacts:
    - path: "src/common/types/sidecarTransport.ts"
      provides: "RUST_DIALOG_OPEN channel constant in RUST_INVOKE_CHANNELS"
      contains: "dialog_open"
    - path: "src-tauri/src/main.rs"
      provides: "dialog_open dispatch arm + AppHandle threading + dialog plugin registration"
      contains: "dialog_open"
    - path: "src/backend/sidecar/electronStub.ts"
      provides: "Real dialog.showOpenDialog forwarding through requestRustInvoke"
  key_links:
    - from: "src/backend/sidecar/electronStub.ts"
      to: "src/backend/sidecar/sidecarRpc.ts"
      via: "requestRustInvoke(RUST_DIALOG_OPEN, [options])"
      pattern: "requestRustInvoke\\(\\s*RUST_DIALOG_OPEN"
    - from: "src-tauri/src/main.rs"
      to: "tauri-plugin-dialog"
      via: "app.dialog().file().blocking_pick_folder()"
      pattern: "blocking_pick_folder"
---

<objective>
Give `dialog`'s open-directory path **real behavior** in `electronStub.ts`, bound to a Tauri
command through the existing generic `requestRustInvoke()` / `dispatch_rust_channel()` channel
(checklist steps 3 and 6, D-09), and close the live REQ-30-07 gap where `notify()`'s no-op is
currently **silent** rather than logged.

Purpose: an install with no folder picker is not a completable flow. And checklist step 3's whole
point is that silent no-ops are the failure mode — a logged no-op is the difference between a
decision and a bug.
Output: one new `rustInvoke` channel end-to-end (TS constant → sidecar stub → Rust arm), the
`tauri-plugin-dialog` dependency + capability, and a one-line `else` branch in `notify()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-CONTEXT.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-RESEARCH.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-PATTERNS.md
@.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
@.planning/phases/28-tauri-keyring-real-safestorage-via-the-keyring-crate/28-PROOF.md

<interfaces>
<!-- Contracts the executor needs; do not go exploring for these. -->

From src/common/types/sidecarTransport.ts (~L133-155):
  export const RUST_KEYRING_GET = 'keyring_get' as const   // ... SET / DELETE / AVAILABLE
  export const RUST_INVOKE_CHANNELS = [RUST_KEYRING_GET, ...] as const
  // "Must be kept in sync with Rust's `dispatch_rust_channel` match arms" — the existing comment

From src/backend/sidecar/sidecarRpc.ts (~L258-289):
  export function requestRustInvoke(channel: RustInvokeChannel, args: unknown[]): Promise<unknown>
  // Rejects before emitting for any channel not in RUST_INVOKE_CHANNELS

From src/backend/sidecar/electronStub.ts (~L111-120) — current stub, shape MUST be preserved:
  export const dialog = { ..., showOpenDialog: async () => ({ canceled: true, filePaths: [] }), ... }
  // Electron's real OpenDialogReturnValue is { canceled: boolean, filePaths: string[] }
  // main.ts's openDialog handler destructures `const { filePaths, canceled } = await ...`

From src-tauri/src/main.rs (verified line numbers):
  L206  fn dispatch_rust_channel(channel: &str, args: &[Value]) -> Result<Value, String>
        — four keyring_* arms + a catch-all `Err(format!("rustInvoke:unknown-channel:{channel}"))`
  L440  let worker_state = state.clone();       // `app` is NOT cloned into this worker thread today
  L442  let result = dispatch_rust_channel(&channel, &args);
  L477  fn main() {  L479  .plugin(tauri_plugin_opener::init())

From src-tauri/capabilities/default.json:
  "permissions": ["core:default", "opener:default", "opener:allow-open-url"]
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the tauri-plugin-dialog crate before adding it</name>
  <action>Pause and ask the developer to verify the `tauri-plugin-dialog` crate on crates.io before any `cargo add` runs. Do not add the dependency, do not edit `Cargo.toml`, and do not proceed to Task 2 until an explicit approval with a version string is received. This gate is not auto-approvable — `workflow.auto_advance` does not apply to package-legitimacy checkpoints.</action>
  <what-built>Nothing yet — this gate runs BEFORE any dependency is added.</what-built>
  <how-to-verify>
`tauri-plugin-dialog` is marked **[ASSUMED]** in 30-RESEARCH.md's Package Legitimacy Audit:
`cargo search` confirmed the name and version `2.7.2` exist on crates.io, but the name was
recalled from training knowledge rather than sourced from official Tauri documentation, and
slopcheck was not run (Rust-ecosystem coverage unconfirmed).

Please confirm, in a browser:
1. Visit https://crates.io/crates/tauri-plugin-dialog
2. Confirm the repository field points at `github.com/tauri-apps/plugins-workspace` (the same
   org that publishes the already-adopted `tauri-plugin-opener`)
3. Confirm the latest 2.x version and note it — the plan will pin whatever you confirm
4. Confirm it is the official Tauri v2 dialog plugin, not a lookalike

Expected outcome: an official tauri-apps crate with a current 2.x release.
  </how-to-verify>
  <resume-signal>Type "approved: &lt;version&gt;" to proceed, or name a different crate/approach.</resume-signal>
  <acceptance_criteria>
    - Human has confirmed the crate's publisher org and the exact version to pin
    - The confirmed version string is recorded in this plan's SUMMARY
    - No `cargo add` has run before this approval
  </acceptance_criteria>
  <done>The crate is human-verified as the official Tauri dialog plugin and a version is pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dialog_open channel end-to-end in Rust</name>
  <files>src-tauri/Cargo.toml, src-tauri/capabilities/default.json, src-tauri/src/main.rs</files>
  <read_first>
    - src-tauri/src/main.rs lines 190-290 (`dispatch_rust_channel`'s four `keyring_*` arms — the
      flat-`String` error convention and the trailing catch-all to preserve)
    - src-tauri/src/main.rs lines 400-470 (`start_reader`'s `frontendMessage` / `openExternal` /
      `rustInvoke` branches — note `worker_state` is cloned but `app` is not)
    - src-tauri/src/main.rs lines 470-500 (`fn main()`'s `tauri::Builder` chain and the existing
      `.plugin(tauri_plugin_opener::init())` line at L479 — the exact registration shape to mirror)
    - src-tauri/Cargo.toml (the full `[dependencies]` block — `tauri`, `tauri-plugin-opener`,
      `serde`, `serde_json`, `keyring`)
    - src-tauri/capabilities/default.json (the current three-entry `permissions` array)
    - .planning/phases/28-.../28-PROOF.md §5 (the `openExternal` missing-dispatch-arm precedent)
  </read_first>
  <action>
1. `Cargo.toml`: add `tauri-plugin-dialog` pinned to the version approved in Task 1, placed
   alongside `tauri-plugin-opener` in `[dependencies]`.
2. `capabilities/default.json`: append the dialog plugin's default permission to the existing
   `permissions` array (`"dialog:default"`, or the narrower open-scoped permission if the plugin
   exposes one). Without this the call is permission-denied even though the plugin is registered.
3. `main.rs` `fn main()`: register the plugin with `.plugin(tauri_plugin_dialog::init())`
   immediately after the existing `.plugin(tauri_plugin_opener::init())` at L479.
4. Widen `dispatch_rust_channel`'s signature to take the app handle:
   `fn dispatch_rust_channel(channel: &str, args: &[Value], app: &AppHandle) -> Result<Value, String>`.
   The four existing `keyring_*` arms stay byte-identical (they simply ignore `app`); the
   catch-all `Err(format!("rustInvoke:unknown-channel:{channel}"))` stays last.
5. Add one new arm `"dialog_open"` that calls the plugin's blocking folder picker
   (`app.dialog().file().blocking_pick_folder()`) and maps the result to
   `Value::String(path)` on Some and `Value::Null` on None. Follow the existing arms' error
   convention: log to stderr with the `[shell]` prefix and return a flat `Err(String)` on failure.
   Because it blocks, it must stay on the spawned worker thread — do NOT move dispatch onto the
   reader thread.
6. In `start_reader`'s `rustInvoke` branch (~L440), add `let worker_app = app.clone();` beside the
   existing `let worker_state = state.clone();` and pass `&worker_app` into the widened
   `dispatch_rust_channel(&channel, &args, &worker_app)` call. `AppHandle` is already `Clone`
   (used by this file's `.emit()` calls).

Do NOT add a new frame-kind branch — `dialog_open` rides the EXISTING `rustInvoke` request/response
branch; only the match arm and the `app` threading are new. Do not touch the `frontend_message`
relay (D-06 needs zero Rust changes there).
  </action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd src-tauri && cargo check` exits 0
    - `grep -c "tauri-plugin-dialog" src-tauri/Cargo.toml` == 1, pinned to the Task-1-approved version
    - `grep -c "dialog" src-tauri/capabilities/default.json` ≥ 1
    - `grep -c "tauri_plugin_dialog::init()" src-tauri/src/main.rs` == 1
    - `grep -c "blocking_pick_folder" src-tauri/src/main.rs` == 1
    - `grep -c "worker_app" src-tauri/src/main.rs` ≥ 2 (the clone and the call site)
    - `dispatch_rust_channel`'s catch-all `rustInvoke:unknown-channel` arm is still present and
      still last: `grep -c "rustInvoke:unknown-channel" src-tauri/src/main.rs` == 1
    - The four `keyring_*` arms are unchanged: `git diff src-tauri/src/main.rs` shows no edit to
      any line containing `keyring::Error` or `KEYRING_SERVICE`
    - The `frontend_message` relay is untouched: `git diff src-tauri/src/main.rs | grep -c "FRONTEND_MESSAGE_EVENT"` == 0
  </acceptance_criteria>
  <done>Rust exposes a `dialog_open` rustInvoke channel backed by the official Tauri dialog plugin, with the app handle correctly threaded into the worker thread.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the TS side — RUST_DIALOG_OPEN, real showOpenDialog, logged notify() no-op</name>
  <files>src/common/types/sidecarTransport.ts, src/backend/sidecar/electronStub.ts, src/backend/dialog/dialog.ts, src/backend/sidecar/__tests__/dialogStub.test.ts</files>
  <read_first>
    - src/common/types/sidecarTransport.ts lines 125-180 (the `RUST_KEYRING_*` constant block, the
      `RUST_INVOKE_CHANNELS` array with its keep-in-sync comment, and the
      `UNPORTED_CHANNEL_MARKER` doc comment)
    - src/backend/sidecar/electronStub.ts lines 100-170 (the current `dialog` stub object, the
      `Notification` stub whose `isSupported()` returns false, and `shell.openExternal`'s
      forward-to-transport pattern)
    - src/backend/sidecar/keyringTokenStore.ts (an existing `await requestRustInvoke(RUST_KEYRING_GET, [])`
      call site — the exact call shape to mirror, including its total/never-throw error handling)
    - src/backend/dialog/dialog.ts lines 55-80 (`notify()` — the `if (Notification.isSupported()
      && !isSteamDeckGameMode) {...}` block with NO else branch: the live REQ-30-07 gap)
    - src/backend/main.ts (the `openDialog` handler — it destructures `{ filePaths, canceled }`,
      which is why the return shape must be preserved exactly)
    - src/backend/sidecar/__tests__/skeletonFlows.test.ts (the three-way mock preamble)
  </read_first>
  <action>
1. `src/common/types/sidecarTransport.ts`: add
   `export const RUST_DIALOG_OPEN = 'dialog_open' as const` with a doc comment matching the
   `RUST_KEYRING_*` convention ("Rust-side channel name: open a native folder-picker dialog via
   the Tauri dialog plugin"), and append `RUST_DIALOG_OPEN` to `RUST_INVOKE_CHANNELS`. The string
   literal must be byte-identical to the Rust match arm added in Task 2 — the existing
   keep-in-sync comment on that array is the contract.
2. `src/backend/sidecar/electronStub.ts`: replace `dialog.showOpenDialog`'s always-canceled stub
   with a real body that `await requestRustInvoke(RUST_DIALOG_OPEN, [options])` and translates the
   result into Electron's exact `OpenDialogReturnValue` shape: `canceled` is true when the Rust
   result is `null`, `filePaths` is a one-element array of the picked path otherwise. Preserve the
   declared return type. On a rejection from `requestRustInvoke` (timeout, unknown channel,
   permission denial), log one warning and resolve `{ canceled: true, filePaths: [] }` — never
   throw to the caller, matching `keyringTokenStore.ts`'s total-method convention. The other five
   `dialog.*` members stay stubbed (D-09 scopes this to open-directory only; the rest is Phase 31).
3. `src/backend/dialog/dialog.ts`: add an `else` branch to `notify()` that logs (e.g.
   `logInfo`, `LogPrefix.Backend`) that the notification was skipped, naming the title and the
   reason (Notification unsupported or Steam Deck game mode). This fixes both builds and is the
   concrete REQ-30-07 `notify()` clause — a logged no-op, not a silent one (D-09). Do not add real
   Notification behavior; `tauri-plugin-notification` stays deferred to Phase 33.
4. Create `src/backend/sidecar/__tests__/dialogStub.test.ts` with the three-way mock preamble
   (`os` + `electron` + `electron-store`, `homedir()` → a per-pid tmp dir). Cover:
   - `RUST_DIALOG_OPEN` is a member of `RUST_INVOKE_CHANNELS` (so `requestRustInvoke` will emit
     rather than pre-reject);
   - `dialog.showOpenDialog` with a mocked `requestRustInvoke` resolving a path returns
     `{ canceled: false, filePaths: [thatPath] }`;
   - resolving `null` returns `{ canceled: true, filePaths: [] }`;
   - a REJECTING `requestRustInvoke` still resolves `{ canceled: true, filePaths: [] }` and does
     not throw;
   - `notify()` logs on the skipped path — assert via the logger spy, or (following
     `electronUntouched.test.ts`'s by-construction gate idiom) assert the `else` branch exists in
     stripped source.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/dialogStub.test.ts && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/backend/sidecar/__tests__/dialogStub.test.ts` exits 0 with ≥5 passing tests
    - `grep -c "RUST_DIALOG_OPEN" src/common/types/sidecarTransport.ts` ≥ 2 (declaration + array)
    - The TS constant and the Rust arm use the identical string:
      `grep -c "'dialog_open'" src/common/types/sidecarTransport.ts` == 1 and
      `grep -c '"dialog_open"' src-tauri/src/main.rs` == 1
    - `grep -c "canceled: true, filePaths: \[\]" src/backend/sidecar/electronStub.ts` ≥ 1 (the
      cancel/failure path) AND the file now contains `requestRustInvoke`
    - `grep -c "from 'electron'" src/backend/sidecar/electronStub.ts` == 0
    - `notify()` has an else branch that logs:
      `grep -v '^ *[*/]' src/backend/dialog/dialog.ts | grep -A3 "isSteamDeckGameMode" | grep -c "logInfo"` ≥ 1
    - The other five `dialog.*` stub members are unchanged in `electronStub.ts`
    - `npx tsc --noEmit` reports no new errors; `npx jest src/backend/sidecar src/preload/__tests__` green
  </acceptance_criteria>
  <done>The sidecar can ask Rust for a real folder pick and receives Electron's exact return shape; notify() logs instead of vanishing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar → Rust `rustInvoke` | A new channel string crosses the sidecar→shell boundary |
| Rust → OS file dialog | The user selects a filesystem path; the path returns into the sidecar |
| supply chain | One new Rust crate enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | Tampering | supply chain — `tauri-plugin-dialog` | mitigate | `[ASSUMED]` per the Package Legitimacy Audit; Task 1 is a blocking human checkpoint verifying publisher org and version on crates.io BEFORE `cargo add`. Not auto-approvable. |
| T-30-11 | Elevation of Privilege | `dispatch_rust_channel`'s widened surface | mitigate | The allowlist stays the gate: `requestRustInvoke` refuses any channel not in `RUST_INVOKE_CHANNELS`, and Rust's catch-all still rejects unknown channels. Only `dialog_open` is added, and it takes no path argument from the renderer — the path comes FROM the OS picker, not INTO it. |
| T-30-12 | Elevation of Privilege | Tauri capability permissions | mitigate | Add the narrowest dialog permission the plugin exposes; do not grant a blanket capability. Save/message-box paths stay unwired (Phase 31). |
| T-30-13 | Denial of Service | blocking picker on the reader thread | mitigate | Dispatch stays on the spawned worker thread (the same reason Phase 28 moved keyring dispatch off the reader thread — a modal Keychain/dialog prompt must not head-of-line block pending invokes). |
| T-30-14 | Information Disclosure | `notify()`'s new log line | mitigate | Log the notification title only, never a token, key or path; mirrors T-28-04's never-log-the-secret rule. |
| T-30-SC | Tampering | cargo install | mitigate | Blocking human checkpoint (Task 1) before any dependency is added; `[ASSUMED]` packages are never auto-approvable. |
</threat_model>

<verification>
- `cd src-tauri && cargo check` exits 0
- `npx jest src/backend/sidecar src/preload/__tests__` green
- `npx tsc --noEmit` reports no new errors
- The channel string is identical on both sides of the seam (TS constant == Rust match arm)
- The Electron build's `notify()` still notifies when `Notification.isSupported()` is true —
  only the else branch is new
</verification>

<success_criteria>
`dialog`'s open-directory path has real behavior in the Tauri build, reached through the existing
generic `rustInvoke` mechanism with no bespoke correlated-request machinery invented, and
`notify()` is a **logged** no-op rather than a silent one. The one new Rust crate passed a
blocking human legitimacy check before installation.
</success_criteria>

<output>
Create `.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-03-SUMMARY.md` when done
</output>
