---
phase: 30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check
plan: 04
type: execute
wave: 3
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
  - .planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-PORTED-CHANNELS.md
  - .planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-HUMAN-UAT.md
  - .planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-VALIDATION.md
autonomous: false
requirements: [REQ-30-02, REQ-30-03, REQ-30-04, REQ-30-08, REQ-30-09]

must_haves:
  truths:
    - "A reader of SEAM.md can see exactly which channels are now ported and which remain deferred"
    - "The D-05a bypass and D-05b reuse decisions are recorded with their reasons, because Phase 32 inherits the boundary"
    - "The two-token divergence is an Accepted Constraint, not an undocumented surprise"
    - "The deferred UAT item names the live QR scan AND the install E2E it gates in ONE entry"
    - "Both npm start and npm run tauri:dev still work after the phase"
  artifacts:
    - path: ".planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-PORTED-CHANNELS.md"
      provides: "The enumerated declared ported-channel list Phase 31 starts from (REQ-30-08)"
      contains: "listSteamLibraryTargets"
    - path: ".planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-HUMAN-UAT.md"
      provides: "The single deferred UAT item covering the live QR scan and the install E2E it gates"
      contains: "G-23-01"
    - path: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      provides: "§1 entries for the newly ported channels, §3 deferred-table narrowing, D-03/D-05a/D-05b Accepted Constraints"
  key_links:
    - from: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      to: ".planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-PORTED-CHANNELS.md"
      via: "§1 cross-reference to the enumerated list"
      pattern: "30-PORTED-CHANNELS"
---

<objective>
Close the phase's documentation and claim-honesty obligations: update SEAM.md (checklist step 5),
ship the **enumerated** declared ported-channel list, record the D-03/D-05a/D-05b reasons Phase 32
inherits, write the single deferred UAT item, and prove the additive/reversible invariant by
starting both builds.

Purpose: SEAM.md is the governing document for every later Tauri phase. A slice that is wired but
undeclared is a slice Phase 31 will re-discover by accident — and a phase whose claim level is not
pinned down will be misread as hardware-proven when it is not.
Output: SEAM.md updates, `30-PORTED-CHANNELS.md`, `30-HUMAN-UAT.md`, a finalized
`30-VALIDATION.md`, and a human-verified both-builds smoke pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-CONTEXT.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-RESEARCH.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-VALIDATION.md
@.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-01-SUMMARY.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-02-SUMMARY.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the enumerated ported-channel list and update SEAM.md</name>
  <files>.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-PORTED-CHANNELS.md, .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</files>
  <read_first>
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md — FULL FILE. Specifically §1
      "The four wired channels" (the list format to extend), §1's "`safeStorage` (real, Phase 28)"
      and "The store layer (real, Phase 29)" subsections (the graduated-from-deferred format to
      copy), §3's ranked deferred table (row 2 `dialog`, row 5 Login channel — the two rows this
      phase changes) and its "~217 of 220 endpoints" paragraph, and "Accepted Constraints" (entry
      format: bold decision ID, the decision, rejected alternatives, where it is mirrored in code)
    - .planning/phases/30-.../30-01-SUMMARY.md, 30-02-SUMMARY.md, 30-03-SUMMARY.md — what actually
      shipped; the list must describe reality, not this plan's intent
    - .planning/phases/30-.../30-RESEARCH.md Q3 and Q6 (the two enumerated channel sets, verbatim)
  </read_first>
  <action>
Create `30-PORTED-CHANNELS.md` — an **enumerated list, never prose** (D-11: the read subset is the
part most easily lost in prose). One table with a row per channel this phase ported, columns:
Channel · Kind (invoke/push) · Registration module · Real backend code reached · Requirement.
Rows — exactly these nine, confirmed against the shipped summaries: `checkSteamInstalled`,
`steamStartQR`, `steamPollQR` (steamAuthFlowRegistration.ts); `install`, `uninstall`,
`updateGame`, `checkGameUpdates`, `listSteamLibraryTargets` (installFlowRegistration.ts);
`gameStatusUpdate` (push, over the existing generic `frontendMessage` → `frontend_message` relay,
zero Rust changes — the third rider after `pushGameToLibrary` and `storeChanged`).

Add a second, separately-headed enumerated list of what this phase DELIBERATELY did NOT port and
which therefore keeps rejecting non-fatally per Invariant B, each with its one-line reason:
`steamStartCredentials`, `steamSubmitGuard`, `steamPollCredential`, `getSteamUserInfo`,
`logoutSteam` (D-02, QR only); the six `DownloadDialog` channels — `requestAppSettings`,
`requestGameSettings`, `checkDiskSpace`, `getGameOverride`, `getGameSdl`,
`getPrivateBranchPassword` — with the reason that `DownloadDialog` never mounts for
`runner === 'steam'` (`InstallGameModal.ts:66-74` is a commented chokepoint);
`getAlternativeWine` (cosmetic — `SteamGame` ignores `wineVersion`); the queue channels
`getDMQueueInformation`/`removeFromDMQueue`/`pauseCurrentDownload`/`resumeCurrentDownload`/
`cancelDownload` (Phase 32); byte-level `progressUpdate` throughput (Phase 32); the CrossOver
bottle and macOS bridge install branches (D-07). Add a one-line note that
`dialog.showOpenDialog` is ported while the other five `dialog.*` members are not (Phase 31).

Then update SEAM.md:
1. §1 — add a new subsection in the same graduated format as "`safeStorage` (real, Phase 28)":
   name the channels, name the two registration modules, state that `gameStatusUpdate` needed zero
   Rust changes, and cross-reference `30-PORTED-CHANNELS.md` by filename for the enumerated set.
2. §3 — row 5 ("Login channel") becomes CLOSED for the QR branch with the credential/guard/logout
   branches explicitly still deferred; row 2 (`dialog`) narrows to "open-directory ported via
   `dialog_open`; remaining files deferred to Phase 31". Update the "~217 of 220 endpoints
   unported" sentence to reflect the newly wired count.
3. "Accepted Constraints" — add three entries in the existing format:
   - **D-03 (Phase 30) — two-token divergence (ACCEPTED, document-only).** Signing in under Tauri
     does not sign you in under Electron: the sidecar stores a keyring-native Keychain entry,
     Electron stores Chromium OSCrypt ciphertext in `configStore`. Correct consequence of Phase 28
     D-01, not a bug. Convergence rejected (would require hand-rolling OSCrypt in the sidecar).
     Mirrored in `src/backend/sidecar/keyringTokenStore.ts`'s docstring. No new proof artifact.
   - **D-05a (Phase 30) — install is a direct `SteamGame` bypass, NOT a `downloadqueue.ts` port.**
     Reason from 30-02: the queue's only Steam-relevant behavior is a status push plus install
     sizing (two direct calls); everything else is runner-irrelevant or Phase 32's cluster. State
     explicitly that **Phase 32 inherits this boundary** and should build its own curated queue
     port when it needs pause/resume/cancel.
   - **D-05b/D-12 (Phase 30) — `uninstall`/`checkGameUpdates` reuse the runner-generic handlers
     unchanged, all runners.** Reason: `libraryManagerMap`'s import cost is already sunk via
     `steamFlowRegistration.ts`'s load-bearing first import (verified), so a Steam-only reshape
     buys zero import savings and only forks Tauri's behavior from Electron's.
Do NOT edit SEAM.md's "Load-Bearing Invariants" section — A and B are unchanged and still binding.
  </action>
  <verify>
    <automated>test -f .planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-PORTED-CHANNELS.md && grep -c "30-PORTED-CHANNELS" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</automated>
  </verify>
  <acceptance_criteria>
    - `30-PORTED-CHANNELS.md` exists; each of `checkSteamInstalled`, `steamStartQR`, `steamPollQR`,
      `install`, `uninstall`, `updateGame`, `checkGameUpdates`, `listSteamLibraryTargets`,
      `gameStatusUpdate` appears at least once (grep -c per name, all ≥ 1)
    - The not-ported section names all six `DownloadDialog` channels plus `logoutSteam`
      (grep -c per name, all ≥ 1)
    - SEAM.md Accepted Constraints contains the literal strings `D-03`, `D-05a`, `D-05b` and
      `Phase 32 inherits`
    - SEAM.md references the enumerated list by filename:
      `grep -c "30-PORTED-CHANNELS" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` ≥ 1
    - SEAM.md's Load-Bearing Invariants section is unchanged:
      `git diff .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md | grep -c "Load-Bearing Invariants"` == 0
    - SEAM.md §3's login row is no longer described as wholly deferred (it names the QR branch as
      ported and the credential/guard/logout branches as still deferred)
  </acceptance_criteria>
  <done>SEAM.md reflects the new seam state and the enumerated channel list Phase 31 starts from exists.</done>
</task>

<task type="auto">
  <name>Task 2: Write the single deferred UAT item and finalize 30-VALIDATION.md</name>
  <files>.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-HUMAN-UAT.md, .planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-VALIDATION.md</files>
  <read_first>
    - .planning/phases/30-.../30-VALIDATION.md (the claim-discipline section, the Pre-Existing
      Conditions table, the Per-Task Verification Map the planner already populated, and the
      Validation Sign-Off checklist)
    - .planning/phases/30-.../30-CONTEXT.md — D-04 and its ⚠ Known tension note; the wording
      obligation for the deferred item comes from there verbatim
    - .planning/phases/30-.../30-RESEARCH.md Open Question 2 (the `enableSteamNativeInstall`
      prerequisite) and its "Pre-existing OPEN gaps" subsection
    - An existing `*-HUMAN-UAT.md` elsewhere in `.planning/phases/` (the Phase 21/26 deferred-UAT
      artifact format to mirror)
    - .planning/phases/30-.../30-01-SUMMARY.md, 30-02-SUMMARY.md, 30-03-SUMMARY.md
  </read_first>
  <action>
Create `30-HUMAN-UAT.md` containing exactly **ONE** deferred item, not two. Its title and body must
name BOTH the live human QR scan AND the install E2E that scan gates, so a later reader cannot
conclude the install slice was independently hardware-proven. Include:
- Why it is deferred (D-04: sign-off is automated tests now; the scan needs a phone with the Steam
  mobile app and a real Steam account).
- The exact tester steps: `npm run tauri:dev`, sign in via QR, confirm the library populates,
  click Install on a Steam title, confirm the native folder picker opens (the new `dialog_open`
  path), confirm the depot download starts and the library button state transitions
  (`gameStatusUpdate`), then Uninstall.
- A **prerequisite** step: confirm `enableSteamNativeInstall` is `true` in the shared config —
  otherwise `SteamGame.install()` silently takes the legacy `steam://install` branch and the depot
  path this phase exists to prove is never exercised (30-RESEARCH Open Question 2).
- A **pre-existing conditions** block naming **G-23-01** (a `Blocked` depot key aborts the whole
  install) and **G-23-02** (native install applies no execute bits — a launch may need a manual
  `chmod +x`) as OPEN Phase 23 gaps sitting under any real depot run. State plainly that hitting
  either is NOT a Phase 30 regression.
- An explicit claim-level statement: this phase's honest claim is **"wired and unit-proven"**,
  never "hardware-proven".

Then finalize `30-VALIDATION.md`: set `status: complete` in the frontmatter, set
`wave_0_complete: true`, fill each Per-Task Verification Map row's Status column from the actual
results of plans 30-01..30-03, tick only the Validation Sign-Off boxes genuinely satisfied
(leave any unsatisfied box unticked with a one-line note), set `nyquist_compliant: true` only if
every task genuinely shipped with an automated verify, and change **Approval** from `pending` to
the real outcome. Leave the MANDATORY TEST-ISOLATION RULE, the WHAT JEST CANNOT PROVE, and the
Pre-Existing Conditions sections intact and unedited.
  </action>
  <verify>
    <automated>grep -c "G-23-01" .planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-HUMAN-UAT.md && grep -c "pending" .planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `30-HUMAN-UAT.md` exists and contains exactly one deferred item heading
      (`grep -c "^## " ` on the item section == 1 for the deferred-items block)
    - That single item's text contains BOTH `QR` and `install` — the scan and the E2E it gates
      are one entry, not two
    - It contains the literal strings `G-23-01`, `G-23-02`, `enableSteamNativeInstall`, and
      `wired and unit-proven`
    - It does NOT contain the string `hardware-proven` used as a claim about this phase (only, if
      at all, in a sentence explicitly denying it)
    - `30-VALIDATION.md` frontmatter has `status: complete` and no Per-Task Verification Map row
      still reads `⬜ pending`
    - `30-VALIDATION.md` still contains the headings `MANDATORY TEST-ISOLATION RULE`,
      `WHAT JEST CANNOT PROVE`, and `Pre-Existing Conditions` (grep -c each == 1)
  </acceptance_criteria>
  <done>The deferred UAT obligation is a single honest entry and the validation contract is closed out against real results.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Both-builds smoke pass (the additive/reversible invariant)</name>
  <action>Run `npm start`, then `npm run build && npm run tauri:dev`, and pause for the developer to confirm both builds launch clean. Capture the sidecar stderr and renderer console output verbatim into 30-04-SUMMARY.md — specifically whether any `is not a constructor` error appears, and which channels still emit `UNPORTED_CHANNEL_MARKER` warnings. Do not attempt the live QR scan or a real depot install here; that is the deferred UAT item from Task 2.</action>
  <what-built>
Plans 30-01..30-03 added two curated sidecar registration modules (five + three channels), one new
`rustInvoke` channel backed by the Tauri dialog plugin, a shared `checkGameUpdates` module, and a
logged `notify()` no-op. Every jest suite is green, but jest **cannot** prove sidecar module-init
order — the 27-05 `SteamLibraryManager is not a constructor` crash reproduced only in the esbuild
bundle. That is what this checkpoint exists to catch.
  </what-built>
  <how-to-verify>
1. `npm start` — the Electron build must launch as before, with no new errors and no behavior
   change. Sign-in state, library and install behavior must be exactly as they were.
2. `npm run build` then `npm run tauri:dev` — the Tauri window must open with NO
   `SteamLibraryManager is not a constructor` (or any other constructor/init) error in the
   sidecar's stderr, and the app must reach a painted window rather than a blank one.
3. In the Tauri console, confirm the previously-noisy unported-channel warnings for
   `checkSteamInstalled` / `steamStartQR` / `listSteamLibraryTargets` are GONE, while warnings for
   channels this phase deliberately did not port (e.g. `getDMQueueInformation`) are still
   warnings, not crashes.
4. Confirm the Steam login screen renders its QR tab (the `checkSteamInstalled` gate resolving).

Expected outcome: both builds start; no constructor error; unported channels still warn
non-fatally. Do NOT attempt the live QR scan or a real install here — that is the deferred UAT
item written in Task 2.
  </how-to-verify>
  <resume-signal>Type "approved" if both builds start clean, or paste the error output.</resume-signal>
  <acceptance_criteria>
    - `npm start` launches the Electron build with no new console errors
    - `npm run tauri:dev` opens a painted (non-blank) window
    - Sidecar stderr contains no `is not a constructor` error
    - The three newly-ported read channels no longer emit `UNPORTED_CHANNEL_MARKER` warnings
    - At least one deliberately-unported channel still emits a warning (Invariant B intact,
      not turned into a crash)
    - The observed outcome is recorded verbatim in 30-04-SUMMARY.md
  </acceptance_criteria>
  <done>Both builds work after the phase; module-init order is proven against the real bundle, which no jest run can do.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future phases | SEAM.md and the declared list are what Phase 31/32 plan against; an inaccurate entry propagates as a wrong assumption |
| claim record → verifier | An over-strong claim in VERIFICATION/UAT reads as proof that was never obtained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-15 | Repudiation | phase claim level | mitigate | REQ-30-03 claim discipline is enforced structurally: ONE deferred UAT item naming both the live QR scan and the install E2E it gates, plus an explicit "wired and unit-proven, never hardware-proven" statement. Acceptance criteria grep for the exact strings. |
| T-30-16 | Spoofing | attribution of pre-existing failures | mitigate | G-23-01 / G-23-02 are named as OPEN Phase 23 gaps in the UAT artifact so a depot failure is not misattributed as a Phase 30 regression. |
| T-30-17 | Tampering | SEAM.md's binding invariants | mitigate | Task 1 forbids edits to the Load-Bearing Invariants section; the acceptance criterion greps the diff to prove it. |
| T-30-18 | Information Disclosure | UAT instructions | accept | The tester steps reference a real Steam account but record no credentials, tokens or paths in the artifact. |
| T-30-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages. |
</threat_model>

<verification>
- `30-PORTED-CHANNELS.md` and `30-HUMAN-UAT.md` exist and pass their grep gates
- `30-VALIDATION.md` has no `⬜ pending` rows left and `status: complete`
- SEAM.md diff touches only §1, §3 and Accepted Constraints
- Human checkpoint recorded: both builds start, no constructor error
- Full suite green: `npx jest src/backend/sidecar src/preload/__tests__`
</verification>

<success_criteria>
The seam is declared, not merely wired: SEAM.md's §1/§3 reflect reality, the enumerated ported-
channel list Phase 31 starts from exists, the D-03/D-05a/D-05b reasons Phase 32 inherits are
recorded in-repo, the deferred proof is one honest entry naming both the QR scan and the install
E2E, and both `npm start` and `npm run tauri:dev` still work (REQ-30-09).
</success_criteria>

<output>
Create `.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-04-SUMMARY.md` when done
</output>
