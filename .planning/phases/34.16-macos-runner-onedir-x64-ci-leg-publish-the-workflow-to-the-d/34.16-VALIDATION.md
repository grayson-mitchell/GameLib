---
phase: 34.16
slug: macos-runner-onedir-x64-ci-leg
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-24
---

# Phase 34.16 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `34.16-RESEARCH.md` §Validation Architecture and CONTEXT.md D-07 / D-12.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29 (ts-jest), multi-project via root `jest.config.js` |
| **Config file** | `meta/jest.config.js` — `displayName: 'Meta'` |
| **Quick run command** | `pnpm jest --selectProjects Meta meta/__tests__/verifyRunnerBundle.test.ts meta/__tests__/downloadHelperBinaries.test.ts` |
| **Full suite command** | `pnpm jest --selectProjects Meta` |
| **Estimated runtime** | ~24 seconds full Meta project (measured 2026-08-24: `23.267s`, 22 suites / 522 tests) |

**`--selectProjects` is case-sensitive and fails OPEN.** The displayName is `Meta`, capital M.
`--selectProjects meta` matches nothing and **exits 0** — a green-looking vacuous run. Never write
a lowercase project name into a plan, a task, or a gate command.

---

## ⚠ Pre-existing RED baseline — read before setting any "suite green" bar

`pnpm jest --selectProjects Meta` is **RED at HEAD** (`a1cd145ae`), independent of this phase:

```
Test Suites: 1 failed, 21 passed, 22 total
Tests:       1 failed, 1 skipped, 520 passed, 522 total
  meta/__tests__/genI18nGateScope.test.ts:415 — fork-touched snapshot ≠ fresh snapshot
```

**Diagnosis (verified 2026-08-24):** the fork-touched file snapshot is stale against six committed
files, five of them added by quick task `260824-u8b` (commit `68bada5bf`) which did not regenerate
the snapshot. Confirmed **not** caused by this session's working tree — `git status --porcelain`
over `InstallModal/` and `LibraryHeader/` is empty.

**Consequences that bind this phase's plans:**

1. **No plan may state "full Meta suite green" as an acceptance criterion or a phase gate.** That
   bar is unreachable today for reasons this phase does not own, and asserting it would make every
   task using it fail for an unrelated cause.
2. The correct per-task bar is **the specific test files this phase touches**, named explicitly —
   `verifyRunnerBundle.test.ts`, `downloadHelperBinaries.test.ts`, `buildRunnersOnedir.test.ts`,
   and the new `pinRunnerDigests.test.ts`.
3. The correct **phase** bar is: those four files green **and** the Meta failure count unchanged at
   exactly 1 (`genI18nGateScope.test.ts`) — i.e. this phase introduces no new red. A plan asserting
   "1 failed, 21 passed" as the expected baseline is checkable; "all green" is not.
4. **Do not fix `genI18nGateScope.test.ts` inside this phase.** It is unrelated i18n-snapshot debt
   and regenerating that artifact is known on this repo to break the pins guarding it. Route it out.

---

## Sampling Rate

- **After every task commit:** `pnpm jest --selectProjects Meta <the touched test file>` (~3–6s)
- **After every plan wave:** `pnpm jest --selectProjects Meta` — assert **1 failed / 21 passed**,
  failure confined to `genI18nGateScope.test.ts`
- **Before `/gsd:verify-work`:** the four owned test files green; Meta failure count still exactly 1
- **Max feedback latency:** 24 seconds
- **Live-gate items are NOT covered by any of the above** — see Manual-Only Verifications. Per D-04,
  everything requiring a real `workflow_dispatch` is isolated into a LIVE-GATE document and does not
  gate the code-side waves.

---

## Per-Task Verification Map

Task IDs are assigned at plan time; rows below are the requirement-level contract each plan's tasks
must inherit. The planner MUST expand this into per-task rows and the executor MUST fill Status.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | REQ-34.9-02 (no-checkout commit lands on `gamelib/main`) | — | Commit adds exactly one file; push targets `gamelib`, never `origin` (Heroic upstream) | manual-only | Q1 verification commands in RESEARCH.md §Q1 | N/A — live git against a remote | ⬜ pending |
| TBD | TBD | TBD | REQ-34.9-03 (downloader sources darwin archives from the published run) | — | Fetch failure writes nothing under `public/bin` | integration (LIVE GATE, D-12a) | `pnpm download-helper-binaries` on a clean tree, transcript captured | ✅ script exists; no automated assertion beyond exit code | ⬜ pending |
| TBD | TBD | TBD | REQ-34.9-04 (sha256 digest verification) | — | Digest mismatch throws and refuses all writes under `public/bin` | unit + deliberate-corruption proof (D-12b) | `pnpm jest --selectProjects Meta meta/__tests__/downloadHelperBinaries.test.ts` | ✅ existing digest/sentinel gate tests | ⬜ pending |
| TBD | TBD | TBD | **REQ-34.16-01** (mint: guard covers x64 as well as arm64 — item 12) | — | Guard fails closed per-arch; each failure names its own arch | unit (script wiring) + manual RED proof (D-07) | `pnpm jest --selectProjects Meta meta/__tests__/verifyRunnerBundle.test.ts` | ✅ file exists; ❌ no real-x64-tree fixture (Wave 0) | ⬜ pending |
| TBD | TBD | TBD | **REQ-34.16-02** (mint: guard actually executes in CI — item 13) | — | `install-deps` stops throwing once sentinels are real, so the build step is reached | manual-only (LIVE GATE) | Observe a real CI dispatch transcript | N/A — a CI-execution fact | ⬜ pending |
| TBD | TBD | TBD | **REQ-34.16-03** (mint: `release:mac` x64 exposure — item 18 / C2-05) | C2-05 | An unverified x64 build cannot reach the `electron-updater` feed | unit (`indexOf`-only, see D-09 caveat below) | `pnpm jest --selectProjects Meta meta/__tests__/verifyRunnerBundle.test.ts` | ✅ `meta/__tests__/verifyRunnerBundle.test.ts:605-622` | ⬜ pending |
| TBD | TBD | TBD | **REQ-34.16-04** (mint: `pin:runner-digests` — supports D-10/D-11) | — | Partial/failed fetch leaves `runnersOnedirDigests.json` unmodified | unit | `pnpm jest --selectProjects Meta meta/__tests__/pinRunnerDigests.test.ts` | ❌ **Wave 0 — file does not exist** | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**D-09 caveat, carried forward so no one mistakes this row for protection.** The REQ-34.16-03 test
asserts only `indexOf('pnpm verify:runner-bundle') < indexOf('electron-builder')`. It stays **green
if a future edit deletes the `--arch=x64` invocation entirely.** Item 18's closure is correct at the
moment it lands and is **unprotected against regression**. No plan, summary, or verification document
may describe this closure as "pinned".

---

## Wave 0 Requirements

- [ ] `meta/__tests__/pinRunnerDigests.test.ts` — **new file, no coverage exists.** Must cover: fetch
      success → correct rewrite; fetch failure → no partial write; malformed `SHA256SUMS` line →
      clear error; run-id field addition does not perturb `layout` / `digests`.
- [ ] `meta/__tests__/downloadHelperBinaries.test.ts` — add a case proving a **sibling top-level JSON
      key** on `runnersOnedirDigests.json` (e.g. a future `runId`) has zero effect on
      `darwinLayoutMarker()`. Belongs in the existing
      `describe('computeLayoutMarker (darwinLayoutMarker sensitivity, mutation-proof)')` block.
      RESEARCH.md resolves the *code* answer (the marker is structurally blind to sibling fields);
      this test is what stops that answer from silently decaying.
- [ ] `meta/__tests__/buildRunnersOnedir.test.ts` — `writeSha256Sums` / `writeBuildManifest` are
      neither exported nor tested. If `pin:runner-digests` parses their output, either export and
      unit-test the writers, or add a format-contract fixture the parser is written against.
      Otherwise the parser is written against an unpinned format.
- [ ] **No fixture simulating a real x64 onedir tree exists**, and per constraint 3 one must NOT be
      invented — its shape is unobserved. Deliberate Wave 0 *non*-requirement. Once CI produces a
      real x64 tree, capture a fixture from it; today the guard's x64 path has zero regression
      coverage and the plan must say so rather than imply otherwise.

---

## Manual-Only Verifications

**D-04 splits these from the code-side waves.** Items marked LIVE GATE cannot run until the
workflow exists on `gamelib/main` and a real dispatch has published artifacts; they belong in a
`34.16-LIVE-GATE.md` following the `34.9-LIVE-GATE-RERUN.md` precedent, not in a task's
`<acceptance_criteria>`.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Workflow-only commit reaches `gamelib/main` without any checkout in this worktree | REQ-34.9-02 | `.husky/post-checkout` runs `pnpm i && pnpm download-helper-binaries`, which **throws** on the six sentinels. Also a push to a remote — outside any plan's authorization (D-03). | RESEARCH.md §Q1 Option A (`git commit-tree`, recommended — fully local). Verify single-file diff **before** pushing; verify byte-identity of the two workflow copies after. **HUMAN GATE.** |
| Guard goes RED on a real x64 tree, naming `arch=x64` | REQ-34.16-01 | Requires a real downloaded x64 onedir tree — cannot exist before the dispatch | D-07: inject a broken symlink into a real downloaded x64 tree; assert the guard fails **and** its message names `arch=x64`. Green-only evidence does not satisfy D-07. |
| Guard executes at all in CI | REQ-34.16-02 | A CI-execution fact, not a code property | **LIVE GATE.** Observe the macOS leg reaching the build step after `install-deps` stops throwing. |
| Clean-tree fetch of all six archives | REQ-34.9-03 | Needs the real published release | **LIVE GATE, D-12a.** `pnpm download-helper-binaries` on a clean tree; capture transcript. |
| Digest corruption throws and writes nothing under `public/bin` | REQ-34.9-04 | Deliberate failure injection against a live fetch path | **D-12b.** One-character digest corruption; assert throw **and** assert `public/bin` untouched. |
| x64 runners actually execute under Rosetta | REQ-34.9-03 | Proves the archives work, not merely that they hash correctly | **D-12c.** Run the extracted x64 `legendary` / `gogdl` / `nile`; an invocation that proves execution, not presence. |
| Whether a real x64 onedir tree passes the guard's structural checks | REQ-34.16-01 | **Genuinely unobserved** (constraint 3). Neither direction may be assumed. | Follow RESEARCH.md §Q3's guard-bug-vs-build-bug decision procedure. The plan must **branch** here, not predict. |

---

## Validation Sign-Off

- [ ] All tasks have an `<automated>` verify command **or** a declared Wave 0 dependency **or** an
      explicit LIVE-GATE routing
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify
- [ ] Wave 0 covers all ❌ MISSING references above
- [ ] No watch-mode flags
- [ ] No task or gate uses `--selectProjects meta` (lowercase — matches nothing, exits 0)
- [ ] No task asserts "full Meta suite green"; the baseline is **1 failed / 21 passed**
- [ ] No document describes item 18's closure as "pinned" (D-09)
- [ ] Feedback latency < 24s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
