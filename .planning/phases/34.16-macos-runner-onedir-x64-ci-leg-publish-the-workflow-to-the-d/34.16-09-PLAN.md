---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 09
type: execute
wave: 5
depends_on:
  - 34.16-07
files_modified:
  - .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md
autonomous: true
gap_closure: true
closes_findings:
  - F-34.16-D
requirements:
  - REQ-34.9-02
decisions: []

must_haves:
  truths:
    - "F-34.16-D: the widened extractor is proven against the REAL pinned legendary repo, cloned fresh, not only against a captured fixture -- the fixture proves parsing, this proves the whole build path"
    - "The build path BEYOND extraction is observed for the currently pinned legendary, whose upstream dropped requirements.txt and setup.py in favour of pyproject.toml -- a second silent contract the same bump could have invalidated"
    - "The outcome is recorded as one of exactly three branch verdicts, and a failure is a recorded finding routed onward, never softened into a pass"
    - "Nothing outside the gitignored .build-tools/ tree is written: public/bin, build/bin, meta/ and the six PENDING-CI-PUBLISH sentinels are byte-unchanged before and after"
    - "The third-party Python packages this build installs into a throwaway venv are enumerated and registry-checked BEFORE the install runs, not assumed"
    - "This plan runs no gesture from any 34.16-LIVE-GATE.md scored item: no push, no dispatch, no pin:runner-digests, no download-helper-binaries, no dist:mac, no verify:runner-bundle"
  artifacts:
    - path: ".planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md"
      provides: "the branch verdict, the full transcript excerpts, the measured tree shape, and the untouched-tree proof"
      contains: "BRANCH VERDICT"
  key_links:
    - from: "meta/buildRunnersOnedir.ts:extractUpstreamPyinstallerCommand"
      to: "a real cloned legendary repo at the pinned tag"
      via: "pnpm build-runners-onedir --arch=arm64 --runner=legendary, run end to end on this machine"
      pattern: "build-runners-onedir"
---

<objective>
Prove plan `34.16-07`'s widening against the real thing: clone the pinned legendary repo and run the
onedir build end to end on this arm64 machine.

Purpose: `F-34.16-D` was discovered by burning a live-gate attempt. The fixture test in plan
`34.16-07` proves the extractor parses the right text; it does not prove that the derived command
actually builds, nor that the steps AFTER extraction still work for a legendary that no longer ships
`requirements.txt` or `setup.py`. `RUN ATTEMPT 3` costs a push, a dispatch and two macOS runners.
This plan costs a few minutes locally and can convert a second wasted attempt into a finding
discovered now.

**This is a BRANCH, not a prediction.** Do not read ahead expecting a pass. PyInstaller cannot
cross-compile (34.9-CONTEXT.md constraint 1), so this proves the arm64 leg only; the x64 leg remains
CI-only and unproven, exactly as `34.16-LIVE-GATE.md` §11 already records.

Output: `34.16-ONEDIR-BUILD-PROOF.md` carrying one of three branch verdicts and the evidence that
produced it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-CONTEXT.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-07-SUMMARY.md
@meta/buildRunnersOnedir.ts

<measured_facts>
<!-- Measured by the planner 2026-08-25 against live upstream and the local tree. VERIFY each,
     do not trust this block. It exists so the branch table below is concrete. -->

- `.build-tools/` is gitignored (`.gitignore:50` — `/.build-tools/`). `buildRunner` writes ONLY under
  `.build-tools/runners-onedir/{src,out}`; nothing it does touches `public/bin` or `build/bin`.
- `cloneRepo` does `rmSync(destDir, {recursive:true, force:true})` first, so the existing
  `.build-tools/runners-onedir/src/legendary` directory is destroyed and re-cloned. That directory is
  gitignored build-tooling state, not a source of truth.
- `main()` with `--runner=legendary` builds one runner and still writes `SHA256SUMS-arm64` and
  `BUILD-MANIFEST-arm64.json` into `.build-tools/runners-onedir/out/`, overwriting any previous
  local ones with a legendary-only set. Gitignored; CI regenerates them; harmless — but say so.
- At the currently pinned legendary tag the repo root holds `pyproject.toml` and `uv.lock` and does
  NOT hold `requirements.txt` or `setup.py`. The previous pin's root DID hold `requirements.txt`.
  Consequences to observe rather than assume:
    * `installDependencies` (L461) takes its `pip install -e .` branch, not its `-r requirements.txt`
      branch. That branch's comment says it exists for gogdl; legendary now reaches it too.
    * `resolvePyinstallerRequirement` (L500) finds no `requirements.txt` and falls back to
      `PYINSTALLER_FALLBACK_PIN`.
    * `pyproject.toml` declares `build-system.requires = ["uv_build>=...,<..."]`, so `pip install -e .`
      resolves a build backend from PyPI.
  None of this is a prediction of failure. It is the list of things to watch in the transcript.
- Rosetta/arch: this machine is arm64, and `buildRunner` refuses any arch that is not
  `process.arch`. Use `--arch=arm64`. An x64 local build is structurally impossible here.
</measured_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and registry-check the third-party Python packages this build will install, and snapshot every tree it must not touch</name>

  <read_first>
    - `meta/buildRunnersOnedir.ts` L428-L545 — `cloneRepo`, `createVenv`, `installDependencies`,
      `resolvePyinstallerRequirement`, `installPyinstaller`, `runOnedirBuild`. These are the steps
      whose inputs you are enumerating.
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-RESEARCH.md`
      `## Package Legitimacy Audit` — it records that the PHASE adds no packages. That determination
      is about this repo's manifests and does not cover the Python packages the build path installs
      into a throwaway venv, which is what this task covers.
    - `.gitignore` — confirm `/.build-tools/` is listed before relying on it
  </read_first>

  <files>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md</files>

  <action>
    Read the pinned legendary tag fresh from `meta/releaseTags.ts` and the repo slug fresh from
    `ONEDIR_RUNNERS`; do not transcribe either.

    Fetch that tag's `pyproject.toml` with
    `gh api "repos/<slug>/contents/pyproject.toml?ref=<tag>" --jq '.content' | base64 -d`, redirected
    to a file with its exit status captured by redirect (a pipe swallows failures and exits 0). From
    it, enumerate: every `project.dependencies` entry, the `pyinstaller_build` optional-dependency
    entries, and every `build-system.requires` entry. Add `PYINSTALLER_FALLBACK_PIN`'s package from
    `meta/buildRunnersOnedir.ts`.

    For each enumerated package name, confirm it exists on PyPI and is not a lookalike:
    `curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/<name>/json"` must print `200`,
    and record the project's own PyPI URL. Record the full table in the proof document. If ANY name
    does not resolve, or resolves to a project whose name differs from the declared one by
    homoglyph/typo, STOP: do not run Task 2, and record it as a finding instead.

    Then snapshot every tree the build must not touch, so Task 2's untouched claim is a measurement
    rather than an assertion:
      - `git status --porcelain > <snapshot>` (the whole tree; a concurrent session's unrelated
        dirtiness is expected and already recorded in the gate's Precondition 5 — record it, do not
        clean it)
      - `shasum -a 256 meta/runnersOnedirDigests.json`
      - `grep -c 'PENDING-CI-PUBLISH' meta/runnersOnedirDigests.json`
      - `shasum -a 256 meta/releaseTags.ts`
      - `find public/bin build/bin -maxdepth 3 2>/dev/null | sort | shasum -a 256`
      - `date` plus a touchfile under the scratchpad, so a `find -newer` sweep is available afterwards

    Create `34.16-ONEDIR-BUILD-PROOF.md` with frontmatter in the style of
    `34.9-GUARD-PROOF.md`/`34.9-WRAPPER-PROOF.md` (phase, plan, type: build-proof, created, verdict
    left `TBD` until Task 2 fills it) and write the package table plus the snapshots into it. Do not
    write a verdict in this task.
  </action>

  <verify>
    <automated>grep -c 'https://pypi.org/pypi/' .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md</automated>
  </verify>

  <acceptance_criteria>
    - The proof document contains a package table with one row per enumerated package, each carrying
      the declared name, where it was declared (`project.dependencies`,
      `optional-dependencies.pyinstaller_build`, `build-system.requires`, or
      `PYINSTALLER_FALLBACK_PIN`), and its PyPI HTTP status. Every status is `200`.
    - The table's package set is derived from the FETCHED `pyproject.toml` at the pinned tag, and the
      fetch command plus its `.exit` file are quoted. Not from memory and not from this plan's
      `<measured_facts>` block.
    - All six snapshot values are recorded verbatim in the proof document, including the
      `PENDING-CI-PUBLISH` count (which must be `6`) and the `meta/releaseTags.ts` sha256, which must
      equal `f6c4e5006f6a4128a1f4cf7644292ad5e361898fde513c0f39edb1b6e6ff8895` — the value
      `34.16-LIVE-GATE.md` Precondition 7 recorded.
    - `git diff --stat` shows this task changed exactly one file, the proof document itself.
    - No forbidden command was run: the task transcript contains no `git checkout`, `git stash`,
      `git reset`, `git push`, `gh workflow run`, `pnpm pin:runner-digests`,
      `pnpm download-helper-binaries`, `pnpm dist:mac` or `pnpm verify:runner-bundle`. Assert with a
      single `grep -cE` over the transcript printing `0`.
  </acceptance_criteria>

  <done>
    Every third-party Python package the build will install is enumerated from the pinned upstream's
    own `pyproject.toml` and confirmed to resolve on PyPI, and every tree the build must not touch is
    snapshotted with a measurable baseline.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the real onedir build for legendary at the pinned tag on arm64, record the branch verdict, and prove nothing outside .build-tools changed</name>

  <read_first>
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md`
      — Task 1's package table and snapshots
    - `meta/buildRunnersOnedir.ts` L639-L715 (`buildRunner`) and L814-L832 (`main`) — the exact step
      order, so a failure can be attributed to the right step
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md`
      §5 — the evidence-capture contract: redirect form, one distinct filename per block, `xxd` the
      exit file. Follow it here even though this is not a scored gate item.
  </read_first>

  <files>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md</files>

  <action>
    Run, with a generous timeout (this clones, creates a venv, pip-installs, and runs PyInstaller —
    allow the full 600s):

      `pnpm build-runners-onedir --arch=arm64 --runner=legendary > <dir>/onedir-legendary.log 2>&1; echo $? > <dir>/onedir-legendary.exit`

    Capture the exit status by redirect only. Read the `.exit` file back with `xxd` before scoring
    it: `300a` is `0\n`, `310a` is `1\n`; a 1-byte file is the known zsh capture-failure signature.
    Do not use `cat -A` — macOS BSD `cat` has no `-A`.

    Do NOT pass `--arch=x64`: `buildRunner` refuses an arch that is not `process.arch`, and
    PyInstaller cannot cross-compile. Do NOT run the other two runners; legendary is the one the
    widening changed and the one that broke CI.

    Then record the BRANCH VERDICT as exactly one of these three values, applying the decision
    procedure below to the transcript. Do not record "mostly worked" or "passed with warnings".

      - **CLEAN BUILD** — exit 0, an archive written under `.build-tools/runners-onedir/out/`, and the
        extracted tree present at `.build-tools/runners-onedir/src/legendary/legendary/dist/legendary`.
        `F-34.16-D`'s code half is proven end to end for arm64.
      - **EXTRACTION STILL BROKEN** — the failure comes from `extractUpstreamPyinstallerCommand` or
        the onedir derivation. Plan `34.16-07` is incomplete. Record the message verbatim; this
        re-opens `34.16-07`.
      - **POST-EXTRACTION FAILURE** — extraction succeeded (the transcript shows the derived command)
        and the failure is in a LATER step: clone, venv, `pip install -e .` (including its `uv_build`
        backend resolution), the PyInstaller install, or the PyInstaller run itself. This is a NEW
        finding, not a defect in plan `34.16-07`, and it is exactly the second silent contract
        `<measured_facts>` warned about. Record which step, the verbatim error, and stop — do not
        attempt a fix inside this plan.

    If the message alone is not conclusive, inspect the named path with `ls -la` and `file <path>`
    and record that output BEFORE classifying. Do not classify from a step name alone.

    On CLEAN BUILD, additionally measure and record, because these are the first arm64 measurements
    taken with the widened extractor and they are directly comparable to the shape
    `34.16-LIVE-GATE.md` §11 will ask for on x64:
      - the derived onedir command exactly as the transcript printed it, and confirmation that its
        first token is the venv pyinstaller path and that no `uv`/`run`/`--module`/`PyInstaller`
        token survives in it;
      - `find <distDir> | wc -l` and `find <distDir> -type l | wc -l`;
      - whether a `Python.framework` and an `_internal` directory exist under `<distDir>`;
      - `<distDir>/legendary --version` output and exit code — a correctly-archived broken binary
        would satisfy every other measurement;
      - `shasum -a 256` of the produced archive.

    On ANY verdict, prove the blast radius by re-measuring all six Task-1 snapshots and diffing them
    against the recorded values, plus a
    `find public/bin build/bin meta -newer <the Task-1 touchfile> -not -path '*/node_modules/*'`
    sweep that must list nothing. "The build only writes to .build-tools" is a claim about behaviour
    and needs its own measurement — that is the whole point of taking the snapshot first.

    Restore nothing by git. If anything must be restored, use `cp` from a pre-made backup plus a
    `shasum -a 256` equality check. `git checkout -- <path>` fires `.husky/post-checkout`, which runs
    `pnpm i && pnpm download-helper-binaries`, and the latter throws by design on the six sentinels.
    Never `git stash`, never `git reset` — they strand concurrent sessions on this repo.

    Write the verdict, the transcript excerpts, the measurements and the blast-radius proof into
    `34.16-ONEDIR-BUILD-PROOF.md` and set its frontmatter `verdict:` field.
  </action>

  <verify>
    <automated>grep -cE '^\*\*BRANCH VERDICT:\*\* (CLEAN BUILD|EXTRACTION STILL BROKEN|POST-EXTRACTION FAILURE)$' .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md</automated>
  </verify>

  <acceptance_criteria>
    - The proof document contains exactly one line matching
      `^\*\*BRANCH VERDICT:\*\* (CLEAN BUILD|EXTRACTION STILL BROKEN|POST-EXTRACTION FAILURE)$`, and
      the frontmatter `verdict:` field carries the same value. Any other wording fails this criterion;
      "mostly passed" is not one of the three.
    - The `.exit` file's `xxd` output is quoted in the document and agrees with the recorded verdict.
      A CLEAN BUILD verdict with an exit file that is not `300a` is a contradiction and fails.
    - The transcript excerpt shows the derived onedir command as the script printed it, and the
      document states explicitly whether any of `uv`, `run`, `--module`, `PyInstaller` appears in it.
      For a CLEAN BUILD none may appear.
    - Blast radius proved, not asserted: all six Task-1 snapshot values are re-measured and shown
      equal, `grep -c 'PENDING-CI-PUBLISH' meta/runnersOnedirDigests.json` still prints `6`,
      `shasum -a 256 meta/releaseTags.ts` still equals
      `f6c4e5006f6a4128a1f4cf7644292ad5e361898fde513c0f39edb1b6e6ff8895`, and the
      `find ... -newer <touchfile>` sweep over `public/bin`, `build/bin` and `meta` produces empty
      output, quoted.
    - `git status --porcelain` differs from the Task-1 snapshot ONLY by the proof document. Quote the
      diff of the two porcelain outputs.
    - On CLEAN BUILD: the file count, symlink count, `Python.framework`/`_internal` presence, the
      `--version` output with its exit code, and the archive sha256 are all recorded. A tree that
      exists is not the same claim as a binary that runs.
    - On EXTRACTION STILL BROKEN or POST-EXTRACTION FAILURE: the failing step is named, the error is
      quoted verbatim, and the document states which plan or phase the finding routes to. No fix is
      attempted inside this plan and none is present in `git diff`.
    - No forbidden command was run: a single `grep -cE 'git (checkout|stash|reset)|git push|gh workflow run|pnpm (pin:runner-digests|download-helper-binaries|dist:mac|verify:runner-bundle)'`
      over this task's transcript prints `0`.
    - `git diff --name-only` for this task lists exactly one path, the proof document. In particular
      `meta/buildRunnersOnedir.ts` is unchanged by this plan.
  </acceptance_criteria>

  <done>
    The real pinned legendary repo has been cloned and built end to end on this machine with the
    widened extractor, the outcome is recorded as one of three named branch verdicts with the
    evidence that produced it, and every tree outside gitignored `.build-tools/` is measured
    unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub repo -> local clone | A third-party repository is cloned at a pinned tag into `.build-tools/runners-onedir/src/legendary`. |
| upstream `pyproject.toml` -> PyPI installs into a throwaway venv | The clone's own declared dependencies, plus a PyPI-resolved build backend, are installed by `pip` on this developer machine. This is the largest real risk in the plan and is modelled, not waved past. |
| upstream workflow YAML -> a spawned process | Text from the clone becomes the argument list for a PyInstaller process that then executes the clone's own Python source. This is the boundary plan `34.16-07` widened. |
| build outputs -> the repo | Confined to gitignored `.build-tools/`. Measured, not assumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.16G-11 | Tampering | executed command derived from upstream YAML | mitigate | Inherited from plan `34.16-07`: `spawn` takes an argv array with no shell, and the executable is always the venv's own pyinstaller path, never a token from upstream. Task 2 measures this directly by asserting no launcher token survives in the printed derived command. |
| T-34.16G-12 | Elevation of Privilege | arbitrary code execution during `pip install -e .` and the PyInstaller analysis pass | accept, with a named pre-check | A source install runs upstream's build backend, and PyInstaller imports the target's modules. Both execute third-party code on this machine. Accepted because it is the pre-existing, unavoidable mechanism of the whole onedir design (34.9) and is what CI already does on every run; bounded by the tag pin, by the throwaway venv under gitignored `.build-tools/`, and by Task 1's requirement that every declared package resolve on PyPI before the install runs. |
| T-34.16G-13 | Tampering | the build writing outside `.build-tools/` | mitigate | Six snapshots taken BEFORE the run and re-measured after, plus a `find -newer` sweep over `public/bin`, `build/bin` and `meta`. A mutating command's own success report is not proof of its effect (F-34.9-02). |
| T-34.16G-14 | Tampering | the six `PENDING-CI-PUBLISH` sentinels or `meta/releaseTags.ts` drifting mid-plan | mitigate | Both asserted byte-unchanged against `34.16-LIVE-GATE.md` Precondition 7's recorded sha256 and a sentinel count of 6. A drift here would invalidate live-gate preconditions for RUN ATTEMPT 3. |
| T-34.16G-15 | Repudiation | a local pass being read as evidence for the x64 leg | mitigate | The document states in its own header that PyInstaller cannot cross-compile, that this proves arm64 only, and that `34.16-LIVE-GATE.md` §11's x64 branch is untouched by this result. |
| T-34.16G-SC | Tampering | package-manager installs | mitigate | Task 1 enumerates every third-party Python package from the pinned upstream's own `pyproject.toml` plus `PYINSTALLER_FALLBACK_PIN`, and requires each to resolve on `pypi.org` with a `200` before Task 2 runs. Any unresolvable or lookalike name STOPS the plan and becomes a finding. No npm, no cargo; this repo's dependency manifests are unchanged. |
</threat_model>

<verification>
- `pnpm build-runners-onedir --arch=arm64 --runner=legendary` executed once, exit status captured by
  redirect and read back with `xxd`.
- Exactly one BRANCH VERDICT line, matching one of the three permitted values, in both the body and
  the frontmatter.
- Six pre-run snapshots re-measured equal; `find -newer` sweep empty; sentinel count still 6;
  `meta/releaseTags.ts` sha256 unchanged.
- `git diff --name-only` lists only the proof document.
</verification>

<success_criteria>
The pinned legendary repo has been cloned and built with the widened extractor on real hardware, and
the outcome — whichever of the three it is — is recorded with enough evidence that `RUN ATTEMPT 3`
starts from a diagnosis rather than from a hope.
</success_criteria>

<output>
Create `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-09-SUMMARY.md` when done.

Record for plan `34.16-11`: the branch verdict, the one-line reason, and — on anything other than
CLEAN BUILD — the finding text to append to `34.16-LIVE-GATE.md`'s `F-34.16-D` section and the phase
or plan it routes to.
</output>
