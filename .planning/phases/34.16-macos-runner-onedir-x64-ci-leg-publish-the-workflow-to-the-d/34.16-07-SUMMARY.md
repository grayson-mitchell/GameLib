---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 07
subsystem: infra
tags: [pyinstaller, ci, macos-onedir, meta-build, gap-closure]

# Dependency graph
requires:
  - phase: 34.16-03
    provides: "BUILD-MANIFEST-{arch}.json format contract (upstreamLine, upstreamWorkingDirectory, onedirCommand, droppedExpressions field names) that this plan must not disturb"
provides:
  - "extractUpstreamPyinstallerCommand recognises 3 enumerated invocation forms (bare, uv-run-module, python-m) instead of only bare `pyinstaller`"
  - "deriveOnedirInvocation(): form-agnostic --onefile->--onedir swap + ${{ }}-expression drop, operating on pyinstallerArgs (never a launcher token)"
  - "runOnedirBuild spawns pyinstallerPath(repoDir) with a derived argv array directly -- no more argv[0] string-splitting substitution"
  - "4 byte-verified upstream legendary-0.21.0 workflow fixtures as a permanent regression specimen"
affects: [34.16-11]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Enumerated invocation-form matching (matchInvocationForm): fixed launcher-prefix token counts per form, zero tolerance for intervening tokens -- deliberately narrower than a loosened regex so a future upstream flag insertion trips the zero-match refusal instead of being silently absorbed"
    - "Mutation-proven refusal/derivation logic: temporarily narrow a matcher, confirm exactly the expected test goes red, restore via cp+shasum (never git checkout --)"

key-files:
  created:
    - meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/build-base.yml
    - meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/python.yml
    - meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/release.yml
    - meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/lint-ruff.yml
  modified:
    - meta/buildRunnersOnedir.ts
    - meta/__tests__/buildRunnersOnedir.test.ts
    - .prettierignore

key-decisions:
  - "Reported the ACTUAL RED count (7 extractor-shape tests + 5 deriveOnedirInvocation tests = 12), not the plan's predicted 9 -- the un-widened extractor's regex genuinely passes some near-miss/control cases (module-name discrimination, launcher discrimination, zero-match refusal, mentions-only, bare control) without any widening, so only the shape-recognition and multi-match-mixed-shapes tests plus the newly-added deriveOnedirInvocation cases were genuinely red"
  - "deriveOnedirInvocation reconstructs pyinstallerArgs into a joined string before applying the original expression-drop regex, then re-splits -- preserves 100% of the original toOnedirCommand behaviour for its 3 pre-existing byte-pinned tests while correctly handling multi-token \${{ ... }} expressions for module forms"

requirements-completed: [REQ-34.9-02]

# Metrics
duration: 12min
completed: 2026-08-25
---

# Phase 34.16 Plan 07: Widen the pyinstaller extractor to recognise legendary 0.21.0's uv/python-module invocation shapes

**Widened `extractUpstreamPyinstallerCommand` from a single bare-`pyinstaller`-token match to three enumerated invocation forms (bare, `uv run --module/-m PyInstaller`, `python(3) -m PyInstaller`), each RED-proven individually against the real captured legendary-0.21.0 workflow fixtures, with both pre-existing refusals and the argv-list spawn control kept intact.**

## Performance

- **Duration:** ~12 min (commit-to-commit: 2026-08-25T19:41:40+12:00 to 2026-08-25T19:53:12+12:00; continuation session additionally spent time on the stale header comment, mutation proof, and verification sweep)
- **Tasks:** 2/2 completed
- **Files modified:** 7 (3 source/test files modified + 4 new fixture files)

## Accomplishments

- `extractUpstreamPyinstallerCommand` now recognises legendary 0.21.0's real `uv run --module PyInstaller` Build step (previously threw, breaking the arm64 CI leg -- F-34.16-D) while keeping the bare `pyinstaller` form for gogdl/nile unchanged.
- Both pre-existing refusals (zero-match naming the workflows dir; multi-match naming every match on >1 distinct `(command, workingDirectory)` pair) survive unweakened and are now proven against mixed bare/module-form fixtures too.
- The spawned executable can no longer be chosen by upstream YAML text: `runOnedirBuild` now spawns `pyinstallerPath(repoDir)` directly with a derived `argv` array; the old `argv[0] = pyinstallerPath(repoDir)` string-splitting substitution is gone (T-34.16G-01/02).
- `BUILD-MANIFEST-{arch}.json`'s field names (`upstreamLine`, `upstreamWorkingDirectory`, `onedirCommand`, `droppedExpressions`) are unchanged -- confirmed via the untouched `buildManifestObject` describe block staying green.

## Task Commits

1. **Task 1: RED-prove the three widening shapes against the un-widened extractor** - `4a355378f` (test)
2. **Task 2: Widen the extractor, make onedir derivation form-aware, stop taking the spawned executable from upstream text** - `abc4cbe8b` (feat)

_No separate plan-metadata commit for this SUMMARY was created by this executor -- STATE.md/ROADMAP.md updates are explicitly out of scope for this session per orchestrator instruction (see Deviations)._

## Files Created/Modified

- `meta/buildRunnersOnedir.ts` - `matchInvocationForm()` (enumerated 3-form table), widened `UpstreamPyinstallerCommand`/`extractUpstreamPyinstallerCommand` (adds `form`, `pyinstallerArgs`; zero-match error message lists all 3 forms), new `deriveOnedirInvocation()`/`OnedirInvocationResult`, `toOnedirCommand()` reimplemented on top of it (`form` param defaults to `'bare'`), `runOnedirBuild` takes `onedirArgs: string[]` and spawns the venv pyinstaller path directly, `buildRunner`'s pipeline and return object updated accordingly, header comment rewritten to describe the 3-form contract.
- `meta/__tests__/buildRunnersOnedir.test.ts` - 12 Task-1 tests (RED-proving each shape, both refusals, mention-rejection) + 5 Task-2 tests (`deriveOnedirInvocation` per-form derivation, module-form `--onefile` refusals, launcher-token-absence assertions, `toOnedirCommand` with an explicit `form` argument).
- `meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/{build-base.yml,python.yml,release.yml,lint-ruff.yml}` - real legendary 0.21.0 workflow files, captured verbatim via `gh api .../contents/...?ref=0.21.0`, byte-verified against upstream twice (Task 1 capture, and again during this session's evidence-gathering pass).
- `.prettierignore` - added `meta/__tests__/fixtures/upstream-workflows` so the verbatim fixture captures are never reformatted.

## Evidence for plan 34.16-11 (F-34.16-D LIVE-GATE record)

### RED test names and pre-fix failure text (12 total, run against the pre-widening extractor at commit `4a355378f`'s parent state)

Re-derived in this session by temporarily copying `git show 4a355378f:meta/buildRunnersOnedir.ts` (the un-widened extractor) into place, running the full current test file (Task 1 + Task 2 tests) against it, then restoring the widened file via `cp` + `shasum -a 256` verification (never `git checkout --`).

**Note on count:** the plan predicted "nine" RED tests; the actual count is **12** (7 from Task 1's `extractUpstreamPyinstallerCommand` shape/refusal tests + 5 from Task 2's `deriveOnedirInvocation` tests, which did not exist as a function until Task 2 landed). Task 1 alone RED-proved 7 of its 12 added tests (the other 5 -- bare control, module-name discrimination, launcher discrimination, zero-match refusal, mentions-only -- pass against both the un-widened and widened extractor by design, since they assert the un-widened behaviour is preserved).

1. **`Shape B ("uv run --module PyInstaller") extracts as a recognised invocation`**
   `No "run:" step invoking pyinstaller directly (as its first token) found in any *.yml/*.yaml workflow under .../onedir-fixture-shape-b-.../.github/workflows`

2. **`Shape B' ("uv run -m PyInstaller", short module flag) extracts as a recognised invocation`**
   `No "run:" step invoking pyinstaller directly (as its first token) found in any *.yml/*.yaml workflow under .../onedir-fixture-shape-bprime-.../.github/workflows`

3. **`Shape C ("python -m PyInstaller") extracts as a recognised invocation`**
   `No "run:" step invoking pyinstaller directly (as its first token) found in any *.yml/*.yaml workflow under .../onedir-fixture-shape-c-.../.github/workflows`

4. **`Shape C' ("python3 -m PyInstaller", versioned interpreter) extracts as a recognised invocation`**
   `No "run:" step invoking pyinstaller directly (as its first token) found in any *.yml/*.yaml workflow under .../onedir-fixture-shape-cprime-.../.github/workflows`

5. **`Shape B with a working-directory: sibling still resolves the sibling for a module form`**
   `No "run:" step invoking pyinstaller directly (as its first token) found in any *.yml/*.yaml workflow under .../onedir-fixture-shape-b-workdir-.../.github/workflows`

6. **`the REAL captured legendary-0.21.0 fixture set extracts exactly one result -- the "uv run --module PyInstaller" Build step`**
   `No "run:" step invoking pyinstaller directly (as its first token) found in any *.yml/*.yaml workflow under .../onedir-fixture-real-legendary-.../.github/workflows`

7. **`two DISTINCT invocations -- one bare shape and one MODULE shape -- still throw naming both (multi-match refusal sees the new forms too)`**
   `expect(received).toBeDefined()` / `Received: undefined` (the un-widened extractor found only the bare-shape match, so it returned normally instead of throwing the expected multi-match refusal)

8. **`uv-run-module form: swaps --onefile for --onedir, drops the GitHub expression, and the args list contains no launcher token`**
   `TypeError: (0 , buildRunnersOnedir_1.deriveOnedirInvocation) is not a function`

9. **`python-m form: swaps --onefile for --onedir and the args list contains no launcher token`**
   `TypeError: (0 , buildRunnersOnedir_1.deriveOnedirInvocation) is not a function`

10. **`module form: throws naming --onefile when the upstream args lack it (refusal proven beyond the bare form)`**
    `expect(received).toThrow(expected)` / `Received message: "(0 , buildRunnersOnedir_1.deriveOnedirInvocation) is not a function"`

11. **`toOnedirCommand with form="uv-run-module" derives identically to calling deriveOnedirInvocation directly on the sliced args`**
    `Derived onedir command no longer starts with "pyinstaller": "uv run --module PyInstaller --onedir --name legendary cli.py"` (pre-widening `toOnedirCommand` ignored the unrecognised second argument and string-split the whole line, so the launcher tokens leaked into the derived command)

12. **`toOnedirCommand with form="python-m" derives identically to calling deriveOnedirInvocation directly on the sliced args`**
    `Derived onedir command no longer starts with "pyinstaller": "python -m PyInstaller --onedir --name legendary cli.py"`

### GREEN after (all 12, plus the full suite)

All 12 tests above pass against the widened extractor at commit `abc4cbe8b`. Full verification: `npx jest --selectProjects Meta meta/__tests__/buildRunnersOnedir.test.ts` -> `Tests: 1 skipped, 565 passed, 566 of 567 total` (the one non-skipped failure, `genI18nGateScope.test.ts`, is a pre-existing baseline failure from the concurrent dirty working tree, unrelated to this plan, and present both before and after these commits). Full `npx jest --selectProjects Meta` (all 23 Meta suites) confirms the identical failure set.

### Mutation proof

Backed up `meta/buildRunnersOnedir.ts` (sha256 `7b9fc3ffcc3668480d9f205ecb754253053a228ebbb71ebd1e3ed0c7fd08ae62`), then narrowed `matchInvocationForm`'s `uv-run-module` branch to drop `-m` support (`tokens[2] === '--module'` only, removing the `|| tokens[2] === '-m'` alternative). Reran the suite: **exactly** `Shape B' ("uv run -m PyInstaller", short module flag) extracts as a recognised invocation` went red, alongside the unrelated pre-existing `genI18nGateScope` baseline failure -- no other test was affected (`Tests: 2 failed, 1 skipped, 564 passed, 567 total`). Restored via `cp` from the backup (never `git checkout --`), verified `shasum -a 256` equality (`7b9fc3ff...` on both sides) before rerunning the full suite to confirm GREEN again (`565 passed`).

### Fixture sha256 pairs (all byte-identical to upstream `legendary-gl/legendary` at tag `0.21.0`, re-verified via a fresh `gh api .../contents/...?ref=0.21.0` fetch during this session)

| File | sha256 |
|---|---|
| `meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/build-base.yml` | `af81984dfeed99d7b5d8f23dc06a9de7682ef2de78889479d03838143065274c` |
| `meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/lint-ruff.yml` | `d236139b8078737aa427f9e70d36c06446378104239a013f1d5e50ee51c7c038` |
| `meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/python.yml` | `f4334b52d287d02965f5af77e2858539910b8c3ac52e6114a7ed0cb2a69ecbb3` |
| `meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/release.yml` | `da8c869916772035696ea9d745eb1177c829b9d591152e0c00b0c1a34d1494e7` |

### Commit shas

- Task 1 (RED): `4a355378f243f3751344668573df0d09982178be`
- Task 2 (GREEN, widening): `abc4cbe8b78cfd4bfae4125011c420fb0a47eaac`

### Additional acceptance-criteria checks (all passing)

- `grep -c 'argv\[0\] =' meta/buildRunnersOnedir.ts` -> `0` (RED proof: `git show 4a355378f:meta/buildRunnersOnedir.ts | grep -c 'argv\[0\] ='` -> `1`)
- `grep -v '^ *//' meta/buildRunnersOnedir.ts | grep -c 'shell: true'` -> `0`
- `grep -c 'as its first token' meta/buildRunnersOnedir.ts` -> `0` (RED proof: same command against `4a355378f` -> `1`)
- `shasum -a 256 meta/releaseTags.ts` -> `f6c4e5006f6a4128a1f4cf7644292ad5e361898fde513c0f39edb1b6e6ff8895` (unchanged, matches precondition)
- `pnpm codecheck` (`tsc --noEmit`) -> exit 0, no output
- `npx eslint -f json meta/buildRunnersOnedir.ts meta/__tests__/buildRunnersOnedir.test.ts` -> 0 severity-2 findings

## Deviations from Plan

### Auto-fixed Issues

None -- no bugs, missing functionality, or blocking issues were encountered beyond what the plan itself anticipated and scoped.

### Other deviations

**1. [Documentation accuracy] Reported RED count is 12, not the plan's predicted 9**

- **Found during:** Task 1 (originally), reconfirmed while gathering evidence for this SUMMARY.
- **Detail:** The plan's `<output>` section says "the nine RED test names." The actual RED count across the 17 new tests (12 from Task 1 + 5 from Task 2) is 12: 7 `extractUpstreamPyinstallerCommand` tests (Shape B, B', C, C', workdir-variant, real-fixture, multi-match-mixed-shapes) plus 5 `deriveOnedirInvocation`-related tests added in Task 2 (which could not exist as RED/GREEN pairs until the function itself was introduced). The other 5 Task-1 tests (bare control, module-name discrimination, launcher discrimination, zero-match refusal, mentions-only) pass against BOTH the un-widened and widened extractor by design -- they assert behaviour the widening must NOT change.
- **Resolution:** Documented the actual, re-verified numbers and test-by-test failure text above rather than conforming to the plan's predicted count.
- **Files affected:** none (documentation-only finding, recorded here for 34.16-11's consumption).

**2. [Scope discipline] No STATE.md / ROADMAP.md / requirements-completed automation run**

- **Reason:** The orchestrating session's critical guardrails explicitly prohibit this executor from touching `.planning/STATE.md` or `.planning/ROADMAP.md` (orchestrator-only in this session). The standard `<state_updates>` and `<final_commit>` steps were skipped accordingly; `requirements-completed: [REQ-34.9-02]` is recorded in this SUMMARY's frontmatter for the orchestrator to apply via its own `requirements.mark-complete` call.

## Known Stubs

None.

## Threat Flags

None -- the two STRIDE controls this plan targets (T-34.16G-01: spawned executable never chosen by upstream text; T-34.16G-02: argv-list spawn, never `shell: true`) are both proven intact by the acceptance-criteria greps above, and no new network endpoint, auth path, file-access pattern, or schema surface was introduced.
