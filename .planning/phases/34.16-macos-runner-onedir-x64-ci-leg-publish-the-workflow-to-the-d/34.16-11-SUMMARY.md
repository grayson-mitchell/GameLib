---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 11
subsystem: docs
tags: [live-gate-contract, gap-closure, findings-closure, structural-reachability-review]

# Dependency graph
requires:
  - phase: 34.16-07
    provides: "extractUpstreamPyinstallerCommand widened to three invocation shapes, RED-proven"
  - phase: 34.16-08
    provides: "two-layer version-drift tripwire (offline + networked)"
  - phase: 34.16-09
    provides: "local arm64 onedir build proof against the real, pinned legendary repo -- BRANCH VERDICT: CLEAN BUILD"
  - phase: 34.16-10
    provides: "F-34.16-D's 0.20.43 pin sub-question settled -- PIN RESOLVED, finding's own guess graded unsupported"
provides:
  - "34.16-LIVE-GATE.md contract amended: attempt-scoped precondition ledger (12 slots), Precondition 9 (ref-equals-HEAD), a section 13 VOID rule, section 7 criterion 4 deriving its expected diff-tree status from a measured slot instead of a hardcoded literal, section 8 criterion 0 + the --repo amendment"
  - "F-34.16-E and F-34.16-F closed (status: fixed) by this plan's own commits"
  - "F-34.16-D closed as status: fixed while remaining blocking: true -- code and process gap resolved, live x64 CI evidence still owed to a future RUN ATTEMPT 3"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Attempt-scoped evidence ledger with named slots, cited by slot name from every dependent criterion, backed by a VOID verdict (neither PASS nor FAIL) when a cited slot is absent or TBD -- converts a prose re-capture instruction into a structurally-enforced one"
    - "Deriving an expected diff-tree/measurement outcome from a freshly-measured slot instead of a literal transcribed at authoring time, with the three reachable cases (absent / present-different / present-identical) named explicitly rather than collapsed to a single expected value"
    - "Dated SETTLED blocks appended to closed findings, each required to cite the specific SUMMARY/proof artifact it draws from rather than re-describing the outcome from memory"

key-files:
  created:
    - .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-11-SUMMARY.md
  modified:
    - .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md

key-decisions:
  - "F-34.16-D stays blocking: true despite status: fixed -- a gap cycle can close the code/process defect but cannot manufacture live x64 CI evidence; only a real RUN ATTEMPT 3 Scored Item 2 can discharge it"
  - "The historical RUN ATTEMPT 1 and RUN ATTEMPT 2 records, and all Preconditions recorded there, are left byte-unchanged -- they are true records of what was measured at the time, not retroactively corrected to match current HEAD"
  - "The one specific acceptance-criterion RED-specimen check (grep -c '60f4f1e6' / 'e4bc5bbc' inside section 6) was found to be vacuous in BOTH the pre-edit and post-edit file -- recorded honestly below rather than claimed as a proven change"

requirements-completed: [REQ-34.9-02, REQ-34.9-03, REQ-34.9-04]

# Metrics
duration: ~2h
completed: 2026-08-25
---

# Phase 34.16 Plan 11: Close F-34.16-D/E/F -- attempt-scoped ledger, derived criteria, gap-cycle settlement -- Summary

**Rewrote three defective or process-gap sections of `34.16-LIVE-GATE.md`'s own scored-item contract (an attempt-scoped precondition ledger with a VOID enforcement rule, a diff-tree criterion that now derives its expected status from a measured slot instead of a hardcoded literal, and a carried-over `--repo` fix), then recorded the gap cycle's own settlement of `F-34.16-D`'s code defect -- all while proving every historical attempt record and fifteen protected frontmatter fields stayed byte-unchanged.**

## Performance

- **Duration:** ~2h
- **Tasks:** 3/3 completed
- **Files modified:** 1 (`34.16-LIVE-GATE.md`; no other file touched)

## Accomplishments

- **Task 1 (closes `F-34.16-F`):** Replaced section 6's prose re-capture instruction with a
  12-slot `ATTEMPT PRECONDITION LEDGER` template (all `Observed`/`Evidence file` cells `TBD`),
  added Precondition 9 (`P9.REF-EQ-HEAD`, the dispatched-ref-equals-`HEAD` check `F-34.16-A`
  showed was missing), rewrote Preconditions 2 and 3 as failure-SET assertions instead of
  transcribed passed-counts, rewrote Precondition 6 as a re-measured `P6.SHA-WORKTREE` /
  `P6.SHA-MAIN` pair plus a derived `P6.MAIN-HAS-FILE` fact instead of a stale transcribed sha,
  and added a section 13 VOID rule stating any criterion citing an absent/`TBD` ledger slot is
  VOID rather than PASS/FAIL. Section 7 criterion 2 now cites `P6.SHA-WORKTREE` by name and is
  explicitly VOID if that slot is unfilled.
- **Task 2 (closes `F-34.16-E`, carries over `F-34.16-C`):** Rewrote section 7 criterion 4 to
  derive its expected `git diff-tree` status from `P6.MAIN-HAS-FILE` across three named cases
  (`absent`->`A`, `present-different`->`M`, `present-identical`->zero lines with an explicit
  operator disposition) instead of hardcoding an unconditional `A`-or-abort. Added
  `--repo grayson-mitchell/GameLib` to section 8's gesture line and its `gh run list` criterion-1
  command (the unactioned half of `F-34.16-C`'s own fix). Added section 8 criterion 0, re-measuring
  `P9.REF-EQ-HEAD` immediately before the dispatch gesture, numbered `0` deliberately so it reads
  as a gate on the gesture rather than as evidence about its result.
- **Task 3 (records the gap-cycle settlement):** Moved `F-34.16-D`/`F-34.16-E`/`F-34.16-F` to
  `status: fixed` with `fixed_by:` commits in the frontmatter `findings:` list (`F-34.16-D` keeps
  `blocking: true`); appended three dated `**SETTLED 2026-08-25 by the 34.16 gap cycle (plans
  07-11):**` blocks, each citing the specific SUMMARY or proof document it draws from; rewrote
  `### Required before RUN ATTEMPT 3` as a forward-looking checklist (items 1-4 DONE with named
  artifacts, item 5 OPEN and human-owned); proved all three historical-record regions and all
  fifteen protected frontmatter fields byte-unchanged before committing.

## Task Commits

Each task was committed atomically, isolated with `git commit --only` against the single file this
plan touches (the working tree carried ~15 unrelated files from a concurrent session throughout;
none were staged or touched):

1. **Task 1: Close `F-34.16-F`** -- `3fd92c5ea` (docs)
2. **Task 2: Close `F-34.16-E`, carry over `F-34.16-C`'s `--repo` amendment** -- `eb30450ae` (docs)
3. **Task 3: Update finding statuses, append SETTLED blocks, rewrite the RUN ATTEMPT 3 checklist** -- `5c8e0b8d7` (docs)

No separate plan-metadata commit for this SUMMARY -- STATE.md/ROADMAP.md/REQUIREMENTS.md updates
are explicitly out of scope for this session per the orchestrator's critical repo guardrails (a
documented recurring `gsd-sdk query state.*` corruption defect on this repo).

## Files Created/Modified

- `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md` -- the only file this plan modifies. All edits confined to: (a) section 5's evidence-capture note, (b) section 6 (Preconditions, wholly replaced), (c) section 7 criteria 2 and 4, (d) section 8's gesture line, criterion 0, and criterion 1's `gh run list` command, (e) section 13's VOID rule, (f) the frontmatter `findings:` list entries for `F-34.16-D`/`-E`/`-F`, (g) the ends of the `F-34.16-D`/`-E`/`-F` finding sections (SETTLED blocks appended), (h) `### Required before RUN ATTEMPT 3` (rewritten in place). Nothing below the `## RUN RECORD` divider's historical narrative content (the `## Preconditions`, `## RUN ATTEMPT 1`, and `## RUN ATTEMPT 2` records themselves) was touched -- proven below.
- `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-11-SUMMARY.md` -- this document.

## Structural Reachability Review

Applied per decision D-E (`.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md`)
to every criterion added or amended by this plan, before the amended contract is left for a future
RUN ATTEMPT 3 to execute. All seven tests were applied to each item; only non-trivial findings are
elaborated in the Verdict/Evidence cells, trivial applicability is stated plainly rather than
omitted (per the reference's own "found nothing" vs. "did not look" distinction).

| Item/sub-check/precondition | Surface it names | What it asks for | Verdict | Evidence |
|---|---|---|---|---|
| Section 6 ATTEMPT PRECONDITION LEDGER (12 slots, template) | A markdown table an operator copies and fills manually, no code path | Every `Observed`/`Evidence file` cell filled with a value measured AT THAT ATTEMPT | REACHABLE | T1/T2/T7 N/A (no URL scheme, no concurrent UI gesture, no app UI involved -- purely operator-driven document editing). T3: the ledger table itself IS the sink; there is no separate emitter to miss (`34.16-LIVE-GATE.md:190-205`). T4: a skipped fill is observable via `grep 'TBD'` against the attempt's own copied ledger row -- the absence case is falsifiable. T5: see the dedicated pairing-pass statement below. T6: `SESSION.DIR`'s premise (a fresh dir) is backed by `mktemp -d`'s own uniqueness guarantee, not by an assumption about prior state (`34.16-LIVE-GATE.md:194`, section 5 note). |
| Precondition 9 / `P9.REF-EQ-HEAD` | `git fetch gamelib <ref>` + `git rev-parse` comparison against local `HEAD` | Confirms the ref that will actually be dispatched matches the code the operator believes they are testing | REACHABLE | T1 N/A (git ref comparison, no HTTP origin). T3: sink is the ledger cell itself (`34.16-LIVE-GATE.md:205, 257-266`); also independently re-measured at dispatch time by section 8 criterion 0, giving this fact two independent capture points rather than one. T6: explicitly reasons about pre-existing external state -- names why Precondition 1 (COMMITTED-only) cannot substitute, which is exactly the gap `F-34.16-A` exposed (`34.16-LIVE-GATE.md:260-266`). |
| Section 7 criterion 2 (workflow-blob hash matches `P6.SHA-WORKTREE`) | `git hash-object` / `shasum -a 256` compared against the ledger slot | The pushed blob is the same bytes measured at precondition time, VOID if the slot is unfilled | REACHABLE | T3: sink is the criterion's own recorded evidence line (`34.16-LIVE-GATE.md:297-300`). T4: VOID-if-absent is itself falsifiable -- an attempt that skips the ledger produces a `TBD` slot, observable by `grep`, which is precisely the failure mode section 13's VOID rule targets and section 13 itself states attempt 2 would have been caught by it (`34.16-LIVE-GATE.md:651`). |
| Section 7 criterion 4, three derived cases | `git diff-tree --no-commit-id --name-status` output compared against `P6.MAIN-HAS-FILE` | A different expected status per case (`absent`->`A`, `present-different`->`M`, `present-identical`->zero lines) instead of one hardcoded literal | CONDITIONAL (self-defending) | T5: the pair {`git fetch gamelib main` inside Precondition 6, which derives `P6.MAIN-HAS-FILE`} x {section 7 criterion 1's OWN re-fetch of `gamelib/main` for a fresh `MAIN_SHA`, `34.16-LIVE-GATE.md:291-296`} was considered by name. If `gamelib/main` moves in the narrow window between the two fetches (a concurrent push), `P6.MAIN-HAS-FILE` (captured at Precondition-6 time) could disagree with the tree criterion 4 actually observes. This is NOT silently missed: criterion 4's own ABORT conditions clause already fires on "a status that disagrees with `P6.MAIN-HAS-FILE`" (`34.16-LIVE-GATE.md:325-328`), so the race is caught as an ABORT rather than a false PASS. Realistic likelihood is low -- `gamelib` is a personal fork; the only writer to `main` in this gate's own flow is criterion 6's push, which has not run yet at criterion-4 time -- but the disposition is stated here rather than assumed silent. T4: the `present-identical` (zero-line) case is a genuine absence assertion, made falsifiable because criterion 6's byte-identity diff (`34.16-LIVE-GATE.md:334`) independently proves the same fact by a different measurement -- an actual difference on `main` would make that diff non-empty. |
| Section 8 criterion 0 (re-measure `P9.REF-EQ-HEAD` pre-dispatch) | A second `git fetch`+`git rev-parse` pair, numbered `0`, immediately before `gh workflow run` | Catches ref drift between section 6 (precondition time) and the actual dispatch gesture | REACHABLE | T5: this criterion exists BECAUSE of a Test-5-style interaction the document itself names -- "an operator can commit or be handed commits between section 6 and this gesture" (`34.16-LIVE-GATE.md:365`) -- i.e., it is the amended contract's own answer to a state-mutating-vs-evidence-bearing pair, not an interaction this review is newly discovering. T3: sink is the criterion's own recorded evidence line, same slot name as Precondition 9, so a stale section-6-time value cannot silently satisfy it. |
| Section 8 gesture + criterion 1's `gh run list`, `--repo grayson-mitchell/GameLib` | Every `gh` invocation in section 8 | Prevents `gh` resolving to `origin` (the unrelated Heroic upstream) and producing a textually near-identical but opposite-meaning 404 | REACHABLE | T1: this IS an origin/scheme-reachability fix -- the surface (`gh`'s default-remote resolution) was previously rejecting the intended repo silently; now every command in section 8 states the flag explicitly rather than relying on a once-per-session `gh repo set-default` (`34.16-LIVE-GATE.md:350-356, 375`). Grepped and confirmed present at both call sites. |

**Test 5 pairing pass, recorded explicitly.** Reduction applied: every state-mutating requirement
in this plan's added/amended content against every evidence-bearing requirement in the same
content (M x N, not the full cross product) --

- **State-mutating (M):** Precondition 6's `git fetch gamelib main`; Precondition 9 / section 8
  criterion 0's `git fetch gamelib <ref>`; section 7 criterion 1's own re-fetch of `gamelib/main`
  for a fresh `MAIN_SHA`.
- **Evidence-bearing (N):** section 7 criterion 2 (`P6.SHA-WORKTREE` match), section 7 criterion 4
  (`P6.MAIN-HAS-FILE`-derived diff-tree status), section 8 criterion 0 (`P9.REF-EQ-HEAD`
  re-measure).

3 x 3 = 9 pairs considered. One flagged: {Precondition 6's `git fetch gamelib main`} x {section 7
criterion 1's re-fetch of `gamelib/main`} against {section 7 criterion 4's derived expectation} --
recorded above as CONDITIONAL (self-defending, the ABORT-on-disagreement clause already catches
it). The other 8 pairs: no interaction found -- Precondition 9's and criterion 0's fetches target
the feature ref, not `main`, and do not touch any state criterion 2 or criterion 4 depend on;
criterion 0's own re-measurement is itself the contract's answer to its one flagged pair (section 6
time vs. dispatch time), not a new interaction this review found unaddressed.

## Region hashes -- proof of byte-unchanged historical records

Measured immediately before Task 3's edits (which is the only task touching content below the
`## RUN RECORD` divider) and re-measured immediately after, via
`sed -n '<start>,<end-1>p' <file> | shasum -a 256`. All three regions are also independently
confirmed identical to the pristine pre-Task-1 backup
(`/private/tmp/.../scratchpad/34.16-LIVE-GATE.md.orig`, sha256
`8fd83aa1c146c1fd79b39c58e7784038f4d04a169aa08c21a7270fbf518b6710`), proving Tasks 1 and 2 also
never touched these regions, despite the file's line numbers shifting as content was added above
the divider.

| Region | Before Task 3 | After Task 3 |
|---|---|---|
| `## Preconditions` -> `## RUN ATTEMPT 1` | `54ad1dc5c9b2adc3ff4d4ae5eb448ac595342e7f25ee8b738a258deee8014bdc` | `54ad1dc5c9b2adc3ff4d4ae5eb448ac595342e7f25ee8b738a258deee8014bdc` |
| `## RUN ATTEMPT 1` -> first `### Finding` | `4d775382c4be24b88631840d0ccf92795440817adb7c855bdf89ff92263e4a33` | `4d775382c4be24b88631840d0ccf92795440817adb7c855bdf89ff92263e4a33` |
| `## RUN ATTEMPT 2` -> first `### Finding` (i.e. `F-34.16-D`'s own heading) | `9cb44040f6b9e0f878399bd07799de127491347015f4fc272ee76bbf90b63db5` | `9cb44040f6b9e0f878399bd07799de127491347015f4fc272ee76bbf90b63db5` |

All three: **IDENTICAL**. Note the `F-34.16-D`/`-E`/`-F` finding-section bodies themselves (which
DO carry this plan's appended SETTLED blocks) sit deliberately outside these region boundaries --
the third region's end boundary was chosen at `F-34.16-D`'s own heading precisely so an append
inside that finding's body cannot silently perturb the RUN ATTEMPT 2 narrative's hash.

## Fifteen protected frontmatter fields -- proof of byte-unchanged values

Extracted via exact-line grep (`grep -E '^(field):'`) before and after Task 3, text compared with
line-number prefixes stripped (line numbers shifted by +3 as a direct, expected consequence of
Task 3 adding lines to the `findings:` list above `run_by:` -- the field VALUES did not move).

| Field | Before | After |
|---|---|---|
| `status` | `run-complete # attempt 2 scored; FAIL routes to a gap cycle per §13` | identical |
| `blocking` | `true` | identical |
| `created` | `2026-08-24` | identical |
| `supersedes_run` | `null` | identical |
| `verdict` | `FAIL # RUN ATTEMPT 2, 2026-08-25. Item 2 FAILED at §8 criterion 3 (arm64 leg concluded failure); Items 3-6 NOT ATTEMPTED per §13 ordering` | identical |
| `run_date` | `2026-08-25 # attempt 1 ABORTED at Item 2; attempt 2 scored FAIL` | identical |
| `items_total` | `6` | identical |
| `items_passed` | `1 # Item 1 only, with 3 recorded deviations (F-34.16-E, F-34.16-F, --no-verify)` | identical |
| `items_failed` | `1 # Item 2` | identical |
| `items_carried_forward` | `0` | identical |
| `items_not_attempted` | `4 # Items 3, 4, 5, 6 -- §13 ordering, each depends on Item 2 having PASSED` | identical |
| `runner` | `human` | identical |
| `session_dir` | `/tmp/gamelib-34.16-gate-rKAUE2 # attempt 1 ONLY -- attempt 2 ran without a fresh capture, see F-34.16-F` | identical |
| `launch_ordinals_used` | `1, 2 # ordinal 2 reached and failed; ordinals 3-6 never launched` | identical |
| `run_by` | `34.16-06` | identical |

All 15: **IDENTICAL** (`diff` of the stripped-line-number extracts returns no output).

`F-34.16-A` stays `status: resolved`; `F-34.16-B` stays `status: fixed`; `F-34.16-C` stays
`status: resolved` -- none of these three findings' `status:` or `summary:` fields were touched by
this plan; only `F-34.16-D`/`-E`/`-F` were amended, exactly as scoped.

## The one thing still owed to a human before RUN ATTEMPT 3

**Item 5 of `### Required before RUN ATTEMPT 3` -- push the fix, re-push the workflow blob if it
changed (it did not; expect `P6.MAIN-HAS-FILE = present-identical`), then re-dispatch with
`--repo`.** Everything else this gap cycle could close (items 1-4: the extractor widening, the
tripwire, the criterion-4 amendment, and the precondition re-capture mechanism) is DONE.

## Deviations from Plan

### Auto-fixed Issues

None. No bugs, missing functionality, or blocking issues were encountered while editing the
document; every change made was exactly the amendment the plan's task text specified.

### Other deviations

**Vacuous acceptance-criterion check, recorded honestly.** One planner-specified RED-specimen
assertion -- a combined `grep -c` for the literal hash fragments `60f4f1e6` and `e4bc5bbc` scoped
to section 6 via `awk '/^## 6\. Preconditions/,/^## 7\./'` -- printed `0` in **both** the pristine
pre-edit backup and the post-edit file. This is because section 6's original text never literally
quoted either hash string; it only referenced "the value plan 34.16-02's summary recorded" in
prose. The check is therefore attributable to nothing this plan did (0/0, not a false claim of
change where none occurred), and is recorded here plainly rather than silently passed over, per the
plan's own instruction to make the change "attributable rather than vacuous."

**No forbidden command was run.** Self-audited by reviewing every Bash invocation issued across
this execution: all were read-only (`grep`, `sed`, `awk`, `shasum`, `git status`/`show`/`log`/
`rev-parse`/`remote -v`, `npx prettier --check`) plus the two per-task `git commit --only <path>`
commits and one metadata check. No `git push`, `gh workflow run`, `git checkout --`/`restore`,
`git stash`, `git reset`, nor any of the four forbidden `pnpm` scripts (`pin:runner-digests`,
`download-helper-binaries`, `dist:mac`, `verify:runner-bundle`) was invoked at any point. This is a
self-audit by direct review of the session's own tool-call record, not a grep over a saved
transcript file (no such file exists as a filesystem artifact accessible to this session).

## Known Stubs

None. This plan touches only a `.planning/` markdown document; no UI, data source, or component
was created or modified.

## Threat Flags

None. This plan's own `<threat_model>` (T-34.16G-20 through T-34.16G-25, all `mitigate`) already
covers every surface this plan's edits touch -- an authoring agent editing a blocking gate
contract's own findings and scored-item criteria, with no new network endpoint, auth path, file
access pattern, or schema surface introduced. Every mitigation named in that register (region
hashes, exact-line frontmatter greps, `grep -c '^## RUN ATTEMPT 3'` = 0, `--repo` amendment,
Structural Reachability Review, `blocking: true` retained, forbidden-command self-audit) is
recorded as satisfied above.

## Self-Check

```
FOUND: .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md
FOUND: .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-11-SUMMARY.md
FOUND: 3fd92c5ea (git log --oneline --all)
FOUND: eb30450ae (git log --oneline --all)
FOUND: 5c8e0b8d7 (git log --oneline --all)
```

## Self-Check: PASSED
