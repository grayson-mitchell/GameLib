---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 09
subsystem: infra
tags: [pyinstaller, ci, macos-onedir, meta-build, gap-closure, build-proof]

# Dependency graph
requires:
  - phase: 34.16-07
    provides: "widened extractUpstreamPyinstallerCommand recognising uv-run-module/python-m invocation forms, plus deriveOnedirInvocation()"
provides:
  - "F-34.16-D proven end to end against the REAL cloned legendary-gl/legendary@0.21.0 repo on arm64 -- not only the captured fixture"
  - "34.16-ONEDIR-BUILD-PROOF.md: package registry check + pre/post-run blast-radius proof + BRANCH VERDICT contract, reusable for a future x64-analogue check"
  - "Confirmed shape fact for plan 34.16-11's x64 comparison: Python.framework lives under _internal/, not at the onedir tree root"
affects: [34.16-11]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Registry-check-before-install gate: every third-party Python package a build script will pip-install is enumerated from the pinned upstream's OWN manifest and confirmed to resolve on PyPI (200 + canonical-name match) before the install runs, applied here to a throwaway build venv rather than this repo's own dependency manifests"
    - "Snapshot-diff blast-radius proof: six named trees hashed/counted before a mutating build, re-measured and diffed after, plus a find -newer sweep -- 'the build only writes to .build-tools' treated as a measurement, not the script's own success report (F-34.9-02)"

key-files:
  created:
    - .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md
  modified: []

key-decisions:
  - "No code change made or needed -- this plan is proof-only. The verdict was CLEAN BUILD on the first attempt, so no finding was appended to F-34.16-D and no re-open of 34.16-07 was required."

requirements-completed: [REQ-34.9-02]

# Metrics
duration: ~25min
completed: 2026-08-25
---

# Phase 34.16 Plan 09: Onedir build proof -- legendary @ pinned tag, real clone, arm64 -- CLEAN BUILD

**Cloned the real, pinned `legendary-gl/legendary@0.21.0` repo fresh and ran the onedir build end to end on this arm64 machine through plan 34.16-07's widened `uv-run-module` extraction path: exit 0, a running `legendary --version` binary, `Python.framework` correctly present (nested under `_internal/`), archive sha256 `004beb7b8...e3f9e` -- proving `F-34.16-D`'s code half against the real thing, not only the captured fixture, before burning a third live-gate attempt.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 2/2 completed
- **Files modified:** 1 (new proof document)

## Accomplishments

- Enumerated every third-party Python package the onedir build path installs -- `requests`, `filelock`, `pycryptodomex` (`project.dependencies`), `pyinstaller` (`optional-dependencies.pyinstaller_build`, declared but never itself installed since `installPyinstaller()` always takes the `PYINSTALLER_FALLBACK_PIN` branch when no `requirements.txt` exists), `uv_build`/`uv-build` (`build-system.requires`), and `pyinstaller==6.20.0` (`PYINSTALLER_FALLBACK_PIN`) -- all 6 registry-checked at `200` on PyPI with matching canonical names before Task 2 ran.
- Ran the real build: `pnpm build-runners-onedir --arch=arm64 --runner=legendary` against a fresh clone, exit 0 (`xxd`-verified `300a`). Confirmed **CLEAN BUILD** -- the widened extractor correctly recognised legendary 0.21.0's real `uv run --module PyInstaller --onefile --name legendary ${{ steps.strip.outputs.option }} -i ../assets/windows_icon.ico cli.py` line, derived `pyinstaller --onedir --name legendary -i ../assets/windows_icon.ico cli.py` with no launcher token surviving, and PyInstaller produced a working onedir tree at `.build-tools/runners-onedir/src/legendary/legendary/dist/legendary`: 141 files, 4 symlinks, `_internal/Python.framework` present, `legendary --version` runs and exits 0.
- Proved the second silent contract `<measured_facts>` warned about (legendary's move from `requirements.txt`/`setup.py` to `pyproject.toml`+`uv.lock`) does NOT break the steps after extraction: `pip install -e .` resolved the `uv_build` backend from PyPI and completed; `installPyinstaller()`'s fallback-pin path worked exactly as `resolvePyinstallerRequirement()` predicts when no `requirements.txt` exists.
- Blast-radius proof: all six Task-1 snapshots (git status, `runnersOnedirDigests.json` sha256, `PENDING-CI-PUBLISH` sentinel count, `releaseTags.ts` sha256, `public/bin`+`build/bin` tree shasum) re-measured byte-identical after the build; a `find -newer` sweep over `public/bin`, `build/bin`, `meta` produced empty output. The build wrote only under gitignored `.build-tools/`.
- New shape fact for plan 34.16-11's future x64 comparison: `Python.framework` is nested under `<distDir>/_internal/`, not at the onedir tree root -- a naive top-level existence check would have false-negatived a healthy build.

## Task Commits

Each task was committed atomically:

1. **Task 1: Enumerate and registry-check the third-party Python packages, snapshot every tree the build must not touch** - `ec8e66cd1` (docs)
2. **Task 2: Run the real onedir build for legendary at the pinned tag on arm64, record the branch verdict, prove nothing outside .build-tools changed** - `7ab1f9b76` (docs)

_No separate plan-metadata commit for this SUMMARY -- STATE.md/ROADMAP.md updates are explicitly out of scope for this session per the orchestrator's critical repo guardrails (the same scope exclusion 34.16-07's executor recorded)._

## Files Created/Modified

- `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md` - the package registry-check table, six pre-run snapshots, the full build transcript, the `BRANCH VERDICT: CLEAN BUILD` line (both body and frontmatter), post-extraction measurements (file/symlink counts, `Python.framework`/`_internal` presence, `--version` output, archive sha256), and the blast-radius diff proof.

## Evidence for plan 34.16-11

- **Branch verdict:** `CLEAN BUILD`.
- **One-line reason:** the real, freshly-cloned `legendary-gl/legendary@0.21.0` repo built end to end through plan 34.16-07's widened `uv-run-module` extraction path on arm64 -- exit 0, a running `legendary --version` binary, archive sha256 `004beb7b855ed0eadda09f20165c82992fd5362d78a1c204c5e1c8e2955e3f9e`.
- **Finding to append to `34.16-LIVE-GATE.md`'s `F-34.16-D` section:** none required. No failure was observed; `F-34.16-D`'s code half is now proven against the real upstream repo for arm64, not only the captured fixture. The x64 leg's status is UNCHANGED by this plan -- it remains CI-only and unproven, exactly as `34.16-LIVE-GATE.md` §11 already records, because PyInstaller cannot cross-compile and no x64 build is possible on this arm64 machine.
- **Routing:** none. `RUN ATTEMPT 3` (the live-gate dispatch) can proceed with the arm64-leg risk retired.

## Deviations from Plan

### Auto-fixed Issues

None -- no bugs, missing functionality, or blocking issues were encountered. The build succeeded on the first attempt.

### Other deviations

None. The plan's own branch structure anticipated exactly this outcome as one of three named possibilities and did not predict which; CLEAN BUILD was the observed result and is recorded as such, not softened or reframed.

## Known Stubs

None.

## Threat Flags

None -- this plan is proof-only and touches no production code path. The three STRIDE controls this plan's own threat model targets (T-34.16G-11 spawned-executable-not-from-upstream-text, T-34.16G-13 tamper-outside-.build-tools, T-34.16G-14 sentinel/releaseTags drift, T-34.16G-15 arm64-result-misread-as-x64-evidence, T-34.16G-SC package-legitimacy) are all directly measured and recorded intact in `34.16-ONEDIR-BUILD-PROOF.md`; no new network endpoint, auth path, file-access pattern, or schema surface was introduced by this plan.

## Self-Check: PASSED

All 2 files confirmed present on disk (`34.16-ONEDIR-BUILD-PROOF.md`, `34.16-09-SUMMARY.md`); all 3
commit shas (`ec8e66cd1` Task 1, `7ab1f9b76` Task 2, `bd7de0e75` this SUMMARY) confirmed present in
`git log --oneline --all`.
