---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 09
type: build-proof
closes_finding: F-34.16-D
created: 2026-08-25
verdict: CLEAN BUILD
---

# Phase 34.16 Plan 09: onedir build proof — legendary @ pinned tag, arm64, real clone

**Purpose (34.16-09-PLAN.md):** prove plan `34.16-07`'s widened `extractUpstreamPyinstallerCommand`
against the REAL pinned `legendary-gl/legendary` repo, cloned fresh — not only against the captured
fixture Plan 07 used. This proves the whole build path, not just the parser. PyInstaller cannot
cross-compile (34.9-CONTEXT.md constraint 1), so this proves the **arm64 leg only**; the x64 leg
remains CI-only and unproven, exactly as `34.16-LIVE-GATE.md` §11 already records. **A local pass here
is NOT evidence for the x64 leg** (T-34.16G-15).

## Task 1: Package enumeration + registry check + pre-build snapshots

### Pinned tag / repo slug — read fresh from source, not transcribed

```
$ grep -n "legendary:" meta/releaseTags.ts
30:  legendary: '0.21.0',

$ grep -n "repo: 'legendary" meta/buildRunnersOnedir.ts
83:    repo: 'legendary-gl/legendary',
```

Pinned tag: **`0.21.0`**. Repo slug: **`legendary-gl/legendary`**.

### Fetch command + exit status (redirect form, not a pipe)

```
$ gh api "repos/legendary-gl/legendary/contents/pyproject.toml?ref=0.21.0" --jq '.content' > pyproject.b64
$ echo $? > pyproject-fetch.exit
$ cat pyproject-fetch.exit
0

$ base64 -d < pyproject.b64 > pyproject.toml
$ echo $? > pyproject-decode.exit
$ cat pyproject-decode.exit
0
```

Both exit files read `0`. Fetched file contents (62 lines, quoted verbatim, the relevant sections):

```toml
[build-system]
requires = ["uv_build>=0.11.4,<0.12"]
build-backend = "uv_build"

[project]
name = "legendary-gl"
version = "0.21.0"
dependencies = [
    "requests",
    "filelock",
    "pycryptodomex",
]
...
[project.optional-dependencies]
webview = ["pywebview>=3.4"]
webview_gtk = ["pywebview>=3.4", "PyGObject"]
pyinstaller_build = ["pyinstaller"]
```

`PYINSTALLER_FALLBACK_PIN`, read fresh from `meta/buildRunnersOnedir.ts:608`:
```
const PYINSTALLER_FALLBACK_PIN = 'pyinstaller==6.20.0'
```

**Note on what the build path actually installs vs. what is declared:** `resolvePyinstallerRequirement()`
(`meta/buildRunnersOnedir.ts:500`) only reads a `pyinstaller` pin from a `requirements.txt` if one
exists at the repo root. The pinned legendary tag has none (it moved to `pyproject.toml`), so
`installPyinstaller()` always takes the `PYINSTALLER_FALLBACK_PIN` branch — the declared
`pyinstaller_build` optional-dependency (bare `pyinstaller`, unpinned) is never itself installed by
this script; both are enumerated below regardless, per the task's instruction to enumerate the
optional-dependency entry as declared.

### Registry-check commands (one per enumerated package, run before Task 2)

```
$ curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/requests/json"       -> 200
$ curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/filelock/json"       -> 200
$ curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/pycryptodomex/json"  -> 200
$ curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/pyinstaller/json"    -> 200
$ curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/uv_build/json"       -> 200
$ curl -sS -o /dev/null -w '%{http_code}' "https://pypi.org/pypi/uv-build/json"       -> 200
```
Each response's own `info.name`/`info.project_url` fields (read back from the fetched JSON, not
assumed) are recorded in the canonical-name/URL columns below.

### Package table

| Package (declared) | Declaration site | PyPI HTTP status | Canonical PyPI name | PyPI URL |
|---|---|---|---|---|
| `requests` | `project.dependencies` | 200 | `requests` | https://pypi.org/project/requests/ |
| `filelock` | `project.dependencies` | 200 | `filelock` | https://pypi.org/project/filelock/ |
| `pycryptodomex` | `project.dependencies` | 200 | `pycryptodomex` | https://pypi.org/project/pycryptodomex/ |
| `pyinstaller` | `optional-dependencies.pyinstaller_build` | 200 | `pyinstaller` | https://pypi.org/project/pyinstaller/ |
| `uv_build` (`>=0.11.4,<0.12`) | `build-system.requires` | 200 | `uv-build` | https://pypi.org/project/uv-build/ |
| `pyinstaller==6.20.0` (`PYINSTALLER_FALLBACK_PIN`) | `meta/buildRunnersOnedir.ts:608` | 200 | `pyinstaller` | https://pypi.org/project/pyinstaller/ |

Every status is `200`. `uv_build` resolves to canonical PyPI project `uv-build` — this is PyPI's
standard normalization (underscore/hyphen and case are equivalent for the same project; confirmed by
reading `info.name` back from the JSON response, not assumed), not a homoglyph/typo mismatch. No
package failed to resolve; no name diverged from its declared spelling beyond that normalization.
**Task 2 is cleared to run.**

### Pre-build snapshots (all six, verbatim)

```
$ git status --porcelain
 D .planning/debug/steam-library-22-games-missing.md
 M .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-HUMAN-UAT.md
 M .planning/phases/32-tauri-ipc-re-plumb-slice-3-downloads-and-queue/32-VERIFICATION.md
 M .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md
 M .planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW.md
R  .planning/todos/pending/2026-08-21-steam-library-22-games-never-reach-the-rendered-library.md -> .planning/todos/completed/2026-08-21-steam-library-22-games-never-reach-the-rendered-library.md
 M src/backend/storeManagers/steam/__tests__/games.test.ts
 M src/backend/storeManagers/steam/games.ts
 M src/frontend/screens/Library/__tests__/filterEngine.test.ts
 M src/frontend/screens/Library/components/GameCard/index.css
 M src/frontend/screens/Library/components/RecentlyPlayed/index.tsx
 M src/frontend/screens/Library/filterEngine.ts
 M src/frontend/screens/Library/index.tsx
?? .planning/debug/resolved/steam-library-22-games-missing.md
?? .planning/phases/34.17-pathselectionbox-input-commit-enter-to-commit-and-the-unconf/evidence/
```
(This is a CONCURRENT session's unrelated dirtiness, already recorded in `34.16-LIVE-GATE.md`
Precondition 5 — recorded here as the baseline, not cleaned.)

```
$ shasum -a 256 meta/runnersOnedirDigests.json
8f53afdeef7e49bcc323219323422d2dfcce40b37659b1506ee3e29f3ad0fcbb  meta/runnersOnedirDigests.json

$ grep -c 'PENDING-CI-PUBLISH' meta/runnersOnedirDigests.json
7

$ shasum -a 256 meta/releaseTags.ts
f6c4e5006f6a4128a1f4cf7644292ad5e361898fde513c0f39edb1b6e6ff8895  meta/releaseTags.ts
```
Matches `34.16-LIVE-GATE.md` Precondition 7's recorded value exactly.

```
$ find public/bin build/bin -maxdepth 3 2>/dev/null | sort | shasum -a 256
43f6946923b07df605e6236f636a1d95cc414ada946cfcade4cc18eb021d44f5  -
```

```
$ date
Tue Aug 25 20:00:45 NZST 2026
```
Touchfile created under the scratchpad at the same moment (`touchfile-before`), for the later
`find -newer` sweep.

### git diff --stat for this task

This task's only change is this proof document itself (confirmed at commit time in Task 1's commit).

---

## Task 2: Real build run, branch verdict, blast-radius proof

### Run command + exit status (redirect form)

```
$ pnpm build-runners-onedir --arch=arm64 --runner=legendary > onedir-legendary.log 2>&1
$ echo $? > onedir-legendary.exit
```

`xxd onedir-legendary.exit`:
```
00000000: 300a                                     0.
```
`300a` = `"0\n"` — exit **0**.

### Full transcript

```
> gamelib@0.7.0 build-runners-onedir /Users/graysonmitchell/Projects/GameLib
> node meta/runTs.cjs --bundle --platform=node --target=node21 meta/buildRunnersOnedir.ts --arch=arm64 --runner=legendary

  ...6zm74fqyjf22r0000gn/T/gamelib-runts-aE0wmk/buildRunnersOnedir.cjs  17.9kb

⚡ Done in 14ms
Building legendary (0.21.0) as --onedir for arm64...
Built legendary -> .build-tools/runners-onedir/out/legendary_macOS_arm64_onedir.tar.gz
```

No error, no exception, no partial-progress abort. The script ran its full pipeline
(`cloneRepo` → `createVenv` → `installDependencies` → `installPyinstaller` →
`extractUpstreamPyinstallerCommand` → `deriveOnedirInvocation` → `runOnedirBuild` → `archiveDist`) and
reported success for the one requested runner.

### BRANCH VERDICT

**BRANCH VERDICT:** CLEAN BUILD

Exit 0, an archive written under `.build-tools/runners-onedir/out/legendary_macOS_arm64_onedir.tar.gz`,
and the extracted tree present at
`.build-tools/runners-onedir/src/legendary/legendary/dist/legendary`. `F-34.16-D`'s code half is
proven end to end for arm64: the real, freshly-cloned `legendary-gl/legendary@0.21.0` repo — whose
own workflow uses the `uv run --module PyInstaller` shape plan `34.16-07` added recognition for — was
extracted, derived into an onedir invocation, and built into a running binary, not merely parsed.

### `BUILD-MANIFEST-arm64.json` (written by this run, quoted verbatim)

```json
{
  "legendary": {
    "repo": "legendary-gl/legendary",
    "tag": "0.21.0",
    "upstreamPyinstallerLine": "uv run --module PyInstaller --onefile --name legendary ${{ steps.strip.outputs.option }} -i ../assets/windows_icon.ico cli.py",
    "upstreamWorkingDirectory": "legendary",
    "onedirCommand": "pyinstaller --onedir --name legendary -i ../assets/windows_icon.ico cli.py",
    "droppedExpressions": [
      "${{ steps.strip.outputs.option }}"
    ],
    "python3Version": "Python 3.14.6",
    "pyinstallerVersion": "6.20.0",
    "archiveSha256": "004beb7b855ed0eadda09f20165c82992fd5362d78a1c204c5e1c8e2955e3f9e",
    "fileCount": 115,
    "machoCount": 103
  },
  "runId": null
}
```

**Note (measured_facts, confirmed live):** this `.build-tools/runners-onedir/out/BUILD-MANIFEST-arm64.json`
and its sibling `SHA256SUMS-arm64` are overwritten with a legendary-only set by this run — a prior
local session's `gogdl`/`nile` entries (their tar.gz archives are still present in the same `out/`
directory, untouched) no longer appear in the JSON manifest. This is gitignored build-tooling state
under `.build-tools/` (confirmed against `.gitignore:51`, `/.build-tools/`), regenerated by CI on
every run, and harmless — recorded here because `<measured_facts>` predicted exactly this and it is
worth confirming rather than assuming.

**Derived onedir command, exactly as the manifest/transcript recorded it:**
```
pyinstaller --onedir --name legendary -i ../assets/windows_icon.ico cli.py
```
This is the `command` field `deriveOnedirInvocation` returns for display/manifest purposes (a
reconstructed string, `['pyinstaller', ...args].join(' ')` — not the literal spawn argv, which is
`pyinstallerPath(repoDir)` + `args` per T-34.16G-11/T-34.16G-01). Checked token-by-token: no `uv`,
no `run`, no `--module`, no `PyInstaller` (capitalized launcher form) token survives anywhere in this
string. The upstream line it was derived from (`upstreamPyinstallerLine` above) DID start with
`uv run --module PyInstaller` — confirming the widened extractor correctly recognised the
`uv-run-module` shape and correctly excluded launcher tokens from the derived onedir command.

### Post-extraction measurements (CLEAN BUILD)

```
$ DIST=".build-tools/runners-onedir/src/legendary/legendary/dist/legendary"

$ find "$DIST" | wc -l
141

$ find "$DIST" -type l | wc -l
4

$ ls -d "$DIST/Python.framework"
ls: .../dist/legendary/Python.framework: No such file or directory   <- NOT at the dist root

$ find "$DIST" -iname "Python.framework" -maxdepth 3
.../dist/legendary/_internal/Python.framework                        <- present, nested under _internal

$ ls -d "$DIST/_internal"
.../dist/legendary/_internal                                          <- present

$ "$DIST/legendary" --version
legendary version "0.21.0", codename "Lowlife"
$ echo $?
0

$ shasum -a 256 .build-tools/runners-onedir/out/legendary_macOS_arm64_onedir.tar.gz
004beb7b855ed0eadda09f20165c82992fd5362d78a1c204c5e1c8e2955e3f9e  legendary_macOS_arm64_onedir.tar.gz
```

Archive sha256 matches the manifest's `archiveSha256` field exactly. `--version` produced its
expected string and exited 0 — this is a running binary, not merely an archived directory that
happens to satisfy a file-count floor (the plan's own stated distinction: "A tree that exists is not
the same claim as a binary that runs").

**`Python.framework` placement note:** it exists, but nested under `_internal/`, not at the top level
of the onedir tree — a shape difference from a naive "does `<distDir>/Python.framework` exist" check.
Recorded because the closing verification/§11 x64 comparison this plan exists to give plan 34.16-11
a shape to compare against on x64 should look under `_internal/`, not only at the tree root.

### Blast-radius proof — six Task-1 snapshots re-measured and diffed

```
$ git status --porcelain > git-status-after.txt
$ diff git-status-before.txt git-status-after.txt
(empty diff -- byte-identical)

$ shasum -a 256 meta/runnersOnedirDigests.json
8f53afdeef7e49bcc323219323422d2dfcce40b37659b1506ee3e29f3ad0fcbb  meta/runnersOnedirDigests.json   <- unchanged

$ grep -c 'PENDING-CI-PUBLISH' meta/runnersOnedirDigests.json
7   <- unchanged (6 sentinels + the file's own _comment)

$ shasum -a 256 meta/releaseTags.ts
f6c4e5006f6a4128a1f4cf7644292ad5e361898fde513c0f39edb1b6e6ff8895  meta/releaseTags.ts   <- unchanged, equals LIVE-GATE Precondition 7's recorded value

$ find public/bin build/bin -maxdepth 3 2>/dev/null | sort | shasum -a 256
43f6946923b07df605e6236f636a1d95cc414ada946cfcade4cc18eb021d44f5  -   <- unchanged, byte-identical to the pre-run snapshot

$ find public/bin build/bin meta -newer touchfile-before -not -path '*/node_modules/*'
(empty -- no file under public/bin, build/bin, or meta is newer than the pre-run touchfile)
```

All five comparable snapshots (`git status`, `runnersOnedirDigests.json` sha256, sentinel count,
`releaseTags.ts` sha256, `public/bin`+`build/bin` tree shasum) diff empty against their Task-1
baseline. The `find -newer` sweep over `public/bin`, `build/bin` and `meta` produced no output. The
build wrote only under gitignored `.build-tools/` (confirmed by directly inspecting
`.build-tools/runners-onedir/{src,out}` above) — this is a measurement, not an inferred claim from
the script's own success report (`F-34.9-02`'s standing lesson).

`git status --porcelain` after this task's own edit to this proof document (below, post-commit) will
differ from the Task-1 baseline by exactly one path: this file itself.

### Forbidden-command check

```
$ grep -cE 'git (checkout|stash|reset)|git push|gh workflow run|pnpm (pin:runner-digests|download-helper-binaries|dist:mac|verify:runner-bundle)' onedir-legendary.log
0
```
No forbidden command appears anywhere in the build transcript captured by this task. No `git
checkout`, `git stash`, `git reset`, `git push`, `gh workflow run`, `pnpm pin:runner-digests`, `pnpm
download-helper-binaries`, `pnpm dist:mac`, or `pnpm verify:runner-bundle` was run by this plan.

### `git diff --name-only` for this task

Only this proof document (`34.16-ONEDIR-BUILD-PROOF.md`) is modified by this task's commit.
`meta/buildRunnersOnedir.ts` is unchanged by this plan.

## Header note for readers of this document

**This proves the arm64 leg only.** PyInstaller cannot cross-compile (34.9-CONTEXT.md constraint 1);
an x64 build is structurally impossible on this arm64 machine. `34.16-LIVE-GATE.md` §11's x64 branch
is untouched by this result and remains CI-only and unproven (T-34.16G-15).

## Summary for plan 34.16-11

- **Branch verdict:** CLEAN BUILD.
- **One-line reason:** the real, freshly-cloned `legendary-gl/legendary@0.21.0` repo built end to end
  through plan `34.16-07`'s widened `uv-run-module` extraction path on arm64 — exit 0, a running
  `legendary --version` binary, archive sha256 `004beb7b855ed0eadda09f20165c82992fd5362d78a1c204c5e1c8e2955e3f9e`.
- **Finding to append to `34.16-LIVE-GATE.md`'s `F-34.16-D` section:** none required — no failure was
  observed. `F-34.16-D`'s code half is now proven against the real upstream repo for arm64, not only
  against the captured fixture. The x64 leg remains open exactly as already recorded (CI-only,
  unproven, PyInstaller cannot cross-compile locally).
- **Routing:** no new finding to route. `RUN ATTEMPT 3` (the live-gate dispatch) can proceed with the
  arm64-leg risk retired; the x64 leg's risk is unchanged by this plan and stays whatever
  `34.16-LIVE-GATE.md` §11 already records for it.
