---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 11
type: execute
wave: 7
depends_on:
  - 34.16-07
  - 34.16-08
  - 34.16-09
  - 34.16-10
files_modified:
  - .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md
autonomous: true
gap_closure: true
closes_findings:
  - F-34.16-E
  - F-34.16-F
  - F-34.16-D
requirements:
  - REQ-34.9-02
  - REQ-34.9-03
  - REQ-34.9-04
decisions: [D-03, D-04]

must_haves:
  truths:
    - "F-34.16-E: §7 criterion 4 can be satisfied on a re-push and on an already-identical re-publish, and still aborts on a second line, on a D, and on a status that disagrees with whether main carries the file"
    - "F-34.16-F: §6 carries a ninth precondition asserting the dispatched remote ref equals HEAD, and that same fact is re-measured immediately before the dispatch rather than only at §6 time"
    - "F-34.16-F: no §6 expected value is a literal transcribed from a prior attempt -- every one is re-measured at attempt time and compared against a source that cannot go stale"
    - "F-34.16-F: skipping the §6 re-capture is prevented by a MECHANISM, not a stronger sentence -- scored items cite ledger slots, and an item scored against an empty slot is void"
    - "F-34.16-C carry-over: §8's command block carries --repo, so its 404 can no longer be confused with the criterion-1 failure it textually resembles"
    - "The findings' frontmatter statuses reflect what actually closed, and F-34.16-D stays blocking until RUN ATTEMPT 3's Item 2 supplies the live evidence no plan can supply"
    - "D-03/D-04 author/runner separation holds: this plan writes no result, no verdict, no session_dir, no per-item outcome, and every historical attempt record is proven byte-unchanged"
  artifacts:
    - path: ".planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md"
      provides: "an attempt-scoped precondition ledger, a ninth precondition, a re-push-aware criterion 4, a --repo-carrying §8, and updated finding statuses"
      contains: "ATTEMPT PRECONDITION LEDGER"
  key_links:
    - from: "34.16-LIVE-GATE.md §7 criterion 4"
      to: "34.16-LIVE-GATE.md §6 Precondition 6's main-carries-the-file measurement"
      via: "a ledger slot citation, so the expected diff-tree status is derived from a measured fact rather than hardcoded"
      pattern: "P6.MAIN-HAS-FILE"
    - from: "34.16-LIVE-GATE.md §8 criterion 0"
      to: "34.16-LIVE-GATE.md §6 Precondition 9"
      via: "the same remote-ref-equals-HEAD assertion, re-measured immediately before the dispatch"
      pattern: "P9.REF-EQ-HEAD"
---

<objective>
Repair `34.16-LIVE-GATE.md`'s own contract so `RUN ATTEMPT 3` can start legitimately: close
`F-34.16-E` and `F-34.16-F`, carry over the `F-34.16-C` amendment the gate itself asked for and never
made, and record what this gap cycle settled about `F-34.16-D`.

Purpose: attempt 2 recorded three deviations, two of which are defects in this document rather than
in anything it measures. Criterion 4, read literally, instructs an abort on the correct outcome of a
re-push. And §6's re-capture was skipped because it was an instruction in prose, which is exactly the
failure mode a mechanism prevents and a stronger sentence does not.

**This plan is an AUTHOR, never a RUNNER.** Per D-03/D-04 and the project's standing reference
(`.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md`, decision D-E),
the agent executing this plan is FORBIDDEN from: pushing to any remote, dispatching any workflow, and
running `pnpm pin:runner-digests`, `pnpm download-helper-binaries`, `pnpm dist:mac` or
`pnpm verify:runner-bundle`. It must not write, fabricate or pre-fill any RUN RECORD result, verdict,
`session_dir`, ledger value or per-item outcome. Every ledger cell it creates is literally `TBD`.

Output: an amended contract, updated finding statuses, and a proof that every historical result in
the document is byte-unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-CONTEXT.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-06-PLAN.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-07-SUMMARY.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-08-SUMMARY.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-09-SUMMARY.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-10-SUMMARY.md
@.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md

<measured_fact_that_changes_criterion_4>
<!-- Measured by the planner 2026-08-25, read-only, without fetching. RE-MEASURE before relying on
     it; the point is the CASE it proves reachable, not the value. -->

    git cat-file -p HEAD:.github/workflows/build-runners-onedir-macos.yml         | shasum -a 256
    git cat-file -p gamelib/main:.github/workflows/build-runners-onedir-macos.yml | shasum -a 256

both printed `e4bc5bbc2ab40966f3354ebdaa892bbde97fae56e15290ab671b0651c9ff26e9`. `gamelib/main`
ALREADY carries a byte-identical copy, because attempt 2's Item 1 re-pushed it after `F-34.16-B`'s
fix, and no plan in this gap cycle changes that workflow file.

**Consequence, and this is a THIRD case `F-34.16-E`'s stated amendment does not cover.** If the
workflow is unchanged when attempt 3 runs §7, the tree built from `MAIN_SHA` plus that blob is
IDENTICAL to `MAIN_SHA`'s tree, so
`git diff-tree --no-commit-id --name-status -r "$MAIN_SHA" "$NEW_COMMIT"` prints ZERO lines — not
`A`, not `M`. Criterion 4 as written demands "EXACTLY one line" and would abort Item 1 on the
correct, already-satisfied state. `F-34.16-E` names `A` and `M`; the empty case is the degenerate
member of the same family ("a status that disagrees with whether `main` already carries the file")
and the amendment must cover all three.
</measured_fact_that_changes_criterion_4>

<carried_over_amendment>
<!-- F-34.16-C is marked `status: resolved` because attempt 2's operator passed --repo by hand. The
     amendment its own text asked for was never made to the document. -->

`F-34.16-C` says verbatim: *"§8's command block should be amended to include the flag before the next
attempt."* §8's `Gesture:` line and its criterion-1 `gh run list` command still carry no `--repo`.
The hazard is specific and nasty: without it, `gh` resolves to `origin` (the unrelated Heroic
upstream) and returns a 404 that is textually near-identical to the one §8 criterion 1 scores as a
real failure, and means the opposite. Carrying this over is in scope for a plan whose subject is
repairing this contract.
</carried_over_amendment>
</context>

<tasks>

<task type="auto">
  <name>Task 1: F-34.16-F — make the §6 re-capture structurally unskippable, and remove every transcribed expected value</name>

  <read_first>
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md`
      §5 (Evidence capture), §6 (Preconditions, all eight), §13 (Verdict rule), the
      `### Finding F-34.16-A` "Precondition gap this exposes" paragraph, and the whole
      `### Finding F-34.16-F`
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` — decision
      D-E (a contract's author may never run it) and the seven-test Structural Reachability Review,
      which you must apply to every criterion you add or amend
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-06-PLAN.md`
      — how the runner plan consumes §6, so the slot names you invent are usable by it
  </read_first>

  <files>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</files>

  <action>
    Take a `cp` backup of the document first and record its `shasum -a 256`. Restore only by `cp` plus
    a `shasum -a 256` equality check if anything goes wrong. Never `git checkout -- <path>`
    (`.husky/post-checkout` runs `pnpm i && pnpm download-helper-binaries`, and the latter throws by
    design on the six `PENDING-CI-PUBLISH` sentinels), never `git stash`, never `git reset`.

    Add to §6, above the numbered list, an **ATTEMPT PRECONDITION LEDGER** template: a markdown table
    with one row per slot and the columns `Slot`, `What is measured`, `Command`, `Observed`,
    `Evidence file`. Every `Observed` cell in the TEMPLATE is the literal string `TBD`. Slots:

      `SESSION.DIR`, `P1.HEAD`, `P2.META`, `P3.BACKEND`, `P4.REMOTES`, `P5.STATUS`,
      `P6.SHA-WORKTREE`, `P6.SHA-MAIN`, `P6.MAIN-HAS-FILE`, `P7.RELEASETAGS`, `P8.NOCHECKOUT`,
      `P9.REF-EQ-HEAD`

    State, immediately above the table: an attempt's FIRST gesture is creating its own copy of this
    ledger, under a `### ATTEMPT <N> PRECONDITION LEDGER` heading in the RUN RECORD, filled with
    measured values and evidence filenames. Not a prior attempt's values, and not a copy of this
    template with `TBD` left in place.

    Make it a mechanism rather than an instruction, in three coupled ways:
      1. Scored items CITE slots. Rewrite §7 criterion 2 to compare against `P6.SHA-WORKTREE` by name
         instead of "Precondition 6". Task 2 adds the remaining citations.
      2. Add to §13 a VOID rule: an item scored while any slot it cites is absent or still reads
         `TBD` for that attempt is VOID — not PASS, not FAIL — and the attempt does not close. Note
         that attempt 2 would have been caught by this rule: §7 criterion 2 was unsatisfiable as
         written because no attempt-2 capture existed, and Item 1's PASS had to rest on a different
         measurement.
      3. Add to §5 that `$SESSION_DIR` is created fresh per attempt and its value recorded in that
         attempt's ledger row, never carried over. The frontmatter `session_dir` field names the
         MOST RECENT attempt's directory and is written by the runner only.

    Add **Precondition 9**, from `F-34.16-A`'s own "Precondition gap this exposes" paragraph:
      - `git rev-parse --abbrev-ref HEAD` equals the ref §8's gesture will dispatch. (A dispatch of a
        ref you are not on is the same class of failure wearing a different hat.)
      - `git fetch gamelib <that ref>` then `git rev-parse gamelib/<that ref>` equals
        `git rev-parse HEAD`.
      - State plainly why Precondition 1 cannot substitute: it verifies plans are COMMITTED, and a
        commit-only check structurally cannot see an unpushed branch. That is exactly how attempt 1
        dispatched a ref predating the entire phase.

    Remove every transcribed expected value from §6, because a literal copied from a prior attempt
    goes stale the moment the thing it describes changes — which is what `F-34.16-B`'s fix did to
    Precondition 6:
      - **Precondition 6** becomes a re-measurement PAIR plus a derived fact:
        `shasum -a 256 .github/workflows/build-runners-onedir-macos.yml` -> `P6.SHA-WORKTREE`;
        `git fetch gamelib main` then
        `git cat-file -p gamelib/main:.github/workflows/build-runners-onedir-macos.yml | shasum -a 256`
        -> `P6.SHA-MAIN`, recording explicitly whether `main` carries the file at all (a `cat-file`
        failure IS the answer "no", and is recorded as such rather than as an error) -> the
        `P6.MAIN-HAS-FILE` slot, whose value is one of `absent`, `present-identical`,
        `present-different`. Delete the sentence that compares against
        `34.16-02-SUMMARY.md`'s recorded value: that summary predates `F-34.16-B`'s fix and is stale
        by behaviour.
      - **Preconditions 2 and 3** become FAILURE-SET assertions rather than count literals: record the
        observed counts, and require that the failing SUITE set is exactly
        `meta/__tests__/genI18nGateScope.test.ts` for `Meta` and exactly
        `src/backend/storeManagers/steam/__tests__/decompressPool.test.ts` for `Backend` (3 failed
        tests), with any OTHER failure being a precondition failure. Say why: the passed-suite count
        legitimately moves whenever a plan adds a suite — plan 34.16-05 already moved Meta from 21 to
        22, attempt 1's operator had to reason past the plan's literal to score it, and this gap
        cycle adds more Meta suites. The failure SET is the durable property; the counts are a record.
        Keep the existing warning that `--selectProjects` is case-sensitive and fails OPEN.
      - **Precondition 7** keeps its literal-free form: capture `shasum -a 256 meta/releaseTags.ts`
        into `P7.RELEASETAGS` at attempt time and require the same value at the end of the gate. This
        one is a within-attempt baseline, which is the correct use of a recorded value; say so, so a
        later editor does not "fix" it too.

    Then apply the seven-test Structural Reachability Review from the standing reference to every
    criterion you added or amended here, and to the ledger mechanism itself as an evidence-capture
    instruction. Record the table — one row per item with a REACHABLE / IMPOSSIBLE / CONDITIONAL
    verdict and its evidence — in this plan's SUMMARY. Test 5's pairing pass is not optional: name how
    many state-mutating × evidence-bearing pairs you considered and which you flagged. In particular
    consider whether requiring a `git fetch gamelib main` inside Precondition 6 can invalidate §7
    criterion 1's requirement that `MAIN_SHA` be resolved FRESH immediately before the tree is built.

    Write NOTHING below the `## RUN RECORD — TBD` divider in this task.
  </action>

  <verify>
    <automated>grep -c 'ATTEMPT PRECONDITION LEDGER' .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</automated>
  </verify>

  <acceptance_criteria>
    - All twelve slot names appear in §6's ledger template:
      `for s in SESSION.DIR P1.HEAD P2.META P3.BACKEND P4.REMOTES P5.STATUS P6.SHA-WORKTREE P6.SHA-MAIN P6.MAIN-HAS-FILE P7.RELEASETAGS P8.NOCHECKOUT P9.REF-EQ-HEAD; do grep -c "$s" <doc>; done`
      — every count >= 1. RED specimen: the same loop against
      `git show HEAD:.planning/phases/34.16-.../34.16-LIVE-GATE.md` prints `0` for all twelve.
    - The template is unfilled: every `Observed` cell in the §6 template table is the literal `TBD`.
      Assert by extracting the template table's rows and confirming each contains `TBD`, and quote
      the table.
    - Precondition 9 exists and asserts BOTH the abbrev-ref check and the
      `git rev-parse gamelib/<ref>` == `git rev-parse HEAD` check, and states why Precondition 1
      cannot substitute. `grep -c 'rev-parse gamelib/' <doc>` >= 1; RED specimen against
      `git show HEAD:<doc>` prints `0`.
    - No transcribed expected value survives in §6. Behavioural assertion with a RED specimen: the
      superseded workflow sha must not appear anywhere in §6.
      `awk '/^## 6\. Preconditions/,/^## 7\./' <doc> | grep -c '60f4f1e6'` prints `0` and
      `awk '/^## 6\. Preconditions/,/^## 7\./' <doc> | grep -c 'e4bc5bbc'` prints `0`.
      Confirm the same extraction over `git show HEAD:<doc>` and record what it printed, so the
      change is attributable rather than vacuous.
    - Preconditions 2 and 3 name their failing SUITE paths and no longer state a passed-suite count as
      the bar. `awk '/^## 6\. Preconditions/,/^## 7\./' <doc> | grep -c 'genI18nGateScope.test.ts'`
      and the same for `decompressPool.test.ts` each print >= 1, and neither precondition contains the
      phrase `21 passed`.
    - §13 carries the VOID rule and names attempt 2 as the case it would have caught:
      `grep -c 'VOID' <doc>` >= 1.
    - §7 criterion 2 cites `P6.SHA-WORKTREE` by slot name.
    - The Structural Reachability Review table is in the SUMMARY with one row per added/amended item,
      each carrying a verdict and a cited evidence file:line or command. The Test 5 pairing pass
      states how many pairs were considered and which were flagged, including the
      `git fetch gamelib main` × §7-criterion-1-freshness pair by name.
    - Nothing below the divider changed in this task: extract from `^## RUN RECORD` to end of file and
      `shasum -a 256` it before and after — the values are equal. Quote both.
    - No forbidden command was run: a single
      `grep -cE 'git (checkout|stash|reset)|git push|gh workflow run|pnpm (pin:runner-digests|download-helper-binaries|dist:mac|verify:runner-bundle)'`
      over the task transcript prints `0`. Note `git fetch` is written INTO the contract for the human
      runner and is not executed by this task.
  </acceptance_criteria>

  <done>
    §6 carries an unfilled attempt-scoped ledger, a ninth precondition covering the unpushed-ref gap,
    and zero transcribed expected values; §13 voids any item scored against an empty slot; and the
    whole change is proven not to have touched a single byte below the RUN RECORD divider.
  </done>
</task>

<task type="auto">
  <name>Task 2: F-34.16-E — amend §7 criterion 4 for all three outcomes, and carry over the §8 --repo amendment</name>

  <read_first>
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md`
      §7 in full (all six criteria plus the non-fast-forward note), §8 in full (the Gesture line and
      all seven criteria), `### Finding F-34.16-E`, `### Finding F-34.16-C`
    - Task 1's amended §6, for the exact slot names to cite
    - `.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` — the seven
      tests, applied again to these amendments
  </read_first>

  <files>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</files>

  <action>
    Rewrite §7 criterion 4 so its expected `git diff-tree` output is DERIVED from the
    `P6.MAIN-HAS-FILE` slot rather than hardcoded. Three cases, each named, each with its own
    disposition:

      - `P6.MAIN-HAS-FILE = absent`  -> expect exactly one line, status `A`, that one path. First
        publish.
      - `P6.MAIN-HAS-FILE = present-different` -> expect exactly one line, status `M`, that one path.
        A re-push after the workflow changed. `A` here would mean the tree was built from the wrong
        base and ABORTS.
      - `P6.MAIN-HAS-FILE = present-identical` -> expect ZERO lines, and the push is a NO-OP
        re-publish. Spell out the disposition explicitly, because it is reachable today: `main`
        already carries a byte-identical blob, so criterion 6's byte-identity diff is already
        satisfied and is the proof. State whether the operator pushes anyway (a commit with an
        identical tree) or records the no-op and skips the push, and make criterion 6 satisfiable
        either way. Do not leave this case to the runner's judgement mid-gate — that is how a
        criterion "read literally" produces the wrong abort.

    ABORT conditions, unchanged in spirit and now stated once for all three cases: a second line, any
    path other than `.github/workflows/build-runners-onedir-macos.yml`, a `D` status, or a status
    that disagrees with `P6.MAIN-HAS-FILE`. Say explicitly that the criterion's INTENT is "exactly
    that one path, no collateral change" and that the status is evidence about the base, not the
    subject of the test.

    Record, inside the criterion, the dated note that attempt 2 observed one line with status `M` and
    recorded it as a deviation rather than aborting, because the criterion's intent was satisfied.
    That is a statement about the CONTRACT's history and belongs in §7; do not restate or alter
    attempt 2's own record below the divider.

    Then carry over `F-34.16-C`'s amendment, which the gate asked for and never made: add
    `--repo grayson-mitchell/GameLib` to §8's `Gesture:` command and to the `gh run list` command in
    §8 criterion 1, and add a one-line note under §8 stating that EVERY `gh` invocation in this gate
    carries `--repo` because without it `gh` resolves to `origin` (the Heroic upstream) and its 404 is
    textually near-identical to the one criterion 1 scores as a real failure and means the opposite.
    Cite `F-34.16-C`.

    Add §8 **criterion 0**, re-measuring `P9.REF-EQ-HEAD` immediately before the dispatch: `git fetch`
    the feature ref and assert `git rev-parse gamelib/<ref>` equals `git rev-parse HEAD` at that
    moment. Say why a §6-time check is insufficient on its own: an operator can commit or be handed
    commits between §6 and the dispatch, and `workflow_dispatch` executes the workflow file from the
    REF, not from the local tree. Number it `0` deliberately so it reads as a gate on the gesture
    rather than as evidence about its result.

    Apply the seven-test Structural Reachability Review to criterion 4's three cases, to §8 criterion
    0, and to the `--repo` amendment. Record the table in the SUMMARY. Test 4 (absence-observability)
    applies directly to the `present-identical` zero-line case: state what the PRESENCE case would
    look like so "zero lines" is a falsifiable observation and not an unfalsifiable structural
    guarantee.

    Write NOTHING below the `## RUN RECORD — TBD` divider in this task. Restore by `cp` + `shasum`
    only; never `git checkout`, `git stash` or `git reset`.
  </action>

  <verify>
    <automated>grep -c 'present-identical' .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</automated>
  </verify>

  <acceptance_criteria>
    - §7 criterion 4 names all three cases. Extract §7 with
      `awk '/^## 7\. Scored Item 1/,/^## 8\./' <doc>` and assert `grep -c` >= 1 for each of `absent`,
      `present-different`, `present-identical`, and >= 1 for `P6.MAIN-HAS-FILE`. RED specimen: the
      same extraction over `git show HEAD:<doc>` prints `0` for all four.
    - The old unconditional abort is gone: the extracted §7 no longer contains a sentence requiring an
      `A` status unconditionally. Assert
      `awk '/^## 7\. Scored Item 1/,/^## 8\./' <doc> | grep -c 'any `M`/`D` status'` prints `0`, and
      record that the same command over `git show HEAD:<doc>` prints `1`.
    - The zero-line case has an explicit disposition: the extracted §7 states what the operator does
      when `main` already carries an identical blob, and states how criterion 6 is satisfied in that
      case. Quote the paragraph. A criterion that merely mentions the case without dispositioning it
      fails this bar.
    - §8 carries `--repo` in both its Gesture line and its criterion-1 `gh run list` command:
      `awk '/^## 8\. Scored Item 2/,/^## 9\./' <doc> | grep -c '\-\-repo grayson-mitchell/GameLib'`
      prints `2` or more. RED specimen: the same extraction over `git show HEAD:<doc>` prints `0`.
    - §8 criterion 0 exists, cites `P9.REF-EQ-HEAD`, and states why the §6-time check alone is
      insufficient. `awk '/^## 8\. Scored Item 2/,/^## 9\./' <doc> | grep -c 'P9.REF-EQ-HEAD'` >= 1.
    - The Structural Reachability Review table for these amendments is in the SUMMARY, with the
      Test 4 absence-observability row for the zero-line case explicitly present.
    - Nothing below the divider changed in this task: the `^## RUN RECORD`-to-EOF extract's
      `shasum -a 256` is unchanged across the task. Quote before and after.
    - No forbidden command was run: the single combined `grep -cE` over the task transcript prints `0`.
  </acceptance_criteria>

  <done>
    Criterion 4 derives its expected status from a measured fact and covers first publish, re-push
    after change, and already-identical re-publish, with one abort rule stated for all three; §8
    carries `--repo` and a pre-dispatch ref check; and no historical record was touched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update the findings' statuses, append the dated settlements, and prove every historical result byte-unchanged</name>

  <read_first>
    - the four SUMMARYs from plans `34.16-07`, `-08`, `-09`, `-10` — the ONLY source for what actually
      closed. Do not infer an outcome you did not read.
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md`
      frontmatter `findings:` list, and the three finding sections `F-34.16-D`, `F-34.16-E`,
      `F-34.16-F`
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-ONEDIR-BUILD-PROOF.md`
      and `34.16-PIN-ARCHAEOLOGY.md` — the two artifacts whose verdicts are being recorded
  </read_first>

  <files>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</files>

  <action>
    Update ONLY these frontmatter fields, and only inside the `findings:` list:

      - `F-34.16-E`: `status: open` -> `status: fixed`, add `fixed_by:` naming this plan's commit, and
        extend its `summary:` with one clause recording that the amendment covers three cases because
        a third (already-identical, zero-line) was found reachable during the fix.
      - `F-34.16-F`: `status: open` -> `status: fixed`, add `fixed_by:`, and extend its `summary:` to
        name the mechanism that replaced the prose instruction (the attempt-scoped ledger, the slot
        citations, and §13's VOID rule).
      - `F-34.16-D`: `status: open` -> `status: fixed`, add `fixed_by:` naming the gap-cycle commits,
        and **leave `blocking: true` as it is**. Extend its `summary:` to say plainly that the code
        half is fixed and tripwired, and that the blocking condition is discharged only by RUN ATTEMPT
        3's Scored Item 2 — live evidence no plan in this cycle can produce. Do not mark it resolved.
        A "fixed" that has never built in CI is precisely the green check this project keeps getting
        burned by.
      - `F-34.16-C`: leave `status: resolved` and do NOT rewrite its summary; add nothing to it beyond
        what Task 2 already put in §8. Its status was always about the operator workaround.

    Change NO other frontmatter field. `verdict`, `run_date`, `items_total`, `items_passed`,
    `items_failed`, `items_carried_forward`, `items_not_attempted`, `runner`, `session_dir`,
    `launch_ordinals_used`, `run_by`, the top-level `status`, `blocking`, `created` and
    `supersedes_run` all stay exactly as the runner left them. Writing any of them is a D-03/D-E
    violation regardless of what value you believe is correct.

    Append to the ends of the `F-34.16-D`, `F-34.16-E` and `F-34.16-F` sections a clearly-labelled,
    dated block beginning `**SETTLED 2026-08-__ by the 34.16 gap cycle (plans 07-11):**`. These are
    the only edits permitted below the `## RUN RECORD` divider, and they are amendments to FINDINGS,
    never to results. Content:

      - Under `F-34.16-D`: the extractor widening with its per-shape RED/GREEN evidence pointer, the
        two tripwire layers with their stated detection latencies, the local build proof's BRANCH
        VERDICT from `34.16-ONEDIR-BUILD-PROOF.md`, and the sub-question's SETTLED VERDICT from
        `34.16-PIN-ARCHAEOLOGY.md` — including, if the archaeology found the finding's own stated
        conclusion unsupported, a plain sentence saying so. Do not silently drop it.
      - Under `F-34.16-E`: the three cases, and the fact that the third was discovered by measuring
        `gamelib/main`'s current blob rather than reasoning from the finding's text.
      - Under `F-34.16-F`: the mechanism, and the honest statement of what it does NOT prevent — a
        runner can still fill a ledger cell with a wrong value; what the mechanism removes is the
        ability to score an item while its cited slot is EMPTY.

    Then rewrite `### Required before RUN ATTEMPT 3` in place — it is a forward-looking checklist, not
    a result — marking items 1-4 as DONE with their closing artifacts named, leaving item 5 (push,
    re-push if the workflow changed, re-dispatch with `--repo`) OPEN and human-owned, and adding: the
    workflow file was NOT changed by this gap cycle, so `P6.MAIN-HAS-FILE` is expected to measure
    `present-identical` and §7 criterion 4's third case is the one to expect — measure it, do not
    assume it.

    Finally, prove the blast radius. Before any edit in this task, extract and `shasum -a 256` these
    regions from the pre-edit file, and re-measure each after:
      - `awk '/^## Preconditions/,/^## RUN ATTEMPT 1/'` — attempt 1's recorded precondition results.
        These stay byte-identical even though `F-34.16-F` says they are stale for attempt 2: they are
        a true record of what attempt 1 measured, and the repair is structural, not retroactive.
      - `awk '/^## RUN ATTEMPT 1/,/^### Finding/'` — attempt 1's narrative and its Item 1/Item 2
        records.
      - `awk '/^## RUN ATTEMPT 2/,/^### Finding/'` — attempt 2's narrative, its `### Score` table and
        its Item 1/Item 2 records.
    Record all six values. Restore by `cp` + `shasum` if any differs; never `git checkout`,
    `git stash` or `git reset`.
  </action>

  <verify>
    <automated>grep -cE '^\s+status: (fixed|open|resolved)' .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</automated>
  </verify>

  <acceptance_criteria>
    - Frontmatter: `F-34.16-D`, `F-34.16-E` and `F-34.16-F` each read `status: fixed` and each carry a
      `fixed_by:` value. `F-34.16-A` stays `resolved`, `F-34.16-B` stays `fixed`, `F-34.16-C` stays
      `resolved`. Quote the whole `findings:` block before and after.
    - `F-34.16-D` still reads `blocking: true`. A gap cycle that marks it non-blocking without a
      passing CI build has manufactured the exact false record this project keeps finding.
    - The protected frontmatter fields are byte-unchanged. Assert each by an exact-line grep before
      and after: `verdict:`, `run_date:`, `items_total:`, `items_passed:`, `items_failed:`,
      `items_carried_forward:`, `items_not_attempted:`, `runner:`, `session_dir:`,
      `launch_ordinals_used:`, `run_by:`, the top-level `status:`, `blocking:`, `created:`,
      `supersedes_run:`. Quote the before/after pairs.
    - Three region hashes recorded before and after, all three EQUAL:
      `## Preconditions` -> `## RUN ATTEMPT 1`; `## RUN ATTEMPT 1` -> first `### Finding`;
      `## RUN ATTEMPT 2` -> first `### Finding`. Quote all six values. This is the measurable form of
      D-03/D-E author/runner separation.
    - Exactly three `**SETTLED ` blocks exist, one per finding:
      `grep -c '^\*\*SETTLED ' <doc>` prints `3`.
    - Every claim inside a SETTLED block traces to a named artifact: each block cites at least one of
      `34.16-07-SUMMARY.md`, `34.16-08-SUMMARY.md`, `34.16-ONEDIR-BUILD-PROOF.md`,
      `34.16-PIN-ARCHAEOLOGY.md`. No SETTLED block asserts an outcome the executor did not read in a
      SUMMARY or proof document.
    - The `F-34.16-D` SETTLED block states the local build's BRANCH VERDICT verbatim and states that
      it proves arm64 only. If that verdict was not `CLEAN BUILD`, the block says so plainly and names
      where the residual routes.
    - `### Required before RUN ATTEMPT 3` marks items 1-4 DONE with named artifacts, leaves item 5
      OPEN, and contains the `present-identical` expectation with the word "measure" rather than
      "assume". `grep -c 'present-identical' <doc>` >= 2 (the §7 amendment plus this note).
    - No result was fabricated: `grep -cE 'RUN ATTEMPT 3' <doc>` shows only forward-looking references,
      and there is no `## RUN ATTEMPT 3` heading, no attempt-3 ledger with a filled cell, and no
      attempt-3 verdict anywhere. Assert `grep -c '^## RUN ATTEMPT 3' <doc>` prints `0`.
    - `npx prettier --check <doc>` — record the result. `.planning` is in `.prettierignore`, so
      confirm the path is ignored rather than reformatting it.
    - No forbidden command was run: the single combined `grep -cE` over the task transcript prints `0`.
  </acceptance_criteria>

  <done>
    The three findings carry accurate statuses with `F-34.16-D` still blocking on live evidence, each
    has a dated settlement citing the artifact that produced it, the attempt-3 checklist reflects what
    is actually left, and all three historical result regions plus fifteen protected frontmatter
    fields are proven byte-unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring agent -> a blocking gate contract | This plan edits the document that decides whether six requirements close. An author that writes a result, or weakens a criterion, corrupts the only record of what was proven. |
| contract text -> a human runner's actions | Every sentence here is executed by a person under time pressure. A criterion that instructs the wrong abort — which is exactly `F-34.16-E` — is an availability defect with a real cost in macOS runner minutes and attempts. |
| gap-cycle SUMMARYs -> settlement text | The settlements restate outcomes produced by other plans. A restatement can be wrong while the record it summarises is right, usually by dropping a qualifier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.16G-20 | Repudiation | an author writing a runner's result | mitigate | Three region hashes (`## Preconditions`, attempt 1's narrative, attempt 2's narrative) and fifteen exact-line frontmatter greps measured before and after. Permitted edits are enumerated positively; everything else is asserted unchanged. `grep -c '^## RUN ATTEMPT 3'` = 0 proves no attempt was fabricated. |
| T-34.16G-21 | Tampering | a settlement dropping a qualifier from the record it summarises | mitigate | Every SETTLED block must cite the artifact it draws from, must quote the BRANCH VERDICT verbatim rather than paraphrase it, and must state the arm64-only scope. If the archaeology contradicted the finding's own conclusion, saying so is required rather than optional. |
| T-34.16G-22 | Denial of Service | a criterion that aborts a correct outcome | mitigate | Criterion 4 now derives its expectation from the measured `P6.MAIN-HAS-FILE` slot and disposes of all three reachable outcomes, including the zero-line case the planner measured as the one attempt 3 will actually hit. The Structural Reachability Review is applied to each amended criterion before the contract is published, per decision D-E. |
| T-34.16G-23 | Spoofing | `gh` resolving to the Heroic upstream and returning a misleading 404 | mitigate | `--repo grayson-mitchell/GameLib` added to §8's gesture and criterion-1 command, with a note stating why the upstream 404 is textually near-identical to a real criterion-1 failure and means the opposite. Carried over from `F-34.16-C`'s own unactioned request. |
| T-34.16G-24 | Elevation of Privilege | an authoring agent pushing or dispatching | mitigate | Stated as a prohibition in the objective and asserted per task by a combined `grep -cE` over the transcript covering `git push`, `gh workflow run`, `git checkout/stash/reset`, and the four forbidden `pnpm` scripts, each printing `0`. |
| T-34.16G-25 | Tampering | marking a finding resolved without live evidence | mitigate | `F-34.16-D` keeps `blocking: true` and `status: fixed` (never `resolved`), with the summary naming RUN ATTEMPT 3 Scored Item 2 as the only thing that can discharge it. Asserted as an acceptance criterion. |
| T-34.16G-SC | Tampering | npm/pip/cargo installs | n/a for this plan | This plan edits one markdown file and installs nothing. |
</threat_model>

<verification>
- Twelve ledger slots present and unfilled; Precondition 9 present; no transcribed sha in §6.
- §7 criterion 4 disposes of `absent` / `present-different` / `present-identical`; the unconditional
  `A` abort is gone, with a RED specimen from `git show HEAD:<doc>` for each assertion.
- §8 carries `--repo` twice and a criterion 0 citing `P9.REF-EQ-HEAD`.
- Three finding statuses updated; `F-34.16-D` still `blocking: true`; fifteen protected frontmatter
  lines and three historical regions proven byte-unchanged.
- Structural Reachability Review tables recorded in the SUMMARY for every added/amended criterion,
  including Test 5's pairing pass with a stated count.
</verification>

<success_criteria>
`RUN ATTEMPT 3` can start legitimately: its preconditions are re-measured rather than inherited, its
ninth precondition closes the unpushed-ref gap that killed attempt 1, its §7 criterion 4 no longer
instructs an abort on the outcome attempt 3 will actually produce, every `gh` call carries `--repo`,
and every result attempts 1 and 2 recorded is provably untouched.
</success_criteria>

<output>
Create `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-11-SUMMARY.md` when done.

Include the Structural Reachability Review tables, the three region hashes before and after, the
fifteen protected frontmatter before/after pairs, and a one-line statement of what the human owes
before RUN ATTEMPT 3 — which is item 5 and nothing else.
</output>
