---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 10
subsystem: infra
tags: [ci, macos-onedir, meta-build, provenance, gap-closure]

# Dependency graph
requires:
  - phase: 34.16-07
    provides: "widened extractUpstreamPyinstallerCommand and the 34.9-era provenance comments this plan re-grades"
  - phase: 34.16-09
    provides: "confirmation the current pin (legendary-gl/legendary@0.21.0) builds clean end to end on arm64, ruling out a live re-pin as this plan's disposition"
provides:
  - "F-34.16-D's open sub-question settled as a dated, re-derivable fact: PIN RESOLVED — the finding's probe queried the post-bump repo (legendary-gl/legendary) for the pre-bump tag (0.20.43), which resolves cleanly in the pre-bump repo (Heroic-Games-Launcher/legendary)"
  - "meta/buildRunnersOnedir.ts's provenance comments qualified to name the repo/tag/commit their validation actually covers, rather than reading as if validated against the currently pinned project"
  - "34.16-PIN-ARCHAEOLOGY.md: the reusable evidence document for plan 34.16-11's F-34.16-D closure note"
affects: [34.16-11]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Provenance-sentence census: grep for the discovery phrases ('found live', 'during this plan's execution') across meta/, disposition every hit individually (qualified / corrected / marked unverified / no change needed), and prove none is silently left contradicting the settled verdict"

key-files:
  created:
    - .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-PIN-ARCHAEOLOGY.md
  modified:
    - meta/buildRunnersOnedir.ts

key-decisions:
  - "Verdict is PIN RESOLVED, not PIN NEVER RESOLVED or INDETERMINATE — commit 0034ad265's own message ('legendary moved to legendary-gl/legendary') and diff prove it changed the pinned REPOSITORY in the same commit as the version bump; the tag 0.20.43 resolves cleanly (gh api, exit 0, real commit f6fb3118...) in the pre-bump repo Heroic-Games-Launcher/legendary. No pin, repo, or version was changed by this plan."
  - "The finding's own stated conclusion ('deleted tag or digit transposition') graded as unsupported and recorded plainly, per the plan's explicit instruction not to quietly drop a wrong conclusion — its own timeline table already named the repo move but did not apply it to the sub-question."
  - "Three of six provenance sentences in meta/buildRunnersOnedir.ts needed qualification (the header, the duplicate-workflow-file note, the working-directory discovery note); three needed none (two gogdl-scoped, one a platform-generic spawn() fact) because gogdl's repo/tag pairing is untouched by 0034ad265 and the spawn() fact is not repo-scoped."

requirements-completed: [REQ-34.9-02]

# Metrics
duration: ~35min
completed: 2026-08-25
---

# Phase 34.16 Plan 10: Settle F-34.16-D's `0.20.43` pin sub-question

**Re-ran all four planner probes plus git log/show and both tag-namespace listings against the live GitHub API: commit `0034ad265` changed legendary's pinned REPOSITORY (`Heroic-Games-Launcher/legendary` -> `legendary-gl/legendary`) in the same commit as its version bump, so the finding's probe queried the wrong namespace for the old tag — `0.20.43` resolves cleanly (real commit `f6fb3118...`) in the repo that was actually pinned. SETTLED VERDICT: PIN RESOLVED. Qualified the three provenance sentences in `meta/buildRunnersOnedir.ts` that read as if validated against the current pin; left three others unchanged where the disposition genuinely requires no edit.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-25T08:02:00Z (approx, first probe)
- **Completed:** 2026-08-25T08:37:59Z
- **Tasks:** 2/2 completed
- **Files modified:** 2 (1 new evidence document, 1 comment-only source edit)

## Accomplishments

- Re-ran all four `<planner_probe>` commands verbatim; all four reproduced exactly. Added the two `git log`/`git show` commands and both tag-namespace listings the plan required beyond the planner's probes.
- Established, by quoting commit `0034ad265`'s own message and diff, that it changed legendary's pinned repository AND version in one commit ("legendary moved to legendary-gl/legendary") — the repo-slug diff (`e1c9f6174` vs. HEAD) confirms `Heroic-Games-Launcher/legendary` -> `legendary-gl/legendary`, while gogdl and nile's repo slugs are untouched.
- Confirmed via `gh api` that `0.20.43` resolves to a real commit (`f6fb31181594f564e822bcd9f0905aabaf7ad45f`) in `Heroic-Games-Launcher/legendary` and 404s in both `legendary-gl/legendary` and `derrod/legendary` — settling the sub-question as **PIN RESOLVED — FINDING'S PREMISE WAS A REPO MISMATCH**, not a deleted tag or a digit transposition.
- Graded the finding's own stated conclusion explicitly: **unsupported**. Its own timeline table already named the repo move but the finding's "deleted tag or digit transposition" reasoning never connected that fact to the tag-lookup 404.
- Enumerated all six provenance sentences describing the extraction contract under `meta/` (a repo-wide search outside `meta/` found no other in-source hits — two unrelated `Rule 1` hits in `src/backend/sidecar/` were checked and confirmed off-topic) and dispositioned each individually: three qualified (header, duplicate-workflow-file note, working-directory discovery), two left unchanged (gogdl-scoped, repo/tag untouched by the bump), one left unchanged (platform-generic `spawn()` fact).
- Confirmed the qualified duplicate-workflow-file shape is not currently exercised by the pinned repo: `legendary-gl/legendary@0.21.0`'s captured fixture set has exactly one pyinstaller invocation (in `build-base.yml`), not the two (`python.yml` + `release.yml`) the original 34.9-era discovery described.
- Confirmed the qualified working-directory fact IS still true of the current pin: the captured `build-base.yml:44` still declares `working-directory: legendary` on the pyinstaller step.

## Task Commits

1. **Task 1: Settle the sub-question, write the SETTLED VERDICT** — `9345b78b8` (docs)
2. **Task 2: Qualify the extraction-contract provenance comments** — `c22ea5f73` (docs, `meta/buildRunnersOnedir.ts`) + `1f8f7061b` (docs, post-edit census appended to `34.16-PIN-ARCHAEOLOGY.md`)

_No separate plan-metadata commit for STATE.md/ROADMAP.md was created by this executor — those files are explicitly out of scope for this session per the orchestrator's critical repo guardrails (the same scope exclusion 34.16-07's and 34.16-09's executors recorded)._

## Files Created/Modified

- `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-PIN-ARCHAEOLOGY.md` — all four planner probes plus `git log`/`git show`/tag-listing commands with verbatim output and exit status; the repo-slug diff; the three corroborating in-repo citations graded for what they do and don't prove; the `**SETTLED VERDICT:** PIN RESOLVED` line (body and frontmatter); the finding's-own-conclusion grading; the pre-edit and post-edit provenance-sentence census with per-sentence disposition; the `meta/releaseTags.ts` shasum proof; the forbidden-command check; the record for plan 34.16-11.
- `meta/buildRunnersOnedir.ts` — three comment blocks added (no code, signature, or `RELEASE_TAGS` changes): a 13-line `PROVENANCE` block after the header's run-value-folding note (lines ~130-142), an 11-line `PROVENANCE` block after the duplicate-workflow-file note (lines ~384-394), and a 7-line extension of the working-directory discovery note (lines ~668-674). Each names the repo/tag/commit the original discovery actually covers and states whether/how it carries over to the currently pinned `legendary-gl/legendary@0.21.0`.

## Evidence for plan 34.16-11 (F-34.16-D LIVE-GATE record)

- **Verdict:** PIN RESOLVED — FINDING'S PREMISE WAS A REPO MISMATCH.
- **One-paragraph basis:** commit `0034ad265` changed legendary's pinned repository from `Heroic-Games-Launcher/legendary` to `legendary-gl/legendary` in the same commit as its version bump (own commit message: "legendary moved to legendary-gl/legendary"). The finding's probe queried the new repo for the old tag; the old tag resolves cleanly in the old repo (`gh api repos/Heroic-Games-Launcher/legendary/git/ref/tags/0.20.43` -> real commit `f6fb3118...`), so the 34.9-era provenance comment is TRUE, but the validation it describes does not carry over to the currently pinned project, which is a genuinely different repository, not merely a newer version.
- **Was the finding's stated conclusion supported?** No — neither "deleted tag" nor "digit transposition of `0.20.34`" is supported; the tag was never deleted and is not a typo. The correct explanation (repo move) was already present in the finding's own timeline table but not applied to the sub-question.
- **Routed follow-up:** none required — no pin change, no repo change, no version change is warranted. The comment repair applied in Task 2 is the complete closure of the record-accuracy half of `F-34.16-D`'s open sub-question.

## Verification Results

- `grep -cE '^\*\*SETTLED VERDICT:\*\* (PIN RESOLVED|PIN NEVER RESOLVED|INDETERMINATE)' .../34.16-PIN-ARCHAEOLOGY.md` -> `1`.
- `grep -c 'Heroic-Games-Launcher/legendary\|legendary-gl/legendary' meta/buildRunnersOnedir.ts` -> `8` (was `1` before this plan).
- `grep -c '0034ad265' meta/buildRunnersOnedir.ts` -> `1`; the same grep against `git show HEAD~2:meta/buildRunnersOnedir.ts` (pre-edit) -> `0` (RED specimen satisfied).
- `git diff -U0 -- meta/buildRunnersOnedir.ts` (task 2 commit) — every added/removed line is a `//` comment line; no code, signature, or `RELEASE_TAGS` change.
- `pnpm codecheck` -> exit `0`, no output.
- `pnpm jest --selectProjects Meta meta/__tests__/buildRunnersOnedir.test.ts meta/__tests__/runnerBuildInvocations.test.ts` -> `Test Suites: 1 failed, 23 passed, 24 total` / `Tests: 1 failed, 1 skipped, 575 passed, 577 total`. The one failed suite is `meta/__tests__/genI18nGateScope.test.ts` — the pre-existing, documented, concurrent-session baseline failure named in this plan's guardrails, not caused by this plan. Both target files (`buildRunnersOnedir.test.ts`, `runnerBuildInvocations.test.ts`) pass fully.
- `npx eslint -f json meta/buildRunnersOnedir.ts` -> `0` severity-2 findings.
- `npx prettier --check meta/buildRunnersOnedir.ts` -> `All matched files use Prettier code style!`, measured in place.
- `shasum -a 256 meta/releaseTags.ts` -> `f6c4e5006f6a4128a1f4cf7644292ad5e361898fde513c0f39edb1b6e6ff8895`, matching the `34.16-LIVE-GATE.md` Precondition 7 value, both before Task 1 and after Task 2.
- Forbidden-command grep over this plan's own transcript (`git (checkout|stash|reset)|git push|gh workflow run|pnpm (pin:runner-digests|download-helper-binaries|dist:mac|verify:runner-bundle)`) -> `0`. Every `gh api` call was a read-only GET; every `git` command used was `log`/`show`/`status`/`diff`/`add`/`commit --only`.

## Deviations from Plan

### Auto-fixed Issues

None — no bugs, missing functionality, or blocking issues were encountered.

### Other deviations

**1. [Task ordering] Split Task 2 into two commits instead of one**

- **Found during:** Task 2, after committing the `meta/buildRunnersOnedir.ts` comment edit.
- **Detail:** Task 2's own acceptance criteria require the post-edit provenance census (enumeration greps re-run after the edit, quoted next to the pre-edit census) to appear in `34.16-PIN-ARCHAEOLOGY.md`. Writing that section required knowing the edit's final line numbers, so it could only be added after the code edit landed. Rather than holding the code-edit commit unstaged while composing the doc addendum, committed the code edit first (`c22ea5f73`) and the doc addendum second (`1f8f7061b`), both still within Task 2's declared file scope (`meta/buildRunnersOnedir.ts`, `34.16-PIN-ARCHAEOLOGY.md`).
- **Resolution:** Both commits are atomic and individually verifiable; no task boundary was crossed and no unrelated file was touched by either.
- **Files affected:** none beyond the two already declared in Task 2's `<files>`.

## Known Stubs

None.

## Threat Flags

None — this plan is a documentation/comment-only settlement. `T-34.16G-16` (verdict re-derivability), `T-34.16G-18` (comment must name a repo slug, not just a version), and `T-34.16G-19` (scope creep into `meta/releaseTags.ts`) are the three `mitigate`-disposition threats this plan's own threat model targets, and all three are directly measured above (repo-slug count 8, `0034ad265` grep, `releaseTags.ts` shasum unchanged). No new network endpoint, auth path, file-access pattern, or schema surface was introduced.

## Self-Check: PASSED

- `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-PIN-ARCHAEOLOGY.md` — FOUND
- `meta/buildRunnersOnedir.ts` — FOUND (comment edits present, confirmed via `grep -n PROVENANCE`)
- Commit `9345b78b8` — FOUND in `git log --oneline --all`
- Commit `c22ea5f73` — FOUND in `git log --oneline --all`
- Commit `1f8f7061b` — FOUND in `git log --oneline --all`
