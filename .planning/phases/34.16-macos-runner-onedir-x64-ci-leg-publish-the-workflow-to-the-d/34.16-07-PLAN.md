---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 07
type: execute
wave: 4
depends_on:
  - 34.16-03
files_modified:
  - meta/buildRunnersOnedir.ts
  - meta/__tests__/buildRunnersOnedir.test.ts
  - meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/build-base.yml
  - meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/python.yml
  - meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/release.yml
  - meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/lint-ruff.yml
  - .prettierignore
autonomous: true
gap_closure: true
closes_findings:
  - F-34.16-D
requirements:
  - REQ-34.9-02
decisions: []

must_haves:
  truths:
    - "F-34.16-D: extractUpstreamPyinstallerCommand returns legendary's real Build-step invocation from the pinned legendary workflow set instead of throwing 'No run: step invoking pyinstaller directly'"
    - "The widening recognises exactly three invocation shapes -- a bare console-script call, `uv run --module PyInstaller`, and `python -m PyInstaller` -- each RED-proven by its OWN test against the un-widened extractor"
    - "The extractor's two pre-existing refusals survive unweakened: it still throws naming the workflows dir on zero matches, and still throws naming every match on more than one DISTINCT (command, workingDirectory) pair"
    - "A mere MENTION of pyinstaller is still never extracted: a step `name:`, a `pip install pyinstaller` / `uv pip install pyinstaller` argument, and an extras name that merely contains the substring (`uv sync --extra pyinstaller_build`) all fail to match"
    - "A non-PyInstaller module invocation is never extracted -- legendary's own `python -m zipapp` step, which lives in the SAME file as the real invocation, must not become a second match"
    - "The spawned executable can no longer be chosen by upstream text: the onedir build spawns the venv's own pyinstaller binary with a derived ARGUMENT LIST, and the recognised launcher tokens are dropped rather than forwarded"
    - "The onedir derivation succeeds for every recognised shape and the step's working-directory: sibling still resolves for the module forms"
    - "BUILD-MANIFEST-{arch}.json's key set and per-runner field set are unchanged by this plan, so plan 34.16-05's pin script and plan 34.16-03's format-contract tests are unaffected"
  artifacts:
    - path: "meta/buildRunnersOnedir.ts"
      provides: "the widened extractor, the form-aware onedir derivation, and an argv-list spawn that never takes its executable from upstream text"
      contains: "uv"
    - path: "meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/build-base.yml"
      provides: "the real upstream file that broke the arm64 CI leg, captured verbatim as a permanent regression specimen carrying four near-miss lines"
      contains: "uv run --module PyInstaller"
    - path: "meta/__tests__/buildRunnersOnedir.test.ts"
      provides: "one test per recognised shape, both preserved refusals, every mention/near-miss negative, and the derivation/argv proofs"
      contains: "legendary-0.21.0"
  key_links:
    - from: "meta/buildRunnersOnedir.ts:extractUpstreamPyinstallerCommand"
      to: "meta/buildRunnersOnedir.ts:runOnedirBuild"
      via: "a derived argument LIST plus a form tag, never a command string whose first token is executed"
      pattern: "pyinstallerPath\\(repoDir\\)"
---

<objective>
Close `F-34.16-D`'s code half: widen `extractUpstreamPyinstallerCommand` to recognise the module
invocation forms legendary 0.21.0 migrated to, without weakening either of its refusals and without
letting upstream text choose the executable.

Purpose: the arm64 CI leg of RUN ATTEMPT 2 died at `Build the three onedir runners` with
`No "run:" step invoking pyinstaller directly (as its first token) found in any *.yml/*.yaml workflow
under .../src/legendary/.github/workflows`. The x64 leg was queued behind it and would have died
identically. Until this is fixed, `34.16-LIVE-GATE.md` Scored Item 2 cannot pass and Items 3-6 cannot
even be attempted.

Output: a widened extractor, a form-aware onedir derivation, an argv-list spawn, a verbatim capture
of the real upstream workflow set as a permanent fixture, and a per-shape RED-proven test battery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-CONTEXT.md
@meta/buildRunnersOnedir.ts
@meta/__tests__/buildRunnersOnedir.test.ts

<interfaces>
<!-- Current contracts in meta/buildRunnersOnedir.ts. Use these directly. -->

export interface UpstreamPyinstallerCommand {
  command: string
  workingDirectory?: string
}
export function extractUpstreamPyinstallerCommand(repoDir: string): UpstreamPyinstallerCommand   // L243

export interface OnedirCommandResult {
  command: string
  droppedExpressions: string[]
}
export function toOnedirCommand(upstreamLine: string): OnedirCommandResult                        // L321

function extractRunValues(text: string): RunValueMatch[]                                          // L196
function findSiblingKey(lines, runLineIndex, keyIndent, keyName): string | undefined              // L167
function indentOf(line: string): number                                                           // L147
async function runOnedirBuild(repoDir, buildCwd, onedirCommand: string): Promise<void>            // L522
function pyinstallerPath(repoDir: string): string   // join(repoDir, '.venv', 'bin', 'pyinstaller') L420
export async function buildRunner(runner, arch): Promise<RunnerBuildResult>                       // L639

// RunnerBuildResult fields consumed by buildManifestObject() (L741) and therefore
// by meta/pinRunnerDigests.ts -- these names must NOT change:
//   upstreamLine, upstreamWorkingDirectory, onedirCommand, droppedExpressions
</interfaces>

<upstream_facts>
<!-- Captured by the planner from the live upstream at the currently pinned legendary tag,
     2026-08-25, via `gh api repos/legendary-gl/legendary/contents/.github/workflows/...`.
     RE-CAPTURE these files yourself in Task 1; do not trust this block as the artifact. -->

`.github/workflows/` at the pinned legendary tag holds exactly four files:
`build-base.yml`, `python.yml`, `release.yml`, `lint-ruff.yml`.

`build-base.yml` carries the ONE real invocation, folded across six physical lines, in a step with a
`working-directory:` sibling whose value is `legendary`. Its `run:` value begins with the four tokens
`uv`, `run`, `--module`, `PyInstaller`, and its remaining arguments are `--onefile`, `--name`,
`legendary`, a `${{ ... }}` expression, `-i`, a `../assets/...` icon path, and `cli.py`.

The SAME four files also contain these near-misses, every one of which must NOT be extracted:
  - `build-base.yml`   job key `pyinstaller:` (a mapping key, not a `run:` value)
  - `build-base.yml`   `run: uv sync --extra pyinstaller_build --no-install-local`  (substring only)
  - `build-base.yml`   `run: python -m zipapp ...` folded over four lines (a module form, wrong module)
  - `release.yml`      `run: uv build` and `run: uv publish`  (uv, but not `uv run --module`)
</upstream_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the real upstream workflow set and RED-prove every recognised shape and every near-miss against the un-widened extractor</name>

  <read_first>
    - `meta/buildRunnersOnedir.ts` L130-L300 — the extractor's header comment (which states the
      "FIRST token is literally pyinstaller" contract you are about to widen), `indentOf`,
      `findSiblingKey`, `extractRunValues`, `extractUpstreamPyinstallerCommand`
    - `meta/__tests__/buildRunnersOnedir.test.ts` L96-L208 — the existing
      `extractUpstreamPyinstallerCommand` describe block; match its `mkdtempSync` + `mkdirSync` +
      `writeFileSync` fixture idiom and its `afterEach` cleanup exactly
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md`
      the `### Finding F-34.16-D` section — the verbatim CI error and the scope table
    - `.prettierignore` — note the existing `.github/workflows/build-base.yml` entry, the precedent
      for excluding a YAML file that must stay byte-verbatim
  </read_first>

  <files>meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/build-base.yml, meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/python.yml, meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/release.yml, meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/lint-ruff.yml, meta/__tests__/buildRunnersOnedir.test.ts, .prettierignore</files>

  <behavior>
    Twelve NEW test cases, each named so its subject is unambiguous. Nine of them MUST be RED against
    the un-widened extractor and GREEN after Task 2. Three (the preserved refusals) MUST be GREEN in
    both states — that is what proves the widening did not weaken them.

    RED-now, GREEN-after-Task-2 (one test per shape — never one test covering several):
    - Shape A, bare console script, adjacent to the module forms so the three are directly
      comparable: a synthetic fixture whose `run:` value's first token is `pyinstaller` extracts to
      that exact folded value. (This one is GREEN now too; it is the control.)
    - Shape B, `uv run --module PyInstaller` — synthetic fixture, minimal, no working-directory.
    - Shape B', `uv run -m PyInstaller` — the short module flag.
    - Shape C, `python -m PyInstaller` — synthetic fixture.
    - Shape C', `python3 -m PyInstaller` — the versioned interpreter name.
    - Shape B with a `working-directory:` sibling — the sibling still resolves for a module form.
    - The REAL captured fixture set: copying all four captured files into a tmp
      `<repoDir>/.github/workflows/` and extracting yields exactly one result, whose command is the
      full folded `uv run --module PyInstaller ...` value and whose workingDirectory is the step's
      own sibling value. Read the expected values from the captured `build-base.yml` at test time
      rather than transcribing them into the assertion by hand where practical.
    - Module-name discrimination: a fixture containing ONLY `python -m zipapp --output dist/x build`
      throws the zero-match error naming the workflows dir. A module form with the wrong module is
      not an invocation.
    - Launcher discrimination: a fixture containing ONLY `uv build` and `uv publish` throws the
      zero-match error. `uv` alone is not a PyInstaller invocation.

    GREEN both before and after (the preserved refusals and the preserved mention-rejection):
    - Zero matches still throws an error whose message contains the absolute workflows dir path.
    - Two DISTINCT invocations still throw an error naming BOTH commands. Write this one with one
      bare shape and one MODULE shape, so the multi-match refusal is proven to see the new forms too
      and not only the old one.
    - Mentions are still not invocations, asserted as separate cases in one test:
      `run: pip3 install --upgrade setuptools pyinstaller`,
      `run: uv pip install pyinstaller`,
      `run: uv sync --extra pyinstaller_build --no-install-local`,
      and a step whose `name:` contains `pyinstaller` while its `run:` does not — all in one fixture
      that must produce the zero-match throw.
  </behavior>

  <action>
    Capture the fixture first. For each of the four workflow filenames listed in `<upstream_facts>`,
    fetch the file at the tag `RELEASE_TAGS.legendary` currently holds — read that value fresh from
    `meta/releaseTags.ts`, never transcribe it — with
    `gh api "repos/legendary-gl/legendary/contents/.github/workflows/<file>?ref=<tag>" --jq '.content' | base64 -d`
    redirected to
    `meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/<file>`. Capture the exit status by
    redirect (`cmd > f.log 2>&1; echo $? > f.exit`), never through a pipe — a pipe swallows the
    failure and exits 0. Confirm each captured file is non-empty and that `build-base.yml` contains
    the literal `uv run --module PyInstaller`.

    Name the fixture directory for the tag it was captured at (`legendary-0.21.0`) so a later tag
    bump adds a sibling directory rather than silently mutating this specimen. Add a
    `README.md`-free, comment-free verbatim capture: do NOT reformat, re-indent, or strip anything.
    The near-miss lines are the point.

    Add the fixture directory to `.prettierignore` under a comment saying these are verbatim
    third-party captures that must not be reformatted, mirroring the existing
    `.github/workflows/build-base.yml` entry's intent.

    Write the twelve cases into `meta/__tests__/buildRunnersOnedir.test.ts`, inside the existing
    `describe('extractUpstreamPyinstallerCommand (never guesses)')` block, reusing its
    `mkdtempSync(join(tmpdir(), 'onedir-fixture-<name>-'))` + `afterEach` cleanup idiom. For the real
    fixture case, copy the four captured files into the tmp `<repoDir>/.github/workflows/` with
    `copyFileSync` — do NOT commit a nested `.github` directory inside `meta/`.

    Run the suite and RECORD the RED transcript: which of the twelve fail, with the error text. Nine
    failing, three passing is the expected RED state. Capture it with the redirect form into the
    plan's evidence and quote the counts in the SUMMARY. A test battery whose RED state was never
    observed proves nothing about what it detects.
  </action>

  <verify>
    <automated>pnpm jest --selectProjects Meta meta/__tests__/buildRunnersOnedir.test.ts > /tmp/34.16-07-t1-red.log 2>&1; echo $? > /tmp/34.16-07-t1-red.exit; grep -E "Tests:|Test Suites:" /tmp/34.16-07-t1-red.log</automated>
  </verify>

  <acceptance_criteria>
    - The four captured files exist under
      `meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/` and each is non-empty:
      `find meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0 -name '*.yml' -size +0 | wc -l`
      prints `4`.
    - `grep -c 'uv run --module PyInstaller' meta/__tests__/fixtures/upstream-workflows/legendary-0.21.0/build-base.yml`
      prints `1`.
    - The captured `build-base.yml` still carries all three of its near-misses:
      `grep -c 'uv sync --extra pyinstaller_build'` prints `1` and
      `grep -c 'python -m zipapp'` prints `1`; and in `release.yml`,
      `grep -cE '^ *run: uv (build|publish)$'` prints `2`. These are the known-bad inputs the
      negative tests exist to reject; if the capture lost them the fixture is worthless.
    - The captured files are byte-identical to upstream at the pinned tag: for each file,
      `gh api "repos/legendary-gl/legendary/contents/.github/workflows/<file>?ref=<tag>" --jq '.content' | base64 -d | shasum -a 256`
      equals `shasum -a 256` of the committed copy. Record all four pairs.
    - RED state recorded: the Task-1 run exits non-zero and `Tests:` reports exactly 9 failed among
      the 12 new cases. The three GREEN-in-both-states cases (zero-match throw, multi-match throw,
      mentions-are-not-invocations) are NOT among the failures. Quote the failing test names.
    - `git diff --stat -- meta/buildRunnersOnedir.ts` is EMPTY after this task. Task 1 changes no
      production code; if it did, the RED proof would be measuring a moving target.
    - Repo-wide prettier debt does not grow: run
      `npx prettier --check . > /tmp/34.16-07-prettier-before.log 2>&1; echo $? > /tmp/...exit`
      before the fixture is added and again after, and assert the count of listed files is identical.
      (`pnpm prettier` is RED repo-wide on this branch for reasons this phase does not own — the bar
      is "no NEW debt", never "green".)
  </acceptance_criteria>

  <done>
    The real upstream file set that broke CI is committed verbatim as a fixture, twelve tests exist,
    nine of them are observed RED against the un-widened extractor with their failure text recorded,
    and the three refusal/mention tests are observed GREEN before any production change.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen the extractor, make the onedir derivation form-aware, and stop taking the spawned executable from upstream text</name>

  <read_first>
    - `meta/buildRunnersOnedir.ts` L243-L300 (`extractUpstreamPyinstallerCommand`), L321-L360
      (`toOnedirCommand`), L522-L545 (`runOnedirBuild` — note `argv[0] = pyinstallerPath(repoDir)`,
      the line this task deletes), L639-L715 (`buildRunner`), L741-L768 (`buildManifestObject`)
    - `meta/__tests__/buildRunnersOnedir.test.ts` L70-L94 — the three existing `toOnedirCommand`
      tests, which call it with ONE argument and must keep passing unmodified
    - `meta/__tests__/buildRunnersOnedir.test.ts` L347-L384 — the `buildManifestObject` key-set
      contract this task must not disturb
    - `meta/pinRunnerDigests.ts` — confirm which manifest fields it reads before changing any
  </read_first>

  <files>meta/buildRunnersOnedir.ts, meta/__tests__/buildRunnersOnedir.test.ts</files>

  <behavior>
    - `extractUpstreamPyinstallerCommand` returns, in addition to its existing `command` and
      `workingDirectory`, a `form` discriminator and a `pyinstallerArgs` array holding the tokens
      that follow the recognised launcher prefix.
    - A new exported pure function derives the onedir invocation from that result: it swaps the one
      `--onefile` for `--onedir`, drops every `${{ ... }}` expression and reports them, and returns
      an ARGUMENT LIST containing no launcher token. For the `uv run --module PyInstaller` shape the
      returned args contain none of `uv`, `run`, `--module`, `-m`, `PyInstaller`. For the
      `python -m PyInstaller` shape they contain none of `python`, `python3`, `-m`, `PyInstaller`.
    - The derived display string still begins with `pyinstaller ` for every form, so
      `BUILD-MANIFEST-{arch}.json`'s `onedirCommand` keeps the meaning it has today.
    - The `--onefile` refusals survive per form: zero occurrences throws naming `--onefile`; more
      than one throws as ambiguous. Prove both for a module form, not only for the bare form.
    - `runOnedirBuild` spawns `pyinstallerPath(repoDir)` with the derived argument list. No token
      originating in upstream text is ever used as the executable.
  </behavior>

  <action>
    Widen by an explicit, enumerated form table — not by loosening the existing regex. Declare a
    module-level constant listing exactly three recognised forms, each with a matcher anchored at the
    start of the folded `run:` value and a fixed launcher-prefix token count:

      - `bare`           — first token `pyinstaller`, prefix length 1. Keep the existing
                           `/^pyinstaller(\s|$)/` matcher byte-for-byte; do not rewrite it.
      - `uv-run-module`  — the adjacent token sequence `uv`, `run`, then `--module` or `-m`, then a
                           token equal to `pyinstaller` compared case-insensitively. Prefix length 4.
      - `python-m`       — first token `python` or `python3`, then `-m` or `--module`, then a token
                           equal to `pyinstaller` compared case-insensitively. Prefix length 3.

    Deliberate narrowness, and say so in the source comment: NO intervening tokens are tolerated
    between the launcher tokens. `uv run --with <anything> --module PyInstaller` does NOT match, and
    that is the intended behaviour — a new upstream flag must trip the tripwire and be widened by a
    human decision, never absorbed silently. Compare the module token case-insensitively so
    `python -m pyinstaller` is recognised, but compare it to that one name only, so legendary's own
    `python -m zipapp` step in the same file can never become a second match.

    Match against the FOLDED value that `extractRunValues` already produces; do not add a second
    parsing path. Leave `indentOf`, `findSiblingKey` and `extractRunValues` untouched — the
    working-directory sibling walk is form-independent and already correct.

    Keep both refusals exactly where they are. The zero-match error message must keep naming the
    workflows dir; update only its wording so it no longer claims the first token must literally be
    `pyinstaller` — that sentence becomes false the moment this task lands, and a stale error message
    is what sends the next reader down the wrong path. The multi-match error must keep naming every
    matching file and command, and must keep de-duplicating on the DISTINCT
    `(command, workingDirectory)` pair — legendary's own duplicate-workflow shape depends on it.

    Add `form: InvocationForm` and `pyinstallerArgs: string[]` to `UpstreamPyinstallerCommand`.
    Leave `command` meaning exactly what it means today (the full folded upstream value) so
    `RunnerBuildResult.upstreamLine` and the manifest's `upstreamPyinstallerLine` are unchanged.

    Export `deriveOnedirInvocation(upstream: UpstreamPyinstallerCommand)` returning
    `{ command: string; args: string[]; droppedExpressions: string[] }`. Implement it by operating on
    `pyinstallerArgs`, then set `command` to `pyinstaller` joined with the derived args. Give
    `toOnedirCommand` a second optional parameter defaulting to the bare form and implement it on top
    of the same logic, so the three existing tests at L70-L94 keep passing with NO edit — that
    untouched-and-still-green state is part of this task's evidence.

    Change `runOnedirBuild`'s third parameter from a command string to the derived `args` array and
    spawn `pyinstallerPath(repoDir)` with it. DELETE the `argv[0] = pyinstallerPath(repoDir)`
    substitution and the `onedirCommand.split(/\s+/)` line; keep the existing comment explaining why
    `buildCwd` (not `repoDir`) is the spawn cwd, since that reasoning is unchanged and was found live.

    Update `buildRunner` to thread the new shape through. `RunnerBuildResult`'s field NAMES do not
    change; `onedirCommand` receives the derived display string.

    Add the per-form `toOnedirCommand`/`deriveOnedirInvocation` cases and the launcher-token-absence
    assertions from `<behavior>` to the test file. Then re-run and record the GREEN transcript, and
    record one mutation proof: temporarily narrow the `uv-run-module` matcher to require `--module`
    only (dropping `-m`), observe the `uv run -m PyInstaller` test go RED alone, then restore the
    file by `cp` from a pre-made backup and confirm with `shasum -a 256` equality. Never restore with
    `git checkout -- <path>`: `.husky/post-checkout` runs `pnpm i && pnpm download-helper-binaries`,
    and the latter throws by design on the six `PENDING-CI-PUBLISH` sentinels. Never `git stash` and
    never `git reset` — they strand concurrent sessions on this repo.
  </action>

  <verify>
    <automated>pnpm jest --selectProjects Meta meta/__tests__/buildRunnersOnedir.test.ts > /tmp/34.16-07-t2-green.log 2>&1; echo $? > /tmp/34.16-07-t2-green.exit; grep -E "Tests:|Test Suites:" /tmp/34.16-07-t2-green.log</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm jest --selectProjects Meta meta/__tests__/buildRunnersOnedir.test.ts` exits 0 with zero
      failed tests. Quote the `Tests:` line. Read the exit status from the `.exit` file, not from a
      pipe.
    - The nine cases recorded RED in Task 1 are now GREEN, named individually in the SUMMARY against
      their Task-1 failure text. A count alone is not acceptable evidence that the same nine flipped.
    - The three existing `toOnedirCommand` tests at `meta/__tests__/buildRunnersOnedir.test.ts`
      L70-L94 are byte-unchanged and still passing:
      `git diff -U0 -- meta/__tests__/buildRunnersOnedir.test.ts` shows no hunk touching those lines.
    - Executable provenance, asserted behaviourally rather than at the call site: for a
      `uv run --module PyInstaller --onefile --name x cli.py` input, `deriveOnedirInvocation(...).args`
      contains none of `uv`, `run`, `--module`, `-m`, `PyInstaller`, `pyinstaller`, and its first
      element is `--onedir`. Same shape of assertion for the `python -m` form.
    - Source assertion with a stated RED specimen: `grep -c 'argv\[0\] =' meta/buildRunnersOnedir.ts`
      prints `0`. RED proof — the same grep against `git show HEAD:meta/buildRunnersOnedir.ts` prints
      `1`. Record both.
    - `grep -v '^ *//' meta/buildRunnersOnedir.ts | grep -c 'shell: true'` prints `0` — the spawn
      path takes an argv list and no shell, so no upstream token is ever interpreted by a shell.
      RED proof: the same pipeline against a scratch copy with `shell: true` injected prints `1`.
    - The manifest contract is untouched: the `buildManifestObject` describe block at
      `meta/__tests__/buildRunnersOnedir.test.ts` L347-L384 is byte-unchanged and green, and
      `grep -c 'upstreamPyinstallerLine\|onedirCommand\|droppedExpressions\|upstreamWorkingDirectory' meta/buildRunnersOnedir.ts`
      is >= 4 with all four names still present.
    - The stale contract sentence is gone: `grep -c 'as its first token' meta/buildRunnersOnedir.ts`
      prints `0`. RED proof — the same grep against `git show HEAD:meta/buildRunnersOnedir.ts` prints
      a non-zero count. Record both.
    - Mutation proof recorded: the named narrowing turned exactly the `uv run -m PyInstaller` test
      RED and nothing else; restoration confirmed by `shasum -a 256` equality against the pre-mutation
      backup, and the restore was done by `cp`, with the command quoted.
    - `pnpm codecheck` exits 0. (Measured green on this branch at planning time; a non-zero exit is
      this plan's regression, not pre-existing debt.)
    - `npx eslint -f json meta/buildRunnersOnedir.ts meta/__tests__/buildRunnersOnedir.test.ts > /tmp/34.16-07-eslint.json 2>&1; echo $? > /tmp/34.16-07-eslint.exit`
      then count entries with `severity === 2` across both files — must be `0`. Count severity 2 only;
      warnings are not CI-blocking and a finding can name the wrong file with the right line numbers.
    - `shasum -a 256 meta/releaseTags.ts` equals
      `f6c4e5006f6a4128a1f4cf7644292ad5e361898fde513c0f39edb1b6e6ff8895` — the value
      `34.16-LIVE-GATE.md` Precondition 7 recorded. `RELEASE_TAGS` must not drift; this plan reads it
      and never writes it.
    - `pnpm jest --selectProjects Meta` — the FAILURE SET is unchanged: exactly one failed suite,
      `meta/__tests__/genI18nGateScope.test.ts`. Do not assert a passed-suite count: plan 34.16-05
      already moved it once (21 -> 22) and this plan does not move it again, but the durable property
      is the failure set. Note the exact capitalisation `--selectProjects Meta` — a lowercase project
      name matches nothing and exits 0, a vacuous green.
  </acceptance_criteria>

  <done>
    The extractor recognises three enumerated invocation shapes and no more; both refusals and every
    mention-rejection still hold, proven against the real four-file upstream fixture; the onedir
    derivation returns an argument list from which the launcher tokens are absent; the spawned
    executable is the venv's own pyinstaller and cannot be chosen by upstream text; and the manifest
    format is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub repo -> `.build-tools/runners-onedir/src/<runner>` | A third-party repository is cloned at a pinned tag. Every byte of its `.github/workflows/*.yml` is untrusted input. |
| parsed `run:` value -> spawned process argv | Text taken from that untrusted YAML is turned into arguments for a process this repo executes on a developer machine and on a GitHub runner. This plan WIDENS what upstream text is accepted here, so the boundary is modelled rather than declared irrelevant. |
| captured fixture -> committed repo content | Four third-party YAML files are committed into `meta/__tests__/fixtures/`. They are read by tests only; they are never executed and never reach a build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.16G-01 | Elevation of Privilege | `runOnedirBuild` executable selection | mitigate | The executable is `pyinstallerPath(repoDir)` — the venv binary this repo installed — passed as `spawn`'s first argument. Task 2 deletes the `argv[0] = pyinstallerPath(repoDir)` substitution, so no upstream token is ever a candidate executable, not even transiently. Asserted by `grep -c 'argv\[0\] ='` = 0 with a RED specimen, and behaviourally by the launcher-token-absence assertion on `deriveOnedirInvocation(...).args`. |
| T-34.16G-02 | Tampering | argument injection via crafted `run:` value | mitigate | `spawn` is called with an argv ARRAY and no `shell: true` (asserted, with a RED specimen), so shell metacharacters in upstream text are inert bytes. Residual: upstream can still influence pyinstaller's own FLAGS at the pinned tag. That is the pre-existing premise of the whole extract-from-upstream design (34.9 D-03) and is bounded by the tag pin plus the digest-verified publication path; this plan does not widen it and does not weaken it. |
| T-34.16G-03 | Tampering | matcher over-permissiveness | mitigate | The widening is an enumerated three-entry form table with fixed prefix lengths and NO tolerance for intervening tokens, not a loosened regex. `uv build`, `uv publish`, `uv pip install pyinstaller`, `uv sync --extra pyinstaller_build`, `pip3 install ... pyinstaller` and `python -m zipapp` are each proven non-matching against the REAL upstream file that contains them. A future upstream flag insertion fails closed at the zero-match throw. |
| T-34.16G-04 | Spoofing | wrong repository parsed | accept | The repo slug comes from `ONEDIR_RUNNERS` (module constant) and the tag from `RELEASE_TAGS`; neither is env- or argv-supplied. Unchanged by this plan. |
| T-34.16G-05 | Denial of Service | ambiguous-match throw on a benign upstream change | accept | Fail-closed is the intended posture: a second distinct invocation aborts the build with both commands named, rather than guessing. This is the pre-existing contract and it is preserved deliberately. |
| T-34.16G-SC | Tampering | npm/pip/cargo installs | n/a for this plan | This plan installs no packages and adds no dependency to any manifest. `34.16-RESEARCH.md` `## Package Legitimacy Audit` records the phase-level determination. The third-party PyTHON installs that the build path performs are modelled in plan `34.16-09`, which is the plan that actually runs them. |
</threat_model>

<verification>
- `pnpm jest --selectProjects Meta meta/__tests__/buildRunnersOnedir.test.ts` green, exit read from a
  `.exit` file.
- `pnpm jest --selectProjects Meta` failure set unchanged: one failed suite, `genI18nGateScope.test.ts`.
- `pnpm codecheck` exit 0; scoped `npx eslint -f json` severity-2 count 0 on the two touched TS files.
- `shasum -a 256 meta/releaseTags.ts` equals the gate's Precondition 7 value.
- Repo-wide prettier file count unchanged before/after.
</verification>

<success_criteria>
`extractUpstreamPyinstallerCommand`, pointed at the real captured legendary workflow set, returns the
one true invocation with its `working-directory:` sibling; the derived onedir argument list contains
no launcher token; the two refusals and every mention-rejection are proven intact against the same
real file that carries four near-misses; and the nine per-shape tests are documented as having failed
before the widening and passed after it.
</success_criteria>

<output>
Create `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-07-SUMMARY.md` when done.

Record in it, because plan `34.16-11` writes these into `34.16-LIVE-GATE.md`'s `F-34.16-D` section
and cannot re-derive them: the nine RED test names with their pre-fix failure text, the same nine
GREEN after, the mutation proof, the four fixture sha256 pairs, and the commit sha.
</output>
