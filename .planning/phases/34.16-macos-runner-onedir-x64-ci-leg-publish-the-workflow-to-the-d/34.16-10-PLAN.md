---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 10
type: execute
wave: 6
depends_on:
  - 34.16-07
  - 34.16-09
files_modified:
  - meta/buildRunnersOnedir.ts
  - .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-PIN-ARCHAEOLOGY.md
autonomous: true
gap_closure: true
closes_findings:
  - F-34.16-D
requirements:
  - REQ-34.9-02
decisions: []

must_haves:
  truths:
    - "F-34.16-D's open sub-question is SETTLED as a recorded fact with named commands and outputs, not left as a suspicion"
    - "The settlement accommodates every outcome: the pin resolved, the pin never resolved, or the evidence is indeterminate -- each has a pre-written disposition and none is assumed"
    - "Every sentence in the repo describing the 34.9-era extraction contract's provenance is true after this plan, and any part that is not is corrected or explicitly qualified"
    - "If the settlement shows the finding's own stated conclusion was wrong, that is recorded plainly rather than quietly dropped"
    - "meta/releaseTags.ts is byte-unchanged: this plan settles a question about a pin, it does not change one"
  artifacts:
    - path: ".planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-PIN-ARCHAEOLOGY.md"
      provides: "the commands, their outputs, the verdict, and the disposition of every in-repo sentence the verdict touches"
      contains: "SETTLED VERDICT"
    - path: "meta/buildRunnersOnedir.ts"
      provides: "a provenance comment that is true of the currently pinned repo and tag"
      contains: "extractUpstreamPyinstallerCommand"
  key_links:
    - from: "meta/buildRunnersOnedir.ts extraction-contract provenance comment"
      to: "the repo/tag pair the contract was actually validated against"
      via: "a named commit and a named repository slug, both re-measurable"
      pattern: "0034ad265"
---

<objective>
Settle `F-34.16-D`'s open sub-question — whether the legendary pin that commit `0034ad265` replaced
ever resolved — and then make every in-repo sentence that depends on the answer true.

Purpose: the finding observes that `legendary-gl/legendary` has no `0.20.43` tag today and reasons
that this is "either a deleted tag or a digit transposition of `0.20.34`". If the old pin never
resolved, then `meta/buildRunnersOnedir.ts`'s 34.9-era comment — *"found live against the real,
cloned nile/legendary/heroic-gogdl workflows during this plan's execution"* — describes a build of
something other than the pinned version, and that comment is currently load-bearing documentation
for the whole extract-from-upstream design. The finding states this as a question. This plan answers
it with commands and outputs, and repairs the record in whichever direction the answer points.

Output: `34.16-PIN-ARCHAEOLOGY.md` with a SETTLED VERDICT, plus whatever comment repair that verdict
requires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-CONTEXT.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LEARNINGS.md
@meta/buildRunnersOnedir.ts
@meta/releaseTags.ts

<planner_probe>
<!-- The planner ran four read-only probes on 2026-08-25 while writing this plan. They are recorded
     here so the task is targeted, NOT so the executor can skip re-running them. Re-run every one;
     treat a disagreement with this block as the finding, not as an error. -->

1. `gh api repos/legendary-gl/legendary/git/ref/tags/0.20.43`  -> HTTP 404 Not Found
2. `gh api repos/derrod/legendary/git/ref/tags/0.20.43`        -> HTTP 404 Not Found
3. `gh api repos/Heroic-Games-Launcher/legendary/git/ref/tags/0.20.43` -> resolved to an object sha
4. `git show e1c9f6174:meta/buildRunnersOnedir.ts` shows `ONEDIR_RUNNERS.legendary.repo` as
   `Heroic-Games-Launcher/legendary`, whereas HEAD shows `legendary-gl/legendary`.

Commit `0034ad265`'s own message says "legendary moved to legendary-gl/legendary" — i.e. that commit
changed BOTH the repository slug and the tag. The finding's probe queried the POST-bump repository
for a PRE-bump tag. If probes 3 and 4 reproduce, the sub-question's premise is a repo mismatch rather
than a deleted tag or a transposition, and the implication drawn from it does not follow.

Corroborating in-repo evidence to weigh, from 34.9's own records:
  - `34.9-LIVE-GATE-RERUN.md:445` — a real binary printing `legendary version "0.20.43", codename
    "Riding Shotgun (Heroic)"`. A binary cannot print a version for a tag that never cloned.
  - `34.9-03-SUMMARY.md:65` — all three runners built as real `--onedir` bundles, legendary at that
    same version.
  - `34.9-WRAPPER-PROOF.md:358` — the marker line `Building legendary (0.20.43) as --onedir for
    arm64...`. Note this one proves only that the build STARTED: that run was deliberately SIGTERM'd
    before its clone. Cite it for what it shows and not for more.
</planner_probe>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settle the sub-question with named commands, and record the verdict as one of three</name>

  <read_first>
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md`
      `### Finding F-34.16-D`, the paragraph beginning "Open sub-question, NOT resolved here" — the
      exact claim being tested
    - `meta/buildRunnersOnedir.ts` L110-L146 — the extractor header comment, including the
      "found live against the real, cloned nile/legendary/heroic-gogdl workflows during this plan's
      execution" sentence and the "Rule 1 fix" sentences that share its provenance
    - `meta/releaseTags.ts` — its header's "a future version bump belongs to its own dedicated
      upgrade plan" warning
  </read_first>

  <files>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-PIN-ARCHAEOLOGY.md</files>

  <action>
    Re-run all four `<planner_probe>` commands yourself and record each command with its verbatim
    output. Add these:
      - `git log --format='%h %ad %s' --date=short -- meta/releaseTags.ts` — the full history of the
        pin, which is two commits.
      - `git show 0034ad265 -- meta/releaseTags.ts meta/buildRunnersOnedir.ts meta/downloadHelperBinaries.ts`
        — read what that commit changed about the legendary REPO slug as well as the tag. Quote both.
      - `gh api "repos/Heroic-Games-Launcher/legendary/tags?per_page=20" --jq '.[].name'` and the same
        for `legendary-gl/legendary`. Two different tag namespaces is the hypothesis under test.
      - the three corroborating in-repo citations from `<planner_probe>`, quoted with their file and
        line, each labelled with what it does and does not prove.

    Capture every command's exit status by redirect (`cmd > f.log 2>&1; echo $? > f.exit`), never
    through a pipe — a pipe swallows the failure and exits 0. A `gh api` 404 is an expected outcome
    here, not an error; record it as data.

    Then record a SETTLED VERDICT as exactly one of these three, on its own line in the form
    `**SETTLED VERDICT:** <value>`:

      - **PIN RESOLVED — FINDING'S PREMISE WAS A REPO MISMATCH.** The tag existed in the repository
        that was pinned at the time. The 34.9-era provenance comment is TRUE. What the finding
        actually surfaced is larger than it stated: commit `0034ad265` changed the legendary
        REPOSITORY, not merely its version, so the extraction contract validated in 34.9 was
        validated against a different project than the one now pinned. That, not "a version bump", is
        the root cause of `F-34.16-D`.
      - **PIN NEVER RESOLVED.** No repository reachable from this repo's history carries that tag.
        The 34.9-era provenance comment describes a build of something other than the pinned version
        and is FALSE as written.
      - **INDETERMINATE.** The evidence does not distinguish the two — for example the tag is absent
        everywhere today but 34.9's own transcripts show a binary self-reporting it, and no surviving
        record identifies which repository was cloned. Record precisely which observation is missing.

    Whichever verdict lands, also record explicitly whether the finding's own stated conclusion
    ("either a deleted tag or a digit transposition of `0.20.34`") is supported, unsupported or
    unaddressed by the evidence. Do not quietly drop a conclusion that turns out to be wrong; naming
    it is the point.

    Do NOT change `meta/releaseTags.ts`. If the evidence suggests a bump, a revert or a repo change is
    warranted, record that as a routed follow-up naming the phase it belongs to, and stop. A version
    change belongs to its own dedicated upgrade plan and `meta/releaseTags.ts`'s sha256 is live-gate
    Precondition 7, which must be identical at the end of the gate.

    Write everything into `34.16-PIN-ARCHAEOLOGY.md` with frontmatter carrying the verdict.
  </action>

  <verify>
    <automated>grep -cE '^\*\*SETTLED VERDICT:\*\* (PIN RESOLVED|PIN NEVER RESOLVED|INDETERMINATE)' .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-PIN-ARCHAEOLOGY.md</automated>
  </verify>

  <acceptance_criteria>
    - Exactly one line matches
      `^\*\*SETTLED VERDICT:\*\* (PIN RESOLVED|PIN NEVER RESOLVED|INDETERMINATE)`, and the same value
      appears in the document's frontmatter.
    - All four planner probes are re-run and their verbatim outputs quoted, each with its `.exit`
      value. Where an output disagrees with `<planner_probe>`, the disagreement is called out
      explicitly and the executor's own measurement wins.
    - The repo-slug question is answered with a quoted diff: the document shows what
      `ONEDIR_RUNNERS.legendary.repo` was before `0034ad265` and what it is at HEAD. A verdict that
      does not address the slug has not tested the hypothesis.
    - Both tag listings are recorded, and the document states whether the pre-bump tag appears in
      either namespace.
    - The three corroborating in-repo citations are quoted with `file:line`, and each carries a
      one-line statement of what it does NOT prove. In particular the `34.9-WRAPPER-PROOF.md:358`
      marker must be labelled as proving only that the build started, because that run was
      deliberately terminated before its clone.
    - The document states, in one sentence, whether the finding's "deleted tag or digit
      transposition" conclusion is supported by the evidence.
    - `git diff --name-only` for this task lists exactly one path, the archaeology document.
    - `shasum -a 256 meta/releaseTags.ts` equals
      `f6c4e5006f6a4128a1f4cf7644292ad5e361898fde513c0f39edb1b6e6ff8895` — the value
      `34.16-LIVE-GATE.md` Precondition 7 recorded — measured and quoted after the task.
    - No forbidden command was run: a single
      `grep -cE 'git (checkout|stash|reset)|git push|gh workflow run|pnpm (pin:runner-digests|download-helper-binaries|dist:mac|verify:runner-bundle)'`
      over the task transcript prints `0`.
  </acceptance_criteria>

  <done>
    The sub-question is answered by re-run commands with quoted outputs, the verdict is one of three
    named values, the finding's own conclusion is explicitly graded against the evidence, and
    `meta/releaseTags.ts` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the in-repo record true under the settled verdict</name>

  <read_first>
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-PIN-ARCHAEOLOGY.md`
      — Task 1's verdict
    - `meta/buildRunnersOnedir.ts` L110-L146 and L285-L300 — every sentence in this file that claims
      provenance for the extraction contract: the header's "found live against the real, cloned
      nile/legendary/heroic-gogdl workflows during this plan's execution", and the "Rule 1 fix (found
      live during this plan's real legendary build)" note above the duplicate-workflow refusal
    - `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-07-SUMMARY.md`
      — what plan 07 already changed in this file, so this task's diff stays limited to comments
  </read_first>

  <files>meta/buildRunnersOnedir.ts, .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-PIN-ARCHAEOLOGY.md</files>

  <action>
    First, enumerate the sentences at risk. Grep this repo's SOURCE (not `.planning/`) for every
    provenance claim about the extraction contract — start from the two named in `<read_first>` and
    search for `found live`, `during this plan's execution` and `real legendary build` under `meta/`.
    Record the full list in the archaeology document with a per-sentence disposition. A repair that
    fixes one sentence and leaves a second one saying the opposite is worse than no repair.

    Then apply the disposition the verdict requires:

    - Under **PIN RESOLVED — FINDING'S PREMISE WAS A REPO MISMATCH**: the provenance sentences stand.
      They are true but now dangerously incomplete, because they read as if the contract were
      validated against the currently pinned project. QUALIFY them: name the repository slug and the
      tag the contract was actually validated against, name commit `0034ad265` as the point at which
      BOTH the slug and the tag changed, and say plainly that the validation therefore does not carry
      over to the current pin — which is precisely why `F-34.16-D` happened and why the tripwire in
      plan `34.16-08` exists. Point the reader at `meta/runnerBuildInvocations.ts` as the artifact
      that now carries live provenance.
    - Under **PIN NEVER RESOLVED**: the provenance sentences are false. CORRECT them — state what is
      actually known about which tree the contract was derived from, and remove the claim that it was
      validated against the pinned version. Do not soften a false sentence into a vague one; a vague
      provenance claim is the same defect wearing a hedge.
    - Under **INDETERMINATE**: mark the claim as unverified, name the specific observation that would
      settle it, and cite `34.16-PIN-ARCHAEOLOGY.md`. Do not leave it reading as established fact.

    In all three cases the comment must end up naming a repository slug, not just a version, since
    the whole point of the settlement is that a slug and a version are different things.

    Change comments only. No behaviour change, no signature change, no `RELEASE_TAGS` change. If you
    find yourself editing an expression, stop — that belongs to a different plan.

    Restore anything you disturb by `cp` from a pre-made backup plus a `shasum -a 256` equality
    check. Never `git checkout -- <path>` (fires `.husky/post-checkout`, which runs
    `pnpm download-helper-binaries`, which throws by design on the six `PENDING-CI-PUBLISH`
    sentinels), never `git stash`, never `git reset`.
  </action>

  <verify>
    <automated>pnpm jest --selectProjects Meta meta/__tests__/buildRunnersOnedir.test.ts meta/__tests__/runnerBuildInvocations.test.ts > /tmp/34.16-10-t2.log 2>&1; echo $? > /tmp/34.16-10-t2.exit; grep -E "Tests:|Test Suites:" /tmp/34.16-10-t2.log</automated>
  </verify>

  <acceptance_criteria>
    - The archaeology document lists every provenance sentence found under `meta/` with a per-sentence
      disposition (`qualified`, `corrected`, `marked unverified`, or `no change needed, because ...`).
      The grep commands used to enumerate them are quoted, so the census is reproducible.
    - The edited comment names a repository SLUG. Behavioural form of the assertion, with a RED
      specimen: `grep -c 'Heroic-Games-Launcher/legendary\|legendary-gl/legendary' meta/buildRunnersOnedir.ts`
      is >= 1 where it was previously `1` (the `ONEDIR_RUNNERS` entry alone) — record the before and
      after counts and quote the new comment lines, so the increase is attributable to the comment and
      not to an accidental code edit.
    - Comment-only change proved, not asserted: `git diff -U0 -- meta/buildRunnersOnedir.ts` for THIS
      task shows only added/removed lines that are comments. Quote the diff. Additionally
      `pnpm codecheck` exits 0 and
      `pnpm jest --selectProjects Meta meta/__tests__/buildRunnersOnedir.test.ts meta/__tests__/runnerBuildInvocations.test.ts`
      exits 0 with zero failures — a comment edit cannot change either, and if it does, the edit was
      not comment-only.
    - Under PIN RESOLVED specifically: the comment states that the validation does not carry over to
      the current pin, and names `0034ad265`. `grep -c '0034ad265' meta/buildRunnersOnedir.ts` prints
      `1` or more. RED specimen: the same grep against `git show HEAD:meta/buildRunnersOnedir.ts`
      prints `0`.
    - No stale contradiction survives: after the edit, re-run the enumeration greps and confirm every
      hit is either edited or explicitly dispositioned as "no change needed". Quote the post-edit hit
      list next to the pre-edit one.
    - `npx eslint -f json meta/buildRunnersOnedir.ts` yields zero entries with `severity === 2`.
    - `npx prettier --check meta/buildRunnersOnedir.ts` passes, measured in place (never on a temp
      copy — a copy resolves a different config). If it does not pass, the comment reflow is this
      plan's own debt and must be fixed here rather than left for the repo-wide red.
    - `shasum -a 256 meta/releaseTags.ts` still equals
      `f6c4e5006f6a4128a1f4cf7644292ad5e361898fde513c0f39edb1b6e6ff8895`.
    - `pnpm jest --selectProjects Meta` failure SET unchanged: exactly one failed suite,
      `meta/__tests__/genI18nGateScope.test.ts`. Exact capitalisation `Meta`.
  </acceptance_criteria>

  <done>
    Every provenance sentence under `meta/` about the extraction contract has been enumerated and
    dispositioned, the surviving text is true under the settled verdict and names a repository slug
    rather than only a version, and the change is proved comment-only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API responses -> a recorded historical verdict | Tag existence is read from a third party that can delete or re-point tags at any time. The verdict is a claim about the past inferred from present-day API state plus this repo's own transcripts. |
| a source comment -> a future maintainer's decision | The provenance comment is load-bearing documentation for the whole extract-from-upstream design. A wrong or incomplete comment routes the next reader into the wrong repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.16G-16 | Repudiation | the archaeology verdict itself | mitigate | Every probe is recorded as a command with its verbatim output and exit status, against named repository slugs, so the verdict is re-derivable and falsifiable rather than an assertion. An INDETERMINATE branch exists precisely so absence of evidence is not recorded as evidence. |
| T-34.16G-17 | Tampering | upstream tag deletion or re-pointing changing the answer later | accept | The verdict is dated and scoped to what was observable on the day. Plan `34.16-08`'s networked checker is the standing mechanism for detecting later upstream movement; this plan settles history, not the future. |
| T-34.16G-18 | Spoofing | a comment that names a version but not a repository | mitigate | The repaired comment is required to name a repository slug, with a before/after grep count. A version alone is exactly the ambiguity that produced this sub-question. |
| T-34.16G-19 | Tampering | scope creep into `meta/releaseTags.ts` | mitigate | `shasum -a 256 meta/releaseTags.ts` asserted equal to `34.16-LIVE-GATE.md` Precondition 7's recorded value at the end of both tasks; any warranted version change is recorded as a routed follow-up and explicitly not made here. |
| T-34.16G-SC | Tampering | npm/pip/cargo installs | n/a for this plan | No package is installed and no dependency manifest changes. |
</threat_model>

<verification>
- One SETTLED VERDICT line from a closed set of three, in body and frontmatter.
- All four planner probes re-run with verbatim outputs and exit statuses.
- Provenance-sentence census quoted before and after, with a per-sentence disposition.
- `git diff -U0 -- meta/buildRunnersOnedir.ts` comment-only; `pnpm codecheck` exit 0; the two Meta
  suites green; scoped eslint severity-2 count 0; `npx prettier --check` passes in place.
- `shasum -a 256 meta/releaseTags.ts` unchanged.
</verification>

<success_criteria>
The `0.20.43` question is a settled, dated, re-derivable fact rather than an open suspicion, and no
sentence in `meta/` still describes the extraction contract's provenance in a way the settlement
contradicts.
</success_criteria>

<output>
Create `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-10-SUMMARY.md` when done.

Record for plan `34.16-11`, which writes the SETTLED note into `34.16-LIVE-GATE.md`'s `F-34.16-D`
section and cannot re-derive it: the verdict, the one-paragraph basis, whether the finding's stated
conclusion was supported, and any routed follow-up.
</output>
