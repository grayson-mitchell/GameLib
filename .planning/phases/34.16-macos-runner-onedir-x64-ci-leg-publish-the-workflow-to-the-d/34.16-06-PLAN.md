---
phase: 34.16-macos-runner-onedir-x64-ci-leg
plan: 06
type: execute
wave: 3
depends_on:
  - 34.16-01
  - 34.16-02
  - 34.16-03
  - 34.16-04
  - 34.16-05
files_modified:
  - .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md
  - meta/runnersOnedirDigests.json
  - electron-builder.yml
autonomous: false
requirements:
  - REQ-34.9-02
  - REQ-34.9-03
  - REQ-34.9-04
  - REQ-34.16-01
  - REQ-34.16-02
  - REQ-34.16-04
decisions: [D-01, D-02, D-03, D-04, D-07, D-11, D-12]

must_haves:
  truths:
    - "`build-runners-onedir-macos.yml` exists on gamelib/main and is byte-identical to the feature-branch copy"
    - "A real workflow_dispatch has published six onedir archives and four audit assets from one identified run"
    - "meta/runnersOnedirDigests.json holds six real sha256 digests and the run id they came from"
    - "A one-character digest corruption throws and writes nothing under public/bin"
    - "The x64 runners execute under Rosetta"
    - "The guard's behaviour against a real x64 onedir tree is OBSERVED and classified, in both directions"
    - "The guard is observed executing in CI for both arches, with no new CI wiring added"
  artifacts:
    - path: ".planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md"
      provides: "a filled RUN RECORD with a verdict and per-item Observed fields"
      contains: "RUN RECORD"
    - path: "meta/runnersOnedirDigests.json"
      provides: "six real digests plus the source run id"
      contains: "runId"
  key_links:
    - from: "gamelib/main:.github/workflows/build-runners-onedir-macos.yml"
      to: "HEAD:.github/workflows/build-runners-onedir-macos.yml"
      via: "git cat-file -p on both refs, diffed — byte-identity, measured"
      pattern: "diff <\\(git cat-file -p gamelib/main"
---

<objective>
Run `34.16-LIVE-GATE.md`. This plan is the gate's **only** writer of results.

Purpose: D-04 split this phase in two. Plans 34.16-01..05 are the code-side half — authorable,
testable and committable on `fix/steam-native-install-stability` without any live dispatch. This plan
is the other half: everything that needs a real push, a real workflow run, real published artifacts
and a real x64 Mach-O tree. That work cannot be automated away, and D-03 puts the push itself outside
any plan's authorization.

Output: a filled RUN RECORD, six scored verdicts, and — if all six PASS — six closed ledger items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-CONTEXT.md
@.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-RESEARCH.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/deferred-items.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-GUARD-PROOF.md
@.planning/phases/34.9-macos-runner-onedir-repackaging-eliminate-the-pyinstaller-co/34.9-LIVE-GATE-RERUN.md
</context>

<constraints>
- **`34.16-LIVE-GATE.md` is NORMATIVE. Cite its numbered criteria; never paraphrase them.** A plan's
  lossy restatement of a correct contract is what an executor actually reads — that is precisely how
  `F-34.9-21-03` happened (`34.9-GUARD-PROOF.md` §2.5 AMENDMENT v2 §A3, which is binding on every
  plan that drives a proof contract). Any deliberate departure is recorded as a deviation BEFORE the
  gesture, never reconstructed afterwards.
- **This plan pushes to `gamelib`, never `origin`.** `origin` is
  `Heroic-Games-Launcher/HeroicGamesLauncher`, an unrelated upstream project.
- **No checkout in this worktree, at any point.** `.husky/post-checkout` runs
  `pnpm i && pnpm download-helper-binaries`. `git checkout -- <path>` fires the same hook. Restore
  files by `cp` from a pre-made backup plus a `shasum -a 256` equality check. **Never `git stash`,
  never `git reset`** — they strand concurrent sessions on this repo.
- **`--selectProjects` is case-sensitive and fails OPEN.** `Meta` and `Backend`, capitalised.
  Lowercase matches nothing and exits 0.
- **Both jest baselines are RED before this plan starts** and must stay exactly as red: Meta
  1 failed / 21 passed (`genI18nGateScope.test.ts`), Backend 1 failed suite / 3 failed tests
  (`decompressPool.test.ts`). Neither is this phase's to fix. A changed baseline makes the gate's
  preconditions unscorable.
- **`meta/releaseTags.ts` must be byte-unchanged** across every commit this plan makes. Read its
  values fresh; never quote a version literal.
- **`gsd-sdk query state.*` verbs corrupt STATE.md on this repo.** If STATE.md is updated, snapshot
  it first, run, diff — or hand-apply the edit.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Capture the gate's preconditions</name>
  <read_first>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md §5 (evidence capture) and §6 (preconditions), .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-02-SUMMARY.md (the recorded workflow-file sha256)</read_first>
  <files>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</files>
  <action>
Create the session directory with `mktemp -d /tmp/gamelib-34.16-gate-XXXXXX` and write its absolute
path into the gate's `session_dir` frontmatter field.

Discharge all eight preconditions in §6, in order, capturing each into its own distinctly-named file
under the session directory using §5's redirect form
(`<command> > "$SESSION_DIR/<name>.log" 2>&1; echo $? > "$SESSION_DIR/<name>.exit"`), never the
`| tee` + `${PIPESTATUS[0]}` idiom — that silently no-ops under zsh and writes a 1-byte bare-newline
exit file. Read every exit file back with `xxd` before scoring it (`300a` = 0, `310a` = 1); do not
use `cat -A`, which BSD `cat` does not have.

Precondition 1 additionally requires confirming that plans 34.16-01 through 34.16-05 are all
committed on this branch. Verify by their five SUMMARY files existing AND by the source facts they
claim: both `--arch=` invocations in `dist:mac`/`release:mac`, the ref-guard step in the workflow,
`GITHUB_RUN_ID` in `meta/buildRunnersOnedir.ts`, the `runId` key in `meta/runnersOnedirDigests.json`,
and the `pin:runner-digests` script entry. A SUMMARY is a claim; the source is the fact.

Write a `## Preconditions` block into the gate document recording each precondition's observed value
verbatim — the two jest count lines, the `git remote -v` output, both `shasum -a 256` values, and
`git rev-parse --short HEAD`.

**This task runs no gesture from any Scored Item.** It does not push, dispatch, pin, download, or
invoke `verify:runner-bundle`. It does not write `verdict`, `run_date`, `items_passed`,
`items_failed`, or any `Observed:` field.
  </action>
  <acceptance_criteria>
- `$SESSION_DIR` exists and its absolute path is written into the gate's `session_dir` frontmatter (no longer `TBD`).
- Eight distinctly-named `.log` files and their `.exit` siblings exist under `$SESSION_DIR`; no filename is reused.
- The gate document's `## Preconditions` block records: Meta counts, Backend counts, `git remote -v`, `git status --porcelain`, `git rev-parse --short HEAD`, `shasum -a 256` of `.github/workflows/build-runners-onedir-macos.yml`, and `shasum -a 256` of `meta/releaseTags.ts`.
- The recorded workflow-file sha256 equals the value in `34.16-02-SUMMARY.md`.
- Meta reads exactly `1 failed, 21 passed`; Backend reads exactly `1 failed, 180 passed` with 3 failed tests in `decompressPool.test.ts`. Any other failure is a precondition failure and stops this plan.
- `grep -c 'TBD' <gate>` on the frontmatter block shows `verdict`, `run_date`, `items_passed`, `items_failed`, `launch_ordinals_used` still `TBD` — this task wrote no result.
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest --selectProjects Meta && pnpm jest --selectProjects Backend; echo "record both count lines; do not gate on exit status — both baselines are expected RED"</automated>
  </verify>
  <done>The session directory exists, all eight preconditions are recorded verbatim in the gate document, both baselines match their expected RED shape, and no scored gesture has been run.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HUMAN GATE — land the workflow on gamelib/main and dispatch (Scored Items 1 and 2)</name>
  <read_first>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md §7 and §8, .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-RESEARCH.md §Q1 (NORMATIVE — cite, do not paraphrase)</read_first>
  <files>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</files>
  <action>Present the command block below verbatim and STOP. Per D-03 no plan may push to a remote or dispatch a workflow: the developer executes every step. Do not run steps 1-8 on the developer's behalf, and do not "helpfully" run steps 1-5 because they are non-destructive — a `MAIN_SHA` resolved by one actor and pushed by another reintroduces the exact staleness Pitfall 1 names. After the developer reports back, record their observations into `34.16-LIVE-GATE.md` §7 and §8 `Observed:` fields, citing each numbered criterion.</action>
  <what-built>
Plans 34.16-01..05 landed the entire code-side half of this phase on
`fix/steam-native-install-stability`: both per-arch guard invocations, the workflow ref guard and its
five RED-proven tests, the `runId` provenance field in `writeBuildManifest()`, the `runId` slot in
`meta/runnersOnedirDigests.json` with a behaviourally-proven marker-invariance test, the extended
digest-mismatch error, and `pnpm pin:runner-digests` with twelve-plus tests covering every
refuse-to-write path.

Nothing above can be exercised end-to-end until `build-runners-onedir-macos.yml` exists on the
repository's **default branch** — GitHub refuses `workflow_dispatch` for a workflow absent from it,
regardless of `--ref`. Per **D-03 the phase prepares and the developer pushes**: no plan is
authorized to push to a remote.
  </what-built>
  <how-to-verify>
**Steps 1-5 are non-destructive and were live-verified against this repository on 2026-08-24. Step 6
is the push.** None of steps 1-5 touch this worktree's index, HEAD or working tree, and none fire a
git hook — hooks fire on `checkout`/`switch`/normal-flow `commit`, never on plumbing primitives.

```sh
# 1. Resolve main's tip FRESH. Never reuse a value captured earlier in the session.
git fetch gamelib main
MAIN_SHA=$(git rev-parse gamelib/main); echo "$MAIN_SHA"

# 2. Hash the final workflow file into the object database.
BLOB_SHA=$(git hash-object -w .github/workflows/build-runners-onedir-macos.yml); echo "$BLOB_SHA"
shasum -a 256 .github/workflows/build-runners-onedir-macos.yml   # must match Precondition 6

# 3. Build main's tree + this one blob, in a SCRATCH index. Fresh mktemp per attempt.
export GIT_INDEX_FILE=$(mktemp)
git read-tree "$MAIN_SHA"
git update-index --add --cacheinfo 100644,"$BLOB_SHA",.github/workflows/build-runners-onedir-macos.yml
NEW_TREE=$(git write-tree)
unset GIT_INDEX_FILE

# 4. Create the commit object. Nothing is checked out.
NEW_COMMIT=$(git commit-tree "$NEW_TREE" -p "$MAIN_SHA" \
  -m "ci: publish build-runners-onedir-macos.yml to default branch")

# 5. GATE: exactly one added file, or ABORT before the push.
git diff-tree --no-commit-id --name-status -r "$MAIN_SHA" "$NEW_COMMIT"
#   Expected, exactly one line:
#   A       .github/workflows/build-runners-onedir-macos.yml
git status --porcelain; git rev-parse HEAD   # must be unchanged from Precondition 5

# 6. PUSH — gamelib, never origin. Push the commit OBJECT, never a local branch ref.
git push gamelib "$NEW_COMMIT":refs/heads/main

# 7. Byte-identity (D-02), still with no checkout.
git fetch gamelib main
diff <(git cat-file -p gamelib/main:.github/workflows/build-runners-onedir-macos.yml) \
     <(git cat-file -p HEAD:.github/workflows/build-runners-onedir-macos.yml)
#   Exit 0, no output.

# 8. DISPATCH against the FEATURE ref — main has no build script.
gh workflow run build-runners-onedir-macos.yml --ref fix/steam-native-install-stability
gh run list --workflow=build-runners-onedir-macos.yml --limit 3
gh run watch <run-id>
```

**On `! [rejected] ... (non-fast-forward)`:** `main` moved between steps 1 and 6. Re-run steps 1-5
with a fresh `MAIN_SHA` and push again. **Never `--force`.**

**On more than one line, or any `M`/`D` status, at step 5:** the tree was built from the wrong base
or the wrong blob path. ABORT. Do not proceed to step 6.

Then score `34.16-LIVE-GATE.md` §7 (six numbered criteria) and §8 (seven numbered criteria) and fill
both `Observed:` fields. §8 criterion 6 is the one to check most carefully: both
`BUILD-MANIFEST-{arch}.json` must carry a non-null, EQUAL `runId`. A `null` there means the dispatch
ran a ref predating plan 34.16-03, and Scored Item 3 becomes unscorable.
  </how-to-verify>
  <acceptance_criteria>
- `git diff-tree` at step 5 printed exactly one line, `A` status, that one path — recorded verbatim in §7 Observed.
- `git status --porcelain` and `git rev-parse HEAD` are identical before and after steps 1-5.
- The push targeted `gamelib`; the recorded command contains no occurrence of `origin` and no `--force`.
- The step-7 `diff` exited 0 with no output.
- The dispatch was accepted with no `HTTP 404 ... not found on the default branch`, and the run URL is recorded.
- Both matrix legs (`macos-13`/x64, `macos-14`/arm64) concluded successfully, and the ref-guard step is present in the run log having printed its confirmation line.
- `gh release view runners-onedir-macos` lists six `*_onedir.tar.gz` assets plus `SHA256SUMS-x64`, `SHA256SUMS-arm64`, `BUILD-MANIFEST-x64.json`, `BUILD-MANIFEST-arm64.json`.
- Both manifests' `runId` are non-null and equal; the value is recorded.
- §7 and §8 `Observed:` fields are filled with per-criterion results, each citing its criterion number.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the run URL and the agreed run id, or describe which numbered criterion failed.</resume-signal>
  <verify>
    <automated>git fetch gamelib main && diff <(git cat-file -p gamelib/main:.github/workflows/build-runners-onedir-macos.yml) <(git cat-file -p HEAD:.github/workflows/build-runners-onedir-macos.yml) && gh run list --workflow=build-runners-onedir-macos.yml --limit 3</automated>
  </verify>
  <done>`gamelib/main` carries the workflow byte-identical to HEAD's copy, added by a single-file commit; both matrix legs concluded successfully; six archives and four audit assets are published; both manifests carry the same non-null `runId`; §7 and §8 `Observed:` are filled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN GATE — pin, fetch, corrupt, execute (Scored Items 3 and 4)</name>
  <read_first>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md §9 and §10</read_first>
  <files>meta/runnersOnedirDigests.json, electron-builder.yml, .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</files>
  <action>Hand §9's eight criteria and §10's nine criteria to the developer as written, cited by number, and STOP. `34.16-LIVE-GATE.md` is normative — cite it, never paraphrase it (`34.9-GUARD-PROOF.md` §2.5 §A3). After the developer reports back, record their observations into the §9 and §10 `Observed:` fields and commit the pin together with the `electron-builder.yml` marker removal as ONE commit, per §9 criterion 7.</action>
  <what-built>
Scored Item 2 published six real archives and four audit assets from one identified run.
`meta/pinRunnerDigests.ts` exists and is unit-tested against mocked fetches, but has **never run
against the real release** — there was nothing to fetch until now.
  </what-built>
  <how-to-verify>
Run §9's eight criteria and §10's nine criteria in order, capturing every block with §5's redirect
form under a distinct filename, and reading each exit file back with `xxd`.

The three criteria most often skipped under time pressure, called out so they are not:

- **§9 criterion 6** — independently re-derive at least one pinned digest per arch by downloading the
  archive and running `shasum -a 256` locally. A mutating command's own success report is not proof
  of its effect (F-34.9-02). Without this, Scored Item 3 is a self-report.
- **§9 criterion 7** — remove `electron-builder.yml`'s
  `[REMOVE WHEN DIGESTS ARE PINNED -- 34.16-LIVE-GATE.md Scored Item 3]` marker and rewrite the
  sentence it tags, **in the same commit that pins the digests**. That prose becomes
  stale-by-behaviour on exactly this commit; deferring it is what CONTEXT.md constraint 4 forbids.
- **§10 criteria 6-8** — the deliberate one-character digest corruption. **A green fetch alone proves
  nothing here**: the pin script read those digests from the same publication the fetch verifies
  against, so a green-only result is near-tautological. Criterion 6 (it throws, naming the run id per
  D-11) and criterion 7 (it wrote nothing under `public/bin`) are two separate claims needing two
  separate measurements. Back up by `cp` first; restore by `cp` and re-verify by `shasum -a 256`.
  **Never `git checkout -- <path>`** — it fires `.husky/post-checkout`, whose
  `pnpm download-helper-binaries` step would then fire a full runner re-download at an arbitrary
  moment. Never `git stash`, never `git reset`.

**§10 criterion 4 is also the input to Scored Item 5** — record `find <tree> | wc -l` and
`find <tree> -type l | wc -l` for all six trees. The x64 numbers are the first measurement of that
tree's shape that has ever existed.

Confirm `shasum -a 256 meta/releaseTags.ts` still equals Precondition 7's value (§9 criterion 8).
  </how-to-verify>
  <acceptance_criteria>
- §9's eight criteria and §10's nine criteria each have a recorded observation citing its number.
- All six digests are 64-char lowercase hex; the `digests` key set is unchanged; `runId` equals Scored Item 2's recorded value; `layout` is still `onedir-v1`.
- At least one digest per arch was independently recomputed locally and matched.
- `grep -c 'REMOVE WHEN DIGESTS ARE PINNED -- 34.16-LIVE-GATE.md Scored Item 3' electron-builder.yml` prints `0` and `grep -c 'ARM64 ONLY' electron-builder.yml` prints `0`, both in the same commit as the pin.
- The corrupted-digest run exited non-zero (`xxd` shows `310a`), its message named the archive, both digests, `refusing to write anything under public/bin`, and the pinned run id; and a `find public/bin -newer <backup>` listed nothing.
- `meta/runnersOnedirDigests.json` was restored to its pinned state by `cp`, proven by `shasum -a 256`.
- All three x64 runners printed a version string and exited 0 under `arch -x86_64`.
- `shasum -a 256 meta/releaseTags.ts` unchanged from Precondition 7.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the six per-tree file/symlink counts, or describe which numbered criterion failed.</resume-signal>
  <verify>
    <automated>python3 -c "import json,re; d=json.load(open('meta/runnersOnedirDigests.json')); v=list(d['digests'].values()); assert all(re.fullmatch(r'[0-9a-f]{64}', x) for x in v), v; assert len(v)==6; assert d['layout']=='onedir-v1'; assert d['runId']; print('OK six real digests + runId', d['runId'])" && grep -c 'REMOVE WHEN DIGESTS ARE PINNED' electron-builder.yml</automated>
  </verify>
  <done>Six real digests and the source run id are pinned; at least one digest per arch was independently recomputed; the `electron-builder.yml` marker is gone in the same commit; the corruption injection threw and wrote nothing; all three x64 runners ran under Rosetta; `meta/releaseTags.ts` byte-unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN GATE — the x64 guard branch and its CI execution (Scored Items 5 and 6)</name>
  <read_first>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md §11 and §12, .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-RESEARCH.md §Q3 (the decision procedure — NORMATIVE)</read_first>
  <files>.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</files>
  <action>Hand §11's six criteria and §12's five criteria to the developer as written, cited by number, and STOP. Do NOT pre-classify §11 criterion 2's branch verdict, and do not offer an expected outcome alongside the criteria — the whole point of this item is that the result is unobserved, and an executor primed with a prediction is how a BUILD BUG gets recorded as a PASS-with-caveats. After the developer reports back, record their observations, then fill the gate's `verdict`, `run_date`, `items_passed`, `items_failed`, `items_not_attempted` and `launch_ordinals_used` frontmatter fields and write the `## RUN RECORD` section.</action>
  <what-built>
Scored Item 4 produced the first real x64 darwin onedir tree that has ever existed on any machine or
in this repository, and measured its file and symlink counts.
  </what-built>
  <how-to-verify>
**Do not approach §11 expecting a PASS.** Whether a real x64 onedir tree passes the guard's
framework-structure checks has never been observed; ledger item 12 refuses to assert either
direction and CONTEXT.md constraint 3 requires this phase to preserve that refusal. The static
evidence (zero arch literals in `meta/verifyRunnerBundle.ts`'s check logic, zero in
`meta/preserveRunnerSymlinks.ts`) is input to §11's decision procedure, not a verdict.

Run §11's six criteria. Criterion 2 records exactly one of three branch verdicts — **CLEAN PASS**,
**BUILD BUG**, or **GUARD BUG** — applying §11's decision procedure to the guard's own per-check
failure output. Never record "mostly passed" or "passed with warnings". If the message alone is not
conclusive, inspect the reported path with `ls -la` and `file <path>` and record that output BEFORE
classifying.

A **BUILD BUG** or **GUARD BUG** verdict is a FAIL of Scored Item 5 and routes to a gap cycle. Record
the classification and its evidence and stop; do not edit `meta/verifyRunnerBundle.ts` mid-gate
(§11 criterion 6 asserts that file is byte-unchanged across the whole item).

A CLEAN PASS still requires criterion 3 — the broken-symlink injection making the guard go red with
`x64` in its message. **D-07 is explicit that CI green is execution evidence only**; a green-only
result satisfies neither direction of the bar.

Then run §12's five criteria. The vehicle is
`gh workflow run build-main.yml --ref fix/steam-native-install-stability` — `build-base.yml` is
`workflow_call`-only, and both its callers already exist on `gamelib/main`, so no second push is
needed. **Known branch:** `gh secret list` returned only `TAURI_SIGNING_PRIVATE_KEY` and
`TAURI_SIGNING_PRIVATE_KEY_PASSWORD` on this fork (verified 2026-08-24) — `WORKFLOW_TOKEN` does not
exist. If the run fails for a credentials reason **before** reaching `pnpm dist:mac`, record Scored
Item 6 as **NOT ATTEMPTED** with the failing step named. Not FAIL, and above all not PASS by
inference from the local runs in Scored Item 5.

§12 criterion 3 is the closure of ledger item 13: **two distinct `verify:runner-bundle` banners, one
per arch, both before any `electron-builder  version=` line**, with their log line numbers recorded.
One banner is a FAIL of that criterion.

§12 criterion 5 measures D-06's "no new wiring" claim rather than assuming it: `git diff` must show
no change to `build-base.yml`, `install-deps/action.yml`, `build-main.yml` or `build-prs.yml`.

Finally, fill the gate's frontmatter — `verdict`, `run_date`, `items_passed`, `items_failed`,
`items_not_attempted`, `launch_ordinals_used` — and write the `## RUN RECORD` section with a
per-item outcome table, any findings, and any deviations. §13's rule is **no partial pass**: all six
scored items must PASS. A NOT ATTEMPTED item is not a PASS and leaves its requirement open with a
dated, named precondition in `deferred-items.md` style.
  </how-to-verify>
  <acceptance_criteria>
- §11's six criteria each have a recorded observation citing its number, and criterion 2 records exactly one of `CLEAN PASS` / `BUILD BUG` / `GUARD BUG`.
- The broken-symlink injection is recorded with the framework path, the guard's non-zero exit (`xxd`-verified), and the message text containing `x64`.
- `shasum -a 256 meta/verifyRunnerBundle.ts` is identical before and after Scored Item 5.
- The x64 tree was restored and its `Versions/Current` `readlink` output recorded.
- §12's five criteria each have a recorded observation, or Scored Item 6 is recorded NOT ATTEMPTED with the failing step named.
- If attempted: both per-arch `verify:runner-bundle` banners are recorded with log line numbers, both preceding any `electron-builder  version=` line.
- `git diff` shows no change to `build-base.yml`, `install-deps/action.yml`, `build-main.yml` or `build-prs.yml`.
- The gate's frontmatter carries a real `verdict`, `run_date`, and item counts summing to 6; no field still reads `TBD`.
- No sentence anywhere in the RUN RECORD describes item 18 / C2-05's closure as "pinned".
  </acceptance_criteria>
  <resume-signal>Type "approved" with the §11 branch verdict and the §12 disposition, or describe which numbered criterion failed.</resume-signal>
  <verify>
    <automated>grep -c 'TBD' .planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-LIVE-GATE.md</automated>
  </verify>
  <done>§11's branch verdict is recorded as exactly one of CLEAN PASS / BUILD BUG / GUARD BUG with its evidence; the broken-symlink injection went red naming `arch=x64`; `meta/verifyRunnerBundle.ts` is byte-unchanged; §12 is PASS or NOT ATTEMPTED with the failing step named; the gate's frontmatter carries a real verdict and item counts summing to 6, with no field left `TBD`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this worktree -> `gamelib/main` (a push) | Local objects cross into the fork's default branch, from which any collaborator's workflow can be dispatched |
| `origin` (Heroic upstream) vs `gamelib` (the fork) | Two remotes, one of which is an unrelated third-party project |
| the rolling release -> `public/bin` on this machine | Remote archive bytes cross into executable files that later ship inside a packaged app |
| a packaged x64 build -> `electron-updater` feed | Real users' auto-update channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.16-04 | Tampering | pushing to `origin` (Heroic upstream) by muscle memory | mitigate | Every push command in this plan names `gamelib` explicitly; Precondition 4 records `git remote -v` before any push; an acceptance criterion asserts the recorded command contains no `origin` and no `--force`. |
| T-34.16-21 | Tampering | the `main` commit carrying more than the one intended file | mitigate | `git diff-tree --name-status` between `MAIN_SHA` and the new commit must print exactly one `A` line, checked BEFORE the push, with an explicit ABORT instruction on anything else. |
| T-34.16-01 | Tampering | a corrupted or substituted runner archive extracted into `public/bin` | mitigate | Scored Item 4's deliberate one-character corruption proves the fail-closed digest gate throws AND writes nothing — two separately measured claims, not one. Scored Item 3 criterion 6 independently re-derives a pinned digest rather than trusting the pin script's own report. |
| T-34.16-03 | Tampering | an unverified x64 build reaching the auto-update channel | mitigate | Plan 34.16-01's `--arch=x64` invocation precedes `electron-builder -p always`; Scored Item 5 proves the guard can actually fail on an x64 tree, and Scored Item 6 proves it executes. **Unpinned against regression per D-09** — never described as "pinned". |
| T-34.16-06 | Information Disclosure | secrets pasted into a committed transcript | mitigate | §4's redaction discipline, plus a mandatory `grep -i 'ghp_\|github_pat_\|BEGIN .*PRIVATE KEY'` sweep of the session directory before any transcript excerpt is committed. |
| T-34.16-22 | Repudiation | a gate whose author also filled its results | mitigate | The gate document was authored by the planner with every result field left `TBD`; this plan is the sole writer of results. Same author/runner separation as `34.9-LIVE-GATE.md`/`-RERUN.md`. |
| T-34.16-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages. RESEARCH.md records the Package Legitimacy Gate as skipped because the phase adds no external dependency. |
</threat_model>

<verification>
- `34.16-LIVE-GATE.md` frontmatter has no remaining `TBD`; `items_passed + items_failed + items_not_attempted == 6`.
- Every `Observed:` field is filled, each observation citing the criterion number it discharges.
- `shasum -a 256 meta/releaseTags.ts` matches Precondition 7 at the end of the plan.
- `pnpm jest --selectProjects Meta` — still 1 failed / 21 passed, confined to `genI18nGateScope.test.ts`.
- `pnpm jest --selectProjects Backend` — still 1 failed suite / 3 failed tests, confined to `decompressPool.test.ts`.
- `git status --porcelain` shows no leftover backup or scratch artifact.
</verification>

<success_criteria>
- All six scored items PASS (§13's no-partial-pass rule), OR each non-PASS item is recorded FAIL/NOT ATTEMPTED with its criterion number and a dated, named precondition.
- `gamelib/main` carries `build-runners-onedir-macos.yml`, byte-identical to `HEAD`'s copy, added by a single-file commit.
- `meta/runnersOnedirDigests.json` holds six real digests and the source run id; `layout` unchanged.
- `electron-builder.yml`'s pin-time marker is gone, removed in the pinning commit.
- The x64 guard's behaviour is classified by observation, in both directions, never predicted.
- `meta/releaseTags.ts` byte-unchanged; no new CI wiring added anywhere.
</success_criteria>

<output>
Create `.planning/phases/34.16-macos-runner-onedir-x64-ci-leg-publish-the-workflow-to-the-d/34.16-06-SUMMARY.md` when done.

Also update `.planning/phases/34.9-.../deferred-items.md`: for each of items 1, 2, 3, 12, 13 and
18 / C2-05, append a dated closure line citing the scored item that closed it, or — for any item
whose scored item did not PASS — a dated line restating the blocker as a mechanism and naming the
new precondition. **Do not delete or rewrite any existing text in that ledger**; its entries are
append-only records, and the file's own convention is to flip a disposition with a new dated line
rather than an in-place edit.

If STATE.md is updated: snapshot it first, run, diff — `gsd-sdk query state.*` verbs corrupt STATE.md
on this repo — or hand-apply the edit.
</output>
