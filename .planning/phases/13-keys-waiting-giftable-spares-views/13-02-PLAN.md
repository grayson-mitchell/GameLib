---
phase: 13-keys-waiting-giftable-spares-views
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/humble/electronStores.ts
  - src/backend/humble/user.ts
  - src/backend/humble/library.ts
  - src/backend/humble/ipc_handler.ts
  - src/common/types/ipc.ts
  - src/backend/humble/__tests__/electronStores.test.ts
autonomous: true
requirements: [HVIEW-02]

must_haves:
  truths:
    - "D-59: A gifted-at timestamp keyed by machineName persists in its own electron-store file (humble_gifted_at), joining the D-04 disconnect-wipe exemption"
    - "humbleGiftedAtStore survives HumbleUser.disconnect() (never wiped), like humbleRevealedStore/humbleOwnershipOverrideStore"
    - "humbleRecordGiftLinkOpened re-validates ownedElsewhere && state === UNREVEALED server-side and no-ops otherwise"
    - "humbleGetGiftedAt returns a machineName→timestamp map the Spares view can read"
  artifacts:
    - path: "src/backend/humble/electronStores.ts"
      provides: "humbleGiftedAtStore CacheStore sibling"
      contains: "humble_gifted_at"
    - path: "src/backend/humble/library.ts"
      provides: "recordGiftLinkOpened + getAllGiftedAt on HumbleLibrary"
      exports: ["recordGiftLinkOpened", "getAllGiftedAt"]
    - path: "src/backend/humble/ipc_handler.ts"
      provides: "humbleRecordGiftLinkOpened + humbleGetGiftedAt handlers with server-side validation"
      contains: "humbleRecordGiftLinkOpened"
    - path: "src/common/types/ipc.ts"
      provides: "IPC signatures for the two new channels"
      contains: "humbleRecordGiftLinkOpened"
  key_links:
    - from: "src/backend/humble/user.ts"
      to: "humbleGiftedAtStore"
      via: "disconnect carve-out comment (NOT a .clear() call)"
      pattern: "humbleGiftedAtStore"
    - from: "src/backend/humble/ipc_handler.ts"
      to: "HumbleLibrary.recordGiftLinkOpened"
      via: "addHandler after server-side membership re-check"
      pattern: "recordGiftLinkOpened"
---

<objective>
Add the backend persistence + IPC for the Giftable-Spares gift action: a per-key gifted-at
timestamp store that guards against double-gifting, a server-side-validated write channel, and
a read channel the Spares view queries. No new Humble network calls (D-57 resolved to a static
deep-link; GameLib never possesses a gift-link value).

Purpose: Provide the disconnect-surviving persistence and the validated IPC surface that Plan 04's
Giftable Spares view depends on, following the exact `humbleOwnershipOverrideStore` /
`humbleSetOwnershipOverride` precedents.

Output: Extended electronStores.ts, user.ts carve-out, library.ts, ipc_handler.ts, ipc.ts, plus a
disconnect-survival store test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-CONTEXT.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-RESEARCH.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-PATTERNS.md

<interfaces>
Existing precedents the executor mirrors (already read into the pattern map):

electronStores.ts — humbleOwnershipOverrideStore:
  new CacheStore<{ overriddenAt: number }, string>('humble_ownership_override', null)
  exported in the file's final `export { ... }` block.

library.ts — setOwnershipOverride (lines ~338): sets the override store then recomputes; exported
  from `export const HumbleLibrary = { ... }` (lines ~632-640). getKeys() (lines ~280) is the
  read-side analog.

ipc_handler.ts — humbleSetOwnershipOverride (lines ~39-54): finds the key by machineName,
  logWarning + return (no-op) when validation fails, otherwise calls the library mutator.

common/types/ipc.ts — humbleSetOwnershipOverride (line 282), humbleClearOwnershipOverride (line 283)
  in the AsyncIPCFunctions interface. openExternalUrl (line 71) already exists — do NOT add a new
  open channel.

user.ts — disconnect(): store clears at lines 484-490 (configStore/humbleLibraryStore/humbleSyncStore
  .clear()); carve-out comment at lines 514-524 naming humbleRevealedStore + humbleOwnershipOverrideStore
  as deliberately excluded. wipeSteps (lines 497-503) is session-clear only — not a store list.

__tests__/electronStores.test.ts — mocks electron-store, imports the stores, has a
  humbleOwnershipOverrideStore describe block with a disconnect-survival test (clear the three wiped
  stores, assert the override store still has the key).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: humbleGiftedAtStore + disconnect carve-out + survival test</name>
  <files>src/backend/humble/electronStores.ts, src/backend/humble/user.ts, src/backend/humble/__tests__/electronStores.test.ts</files>
  <read_first>
    - src/backend/humble/electronStores.ts (full file — humbleOwnershipOverrideStore declaration + export block to mirror)
    - src/backend/humble/user.ts lines 471-525 (disconnect: the store-clear lines 484-490 and the carve-out comment 514-524 — the exact block to extend)
    - src/backend/humble/__tests__/electronStores.test.ts (the humbleOwnershipOverrideStore survival-test block to copy with names swapped)
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-RESEARCH.md (Pitfall 5 — the highest-risk regression in this phase)
  </read_first>
  <behavior>
    - humbleGiftedAtStore.set(machineName, { giftedAt }) then .has(machineName) === true.
    - After configStore.clear() + humbleLibraryStore.clear() + humbleSyncStore.clear(), humbleGiftedAtStore.get(machineName) still returns { giftedAt } (disconnect survival, D-59/D-04).
  </behavior>
  <action>
    In `electronStores.ts`, add `const humbleGiftedAtStore = new CacheStore<{ giftedAt: number }, string>('humble_gifted_at', null)` immediately after `humbleOwnershipOverrideStore`, with a
    doc-comment mirroring that store's wording: cite D-59 (reinterpreted per D-57 research — records
    when the user confirmed the "Open Humble" gift action, keyed by machineName; guards against
    double-gifting), and state it is NEVER cleared by HumbleUser.disconnect() and must not be added to
    the disconnect store-clears. Add `humbleGiftedAtStore` to the file's final `export { ... }` block.
    In `user.ts` `disconnect()`, do NOT add any `humbleGiftedAtStore.clear()` call near lines 484-490;
    instead extend the carve-out comment at lines 514-524 to also name `humbleGiftedAtStore` (three
    excluded stores now, not two) with a one-clause reason mirroring the existing D-04/D-42 rationale
    (a gifted-at record must survive a disconnect/reconnect so the double-gift guard is not silently
    dropped). In `electronStores.test.ts`, add a `describe('humbleGiftedAtStore', ...)` block copying
    the existing override-store survival test structure: a creation/keying test under store name
    `humble_gifted_at`, and a D-59 survival test that sets a gifted-at, clears
    configStore/humbleLibraryStore/humbleSyncStore, then asserts the gifted-at value persists.
  </action>
  <verify>
    <automated>pnpm test -- src/backend/humble/__tests__/electronStores.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "humble_gifted_at" src/backend/humble/electronStores.ts` ≥ 1
    - `humbleGiftedAtStore` appears in the `export { ... }` block of electronStores.ts
    - `grep -c "humbleGiftedAtStore" src/backend/humble/user.ts` ≥ 1 AND it appears ONLY inside the carve-out comment block (no `.clear()` call on it, and it is not near lines 484-490)
    - electronStores.test.ts has a test that clears configStore/humbleLibraryStore/humbleSyncStore and asserts humbleGiftedAtStore still has the key
    - `pnpm test -- src/backend/humble/__tests__/electronStores.test.ts` exits 0
  </acceptance_criteria>
  <done>Gifted-at store exists, is exported, is excluded from disconnect wipes by comment carve-out, and survival is proven by a passing test.</done>
</task>

<task type="auto">
  <name>Task 2: library.ts record/read functions + validated IPC channels</name>
  <files>src/backend/humble/library.ts, src/backend/humble/ipc_handler.ts, src/common/types/ipc.ts</files>
  <read_first>
    - src/backend/humble/library.ts lines 280-286 (getKeys) and 338-350 (setOwnershipOverride/clearOwnershipOverride) and 632-640 (HumbleLibrary export block)
    - src/backend/humble/ipc_handler.ts lines 39-55 (humbleSetOwnershipOverride/humbleClearOwnershipOverride handlers — the validate-then-mutate + logWarning-on-reject shape)
    - src/common/types/ipc.ts lines 71, 282-283 (openExternalUrl + humble override signatures)
    - src/backend/humble/electronStores.ts (humbleGiftedAtStore added in Task 1)
  </read_first>
  <action>
    In `library.ts`, import `humbleGiftedAtStore` from electronStores and add two functions in the
    style of `setOwnershipOverride`: `recordGiftLinkOpened(machineName: string): void` that calls
    `humbleGiftedAtStore.set(machineName, { giftedAt: Date.now() })` (NO recomputeOwnership call —
    gifting does not affect classification/ownership), and `getAllGiftedAt(): Record<string, number>`
    that reads every gifted-at entry and returns a plain map of machineName→giftedAt milliseconds
    (iterate the store's keys; return `{}` when empty). Add both to the `HumbleLibrary` export object.
    In `ipc_handler.ts`, add `addHandler('humbleRecordGiftLinkOpened', ...)` mirroring
    `humbleSetOwnershipOverride`: find the key via `HumbleLibrary.getKeys().find(k => k.machineName === machineName)`,
    and if the key is missing OR `!key.ownedElsewhere` OR `key.state !== 'UNREVEALED'`, `logWarning`
    (log only the machineName — C4: never log a URL or token) and return without persisting; otherwise
    call `HumbleLibrary.recordGiftLinkOpened(machineName)`. Add `addHandler('humbleGetGiftedAt', () => HumbleLibrary.getAllGiftedAt())`.
    In `common/types/ipc.ts` AsyncIPCFunctions, add `humbleRecordGiftLinkOpened: (machineName: string) => Promise<void>`
    and `humbleGetGiftedAt: () => Promise<Record<string, number>>` directly after
    `humbleClearOwnershipOverride` (line 283), each with a doc-comment noting D-59/D-57 resolution and
    that the backend validates ownedElsewhere+UNREVEALED server-side (never trust renderer-only gating).
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
  </verify>
  <acceptance_criteria>
    - library.ts exports `recordGiftLinkOpened` and `getAllGiftedAt` from the `HumbleLibrary` object
    - `recordGiftLinkOpened` does NOT call `recomputeOwnership`
    - ipc_handler.ts `humbleRecordGiftLinkOpened` handler rejects (logWarning + return) when `!key.ownedElsewhere || key.state !== 'UNREVEALED'`
    - `grep -c "humbleRecordGiftLinkOpened\|humbleGetGiftedAt" src/common/types/ipc.ts` ≥ 2
    - ipc_handler.ts log line for the reject path references machineName only (no URL/token interpolation)
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
  <done>Both channels registered with server-side membership validation; library read/write functions exported; types compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | The Spares view triggers humbleRecordGiftLinkOpened with a machineName string — untrusted renderer input crosses into a persisted write |
| main → disk (electron-store) | gifted-at timestamps persist to a store that must survive disconnect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | humbleRecordGiftLinkOpened IPC handler | mitigate | Re-validate ownedElsewhere && state === UNREVEALED server-side against the live key set; logWarning + no-op on mismatch (never trust the renderer only showed the button on eligible rows) — mirrors humbleSetOwnershipOverride's non-fuzzy rejection |
| T-13-03 | Information Disclosure | ipc_handler.ts / library.ts log lines | mitigate | Log only machineName (an internal identifier, not a secret); never log a URL, gamekey, or any Humble-issued token (C4 discipline). GameLib never possesses a gift-link value in this flow |
| T-13-04 | Tampering (data integrity) | user.ts disconnect wipe path | mitigate | humbleGiftedAtStore is excluded from the disconnect store-clears via the carve-out comment; a disconnect-survival unit test asserts the record persists (prevents the Pitfall 5 regression) |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH.md audit: not applicable); no install task exists |
</threat_model>

<verification>
- `pnpm test -- src/backend/humble/__tests__/electronStores.test.ts` green
- `pnpm codecheck` exits 0
- Full backend suite `pnpm test -- src/backend/humble` green
</verification>

<success_criteria>
- Gifted-at store persists, survives disconnect, and is proven by test
- Both IPC channels validate membership server-side and compile against the frontend contract
- No secret/URL/token is ever logged; no new Humble network call added
</success_criteria>

<output>
Create `.planning/phases/13-keys-waiting-giftable-spares-views/13-02-SUMMARY.md` when done
</output>
