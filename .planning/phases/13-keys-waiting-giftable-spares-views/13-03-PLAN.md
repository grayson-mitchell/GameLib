---
phase: 13-keys-waiting-giftable-spares-views
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/frontend/App.tsx
  - src/frontend/screens/Humble/Keys/index.tsx
  - src/frontend/screens/Humble/Keys/All/index.tsx
  - src/frontend/screens/Humble/Keys/Waiting/index.tsx
  - src/frontend/screens/Humble/Keys/components/UrgencyBadge/index.tsx
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
  - src/frontend/screens/Humble/Keys/index.css
  - public/locales/en/translation.json
autonomous: true
requirements: [HVIEW-01]

must_haves:
  truths:
    - "Opening /humble-keys redirects to /humble-keys/waiting (default tab, D-50)"
    - "Three real sub-routes exist: /humble-keys/waiting, /humble-keys/spares, /humble-keys/all (D-51)"
    - "The All-keys tab renders the unchanged D-21 grouped list moved verbatim"
    - "Keys waiting lists selectKeysWaiting output as one flat list with the D-64 header blurb and count badge"
    - "Rows in claimable states within 30 days render a two-tier urgency badge (danger ≤7d, warning ≤30d)"
    - "The sync-status header (refresh/last-synced/fail-soft banner) renders once in the parent, above the tab bar"
  artifacts:
    - path: "src/frontend/screens/Humble/Keys/index.tsx"
      provides: "Parent route shell: route guard + shared sync header + tab nav + <Outlet/> + live counts"
      contains: "Outlet"
    - path: "src/frontend/screens/Humble/Keys/All/index.tsx"
      provides: "All-keys tab (verbatim D-21 grouped list)"
      contains: "GROUP_ORDER"
    - path: "src/frontend/screens/Humble/Keys/Waiting/index.tsx"
      provides: "HVIEW-01 Keys-waiting flat list + blurb + empty state"
      contains: "selectKeysWaiting"
    - path: "src/frontend/screens/Humble/Keys/components/UrgencyBadge/index.tsx"
      provides: "Presentational urgency badge (D-61/62/63 copy + tier color)"
      exports: ["default"]
    - path: "src/frontend/App.tsx"
      provides: "Nested humble-keys parent+child route table"
      contains: "'waiting'"
  key_links:
    - from: "src/frontend/App.tsx"
      to: "src/frontend/screens/Humble/Keys/Waiting"
      via: "child route lazy import"
      pattern: "Keys/Waiting"
    - from: "src/frontend/screens/Humble/Keys/Waiting/index.tsx"
      to: "src/common/humble/viewFilters.ts"
      via: "selectKeysWaiting import"
      pattern: "selectKeysWaiting"
    - from: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      to: "src/common/humble/urgencyBadge.ts"
      via: "UrgencyBadge fed by getUrgencyTier"
      pattern: "urgencyTier"
---

<objective>
Turn the single Humble Keys page into a three-tab, real-sub-route surface and ship the first two
tabs: All keys (verbatim D-21 grouped list) and Keys waiting (HVIEW-01). Add the presentational
urgency badge and extend HumbleKeyRow with an optional urgency-tier prop that renders in every tab.

Purpose: Establish the tab shell and route paths that Phase 14's C2 guard depends on, and deliver
HVIEW-01 without regressing the existing All-keys layout (Pitfall 4). The gift action and Spares tab
land in Plan 04.

Output: Nested route table, parent shell, All + Waiting child routes, UrgencyBadge component,
HumbleKeyRow urgency extension, CSS, and i18n strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-CONTEXT.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-RESEARCH.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-PATTERNS.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-UI-SPEC.md
@src/frontend/screens/Humble/Keys/index.tsx
@src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nested route table + parent shell + verbatim All-keys move</name>
  <files>src/frontend/App.tsx, src/frontend/screens/Humble/Keys/index.tsx, src/frontend/screens/Humble/Keys/All/index.tsx</files>
  <read_first>
    - src/frontend/App.tsx lines 119-198 (createHashRouter + makeLazyFunc + the flat 'humble-keys' entry at 177-180 + the Navigate usage in the '*' route)
    - src/frontend/screens/Humble/Keys/index.tsx (full file — the entire current page; the render body at lines 187-207 moves to All, the guard/header/cooldown logic stays)
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-PATTERNS.md (parent index.tsx + App.tsx sections — exact move boundaries and NavLink render-prop shape)
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-UI-SPEC.md (Routes block + Tab bar section — locked path segments, active/inactive tab colors, count-badge chrome)
    - src/frontend/components/UI/Sidebar/components/SidebarItem/index.tsx (NavLink `className={({isActive}) => classNames(...)}` render-prop analog)
  </read_first>
  <action>
    In `App.tsx`, replace the flat `humble-keys` route entry with a parent route keeping
    `lazy: makeLazyFunc(import('./screens/Humble/Keys'))` and adding a `children` array:
    `{ index: true, element: <Navigate to="waiting" replace /> }` (D-50), then three child routes
    with `path: 'waiting' | 'spares' | 'all'` each lazy-importing `./screens/Humble/Keys/Waiting`,
    `.../Spares`, `.../All`. Reuse the `Navigate` import already present in this file; keep
    `createHashRouter` (do not switch router type). NOTE: the `Spares` child imports a component that
    Plan 04 creates — to keep Wave 2 self-contained and compiling, in THIS plan create a minimal
    placeholder `Spares/index.tsx` default-exporting a component that renders the Spares header blurb
    + empty-state copy only (Plan 04 replaces it with the full gift-enabled view); alternatively wire
    the `spares` child in Plan 04 — but the App.tsx children array must not reference a non-existent
    module, so ship the placeholder here.
    Move the grouped-list render body from `index.tsx` (the `hasKeys ? (<div className="humbleKeysGroupList">…GROUP_ORDER.map…) : (<div className="humbleKeysEmptyState">…)` block) plus the
    `groupAndSortKeys(keys)`/`hasKeys` computation and the `GROUP_ORDER`/`groupAndSortKeys`/`HumbleKeyGroup`
    imports into a new `All/index.tsx` VERBATIM (Pitfall 4: do not alter spacing, group order, or
    collapse defaults). All/index.tsx reads `humble.keys` from ContextProvider itself.
    Rewrite parent `index.tsx` to: keep the D-20 route guard (`if (!humble?.isLoggedIn) return <Navigate to={humbleLoginPath} replace />`), keep the entire sync-status header + cooldown/progress
    listener logic (lines 43-185 region: refresh button, spinner, last-synced text, fail-soft
    WarningMessage banner) rendered ONCE above a new tab bar, then render the tab bar (three `<NavLink>`
    with `to="waiting"/"spares"/"all"` relative to the parent, active class using `var(--accent)` /
    inactive `var(--text-secondary)` per UI-SPEC) followed by `<Outlet />`. Compute
    `selectKeysWaiting(humble.keys ?? []).length` and `selectGiftableSpares(humble.keys ?? []).length`
    in the parent and render them as count badges in the waiting/spares tab labels only (D-52; "All keys"
    uncounted), reusing the `.humbleKeyGroupCount` pill chrome. Import `<Outlet/>` and `NavLink` from
    react-router-dom.
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx `humble-keys` route has a `children` array with an index `<Navigate to="waiting" replace />` and three paths 'waiting', 'spares', 'all'
    - `src/frontend/screens/Humble/Keys/All/index.tsx` exists and contains `GROUP_ORDER` and `HumbleKeyGroup` (moved verbatim)
    - parent `index.tsx` contains `<Outlet` and no longer contains `GROUP_ORDER.map` (that logic moved to All)
    - parent `index.tsx` renders three `NavLink` elements with `to="waiting"`, `to="spares"`, `to="all"`
    - parent `index.tsx` still contains the `humbleLoginPath` route guard and the `humbleKeysRefreshButton`
    - `grep -c "selectKeysWaiting\|selectGiftableSpares" src/frontend/screens/Humble/Keys/index.tsx` ≥ 2 (counts computed in parent)
    - `pnpm codecheck` exits 0 (all child module imports resolve, including the Spares placeholder)
  </acceptance_criteria>
  <done>Route table nested with default redirect; All-keys layout preserved verbatim in its own child; parent shell renders shared header + tab nav + Outlet + counts; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Keys-waiting view + UrgencyBadge + HumbleKeyRow urgency extension + CSS + i18n</name>
  <files>src/frontend/screens/Humble/Keys/Waiting/index.tsx, src/frontend/screens/Humble/Keys/components/UrgencyBadge/index.tsx, src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx, src/frontend/screens/Humble/Keys/index.css, public/locales/en/translation.json</files>
  <read_first>
    - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx (full file — the Props type, the state-badge span at lines 52-56, the owned-badge + D-42 override block at 67-87 that MUST stay verbatim)
    - src/common/humble/urgencyBadge.ts (getUrgencyTier + getUrgencyCountdownParts signatures from Plan 01)
    - src/common/humble/viewFilters.ts (selectKeysWaiting from Plan 01)
    - src/frontend/screens/Humble/Keys/index.css lines 198-225 (`.humbleKeyStateBadge` family chrome to copy for `.humbleUrgencyBadge`)
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-UI-SPEC.md (Copywriting Contract — exact locked strings; Row extension + Urgency badge eligibility sections)
    - public/locales/en/translation.json line 543 (the `humbleKeys` block to extend)
  </read_first>
  <action>
    Create `components/UrgencyBadge/index.tsx`: a presentational component with props
    `{ tier: UrgencyTier; expiration: string | null }` that returns null when `tier === null || expiration === null`,
    otherwise renders a `<span className={\`humbleUrgencyBadge humbleUrgencyBadge--${tier}\`}>` whose
    text is derived from `getUrgencyCountdownParts(expiration)` mapped to the LOCKED copy:
    `{{H}}h left` for the hours kind, `1 day left` when value===1 for the days kind, else `{{N}} days left`
    (use `t()` with new keys under `humbleKeys.*`). Import `getUrgencyCountdownParts` and `UrgencyTier`
    from `common/humble/urgencyBadge`.
    Extend `HumbleKeyRow`'s Props with two OPTIONAL fields: `urgencyTier?: UrgencyTier` (D-63, all tabs)
    and `giftAction?: { giftedAt: number | null; onGift: () => void }` (D-60, Spares only — declared now,
    rendered in Plan 04). Render `<UrgencyBadge tier={urgencyTier ?? null} expiration={humbleKey.expiration} />`
    immediately beside the existing state-badge span. Do NOT touch or relocate the owned-badge + D-42
    override block (lines 67-87) — it must render byte-for-byte as today (Pitfall 3 safety valve). Update
    the file's top D-22 read-only doc-comment to note the new Spares-only gift-action exception alongside
    the existing D-42 exception.
    Create `Waiting/index.tsx`: read `humble.keys` from ContextProvider, compute
    `selectKeysWaiting(humble?.keys ?? [])`, render the D-64 header blurb `<p>` with locked copy
    "Keys you don't own yet — claim them before they expire.", then a single flat `<ul>` mapping each key
    to `<HumbleKeyRow key={...} humbleKey={key} urgencyTier={getUrgencyTier(key.state, key.expiration)} />`
    (import getUrgencyTier). Do NOT use HumbleKeyGroup (flat list, no section headers, D-56). Render the
    locked empty state (heading "You're all caught up", body "No keys are waiting to be claimed. Check the
    All keys tab to see your full inventory.") when the list is empty.
    In `index.css`, add `.humbleUrgencyBadge` copying the `.humbleKeyStateBadge` chrome
    (`--space-3xs`/`--space-2xs` padding, `--space-3xs` radius, `--text-sm`/`--semibold`, `var(--neutral-01)`
    text) with `.humbleUrgencyBadge--danger { background: var(--status-danger); }` and
    `.humbleUrgencyBadge--warning { background: var(--status-warning); }`; add the tab-bar strip styles
    (`var(--background)` strip, active NavLink `var(--accent)`, inactive `var(--text-secondary)`,
    `--space-sm` vertical / `--space-md` horizontal per-tab padding) and a `.humbleKeysFlatList` list style.
    Use semantic tokens only — no hex, no px except the existing fixed exceptions.
    In `translation.json` `humbleKeys` block, add all new keys used above (tab labels with counts,
    waiting blurb, waiting empty title/body, urgency `daysLeft`/`oneDayLeft`/`hoursLeft`) using the EXACT
    locked copy from 13-UI-SPEC.md.
  </action>
  <verify>
    <automated>pnpm codecheck && pnpm test -- src/backend/humble/__tests__/viewFilters.test.ts src/backend/humble/__tests__/urgencyBadge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `UrgencyBadge/index.tsx` returns null when tier is null and otherwise emits `humbleUrgencyBadge--danger`/`--warning` class
    - `HumbleKeyRow` Props includes optional `urgencyTier` and `giftAction`; the owned-badge/D-42 override block is unchanged (still contains `humbleKeys.notTheSameGame`)
    - `Waiting/index.tsx` imports `selectKeysWaiting` and `getUrgencyTier`, renders a flat `<ul>` (no `HumbleKeyGroup` import)
    - `Waiting/index.tsx` contains the locked blurb string "claim them before they expire" and empty-state "You're all caught up"
    - index.css defines `.humbleUrgencyBadge--danger` with `var(--status-danger)` and `--warning` with `var(--status-warning)`; `grep -c "#" ` on the added block finds no hex color
    - translation.json parses (`node -e "require('./public/locales/en/translation.json')"` exits 0) and contains the new humbleKeys urgency + waiting keys
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
  <done>Keys-waiting tab renders the sorted flat list with header blurb, urgency badges show for ≤30-day claimable rows, the read-only safety valve is intact, and strings match the locked copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route guard (logged-out → login) | The parent route inherits the D-20 `humbleLoginPath` bounce for all three child routes; no new privilege boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Information Disclosure | All-keys layout regression during router split | mitigate | Move the D-21 grouped-list body verbatim into All/index.tsx; codecheck + the human-verify checkpoint (Plan 05) confirm no group/collapse/spacing regression (Pitfall 4) |
| T-13-06 | Tampering | Safety-valve removal (D-42 override) while extending HumbleKeyRow | mitigate | HumbleKeyRow is extended, not forked; the owned-badge + override block is preserved unchanged and asserted by acceptance criteria (Pitfall 3 / WR-02) |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `pnpm codecheck` exits 0
- Pure-helper tests remain green (imports resolve): `pnpm test -- src/backend/humble/__tests__/viewFilters.test.ts src/backend/humble/__tests__/urgencyBadge.test.ts`
- Manual routing/urgency confirmation deferred to Plan 05 human-verify checkpoint
</verification>

<success_criteria>
- /humble-keys redirects to /humble-keys/waiting; all three tab routes resolve
- All-keys grouped list is byte-for-byte the prior behavior in its own child route
- Keys-waiting lists the sorted flat list with blurb, counts, and urgency badges
- Read-only + D-42 safety valve preserved on every row
</success_criteria>

<output>
Create `.planning/phases/13-keys-waiting-giftable-spares-views/13-03-SUMMARY.md` when done.
Record the locked route paths (/humble-keys/waiting, /spares, /all) in the summary — Phase 14's C2
guard consumes /humble-keys/spares.
</output>
