---
phase: 13-keys-waiting-giftable-spares-views
plan: 05
type: execute
wave: 4
depends_on: ["13-03", "13-04"]
files_modified: []
autonomous: false
requirements: [HVIEW-01, HVIEW-02]

must_haves:
  truths:
    - "Opening Humble Keys lands on the Keys waiting tab by default"
    - "Tab navigation, deep links, and browser back/forward work across waiting/spares/all"
    - "Keys waiting is sorted soonest-expiring first with ≤30-day urgency badges (danger ≤7d, warning ≤30d)"
    - "Giftable spares gift action shows the irreversibility warning; Cancel does nothing; Open Humble opens the Humble keys page and records the gifted annotation"
    - "All-keys tab is visually unchanged from the pre-Phase-13 grouped list"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the two new views end-to-end on a real connected Humble account, covering the
behaviors that the frontend jest project cannot assert automatically (no jsdom/DOM render available):
tab routing + default redirect, urgency-badge appearance, the gift confirmation dialog + external
open, and the All-keys no-regression check.

Purpose: Confirm HVIEW-01 and HVIEW-02 are actually usable and that the router refactor did not
regress the D-21 All-keys layout, before Phase 14 builds its C2 redirect on top of the spares route.

Output: Human sign-off (or a defect list to route into gap closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-CONTEXT.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-UI-SPEC.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate</name>
  <files></files>
  <read_first>
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-VALIDATION.md (full suite + per-task commands)
  </read_first>
  <action>
    Before pausing for the human, run the full automated gate so the human only verifies genuinely
    manual behaviors: `pnpm codecheck` (tsc + lint) and `pnpm test` (full jest suite across the backend
    and frontend projects). Confirm the Phase 13 pure-helper tests (viewFilters, urgencyBadge) and the
    humbleGiftedAtStore disconnect-survival test are green. If any command fails, stop and report — do
    not advance to the human checkpoint on a red gate.
  </action>
  <verify>
    <automated>pnpm codecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm codecheck` exits 0
    - `pnpm test` exits 0 with viewFilters.test.ts, urgencyBadge.test.ts, and the humbleGiftedAtStore tests all passing
  </acceptance_criteria>
  <done>Full automated gate green; ready for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the three-tab Humble Keys surface</name>
  <files></files>
  <what-built>
    Three-tab Humble Keys surface: Keys waiting (HVIEW-01, urgency-sorted with ≤30-day badges),
    Giftable spares (HVIEW-02, confirm-gated "Gift on Humble" deep-link + gifted annotation), and the
    unchanged All keys grouped list. Real react-router sub-routes with a default redirect to Keys waiting.
  </what-built>
  <action>
    Pause for the human to verify the running app. Walk them through the how-to-verify steps below;
    do not resume until they type "approved" or provide a defect list.
  </action>
  <how-to-verify>
    1. Start the app connected to a real Humble account: `pnpm start` (or the project's dev launch).
    2. Open Humble Keys from the sidebar → confirm it lands on the "Keys waiting" tab by default (URL ends /humble-keys/waiting).
    3. Keys waiting: confirm keys with the soonest expiration are at the top; keys with no expiration are below, alphabetical. Confirm keys expiring within 7 days show a red "N days left"/"Nh left" badge and 8–30 days show an orange badge; keys beyond 30 days and REDEEMED/UNREDEEMABLE show no urgency badge. Confirm the header blurb reads "Keys you don't own yet — claim them before they expire." Confirm the tab label shows a live count.
    4. Switch to "Giftable spares": confirm only owned-elsewhere + unrevealed keys appear; confirm the blurb "You already own these games — keep the keys unrevealed and gift them instead." Confirm any fuzzy-matched row still shows the "Likely owned on Steam" badge and the "Not the same game" override button.
    5. On a spare row click "Gift on Humble": confirm the confirmation dialog shows the irreversibility warning ("...once redeemed, it's gone for good..."). Click Cancel → confirm nothing opens. Click "Gift on Humble" again → "Open Humble" → confirm the default browser opens https://www.humblebundle.com/home/keys and the row now shows "Opened Humble gift page {date}".
    6. Switch to "All keys": confirm the grouped list looks exactly as it did before Phase 13 (same groups, order, collapse behavior) and shows NO gift action.
    7. Use the browser back button and paste a deep link (…/humble-keys/spares) to confirm sub-routes and back/forward work. Confirm the refresh button + last-synced text + any fail-soft banner render once, above the tabs, on every tab.
  </how-to-verify>
  <verify>
    <human-check>All seven verification steps pass on a real connected Humble account</human-check>
  </verify>
  <resume-signal>Type "approved" if all checks pass, or list the specific failures to route into gap closure.</resume-signal>
  <done>Human confirms all seven steps, or a defect list is captured for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no code changes, no new inputs or endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Repudiation | Manual sign-off completeness | mitigate | The checkpoint script enumerates every HVIEW-01/HVIEW-02 success criterion, the D-42 safety valve, and the D-21 no-regression check so no criterion is silently skipped |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; no install task exists |
</threat_model>

<verification>
- `pnpm codecheck` + `pnpm test` green (Task 1)
- Human sign-off on all seven checkpoint steps
</verification>

<success_criteria>
- All Phase 13 success criteria confirmed in the running app on a real account
- All-keys layout confirmed unchanged; fuzzy safety valve confirmed present in spares
- Route paths confirmed for Phase 14's C2 dependency
</success_criteria>

<output>
Create `.planning/phases/13-keys-waiting-giftable-spares-views/13-05-SUMMARY.md` when done
</output>
