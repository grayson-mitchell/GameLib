---
phase: 13-keys-waiting-giftable-spares-views
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - src/frontend/screens/Humble/Keys/Spares/index.tsx
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
  - src/frontend/screens/Humble/Keys/index.css
  - public/locales/en/translation.json
autonomous: true
requirements: [HVIEW-02]

must_haves:
  truths:
    - "Giftable spares lists selectGiftableSpares output (ownedElsewhere + UNREVEALED) with the D-64 header blurb"
    - "D-58 / D-60: Each spare row shows a 'Gift on Humble' action — existing ONLY in the Giftable spares view (D-60) — that opens a confirmation dialog EVERY time, with no 'don't ask again' (D-58), before any external navigation"
    - "Confirming the dialog records the gifted-at timestamp (humbleRecordGiftLinkOpened) and opens humblebundle.com/home/keys externally"
    - "D-59: A previously-gifted row shows the 'Opened Humble gift page {date}' annotation instead of hiding the action (double-gift guard)"
    - "The D-41 owned badge + D-42 override still render on every fuzzy-matched spare row"
  artifacts:
    - path: "src/frontend/screens/Humble/Keys/Spares/index.tsx"
      provides: "HVIEW-02 Giftable-spares view with gift action + confirm dialog + gifted-at read"
      contains: "selectGiftableSpares"
    - path: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      provides: "giftAction rendering (button or gifted annotation), Spares-only"
      contains: "giftAction"
  key_links:
    - from: "src/frontend/screens/Humble/Keys/Spares/index.tsx"
      to: "window.api.humbleGetGiftedAt"
      via: "useEffect fetch of gifted-at map"
      pattern: "humbleGetGiftedAt"
    - from: "src/frontend/screens/Humble/Keys/Spares/index.tsx"
      to: "window.api.humbleRecordGiftLinkOpened"
      via: "confirm-dialog confirm handler"
      pattern: "humbleRecordGiftLinkOpened"
    - from: "src/frontend/screens/Humble/Keys/Spares/index.tsx"
      to: "window.api.openExternalUrl"
      via: "confirm-dialog confirm handler (static URL)"
      pattern: "openExternalUrl"
---

<objective>
Replace the Plan 03 Spares placeholder with the full Giftable-Spares view (HVIEW-02): the
owned-elsewhere + UNREVEALED list, a confirmation-gated "Gift on Humble" deep-link action, and the
gifted-at double-gift guard annotation. This is the single new interactive affordance the phase adds
and Phase 14's C2 redirect destination.

Purpose: Complete HVIEW-02 by wiring the frontend gift action to the Plan 02 backend persistence and
the existing external-open IPC, behind the D-58 irreversibility confirmation, while preserving the
mandatory fuzzy-match safety valve.

Output: Full Spares/index.tsx, HumbleKeyRow gift-action rendering, CSS, and gift-dialog i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-CONTEXT.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-RESEARCH.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-PATTERNS.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-UI-SPEC.md
@src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Giftable Spares view + confirm-gated gift action + HumbleKeyRow gift rendering</name>
  <files>src/frontend/screens/Humble/Keys/Spares/index.tsx, src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx, src/frontend/screens/Humble/Keys/index.css, public/locales/en/translation.json</files>
  <read_first>
    - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx (the giftAction prop declared in Plan 03; the owned-badge + D-42 override block that MUST stay verbatim)
    - src/frontend/screens/Humble/Keys/Waiting/index.tsx (Plan 03 — the view shape to mirror: ContextProvider read, blurb, flat list, empty state)
    - src/common/humble/viewFilters.ts (selectGiftableSpares)
    - src/frontend/screens/Library/components/GameCard/index.tsx (showDialogModal destructure from ContextProvider + confirm/cancel button shape)
    - src/frontend/types.ts (DialogModalOptions / showDialogModal signature)
    - src/common/types/ipc.ts (humbleRecordGiftLinkOpened, humbleGetGiftedAt from Plan 02; openExternalUrl existing)
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-UI-SPEC.md (Copywriting Contract — exact locked gift dialog + blurb + empty-state + annotation copy)
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-PATTERNS.md (Spares/index.tsx section — gift-confirm dialog draft; HumbleKeyRow gift-render insertion point)
  </read_first>
  <action>
    Rewrite `Spares/index.tsx` (replacing the Plan 03 placeholder): read `humble.keys` and
    `showDialogModal` from ContextProvider; compute `selectGiftableSpares(humble?.keys ?? [])`. On mount,
    `useEffect` → `window.api.humbleGetGiftedAt()` into a `Record<string, number>` state map (machineName→ms).
    Render the D-64 header blurb `<p>` with locked copy "You already own these games — keep the keys
    unrevealed and gift them instead.", then a flat `<ul>` of `<HumbleKeyRow>` (same flat-list pattern as
    Waiting — no HumbleKeyGroup), passing to each row `urgencyTier={getUrgencyTier(key.state, key.expiration)}`
    (D-63 badges in this tab too) and `giftAction={{ giftedAt: giftedMap[key.machineName] ?? null, onGift: () => openGiftDialog(key) }}`.
    `openGiftDialog(key)` calls `showDialogModal({ showDialog: true, title: t('humbleKeys.giftConfirmTitle', 'Gift this key?'),
    message: <locked body copy>, buttons: [ Cancel → showDialogModal({ showDialog: false }); "Open Humble" →
    { void window.api.humbleRecordGiftLinkOpened(key.machineName); window.api.openExternalUrl('https://www.humblebundle.com/home/keys');
    optimistically set giftedMap[key.machineName]=Date.now(); showDialogModal({ showDialog: false }) } ] })`.
    Use the EXACT static URL `https://www.humblebundle.com/home/keys` (no gamekey/machineName interpolation —
    research A1/Open-Question 1; V5 threat avoided by using a literal URL). Do NOT add a clipboard write
    (D-57 resolved to deep-link, Pitfall 2). Do NOT add a "don't ask again" checkbox (D-58). Render the locked
    empty state (heading "No giftable spares", body "Keys show up here once they're confirmed owned elsewhere
    and haven't been revealed yet.").
    Extend `HumbleKeyRow` to render the `giftAction` prop ONLY when provided: if `giftAction.giftedAt !== null`,
    render the "Opened Humble gift page {{date}}" annotation (`new Date(giftedAt).toLocaleDateString()`),
    otherwise render a "Gift on Humble" `<button type="button">` (optionally with FontAwesome
    `faUpRightFromSquare`/`faExternalLinkAlt` per installed FA version) calling `giftAction.onGift`. Keep this
    strictly additive — the owned-badge + D-42 override block stays verbatim (Pitfall 3 safety valve). The gift
    action must render only for rows given a `giftAction` prop, i.e. never in Waiting/All (D-60).
    In `index.css`, add the "Gift on Humble" button + gifted-annotation styles using semantic tokens
    (`var(--accent)` hover consistent with the override link; `var(--text-secondary)` for the annotation caption).
    In `translation.json` `humbleKeys` block, add the gift keys with EXACT locked copy: giftConfirmTitle,
    giftConfirmBody ("Anyone with this link can claim the key — once redeemed, it's gone for good. You'll finish
    gifting it on Humble's own site."), giftConfirmAction ("Open Humble"), giftOnHumble ("Gift on Humble"),
    giftedAnnotation ("Opened Humble gift page {{date}}"), spares blurb, spares empty title/body.
  </action>
  <verify>
    <automated>pnpm codecheck && pnpm test -- src/backend/humble/__tests__/viewFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `Spares/index.tsx` imports `selectGiftableSpares` and calls `window.api.humbleGetGiftedAt` in a useEffect
    - the confirm button handler calls `window.api.humbleRecordGiftLinkOpened` AND `window.api.openExternalUrl('https://www.humblebundle.com/home/keys')`
    - `Spares/index.tsx` contains NO `clipboard`/`clipboardWriteText` call and NO "don't ask again" string
    - the openExternalUrl argument is the literal static URL with no `${` interpolation of key/machineName
    - `HumbleKeyRow` renders "Gift on Humble" only when `giftAction` is provided and shows the gifted annotation when `giftAction.giftedAt !== null`; the D-42 override block still contains `humbleKeys.notTheSameGame`
    - `Spares/index.tsx` contains the locked blurb "keep the keys unrevealed and gift them instead" and empty-state "No giftable spares"
    - translation.json parses and contains giftConfirmBody with "gone for good"
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
  <done>Giftable-spares tab lists ownedElsewhere+UNREVEALED keys, the confirm-gated "Gift on Humble" deep-link records gifted-at and opens the static Humble URL, gifted rows show the annotation, and the fuzzy safety valve is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → external browser (shell.openExternal) | The gift action opens an external URL; the URL is a fixed literal, not built from cached fields |
| renderer → main (IPC) | humbleRecordGiftLinkOpened persists a gifted-at record; the backend (Plan 02) re-validates membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | Deep-link URL construction | mitigate | Use the literal `https://www.humblebundle.com/home/keys` — no interpolation of gamekey/machineName into the URL (V5; A1). If a per-order URL is ever adopted it must be `encodeURIComponent`-wrapped and gated behind a human-verify check |
| T-13-08 | Repudiation / user error | Irreversible gift exposure without confirmation | mitigate | D-58 confirmation dialog with the irreversibility warning gates every gift action; no auto-gift, no "reveal/gift all", no "don't ask again"; gifted-at annotation guards against double-gifting the same spare (D-59) |
| T-13-06 | Tampering | Safety-valve removal (D-42 override) on Spares rows | mitigate | HumbleKeyRow extended not forked; owned-badge + override preserved verbatim on every fuzzy spare row (Pitfall 3 / WR-02) |
| T-13-03 | Information Disclosure | No gift-link secret is handled client-side | accept | GameLib never possesses a gift-link value (deep-link only); nothing secret is logged, copied, or stored — the only persisted datum is a machineName+timestamp |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `pnpm codecheck` exits 0
- `pnpm test` (full suite) green — backend store/IPC + pure-helper coverage from Plans 01/02 still pass
- Gift dialog interaction + external open confirmed in the Plan 05 human-verify checkpoint
</verification>

<success_criteria>
- HVIEW-02 met: Giftable spares lists the correct set and exposes the gift affordance
- Gift action is confirmation-gated, records gifted-at, and opens the static Humble keys page
- Double-gift guard annotation renders; fuzzy safety valve preserved; no secret handled or logged
</success_criteria>

<output>
Create `.planning/phases/13-keys-waiting-giftable-spares-views/13-04-SUMMARY.md` when done
</output>
