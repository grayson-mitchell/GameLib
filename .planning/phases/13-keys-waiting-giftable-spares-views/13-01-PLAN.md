---
phase: 13-keys-waiting-giftable-spares-views
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/common/humble/viewFilters.ts
  - src/common/humble/urgencyBadge.ts
  - src/backend/humble/__tests__/viewFilters.test.ts
  - src/backend/humble/__tests__/urgencyBadge.test.ts
autonomous: true
requirements: [HVIEW-01, HVIEW-02]

must_haves:
  truths:
    - "selectKeysWaiting returns only keys where !ownedElsewhere AND state ∈ {UNPICKED, UNREVEALED, REVEALED}"
    - "selectKeysWaiting sorts dated keys soonest-expiring first, then undated keys alphabetically by title (single flat list, no groups)"
    - "selectGiftableSpares returns only keys where ownedElsewhere AND state === UNREVEALED (REVEALED excluded)"
    - "getUrgencyTier returns 'danger' when ≤7 days remain, 'warning' when ≤30 days, null beyond 30 days, null for past/REDEEMED/UNREDEEMABLE"
    - "getUrgencyCountdownParts yields hours phrasing under 24h and day phrasing at/above 24h"
  artifacts:
    - path: "src/common/humble/viewFilters.ts"
      provides: "selectKeysWaiting + selectGiftableSpares pure predicates/comparators"
      exports: ["selectKeysWaiting", "selectGiftableSpares"]
    - path: "src/common/humble/urgencyBadge.ts"
      provides: "getUrgencyTier + countdown-copy helper"
      exports: ["getUrgencyTier", "getUrgencyCountdownParts", "UrgencyTier"]
    - path: "src/backend/humble/__tests__/viewFilters.test.ts"
      provides: "membership + sort coverage for HVIEW-01/HVIEW-02"
      contains: "selectKeysWaiting"
    - path: "src/backend/humble/__tests__/urgencyBadge.test.ts"
      provides: "tiering + countdown coverage for D-61/62/63"
      contains: "getUrgencyTier"
  key_links:
    - from: "src/backend/humble/__tests__/viewFilters.test.ts"
      to: "src/common/humble/viewFilters.ts"
      via: "import"
      pattern: "from 'common/humble/viewFilters'"
    - from: "src/backend/humble/__tests__/urgencyBadge.test.ts"
      to: "src/common/humble/urgencyBadge.ts"
      via: "import"
      pattern: "from 'common/humble/urgencyBadge'"
---

<objective>
Create the two pure, unit-tested view-logic helpers that both focused Humble Keys views
(Keys waiting, Giftable spares) and the urgency badge depend on. Zero React, zero i18n,
zero I/O — same tier and style as the existing `src/common/humble/groupKeys.ts` and
`expirationDisplay.ts`, unit-tested from the backend jest project.

Purpose: Isolate all HVIEW-01/HVIEW-02 membership, sort, and urgency-tier decisions in
deterministic functions so the frontend views (Plan 03/04) are thin renderers over
already-tested logic. This is the foundation both later frontend plans import.

Output: `viewFilters.ts`, `urgencyBadge.ts`, and their two jest test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-CONTEXT.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-RESEARCH.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-PATTERNS.md
@.planning/phases/13-keys-waiting-giftable-spares-views/13-UI-SPEC.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: viewFilters.ts — Keys-waiting + Giftable-spares membership and sort</name>
  <files>src/common/humble/viewFilters.ts, src/backend/humble/__tests__/viewFilters.test.ts</files>
  <read_first>
    - src/common/humble/groupKeys.ts (analog: header doc-comment framing, `byExpiringSoonest` comparator shape, `WAITING_STATES`-style exported-constant convention, `(keys: HumbleKey[]) => HumbleKey[]` pure signature)
    - src/common/types/humble.ts (HumbleKey field set: gamekey, machineName, state, title, platform, expiration, origin, ownedElsewhere, matchConfidence, and the HumbleKeyState union)
    - src/backend/humble/__tests__/groupKeys.test.ts (analog: `makeKey(overrides)` fixture-builder pattern and describe/test nesting to copy verbatim)
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-PATTERNS.md (viewFilters.ts section — drafted implementation to use as the starting point)
  </read_first>
  <behavior>
    - selectKeysWaiting: a key with ownedElsewhere=false and state='UNREVEALED' is included; 'UNPICKED' and 'REVEALED' are included; 'REDEEMED' and 'UNREDEEMABLE' are excluded; any ownedElsewhere=true key is excluded regardless of state.
    - selectKeysWaiting sort: two dated keys order by expiration ascending (soonest first); a dated key always precedes an undated key; two undated keys order by title via localeCompare. Result is one flat array, not grouped.
    - selectGiftableSpares: a key with ownedElsewhere=true and state='UNREVEALED' is included; ownedElsewhere=true with state='REVEALED' is EXCLUDED (D-55); ownedElsewhere=false is excluded for any state.
    - Membership uses ownedElsewhere as the sole owner signal regardless of matchConfidence (D-54): a fuzzy-matched ownedElsewhere key lands in spares, not waiting.
  </behavior>
  <action>
    Create `src/common/humble/viewFilters.ts` mirroring `groupKeys.ts`'s header doc-comment
    (pure, no React/i18n/I/O, unit-tested from the backend jest project; cite D-53/D-54/D-55/D-56
    instead of D-21). Export `selectKeysWaiting(keys: HumbleKey[]): HumbleKey[]` filtering on
    `!ownedElsewhere && WAITING_STATES.has(k.state)` where `WAITING_STATES` is a
    `Set<HumbleKeyState>` of exactly UNPICKED, UNREVEALED, REVEALED, then `.sort(compareWaiting)`.
    `compareWaiting` returns the ascending expiration-time delta when both `expiration` values are
    non-null, `-1`/`1` to sort a dated key before an undated one, and `a.title.localeCompare(b.title)`
    when both are undated (D-56). Export `selectGiftableSpares(keys: HumbleKey[]): HumbleKey[]`
    filtering on `k.ownedElsewhere && k.state === 'UNREVEALED'` (D-54/D-55 — REVEALED excluded).
    Do NOT reuse `HumbleKeyGroup`/grouping — this is a flat list. Do NOT add any gift-link field
    lookup (research Pitfall 1: no such field exists). Write `viewFilters.test.ts` in
    `src/backend/humble/__tests__/` copying `groupKeys.test.ts`'s `makeKey` fixture-builder
    verbatim (HumbleKey shape is unchanged this phase), with `describe('selectKeysWaiting', ...)`
    covering each state's inclusion/exclusion, the ownedElsewhere exclusion, the dated-before-undated
    ordering, soonest-first ordering, and alphabetical undated tiebreak; and
    `describe('selectGiftableSpares', ...)` covering the UNREVEALED-only rule and REVEALED exclusion.
  </action>
  <verify>
    <automated>pnpm test -- src/backend/humble/__tests__/viewFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/common/humble/viewFilters.ts` exports `selectKeysWaiting` and `selectGiftableSpares`
    - `grep -c "WAITING_STATES" src/common/humble/viewFilters.ts` ≥ 1 and the set contains exactly UNPICKED, UNREVEALED, REVEALED
    - `viewFilters.ts` contains NO reference to any `giftLink`/`gift_link`/`giftUrl` identifier
    - `viewFilters.test.ts` has a test asserting a REVEALED + ownedElsewhere key is excluded from selectGiftableSpares
    - `viewFilters.test.ts` has a test asserting a dated key sorts before an undated key in selectKeysWaiting
    - `pnpm test -- src/backend/humble/__tests__/viewFilters.test.ts` exits 0
  </acceptance_criteria>
  <done>Both selectors implemented and green; membership + sort behaviors covered by passing tests.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: urgencyBadge.ts — tier color decision + countdown copy parts</name>
  <files>src/common/humble/urgencyBadge.ts, src/backend/humble/__tests__/urgencyBadge.test.ts</files>
  <read_first>
    - src/common/humble/expirationDisplay.ts (analog: discriminated-return-type + doc-comment shape, per-state early-return eligibility gating, pure `(state, expiration, now=new Date())` signature)
    - src/common/types/humble.ts (HumbleKeyState union)
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-RESEARCH.md (Code Examples — drafted getUrgencyTier)
    - .planning/phases/13-keys-waiting-giftable-spares-views/13-UI-SPEC.md (Copywriting Contract — countdown copy rules; "weeks phrasing" is REJECTED)
  </read_first>
  <behavior>
    - getUrgencyTier('UNREVEALED', <3 days out>, now) === 'danger'; (<20 days out>) === 'warning'; (<45 days out>) === null.
    - getUrgencyTier for state REDEEMED or UNREDEEMABLE === null regardless of expiration (D-63).
    - getUrgencyTier with expiration=null === null; with a past expiration (daysLeft<0) === null.
    - getUrgencyCountdownParts under 24h returns hours phrasing (kind 'hours', value = Math.max(1, Math.ceil(hoursLeft))); at/above 24h returns days phrasing (kind 'days', value = Math.ceil(daysLeft)); exactly-1-day range yields value 1.
  </behavior>
  <action>
    Create `src/common/humble/urgencyBadge.ts` mirroring `expirationDisplay.ts`'s pure-function
    doc-comment + discriminated-type style. Export `type UrgencyTier = 'danger' | 'warning' | null`.
    Define `BADGE_ELIGIBLE_STATES` as a `Set<HumbleKeyState>` of exactly UNPICKED, UNREVEALED,
    REVEALED and `MS_PER_DAY = 86_400_000`. Export
    `getUrgencyTier(state: HumbleKeyState, expiration: string | null, now: Date = new Date()): UrgencyTier`
    returning null when the state is not badge-eligible or expiration is null; computing
    `daysLeft = (new Date(expiration).getTime() - now.getTime()) / MS_PER_DAY`; returning null for
    `daysLeft < 0`, 'danger' for `<= 7`, 'warning' for `<= 30`, else null (D-61/D-63). Add a second
    pure helper `getUrgencyCountdownParts(expiration: string | null, now: Date = new Date())`
    returning a discriminated result — `{ kind: 'hours'; value }` when the remaining span is under
    24h (`value = Math.max(1, Math.ceil(hoursLeft))`), otherwise `{ kind: 'days'; value }`
    (`value = Math.ceil(daysLeft)`), and a `null`/blank case when expiration is null or already past
    — so the frontend UrgencyBadge maps these to the locked "{{N}} days left" / "1 day left" / "{{H}}h left"
    strings without doing date math itself. Do NOT fold hour-math into `getUrgencyTier` (it only
    decides color). Do NOT add weeks phrasing (UI-SPEC rejects it). Write `urgencyBadge.test.ts` in
    `src/backend/humble/__tests__/` with table-driven cases pinning `now` and using ISO expiration
    strings for the 7-day and 30-day boundaries, the REDEEMED/UNREDEEMABLE never-badge cases, the
    null-expiration case, the negative/past case, and the under-24h vs ≥24h countdown-parts split.
  </action>
  <verify>
    <automated>pnpm test -- src/backend/humble/__tests__/urgencyBadge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/common/humble/urgencyBadge.ts` exports `getUrgencyTier`, `getUrgencyCountdownParts`, and the `UrgencyTier` type
    - `urgencyBadge.ts` contains NO string literal with the word "week" (weeks phrasing rejected)
    - `urgencyBadge.test.ts` asserts getUrgencyTier returns null for state 'REDEEMED' with a near-future expiration
    - `urgencyBadge.test.ts` asserts a boundary case at exactly 7 days is 'danger' and at ~8 days is 'warning'
    - `pnpm test -- src/backend/humble/__tests__/urgencyBadge.test.ts` exits 0
  </acceptance_criteria>
  <done>Tier + countdown helpers implemented and green; boundary and never-badge cases covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure functions over already-synced in-process data; no external input, no I/O, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Sort/predicate correctness (a false-positive owner match could hide a needed key from Keys waiting) | mitigate | ownedElsewhere is consumed as-is (Plan 03/04 preserve the D-42 override safety valve in the UI); this plan pins membership rules with exhaustive unit tests so no state silently changes bucket |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH.md Package Legitimacy Audit: not applicable); no install task exists |
</threat_model>

<verification>
- `pnpm test -- src/backend/humble/__tests__/viewFilters.test.ts src/backend/humble/__tests__/urgencyBadge.test.ts` green
- `pnpm codecheck` (tsc + lint) exits 0
</verification>

<success_criteria>
- Both pure helpers exist, exported, imported cleanly by their tests
- HVIEW-01 membership + sort and HVIEW-02 membership are proven by passing unit tests
- No gift-link field is referenced or added; no weeks phrasing introduced
</success_criteria>

<output>
Create `.planning/phases/13-keys-waiting-giftable-spares-views/13-01-SUMMARY.md` when done
</output>
