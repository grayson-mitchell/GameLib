---
phase: 08-new-steam-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/WebView/index.tsx
  - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx
  - public/locales/en/translation.json
autonomous: true
requirements: [STORE-01]

must_haves:
  truths:
    - "A 'Steam Store' item appears in the sidebar Stores submenu, positioned after 'Amazon Luna' and before the conditional Zoom item"
    - "Clicking 'Steam Store' navigates to /store/steam and the WebView loads https://store.steampowered.com/"
    - "The last-visited Steam store URL is persisted and restored via the existing last-url-steam / validStoredUrl mechanism"
    - "No LoginWarning is shown for the Steam store tab (unlike Epic/GOG/Amazon/Zoom)"
  artifacts:
    - path: "src/frontend/screens/WebView/index.tsx"
      provides: "steam case in validStoredUrl, steamStore URL constant, /store/steam entry in urls map"
      contains: "store.steampowered.com"
    - path: "src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx"
      provides: "Steam Store sub-item in the Stores submenu"
      contains: "/store/steam"
    - path: "public/locales/en/translation.json"
      provides: "steam-store i18n key"
      contains: "steam-store"
  key_links:
    - from: "src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx"
      to: "/store/steam route (App.tsx generic store/:store → WebView)"
      via: "SidebarItem url prop"
      pattern: "url=\"/store/steam\""
    - from: "src/frontend/screens/WebView/index.tsx"
      to: "https://store.steampowered.com/"
      via: "urls['/store/steam'] map entry + persist:steam partition"
      pattern: "'/store/steam'"
---

<objective>
Add the Steam storefront as a browse-only tab in the sidebar Stores submenu (STORE-01). This slots Steam into the existing generic `store/:store` WebView — a data-only change: one `validStoredUrl` case, one URL constant, one `urls` map entry, one sidebar sub-item, and one i18n key. No new screen or route is created.

Purpose: Satisfy STORE-01 — "User can browse the Steam storefront from the sidebar Stores section, alongside the Epic and GOG store tabs" — reusing the exact Epic/GOG/Amazon/Zoom pattern.
Output: A "Steam Store" sub-item that opens store.steampowered.com inside GameLib's WebView with persistent session and last-URL persistence, and NO LoginWarning (D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-new-steam-surfaces/08-CONTEXT.md
@.planning/phases/08-new-steam-surfaces/08-UI-SPEC.md
@.planning/phases/08-new-steam-surfaces/08-PATTERNS.md

<interfaces>
<!-- Key existing structures the executor edits. Extracted from the codebase; no exploration needed. -->

WebView/index.tsx (src/frontend/screens/WebView/index.tsx):
- validStoredUrl(url, store): switch over store keys ('epic'|'gog'|'amazon'|'zoom'), each `case` returns url.includes('<domain>'); default returns false. Add a 'steam' case BEFORE default.
- Store URL constants (~lines 62-65): `const epicStore = ...`, `gogStore`, `amazonStore`, `zoomStore`. Add `const steamStore = 'https://store.steampowered.com/'`.
- urls map (~lines 76-88): `{ '/store/epic': epicStore, '/store/gog': gogStore, '/store/amazon': amazonStore, '/store/zoom': zoomStore, ... }`. Add `'/store/steam': steamStore`.
- partition attribute (~line 383): `partition={`persist:${startUrl === epicLoginUrl ? 'epicstore' : store}`}` — already resolves to `persist:steam`. DO NOT CHANGE.
- showLoginWarningFor state (~lines 298-300): `useState<null | 'epic' | 'gog' | 'amazon' | 'zoom'>(null)` — DO NOT add 'steam'. LoginWarning useEffect (~lines 308-328) — DO NOT add a steam branch.

SidebarLinks/index.tsx (src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx):
- Stores SidebarSubmenu block (lines 142-167): SidebarItem entries for epic/gog/amazon, then `{zoom.enabled && (<SidebarItem ... url="/store/zoom" .../>)}`. Sub-items have className="SidebarLinks__subItem", a `url` prop, a `label={t(...)}` prop, and NO icon prop.
- SidebarItem is already imported (line 28). No ContextProvider destructure change needed (Steam has no username/enabled gate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the Steam storefront into the WebView</name>
  <files>src/frontend/screens/WebView/index.tsx</files>
  <read_first>
    - src/frontend/screens/WebView/index.tsx (full current state — see validStoredUrl lines 17-30, store URL constants lines 62-65, urls map lines 76-88, showLoginWarningFor lines ~298-328, partition line ~383)
    - .planning/phases/08-new-steam-surfaces/08-PATTERNS.md § "src/frontend/screens/WebView/index.tsx" (exact case/const/map insertions)
    - .planning/phases/08-new-steam-surfaces/08-CONTEXT.md § D-05, D-06, D-07, D-09, D-10 (browse-only WebView, NO LoginWarning, persistent partition, plain URL, persistence)
    - .planning/phases/08-new-steam-surfaces/08-UI-SPEC.md § "WebView wiring (no new screen)"
  </read_first>
  <action>
    Make three additive edits, mirroring the existing Epic/GOG/Amazon/Zoom entries exactly (per D-10):
    1. In `validStoredUrl`, add `case 'steam':` immediately before `default:`, returning `url.includes('store.steampowered.com')`.
    2. Alongside the `epicStore`/`gogStore`/`amazonStore`/`zoomStore` constants, add `const steamStore = 'https://store.steampowered.com/'` — the PLAIN URL with no language/country params (per D-09; do NOT replicate Epic's `/${lang}/` pattern).
    3. In the `urls` map, add the entry `'/store/steam': steamStore`.
    Do NOT touch the `partition` attribute — `persist:${store}` already resolves to `persist:steam`, giving Steam a durable web session for free (per D-07).
    Do NOT add `'steam'` to the `showLoginWarningFor` state union type, and do NOT add a Steam branch to the LoginWarning `useEffect` — Steam has no in-app web login flow (per D-06). The type must remain exactly `null | 'epic' | 'gog' | 'amazon' | 'zoom'`.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && grep -q "case 'steam':" src/frontend/screens/WebView/index.tsx && grep -q "store.steampowered.com" src/frontend/screens/WebView/index.tsx && grep -q "'/store/steam': steamStore" src/frontend/screens/WebView/index.tsx && grep -q "null | 'epic' | 'gog' | 'amazon' | 'zoom'" src/frontend/screens/WebView/index.tsx && ! grep -q "'amazon' | 'zoom' | 'steam'" src/frontend/screens/WebView/index.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `validStoredUrl` contains a `case 'steam':` returning `url.includes('store.steampowered.com')`.
    - Source assertion: a `steamStore` constant equals `'https://store.steampowered.com/'` (no `${lang}` interpolation on the steam URL).
    - Source assertion: `urls` map contains `'/store/steam': steamStore`.
    - Source assertion: the `showLoginWarningFor` state type union is unchanged (`null | 'epic' | 'gog' | 'amazon' | 'zoom'`) and does NOT include `'steam'`.
    - Behavior: navigating to `/store/steam` loads `https://store.steampowered.com/` under the `persist:steam` partition; last-visited Steam URL persists via `last-url-steam`.
  </acceptance_criteria>
  <done>Steam is wired into the WebView data maps; no LoginWarning branch added; partition untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Steam Store sidebar sub-item and its i18n key</name>
  <files>src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx, public/locales/en/translation.json</files>
  <read_first>
    - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx (lines 142-167, the Stores SidebarSubmenu block with the Epic/GOG/Amazon sub-items and the zoom.enabled conditional)
    - .planning/phases/08-new-steam-surfaces/08-PATTERNS.md § "src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx" (exact sub-item to insert) and § "public/locales/en/translation.json" (Key 1: steam-store)
    - .planning/phases/08-new-steam-surfaces/08-UI-SPEC.md § "Surface 1: Sidebar Stores Submenu" (ordering + component + i18n key)
    - .planning/phases/08-new-steam-surfaces/08-CONTEXT.md § D-08 (register Steam store with label "Steam Store")
    - public/locales/en/translation.json around line 1225 (the existing `zoom-store` flat key, sibling of `store`/`gog-store`/`amazon-luna`)
  </read_first>
  <action>
    1. In SidebarLinks, insert a new `SidebarItem` inside the Stores `SidebarSubmenu`, positioned AFTER the Amazon Luna sub-item and BEFORE the `{zoom.enabled && (...)}` conditional (Steam is first-class and always rendered — no conditional guard, per D-08 and the UI-SPEC ordering: Epic → GOG → Amazon Luna → Steam Store → Zoom). The item uses `className="SidebarLinks__subItem"`, `url="/store/steam"`, and `label={t('steam-store', 'Steam Store')}`. Do NOT add an `icon` prop (sub-items have no icons). Do NOT add a `steam` destructure from ContextProvider (no gate needed).
    2. In translation.json, add the flat key `"steam-store": "Steam Store"` as a sibling of the existing `"zoom-store"` key (near line 1225), following the exact pattern of `store`/`gog-store`/`amazon-luna`/`zoom-store`.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && grep -q 'url="/store/steam"' src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx && grep -q "steam-store" src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx && node -e "const t=require('./public/locales/en/translation.json'); if(t['steam-store']!=='Steam Store') throw new Error('missing/incorrect steam-store key'); console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: SidebarLinks contains a `SidebarItem` with `url="/store/steam"` and `label={t('steam-store', 'Steam Store')}`, with NO `icon` prop.
    - Source assertion: the Steam sub-item appears after the `amazon-luna` sub-item and before the `zoom.enabled` conditional in the Stores `SidebarSubmenu`.
    - CLI: `node -e "JSON.parse(require('fs').readFileSync('public/locales/en/translation.json'))"` succeeds (valid JSON) and `translation['steam-store'] === 'Steam Store'`.
    - Behavior: opening the Stores submenu shows Epic Store, GOG Store, Amazon Luna, Steam Store, then Zoom Store (Zoom only when enabled).
  </acceptance_criteria>
  <done>Steam Store sub-item renders in the correct position with the `steam-store` label key present in translation.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView renderer → external web (store.steampowered.com) | The WebView loads untrusted external web content under a persistent session partition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure / Tampering | WebView `<webview>` loading store.steampowered.com under `persist:steam` | accept | Reuse the EXISTING WebView chrome and `persist:${store}` partition already used for Epic/GOG/Amazon/Zoom — no new isolation model. Browse-only (D-05): no injected scripts, no "install in GameLib" buttons. Steam session is user-owned and identical in risk profile to the existing store tabs. |
| T-08-02 | Tampering | `urls` map / `steamStore` constant | mitigate | The Steam URL is a hard-coded constant (`https://store.steampowered.com/`), never built from user input. No dynamic URL construction. |
| T-08-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new packages introduced in this phase (confirmed by UI-SPEC Registry Safety and RESEARCH). No install tasks. |
</threat_model>

<verification>
- `pnpm codecheck` (tsc --noEmit) exits 0 — the new `case 'steam':` and `steamStore` typecheck cleanly and the `showLoginWarningFor` union is unchanged.
- `node -e "JSON.parse(require('fs').readFileSync('public/locales/en/translation.json'))"` succeeds (translation.json remains valid JSON).
- Manual UAT (deferred to a `/gsd:verify-work` pass): open Stores submenu → "Steam Store" appears in position 4; click it → store.steampowered.com loads with WebView chrome and no LoginWarning; navigate then return → last URL restored.
</verification>

<success_criteria>
- STORE-01 satisfied: a Steam tab appears in the sidebar Stores section alongside Epic and GOG, and opening it browses the Steam storefront inside GameLib.
- No LoginWarning is wired for Steam (D-06 honored).
- `pnpm codecheck` passes; translation.json is valid and contains `steam-store`.
</success_criteria>

<output>
Create `.planning/phases/08-new-steam-surfaces/08-01-SUMMARY.md` when done.
</output>
