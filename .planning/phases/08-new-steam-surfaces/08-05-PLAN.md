---
phase: 08-new-steam-surfaces
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/frontend/screens/ConsoleMode/components/LaunchOverlay/index.tsx
autonomous: true
requirements: [CONSOLE-01]

must_haves:
  truths:
    - "The Steam 'Launched in Steam' overlay stays visible until GameLib loses focus (window blur — the game took over) rather than dismissing on a fixed 1500ms timer"
    - "A max safety timeout still dismisses the overlay so it cannot hang if focus never changes"
    - "Non-Steam launch behavior is unchanged"
  artifacts:
    - path: "src/frontend/screens/ConsoleMode/components/LaunchOverlay/index.tsx"
      provides: "Steam branch dismisses on window blur with a max-timeout safety net (replacing the fixed 1500ms timer)"
      contains: "blur"
  key_links:
    - from: "src/frontend/screens/ConsoleMode/components/LaunchOverlay/index.tsx Steam branch"
      to: "onDismiss"
      via: "window 'blur' listener (primary) + setTimeout safety net (fallback)"
      pattern: "addEventListener\\('blur'"
---

<objective>
Close UAT gap D: the Console "Launched in Steam" overlay currently dismisses on a fixed `setTimeout(onDismiss, 1500)`, which fires ~1.5s too early — on macOS GameLib backgrounds for a couple seconds before the game actually loads, leaving a confusing gap. Dismiss on window blur (the Steam game taking foreground) instead, with a max-timeout safety net so the overlay can't hang if focus never changes.

Purpose: Make the launch overlay persist until the game visibly takes over, matching the honest fire-and-forget framing without disappearing prematurely.
Output: the Steam branch of the LaunchOverlay mount effect swapped from a fixed 1500ms timer to a window-`blur`-driven dismiss plus a bounded safety timeout.

Root-cause anchor (08-UAT.md Diagnosis D): `LaunchOverlay/index.tsx` Steam branch uses `setTimeout(onDismiss, 1500)` (line 79).

Gap-closure note: Follow-on minor enhancement for an already-shipped, verified Phase 8. Isolated to a single file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-new-steam-surfaces/08-UAT.md

<interfaces>
<!-- Existing structure the executor edits. Extracted from the codebase; no exploration needed. -->

LaunchOverlay/index.tsx mount effect (lines 69-94): a single `useEffect(..., [])` that branches on `game.runner === 'steam'`.
- Steam branch (lines 71-80): fires `launch({...})` fire-and-forget, then `const timer = setTimeout(onDismiss, 1500); cleanup = () => clearTimeout(timer)`.
- Non-Steam branch (lines 81-91): `launch({...}).finally(() => onDismiss())` — leave UNCHANGED.
- The effect returns `cleanup` (line 92); it is intentionally `[]`-deps (fires once on mount).
This is an Electron renderer — the renderer `window` fires a `'blur'` event when the app window loses OS focus (which is exactly what happens when the Steam client brings the game to the foreground).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dismiss the Steam launch overlay on window blur with a max-timeout safety net</name>
  <files>src/frontend/screens/ConsoleMode/components/LaunchOverlay/index.tsx</files>
  <read_first>
    - src/frontend/screens/ConsoleMode/components/LaunchOverlay/index.tsx (full mount effect lines 69-94 — Steam branch lines 71-80 with the `setTimeout(onDismiss, 1500)`; note the existing `let cleanup` pattern and `[]` deps)
    - .planning/phases/08-new-steam-surfaces/08-UAT.md § "## Gaps" (gap D truth) + § "## Diagnosis" D (dismiss on window blur / foreground loss instead of fixed 1500ms, WITH a max-timeout safety net)
  </read_first>
  <action>
    In the mount `useEffect`, replace ONLY the Steam branch's fixed 1500ms dismiss (lines 79-80) with a blur-driven dismiss plus a bounded safety net. Keep the `void launch({...})` fire-and-forget call exactly as-is. After firing launch:
    1. Define a one-shot dismiss: `let dismissed = false; const doDismiss = () => { if (dismissed) return; dismissed = true; onDismiss() }`.
    2. Add a window blur listener that dismisses when GameLib loses focus (the game took over): `const onBlur = () => doDismiss(); window.addEventListener('blur', onBlur)`.
    3. Add a max-timeout safety net so the overlay can't hang if focus never changes (e.g. the game fails to launch, or the user is on a platform where blur doesn't fire): `const safety = setTimeout(doDismiss, 8000)` — pick a value clearly longer than a normal handoff (documented in a comment as the "can't-hang" ceiling, NOT the expected dismiss path).
    4. Set the effect's Steam-branch `cleanup` to remove the listener AND clear the timer: `cleanup = () => { window.removeEventListener('blur', onBlur); clearTimeout(safety) }`.
    Do NOT change the non-Steam branch, the spinner, the label, the BackHint gating, or `useCancelOnHold`. The overlay should still show "Launched in Steam" with the idle spinner; only the dismiss trigger changes from a 1500ms timer to blur + safety timeout.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && grep -q "addEventListener('blur'" src/frontend/screens/ConsoleMode/components/LaunchOverlay/index.tsx && grep -q "removeEventListener('blur'" src/frontend/screens/ConsoleMode/components/LaunchOverlay/index.tsx && ! grep -q "setTimeout(onDismiss, 1500)" src/frontend/screens/ConsoleMode/components/LaunchOverlay/index.tsx && grep -q "console.steam.launched" src/frontend/screens/ConsoleMode/components/LaunchOverlay/index.tsx && pnpm codecheck && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the Steam branch adds a `window.addEventListener('blur', ...)` that calls a one-shot dismiss, and the cleanup removes it.
    - Source assertion: a `setTimeout(..., <ms>)` safety net (clearly > a normal handoff, e.g. 8000ms) also calls the same one-shot dismiss; the fixed `setTimeout(onDismiss, 1500)` is GONE.
    - Source assertion: the one-shot guard (`dismissed` flag) prevents double-dismiss when both blur and the safety timer fire.
    - Source assertion: the non-Steam branch, spinner, "Launched in Steam" label, and BackHint gating are unchanged.
    - CLI: `pnpm codecheck` exits 0.
    - Behavior: the "Launched in Steam" overlay stays up until GameLib loses focus (game foregrounds) and, worst case, auto-dismisses at the safety ceiling — it no longer disappears ~1.5s early.
  </acceptance_criteria>
  <done>The Steam launch overlay dismisses on window blur (with an 8s-class safety net) instead of a fixed 1500ms timer; non-Steam launch is unchanged and codecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer window focus events → overlay lifecycle | The overlay's dismissal now depends on a `blur` event that may or may not fire depending on OS/window-manager behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Denial of Service (stuck overlay) | blur-driven dismiss | mitigate | A bounded `setTimeout` safety net always dismisses even if `blur` never fires; a one-shot `dismissed` guard prevents double-dismiss; the effect cleanup removes the listener and clears the timer so no leak occurs on unmount. |
| T-08-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new packages; single-file frontend edit only. |
</threat_model>

<verification>
- `pnpm codecheck` (tsc --noEmit) exits 0.
- `grep -n "addEventListener('blur'" LaunchOverlay/index.tsx` present; `grep -n "setTimeout(onDismiss, 1500)"` absent.
- Manual re-UAT (deferred to `/gsd:verify-work`): activating an installed Steam game keeps "Launched in Steam" visible until the game foregrounds (GameLib blurs), then dismisses; if the game never takes focus, the overlay still dismisses at the safety ceiling.
</verification>

<success_criteria>
- UAT gap D closed: the Steam launch overlay persists until GameLib loses focus rather than dismissing on a fixed 1500ms timer, with a max-timeout safety net.
- Non-Steam launch behavior and all other overlay visuals are unchanged; `pnpm codecheck` passes.
</success_criteria>

<output>
Create `.planning/phases/08-new-steam-surfaces/08-05-SUMMARY.md` when done.
</output>
