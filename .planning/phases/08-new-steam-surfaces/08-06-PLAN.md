---
phase: 08-new-steam-surfaces
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/frontend/screens/Discounts/index.tsx
  - src/frontend/screens/Discounts/components/DiscountFilters/index.tsx
autonomous: true
requirements: [STORE-01]

must_haves:
  truths:
    - "The Deals/Discounts 'Hide Owned' toggle hides games owned in ANY store (Epic, GOG, Amazon, Steam, Zoom), not just GOG"
    - "The owned set is built from all store libraries and matched against catalog products by normalized title"
    - "The 'Hide Owned' toggle is available whenever the user has any owned games (not gated on GOG login alone)"
  artifacts:
    - path: "src/frontend/screens/Discounts/index.tsx"
      provides: "all-store owned set + hideOwned applied in filteredSorted"
      contains: "steam.library"
    - path: "src/frontend/screens/Discounts/components/DiscountFilters/index.tsx"
      provides: "Hide Owned toggle gated on any-store ownership, not isGogLoggedIn"
      contains: "canHideOwned"
  key_links:
    - from: "src/frontend/screens/Discounts/index.tsx ContextProvider"
      to: "owned set from epic/gog/amazon/steam/zoom libraries"
      via: "normalized-title Set consumed by the filteredSorted hideOwned predicate"
      pattern: "steam.library"
    - from: "src/frontend/screens/Discounts/index.tsx"
      to: "DiscountFilters Hide Owned visibility"
      via: "canHideOwned prop (any library non-empty) replacing isGogLoggedIn gate"
      pattern: "canHideOwned"
---

<objective>
Close UAT gap F: on the Deals/Discounts page, "Hide Owned" only accounts for GOG. The screen destructures only `{ gog }` from ContextProvider and gates the toggle on `isGogLoggedIn`, so games owned via Epic, Amazon, Steam, or Zoom are not treated as owned. Fix: pull all store libraries from ContextProvider, build an owned set from ALL of them, apply the `hideOwned` filter against catalog products by normalized title, and make the toggle available whenever the user has any owned games.

Purpose: Make "Hide Owned" honest across the whole unified library — the core GameLib value proposition (one launcher across Epic/GOG/Amazon/Steam) applied to the Deals surface.
Output: an all-store owned set + a working `hideOwned` filter in `filteredSorted`, and a broadened toggle-visibility gate.

Root-cause anchors (08-UAT.md Diagnosis F): `Discounts/index.tsx:44` `const { gog } = useContext(ContextProvider)`; `:45` `isGogLoggedIn`. NOTE: this is a pre-existing cross-store Deals-page bug (not a Steam-surface regression) that the user scoped INTO this Phase 8 gap-closure run. Mapped to STORE-01 as the closest storefront/Deals requirement.

Gap-closure note: Follow-on cross-surface bug fix. During planning it was confirmed that the `hideOwned` toggle state is persisted and shown but is NOT currently wired into the `filteredSorted` predicate (no ownership match exists in index.tsx today) — so this plan must ACTUALLY apply the filter, not merely broaden an existing GOG-only match.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-new-steam-surfaces/08-UAT.md

<interfaces>
<!-- Existing structures the executor edits. Extracted from the codebase; no exploration needed. -->

ContextProvider exposes per-store objects each with `.library: GameInfo[]`: `epic`, `gog`, `amazon`, `steam`, `zoom` (same shape used by ConsoleMode/index.tsx). `gog.username` is the GOG-login signal. GameInfo has `.title: string` and `.app_name: string`.

Discounts/index.tsx:
- Line 44: `const { gog } = useContext(ContextProvider)`. Line 45: `const isGogLoggedIn: boolean = !!gog?.username`.
- filteredSorted useMemo (lines 265-397): builds `products.filter((p) => {...})` then sorts. `CatalogProduct` has `.title`, `.productType`, `.price`, `.genres`, `.features`, `.releaseDate`, etc. — the filter predicate currently checks search/dlc/price/rating/year/pegi/genres/features/os. It does NOT currently reference `hideOwned` or any owned library. The dep array (lines 383-397) likewise omits `hideOwned`.
- `hideOwned` state exists (line 97) and is passed to DiscountFilters (line 579); `isGogLoggedIn` is passed at line 583.

DiscountFilters/index.tsx:
- Prop `isGogLoggedIn: boolean` (line 58), consumed at line 288: `{isGogLoggedIn && ( <><FormControlLabel ...wishlistOnly/> <FormControlLabel ...hideOwned/></> )}` — the SAME gate wraps BOTH "Wishlist Only" (a GOG-wishlist concept) and "Hide Owned". Only Hide Owned should become cross-store; Wishlist Only stays GOG-gated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build an all-store owned set, apply the hideOwned filter, and broaden the toggle gate</name>
  <files>src/frontend/screens/Discounts/index.tsx, src/frontend/screens/Discounts/components/DiscountFilters/index.tsx</files>
  <read_first>
    - src/frontend/screens/Discounts/index.tsx (ContextProvider destructure line 44, isGogLoggedIn line 45, hideOwned state line 97, filteredSorted useMemo lines 265-397 incl. predicate 275-317 and dep array 383-397, DiscountFilters render props lines 553-588)
    - src/frontend/screens/Discounts/components/DiscountFilters/index.tsx (props interface incl. isGogLoggedIn line 58, the combined gate at lines 288-313 wrapping Wishlist Only + Hide Owned)
    - src/frontend/screens/ConsoleMode/index.tsx (lines 118-135 — reference for how the other surface spreads epic/gog/amazon/steam/zoom `.library` from ContextProvider)
    - .planning/phases/08-new-steam-surfaces/08-UAT.md § "## Gaps" (gap F truth) + § "## Diagnosis" F
  </read_first>
  <action>
    Edit both files as one cohesive change so `pnpm codecheck` is clean at the end.
    In `Discounts/index.tsx`:
    1. Broaden the ContextProvider destructure at line 44 to `const { epic, gog, amazon, steam, zoom } = useContext(ContextProvider)`. Keep `isGogLoggedIn = !!gog?.username` (still used to gate Wishlist Only).
    2. Build a memoized owned-title set from ALL five libraries. Add a `useMemo` that concatenates `epic.library, gog.library, amazon.library, steam.library, zoom.library`, maps each GameInfo to a normalized key `game.title.trim().toLowerCase()` (skip empties), and returns a `Set<string>` — dep array `[epic.library, gog.library, amazon.library, steam.library, zoom.library]`. Name it `ownedTitles`.
    3. Compute `const canHideOwned = ownedTitles.size > 0` (the user has at least one owned game in any store).
    4. Apply the filter inside `filteredSorted`'s `products.filter((p) => {...})` predicate: add `if (hideOwned && ownedTitles.has(p.title.trim().toLowerCase())) return false`. Add BOTH `hideOwned` and `ownedTitles` to the `filteredSorted` dep array (they are currently absent — without this the toggle would not re-filter).
    5. Update the DiscountFilters render: replace the `isGogLoggedIn={isGogLoggedIn}` prop pass (line 583) — keep passing `isGogLoggedIn` for Wishlist Only, and additionally pass `canHideOwned={canHideOwned}`.
    In `DiscountFilters/index.tsx`:
    6. Add a `canHideOwned: boolean` prop to the props interface (next to `isGogLoggedIn`) and destructure it.
    7. Split the combined gate at lines 288-313: keep "Wishlist Only" wrapped in `{isGogLoggedIn && (...)}`, but move the "Hide Owned" FormControlLabel into its own `{canHideOwned && (...)}` block so Hide Owned shows whenever the user owns games in any store, independent of GOG login.
    Match strictly by normalized title (trim+lowercase) since Deals catalog products and library games do not share appName across stores. Do not alter any other filter or sort logic.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && grep -q "steam.library" src/frontend/screens/Discounts/index.tsx && grep -q "ownedTitles" src/frontend/screens/Discounts/index.tsx && grep -q "canHideOwned" src/frontend/screens/Discounts/index.tsx && grep -q "canHideOwned" src/frontend/screens/Discounts/components/DiscountFilters/index.tsx && grep -q "hideOwned && ownedTitles" src/frontend/screens/Discounts/index.tsx && pnpm codecheck && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `Discounts/index.tsx` destructures `epic, gog, amazon, steam, zoom` from ContextProvider.
    - Source assertion: an `ownedTitles` `Set<string>` is built from all five `.library` arrays using normalized (`trim().toLowerCase()`) titles.
    - Source assertion: the `filteredSorted` predicate contains `if (hideOwned && ownedTitles.has(...)) return false`, and BOTH `hideOwned` and `ownedTitles` appear in the `filteredSorted` dep array.
    - Source assertion: `DiscountFilters` has a `canHideOwned` prop; the "Hide Owned" control is gated on `canHideOwned` while "Wishlist Only" remains gated on `isGogLoggedIn`.
    - CLI: `pnpm codecheck` exits 0.
    - Behavior: enabling "Hide Owned" removes catalog products whose title matches a game owned in ANY store (Epic/GOG/Amazon/Steam/Zoom); the toggle is visible whenever the user owns any games.
  </acceptance_criteria>
  <done>"Hide Owned" on the Deals page hides games owned in any store via a normalized-title owned set from all five libraries, is actually wired into filteredSorted, and its toggle is gated on any-store ownership rather than GOG login alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local library data (5 stores) → Deals catalog filter | Owned-title matching joins locally-held library titles against remote catalog product titles; a normalization mismatch could over- or under-hide, but no untrusted input crosses a privilege boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information (incorrect filtering) | ownedTitles normalized-title match | accept | Title-based matching is best-effort (same approach the feature already implied); normalization (trim+lowercase) is applied consistently on both sides. Edge cases (punctuation/edition suffixes) may miss, but the failure mode is a game not being hidden — non-destructive, no data exposure. |
| T-08-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new packages; frontend-only edits to two existing files. |
</threat_model>

<verification>
- `pnpm codecheck` (tsc --noEmit) exits 0.
- `grep -n "ownedTitles" Discounts/index.tsx` shows the all-store owned set; `grep -n "hideOwned && ownedTitles"` shows the applied filter.
- `grep -n "canHideOwned" DiscountFilters/index.tsx` shows the broadened toggle gate.
- Manual re-UAT (deferred to `/gsd:verify-work`): with games owned across Epic/Steam/Amazon, enabling "Hide Owned" on the Deals page hides all of them (not just GOG titles).
</verification>

<success_criteria>
- UAT gap F closed: "Hide Owned" on the Deals page accounts for all five store libraries, is genuinely applied in the filter pipeline, and its toggle is available whenever the user owns games in any store.
- `pnpm codecheck` passes; no other Deals filter/sort behavior changes.
</success_criteria>

<output>
Create `.planning/phases/08-new-steam-surfaces/08-06-SUMMARY.md` when done.
</output>
