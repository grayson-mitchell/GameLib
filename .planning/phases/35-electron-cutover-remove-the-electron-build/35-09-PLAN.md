---
phase: 35-electron-cutover-remove-the-electron-build
plan: 09
type: execute
wave: 6
replans: 35-09-PLAN.SUPERSEDED-2026-08-29.md
depends_on:
  - 35-01
  - 35-02
  - 35-08
files_modified:
  - src-tauri/src/main.rs
  - src/backend/storeManagers/legendary/user.ts
  - src/backend/storeManagers/legendary/__tests__/epicLogoutDomains.test.ts
autonomous: false
requirements:
  - REQ-35-07
decisions: [D-09-CORRECTED, D-05, D-16]

must_haves:
  truths:
    - "D-09-CORRECTED (operator decision, 2026-08-29, option (a)): the Epic cookie clear is widened from the single `epicgames.com` suffix to an EXPLICIT LIST of Epic-owned apex domains. It stays DOMAIN-SCOPED. `clear_all_browsing_data()` remains banned — `REQ-34.4.1-06` and three in-source prohibitions stand unamended, and this plan must not weaken any of them"
    - "The clear must reach `EPIC_DEVICE` on `.fortnite.com`, `.twinmotion.com`, `.unrealengine.com` and `.metahuman.com`. An `epicgames.com` suffix filter cannot match those BY CONSTRUCTION, which is why widening the filter — not changing the mechanism — is the fix"
    - "A NAIVE TS-ONLY LOOP DOES NOT WORK AND MUST NOT BE SHIPPED. `humble_login_clear_cookies` (`main.rs:5747`) has a macOS-only fallback gated on `cookie_domain_matches(domain, Some(EPIC_COOKIE_DOMAIN))` where that constant is the single literal `\"epicgames.com\"`. Epic's window comes from `open_pristine_epic_login_window`, which never registers a Tauri-managed `WebviewWindow`, so `existing_window` is structurally always `None` on that path. Passing `fortnite.com` fails the guard and falls through to `humble_login:no-window:{label}` — an ERROR, not a clear. Both halves change together or four of five domains silently fail"
    - "The guard must stay NARROW for every other caller. Humble, GOG and Amazon all fail the Epic domain check today and fall straight through to the existing no-window error, completely unchanged. Widening the Epic set must not widen it to them — and that is a property to TEST, not to assert in a comment"
    - "`deleted === 0` FAILS the logout. Today `legendary/user.ts` swallows every wipe step into `logWarning('... failed (continuing)')`. That swallow is the defect class that produced the original lying report, and fixing it is the one requirement worth keeping from the superseded plan"
    - "Cookies only for the four sibling domains. `clearEpicStorage` is origin-scoped BY CONSTRUCTION — it runs JS inside the target page's own origin, which is exactly what stops it touching another storefront — and `EPIC_DEVICE` is a COOKIE. Do NOT add a per-origin storage window for the siblings"
    - "The premise of the superseded plan is dead and must not be reinstated: `35-AB-RETEST.md` Item 7 records the lying-self-report symptom as NOT REPRODUCING, with `EPIC_SESSION_AP` measured ABSENT by structured binarycookies parse. The remaining defect is that the domain-scoped clear is INCOMPLETE, not that it is inert"
  artifacts:
    - path: "src-tauri/src/main.rs"
      provides: "an Epic-owned apex domain SET replacing the single EPIC_COOKIE_DOMAIN literal, with the :5770 fallback guard matching any member"
      contains: "EPIC_COOKIE_DOMAINS"
    - path: "src/backend/storeManagers/legendary/__tests__/epicLogoutDomains.test.ts"
      provides: "proof that every listed domain is attempted, that the deltas are summed, and that a zero total FAILS the logout"
  key_links:
    - from: "src/backend/storeManagers/legendary/user.ts clearEpicCookies"
      to: "humble_login_clear_cookies"
      via: "one seam.clearCookies call PER DOMAIN over the Epic-owned list, deltas summed"
      pattern: "EPIC_COOKIE_HOSTS"
    - from: "main.rs:5770 macOS Epic fallback guard"
      to: "clear_default_data_store_cookies_for_domain"
      via: "cookie_domain_matches against ANY member of the Epic-owned set"
      pattern: "EPIC_COOKIE_DOMAINS"
---

<objective>
Make the Epic logout cookie clear reach every Epic-owned domain it is supposed to, and make a clear
that removes nothing fail the logout instead of logging a warning and continuing.

Purpose: this is the standing 34.6 live-gate **Step 8 FAIL**, re-scoped to the defect that is
actually still open. The lying self-report is already fixed (34.4.1 plan 23) and the A/B re-test
confirmed it live. What remains is that the clear is **incomplete**: six live cookies survive on
`epicgames.com` suffixes, and `EPIC_DEVICE` survives on four Epic-owned sibling domains that the
current filter cannot match by construction.

This plan is a REPLAN. The superseded version is retained at
`35-09-PLAN.SUPERSEDED-2026-08-29.md` with a do-not-execute banner; read `deferred-items.md`'s
`D-35-09-01` before starting, because it carries the measured evidence this plan is built on.

Output: one widened domain set in Rust, one looped call site in TypeScript, a swallow turned into a
failure, and a live gate that restores the survivor clause the superseded plan dropped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md
@.claude/skills/spike-findings-gamelib/references/tauri-login-webview-cookies.md

<interfaces>
THE RUST SIDE
  `src-tauri/src/main.rs:3167`  `const EPIC_COOKIE_DOMAIN: &str = "epicgames.com"` — `#[cfg(macos)]`.
                                Its doc comment at `:3161` asserts this is "the only value that
                                arm's `clearEpicCookies` step ever passes as `domain`". That
                                statement BECOMES FALSE in this plan.
  `src-tauri/src/main.rs:5747`  the `humble_login_clear_cookies` dispatch arm.
  `src-tauri/src/main.rs:5769`  the macOS-only Epic fallback and its domain guard.
  `src-tauri/src/main.rs:3698`  `clear_default_data_store_cookies_for_domain(app, domain)`.
  `src-tauri/src/main.rs:1789`  `cookie_domain_matches(host, domain)` — THE ONE comparator in this
                                project. Born from F-34.4.2-19 and weeks of silent Humble-login
                                failure. Do NOT write a second one.
  `src-tauri/src/main.rs:9903`  `f_34_4_2_12_wry_blocking_cookies_calls_are_macos_gated` — a
                                STRUCTURAL source scan asserting an EXACT set of (arm, guard) pairs
                                across three cookie arms, built specifically so a call site can
                                neither migrate between arms nor silently lose its guard.

THE TS SIDE
  `src/backend/storeManagers/legendary/user.ts:24`   `EPIC_COOKIE_HOST = 'epicgames.com'`.
  `src/backend/storeManagers/legendary/user.ts:137+` `logout()`'s `wipeSteps`, forked on
                                                     `getLoginWindowSeam()`. The `seam !== null`
                                                     (Tauri) branch holds `clearEpicCookies` and
                                                     `clearEpicStorage`.
  the `for (const [name, step] of wipeSteps)` loop and its
  `logWarning('... failed (continuing)')` — the swallow this plan removes.
  `src/backend/humble/user.ts` `disconnect()` — the sibling implementation. This plan does NOT
  change Humble; read it only as the shape to mirror.

MEASURED SURVIVORS (from `35-AB-RETEST.md` Item 7, Tauri leg — the target set)
  on `epicgames.com` suffixes:  EPIC_LOGIN_ID, _tald, _epicSID, EPIC_DEVICE,
                                __cf_bm on `.www.` and on `.ecosec.on.`   = 6
  on Epic-owned siblings:       EPIC_DEVICE on .fortnite.com, .twinmotion.com,
                                .unrealengine.com, .metahuman.com          = 4
  already fixed, do not regress: EPIC_SESSION_AP measured ABSENT.

jest: `--selectProjects Backend`. Rust: `cargo test --manifest-path src-tauri/Cargo.toml`.
</interfaces>
</context>

<constraints>
- **`clear_all_browsing_data()` stays banned.** Do not add it, do not weaken any of the three
  in-source prohibitions (`main.rs:2066`, `:5742`, `:6237`), and do not touch `REQ-34.4.1-06`.
- **Do not write a second cookie-domain comparator.** Reuse `cookie_domain_matches`.
- **Do not resolve or hardcode any per-OS webview data path.**
- **Do not trust a clear call's return value as evidence.** The existing post-removal re-read is the
  evidence; keep it.
- **Do not call `.cookies()` re-entrantly** — recorded deadlock.
- **Do not add a per-origin storage window for the sibling domains.** Cookies only.
- **Do not add a capability grant.** `capabilities/default.json` is scoped `"windows": ["main"]`
  where untrusted remote content renders; this work is entirely Rust-side and needs none.
- **Do not log cookie values, names, tokens or account identifiers.** Counts and domains only. This
  repo is public (T-35-04).
- Never `git stash`, never `git reset`, never `git checkout -- <path>`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Widen the Epic domain set in Rust, keeping every other caller narrow</name>
  <read_first>`deferred-items.md` `D-35-09-01` IN FULL, including the operator decision and the five measured constraints; src-tauri/src/main.rs:3155-3170 for `EPIC_COOKIE_DOMAIN` and its doc comment; src-tauri/src/main.rs:5747-5790 for the whole `humble_login_clear_cookies` arm including the fallback guard and its comment; src-tauri/src/main.rs:3698+ for `clear_default_data_store_cookies_for_domain`; src-tauri/src/main.rs:1789-1815 for `cookie_domain_matches`; src-tauri/src/main.rs:9903+ for the structural (arm, guard) gate IN FULL</read_first>
  <files>src-tauri/src/main.rs</files>
  <action>
Replace the single `EPIC_COOKIE_DOMAIN` literal with an Epic-owned apex SET — name it
`EPIC_COOKIE_DOMAINS` so every existing reference goes red at compile time rather than silently
picking up a changed meaning:

    epicgames.com, fortnite.com, unrealengine.com, twinmotion.com, metahuman.com

Update the `:5770` fallback guard to match ANY member, via `cookie_domain_matches`. Do not
hand-roll a suffix test.

**Rewrite the doc comment at `:3161`, do not leave it.** It currently asserts the constant is "the
only value that arm's `clearEpicCookies` step ever passes as `domain`" — false the moment this
lands. State instead why the set is a list of APEX domains rather than hostnames, that
`cookie_domain_matches` does the suffix work, and that the list is maintained by hand and can go
stale if Epic adds a domain (the operator accepted that cost explicitly over a filter that cannot
match by construction).

**Keep the guard narrow for everyone else, and prove it.** The arm's existing comment records that
Humble/GOG/Amazon all fail the Epic domain check and fall through unchanged. Add `#[cfg(test)]`
coverage asserting BOTH directions: every member of the Epic set matches, and
`humblebundle.com` / `gog.com` / `amazon.com` do NOT. A test that only checks the positive
direction cannot detect an over-wide set, which is the failure mode that matters here.

**Re-derive the `:9903` structural gate, do not re-pin it blind.** It asserts an EXACT set of
(arm, guard) pairs and exists so a call site cannot migrate or lose its guard silently. Work out
what the new true set is by reading the code, then update the expectation and say in the summary
what changed and why. If the gate's expectation does not move, say that too — and say how you
established it.
  </action>
  <acceptance_criteria>
- `grep -c 'EPIC_COOKIE_DOMAIN\b' src-tauri/src/main.rs` is `0` — the old single-value name is gone, not shadowed.
- All five domains appear literally in `main.rs`.
- `grep -c 'clear_all_browsing_data' src-tauri/src/main.rs` is unchanged from its pre-edit value — state both numbers. The three prohibition comments must still be present.
- A `#[cfg(test)]` test asserts all five Epic domains match the guard AND that `humblebundle.com`, `gog.com` and `amazon.com` do not.
- The `:3161` doc comment no longer contains the words "the only value".
- `cargo test --manifest-path src-tauri/Cargo.toml` passes; baseline is 191 passed / 0 failed.
- `pnpm test --selectProjects Backend` shows exactly the 3 known-red `lzmaLoader`/`decompressPool` failures and no others.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>Every Epic-owned domain reaches the macOS clear path, and no other storefront's does.</done>
</task>

<task type="auto">
  <name>Task 2: Loop the TS clear over the domain list, and make a zero-delta clear FAIL the logout</name>
  <read_first>src/backend/storeManagers/legendary/user.ts IN FULL — `EPIC_COOKIE_HOST` at :24, the whole `logout()` including both seam branches, the `clearEpicCookies` step's `try/finally` window handling, and the `for (const [name, step] of wipeSteps)` loop with its `logWarning('... failed (continuing)')`; src/backend/humble/user.ts's `disconnect()` for the shape being mirrored; any existing test asserting the current single-domain behaviour</read_first>
  <files>src/backend/storeManagers/legendary/user.ts, src/backend/storeManagers/legendary/__tests__/epicLogoutDomains.test.ts</files>
  <action>
Turn `EPIC_COOKIE_HOST` into `EPIC_COOKIE_HOSTS`, the same five apex domains, and keep the two
sides' lists visibly paired — a comment on each naming the other, since a Rust set and a TS list
that drift apart is exactly the silent half-fix this plan exists to prevent.

In `clearEpicCookies`, call `seam.clearCookies(label, host)` once per domain inside the EXISTING
single hidden window — open once, loop, close once in the existing `finally`. Do not open a window
per domain. Sum the per-domain deltas.

**Log per-domain counts and the total, never a cookie name or value.** A per-domain breakdown is
what makes an incomplete clear diagnosable next time; a bare total is what hid this defect.

**Make a zero total FAIL.** Today every step is swallowed by
`logWarning('... failed (continuing)')`. Change it so a `clearEpicCookies` step that removed nothing
across ALL domains rejects, and so the logout surfaces that rather than reporting success. Decide
deliberately and state in the summary what happens to the OTHER wipe steps' failures — whether they
also become fatal or stay warnings — and why. Do not change their behaviour silently in either
direction.

Note the asymmetry and handle it: an individual domain legitimately returning 0 is NOT an error (a
user may never have visited `twinmotion.com`). Only a zero TOTAL is.

Write `epicLogoutDomains.test.ts` proving: every listed domain is attempted; deltas are summed; a
zero total rejects; a nonzero total from a single domain does not; and no cookie name or value
reaches any log call. **RED-prove each assertion against a deliberately broken implementation** —
35-08's pairing test, its entire threat mitigation, passed against a broken `stop` and was only
caught because the executor broke the code on purpose. Report what you broke and what failed.
  </action>
  <acceptance_criteria>
- `grep -c 'EPIC_COOKIE_HOST\b' src/backend/storeManagers/legendary/user.ts` is `0`.
- All five domains appear in `legendary/user.ts`, and a comment pairs the list with its Rust counterpart by name.
- `seam.open` is called exactly ONCE in `clearEpicCookies`, and `seam.close` exactly once.
- A test asserts a zero TOTAL rejects and that a single zero domain among nonzero others does not.
- Each new test is RED-proven; the summary names the mutation used for each and the failure it produced.
- No test fixture, log string or comment contains a real cookie value, token or account identifier.
- `pnpm test --selectProjects Backend` shows exactly the 3 known-red failures; `pnpm codecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --selectProjects Backend</automated>
  </verify>
  <done>An Epic logout attempts every Epic-owned domain, reports what it actually removed per domain, and refuses to call a clear that removed nothing a success.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The 34.6 Step 8 re-run, WITH the survivor clause the superseded plan dropped</name>
  <files>none — observation only</files>
  <action>
Human-run live verification. This is 34.6's Step 8, re-driven against its ORIGINAL D-13 bar — both halves, scored by identity and not by count.
  </action>
  <read_first>.planning/phases/34.6-*/34.6-LIVE-GATE.md Step 8 IN FULL, including its recorded FAIL and the sixth contract defect noted at its end; Task 2's summary</read_first>
  <what-built>
The Epic cookie clear now attempts five Epic-owned apex domains instead of one, sums the deltas,
logs a per-domain breakdown, and fails the logout if nothing was removed anywhere.
`clear_all_browsing_data()` was not used and remains banned.
  </what-built>
  <how-to-verify>
**Do NOT attempt to plant a cookie via the Tauri DevTools console.** That is what made 34.6's
original Step 8 partly undrivable and was logged as that gate's sixth contract defect — the console
accepts neither paste nor Enter on macOS. Use cookies already in the jar instead, exactly as the
34.6 operator did.

1. `pnpm tauri:dev`. Log into Epic through the embedded login window. Separately, log into at least
   one OTHER storefront — GOG or Humble — so the jar holds a foreign session. Confirm both.
2. Quit and relaunch. Confirm you are STILL logged into both. **Without this the rest is vacuous** —
   it is what proves the jar is disk-backed.
3. Read the jar independently BEFORE logging out. Record the Epic-scoped count and the foreign
   storefront's cookie names. Do not record values.
4. Log out of Epic through the app's own control. Note exactly what the UI said.
5. **Re-read the jar independently** — a fresh query, not a value cached from step 3. Assert BOTH:
   (a) zero Epic-owned cookies remain, across all five domains; and
   (b) **the foreign storefront's cookies SURVIVE, matched by exact name.** Clause (b) is the whole
   point and is what the superseded plan would have failed. A clear that empties the jar is a
   REGRESSION, not a pass.
6. Open the Epic login window again — you must be required to enter credentials.
7. Confirm the OTHER storefront is still logged in. If it is not, this is a FAIL: report it and stop.
8. Check both log sinks for the per-domain breakdown. Confirm no cookie name, value, token or
   account identifier appears in either.

**Staleness guard, from the 34.6 run:** do not read the jar until it has demonstrably been
rewritten — that run checked `~/Library/HTTPStorages/gamelib-shell.binarycookies` mtime and size
moved. A pre-flush read produces a false result.

**Non-vacuity control, also from that run:** an earlier attempt used a parser that never extracted
values at all and reported "EMPTY" for every cookie including live ones. Confirm your reader
reports sensible nonzero lengths for known-live cookies before drawing any conclusion. `strings(1)`
is NOT sound here — a binary jar retains tombstoned bytes, so a name appearing in the file is not
evidence of a live cookie.

Name the build. This is a DEV observation; the packaged run is re-checked in 35-19.
  </how-to-verify>
  <acceptance_criteria>
- Step 2 confirmed both sessions survived a restart, so step 5 is meaningful.
- Clause (a): zero Epic-owned cookies remain across all five domains, by independent re-read.
- Clause (b): the foreign storefront's cookies survive by exact name, and it is still logged in.
- Opening the Epic login window requires credentials.
- The per-domain breakdown is present in the logs and carries no cookie name, value or identifier.
- The reader was proven non-vacuous on known-live cookies, and the jar was proven flushed first.
- The report names the build and states this is a DEV observation.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the results for steps 1-8, or report FAIL naming which clause failed and pasting the counts.</resume-signal>
  <verify>
    <human-check>Epic cookies gone across all five domains, the other storefront untouched and still signed in, and re-login requires credentials.</human-check>
  </verify>
  <done>34.6 Step 8 is discharged on BOTH its clauses — the clear is complete AND correctly scoped — rather than on one clause with the other quietly dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared WKWebView/WebView2 data store -> any window of the app | one jar backs every window and every storefront; both an under-broad and an over-broad clear are defects here |
| logged-out user -> the next user of the same OS profile | the security property logout is supposed to establish |
| the hand-maintained Epic domain list -> the clear's actual reach | a domain Epic adds later is a silent gap; accepted by operator decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-37 | Information Disclosure | An incomplete clear leaves Epic session material behind, so the next user of a shared machine opens the login window already authenticated. This is the live, still-open half of 34.6 Step 8 | mitigate | The domain set is widened to every Epic-owned apex domain measured as carrying a survivor; the existing post-removal re-read remains the evidence; a zero total now FAILS the logout; Task 3 scores clause (a) by independent identity re-read, not by count |
| T-35-38 | Denial of Service | An OVER-broad clear signs the user out of storefronts they never touched — the exact harm `REQ-34.4.1-06` closed, and what the superseded plan would have caused | mitigate | `clear_all_browsing_data()` stays banned with its three in-source prohibitions intact and grep-asserted unchanged; the macOS fallback guard is widened only to the named Epic set, with a `#[cfg(test)]` test asserting Humble/GOG/Amazon still do NOT match; Task 3 clause (b) requires a foreign storefront's cookies to survive BY NAME and it to remain signed in |
| T-35-39 | Repudiation | A clear that removes nothing reports success, so the failure is unobservable — the original defect class | mitigate | A zero total rejects rather than warning-and-continuing; per-domain counts are logged so an incomplete clear is diagnosable rather than merely absent |
| T-35-40 | Information Disclosure | Cookie names or values reaching a log sink in a PUBLIC repo (T-35-04) | mitigate | Counts and domains only, mirroring `humble/user.ts`; asserted by an acceptance criterion on the tests and by a Task 3 step reading both sinks |
| T-35-41 | Tampering | The Rust set and the TS list drifting apart, so a domain is attempted on one side and rejected on the other — a silent half-fix | mitigate | The two lists carry paired comments naming each other; the Rust rename to `EPIC_COOKIE_DOMAINS` forces every existing reference red at compile time rather than letting a changed meaning pass silently |
</threat_model>
