---
phase: 35-electron-cutover-remove-the-electron-build
plan: 29
type: execute
wave: 3
gap_closure: true
depends_on: ["35-20", "35-21", "35-22", "35-23", "35-25", "35-26", "35-27"]
files_modified:
  - .planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md
  - .planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md
  - .planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md
  - .planning/phases/35-electron-cutover-remove-the-electron-build/35-VALIDATION.md
autonomous: false
requirements: [REQ-35-07, REQ-35-20]
closes_gate_criteria: [6, 10, 14, 16, 21]
closes_deferred: [D-35-19-15]

must_haves:
  truths:
    - "Criteria 6, 10, 14 and 16 carry a fresh `Observed:` and a fresh verdict, measured against a packaged macOS arm64 `.app` built from the fix commits"
    - "Criterion 21 is re-measured with a non-primary Epic domain CONFIRMED PRESENT before logout, so a zero is not vacuous"
    - "`35-LIVE-GATE.md`'s top-level verdict states the new PASS/FAIL tally, and `35-VERIFICATION.md`'s status reflects it"
  artifacts:
    - path: ".planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md"
      provides: "A re-run record appended per criterion, with the build identity it was measured against"
      contains: "RE-RUN"
    - path: ".planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md"
      provides: "A `re_verification` block reflecting the re-measured gaps"
  key_links:
    - from: "the fix plans 35-20/21/22/23/25/26/27"
      to: "the packaged `.app` under test"
      via: "a build produced AFTER all seven land, identified by commit SHA"
      pattern: "commit"
    - from: "the re-run verdicts"
      to: "35-VERIFICATION.md gap 1 and gap 5"
      via: "an explicit per-gap resolution"
      pattern: "REQ-35-20|REQ-35-07"
---

<objective>
Re-measure the four FAILing live-gate criteria and the one vacuous PASS against a packaged macOS
arm64 `.app` built from the fix commits, and propagate the result into `35-LIVE-GATE.md` and
`35-VERIFICATION.md`.

`REQ-35-20`'s own closure clause is "Any FAIL means the phase does not close", and `35-LIVE-GATE.md`
is `blocking: true` with a recorded verdict of FAIL (17 PASS / 4 FAIL over 21 of 21 measured). Every
one of those four FAILs now has a code fix. Criterion 21 PASSED but `D-35-19-15` records that it did
not exercise the multi-domain cookie clear it was written to prove.

A live gate re-run is cheap if it is scoped. This is four gestures plus one re-run, not a 21-criterion
sweep — but each gesture must be measured against a build that contains the fixes, and the record
must say which build.

**Standing rule D-E: the author of a gate contract may never also run it.** This plan does not
author new criteria — the four contracts already exist in `35-LIVE-GATE.md` and are re-used verbatim.
Criterion 21's re-run adds a seeding PRECONDITION that `D-35-19-15` itself specifies; that
precondition is quoted from the deferred item rather than invented here.

This plan is `autonomous: false`: tasks 2 and 3 are blocking human gestures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md

<interfaces>
<!-- The four FAILing criteria and the vacuous PASS, with what each now depends on. -->

Criterion  6  Tray: a recent-game entry launches      <- plan 35-20 Task 2 (addRecentGame on the Steam path)
Criterion 10  Deep link: cold start                   <- plan 35-20 Task 1 (`steam` in RUNNERS) and Task 2
Criterion 14  installed.json watcher                  <- plan 35-20 Task 3 (sendFrontendMessage)
Criterion 16  Wake lock: system assertion / F-35-08-A <- plan 35-27 (the `pendingOps`/kind split)
Criterion 21  Epic logout, multi-domain clear         <- plan 35-23 (per-domain census) + D-35-19-15's seeding step

Regression surfaces the fixes could plausibly disturb — check each, because the fix set touches the
Steam launch path, the shell's URL handoff and the sleep locks that criteria 4/5/15 depend on:
  Criterion  4  Library renders and a game launches   <- 35-21 Task 1 could break `steam://` if the
                                                          allow-list omitted the scheme
  Criterion  5  Tray: menu opens, About opens          <- 35-20 Task 2 changes what the tray reads
  Criterion 15  Wake lock: display assertion           <- 35-27 changes the lock/unlock pair

THE CRITERION-16 TRAP, quoted from the gate record so a re-runner cannot fall into it:
  "THE CONTRACT'S OWN GESTURE CANNOT DETECT F-35-08-A. With both game and download active the counts
   were 1 and 1 -- which reads as the stated 'best case'. That is an ARTEFACT ... The two
   configurations that DO expose it are (a) a game running with no download -- criterion 15 -- and
   (b) letting the download finish while the game keeps running. A future re-run must not conclude
   'no cross-labelled assertion' from a simultaneous capture alone."

THE CRITERION-21 SEEDING STEP, quoted from D-35-19-15:
  "before logging out, drive the login webview to at least one non-primary Epic domain (e.g.
   unrealengine.com) so those cookies exist, CONFIRM they are present, then log out and confirm a
   non-zero clear count for that domain. Without the confirm-present step the re-test reproduces this
   same vacuous zero."

THE CRITERION-10 TEST-MACHINE HAZARD, from the original run: SIX bundles claimed the `gamelib:`
scheme in LaunchServices, four pointing at paths that no longer exist (three unmounted DMG volumes
plus an old Electron-era build at `dist/mac-arm64/GameLib.app`). `open "gamelib://..."` returned
exit 0 and did NOTHING. Repair with `lsregister` before scoring, and score the repair as environment,
not product.

BUILD REQUIREMENTS:
  - PACKAGED, not dev. `R-34.5-G1-PKG`'s whole lesson, and after `REQ-35-09` a `pnpm tauri:dev` run
    is structurally incapable of exercising `frontendDist` at all.
  - `pnpm tauri:dev:packaged` produces a `tauri build --debug` artifact; a release build is the
    other option. Criterion 17 was previously measured on the debug-packaged build. Say which you
    used and be consistent across all five criteria.
  - `pnpm tauri:dev` exits 0 WITHOUT replacing a running instance — quit any running GameLib first.
  - Dev teardown ORPHANS the node sidecar and a name-based sweep reports CLEAN. Sweep by PATH.

OUT OF SCOPE — do not attempt, do not let a gesture drift into them:
  - Windows and Linux smoke launches: Phase 38, items `38-W04`/`38-W05`, by REQ-35-20's own recorded
    option-c scope reduction.
  - `pnpm lint` exiting 0: Phase 39.
  - Criterion 17's updater `ReleaseNotFound`: already dispositioned as a CONTRACT-EXPECTATION DEFECT,
    not a code defect. Do not re-litigate it.
  - `D-35-19-03`, `-10`, `-11`, `-12`, `-13`, `-14`, and `35-REVIEW.md`'s `WR-01`: still deferred.
  - `D-35-19-07`, `-08`, `-16`: ALREADY FIXED on `fix/steam-native-install-stability` and live-verified
    2026-08-30. Do not re-plan or re-test move-install as broken.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce and identify the build under test</name>
  <files>(none — produces a build artifact; no repo file is modified)</files>

  <read_first>
    - The SUMMARYs of all seven `depends_on` plans, to confirm each landed and to collect the commit
      SHAs.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md`'s header — the
      build identity format the original run used, so the re-run record matches it.
    - `package.json` scripts `tauri:dev:packaged` and the release build path, plus
      `.github/workflows/release-tauri.yml` if you choose the release leg.
  </read_first>

  <action>
    Verify all seven fix plans have landed, then produce ONE packaged macOS arm64 `.app` from that
    tree. Record the exact commit SHA the artifact was built from and the build command used.

    A build produced before any fix lands measures nothing, and a re-run recorded against an
    unidentified build is unfalsifiable — the record must let a later reader rebuild the same thing.

    Repair the LaunchServices state before criterion 10 is attempted: enumerate every bundle claiming
    the `gamelib:` scheme, unregister the stale ones, and re-register only the artifact under test.
    Record the before and after claimant lists. This is environment repair and is scored as
    environment, not as product.

    Verify the sidecar from the previous session is not still running — sweep by PATH, not by process
    name; a name-based sweep reports CLEAN over an orphaned sidecar.

    Run the mechanized suites against this same tree and record their results, so a live FAIL can be
    attributed: `pnpm test`, `pnpm codecheck`, and
    `cargo test --manifest-path src-tauri/Cargo.toml`. Note that a full `pnpm test` run manufactures
    a different failure set under load than targeted runs do — if something fails, re-run it targeted
    before treating it as real.
  </action>

  <acceptance_criteria>
    - All seven `depends_on` SUMMARYs exist — verify with `ls` and record the list.
    - The build's commit SHA, `git status --short` output at build time (expected: clean), and the
      literal build command are recorded.
    - The artifact path is recorded, and its `mtime` is later than the last fix commit's timestamp.
    - The `gamelib:` LaunchServices claimant list is recorded before and after repair, and after
      repair contains exactly one entry: the artifact under test.
    - No orphaned `gamelib-shell` or sidecar process from a prior session is running — proven by a
      PATH-based sweep whose output is recorded.
    - `pnpm test`, `pnpm codecheck` and `cargo test --manifest-path src-tauri/Cargo.toml` results
      recorded. Any failure is triaged (real vs full-suite-load artefact) before the live run starts.
  </acceptance_criteria>

  <verify>
    <automated>pnpm codecheck &amp;&amp; cargo test --manifest-path src-tauri/Cargo.toml</automated>
  </verify>

  <done>
    A packaged macOS arm64 `.app` exists, is identified by commit SHA, is the sole `gamelib:`
    claimant, and the mechanized suites' state against the same tree is recorded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-measure criteria 6, 10, 14 and 16 against the packaged build</name>
  <files>(none — live gesture; results are recorded by Task 4)</files>

  <action>
    Do not modify any file in this task. Run each criterion's EXISTING contract from 35-LIVE-GATE.md verbatim against the Task 1 artifact and record a fresh `Observed:` and verdict for each. Do not soften an `Expected` to make a FAIL disappear. Task 4 writes the results into the records; this task only measures.
  </action>

  <read_first>
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md`, sections
      `### 6.`, `### 10.`, `### 14.` and `### 16.` in full — the `What`, `Preconditions`, `Gesture
      sequence`, `Sink` and `Expected` of each. Re-use these contracts VERBATIM; do not soften an
      `Expected`.
    - The same file's `Verdict vocabulary` paragraph — `PASS` / `FAIL` / `BLOCKED` / `NOT ATTEMPTED`,
      and the rule that NOT ATTEMPTED is reserved for an unmet precondition or a test-machine gap,
      never for a product failure.
    - Criterion 16's "THE CONTRACT'S OWN GESTURE CANNOT DETECT F-35-08-A" paragraph, quoted in this
      plan's `<interfaces>`.
    - Plan `35-20`'s SUMMARY for the exact `sendFrontendMessage` call shape (criterion 14's grep) and
      plan `35-27`'s SUMMARY for which configurations its tests reproduce (criterion 16's gesture).
  </read_first>

  <what-built>
    Criterion 6's cause (`addRecentGame` never running on the Steam launch path), criterion 10's
    cause (`steam` absent from the `RUNNERS` enum), criterion 14's cause (the watcher sending no
    frontend message) and criterion 16's cause (a shared `pendingOps` counter governing a
    per-operation assertion's lifetime) all have code fixes, unit-proven RED-then-GREEN. None is
    live-proven. This gesture is the closure signal.
  </what-built>

  <how-to-verify>
    Run each criterion's existing gesture sequence verbatim against the Task 1 artifact. Fill a fresh
    `Observed:` per criterion with literal output — log lines, `pmset` blocks, `ps` output — not
    prose summaries.

    **Criterion 6 (tray recent game).** Launch a STEAM title from the library. Then verify BOTH
    halves, because storage is not execution:
      (a) `~/Library/Application Support/gamelib/store/config.json` -> `games.recent` has that title
          at POSITION 1 and its entry carries `runner: "steam"`. Record the entry verbatim.
      (b) Open the tray menu and confirm the title APPEARS in the recent-games submenu, then CLICK it
          and confirm the game launches without the main window being shown first. Asserting only the
          recorded string proves nothing — the original run never reached the click step, and the
          runner-less-entry hazard it noted is exactly what part (b) exists to catch.
    Note the submenu renders only the first 5 entries (`max_recent_games` = 5); the original FAIL was
    partly the title sitting at position 6. Position 1 is what makes this scoreable.

    **Criterion 10 (deep link, cold start).** Quit GameLib fully, confirm with `ps aux`. Then
    `open "gamelib://launch?appName=<the Steam appId of the criterion-6 title>"`. Expect
    `on_open_url fired with 1 url(s)` and `delivered OS deep link to sidecar: ok` in
    `gamelib-shell.log`, AND the title actually invoked. The previous run got the first two and not
    the third — that is the whole finding, so do not score on the handoff lines alone. Also confirm
    `gamelib.log` shows a `[Steam]` probe, whose total absence was the original diagnostic.

    **Criterion 14 (installed.json watcher).** With the app running, logged into Epic, and the
    Library view OPEN and VISIBLE, externally `touch` or edit Legendary's `installed.json` from a
    Terminal. Back the file up first and byte-verify the restore afterwards. Expect
    `installed.json updated, refreshing library` in `gamelib.log` AND the Library view updating
    WITHOUT a manual refresh. The backend half already passed; the UI half is the verdict. Avoid the
    original run's confound: do not log in to Epic partway through the observation — be logged in and
    viewing the Library before the gesture, with nothing else intervening.

    **Criterion 16 (wake lock).** Do NOT score from a simultaneous game+download capture — that
    configuration reads as the "best case" even when the defect is present. Use the two exposing
    configurations instead:
      (a) game running, NO download: capture `pmset -g assertions`; expect exactly one
          `PreventUserIdleDisplaySleep` named `"GameLib: a game is running"` and ZERO
          `PreventUserIdleSystemSleep` named `"GameLib: a download is in progress"`.
      (b) start a download, launch a game while it runs, then let the DOWNLOAD FINISH with the game
          still running: capture `pmset -g assertions` at least 60 seconds after the download's
          `Finished Installation` log line. Expect the download-labelled system assertion to be GONE
          while the game's display assertion remains.
    Baseline both captures as CLEAN (both label counts 0, no game running) before starting — the
    original run established that as mandatory. Record the assertion HANDLES and elapsed times, not
    just the counts: handle identity is what proved the original diagnosis.

    **Regression checks, same session, recorded with a verdict each:**
      - Criterion 4: a game launches from the library (proves `35-21`'s `open_external` allow-list
        did not break `steam://`).
      - Criterion 5: the tray menu opens and About opens.
      - Criterion 15: a game alone still holds its display assertion.

    Score each criterion `PASS` / `FAIL` / `BLOCKED` / `NOT ATTEMPTED` against its EXISTING
    `Expected`. Do not soften a FAIL. A cause you cannot establish is recorded as unestablished with
    the competing hypotheses named, not collapsed into a guess.
  </how-to-verify>

  <resume-signal>
    Type "approved" with a verdict and a filled `Observed:` for each of criteria 6, 10, 14, 16, plus
    the three regression verdicts. If any is FAIL, say so plainly — a FAIL here means `REQ-35-20`
    still does not close, and that is a valid, reportable outcome. Do not adjust an `Expected` to
    make a FAIL disappear.
  </resume-signal>

  <verify>
    <automated>MISSING — OS-level tray, deep-link and IOKit power-assertion behaviour is unreachable from jest and from a dev build. The recorded gate evidence in <how-to-verify> is the check.</automated>
  </verify>

  <done>
    Criteria 6, 10, 14 and 16 each have a filled `Observed:` and a verdict, plus verdicts for the three regression criteria 4, 5 and 15.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-measure criterion 21 with a non-primary Epic domain seeded and confirmed present</name>
  <files>(none — live gesture; results are recorded by Task 4)</files>

  <action>
    Do not modify any file in this task. Run criterion 21's existing contract with the seeding precondition D-35-19-15 specifies, quoted rather than invented. A run without the CONFIRM-PRESENT step is recorded NOT ATTEMPTED, never PASS.
  </action>

  <read_first>
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md`, section
      `## D-35-19-15` in full — the observed per-domain table (epicgames.com=6 and four zeros), why
      those zeros are uninterpretable, and the exact seeding-plus-confirm procedure it specifies.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md`, section
      `### 21.` — the existing contract, which stands unchanged; this run adds only the precondition
      `D-35-19-15` names.
    - Plan `35-23`'s SUMMARY for the verbatim per-host census log line format — you will grep
      `gamelib.log` for it.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md` Item 7 — the
      original measurement of `EPIC_LOGIN_ID`/`_epicSID`/`_tald`/`EPIC_DEVICE` surviving logout on
      `.fortnite.com`, `.twinmotion.com`, `.unrealengine.com` and `.metahuman.com`. That is the
      behaviour the widening exists for and the behaviour this run must exercise.
    - The standing project rule that a cookie `count === 0` with no liveness proof is
      indistinguishable from a dead API — that is why the census exists and why "confirm present" is
      not optional.
  </read_first>

  <what-built>
    Plan `35-23` ported Humble's cookie census to the Epic per-domain clear, so each per-host line now
    carries `total=`, `matched=` and `verdict=` on both sides. A zero is now self-interpreting. It
    also changed the fatal-logout predicate so an empty jar is not a failure while a populated jar
    with a zero delta still is. Neither is live-proven.
  </what-built>

  <how-to-verify>
    1. Start from a logged-OUT Epic state, then log in through the embedded login webview so the
       session is real. Scripted login tests need a logout preamble — the jar is sticky across
       restarts.
    2. **Seed a non-primary domain.** Drive the login webview (or the in-app store browser) to at
       least one non-primary Epic property — `unrealengine.com` is `D-35-19-15`'s own example.
    3. **CONFIRM PRESENT before logging out.** Verify from `gamelib.log`'s census lines, or an
       equivalent direct read, that that domain now has a NON-ZERO `total` and a non-zero `matched`.
       Record the literal line. **Without this step the re-test reproduces the same vacuous zero and
       proves nothing** — that is `D-35-19-15`'s entire finding, restated.
    4. Log out.
    5. Record the per-host census line for EVERY one of the five hosts, verbatim, both sides.
    6. Score:
       - the seeded non-primary domain must show a NON-ZERO measured clear count;
       - every remaining zero must carry a verdict that says which kind of zero it is
         (`SUPPORTED_BUT_EMPTY` vs `UNSUPPORTED_OR_ERROR` vs `UNDECIDABLE`);
       - `epicgames.com` must still clear as before.
    7. Confirm the logout is real: credentials are required to log back in, per criterion 21's
       existing `Expected`. Verify the pre-logout session survived a restart first, per criterion 20,
       so "you had to log in again" cannot be explained by a session that was never persisted.
    8. Verify no cookie name, value or account identifier appears in any recorded log line before
       pasting it into the record — this repo is public.
  </how-to-verify>

  <resume-signal>
    Type "approved" with the confirm-present line, all five per-host census lines and the verdict. If
    the seeding step could not be completed, record `NOT ATTEMPTED` with the reason and say plainly
    that `D-35-19-15` stays open — a second vacuous zero must not be recorded as a PASS.
  </resume-signal>

  <verify>
    <automated>MISSING — a real multi-domain cookie clear needs a live webview session and an independent post-clear read; no unit test can produce one. The recorded census lines in <how-to-verify> are the check.</automated>
  </verify>

  <done>
    The confirm-present line and all five per-host census lines are recorded, and the seeded non-primary domain shows a non-zero measured clear count — or the run is scored NOT ATTEMPTED with D-35-19-15 left open.
  </done>
</task>

<task type="auto">
  <name>Task 4: Propagate the results into the gate, the verification, the deferred items and the validation map</name>
  <files>.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md, .planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md, .planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md, .planning/phases/35-electron-cutover-remove-the-electron-build/35-VALIDATION.md</files>

  <read_first>
    - Tasks 2 and 3's recorded observations.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md` — its
      frontmatter `verdict:` line and the per-criterion `Observed:`/`Verdict:` structure.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md` — its
      frontmatter `status`, `score`, `gaps` and the `re_verification: null` field that exists for
      exactly this.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-VALIDATION.md` — the
      per-task verification map. EXTEND it with rows for plans `35-20` .. `35-29`; do not replace it.
    - The recorded lesson that a propagation can miss a status doc: a gate result that lands in one
      file and not its siblings goes undetected for two runs.
  </read_first>

  <action>
    Append a dated RE-RUN block to each of criteria 6, 10, 14, 16 and 21 in `35-LIVE-GATE.md`,
    carrying the build's commit SHA, the fresh `Observed:` and the fresh `Verdict:`. APPEND — do not
    overwrite the original `Observed:`. The original run's evidence is what makes the re-run
    interpretable, and this file's own format already accommodates appended resolutions.

    Update `35-LIVE-GATE.md`'s frontmatter `verdict:` line to the new tally, in the same format as the
    existing one, and state which criteria changed verdict and which did not.

    Update `35-VERIFICATION.md`: fill `re_verification` with the date and outcome, and update each of
    the five gaps' status against what was actually measured. Update `status` and `score`. If any
    criterion still FAILs, `status` stays `gaps_found` — do not flip it to `passed` on a partial
    result. `REQ-35-20`'s own clause is "any FAIL means the phase does not close", and a closure gate
    that forces a false record is a recorded failure shape here.

    Append dated resolution notes to `deferred-items.md` for `D-35-19-05`, `D-35-19-06`, `D-35-19-09`,
    `D-35-19-15` and `D-35-08-02` — each stating what was measured and whether it is closed or still
    open. Do not touch `D-35-19-03`, `-10`, `-11`, `-12`, `-13`, `-14`; they stay deferred.

    Extend `35-VALIDATION.md`'s per-task verification map with one row per task across plans `35-20`
    .. `35-29`, following the existing column shape (Task ID / Plan / Wave / Requirement / Threat Ref
    / Secure Behavior / Test Type / Automated Command / File Exists / Status). Extend, do not replace
    — the existing 19-plan map is the record of what was already validated.

    Hand-edit every one of these files. Do NOT invoke any `gsd-sdk state.*` verb: four of them corrupt
    `STATE.md` and `ROADMAP.md`, and `annotate-dependencies` deletes plan rows.
  </action>

  <acceptance_criteria>
    - `grep -c 'RE-RUN' .planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md`
      returns at least 5 — one per re-measured criterion.
    - Each RE-RUN block names the build commit SHA from Task 1.
    - The original `Observed:` text for criteria 6, 10, 14, 16 and 21 is still present — verify by
      grepping for a distinctive phrase from each original block and recording the result.
    - `35-LIVE-GATE.md`'s frontmatter `verdict:` states a tally whose PASS + FAIL + BLOCKED +
      NOT ATTEMPTED sums to 21. State the arithmetic in the SUMMARY.
    - `35-VERIFICATION.md`'s `re_verification` is no longer `null`, and every one of its five gaps has
      an explicit resolved/still-open status.
    - `35-VERIFICATION.md`'s `status` is `passed` ONLY if zero criteria FAIL and all five gaps
      resolved. Otherwise it stays `gaps_found` with the remainder named.
    - `deferred-items.md` has a dated note under each of the five named items, and
      `grep -c '^## D-35-19-10' .planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md`
      still returns 1 with its body unchanged (`git diff` shows no hunk inside it).
    - `35-VALIDATION.md` gained rows for every task in plans `35-20`..`35-29`, and the pre-existing
      rows for `35-01`..`35-19` are byte-identical — verify with `git diff` and record.
    - `pnpm test --selectProjects Meta` passes — planning gates run in CI.
  </acceptance_criteria>

  <verify>
    <automated>pnpm test --selectProjects Meta &amp;&amp; git diff --stat .planning/phases/35-electron-cutover-remove-the-electron-build/</automated>
  </verify>

  <done>
    All four planning records carry the re-run result consistently, the original evidence is intact,
    the deferred items that stay deferred are untouched, and `35-VERIFICATION.md`'s status matches the
    measured tally rather than the desired one.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS -> shell (`gamelib://` deep link) | Re-measured here against a repaired LaunchServices state |
| gate record -> phase closure decision | The record is what a later reader and the roadmap trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-134 | Repudiation | a gate re-run measuring the wrong build | mitigate | Task 1 records the commit SHA, the build command, a clean `git status`, and an artifact `mtime` later than the last fix commit. A re-run against an unidentified build is unfalsifiable. |
| T-35-135 | Repudiation | criterion 16 scored PASS from a blind gesture | mitigate | The simultaneous game+download capture is explicitly banned as evidence; the two exposing configurations are prescribed, with assertion HANDLES and elapsed times recorded, not just counts. |
| T-35-136 | Repudiation | criterion 21 reproducing a vacuous zero | mitigate | The seeding step plus an explicit CONFIRM-PRESENT read is a precondition; a run without it is recorded `NOT ATTEMPTED`, never PASS. |
| T-35-137 | Repudiation | criterion 6 scored from stored state alone | mitigate | Both halves required: the `games.recent` entry at position 1 with `runner: "steam"`, AND the tray submenu click actually launching. Storage is not execution. |
| T-35-138 | Tampering | a closure gate forcing a false record | mitigate | `status: passed` requires zero FAILs and all five gaps resolved; a partial result stays `gaps_found`. The original `Observed:` blocks are appended to, never overwritten. |
| T-35-139 | Information Disclosure | pasting live log excerpts into a public repo | mitigate | No cookie name, value, token or account identifier may appear in any recorded line; checked before pasting, mirroring the redaction discipline the Epic and Humble clear paths already enforce in code. |
| T-35-140 | Tampering | `gsd-sdk state.*` corrupting planning files | mitigate | All four files are hand-edited; no SDK state verb is invoked. |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed. |
</threat_model>

<verification>
- Task 1's mechanized suites recorded against the build tree.
- Tasks 2 and 3 returned a filled `Observed:` and a verdict for every re-measured criterion, plus
  three regression verdicts.
- Task 4's propagation is consistent across all four planning files, with the original evidence
  intact and the still-deferred items untouched.
- `pnpm test --selectProjects Meta` passes.

Explicitly NOT in scope and not to be scored here: Windows/Linux smoke launches (Phase 38,
`38-W04`/`38-W05`), `pnpm lint` exiting 0 (Phase 39), criterion 17's updater 404 (already
dispositioned as a contract-expectation defect).
</verification>

<success_criteria>
- Criteria 6, 10, 14 and 16 carry a fresh verdict measured against an identified packaged macOS
  arm64 `.app`.
- Criterion 21 is re-measured with a non-primary Epic domain confirmed present before logout.
- `35-LIVE-GATE.md`, `35-VERIFICATION.md`, `deferred-items.md` and `35-VALIDATION.md` all agree.
- If the tally is 21 PASS / 0 FAIL and all five gaps resolve, `REQ-35-20`'s closure clause is met and
  `35-VERIFICATION.md` flips to `passed`. If not, the remainder is named and the phase stays open —
  which is a valid outcome, not a failure of this plan.
</success_criteria>

<output>
Create `.planning/phases/35-electron-cutover-remove-the-electron-build/35-29-SUMMARY.md` when done.
Record: the build SHA and command; the LaunchServices before/after claimant lists; the five fresh
`Observed:`/`Verdict:` pairs; the three regression verdicts; the new gate tally with its arithmetic;
and, per gap in `35-VERIFICATION.md`, resolved or still-open with the reason.
</output>
