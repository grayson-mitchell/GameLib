---
phase: 35-electron-cutover-remove-the-electron-build
plan: 28
type: execute
wave: 2
gap_closure: true
depends_on: ["35-25", "35-26"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REQ-35-20]

must_haves:
  truths:
    - "Every Phase 35 status-table row states a status that matches the evidence, and every non-`Planned` row cites the SUMMARY or gate criterion that discharged it"
    - "No row is marked complete that cannot be evidenced"
    - "Each of `REQ-35-16` and `REQ-35-17` states the status its OWN owning plan's recorded Task 3 checkpoint outcome supports — `35-25` and `35-26` respectively — and neither is deferred to plan `35-29`, which carries neither ID"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "A Phase 35 status table that is true as of the correction date"
  key_links:
    - from: ".planning/REQUIREMENTS.md status table"
      to: "the discharging `35-NN-SUMMARY.md` or `35-LIVE-GATE.md` criterion"
      via: "an explicit citation per corrected row"
      pattern: "35-[0-9]{2}"
    - from: ".planning/REQUIREMENTS.md `REQ-35-16` and `REQ-35-17` status cells"
      to: "`35-25-SUMMARY.md` and `35-26-SUMMARY.md`'s recorded Task 3 checkpoint outcomes"
      via: "a per-row citation of the OWNING plan, never `35-29`"
      pattern: "35-2[56]"
---

<objective>
Close the records-hygiene item the verifier called for. `.planning/REQUIREMENTS.md` marks 18 of the
21 Phase 35 rows `Planned (2026-08-28 -- minted during /gsd-plan-phase 35; 19 plans across 13 waves)`
— boilerplate from mint time — when at least ten are demonstrably complete. Only `REQ-35-02`,
`REQ-35-18` and `REQ-35-19` carry a real status.

The requirement BODY bullets below the table are largely accurate and already carry per-requirement
completion notes; the STATUS COLUMN did not keep up. A status table that says `Planned` for shipped
work is the same class of defect as a SUMMARY that is wrong while its source is right: the next
reader trusts the wrong one.

Purpose: `35-VERIFICATION.md` names this explicitly, and the operator scoped it in. A phase cannot
close on records that misdescribe it.

Output: a Phase 35 status table where every row's status is evidenced, and every row that cannot be
evidenced stays `Planned`.

**This plan marks nothing complete that it cannot evidence, and holds nothing open that its
evidence discharges.** The four requirements the verifier left open fall into TWO different
situations, and collapsing them into one blanket deferral is itself a records defect:

- `REQ-35-07` and `REQ-35-20` close on plan `35-29`'s live gate re-run, which has not happened.
  These are the only two IDs `35-29`'s own `requirements:` frontmatter carries. Both rows stay open
  citing `35-29`.
- `REQ-35-16` and `REQ-35-17` do NOT depend on the live gate. Neither closure clause in
  `REQUIREMENTS.md` names a gate criterion, and neither ID appears anywhere in `35-29-PLAN.md` --
  `grep -c` returns 0 for both. `REQ-35-16` is owned by plan `35-25` and `REQ-35-17` by plan `35-26`,
  and each closes via that plan's own BLOCKING human checkpoint in its Task 3. Both are wave-1 plans
  and this plan is wave 2 depending on both, so both checkpoint outcomes are already recorded and
  citable when this runs. Each row is therefore decided from its OWN plan's recorded outcome:
  `Complete` if that checkpoint succeeded, open citing THAT plan if it did not.

Deferring `REQ-35-16` or `REQ-35-17` to `35-29` would understate the record permanently — `35-29`
does not modify `.planning/REQUIREMENTS.md`, so no later plan corrects it. That is the same defect
class this plan exists to remove, pointed the other way.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md

<interfaces>
<!-- Measured 2026-08-30. -->

`.planning/REQUIREMENTS.md` Phase 35 status table, rows 423-443:
  REQ-35-02  Complete (2026-08-30 -- 35-15/35-16/35-18 portions, commits 7ed470b19 / 2e826a395)
  REQ-35-18  Complete (2026-08-29 -- 35-05 and 35-16, commit 8dafd8d91)
  REQ-35-19  Complete (2026-08-29 -- 35-17, commits a4f9631ef ... 0bdf1d6a4)
  ALL EIGHTEEN OTHERS: the identical mint-time boilerplate string.

The requirement BODY bullets (lines ~1137-1156) are the better source: several already carry
per-requirement completion notes with dates, plan numbers and commit SHAs
(`REQ-35-02`, `REQ-35-18`, `REQ-35-19`, `REQ-35-21`).

Evidence sources available, in descending order of authority:
  1. `.planning/phases/35-electron-cutover-remove-the-electron-build/35-NN-SUMMARY.md` — all 19 exist
  2. `35-LIVE-GATE.md` — 21 criteria with `Observed:` and a verdict each
  3. `35-VERIFICATION.md` — the independent 11/17 truth table, verified against SOURCE rather than
     read out of SUMMARYs
  4. `.planning/ROADMAP.md`'s Phase 35 plan checklist (18 of 19 boxes ticked)

`35-VERIFICATION.md`'s findings, and what each one's CURRENT status actually turns on.
`35-VERIFICATION.md` was written BEFORE the wave-1 gap-closure plans ran, so wherever a wave-1 plan
OWNS the requirement it records the PRE-FIX state and is superseded by that plan's SUMMARY:
  - `REQ-35-07`: code verified correct, closure evidence does NOT exist (D-35-19-15). Closes on plan
    `35-29`'s gate re-run — `35-29` carries this ID. NOT complete at this wave.
  - `REQ-35-20`: gate verdict FAIL. Closes on plan `35-29`'s gate re-run — `35-29` carries this ID.
    NOT complete at this wave.
  - `REQ-35-16`: PRE-FIX state was `openDialog` ✓, watcher backend ✓ / UI ✗, `winetricksInstall` ✗.
    Owner is plan `35-25` (wave 1, `requirements: [REQ-35-16]`) — its sole remaining owner. `35-25`
    Task 4 records that the `openDialog` and `installed.json` clauses are ALREADY discharged and
    their text stands, leaving the winetricks clause as the only outstanding one. Closure signal =
    `35-25` Task 3's live MOUSE gesture, whose steps 4, 6 and 7 are each scored `WORKED` or
    `DID-NOT-WORK`. Verified 2026-08-30: `grep -c 'REQ-35-16' 35-29-PLAN.md` = **0**.
  - `REQ-35-17`: PRE-FIX state was path-rejection ✓, both SEAM items ✓, EOS ✗. Owner is plan `35-26`
    (wave 1, `requirements: [REQ-35-17]`) — its sole remaining owner, with the EOS clause the only
    outstanding one. Closure signal = `35-26` Task 3's live EOS dialog gesture, whose steps are each
    scored `PASS` or `FAIL` across ONE LIGHT and ONE DARK theme, with a screenshot filename per theme
    and the cancel path proven non-destructive from `gamelib.log`. Verified 2026-08-30:
    `grep -c 'REQ-35-17' 35-29-PLAN.md` = **0**.

A plan cannot discharge a requirement it never mentions. `35-29`'s frontmatter is
`requirements: [REQ-35-07, REQ-35-20]` and those are the only two IDs any row here may cite it for.
  - `REQ-35-04`: all three tray settings honoured and the About window reachable, BUT the
    recent-games submenu is hollow for Steam (criterion 6 FAIL). Partial at best.
  - `REQ-35-10`/`REQ-35-11`: `R-34.5-G1-PKG` closed on BOTH halves, artifact-proven against a real
    packaged `.app` by the verifier in its own process.
  - `REQ-35-14`: `git tag -l` confirms `pre-electron-cutover` exists; the verifier used it as a live
    oracle. Checklist has zero MISSING rows.
  - `REQ-35-15`: `35-AB-RETEST.md` exists, 7 items scored across 2 shells, every `Observed:` filled.

DO NOT run any `gsd-sdk state.*` verb against `.planning/`. Four of them corrupt `STATE.md` and
`ROADMAP.md`; `annotate-dependencies` DELETES plan rows; `plain-diff` and a `cat -n` diff both
misreport. Hand-edit this file.

Plan `35-25` also edits `.planning/REQUIREMENTS.md`, amending `REQ-35-16`'s BODY bullet in its
Task 4. Plan `35-26` does not touch this file at all. This plan runs after both
(`depends_on: ["35-25", "35-26"]`) and touches only the STATUS TABLE. Do not edit any body bullet.

Second signal on `35-25`: its Task 4 runs ONLY if Task 3's mouse gesture scored `WORKED`. `35-25`
states outright that if the mouse click still does nothing, Task 4 must NOT run and `REQ-35-16` must
NOT be amended. So the presence of Task 4's dated correction inside `REQ-35-16`'s body bullet is an
independent corroboration that `35-25`'s checkpoint succeeded — and its absence corroborates the
opposite. Use it to cross-check the SUMMARY, not to replace reading the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the evidence map before touching a single row</name>
  <files>(none — measurement only; no file is modified by this task)</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 420-445 (the Phase 35 status table) and lines 1130-1160 (the
      Phase 35 requirement body bullets). Read both fully — the body bullets are where most of the
      evidence already is.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md` in full,
      especially the 17-row truth table and the five gaps. It is the only source that verified claims
      against SOURCE rather than reading them out of a SUMMARY.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md` — the verdict
      header and the four FAIL criteria (6, 10, 14, 16).
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-25-SUMMARY.md` — in
      particular its record of Task 3's live mouse gesture and the per-step `WORKED` /
      `DID-NOT-WORK` verdicts. This is the sole deciding evidence for `REQ-35-16`.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-26-SUMMARY.md` — in
      particular its record of Task 3's live EOS gesture: the per-step `PASS` / `FAIL` verdicts, the
      two theme screenshot filenames, and the log evidence that cancel destroyed nothing. This is the
      sole deciding evidence for `REQ-35-17`.
    - Each other `35-NN-SUMMARY.md` you intend to cite. Cite only ones you have actually read.
  </read_first>

  <action>
    Produce a 21-row evidence map, one row per `REQ-35-01` .. `REQ-35-21`. Each row records:
    the requirement ID; the proposed status (`Complete`, `Partial`, or `Planned`); and the SPECIFIC
    evidence — a `35-NN-SUMMARY.md` section, a gate criterion number and its verdict, or a
    `35-VERIFICATION.md` truth-table row.

    Rules that decide the status, applied in this order:
    - If `35-VERIFICATION.md` scored the corresponding truth `✗ FAILED` or `✗ FAILED (partial)` and
      NO wave-1 gap-closure plan owns that truth, the row is NOT `Complete`. The verifier checked
      source; a SUMMARY claiming otherwise loses. EXCEPTION, and it is narrow: where a wave-1
      gap-closure plan owns the failed truth (`35-25` owns `REQ-35-16`, `35-26` owns `REQ-35-17`) and
      has since run, that plan's SUMMARY and its blocking checkpoint outcome are the LATER and
      therefore governing evidence — `35-VERIFICATION.md` records only the pre-fix state for those
      two IDs. This exception applies to exactly those two IDs and no others.
    - If the requirement's own closure clause names a live gate criterion and that criterion is FAIL
      or was never run, the row is NOT `Complete`.
    - `Complete` requires a citation a reader can follow to something that exists. "Plan 35-NN
      landed" is not evidence; "35-NN-SUMMARY.md, commit `<sha>`, gate criterion N PASS" is.
    - Anything you cannot evidence stays `Planned`, unchanged. An honest `Planned` is better than an
      unevidenced `Complete`, and this table's whole defect is a status that outran its evidence.

    Do not edit `.planning/REQUIREMENTS.md` in this task.

    Decide the four requirements the verifier left open BY OWNER, one at a time. Do not treat them
    as a block — they do not share a blocker.

    - `REQ-35-07` and `REQ-35-20`: open, each naming plan `35-29` as its outstanding dependency.
      These two IDs, and only these two, are what `35-29`'s `requirements:` frontmatter carries.
    - `REQ-35-16`: decided from `35-25-SUMMARY.md`'s record of its Task 3 live mouse gesture.
      If steps 4, 6 and 7 are EACH recorded `WORKED`, propose `Complete`, citing
      `35-25-SUMMARY.md`, that Task 3 gesture, and the commit. Cross-check that `35-25` Task 4's
      dated correction is present in `REQ-35-16`'s body bullet — Task 4 only runs when Task 3
      passed, so its absence contradicts a `WORKED` reading and you must resolve the contradiction
      before proposing anything.
      If ANY of those steps is recorded `DID-NOT-WORK`, or `35-25-SUMMARY.md` has no Task 3 record at
      all, propose `Planned` naming plan `35-25` as the blocker and stating WHICH step failed or
      which record is missing.
    - `REQ-35-17`: decided from `35-26-SUMMARY.md`'s record of its Task 3 live EOS gesture.
      If EVERY step is recorded `PASS`, both themes carry a screenshot filename, and the cancel path
      is recorded non-destructive from `gamelib.log`, propose `Complete`, citing
      `35-26-SUMMARY.md`, that Task 3 gesture, and the commit.
      If ANY step is recorded `FAIL`, a theme's screenshot filename is missing, or
      `35-26-SUMMARY.md` has no Task 3 record at all, propose `Planned` naming plan `35-26` as the
      blocker and stating WHICH step failed or which record is missing.
    - `REQ-35-04`: partial pending criterion 6's re-measure.

    Do NOT cite plan `35-29` for `REQ-35-16` or `REQ-35-17` under any circumstance, in either
    direction. Neither ID appears in `35-29-PLAN.md`, `35-29` does not modify
    `.planning/REQUIREMENTS.md`, and no later plan corrects a wrong citation here — so a deferral to
    `35-29` for either ID is permanent and wrong.

    The evidence bar is unchanged in both directions: a checkpoint outcome you cannot actually read
    out of a SUMMARY is not evidence, and the row stays `Planned` saying exactly what is missing.
  </action>

  <acceptance_criteria>
    - The map has exactly 21 rows, one per requirement ID, with no ID skipped or duplicated.
    - Every row proposing `Complete` names a file path plus a section, criterion number or commit
      SHA. Rows proposing `Planned` may cite the blocker instead.
    - `REQ-35-07` and `REQ-35-20` are NOT proposed `Complete`, and each names plan `35-29` as its
      outstanding dependency.
    - The `REQ-35-16` row names plan `35-25`, and no other plan, as the source of the evidence that
      decided it, and quotes the literal per-step verdict words it read out of `35-25-SUMMARY.md`
      (`WORKED` or `DID-NOT-WORK`) for steps 4, 6 and 7. It is proposed `Complete` if and only if all
      three read `WORKED`.
    - The `REQ-35-17` row names plan `35-26`, and no other plan, as the source of the evidence that
      decided it, and quotes the literal per-step verdicts (`PASS` / `FAIL`) plus BOTH theme
      screenshot filenames it read out of `35-26-SUMMARY.md`. It is proposed `Complete` if and only
      if every step reads `PASS` and both screenshot filenames are present.
    - Neither the `REQ-35-16` row nor the `REQ-35-17` row mentions plan `35-29`. Check by grepping
      the map text for `35-29` and confirming the only hits are the `REQ-35-07` and `REQ-35-20` rows;
      record that grep and its hits.
    - Record, in the map, the measured `grep -c 'REQ-35-16' 35-29-PLAN.md` and
      `grep -c 'REQ-35-17' 35-29-PLAN.md` counts — re-run them yourself, do not copy the number from
      this plan.
    - If `REQ-35-16` or `REQ-35-17` is proposed `Planned`, the row states the SPECIFIC failed step or
      missing record, not a generic "pending".
    - `REQ-35-04` is not proposed `Complete` while criterion 6 stands FAIL.
    - Every cited SUMMARY file exists — verify with `ls`, and record the check.
    - `git status --short` shows no modified file — this task measures only.
  </acceptance_criteria>

  <verify>
    <automated>ls .planning/phases/35-electron-cutover-remove-the-electron-build/*-SUMMARY.md | wc -l</automated>
  </verify>

  <done>
    A 21-row evidence map exists, every `Complete` proposal is backed by a followable citation, and
    the four gate-dependent requirements are explicitly held open.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply the corrections to the status table only</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - Task 1's evidence map.
    - `.planning/REQUIREMENTS.md` lines 423-443 as they stand AFTER plan `35-25`'s body-bullet
      amendment — re-read, do not work from a stale snapshot. A concurrent session can move HEAD.
    - The three already-correct rows (`REQ-35-02`, `REQ-35-18`, `REQ-35-19`) — match their format
      exactly so the table stays uniform.
    - `35-25-SUMMARY.md` and `35-26-SUMMARY.md`, at least far enough to confirm the exact sections
      Task 1's map cites for `REQ-35-16` and `REQ-35-17` are really in them. You are about to write
      those citations into a permanent record; confirm each one resolves.
  </read_first>

  <action>
    Rewrite each status cell that Task 1's map says is wrong, following the existing format of the
    three correct rows: a status word, a parenthesised date, and the evidence.

    Use a status vocabulary of exactly three values — `Complete`, `Partial`, `Planned` — and if
    `Partial` is not already used in this file, say in the corrected cell what remains outstanding
    and which plan owns it, so the word carries information rather than ambiguity.

    Every corrected cell carries the correction date `2026-08-30` and the citation from the map.

    Transcribe Task 1's map — do not re-decide any row here, and do not substitute a plan number.
    In particular: `REQ-35-16`'s cell cites plan `35-25` and `REQ-35-17`'s cites plan `35-26`,
    whatever their status word turned out to be. Only `REQ-35-07` and `REQ-35-20` may cite plan
    `35-29`, because those are the only two IDs `35-29` carries.

    Leave every requirement BODY bullet untouched. Plan `35-25` owns the `REQ-35-16` body amendment;
    editing a body bullet here would collide with it and would also mix two kinds of change in one
    diff.

    Hand-edit. Do not invoke any `gsd-sdk state.*` verb.
  </action>

  <acceptance_criteria>
    - `git diff .planning/REQUIREMENTS.md` shows changes confined to lines within the Phase 35 status
      table. Verify by inspecting the diff hunk line ranges against the table's boundaries, and state
      those ranges in the SUMMARY.
    - No line containing `- [ ] **REQ-35-` or `- [x] **REQ-35-` appears in the diff — the body
      bullets are untouched.
    - `grep -c 'minted during' .planning/REQUIREMENTS.md` decreases by exactly the number of rows
      Task 1's map said to correct. State the before and after counts.
    - No Phase 35 status cell reads `Complete` for `REQ-35-07` or `REQ-35-20`, and each of those two
      cells names plan `35-29`.
    - The `REQ-35-16` and `REQ-35-17` status cells match Task 1's map exactly: same status word, same
      cited plan. Quote both cells VERBATIM in the SUMMARY.
    - `REQ-35-16`'s status cell contains the string `35-25` and does NOT contain the string `35-29`.
      `REQ-35-17`'s status cell contains the string `35-26` and does NOT contain the string `35-29`.
      Verify with a grep over those two lines and record both results.
    - Wherever either of those two cells reads `Complete`, it cites that plan's `35-NN-SUMMARY.md`
      AND its Task 3 checkpoint. Wherever either reads `Planned`, it names the specific failed step
      or missing record, and names that plan — not `35-29` — as the blocker.
    - Every corrected cell contains at least one of: a `35-NN-SUMMARY.md` reference, a commit SHA, or
      a gate criterion number — verify with a grep over the changed lines and record the result.
    - The table still has exactly 21 Phase 35 rows and no row was reordered or deleted.
    - `pnpm test --selectProjects Meta` passes — planning gates run in CI and two of them have been
      silently red before; a docs edit is not exempt from checking.
  </acceptance_criteria>

  <verify>
    <automated>git diff --stat .planning/REQUIREMENTS.md &amp;&amp; pnpm test --selectProjects Meta</automated>
  </verify>

  <done>
    Every Phase 35 status cell states an evidenced status; `REQ-35-07` and `REQ-35-20` remain open
    citing `35-29`; `REQ-35-16` and `REQ-35-17` each state what their own owning plan's checkpoint
    outcome supports and cite `35-25` / `35-26` respectively; and no body bullet or row ordering
    changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning records -> every future reader and tool | `REQUIREMENTS.md` is read by planners, verifiers and coverage gates; a wrong status propagates into decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-130 | Repudiation | a status marked `Complete` without evidence | mitigate | Every `Complete` cell must carry a followable citation; anything unevidenced stays `Planned`. Task 1 builds the map before any edit, so no cell is corrected from memory. |
| T-35-131 | Tampering | a closure gate forcing a false record — OVERSTATING | mitigate | `REQ-35-07` and `REQ-35-20` are held open pending `35-29`, so the desire to close the phase cannot pull a status forward past its evidence. This is a recorded failure shape in this project. |
| T-35-134 | Repudiation | a status held open past its evidence — UNDERSTATING | mitigate | `REQ-35-16` and `REQ-35-17` are decided from their OWN owning plans' recorded Task 3 checkpoint outcomes (`35-25`, `35-26`), never deferred to `35-29`, whose frontmatter carries neither ID and which does not modify this file. Task 1's criteria are two-sided and forbid citing `35-29` for either ID, so a wrong deferral cannot be hard-coded. No later plan corrects this file, so an understated row here would be permanent. |
| T-35-132 | Tampering | `gsd-sdk state.*` corrupting `REQUIREMENTS.md`/`STATE.md` | mitigate | The file is hand-edited; no SDK state verb is invoked. Four of those verbs are known to corrupt planning files and one deletes plan rows. |
| T-35-133 | Repudiation | a docs edit skipping the CI planning gates | mitigate | `pnpm test --selectProjects Meta` is run; planning gates now execute in CI and have been silently red before. |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed. |
</threat_model>

<verification>
- `git diff .planning/REQUIREMENTS.md` is confined to the Phase 35 status table, with the hunk line
  ranges stated.
- `pnpm test --selectProjects Meta` passes.
- The 21-row evidence map is in the SUMMARY, in full.
- The `REQ-35-16` and `REQ-35-17` status cells are quoted verbatim in the SUMMARY; neither contains
  the string `35-29`, and they cite `35-25` and `35-26` respectively.
- The re-measured `grep -c 'REQ-35-16' 35-29-PLAN.md` and `grep -c 'REQ-35-17' 35-29-PLAN.md` counts
  are recorded.

Do NOT gate on `pnpm lint` exiting 0 or on a green `.husky/pre-push`.
</verification>

<success_criteria>
- Every Phase 35 status cell is evidenced or honestly `Planned`.
- `REQ-35-07` and `REQ-35-20` are still open, each citing plan `35-29`.
- `REQ-35-16` and `REQ-35-17` each state the status their OWN owning plan's recorded Task 3
  checkpoint outcome supports — `35-25` and `35-26` respectively — and neither cites `35-29`.
- No requirement body bullet changed.
</success_criteria>

<output>
Create `.planning/phases/35-electron-cutover-remove-the-electron-build/35-28-SUMMARY.md` when done.
Record the full 21-row evidence map, the before/after `minted during` counts, and the diff hunk line
ranges proving the edit stayed inside the status table.
</output>
