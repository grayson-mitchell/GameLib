---
phase: 35-electron-cutover-remove-the-electron-build
plan: 11
type: execute
wave: 3
depends_on:
  - 35-02
  - 35-06
files_modified:
  - src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts
  - src/backend/sidecar/installFlowRegistration.ts
  - src/backend/downloadmanager/downloadqueue.ts
  - src/backend/sidecar/appShellFlowRegistration.ts
  - public/locales/en/gamelib.json
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
autonomous: false
requirements:
  - REQ-35-17
decisions: [D-05, D-18]

must_haves:
  truths:
    - "D-05: the EOS remove dialog and the path-rejection dialog stop being native system dialogs sized wrong for their content and become app-styled, consistent with the ~14 other dialogs that already use the app pattern"
    - "SEAM Phase 33 D-04 is a REAL FEATURE GAP, not a documentation convergence. `main.ts:613`'s `initQueue(true)` — the boot-time download-queue auto-resume — was deliberately never replicated on the sidecar, and `main.ts` dies at the point of no return. Either it is ported or the app permanently stops resuming interrupted downloads at startup"
    - "The auto-resume port is CONTINGENT: `G-30-02`'s underlying install-resume bug and the CrossOver-bottle resume bug (Phase 33 D-07) are the reason the suppression exists. Auto-resuming into a known-broken resume path surfaces the bug more often, it does not fix anything. The contingency is resolved by measurement, and if it is not met the suppression is RE-RECORDED with a new owner rather than silently carried"
    - "SEAM Phase 31 D-02 (settings divergence) is moot by construction once there is one build, and is closed with a dated note after confirming no dead cross-build reflect code exists to clean up — confirmed, not assumed"
    - "Every new or changed user-facing string lands in `public/locales/en/gamelib.json`, never `translation.json`"
  artifacts:
    - path: "src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts"
      provides: "the EOS remove confirmation routed through the app-styled dialog path"
    - path: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      provides: "dated dispositions for Phase 31 D-02 and Phase 33 D-04"
      contains: "Phase 35"
  key_links:
    - from: "src/backend/downloadmanager/downloadqueue.ts initQueue"
      to: "the sidecar boot path"
      via: "startup auto-resume ported from main.ts:613's initQueue(true), contingent on G-30-02"
      pattern: "initQueue"
---

<objective>
Close the two folded UI-affordance todos and the two SEAM convergence items that land here, and
resolve the one of them that is a real feature gap rather than a paperwork exercise.

Purpose: D-05's rule reaches the dialogs — an EOS remove confirmation rendered as a native system
dialog and a path-rejection dialog using the oversized large-text window are both inconsistent
affordances in a build that is about to become the only build. And SEAM's Phase 33 D-04 is the item
that matters most: `main.ts:613`'s `initQueue(true)` boot-time auto-resume has no sidecar
equivalent, and its carrier file is deleted two plans from now.

Output: two app-styled dialogs, a resolved disposition for boot-time auto-resume, and dated SEAM
entries for both convergence items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-CONTEXT.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-RESEARCH.md
@.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md

<interfaces>
TODO 1 — `.planning/todos/2026-08-24-eos-remove-dialog-renders-as-a-native-system-dialog-not-app-styled.md`.
The Legendary/EOS overlay remove-confirmation uses `dialog.ts`'s `showDialogBoxModalAuto`-style
native dialog instead of the app-styled `Dialog` component ~14 other dialogs already use. Site:
`eos_overlay.ts`. Not broken, just inconsistent — a straightforward D-05-adjacent polish item.

TODO 2 — `.planning/todos/2026-08-26-path-rejection-dialog-uses-an-oversized-large-text-window.md`.
Two `showDialogBoxModalAuto({title, message, type: 'ERROR'})` call sites in
`installFlowRegistration.ts`, per the todo at lines 317 and 444 — **re-grep, do not trust the line
numbers**. The todo's own body marks G2-3 PASSED, meaning the reject-direction LOGIC is fine and
only the dialog's visual size is residual.

**Before restyling either dialog, read `Skill("sketch-findings-gamelib")`'s reference files** —
`navigation-shell.md` in particular — for the validated app dialog patterns, the multi-theme
survival rules, and the traffic-light inset. Note two recorded traps that apply directly here:
a primitive's stylesheet can be 100% DEAD against its component (which is what hides 25 dialogs),
and MUI runs in LIGHT mode with no `palette`, so every component needs its own colours. Verify the
app-styled `Dialog` you route to actually renders styled, rather than assuming the class name does
something — asserting a CSS class appears in a mock render tree passes even when the class has no
visible effect, and this project has shipped exactly that failure.

SEAM ITEM — Phase 31 D-02 (settings divergence), `SEAM.md`. Accepted as document-only: the sidecar
persists settings but pushes no `settingsChanged` reflect notification to a concurrently-running
Electron instance. Moot once there is one build. **Confirm no dead cross-build reflect code exists**
before closing — research found none, but "found none" from a research pass is not a grep.

SEAM ITEM — Phase 33 D-04 (boot-time auto-resume), `SEAM.md:214-218`. `main.ts:613`'s
`initQueue(true)` is Electron's 5-second boot-time download-queue auto-resume. Deliberately NOT
replicated on the sidecar because the underlying install-resume path was already parked as a known
bug (`G-30-02`) plus a separate CrossOver-bottle resume bug (Phase 33 D-07). The suppression is
logged, not silent. Verified at plan time: `main.ts:611-613` carries a comment beginning
"initQueue's doc comment in downloadqueue.ts ... GOG/Epic/Amazon keep" — read it in full.

Strings: `public/locales/en/gamelib.json` only. `{{count}}` is a RESERVED i18next interpolation
name — a plural key using it silently returns the English default.

jest: `--selectProjects Backend` / `Frontend` (capital first letter).
</interfaces>
</context>

<constraints>
- **Do not modify `src/backend/main.ts`.** Read it to port from; plan 35-14 deletes it.
- **Do not enable boot-time auto-resume if `G-30-02` is still open.** Auto-resuming into a broken
  resume path makes the bug more frequent, not fixed. If the contingency fails, port the code
  DISABLED behind an explicit flag with a comment naming `G-30-02`, and re-record the suppression
  in `SEAM.md` with Phase 35 as its new owner — so the item does not simply vanish with `main.ts`.
- **Do not close a SEAM item without a dated note.** Dispositions are table rows or bullet appends,
  never new `### <ID>` headings — the house format.
- **Do not assert a dialog is app-styled by checking for a class name in a render tree.** That
  passes against a dead stylesheet. Verify against a real render, and if that is not automatable
  say so and record it as a manual check rather than claiming coverage.
- **Do not put strings in `translation.json`.**
- Never `git stash`, never `git reset`, never `git checkout -- <path>`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Route the EOS remove and path-rejection dialogs onto the app-styled path</name>
  <read_first>Read `Skill("sketch-findings-gamelib")` — specifically `references/navigation-shell.md` — for the validated dialog patterns and theme rules; both todo files IN FULL; src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts IN FULL; src/backend/sidecar/installFlowRegistration.ts — grep `showDialogBoxModalAuto` to find the real current line numbers, then read both call sites and their surrounding reject logic; src/backend/dialog/dialog.ts for `showDialogBoxModalAuto`'s signature and what distinguishes the native from the app-styled path; at least two of the ~14 existing app-styled `Dialog` call sites for the house pattern</read_first>
  <files>src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts, src/backend/sidecar/installFlowRegistration.ts, public/locales/en/gamelib.json</files>
  <action>
First establish, by reading `dialog.ts`, what actually makes a dialog render as a native system
dialog versus the app-styled component — it may be a `type`, a flag, or a different function
entirely. State the mechanism in the summary before changing anything; "route it to the app-styled
path" is not an instruction you can follow without knowing which switch that is.

**EOS remove dialog.** Change the remove-confirmation in `eos_overlay.ts` to use the same
app-styled path the other ~14 dialogs use. Keep the semantics identical: same title, same message,
same button set, same return contract for confirm/cancel. If the confirmation's return value is
consumed to gate a destructive removal, do not change its shape — a confirm dialog whose cancel
path stops being distinguishable from confirm is far worse than a native-looking one.

**Path-rejection dialog.** The two `showDialogBoxModalAuto({title, message, type: 'ERROR'})` call
sites in `installFlowRegistration.ts` render in the large-text window model. The todo records the
reject LOGIC as already correct (G2-3 PASSED), so change only the presentation: use the sizing the
other error dialogs use. Do not touch the rejection conditions.

Any string that changes goes in `public/locales/en/gamelib.json`. If you reuse an existing key, do
not rename it via the `t()` default argument — that is a silent no-op when the key already exists.

Where automated coverage is possible, add it. Where it is not — and rendering-fidelity generally is
not, per the dead-stylesheet trap — say so explicitly and defer to Task 3's manual check rather
than writing an assertion that would pass against an unstyled dialog.
  </action>
  <acceptance_criteria>
- The summary names the concrete mechanism that distinguishes native from app-styled dialogs in `dialog.ts`.
- `grep -c "showDialogBoxModalAuto" src/backend/storeManagers/legendary/eos_overlay/eos_overlay.ts` prints `0`, or the summary explains why a remaining call is not the remove confirmation.
- The path-rejection call sites in `installFlowRegistration.ts` no longer use the large-text window model, and the surrounding rejection conditions are unchanged: `git diff src/backend/sidecar/installFlowRegistration.ts` shows no change to any `if`/comparison in the reject logic.
- The EOS confirm/cancel return contract is unchanged: `git diff` shows the consuming branch untouched.
- No string was added to `public/locales/en/translation.json`: `git diff --name-only | grep -c translation.json` prints `0`.
- `pnpm codecheck` exits 0 and the repo's i18n lint passes.
- `pnpm test --selectProjects Backend` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm codecheck && pnpm test --selectProjects Backend</automated>
  </verify>
  <done>Both dialogs use the app's own dialog surface, with their logic and return contracts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve boot-time auto-resume and close both SEAM convergence items with dated notes</name>
  <read_first>.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md:200-230 for Phase 33 D-04 and its neighbours; src/backend/main.ts:600-625 IN FULL for `initQueue(true)` and its comment; src/backend/downloadmanager/downloadqueue.ts — `initQueue`'s doc comment and its `isStartup` parameter's full behaviour including the 5-second timer; the `G-30-02` record (grep `G-30-02` across `.planning/`) to establish whether the underlying install-resume bug is OPEN or CLOSED; the Phase 33 D-07 CrossOver-bottle resume bug record; src/backend/sidecar/appShellFlowRegistration.ts for where sidecar boot-time work is registered; `35-AB-RETEST.md` for any row touching resume behaviour</read_first>
  <files>src/backend/downloadmanager/downloadqueue.ts, src/backend/sidecar/appShellFlowRegistration.ts, .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</files>
  <action>
**Establish the contingency first, by measurement.** Grep `.planning/` for `G-30-02` and for the
Phase 33 D-07 CrossOver-bottle resume bug and determine, for each, whether it is OPEN or CLOSED
today. Write both answers, with the documents you read, into the summary BEFORE writing any code.
This determines which of two branches you take, and taking the wrong one either ships a feature
into a broken path or silently drops a feature with `main.ts`.

**Branch A — both resume bugs CLOSED:** port the boot-time auto-resume to the sidecar. Register it
where the sidecar's other boot-time work lives, call `initQueue(true)` with the same `isStartup`
semantics and the same 5-second delay, and carry `main.ts:611-613`'s comment across. Add a test
asserting the startup path calls `initQueue` with `isStartup=true` exactly once and that a non-boot
initialisation does not.

**Branch B — either bug still OPEN:** port the code but leave it DISABLED behind an explicit,
named flag (not a commented-out block — a commented-out block is invisible to grep and to the next
reader). The flag's comment must name `G-30-02` and/or Phase 33 D-07 as the blocker and state that
enabling it before those close would surface the resume bug more often rather than fix anything.
Add a test asserting the disabled path does NOT call `initQueue(true)` at boot, so the suppression
is enforced rather than merely intended.

**Then close both SEAM items with dated notes**, as table rows or bullet appends — never new
`### <ID>` headings:
  - **Phase 31 D-02 (settings divergence):** first CONFIRM by grep that no dead cross-build reflect
    code exists (search for `settingsChanged` reflect/push paths that target a second build). Paste
    the grep and its result into the note. Then close it as moot-by-construction, dated, naming
    Phase 35 plan 11 — and state that the closure rests on a grep, not on a research assertion.
  - **Phase 33 D-04 (boot-time auto-resume):** record which branch was taken, the OPEN/CLOSED status
    of each blocker as measured today, and — under Branch B — that the suppression's owner is now
    Phase 35 rather than `main.ts`, with the flag's name so a later reader can find it. Under
    Branch B this item is NOT closed; it is re-homed with a named carrier. Say that explicitly
    rather than marking it done.
  </action>
  <acceptance_criteria>
- The summary states, for each of `G-30-02` and the Phase 33 D-07 CrossOver-bottle resume bug, `OPEN` or `CLOSED` and names the document that established it.
- The summary states which branch (A or B) was taken and why, consistent with those two statuses.
- Under either branch, `grep -rc "initQueue" src/backend/sidecar/ | grep -v ':0'` returns at least one site — the code was ported, not dropped with `main.ts`.
- Under Branch B, the flag is a real named identifier, not a comment: `grep -rc "G-30-02" src/backend/` is at least `1` and the flag name appears in the summary.
- A test exists asserting the intended behaviour of the chosen branch (fires once at boot under A; does not fire under B), and `pnpm test --selectProjects Backend` passes.
- `SEAM.md` contains a dated Phase 35 note for BOTH Phase 31 D-02 and Phase 33 D-04: `grep -c "Phase 35" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` is at least `2`.
- The Phase 31 D-02 note contains the literal grep command used and its result.
- `git diff .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md | grep -c '^+### '` prints `0` — no new headings, appends only.
- `src/backend/main.ts` unmodified.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --selectProjects Backend && node -e "const s=require('fs').readFileSync('.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md','utf8');const n=(s.match(/Phase 35/g)||[]).length;if(n<2)throw new Error('expected >=2 Phase 35 dispositions in SEAM.md, found '+n);console.log('SEAM dispositions:',n)"</automated>
  </verify>
  <done>Boot-time auto-resume survives the deletion of `main.ts` — enabled if its blockers are closed, explicitly suppressed with a named owner if not — and both SEAM items carry dated Phase 35 dispositions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Look at the two dialogs</name>
  <files>none — observation only; no file is edited by this task</files>
  <action>
Human-run visual verification in one light and one dark theme. Rendering fidelity is deliberately not asserted in code — a class name in a mock render tree passes against a completely dead stylesheet. Exercise BOTH the cancel and confirm branches of the destructive EOS dialog. Capture screenshots at non-dotfile paths (`screencapture` writes nothing to a dotfile and still exits 0).
  </action>
  <read_first>Task 1's summary, including the native-vs-app-styled mechanism it identified</read_first>
  <what-built>
The EOS remove confirmation and the two path-rejection error dialogs now route through the
app-styled dialog path. Their logic, conditions and return contracts are unchanged. Rendering
fidelity is deliberately NOT asserted in code — a class name present in a mock render tree passes
against a completely dead stylesheet, and this project has already shipped that failure.
  </what-built>
  <how-to-verify>
`pnpm tauri:dev`, then:

1. Trigger the EOS overlay remove flow. Confirm the confirmation is the app's own dialog — the same
   visual family as the other dialogs in the app — not a macOS system alert.
2. Click Cancel. Confirm nothing is removed.
3. Trigger it again and click Confirm. Confirm the removal happens. **Both branches must be
   checked** — a restyle that broke the cancel path would look fine and destroy data.
4. Trigger a path rejection during an install (pick a destination the app rejects). Confirm the
   error dialog is normally sized, not the oversized large-text window, and that the message is
   readable and complete.
5. Switch to a dark theme and repeat steps 1 and 4. MUI runs in light mode here with no `palette`,
   so a dialog that inherits nothing is a real and recorded failure mode.
6. Confirm no raw i18n keys appear in either dialog.

Capture a screenshot of each dialog in one light and one dark theme. Note: `screencapture` writes
NOTHING to a dotfile path and still exits 0 — use a normal filename.
  </how-to-verify>
  <acceptance_criteria>
- The EOS remove confirmation renders as an app dialog, and BOTH its cancel and confirm paths behave correctly.
- The path-rejection dialog is normally sized with a complete, readable message.
- Both dialogs render correctly in a dark theme as well as light.
- No raw i18n keys appear in either dialog.
- Four screenshots captured (two dialogs x two themes) at non-dotfile paths.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the six step results and the screenshot paths, or describe what looks wrong.</resume-signal>
  <verify>
    <human-check>Both dialogs render app-styled in light and dark themes; the EOS cancel path still cancels and the confirm path still removes.</human-check>
  </verify>
  <done>The restyle is verified by looking at it in both themes, which is the only thing that can verify it, and the destructive dialog's cancel path is confirmed intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user confirmation -> destructive EOS overlay removal | a dialog return value gates a destructive filesystem operation |
| interrupted download state on disk -> boot-time auto-resume | resuming acts on partially-written install state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-45 | Tampering | Restyling the EOS remove confirmation and breaking the confirm/cancel distinction, so cancel proceeds with a destructive removal. A dialog restyle that changes a return contract is a data-loss bug wearing a cosmetic diff | mitigate | The return contract is required to be unchanged (asserted by a `git diff` criterion on the consuming branch), and Task 3 exercises BOTH branches live rather than only the happy path |
| T-35-46 | Denial of Service | Enabling boot-time auto-resume while `G-30-02`'s install-resume bug is open — every startup drives the app into a known-broken path, turning an occasional bug into a launch-time one | mitigate | The port is explicitly contingent on a measured OPEN/CLOSED status for both blockers, with Branch B porting it disabled behind a named flag whose suppression is enforced by a test, not merely intended |
| T-35-47 | Repudiation | Phase 33 D-04 silently vanishing when `main.ts` is deleted, leaving no record that boot-time auto-resume ever existed | mitigate | Under Branch B the item is explicitly RE-HOMED to Phase 35 with the flag name recorded in `SEAM.md`, not closed. `main.ts`'s deletion cannot take the knowledge with it |
| T-35-48 | Repudiation | Closing Phase 31 D-02 on a research assertion that no dead reflect code exists | mitigate | The closure note must contain the literal grep and its result, and must state that it rests on that grep |
| T-35-49 | Information Disclosure | A path-rejection error dialog echoing a full filesystem path, including a home directory and username, into a screenshot attached to a public issue | accept | Existing behaviour; the message content is unchanged by this plan, only its presentation. Note for the screenshots: redact the home path before committing any capture, per the standing redact-forward decision |
| T-35-SC | Tampering | npm/cargo installs | accept | This plan adds no dependency |
</threat_model>

<verification>
- `pnpm codecheck`, `pnpm test --selectProjects Backend`, and the i18n lint all pass.
- `SEAM.md` carries at least two dated Phase 35 dispositions, appended as rows/bullets, no new headings.
- The `G-30-02` and Phase 33 D-07 statuses are recorded with their sources, and the chosen branch follows from them.
- `src/backend/main.ts` unmodified.
- Task 3's four screenshots captured and both EOS dialog branches exercised.
</verification>

<success_criteria>
No dialog in the app looks like it belongs to a different application, the destructive one still
cancels correctly, and the boot-time auto-resume feature survives `main.ts`'s deletion — either
working or explicitly and findably suppressed with a named owner.
</success_criteria>

<output>
Create `.planning/phases/35-electron-cutover-remove-the-electron-build/35-11-SUMMARY.md` when done.
</output>
