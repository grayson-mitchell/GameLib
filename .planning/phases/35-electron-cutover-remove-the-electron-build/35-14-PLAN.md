---
phase: 35-electron-cutover-remove-the-electron-build
plan: 14
type: execute
wave: 8
depends_on:
  - 35-02
  - 35-03
  - 35-11
  - 35-12
  - 35-13
files_modified:
  - src/backend/main.ts
  - src/backend/updater.ts
  - src/preload/index.ts
  - electron.vite.config.ts
  - electron-builder.yml
  - package.json
  - e2e/
  - playwright.config.ts
autonomous: false
requirements:
  - REQ-35-13
  - REQ-35-14
decisions: [D-13, D-17, D-05]

must_haves:
  truths:
    - "D-17: THIS IS THE POINT OF NO RETURN, and it is named. Everything before this plan was additive and both shells built. After it, only Tauri exists. The plan is deliberately small so the irreversible step is the diff you can read in full — the 67-file diff lands afterwards, in the safe zone, behaviourally inert"
    - "The tag `pre-electron-cutover` is pushed BEFORE any deletion, so the last commit at which both shells build is recoverable by name rather than by archaeology"
    - "D-17: `src/preload/api/*` SURVIVES — `tauriAttach` consumes it. Only the `contextBridge` block in `src/preload/index.ts` goes. The file's own header already documents that Tauri never loads that bundle, which is why the preload cut is genuinely small (D-00f)"
    - "D-13: the updater handover is a clean break. `src/backend/updater.ts` and `electron-updater` are deleted with no migration shim, because `gh release list` shows zero GameLib-branded releases — every listed tag is inherited upstream Heroic (D-00e). Tauri's updater plugin is already configured with a pubkey, a GitHub endpoint and `installMode: passive`"
    - "The `e2e/` Playwright suite dies here and that is decided explicitly, not discovered. It launches ELECTRON via `_electron` against `build/main/main.js`, so `main.ts`'s deletion kills all five specs — and with them the only thing that sets `CI=e2e`, which is the harness `35-CONTEXT.md` D-19 and the ROADMAP both point at as a cheap way to prove packaged asset resolution"
    - "`pnpm tauri:dev` and `pnpm tauri:dev:packaged` both still work after this plan, and that is verified before the plan is done — the sidecar entry `src/sidecar/index.ts` has no import edge into `src/backend/main.ts`, proven by `35-PREFLIGHT.md`'s CENSUS-MAINTS-EDGES"
  artifacts:
    - path: "src/preload/index.ts"
      provides: "the preload entry with the contextBridge block removed and src/preload/api/* intact"
  key_links:
    - from: "git tag pre-electron-cutover"
      to: "the last commit at which both shells build"
      via: "an annotated tag pushed before any deletion"
      pattern: "pre-electron-cutover"
---

<objective>
Delete the Electron entry points. This is the irreversible step, and it is one small reviewable plan
by design.

Purpose: D-17. The 67-file `from 'electron'` → `from 'backend/platform'` rewrite cannot be
additive — once it lands, the Electron main process would run against the sidecar shim instead of
real Electron. So "keep `pnpm start` working until the last plan" is not available. The choice is
therefore where to put the seam, and CONTEXT.md's answer is: put the irreversible step in the SMALL
diff and the large diff in the safe zone. The intuitive inverse — rewrite first, delete after —
puts the point of no return INSIDE the 67-file diff and leaves an interval where the Electron build
is silently broken while still appearing to exist. That is the worst state to be interrupted in.

`35-RESEARCH.md` validated this ordering with one refinement, adopted here: the esbuild
`--alias:electron=` does NOT need to move in this plan. It is still load-bearing until plan 35-15
removes the last `require('electron')` it rescues. Plan 35-18 removes it.

Output: a tag, four deleted entry points, a preload with its `contextBridge` removed, and a working
Tauri build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-CONTEXT.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-RESEARCH.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-PREFLIGHT.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md

<interfaces>
WHAT DIES:
  src/backend/main.ts        the Electron main entry. Contains `protocol.handle('gamelib')` (:501)
                             and `setAsDefaultProtocolClient` (:507) — replaced by plan 35-07;
                             `Menu.setApplicationMenu(null)` (:309); `powerSaveBlocker` (:650/:655)
                             — replaced by plan 35-08; `screen` (:386); the `electron-updater` wiring;
                             the `isPackaged` CSP branches (:321/:329) — replaced by plan 35-03's
                             `devUrl`; the `installed.json` watcher (:1038) — ported by plan 35-10;
                             `initQueue(true)` (:613) — dispositioned by plan 35-11.
                             **Every one of those has already been ported or dispositioned. Confirm
                             that before deleting, item by item — this is the last moment the
                             original is readable.**
  src/backend/updater.ts     `electron-updater` wiring (D-13)
  electron.vite.config.ts    replaced by `vite.config.ts` (plan 35-03)
  electron-builder.yml       the electron-builder packaging config
  package.json               `electron-vite`, `electron-builder`, `electron-updater` deps, and the
                             scripts that call them: `start`, `test:e2e`, `release:linux`,
                             `release:mac`, `release:win`, `dist:linux`, `dist:mac`, `dist:win`,
                             `sign:win`, `clean:dist-*`. Grep the scripts block; do not work from
                             this list.

WHAT SURVIVES — and this is the part that is easy to get wrong:
  src/preload/api/*          **MUST SURVIVE.** `tauriAttach` consumes it. Only the `contextBridge`
                             block inside `src/preload/index.ts` is removed. That file's own header
                             already states Tauri never loads the bundle (D-00f).
  src/sidecar/index.ts       the sidecar entry — a SEPARATE entry point from `main.ts`. This is what
                             Tauri spawns.
  meta/esbuildWorkerBundleShared.ts's `--alias:electron=` — still needed until plan 35-15. Do NOT
                             remove it here.
  the `electron` devDependency itself — plan 35-18 removes it, after the 67-file rewrite.

THE PRE-FLIGHT THAT GATES THE ORDERING — `35-PREFLIGHT.md`'s `CENSUS-MAINTS-EDGES`. Required
answer: zero import edges from `src/sidecar/` or `src/backend/sidecar/` into `src/backend/main.ts`.
A single stray edge breaks the Tauri path at exactly this commit. Re-run the grep here; do not rely
on the recorded result alone, because five plans have landed since it was taken.

THE e2e SUITE — `35-PREFLIGHT.md`'s `CENSUS-E2E`. `e2e/helpers.ts` imports `_electron` from
`@playwright/test` and launches `join(__dirname,'../build/main/main.js')`; all five specs
(`api`, `categories`, `languages_selector`, `settings`, `webview_controls`) route through that
helper; `test:e2e` runs `electron-vite build && cross-env CI=e2e xvfb-maybe -- playwright test`.
`paths.ts:75`'s `|| process.env.CI === 'e2e'` clause becomes unreachable in practice once nothing
sets it.

`gh release list` shows 20 releases, all inherited Heroic tags (`v2.22.1` down to `v2.14.0`), none
GameLib-branded — D-00e, so no updater migration path exists to design.

RECORDED GOTCHA: `git checkout -- <path>` fires `.husky/post-checkout` -> `download-helper-binaries`
-> throws. Restore with `cp` + `shasum -a 256`. Never `git stash`, never `git reset` — concurrent
sessions share this working tree.
</interfaces>
</context>

<constraints>
- **Tag before you delete.** If the tag push fails, stop. The recoverable point is the whole safety
  property of this ordering.
- **Do not delete `src/preload/api/*`.** Only the `contextBridge` block in `src/preload/index.ts`.
- **Do not remove the esbuild `--alias:electron=`** and do not remove the `electron` devDependency.
  Both are plan 35-18's, after the rewrite.
- **Do not start the 67-file rewrite here.** No file gains `from 'backend/platform'` in this plan.
  Keeping the irreversible step small is the entire design.
- **Do not delete `src/backend/tray_icon/tray_icon.ts`, `src/backend/main_window.ts` or any other
  backend module that still imports from `'electron'`.** They are plan 35-15's. This plan deletes
  ENTRY POINTS and BUILD CONFIG only.
- **Do not proceed if `CENSUS-MAINTS-EDGES` is non-zero** when re-run. Cut the edges first.
- Never `git stash`, never `git reset`, never `git checkout -- <path>`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the pre-flight, confirm every ported behaviour, and tag pre-electron-cutover</name>
  <read_first>src/backend/main.ts IN FULL — this is the last moment it is readable, and you are confirming that each of its behaviours has a successor; .planning/phases/35-*/35-PREFLIGHT.md section CENSUS-MAINTS-EDGES; the summaries of plans 35-03, 35-07, 35-08, 35-09, 35-10 and 35-11 — each claims to have ported or dispositioned one of `main.ts`'s behaviours; src/sidecar/index.ts to confirm what the Tauri path actually loads</read_first>
  <files>.planning/phases/35-electron-cutover-remove-the-electron-build/35-CUTOVER-CHECKLIST.md</files>
  <action>
Do not delete anything in this task. Produce a checklist and a tag.

Create `35-CUTOVER-CHECKLIST.md` with one row per behaviour `src/backend/main.ts` implements, built
by reading the file end to end — not from the list in this plan, which is a summary and may be
incomplete. For each row record: the `main.ts` line, what it does, its successor (plan number and
file), and a `CONFIRMED` / `MISSING` status verified by looking at the successor, not by trusting the
plan summary that claimed it.

Known rows to confirm at minimum: `protocol.handle('gamelib')` and `setAsDefaultProtocolClient`
(:501/:507 -> plan 35-07's Rust registration); `powerSaveBlocker` (:650/:655 -> plan 35-08);
`Menu.setApplicationMenu(null)` (:309); `screen` usage (:386) and `main_window.ts:33`'s
`screen.getPrimaryDisplay()` window sizing — **confirm rather than assume this is moot now that
Tauri owns the window**; the `isPackaged` CSP branches (:321/:329 -> plan 35-03's `devUrl`); the
`electron-updater` wiring (-> D-13, deleted, Tauri updater plugin already configured); the
`installed.json` watcher (:1038 -> plan 35-10); `initQueue(true)` (:613 -> plan 35-11); the i18n
`addPath`/`loadPath` under `publicDir/locales` (:481/:483) -> **confirm where the sidecar reads
locales from**, since plan 35-04 bundled them for `publicDir` and this is the consumer that proves
it matters.

**Any row marked `MISSING` stops this plan.** Report it and do not tag. A behaviour with no
successor becomes unrecoverable knowledge the moment the file is gone.

Re-run `grep -rn "backend/main'\|backend/main\"\|from '\.\./main'\|from './main'" src/sidecar/
src/backend/sidecar/` and paste the result into the checklist. Zero matches is required. If
non-zero, stop and name each edge.

Then confirm both shells still build at this commit — `pnpm start` reaches a window and
`pnpm tauri:dev` reaches a window — and record it. This is the last commit at which that is true,
which is exactly what the tag is for.

Finally, create and push an ANNOTATED tag `pre-electron-cutover` whose message records: the commit
sha, the date, that this is the last commit at which both shells build, and a one-line statement of
what the next commit removes. Verify the push with `git ls-remote --tags origin pre-electron-cutover`.
If the push fails, STOP.
  </action>
  <acceptance_criteria>
- `35-CUTOVER-CHECKLIST.md` exists with one row per `main.ts` behaviour, each carrying a `main.ts` line reference, a named successor, and a status of `CONFIRMED`.
- Zero rows are `MISSING`.
- The checklist contains the re-run `CENSUS-MAINTS-EDGES` grep and its output, and the output is empty.
- The checklist records that both `pnpm start` and `pnpm tauri:dev` reached a window at this commit.
- `git tag -l pre-electron-cutover` prints the tag and `git ls-remote --tags origin pre-electron-cutover` returns a matching ref — the tag is pushed, not just local.
- `git cat-file -t pre-electron-cutover` prints `tag` (annotated, not lightweight).
- `git diff --name-only HEAD~1` lists only the checklist file — nothing was deleted in this task.
  </acceptance_criteria>
  <verify>
    <automated>git cat-file -t pre-electron-cutover && git ls-remote --tags origin pre-electron-cutover | grep -q pre-electron-cutover && node -e "const s=require('fs').readFileSync('.planning/phases/35-electron-cutover-remove-the-electron-build/35-CUTOVER-CHECKLIST.md','utf8');if(/MISSING/.test(s))throw new Error('checklist has MISSING rows — do not proceed');if(!/CONFIRMED/.test(s))throw new Error('no CONFIRMED rows');console.log('cutover checklist clean, tag pushed')"</automated>
  </verify>
  <done>Every behaviour in the file about to be deleted has a confirmed successor, the sidecar has no edge into it, and the last both-shells-build commit is recoverable by name.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide the fate of the e2e Playwright suite</name>
  <files>none — decision only; the deletion happens in Task 3</files>
  <action>
Human decision. The `e2e/` Playwright suite launches Electron via `_electron` against `build/main/main.js` and dies with `src/backend/main.ts`; it is also the only thing that sets `CI=e2e`. Present the three options below and select one. Deleting test coverage silently is a recorded failure shape here, which is why this is a decision rather than a line in a deletion list.
  </action>
  <read_first>.planning/phases/35-*/35-PREFLIGHT.md section CENSUS-E2E; e2e/helpers.ts IN FULL; the five spec files in e2e/ (skim each for what it covers); playwright.config.ts; package.json's `test:e2e` script; src/backend/constants/paths.ts:73-76 for the `CI === 'e2e'` clause</read_first>
  <decision>What happens to the five-spec Playwright e2e suite, which launches Electron and dies with `src/backend/main.ts`?</decision>
  <context>
`e2e/helpers.ts` launches Electron via `_electron` from `@playwright/test`, pointed at
`build/main/main.js`. All five specs — `api`, `categories`, `languages_selector`, `settings`,
`webview_controls` — route through that helper. Deleting `src/backend/main.ts` and
`electron-vite` makes every one of them unrunnable.

Neither `35-CONTEXT.md` nor `35-RESEARCH.md` nor `35-PATTERNS.md` caught this; it was found at plan
time. It matters beyond the lost coverage: `test:e2e` is the ONLY thing in the repo that sets
`CI=e2e`, and `paths.ts:75`'s `|| process.env.CI === 'e2e'` clause is the "ready-made harness" both
`35-CONTEXT.md` D-19 and the ROADMAP point at as a cheap way to prove packaged asset resolution
without a full packaging run. That harness is Electron-backed and does not survive this phase.
(Plan 35-04 already proved `R-34.5-G1-PKG` against a real packaged artifact instead, so nothing is
currently blocked on it.)

Deleting test coverage silently is a failure shape this project has recorded repeatedly, which is
why this is a decision rather than a line in a deletion list.
  </context>
  <options>
    <option id="option-a">
      <name>Delete `e2e/`, `playwright.config.ts`, the `test:e2e` script and `@playwright/test`; record the lost coverage as a deferred item</name>
      <pros>Honest and immediate — an unrunnable suite is exactly the affordance-that-lies D-05 targets. No dead code, no red CI. The five specs' coverage is written down as a deferred item so it is reclaimable rather than forgotten.</pros>
      <cons>Real coverage loss with no successor in this phase. Whatever `settings`, `categories` and `languages_selector` were guarding becomes unguarded.</cons>
    </option>
    <option id="option-b">
      <name>Port the suite to `tauri-driver` / WebdriverIO in this phase</name>
      <pros>Coverage survives the cutover with no gap.</pros>
      <cons>`tauri-driver` is a different driver, a different runner and a different assertion API — this is a rewrite of all five specs plus new CI wiring, inside a phase that is already the largest in the project. It is its own project, and doing it here puts a green-CI dependency on untested new infrastructure at the point of no return.</cons>
    </option>
    <option id="option-c">
      <name>Delete the suite now, and keep the `CI=e2e` clause in `paths.ts` with a comment explaining it is currently unreachable</name>
      <pros>Same as (a), but preserves the cheap packaged-resolution harness hook for whoever builds the Tauri e2e path later, with an explanation instead of an orphan.</pros>
      <cons>Leaves a live conditional nothing sets — a small piece of dead logic in a security-adjacent path resolution, which is the kind of thing a later reader deletes without understanding.</cons>
    </option>
  </options>
  <acceptance_criteria>
- The reply names exactly one of `option-a`, `option-b`, `option-c`.
- If `option-a` or `option-c`, the reply confirms a deferred item recording the five specs' coverage is to be written.
- If `option-c`, the reply confirms the `CI === 'e2e'` clause stays with an explanatory comment.
- If `option-b`, the reply acknowledges this adds a suite rewrite to this plan and states whether the point of no return should wait for it.
  </acceptance_criteria>
  <resume-signal>Select: option-a, option-b, or option-c.</resume-signal>
  <verify>
    <human-check>An option is selected and the disposition of the lost coverage and of the `CI=e2e` clause is stated.</human-check>
  </verify>
  <done>The e2e suite's death is a decision with a recorded disposition rather than a side effect of a deletion list.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the Electron entry points and build config</name>
  <read_first>35-CUTOVER-CHECKLIST.md from Task 1; Task 2's selected option; src/preload/index.ts IN FULL — you must know exactly where the `contextBridge` block starts and ends and what else the file does; src/backend/updater.ts IN FULL and every importer of it (grep); package.json's full `scripts`, `dependencies` and `devDependencies` blocks; electron-builder.yml (skim — confirming nothing outside electron-builder consumes it); .github/workflows/build-base.yml and any other workflow invoking a script you are about to delete</read_first>
  <files>src/backend/main.ts, src/backend/updater.ts, src/preload/index.ts, electron.vite.config.ts, electron-builder.yml, package.json, e2e/, playwright.config.ts</files>
  <action>
One commit per group, so any single group can be reverted independently.

**Commit A — entry points.** `git rm src/backend/main.ts src/backend/updater.ts`. Before removing
`updater.ts`, grep its importers: if anything other than `main.ts` imports it, name that importer
and handle it rather than leaving a broken import.

**Commit B — the preload contextBridge.** In `src/preload/index.ts`, remove ONLY the
`contextBridge` block. Leave every `src/preload/api/*` module untouched and leave any other content
in `index.ts` that `tauriAttach` or the Tauri path relies on. Read the file's own header first —
it documents that Tauri never loads this bundle, which is what makes this cut small. If removing
the block leaves the file empty or with only unused imports, say so and delete the file, but only
after confirming nothing imports `src/preload/index` itself.

**Commit C — build config and scripts.** `git rm electron.vite.config.ts electron-builder.yml`.
From `package.json` remove the `electron-vite`, `electron-builder` and `electron-updater` entries
and every script that invokes them — grep the scripts block for `electron-vite`,
`electron-builder`, `electron-updater` and remove each match. Keep `electron` itself (plan 35-18).
Then grep `.github/workflows/` for every script name you removed and delete or repoint the steps
that call them, in the same commit — a workflow step calling a deleted script fails the run.

**Commit D — the e2e disposition**, per Task 2's selected option. Under option-a or option-c:
`git rm -r e2e playwright.config.ts`, remove `test:e2e` and `@playwright/test`, and write a deferred
item under `.planning/` recording what each of the five specs covered and that the coverage is now
unguarded. Under option-c also add the explanatory comment to `paths.ts:75`'s `CI === 'e2e'` clause.
Under option-b, stop and report — that option changes this plan's shape and the point-of-no-return
ordering.

**Then verify the Tauri path still works, before calling this done.** `pnpm exec vite build`
succeeds; `pnpm build:sidecar` succeeds; `pnpm tauri:dev` reaches a window; `pnpm codecheck` exits 0;
`pnpm test` passes. A failure here is the interval D-17 exists to make short — fix it now, in this
plan, rather than carrying a broken Tauri build into the 67-file wave.
  </action>
  <acceptance_criteria>
- `test ! -f src/backend/main.ts && test ! -f src/backend/updater.ts && test ! -f electron.vite.config.ts && test ! -f electron-builder.yml` exits 0.
- `ls src/preload/api/` lists the same files it listed before this plan — nothing under `api/` was deleted.
- `grep -c "contextBridge" src/preload/index.ts` prints `0`, or the file no longer exists and nothing imports `src/preload/index`.
- `node -e "const p=require('./package.json'); for(const k of ['electron-vite','electron-builder','electron-updater']) if(p.dependencies[k]||p.devDependencies[k]) throw new Error(k+' survives'); if(!p.devDependencies['electron']) throw new Error('electron devDep removed too early — that is plan 35-18'); for(const [k,v] of Object.entries(p.scripts)) if(/electron-vite|electron-builder|electron-updater/.test(v)) throw new Error('script '+k+' still calls a removed tool')"` exits 0.
- `grep -rn "electron-vite\|electron-builder\|test:e2e\|release:mac\|release:win\|release:linux\|dist:mac\|dist:win\|dist:linux" .github/workflows/` returns nothing, or every hit is a step that still resolves.
- `git log --oneline -4` shows four separate commits, A through D.
- `pnpm exec vite build`, `pnpm build:sidecar`, `pnpm codecheck` and `pnpm test` all succeed, and `pnpm tauri:dev` reached a window (recorded in the summary).
- `grep -c "alias:electron=" meta/esbuildWorkerBundleShared.ts` prints `1` — the alias survives.
- Under option-a/c, a deferred item exists under `.planning/` naming all five deleted specs.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vite build && pnpm build:sidecar && pnpm codecheck && pnpm test</automated>
  </verify>
  <done>Electron is gone as a runnable shell. Only Tauri builds, and it does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the tagged commit -> everything after it | the last recoverable both-shells state; the boundary this plan crosses |
| `src/preload/index.ts` contextBridge -> the renderer | the Electron-era privilege boundary being removed; `src/preload/api/*` remains, consumed by `tauriAttach` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-59 | Denial of Service | Deleting `src/backend/main.ts` while a sidecar module still imports from it, breaking the Tauri path at the one commit that cannot be cleanly rolled back | mitigate | `CENSUS-MAINTS-EDGES` is RE-RUN in Task 1 (not trusted from `35-PREFLIGHT.md`, since five plans landed since) and required empty; Task 1's checklist blocks the tag if anything is `MISSING`; Task 3 verifies the full Tauri build before the plan is done |
| T-35-60 | Repudiation | A `main.ts` behaviour with no successor becoming unrecoverable knowledge the moment the file is deleted | mitigate | `35-CUTOVER-CHECKLIST.md` enumerates every behaviour with a named successor and a `CONFIRMED` status verified against the successor rather than against the summary that claimed it. Any `MISSING` row stops the plan |
| T-35-61 | Elevation of Privilege | Over-deleting `src/preload/` and removing `api/*`, which `tauriAttach` consumes — the renderer's entire IPC surface | mitigate | Only the `contextBridge` block is removed; an acceptance criterion asserts `src/preload/api/`'s file list is unchanged; it lands in its own commit so the blast radius is one reviewable diff |
| T-35-62 | Tampering | Deleting `src/backend/updater.ts` while something other than `main.ts` imports it, leaving a broken import that surfaces at build time in the next wave | mitigate | Importers are grepped before deletion and any non-`main.ts` importer must be named and handled |
| T-35-63 | Denial of Service | A CI workflow step invoking a deleted `package.json` script, failing every run after this commit | mitigate | Workflow references are grepped and fixed in the SAME commit as the script removal, with an acceptance criterion covering it |
| T-35-64 | Repudiation | Deleting five e2e specs as an unremarked line in a deletion list, so the lost coverage is discovered later with no record of what it covered | mitigate | Task 2 is a blocking decision checkpoint, and options a and c both require a deferred item naming all five specs and what each covered |
| T-35-65 | Tampering | Removing the esbuild `--alias:electron=` or the `electron` devDependency here, before the 67-file rewrite removes the `require('electron')` calls the alias rescues — breaking the sidecar build in the same commit as the point of no return | mitigate | Both are explicit constraints with acceptance criteria asserting they survive; plan 35-18 owns them |
| T-35-SC | Tampering | npm/cargo installs | accept | This plan installs nothing; it only removes dependencies |
</threat_model>

<verification>
- `pre-electron-cutover` exists as an annotated tag on the remote, on the last both-shells-build commit.
- `35-CUTOVER-CHECKLIST.md` has zero `MISSING` rows and an empty `CENSUS-MAINTS-EDGES` result.
- Four separate commits, A-D.
- `pnpm exec vite build`, `pnpm build:sidecar`, `pnpm codecheck`, `pnpm test` all pass; `pnpm tauri:dev` reaches a window.
- `src/preload/api/*` intact; the esbuild alias and the `electron` devDependency both survive.
- The e2e disposition is recorded per Task 2's decision.
</verification>

<success_criteria>
The project has exactly one shell. The diff that made it irreversible is small enough to read in
full, the state before it is recoverable by tag, and the Tauri build works at the commit that
crossed the line.
</success_criteria>

<output>
Create `.planning/phases/35-electron-cutover-remove-the-electron-build/35-14-SUMMARY.md` when done.
</output>
