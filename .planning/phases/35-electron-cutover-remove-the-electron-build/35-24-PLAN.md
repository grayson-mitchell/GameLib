---
phase: 35-electron-cutover-remove-the-electron-build
plan: 24
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - meta/i18nForkTouchedFiles.json
  - meta/__tests__/genI18nGateScope.test.ts
autonomous: true
requirements: [REQ-35-20]

must_haves:
  truths:
    - "`pnpm test --selectProjects Meta -- genI18nGateScope` is green, with no test skipped that was running before"
    - "The A-17 anti-rot check passes because the committed artifact matches the live git derivation, not because the check was relaxed"
    - "The A-03 ratchet still fails by name when a NEW fork-touched file appears"
  artifacts:
    - path: "meta/i18nForkTouchedFiles.json"
      provides: "The committed fork-touched artifact, regenerated against the current merge-base"
    - path: "meta/__tests__/genI18nGateScope.test.ts"
      provides: "Guard fixture counts and the declared unscanned-debt set, re-baselined in the same commit as the artifact"
  key_links:
    - from: "meta/__tests__/genI18nGateScope.test.ts A-17 ANTI-ROT"
      to: "meta/i18nForkTouchedFiles.json"
      via: "deep equality against the live `git diff` derivation"
      pattern: "forkTouchedSnapshot"
    - from: "meta/__tests__/genI18nGateScope.test.ts A-03 RATCHET"
      to: "DECLARED_UNSCANNED_DEBT"
      via: "the set of fork-touched files absent from the hand-curated scope must equal the declared debt exactly"
      pattern: "DECLARED_UNSCANNED_DEBT"
---

<objective>
Close verification gap 4: `meta/__tests__/genI18nGateScope.test.ts`'s `A-17 ANTI-ROT` spec fails
because the committed `meta/i18nForkTouchedFiles.json` is stale by six fork-divergent files that
Phase 35 created. This is a real Phase 35 regression and no later phase owns it.

Purpose: `35-VERIFICATION.md` truth "the repo's own test suite is green — no Phase 35 regression in
mechanized gates" is currently FAILED on exactly this one spec.

Output: one coordinated commit that regenerates the artifact AND re-baselines everything that
hard-codes its shape, leaving the suite green with no check weakened.

**This gap has a known-bad "sanctioned" fix.** Running `pnpm gen-i18n-gate-scope` on its own was
already attempted and cascaded the suite from 1 failure to 5, because the `--rewrite-scope` guard
fixtures hard-code the counts `163 -> 199` and the `A-03` ratchet declares an exact debt set. A task
that just runs the regen script is wrong.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md

<interfaces>
<!-- Measured on this branch 2026-08-30. Treat the numbers as a starting point and RE-MEASURE; a
     concurrent session can move HEAD, and this snapshot can go stale under you. -->

Current state:
  meta/i18nGateScope.json         163 files, `generatedBy` marks it HAND-EDITED (not a generator run)
  meta/i18nForkTouchedFiles.json  199 files, written by `pnpm gen-i18n-gate-scope`
  DECLARED_UNSCANNED_DEBT         the 36 fork-touched files absent from the hand-curated scope

The single failing spec:
  genI18nGateScope > staleness guard -- the reverse direction (REQ-34.10-14)
    > with a real git diff against the upstream merge-base
      > A-17 ANTI-ROT: the committed meta/i18nForkTouchedFiles.json equals the LIVE git derivation
  at meta/__tests__/genI18nGateScope.test.ts:431
    expect([...forkTouchedSnapshot.files].sort()).toEqual([...freshSnapshotFiles()].sort())

The SIX files the live derivation produces and the committed artifact lacks (jest diff, `-` side):
  src/frontend/components/UI/PathSelectionBox/index.tsx
  src/frontend/components/UI/WebviewControls/index.tsx
  src/frontend/screens/DownloadManager/index.tsx
  src/frontend/screens/Library/components/CategoriesManager/index.tsx
  src/frontend/screens/Library/components/InstallModal/defaultPlatform.ts
  src/frontend/screens/Settings/components/UseDarkTrayIcon.tsx
All six exist on disk. NONE of the six is in the hand-curated `meta/i18nGateScope.json`, so all six
land in `DECLARED_UNSCANNED_DEBT`, not in scope.

Everything that hard-codes the count, and cascades when the artifact is regenerated:
  test:589  it('A0 fixture sanity: ... the REAL 163-file hand-curated snapshot and the fresh snapshot is the REAL 199')
  test:590  expect(scopeSnapshot.files.length).toBe(163)          <- stays 163
  test:591  expect(forkTouchedSnapshot.files.length).toBe(199)    <- moves
  test:592  expect(freshSnapshot().files.length).toBe(199)        <- moves
  test:618  it('A2 REFUSAL NAMES WHAT IT WOULD HAVE DONE: ... refuses with the real 163 -> 199 diff ...')
  test:641  it('A3 NON-VACUITY / POSITIVE CONTROL: ... DOES rewrite it to 199')
  test:654  expect(rewritten.files.length).toBe(199)              <- moves
  test:659  it('A4 BOOTSTRAP: ... creates it with 199 files')
  test:672  expect(JSON.parse(...).files.length).toBe(199)        <- moves
  Plus the `freshSnapshot()` docstring around test:562, which still describes a stale "185 files /
  162 -> 185" delta from an earlier phase and is already wrong.

`freshSnapshot()` in the guard fixtures is built FROM `forkTouchedSnapshot.files`, so regenerating
the artifact moves both sides of those assertions together. That is why they cascade as a set.

The A-03 ratchet:
  test:165  it('A-03 RATCHET: the set of unscanned fork-touched files equals the DECLARED debt exactly')
  test:166  expect(unscanned.sort()).toEqual([...DECLARED_UNSCANNED_DEBT].sort())
  DECLARED_UNSCANNED_DEBT is declared at test:90.

Non-vacuity specs that MUST still fail against a mutation after your change:
  test:169  A-03 RATCHET non-vacuity (a file drifting OUT of scope)
  test:186  A-17 non-vacuity (a NEW fork-touched file appearing in the artifact)
  test:436  A-17 ANTI-ROT non-vacuity (a mutated copy of the committed artifact)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the drift before changing anything</name>
  <files>(none — measurement only; no file is modified by this task)</files>

  <read_first>
    - `meta/__tests__/genI18nGateScope.test.ts` in full. It is ~720 lines and every section matters:
      the `DECLARED_UNSCANNED_DEBT` array at :90, the `A-17 CI-READABLE RATCHET` describe at :148,
      the staleness-guard describe containing the failing spec at ~:420, and the `--rewrite-scope
      guard` describe at :521.
    - `meta/genI18nGateScope.ts` — how the derivation is produced, what `--rewrite-scope` does, and
      the provenance marker that distinguishes a hand-curated scope file from a generated one.
    - `meta/i18nGateScope.json`'s `generatedBy` field — it reads "hand-edited ... NOT a
      `pnpm gen-i18n-gate-scope` run". The A5 provenance ratchet asserts this and it must stay true.
    - The comment block around test:500-520 explaining why the fork-touched artifact exists as a
      CI-readable input at all (the ratchet used to be `describe.skip` in CI because
      `actions/checkout@v6` clones at depth 1 with no Heroic remote).
  </read_first>

  <action>
    Re-derive the current numbers yourself rather than trusting the snapshot in this plan's
    `<interfaces>` block — HEAD may have moved.

    Run the suite and capture: the exact set of files present in the live derivation and absent from
    the committed artifact, the exact set present in the artifact and absent from the derivation (if
    any — this direction matters too and the plan's snapshot shows none, so a non-empty result here
    is new information), the current `meta/i18nGateScope.json` file count, and the current
    `meta/i18nForkTouchedFiles.json` file count.

    For EACH drifted file, determine and record: whether it is in `meta/i18nGateScope.json` (so it
    belongs in scope) or not (so it belongs in `DECLARED_UNSCANNED_DEBT`), and one line on why Phase
    35 made it fork-divergent.

    Adding a file to `DECLARED_UNSCANNED_DEBT` is a ratchet LOOSENING — it declares the file as
    accepted, unscanned i18n debt. Adding one to `meta/i18nGateScope.json` is a deliberate widening
    the file's own comment says has already cost this project twice by accident. Do NOT widen the
    hand-curated scope in this plan. Record, per file, that it is being recorded as debt and that
    promoting it to scope is separate, deliberate work.

    Produce no source edits in this task. Its output is the measurement the next task acts on.
  </action>

  <acceptance_criteria>
    - The SUMMARY (or a scratch note carried into Task 2) records, as literal lists:
      the artifact-missing set, the derivation-missing set, and the two file counts, each with the
      command that produced it.
    - Every drifted file is classified `SCOPE` or `DEBT` against `meta/i18nGateScope.json`, with a
      one-line Phase 35 provenance note each.
    - The classification is derived by reading `meta/i18nGateScope.json`, not by assuming this plan's
      snapshot is still accurate.
    - `git status --short` shows no modified file — this task measures only.
  </acceptance_criteria>

  <verify>
    <automated>npx jest --config meta/jest.config.js meta/__tests__/genI18nGateScope.test.ts 2>&amp;1 | tail -40</automated>
  </verify>

  <done>
    The exact drift is measured in both directions, every drifted file is classified, and nothing has
    been edited.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the artifact and re-baseline every dependent count and the declared debt, in ONE change</name>
  <files>meta/i18nForkTouchedFiles.json, meta/__tests__/genI18nGateScope.test.ts</files>

  <read_first>
    - Task 1's measurement output.
    - `meta/__tests__/genI18nGateScope.test.ts` lines 85-200 (the debt array and the A-03/A-17
      ratchets) and lines 521-720 (the `--rewrite-scope` guard, including `seedScope()`,
      `freshSnapshot()` and every `toBe(199)` / `toBe(163)`).
    - `package.json` scripts `gen-i18n-gate-scope` and `gen-i18n-scope:rewrite` — you want the FORMER.
      `--rewrite-scope` would rewrite the hand-curated scope file and is exactly what the A2 refusal
      spec exists to prevent.
  </read_first>

  <action>
    In one coordinated change:

    1. Run `pnpm gen-i18n-gate-scope` (NOT `gen-i18n-scope:rewrite`) so
       `meta/i18nForkTouchedFiles.json` is rewritten from the live derivation and
       `meta/i18nGateScope.json` is left byte-identical.
    2. Confirm `meta/i18nGateScope.json` is genuinely unchanged (`git diff --stat` shows no entry for
       it) and that its `generatedBy` still reads as hand-curated. If the script touched it, stop —
       that is a different defect and the A5 provenance ratchet is the thing that just caught it.
    3. Add each Task-1 `DEBT` file to `DECLARED_UNSCANNED_DEBT`, keeping the array's existing sort
       order, each with a trailing comment naming why it became fork-divergent in Phase 35.
    4. Update every hard-coded count in the `--rewrite-scope guard` describe to the new derivation
       size: the `toBe(...)` assertions at test:591, :592, :654 and :672, and the three `it(...)`
       titles at :589, :618 and :659 that state the numbers in prose. A title that names a stale
       number is a lie a reader will trust.
    5. Correct the `freshSnapshot()` docstring around test:562 — it currently describes a
       "185 files / 162 -> 185" delta from an earlier phase and is already wrong independently of
       this change.
    6. Leave `expect(scopeSnapshot.files.length).toBe(163)` alone unless Task 1 measured otherwise:
       the hand-curated scope is not being widened.

    Commit all of it together. A commit that regenerates the artifact without the fixture and debt
    updates leaves the suite at 5 failures, which is what happened the last time this was attempted.
  </action>

  <acceptance_criteria>
    - `npx jest --config meta/jest.config.js meta/__tests__/genI18nGateScope.test.ts` reports 0
      failures. Record the full `Tests:` line in the SUMMARY.
    - The skipped-test count is unchanged from the pre-fix run (1 skipped). A previously-running spec
      that becomes skipped is a regression wearing a green badge — compare the two runs explicitly and
      state both counts.
    - `git diff --stat meta/i18nGateScope.json` produces no output — the hand-curated scope is
      untouched.
    - Non-vacuity still holds. Prove each by mutation and record the failing output:
      (a) A-17 ANTI-ROT: delete one entry from the regenerated `meta/i18nForkTouchedFiles.json`, run
          the suite, confirm the spec fails naming that file, restore the file byte-identically.
      (b) A-03 RATCHET: remove one entry from `DECLARED_UNSCANNED_DEBT`, confirm the ratchet fails
          naming it, restore.
      (c) A0 fixture sanity: revert one `toBe(<new count>)` to the old count, confirm it fails,
          restore.
      Restore by `cp` from a copy plus a `shasum` byte-identity check — NOT by
      `git checkout -- <file>`, which fires this repo's `post-checkout` hook and makes the helper
      download throw.
    - The number of entries added to `DECLARED_UNSCANNED_DEBT` equals the number of `DEBT`-classified
      files from Task 1, and `meta/i18nForkTouchedFiles.json`'s new length minus
      `meta/i18nGateScope.json`'s length equals the new `DECLARED_UNSCANNED_DEBT` length. State that
      arithmetic in the SUMMARY as a self-check.
    - `pnpm test --selectProjects Meta` passes as a whole — regenerating an artifact breaks the pins
      that guard it, and this file is not necessarily the only pin.
  </acceptance_criteria>

  <verify>
    <automated>pnpm test --selectProjects Meta</automated>
  </verify>

  <done>
    The suite is green on `genI18nGateScope`, the hand-curated scope is unchanged, all three
    non-vacuity controls still fail against a mutation, and the debt arithmetic balances.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo history -> committed artifact | A CI-readable snapshot of a `git diff` that CI itself cannot recompute (depth-1 clone, no Heroic remote) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-116 | Tampering | `DECLARED_UNSCANNED_DEBT` widening | mitigate | Each added entry carries a named reason, the added count is reconciled against the artifact-minus-scope arithmetic, and the A-03 non-vacuity control is re-proven by mutation so the ratchet still names a new drift. |
| T-35-117 | Repudiation | a green run that skipped the spec | mitigate | The skipped-test count is compared before and after; the A-17 ratchet was already once silently `describe.skip` in CI, which is why the committed artifact exists at all. |
| T-35-118 | Tampering | accidental rewrite of the hand-curated `i18nGateScope.json` | mitigate | `gen-i18n-gate-scope` is used, never `gen-i18n-scope:rewrite`; `git diff --stat` on that file must be empty and the A5 provenance ratchet must still read it as hand-curated. |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. |
</threat_model>

<verification>
- `pnpm test --selectProjects Meta` passes (capital `M` — `meta` matches nothing and exits 0).
- The three mutation controls each recorded with their failing output.
- Skipped-count before and after both stated.
- `pnpm codecheck` exits 0.

Do NOT gate on `pnpm lint` exiting 0 or on a green `.husky/pre-push`; both are red repo-wide and
`pnpm lint` is Phase 39's job. Pushes here use `--no-verify`.
</verification>

<success_criteria>
- `A-17 ANTI-ROT` passes because the artifact matches the derivation, not because a check was
  relaxed or skipped.
- The `--rewrite-scope` guard fixtures and the A-03 declared debt were updated in the SAME commit as
  the artifact.
- The hand-curated `meta/i18nGateScope.json` is byte-identical to its pre-plan state.
</success_criteria>

<output>
Create `.planning/phases/35-electron-cutover-remove-the-electron-build/35-24-SUMMARY.md` when done.
Record: the drift measurement in both directions; the per-file DEBT classification with reasons; the
old and new counts; the debt arithmetic self-check; the before/after skipped counts; and the failing
output of all three mutation controls.
</output>
