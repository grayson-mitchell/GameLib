---
phase: 35-electron-cutover-remove-the-electron-build
plan: 10
type: execute
wave: 7
depends_on:
  - 35-02
  - 35-07
files_modified:
  - src-tauri/src/main.rs
  - src/backend/sidecar/wineToolsFlowRegistration.ts
  - src/backend/storeManagers/legendary/library.ts
  - src/backend/sidecar/libraryFlowRegistration.ts
  - src/backend/sidecar/__tests__/installedJsonWatcher.test.ts
  - src/backend/sidecar/__tests__/winetricksInstallChannel.test.ts
autonomous: true
requirements:
  - REQ-35-16
decisions: [D-18, D-05]

must_haves:
  truths:
    - "The `installed.json` watcher is ported. `src/backend/main.ts:1038-1045` runs a debounced `watch(legendaryInstalled, ...)` that refreshes the Legendary library; the sidecar has no equivalent, which is why `legendary sync-saves` computes a save path against a stale view and leaves the save-path field empty. `main.ts` dies at the point of no return, so this is a release blocker, not a nicety"
    - "The debounce survives the port. `watch` fires twice while Legendary is still writing chunks, and the original code's own comment says so — a port that drops the debounce makes `LegendaryLibrary` fail on a partial file, which is a different and worse bug"
    - "`winetricksInstall` stops being a live silent no-op. It is registered on both ends — frontend `SearchBar`/`Winetricks` components and backend `wineToolsFlowRegistration.ts` — and produces literally nothing when invoked: zero log lines in EITHER sink"
    - "Both fixes are informed by `35-AB-RETEST.md`'s rows for these items. If a defect reproduces under BOTH shells it is shared-logic; if TAURI-ONLY it is a port defect. Those need different fixes and the A/B record is the only place that distinction still exists"
    - "Each fix is proven by an assertion that would have caught the original silent failure — a green test that passes against the broken code proves nothing here, because the failure mode of both defects is silence, not an error"
  artifacts:
    - path: "src/backend/sidecar/__tests__/installedJsonWatcher.test.ts"
      provides: "proof the sidecar refreshes the library on an installed.json write, with the debounce intact"
      contains: "debounce"
    - path: "src/backend/sidecar/__tests__/winetricksInstallChannel.test.ts"
      provides: "proof the send channel reaches a handler, RED-proven against the pre-fix wiring"
  key_links:
    - from: "installed.json on disk"
      to: "the sidecar's Legendary library refresh"
      via: "a debounced fs watch ported from main.ts:1038-1045"
      pattern: "watch"
    - from: "the frontend winetricksInstall send"
      to: "wineToolsFlowRegistration.ts's handler"
      via: "the sidecar send-channel dispatch path"
      pattern: "winetricksInstall"
---

<objective>
Close the two folded Tauri channel dead-ends that fail invisibly: the `installed.json` watcher that
was never ported, and the `winetricksInstall` send channel that does nothing.

Purpose: both are survivable today only because the Electron build still ships alongside. After the
cutover there is no fallback, so each becomes a release blocker. Both share a failure shape this
repo has recorded repeatedly — the user acts, nothing happens, and nothing is logged. Absence of an
error is not evidence of success.

Output: a ported debounced watcher, a `winetricksInstall` channel that reaches its handler, and two
tests that each fail against the pre-fix code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-CONTEXT.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-RESEARCH.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md

<interfaces>
THE WATCHER — `src/backend/main.ts:1027-1050`. Verified shape at plan time:
    import { existsSync, watch } from 'graceful-fs'      // :27
    let watchTimeout: NodeJS.Timeout | undefined          // :1038
    watch(legendaryInstalled, () => {
      logInfo('installed.json updated, refreshing library', LogPrefix.Legendary)
      // `watch` might fire twice (while Legendary/we are still writing chunks of the file),
      // which would in turn make LegendaryLibrary fail to ...
      if (watchTimeout) clearTimeout(watchTimeout)
      watchTimeout = setTimeout(...)
    })
Read the full block including the `setTimeout` body and delay before porting — the delay value and
the callback are both load-bearing and neither is reproduced here.
Todo files (TWO — DEDUPE into one fix and cite both):
  `.planning/todos/2026-08-24-installed-json-watcher-never-ported-to-the-tauri-sidecar.md`
  `.planning/todos/2026-08-25-installed-json-watcher-not-ported-to-tauri.md`
Surfaced symptom: `legendary sync-saves` computing a save path from an empty/stale `installed.json`
view, leaving the save-path field empty.

THE SEND CHANNEL — `.planning/todos/2026-08-24-winetricksinstall-send-channel-is-a-live-silent-no-op.md`.
Registered on both ends: frontend `SearchBar`/`Winetricks` components and
`src/backend/sidecar/wineToolsFlowRegistration.ts`. Live-reproduced: click Install, zero log lines
in either sink. This matches the standing gotcha `sidecar-send-channels-fail-silently` — the same
root-cause class has already been fixed elsewhere in this codebase, so **find that prior fix and
apply the same pattern rather than inventing one**. Grep for other `send`-kind registrations that
demonstrably work and compare the wiring.

THE TWO SINKS partition and are not interchangeable: `~/Library/Logs/GameLib/gamelib.log` and the
terminal `[shell]` output. An "it logs nothing" claim must name which sink was read.

`35-AB-RETEST.md` rows: item 2 (`installed.json` watcher) and item 4 (`winetricksInstall`). Read
both verdicts before choosing an approach — `BOTH` means shared logic, `TAURI-ONLY` means a port
defect, and they call for different fixes.

jest: `--selectProjects Backend` (capital B). Rust: `cargo test --manifest-path src-tauri/Cargo.toml`.
</interfaces>
</context>

<constraints>
- **Do not modify `src/backend/main.ts`.** It is the point-of-no-return plan's to delete. Port FROM
  it by reading; leave it untouched so the Electron build survives until plan 35-14.
- **Do not drop the watcher's debounce.** The original comment records why it exists: `watch` fires
  mid-write and an un-debounced refresh reads a partial file.
- **Do not close either todo on a green test alone.** Both defects are silent failures; a test
  written after the fix that passes against the broken code proves nothing. Each test must be
  RED-proven — run it against the pre-fix code (stash the fix in a scratch copy, or assert against
  a deliberately-unwired handler) and record that it failed.
- **If the `winetricksInstall` root cause turns out to be in the Rust send-dispatch path**, fix it
  in `main.rs` here rather than deferring — this plan's wave already owns that file. But state
  plainly whether the defect was in registration, dispatch, or the frontend emit.
- Never `git stash`, never `git reset`, never `git checkout -- <path>` — use `cp` + `shasum -a 256`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Port the debounced installed.json watcher to the sidecar</name>
  <read_first>src/backend/main.ts:1020-1060 IN FULL — the watch registration, the debounce, the setTimeout body and its delay, and where `legendaryInstalled` comes from; src/backend/storeManagers/legendary/library.ts for the refresh function the timeout calls and what it assumes about file completeness; src/backend/sidecar/libraryFlowRegistration.ts for how the sidecar currently registers library lifecycle work and where a watcher would naturally live; src/backend/constants/paths.ts for `legendaryInstalled`'s path constant; the two watcher todo files; `35-AB-RETEST.md` item 2</read_first>
  <files>src/backend/storeManagers/legendary/library.ts, src/backend/sidecar/libraryFlowRegistration.ts, src/backend/sidecar/__tests__/installedJsonWatcher.test.ts</files>
  <action>
Port the watcher into the sidecar's library lifecycle. Decide deliberately WHERE it is installed —
it must start when the sidecar is ready and the Legendary library is initialised, not at module
load, because `legendaryInstalled` may not exist yet on a fresh profile (`main.ts` guards with
`existsSync`; carry that guard).

Carry across, unchanged in substance: the `existsSync` guard, the `watch` registration, the
`clearTimeout`/`setTimeout` debounce with the SAME delay value the original uses, the
`logInfo('installed.json updated, refreshing library', LogPrefix.Legendary)` line, and the refresh
call in the timeout body. Carry the original comment explaining the double-fire, since it is the
reason the debounce exists.

Add teardown: the watcher must be closable on sidecar shutdown. `main.ts` never needed this because
the process exited; a sidecar that re-initialises the library would otherwise accumulate watchers,
each firing a refresh, turning one write into N refreshes.

Write `src/backend/sidecar/__tests__/installedJsonWatcher.test.ts` asserting:
  - writing to a temp `installed.json` triggers exactly ONE library refresh, not two, when two
    writes land inside the debounce window — this is the debounce assertion and it is the one that
    matters;
  - two writes SEPARATED by more than the debounce window trigger two refreshes, so the debounce is
    proven to be a debounce and not an accidental once-only guard;
  - no refresh fires when the file does not exist (the `existsSync` guard);
  - teardown stops the watcher: after close, a write triggers no refresh.
**RED-prove it:** run the test against the pre-fix sidecar (no watcher installed) and confirm it
fails, then against the fix and confirm it passes. Record both runs. A test authored after the fix
that was never seen red is not evidence.
  </action>
  <acceptance_criteria>
- `grep -rc "installed.json updated, refreshing library" src/backend/ | grep -v ':0'` lists both `main.ts` (untouched) and the new sidecar site.
- `src/backend/main.ts` is unmodified: `git diff --name-only` does not list it.
- The sidecar watcher uses the SAME debounce delay as `main.ts:1044`'s `setTimeout` — state both values in the summary and show they match.
- The new test file contains a case asserting ONE refresh for two writes inside the window and a case asserting TWO refreshes for two writes outside it.
- The summary records the RED run: the test failed against the pre-fix code, with the failure output quoted.
- `pnpm test --selectProjects Backend -- installedJsonWatcher` passes.
- `pnpm codecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --selectProjects Backend -- installedJsonWatcher</automated>
  </verify>
  <done>An `installed.json` write refreshes the sidecar's Legendary library, once per burst, and stops doing so after teardown.</done>
</task>

<task type="auto">
  <name>Task 2: Find and fix the winetricksInstall send-channel dead end</name>
  <read_first>The winetricksInstall todo file IN FULL; src/backend/sidecar/wineToolsFlowRegistration.ts IN FULL; the frontend `Winetricks`/`SearchBar` components that emit the channel (grep `winetricksInstall` under `src/frontend/`); at least TWO other `send`-kind channel registrations in `src/backend/sidecar/*FlowRegistration.ts` that are known to WORK, so you can diff the working wiring against the broken one; src-tauri/src/main.rs's send-dispatch path (grep `sidecar_send`); `35-AB-RETEST.md` item 4</read_first>
  <files>src/backend/sidecar/wineToolsFlowRegistration.ts, src-tauri/src/main.rs, src/backend/sidecar/__tests__/winetricksInstallChannel.test.ts</files>
  <action>
Diagnose before fixing. The standing gotcha `sidecar-send-channels-fail-silently` says this
root-cause class has been fixed before in this codebase — find that prior fix by comparing this
channel's registration against two send channels that demonstrably work, and state the difference
you found. The three candidate locations, in order of likelihood, are: (a) the sidecar-side
registration (registered on the wrong registry, wrong name, or after the dispatch table was frozen),
(b) the Rust `sidecar_send` dispatch (channel absent from a routing list), (c) the frontend emit
(wrong channel name or wrong `send` vs `invoke` kind).

**Name which of the three it was in the summary.** "Fixed it" without naming the layer is how the
next instance of this class gets rediscovered from scratch.

Apply the same fix pattern the prior instance used. Do not invent a new mechanism, and do not
convert the channel from `send` to `invoke` as a workaround unless the diagnosis shows `send` is
genuinely wrong for it — that would hide the class rather than fix it.

Write `src/backend/sidecar/__tests__/winetricksInstallChannel.test.ts` asserting:
  - emitting `winetricksInstall` reaches the registered handler with the expected arguments;
  - the handler emits at least one log line, and the test asserts on it — the original defect was
    ZERO log lines in either sink, so a silent success would be indistinguishable from the bug;
  - a channel name that is NOT registered does not reach the handler (the negative control, so the
    test is not vacuously passing on any input).
**RED-prove it** against the pre-fix wiring and record the failure output.

If the fix lands in `main.rs`, keep it in its own commit separate from the sidecar-side change so
either half can be reverted independently.
  </action>
  <acceptance_criteria>
- The summary names the layer the defect was in: exactly one of `sidecar registration`, `rust dispatch`, `frontend emit`, and describes the difference against a working send channel.
- `pnpm test --selectProjects Backend -- winetricksInstallChannel` passes, and the test file contains a negative-control case for an unregistered channel name.
- The summary records the RED run against the pre-fix wiring, with the failure output quoted.
- The channel is still a `send`-kind channel, or the summary justifies why it had to change kind.
- If `src-tauri/src/main.rs` changed, `git log --oneline -2` shows it as a separate commit, and `cargo test --manifest-path src-tauri/Cargo.toml` passes.
- `pnpm codecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test --selectProjects Backend -- winetricksInstallChannel</automated>
  </verify>
  <done>Clicking Install in Winetricks reaches a handler that logs, and the fix is attributed to a named layer so the next instance of this class is diagnosable rather than rediscovered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `installed.json` on disk -> the sidecar's library model | a file written by an external process (`legendary`) drives an in-process refresh |
| frontend `send` emit -> sidecar handler -> `winetricks` subprocess | user-selected verb names reach a shell-invoked tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-41 | Tampering | Refreshing the library from a PARTIALLY-WRITTEN `installed.json` — `watch` fires mid-write and `LegendaryLibrary` parses a truncated file, corrupting the in-memory model | mitigate | The debounce is ported with the same delay and is asserted two ways: one refresh for two writes inside the window, two for two writes outside it. The original comment explaining the double-fire is carried across so a later reader does not remove it |
| T-35-42 | Denial of Service | Accumulating watchers across library re-initialisations, so one file write triggers N refreshes and N `legendary` invocations | mitigate | Teardown added and asserted by a test that writes after close and expects no refresh — a case `main.ts` never needed because the process exited |
| T-35-43 | Repudiation | Both defects fail SILENTLY. A fix verified only by "no error appeared" is indistinguishable from the bug | mitigate | Both tests are RED-proven against the pre-fix code with the failure output recorded, and the `winetricksInstall` test asserts on a log line rather than on absence of a throw |
| T-35-44 | Elevation of Privilege | `winetricksInstall` reaching a subprocess with a user-influenced verb name once the channel actually works — a dead channel has no injection surface, a live one does | mitigate | Confirm during the fix that the handler's existing argument validation is intact and that the verb reaches `winetricks` as an argv element rather than through a shell string. If the handler had no validation because it was never reachable, add it and say so — a newly-live channel is a newly-live input path |
| T-35-SC | Tampering | npm/cargo installs | accept | This plan adds no dependency. `graceful-fs` is already a project dependency used by `main.ts`'s watcher |
</threat_model>

<verification>
- `pnpm test --selectProjects Backend -- installedJsonWatcher winetricksInstallChannel` passes.
- Both RED runs recorded with quoted failure output.
- `src/backend/main.ts` unmodified.
- `pnpm codecheck` exits 0; `cargo test --manifest-path src-tauri/Cargo.toml` passes if `main.rs` changed.
- The `winetricksInstall` defect is attributed to a named layer.
</verification>

<success_criteria>
Two invisible-failure channels become visible-success channels, each guarded by an assertion that
was seen red against the broken code. `legendary sync-saves` computes a real save path, and clicking
Install in Winetricks does something.
</success_criteria>

<output>
Create `.planning/phases/35-electron-cutover-remove-the-electron-build/35-10-SUMMARY.md` when done.
</output>
