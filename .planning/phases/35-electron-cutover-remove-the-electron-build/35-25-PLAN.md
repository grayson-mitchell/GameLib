---
phase: 35-electron-cutover-remove-the-electron-build
plan: 25
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/frontend/components/UI/SearchBar/index.tsx
  - src/frontend/components/UI/SearchBar/index.scss
  - src/frontend/components/UI/SearchBar/__tests__/suggestionFocusRace.test.tsx
  - src/frontend/components/UI/Winetricks/WinetricksSearch/index.tsx
  - src/frontend/components/UI/Winetricks/index.tsx
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [REQ-35-16]

must_haves:
  truths:
    - "Clicking the Install button on a winetricks suggestion with the MOUSE fires its `onClick` and sends `winetricksInstall`"
    - "REQ-35-16's winetricks clause names the real cause and is satisfiable against it"
  artifacts:
    - path: "src/frontend/components/UI/SearchBar/index.tsx"
      provides: "A suggestions list that survives from pointerdown to click"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-35-16 amended so its winetricks clause is satisfiable"
  key_links:
    - from: "src/frontend/components/UI/Winetricks/WinetricksSearch/index.tsx"
      to: "src/frontend/components/UI/Winetricks/index.tsx `install()`"
      via: "the suggestion button's `onClick` -> `onInstallClicked`"
      pattern: "onInstallClicked"
    - from: "src/frontend/components/UI/Winetricks/index.tsx `install()`"
      to: "the `winetricksInstall` send channel"
      via: "`window.api.winetricksInstall(runner, appName, component)`"
      pattern: "winetricksInstall"
---

<objective>
Close verification gap 2, `REQ-35-16`'s winetricks clause.

**The requirement as written is unsatisfiable, and that is an operator-confirmed finding, not an
excuse.** `REQ-35-16` demands the `winetricksInstall` fix be "attributed to a named layer (sidecar
registration, Rust dispatch, or frontend emit)". All three layers were re-measured correct.
`wineToolsFlowRegistration.ts:335` registers the channel correctly and the channel works end to end
under KEYBOARD activation. The real break is renderer hit-testing: `pointerdown`/`mousedown` arrive
at the Install button, `mouseup`/`click` never do, attributed to a React unmount between the two.

The operator chose a real fix over an accepted-gap record, and requires the requirement text to be
amended so it is satisfiable against the real cause.

**A prior theory about this was already ADOPTED and then DISPROVEN.** Plan 34.6-16 shipped an
`onMouseDown={(e) => e.preventDefault()}` guard on the suggestions `<ul>` against a `:focus-within`
unmount mechanism; plan 34.6-17's live re-drive shipped that guard, verified it in the running
bundle, and the button was STILL dead to the mouse. That guard is still in the file with its causal
claim explicitly retracted. Do not re-derive it as the answer.

Purpose: `REQ-35-16` is one of three folded channel dead ends, all of which fail INVISIBLY. Two are
closed. This is the third.

Output: the identified and fixed renderer unmount, plus an amended `REQ-35-16`.

This plan is `autonomous: false`: task 3 is a blocking human gesture, and the requirement amendment
must not land without it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md

<interfaces>
<!-- Extracted from the codebase 2026-08-30. -->

The click chain, three components deep:

src/frontend/components/UI/Winetricks/index.tsx:~86
  const WINETRICKS_DECLINED_GUARD = declined
  function install(component: string) {
    if (WINETRICKS_DECLINED_GUARD) return
    window.api.winetricksInstall(runner, appName, component)   // SEND-kind, returns no promise
  }
  Its comment records why the guard exists: a mis-routed send produces no reject, no timeout and no
  log line, so the only honest treatment is to gate it behind the invoke-kind probes' decline.

src/frontend/components/UI/Winetricks/WinetricksSearch/index.tsx
  const install = (component: string) => { setSearch(''); onInstallClicked(component) }
  suggestions = searchResults.map(c => (
    <li key={c}><span>{c}</span><button className="button" onClick={() => install(c)}>Install</button></li>
  ))
  <SearchBar suggestionsListItems={suggestions} onInputChanged={setSearch} value={search} ... />
  NOTE: `onInputChanged` is a NEW function identity on every render.

src/frontend/components/UI/SearchBar/index.tsx
  {value.length > 0 && (<> <ul className="autoComplete" onMouseDown={e => e.preventDefault()}> ... )}
  useEffect(..., [input, value, onInputChanged])   // re-runs whenever onInputChanged's identity changes
  The long comment above the `<ul>` carries the RETRACTED causal claim (34.6-REVIEW WR-03).

src/frontend/components/UI/SearchBar/index.scss
  .autoComplete { display: none }
  .SearchBar:focus-within ul.autoComplete { display: block }
  ul.autoComplete:empty { display: none }

Test environment: jsdom / react-test-renderer are NOT installed (`src/frontend/jest.config.js` says
so in its own header). Tests invoke the function component directly and inspect the returned
React-element object graph. `src/frontend/components/UI/SearchBar/__tests__/suggestionFocusRace.test.tsx`
is the existing example and it mocks `react`'s `useRef`/`useEffect`/`useCallback`.

CONSEQUENCE, and this is binding: **no jest test in this repo can prove a real pointer sequence.**
Whatever you conclude, the proof of the fix is the live gesture in Task 3, and the unit test is a
regression pin against the specific shape you identify — not the closure signal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify the unmount — measure it, do not infer it</name>
  <files>(none — measurement only; no file is modified by this task)</files>

  <read_first>
    - `src/frontend/components/UI/SearchBar/index.tsx` in full, especially the long comment above the
      `<ul>` including its **⚠ CAUSAL CLAIM RETRACTED** paragraph. That paragraph is the record of a
      theory that was adopted, shipped, live-tested and disproven. Read it before forming a new one.
    - `src/frontend/components/UI/SearchBar/index.scss` in full — the `display: none` /
      `:focus-within` / `:empty` rules that decide whether the list is in the layout at all.
    - `src/frontend/components/UI/Winetricks/WinetricksSearch/index.tsx` in full — note that the
      `install` handler calls `setSearch('')` BEFORE `onInstallClicked`, and that `onInputChanged` is
      re-created on every render.
    - `src/frontend/components/UI/Winetricks/index.tsx` in full — the `declined` state, the two
      `useEffect` probes that set it, and `install()`'s early return. A `declined` that flips true
      would make the button silently do nothing WITHOUT any unmount, which is a competing hypothesis
      you must rule in or out rather than assume away.
    - `src/frontend/components/UI/SearchBar/__tests__/suggestionFocusRace.test.tsx` in full,
      including its closing paragraph stating exactly what it cannot prove.
    - The parent that mounts the Winetricks panel (`ProgressDialog` and the settings surface that
      opens it) — a dialog that re-renders or remounts on a state change one level up is a live
      candidate and is not visible from inside these three files.
  </read_first>

  <action>
    Establish which element stops existing between `pointerdown` and `mouseup`, by measurement on a
    running build. Do not ship a fix for an inferred mechanism — this defect has already consumed one
    adopted-then-disproven theory across two plans.

    Drive a real build (`pnpm tauri:dev`, never a bare `tauri dev`, which serves a stale static
    bundle) and instrument the actual click. Capture, at minimum: which of `pointerdown`,
    `mousedown`, `mouseup`, `click` fire on the button and in what order; whether the `<li>`/`<button>`
    is still in the DOM at `mouseup` time; the computed `display` of `ul.autoComplete` across the
    sequence; and whether `document.activeElement` moves. A MutationObserver on the list's parent,
    logging removals with timestamps, distinguishes "unmounted by React" from "hidden by CSS" —
    these demand different fixes and the record must not guess between them.

    Rule the competing hypotheses in or out explicitly, each with its own observation:
    (a) React unmount of the list subtree; (b) CSS `display: none` via `:focus-within` losing (the
    already-disproven shape — re-check it rather than assume, since the guard is present);
    (c) `ul.autoComplete:empty` firing because `searchResults` emptied; (d) `declined === true`, so
    the click DOES land and `install()` early-returns silently; (e) a remount of the whole Winetricks
    panel from a parent state change.

    Before driving anything through a DevTools console, run a trivial side-effect control first — a
    wedged Web Inspector console executes NOTHING while still looking responsive, and every
    subsequent "observation" would be fabricated.

    This task produces a finding, not a code change. If the measurement contradicts the operator's
    stated attribution ("a React unmount"), say so plainly and record what you actually observed.
  </action>

  <acceptance_criteria>
    - A recorded event log showing which of `pointerdown`/`mousedown`/`mouseup`/`click` fired, in
      order, with timestamps, from a real `pnpm tauri:dev` build.
    - A recorded answer to "was the button still in the DOM at `mouseup`?" backed by a
      MutationObserver record or an equivalent direct observation — not by reading source.
    - Each of the five hypotheses (a)-(e) marked RULED IN or RULED OUT, each with the specific
      observation that decided it. "Not investigated" is an acceptable verdict only if some other
      hypothesis is RULED IN and fully explains the observation.
    - The side-effect control proving the console was live is recorded before any other console
      output is trusted.
    - `git status --short` shows no modified source file — this task measures only.
    - If the measurement is inconclusive after a genuine attempt, STOP and report rather than
      proceeding to Task 2 on a guess. A second disproven theory is worse than an open finding.
  </acceptance_criteria>

  <verify>
    <automated>MISSING — no automated verification is possible for this task; the acceptance
    criteria are the recorded live observations. Task 2's regression pin is the first automated
    check, and Task 3 is the closure signal.</automated>
  </verify>

  <done>
    The element that disappears between pointerdown and mouseup is named, from observation, and the
    four rejected hypotheses each carry the observation that rejected them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix the identified cause and pin it with the tightest test this project can express</name>
  <files>src/frontend/components/UI/SearchBar/index.tsx, src/frontend/components/UI/SearchBar/index.scss, src/frontend/components/UI/SearchBar/__tests__/suggestionFocusRace.test.tsx, src/frontend/components/UI/Winetricks/WinetricksSearch/index.tsx, src/frontend/components/UI/Winetricks/index.tsx</files>

  <read_first>
    - Task 1's finding.
    - `src/frontend/components/UI/SearchBar/__tests__/suggestionFocusRace.test.tsx` — the
      element-graph walking pattern (`walk`, `ElementLike`) and the `react` hook mocks. Your pin
      follows this shape; there is no DOM available.
    - `src/frontend/jest.config.js` header — the standing reason no DOM test exists here, and the
      standing rule that a test must exercise the PRODUCTION call shape, not a hand-built ideal one.
    - Whichever of the three components Task 1 implicated, in full, before editing it.
  </read_first>

  <action>
    Fix the cause Task 1 named, at the layer that owns it. Keep the existing
    `onMouseDown={(e) => e.preventDefault()}` guard on the `<ul>` — its causal claim is retracted but
    the mechanism it guards is real and independently worth keeping; the file's own comment says so.

    Do not weaken the `WINETRICKS_DECLINED_GUARD` early return in `Winetricks/index.tsx`. It exists
    because `winetricksInstall` is send-kind and a mis-routed send is silent; removing the gate would
    convert a visible dead button into an invisible dead channel, which is worse.

    Update the retracted-claim comment block in `SearchBar/index.tsx` so it records the NEW measured
    cause alongside the old retracted one. Do not delete the retraction — it is the record that stops
    the disproven theory being re-adopted a third time.

    Write the regression pin at the tightest layer this project can reach. Be explicit in the test's
    own header about what it does and does not prove: with no DOM, it can assert handler wiring,
    conditional-render predicates and prop shapes on the returned element graph, and it cannot assert
    a rendered outcome in a real engine. Say which live gesture is the real proof.
  </action>

  <acceptance_criteria>
    - The changed file(s) match the cause Task 1 named. If Task 1 named a cause in a file not listed
      in this plan's `files_modified`, STOP and report — a scoped-out file breaks its own gate, and
      silently widening the file set defeats the wave's parallelism guarantee.
    - `grep -n 'onMouseDown' src/frontend/components/UI/SearchBar/index.tsx` still returns the
      `preventDefault` guard.
    - `grep -n 'WINETRICKS_DECLINED_GUARD' src/frontend/components/UI/Winetricks/index.tsx` still
      returns the early-return guard.
    - `grep -n 'RETRACTED' src/frontend/components/UI/SearchBar/index.tsx` still returns the
      retraction paragraph.
    - The new/extended test in `suggestionFocusRace.test.tsx` is RED-proven: it fails against the
      pre-fix shape of whatever you changed. Record the RED output verbatim. If the fix's shape makes
      a RED proof structurally impossible at this layer, say so explicitly and state which live
      gesture carries the proof instead — do not ship a green test that would also be green against
      the defect.
    - The test's header names what it cannot prove and which live gesture proves it.
    - `pnpm test --selectProjects Frontend` passes and `pnpm codecheck` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>pnpm test --selectProjects Frontend &amp;&amp; pnpm codecheck</automated>
  </verify>

  <done>
    The measured cause is fixed, the two existing guards and the retraction record survive, and the
    pin either fails against the pre-fix shape or explicitly states why it cannot.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Prove the Install button works by MOUSE on a real build</name>
  <files>(none — live gesture against a running build; no file is modified)</files>

  <action>
    Do not modify source in this task. Drive the packaged-shape gesture below, record the literal evidence, and score each step WORKED or DID-NOT-WORK. If the mouse click still does nothing, STOP: Task 4 must not run and the requirement must not be amended.
  </action>

  <read_first>
    - Task 2's SUMMARY entry.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md`, gap 2 —
      the statement of what "closed" means for this requirement.
  </read_first>

  <what-built>
    The renderer cause identified in Task 1 is fixed in Task 2, and a regression pin is in place at
    the tightest layer this project's test environment can express. No DOM-level test in this repo
    can prove a real pointer sequence, so this gesture is the closure signal, not the test run.
  </what-built>

  <how-to-verify>
    1. Build and run: `pnpm tauri:dev`. Never a bare `tauri dev` — it serves a stale static bundle.
       If an instance is already running, quit it first: `pnpm tauri:dev` exits 0 WITHOUT replacing a
       running instance, so you would be testing the old build and would not be told.
    2. Open a game's Settings, then the Winetricks panel.
    3. Type a component name (at least 2 characters — the suggestions list is gated on that).
    4. Click the "Install" button on a suggestion **with the mouse**. Do not use the keyboard: the
       channel is already known to work under keyboard activation, so a keyboard pass proves nothing
       about this defect.
    5. Confirm in `gamelib.log` that the `winetricksInstall` send reached the backend, and confirm
       the panel enters its installing state.
    6. Repeat the mouse click on a SECOND suggestion in the same session, to rule out a
       first-click-only artefact.
    7. Regression check, same session: use the OTHER `SearchBar` consumer — the library search
       suggestions — and confirm clicking a suggestion there still works. `SearchBar` is a shared
       primitive and `LibrarySearchBar` passes clickable `<li onClick=...>` items too.
    8. Record the literal log lines and, for each of steps 4, 6 and 7, an explicit
       WORKED / DID-NOT-WORK.
  </how-to-verify>

  <resume-signal>
    Type "approved" with the log excerpts, or describe exactly what happened instead. If the mouse
    click still does nothing, this is the second disproven theory: STOP, do not amend REQ-35-16, and
    report — Task 4 must not run.
  </resume-signal>

  <verify>
    <automated>MISSING — no automated verification exists for a real pointer sequence in this repo (no jsdom, no react-test-renderer). The recorded live evidence in <how-to-verify> is the check.</automated>
  </verify>

  <done>
    Steps 4, 6 and 7 each carry an explicit WORKED verdict with the `gamelib.log` excerpt that backs it.
  </done>
</task>

<task type="auto">
  <name>Task 4: Amend REQ-35-16 so its winetricks clause is satisfiable against the real cause</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md`, the `REQ-35-16` bullet in full (in the Phase 35 requirement body
      list) — specifically the clause "and the fix is **attributed to a named layer** (sidecar
      registration, Rust dispatch, or frontend emit) so the next instance of this class is
      diagnosable rather than rediscovered".
    - Tasks 1-3's findings, which supply the replacement text.
    - `.planning/REQUIREMENTS.md`'s Phase 35 STATUS TABLE rows — read them, but do NOT edit them.
      Plan `35-28` owns the status column and edits this same file; touching a status row here would
      collide with it.
  </read_first>

  <action>
    Amend `REQ-35-16`'s winetricks clause so it is satisfiable against the cause actually measured.
    Keep the original three-layer wording visible as superseded text with a dated correction note
    appended — the pattern the other Phase 35 requirement bodies already use — rather than silently
    rewriting history. A requirement quietly reworded to match what shipped is indistinguishable from
    one that was met.

    The correction states: all three named layers were re-measured correct; the channel works under
    keyboard activation; the real cause is the renderer hit-testing failure Task 1 measured, named
    precisely; and the closure signal is the Task 3 live mouse gesture, not a green suite — because
    this project's test environment structurally cannot express a pointer sequence.

    Preserve every other clause of `REQ-35-16` — the `openDialog` long-running channel and the
    `installed.json` watcher halves are already discharged and their text stands.

    Edit ONLY the `REQ-35-16` requirement body bullet. Do not touch any status-table row.
  </action>

  <acceptance_criteria>
    - `git diff .planning/REQUIREMENTS.md` shows changes confined to the `REQ-35-16` body bullet, and
      touches no line in the Phase 35 status table.
    - The amended bullet contains BOTH the original three-layer wording (marked superseded) and the
      dated correction. `grep -c 'attributed to a named layer' .planning/REQUIREMENTS.md` still
      returns at least 1.
    - The correction names the measured cause in the same words Task 1 recorded, and cites the Task 3
      gesture as the closure signal.
    - The `openDialog` and `installed.json` clauses of `REQ-35-16` are byte-identical to their
      pre-plan text.
    - `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" --help` is NOT required; do not run any
      `gsd-sdk state.*` verb against `.planning/` — four of them corrupt `STATE.md` and `ROADMAP.md`,
      and `annotate-dependencies` deletes plan rows. Hand-edit this file.
  </acceptance_criteria>

  <verify>
    <automated>git diff --stat .planning/REQUIREMENTS.md</automated>
  </verify>

  <done>
    `REQ-35-16` states a satisfiable winetricks clause, keeps its superseded wording visible, and its
    other two clauses are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> sidecar (`winetricksInstall`) | A send-kind channel: a mis-routed send produces no reject, no timeout and no log line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-119 | Repudiation | `winetricksInstall` send-kind channel | mitigate | `WINETRICKS_DECLINED_GUARD` is preserved so a declined probe still gates the send; the live gesture in Task 3 requires a `gamelib.log` line, so "the click worked" is never scored from the UI alone. |
| T-35-120 | Tampering | a second disproven causal theory shipping as a fix | mitigate | Task 1 is measurement-only with per-hypothesis verdicts, Task 2 requires a RED proof or an explicit statement of why one is impossible, and Task 3 blocks the requirement amendment on a live mouse gesture. The prior retracted claim stays in-source. |
| T-35-121 | Repudiation | amending a requirement to match what shipped | mitigate | The superseded three-layer wording stays visible with a dated correction, so the amendment is auditable rather than a silent rewrite. |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed. In particular, do NOT install `jsdom`/`react-test-renderer` to make a DOM test possible — that is a package-manager install requiring a blocking human legitimacy checkpoint, and it is out of this plan's scope. |
</threat_model>

<verification>
- `pnpm test --selectProjects Frontend` passes; `pnpm codecheck` exits 0.
- Task 3's live gesture returned WORKED for steps 4, 6 and 7, with log excerpts.
- Task 1's five hypothesis verdicts are all recorded.

Do NOT gate on `pnpm lint` exiting 0 or on a green `.husky/pre-push`.
</verification>

<success_criteria>
- The Install button on a winetricks suggestion fires on a real mouse click, proven live, twice.
- The shared `SearchBar` primitive's other consumer is confirmed unregressed in the same session.
- `REQ-35-16` has a satisfiable winetricks clause with its superseded wording preserved.
</success_criteria>

<output>
Create `.planning/phases/35-electron-cutover-remove-the-electron-build/35-25-SUMMARY.md` when done.
Record: the Task 1 event log and per-hypothesis verdicts; the file(s) changed and why that layer;
the RED output (or the explicit statement of why one is impossible); Task 3's literal log excerpts
and three WORKED/DID-NOT-WORK verdicts; and the exact REQ-35-16 correction text.
</output>
