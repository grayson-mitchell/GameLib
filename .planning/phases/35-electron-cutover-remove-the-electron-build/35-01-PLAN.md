---
phase: 35-electron-cutover-remove-the-electron-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-electron-cutover-remove-the-electron-build/35-PREFLIGHT.md
  - meta/probeSeaInWorker.ts
autonomous: true
requirements:
  - REQ-35-11
  - REQ-35-16
  - REQ-35-20
decisions: [D-00c, D-07, D-14, D-16, D-17, D-19]

must_haves:
  truths:
    - "D-14: whether `require('node:sea').isSea()` returns the same value inside a `worker_threads.Worker` spawned by the packaged SEA binary as it does on the sidecar's main thread is MEASURED, not assumed. This is security-relevant: `devSecretVault.ts`'s fail-closed guardrail (c) depends on `isPackagedSidecar()`, and plan 35-04 makes `app.isPackaged` a third caller of that same function"
    - "D-16: whether `.github/workflows/release-tauri.yml` performs any runtime check at all, or only builds and uploads, is recorded as a measured fact — so 'CI artifacts plus a smoke launch' stops being ambiguous about who runs the smoke launch"
    - "D-07: the repo's EXISTING hand-rolled single-instance guard and its recorded rejection of `tauri-plugin-single-instance` (D-44-A) are read and restated, so plan 35-07 chooses between extending it and replacing it on evidence rather than bolting the plugin on blind"
    - "D-17: `src/sidecar/index.ts` and `src/backend/sidecar/` are proven to have ZERO import edges into `src/backend/main.ts`. A single stray edge would break the Tauri path at exactly the point-of-no-return commit, which is the worst possible place to discover it"
    - "The censuses that later plans consume — `session.fromPartition` callers, `Menu`/`Tray`/`protocol`/`powerSaveBlocker` consumers, the `flatpak/`+`flathub/` deletion manifest, and the `e2e/` Electron coupling — are written down once, with commands, rather than re-derived by five separate executors"
    - "The `e2e/` Playwright suite's dependence on Electron is surfaced as a phase input BEFORE the point of no return, not discovered by a red CI run after it"
  artifacts:
    - path: ".planning/phases/35-electron-cutover-remove-the-electron-build/35-PREFLIGHT.md"
      provides: "measured answers to the five research open questions and the two pattern-mapper decisions, each with the command that produced it"
      contains: "OQ-1"
      min_lines: 90
    - path: "meta/probeSeaInWorker.ts"
      provides: "the empirical node:sea worker-thread probe backing OQ-1"
      contains: "worker_threads"
  key_links:
    - from: "meta/probeSeaInWorker.ts"
      to: "node:sea"
      via: "guarded require inside a spawned worker thread, mirroring isPackagedSidecar()'s shape"
      pattern: "isSea"
    - from: ".planning/phases/35-electron-cutover-remove-the-electron-build/35-PREFLIGHT.md"
      to: "plans 35-04, 35-07, 35-09, 35-12, 35-13, 35-19"
      via: "each downstream plan reads its own OQ/census section instead of re-measuring"
      pattern: "OQ-"
---

<objective>
Answer the five open questions `35-RESEARCH.md` deliberately left unresolved, plus the two decisions
`35-PATTERNS.md` handed to the planner, by measurement — and write the answers down once so six
later plans consume them instead of each re-deriving (or guessing at) the same facts.

Purpose: this repo's recorded failure mode is confident guessing. `35-RESEARCH.md` flagged five
items rather than guessing at them, and two of the five gate security-relevant or irreversible work
(`node:sea` in a worker thread gates D-14's unification of the value `devSecretVault.ts`'s
fail-closed guard reads; the sidecar→`main.ts` import-edge check gates the point-of-no-return
ordering). Cheap now, unrecoverable later.

Output: `35-PREFLIGHT.md` — one section per question, each with the exact command run, the raw
result, and a one-line disposition. No fixes are made by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-CONTEXT.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-RESEARCH.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-PATTERNS.md

<interfaces>
<!-- Measured at plan time, 2026-08-28, HEAD 9870cf05c. Do not re-derive these five;
     they are already settled. Re-derive only what the tasks below explicitly ask for. -->

ALREADY SETTLED AT PLAN TIME — record these in 35-PREFLIGHT.md as given, with one
confirming command each, and do NOT spend effort re-opening them:

  OQ-2 (userData path parity) — RESOLVED by 35-PATTERNS.md. `src/backend/sidecar/pathShim.ts`
       already implements `getPath('userData')` per-OS: darwin ->
       `~/Library/Application Support/GameLib`, win32 -> `%APPDATA%/GameLib`, linux ->
       `$XDG_CONFIG_HOME/GameLib`. `conf`'s constructor takes `cwd` explicitly. No new
       path-derivation code is needed and there is no relocation risk.

  OQ-3 (D-09's second caller) — RESOLVED by measurement at plan time. CONTEXT.md's
       `legendary/user.ts` is an abbreviation, not a stale reference. The real path is
       `src/backend/storeManagers/legendary/user.ts`. Full `session.fromPartition` /
       `clearStorageData` census (non-test): `src/backend/humble/user.ts`,
       `src/backend/humble/ipc_handler.ts`, `src/backend/humble/loginWindowSeam.ts`,
       `src/backend/humble/adapter.ts`, `src/backend/sidecar/humbleFlowRegistration.ts`,
       `src/backend/sidecar/electronStub.ts`, `src/backend/storeManagers/legendary/user.ts`.

  OQ-5 (electron.vite renderer block) — RESOLVED by 35-PATTERNS.md, which read the file in
       full and confirmed CONTEXT.md's characterization byte-for-byte. The block is
       `electron.vite.config.ts:60-101`. `srcAliases` is defined above it and is shared with
       the `main:`/`preload:` blocks.

STILL OPEN — these are this plan's actual work:

  OQ-1  `node:sea` `isSea()` inside a spawned worker thread. Existing shape to mirror:
        `src/backend/sidecar/humbleFlowRegistration.ts:159` `isPackagedSidecar()` — guarded
        `require('node:sea')`, `catch -> true` (fail closed). Two other call sites already
        re-derive it locally rather than trusting a cached cross-context value:
        `decompressPool.ts`'s `resolveWorkerSpec()` and `lzmaNativeBinding.ts`'s
        `resolveNativeBinding()`.

  OQ-4  CI smoke sufficiency. `.github/workflows/release-tauri.yml` is 436 lines and its LAST
        step is `tauri-apps/tauri-action@v1` at line 429 (`releaseDraft: true`,
        `prerelease: true`). Nothing follows it. Confirm there is no runtime check anywhere in
        the file and state the consequence for D-16.

  PD-A  Single-instance. `src-tauri/src/main.rs:5504-5512` carries the D-44-A decision record
        REJECTING `tauri-plugin-single-instance`, with a mechanism: "a plugin-based guard
        cannot run before `tauri::Builder::default()`, so a secondary process would still reach
        `.setup()` and spawn its own sidecar before the plugin could ever tell it 'you are
        secondary'." `acquire_single_instance()` is `#[cfg(unix)]` only; `main.rs:5571`'s
        `#[cfg(not(unix))]` arm never calls it, and the ledger row `U-34.5-18` names the Windows
        gap as accepted. `protocol_url_arg()` at `main.rs:5388` is the single argv-validation
        choke point, already covered by `#[cfg(test)]` tests at `main.rs:8267+`.

  PD-B  `Menu`/`Tray`/`protocol` export fate. Partially measured at plan time:
        `Menu` has a NON-tray consumer (`src/backend/storeManagers/storeManagerCommon/games.ts`)
        so it cannot simply die with `tray_icon.ts`; `Tray` appears only in
        `src/backend/tray_icon/tray_icon.ts`; `protocol` has one consumer,
        `src/backend/images_cache.ts` (the D-05-accepted `imagecache` scheme).
        Confirm and complete this census.

jest projects are CASE-SENSITIVE: `--selectProjects Backend` (capital B). `meta` matches
nothing and exits 0; the correct name is `Meta`.
</interfaces>
</context>

<constraints>
- **This plan measures. It does not fix.** No production source file is edited. The only new
  code is a throwaway probe under `meta/`, and it is deleted by plan 35-18 (which owns the final
  `electron`-absence sweep) — record that hand-off in `35-PREFLIGHT.md` so the probe does not
  become permanent debris.
- **Do not add `tauri-plugin-single-instance` or `tauri-plugin-deep-link` here.** PD-A produces a
  recommendation with evidence; plan 35-07 owns the choice and the `Cargo.toml` edit.
- **Every answer must name the command that produced it.** A conclusion without a reproducible
  command is the "green check that proves nothing" shape this project has recorded eleven times.
- Restore any file by `cp` + `shasum -a 256`, never `git checkout -- <path>` (fires
  `.husky/post-checkout` -> `download-helper-binaries` -> throws on sentinels). Never `git stash`,
  never `git reset` — concurrent sessions share this working tree.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Measure node:sea isSea() inside a spawned worker thread (OQ-1)</name>
  <read_first>src/backend/sidecar/humbleFlowRegistration.ts around line 159 for `isPackagedSidecar()`'s exact guarded shape; src/backend/sidecar/devSecretVault.ts lines 40-80 and 270-295 for guardrail (c) and why it refuses to re-derive; src/backend/storeManagers/steam/depot/decompressPool.ts `resolveWorkerSpec()` for how a worker is actually spawned in this codebase (entry path, SEA vs dev branch); meta/buildSidecarSea.ts for how the SEA binary is assembled</read_first>
  <files>meta/probeSeaInWorker.ts</files>
  <action>
Write `meta/probeSeaInWorker.ts`: a standalone script that spawns a `worker_threads.Worker` and
has BOTH the main thread and the worker independently evaluate `require('node:sea').isSea()`
using the exact guarded try/catch shape `isPackagedSidecar()` uses, then prints a single line of
the form `main=<bool|throw> worker=<bool|throw>`.

Mirror `isPackagedSidecar()`'s shape exactly — `require('node:sea')` (not `import`), typed as
`{ isSea: () => boolean }`, `catch` returning the fail-closed value. Do not "improve" it; the
point is to measure the behaviour of the code that actually ships.

Run the probe in BOTH contexts and record both results in `35-PREFLIGHT.md`:
  (a) plain `node` (dev/unpackaged) — expected `main=false worker=false`;
  (b) inside a packaged SEA sidecar binary. Use the existing SEA build path
      (`pnpm build:sidecar-sea`) rather than inventing a new one. If wiring the probe into the
      SEA bundle is more than a small amount of work, the acceptable substitute is to add a
      temporary `GAMELIB_PROBE_SEA_WORKER=1` env-gated branch to the sidecar's own entry that
      performs the same two evaluations and logs them, then run the built binary with that env
      var set. State in `35-PREFLIGHT.md` which of the two routes was used.

Record the disposition against D-14 explicitly, in one of exactly two forms:
  - `AGREES` — the worker and main thread return the same value. D-14's unification is safe as
    designed; plan 35-04 proceeds; no new test case is owed.
  - `DIVERGES` — they differ. This is a latent fail-OPEN in `devSecretVault.ts`'s guardrail (c),
    and plan 35-04 MUST additionally add a worker-thread-context case to
    `src/backend/sidecar/__tests__/devSecretVault.test.ts` and must not land the unification
    until that case is green. Say so in the disposition line, naming the test file.

If (b) cannot be run at all in this environment, record `UNMEASURED` with the reason and mark
D-14 as carrying an unretired risk — do NOT record it as `AGREES` on the strength of the
reasoning that a worker thread shares its parent's process. That reasoning is exactly what
RESEARCH.md declined to accept as sufficient.
  </action>
  <acceptance_criteria>
- `meta/probeSeaInWorker.ts` exists and contains the substrings `worker_threads`, `node:sea` and `isSea`.
- Running the probe under plain `node` prints one line matching `main=(true|false|throw) worker=(true|false|throw)` and exits 0.
- `35-PREFLIGHT.md` contains a section headed `OQ-1` carrying: the literal command(s) run, the raw output line for the dev context, the raw output line (or an explicit `UNMEASURED` + reason) for the SEA context, and a disposition line whose value is exactly one of `AGREES`, `DIVERGES`, `UNMEASURED`.
- If the disposition is `DIVERGES` or `UNMEASURED`, the section names `src/backend/sidecar/__tests__/devSecretVault.test.ts` as the file plan 35-04 must extend.
- `pnpm codecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm codecheck && node -e "const s=require('fs').readFileSync('.planning/phases/35-electron-cutover-remove-the-electron-build/35-PREFLIGHT.md','utf8'); const m=s.match(/OQ-1[\s\S]*?(AGREES|DIVERGES|UNMEASURED)/); if(!m) throw new Error('OQ-1 disposition missing'); console.log('OQ-1 disposition:', m[1])"</automated>
  </verify>
  <done>The `node:sea` worker-thread question is answered by a probe that ran, with both contexts recorded, or is explicitly and visibly marked unmeasured.</done>
</task>

<task type="auto">
  <name>Task 2: Answer OQ-4 (CI smoke sufficiency) and the two pattern-mapper decisions PD-A / PD-B</name>
  <read_first>.github/workflows/release-tauri.yml IN FULL (436 lines — the matrix at :50-64, the tauri-action step at :429 and everything between; do not rely on a grep of step names, a runtime check could be an inline `run:` on any step); src-tauri/src/main.rs:5504-5620 for the D-44-A single-instance decision record, `acquire_single_instance()` and the `#[cfg(not(unix))]` arm; src-tauri/src/main.rs:5347-5470 for `protocol_url_arg()` and its callers; src-tauri/src/main.rs:6030-6060 for the socket accept loop's re-validation discipline; src/backend/tray_icon/tray_icon.ts IN FULL; src/backend/images_cache.ts</read_first>
  <files>.planning/phases/35-electron-cutover-remove-the-electron-build/35-PREFLIGHT.md</files>
  <action>
**OQ-4 — does CI verify anything at runtime?** Read `release-tauri.yml` end to end. Record: the
exact matrix legs; whether ANY step launches the built artifact, runs a headless smoke check, or
asserts anything about the produced bundle beyond its existence; and the file's last step. State
the consequence for D-16 as one of three dispositions, with a one-line justification:
  - `CI-SUFFICIENT` — CI already performs a runtime check that satisfies "plus a smoke launch";
  - `NEEDS-CI-STEP` — a new automated step is required, and name concretely what it would run;
  - `NEEDS-HUMAN-SMOKE` — a human must download and launch a CI artifact on real Windows/Linux
    hardware, which is a scheduling commitment, not a code change.
Also record whether closing the user-deferred plan `34-07` (D-00c) would change the answer, since
CONTEXT.md keeps that available as a sequencing fallback.

**PD-A — single-instance: extend the hand-rolled guard, or replace it with the plugin?** Restate
D-44-A's rejection mechanism verbatim from `main.rs`, then record three facts: (i) whether that
mechanism still holds at the pinned `tauri = 2.11.5` (i.e. whether a plugin's callback can now run
before `tauri::Builder::default()` — if you cannot establish this, say so rather than assuming);
(ii) that Windows currently has no guard at all and that `U-34.5-18` records this as accepted;
(iii) that `protocol_url_arg()` is already the validation choke point for argv AND the socket, and
already has Rust unit tests. Then give a recommendation with the tradeoff stated in one sentence
each for: (A) extend the hand-rolled guard to Windows with a named pipe, mirroring the
connect-first/bind-second + fail-open shape; (B) adopt `tauri-plugin-single-instance` and accept a
double sidecar spawn on the secondary process; (C) register `gamelib://` on macOS and Linux only
and ship no Windows registration, recording the Windows deep-link gap under D-05's rule.
Plan 35-07 turns this into a decision checkpoint — do NOT choose here, produce the evidence.

**PD-B — `Menu` / `Tray` / `protocol` / `powerSaveBlocker` / `session` / `screen` / `net` export
fate.** Complete the consumer census for each of the 22 `electronStub.ts` exports, counting only
non-test, non-mock consumers, and EXCLUDING `src/backend/main.ts` and `src/preload/index.ts` (both
die at the point of no return). For each export record: consumer count after those exclusions, the
consumer file(s), and a disposition — `SURVIVES` (has a live consumer), `DEAD` (zero consumers
after the deletions, so plan 35-13 drops it from the platform module), or `SURVIVES-AS-GAP` (only
consumer is itself an accepted D-05 gap, e.g. the `imagecache` scheme). Plan-time partial findings
to confirm and complete: `Menu` -> `storeManagerCommon/games.ts` plus `tray_icon.ts`;
`Tray` -> `tray_icon.ts` only; `protocol` -> `images_cache.ts` only.
**This census is what tells plan 35-13 whether D-02's "same 22 exports" is literally achievable or
whether the platform module ships fewer, with a named, justified delta.** Record the resulting
count explicitly as `PLATFORM_EXPORT_COUNT: <n>` with the dropped names listed.

**Also record three censuses later plans consume, each with its command:**
  - `CENSUS-FLATPAK` — `find flatpak flathub -type f`, plus every `package.json` script naming
    `flatpak` or `flathub`. Plan-time partial: 7 files under `flatpak/`,
    `flathub/update-flathub.ts`, and the scripts `flatpak-build`, `flatpak:prepare`,
    `flatpak:prepare-release`, `release:updateFlathub:ci`. Confirm and complete.
  - `CENSUS-E2E` — the `e2e/` Playwright suite's Electron coupling. Plan-time finding, CONFIRM IT:
    `e2e/helpers.ts` imports `_electron` from `@playwright/test` and launches
    `build/main/main.js`; all five specs go through that helper; `test:e2e` runs
    `electron-vite build && cross-env CI=e2e ... playwright test`. **State the consequence
    plainly: the `CI=e2e` harness that `35-CONTEXT.md` D-19 and the ROADMAP both describe as a
    ready-made cheap harness for proving `R-34.5-G1-PKG` half (b) is an ELECTRON harness and dies
    with Electron.** This is a correction to the phase's own inputs; plans 35-04 and 35-14 depend
    on it.
  - `CENSUS-MAINTS-EDGES` — run `grep -rn "backend/main'\|backend/main\"\|from '\.\./main'\|from './main'" src/sidecar/ src/backend/sidecar/` and record the result. Zero matches is the required
    answer for D-17's ordering to hold. If it is non-zero, say so loudly and name each edge —
    plan 35-14's ordering is invalid until they are cut.
  </action>
  <acceptance_criteria>
- `35-PREFLIGHT.md` contains sections headed exactly `OQ-4`, `PD-A`, `PD-B`, `CENSUS-FLATPAK`, `CENSUS-E2E`, `CENSUS-MAINTS-EDGES`.
- The `OQ-4` section's disposition line reads exactly one of `CI-SUFFICIENT`, `NEEDS-CI-STEP`, `NEEDS-HUMAN-SMOKE`.
- The `PD-A` section quotes the D-44-A rejection sentence from `src-tauri/src/main.rs` and presents all three options A, B and C, each with a stated tradeoff, and reaches no binding choice.
- The `PD-B` section lists all 22 export names from `src/backend/sidecar/electronStub.ts`, each with a consumer count and a disposition of `SURVIVES`, `DEAD` or `SURVIVES-AS-GAP`, and contains a line matching `PLATFORM_EXPORT_COUNT: [0-9]+`.
- The `CENSUS-MAINTS-EDGES` section contains the literal grep command and its raw output or the word `none`.
- The `CENSUS-E2E` section states in its own words that the `CI=e2e` harness is Electron-backed and does not survive the cutover.
- Every section contains at least one fenced command block; `grep -c '```' 35-PREFLIGHT.md` returns an even number greater than or equal to 12.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const f='.planning/phases/35-electron-cutover-remove-the-electron-build/35-PREFLIGHT.md';const s=require('fs').readFileSync(f,'utf8');for(const h of ['OQ-1','OQ-4','PD-A','PD-B','CENSUS-FLATPAK','CENSUS-E2E','CENSUS-MAINTS-EDGES']){if(!new RegExp('^#+ *'+h,'m').test(s))throw new Error('missing section '+h)}if(!/PLATFORM_EXPORT_COUNT: *[0-9]+/.test(s))throw new Error('PLATFORM_EXPORT_COUNT missing');if(!/(CI-SUFFICIENT|NEEDS-CI-STEP|NEEDS-HUMAN-SMOKE)/.test(s))throw new Error('OQ-4 disposition missing');console.log('35-PREFLIGHT.md structure OK')"</automated>
  </verify>
  <done>All five research open questions and both pattern-mapper decisions have measured, commanded, dispositioned answers in one file, and the `e2e`/`CI=e2e` correction is on the record before any irreversible step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged SEA binary -> spawned worker thread | `isPackagedSidecar()`'s value crosses this boundary implicitly; `devSecretVault.ts`'s fail-closed guard consumes it |
| OS -> `gamelib://` URL delivery (argv / socket / future plugin callback) | untrusted URL string crosses here; `protocol_url_arg()` is the choke point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Elevation of Privilege | `node:sea` `isSea()` evaluated in a worker-thread context, read by `devSecretVault.ts` guardrail (c) | mitigate | Task 1 measures both contexts empirically before plan 35-04 makes `app.isPackaged` a third caller. A `DIVERGES` or `UNMEASURED` result blocks the unification until `devSecretVault.test.ts` gains a worker-context case — recorded as a named obligation, not left to the next executor's judgement |
| T-35-02 | Information Disclosure | `35-PREFLIGHT.md` recording probe output | accept | The probe prints two booleans and no path, credential or user identifier. Nothing measured here is sensitive |
| T-35-03 | Spoofing | Deep-link URL delivered to a secondary process (PD-A) | mitigate | PD-A explicitly records that `protocol_url_arg()` is the single validation choke point for BOTH argv and the socket payload, and that the socket path re-validates rather than trusting an "internal" source (T-34.5-G6-20). Any option 35-07 picks must route through it; the census makes that requirement visible rather than implicit |
| T-35-SC | Tampering | npm/cargo installs | mitigate | This plan installs nothing. `35-RESEARCH.md`'s Package Legitimacy Audit flags `conf` as `[SUS]` (assessed a likely false positive) and approves both `tauri-plugin-*` crates; the blocking human checkpoints for those live in plans 35-05 and 35-07, which perform the installs |
</threat_model>

<verification>
- `pnpm codecheck` exits 0.
- `35-PREFLIGHT.md` passes the structure check in Task 2's automated verify.
- Every one of OQ-1, OQ-4, PD-A, PD-B carries a disposition drawn from its allowed vocabulary — no free-text hedges.
- `git diff --stat` shows exactly two files added and no production source file modified.
</verification>

<success_criteria>
No later plan in Phase 35 needs to re-derive an open question. Specifically: 35-04 knows whether
D-14's unification is safe, 35-07 knows the single-instance evidence, 35-13 knows how many exports
the platform module ships, 35-14 knows the sidecar has no edge into `main.ts`, 35-12 has its
deletion manifest, and 35-19 knows who runs the Windows/Linux smoke launch.
</success_criteria>

<output>
Create `.planning/phases/35-electron-cutover-remove-the-electron-build/35-01-SUMMARY.md` when done.
</output>
