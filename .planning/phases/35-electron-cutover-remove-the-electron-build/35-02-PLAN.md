---
phase: 35-electron-cutover-remove-the-electron-build
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md
autonomous: false
requirements:
  - REQ-35-15
decisions: [D-18, D-05]

must_haves:
  truths:
    - "D-18: for each parked bug and folded todo, the record says whether it reproduces under Electron, under Tauri, under both, or under neither — captured while both shells still build. This signal is destroyed permanently the moment Electron is deleted; 'does this reproduce under Electron?' becomes unanswerable forever"
    - "D-18: this is OBSERVATION, not fixing. No production code changes. A bug that reproduces is recorded and left alone; the fix lands in plans 35-09, 35-10 and 35-11 informed by the record"
    - "The named parked bug `debug-uninstall-game-vanishes-parked` — seven hypotheses eliminated, root cause unknown — is one of the observed items, and its own debug ledger is opened rather than summarised from memory"
    - "Anything found to be Tauri-only AND severe is flagged as a candidate blocker for the D-16 gate at the moment it is observed, not left for someone to notice later"
    - "The record names WHICH BUILD each observation was made on, per the phase's standing packaged-not-dev discipline — here both legs are dev builds, and that is stated rather than left implicit"
  artifacts:
    - path: ".planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md"
      provides: "the A/B observation record for 7 items across 2 shells, with every Observed field filled"
      contains: "Observed:"
      min_lines: 120
  key_links:
    - from: ".planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md"
      to: "plans 35-09, 35-10 and 35-11"
      via: "each folded-todo fix cites its own A/B row before choosing an approach"
      pattern: "reproduces"
---

<objective>
Run every parked bug and folded todo against BOTH shells while both still build, and write down
which reproduce where. Fix nothing.

Purpose: D-18. The moment `src/backend/main.ts` is deleted (plan 35-14), the question "does this
reproduce under Electron?" becomes permanently unanswerable. A defect that reproduces under both
shells is a logic bug in shared code; one that reproduces only under Tauri is a port defect. Those
two demand different fixes, and after the cutover you can no longer tell them apart. The
observation is cheap; the window closes once.

Output: `35-AB-RETEST.md`, one row per item per shell, every `Observed:` field filled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-CONTEXT.md
@.planning/phases/35-electron-cutover-remove-the-electron-build/35-RESEARCH.md

<interfaces>
<!-- The seven items, with their source documents. Open each document; do not work from these
     one-line summaries. -->

1. PARKED BUG — `.planning/debug/debug-uninstall-game-vanishes-parked.md`
   Still open. Seven hypotheses eliminated, root cause not found. The debug doc's own repro
   steps are the protocol; do not invent one.

2. `.planning/todos/2026-08-24-installed-json-watcher-never-ported-to-the-tauri-sidecar.md`
   AND its duplicate `.planning/todos/2026-08-25-installed-json-watcher-not-ported-to-tauri.md`
   — DEDUPE: one row, both files cited. The Electron mechanism is
   `src/backend/main.ts:1038-1045`, a `watch(legendaryInstalled, ...)` with a debounce that
   refreshes the Legendary library. Surfaced symptom: `legendary sync-saves` computing an empty
   save path from a stale `installed.json` view.

3. `.planning/todos/2026-08-24-opendialog-is-missing-from-long-running-channels-so-every-file-picker-flow-dies-silently.md`
   ~60s picker timeout; live-reproduced killing `moveInstall` and `importGame`.

4. `.planning/todos/2026-08-24-winetricksinstall-send-channel-is-a-live-silent-no-op.md`
   A `send`-kind channel registered on both ends producing nothing. Related standing gotcha:
   `sidecar-send-channels-fail-silently`.

5. `.planning/todos/2026-08-22-about-window-is-unreachable-under-tauri.md`
   `tauriShowAboutWindow` (`tauriChildWindows.ts:98`) is fully implemented with zero callers.
   Under Electron its only caller is the tray menu (`tray_icon.ts:124`).

6. `.planning/todos/2026-08-24-eos-remove-dialog-renders-as-a-native-system-dialog-not-app-styled.md`

7. `.planning/todos/2026-08-26-path-rejection-dialog-uses-an-oversized-large-text-window.md`
   Two call sites in `installFlowRegistration.ts` (lines 317/444 per the todo).

PLUS the Epic-logout pair, whose disposition is already fixed by D-09 but whose A/B behaviour is
still worth one row because it is the standing 34.6 Step 8 FAIL:
   `.planning/todos/2026-08-24-epic-logout-reports-clearing-cookies-it-does-not-clear.md`
   `.planning/todos/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md`

HARNESS (confirmed present and unmodified in `package.json`):
   Electron leg: `pnpm start`      -> `electron-vite dev --watch`
   Tauri leg:    `pnpm tauri:dev`  -> `electron-vite build && pnpm build:sidecar && pnpm build:decompress-worker-dev && tauri dev`

STANDING GOTCHAS THAT WILL BITE THIS RUN:
- `pnpm tauri:dev` exits 0 WITHOUT replacing an already-running instance. Kill any running
  GameLib before each Tauri leg, and confirm the new process is the one you are observing.
- Never run bare `tauri dev` — it serves a stale static bundle.
- Run the two legs IN SEQUENCE, never simultaneously. Phase 31 D-02 records that the two builds
  carry divergent settings state; two live instances also split the `[shell]` log sink
  (`concurrent-instance-splits-shell-sink`).
- The two log sinks partition: `~/Library/Logs/GameLib/gamelib.log` and the terminal `[shell]`
  output are NOT the same sink. When a row's evidence is "no log line", say which sink was read.
</interfaces>
</context>

<constraints>
- **Observation only. Zero production source edits.** If a fix is obvious, write it in the row's
  `Notes:` field and move on. Plans 35-09, 35-10 and 35-11 own the fixes.
- **Run this before plan 35-03 lands.** 35-03 migrates the renderer to plain Vite and changes what
  `pnpm tauri:dev` does. If 35-03 has already merged, re-confirm both `pnpm start` and
  `pnpm tauri:dev` boot to a usable window before observing anything, and record which commit you
  observed at.
- **Both legs here are DEV builds.** State that in the document header. A dev observation is
  evidence about dev behaviour only — it is not, and must not be cited as, evidence about the
  packaged artifact.
- **An item you could not reach is `NOT ATTEMPTED`, never `does not reproduce`.** An unreachable
  surface producing a pass is a recorded failure shape in this project.
- Never `git stash`, never `git reset`, never `git checkout -- <path>`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Author the A/B observation protocol</name>
  <read_first>All nine source documents named in `<interfaces>` above, in full — the parked debug doc and the eight todo files. Then `.planning/phases/34.18-*/34.18-LIVE-GATE.md` for the `Observed:`-field format precedent (21/21, every field filled)</read_first>
  <files>.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md</files>
  <action>
Create `35-AB-RETEST.md` with a header block recording: the commit observed at (`git rev-parse
HEAD`), the date, the two harness commands verbatim, and an explicit line stating both legs are
DEV builds and carry no evidence about the packaged artifact.

Then one `## Item N — <slug>` section per item (7 sections; items 2's two todo files are ONE
section citing both paths; the Epic-logout pair is ONE section citing both paths). Each section
carries:
  - `Source:` the todo/debug file path(s)
  - `Repro steps:` transcribed from the source document, not paraphrased. If the source document
    has no repro steps, say `Source has no repro steps` and write the minimal steps you will use,
    so the run is reproducible by someone else.
  - `Sink to read:` which of the two log sinks carries the evidence for this item, named
    explicitly — `~/Library/Logs/GameLib/gamelib.log`, the terminal `[shell]` output, or both.
    An item whose evidence is "nothing was logged" MUST name a sink, because absence in the wrong
    sink proves nothing.
  - `Electron leg — Observed:` (left blank for Task 2)
  - `Tauri leg — Observed:` (left blank for Task 2)
  - `Verdict:` (left blank; allowed values, stated in the document: `BOTH`, `TAURI-ONLY`,
    `ELECTRON-ONLY`, `NEITHER`, `NOT ATTEMPTED`)
  - `Severity if TAURI-ONLY:` your pre-assessment of whether a Tauri-only reproduction would
    block the D-16 gate. Write this BEFORE observing, so the severity call is not rationalised
    after the fact.
  - `Notes:`

Item 5 (About window) is a reachability observation, not a bug repro: under Electron, record
whether the tray About item opens the window; under Tauri, record that no user action reaches
`tauriShowAboutWindow`. That asymmetry is the point.
  </action>
  <acceptance_criteria>
- `35-AB-RETEST.md` exists with exactly 7 `## Item ` sections.
- `grep -c 'Observed:' 35-AB-RETEST.md` returns at least 14 (two per item).
- Every section contains all of: `Source:`, `Repro steps:`, `Sink to read:`, `Verdict:`, `Severity if TAURI-ONLY:`.
- The header contains the output of `git rev-parse HEAD` and the literal string `DEV build`.
- The allowed `Verdict:` vocabulary (`BOTH`, `TAURI-ONLY`, `ELECTRON-ONLY`, `NEITHER`, `NOT ATTEMPTED`) is stated in the document.
- No file outside `.planning/` is modified: `git status --porcelain -- src src-tauri meta package.json` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const f='.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md';const s=require('fs').readFileSync(f,'utf8');const items=(s.match(/^## Item /gm)||[]).length;if(items!==7)throw new Error('expected 7 items, found '+items);const obs=(s.match(/Observed:/g)||[]).length;if(obs<14)throw new Error('expected >=14 Observed fields, found '+obs);for(const k of ['Source:','Repro steps:','Sink to read:','Verdict:','Severity if TAURI-ONLY:'])if(!s.includes(k))throw new Error('missing field '+k);if(!/DEV build/.test(s))throw new Error('missing DEV build disclosure');console.log('protocol OK:',items,'items,',obs,'Observed fields')"</automated>
  </verify>
  <done>A runnable, reproducible observation protocol exists with every field present and every `Observed:` slot empty and waiting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the A/B observation under both shells and fill every Observed field</name>
  <files>.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md</files>
  <action>
Human-run observation. Drive both shells in sequence per the protocol below and fill every `Observed:` and `Verdict:` field in `35-AB-RETEST.md`. Observation only — no production source file may change. Redact account names, identifiers and home paths before committing any log excerpt.
  </action>
  <read_first>.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md as authored by Task 1</read_first>
  <what-built>
Task 1 authored `35-AB-RETEST.md`: 7 items, transcribed repro steps, a named log sink per item,
and a pre-committed severity call per item. Every `Observed:` field is blank. Nothing else in the
repo changed.
  </what-built>
  <how-to-verify>
Run each item twice — once per shell — in sequence, never with both running.

**Electron leg, once:**
1. Ensure no GameLib process is running (`pkill -f GameLib` or Activity Monitor).
2. `pnpm start`
3. Work through all 7 items' repro steps. For each, fill `Electron leg — Observed:` with what you
   actually saw, plus the relevant log lines from the sink the item names. If an item is not
   reachable under Electron, write `NOT ATTEMPTED` and why.
4. Quit.

**Tauri leg, once:**
5. Ensure no GameLib process is running. This matters: `pnpm tauri:dev` exits 0 without replacing
   a running instance, so a stale window will silently be the thing you observe.
6. `pnpm tauri:dev`
7. Repeat all 7 items. Fill `Tauri leg — Observed:`.
8. Quit.

**Then, for each item:** set `Verdict:` to one of `BOTH`, `TAURI-ONLY`, `ELECTRON-ONLY`,
`NEITHER`, `NOT ATTEMPTED`. If any verdict is `TAURI-ONLY` and your pre-committed severity said it
would block, add a line `BLOCKS D-16 GATE` to that item and say so in your reply — plan 35-18's
gate document must carry it.

Fix nothing. If you find yourself editing source, stop and note it instead.
  </how-to-verify>
  <acceptance_criteria>
- Every `Observed:` field in `35-AB-RETEST.md` is non-empty (no field left as a placeholder, no field reading only `-` or `TBD`).
- Every `Verdict:` field holds exactly one of the five allowed values.
- Any item whose verdict is `TAURI-ONLY` carries either `BLOCKS D-16 GATE` or an explicit one-line reason it does not.
- The document records the actual harness invocations used and the commit observed at.
- `git status --porcelain -- src src-tauri meta package.json` still prints nothing — this task changed no code.
  </acceptance_criteria>
  <resume-signal>Type "approved" when every Observed and Verdict field is filled, or describe what could not be reached and why.</resume-signal>
  <verify>
    <automated>node -e "const f='.planning/phases/35-electron-cutover-remove-the-electron-build/35-AB-RETEST.md';const s=require('fs').readFileSync(f,'utf8');const bad=s.split('\n').filter(l=>/(Observed:|Verdict:)\s*(TBD|-)?\s*$/.test(l));if(bad.length)throw new Error('unfilled fields:\n'+bad.join('\n'));const v=(s.match(/Verdict:\s*(BOTH|TAURI-ONLY|ELECTRON-ONLY|NEITHER|NOT ATTEMPTED)/g)||[]).length;if(v!==7)throw new Error('expected 7 valid verdicts, found '+v);console.log('A/B record complete, 7 verdicts')"</automated>
  </verify>
  <done>The A/B signal is captured. Every item is attributed to a shell, and any Tauri-only severe defect is on the record as a D-16 gate candidate before the delete makes the comparison impossible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live app session -> log sinks | observation evidence crosses here; logs may contain paths and account identifiers |
| Epic/Humble logged-in session -> the logout item's observation | a real credentialed session is exercised to observe the logout defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Information Disclosure | Log excerpts pasted into `35-AB-RETEST.md`, which is committed to a public repo | mitigate | Redact-forward per the standing `steam-identity-in-repo-redact-forward-decision`: before committing, replace account names, SteamID64s, Epic/Humble identifiers, auth tokens and absolute home paths with `<redacted>`. Quote the log LINE SHAPE, not the payload |
| T-35-05 | Repudiation | An observation recorded without naming which sink or which shell produced it | mitigate | The protocol requires `Sink to read:` per item and separates `Electron leg`/`Tauri leg` fields. An absence claim read from the wrong sink is a recorded failure shape here (`never use a tee'd transcript to prove a backend path did NOT run`) |
| T-35-06 | Tampering | The Epic-logout item leaves a session in a partially-cleared state on the observer's machine | accept | Developer machine only; D-09's fix in plan 35-09 is the remedy. Record the residual state in `Notes:` so the observer knows to re-login |
| T-35-SC | Tampering | npm/cargo installs | accept | This plan installs nothing and adds no dependency. `pnpm start`/`pnpm tauri:dev` run against the already-installed lockfile |
</threat_model>

<verification>
- `35-AB-RETEST.md` has 7 items, 14+ filled `Observed:` fields, and 7 verdicts from the fixed vocabulary.
- No production source file changed: `git status --porcelain -- src src-tauri meta package.json` is empty.
- Every log excerpt in the document is redacted per T-35-04.
- Any `TAURI-ONLY` + blocking item is named in the summary so plan 35-19 can carry it into the gate.
</verification>

<success_criteria>
The A/B signal exists on disk before anything irreversible happens. For each of the seven items, a
later reader can tell whether the defect predates the Tauri port or was introduced by it — a
question that becomes permanently unanswerable after plan 35-14.
</success_criteria>

<output>
Create `.planning/phases/35-electron-cutover-remove-the-electron-build/35-02-SUMMARY.md` when done.
</output>
