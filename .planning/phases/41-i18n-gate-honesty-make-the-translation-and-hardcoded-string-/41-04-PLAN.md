---
phase: 41-i18n-gate-honesty-make-the-translation-and-hardcoded-string-
plan: 04
type: execute
wave: 2
depends_on: [41-02]
files_modified:
  - meta/i18nGateScope.json
  - meta/__tests__/genI18nGateScope.test.ts
  - meta/__tests__/hardcodedStringGate.test.ts
autonomous: true
requirements: [REQ-41-04]

must_haves:
  truths:
    - "facetLabels.ts, chipLabels.ts and helpers/gamepad.ts are scanned by the BLOCKING gate on every run, at zero violations"
    - "None of the three remains in DECLARED_UNSCANNED_DEBT"
    - "The committed scope artifact keeps its hand-curated provenance — it was NOT regenerated"
    - "A hardcoded literal added to any of the three now fails CI by name instead of sitting in a comment-only register"
  artifacts:
    - path: "meta/i18nGateScope.json"
      provides: "Blocking gate scope widened from 171 to 174 files, hand-edited"
      contains: "facetLabels.ts"
    - path: "meta/__tests__/genI18nGateScope.test.ts"
      provides: "DECLARED_UNSCANNED_DEBT shrunk 44 → 41, scope count pin 171 → 174, header comment corrected"
      contains: "REQ-41-04"
    - path: "meta/__tests__/hardcodedStringGate.test.ts"
      provides: "The WR-18 block's W1 inverted — the three files are now IN scope and covered by the blocking report, not by an audit-mode ratchet"
      contains: "REQ-41-04"
  key_links:
    - from: "meta/i18nGateScope.json"
      to: "meta/hardcodedStringGate.ts"
      via: "scanScope() reads scope.files and scans each on every run"
      pattern: "i18nGateScope\\.json"
    - from: "meta/__tests__/genI18nGateScope.test.ts"
      to: "meta/i18nGateScope.json"
      via: "the A-17 ratchet comparing the live derivation's unscanned set against DECLARED_UNSCANNED_DEBT"
      pattern: "DECLARED_UNSCANNED_DEBT"
---

<objective>
Fold `facetLabels.ts`, `chipLabels.ts` and `helpers/gamepad.ts` out of `DECLARED_UNSCANNED_DEBT`
and into `meta/i18nGateScope.json` at zero violations — the closing half of REQ-41-04 and the
condition the 2026-08-27 todo names as "what done looks like".

Purpose: until this lands, the three files are vouched for by a comment. After it, the blocking
gate scans them on every `pnpm test:ci` run, so a hardcoded literal added to any of them fails CI
by name.

Output: scope 171 → **174** files at 0 violations; `DECLARED_UNSCANNED_DEBT` 44 → **41** entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-i18n-gate-honesty-make-the-translation-and-hardcoded-string-/41-02-SUMMARY.md
@.planning/todos/pending/2026-08-27-i18n-gate-flags-declaration-site-literals-as-violations.md

## DO NOT run `pnpm gen-i18n-gate-scope`

The phase brief anticipated "plan for the pin regeneration explicitly rather than discovering it".
Measurement at HEAD says the correct action is the **opposite**: hand-edit, never regenerate.

`meta/i18nGateScope.json` carries hand-curated provenance:

```
generatedBy: "hand-edited (34.13 review CR-02(b), then A-02/A-03, then 34.12-04 Task 2 repointed
              the renamed onboarding-tour component's path) , then quick 260905-d33 added the new
              AboutDialog component -- NOT a `pnpm gen-i18n-gate-scope` run; see
              meta/__tests__/genI18nGateScope.test.ts's staleness guard"
```

`genI18nGateScope.test.ts:796` asserts `isHandCuratedProvenance(scopeSnapshot.generatedBy)` is
`true`, and `isHandCuratedProvenance` (`genI18nGateScope.ts:291`) is simply "provenance is not
`GENERATOR_PROVENANCE`". A `pnpm gen-i18n-gate-scope` run would either be REFUSED by the generator's
own clobber guard (it returns a `ScopeRewriteRefusal` when it finds hand-curated provenance) or, if
forced, would rewrite the scope to all **215** fork-touched files and flip that assertion. Either
way it is the wrong instrument. **Add three paths by hand; leave `baseCommit`, `baseVersion`,
`generatedAt` and `generatedBy` alone** apart from appending this phase's note to `generatedBy` in
the same style as the existing entries.

## Measured at HEAD (2026-09-06) — the pins this plan must move

| Artifact / pin | Location | Value now | Value after |
|---|---|---|---|
| `i18nGateScope.json` `files` | — | 171 | **174** |
| `i18nForkTouchedFiles.json` `files` | — | 215 | **215** (all three are ALREADY fork-touched — this is a scope move, not a fork-touched move) |
| scope count pin | `genI18nGateScope.test.ts:793` | `toBe(171)` | `toBe(174)` |
| fork-touched pins | `genI18nGateScope.test.ts:794-795` | `toBe(215)` | unchanged |
| A-17 ratchet | `genI18nGateScope.test.ts:353` | `unscanned.sort()` equals `DECLARED_UNSCANNED_DEBT.sort()` | still equal, both shrunk by 3 |
| `DECLARED_UNSCANNED_DEBT` | `genI18nGateScope.test.ts:269-314` | 44 entries | **41** |
| generator refusal pin | `genI18nGateScope.test.ts:838` | `refusal.missing` equals `DECLARED_UNSCANNED_DEBT` | still equal after both edits |
| WR-18 W1 | `hardcodedStringGate.test.ts:1471` | asserts the files are NOT in scope | **inverted** |

The three entries to remove from `DECLARED_UNSCANNED_DEBT` are at `:283`, `:291` and `:296`.

## Two corrections to fold into the header comment

1. `genI18nGateScope.test.ts:66-72` says gamepad.ts's three violations are at `:323`, `:370` and
   `:377`. Measured: **405, 452, 459**. The comment is stale.
2. The same paragraph says "The right fix is in the gate: stop flagging CSS-selector-shaped
   literals." Plan 41-02 took a narrower route — the three literals are `closest()` ARGUMENTS, so
   the fix was a method-name gate (`'closest'` added to `TECHNICAL_DOM_API_METHOD_NAMES`), not a
   content-shape regex. Record what was actually done.

## Scope fence

`meta/` only. The three frontend files are **read, never edited**. No runtime code path. No
network.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-edit the scope artifact and shrink the declared debt register in one commit</name>
  <read_first>
    - `meta/i18nGateScope.json` — its top-level keys and the sort order of `files`
    - `meta/__tests__/genI18nGateScope.test.ts` :40-100 (the header paragraphs describing each
      declared-debt entry, including the gamepad.ts paragraph at :66-72)
    - `meta/__tests__/genI18nGateScope.test.ts` :269-314 (`DECLARED_UNSCANNED_DEBT`) and :340-360
      (the A-17 ratchet that compares it against the live derivation)
    - `meta/__tests__/genI18nGateScope.test.ts` :785-800 (the count pins)
  </read_first>
  <files>meta/i18nGateScope.json, meta/__tests__/genI18nGateScope.test.ts</files>
  <action>
    These two edits are a matched pair. The A-17 ratchet asserts set equality between the live
    unscanned derivation and `DECLARED_UNSCANNED_DEBT`, so splitting them across commits leaves the
    Meta project red in between. Make both, then verify once.

    **(a) `meta/i18nGateScope.json`** — insert the three paths into `files`, preserving the array's
    existing sort order exactly (verify the array is sorted first; if it is, insert in sorted
    position and assert the result is still sorted):
    - `src/frontend/helpers/gamepad.ts`
    - `src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts`
    - `src/frontend/screens/Library/facetLabels.ts`

    Append to `generatedBy`, in the same voice as the existing text: `, then Phase 41 (REQ-41-04)
    added facetLabels.ts, chipLabels.ts and helpers/gamepad.ts after the gate stopped flagging
    their declaration-site key/defaultText tables and closest() selector arguments -- still NOT a
    \`pnpm gen-i18n-gate-scope\` run`. Do not touch `baseCommit`, `baseVersion`, `generatedAt` or
    `excluded`.

    **(b) `meta/__tests__/genI18nGateScope.test.ts`** —
    - delete the three entries from `DECLARED_UNSCANNED_DEBT` (44 → 41)
    - update the count pin at `:793` from `toBe(171)` to `toBe(174)`; leave `:794-795` at 215
    - rewrite the header paragraphs covering these three files: the gamepad.ts paragraph (:66-72)
      and the chipLabels/facetLabels paragraph (:74-82) both currently describe live debt that no
      longer exists. Replace them with a dated Phase 41 entry recording: the measured before counts
      (3 / 35 / 8 = 46), that the fix was in the gate not the register, the two corrections listed
      in the context (the real line numbers 405/452/459, and that the gamepad fix was a
      method-name gate on `closest` rather than a CSS-content-shape check), and that the scope
      moved 171 → 174 by hand edit with provenance preserved.

    Then run the derivation-vs-register check and confirm the sets still match exactly.
  </action>
  <verify>
    <automated>node -e 'const s=JSON.parse(require("fs").readFileSync("meta/i18nGateScope.json","utf8"));const want=["src/frontend/helpers/gamepad.ts","src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts","src/frontend/screens/Library/facetLabels.ts"];const miss=want.filter(f=>!s.files.includes(f));const sorted=JSON.stringify(s.files)===JSON.stringify([...s.files].sort());if(s.files.length!==174){console.error("scope count",s.files.length);process.exit(1)}if(miss.length){console.error("missing",miss);process.exit(1)}if(!sorted){console.error("files array no longer sorted");process.exit(1)}console.log("OK scope 174, all three present, sorted")'</automated>
    <automated>node -e 'const s=JSON.parse(require("fs").readFileSync("meta/i18nGateScope.json","utf8"));const g=String(s.generatedBy||"");if(/gen-i18n-gate-scope run/.test(g)===false||!/hand-edited/.test(g)){console.error("provenance no longer reads as hand-curated:",g);process.exit(1)}console.log("OK hand-curated provenance preserved")'</automated>
    <automated>grep -v '^\s*[/*]' meta/__tests__/genI18nGateScope.test.ts | grep -cE "'src/frontend/(helpers/gamepad\.ts|screens/Library/facetLabels\.ts|screens/Library/components/FilterChipRow/chipLabels\.ts)'" | awk '{ if ($1 != 0) { print "a promoted file is still listed in DECLARED_UNSCANNED_DEBT: " $1; exit 1 } else print "OK all three removed from the debt register" }'</automated>
    <automated>npx jest --selectProjects Meta --testPathPattern genI18nGateScope --runInBand 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `meta/i18nGateScope.json` has 174 files, sorted, containing all three paths
    - `generatedBy` still reads as hand-curated and records this phase's edit; `baseCommit`,
      `baseVersion`, `generatedAt` and `excluded` are byte-unchanged (verify with
      `git diff meta/i18nGateScope.json` — only `files` and `generatedBy` lines should differ)
    - `pnpm gen-i18n-gate-scope` was **NOT** run — state this explicitly in the SUMMARY
    - `DECLARED_UNSCANNED_DEBT` has exactly 41 entries and none of the three
    - The `:793` pin reads `toBe(174)`; `:794-795` still read `toBe(215)`
    - The A-17 ratchet passes (live unscanned derivation still equals the register)
    - The header comment records the corrected line numbers (405/452/459) and the actual fix shape
    - `npx jest --selectProjects Meta --testPathPattern genI18nGateScope --runInBand` passes
  </acceptance_criteria>
  <done>meta/i18nGateScope.json holds 174 sorted files with hand-curated provenance intact, DECLARED_UNSCANNED_DEBT holds 41 entries, the :793 pin reads 174, and the A-17 ratchet still matches.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the WR-18 audit-mode ratchet now that the blocking report covers these files</name>
  <read_first>
    - `meta/__tests__/hardcodedStringGate.test.ts` — the WR-18 describe block as plan 41-02 left it
      (W1 asserting the files are outside scope/allowlist, W2 pinning zero violations, W3 pinning
      exempted counts, W4 no-collateral, plus the sabotage tests)
    - `meta/__tests__/hardcodedStringGate.test.ts` :1420-1450 (`scannedFiles matches the committed
      scope snapshot exactly`, the `allowlist` length pin at 2, and the `fileExempt` pin)
  </read_first>
  <files>meta/__tests__/hardcodedStringGate.test.ts</files>
  <action>
    Task 1 makes W1 FAIL: it asserts the three files are NOT in `meta/i18nGateScope.json`, which is
    no longer true. Handle it here, in the same wave, so the Meta project is never left red.

    Rework the block:
    - **W1 — invert.** Assert all three files ARE in `meta/i18nGateScope.json` and are still NOT in
      `meta/i18nGateAllowlist.json`. The allowlist is a DEFERRAL register for genuine postponed
      debt (`expectedCount` + a blocking reason); a false positive recorded there would read as real
      debt forever, and the todo is explicit that neither shape belongs there. Keep the existing
      `allowlist` length pin at exactly 2 untouched.
    - **W2 — re-source from the BLOCKING report.** Replace `scanScope({ extraFiles: [...] })` with
      plain `scanScope()` and assert zero violations attributed to each of the three. They are now
      covered by the same report as the other 171 files; an `extraFiles` audit would be measuring a
      widening that no longer exists.
    - **W3 — keep the `exempted > 0` per-file pins**, re-sourced the same way. This is what keeps a
      zero-violation file distinguishable from an unscanned one.
    - **W4 — retire.** "No collateral outside the extraFiles" was a property of audit mode. The
      suite's existing `expect(report.violations).toHaveLength(0)` over the whole blocking scope
      subsumes it. Delete W4 and say so in the block comment rather than leaving a test that no
      longer asserts anything meaningful.
    - **Sabotage tests — keep both** (`chipLabels.ts` and `gamepad.ts` scratch copies). They remain
      the only proof that a genuine untranslated literal is still caught in files whose real
      literals are all exempt.

    Rename the block to reflect what it now is — coverage of three newly-scoped files, not a ratchet
    over unscanned debt — and cite `REQ-41-04`. Record the whole arc in its comment: 46 violations
    → gate widened (41-02) → 0 → promoted into the blocking scope (41-04).
  </action>
  <verify>
    <automated>npx jest --selectProjects Meta --testPathPattern hardcodedStringGate --runInBand 2>&1 | tail -25</automated>
    <automated>grep -v '^\s*[/*]' meta/__tests__/hardcodedStringGate.test.ts | grep -c "not.toContain" | awk '{ print "residual not.toContain assertions: " $1 }'</automated>
    <automated>node -e 'const {scanScope}=require("ts-node") ' 2>/dev/null; node meta/runTs.cjs --bundle --platform=node --target=node21 meta/hardcodedStringGate.ts >/dev/null 2>&1; echo "gate module builds: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --selectProjects Meta --testPathPattern hardcodedStringGate --runInBand` passes
    - W1 now asserts the three files ARE in the committed scope and are NOT in the allowlist
    - W2/W3 are sourced from `scanScope()` with no `extraFiles`, and assert 0 violations plus a
      non-zero `exempted` count per file
    - W4 is deleted with the reason recorded in the block comment
    - Both sabotage tests survive and still pass
    - The allowlist length pin remains exactly 2, and `fileExempt` remains exactly
      `['src/frontend/bootErrorSurface.ts']`
  </acceptance_criteria>
  <done>W1 asserts the three files are in scope and out of the allowlist, W2/W3 are sourced from scanScope() with no extraFiles, W4 is deleted with its reason recorded, and both sabotage tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the promoted gate end to end and close the 2026-08-27 todo</name>
  <read_first>
    - `.planning/todos/pending/2026-08-27-i18n-gate-flags-declaration-site-literals-as-violations.md`
      — specifically its "What done looks like" section
    - `.planning/todos/completed/` — the naming and closure-record convention used by this repo
  </read_first>
  <files>meta/__tests__/hardcodedStringGate.test.ts</files>
  <action>
    Run the full measurement and record it. Do not rely on a green suite as the result — this
    project's history is largely gates that were green while proving nothing.

    1. `scanScope()` — record `scannedFiles` (expect 174), `violations.length` (expect 0),
       `totalCandidates`, `staleExemptions.length` (expect 0), `fileExempt` (expect exactly
       `['src/frontend/bootErrorSurface.ts']`).
    2. Per-file: for each of the three, record `violations` (expect 0) and `exempted` (expect > 0).
       An `exempted` of 0 for any of them means the scanner is not actually looking and must be
       investigated before this plan is called done.
    3. Sabotage once more at the whole-gate level: append a bare literal to a scratch copy of
       `facetLabels.ts` in `mkdtempSync`, scan it via `extraFiles`, and confirm the violation count
       rises by exactly 1. Record the delta. Never write to the real file.

    Then close the todo: move
    `.planning/todos/pending/2026-08-27-i18n-gate-flags-declaration-site-literals-as-violations.md`
    to `.planning/todos/completed/` with a closure record appended stating what shipped (the four
    gate widenings from 41-02, the scope promotion from this plan), the measured numbers above, and
    the two corrections to its own framing: the gamepad.ts case was a `closest()` argument rather
    than a bare-selector content-shape problem, and the D-14 tuple exemption already existed and was
    WIDENED rather than duplicated.

    If any measured number differs from the expectation above, record the actual number and the
    discrepancy — do not adjust the expectation to match the observation without saying so.
  </action>
  <verify>
    <automated>npx jest --selectProjects Meta --runInBand 2>&1 | tail -20</automated>
    <automated>ls .planning/todos/pending/2026-08-27-i18n-gate-flags-declaration-site-literals-as-violations.md 2>/dev/null | wc -l | awk '{ if ($1 != 0) { print "todo still pending"; exit 1 } else print "OK todo moved out of pending" }'</automated>
    <automated>ls .planning/todos/completed/ | grep -c "2026-08-27-i18n-gate-flags-declaration-site-literals" | awk '{ if ($1 != 1) { print "closure record not found in completed/"; exit 1 } else print "OK closure record present" }'</automated>
  </verify>
  <acceptance_criteria>
    - All five whole-gate numbers recorded: 174 scanned, 0 violations, `totalCandidates`, 0 stale
      exemptions, `fileExempt` exactly one entry
    - Per-file `exempted` counts recorded and all three are non-zero
    - The whole-gate sabotage raised the violation count by exactly 1 — delta recorded
    - The 2026-08-27 todo is in `.planning/todos/completed/` with a closure record carrying the
      measured numbers and the two framing corrections
    - `npx jest --selectProjects Meta --runInBand` green with the expected suite count
  </acceptance_criteria>
  <done>All whole-gate numbers are recorded (174 / 0 / 0 stale / one fileExempt), every per-file exempted count is non-zero, the sabotage delta is exactly 1, and the 2026-08-27 todo is closed with a measured record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time lint tooling and its committed scope artifact | This plan edits a JSON list of file paths and two jest suites. No untrusted input, no network, no auth, no user data, no runtime code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04-01 | Tampering | `meta/i18nGateScope.json` | mitigate | Widening a blocking gate's scope by hand is the deliberate act this artifact's clobber guard exists to protect. The edit is asserted to add exactly 3 paths, keep the array sorted, keep `baseCommit`/`baseVersion`/`generatedAt`/`excluded` byte-unchanged, and preserve hand-curated provenance. `pnpm gen-i18n-gate-scope` is explicitly NOT run — a regeneration would rewrite the scope to all 215 fork-touched files and flip the provenance assertion. |
| T-41-04-02 | Elevation of privilege | scope entry that is never actually scanned | mitigate | W3's per-file `exempted > 0` pin and the whole-gate `scannedFiles === scope.files.length` pin together prove the three new entries are really scanned, not merely listed. A path in the scope that the scanner skips would pass a naive count check and fail these. |
| T-41-04-03 | Repudiation | debt register and scope drifting apart | mitigate | The A-17 ratchet asserts SET equality between the live unscanned derivation and `DECLARED_UNSCANNED_DEBT`, and both edits land in one commit. This project has a recorded case of a count that did not move while the SET did. |
| T-41-04-04 | Information disclosure | closure record contents | accept | The closure record contains repo-relative paths and violation counts, all already public in the repo. |
| T-41-04-SC | Tampering | npm/pip/cargo installs | mitigate | **No package-manager install occurs in this plan.** No dependency added, removed or upgraded; `package.json` is not in `files_modified`. Supply-chain checkpoint not applicable. |
</threat_model>

<verification>
1. `scanScope()` → 174 files, 0 violations, 0 stale exemptions (before: 171 / 0 / 0). Record.
2. `npx jest --selectProjects Meta --runInBand` — whole project green; confirm the suite count
   (36 at HEAD) and record the total test count.
3. `pnpm codecheck` — exit 0.
4. `pnpm lint` — exit 0 within the 4157 ceiling; record the count.
5. `git diff --stat meta/i18nGateScope.json` — only `files` and `generatedBy` lines differ.
6. `git status --porcelain` names only the three files in `files_modified` plus the todo move.
</verification>

<success_criteria>
- Blocking scope: **171 → 174** files at **0** violations, hand-edited with provenance preserved
- `DECLARED_UNSCANNED_DEBT`: **44 → 41** entries
- `pnpm gen-i18n-gate-scope` was never run
- Every pin that depended on the old numbers is updated in the SAME commit as the artifact
  (`:793` 171 → 174; the A-17 ratchet still set-equal; the generator refusal pin still consistent)
- Each of the three files reports 0 violations and a non-zero `exempted` count from the BLOCKING
  report, not from an audit-mode widening
- A sabotage literal added to a scratch copy still raises the count by exactly 1
- The 2026-08-27 todo is closed with a measured closure record
- Nothing under `src/` was edited
</success_criteria>

<output>
Create `.planning/phases/41-i18n-gate-honesty-make-the-translation-and-hardcoded-string-/41-04-SUMMARY.md`
when done. It MUST carry: scope before/after (171 → 174), debt register before/after (44 → 41),
per-file violation and exempted counts, the whole-gate sabotage delta, an explicit statement that
`pnpm gen-i18n-gate-scope` was not run, and the todo closure path.
</output>
