---
phase: 41-i18n-gate-honesty-make-the-translation-and-hardcoded-string-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - meta/hardcodedStringGate.ts
  - meta/__tests__/hardcodedStringGate.test.ts
autonomous: true
requirements: [REQ-41-04]

must_haves:
  truths:
    - "The gate reports zero violations for facetLabels.ts, chipLabels.ts and helpers/gamepad.ts in audit mode (was 8 + 35 + 3 = 46)"
    - "The gate still reports a genuine bare English literal added to any of those three files"
    - "Each widened exemption has a paired negative fixture proving the shape alone is not enough to exempt"
    - "The committed blocking scope still reports zero violations and zero stale exemptions"
  artifacts:
    - path: "meta/hardcodedStringGate.ts"
      provides: "Widened D-14 tuple exemption (ns-prefixed keys), a key/defaultText object-literal exemption, `closest` as a technical DOM API, and a widened t-alias type match"
      contains: "isKeyDefaultObjectProperty"
    - path: "meta/__tests__/hardcodedStringGate.test.ts"
      provides: "Positive + paired-negative fixtures for each widening, and the WR-18 ratchet re-based from 8/35 to 0/0/0"
      contains: "REQ-41-04"
  key_links:
    - from: "meta/hardcodedStringGate.ts"
      to: "src/frontend/screens/Library/facetLabels.ts"
      via: "isKeyDefaultTupleElement over an ns-prefixed dotted key"
      pattern: "DOTTED_KEY_RE"
    - from: "meta/hardcodedStringGate.ts"
      to: "src/frontend/helpers/gamepad.ts"
      via: "isTechnicalDomApiArgument gated on the method name `closest`"
      pattern: "'closest'"
    - from: "meta/__tests__/hardcodedStringGate.test.ts"
      to: "meta/hardcodedStringGate.ts"
      via: "scanScope({ extraFiles: [...] }) audit-mode measurement"
      pattern: "extraFiles"
---

<objective>
Widen the **existing** D-14 exemptions in `meta/hardcodedStringGate.ts` so the gate stops flagging
declaration-site `[key, defaultText]` data tables and DOM selector arguments as untranslated UI
text, and re-base the ratchet that currently pins those false positives in place.

Purpose: 46 violations across three files are structural false positives — every one is either an
i18n key literal, its paired English default, or a CSS selector. They are the only reason those
files sit in `DECLARED_UNSCANNED_DEBT` instead of the blocking scope. Plan 41-04 does the
promotion; this plan makes the promotion possible at zero violations.

Output: `scanScope({ extraFiles: [all three] })` reports **0** violations (currently 46), with the
committed blocking scope unchanged at 171 files / 0 violations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/2026-08-27-i18n-gate-flags-declaration-site-literals-as-violations.md

## Measured at HEAD (2026-09-06) — the actual 46 violations, by shape

Run with `scanScope({ extraFiles: [file] })` against the committed gate. Baseline (scope only):
**171 files scanned, 0 violations.**

| File | Count | Kind | Root cause — measured, not inferred |
|---|---|---|---|
| `src/frontend/screens/Library/facetLabels.ts` | 8 | `argument` | `RUNNABILITY_LABELS`'s 4 tuples ARE the `[key, defaultText]` shape `isKeyDefaultTupleElement` already exempts — but their keys are namespace-prefixed (`gamelib:library.filterPanel.runsNatively`) and `DOTTED_KEY_RE` (`:1176`) has no `ns:` alternative, so the tuple check rejects them. |
| `src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` | 26 | `object-property` | `chipLabelSpec` returns `{ ns, key, defaultText }` OBJECT literals, not 2-tuples. Same idiom, different container — the existing structural check only looks at `ArrayLiteralExpression`. |
| (same file) | 8 | `argument` | `resolveLabel(spec, t: TFunc, tGamelib: TFunc)` — `collectTAliases` (`:1006`) matches a parameter whose declared type text `includes('TFunction')`. The local alias type here is spelled `TFunc`, so `tGamelib(...)` is not recognised as a t-call. (The parameter literally named `t` IS recognised, via the bare-name seed — which is why only the `tGamelib` call sites are flagged.) |
| `src/frontend/helpers/gamepad.ts` | 3 | `argument` | `el.closest('.MuiPopover-root')` at **:405**, `.closest('.MuiDialog-root')` at **:452** and **:459**. `TECHNICAL_DOM_API_METHOD_NAMES` (`:709`) already covers `querySelector`/`querySelectorAll` — the same file's `parent.querySelector('.playIcon')` calls are NOT flagged. `closest` is simply absent from the set. |

**Two corrections to the inherited framing, both established by the measurement above.**

1. The phase-planning brief and the ROADMAP describe `gamepad.ts` as needing a **bare CSS class
   selector content-shape heuristic** (a `.MuiFoo-bar` regex alongside `CSS_ATTR_SELECTOR_RE`).
   The measurement says otherwise: all three literals are `closest()` ARGUMENTS, so the
   method-name-gated route this file already established for `querySelector` is available and is
   strictly narrower. A content-shape regex would exempt any literal shaped `.kebab-word` ANYWHERE
   in the 171-file scope; adding `'closest'` to the method-name set exempts nothing outside a
   `.closest()` call. **Take the narrow route.** Record this correction in the SUMMARY.

2. `genI18nGateScope.test.ts`'s header comment (`:66-72`) cites gamepad.ts lines `:323`, `:370`,
   `:377`. The real lines are **405, 452, 459** — the comment is stale. Plan 41-04 owns that file;
   note the correction for it, do not edit it here.

## Prototype: this design is proven, not guessed

The four changes below were prototyped against a scratch copy of the gate and measured at HEAD on
2026-09-06:

- `scanScope()` (blocking scope): 171 files, **0** violations — unchanged, no collateral
- `scanScope({ extraFiles: [all three] })`: 174 files, **0** violations (from 46)
- `staleExemptions`: 0; `fileExempt`: `['src/frontend/bootErrorSurface.ts']` — both unchanged
- Negative fixtures still FLAG: the existing `['Windows only', 'Not available on macOS']` tuple
  (2 violations), the object-literal analogue with a non-dotted key (2), an object missing its
  `defaultText` sibling (1), a bare appended literal (1), and an alias typed `Other` rather than
  `TFunc` (2)

Reproduce these numbers before and after your edit; do not take them on trust.

## Scope fence

`meta/` only. The three frontend files are **read, never edited** — they move between gate scope
registers, their contents do not change. No runtime code path.

## Interfaces — the exact edit anchors

```
meta/hardcodedStringGate.ts:709-711   TECHNICAL_DOM_API_METHOD_NAMES  ('querySelector','querySelectorAll',...)
meta/hardcodedStringGate.ts:1006      if (typeText.includes('TFunction')) {
meta/hardcodedStringGate.ts:1176      const DOTTED_KEY_RE = /^[a-zA-Z][a-zA-Z0-9]*(\.[a-zA-Z][a-zA-Z0-9_]*)+$/
meta/hardcodedStringGate.ts:1188      function isKeyDefaultTupleElement(node: Node): boolean
meta/hardcodedStringGate.ts (scanSource exemption chain)  if (isKeyDefaultTupleElement(node)) { exempted += 1; return }
```

The exemption chain is ordered cheapest-first (structural → one-parent-hop → whole-file dataflow);
any new structural check belongs next to `isKeyDefaultTupleElement`, before the dataflow checks.

Every exemption in this file is a hole in the gate (T-34.8-09/10/11) and the file's own convention
is that each one is kept as narrow as its real-file source demands and is **proven by a paired
negative fixture**. Honour that convention: no widening lands without its negative.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recognise the [key, defaultText] shape in both containers — ns-prefixed tuples and {key, defaultText} objects</name>
  <read_first>
    - `meta/hardcodedStringGate.ts` :1170-1200 (`DOTTED_KEY_RE`, `isKeyDefaultTupleElement` and the
      comment above them explaining why the check is deliberately narrow)
    - `meta/hardcodedStringGate.ts` — the `scanSource` exemption chain, at the
      `isKeyDefaultTupleElement(node)` call site
    - `meta/__tests__/hardcodedStringGate.test.ts` — the `describe('D-14: [key, default] tuple
      tables')` block, including its existing `['Windows only', 'Not available on macOS']` negative
    - `src/frontend/screens/Library/facetLabels.ts` :36-41 and
      `src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` :37-210 (the real
      shapes being exempted)
  </read_first>
  <files>meta/hardcodedStringGate.ts, meta/__tests__/hardcodedStringGate.test.ts</files>
  <behavior>
    - `['gamelib:library.filterPanel.runsNatively', 'Runs natively']` → both elements exempt
    - `['Windows only', 'Not available on macOS']` → still 2 violations (the existing negative)
    - `{ ns: 'gamelib', key: 'header.uncategorized', defaultText: 'Uncategorized' }` → `key` and
      `defaultText` exempt; the `ns` value is not user-facing prose and is already handled
    - `{ ns: 'gamelib', key: 'Windows only', defaultText: 'Not available on macOS' }` → still 2
      violations (a non-key-shaped `key` does not buy the exemption)
    - `{ ns: 'gamelib', key: 'header.uncategorized', label: 'Uncategorized' }` → still 1 violation
      (no `defaultText` sibling, so no pairing exists to exempt)
  </behavior>
  <action>
    Two edits in `meta/hardcodedStringGate.ts`, both structural, both next to the existing tuple check.

    **(a) Widen `DOTTED_KEY_RE` with an optional namespace prefix.** This repo's `t()` calls use the
    i18next `namespaceSeparator: ':'` form (`gamelib:library.filterPanel.runsNatively`), and the
    existing regex only admits the bare `keySeparator: '.'` form. Add a single optional leading
    group so both spellings are admitted and nothing else is:

    `/^([a-zA-Z][a-zA-Z0-9]*:)?[a-zA-Z][a-zA-Z0-9]*(\.[a-zA-Z][a-zA-Z0-9_]*)+$/`

    The dotted tail stays MANDATORY — that is what keeps the existing negative fixture red, and it
    is why `'Windows only'` (a space, no dot) cannot pass. Extend the comment above the regex to
    say so, and to name `facetLabels.ts` as the real-file source that forced the widening.

    **(b) Add `isKeyDefaultObjectProperty(node)`** immediately above `isKeyDefaultTupleElement`,
    with the same "structural, not dataflow-traced" rationale in its docstring, naming
    `chipLabels.ts`'s `chipLabelSpec` as its real-file source. It exempts a string literal that is
    the initializer of a `key` or `defaultText` property assignment in an object literal that has
    BOTH properties, both initialized by string literals, with the `key` initializer matching
    `DOTTED_KEY_RE`. Require both siblings — an object with only one of the pair is not the idiom
    and must stay flagged.

    Wire it into the exemption chain in the same branch as the tuple check
    (`if (isKeyDefaultTupleElement(node) || isKeyDefaultObjectProperty(node))`) so the `exempted`
    counter still increments — a zero-violation file must remain distinguishable from an unscanned
    one (T-34.8-07).

    Then add fixtures to the existing `describe('D-14: [key, default] tuple tables')` block in
    `meta/__tests__/hardcodedStringGate.test.ts`, following the block's established idiom of
    inlining a realistic source string and calling `scanSource('fixture.tsx', source, EMPTY_GLOSSARY)`:
    one positive per new shape and one paired negative per new shape, exactly the five behaviours
    listed above. Mark each new fixture `REQ-41-04` in its test name so the coverage is traceable.
  </action>
  <verify>
    <automated>npx jest --selectProjects Meta --testPathPattern hardcodedStringGate --runInBand 2>&1 | tail -25</automated>
    <automated>grep -v '^\s*[/*]' meta/hardcodedStringGate.ts | grep -c "isKeyDefaultObjectProperty" | awk '{ if ($1 < 3) { print "expected >=3 code references (decl, wire, plus use), got " $1; exit 1 } else print "OK wired: " $1 " code references" }'</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --selectProjects Meta --testPathPattern hardcodedStringGate --runInBand` passes
    - At least 5 new fixtures exist (2 positive + 3 negative), each named with `REQ-41-04`
    - Comment-stripped grep proves `isKeyDefaultObjectProperty` exists in CODE and is wired into
      the exemption chain, not merely described in a comment
    - The `exempted` counter still increments for the newly-exempted literals (assert `exempted`
      is non-zero in at least one positive fixture)
    - Record the per-file audit counts after this task alone: expect `facetLabels.ts` 8 → 0 and
      `chipLabels.ts` 34 → 8 (the 8 `tGamelib` `argument` hits survive until Task 2)
  </acceptance_criteria>
  <done>facetLabels.ts audits at 0 violations and chipLabels.ts at 8; both new exemptions have paired negatives that still flag; exempted counters still increment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recognise `closest()` as a DOM API and the local `TFunc` t-alias spelling</name>
  <read_first>
    - `meta/hardcodedStringGate.ts` :688-730 (`isConfigStoreKeyArgument`, the
      `TECHNICAL_DOM_API_METHOD_NAMES` set and the docstring explaining why it is method-name-gated
      rather than content-shaped)
    - `meta/hardcodedStringGate.ts` :989-1020 (`collectTAliases`, both detection passes)
    - `src/frontend/helpers/gamepad.ts` :400-465 (the three `closest()` call sites; note that
      `querySelector('.playIcon')` in the same file is already exempt)
    - `src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` :242-300 (`TFunc` and
      `resolveLabel`)
  </read_first>
  <files>meta/hardcodedStringGate.ts, meta/__tests__/hardcodedStringGate.test.ts</files>
  <behavior>
    - `el.closest('.MuiDialog-root')` → exempt
    - `someObject.closest('Please try again')` → also exempt, and that is the ACCEPTED cost of the
      method-name gate (identical to the pre-existing `querySelector` precedent); the negative
      fixture below pins what is NOT exempted
    - `const x = '.MuiDialog-root'` at top level, not inside any call → still 1 violation (proves
      the exemption is call-gated, not content-shaped)
    - `function f(tGamelib: TFunc) { return tGamelib('gamelib:a.b', 'Hidden only') }` → exempt
    - `function f(g: Other) { return g('gamelib:a.b', 'Hidden only') }` → still 2 violations
  </behavior>
  <action>
    Two edits in `meta/hardcodedStringGate.ts`.

    **(a) Add `'closest'` to `TECHNICAL_DOM_API_METHOD_NAMES`**, with a comment recording: the
    three real call sites (`helpers/gamepad.ts:405,452,459`), that a `closest()` argument is by
    definition a CSS selector, and that this is deliberately the SAME method-name gate the set's
    existing `querySelector` entry uses rather than a `.kebab-class` content-shape regex — a
    content-shape check would exempt that shape everywhere in the 171-file scope, whereas this one
    cannot reach outside a `.closest()` call.

    **(b) Widen the t-alias type match at `:1006`** from `typeText.includes('TFunction')` to an
    anchored, word-boundary regex admitting exactly two spellings: `/\bTFunc(tion)?\b/`. Deliberately
    NOT `includes('TFunc')` — a bare substring test would also swallow `TFuncKeyPrefix` or any
    future type merely starting with those characters. Comment it with `chipLabels.ts`'s locally
    declared `export type TFunc = (key: string, defaultValue: string, options?) => string` as the
    real-file source, and note that the parameter literally named `t` in the same signature was
    already covered by `collectTAliases`'s bare-name seed — only the aliased `tGamelib` needed this.

    Add fixtures for all five behaviours above to `meta/__tests__/hardcodedStringGate.test.ts`.
    Put the `closest` ones next to the existing technical-DOM-API tests and the `TFunc` ones next
    to the existing `collectTAliases` tests, so each widening sits with the check it widens. Name
    each with `REQ-41-04`.
  </action>
  <verify>
    <automated>npx jest --selectProjects Meta --testPathPattern hardcodedStringGate --runInBand 2>&1 | tail -25</automated>
    <automated>grep -v '^\s*[/*]' meta/hardcodedStringGate.ts | grep -c "'closest'" | awk '{ if ($1 != 1) { print "expected exactly 1 code occurrence of the closest entry, got " $1; exit 1 } else print "OK closest added once" }'</automated>
    <automated>grep -v '^\s*[/*]' meta/hardcodedStringGate.ts | grep -c "includes('TFunction')" | awk '{ if ($1 != 0) { print "old TFunction substring test still present in code"; exit 1 } else print "OK old substring test replaced" }'</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --selectProjects Meta --testPathPattern hardcodedStringGate --runInBand` passes
    - At least 5 new fixtures exist covering the five behaviours, each named with `REQ-41-04`
    - The old `includes('TFunction')` test is GONE from code (raw grep is valid here: this asserts
      an ABSENCE)
    - `'closest'` appears exactly once in comment-stripped source
    - Record the per-file audit counts after this task: expect all three files at 0
  </acceptance_criteria>
  <done>All three files audit at 0 violations; the old includes('TFunction') test is gone from code; 'closest' appears exactly once in comment-stripped source.</done>
</task>

<task type="auto">
  <name>Task 3: Re-base the WR-18 ratchet from 8/35 pinned false positives to 0/0/0 proven exempt</name>
  <read_first>
    - `meta/__tests__/hardcodedStringGate.test.ts` :1462-1600 — the whole
      `describe('measured ratchet over facetLabels.ts / chipLabels.ts (WR-18, DECISION 3, quick
      260827-vpl)')` block: W1 (not in scope/allowlist), W2 (8 and 35 pinned), W3 (pinned text set),
      W4 (no collateral), and the `mkdtempSync` non-vacuity sabotage test
    - `meta/__tests__/hardcodedStringGate.test.ts` :1420-1450 (the `scannedFiles` and
      `fileExempt`/allowlist pins that must stay untouched by this plan)
  </read_first>
  <files>meta/__tests__/hardcodedStringGate.test.ts</files>
  <action>
    Tasks 1 and 2 make W2 (`expect(facet).toHaveLength(8)` / `expect(chip).toHaveLength(35)`) and
    W3 (the pinned 35-element text set) FAIL — they pin exactly the false positives this plan
    removes. **This is the cross-gate interaction to handle in-plan, not to discover.** Leaving the
    re-base for plan 41-04 would leave the Meta project red between two waves.

    Rewrite the block in place, keeping its structure and its purpose (this is still these files'
    only coverage until 41-04 promotes them):

    - **W1 — keep unchanged.** It asserts the two files are NOT in `meta/i18nGateScope.json` and
      NOT in `meta/i18nGateAllowlist.json`. That remains TRUE after this plan; plan 41-04 is what
      inverts it. Add `helpers/gamepad.ts` to the same assertion so all three are covered.
    - **W2 — re-base to zero, all three files.** `scanScope({ extraFiles: [FACET, CHIP, GAMEPAD] })`
      must report 0 violations attributed to each of the three. Record in a comment the measured
      BEFORE counts (8 / 35 / 3 = 46) and the commit that removed them, so the diff explains itself.
    - **W3 — replace the text pin with an EXEMPTED pin.** A zero-violation file is indistinguishable
      from an unscanned one unless the scanner proves it looked. Assert `scanSource` over each of
      the three real files returns `violations.length === 0` AND `exempted > 0`, and pin each
      file's `exempted` count exactly. That is the direct successor of W3's "a silent swap, same
      count different literal, still fails here" property: the exempted count moves if a literal is
      added or removed.
    - **W4 — keep unchanged**, widened to the third file: no violation attributed to any file
      outside the three.
    - **Non-vacuity — keep, and extend.** The existing sabotage test appends a bare literal to a
      scratch copy of `chipLabels.ts` and asserts the count rises by one. Keep it (it is now the
      proof that a genuine untranslated string is still caught in a file whose real literals are
      all exempt), and add the same sabotage for a scratch copy of `helpers/gamepad.ts`. Continue
      to use `mkdtempSync` + `extraFiles`; never write to the real files, never commit a scratch
      file.

    Update the block's header comment: it currently explains that the two files have "zero
    automated coverage" and that "43 violations" were never triaged. Both statements are now
    historical. Rewrite it to state what the block proves TODAY and cite REQ-41-04.
  </action>
  <verify>
    <automated>npx jest --selectProjects Meta --testPathPattern hardcodedStringGate --runInBand 2>&1 | tail -25</automated>
    <automated>grep -v '^\s*[/*]' meta/__tests__/hardcodedStringGate.test.ts | grep -cE "toHaveLength\(8\)|toHaveLength\(35\)" | awk '{ if ($1 != 0) { print "stale 8/35 false-positive pins still present in code"; exit 1 } else print "OK stale pins removed" }'</automated>
    <automated>grep -v '^\s*[/*]' meta/__tests__/hardcodedStringGate.test.ts | grep -c "gamepad" | awk '{ if ($1 < 3) { print "gamepad.ts not covered by W1/W2/W4 in code, got " $1; exit 1 } else print "OK gamepad covered: " $1 " code references" }'</automated>
  </verify>
  <acceptance_criteria>
    - The Meta project is GREEN after this task — no test anywhere pins the removed false positives
    - W1 covers all three files and still asserts they are outside the committed scope and allowlist
    - W2 asserts 0 violations for each of the three, with the BEFORE counts (8/35/3) recorded in a
      comment
    - W3 pins a non-zero `exempted` count per file, so a zero-violation file stays distinguishable
      from an unscanned one
    - The sabotage/non-vacuity test covers both `chipLabels.ts` and `helpers/gamepad.ts` scratch
      copies, and both were observed FAILING the corresponding zero-violation assertion (i.e. the
      sabotage raises the count) — record the observed deltas
    - No scratch file is committed; `git status --porcelain` names only the two files in
      `files_modified`
  </acceptance_criteria>
  <done>The Meta project is green with no surviving 8/35 false-positive pins, W2 asserts 0 per file, W3 pins non-zero exempted counts, and both sabotage tests raise the count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time lint tooling only | `hardcodedStringGate.ts` is a pure ts-morph scanner over repo-local source text, explicitly forbidden from network or git access by its own header, running on `useInMemoryFileSystem: true`. No untrusted input, no network, no auth, no user data, no runtime code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Tampering | the four widened exemptions | mitigate | Every exemption is a permanent hole in a blocking gate (the file's own T-34.8-09/10/11 framing). Each widening lands with a PAIRED NEGATIVE fixture proving the shape alone does not buy exemption: a non-dotted tuple key, a non-dotted object key, an object missing its `defaultText` sibling, a `.MuiDialog-root` literal outside any call, and a non-`TFunc` alias. The regex is anchored (`/\bTFunc(tion)?\b/`) rather than a substring test so a lookalike type name cannot slip through. |
| T-41-02-02 | Tampering | `closest()` method-name gate | accept | A same-named `closest()` on a non-DOM object would exempt its argument. This is exactly the residual risk the pre-existing `querySelector`/`getItem` entries already carry, and it is strictly narrower than the CSS-content-shape alternative the ROADMAP proposed. Rationale recorded at the code site. |
| T-41-02-03 | Elevation of privilege | gate becoming vacuous | mitigate | W3's `exempted > 0` pin and the `mkdtempSync` sabotage tests prove the scanner still LOOKS at these files and still catches a genuine bare literal. A widened gate that silently stopped scanning would fail both. |
| T-41-02-04 | Repudiation | ratchet re-base hiding real debt | mitigate | The BEFORE counts (8/35/3 = 46) and the reason each disappeared are recorded in the test block's comment and in the SUMMARY, so the re-base is auditable rather than a count that simply changed. |
| T-41-02-SC | Tampering | npm/pip/cargo installs | mitigate | **No package-manager install occurs in this plan.** No dependency added, removed or upgraded; `package.json` is not in `files_modified`. Supply-chain checkpoint not applicable. |
</threat_model>

<verification>
1. Reproduce the audit measurement explicitly and record it in the SUMMARY — this is the plan's
   central number and must not be inferred from a green suite:
   `scanScope()` → expect 171 files / 0 violations / 0 staleExemptions;
   `scanScope({ extraFiles: [facetLabels, chipLabels, gamepad] })` → expect 174 files /
   **0** violations (before: 46).
2. `npx jest --selectProjects Meta --runInBand` — whole project green. Confirm the run reports the
   expected suite count (36 at HEAD); `--selectProjects` is case-sensitive and exits 0 on a typo.
3. `pnpm codecheck` — exit 0.
4. `pnpm lint` — exit 0 within the 4157 ceiling; record the observed warning count and compare it
   against the pre-task count (this phase must not return lint debt).
5. `git status --porcelain` names only `meta/hardcodedStringGate.ts` and
   `meta/__tests__/hardcodedStringGate.test.ts`.
</verification>

<success_criteria>
- Audit-mode violations for the three files: **46 → 0**, measured and recorded
- Blocking scope unchanged: 171 files, 0 violations, 0 stale exemptions, `fileExempt` still exactly
  `['src/frontend/bootErrorSurface.ts']`
- Each of the four widenings has at least one paired negative fixture that still FLAGS
- The sabotage tests were observed raising the violation count on scratch copies of both
  `chipLabels.ts` and `gamepad.ts`
- No stale `toHaveLength(8)` / `toHaveLength(35)` false-positive pin survives anywhere
- Nothing under `src/` was edited; `meta/i18nGateScope.json` and `meta/i18nGateAllowlist.json` are
  untouched (plan 41-04 owns the scope artifact)
</success_criteria>

<output>
Create `.planning/phases/41-i18n-gate-honesty-make-the-translation-and-hardcoded-string-/41-02-SUMMARY.md`
when done. It MUST carry: the before/after audit counts (46 → 0) per file, the blocking-scope
numbers before and after, the sabotage deltas, and the two inherited-framing corrections
(gamepad.ts is a `closest()` argument case not a content-shape case; the `:323/:370/:377` line
numbers in `genI18nGateScope.test.ts`'s header are stale and should read 405/452/459 — for plan
41-04 to fix).
</output>
