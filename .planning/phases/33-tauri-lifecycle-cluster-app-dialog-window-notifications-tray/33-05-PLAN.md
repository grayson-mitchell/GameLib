---
phase: 33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray
plan: 05
type: execute
wave: 2
depends_on: ["33-01", "33-02"]
files_modified: []
autonomous: false
requirements: [REQ-33-10]
must_haves:
  truths:
    - "Clicking Install on a Steam title under npm run tauri:dev resolves the badge — succeeds or shows a clean error dialog — and NEVER hangs"
    - "The live proof is run against a sidecar rebuilt from the current tree (not a stale bundled binary)"
    - "The rehydrated-library case (library from the persisted store, connection stale by Install time) is exercised"
  artifacts:
    - path: ".planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-05-SUMMARY.md"
      provides: "Recorded live-proof outcome (pass / observed-hang gap)"
  key_links:
    - from: "live npm run tauri:dev install click"
      to: "terminal badge state (done/error, never installing-forever)"
      via: "G-30-02 fix (Plans 33-01 + 33-02)"
      pattern: "N/A (human-observed)"
---

<objective>
The load-bearing D-13 phase-close gate: prove the G-30-02 install-hang fix LIVE on real hardware
under `npm run tauri:dev`, because jest was provably green while the live build hung TWICE (30-05,
30-07). This bug class only reproduces against a real, stale sidecar CM socket that mocks cannot
recreate — so unit tests are necessary but NOT sufficient to close G-30-02.

Purpose: Plans 33-01 (badge-clear + watchdog) and 33-02 (relog revalidation) implement the fix and
are unit-proven; this checkpoint is the distinct, additional live verification D-13 mandates before
the phase can close. The rest of the cluster (dialog/app/notification/shell) may stay unit-proven +
live-UAT-deferred — this gate is G-30-02-specific.

Output: A recorded live-proof outcome. A PASS closes the D-13 gate; an observed hang opens a gap
(`/gsd-plan-phase 33 --gaps`) rather than declaring the phase done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-CONTEXT.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-RESEARCH.md
@.planning/debug/steam-install-spinner-hangs-tauri-live-g3002.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-01-PLAN.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: LIVE hardware proof of the G-30-02 install-hang fix (D-13)</name>
  <action>Manual live verification (no autonomous work): rebuild the sidecar from the current tree, run `npm run tauri:dev` with a signed-in, store-rehydrated Steam library and `enableSteamNativeInstall:true`, click Install on a Steam title, and confirm the 'installing' badge resolves (success or a clean failure dialog) and NEVER hangs. This is the D-13 phase-close gate — distinct from and additional to the Plan 33-01/33-02 unit tests.</action>
  <read_first>
    - .planning/debug/steam-install-spinner-hangs-tauri-live-g3002.md (the reproducing condition + the two prior false-fixes)
    - .planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-01-PLAN.md (badge-clear + watchdog fix under test)
    - .planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-02-PLAN.md (relog/canary CM revalidation under test)
  </read_first>
  <acceptance_criteria>
    - Under `npm run tauri:dev` (sidecar rebuilt from the current tree), clicking Install on a Steam title resolves the "installing" badge — success OR a clean failure dialog — and NEVER shows an indefinite spinner.
    - The rehydrated-library case (library from the persisted store, CM connection stale by Install time) ultimately SUCCEEDS on retry, not merely fails fast (D-02 fixed constraint).
    - The sidecar under test is confirmed rebuilt from the current tree (not a stale bundled binary) before the click.
    - Outcome (PASS: succeeded / clean-error+retry-succeeded, or a described hang gap) is recorded in 33-05-SUMMARY.md; a still-hanging build is NOT approved.
  </acceptance_criteria>
  <what-built>
    Plans 33-01 and 33-02 hardened the shared install path: the `installQueueElement` finally-guard
    now clears the "installing" badge on a Steam `status === 'error'` and shows a failure dialog
    (WR-01/D-10, D-03), a watchdog bounds the whole `.install()` await (D-01b), and
    `ensureConnected()` now revalidates a stale CM socket via a bounded canary + `client.relog()`
    (D-02) so a rehydrated-library install reconnects and proceeds instead of parking.
  </what-built>
  <how-to-verify>
    1. Rebuild the sidecar from the CURRENT tree (do NOT test a stale bundled/pre-fix sidecar binary
       — a stale binary reproduces the exact "should be fixed but isn't" symptom; 33-RESEARCH open
       item). Confirm the build picks up the just-changed `downloadmanager/utils.ts` + `user.ts`.
    2. Launch `npm run tauri:dev` with a signed-in Steam account whose library was rehydrated from
       the persisted store (so the CM connection is plausibly stale by Install time — the exact
       reproducing condition). Ensure `enableSteamNativeInstall: true`.
    3. Click Install on a Steam title.
    4. Observe the "installing" badge. EXPECTED: it RESOLVES — either the install proceeds/succeeds,
       OR a clean failure dialog appears (e.g. "Steam connection stale, try again") and the badge
       clears. REQUIRED: it must NEVER hang indefinitely (the two prior live retests hung forever).
    5. If a failure dialog appears, click Install again (relog should now have revalidated the
       socket) and confirm the install proceeds — the rehydrated case must ultimately SUCCEED, not
       merely fail fast (D-02 fixed constraint).
    6. (Diagnose-only, if it still hangs) capture which branch ran (`installNative` vs
       `installBottleNative`) and whether the watchdog log fired — feed into a gap plan.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the badge resolved and never hung (record whether it succeeded outright or via
    a clean error dialog + retry). Otherwise describe the observed behavior (hang / which branch /
    whether the watchdog fired) so a `/gsd-plan-phase 33 --gaps` cycle can open — do NOT approve a
    still-hanging build.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unit-green build ↔ live reality | the D-13 gate exists because green mocks masked a live hang twice; the checkpoint is the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-14 | Repudiation | phase declared "fixed" on unit-green alone (the exact trap that mis-closed G-30-02 twice) | mitigate | D-13 makes live proof a blocking checkpoint distinct from unit tests; a still-hanging build cannot be approved |
| T-33-15 | Tampering | live test run against a stale bundled sidecar binary (false negative/positive) | mitigate | Verification step 1 mandates rebuilding the sidecar from the current tree before the live click |
| T-33-SC | Tampering | npm/cargo installs | mitigate | No package installs in this checkpoint plan; N/A |
</threat_model>

<verification>
- Human confirms the badge resolves and never hangs under `npm run tauri:dev` (rebuilt sidecar).
- Outcome recorded in 33-05-SUMMARY.md: PASS (succeeded / clean-error+retry-succeeded) or a described gap.
</verification>

<success_criteria>
- The G-30-02 install-hang is proven fixed on live hardware — the D-13 phase-close gate is satisfied.
- A rehydrated-library install ultimately succeeds; no infinite spinner under any branch observed.
</success_criteria>

<output>
Create `.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-05-SUMMARY.md` when done, recording the live outcome and D-13 gate status.
</output>
