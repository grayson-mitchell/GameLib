---
phase: 33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray
plan: 06
type: execute
wave: 3
depends_on: ["33-01", "33-02", "33-03", "33-04"]
files_modified:
  - .planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-PORTED-CHANNELS.md
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
autonomous: true
requirements: [REQ-33-04, REQ-33-09, REQ-33-11]
must_haves:
  truths:
    - "Every channel ported this phase is enumerated in 33-PORTED-CHANNELS.md (declared, not discovered)"
    - "Every logged no-op / re-deferral this phase makes is named explicitly (session, powerSaveBlocker, the two Sync dialog members, WR-02 fan-out, tray/protocol/multi-window/nativeImage/updater)"
    - "SEAM.md §1 lists the newly-ported rows; §3's deferred table no longer lists them"
  artifacts:
    - path: ".planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-PORTED-CHANNELS.md"
      provides: "The declared ported-channel + logged-no-op boundary for Phase 33"
      contains: "notification_show"
    - path: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      provides: "§1/§3 updated: ported rows moved out of the deferred backlog"
      contains: "33"
  key_links:
    - from: "33-PORTED-CHANNELS.md"
      to: "SEAM.md §1"
      via: "ported rows moved out of §3 deferred table into §1"
      pattern: "dialog_message|notification_show|app_exit"
---

<objective>
Declare the Phase 33 boundary so it is documented, not discovered (checklist steps 5/6): ship
`33-PORTED-CHANNELS.md` enumerating every channel given real behavior this phase AND every logged
no-op / re-deferral, and update `SEAM.md` §1/§3 to move the ported rows out of the deferred
backlog. Formalize the WR-02 re-scope declaration (D-11).

Purpose: Every prior slice (30/31/32) shipped a `NN-PORTED-CHANNELS.md`; this closes the same
loop. Re-deferring tray/protocol/multi-window/nativeImage/updater to Phase 34/35 is legitimate
only because it is declared and Electron still runs byte-identically (additive/reversible
invariant).

Output: The declared-boundary artifact + SEAM update. Wave 3 — depends on the ported work
(33-01..33-04) existing to declare it accurately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-CONTEXT.md
@.planning/phases/32-tauri-ipc-replumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md
@.planning/phases/31-tauri-ipc-replumb-slice-2-settings-and-config/31-PORTED-CHANNELS.md
@.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-01-PLAN.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-03-PLAN.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 33-PORTED-CHANNELS.md (declared boundary)</name>
  <files>.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/32-tauri-ipc-replumb-slice-3-downloads-and-queue/32-PORTED-CHANNELS.md (the format to mirror)
    - .planning/phases/31-tauri-ipc-replumb-slice-2-settings-and-config/31-PORTED-CHANNELS.md (the Sync-pair / logged-no-op declaration style)
    - The 33-01..33-04 SUMMARYs (source of truth for what actually landed — especially whether trashItem shipped real or as a logged no-op)
  </read_first>
  <action>
    Create `33-PORTED-CHANNELS.md` mirroring `32-PORTED-CHANNELS.md`. Enumerate PORTED (real
    behavior): `dialog_message` extended to real multi-button `showMessageBox` (with the D-07
    cancelId fail-safe), `notification_show` / `Notification`, `app_exit`/`app_relaunch` /
    `app.quit`/`exit`/`relaunch`, and the real `shell` methods (`showItemInFolder`/`openPath`, and
    `trashItem` — state real-vs-logged-no-op per the 33-04 SUMMARY). Enumerate LOGGED NO-OPS /
    RE-DEFERRALS explicitly: `session` and `powerSaveBlocker` (D-08/D-09 accept-and-document, Phase
    35 revisit); the two Sync dialog members `showMessageBoxSync`/`showOpenDialogSync` (D-06);
    WR-02's non-Steam DLC fan-out re-scope (D-11 — Steam-focused install path, guarded/logged not
    silent); boot-time auto-resume of interrupted installs (`initQueue(isStartup=true)`) which stays
    deferred to Phase 35 per Phase 32 D-05 / this phase's D-04 (untouched, not re-examined here); and
    the Phase-34/35 re-deferrals: tray, custom-protocol registration, full multi-window
    (`BrowserWindow`), `nativeImage`, updater hooks. Note the G-30-02 install-hang fix and its D-13
    live-proof gate status (reference Plan 33-05's outcome). Cross-reference REQ-33-xx IDs.
  </action>
  <acceptance_criteria>
    - File exists and enumerates PORTED and LOGGED-NO-OP/RE-DEFERRED sections.
    - `grep -n "notification_show\|app_exit\|dialog_message\|session\|powerSaveBlocker\|nativeImage\|updater\|WR-02" 33-PORTED-CHANNELS.md` (in the phase dir) hits all of these terms.
    - trashItem's real-vs-logged status matches the 33-04 SUMMARY.
    - Sync dialog members and the Phase 34/35 re-deferrals are named explicitly.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-PORTED-CHANNELS.md && grep -c "notification_show\|app_exit\|session\|powerSaveBlocker\|WR-02\|nativeImage" .planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-PORTED-CHANNELS.md</automated>
  </verify>
  <done>The Phase 33 ported/logged-no-op/re-deferred boundary is fully declared in one artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Update SEAM.md §1/§3 — move ported rows out of the deferred backlog</name>
  <files>.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</files>
  <read_first>
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md (§1 ported list, §3 the ranked deferred backlog with the `app`/`dialog`/`shell`/`Notification`/`session`/`powerSaveBlocker` priority rows, the Incremental-Port Checklist, the Load-Bearing Invariants and Accepted Constraints)
    - The 33-PORTED-CHANNELS.md just authored (source of the moved rows)
  </read_first>
  <action>
    In `SEAM.md`: move the rows this phase ported (real `showMessageBox` multi-button, `Notification`,
    the remaining `shell` methods, `app` lifecycle essentials) OUT of §3's deferred/ranked backlog and
    INTO §1's ported section, citing Phase 33. Retire/annotate the corresponding priority rows. Record
    as Accepted Constraints: `session`/`powerSaveBlocker` (D-08/D-09 accepted parity gaps, Phase 35
    revisit) and the WR-02 non-Steam DLC re-scope (D-11). Leave the still-deferred rows (tray,
    protocol, full multi-window, `nativeImage`, updater) in §3, annotated with their target phase
    (34/35). Do not alter the Load-Bearing Invariants A/B text. Keep §3 accurate: only genuinely
    still-deferred items remain there.
  </action>
  <acceptance_criteria>
    - `grep -n "Phase 33\|33-" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` shows the newly-ported rows attributed to Phase 33 in §1.
    - The ported members (`showMessageBox` multi-button / `Notification` / `shell` methods / `app` lifecycle) are no longer listed as deferred in §3.
    - `session`/`powerSaveBlocker` and WR-02 are recorded as Accepted Constraints; tray/protocol/multi-window/nativeImage/updater remain deferred with target-phase annotations.
    - Load-Bearing Invariants A/B text is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Phase 33\|powerSaveBlocker\|nativeImage" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md | head -20</automated>
  </verify>
  <done>SEAM §1/§3 reflect the Phase 33 ports; only genuinely-deferred rows remain in the backlog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared boundary ↔ actual code | a mis-declared ported list hides a silent gap (the exact failure the declared-channel-list discipline prevents) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-16 | Repudiation | a logged no-op (e.g. trashItem, session) undeclared, later mistaken for a silent regression | mitigate | 33-PORTED-CHANNELS.md enumerates every logged no-op explicitly; SEAM records Accepted Constraints |
| T-33-17 | Information disclosure | WR-02 non-Steam DLC drop left undeclared | mitigate | D-11 re-scope declared in 33-PORTED-CHANNELS.md and SEAM Accepted Constraints (paired with the code guard in Plan 33-01) |
| T-33-SC | Tampering | npm/cargo installs | mitigate | Documentation-only plan; no package installs; N/A |
</threat_model>

<verification>
- `33-PORTED-CHANNELS.md` exists and enumerates ported + logged-no-op/re-deferred channels.
- SEAM §1/§3 updated; ported rows moved, deferred rows annotated with target phase.
- No code files touched (documentation-only) — both builds remain byte-identical (REQ-33-11).
</verification>

<success_criteria>
- The Phase 33 boundary is declared, not discovered; SEAM reflects the ports; WR-02 re-scope is on record.
</success_criteria>

<output>
Create `.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-06-SUMMARY.md` when done.
</output>
