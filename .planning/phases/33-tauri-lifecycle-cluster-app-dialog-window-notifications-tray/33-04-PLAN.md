---
phase: 33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray
plan: 04
type: execute
wave: 2
depends_on: ["33-03"]
files_modified:
  - src-tauri/Cargo.toml
  - package.json
  - src-tauri/src/main.rs
  - src/common/types/sidecarTransport.ts
  - src/backend/sidecar/electronStub.ts
  - src/backend/sidecar/__tests__/lifecycleStub.test.ts
autonomous: true
requirements: [REQ-33-06, REQ-33-07, REQ-33-08, REQ-33-11]
user_setup: []
must_haves:
  truths:
    - "Notification.isSupported() returns true and Notification.show() delivers a real OS notification via tauri-plugin-notification"
    - "shell.showItemInFolder and shell.openPath perform real reveal/open via tauri-plugin-opener"
    - "shell.trashItem either really trashes via tauri-plugin-fs, or is a LOGGED no-op declared in 33-PORTED-CHANNELS.md (never silent)"
    - "app.quit/exit/relaunch forward a real exit/relaunch request to Tauri's AppHandle so the process actually exits (no zombie sidecar)"
    - "session and powerSaveBlocker remain no-ops that LOG (never silent)"
  artifacts:
    - path: "src/backend/sidecar/electronStub.ts"
      provides: "real Notification/shell/app forwards + logged session/powerSaveBlocker no-ops"
      contains: "RUST_NOTIFICATION_SHOW"
    - path: "src-tauri/src/main.rs"
      provides: "notification_show arm + app_exit/app_relaunch arms; notification plugin registered"
      contains: "notification_show"
    - path: "src/common/types/sidecarTransport.ts"
      provides: "new channel consts added to RUST_INVOKE_CHANNELS allowlist"
      contains: "RUST_NOTIFICATION_SHOW"
  key_links:
    - from: "src/backend/sidecar/electronStub.ts (Notification.show)"
      to: "RUST_NOTIFICATION_SHOW arm"
      via: "requestRustInvoke"
      pattern: "requestRustInvoke\\(RUST_NOTIFICATION_SHOW"
    - from: "src/backend/sidecar/electronStub.ts (app.exit/relaunch)"
      to: "RUST_APP_EXIT / RUST_APP_RELAUNCH arms → AppHandle"
      via: "requestRustInvoke"
      pattern: "RUST_APP_(EXIT|RELAUNCH)"
---

<objective>
Port the cheap medium-value cluster members to real Tauri behavior (D-05): `Notification` (Tauri
notification plugin), the remaining `shell` methods (`showItemInFolder`/`trashItem`/`openPath`),
and `app` lifecycle essentials (`quit`/`exit`/`relaunch` → Tauri `AppHandle`). Also make the two
accepted parity gaps explicit: `session`/`powerSaveBlocker` stay no-ops but LOG (D-08/D-09).

Purpose: These are mechanical wins following the 3-phase-proven `electronStub` → `requestRustInvoke`
→ `dispatch_rust_channel` pattern. Re-deferred cluster members (tray/protocol/multi-window/
nativeImage/updater) stay non-fatal per Invariant B; `nativeImage` stays out because the Tauri
notification plugin's icon param is optional (33-RESEARCH verified).

Output: Real Notification/shell/app forwards, logged parity gaps, new notification plugin
dependency. Wave 2 — shares electronStub.ts/main.rs/sidecarTransport.ts with Plan 33-03 (wave 1),
so it runs after.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-CONTEXT.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-RESEARCH.md
@.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-PATTERNS.md

<interfaces>
<!-- Extracted by 33-PATTERNS.md/33-RESEARCH.md — use directly. -->

New channel consts (add to RUST_INVOKE_CHANNELS allowlist) — src/common/types/sidecarTransport.ts (RUST_DIALOG_* const block L151-183):
  RUST_NOTIFICATION_SHOW, RUST_APP_EXIT, RUST_APP_RELAUNCH. requestRustInvoke pre-rejects any channel
  NOT in RUST_INVOKE_CHANNELS (T-28-03 allowlist, sidecarRpc.ts L286-288) — forgetting this breaks the forward.

electronStub members to upgrade — src/backend/sidecar/electronStub.ts:
  - Notification class (L281-291): isSupported() currently returns false; show()/close() no-op. Flip isSupported()→true; show() forwards title/body to RUST_NOTIFICATION_SHOW mirroring showErrorBox's try/catch-around-requestRustInvoke shape (L190-203). No icon/nativeImage.
  - shell (L313-327): openExternal already transport-bound (L313-316). showItemInFolder/openPath → tauri-plugin-opener (already installed); trashItem → tauri-plugin-fs OR logged no-op.
  - app (L137-152): quit/exit/relaunch no-ops → forward to RUST_APP_EXIT / RUST_APP_RELAUNCH (fire-and-forget, mirror shell.openExternal's `transport?.openExternal(url)` shape — void return acceptable).
  - session / powerSaveBlocker: keep no-ops but add a logInfo/console.warn (logged-no-op discipline, matches shell.showItemInFolder's Phase 31 "upgraded from silent to logged" precedent).

Rust arms to add — src-tauri/src/main.rs dispatch_rust_channel (mirror dialog_message's args-parse idiom):
  - notification_show: call tauri-plugin-notification builder (title/body). Register plugin in the Tauri builder (`.plugin(tauri_plugin_notification::init())`).
  - app_exit / app_relaunch: call AppHandle exit/relaunch. (frontend_message relay unchanged.)

Zero-change consumer: src/backend/dialog/dialog.ts `notify()` (L61-79) already gates on Notification.isSupported() with a logged fallback — no change needed once isSupported()→true.

New dependencies (33-RESEARCH Package Legitimacy Audit — all first-party tauri-apps, Approved):
  - Cargo: `tauri-plugin-notification = "2"` (matches existing `tauri-plugin-dialog = "2"` pinning).
  - npm: `@tauri-apps/plugin-notification` ^2.3.3.
  - Cargo (only if trashItem wired): `tauri-plugin-fs = "2"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the notification plugin + new channel consts + real Notification (D-05)</name>
  <files>src-tauri/Cargo.toml, package.json, src-tauri/src/main.rs, src/common/types/sidecarTransport.ts, src/backend/sidecar/electronStub.ts</files>
  <read_first>
    - src-tauri/Cargo.toml (existing `tauri-plugin-dialog = "2"` / `tauri-plugin-opener` entries + pinning convention)
    - package.json (existing `@tauri-apps/plugin-opener` entry)
    - src-tauri/src/main.rs (the Tauri builder `.plugin(...)` chain + `dispatch_rust_channel` `dialog_message` arm to mirror)
    - src/common/types/sidecarTransport.ts (RUST_DIALOG_* const block L151-183 + RUST_INVOKE_CHANNELS allowlist array)
    - src/backend/sidecar/electronStub.ts (Notification class L281-291 + showErrorBox L190-203 forward shape)
    - 33-RESEARCH.md § "Notification (D-05)" + § "Package Legitimacy Audit"
  </read_first>
  <action>
    Add `tauri-plugin-notification = "2"` to `src-tauri/Cargo.toml` and `@tauri-apps/plugin-notification`
    (^2.3.3) to `package.json`; install (the first-party official crate/package — confirm it is the
    singular `notification`, NOT the third-party plural `notifications` fork per 33-RESEARCH). Register
    the plugin in the Tauri builder in `main.rs` (`.plugin(tauri_plugin_notification::init())`
    alongside the existing dialog/opener plugin registrations). Add a `notification_show` arm to
    `dispatch_rust_channel` mirroring `dialog_message`'s args-parse idiom (read `title`/`body`, call the
    notification builder, return `Ok(Value::Null)` or a bool). Add `RUST_NOTIFICATION_SHOW` to
    `sidecarTransport.ts`'s const block AND to the `RUST_INVOKE_CHANNELS` allowlist. In
    `electronStub.ts`, flip `Notification.isSupported()` to `true` and implement `show()` to forward
    `{title, body}` via `requestRustInvoke(RUST_NOTIFICATION_SHOW, [...])` inside a try/catch that
    `console.warn`s and no-ops on failure (never throws — total-method). No icon/nativeImage plumbing.
  </action>
  <acceptance_criteria>
    - `grep -n "tauri-plugin-notification" src-tauri/Cargo.toml` and `grep -n "@tauri-apps/plugin-notification" package.json` both hit.
    - `grep -n "notification_show\|tauri_plugin_notification::init" src-tauri/src/main.rs` shows the arm and the plugin registration.
    - `grep -n "RUST_NOTIFICATION_SHOW" src/common/types/sidecarTransport.ts` shows the const AND its inclusion in RUST_INVOKE_CHANNELS.
    - `Notification.isSupported()` returns `true`; `show()` forwards via requestRustInvoke and never throws.
    - `cargo build --manifest-path src-tauri/Cargo.toml` compiles; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && npx tsc --noEmit && grep -n "RUST_NOTIFICATION_SHOW" src/common/types/sidecarTransport.ts</automated>
  </verify>
  <done>Real OS notifications are delivered via the first-party notification plugin; the new channel is allowlisted; both builds compile.</done>
</task>

<task type="auto">
  <name>Task 2: Real shell methods + app lifecycle forward + logged session/powerSaveBlocker (D-05/D-08/D-09)</name>
  <files>src-tauri/src/main.rs, src/common/types/sidecarTransport.ts, src/backend/sidecar/electronStub.ts, src-tauri/Cargo.toml</files>
  <read_first>
    - src/backend/sidecar/electronStub.ts (shell L313-327, app L137-152, session/powerSaveBlocker members, showItemInFolder's existing "logged no-op" precedent)
    - src-tauri/src/main.rs (dispatch_rust_channel arms + AppHandle access + existing tauri-plugin-opener usage)
    - 33-RESEARCH.md § "Shell Cluster (D-05)" + "App Lifecycle Essentials (D-05)" + "Parity Gaps (D-08/D-09)"
    - 33-PATTERNS.md § "shell remaining methods" + "app.quit/exit/relaunch" + "Logged no-op discipline"
  </read_first>
  <action>
    Shell: back `shell.showItemInFolder` and `shell.openPath` onto `tauri-plugin-opener` (already
    installed) — either via its existing reveal/open command or a new small `dispatch_rust_channel`
    arm; forward from electronStub with the try/catch total-method shape. For `shell.trashItem`:
    wire it to `tauri-plugin-fs`'s trash capability (add `tauri-plugin-fs = "2"` to Cargo.toml + a
    `dispatch_rust_channel` arm + plugin registration) IF the v2 trash permission is straightforward;
    otherwise, if the fs-plugin trash capability is unavailable/non-trivial in v2, make `trashItem` a
    LOGGED no-op (`console.warn`, never silent) and record it as a declared logged-no-op for Plan 06's
    `33-PORTED-CHANNELS.md` — do NOT leave it a silent no-op either way. App lifecycle: add
    `RUST_APP_EXIT`/`RUST_APP_RELAUNCH` consts (const block + RUST_INVOKE_CHANNELS allowlist) and
    `app_exit`/`app_relaunch` arms calling Tauri's `AppHandle` exit/relaunch; forward
    `electronStub.app.quit`/`exit`/`relaunch` to them (fire-and-forget, mirror `shell.openExternal`'s
    void forwarding shape). Parity gaps: leave `session`/`powerSaveBlocker` as no-ops but add a
    `logInfo`/`console.warn` to each (logged-no-op discipline) noting the accepted gap and the Phase 35
    revisit. New lifecycle channels must not read/write `TOKEN_STORE_KEY`/secrets (Phase 28 D-04).
  </action>
  <acceptance_criteria>
    - `grep -n "RUST_APP_EXIT\|RUST_APP_RELAUNCH" src/common/types/sidecarTransport.ts src-tauri/src/main.rs` shows consts, allowlist entries, and arms.
    - `shell.showItemInFolder`/`openPath` forward for real (no bare logged-no-op remaining for these two); `trashItem` is either real (tauri-plugin-fs wired) or a LOGGED no-op — never silent.
    - `app.quit`/`exit`/`relaunch` forward via requestRustInvoke to the app_exit/app_relaunch arms.
    - `session`/`powerSaveBlocker` each emit a log line (grep shows the added log).
    - No new lifecycle channel touches `TOKEN_STORE_KEY`/`SECRET_STORE_KEYS`.
    - `cargo build --manifest-path src-tauri/Cargo.toml` compiles; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && npx tsc --noEmit && grep -n "RUST_APP_EXIT\|RUST_APP_RELAUNCH" src/common/types/sidecarTransport.ts</automated>
  </verify>
  <done>shell reveal/open/trash and app exit/relaunch are real (or trashItem a declared logged no-op); session/powerSaveBlocker log their accepted gaps.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for the cluster forwards + logged no-ops</name>
  <files>src/backend/sidecar/__tests__/lifecycleStub.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/dialogStub.test.ts (the `requestRustInvoke` mock strategy to reuse)
    - src/backend/sidecar/__tests__/skeletonFlows.test.ts (new-channel test shape)
    - 33-PATTERNS.md § "Total-method / never-throw convention" + "Logged no-op discipline"
  </read_first>
  <action>
    Create `lifecycleStub.test.ts` asserting: `Notification.isSupported()` is `true` and `show()`
    forwards to `RUST_NOTIFICATION_SHOW` and does not throw on transport rejection; `shell.showItemInFolder`/`openPath`
    forward (and do not throw on rejection); `shell.trashItem` either forwards or logs (assert it does
    not throw and, if a no-op, that it logs); `app.exit`/`relaunch` forward to the app channels;
    `session`/`powerSaveBlocker` members log (spy on the logger/console.warn). Reuse the existing
    `requestRustInvoke` mock strategy. Confirm no member rejects/throws (total-method).
  </action>
  <acceptance_criteria>
    - `npx jest src/backend/sidecar/__tests__/lifecycleStub.test.ts` passes.
    - Tests cover Notification, shell (3 methods), app (exit/relaunch), and the session/powerSaveBlocker log lines.
    - A test asserts each member resolves (never throws) on transport rejection.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/lifecycleStub.test.ts</automated>
  </verify>
  <done>Cluster forwards and logged parity gaps are unit-proven; no member throws.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new npm/cargo plugin deps ↔ build | supply-chain surface for `@tauri-apps/plugin-notification` / `tauri-plugin-notification` / (opt) `tauri-plugin-fs` |
| sidecar app.exit/relaunch ↔ Tauri process | a forward that exits the real user-visible process |
| shell.openPath / trashItem ↔ filesystem | path argument crosses into an OS reveal/open/trash operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Tampering (supply chain) | new notification/fs plugin deps | mitigate | All first-party `tauri-apps/plugins-workspace` crates/packages, same org as already-trusted `tauri-plugin-dialog`/`opener`; 33-RESEARCH Package Legitimacy Audit = Approved; confirm singular `notification` (not the plural fork) |
| T-33-11 | Elevation of privilege | shell.openPath / trashItem path injection | mitigate | Paths originate from in-app backend callers; forwarded to the vetted Tauri opener/fs plugin which applies its own capability scoping, not a raw shell-out |
| T-33-12 | Denial of Service | app_exit triggered unexpectedly kills the app | accept | Only two sidecar-reachable call sites (resetHeroic, handleExit) invoke these; both are deliberate user/exit actions |
| T-33-13 | Information disclosure | new lifecycle channel writes secrets to configStore | mitigate | Channels are fire-and-forget process/notification ops; none read/write `TOKEN_STORE_KEY`/`SECRET_STORE_KEYS` (Phase 28 D-04 fail-closed) |
| T-33-SC | Tampering | npm/cargo installs | mitigate | See T-33-10; installs limited to audited first-party plugins; no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages |
</threat_model>

<verification>
- `cargo build --manifest-path src-tauri/Cargo.toml` compiles.
- `npx tsc --noEmit` passes.
- `npx jest src/backend/sidecar/__tests__/lifecycleStub.test.ts` and `npx jest electronUntouched` pass.
- `grep -rn "from 'electron'" src/backend/sidecar/electronStub.ts` returns nothing (curated-import discipline).
- New channels appear in `RUST_INVOKE_CHANNELS` (else requestRustInvoke pre-rejects).
</verification>

<success_criteria>
- Notification/shell/app forwards deliver real behavior; trashItem is real or a declared logged no-op.
- session/powerSaveBlocker log their accepted gaps.
- Both builds still work; no Electron behavior change; no secrets written by new channels.
</success_criteria>

<output>
Create `.planning/phases/33-tauri-lifecycle-cluster-app-dialog-window-notifications-tray/33-04-SUMMARY.md` when done.
</output>
