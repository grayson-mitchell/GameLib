---
phase: 38-deferred-hardware-and-environment-uat-gates-windows-linux-ma
plan: 04
type: execute
wave: 3
depends_on: ["38-01", "38-03"]
files_modified:
  - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md
  - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
autonomous: false
requirements: []   # NONE - collection phase, no requirement IDs exist for Phase 38.

must_haves:
  truths:
    - "D-38-12: two installs, one scoring artifact. A local `tauri build` NSIS installer is smoke-launched FIRST for cheap feedback on installer bugs, and 38-W04 is then scored ONLY on the CI artifact from a workflow_dispatch run of release-tauri.yml."
    - "D-38-13: the dispatch run targets the branch tip after the 4 outstanding commits are pulled, and the ledger evidence names a commit sha that is still the branch tip at the time it is written."
    - "D-38-14: no `v*` tag is pushed. The tag path co-triggers draft-release-mac.yml and draft-release-linux.yml and opens a draft GitHub Release - that is Phase 34-07's deferred live gate, not Phase 38's work."
    - "D-38-16: 38-W04 discharges only with BOTH positive evidence the code path executed (the run id, the artifact digest, the installed exe path, the surviving process) AND the operator's PASS/FAIL, in a dated session block in 38-HUMAN-UAT.md."
    - "38-W04 is MOVED from human_verification to human_verification_discharged - never annotated in place, because audit-uat counts array membership and ignores any result field."
    - "The scope reduction is not compounded: 38-W04's reduction_note records that it is ALREADY a recorded scope reduction against D-16, so scoring it on a local build alone is not acceptable."
  artifacts:
    - path: ".planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md"
      provides: "Dated session block for 38-W04 with the run id, artifact digest and operator verdict"
      contains: "38-W04"
    - path: ".planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md"
      provides: "38-W04 moved into human_verification_discharged with a result"
      contains: "human_verification_discharged"
  key_links:
    - from: ".github/workflows/release-tauri.yml"
      to: "the NSIS installer artifact under gate"
      via: "workflow_dispatch run against the branch tip"
      pattern: "workflow_dispatch"
    - from: "38-HUMAN-UAT.md session block"
      to: "38-VERIFICATION.md human_verification_discharged"
      via: "the discharge move, made only after both evidence halves exist"
      pattern: "38-W04"
---

<objective>
Discharge `38-W04` — smoke-launch the CI-produced Windows NSIS installer — against a CI artifact
whose provenance is a `workflow_dispatch` run of `release-tauri.yml` on the branch tip, after a
cheap local build has already shaken out installer bugs.

Purpose: `38-W04` is a RECORDED SCOPE REDUCTION against D-16, not a routine deferral. D-16 required
"the CI matrix producing installable artifacts PLUS a smoke launch"; Phase 35 closed on artifact
PRODUCTION alone, and REQUIREMENTS.md REQ-35-20 carries the acknowledgment. Scoring this item on a
locally-built installer would satisfy the smoke half while abandoning the CI-provenance half D-16
actually names - a second reduction on top of the first. The `release-tauri.yml` header
self-declares the pipeline UNPROVEN LIVE; nothing, human or CI, has ever executed its NSIS output.

Output: one discharged item, backed by a run id, an artifact digest and an operator verdict.

**NO SOURCE FILE IS MODIFIED.** A defect this run uncovers is recorded and filed as a todo; Phase 38
does not repair what it observes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-CONTEXT.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md

<the_item>
`38-W04`, verbatim from `38-VERIFICATION.md`:

- **test:** "Smoke-launch the CI-produced WINDOWS installer artifact. Download the NSIS installer
  from the `release-tauri.yml` workflow run for the commit under gate, install it, and launch the
  resulting GameLib.exe."
- **expected:** "The installer completes without error. GameLib launches, a window appears, and the
  process survives at least 10 seconds without crashing — the same bar `35-LIVE-GATE.md` criterion 1
  applies to the macOS artifact."
- **platform_gate:** `src-tauri/tauri.conf.json` `bundle.targets` includes `nsis` — an `.exe`/NSIS
  installer cannot execute on macOS at all; a binary-format boundary, not an unreached code branch.
- **origin:** Phase 35, `35-19` Task 2, option-c.
</the_item>

<workflow_facts>
Measured at plan time from `.github/workflows/release-tauri.yml`:

- `on:` carries BOTH `push: tags: v*` (line ~40) AND `workflow_dispatch` (line ~42). The dispatch
  path is what this plan uses.
- The header comment self-declares the pipeline UNPROVEN LIVE.
- The last step is `tauri-apps/tauri-action@v1` (~line 429), which uploads the NSIS installer to a
  draft release. It performs NO runtime check of its own - the artifact has never been executed by
  anything since the Tauri rearchitecture began (`35-PREFLIGHT.md` OQ-4).
- `src-tauri/tauri.conf.json:31` sets `"targets": ["nsis", "appimage", "dmg"]`.

D-38-14 is a HARD PROHIBITION, not a preference: pushing a `v*` tag co-triggers
`draft-release-mac.yml` and `draft-release-linux.yml` and opens a draft GitHub Release. That is
Phase 34-07's deferred live gate. Use `workflow_dispatch` only.
</workflow_facts>

<deferred_not_folded>
Recorded so it is not re-discovered mid-run:

- The dispatch run also produces an AppImage. RETAIN it for the Phase 42 Linux sitting (`38-W05`)
  rather than spending a second CI cycle. That is Phase 42's concern - note the artifact url and
  digest here, do not attempt to score `38-W05`, which no longer belongs to this phase.
- Whether a green dispatch run says anything about Phase 34-07's deferred live gate, or about
  REQ-35-20 as a whole, is 34-07's close to claim. Not folded. Do not write a closure claim for
  either into this phase's record.
</deferred_not_folded>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local NSIS smoke build, then dispatch release-tauri.yml against the branch tip</name>
  <files>.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md</files>
  <read_first>
    - `.github/workflows/release-tauri.yml` in full - the `on:` block, the Windows job's steps, the `tauri-apps/tauri-action@v1` step near line 429, and any input parameters `workflow_dispatch` declares. You must know what inputs the dispatch expects before invoking it.
    - `src-tauri/tauri.conf.json` - `bundle.targets` at line ~31, plus the `productName`/`identifier`/`version` fields, so you can recognise the installer filename the build emits.
    - `package.json` scripts - `tauri:dev` and `tauri:dev:packaged` (`tauri build --debug`). Note that a `--debug` build is NOT what this task needs: Phase 35's F-5-01 records that `tauri build --debug` structurally never executes the SEA sidecar, so a debug artifact is an inert stand-in. Build a RELEASE NSIS installer.
    - `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md` - the HEAD sha recorded by plan 38-03. The dispatch must target that sha, and it must still be the branch tip when the ledger evidence is written.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md` criterion 1 - the exact bar this item borrows ("a window appears, the process survives at least 10 seconds").
  </read_first>
  <action>
    Confirm the sha first. Run `git rev-parse HEAD` and `git rev-list --count HEAD..origin/<branch>`
    bare. If the checkout is not at the branch tip, pull before doing anything else - D-38-13
    requires the evidence to name a commit that is STILL the branch tip, and building a Windows
    artifact without quick `260906-hq8`'s `runTs.cjs` win32 esbuild fix would be self-defeating.
    Record the sha.

    LOCAL BUILD FIRST (D-38-12, cheap-feedback half). Produce a RELEASE NSIS installer with
    `tauri build` (not `--debug`). Record the build's exit status, the wall time, any warnings, and
    the absolute path of the emitted `.exe`. Then install it and launch the installed GameLib.exe.
    This half is DIAGNOSTIC ONLY - it is not the scoring artifact and no verdict is recorded against
    38-W04 from it. Its purpose is to surface installer bugs before a CI cycle is spent. If the local
    build fails, record the failure verbatim, file it as a todo under `.planning/todos/pending/`, and
    STOP before dispatching - a CI run against a tree whose local build is broken buys nothing.

    THEN DISPATCH (D-38-12, scoring half). Trigger `release-tauri.yml` via `workflow_dispatch`
    against the recorded branch tip - `gh workflow run release-tauri.yml --ref <branch>` with
    whatever inputs the workflow declares, then `gh run list` / `gh run watch` to follow it. DO NOT
    PUSH A `v*` TAG under any circumstances (D-38-14): the tag path co-triggers
    `draft-release-mac.yml` and `draft-release-linux.yml` and opens a draft GitHub Release, which is
    Phase 34-07's gate and not this phase's to fire.

    Record the run id, the run url, the commit sha the run actually checked out (read it from the
    run, do not assume it matches your local HEAD), each leg's conclusion, and the total duration.
    Verify the run's sha equals the recorded branch tip; if the workflow resolved a different ref,
    STOP and record the discrepancy rather than scoring against the wrong build.

    Download the Windows NSIS installer artifact from that run. Record its filename, its byte size,
    and its sha256. Also record the AppImage artifact's filename, size and sha256 and note it as
    RETAINED FOR PHASE 42 - do not attempt to launch it.

    Write all of this into `38-HUMAN-UAT.md` as a dated session block using the template plan 38-01
    installed and plan 38-03 exercised: `### Session <date> - Windows host, CI artifact, <sha>`, then
    for the item `Item: 38-W04`, `Armed:` (run id, run url, run sha, artifact sha256), `Observed:`
    (left for Task 2), `Verdict:` (left for Task 2), `Artifact:` (the installer path on disk).
    Before pasting any CI log excerpt, redact secrets: grep the excerpt for `token`, `Bearer`,
    `secret`, `key`, `password`, and any base64-looking run of 40+ characters, and replace with
    `[REDACTED:<kind>]`. This repository is public.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md';const t=fs.readFileSync(p,'utf8');const b=t.split('### Session ').pop();if(!/38-W04/.test(b))throw new Error('no 38-W04 session block');if(!/run[ _]?id/i.test(b))throw new Error('CI run id not recorded');if(!/[0-9a-f]{64}/.test(b))throw new Error('no sha256 artifact digest recorded');if(!/Armed:/.test(b))throw new Error('Armed evidence field missing');const cp=require('child_process');const tags=cp.execFileSync('git',['tag','--points-at','HEAD'],{encoding:'utf8'}).trim();if(/^v/m.test(tags))throw new Error('D-38-14 VIOLATED: a v* tag points at HEAD');console.log('TASK1 GATE PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --count HEAD..origin/fix/steam-native-install-stability` returns `0` at the moment the dispatch is triggered, and the sha is recorded in the session block.
    - `git tag --points-at HEAD` lists no tag matching `^v`, and `git log --oneline origin/<branch>..HEAD` shows no tag push occurred (D-38-14).
    - The session block records the local build's exit status and the absolute path of the locally-emitted NSIS `.exe`, explicitly labelled DIAGNOSTIC ONLY / not the scoring artifact.
    - The local build used `tauri build` in release mode; the session block states that `--debug` was NOT used and cites Phase 35's F-5-01 as the reason.
    - The session block records the `workflow_dispatch` run id, run url, the commit sha the run checked out, each leg's conclusion, and asserts run-sha == recorded branch tip.
    - The session block records the NSIS artifact filename, byte size and sha256 (a 64-hex-character string), and separately records the AppImage's filename/size/sha256 marked RETAINED FOR PHASE 42.
    - No pasted CI excerpt contains an unredacted value matching `token`, `Bearer `, `secret`, `password`, or a 40+ character base64 run.
    - `git status --porcelain` shows zero modified paths under `src/`, `src-tauri/src/`, `meta/` or `.github/`.
  </acceptance_criteria>
  <done>A CI-produced NSIS installer with a recorded digest, from a workflow_dispatch run whose checked-out sha equals the branch tip, sits on disk ready to be installed - and no tag was pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install and smoke-launch the CI artifact, then discharge 38-W04</name>
  <files>.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md, .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md` - `38-W04`'s full entry including its `reduction_note` and `prior_state`, plus the `audit_tool_note` (discharge by MOVING between arrays, never by annotating a `result:` in place) and the "How to close an item" body section.
    - `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md` - the session block Task 1 wrote, and the Recording protocol section installed by plan 38-01.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md` criterion 1 - the identical bar applied to the macOS artifact, so the Windows verdict is scored on the same standard.
    - `src/backend/logger/paths.ts:16` - the installed app writes to `%LOCALAPPDATA%\GameLib\logs\gamelib.log`, which is where the launch evidence lives if the window itself proves ambiguous.
  </read_first>
  <what-built>
    Task 1 produced a locally-built release NSIS installer as a diagnostic pre-check, then triggered a
    `workflow_dispatch` run of `release-tauri.yml` against the branch tip, recorded the run id, the
    run's checked-out sha and the artifact's sha256, and downloaded the CI-produced NSIS installer.
    No `v*` tag was pushed.

    Everything automatable is done. What remains is the observation the item exists for: installing
    the CI artifact on a real Windows host and watching whether GameLib launches and survives. This
    artifact has never been executed by anything, human or CI, since the Tauri rearchitecture began.
  </what-built>
  <how-to-verify>
    Score ONLY the CI artifact - the file whose sha256 Task 1 recorded from the workflow run. The
    locally-built installer is not the scoring artifact (D-38-12).

    1. Before installing, confirm the file you are about to run is the CI one: re-compute its sha256
       and compare against the digest recorded in the session block. Record the comparison result.
    2. Run the installer. Record: did it complete without error? Any dialog, warning, SmartScreen
       prompt or code-signing complaint - record it verbatim, including the exact wording. An
       unsigned-binary warning is a RESULT, not an obstacle to work around silently.
    3. Record the installed location and the resulting `GameLib.exe` absolute path.
    4. Launch the installed `GameLib.exe`. Start a timer. Record: did a window appear? How long did
       it take? What did it show - the library, a splash, an error surface?
    5. Let it run for at least 10 seconds without interacting. Record whether the process survived,
       and confirm it by checking the process is still live (Task Manager or `tasklist`), not by
       impression.
    6. Read `%LOCALAPPDATA%\GameLib\logs\gamelib.log` and quote the launch lines verbatim. This is
       the "positive evidence the code path executed" half of D-38-16 - a window that appeared is the
       operator's observation, a log line is the artifact. Redact any token, session or credential
       value before pasting; this repository is public.
    7. State the verdict: PASS or FAIL against the bar "installer completes without error; GameLib
       launches; a window appears; the process survives at least 10 seconds without crashing".

    If the verdict is FAIL, DO NOT FIX ANYTHING. Record the failure verbatim, file it as a new todo
    under `.planning/todos/pending/`, and still discharge the item - a FAIL is a discharged
    observation. Phase 38 does not repair what it observes; that is its stated out-of-scope boundary.
  </how-to-verify>
  <action>
    Present the checkpoint below to the operator and STOP. Do not install or launch the artifact
    yourself and never record a verdict on the operator's behalf.

    After the operator responds, fill `38-W04`'s stanza in the `38-HUMAN-UAT.md` session block -
    `Armed:` (CI run id, artifact sha256, the pre-install digest re-comparison result), `Observed:`
    (installer outcome with any SmartScreen or signing dialog quoted verbatim, the installed
    `GameLib.exe` absolute path, whether a window appeared and after how long, the confirmed
    process-alive check at >= 10 seconds), `Verdict:` (exactly PASS or FAIL), `Artifact:` (the log
    path and line range) - redacting any credential-shaped value first.

    Then MOVE `38-W04`'s complete entry from `human_verification` into
    `human_verification_discharged` in `38-VERIFICATION.md`, adding `result:`, an `evidence:` field
    naming the run id and artifact sha256, and a `discharged:` date. Never annotate in place - the
    audit counts array membership and ignores any `result:` field. Leave `status:` at
    `human_needed`. If the verdict is FAIL, write a new todo under `.planning/todos/pending/`
    describing the defect and change no source file.
  </action>
  <verify>
    <automated>node -e "const cp=require('child_process');const out=cp.execFileSync(process.platform==='win32'?'gsd-sdk.cmd':'gsd-sdk',['query','audit-uat'],{encoding:'utf8',maxBuffer:67108864});const j=JSON.parse(out);const s=j.summary,b=s.by_phase,f=[];if(b['38']!==16)f.push('by_phase[38]='+b['38']+' expected 16');if(b['42']!==5)f.push('by_phase[42]='+b['42']+' expected 5');if(b['43']!==4)f.push('by_phase[43]='+b['43']+' expected 4');if(s.total_items!==50)f.push('total_items='+s.total_items+' expected 50');if(f.length){console.error('DISCHARGE GATE FAIL: '+f.join('; '));console.error(JSON.stringify(s,null,2));process.exit(1);}console.log('DISCHARGE GATE PASS '+JSON.stringify(s));"</automated>
  </verify>
  <done>38-W04 sits in human_verification_discharged with a result, a run-id-and-digest evidence field and a discharge date; the audit reports 16 open Phase 38 items and 50 in total; no source file changed.</done>
  <acceptance_criteria>
    - The session block for `38-W04` has all five fields populated: `Item:`, `Armed:`, `Observed:`, `Verdict:`, `Artifact:`. `Verdict:` is exactly `PASS` or `FAIL`, never a hedge.
    - `Armed:` names the CI run id and the artifact sha256, and records the pre-install digest re-comparison result.
    - `Observed:` records the installer outcome including any SmartScreen or signing dialog verbatim, the installed `GameLib.exe` absolute path, whether a window appeared and after how long, and the confirmed process-alive check at >= 10 seconds.
    - At least one verbatim `gamelib.log` line from the launch is quoted, with any credential-shaped value redacted as `[REDACTED:<kind>]`.
    - `38-W04`'s entry has been MOVED from `human_verification` into `human_verification_discharged` in `38-VERIFICATION.md`, carrying a `result:` of `pass` or `fail`, an `evidence:` field naming the run id and artifact sha256, and a `discharged:` date. It is NOT annotated in place.
    - `38-VERIFICATION.md` `status:` is still exactly `human_needed`.
    - `gsd-sdk query audit-uat` reports `by_phase["38"] == 16` (was 17), `by_phase["42"] == 5`, `by_phase["43"] == 4`, `total_items == 50`. A flat 17 means the move did not take.
    - If the verdict is FAIL, a new file exists under `.planning/todos/pending/` describing the defect, and `git status --porcelain` still shows zero modified paths under `src/`, `src-tauri/src/`, `meta/` or `.github/`.
  </acceptance_criteria>
  <resume-signal>Type "PASS" or "FAIL" with the observed detail, or describe what blocked the install.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI artifact to local host | An installer downloaded from a workflow run is executed with user privileges on the machine that holds this project's Steam and Epic credentials. Provenance must be established before execution, not after. |
| CI logs and app logs to a PUBLIC repository | GameLib is a public fork. Anything pasted into `38-HUMAN-UAT.md` is published. `release-tauri.yml` handles signing secrets, and `gamelib.log` carries application state. |
| GitHub Actions trigger surface | `release-tauri.yml` has two triggers. One of them publishes a draft Release and fans out to two other workflows. Choosing the wrong one fires another phase's gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Tampering | The downloaded NSIS installer | mitigate | Record the artifact sha256 at download time and RE-COMPARE it immediately before execution (Task 2 step 1); assert the run's checked-out sha equals the recorded branch tip, so the binary's provenance is a specific commit rather than "a CI run". |
| T-38-04-02 | Elevation of privilege | Executing an unsigned installer on the dev host | accept | The artifact is built from this project's own tree by this project's own workflow, and executing it IS the item. A SmartScreen or unsigned-binary warning is recorded as a RESULT rather than dismissed silently. Apple signing/notarization is a separate, already-filed concern and is not folded here. |
| T-38-04-03 | Information disclosure | CI log and `gamelib.log` excerpts pasted into a public repo | mitigate | Grep every excerpt for `token`, `Bearer `, `secret`, `key`, `password`, and 40+ character base64 runs before pasting; replace with `[REDACTED:<kind>]`. Quote only evidence lines, never a whole tail. |
| T-38-04-04 | Denial of service | Firing the `v*` tag trigger | mitigate | D-38-14 is enforced by an automated gate asserting `git tag --points-at HEAD` lists no `v*` tag. The tag path co-triggers two other workflows and opens a draft Release belonging to Phase 34-07. |
| T-38-04-05 | Repudiation | Scoring the local build instead of the CI artifact | mitigate | The session block must label the local `.exe` DIAGNOSTIC ONLY and the `Armed:` field must name the CI run id and CI artifact digest; the pre-install digest re-comparison makes substituting the local file detectable. |
| T-38-04-06 | Tampering | "Fixing" a defect the smoke launch uncovers | mitigate | Out-of-scope boundary is enforced by an acceptance criterion asserting zero modified paths under `src/`, `src-tauri/src/`, `meta/`, `.github/`; a FAIL is filed as a todo and still discharges the item. |
| T-38-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependency is added. `cargo build` resolves the crates pinned in `src-tauri/Cargo.lock` and `pnpm i` resolves `pnpm-lock.yaml`; neither introduces a package requiring a legitimacy checkpoint. Any step that would ADD an entry to `package.json` or `Cargo.toml` is an immediate STOP. |
</threat_model>

<verification>
1. `git tag --points-at HEAD` lists no `v*` tag (D-38-14).
2. The session block names a `workflow_dispatch` run id whose checked-out sha equals the recorded branch tip (D-38-13).
3. The `Armed:` field carries the CI artifact sha256, and the pre-install re-comparison result is recorded (D-38-12).
4. The local build is labelled DIAGNOSTIC ONLY and no verdict is derived from it (D-38-12).
5. `Verdict:` is exactly PASS or FAIL, backed by a verbatim `gamelib.log` line and a confirmed process-alive check at >= 10 seconds (D-38-16).
6. `gsd-sdk query audit-uat` reports `by_phase["38"] == 16` and `total_items == 50`; `status:` is still `human_needed`.
7. `git status --porcelain` shows no modified paths under `src/`, `src-tauri/src/`, `meta/`, `.github/`.
</verification>

<success_criteria>
`38-W04` is discharged on a CI artifact whose provenance is a named `workflow_dispatch` run against
a named branch-tip sha, with a recorded digest, an operator verdict, and a verbatim log line - not
on a local build, and not by pushing a tag. The audit confirms the move rather than the plan
asserting it.
</success_criteria>

<output>
Create `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-04-SUMMARY.md` when done, recording the run id, the run url, the artifact sha256, the verdict, and the audit map before and after the discharge.
</output>
