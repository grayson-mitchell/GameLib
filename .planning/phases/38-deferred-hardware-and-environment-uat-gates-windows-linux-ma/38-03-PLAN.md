---
phase: 38-deferred-hardware-and-environment-uat-gates-windows-linux-ma
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
  - .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md
autonomous: false
requirements: []   # NONE - collection phase, no requirement IDs exist for Phase 38.

must_haves:
  truths:
    - "The Windows toolchain is established: rustc and cargo resolve on PATH, src-tauri/target/ exists, and `pnpm tauri:dev` launches a GameLib window against the branch tip - measured, with the versions and the commit sha recorded."
    - "The branch tip is current: the 4 commits outstanding from origin/fix/steam-native-install-stability are pulled, and the resulting HEAD sha is recorded as the sha every subsequent sitting scores against."
    - "D-38-18: a `logInfo` call from the renderer demonstrably lands in %LOCALAPPDATA%\\GameLib\\logs\\gamelib.log on this host - proven by reading the line back, not by trusting src/backend/logger/paths.ts:16."
    - "D-38-15: the 'GameLib's DevTools console accepts no input' assumption is RE-TESTED on WebView2 before the sitting, and the answer is recorded in 38-HUMAN-UAT.md either way - accepting input or not."
    - "D-38-17 (BLOCKING): the probe/diagnostic surface the sittings will read carries a visibly-changing heartbeat and is verified end-to-end headlessly before any human is asked to read it. A static instrument produced four confident void readings on 2026-09-06 and must not do so again."
    - "D-38-16: 38-HUMAN-UAT.md's dated-session-block template is exercised once, on this preflight, so the sittings inherit a proven recording format rather than a proposed one."
    - "The gamepad is visible to the running app: navigator.getGamepads() reports a non-empty slot for the connected pad under WebView2, evidenced by a heartbeat-carrying readout, before any 38-C item is attempted."
  artifacts:
    - path: ".planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md"
      provides: "Recorded environment: toolchain versions, HEAD sha, log path proof, DevTools answer, heartbeat proof, gamepad slot readout"
      contains: "HEAD sha"
    - path: ".planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md"
      provides: "The DevTools re-test answer recorded in the sitting protocol itself"
      contains: "WebView2"
  key_links:
    - from: "renderer logInfo call"
      to: "%LOCALAPPDATA%\\GameLib\\logs\\gamelib.log"
      via: "the logInfo IPC listener, not console.*"
      pattern: "gamelib\\.log"
    - from: "38-PREFLIGHT.md"
      to: "38-HUMAN-UAT.md"
      via: "the DevTools answer and the heartbeat proof are copied into the sitting protocol"
      pattern: "DevTools"
---

<objective>
Establish, on the Windows host, everything the four sittings depend on - a working Rust/Tauri
toolchain at the correct commit, a proven evidence path, a proven instrument, and a proven recording
format - and answer the one open measurement question (D-38-15) before any item is attempted.

Purpose: two of this project's most expensive recorded failures were preflight failures. A probe
page missing a `</script>` tag rendered fine, sat on its initial status string and produced FOUR
confident, void "the pad is dead" readings across two browsers and two origins. And a
macOS-derived constraint (`blocked_by: "a Windows machine"`) stood false for ten days because
nobody re-measured it on the platform it was being applied to. This plan pays both costs once,
before 17 items ride on them.

Output: `38-PREFLIGHT.md` recording the measured environment, and the DevTools answer written into
`38-HUMAN-UAT.md` itself.

**NO SOURCE FILE IS MODIFIED. Build outputs under `src-tauri/target/` and `node_modules/` are
expected; a change under `src/`, `meta/` or `.github/` is a scope violation.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-CONTEXT.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md
@.planning/.continue-here.md
@.planning/HANDOFF.json

<measured_environment>
Measured at plan time, 2026-09-06, on this Windows host:

- `node` 24.19.0, `pnpm` 10.28.0, `node_modules/` present.
- `rustc` / `cargo` ABSENT. `src-tauri/target/` does not exist. `pnpm tauri:dev` CANNOT run yet.
- `python3` resolves to the Microsoft Store stub, which breaks `pnpm planning-gates`
  (`package.json:39` invokes `python3 meta/runPlanningGates.py` literally).
- `graphify` ABSENT, so both PATH-resolved graphify `PreToolUse` hooks exit 127. This is NOISY BUT
  NON-BLOCKING BY DESIGN - quick `260906-h2k` established that `PreToolUse` blocks only on exit 2,
  so a missing binary surfaces a visible prompt instead of silently disabling a workflow CLAUDE.md
  calls mandatory. Do NOT add a `command -v` guard to "fix" it.
- Git: HEAD `90a2ea413`, on `fix/steam-native-install-stability`, **4 commits behind and 2 ahead of
  `origin/fix/steam-native-install-stability`**. The 4 behind include quick `260906-hq8`'s fix to
  `runTs.cjs` spawning esbuild via `process.execPath` on win32 - a Windows-specific fix, so building
  Windows artifacts without it would be self-defeating (D-38-13).
- `HANDOFF.json`'s push blocker is STALE: the commits are on origin and this checkout is BEHIND, not
  ahead. Do not act on it.
</measured_environment>

<controller_finding>
From `.continue-here.md`, measured 2026-09-06 with a complete Steam-running / Steam-quit 2x2.

GameLib runs in WKWebView on macOS and WebView2 (Chromium) on Windows, and the two engines disagree
about which gamepads exist. The PowerA Advantage Wired for Nintendo Switch 2 (`0x20D6` / `0xA720`)
is read by Chromium and returns 0 slots in WebKit. macOS classes the device correctly
(`DeviceUsagePage=1` / `DeviceUsage=5`, `AppleUserHIDEventDriver`), so this is an ENGINE difference,
not a pairing failure. Steam Input exclusivity was RULED OUT as a cause.

Consequence: the pad is a Windows asset, the controller items are a Windows sitting, and
`nintendo.ts:69`'s deliberately widened third-party predicate has never been exercised anywhere.
This plan must confirm the pad is visible to the RUNNING GameLib app under WebView2 - the finding
above was measured in browsers, not in the shipped shell.
</controller_finding>

<dispatch_gate>
`gamepadAction` is dispatched ONLY from the `navigator.getGamepads()` polling loop
(`src/frontend/helpers/gamepad.ts:559,678`, rAF-driven at `:593,628`, gated on the
`gamepadconnected` event). There is NO keyboard entry point into
`src/preload/api/tauriGamepadInput.ts`. Nothing in the C-series can be exercised at a keyboard, and
38-C03's "Tab / Shift+Tab" wording is the trap - pressing the physical Tab key exercises the
webview's native focus traversal and never reaches the preload module at all.
</dispatch_gate>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring the checkout to the branch tip and stand up the Rust/Tauri toolchain</name>
  <files>.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md</files>
  <read_first>
    - `.planning/.continue-here.md` - the BLOCKING CONSTRAINTS block and the Critical Anti-Patterns table in full. Two of the five apply directly to this task: never pipe a command whose exit code you intend to trust, and never use `git checkout -- <file>` to restore, because this repo's post-checkout hook runs `pnpm i` and `download-helper-binaries` and throws.
    - `.planning/HANDOFF.json` - confirm for yourself that its push blocker is stale before acting on anything in it.
    - `package.json` scripts block - in particular `tauri:dev` (line ~32), which is `pnpm build:sidecar && pnpm build:decompress-worker-dev && tauri dev`, and `planning-gates` (line ~39).
    - `src-tauri/tauri.conf.json` - `bundle.targets` at line ~31 (`nsis`, `appimage`, `dmg`), which is the binary-format boundary 38-W04 and 38-W05 rest on.
    - `.husky/pre-push` - the lint ratchet at `--max-warnings 4157`. You are PULLING, not pushing, so it should not fire; know what it is before it surprises you.
  </read_first>
  <action>
    Bring the checkout current. `git fetch` then `git pull --ff-only` (or `git rebase` if the 2 local
    commits require it) so the 4 outstanding commits from
    `origin/fix/steam-native-install-stability` land, INCLUDING quick `260906-hq8`'s `runTs.cjs`
    win32 esbuild fix. Run each git command BARE, never inside a pipe - `git push | tail` reported
    exit 0 for a push that failed on 2026-09-06 and the harness reported success. Record the
    resulting HEAD sha; that sha is what every subsequent sitting scores against and what
    `38-W04`'s ledger evidence must name (D-38-13).

    Install the Rust toolchain: `rustup` with the `stable-msvc` default toolchain (MSVC, not GNU -
    Tauri's Windows target and the WebView2 bindings expect the MSVC ABI), plus the Visual Studio
    Build Tools C++ workload and the WebView2 runtime if absent. Prefer `winget` for each. Verify by
    running `rustc --version` and `cargo --version` bare and recording both.

    Repair `python3` so `pnpm planning-gates` can run: the Microsoft Store stub must be shadowed by
    a real interpreter on PATH ahead of it (disable the `python3.exe` App Execution Alias in Windows
    Settings, or place a `python3` shim ahead of the stub). Verify with `python3 --version` returning
    a real version, then run `pnpm planning-gates` and record the pass/fail line count. If the gates
    are red for a reason unrelated to this phase, record the failing gate names and CONTINUE - this
    plan does not own the planning gates, and Phase 39's dispositions are the reference for what is
    known-red.

    Do NOT install `graphify`. Its absence is a recorded, deliberate, non-blocking condition (quick
    `260906-h2k`) and adding a `command -v` guard to silence the hook would exit 0 and silently
    disable a workflow CLAUDE.md calls mandatory.

    Build and launch. Run `pnpm tauri:dev` - NEVER bare `tauri dev`, which serves a stale static
    bundle and would make every subsequent observation void. Confirm a GameLib window appears and
    survives. Record the first-build wall time and any build errors verbatim.

    Create `38-PREFLIGHT.md` with frontmatter (`phase`, `status: in_progress`, `created`,
    `host`, `head_sha`) and a `## Environment` section recording, verbatim: the HEAD sha before and
    after the pull and the four commit subjects pulled; `rustc --version`; `cargo --version`;
    `node --version`; `pnpm --version`; `python3 --version`; the `pnpm planning-gates` result;
    whether `pnpm tauri:dev` launched; and the Windows build/OS version. Every value is a captured
    command output, not a recollection.
  </action>
  <verify>
    <automated>node -e "const cp=require('child_process');const run=(c,a)=>cp.execFileSync(c,a,{encoding:'utf8',shell:true}).trim();const out={};for(const [k,c] of [['rustc','rustc --version'],['cargo','cargo --version'],['node','node --version'],['pnpm','pnpm --version']]){out[k]=run(c);}const fs=require('fs');if(!fs.existsSync('src-tauri/target'))throw new Error('src-tauri/target missing - cargo never built');const head=run('git rev-parse HEAD');const behind=run('git rev-list --count HEAD..origin/fix/steam-native-install-stability');if(behind!=='0')throw new Error('still '+behind+' commits behind origin - D-38-13 requires the branch tip');const pf='.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md';const t=fs.readFileSync(pf,'utf8');if(t.indexOf(head.slice(0,9))<0)throw new Error('38-PREFLIGHT.md does not record the current HEAD sha '+head);console.log('TASK1 GATE PASS',JSON.stringify(out),head);"</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --count HEAD..origin/fix/steam-native-install-stability` returns `0` - the 4 outstanding commits are pulled.
    - `rustc --version` and `cargo --version` both exit 0 and their output is recorded verbatim in `38-PREFLIGHT.md`.
    - The directory `src-tauri/target/` exists.
    - `python3 --version` returns a real CPython version string, not the Microsoft Store stub's redirect behaviour.
    - `pnpm tauri:dev` launched a GameLib window; `38-PREFLIGHT.md` records that outcome and the command used, and states explicitly that bare `tauri dev` was NOT used.
    - `38-PREFLIGHT.md` contains the current `git rev-parse HEAD` sha and the subjects of the 4 pulled commits.
    - `git status --porcelain` shows ZERO modified paths under `src/`, `meta/` or `.github/`. Untracked build output under `src-tauri/target/` is expected and permitted.
    - No `command -v graphify` guard was added to any hook file; `git diff --name-only` shows no path under `.claude/`.
  </acceptance_criteria>
  <done>The Windows host builds and runs GameLib from the branch tip, the environment is recorded as captured command output, and every subsequent sitting has a named commit sha to score against.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the evidence path and build a heartbeat-carrying instrument</name>
  <files>.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md</files>
  <read_first>
    - `src/backend/logger/paths.ts` lines 10-22 - `getBaseLogPath()`. The Windows branch returns `join(localAppData, 'GameLib', 'logs')` with `localAppData` from `process.env.LOCALAPPDATA`, falling back to `join(homedir(), 'AppData', 'Local')`.
    - `src/frontend/helpers/gamepad.ts` around lines 550-700 - the `navigator.getGamepads()` polling loop, the rAF drivers at ~593 and ~628, the `gamepadconnected` gate, and the `window.api.gamepadAction` dispatch at ~559 and ~678. Re-derive the CURRENT action list from this file; `38-VERIFICATION.md`'s `sweep_notes.re_derive_before_running` warns that the action set moved after Phase 34.1 wrote these items, and a gate literal here goes stale by BEHAVIOUR rather than by line number.
    - `src/frontend/helpers/gamepad_layouts/nintendo.ts` - especially the widened third-party predicate around line 69, which has never been exercised on any platform.
    - `src/preload/api/tauriGamepadInput.ts` lines 40-70 - `getFocusableElements()` and the `hasZeroArea` filter at :57 (`rect.width <= 0 || rect.height <= 0`), plus the `disabled` and `aria-hidden="true"` drops. This is the Tauri-only predicate that makes 38-C08 an independent observation from the retired 38-C07.
    - `.planning/.continue-here.md` - the inert-instrument incident in full. This task exists because of it.
  </read_first>
  <action>
    Prove the evidence path FIRST, because every later item's "positive evidence the code path
    executed" rides on it. From the running app (`pnpm tauri:dev`), cause a renderer-side `logInfo`
    to fire with a unique nonce string, then read that nonce back out of
    `%LOCALAPPDATA%\GameLib\logs\gamelib.log` on disk. Record in `38-PREFLIGHT.md`: the resolved
    absolute path, the nonce, and the matching log line verbatim. If the nonce does not appear,
    STOP - `38-HUMAN-UAT.md`'s entire "instrument rather than ask" protocol is unfounded on this
    host and every item's evidence half would be unverifiable.

    Build the instrument, under D-38-17's BLOCKING constraint. The sittings need a readable
    diagnostic surface for gamepad state (`navigator.getGamepads()` slots, the connected pad's `id`,
    axes and button values) and for focus state (`document.activeElement`, and the list
    `getFocusableElements()` actually returns). Whatever form it takes - an in-app dev overlay, a
    served probe page, or a `logInfo` stream - it MUST carry a VISIBLY-CHANGING HEARTBEAT: a
    monotonically incrementing counter or a rendered timestamp that updates on every poll tick, so
    that "no gamepad detected" is distinguishable from "the script never ran". A static readout
    saying zero is exactly what produced four confident void readings on 2026-09-06.

    VERIFY THE INSTRUMENT END-TO-END HEADLESSLY BEFORE ANY HUMAN IS ASKED TO READ IT. Capture the
    rendered DOM twice, at least two heartbeat intervals apart (`--headless --dump-dom` or the
    equivalent for the surface you chose, or two timestamped `logInfo` samples if the instrument is
    log-based), and assert the heartbeat VALUE DIFFERS between the two captures. A single capture
    proves nothing - the inert probe rendered fine on its first paint. Record both captures verbatim
    in `38-PREFLIGHT.md`, with the differing heartbeat values called out.

    Keep the instrument OUT of the source tree. Put any probe page or script under the session
    scratchpad, not under `src/`. If a dev-only overlay genuinely must live in the app to be useful,
    STOP and raise it - Phase 38 ships no code, and adding one is a decision this plan does not own.

    With the instrument proven live, confirm the pad is visible to the RUNNING APP under WebView2:
    read `navigator.getGamepads()` through the instrument with the pad connected, and record the
    slot index, the reported `id` string and whether the `gamepadconnected` event fired. Then
    disconnect the pad and re-read, recording the changed readout - a negative control, so a later
    "detected" is distinguishable from an instrument that always says detected. If no slot is
    reported with the pad connected, record that verbatim as the measured finding and raise it: the
    seven controller items would then be unscoreable on this host too, which is a scope question,
    not something to retry until it works.

    Finally, exercise `38-HUMAN-UAT.md`'s dated-session-block template ONCE, on this preflight, by
    writing a `### Session <date> - preflight` block with `Item:`, `Armed:`, `Observed:`, `Verdict:`
    and `Artifact:` filled in for the log-path proof and for the gamepad-visibility readout. The
    sittings then inherit a format that has been used, not one that has been proposed.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const pf='.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md';const t=fs.readFileSync(pf,'utf8');if(!/gamelib\.log/i.test(t))throw new Error('log path proof missing');if(!/LOCALAPPDATA|AppData.Local.GameLib/i.test(t))throw new Error('Windows log path not recorded');if(!/heartbeat/i.test(t))throw new Error('heartbeat proof missing');const hb=t.match(/heartbeat[^\n]*?(\d+)/gi)||[];if(hb.length<2)throw new Error('fewer than two heartbeat captures recorded - a single capture proves nothing');if(!/getGamepads/i.test(t))throw new Error('gamepad slot readout missing');if(!/negative control/i.test(t))throw new Error('negative control (pad disconnected) not recorded');const ua=fs.readFileSync('.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md','utf8');if(!/### Session /.test(ua))throw new Error('session block template never exercised');console.log('TASK2 GATE PASS');"</automated>
    <automated>node -e "const cp=require('child_process');const out=cp.execFileSync('git',['status','--porcelain'],{encoding:'utf8'});const bad=out.split('\n').map(l=>l.slice(3)).filter(p=>/^(src\/|meta\/|\.github\/|\.claude\/)/.test(p));if(bad.length)throw new Error('SCOPE VIOLATION - source paths modified: '+bad.join(', '));console.log('SCOPE GATE PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `38-PREFLIGHT.md` records the absolute resolved log path containing `GameLib\logs\gamelib.log`, a unique nonce string, and the verbatim log line in which that nonce appears.
    - `38-PREFLIGHT.md` records TWO instrument captures taken at least two heartbeat intervals apart, with DIFFERENT heartbeat values, both quoted verbatim. One capture is insufficient and fails this criterion.
    - `38-PREFLIGHT.md` records the `navigator.getGamepads()` readout with the pad connected - slot index, `id` string, and whether `gamepadconnected` fired - AND a negative-control readout with the pad disconnected showing a different result.
    - `38-PREFLIGHT.md` records the re-derived CURRENT gamepad action list read out of `src/frontend/helpers/gamepad.ts`, not the list as written in Phase 34.1's items.
    - `38-HUMAN-UAT.md` contains a `### Session ` heading with `Item:`, `Armed:`, `Observed:`, `Verdict:` and `Artifact:` fields populated for at least two preflight observations.
    - `git status --porcelain` shows zero modified or added paths under `src/`, `meta/`, `.github/` or `.claude/`. Any probe artifact lives in the session scratchpad.
  </acceptance_criteria>
  <done>The evidence path is proven by a nonce read back off disk, the instrument is proven live by two differing heartbeat captures plus a negative control, and the recording format has been used once rather than merely specified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-test the DevTools console assumption on WebView2 (D-38-15)</name>
  <files>.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md, .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md</files>
  <read_first>
    - `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md` - the "Before the controller sitting" step 3 as repaired by plan 38-01, which now marks this claim as UNDER RE-TEST rather than asserting it.
    - `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-CONTEXT.md` - D-38-15 in full, including its reasoning about why the answer matters across 17 items and why it must be recorded either way.
    - `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md` as written by Tasks 1 and 2 - the instrument you would otherwise be forced to round-trip through.
  </read_first>
  <what-built>
    Tasks 1 and 2 established the toolchain, launched GameLib from the branch tip via `pnpm
    tauri:dev`, proved that a renderer `logInfo` call reaches
    `%LOCALAPPDATA%\GameLib\logs\gamelib.log`, and built a heartbeat-carrying instrument verified
    live by two differing headless captures plus a pad-disconnected negative control.

    Everything automatable is done. What remains is a single measurement no script can make: whether
    GameLib's DevTools console under WebView2 accepts typed and pasted input. The existing claim that
    it does not is a WKWebView measurement carried onto a different engine - Edge DevTools is a
    different implementation and plausibly behaves differently. If it accepts input, probes across
    the remaining 17 items can be evaluated interactively instead of round-tripping through
    `logInfo`, which is materially cheaper. If it does not, the `logInfo` route proven in Task 2 is
    the only path and the sittings must plan for it.
  </what-built>
  <how-to-verify>
    With GameLib running from `pnpm tauri:dev` (the window from Task 1, or relaunch it the same way -
    never bare `tauri dev`):

    1. Open DevTools on the GameLib window (F12, or Ctrl+Shift+I, or right-click then Inspect).
       Record which of those actually opened it.
    2. Click into the Console panel's input line. Type `1+1` on the keyboard and press Enter.
       Record: did the characters appear? Did Enter evaluate them? Was `2` printed?
    3. Copy the string `navigator.getGamepads().length` to the clipboard from another application,
       then paste it into the Console with Ctrl+V and press Enter. Record: did the paste land? Did
       it evaluate? What number was printed?
    4. If step 3 evaluated, record the returned number - it is a second, independent reading of the
       gamepad-slot question Task 2 measured through the instrument, and the two should agree.
    5. If either step failed, record HOW it failed - characters not appearing, Enter doing nothing,
       paste silently dropped, panel not focusable - in the operator's own words. "It didn't work"
       is not a recorded answer.

    Then write the answer into BOTH `38-PREFLIGHT.md` and `38-HUMAN-UAT.md`'s "Before the sitting"
    section, replacing the UNDER RE-TEST marker with the measured result and the date. Record it
    either way; the point of D-38-15 is that the answer is measured on this engine rather than
    inherited from another.
  </how-to-verify>
  <action>
    Present the checkpoint below to the operator and STOP. Do not answer it yourself and do not infer
    the result from source - this is a measurement on a different browser engine, and inferring it is
    exactly the rot D-38-15 exists to end.

    After the operator responds, transcribe all four sub-results verbatim into `38-PREFLIGHT.md`
    (how DevTools was opened; whether typed input evaluated; whether pasted input evaluated; the
    returned value of `navigator.getGamepads().length` if it evaluated) and rewrite
    `38-HUMAN-UAT.md`'s "Before the sitting" DevTools line: replace the UNDER RE-TEST marker plan
    38-01 installed with a dated statement of the measured result naming WebView2, plus the
    consequence for the sittings - interactive evaluation permitted with a disk artifact still
    required, or `logInfo` as the only evidence path citing Task 2's nonce proof by name.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-HUMAN-UAT.md','utf8');if(/UNDER RE-TEST/i.test(t))throw new Error('D-38-15 marker still present - the answer was not recorded');if(!/WebView2/.test(t))throw new Error('WebView2 not named in the recorded answer');if(!/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]/.test(t))throw new Error('recorded answer is not dated');const p=fs.readFileSync('.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-PREFLIGHT.md','utf8');if(!/DevTools/i.test(p))throw new Error('38-PREFLIGHT.md does not record the DevTools result');console.log('TASK3 GATE PASS');"</automated>
  </verify>
  <done>The DevTools console behaviour under WebView2 is recorded as a dated measurement in both 38-PREFLIGHT.md and 38-HUMAN-UAT.md, whichever way it went, and no macOS-derived assumption is carried forward unmeasured.</done>
  <acceptance_criteria>
    - `38-HUMAN-UAT.md` no longer contains an UNDER RE-TEST marker on the DevTools claim; it contains a dated statement of the measured result naming WebView2.
    - `38-HUMAN-UAT.md` and `38-PREFLIGHT.md` both record all four sub-results: how DevTools was opened, whether typed input evaluated, whether pasted input evaluated, and the returned value of `navigator.getGamepads().length` if step 3 evaluated.
    - If the console accepts input, `38-HUMAN-UAT.md` states that probes may be evaluated interactively AND that the `logInfo` route remains valid for anything that must land on disk as an artifact (D-38-16 requires a written artifact, and a console readout that is never captured is not one).
    - If the console does not accept input, `38-HUMAN-UAT.md` states that the `logInfo` route proven in Task 2 is the only evidence path, and cites the Task 2 nonce proof by name.
    - Any failure mode is recorded in the operator's own words, not summarised as "did not work".
    - `git status --porcelain` shows zero modified paths under `src/`, `meta/`, `.github/` or `.claude/`.
  </acceptance_criteria>
  <resume-signal>Type "recorded" once the measured DevTools answer is written into both files, or describe what blocked the measurement.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| instrument to human reader | An instrument that cannot prove its own liveness converts "the code path never ran" into "the feature is broken" with no visible difference. Four void readings on 2026-09-06 came from exactly this. |
| app log file to planning record | `gamelib.log` is a general application log. Evidence pasted from it into `38-HUMAN-UAT.md` is committed to a PUBLIC repository (GameLib is a public fork), so anything the log carries becomes public. |
| toolchain installers to host | `rustup`, VS Build Tools, WebView2 runtime and any `python3` shim are third-party binaries fetched over the network onto the machine that will hold Steam and Epic credentials during the sittings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-01 | Spoofing | The diagnostic instrument | mitigate | D-38-17: visibly-changing heartbeat plus TWO headless captures with differing values, plus a pad-disconnected negative control, asserted by the Task 2 automated gate before any human reads it. |
| T-38-03-02 | Information disclosure | `gamelib.log` excerpts pasted into `38-HUMAN-UAT.md` and committed publicly | mitigate | Before pasting any log excerpt, grep the excerpt for `token`, `refresh`, `Bearer`, `session`, `cookie`, `password`, `api_key`, `steamid` and any 17-digit numeric run, and redact to `[REDACTED:<kind>]`. Quote only the lines that carry the evidence, never a whole log tail. |
| T-38-03-03 | Tampering | Third-party toolchain installers | mitigate | Install via `winget` from Microsoft's own catalogue or via `rustup` from `rustup.rs`; record the exact package ids and resolved versions in `38-PREFLIGHT.md` so the environment is reproducible and auditable. |
| T-38-03-04 | Elevation of privilege | The `python3` shim placed ahead of the Store stub on PATH | mitigate | Prefer disabling the Microsoft Store App Execution Alias over writing a shim into a PATH directory. If a shim is written, record its absolute path and contents in `38-PREFLIGHT.md`. |
| T-38-03-05 | Tampering | Scope creep - adding a dev overlay to `src/` to make instrumentation easier | mitigate | Phase 38 ships no code. The Task 2 automated scope gate fails on any modified path under `src/`, `meta/`, `.github/`, `.claude/`; a genuinely-needed in-app overlay is an explicit STOP, not a quiet commit. |
| T-38-03-SC | Tampering | npm/pip/cargo installs | mitigate | No NEW package-manager dependency is added by this plan. `pnpm i` may run from the post-checkout hook and `cargo build` will fetch the crates already pinned in `src-tauri/Cargo.lock`; both resolve existing pins rather than introducing packages, so the Package Legitimacy Gate's `[ASSUMED]`/`[SUS]` checkpoint has no candidates. If any step would ADD a dependency to `package.json` or `Cargo.toml`, STOP - that is out of scope for a phase that ships no code. |
</threat_model>

<verification>
1. `git rev-list --count HEAD..origin/fix/steam-native-install-stability` returns 0.
2. `rustc --version`, `cargo --version`, `python3 --version` all exit 0; `src-tauri/target/` exists.
3. `pnpm tauri:dev` launched a window; `38-PREFLIGHT.md` records the command and states bare `tauri dev` was not used.
4. A nonce written by a renderer `logInfo` call is read back out of `%LOCALAPPDATA%\GameLib\logs\gamelib.log` and quoted verbatim.
5. Two instrument captures with DIFFERING heartbeat values are recorded, plus a pad-disconnected negative control.
6. The DevTools answer is recorded in both `38-PREFLIGHT.md` and `38-HUMAN-UAT.md`, dated, naming WebView2, either way.
7. `git status --porcelain` shows no modified paths under `src/`, `meta/`, `.github/`, `.claude/`.
</verification>

<success_criteria>
A GameLib window runs on the Windows host from a named branch-tip sha; a log line written by the
renderer is provably readable off disk; the instrument the sittings will read has been proven to be
alive by two differing captures and a negative control; the DevTools question is answered by
measurement on WebView2 rather than inherited from WKWebView; and none of it required a source
change.
</success_criteria>

<output>
Create `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-03-SUMMARY.md` when done, and leave `38-PREFLIGHT.md` at `status: complete` with the HEAD sha every subsequent sitting will cite.
</output>
