---
phase: 38-deferred-hardware-and-environment-uat-gates-windows-linux-ma
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md
  - .planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VERIFICATION.md
  - .planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md
  - .planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md
  - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-VERIFICATION.md
  - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-LIVE-GATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []   # NONE - collection phase, no requirement IDs exist for Phase 38.

must_haves:
  truths:
    - "D-38-01: each of the 9 retired items has its ORIGIN-side receipt updated from 'open - not yet run in phase 38' to a retirement outcome naming Phase 35's Electron removal and the surviving Tauri twin, so the origin's record does not read as a pending observation that will never happen."
    - "D-38-03: each retirement receipt at the origin names the twin id that carries the observation forward, so a reader of 34.13-UAT.md can see that nothing was lost without opening Phase 38."
    - "D-38-08: every item relocated to Phase 42 has an origin-side receipt naming to_phase 42, so relocation rule (3)'s two-way requirement survives the second hop."
    - "D-38-09: every item relocated to Phase 43 has an origin-side receipt naming to_phase 43, and 40-LIVE-GATE.md's Non-closure statement names Phase 43 rather than Phase 38."
    - "Relocation rule (3) holds transitively: no item id resolves to a receipt whose named destination no longer owns it."
    - "The ledger is unperturbed: audit-uat still reports by_phase 38=17, 42=5, 43=4 and total 51 after this plan, and 35-VERIFICATION.md still emits its 7 items."
  artifacts:
    - path: ".planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md"
      provides: "Updated outcomes for the 9 retired items and 3 relocated to Phase 42"
      contains: "retired - unscoreable"
    - path: ".planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md"
      provides: "human_verification_relocated receipt for 38-W05's hop to Phase 42"
      contains: "human_verification_relocated"
    - path: ".planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-VERIFICATION.md"
      provides: "human_verification_relocated receipt for 38-E01..38-E04's hop to Phase 43"
      contains: "to_phase: \"43\""
  key_links:
    - from: ".planning/phases/34.13-.../34.13-UAT.md"
      to: ".planning/phases/42-deferred-linux-host-uat-gates/42-VERIFICATION.md"
      via: "human_verification_relocated entries whose to_phase is updated to 42"
      pattern: "to_phase: \"42\""
    - from: ".planning/phases/40-.../40-LIVE-GATE.md"
      to: ".planning/phases/43-off-macos-embed-backend-webview2-and-webkit2gtk/43-VERIFICATION.md"
      via: "Non-closure statement naming Phase 43 as the owner of 38-E03/38-E04"
      pattern: "Phase 43"
---

<objective>
Close the origin-side half of every disposition plan 38-01 made, so that relocation rule (3) -
"relocation is two-way; the origin phase keeps a receipt naming this phase and the item ID" - holds
after the second hop, and so that no origin phase's record still reads "open, not yet run in phase
38" for an item Phase 38 no longer owns or has retired as unscoreable.

Purpose: 38-VERIFICATION.md's own "How to close an item" step 4 says it outright - a park is a
promise with no receipt unless someone walks back. Phase 34.9 proved the cost: eight items routed
to a phase that never existed, six dangling 9 to 11 days while every gate read `unmapped 0`. The
forward half landed in 38-01. This is the return half.

Output: updated receipts at the three origin phases (34.13, 35, 40), plus the ROADMAP cross-notes
that keep § Phase 35 and § Phase 40 from pointing at a Phase 38 that no longer owns their items.

**DOCS-ONLY. NO SOURCE FILE IS TOUCHED BY THIS PLAN.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-CONTEXT.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md
@.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-01-SUMMARY.md

<receipt_inventory>
Measured at plan time, 2026-09-06. `grep -rn human_verification_relocated .planning --include=*.md`
finds the key in exactly five phase artifacts: 34.1, 34.11, 34.13, 34.4.1 and 38 itself.

| Origin | Artifact | Key present? | Items this plan must touch |
|---|---|---|---|
| 34.13 | `34.13-UAT.md` (frontmatter) | YES - 18 rows | 9 retired: 38-C07, 38-S01, 38-S03, 38-S05, 38-S07, 38-S09, 38-S11, 38-S13, 38-S15. 3 relocated to 42: 38-S04, 38-S10, 38-S12. 1 split: 38-S16 spawning 38-S17 in Phase 42. |
| 35 | `35-VERIFICATION.md` + `35-LIVE-GATE.md` "Windows/Linux disposition" section | NO - the receipt for 38-W04/38-W05 was written as PROSE only | 38-W05 (moved to Phase 42). 38-W04 stays in 38 - record that it stayed, do not imply it moved. |
| 40 | `40-VERIFICATION.md` + `40-LIVE-GATE.md` "Non-closure statement" (line ~788) | NO | 38-E01, 38-E02, 38-E03, 38-E04 (all moved to Phase 43). |
| 34.1, 34.10, 34.11, 34.4.1 | their own artifacts | 34.1/34.11/34.4.1 YES | NOTHING. Their items (38-W01, 38-W02, 38-W03, 38-C01..38-C06) all stay in Phase 38. Touching them is out of scope and adds diff noise to files with live ledgers. |

`34.13-UAT.md`'s receipt shape is the house form and must be preserved exactly:
`origin_item`, `to_phase`, `to_item`, `blocked_by`, `relocated`, `decided_by`, `outcome`.
</receipt_inventory>

<audit_visibility_hazard>
Two of the files this plan edits are LIVE audit inputs and one is not. Know which before editing.

- `35-VERIFICATION.md` is `status: human_needed` with 7 open items. It IS parsed by `audit-uat`.
  Adding a `human_verification_relocated:` key to its frontmatter does not touch `human_verification`
  and must not change its item count - but the file must still parse. Re-measure after.
- `40-VERIFICATION.md` is `status: gaps_closed_partially`, which `audit-uat` admits neither as
  `human_needed` nor `gaps_found`, so Phase 40 is INVISIBLE to the audit entirely and always has
  been. Editing it is audit-neutral. Do not "fix" that status - it is Phase 40's recorded state and
  flipping it would surface items that were deliberately pushed to `.planning/todos/pending/`.
- `34.13-UAT.md` is a `type: uat` artifact with `status: complete`; the audit's 7 items for phase
  34.13 come from `34.13-VERIFICATION.md`, not from this file's frontmatter. Editing the
  `human_verification_relocated` block is audit-neutral. Re-measure anyway.
</audit_visibility_hazard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the 34.13 receipts for the 9 retirements, the 3 Phase-42 hops and the S16 split</name>
  <files>.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md</files>
  <read_first>
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md` frontmatter in full - all 18 `human_verification_relocated` entries. You need to see which `to_item` each `origin_item` maps to before changing any `outcome`, because the G-* origin names do not sort in the same order as the 38-* ids.
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md` around line 496 - the `G-GAMEPAD-CARET | electron` table row, which carries a long RELOCATED note in the BODY as well as the frontmatter receipt. Both halves must agree after this edit.
    - `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-VERIFICATION.md` as edited by plan 38-01 - the `retired_reason` text you write here must not contradict the `retired_reason` written there.
    - `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-01-SUMMARY.md` - the recorded before/after audit numbers and the twin re-check.
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-VERIFICATION.md` lines 30-40 and ~144 - both cite the two-way receipt as EVIDENCE for a verified truth. If that evidence text now describes a state that no longer exists, it must be amended in the same pass or the phase's own verification rots.
  </read_first>
  <action>
    Edit `34.13-UAT.md`'s `human_verification_relocated` frontmatter block only - do not renumber,
    reorder or delete any entry.

    For the NINE retired items (`to_item` values 38-C07, 38-S01, 38-S03, 38-S05, 38-S07, 38-S09,
    38-S11, 38-S13, 38-S15), change `outcome:` from "open - not yet run in phase 38" to a retirement
    outcome that states four things: the item was RETIRED AS UNSCOREABLE in Phase 38 on 2026-09-06 by
    plan 38-01 under D-38-01; the cause is Phase 35's removal of the Electron build, so the runtime
    the row names no longer exists; it is recorded neither as a pass nor as a deletion, and now sits
    in 38-VERIFICATION.md's `human_verification_discharged` array with `result: unscoreable`; and the
    surviving observation is its Tauri twin, named by id (38-S01 names 38-S02, 38-S03 names 38-S04,
    38-S05 names 38-S06, 38-S07 names 38-S08, 38-S09 names 38-S10, 38-S11 names 38-S12, 38-S13 names
    38-S14, 38-S15 names 38-S16, 38-C07 names 38-C08). For the three whose twin has itself moved to
    Phase 42 (38-S04, 38-S10, 38-S12), the outcome must name Phase 42 as the twin's current owner, or
    the receipt sends a reader to a file that no longer holds it.

    For the THREE items relocated onward to Phase 42 (`to_item` 38-S04, 38-S10, 38-S12), change
    `to_phase:` from "38" to "42", keep `to_item` unchanged (ids keep the 38- prefix), and rewrite
    `outcome:` to record the two-hop history: filed in Phase 38 on its original date, moved to Phase
    42 on 2026-09-06 by plan 38-01 under D-38-08 because Phase 38 narrowed to Windows plus controller
    only, still open and not yet run.

    For `38-S16` (origin_item `G-D20-Q6-COPY / tauri`), keep `to_phase: "38"` and rewrite `outcome:`
    to record the split under D-38-11: the item narrowed to matrix row 5 (Windows) and stayed in
    Phase 38; its Linux row-7 branch was minted as `38-S17` and lives in Phase 42; a run against
    38-S16 alone no longer discharges the Linux half, which is the point of relocation rule (4).
    Add a corresponding NEW entry for `38-S17` with `origin_item: "G-D20-Q6-COPY / tauri - Linux
    row-7 half"`, `to_phase: "42"`, `to_item: "38-S17"`, a Linux machine-switch `blocked_by`,
    `relocated: 2026-09-06`, `decided_by: "38-CONTEXT.md D-38-11, plan 38-01"`, and an outcome
    recording that it was minted by the split rather than relocated from a run.

    For `38-S15` (origin_item `G-D20-Q6-COPY / electron`) the retirement outcome must additionally
    note that this row was scored across BOTH matrix rows 5 and 7, so its retirement removes the
    Electron half of both branches at once, and that the Tauri halves survive as 38-S16 (Windows,
    Phase 38) and 38-S17 (Linux, Phase 42).

    Then reconcile the BODY. The `G-GAMEPAD-CARET | electron` table row near line 496 carries a
    RELOCATED note that ends "Receipt in this file's frontmatter under human_verification_relocated".
    Append a dated sentence recording the 2026-09-06 retirement and naming 38-C08 as the surviving
    observation, so the body and the frontmatter do not diverge. Do not rewrite the existing note -
    its two research facts (the caret exists on MainButton but the GameCard entry is a context-menu
    item; the caret leaves the tab order by UNMOUNTING, not by `disabled`) still apply to 38-C08 and
    must be preserved verbatim.

    Finally, amend `34.13-VERIFICATION.md`'s two evidence passages (around line 33 and around line
    144) which assert that "38-VERIFICATION.md contains matching origin_phase: 34.13 / origin_item
    entries ... confirming the relocation receipt is intact, not rotted". After 38-01 that is only
    partly true: three of those items now live in 42-VERIFICATION.md and nine sit in Phase 38's
    discharged array. Append a dated 2026-09-06 clause naming the new distribution, so the evidence
    text describes the state a re-checker will actually find. Do NOT change
    `34.13-VERIFICATION.md`'s `status:` or its `human_verification` array - the audit reads it and
    the 7 open items there are unrelated accepted-open rows.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md';const t=fs.readFileSync(p,'utf8');const fm=t.split('---')[1];const blocks=fm.split(/\n  - origin_item:/).slice(1);const byItem={};for(const b of blocks){const m=/to_item: \"([^\"]+)\"/.exec(b);if(m)byItem[m[1]]=b;}const retired=['38-C07','38-S01','38-S03','38-S05','38-S07','38-S09','38-S11','38-S13','38-S15'];for(const id of retired){const b=byItem[id];if(!b)throw new Error('missing receipt for '+id);if(/open - not yet run in phase 38/.test(b))throw new Error(id+' outcome still says open');if(!/unscoreable/i.test(b))throw new Error(id+' outcome does not say unscoreable');}for(const id of ['38-S04','38-S10','38-S12']){const b=byItem[id];if(!b)throw new Error('missing receipt for '+id);if(!/to_phase: \"42\"/.test(b))throw new Error(id+' to_phase not updated to 42');}if(!byItem['38-S17'])throw new Error('38-S17 receipt not minted');if(!/to_phase: \"42\"/.test(byItem['38-S17']))throw new Error('38-S17 receipt does not point at phase 42');console.log('TASK1 GATE PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - None of the nine retired `to_item` receipts in `34.13-UAT.md` still contains the string `open - not yet run in phase 38`; each contains the case-insensitive substring `unscoreable`, the string `38-01`, and the id of its own Tauri twin.
    - The receipts for `38-S04`, `38-S10` and `38-S12` each contain `to_phase: "42"`; their `to_item` values are unchanged and still carry the `38-` prefix.
    - A new receipt entry exists with `to_item: "38-S17"` and `to_phase: "42"`.
    - The `38-S16` receipt still reads `to_phase: "38"` and its outcome names `38-S17` and `42`.
    - `34.13-UAT.md`'s body row for `G-GAMEPAD-CARET | electron` still contains the literal substrings `SteamInstallCaret`, `MainButton.tsx:356`, `GameCard/index.tsx:386-397` and `UNMOUNTING` (the research facts are preserved, not rewritten), AND now contains `2026-09-06` and `38-C08`.
    - `34.13-VERIFICATION.md` contains the string `2026-09-06` in both amended evidence passages, and `grep -n '^status:' 34.13-VERIFICATION.md` is unchanged from its pre-task value.
    - `gsd-sdk query audit-uat` still reports `by_phase["34.13"] == 7`, `by_phase["38"] == 17`, `by_phase["42"] == 5`, `by_phase["43"] == 4`, `total_items == 51`.
    - `git status --porcelain` shows ZERO changed or added paths under `src/`, `src-tauri/`, `meta/` or `.github/`.
  </acceptance_criteria>
  <done>34.13's receipts describe the actual current owner and actual current disposition of all thirteen items it routed to Phase 38, and its own verification evidence text matches what a re-checker will find.</done>
</task>

<task type="auto">
  <name>Task 2: Write the missing origin receipts at Phase 35 and Phase 40, and cross-note the ROADMAP</name>
  <files>.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md, .planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md, .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-VERIFICATION.md, .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-LIVE-GATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md` frontmatter in full. Confirm its `status:` value and count the entries in its `human_verification` array - the audit reports 7 for this phase and that number must not move.
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/35-LIVE-GATE.md` - the "Windows/Linux disposition" section, which is where 38-W04 and 38-W05 were routed as prose rather than as a structured receipt.
    - `.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-VERIFICATION.md` frontmatter - confirm `status: gaps_closed_partially` before editing, and confirm whether a `human_verification` key exists at all.
    - `.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-LIVE-GATE.md` lines 785-805 - the "Non-closure statement (binding, mirrors ROADMAP.md's Phase 38 ledger from this side)" section. It names Phase 38 as the owner of 38-E01..38-E04; after 38-01 that is Phase 43.
    - `.planning/ROADMAP.md` § Phase 35 (the `- [x] **Phase 35: Electron cutover**` overview bullet, which ends "Windows/Linux and off-macOS Epic logout -> Phase 38") and § Phase 40 (line ~4798, whose closing paragraph names 38-E01..38-E04 as deferred to Phase 38).
    - `.planning/phases/34.13-steam-install-time-wine-bottle-form-gog-parity/34.13-UAT.md` frontmatter lines 1-20 - the receipt shape to copy.
  </read_first>
  <action>
    Phase 35. Add a `human_verification_relocated:` key to `35-VERIFICATION.md`'s frontmatter using
    34.13-UAT.md's field shape, with TWO entries so the record is complete rather than only recording
    the item that moved:
    - `origin_item: "35-19 Task 2, option-c - Windows NSIS smoke launch"`, `to_phase: "38"`,
      `to_item: "38-W04"`, `outcome: "open - remains in Phase 38 after the 2026-09-06 split; Phase 38
      retained the Windows items"`.
    - `origin_item: "35-19 Task 2, option-c - Linux AppImage smoke launch"`, `to_phase: "42"`,
      `to_item: "38-W05"`, `outcome:` recording the two-hop history - filed in Phase 38 2026-08-30,
      moved to Phase 42 on 2026-09-06 by plan 38-01 under D-38-08, still open.
    Both entries carry `blocked_by`, `relocated`, and `decided_by: "35-19 Task 2 option-c; second hop
    per 38-CONTEXT.md D-38-08, plan 38-01"`. Also add a `note:` on the 38-W05 entry recording that
    both items are the SMOKE-LAUNCH half of D-16's "artifacts plus a smoke launch", acknowledged as a
    scope reduction in REQUIREMENTS.md REQ-35-20, and that the reduction is not compounded by the
    relocation - the item is still owed, only by a different phase.

    Do NOT change `35-VERIFICATION.md`'s `status:` or its `human_verification` array. It carries 7
    live audit items including criterion 14's genuinely-open UI-repaint item; any status change makes
    all 7 vanish.

    In `35-LIVE-GATE.md`'s "Windows/Linux disposition" section, append a dated 2026-09-06 paragraph
    recording that the Linux half (38-W05) now lives in Phase 42 and the Windows half (38-W04)
    remains in Phase 38, and pointing at `35-VERIFICATION.md`'s new `human_verification_relocated`
    block as the structured receipt. This is the forward-half/return-half discipline: the document
    that predicted the deferral records where the deferral went.

    Phase 40. Add a `human_verification_relocated:` key to `40-VERIFICATION.md`'s frontmatter with
    four entries, one each for 38-E01, 38-E02, 38-E03 and 38-E04: `origin_item: "40-10 Task 2
    (D-04)"` (for E03 and E04 extend it with "coordinates with plan 40-11's live gate Item 1" and
    "Item 3" respectively), `to_phase: "43"`, `to_item` the unchanged 38-E id, `blocked_by`,
    `relocated: 2026-09-06`, `decided_by: "38-CONTEXT.md D-38-09, plan 38-01"`, and an outcome
    recording the two-hop history plus the reason: 38-E01 and 38-E02 are IMPLEMENTATION tasks before
    they are verification tasks, so keeping them in Phase 38 would hold that phase open no matter
    which host ran, and Phase 43 exists to widen the
    `[target.'cfg(target_os = "macos")'.dependencies]` gate first. Leave
    `40-VERIFICATION.md`'s `status: gaps_closed_partially` exactly as it is.

    In `40-LIVE-GATE.md`'s "Non-closure statement" section, append a dated 2026-09-06 paragraph: the
    statement's substance is UNCHANGED - a macOS PASS still does not close 38-E03 or 38-E04 - but
    their owning phase is now Phase 43, and 38-E01/38-E02 moved there too because no off-macOS code
    path exists for any gate to exercise. Preserve the existing statement verbatim; append, do not
    rewrite.

    ROADMAP. Two small cross-notes so neither section points at a phase that no longer owns the item.
    In the `- [x] **Phase 35: Electron cutover**` overview bullet, amend the trailing "Routed out"
    clause so the Linux AppImage smoke launch reads as routed to Phase 38 and then on to Phase 42 on
    2026-09-06. In § Phase 40, append a dated sentence to the "RETURN HALF OF THE 38-E03/38-E04
    NON-CLOSURE" block recording that all four E items moved to Phase 43 on 2026-09-06 and that the
    non-closure statement itself is unaffected. Do not rewrite either block's existing text - both
    are load-bearing records of predictions that came true.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=p=>fs.readFileSync(p,'utf8');const v35=r('.planning/phases/35-electron-cutover-remove-the-electron-build/35-VERIFICATION.md');if(!/human_verification_relocated:/.test(v35))throw new Error('35-VERIFICATION missing relocation receipt');if(!/to_item: \"38-W05\"/.test(v35)||!/to_item: \"38-W04\"/.test(v35))throw new Error('35 receipt missing W04 or W05');if(!/^status: /m.test(v35))throw new Error('35-VERIFICATION status line lost');const v40=r('.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-VERIFICATION.md');if(!/human_verification_relocated:/.test(v40))throw new Error('40-VERIFICATION missing relocation receipt');for(const id of ['38-E01','38-E02','38-E03','38-E04'])if(!new RegExp('to_item: \"'+id+'\"').test(v40))throw new Error('40 receipt missing '+id);if(!/status: gaps_closed_partially/.test(v40))throw new Error('40-VERIFICATION status changed - forbidden');const g40=r('.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-LIVE-GATE.md');if(!/Non-closure statement/.test(g40))throw new Error('40-LIVE-GATE non-closure section lost');if(!/Phase 43/.test(g40))throw new Error('40-LIVE-GATE does not name Phase 43');console.log('TASK2 GATE PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `35-VERIFICATION.md` contains a `human_verification_relocated:` key with exactly 2 entries, one `to_item: "38-W04"` with `to_phase: "38"` and one `to_item: "38-W05"` with `to_phase: "42"`.
    - `35-VERIFICATION.md`'s `status:` line and `human_verification` array are byte-identical to their pre-task values except for the added key; prove it by asserting `gsd-sdk query audit-uat` still reports `by_phase["35"] == 7`.
    - `35-LIVE-GATE.md`'s "Windows/Linux disposition" section contains the literal string `2026-09-06` and the literal string `Phase 42`.
    - `40-VERIFICATION.md` contains a `human_verification_relocated:` key with 4 entries, all `to_phase: "43"`, covering `38-E01` through `38-E04`, and still contains the exact line `status: gaps_closed_partially`.
    - `40-LIVE-GATE.md` still contains its original non-closure sentence `A macOS PASS recorded in this gate does not close Phase 38's 38-E03 or 38-E04.` verbatim, AND now contains an appended paragraph containing `2026-09-06` and `Phase 43`.
    - `.planning/ROADMAP.md`'s Phase 35 overview bullet contains `Phase 42`; § Phase 40's return-half block contains `Phase 43` and `2026-09-06`.
    - `gsd-sdk query audit-uat` reports `by_phase` 27=2, 30=2, 32=2, 33=3, 34=2, 35=7, 34.13=7, 38=17, 42=5, 43=4 and `total_items == 51` - identical to the post-38-01 state. Any change means an edit perturbed a live ledger.
    - `git status --porcelain` shows ZERO changed or added paths under `src/`, `src-tauri/`, `meta/` or `.github/`.
  </acceptance_criteria>
  <done>Every item Phase 38 relocated onward carries a structured receipt at its true origin, and no origin document tells a reader to look in a phase that no longer owns the item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan's edits to `35-VERIFICATION.md` | That file is a LIVE audit input with 7 open items. A malformed frontmatter edit, or an accidental `status:` change, silently deletes all 7 from the only tool that counts them. |
| this plan's edits to `34.13-VERIFICATION.md` | Also a live audit input (7 items). The evidence-text amendment must not touch `status:` or `human_verification`. |
| receipt text to future reader | A receipt that names a destination which no longer owns the item is worse than no receipt: it reads as authoritative and sends the reader to the wrong file. This is the Phase 34.9 failure mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Denial of service | `35-VERIFICATION.md` / `34.13-VERIFICATION.md` live ledgers | mitigate | Both tasks assert the full post-38-01 `by_phase` map and `total_items == 51` after editing; any drift is a hard fail, not a note. |
| T-38-02-02 | Repudiation | Receipts naming a stale destination | mitigate | Automated gates assert `to_phase` values by id for all thirteen relocated/retired items rather than trusting the edit. |
| T-38-02-03 | Tampering | `40-VERIFICATION.md` status field | mitigate | Gate asserts `status: gaps_closed_partially` is still present. Flipping it would surface items deliberately pushed to `.planning/todos/pending/` and silently reopen Phase 40. |
| T-38-02-04 | Information disclosure | Load-bearing prose being rewritten rather than appended | mitigate | Gates assert the original `G-GAMEPAD-CARET` research facts and the original 40-LIVE-GATE non-closure sentence survive verbatim. |
| T-38-02-05 | Elevation of privilege | Scope creep from a docs-only plan into source | mitigate | Both tasks assert `git status --porcelain` shows zero paths under `src/`, `src-tauri/`, `meta/`, `.github/`. |
| T-38-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing and touches no lockfile. No package-manager surface exists here. |
</threat_model>

<verification>
1. `gsd-sdk query audit-uat` reports the identical post-38-01 map: 27=2, 30=2, 32=2, 33=3, 34=2, 35=7, 34.13=7, 38=17, 42=5, 43=4, total 51.
2. All nine retired `to_item` receipts in `34.13-UAT.md` say `unscoreable` and none says `open - not yet run in phase 38`.
3. `38-S04`, `38-S10`, `38-S12` receipts read `to_phase: "42"`; a new `38-S17` receipt exists.
4. `35-VERIFICATION.md` and `40-VERIFICATION.md` each carry a `human_verification_relocated:` key covering their items, with `40-VERIFICATION.md`'s status untouched.
5. `40-LIVE-GATE.md` retains its original non-closure sentence verbatim and names Phase 43 in an appended dated paragraph.
6. `git status --porcelain` shows no source-tree paths.
</verification>

<success_criteria>
Relocation rule (3) holds transitively across every hop plan 38-01 made. Following any item id from
its original phase leads to the file that actually owns it today, and every retired item's origin
record says it was retired as unscoreable with its surviving twin named - not that it is "open, not
yet run" in a phase that will never run it.
</success_criteria>

<output>
Create `.planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/38-02-SUMMARY.md` when done. Record the audit map measured before and after this plan (they must be identical), and list every receipt id touched with its old and new `to_phase` / `outcome` shape.
</output>
