---
phase: 34.15-steam-platform-signal-and-sync-integrity
plan: 09
type: execute
wave: 4
depends_on: ["34.15-01", "34.15-02", "34.15-03", "34.15-04", "34.15-05", "34.15-06", "34.15-07", "34.15-08"]
files_modified:
  - .planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-UAT.md
autonomous: false
requirements: []
decisions: [D-05, D-15, D-16]
user_setup: []

must_haves:
  truths:
    - "The full suite, typecheck and lint are green across every file this phase touched"
    - "The shipped-gate census proves the three D-17 saboteurs, the B1 EligibilityLoadingRow pin, and metadataCapture.ts are all byte-identical"
    - "D-05: none of 34.14's safety layers was weakened anywhere in the phase — verified by diff, not by claim"
    - "D-16: ONE BLOCKING human UAT gate covering items 1 and 2 is authored with a concrete, repeatable forcing procedure per branch and discharged by the developer"
  artifacts:
    - path: ".planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-UAT.md"
      provides: "the D-16 blocking ledger, status human_needed until discharged"
      contains: "human_needed"
  key_links:
    - from: "the UAT forcing procedure"
      to: "api.steampowered.com"
      via: "/etc/hosts -> 203.0.113.1 (TEST-NET-3, unroutable), NEVER 127.0.0.1"
      pattern: "203.0.113.1"
---

<objective>
Reconcile the whole phase against the full test suite and the shipped gates it was forbidden to
break, then author and discharge the single BLOCKING human UAT gate (D-16).

Purpose: two of this phase's three items are about behaviour a warm, online library cannot
reproduce on demand. Item 1's real question — does the bulk PICS call actually populate the cache
against a real ~378-game library, how long does it take, and what is the real-world `oslist`
presence rate — has no automated substitute: the `oslist` wire shape is MEDIUM confidence, sourced
from third-party PICS dumps rather than an authoritative Valve source, and this gate is what
confirms it empirically. Item 2's original repro had **6 GOG games hidden behind a centred Steam
spinner**, which needs a real merged grid with another runner's real data.

Output: a green suite, a diffed gate census, and a discharged blocking ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-CONTEXT.md
@.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-VALIDATION.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-UAT.md

<interfaces>
<!-- FORCING MECHANISM, established from source this session. `steam-user` bootstraps its CM server
     list from api.steampowered.com/ISteamDirectory/GetCMListForConnect
     (node_modules/steam-user/components/09-logon.js:294-320). On failure it retries with 1s gaps
     and, after 10 attempts, emits `error` — which is what makes `SteamUser.ensureConnected()`
     resolve false and drives refresh()'s exit path 1 ('client-not-ready').
     Blocking `api.steampowered.com` therefore forces item 2's primary branch deterministically. -->

<!-- BLOCKING METHOD — carried verbatim from 34.14's own UAT findings, which paid for this lesson:
     `/etc/hosts` -> `127.0.0.1` REFUSES in ~1ms, so a timeout path never runs and a pending UI
     stays sub-perceptual. Use TEST-NET-3 `203.0.113.1` (reserved, unroutable, silently drops).
     Confirm with `curl` exit 28 (hang) vs exit 7 (refused). `dig` does NOT read /etc/hosts — it is
     the wrong instrument. After removal, verify by REAL RESOLUTION + HTTP 200, not by assuming the
     flush worked: 34.14 found the OS resolver still serving a stale 203.0.113.1 after a flush while
     /etc/hosts was already clean, which would have silently converted a "blocked" run into an
     unblocked one. -->

<!-- Ledger frontmatter shape to mirror (34.14-UAT.md:1-16): phase, plan, gate, status, result,
     created, completed, filled_by, findings_filed. -->

<!-- Shipped gates this phase was forbidden to break — census targets:
     steamPlatformRow.test.ts:194-249   the three D-17 saboteurs
     steamEligibilityWiring.test.ts:185-235  Group B, incl. B1's <EligibilityLoadingRow count === 1
     metadataCapture.ts                 the whole file, byte-identical
     Library/index.tsx:1011             the D-11-fenced global overlay -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: full-suite reconciliation and shipped-gate census</name>
  <files>(no source files — this task verifies and reports; fix-forward only if something is red)</files>
  <read_first>
    - .planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-VALIDATION.md (the sampling contract and the Wave 0 list)
    - .planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-01-SUMMARY.md through 34.15-08-SUMMARY.md (what each plan claims to have landed)
  </read_first>
  <action>
Run and record, in this order:
  1. `pnpm test:ci` — the full suite. Record suite/test counts and compare against the 34.14-era
     baseline recorded in that phase's summaries (39 suites / ~1372 tests at that scale). A DROP in
     count is as much a finding as a failure.
  2. `npx tsc --noEmit -p tsconfig.json` — must be clean. ts-jest here is TRANSPILE-ONLY, so this
     is the ONLY step that can catch a type-level regression; a green jest run does not imply it.
  3. The project's lint command (read `package.json` scripts and use the existing one; do not
     invent flags).

Shipped-gate census — verify by DIFF, not by claim. For each, run the command and record the
literal output in the summary:
  - `git diff --stat main...HEAD -- src/backend/storeManagers/steam/metadataCapture.ts` -> must be empty.
    `depotSignalCaptured()` is the pinned read boundary; a plan that edited it is wrong by construction.
  - `git diff main...HEAD -- src/frontend/screens/Library/components/InstallModal/__tests__/steamPlatformRow.test.ts`
    -> inspect and confirm ZERO changed lines fall between the `describe('the D-17 gates are not vacuous'`
    marker and the following `describe(` marker. The three saboteurs target `hasSteamWindowsDepot`
    and `selectSteamPlatformOptions`, neither of which this phase touches, so they must be untouched.
  - `git diff main...HEAD -- src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts`
    -> confirm zero changed lines between the `describe('Group B` and `describe('Group C` markers (D-15).
  - `grep -c "<EligibilityLoadingRow" src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx`
    -> must be `1`.
  - `grep -c "refreshing && !refreshingInTheBackground && <UpdateComponent />" src/frontend/screens/Library/index.tsx`
    -> must be `1` (D-11's fenced global overlay survived verbatim).
  - `git diff --stat main...HEAD -- src/frontend/screens/Library/components/InstallModal/DownloadDialog/ src/frontend/screens/Library/components/InstallModal/ImportDialog/ src/frontend/screens/Library/components/InstallModal/ThirdPartyDialog/ src/frontend/screens/Library/components/InstallModal/steamSectionGating.ts`
    -> must be empty (D-13 scope fence).
  - `grep -rn -- "!== false" src/backend/storeManagers/steam/platformCapture.ts src/frontend/screens/Library/components/InstallModal/steamPlatformRow.ts src/backend/storeManagers/steam/installFormIpc.ts`
    -> must return nothing outside comment lines.

D-05 non-weakening audit — read the diffs of `steamPlatformRow.ts`, `InstallModal/index.tsx` and
`SteamDialog/index.tsx` and confirm in the summary, each as a named line item:
  - 34.14 D-04's fail-open (`windowsDepotOffered`'s `|| (probeSettled && !depotSignalCaptured)`
    clause) is intact and was not "corrected" toward fail-closed;
  - the pending row (`EligibilityLoadingRow`) still mounts on the same condition;
  - the Install-disable (`eligibilityPending`) is unchanged;
  - `readonlyPlatformValue('pending')` still pins the disabled selector.

If ANY of the above is red or non-empty, fix forward in the owning file and re-run. Do not proceed
to Task 2 with a red suite — an unreachable-surface UAT observation is worse than no observation.

Do NOT run `git stash` at any point. Two data-loss events in this project were caused by an
executor stashing a concurrent session's work; prohibiting it in the prompt did not prevent it the
last time, so it is repeated here as an explicit instruction. If the tree is dirty with work you do
not recognise, STOP and report.
  </action>
  <verify>
    <automated>pnpm test:ci &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:ci` exits 0, with the suite/test counts recorded in the summary
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - The lint command exits 0
    - `git diff --stat main...HEAD -- src/backend/storeManagers/steam/metadataCapture.ts` produces NO output
    - `grep -c "<EligibilityLoadingRow" .../SteamDialog/index.tsx` outputs `1`
    - `grep -c "refreshing && !refreshingInTheBackground && <UpdateComponent />" src/frontend/screens/Library/index.tsx` outputs `1`
    - The four D-05 non-weakening line items are each recorded with the diff evidence that supports them
    - No `git stash` was executed
  </acceptance_criteria>
  <done>The suite is green, the type checker is clean, and every gate this phase was forbidden to break is proven untouched by diff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: D-16 BLOCKING human UAT gate — items 1 and 2</name>
  <files>.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-UAT.md</files>
  <read_first>
    - .planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-UAT.md (the ledger format, the cleanup section, and the blocking-method findings this procedure inherits)
    - .planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-05-SUMMARY.md (which exit paths were instrumented and how)
  </read_first>
  <action>
Author `.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-UAT.md` from the
`34.14-UAT.md` template, run the executor pre-flight steps P0-P2 in the `<how-to-verify>` block
below, then HAND THE PROCEDURE TO THE DEVELOPER. The executor does not self-certify any branch —
it drives the pre-flight, transcribes the developer's observations into the ledger, and
corroborates each against the app log and on-disk cache state. `status` stays `human_needed` in
frontmatter until the developer discharges the gate.
  </action>
  <what-built>
Item 1: `refresh()` now bulk-captures platform data from Steam CM PICS `appinfo.common.oslist`
before hydrating any `GameInfo`, scoped to apps whose depot signal was never captured, failing soft.
Item 2: `refresh()` reports `steamSyncStatus` on all four exit paths; `GlobalState` owns an
`'idle' | 'syncing' | 'failed'` tri-state; the Library screen renders an inline, non-blocking Steam
notice with a Steam-scoped retry instead of a centred overlay.
  </what-built>
  <how-to-verify>
FIRST, create `.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-UAT.md` with
frontmatter mirroring `34.14-UAT.md`: `phase`, `plan: 09`, `gate: D-16`, **`status: human_needed`**,
`result: ""`, `created`, `completed: ""`, `filled_by: ""`, `findings_filed: 0`.

**`status` STAYS `human_needed` until the gate is discharged.** Switching it to `gaps_found` makes
the phase vanish from `gsd-sdk query audit-uat` entirely. Never record a `pass` for a surface that
could not render — use `blocked` or `n-a` with the reason. This project has ledgered a `pass` that
covered an unreachable surface.

**Never paste into the Tauri DevTools console** — it is unusable there. All observations are made by
looking at the running app and at the log file, never by running code in it.

**Pre-flight (executor, before handing to the developer):**
P0. Confirm no GameLib/Electron instance is already running (`pgrep -fl gamelib-shell`; also check
    for an Electron window). The single-instance guard will silently make a launch a no-op. Fully
    quit anything found.
P1. Back up `~/Library/Application Support/GameLib/store_cache/steam_metadata.json` to a `.bak`
    beside it, and record the file's size and entry count in the ledger. This is the before-picture
    for Branch A's measurement.
P2. Confirm at least one NON-Steam runner (GOG preferred — the original repro used it) is logged in
    with a populated library. Branch C is meaningless without it. If none is, record Branch C as
    `blocked` with that reason rather than running it.

---
**BRANCH A — item 1 success path: does the bulk capture actually populate the cache?**
(Requires a real ~378-game library and a LIVE CM socket. This is the branch that empirically
confirms the MEDIUM-confidence `oslist` wire shape.)

A1. Confirm the network is UNBLOCKED: `grep steampowered /etc/hosts` returns nothing, and
    `curl -sS -o /dev/null -w '%{http_code}\n' https://api.steampowered.com/` returns a code with
    exit 0.
A2. Fully quit GameLib. Delete `steam_metadata.json` (the `.bak` from P1 is your restore path) so
    the capture has a genuinely cold cache to repair. Record that you did this.
A3. Launch `npm start` (Electron). Note the wall-clock time when the window appears.
A4. Watch the log for the new capture summary line and record it VERBATIM, including the
    scoped/captured/skipped counts and whether `failed` was true.
A5. Record the elapsed time between "Steam: fetched N owned games" and the capture summary line.
    This is the real-world PICS latency the plan could not know.
A6. After the sync completes, inspect `steam_metadata.json` on disk. Record:
    - total entry count;
    - how many entries have `platformsCaptured: true`;
    - how many have `is_windows_native` present (any value);
    - how many have `is_mac_native: true`.
    The ratio of (captured) to (scoped) is the real-world `oslist` presence rate. A very low ratio
    would mean assumption A2 in RESEARCH.md ("absence correlates with Tool-type entries, not
    ordinary games") is WRONG — file that as a finding, do not smooth it over.
A7. Open the install dialog for a game you know ships both Windows and macOS builds (Terraria,
    appid 105600, is the 34.14 reference). Record whether the platform glyph row shows BOTH glyphs
    immediately, and whether the platform selector defaults sensibly. On a cold cache before this
    phase this rendered a confident Windows-only row.
A8. Repeat A3-A7 under `pnpm tauri:dev`. (Use `pnpm tauri:dev`, never `tauri dev` — the latter
    serves a stale static bundle.) Record both runtimes separately; a pass on one is not a pass on
    the other.

**BRANCH B — item 1 fail-soft.**
B1. There is no reliable way to force a PICS timeout while keeping the CM socket live from outside
    the app. Attempt it only if you can (e.g. by blocking mid-sync); otherwise record Branch B as
    **`n-a`** with the reason "cannot be forced externally; covered by the automated fail-soft tests
    in `platformCapture.test.ts` and `library.test.ts` (capture reports `failed: true` -> refresh
    still returns the success shape)". Do NOT record it as `pass`.

---
**BRANCH C — item 2: a Steam failure must not hide other runners' games.**
(Requires another runner's library populated — see P2.)

C1. Fully quit GameLib.
C2. Block Steam's CM bootstrap endpoint with the UNROUTABLE TEST-NET-3 address:
    `sudo sh -c 'printf "\n203.0.113.1 api.steampowered.com\n" >> /etc/hosts'`
    then flush DNS: `sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder`.
    NEVER use `127.0.0.1` — loopback REFUSES in ~1ms, so the timeout path never runs and a pending
    UI stays invisible. This is a ledgered failure of a previous gate's method, not a theoretical
    concern.
C3. VERIFY the block took, with the right instrument:
    `curl -sS --max-time 8 https://api.steampowered.com/ ; echo "exit=$?"` must print **exit=28**
    (timeout/hang). **exit=7** means "refused" — the block is wrong, fix it before continuing.
    Do NOT use `dig` to verify: `dig` does not read `/etc/hosts`.
C4. Launch `npm start`. Observe the Library screen and record, one row each:
    - Does an inline Steam notice appear stating the Steam library could not be synced?
    - Is it an INLINE ROW in normal flow, or does it cover/centre over the grid? (The defect this
      phase kills is a centred overlay. A centred overlay here is a FAIL, not a nitpick.)
    - Are the other runner's games VISIBLE and INTERACTIVE behind/below it? Click one to confirm
      interactivity — visibility alone is not the claim being tested.
    - Does the notice ever clear on its own? (It must not — the point is a terminal state.)
    - Take a screenshot and reference it in the ledger.
C5. Corroborate from the log: find the `Steam client not ready, skipping library refresh` warning.
    Record the line verbatim. If it is absent, the app did not take exit path 1 and the branch's
    premise did not hold — record `blocked`, not `pass`.
C6. With the block still in place, click the notice's **Retry** button. Record: does a syncing state
    appear, and does it return to the failed state (rather than spinning forever)? Click it a second
    time immediately and confirm the double-entry guard does not produce two concurrent syncs.
C7. Remove the block by CONTENT PATTERN, never by line count (34.14 found four lines where the plan
    anticipated two):
    `sudo sed -i '' '/steampowered\.com/d' /etc/hosts`
    then flush DNS as in C2.
C8. VERIFY the removal by REAL RESOLUTION, not by assuming the flush worked:
    `grep steampowered /etc/hosts` returns nothing AND
    `curl -sS -o /dev/null -w '%{http_code} exit=%{exitcode}\n' https://api.steampowered.com/`
    returns a real HTTP code with exit 0. 34.14 found the OS resolver still serving a stale
    203.0.113.1 after a flush while `/etc/hosts` was already clean.
C9. With the network restored and the app still open, click **Retry** again. Record whether the
    Steam library populates and the notice disappears. This is the whole point of a scoped retry.
C10. Repeat C1-C9 under `pnpm tauri:dev`. **The Tauri leg is the one that matters most for the IPC
     channel** — a missing preload listener slot fails SILENTLY on the sidecar, so an Electron-only
     pass would not detect it.

**BRANCH D — item 2 exit path 4 (hydration throw).**
D1. This cannot be forced without modifying source. Record as **`n-a`** with the reason "covered by
    the automated forcing test in `library.test.ts` (mocked `steamLibraryStore.set` throws ->
    `steamSyncStatus: 'failed'`, reason `sync-failed`, and no `'idle'` is emitted)". Do NOT record
    `pass`.

---
**CLEANUP (mandatory, each recorded as a ledger row with its verification evidence):**
- `/etc/hosts` block removed, verified by `grep steampowered /etc/hosts` returning nothing.
- DNS flushed AND verified by real resolution + a live HTTP response, not by assuming.
- `steam_metadata.json` state recorded (it will legitimately have been rebuilt by Branch A — that
  is the deliverable, not damage; note the new entry count vs the P1 backup).
- All GameLib instances quit.

**LEDGER COMPLETION:** every branch gets an explicit verdict — `pass`, `fail`, `blocked`, or `n-a`
with a reason. A branch whose surface could not render is `blocked` or `n-a`, never `pass`. File any
observation that contradicts a plan assumption as a FINDING, including RESEARCH.md's assumption A2
(`oslist` presence rate) and D-15's coherence question (does the disabled selector showing "macOS"
while the glyph row is suppressed read as incoherent in practice? — if yes, that is a finding to
raise, not a licence to change 34.14's pin).
  </how-to-verify>
  <verify>
    <human-check>Every branch row in `34.15-UAT.md` carries an explicit verdict; Branch A's cache measurements and PICS elapsed time are recorded with real numbers; Branch C's inline-vs-overlay observation and the other-runner interactivity click are both recorded on BOTH runtimes; the `/etc/hosts` removal and DNS flush are recorded as verified by real resolution.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-UAT.md` exists with `status: human_needed` in frontmatter until the developer discharges it
    - Branch A is recorded for BOTH `npm start` and `pnpm tauri:dev`, each with the verbatim capture summary log line and the on-disk cache counts
    - Branch C is recorded for BOTH runtimes, each with the `curl` exit-28 verification captured before the run
    - No row is marked `pass` for a surface that did not render
    - The cleanup section records the `/etc/hosts` removal verified by `grep` returning nothing AND by a live HTTP response
    - `filled_by` names the human who made the observations and states that the assistant transcribed rather than self-certified
  </acceptance_criteria>
  <done>Every branch of the D-16 ledger carries an explicit verdict on both runtimes, the cleanup rows are recorded with verification evidence, and the developer has discharged the gate.</done>
  <resume-signal>Type "approved" once every branch carries a verdict and the cleanup rows are recorded, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> system config (UAT only) | The forcing procedure asks for a `sudo` edit to `/etc/hosts` plus a DNS flush on the developer's own machine. |
| operator filesystem -> app cache (UAT only) | Branch A deletes `steam_metadata.json` to force a cold capture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.15-09-01 | Denial of Service | the `/etc/hosts` block left in place after testing | mitigate | The procedure is APPEND-ONLY (never a file rewrite), removal is by CONTENT PATTERN not line count (34.14 found four lines where two were expected), and cleanup is verified by `grep` returning nothing PLUS a live HTTP response rather than by assuming the flush worked. A forgotten block would silently break Steam for the operator's daily use. |
| T-34.15-09-02 | Tampering | Branch A's cache deletion losing operator data | mitigate | P1 takes a `.bak` beside the file before anything is deleted, and the cleanup section records the before/after entry counts. The rebuild is the deliverable being measured, not collateral. |
| T-34.15-09-03 | Repudiation | a `pass` recorded for a surface that never rendered | mitigate | Every branch requires log corroboration (C5's verbatim warning line, A4's verbatim summary line) before a verdict, and `blocked`/`n-a` are named as the correct verdicts otherwise. This project has ledgered a `pass` that covered an unreachable surface. |
| T-34.15-09-04 | Tampering | executor `git stash` stranding a concurrent session's work | mitigate | Task 1 explicitly forbids `git stash` and instructs the executor to STOP and report on an unrecognised dirty tree. Two data-loss events in this project came from exactly this, and a prior prohibition in prose did not prevent the second one. |
| T-34.15-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages anywhere in this phase. Package Legitimacy Gate not triggered. |
</threat_model>

<verification>
- `pnpm test:ci` green, counts recorded and compared to baseline
- `npx tsc --noEmit -p tsconfig.json` exits 0
- Every shipped-gate census command's literal output recorded in the summary
- `34.15-UAT.md` exists, every branch has a verdict, cleanup verified
</verification>

<success_criteria>
- Full suite, typecheck and lint green
- metadataCapture.ts, the three D-17 saboteurs, Group B/B1, the D-11 overlay, and the three sibling dialogs all proven untouched BY DIFF
- The four D-05 non-weakening line items evidenced
- The D-16 blocking gate authored with a concrete forcing procedure per branch, run on BOTH runtimes, and discharged by the developer
- `status` remained `human_needed` until discharge
</success_criteria>

<output>
Create `.planning/phases/34.15-steam-platform-signal-and-sync-integrity/34.15-09-SUMMARY.md` when done
</output>
