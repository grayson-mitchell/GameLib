---
phase: 34.15-steam-platform-signal-and-sync-integrity
verified: 2026-08-16T00:00:00Z
status: passed
score: 16/16 must-haves verified (16 CONTEXT decisions + 3 ROADMAP scope items)
overrides_applied: 0
---

# Phase 34.15: Steam platform-signal and sync integrity Verification Report

**Phase Goal:** Close the three pre-existing Steam defects that Phase 34.14's D-08 UAT gate
surfaced but which 34.14 never claimed to fix — bulk platform capture at sync time, a terminal
sync failure state that does not hide the whole library, and mac-side absent-is-not-negative
parity.

**Verified:** 2026-08-16
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### The three ROADMAP scope items

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | Bulk platform capture replaces lazy per-game-only capture; three-valued contract (`undefined`/`false`/`true`) intact | ✓ VERIFIED | `platformCapture.ts` — `parseOslistPlatforms` returns `null` (write nothing) for absent/empty/unrecognised `oslist`, never manufactures `false`; `mergePlatformCapture` is read-modify-write, carries forward `mac_arch*`/`forcedWindowsViaBottle`. `captureOwnedAppPlatforms` wired inline in `library.ts` Step 1.5, scoped to `!depotSignalCaptured()`. **Live UAT (Branch A, both runtimes):** cold cache 381/370/**0**/181 → post-sync 379/377/**377**/186 `is_windows_native` entries, `scoped=378 captured=363 skipped=15 failed=false`, reproduced byte-identical on Electron and Tauri. No collapse of `undefined→false` observed or coded anywhere in the diff. |
| 2 | Sync has a terminal failure state; failure does not hide the whole library | ✓ VERIFIED | `GlobalState.tsx` owns `steamSyncStatus: 'idle'\|'syncing'\|'failed'`, replacing `refreshingInTheBackground` (confirmed not read by the new resolver). `steamSyncStatus` emitted on all 4 documented `refresh()` exit paths (`library.ts:681,693,724,933,944`), including the previously-undocumented exit path 4 (hydration-loop throw). Preload allowlist entry `handleSteamSyncStatus` present and consumed. `SteamSyncNotice` renders inline (`Library/index.tsx:1026`, sibling to the pre-existing fenced overlay at `:1024`, D-11 unmodified). **Live UAT (Branch C, both runtimes):** inline notice appeared, GOG's 6+ games stayed visible AND clickable behind it, notice did not self-clear, Steam-scoped retry (`runner=steam`, never `runner=all`) round-tripped through failure→retry→success on both Electron and Tauri, confirming the sidecar preload slot is genuinely wired (the highest silent-failure-risk item in the phase). |
| 3 | Absent `is_mac_native` read as "no Mac build" — fixed WITHOUT loosening to `!== false` | ✓ VERIFIED | `hasSteamMacDepot` (`steamPlatformRow.ts:89-93`) is `gameInfo?.is_mac_native === true`, strict. `resolveDepotAvailability` widened (not forked) to a mac triple via the identical seed/probe idiom as Windows. `installFormIpc.ts:128` computes `hasMacDepot` the same strict way on the same round trip as `hasWindowsDepot`. Grep for `!== false` across all 3 phase-touched Steam files (`platformCapture.ts`, `steamPlatformRow.ts`, `installFormIpc.ts`) returns exactly 3 hits, **all three inside comments explaining what NOT to do** — zero in live code. The three `treatsAbsentAsAvailable`/`includesWindowsRegardless`/`readonlyRowLeaksDepot` saboteur gates in `steamPlatformRow.test.ts` are present and green, unchanged in shape. |

### Observable Truths (CONTEXT.md D-01..D-16)

| # | Decision | Status | Evidence |
|---|---------|--------|----------|
| 1 | D-01: bulk source is PICS `getProductInfo`, not `appdetails` | ✓ VERIFIED | `platformCapture.ts` imports `PlatformCapturePicsClient.getProductInfo`; zero `appdetails` reference in the module. Live UAT F-3: `appdetails` observed 403-throttled in the same session PICS succeeded — corroborates the override. |
| 2 | D-02: PICS write is a MERGE; empty `oslist` writes nothing | ✓ VERIFIED | `mergePlatformCapture` spreads `existing` first; `parseOslistPlatforms` returns `null` for absent/empty/all-unrecognised tokens, and the caller skips the write on `null` (`skippedCount += 1; continue`). |
| 3 | D-03: capture runs INLINE in `refresh()`, fail-soft | ✓ VERIFIED | `library.ts` Step 1.5, awaited before Step 2/3. Whole function body of `captureOwnedAppPlatforms` (Plan 09 fix) is inside one `try/catch` that never rethrows — closes the synchronous-throw gap code review's WR-03 flagged. Regression test proves the filter-step-throws case fails soft without reaching `getProductInfo`. |
| 4 | D-04: scoped to `!depotSignalCaptured()` | ✓ VERIFIED | `platformCapture.ts:245-246`, imports `depotSignalCaptured` verbatim from `metadataCapture.ts` (confirmed byte-identical diff since phase start per Plan 09's census). |
| 5 | D-05: non-consequence — unresolved stays reachable, safety layers not weakened | ✓ VERIFIED | Plan 09's D-05 audit (fail-open clause, pending row, Install-disable, `readonlyPlatformValue('pending')` pin) all confirmed present verbatim by direct read; `init()`'s independent push and the cached-library early returns are untouched. |
| 6 | D-06: real tri-state `steamSyncStatus`, not a patched boolean | ✓ VERIFIED | `GlobalState.tsx:144,312,1338`; `refreshingInTheBackground` is not read by `resolveSteamSyncIndicator`. |
| 7 | D-07: `steamSyncStatus` channel end to end, all 4 exit paths, preload allowlist entry | ✓ VERIFIED | `ipc.ts:657`, `preload/api/steam.ts:62` (`frontendListenerSlot('steamSyncStatus')`), `GlobalState.tsx:1337` subscribes. Live-confirmed reaching the renderer through the Tauri sidecar in UAT Branch C. |
| 8 | D-08: inline non-blocking notice with Steam-scoped retry, localisation gate BLOCKING | ✓ VERIFIED | `SteamSyncNotice/index.tsx` — no `position: absolute/fixed`, retry calls `refreshLibrary({ library: 'steam', ... })`. `gamelib.json` `library.steamSync.*` keys present; `genI18nGateScope.test.ts` green after Plan 09 folded both new fork-touched files into scope. |
| 9 | D-09: sync-state→render decision in a pure sibling module, RED-proven source gate, running-and-failing case covered | ✓ VERIFIED | `librarySyncIndicator.ts` + `librarySyncNoticeSource.test.ts` (RED-proven per Plan 08/VALIDATION sign-off, splices the verbatim known-bad `:1013-1018` expression and asserts `.toThrow()`). `resolveSteamSyncIndicator`'s branch 2 explicitly covers failed-with-cached-games. |
| 10 | D-10: one widget, two modes, inline row | ✓ VERIFIED | `SteamSyncNotice` component doc comment + implementation; single `<div className="SteamSyncNotice">` shape for both modes. |
| 11 | D-11: global foreground overlay untouched | ✓ VERIFIED | `Library/index.tsx:1024` unchanged (`grep -c` census in Plan 09 SUMMARY confirms `1`, byte-identical). |
| 12 | D-12: `resolveDepotAvailability` widened, not forked; 3 saboteurs reconciled explicitly | ✓ VERIFIED | Same function returns `{ depotSignalResolved, windowsDepotOffered, macDepotOffered }`; saboteur region diff since phase start is empty (Plan 09 census). |
| 13 | D-13: entire glyph row suppressed while unresolved, SteamDialog-scoped only | ✓ VERIFIED | `depotSignalResolved &&` gate at `SteamDialog/index.tsx:412`; `availablePlatforms` (unfiltered) still feeds the other 4 dialogs (census: `availablePlatforms={availablePlatforms}` appears exactly 4 times, none inside `<SteamDialog>`). |
| 14 | D-14: Windows-default-on-unknown with rationale, re-derives on resolve | ✓ VERIFIED | `getDefaultplatform` re-derivation wired via `steamHeaderPlatforms`/`macDepotOffered` effect in `index.tsx`; Install stays disabled for the whole pending window so re-derivation is safe. |
| 15 | D-15: 34.14 pending-row pin untouched, B1 gate unmodified | ✓ VERIFIED | `readonlyPlatformValue('pending')` still pins `'Mac'`, confirmed byte-identical inside the DO-NOT-TOUCH saboteur region (Plan 09 census: zero changed lines in Group B through Group C). |
| 16 | D-16: ONE blocking human UAT gate, concrete forcing procedure, discharged | ✓ VERIFIED | `34.15-UAT.md` — 4 pass / 2 n-a / 0 fail, real measured evidence (log lines, disk state, screenshots referenced) from a live ~378-game library on both runtimes, `filled_by: developer`, status `passed`. |

**Score:** 16/16 CONTEXT decisions verified; 3/3 ROADMAP scope items verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/backend/storeManagers/steam/platformCapture.ts` | oslist parser, merge-write, D-04-scoped bulk PICS capture | ✓ VERIFIED | 324 lines; exports `parseOslistPlatforms`, `mergePlatformCapture`, `captureOwnedAppPlatforms`; wired into `library.ts`; regression-tested for the synchronous-throw case fixed in Plan 09 |
| `src/backend/storeManagers/steam/library.ts` (Step 1.5 + exit-path emits) | inline fail-soft capture + 4-exit-path `steamSyncStatus` | ✓ VERIFIED | all 5 `sendFrontendMessage('steamSyncStatus', ...)` call sites present (`:681,693,724,933,944`) |
| `src/common/types/ipc.ts`, `src/preload/api/steam.ts`, `GlobalState.tsx`, `frontend/types.ts`, `ContextProvider.tsx` | `steamSyncStatus` channel declared, allowlisted, consumed | ✓ VERIFIED | full 5-touchpoint census present; live-confirmed through the sidecar in UAT |
| `src/common/types/steam.ts` — `SteamBottleEligibilityVerdict.hasMacDepot` | mac field on the eligibility verdict | ✓ VERIFIED | present, populated same round trip as `hasWindowsDepot` in `installFormIpc.ts` |
| `src/frontend/screens/Library/components/InstallModal/steamPlatformRow.ts` | widened `resolveDepotAvailability`, `hasSteamMacDepot`, `resolveSteamHeaderPlatforms` (CR-01 fix) | ✓ VERIFIED | all three present; `resolveSteamHeaderPlatforms` tests assert CONTENT (`.toContain`/`.not.toContain`), not merely gating |
| `src/frontend/screens/Library/components/InstallModal/SteamDialog/index.tsx` | glyph row gated + fed from resolved content | ✓ VERIFIED | `headerPlatforms` prop (renamed from `availablePlatforms`) fed by `resolveSteamHeaderPlatforms` in the parent |
| `src/frontend/screens/Library/librarySyncIndicator.ts` | pure `resolveSteamSyncIndicator` resolver | ✓ VERIFIED | 4-branch resolver, zero runtime imports, covers running-and-failing + idle-empty |
| `src/frontend/screens/Library/components/SteamSyncNotice/index.tsx` | inline 2-mode widget with retry | ✓ VERIFIED | present, both modes, retry wired to scoped `refreshLibrary` |
| `.planning/phases/.../34.15-UAT.md` | D-16 blocking ledger | ✓ VERIFIED | discharged, `status: passed`, real measured evidence |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `platformCapture.ts` | `library.ts` `refresh()` | `await captureOwnedAppPlatforms(...)` between Step 1 and Step 2/3 | WIRED | confirmed at `library.ts` Step 1.5 |
| `library.ts` exit paths | `GlobalState.tsx` | `sendFrontendMessage('steamSyncStatus', ...)` → `preload` allowlist → `handleSteamSyncStatus` subscriber | WIRED | live-confirmed on both runtimes in UAT Branch C |
| `resolveDepotAvailability` output | `SteamDialog` header | `resolveSteamHeaderPlatforms(platforms, windowsDepotOffered, macDepotOffered)` → `headerPlatforms` prop | WIRED | CR-01 fix; content-asserting tests pass |
| `resolveSteamSyncIndicator` | `Library/index.tsx` render | `{ mode: steamSyncMode } = resolveSteamSyncIndicator(...)`; `steamSyncMode !== 'hidden' && <SteamSyncNotice mode={steamSyncMode} />` | WIRED | live-confirmed inline placement, no overlay, on both runtimes |
| `SteamSyncNotice` retry button | `GlobalState.refreshLibrary` | `refreshLibrary({ library: 'steam', origin: 'steam-sync-notice-retry' })` | WIRED | live-confirmed `runner=steam`-scoped, never `runner=all`, on both runtimes |

### Behavioral Spot-Checks / Live Evidence

This phase's core empirical claims (bulk PICS capture rate, sync-failure UI shape, retry scoping)
were verified against a real ~378-game Steam library on both Electron and Tauri runtimes by the
developer in `34.15-UAT.md`, not merely by unit test. Re-run of the phase's automated test files
during this verification pass:

```
npx jest platformCapture.test.ts library.test.ts librarySyncIndicator.test.ts
     librarySyncNoticeSource.test.ts steamPlatformRow.test.ts steamDialogSource.test.ts
     steamEligibilityWiring.test.ts steamSyncStatusWiring.test.ts --silent
→ 8 suites, 458 tests, all passed
```

`npx tsc --noEmit -p tsconfig.json` → 0 errors (re-run, confirms verified_current_state).

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX` debt markers in any phase-touched file (checked directly against 16 core
files spanning backend, IPC, and frontend layers — zero matches).

### Code Review Findings — Disposition

| Finding | Severity | Status | Assessment |
|---|---|---|---|
| CR-01 — SteamDialog header renders stale seed after signal resolves | BLOCKER | RESOLVED (`77f094bfd`) | Verified real: `resolveSteamHeaderPlatforms` extracted as a pure function, tests assert row CONTENT (not just gating) against both the stale-seed-omits-Mac and the readonly-macos-leaks-Windows reachable states named in the finding. |
| WR-01 — `'selectable'` option list always offered macOS regardless of `macDepotOffered` | WARNING | RESOLVED (`dabd1ccc4`) | Verified real: `selectSteamPlatformOptions` gained a required `hasMacDepot` param, applied only to `'selectable'`; confirmed the pinned 34.13-era test at `:140-143` was NOT one of the DO-NOT-TOUCH saboteurs before changing it; new `macIncludedRegardless` saboteur RED-proves the fix. |
| WR-02 — two writers (`platformCapture.ts` PICS, `games.ts` `appdetails`) share the same fields with no precedence rule | WARNING | OPEN, not blocking | Both writers write real, source-confirmed booleans on write — neither collapses `undefined→false` (the phase's hard constraint), so this does not violate the three-valued contract. It is a data-quality/provenance gap (last-writer-wins), not a contract break. Live evidence (UAT F-2/F-3) shows the two sources were complementary in this session, not contradictory — the disagreement is a documented theoretical risk, not an observed defect. Judged compatible with the phase's stated goal (item 1 is "reduce how often the signal is missing," which is achieved; reconciling two present-but-disagreeing signals was never one of the three ROADMAP items). Should be tracked as a follow-up, not treated as unmet phase goal. |
| WR-03 — "all 4 exit paths" depended on an unenforced cross-module contract | WARNING | Effectively closed by Plan 09's fix | The synchronous-throw gap WR-03 named was fixed in Plan 09 (`9d03d2cb2`): `captureOwnedAppPlatforms`'s try/catch now wraps its entire body including the filter step, so the function's own "never throws" contract is now actually enforced inside the module rather than merely documented. Verified directly by reading the current source and confirming the regression test exists and passes. |
| WR-04 — retry gives no visible feedback while in flight when the library already has cached games | WARNING | OPEN, not blocking | Real UX gap (notice disappears between click and next terminal event when `steamLibraryCount > 0`), but item 2's ROADMAP charter is "gives the sync a terminal failure state and stops it from hiding the whole library" — both of which hold; a missing transient "retrying…" indicator does not reopen either. Not logged to `deferred-items.md` or a todo file; recommend the developer file a follow-up todo so it is not lost, but it does not block this phase's goal. |

Neither WR-02 nor WR-04 is formally logged as a deferred item or todo — flagged here as a
process gap for the developer's attention, not as a phase-goal failure (see the FAILED-worthiness
analysis above: neither one, if left exactly as-is, prevents the three ROADMAP items from being
true in the codebase today).

### Requirements Coverage

No REQ-IDs were minted for this phase. Coverage is tracked by the 16 CONTEXT.md decision IDs — see
the Observable Truths table above. All 16 are accounted for by at least one plan's `decisions:`
frontmatter field, cross-referenced by direct grep against `34.15-CONTEXT.md` decision headers (no
orphaned or unclaimed decisions found).

### Human Verification Required

None. The phase's one blocking human gate (D-16) is already discharged with real, dated, log-corroborated
evidence from a live ~378-game library on both runtimes — not a prediction, not a self-certification.
No new human-testable surface was introduced by this verification pass that D-16 did not already cover.

### Gaps Summary

No gaps found against the phase's stated goal. The three ROADMAP scope items are all observably
true in the codebase and confirmed live on both runtimes (Electron and Tauri) against a real Steam
library, not merely by test. The single BLOCKER the phase's own code review found (CR-01) was
fixed and the fix was verified to assert the row's rendered content, not merely its gating — closing
exactly the class of "passing-but-meaningless test" this project has a documented history of. Two
of four review WARNINGs were also fixed and verified (WR-01, effectively WR-03); the remaining two
(WR-02, WR-04) are real but orthogonal to the three ROADMAP items and do not violate the phase's
hard constraints (three-valued contract intact, no `!== false` loosening in live code). They should
be tracked as follow-up work — recommend filing a todo for WR-02 (writer precedence) and WR-04
(retry-in-flight feedback) so they are not lost, since neither currently has a deferred-items.md or
todo entry.

---

_Verified: 2026-08-16_
_Verifier: Claude (gsd-verifier)_
