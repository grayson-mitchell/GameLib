---
phase: 34.15-steam-platform-signal-and-sync-integrity
plan: 04
subsystem: steam-install-modal
tags: [typescript, jest, steam, install-modal, pure-function]

# Dependency graph
requires:
  - phase: 34.15-03
    provides: "SteamBottleEligibilityVerdict.hasMacDepot / probeHasMacDepot on the eligibility probe round trip, which this plan's widened resolveDepotAvailability consumes as an input"
provides:
  - "resolveDepotAvailability returns { depotSignalResolved, windowsDepotOffered, macDepotOffered } -- one function answering both platform questions from one resolution moment"
  - "hasSteamMacDepot(gameInfo) -- the mac-side mirror of hasSteamWindowsDepot, is_mac_native === true"
  - "macTreatsAbsentAsAvailable and macLoosensToNotFalse saboteur tests proving the mac branch is non-vacuous and does not loosen to !== false"
affects: [InstallModal/index.tsx (Plan 07), SteamDialog (Plan 07)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Deliberate asymmetric fail-open/fail-conservative branches inside one shared resolver function, each documented at length so a reviewer finding only one branch cannot mistake it for an oversight"
    - "Reconciliation-by-comment when a CONTEXT.md decision's stated rationale (three saboteurs 'pin' the widened function) does not match what the test file actually shows -- documented in code and in this summary rather than silently corrected"

key-files:
  created: []
  modified:
    - src/frontend/screens/Library/components/InstallModal/steamPlatformRow.ts
    - src/frontend/screens/Library/components/InstallModal/__tests__/steamPlatformRow.test.ts

key-decisions:
  - "D-12 (34.15 CONTEXT): resolveDepotAvailability widened in place, not split into a sibling function -- one function keeps owning 'what do we know about this game's platforms' for both platforms"
  - "macDepotOffered = hasMacDepot && depotSignalCaptured, with NO fail-open disjunct -- deliberately asymmetric with windowsDepotOffered's `|| (probeSettled && !depotSignalCaptured)`, because an unbacked mac install is broken, not merely suboptimal"
  - "Reconciliation finding: CONTEXT.md D-12 states the widening 'widens a function whose behaviour three shipped saboteur gates pin.' Reading the test file shows this is not the actual mapping -- none of the three D-17 saboteurs (treatsAbsentAsAvailable, includesWindowsRegardless, readonlyRowLeaksDepot) targets resolveDepotAvailability; they target hasSteamWindowsDepot and selectSteamPlatformOptions instead. All three are confirmed byte-identical via git diff -U0 and stay green with zero changes. Documented in a comment at the top of the block that DOES change (the resolveDepotAvailability describe block) rather than silently correcting CONTEXT.md."

patterns-established:
  - "A widened pure resolver's asymmetric branches are each documented with the OTHER branch's shape named explicitly, so 'why does only one side have this clause' is answered inline rather than requiring cross-referencing 34.14's CONTEXT.md"

requirements-completed: []

# Metrics
duration: ~25min
completed: 2026-08-16
---

# Phase 34.15 Plan 04: Widen resolveDepotAvailability for the Mac Signal Summary

**`resolveDepotAvailability` now returns `{ depotSignalResolved, windowsDepotOffered, macDepotOffered }` from one seed/probe resolution moment, with the mac branch deliberately withholding 34.14's Windows fail-open clause -- proven non-vacuous by two new named saboteurs, while the three pre-existing D-17 saboteur gates are confirmed byte-identical.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-08-16
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added `hasSteamMacDepot(gameInfo)`, the mac-side mirror of `hasSteamWindowsDepot`, returning `gameInfo?.is_mac_native === true`, placed immediately beside its Windows sibling with a doc comment recording the `library.ts:785` `?? false` collapse asymmetry a future reader would otherwise trip on.
- Widened `ResolveDepotAvailabilityInput` with `seedHasMacDepot` / `probeHasMacDepot` (both required) and `ResolveDepotAvailabilityOutput` with `macDepotOffered`.
- Implemented `macDepotOffered = hasMacDepot && depotSignalCaptured` using the identical seed/probe selection idiom as the Windows line above it, but WITHOUT the Windows fail-open disjunct -- documented at length in-line, naming both branches so a reviewer reading only one cannot mistake the omission for a bug.
- Extended the 8-fixture `resolveDepotAvailability` test block: added mac fields to every existing row (preserving every original Windows assertion unchanged), renamed the D-04 fail-open row to name the 34.15 D-12 asymmetry it now also demonstrates, and added three new mac-only rows.
- Added two new named saboteurs: `macTreatsAbsentAsAvailable` (copies the Windows fail-open clause onto mac; disagrees with the real function on the asymmetry row) and `macLoosensToNotFalse` (the `is_mac_native !== false` shape the ROADMAP prohibits; disagrees with `hasSteamMacDepot` on `{}`).
- Confirmed via `git diff -U0` that the `describe('the D-17 gates are not vacuous', ...)` block (the three pre-existing saboteurs `treatsAbsentAsAvailable`, `includesWindowsRegardless`, `readonlyRowLeaksDepot`) is byte-identical -- no lines inside that block were added, removed, or changed.

## Task Commits

Each task was committed atomically:

1. **Task 1: hasSteamMacDepot seed reader + widen resolveDepotAvailability** - `780d25188` (feat)
2. **Task 2: reconcile the test surface against the corrected mapping** - `dfa08b1fb` (test)

_No separate plan-metadata commit was made per this plan's instructions -- STATE.md/ROADMAP.md updates are owned by the orchestrator, not this executor._

## Files Created/Modified
- `src/frontend/screens/Library/components/InstallModal/steamPlatformRow.ts` - `hasSteamMacDepot` added beside `hasSteamWindowsDepot` (DO-NOT-TOUCH, confirmed zero removed lines from that function via `git diff`); `ResolveDepotAvailabilityInput`/`Output` widened; `resolveDepotAvailability` computes `macDepotOffered` with the documented asymmetry; module header updated to name all three outputs. `readonlyPlatformValue` and `selectSteamPlatformOptions` untouched (confirmed via `git diff`).
- `src/frontend/screens/Library/components/InstallModal/__tests__/steamPlatformRow.test.ts` - 8 existing `resolveDepotAvailability` fixtures gained mac fields with Windows assertions unchanged; 3 new mac-only rows added; `macTreatsAbsentAsAvailable` and `macLoosensToNotFalse` saboteurs added; the D-17 saboteur block left byte-identical.

## Decisions Made
- Followed D-12 exactly: widened the existing function, added no sibling.
- Did NOT add the Windows fail-open disjunct to `macDepotOffered` -- per CONTEXT.md's explicit instruction that this asymmetry must not be "corrected" toward symmetry.
- Zero `!== false` anywhere in production code (verified: `grep -v '^\s*[/* ]' steamPlatformRow.ts | grep -c '!== false'` → `0`). The `!== false` literal exists only inside the two saboteur test functions, whose entire purpose is to demonstrate the prohibited shape by name -- the same accepted precedent as the three shipped Windows-side saboteurs, and as 34.15-03's `macTreatsAbsentAsAvailable` in `installFormIpc.test.ts`.

## Deviations from Plan

### Auto-fixed Issues

None required beyond what the plan explicitly anticipated. The plan's Task 1 acceptance criteria deliberately expected `npx tsc --noEmit` to report errors after Task 1 alone (8 errors in the test file, 1 in `InstallModal/index.tsx`) as the intended TypeScript-level RED; this was verified to match exactly before proceeding to Task 2, which closed the test-file errors to zero.

### Reconciliation finding (documented per plan instruction, not a deviation from the plan's own text)

CONTEXT.md's D-12 states this widening "widens a function whose behaviour three shipped saboteur gates pin." The plan's own `<interfaces>` section (line 107-111) had already corrected this before implementation began, naming the true mapping: `treatsAbsentAsAvailable` targets `hasSteamWindowsDepot`; `includesWindowsRegardless` and `readonlyRowLeaksDepot` target `selectSteamPlatformOptions`'s `'selectable'` and `'readonly-macos'` branches. None of the three reaches `resolveDepotAvailability`. This was reconciled exactly as the plan's Task 2 instructed: a comment was placed at the top of the `resolveDepotAvailability` describe block (the block that DOES change) stating the correction explicitly, and `git diff -U0` was run to confirm the D-17 block has zero changed lines. This is not a deviation requiring a Rule 1-4 classification -- it is the plan executing its own explicitly-planned reconciliation step.

---
**Total deviations:** 0
**Impact on plan:** None. Plan executed exactly as written, including its own pre-identified test-mapping correction.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 07 (which owns `InstallModal/index.tsx` and `SteamDialog`) can now call the widened `resolveDepotAvailability` and read `macDepotOffered` directly. The sole remaining `npx tsc --noEmit` error project-wide is in `InstallModal/index.tsx:201` (the still-unwidened call site), which is exactly Plan 07's declared scope -- confirmed by re-running `npx tsc --noEmit -p tsconfig.json` after both tasks landed.
- `npx jest src/frontend/screens/Library/components/InstallModal` is green: 10 suites, 319 tests (up from prior baseline; 42 tests in `steamPlatformRow.test.ts` alone, up from 34).
- No blockers.

## Self-Check: PASSED

- `src/frontend/screens/Library/components/InstallModal/steamPlatformRow.ts` - FOUND
- `src/frontend/screens/Library/components/InstallModal/__tests__/steamPlatformRow.test.ts` - FOUND
- Commit `780d25188` - FOUND in `git log --oneline`
- Commit `dfa08b1fb` - FOUND in `git log --oneline`

---
*Phase: 34.15-steam-platform-signal-and-sync-integrity*
*Completed: 2026-08-16*
