---
phase: 31-tauri-ipc-re-plumb-slice-2-settings-and-config
plan: 03
type: execute
wave: 2
depends_on: ["31-01", "31-02"]
files_modified:
  - .planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-PORTED-CHANNELS.md
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
autonomous: true
requirements: [REQ-31-02, REQ-31-05, REQ-31-06, REQ-31-07]

must_haves:
  truths:
    - "31-PORTED-CHANNELS.md enumerates the write path, the six generic reads, and the three real dialog members as ported this phase"
    - "31-PORTED-CHANNELS.md names the Sync-pair and shell/clipboard no-ops explicitly as deferred (boundary declared, not discovered)"
    - "SEAM.md section 1 gains the newly-ported settings/config + dialog rows; section 3's dialog priority-2 row is retired"
    - "SEAM.md Accepted Constraints records the D-02 Tauri/Electron settings divergence"
    - "The deferred live settings/dialog UAT is logged (D-05); the phase claim reads 'wired and unit-proven', not 'hardware-proven'"
  artifacts:
    - path: ".planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-PORTED-CHANNELS.md"
      provides: "declared ported-channel list mirroring 30-PORTED-CHANNELS.md"
      contains: "setSetting"
    - path: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      provides: "section 1/3 update + D-02 Accepted Constraint"
      contains: "dialog"
  key_links:
    - from: "31-PORTED-CHANNELS.md"
      to: "SEAM.md section 1"
      via: "cross-reference by filename (mirrors 30-PORTED-CHANNELS.md)"
      pattern: "PORTED-CHANNELS"
---

<objective>
Produce the declared ported-channel list artifact for slice 2 and reconcile SEAM.md — the boundary
this phase established must be DECLARED, not left for Phase 32 to rediscover.

Purpose: 31-PORTED-CHANNELS.md is the artifact Phase 32 starts from (mirroring how this phase
started from 30-PORTED-CHANNELS.md). SEAM.md is the governing document — its section 1/3 tables and
Accepted Constraints must reflect what shipped so the next slice reads an accurate seam.

Output: 31-PORTED-CHANNELS.md; SEAM.md sections 1/3 updated, dialog priority-2 row retired, D-02
divergence recorded as an Accepted Constraint, deferred UAT logged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-PORTED-CHANNELS.md
@.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
@.planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-RESEARCH.md
@.planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-01-PLAN.md
@.planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 31-PORTED-CHANNELS.md mirroring 30-PORTED-CHANNELS.md</name>
  <files>.planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/30-tauri-ipc-re-plumb-slice-1-install-uninstall-update-check/30-PORTED-CHANNELS.md (the exact format to mirror: header, claim-scope note, "Ported this phase" table, "Deliberately NOT ported" section)
    - .planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-01-SUMMARY.md and 31-02-SUMMARY.md (what actually shipped — verify the doc against these, not against intent)
    - .planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-RESEARCH.md (Q1/Q2 — the corrected port list + the infrastructure-only dialog note)
  </read_first>
  <action>
    Create 31-PORTED-CHANNELS.md following 30-PORTED-CHANNELS.md's structure verbatim. Include:
    (a) A header + claim-scope note stating each row means "registered on the sidecar and no longer
    marker-rejects" (or, for the dialog members, "real Tauri behavior, unit-proven"), NOT "the UI
    flow works end-to-end" — carry Phase 30's honesty framing. Add the D-05 note that the three
    dialog members are DECLARED INFRASTRUCTURE (RESEARCH Q2: zero in-scope flow reaches them),
    proven by direct electronStub unit test, not a settings-screen E2E.
    (b) A "Ported this phase" table with columns Channel / Kind / Registration module or real code
    reached / Requirement, listing: setSetting (send, GlobalConfig/GameConfig.setSetting, REQ-31-02),
    writeConfig (invoke, backend/utils writeConfig, REQ-31-02), getMaxCpus, showUpdateSetting,
    getLogContent, getSystemInfo, hasExecutable, isNative (invoke reads, REQ-31-01), and
    showMessageBox/showErrorBox/showSaveDialog (rustInvoke, RUST_DIALOG_MESSAGE / RUST_DIALOG_SAVE,
    REQ-31-03).
    (c) A "Deliberately NOT ported" section naming, with reason: getUserInfo (Epic-only, wrong
    screen), readConfig (Legendary-only helper), showMessageBoxSync/showOpenDialogSync (logged
    no-op, sync-over-async, Phase 33), showLogFileInFolder/copySystemInfoToClipboard (D-04 logged
    no-ops, Phase 33 shell/clipboard clusters), and the runner tool-version / EOS-overlay / egsSync
    channels (Invariant B, D-01). Verify each name against 31-RESEARCH.md Q1's table.
  </action>
  <verify>
    <automated>grep -q "showMessageBox" .planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-PORTED-CHANNELS.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and mirrors 30-PORTED-CHANNELS.md's section structure (claim-scope note, Ported table, Deliberately NOT ported).
    - The Ported table lists all 8 settings/config channels + the 3 dialog members, each with its REQ-31-0x id.
    - The Deliberately NOT ported section names getUserInfo, readConfig, the Sync pair, the two D-04 no-ops, and the runner/EOS/egsSync channels with reasons.
    - The dialog members are labelled DECLARED INFRASTRUCTURE (unit-proven, not E2E) per RESEARCH Q2.
  </acceptance_criteria>
  <done>31-PORTED-CHANNELS.md exists, mirrors the Phase 30 artifact, and declares both the ported set and the deferred boundary with reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile SEAM.md — move ported rows to section 1, retire the dialog priority-2 row, record D-02, log deferred UAT</name>
  <files>.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</files>
  <read_first>
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md (section 1 Ported; section 3 Deferred table incl. the dialog priority-2 row ~line 220 and the IPC re-plumb paragraph ~line 229; Accepted Constraints ~line 264; Invariants A/B)
    - .planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-PORTED-CHANNELS.md (Task 1 output — the source of truth for what moved)
    - .planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-RESEARCH.md (D-02 divergence rationale for the Accepted Constraint wording)
  </read_first>
  <action>
    Update SEAM.md to reflect slice 2:
    (a) In section 1 (Ported), add a "settings/config + dialog cluster (real, Phase 31)" subsection
    mirroring the "Steam QR login + native install slice (real, Phase 30)" subsection shape, that
    references 31-PORTED-CHANNELS.md by filename and names the write path, the six generic reads, and
    the three real dialog members. Update the IPC re-plumb running tally paragraph (currently "13
    wired total") to add this slice's channels.
    (b) In section 3 (Deferred), RETIRE the dialog priority-2 row: since showMessageBox/showErrorBox/
    showSaveDialog are now real, the only remaining dialog members are the two Sync no-ops — remove
    the row or rewrite it to say the dialog cluster is closed except the Sync pair (deferred to
    Phase 33). Do NOT leave it implying the async members are still deferred.
    (c) In Accepted Constraints, add a "D-02 (Phase 31) — Tauri/Electron settings divergence
    (ACCEPTED, document-only)" entry: the sidecar persists settings locally through the store layer /
    raw graceful-fs write and pushes no settingsChanged reflect to the Electron build; consistent
    with the Phase 30 D-03 two-token divergence and Phase 29 D-07 clobber; converge at the Phase 35
    cutover. Cross-reference the code comment added in settingsFlowRegistration.ts (Plan 31-01).
    (d) Log the deferred live UAT (D-05): note that the settings write/reflect and the native dialog
    rendering under tauri:dev are unit-proven but their live UAT is deferred — the phase claim is
    "wired and unit-proven", not "hardware-proven". Do not alter Invariants A/B.
  </action>
  <verify>
    <automated>grep -q "31-PORTED-CHANNELS" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</automated>
  </verify>
  <acceptance_criteria>
    - SEAM.md section 1 has a Phase 31 settings/config + dialog subsection referencing 31-PORTED-CHANNELS.md by filename.
    - The section 3 dialog priority-2 row no longer implies the async members are deferred (retired or rewritten to name only the Sync pair).
    - Accepted Constraints contains a "D-02 (Phase 31) — Tauri/Electron settings divergence" entry cross-referencing the settingsFlowRegistration.ts comment.
    - A deferred live-UAT note (D-05) is present; Invariants A and B are unchanged.
  </acceptance_criteria>
  <done>SEAM.md reflects slice 2: ported rows in section 1, dialog priority-2 row retired, D-02 constraint recorded, deferred UAT logged, Invariants A/B untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (documentation only) | This plan modifies planning artifacts only; no code, no trust boundary crossed, no new runtime surface added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Information disclosure | 31-PORTED-CHANNELS.md / SEAM.md overstating what shipped | mitigate | Task 1's claim-scope note and Task 2's deferred-UAT log keep the phase claim at "wired and unit-proven", not "hardware-proven" (D-05), preventing a later reader from treating registration as end-to-end proof. Severity: low. |
| T-31-SC | Tampering | npm/cargo installs | accept | No packages, no code — documentation only. |
</threat_model>

<verification>
- `test -f .planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-PORTED-CHANNELS.md` succeeds.
- `grep -q "31-PORTED-CHANNELS" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` succeeds.
- SEAM.md section 3 dialog row no longer lists the async members as deferred.
</verification>

<success_criteria>
- 31-PORTED-CHANNELS.md exists, mirrors 30-PORTED-CHANNELS.md, and declares the ported set + deferred boundary with reasons.
- SEAM.md section 1 gains the Phase 31 subsection; section 3 dialog priority-2 row retired; D-02 constraint recorded; deferred UAT logged.
- The phase's honest claim is preserved end to end: wired and unit-proven, not hardware-proven.
</success_criteria>

<output>
Create `.planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-03-SUMMARY.md` when done
</output>
