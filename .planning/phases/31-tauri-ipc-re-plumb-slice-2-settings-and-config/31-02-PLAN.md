---
phase: 31-tauri-ipc-re-plumb-slice-2-settings-and-config
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/types/sidecarTransport.ts
  - src-tauri/src/main.rs
  - src/backend/sidecar/electronStub.ts
  - src/backend/sidecar/__tests__/dialogStub.test.ts
autonomous: true
requirements: [REQ-31-03, REQ-31-04, REQ-31-05, REQ-31-07]

must_haves:
  truths:
    - "electronStub.dialog.showMessageBox forwards to Rust via requestRustInvoke(RUST_DIALOG_MESSAGE) and maps the bool result to {response, checkboxChecked:false}"
    - "electronStub.dialog.showErrorBox forwards to Rust via RUST_DIALOG_MESSAGE (error kind) and returns void"
    - "electronStub.dialog.showSaveDialog forwards to Rust via requestRustInvoke(RUST_DIALOG_SAVE) and maps Option<FilePath> to {canceled, filePath}"
    - "A requestRustInvoke rejection resolves each dialog member as a safe default (never throws to the caller)"
    - "showMessageBoxSync/showOpenDialogSync stay logged no-ops returning a safe default"
    - "shell.showItemInFolder and clipboard.writeText emit a console.warn (logged no-op, no longer silent)"
    - "Two new match arms (dialog_message, dialog_save) exist in dispatch_rust_channel and cargo check passes"
  artifacts:
    - path: "src/common/types/sidecarTransport.ts"
      provides: "RUST_DIALOG_MESSAGE + RUST_DIALOG_SAVE constants in RUST_INVOKE_CHANNELS allowlist"
      contains: "RUST_DIALOG_MESSAGE"
    - path: "src-tauri/src/main.rs"
      provides: "dialog_message + dialog_save match arms in dispatch_rust_channel"
      contains: "dialog_message"
    - path: "src/backend/sidecar/electronStub.ts"
      provides: "real showMessageBox/showErrorBox/showSaveDialog + logged D-04 no-ops"
      contains: "RUST_DIALOG_MESSAGE"
  key_links:
    - from: "electronStub.ts dialog.showMessageBox"
      to: "dispatch_rust_channel dialog_message"
      via: "requestRustInvoke(RUST_DIALOG_MESSAGE)"
      pattern: "requestRustInvoke\\(RUST_DIALOG_MESSAGE"
    - from: "electronStub.ts dialog.showSaveDialog"
      to: "dispatch_rust_channel dialog_save"
      via: "requestRustInvoke(RUST_DIALOG_SAVE)"
      pattern: "requestRustInvoke\\(RUST_DIALOG_SAVE"
---

<objective>
Close SEAM.md §3's `dialog` priority-2 cluster by giving the three async members
(`showMessageBox`, `showErrorBox`, `showSaveDialog`) real Tauri behavior via the existing generic
`requestRustInvoke()` → `dispatch_rust_channel()` → `tauri-plugin-dialog` path, and upgrade the
D-04 `shell`/`clipboard` conveniences from SILENT no-ops to LOGGED no-ops.

Purpose: These three dialog members are declared INFRASTRUCTURE, not flow-driven — RESEARCH Q2
found zero in-scope settings/config flow reaches them. Build them real anyway (locked D-03, closes
the dialog cluster) and validate via direct `electronStub.dialog.*` unit tests, never a
settings-screen E2E. The D-04 fix closes checklist step 3's silent-no-op gap.

Output: two new `RUST_DIALOG_*` constants; two new Rust match arms; three real dialog members +
two logged no-ops in `electronStub.ts`; extended `dialogStub.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-RESEARCH.md
@.planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-PATTERNS.md

<interfaces>
<!-- Contracts extracted from 31-RESEARCH.md Q3 + 31-PATTERNS.md item 3. Rust side is fully
     pre-generalized (plugin registered, AppHandle param present, worker-thread dispatch correct)
     — adding the two arms is a pure data change. -->

Existing constants (sidecarTransport.ts ~lines 151,158-164): `RUST_DIALOG_OPEN = 'dialog_open'`
plus `RUST_INVOKE_CHANNELS = [RUST_KEYRING_GET, RUST_KEYRING_SET, RUST_KEYRING_DELETE,
RUST_KEYRING_AVAILABLE, RUST_DIALOG_OPEN] as const`. A channel absent from RUST_INVOKE_CHANNELS is
refused before a frame is emitted (T-28-03 allowlist).

Existing electronStub forward pattern to mirror (electronStub.ts:139-164, showOpenDialog):
try { const r = await requestRustInvoke(RUST_DIALOG_OPEN, [options]); return typeof r==='string' ?
{canceled:false, filePaths:[r]} : {canceled:true, filePaths:[]} } catch (e) { console.warn(...);
return {canceled:true, filePaths:[]} }. Never throw to the caller (total-method convention).
electronStub.ts MUST NOT import backend/logger — use console.warn (file header lines 35-41).

Existing Rust arm to mirror (main.rs:341-361, "dialog_open"): calls
app.dialog().file().blocking_pick_folder()/blocking_pick_file(), returns Ok(Value::String(path))
or Ok(Value::Null). dispatch_rust_channel signature already takes &AppHandle; dispatched on a
spawned worker thread (main.rs:547) — satisfies blocking_show()/blocking_save_file() thread rule.

Rust API shapes (CITED docs.rs/tauri-plugin-dialog):
- message: app.dialog().message(msg).title(t).kind(MessageDialogKind::Error|Warning|Info)
  .blocking_show() -> bool. Map true→response:0, false→response:1; checkboxChecked always false
  (no v2 equivalent — documented accepted gap; zero real callers read it).
- save: app.dialog().file().set_file_name(...).add_filter(...).blocking_save_file()
  -> Option<FilePath>. None→canceled, Some(path)→chosen path (same Option shape as pick_folder).
- showErrorBox(title, content) → Electron returns void; map to message(content).kind(Error)
  .title(title).blocking_show(), discard the return.

Current D-04 SILENT stubs to make LOGGED: electronStub.ts:204 `shell.showItemInFolder: (): void => {}`
and :269 `clipboard.writeText: (): void => {}`.

dialogStub.test.ts: mocked requestRustInvoke harness (lines 40-42, 56-90: program/callLog/warnSpy
fixtures), per-member resolve/cancel/reject cases (lines 92-127). STALE assertion at lines 129-141
("the other five dialog.* stub members are unchanged") WILL BREAK once three become real — update it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two RUST_DIALOG_* constants and the two dispatch_rust_channel match arms (contracts first)</name>
  <files>src/common/types/sidecarTransport.ts, src-tauri/src/main.rs</files>
  <read_first>
    - src/common/types/sidecarTransport.ts (the RUST_DIALOG_OPEN constant + RUST_INVOKE_CHANNELS array)
    - src-tauri/src/main.rs (lines 259, 341-361, 547, 585 — dispatch_rust_channel signature, the dialog_open arm, the worker-thread dispatch site, plugin registration)
    - 31-RESEARCH.md Q3 (Rust-side generalization status + "Rust API shapes needed")
    - 31-PATTERNS.md item 3 (the match-arm and constants shapes)
  </read_first>
  <action>
    In sidecarTransport.ts add `RUST_DIALOG_MESSAGE = 'dialog_message'` and
    `RUST_DIALOG_SAVE = 'dialog_save'` as `as const` constants next to `RUST_DIALOG_OPEN`, and add
    both to the `RUST_INVOKE_CHANNELS` array (the requestRustInvoke allowlist — a channel absent
    here is refused before a frame is emitted).
    In src-tauri/src/main.rs's `dispatch_rust_channel()`, add two new match arms BEFORE the `_ =>`
    catch-all, mirroring the existing `"dialog_open"` arm:
    - `"dialog_message"` → parse the message/title/kind from `args[0]`, call
      `app.dialog().message(msg).title(title).kind(<mapped MessageDialogKind>).blocking_show()`,
      return `Ok(Value::Bool(result))` (electronStub maps bool→response). Default kind Info when
      unspecified; Error when the caller signals error (used for showErrorBox).
    - `"dialog_save"` → build `app.dialog().file()` with optional `set_file_name`/`add_filter`
      from `args[0]`, call `.blocking_save_file()`, return `Ok(Value::String(path))` on Some or
      `Ok(Value::Null)` on None (same Option shape as dialog_open). Run on the existing spawned
      worker thread (already the dispatch site) — do NOT add new thread-spawning logic.
    No new Cargo dependency, no new plugin registration, no new capability permission (RESEARCH Q3:
    the plugin is already registered and the AppHandle param already present).
  </action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RUST_DIALOG_MESSAGE\|RUST_DIALOG_SAVE" src/common/types/sidecarTransport.ts` shows both constants defined AND both present in the RUST_INVOKE_CHANNELS array.
    - `grep -n '"dialog_message"\|"dialog_save"' src-tauri/src/main.rs` shows both match arms, placed before the `_ =>` catch-all.
    - No new dependency line was added to src-tauri/Cargo.toml (`git diff src-tauri/Cargo.toml` is empty).
    - `cargo check --manifest-path src-tauri/Cargo.toml` succeeds (Rust arms compile).
  </acceptance_criteria>
  <done>Both constants exist and are in the allowlist; both Rust match arms compile via cargo check; no new Cargo/plugin/capability wiring added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the three async dialog members real and upgrade the D-04 no-ops to logged</name>
  <files>src/backend/sidecar/electronStub.ts, src/backend/sidecar/__tests__/dialogStub.test.ts</files>
  <read_first>
    - src/backend/sidecar/electronStub.ts (lines 35-41 header no-logger rule; 136-164 dialog members incl. showOpenDialog; 204 shell.showItemInFolder; 269 clipboard.writeText)
    - src/backend/sidecar/__tests__/dialogStub.test.ts (the program/callLog/warnSpy harness lines 56-90; showOpenDialog cases 92-127; the stale "other five unchanged" assertion 129-141)
    - src/common/types/sidecarTransport.ts (post-Task-1 constants)
    - 31-RESEARCH.md Q2 (infrastructure-only, no in-scope caller) + Pitfall 3 + "Rust API shapes needed"
    - 31-PATTERNS.md item 3 + item 4
  </read_first>
  <behavior>
    Tests to add/adjust in dialogStub.test.ts (write first, then implement until green), reusing the
    program/callLog/warnSpy harness:
    - showMessageBox: when requestRustInvoke resolves true → returns {response:0, checkboxChecked:false};
      resolves false → {response:1, checkboxChecked:false}; and it called requestRustInvoke with
      RUST_DIALOG_MESSAGE. On reject → resolves a safe default ({response:1,...}), never throws, and warns.
    - showErrorBox: forwards to RUST_DIALOG_MESSAGE (error kind), returns undefined/void, never throws.
    - showSaveDialog: requestRustInvoke resolves a string path → {canceled:false, filePath:<path>};
      resolves null → {canceled:true, filePath:undefined}; called with RUST_DIALOG_SAVE. On reject →
      {canceled:true}, warns, never throws.
    - showMessageBoxSync / showOpenDialogSync: remain synchronous logged no-ops returning the
      documented safe default (e.g. response 0 / cancelled), and emit a console.warn (assert warnSpy).
    - D-04 (REQ-31-04): shell.showItemInFolder and clipboard.writeText each emit a console.warn when
      called (assert warnSpy called with a message naming the no-op + D-04 + Phase 33).
    - UPDATE the stale assertion (lines 129-141) that claims showMessageBox/showSaveDialog are still
      stubs — three of those five members are real now; adjust it to only assert the two Sync members
      remain no-ops (do NOT delete the whole guard — narrow it).
  </behavior>
  <action>
    In electronStub.ts replace the no-op bodies of `dialog.showMessageBox`, `dialog.showErrorBox`,
    `dialog.showSaveDialog` with the exact try/catch-to-safe-default shape `showOpenDialog` uses
    (electronStub.ts:139-164): each awaits `requestRustInvoke(RUST_DIALOG_MESSAGE, [...])` /
    `requestRustInvoke(RUST_DIALOG_SAVE, [...])`, maps the Rust result per the interfaces block
    (bool→{response, checkboxChecked:false}; Option/null-vs-string→{canceled, filePath}; errorBox→void),
    and on catch logs `console.warn` and resolves the safe default — NEVER throws to the caller
    (total-method convention). Leave `showMessageBoxSync`/`showOpenDialogSync` as no-ops but add a
    `console.warn` naming them logged no-ops (D-03) and return the documented safe default.
    For D-04: change `shell.showItemInFolder` (line ~204) and `clipboard.writeText` (line ~269) from
    silent `() => {}` to a `console.warn(...)` body naming the no-op, D-04, and the Phase 33 deferral
    (matching PATTERNS item 4). Do NOT import backend/logger (file header rule) — console.warn only.
    Do NOT touch `shell.openPath`/`showConfigFileInFolder` or `clipboard`-adjacent members that are
    out of D-04's named scope; they stay as-is.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/dialogStub.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "requestRustInvoke(RUST_DIALOG_MESSAGE\|requestRustInvoke(RUST_DIALOG_SAVE" src/backend/sidecar/electronStub.ts` shows the three members forwarding to the two channels.
    - dialogStub.test.ts asserts showMessageBox maps true→{response:0,checkboxChecked:false} and false→{response:1,...}, and showSaveDialog maps string→{canceled:false,filePath} / null→{canceled:true}.
    - dialogStub.test.ts asserts a requestRustInvoke rejection resolves each member as a safe default and calls warnSpy (no throw).
    - dialogStub.test.ts asserts shell.showItemInFolder and clipboard.writeText each call console.warn (logged, not silent).
    - The stale "other five dialog.* members unchanged" assertion is narrowed to the two Sync members only.
    - `grep -rn "from 'electron'\|backend/logger" src/backend/sidecar/electronStub.ts` shows no real electron import and no backend/logger import.
    - `npx jest src/backend/sidecar/__tests__/dialogStub.test.ts` passes.
  </acceptance_criteria>
  <done>The three async dialog members forward to Rust via requestRustInvoke and map results correctly with safe-default catch; the Sync pair stays a logged no-op; D-04 shell/clipboard no-ops are logged; the stale assertion is narrowed; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar → Rust | dialog requests cross from the Node sidecar to the Rust shell via the allowlisted `requestRustInvoke` channel |
| Rust dialog → caller | `showSaveDialog` returns a filesystem path that a future consumer could use to write a file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | `showSaveDialog` returned path used downstream to write outside intended dirs | accept | `showSaveDialog` has ZERO real callers repo-wide (RESEARCH Q2) — the returned path is consumed by no in-scope flow. The user chooses the path via the native OS save dialog (Rust `tauri-plugin-dialog`), so the path is user-intended by construction. Any future consumer owns validating the path before writing. Severity: low. |
| T-31-04 | Elevation of privilege | new `dialog_message`/`dialog_save` allowlist entries widen the sidecar→Rust surface | mitigate | Both channels are added ONLY to `RUST_INVOKE_CHANNELS` (the existing T-28-03 allowlist) and dispatch through the already-worker-threaded `dispatch_rust_channel`; no new capability permission is added and Tauri v2 capabilities gate webview→Rust IPC only (not these Rust-side plugin calls). No new attack surface beyond the already-shipped `dialog_open`. Severity: low. |
| T-31-SC | Tampering | npm/cargo installs | accept | No new packages this phase — `tauri-plugin-dialog 2.7.2` already present since Phase 30 (RESEARCH Package Legitimacy Audit: nothing to audit). |
</threat_model>

<verification>
- `cargo check --manifest-path src-tauri/Cargo.toml` succeeds.
- `npx jest src/backend/sidecar/__tests__/dialogStub.test.ts` green.
- `npx jest src/backend/sidecar/__tests__/electronUntouched.test.ts` still green (additive/reversible guard, REQ-31-07).
- `git diff src-tauri/Cargo.toml` empty (no new Cargo dependency).
</verification>

<success_criteria>
- Two RUST_DIALOG_* constants added to the allowlist; two Rust match arms compile.
- showMessageBox/showErrorBox/showSaveDialog forward to Rust and map results with a safe-default catch, never throwing.
- Sync pair stays a logged no-op; D-04 shell/clipboard no-ops emit console.warn.
- Stale dialogStub assertion narrowed; both automated guards pass; no new Cargo/electron/logger imports.
</success_criteria>

<output>
Create `.planning/phases/31-tauri-ipc-re-plumb-slice-2-settings-and-config/31-02-SUMMARY.md` when done
</output>
