---
phase: 17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i
plan: 07
type: execute
wave: 4
depends_on: ["17-05", "17-06"]
files_modified:
  - .planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-VALIDATION.md
autonomous: false
requirements: [MACSTEAM-01, MACSTEAM-02, MACSTEAM-03, MACSTEAM-04, MACSTEAM-05, MACSTEAM-06]

must_haves:
  truths:
    - "On a real macOS + CrossOver machine, a Windows-only Steam game provisions a bottle, logs in once, installs, launches, and shows the bottle indicator"
    - "Linux Proton, Windows, native-Mac Steam, and GOG/Epic bottle behavior are confirmed unchanged"
    - "The full automated suite is green before sign-off"
  artifacts:
    - path: ".planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-VALIDATION.md"
      provides: "Filled per-task verification map + manual UAT results + sign-off"
      contains: "Approval"
  key_links:
    - from: "17-VALIDATION.md"
      to: "MACSTEAM-01..06"
      via: "requirement → test/UAT mapping"
      pattern: "MACSTEAM"
---

<objective>
The phase gate. Everything the automated suite can prove is proven by Waves 0-3; the genuinely manual surface (real CrossOver bottle creation, real SteamSetup click-through, real bottled login, real install/launch, and the visual indicator) needs a human pass on a macOS machine with CrossOver installed — mirroring the Phase 3/10/13 human-verify precedent. This plan runs the full suite, then walks the human through the end-to-end macOS flow AND the scope-fence non-regressions, and records the result in 17-VALIDATION.md.

Purpose: Confirm MACSTEAM-01..06 hold in reality, not just in unit tests, before `/gsd:verify-work`.
Output: a completed 17-VALIDATION.md with the UAT results and sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-VALIDATION.md
@.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-RESEARCH.md
@.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-05-SUMMARY.md
@.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate + fill the automated half of 17-VALIDATION.md</name>
  <read_first>
    - .planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-VALIDATION.md (template to fill)
    - .planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-RESEARCH.md (Validation Architecture → Phase Requirement → Test Map, Sampling Rate)
    - all 17-0N-SUMMARY.md files (which tests each plan added)
  </read_first>
  <action>
    Run the full automated suite (`npm test`) and `npm run codecheck`. Fill 17-VALIDATION.md's Test Infrastructure (Jest 29.7.0, ts-jest, two projects; quick `npm test -- --testPathPattern=steam`; full `npm test`), Sampling Rate, and the Per-Task Verification Map — one row per MACSTEAM requirement mapping to the concrete automated command + the plan/task that owns it, marking each ✅ green. List every manual-only behavior (bottle provisioning, bottled login persistence, install/launch through the bottle, D-08 visual) in the Manual-Only Verifications table with instructions. Set `nyquist_compliant: true` only after confirming no 3 consecutive tasks across the phase lack an automated verify.
  </action>
  <verify>
    <automated>npm test && npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green) and `npm run codecheck` exits 0.
    - 17-VALIDATION.md Per-Task Verification Map has a row for each of MACSTEAM-01..06 with a concrete automated command and ✅ status (or an explicit Manual-Only entry where automation is impossible).
    - 17-VALIDATION.md frontmatter `wave_0_complete: true` and `nyquist_compliant: true`.
    - No `{placeholder}` tokens remain in the filled sections (grep for `{` in the table rows returns none).
  </acceptance_criteria>
  <done>The automated gate is green and recorded; the validation doc's automated half is complete and manual items enumerated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end macOS UAT on real CrossOver + scope-fence non-regression</name>
  <what-built>The complete macOS bottled-Steam runtime: per-OS isNative(), guided bottle provisioning + login, bottled install/launch/uninstall, bottle-scoped ACF badge reconciliation, and the D-08 indicator.</what-built>
  <how-to-verify>
    On a macOS machine with CrossOver installed and a Steam account owning at least one Windows-only (no Mac build) game:
    1. PROVISION + ALL ENTRY POINTS (MACSTEAM-02/04): With no Steam bottle yet, click Install on a confirmed Windows-only Steam game FROM THE GAME-DETAILS PAGE → the guided setup appears (consent + engine choice), NOT a native steam://install and NOT a silent no-op. Cancel, then trigger the SAME game's Install/Play FROM THE LIBRARY GRID tile → confirm the guided setup fires there too (proves the backend-signal + global-listener path covers every entry point, not just the modal). Confirm GameLib creates the dedicated bottle (distinct from the shared GameLib GOG/Epic bottle) and the real SteamSetup.exe window opens for click-through.
    2. LOGIN (MACSTEAM-03): Complete the one-time bottled-Steam login in the Steam window (same account recommended). Confirm subsequent actions don't re-prompt login.
    3. INSTALL (MACSTEAM-04/05): Install the game through the bottled Steam; verify the badge flips to installed from the bottle's ACF (Install Info shows platform Windows, install path under the CrossOver bottle drive_c), and progress/completion surface without a focus round-trip.
    4. LAUNCH (MACSTEAM-04): Launch the game; verify it runs via the bottled Steam client (not the native Mac Steam).
    5. INDICATOR (MACSTEAM-06): On the game page, confirm the "Runs via Windows Steam bottle" row shows for this game.
    6. D-11 GUARD (MACSTEAM-01): Confirm a game whose platform data hasn't synced yet is NOT force-bottled (treated native until confirmed).
    7. SCOPE FENCES (regression): Confirm a native-Mac Steam game still uses native steam:// (no bottle, no indicator); confirm a GOG or Epic Wine game still uses the shared GameLib bottle (unchanged); if a Linux box is available, confirm Windows-only Steam games still delegate to Proton. If steamwebhelper hangs during setup, confirm the recovery hint (Pitfall 4) is shown.
    Record each numbered result (pass / fail + notes) in 17-VALIDATION.md's Manual-Only Verifications section.
  </how-to-verify>
  <read_first>
    - .planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-VALIDATION.md
    - .planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-06-SUMMARY.md (SteamBottleSetup entry-point + i18n keys)
  </read_first>
  <acceptance_criteria>
    - Each of the 7 numbered checks has a recorded pass/fail + notes in 17-VALIDATION.md.
    - Step 1 explicitly confirms the guided flow fires from BOTH the game-details page AND the library grid (all entry points), not only the install-modal path.
    - The three scope-fence non-regressions (native-Mac steam://, GOG/Epic shared bottle, Linux Proton where testable) are explicitly confirmed unchanged.
    - 17-VALIDATION.md Approval line is set to `approved YYYY-MM-DD` on a clean pass, or failures are captured as gaps for `/gsd:plan-phase 17 --gaps`.
  </acceptance_criteria>
  <resume-signal>Type "approved" when the end-to-end macOS UAT passes and scope fences are confirmed, or list failures to route into gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human tester → running app | Manual exercise of the real provisioning/login/install path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | end-to-end bottle-name/appId argv (live) | mitigate | UAT exercises the real spawn path; earlier plans' sanitize/numeric guards are the controls — this checkpoint confirms no injection surface is reachable via the UI |
| T-17-03 | Denial of Service | live steamwebhelper hang (A3) | mitigate | UAT step 7 confirms the recovery hint appears rather than a silent hang |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- `npm test` + `npm run codecheck` green (Task 1).
- 17-VALIDATION.md fully filled with automated map + manual UAT results + sign-off.
- All six MACSTEAM requirements confirmed (automated where possible, manual for the runtime surface).
</verification>

<success_criteria>
The macOS bottled-Steam runtime works end-to-end on real hardware, the scope fences (Linux/Windows/native-Mac/GOG/Epic) are confirmed unchanged, and the phase is validated and signed off ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-07-SUMMARY.md` when done. Note any failures as candidate gaps for `/gsd:plan-phase 17 --gaps`.
</output>
