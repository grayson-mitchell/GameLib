---
phase: 17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i
plan: 16
type: execute
wave: 10
depends_on: []
gap_closure: true
autonomous: true
requirements: [MACSTEAM-02, MACSTEAM-04]
files_modified:
  - src/backend/storeManagers/steam/bottle.ts
  - src/backend/storeManagers/steam/__tests__/bottle.test.ts

must_haves:
  truths:
    - "provisioned is never written false immediately after a wait:false SteamSetup launch while steam.exe is absent"
    - "Once a ready bottle (cxbottle.conf + bottled steam.exe) is observed, provisioned is persisted true (guided-setup surface stops sticking on a stale false)"
    - "The win32->win64 recreate stops the bottle's own Wine processes via wineserver -k BEFORE cxbottle --delete, so the delete succeeds even while the bottled Steam client is running"
    - "wineserver -k is scoped to the target bottle's own WINEPREFIX and never touches the shared GameLib GOG/Epic bottle or any other prefix"
    - "The installer/game raise poll loop leaves no live timer after completion/teardown; the bottle test suite exits 0 with no worker-force-exit"
  artifacts:
    - path: "src/backend/storeManagers/steam/bottle.ts"
      provides: "GAP A/B/C fixes: readiness-reconciled provisioned flag, pre-delete WINEPREFIX-scoped wineserver kill, leak-safe raise loop"
      contains: "wineserver"
    - path: "src/backend/storeManagers/steam/__tests__/bottle.test.ts"
      provides: "Regression tests for all three gaps"
      contains: "GAP-17-PROVISIONED-FLAG-STUCK"
  key_links:
    - from: "isBottleReady"
      to: "steamBottleConfigStore.set('provisioned', true)"
      via: "lazy readiness reconcile"
      pattern: "set\\(['\"]provisioned['\"], *true\\)"
    - from: "provisionBottle win32-recreate branch"
      to: "wineserver -k (WINEPREFIX-scoped)"
      via: "process kill before cxbottle --delete"
      pattern: "wineserver"
---

<objective>
Close the three remaining OPEN code gaps from 17-VALIDATION.md § "UAT Findings & Candidate Gaps" (session 5, real CrossOver). All three modify the SAME two files, so they ship as one atomic plan with three sequential tasks:

- GAP A — GAP-17-PROVISIONED-FLAG-STUCK (MAJOR, MACSTEAM-02): `provisioned` config flag is persisted `false` in a race against a `wait:false` SteamSetup launch and never re-evaluated, so the guided-setup surface can keep returning even after Steam is fully installed.
- GAP B — GAP-17-CEF-RECREATE-RUNNING (MAJOR, MACSTEAM-02): the win32->win64 recreate branch runs `cxbottle --delete` without first stopping the bottle's Wine processes, so the delete aborts ("applications still running") in exactly the CEF-grey-bar scenario it targets.
- GAP C — focus-not-moving + test-teardown leak (MINOR, MACSTEAM-02/04): the macOS raise loop's real `sleep(1500)` retry survives Jest teardown (worker force-exit), and focus does not reliably move to the bottled Steam window.

Purpose: make the guided-setup/provisioning surface honest on real hardware and stop the bottle test suite from force-exiting non-zero.
Output: patched `bottle.ts` + extended `bottle.test.ts`; existing 17-01..17-15 plans untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-VALIDATION.md
@.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-CONTEXT.md
@.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/deferred-items.md
@src/backend/storeManagers/steam/bottle.ts
@src/backend/storeManagers/steam/__tests__/bottle.test.ts

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. No exploration required. -->

From src/backend/utils.ts (spawnAsync — third arg is SpawnOptions, which carries `env`):
```typescript
export const spawnAsync = (
  command: string,
  args: string[],
  options?: SpawnOptions,        // { env?: NodeJS.ProcessEnv, ... }
  onOutput?: (data: string) => void
) => Promise<{ code: number | null; stdout: string; stderr: string }>
```

From src/backend/storeManagers/steam/bottle.ts (existing, load-bearing identifiers):
```typescript
const CXBOTTLE_BIN =
  '/Applications/CrossOver.app/Contents/SharedSupport/CrossOver/bin/cxbottle'
// => CrossOver bin dir  = dirname(CXBOTTLE_BIN)
// => wineserver binary  = join(dirname(CXBOTTLE_BIN), 'wineserver')
// => CX_ROOT            = dirname(dirname(CXBOTTLE_BIN))  (.../SharedSupport/CrossOver)

export function getBottleDir(bottleName: string): string           // the bottle's own WINEPREFIX
export function getBottleSteamExePath(bottleName: string): string  // both-root resolved steam.exe
export function isBottleProvisioned(bottleName?: string): boolean  // cxbottle.conf exists
export function isBottleReady(bottleName?: string): boolean        // conf + steam.exe both exist
export async function provisionBottle(opts?): Promise<ProvisionBottleResult>
// step 2b: win32-recreate branch (spawnAsync cxbottle --delete --force + rmSync fallback)
// step 8:  steamBottleConfigStore.set('provisioned', fullyProvisioned)   <-- GAP A target
async function raiseFrontmostBottledProcess(context, raiseScript): Promise<void> // GAP C poll loop
async function raiseInstallerWindow(context): Promise<void>        // installer raiser (item 1, no visible filter)
async function raiseBottledGameWindow(context): Promise<void>      // game raiser (already visible-filtered)
```

From src/backend/storeManagers/steam/electronStores.ts:
```typescript
// steamBottleConfigStore: TypeCheckedStoreBackend with get / get_nodefault / set
// keys used here: 'provisioned' (boolean), 'bottleName' (string)
```

From src/backend/constants/environment.ts (dynamically imported inside the raise loop):
```typescript
export const isMac: boolean
```
</interfaces>

<test_command_landmine>
Jest `--testPathPattern=steam/bottle` matches ZERO files ("No tests found", exit 1) because `__tests__/` sits between `steam/` and `bottle`. EVERY bottle-test verify command in this plan MUST use a `.*` pattern:
`npm test -- --testPathPattern="steam.*bottle"`
Also run `npm run codecheck` (tsc --noEmit) as a verify. Do NOT touch library.ts in this plan.
</test_command_landmine>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP A — readiness-reconciled provisioned flag (never write false against a wait:false race)</name>
  <files>src/backend/storeManagers/steam/bottle.ts, src/backend/storeManagers/steam/__tests__/bottle.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/bottle.ts — `isBottleReady()` (~L224-230) and `provisionBottle()` step 8 (~L632-645), plus step 2b win32-recreate (which legitimately DOES set `provisioned:false`).
    - src/backend/storeManagers/steam/__tests__/bottle.test.ts — the `provisionBottle` describe block (~L341-584); note the default `mockedReadFileSync` returns win64 so the recreate branch does not fire unless a test arranges win32.
    - src/backend/main.ts:900 — the `steamBottleStatus` IPC handler reads `steamBottleConfigStore.get_nodefault('provisioned')` raw (do NOT edit main.ts; the reconcile must live in bottle.ts so the flag is already correct by the time the handler reads it).
    - 17-VALIDATION.md § "GAP-17-PROVISIONED-FLAG-STUCK" (root cause + fix direction).
  </read_first>
  <behavior>
    - Test: after `provisionBottle()` runs the full create->download->SteamSetup(wait:false) path with steam.exe absent at step 8 (conf true via create, steamExe false, win64 conf), `steamBottleConfigStore.set` is NEVER called with `('provisioned', false)`.
    - Test: `isBottleReady('GameLibSteam')` returning true (conf + steam.exe both present) persists `steamBottleConfigStore.set('provisioned', true)` — the lazy readiness reconcile.
    - Test: `isBottleReady()` returning false (half-provisioned: conf only) does NOT write `provisioned` at all.
    - Test (guard against redundant writes): when the stored `provisioned` is already true, a ready `isBottleReady()` observation does not need to re-write (assert it is not called with `('provisioned', false)`; a repeated `true` write is acceptable but prefer the get_nodefault guard).
    - Existing recreate test ("an existing win32 bottle is deleted and recreated…") still passes — the win32 branch's legitimate `set('provisioned', false)` is preserved.
  </behavior>
  <action>
    Stop deriving `provisioned` from a race against the `wait:false` SteamSetup launch (GAP-17-PROVISIONED-FLAG-STUCK).

    (1) In `provisionBottle()` step 8: replace the unconditional `steamBottleConfigStore.set('provisioned', fullyProvisioned)` with a guarded write that ONLY persists `provisioned: true` when `existsSync(steamExePath)` is true. NEVER persist `false` here — right after a `wait:false` launch steam.exe is legitimately absent, so leave the flag untouched (it stays whatever it was) rather than clobbering it false. Keep the existing `logInfo` line, adjusting its message so it does not claim a false value was persisted.

    (2) Add the lazy readiness reconcile inside `isBottleReady()`: compute `ready = isBottleProvisioned(name) && existsSync(getBottleSteamExePath(name))` as today, and when `ready === true` AND the stored flag (`steamBottleConfigStore.get_nodefault('provisioned')`) is not already `true`, call `steamBottleConfigStore.set('provisioned', true)` before returning `ready`. This is the "first time a real bottled steam.exe is observed" reconcile the gap calls for — it self-heals the flag the moment any routing call (games.ts install/launch/uninstall pre-flights, or provisionBottle's step-3 short-circuit) observes readiness, so `steamBottleStatus` (main.ts:900) reads a correct value with no main.ts change. Add a doc comment referencing GAP-17-PROVISIONED-FLAG-STUCK and noting the intentional write-through side-effect.

    (3) Do NOT alter the step 2b win32-recreate branch's `steamBottleConfigStore.set('provisioned', false)` — that reset is correct (a freshly deleted bottle IS un-provisioned).

    Add the four `bottle.test.ts` cases described in <behavior> under the existing `isBottleReady` and `provisionBottle` describe blocks, reusing the existing `setBottleFs` helper and path-aware `mockedExistsSync` pattern.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="steam.*bottle" && npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "set\\(['\"]provisioned['\"], *true\\)" src/backend/storeManagers/steam/bottle.ts` matches inside `isBottleReady()`.
    - No `steamBottleConfigStore.set('provisioned', false)` remains in step 8 (only the win32-recreate branch may set false): `grep -c "set('provisioned', false)" src/backend/storeManagers/steam/bottle.ts` returns 1.
    - New test asserting `mockedSet` is NOT called with `('provisioned', false)` on the normal create->SteamSetup(wait:false) path passes.
    - New test asserting a ready `isBottleReady()` persists `('provisioned', true)` passes.
    - `npm test -- --testPathPattern="steam.*bottle"` full bottle suite green; `npm run codecheck` exit 0.
  </acceptance_criteria>
  <done>provisioned is never persisted false during the wait:false window; it flips true the first time a ready bottle is observed; recreate reset preserved; bottle suite + tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP B — stop the bottle's own Wine processes (WINEPREFIX-scoped wineserver -k) before cxbottle --delete</name>
  <files>src/backend/storeManagers/steam/bottle.ts, src/backend/storeManagers/steam/__tests__/bottle.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/bottle.ts — the win32-recreate branch in `provisionBottle()` step 2b (~L446-494), the `CXBOTTLE_BIN` constant (~L49-50), `getBottleDir()` (~L56-62).
    - src/backend/utils.ts:843 — `spawnAsync(command, args, options)` signature; third arg `SpawnOptions` carries `env`.
    - src/backend/storeManagers/steam/__tests__/bottle.test.ts — the "an existing win32 bottle is deleted and recreated…" test (~L509-571) and its `mockedSpawnAsync.mockImplementation` argv-branching pattern.
    - 17-VALIDATION.md § "GAP-17-CEF-RECREATE-RUNNING" (root cause: `cxbottle --delete` refuses while apps run in the prefix; fix: WINEPREFIX-scoped `wineserver -k` first; SCOPE-FENCE: never kill other prefixes).
  </read_first>
  <behavior>
    - Test: in the win32-recreate path, the `wineserver -k` spawn runs BEFORE the `cxbottle --delete` spawn — assert ordering via `mock.invocationCallOrder` (or by inspecting the ordered `mockedSpawnAsync.mock.calls`).
    - Test: the `wineserver -k` spawn is scoped to the bottle's own WINEPREFIX — assert the options `env.WINEPREFIX === getBottleDir('GameLibSteam')` and that `env.CX_ROOT` is set; assert WINEPREFIX contains the bottle name and is NOT the shared `GameLib` bottle dir.
    - Test: the wineserver binary invoked resolves under the CrossOver bin dir (same dir as `CXBOTTLE_BIN`), i.e. the spawned command path ends with `/bin/wineserver`.
    - Test: the `rmSync` fallback still only runs AFTER the kill + delete (existing recreate test behavior preserved).
    - Existing win64 idempotent + non-win32 tests still pass (wineserver -k must NOT fire when the recreate branch is not taken).
  </behavior>
  <action>
    Before the `cxbottle --delete --force` inside the win32-recreate branch (step 2b), stop the bottle's Wine processes so the delete cannot abort with "There are still applications running… Aborting" (GAP-17-CEF-RECREATE-RUNNING).

    (1) Derive the wineserver binary and CX_ROOT from the existing `CXBOTTLE_BIN` constant (do NOT hardcode a second absolute path — keep a single source of truth): `WINESERVER_BIN = join(dirname(CXBOTTLE_BIN), 'wineserver')` and `CX_ROOT = dirname(dirname(CXBOTTLE_BIN))`. Add a small module-level helper, e.g. `killBottleWineServer(bottleName: string)`, that `await spawnAsync(WINESERVER_BIN, ['-k'], { env: { ...process.env, WINEPREFIX: getBottleDir(bottleName), CX_ROOT } })`, wrapped in try/catch with a `logWarning` on failure (best-effort — a kill failure must not abort provisioning). Use `path`'s `dirname`/`join` (add `dirname` to the existing `import { join } from 'path'`).

    (2) SCOPE-FENCE (T-17): `WINEPREFIX` MUST be `getBottleDir(bottleName)` — the dedicated Steam bottle's own prefix — and nothing else. Never pass the shared `GameLib` GOG/Epic bottle dir or a bare/empty WINEPREFIX (an unset WINEPREFIX would let wineserver -k hit the default prefix). Document this scope-fence in the helper's doc comment.

    (3) In the win32-recreate branch, call `await killBottleWineServer(bottleName)` immediately BEFORE the `spawnAsync(CXBOTTLE_BIN, ['--bottle', bottleName, '--delete', '--force'])` call. Leave the existing `rmSync(getBottleDir(bottleName), ...)` fallback where it is (it now runs only after the kill + delete).

    Add the `bottle.test.ts` cases from <behavior> by extending the existing win32-recreate test's `mockedSpawnAsync.mockImplementation` to also recognize the wineserver call, and assert ordering + the WINEPREFIX/CX_ROOT env scoping.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="steam.*bottle" && npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "wineserver" src/backend/storeManagers/steam/bottle.ts` shows the binary derived via `join(dirname(CXBOTTLE_BIN), 'wineserver')` (no second hardcoded absolute path).
    - New test asserts the `wineserver -k` spawn's `invocationCallOrder` is LESS THAN the `cxbottle --delete` spawn's in the recreate path.
    - New test asserts the wineserver spawn options `env.WINEPREFIX === getBottleDir('GameLibSteam')` and `env.CX_ROOT` is defined and non-empty.
    - Win64 idempotent test confirms `wineserver` is NOT spawned when the recreate branch is not taken.
    - `npm test -- --testPathPattern="steam.*bottle"` green; `npm run codecheck` exit 0.
  </acceptance_criteria>
  <done>The win32->win64 recreate kills only the target bottle's Wine processes (WINEPREFIX-scoped) before deleting, so the delete succeeds while Steam is running; no other prefix is touched; suite + tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: GAP C — leak-safe raise loop (unref + cancel) and conservative focus reliability</name>
  <files>src/backend/storeManagers/steam/bottle.ts, src/backend/storeManagers/steam/__tests__/bottle.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/bottle.ts — `raiseFrontmostBottledProcess()` (~L300-360, the `sleep`/12-attempt/~18s poll loop and the `app.hide()` fallback), `raiseInstallerWindow()` (~L362-376, `item 1 of procs`, no visible filter), `raiseBottledGameWindow()` (~L386-402, already `visible is true`-filtered).
    - src/backend/storeManagers/steam/__tests__/bottle.test.ts — mock setup (`spawnAsync` is mocked; the loop calls `osascript` via `spawnAsync`, whose default mock resolves empty stdout -> the loop never "succeeds" and would run all 12 attempts on a macOS host).
    - deferred-items.md § "From 17-12" — the exact leak symptom ("import a file after the Jest environment has been torn down" at `bottle.ts:~289`; process exits non-zero after teardown; `raiseInstallerWindow` fired `void` from `provisionBottle()`/dispatch).
    - 17-VALIDATION.md § "Also observed (session 5, MINOR UX)" + GAP C fix direction (conservative focus improvement; eliminate the teardown leak with a real source-level fix, not just a test mock).
  </read_first>
  <behavior>
    - Test: after the raise loop is stopped (via the new teardown/cancel hook) or completes, no live timer remains — under `jest.useFakeTimers()`, `jest.getTimerCount()` returns to 0.
    - Test: invoking a flow that fires `void raiseInstallerWindow(...)` (or calling it directly) and then the cancel hook does not leave a pending dynamic-import/timer that would fire post-teardown.
    - Suite-level: `npm test -- --testPathPattern="steam.*bottle"` EXITS 0 (was exiting 1 with "A worker process has failed to exit gracefully").
  </behavior>
  <action>
    Make the macOS raise poll loop leak-safe at the source (not merely hidden in tests), and make the installer raiser's target selection conservative-but-more-reliable. macOS-only, fire-and-forget throughout — never block or change the success/failure semantics of install/launch/provision.

    (1) Unref the retry timer: in `raiseFrontmostBottledProcess`, change the local `sleep(ms)` so the underlying `setTimeout` handle is unref'd — capture the handle and call `handle.unref?.()` (optional-chained so it is a no-op in any non-Node/browser context) before awaiting. An unref'd timer does not keep the Node process (Jest worker) alive, directly resolving the "worker failed to exit gracefully" force-exit.

    (2) Add a cancellation guard so the loop cannot fire a dynamic `import(...)` after teardown: introduce a module-scoped cancellation flag and a set/handle of in-flight timers. Before each `sleep`/`tryRaise` iteration and before the post-loop re-raise, check the flag and bail early if cancelled. Export a test-only teardown hook (e.g. `__stopBottledRaiseLoops()`) that sets the flag and clears any tracked timer handles. Wire an `afterEach`/`afterAll` in `bottle.test.ts` to call it so no loop survives a test. Keep the runtime behavior identical when not cancelled.

    (3) Conservative focus reliability for `raiseInstallerWindow`: change its AppleScript to filter to `visible is true` processes (mirroring the already-working `raiseBottledGameWindow`) and pick the FIRST visible match rather than blindly `item 1 of procs` (which can select a hidden/background helper). Keep the same `INSTALLER_PROCESS_NAMES` name-matching, the `set frontmost` focus-steal-defeating trick, the `"none"` sentinel, and the best-effort/fire-and-forget contract. Do not otherwise alter timing or the app.hide fallback.

    Add the `bottle.test.ts` cases from <behavior>. Use `jest.useFakeTimers()` where needed to assert `jest.getTimerCount()` reaches 0 after the cancel hook; restore real timers in teardown.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="steam.*bottle" && npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern="steam.*bottle"` exits 0 (echo $? == 0) with NO "A worker process has failed to exit gracefully" / "import a file after the Jest environment has been torn down" message.
    - `grep -nE "unref" src/backend/storeManagers/steam/bottle.ts` shows the sleep timer handle is unref'd.
    - `raiseInstallerWindow`'s script includes a `visible is true` filter: `grep -n "visible is true" src/backend/storeManagers/steam/bottle.ts` matches in both raisers (installer + game).
    - New test confirms `jest.getTimerCount()` returns to 0 after the cancel/teardown hook.
    - `npm run codecheck` exit 0; full bottle suite green.
  </acceptance_criteria>
  <done>The raise loop no longer keeps the Jest worker alive or fires imports post-teardown (suite exits 0); the installer raiser targets a visible process; behavior stays best-effort fire-and-forget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend/IPC -> bottle.ts | bottle name / appId argv crossing into CLI (`cxbottle`, `wineserver`, `runWineCommand`) — untrusted-shaped input reaching process spawn |
| bottle.ts -> host processes | `wineserver -k` terminates OS processes scoped by an env var (WINEPREFIX) — a mis-scope could DoS unrelated bottles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | bottle name -> cxbottle/wineserver argv (Task 2) | mitigate | Preserve the existing `sanitizeBottleName()` discrete-word chokepoint (rejects `/ \ .. \0`); `provisionBottle()` already sanitizes before step 2b, so the recreate branch's `getBottleDir(bottleName)` is on an already-sanitized name. wineserver args are discrete words (`['-k']`), never a shell string. |
| T-17-04 | Tampering | appId -> bottled-Steam verb dispatch | mitigate | Unchanged: `NUMERIC_APP_ID` (`/^\d+$/`) guard in `dispatchToBottledSteam` preserved — this plan does not touch that path. |
| T-17-DoS | Denial of Service | `wineserver -k` process kill (Task 2, GAP B) | mitigate | SCOPE-FENCE: `env.WINEPREFIX` MUST be `getBottleDir(bottleName)` (the dedicated Steam bottle only). Never the shared `GameLib` GOG/Epic prefix and never unset/empty (an unset WINEPREFIX would target the default prefix). A unit test asserts the WINEPREFIX equals the target bottle dir and is not the shared bottle. |
| T-17-06 | Information Disclosure | bottled-Steam credentials (D-04 opaque) | accept | No task inspects loginusers.vdf/sentry; auth stays opaque. GAP A only reads/writes the `provisioned` boolean, never credentials. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies added in this plan (uses existing `graceful-fs`, `path`, `backend/utils.spawnAsync`). No package-manager install tasks -> no legitimacy gate required. |
</threat_model>

<verification>
- `npm test -- --testPathPattern="steam.*bottle"` — full bottle suite green AND exits 0 (the GAP C leak previously forced a non-zero exit).
- `npm run codecheck` (tsc --noEmit) — exit 0.
- `grep -c "set('provisioned', false)" src/backend/storeManagers/steam/bottle.ts` returns 1 (win32-recreate branch only; step 8 no longer writes false).
- `grep -nE "wineserver|unref|visible is true" src/backend/storeManagers/steam/bottle.ts` confirms all three fixes are present in source.
- Existing 17-01..17-15 plan files and their SUMMARYs are untouched.
</verification>

<success_criteria>
1. GAP A: `provisioned` is never written `false` during the wait:false SteamSetup window; it self-heals to `true` the first time `isBottleReady()` observes a ready bottle (conf + bottled steam.exe). (MACSTEAM-02)
2. GAP B: the win32->win64 recreate stops the bottle's own Wine processes via a WINEPREFIX-scoped `wineserver -k` BEFORE `cxbottle --delete`, and the kill never touches any prefix other than the target bottle's. (MACSTEAM-02)
3. GAP C: the raise poll loop leaves no live timer after completion/teardown — the bottle suite exits 0 with no worker force-exit — and the installer raiser targets a visible process. (MACSTEAM-02, MACSTEAM-04)
4. Full bottle suite + `tsc --noEmit` green; no existing test regressed.
</success_criteria>

<output>
Create `.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-16-SUMMARY.md` when done.
</output>
