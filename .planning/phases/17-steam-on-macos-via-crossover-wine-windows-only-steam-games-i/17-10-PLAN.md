---
phase: 17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/Game/GamePage/components/SteamBottleSetup.scss
  - src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx
  - src/frontend/screens/Game/GamePage/components/__tests__/SteamBottleSetup.styles.test.ts
autonomous: true
gap_closure: true
requirements: [MACSTEAM-02]

must_haves:
  truths:
    - "The Steam bottle setup banner renders with a visible background, padding, and a fixed on-screen position — not plain text overlaid on the window"
    - "The error state of the banner ('could not download steam' / try-again) is legible and styled"
  artifacts:
    - path: "src/frontend/screens/Game/GamePage/components/SteamBottleSetup.scss"
      provides: "Toast + message + action + dismiss styling for the guided-setup banner"
      contains: ".steamBottleSetupToast"
    - path: "src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx"
      provides: "Imports the co-located stylesheet"
      contains: "SteamBottleSetup.scss"
  key_links:
    - from: "src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx"
      to: "SteamBottleSetup.scss"
      via: "import './SteamBottleSetup.scss'"
      pattern: "SteamBottleSetup\\.scss"
---

<objective>
Close the cosmetic sub-issue of UAT GAP 1 (MACSTEAM-02): the Steam bottle setup
banner (`.steamBottleSetupToast`) renders with NO background — plain text over
the window — because zero CSS rules exist for it (confirmed in
.planning/debug/steam-bottle-provision-stuck-loop.md).

Purpose: give the guided-setup banner a legible, styled surface mirroring the
existing HumbleExpiryToast pattern so the provisioning/error/login messages are
readable.

Output: a co-located `SteamBottleSetup.scss`, imported by the component, with
rules for the toast container and its children.

Independent of the backend readiness fixes (17-08/17-09) — no file overlap —
runs in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/steam-bottle-provision-stuck-loop.md
@src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx
@src/frontend/components/UI/HumbleExpiryToast/index.scss

<interfaces>
<!-- The banner classNames already emitted by SteamBottleSetup.tsx that need styling. -->
  .steamBottleSetupToast      (root banner container — the D-09 non-blocking surface)
  .steamBottleSetupMessage    (each message <span>; several stacked in the provisioning state)
  .steamBottleSetupAction     ("Try again" button in the error state)
  .steamBottleSetupDismiss    ("Done" button)
  .steamBottleSetupDialog      (consent MUI Dialog wrapper className — minor)
  .steamBottleSetupEngineLabel ("Compatibility engine" label — minor)

<!-- Reference pattern (HumbleExpiryToast/index.scss): fixed bottom/right, z-index 20,
     var(--background-darker) bg, var(--space-md) padding, border-radius, box-shadow,
     border-inline-start accent, flex layout, var(--space-*) tokens. -->

<!-- Jest note: this repo has NO scss moduleNameMapper and testEnvironment is 'node'.
     Do NOT import the component (or the scss) in the test — assert against the scss
     FILE TEXT via fs.readFileSync so no scss transform is needed. HumbleExpiryToast.tsx
     already imports its scss the same way, so the bundler handles scss imports fine. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and import SteamBottleSetup.scss styling the guided-setup banner + its states</name>
  <files>src/frontend/screens/Game/GamePage/components/SteamBottleSetup.scss, src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx, src/frontend/screens/Game/GamePage/components/__tests__/SteamBottleSetup.styles.test.ts</files>
  <action>
    1. Create SteamBottleSetup.scss co-located with the component. Mirror the
       HumbleExpiryToast/index.scss pattern using the same design tokens:
       - `.steamBottleSetupToast`: position fixed; bottom/right at var(--space-lg);
         z-index 20; a visible background (var(--background-darker, var(--body-background)));
         color var(--text-default); padding var(--space-md); border-radius var(--space-2xs);
         box-shadow for lift; max-width min(420px, calc(100vw - 2*var(--space-lg)));
         display flex; flex-direction column (the provisioning state stacks several
         message spans + a dismiss button); gap var(--space-sm). Add a
         border-inline-start accent (var(--status-warning) is appropriate for the
         error/attention banner, matching HumbleExpiryToast).
       - `.steamBottleSetupMessage`: font-size var(--text-sm); readable line-height;
         flex/normal wrapping.
       - `.steamBottleSetupAction`: outlined button styling (transparent bg, accent
         border/color, hover fill) mirroring .humbleExpiryToastAction.
       - `.steamBottleSetupDismiss`: subtle text button mirroring .humbleExpiryToastDismiss;
         align it to the end of the column (e.g. align-self flex-end).
       - `.steamBottleSetupEngineLabel`: minor margin for the "Compatibility engine" label
         inside the consent dialog.
       Keep it token-based (no hardcoded colors) so it inherits themes. The consent
       step itself is a real MUI Dialog (already styled by Dialog); only add
       `.steamBottleSetupDialog` tweaks if needed — the primary fix is the toast.
    2. In SteamBottleSetup.tsx, add `import './SteamBottleSetup.scss'` at the top of
       the import block (matching HumbleExpiryToast/index.tsx which does the same).
    3. Create __tests__/SteamBottleSetup.styles.test.ts as a pure-node test (no
       component import, no scss import): read the scss file with fs.readFileSync and
       assert it contains selectors `.steamBottleSetupToast`, `.steamBottleSetupMessage`,
       `.steamBottleSetupAction`, `.steamBottleSetupDismiss`, and that the toast rule
       declares a `background`, a `z-index`, and `padding` (the exact regression: the
       banner previously had zero rules → no background). Also read SteamBottleSetup.tsx
       and assert it imports './SteamBottleSetup.scss'. This guards against the banner
       ever being unstyled again.
  </action>
  <verify>
    <automated>yarn jest src/frontend/screens/Game/GamePage/components/__tests__/SteamBottleSetup.styles.test.ts</automated>
  </verify>
  <done>SteamBottleSetup.scss exists with toast/message/action/dismiss rules (background + z-index + padding on the toast), is imported by the component, and the styles test is green.</done>
</task>

</tasks>

<verification>
- `yarn jest src/frontend/screens/Game/GamePage/components` — frontend component-dir suites green, no regression.
- Grep confirms `SteamBottleSetup.tsx` imports `SteamBottleSetup.scss` and the scss defines `.steamBottleSetupToast` with a background.
</verification>

<success_criteria>
- The guided-setup banner has a visible, legible styled surface (background, padding, fixed position, z-index) in both provisioning and error states.
- No new npm packages; no jsdom dependency introduced (test is fs-based).
</success_criteria>

<output>
Create `.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-10-SUMMARY.md` when done.
</output>
