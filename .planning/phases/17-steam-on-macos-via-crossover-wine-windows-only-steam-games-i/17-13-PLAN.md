---
phase: 17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i
plan: 13
type: execute
wave: 7
depends_on: []
files_modified:
  - src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx
  - public/locales/en/gamepage.json
autonomous: true
gap_closure: true
requirements: [MACSTEAM-02, MACSTEAM-03]

must_haves:
  truths:
    - "During guided provisioning, the setup surface explicitly instructs the user to UNTICK 'Run Steam' on SteamSetup.exe's final screen, because GameLib launches Steam itself once the bottle is ready."
    - "The setup surface still surfaces the steamwebhelper self-update recovery hint (the 'Updating Steam' may be waiting on its own update guidance) so a hung client is not mistaken for a frozen one."
  artifacts:
    - path: "public/locales/en/gamepage.json"
      provides: "bottle.setup.uncheckRunSteam instruction copy (gamepage namespace)"
      contains: "uncheckRunSteam"
    - path: "src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx"
      provides: "the untick-Run-Steam instruction rendered in the provisioning banner"
      contains: "uncheckRunSteam"
  key_links:
    - from: "src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx"
      to: "public/locales/en/gamepage.json bottle.setup.uncheckRunSteam"
      via: "t('bottle.setup.uncheckRunSteam', ...) rendered in the provisioning banner spans"
      pattern: "uncheckRunSteam"
---

<objective>
Close UAT MAJOR **GAP-17-STEAMWEBHELPER-HANG** (aside finding, MACSTEAM-02/03): SteamSetup.exe's final screen leaves **"Run Steam" ticked by default**; leaving it ticked launched the bottled client whose steamwebhelper self-update HUNG (RESEARCH.md Pitfall 4 / A3) and required Force Quit. GameLib drives launch itself via `tellBottledSteamTo*` once the bottle is ready, so the installer must NOT auto-run the client.

**Fix direction (locked by the gap — copy/i18n only, do NOT over-engineer process detection):**
Update the guided-setup instruction copy in `SteamBottleSetup.tsx` (+ i18n `gamepage`) to explicitly tell the user to **untick "Run Steam"** on SteamSetup's final page (GameLib launches Steam itself when the bottle is ready). The existing steamwebhelper recovery hint (`bottle.setup.hangHint`, "If Steam appears stuck on 'Updating Steam'…") is already present — keep it; the missing piece is the untick-Run-Steam instruction that prevents the hang in the first place.

Purpose: prevents the Force-Quit hang path during first-run provisioning (test 2/3 flow) and satisfies UAT step-7's "recovery hint shown" expectation.
Output: a new `bottle.setup.uncheckRunSteam` i18n key rendered in the provisioning banner, alongside the retained hang recovery hint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-VALIDATION.md
@.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-06-SUMMARY.md

<interfaces>
<!-- The component being edited (SteamBottleSetup.tsx) renders a provisioning banner (NOT a
     dialog) once the user confirms. The instruction spans live in the non-error banner branch: -->
  // src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx (lines ~213-244):
  //   provisioned ? t('bottle.setup.readyForLogin') : t('bottle.setup.provisioning')
  //   then three guidance spans, in order:
  //     t('bottle.setup.login', 'Finish by logging in to Steam in the window that opens.')
  //     t('bottle.setup.sameAccount', 'Tip: use the same Steam account…')
  //     t('bottle.setup.hangHint', 'If Steam appears stuck on "Updating Steam"…')   // KEEP — already present
  //   Each span is <span className="steamBottleSetupMessage">{t(...)}</span>.
  //   useTranslation('gamepage') — the 'gamepage' namespace (public/locales/en/gamepage.json).

<!-- Existing i18n keys under bottle.setup (public/locales/en/gamepage.json):
     confirm, consent, login, provisioning, readyForLogin, hangHint, sameAccount, done, … (nested object).
     Add uncheckRunSteam alongside them. Keep valid JSON, alphabetical-ish per existing style. -->
<!-- Frontend jest has NO jsdom — do NOT add a component-render test; verification is JSON validity +
     source grep + the i18n --check gate. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "untick Run Steam" instruction copy to the provisioning banner + i18n key</name>
  <files>src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx, public/locales/en/gamepage.json</files>
  <read_first>
    - src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx (the provisioning banner branch ~196-249; the three existing guidance spans ~213-244 incl. the retained hangHint)
    - public/locales/en/gamepage.json (the bottle.setup nested object — mirror an existing key's style, e.g. login/hangHint)
    - .planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-VALIDATION.md (GAP-17-STEAMWEBHELPER-HANG section — the exact fix intent)
  </read_first>
  <action>
    Add a single, prominent instruction telling the user to untick "Run Steam" on the installer's final page — the action that prevents the steamwebhelper hang — without disturbing the existing spans.

    1. In `public/locales/en/gamepage.json`, add a new key under the `bottle.setup` object:
       `"uncheckRunSteam": "On the final Steam installer screen, UNTICK \"Run Steam\" before clicking Finish — GameLib will start Steam itself once the bottle is ready."`
       Keep the file valid JSON and consistent with the surrounding key style/escaping.

    2. In `SteamBottleSetup.tsx`, render the new instruction as a `<span className="steamBottleSetupMessage">` inside the non-error provisioning banner branch, placed as the FIRST guidance span (immediately after the provisioning/readyForLogin status span, BEFORE the existing `login` span) so the user sees the untick guidance while the installer is still open. Use `t('bottle.setup.uncheckRunSteam', '<same default string>')`. Do NOT remove or reorder the existing `login`, `sameAccount`, or `hangHint` spans — the hang recovery hint stays.

    3. Leave the consent-dialog branch, provisioning logic, polling, and all other copy unchanged. This is copy/i18n only — no process-detection or backend changes (per the gap's "do not over-engineer" constraint).
  </action>
  <verify>
    <automated>node -e "const j=require('./public/locales/en/gamepage.json'); if(!j.bottle.setup.uncheckRunSteam) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "uncheckRunSteam" src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx` ≥ 1 (rendered) and `node -e "const j=require('./public/locales/en/gamepage.json'); if(!j.bottle.setup.uncheckRunSteam) process.exit(1)"` exits 0 (key present, valid JSON).
    - Order assertion: the uncheckRunSteam span precedes the existing login span — `grep -n "uncheckRunSteam\|bottle.setup.login" src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx` shows uncheckRunSteam on an EARLIER line than bottle.setup.login.
    - Non-regression: the existing hang recovery hint is retained — `grep -c "hangHint" src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx` ≥ 1 and `node -e "const j=require('./public/locales/en/gamepage.json'); if(!j.bottle.setup.hangHint) process.exit(1)"` exits 0.
    - Non-regression: `git diff --name-only` shows changes ONLY to SteamBottleSetup.tsx and public/locales/en/gamepage.json (no backend, no other component).
    - `npm run codecheck` exits 0 (tsc --noEmit).
    - HUMAN-OBSERVABLE (macOS + CrossOver, UAT retest, deferred): on the provisioning banner the user is clearly told to untick "Run Steam"; following it, the bottled client is NOT auto-launched by the installer and the steamwebhelper hang does not occur — automated coverage locks the key + render + ordering.
  </acceptance_criteria>
  <done>The provisioning banner explicitly instructs the user to untick "Run Steam" on SteamSetup's final screen (rendered before the login guidance), the steamwebhelper recovery hint is retained, and the new i18n key is valid — closing GAP-17-STEAMWEBHELPER-HANG with a copy/i18n-only change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → user (instruction copy) | Static, translated instruction text only. No input parsed, no IPC added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13-COPY | Information Disclosure | setup banner copy | accept | Static i18n string; no dynamic data, no user input, no new channel. Attack surface is nil. |
| T-17-13-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this plan. |
</threat_model>

<verification>
- `node -e "const j=require('./public/locales/en/gamepage.json'); if(!j.bottle.setup.uncheckRunSteam) process.exit(1)"` exits 0 (key present, valid JSON).
- `grep` confirms the uncheckRunSteam span is rendered and ordered before the login span, and hangHint is retained.
- `npm run codecheck` exits 0.
- `git diff --name-only` confirms only SteamBottleSetup.tsx + gamepage.json changed.
- Manual UAT (17-07 retest, macOS + CrossOver): the banner tells the user to untick "Run Steam"; following it avoids the steamwebhelper Force-Quit hang, satisfying step-7's recovery-hint expectation.
</verification>

<success_criteria>
The guided-setup provisioning banner explicitly instructs the user to untick "Run Steam" on SteamSetup.exe's final screen (so GameLib, not the installer, launches Steam once the bottle is ready), while retaining the steamwebhelper self-update recovery hint — closing GAP-17-STEAMWEBHELPER-HANG. No backend or process-detection changes.
</success_criteria>

<output>
Create `.planning/phases/17-steam-on-macos-via-crossover-wine-windows-only-steam-games-i/17-13-SUMMARY.md` when done. Record the new i18n key, its placement (first guidance span in the provisioning banner), and confirm the existing hangHint recovery copy was retained.
</output>
