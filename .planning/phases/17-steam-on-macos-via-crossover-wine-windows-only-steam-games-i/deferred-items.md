# Deferred Items — Phase 17

## From 17-11 (GAP 3 gap-closure)

- **Pre-existing async leak in `src/backend/storeManagers/steam/library.ts` (`pollInstallOnce` → `readAcfState`)**: `npm test` full-suite run prints an uncaught `TypeError: Cannot read properties of undefined (reading 'map')` from a leftover `setTimeout`-driven poll firing after Jest's suite teardown ("A worker process has failed to exit gracefully"). All 48 suites / 938 tests still pass — this is a stray timer leak, not a test failure. Out of scope for 17-11 (that plan's files are frontend-only: `SteamBottleSetup.ts`, `types.ts`, `GameContext.tsx`, `GamePage/index.tsx`, `MainButton.tsx`, `GameStatus.tsx`, `gamepage.json`, and the selector test — none touch `steam/library.ts`). Needs its own investigation/fix (likely a missing `clearInterval`/`.unref()` in a test or the poller itself).

## From 17-12 (GAP-17-PFX86-PATH gap-closure)

- **Pre-existing async leak in `src/backend/storeManagers/steam/bottle.ts` (`raiseInstallerWindow`)**: running `npm test -- --testPathPattern="steam.*bottle"` prints `Jest did not exit one second after the test run has completed` followed by `ReferenceError: You are trying to 'import' a file after the Jest environment has been torn down` pointing at `bottle.ts:289` (`raiseInstallerWindow`'s real (non-fake-timer) `sleep(1500)` retry loop, fired `void`/fire-and-forget from `provisionBottle()` step 7 and the `install` verb in `dispatchToBottledSteam()`). All 55 tests in the suite still pass; the process just exits non-zero (`1`) after teardown because the ~18s poll loop is still in flight when Jest force-exits. Out of scope for 17-12 — this plan only touches `resolveBottleSteamRoot`/`getBottleSteamExePath`/`getBottleSteamappsDir`, never `raiseInstallerWindow`, `provisionBottle`, or `dispatchToBottledSteam`, and the leak pre-dates this plan (introduced by the GAP 5 installer-window-raise feature). Needs its own fix: either fake-timer the `raiseInstallerWindow` retry loop in the `provisionBottle`/`tellBottledSteamToInstall` tests, or have the tests mock `raiseInstallerWindow` itself.
- **Plan-authoring note**: the plan's literal verify commands (`npm test -- --testPathPattern=steam/bottle`, `npm test -- --testPathPattern=steam/library`) match ZERO files and exit 1 with "No tests found" — the actual test paths are `src/backend/storeManagers/steam/__tests__/bottle.test.ts` / `.../library.test.ts`, and `__tests__/` sits between `steam/` and `bottle`/`library` so the literal regex fragment never matches. Verified instead with `npm test -- --testPathPattern="steam.*bottle"` and `npm test -- --testPathPattern="steam.*library"` (both pass, 55/55 and 74/74 respectively). No source or plan-locked behavior at issue — just a verify-command authoring detail for future plans to use `.*` between segments.
