---
phase: 23-steam-full-ownership-install-stateflags-4
plan: 04
type: execute
wave: 4
depends_on: [23-03]
files_modified:
  - .planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md
autonomous: false
requirements: [REQ-23-07]

must_haves:
  truths:
    - "23-UAT.md exists documenting the three D-07 real-hardware gates with exact steps, macOS-first"
    - "A multi-depot larger title installs under StateFlags=4 across depots with no Steam verify pass and no re-download, then launches"
    - "A confirmed hard-DRM title launches under StateFlags=4 with no re-validation (closes spike-001's DRM caveat)"
    - "An interrupt-then-resume run reconciles to a Steam-trusted StateFlags=4 and launches with no full re-download and no silent Steam-in-CrossOver auto-open"
  artifacts:
    - path: ".planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md"
      provides: "D-07 hardware validation record (3 gates) following the 21-UAT.md format"
      contains: "StateFlags"
  key_links:
    - from: "23-UAT.md"
      to: "human verifier on real macOS + real Steam client"
      via: "blocking checkpoint gating phase completion"
      pattern: "StateFlags"
---

<objective>
Author and execute the D-07 pre-ship real-hardware validation gate (REQ-23-07). StateFlags=4 tells Steam "do not verify" — the only way to prove Steam actually trusts a GameLib-authored `4` across the real risk surface is real hardware. These three checks cannot be automated (they require a live Steam client, real owned titles, and real depots) and they BLOCK phase completion.

Purpose: The exec-bit surprise (`os error 256`) is the template for this phase's risk — sha1 proves content, not everything Steam's verify pass silently provided. The three gates cover the surfaces spike-003 did not: multi-depot, hard-DRM, and interrupt-resume.
Output: `23-UAT.md` (the validation record) + a blocking human-verify checkpoint recording pass/fail for all three gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-steam-full-ownership-install-stateflags-4/23-CONTEXT.md
@.planning/phases/23-steam-full-ownership-install-stateflags-4/23-VALIDATION.md
@.planning/phases/21-steam-native-install/21-UAT.md
@.planning/spikes/003-stateflags4-full-ownership/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 23-UAT.md with the three D-07 hardware gates</name>
  <files>.planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md</files>
  <read_first>
    - .planning/phases/21-steam-native-install/21-UAT.md — the format precedent (gate table, per-gate steps, pass/fail/notes columns) to mirror.
    - .planning/phases/23-steam-full-ownership-install-stateflags-4/23-VALIDATION.md — the "Manual-Only Verifications" table (the three D-07 gates + REQ mapping); note its automated commands say `yarn vitest` which is WRONG (repo uses `pnpm jest`) — do not carry that error into 23-UAT.md.
    - .planning/phases/23-steam-full-ownership-install-stateflags-4/23-CONTEXT.md — D-07 exact wording (multi-depot, hard-DRM, interrupt-resume; macOS-first; WazHack 264160 single-depot control, Cyberpunk multi-depot target).
    - .planning/spikes/003-stateflags4-full-ownership/README.md — the spike's own open items now owned by this phase.
  </read_first>
  <action>
    Create 23-UAT.md following the 21-UAT.md structure. Document three gates, macOS-first. Gate 1 MULTI-DEPOT: install a multi-depot larger title (Cyberpunk, once Phase 21 D-UAT-08 is verified; note WazHack 264160 as the single-depot control) via the native install path; steps confirm the written .acf has StateFlags "4", Steam shows Ready with NO verify pass and NO re-download across depots, and the game launches. Gate 2 HARD-DRM: install a confirmed hard-DRM title; launch via steam://; confirm launch with no re-validation (closes spike-001's DRM caveat). Gate 3 INTERRUPT-RESUME: start an install, kill Steam/GameLib mid-download, resume; confirm reconciliation produces a Steam-trusted StateFlags "4", the game launches, there is NO full re-download, and NO silent Steam-in-CrossOver auto-open occurs. For each gate include preconditions, exact steps, expected result, a pass/fail box, and a notes field. State explicitly that Windows/Linux coverage is a deferred follow-up (not a Phase 23 gate). Add an inspection tip: use .planning/spikes/003-stateflags4-full-ownership/inspect-acf.mjs to read the written manifest's field set.
  </action>
  <verify>
    <automated>test -f .planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md && grep -q "StateFlags" .planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 23-UAT.md exists and contains three clearly numbered gates (multi-depot, hard-DRM, interrupt-resume).
    - Each gate has preconditions, exact steps, expected result, and a pass/fail box.
    - The doc states macOS-first and that Windows/Linux is a deferred follow-up.
    - No `yarn vitest` command appears in the doc (repo uses pnpm jest; these gates are manual anyway).
  </acceptance_criteria>
  <done>23-UAT.md authored with all three D-07 gates in the 21-UAT.md format.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Execute the three D-07 real-hardware gates (macOS)</name>
  <action>Human-only verification task (no code). On real macOS with a real Steam client and owned titles, run each of the three D-07 gates defined in 23-UAT.md, inspect the written manifest with .planning/spikes/003-stateflags4-full-ownership/inspect-acf.mjs, and record PASS/FAIL + observations in each gate box. This is a blocking gate: any FAIL blocks phase completion and routes to /gsd-plan-phase 23 --gaps.</action>
  <what-built>
    The full StateFlags=4 native-install path (Plans 01-03): file-mode fidelity, the completeness gate that writes a trustworthy 4 or falls back to 1026, buildid threading, and sha1-gated resume/reconciliation.
  </what-built>
  <how-to-verify>
    On real macOS with a real Steam client and owned titles, execute each gate in 23-UAT.md and record the result:
    1. MULTI-DEPOT: install a multi-depot larger title via GameLib's native path. Confirm the written appmanifest_{appId}.acf has StateFlags "4" (inspect with inspect-acf.mjs), Steam shows the game Ready with NO verify pass and NO re-download across depots, and it launches.
    2. HARD-DRM: install a confirmed hard-DRM title, launch via steam://, confirm launch with no re-validation.
    3. INTERRUPT-RESUME: start an install, kill Steam/GameLib mid-download, resume, confirm reconciliation yields a Steam-trusted StateFlags "4", the game launches, there is no full re-download, and no silent Steam-in-CrossOver auto-open.
    Fill in each pass/fail box in 23-UAT.md with observations. Any FAIL blocks phase completion and should route to `/gsd-plan-phase 23 --gaps`.
  </how-to-verify>
  <acceptance_criteria>
    - All three gates recorded PASS in 23-UAT.md, OR failures documented for gap closure.
    - Multi-depot install confirmed StateFlags=4 with no verify/re-download.
    - Hard-DRM title confirmed launching under StateFlags=4.
    - Interrupt-resume confirmed reconciled StateFlags=4 + launch + no re-download + no bottle auto-open.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all three gates pass, or describe the failing gate(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GameLib StateFlags=4 assertion → real Steam client | shipping a "do not verify" manifest that real Steam has never been proven to trust across multi-depot/DRM/resume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-11 | Tampering / Repudiation | StateFlags=4 shipped without real-hardware proof Steam trusts it across the untested-by-spike surfaces (multi-depot, hard-DRM, interrupt-resume) | mitigate | this blocking human gate: no ship until all three gates pass on real macOS hardware; failures route to gap closure |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | no installs in this plan (documentation + manual verification only) |
</threat_model>

<verification>
- 23-UAT.md exists with three gates; all three recorded PASS on real macOS hardware before `/gsd-verify-work`.
- This is a manual gate — no automated command proves Steam's trust of a StateFlags=4 manifest; that is exactly why D-07 exists.
</verification>

<success_criteria>
- All three D-07 gates pass on real macOS: multi-depot StateFlags=4 (no verify/re-download), hard-DRM launch, interrupt-resume reconciled StateFlags=4 (no re-download, no bottle auto-open).
- Windows/Linux coverage explicitly deferred, not silently dropped.
</success_criteria>

<output>
Create `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-04-SUMMARY.md` when done.
</output>
