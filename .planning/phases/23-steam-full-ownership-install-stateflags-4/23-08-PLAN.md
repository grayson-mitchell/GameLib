---
phase: 23-steam-full-ownership-install-stateflags-4
plan: 08
type: execute
wave: 8
depends_on: [23-07]
gap_closure: true
files_modified:
  - src/backend/storeManagers/steam/depot.ts
  - src/backend/storeManagers/steam/depot/flagsCensus.ts
  - src/backend/storeManagers/steam/__tests__/depot.test.ts
  - src/backend/storeManagers/steam/__tests__/flagsCensus.test.ts
  - src/backend/storeManagers/steam/__tests__/depot.finalize.test.ts
autonomous: true
requirements: [REQ-23-06, REQ-23-01, REQ-23-07]

must_haves:
  truths:
    - "A native macOS game installed via the StateFlags=4 full-ownership path lands its Mach-O executables with the execute bit — the manifest's EDepotFileFlag EXECUTABLE_FLAG(32)/CUSTOM_EXECUTABLE_FLAG(128) entries actually reach chmod 0o755"
    - "The root cause confirmed in 23-TRACE.md is fixed at its actual site, not masked by a downstream safety net"
    - "A run that applied ZERO file modes while the manifest claimed executable-flagged files can never report allModesApplied: true — the completeness gate fails closed to StateFlags=1026 instead of writing a false 4"
    - "The fail-closed gate reads only its own run's chmod counters — a concurrent install of a different appId can never satisfy it on another run's behalf"
    - "A zero-byte manifest entry carrying an executable flag also gets its modes applied (the size===0 early return no longer skips mode application)"
    - "A regression test fails against today's behavior and passes after the fix, so this defect cannot silently return"
  artifacts:
    - path: "src/backend/storeManagers/steam/depot.ts"
      provides: "Root-cause fix for G-23-02 + fail-closed completeness gate when zero modes were applied"
      contains: "applyEDepotFileModes"
    - path: "src/backend/storeManagers/steam/__tests__/depot.test.ts"
      provides: "RED-first regression tests: executable-flagged file lands +x; zero-byte executable-flagged file lands +x"
      contains: "EXECUTABLE_FLAG"
    - path: "src/backend/storeManagers/steam/__tests__/depot.finalize.test.ts"
      provides: "Fail-closed gate test: zero chmods against an executable-claiming manifest yields StateFlags 1026, never 4"
      contains: "1026"
  key_links:
    - from: "manifest EDepotFileFlag (EXECUTABLE_FLAG=32 / CUSTOM_EXECUTABLE_FLAG=128)"
      to: "chmod 0o755 on the landed file"
      via: "DepotPlanFile.flags -> downloadSingleFile -> applyEDepotFileModes"
      pattern: "applyEDepotFileModes"
    - from: "flags census chmodAttempts/executableFlagged counters"
      to: "canWriteFullOwnership's allModesApplied input"
      via: "zero-modes-applied against an executable-claiming manifest forces the 1026 fallback"
      pattern: "allModesApplied"
---

<objective>
Fix blocker gap **G-23-02** at the root cause confirmed by `23-TRACE.md`, and make the StateFlags=4
completeness gate fail closed when no modes were applied at all.

Today a native macOS StateFlags=4 install lands **0 of 18,809 files with `+x`** (HUMANKIND, appId
1124300), so `Humankind.app/Contents/MacOS/Humankind` is `-rw-r--r--` and launch dies with
`os error 256`. Worse, the install still reports success and writes `StateFlags=4`, because
`allModesApplied` is computed as `failures.length === 0` (depot.ts:1689) — vacuously true when zero
mode applications were ever attempted. That vacuous-truth defect is real regardless of which
hypothesis the trace confirmed, and is fixed unconditionally here.

**Task 1 is CONDITIONAL on 23-TRACE.md's VERDICT.** Read the verdict first and execute only the
matching branch. Do not guess, and do not proceed if the verdict is `AMBIGUOUS` — return to 23-07.

**User-locked constraint:** a blanket "chmod +x everything under `.app/Contents/MacOS/`" safety net is
NOT the primary fix and must not be written as one — it masks the flags bug. Task 3 permits a
narrowly-scoped Mach-O fallback ONLY if the trace proved manifest flags are genuinely absent for some
titles, and only as a clearly-labelled secondary measure behind the real fix.

Requirement mapping: the mode-application fix is **REQ-23-06** (EDepotFileFlag mode replication — the
requirement is marked Complete but was never true on the real parser path); the fail-closed gate is
**REQ-23-01** (`canWriteFullOwnership` must fail closed on any ambiguity); both unblock **REQ-23-07**'s
Gate 2 clean re-run and Gate 3.

Purpose: every native macOS game installed via the StateFlags=4 path is currently unlaunchable.
Output: a root-cause fix with RED-first regression tests, and a completeness gate that can no longer
issue a trustworthy `4` for an install whose modes were never applied.

**Scope fence:** do NOT change depot selection (`depot/select.ts`), `NON_RETRYABLE_ERESULTS`
(`depotErrors.ts`), the buildid threading, the single-flight guard from 23-05, or the progress/throttle
logic. Do NOT weaken the Directory(64)/Symlink(512) guards at depot.ts:1092/1100/1262 (23-code-review
WR-01) or the `resolveContainedPath` containment discipline (T-23-08). macOS-first per D-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-steam-full-ownership-install-stateflags-4/23-CONTEXT.md
@.planning/phases/23-steam-full-ownership-install-stateflags-4/23-TRACE.md
@.planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md
@.planning/phases/23-steam-full-ownership-install-stateflags-4/23-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Executor should use these directly. -->

The mode chain in `src/backend/storeManagers/steam/depot.ts` (line anchors are pre-23-06; re-confirm
after 23-06's census wiring shifted them):
```
fetchDepotPlanEntry()  :524-531  files = parsed.files.map(f => ({ filename: decryptFilename(f.filename, key),
                                   size: Number(f.size), sha_content: f.sha_content,
                                   flags: f.flags, linktarget: f.linktarget, chunks: f.chunks ?? [] }))
downloadSingleFile()   :1092     if (file.flags && file.flags & DIRECTORY_FLAG) { mkdir; return }
                       :1100     if (file.flags && file.flags & SYMLINK_FLAG)   { symlink; return }
                       :1134-45  if (!file.chunks.length || Number(file.size) === 0) {
                                   ...; const empty = await open(dest,'w'); await empty.close(); return  }  <-- returns BEFORE modes
                       :1177     whole-file sha1 check (throws on mismatch)
                       :1195     if (file.flags) { const r = await applyEDepotFileModes(dest, file.flags)
                                   if (!r.ok) throw ... }
applyEDepotFileModes() :1218-31  if (flags & (EXECUTABLE_FLAG|CUSTOM_EXECUTABLE_FLAG)) await chmod(dest, 0o755)
                                 if (flags & (READONLY_FLAG|HIDDEN_FLAG)) return applyDepotFileFlags(dest, flags, process.platform)
                                 return { ok: true }
healReconciledFileModes() :1261  if (jobFiles.has(file) || !file.flags) continue
                       :1262     if (file.flags & (DIRECTORY_FLAG|SYMLINK_FLAG)) continue
downloadDepotFiles()   :1689     return { ..., allModesApplied: failures.length === 0 }   <-- VACUOUSLY TRUE
```

Upstream parser (verified this session) — `node_modules/steam-user/components/content_manifest.js`
`exports.parse(buffer)` returns `{ files: ContentManifestPayload.mappings, ...metadata }`. The proto
(`node_modules/steam-user/protobufs/content_manifest.proto`) declares
`optional uint32 flags = 3` on `ContentManifestPayload.FileMapping`. `parse()` post-walks each file
converting ByteBuffer/Long/Buffer values to strings (which is why `sha_content` arrives as a hex
string, hence depot.ts:1174's `Buffer.isBuffer(...) ? ... : String(...)` branch); `flags` is a plain
number and is untouched by that walk.

Completeness gate (`canWriteFullOwnership`, depot.ts ~:832-860) — its inputs today:
```
outcome === 'completed', failures.length === 0, buildid !== '0',
allFilesVerified === true, opts.allModesApplied === true
```
`finalizeToSteam` (~:1786-1816) passes `allModesApplied: opts.allModesApplied ?? false` and picks
`stateFlags: <gate> ? '4' : undefined`; `manifest.ts` defaults `stateFlags ?? '1026'`. The 1026 path
is the D-01 fallback and must stay byte-identical.

Census surface from 23-06 (`src/backend/storeManagers/steam/depot/flagsCensus.ts`):
`summarizeDepotFlags(plan): DepotFlagsCensus` with `totalFiles`, `flagBearing`, `executableFlagged`,
`readonlyFlagged`, `hiddenFlagged`, `directoryEntries`, `symlinkEntries`, `zeroSizeEntries`,
`distinctFlagValues`; plus the per-run counters `modeCallsites` and `chmodAttempts` in depot.ts.

Existing test style: `src/backend/storeManagers/steam/__tests__/depot.test.ts` runs
`downloadDepotFiles` against a real tmpdir with hand-built `DepotPlanFile` literals (see the
`flags: 8 | 32` / `flags: 64` / `flags: 512` describe blocks). `depot.finalize.test.ts` covers
`finalizeToSteam`/`canWriteFullOwnership`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the confirmed root cause from 23-TRACE.md's VERDICT</name>
  <files>src/backend/storeManagers/steam/depot.ts, src/backend/storeManagers/steam/__tests__/depot.test.ts</files>
  <read_first>
    - `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-TRACE.md` — **READ THE VERDICT SECTION FIRST.** It names exactly one confirmed hypothesis (H1-H5) and the four field-cited refutations. If the verdict is `AMBIGUOUS` or absent, STOP and report back rather than choosing a branch.
    - MANDATORY graphify first: `graphify explain "fetchDepotPlanEntry"`, `graphify explain "downloadSingleFile"`, `graphify explain "applyEDepotFileModes"`.
    - src/backend/storeManagers/steam/depot.ts — `fetchDepotPlanEntry` (494-539), `downloadSingleFile` (1053-1203), `applyEDepotFileModes` (1218-1231), `healReconciledFileModes` (1251+), plus the `DepotPlanFile` type declaration and the flag constants. Line anchors shifted by 23-06's census wiring — re-locate by symbol, not by number.
    - node_modules/steam-user/components/content_manifest.js and node_modules/steam-user/protobufs/content_manifest.proto — only if the verdict is H1 or H5.
    - src/backend/storeManagers/steam/__tests__/depot.test.ts — the existing `flags`-bearing describe blocks (~2564, 2639, 2840) for the tmpdir + DepotPlanFile literal test style to mirror.
  </read_first>
  <behavior>
    - RED first, in every branch: write a test that FAILS against today's code and passes only after the fix. Do not write the fix before seeing the test fail.
    - Test (branch-independent, the acceptance behavior): a `downloadDepotFiles` run over a plan whose manifest entry carries `EXECUTABLE_FLAG` (32) leaves the landed file with an execute bit set — asserted via `fs.stat(dest).mode & 0o111` being non-zero, on a real tmpdir. If the verdict is H1/H5, this test must be driven through the SAME code path the fix repairs (e.g. a plan produced by the repaired mapping / a plan that round-trips the repaired boundary), not by a hand-built literal that bypasses the defect.
    - Test (H1/H5 branches): a `DepotPlanFile` produced from a realistic parser-shaped input carries a numeric `flags` value — proving the mapping/boundary no longer drops it. Build the input to mirror the real `ContentManifestPayload.FileMapping` shape.
    - Test (H2 branch): the census reports `executableFlagged: 0` for a manifest that genuinely has no executable bits, and the completeness gate therefore refuses `StateFlags=4` (this is Task 2's gate, exercised here for the H2 case).
    - Test (H3 branch): after the fix, the chmod target path equals the landed file path for every executable-flagged entry, and the mode survives to the end of the run.
    - Test (H4 branch): a resume whose reconciler skips already-present executable-flagged files still ends with those files carrying `+x` (i.e. `healReconciledFileModes` covers them).
  </behavior>
  <action>
    Read `23-TRACE.md`'s VERDICT and execute ONLY the matching branch. Record in the summary which branch
    ran and quote the verdict line that selected it.

    - **H1 (flags never populated by the parser mapping — the leading hypothesis).** Repair
      `fetchDepotPlanEntry`'s mapping at depot.ts:524-531 so the per-file `flags` bitfield from
      `ContentManifestPayload.FileMapping` field 3 actually reaches `DepotPlanFile.flags`. Determine
      empirically (from the trace evidence and the decoded object) whether the value is absent, exposed
      under a different key, or nested, and read it correctly — do NOT paper over it with a default.
      Preserve the existing `flags?: number` semantics: absent must remain absent (so the
      Directory/Symlink/`if (file.flags)` guards keep their current meaning), never coerced to `0` in a
      way that changes a guard, and never coerced to a nonzero default. Add a plan-build-time
      `logWarning` when `summarizeDepotFlags(plan).flagBearing === 0` for a non-empty plan — a manifest
      where literally no entry carries any flag is anomalous and must be visible in the log, not silent.
    - **H5 (flags dropped between plan-build and download).** Fix the serialization/copy boundary the
      trace identified so `flags` survives it, mirroring how `sha_content` is already defensively
      handled at depot.ts:1174. Add a test that round-trips a plan across that boundary and asserts
      `flags` survives.
    - **H3 (modes applied then lost / wrong path).** Fix the identified site — e.g. make mode
      application happen after the final write of the file, or correct the destination path so chmod
      targets the landed file. Reuse `resolveContainedPath` for any path work; never `path.join`
      (T-23-08).
    - **H4 (reconciler skipped the files and the heal loop skipped them too).** Fix the skip condition in
      `healReconciledFileModes` so a reconciled file that should carry modes actually gets them, keeping
      its Directory(64)/Symlink(512) skip guard (WR-01) intact.
    - **H2 (manifest genuinely carries no executable bits).** There is no mapping bug to fix here — leave
      Task 1's code unchanged, record that in the summary, and rely on Task 2's fail-closed gate plus
      Task 3's narrowly-scoped fallback.

    In every branch: change ONLY what the verdict implicates. Do not add a blanket chmod, do not relax
    any guard, do not touch `select.ts`, `depotErrors.ts`, buildid threading, or the 1026 writer.
  </action>
  <verify>
    <automated>pnpm jest src/backend/storeManagers/steam/__tests__/depot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The summary states which VERDICT branch ran and quotes the verdict line from 23-TRACE.md that selected it.
    - A new test in depot.test.ts asserts `(await stat(dest)).mode & 0o111` is non-zero for an `EXECUTABLE_FLAG`-carrying manifest entry after `downloadDepotFiles`, and the git history shows it committed RED (failing) before the fix commit.
    - `pnpm jest src/backend/storeManagers/steam/__tests__/depot.test.ts` exits 0 with all previously-green tests still passing.
    - `git diff` shows NO change to `src/backend/storeManagers/steam/depot/select.ts` or `src/backend/storeManagers/steam/depotErrors.ts`.
    - `grep -v '^\s*[*/]' src/backend/storeManagers/steam/depot.ts | grep -c "Contents/MacOS"` returns 0 — no blanket `.app` path-based chmod was introduced in this task.
    - The Directory/Symlink guards remain: `grep -c "DIRECTORY_FLAG\|SYMLINK_FLAG" src/backend/storeManagers/steam/depot.ts` is >= its pre-change count.
    - `npx tsc --noEmit -p .` exits 0.
  </acceptance_criteria>
  <done>The confirmed root cause is fixed at its own site, proven by a test that failed before the change and passes after.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed — zero modes applied can never earn StateFlags=4; zero-byte entries get modes too</name>
  <files>src/backend/storeManagers/steam/depot.ts, src/backend/storeManagers/steam/__tests__/depot.test.ts, src/backend/storeManagers/steam/__tests__/depot.finalize.test.ts, src/backend/storeManagers/steam/depot/flagsCensus.ts, src/backend/storeManagers/steam/__tests__/flagsCensus.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/depot.ts — `canWriteFullOwnership` (~832-860), `finalizeToSteam` (~1786-1816), the `DepotDownloadResult` construction at ~1689 (`allModesApplied: failures.length === 0`), and `downloadSingleFile`'s zero-size early return (~1134-1145). Re-locate by symbol; 23-06/Task 1 shifted line numbers.
    - src/backend/storeManagers/steam/depot/flagsCensus.ts — the census fields shipped by 23-06 (`executableFlagged`, `flagBearing`) and the per-run counters `modeCallsites` / `chmodAttempts`.
    - src/backend/storeManagers/steam/__tests__/depot.finalize.test.ts — the existing `canWriteFullOwnership` / `finalizeToSteam` describe blocks and their opts-shape fixtures.
    - src/backend/storeManagers/steam/depot/manifest.ts — the `stateFlags ?? '1026'` / `bytes ?? '0'` / `buildid ?? '0'` defaults that are the single source of the safe fallback (must stay untouched).
  </read_first>
  <behavior>
    - Test: a `downloadDepotFiles` run over a plan whose census reports `executableFlagged > 0` but whose run recorded `chmodAttempts === 0` returns `allModesApplied: false` — the vacuous-truth case that let HUMANKIND write a `4` with zero execute bits.
    - Test: a plan with `executableFlagged: 0` (genuinely no executable-flagged entries) and `chmodAttempts: 0` still returns `allModesApplied: true` — the gate must not false-positive on manifests that legitimately have no exec bits (e.g. a pure data depot), or every install would fall back to 1026.
    - Test (CONCURRENCY): with two `downloadDepotFiles` runs for DIFFERENT appIds overlapping in time — run A applying real chmods, run B legitimately applying zero against a plan with `executableFlagged > 0` — run B still returns `allModesApplied: false`. Run A's chmod activity must not satisfy run B's gate. Run both start orders.
    - Test: `canWriteFullOwnership` with `allModesApplied: false` yields `StateFlags "1026"` from `finalizeToSteam`, with the 1026 field set byte-identical to the existing fallback (buildid `"0"`, bytes `"0"`).
    - Test: a manifest entry with `size: 0`, no chunks, and `flags: EXECUTABLE_FLAG` lands as an empty file that DOES carry an execute bit (today the zero-size early return skips mode application entirely).
    - Test: a `size: 0` entry with `flags: DIRECTORY_FLAG` or `flags: SYMLINK_FLAG` still takes its existing directory/symlink branch and never reaches chmod (WR-01 guard preserved).
  </behavior>
  <action>
    Two unconditional fixes, both independent of the trace verdict.

    (a) **Fail-closed completeness gate.** `allModesApplied` is currently `failures.length === 0`
    (depot.ts ~:1689), which is vacuously true when zero mode applications ever ran. Strengthen it: a
    run must additionally have actually applied modes whenever the manifest claimed executable-flagged
    entries. Use 23-06's census + counters — if `summarizeDepotFlags(plan).executableFlagged > 0` and
    **this run's own** `chmodAttempts === 0`, set `allModesApplied: false` so `canWriteFullOwnership` fails
    closed to the unchanged `1026` handoff (D-01) instead of writing a false `4`. Emit a `logWarning`
    naming both numbers when this fires — a silent downgrade to 1026 would be its own debugging trap.

    **Counter scoping (contract established by 23-06, do not re-litigate here):** `chmodAttempts` and
    `modeCallsites` are scoped per `downloadDepotFiles` invocation, NOT module-level, because
    `downloadDepotFiles` is single-flight per appId but NOT globally single-flight — concurrent
    installs of different appIds are supported and regression-tested since 23-05. Read the counters
    belonging to THIS run only. If 23-06 shipped a shape that does not guarantee that (e.g. shared
    module state), fix the scoping before wiring the gate — a counter value leaked from a concurrent
    install would silently defeat this fail-closed check and reopen T-23-27 (a false `StateFlags=4`
    over an install whose modes were never applied).

    Do NOT change `canWriteFullOwnership`'s other inputs (`outcome`, `failures`, `buildid`,
    `allFilesVerified`), and do NOT change `manifest.ts`'s defaults — the 1026 fallback must stay
    byte-identical.

    Note the ordering subtlety: on a fully-reconciled resume where every file was already correct AND
    already had its modes, `chmodAttempts` may legitimately be non-zero only because
    `healReconciledFileModes` re-applies idempotently — that path already runs chmod, so it satisfies
    the gate. If a code path is found where a legitimately-complete install would record
    `chmodAttempts === 0` despite `executableFlagged > 0`, prefer fixing that path over loosening the
    gate; fail-closed to 1026 is the correct, honest outcome (D-01) and is never a crash.

    (b) **Zero-byte entries must get their modes.** `downloadSingleFile`'s
    `if (!file.chunks.length || Number(file.size) === 0)` branch creates the empty file and returns
    BEFORE the `if (file.flags)` mode block (depot.ts ~:1134-1145 vs ~:1195). A zero-byte
    executable-flagged entry therefore lands without `+x`. Restructure so the empty-file branch also
    runs `applyEDepotFileModes` with the same error handling (a mode failure throws, recorded as a
    `DepotDownloadFailure`, never a silent success — T-23-03). The Directory(64) and Symlink(512)
    branches above it must continue to return BEFORE any mode application, unchanged (WR-01): a
    directory or symlink path must never reach chmod/attrib.exe. Extend `healReconciledFileModes`'s
    coverage to match if the equivalent gap exists there.
  </action>
  <verify>
    <automated>pnpm jest src/backend/storeManagers/steam</automated>
  </verify>
  <acceptance_criteria>
    - `depot.finalize.test.ts` contains a test asserting `StateFlags "1026"` (never `"4"`) is written when `executableFlagged > 0` and `chmodAttempts === 0`, and the git history shows it committed RED before the fix.
    - `depot.finalize.test.ts` contains a test asserting `allModesApplied: true` is still returned for a plan with `executableFlagged: 0` — no false downgrade.
    - A test proves the fail-closed gate reads only its own run's counters: with a concurrent install of a DIFFERENT appId applying real chmods, a run that itself applied zero modes against an `executableFlagged > 0` plan still returns `allModesApplied: false` (both start orders). The gate is not defeatable by another install's activity.
    - `grep -v '^\s*[*/]' src/backend/storeManagers/steam/depot.ts | grep -cE "^(let|const|var) +(chmodAttempts|modeCallsites)"` returns 0 — the counters remain per-invocation, never module-level.
    - `depot.test.ts` contains a test asserting a `size: 0` + `flags: 32` entry lands with `(await stat(dest)).mode & 0o111` non-zero.
    - `depot.test.ts` still asserts a `size: 0` + `flags: 64` entry becomes a real directory and a `flags: 512` entry becomes a real symlink, with no chmod applied to either (existing WR-01 tests still green).
    - `git diff src/backend/storeManagers/steam/depot/manifest.ts` is empty — the 1026 fallback defaults are untouched.
    - `pnpm jest src/backend/storeManagers/steam` exits 0 — full steam suite green.
    - `npx tsc --noEmit -p .` exits 0.
  </acceptance_criteria>
  <done>An install that never applied a single mode can no longer claim a trustworthy StateFlags=4, and zero-byte executables get their execute bit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (SECONDARY, CONDITIONAL): narrowly-scoped Mach-O executable fallback</name>
  <files>src/backend/storeManagers/steam/depot.ts, src/backend/storeManagers/steam/__tests__/depot.test.ts</files>
  <read_first>
    - `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-TRACE.md` — the VERDICT and the H2 row. **This task runs ONLY if the verdict is H2** (manifest flags genuinely absent/without executable bits for the failing title). For any other verdict, SKIP this task entirely and record "skipped — verdict was H<n>, not H2" in the summary.
    - src/backend/storeManagers/steam/depot.ts — `downloadSingleFile`'s post-sha1 mode block and `resolveContainedPath` usage; the Directory/Symlink guards.
    - .planning/phases/23-steam-full-ownership-install-stateflags-4/23-SECURITY.md — T-23-01 (argv-form subprocess only, never shell-form) and the containment discipline (`resolveContainedPath`; `path.join` is NOT containment).
  </read_first>
  <behavior>
    - Test: with the fallback active, a file whose first bytes are a Mach-O magic (`0xFEEDFACF` / `0xFEEDFACE` / the `0xCAFEBABE` fat magic, little- and big-endian as applicable) and which carries NO executable flag in the manifest lands with an execute bit.
    - Test: a non-Mach-O file (e.g. a plain text asset) with no executable flag does NOT get an execute bit — the fallback is content-gated, not path-gated.
    - Test: the fallback never runs on a Directory(64) or Symlink(512) manifest entry.
    - Test: the fallback is a no-op on non-POSIX platforms (`process.platform === 'win32'`).
  </behavior>
  <action>
    **Only if 23-TRACE.md's verdict is H2.** Add a clearly-labelled SECONDARY fallback: after the
    manifest-driven mode application, for POSIX platforms only, detect a Mach-O executable by reading
    its magic header bytes from the already-landed file and apply `chmod 0o755` if the manifest did not
    already flag it executable.

    Constraints, all load-bearing:
    - **Content-gated, never path-gated.** Detect by Mach-O magic bytes, NOT by a
      `Contents/MacOS/` path pattern — a path-based blanket chmod is exactly the safety net the user
      rejected, and it would also chmod attacker-controllable manifest paths.
    - Resolve the destination with `resolveContainedPath` only; never `path.join` (T-23-08).
    - Never apply to Directory(64)/Symlink(512) entries — reuse the existing guard, do not reimplement it.
    - No subprocess. If any subprocess were ever needed, it must be argv-form, never shell-form (T-23-01).
    - Add a `logWarning` naming the file whenever the fallback fires, so a manifest missing its
      executable flags stays VISIBLE rather than silently patched over.
    - Add a code comment stating plainly that this is a secondary compensator for manifests whose
      EDepotFileFlag executable bits are absent (per the 23-TRACE.md H2 verdict), and that the primary
      contract remains the manifest's own flags.
  </action>
  <verify>
    <automated>pnpm jest src/backend/storeManagers/steam</automated>
  </verify>
  <acceptance_criteria>
    - If the verdict was not H2: no code change in this task, and the summary records "skipped — verdict was H<n>".
    - If the verdict was H2: `grep -v '^\s*[*/]' src/backend/storeManagers/steam/depot.ts | grep -c "Contents/MacOS"` returns 0 — detection is by magic bytes, never by path.
    - A test asserts a non-Mach-O unflagged file is left WITHOUT an execute bit.
    - A test asserts the fallback is a no-op when `process.platform` is `win32`.
    - `pnpm jest src/backend/storeManagers/steam` exits 0 and `npx tsc --noEmit -p .` exits 0.
  </acceptance_criteria>
  <done>Either the fallback is correctly scoped, content-gated, and loudly logged — or it was correctly skipped because the trace proved a real mapping bug instead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Steam CM/CDN manifest → `DepotPlanFile` (filename, flags, linktarget) → filesystem | Fully untrusted remote data selects both the destination path and the permission bits applied to it |
| Depot writer → user-writable install root (`steamapps/common/<installdir>`) | chmod is applied to paths inside a directory the user (and any local process) can also write |
| `canWriteFullOwnership` → `StateFlags=4` manifest the real Steam client trusts | A false `4` tells Steam to skip its own verify pass on content GameLib could not prove complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-24 | Elevation of Privilege | A manifest-controlled `flags` value newly reaching `chmod` could set the execute bit on a file the attacker also controls the contents of, turning a data file into an executable dropped inside the user's Steam library | mitigate | The exec bit is applied only to files that passed the whole-file SHA1 check against the signed manifest (depot.ts:1177) — content is pinned before mode. Mode is `0o755` (no setuid/setgid, no group/other write), matching what Steam's own verify pass applies. Task 3, if it runs, is additionally gated on Mach-O magic bytes read from the verified file |
| T-23-25 | Elevation of Privilege | chmod following a symlink placed in the install root by a local process could set `0o755` on a file OUTSIDE the install root (path traversal via symlink) | mitigate | Every destination is resolved through `resolveContainedPath` (resolve + relative containment; `path.join` is explicitly NOT containment), and the Directory(64)/Symlink(512) manifest entries return before any mode application (WR-01, depot.ts:1092/1100/1262). Acceptance criteria assert the guard count does not decrease and that `size: 0` directory/symlink entries still take their own branch. Task 2(b) explicitly preserves the Directory/Symlink early returns ahead of the new zero-byte mode application |
| T-23-26 | Tampering | The zero-byte mode-application change (Task 2b) could reorder the Directory/Symlink guards and let a symlink entry reach chmod/attrib.exe | mitigate | The guards stay strictly above the empty-file branch; a regression test asserts `flags: 64` still yields a real directory and `flags: 512` a real symlink, neither chmod'd |
| T-23-27 | Spoofing | A false `StateFlags=4` tells the Steam client to trust an install whose modes (and therefore whose executables) were never actually applied — Steam then skips the verify pass that would have repaired it | mitigate | Task 2(a) makes `allModesApplied` fail closed when `executableFlagged > 0` and `chmodAttempts === 0`, forcing the byte-identical `1026` verify-handoff (D-01). The downgrade is logged, never silent. The counters feeding the gate are scoped per `downloadDepotFiles` invocation (23-06 contract), so a concurrent install of a different appId — supported since 23-05 — cannot satisfy the gate on another run's behalf; a concurrency regression test asserts this in both start orders |
| T-23-28 | Denial of Service | An over-strict fail-closed gate could force every install to 1026, silently destroying the whole phase's value | mitigate | A required test asserts `allModesApplied: true` is still returned for a plan with `executableFlagged: 0`; the warning log makes any unexpected downgrade immediately visible |
| T-23-29 | Tampering (supply chain, upstream) | The H1/H5 fix depends on `steam-user`'s undocumented `components/content_manifest.js` internals (already flagged T-21-10); a version bump could change the decoded shape again and silently reintroduce zero-flags | mitigate | Task 1's H1 branch adds a plan-build `logWarning` when `flagBearing === 0` for a non-empty plan, and Task 2's fail-closed gate converts the silent failure into an honest 1026. No steam-user version change is made in this plan |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — source + test changes only. No dependency surface added, so no legitimacy audit is triggered |
</threat_model>

<verification>
- `pnpm jest src/backend/storeManagers/steam` exits 0 — full steam suite green plus the new RED-first regression tests.
- `npx tsc --noEmit -p .` exits 0.
- Grep gates: zero non-comment `Contents/MacOS` occurrences in depot.ts (no path-based blanket chmod); `DIRECTORY_FLAG`/`SYMLINK_FLAG` guard count not decreased; `git diff` empty for `depot/select.ts`, `depotErrors.ts`, and `depot/manifest.ts`.
- Git history shows each regression test committed RED before its GREEN fix commit.
- The summary names which VERDICT branch ran (Task 1) and whether Task 3 ran or was skipped.
</verification>

<success_criteria>
- An `EXECUTABLE_FLAG`/`CUSTOM_EXECUTABLE_FLAG` manifest entry reliably lands with `+x` through the real download path — proven by a test that failed before the fix.
- The root cause was fixed at its own site; no blanket path-based chmod safety net was introduced as the primary fix.
- A run that applied zero modes against an executable-claiming manifest fails closed to the unchanged `1026` handoff rather than writing a false `4`, and says so in the log.
- Zero-byte executable-flagged entries get their modes; Directory/Symlink entries still never reach chmod.
- No regression to depot selection, error classification, buildid threading, the 23-05 single-flight guard, or the 1026 fallback field set.
</success_criteria>

<output>
Create `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-08-SUMMARY.md` when done.
</output>
