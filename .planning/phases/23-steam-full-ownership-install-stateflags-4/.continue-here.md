---
context: phase
phase: 23-steam-full-ownership-install-stateflags-4
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-08-17T02:03:44.027Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: A "never implemented" claim in project memory can be STALE — a later phase can ship the fix under a different name. This session initially re-planned a fix that already existed (Phase 25, commit `9923545e3`) because `steam-install-slow-start-outcome.md` still said "Fix direction (future cycle): fan attempt-0 across top-N healthy hosts." Before acting on any memory claim about "not yet fixed" / "never implemented" code, grep the current codebase for the described symptom/mechanism first. The `gsd-quick` planner caught this one BEFORE writing code by re-reading the live log against current source — do the same.

**Do not proceed until the box above is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale "never implemented" memory | See constraint above | blocking | Grep the actual current code for the claimed-missing mechanism before re-planning a fix for it |
| Guessing at network performance bugs | Mid-session, a "connection pool cap" hypothesis was floated for the remaining throughput gap. It was checked (not assumed) by reading `node_modules/undici/lib/dispatcher/pool.js` directly, and ruled out — undici's default per-origin Pool has `connections: null` = unbounded. | advisory | Verify HTTP-client-library behavior against its actual source before proposing a fix for it, especially for Node's built-in `fetch`/undici defaults which are easy to misremember |

<current_state>
Phase 23 is at 9/10 plans complete. Plan 23-10 (Wave 10, the final plan) is three blocking-human
hardware-verification tasks against `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md`
— no code changes are expected from 23-10 itself. Task 1 (Gate 2 CLEAN re-run: HUMANKIND installs
and launches with StateFlags=4 and zero manual chmod) is in progress but has not produced any
recorded evidence in 23-UAT.md yet — every attempt this session was aborted before reaching the
census/launch steps, for reasons described below. Tasks 2 and 3 have not been started at all.

**This is a real-hardware session** — testing happens via `pnpm tauri:dev` (never bare `tauri dev`,
which serves a stale static bundle) against a real Steam account, installing/uninstalling real
titles (HUMANKIND, appId 1124300). Nothing here is jest-closable.
</current_state>

<completed_work>

Completed Tasks: none of 23-10's three tasks are done.

This session's actual work landed as a SEPARATE quick task (260817-ihr), not as part of 23-10:
- Task 1 install attempt #1 (real hardware): stalled completely at 29% for 13 minutes, correctly
  aborted by the no-progress watchdog (working as intended — StateFlags fell back to 1026, no
  false 4). Root-caused via the live `chunk-stream stats` log to two REAL depot-downloader
  defects, unrelated to the StateFlags=4/chmod logic 23-10 exists to verify.
- Quick task `260817-ihr` planned and executed (commits `09b8ac60a`, `f77de7c57`, `a0e2f07f4`,
  `d89059e06`): fixed a one-way-door host-demotion circuit breaker (`DEMOTED_PROBE_INTERVAL=32`
  in `hostHealth.ts`) and an accidental 8-request concurrency ceiling instead of the intended 32
  (new `InflightLimiter` at `TARGET_INFLIGHT_CHUNKS=32` in `depot.ts`/`inflightLimiter.ts`).
  1225/1225 Steam tests pass, codecheck/eslint clean. Full detail:
  `.planning/quick/260817-ihr-fix-the-steam-depot-downloader-s-single-/260817-ihr-SUMMARY.md`.
- Task 1 install attempt #2 (real hardware, with the fix live in the build): ran cleanly — zero
  rotations/timeouts/errors — but throughput plateaued around 4-12 MiB/s with rising per-request
  latency, well below the official Steam client's own ~489 Mbps on the same network/account.
  Cancelled mid-download (at 2,674 files, `appmanifest_1124300.acf` present but stale) to
  investigate rather than let a slow-but-working install run to completion.
- Investigated a "connection-reuse cap" hypothesis for the remaining gap and RULED IT OUT by
  reading undici's actual source — see constraint above.
- Corrected the now-stale `steam-install-slow-start-outcome` memory and filed a new one,
  `steam-depot-throughput-real-bottlenecks`, with the full evidence chain.
</completed_work>

<remaining_work>

- Task 1: full uninstall of HUMANKIND (install dir + `.acf`), decide how to proceed on the
  throughput question (see `<blockers>`), get a install to completion, run the census/log/`.acf`/
  launch steps exactly as written in `23-10-PLAN.md` Task 1, record Attempt 3 in `23-UAT.md`.
- Task 2: Gate 3 (INTERRUPT-RESUME) — not started at all.
- Task 3: Gate 1 launch re-confirmation + G-23-01 official-Steam-client (KCD2) diagnostic — not
  started at all. Note: Task 3b (KCD2 in the OFFICIAL Steam client, not GameLib) is independent of
  everything above and could be run in parallel/first if useful.
</remaining_work>

<decisions_made>

- Routed the throughput fix through `/gsd-quick` rather than editing code directly mid-checkpoint,
  per this project's GSD workflow enforcement (CLAUDE.md) — even though 23-10 itself is a
  no-code-changes plan, the blocker it hit required real code changes, so those went through their
  own quick-task cycle rather than being bolted onto 23-10.
- Chose to fix the two evidenced depot-downloader defects rather than accept the original "single-
  host fan-out" framing, because the framing was disproven by the stalled run's own log before any
  code was written.
- Chose to STOP and investigate rather than let the second (slow-but-not-stalling) install attempt
  run to completion, to avoid burning ~an hour+ of bandwidth on an attempt that might still be
  leaving real throughput on the table.
</decisions_made>

<blockers>
- HUMANKIND native install throughput is still well below the official Steam client's (~4-12 MiB/s
  rising-latency vs Steam's own ~489 Mbps / ~5min on the same network), even after the concurrency
  fix. NOT stalling, NOT failing — just slow. Root cause undetermined between two live hypotheses:
  (a) `InflightLimiter` only wraps the network fetch phase, so workers mid-decode/mid-write don't
  count toward the 32-slot budget, naturally under-utilizing it; (b) genuine capacity/congestion on
  the 3 regional SteamCache nodes that fairly won host selection (lowest `weightedload`) over the
  generic CDN edges. See `steam-depot-throughput-real-bottlenecks` memory for the full log evidence.
  Workaround: none yet — needs a human decision (see next_action) on whether to accept current
  throughput and finish Gate 2 anyway, or dig further first.
</blockers>

## Required Reading (in order)
1. `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-10-PLAN.md` — the three tasks,
   read verbatim, do not rewrite them
2. `.planning/quick/260817-ihr-fix-the-steam-depot-downloader-s-single-/260817-ihr-SUMMARY.md` —
   what actually shipped this session and why
3. `.planning/debug/resolved/steam-install-slow-start.md` — the 2026-08-17 follow-up section
   appended at the end, which supersedes its own Thread C "never implemented" note
4. `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md` — Gate 2's existing
   Attempt 1/Attempt 2 history (do not delete), still awaiting Attempt 3

## Critical Anti-Patterns (do NOT repeat these)
- STALE MEMORY AS FACT: see the blocking constraint above — re-verify "never implemented" /
  "not yet fixed" claims against current code before planning around them
- GUESSING AT NETWORK-LAYER ROOT CAUSE: verify HTTP client behavior against source, not memory,
  before proposing a fix

## Infrastructure State
- HUMANKIND (appId 1124300) install: PARTIAL/STALE as of this handoff — 2,674 files present at
  `~/Library/Application Support/Steam/steamapps/common/Humankind`, `appmanifest_1124300.acf`
  present at `StateFlags 1026`. Must be fully removed (both the install dir and the `.acf`) before
  any retry, per the plan's own Task 1 step 1.
- Working tree: clean except pre-existing untracked `.planning/spikes/003-*/snapshot-*.acf` files
  (not part of this work, left untouched all session — do not stage/commit them).
- No dev server / background process left running from this session.

<context>
The mental model going in: 23-10 is a pure verification plan (no code changes expected) sitting on
top of a codebase that's believed StateFlags=4/chmod-correct since 23-08. The session's actual
trajectory was almost entirely about getting ANY completed HUMANKIND install at all — first past an
orphaned-download bug and an 8-minute watchdog-as-duration-ceiling bug (both closed before this
session started, see `phase-23-waves-7-9-complete-wave-10-blocked` memory), then past two NEW
depot-throughput bugs found live tonight. None of this has touched or cast doubt on the actual
StateFlags=4/execute-bit logic under test — it's purely been "can we get a download to finish."
</context>

<next_action>
Start with: ask the user whether to (a) just retry the HUMANKIND install now that the concurrency
fix is live and stall-free, accepting the current ~4-12 MiB/s throughput and letting it run to
completion to finally get Gate 2's Attempt 3 data, or (b) open a dedicated debug session to chase
the remaining throughput gap (workers-in-decode-not-fetch vs regional-node-capacity) before
re-attempting. Either way, the FIRST concrete action is a full uninstall of HUMANKIND (install dir
+ `appmanifest_1124300.acf`, Steam quit) per Task 1 step 1 — the current on-disk state is stale.
</next_action>
