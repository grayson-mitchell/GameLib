---
status: complete
phase: 23.2-steam-depot-selection-required-vs-optional-depots-and-skip-a
artifact: uat
source: [23.2-VERIFICATION.md]
started: 2026-08-19
updated: 2026-08-19
total_items: 3
pending_items: 0
passed_items: 3
failed_items: 0
blocked_items: 0
open_gaps: [G-23.2-01]  # NOT one of the 3 declared items -- surfaced during item 1's live run
requirements: [REQ-23.2-01, REQ-23.2-02, REQ-23.2-03, REQ-23.2-04, REQ-23.2-05, REQ-23.2-06, REQ-23.2-07, REQ-23.2-08]
run_via: "/gsd-verify-work 23.2"
notes: "ALL 3 ITEMS PASS on real hardware 2026-08-19 (LATEST). The phase's acceptance benchmark -- unobserved through execution and verification by design -- is now OBSERVED: a live GameLib install of appId 1771300 at 17:16:39-17:17:54 NZST hit the real eresult=40 refusal on depot 1771304, skipped it, and COMPLETED, writing a manifest matching the official Windows client on every benchmark field. The user was shown the skip notice for real, alongside two other toasts -- which additionally proves in production that notifiedDepotSkipped and notifiedWaiting are genuinely separate flags (the D-06/D-07 trap), something jest could not establish. Parent gap G-23-01 is now RESOLVED in 23-UAT.md with a proven_by line naming real hardware, and Phase 23's deferred-items.md entry is CLOSED. Two honesty limits stand: the run was a RESUME over pre-existing content (the defect under test fires in buildDepotPlan before any bytes transfer, so it is fully exercised; the bulk download path is not re-proven), and GameLib's manifest is not byte-identical to the official client's outside the benchmark fields (notably it omits SharedDepots). ONE NEW DEFECT surfaced, outside the three declared items and unrelated to the depot-skip work: G-23.2-01 -- the just-installed game VANISHED from the GameLib library UI while its backend record stayed healthy. This phase's own truths are unaffected. PRIOR: Phase 23.2 executed 4/4 plans with a CLEAN code review (0 blockers / 0 warnings) and a green full suite (295 suites / 6169 tests), and verification scored 8/9 truths VERIFIED at jest level with three RED proofs independently re-run by the orchestrator. It is NOT `passed`, and the missing 1/9 is the phase's whole premise: 23.2-CONTEXT.md's externally-authored acceptance benchmark — that a GameLib install of appId 1771300 should now produce InstalledDepots {1771302, 1771303, 1771306} at SizeOnDisk 96422090071, byte-matching the official Windows client — was never observed against a live install. That was DELIBERATE (re-running the diagnostic costs 90 GB and regenerates identical data), not an omission. Parent gap G-23-01 in 23-UAT.md is deliberately still `open` for the same reason; item 3 below is the only thing that may close it."
---

## Current Test

[testing complete — all 3 items passed; 1 new unrelated gap filed, see Gaps]

## Tests

### 1. Live KCD2 install completes with the blocked depot skipped
expected: Trigger a live GameLib install of KCD2 (Steam appId 1771300) on the same macOS/CrossOver account used for the 2026-08-19 official-client diagnostic, letting Steam refuse depot 1771304's key as it did before. The install **completes rather than aborting**, and `appmanifest_1771300.acf` shows `StateFlags "4"`, `InstalledDepots = {1771302, 1771303, 1771306}` with **1771304 absent**, `SizeOnDisk 96422090071`, and a non-zero `buildid` — matching the official Windows client's own manifest exactly.
why_human: This is the phase's own acceptance benchmark, and it is authored by Valve's client rather than by us — which is what makes it worth testing. No live install ran during the phase by design. Every claim above is proven only against jest-level simulation (`depot.finalize.test.ts`'s KCD2-shaped end-to-end test mocks `fetchChunk` and `getDepotDecryptionKey`). A jest mock returning `eresult=40` on cue is not proof that Steam's real CM server behaves identically on a fresh connection.
result: pass
run_on: 2026-08-19 17:16:39–17:17:54 NZST, real macOS hardware, GameLibSteam CrossOver bottle, real Steam CM/CDN
method: |
  KCD2's 90G of content was already on disk from the official Windows client's own
  install. Rather than delete and re-download it (forbidden by 23.2-CONTEXT.md), the
  official `appmanifest_1771300.acf` was moved out of the bottle's `steamapps/` — three
  md5-verified copies retained (`6ab52670efc1bf024cf30c4b6c5fccab`). GameLib then read no
  ACF, showed KCD2 as not-installed, and a real install was triggered from the UI.
  Confirmed beforehand that `reconcile.ts:126-157` skips any file whose sha1 matches and
  that neither `depot.ts` nor `reconcile.ts` contains any unlink/rm/rimraf, so existing
  content was never at risk. Bottle `steam.exe` was not running.
proven_by: |
  REAL-HARDWARE EVIDENCE (~/Library/Logs/GameLib/gamelib.log, 2026-08-19):
  - 17:16:44 `fetchDepotPlanEntry: couldn't get decryption key for depot 1771304
    (app 1771300): eresult=40` — real Steam CM refusal, not a jest mock.
  - 17:16:44 `buildDepotPlan: skipping depot 1771304 (app 1771300) for appId 1771300 —
    Steam refused the key/manifest with eresult=40 (Blocked); the install is continuing
    without it` — the skip-and-warn path firing in production for the first time.
  - 17:16:45 `buildDepotPlan: total 5953ms for appId 1771300 (3 depot(s))` and
    `steam-flags-census stage=plan-build appId=1771300 depots=3 ... skipped=1771304`.
  - 17:17:51 `steam-flags-census stage=download-complete ... jobCount=0
    reconciledSkipped=138` — all 138 files verified off disk, zero downloaded.
  - 17:17:51 `Writing StateFlags=4 full-ownership manifest for appId 1771300
    (sizeOnDisk=96422090071, buildid=23914554)`.
  - 17:17:54 `install polling complete for appId 1771300 — badge flipped to installed`.
  Whole run: 71.5s from click. The install COMPLETED rather than aborting — the exact
  inverse of the 2026-07-21 attempt that failed the whole install on this depot.

  RESULTING MANIFEST vs THE OFFICIAL WINDOWS CLIENT'S (benchmark fields, all exact):
  | Field | Official | GameLib | |
  | StateFlags | 4 | 4 | match |
  | SizeOnDisk | 96422090071 | 96422090071 | match |
  | buildid | 23914554 | 23914554 | match |
  | InstalledDepots | 1771302/1771303/1771306 | identical block, same manifest GIDs and sizes | match |
  | 1771304 | absent | absent | match |
  `SizeOnDisk` is MEASURED from disk (`measureInstalledBytes`), not summed from the plan
  (`depot.ts:797-798`), so matching 96422090071 is an independent check rather than
  arithmetic echoing the benchmark back.
honesty_limits: |
  1. This was a RESUME over pre-existing content, not a fresh 90G download. It fully
     exercises the defect under test — the 1771304 key refusal happens in `buildDepotPlan`
     BEFORE any bytes transfer — plus the completeness gate and the manifest write. It does
     NOT re-prove the bulk download path, which 23.2-CONTEXT.md forbids re-running.
  2. The written manifest is NOT byte-identical to the official client's overall: GameLib
     omits `SharedDepots { "228989" "228980" }`, `LauncherPath`, `StagingSize`,
     `DownloadType`, `UpdateResult`, `BytesToStage`/`BytesStaged`, `TargetBuildID`,
     `AllowOtherDownloadsWhileRunning`, `ScheduledAutoUpdate`, and writes `MountedDepots`
     where the official client writes `MountedConfig`. Every field this test names matches
     exactly; the divergence is outside the stated benchmark and is consistent with
     23.2-MANIFEST-WRITE-TAXONOMY.md. The `SharedDepots` omission (228980 = Steamworks
     Common Redistributables) is logged as an observation, not scored here — see Gaps.

### 2. The user actually sees the skip notice, rendered for real
expected: On that same install, **two distinct OS-level notifications** appear: the ordinary "Installation Finished" toast **and** the new depot-skip toast naming depot 1771304, reading approximately "Installed without depot 1771304. Steam wouldn't release its key for this account, so that content was skipped — the game should still run."
why_human: `library.test.ts` asserts on the **options object** passed to a mocked `i18next.t` — necessarily so, because the jest i18next mock discards interpolation options (documented against REQ-23.2-06). That proves the depot id reaches the translation layer; it does **not** prove the real catalog string renders, nor that the OS notification surfaces. This project has been bitten before by a UAT pass covering a surface that could not render at all. Confirm both toasts co-occur — a native-handoff install that skipped a depot is exactly the case where the old shared-flag bug would have suppressed one of them.
result: pass
run_on: 2026-08-19, same install as item 1, macOS Notification Center
proven_by: |
  THREE distinct notifications observed, not two — all rendered with real i18next
  interpolation, no raw keys and no un-interpolated {{...}} placeholders:
   1. the depot-skip notice — Steam not releasing the key, game will still run
      (`steam.download.notify.depotSkipped`, library.ts:2112-2119,
      gated on `notifiedDepotSkipped`)
   2. "Restart Steam to finish installing {{game}}"
      (`steam.waitingForSteam.notify`, library.ts:2139-2148,
      gated on `notifiedWaiting`)
   3. "Installation Finished"
      (`notify.install.finished`, library.ts:2090-2093, ungated)
  This EXCEEDS the item's bar and closes the D-06/D-07 concern empirically rather than
  by construction. The phase's stated trap was that routing the skip notice through the
  pre-existing `notifiedWaiting` flag would make notices 1 and 2 mutually exclusive —
  whichever `notify()` ran first would permanently suppress the other, and a
  native-handoff install with a skipped depot is exactly that scenario. All three
  coexisting on real hardware proves the flags are genuinely separate in production.
  jest could not reach this: `library.test.ts` inspects the options object handed to a
  mocked `t()` and never renders a catalog string or surfaces an OS notification.
supersedes_note: |
  Amends this item's own premise: it asked for "two distinct" notifications. Three is
  correct, not an over-fire. Notice 2 is intentional (commit 54eae3c41, "signal pending
  Steam adoption on the full-ownership fast path") and accurate — a GameLib-written ACF
  is adopted only at Steam's next startup scan, so the game is genuinely not yet visible
  to Steam at this moment.

### 3. Only then, close G-23-01
expected: Once items 1 and 2 pass, update `23-UAT.md`'s `G-23-01` entry (currently `status: open`, `open_gaps: [G-23-01]`) and Phase 23's `deferred-items.md` to reflect closure, with a dated note pointing at the hardware evidence from item 1.
why_human: `23-UAT.md` records G-23-01 as staying open **"BY DECISION, not by oversight"**, pending exactly this kind of real-world confirmation. Phase 23.2 added `fix_shipped_23_2` and `why_still_open` to that record rather than flipping its status, because the sibling gap G-23-02 sets this document's bar for `resolved`: a `proven_by:` line naming real hardware. Do not flip the status without one.
result: pass
done_on: 2026-08-19, after items 1 and 2 both passed
changes_made: |
  `.planning/phases/23-.../23-UAT.md`:
   - frontmatter `open_gaps: [G-23-01]` -> `open_gaps: []`
   - frontmatter `notes:` amended — closure stated first, the superseded
     "G-23-01 stays OPEN by decision" clause retained and marked as such
   - `G-23-01` `status: open` -> `status: resolved`, with a `proven_by:` line
     naming real macOS hardware, timestamps, log lines and the manifest
     comparison — meeting the bar sibling gap G-23-02 set for this document
   - added `resolution_method_note:` stating plainly that this was a RESUME over
     pre-existing content, not a fresh 90GB download
   - `why_still_open:` renamed `why_was_still_open_DISCHARGED_2026_08_19:` and
     retained, so the bar this gap was held to stays legible rather than vanishing
   - `missing:` -> `[]`, with both entries moved to `missing_DISCHARGED` and
     annotated with what actually shipped
  `.planning/phases/23-.../deferred-items.md`:
   - section retitled to CLOSED, with a closure note at the top and the
     historical record left intact below it
   - the inline `status: open` at the end struck through and updated
amendment_recorded: |
  The old record's branch-2 prescription said the fix would "introduce a
  required-vs-optional depot distinction at selection time in depot/select.ts".
  It did not — select.ts is byte-identical to the planning commit (REQ-23.2-02)
  and the plan is reduced downstream in buildDepotPlan's loop. The OUTCOME branch 2
  demanded was delivered; the MECHANISM it assumed was not the one adopted. Noted
  in deferred-items.md rather than quietly closed over.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0
new_gaps_found_outside_declared_items: 1  # G-23.2-01, see below

## Gaps

- id: G-23.2-01
  truth: "A Steam game that GameLib has just installed remains visible in the library UI."
  status: failed
  reason: "User reported: immediately after the KCD2 install completed, KCD2 was MISSING from the GameLib library — search does not find it. Discovered during test 1, outside the three declared items."
  severity: major
  test: 1
  scope_note: "Does NOT affect test 1's verdict — test 1 asserts on the written .acf and the install-completion path, both of which passed on real hardware before this surfaced."
  triage_so_far: |
    Backend state is CORRECT and is not the cause. store_cache/steam_library.json holds a
    complete, healthy KCD2 entry: is_installed true, is_windows_native true,
    steamPlatformsCaptured true, is_delisted false, and a full install object
    (install_path in the GameLibSteam bottle, install_size '89.8 GiB', platform
    'Windows') — structurally identical to appId 2457220 (Avowed), another bottle/Windows
    install that renders normally. The backend also logged "badge flipped to installed"
    (17:17:54) and two clean full syncs afterwards ("Steam library sync complete: 378
    games" at 17:17:55 and 17:18:05). So the game is present in the store with good data
    and is not rendering.
  narrowed_2026_08_19: |
    RESTART DIAGNOSTIC RUN — KCD2 REAPPEARED. Same backend data, same filters, renders
    fine after an app restart. So the fault is RENDERER-SIDE STATE, not filter logic and
    not backend data. Frontend filtering is ruled out.
  architecture_finding: |
    The Steam library is unusual and this matters for the fix. `refreshLibrary`'s state
    merge in GlobalState.tsx (the `setState` at ~L1039) has branches for epic/gog/zoom/
    amazon/sideload but NO steam branch at all — grep for `includesSteam`, `next.steam`,
    `loadSteamLibrary` or `steamLibraryStore` in that file returns NOTHING. The frontend's
    steam library is populated EXCLUSIVELY by the `handleGamePush` subscription
    (GlobalState.tsx:1494-1511), one game per message, with the backend re-pushing every
    cached game on a refresh (`library.ts:752` — `cached.forEach((g) =>
    sendFrontendMessage('pushGameToLibrary', g))`).

    The steam branch of that handler uses the correct functional `setState(prevState =>
    ...)` form and only ever REPLACES an entry by index or APPENDS a new one — it has no
    removal path. Therefore the entry cannot have been deleted by a push. A vanish means
    `state.steam.library` was RESET (or replaced by a shorter list) and then not
    re-populated, or the pushed object was replaced by one the list refuses to render.
  census_trap_noted: |
    Grepping `src/frontend` for the CHANNEL name `pushGameToLibrary` returns ZERO hits —
    the frontend subscribes via the preload API-METHOD name `handleGamePush`
    (`src/preload/api/library.ts:14`). Anyone censusing this seam by channel name will
    wrongly conclude the frontend has no subscriber. Cf. the standing lesson that a
    census keyed on the wrong namespace cannot find frontend callers.
  still_unverified:
    - "WHICH write resets state.steam.library. Not identified — needs a debug session, not more static reading."
    - "`Steam bulk platform capture: scoped=1 captured=0 skipped=1` fired at both 17:17:55 and 17:18:05, capturing NOTHING for the one scoped game. Related to the 34.14/34.15 platform-signal work. NOT established as connected to the vanish — recorded only so it is not lost."
  recommended_next: "/gsd-debug — reproduce by installing any Steam title and watching state.steam.library length across the install; the reset write is the target."
  artifacts:
    - "src/frontend/state/GlobalState.tsx:1494-1511 (handleGamePush steam branch — replace-or-append, no removal)"
    - "src/frontend/state/GlobalState.tsx:~1039 (refreshLibrary merge — no steam branch)"
    - "src/backend/storeManagers/steam/library.ts:752 (backend re-pushes all cached games on refresh)"
    - "src/preload/api/library.ts:14 (handleGamePush = frontendListenerSlot('pushGameToLibrary'))"
  missing: []
