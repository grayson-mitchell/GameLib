---
phase: 23.2-steam-depot-selection-required-vs-optional-depots-and-skip-a
verified: 2026-08-19T04:32:32Z
status: passed
amended: 2026-08-19T05:30:00Z  # human_needed -> passed; all 3 human_verification items DISCHARGED on real hardware
score: 9/9 must-haves verified — 8 at jest level with independently re-proven RED cases, and the 9th (the real-hardware acceptance benchmark) OBSERVED 2026-08-19 via /gsd-verify-work 23.2
overrides_applied: 0
human_verification_status: ALL 3 DISCHARGED 2026-08-19 — see 23.2-HUMAN-UAT.md (status complete, 3/3 pass). Items retained verbatim below as the record of what was owed and why.
human_verification_outcome: |
  1. DISCHARGED — PASS. Live install of appId 1771300, 2026-08-19 17:16:39-17:17:54 NZST,
     GameLibSteam CrossOver bottle, real Steam CM/CDN. Real `eresult=40` refusal on depot
     1771304 logged at 17:16:44, followed by `buildDepotPlan: skipping depot 1771304 ...
     the install is continuing without it`; plan reduced to 3 depots; 17:17:51 `Writing
     StateFlags=4 full-ownership manifest for appId 1771300 (sizeOnDisk=96422090071,
     buildid=23914554)`. Resulting .acf matches the official Windows client on every
     benchmark field, 1771304 absent. HONESTY LIMIT: a RESUME over pre-existing content
     (jobCount=0, reconciledSkipped=138), not a fresh 90GB download — the defect under
     test fires in buildDepotPlan BEFORE any bytes transfer, so it is fully exercised,
     but the bulk download path is not re-proven. 23.2-CONTEXT.md forbids re-running it.
  2. DISCHARGED — PASS, and EXCEEDED. THREE distinct OS notifications coexisted, all with
     real i18next interpolation: the depot-skip notice, "Restart Steam to finish
     installing", and "Installation Finished". This proves in production what jest could
     not: `notifiedDepotSkipped` (library.ts:2110) and `notifiedWaiting` (library.ts:2139)
     are genuinely separate flags, so the D-06/D-07 shared-guard trap — where whichever
     notify() ran first would permanently suppress the other — is absent.
  3. DISCHARGED — DONE. 23-UAT.md's G-23-01 flipped open -> resolved with a `proven_by:`
     line naming real hardware (the bar sibling gap G-23-02 set), `open_gaps` -> [], and
     Phase 23's deferred-items.md section retitled CLOSED. Prior reasoning retained rather
     than overwritten.
note_on_new_defect: |
  One defect surfaced during item 1's live run that is NOT covered by any truth in this
  report and does NOT affect any of them: the just-installed game vanished from the
  GameLib library UI while its backend record stayed healthy. Filed as G-23.2-01 (major)
  on 23.2-HUMAN-UAT.md. It concerns library rendering, not depot selection, skip-and-warn,
  the completeness gate, or the manifest write.
human_verification:
  - test: "Trigger a live GameLib install of KCD2 (Steam appId 1771300) on the same macOS/CrossOver account used for the 2026-08-19 official-client diagnostic, letting Steam refuse depot 1771304's key as it did before."
    expected: "Install completes (not aborted). appmanifest_1771300.acf shows StateFlags \"4\", InstalledDepots = {1771302, 1771303, 1771306} (1771304 absent), SizeOnDisk 96422090071, buildid non-zero — matching the official Windows client's own manifest exactly."
    why_human: "This is the phase's own externally-authored acceptance benchmark (23.2-CONTEXT.md 'Specifics' section). No live install was run during this phase by design (would cost 90GB and regenerate identical data per CONTEXT.md) — every claim above is proven only against a jest-level simulation (depot.finalize.test.ts's KCD2-shaped end-to-end test with mocked fetchChunk/getDepotDecryptionKey), not real Steam CM/CDN traffic. A jest mock returning eresult=40 on cue is not proof Steam's real CM server behaves identically on a fresh connection."
  - test: "Confirm the user actually sees two separate notifications on that same install: the ordinary 'Installation Finished' toast AND the new depot-skip toast naming depot 1771304, both rendered with real (non-mocked) i18next interpolation."
    expected: "Two distinct OS-level notifications appear, the second reading something like 'Installed without depot 1771304. Steam wouldn't release its key for this account, so that content was skipped — the game should still run.'"
    why_human: "library.test.ts asserts on the *options object* passed to a mocked i18next.t (documented explicitly in REQUIREMENTS.md REQ-23.2-06 as necessary because the jest i18next mock discards interpolation options) — it does not render the real catalog string or a real OS notification. Visual/notification-surface confirmation needs a human."
  - test: "After 23.2 is accepted, update 23-UAT.md's G-23-01 entry (currently open_gaps: [G-23-01]) and deferred-items.md to reflect closure, once the live-hardware check above passes."
    expected: "G-23-01 moves from open to closed with a dated note pointing at this phase and the hardware evidence above."
    why_human: "23-UAT.md explicitly records G-23-01 as staying open 'BY DECISION, not by oversight' pending exactly this kind of real-world confirmation (memory: phase-23.2 record). This verification must not silently flip that record — it is the phase's own honesty limit, and the CONTEXT.md instructions given to this verifier explicitly warn not to contradict it without evidence."
---

# Phase 23.2: Steam depot selection — required-vs-optional depots and skip-and-warn on a Blocked key Verification Report

**Phase Goal:** Close G-23-01. Make a non-essential owned depot whose decryption key Steam refuses a skip-and-warn (install continues, user is told which depot was skipped) instead of a whole-install abort, and reconcile GameLib's selected depot set against the official client's.

**Verified:** 2026-08-19T04:32:32Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped 1:1 to REQ-23.2-01..08)

| # | Truth (REQ) | Status | Evidence |
|---|---|---|---|
| 1 | Only literal `EResult 40` triggers skip-and-warn; every other member of `NON_RETRYABLE_ERESULTS` (8,9,15,17,42,43) still aborts (REQ-23.2-01) | VERIFIED | `depot.ts:776` — `if (eresult === 40) { ... skippedDepots.push(...); continue }` then unconditional `throw err` below for any other code (`depot.ts:791`). `NON_RETRYABLE_ERESULTS` unchanged: `depotErrors.ts:52` `new Set([8,9,15,17,40,42,43])`, confirmed byte-identical vs the pre-phase planning commit `4e5cb60fc` by `git diff`. Orchestrator independently re-proved the RED case by hand: widening `eresult === 40` to `typeof eresult === 'number'` failed all 6 negative parameterized cases. Jest re-run by this verifier: `depot.test.ts` — 154+ cases pass including the `it.each([8,9,15,17,42,43])` RED-proof block. |
| 2 | No essentiality classification at selection time — `select.ts` unchanged (REQ-23.2-02) | VERIFIED | `git diff 4e5cb60fc -- src/backend/storeManagers/steam/depot/select.ts` returns empty (zero hunks). Reduction happens downstream inside `buildDepotPlan`'s loop (`depot.ts:759-800`), not in `selectDepots`/`selectAllDepots`. Pinned by a source census in `depot.test.ts` (`describe('D-02/D-04/D-05 source censuses...')`, line 1535). |
| 3 | The reduced plan feeds the completeness gate — `allFilesVerified`/`allModesApplied`/byte totals computed over the post-skip plan (REQ-23.2-03) | VERIFIED | `depot.ts:794-799`: `totalBytes += entry.files...` sits after the `continue` for a skipped depot, so it excludes skipped bytes for free. `downloadSteamDepots`'s `attempted` array (`depot.ts:2872-2878`) is built only from `plan.depots`, which never contains a skipped entry. `finalizeToSteam`'s `canWrite4` computation (`depot.ts:2594-2600`) has no skip-awareness at all — it only ever sees the already-reduced `attempted`/`buildid`/`lastResult`. End-to-end jest case in `depot.finalize.test.ts` (KCD2 shape, `depotId` 1771302/1771303/1771306 kept, 1771304 skipped) asserts `StateFlags "4"` with 1771304 absent as a block key — re-run green by this verifier. |
| 4 | `canWriteFullOwnership` unchanged, no bypass parameter (REQ-23.2-04) | VERIFIED | `depot.ts:1065-1080` — identical signature/body to Phase 23's form (5 fields: outcome, failures, buildid, allFilesVerified, allModesApplied), no new parameter. Orchestrator confirmed byte-identical against the planning commit with a control proving the diff method can detect a real change. A sibling test (skip + a SHA1-mismatch failure) still falls back to `StateFlags "1026"` — proving the skip is not an escape hatch. |
| 5 | No recovery path for a skipped depot; an all-skipped install fails as an error, never reports success (REQ-23.2-05) | VERIFIED | All-skipped guard: `depot.ts:811-817` — `if (!depots.length && skippedDepots.length) throw ...`. Pinned test: `depot.test.ts:1601` (`'D-05: depot.ts (after stripSourceComments) has no retry/repair/re-check path keyed on skippedDepots'`) with an adjacent RED-proof at `depot.test.ts:1617` asserting the census fires against a known-bad specimen that keys a retry off `skippedDepots` — confirms the check is non-vacuous, not just currently true. |
| 6 | A completion notice names the skipped depot, additive to the existing "Installation Finished" toast, gated so it fires at most once and never on an OFF-path poll (REQ-23.2-06) | VERIFIED (jest) | `library.ts:2090-2119` — the pre-existing `notify({ ...finished })` call is followed immediately by a *separate* `if (poll?.skippedDepots?.length && !poll.notifiedDepotSkipped) { poll.notifiedDepotSkipped = true; notify(...) }` block, using its own dedicated flag (never `notifiedWaiting`). Threading confirmed: `games.ts:1584/1589` passes `outcome.skippedDepots ?? []` into `startInstallPolling`'s two native-handoff call sites. Orchestrator independently re-proved the RED case for the shared-guard trap: swapping the skip branch onto `notifiedWaiting` failed exactly the three-notice-coexistence test while six sibling 23.2-04 tests stayed green. **This truth is proven only at the call-site/unit level** — see human_verification item 2 for the real-notification gap. |
| 7 | New i18n key carries the skip copy; `depotBlocked` stays byte-identical and reachable as the residual all-blocked path (REQ-23.2-07) | VERIFIED | `translation.json:1227` — `steam.download.notify.depotSkipped` present with `{{depots}}` interpolation (never `{{count}}`). `depotBlocked` (`translation.json:1219`) unchanged text; `git diff 4e5cb60fc -- depotErrors.ts` shows a comment-only addition (documenting the new residual-path disposition) with the string literal itself untouched. Pinned by `steamDepotSkipCopyCensus.test.ts` (re-run green by this verifier, part of a 5-suite/635-test run). |
| 8 | A native install that downloads zero bytes never clobbers an existing manifest; a install that genuinely attempted a download and then failed/cancelled still writes the Phase 21 `1026` verify-handoff (REQ-23.2-08 / D-08) | VERIFIED | `shouldFinalizeAfterThrow` (`depot.ts:1100-1104`) gates the catch-path `finalize()` call at `depot.ts:2973` on `downloadAttempted === true`; `downloadAttempted` is set at `depot.ts:2929`, immediately before the `downloadDepotFiles` await. Orchestrator independently re-proved this RED by hand (forcing the predicate to always return `true` broke both bottle-path exact-manifest-text-equality cases). This verifier confirmed the captured RED output in `23.2-02-SUMMARY.md` shows the documented stub shape (`StateFlags 1026`, `buildid 0`, empty `InstalledDepots`) against the unmodified source, and the regression test derives its target directory from `bottle.ts`'s own real `getBottleDir`/`getBottleSteamappsDir` helpers (`depot.finalize.test.ts` imports and structural assertion confirmed), not a bare tmpdir. |
| 9 | GameLib's selected KCD2 depot set minus the skipped depot equals the official client's installed set exactly, at StateFlags=4/SizeOnDisk 96422090071 (the phase's acceptance benchmark) | **VERIFIED — REAL HARDWARE 2026-08-19** (amended; was UNCERTAIN — jest-only) | **AMENDMENT 2026-08-19 (/gsd-verify-work 23.2):** the live run this truth was waiting on has now happened. GameLib installed appId 1771300 against real Steam CM/CDN at 17:16:39–17:17:54 NZST in the GameLibSteam bottle. Steam refused depot 1771304's key with a real `eresult=40`; `buildDepotPlan` skipped it and continued (`total 5953ms for appId 1771300 (3 depot(s))`); the install COMPLETED and wrote `StateFlags=4 ... sizeOnDisk=96422090071, buildid=23914554`. The resulting `appmanifest_1771300.acf` matches the official Windows client's on every benchmark field, with an `InstalledDepots` block byte-identical for 1771302/1771303/1771306 (same manifest GIDs and sizes) and 1771304 absent. `SizeOnDisk` is measured from disk (`measureInstalledBytes`), not summed from the plan, so the match is an independent check rather than arithmetic echoing the benchmark. Honesty limit: this was a resume over pre-existing content (`jobCount=0 reconciledSkipped=138`), so the bulk download path is not re-proven — but the refusal-and-skip happens in `buildDepotPlan` before any bytes transfer, so this truth's own claim is fully exercised. ORIGINAL (superseded) EVIDENCE: | `depot.finalize.test.ts:540-758` runs a real (unmocked internal logic) `downloadSteamDepots` against a KCD2-shaped 4-depot plan with a mocked Steam CM client (`getDepotDecryptionKey`/`fetchChunk` stubs), and the resulting manifest matches the benchmark exactly in that simulation. **No live install against Steam's real CM/CDN network was run in this phase** — 23.2-CONTEXT.md states this explicitly ("the diagnostic is DONE — do not re-run it... a live GameLib install of 1771300 should produce that same depot set"), and 23-UAT.md's G-23-01 remains `open` for exactly this reason. Routed to human_verification item 1. |

**Score (AMENDED 2026-08-19):** **9/9 truths VERIFIED.** Eight at jest/unit level with independently re-proven RED cases, and the ninth — the phase's own real-world acceptance benchmark — **observed on real hardware** during `/gsd-verify-work 23.2`. Truth #6's notification-rendering half, previously "VERIFIED (jest)" with a routed human item, is also now confirmed live: three distinct OS notifications coexisted, proving the separate-flag design holds in production.

*Superseded score: 8/9 VERIFIED at jest/unit level, 1/9 UNCERTAIN pending a live-hardware run.*

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/backend/storeManagers/steam/depot.ts` | `shouldFinalizeAfterThrow` + `DepotPlan.skippedDepots` + skip-and-continue loop + all-skipped guard | VERIFIED | All four present, line-anchored above. |
| `src/backend/storeManagers/steam/__tests__/depot.finalize.test.ts` | Bottle-path D-08 clobber regression + KCD2 end-to-end skip case | VERIFIED | `getBottleSteamappsDir` used (5 matches confirmed), KCD2 constants present. |
| `public/locales/en/translation.json` | New `steam.download.notify.depotSkipped` key, unchanged `depotBlocked` | VERIFIED | Confirmed by direct read. |
| `src/backend/storeManagers/steam/library.ts` | Skip completion notice at the confirmed-installed poller branch | VERIFIED | `notifiedDepotSkipped` flag, additive `notify()` call confirmed at `library.ts:2110-2120`. |
| `.planning/phases/23.2.../23.2-MANIFEST-WRITE-TAXONOMY.md` | Source-anchored A/B/C manifest-write taxonomy | VERIFIED | Read in full; every case cites function name + line range; git-log adjudication (`eacbc7ccf`, 2026-07-15) present and correctly dated relative to the 2026-07-21 UAT run it corrects. |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `buildDepotPlan` depot loop | `DepotPlan.skippedDepots` | `catch` on `withPlanBuildRetry(fetchDepotPlanEntry)`, `eresult === 40` | WIRED (`depot.ts:762-791`) |
| `downloadSteamDepots` | `DepotDownloadOutcome.skippedDepots` | `plan.skippedDepots` captured at `depot.ts:2870`, threaded through every return path | WIRED |
| `depot.ts` catch block | `finalizeToSteam` | `shouldFinalizeAfterThrow({ downloadAttempted })` gate at `depot.ts:2973` | WIRED |
| `games.ts` `runNativeDepotDownload` | `library.ts` `startInstallPolling` | `skippedDepots: outcome.skippedDepots ?? []` at `games.ts:1584/1589` | WIRED |
| `library.ts` `activePolls` confirmed-installed branch | `notify()` | `poll.skippedDepots.length && !poll.notifiedDepotSkipped` at `library.ts:2110-2119` | WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Targeted phase-touched test suites pass | `pnpm jest depot.test.ts depot.finalize.test.ts steamDepotSkipCopyCensus.test.ts library.test.ts games.test.ts` | 5 suites, 635 tests, 0 failed | PASS |
| `select.ts`/`depotErrors.ts` string-level diffs since planning commit | `git diff 4e5cb60fc -- select.ts depotErrors.ts` | `select.ts` empty; `depotErrors.ts` comment-only | PASS |
| Debt markers in phase-touched files | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER" depot.ts depotErrors.ts games.ts library.ts translation.json` | no matches | PASS |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|---|---|---|---|
| REQ-23.2-01 | 23.2-03 | SATISFIED (jest) | Truth #1 |
| REQ-23.2-02 | 23.2-03 | SATISFIED | Truth #2 |
| REQ-23.2-03 | 23.2-03 | SATISFIED (jest) | Truth #3 |
| REQ-23.2-04 | 23.2-03 | SATISFIED | Truth #4 |
| REQ-23.2-05 | 23.2-03 | SATISFIED | Truth #5 |
| REQ-23.2-06 | 23.2-04 | SATISFIED (jest, call-site only) | Truth #6, human_verification item 2 |
| REQ-23.2-07 | 23.2-04 | SATISFIED | Truth #7 |
| REQ-23.2-08 | 23.2-02 | SATISFIED | Truth #8 |

No orphaned requirements — REQUIREMENTS.md lines 182-189 map 1:1 to the 8 IDs declared across the four plans' frontmatter.

**Note (informational, not a gap):** REQUIREMENTS.md's traceability table (lines 346-353) still shows all 8 IDs as "Pending" and each requirement's own checkbox (lines 182-189) is unchecked `- [ ]`, unlike Phase 23's requirements which were flipped to `- [x]`/"Complete" only after that phase's real-hardware UAT gates passed (REQ-23-07). That precedent, plus this phase's own unresolved human-verification item above, suggests the checkboxes are intentionally still open pending the live-hardware confirmation — flipping them now would be premature, not a bug in this verification.

### Anti-Patterns Found

None. No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers in any file this phase touched. No stub returns, no hardcoded-empty props on rendered data found in the diffed files.

### Human Verification Required — ✅ ALL THREE DISCHARGED 2026-08-19

Run via `/gsd-verify-work 23.2`; recorded in `23.2-HUMAN-UAT.md` (status `complete`, 3/3 `pass`).

1. ✅ **PASS** — Live KCD2 install against real Steam CM/CDN completed with depot 1771304 skipped on a real `eresult=40`; manifest matches the acceptance benchmark on every field.
2. ✅ **PASS (exceeded)** — Three distinct notifications appeared, not two, all with real interpolation — proving the separate-flag design holds live.
3. ✅ **DONE** — `23-UAT.md`'s `G-23-01` is now `resolved` with a `proven_by:` line naming real hardware; `open_gaps: []`; Phase 23's `deferred-items.md` entry retitled CLOSED with prior reasoning retained.

**New defect found during (1), outside this report's scope:** `G-23.2-01` (major) — the just-installed game vanished from the GameLib library UI while its backend record stayed healthy. Filed on `23.2-HUMAN-UAT.md`. Affects library rendering, not this phase's truths.

### Gaps Summary

No code-level gaps. Every must-have derived from REQ-23.2-01..08 has direct, line-anchored evidence in the current source tree, all touched test suites are green (independently re-run by this verifier: 5 suites / 635 tests), and the orchestrator's own hand-reproven RED cases for D-01, D-08, and the D-06/D-07 shared-guard trap corroborate the SUMMARYs rather than merely restating them.

The one open item is not a defect but an explicit, deliberate scope boundary carried from 23.2-CONTEXT.md: this phase's entire premise rests on a real-world diagnostic (the official Windows client's own `appmanifest_1771300.acf`) that was captured once and is not to be re-run, and the phase's own fix has — by design — never been run against a live Steam install. `23-UAT.md`'s `G-23-01` stays `open` for exactly this reason and this verification does not override that. Per this project's own process lessons ("a truth ticked by confirming a function is CALLED proves wiring, not behaviour" / "green jest is not a satisfied gate"), truth #9 and the notification-rendering half of truth #6 are marked UNCERTAIN rather than VERIFIED, and the phase status is `human_needed` rather than `passed`.

---

*Verified: 2026-08-19T04:32:32Z*
*Verifier: Claude (gsd-verifier)*
