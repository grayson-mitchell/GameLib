---
phase: 12-ownership-dedup
plan: 04
type: execute
wave: 4
depends_on: [12-03]
files_modified:
  - src/common/types/ipc.ts
  - src/backend/humble/ipc_handler.ts
  - src/preload/api/humble.ts
  - src/backend/main.ts
autonomous: true
requirements: [HDEDUP-01]
must_haves:
  truths:
    - "The renderer can fire a 'Not the same game' override for a fuzzy-matched key via a typed humble:* IPC channel"
    - "The backend rejects an override on a non-fuzzy (exact) match server-side, never trusting renderer-only gating (D-42)"
    - "Ownership recomputes whenever a Steam-inclusive library refresh completes (D-47), keeping steam/library.ts Humble-unaware"
  artifacts:
    - path: "src/common/types/ipc.ts"
      provides: "humbleSetOwnershipOverride / humbleClearOwnershipOverride channel types"
      contains: "humbleSetOwnershipOverride"
    - path: "src/backend/humble/ipc_handler.ts"
      provides: "override handlers delegating to HumbleLibrary with server-side fuzzy validation"
      contains: "humbleSetOwnershipOverride"
    - path: "src/preload/api/humble.ts"
      provides: "preload bridge for the two override channels"
      contains: "humbleSetOwnershipOverride"
    - path: "src/backend/main.ts"
      provides: "Steam-refresh recompute trigger in the refreshLibrary handler"
      contains: "recomputeOwnership"
  key_links:
    - from: "src/backend/main.ts refreshLibrary handler"
      to: "HumbleLibrary.recomputeOwnership()"
      via: "called after a steam|all|undefined refresh completes"
      pattern: "recomputeOwnership"
    - from: "src/backend/humble/ipc_handler.ts"
      to: "HumbleLibrary.setOwnershipOverride"
      via: "addHandler delegation"
      pattern: "setOwnershipOverride"
---

<objective>
Expose the override action to the renderer as typed `humble:*` IPC channels (with server-side fuzzy-only validation), and hook the second D-47 recompute trigger into `main.ts`'s generic `refreshLibrary` handler so ownership refreshes whenever the Steam library does — without `steam/library.ts` ever importing anything Humble.

Purpose: HDEDUP-01's user-correction path and its Steam-side recompute trigger. This is the composition-root seam that preserves the one-way Humble→Steam dependency direction.
Output: Two new IPC channel types + handlers + preload bridges, and a Steam-refresh recompute call in `main.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-ownership-dedup/12-RESEARCH.md
@.planning/phases/12-ownership-dedup/12-PATTERNS.md

<interfaces>
<!-- HumbleLibrary methods added in Plan 12-03:
     recomputeOwnership(): void|Promise<void>  (Steam-refresh entrypoint, self-gates on connectivity)
     setOwnershipOverride(machineName: string): Promise<void>
     clearOwnershipOverride(machineName: string): Promise<void> -->

<!-- ipc.ts existing humble block (~lines 270-277): humbleSync/humbleGetKeys/humbleGetSyncState in AsyncIPCFunctions.
     preload/api/humble.ts uses makeHandlerInvoker('channelName').
     ipc_handler.ts registerHumbleIpcHandlers() uses addHandler('channel', async (e, arg) => ...). -->

<!-- main.ts refreshLibrary handler (~lines 959-969):
     addHandler('refreshLibrary', async (e, library?) => {
       if (library !== undefined && library !== 'all') { await libraryManagerMap[library].refresh() }
       else { ...Promise.allSettled(all managers refresh) }
     })
     import { HumbleLibrary } from './humble/library' (or existing import path). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Override IPC channels + handlers + preload bridge</name>
  <files>src/common/types/ipc.ts, src/backend/humble/ipc_handler.ts, src/preload/api/humble.ts</files>
  <read_first>
    - src/common/types/ipc.ts (the humble block in AsyncIPCFunctions ~lines 270-277, to add the two new channel signatures in the same style)
    - src/backend/humble/ipc_handler.ts (the registerHumbleIpcHandlers body ~lines 26-40, especially the humbleSync/humbleGetKeys addHandler lines and the WR-09/T-11-02 store-isolation comments)
    - src/preload/api/humble.ts (the full file — the makeHandlerInvoker exports for humbleSync/humbleGetKeys to mirror)
    - src/backend/humble/library.ts (the set/clearOwnershipOverride method signatures + the matchConfidence field, so the handler can validate)
  </read_first>
  <action>
    In `ipc.ts` AsyncIPCFunctions, add `humbleSetOwnershipOverride: (machineName: string) => Promise<void>` and `humbleClearOwnershipOverride: (machineName: string) => Promise<void>`, with a comment noting these are the D-42 "Not the same game" override (fuzzy-matched rows only; validated server-side). In `ipc_handler.ts` `registerHumbleIpcHandlers()`, add `addHandler('humbleSetOwnershipOverride', async (e, machineName) => HumbleLibrary.setOwnershipOverride(machineName))` and the clear counterpart. Per RESEARCH.md Open Question 2 / the T-12-03 threat: the fuzzy-only enforcement must exist server-side — implement the guard inside `HumbleLibrary.setOwnershipOverride` (Plan 12-03) OR add a guard here that looks up the target key's `matchConfidence` and no-ops (with a `logWarning`) if it is not `'fuzzy'`; confirm which layer holds it and ensure exactly one authoritative server-side check exists (do not rely on the renderer disabling a button). In `preload/api/humble.ts`, export `humbleSetOwnershipOverride = makeHandlerInvoker('humbleSetOwnershipOverride')` and the clear counterpart, alongside the existing humbleSync/humbleGetKeys invokers.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20 ; grep -c "humbleSetOwnershipOverride" src/common/types/ipc.ts src/backend/humble/ipc_handler.ts src/preload/api/humble.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/common/types/ipc.ts` declares `humbleSetOwnershipOverride` and `humbleClearOwnershipOverride` in AsyncIPCFunctions returning `Promise<void>`
    - `src/backend/humble/ipc_handler.ts` registers both channels via `addHandler`, delegating to `HumbleLibrary`
    - A server-side check ensures an override only persists for a `matchConfidence === 'fuzzy'` key (grep shows a fuzzy validation in either the handler or the delegated method; renderer-only gating is NOT the sole guard)
    - `src/preload/api/humble.ts` exports both `makeHandlerInvoker` bridges
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The two override channels are typed, handled with server-side fuzzy validation, and bridged through preload; tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Steam-refresh recompute trigger in main.ts</name>
  <files>src/backend/main.ts</files>
  <read_first>
    - src/backend/main.ts (the refreshLibrary handler ~lines 959-969, and the existing HumbleLibrary import if present near the top; confirm steam/library.ts has no Humble import so this seam is the only coupling point)
    - src/backend/humble/library.ts (the exported recomputeOwnership() entrypoint from Plan 12-03 — confirm it self-gates on Steam connectivity so calling it unconditionally after a refresh is safe)
  </read_first>
  <action>
    In `main.ts`'s `refreshLibrary` handler, after the refresh work completes, call `HumbleLibrary.recomputeOwnership()` when the refresh was Steam-inclusive — i.e. when `library === 'steam' || library === undefined || library === 'all'` (undefined/all both include Steam). Import `HumbleLibrary` from the humble library module if not already imported. This keeps `steam/library.ts` completely Humble-unaware (the composition root owns the cross-domain wiring per the one-way dependency direction). Because `recomputeOwnership()` self-gates on Steam connectivity + non-empty games (Plan 12-03), calling it here is safe even if the refresh failed or produced no games — it is a no-op in that case. Do not await in a way that blocks the handler's return beyond what the existing refresh already does; a fire-and-forget or awaited call is acceptable as long as any rejection is caught/logged (do not create an unhandled rejection).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20 ; grep -c "recomputeOwnership" src/backend/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/main.ts` calls `HumbleLibrary.recomputeOwnership()` inside/after the `refreshLibrary` handler, gated to `steam | all | undefined`
    - `src/backend/storeManagers/steam/library.ts` still has zero Humble imports (grep for `humble` in that file returns nothing new)
    - The call cannot produce an unhandled promise rejection (awaited-in-try or `.catch(logWarning)`)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>A Steam-inclusive library refresh triggers the Humble ownership recompute from the composition root; the one-way dependency direction is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → override IPC | A state-changing call the renderer must not be trusted to have gated correctly |
| main.ts → humble/steam | Cross-domain wiring lives only at the composition root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | humbleSetOwnershipOverride handler | mitigate | Backend validates `matchConfidence === 'fuzzy'` server-side before persisting an override — a compromised/buggy renderer calling override on an exact-match row is rejected + logged; never trust renderer-only enforcement of D-42 (consistent with humbleCheckHealth's server-side cooldown discipline) |
| T-12-07 | Denial of Service | recomputeOwnership from refreshLibrary | accept | The recompute is a bounded in-memory pass over already-cached data and self-gates on connectivity; a failed/empty refresh makes it a no-op — no unbounded work, no network |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `grep` confirms both override channels across ipc.ts/ipc_handler.ts/preload, the server-side fuzzy guard, and the main.ts recompute call
- `steam/library.ts` still has no Humble import
</verification>

<success_criteria>
- Override action is a typed, server-validated (fuzzy-only) IPC round-trip
- Steam-inclusive refresh triggers ownership recompute from main.ts
- One-way Humble→Steam dependency direction preserved
</success_criteria>

<output>
Create `.planning/phases/12-ownership-dedup/12-04-SUMMARY.md` when done
</output>
