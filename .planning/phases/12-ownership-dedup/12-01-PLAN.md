---
phase: 12-ownership-dedup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/types/humble.ts
  - src/backend/humble/constants.ts
  - src/backend/humble/classify.ts
  - src/backend/humble/__tests__/classify.test.ts
autonomous: true
requirements: [HDEDUP-01, HDEDUP-02]
must_haves:
  truths:
    - "HumbleKey rows carry a steamAppId (when the tpk is a Steam key), an ownedElsewhere flag, and a matchConfidence label"
    - "A Steam-platform key's Steam AppID is captured from the live order-detail tpk during classification"
    - "A version bump forces already-cached (including D-24 frozen/terminal) orders to re-fetch once so pre-Phase-12 REDEEMED keys backfill steamAppId"
  artifacts:
    - path: "src/common/types/humble.ts"
      provides: "HumbleKey extended with steamAppId?, ownedElsewhere, matchConfidence"
      contains: "matchConfidence"
    - path: "src/backend/humble/constants.ts"
      provides: "HUMBLE_CLASSIFIER_VERSION=3 + HUMBLE_FUZZY_MATCH_THRESHOLD=0.85"
      contains: "HUMBLE_FUZZY_MATCH_THRESHOLD"
    - path: "src/backend/humble/classify.ts"
      provides: "steamAppId capture in classifyOrder loop"
      contains: "steamAppId"
  key_links:
    - from: "src/backend/humble/classify.ts"
      to: "HumbleKey.steamAppId"
      via: "object literal pushed in classifyOrder"
      pattern: "steamAppId"
    - from: "src/backend/humble/constants.ts"
      to: "src/backend/humble/library.ts reclassifyAll"
      via: "HUMBLE_CLASSIFIER_VERSION mismatch triggers one-time full re-fetch"
      pattern: "HUMBLE_CLASSIFIER_VERSION"
---

<objective>
Establish the type + data-capture foundation for ownership dedup: extend `HumbleKey` with the three ownership-overlay fields, add the fuzzy-match threshold constant, bump the classifier version (the zero-new-code backfill mechanism), and capture `steam_app_id` from the live Humble tpk during classification.

Purpose: Every downstream plan (matching core, backend wiring, IPC, UI) reads these types and this captured field. The steamAppId capture is the linchpin of HDEDUP-02's collapse-onto-Steam-entry behavior — without the version bump, already-redeemed keys (D-24 frozen) never gain the field and the collapse silently never fires for existing users.
Output: Extended `HumbleKey` type, two new/updated constants, `classifyOrder` capturing `steamAppId`, extended classify tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ownership-dedup/12-CONTEXT.md
@.planning/phases/12-ownership-dedup/12-RESEARCH.md
@.planning/phases/12-ownership-dedup/12-PATTERNS.md

<interfaces>
<!-- Current HumbleKey (src/common/types/humble.ts ~lines 95-107) — extend IN PLACE, do NOT add ownedElsewhere to HumbleKeyState. -->
export interface HumbleKey {
  gamekey: string
  machineName: string
  state: HumbleKeyState        // 'UNPICKED'|'UNREVEALED'|'REVEALED'|'REDEEMED'|'UNREDEEMABLE'
  title: string
  platform: string             // derived from key_type (D-28): 'steam'|'gog'|'epic'|...
  expiration: string | null
  origin: string               // bundle/order label
}

<!-- classify.ts existing per-tpk push (~line 291-306); read via untyped Record<string,unknown> cast, inside the per-tpk try/catch. -->
<!-- validation.ts (~line 80-84) already proves steam_app_id is present in the live payload (presence-only boolean). -->
<!-- constants.ts current: HUMBLE_CLASSIFIER_VERSION = 2 (changelog-comment convention). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend HumbleKey type and add dedup constants</name>
  <files>src/common/types/humble.ts, src/backend/humble/constants.ts</files>
  <read_first>
    - src/common/types/humble.ts (the HumbleKey interface ~lines 95-107, the HumbleKeyState union ~lines 73-87, and the doc-comment discipline at lines 89-94 — mirror that comment style)
    - src/backend/humble/constants.ts (the HUMBLE_CLASSIFIER_VERSION constant + changelog comment ~lines 31-49, and the HUMBLE_COOLDOWN_MS/D-33 comment style ~lines 27-29)
  </read_first>
  <behavior>
    - Test 1 (type-level, verified via tsc): HumbleKey accepts `steamAppId?: string`, `ownedElsewhere: boolean`, `matchConfidence: 'exact' | 'fuzzy' | 'none'`
    - Test 2: HumbleKeyState union is UNCHANGED — ownedElsewhere is NOT a 6th state
    - Test 3: `HUMBLE_CLASSIFIER_VERSION === 3` and `HUMBLE_FUZZY_MATCH_THRESHOLD === 0.85`
  </behavior>
  <action>
    In `src/common/types/humble.ts`, extend the `HumbleKey` interface in place with three new members: `steamAppId?: string` (optional — absent for non-Steam keys and for pre-Phase-12 cached rows until backfill), `ownedElsewhere: boolean`, and `matchConfidence: 'exact' | 'fuzzy' | 'none'`. Add doc comments mirroring the existing lines-89-94 style: note that `steamAppId` is the Steam AppID carried directly by the order-detail API for `platform === 'steam'` tpks; note that `ownedElsewhere` is an overlay orthogonal to `state` per spec §2.3 that NEVER influences classification precedence and is NEVER read by classify.ts; note that `matchConfidence` is the D-41 provenance ('exact' AppID vs 'fuzzy' name, 'none' when not owned/uncomputed) so Phase 14's C2 guard can treat fuzzy matches gently. Do NOT add `ownedElsewhere` to the `HumbleKeyState` union. Add a one-line comment on `HumbleOrderCacheEntry.keys` noting the rows now carry ownership-overlay fields (no `allTerminal` semantic change).
    In `src/backend/humble/constants.ts`, change `HUMBLE_CLASSIFIER_VERSION` from `2` to `3` and append a changelog line to its comment: `//   3 = Phase 12: steam_app_id capture added to classifyOrder`. Add `export const HUMBLE_FUZZY_MATCH_THRESHOLD = 0.85` with a comment locking it to HDEDUP-01 success criterion 3 (85%+, not the community-norm 70%), mirroring the D-33 comment style.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20 ; grep -c "HUMBLE_FUZZY_MATCH_THRESHOLD" src/backend/humble/constants.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/common/types/humble.ts` contains `matchConfidence: 'exact' | 'fuzzy' | 'none'` and `ownedElsewhere: boolean` and `steamAppId?: string` inside the HumbleKey interface
    - `src/common/types/humble.ts` HumbleKeyState union still has exactly the 5 original states (grep shows no `ownedElsewhere` inside the union type)
    - `src/backend/humble/constants.ts` contains `HUMBLE_CLASSIFIER_VERSION = 3`
    - `src/backend/humble/constants.ts` contains `export const HUMBLE_FUZZY_MATCH_THRESHOLD = 0.85`
    - `npx tsc --noEmit` reports no new errors introduced by these changes
  </acceptance_criteria>
  <done>HumbleKey carries the three ownership fields, the classifier version is 3, and the fuzzy threshold constant is exported. tsc is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture steam_app_id during classification</name>
  <files>src/backend/humble/classify.ts, src/backend/humble/__tests__/classify.test.ts</files>
  <read_first>
    - src/backend/humble/classify.ts (the module docstring ~lines 220-228 "No I/O, no logging, no store import" — this discipline is a hard contract; and the classifyOrder per-tpk loop ~lines 229-340, especially the field reads at 272-285 and the object literal pushed at 291-306, and the per-tpk try/catch at 247-312)
    - src/backend/humble/validation.ts (~lines 80-84 — the existing steam_app_id presence check; confirms the raw field name and that it may be string OR number)
    - src/backend/humble/__tests__/classify.test.ts (~lines 1-13 — the no-mocking fixture-driven test style; and existing steam-key fixtures)
    - src/backend/humble/__tests__/fixtures/tpks.ts (existing tpk fixtures to reuse or extend for a steam-key-with-appid case)
  </read_first>
  <behavior>
    - Test 1: A `key_type === 'steam'` tpk carrying `steam_app_id: 620` (number) classifies into a HumbleKey with `steamAppId === '620'` (stringified to match GameInfo.app_name)
    - Test 2: A `key_type === 'steam'` tpk carrying `steam_app_id: '620'` (string) yields `steamAppId === '620'`
    - Test 3: A non-Steam tpk (e.g. `key_type === 'gog'`) yields `steamAppId === undefined`
    - Test 4: Every classified key defaults `ownedElsewhere: false` and `matchConfidence: 'none'` (dedup.ts fills these later)
    - Test 5: A malformed `steam_app_id` (e.g. an object) does not throw — the per-tpk try/catch keeps the order classifiable with `steamAppId === undefined`
  </behavior>
  <action>
    In `classifyOrder`'s existing per-tpk loop, read the raw AppID via the same untyped `Record<string, unknown>` cast already used for other tpk fields: capture `tpk.steam_app_id` into `rawAppId`. Compute `steamAppId` as `String(rawAppId)` ONLY when `platform === 'steam'` (the already-derived `key_type` label) AND `rawAppId` is a `string` or `number`; otherwise `undefined`. Add `steamAppId`, `ownedElsewhere: false`, and `matchConfidence: 'none'` to the object literal pushed into `keys` (~line 298-306). Keep this read INSIDE the existing per-tpk try/catch so a malformed shape skips gracefully (T-11-05 precedent). Do NOT compute ownership here — only capture the raw field; the module's "no I/O, no store" contract is preserved (this is a pure field-extraction addition, no new imports). Extend `classify.test.ts` with the five behavior cases above, reusing/extending the existing `fixtures/tpks.ts` fixtures (add a steam-key-with-appid fixture if none exists).
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/classify.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/humble/classify.ts` contains a read of `steam_app_id` and pushes `steamAppId` into the HumbleKey object literal
    - classify.ts adds NO new import (still no electron-store / no sendFrontendMessage — pure-function contract intact)
    - `npx jest src/backend/humble/__tests__/classify.test.ts` exits 0 with the five new cases passing
    - A non-Steam tpk classifies with `steamAppId === undefined`
    - A steam tpk with `steam_app_id: 620` classifies with `steamAppId === '620'`
  </acceptance_criteria>
  <done>classifyOrder captures steamAppId (stringified) for Steam keys, defaults the two overlay fields, tolerates malformed input, and the extended classify.test.ts is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Humble API → classify.ts | Untrusted tpk fields (incl. `steam_app_id`) cross into the app; already zod-passthrough-tolerant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01a | Input Validation | classify.ts steam_app_id read | mitigate | `steam_app_id` is only ever `String()`-coerced and stored — never interpolated into a shell/path/SQL/regex; read stays inside the existing per-tpk try/catch (malformed value → `undefined`, order still classifies) |
| T-12-01b | Tampering | HumbleKeyState union | accept | Overlay fields are additive; classification precedence is unchanged. Verified by the "union unchanged" test — no high-severity surface |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (type extension compiles across the codebase)
- `npx jest src/backend/humble/__tests__/classify.test.ts` green
- `grep` confirms the three new HumbleKey fields and the two new constants
</verification>

<success_criteria>
- HumbleKey carries `steamAppId?`, `ownedElsewhere`, `matchConfidence`; HumbleKeyState is unchanged
- `HUMBLE_CLASSIFIER_VERSION === 3`, `HUMBLE_FUZZY_MATCH_THRESHOLD === 0.85`
- classifyOrder captures a stringified steamAppId for Steam keys only, defaults overlay fields, tolerates malformed input
</success_criteria>

<output>
Create `.planning/phases/12-ownership-dedup/12-01-SUMMARY.md` when done
</output>
