---
phase: 12-ownership-dedup
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - src/backend/humble/electronStores.ts
  - src/backend/humble/user.ts
  - src/backend/humble/library.ts
  - src/backend/humble/__tests__/electronStores.test.ts
  - src/backend/humble/__tests__/library.test.ts
autonomous: true
requirements: [HDEDUP-01, HDEDUP-02]
must_haves:
  truths:
    - "A 'Not the same game' override persists keyed by machine_name and survives a disconnect/reconnect cycle (D-43)"
    - "Ownership recomputes at the end of every Humble sync and is re-pushed to the renderer"
    - "When Steam is disconnected or its library is empty/stale, existing ownership flags are kept at last-known values — never zeroed (D-48)"
    - "Pre-Phase-12 cached (D-24 frozen) orders re-fetch once and backfill steamAppId via the version bump"
  artifacts:
    - path: "src/backend/humble/electronStores.ts"
      provides: "humbleOwnershipOverrideStore, exempted from disconnect wipe"
      contains: "humbleOwnershipOverrideStore"
    - path: "src/backend/humble/library.ts"
      provides: "recomputeOwnership() export + sync-end hook + set/clearOwnershipOverride methods"
      contains: "recomputeOwnership"
  key_links:
    - from: "src/backend/humble/library.ts"
      to: "src/backend/humble/dedup.ts recomputeOwnership"
      via: "import + call after sync, injecting steam games + override predicate"
      pattern: "recomputeOwnership"
    - from: "src/backend/humble/library.ts"
      to: "humbleKeysUpdated frontend message"
      via: "re-push after dedup mutation"
      pattern: "humbleKeysUpdated"
    - from: "src/backend/humble/library.ts recomputeOwnership"
      to: "steamLibraryStore.get('games', [])"
      via: "reads Steam owned-apps, gated on Steam connectivity"
      pattern: "steamLibraryStore"
---

<objective>
Wire the pure `dedup.ts` matcher into the live Humble backend: add the override store (D-42/43, disconnect-exempt like `humbleRevealedStore`), hook the recompute pass into the end of `runSync`, expose a `recomputeOwnership()` entrypoint for the Steam-refresh trigger (Plan 04), add `set/clearOwnershipOverride` methods, and gate the whole write on Steam connectivity so a Steam hiccup never zeroes out ownership.

Purpose: HDEDUP-01's persistence + recompute + keep-last-known behavior, and the backfill that makes HDEDUP-02 fire for existing users. This is where all store I/O lives (the pure module has none).
Output: Extended `electronStores.ts`, `user.ts` disconnect-exemption confirmation, `library.ts` recompute wiring + override methods, override-store + backfill tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-ownership-dedup/12-RESEARCH.md
@.planning/phases/12-ownership-dedup/12-PATTERNS.md

<interfaces>
<!-- dedup.ts export (Plan 12-02): -->
export function recomputeOwnership(
  keys: HumbleKey[],
  steamGames: GameInfo[],
  isOverridden: (machineName: string) => boolean
): HumbleKey[]

<!-- library.ts existing shapes (read this session):
     getKeys() ~lines 276-282: iterates humbleLibraryStore.entries(), flattens entry.keys
     runSync ~lines 425-449: storedClassifierVersion vs HUMBLE_CLASSIFIER_VERSION → reclassifyAll → full re-fetch
     progressive push ~line 480: sendFrontendMessage('humbleKeysUpdated', getKeys())
     export block ~lines 555-559: export const HumbleLibrary = { loadCached, sync, getKeys, getSyncState } -->

<!-- electronStores.ts precedent (~lines 28-36): humbleRevealedStore = new CacheStore<{revealedAt:number}, string>('humble_revealed', null) — NEVER cleared on disconnect. Export block ~lines 38-43. -->

<!-- user.ts disconnect() ~lines 471-521: clears configStore/humbleLibraryStore/humbleSyncStore; EXEMPTION comment ~lines 514-520 names humbleRevealedStore as never-cleared. -->

<!-- Steam ownership source: import { steamLibraryStore } from 'backend/storeManagers/steam/electronStores' → .get('games', []): GameInfo[] (app_name = stringified AppID).
     Steam connectivity: SteamUser.isLoggedIn() (backend/storeManagers/steam/user). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Override store + disconnect exemption</name>
  <files>src/backend/humble/electronStores.ts, src/backend/humble/user.ts, src/backend/humble/__tests__/electronStores.test.ts</files>
  <read_first>
    - src/backend/humble/electronStores.ts (the full file — specifically humbleRevealedStore ~lines 28-36 with its D-04/D-30 "NEVER cleared by disconnect" comment, and the export block ~lines 38-43)
    - src/backend/humble/user.ts (disconnect() ~lines 471-521, especially which stores are cleared at ~lines 484/489-490 and the exemption comment ~lines 514-520)
    - src/backend/humble/__tests__/user.test.ts (existing disconnect test style, to mirror for the override-survival assertion)
  </read_first>
  <action>
    In `electronStores.ts`, add `humbleOwnershipOverrideStore = new CacheStore<{ overriddenAt: number }, string>('humble_ownership_override', null)` immediately next to `humbleRevealedStore`, keyed by `machine_name` per D-42. Copy the D-04/D-30 comment style and cross-reference D-42/D-43: this store is NEVER cleared by `HumbleUser.disconnect()` — a user's "Not the same game" correction must survive disconnect/reconnect so it cannot silently regress and re-block a Phase 14 claim. Add it to the `export { ... }` block. In `user.ts`, confirm `disconnect()` does NOT clear `humbleOwnershipOverrideStore` (it should already be absent from the wipe list — do not add it), and extend the existing exemption comment (~lines 514-520) to name the new override store alongside `humbleRevealedStore` per D-43 ("never delete this exclusion"). Add a test to `electronStores.test.ts` (create the file mirroring existing __tests__ style if it does not exist): set an override, simulate a disconnect wipe of the library/sync/config stores, then assert the override still reads back.
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/electronStores.test.ts --silent ; grep -c "humbleOwnershipOverrideStore" src/backend/humble/electronStores.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/humble/electronStores.ts` exports `humbleOwnershipOverrideStore` created with the store name `'humble_ownership_override'`
    - `src/backend/humble/user.ts` disconnect() does NOT reference `humbleOwnershipOverrideStore` in its clear/wipe path; its exemption comment names the override store
    - `npx jest src/backend/humble/__tests__/electronStores.test.ts` exits 0, including a test that an override survives a disconnect-style wipe
  </acceptance_criteria>
  <done>The override store exists, mirrors humbleRevealedStore's disconnect-survival exactly, and a test proves the override survives disconnect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompute wiring, override methods, backfill, connectivity gate</name>
  <files>src/backend/humble/library.ts, src/backend/humble/__tests__/library.test.ts</files>
  <read_first>
    - src/backend/humble/library.ts (getKeys ~276-282; runSync ~425-480 incl. the reclassifyAll branch and the progressive humbleKeysUpdated push at ~480; the terminal setSyncState block ~536-550; the export block ~555-559)
    - src/backend/humble/dedup.ts (the recomputeOwnership signature + behavior from Plan 12-02)
    - src/backend/humble/electronStores.ts (humbleOwnershipOverrideStore from Task 1; humbleLibraryStore for entries()/set())
    - src/backend/storeManagers/steam/electronStores.ts (steamLibraryStore.get('games', []) shape)
    - src/backend/storeManagers/steam/user.ts (SteamUser.isLoggedIn() or the equivalent connectivity check)
    - src/backend/humble/__tests__/library.test.ts (the existing reclassifyAll / classifier-version test to extend)
  </read_first>
  <behavior>
    - Test "classifier version" (backfill): seed humbleLibraryStore with a pre-Phase-12 cache entry (no steamAppId, classifierVersion unset or 2), run sync (mocked adapter returns a steam tpk with steam_app_id), assert every key re-fetched + steamAppId populated + version stamped 3
    - Test "recompute after sync": after a sync where Steam owns a matching AppID, the committed HumbleKey has ownedElsewhere: true / matchConfidence: 'exact', and humbleKeysUpdated was sent AFTER the dedup mutation
    - Test "keep-last-known" (D-48): with a key already ownedElsewhere: true in the store, call recomputeOwnership() while SteamUser.isLoggedIn() is false (or steam games array empty) → the flag stays true, no write occurs (no-op)
    - Test "override clears fuzzy": setOwnershipOverride(machineName) then recompute → that key's ownedElsewhere becomes false / matchConfidence 'none'; clearOwnershipOverride restores computed match
  </behavior>
  <action>
    Import `recomputeOwnership` from `./dedup`, `humbleOwnershipOverrideStore` from `./electronStores`, `steamLibraryStore` from the Steam store manager, and the Steam connectivity check. Add an exported `recomputeOwnership()` function on `HumbleLibrary` (the Steam-refresh entrypoint used by Plan 04) that: (1) gates on Steam being connected AND `steamLibraryStore.get('games', []).length > 0` — if either fails, return immediately as a no-op leaving all cached `ownedElsewhere`/`matchConfidence` untouched (D-48 keep-last-known, Pitfall 3 / Assumption A3 double-gate); (2) otherwise iterates `humbleLibraryStore.entries()`, calls the pure `dedup.recomputeOwnership(entry.keys, steamGames, (mn) => humbleOwnershipOverrideStore.has(mn))`, writes back via `humbleLibraryStore.set(gamekey, { ...entry, keys: mutatedKeys })`; (3) after the full pass, re-pushes `sendFrontendMessage('humbleKeysUpdated', getKeys())` once (Pitfall 5 — the per-order progressive push does NOT cover this distinct final step). Call this same function as a final step at the end of `runSync()` (after all orders commit, before/with the terminal setSyncState). Add `setOwnershipOverride(machineName)` (write `{ overriddenAt: Date.now() }` to the override store, then re-run recomputeOwnership) and `clearOwnershipOverride(machineName)` (delete from the store, then re-run recompute) methods, exported on `HumbleLibrary`. The backfill itself needs NO new code — the version bump to 3 (Plan 12-01) already makes runSync's existing reclassifyAll branch re-fetch every gamekey. Extend `library.test.ts` with the four behavior cases.
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/library.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/humble/library.ts` imports `recomputeOwnership` from `./dedup` and `steamLibraryStore` from the Steam store manager
    - `HumbleLibrary` export block includes `recomputeOwnership`, `setOwnershipOverride`, `clearOwnershipOverride`
    - The recompute is a no-op (no `humbleLibraryStore.set`) when Steam is disconnected OR the steam games array is empty — verified by the "keep-last-known" test
    - `sendFrontendMessage('humbleKeysUpdated', ...)` is called after the dedup mutation (grep shows a push in the recompute path, distinct from the per-order loop)
    - `npx jest src/backend/humble/__tests__/library.test.ts` exits 0 with all four new cases passing, including the backfill "classifier version" case
  </acceptance_criteria>
  <done>Ownership recomputes at sync end and via the exported entrypoint, persists onto cached rows, re-pushes to the renderer, is a strict no-op on stale/absent Steam data, respects overrides, and the version-bump backfill is proven by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Steam library cache → humble recompute | Empty/stale Steam data must not be treated as authoritative "owns nothing" |
| Disconnect flow → override store | A wipe must not delete user corrections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering (of decision-relevant state) | library.ts recomputeOwnership | mitigate | Double-gate the write on `SteamUser.isLoggedIn()` AND `steamLibraryStore.get('games', []).length > 0`; a stale/disconnected Steam session makes recompute a complete no-op (D-48) so a false `ownedElsewhere: false` never strips Phase 14's C2 protection |
| T-12-04 | Tampering | humbleOwnershipOverrideStore vs disconnect() | mitigate | Store is added to the disconnect-survival exemption alongside humbleRevealedStore; a test asserts an override survives a disconnect wipe (Pitfall 4) |
| T-12-06 | Information Disclosure | humbleKeysUpdated payload | accept | getKeys() already returns only the display-safe projection (no raw key values, C4/T-11-01) — overlay fields are a boolean + a label, non-sensitive |
</threat_model>

<verification>
- `npx jest src/backend/humble/__tests__/electronStores.test.ts src/backend/humble/__tests__/library.test.ts` green
- `grep` confirms the connectivity double-gate and the post-dedup humbleKeysUpdated push
- Override survives disconnect (test)
</verification>

<success_criteria>
- Recompute runs at sync end + via exported entrypoint, persists onto cached rows, re-pushes to renderer
- Keep-last-known holds: no write when Steam is disconnected/empty
- Override persists, clears only fuzzy matches, and survives disconnect
- Version-bump backfill re-fetches frozen orders (proven by test)
</success_criteria>

<output>
Create `.planning/phases/12-ownership-dedup/12-03-SUMMARY.md` when done
</output>
