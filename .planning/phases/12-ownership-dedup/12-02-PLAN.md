---
phase: 12-ownership-dedup
plan: 02
type: tdd
wave: 2
depends_on: [12-01]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/backend/humble/dedup.ts
  - src/backend/humble/__tests__/dedup.test.ts
  - src/backend/humble/__tests__/fixtures/steamGames.ts
autonomous: false
requirements: [HDEDUP-01]
must_haves:
  truths:
    - "A key with steamAppId matches ownership by exact AppID equality only — no fuzzy second-guessing (D-44)"
    - "A key without steamAppId falls through to normalized-title fuzzy matching at 85%+ (D-45)"
    - "A DLC/edition title does NOT false-positive match its base game (success criterion 3)"
    - "UNPICKED pseudo-entries are excluded from matching entirely (D-27)"
  artifacts:
    - path: "src/backend/humble/dedup.ts"
      provides: "Pure ownership-matching module: recomputeOwnership, fuzzyMatch, titleSimilarity, isDlcFalsePositiveRisk, normalizeTitle"
      min_lines: 40
      contains: "recomputeOwnership"
    - path: "src/backend/humble/__tests__/dedup.test.ts"
      provides: "AppID/fuzzy/DLC-guard/UNPICKED-exclusion/keep-last-known coverage"
      contains: "isDlcFalsePositiveRisk"
    - path: "src/backend/humble/__tests__/fixtures/steamGames.ts"
      provides: "GameInfo[] fixture incl. documented DLC/edition-variant titles"
  key_links:
    - from: "src/backend/humble/dedup.ts"
      to: "fastest-levenshtein distance()"
      via: "import"
      pattern: "fastest-levenshtein"
    - from: "src/backend/humble/dedup.ts recomputeOwnership"
      to: "HumbleKey.ownedElsewhere / matchConfidence"
      via: "returns mutated key rows"
      pattern: "matchConfidence"
---

<objective>
Create the pure ownership-matching module `dedup.ts` — the only genuinely novel code in this phase — behind a package-legitimacy gate for its one new dependency (`fastest-levenshtein`). Two-tier matching per D-44/D-45: exact AppID when `steamAppId` is present, length-sensitive normalized-Levenshtein fuzzy fallback at 85%+ with an explicit DLC-keyword guard otherwise.

Purpose: HDEDUP-01's cross-reference logic. The algorithm choice is the single highest-risk decision in the phase — a token-set algorithm would structurally cause the DLC false-positive this phase must prevent, so this uses a length-sensitive ratio instead.
Output: `fastest-levenshtein` installed (after legitimacy gate), `dedup.ts` pure module, `dedup.test.ts` + `fixtures/steamGames.ts` covering AppID/fuzzy/DLC/UNPICKED/keep-last-known.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-ownership-dedup/12-RESEARCH.md
@.planning/phases/12-ownership-dedup/12-PATTERNS.md

<interfaces>
<!-- Extended HumbleKey (from Plan 12-01) — dedup.ts input/output type. -->
export interface HumbleKey {
  gamekey: string; machineName: string; state: HumbleKeyState
  title: string; platform: string; expiration: string | null; origin: string
  steamAppId?: string; ownedElsewhere: boolean
  matchConfidence: 'exact' | 'fuzzy' | 'none'
}

<!-- Steam ownership source: steamLibraryStore.get('games', []) returns GameInfo[],
     where app_name is the stringified Steam AppID (steam/library.ts line 200: app_name: appIdStr).
     dedup.ts receives GameInfo[] as an argument — it does NOT import the store (pure module). -->

<!-- HUMBLE_FUZZY_MATCH_THRESHOLD = 0.85 exported from constants.ts (Plan 12-01). -->

<!-- RESEARCH.md Pattern 3 contains the vetted reference implementation of
     normalizeTitle / titleSimilarity / isDlcFalsePositiveRisk / fuzzyMatch,
     EDITION_SUFFIXES + DLC_KEYWORDS lists — copy that logic. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — fastest-levenshtein</name>
  <what-built>Nothing yet. This gate precedes the install of the one new runtime dependency. Per the package-provenance rule, `fastest-levenshtein` is tagged `[ASSUMED]` (discovered via WebSearch, not Context7/official docs) despite a clean `[OK]` slopcheck verdict recorded during research — an [ASSUMED] package install is never auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/fastest-levenshtein and confirm: version `1.0.16` exists, published 2022-08-02, source repo `github.com/ka-weihe/fastest-levenshtein`, no postinstall script, high download count.
    2. Confirm the package name is spelled exactly `fastest-levenshtein` (no typosquat variant like `fastest-levenstein` / `fast-levenshtein` — the latter is a different, older package).
    3. Optionally re-run `npx slopcheck install fastest-levenshtein` and confirm `[OK]` and clean `git status` afterward (the research note warns slopcheck performs a real install + revert).
  </how-to-verify>
  <action>Pause execution. Do NOT run `pnpm add fastest-levenshtein` or create dedup.ts until the human completes the legitimacy verification below and approves. This gate is never auto-approvable (`[ASSUMED]` provenance, T-12-SC), so `workflow.auto_advance` does not apply.</action>
  <verify>
    <automated>MISSING — human legitimacy verification per [ASSUMED] package gate (T-12-SC); not auto-approvable</automated>
  </verify>
  <resume-signal>Type "approved" to authorize the `pnpm add fastest-levenshtein` in Task 2, or "reject" to fall back to a hand-rolled normalized-Levenshtein implementation (no new dependency).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure dedup module + tests</name>
  <files>package.json, pnpm-lock.yaml, src/backend/humble/dedup.ts, src/backend/humble/__tests__/dedup.test.ts, src/backend/humble/__tests__/fixtures/steamGames.ts</files>
  <read_first>
    - .planning/phases/12-ownership-dedup/12-RESEARCH.md (Pattern 3 — the vetted normalizeTitle/titleSimilarity/isDlcFalsePositiveRisk/fuzzyMatch reference, EDITION_SUFFIXES + DLC_KEYWORDS lists, and the "Test fixtures to reuse verbatim" block)
    - src/backend/humble/classify.ts (module docstring ~lines 220-228 — copy the identical "No I/O, no logging, no store import" discipline for dedup.ts)
    - src/backend/humble/__tests__/classify.test.ts (fixture-driven no-mocking test style to mirror)
    - src/backend/humble/__tests__/fixtures/tpks.ts (fixture-module convention — named exported constants — for the new steamGames.ts)
    - src/common/types/game.ts or src/common/types.ts (the GameInfo type, specifically the app_name field, so the fixture is well-typed)
  </read_first>
  <behavior>
    - Test "appid": a HumbleKey with `steamAppId: '620'` against a GameInfo with `app_name: '620'` → `ownedElsewhere: true`, `matchConfidence: 'exact'`; against a library WITHOUT app_name 620 → `ownedElsewhere: false`, `matchConfidence: 'none'` (no fuzzy fallback — D-44 AppID verdict is final)
    - Test "fuzzy match": no steamAppId; `"Assault Android Cactus+"` vs owned `"Assault Android Cactus"` → matched `fuzzy`; `"FRAMED Collection"` vs `"Framed Collection"` → matched `fuzzy`; `"Into the Breach"` vs `"Into The Breach (Steam)"` → matched `fuzzy`
    - Test "dlc": `"Game X: Season Pass"` vs owned `"Game X"` → NOT matched (`ownedElsewhere: false`); `"Batman"` vs owned `"Batman: Arkham Knight"` → NOT matched
    - Test "unpicked": a key with `state: 'UNPICKED'` is skipped — never matched, returned unchanged
    - Test "cross-platform" (D-45): a `platform: 'gog'` key (no steamAppId) fuzzy-matches a Steam-owned title → matched `fuzzy` (no platform gate)
    - Test "keep-last-known" (D-48 unit-level): `recomputeOwnership` called with an EMPTY GameInfo[] returns the input keys with their `ownedElsewhere`/`matchConfidence` UNCHANGED (the caller, not this module, decides connectivity — but assert the function does not zero-out on empty input; see note)
  </behavior>
  <action>
    After the Task 1 gate is approved, run `pnpm add fastest-levenshtein` (confirm `git status` shows only intended package.json/pnpm-lock.yaml changes). Create `src/backend/humble/dedup.ts` as a pure module (docstring copying classify.ts's "No I/O, no logging, no store import" contract — no electron-store import, no sendFrontendMessage). Implement `normalizeTitle`, `titleSimilarity` (using `import { distance } from 'fastest-levenshtein'`, returning `1 - distance/maxLen`), `isDlcFalsePositiveRisk`, and `fuzzyMatch` exactly per RESEARCH.md Pattern 3, with the `EDITION_SUFFIXES` and `DLC_KEYWORDS` as fixed hardcoded module-level constants (NEVER dynamic/API-sourced — T-12-01 ReDoS guard). Read the threshold from `HUMBLE_FUZZY_MATCH_THRESHOLD` (constants.ts). Export `recomputeOwnership(keys: HumbleKey[], steamGames: GameInfo[], isOverridden: (machineName: string) => boolean): HumbleKey[]`: for each key — skip UNPICKED (return unchanged); if `steamAppId` present, exact-match against `steamGames.some(g => g.app_name === key.steamAppId)` and set `matchConfidence: 'exact'` on hit, `'none'` on miss, with NO fuzzy fallback (D-44); else run `fuzzyMatch(key.title, g.title)` across `steamGames`, setting `'fuzzy'` on hit; then apply the D-42 override — if `isOverridden(key.machineName)` is true, force `ownedElsewhere: false`, `matchConfidence: 'none'` (override only ever clears a fuzzy match). The module must NOT read any store — the override predicate and the Steam games array are injected by the caller (library.ts, Plan 03), exactly like classify.ts injects `isRevealed`. For the "keep-last-known" unit test, note the module itself computes from whatever it is given; the D-48 connectivity gate lives in the caller (Plan 03) — this test asserts the module does not crash/zero-out on an empty games array and simply produces no matches for that call. Create `fixtures/steamGames.ts` with a `GameInfo[]` including the documented DLC/edition-variant titles from RESEARCH.md's fixture block. Write `dedup.test.ts` covering all behavior cases with jest-named `-t` selectors matching the RESEARCH.md test map ("appid", "fuzzy match", "dlc", "unpicked", "keep-last-known").
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/dedup.test.ts --silent && git status --porcelain | grep -E 'package.json|pnpm-lock' </automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/humble/dedup.ts` exists, exports `recomputeOwnership`, `titleSimilarity`, `isDlcFalsePositiveRisk`, `fuzzyMatch`, `normalizeTitle`
    - dedup.ts imports `fastest-levenshtein` and NO electron-store / no sendFrontendMessage (pure-module contract — grep shows no `electronStores`/`sendFrontendMessage` import)
    - `EDITION_SUFFIXES` and `DLC_KEYWORDS` are module-level `const` arrays (not parameters, not read from any config/IPC/API source)
    - `npx jest src/backend/humble/__tests__/dedup.test.ts` exits 0 with the "appid", "fuzzy match", "dlc", "unpicked", "cross-platform", "keep-last-known" cases all passing
    - `"Game X: Season Pass"` does NOT match `"Game X"` and `"Batman"` does NOT match `"Batman: Arkham Knight"`
    - `package.json` lists `fastest-levenshtein` and `git status` shows no unrelated dependency drift
  </acceptance_criteria>
  <done>fastest-levenshtein installed after the legitimacy gate; dedup.ts is a pure two-tier matcher (exact AppID final, fuzzy 85%+ with DLC guard) with full green test coverage including the project's own documented false-positive fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → project | New dependency `fastest-levenshtein` enters the supply chain |
| Humble API titles → dedup.ts | Untrusted title strings used only as string-comparison inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | `pnpm add fastest-levenshtein` | mitigate | slopcheck `[OK]` + blocking-human legitimacy checkpoint (Task 1) before install; [ASSUMED] provenance, never auto-approved; verify `github.com/ka-weihe/fastest-levenshtein` + no postinstall on npmjs.com |
| T-12-01 | Denial of Service | normalizeTitle regex (`new RegExp(\\b${suffix}\\b)`) | mitigate | `EDITION_SUFFIXES`/`DLC_KEYWORDS` stay fixed hardcoded developer-controlled constants — NEVER accepted from a config file, IPC payload, or the Humble API response (blocks ReDoS via a user-extensible list) |
| T-12-05 | Input Validation | title strings from Humble API | mitigate | Titles are only ever normalized + edit-distance-compared — never interpolated into a shell/path/SQL/dynamically-constructed regex from untrusted data |
</threat_model>

<verification>
- `npx jest src/backend/humble/__tests__/dedup.test.ts` green (all named cases)
- `grep` confirms no store/IPC import in dedup.ts (pure-module contract)
- `git status` shows only intended package.json/pnpm-lock.yaml + new-file changes
</verification>

<success_criteria>
- dedup.ts implements exact-AppID-final + fuzzy-85%-with-DLC-guard two-tier matching per D-44/D-45
- The project's documented DLC/edition false-positive fixtures all pass (success criterion 3)
- fastest-levenshtein installed only after the blocking-human legitimacy gate
</success_criteria>

<output>
Create `.planning/phases/12-ownership-dedup/12-02-SUMMARY.md` when done
</output>
