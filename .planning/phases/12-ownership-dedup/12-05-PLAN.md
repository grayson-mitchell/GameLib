---
phase: 12-ownership-dedup
plan: 05
type: execute
wave: 5
depends_on: [12-01, 12-04]
files_modified:
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.css
  - src/frontend/screens/Game/GamePage/components/HumbleOriginInfo.tsx
  - src/frontend/screens/Game/GamePage/components/__tests__/HumbleOriginInfo.test.tsx
  - src/frontend/screens/Game/GamePage/index.tsx
  - public/locales/en/translation.json
  - public/locales/en/gamepage.json
autonomous: false
requirements: [HDEDUP-01, HDEDUP-02]
must_haves:
  truths:
    - "A matched Humble key row shows 'Owned on Steam' (exact) or 'Likely owned on Steam' (fuzzy) — fact only, no re-sorting/dimming (D-38/39/41)"
    - "A fuzzy-matched row (only) shows a 'Not the same game' override affordance that fires the override IPC (D-42)"
    - "A Steam game with a REDEEMED, confirmed-matched Humble key shows a Humble-origin annotation on its details 'info' tab (D-35)"
    - "A REDEEMED key with no confirmed match renders no annotation and no mismatch flag (D-37)"
  artifacts:
    - path: "src/frontend/screens/Game/GamePage/components/HumbleOriginInfo.tsx"
      provides: "Redeemed-only Steam-side Humble-origin annotation"
      contains: "REDEEMED"
    - path: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      provides: "owned badge + fuzzy-only override affordance"
      contains: "ownedElsewhere"
  key_links:
    - from: "src/frontend/screens/Game/GamePage/components/HumbleOriginInfo.tsx"
      to: "humble.keys context"
      via: "find REDEEMED key where steamAppId === gameInfo.app_name"
      pattern: "steamAppId === gameInfo.app_name"
    - from: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      to: "window.api.humbleSetOwnershipOverride"
      via: "override affordance onClick (fuzzy rows only)"
      pattern: "humbleSetOwnershipOverride"
    - from: "src/frontend/screens/Game/GamePage/index.tsx"
      to: "HumbleOriginInfo"
      via: "mounted in the info TabPanel"
      pattern: "HumbleOriginInfo"
---

<objective>
Surface ownership to the user: an owned-badge (and fuzzy-only override affordance) on the Phase 11 Humble Keys page rows, and a redeemed-only Humble-origin annotation on the Steam game-details page — the visible proof of HDEDUP-01 and the HDEDUP-02 collapse.

Purpose: HDEDUP-01 visibility (D-38/39/41/42) and HDEDUP-02 annotation (D-35/36/37/40). Both are pure client-side reads of the existing `humble.keys` context — no new data path.
Output: Extended `HumbleKeyRow`, new `HumbleOriginInfo` component mounted in GamePage, component test, i18n keys, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-ownership-dedup/12-CONTEXT.md
@.planning/phases/12-ownership-dedup/12-PATTERNS.md

<interfaces>
<!-- Extended HumbleKey (Plan 12-01): ownedElsewhere: boolean; matchConfidence: 'exact'|'fuzzy'|'none'; steamAppId?: string -->

<!-- humble.keys context: useContext(ContextProvider) → const { humble } = ...; humble.keys is HumbleKey[]
     (established in Keys/index.tsx line 41). Delivered reactively via humbleKeysUpdated (Phase 11). -->

<!-- HumbleKeyRow existing (~lines 47-69): renders humbleKeyStateBadge + humbleKeyRowInfo (title + caption) + humbleKeyRowExpiration.
     DOCSTRING lines 11-13: D-22 strictly read-only — D-42 override is the SANCTIONED exception (fuzzy rows only). -->

<!-- window.api.humbleSetOwnershipOverride(machineName) / humbleClearOwnershipOverride(machineName) — preload bridges from Plan 12-04.
     Fire-and-forget like Keys/index.tsx line 142: onClick={() => window.api.humbleSync()} -->

<!-- PlatformSupport.tsx analog: { gameInfo: GameInfo } prop, useTranslation('gamepage'), root <div>.
     GamePage/index.tsx info TabPanel mount ~lines 563-565 (PlatformSupport/DownloadSizeInfo/InstalledInfo siblings);
     barrel import block ~lines 53-63. -->

<!-- i18n: Keys page uses translation.json (humbleKeys.* namespace); GamePage uses gamepage.json (info.* namespace). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Owned badge + fuzzy-only override on HumbleKeyRow</name>
  <files>src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx, src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.css, public/locales/en/translation.json</files>
  <read_first>
    - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx (the full file — the render block ~lines 47-69, the badge/caption/expiration class-naming convention, and the D-22 read-only docstring ~lines 11-13)
    - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.css (existing humbleKeyStateBadge / humbleKeyRowCaption styling — use the same semantic tokens, no hard-coded hex/px)
    - src/frontend/screens/Humble/Keys/index.tsx (line 142 fire-and-forget onClick pattern; line 41 useContext(ContextProvider))
    - public/locales/en/translation.json (existing humbleKeys.* keys, to add new ones in the same namespace)
  </read_first>
  <action>
    Extend `HumbleKeyRow` to render a new `<span className="humbleKeyOwnedBadge">` (following the `humbleKeyStateBadge`/`humbleKeyRowCaption` naming convention, styled via semantic tokens in index.css) ONLY when `humbleKey.ownedElsewhere` is true. Badge text switches on `matchConfidence` per D-41: `'exact'` → t('humbleKeys.ownedOnSteam', 'Owned on Steam'), `'fuzzy'` → t('humbleKeys.likelyOwnedOnSteam', 'Likely owned on Steam'). Fact-only copy — NO "claim this" / "giftable spare" recommendation (D-39; that is Phase 13). Add the "Not the same game" override affordance ONLY when `matchConfidence === 'fuzzy'` (D-42): a small button/link calling `window.api.humbleSetOwnershipOverride(humbleKey.machineName)` fire-and-forget (mirroring Keys/index.tsx line 142). D-39 presentation-only: do NOT re-sort, dim, or otherwise alter the D-21 layout / groupKeys ordering — the badge is a purely additive inline element. Update the D-22 read-only docstring (~lines 11-13) to note the D-42 override is the single sanctioned exception (fuzzy rows only); every other interaction remains forbidden. Add the two badge keys + the override label key (e.g. `humbleKeys.notTheSameGame`, 'Not the same game') to translation.json's humbleKeys namespace.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -20 ; grep -c "humbleSetOwnershipOverride\|ownedElsewhere" src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HumbleKeyRow renders an owned badge only when `ownedElsewhere` is true, with text branching on `matchConfidence` ('exact' vs 'fuzzy')
    - The override affordance calling `window.api.humbleSetOwnershipOverride(humbleKey.machineName)` renders ONLY when `matchConfidence === 'fuzzy'` (grep shows the fuzzy condition guarding it)
    - No re-sort/dim logic added; groupKeys/GROUP_ORDER imports unchanged (D-39 presentation-only)
    - index.css uses semantic tokens only (no hard-coded hex or px in the new rules)
    - translation.json contains `humbleKeys.ownedOnSteam`, `humbleKeys.likelyOwnedOnSteam`, and the override label key
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Matched rows show the correct exact/fuzzy badge; fuzzy rows (only) offer the override; layout ordering is untouched; strings via t().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HumbleOriginInfo annotation + GamePage mount</name>
  <files>src/frontend/screens/Game/GamePage/components/HumbleOriginInfo.tsx, src/frontend/screens/Game/GamePage/components/__tests__/HumbleOriginInfo.test.tsx, src/frontend/screens/Game/GamePage/index.tsx, public/locales/en/gamepage.json</files>
  <read_first>
    - src/frontend/screens/Game/GamePage/components/PlatformSupport.tsx (the full file — the { gameInfo: GameInfo } prop shape, useTranslation('gamepage'), root div; copy this structure verbatim)
    - src/frontend/screens/Game/GamePage/index.tsx (the info TabPanel mount ~lines 563-565 and the component barrel import block ~lines 53-63)
    - src/frontend/screens/Humble/Keys/index.tsx (line 41: const { humble } = useContext(ContextProvider) — the established way to read humble.keys)
    - public/locales/en/gamepage.json (existing info.* keys, to add the annotation key in the same namespace)
    - src/backend/storeManagers/steam/library.ts (~lines 261-262 — the app_name equality lookup pattern this mirrors)
  </read_first>
  <behavior>
    - Test 1: renders the annotation text (with the origin/bundle name) when `humble.keys` contains a `state === 'REDEEMED'` key whose `steamAppId === gameInfo.app_name` and `gameInfo.runner === 'steam'`
    - Test 2: renders nothing (returns null) when `gameInfo.runner !== 'steam'`
    - Test 3: renders nothing when the only matching-AppID key is NOT in REDEEMED state (D-40 redeemed-only)
    - Test 4: renders nothing (no mismatch flag, no guessing) when no key matches this AppID (D-37)
  </behavior>
  <action>
    Create `src/frontend/screens/Game/GamePage/components/HumbleOriginInfo.tsx` copying `PlatformSupport.tsx`'s shape: `{ gameInfo: GameInfo }` prop, `useTranslation('gamepage')`, plus `useContext(ContextProvider)` for `humble.keys`. Derive `matchedKey = humble.keys?.find(k => k.state === 'REDEEMED' && k.steamAppId === gameInfo.app_name)`. Return `null` when `gameInfo.runner !== 'steam'` or no `matchedKey` (D-37 no annotation / no mismatch flag on unmatched redeemed rows; D-40 redeemed-only). Otherwise render `<div className="humbleOriginInfo">` with `t('info.humbleOrigin', 'Includes a key from Humble Bundle: {{origin}}', { origin: matchedKey.origin })` — origin-only copy, NO purchase date (D-35). Mount `<HumbleOriginInfo gameInfo={gameInfo} />` as a sibling in the info TabPanel (~lines 563-565) and add it to the barrel import block (~lines 53-63). Add the `info.humbleOrigin` key to gamepage.json. Write `HumbleOriginInfo.test.tsx` covering the four behavior cases (mock the ContextProvider `humble.keys` value).
  </action>
  <verify>
    <automated>npx jest src/frontend/screens/Game/GamePage/components/__tests__/HumbleOriginInfo.test.tsx --silent</automated>
  </verify>
  <acceptance_criteria>
    - `HumbleOriginInfo.tsx` finds a REDEEMED key where `steamAppId === gameInfo.app_name` and returns null for non-steam runner or no match
    - The annotation copy is origin-only (contains `matchedKey.origin`, no purchase date)
    - `GamePage/index.tsx` mounts `<HumbleOriginInfo gameInfo={gameInfo} />` in the info TabPanel and imports it in the barrel block
    - gamepage.json contains `info.humbleOrigin`
    - `npx jest .../HumbleOriginInfo.test.tsx` exits 0 with all four cases (redeemed-match / non-steam / non-redeemed / no-match) passing
  </acceptance_criteria>
  <done>The Steam details page shows a redeemed-only, confirmed-match-only, origin-only Humble annotation; unmatched/non-redeemed cases render nothing; component test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify owned badge + Steam annotation in the running app</name>
  <what-built>The owned-badge (exact/fuzzy) + fuzzy-only override on the Humble Keys page, and the redeemed-only Humble-origin annotation on the Steam game-details page. All backend matching, persistence, and recompute triggers are already wired (Plans 12-01..04).</what-built>
  <how-to-verify>
    1. Launch the app with a connected Humble account and a connected Steam account whose library overlaps at least one Humble key.
    2. Open the Humble Keys page: confirm a matching row shows "Owned on Steam" (exact AppID match) or "Likely owned on Steam" (fuzzy). Confirm state groups + expiring-soonest ordering are UNCHANGED (no dimming, no re-sort).
    3. On a fuzzy-matched row, click "Not the same game" — confirm the badge clears and stays cleared after an app restart (override persisted). Reconnect Humble and confirm the correction did NOT regress (survives disconnect, D-43).
    4. Open a Steam game that has a REDEEMED Humble key: confirm the details "info" tab shows "Includes a key from Humble Bundle: {bundle name}" (origin only, no date). Open a Steam game with NO redeemed Humble key: confirm NO annotation appears.
    5. Toggle Steam logout, trigger a refresh, and confirm previously-owned badges do NOT flip to unowned (D-48 keep-last-known).
  </how-to-verify>
  <action>Pause execution and present the verification steps above to the user in the running Electron app. Do NOT mark the phase complete or advance until the user approves. All implementation is already done in Tasks 1-2 and Plans 12-01..04 — this task only gathers human sign-off on the visual + persistence behavior that cannot be asserted automatically.</action>
  <verify>
    <human-check>Owned badge (exact/fuzzy), fuzzy-only override that persists + survives reconnect, redeemed-only Steam annotation, and keep-last-known all behave as specified</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any issue (wrong badge text, override not persisting, annotation on unmatched game, flags flipping on Steam logout).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| context data → render | Presentational reads of humble.keys; no new secret surface |
| override button → IPC | Renderer initiates a state change (authoritative check is server-side, Plan 12-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03f | Tampering | override affordance render gate | mitigate | The fuzzy-only render gate is UX-only; the authoritative fuzzy-only enforcement is the server-side check in Plan 12-04 (T-12-03) — the renderer gate is defense-in-depth, not the trust boundary |
| T-12-08 | Information Disclosure | HumbleOriginInfo / badge | accept | Renders only display-safe overlay fields (boolean + label + bundle name already in the display-safe HumbleKey projection); no raw key value ever reaches this component (C4/T-11-01) |
</threat_model>

<verification>
- `npx jest src/frontend/screens/Game/GamePage/components/__tests__/HumbleOriginInfo.test.tsx` green
- `npx tsc --noEmit` clean
- Human-verify checkpoint approved
</verification>

<success_criteria>
- Keys page shows exact/fuzzy owned badge; fuzzy rows offer a persisting, disconnect-surviving override; layout ordering untouched
- Steam details page shows a redeemed-only, confirmed-match-only, origin-only Humble annotation; unmatched/non-redeemed render nothing
- Keep-last-known verified in the running app
</success_criteria>

<output>
Create `.planning/phases/12-ownership-dedup/12-05-SUMMARY.md` when done
</output>
