---
phase: 08.1
slug: steam-delisted-games-library-filters
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-04
---

# Phase 08.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29.x |
| **Config file** | `jest.config.js` (root) |
| **Quick run command** | `pnpm test -- --testPathPattern=steam/__tests__/games` |
| **Full suite command** | `pnpm test && pnpm lint && pnpm tsc --noEmit` |
| **Estimated runtime** | ~30 seconds (quick) / ~2 min (full suite) |

> **Note:** This project has no frontend component tests (`@testing-library/react` is installed but unused — all Jest tests live in `src/backend/`). Frontend behaviors (LIB-08 badge/install-hide, LIB-09 tri-state filter + localStorage migration) are verified by `pnpm tsc --noEmit` + `grep` source assertions during execution and by manual UAT before `/gsd:verify-work`.

---

## Sampling Rate

- **After every task commit:** Run `pnpm test -- --testPathPattern=steam/__tests__/games` (backend tasks) or the task's `grep`/`pnpm tsc --noEmit` assertion (frontend tasks)
- **After every plan wave:** Run `pnpm test && pnpm lint && pnpm tsc --noEmit`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.1-01-T1 | 01 | 1 | LIB-07 | — | N/A (docs traceability) | source assertion | `grep -c "LIB-0[789]" .planning/REQUIREMENTS.md` | ✅ | ⬜ pending |
| 08.1-01-T2 | 01 | 1 | LIB-07 | T-01 (install dead-path) | `isGameAvailable()` returns false when `is_delisted === true` | unit (TDD) | `pnpm test -- --testPathPattern=steam/__tests__/games` | ✅ | ⬜ pending |
| 08.1-02-T1 | 02 | 1 | LIB-08 | T-02 (install handoff) | install button + context-menu install hidden for delisted; launch/uninstall preserved | source assertion | `pnpm tsc --noEmit` + GameCard grep | ✅ | ⬜ pending |
| 08.1-02-T2 | 02 | 1 | LIB-08 | T-03 (DOM injection) | static i18n badge text, no untrusted store text in DOM | source assertion | `grep -q "gameCardDelistedBadge" && grep -q "Game no longer available"` | ✅ | ⬜ pending |
| 08.1-03-T1 | 03 | 1 | LIB-07, LIB-09 | — | `FilterMode` type migration compiles across 5 consumers | source assertion | `grep -q "export type FilterMode"` | ✅ | ⬜ pending |
| 08.1-03-T2 | 03 | 1 | LIB-07, LIB-09 | T-04 (localStorage migration) | `migrateFilterMode` falls back to `'off'` for legacy/malformed values | source assertion | `grep -q "migrateFilterMode"` + `!showNonAvailable` count = 0 | ✅ | ⬜ pending |
| 08.1-03-T3 | 03 | 1 | LIB-09 | — | tri-state Off→Show→Only controls compile | source assertion | `pnpm tsc --noEmit` across 5 migration files | ✅ | ⬜ pending |
| 08.1-04-T1 | 04 | 2 | LIB-09 | — | 'only'-mode empty-state messaging | source assertion | `grep -q "no_hidden_games" && grep -q "LibraryContext"` | ✅ | ⬜ pending |
| 08.1-04-T2 | 04 | 2 | LIB-09 | — | active 'only' button CSS via `var(--accent)` | source assertion | `grep -q 'aria-pressed="true"' && grep -q "var(--accent)"` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `src/backend/storeManagers/steam/__tests__/games.test.ts` — extend with `isGameAvailable() when is_delisted:true resolves false` (one new `it()` block alongside existing tests at line 914). Handled inline by Plan 08.1-01 T2 via TDD (RED test written before implementation) — no separate Wave 0 plan needed.

*The test file already exists; the framework (Jest) is already configured. No infrastructure install required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Delisted badge renders on card | LIB-08 | No frontend component tests in project | Launch app, view a delisted Steam game in Library — confirm greyed "Game no longer available" badge |
| Install button/menu hidden for delisted | LIB-08 | No frontend component tests in project | View delisted game — confirm no install button and no context-menu install item; launch/uninstall still present for installed delisted |
| Tri-state filter Off→Show→Only cycle | LIB-09 | No frontend component tests in project | Click Hidden / Non-available filter controls — confirm three-state cycle with accent-highlighted "only" button |
| localStorage boolean→tri-state migration | LIB-09 | No frontend component tests in project | With legacy `"true"`/`"false"` values in localStorage, reload — confirm Library renders without crash, falling back to `'off'` |
| Both-'Only' union (hidden OR non-available) | LIB-09 | No frontend component tests in project | Activate both 'only' modes — confirm union (games that are hidden OR non-available) shown |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-04
