---
phase: 08.1-steam-delisted-games-library-filters
plan: 04
type: execute
wave: 2
depends_on: ["08.1-02", "08.1-03"]
files_modified:
  - src/frontend/screens/Library/components/EmptyLibrary/index.tsx
  - src/frontend/components/UI/Dropdown/index.scss
autonomous: true
requirements: [LIB-09]
must_haves:
  truths:
    - "When 'Only Hidden' is active and there are no hidden games, the Library shows 'No hidden games in your library'"
    - "When 'Only non-available' is active and there are no delisted/non-available games, the Library shows 'No non-available games in your library'"
    - "Other filter combinations still fall back to the existing generic 'no results' / 'no games' messages"
    - "The 'only' filter button is visibly accented (not hidden) while its filter is in the Only state"
  artifacts:
    - path: "src/frontend/screens/Library/components/EmptyLibrary/index.tsx"
      provides: "LibraryContext read + context-aware empty-state messages for the two 'only' modes"
      contains: "showNonAvailable"
    - path: "src/frontend/components/UI/Dropdown/index.scss"
      provides: ".only[aria-pressed=true] active-state styling (display + accent)"
      contains: "aria-pressed"
  key_links:
    - from: "src/frontend/screens/Library/components/EmptyLibrary/index.tsx"
      to: "LibraryContext showHidden/showNonAvailable"
      via: "useContext(LibraryContext) tri-state read"
      pattern: "=== 'only'"
    - from: "src/frontend/components/UI/Dropdown/index.scss"
      to: ".only button in LibraryFilters"
      via: "aria-pressed active-state selector"
      pattern: "aria-pressed"
---

<objective>
Complete the LIB-09 tri-state experience: context-aware empty-state messaging for the two "Only"
modes, and the CSS that keeps the "only" button visible + accented while its filter is active.

This is the wave-2 polish layer. `EmptyLibraryMessage` gains a `LibraryContext` read (it does not
currently read filter state) to show mode-specific empty copy, and `Dropdown/index.scss` gets the
`aria-pressed` active rule that pairs with the `aria-pressed` binding added to the "only" button in
Plan 03.

Purpose: Give the user clear feedback when an "Only" filter yields no results, and a visible active
state on the "only" control.
Output: Modified EmptyLibrary component + one new SCSS rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-steam-delisted-games-library-filters/08.1-PATTERNS.md
@.planning/phases/08.1-steam-delisted-games-library-filters/08.1-UI-SPEC.md

<interfaces>
<!-- Verified from codebase + Plan 03 output. Executor uses these directly. -->

FilterMode (from Plan 03) — src/frontend/types.ts:
`export type FilterMode = 'off' | 'show' | 'only'`. LibraryContext exposes
`showHidden: FilterMode` and `showNonAvailable: FilterMode`.

EmptyLibrary — src/frontend/screens/Library/components/EmptyLibrary/index.tsx (verified):
- Imports `useContext` from react, `ContextProvider` from 'frontend/state/ContextProvider' (line 2).
- Reads `{ epic, gog, amazon, zoom, sideloadedLibrary } = useContext(ContextProvider)` (line 10).
- Builds a `message` (default `emptyLibrary.noGames`; switches to `emptyLibrary.noResults` when any
  library has games, ~line 37).
- Returns `<p className="noResultsMessage">{message}</p>` (line 43).
- Does NOT currently read LibraryContext or `t()` for `library.*` keys.

i18n keys (added by Plan 02, in public/locales/en/translation.json):
- `library.no_hidden_games` → "No hidden games in your library"
- `library.no_non_available_games` → "No non-available games in your library"
(Use `t('key', 'fallback')` so the strings render even before locale propagation.)

Dropdown SCSS — src/frontend/components/UI/Dropdown/index.scss (verified, `.only` block ~lines 42-57):
`.only { display: none; ...; &:hover { color: var(--accent); } }` plus a
`&:hover .only, &:focus-within .only { display: block; }` reveal rule. The "only" button is hidden
until hover unless forced visible.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Context-aware empty-state messages for 'only' modes</name>
  <files>src/frontend/screens/Library/components/EmptyLibrary/index.tsx</files>
  <read_first>
    - src/frontend/screens/Library/components/EmptyLibrary/index.tsx (full file — imports, the useContext(ContextProvider) read, the message-building conditionals, the return)
    - src/frontend/screens/Library/LibraryContext.tsx (confirm the exported context object to import)
    - .planning/phases/08.1-steam-delisted-games-library-filters/08.1-PATTERNS.md (§ EmptyLibrary/index.tsx — the LibraryContext read + 'only' overrides)
    - .planning/phases/08.1-steam-delisted-games-library-filters/08.1-UI-SPEC.md (§ Copywriting Contract — the two empty-state rows)
  </read_first>
  <action>
    Import `LibraryContext` from `frontend/screens/Library/LibraryContext` and add
    `const { showHidden, showNonAvailable } = useContext(LibraryContext)` alongside the existing
    `useContext(ContextProvider)`. Destructure `t` from `useTranslation()` (the file already uses
    `i18n`; add `t`). After the existing generic message logic (the `noGames` / `noResults`
    assignment), add two overrides BEFORE the return:
    - if `showHidden === 'only' && showNonAvailable !== 'only'` →
      `message = <>{t('library.no_hidden_games', 'No hidden games in your library')}</>`
    - else if `showNonAvailable === 'only' && showHidden !== 'only'` →
      `message = <>{t('library.no_non_available_games', 'No non-available games in your library')}</>`
    Leave the both-'only' case and all non-'only' combinations on the existing generic message. Keep
    the `<p className="noResultsMessage">{message}</p>` return unchanged.
  </action>
  <verify>
    <automated>grep -q "no_hidden_games" src/frontend/screens/Library/components/EmptyLibrary/index.tsx && grep -q "LibraryContext" src/frontend/screens/Library/components/EmptyLibrary/index.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LibraryContext" src/frontend/screens/Library/components/EmptyLibrary/index.tsx` shows the import and a `useContext(LibraryContext)` read of `showHidden` and `showNonAvailable`
    - The component sets `message` to the `library.no_hidden_games` string when `showHidden === 'only' && showNonAvailable !== 'only'`, and to `library.no_non_available_games` when `showNonAvailable === 'only' && showHidden !== 'only'`
    - Both-'only' and all non-'only' combinations retain the existing generic `emptyLibrary.*` message
    - `pnpm tsc --noEmit` reports no errors in EmptyLibrary/index.tsx
  </acceptance_criteria>
  <done>The empty Library shows mode-specific copy for each single-'only' filter with no matches, and the generic message otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Active-state CSS for the 'only' button</name>
  <files>src/frontend/components/UI/Dropdown/index.scss</files>
  <read_first>
    - src/frontend/components/UI/Dropdown/index.scss (the `.toggleWithOnly` / `.only` block ~lines 42-57, including the hover reveal rule)
    - .planning/phases/08.1-steam-delisted-games-library-filters/08.1-PATTERNS.md (§ Dropdown/index.scss — the aria-pressed rule)
    - .planning/phases/08.1-steam-delisted-games-library-filters/08.1-UI-SPEC.md (§ Tri-State Filter Control — "Visual state of the 'only' button")
  </read_first>
  <action>
    Immediately after the existing `.only` block (and its hover reveal rule), add one rule:
    `.only[aria-pressed="true"] { display: block; color: var(--accent); }` — this forces the "only"
    button visible (overriding the default `display: none`) and applies the accent color when its
    filter is in the Only state (paired with the `aria-pressed` binding from Plan 03). Use the
    `var(--accent)` semantic token only — no raw hex. Do not change the existing `.only` default or
    hover rules.
  </action>
  <verify>
    <automated>grep -q 'aria-pressed="true"' src/frontend/components/UI/Dropdown/index.scss && grep -q "var(--accent)" src/frontend/components/UI/Dropdown/index.scss && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'aria-pressed="true"' src/frontend/components/UI/Dropdown/index.scss` shows a `.only[aria-pressed="true"]` rule setting `display: block` and `color: var(--accent)`
    - The existing `.only` default (`display: none`) and hover-reveal rules are unchanged
    - No raw hex values introduced; the rule uses `var(--accent)`
  </acceptance_criteria>
  <done>The 'only' button is forced visible and accent-colored while its filter is in the Only state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LibraryContext → EmptyLibrary render | tri-state filter values determine which static i18n message is shown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-09 | Injection (XSS) | empty-state message text | accept | Messages are static i18n strings (`library.no_hidden_games`/`no_non_available_games`), not user/store data; React escapes text nodes |
| T-08.1-10 | Tampering | malformed FilterMode reaching EmptyLibrary | accept | Values originate from the migrated LibraryContext (Plan 03 `migrateFilterMode` already normalizes to `'off'|'show'|'only'`); EmptyLibrary only compares against `'only'` and falls through to the generic message otherwise |
| T-08.1-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; no install tasks |
</threat_model>

<verification>
- `pnpm tsc --noEmit` → no errors in EmptyLibrary/index.tsx
- `pnpm lint` → clean for the two touched files
- `.only[aria-pressed="true"]` rule present with `var(--accent)`
- EmptyLibrary reads LibraryContext and renders the two 'only'-mode strings
</verification>

<success_criteria>
- Single-'only' empty states show the correct mode-specific copy; generic message otherwise (D-08)
- 'only' button is visibly accented while active
- All styling via semantic tokens; no raw hex
</success_criteria>

<output>
Create `.planning/phases/08.1-steam-delisted-games-library-filters/08.1-04-SUMMARY.md` when done
</output>
