---
phase: 11-library-sync-5-state-key-model
plan: 05
type: execute
wave: 5
depends_on: [11-04]
files_modified:
  - .planning/phases/11-library-sync-5-state-key-model/11-VALIDATION.md
autonomous: false
requirements: [HSYNC-01, HSYNC-02, HSYNC-03, HSYNC-04]

must_haves:
  truths:
    - "The full jest suite passes green before the phase gate"
    - "11-VALIDATION.md records the executed requirement→test map and the live-API findings for the [ASSUMED] items (A1 UNPICKED shape, A3 redeemed_key_value shape)"
    - "A real Humble account renders every populated state correctly and fails soft on an unreachable API"
  artifacts:
    - path: ".planning/phases/11-library-sync-5-state-key-model/11-VALIDATION.md"
      provides: "Filled validation strategy + live-account findings; nyquist_compliant true"
      contains: "nyquist_compliant"
  key_links:
    - from: ".planning/phases/11-library-sync-5-state-key-model/11-VALIDATION.md"
      to: "src/backend/humble/__tests__ (classify/library/user)"
      via: "requirement→test map references the actual automated commands"
      pattern: "jest"
---

<objective>
Close Phase 11: fill the validation contract from the executed test map, run the full suite, and
verify the synced 5-state inventory against a real Humble account — the human-in-the-loop confirmation
that the classification is correct on live data and that fail-soft behaves as designed.

Purpose: Records the Nyquist validation evidence for HSYNC-01..04 and resolves the two RESEARCH
[ASSUMED] API-shape items (A1 un-picked Choice month detection, A3 `redeemed_key_value` presence
semantics) that Phase 10's live gate could not cover. This is the phase's UAT sign-off.
Output: Populated `11-VALIDATION.md` (nyquist_compliant: true) with the live-account findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-library-sync-5-state-key-model/11-CONTEXT.md
@.planning/phases/11-library-sync-5-state-key-model/11-RESEARCH.md
@.planning/phases/11-library-sync-5-state-key-model/11-VALIDATION.md
@.planning/phases/11-library-sync-5-state-key-model/11-04-SUMMARY.md
@.planning/phases/10-humble-auth-adapter-scaffold/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill 11-VALIDATION.md from the executed test map + run the full suite</name>
  <files>.planning/phases/11-library-sync-5-state-key-model/11-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-library-sync-5-state-key-model/11-VALIDATION.md (draft template to complete)
    - .planning/phases/11-library-sync-5-state-key-model/11-RESEARCH.md (Validation Architecture — Phase Requirements→Test Map, Sampling Rate, Wave 0 Gaps)
    - .planning/phases/11-library-sync-5-state-key-model/11-01-SUMMARY.md, 11-02-SUMMARY.md (actual test files created)
    - .planning/phases/10-humble-auth-adapter-scaffold/10-VALIDATION.md (redacted findings format to mirror)
  </read_first>
  <action>
    Populate `11-VALIDATION.md`: set Test Infrastructure (Framework jest 29.7.0 / ts-jest 29.3.2; config `jest.config.js`;
    quick run `npx jest src/backend/humble/__tests__ --no-coverage`; full suite `pnpm test`). Fill the Per-Task
    Verification Map rows from the RESEARCH Validation Architecture "Phase Requirements → Test Map" using the actual
    files that now exist (classify.test.ts, library.test.ts, user.test.ts), each mapped to its HSYNC-0x requirement,
    threat ref (T-11-0x), and automated command; mark them ✅ green after the run. Fill the Manual-Only Verifications
    table with the fail-soft banner / freshness indicator / progressive-fill visual checks (why manual: no headless DOM
    infra for this screen). Complete the Wave 0 Requirements + Validation Sign-Off checklists. Set frontmatter
    `nyquist_compliant: true` and `wave_0_complete: true`. Run `pnpm test` and record the pass result.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
    11-VALIDATION.md is fully populated with the real requirement→test map, all sign-off boxes checked, `nyquist_compliant: true`; the full jest suite passes.
  </done>
  <acceptance_criteria>
    - `11-VALIDATION.md` frontmatter contains `nyquist_compliant: true` and `wave_0_complete: true`
    - The Per-Task Verification Map references `classify.test.ts`, `library.test.ts`, and `user.test.ts` with HSYNC-01..04 mapped
    - No `{placeholder}` tokens remain in the file (grep: `grep -c "{.*}" .planning/phases/11-library-sync-5-state-key-model/11-VALIDATION.md == 0`)
    - `pnpm test` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-account UAT + live-API [ASSUMED] resolution</name>
  <files>.planning/phases/11-library-sync-5-state-key-model/11-VALIDATION.md</files>
  <action>
    Human-verify checkpoint (no source-code changes). Drive the running dev build against a real Humble account per
    <how-to-verify>, then append the redacted live-account findings (A1 UNPICKED shape, A3 redeemed_key_value shape,
    fail-soft behavior) to 11-VALIDATION.md as a "Live UAT findings" section. Counts / states / pass-fail only — never
    a cookie or a raw key value.
  </action>
  <what-built>
    A read-only "Humble Keys" sidebar page that syncs a connected Humble account's full key inventory,
    classifies each key into one of five states (UNPICKED / UNREVEALED / REVEALED / REDEEMED / UNREDEEMABLE),
    caches it, fails soft on an unreachable API, and always shows sync freshness. All classification/sync/fail-soft
    logic is covered by passing unit tests (Task 1); this checkpoint confirms it against real live data.
  </what-built>
  <how-to-verify>
    On a machine with a real Humble account (dev build, `pnpm dev`):
    1. Connect the Humble account (Manage Accounts) if not already connected. The "Humble Keys" entry appears in the
       sidebar only while connected — confirm it appears, and confirm it is hidden after Disconnect.
    2. Open Humble Keys. Confirm keys appear grouped by state in the fixed order (UNPICKED, UNREVEALED, REVEALED,
       REDEEMED, UNREDEEMABLE), empty groups hidden, and within each group expiring-soonest first. Confirm a
       "Last synced X ago" line is shown.
    3. Confirm rows are strictly read-only — no reveal/copy/expand/link-out affordance, no clickable/hover behavior.
    4. Click the manual refresh button — confirm it spins during sync and the "Syncing… N/M orders" progressive-fill
       indicator appears for a multi-order account; keys fill in as orders resolve.
    5. Fail-soft: disconnect the network (or point the app at an unreachable host) and refresh — confirm the cached
       list stays visible with the orange "Couldn't refresh — showing data from {time}" banner (no toast, no blank/error).
    6. Live [ASSUMED] resolution — report for the VALIDATION addendum (structure only, NEVER paste a raw key value):
       (A3) Is there at least one REDEEMED key, and does it classify green/REDEEMED? Is any UNREVEALED key present and
       teal? (A1) Does the account currently have an un-picked Humble Choice month, and if so does the grey UNPICKED
       pseudo-entry render (with or without a pick deadline)? If no un-picked month is present, note the UNPICKED path
       as "unverified on live data — defensive code path only" (same as Phase 10's identity-advisory precedent).
    Then append these findings (redacted — counts/states/pass-fail only, no cookie, no key value) to 11-VALIDATION.md.
  </how-to-verify>
  <verify>
    <human-check>Human confirms 5-state inventory renders, rows are read-only, progressive fill works, and the fail-soft banner appears on an unreachable API; A1/A3 findings appended to 11-VALIDATION.md.</human-check>
  </verify>
  <done>
    5-state inventory renders correctly on a real account, rows are read-only, progressive fill + fail-soft banner confirmed, and the redacted live-API findings (A1/A3) are recorded in 11-VALIDATION.md.
  </done>
  <resume-signal>Type "approved" once the 5-state inventory, read-only rows, progressive fill, and fail-soft banner are confirmed and the A1/A3 findings are recorded — or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Humble API → app | Real-account data exercises the full sync + classification path |
| human report → VALIDATION.md | Findings must be redacted before transcription |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Information Disclosure | UAT findings transcription | mitigate | VALIDATION addendum records counts/states/pass-fail only — never a cookie or raw key value (D-15 redaction discipline) |
| T-11-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `pnpm test` exits 0
- 11-VALIDATION.md is fully populated (nyquist_compliant: true) with the live-account addendum
- Human confirms 5-state rendering, read-only rows, progressive fill, and fail-soft banner on a real account
</verification>

<success_criteria>
Phase 11's four requirements are validated: automated unit coverage is green and recorded, and a
real Humble account renders the complete 5-state inventory with working fail-soft behavior — with
the two [ASSUMED] live-API items resolved or explicitly flagged as unverified-defensive.
</success_criteria>

<output>
Create `.planning/phases/11-library-sync-5-state-key-model/11-05-SUMMARY.md` when done
</output>
