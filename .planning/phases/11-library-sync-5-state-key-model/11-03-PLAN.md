---
phase: 11-library-sync-5-state-key-model
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/backend/humble/ipc_handler.ts
  - src/preload/api/humble.ts
  - src/frontend/state/GlobalState.tsx
  - src/frontend/state/ContextProvider.tsx
  - src/frontend/types.ts
autonomous: true
requirements: [HSYNC-01, HSYNC-04]

must_haves:
  truths:
    - "humbleSync / humbleGetKeys / humbleGetSyncState are registered as typed IPC handlers"
    - "The renderer reaches keys/sync-state ONLY through dedicated handlers, never the generic storeGet bridge"
    - "The humble context slice carries keys, syncedAt, syncError, syncing"
    - "Startup sync runs only AFTER the health check confirms a non-expired session"
    - "A successful login/reconnect triggers a sync (D-23)"
  artifacts:
    - path: "src/preload/api/humble.ts"
      provides: "humbleSync/humbleGetKeys/humbleGetSyncState invokers + handleHumbleKeysUpdated/handleHumbleSyncProgress listeners"
      contains: "humbleGetKeys"
    - path: "src/frontend/state/GlobalState.tsx"
      provides: "humble slice keys/syncedAt/syncError/syncing + IPC listeners + startup/login sync triggers"
      contains: "handleHumbleKeysUpdated"
  key_links:
    - from: "src/backend/humble/ipc_handler.ts"
      to: "src/backend/humble/library.ts (HumbleLibrary)"
      via: "addHandler delegation"
      pattern: "HumbleLibrary"
    - from: "src/frontend/state/GlobalState.tsx"
      to: "window.api.humbleCheckHealth().then(humbleSync)"
      via: "chained startup trigger after health check"
      pattern: "humbleSync"
---

<objective>
Expose the sync backend to the renderer: register the typed `humble:*` IPC handlers, extend the
preload bridge, and wire the frontend `humble` context slice + its startup/login sync triggers.

Purpose: HSYNC-01 (keys + sync state reach the renderer through a typed, display-safe boundary —
never the generic store bridge, per WR-09) and HSYNC-04's data path (syncError/syncedAt available
to the UI banner + freshness indicator built in Plan 04). No screen yet — this is pure wiring.
Output: Extended `ipc_handler.ts`, `preload/api/humble.ts`, `GlobalState.tsx`, `ContextProvider.tsx`, `types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-library-sync-5-state-key-model/11-CONTEXT.md
@.planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md
@.planning/phases/11-library-sync-5-state-key-model/11-02-SUMMARY.md

<interfaces>
<!-- Existing wiring patterns to mirror. Use directly, do not explore. -->

From src/backend/humble/ipc_handler.ts (registerHumbleIpcHandlers — extend in place):
```typescript
addHandler('humbleCheckHealth', () => HumbleUser.checkHealthAndFlagExpiry())
addListener('humbleDisconnect', () => { HumbleUser.disconnect().catch(...) })
```

From src/backend/humble/library.ts (Plan 02): `HumbleLibrary.sync()`, `HumbleLibrary.getKeys()`, `HumbleLibrary.getSyncState()`.

From src/preload/api/humble.ts (existing — extend):
```typescript
export const humbleCheckHealth = makeHandlerInvoker('humbleCheckHealth')
export const handleHumbleAuthState = frontendListenerSlot('humbleAuthState')
```

From src/frontend/state/GlobalState.tsx:
- StateProps.humble (lines 84-90): `{ isLoggedIn?, username?, expired?, encryptionDegraded? }`
- init (lines 232-246): `humble: { isLoggedIn: humbleConfigStore.get_nodefault('isLoggedIn'), ... }`
- IPC listener (lines 1045-1054): `window.api.handleHumbleAuthState((e, humbleState) => this.setState(...))`
- startup (lines 1062-1063): `if (this.state.humble.isLoggedIn) { void window.api.humbleCheckHealth() }`
- humbleLogin (lines 738-...): sets logged-in state after login/reconnect
- provided value (lines 1316-1323): `humble: { isLoggedIn, username, expired, encryptionDegraded, login, logout }`

From src/frontend/types.ts (ContextType.humble lines 106-113) and src/frontend/state/ContextProvider.tsx (default humble lines 39-42).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register humble sync IPC handlers + extend preload bridge</name>
  <files>src/backend/humble/ipc_handler.ts, src/preload/api/humble.ts</files>
  <read_first>
    - src/backend/humble/ipc_handler.ts (full file — registerHumbleIpcHandlers pattern to extend)
    - src/backend/humble/library.ts (HumbleLibrary API — Plan 02)
    - src/preload/api/humble.ts (full file — makeHandlerInvoker / frontendListenerSlot pattern)
    - src/preload/api/steam.ts (getSteamSyncedAt invoker precedent)
    - .planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md (ipc_handler section, WR-09 anti-pattern)
  </read_first>
  <action>
    In `src/backend/humble/ipc_handler.ts` add to `registerHumbleIpcHandlers()`, mirroring the existing `addHandler`
    delegation style: `addHandler('humbleSync', async () => HumbleLibrary.sync())`,
    `addHandler('humbleGetKeys', () => HumbleLibrary.getKeys())`,
    `addHandler('humbleGetSyncState', () => HumbleLibrary.getSyncState())`. Import `HumbleLibrary` from `./library`.
    These return only the display-safe HumbleKey[]/HumbleSyncState projections (Plan 01/02 already exclude raw key
    values) — do NOT register humbleLibraryStore/humbleRevealedStore on any generic frontend `storeGet` bridge (WR-09).
    In `src/preload/api/humble.ts` add `export const humbleSync = makeHandlerInvoker('humbleSync')`,
    `export const humbleGetKeys = makeHandlerInvoker('humbleGetKeys')`,
    `export const humbleGetSyncState = makeHandlerInvoker('humbleGetSyncState')`,
    `export const handleHumbleKeysUpdated = frontendListenerSlot('humbleKeysUpdated')`, and
    `export const handleHumbleSyncProgress = frontendListenerSlot('humbleSyncProgress')`.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <done>
    Three new handlers registered against HumbleLibrary; five new preload exports exist; no generic store bridge exposure; codecheck clean.
  </done>
  <acceptance_criteria>
    - `src/backend/humble/ipc_handler.ts` contains `addHandler('humbleSync'`, `addHandler('humbleGetKeys'`, `addHandler('humbleGetSyncState'`
    - `src/preload/api/humble.ts` contains `humbleSync`, `humbleGetKeys`, `humbleGetSyncState`, `handleHumbleKeysUpdated`, `handleHumbleSyncProgress`
    - No `storeGet`/generic-store registration of humbleLibraryStore or humbleRevealedStore is added (grep: `grep -rn "humbleLibraryStore\|humbleRevealedStore" src/preload | grep -v '^#' | wc -l == 0`)
    - `npm run codecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend the humble context slice + startup/login sync triggers</name>
  <files>src/frontend/state/GlobalState.tsx, src/frontend/state/ContextProvider.tsx, src/frontend/types.ts</files>
  <read_first>
    - src/frontend/state/GlobalState.tsx (StateProps.humble 84-90, init 232-246, handleHumbleAuthState listener 1045-1054, startup 1062-1063, humbleLogin 738+, provided value 1316-1323)
    - src/frontend/state/ContextProvider.tsx (default humble 39-42)
    - src/frontend/types.ts (ContextType.humble 106-113)
    - src/preload/api/humble.ts (new invokers/listeners — Task 1)
    - .planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md (GlobalState/ContextProvider slice section, startup-trigger chain)
  </read_first>
  <action>
    In `src/frontend/state/GlobalState.tsx` StateProps.humble, add `keys?: HumbleKey[]`, `syncedAt?: number | null`,
    `syncError?: HumbleSyncState['syncError']`, `syncing?: boolean` (import the types from `common/types/humble`).
    In the state initializer, seed these to `[]`, `null`, `'none'`, `false` (NOT read from humbleConfigStore — the
    new data lives in the humbleLibrary/humbleSync stores and arrives via IPC). Add two IPC listeners next to the
    existing `handleHumbleAuthState` listener: `window.api.handleHumbleKeysUpdated((e, keys) => this.setState(prev => ({ humble: { ...prev.humble, keys, syncing: false } })))` and
    `window.api.handleHumbleSyncProgress((e, { done, total }) => this.setState(prev => ({ humble: { ...prev.humble, syncing: done < total } })))`.
    In the startup block, chain the sync after the health check so an already-expired session never syncs:
    `if (this.state.humble.isLoggedIn) { void window.api.humbleCheckHealth().then(() => window.api.humbleSync()) }`.
    Also fetch initial cached keys + sync state on mount via `window.api.humbleGetKeys()` and `window.api.humbleGetSyncState()`,
    setting them into state (so the cache renders before the first sync completes — cache-then-sync). In `humbleLogin`
    (the post-login/reconnect handler), after the logged-in state is set, trigger `void window.api.humbleSync()` (D-23).
    Extend the provided `humble` value (lines 1316-1323) to include `keys`, `syncedAt`, `syncError`, `syncing`.
    In `src/frontend/types.ts` ContextType.humble, add `keys?: HumbleKey[]`, `syncedAt?: number | null`,
    `syncError?: 'none' | 'denied' | 'network' | 'partial'`, `syncing?: boolean` (import HumbleKey). In
    `src/frontend/state/ContextProvider.tsx` default humble object, add `keys: [], syncedAt: null, syncError: 'none', syncing: false`.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <done>
    The humble slice carries keys/syncedAt/syncError/syncing end to end (StateProps → init → listeners → provided value → ContextType → default); startup sync is chained after the health check; login triggers a sync; codecheck clean.
  </done>
  <acceptance_criteria>
    - `src/frontend/state/GlobalState.tsx` contains `handleHumbleKeysUpdated` and `handleHumbleSyncProgress` listeners
    - `GlobalState.tsx` startup calls `humbleCheckHealth().then(` chaining `humbleSync` (grep: `grep -c "humbleCheckHealth().then" src/frontend/state/GlobalState.tsx >= 1`)
    - `src/frontend/types.ts` ContextType.humble contains `syncError` and `syncing`
    - `src/frontend/state/ContextProvider.tsx` default humble contains `syncError: 'none'`
    - `npm run codecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (IPC) | Keys + sync state cross the process boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Information Disclosure | IPC surface for cache/revealed stores | mitigate | Expose only via dedicated typed handlers returning display-safe HumbleKey[]/HumbleSyncState; do NOT register the stores on the generic frontend storeGet bridge (WR-09) |
| T-11-01 | Information Disclosure | humbleGetKeys payload | mitigate | HumbleKey (Plan 01) carries no raw key value — projection is display-safe by construction |
| T-11-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npm run codecheck` exits 0
- Sync IPC handlers delegate to HumbleLibrary; preload bridge exposes them; context slice carries the four new fields
</verification>

<success_criteria>
The renderer receives keys + sync state through a typed, display-safe IPC boundary; startup sync
runs only after a healthy session is confirmed; login/reconnect triggers a sync; the cache renders
before the first sync completes.
</success_criteria>

<output>
Create `.planning/phases/11-library-sync-5-state-key-model/11-03-SUMMARY.md` when done
</output>
