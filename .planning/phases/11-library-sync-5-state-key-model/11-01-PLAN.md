---
phase: 11-library-sync-5-state-key-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/types/humble.ts
  - src/common/types/ipc.ts
  - src/backend/humble/electronStores.ts
  - src/backend/humble/constants.ts
  - src/backend/humble/classify.ts
  - src/backend/humble/adapter.ts
  - src/backend/humble/__tests__/classify.test.ts
  - src/backend/humble/__tests__/adapter.test.ts
  - src/backend/humble/__tests__/fixtures/tpks.ts
autonomous: true
requirements: [HSYNC-01, HSYNC-02, HSYNC-03, HSYNC-04]

must_haves:
  truths:
    - "classifyTpk returns exactly one of UNPICKED/UNREVEALED/REVEALED/REDEEMED/UNREDEEMABLE for any input"
    - "Expiration in the past classifies UNREDEEMABLE regardless of redeemed_key_value or local flag"
    - "A key with the local REVEALED flag and no redeemed_key_value classifies REVEALED"
    - "Three separate electron-store instances exist: library cache, sync-state, revealed-flag"
    - "HTTP 429 from Humble maps to AdapterResult access_denied so the first 429 aborts the sync into the same backoff path as 403 (D-25)"
  artifacts:
    - path: "src/backend/humble/classify.ts"
      provides: "Pure 5-state classification (classifyTpk + classifyOrder)"
      exports: ["classifyTpk", "classifyOrder"]
    - path: "src/backend/humble/electronStores.ts"
      provides: "humbleLibraryStore, humbleSyncStore, humbleRevealedStore"
      contains: "humbleRevealedStore"
    - path: "src/common/types/humble.ts"
      provides: "HumbleKeyState, HumbleKey, HumbleOrderCacheEntry, HumbleSyncState"
      contains: "HumbleKeyState"
  key_links:
    - from: "src/backend/humble/classify.ts"
      to: "src/backend/humble/electronStores.ts (humbleRevealedStore)"
      via: "classifyOrder reads the revealed-flag store (passed as argument, not imported)"
      pattern: "classifyOrder"
    - from: "src/backend/humble/adapter.ts (mapAxiosError)"
      to: "src/backend/humble/library.ts (sync abort/backoff path — Plan 02)"
      via: "HTTP 429 mapped to access_denied so 429 routes through the existing abort+cooldown branch (D-25/D-33)"
      pattern: "access_denied"
---

<objective>
Establish the Phase 11 type contracts, the three-store persistence split, and the pure 5-state
classification function — the interface + business-logic foundation every downstream plan builds on.

Purpose: Define the data model (HumbleKey / HumbleKeyState / cache-entry / sync-state), create the
two-store REVEALED-vs-cache split that HSYNC-02 depends on (survives disconnect), implement the
D-30 classification precedence as a pure, unit-tested function that needs zero network or store mocking,
and close the adapter's error-mapping gap so HTTP 429 (rate-limit) surfaces as a typed access_denied
result — the fail-soft foundation HSYNC-04 / D-25 depend on (network/timeout/5xx stay thrown by design
and are caught in Plan 02's library.ts, mirroring the existing user.ts health-check pattern).
Output: Extended `common/types/humble.ts` + `common/types/ipc.ts`, three new CacheStore instances,
new `classify.ts` with full test coverage, tightened `OrderDetailSchema`, and a 429→access_denied
adapter mapping with its test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-library-sync-5-state-key-model/11-CONTEXT.md
@.planning/phases/11-library-sync-5-state-key-model/11-RESEARCH.md
@.planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor must extend, not replace. Extracted from codebase. -->

From src/common/types/humble.ts (existing — extend, keep style):
```typescript
export type AdapterResult<T> =
  | { status: 'ok'; data: T }
  | { status: 'session_expired' }
  | { status: 'access_denied' }
  | { status: 'schema_error'; raw: unknown }

export interface HumbleUserData { username: string }
export interface HumbleAuthState { isLoggedIn: boolean; username?: string; expired?: boolean }
```

From src/backend/humble/adapter.ts (existing OrderDetailSchema — tighten all_tpks element, keep .passthrough() at every level):
```typescript
const OrderDetailSchema = z.object({
  gamekey: z.string().optional(),
  tpkd_dict: z.object({ all_tpks: z.array(z.unknown()).optional() }).passthrough().optional()
}).passthrough()
```

From src/backend/humble/adapter.ts (existing mapAxiosError — extend the 401/403 split with a 429 case, keep the rethrow tail):
```typescript
function mapAxiosError<T>(err: unknown): AdapterResult<T> {
  if (axios.isAxiosError(err)) {
    if (err.response?.status === 401) return { status: 'session_expired' }
    if (err.response?.status === 403) return { status: 'access_denied' }
  }
  logError([...], LogPrefix.Backend)
  throw err // network / timeout / 5xx stay thrown — Phase 10 contract, callers catch
}
```

From src/backend/humble/electronStores.ts (existing — full file, extend in place):
```typescript
import { TypeCheckedStoreBackend } from '../electron_store'
const configStore = new TypeCheckedStoreBackend('humbleConfigStore', { cwd: 'humble_store' })
export { configStore }
```

From src/backend/cache.ts (CacheStore API to use): `new CacheStore<ValueType, KeyType>(filename, max_value_lifespan)`;
methods `get(key, fallback?)`, `set(key, value)`, `delete(key)`, `has(key)`, `clear()`,
`entries(): [key, value][]` (excludes internal `__timestamp.*`). Pass `null` for indefinite lifespan.

From src/backend/storeManagers/steam/electronStores.ts (CacheStore instantiation precedent):
```typescript
const steamLibraryStore = new CacheStore<GameInfo[], 'games'>('steam_library', null)
const steamSyncStore = new CacheStore<number, 'syncedAt'>('steam_sync', null)
```

From src/common/types/ipc.ts (AsyncIPCFunctions humble block ~line 247, and FrontendMessages ~line 468):
```typescript
humbleCheckHealth: () => Promise<void>
humbleGetUserInfo: () => Promise<HumbleUserData | undefined>
// FrontendMessages:
humbleAuthState: (state: HumbleAuthState) => void
```
Import block (~line 55): `import type { HumbleAuthState, HumbleUserData, HumbleValidationReport } from './humble'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Humble type contracts, IPC types, stores, and constants</name>
  <files>src/common/types/humble.ts, src/common/types/ipc.ts, src/backend/humble/electronStores.ts, src/backend/humble/constants.ts</files>
  <read_first>
    - src/common/types/humble.ts (existing shape/JSDoc style to continue)
    - src/common/types/ipc.ts (lines 50-62 import block, 247-266 AsyncIPCFunctions humble block, 424-468 FrontendMessages)
    - src/backend/humble/electronStores.ts (full 9-line file to extend)
    - src/backend/humble/constants.ts (full file to extend)
    - src/backend/storeManagers/steam/electronStores.ts (CacheStore instantiation + SteamMetadataCacheEntry shape precedent)
    - src/backend/cache.ts (CacheStore API)
    - .planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md (electronStores + types sections)
  </read_first>
  <action>
    In `src/common/types/humble.ts` add (matching the existing string-literal-union + JSDoc-per-type style):
    `HumbleKeyState` = union of the five literals `'UNPICKED' | 'UNREVEALED' | 'REVEALED' | 'REDEEMED' | 'UNREDEEMABLE'`;
    `HumbleKey` = the display-safe per-row shape with fields `gamekey: string`, `machineName: string` (stable per-tpk identity for the revealed-flag key and dedupe), `state: HumbleKeyState`, `title: string`, `platform: string`, `expiration: string | null`, `origin: string` (bundle/order label) — and NO raw key value field (never store `redeemed_key_value`; only its presence is derived);
    `HumbleOrderCacheEntry` = the `humbleLibraryStore` value shape: `{ gamekey: string; keys: HumbleKey[]; allTerminal: boolean }` (required `allTerminal` boolean per Pitfall 3 explicit-membership partitioning — this is a new store with no migration concern);
    `HumbleSyncState` = `{ syncedAt: number | null; syncError: 'none' | 'denied' | 'network' | 'partial'; cooldownUntil?: number }` (the `'network'` value is produced by Plan 02's library.ts catch path for a transient network/timeout/5xx throw; `'denied'` by the access_denied/429 abort path).
    In `src/common/types/ipc.ts`: add to the import block `HumbleKey, HumbleSyncState`; in `AsyncIPCFunctions` humble block add `humbleSync: () => Promise<{ status: 'ok' | 'partial' | 'failed' }>`, `humbleGetKeys: () => Promise<HumbleKey[]>`, `humbleGetSyncState: () => Promise<HumbleSyncState>`; in `FrontendMessages` add `humbleKeysUpdated: (keys: HumbleKey[]) => void` and `humbleSyncProgress: (progress: { done: number; total: number }) => void`, each with a one-line JSDoc naming what fires it and the redaction constraint (no cookie, no raw key value).
    In `src/backend/humble/electronStores.ts`: add `import CacheStore from '../cache'`, instantiate `humbleLibraryStore = new CacheStore<HumbleOrderCacheEntry, string>('humble_library', null)`, `humbleSyncStore = new CacheStore<number, 'syncedAt'>('humble_sync', null)`, and `humbleRevealedStore = new CacheStore<{ revealedAt: number }, string>('humble_revealed', null)`; add the D-04/D-30 comment above `humbleRevealedStore` stating it is NEVER cleared by disconnect (separate file on disk); export all four stores. Replace the existing "Do NOT add humbleLibraryStore … Phase 11 scope" comment since that scope is now active.
    In `src/backend/humble/constants.ts`: add `HUMBLE_SYNC_CONCURRENCY = 3` (D-25 pool bound) and `HUMBLE_COOLDOWN_MS = 15 * 60 * 1000` (D-33 403/429 cooldown, Claude's discretion duration) with explaining comments.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <done>
    All four files compile; the four new types are exported from humble.ts; the three new stores are exported from electronStores.ts; the two constants exist.
  </done>
  <acceptance_criteria>
    - `src/common/types/humble.ts` contains `export type HumbleKeyState`, `export interface HumbleKey`, `export interface HumbleOrderCacheEntry`, `export interface HumbleSyncState`
    - `HumbleKey` has NO field named `redeemed_key_value` or `key` holding the raw key string (grep: `grep -v '^#' src/common/types/humble.ts | grep -c 'redeemed_key_value' == 0`)
    - `src/backend/humble/electronStores.ts` exports `humbleLibraryStore`, `humbleSyncStore`, `humbleRevealedStore`
    - `src/common/types/ipc.ts` contains `humbleSync`, `humbleGetKeys`, `humbleGetSyncState`, `humbleKeysUpdated`, `humbleSyncProgress`
    - `src/backend/humble/constants.ts` contains `HUMBLE_SYNC_CONCURRENCY` and `HUMBLE_COOLDOWN_MS`
    - `npm run codecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure 5-state classification function + tightened OrderDetailSchema + 429 adapter mapping</name>
  <files>src/backend/humble/classify.ts, src/backend/humble/adapter.ts, src/backend/humble/__tests__/classify.test.ts, src/backend/humble/__tests__/adapter.test.ts, src/backend/humble/__tests__/fixtures/tpks.ts</files>
  <read_first>
    - src/backend/humble/adapter.ts (OrderDetailSchema lines 27-49, mapAxiosError lines 107-119 — the 401/403 split + rethrow tail, describeSchemaFailure pattern, AdapterResult usage)
    - src/backend/humble/__tests__/adapter.test.ts (jest mock/structure conventions; note the existing `makeAxiosError(status)` helper and the intentional "non-axios / unmapped-status error rethrows" test for 500 + the WR-04 timeout-rethrows test — those TWO must stay green, only 429 behavior is added)
    - src/common/types/humble.ts (HumbleKeyState / HumbleKey / HumbleOrderCacheEntry added in Task 1)
    - .planning/phases/11-library-sync-5-state-key-model/11-RESEARCH.md (Pattern 4 classification precedence, Pitfall 5 stale-recompute, Open Question 2 redeemed_key_value nullish, Assumptions A1-A4)
    - .planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md (classify.ts section)
  </read_first>
  <behavior>
    - classifyTpk: expiration date in the past ⇒ 'UNREDEEMABLE' (beats every other signal)
    - classifyTpk: redeemedKeyValuePresent true, no past expiration ⇒ 'REDEEMED' (beats local flag)
    - classifyTpk: isLocallyRevealed true, not redeemed, not expired ⇒ 'REVEALED'
    - classifyTpk: none of the above ⇒ 'UNREVEALED' (default)
    - classifyOrder: a product with category 'subscriptioncontent' + choice_url and no allocated tpks ⇒ single UNPICKED pseudo-entry (no key), never throws if choice_url/deadline absent — omits the pseudo-entry rather than crashing
    - classifyOrder: reads the local REVEALED flag from an injected revealed-flag lookup (argument), never merges the previous library-cache record forward (expiration/redeemed presence always taken from the fresh input, Pitfall 5)
    - classifyOrder: a malformed/partial tpk entry never throws out of the loop — it is skipped, other tpks in the same order still classify
    - HumbleOrderCacheEntry.allTerminal is true iff every classified key is REDEEMED or UNREDEEMABLE (and false for an UNPICKED pseudo-entry)
    - adapter.mapAxiosError: HTTP 429 ⇒ AdapterResult `access_denied` (D-25 groups 429 with 403 as a backoff-inducing denial → same abort+cooldown path in library.ts). HTTP 5xx, timeouts (ECONNABORTED / no response), and non-axios errors continue to THROW unchanged — that transient class is Phase 10's deliberate contract and is caught downstream in Plan 02's library.ts; do NOT convert it to a typed result here.
  </behavior>
  <action>
    Tighten `OrderDetailSchema` in `adapter.ts`: replace `all_tpks: z.array(z.unknown())` with an element schema
    that adds the fields classification consumes — `redeemed_key_value: z.string().nullish()` (nullish per Open
    Question 2 — treat any falsy value as absent), `expiration: z.string().nullish()` (per-tpk if present; classify
    reads whatever granularity the API supplies), `key_type: z.string().nullish()` (platform label source),
    `human_name: z.string().nullish()`, `machine_name: z.string().nullish()` — and keep `.passthrough()` at every
    object level so a shape drift never fails the whole order. Add `product` fields to the top-level schema for the
    UNPICKED branch: `product: z.object({ category: z.string().nullish(), choice_url: z.string().nullish(), human_name: z.string().nullish() }).passthrough().nullish()`. Export the inferred element type.
    In `adapter.ts` `mapAxiosError`, add ONE branch immediately after the 403 line: `if (err.response?.status === 429) return { status: 'access_denied' }`, with a comment: "D-25: Humble rate-limit (429) is a backoff-inducing denial — routed through the same access_denied abort+cooldown path as 403; never hammered (C5)." Leave the rethrow tail exactly as-is so 5xx/timeout/network keep throwing (Phase 10 contract; library.ts owns catching them). This is the ONLY mapAxiosError change — do not add a network_error variant, do not stop rethrowing transient errors.
    Create `src/backend/humble/classify.ts`: implement `classifyTpk(tpk, isLocallyRevealed, now?): HumbleKeyState`
    with the literal precedence from RESEARCH Pattern 4 (expiration→UNREDEEMABLE, redeemedKeyValuePresent→REDEEMED,
    isLocallyRevealed→REVEALED, else UNREVEALED). Add a code comment referencing Open Question 3 explaining the
    expiration-beats-redeemed rule is intentional per D-30 and must not be "fixed" by reordering.
    Implement `classifyOrder(rawOrder, isRevealed): HumbleOrderCacheEntry` — a pure function taking the fresh
    parsed OrderDetail plus an `isRevealed: (machineName: string) => boolean` lookup (injected, so no electron-store
    import inside this module); derive `redeemedKeyValuePresent` from the presence of a truthy `redeemed_key_value`;
    map platform label from `key_type`; detect the UNPICKED pseudo-entry per D-27 defensively; compute `allTerminal`.
    No I/O, no logging of raw tpk/key values.
    Create `src/backend/humble/__tests__/fixtures/tpks.ts`: one representative raw tpk/order fixture per state
    (UNPICKED subscriptioncontent product, UNREVEALED, REVEALED-via-flag, REDEEMED-via-redeemed_key_value,
    UNREDEEMABLE-via-past-expiration) plus a malformed-tpk fixture and a re-sync fixture (same tpk gaining an
    expiration on the second call).
    Create `src/backend/humble/__tests__/classify.test.ts` covering every classify bullet in <behavior>, including the
    HSYNC-03 reclassification case (cached without expiration → re-classified with a newly-added expiration returns
    UNREDEEMABLE) and the HSYNC-02 revealed-survives case (flag set + no redeemed value → REVEALED).
    Extend `src/backend/humble/__tests__/adapter.test.ts` (do NOT delete the existing 500-rethrows or WR-04
    timeout-rethrows tests): add a case using the existing `makeAxiosError(429)` helper asserting
    `getGamekeys(COOKIE)` resolves to `{ status: 'access_denied' }` (and one for `getOrderDetail`), proving 429 is
    now a typed abort signal rather than a thrown rejection.
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/classify.test.ts src/backend/humble/__tests__/adapter.test.ts --no-coverage</automated>
  </verify>
  <done>
    classify.ts exports classifyTpk + classifyOrder as pure functions; all classify.test.ts cases pass; OrderDetailSchema is tightened but still passthrough at every level; adapter maps 429→access_denied while 5xx/timeout still rethrow (existing tests green); `npm run codecheck` is clean.
  </done>
  <acceptance_criteria>
    - `src/backend/humble/classify.ts` contains `export function classifyTpk(` and `export function classifyOrder(`
    - `classify.ts` does NOT import electron-store / any store module (grep: `grep -v '^#' src/backend/humble/classify.ts | grep -c "electronStores" == 0`)
    - `npx jest src/backend/humble/__tests__/classify.test.ts --no-coverage` exits 0 with a test asserting past-expiration → UNREDEEMABLE even when redeemedKeyValuePresent is true
    - A test asserts the revealed-flag + no-redeemed-value case returns REVEALED
    - A test asserts a re-classified order with a newly-added expiration returns UNREDEEMABLE (HSYNC-03)
    - `adapter.ts` OrderDetailSchema element contains `redeemed_key_value: z.string().nullish()` and retains `.passthrough()`
    - `adapter.ts` mapAxiosError maps HTTP 429 → `access_denied`; a test asserts `getGamekeys` resolves `{ status: 'access_denied' }` on a 429, and the pre-existing 500-rethrows + WR-04 timeout-rethrows tests still pass
    - `npm run codecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Humble API → adapter | Untrusted JSON response crosses into classification logic |
| classification → cache store | Derived data persisted to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | classify.ts / HumbleOrderCacheEntry | mitigate | Store only a derived `redeemedKeyValuePresent` boolean; `HumbleKey` has no raw key-value field (C4) |
| T-11-04 | Information Disclosure | classify.ts logging | mitigate | classify.ts performs no logging of raw tpk/order objects; only structural data ever reaches a logger elsewhere |
| T-11-05 | Tampering / DoS | classifyOrder malformed input | mitigate | Per-tpk try/skip — one malformed tpk never throws out of the loop; `.passthrough()` zod keeps shape drift non-fatal |
| T-11-07 | Denial of Service | adapter 429 handling | mitigate | HTTP 429 mapped to access_denied so the first rate-limit aborts the sync into the backoff+cooldown path (D-25/D-33) — a Humble rate-limit is never hammered (C5) |
| T-11-SC | Tampering | npm installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) — no install surface |
</threat_model>

<verification>
- `npm run codecheck` exits 0
- `npx jest src/backend/humble/__tests__/classify.test.ts src/backend/humble/__tests__/adapter.test.ts --no-coverage` exits 0
- Three CacheStore instances exist and are exported from electronStores.ts
- adapter maps 429→access_denied; 500 + timeout still rethrow (existing Phase 10 tests stay green)
</verification>

<success_criteria>
The 5-state classification precedence (D-30) is implemented as a pure, fully-tested function; the
two-store REVEALED-vs-cache split (HSYNC-02 foundation) exists; the adapter surfaces HTTP 429 as a
typed access_denied abort signal (D-25) while leaving the transient-error rethrow contract intact for
library.ts to catch (HSYNC-04); all Phase 11 type + IPC contracts are defined for downstream plans to consume.
</success_criteria>

<output>
Create `.planning/phases/11-library-sync-5-state-key-model/11-01-SUMMARY.md` when done
</output>
