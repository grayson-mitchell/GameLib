---
phase: 11-library-sync-5-state-key-model
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/backend/humble/library.ts
  - src/backend/humble/user.ts
  - src/backend/humble/__tests__/library.test.ts
  - src/backend/humble/__tests__/user.test.ts
autonomous: true
requirements: [HSYNC-01, HSYNC-02, HSYNC-03, HSYNC-04]

must_haves:
  truths:
    - "sync() partitions gamekeys into new / non-terminal / frozen and fetches only the first two"
    - "A never-cached gamekey is always fetched; an all-terminal cached order is skipped"
    - "The order-detail fetch pool never exceeds HUMBLE_SYNC_CONCURRENCY in flight and aborts the whole sync on the first access_denied"
    - "access_denied/schema_error on getGamekeys leaves the cache untouched and sets syncError + cooldown"
    - "A 403 mid-sync commits already-fetched orders and leaves the rest at prior cache (D-34)"
    - "session_expired bails out without setting syncError (Phase 10 owns expiry)"
    - "disconnect() clears humbleLibraryStore + humbleSyncStore but NOT humbleRevealedStore"
  artifacts:
    - path: "src/backend/humble/library.ts"
      provides: "sync orchestration, cache-then-sync loadCached, fail-soft, getKeys/getSyncState"
      exports: ["HumbleLibrary"]
    - path: "src/backend/humble/user.ts"
      provides: "disconnect() extended to clear cache/sync stores, preserve revealed store"
      contains: "humbleLibraryStore.clear"
  key_links:
    - from: "src/backend/humble/library.ts"
      to: "src/backend/humble/classify.ts (classifyOrder)"
      via: "per-order classification of fresh adapter responses"
      pattern: "classifyOrder"
    - from: "src/backend/humble/library.ts"
      to: "src/backend/humble/adapter.ts (getGamekeys, getOrderDetail)"
      via: "typed AdapterResult status switch"
      pattern: "getOrderDetail"
---

<objective>
Build the sync orchestration layer: gamekeys fetch, skip-terminal partitioning, bounded-concurrency
order-detail fan-out, per-order classification + commit, and the fail-soft path — plus extend
disconnect() to honor the D-04/D-30 store-survival split.

Purpose: HSYNC-01 (concurrency-bounded cache-aggressive sync), HSYNC-03 (skip-terminal re-fetch so
expirations are recomputed), HSYNC-04 (fail-soft: keep cache + syncError, never blank/error), and
HSYNC-02's disconnect-survival half (the REVEALED-flag store outlives the library cache).
Output: New `library.ts` with `HumbleLibrary` (loadCached/sync/getKeys/getSyncState), extended
`disconnect()`, full test coverage for partitioning/concurrency/fail-soft/store-survival.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-library-sync-5-state-key-model/11-CONTEXT.md
@.planning/phases/11-library-sync-5-state-key-model/11-RESEARCH.md
@.planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md
@.planning/phases/11-library-sync-5-state-key-model/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (already built) + existing adapter/user. Use directly. -->

From src/backend/humble/adapter.ts:
```typescript
export function getGamekeys(cookie: string): Promise<AdapterResult<string[]>>
export function getOrderDetail(cookie: string, gamekey: string): Promise<AdapterResult<OrderDetail>>
// AdapterResult<T> = {status:'ok';data:T} | {status:'session_expired'} | {status:'access_denied'} | {status:'schema_error';raw}
```

From src/backend/humble/classify.ts (Plan 01):
```typescript
export function classifyOrder(rawOrder, isRevealed: (machineName: string) => boolean): HumbleOrderCacheEntry
```

From src/backend/humble/electronStores.ts (Plan 01): `humbleLibraryStore`, `humbleSyncStore`, `humbleRevealedStore`, `configStore`.
From src/backend/humble/user.ts: `HumbleUser.getCredentials(): string | undefined`, `HumbleUser.isLoggedIn(): boolean`.
From src/backend/humble/constants.ts (Plan 01): `HUMBLE_SYNC_CONCURRENCY`, `HUMBLE_COOLDOWN_MS`.
From src/backend/ipc.ts: `sendFrontendMessage(channel, ...args)`. From src/backend/logger: `logWarning/logError/logInfo`, `LogPrefix.Backend`.

Steam precedent (src/backend/storeManagers/steam/library.ts): init() serves cache first then background refresh;
refresh() on failure serves cached entries and returns rather than clearing. Mirror the cache-then-sync + fail-soft shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sync orchestration in library.ts</name>
  <files>src/backend/humble/library.ts, src/backend/humble/__tests__/library.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/library.ts (init() cache-then-sync lines 47-97, refresh() fail-soft lines 152-186, persist-timestamp lines 237-240)
    - src/backend/humble/adapter.ts (AdapterResult status contract, getGamekeys/getOrderDetail signatures)
    - src/backend/humble/classify.ts (classifyOrder signature — Plan 01)
    - src/backend/humble/electronStores.ts (three stores — Plan 01)
    - src/backend/cache.ts (CacheStore.entries() for the partition scan, has()/get()/set())
    - .planning/phases/11-library-sync-5-state-key-model/11-RESEARCH.md (Pattern 2 per-order isolation, Pattern 3 runBounded, Pitfall 3 three-bucket partition, Pitfall 4 no-clear-on-abort, Anti-Patterns: access_denied vs session_expired)
    - .planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md (library.ts section)
  </read_first>
  <behavior>
    - partition(cachedEntries, gamekeys): a gamekey NOT present in humbleLibraryStore → newGamekeys (always fetch); present with allTerminal false → nonTerminal (always fetch); present with allTerminal true → frozen (skip). Never uses Array.every() alone to decide new-vs-frozen (Pitfall 3)
    - runBounded(items, limit, worker): never more than `limit` workers in flight (assert via a max-counter in the mock worker); stops dispatching new work after a worker returns outcome 'access_denied'
    - sync(): getGamekeys ok → partition → bounded fetch → per-order classifyOrder → humbleLibraryStore.set(gamekey, entry) committed immediately per resolve (not batched) → humbleSyncStore.set('syncedAt', now) → returns {status:'ok'} or {status:'partial'}
    - sync(): getGamekeys returns access_denied or schema_error → cache untouched, syncStore syncError set to 'denied'/'network', cooldownUntil set (denied only), returns {status:'failed'} — humbleLibraryStore.clear() is NEVER called
    - sync(): getGamekeys returns session_expired → returns {status:'failed'} WITHOUT setting syncError/cooldown (Phase 10 owns expiry)
    - sync(): a 403 on order N of M — orders 1..N-1 already committed stay committed, N+1..M keep prior cache, syncError='partial', returns {status:'partial'} (D-34)
    - sync(): one order returning schema_error keeps that order's prior cache entry and continues the rest (does not abort)
    - loadCached(): reads humbleLibraryStore.entries(), pushes 'humbleKeysUpdated' with the flattened HumbleKey[]; no network
    - getKeys(): returns the flattened, state-grouped-safe HumbleKey[] from cache; getSyncState(): returns HumbleSyncState from humbleSyncStore
  </behavior>
  <action>
    Create `src/backend/humble/library.ts` exporting a `HumbleLibrary` object/namespace with:
    `loadCached()` (sync, cache-then-render — mirror steam library.ts init() cache load, push `humbleKeysUpdated`),
    `sync()` (async), `getKeys(): HumbleKey[]`, `getSyncState(): HumbleSyncState`.
    Implement a private three-bucket `partitionGamekeys()` per Pitfall 3 (explicit `humbleLibraryStore.has(gamekey)`
    membership check BEFORE the allTerminal check). Implement a private `runBounded()` bounded-concurrency pool per
    RESEARCH Pattern 3 (limit = `HUMBLE_SYNC_CONCURRENCY`, `aborted` flag stops dispatch on `access_denied`; in-flight
    tasks still settle but never clear the cache — Pitfall 4). The per-order worker: call `getOrderDetail`, on `ok`
    run `classifyOrder(data, (mn) => humbleRevealedStore.has(mn))` and `humbleLibraryStore.set(gamekey, entry)` immediately;
    on `schema_error` log a redacted warning (gamekey + status only) and keep the cached entry; on `access_denied` return
    the abort signal; on `session_expired` bail the whole sync. Emit `humbleSyncProgress` (done/total) as orders resolve
    (D-26). On completion set `humbleSyncStore.set('syncedAt', Date.now())`, clear syncError on full success, set
    `syncError`/`cooldownUntil` per the failure branches. Never call `humbleLibraryStore.clear()` anywhere in this file.
    Use `LogPrefix.Backend`; never log a raw order/tpk/key value.
    Create `src/backend/humble/__tests__/library.test.ts` covering every <behavior> bullet, mocking `./adapter`,
    `./classify` (or using real classify with fixtures), and the three stores. Assert the concurrency cap with a
    counter, the three-bucket partition, the mid-sync-abort per-order commit (D-34), the no-clear-on-fail (HSYNC-04),
    and the session_expired-no-syncError path.
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/library.test.ts --no-coverage</automated>
  </verify>
  <done>
    library.ts exports HumbleLibrary with loadCached/sync/getKeys/getSyncState; all library.test.ts cases pass; no `humbleLibraryStore.clear()` call exists in library.ts; `npm run codecheck` clean.
  </done>
  <acceptance_criteria>
    - `src/backend/humble/library.ts` exports `HumbleLibrary` with `sync`, `loadCached`, `getKeys`, `getSyncState`
    - `library.ts` never calls `.clear()` on the library store (grep: `grep -v '^#' src/backend/humble/library.ts | grep -c "humbleLibraryStore.clear" == 0`)
    - A test asserts the fetch pool never exceeds `HUMBLE_SYNC_CONCURRENCY` concurrent workers (counter-based)
    - A test asserts a never-cached gamekey is fetched and an all-terminal cached gamekey is not (partition, Pitfall 3)
    - A test asserts getGamekeys access_denied leaves the cache untouched and sets syncError (HSYNC-04)
    - A test asserts a mid-sync 403 commits orders resolved before the abort and returns status 'partial' (D-34)
    - A test asserts session_expired returns 'failed' WITHOUT setting syncError/cooldown
    - `npx jest src/backend/humble/__tests__/library.test.ts --no-coverage` exits 0
    - `npm run codecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend disconnect() to preserve the REVEALED-flag store</name>
  <files>src/backend/humble/user.ts, src/backend/humble/__tests__/user.test.ts</files>
  <read_first>
    - src/backend/humble/user.ts (disconnect() lines 440-472 — the forward-policy comment at 469-470 to extend, not delete)
    - src/backend/humble/electronStores.ts (humbleLibraryStore/humbleSyncStore/humbleRevealedStore — Plan 01)
    - src/backend/humble/__tests__/user.test.ts (existing structure/mocks to extend)
    - .planning/phases/11-library-sync-5-state-key-model/11-RESEARCH.md (Pitfall 1, HSYNC-02 disconnect-survival)
    - .planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md (user.ts disconnect section)
  </read_first>
  <action>
    In `src/backend/humble/user.ts` `disconnect()`, after `configStore.clear()` add `humbleLibraryStore.clear()` and
    `humbleSyncStore.clear()` (import both from `./electronStores`). Do NOT clear `humbleRevealedStore`. Extend the
    existing comment at lines 469-470 (currently "none exists yet in Phase 10; this is forward policy for Phase 11/14")
    to state the REVEALED-flag store now exists and is deliberately skipped per D-04/D-30 — extend, do not delete.
    In `src/backend/humble/__tests__/user.test.ts` add a test that mocks the three stores and asserts, after
    `HumbleUser.disconnect()`, that `humbleLibraryStore.clear` and `humbleSyncStore.clear` were called and
    `humbleRevealedStore.clear` was NOT called.
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/user.test.ts --no-coverage</automated>
  </verify>
  <done>
    disconnect() clears the library + sync stores but never the revealed store; the new user.test.ts case passes; the D-04/D-30 rationale comment is updated in place.
  </done>
  <acceptance_criteria>
    - `src/backend/humble/user.ts` disconnect() contains `humbleLibraryStore.clear()` and `humbleSyncStore.clear()`
    - disconnect() does NOT call `humbleRevealedStore.clear` (grep: `grep -v '^#' src/backend/humble/user.ts | grep -c "humbleRevealedStore.clear" == 0`)
    - A test asserts revealed-store clear is never invoked while library/sync stores are cleared on disconnect
    - `npx jest src/backend/humble/__tests__/user.test.ts --no-coverage` exits 0
    - `npm run codecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Humble API → library.ts | Untrusted responses drive fan-out + cache writes |
| library.ts → renderer | Sync progress / keys pushed over IPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Denial of Service | library.ts fan-out | mitigate | Bounded pool (HUMBLE_SYNC_CONCURRENCY=3), abort-on-first-403, HUMBLE_COOLDOWN_MS gate (D-25/D-33) — never hammer a Humble denial |
| T-11-04 | Information Disclosure | library.ts logging | mitigate | Redacted logging: gamekey identifiers + status strings only, never a raw order/tpk/key value |
| T-11-05 | Tampering / DoS | per-order schema_error | mitigate | Per-order isolation (Pattern 2) — a schema_error on one order keeps its cached entry and continues; sync never crashes |
| T-11-06 | Tampering | fail-soft abort cache integrity | mitigate | Abort only stops dispatch; no code path calls humbleLibraryStore.clear() — completed per-order writes survive (Pitfall 4) |
| T-11-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npx jest src/backend/humble/__tests__/library.test.ts --no-coverage` exits 0
- `npx jest src/backend/humble/__tests__/user.test.ts --no-coverage` exits 0
- `npm run codecheck` exits 0
</verification>

<success_criteria>
Sync is concurrency-bounded, skip-terminal, per-order-committed, and fail-soft; a Humble denial
never wipes or hammers; the REVEALED-flag store survives disconnect (HSYNC-02) while the library +
sync stores are wiped.
</success_criteria>

<output>
Create `.planning/phases/11-library-sync-5-state-key-model/11-02-SUMMARY.md` when done
</output>
