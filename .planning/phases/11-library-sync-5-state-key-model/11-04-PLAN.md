---
phase: 11-library-sync-5-state-key-model
plan: 04
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - src/frontend/screens/Humble/Keys/index.tsx
  - src/frontend/screens/Humble/Keys/index.css
  - src/frontend/screens/Humble/Keys/components/HumbleKeyGroup/index.tsx
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
  - src/frontend/App.tsx
  - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx
  - public/locales/en/translation.json
autonomous: true
requirements: [HSYNC-01, HSYNC-04]

must_haves:
  truths:
    - "D-20: A 'Humble Keys' sidebar entry appears only when humble.isLoggedIn is true"
    - "D-19/D-21: The keys page is a minimal read-only list — each row shows title, a 5-state badge, expiration, and a secondary bundle/order origin label — rendered as a flat list grouped by state in fixed order UNPICKED, UNREVEALED, REVEALED, REDEEMED, UNREDEEMABLE"
    - "D-21: Within each group, keys sort expiring-soonest first; no-expiration keys sort last"
    - "Empty groups (zero keys) are not rendered"
    - "D-32: A 'Last synced X ago' freshness indicator is always shown near the refresh button; D-31: a persistent inline 'Couldn't refresh — showing data from {time}' banner (no toast) shows only on syncError"
    - "D-26: During a multi-order sync the freshness line is replaced by a progressive-fill 'Syncing… N/M orders' indicator as keys fill in"
    - "Rows are strictly read-only — no onClick, no reveal/copy/expand affordance (D-22)"
    - "The manual refresh button spins while syncing and is disabled during the 403 cooldown"
  artifacts:
    - path: "src/frontend/screens/Humble/Keys/index.tsx"
      provides: "Humble Keys page: header, freshness/progress indicator, refresh button, fail-soft banner, grouped list"
      min_lines: 40
    - path: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      provides: "Read-only key row with 5-state badge"
  key_links:
    - from: "src/frontend/App.tsx"
      to: "src/frontend/screens/Humble/Keys"
      via: "lazy route registration for /humble-keys guarded on humble.isLoggedIn"
      pattern: "humble-keys"
    - from: "src/frontend/screens/Humble/Keys/index.tsx"
      to: "ContextProvider humble slice (keys, syncedAt, syncError, syncing)"
      via: "useContext read"
      pattern: "useContext"
---

<objective>
Ship the one new surface for Phase 11: the strictly read-only "Humble Keys" page that proves the
5-state classification is correct, with the fail-soft banner and freshness indicator — plus its
sidebar entry and route.

Purpose: HSYNC-01's visible proof (success criterion 1 — every key classified into exactly one
state, rendered) and HSYNC-04's UI (success criterion 4 — cached data + "couldn't refresh" banner,
never blank/error). Implements D-19/D-20/D-21/D-22/D-26/D-31/D-32/D-33 per the UI-SPEC contract.
Output: New `Humble/Keys` screen + child components + CSS, App route, sidebar entry, `humbleKeys.*` i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-library-sync-5-state-key-model/11-CONTEXT.md
@.planning/phases/11-library-sync-5-state-key-model/11-UI-SPEC.md
@.planning/phases/11-library-sync-5-state-key-model/11-PATTERNS.md
@.planning/phases/11-library-sync-5-state-key-model/11-03-SUMMARY.md

<interfaces>
<!-- 11-UI-SPEC.md is the authoritative visual/behavioral contract for this plan. -->

Context slice (from Plan 03, read via useContext(ContextProvider)):
`humble: { isLoggedIn?, expired?, keys?: HumbleKey[], syncedAt?: number | null, syncError?: 'none'|'denied'|'network'|'partial', syncing?: boolean, ... }`

HumbleKey (from common/types/humble): `{ gamekey, machineName, state: HumbleKeyState, title, platform, expiration: string | null, origin }`
HumbleKeyState: `'UNPICKED' | 'UNREVEALED' | 'REVEALED' | 'REDEEMED' | 'UNREDEEMABLE'`

Refresh action: `window.api.humbleSync()`. Cooldown gate: derive from `humble.syncError === 'denied'` + a cooldownUntil (fetched via `window.api.humbleGetSyncState()`).

Analog files to mirror (do NOT import — replicate structure):
- src/frontend/screens/Library/components/LibraryHeader/index.tsx — formatRelativeTime (17-44), refresh button + `.steamRefreshButton` spin/disabled (59-110), stale indicator (61-92)
- src/frontend/screens/Library/components/GameCard/index.css — `.gameCardUpdateBadge` (53-67) / `.gameCardDelistedBadge` (202-209) badge chrome
- src/frontend/components/UI/WarningMessage/index.tsx — fail-soft banner (reuse with `humbleSyncBanner` className)
- src/frontend/screens/WineManager/index.tsx — page scaffold pattern
- src/frontend/App.tsx — route table (wine-manager 174, download-manager 178, loginweb/steam 162)
- src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx — loggedIn-gated SidebarItem (259-266), Stores submenu block (~173)

5-State Badge color mapping (UI-SPEC §Color): UNPICKED `--status-default`, UNREVEALED `--status-info`,
REVEALED `--status-warning`, REDEEMED `--status-success`, UNREDEEMABLE `--status-danger`; badge text `--neutral-01` on all five.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Humble Keys screen shell + route + sidebar entry</name>
  <files>src/frontend/screens/Humble/Keys/index.tsx, src/frontend/screens/Humble/Keys/index.css, src/frontend/App.tsx, src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx, public/locales/en/translation.json</files>
  <read_first>
    - .planning/phases/11-library-sync-5-state-key-model/11-UI-SPEC.md (Page Layout, Interaction Contracts, Sidebar Entry, Copywriting Contract — authoritative)
    - src/frontend/screens/Library/components/LibraryHeader/index.tsx (formatRelativeTime, refresh button, stale indicator)
    - src/frontend/screens/Library/components/LibraryHeader/index.css (.steamRefreshButton, .steamStaleIndicator, .steamSyncSpinner)
    - src/frontend/components/UI/WarningMessage/index.tsx (banner reuse + className override)
    - src/frontend/screens/WineManager/index.tsx (page scaffold)
    - src/frontend/App.tsx (route table conventions)
    - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx (loggedIn-gated SidebarItem, Stores block position, useContext destructure ~37-45, FA import block 1-14)
    - src/frontend/state/ContextProvider.tsx (humble slice shape)
  </read_first>
  <action>
    Create `src/frontend/screens/Humble/Keys/index.tsx`: read the `humble` slice via `useContext(ContextProvider)`.
    Render a page header per UI-SPEC Page Layout — title `t('humbleKeys.title', 'Humble Keys')`, an always-shown
    freshness line `t('humbleKeys.lastSynced', 'Last synced {{time}} ago', { time })` using a local `formatRelativeTime`
    copied from LibraryHeader (4-bucket minutes/hours/days), and an icon-only `faSyncAlt` refresh button cloning
    `.steamRefreshButton` (transparent bg, `--accent` hover, `fa-spin`/`.spinning` while `humble.syncing`, `disabled`
    during 403 cooldown). While `humble.syncing` and total>1, replace the freshness line with the progressive-fill
    line `t('humbleKeys.syncing', 'Syncing… {{done}}/{{total}} orders', { done, total })` (spinner in `--text-secondary`,
    not accent). Below the header, render the fail-soft banner ONLY when `humble.syncError` is a non-'none' value:
    reuse `WarningMessage` with a `humbleSyncBanner` className, copy `humbleKeys.syncError` (full abort, uses the
    last-synced time) or `humbleKeys.syncErrorPartial` (when `syncError === 'partial'`) — no toast, no dismiss button.
    The cooldown state disables the refresh button and sets its `title` to `humbleKeys.cooldown`. Sort keys into the
    five fixed-order groups (UNPICKED, UNREVEALED, REVEALED, REDEEMED, UNREDEEMABLE), expiring-soonest first within
    each group (no-expiration last); render one `<HumbleKeyGroup>` per NON-EMPTY group (Task 2 builds it — for this
    task, render a placeholder mapping or import the component created in Task 2 within the same plan). Empty state
    (connected, zero keys): centered heading `humbleKeys.emptyTitle` + body `humbleKeys.emptyBody`.
    Create `src/frontend/screens/Humble/Keys/index.css`: page/header/group-section layout using only existing tokens
    (`--space-md` content padding, `--space-lg` gap between groups, `--space-xl` empty-state padding), the
    `.humbleSyncBanner` override (background `--status-warning`, color `--neutral-01`), and refresh-button/indicator
    styles cloned from `.steamRefreshButton`/`.steamStaleIndicator`. No new CSS custom properties, no hard-coded hex.
    In `src/frontend/App.tsx`, register a lazy route `path: 'humble-keys'` (kebab-case, matching wine-manager) via
    `makeLazyFunc(import('./screens/Humble/Keys'))`. Guard it: when `humble.isLoggedIn` is false, redirect to the
    existing login path (reuse the `humbleLoginPath`/`/login` convention) — no disconnected empty state renders.
    In `src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx`, import `faKey`, pull `humble` from the
    `useContext(ContextProvider)` destructure, and add immediately after the Stores `SidebarItemWithSubmenu` block and
    before the `/discounts` item: a `humble?.isLoggedIn && <SidebarItem url="/humble-keys" icon={faKey}
    label={t('sidebar.humbleKeys', 'Humble Keys')} dataTour="sidebar-humble-keys" />` (gated on isLoggedIn ONLY, not
    `!expired` — an expired session still shows cached keys).
    In `public/locales/en/translation.json`, add the `humbleKeys` namespace keys used above (title, lastSynced,
    syncing, syncError, syncErrorPartial, refresh, cooldown, emptyTitle, emptyBody) and the top-level `sidebar.humbleKeys`
    key — matching the file's existing structure and interpolation style.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <done>
    The screen shell renders header + freshness/progress indicator + refresh button + conditional fail-soft banner + grouped list / empty state; route + sidebar entry are wired and gated on humble.isLoggedIn; i18n keys exist; codecheck clean.
  </done>
  <acceptance_criteria>
    - `src/frontend/App.tsx` contains a route with `path: 'humble-keys'`
    - `src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx` contains `url="/humble-keys"` gated on `humble?.isLoggedIn`
    - `src/frontend/screens/Humble/Keys/index.tsx` reads `humble.syncError` and conditionally renders `WarningMessage` with a `humbleSyncBanner` class
    - `index.css` contains no hard-coded hex color (grep: `grep -v '^#' src/frontend/screens/Humble/Keys/index.css | grep -ciE "#[0-9a-f]{3,6}\b" == 0`)
    - `public/locales/en/translation.json` contains `"humbleKeys"` and `"humbleKeys": { ... "syncError"` plus `sidebar.humbleKeys`
    - Group order UNPICKED→UNREVEALED→REVEALED→REDEEMED→UNREDEEMABLE is fixed in code; empty groups are not rendered
    - `npm run codecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: HumbleKeyGroup + read-only HumbleKeyRow + 5-state badge</name>
  <files>src/frontend/screens/Humble/Keys/components/HumbleKeyGroup/index.tsx, src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx, src/frontend/screens/Humble/Keys/index.css, public/locales/en/translation.json</files>
  <read_first>
    - .planning/phases/11-library-sync-5-state-key-model/11-UI-SPEC.md (Component Inventory, 5-State Badge Color Mapping, Row Read-Only Contract, UNPICKED Pseudo-Entry, Copywriting Contract)
    - src/frontend/screens/Library/components/GameCard/index.css (.gameCardUpdateBadge / .gameCardDelistedBadge chrome to reuse)
    - src/frontend/screens/Humble/Keys/index.tsx (Task 1 — how groups/rows are consumed)
    - src/frontend/screens/Humble/Keys/index.css (Task 1 — extend, don't recreate)
  </read_first>
  <action>
    Create `src/frontend/screens/Humble/Keys/components/HumbleKeyGroup/index.tsx`: props `{ state: HumbleKeyState;
    keys: HumbleKey[] }`. Render a group heading (`--text-lg`/`--semibold`) with the localized state label + a count
    pill showing `keys.length`, then the child rows (expiring-soonest-first sort is done by the parent per Task 1;
    this component renders in received order). Never render when `keys.length === 0` (parent already filters, keep a
    defensive guard).
    Create `src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx`: props `{ humbleKey: HumbleKey }`.
    Render as a NON-interactive `<div>`/`<li>` (NOT `<button>`/`<a>`) — no onClick, no cursor:pointer, no hover
    elevation, no action buttons, no copy/reveal/expand affordance (D-22 read-only contract). Layout: a state badge
    `<span>` styled via a `.humbleKeyStateBadge--{state}` modifier class, the title (`--text-md`/`--regular`), a
    secondary caption `t('humbleKeys.rowCaption', '{{platform}} · {{origin}}', {...})` (`--text-xs`/`--text-secondary`),
    and an expiration cell: real key → `humbleKeys.expiresOn` with the formatted date or `humbleKeys.noExpiration`;
    UNPICKED pseudo-entry → title `humbleKeys.unpickedTitle`, no caption, deadline via `humbleKeys.noDeadline` fallback
    when absent (never throws, never blocks the row — Pitfall 2).
    Extend `src/frontend/screens/Humble/Keys/index.css` with the `.humbleKeyStateBadge` base (reusing
    `.gameCardUpdateBadge`'s exact padding `var(--space-3xs) var(--space-2xs)`, border-radius `var(--space-3xs)`,
    `--text-sm`/`--semibold`, color `--neutral-01`) and five `.humbleKeyStateBadge--{UNPICKED|UNREVEALED|REVEALED|REDEEMED|UNREDEEMABLE}`
    modifiers each setting `background` to the mapped `--status-*` token per the UI-SPEC table. Add row layout styles
    (`--space-md` vertical padding, `--space-xs` caption gap) — existing tokens only, no hex.
    In `public/locales/en/translation.json` add the remaining `humbleKeys.*` keys: rowCaption, expiresOn,
    noExpiration, unpickedTitle, noDeadline, plus the five group-heading labels used by HumbleKeyGroup.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <done>
    Groups render heading + count + rows; rows are strictly read-only with a correctly-colored 5-state badge; UNPICKED pseudo-entry renders defensively without a deadline; all row/badge i18n keys exist; codecheck clean.
  </done>
  <acceptance_criteria>
    - `HumbleKeyRow/index.tsx` renders no `onClick`, no `<button`, no `<a ` for the row itself (grep: `grep -c "onClick" src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx == 0`)
    - `index.css` contains `.humbleKeyStateBadge--UNREDEEMABLE` with `background: var(--status-danger)` and `.humbleKeyStateBadge--UNREVEALED` with `background: var(--status-info)`
    - Badge base uses `color: var(--neutral-01)` (dark text) on all five states
    - `public/locales/en/translation.json` contains `humbleKeys.unpickedTitle` and `humbleKeys.noExpiration`
    - `index.css` contains no hard-coded hex color
    - `npm run codecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| context slice → rendered DOM | Key display data reaches the screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Elevation of Privilege | read-only row contract | mitigate | Rows render as non-interactive `<div>`/`<li>` with no reveal/copy/link-out — claim actions are structurally absent until Phase 14's C1/C2 guards exist (D-22) |
| T-11-01 | Information Disclosure | rendered row | mitigate | HumbleKey carries no raw key value (Plan 01); the row has nothing sensitive to display |
| T-11-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npm run codecheck` exits 0
- Sidebar entry visible only when connected; page groups render in fixed order; empty groups hidden; banner conditional on syncError
- Manual visual UAT deferred to Plan 05
</verification>

<success_criteria>
The Humble Keys page renders the 5-state classification as a read-only, state-grouped list with a
fail-soft banner and always-on freshness indicator, reachable via a connected-only sidebar entry —
the visible proof of HSYNC-01 and the home of HSYNC-04's "couldn't refresh" state.
</success_criteria>

<output>
Create `.planning/phases/11-library-sync-5-state-key-model/11-04-SUMMARY.md` when done
</output>
</output>
