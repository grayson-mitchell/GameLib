---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 15
type: execute
wave: 6
depends_on: ["34.5-14"]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
autonomous: false
requirements: [REQ-34.5-12]

must_haves:
  truths:
    - "D-08: the 5-item live gate is BLOCKING. No advisory mode, no pre-authorized override. Justified because 34.4.1's gate has just demonstrated that a live gate catches what a fully-green 3095-test suite cannot."
    - "PRECONDITION 1 IS A SEQUENCING GATE, NOT A NOTE: Phase 34.4.1's OWN blocking live gate must have recorded PASS on at least item 1 (a real login completing from scratch) before gate items 1-3 may run here. 34.5 may be BUILT on that seam; it must not SHIP a real OAuth credential path over a seam whose live gate never ran."
    - "Preconditions are ticked in ORDER and each is evidenced, because an unstated precondition is exactly how a gate gets skipped and becomes UAT debt (Phases 21/30/34.2)."
    - "Sidecar output is read from `~/Library/Logs/GameLib/gamelib.log`, NEVER from the `tauri:dev` terminal — stdout IS the RPC pipe, and both `console.*` and the file logger are invisible there."
    - "Item 4 must exercise BOTH `exe` call sites — `nonesteamgame.ts:258` (Steam VDF) and `shortcuts.ts:227` (macOS `.app` `run.sh`). A pass on one is not a pass on the item (research Correction 3)."
    - "Item 3 must capture ONE REAL redirect and compare its origin against the `www.amazon.com` anchor. Confirming or falsifying research Assumption A1 is part of the item, not a side note."
    - "Item 5 closes a gap that has never been proven: `runWineCommand` has never run for a non-Steam runner under the sidecar, because no non-Steam login has ever worked there."
    - "A FAIL is a gap cycle inside Phase 34.5, not a deferral. Results are recorded verbatim — a partially-passing gate is recorded as partially passing, never rounded."
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md"
      provides: "The executed gate with all preconditions evidenced, all 5 items resulted, and a verdict"
      contains: "verdict:"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md"
      provides: "Gate-dependent proof-level cells updated from PENDING to the recorded result"
  key_links:
    - from: ".planning/phases/34.5-.../34.5-LIVE-GATE.md item results"
      to: ".planning/phases/34.5-.../34.5-PORTED-CHANNELS.md proof-level cells"
      via: "each `unit + LIVE (item N)` cell flipped from PENDING to the recorded PASS/FAIL"
      pattern: "LIVE \\(item"
---

<objective>
Execute the blocking 5-item live gate under `pnpm tauri:dev` on macOS, record every result verbatim,
and propagate those results into the declared channel list.

Purpose: everything before this plan is unit-proven. Three real logins, a shortcut that actually
launches, and a Wine command that actually runs for a non-Steam game are the only things that can
prove this slice works — and every one of them has a precedent in this project of a green suite
hiding a real failure.

Output: a gate with five recorded results, a verdict, and a channel list that no longer says
PENDING where a human has now looked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-VALIDATION.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Satisfy and evidence every gate precondition, in order</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.5-.../34.5-LIVE-GATE.md (the contract plan 34.5-03 wrote — the numbered preconditions and the evidence form each demands)
    - .planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md (its recorded precondition results — precondition 1 here is a direct read of its item 1 result)
    - .planning/STATE.md L37-72 (34.4.1-08's held state, including the moved-aside Humble credential and its two verified backup paths — relevant if a credential must be moved aside again here)
    - .planning/phases/34.5-.../34.5-13-SUMMARY.md (the automated baseline this gate's precondition 4 must match against)
  </read_first>
  <action>
    Work through the preconditions in the order the gate document lists them, ticking each only when
    its stated evidence exists, and writing that evidence INTO the document.

    Precondition 1 is a hard sequencing gate. Read `34.4.1-LIVE-GATE.md` and quote its item-1 result
    line verbatim into this gate. If it has not recorded PASS, STOP: items 1-3 may not run, and the
    correct outcome is to record the block, leave the gate `status: blocked`, and return that state
    rather than proceeding. Do not soften this into "proceed with caution" — this phase must not ship
    a real OAuth credential path over a seam whose live gate never ran.

    For the remaining preconditions: clean rebuild and a real `pnpm tauri:dev` launch with the
    sidecar confirmed alive from `~/Library/Logs/GameLib/gamelib.log` (record the byte growth and a
    `ps` listing); sign-out state for all three storefronts with any moved-aside credential's restore
    path written down; and the four automated commands (`npm run test:ci`, `npx tsc --noEmit`,
    `cargo check`, `cargo test`) recorded with VERBATIM exit codes and pass/fail counts. Read back
    the `GAMELIB_SHELL_EXE` value the shell actually set from `gamelib.log` and write it into the
    document before item 4 runs.

    A precondition that cannot be satisfied is recorded as DEFERRED with its reason and its
    consequence for the items that depend on it — honestly, never softened. Do not tick a
    precondition on the strength of an intention.
  </action>
  <acceptance_criteria>
    - Every precondition in `34.5-LIVE-GATE.md` is either ticked with its evidence written in, or explicitly marked DEFERRED with a reason and a named consequence.
    - Precondition 1 quotes `34.4.1-LIVE-GATE.md`'s item-1 result line verbatim.
    - The four automated commands' exit codes and pass/fail counts are recorded verbatim, unrounded; any flake is named with its suite and signature.
    - The `GAMELIB_SHELL_EXE` value read from `gamelib.log` is written into the document.
    - The document records that sidecar output was read from `gamelib.log` and not the `tauri:dev` terminal.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:ci; npx tsc --noEmit; cd src-tauri && cargo check && cargo test</automated>
  </verify>
  <done>Every precondition is satisfied or honestly deferred with its consequence stated, and the 34.4.1 sequencing gate has been checked rather than assumed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the 5-item blocking live gate</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.5-.../34.5-LIVE-GATE.md (the five items with their What to do / What to observe / Evidence required sub-sections — follow them exactly rather than improvising an equivalent)
    - .planning/phases/34.5-.../34.5-02-SUMMARY.md (the greppable prefix of the origin-evidence log line item 3 needs)
    - .planning/phases/34.5-.../34.5-01-SUMMARY.md (how `GAMELIB_SHELL_EXE` is logged, for item 4)
    - .claude/skills/spike-findings-gamelib/SKILL.md and the project's `sidecar-console-and-logger-are-invisible` note (sidecar output is read from `~/Library/Logs/GameLib/gamelib.log`, never the `tauri:dev` terminal — stdout IS the RPC pipe)
  </read_first>
  <action>
    Drive the five gate items on real hardware under `pnpm tauri:dev` on macOS, filling each item's
    Result slot in `34.5-LIVE-GATE.md` as it completes. Record evidence in the form each item's
    Evidence required sub-section demands.

    Item 3 additionally requires the observed redirect ORIGIN, read from `gamelib.log` via the
    evidence-log prefix plan 34.5-02 added, written down VERBATIM before it is compared to the
    `www.amazon.com` anchor. Do not adjust the anchor during the gate — a mismatch is a recorded FAIL
    of research Assumption A1 and is the useful outcome.

    Item 4 requires BOTH `exe` call sites to be exercised by actually launching both artifacts: the
    Steam shortcut written via `nonesteamgame.ts:258` and the macOS `.app` wrapper written via
    `shortcuts.ts:227`. A pass on one is not a pass on the item.

    A FAIL is recorded as a FAIL and becomes a gap cycle inside Phase 34.5. Do not round a partial
    pass to green, and do not close the phase on one.
  </action>
  <acceptance_criteria>
    - All five Result slots in `34.5-LIVE-GATE.md` carry a PASS or FAIL with the evidence that item demands, or the gate records a BLOCKED state at precondition 1.
    - Item 3's recorded evidence includes the observed redirect origin verbatim, and no full URL, query string or authorization code appears anywhere in the recorded evidence.
    - Item 4's evidence names both `nonesteamgame.ts:258` and `shortcuts.ts:227` artifacts and states that each was launched.
    - Item 5's evidence names the non-Steam runner and the Wine action that was run.
    - Every evidence entry cites `~/Library/Logs/GameLib/gamelib.log` where a log line is the evidence, not the `tauri:dev` terminal.
  </acceptance_criteria>
  <done>All five items are recorded with evidence, and the developer has signalled the result or named exactly which item blocked.</done>
  <what-built>
    All 38 channels of slice 8 are registered on the Tauri sidecar across four registration modules,
    behind three wave-1 seams: `pathShim` now resolves `desktop`/`exe`/`documents` with `exe` carried
    from the Rust shell via `GAMELIB_SHELL_EXE`; nile's OAuth redirect matcher is host-anchored on
    `www.amazon.com`; and `runWineCommand` is registered as a seam. Everything is unit-proven — 34
    handle and 4 send channels are kind-pinned in both directions, the `exe` loud-throw is pinned
    against the filesystem, the reach ledger is regenerated by measurement, and both Discretion
    sweeps have run. Nothing below has been observed working on real hardware.
  </what-built>
  <how-to-verify>
    Read sidecar output from `~/Library/Logs/GameLib/gamelib.log`, NOT the `tauri:dev` terminal.

    1. Item 1 — Epic login. From a fully signed-out state, run `pnpm tauri:dev`, log in via Epic,
       and confirm you reach a signed-in state with a POPULATED library, not merely a signed-in
       badge.
    2. Item 2 — GOG login. Same, via GOG.
    3. Item 3 — Amazon login. Same, via Amazon. Then find the captured redirect's origin in
       `gamelib.log` using the evidence-log prefix named in the gate document, and record it
       verbatim. If it is not `www.amazon.com`, record the real value — that is a FAIL of
       Assumption A1 and the whole point of the item.
    4. Item 4 — shortcuts. Add a game to Steam AND create a desktop shortcut, then launch BOTH.
       Confirm GameLib starts, not node. Both `exe` call sites must be exercised.
    5. Item 5 — Wine. After a login above succeeds, run a Wine tool action against a NON-Steam game
       and confirm it completes. This combination has never run under the sidecar.
  </how-to-verify>
  <resume-signal>Type "gate recorded" with the per-item results, or describe which item failed or blocked and what was observed</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verdict and propagate results into the declared channel list</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/34.5-.../34.5-LIVE-GATE.md (as filled by Task 2 — the five recorded results)
    - .planning/phases/34.5-.../34.5-PORTED-CHANNELS.md (the PENDING cells to update and the residuals section)
    - .planning/phases/34.5-.../ported-channels-gate.py (its check 4 accepts both PENDING and a recorded PASS/FAIL — confirm which form it expects before editing)
    - .planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md § "What this gate falsifies" (the precedent for striking a falsified standing claim rather than quietly leaving it)
  </read_first>
  <action>
    Update `34.5-LIVE-GATE.md`'s frontmatter with the real `items_passed`, `items_failed` and
    `verdict`, and fill the Verdict table. In `## What this gate falsifies`, strike each standing
    claim a PASS retires — at minimum "no non-Steam runner has ever executed a Wine command under the
    sidecar" (item 5) and "the `www.amazon.com` anchor is unverified against GameLib's own traffic"
    (item 3). Strike only what actually passed; leave the rest standing and say so.

    Update `34.5-PORTED-CHANNELS.md`: every `unit + LIVE (item N)` cell whose item PASSED becomes the
    recorded PASS form the gate script accepts; every cell whose item FAILED is recorded as FAILED
    with a pointer to the gate item, NOT reverted to `unit` and NOT quietly softened. Update the
    residuals section: if item 3 confirmed the anchor, downgrade Assumption A1's residual to
    confirmed with the observed origin quoted; if item 4 passed, do the same for Assumption A2's
    `current_exe()` residual. If either failed, promote it to a named gap.

    Re-run `ported-channels-gate.py` and its `--self-test` after the edits; both must still exit 0.

    If ANY item FAILED, state plainly in the SUMMARY that the phase is NOT complete, list the failed
    items as gap-cycle candidates with what was observed, and do not write a closing narrative that
    implies otherwise. If the gate was BLOCKED at precondition 1, record that and return — the phase
    stays open.
  </action>
  <acceptance_criteria>
    - `34.5-LIVE-GATE.md`'s frontmatter `items_passed` + `items_failed` equals 5, or the document records a BLOCKED state with the reason.
    - No `PENDING` remains in a `LIVE (item N)` cell of `34.5-PORTED-CHANNELS.md` for an item that has a recorded result.
    - `python3 .planning/phases/34.5-.../ported-channels-gate.py` and `--self-test` both exit 0 after the edits.
    - The `What this gate falsifies` section strikes only claims whose item actually passed, and the SUMMARY names which claims remain standing.
    - If any item failed, the SUMMARY says the phase is NOT complete and lists the gap-cycle candidates with observed behaviour.
  </acceptance_criteria>
  <verify>
    <automated>python3 .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py && python3 .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py --self-test</automated>
  </verify>
  <done>The gate carries five recorded results and a verdict, the declared channel list reflects them exactly, and only the claims that were actually falsified have been struck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unit-proven code → real storefront OAuth | The first time this slice's credential paths meet a live remote service |
| written launch artifact → Steam / Finder execution | Only observable when a real shortcut is really clicked |
| gate result → phase closure | A softened result is how a phase closes over a defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-59 | Repudiation | shipping a real OAuth credential path over an ungated seam | mitigate | Precondition 1 is a hard stop: 34.4.1's own gate must record PASS on item 1 before items 1-3 run; a missing PASS leaves this gate `blocked` and the phase open. |
| T-34.5-60 | Spoofing | the `www.amazon.com` anchor being wrong in the real world | mitigate | Item 3 records the OBSERVED origin from `gamelib.log` verbatim; a mismatch is recorded as a FAIL of Assumption A1, not fixed by adjusting the expectation during the gate. |
| T-34.5-61 | Tampering | a shortcut written with a wrong launch command | mitigate | Item 4 requires BOTH `exe` call sites to be exercised by actually launching both artifacts, because the value is only observable at click time. |
| T-34.5-62 | Repudiation | a partial pass rounded to green | mitigate | Results are recorded verbatim; a FAIL becomes a gap cycle inside Phase 34.5; the SUMMARY must state the phase is not complete. D-08 forbids advisory mode and pre-authorized override. |
| T-34.5-63 | Information Disclosure | credentials or codes captured into gate evidence | mitigate | Item 3 records the redirect ORIGIN only — never the full URL, the query string or the code. Plan 34.5-02's evidence log is already origin-only by construction. |
| T-34.5-SC | Tampering | npm installs | accept | Zero new packages; this plan runs and records, it does not build. |
</threat_model>

<verification>
- All five gate items carry a recorded PASS or FAIL with evidence, or the gate records a BLOCKED state.
- `ported-channels-gate.py` and its self-test both exit 0 after the propagation edits.
- No `LIVE (item N)` cell still reads PENDING for a resulted item.
- The four automated commands' exit codes are recorded verbatim in the gate document.
</verification>

<success_criteria>
Three real logins, two real shortcuts and one real non-Steam Wine command have either been observed
working and recorded, or their failure has been recorded honestly as a gap cycle — with the declared
channel list saying exactly the same thing the gate does.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-15-SUMMARY.md` when done
</output>
