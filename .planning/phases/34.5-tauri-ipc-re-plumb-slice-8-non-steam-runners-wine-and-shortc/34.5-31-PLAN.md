---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 31
type: execute
wave: 7
depends_on: [34.5-30]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
  - .planning/IPC-PORT-INVENTORY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-04, REQ-34.5-05, REQ-34.5-11, REQ-34.5-12, REQ-34.5-13]

must_haves:
  truths:
    - "All five blocking gate items are attempted on real macOS hardware and each carries a recorded result"
    - "The verdict is computed honestly, with the arithmetic stated, and no FAIL is softened"
    - "Every tracking document agrees with the recorded verdict, and no standing claim is struck without its item passing"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md"
      provides: "the completed third blocking gate record"
      contains: "Overall verdict"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN-2.md verdict"
      to: ".planning/ROADMAP.md and .planning/STATE.md"
      via: "propagation without softening"
      pattern: "34.5-LIVE-GATE-RERUN-2"
---

<objective>
Execute the BLOCKING five-item live gate against `34.5-LIVE-GATE-RERUN-2.md` on real macOS hardware,
record every result, compute the verdict, and propagate it into every document that tracks it.

This is the third attempt. Runs 1 and 2 both failed 0/5 on two different root causes, and items 4 and
5 have never been attempted at all. Per D-08, **any item recorded FAIL means Phase 34.5 does not
close** — no advisory mode, no pre-authorized override, no deferred UAT entry, no partial-pass
closure. A third FAIL routes to another gap cycle inside 34.5.

Every blocking defect this phase has found was found by a human driving the UI while a fully green
test suite (3279, then 3387, then 3447 tests) reported nothing wrong. That is why this plan exists
and why it cannot be automated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md

**The contract is authoritative.** `34.5-LIVE-GATE-RERUN-2.md` governs; where this plan and the
contract differ, the contract wins. Fill its Result slots; do not rewrite its items.

**Evidence-handling rule (T-34.5-G6-02), binding on every Result slot:** never paste an
`access_token`, `refresh_token`, cookie value, session identifier, or the Steam `userdata` account
id. Quote only the lines an item names; redact in place rather than omitting a whole line.

**Sidecar output is read from `~/Library/Logs/GameLib/gamelib.log`, NEVER from the `pnpm tauri:dev`
terminal** (`sidecar-console-and-logger-are-invisible`). The one exception is `main.rs`'s
`eprintln!("[shell] …")` lines, which reach only the terminal — an item relying on one says so.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Satisfy and evidence all nine preconditions, in order</name>
  <what-built>
    `34.5-LIVE-GATE-RERUN-2.md` (plan 34.5-30) carries nine numbered preconditions with empty RESULT
    slots. Behind them sit this gap cycle's fixes from plans 34.5-23 through 34.5-29.
  </what-built>
  <how-to-verify>
    Work the contract's preconditions 1 through 9 IN ORDER and record each RESULT slot verbatim. Do
    not reorder them and do not skip one because it "obviously holds" — an unstated or assumed
    precondition is exactly how a gate becomes UAT debt, and this phase's own first run did it.

    Three carry a hard STOP:
    - **Precondition 4** — a `[bootstrap] SIDECAR ASSET ROOT DEFECT` line anywhere in this session's
      `gamelib.log` BLOCKS items 1-3, and therefore item 5. If it fires, stop and route back to a gap
      cycle rather than attempting items on a build that has announced its own asset root is broken.
    - **Precondition 7** — `[bootstrap] GAMELIB_SHELL_EXE received=<UNSET>` BLOCKS item 4.
    - **Precondition 8** — if any of the three gap-cycle fixes is absent from the source at HEAD,
      this build is not the one the cycle produced. Stop and rebuild rather than attributing a result
      to a fix that is not present.

    Precondition 5 in particular: verify credential state FRESH. Plan 34.5-20's SUMMARY records the
    prior session ending with GOG signed back IN and Amazon's password reset. Do not assume any aside
    survived. Any re-aside uses a NEW dated suffix, with `cmp` proving byte-identity before the
    original is removed. Never delete blind.
  </how-to-verify>
  <action>
    Present the contract's nine preconditions to the developer in order and collect each RESULT
    block's verbatim command output. Fill the contract's own RESULT slots as they come in; do not
    batch them at the end, and do not fill a slot from an assumption. If precondition 4, 7 or 8
    fires its hard STOP, halt the plan here and report which one blocked — do not proceed to Task 2.
  </action>
  <resume-signal>Paste all nine RESULT blocks with their verbatim command output. Then type "preconditions satisfied", or name which one blocked and why.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive all five gate items on real hardware</name>
  <what-built>
    A `pnpm tauri:dev` build with all nine preconditions satisfied, carrying this cycle's fixes: the
    capture round-trip no longer bounded at 60 s and its failures loud, the captured code routed
    through the post-login library refresh, Epic's selected fix, the keyring dedupe, and the login
    window's origin display and light interface style.
  </what-built>
  <how-to-verify>
    Work items 1 through 5 in order, exactly as `34.5-LIVE-GATE-RERUN-2.md` specifies them. For each,
    gather EVERY evidence clause the item lists — not a subset. Items 1-3 each require five clauses,
    including the two run 2's evidence lacked entirely: the `[useTauriOAuthLogin] runner=… phase=…`
    line, and the library populated in the UI **with the runner present in the Library filter
    options**.

    **Attempt items 4 and 5.** Both have been NOT ATTEMPTED on both prior runs, so neither research
    Assumption A2 nor Pitfall 2 has ever been tested. Their targets, invocations, prerequisites and
    evidence lines are inlined in the contract — nothing should need discovering mid-run. If an item
    genuinely cannot be attempted, record NOT ATTEMPTED with the specific reason; do not record it as
    FAIL or BLOCKED, and do not leave it silent.

    Recording rules, all three binding:
    - **Record what happened, not what should have happened.** If an item fails, record FAIL with its
      evidence. Do not soften, do not round, do not describe a partial result as a pass.
    - **A mutating call's own report is not proof of its effect.** A backend `status=captured` is
      necessary and not sufficient; run 2 had it for two runners and both items still FAILED.
    - **Record any NEW observation in the "New findings (this run)" section WITHOUT diagnosing it.**
      Both prior runs followed this rule and both produced actionable findings because of it.
      Diagnosis belongs to the next cycle, if there is one.
  </how-to-verify>
  <action>
    Walk the developer through items 1 through 5 in the contract's order, collecting every evidence
    clause each item names. Record results verbatim as they arrive. Do not soften a FAIL, do not
    infer an unobserved clause, and do not diagnose any new observation — new findings are recorded
    observation-only, exactly as both prior runs did. If the developer declines an item, record
    NOT ATTEMPTED with their stated reason rather than FAIL or BLOCKED.
  </action>
  <resume-signal>Paste, per item, the result and every evidence clause the contract names. Add any new observations to the findings list. Then type "items recorded".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verdict and resolve the falsification ledger</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` (whole file — this is the file being modified; its Verdict table, falsification section and arithmetic rule are authoritative)
    - `~/Library/Logs/GameLib/gamelib.log` — re-verify every pasted line against the file directly
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` D-08 (the no-partial-pass rule, verbatim)
  </read_first>
  <action>
    Fill the Result slots from Task 2's evidence, then the Verdict table, then the frontmatter.

    **Re-verify every pasted log line against `gamelib.log` yourself before recording it.** Plan
    34.5-20 found and corrected a factual error in gathered evidence exactly this way (a claimed "4
    legendary timeouts" the log showed as 3). If a pasted line disagrees with the file, record the
    FILE's version and note the discrepancy explicitly.

    Set frontmatter `status: complete`, `verdict`, `items_passed`, `items_failed`,
    `items_not_attempted`, `preconditions_satisfied`/`_blocked`/`_pending`, and `completed`.

    **Apply the arithmetic rule literally.** `items_passed + items_failed` must equal 5 on a clean
    completion. Any NOT ATTEMPTED item counts toward NEITHER, and the shortfall must be stated
    explicitly in the frontmatter `notes` — as both prior runs did — never quietly rounded to make
    the arithmetic look clean.

    **Apply D-08 literally.** 5 of 5 PASS closes the phase. Anything else does not: any item recorded
    FAIL means Phase 34.5 does not close, and the correct next step is another gap cycle inside 34.5.

    Resolve "What this gate falsifies" for all four standing claims. **Nothing is struck without the
    corresponding ITEM passing** — not on the strength of a sub-clause being independently confirmed
    while its parent item fails. Run 2 had to draw exactly this distinction for Assumption A1
    (confirmed at the mechanism level, claim still STANDING because item 3 failed on the library
    half); draw it again if it arises, and spell the distinction out in the document so a future
    reader cannot conflate the two.

    Write a "What this run nevertheless proved" section, as both prior runs did, distinguishing what
    genuinely advanced from what did not — honestly, in both directions.

    Before committing: grep the finished document for `token=`, `code=`, `Cookie` and `userdata` and
    confirm no credential value is present.
  </action>
  <verify>
    <automated>grep -qE "^verdict: (PASS|FAIL)$" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `verdict` is `PASS` or `FAIL`; `items_passed`, `items_failed` and `items_not_attempted` are all integers, and their stated arithmetic reconciles to 5 in the `notes` field.
    - All five Result slots are filled, each with the evidence clauses its item names.
    - The Verdict table has one row per item with its result and a one-line reason.
    - All four standing claims are resolved as STRUCK or STANDING, and every STRUCK claim names the ITEM that passed to earn it.
    - `git status --porcelain` shows `34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` UNMODIFIED — both remain the records of their own failed runs.
    - `grep -Ec "token=|Cookie|userdata" ` on the finished document returns 0, or every match is verifiably redacted; record the check in the SUMMARY.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Propagate the verdict everywhere it is tracked, without softening it</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` (the completed record — the sole source of truth for this task)
    - `.planning/phases/34.5-.../34.5-PORTED-CHANNELS.md` (the 38-row ledger, its 10 gate-dependent LIVE cells, and its `## Accepted residuals` section)
    - `.planning/phases/34.5-.../deferred-items.md` (items 1-12; do NOT renumber the existing entries)
    - `.planning/ROADMAP.md` § Phase 34.5 (the status paragraph and the plan checklist)
    - `.planning/STATE.md` (frontmatter and `## Current Position` only)
    - `.planning/phases/34.5-.../ported-channels-gate.py`
  </read_first>
  <action>
    Propagate the recorded verdict into every tracking document, mirroring what plan 34.5-21 did for
    run 2. A FAIL is recorded as a FAIL everywhere; a PASS is recorded only where the gate actually
    earned it.

    1. **`34.5-PORTED-CHANNELS.md`** — update all 10 gate-dependent `LIVE (item N)` proof cells to
       this run's result. Update `## Accepted residuals`: Assumption A1, Assumption A2 (both call
       sites), research Pitfall 2, and `R-34.5-G1-PKG`. Keep `R-34.5-G1-PKG` OPEN regardless of this
       run's outcome — it is the packaged asset root, and this gate runs `pnpm tauri:dev`; a dev PASS
       never covers it, and its home is the packaging work, not Phase 34.6. Then run
       `python3 ported-channels-gate.py` and its `--self-test`; both must exit 0.
    2. **`deferred-items.md`** — record the disposition of F-34.5-G6-01 through 06 (closed by this
       cycle, or still open with this run's evidence) and append any new findings the run recorded.
       Append under a new dated section; do not renumber items 1-12.
    3. **`.planning/IPC-PORT-INVENTORY.md`** — update the Phase 34.5 status paragraph to name all
       three gate runs and this one's verdict, pointing at `34.5-LIVE-GATE-RERUN-2.md` as evidence.
       Do NOT touch channel membership or the 38/3/16 arithmetic.
    4. **`.planning/ROADMAP.md`** — mark plans 34.5-22 through 34.5-31 `[x]`, annotate 34.5-31's entry
       with the verdict, and replace the Phase 34.5 status paragraph with this run's actual outcome
       and routing. **Phase 34.6's and Phase 35's dependency lines are eligible for editing ONLY on a
       clean 5/5 PASS.** On anything less, leave them exactly as they are — they correctly block on
       Phase 34.5's closure.
    5. **`.planning/STATE.md`** — hand-edit ONLY. Never use `gsd-sdk` state writes: the standing
       finding is that they revert `stopped_at`, mangle counters and splice status into historical
       blocks. Recount completed plans from the phase directory rather than incrementing blindly, and
       name the next action exactly once — `/gsd-verify-work` on a 5/5 PASS, or
       `/gsd-plan-phase 34.5 --gaps` on anything less. Verify with `git diff` that only the
       frontmatter and the `## Current Position` block changed; no historical block may be touched.

    Do not soften anything anywhere. If the run failed, every one of these documents says so.
  </action>
  <verify>
    <automated>python3 .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py &amp;&amp; python3 .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `ported-channels-gate.py` and its `--self-test` both exit 0 after the edit.
    - All 10 gate-dependent LIVE cells in `34.5-PORTED-CHANNELS.md` reflect this run's result; no cell still cites run 1 or run 2 as its current evidence.
    - `## Accepted residuals` carries entries for A1, A2, Pitfall 2 and `R-34.5-G1-PKG`, with `R-34.5-G1-PKG` recorded OPEN regardless of the verdict.
    - `deferred-items.md` records the disposition of all six F-34.5-G6 findings, appended under a new dated section, with items 1-12 unrenumbered.
    - `ROADMAP.md` shows plans 34.5-22..31 marked `[x]` and a Phase 34.5 status paragraph naming all three gate runs. On a non-5/5 result, `git diff` proves Phase 34.6's and Phase 35's dependency lines are untouched.
    - `git diff .planning/STATE.md` shows exactly two changed regions — frontmatter and `## Current Position` — and the next action is named exactly once.
    - The SUMMARY records the verdict in its first sentence, unsoftened.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live session → repository documents | Real Epic/GOG/Amazon session artefacts cross into a public-fork record |
| developer report → recorded verdict | A human summary can drift from what the log says |
| gate verdict → downstream phase gating | Phases 34.6 and 35 unblock on this verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-G6-02 | Information disclosure | pasted `gamelib.log` excerpts in the gate record | mitigate | Only the lines an item names may be quoted, redacted in place; Task 3's acceptance criteria require a pre-commit grep for `token=`, `Cookie`, `userdata` |
| T-34.5-G6-27 | Repudiation | a developer summary adopted as fact | mitigate | Task 3 re-verifies every pasted line against `gamelib.log` directly and records any discrepancy, following the precedent that caught the "4 vs 3 timeouts" error in run 2 |
| T-34.5-G6-37 | Elevation of privilege | a non-5/5 result unblocking Phase 34.6 or Phase 35 | mitigate | Task 4 makes those two dependency lines editable ONLY on a clean 5/5 PASS, pinned by a `git diff` acceptance criterion on anything less |
| T-34.5-G6-38 | Tampering | `gsd-sdk` state writes corrupting `STATE.md` | mitigate | Task 4 mandates hand-editing only, a recount from the phase directory rather than a blind increment, and a `git diff` proving only two regions changed |
| T-34.5-G6-36 | Repudiation | a sub-clause confirmation read as retiring its parent claim | mitigate | Task 3 requires every STRUCK claim to name the ITEM that passed; a confirmed sub-clause under a failing item strikes nothing |
| T-34.5-G6-30 | Elevation of privilege | a wrong `exe` value in a Steam VDF launch command | mitigate | Item 4's evidence requires reading the written `Exe` field and `run.sh` body back plus a `ps aux` proving the launched process is the Tauri shell binary and never bare `node` |
| T-34.5-G6-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages; it rebuilds from existing lockfiles only |
</threat_model>

<verification>
- All nine preconditions carry recorded RESULT blocks.
- All five items carry a result; `items_passed + items_failed + items_not_attempted` reconciles to 5.
- `ported-channels-gate.py` and `--self-test` exit 0.
- `34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` are byte-unchanged.
</verification>

<success_criteria>
The third blocking gate has run on real hardware with every item attempted and recorded, the verdict
is computed by the contract's own arithmetic and D-08's no-partial-pass rule, no standing claim is
struck without its item passing, and every tracking document agrees with the result — closing the
phase on 5/5, or routing to a fourth gap cycle on anything less.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-31-SUMMARY.md` when done
</output>
