---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 13
type: execute
wave: 4
depends_on: ["34.5-05", "34.5-06", "34.5-07", "34.5-08", "34.5-09", "34.5-10", "34.5-11", "34.5-12"]
files_modified:
  - src/backend/sidecar/__tests__/electronReachLedger.test.ts
  - src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts
autonomous: true
requirements: [REQ-34.5-10, REQ-34.5-13]

must_haves:
  truths:
    - "The electron-reach ledger's `ENTRY_POINTS` gains this slice's four registration modules, and `BASELINE_ELECTRON_REACHING_MODULES` is REGENERATED BY MEASUREMENT at execution time — never transcribed from planning prose."
    - "Every prior slice's measured baseline disagreed with its own planning-time prediction at least once (34.4 predicted two new entries and needed three), so a transcribed number is a defect, not a shortcut. If the measurement disagrees with this plan's prediction, THE MEASUREMENT WINS and the delta is reported as a planning gap."
    - "`save_sync.ts` is a CONFIRMED new entry (direct `electron` import, not yet baselined). The shortcuts modules and the `legendary`/`gog`/`nile` user and library files are ALREADY baselined via prior slices' transitive reach and must NOT be re-added as if new."
    - "The ledger is growth-only, not strict-equality, so decoupling work never turns it red — and its baseline array IS the Phase 35 cutover work-list."
    - "This plan runs in wave 4, after every code plan, specifically so the regeneration and both sweeps see the phase's COMPLETE diff. A ledger measured before the last code plan lands is a ledger that has to be measured twice."
    - "Sweep A targets 34.4's gate-item-2 failure mode: a stale `isTauri()` guard whose own comment asserted a premise a later plan had falsified, leaving a ported channel unreachable while every unit test, `tsc`, `cargo check` and the reach ledger stayed green."
    - "Sweep B targets 34.4-10's regression class: a bare/aliased import invisible to both `tsc --noEmit` and jest, but fatal to `npm start`."
    - "The completeness check that all 38 channels are registered lands HERE — this is the first point in the phase at which 'all 38' is a true statement."
  artifacts:
    - path: "src/backend/sidecar/__tests__/electronReachLedger.test.ts"
      provides: "Four new ENTRY_POINTS and a measured baseline"
      contains: "runnerAuthFlowRegistration"
    - path: "src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts"
      provides: "The all-38 completeness assertion across the four modules"
  key_links:
    - from: "src/backend/sidecar/__tests__/electronReachLedger.test.ts ENTRY_POINTS"
      to: "the four 34.5 registration modules"
      via: "join(REPO_ROOT, …) entries with a Phase 34.5 comment in the existing convention"
      pattern: "runnerAuthFlowRegistration|wineToolsFlowRegistration|shortcutsFlowRegistration|runnerMiscFlowRegistration"
---

<objective>
Measure what this phase actually reaches, prove all 38 channels are registered, and run the two
sweeps that caught real defects in the previous two phases.

Purpose: this slice's automated suite can be entirely green while a ported channel is unreachable
(34.4 gate item 2) or the Electron renderer no longer builds (34.4-10). Neither failure is visible
to `tsc`, jest or the reach ledger, so both need a deliberate check against this phase's own diff.

Output: a measured cutover work-list, a completeness proof, and two sweeps with their output on the
record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-RESEARCH.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-VALIDATION.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-06-SUMMARY.md

<interfaces>
The 38 channels, by module, as registered by plans 34.5-05 through 34.5-12:
  runnerAuthFlowRegistration (11): login, getUserInfo, isLoggedIn, getEpicGamesStatus,
    logoutLegendary, authGOG, logoutGOG(SEND), getAmazonLoginData, authAmazon, getAmazonUserInfo,
    logoutAmazon
  wineToolsFlowRegistration (9): runWineCommand, getAlternativeWine, wine.isValidVersion,
    installWineVersion, refreshWineVersionInfo, removeWineVersion, toggleDXVK, toggleDXVKNVAPI,
    toggleVKD3D
  shortcutsFlowRegistration (7): addShortcut(SEND), removeShortcut(SEND), processShortcut(SEND),
    shortcutsExists, addToSteam, removeFromSteam, isAddedToSteam
  runnerMiscFlowRegistration (11): getLegendaryVersion, getGogdlVersion, getCometVersion,
    getNileVersion, callTool, egsSync, getGOGLinuxInstallersLangs, syncSaves, syncGOGSaves,
    downloadRuntime, isRuntimeInstalled
  Totals: 34 handle + 4 send = 38.

Ledger file structure (src/backend/sidecar/__tests__/electronReachLedger.test.ts):
  ENTRY_POINTS array at L44 — 11 entries today, each block commented with its phase/plan/REQ
  BASELINE_ELECTRON_REACHING_MODULES — the measured, sorted, repo-relative array
  computeElectronReach() — the measurement function; prior plans printed it via a TEMPORARY
    statement removed before commit (see 34.3-07-SUMMARY and 34.4.1-06)
  requiredModules — the anti-degradation floor; extend it, never lower it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ENTRY_POINTS and regenerate the baseline by measurement</name>
  <files>src/backend/sidecar/__tests__/electronReachLedger.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/electronReachLedger.test.ts (full file — the header docstring's growth-only/subset semantics, the anti-vacuity tests, the "regenerate at execution time, not from planning-time prose" rule, the 11-entry `ENTRY_POINTS` array with its per-phase comment convention, `computeElectronReach()`, and the `requiredModules` floor)
    - .planning/phases/34.4.1-.../34.4.1-06-SUMMARY.md and .planning/phases/34.4-.../34.4-08-SUMMARY.md (the two precedents where the MEASUREMENT disagreed with the plan's prediction and the measurement won)
    - src/backend/save_sync.ts L1-20 (its direct `electron` import — the confirmed new edge this extension exists to capture)
    - .planning/REQUIREMENTS.md REQ-34.5-10 (the full obligation, including which files are ALREADY baselined and must not be re-added)
  </read_first>
  <action>
    Add all four of this slice's registration modules to `ENTRY_POINTS`, in one commented block
    matching the existing per-phase convention (`// Phase 34.5 Plan 13 (REQ-34.5-10, 34.3 D-10 /
    34.4 D-10 standing obligation): this slice's four registration modules.`):
      `src/backend/sidecar/runnerAuthFlowRegistration.ts`
      `src/backend/sidecar/wineToolsFlowRegistration.ts`
      `src/backend/sidecar/shortcutsFlowRegistration.ts`
      `src/backend/sidecar/runnerMiscFlowRegistration.ts`
    All four are mandatory. This plan is deliberately scheduled in wave 4 so the measurement sees the
    complete diff; omitting one would leave a curated-import edge unmeasured while the suite stayed
    green.

    Then REGENERATE `BASELINE_ELECTRON_REACHING_MODULES` by actually running
    `computeElectronReach()` through a temporary measurement statement that you REMOVE before
    committing. Do not hand-write any entry from this plan's prose.

    Record in the SUMMARY: the before/after module count, the before/after `visitedFiles.size`, the
    exact list of newly-reached modules, and an explicit statement of whether the measurement AGREED
    with the prediction. The prediction is: `save_sync.ts` is new; the shortcuts modules and the
    `legendary`/`gog`/`nile` user and library files are already baselined and appear as no-change.
    If the measurement disagrees, the MEASUREMENT WINS — add what it found with a reach-path comment
    naming the import chain, and flag the delta as a planning-time gap rather than absorbing it
    silently.

    Extend `requiredModules` with the new edge(s) so the anti-degradation floor rises. Never lower
    it, and never delete an existing entry to make a number line up.
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts` exits 0.
    - All four module names appear in `ENTRY_POINTS`: `grep -c "runnerAuthFlowRegistration\|wineToolsFlowRegistration\|shortcutsFlowRegistration\|runnerMiscFlowRegistration" src/backend/sidecar/__tests__/electronReachLedger.test.ts` returns at least 4.
    - `save_sync.ts` appears in the regenerated baseline.
    - No temporary measurement statement remains: record the count of `computeElectronReach()` occurrences before and after in the SUMMARY and show they match.
    - The SUMMARY reports before/after module count, before/after `visitedFiles.size`, the new-module list, and an explicit agreed/disagreed verdict against the prediction.
    - `requiredModules` gained at least one entry and lost none: `git diff` on that array shows no deletions.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts</automated>
  </verify>
  <done>The Phase 35 cutover work-list reflects what this phase actually reaches, measured rather than predicted, with any disagreement reported instead of absorbed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove all 38 channels are registered with the right kind</name>
  <files>src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts (as written by plan 34.5-04 — its growth-tolerant containment pin and the comment stating the completeness check belongs to THIS plan)
    - .planning/IPC-PORT-INVENTORY.md § "Phase 34.5" (the 57-name list and plan 34.5-03's reconciliation note — the source of truth for which 38 are in scope)
    - src/backend/main.ts, src/backend/shortcuts/ipc_handler.ts, src/backend/tools/ipc_handler.ts, src/backend/utils/ipc_handler.ts, src/backend/wine/manager/ipc_handler.ts, src/backend/wine/runtimes/ipc_handler.ts (the registration-kind ground truth for all 38)
    - src/common/types/sidecarTransport.ts (`UNPORTED_CHANNEL_MARKER` — the machinery that must still catch the 3 dropped and 16 deferred)
  </read_first>
  <action>
    Convert the growth-tolerant containment pin into a completeness assertion now that every cluster
    plan has landed. Keep the containment property (nothing outside a module's declared set) and ADD:

      1. Exactly 34 channels across the four modules are in `handlerRegistry` and exactly 4 are in
         `listenerRegistry`, and the send set is precisely
         `['logoutGOG', 'addShortcut', 'processShortcut', 'removeShortcut']`. Assert set equality,
         not just size — a size-only check passes when two channels swap kinds.
      2. Per-module counts: 11 / 9 / 7 / 11.
      3. SEAM Invariant B: the 3 DROPPED zoom channels (`authZoom`, `getZoomUserInfo`, `logoutZoom`)
         and the 16 DEFERRED channels (EOS overlay 8, SteamGridDB 5, winetricks 3) are NOT registered
         by any of the four modules, and therefore still reject with `UNPORTED_CHANNEL_MARKER`. List
         all 19 names explicitly in the test — this is the assertion that keeps "drop" and "defer"
         honest, and its whole point is that no new code exists for them.

    Add a comment stating that this suite is the phase's completeness gate and that
    `34.5-PORTED-CHANNELS.md` (plan 34.5-14) declares the same 38 rows — the two must agree, and
    plan 34.5-14's gate script cross-checks them.
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/runnerSliceRegistration.test.ts` exits 0.
    - A set-equality assertion exists for the 4 send channels (not a size check).
    - Per-module count assertions exist for 11 / 9 / 7 / 11, totalling 38.
    - All 19 dropped-or-deferred channel names are listed and asserted absent from both registries.
    - The SUMMARY records the actual measured counts, and states whether they matched 34/4/38 on the first run.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/runnerSliceRegistration.test.ts</automated>
  </verify>
  <done>All 38 channels are proven registered with the correct kind, and the 19 that must keep rejecting are proven still to reject.</done>
</task>

<task type="auto">
  <name>Task 3: Run both Discretion sweeps and the full green check</name>
  <files>src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts</files>
  <read_first>
    - .planning/STATE.md L37-100 (34.4's gate item 2 failure and 34.4-10's bare-import regression — the two failure modes these sweeps target, in the words of the sessions that hit them)
    - .planning/phases/34.4.1-.../34.4.1-06-PLAN.md Task 3 (the sweep procedure this task follows: enumerate the phase's own diff, classify every hit, record every command AND its output)
    - .planning/phases/34.5-.../34.5-VALIDATION.md § "Sampling Rate" and § "Validation Sign-Off" (the `cargo check` manual-obligation item)
    - .planning/REQUIREMENTS.md REQ-34.5-13 (the behavioural additive/reversible invariant and both named sweeps)
  </read_first>
  <action>
    This task produces EVIDENCE. Record every command and its OUTPUT in the SUMMARY. If a sweep finds
    something, fix it here and say so.

    Sweep A — stale `isTauri()` guard against THIS phase's own diff:
      1. `git diff --name-only <phase base commit>...HEAD` to enumerate this phase's touched files.
      2. `grep -rn "isTauri" <those files>`, plus the frontend surfaces that call this slice's
         channels: the login/WebView screens, the settings screens that call the Wine channels, and
         the game-context menus that call the shortcuts channels. Locate those call sites by
         searching the frontend for each of the 38 channel names rather than by guessing directories.
      3. Classify EVERY hit in the SUMMARY as (i) a correct transport-routing guard in
         `preload/ipc.ts` / `preload/tauriTransport.ts` / `preload/api/helpers.ts`, (ii) a deliberate
         new guard this phase added, or (iii) a STALE feature guard that short-circuits a channel
         this phase just ported. Any (iii) is a defect: fix it and record the fix.
      4. For each of the 38 channels, name in the SUMMARY the frontend call site that reaches it and
         confirm no early return sits between the call site and the transport. This is the check
         34.4's gate item 2 proved a green suite cannot substitute for.

    Sweep B — bare/aliased import build regression:
      1. Run `npm start` and capture the line proving the Electron renderer dev server compiles.
      2. Run `pnpm tauri:dev` far enough to prove the frontend is served; capture the line. Read the
         sidecar's own output from `~/Library/Logs/GameLib/gamelib.log`, NOT the terminal — stdout is
         the RPC pipe.
      3. `grep -rn "^import .*from '\(preload\|common\|backend\|frontend\)/"` across this phase's
         changed files and confirm each alias resolves in the Vite config, or convert it to a
         relative import.

    Finally run and record, verbatim and unrounded: `npm run test:ci`, `npx tsc --noEmit`,
    `cd src-tauri && cargo check`, `cargo test`. Record the exit code AND the pass/fail counts for
    each. A known-flaky suite is reported as flaky with its name and signature, never rounded to
    green — STATE.md records a prior plan whose acceptance criterion was not met on exactly this
    half and it must not be repeated silently.
  </action>
  <acceptance_criteria>
    - `npm run test:ci`, `npx tsc --noEmit`, `cargo check` and `cargo test` are all run and their exit codes recorded verbatim in the SUMMARY, with pass/fail counts.
    - The SUMMARY contains a classification line for EVERY `isTauri()` hit found in this phase's diff and in the frontend call sites of the 38 channels.
    - The SUMMARY names a frontend call site for each of the 38 channels, or states explicitly which channels have no frontend caller and why that is expected.
    - The SUMMARY records the actual `npm start` and `pnpm tauri:dev` output lines proving both builds compile, and states that sidecar output was read from `gamelib.log`.
    - `cargo check` is recorded as a MANUAL result with a note that no `cargo` step exists in `.github/workflows/*.yml`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:ci && npx tsc --noEmit && cd src-tauri && cargo check && cargo test</automated>
  </verify>
  <done>Both builds compile, both suites are recorded honestly rather than rounded, and every `isTauri()` guard on this phase's surface is classified with a named call site per channel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar curated import graph → Electron runtime | The reach ledger is the only measurement of what still crosses this boundary, and its baseline is the Phase 35 cutover work-list |
| green test suite → shipped behaviour | Both sweeps exist because a fully-green suite has twice coexisted with a real, user-visible defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-49 | Repudiation | a transcribed rather than measured reach baseline | mitigate | Baseline regenerated by running `computeElectronReach()`; the SUMMARY reports before/after counts and an explicit agreed/disagreed verdict; the measurement wins over the prediction on disagreement. |
| T-34.5-50 | Repudiation | a ported channel left unreachable behind a stale `isTauri()` guard | mitigate | Sweep A classifies every hit in this phase's diff and names a frontend call site per channel — the exact defect 34.4's gate item 2 caught while `tsc`, jest, `cargo check` and the ledger were all green. |
| T-34.5-51 | Denial of Service | a bare/aliased import fatal to `npm start` | mitigate | Sweep B runs BOTH dev servers, because `tsc --noEmit` and jest structurally cannot see this class (34.4-10's regression). |
| T-34.5-52 | Repudiation | a channel silently registered with the wrong kind | mitigate | Set-equality assertion on the 4 send channels plus per-module counts of 11/9/7/11 — a size-only check would pass when two channels swap kinds. |
| T-34.5-53 | Repudiation | a dropped or deferred channel quietly gaining an implementation | mitigate | All 19 dropped-or-deferred names asserted absent from both registries, preserving SEAM Invariant B — "drop" and "defer" mean let it reject and say so, not build a degradation path. |
| T-34.5-54 | Repudiation | a flaky suite rounded to green | mitigate | Exit codes and pass/fail counts recorded verbatim; a flake is named with its signature. STATE.md records the prior instance this rule exists to prevent. |
| T-34.5-SC | Tampering | npm/cargo installs | accept | Zero new packages in either ecosystem. |
</threat_model>

<verification>
- `cd src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts sidecar/__tests__/runnerSliceRegistration.test.ts` exits 0.
- `npm run test:ci`, `npx tsc --noEmit`, `cargo check`, `cargo test` all recorded with verbatim exit codes.
- `npm start` and `pnpm tauri:dev` both compile, with the proving output lines captured.
- No temporary measurement statement remains in the ledger test.
</verification>

<success_criteria>
The cutover work-list is measured, all 38 channels are proven registered with the right kind, the 19
dropped-or-deferred channels are proven still to reject, and the two defect classes that a green
suite cannot see have both been swept against this phase's own diff.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-13-SUMMARY.md` when done
</output>
