---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 06
type: execute
wave: 2
depends_on: ["34.5-04"]
files_modified:
  - src/backend/storeManagers/legendary/user.ts
  - src/backend/storeManagers/legendary/__tests__/user.test.ts
  - src/backend/sidecar/runnerAuthFlowRegistration.ts
  - src/backend/sidecar/__tests__/runnerAuthFlows.test.ts
autonomous: true
requirements: [REQ-34.5-04]

must_haves:
  truths:
    - "The seven Epic and GOG auth/sign-out channels are registered on the sidecar: `login`, `getUserInfo`, `isLoggedIn`, `getEpicGamesStatus`, `logoutLegendary`, `authGOG`, `logoutGOG`."
    - "`logoutGOG` is registered with `ipcMain.on`, NOT `ipcMain.handle` — it is `addListener` at `main.ts:880` while its two sibling sign-outs are `addHandler`. A mis-registered send channel fails SILENTLY under the sidecar, and a silently-broken sign-out is a repudiation risk."
    - "`GOGUser.logout()` is SYNCHRONOUS (`gog/user.ts:140`, no `async`), so its fire-and-forget body uses try/catch — not `.catch()`, which would be a no-op on a non-promise."
    - "PLAN-TIME FINDING, verified against source: `LegendaryUser.logout()` (`legendary/user.ts:71`) calls `session.fromPartition('persist:epicstore')` and then five `ses.clear*()` methods. `electronStub.session.fromPartition` returns `{}` — so under the sidecar the FIRST of those calls throws a TypeError, aborting the function before `configStore.delete('userInfo')` and `clearCache('legendary')` ever run. Porting this channel verbatim ships a sign-out that leaves profile state behind."
    - "The credential-side cleanup is made UNABORTABLE and runs on every path; the cookie-side cleanup becomes a guarded best-effort step — the exact ordering and shape 34.4.1-06 established for `humbleDisconnect` (ASVS V3)."
    - "Under Tauri the Epic cookies land in the app-wide jar (inherited T-34.4.1-47), so the replacement clear is DOMAIN-SCOPED to Epic's own hosts and never a blanket wipe — a blanket clear would sign the user out of Humble, GOG and Amazon too."
    - "Electron behaviour is byte-equivalent: when the real `session` API is present, the same five clear calls run in the same order."
  artifacts:
    - path: "src/backend/storeManagers/legendary/user.ts"
      provides: "A logout whose credential cleanup cannot be aborted by an unavailable session API, plus a domain-scoped Tauri cookie clear"
      contains: "getLoginWindowSeam"
    - path: "src/backend/sidecar/runnerAuthFlowRegistration.ts"
      provides: "The 7 Epic + GOG auth channels, 6 handle and 1 send"
      contains: "ipcMain.on('logoutGOG'"
    - path: "src/backend/sidecar/__tests__/runnerAuthFlows.test.ts"
      provides: "Bidirectional registration-kind cross-check including the logoutGOG send asymmetry"
  key_links:
    - from: "src/backend/sidecar/runnerAuthFlowRegistration.ts"
      to: "LegendaryUser / GOGUser"
      via: "curated named imports after the load-bearing `import '../storeManagers'`"
      pattern: "LegendaryUser|GOGUser"
    - from: "src/backend/storeManagers/legendary/user.ts logout()"
      to: "LoginWindowSeam.clearCookies"
      via: "guarded best-effort step after the CLI auth delete, before the unconditional configStore cleanup"
      pattern: "clearCookies"
---

<objective>
Port Epic's and GOG's auth and sign-out channels onto the sidecar, and fix the sign-out defect that
porting them verbatim would ship.

Purpose: these are the channels the declared-blocked login UI names today. They are also where the
phase's only Repudiation threat lives — one of the three sign-outs is send-kind and fails silently,
and Epic's sign-out currently aborts halfway through under the sidecar's session stub.

Output: seven registered channels, a sign-out whose credential cleanup cannot be skipped, and a
test that would catch either failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-RESEARCH.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-06-SUMMARY.md

<interfaces>
Electron registration ground truth (verified by grep, all in src/backend/main.ts):
  L772  addHandler('getEpicGamesStatus', async () => isEpicServiceOffline())
  L868  addHandler('getUserInfo',        async () => LegendaryUser.getUserInfo())
  L875  addHandler('isLoggedIn',         () => LegendaryUser.isLoggedIn())
  L877  addHandler('login',              async (event, sid) => LegendaryUser.login(sid))
  L878  addHandler('authGOG',            async (event, code) => GOGUser.login(code))
  L879  addHandler('logoutLegendary',    () => LegendaryUser.logout())
  L880  addListener('logoutGOG',         () => GOGUser.logout())      <-- SEND KIND

Method signatures (verified):
  LegendaryUser.login(sid): Promise<...>        LegendaryUser.logout(): Promise<void>  (async)
  LegendaryUser.isLoggedIn(): boolean           LegendaryUser.getUserInfo(): UserInfo | undefined
  GOGUser.login(code): Promise<...>  (async)    GOGUser.logout(): void                 (SYNCHRONOUS)
  isEpicServiceOffline: exported from src/backend/utils.ts (export block ~L1697)

The seam already built by 34.4.1 (src/backend/humble/loginWindowSeam.ts):
  getLoginWindowSeam(): LoginWindowSeam | null      // null in the Electron build
  seam.open(url, { visible, userAgent }): Promise<string>   // resolves the window label
  seam.clearCookies(label, domain): Promise<number>          // domain-scoped, returns count deleted
  seam.close(label): Promise<boolean>

The sidecar's session stub (src/backend/sidecar/electronStub.ts, ~L585):
  session.fromPartition(_partition) -> logs a warning and returns `{}` — it has NO clear* methods.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make LegendaryUser.logout()'s credential cleanup unabortable and domain-scope its Tauri clear</name>
  <files>src/backend/storeManagers/legendary/user.ts, src/backend/storeManagers/legendary/__tests__/user.test.ts</files>
  <read_first>
    - src/backend/storeManagers/legendary/user.ts (full file — `logout()` at L55-79 with its `session.fromPartition('persist:epicstore')` block and five `ses.clear*()` calls, `isLoggedIn()`, `getUserInfo()`, `login()`, and the `import { session } from 'electron'` at line 7)
    - src/backend/humble/user.ts `disconnect()` (the 34.4.1-06 precedent: credential store cleared FIRST and unconditionally, then a guarded best-effort `wipeSteps` loop, then the seam branch that opens a hidden window, calls `clearCookies`, and closes it in a `finally`)
    - src/backend/sidecar/electronStub.ts § "session (D-09, accepted no-op)" (the `fromPartition` stub returning `{}` — this is the source of the defect)
    - src/backend/humble/loginWindowSeam.ts L76-118 (`LoginWindowSeam`'s `open`/`clearCookies`/`close` signatures and `getLoginWindowSeam()`'s null-in-Electron contract)
    - .planning/phases/34.4.1-.../34.4.1-06-SUMMARY.md (the domain-scoped-never-blanket rule and why `clear_all_browsing_data` is forbidden repo-wide)
  </read_first>
  <action>
    Restructure `LegendaryUser.logout()` so the ordering guarantees survive an unavailable session
    API. Keep the CLI `auth --delete` call and its existing early-return-on-error behaviour exactly
    as they are — that is the real credential revocation and it already works under the sidecar.

    Replace the bare five-call session block with the `humble/user.ts disconnect()` shape:
      - Electron path (`getLoginWindowSeam() === null` AND the resolved `session.fromPartition`
        result actually exposes the clear methods): run the SAME five calls —
        `clearStorageData`, `clearCache`, `clearAuthCache`, `clearHostResolverCache`, `clearData` —
        in the SAME order, each inside the guarded step loop so one failure cannot abort the rest.
        Electron behaviour must remain equivalent.
      - Tauri path (a seam is installed): open a HIDDEN login window on Epic's login origin, call
        `seam.clearCookies(label, <epic host>)`, log the returned COUNT only (never a cookie name,
        domain or value), and close the window in a `finally` so no path leaves it open. Determine
        the correct host by reading which origin `legendary`'s login flow actually navigates to
        (`oauthLoginCapture.ts`'s `legendary` case and the login URL the frontend opens) — do not
        guess, and record the chosen host and your evidence in the SUMMARY.
      - Neither path may call anything resembling a blanket wipe. `clear_all_browsing_data` must
        remain absent from the entire repo.

    Move `configStore.delete('userInfo')` and `clearCache('legendary')` so they run
    UNCONDITIONALLY after the guarded block, on every path including the one where every cookie step
    failed. Add a comment stating the rule explicitly: the credential-side cleanup is the security
    boundary (ASVS V3) and the cookie-side cleanup is best-effort; a sign-out that revoked the CLI
    session but left `userInfo` behind is worse than one that left a cookie behind.

    Create `src/backend/storeManagers/legendary/__tests__/user.test.ts` (no test file exists for this
    module today) covering: the CLI-error early return is unchanged; with a session object that has
    no `clear*` methods, `logout()` does NOT throw and `configStore.delete('userInfo')` STILL runs;
    with a seam installed, `clearCookies` is called with the Epic host and the window is closed even
    when the clear rejects; with a full Electron-shaped session, all five clear calls run in order;
    and no test path invokes a blanket clear.
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest storeManagers/legendary/__tests__/user.test.ts` exits 0 with at least 5 cases.
    - A test proves `configStore.delete('userInfo')` runs when the session object exposes no `clear*` methods — this is the defect this task fixes and it must fail if the fix is reverted.
    - A test asserts the call ORDER: the CLI `auth --delete` runs before any cookie step, and the cookie steps run before `configStore.delete`.
    - `grep -rc "clear_all_browsing_data" src src-tauri/src` returns 0 for every file.
    - The SUMMARY names the Epic cookie host chosen for `clearCookies` and the source evidence for it.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && cd src/backend && npx jest storeManagers/legendary/__tests__/user.test.ts</automated>
  </verify>
  <done>Epic sign-out always clears the stored profile, clears only Epic's cookies under Tauri, and behaves identically to before under Electron.</done>
</task>

<task type="auto">
  <name>Task 2: Register the seven Epic and GOG channels</name>
  <files>src/backend/sidecar/runnerAuthFlowRegistration.ts</files>
  <read_first>
    - src/backend/sidecar/runnerAuthFlowRegistration.ts (the declared contract from plan 34.5-04, including the load-bearing `import '../storeManagers'` and its comment)
    - src/backend/sidecar/steamAuthFlowRegistration.ts (full file — the argument-unpacking convention at L148-157, the `redeemSteamKey` trust-boundary validation at the ported `main.ts:906-917` block, and the `ipcMain.on('logoutSteam', …)` send body at L183-187 with its G-30-01 comment)
    - src/backend/sidecar/humbleLoginFlowRegistration.ts L285-315 (`void (async () => { try {…} catch { logSendFailure(…) } })()` — the fire-and-forget guard shape, and `logSendFailure`'s definition)
    - src/backend/main.ts L764-772 and L866-886 (the seven Electron bodies verbatim, and the `isEpicServiceOffline` import)
    - src/backend/storeManagers/gog/user.ts L140-152 (`logout()` — confirm for yourself it is SYNCHRONOUS before choosing try/catch over `.catch()`)
  </read_first>
  <action>
    Fill `registerRunnerAuthFlows()` with seven registrations.

    Six `ipcMain.handle` channels, each delegating exactly as its Electron source does:
      - `getEpicGamesStatus` → `isEpicServiceOffline()` (import from `../utils`).
      - `getUserInfo` → `LegendaryUser.getUserInfo()`.
      - `isLoggedIn` → `LegendaryUser.isLoggedIn()`.
      - `logoutLegendary` → `LegendaryUser.logout()`.
      - `login` → `LegendaryUser.login(sid)`, unpacking `args[0]`.
      - `authGOG` → `GOGUser.login(code)`, unpacking `args[0]`.

    For `login` and `authGOG`, add the trust-boundary validation the `redeemSteamKey` precedent
    establishes: the payload is untrusted at runtime despite its type contract. Reject when `args[0]`
    is not a non-empty string, returning the same shape the runner's own failure return uses (read
    `LegendaryUser.login`/`GOGUser.login` to determine that shape — do not invent a new one) and log
    a rejection line that does NOT include the value, since an authorization code is a credential.

    One `ipcMain.on` channel — `logoutGOG`. This is the phase's Repudiation surface. Register it with
    `ipcMain.on`, never `ipcMain.handle`, and wrap `GOGUser.logout()` in a synchronous try/catch
    inside the fire-and-forget guard, logging any failure via `logSendFailure`. Do NOT attach
    `.catch()` — `GOGUser.logout()` returns `void`, so `.catch()` would be a TypeError, and its
    failure would then be invisible in exactly the way a send channel already is. Add a comment
    naming the asymmetry: three sign-outs, three runners, and only this one is send-kind; it is
    inherited Electron behaviour, deliberately preserved, and the registration-kind test is what
    keeps it honest.

    Import `LegendaryUser` and `GOGUser` by name from `../storeManagers/legendary/user` and
    `../storeManagers/gog/user`, AFTER the load-bearing `import '../storeManagers'` — the same
    ordering discipline `steamAuthFlowRegistration.ts` documents, which exists to avoid the
    re-entrant "X is not a constructor" mid-evaluation crash. Verify that ordering actually matters
    here by reading each runner's `user.ts` import chain, and record the finding in the SUMMARY
    either way.
  </action>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle(" src/backend/sidecar/runnerAuthFlowRegistration.ts` returns 6.
    - `grep -c "ipcMain.on('logoutGOG'" src/backend/sidecar/runnerAuthFlowRegistration.ts` returns 1.
    - `grep -c "ipcMain.handle('logoutGOG'" src/backend/sidecar/runnerAuthFlowRegistration.ts` returns 0.
    - `grep -c "GOGUser.logout().catch" src/backend/sidecar/runnerAuthFlowRegistration.ts` returns 0 — the sync method is not treated as a promise.
    - The `login` and `authGOG` handlers each reject a non-string / empty payload, and neither logs the payload value.
    - `import '../storeManagers'` appears BEFORE any `../storeManagers/<runner>/user` import in the file.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Six invoke channels and one correctly-registered send channel, with both credential-bearing payloads validated at the boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Bidirectional registration-kind test for the Epic and GOG channels</name>
  <files>src/backend/sidecar/__tests__/runnerAuthFlows.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/humbleLoginFlows.test.ts L140-175 (Describe 1 — the exact bidirectional template; skip Describes 2-4)
    - src/backend/sidecar/__tests__/steamAuthFlows.test.ts L40-70 (the `jest.mock('os', …)` disposable-homedir block plus the `electron`/`electron-store` real-shim mocks — MANDATORY for any test that drives the real `configStore` chain, which this one does via `legendaryConfig`/`gogdlConfig`/`gog_store`)
    - src/backend/sidecar/runnerAuthFlowRegistration.ts (as written by Task 2)
    - src/backend/main.ts L772, L868, L875, L877, L878, L879, L880 (the kind ground truth)
  </read_first>
  <action>
    Create `runnerAuthFlows.test.ts`, calling `registerRunnerAuthFlows()` ONCE at file scope.

    Apply the disposable-homedir harness before anything else: `jest.mock('os', …)` redirecting
    `homedir()` into a per-pid tmp directory, plus the `electron` → `../electronStub` and
    `electron-store` → `../fileStore` mocks. This suite reaches the real `configStore` chain
    (`legendaryConfig`, `gogdlConfig`, `gog_store`), and project memory records a prior suite
    clobbering the developer's real application-support directory. Never disable
    `jest.setupContainment.ts`.

    Assertions:
      1. Bidirectional kind for all seven channels — the six handle channels are in
         `handlerRegistry` and absent from `listenerRegistry`; `logoutGOG` is in `listenerRegistry`
         exactly once and absent from `handlerRegistry`. Name REQ-34.5-04 in each test name.
      2. A dedicated case for the asymmetry, commented as such: `logoutLegendary` and (once plan
         34.5-10 lands) `logoutAmazon` are handle-kind while `logoutGOG` is send-kind, and a
         regression that "tidied" them into consistency would break the renderer silently.
      3. `login` and `authGOG` reject a non-string payload and an empty string without invoking the
         underlying runner method (assert the mocked `LegendaryUser.login`/`GOGUser.login` were not
         called), and the rejection log does not contain the payload value.
      4. `logoutGOG`'s listener body does not throw when `GOGUser.logout()` throws synchronously —
         invoke the registered listener directly with a throwing mock and assert no exception
         escapes and a failure was logged.
      5. None of the four DROPPED zoom channels (`authZoom`, `getZoomUserInfo`, `logoutZoom`) and
         none of Amazon's four channels are registered by this module — Amazon belongs to plan
         34.5-10 and zoom is dropped permanently (D-02).
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/runnerAuthFlows.test.ts` exits 0.
    - The suite contains at least 14 kind assertions (7 channels × both directions).
    - The `jest.mock('os', …)` disposable-homedir block is present; `grep -c "homedir" src/backend/sidecar/__tests__/runnerAuthFlows.test.ts` returns at least 1 and no assertion targets a path outside the tmp redirect.
    - A case exists proving a synchronously-throwing `GOGUser.logout()` does not escape the listener.
    - A case asserts `authZoom`, `getZoomUserInfo` and `logoutZoom` are absent from both registries.
    - `cd src/backend && npx jest sidecar/__tests__/runnerSliceRegistration.test.ts` still exits 0, and `git diff --name-only` does NOT list that file — this plan must not edit it.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/runnerAuthFlows.test.ts sidecar/__tests__/runnerSliceRegistration.test.ts</automated>
  </verify>
  <done>The send/handle split is pinned in both directions, the sign-out cannot fail invisibly, and no dropped or out-of-plan channel crept in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → `login`/`authGOG` payload | Carries an OAuth authorization code / SID — a credential, untrusted at runtime despite its TypeScript type |
| sidecar → runner CLI config dirs (`legendaryConfig`, `gogdlConfig`, `gog_store`) | Where the real tokens live; the port's risk here is path resolution, not encryption (D-12) |
| app-wide cookie jar → per-runner sign-out | Epic cookies now share the jar with Humble, GOG and Amazon (inherited T-34.4.1-47) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-18 | Repudiation | `logoutGOG` send channel failing silently | mitigate | Registered with `ipcMain.on` and cross-checked in BOTH directions by test; body uses a synchronous try/catch (not `.catch()`, which would itself throw on a void return) and logs via `logSendFailure`. A test drives a throwing `GOGUser.logout()` and asserts nothing escapes. |
| T-34.5-19 | Repudiation | Epic sign-out aborting before `configStore.delete('userInfo')` | mitigate | The credential-side cleanup is moved after a guarded best-effort block and runs unconditionally; a dedicated test proves it still runs when the session object exposes no `clear*` methods. |
| T-34.5-20 | Information Disclosure | a blanket cookie clear signing the user out of other storefronts | mitigate | Domain-scoped `clearCookies(label, <epic host>)` only; `clear_all_browsing_data` grep-asserted absent repo-wide (inherited T-34.4.1-47 discipline). |
| T-34.5-21 | Information Disclosure | logging an authorization code or SID | mitigate | Validation-rejection and clear-step log lines record only counts and reasons; tests assert the payload value is absent from the emitted string. |
| T-34.5-22 | Spoofing | a malformed/empty credential payload reaching the runner CLI | mitigate | ASVS V5 type-and-emptiness validation at the handler boundary, following the ported `redeemSteamKey` precedent; the runner method is not invoked on a rejected payload. |
| T-34.5-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `cd src/backend && npx jest sidecar/__tests__/runnerAuthFlows.test.ts storeManagers/legendary/__tests__/user.test.ts` exits 0.
- `grep -c "ipcMain.on('logoutGOG'" src/backend/sidecar/runnerAuthFlowRegistration.ts` returns 1.
- `grep -rc "clear_all_browsing_data" src src-tauri/src` returns 0.
</verification>

<success_criteria>
Epic and GOG can authenticate and sign out through the sidecar; the one send-kind sign-out is
registered as a send and proven not to swallow failures invisibly; and Epic's sign-out clears the
stored profile even when the cookie half of it cannot run.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-06-SUMMARY.md` when done
</output>
