---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 21
type: execute
wave: 5
depends_on: [34.5-20]
gap_closure: true
closes_gaps: [G-6, G-4, G-5]
requirements: [REQ-34.5-11, REQ-34.5-12, REQ-34.5-13]
autonomous: true
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
  - .planning/IPC-PORT-INVENTORY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md

must_haves:
  truths:
    - "Every `LIVE (item N)` proof-level cell in `34.5-PORTED-CHANNELS.md` matches the re-run's recorded result — no cell still carries a state from the superseded run"
    - "`ported-channels-gate.py` and its `--self-test` both exit 0 after every edit"
    - "The 38 + 3 + 16 = 57 arithmetic still reconciles against `IPC-PORT-INVENTORY.md`"
    - "ROADMAP and STATE record the re-run's actual verdict and the correct next step — phase closure on a clean 5/5, another gap cycle otherwise"
    - "Residuals A1, A2, research Pitfall 2 and `R-34.5-G1-PKG` each carry their post-run state, struck only where an item PASSed"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md"
      provides: "The declared 38 with LIVE cells reflecting the re-run"
      contains: "LIVE (item"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 34.5 block reflecting the gap cycle's plans and the phase's real status"
      contains: "34.5-21-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Current position, blocker state and next step"
      contains: "34.5"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN.md verdict"
      to: "34.5-PORTED-CHANNELS.md LIVE cells"
      via: "per-item result copied into each gate-dependent proof-level cell"
      pattern: "LIVE \\(item \\d\\) — (PASS|FAIL|PENDING)"
    - from: ".planning/ROADMAP.md Phase 34.5 block"
      to: ".planning/IPC-PORT-INVENTORY.md Phase 34.5 section"
      via: "the 38/3/16 = 57 arithmetic the gate script cross-checks"
      pattern: "57"
---

<objective>
Propagate the re-run's verdict everywhere the failed run's result is currently recorded, and route
the phase correctly.

`34.5-PORTED-CHANNELS.md` currently carries FAIL in every `LIVE (item 1|2|3|5)` cell and PENDING in
item 4's two cells; `IPC-PORT-INVENTORY.md`, `ROADMAP.md` and `STATE.md` all record a phase that
does not close. Exactly one of two outcomes is now true, and this plan must handle whichever
happened without softening it:

- **5 of 5 PASS** — the gate is met, the phase closes, and each standing claim becomes eligible to
  be struck, item by item.
- **Anything less** — D-08 applies unchanged: the phase still does not close and the next step is
  another gap cycle. Record it as plainly as `34.5-15-SUMMARY.md` recorded the first failure.

Purpose: leave no document claiming more, or less, than the gate observed.
Output: propagated channel list, deferred items, inventory, roadmap and state; gate script green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-20-SUMMARY.md
@.planning/STATE.md

<interfaces>
<!-- Contracts the executor needs. -->

`ported-channels-gate.py` enforces, among others:
- proof-level cells must match `^(unit|unit \+ LIVE \(item \d+\) — (PENDING|PASS|FAIL))$` — a cell
  may be flipped to PASS, but all explanatory prose belongs in the RIDERS cell, never in the
  proof-level cell
- the set of channels credited with `LIVE (item 4)` must be EXACTLY `{addToSteam, addShortcut}`,
  each naming its own call site (`nonesteamgame.ts:258`, `shortcuts.ts:227`) in Riders
- `## Accepted residuals` must still contain the literal markers `ctrl+r`, `T-34.4.1-47`,
  `Assumption A1`, `Assumption A2`
- `## Obligations closed and opened` must still cite `T-34.4.1-44b`
- the 38/3/16 = 57 arithmetic must set-reconcile against `IPC-PORT-INVENTORY.md`

The 10 gate-dependent rows: item 1 → `getUserInfo`, `isLoggedIn`, `login`; item 2 → `authGOG`;
item 3 → `getAmazonLoginData`, `authAmazon`, `getAmazonUserInfo`; item 4 → `addToSteam`,
`addShortcut`; item 5 → `runWineCommand`.

`STATE.md`: `gsd-sdk` state writes are known to corrupt this file (they revert `stopped_at`, mangle
counters, and splice status into historical blocks). Hand-edit it and re-read after writing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Propagate the verdict into the declared channel list and its residuals</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md</files>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` — the filled Result slots and the Verdict table; the only source of truth for every cell edited here
    - `.planning/phases/34.5-.../34.5-PORTED-CHANNELS.md` IN FULL — the header note about the failed run, the 10 gate-dependent rows, `## Accepted residuals`, `## Automated vs live proof boundary`
    - `.planning/phases/34.5-.../ported-channels-gate.py` — checks 3, 4, 7, 8 and 9 in particular, so an edit cannot accidentally trip them
    - `.planning/phases/34.5-.../deferred-items.md` — the existing numbering, so new entries append rather than renumber
    - `.planning/phases/34.5-.../34.5-APP-ROOT-SWEEP.md` § 3 — the `R-34.5-G1-PKG` residual's wording
  </read_first>
  <action>
    Update `34.5-PORTED-CHANNELS.md` from `34.5-LIVE-GATE-RERUN.md`, never from memory of what was
    expected.

    Rewrite the header note (currently "The live gate has now run ... and FAILED") to describe BOTH
    runs: the superseded FAILED first run in `34.5-LIVE-GATE.md`, and the re-run in
    `34.5-LIVE-GATE-RERUN.md` with its actual verdict. Keep the standing warning that no
    `LIVE (item N)` cell may be read as "seen working" unless it says PASS.

    Flip each of the 10 gate-dependent proof-level cells to the re-run's recorded state. The
    proof-level cell stays a bare `unit + LIVE (item N) — PASS|FAIL|PENDING`; all explanation goes
    in the Riders cell, which must point at `34.5-LIVE-GATE-RERUN.md` and may keep a one-line note
    that the first run failed on the `publicDir` defect now fixed by plans 34.5-16/17/18.

    Update `## Accepted residuals` for each of: Assumption A1 (the `www.amazon.com` anchor —
    CONFIRMED, FALSIFIED or still UNTESTED, per item 3's recorded state); Assumption A2
    (`GAMELIB_SHELL_EXE` correctness at both call sites — and, even on a PASS, the packaged branch
    `spawn_sidecar_packaged` stays open); research Pitfall 2's non-Steam Wine claim (struck only on
    an item-5 PASS); and a NEW residual entry for `R-34.5-G1-PKG`, the unresolved packaged asset
    root. The literal strings `Assumption A1`, `Assumption A2`, `T-34.4.1-47` and `ctrl+r` must
    survive the edit — the gate script requires all four.

    Update `## Automated vs live proof boundary`'s live half to describe what the re-run actually
    found, replacing the failed run's narrative, and update its recorded suite counts to this
    cycle's measured values (from plan 34.5-20's precondition 6) rather than the stale 3279/40
    figures.

    Append to `deferred-items.md`: any new out-of-scope discovery the re-run surfaced, plus a
    numbered entry for `R-34.5-G1-PKG` naming its future home (the packaging work, not Phase 34.6's
    channel port — say which and why). Do not renumber existing items.

    Re-run `python3 ported-channels-gate.py` and `python3 ported-channels-gate.py --self-test` after
    EVERY edit. If an assertion fails, restore the missing declaration — never delete or weaken an
    assertion to make the gate pass; the script's own docstring states this rule.
  </action>
  <verify>
    <automated>cd .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc && python3 ported-channels-gate.py && python3 ported-channels-gate.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - Both gate-script invocations exit 0.
    - All 10 gate-dependent proof-level cells match `34.5-LIVE-GATE-RERUN.md`'s Verdict table
      exactly; no cell retains a state from the superseded run.
    - A cell reads PENDING only if its item was genuinely NOT ATTEMPTED, and its Riders cell then
      says so and names why.
    - `## Accepted residuals` carries entries for A1, A2, Pitfall 2 and `R-34.5-G1-PKG`, and still
      contains the four literal markers the gate script requires.
    - The suite counts in `## Automated vs live proof boundary` match plan 34.5-20's recorded
      precondition-6 output verbatim.
    - `git status --porcelain` is silent for both `34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md`.
  </acceptance_criteria>
  <done>The declared channel list tells the truth about both runs.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the inventory and the ROADMAP Phase 34.5 block</name>
  <files>.planning/IPC-PORT-INVENTORY.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/IPC-PORT-INVENTORY.md` § "Phase 34.5" and § "Phase 34.6" — the authoritative work-list and the 38/3/16 split the gate script cross-checks
    - `.planning/ROADMAP.md` Phase 34.5 block (~line 1531) in full: its `**Plans:**` line, its 15-entry executed plan list, the "Gap cycle (planned 2026-08-01 ...)" sub-list of six unchecked entries, and the two dated correction notes
    - `.planning/ROADMAP.md` Phase 34.6 and Phase 35 blocks — their dependency lines name Phase 34.5's completion
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` frontmatter — the verdict this task propagates
  </read_first>
  <action>
    In `IPC-PORT-INVENTORY.md`, update Phase 34.5's status prose to name the re-run and its verdict,
    and to point at `34.5-LIVE-GATE-RERUN.md` as the evidence document. Do not change channel
    membership or the 38/3/16 split — no channel moved in this gap cycle, and the arithmetic must
    keep reconciling.

    In `ROADMAP.md`'s Phase 34.5 block, the six gap-cycle entries were already added UNCHECKED at
    planning time under a "Gap cycle (planned 2026-08-01 ...)" sub-heading, and the `**Plans:**`
    line already reads 21. Mark each of the six done and annotate plan 34.5-20's entry with its
    verdict. Verify each entry still describes what the plan actually did — correct the description
    where execution deviated, never leave a stale one:
    - `34.5-16-PLAN.md` — app-root consumer sweep + `GAMELIB_APP_ROOT` handoff from both Rust spawn paths + `electronStub.getAppPath()` (gap wave 1)
    - `34.5-17-PLAN.md` — existence-checked `archSpecificBinary` fallback + real-filesystem coverage under sidecar-like cwd (gap wave 2)
    - `34.5-18-PLAN.md` — sidecar boot receipt logging of `GAMELIB_SHELL_EXE` + asset-root self-check (gap wave 2)
    - `34.5-19-PLAN.md` — `34.5-LIVE-GATE-RERUN.md` authored with `verdict: null`, precondition-5 defect closed (gap wave 3)
    - `34.5-20-PLAN.md` — the blocking 5-item live gate RE-RUN (gap wave 4, non-autonomous), annotated with its verdict
    - `34.5-21-PLAN.md` — propagation: channel list, inventory, ROADMAP, STATE (gap wave 5)

    Replace the block's "PHASE DOES NOT CLOSE" status paragraph with the re-run's actual outcome,
    keeping the pointer to the FAILED first run so the history is not erased. On a clean 5/5, state
    that the phase closes and Phase 34.6 is unblocked. On anything less, state that D-08 still
    holds, the phase still does not close, and another gap cycle is next — and in that case do NOT
    edit Phase 34.6's or Phase 35's dependency lines.

    Leave both dated correction notes (the 55→57 count and the dependency correction) intact.
  </action>
  <verify>
    <automated>grep -c "34.5-1[6-9]-PLAN.md\|34.5-2[01]-PLAN.md" .planning/ROADMAP.md && cd .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc && python3 ported-channels-gate.py</automated>
  </verify>
  <acceptance_criteria>
    - All six gap-cycle entries appear in the Phase 34.5 block, each marked `[x]`, and the
      `**Plans:**` line still reads 21.
    - Plan 34.5-20's entry is annotated with the re-run's verdict.
    - The status paragraph states the re-run's verdict and the correct routing (closure or another
      gap cycle), and still points at `34.5-LIVE-GATE.md` for the superseded run.
    - `ported-channels-gate.py` exits 0 — the inventory cross-check still reconciles at 38/3/16 = 57.
    - Both dated correction notes are unchanged.
    - Phase 34.6 and Phase 35 dependency lines are untouched unless the gate PASSed 5/5.
  </acceptance_criteria>
  <done>The roadmap and the inventory agree with the gate document.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md by hand and name the next step</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` IN FULL — the frontmatter counters, `stopped_at`, and the `## Current Position` blocker block written after the failed run
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` frontmatter and Verdict table
    - `.planning/phases/34.5-.../34.5-20-SUMMARY.md` — the executed gate's own record
  </read_first>
  <action>
    Hand-edit `.planning/STATE.md`. Do NOT use `gsd-sdk` state writes for this file: they are known
    to revert `stopped_at`, mangle the counters and splice status into historical blocks. After
    writing, re-read the file and confirm the frontmatter and the `## Current Position` block are
    both intact and mutually consistent.

    Update: `stopped_at` to name plan 34.5-21 and the re-run's verdict; `last_updated` and
    `last_activity` to now; `total_plans`/`completed_plans` to include this gap cycle's six plans
    (recount from the phase directory rather than incrementing blindly); and `status` to the value
    matching the outcome.

    Replace the `## Current Position` ACTIVE BLOCKER block with the re-run's result. On a clean 5/5:
    record that Phase 34.5 closes, that the `publicDir` defect is fixed and live-proven, which
    standing claims were struck, and that the next step is `/gsd-plan-phase 34.6`. On anything less:
    keep an ACTIVE BLOCKER block, state per-item what failed with its evidence pointer, and name
    `/gsd-plan-phase 34.5 --gaps` as the next step.

    Either way, record two durable lessons in the position block, because they are what this cycle
    actually bought: (1) the `publicdir-getapppath-chunking` family reached FOUR recurrences because
    a known gotcha was documented at one call site (`bootstrap.ts:156`, for `locales/`) and never
    swept across its siblings — `34.5-APP-ROOT-SWEEP.md` is the sweep that should have existed; and
    (2) a 3447/3447 green suite coexisted with this defect because jest runs at repo-root cwd, so
    coverage that does not reproduce the deployment's cwd proves nothing about the deployment.
  </action>
  <verify>
    <automated>head -20 .planning/STATE.md && grep -c "34.5-LIVE-GATE-RERUN.md" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `STATE.md` frontmatter parses and its `stopped_at`, `last_updated`, `last_activity`, `status`
      and plan counters all reflect this cycle; the plan counters were recounted from the phase
      directory, not incremented blindly.
    - `## Current Position` states the re-run verdict, per-item, with a pointer to
      `34.5-LIVE-GATE-RERUN.md`.
    - The named next step is `/gsd-plan-phase 34.6` on a clean 5/5, or `/gsd-plan-phase 34.5 --gaps`
      otherwise — exactly one of the two, never both.
    - Both durable lessons appear in the position block.
    - No historical block elsewhere in the file was rewritten (verify by reading the diff, not by
      trusting the tool).
  </acceptance_criteria>
  <done>A reader opening STATE.md knows what happened, what it proved, and what to run next.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate evidence → public planning documents | This repository is a public fork; propagated excerpts are published |
| Declared proof levels → future readers (Phase 34.6, Phase 35) | These documents are the input to the cutover's completeness gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-G6-09 | Repudiation | Proof-level cells overstating what was observed | mitigate | Every cell is copied from `34.5-LIVE-GATE-RERUN.md`'s Verdict table, and `ported-channels-gate.py` structurally forbids a `LIVE (item N)` cell without an explicit PENDING/PASS/FAIL state; both the gate and its `--self-test` must exit 0 |
| T-34.5-G6-10 | Tampering | Weakening a gate-script assertion to make an edit pass | mitigate | Task 1's action states the script's own rule — restore the missing declaration, never delete an assertion — and `--self-test` proves each assertion still rejects the input it exists to reject |
| T-34.5-G6-11 | Information disclosure | Credentials leaking through propagated log excerpts | mitigate | Only per-item outcomes and path/count evidence are copied; the redaction rule from plan 34.5-20 carries forward, and the acceptance criteria forbid any credential value in the diff |
| T-34.5-G6-12 | Tampering | `gsd-sdk` state writes corrupting `STATE.md` | mitigate | Task 3 mandates hand-editing and a re-read of the whole file, per the standing `gsd-sdk-state-writes-corrupt-state-md` finding |
| T-34.5-SC | Tampering | npm/pip/cargo installs | mitigate | Documentation-only plan; `files_modified` contains no manifest or lockfile |
</threat_model>

<verification>
- `python3 ported-channels-gate.py` and `--self-test` both exit 0
- `.planning/ROADMAP.md` Phase 34.5 lists 21 plans and states the re-run's verdict
- `.planning/STATE.md` frontmatter and position block are consistent and hand-verified
- No credential value anywhere in the diff
</verification>

<success_criteria>
- Every document that recorded the failed run now records both runs, accurately
- The next step is named unambiguously and follows D-08 rather than convenience
- The two durable lessons from this cycle are written down where the next planner will read them
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-21-SUMMARY.md` when done
</output>
