---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 45
type: execute
wave: 2
depends_on: [34.5-44]
files_modified:
  - src/backend/sidecar/nativeImageShim.ts
  - src/backend/sidecar/electronStub.ts
  - src/backend/shortcuts/shortcuts/shortcuts.ts
  - src/backend/sidecar/__tests__/nativeImageShim.test.ts
  - src/backend/sidecar/__tests__/shortcutsFlows.test.ts
  - src/backend/sidecar/__tests__/invokeReturnValueSweep.test.ts
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-JSON-TRANSPORT-SWEEP.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
  # `src-tauri/src/main.rs` is plan 34.5-44's file, but 34.5-44 is WAVE 1 and this plan is WAVE 2,
  # so every edit here is SEQUENCED, not concurrent — that ordering is the entire reason
  # `depends_on: [34.5-44]` exists. An executor must NOT refuse these edits on fence grounds.
  # Task 1 edits it UNCONDITIONALLY, COMMENT-ONLY: it reconciles the doc-comment plan 34.5-44 wrote
  # about the UNQUOTED `run.sh` template, which Task 1's quoting fix makes false. Task 3 branch C
  # may edit it FURTHER as a CODE change, but only if Task 2's verdict names the Rust leg — see
  # threat T-34.5-C6-23.
  - src-tauri/src/main.rs
  # CONDITIONAL — only if Task 3 takes branch C. If the diagnosis does NOT name the Rust leg, this
  # file may not appear in the diff at all.
  - src/backend/__tests__/longRunningChannels.test.ts
autonomous: true
gap_closure: true
requirements: [REQ-34.5-01, REQ-34.5-05, REQ-34.5-12]

must_haves:
  truths:
    - "convertPngToICNS completes end to end under the sidecar with NOTHING mocked: real nativeImage shim, real @shockpkg/icon-encoder, a non-empty shortcut.icns written to disk"
    - "generateMacOsApp reaches its run.sh write at shortcuts.ts:235 instead of logging 'Error generating MacOS App'"
    - "The generated run.sh cannot execute a command smuggled in through a game's app_name or runner"
    - "nativeImage's other three members return a real chainable image object, not a bare {} — steamhelper.ts:121 calls .toJPEG(90) on createFromDataURL's result on the addToSteam path"
    - "The layer that drops addToSteam's return value is IDENTIFIED by a discriminator that distinguishes it from the other candidate layers, never by the most plausible reading"
    - "All 34 invoke-kind channels in the four 34.5 registration modules are classified for the same return-value drop — 34 rows, not a sample"
    - "A defect found in steamFlowRegistration.ts is recorded as a finding and NOT edited (plan 34.5-46 owns that file this wave)"
    - "The two artifacts plan 34.5-44 left behind describing the UNQUOTED run.sh — ledger row U-34.5-19 and the main.rs doc-comment — are reconciled in the SAME commit as the quoting fix, so neither survives asserting a defect that no longer exists"
    - "The new ledger row this plan creates is provable by its SUBJECT TEXT, not by a bare id that a row plan 34.5-44 already committed would satisfy on its own"
  artifacts:
    - path: "src/backend/sidecar/nativeImageShim.ts"
      provides: "a real headless nativeImage with createFromBuffer/createFromPath/createFromDataURL/createEmpty and a chainable resize/crop/toPNG/toJPEG"
      contains: "createFromBuffer"
      min_lines: 80
    - path: "src/backend/sidecar/__tests__/nativeImageShim.test.ts"
      provides: "unit proof the shim decodes a real image buffer and emits real PNG bytes, and that non-darwin declines loudly"
      contains: "createFromBuffer"
    - path: "src/backend/sidecar/__tests__/invokeReturnValueSweep.test.ts"
      provides: "an anti-rot gate: every non-void-declared invoke channel in the four modules has a returning handler path"
      contains: "runnerAuthFlowRegistration"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-JSON-TRANSPORT-SWEEP.md"
      provides: "the F-34.5-G6-08 discriminator table with its verdict, plus the 34-row invoke-return sweep"
      contains: "Invoke-return-value sweep"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md"
      provides: "the amended U-34.5-12 cell, U-34.5-19 RETIRED against its own second branch, and ONE new row whose id is read off the live file and whose subject is the real-.ico decode risk"
      contains: "ImageIO/sips multi-image .ico decode"
  key_links:
    - from: "src/backend/shortcuts/shortcuts/shortcuts.ts:259 nativeImage.createFromBuffer"
      to: "src/backend/sidecar/nativeImageShim.ts"
      via: "electronStub's re-export, resolved by the sidecar's `electron` module alias"
      pattern: "from '\\./nativeImageShim'"
    - from: "src/backend/sidecar/shortcutsFlowRegistration.ts:174 return addNonSteamGame(game)"
      to: "the invoke response frame's own `result` key on the sidecar's stdout"
      via: "sidecarRpc.ts:126 dispatchInvoke"
      pattern: "\"result\""
---

<objective>
Two defects on the shortcuts cluster, in this order: (A) give the sidecar a REAL `nativeImage` so
`addShortcut`'s macOS `.app` path stops being structurally dead, and (B) **diagnose** — then fix —
`addToSteam`'s dropped invoke return value, and sweep every other invoke-kind channel in this
slice for the same class of drop.

Closes **F-34.5-G6-07** / **U-34.5-12** and **F-34.5-G6-08** / **U-34.5-15**.

Purpose:

**Defect A.** `src/backend/sidecar/electronStub.ts:651-655` exports `nativeImage` with only
`createFromPath`, `createFromDataURL` and `createEmpty`, each returning a bare `{}`. So
`convertPngToICNS` (`shortcuts.ts:250`) throws at its `.createFromBuffer(iconBuffer)` call
(`shortcuts.ts:259-260`), `generateMacOsApp` never reaches its `run.sh` write
(`shortcuts.ts:226-238`), and the throw is swallowed into the generic
`Error generating MacOS App` at `shortcuts.ts:240`. This is **structurally dead for EVERY macOS
game** — not target-specific and not icon-format-specific.

**Defect B.** `addNonSteamGame` is `Promise<boolean>` with total return coverage, and
`shortcutsFlowRegistration.ts:168-176` returns it directly — yet gate 3 recorded
`await window.api.addToSteam('1207659037','gog')` resolving to `undefined` while the
`shortcuts.vdf` write SUCCEEDED. The drop is therefore **downstream of the handler**, and this
phase has already shipped one plausible, unifying, WRONG hypothesis (the teardown/unmount reading
in its own UAT gap). Task 2 builds a discriminator before Task 3 fixes anything.

Output: a real `nativeImageShim.ts`; an injection-safe `run.sh` launch command; a written
discriminator verdict and a 34-row invoke-return sweep in `34.5-JSON-TRANSPORT-SWEEP.md`; an
anti-rot jest gate; and one new `34.5-UNTESTED-ITEMS.md` row for what stays live-only.

**Live observables that would prove this — nothing else counts.**
- **U-34.5-12:** a live session in which `addShortcut` produces
  `<title>.app/Contents/MacOS/run.sh` whose body launches the shell exe with `--no-gui` and a
  `gamelib://launch` URL naming this appName and runner, with **no** `Error generating MacOS App`
  line in `~/Library/Logs/GameLib/gamelib.log`.
- **U-34.5-15:** a live session in which `await window.api.addToSteam(...)` resolves to `true`
  **and** the submenu label flips to "Remove from Steam" without a reload.

A green suite proves the shim and the dispatch wiring only. **This phase's live gate has beaten a
green suite three times, and the suite that should have caught Defect A instead MOCKED the missing
member** (`shortcutsFlows.test.ts:131-140`). Neither finding closes on unit evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE6-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-JSON-TRANSPORT-SWEEP.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.claude/skills/spike-findings-gamelib/SKILL.md

**`graphify-out/graph.json` exists. Orient with graphify BEFORE opening raw source:**
`graphify explain "convertPngToICNS"`, `graphify explain "generateMacOsApp"`,
`graphify query "sidecar RPC request response frame stdin stdout handler result"`.

<interfaces>
**Ten facts established by reading the live tree at planning time. Several CONTRADICT the plan
brief and the finding text. Trust these.**

1. **`addNonSteamGame`'s return coverage is TOTAL — the brief's line numbers are off by one
   return.** The brief cites `nonesteamgame.ts:206, 338, 353, 367`; **206 is the function
   declaration**, and the first `return false` is at **220** (the `checkSteamUserDataDir` error
   arm). The remaining three are `338` (`if (!added)`), `353` (`if (errors.length === 0)`) and
   `367` (that `if`'s `else`). Lines 341/354 are an `if/else` pair, so there is **no implicit
   fall-through path** and no `return` without a value anywhere in the function. **Hypothesis
   "the handler returns void" is ruled out by source before Task 2 starts** — do not spend the
   diagnosis budget re-deriving it, but DO pin it with the frame-level test in Task 2.

2. **`undefined` is structurally unreachable from the Rust leg.** `src-tauri/src/main.rs:4947`
   is `Ok(value.get("result").cloned().unwrap_or(Value::Null))`. A response frame with no own
   `result` key becomes `Value::Null`, which Tauri serialises as JSON `null`. **The smallest value
   the Rust→renderer leg can deliver is `null`, never `undefined`.** This asymmetry is the single
   most decisive fact available to the diagnosis: a genuine `null` at the caller implicates the
   sidecar's response envelope; a genuine `undefined` implicates something ABOVE Rust (the caller's
   own evaluation form, or a promise that never resolved with a value at all).

3. **`addToSteam` is ABSENT from `LONG_RUNNING_CHANNELS` (`main.rs:134-159`)**, so
   `timeout_for("addToSteam")` returns `Some(INVOKE_TIMEOUT)` = 60s (`main.rs:162-168`). And
   `addNonSteamGame` does two networked things BEFORE the VDF write: `getWikiGameInfo(game)` at
   `nonesteamgame.ts:209` (PCGamingWiki + gamesdb — the UAT recorded PCGamingWiki **403s** in this
   very session) and `await prepareImagesForSteam(...)` at `nonesteamgame.ts:281` (SteamGridDB /
   CDN artwork downloads). A >60s call returns `Err("sidecar invoke timed out")` (REJECTION, a
   third distinct signature) and the reader thread then logs
   `[shell] response for unknown/timed-out id=<id> (dropped)` (`main.rs:4957-4959`) when the real
   answer lands late. **That log line is a unique fingerprint for this mechanism and for nothing
   else.**

4. **The preload leg is a bare passthrough.** `src/preload/tauriTransport.ts:92-97` is
   `return tauriInvoke<Ret>(SIDECAR_INVOKE, { channel, args })` — no unwrapping, no defaulting.
   `src/preload/ipc.ts:38-47`'s `makeHandlerInvoker` returns that promise directly under
   `isTauri()`. `src/preload/api/index.ts` spreads `Menu` (which owns
   `addToSteam = makeHandlerInvoker('addToSteam')`, `api/menu.ts:12`) into the default export, and
   **no later-spread namespace re-exports `addToSteam`**, so there is no shadowing.
   `tauriAttach.ts:71` assigns that object wholesale to `window.api`. **No layer between the Tauri
   command and `window.api.addToSteam` can convert a value to `undefined`.**

5. **A SECOND live instance of Defect A's class sits on the `addToSteam` path.**
   `src/backend/shortcuts/nonesteamgame/steamhelper.ts:121` does
   `nativeImage.createFromDataURL(imgUrl).toJPEG(90)`, reached from `prepareImagesForSteam`, which
   `addNonSteamGame` awaits at `nonesteamgame.ts:281`. The stub's `createFromDataURL` returns a
   bare `{}`, so `.toJPEG` is not a function. It did not fire on gate 3 only because every image
   URL in that session began with `http` and took the `downloadImage` branch instead. **Standing
   Rule 5: a gotcha at one call site is a debt marker for every sibling until something sweeps them
   all.** Fixing only `createFromBuffer` leaves this loaded.

6. **The existing suite MOCKS the exact member that was missing.**
   `src/backend/sidecar/__tests__/shortcutsFlows.test.ts:131-140` overrides
   `nativeImage.createFromBuffer` with a `jest.fn()`, and its own comment at line 126-127 says
   "the one member `convertPngToICNS` needs and the real stub does not implement". Line 121-123
   ALSO mocks `@shockpkg/icon-encoder`'s `IconIcns`. **That pair of mocks is why a 4462-test green
   suite never saw F-34.5-G6-07**, and both must be removed, not adjusted.

7. **The macOS toolchain is present and its limits are MEASURED, not assumed** (verified on this
   machine at planning time):
   - `/usr/bin/sips` and `/usr/bin/iconutil` both exist.
   - sips **reads** `.ico` (a PNG-embedded ICO probed as `format: ico`, 256x256) but **cannot
     write** ico (exit 13, "not a valid file").
   - sips **sniffs content, not extension** — it processed a file named `work.bin` correctly, so
     the temp file needs no meaningful suffix.
   - `--resampleWidth 512` followed by `-c 512 512` yields a 512x512 PNG.
   - **`--cropOffset 0 0` is a NO-OP.** Proved with an asymmetric fixture (a 256px icon padded to
     512x1024 with a solid colour): the offset crop and the default crop produced **byte-identical
     output** (same md5). **sips crops CENTERED and Electron's `crop({x:0,y:0})` top-left origin
     cannot be reproduced with that flag.** This is a real fidelity divergence — document it, do
     not paper over it, and do not ship `--cropOffset` as though it worked.

8. **The chain does NOT just move its failure one line down.** Verified at planning time by running
   the real `@shockpkg/icon-encoder` against a real 512x512 PNG produced by the sips pipeline:
   `new IconIcns().addFromPng(png, ['ic11'], true)` then `.encode()` succeeded and returned 50173
   bytes. `shortcuts.ts:268-270`'s encoder call is therefore satisfied by a genuine 512px PNG —
   the `stale-outdir-file-to-dir-transition` failure mode ("fixing one layer just moves the failure
   down a layer") was checked for and does not apply here.

9. **The `.app` bundle NAME is already safe; the `run.sh` BODY is not.** `shortcuts.ts:152` does
   `gameTitle = sanitize(gameTitle)` before building the darwin path at `:168`, so a `../` in a
   title is not a traversal vector. But `shortcuts.ts:227-235` interpolates
   `${app.getPath('exe')} --no-gui gamelib://launch?appName=${app_name}&runner=${runner}`
   **UNQUOTED** into a `#!/bin/bash` script. The `addToSteam` sibling at `nonesteamgame.ts:299`
   already wraps its URL in double quotes for the VDF; the `run.sh` does not. This plan is what
   makes that script reachable for the first time, so the exposure is this plan's to close.
   `src/backend/protocol.ts:17-25`'s `parseHeroicUrl` uses `new URL(...)` and
   `url.searchParams.get(...)`, both of which percent-decode, so `encodeURIComponent` round-trips
   cleanly and does not break plan 34.5-44's deep-link handling.

10. **The four registration modules hold 34 invoke-kind channels, and a naive regex finds only
    23.** Per-module `ipcMain.handle(` counts are `runnerAuth` 10, `wineTools` **9** (not the 7 a
    name-only grep suggests — `wine.isValidVersion` contains a dot and `toggleVKD3D` is easy to
    miss), `shortcuts` 4, `runnerMisc` 11. A single-line `ipcMain\.handle\('([^']+)'` regex matches
    only **23** of them because 11 registrations use the multi-line form with the channel name on
    the following line. **An enumeration that yields 23 is proof of an incomplete sweep**; a
    multi-line (`perl -0777` / `[\s\S]`) pattern yields 34.

**The full 34-channel roster the sweep must cover** (alphabetical):
`addToSteam`, `authAmazon`, `authGOG`, `callTool`, `downloadRuntime`, `egsSync`,
`getAlternativeWine`, `getAmazonLoginData`, `getAmazonUserInfo`, `getCometVersion`,
`getEpicGamesStatus`, `getGOGLinuxInstallersLangs`, `getGogdlVersion`, `getLegendaryVersion`,
`getNileVersion`, `getUserInfo`, `installWineVersion`, `isAddedToSteam`, `isLoggedIn`,
`isRuntimeInstalled`, `login`, `logoutAmazon`, `logoutLegendary`, `refreshWineVersionInfo`,
`removeFromSteam`, `removeWineVersion`, `runWineCommand`, `shortcutsExists`, `syncGOGSaves`,
`syncSaves`, `toggleDXVK`, `toggleDXVKNVAPI`, `toggleVKD3D`, `wine.isValidVersion`.

**Declared return types live in `AsyncIPCFunctions`, `src/common/types/ipc.ts`.** Spot-checked
examples the sweep will need: `addToSteam` → `Promise<boolean>` (:441); `removeFromSteam` →
`Promise<void>` (:442) and its handler correctly does not return; `isAddedToSteam` →
`Promise<boolean>` (:443); `refreshWineVersionInfo` → `Promise<void>` (:437) with an inherited
swallow-and-return-nothing shape documented in place; `toggleDXVK`/`toggleDXVKNVAPI`/`toggleVKD3D`
→ `Promise<boolean>` (:507-509), all three returning `DXVK.installRemove(...)` directly.

**The RPC harness to reuse — do NOT build a parallel one.**
`src/backend/sidecar/__tests__/appShellFlows.test.ts:218-277` already provides `collectFrames`,
`flush`, `startSidecar` (PassThrough pair fed to `init`) and `writeInvoke`/`writeSend`. That is the
**production call shape**: a real newline-delimited `{id, kind:'invoke', channel, args}` frame into
the real `startRpcServer` loop, with the real emitted response line parsed back off stdout. The
`[null]` precedent at `skeletonFlows.test.ts:721-736` is the same discipline applied to args.

**`sidecarRpc.ts:124-135`'s `dispatchInvoke`** builds `{ id, ok: true, result }` and hands it to
`writeLine`, which is `outputStream.write(JSON.stringify(value) + '\n')` (`:91-93`).
**`JSON.stringify` SILENTLY OMITS a key whose value is `undefined`** — so asserting
`frame.result === true` is not the same assertion as asserting the frame HAS an own `result` key.
The sweep's own finding #8 is the precedent for taking that distinction seriously.

**Do NOT edit `src/common/types/ipc.ts`.** Plan 34.5-43 owns the `branch`/`build` parameter-name
correction at `:217` (a real transposition both `string | undefined`, invisible to `tsc`). If this
plan's sweep can cheaply detect that CLASS of silent-shape defect across the 34 channels, record
the finding in `34.5-JSON-TRANSPORT-SWEEP.md` — never fix it here.

**Do NOT edit `src/backend/sidecar/steamFlowRegistration.ts`.** Plan 34.5-46 owns it **this same
wave**, and `use_worktrees=false` means every plan executes on the main tree, so an overlap is a
real collision. A drop found there is a recorded finding, nothing more.

**`src-tauri/src/main.rs` is plan 34.5-44's file, but 34.5-44 is WAVE 1 and this plan is WAVE 2.**
The dependency exists precisely so a Rust-side fix here is sequenced rather than concurrent. Task 3
branch C may edit it — but only if Task 2's verdict names the Rust leg, and only with the
explicit hand-off note that branch requires. Plan 34.5-43 hit the same fence and opened ledger row
`U-34.5-16` rather than editing; that row is about `getInstallInfo`'s own 60s exposure and is NOT
this plan's to retire.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the sidecar a REAL nativeImage, un-dead the macOS .app path, and close the run.sh injection it opens</name>
  <read_first>
    - `src/backend/sidecar/electronStub.ts` lines 640-700 — the `nativeImage` export at 651-655, its "safe no-ops; out of scope per 27-CONTEXT" comment at 649, and the `session.fromPartition` stub immediately above it, which is the house precedent for a stub that declines LOUDLY rather than silently
    - `src/backend/sidecar/pathShim.ts` — the WHOLE file (115 lines). This is the exact structural precedent for the new module: a real `*Shim.ts` implementing a genuine Electron surface for the sidecar, whose header states "Unimplemented names throw loudly rather than silently returning garbage"
    - `src/backend/shortcuts/shortcuts/shortcuts.ts` lines 140-280 — `shortcutFiles` (and its `sanitize(gameTitle)` at 152), `generateMacOsApp` (176-248) including the `run.sh` launch-command construction at 226-238 and the `Error generating MacOS App` arm at 239-247, and `convertPngToICNS` (250-278) with the exact chain `createFromBuffer().resize({width:512}).crop({x:0,y:0,width:512,height:512}).toPNG()`
    - `src/backend/shortcuts/utils.ts` — `getIcon` (the function that decides which bytes reach `createFromBuffer`: a GOG `goggame-<id>.ico` from the install dir, a `support/icon.png`, or a downloaded `art_square` jpg)
    - `src/backend/shortcuts/nonesteamgame/steamhelper.ts` lines 110-130 — the `createFromDataURL(imgUrl).toJPEG(90)` sibling call site on the `addToSteam` path
    - `src/backend/sidecar/__tests__/shortcutsFlows.test.ts` lines 100-230 — the `@shockpkg/icon-encoder` mock (121-123), the `electron` mock's `createFromBuffer: jest.fn()` override (131-140), the `FIXTURE_ICON_PATH` fixture (217-226) and the `mockCreateFromBuffer`/`mockIconIcns` handles (214-215). Every one of these is what this task removes or replaces
    - `src/backend/protocol.ts` lines 17-45 — `parseHeroicUrl`'s `new URL(...)` + `searchParams.get(...)`, the proof that percent-encoding the URL's query values round-trips
  </read_first>
  <behavior>
    - `createFromBuffer(<real PNG bytes>)` returns an object exposing `resize`, `crop`, `toPNG`, `toJPEG`, `isEmpty` and `getSize` — never a bare `{}`, and `typeof result.resize === 'function'` is true.
    - `createFromBuffer(buf).resize({ width: 512 }).crop({ x: 0, y: 0, width: 512, height: 512 }).toPNG()` returns a Buffer whose first eight bytes are the PNG signature `89504e470d0a1a0a`, and whose decoded dimensions are 512x512.
    - The same chain applied to a real ICO buffer also returns a valid 512x512 PNG Buffer — `getIcon` returns a `goggame-<id>.ico` for an installed GOG title, so ICO is the primary production input, not an edge case.
    - `resize` and `crop` are LAZY: calling them performs no conversion, and exactly ONE converter invocation happens per `toPNG()`/`toJPEG()` call, regardless of how many ops were chained.
    - `createFromDataURL('data:image/png;base64,<real png>')` returns the same chainable object, and `.toJPEG(90)` on it returns a Buffer beginning `ffd8ff` — the `steamhelper.ts:121` shape.
    - `createFromPath(<real file path>)` returns the same chainable object; `createFromPath` of a non-existent path returns an object whose `isEmpty()` is true and does not throw at construction.
    - `createEmpty()` returns an object whose `isEmpty()` is true.
    - On a platform where no converter is available, `toPNG()`/`toJPEG()` throw an Error whose message contains the literal marker `[electronStub] nativeImage:` and the platform name — a named, greppable, loud decline, never a silent empty Buffer.
    - `convertPngToICNS` run with NOTHING mocked (real shim, real `IconIcns`) returns `true` and writes a non-empty `shortcut.icns`.
    - `generateMacOsApp` run with NOTHING mocked writes `<title>.app/Contents/MacOS/run.sh` with mode `755`, and logs NO `Error generating MacOS App`.
    - A game whose `app_name` is the literal `x&runner=gog;touch /tmp/gamelib-pwned` produces a `run.sh` whose body contains NO unescaped `;` or `&` outside a quoted region, and whose `gamelib://` URL, when parsed by `new URL(...)`, yields `searchParams.get('appName')` equal to that exact original string.
  </behavior>
  <action>
    Create `src/backend/sidecar/nativeImageShim.ts`, modelled structurally on `pathShim.ts` (a real
    shim module, not a no-op recorder; header comment stating what is implemented, what is not, and
    that the unimplemented path declines loudly). Export a single `nativeImage` object with
    `createFromBuffer(buffer)`, `createFromPath(path)`, `createFromDataURL(dataUrl)` and
    `createEmpty()`. Each returns the SAME internal image type: an object holding the source bytes
    plus an ordered list of pending ops, exposing `resize(options)`, `crop(rect)`, `toPNG()`,
    `toJPEG(quality)`, `isEmpty()` and `getSize()`. `resize` and `crop` record their op and return
    the receiver so the `shortcuts.ts:259-263` chain works unmodified; only `toPNG`/`toJPEG`
    execute.

    Execution uses macOS's `sips`, invoked as `execFileSync('/usr/bin/sips', argv, ...)` — an
    ABSOLUTE path, never a bare `sips` resolved through `PATH`, and an argv ARRAY, never a shell
    string (T-34.5-C6-19). Write the source bytes to a fresh per-call directory created with
    `mkdtempSync(join(tmpdir(), 'gamelib-nativeimage-'))`, mode the file `0o600`, and remove the
    whole directory in a `finally`. Do NOT derive any temp path segment from a game title, appName
    or icon URL. sips sniffs content rather than extension (interfaces fact 7), so the temp file
    needs no meaningful suffix. Pass `{ timeout: 15_000, maxBuffer: 32 * 1024 * 1024 }`, and reject
    an input buffer larger than 32 MiB before writing it (T-34.5-C6-18/20).

    Translate the recorded ops to sips flags: a `resize({width})` becomes `--resampleWidth <width>`;
    a `resize({width, height})` becomes `-z <height> <width>`; a `crop({width, height})` becomes
    `-c <height> <width>`. Output format is `-s format png` for `toPNG()` and `-s format jpeg`
    (plus `-s formatOptions <quality>`) for `toJPEG(quality)`. Validate every numeric dimension with
    `Number.isInteger` and a positive upper clamp before it reaches argv. **Do NOT emit
    `--cropOffset`** — it was measured to be a no-op at planning time (interfaces fact 7). Instead
    put a comment on the crop translation recording, in its own words, that sips crops CENTERED
    while Electron's `crop({x, y})` takes a top-left origin, that `--cropOffset` was tested against
    an asymmetric fixture and produced byte-identical output, and that a non-zero `x`/`y` is
    therefore NOT honoured. `convertPngToICNS` passes `{x: 0, y: 0}` so the divergence is
    unobservable for the one production caller; a future caller passing a non-zero origin must be
    able to find this comment.

    Add a fast path that skips the converter entirely: when NO ops are pending and the source bytes
    already carry the requested output format's magic (`89504e47` for PNG, `ffd8ff` for JPEG),
    return the source Buffer unchanged. That is what makes `steamhelper.ts:121`'s
    `createFromDataURL(...).toJPEG(90)` work on a platform with no `sips`, for the common case where
    the data URL already holds JPEG bytes.

    When a converter is genuinely needed and `process.platform !== 'darwin'`, throw an Error whose
    message begins with the literal `[electronStub] nativeImage:` and names the platform and the
    operation. Model the tone on `electronStub.ts:640-647`'s `session.fromPartition` stub and
    `pathShim.ts`'s "throw loudly rather than silently return garbage" rule: a loud, named,
    greppable failure is the correct outcome, and a silent empty Buffer would reproduce exactly the
    class of defect this task exists to remove.

    In `src/backend/sidecar/electronStub.ts`, DELETE the four-line `nativeImage` object at 651-655
    and re-export the shim's implementation in its place, keeping the export name `nativeImage`
    so every existing `import { nativeImage } from 'electron'` resolves unchanged. Replace the
    "safe no-ops; out of scope per 27-CONTEXT" comment with one that names F-34.5-G6-07, states
    that all four members previously returned a bare `{}`, and names BOTH live call sites the
    change serves — `shortcuts.ts:259` (`createFromBuffer`, the dead macOS `.app` path) and
    `steamhelper.ts:121` (`createFromDataURL(...).toJPEG(90)`, reached from `prepareImagesForSteam`
    on the `addToSteam` path and previously loaded for any non-`http` image URL).

    In `src/backend/shortcuts/shortcuts/shortcuts.ts`, close the injection this task makes
    reachable (T-34.5-C6-17). Build the `run.sh` launch command with the exe path in double quotes
    and the whole `gamelib://` URL in single quotes, and percent-encode both query values with
    `encodeURIComponent(app_name)` and `encodeURIComponent(runner)`. `parseHeroicUrl`'s
    `new URL(...)` + `searchParams.get(...)` decodes them, so the deep link plan 34.5-44 wired keeps
    working. Leave `nonesteamgame.ts:299` alone — its URL is already double-quoted for the VDF, and
    the Electron-shaped `--no-sandbox` argument at `:294` is plan 34.5-44's argv concern, not this
    plan's.

    Because that changes the literal `run.sh` body, update `U-34.5-12`'s "Retired by" cell in
    `34.5-UNTESTED-ITEMS.md` in the SAME commit to the new quoted/encoded form. State in the cell
    that the change is a security mitigation, not a relaxation, and that the semantic requirement is
    unchanged: the shell exe, `--no-gui`, and a `gamelib://launch` URL whose parsed `appName` and
    `runner` params equal this game's. Plan 34.5-50 authors the gate contract in wave 3 from this
    ledger, so an un-updated cell would send the operator hunting for a string that can no longer
    appear.

    **Same commit — reconcile the two artifacts plan 34.5-44 left behind about the UNQUOTED body.**
    Plan 34.5-44 (wave 1, landed before this plan runs) read the same `shortcuts.ts:227-229`
    template, decided under its **D-44-C** NOT to quote it, and left two artifacts that this task's
    fix makes FALSE the moment it lands. Both are reconciled here, in the SAME commit as the fix —
    not in a follow-up, and not by a later plan, because no later plan touches either of them.

    (a) **Ledger row `U-34.5-19`.** It describes the unquoted body as a PRESENT defect: `bash` treats
    the `&` as a control operator, the launch is backgrounded and `runner=` is discarded. Its own
    retirement condition has two branches — *"either a live `.app` launch observed to start the
    correct game with the current unquoted body, OR an explicit decision to quote it accompanied by a
    matching amendment to `U-34.5-12` in the same commit"* — and this task satisfies the SECOND
    verbatim, because the `U-34.5-12` amendment directly above IS that matching amendment. So mark
    `U-34.5-19` **RETIRED** against that second branch and write in its Status cell, in plain words,
    that it **retired because the defect was FIXED, not because it was observed passing** — use that
    exact phrase. The first branch was never met: no live `.app` launch was ever observed, and this
    plan produces none. Name plan `34.5-44` as the creating plan, `D-44-C` as the decision that
    created the row, and this plan (`34.5-45`) plus its commit as the fix. Do NOT delete the row —
    edit that one row's cells in place and nothing else, because plans 34.5-46/48/49 append to this
    same file this same wave and the ledger's append-only discipline applies. This is Ledger Rule 1
    applied to the inverse case: a hole that CEASED TO EXIST must be reconciled explicitly, never
    silently. `U-34.5-11`'s cell — retired because its subject was deleted, and spelling out that
    "it is NOT a pass" — is this project's precedent for the wording.

    (b) **The `main.rs` doc-comment plan 34.5-44 wrote.** Plan 34.5-44's `protocol_url_arg`
    deliberately ACCEPTS a runner-less URL and doc-comments that tolerance as load-bearing, with the
    present-tense rationale *"`shortcuts.ts:227`'s unquoted `run.sh` template makes `bash` split on
    the `&`"*, citing D-44-C / `U-34.5-19`. Its `#[cfg(test)]` case
    `protocol_url_arg_accepts_a_runnerless_url` carries the same citation. After this task, current
    code no longer emits that shape, so a committed comment would be asserting a false fact as the
    stated reason for a production behaviour.

    **Keep the tolerance; rewrite only the rationale.** Do NOT remove the runner-less acceptance and
    do NOT touch any Rust code, test assertion or test name: `.app` bundles generated BEFORE this fix
    already exist on users' disks, are not regenerated by this plan, and still deliver the truncated
    runner-less URL — the tolerance is now a compatibility guarantee for those bundles plus
    defence-in-depth, and dropping it would break exactly the shortcuts this phase is trying to make
    work. Edit the COMMENT TEXT ONLY: put the `bash` split in the PAST tense, keep the `U-34.5-19`
    citation so the history stays traceable, and include the literal phrase
    `no longer emitted by current code (fixed in plan 34.5-45)`.

    **Fence note — read this before refusing the edit.** `src-tauri/src/main.rs` is plan 34.5-44's
    file, but 34.5-44 is **wave 1** and this plan is **wave 2**, so the edit is SEQUENCED, not
    concurrent. That ordering is the entire reason `depends_on: [34.5-44]` exists, and it is the same
    reasoning Task 3 branch C already relies on. This is NOT a fence violation and an executor must
    not refuse it on those grounds. Confirm 34.5-44 has landed by checking that the doc-comment is
    actually present in `main.rs`; if it is absent, STOP and report the ordering violation rather
    than inventing a comment to correct.

    In `src/backend/sidecar/__tests__/shortcutsFlows.test.ts`, **remove** the
    `createFromBuffer: jest.fn()` override from the `electron` mock factory (131-140) and **remove**
    the `@shockpkg/icon-encoder` mock (121-123) entirely, along with the now-dead `mockCreateFromBuffer`
    and `mockIconIcns` handles and any assertion that reads them. Replace `FIXTURE_ICON_PATH`'s
    `Buffer.from('fake-fixture-icon-bytes')` with REAL image bytes — those seven words are not a
    decodable image and would fail the real shim. Generate the fixture at test setup from a file
    that ships with macOS rather than committing a binary. Update the file's header docstring, which
    currently claims `nativeImage.createFromBuffer` and `IconIcns` are mocked, to say the whole
    `convertPngToICNS` chain now runs REAL — the docstring is what a future reader trusts, and
    leaving it stale would reproduce the same blindness in the next phase.

    Create `src/backend/sidecar/__tests__/nativeImageShim.test.ts` covering every `<behavior>`
    bullet. Build the ICO fixture in-process by wrapping a real PNG in a 22-byte ICO container (a
    6-byte header of `0,1,1` little-endian u16s, then a 16-byte directory entry whose `bytesInRes`
    is the PNG length and whose `imageOffset` is 22) — this was validated at planning time and sips
    reports it as `format: ico`, 256x256. Assert PNG/JPEG output by MAGIC BYTES and by re-probing
    the produced file's dimensions, never by byte length alone.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/nativeImageShim.test.ts src/backend/sidecar/__tests__/shortcutsFlows.test.ts --silent &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createFromBuffer" src/backend/sidecar/nativeImageShim.ts` is at least 1, and `grep -c "createFromBuffer" src/backend/sidecar/electronStub.ts` is 0 — the implementation lives in the shim, and `electronStub.ts` only re-exports it.
    - `grep -v '^\s*\*' src/backend/sidecar/electronStub.ts | grep -v '^\s*//' | grep -c "createFromPath: () => ({})"` is 0. The four bare-`{}` members are gone from production source, not merely supplemented.
    - `grep -c "execFileSync('/usr/bin/sips'" src/backend/sidecar/nativeImageShim.ts` is at least 1 and `grep -cE "exec(Sync)?\(|execFile[A-Za-z]*\(\s*'sips'" src/backend/sidecar/nativeImageShim.ts` is 0 — absolute path, argv array, no shell string.
    - `grep -c "cropOffset" src/backend/sidecar/nativeImageShim.ts` counts only comment lines: re-run as `grep -v '^\s*\*' src/backend/sidecar/nativeImageShim.ts | grep -v '^\s*//' | grep -c "cropOffset"` and confirm 0.
    - `grep -v '^\s*\*' src/backend/sidecar/nativeImageShim.ts | grep -v '^\s*//' | grep -c "mkdtempSync"` is at least 1, and the same filtered grep for `0o600` is at least 1.
    - `grep -c "createFromBuffer: jest.fn()" src/backend/sidecar/__tests__/shortcutsFlows.test.ts` is 0, and `grep -c "@shockpkg/icon-encoder" src/backend/sidecar/__tests__/shortcutsFlows.test.ts` is 0. Both mocks removed, not weakened.
    - `grep -c "fake-fixture-icon-bytes" src/backend/sidecar/__tests__/shortcutsFlows.test.ts` is 0 — the fixture is real image bytes.
    - A test asserts `convertPngToICNS`'s downstream artifact exists: after driving `addShortcut` through the real registration on a darwin-forced platform, `<title>.app/Contents/Resources/shortcut.icns` exists with a size greater than 0 AND `<title>.app/Contents/MacOS/run.sh` exists. Both files, not one.
    - A test asserts `gamelib.log`'s failure string is absent: the `logError` spy received NO call whose stringified arguments contain `Error generating MacOS App`.
    - A test asserts the PNG signature by bytes: `toPNG()`'s result `.subarray(0, 8).toString('hex')` equals `89504e470d0a1a0a`. A length-only assertion does not satisfy this criterion.
    - A test asserts the ICO input path: the ICO fixture through the full `resize`→`crop`→`toPNG` chain yields a 512x512 PNG, re-probed for dimensions rather than assumed.
    - A test asserts exactly ONE converter invocation per `toPNG()` after two chained ops — spy on the child-process call and assert `toHaveBeenCalledTimes(1)`.
    - A test asserts the non-darwin decline: with `process.platform` forced to `linux` and a source buffer whose format does NOT already match the request, `toPNG()` throws an Error whose message contains `[electronStub] nativeImage:` and the string `linux`.
    - **Injection proof, and it must be proven RED.** A test drives the real `generateMacOsApp` with `app_name` set to `x&runner=gog;touch /tmp/gamelib-pwned`, reads the written `run.sh` back, and asserts (a) `new URL(<the gamelib:// substring>).searchParams.get('appName')` equals that exact original string, and (b) the body contains no bare `;` outside the single-quoted URL region. Then temporarily revert the `encodeURIComponent` calls, confirm the test FAILS, restore, confirm it passes. **Record both the verbatim failure output and the verbatim pass in the SUMMARY.** An assertion never shown to fail against a known-bad input passes vacuously and is worth nothing.
    - `34.5-UNTESTED-ITEMS.md`'s `U-34.5-12` row's "Retired by" cell no longer contains the unquoted literal `--no-gui gamelib://launch?appName=`, and does contain the words `security mitigation, not a relaxation`.
    - **`U-34.5-19` is reconciled, and the reconciliation is greppable.** The row still EXISTS in the table (it is amended in place, never deleted), its Status cell contains `RETIRED`, and `grep -cF "retired because the defect was FIXED, not because it was observed passing" 34.5-UNTESTED-ITEMS.md` is exactly 1. That same row names `34.5-44` as its creating plan and `34.5-45` as the fix. A `U-34.5-19` left OPEN, or deleted, fails this criterion equally — the ledger must describe the tree as it now is.
    - **The `main.rs` doc-comment is reconciled.** `grep -cF "no longer emitted by current code (fixed in plan 34.5-45)" src-tauri/src/main.rs` is at least 1, and `grep -c "U-34.5-19" src-tauri/src/main.rs` is UNCHANGED from its value before this task — the citation is corrected, never deleted. Record both counts in the SUMMARY.
    - **The Task 1 `main.rs` diff is COMMENT-ONLY.** `git diff -U0 -- src-tauri/src/main.rs | grep -E '^[+-]' | grep -v '^[+-][+-]' | grep -vE '^[+-][[:space:]]*(///|//)'` produces NO output for this task's hunks. `protocol_url_arg`'s body, the runner-less tolerance and every `#[cfg(test)]` case including `protocol_url_arg_accepts_a_runnerless_url` are unchanged.
    - `cargo build --manifest-path src-tauri/Cargo.toml` succeeds. Run it even though the edit is comment-only — a mis-terminated block comment is precisely how a "comment-only" edit breaks a build.
    - `npx tsc --noEmit` exits 0.
    - The SUMMARY states explicitly that `U-34.5-12` stays **OPEN**: a jest-written `.app` under a redirected homedir is not the live observation the row's own cell names. Ledger Rule 1 — a row retires only by the observation in its own cell, never by a passing suite.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DIAGNOSE F-34.5-G6-08 — build the discriminator before any fix exists</name>
  <read_first>
    - `src/backend/sidecar/sidecarRpc.ts` lines 89-140 — `writeLine` (91-93) and `dispatchInvoke` (108-136). Note that `writeLine` is `JSON.stringify` and that `JSON.stringify` OMITS a key whose value is `undefined`, so `{id, ok:true, result: undefined}` serialises as a frame with no `result` key at all
    - `src/backend/sidecar/shortcutsFlowRegistration.ts` lines 150-197 — the `addToSteam` registration at 168-176 and its `return addNonSteamGame(game)` at 174
    - `src/backend/shortcuts/nonesteamgame/nonesteamgame.ts` lines 200-369 — the whole of `addNonSteamGame`: the four returns at 220/338/353/367, the `if/else` at 341/354 proving no fall-through, and the two networked awaits at 209 (`getWikiGameInfo`) and 281 (`prepareImagesForSteam`) that precede the VDF write at 318-321
    - `src-tauri/src/main.rs` lines 134-168 (`LONG_RUNNING_CHANNELS`, `timeout_for`), 301-357 (`SidecarState::invoke`'s `recv_timeout` arm and the `sidecar_invoke` command) and 4926-4967 (the reader thread's response branch, especially `unwrap_or(Value::Null)` at 4947 and the unknown/timed-out diagnostic at 4957-4959)
    - `src/preload/tauriTransport.ts` lines 88-105 and `src/preload/ipc.ts` lines 37-47 — the two passthrough legs
    - `src/frontend/screens/Game/GameSubMenu/index.tsx` lines 211-230 — `handleAddToSteam`'s `.then((added) => setAddedToSteam(added))` at 219-220, and the `isAddedToSteam` effect at 227-229 that also feeds the same state
    - `src/backend/sidecar/__tests__/appShellFlows.test.ts` lines 218-277 — `collectFrames`/`flush`/`startSidecar`/`writeInvoke`, the harness to reuse
    - `src/backend/sidecar/__tests__/skeletonFlows.test.ts` lines 715-745 — the `[null]` args-array regression test, this project's precedent for driving the REAL transport shape rather than reconstructing an ideal one
    - `34.5-JSON-TRANSPORT-SWEEP.md` — the whole file, especially finding #1 (the live-proven defect and how it was pinned) and finding #8 (a latent hazard recorded rather than silently fixed). This task appends to this document
  </read_first>
  <action>
    Produce a WRITTEN discriminator, then a verdict. Weight test design over explanatory fit: the
    `f10-diagnosis-process-lesson` in this project's memory records what shipping a plausible
    correlation as a cause costs, and this phase has already shipped one unifying hypothesis that
    was wrong. Do not begin by choosing the most likely layer.

    Enumerate the candidate mechanisms as H1..H5 and give each a signature no other hypothesis
    produces:

    **H1 — the observation is a caller/console artifact.** GameLib runs on WKWebView under Tauri, so
    the gate operator used Safari Web Inspector, not Chrome devtools; a top-level `await` there can
    report a wrapper's completion value rather than the resolved value. Signature: the resolved
    value read through an explicit `.then((v) => console.log('[G6-08]', typeof v, JSON.stringify(v)))`
    is a real boolean, while the bare `await` prompt still shows `undefined`.

    **H2 — the handler returns nothing.** Ruled out by source (interfaces fact 1) but PINNED here,
    not assumed. Signature: the emitted response frame carries `result: true`.

    **H3 — the sidecar's response envelope drops the key.** Signature: the emitted stdout line is
    valid JSON with `ok: true` and **no own `result` key**, which `main.rs:4947` then turns into
    `Value::Null` → JS `null`.

    **H4 — the Rust leg substitutes a value.** Signature: `null` at the caller. Note the standing
    fact that `undefined` is unreachable from this leg (interfaces fact 2), so H4 and H3 are
    distinguishable from H1 by the caller-side `typeof` alone: `object`/`null` implicates H3/H4,
    `undefined` implicates H1 or H5.

    **H5 — the 60s Rust bound elapsed.** `addToSteam` is absent from `LONG_RUNNING_CHANNELS` and
    does two networked things before the VDF write (interfaces fact 3), and the VDF write succeeding
    after a caller-side settle is exactly what a timeout-then-late-completion looks like. Signature,
    unique to this mechanism and to nothing else: the promise REJECTS with
    `sidecar invoke timed out`, AND `[shell] response for unknown/timed-out id=` appears in the
    preserved shell output.

    Write that as a hypothesis-by-observable MATRIX into a new `## F-34.5-G6-08 discriminator (gap
    cycle 6, plan 34.5-45)` section of `34.5-JSON-TRANSPORT-SWEEP.md`, with one row per hypothesis
    and one column per observable (caller-side `typeof`; whether the stdout frame has an own
    `result` key; whether the promise rejects; whether the unknown/timed-out shell line appears).
    Every cell must be filled — a matrix with blanks cannot discriminate.

    Then run the offline arms. Add a test to `src/backend/sidecar/__tests__/shortcutsFlows.test.ts`
    that uses the `appShellFlows.test.ts` harness shape to write a REAL
    `{id, kind:'invoke', channel:'addToSteam', args:['<appName>','gog']}` frame into the real
    `startRpcServer` loop and read the REAL emitted stdout line back. Assert on the PARSED FRAME,
    not on the handler's return value: the frame must have `ok === true`, must satisfy
    `Object.prototype.hasOwnProperty.call(frame, 'result')`, and `frame.result` must be `true`. The
    own-property assertion is the H2/H3 discriminator and is NOT implied by `frame.result === true`
    — `JSON.stringify` omits an `undefined` value, so a dropped key and a `false` value are
    different failures. Do not reconstruct the preload call site; a test that rebuilds the call
    shape is a replica and drifts silently.

    Add one source-text assertion pinning interfaces fact 2, so the Rust leg's floor cannot silently
    change: read `src-tauri/src/main.rs` off disk and assert the response branch contains
    `unwrap_or(Value::Null)`. Prove it RED by temporarily editing that literal in a scratch copy of
    the extracted region, confirming the assertion fails, and restoring.

    Record the H5 exposure as an OBSERVATION, not a conclusion: state in the section whether
    `addToSteam` is present in `LONG_RUNNING_CHANNELS` at execution time, name the two networked
    awaits, and state plainly that H5 can only be confirmed or excluded by the live log, because a
    unit test cannot make a real PCGamingWiki request time out.

    Close the section with an explicit VERDICT paragraph in one of exactly three shapes, and no
    other: **IDENTIFIED** (one hypothesis matched its signature and every other hypothesis was
    excluded by an observable it failed — name the excluding observable per hypothesis);
    **NARROWED** (two or more survive; name them, name the observable that would separate them, and
    state which of them can only be separated live); or **NOT REPRODUCED** (no offline arm
    reproduced the drop; the surviving candidates are H1 and H5 and both are live-only). A verdict
    that names a cause without naming the observable that excluded each rival is exactly the
    correlation-as-cause failure this task exists to prevent, and does not satisfy this task.

    **Do not fix anything in this task.** Any instrumentation added to reach the verdict is removed
    before the task completes.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/shortcutsFlows.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-JSON-TRANSPORT-SWEEP.md` contains a heading matching `## F-34.5-G6-08 discriminator`.
    - That section contains a markdown table with at least 5 hypothesis rows (H1..H5) and at least 4 observable columns, and `grep -c "^| H" ` over the section returns at least 5. No cell is empty or `TBD`.
    - The section's final paragraph begins with exactly one of the literals `VERDICT: IDENTIFIED`, `VERDICT: NARROWED` or `VERDICT: NOT REPRODUCED`.
    - If the verdict is `IDENTIFIED`, the paragraph names, for EVERY other hypothesis, the specific observable whose value excluded it. A verdict asserting a cause without per-rival exclusions fails this criterion.
    - A test in `shortcutsFlows.test.ts` writes a literal `{id, kind:'invoke', channel:'addToSteam', args:[...]}` frame into `startRpcServer` and asserts on the parsed stdout frame. `grep -c "hasOwnProperty.call(frame, 'result')" src/backend/sidecar/__tests__/shortcutsFlows.test.ts` is at least 1 — an own-key assertion, distinct from a value assertion, must be present.
    - A source-text assertion reads `src-tauri/src/main.rs` and matches `unwrap_or(Value::Null)`, and the SUMMARY records the verbatim failure message observed when that literal was perturbed plus the verbatim pass after restoring.
    - The section states, in its own words, whether `addToSteam` appears in `LONG_RUNNING_CHANNELS` at execution time and names `getWikiGameInfo` and `prepareImagesForSteam` as the two networked awaits preceding the VDF write.
    - `git diff` for this task shows changes ONLY to `34.5-JSON-TRANSPORT-SWEEP.md` and `src/backend/sidecar/__tests__/shortcutsFlows.test.ts`. No production source file changes in Task 2 — a fix landing in the diagnosis task means the discriminator was never run.
    - `grep -c "console.log" src/backend/sidecar/__tests__/shortcutsFlows.test.ts` is 0 — instrumentation removed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix per the verdict, then sweep all 34 invoke channels for the same drop</name>
  <read_first>
    - The `## F-34.5-G6-08 discriminator` section Task 2 wrote — its VERDICT paragraph selects this task's branch, and nothing else does
    - `src/common/types/ipc.ts`'s `AsyncIPCFunctions` block — the declared return type for each of the 34 channels. **Read only; plan 34.5-43 owns every edit to this file**
    - `src/backend/sidecar/runnerAuthFlowRegistration.ts`, `wineToolsFlowRegistration.ts`, `shortcutsFlowRegistration.ts`, `runnerMiscFlowRegistration.ts` — every `ipcMain.handle` body in all four
    - `34.5-JSON-TRANSPORT-SWEEP.md` — the existing 11-row findings table and its `## Verdict` and `## Ledger update` sections, the shape the new sweep section must match
    - `34.5-UNTESTED-ITEMS.md` — the preamble's three Rules and the table shape of rows `U-34.5-12`/`U-34.5-15`/`U-34.5-16`, so the new row matches. **`U-34.5-16` is plan 34.5-43's `getInstallInfo` timeout row and is not this plan's to touch or retire.** Read this file OFF DISK at execution time to find the next free `U-34.5-NN`: plan 34.5-44 (wave 1) has already appended `U-34.5-17`..`U-34.5-20`, and plans 34.5-46/48/49 append this same wave, so **no id may be hardcoded out of this plan text**
    - `src/backend/__tests__/longRunningChannels.test.ts` lines 41-90 and 347-380 — `EXPECTED_LONG_RUNNING_CHANNELS` and the exact-set assertion, needed only if branch C fires
    - `src/frontend/screens/Game/GameSubMenu/index.tsx` lines 211-230 — the toggle whose failure to flip is part of the finding, not separate from it
  </read_first>
  <action>
    **Fix, branching on Task 2's verdict. Take exactly one branch and name it in the SUMMARY.**

    **Branch A — the verdict names the sidecar envelope (H2/H3).** Fix it where the discriminator
    located it. If the mechanism is `JSON.stringify` omitting an `undefined` `result`, do NOT
    blanket-coerce `result` to `null` in `dispatchInvoke` — that would silently convert every
    void-returning handler's `undefined` into `null` across all 34 channels and reproduce this
    sweep's own finding #8 hazard in the opposite direction. Fix the specific handler the
    discriminator named, and record the general hazard as a row in the sweep table instead.

    **Branch B — the verdict is `NOT REPRODUCED` and the surviving candidates are H1 and H5.** Ship
    no speculative production change. Instead: (i) record in the sweep section that F-34.5-G6-08's
    *transport* claim is UNCONFIRMED and that the finding's remaining verified content is the UI
    symptom; (ii) hand the live discriminator to the gate contract by writing, into the sweep
    section, the exact devtools invocation the operator must use —
    `window.api.addToSteam(appName, runner).then((v) => console.log('[G6-08]', typeof v, JSON.stringify(v)))`,
    a `.then` console readout rather than a bare `await`, because the bare `await` prompt is
    precisely what H1 says is unreliable — plus the two log greps that separate H5
    (`sidecar invoke timed out` and `response for unknown/timed-out id=`); (iii) leave `U-34.5-15`
    OPEN with its cell amended to require the `.then` readout. Plan 34.5-50 authors the contract in
    wave 3 and will read this.

    **Branch C — the verdict names the Rust leg (H5, the 60s bound).** `src-tauri/src/main.rs` is
    plan 34.5-44's file, but 34.5-44 is **wave 1** and this plan is **wave 2**, so the edit is
    sequenced, not concurrent — that ordering is the entire reason `depends_on: [34.5-44]` exists.
    Before editing, confirm 34.5-44 has landed by checking that `main.rs` contains the `gamelib://`
    / single-instance handling that plan added; if it has not, STOP and report the ordering
    violation rather than editing. Add `addToSteam` to `LONG_RUNNING_CHANNELS` with a comment in the
    house style of the `oauthCaptureLogin` and `humbleStartLogin` entries, naming F-34.5-G6-08, the
    two networked awaits, and the measured evidence from Task 2. Then update
    `EXPECTED_LONG_RUNNING_CHANNELS` in `src/backend/__tests__/longRunningChannels.test.ts` —
    deliberately, as a set addition; that test's exact-set assertion exists to make this a
    conscious act. State the DoS tradeoff explicitly in the SUMMARY: the exemption converts a wrong
    answer at 60s into a never-settling promise, which is `main.rs`'s own stated bargain
    (`LONG_RUNNING_CHANNELS`' header comment).

    **All three branches share one obligation.** A fix that makes the handler return a boolean while
    `GameSubMenu/index.tsx:220`'s `.then((added) => setAddedToSteam(added))` still fails to flip the
    label has **NOT** closed F-34.5-G6-08. State in the SUMMARY which of the two halves — the
    resolved value, and the visible toggle — this plan's evidence covers, and record the other as
    still requiring the live observation named in `U-34.5-15`'s own cell. Note while reading
    `GameSubMenu` that `isAddedToSteam` (the effect at :227) feeds the same state, so a live PASS
    must distinguish "the toggle flipped because `addToSteam` resolved `true`" from "the toggle
    flipped on a later `isAddedToSteam` read".

    **Then the sweep — it is part of `U-34.5-15`'s retirement condition, not an extra.**

    Enumerate every `ipcMain.handle` registration in the four modules
    (`runnerAuthFlowRegistration.ts`, `wineToolsFlowRegistration.ts`, `shortcutsFlowRegistration.ts`,
    `runnerMiscFlowRegistration.ts`) with a MULTI-LINE-capable pattern. A single-line
    `ipcMain\.handle\('([^']+)'` regex finds only **23** of the **34**, because 11 registrations put
    the channel name on the following line — an enumeration that returns 23 is proof of an
    incomplete sweep, not a smaller surface. Per-module counts are 10 / 9 / 4 / 11.

    For each of the 34, record: the channel; its module; its declared return type from
    `AsyncIPCFunctions`; whether the handler body has a `return` producing a value on every path;
    and a classification of **RETURNS** (declared non-void, handler returns it), **VOID-PARITY**
    (declared `Promise<void>`, handler correctly returns nothing — `removeFromSteam` and
    `refreshWineVersionInfo` are the known examples, the latter with an inherited
    swallow-and-return-nothing shape already documented in place), or **DROP** (declared non-void,
    handler has a path that returns nothing). Write all 34 rows into a new
    `## Invoke-return-value sweep (gap cycle 6, F-34.5-G6-08 / U-34.5-15)` section of
    `34.5-JSON-TRANSPORT-SWEEP.md`. **Zero DROP hits is a legitimate outcome** — the failure this
    task must avoid is covering fewer than 34 channels, not finding nothing. State the count
    explicitly either way.

    While reading the declared types, note whether any of the 34 shows the CLASS of silent-shape
    defect plan 34.5-43 found at `ipc.ts:217` — a declared parameter ORDER that disagrees with what
    the handler unpacks out of `args`, where both slots are the same type and `tsc` is therefore
    blind. Record any hit as a finding row in the sweep section with its file and line. **Do not
    edit `src/common/types/ipc.ts`** — plan 34.5-43 owns that correction, and a second editor would
    collide with it.

    **Sweep fence.** `steamFlowRegistration.ts` is plan 34.5-46's file **this same wave** and
    `use_worktrees=false`, so any drop found there is RECORDED as a finding row in
    `34.5-JSON-TRANSPORT-SWEEP.md` naming the file, line and channel, and is **never edited here**.
    Say so in the section's own text so a later reader does not mistake the omission for coverage.

    Create `src/backend/sidecar/__tests__/invokeReturnValueSweep.test.ts` as the anti-rot gate, so
    this sweep does not become the fifth `publicdir-getapppath-chunking`-style recurrence. It reads
    the four registration modules off disk, extracts every `ipcMain.handle` channel with the
    multi-line pattern, asserts the extracted count is exactly 34 with per-module counts
    10/9/4/11, and asserts that every channel whose `AsyncIPCFunctions` entry is declared non-void
    has a `return` inside its handler body. Carry an explicit allow-list for VOID-PARITY channels,
    each entry annotated with the reason, so the gate is a statement about intent rather than a
    count. **Prove the gate RED at authoring time**: temporarily delete the `return` keyword from
    `shortcutsFlowRegistration.ts`'s `addToSteam` body, confirm the test fails, restore, confirm it
    passes.

    Add ONE new row to `34.5-UNTESTED-ITEMS.md` per **Ledger Rule 2**. **Do NOT hardcode the id out
    of this plan text — re-read `34.5-UNTESTED-ITEMS.md` off disk at execution time and take the next
    free `U-34.5-NN`.** Plan 34.5-44 (wave 1, landed before this plan) has already committed rows
    `U-34.5-17`, `U-34.5-18`, `U-34.5-19` and `U-34.5-20`, and plans 34.5-46, 34.5-48 and 34.5-49
    append to this same file in this same wave on the same tree (`use_worktrees=false`) — so the
    highest id present on disk is the only authority. Append at the end of the table; renumber
    nothing, reuse nothing, delete nothing. State the id you allocated verbatim in the SUMMARY.

    Subject: whether macOS ImageIO, via `sips`, decodes a REAL GOG `goggame-<appId>.ico` — which is
    typically a multi-image DIB/BMP-encoded container — as opposed to the synthetic PNG-embedded ICO
    the unit fixture builds. **The row's Item cell must contain the literal marker phrase
    `ImageIO/sips multi-image .ico decode`**, because this row is proven by its SUBJECT and never by
    its id — see the acceptance criterion and its RED reasoning. Evidence cell: the fixture was
    verified at planning time only in its PNG-embedded form; the two encodings take different
    decoder paths. Retirement condition: the same live session that retires `U-34.5-12`, additionally
    showing a non-empty `Contents/Resources/shortcut.icns` inside the produced `.app` for a real GOG
    title whose install directory contains a `goggame-*.ico`. Status: OPEN, created by plan 34.5-45.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/invokeReturnValueSweep.test.ts src/backend/sidecar/__tests__/shortcutsFlows.test.ts src/backend/__tests__/longRunningChannels.test.ts --silent &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY names which branch (A, B or C) was taken and quotes Task 2's VERDICT line verbatim as the reason. A fix without a quoted verdict fails this criterion.
    - `34.5-JSON-TRANSPORT-SWEEP.md` contains a heading matching `## Invoke-return-value sweep`, and that section's table has exactly **34** channel rows. Verify with a row count, and state the per-module tally 10/9/4/11 in the section text.
    - The section states an explicit integer count of **DROP**-classified channels, including when that integer is 0.
    - The section contains a sentence naming `steamFlowRegistration.ts`, plan `34.5-46` and the words `recorded as a finding` and `not edited`.
    - `npx jest src/backend/sidecar/__tests__/invokeReturnValueSweep.test.ts` exits 0, and the test asserts the literal counts 34 and 10/9/4/11. A gate that accepts 23 channels fails this criterion.
    - The SUMMARY records the verbatim failure message observed when `addToSteam`'s `return` keyword was deleted, and the verbatim pass after restoring. A gate never shown to fail against a known-bad input is vacuous.
    - `git diff --name-only src/common/types/ipc.ts` is EMPTY — plan 34.5-43 owns that file.
    - `git diff --name-only src/backend/sidecar/steamFlowRegistration.ts` is EMPTY — plan 34.5-46 owns that file this wave.
    - If branch C was NOT taken: `git diff --name-only src-tauri/ src/backend/__tests__/longRunningChannels.test.ts` is EMPTY. If branch C WAS taken: `EXPECTED_LONG_RUNNING_CHANNELS` contains `addToSteam`, `npx jest src/backend/__tests__/longRunningChannels.test.ts` exits 0, and the SUMMARY states the DoS tradeoff in its own words AND records the check that confirmed plan 34.5-44 had already landed in `main.rs`.
    - **The new row is proven by its SUBJECT, not by its id.** `grep -cF "ImageIO/sips multi-image .ico decode" 34.5-UNTESTED-ITEMS.md` is exactly 1; that single hit is a table row; its Status cell contains `OPEN`; and its "Created by" cell contains `34.5-45`. **RED reasoning, and it must be recorded:** run the identical grep BEFORE writing the row and confirm it returns **0**, then again after and confirm **1**, and put both verbatim outputs in the SUMMARY. A bare `grep -c "U-34.5-17"` would pass whether or not this plan wrote anything at all, because plan 34.5-44 committed `U-34.5-17` in wave 1 — it cannot discriminate "this plan did its job" from "this plan did nothing", which is exactly this project's signature vacuous-assertion failure. The subject grep can only pass if THIS plan added THIS row.
    - The SUMMARY states verbatim the `U-34.5-NN` id this plan allocated, and that id is NOT one of `U-34.5-17`..`U-34.5-20` — those four are plan 34.5-44's, committed in wave 1.
    - `grep -c "U-34.5-16" 34.5-UNTESTED-ITEMS.md` is unchanged from before this plan — plan 34.5-43's row is neither edited nor retired here.
    - The SUMMARY states which of F-34.5-G6-08's two halves (the resolved value; the visible submenu toggle) this plan's evidence covers, and that `U-34.5-15` stays **OPEN** pending the live observation named in its own cell.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game metadata → generated `run.sh` | `app_name` and `runner` are interpolated into a `#!/bin/bash` script the user later double-clicks; store-supplied and sideload-supplied values cross here |
| remote store CDN / game install dir → image decoder | `getIcon` returns a downloaded `art_square` jpg or a `goggame-<id>.ico` from a game's own install directory; those bytes now reach macOS ImageIO through a child process |
| renderer → sidecar invoke | `appName` and `runner` cross as JSON args and select a store manager |
| sidecar → shared `/tmp` | image conversion materialises intermediate files on a multi-user-readable filesystem |
| sidecar response envelope → renderer state | the resolved value drives `setAddedToSteam`, a user-visible truth claim about the Steam library |
| diagnosis verdict → wave-3 gate contract | plan 34.5-50 writes gate item 4 from this plan's recorded verdict and ledger cells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C6-17 | Elevation of privilege | `shortcuts.ts:227-235`'s `run.sh` body — the launch command is interpolated UNQUOTED into a bash script, so an `app_name` or `runner` containing `;`, backticks or `$(...)` executes on double-click | mitigate | Task 1 double-quotes the exe path, single-quotes the whole `gamelib://` URL, and percent-encodes both query values with `encodeURIComponent`. `protocol.ts:17-25`'s `new URL(...)` + `searchParams.get(...)` decode them, so plan 34.5-44's deep link is unaffected. Pinned by an acceptance criterion that drives a real `app_name` of `x&runner=gog;touch /tmp/gamelib-pwned` and is **proven RED** by reverting the encoding. **This plan creates the exposure** by making a structurally dead path reachable, so it owns the mitigation |
| T-34.5-C6-18 | Tampering | untrusted remote icon bytes (a CDN `art_square` jpg, or a `goggame-*.ico` from a game's install dir) reaching macOS ImageIO via `sips` | mitigate | Bytes are written to a fresh `mkdtempSync` directory at mode `0o600` — never a predictable path, never a name derived from a game title or URL, so there is no symlink/TOCTOU target to pre-place. Input is size-capped at 32 MiB before it is written. The decoder itself is the OS's, running out-of-process; a malformed image crashes the child, not the sidecar. Temp directory removed in a `finally` |
| T-34.5-C6-19 | Tampering | argument injection into the converter invocation | mitigate | `execFileSync('/usr/bin/sips', argv, ...)` — absolute path (a bare `sips` would be `PATH`-hijackable) and an argv ARRAY, never a shell string. Every numeric dimension is validated with `Number.isInteger` and clamped before it reaches argv. Acceptance criteria grep for both properties, and for the absence of any shell-string exec form |
| T-34.5-C6-20 | Denial of service | a crafted image making the converter hang or emit unbounded output, wedging the sidecar's invoke | mitigate | `{ timeout: 15_000, maxBuffer: 32 * 1024 * 1024 }` on the child process, plus the 32 MiB input cap. 15s sits well inside `main.rs`'s 60s `INVOKE_TIMEOUT`, so a stuck conversion surfaces as a named shim error rather than as an opaque transport timeout |
| T-34.5-C6-21 | Information disclosure | intermediate `shortcut.png` / conversion temp files left on a shared filesystem | mitigate | Per-call `mkdtempSync` directory, mode `0o600`, removed in a `finally`. Note the pre-existing `shortcuts.ts:265-271` behaviour writes and then `unlinkSync`s its own `shortcut.png` inside the `.app`'s Resources folder — inherited, unchanged, and recorded here so it is not rediscovered as new |
| T-34.5-C6-22 | Repudiation | a plausible-but-unverified correlation shipped as the cause of F-34.5-G6-08 | mitigate | Task 2 requires a hypothesis-by-observable matrix with every cell filled, a verdict in one of exactly three literal shapes, and — for an `IDENTIFIED` verdict — a named excluding observable per rival hypothesis. Task 3's branch is selected by the verdict and the SUMMARY must quote it verbatim. This project's own `f10-diagnosis-process-lesson` is the reason this is a gate and not a suggestion |
| T-34.5-C6-23 | Denial of service | branch C removing `addToSteam`'s 60s invoke bound by adding it to `LONG_RUNNING_CHANNELS` | accept, conditionally | Taken ONLY on an H5 verdict. The exemption trades a wrong answer at 60s for a never-settling promise — `main.rs`'s own stated bargain for the eleven channels already on that list, and the honest failure mode, since the sidecar dying still wakes `rx.recv()` with a disconnect error. `longRunningChannels.test.ts`'s exact-set assertion forces the addition to be a conscious act, and the SUMMARY must state the tradeoff. Also requires confirming plan 34.5-44 has already landed in `main.rs` before editing |
| T-34.5-C6-24 | Tampering | the `shortcutsFlows.test.ts` mocks being adjusted rather than removed, preserving the blindness that hid F-34.5-G6-07 from a 4462-test green suite | mitigate | Acceptance criteria require `createFromBuffer: jest.fn()` and the entire `@shockpkg/icon-encoder` mock to grep to ZERO occurrences, the placeholder fixture string `fake-fixture-icon-bytes` to be gone, and the file's header docstring — which currently asserts both are mocked — to be corrected. A stale docstring is what the next reader trusts |
| T-34.5-C6-25 | Spoofing | the UI asserting "Added to Steam" (or failing to) independently of what the backend actually wrote | mitigate | Task 3 requires the SUMMARY to state which of the two halves the evidence covers, and to note that `GameSubMenu`'s `isAddedToSteam` effect at `:227` feeds the same state — so a live PASS must distinguish a toggle that flipped because `addToSteam` resolved `true` from one that flipped on a later `isAddedToSteam` read. `U-34.5-15` stays OPEN until its own cell's observation is captured |
| T-34.5-C6-26 | Repudiation | plan 34.5-44's two artifacts describing the UNQUOTED `run.sh` — ledger row `U-34.5-19` and the `main.rs` doc-comment that cites it as the rationale for the parser's runner-less tolerance — surviving unchanged after Task 1 quotes the template, leaving the ledger describing a defect that no longer exists and a committed Rust comment asserting a false fact | mitigate | Task 1 retires `U-34.5-19` in the SAME commit against its own SECOND retirement branch, stating in the row that it retired because the defect was **FIXED** and not because it was observed passing, and rewrites the `main.rs` comment to past tense while deliberately KEEPING the tolerance (pre-existing `.app` bundles on disk still emit the truncated URL). Acceptance criteria pin both with `grep -cF` on literal phrases and require the `main.rs` diff to be comment-only. `U-34.5-11`'s "retired because its subject was deleted — it is NOT a pass" cell is the precedent |
| T-34.5-C6-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs **no packages**, so `34.5-RESEARCH.md`'s `## Package Legitimacy Audit` ("Not applicable — no new packages") stays true. `sips` and `iconutil` are OS-provided binaries invoked by absolute path, not dependencies. Any install would require slopcheck plus a blocking human checkpoint before the install command runs |
</threat_model>

<verification>
- `npm run test:ci` exits 0; the verbatim `Tests:` line is recorded in the SUMMARY and any delta against the phase's last recorded count is explained rather than absorbed. Removing two mocks from `shortcutsFlows.test.ts` makes previously-mocked code real — a changed count here is expected and must be accounted for, not waved through.
- `npx tsc --noEmit` exits 0.
- `npx jest src/backend/sidecar/__tests__/nativeImageShim.test.ts src/backend/sidecar/__tests__/shortcutsFlows.test.ts src/backend/sidecar/__tests__/invokeReturnValueSweep.test.ts` exits 0.
- `npx jest src/backend/sidecar/__tests__/electronUntouched.test.ts src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts` exits 0 — SEAM Invariants A and B are unweakened. The 3 dropped and 16 deferred channels still reject non-fatally.
- `npx jest src/backend/__tests__/longRunningChannels.test.ts` exits 0. If branch C was not taken this file must be untouched; if it was, the exact-set assertion must have been updated deliberately.
- `git diff --name-only` lists no file outside this plan's `files_modified`. Specifically ZERO paths for `src/backend/sidecar/steamFlowRegistration.ts` (plan 34.5-46, same wave), `src/common/types/ipc.ts` (plan 34.5-43), `src/backend/protocol.ts` / `src/backend/sidecar/bootstrap.ts` (plan 34.5-44), `src/frontend/screens/Settings/sections/AdvancedSettings/index.tsx` (plan 34.5-48) and `.planning/IPC-PORT-INVENTORY.md` (plans 34.5-43 / 34.5-49). `src-tauri/src/main.rs` IS expected in the diff — Task 1's comment-only `U-34.5-19` reconciliation always touches it — but if branch C was NOT taken, that file's entire diff must satisfy Task 1's comment-only check and no other path under `src-tauri/` may appear.
- Electron parity (REQ-34.5-13): `npm start` boots. The real `electron` module supplies its own `nativeImage`, so the shim is reachable only through the sidecar's `electron` alias; confirm by reading the diff hunk in `electronStub.ts`, not by inferring from a green suite. The `shortcuts.ts` quoting/encoding change DOES affect the Electron build's `run.sh` too — that is intended (it is the same injection on both builds) and must be stated as a deliberate behavioural change in the SUMMARY rather than presented as invariant-preserving.
- `34.5-JSON-TRANSPORT-SWEEP.md` carries BOTH new sections — the discriminator with its literal verdict line, and the 34-row sweep with its explicit DROP count.
- `34.5-UNTESTED-ITEMS.md` carries three changes and no others: the amended `U-34.5-12` cell, `U-34.5-19` marked RETIRED against its own second branch with the "FIXED, not observed passing" wording, and ONE new appended row carrying the literal `ImageIO/sips multi-image .ico decode` at the next free id. `U-34.5-16` is untouched, and no row other than `U-34.5-12`/`U-34.5-19` is edited.

**Redact before committing:** no `access_token`, `refresh_token`, cookie value, session identifier,
OAuth `code=` value, or Steam `userdata` account id may appear in any `.planning/` file this plan
writes. Grep every touched `.planning/` file for `token=`, `code=`, `Cookie` and `userdata` and
record the result.
</verification>

<success_criteria>
The sidecar has a real `nativeImage`: `convertPngToICNS` completes end to end with NOTHING mocked,
`generateMacOsApp` reaches its `run.sh` write, and the two mocks that hid F-34.5-G6-07 from a green
suite are gone rather than adjusted. The `run.sh` body it now produces cannot execute a command
smuggled through a game's `app_name`, proven by an assertion shown RED against the un-encoded form.
F-34.5-G6-08 carries a written discriminator with a verdict in one of three literal shapes, the fix
branch is the one that verdict selects, and no rival hypothesis was excluded by plausibility. All 34
invoke-kind channels in the four 34.5 registration modules are classified with an explicit DROP
count, pinned by an anti-rot gate proven to reject a deleted `return`; a drop in
`steamFlowRegistration.ts` is a recorded finding and nothing was edited there.

**Neither finding closes on this plan.**

`F-34.5-G6-07` / `U-34.5-12` closes when a live session produces
`<title>.app/Contents/MacOS/run.sh` launching the shell exe with `--no-gui` and a `gamelib://launch`
URL whose parsed `appName` and `runner` match the game, with no `Error generating MacOS App` line in
a preserved `gamelib.log`.

`F-34.5-G6-08` / `U-34.5-15` closes when `await window.api.addToSteam(...)` resolves to `true` AND
the submenu label flips to "Remove from Steam" without a reload — both halves, and distinguishable
from a flip caused by the later `isAddedToSteam` read.

One new ledger row (real `goggame-*.ico` decoding, id taken from the live file rather than hardcoded,
identified by the literal `ImageIO/sips multi-image .ico decode`) is OPEN from this plan's own commit.

`U-34.5-19` is RETIRED in that same commit against its own SECOND branch — because the defect was
FIXED, never because the unquoted body was observed passing — and the `main.rs` doc-comment plan
34.5-44 wrote about that unquoted template no longer asserts it as present-tense fact, while the
runner-less tolerance it justifies is deliberately KEPT for `.app` bundles already on disk.

Ledger Rule 1 binds: a row retires only by the observation named in its own cell — never by a
passing suite, never by inference from a neighbouring row.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-45-SUMMARY.md` when done
</output>
