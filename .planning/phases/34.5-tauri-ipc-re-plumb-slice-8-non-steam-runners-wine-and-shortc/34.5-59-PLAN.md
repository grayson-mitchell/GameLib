---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 59
type: execute
wave: 5
gap_closure: true
cycle: 7
depends_on: ["34.5-56", "34.5-57", "34.5-58"]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
autonomous: false
blocking: true
requirements: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-12]
executes_gate: 34.5-LIVE-GATE-RERUN-4.md

must_haves:
  truths:
    - "The fifth blocking live gate is executed on real macOS hardware against the contract plan 34.5-56 authored, with no clause rewritten mid-run except by an explicitly recorded strike."
    - "Every item's verdict is independently verified against archived disk and log evidence, never taken on the operator's word alone."
    - "The arithmetic reconciles: items_passed + items_failed + items_blocked + items_not_attempted == 4."
    - "Anything short of a clean 4 of 4 routes to gap cycle 8 inside Phase 34.5 — no forced close, no advisory note, no deferred UAT entry, no pre-authorised override (D-08)."
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md"
      provides: "Every RESULT slot filled, frontmatter verdict and four counts set, known-holes resolved row by row, new findings recorded"
      contains: "Overall verdict"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md"
      provides: "A new dated section for this run's own findings"
      contains: "fifth blocking live gate"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN-4.md frontmatter"
      to: "the four item Result slots"
      via: "arithmetic reconciliation against items_total"
      pattern: "items_total: 4"
---

<objective>
Execute the fifth blocking live gate on real macOS hardware.

Purpose: Phase 34.5 has not closed across four runs. D-08 admits no partial pass. This run either
produces a clean 4 of 4 or it routes to gap cycle 8.

Output: `34.5-LIVE-GATE-RERUN-4.md` filled in full, with a verdict, reconciled arithmetic, a
row-by-row known-holes resolution, and this run's own findings recorded but not diagnosed.
</objective>

<preflight_halt_check>
**Before anything else, read `34.5-CYCLE7-REVIEW.md`'s frontmatter.** If `halt_gate: true`, this
plan does NOT launch. Record the halt, name the blocker(s), and stop — the gate runs only after each
blocker is fixed and that file is amended to `halt_gate: false` with the fix recorded.

**Also confirm plan 34.5-58's session is torn down**: `pgrep -f 'target/debug/gamelib-shell'` must
be empty. A surviving second instance splits the `[shell]` sink while leaving `gamelib.log` shared,
producing a populated, plausible-looking log next to a transcript missing every scored literal — the
most dangerous evidence shape this standard can produce, and a measured one.
</preflight_halt_check>

<binding_rules>
- **This plan does not author the contract; plan 34.5-56 did.** Decision D-E: the plan that authors
  a gate contract may never also run it, and the converse discipline applies here — the runner
  does not silently rewrite a clause it finds inconvenient. A clause that turns out to be
  unperformable is **STRUCK, explicitly, with its reasoning recorded in place**, and counts toward
  the contract-defect tally (T-34.4.2-42) rather than being quietly reinterpreted. Run 4's item 3
  is the precedent: a console step was struck mid-run and its real-UI replacement was recorded as a
  considered substitution, not a silent deviation.
- **`34.5-LIVE-GATE.md`, `-RERUN.md`, `-RERUN-2.md` and `-RERUN-3.md` are NOT edited.** This plan
  edits `-RERUN-4.md` only.
- **A mutating call's own report is never proof of its effect.** Every item's verdict is verified
  from archived disk and log evidence by this executor, independently of what the operator reports.
  Run 4's item 4 is the precedent: the operator reported "done" and the independent check found the
  action had never happened.
- **A green suite is necessary and never sufficient.** This gate has beaten one four times running.
- **Findings are RECORDED, not diagnosed.** Diagnosis belongs to the SUMMARY and to gap cycle 8 if
  one is needed. Continue the `F-34.5-G6-` sequence from the next free id — `F-34.5-G6-22` unless
  plan 34.5-54's or 34.5-57's SUMMARY allocated further ids, in which case take the next free one
  and state which it is.
- **No DevTools console.** It cannot be pasted into on this build. Every gesture goes through real
  UI controls.
</binding_rules>

<context>
@.planning/PROJECT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-ROUTING.md
@.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the contract's preconditions and all four items on real macOS hardware</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md</files>
  <read_first>
    - .planning/phases/.../34.5-LIVE-GATE-RERUN-4.md (the WHOLE contract — preconditions, the
      capture standard, all four items and their clause lettering, the cross-cutting observations)
    - .planning/phases/.../34.5-CYCLE7-REVIEW.md (frontmatter only, for `halt_gate`)
    - .planning/phases/.../34.5-KEYRING-ARM-SESSION.md (frontmatter only, to confirm teardown)
  </read_first>
  <what-built>
    Cycle 7 shipped four fixes, none of them live-proven: an in-page origin banner inside the login
    sheet (34.5-52, closing the invisible-title defect that failed item 1); redacted nile credential
    logging (34.5-53); a corrected and test-pinned DXVK evidence-line set plus the discovery that the
    install direction DOES call `runWineCommand` (34.5-54); and honest declines for all ten
    SteamGridDB and winetricks call sites (34.5-55).
  </what-built>
  <action>
Execute `34.5-LIVE-GATE-RERUN-4.md` on real macOS hardware with the operator present. This task
performs no code change and fills no RESULT slot: it reads `halt_gate` from `34.5-CYCLE7-REVIEW.md`,
asserts plan 34.5-58's teardown, ticks every precondition in order, captures the session per the
dual-sink standard, and drives all four items plus the mandatory uninstall observation through real
UI controls only. Clause scoring happens in Task 2, from the archived evidence, never from the
operator's report.
  </action>
  <how-to-verify>
    Work through `34.5-LIVE-GATE-RERUN-4.md` from the top. Tick each precondition in order and tell
    me its result before moving on — an unstated precondition is exactly how a gate becomes UAT debt.

    Set-up, in this order:
    1. Confirm nothing is running: `pgrep -f 'target/debug/gamelib-shell'` prints nothing.
    2. Create the session directory the contract names, and start every launch under
       `tee -a <session-dir>/terminal.log` with the delimiter line the contract specifies. Never a
       bare `tee`, never a `>` redirect — a truncating capture has already destroyed two items'
       evidence once in this project.
    3. Launch with `pnpm tauri:dev` — never `tauri dev`.

    Then the four items, in the contract's order. Two of them need your close attention because
    they are where the last run went wrong:

    **Item 1, the origin banner.** When the GOG login sheet appears, look at its TOP-LEFT corner.
    There should be a small dark label showing the site's origin. Read it out to me verbatim. Then
    continue the login until the page moves to the next step, and read the label out again — I
    expect the two readings to differ (`login.gog.com` then `embed.gog.com`). Also confirm the
    "Cancel sign-in" strip is still visible in the top-RIGHT and looks clickable — but do NOT click
    it, that would cancel the login and destroy the rest of item 1's evidence.

    **Item 4, the DXVK toggle.** This is the item that has now been unmeasured four runs running,
    because last time the switch was already ON and nothing was ever exercised. Before you touch
    anything, I will check the config file and the prefix from disk and tell you the starting state.
    If the switch is already ON, you will click it OFF first, I will re-check the prefix, and only
    then will you click it ON. Tell me each time whether you saw the label change to "Please
    wait..." and whether the switch actually moved. After the ON click, wait — do not close the
    settings panel for at least 30 seconds; the backend finishes its work after the switch flips.

    Also, at some point in the session, please uninstall one GOG game and tell me whether its
    Library tile stops showing as installed WITHOUT restarting the app. This one is an observation,
    not a scored item — but it has been skipped two runs running and I need either the observation
    or your explicit "not attempted, because X".

    Do not close the app until I confirm the logs are archived.
  </how-to-verify>
  <acceptance_criteria>
    - `halt_gate` was read and is `false`; recorded.
    - Every precondition has a ticked result recorded in the contract.
    - `pgrep -f 'target/debug/gamelib-shell'` is empty before launch 1; the PID is recorded after
      the window appears and again at teardown; the count is 1 throughout. If it is ever not 1, the
      launch is ABORTED and re-run — its evidence is not scorable.
    - The session directory's closing inventory (`ls -la` plus `wc -l <session-dir>/*`) is recorded
      and no capture file is zero-length.
    - `gamelib.log` is archived per launch BEFORE the next launch renames it to `.old`; any
      pre-existing `.old` is archived on the first pass.
    - Item 4's genuine-OFF precondition is satisfied and RECORDED from disk — the config value, its
      mtime, and the `ls` of the prefix showing `current_dxvk-macOS` absent — before the scored ON
      click.
    - The uninstall observation is either gathered or explicitly refused with a named blocker.
  </acceptance_criteria>
  <resume-signal>Report each precondition result, then each item's observations as you complete it. Type "logs archived" once the session directory inventory is captured and before closing the app.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Independently verify every clause from archived evidence and fill the contract</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md</files>
  <read_first>
    - The archived `terminal.log` and every `gamelib-launch-${N}.log` in the session directory
    - .planning/phases/.../34.5-LIVE-GATE-RERUN-4.md (every clause and its named sink)
    - .planning/phases/.../34.5-54-SUMMARY.md (the corrected DXVK evidence facts and the OFF-click
      discrimination rule)
  </read_first>
  <action>
Fill every RESULT slot in `34.5-LIVE-GATE-RERUN-4.md` from ARCHIVED evidence. For each clause,
record the exact command run and its raw output, including empty outputs — "returns nothing at all,
not even an attempt" is a materially different finding from "returned a failure", and run 4's own
resolution table depended on that distinction.

Per-clause discipline:
- Grep the sink the clause names, not a different one. `[shell]` lines are in `terminal.log`;
  sidecar `logInfo`/`logWarning`/`logDebug` are in `gamelib-launch-${N}.log`; sidecar `console.*` is
  nowhere. A required line grepped from the wrong sink is not an absence.
- For item 4, scope every grep to lines AFTER the recorded timestamp of the scored ON click, and
  discriminate `reg add` / `native,builtin` (install direction) from `wineboot` / `reg delete`
  (restore direction, i.e. the OFF click you may have had to perform first). Record both the
  timestamp used and the discriminating tokens.
- For item 2's redaction clause, record all four greps. **If any match reveals a real credential,
  redact it in place immediately and never quote the value** — the surrounding structure is the
  evidence, the value is not.
- For item 1's banner clauses, record the operator's two verbatim transcriptions AND the shell's own
  `login origin banner injected for` / `origin banner updated len=` lines from the transcript. Two
  independent components must agree.

Then complete the contract's remaining sections:
- **Verdict table**: one row per item with PASS / FAIL / BLOCKED / NOT ATTEMPTED and a one-line
  reason. **Do not soften a FAIL.** An item with 8 of 9 clauses passing is FAIL, exactly as run 4
  recorded for item 1.
- **Frontmatter**: set `verdict`, `items_passed`, `items_failed`, `items_blocked`,
  `items_not_attempted`, and `status: complete`. Assert in the `notes:` block that the four counts
  sum to 4, showing the arithmetic. Any shortfall from 4 PASSes is stated explicitly — never
  quietly rounded to make the arithmetic look clean.
- **Known-holes resolution**, row by row, by (id, creating plan) pair. Per Ledger Rule 1: never by a
  passing suite, never by inference from a neighbouring row. Each resolution names the exact
  observation, or states why the row stays open. `U-34.5-31` (the origin banner) and `U-34.5-05`
  (the retargeted transcription) are both scored here — and they are DIFFERENT observations, so
  score them independently. Note explicitly that a row can retire on its own clause even if its
  parent item FAILS, and can stay open even if its parent item PASSES; run 4 produced both
  directions in a single run.
- **What this gate falsifies**: carry the four standing claims forward. Apply the rule unchanged —
  **nothing is struck without the corresponding ITEM passing.** Research Pitfall 2 is struck ONLY by
  an item-4 PASS driven against a genuine OFF→ON click; and state whether the target used was
  storefront-authenticated (retiring both readings) or `sideload` (retiring only the narrow literal
  reading).
- **Cross-cutting observations**: record each result, including the uninstall observation as either
  a gathered result, a non-reproduction, or an explicit refusal naming its blocker.
- **Contract-defect tally (T-34.4.2-42)**: record the count of structural impossibilities and
  unresolved requirement interactions actually encountered during the run, whatever it is —
  including zero. Name each struck clause. This is the number that tells a future author whether
  the seven-test review is holding.
- **New findings**: record without diagnosing, continuing the `F-34.5-G6-` sequence from the next
  free id and stating which id a future plan takes next.

Before committing, run the contract's own mandatory pre-commit redaction sweep over the staged diff
and paste its output into the run's record. Any match that is not the accepted published-SteamID64
exception blocks the commit until redacted in place.
  </action>
  <verify>
    <automated>grep -cE "^verdict: (PASS|FAIL)" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md</automated>
  </verify>
  <acceptance_criteria>
    - Every RESULT slot is filled; no slot remains empty.
    - Frontmatter `verdict` is `PASS` or `FAIL`; all four count fields are integers; `status:
      complete`.
    - `items_passed + items_failed + items_blocked + items_not_attempted == 4`, with the arithmetic
      shown in the `notes:` block.
    - The known-holes resolution table has one row per OPEN row the contract named, with no row left
      unaddressed.
    - Every clause's evidence records the exact command AND its raw output, including empty outputs.
    - Item 4's evidence records the ON-click timestamp used for scoping and the discriminating
      tokens.
    - The contract-defect tally is present with an integer, even if zero.
    - The pre-commit redaction sweep output is pasted in, and no unredacted credential value appears
      anywhere in the staged diff.
    - `git diff --stat` does NOT list `34.5-LIVE-GATE-RERUN-3.md`, `-RERUN-2.md`, `-RERUN.md` or
      `34.5-LIVE-GATE.md`.
  </acceptance_criteria>
  <done>The contract is a complete, arithmetically reconciled record whose every verdict is backed by archived evidence from a component other than the one under test.</done>
</task>

<task type="auto">
  <name>Task 3: Record this run's findings in deferred-items.md, undiagnosed</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md</files>
  <read_first>
    - .planning/phases/.../deferred-items.md (the numbering — items 29/30/31 were added by plan
      34.5-54, so this task continues from 32)
    - .planning/phases/.../34.5-LIVE-GATE-RERUN-4.md (this run's own "New findings" section)
  </read_first>
  <action>
Append a dated section headed
`## Found during the fifth blocking live gate (2026-08-13, plan 34.5-59)` to `deferred-items.md`,
continuing the item numbering from 32. State the verdict and the four counts at the top, and link
the evidence file.

One numbered item per new finding, each carrying its `F-34.5-G6-` id, what was observed, and
explicitly what was NOT determined. **Record, do not diagnose** — diagnosis belongs to the SUMMARY
and to gap cycle 8 if one is needed. If the run produced zero new findings, say so explicitly in one
item rather than omitting the section; a missing section reads as "not looked at".

Also record, as its own item, the contract-defect tally from Task 2 — including a zero. That number
is the reviewable clause the authoring reference holds this project to, and it must surface as a
number every run, not only when it is non-zero.

Do not edit `34.5-UNTESTED-ITEMS.md`, `STATE.md`, `ROADMAP.md` or `34.5-PORTED-CHANNELS.md` from
this plan — plan 34.5-60 owns the propagation sweep, and splitting it across two plans is how a
reconciliation goes half-done.
  </action>
  <verify>
    <automated>grep -c "fifth blocking live gate" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - A new dated section exists, numbered continuing from 32, stating the verdict and the four
      counts.
    - Every new finding has a unique `F-34.5-G6-` id, and the section states which id is next free.
    - The contract-defect tally appears as its own numbered item with an integer.
    - Items 1-31 of `deferred-items.md` are byte-unchanged.
    - `git diff --stat` from this plan lists exactly two files.
  </acceptance_criteria>
  <done>This run's observations are on the record with ids allocated and nothing diagnosed prematurely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live OAuth flows -> archived logs | Real codes, verifiers, cookies and account ids pass through the files this run archives and greps |
| archived logs -> the committed contract | Quoted excerpts cross into a public fork's git history permanently |
| operator report -> recorded verdict | A human's account of what happened is not the same as what happened |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C7-31 | Information disclosure | quoted log excerpts in the filled contract | mitigate | Seven redaction value classes including `code_verifier`; in-place redaction rather than line omission; the mandatory pre-commit sweep over the staged diff, whose output is pasted into the record; the published-SteamID64 exception applied explicitly so it is not re-raised. This project has committed a real account id once and live PKCE material once. |
| T-34.5-C7-32 | Repudiation | an item scored on the operator's word | mitigate | Every verdict independently verified against archived disk and log evidence by the executor. Run 4's item 4 is the measured precedent for why: "done" was reported for an action that never occurred. |
| T-34.5-C7-33 | Tampering | prior gate records | mitigate | Acceptance criteria assert `git diff --stat` lists no prior gate document. |
| T-34.5-C7-34 | Spoofing | a partially-captured run reading as fully measured | mitigate | Exactly-ONE-instance assertion before each launch with the PID recorded twice; per-launch `gamelib.log` archiving before the `.old` rename; a closing inventory with `wc -l` so a zero-length capture is visible during the run, not discovered after the app is closed. |
| T-34.5-C7-35 | Elevation of privilege | the item-4 OFF click against a real Wine prefix | accept | Reaching a genuine OFF state runs real `reg delete` and `wineboot -u` commands against the operator's own single-game test prefix. Accepted and recorded, per plan 34.5-56's own register; it is the only route to a genuine transition. |
| T-34.5-C7-SC | Tampering | npm/pip/cargo installs | mitigate | This run installs no package. Any package the gate needs would trigger the Package Legitimacy Gate and a blocking human checkpoint before install. |
</threat_model>

<verification>
- `halt_gate: false` confirmed before launch.
- Every precondition ticked; the exactly-one-instance assertion recorded twice per launch.
- Every RESULT slot filled from archived evidence with commands and raw outputs.
- Arithmetic reconciles to 4, shown.
- Known-holes resolved row by row on each row's own bar.
- Contract-defect tally recorded as an integer.
- Redaction sweep output pasted; no unredacted credential in the staged diff.
</verification>

<success_criteria>
- The gate is executed exactly as authored, with any unperformable clause STRUCK and recorded rather
  than reinterpreted.
- A clean 4/0/0/0 closes Phase 34.5 at plan 34.5-60. Anything else routes to gap cycle 8 — D-08
  admits no advisory mode, no partial pass and no pre-authorised override.
- Nothing is softened, and no item's sub-clause success is allowed to strike a standing claim its
  parent item did not pass.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-59-SUMMARY.md` when done.
</output>
