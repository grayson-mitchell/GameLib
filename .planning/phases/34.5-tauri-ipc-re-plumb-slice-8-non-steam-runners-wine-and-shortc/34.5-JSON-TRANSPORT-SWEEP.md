# JSON-transport `undefined`→`null` hazard — sweep (fix/34.5-33 follow-up)

Written after fixing the live-proven `steamFlowRegistration.ts` `refreshLibrary` regression
(2026-08-01 `pnpm tauri:dev` session, `gamelib.log` lines 14-16: `refreshLibrary failed
runner=null: unknown runner`). Not a new plan — a scoped follow-up fix. This document records the
required sweep for the same defect class across every sidecar registration module, per this
follow-up's own scope item 3: a gotcha documented at one call site with no sibling sweep has
already recurred four times in this project (`publicdir-getapppath-chunking`); this one does not
get a fifth.

## The defect class

Under Electron, `ipcRenderer.invoke(channel, ...args)` uses the structured-clone algorithm, which
preserves `undefined` exactly. Under the Tauri sidecar (`preload/tauriTransport.ts` →
`requestRustInvoke` → the Rust shell → the sidecar's stdin RPC frame), request arguments are
JSON-encoded. `JSON.stringify([undefined])` produces `"[null]"` — an explicitly-passed `undefined`
argument (an array ELEMENT that IS `undefined`, as opposed to an OMITTED argument, which simply
shortens the args array and never reaches this hazard at all) arrives at the sidecar handler as
`null`, never `undefined`.

**The defect fires when a handler compares a JSON-transport-delivered argument against `undefined`
with a strict `!==`/`===` operator whose FALSE branch is presumed unreachable for the "no value
supplied" case, and that false branch's behaviour differs from the true branch's** — e.g. gating
entry into a "the caller supplied a specific value" branch that throws on an unrecognized value,
when "no value supplied" was meant to route elsewhere. Optional chaining (`?.`), truthy/falsy
checks (`if (x)`), and default parameters combined with falsy checks are NOT vulnerable to this
class, because `null` and `undefined` are both falsy and both short-circuit `?.` identically —
only a *strict* equality/inequality check against `undefined` specifically (not `null`, not a
truthy check) creates the hazard.

## Method

Swept every `src/backend/sidecar/*FlowRegistration.ts` module plus `storeRegistration.ts` for
`!== undefined`, `=== undefined`, and `typeof x === 'undefined'` applied to a value derived from a
handler's `...args: unknown[]` (or a named RPC parameter). `hasOwnProperty`/`typeof x === 'string'`
type-guard patterns were also checked, since a defect could hide behind a widened type guard too.

```
grep -n "undefined" src/backend/sidecar/*FlowRegistration.ts src/backend/sidecar/storeRegistration.ts
grep -n "hasOwnProperty\|typeof .* ===\|typeof .* !==" src/backend/sidecar/*FlowRegistration.ts src/backend/sidecar/storeRegistration.ts
```

## Findings

| # | Site | Pattern | Null-safe? | Reason |
|---|------|---------|------------|--------|
| 1 | `steamFlowRegistration.ts:117` (`handleRefreshLibrary`) | `rawRunner !== undefined && rawRunner !== 'all'` | **FIXED** (this follow-up) | The exact live-proven defect. `null` used to satisfy the gate, land in the single-runner branch, fail the `hasOwnProperty` check, and throw `unknown runner 'null'`. Gate now also excludes `null` explicitly; regression test added (`skeletonFlows.test.ts`, dispatches a literal `[null]` args array — the real transport shape). |
| 2 | `appShellFlowRegistration.ts:249,250,269,278` | `powerId !== undefined` / `displaySleepId !== undefined` | Safe | These compare **module-scope local state** (`let powerId: number \| undefined`), never an RPC-delivered argument. No JSON boundary crosses these values — they are set by this module's own `powerSaveBlocker` calls, not by a renderer payload. |
| 3 | `enrichmentFlowRegistration.ts:151` | `const forceRefresh = args[3] as boolean \| undefined` → `getWikiGameInfo(game, forceRefresh)` | Safe | `getWikiGameInfo(game, forceRefresh = false)` (`wiki_game_info.ts:22`) only ever tests `forceRefresh` with `if (!forceRefresh …)` — a truthy/falsy check. `null` and `undefined` are both falsy; behaviour is identical either way. |
| 4 | `oauthLoginFlowRegistration.ts:69` | `const params = args[0] as {...} \| undefined` → `params?.runner`, `params?.url` | Safe | Every read goes through optional chaining (`params?.runner`). `null?.runner` and `undefined?.runner` both evaluate to `undefined` — `isKnownRunner(undefined)`/`isHttpsUrl(undefined)` both correctly return `false` either way. |
| 5 | `runnerAuthFlowRegistration.ts:196` (`authAmazon`) | `const data = args[0] as Partial<NileRegisterData> \| undefined \| null` then `typeof data === 'object' && data !== null && …` | **Already correct** | This site was already written with `null` in its type union and an explicit `data !== null` guard before this sweep — the only other place in the sidecar that had already independently reasoned about this exact transport hazard for an object-shaped argument. No change needed; cited here as a positive example, not a gap. |
| 6 | `shortcutsFlowRegistration.ts:211` (`addShortcut`) | `const fromMenu = args[2] as boolean \| undefined` → `addShortcuts(fromMenu)` | Safe | Every store manager's `addShortcuts(fromMenu?: boolean)` forwards to `shortcuts.ts`'s `addShortcuts()`, which only ever tests `fromMenu` with `if (addDesktopShortcuts \|\| fromMenu)` / `if (addStartMenuShortcuts \|\| fromMenu)` — truthy/falsy checks, identical for `null` and `undefined`. |
| 7 | `wineToolsFlowRegistration.ts:144` (`refreshWineVersionInfo`) | `const fetch = args[0] as boolean \| undefined` → `updateWineVersionInfos(fetch)` | Safe | `updateWineVersionInfos(fetch = false, …)` (`wine/manager/utils.ts:146`) only tests `if (fetch) {…}` — truthy/falsy, identical for `null`/`undefined`. |
| 8 | `settingsFlowRegistration.ts:239` (`getSystemInfo`) | `getSystemInfo(cache as boolean \| undefined)` | Safe (currently — latent default-param caveat noted) | `getSystemInfo(cache = true)` (`utils/systeminfo/index.ts:75`) uses a default parameter (`cache = true`) that ONLY applies when the argument is exactly `undefined`, not `null` — `getSystemInfo(null)` would set `cache = null` (falsy), skipping the cache read instead of defaulting to `true`. This IS a latent behavioural difference. It is currently unreachable: every renderer call site either omits the argument entirely (`useAwaited(window.api.systemInfo.get)`, invoked with zero args → the args array is SHORTER, not `[undefined]` → arrives as real JS `undefined` at the handler, no JSON boundary involved) or passes an explicit literal boolean (`window.api.systemInfo.get(false)`, `Settings/sections/SystemInfo/index.tsx:93`). No call site passes a variable that could itself be `undefined`. Flagged here so a future caller who does introduce such a call knows the default-parameter behaviour silently degrades (always-refetch) rather than throwing — lower severity than finding #1, but recorded per this sweep's own instruction to name every hit. |
| 9 | `humbleLoginFlowRegistration.ts:256` (`revealPost`'s `csrfToken`) | `input.csrfToken ?? null` sent across `requestRustInvoke`, documented explicitly: "`csrfToken` crosses the wire as `null` (never `undefined` …) — the Rust arm's own `reveal_post_args` maps a missing arg AND an explicit `null` to the same `None`" | **Already correct** | This is the sidecar-to-Rust direction of the SAME hazard (not renderer-to-sidecar), and it was already fixed and explicitly documented before this sweep — the second existing example of someone in this codebase independently re-deriving this exact transport rule. Cited as a positive precedent. |
| 10 | `steamFlowRegistration.ts:123` (`handleRefreshLibrary`'s `hasOwnProperty` check, downstream of #1) | `Object.prototype.hasOwnProperty.call(libraryManagerMap, rawRunner)` | Safe (post-fix) | Once #1's gate excludes both `undefined` and `null`, this line only ever receives a genuine runner-name string. Before the fix, `null` reached this line and correctly returned `false` (no crash) — the observed failure was the *subsequent* `throw`, not this check itself. |
| 11 | Every other `*FlowRegistration.ts` module (`clipboardFlowRegistration.ts`, `dialogFlowRegistration.ts`, `downloadQueueFlowRegistration.ts`, `gameDetailsFlowRegistration.ts`, `humbleFlowRegistration.ts`, `installFlowRegistration.ts`, `loggerFlowRegistration.ts`, `steamAuthFlowRegistration.ts`, `storeRegistration.ts`) | — | Safe (no hit) | Grepped for the same patterns; no `!== undefined`/`=== undefined`/`typeof x === 'undefined'` comparison against an RPC-delivered argument was found in any of these modules. `typeof x === 'string'` guards present in several of them (`humbleLoginFlowRegistration.ts`, `runnerAuthFlowRegistration.ts`, `settingsFlowRegistration.ts`, `steamAuthFlowRegistration.ts`) correctly reject `null` as a non-string, the same as they reject any other wrong type — these are type-validation guards, not optional-argument presence gates, and are unaffected by this hazard class. |

## Verdict

**One defect found and fixed** (#1). Two sites in the codebase (`runnerAuthFlowRegistration.ts`
`authAmazon`, `humbleLoginFlowRegistration.ts` `revealPost`) had already independently
identified and correctly handled this exact hazard before this sweep — cited above as positive
precedent, proof the pattern is knowable in advance, not just in hindsight. One latent,
currently-unreachable caveat noted (#8) for the record, not fixed, since no live caller exercises
it and "fix everything that could theoretically matter" is out of this follow-up's scope (the
task's own scope item 3 asks for a sweep and a fix of same-defect hits, not a preemptive rewrite
of every optional-argument site in the sidecar).

## Ledger update

See `34.5-UNTESTED-ITEMS.md` rows `U-34.5-07`/`U-34.5-08` — this regression and its fix are new
live evidence bearing on those rows. Per the ledger's own rules (Rule 1: a row is retired ONLY by
the exact observation named in its own row), neither row is retired by this fix — a live
`pnpm tauri:dev` session showing a clean `[refreshLibrary] runner=all origin=mount` with no
`unknown runner` line following it is still required.
