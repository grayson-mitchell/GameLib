---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 39
type: execute
wave: 2
depends_on: [34.5-38]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
autonomous: true
gap_closure: true
cycle: 5
requirements: [REQ-34.5-01, REQ-34.5-03, REQ-34.5-05, REQ-34.5-12]

must_haves:
  truths:
    - "Gate items 4 and 5 each have a target verified TODAY, exact invocations, named evidence lines that provably exist in source, and their prerequisites checked"
    - "Neither item can be skipped a third time for want of a working login — each has a stated dependency analysis and, where one exists, a login-independent fallback target"
    - "Every log line the preflight names was read from the file that emits it, at planning time"
    - "Nothing in this plan claims a live behaviour on the strength of a passing test"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md"
      provides: "verified preflight for the two never-attempted gate items"
      contains: "shortcuts.vdf"
  key_links:
    - from: "34.5-G6-ITEM45-PREFLIGHT.md"
      to: "34.5-LIVE-GATE-RERUN-2.md"
      via: "plan 34.5-40 inlines this document's verified values into items 4 and 5"
      pattern: "ITEM45-PREFLIGHT"
---

<objective>
Produce `34.5-G6-ITEM45-PREFLIGHT.md`: the verified target, exact invocations, named evidence lines
and checked prerequisites for the two gate items that have NEVER been attempted — item 4
(`addToSteam` plus the macOS `.app` shortcut, BOTH `exe` call sites) and item 5 (`runWineCommand` for
a non-Steam runner).

**This is plan 34.5-29's Tasks 2 and 3, re-homed.** Cycle 4 deliberately withheld them so a preflight
written days early would not be stale by the time the gate finally ran — `34.5-CYCLE4-ROUTING.md`
records that reasoning verbatim, and names it as the precise reason items 4 and 5 have already been
skipped twice. Plan 34.5-29 itself is NOT edited, NOT renumbered and NOT executed; it stays on disk
as the record of its own halt. Its Task 1 (the Epic user-agent fix) is VOID and is not carried here.

Purpose: remove the "we did not get to it" failure mode by verifying everything NOW, one wave before
the contract that inlines it and two before the run that consumes it.

Output: `34.5-G6-ITEM45-PREFLIGHT.md`, plus any new `34.5-UNTESTED-ITEMS.md` row this plan's own
findings create (ledger Rule 2 — same commit as the hole).

**Scope fence.** This plan ships no code. It reads source and the local filesystem, and writes two
planning documents. `git diff --name-only` must match zero `.ts`, `.tsx` or `.rs` paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.claude/skills/spike-findings-gamelib/SKILL.md

**Run `graphify explain "runWineCommand"`, `graphify query "addToSteam nonesteamgame shortcuts vdf exe"`
and `graphify query "toggleDXVK wineToolsFlowRegistration tools index platform guards"` BEFORE opening
raw source** (project rule, `CLAUDE.md`).

<interfaces>
Verified at planning time by direct source read — re-verify each before relying on it, and record
the re-verification:

- `src/backend/shortcuts/nonesteamgame/nonesteamgame.ts` ~line 258:
  `newEntry.Exe = "${app.getPath('exe')}"` — the Steam shortcut VDF `Exe` field. The value written
  is derived ONLY from `getPath('exe')`; the surrounding branches switch on `isFlatpak`,
  `isAppImage`, `isWindows`+`PORTABLE_EXECUTABLE_FILE`. **No branch is keyed on the runner.**
- `src/backend/shortcuts/shortcuts/shortcuts.ts` ~line 227: the `run.sh` launch command is
  `${app.getPath('exe')} --no-gui gamelib://launch?appName=${app_name}&runner=${runner}` — the
  runner appears only inside the deep-link query string; **the executable half is runner-independent.**
- `src/backend/sidecar/pathShim.ts` — the `exe` case throws loudly when `GAMELIB_SHELL_EXE` is unset;
  it never returns an empty string.
- `src/backend/sidecar/wineToolsFlowRegistration.ts` — all 9 Wine channels are registered with
  `ipcMain.handle` (invoke-kind): `runWineCommand` (:87), `getAlternativeWine` (:92),
  `wine.isValidVersion` (:96), `installWineVersion` (:107), `refreshWineVersionInfo` (:143),
  `removeWineVersion` (:154), `toggleDXVK` (:186), `toggleDXVKNVAPI` (:195), `toggleVKD3D` (:209).
  **None of the 9 is send-kind** — so the silent-failure hazard that applies to `addShortcut` does
  NOT apply to item 5's dispatch. Confirm this before writing it down.
- `src/backend/tools/index.ts` platform guards: `:73` (`tool.os !== process.platform`), `:228`
  (macOS permits `dxvk` only — **VKD3D already excluded**, so a "toggle VKD3D" instruction would be a
  guaranteed non-event), `:245` (`dxvk-macOS`), `:219` (`-DXMT` builds), `:642`
  (`isMac ? macEnvs : linuxEnvs`).
- `src/backend/launcher.ts` `runWineCommand()` at ~line 1504 — the item-5 seam. Its Steam path is
  proven transitively via already-ported `install`/`launch`; no NON-Steam runner has ever reached it
  under the sidecar (research Pitfall 2, STANDING).
- `src/common/types.ts:195` — the runner union is
  `'legendary' | 'gog' | 'sideload' | 'nile' | 'zoom' | 'steam'`, and
  `src/backend/storeManagers/sideload/` exists. `sideload` is therefore a real, non-Steam runner
  that requires no storefront login at all — a candidate that must be EVALUATED against each item's
  own wording, never silently substituted.

Item 4's unconsumed groundwork, carried from `34.5-LIVE-GATE-RERUN.md` item 4 rather than
rediscovered — **all of it is dated 2026-07-29 and is presumed stale until re-verified**:
- Target: GOG *Alan Wake*, `appName` `1207659037`, installed at `~/GameLib/Alan Wake`.
- Both channels are reachable from the devtools console: `window.api.addShortcut(appName, runner, false)`
  (send, fire-and-forget) and `await window.api.addToSteam(appName, runner)` (invoke, returns boolean).
- **Operator warning: quit Steam FIRST** — Steam holds `shortcuts.vdf` in memory and rewrites it
  from memory on exit, clobbering any write made while it is running.
- `shortcuts.vdf` did NOT exist when the groundwork was recorded; its first appearance is itself part
  of item 4(a)'s evidence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify item 4's target and write its exact invocation steps — including a login-independent path</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` § Item 4 (the unconsumed groundwork verbatim, including the quit-Steam warning, the two expected writes, and the recorded reason it was skipped)
    - `.planning/phases/34.5-.../34.5-29-PLAN.md` Task 2 (the re-homed instruction — read to execute, never to edit)
    - `src/backend/shortcuts/nonesteamgame/nonesteamgame.ts` around line 258 — read the whole enclosing function, not the single line, to confirm which branches can change the written `Exe` value
    - `src/backend/shortcuts/shortcuts/shortcuts.ts` around line 227 — `generateMacOsApp`'s `run.sh` body and the `.app` directory layout it writes
    - `src/backend/sidecar/pathShim.ts` (the `exe` case and its loud-throw contract)
    - `src/backend/sidecar/shortcutsFlowRegistration.ts` (which of the 7 shortcuts channels are invoke-kind vs send-kind — `addShortcut` is send-kind and fails SILENTLY under the sidecar)
  </read_first>
  <action>
    Create `34.5-G6-ITEM45-PREFLIGHT.md` with frontmatter (`phase`, `plan: 39`, `type: preflight`,
    `cycle: 5`, `created`, `consumed_by: 34.5-40-PLAN.md`) and a `## Item 4 — both exe call sites`
    section containing, in this order:

    1. **Target verification, run NOW, output quoted verbatim.** Is GOG *Alan Wake* (`appName`
       `1207659037`) still installed? Check BOTH
       `~/Library/Application Support/GameLib/gog_store/installed.json` AND the on-disk directory
       `~/GameLib/Alan Wake`. If it is not installed, pick a different installed game, and record its
       real `appName` and `runner` read out of the corresponding store's `installed.json`. Do not
       carry a stale appName forward — that is precisely what made this item skippable twice.
    2. **The login-dependency analysis — the thing that made this item skippable twice.** Gate
       precondition 5 requires being signed OUT of Epic, GOG and Amazon, and item 4's historical
       target depends on a GOG library that a fresh login must repopulate. Establish from source, and
       record with citations, whether the VALUE written at either `exe` call site depends on the
       runner at all (see this plan's `<interfaces>`: `nonesteamgame.ts`'s `Exe` is derived from
       `getPath('exe')` with branches keyed on packaging format, not runner; `shortcuts.ts`'s
       `run.sh` carries the runner only inside the `gamelib://launch?...&runner=` query string).
       Then state, in one explicit sentence, whether item 4 is attemptable WITHOUT a successful
       in-session GOG login, and name a concrete fallback target if it is — a `sideload`-runner game
       or any game whose install metadata survives sign-out. If the answer is that item 4 genuinely
       requires a fresh login, say THAT plainly instead, and name what would unblock it. Do not
       invent an answer: read the source and record what it says.
    3. **The exact Steam userdata path**, resolved on this machine rather than transcribed: list
       `~/Library/Application Support/Steam/userdata/` and record which account-id directory exists.
       Record the full `shortcuts.vdf` path. **REDACT the account id in the committed document**
       (`userdata/<REDACTED>/config/shortcuts.vdf`) — it is a Steam account identifier and this is a
       public fork.
    4. **Whether `shortcuts.vdf` currently exists**, with its size and mtime if so, or its absence
       stated. Its first appearance is part of item 4(a)'s evidence, so its prior state must be on
       record.
    5. **The two exact devtools-console invocations** with the verified appName and runner
       substituted in — `window.api.addShortcut(...)` and `await window.api.addToSteam(...)`. Note
       that `addShortcut` is send-kind and therefore reports nothing on failure, so its evidence must
       come from the filesystem, never from the absence of an error.
    6. **The quit-Steam warning as a numbered step BEFORE the invocations**, with its reason stated.
    7. **What to read back per call site, and what a PASS looks like**: the VDF `Exe` field for
       `addToSteam`; the `run.sh` body for the desktop shortcut; and in both cases a `ps aux` line
       after clicking, showing the launched process is the Tauri shell binary and NEVER a bare `node`
       process. State explicitly that proving ONE call site is not sufficient — they are two
       independent writers of the same `GAMELIB_SHELL_EXE`-derived value and either can regress alone
       (research Correction 3).
    8. **The dev-versus-packaged limit**, carried verbatim so a dev PASS is never later read as
       covering packaging: `current_exe()` resolves to `src-tauri/target/debug/gamelib-shell` in dev;
       a packaged build resolves through `spawn_sidecar_packaged` to a path inside the `.app` bundle.
       Residual `R-34.5-G1-PKG` stays OPEN regardless of this item's outcome.

    Every filesystem claim in this section carries the command that produced it and that command's
    verbatim output. A claim without its command is not evidence.
  </action>
  <verify>
    <automated>grep -q "shortcuts.vdf" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md &amp;&amp; grep -q "R-34.5-G1-PKG" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md &amp;&amp; grep -q "REDACTED" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - A VERIFIED installed game is recorded with its real `appName` and `runner`, read from `installed.json` AND cross-checked against the on-disk directory, with both commands and their verbatim output quoted.
    - The login-dependency analysis is present, cites both `exe` call sites by file and line, and ends in one explicit sentence stating whether item 4 is attemptable without a fresh GOG login — with a named fallback target if it is.
    - The Steam `userdata` account-id directory was resolved on this machine and the committed document shows it REDACTED; `grep -c "userdata/[0-9]" ` on the document returns 0.
    - `shortcuts.vdf`'s current existence, size and mtime are recorded, or its absence is stated.
    - Both devtools invocations appear with the verified appName substituted, not a placeholder, and `addShortcut` is flagged send-kind with its silent-failure consequence.
    - The quit-Steam step precedes the invocations and carries its reason.
    - Read-back and PASS criteria are stated per call site, with the explicit rule that proving one is not sufficient.
    - The dev-versus-packaged limit and `R-34.5-G1-PKG` staying OPEN are both recorded.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Name a concrete, reachable item-5 Wine action against a non-Steam runner</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` § Item 5 (the standing Pitfall 2 claim and why a FAIL-by-blockage retires nothing)
    - `.planning/phases/34.5-.../34.5-29-PLAN.md` Task 3 (the re-homed instruction — read to execute, never to edit)
    - `.planning/phases/34.5-.../34.5-RESEARCH.md` § "Pitfall 2" (the precise claim: the plumbing is real and Steam-proven; the untested combination is Wine execution for a NON-Steam game under the sidecar)
    - `src/backend/launcher.ts` around line 1504 (`runWineCommand` — its signature, its early-return paths, and its callers)
    - `src/backend/tools/index.ts` lines 60-90, 210-260 and 630-650 (the platform guards, the macOS `dxvk`-only rule with VKD3D already excluded, `dxvk-macOS`, `-DXMT`, and `isMac ? macEnvs : linuxEnvs`)
    - `src/backend/sidecar/wineToolsFlowRegistration.ts` (all 9 registrations and their kind)
    - `src/frontend/screens/Settings/` — the Wine/compatibility settings panel that surfaces the chosen action, to record its exact UI path
  </read_first>
  <action>
    Append a `## Item 5 — runWineCommand for a non-Steam runner` section to
    `34.5-G6-ITEM45-PREFLIGHT.md`. Item 5 has never been attempted, both times because no
    authenticated non-Steam runner existed to drive it against. Make the action unambiguous.

    Establish from source, and record with file-and-line citations:

    1. **Which Wine action is actually reachable on macOS** for a non-Steam game. `tools/index.ts`
       permits `dxvk` on macOS and already excludes VKD3D, so a "toggle VKD3D" instruction would be a
       guaranteed non-event proving nothing. Name ONE specific permitted action and the exact UI path
       to reach it — which settings panel, which control, for which game.
    2. **Which channel that action dispatches**, by name, and its registration kind read from
       `wineToolsFlowRegistration.ts`. If it is invoke-kind, say so and say why that matters (a
       send-kind channel fails 100% silently under the sidecar — no reject, no timeout, no log line);
       if the chosen action turns out to ride a send channel after all, name the separate observable
       that proves it ran, because its own absence of error proves nothing.
    3. **The exact `gamelib.log` lines that constitute evidence** of dispatch and of successful
       completion — quote the literal log-line shapes from the source that emits them, read at
       planning time, and record that source file's path beside each. Note that
       `src/backend/tools/index.ts` logs under `LogPrefix.ToolInstaller` (e.g. the
       `installing <tool> on...`, `<tool> already installed!`, `Removing DXVK DLLs`,
       `Restoring Wine stock DLLs` lines) — pick the ones your chosen action actually reaches, and do
       not cite a line the action does not produce. This is the same gate-contract defect run 1 hit
       at its own precondition 5: it asked for a value nothing ever wrote to `gamelib.log`.
    4. **The observable in-app effect** — what the user should SEE change, stated separately from the
       log evidence. A log line alone is the dispatcher's own report, and per this cycle's binding
       rule a mutating call's own report is never proof of its effect.
    5. **Every prerequisite, each with the command that verifies it**: an installed non-Steam game, a
       downloaded Wine/CrossOver version, a prepared prefix, and anything else the chosen action
       needs — so the gate does not stall discovering a missing dependency mid-run.
    6. **The runner-eligibility question, answered explicitly and NOT resolved by substitution.**
       `sideload` is a real non-Steam runner (`src/common/types.ts:195`,
       `src/backend/storeManagers/sideload/`) that needs no storefront login, and would therefore be
       drivable even if items 1-3 fail. Record whether a `sideload` target satisfies item 5's own
       wording ("`runWineCommand` succeeds for at least one NON-Steam runner") and REQ-34.5-03's
       framing of the gap. If it does, say so and name it as the login-independent path. If it does
       NOT — for instance if Pitfall 2's claim is specifically about the storefront runners whose
       auth was the blocker — say THAT, and flag to plan 34.5-40 that the contract's item-5 wording
       needs sharpening. **Do not silently substitute a weaker target for a stronger one**; state the
       trade and let the contract decide.

    Close the section by stating plainly that Pitfall 2's standing claim ("no non-Steam runner has
    ever executed a Wine command under the sidecar") is retired ONLY by an item-5 PASS — not by this
    preflight, and not by a green test suite.

    **Ledger obligation (Rule 2).** If this task's own findings create a new untested hole — for
    example, if the eligible item-5 target turns out to be `sideload` only, leaving GOG/Epic/Amazon
    Wine execution still unproven — add the corresponding row to `34.5-UNTESTED-ITEMS.md` **in the
    same commit**, with its own "Retired by" observation. Do not defer it to plan 34.5-42.
  </action>
  <verify>
    <automated>grep -q "## Item 5" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md &amp;&amp; grep -q "Pitfall 2" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one macOS-permitted Wine action is named, with its UI path and the `tools/index.ts` citation proving it is not excluded by the platform guards.
    - The dispatching channel is named with its registration kind, read from `wineToolsFlowRegistration.ts` and cited by line.
    - Every evidence log line is quoted as a literal shape with the emitting source file's path recorded beside it; the SUMMARY lists each file the shapes were read from.
    - The observable in-app effect is stated separately from the log evidence.
    - Every prerequisite is listed with a verifying command.
    - The runner-eligibility question is answered explicitly, with a stated trade rather than a silent substitution, and any needed contract-wording change is flagged to plan 34.5-40 by name.
    - The document states Pitfall 2 is retired only by an item-5 PASS.
    - If a new untested hole was created, a matching `34.5-UNTESTED-ITEMS.md` row exists in the same commit; if none was created, the SUMMARY says so explicitly.
    - `npx tsc --noEmit` exits 0 and `git diff --name-only` matches zero `.ts`, `.tsx` or `.rs` paths.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `getPath('exe')` → Steam VDF / `run.sh` | A shell-derived path becomes an executed launch command on the developer's machine |
| machine-local identifiers → repository document | The Steam `userdata` account id is a real account identifier in a public fork |
| a Wine action → a real Wine prefix | The chosen item-5 action mutates a real prefix on the developer's machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C5-05 | Elevation of privilege | a wrong `exe` value written into a Steam VDF launch command | mitigate | Task 1 requires reading the written `Exe` field and the `run.sh` body back, plus a `ps aux` proving the launched process is the shell binary and never bare `node`; `pathShim`'s `exe` case throws loudly when `GAMELIB_SHELL_EXE` is unset rather than writing an empty string |
| T-34.5-C5-06 | Information disclosure | Steam `userdata` account id in a public-fork document | mitigate | Task 1 resolves the id on-machine but commits it REDACTED, and its acceptance criteria include a grep proving no raw numeric userdata path survives |
| T-34.5-C5-07 | Repudiation | a send-kind channel's silence read as success | mitigate | Task 1 flags `addShortcut` as send-kind with filesystem-only evidence; Task 2 requires the item-5 channel's kind to be named and, if send-kind, a separate observable named |
| T-34.5-C5-08 | Repudiation | a weaker item-5 target silently substituted for the one the requirement names | mitigate | Task 2 must state the runner-eligibility trade explicitly and flag any contract-wording change to plan 34.5-40; substitution without that record is a criterion failure |
| T-34.5-C5-09 | Tampering | citing a `gamelib.log` line that does not exist | mitigate | Every evidence line must be quoted from the emitting source file at planning time with that file's path recorded — the exact defect run 1's precondition 5 shipped |
| T-34.5-C5-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages and touches no source file |
</threat_model>

<verification>
- `34.5-G6-ITEM45-PREFLIGHT.md` exists with both item sections, every filesystem claim carrying its command and verbatim output.
- `npx tsc --noEmit` exits 0; `git diff --name-only` matches zero `.ts`, `.tsx`, `.rs` paths.
- `git status --porcelain` shows `34.5-29-PLAN.md`, `34.5-30-PLAN.md` and `34.5-31-PLAN.md` UNMODIFIED.
- Any new untested hole has its `34.5-UNTESTED-ITEMS.md` row in the same commit that created it.
</verification>

<success_criteria>
Gate items 4 and 5 each have a target verified today, exact invocations, evidence lines proven to
exist in the source that emits them, checked prerequisites, and a written answer to the dependency
question that caused both prior skips — so neither item can be skipped for a third time.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-39-SUMMARY.md` when done
</output>
