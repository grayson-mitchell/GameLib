---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 47
task: 3
status: resolved-non-reproduction
---

# Defect B discriminator — Manage Accounts stuck on "logging into gog"

## Step 3a — is the brief's "no navigation" premise stale?

**Finding: YES, stale.** The brief handed to the planner claims the Tauri OAuth path's
`completeOAuthLogin` "only does `setState` + `handleSuccessfulLogin` — no navigation." That is
false as of the live tree: `src/frontend/screens/WebView/index.tsx` already contains
`handleTauriOAuthSuccess`, which calls `completeOAuthLogin(payload)` and then, if
`mountedRef.current` is true, `navigate('/login')`.

**Commit that introduced it:**

```
commit 343a0f6d953d80ab1493a8619713a9d3f17529b4
Author: Grayson Mitchell
Date:   Mon Aug 3 02:08:43 2026 +1200 (committer date, same offset both fixes below use)
fix(34.5): navigate off the login route after a Tauri OAuth login
```

**UAT observation it is compared against** (`.planning/phases/34.5-.../34.5-UAT.md`, the gap
entry starting "After a successful GOG login, the Manage Accounts panel updates and the Library
renders the games"):

```
session frontmatter: started: 2026-08-03T00:19:00Z, updated: 2026-08-03T00:50:00Z
reason quotes raw gamelib.log lines at (00:43:19)..(00:43:54)
```

**Reading the two on the same clock.** Both `343a0f6d9` and `eb117d9e4` (the sibling store fix,
committed 2026-08-03T01:24:43+12:00 — 44 minutes before the navigation fix, confirmed
chronologically first via `git merge-base --is-ancestor eb117d9e4 343a0f6d9`) carry the SAME
`+12:00` committer offset, so the developer's machine clock is internally consistent across this
session. The UAT doc's YAML timestamps use a `Z` suffix, but its own raw log lines
(`00:19:10` boot, `00:43:19` etc.) match the SAME raw wall-clock reading the commits' local time
comes from — i.e. the `Z` is this document's own annotation convention over the raw
`gamelib.log` clock, not a true UTC conversion. Read on that single raw-clock axis, the order is:

| Event | Raw local time (2026-08-03) |
|---|---|
| UAT boot | 00:19:10 |
| UAT gap observed (capture→teardown→idle) | 00:43:19–00:43:54 |
| UAT session `updated` | ~00:50 |
| `eb117d9e4` (store-snapshot fix) committed | 01:24:43 |
| `eb117d9e4` live-verified (per debug file) | 01:39–01:40 |
| `343a0f6d9` (navigation fix, THIS defect) committed | 02:08:43 |

**Conclusion: the UAT observation (00:43–00:50) PREDATES the navigation fix (02:08:43) by over an
hour.** This is a stale observation against since-changed code, not a fix layered on a live
current-tree symptom. Per this plan's Step 3a instruction, the honest disposition is a live
RE-observation on current HEAD — not a second fix authored against an unverified premise.

**`34.5-LIVE-GATE-RERUN-2.md` cross-check:** dated 2026-08-02 (`created`/`completed` frontmatter),
i.e. the day before this UAT session and both fixes. That gate's own GOG item never reached the
render layer (it failed upstream on the `publicDir`/spawn defect, per its own history section), so
it never exercised — and cannot corroborate or contradict — the Manage Accounts navigation
question either way. Named here per the instruction, contributes no additional evidence.

## Step 3b — three-way discriminator

Given Step 3a's finding, the strongest prior is READING 1 became moot (the guard now navigates)
— but per this plan's own standing rule (`f10-diagnosis-process-lesson`), a prior is not a
diagnosis. All three readings stay live candidates until instrumented and observed.

Additional STATIC finding this planning pass turned up, narrowing READING 3 before any live run:
`src/frontend/screens/Login/components/Runner/index.tsx` — the actual Manage Accounts tile
component — has exactly two post-click states, `isLoggingOut` ("Logging out…") and the
logged-in/logged-out toggle. **There is no third "signing in…" tile state anywhere in
`Runner/index.tsx`.** The "Signing in to Gog" text the UAT operator described has only one
possible render source in the current tree: `TauriLoginPanel.tsx`'s `phase === 'awaiting'` branch,
rendered on the `/loginweb/gog` route itself (reached by clicking the GOG tile FROM Manage
Accounts, which is almost certainly why the operator called it "the manage accounts screen").
This makes READING 3 structurally unlikely on the current tree, but Probe 4 below still checks it
empirically rather than asserting it by static reading alone.

| Reading | Mechanism | Log signature (present under it, absent under the other two) |
|---|---|---|
| **READING 1** — "no navigation ran" | `completeOAuthLogin` executed but `mountedRef.current` was `false` when `handleTauriOAuthSuccess` fired, so the `if` branch (and `navigate`) never ran, while the panel is still visibly mounted from the user's POV | Probe 1 logs `mountedRef=false`; probe 2 (inside the `if`) is **absent** for that login; probe 3 (true unmount) is **absent or fires long after** probe 1 |
| **READING 2** — "the consumer unmounted" | The WebView route genuinely tore down before `handleTauriOAuthSuccess` ran (or before probe 4's re-render), so nothing was left to navigate *from* | Probe 3 (true unmount) fires **BEFORE** probe 1, or probe 1 never fires at all for that login attempt |
| **READING 3** — "navigation ran, a different surface is stuck" | `navigate('/login')` fires and the WebView route unmounts correctly, but a separate Manage Accounts tile state (not `TauriLoginPanel`) is what stayed stuck | Probes 1+2 both fire in order (`mountedRef=true`, then probe 2); probe 4 does **NOT** re-fire after navigation (proving `TauriLoginPanel`'s `awaiting` branch is not what is still rendering) AND the operator reports a stuck panel anyway |

Four temporary `[34.5-47-discriminator]` probes were added (marked `// 34.5-47 DISCRIMINATOR —
REMOVE`), each carrying the runner and `Date.now()`:

1. `src/frontend/screens/WebView/index.tsx` — entry to `handleTauriOAuthSuccess`, logs
   `mountedRef.current` BEFORE the branch.
2. Same file, immediately after `navigate('/login')` inside the `if`.
3. Same file, the `mountedRef` cleanup function in the empty-dep `useEffect` (true unmount only).
4. `src/frontend/screens/WebView/components/TauriLoginPanel.tsx` — entry to the `phase ===
   'awaiting'` render branch (located via the i18n key `webview.login.oauth.awaiting.heading`,
   not the English copy).

`pnpm tsc --noEmit` and the full `src/frontend/screens/WebView` jest suite (171/171) both pass
with the probes in place — confirmed this session.

## Step 3c — live run

**RUN, by the developer, under the already-started `pnpm tauri:dev` session** (process hygiene
precondition confirmed clean beforehand: `ps aux | grep -E "gamelib-shell|sidecar.js"` returned
empty before the dev server was started; after start, exactly one `gamelib-shell` and one
`sidecar.js` process). The developer signed out of GOG from Manage Accounts and signed back in
with real credentials.

**Developer's verbatim report:** *"returns to manage accounts on its own"* — the panel does NOT
stay stuck on "Signing in to Gog".

**Probe trace, pulled from `~/Library/Logs/GameLib/gamelib.log`, byte-verified via `cmp` against a
named sidecar copy (F-34.5-G6-15) before being transcribed here, then the sidecar copy discarded
(it is a runtime log, not a plan artifact, and this project's own standing caution about
committing live credential-adjacent material — see MEMORY's "committed live PKCE material" entry
— argues against tracking a raw log dump; the relevant lines are fully reproduced below instead):**

```
(22:16:38) [Frontend]: [34.5-47-discriminator] probe=4-awaiting-render runner=gog t=1786443398651
(22:16:39) [Backend]:  [oauthLoginCapture] runner=gog nav host=login.gog.com
(22:17:05) [Backend]:  [oauthLoginCapture] runner=gog nav host=embed.gog.com
(22:17:05) [Backend]:  [oauthLoginCapture] runner=gog status=captured
(22:17:06) [Frontend]: [useTauriOAuthLogin] runner=gog phase=finalizing (capture complete, exchanging with auth channel)
(22:17:07) [Frontend]: [useTauriOAuthLogin] runner=gog phase=idle (login completed, library refresh triggered)
(22:17:07) [Frontend]: [34.5-47-discriminator] probe=1-handleTauriOAuthSuccess-entry runner=gog mountedRef=true t=1786443427420
(22:17:07) [Frontend]: [refreshLibrary] runner=gog origin=login-success
(22:17:07) [Frontend]: [34.5-47-discriminator] probe=2-navigate-fired runner=gog t=1786443427422
(22:17:07) [Frontend]: [34.5-47-discriminator] probe=3-mountedRef-cleanup runner=gog t=1786443427430
(22:17:07) [Frontend]: [useTauriOAuthLogin] runner=gog phase=teardown inflight=false
(22:17:10) [Gog]:      refreshLibrary complete runner=gog managers=1
```

**What the trace shows:**
- Probe 4 fires exactly once, at 22:16:38 — the panel rendered "Signing in to Gog" while the
  window was genuinely open, then never re-rendered that branch again (no second probe=4 line
  anywhere in the log after this).
- Probe 1 (`mountedRef=true`) fires at 22:17:07.420 — the panel was still mounted when
  `handleTauriOAuthSuccess` ran. This is the READING 1 falsifier: `mountedRef.current` was
  **true**, not false.
- Probe 2 fires 2ms later (22:17:07.422) — `navigate('/login')` ran. This is the READING 1
  falsifier's second half: the branch was not skipped.
- Probe 3 (the TRUE unmount) fires 8ms after probe 1 (22:17:07.430) — **after**, not before,
  probes 1 and 2. This is the READING 2 falsifier: the consumer did not unmount before
  `handleTauriOAuthSuccess` ran; it unmounted 8-10ms later, as the ordinary CONSEQUENCE of the
  router leaving the `/loginweb/gog` route because `navigate('/login')` just fired — not as a
  premature teardown that raced it.
- No further probe=4 line, and no repeated "Signing in to Gog" report from the developer, is the
  READING 3 falsifier: nothing kept re-rendering the `awaiting` branch after navigation, and there
  is no second surface (confirmed structurally in Step 3b: `Runner/index.tsx` has no "signing in"
  state) that could have been stuck instead.

**None of the three broken-behavior signatures (READING 1, 2, or 3) are present.** The trace is
exactly the FIXED shape `handleTauriOAuthSuccess`'s own docstring describes: `mountedRef=true` →
`navigate` fires → the route unmounts as an effect of that navigation, not a cause of a bug.

## Step 3d — fix

**No code fix applied.** Per Step 3a's staleness finding (commit `343a0f6d953d80ab1493a8619713a9d3f17529b4`
predates the UAT observation by over an hour on the same developer-machine clock) and this
session's live non-reproduction, the correct disposition is Step 3d's explicitly pre-registered
branch: *"If the Step 3a date comparison showed the observation is stale AND the live run
reproduces nothing: record 'not reproducible on current HEAD, fix `handleTauriOAuthSuccess`
presumed effective' with the log evidence, change no code, and open the ledger row."*

`handleTauriOAuthSuccess` and `TauriLoginPanel.tsx`'s `awaiting` branch are both restored to
byte-identical pre-probe form (`git diff` against the pre-probe commit shows only unrelated
pre-existing prettier reformatting two lines away from either edit site — confirmed, not a
functional change). No fix is layered on top; there is nothing left to fix as of this reading.

## Observed

READING NONE

The selected reading is READING NONE. The live probe trace matches none of the three hypothesized BROKEN-behaviour
signatures — it matches the FIXED shape instead. Per the plan's own instruction ("if the trace
matches none of the three, record READING NONE... and stop — do not invent a fourth reading post
hoc"), this is recorded as READING NONE rather than force-fit into one of the three broken
readings.

**This is a non-reproduction, not a retirement of the underlying defect claim, and not a
guarantee.** One observation, on one machine, one login, is what exists. Per Ledger Rule 1 (a row
retires only on the observation named in its own cell) and the coordinator's explicit instruction
not to overstate this, the residual risk — that some OTHER machine, timing, or teardown path could
still reproduce the mid-flight-teardown race this file's own docstring warns about — is carried
forward as an open ledger row (`U-34.5-07` update below) rather than closed out. **Gate item 2 is
NOT retired by this discriminator.** Only a gate RUN can retire ledger rows, per this plan's own
inherited Standing Rule 1 and 34.5-UAT.md's own precedent for the sibling Library-render fix.
