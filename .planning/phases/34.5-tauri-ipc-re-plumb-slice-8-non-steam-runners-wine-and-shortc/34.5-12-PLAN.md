---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 12
type: execute
wave: 3
depends_on: ["34.5-01", "34.5-07"]
files_modified:
  - src/backend/sidecar/runnerMiscFlowRegistration.ts
  - src/backend/sidecar/__tests__/runnerMiscFlows.test.ts
autonomous: true
requirements: [REQ-34.5-07, REQ-34.5-08]

must_haves:
  truths:
    - "The three 'other' channels are ported — `callTool`, `egsSync`, `getGOGLinuxInstallersLangs` — and the two saves-sync channels — `syncSaves`, `syncGOGSaves`. All five are `addHandler`/invoke, completing the misc module at 11."
    - "Research answered CONTEXT.md's open gray area: none of the three 'other' channels hides an Electron dependency the stub does not cover — they route only through already-baselined `dialog.ts`/`utils.ts`."
    - "Research Pitfall 4: `callTool`'s `winetricks` branch ALREADY WORKS. `tools/ipc_handler.ts:31-33` calls `Winetricks.run(runner, appName)` on the shared `tools/index.ts` object — pure Node, no Electron — despite the three winetricks IPC channels being deferred to Phase 34.6. FLAG it in the declared-channels doc; do NOT guard it behind a 'wait for 34.6' comment."
    - "Research Pitfall 1 resolves CONTEXT.md's Discretion question: `documents` belongs HERE, not to shortcuts. `getPath('documents')` is called exactly once, at `save_sync.ts:146` inside `getDefaultGogSavePaths`, reached via `syncGOGSaves` — so this cluster is ALSO hard-blocked on REQ-34.5-01."
    - "`callTool` is reassigned from Phase 34.1 by REQ-34.1-10 / 34.1 D-14 because it is Winetricks/winecfg/runExe — Wine tooling, this phase's domain."
    - "Saves-sync CONFLICT BEHAVIOUR is explicitly out of scope. Both runners' sync paths use non-interactive CLI flags and research verified no dialog or prompt is reachable; this is a byte-faithful port of inherited Electron behaviour, not a redesign."
  artifacts:
    - path: "src/backend/sidecar/runnerMiscFlowRegistration.ts"
      provides: "callTool, egsSync, getGOGLinuxInstallersLangs, syncSaves, syncGOGSaves — completing the module at 11"
      contains: "syncGOGSaves"
    - path: "src/backend/sidecar/__tests__/runnerMiscFlows.test.ts"
      provides: "Extended bidirectional coverage to all 11 misc channels plus the documents-path pin"
  key_links:
    - from: "src/backend/sidecar/runnerMiscFlowRegistration.ts syncGOGSaves"
      to: "src/backend/save_sync.ts:146 getPath('documents')"
      via: "libraryManagerMap['gog'].getGame(appName).syncSaves(...) → getDefaultGogSavePaths"
      pattern: "syncGOGSaves"
    - from: "src/backend/sidecar/runnerMiscFlowRegistration.ts callTool"
      to: "src/backend/tools/index.ts Winetricks.run / runWineCommandOnGame"
      via: "curated named imports, never tools/ipc_handler.ts"
      pattern: "runWineCommandOnGame"
---

<objective>
Complete the misc module with the three "other" channels and the two saves-sync channels, closing
the last of this slice's channel work.

Purpose: these five are where CONTEXT.md left three open questions — whether the "other" three hide
uncovered Electron dependencies, where `documents` belongs, and whether `callTool`'s winetricks
branch is blocked by the 34.6 deferral. Research answered all three; this plan implements those
answers and records them where the code lives.

Output: eleven of eleven misc channels, and a saves-sync path that resolves `documents` correctly on
a native game.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-RESEARCH.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-01-SUMMARY.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-07-SUMMARY.md

<interfaces>
Electron registration ground truth (verified by grep):
  src/backend/tools/ipc_handler.ts:25  addHandler('callTool', async (event, { tool, exe, appName, runner }) => { … })
  src/backend/main.ts:840   addHandler('getGOGLinuxInstallersLangs', async (event, appName) => libraryManagerMap['gog'].getLinuxInstallersLanguages(appName))
  src/backend/main.ts:1251  addHandler('egsSync',      async (event, args) => libraryManagerMap['legendary'].toggleGamesSync(args))
  src/backend/main.ts:1255  addHandler('syncGOGSaves', async (event, gogSaves, appName, arg) => libraryManagerMap['gog'].getGame(appName).syncSaves(arg, '', gogSaves))
  src/backend/main.ts:1263  addHandler('syncSaves',    async (event, { arg = '', path, appName, runner }) => { … epic-offline branch … })

callTool's four branches (tools/ipc_handler.ts:25-63) — port ALL of them:
  'winetricks' → Winetricks.run(runner, appName)                     ← already works, do NOT gate on 34.6
  'winecfg'    → runWineCommandOnGame(runner, appName, { gameSettings, commandParts: ['winecfg'], wait: false })
  'runExe'     → runWineCommandOnGame(… commandParts: [exe], startFolder: path.parse(exe).dir …)
  then, for runner === 'gog': checkForOfflineInstallerChanges + sendFrontendMessage('pushGameToLibrary', …)
  finally: sendGameStatusUpdate({ appName, runner, status: 'done' })

Curated import targets:
  src/backend/tools/index.ts:528 → Winetricks ; :874 → runWineCommandOnGame
  src/backend/storeManagers → libraryManagerMap
  src/backend/utils.ts → sendGameStatusUpdate, getGame, isEpicServiceOffline
  src/backend/ipc.ts → sendFrontendMessage

The `documents` dependency (research Pitfall 1):
  syncGOGSaves → libraryManagerMap['gog'].getGame(appName).syncSaves(...) → getDefaultGogSavePaths
    → save_sync.ts:146  DOCUMENTS: game.isNative() ? app.getPath('documents') : '%USERPROFILE%\\Documents'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port callTool, egsSync and getGOGLinuxInstallersLangs</name>
  <files>src/backend/sidecar/runnerMiscFlowRegistration.ts</files>
  <read_first>
    - src/backend/sidecar/runnerMiscFlowRegistration.ts (as written by plan 34.5-07 — the six existing registrations and the module docstring's remaining five entries)
    - src/backend/tools/ipc_handler.ts (full file — `callTool`'s body verbatim including all four branches, the gog post-step and the final `sendGameStatusUpdate`; READ-ONLY, never import it: it also registers `runWineCommandForGame` and the three DEFERRED winetricks channels)
    - src/backend/main.ts L838-844 and L1249-1254 (`getGOGLinuxInstallersLangs` and `egsSync` verbatim)
    - src/backend/tools/index.ts L520-560 and L874-900 (`Winetricks.run` and `runWineCommandOnGame` signatures)
    - .planning/phases/34.5-.../34.5-RESEARCH.md § "Common Pitfalls" Pitfall 4 (the winetricks branch already works — flag, do not block)
  </read_first>
  <action>
    Add three `ipcMain.handle` registrations.

    `callTool` — port the body verbatim with all four branches intact: resolve `gameSettings` via
    `libraryManagerMap[runner].getGame(appName).getSettings()`, switch on `tool` for `winetricks`,
    `winecfg` and `runExe` exactly as the source does (including `runExe`'s `path.parse(exe).dir`
    working directory and its `if (exe)` guard), then the `runner === 'gog'` post-step
    (`checkForOfflineInstallerChanges` + `sendFrontendMessage('pushGameToLibrary', …)`), then the
    unconditional `sendGameStatusUpdate({ appName, runner, status: 'done' })`.

    Add a comment on the `winetricks` branch recording Pitfall 4 explicitly: this branch calls
    `Winetricks.run()` on the shared `tools/index.ts` object, is pure Node with no Electron
    dependency, and works TODAY despite `winetricksAvailable`/`winetricksInstall`/
    `winetricksInstalled` being deferred to Phase 34.6. Do NOT add a guard, a feature flag or a
    "wait for 34.6" early return. State that it must be flagged in `34.5-PORTED-CHANNELS.md` so a
    reader does not assume it is broken.

    Add a comment recording that `callTool` arrived here from Phase 34.1 by REQ-34.1-10 / 34.1 D-14
    because it is Wine tooling and sat in slice 4 only because the inventory grouped channels by
    file.

    `egsSync` → `libraryManagerMap['legendary'].toggleGamesSync(args)`, unpacking `args[0]`.
    `getGOGLinuxInstallersLangs` → `libraryManagerMap['gog'].getLinuxInstallersLanguages(appName)`,
    unpacking `args[0]`.

    Record in the SUMMARY, with file:line evidence, the verification that none of these three
    reaches an Electron API the stub does not cover — this closes a gray area CONTEXT.md left open,
    and the deliverable is the evidence, not the assertion.
  </action>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle(" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns 9.
    - All four `callTool` branch literals (`'winetricks'`, `'winecfg'`, `'runExe'`, and the `runner === 'gog'` post-step) are present.
    - `grep -c "34.6" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns at least 1 AND no early return, feature flag or thrown error guards the winetricks branch — verify by reading the branch, not by grep alone.
    - `grep "^import" src/backend/sidecar/runnerMiscFlowRegistration.ts | grep -c ipc_handler` returns 0.
    - The SUMMARY lists, per channel, every module each of the three reaches and states which are already baselined in the reach ledger.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The three "other" channels are ported with `callTool`'s four branches intact, and the winetricks branch is flagged as working rather than gated on a phase it does not depend on.</done>
</task>

<task type="auto">
  <name>Task 2: Port syncSaves and syncGOGSaves on top of the documents path</name>
  <files>src/backend/sidecar/runnerMiscFlowRegistration.ts</files>
  <read_first>
    - src/backend/main.ts L1255-1300 (both saves-sync bodies verbatim, including `syncSaves`'s `isEpicServiceOffline()` branch and its `logWarning` early return)
    - src/backend/save_sync.ts L120-175 (`getDefaultGogSavePaths` and the `DOCUMENTS` entry at L146 — the single `getPath('documents')` call site in the entire backend, and the `game.isNative()` gate around it)
    - src/backend/storeManagers/legendary/games.ts L843-873 and src/backend/storeManagers/gog/games.ts L850-865 (both `syncSaves` implementations — confirm for yourself the non-interactive `-y` flag and the absence of any dialog before concluding D-15's landmine does not apply)
    - src/backend/sidecar/pathShim.ts (as extended by plan 34.5-01 — confirm the `documents` case exists before registering `syncGOGSaves`)
  </read_first>
  <action>
    Add two `ipcMain.handle` registrations.

    `syncGOGSaves` → `libraryManagerMap['gog'].getGame(appName).syncSaves(arg, '', gogSaves)`,
    unpacking `gogSaves`, `appName` and `arg` from `args[0]`/`args[1]`/`args[2]` in the Electron
    handler's parameter order. Add a comment recording research Pitfall 1: this channel is the sole
    reason `pathShim` needed a `documents` case at all — `save_sync.ts:146` resolves GOG's `DOCUMENTS`
    save-folder variable through it when `game.isNative()` — so this cluster shares REQ-34.5-01's
    hard precondition with the shortcuts cluster despite belonging to a different domain. State that
    CONTEXT.md D-09 grouped `documents` under shortcuts and that grouping was wrong.

    `syncSaves` → port the body verbatim including the `runner === 'legendary'` branch that calls
    `isEpicServiceOffline()` and returns early with the existing `logWarning` when Epic is offline.
    Do not restructure that branch and do not change the early-return's return value.

    Add a comment recording the scope boundary: saves-sync CONFLICT BEHAVIOUR is explicitly out of
    scope for this phase. Both runners' sync paths use non-interactive CLI flags and no dialog or
    prompt is reachable, so D-15's fire-and-forget-rejection landmine does not apply here; this is a
    byte-faithful port of inherited Electron behaviour, not a redesign.
  </action>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle(" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns 11.
    - `grep -c "ipcMain.on(" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns 0 — this module has no send channels.
    - `syncSaves`'s `isEpicServiceOffline()` branch is present and its early return is unchanged from the Electron source.
    - `grep -c "save_sync.ts:146" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns at least 1 — the `documents` dependency is cited, not implied.
    - The SUMMARY states, with file:line evidence, that no dialog or prompt is reachable from either sync path.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Both saves-sync channels are ported faithfully, and the `documents` dependency that CONTEXT.md filed under the wrong cluster is recorded where it actually lives.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the misc cluster's registration-kind coverage</name>
  <files>src/backend/sidecar/__tests__/runnerMiscFlows.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/runnerMiscFlows.test.ts (as written by plan 34.5-07 — EXTEND its arrays and FLIP the forward-looking "not yet registered" pin; do not delete assertions)
    - src/backend/sidecar/__tests__/pathShim.test.ts (plan 34.5-01's env/platform save-restore discipline, for the `documents` assertion)
    - src/backend/tools/ipc_handler.ts and src/backend/main.ts L840, L1251, L1255, L1263 (the kind ground truth — all five are `addHandler`)
    - src/backend/sidecar/__tests__/steamAuthFlows.test.ts L40-70 (the disposable-homedir harness, required because this suite reaches `gog_store`/`gogdlConfig` through the real configStore chain)
  </read_first>
  <action>
    Extend `HANDLE_CHANNELS` with `callTool`, `egsSync`, `getGOGLinuxInstallersLangs`, `syncSaves`
    and `syncGOGSaves`, flipping plan 34.5-07's "not yet registered" pin. Assert the module is now
    complete at 11 handle channels and 0 send channels, and assert `listenerRegistry` gained nothing
    from this module.

    Add a `documents`-path assertion: with the redirected home in place, the value
    `getPath('documents')` resolves to is under that redirected home and is the value
    `getDefaultGogSavePaths` would substitute for `DOCUMENTS` on a native game. If driving
    `getDefaultGogSavePaths` end-to-end is disproportionate, assert the narrower property — that
    `getPath('documents')` no longer throws and resolves under the redirected home — and say so
    plainly in the SUMMARY rather than implying broader coverage.

    Add a `callTool` branch test: with `Winetricks.run` and `runWineCommandOnGame` mocked, each of
    `tool: 'winetricks'`, `'winecfg'` and `'runExe'` reaches the right function with the right
    arguments, and `sendGameStatusUpdate` is called with `status: 'done'` on every branch. Include
    an explicit case proving the `winetricks` branch is NOT gated — it must call `Winetricks.run`,
    not throw or no-op (Pitfall 4).

    Keep the earlier assertion that `abort`, `getSystemInfo`, `copySystemInfoToClipboard` and
    `hasExecutable` are absent from both registries, and add that the three deferred winetricks
    channel names are absent too — `callTool` reaching `Winetricks.run` must not be confused with
    registering winetricks channels.
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/runnerMiscFlows.test.ts` exits 0.
    - The suite asserts both directions for all 11 channels (22 assertions minimum) and asserts 0 send channels.
    - A case proves the `winetricks` branch of `callTool` invokes `Winetricks.run` — it fails if the branch is gated or removed.
    - Cases exist for all three `callTool` tools, each asserting `sendGameStatusUpdate` fired with `status: 'done'`.
    - A `documents` assertion exists and the SUMMARY states exactly what it does and does not prove.
    - `git diff --name-only` does not list `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/runnerMiscFlows.test.ts</automated>
  </verify>
  <done>All eleven misc channels are kind-pinned, `callTool`'s three tools each reach the right function, and the winetricks branch is proven live rather than assumed deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → `callTool` payload (`tool`, `exe`, `appName`, `runner`) | `runExe` constructs a Wine command from a caller-supplied executable path |
| sidecar → user save data | Both sync channels read and write the user's own save files through the runner CLIs |
| sidecar → `getPath('documents')` | Resolves the GOG `DOCUMENTS` save-folder variable on native games |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-44 | Tampering | `callTool`'s `runExe`/`winecfg` command construction from caller-supplied values | accept | Unchanged from Electron and an already-accepted trust boundary — the caller is this app's own frontend, not untrusted remote input. This phase relocates the code onto the sidecar transport; it does not widen the boundary and must not re-architect it (research § Security Domain). |
| T-34.5-45 | Tampering | saves-sync overwriting user save data on a wrong path resolution | mitigate | `documents` is implemented in `pathShim` (REQ-34.5-01) with the same OS conventions Electron uses, and this plan is a hard dependant of that; the alternative — an unimplemented name — throws loudly rather than resolving to a wrong directory. Conflict behaviour itself is unchanged inherited CLI behaviour. |
| T-34.5-46 | Denial of Service | a prompt or dialog blocking a sync path under the sidecar | mitigate | Verified by direct read that both runners' sync paths use non-interactive CLI flags with no reachable dialog; the SUMMARY records the file:line evidence rather than asserting it. |
| T-34.5-47 | Repudiation | `callTool`'s winetricks branch silently gated on a deferred phase | mitigate | A test invokes the branch and asserts `Winetricks.run` is called; a gate or no-op fails the suite (research Pitfall 4). |
| T-34.5-48 | Tampering | a test clobbering the developer's real GOG/gogdl config | mitigate | Disposable-homedir harness plus `jest.setupContainment.ts`, never bypassed (project memory: `tests-clobbering-real-steam-store`). |
| T-34.5-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `cd src/backend && npx jest sidecar/__tests__/runnerMiscFlows.test.ts` exits 0 with 11-channel coverage.
- `grep -c "ipcMain.handle(" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns 11; `ipcMain.on(` returns 0.
- No `ipc_handler` import in the module; the winetricks branch is ungated.
</verification>

<success_criteria>
All 38 of this slice's channels are now registered across the four modules; the three gray areas
CONTEXT.md flagged are answered in source with evidence; and the saves-sync path resolves
`documents` through the seam that was built for it.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-12-SUMMARY.md` when done
</output>
