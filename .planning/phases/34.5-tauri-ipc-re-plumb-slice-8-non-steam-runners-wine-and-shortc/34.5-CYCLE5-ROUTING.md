---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 38
type: routing
cycle: 5
blocking: false
created: 2026-08-02
---

# Phase 34.5 gap cycle 5 — routing and disposition

## Authorisation

Binding developer decisions for this cycle, recorded 2026-08-02, quoted verbatim from
`34.5-38-PLAN.md`'s `<interfaces>` block:

> **D-CYCLE5-A — Gate-first; Epic item 1 enters as BLOCKED.** This cycle authors
> `34.5-LIVE-GATE-RERUN-2.md` (with `verdict: null`, written before any live work, the discipline
> plans 34.5-03 and 34.5-19 followed for runs 1 and 2) and RUNS the gate. Gate item 1 (Epic) is
> entered as **BLOCKED**, with the parked pre-authentication defect named as its cause — it is not
> PASS, not FAIL, and not silently omitted. Items 2, 3, 4 and 5 get a real attempt; 4 and 5 for the
> first time ever. Epic diagnosis is NOT this cycle's blocker and must not gate the contract
> authoring or the run.
>
> **D-CYCLE5-B — the gate run uses the dev secret vault (`GAMELIB_DEV_SECRET_VAULT=1`).** The
> developer accepted this trade knowingly. Per `34.5-UNTESTED-ITEMS.md`'s own binding statement,
> ledger rows **U-34.5-01** and **U-34.5-10** CANNOT be retired by this run and MUST stay OPEN.

Stated plainly, in one line: gap cycle 5 is a GATE cycle — the blocking five-item live gate IS
authored and IS run this cycle, and cycle 4's own deferral of that work terminates here.
`34.5-CYCLE4-ROUTING.md`'s own `## Authorisation` section named the future cycle this hand-off was
left for, quoted verbatim: *"They are to be re-homed into whichever future cycle authorises the
blocking gate."* **This is that cycle.**

## Disposition of plans 34.5-29 / 34.5-30 / 34.5-31

All three remain on disk, unmodified, unexecuted. No edit, renumber or deletion is applied to any
of the three, and a reader must not infer from their `[ ]` state in `ROADMAP.md` that their work
went undone — this document supersedes and re-homes their work by reference, never by editing them.

### 34.5-29

Task 1 (the Epic user-agent fix) is **VOID**, by two independent gates already recorded in
`34.5-CYCLE4-ROUTING.md`: its own `fix-first` self-halt (quoted there: *"`fix-first` or `escalate`
→ STOP IMMEDIATELY. Do not run Task 1, 2 or 3."*), and its own `R1-FALSIFIED` verdict branch
(quoted there: *"`verdict: R1-FALSIFIED` — make NO change to `USER_AGENTS`. R2 survived but is not
confirmed."*). It stays void this cycle for a THIRD, newer reason: the second discriminator's
(`34.5-G6-EPIC-DISCRIMINATOR-2.md`) `verdict: E1` `## Routing` section authorises instrumentation
and diagnosis only — quoted verbatim there: *"This plan does not authorise shipping a fix, and does
not authorise changing `USER_AGENTS`, `EPIC_LOGIN_URL`, or `matchOAuthRedirect`."* — and
`.planning/debug/epic-login-non-interactive.md`'s `## Constraints` section independently forbids
the same three: *"Do NOT change `USER_AGENTS`, `EPIC_LOGIN_URL`, or `matchOAuthRedirect`."*

Tasks 2 and 3 (the item-4/item-5 preflight producing `34.5-G6-ITEM45-PREFLIGHT.md`) are **RE-HOMED
to plan 34.5-39** this cycle.

### 34.5-30

**SUPERSEDED by plan 34.5-40**, which authors `34.5-LIVE-GATE-RERUN-2.md`.

### 34.5-31

**SUPERSEDED by plans 34.5-41 (run + verdict) and 34.5-42 (propagation)**.

## Why the preflight is re-homed HERE and not earlier

`34.5-CYCLE4-ROUTING.md`'s own disposition of plan 34.5-29 named the reason directly, quoted
verbatim: Tasks 2 and 3 *"are deliberately NOT re-homed into cycle 4. They are to be re-homed into
whichever future cycle authorises the blocking gate, because a preflight written now would be stale
again by the time that gate actually runs — this is the precise reason items 4 and 5 have already
been skipped twice (once per prior gate run), each time because groundwork recorded days earlier
had gone stale by the time the item was reached."*

Plan 34.5-39 runs in the wave immediately before the contract (plan 34.5-40), and the contract
carries a precondition re-confirming the preflight target AT GATE TIME — mirroring the shape of
precondition 9 in the superseded `34.5-30-PLAN.md` (*"item 4 and item 5 preflight consumed...its
target verification is re-confirmed AT GATE TIME (the install may have changed since plan 34.5-29
ran)"*), so a third skip requires an explicit refusal rather than a drift.

## Item 1 (Epic) enters BLOCKED — the cause, named

Per D-CYCLE5-A. The cause, precisely, not overstated:

- The POST-authentication root cause is CONFIRMED and its fix is IMPLEMENTED but STATIC-ONLY
  (commit `c857ade8e`, ledger row `U-34.5-11`) — WKWebView silently refuses Epic's client-side
  navigation to its own `redirectUrl`; the `.on_navigation` exfil intercept plus
  `epic_oauth_redirect_observer_script` were built to close it and have never run live. Per
  `.planning/debug/epic-login-non-interactive.md`'s `Resolution.verification`: `cargo check`/
  `cargo test` (92 passed, 0 failed, 1 ignored), `npx tsc --noEmit` clean, `npm run test:ci`
  3548/3548 at the time of that entry — but explicitly *"STATIC/COMPILE/UNIT PROOF ONLY"*, and *"FIX
  DESIGN branch-A step (4), the full live gate...OWED, not attempted, not partially covered by
  anything above."*

- The PRE-authentication half is the blocker and is PARKED by explicit developer decision
  (`developer_override_2026_08_02T19_00_00`). Record its measured shape rather than a slogan: across
  the Tauri arm's runs, `/id/api/email/exists` returned **403 x1, `xhr.timeout` x2 and success x1**,
  and NO Tauri run has ever completed an Epic login (`pre_auth_defect_reframed_as_intermittent`).
  Cite the debug file's own instruction not to treat "403" and "timeout" as interchangeable — its
  `three_outcome_reframe` section states the grouping of all three outcomes as one phenomenon *"is
  now known to conflate two DIFFERENT things: the `xhr.timeout` outcomes fit [a distinct]...shape
  specifically. The 403 outcome remains"* a separate, still-unexplained phenomenon.

- The debug file's own NEW RISK, recorded verbatim: *"the production observer script wraps
  `window.fetch` using the SAME wrapping technique `DEV_LOGIN_DIAGNOSTIC_INIT_SCRIPT` uses...and that
  script's patched network primitives are this file's OWN live suspect for CAUSING the 403 the
  pre-auth thread is investigating....if patched `fetch` is confirmed to be what [the anti-bot
  system]'s fingerprinting keys on, shipping this fix unconditionally could make the pre-auth 403
  permanent for every Epic login attempt rather than opt-in....it is recorded here as an open risk
  for the live gate this cycle cannot run, and as a candidate explanation to check FIRST if/when
  that live gate still shows a 403 after this fix ships."* This risk **must be checked FIRST** when
  the live gate runs.

- The consequence, stated as an EXPECTATION rather than a certainty: a BLOCKED item 1 is not a PASS,
  so the phase is **expected not to reach 5/5 this cycle**. This is not a guarantee: plan 34.5-40's
  opportunistic-attempt clause REQUIRES the operator to drive item 1 for real if Epic's login form
  renders and accepts input, and the measured shape above contains one success in four pre-auth
  attempts. This branch is therefore **expected-unreachable, NOT unreachable-by-construction**, and
  no downstream plan may treat item 1's BLOCKED entry disposition as its settled final result.

## Routing by verdict

| Branch | Outcome | Next action |
|---|---|---|
| items 2,3,4,5 all PASS, item 1 BLOCKED | Phase 34.5 does NOT close (D-08) | a further cycle scoped to Epic ONLY — unparking the pre-auth thread; Phase 34.6's and Phase 35's dependency lines stay untouched |
| any of items 2,3,4,5 FAIL | Phase 34.5 does NOT close | `/gsd-plan-phase 34.5 --gaps` — gap cycle 6 |
| any item NOT ATTEMPTED | counts toward neither passed nor failed | the shortfall is stated in the contract's `notes`; the item stays on the ledger; gap cycle 6 |
| 5 of 5 PASS | closes the phase (D-08) | **EXPECTED to be unreachable this cycle, NOT unreachable by construction.** Item 1 ENTERS as BLOCKED and a BLOCKED item is not a PASS, but plan 34.5-40's opportunistic-attempt clause requires the operator to drive item 1 for real if Epic's login form renders and accepts input (measured shape: 403 x1 / `xhr.timeout` x2 / **success x1**), in which case item 1 resolves PASS or FAIL like any other item. This branch is taken if and only if the gate's RECORDED frontmatter reads `items_passed: 5` with `items_failed: 0`, `items_blocked: 0` and `items_not_attempted: 0` — then D-08 is satisfied, Phase 34.5 closes, and the next action is `/gsd-verify-work`. |

D-08, restated verbatim from `34.5-CONTEXT.md`: *"**Blocking live gate covering all three logins**
(Epic, GOG, Amazon) on macOS — same standard as every prior slice, no advisory mode, no
pre-authorized override."* There is no escape hatch in this cycle that lets a partial result close
the phase.

**Binding note for downstream plans.** The 5-of-5 row is recorded as EXPECTED-unreachable, never as
unreachable-by-construction. No plan in this cycle may write an acceptance criterion that
presupposes the phase does not close; any criterion whose behaviour depends on non-closure MUST key
off the four RECORDED counters (`items_passed` / `items_failed` / `items_blocked` /
`items_not_attempted`) rather than off item 1's entry disposition. This is **not** a loosening of
D-08 and **not** a partial-pass escape hatch: D-08 is unchanged and still binds, anything short of
5 PASS / 0 FAIL / 0 BLOCKED / 0 NOT ATTEMPTED still means Phase 34.5 does not close, and no partial
or blocked result may unblock a downstream phase. The conditional exists to prevent the OPPOSITE
error only — recording a false "does not close" over a 5-of-5 the run genuinely earned.

## Ledger interaction (Rule 2 and Rule 3)

Per D-CYCLE5-B, this run uses `GAMELIB_DEV_SECRET_VAULT=1`, and this choice alone keeps
`U-34.5-01` (the real macOS Keychain path) and `U-34.5-10` (the keyring read-timeout/human-approval
race, plan 34.5-35's 45s/120s constants) OPEN — per `34.5-UNTESTED-ITEMS.md`'s own binding
statement: *"a run using the dev-only secret vault CANNOT serve as evidence for plan 34.5-35's item-3
('only ONE Keychain approval prompt') claim. `U-34.5-01` and `U-34.5-10` therefore need EITHER two
separate live runs, OR one single run with `GAMELIB_DEV_SECRET_VAULT` left unset...A gate operator
who enables the vault to save time on Keychain prompts is implicitly choosing to leave BOTH rows
open for that run, not just `U-34.5-01`."*

**Binding consequence: no plan in this cycle may claim either row retired, and no plan may treat
plan 34.5-35's raised constants as live-proven.** The gate contract (plan 34.5-40) is the place
Rule 3's *"name it as a known hole"* obligation (`34.5-UNTESTED-ITEMS.md` § Rules, Rule 3: *"any
OPEN row that the gate's own items would have covered must be named in the gate contract as a known
hole rather than silently assumed covered"*) is discharged.

## Standing rule carried into every cycle-5 plan

A green test suite is not evidence of live behaviour. This phase has been green at 3279, 3387,
3447, and 3548 tests while failing its own blocking gate. Every plan in this cycle states the LIVE
observable that would prove its claim, and none records anything closed on unit evidence alone.

## Measured baseline (this cycle's HEAD)

Every number below was measured now, at this plan's HEAD, in the order the plan's Task 2 requires.
None is copied or rounded from a prior document.

1. **HEAD**: `c8f314205` (`git rev-parse --short HEAD`). Working tree at measurement time was
   **dirty** (`git status --porcelain`, 6 entries, none created by this plan): `M
   .planning/STATE.md`, `M .vscode/settings.json`, `?? .claude/skills/spike-findings-gamelib/sources/006-cpp-vtable-abi/harness.exe`,
   `?? .claude/skills/spike-findings-gamelib/sources/006-cpp-vtable-abi/harness.pdb`, `??
   .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/__pycache__/`,
   `?? scratchpad/`. `.planning/` paths present: `.planning/STATE.md` (modified — a prior
   orchestrator hand-correction, not this plan's) and the 34.4.1 `__pycache__/` dir (untracked).
   None of these six are this plan's files and none was staged, reverted or cleaned by this plan.

2. **`npm run test:ci`** — exit `0`. Verbatim summary:
   ```
   Test Suites: 181 passed, 181 total
   Tests:       3553 passed, 3553 total
   Snapshots:   0 total
   Time:        65.803 s
   ```
   Zero failures — no suite and no test failed. This DIFFERS from
   `.planning/debug/epic-login-non-interactive.md`'s `Resolution.verification` entry, which recorded
   *"3548/3548...ONE known, PRE-EXISTING, UNRELATED failure carried over unchanged from baseline:
   `longRunningChannels.test.ts`'s WR-08 stripper-integrity guard flags
   `DEV_LOGIN_DIAGNOSTIC_INIT_SCRIPT`'s own multi-line `r#"..."#;` raw-string delimiter lines."*
   Per this plan's own `<interfaces>` instruction, that guard's present state was to be MEASURED,
   not assumed: `git merge-base --is-ancestor` confirms both `d22fe0dfd` (`fix(WR-08): make
   quote-balance guards raw-string aware`) and `50a807a56` (its follow-up) are ancestors of this
   HEAD. **Determination: the WR-08 failure is CLOSED at this HEAD** — the raw-string-aware fix is
   present and the guard now passes; this is not a "known flake" label applied without checking, it
   is a direct observation (zero failed suites, zero failed tests) cross-checked against the fix
   commits' presence in history.
   The jest count (3553) is 5 HIGHER than the debug file's own 3548. `git diff --stat
   c857ade8e..HEAD` (the post-auth-fix commit that recorded 3548, through this HEAD) shows **zero**
   changes under `src/` or `src-tauri/` — only five `.planning/` documents changed (four new
   34.5-38..42 PLAN files plus `ROADMAP.md`/`STATE.md` edits), none of which can add or remove a
   jest test. The +5 delta is therefore **unexplained by any commit in this range** and is recorded
   honestly as an open discrepancy rather than invented as a false explanation — most likely the
   debug file's own count was itself imprecise (its "3548/3548 passing" phrasing sits inconsistently
   beside its own claim of "ONE known...failure" in the same sentence), but this plan does not assert
   that as fact, only as the most likely reading of an inconsistency it did not create.

3. **`npx tsc --noEmit`** — exit `0`. No TypeScript errors (one unrelated `npm warn Unknown project
   config "node-linker"` advisory line, not a compiler diagnostic).

4. **`cd src-tauri && cargo check`** — exit `0`. Verbatim: `Finished \`dev\` profile [unoptimized +
   debuginfo] target(s) in 0.43s`. No errors, no warnings.

5. **`cd src-tauri && cargo test`** — exit `0`. Verbatim: `test result: ok. 92 passed; 0 failed; 1
   ignored; 0 measured; 0 filtered out; finished in 0.06s` (93 tests declared, 1 pre-existing
   `#[ignore]`). This EXACTLY matches the debug file's own 92 passed / 0 failed / 1 ignored — no
   Rust-side discrepancy.

6. **`python3 ported-channels-gate.py`** — exit `0`. Verbatim: `OK: all 38 channels declared exactly
   once with correct send/invoke kind, every proof level permitted with an explicit
   PENDING/PASS/FAIL state on every LIVE cell, per-module counts are 11/9/7/11, the 38+3+16=57
   arithmetic reconciles against IPC-PORT-INVENTORY.md with no name in two buckets, the residuals and
   obligations sections carry their required citations, and live-gate item 4 is scoped to exactly
   {addToSteam, addShortcut} (REQ-34.5-11).`
   **`python3 ported-channels-gate.py --self-test`** — exit `0`. The self-test deliberately
   constructs nine broken documents and asserts the gate rejects each one; its own final line reads
   `SELF-TEST OK: every assertion rejects its corresponding bad input.` (the intermediate `GATE
   FAILED:` lines in its output are the self-test's own expected-rejection assertions succeeding,
   not a failure of this measurement).

**No command exited non-zero.** No isolated single-suite re-run was required by the plan's own
"non-zero exit" branch, because there was no non-zero exit to isolate.

**This is the floor plan 34.5-40's precondition 6 asserts.** The rule: a count AT OR BELOW 3553
jest / 92 Rust (0 failed, 1 ignored) at gate time means HEAD is stale and must be explained before
the run proceeds. The corollary: a count ABOVE this floor is expected and healthy only if a plan in
this cycle landed tests — and no plan in this cycle (34.5-38 through 34.5-42) lands any, so a
HIGHER count at gate time also needs an explanation, exactly as this section had to supply one for
the unexplained +5 jest delta recorded above.

## Outcome

Filled by plan 34.5-42 after the gate runs.
