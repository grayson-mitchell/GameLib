---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 07
subsystem: ipc
tags: [tauri, sidecar, ipc, electron-migration, wine, runtime, legendary, gogdl, comet, nile]

# Dependency graph
requires:
  - phase: 34.5-04
    provides: the declared-but-empty `runnerMiscFlowRegistration.ts` seam this plan fills 6 of 11 channels into
provides:
  - Six real invoke channels on the Tauri sidecar: getLegendaryVersion, getGogdlVersion, getCometVersion, getNileVersion, downloadRuntime, isRuntimeInstalled
  - D-04's correction recorded in source (getCometVersion is GOG's, not Zoom's)
  - A test-suite-wide swap of the "genuinely still unported" canary channel from getLegendaryVersion to winetricksInstall (Phase 34.6-deferred, so it will not need re-swapping)
affects: [34.5-12, 34.5-13, 34.6]

# Tech tracking
tech-stack:
  added: []
  patterns: ["curated named-import registration module (no ipc_handler.ts side-effect import)"]

key-files:
  created:
    - src/backend/sidecar/__tests__/runnerMiscFlows.test.ts
  modified:
    - src/backend/sidecar/runnerMiscFlowRegistration.ts
    - src/backend/sidecar/__tests__/testContainment.test.ts
    - src/backend/sidecar/__tests__/bootstrap.test.ts
    - src/backend/sidecar/__tests__/settingsFlows.test.ts
    - src/backend/sidecar/__tests__/installFlows.test.ts
    - src/backend/sidecar/__tests__/gameDetailsFlows.test.ts
    - src/backend/sidecar/__tests__/enrichmentFlows.test.ts

key-decisions:
  - "D-04 recorded in source: getCometVersion is GOG's channel (gated on gameInfo.runner === 'gog' at launcher.ts:973), not Zoom's; Zoom is exactly 3 channels, all dropped by D-02."
  - "5 pre-existing 'Invariant B' guard tests used getLegendaryVersion as their 'still genuinely unported' stand-in; swapped to winetricksInstall (permanently deferred to Phase 34.6 by D-03) so no further plan re-triggers this same fix."

requirements-completed: [REQ-34.5-06, REQ-34.5-09]

# Metrics
duration: ~20min
completed: 2026-07-29
---

# Phase 34.5 Plan 07: Runner CLI Version + Wine Runtime Channels Summary

**Ported the 4 runner-CLI version probes (Legendary/GOGDL/Comet/Nile) and 2 Wine-runtime channels (downloadRuntime/isRuntimeInstalled) onto the Tauri sidecar as curated pass-through invoke handlers, and recorded D-04's GOG-not-Zoom correction for `getCometVersion` in source.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-07-29 (session start)
- **Completed:** 2026-07-29T07:06:51Z
- **Tasks:** 2
- **Files modified:** 8 (1 registration module, 1 new test file, 6 existing test files touched by the Rule 1 fix below)

## Accomplishments
- `registerRunnerMiscFlows()` now registers 6 of its 11 declared channels: `getLegendaryVersion`, `getGogdlVersion`, `getCometVersion`, `getNileVersion`, `downloadRuntime`, `isRuntimeInstalled` — all `ipcMain.handle`, curated named imports only (`../utils/helperBinaries`, `../wine/runtimes/runtimes`), never `utils/ipc_handler.ts` or `wine/runtimes/ipc_handler.ts`.
- D-04 is now recorded where a future reader will find it: an in-source comment above the `getCometVersion` registration states `comet` is GOG's Galaxy Communication replacement (`launcher.ts:973`), not Zoom's, and cites Zoom's exact 3-channel drop (D-02).
- New `runnerMiscFlows.test.ts` (16 tests) proves bidirectional registration kind for all 6 channels, proves the 4 channels `utils/ipc_handler.ts` also registers (`abort`, `getSystemInfo`, `copySystemInfoToClipboard`, `hasExecutable`) are absent from both registries, asserts no `ipc_handler` import in the module's source, and pins the 5 channels plan 34.5-12 owns as explicitly NOT YET registered (forward-looking pin, not to be deleted by that plan).
- Fixed a real regression this plan's own change caused: 5 pre-existing tests (`bootstrap.test.ts`, `settingsFlows.test.ts`, `installFlows.test.ts`, `gameDetailsFlows.test.ts`, `enrichmentFlows.test.ts`) used `getLegendaryVersion` as their canonical "genuinely still unported" Invariant B stand-in channel — now that it is real, invoking it through the full bootstrap hits the real handler and throws `libraryManagerMap.legendary.runRunnerCommand is not a function` (the storeManagers legendary manager isn't wired for these lighter test harnesses) instead of returning `UNPORTED_CHANNEL_MARKER`. Swapped all 5 to `winetricksInstall`, which D-03 permanently defers to Phase 34.6 — unlike `getLegendaryVersion`, this will not need a further substitution once plan 34.5-12 lands.

## Task Commits

Each task was committed atomically:

1. **Task 1: Register the four runner CLI version channels and the two runtime channels** - `6c877ba23` (feat)
2. **Task 2: Bidirectional registration-kind test for the version and runtime channels** - `7be719ce9` (test) — includes the Rule 1 fix to the 5 pre-existing Invariant B guard tests, folded into this commit since both changes were verified together.

_Note: this plan is not `tdd="true"`; both tasks are `type="auto"`._

## Files Created/Modified
- `src/backend/sidecar/runnerMiscFlowRegistration.ts` - filled in 6 of 11 declared channels (version probes + runtime download/install-check), curated imports only, D-04 comment
- `src/backend/sidecar/__tests__/runnerMiscFlows.test.ts` - new 16-test bidirectional registration-kind + curated-import + forward-looking-pin suite
- `src/backend/sidecar/__tests__/testContainment.test.ts` - classified the new suite in `STRUCTURALLY_CONTAINED_SUITES` with a docstring paragraph (per Block C's set-equality tripwire)
- `src/backend/sidecar/__tests__/bootstrap.test.ts` - Invariant B canary channel `getLegendaryVersion` → `winetricksInstall`
- `src/backend/sidecar/__tests__/settingsFlows.test.ts` - same swap
- `src/backend/sidecar/__tests__/installFlows.test.ts` - same swap
- `src/backend/sidecar/__tests__/gameDetailsFlows.test.ts` - same swap
- `src/backend/sidecar/__tests__/enrichmentFlows.test.ts` - same swap

## Decisions Made
- Used `RuntimeName` (from `common/types`) as the cast type for `downloadRuntime`/`isRuntimeInstalled`'s `args[0]` rather than `never`, for a slightly stronger type signal at the call boundary — no plan text mandated this, a minor implementation choice within Task 1's scope.
- Chose `winetricksInstall` (not another 34.5-12-owned channel) as the new Invariant B canary because it is deferred to a whole separate phase (34.6, D-03) rather than merely a later wave of this same phase — this is the last time this fix needs to happen inside Phase 34.5.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `getLegendaryVersion` becoming real broke 5 pre-existing "Invariant B" guard tests**
- **Found during:** Task 2 (full backend suite run after adding the new test file)
- **Issue:** `bootstrap.test.ts`, `settingsFlows.test.ts`, `installFlows.test.ts`, `gameDetailsFlows.test.ts`, and `enrichmentFlows.test.ts` each had a test proving "at least one channel remains genuinely unported and rejects non-fatally with `UNPORTED_CHANNEL_MARKER`, and the RPC loop keeps serving" — all five used `getLegendaryVersion` as that stand-in channel (itself the result of an established rotation: `checkDiskSpace` → `getLegendaryVersion`, documented in each file's own comments from Phases 32/34.3). Task 1 legitimately ported `getLegendaryVersion` for real, so invoking it through these tests' lighter bootstrap harnesses (which don't fully wire `libraryManagerMap['legendary']`) now throws `Cannot read properties of undefined (reading 'runRunnerCommand')` / `libraryManagerMap.legendary.runRunnerCommand is not a function` instead of the expected marker.
- **Fix:** Swapped the canary channel in all 5 files from `getLegendaryVersion` to `winetricksInstall` (permanently deferred to Phase 34.6 by D-03, confirmed unregistered anywhere in the current sidecar), updating each file's own explanatory comment following the exact "readConfig"/"getUserInfo" precedent plan 34.5-06 already established for this identical situation.
- **Files modified:** `src/backend/sidecar/__tests__/bootstrap.test.ts`, `settingsFlows.test.ts`, `installFlows.test.ts`, `gameDetailsFlows.test.ts`, `enrichmentFlows.test.ts`
- **Verification:** Full backend suite re-run after the fix: 127/127 suites, 2657/2657 tests passing. `npx tsc --noEmit` exit 0.
- **Committed in:** `7be719ce9` (part of Task 2's commit)

---

**Total deviations:** 1 auto-fixed (Rule 1)
**Impact on plan:** Necessary correctness fix directly caused by this plan's own port — no scope creep. No architectural change, no new dependency.

## Issues Encountered
None beyond the deviation above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Plan 34.5-12 can now fill in the remaining 5 channels (`callTool`, `egsSync`, `getGOGLinuxInstallersLangs`, `syncSaves`, `syncGOGSaves`) in `runnerMiscFlowRegistration.ts` — the forward-looking "not yet registered" pin in `runnerMiscFlows.test.ts` will need those 5 assertions flipped to positive (per this plan's own instructions, not deleted). No blockers for that plan. Full backend suite recorded verbatim: 127/127 suites, 2657/2657 tests passing (not rounded to green). `npx tsc --noEmit` exit 0.

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-07-29*

## Self-Check: PASSED

All created/modified files verified present on disk; all task commit hashes (`6c877ba23`, `7be719ce9`) and the metadata commit hash (`4e9da72f0`) verified present in `git log`.
