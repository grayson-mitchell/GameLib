---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 22
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-FINDINGS.md
autonomous: true
gap_closure: true
requirements: [REQ-34.5-04, REQ-34.5-12]

must_haves:
  truths:
    - "The 2026-08-01 gate-run log is preserved off the rotation path before any rebuild destroys it"
    - "A written finding states which of the three gap shapes (missing / still-stubbed / guarded-off consume path) F-34.5-G6-02 actually is, decided from source, not assumed"
    - "The finding names the exact mechanism with file:line citations, and names the falsifying evidence that would overturn it"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-FINDINGS.md"
      provides: "The F-34.5-G6-02 root-cause finding and its evidence, redacted"
      contains: "LONG_RUNNING_CHANNELS"
  key_links:
    - from: "34.5-G6-FINDINGS.md"
      to: "src-tauri/src/main.rs INVOKE_TIMEOUT"
      via: "cited mechanism"
      pattern: "INVOKE_TIMEOUT"
---

<objective>
Diagnose F-34.5-G6-02 ("GOG and Amazon reach `status=captured` at the backend but nothing consumes
the capture") to a named mechanism, from source and from the preserved live-gate log, BEFORE any
fix is planned or written.

Purpose: this project has already paid for a gap analysis that enumerated what was MISSING and
never questioned what was PRESENT (Phase 34.4.1, one wasted cycle). The consume path for a captured
OAuth code was shipped by Phase 34.4.1 plan 09 and is PRESENT in
`src/frontend/screens/WebView/useTauriOAuthLogin.ts`. Whether the real gap is (a) a missing consume
path, (b) a present-but-still-stubbed one, or (c) one that exists but is guarded off is three
different fixes. This plan decides which, and records the decision with citations, so plan 34.5-23
fixes a diagnosed defect rather than a guessed one.

Output: `34.5-G6-FINDINGS.md`, plus a preserved copy of the gate-run log outside the repository.

**NO CODE CHANGES IN THIS PLAN.** Not one line of `.ts`, `.tsx` or `.rs`. If the investigation
suggests a fix, it is written down here, not applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
@.claude/skills/spike-findings-gamelib/SKILL.md

**Codebase orientation is MANDATORY before reading raw source.** `graphify-out/graph.json` exists.
Run `graphify explain "useTauriOAuthLogin"`, `graphify explain "oauthLoginCapture"` and
`graphify query "OAuth capture consumed by the renderer"` FIRST, then read only the files those
results name.

<interfaces>
Contracts the executor needs; extracted from the codebase at planning time so no exploration is
required to understand them.

From `src/frontend/screens/WebView/useTauriOAuthLogin.ts`:
- `export type TauriOAuthLoginState = { phase: 'idle' } | { phase: 'awaiting' } | { phase: 'blocked'; runner: OAuthRunner; channel: string } | { phase: 'cancelled' } | { phase: 'timeout' } | { phase: 'error'; message: string }`
- `export function useTauriOAuthLogin(runner: OAuthRunner | undefined): TauriOAuthLoginState`
- Line 99-102: `const outcome: OAuthCaptureOutcome = await window.api.oauthCaptureLogin({ runner: activeRunner, url })` — this `await` is NOT inside any `try`/`catch`.
- Lines 133-172: on `outcome.status === 'captured'` the hook calls `window.api.login` / `authGOG` / `authAmazon` / `authZoom`, and logs `[useTauriOAuthLogin] runner=... phase=idle (channel resolved)` when the call resolves, or `phase=blocked channel=...` when it rejects with `UNPORTED_CHANNEL_MARKER`.

From `src/backend/sidecar/oauthLoginCapture.ts`:
- `const DEFAULT_DEADLINE_MS = 300_000` (line 62)
- `export function captureOAuthLogin(runner, loginUrl, options?): Promise<OAuthCaptureOutcome>` — never rejects; resolves a typed outcome.

From `src-tauri/src/main.rs`:
- `const INVOKE_TIMEOUT: Duration = Duration::from_secs(60);` (line 104)
- `const LONG_RUNNING_CHANNELS: &[&str] = &[ "install", "updateGame", "uninstall", "checkGameUpdates", "refreshLibrary", "getCrossoverIndex", "repair", "readConfig" ];` (lines 134-145)
- `fn timeout_for(channel: &str) -> Option<Duration>` — `None` for a member of that list, `Some(INVOKE_TIMEOUT)` otherwise (line 148)
- `SidecarState::invoke` — on `rx.recv_timeout(bound)` expiry returns `Err("sidecar invoke timed out".into())`, removes the pending entry, and the late real response is subsequently dropped by the reader thread as an unknown id.

From `src/frontend/screens/WebView/index.tsx`:
- Line 74: `const oauthLoginState = useTauriOAuthLogin(runner as OAuthRunner | undefined)`
- Line 472: `return <TauriLoginPanel runner={runner} state={oauthLoginState} />`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve the 2026-08-01 gate-run log off the rotation path</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` § preconditions 3 and 4 (which state the
      log rotates to `gamelib.log.old` on every process start)
    - `ls -la ~/Library/Logs/GameLib/` to confirm the current file's size and mtime before copying
  </read_first>
  <action>
    `~/Library/Logs/GameLib/gamelib.log` currently holds the complete 2026-08-01 blocking gate
    re-run (mtime 11:15, ~351 KB). It is the ONLY primary evidence for findings F-34.5-G6-01..06 and
    it is DESTROYED the next time `pnpm tauri:dev` starts — every later plan in this cycle rebuilds
    and relaunches, so it must be preserved before any of them run.

    Copy it, preserving mtime, to `~/Library/Logs/GameLib/gamelib.log.34.5-g6-gate2` — a path
    OUTSIDE the repository. Then `cmp` the copy against the original to prove byte-identity and
    record both the `cmp` exit code and both files' `wc -c` output in the SUMMARY.

    **This file must never be committed and must never be copied into the repository.** It contains
    live session state from a real Epic/GOG/Amazon login session. Per T-34.5-G6-02 (the
    evidence-handling rule carried from `34.5-LIVE-GATE-RERUN.md`), only individually quoted,
    redacted lines may enter a repository document, and never an `access_token`, `refresh_token`,
    cookie value, session identifier, or the Steam `userdata` account id. Verify with
    `git status --porcelain` that the copy did not land under the repo tree.

    Also copy `~/Library/Logs/GameLib/gamelib.log.old` to
    `~/Library/Logs/GameLib/gamelib.log.old.34.5-g6-gate2` by the same procedure — it holds the
    pre-launch session and is equally destroyed by the next rotation.
  </action>
  <verify>
    <automated>test -f ~/Library/Logs/GameLib/gamelib.log.34.5-g6-gate2 &amp;&amp; cmp ~/Library/Logs/GameLib/gamelib.log ~/Library/Logs/GameLib/gamelib.log.34.5-g6-gate2 &amp;&amp; git -C . status --porcelain | grep -c "gamelib.log" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `~/Library/Logs/GameLib/gamelib.log.34.5-g6-gate2` exists and `cmp` against the original exits 0.
    - `~/Library/Logs/GameLib/gamelib.log.old.34.5-g6-gate2` exists and `cmp` against `gamelib.log.old` exits 0.
    - `git status --porcelain` shows NO path matching `gamelib.log` — the preserved copies are outside the repository working tree.
    - The SUMMARY records both files' byte counts verbatim.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Decide which of the three gap shapes F-34.5-G6-02 is, from source</name>
  <read_first>
    - `src/frontend/screens/WebView/useTauriOAuthLogin.ts` (whole file — this is the alleged-missing consume path)
    - `src/frontend/screens/WebView/index.tsx` lines 60-90 and 460-480 (where the hook is mounted and its state handed to the panel)
    - `src/frontend/screens/WebView/components/TauriLoginPanel.tsx` lines 175-215 (the `captured-blocked` branch and the `declared-blocked` default)
    - `src/backend/sidecar/oauthLoginCapture.ts` lines 55-70 and 143-256 (`DEFAULT_DEADLINE_MS`, `captureOAuthLogin`)
    - `src/backend/sidecar/oauthLoginFlowRegistration.ts` (whole file — the handler registration)
    - `src-tauri/src/main.rs` lines 100-160 (`INVOKE_TIMEOUT`, `LONG_RUNNING_CHANNELS`, `timeout_for`) and lines 280-345 (`SidecarState::invoke`, `sidecar_invoke`)
    - `src/preload/tauriTransport.ts` lines 88-105 (`invoke`)
    - Run `graphify explain "useTauriOAuthLogin"` and `graphify explain "oauthLoginCapture"` BEFORE opening any of the above
  </read_first>
  <action>
    Answer, from the source above and from the preserved log, exactly ONE of these three, and say
    which in plain words:

    (a) the consume path is MISSING — nothing calls `login`/`authGOG`/`authAmazon` after a capture;
    (b) the consume path is PRESENT BUT STILL STUBBED — Phase 34.5 was supposed to replace a
        34.4.1-era stub and did not;
    (c) the consume path EXISTS and is correct, but the captured outcome never reaches it.

    Gather the following evidence, each of which is a source assertion or a grep over the PRESERVED
    log (`~/Library/Logs/GameLib/gamelib.log.34.5-g6-gate2`), never over a fresh log:

    1. `grep -c "useTauriOAuthLogin" ~/Library/Logs/GameLib/gamelib.log.34.5-g6-gate2` — the hook
       logs on EVERY terminal outcome (`phase=cancelled`, `phase=timeout`, `phase=error`,
       `phase=blocked`, `phase=idle (channel resolved)`). Record the count.
    2. `grep -c "oauthLoginCapture. runner=.* status=" ~/Library/Logs/GameLib/gamelib.log.34.5-g6-gate2`
       — the number of BACKEND terminal outcomes in the same session. Record the count.
    3. The per-attempt wall-clock durations, computed from the `label=` open line and the matching
       `status=` line for each of `runner=legendary`, `runner=gog`, `runner=nile`. Record each in
       seconds.
    4. Whether `oauthCaptureLogin` appears in `LONG_RUNNING_CHANNELS` in `src-tauri/src/main.rs`.
       Quote the array verbatim.
    5. Whether the `await window.api.oauthCaptureLogin(...)` at `useTauriOAuthLogin.ts:99` is inside
       a `try`/`catch`. Quote the enclosing lines.

    **Do not stop at the first reading that fits.** Two readings both fit the symptom "UI stuck on
    Signing in…": (R-A) the transport rejected at its bound and the rejection was swallowed as an
    unhandled promise rejection, and (R-B) the promise simply never settled at all. Name BOTH in the
    document, state which one the evidence selects and by which specific observation, and state what
    observation would falsify the selected one. If the evidence does NOT discriminate between them,
    say so and name the additional observation that would — do not pick the nicer reading.

    Note explicitly whether the same mechanism also explains item 1 (Epic): Epic's three backend
    outcomes were `status=timeout` at 300s each, which is also past any 60s bound — so the hook
    would have lost those too. Record whether F-34.5-G6-01 ("greyed out form") is a SEPARATE defect
    upstream of this one, or the same one; if the evidence cannot tell, say that.

    Count the recurrence, per the standing lesson that a finding recurring across cycles must be
    COUNTED before it is scoped: list EVERY channel registered by this phase whose handler can run
    longer than 60 s of wall clock (start from `oauthCaptureLogin`'s 300 000 ms deadline and check
    the other long-poll channels in `src/backend/sidecar/*FlowRegistration.ts`). A shared bound's
    defect hits every caller — a fix scoped to one channel when four are affected is the Phase 34.2
    failure mode.
  </action>
  <verify>
    <automated>grep -q "oauthCaptureLogin" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-FINDINGS.md &amp;&amp; grep -q "INVOKE_TIMEOUT" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-G6-FINDINGS.md` exists with frontmatter `phase`, `plan: 22`, `type: finding`, `finding: F-34.5-G6-02`, `status: diagnosed`.
    - It states (a), (b) or (c) in one explicit sentence, with the file:line citation that decides it.
    - It records all five numbered evidence items above, each with the literal command run and its literal output.
    - It names BOTH readings R-A and R-B, states which the evidence selects, states the falsifying observation for the selected one, or states plainly that the evidence does not discriminate.
    - It records whether F-34.5-G6-01 is the same defect or a separate one, or states that the evidence cannot tell.
    - It lists every channel whose handler can exceed 60 s wall clock, with a count.
    - No `access_token`, `refresh_token`, cookie value, session identifier, or Steam `userdata` account id appears anywhere in the file — verified by grepping the file for `token=`, `code=`, `Cookie`, `userdata` before committing.
    - `git diff --stat` for this task shows ONLY `34.5-G6-FINDINGS.md`. Zero `.ts`, `.tsx`, `.rs` files changed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Record the fix the finding implies, without applying it</name>
  <read_first>
    - The `34.5-G6-FINDINGS.md` written by Task 2
    - `src/backend/__tests__/longRunningChannels.test.ts` lines 30-60 and 260-300 (`EXPECTED_LONG_RUNNING_CHANNELS`, and the existing drift gate that pins the Rust array against this list)
  </read_first>
  <action>
    Append a `## Implied fix (NOT applied by this plan)` section to `34.5-G6-FINDINGS.md` naming
    each concrete change plan 34.5-23 will make, as a checklist a reader can verify was honoured:

    - the exact edit to `src-tauri/src/main.rs`'s `LONG_RUNNING_CHANNELS`, quoting the array as it
      must read afterward;
    - the matching edit to `EXPECTED_LONG_RUNNING_CHANNELS` in
      `src/backend/__tests__/longRunningChannels.test.ts` (that suite pins the two lists against
      each other, so a one-sided edit turns it red — say so explicitly);
    - the exact `try`/`catch` boundary to add around `useTauriOAuthLogin.ts:99`, and the greppable
      log line its catch must emit;
    - a named STANDING GUARD: a test asserting that a channel whose sidecar handler carries its own
      deadline longer than `INVOKE_TIMEOUT` must appear in `LONG_RUNNING_CHANNELS`, so this class of
      defect cannot recur silently.

    For each item, state the OBSERVABLE downstream effect that proves it worked — never the return
    value of the changed code itself. A green test suite is not one of these: three fully green
    suites (3279, 3387, 3447 tests) coexisted with every blocking defect this phase has found, so
    "tests pass" may not appear as evidence that a live behaviour works.

    Do not modify any source file. This section is a specification for the next plan.
  </action>
  <verify>
    <automated>grep -q "Implied fix (NOT applied by this plan)" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-FINDINGS.md &amp;&amp; git diff --name-only HEAD | grep -Ec '\.(ts|tsx|rs)$' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The `## Implied fix (NOT applied by this plan)` section names all four items above with concrete identifiers (file paths, array contents, line numbers, log-line strings).
    - Each item states an observable downstream effect, and none of those effects is "the test suite is green".
    - `git diff --name-only HEAD` matches zero files with a `.ts`, `.tsx` or `.rs` extension.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live gate log → repository document | Session log holds real OAuth session artefacts; anything quoted into a public-fork repo file crosses from private to public |
| preserved log file → git working tree | An accidental `cp` into the repo would commit credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-G6-02 | Information disclosure | `34.5-G6-FINDINGS.md` quoting `gamelib.log` | mitigate | Task 2's acceptance criteria require grepping the finished file for `token=`, `code=`, `Cookie`, `userdata` before commit; only named log lines may be quoted, redacted in place |
| T-34.5-G6-07 | Information disclosure | preserved log copy | mitigate | Task 1 writes the copy to `~/Library/Logs/GameLib/`, outside the repo, and asserts `git status --porcelain` shows no `gamelib.log` path |
| T-34.5-G6-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. Any install attempt is out of scope and must stop the plan; `slopcheck` plus a blocking human checkpoint would be required first |
</threat_model>

<verification>
- `34.5-G6-FINDINGS.md` exists, is committed, and names a single mechanism with file:line citations.
- Both preserved log copies exist outside the repo and are byte-identical to their originals.
- Zero source files changed by this plan.
</verification>

<success_criteria>
F-34.5-G6-02 is diagnosed to one of three named shapes with cited evidence, the competing reading is
named rather than silently discarded, the recurrence count across channels is recorded, and the
primary evidence survives the next rebuild.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-22-SUMMARY.md` when done
</output>
