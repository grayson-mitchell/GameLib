---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 21
subsystem: docs
tags: [tauri, ipc, oauth, live-gate, propagation]

# Dependency graph
requires:
  - phase: 34.5-20
    provides: "34.5-LIVE-GATE-RERUN.md's filled Result slots and Verdict table — the sole source of truth this plan propagates from"
provides:
  - "34.5-PORTED-CHANNELS.md with all 10 gate-dependent LIVE cells reflecting the RE-RUN's recorded result"
  - "deferred-items.md scoped with the six new downstream/usability findings for the next gap cycle"
  - "IPC-PORT-INVENTORY.md and ROADMAP.md agreeing with the RE-RUN's verdict and routing"
  - "STATE.md hand-corrected with the RE-RUN result and the exact next step"
affects: ["34.5-gap-cycle-2", "phase-34.5-closure"]

# Tech tracking
tech-stack:
  added: []
  patterns: ["propagate-a-live-gate-verdict-into-every-tracking-document-without-softening-a-FAIL"]

key-files:
  created:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-21-SUMMARY.md
  modified:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
    - .planning/IPC-PORT-INVENTORY.md
    - .planning/ROADMAP.md
    - .planning/STATE.md

key-decisions:
  - "Assumption A1 (www.amazon.com anchor) is recorded CONFIRMED at the mechanism level (structural proof from matchOAuthRedirect's own code plus a zero-count origin-mismatch grep), but the broader standing claim stays open because item 3 as a whole did not PASS — a sub-clause confirmation never upgrades a FAILing item."
  - "Added a new R-34.5-G1-PKG residual entry naming the packaged Tauri build's asset root as still unresolved, with its future home explicitly named as the packaging work, not Phase 34.6's channel port."
  - "Phase 34.6 and Phase 35's ROADMAP dependency lines were left untouched since the gate did not PASS 5/5 — only a clean 5/5 result would have unblocked them."

requirements-completed: [REQ-34.5-11, REQ-34.5-12, REQ-34.5-13]

# Metrics
duration: ~55min (continuation agent; killed predecessor's uncommitted Task 1 work audited, completed and committed first)
completed: 2026-08-01
---

# Phase 34.5 Plan 21: Propagate the live-gate RE-RUN verdict everywhere it is tracked

**Every document that recorded the first FAILED gate now also records the RE-RUN's second FAIL (0 of 5 clean, 3 FAIL / 2 NOT ATTEMPTED) — the phase still does not close, and the next step is named exactly once as another gap cycle.**

## Performance

- **Duration:** ~55 min (continuation agent — a prior executor was killed mid-Task-1 with uncommitted edits sitting in the working tree; this session audited, completed, and committed that work rather than restarting)
- **Completed:** 2026-08-01
- **Tasks:** 3/3
- **Files modified:** 5 (`34.5-PORTED-CHANNELS.md`, `deferred-items.md`, `IPC-PORT-INVENTORY.md`, `ROADMAP.md`, `STATE.md`)

## Accomplishments

- Audited a killed predecessor agent's uncommitted `34.5-PORTED-CHANNELS.md` edits: all 10 gate-dependent `LIVE (item N)` cells were already correctly flipped to the RE-RUN's result (items 1/2/3 FAIL, items 4/5 PENDING), and the header/proof-boundary prose correctly distinguished the superseded first FAIL from the RE-RUN's second FAIL. That work was kept as-is.
- Found the predecessor's work incomplete on the `## Accepted residuals` section — no entry existed for research Pitfall 2's non-Steam Wine claim or for the `R-34.5-G1-PKG` packaged-asset-root residual, and Assumption A1's entry still read "UNTESTED" (stale — the RE-RUN confirmed the mechanism). Finished all four residual entries with the RE-RUN's actual findings.
- Appended six new findings (`F-34.5-G6-01`..`06`, items 6-11) plus a `R-34.5-G1-PKG` entry to `deferred-items.md`, without renumbering the five pre-existing items.
- Added a status paragraph to `IPC-PORT-INVENTORY.md`'s Phase 34.5 section naming both live-gate runs and pointing at `34.5-LIVE-GATE-RERUN.md` as evidence, without touching channel membership or the 38/3/16 split.
- Marked all six gap-cycle plans (`34.5-16`..`34.5-21`) done in `ROADMAP.md`, annotated `34.5-20`'s entry with the RE-RUN's verdict, and replaced the Phase 34.5 status paragraph with the RE-RUN's actual outcome — the phase still does not close, so Phase 34.6/35's dependency lines were left untouched.
- Hand-edited `STATE.md`'s frontmatter and `## Current Position` block (never via `gsd-sdk` state writes, per the standing corruption finding), recounting plan completion from the phase directory (21 plans, 21 summaries after this one) rather than incrementing blindly, and naming the next step as exactly `/gsd-plan-phase 34.5 --gaps`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Propagate the verdict into the declared channel list and its residuals** - `1cc01cc1f` (docs)
2. **Task 2: Reconcile the inventory and the ROADMAP Phase 34.5 block** - `fa9c2a037` (docs)
3. **Task 3: Update STATE.md by hand and name the next step** - `90e355601` (docs)

**Plan metadata:** committed separately below (this SUMMARY + the state-tracking files it touches).

## Files Created/Modified

- `.planning/phases/34.5-.../34.5-PORTED-CHANNELS.md` - All 10 gate-dependent LIVE cells flipped to the RE-RUN's result; header/proof-boundary sections rewritten to describe both runs; `## Accepted residuals` completed with A1 (confirmed mechanism, claim still open), A2 (unchanged, item 4 not attempted either run), a new Pitfall-2 entry (unretired), and a new `R-34.5-G1-PKG` entry.
- `.planning/phases/34.5-.../deferred-items.md` - Appended items 6-12: the RE-RUN's six new findings (F-34.5-G6-01..06) plus `R-34.5-G1-PKG`'s future home, under a new dated section; five pre-existing items untouched.
- `.planning/IPC-PORT-INVENTORY.md` - New status paragraph in the Phase 34.5 section naming both gate runs and the RE-RUN's verdict; channel membership and the 38/3/16 arithmetic unchanged.
- `.planning/ROADMAP.md` - All six gap-cycle plan checkboxes marked `[x]`; `34.5-20`'s entry annotated with its verdict; the Phase 34.5 status paragraph replaced with the RE-RUN's actual outcome and routing; both dated correction notes and the Phase 34.6/35 dependency lines left untouched.
- `.planning/STATE.md` - Frontmatter (`stopped_at`, `last_updated`, `last_activity`, `completed_plans: 162`, `percent: 100`) and the `## Current Position` ACTIVE BLOCKER block rewritten by hand; next action named exactly once as `/gsd-plan-phase 34.5 --gaps`; no other historical block touched (verified via `git diff`, which shows exactly two hunks).

## Decisions Made

- Assumption A1's mechanism-level confirmation (structural proof from `matchOAuthRedirect`'s code, zero-count `origin-mismatch` grep) does not retire the broader standing claim, because the claim's own eligibility rule requires a full item-3 PASS, and item 3 FAILed on the library half. Recorded this distinction explicitly everywhere A1 is mentioned, so a future reader cannot conflate "sub-clause confirmed" with "claim retired."
- `R-34.5-G1-PKG`'s future home is named as "the packaging work" rather than Phase 34.6, since it is a build/bundle-layout question (no additional IPC channel involved) and Phase 34.6 scopes exactly 16 named channels (EOS overlay, SteamGridDB, winetricks) that do not include it.
- Left Phase 34.6's and Phase 35's `ROADMAP.md` dependency lines untouched since the gate result was FAIL, not a clean 5/5 — per the plan's own acceptance criteria, those lines are only eligible for editing on a PASS.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Completed the predecessor's incomplete `## Accepted residuals` update**
- **Found during:** Task 1, auditing the killed predecessor agent's uncommitted work
- **Issue:** The predecessor correctly flipped all 10 gate-dependent proof-level cells and rewrote the header/proof-boundary sections, but never touched `## Accepted residuals` — Assumption A1 still read "UNTESTED" (stale, contradicted by the RE-RUN's CONFIRMED finding), and no entries existed for research Pitfall 2's non-Steam Wine claim or the `R-34.5-G1-PKG` packaged-asset-root residual, both explicitly required by this plan's own action and acceptance criteria.
- **Fix:** Rewrote A1's entry to distinguish the confirmed mechanism from the still-open broader claim; updated A2's entry to note item 4 was not attempted in either run; added new entries for Pitfall 2 (unretired, item 5 not attempted this run either) and `R-34.5-G1-PKG` (dev-only fix, packaged branch still open, future home named).
- **Files modified:** `34.5-PORTED-CHANNELS.md`
- **Verification:** `python3 ported-channels-gate.py` and `--self-test` both exit 0 after the edit; all four required literal markers (`ctrl+r`, `T-34.4.1-47`, `Assumption A1`, `Assumption A2`) still present.
- **Committed in:** `1cc01cc1f` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical — completing a required section a killed predecessor never reached)
**Impact on plan:** Necessary to satisfy the plan's own acceptance criteria for Task 1 (residuals section must carry entries for A1, A2, Pitfall 2, and `R-34.5-G1-PKG`). No scope creep — every addition was explicitly named in the plan's action.

## Issues Encountered

A prior executor agent was killed by an API error mid-Task-1, before committing anything, leaving `34.5-PORTED-CHANNELS.md` with uncommitted partial edits in the working tree. This session began by running `git diff` on the file per the orchestrator's briefing, confirmed the partial work was substantively correct in direction (accurately distinguished the FAILED first run from the FAILED re-run, recorded FAIL plainly, did not soften), identified the specific gap (the residuals section), completed it, and committed immediately — consistent with the standing lesson that uncommitted work under this phase has already been lost twice.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 34.5 does **not** close. All 21 plans across the base plan set and two gap cycles are now complete, each with a SUMMARY, but the blocking live gate has FAILED twice (0/5, then 0/5 again on a different, downstream defect family). Per D-08's no-partial-pass rule, the next step is `/gsd-plan-phase 34.5 --gaps` to scope a third gap cycle against:

- The downstream-of-capture defect family (F-34.5-G6-01/02/03 in `deferred-items.md`): Epic's login form never becomes interactive; GOG/Amazon's successful backend OAuth captures are never consumed into a completed, UI-visible, library-populated login.
- Three usability/security defects surfaced by the developer's own reports (F-34.5-G6-04/05/06): no URL/origin shown in the login window (phishing-resistance concern), black-on-black text in Amazon's verification-code field, and a double Keychain prompt on GOG sign-out.
- `R-34.5-G1-PKG`, the packaged Tauri build's asset root, remains an open residual independent of this gap cycle's outcome — its resolution belongs to whichever future work first exercises a packaged (non-dev) build.

Phase 34.6 (EOS overlay, SteamGridDB, winetricks) and Phase 35 (Electron cutover) remain correctly blocked on Phase 34.5's closure in `ROADMAP.md` — their dependency lines were not touched by this plan, since the gate did not PASS 5/5.

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-08-01*

## Self-Check: PASSED

- FOUND: `.planning/phases/34.5-.../34.5-21-SUMMARY.md`
- FOUND commit `1cc01cc1f` (Task 1)
- FOUND commit `fa9c2a037` (Task 2)
- FOUND commit `90e355601` (Task 3)
