---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 58
type: execute
wave: 4
gap_closure: true
cycle: 7
depends_on: ["34.5-56", "34.5-57"]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-KEYRING-ARM-SESSION.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
autonomous: false
requirements: [REQ-34.5-12, REQ-34.5-13]
blocking: false
targets: [U-34.5-01, U-34.5-10]

must_haves:
  truths:
    - "A real Steam or Humble login/logout runs on the real `keyring` arm, with the operator present and the screen unlocked."
    - "The session's outcome — whatever it is — cannot change the blocking gate's required 4/0/0/0."
    - "The NEW failure class `keyring:unavailable ... In dark wake, no UI possible` is recorded as distinct from every prior `keyring:timeout`, with the screen-unlocked requirement stated as a precondition rather than discovered again."
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-KEYRING-ARM-SESSION.md"
      provides: "The session record: preconditions, the four-part bar's per-condition result, the prompt count per slot, and the failure-class note"
      contains: "dark wake"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md"
      provides: "U-34.5-01 and U-34.5-10 updated on their own named observations"
      contains: "U-34.5-10"
  key_links:
    - from: "34.5-KEYRING-ARM-SESSION.md"
      to: "U-34.5-01's four-part mechanical bar"
      via: "one recorded result per numbered condition, from the same session's gamelib.log"
      pattern: "SidecarKeyringSlotStore"
    - from: "this session's teardown"
      to: "plan 34.5-59's launch 1"
      via: "an asserted-empty pgrep before the gate begins"
      pattern: "pgrep -f 'target/debug/gamelib-shell'"
---

<objective>
Close routing item 7 — `U-34.5-01` and `U-34.5-10` — in a **separate, non-blocking session**, per
D-CYCLE7-C.

Purpose: no gate run in this phase's history has ever met the keyring arm's own condition (4). The
fourth run's actual operator session had ZERO keyring activity of any kind. Meanwhile Keychain
prompts have been a gate confounder for three cycles running.

Output: a dedicated session record, and both ledger rows updated on their own named observations.
</objective>

<binding_decision>
**D-CYCLE7-C (operator, 2026-08-13) — LOCKED.** Routing item 7 must NOT be folded into the blocking
gate. It runs as its own **non-blocking** plan: a dedicated Steam-or-Humble login/logout run on the
real `keyring` arm — i.e. **without** `GAMELIB_DEV_SECRET_VAULT=1` — operator present, **screen
unlocked**.

Keychain prompts have confounded the gate for three cycles and stay out of the blocking gate.

This plan must also document the NEW failure class this cycle surfaced —
`keyring:unavailable ... In dark wake, no UI possible` — as **distinct from every prior
`keyring:timeout`**, and state that machine-awake is not sufficient: **the screen must be
unlocked.**
</binding_decision>

<non_blocking_statement>
Restated so it cannot be misread by any downstream reader: **this session's result, in either
direction, has NO effect on plan 34.5-59's required 4 PASS / 0 FAIL / 0 BLOCKED / 0 NOT ATTEMPTED.**
It scores no gate item, it appears in no verdict table, and a FAIL here does not route Phase 34.5
to gap cycle 8 by itself. It exists to close two standing ledger rows that no gate item exercises.

It nevertheless has one hard obligation TOWARD the gate: this session must be **completely torn
down** before plan 34.5-59's first launch, asserted by an empty
`pgrep -f 'target/debug/gamelib-shell'`. A stale second instance splits the `[shell]` sink while
leaving `gamelib.log` shared — a capture-integrity failure that produces the most dangerous evidence
shape this project has measured: a populated, plausible-looking log next to a transcript missing
every scored literal.
</non_blocking_statement>

<context>
@.planning/PROJECT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md

<interfaces>
Confirmed at planning time:
- `src/backend/sidecar/bootstrap.ts:460` — `logInfo('[bootstrap] secret stores: dev-vault', LogPrefix.Backend)`
- `src/backend/sidecar/bootstrap.ts:471` — `logInfo('[bootstrap] secret stores: keyring', LogPrefix.Backend)`
  These are the two arms. A line ending `dev-vault` proves NOTHING about the Keychain path — it is
  not weaker evidence, it is NO evidence.
- `devSecretVault.ts` enables ONLY on an exact-string `=== '1'` match. `0`, `false`, unset, or any
  other value does not enable it.
- `src-tauri/src/main.rs:2816` / `:2845` — `keyring:unavailable:{e}` and `keyring:timeout` are
  distinct, separately-classified Rust-side rejections. `bounded_keyring_read` produces the timeout.
- `KEYRING_READ_TIMEOUT` was raised 8s -> 45s (`src-tauri/src/main.rs`) and
  `KEYRING_FAILURE_MEMO_MS` 15s -> 120s (`src/backend/sidecar/keyringTokenStore.ts`) by plan
  34.5-35. Both constants are live-observed; the claim they were raised to support is not.
- Real slots: `steam-refresh-token`, `humble-session`, `humble-csrf`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the session record with its preconditions and its two per-row bars, before running</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-KEYRING-ARM-SESSION.md</files>
  <read_first>
    - .planning/phases/.../34.5-UNTESTED-ITEMS.md rows `U-34.5-01` and `U-34.5-10` in full, plus the
      "Interaction with plan 34.5-35" section at the end of that file (the binding statement about
      which arm can serve as evidence for which row)
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md Section 3
      (the same capture standard applies here — this is a live session and its evidence must survive)
    - src/backend/sidecar/bootstrap.ts lines 440-480
  </read_first>
  <action>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-KEYRING-ARM-SESSION.md`
with `status: authored` and every RESULT slot EMPTY, before any live work begins — the same
discipline every gate contract in this phase follows.

**Frontmatter:** `phase`, `plan: 58`, `type: live-session`, `blocking: false`, `cycle: 7`,
`status: authored`, `created: 2026-08-13`, `targets: [U-34.5-01, U-34.5-10]`,
`result: null`, and an explicit `affects_gate: false`.

**Preconditions**, numbered, ticked in order:
1. `pgrep -f 'target/debug/gamelib-shell'` is EMPTY. Kill anything found and re-check.
2. `env | grep GAMELIB_DEV_SECRET_VAULT` produces NO output — the variable is unset, not `0` and
   not `false`. Record the raw command output, including the empty case.
3. **The screen is UNLOCKED and stays unlocked for the entire session.** Not merely the machine
   awake — the display unlocked. State the reason inline: this cycle surfaced
   `keyring:unavailable ... In dark wake, no UI possible`, a NEW failure class distinct from every
   prior `keyring:timeout`, in which the Keychain cannot present its approval UI at all. A session
   run against a locked or sleeping display measures nothing and must be aborted, not scored.
4. Disable any screen-saver or display-sleep timer that could fire during the session, or set it
   beyond the expected session length. Record which.
5. Launch with `pnpm tauri:dev` — never `tauri dev`, which serves a stale static bundle.
6. Session capture per the dual-sink standard: one UTC-named session directory created once;
   `tee -a <session-dir>/terminal.log`; the `=== SESSION LAUNCH ${N} — <UTC> ===` delimiter before
   each launch; `cp ~/Library/Logs/GameLib/gamelib.log <session-dir>/gamelib-launch-${N}.log` before
   the next launch renames it to `.old`; archive any pre-existing `.old` on the first pass; a
   closing `ls -la` plus `wc -l` inventory.
7. Record the single `gamelib-shell` PID after the window appears, and again at teardown.

**The actions**, UI-driven, no DevTools console:
- Drive ONE real login and ONE real logout on a runner whose credentials use the keyring — Steam or
  Humble. Record which was chosen and why.
- Approve the Keychain prompt when it appears. Record how many prompts appeared, per slot.

**The two bars, stated separately so neither can absorb the other:**

`U-34.5-01` — all FOUR of its own mechanical conditions, each recorded independently against the
same session's archived log:
  (1) `GAMELIB_DEV_SECRET_VAULT` was unset for that run;
  (2) a `[bootstrap] secret stores: keyring` line is present;
  (3) no `[dev-secret-vault]` line appears anywhere in the log;
  (4) at least one `SidecarKeyringSlotStore` read SUCCEEDS for a real slot.
  Condition (4) is the one that has never held. Record its result with the exact grep and its raw
  output, including the empty case. **A partial pass on (1)(2)(3) with (4) failing is exactly what
  gate 3 recorded, and it retires nothing.**

`U-34.5-10` — its own separate observation: only ONE macOS Keychain approval prompt appears for a
given slot across a full login flow that previously triggered two, AND `gamelib.log` shows at most
one `keyring_get failed: keyring:timeout` line per slot. Record the prompt count per slot as an
integer, observed by the operator, cross-checked against the log's own read attempts.

**A failure-class section**, authored now rather than after the fact, listing the three distinct
outcomes and how to tell them apart from the log alone:
  - `keyring:timeout` — `bounded_keyring_read` exceeded its bound; the Keychain was reachable but
    the read did not complete in time. This is the class every prior run hit.
  - `keyring:unavailable:...` with `In dark wake, no UI possible` — the Keychain could not present
    approval UI at all. **A precondition failure, not a product result.** If this appears, the
    session is ABORTED and re-run with the screen unlocked; it is not recorded as a FAIL of either
    row.
  - A successful read — the only outcome that can satisfy `U-34.5-01`'s condition (4).
  </action>
  <verify>
    <automated>grep -c "affects_gate: false" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-KEYRING-ARM-SESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists with `status: authored`, `result: null`, `blocking: false`,
      `affects_gate: false`, and every RESULT slot empty.
    - Preconditions 1-7 are present and numbered; precondition 3 contains the literal `dark wake`
      and the phrase requiring the display unlocked, not merely the machine awake.
    - `U-34.5-01`'s four conditions appear as four separately-recordable slots, not one combined
      pass/fail.
    - `U-34.5-10`'s prompt count is specified as an integer per slot.
    - The failure-class section names all three outcomes and states that the dark-wake class ABORTS
      rather than scores.
    - No step requires the DevTools console.
  </acceptance_criteria>
  <done>The session is fully specified before it runs, with its abort condition and its two independent bars written down in advance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the keyring-arm session on real macOS hardware</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-KEYRING-ARM-SESSION.md</files>
  <read_first>
    - .planning/phases/.../34.5-KEYRING-ARM-SESSION.md (the record authored by Task 1)
  </read_first>
  <what-built>
    Plan 34.5-35 raised `KEYRING_READ_TIMEOUT` 8s -> 45s and `KEYRING_FAILURE_MEMO_MS` 15s -> 120s to
    close the keyring read-timeout/human-approval race. Both constants have been observed live; the
    claim they were raised to support — a single successful Keychain approval — never has. Plan
    34.5-36's dev secret vault is the reason most sessions bypass the Keychain entirely.
  </what-built>
  <action>
Drive the session recorded in `34.5-KEYRING-ARM-SESSION.md` on real macOS hardware with the operator
present. This task performs no code change: it executes preconditions 1-7 in order, drives one real
login and one real logout on the keyring arm, and captures the session directory per the dual-sink
standard. The executor collects the operator's per-slot Keychain prompt counts and the archived logs;
scoring happens in Task 3, from the archive, never from the report.
  </action>
  <how-to-verify>
    1. Confirm your screen is UNLOCKED and will stay unlocked. Disable display sleep and the screen
       saver for the duration. This is not a formality: the machine being awake is NOT enough — a
       locked display produces `keyring:unavailable ... In dark wake, no UI possible`, which is a
       precondition failure that measures nothing.
    2. Confirm no GameLib is running: `pgrep -f 'target/debug/gamelib-shell'` must print nothing.
    3. Confirm the dev vault is OFF: `env | grep GAMELIB_DEV_SECRET_VAULT` must print nothing.
    4. Create the session directory and start `pnpm tauri:dev` under `tee -a`, per the record's
       precondition 6.
    5. Sign IN to either Steam or Humble through the real UI. When macOS asks you to allow access to
       the keychain, click Allow. **Count the prompts.** Tell me how many appeared and for which
       item name.
    6. Sign OUT of the same runner through the real UI. Count any further prompts.
    7. Close the app. Confirm `pgrep -f 'target/debug/gamelib-shell'` prints nothing again.
    Report back: which runner you used, how many Keychain prompts appeared in total and per item,
    and whether any prompt was dismissed or timed out rather than approved.
  </how-to-verify>
  <acceptance_criteria>
    - The operator reports the runner used and an integer prompt count per slot.
    - `pgrep -f 'target/debug/gamelib-shell'` is empty before launch and after teardown, both
      recorded.
    - The session directory's closing inventory (`ls -la` plus `wc -l`) is recorded and no capture
      file is zero-length.
    - If `In dark wake, no UI possible` appears anywhere in the archived log, the session is
      recorded ABORTED and re-run — it is not scored.
  </acceptance_criteria>
  <resume-signal>Report the runner, the per-slot prompt counts, and any prompt that was not approved. Type "done" when the app is closed and pgrep is empty.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Score both rows independently from the archived log and update the ledger</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-KEYRING-ARM-SESSION.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</files>
  <read_first>
    - The archived `gamelib-launch-${N}.log` files from Task 2's session directory
    - .planning/phases/.../34.5-UNTESTED-ITEMS.md rows `U-34.5-01`, `U-34.5-10`, and the
      "Interaction with plan 34.5-35" section
  </read_first>
  <action>
Fill every RESULT slot in `34.5-KEYRING-ARM-SESSION.md` from the ARCHIVED log, not from the
operator's report. The operator's report is one input; per this phase's binding evidence rule, a
mutating call's own report — and a human's recollection of a prompt — is never proof of its effect.
Cross-check every claim against the log.

Record, with the exact grep and its raw output for each:
  - `grep -c "\[bootstrap\] secret stores: keyring"` and
    `grep -c "\[bootstrap\] secret stores: dev-vault"` — conditions (1)/(2).
  - `grep -c "\[dev-secret-vault\]"` — condition (3), must be `0`.
  - `grep -n "SidecarKeyringSlotStore\|keyring_get"` — condition (4). Record the FULL output
    including the empty case; gate 4's own resolution recorded "returns nothing at all, not even an
    attempt", and that distinction matters.
  - `grep -c "keyring_get failed: keyring:timeout"` per slot — `U-34.5-10`'s log half.
  - `grep -c "In dark wake, no UI possible"` — must be `0` for a scorable session.

Then update the ledger, per Ledger Rule 1 — each row retires ONLY on the observation named in its
own cell, never by inference from its neighbour, and specifically **`U-34.5-01` and `U-34.5-10` do
not retire together**; their bars are different observations that this single session merely happens
to be able to measure at once.

  - `U-34.5-01`: if all four conditions held, mark **RETIRED 2026-08-13 by plan 34.5-58** and quote
    the successful `SidecarKeyringSlotStore` read line (redacting any token value in place). If
    condition (4) failed again, keep it **OPEN** and record which conditions held and the exact
    failure classification of each read attempt.
  - `U-34.5-10`: if exactly one approval prompt appeared for a slot across the full flow AND at most
    one `keyring:timeout` line exists for that slot, mark **RETIRED 2026-08-13 by plan 34.5-58**.
    Otherwise keep it **OPEN** with the integer counts recorded. Note explicitly whether
    `U-34.5-01`'s outcome differed, and that the two are scored independently.
  - Add a dated update paragraph recording the new running count and which rows moved.

Set the session record's frontmatter `result:` to `PASS` (both rows retired), `PARTIAL` (one
retired), `FAIL` (neither retired, session valid) or `ABORTED` (a precondition failed). **In every
case, restate in the record's own closing line that this outcome does not affect plan 34.5-59's
required 4/0/0/0.**
  </action>
  <verify>
    <automated>grep -c "result: PASS\|result: PARTIAL\|result: FAIL\|result: ABORTED" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-KEYRING-ARM-SESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every RESULT slot in the session record is filled; `result:` is one of the four values.
    - Each of `U-34.5-01`'s four conditions has its own recorded grep and raw output.
    - `U-34.5-10`'s per-slot prompt count and `keyring:timeout` count are both recorded as integers.
    - Both ledger rows have a dated status update, and neither is retired by the other's evidence —
      the cells must state their outcomes independently.
    - `grep -c "In dark wake, no UI possible" <archived log>` is recorded, and if non-zero the
      record's `result:` is `ABORTED`.
    - The record's closing line restates `affects_gate: false` in prose.
    - No token, cookie, session id or account id value appears anywhere in the record; run the
      phase's standing seven-class redaction sweep over the staged diff before committing and paste
      its output.
  </acceptance_criteria>
  <done>Both rows are scored on their own bars from archived evidence, and the record states plainly that it cannot move the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS Keychain -> sidecar | Real secrets cross this boundary; the whole point of the session is to exercise it |
| session log -> committed record | Archived logs may contain token material for the slots being read |
| this session's process -> the gate session | A surviving instance splits the `[shell]` sink of the run that follows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C7-27 | Information disclosure | `steam-refresh-token` / `humble-session` / `humble-csrf` values in the archived log | mitigate | Only the presence and classification of a read is quoted, never a value. The phase's seven-class redaction sweep runs over the staged diff before commit, and any match blocks the commit. Archived logs live in the `/tmp` session directory and are never added to git. |
| T-34.5-C7-28 | Denial of service | the blocking gate that follows | mitigate | Teardown is asserted with an empty `pgrep -f 'target/debug/gamelib-shell'` recorded in this plan and re-asserted as a precondition of plan 34.5-59. A second concurrent instance splits the `[shell]` sink while leaving `gamelib.log` shared — a measured capture-integrity failure. |
| T-34.5-C7-29 | Repudiation | a human-reported prompt count | mitigate | The operator's count is one input, cross-checked against the archived log's own read attempts and their classifications. A report alone is never the score. |
| T-34.5-C7-30 | Spoofing | a dev-vault run presented as keyring evidence | mitigate | Precondition 2 records the raw `env` output, and condition (3) requires zero `[dev-secret-vault]` lines across the whole archived log. A `dev-vault` bootstrap line is NO evidence for either row, not weaker evidence. |
| T-34.5-C7-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package. If that changes, the Package Legitimacy Gate applies and a blocking human checkpoint precedes the install. |
</threat_model>

<verification>
- The session record is authored with empty slots BEFORE the run, and filled only afterwards.
- All four of `U-34.5-01`'s conditions scored independently from the archived log.
- `U-34.5-10` scored on its own prompt-count observation, independently of `U-34.5-01`.
- Teardown `pgrep` empty and recorded.
- Redaction sweep output pasted into the record.
</verification>

<success_criteria>
- The keyring arm is exercised deliberately, once, outside the blocking gate (D-CYCLE7-C).
- The dark-wake failure class is documented as a precondition, not rediscovered.
- Neither row is retired by the other's evidence.
- Nothing about this session can change the gate's arithmetic.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-58-SUMMARY.md` when done.
</output>
