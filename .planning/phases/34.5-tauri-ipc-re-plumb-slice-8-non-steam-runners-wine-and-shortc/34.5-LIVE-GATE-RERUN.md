---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 19
executed_by: 34.5-20-PLAN.md
type: live-gate
status: pending
blocking: true
created: 2026-08-01
completed: null
verdict: null
items_total: 5
items_passed: 0
items_failed: 0
supersedes: 34.5-LIVE-GATE.md
notes: |
  CONTRACT written before any live work (plan 34.5-19, wave 3), per D-08 discipline this document
  inherits unchanged from 34.5-LIVE-GATE.md and from Phase 34.4.1's own three-document gate
  history (34.4.1-LIVE-GATE.md / -RERUN.md / -RERUN-3.md). Every Result slot below is EMPTY. This
  document does not itself close G-2, G-3, G-4 or G-6 -- only a recorded live result on each item
  does that. Execution of this contract (filling the Result slots, computing the Verdict) is
  plan 34.5-20's job, not this plan's.
---

# Phase 34.5 Plan 19 — Blocking Live-Gate RE-RUN (REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-12)

This document is a **CONTRACT**, written before any live work begins, exactly as `34.5-LIVE-GATE.md`
was written before the first run (wave 1, plan 03) and as Phase 34.4.1 did across its three gate
documents. It **supersedes `34.5-LIVE-GATE.md` for all five items** — this re-run re-attempts every
one of them, including item 4 (NOT ATTEMPTED last time).

**`34.5-LIVE-GATE.md` is NOT overwritten and is NOT edited by this plan.** It holds the record of
the superseded FAILED first run (2026-08-01: 0 of 5 PASS, 4 FAIL, 1 NOT ATTEMPTED) and stays exactly
as it was written. Anyone citing that document as evidence of a PASS on any item is citing the wrong
document — see precondition 1 below for the same warning, carried forward, about `34.4.1-LIVE-GATE.md`.

**D-08's no-partial-pass rule applies unchanged to this re-run.** Restated verbatim because it is
still binding: *any item recorded FAIL means this phase does not close — no advisory mode, no
pre-authorized override, no deferred UAT entry, no partial-pass closure.* If this re-run again
records any FAIL, the correct next step is another gap cycle inside Phase 34.5, not a forced close.

**Why this run exists.** The first run (`34.5-LIVE-GATE.md`, plan 34.5-15, 2026-08-01) failed 0/5:
items 1, 2, 3 FAILed on `spawn ./{legendary,gogdl,nile} ENOENT` at sidecar startup (the runner
binaries could not spawn because `publicDir` resolved to a nonexistent `src-tauri/public` — full
diagnosis in that document's "Root cause" section), item 5 FAILed by blockage (it depends on a
working login from items 1-3), and item 4 was NOT ATTEMPTED (the developer's choice, to re-run the
whole gate after the fix rather than partially exercise an item whose neighbours had already failed
for an unrelated reason). The intervening gap-cycle wave (plans 34.5-16, 34.5-17, 34.5-18) closed
the root cause at the code level: a `GAMELIB_APP_ROOT` env-var handoff from the Rust shell now fixes
`publicDir` resolution under the sidecar (34.5-16), the previously-unchecked x64 binary fallback now
throws loudly instead of returning an unchecked path (34.5-17), and the sidecar now logs its own
boot-time asset-root self-check plus the `GAMELIB_SHELL_EXE` value it actually received (34.5-18).
None of that work has been live-proven yet — this document is the contract for doing so.

Additionally, the first run's own precondition 5 was diagnosed as **gate-contract-defective, not
code-defective**: it asked for a value from `gamelib.log` that nothing in the sidecar ever wrote
there (the value was necessarily read from the `pnpm tauri:dev` terminal instead, contradicting
precondition 2's own "never from the terminal" rule). Plan 34.5-18 closed that defect by the
preferred of the two options that document named — this re-run's precondition 7 below consumes that
closure directly.

**Evidence-handling rule (T-34.5-G6-02), binding on every Result slot below.** This document lives
in the public fork's repository. When pasting `gamelib.log` excerpts, VDF/config contents, or any
other captured evidence into a Result slot: never paste an `access_token`, `refresh_token`, cookie
value, session identifier, or the Steam `userdata` account id. Quote only the specific log lines an
item or precondition names. If a required line contains one of these values inline, redact it in
place (e.g. `access_token=<REDACTED>`) rather than omitting the whole line — the surrounding
structure (which field was populated, that a value was non-empty) is still evidence; the value
itself is not needed to prove a PASS.

---

## Preconditions

**Tick off in order. An unstated precondition is exactly how a gate gets skipped and becomes UAT
debt (Phases 21, 30, 34.2, and this very phase's first run) — carried forward unchanged.**

1. **Phase 34.4.1's own blocking live gate has recorded PASS on item 1.**

   Satisfied on 2026-08-01 by `34.4.1-LIVE-GATE-RERUN-3.md` (4/4 PASS). Carried verbatim from
   `34.5-LIVE-GATE.md` precondition 1's own RESULT block:

   > 34.4.1's own blocking live gate ran THREE times: run 1 (`34.4.1-LIVE-GATE.md`) FAILED 2/4, run
   > 2 (`34.4.1-LIVE-GATE-RERUN.md`) FAILED 3/4, run 3 (`34.4.1-LIVE-GATE-RERUN-3.md`) **PASSED
   > 4/4**. This precondition is satisfied by the run-3 result — `34.4.1-LIVE-GATE.md` (the
   > document this precondition originally named) holds only the superseded FAILED first run and
   > must NOT be quoted as evidence of a PASS.

   Verdict-table item-1 result line, quoted verbatim from `34.4.1-LIVE-GATE-RERUN-3.md`:

   > `| 1 — login from scratch | -02, -12, GAP-02, GAP-09 | ✅ **PASS** | 75B keyring store, no
   > sessionCookie, keyring-install line present; **WR-07 closed**, **F-4 closed** |`

   Overall run-3 verdict, same document: `**4 of 4 PASS — the gate is met. Phase 34.4.1 closes.**`

   **`34.4.1-LIVE-GATE.md` holds only the superseded FAILED first run (2/4) and `-RERUN.md` holds
   the superseded second run (3/4) — neither may ever be quoted as evidence of a PASS.** This
   precondition is already satisfied and does not need to be re-verified live; it is restated here
   so the re-run's own record is self-contained.

   **RESULT — SATISFIED.** Quoted verbatim from `34.4.1-LIVE-GATE-RERUN-3.md`'s own Verdict table,
   item 1: `| 1 — login from scratch | -02, -12, GAP-02, GAP-09 | ✅ **PASS** | 75B keyring store, no
   sessionCookie, keyring-install line present; **WR-07 closed**, **F-4 closed** |`. Overall run-3
   verdict, same document: `**4 of 4 PASS — the gate is met. Phase 34.4.1 closes.**`
   `34.4.1-LIVE-GATE.md` (2/4) and `34.4.1-LIVE-GATE-RERUN.md` (3/4) were not cited as evidence.

2. **A clean machine.**

   Run `ps aux | grep -E "gamelib-shell|sidecar.js"` BEFORE launching anything, and kill anything
   already running. Record the command's output verbatim both before the kill and after (an empty
   post-kill listing, aside from the grep process itself, is the expected clean state).

   Why this matters: during the 2026-07-29 orchestrator session that satisfied part of the first
   run's preconditions, an ORPHANED `gamelib-shell`/`sidecar.js` pair from an earlier, unrelated
   session (no live parent terminal) was found still running alongside a freshly-launched pair —
   see `34.5-LIVE-GATE.md` precondition 2's own "Housekeeping finding." Two live pairs contend for
   the same store files (`gog_store`, `nile_store`, keyring slots), which would make this re-run's
   own evidence ambiguous (which process wrote what). This precondition exists so that ambiguity
   cannot recur.

   **RESULT — SATISFIED, after killing a recurrence of the exact housekeeping finding this
   precondition warns about.** BEFORE kill, `ps aux | grep -E "gamelib-shell|sidecar.js"` (grep
   process line itself omitted) returned THREE processes:

   ```
   graysonmitchell  87467   0.0  0.1 444736800  18448 s004  S+    9:04AM   0:01.06 node .../build/main/sidecar.js
   graysonmitchell  87420   0.0  0.2 440412816  40240 s004  S+    9:04AM   0:09.08 target/debug/gamelib-shell
   graysonmitchell  77775   0.0  0.2 444736240  33776 s004  S+   12:05AM   0:07.62 node .../build/main/sidecar.js
   ```

   PID 77775 (`sidecar.js`, launched 12:05AM) had no matching `gamelib-shell` parent in the
   listing — an orphaned sidecar from an earlier session, the same shape as `34.5-LIVE-GATE.md`
   precondition 2's own "Housekeeping finding." PIDs 87420/87467 (launched 9:04AM) were a live
   pair. All three were killed: `kill -9 87467 87420 77775`. AFTER the kill, the same `ps aux`
   command returned NO matching processes — clean.

3. **A clean rebuild and a real `pnpm tauri:dev` launch.**

   Confirm the sidecar is alive via `~/Library/Logs/GameLib/gamelib.log` growing AND a `ps` listing
   showing both the shell and sidecar processes simultaneously. **Sidecar output is read from that
   log file, NEVER from the `pnpm tauri:dev` terminal** — the terminal shows the shell process's own
   console (stdout is the RPC pipe), never proof the sidecar's *application* logic ran, per the
   standing `sidecar-console-and-logger-are-invisible` finding. Keep `tail -f
   ~/Library/Logs/GameLib/gamelib.log` open in a second window for the whole run.

   Record the exact rebuild sequence and its output (`electron-vite build` / `npx electron-vite
   build`, `pnpm build:sidecar` / `npm run build:sidecar`, `cargo run --manifest-path
   src-tauri/Cargo.toml`), and the `ps aux` lines showing both PIDs alive together, mirroring the
   evidence shape `34.5-LIVE-GATE.md` precondition 2 already recorded twice.

   **RESULT — SATISFIED.** Rebuild sequence run in order:

   1. `npx electron-vite build` — exit 0, `✓ built in 5.23s`.
   2. `npm run build:sidecar` (`esbuild --bundle --platform=node --target=node22 --format=cjs
      --packages=external --external:electron --external:electron-store --outfile=build/main/sidecar.js
      src/sidecar/index.ts`) — exit 0, `build/main/sidecar.js  1.1mb`, `⚡ Done in 21ms`.
   3. `cargo run --manifest-path src-tauri/Cargo.toml` — `Compiling gamelib-shell v0.7.0`, then
      launched.

   `ps aux` after launch, both PIDs alive simultaneously:

   ```
   graysonmitchell  12428   1.4  0.9 440752592 158672   ??  SN   10:15AM   0:00.85 src-tauri/target/debug/gamelib-shell
   graysonmitchell  12510   0.0  1.3 444803776 224496   ??  SN   10:15AM   0:00.73 node .../build/main/sidecar.js
   ```

   `gamelib.log` growth, read from the file (never the terminal): immediately after launch
   (10:15:52), the file held 28 lines / 3890 bytes (the log rotates to a fresh file on process
   start, confirmed against `gamelib.log.old`). At 10:45:53 — after this precondition's evidence was
   captured and while later preconditions were being worked — the same file held 33 lines / 4370
   bytes, with new lines appended at 10:16:09, 10:45:45 and 10:45:53. The file is actively growing;
   both PIDs remained alive throughout.

4. **The G-1 fix is present in THIS build — proven from the log, not from git.**

   Quote verbatim from THIS session's `gamelib.log` (not a prior run's, not the terminal):

   - The `[bootstrap] appRoot resolved=<path> source=<GAMELIB_APP_ROOT|process.cwd>` line — the
     `source=` field must read `GAMELIB_APP_ROOT`, not `process.cwd` (a `process.cwd` source means
     the Rust-side handoff from plan 34.5-16 did not reach the sidecar this run, and this
     precondition is NOT satisfied even if the resolved path happens to look correct).
   - The `[bootstrap] publicDir resolved=<path> exists=<bool>` line — `exists=` must read `true`.
   - All four `[bootstrap] runner binary <name> path=<path> exists=<bool>` lines, one each for
     `legendary`, `gogdl`, `nile`, `comet` — every `exists=` must read `true`.

   These four line shapes are quoted directly from `src/backend/sidecar/bootstrap.ts` (the boot-time
   asset-root self-check plan 34.5-18 added), read at authoring time rather than copied from a
   SUMMARY, so this contract cannot cite a line that does not exist in the shipped code:

   ```
   [bootstrap] appRoot resolved=${electronStub.app.getAppPath()} source=${appRootSource}
   [bootstrap] publicDir resolved=${publicDir} exists=${publicDirExists}
   [bootstrap] runner binary ${runnerBinaryName} path=${resolvedRunnerPath} exists=${runnerExists}
   ```

   **A `[bootstrap] SIDECAR ASSET ROOT DEFECT` line anywhere in this session's `gamelib.log` BLOCKS
   the run.** Items 1-3 (and therefore item 5, which depends on them) must NOT be attempted while
   that line is present — it is the exact same failure family (`publicdir-getapppath-chunking`,
   4th recurrence) that produced last run's `spawn ./{legendary,gogdl,nile} ENOENT`, now surfaced
   loudly at boot instead of silently six layers downstream. If it fires, treat this precondition as
   NOT satisfied, stop, and route back to a gap cycle rather than attempting items 1-3 on a build
   that has already announced its own asset root is broken.

   **RESULT — SATISFIED.** Quoted verbatim from THIS session's `gamelib.log` (all at 10:15:52):

   ```
   (10:15:52) [INFO]:    [Backend]:         [bootstrap] appRoot resolved=/Users/graysonmitchell/Projects/GameLib source=GAMELIB_APP_ROOT
   (10:15:52) [INFO]:    [Backend]:         [bootstrap] publicDir resolved=/Users/graysonmitchell/Projects/GameLib/public exists=true
   (10:15:52) [INFO]:    [Backend]:         [bootstrap] runner binary legendary path=/Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/legendary exists=true
   (10:15:52) [INFO]:    [Backend]:         [bootstrap] runner binary gogdl path=/Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/gogdl exists=true
   (10:15:52) [INFO]:    [Backend]:         [bootstrap] runner binary nile path=/Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/nile exists=true
   (10:15:52) [INFO]:    [Backend]:         [bootstrap] runner binary comet path=/Users/graysonmitchell/Projects/GameLib/public/bin/arm64/darwin/comet exists=true
   ```

   `source=GAMELIB_APP_ROOT` (not `process.cwd`) — the Rust-side handoff reached the sidecar this
   run. `publicDir exists=true` and all four runner binaries `exists=true`. `grep -c "SIDECAR ASSET
   ROOT DEFECT" gamelib.log` returned `0` — the line is ABSENT. Items 1-3 are cleared to be
   attempted.

5. **Signed OUT of Epic, GOG and Amazon — verified, not assumed.**

   The Epic and GOG credentials were moved aside during the 2026-08-01 run (plan 34.5-15) and the
   backups are expected to still be in place:

   | Runner | Original path | Backup path |
   |---|---|---|
   | Epic (legendary) | `~/Library/Application Support/GameLib/legendaryConfig/legendary/user.json` | same path + `.pre-34.5-15-gate.bak` |
   | GOG | `~/Library/Application Support/GameLib/gog_store/auth.json` | same path + `.pre-34.5-15-gate.bak` |

   This contract **ASSUMES** that aside state persists and **REQUIRES** it be verified — with
   `ls`/`test -f` on both original paths and both backup paths, recorded verbatim — not assumed from
   the prior SUMMARY. If a credential HAS reappeared at its original path (e.g. the developer signed
   back in between runs), move it aside again by the same recoverable procedure: `cp -p` to a NEW,
   dated suffix (never reuse `.pre-34.5-15-gate.bak` for a second aside), `cmp` the copy against the
   original to prove byte-identity, and only then remove the original — never delete blind.

   Amazon was already signed out coming into the first run (`~/Library/Application
   Support/GameLib/nile_store/` held no auth file). Re-verify this rather than assuming it — also
   check `~/Library/Application Support/GameLib/nile_config/` (the CLI-owned credential directory
   per D-12's correction, `34.5-10-SUMMARY.md`) holds no auth state, since a stray credential in
   either directory would compromise item 3's "from scratch" claim.

   **RESULT — SATISFIED.** Verbatim `ls`:

   ```
   === Epic (legendary) ===
   ls: .../legendaryConfig/legendary/user.json: No such file or directory
   -rw-r--r--@ 1 graysonmitchell  staff  6266 Jul 29 07:21 .../legendaryConfig/legendary/user.json.pre-34.5-15-gate.bak
   === GOG ===
   ls: .../gog_store/auth.json: No such file or directory
   -rw-r--r--@ 1 graysonmitchell  staff  478 Jul 29 21:27 .../gog_store/auth.json.pre-34.5-15-gate.bak
   === Amazon (nile) ===
   .../nile_store/ — total 0 (empty directory, no auth file)
   ls: .../nile_config/: No such file or directory
   ```

   Epic and GOG originals absent (signed out), both `.pre-34.5-15-gate.bak` backups present and
   untouched since 2026-07-29 — no re-aside needed. `nile_store/` holds no auth file; `nile_config/`
   does not exist at all — Amazon carries no stray credential in either directory.

6. **`npm run test:ci`, `npx tsc --noEmit`, `cd src-tauri && cargo check`, `cargo test` — all four
   run at the HEAD the gate actually runs on.**

   Exit codes recorded VERBATIM (not rounded to "green"), with a one-line explanation of any
   non-zero exit that is a known pre-existing flake rather than a regression.

   The 2026-08-01 first-run readings (`34.5-LIVE-GATE.md` precondition 4) were **3447 jest tests,
   40→80 Rust tests** (rising to **80** by the end of that run's HEAD) — this is a **floor, not a
   target**: this gap cycle's three plans (34.5-16, 34.5-17, 34.5-18) each added test suites and
   individually recorded higher counts as they landed (3454 → 3459 → **3463 jest tests**, **84 Rust
   tests**, per their own SUMMARYs). A higher count than 3463/84 at gate time is expected and
   healthy; a count LOWER than 3447/80 (the original floor) must be explained before the run
   proceeds, and a count between 3447/80 and 3463/84 must also be explained (it would mean HEAD is
   stale relative to the landed gap-cycle work).

   Also re-run (not required by this precondition, recorded for completeness, per the first run's
   own practice): `python3 ported-channels-gate.py` → must exit 0.

   **RESULT — SATISFIED, with one explained non-zero exit.**

   - `npx tsc --noEmit` — **exit 0**, no output.
   - `cd src-tauri && cargo check` — **exit 0**. `Checking gamelib-shell v0.7.0 ... Finished
     \`dev\` profile [unoptimized + debuginfo] target(s) in 26.14s`.
   - `cargo test` (from `src-tauri`) — **exit 0**. `test result: ok. 84 passed; 0 failed; 1
     ignored; 0 measured; 0 filtered out`. 84 matches the gap-cycle's own recorded count exactly.
   - `npm run test:ci` (`jest --runInBand --silent`) — run TWICE, deliberately, because the first
     reading was non-zero and this precondition requires the non-zero exit be explained, not
     rounded to green:
     - **First run** (executed concurrently with the `cargo run`/rebuild work above, under CPU
       contention) — **exit non-zero**. `Test Suites: 1 failed, 178 passed, 179 total` / `Tests: 2
       failed, 3461 passed, 3463 total`. Both failures were in
       `src/backend/storeManagers/steam/__tests__/depot.test.ts`, one named explicitly in the
       failure output: `D-UAT-06: a PERSISTENT CM drop during plan-build exhausts the bounded
       retry ...`, thrown as `Exceeded timeout of 5000 ms for a test`.
     - **Explanation, verified rather than assumed** (per the standing
       `flake-baselines-can-be-undiagnosed-bugs` lesson — never accept a baseline flake without a
       single-file repro): re-ran `npx jest src/backend/storeManagers/steam/__tests__/depot.test.ts
       --silent` in isolation — **exit 0**, `Tests: 106 passed, 106 total`, `Time: 4.967 s`,
       comfortably under the 5000 ms timeout. This confirms CPU contention (concurrent `cargo
       check`/`cargo test`/rebuild work) starved the `--runInBand` timer, not a regression in the
       test's own logic.
     - **Second run** (clean, nothing else running concurrently) — **exit 0**. `Test Suites: 179
       passed, 179 total` / `Tests: 3463 passed, 3463 total`. 3463 is exactly the gap-cycle's own
       recorded ceiling (34.5-18's landed count) — HEAD is not stale.
   - `python3 ported-channels-gate.py` (completeness, not required) — **exit 0**. `OK: all 38
     channels declared exactly once with correct send/invoke kind, ... live-gate item 4 is scoped
     to exactly {addToSteam, addShortcut} (REQ-34.5-11).`

   3463 jest / 84 Rust is within the expected 3447-3463 / 80-84 band (matches the ceiling exactly);
   no shortfall to explain relative to the floor.

7. **`GAMELIB_SHELL_EXE` read back from `gamelib.log`, from the SIDECAR's own receipt line.**

   Cite `[bootstrap] GAMELIB_SHELL_EXE received=<value>` — the value the CHILD actually observed,
   not the Rust shell's own `eprintln!` claim at `main.rs:1231` (which only ever reaches the shell
   process's stderr, the `pnpm tauri:dev` terminal, and structurally cannot distinguish "set on the
   child env" from "actually visible to `pathShim`"). This line shape is quoted directly from
   `src/backend/sidecar/bootstrap.ts` at authoring time:

   ```
   [bootstrap] GAMELIB_SHELL_EXE received=${shellExeReceived}
   ```

   (or `[bootstrap] GAMELIB_SHELL_EXE received=<UNSET>` at warning level if the env var is unset —
   an `<UNSET>` reading here BLOCKS item 4 for the same reason precondition 4's DEFECT line blocks
   items 1-3: the value item 4 exists to prove correct cannot be correct if it was never received.)

   **This closes the gate-contract defect `34.5-LIVE-GATE.md` precondition 5 recorded**, by the
   PREFERRED of that document's two named closure options ("have the sidecar log the value it
   actually received at boot" — plan 34.5-18 implemented exactly this). Preconditions 3 and 7 no
   longer contradict each other: precondition 3 requires all sidecar evidence come from
   `gamelib.log`, and precondition 7's evidence now lives there too.

   **Dev-vs-packaged caveat, carried verbatim — material to what item 4 retires.** This value comes
   from a **dev** build, where `current_exe()` resolves to `src-tauri/target/debug/gamelib-shell`.
   A packaged build resolves through a different branch (`spawn_sidecar_packaged`, `main.rs:1268`)
   to a path inside the `.app` bundle — a different value from a different code path. **Item 4
   passing in dev does NOT transfer to the packaged artifact.** Precondition 3 specifies `pnpm
   tauri:dev`, so dev is what this gate asks for; recording the limit here so a dev-only PASS is
   never later read as covering packaging.

   **Parallel limit for this cycle:** residual `R-34.5-G1-PKG` (the packaged asset root, recorded in
   `34.5-APP-ROOT-SWEEP.md` § 3 and `34.5-16-SUMMARY.md`) is likewise NOT covered by a dev PASS on
   this precondition or on item 4 — it remains an open residual regardless of this run's outcome.

   **RESULT — SATISFIED.** Quoted verbatim from THIS session's `gamelib.log` (10:15:52):

   ```
   (10:15:52) [INFO]:    [Backend]:         [bootstrap] GAMELIB_SHELL_EXE received=/Users/graysonmitchell/Projects/GameLib/src-tauri/target/debug/gamelib-shell
   ```

   Not `<UNSET>` — a real path was received by the sidecar's own boot-time check, from a **dev**
   build (`src-tauri/target/debug/gamelib-shell`, matching the dev-vs-packaged caveat above exactly).
   Item 4 is cleared to be attempted, for the dev build only; the packaged branch and
   `R-34.5-G1-PKG` remain open regardless of this run's outcome.

---

## Items

**Tick off in order, after every precondition above is satisfied. Each item's Result slot is empty
below and is filled ONLY by plan 34.5-20, never by this authoring plan.**

### Item 1 — Epic login completed FROM SCRATCH, populated library (REQ-34.5-12)

**What changed since the failed run:** the `legendary` runner binary now resolves through the
handed-down app root (`GAMELIB_APP_ROOT`, plan 34.5-16) instead of a `process.cwd()`-relative
`publicDir` that did not exist under the sidecar — precondition 4 above proves that before this item
is attempted.

**What to do:** With precondition 5 satisfied for Epic (no stored credential), drive a real Epic
Games Store login through the seam 34.4.1 built, authenticating with real credentials.

**What to observe:** The login window opens on Epic's real login origin, credential entry succeeds
against the live site, the sidecar's `oauthCaptureLogin` channel reports a matched redirect, and the
resulting library is **populated** (not merely "signed in" with zero games) — an empty library after
login is not a PASS, since it could indicate the profile-data write to `configStore` silently
failed.

**Evidence required:** `gamelib.log` lines showing the OAuth redirect capture succeeding for the
`legendary`/Epic runner, and the app's own UI reflecting a signed-in, non-empty library afterward.

**Result:** [ ] PASS   [x] **FAIL** — evidence (verbatim from `~/Library/Logs/GameLib/gamelib.log`,
this session, 2026-08-01):

```
(11:01:44) [INFO]:    [Backend]:         [oauthLoginCapture] runner=legendary label=loginwin-0-18c7822c64d97db0-fc35f166
(11:02:41) [INFO]:    [Backend]:         [oauthLoginCapture] runner=legendary label=loginwin-1-18c78239bf44f830-52c62626
(11:06:44) [INFO]:    [Backend]:         [oauthLoginCapture] runner=legendary status=timeout
(11:07:41) [INFO]:    [Backend]:         [oauthLoginCapture] runner=legendary status=timeout
(11:10:27) [INFO]:    [Backend]:         [oauthLoginCapture] runner=legendary label=loginwin-4-18c782a63d1f3380-ca6f7e61
(11:15:27) [INFO]:    [Backend]:         [oauthLoginCapture] runner=legendary status=timeout
```

**Correction to a pre-recorded reading:** an earlier pass over this log characterized this as "4
login windows opened, 4 timeouts." Re-verified directly against the file (`grep -n
"runner=legendary" gamelib.log`, full session): there are exactly **3** `label=` opens
(`loginwin-0`, `loginwin-1`, `loginwin-4`) and exactly **3** `status=timeout` lines — never a
`status=captured`. The "4" reading does not appear in the log; the corrected count is 3 opens / 3
timeouts / 0 captured, recorded here instead.

Developer's verbatim report: *"when i click epic in manage accounts window opens has correct
title, but there is a 'greyed out' form. that does not resolve."* The embedded Epic login window
opened (three separate times, once per attempt) but presented a non-interactive ("greyed out")
form that never accepted credential entry — consistent with the OAuth capture never seeing a
matching redirect on any of the three attempts (0 of 3 `status=captured`). The login never
completed; the library was never populated. This is a FAIL on both required conditions (redirect
capture, populated library).

### Item 2 — GOG login completed FROM SCRATCH, populated library (REQ-34.5-12)

**What changed since the failed run:** the `gogdl` runner binary now resolves through the handed-down
app root the same way `legendary` does — precondition 4 proves this before the item is attempted.

**What to do:** With precondition 5 satisfied for GOG (no stored credential), drive a real GOG login
through the seam, authenticating with real credentials.

**What to observe:** The login window opens on GOG's real login origin, credential entry succeeds
against the live site, the sidecar's `oauthCaptureLogin` channel reports a matched redirect for the
`gog` runner, and the resulting library is **populated**.

**Evidence required:** `gamelib.log` lines showing the OAuth redirect capture succeeding for the
`gog` runner, and the app's own UI reflecting a signed-in, non-empty library afterward.

**Result:** [ ] PASS   [x] **FAIL** — evidence (verbatim from `~/Library/Logs/GameLib/gamelib.log`,
this session, 2026-08-01):

```
(11:14:37) [INFO]:    [Backend]:         [oauthLoginCapture] runner=gog label=loginwin-6-18c782e0525ce5a0-4aacc3ce
(11:15:45) [INFO]:    [Backend]:         [oauthLoginCapture] runner=gog status=captured
```

The redirect WAS captured (68 seconds after open, single attempt, no timeout). But:

- No `gogdl auth ...` CLI invocation appears anywhere in the log after `(11:15:45)` — `grep -n
  "Gog\]" gamelib.log` shows the last `gogdl` command was the pre-login `--version` check at
  `(11:01:41)`; nothing runs the follow-up auth exchange the capture is supposed to feed.
- `[TauriLoginPanel] captured-blocked: runner=gog ...` — the frontend's OWN log line for a
  successful capture reaching the panel (`TauriLoginPanel.tsx` `phase === 'blocked'` branch) —
  **never appears** for `gog` anywhere in the log. Only the pre-capture `declared-blocked` line at
  `(11:14:37)` is present; nothing frontend-side logs a state change after the backend capture.

Developer's verbatim report: *"I was already signed into GOG, when i signed out was asked to
approve keycahin twice, then login proceeded as i would expect. When i had finished logging in
though the manage accounts screen had the following message 'Signing in to Gog / A sign-in window
has opened. Complete sign-in there.' I confirmed that GOG games are not showing in Library and Gog
is not available as a filter option"* — this is the panel's `phase === 'awaiting'` copy
(`TauriLoginPanel.tsx` lines 96-109), meaning the UI was still displaying the PRE-capture state
after the backend had already logged `status=captured`. The library was not populated and GOG was
absent from the filter list, confirmed directly by the developer. FAIL — the redirect capture
happened, but the required outcome ("resulting library is populated") was not met, and nothing in
the log shows the frontend ever consuming the capture.

### Item 3 — Amazon login completed FROM SCRATCH, populated library, AND the `www.amazon.com` anchor confirmed against one real redirect (REQ-34.5-02, REQ-34.5-12)

**What changed since the failed run:** the `nile` runner binary now resolves through the handed-down
app root the same way `legendary`/`gogdl` do — precondition 4 proves this before the item is
attempted.

**What to do:** With precondition 5 satisfied for Amazon (no stored credential), drive a real Amazon
Games login through the seam, authenticating with real credentials. This is also the ONLY real proof
of REQ-34.5-02's `matchOAuthRedirect('nile', …)` host anchor (`www.amazon.com`, MEDIUM confidence,
verified only against upstream `nile` source, never against GameLib's own traffic — research
Assumption A1).

**What to observe:** The login window opens on Amazon's real login origin, credential entry succeeds
against the live site, the sidecar's `oauthCaptureLogin` channel reports a matched redirect for the
`nile` runner, and the resulting library is **populated**. Additionally: capture the real redirect's
origin and confirm it matches `www.amazon.com` as `matchOAuthRedirect` assumes.

**Evidence required:**
  (a) `gamelib.log` lines showing the OAuth redirect capture succeeding for the `nile` runner, and
      the app's own UI reflecting a signed-in, non-empty library afterward.
  (b) The captured redirect's origin, read back from the greppable evidence-log line plan 34.5-02
      added — grep `gamelib.log` for `[oauthLoginCapture] runner=nile origin-mismatch origin=`
      (confirmed present verbatim at `src/backend/sidecar/oauthLoginCapture.ts` at authoring time).
      **A PASS on the login itself means this line should NOT fire** (the origin matched and was
      accepted silently); if it DOES fire, the `www.amazon.com` anchor is FALSIFIED and this item
      cannot be a clean PASS even if login otherwise succeeds — record the actual origin observed
      either way. If a clean login completes and the line never fires, record Assumption A1 as
      CONFIRMED (not merely "not falsified"). If the line never fires because the redirect-comparison
      code path is never reached for some other reason (e.g. the item is blocked before login),
      record A1 as UNTESTED — the same distinction the first run's own precondition made.

**Result:** [ ] PASS   [x] **FAIL** — evidence (verbatim from `~/Library/Logs/GameLib/gamelib.log`,
this session, 2026-08-01):

```
(11:03:09) [INFO]:    [Backend]:         [oauthLoginCapture] runner=nile label=loginwin-2-18c782402abf06b8-e4ff7001
(11:08:09) [INFO]:    [Backend]:         [oauthLoginCapture] runner=nile status=timeout
(11:08:39) [INFO]:    [Backend]:         [oauthLoginCapture] runner=nile label=loginwin-3-18c7828d09343cc8-125822c6
(11:10:10) [INFO]:    [Backend]:         [oauthLoginCapture] runner=nile status=captured
```

Two attempts: the first (`loginwin-2`) timed out; the second (`loginwin-3`) captured a matching
redirect after 91 seconds. `grep -c "origin-mismatch" gamelib.log` over the full session returns
**0** — the line never fires.

**Sub-clause (b) — Assumption A1 (`www.amazon.com` anchor): CONFIRMED.** This is not merely "the
line is absent" — it follows structurally from the matcher's own code
(`src/backend/sidecar/oauthLoginCapture.ts:103-127`, read directly, not from a summary). For the
`nile` case, `status: 'captured'` can ONLY ever be logged after `matchOAuthRedirect` returns a
non-null match; that function's `nile` branch returns non-null ONLY when `parsed.hostname ===
'www.amazon.com'` exactly — the alternate branch (the only branch capable of logging
`origin-mismatch`) always returns `null` and therefore can never settle as `captured`. Since
`runner=nile status=captured` is logged at `(11:10:10)` with zero `origin-mismatch` lines anywhere
in the session, the redirect-comparison code path was demonstrably reached (twice — once per
attempt) and matched cleanly against `www.amazon.com` on the second attempt. Per the item's own
recording rule ("If a clean login completes and the line never fires, record Assumption A1 as
CONFIRMED"), this is recorded as CONFIRMED, not UNTESTED — the comparison path being reached is not
ambiguous here; it is a structural precondition of the `captured` log line existing at all.

**This does NOT make item 3 a PASS.** Downstream of the capture, the same defect pattern observed
in item 2 recurs: no `nile auth --login ...` CLI invocation appears anywhere in the log after
`(11:10:10)` (the last one, at `(11:08:18)`, ran BEFORE the capture, to generate the second
attempt's login URL); `[TauriLoginPanel] captured-blocked: runner=nile ...` never fires either.
Developer's verbatim report: *"For amazon, the logon appears... but there are some usability
issues with it. I went to my password manager, but have more than one amazon logon, as there is no
url displayed in the logon flow could not be sure which logon to use. In the end neither worked so
i went through password reset flow. When it cam time to enter my code (emailed to me) the text was
black on black so could not tell my cut and paste worked until i highlighted. Changed my password
and logged in. account manager did not reflect that i had logged it (if indeed i had)."* The
resulting library was never confirmed populated and the account manager never reflected a
signed-in state. FAIL on the item's own compound requirement (matched redirect AND populated
library) even though the A1 sub-clause specifically is CONFIRMED.

### Item 4 — `addToSteam` AND the macOS `.app` desktop shortcut both launch GameLib, not node — BOTH `exe` call sites (REQ-34.5-01, REQ-34.5-12)

**What to do:** Exercise BOTH independent call sites per research Correction 3 — proving one is not
sufficient, since they are two independent writers of the same `GAMELIB_SHELL_EXE`-derived value and
either can regress alone:

(a) Add a non-Steam game to Steam via `addToSteam` and launch it from Steam — call site
    `nonesteamgame.ts:258` (the Steam shortcut VDF `Exe` field).
(b) Separately, create a macOS `.app` desktop shortcut via the shortcuts flow and double-click it —
    call site `shortcuts.ts:227` (the `.app`'s `run.sh` body).

**Unconsumed groundwork, carried across from `34.5-LIVE-GATE.md`'s "Item 4 partial progress"
(2026-07-29) rather than rediscovered:**

- **Target:** GOG *Alan Wake*, `appName` `1207659037`, installed at `~/GameLib/Alan Wake`.
  **Before starting, re-verify the target is still installed** — check `gog_store/installed.json`
  AND the on-disk directory. This groundwork is days old, and a fresh GOG login from item 2 above
  re-populates the library; do not assume the prior install survived unchanged.
- **Expected writes:**

  | Call site | File | Expected content |
  |---|---|---|
  | `nonesteamgame.ts:258` | `~/Library/Application Support/Steam/userdata/35601368/config/shortcuts.vdf` | `Exe` = `"<shell exe path>"` |
  | `shortcuts.ts:227` | `<shortcut>.app/Contents/MacOS/run.sh` | body reads `<shell exe path> --no-gui gamelib://launch?appName=1207659037&runner=gog` |

  `shortcuts.vdf` did NOT exist when the groundwork was recorded — the file appearing at all is
  itself part of item 4(a)'s evidence.
- **Both channels are reachable from the devtools console via `window.api`:**

  ```js
  window.api.addShortcut('1207659037', 'gog', false)   // listener, fire-and-forget
  await window.api.addToSteam('1207659037', 'gog')     // handler, returns boolean
  ```

- **Operator warning, unchanged from the first run: quit Steam FIRST.** Steam holds `shortcuts.vdf`
  in memory and rewrites it from memory on exit, so a write performed while Steam is running will be
  clobbered.

**What to observe:** In both cases, the launched process is the Tauri shell binary (GameLib), never
the `node` interpreter. A wrong value at either call site produces a shortcut/VDF entry that launches
the wrong executable.

**Evidence required:** For each call site, the actual launch command/target written (the VDF `Exe`
field for `addToSteam`, `run.sh`'s body for the desktop shortcut), plus `ps aux` confirming the
launched process is the Tauri shell binary and never a bare `node` process.

**Result:** [ ] PASS   [ ] FAIL   [x] **NOT ATTEMPTED** — asked explicitly, the developer confirmed
neither `nonesteamgame.ts:258` nor `shortcuts.ts:227` was exercised this run. Do NOT read this slot
as PASS, FAIL, or BLOCKED. Why: the developer's session was spent driving items 1-3 (Epic, GOG,
Amazon), none of which reached a confirmed populated/authenticated state this run — item 4's own
groundwork target (GOG's *Alan Wake*) depends on a working, in-session GOG login, and item 2 did
not deliver one (backend `status=captured` occurred, but the UI never reflected a signed-in GOG
account and the library was never confirmed populated — see item 2's Result above). The developer
never reached item 4's devtools console steps.

### Item 5 — `runWineCommand` succeeds for at least one NON-Steam runner (REQ-34.5-03, REQ-34.5-12)

**Last run this FAILED BY BLOCKAGE only.** Nothing about `runWineCommand`'s own code was exercised
or falsified — it was never reached because items 1-3 all failed upstream at the runner-binary spawn
defect. Research Pitfall 2's standing claim — "no non-Steam runner has ever executed a Wine command
under the sidecar" — must NOT be struck until THIS item records a PASS; last run's blockage does not
count as evidence either way.

**What to do:** After a successful login above (items 1-3), run a Wine tool action (e.g. a
winecfg/DXVK toggle/wine-version action reachable via `runWineCommand`) against a non-Steam game from
one of the newly-authenticated runners.

**What to observe:** The Wine command executes successfully under the sidecar.

**Evidence required:** `gamelib.log` lines showing the Wine command dispatch and its successful
completion, plus the observable in-app effect (e.g. the toggle taking effect, or the tool launching)
for the specific non-Steam runner exercised.

**Result:** [ ] PASS   [ ] FAIL   [x] **NOT ATTEMPTED** — asked explicitly, the developer confirmed
this item was not attempted. Why: item 5's own precondition is "a successful login above (items
1-3)"; none of items 1, 2, or 3 delivered a confirmed authenticated, library-populated non-Steam
runner this run (item 1: 0/3 captured; items 2 and 3: capture succeeded at the backend but the UI
never reflected a signed-in account and the library was never confirmed populated), so there was no
authenticated runner to drive a Wine action against. Research Pitfall 2's standing claim — "no
non-Steam runner has ever executed a Wine command under the sidecar" — is NOT retired; nothing
about `runWineCommand`'s own code was exercised or falsified this run, exactly as the first run's
own blockage recorded.

---

## New findings (this run) — recorded for the next gap cycle, not diagnosed here

Per this document's own instruction ("record the observation without diagnosing it ... diagnosis
belongs in the summary and the next gap cycle"), these are recorded as observations with evidence
only. None of these blocks the arithmetic above; they are additional defects the developer's report
surfaced beyond the five items' own PASS/FAIL/NOT-ATTEMPTED conditions.

- **F-34.5-G6-01 — Epic (`legendary`) login form renders but is non-interactive ("greyed out"),
  never resolves.** Item 1's own evidence: 3 opens, 3 timeouts, 0 captures. Developer: *"there is a
  'greyed out' form. that does not resolve."*
- **F-34.5-G6-02 — GOG and Amazon both reach `status=captured` at the backend, but the UI never
  leaves the pre-capture "Signing in…" state and the account manager does not reflect a signed-in
  account.** Evidence: `runner=gog status=captured` (11:15:45) and `runner=nile status=captured`
  (11:10:10), in both cases with (a) no follow-up runner-CLI auth invocation in the log afterward
  and (b) `[TauriLoginPanel] captured-blocked: runner=... ` never firing for either runner anywhere
  in the session (only the pre-capture `declared-blocked` line appears). This is downstream of the
  OAuth capture mechanism itself, which both cases prove works.
- **F-34.5-G6-03 — GOG library not populated post-login; GOG absent from the Library filter
  options.** Developer, verbatim: *"I confirmed that GOG games are not showing in Library and Gog
  is not available as a filter option."*
- **F-34.5-G6-04 — the login window displays no URL/origin to the user.** Developer, verbatim: *"as
  there is no url displayed in the logon flow could not be sure which logon to use."* A usability
  and phishing-resistance defect: the user cannot verify which site they are entering credentials
  into, nor disambiguate between multiple stored credentials for the same domain.
- **F-34.5-G6-05 — black-on-black text in the Amazon verification-code field, unreadable until
  highlighted.** Developer, verbatim: *"the text was black on black so could not tell my cut and
  paste worked until i highlighted."* A contrast/theming defect surfacing specifically under the
  embedded WKWebView-driven login window.
- **F-34.5-G6-06 — signing out of GOG prompted for Keychain approval twice.** Developer, verbatim:
  *"when i signed out was asked to approve keycahin twice."*

These six findings, plus item 1's own FAIL and the item 2/3 downstream-of-capture FAIL pattern,
are the input for the next gap cycle's scoping — not resolved by this document.

---

## Verdict

**RESERVED — filled by plan 34.5-20 only, after all five items above are attempted.**

| Item | Subject | Result |
|------|---------|--------|
| 1 | Epic login completed FROM SCRATCH, populated library | |
| 2 | GOG login completed FROM SCRATCH, populated library | |
| 3 | Amazon login completed FROM SCRATCH, populated library + `www.amazon.com` anchor confirmed | |
| 4 | `addToSteam` AND macOS `.app` shortcut both launch GameLib, not node (both `exe` call sites) | |
| 5 | `runWineCommand` succeeds for at least one non-Steam runner | |

**Overall verdict: [reserved — not evaluated until all five items are attempted or the run is
formally halted at a precondition].**

Per this document's own rule — restated from `34.5-LIVE-GATE.md` and D-08 — "any item recorded FAIL
means this phase does not close ... no advisory mode, no pre-authorized override, no partial-pass
closure."

---

## What this gate falsifies

**Nothing is struck by the mere existence of this contract.** The following four standing claims are
eligible to be struck ONLY on a PASS of their corresponding item — never on this document's authoring,
and never on a partial or blocked result:

1. **That the 34.4.1 login seam carries a real Epic/GOG/Amazon session end-to-end** — eligible on a
   PASS of items 1, 2, and 3 (each independently; a PASS on one does not retire the claim for the
   others).
2. **The `www.amazon.com` host anchor (research Assumption A1)** — eligible on a PASS of item 3, AND
   ONLY if the origin-mismatch grep line (item 3, evidence (b)) does not fire. A login PASS with the
   mismatch line firing does NOT retire this claim — it FALSIFIES it instead (see item 3's own
   recording rule).
3. **`GAMELIB_SHELL_EXE` correctness at both `exe` call sites (research Assumption A2)** — eligible on
   a PASS of item 4, covering BOTH call sites (`nonesteamgame.ts:258` and `shortcuts.ts:227`). A dev
   PASS never retires the packaged half of A2, and never retires residual `R-34.5-G1-PKG` — both stay
   open regardless of this item's result, per precondition 7's parallel limit above.
4. **Research Pitfall 2's non-Steam Wine claim** ("no non-Steam runner has ever executed a Wine
   command under the sidecar") — eligible on a PASS of item 5 only. A FAIL-by-blockage (item 5 never
   reached because items 1-3 failed) does NOT retire this claim, exactly as the first run recorded.

**The rule, stated plainly: nothing is struck without the corresponding item PASSing.** A PASS
recorded here is what makes the strike eligible; the actual striking happens in plan 34.5-20's own
completion record (its SUMMARY and/or a follow-up update to `34.5-PORTED-CHANNELS.md`/
`34.5-RESEARCH.md`), not in this contract document.

---

## Arithmetic rule

**`items_passed + items_failed` must equal 5 when the run completes cleanly.** If any item is NOT
ATTEMPTED (e.g. the run is halted at a precondition, or the developer elects to stop early as
happened with item 4 last run), that item counts toward NEITHER `items_passed` NOR `items_failed`,
and the shortfall must be stated explicitly in the frontmatter `notes` field — exactly as the first
run's own frontmatter did (`4 + 1 (not-attempted) = 5 = items_total`) — never quietly rounded to make
the arithmetic look clean.
