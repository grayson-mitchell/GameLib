---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 37
subsystem: auth
tags: [epic, discriminator, tauri, electron, oauth, diagnosis-only]

# Dependency graph
requires:
  - phase: 34.5 plan 24 / plan 28
    provides: 34.5-G6-EPIC-DISCRIMINATOR.md's first contract (verdict R1-FALSIFIED), whose own
      Result section named this exact Electron-vs-Tauri experiment as the concrete next
      discriminator
  - phase: 34.5 plan 33 / plan 34
    provides: the JSON-transport null-runner fix and the useTauriOAuthLogin teardown
      instrumentation, whose first live witness (phase=teardown inflight=false) appears in this
      plan's Arm T evidence
provides:
  - 34.5-G6-EPIC-DISCRIMINATOR-2.md's filled verdict (E1) and Routing section, closing the
    pre-registered second contract without shipping any code
  - Explicit confirmation that F-34.5-G6-01 is implicated in the Tauri/WKWebView seam
    specifically, not an Epic server-side change independent of this project
affects: [34.5-g6-epic-diagnosis, u-34.5-06, u-34.5-09]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Second pre-registered contract in the same finding's diagnosis chain: readings/prediction
      table/decision rule committed with verdict:null before any observation, filled only in a
      later commit, diffed against the null-verdict commit to prove no post-hoc rewrite"
    - "A contract precondition violated by the observation itself (a real login completing where
      only a keystroke was required) is recorded as a named deviation inside the Result section,
      not silently absorbed or omitted"

key-files:
  created: []
  modified:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR-2.md

key-decisions:
  - "Decision rule applied exactly as pre-registered: Electron interactive (real login completed)
    AND Tauri non-interactive (two full 300s timeouts, no captured redirect) selects E1 -- the
    Tauri/WKWebView seam is implicated specifically, not an Epic-side change independent of the
    port"
  - "Arm E's title bar recorded NOT CAPTURED with its reason (session quit before transcription,
    re-run deliberately declined because it cannot change which verdict the rule selects) rather
    than left blank or invented"
  - "The credential-precondition deviation on Arm E (a real login completed against the
    contract's own no-credentials precondition) is named explicitly in the Result section as
    evidence, stated to not weaken the interactivity finding, and not silently corrected"
  - "Routing selects instrumentation of the login window's own console/script-error signal to
    confirm or refute R2 -- explicitly not a fix; USER_AGENTS, EPIC_LOGIN_URL, and
    matchOAuthRedirect remain untouched, matching the contract's own E1-branch text"
  - "U-34.5-06 (Epic's success path end to end) recorded as still OPEN -- this experiment measures
    form interactivity, not a completed Tauri-side login; Arm T never reached status=captured"

requirements-completed: [REQ-34.5-04, REQ-34.5-12]

# Metrics
duration: 20min
completed: 2026-08-01
---

# Phase 34.5 Plan 37: Electron-vs-Tauri Epic discriminator, verdict E1 (Tauri seam implicated) Summary

**Second pre-registered contract closed: Electron's Epic login form accepted a full credential exchange while Tauri's stayed non-interactive across two 300s timeouts, selecting E1 (Tauri/WKWebView seam) over E2 (Epic-side, port-independent) and routing to console/script-error instrumentation — no code shipped.**

## Performance

- **Duration:** ~20 min (Task 3 authoring + verification span)
- **Completed:** 2026-08-01
- **Tasks:** 3 (Task 1 pre-registration, Task 2 human checkpoint, Task 3 verdict + routing)
- **Files modified:** 1

## Accomplishments
- Filled `34.5-G6-EPIC-DISCRIMINATOR-2.md`'s Result section from the developer's live observations on both arms, re-verifying every pasted log line directly against `gamelib.log.old` (Arm E) and `gamelib.log` (Arm T) with zero discrepancies found.
- Applied the pre-registered decision rule exactly as written: Electron interactive AND Tauri non-interactive → **E1 SELECTED**. Frontmatter `verdict` set to `E1`.
- Added a `## Routing` section naming the concrete next diagnostic step (instrumenting the login window's own console/script-error signal) and explicitly withholding authorisation to change `USER_AGENTS`, `EPIC_LOGIN_URL`, or `matchOAuthRedirect`.
- Named the contract's own precondition violation (Arm E completed a real Epic login, not a single throwaway character) as explicit evidence in the Result section, stating plainly that it strengthens rather than weakens the interactivity finding, per the developer's supplied framing.
- Confirmed `git diff` against Task 1's null-verdict commit (`0b982d23c`) shows changes only in the frontmatter `verdict` field and below the `## Result` heading — the readings, prediction table, and decision rule are byte-identical to their pre-registered form.

## Task Commits

Task 1 was committed in the prior (checkpoint-spawning) session:

1. **Task 1: Author 34.5-G6-EPIC-DISCRIMINATOR-2.md with verdict null** - `0b982d23c` (docs, prior session)
2. **Task 2: Run both arms on real hardware** - human checkpoint, answered by the developer (no commit; observation-only)
3. **Task 3: Fill the verdict and record the route it selects** - `1afac838b` (docs, this session)

## Files Created/Modified
- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR-2.md` - frontmatter `verdict: null` → `verdict: E1`; Result section filled for both arms; `## Routing` section added; `U-34.5-06` status stated explicitly (OPEN, unretired)

## Decisions Made
- The pre-registered decision rule was applied exactly as committed — no reading, prediction-table cell, or decision-rule branch was edited to fit the observation. Verified by `git diff 0b982d23c -- <path>` showing the diff confined to the frontmatter `verdict` line and everything below `## Result`.
- Arm E's title bar is recorded as **NOT CAPTURED**, with the reason stated (session quit before transcription; a re-run was offered and deliberately declined because it cannot change which verdict the rule selects, since Electron was already proven interactive by the form-input observation and the corroborating log evidence).
- The credential-precondition deviation is recorded inline in the Result section, not deferred to a separate document: the contract's own "no real Epic credentials" precondition was not honoured on Arm E (`legendary auth --code` followed by a successful `Game list updated, got 15 games & DLCs`, followed by a logout ~12 seconds later). This is stated to be strictly stronger evidence of interactivity than the single-character bar the precondition would have settled for, and the precondition's violation is named rather than silently absorbed.
- Two observations from Arm T were carried into Routing as context, explicitly not as verdict evidence: (1) the `phase=teardown inflight=false` line is the first live witness of plan 34.5-34's cancellation-window instrumentation, but does not retire `U-34.5-09` since no capture was ever reached to exercise that path; (2) the absence of any visible error text on Arm T (versus the first contract's Arm B error page) narrows the next instrumentation pass toward a silently-throwing script, not a rejected request — noting this session ran with the stock (unset) UA, so it is not a controlled re-run of Arm B.
- `U-34.5-06` (Epic's success path end to end) is stated explicitly as **still OPEN** — this experiment measured form interactivity under two shells, not a completed Tauri-side login; `status=captured` was never reached on either Tauri attempt.

## Deviations from Plan

None — plan executed exactly as written. The plan's own Task 3 scope anticipated and pre-authorized recording a contract-precondition deviation as evidence (see the first contract's own precedent of recording its `len=52` arithmetic defect rather than silently correcting it); this was followed, not an unplanned deviation from this plan's own execution.

## Issues Encountered
None.

## Verification
- `npx tsc --noEmit` — exit 0 (unchanged from baseline).
- `npm run test:ci` — 3547/3547 across 181 suites (unchanged from baseline; this plan changed no `.ts`/`.tsx`/`.rs` file).
- `git diff --name-only` for this plan's two commits (`0b982d23c`, `1afac838b`) lists exactly one file: `34.5-G6-EPIC-DISCRIMINATOR-2.md`. Zero source files touched.
- No `34.5-LIVE-GATE-RERUN-2.md` was created. No `34.5-UNTESTED-ITEMS.md` row was retired.
- Every pasted log line was re-verified directly against `~/Library/Logs/GameLib/gamelib.log.old` (Arm E) and `~/Library/Logs/GameLib/gamelib.log` (Arm T); no discrepancy found in any line.

## User Setup Required
None. This plan is diagnosis-only; no external service configuration or environment change is needed.

## Next Phase Readiness
- **This plan ships no fix.** Per the E1 route, the next diagnostic step is a capability that does not yet exist: relaying the Tauri login window's own `console.error`/`unhandledrejection` (or equivalent script-error) signal back to `gamelib.log`, to name a specific throwing API rather than infer one. That instrumentation is not authorised by this plan and needs its own scoping.
- **Plans 34.5-29/30/31 remain HALTED** per the standing `BINDING DECISION: fix-first` from `34.5-G6-EPIC-DISCRIMINATOR.md` — this plan did not touch, author, or execute any part of them.
- `34.5-CYCLE4-ROUTING.md` item 5 (Epic further diagnosis) is now answered with a verdict (E1) but not closed with a fix — a further instrumentation contract is the named next step.
- `U-34.5-06` and `U-34.5-09` both remain OPEN, unretired by this plan's observations, with their existing retirement conditions unchanged.
- This appears to be the last plan in gap cycle 4's wave 2; the phase does not reach its blocking five-item gate this cycle (unchanged from the standing `fix-first` decision).

## Self-Check: PASSED

- FOUND: `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR-2.md`
- FOUND commit `0b982d23c`
- FOUND commit `1afac838b`

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-08-01*
