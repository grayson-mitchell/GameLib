---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 60
type: execute
wave: 6
gap_closure: true
cycle: 7
depends_on: ["34.5-57", "34.5-58", "34.5-59"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UAT.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE8-ROUTING.md
autonomous: true
requirements: [REQ-34.5-11, REQ-34.5-12, REQ-34.5-13]

must_haves:
  truths:
    - "Every document that records Phase 34.5's status agrees with the fifth gate's own frontmatter verdict — no document lags, and none overstates."
    - "Every ledger row is resolved by the observation named in its OWN cell, one row at a time."
    - "Phase 34.6's and Phase 35's dependency lines on Phase 34.5 are edited if and only if the verdict is a clean 4 of 4."
    - "On a FAIL, gap cycle 8's routing document exists with a work-list built from the run's own non-PASSing items and undiagnosed findings."
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md"
      provides: "Row-by-row resolution against the fifth gate, with the running count reconciled"
      contains: "plan 34.5-59"
    - path: ".planning/STATE.md"
      provides: "Current Position replaced with this run's outcome, prior banner preserved as history"
      contains: "34.5-LIVE-GATE-RERUN-4.md"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN-4.md frontmatter verdict"
      to: "STATE.md, ROADMAP.md, the ledger and PORTED-CHANNELS.md"
      via: "a single-source propagation performed in one plan, after the review, never before it"
      pattern: "verdict:"
---

<objective>
Propagate the fifth blocking gate's verdict everywhere, and resolve the ledger row by row.

Purpose: this phase has four completed gate runs whose outcomes had to be written into six
different documents each time. A half-done reconciliation is how a stale `status: failed` re-plans
completed work, and how a resolved item keeps counting against an audit.

Output: every status document in agreement with the gate's own frontmatter, and — on a FAIL —
`34.5-CYCLE8-ROUTING.md`.
</objective>

<ordering_note>
This plan is wave 6, deliberately AFTER plan 34.5-57's code review (wave 3). A code review that runs
after the reconciliation sweep is **unledgered by construction** — measured three cycles running on
a sibling phase. If `34.5-CYCLE7-REVIEW.md` named a finding and asked plan 34.5-60 to ledger it,
Task 2 does so.
</ordering_note>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-ROUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Propagate the verdict to STATE.md, ROADMAP.md, PORTED-CHANNELS.md and UAT.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UAT.md</files>
  <read_first>
    - .planning/phases/.../34.5-LIVE-GATE-RERUN-4.md (frontmatter and the Verdict section — the
      SINGLE source of truth for every edit in this task)
    - .planning/STATE.md frontmatter and the `## Current Position` block only (the file is ~707KB;
      do not read it whole)
    - .planning/ROADMAP.md § Phase 34.5, § Phase 34.6 and § Phase 35 (specifically their dependency
      lines on Phase 34.5)
    - .planning/phases/.../34.5-PORTED-CHANNELS.md (the per-channel honest proof levels)
    - .planning/phases/.../34.5-UAT.md (currently `status: partial`)
  </read_first>
  <action>
Read the verdict ONCE from `34.5-LIVE-GATE-RERUN-4.md`'s frontmatter and drive every edit below from
it. Do not restate it from memory or from a SUMMARY.

**STATE.md.** Replace the `## Current Position` banner with this run's outcome, and preserve the
existing run-4 banner below it under a `--- historical: run 4's own banner follows ---` marker,
exactly as the file already does for runs 3 and earlier. The new banner states: the verdict and the
four counts; which items passed and which did not; which ledger rows moved and which stayed open;
and the next command. Update `stopped_at` and `last_activity`.

**Do not use `gsd-sdk state.*` write verbs for any of this.** This project has a measured, recurring
defect in which every `state.*` write verb corrupts `STATE.md`, most recently rewriting a `percent`
field from 94 to 63 while its own `updated:` list under-reported the change. Edit by hand,
`git diff` the WHOLE file afterwards, and paste that diff's stat into the SUMMARY. Adjust
`progress.total_plans` by the nine plans this cycle added (34.5-52..60) and
`progress.completed_plans` by the number actually executed; recompute `percent` by hand and show the
arithmetic.

**ROADMAP.md § Phase 34.5.** Update the plan count and append the nine cycle-7 plan lines with their
tick state. Add a dated paragraph recording gap cycle 7's outcome. **Do not mark Phase 34.5 complete
unless the verdict is a clean 4 of 4.**

**Phase 34.6's and Phase 35's dependency lines on Phase 34.5** are editable **ONLY** on a clean
4-of-4, per the contract's own arithmetic rule. On any other result, leave both byte-unchanged and
state in the SUMMARY that they were deliberately not touched.

**34.5-PORTED-CHANNELS.md.** Update the honest proof level of every channel whose level this run
changed — and only those. A channel exercised live this run moves to live-gate-proven; a channel
still only unit-proven stays unit-proven and says so plainly. **No reader may take "wired and
unit-proven" for "seen working."** Record explicitly which SteamGridDB and winetricks channels are
now *honestly declining* under Tauri (source-level, plan 34.5-55) versus *seen declining live* —
they are different claims and the second one is `U-34.5-32`, not this document's to assert.

**34.5-UAT.md.** Update `status:` and record which UAT tests this gate's items covered and which
remain. Note that UAT tests 11 (Epic half), 12 (`egsSync`) and 13 (legendary save sync) travelled to
Phase 34.7 with D-CYCLE6-A and are not this phase's to close; Amazon's half of test 11 stays here.
  </action>
  <verify>
    <automated>grep -c "34.5-LIVE-GATE-RERUN-4.md" .planning/STATE.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `STATE.md`'s `## Current Position` names `34.5-LIVE-GATE-RERUN-4.md`, its verdict and the four
      counts, and the run-4 banner is preserved below a historical marker.
    - `STATE.md`'s `progress` block is internally consistent: `percent` equals
      `round(completed_plans / total_plans * 100)`, with the arithmetic shown in the SUMMARY.
    - The full `git diff .planning/STATE.md` was inspected and its stat pasted into the SUMMARY;
      no field outside the intended set changed.
    - ROADMAP § Phase 34.5 lists nine new plan lines (34.5-52..60) with correct tick state and a
      dated cycle-7 paragraph.
    - If the verdict is not a clean 4 of 4: `git diff .planning/ROADMAP.md` shows NO change to Phase
      34.6's or Phase 35's dependency lines, and Phase 34.5 is not marked complete.
    - `34.5-PORTED-CHANNELS.md` distinguishes source-level honest decline from live-observed decline
      for the SteamGridDB and winetricks clusters.
    - `34.5-UAT.md`'s `status:` reflects the run.
  </acceptance_criteria>
  <done>Every status document agrees with the gate's own frontmatter, and no document overstates what the run proved.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the ledger row by row, each on the observation named in its own cell</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</files>
  <read_first>
    - .planning/phases/.../34.5-UNTESTED-ITEMS.md (the WHOLE file — Rules 1-3, every row, and the
      running count)
    - .planning/phases/.../34.5-LIVE-GATE-RERUN-4.md § "Known-holes resolution" (the run's own
      per-row findings)
    - .planning/phases/.../34.5-CYCLE7-REVIEW.md (any finding it asked this plan to ledger, and the
      id it named)
    - .planning/phases/.../34.5-KEYRING-ARM-SESSION.md (which of `U-34.5-01`/`U-34.5-10` moved, and
      on which of their own bars)
  </read_first>
  <action>
Reconcile `34.5-UNTESTED-ITEMS.md` against the gate's own resolution table, **one row at a time**.

Ledger Rule 1 is binding and is the whole point of this task: **a row retires ONLY by the
observation named in its own cell** — never by a passing test suite, never by inference from a
neighbouring row, and never because the gate item containing it passed overall. Run 4 demonstrated
both directions in a single run: `U-34.5-07` retired on its own clause while its parent item scored
FAIL, and `U-34.5-05` stayed open despite years of adjacent evidence. Score each row against its own
cell and nothing else.

For each row, write one of exactly four dispositions into its Status cell, dated, naming plan
34.5-59 (or 34.5-58 for the keyring rows) as the source:
  - **RETIRED**, quoting the exact observation that closed it, redacted in place if it carries any
    value class.
  - **STILL OPEN**, stating what was and was not shown for it this run.
  - **NOT COVERED BY THIS GATE**, naming the owner.
  - **STILL OPEN, blocked_on: operator-supplied test fixture** — for `U-34.5-29` and `U-34.5-27`
    only, per D-CYCLE7-B. Confirm both retain that annotation and both remain OPEN. Out of scope is
    not retirement, and this cycle's decision does not change either row's own bar.

Pay specific attention to the rows cycle 7 touched:
  - `U-34.5-05` — retargeted to the in-page banner by plan 34.5-52. Its bar is a before/after
    transcription of the BANNER. Score it on that, not on the title bar and not on `U-34.5-31`'s
    evidence.
  - `U-34.5-31` — the banner's spoofability and live-unobserved state. Its bar is three conjunctive
    conditions (banner visible with the real origin, cancel strip simultaneously visible and
    clickable, text changes on origin change). A partial satisfaction is not a retirement.
  - `U-34.5-25` — already RETIRED by plan 34.5-55 on the source-level sweep. Do not re-open it and
    do not re-credit it to this gate.
  - `U-34.5-32` — the live decline for SteamGridDB/winetricks. No gate item drives those surfaces;
    expect NOT COVERED unless a cross-cutting observation happened to reach one.
  - `U-34.5-30` — the DXVK toggle. Retires only on a genuine OFF→ON click producing
    `installing dxvk-macOS on...` plus the `current_dxvk-macOS` marker afterward.
  - `U-34.5-03` — research Pitfall 2. Retires only on an item-4 PASS. A NOT ATTEMPTED retires
    nothing; say so explicitly rather than leaving the row silent.
  - `U-34.5-22` — the uninstall tile-flip. If the observation was gathered, score it; if refused,
    record the refusal. A non-reproduction is recorded as a non-reproduction, never as a retirement.
  - `U-34.5-01` / `U-34.5-10` — already scored by plan 34.5-58 on their own bars. Confirm this
    plan does not re-score them from gate evidence, and that they were not retired together unless
    both bars were independently met.

If `34.5-CYCLE7-REVIEW.md` asked this plan to ledger a finding, add that row here with the id the
review named.

Close with a dated update paragraph stating the before and after counts and listing, by id, every
row that moved and in which direction. State the arithmetic so a reader can check it:
`opened - retired = delta`.
  </action>
  <verify>
    <automated>grep -c "plan 34.5-59\|plan 34.5-58" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</automated>
  </verify>
  <acceptance_criteria>
    - Every row in the table has a dated disposition referencing this cycle, or an explicit
      "untouched by this run" note — no row is silently left as it was.
    - `U-34.5-29` and `U-34.5-27` both still carry `blocked_on: operator-supplied test fixture` and
      are both still OPEN.
    - `U-34.5-25` is not re-opened and is not re-credited to the gate.
    - `U-34.5-01` and `U-34.5-10` carry independent dispositions; neither cites the other's evidence.
    - The closing paragraph states before count, after count, and the `opened - retired = delta`
      arithmetic, and the stated after-count equals an actual recount of the table's rows.
    - No row cites "the suite is green" or "its parent item passed" as a retirement basis.
  </acceptance_criteria>
  <done>Every row is scored on its own bar, and the running count reconciles against a recount rather than a transcription.</done>
</task>

<task type="auto">
  <name>Task 3: Close the phase on a clean 4/4, or author 34.5-CYCLE8-ROUTING.md</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE8-ROUTING.md (Branch B only), .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-ROUTING.md (Branch A only)</files>
  <read_first>
    - .planning/phases/.../34.5-LIVE-GATE-RERUN-4.md (verdict, verdict table, "What this gate
      falsifies", the contract-defect tally, and the new-findings section)
    - .planning/phases/.../34.5-CYCLE7-ROUTING.md (the shape a routing document takes in this
      phase: a work-list of (id, creating plan) pairs, an explicit out-of-scope table, and the
      standing rules the next cycle inherits)
    - .planning/phases/.../deferred-items.md (this run's own findings section, added by plan 34.5-59)
  </read_first>
  <action>
**Branch A — the verdict is a clean 4 PASS / 0 FAIL / 0 BLOCKED / 0 NOT ATTEMPTED.**

Do not create `34.5-CYCLE8-ROUTING.md`. Instead:
  - Mark `34.5-CYCLE7-ROUTING.md`'s frontmatter `status: closed` and add a closing paragraph naming
    which of its ten items closed and how, and which two (items 5 and 9) were scoped out by
    D-CYCLE7-B with their `blocked_on` fixtures still outstanding.
  - Record in the SUMMARY that Phase 34.6's and Phase 35's dependency lines are NOW editable, and
    that Task 1 performed that edit — the ONE condition under which it is permitted.
  - Record the standing residuals that survive a PASS and must not be read as closed:
    `R-34.5-G1-PKG` (the packaged-build asset root — a dev PASS never retires the packaged half of
    research Assumption A2); the 16 deferred channels owned by Phase 34.6; `getDefaultSavePath`;
    Epic / `egsSync` / legendary save sync owned by Phase 34.7; and every ledger row still OPEN
    after Task 2, listed by id.
  - Note that `/gsd-secure-phase 34.5` is owed, consistent with this project's standing practice of
    tracking that obligation per closed phase.

**Branch B — anything else.**

Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE8-ROUTING.md`,
modelled on `34.5-CYCLE7-ROUTING.md`'s own structure:
  - **Why there is a cycle 8**: the verdict and the four counts, the arithmetic reconciliation, and
    D-08 restated verbatim. State plainly: never a forced close, never an advisory note, never a
    deferred UAT entry, never a pre-authorised override, and never a change to Phase 34.6's or
    Phase 35's dependency lines on Phase 34.5.
  - **What passed, stated so it is not re-litigated**, with the standing claims each PASS struck.
  - **Work-list**, one row per non-PASSing item plus every undiagnosed finding from this run, each
    named by its existing `(id, creating plan)` pair. **Diagnose nothing here** — that is cycle 8's
    own planning work.
  - **Explicitly out of scope**, carrying cycle 7's table forward unchanged and adding
    `U-34.5-29` and `U-34.5-27` with their `blocked_on: operator-supplied test fixture` annotations,
    so a future cycle picks them up the moment the fixture exists.
  - **Standing rules inherited**, carrying cycle 7's six forward, and adding any this run
    demonstrated afresh — in particular, record whether the seven-test Structural Reachability
    Review held (the contract-defect tally from plan 34.5-59), since Tests 6 and 7 were applied for
    the first time in this cycle and their result is the first data point on whether they work.

In both branches, verify one last time that plans `34.5-29`, `34.5-30` and `34.5-31` are
byte-unchanged and are referenced nowhere as pending work. They are HALTED and SUPERSEDED, have no
SUMMARY by design, and are deliberately left unticked on disk as the record of their own halt. A
stale unticked record is how this project has previously re-planned completed work.
  </action>
  <verify>
    <automated>git diff --stat .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-29-PLAN.md .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-30-PLAN.md .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-31-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one branch is taken, and which one is stated in the SUMMARY with the verdict that
      selected it.
    - Branch A only: `34.5-CYCLE7-ROUTING.md` is `status: closed` with a closing paragraph naming
      the disposition of all ten of its items; `34.5-CYCLE8-ROUTING.md` does NOT exist; the surviving
      residuals are listed by id; `/gsd-secure-phase 34.5` is recorded as owed.
    - Branch B only: `34.5-CYCLE8-ROUTING.md` exists with all five sections; its work-list has one
      row per non-PASSing item plus one per undiagnosed finding, each carrying an `(id, creating
      plan)` pair; nothing in it is diagnosed; and ROADMAP's Phase 34.6 / Phase 35 dependency lines
      are byte-unchanged.
    - Both branches: the contract-defect tally from plan 34.5-59 is carried into the routing or
      closing record as an integer, with a note that Tests 6 and 7 were applied for the first time
      this cycle.
    - `git diff --stat` on `34.5-29-PLAN.md`, `34.5-30-PLAN.md` and `34.5-31-PLAN.md` produces NO
      output across this entire cycle.
    - `grep -rn "34.5-29-PLAN\|34.5-30-PLAN\|34.5-31-PLAN"` across this cycle's new documents
      returns matches only where they are described as HALTED/SUPERSEDED, never as pending work.
  </acceptance_criteria>
  <done>The phase either closes cleanly with its residuals named, or routes to cycle 8 with a work-list built from evidence and nothing prematurely diagnosed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate frontmatter -> six status documents | A single verdict fans out; any document that lags becomes a false record |
| ledger -> future planning cycles | A stale `status: failed` row re-plans completed work; a resolved item that is never moved keeps counting against an audit |
| Phase 34.5 -> Phase 34.6 / Phase 35 | Their dependency lines are the downstream gate; editing them early would let a non-closing phase unblock the cutover |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C7-36 | Tampering | `.planning/STATE.md` | mitigate | `gsd-sdk state.*` write verbs are BANNED for this plan — a measured, recurring defect corrupts fields outside their own reported `updated:` list (most recently `percent` 94 -> 63). Hand-edit, `git diff` the whole file, paste the stat, and show the `percent` arithmetic. |
| T-34.5-C7-37 | Elevation of privilege | Phase 34.6 / Phase 35 dependency lines | mitigate | Editable ONLY on a clean 4-of-4, per the contract's own arithmetic rule. On any other verdict the acceptance criteria require `git diff` to show them byte-unchanged. |
| T-34.5-C7-38 | Repudiation | ledger rows resolved by inference | mitigate | Ledger Rule 1 applied per row, with an explicit criterion forbidding "the suite is green" or "its parent item passed" as a retirement basis, and a recount reconciling the closing count. |
| T-34.5-C7-39 | Spoofing | a stale HALTED plan read as pending | mitigate | `34.5-29/30/31-PLAN.md` asserted byte-unchanged by a diff command in the verify block, plus a grep confirming every reference to them describes them as halted. This project has re-planned completed work from exactly this shape. |
| T-34.5-C7-40 | Information disclosure | the propagated banners and routing text | mitigate | Verdict prose may quote log excerpts. The phase's seven-class redaction sweep runs over the staged diff before commit; the published-SteamID64 exception applies and nothing else does. |
| T-34.5-C7-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package and touches no manifest. |
</threat_model>

<verification>
- `STATE.md`, `ROADMAP.md`, `34.5-UNTESTED-ITEMS.md`, `34.5-PORTED-CHANNELS.md` and `34.5-UAT.md`
  all agree with `34.5-LIVE-GATE-RERUN-4.md`'s frontmatter verdict.
- The full `STATE.md` diff was inspected and its stat pasted; `percent` arithmetic shown.
- Ledger closing count matches a recount; every row has a dated disposition.
- Exactly one of Branch A / Branch B was taken.
- `34.5-29/30/31-PLAN.md` byte-unchanged.
- Redaction sweep output pasted.
</verification>

<success_criteria>
- No document lags the verdict and none overstates it.
- Every ledger row is resolved on its own bar.
- Downstream dependency lines move only on a clean 4 of 4.
- On a FAIL, cycle 8 starts from a work-list of evidence, not of diagnoses.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-60-SUMMARY.md` when done.
</output>
