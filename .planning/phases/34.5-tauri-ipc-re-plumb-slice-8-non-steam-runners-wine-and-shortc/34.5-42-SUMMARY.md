---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 42
type: execute
cycle: 5
gap_closure: true
completed: 2026-08-02
---

# Plan 34.5-42 — Propagate the third gate's verdict and close gap cycle 5

**The third blocking live gate recorded `verdict: FAIL` — `items_passed: 0`, `items_failed: 2`, `items_blocked: 1`, `items_not_attempted: 2`, reconciling to 5 — and per D-08 Phase 34.5 does NOT close.**

Every document that tracks this gate now agrees with that record, and none of them softens it.

## What was done

| File | Change |
|---|---|
| `34.5-PORTED-CHANNELS.md` | All 10 gate-dependent `LIVE (item N)` cells re-pointed at `34.5-LIVE-GATE-RERUN-2.md`; `## Accepted residuals` updated for A1, A2 (both call sites), Pitfall 2, `R-34.5-G1-PKG` |
| `deferred-items.md` | Dispositions for items 6–11 (F-34.5-G6-01..06); new items 13–22 for F-34.5-G6-07..15 plus the REQUIREMENTS.md inconsistency. Items 1–12 unrenumbered |
| `IPC-PORT-INVENTORY.md` | Phase 34.5 status names all three runs and this verdict; F-34.5-G6-10 recorded as unknown-extent incompleteness. Channel membership and 38/3/16 arithmetic untouched |
| `ROADMAP.md` | Status paragraph rewritten; Plans line recounted from disk; 34.5-29/30/31 annotated-not-ticked; 34.5-42 ticked |
| `34.5-UNTESTED-ITEMS.md` | 11 rows resolved; `U-34.5-09` RETIRED; 4 new rows `U-34.5-12..15` added under Rule 2 |
| `34.5-CYCLE5-ROUTING.md` | `## Outcome` filled — counters verbatim, both applicable branches named |
| `STATE.md` | Hand-edited: frontmatter + `## Current Position` only |

## Ledger disposition, by ID

**RETIRED (1):** `U-34.5-09` — the gate captured its exact observation: two consecutive GOG logins, each `status=captured` (21:30:30, 21:31:41) followed by `phase=idle (login completed, library refresh triggered)` (21:30:41, 21:31:51), zero silent returns to Manage Accounts.

**STAYED OPEN (10):** `U-34.5-01`, `-02`, `-03`, `-04`, `-05`, `-06`, `-07`, `-08`, `-10`, `-11` — each with one sentence on what this run did and did not show.

**CREATED (4, Rule 2, same commit as the findings):** `U-34.5-12` (dead `nativeImage` stub), `U-34.5-13` (shortcut launches the launcher, not the game), `U-34.5-14` (inventory completeness — a Phase 35 precondition), `U-34.5-15` (`addToSteam` drops its return value).

**`U-34.5-01`'s four conditions were evaluated mechanically, not by judgement:** (1) `GAMELIB_DEV_SECRET_VAULT` unset ✓, (2) `[bootstrap] secret stores: keyring` present ✓, (3) zero `[dev-secret-vault]` lines ✓, (4) ≥1 successful `SidecarKeyringSlotStore` read ✗ — zero successes, two `keyring:timeout`. Stays OPEN.

**`U-34.5-01` and `U-34.5-10`'s REASON changed and this is recorded everywhere it appears.** The cycle's plans anticipated a `dev-vault` run under D-CYCLE5-B; the run's session A actually used the real **keyring** arm. Both rows still stay OPEN, but on observation rather than by construction. Plan 34.5-35's raised constants are now live-**OBSERVED** (`ms=120000`; read bounded ~45 s after the 21:23:55 boot) but are **NOT** live-PROVEN, and no document in this cycle describes them as proven.

## Four-counter test (Task 1 point 4 / Task 3 point 1 — the same test, applied once)

```
items_passed: 0   items_failed: 2   items_blocked: 1   items_not_attempted: 2
clean 5-of-5 => False  =>  dependency lines NOT editable
```

Phase 34.6's and Phase 35's dependency lines were therefore left untouched. **Proven, not asserted:** `git diff -U0 .planning/ROADMAP.md` produced hunks at 1545, 1557, 1599→1628 and 1638→1667, while `### Phase 34.6` begins at line 1669. No hunk reaches either section.

## Deviations and conflicts found in the plan's own criteria

Three, all recorded rather than worked around silently:

1. **`ported-channels-gate.py` vocabulary conflict.** Task 1 requires a BLOCKED item's cell to "say BLOCKED with its cause — never PENDING, never FAIL", but `ported-channels-gate.py:150` permits only `^(unit|unit \+ LIVE \(item \d+\) — (PENDING|PASS|FAIL))$`, and the same task requires that script to exit 0. Resolved by keeping `PENDING` in the proof cell (the script's only not-yet-a-result state, and BLOCKED / NOT ATTEMPTED genuinely count toward neither passed nor failed) and carrying the disposition plus its cause in the free-text notes column. Gate and `--self-test` both exit 0.

2. **Task 3's next-action grep is unsatisfiable as literally written.** Its verify block wants `grep -Ec "gsd-verify-work|gsd-plan-phase 34.5 --gaps" .planning/STATE.md` to equal exactly 1, but STATE.md legitimately contains `/gsd-verify-work 34` (L2493) and `/gsd-verify-work 23` (L2925) inside historical blocks that the same task forbids touching. The intent was satisfied — **this cycle's** next action appears exactly once, at L484 in `## Current Position` — and history was left intact. Count is 3; gaming it to 1 would have required corrupting historical records.

3. **Plan 41 had already written some STATE.md/ROADMAP.md propagation** (`6645a2fa0`). That content was read first and extended, not duplicated or contradicted.

## Credential sweep

`grep -Ec "token=|code=|Cookie|userdata"` across every file this cycle touched:

```
34.5-PORTED-CHANNELS.md    0    deferred-items.md          0
34.5-UNTESTED-ITEMS.md     0    34.5-CYCLE5-ROUTING.md     0
34.5-LIVE-GATE-RERUN-2.md 14    34.5-G6-ITEM45-PREFLIGHT.md 8
IPC-PORT-INVENTORY.md      0    ROADMAP.md                 0
STATE.md                  23
```

Unredacted-value count (`userdata/[0-9]`, `access_token=<value>`, `refresh_token=<value>`, `code=<8+ chars>`): **0 in every file this cycle touched.** All matches are redaction examples, the literal word "userdata" in prose, or `sessionCookie` inside absence assertions.

### ⚠ Pre-existing leak found by the sweep — NOT introduced by this cycle, NOT fixed by it

A real Steam account id, **`userdata/<REDACTED — a real Steam account id>`**, is committed **unredacted** in four files:

- `34.5-19-PLAN.md:191`
- `34.5-20-PLAN.md:160`
- `34.5-LIVE-GATE.md:464` and `:471`
- `34.5-LIVE-GATE-RERUN.md:585`

This is exactly what `T-34.5-C5-06` and `T-34.5-G6-02` exist to prevent. Gap cycle 5's own documents redact it correctly (`userdata/<REDACTED>`), which is why the newer files are clean — the leak predates the redaction discipline.

**Publication status, checked 2026-08-02 — NOT yet public, but on track to be.** The fork's published head (`fd5a81fefa`, `gamelib/fix/steam-native-install-stability`) does **not** contain the string; all four leaking commits (`49fdaad79`, `c2b283ec5`, `cf5533832`, plus this plan's own now-redacted one) sit *after* it, and the local branch is **720 commits ahead** of the fork. However `.planning/` **is** tracked and already published (974 files on the fork), so the next push publishes these files. The earliest leaking commit is `49fdaad79`; rewriting `fd5a81fefa..HEAD` covers **262 commits, none of them published**, so no shared history would be disturbed.

**What the value discloses:** the value is a SteamID3 account id, which resolves arithmetically to a SteamID64 and thus to a public Steam profile URL. It is an **identifier, not a credential** — it grants no access. The exposure is identity linkage (GitHub ↔ Steam profile), not account compromise.

**Not fixed here, deliberately:** none of the four is in this plan's `files_modified`, and `34.5-LIVE-GATE.md` / `34.5-LIVE-GATE-RERUN.md` are explicitly forbidden from being edited — they are the completed records of their own runs. Redacting the working tree alone would not remove the value from the four commits, which is what a push actually publishes. **This needs a developer decision BEFORE the next push to `gamelib`, and should be scoped into gap cycle 6.**

(The `?code=SECRETVALUE` matches in `34.5-24-PLAN.md` / `34.5-24-SUMMARY.md` are deliberate test placeholders, not secrets.)

## Verification

- `ported-channels-gate.py` → exit 0; `--self-test` → exit 0 (both re-run after every edit to `34.5-PORTED-CHANNELS.md` and `IPC-PORT-INVENTORY.md`)
- `IPC-PORT-INVENTORY.md` channel-list lines changed: **0**; 38/3/16 arithmetic unchanged
- `34.5-LIVE-GATE.md`, `34.5-LIVE-GATE-RERUN.md`, `34.5-29/30/31-PLAN.md`: byte-unchanged
- `STATE.md` diff confined to lines 6–8, 13–14, 481–485; `## Current Position` spans 34–2918, so no historical block was touched
- Phase-directory recount: 42 `*-PLAN.md`, 39 `*-SUMMARY.md` (the three without one are 34.5-29/30/31, halted and superseded)
- Session logs preserved against the mid-gate rotation: `~/Library/Logs/GameLib/gamelib.log.34.5-g6-gate3-sessionA` and `-sessionB`

## Self-Check: PASSED

All three tasks executed and committed individually. The verdict is recorded as FAIL everywhere it appears; nothing was softened, no ledger row was retired without the exact observation its own row names, and no downstream phase was unblocked. Phase 34.5 is **not** marked complete anywhere.
