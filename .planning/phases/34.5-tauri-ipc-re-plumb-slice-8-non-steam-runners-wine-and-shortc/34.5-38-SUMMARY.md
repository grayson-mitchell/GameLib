---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 38
subsystem: gap-cycle-routing
tags: [gap-cycle-5, routing, gate-authorisation, build-baseline]
dependency-graph:
  requires: [34.5-CYCLE4-ROUTING.md, 34.5-UNTESTED-ITEMS.md, 34.5-G6-EPIC-DISCRIMINATOR-2.md, epic-login-non-interactive.md]
  provides: [34.5-CYCLE5-ROUTING.md]
  affects: [34.5-39-PLAN.md, 34.5-40-PLAN.md, 34.5-41-PLAN.md, 34.5-42-PLAN.md]
tech-stack:
  added: []
  patterns: [pre-registered routing contract, verbatim-quote provenance, measured-not-transcribed build floor]
key-files:
  created:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md
  modified: []
decisions:
  - "D-CYCLE5-A and D-CYCLE5-B recorded in writing, quoted verbatim, as the sole authorisation for gap cycle 5's gate-first approach and dev-secret-vault trade"
metrics:
  duration: "~40 minutes (including live build-health measurement)"
  completed: 2026-08-02
---

# Phase 34.5 Plan 38: Open gap cycle 5 — gate authorisation, plan disposition, measured build floor Summary

Wrote `34.5-CYCLE5-ROUTING.md`: a written authorisation naming gap cycle 5 as the gate cycle (D-CYCLE5-A/B quoted verbatim), a non-destructive disposition of plans 34.5-29/30/31 (all three left byte-unchanged on disk, their work re-homed to 34.5-39/40/41/42 by reference), a routing table covering all four gate-verdict branches including the item-1-BLOCKED path, and a build-health baseline measured live at HEAD `c8f314205` rather than transcribed from any prior document.

## What Was Built

**Task 1 — `34.5-CYCLE5-ROUTING.md` authorisation and disposition (all seven prose sections).** Quoted D-CYCLE5-A (gate-first, Epic item 1 enters BLOCKED with its parked pre-auth cause named) and D-CYCLE5-B (the run uses `GAMELIB_DEV_SECRET_VAULT=1`, keeping `U-34.5-01`/`U-34.5-10` OPEN) verbatim from the plan's own `<interfaces>` block. Dispositioned all three halted plans without editing any of them: 34.5-29 Task 1 (the Epic UA fix) is VOID by three independent gates (its own `fix-first` self-halt, its own `R1-FALSIFIED` verdict branch, and the second discriminator's diagnosis-only `E1` routing plus the debug file's `USER_AGENTS`/`EPIC_LOGIN_URL`/`matchOAuthRedirect` constraint); its Tasks 2/3 (the item-4/item-5 preflight) are re-homed to plan 34.5-39. 34.5-30 is superseded by 34.5-40; 34.5-31 by 34.5-41 (run+verdict) and 34.5-42 (propagation). Named Epic's BLOCKED cause precisely: the post-auth navigation-refusal fix is confirmed and implemented but static-only (commit `c857ade8e`, ledger row `U-34.5-11`); the pre-auth blocker is parked by explicit developer override with a measured shape of 403 x1 / `xhr.timeout` x2 / success x1 across Tauri attempts and zero completed Tauri Epic logins; and the debug file's own new risk (the production `window.fetch` wrapper using the same technique suspected of causing the 403) is carried forward as "must be checked FIRST." Wrote the four-branch routing table, marking the 5/5 branch EXPECTED-unreachable but explicitly not unreachable-by-construction (plan 34.5-40's opportunistic-attempt clause can still drive item 1 to a real PASS/FAIL), and closed it with a binding note forbidding downstream acceptance criteria from presupposing non-closure.

**Task 2 — measured build-health baseline, appended to the same file.** Ran all six required commands live at HEAD `c8f314205` rather than transcribing prior numbers: `npm run test:ci` (181/181 suites, 3553/3553 tests, exit 0 — zero failures, including the WR-08 guard the debug file had recorded as a known pre-existing failure), `npx tsc --noEmit` (exit 0), `cargo check` (exit 0), `cargo test` (92 passed, 0 failed, 1 ignored, exit 0 — matches the debug file's Rust count exactly), and `ported-channels-gate.py` plus its `--self-test` (both exit 0). Determined, rather than assumed, that the WR-08 stripper-integrity guard failure is now CLOSED at this HEAD by confirming both raw-string-aware fix commits (`d22fe0dfd`, `50a807a56`) are ancestors of HEAD and observing zero jest failures directly. Recorded an honest, unresolved discrepancy: the jest count (3553) is 5 higher than the debug file's 3548, but `git diff --stat c857ade8e..HEAD` shows zero changes under `src/` or `src-tauri/` in that range — the delta is not explained by any commit and is recorded as such rather than invented.

## Deviations from Plan

None — plan executed exactly as written. One process note: Tasks 1 and 2 were both authored in a single `Write` call (rather than a `Write` for Task 1 followed by an `Edit` appending Task 2's section) because a transport error interrupted the prior turn immediately before writing, and the recovery instruction from the coordinator was to write the file in one shot to eliminate truncation risk. Both tasks' content and acceptance criteria are fully present in the single resulting file and are committed together in one commit (`0c70cc3bf`) rather than two, since both tasks produce sections of the same single new artifact.

## Self-Check

- `FOUND: .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md`
- `FOUND: 0c70cc3bf` (commit hash verified present in `git log --oneline --all`)
- All plan-mandated markers confirmed present via grep: `## Authorisation`, `## Disposition of plans`, `## Why the preflight`, `## Item 1 (Epic) enters BLOCKED`, `## Routing by verdict`, `## Ledger interaction`, `## Standing rule`, `## Outcome`, `## Measured baseline`, `D-08`, `c857ade8e`, `U-34.5-11`, `403 x1`, `xhr.timeout`, `success x1`, `window.fetch`, `GAMELIB_DEV_SECRET_VAULT=1`.
- `git status --porcelain | grep -Ec "34.5-(29|30|31)-PLAN.md"` returns `0` — all three halted plans remain byte-unchanged.
- `git diff --name-only HEAD~1 HEAD | grep -E "\.ts$|\.tsx$|\.rs$"` returns zero matches — no code touched.

## Self-Check: PASSED

## Commits

- `0c70cc3bf` — docs(34.5-38): open gap cycle 5 — authorise the blocking gate, disposition 29/30/31, measure HEAD
