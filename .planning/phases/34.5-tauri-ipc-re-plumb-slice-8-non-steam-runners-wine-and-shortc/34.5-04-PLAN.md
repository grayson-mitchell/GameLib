---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/sidecar/runnerAuthFlowRegistration.ts
  - src/backend/sidecar/wineToolsFlowRegistration.ts
  - src/backend/sidecar/shortcutsFlowRegistration.ts
  - src/backend/sidecar/runnerMiscFlowRegistration.ts
  - src/backend/sidecar/handlers.ts
  - src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts
autonomous: true
requirements: [REQ-34.5-13]

must_haves:
  truths:
    - "This slice's four registration modules exist and are wired into `handlers.ts` before any channel body is written, so every later cluster plan edits exactly one module file and the waves can run in parallel."
    - "Each module declares, in its own docstring, the exact channel names it will own and their registration KIND — 34 invoke, 4 send (`logoutGOG`, `addShortcut`, `processShortcut`, `removeShortcut`) — so a mis-registration is a documentation contradiction, not an invisible transport bug."
    - "Curated-import discipline holds from the first commit: no module side-effect-imports any `*/ipc_handler.ts` or `main.ts`, because those register onto Electron's real `ipcMain` via `backend/ipc` and drag in deferred winetricks/EOS channels."
    - "REQ-34.5-13: `npm start` and `pnpm tauri:dev` both still work after this plan, and SEAM Invariant B is untouched — no channel that was rejecting stops rejecting yet."
  artifacts:
    - path: "src/backend/sidecar/runnerAuthFlowRegistration.ts"
      provides: "registerRunnerAuthFlows() — the 11 auth/sign-out channels' home"
      exports: ["registerRunnerAuthFlows"]
    - path: "src/backend/sidecar/wineToolsFlowRegistration.ts"
      provides: "registerWineToolsFlows() — the 9 Wine channels' home"
      exports: ["registerWineToolsFlows"]
    - path: "src/backend/sidecar/shortcutsFlowRegistration.ts"
      provides: "registerShortcutsFlows() — the 7 shortcuts channels' home"
      exports: ["registerShortcutsFlows"]
    - path: "src/backend/sidecar/runnerMiscFlowRegistration.ts"
      provides: "registerRunnerMiscFlows() — versions, other, saves-sync and runtime, 11 channels"
      exports: ["registerRunnerMiscFlows"]
    - path: "src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts"
      provides: "Wiring + curated-import guard for all four modules"
  key_links:
    - from: "src/backend/sidecar/handlers.ts"
      to: "the four new registration modules"
      via: "named import + call, following the registerSteamAuthFlows() precedent"
      pattern: "registerRunnerAuthFlows|registerWineToolsFlows|registerShortcutsFlows|registerRunnerMiscFlows"
---

<objective>
Create this slice's four registration modules as declared, wired, empty contracts and prove they are
reachable from `handlers.ts` — before any channel body exists.

Purpose: `handlers.ts` is the one file every cluster would otherwise contend for. Establishing the
four seams here means plans 05-12 each touch exactly one module file, so the remaining waves run in
parallel instead of serialising behind a shared import list. It also pins each cluster's channel
list and registration kind in source before the bodies are written.

Output: four modules that register nothing yet, and a test that fails the moment one of them is
forgotten or reaches for an `ipc_handler`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-RESEARCH.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md

<interfaces>
The house convention every registration module follows (src/backend/sidecar/steamAuthFlowRegistration.ts):
  export function registerSteamAuthFlows(): void { ipcMain.handle(...); ipcMain.on(...) }
  import { ipcMain } from './electronStub'
  import '../storeManagers'   // load-bearing FIRST import — forces storeManagers/index.ts to be the
                              // initialization entry, avoiding the re-entrant "X is not a constructor" crash

handlers.ts wiring shape (existing):
  import { registerSteamAuthFlows } from './steamAuthFlowRegistration'
  ...
  registerSteamAuthFlows()

The 38 channels this slice owns, with their VERIFIED Electron registration kind and source line:
  runnerAuth (11): login m.ts:877 H · getUserInfo m.ts:868 H · isLoggedIn m.ts:875 H ·
    getEpicGamesStatus m.ts:772 H · logoutLegendary m.ts:879 H · authGOG m.ts:878 H ·
    logoutGOG m.ts:880 SEND · getAmazonLoginData m.ts:882 H · authAmazon m.ts:883 H ·
    getAmazonUserInfo m.ts:872 H · logoutAmazon m.ts:884 H
  wineTools (9): runWineCommand m.ts:766 H · getAlternativeWine m.ts:973 H ·
    wine.isValidVersion m.ts:1532 H · toggleDXVK m.ts:999 H · toggleDXVKNVAPI m.ts:1007 H ·
    toggleVKD3D m.ts:1015 H · installWineVersion wine/manager/ipc_handler.ts:14 H ·
    refreshWineVersionInfo wine/manager/ipc_handler.ts:46 H · removeWineVersion wine/manager/ipc_handler.ts:56 H
  shortcuts (7): addShortcut shortcuts/ipc_handler.ts:14 SEND · removeShortcut :41 SEND ·
    shortcutsExists :33 H · addToSteam :60 H · removeFromSteam :65 H · isAddedToSteam :70 H ·
    processShortcut m.ts:1465 SEND
  runnerMisc (11): getLegendaryVersion utils/ipc_handler.ts:18 H · getGogdlVersion :19 H ·
    getCometVersion :20 H · getNileVersion :21 H · callTool tools/ipc_handler.ts:25 H ·
    egsSync m.ts:1251 H · getGOGLinuxInstallersLangs m.ts:840 H · syncSaves m.ts:1263 H ·
    syncGOGSaves m.ts:1255 H · downloadRuntime wine/runtimes/ipc_handler.ts:4 H ·
    isRuntimeInstalled wine/runtimes/ipc_handler.ts:6 H
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the four registration modules as declared empty contracts</name>
  <files>src/backend/sidecar/runnerAuthFlowRegistration.ts, src/backend/sidecar/wineToolsFlowRegistration.ts, src/backend/sidecar/shortcutsFlowRegistration.ts, src/backend/sidecar/runnerMiscFlowRegistration.ts</files>
  <read_first>
    - src/backend/sidecar/steamAuthFlowRegistration.ts (full file — the header docstring's per-channel provenance format, the `import '../storeManagers'` load-bearing-first-import block and its explanatory comment, the `export function registerXFlows(): void` shape, and the `ipcMain.on('logoutSteam', …)` send-kind body)
    - src/backend/sidecar/humbleLoginFlowRegistration.ts L28-60 and L285-315 (the curated-import doc comment, and the `void (async () => { try {…} catch { logSendFailure(…) } })()` fire-and-forget send guard)
    - src/backend/sidecar/handlers.ts L1-100 (the docstring inventory of every registration module and the import block — the format the next task extends)
    - .planning/phases/34.5-.../34.5-RESEARCH.md § "Anti-Patterns to Avoid" (why side-effect-importing `*/ipc_handler.ts` is forbidden: `tools/ipc_handler.ts` also registers `runWineCommandForGame` and the three DEFERRED winetricks channels)
  </read_first>
  <action>
    Create four modules under `src/backend/sidecar/`, each exporting a single no-op-for-now
    `export function registerXFlows(): void {}` with the names
    `registerRunnerAuthFlows`, `registerWineToolsFlows`, `registerShortcutsFlows`,
    `registerRunnerMiscFlows`.

    Each module's header docstring MUST contain, in the `steamAuthFlowRegistration.ts` style:
      - The Phase 34.5 plan and requirement IDs that will fill it (auth → 34.5-06/34.5-10,
        REQ-34.5-04; wine → 34.5-05/34.5-09, REQ-34.5-03; shortcuts → 34.5-08/34.5-11, REQ-34.5-05;
        misc → 34.5-07/34.5-12, REQ-34.5-06/07/08/09).
      - A per-channel list naming every channel it will own, its Electron source file and line, and
        its kind as `handle` or `SEND` — copy the verified table from this plan's `<interfaces>`
        block. Mark each `SEND` entry with the note that a send channel's rejection reaches NO
        caller (no reject, no timeout, no log line to the renderer), so its body must guard its own
        promise.
      - The curated-import rule: import the underlying logic modules directly, NEVER
        `shortcuts/ipc_handler.ts`, `tools/ipc_handler.ts`, `wine/manager/ipc_handler.ts`,
        `wine/runtimes/ipc_handler.ts`, `utils/ipc_handler.ts` or `main.ts`.
      - An explicit line stating the module is intentionally EMPTY as of plan 34.5-04 and that
        registering nothing is correct until its cluster plan lands — so a reader does not "fix" it.

    Add `import { ipcMain } from './electronStub'` and the `import '../storeManagers'` load-bearing
    first import ONLY to `runnerAuthFlowRegistration.ts` (its cluster reaches
    `legendary`/`gog`/`nile` user modules) — carrying the same explanatory comment
    `steamAuthFlowRegistration.ts` uses. For the other three, do not add imports that are not yet
    used; the cluster plans add what they need. If an unused-import lint would fire, leave the
    import out rather than suppressing the lint.
  </action>
  <acceptance_criteria>
    - All four files exist and each exports exactly one function whose name matches `register(RunnerAuth|WineTools|Shortcuts|RunnerMisc)Flows`.
    - Every one of the 38 channel names appears in exactly one of the four docstrings — verify by scripting the 38-name list against the four files and record the per-file counts (11/9/7/11) in the SUMMARY.
    - Each of the 4 send-kind names (`logoutGOG`, `addShortcut`, `processShortcut`, `removeShortcut`) is marked `SEND` in its docstring.
    - `grep -rn "ipc_handler" src/backend/sidecar/runnerAuthFlowRegistration.ts src/backend/sidecar/wineToolsFlowRegistration.ts src/backend/sidecar/shortcutsFlowRegistration.ts src/backend/sidecar/runnerMiscFlowRegistration.ts` returns only comment lines forbidding it — zero `import` statements.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Four modules exist whose docstrings pin all 38 channels, their sources and their kinds, and none of them imports an `ipc_handler`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all four into handlers.ts and prove the wiring</name>
  <files>src/backend/sidecar/handlers.ts, src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts</files>
  <read_first>
    - src/backend/sidecar/handlers.ts (full file — the docstring inventory listing every prior slice's module with its plan and requirement IDs, the import block L60-74, and the call block L96-132 including the `registerAppShellFlows({...})` argument form which is the exception, not the rule)
    - src/backend/sidecar/__tests__/humbleLoginFlows.test.ts L140-175 (Describe 1: the bidirectional registration-kind template using `handlerRegistry`/`listenerRegistry` from `../electronStub`, and its comment explaining why `registerXFlows()` is called ONCE at file scope)
    - src/backend/sidecar/electronStub.ts (the `handlerRegistry` and `listenerRegistry` exports, and `ipcMain`'s `handle`/`on` implementations — confirm whether `on()` wraps handler exceptions before relying on it)
    - .planning/phases/34.5-.../34.5-VALIDATION.md § "Test Infrastructure" (the containment setup file that must not be bypassed)
  </read_first>
  <action>
    Extend `handlers.ts`: add four named imports and four calls, in the existing style and adjacent
    to the other slice registrations. Extend the file's header docstring inventory with one entry per
    new module naming its plan (34.5-04) and its channel count, matching how every prior slice's
    entry reads. Call each with no arguments — do not copy `registerAppShellFlows`'s options-object
    form, which exists for a slice-specific reason.

    Create `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts` asserting:
      1. Each of the four modules exports a callable `registerXFlows` function.
      2. `handlers.ts` imports and calls all four — assert by reading the source text of
         `handlers.ts` and matching each `registerXFlows` name twice (import + call), so a module
         that is imported but never invoked fails the test. Name this the "imported but never
         called" guard in a comment; that failure mode is invisible at runtime because the channel
         simply never registers.
      3. Calling each `registerXFlows()` twice does not throw and does not stack duplicate listeners
         — read `listenerRegistry` before and after to assert the count is stable. (This pins the
         idempotence assumption that the per-cluster tests, which call `registerXFlows()` once at
         file scope, depend on.)
      4. A curated-import guard: none of the four module source files contains an `import` of
         `ipc_handler` or of `../main`. Assert against file text, mirroring the guard-by-source-read
         approach `humbleLoginFlows.test.ts` Describe 3 uses for `humble/ipc_handler`.
      5. A GROWTH-TOLERANT containment pin, deliberately written so no later plan has to edit this
         file: hard-code the four declared channel-name sets (11/9/7/11, exactly as this plan's
         `<interfaces>` block lists them) and assert, for each module, that every channel it
         registers is a MEMBER of its own declared set — and that it registers nothing belonging to
         another module's set or outside the 38 entirely. Today each module registers the empty set,
         which satisfies the subset assertion; as plans 34.5-05 through 34.5-12 land, the sets fill
         in and the assertion keeps holding without an edit. Add a comment stating that the
         COMPLETENESS check (all 38 present) is deliberately NOT here — it belongs to plan 34.5-13,
         which runs after every cluster plan and is the only point at which "all 38" is a true
         statement.
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/runnerSliceRegistration.test.ts` exits 0.
    - `grep -c "registerRunnerAuthFlows" src/backend/sidecar/handlers.ts` returns 2 (import + call); same for the other three names.
    - The test file contains the imported-but-never-called guard and the curated-import guard as distinct assertions.
    - `cd src/backend && npx jest` exits 0 (full backend suite), and the exit code plus pass/fail counts are recorded VERBATIM in the SUMMARY — do not round a known-flaky run to green.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && cd src/backend && npx jest sidecar/__tests__/runnerSliceRegistration.test.ts</automated>
  </verify>
  <done>All four modules are reachable from `handlers.ts`, proven by a test that distinguishes "imported" from "called", and a zero-channel pin stands ready to catch a cluster plan that forgets to register.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar channel registry | A channel that is never registered rejects with `UNPORTED_CHANNEL_MARKER`; a channel registered with the WRONG kind fails silently instead |
| curated import graph → Electron's real `ipcMain` | `backend/ipc`'s `addHandler`/`addListener` must never be reached from the sidecar's graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-11 | Repudiation | a registration module imported but never called | mitigate | The wiring test matches each `registerXFlows` name twice in `handlers.ts` source, so an import-only wiring fails the suite rather than producing a silently unregistered cluster. |
| T-34.5-12 | Tampering | a deferred channel registered by accident | mitigate | Curated-import guard asserts no module imports `tools/ipc_handler.ts` (which also registers the three DEFERRED winetricks channels) or any other `ipc_handler`. |
| T-34.5-13 | Denial of Service | duplicate listener stacking | mitigate | Idempotence assertion: calling `registerXFlows()` twice leaves `listenerRegistry` counts stable, pinning the assumption every per-cluster test relies on. |
| T-34.5-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `cd src/backend && npx jest sidecar/__tests__/runnerSliceRegistration.test.ts` exits 0.
- Full backend suite run and its exit code recorded verbatim.
- All 38 channel names accounted for across the four docstrings, counts 11/9/7/11.
</verification>

<success_criteria>
The slice's four registration seams exist, are wired, are proven reachable, and carry a source-level
declaration of every channel they will own and how it must be registered.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-04-SUMMARY.md` when done
</output>
