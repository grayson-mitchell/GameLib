---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 50
type: execute
wave: 3
depends_on: [34.5-43, 34.5-44, 34.5-45, 34.5-46, 34.5-47, 34.5-48, 34.5-49]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
autonomous: true
gap_closure: true
cycle: 6
requirements: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-04, REQ-34.5-05, REQ-34.5-12]

must_haves:
  truths:
    - "A fourth gate contract exists with verdict: null and items_total: 4, authored before any live work of that run begins"
    - "The Epic descope is stated in the contract's own preconditions, naming Phase 34.7 as receiving owner, and stated as retiring nothing"
    - "Every OPEN ledger row at the live ledger's current state is named in the known-holes table, enumerated by measurement rather than transcribed from this plan"
    - "The run's operating conditions appear as numbered preconditions with mechanically checkable RESULT slots, not as prose advice"
    - "The contract's own verification commands were run against a FILLED specimen at authoring time, not against the empty template"
    - "Every (precondition, item) and (item, item) pair was reviewed for the interaction defect class, and the review is recorded"
    - "None of the three prior gate documents is modified, overwritten or deleted"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md"
      provides: "the fourth blocking gate contract, unfilled, four items"
      contains: "verdict: null"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md"
      provides: "the D-CYCLE6-A Epic descope row naming Phase 34.7 as OWNER"
      contains: "Phase 34.7"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md"
      provides: "an append-only cross-reference note recording that the Epic descope retires nothing"
      contains: "D-CYCLE6-A"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN-3.md"
      to: "34.5-UNTESTED-ITEMS.md"
      via: "the known-holes table, one row per OPEN ledger row, enumerated from the live file"
      pattern: "U-34\\.5-[0-9]+"
    - from: "34.5-LIVE-GATE-RERUN-3.md"
      to: "deferred-items.md"
      via: "the Epic descope precondition cites the deferred-items row by number"
      pattern: "Phase 34\\.7"
    - from: "34.5-LIVE-GATE-RERUN-3.md"
      to: "34.5-PRELOAD-SURFACE-AUDIT.md"
      via: "the known-holes table records U-34.5-14's extent answer from the committed audit, not from prose"
      pattern: "PRELOAD-SURFACE-AUDIT"
---

<objective>
Author `34.5-LIVE-GATE-RERUN-3.md`: the CONTRACT for the fourth blocking live gate, written with
`verdict: null` and `items_total: 4` BEFORE any live work of that run begins. Also write the
`deferred-items.md` row that records the Epic descope, and the one append-only note in
`34.5-UNTESTED-ITEMS.md` that records the descope as retiring nothing.

Purpose: this is the discipline plans 34.5-19, 34.5-30 and 34.5-40 followed. A contract authored
after the fact can be shaped by what happened; one authored before it cannot. This plan writes the
contract and fills in NOTHING.

**This plan runs nothing live.** Plan 34.5-51 executes this contract on real macOS hardware.

Output: one new file plus two append-only edits. **`34.5-LIVE-GATE.md`,
`34.5-LIVE-GATE-RERUN.md` and `34.5-LIVE-GATE-RERUN-2.md` are NOT overwritten, NOT edited and NOT
deleted** — each is the completed record of its own FAILED run (0/5, 0/5, 0/5), and anyone citing
any of them as evidence of a PASS is citing the wrong document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE6-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md

**Run `graphify query "<question>"` before opening raw source** (project rule, `CLAUDE.md`). This
plan opens source only to read the ACTUAL log-line shapes it cites — which it must do rather than
copying them from a SUMMARY or from this plan. A contract that cites a line the code does not emit
is the exact defect run 1 shipped at its own precondition 5.

<interfaces>
**These are POINTERS, not a transcription source.** Every log-line shape cited in the contract must
be re-read from its own emitting file at authoring time, because seven sibling plans in waves 1–2
edited these modules after this plan was written.

- `src/backend/sidecar/bootstrap.ts` — `[bootstrap] secret stores: dev-vault` / `: keyring`;
  `[bootstrap] appRoot resolved=… source=…`; `[bootstrap] publicDir resolved=… exists=…`;
  `[bootstrap] runner binary … path=… exists=…`; `[bootstrap] GAMELIB_SHELL_EXE received=…`;
  `[bootstrap] SIDECAR ASSET ROOT DEFECT`. Also carries whatever deep-link handler plan 34.5-44 added.
- `src/backend/sidecar/oauthLoginCapture.ts` — `[oauthLoginCapture] runner=… label=…`,
  `… nav host=…`, `… status=…`, `runner=nile origin-mismatch origin=…`.
- `src/frontend/screens/WebView/useTauriOAuthLogin.ts` — `[useTauriOAuthLogin] runner=… phase=…`,
  terminal value `idle (login completed, library refresh triggered)`.
- `src/frontend/state/GlobalState.tsx` — `[refreshLibrary] runner=… origin=…` (plan 34.5-47 changed
  its call-site coverage; re-read).
- `src/backend/sidecar/steamFlowRegistration.ts` — `refreshLibrary complete runner=… managers=…`,
  `refreshLibrary failed runner=…` (plan 34.5-46 changed `handleLaunch` in this file; re-read).
- `src/backend/protocol.ts` — `handleProtocol` and its log line (plan 34.5-44's retirement bar).
- `src/backend/shortcuts/shortcuts.ts` (`generateMacOsApp`, the `run.sh` body) and
  `src/backend/shortcuts/nonesteamgame/nonesteamgame.ts` (the VDF `Exe` write) — item 3's two call sites.
- `src/backend/sidecar/electronStub.ts` — `nativeImage` after plan 34.5-45's `createFromBuffer` fix.
- `src/frontend/helpers/declaredUnavailable.ts` — plan 34.5-48's `callOrDeclare` decline line shape.

Artifacts committed by waves 1–2 whose CONTENT this contract consumes, all of which must be read
from the file rather than from this plan:
- `34.5-UNTESTED-ITEMS.md` — the authoritative OPEN-row roster.
- `34.5-PRELOAD-SURFACE-AUDIT.md` and `preload-surface-gate.py` (plan 34.5-49) — the measured
  channel counts and the four security findings.
- `34.5-JSON-TRANSPORT-SWEEP.md` (plan 34.5-45) — the `addToSteam` return-value verdict.
- `34.5-PORTED-CHANNELS.md` (plan 34.5-48's correction) — the "how to find the rest" sweep command.
- The seven SUMMARYs `34.5-43-SUMMARY.md` … `34.5-49-SUMMARY.md` — each one's measured suite counts
  and its explicitly-handed-forward observations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the live ledger by measurement; write the Epic-descope deferred-items row and the ledger's descope note</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-CYCLE6-ROUTING.md` § D-CYCLE6-A in full — the descope's rationale, its five binding consequences, and the sentence "This does not retire anything."
    - `.planning/phases/34.5-.../deferred-items.md` — the whole file, for the numbered-item shape (items 1–23), the existing section headers, and item 23's redaction decision which binds every document this cycle writes.
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` — the preamble, all rows at their CURRENT state, and the three Rules. This file has been edited by up to six wave-1/2 plans since this plan was authored; read the file, never this plan's summary of it.
    - `.planning/phases/34.5-.../34.5-43-SUMMARY.md` … `34.5-49-SUMMARY.md` — each SUMMARY's own record of which ledger rows it created, amended or retired.
  </read_first>
  <action>
    **Step A — enumerate the ledger by measurement.** Produce a roster of every row in
    `34.5-UNTESTED-ITEMS.md` with its ID, its Status cell value, and the plan that created it.
    Derive this from the file itself with a repeatable command whose output is pasted verbatim into
    the SUMMARY — never by transcribing the list in this plan or in the routing document.

    At the time this plan was written the file carried 15 rows, of which twelve read OPEN
    (`U-34.5-01`, `-02`, `-03`, `-04`, `-05`, `-07`, `-08`, `-10`, `-12`, `-13`, `-14`, `-15`) and
    three read RETIRED (`-06`, `-09`, `-11`). Waves 1 and 2 were expected to append roughly eleven
    to twelve further rows and to retire `U-34.5-16` (created by 34.5-43, closed by 34.5-44 Task 4)
    and possibly `U-34.5-14` (34.5-49's audit is that row's own retirement condition). **Treat every
    one of those numbers as a prior to be checked, not as an input.** The roster the contract
    consumes is whatever the file says.

    **Step B — record ID collisions as a finding, do not resolve them silently.** Plans 34.5-44,
    34.5-45, 34.5-46, 34.5-48 and 34.5-49 were authored concurrently and four of them independently
    claim `U-34.5-17` or "the next free id". If the roster contains two rows with the same
    `U-34.5-NN` id, that is a defect in the record: append a numbered `deferred-items.md` finding
    naming both rows and their creating plans, and make every reference in the gate contract a
    **(id, creating plan)** pair so the contract is unambiguous even while the collision stands. Do
    NOT renumber another plan's committed row.

    **Step C — the `deferred-items.md` descope row.** Append a new numbered item under a new
    section header for gap cycle 6, allocating the next free number after the file's current highest
    (23 at the time of writing — verify). The row must state, in this order: that gate item 1 (Epic
    login) is DESCOPED from Phase 34.5's blocking gate per D-CYCLE6-A; that **Phase 34.7 is the
    OWNER**; that the blocker mechanism is the **operator decision of 2026-08-05** — no further time
    is ever spent on the Epic alt-login/403, and Phase 34.7 deletes the interactive Epic login
    outright, making device-auth bootstrap the single Epic sign-in path; that gating 34.5's closure
    on a login path 34.7 deletes can never be durably satisfied, because a PASS would certify code
    scheduled for removal and a FAIL would block 34.5 on work the parking decision forbids; that
    **UAT tests 11 (the Epic half), 12 (`egsSync`) and 13 (legendary save sync) travel with the
    descope to Phase 34.7**, while **Amazon's half of test 11 stays in 34.5** as gate item 2; and
    that **the descope retires nothing** — `U-34.5-06` was retired 2026-08-03 on its own named
    observation (`runner=legendary status=captured` at 20:28:00 followed by `phase=idle` in the same
    session) and `U-34.5-11` was retired the same day as SUBJECT DELETED (commit `da529ca86`),
    neither of them by this descope, and neither may be cited as a descope credit.

    Edit append-only: add the new section and item, touch no existing line.

    **Step D — the ledger's descope note.** Append ONE short note to `34.5-UNTESTED-ITEMS.md` —
    below the existing table, in its own subsection — recording that D-CYCLE6-A removed the Epic item
    from gate 4, naming the `deferred-items.md` item number written in Step C, and stating that the
    descope creates no new row and retires none. This exists because Phase 34.4.1's gate passed 4/4
    while a struck precondition turned an untested hole into an implicit pass; the note is what stops
    "Epic is not in the gate" from later reading as "Epic passed". Do not alter any table row, any
    Status cell, or the preamble's row counts.
  </action>
  <verify>
    <automated>test $(git diff --numstat -- .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md | awk '{s+=$2} END {print s+0}') -eq 0 &amp;&amp; grep -qc "D-CYCLE6-A" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains the verbatim command and verbatim output used to enumerate the ledger roster, and the roster lists every row with ID, Status and creating plan.
    - `git diff --numstat` reports **0 deletions** for both `deferred-items.md` and `34.5-UNTESTED-ITEMS.md`.
    - `deferred-items.md` gains exactly one new numbered item, under a new gap-cycle-6 section header, whose number is one greater than the file's previous highest.
    - That item names Phase 34.7 as OWNER, cites the 2026-08-05 operator decision as the blocker mechanism, lists UAT tests 11 (Epic half), 12 and 13 as travelling with the descope, states Amazon's half of test 11 stays, and states the descope retires nothing with `U-34.5-06` and `U-34.5-11` named and their real retirement reasons given.
    - `34.5-UNTESTED-ITEMS.md` gains exactly one appended note containing the string `D-CYCLE6-A` and the `deferred-items.md` item number; `grep -c "^| U-34.5-" 34.5-UNTESTED-ITEMS.md` returns the same value before and after this task.
    - If any duplicate `U-34.5-NN` id exists in the roster, a second `deferred-items.md` finding records it with both creating plans named, and no committed row was renumbered.
    - `git status --porcelain` shows `34.5-LIVE-GATE.md`, `34.5-LIVE-GATE-RERUN.md` and `34.5-LIVE-GATE-RERUN-2.md` UNMODIFIED.
  </acceptance_criteria>
  <done>The ledger roster is a measurement, the Epic descope is recorded where a future reader will find it, and the descope is on record as retiring nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Author the contract's frontmatter, framing, twelve preconditions and the known-holes table</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` — the whole document, for the shape to mirror: frontmatter fields, `## Why this run exists`, `## D-08 — no partial pass`, `## Evidence-handling rule`, `## The binding evidence rule`, the numbered preconditions with their RESULT slots, `## Known holes (ledger Rule 3)`. **Read it; never edit it.**
    - `.planning/phases/34.5-.../34.5-CYCLE6-ROUTING.md` § D-CYCLE6-A and § D-CYCLE6-D in full — the descope and the four binding operating conditions, which become preconditions rather than prose.
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` § D-08 — the no-partial-pass rule, to be restated verbatim.
    - Task 1's roster output, and `34.5-UNTESTED-ITEMS.md` at its current state.
    - `.planning/phases/34.5-.../34.5-PRELOAD-SURFACE-AUDIT.md` (plan 34.5-49) — the measured channel counts, the unlisted count, and the four channels it flagged as security findings rather than bucket rows.
    - `.planning/phases/34.5-.../34.5-46-SUMMARY.md`, `34.5-47-SUMMARY.md`, `34.5-48-SUMMARY.md`, `34.5-49-SUMMARY.md` — each one's explicitly-handed-forward observation.
    - `src/backend/sidecar/bootstrap.ts` — the boot self-check and secret-store receipt lines, read at authoring time so no cited line is invented.
  </read_first>
  <action>
    Create `34.5-LIVE-GATE-RERUN-3.md` with frontmatter: `phase`, `plan: 50`,
    `executed_by: 34.5-51-PLAN.md`, `type: live-gate`, `status: pending`, `blocking: true`,
    `created`, `cycle: 6`, `verdict: null`, `items_total: 4`, `items_passed: null`,
    `items_failed: null`, `items_blocked: null`, `items_not_attempted: null`,
    `supersedes: 34.5-LIVE-GATE-RERUN-2.md`.

    **`## Why this run exists`** — three runs, three FAILs, each on a different layer: run 1 on
    `spawn ./{legendary,gogdl,nile} ENOENT` (a wrong `publicDir` under the sidecar); run 2 on the
    OAuth capture reaching the backend and never reaching the renderer; run 3 (0 passed / 2 failed /
    1 blocked / 2 not attempted) on a third layer — the backend chain fully succeeding while the
    frontend rendered nothing, plus two items never attempted at all. Name each cycle-6 fix by plan
    number and by what it claims to close: 34.5-43 (`getInstallInfo` ported, the details screen
    unblocked), 34.5-44 (Rust argv parsing, single-instance guard, deep-link delivery to
    `handleProtocol`), 34.5-45 (`nativeImage.createFromBuffer`, the dropped `addToSteam` return
    value and the sibling-channel sweep), 34.5-46 (runner-aware `launch` dispatch), 34.5-47
    (`origin=` threading and post-login navigation), 34.5-48 (the EOS cluster declining visibly),
    34.5-49 (the full preload-surface audit). State plainly that **none of it is live-proven**, and
    that this contract exists to do so.

    **`## D-08 — no partial pass`** — restate verbatim from `34.5-CONTEXT.md`. Add that a fourth
    non-clean result routes to another gap cycle inside Phase 34.5, never a forced close, never an
    advisory note, never a deferred UAT entry, never a pre-authorised override; and that Phase
    34.6's and Phase 35's dependency lines are editable ONLY on a clean **4 of 4** PASS.

    **`## Evidence-handling rule (T-34.5-G6-02)`** — binding on every Result slot and every pasted
    excerpt. Never paste an `access_token`, a `refresh_token`, a cookie value, a session identifier,
    an OAuth `code=` value, or a Steam `userdata` account id; redact IN PLACE (`code=<REDACTED>`,
    `userdata/<REDACTED>`) rather than omitting the whole line, so the line's shape survives as
    evidence; quote only the lines the item names. State the phase's own history as the reason this
    rule is mechanical rather than advisory: a real Steam account id was committed unredacted once,
    and live PKCE material was committed once and caught only by a post-commit audit. Require the
    operator to run a redaction sweep over the staged diff **before** committing the filled contract,
    with the sweep command written into this section and its output pasted into the run's record; and
    carry `deferred-items.md` item 23's standing rule that the 37 already-published SteamID64 files
    are expected sweep matches and must not be re-raised, while any NEW unredacted account id, token,
    cookie, session id or OAuth code still blocks.

    **`## The binding evidence rule`** — stated once, applying to all four items: **a mutating
    call's own report is never proof of its effect.** Run 2 recorded backend `status=captured` for
    GOG and Amazon and it proved nothing — both items still FAILED. Run 3's backend persisted 7 GOG
    titles to `store_cache/gog_library.json` and the Library rendered none. Every evidence clause
    must therefore name at least one observable produced by a **different component** than the one
    under test: a UI state, a file on disk, a log line emitted by another module, or a `ps` listing.

    **`## Item count and the Epic descope (D-CYCLE6-A)`** — its own section, before the
    preconditions, and repeated as precondition 10 so it cannot be skimmed past. **This gate has FOUR
    items, not five.** Items are: (1) GOG login, (2) Amazon login, (3) shortcuts, (4)
    `runWineCommand`. The old item 1 (Epic) is **removed, not renumbered in place and left blank**.
    Phase 34.7 is the receiving owner; cite the `deferred-items.md` item number Task 1 wrote. UAT
    tests 11 (Epic half), 12 (`egsSync`) and 13 (legendary save sync) travel with it; Amazon's half
    of test 11 stays as item 2. **Descoping is not a pass and retires nothing** — `U-34.5-06` and
    `U-34.5-11` were both retired 2026-08-03 for their own, different reasons, and neither may be
    credited to this descope. State the consequence plainly: with four items, a clean result is 4 of
    4, and `items_total: 4` is what the arithmetic rule reconciles against.

    **`## Preconditions`** — TWELVE explicitly NUMBERED steps, each with an empty `RESULT` slot and
    each written as a mechanically checkable condition, never as advice. An unstated precondition is
    exactly how a gate gets skipped and becomes UAT debt.

    1. **Kill orphans first.** `ps aux | grep -E "gamelib-shell|sidecar.js"` must be EMPTY before
       launching; kill anything found and re-check. Gate 3 found an orphaned `gamelib-shell`/
       `sidecar.js` pair contending for the same store files, which makes every store-backed
       observation ambiguous. After launch, the same command must show **exactly one of each** —
       record both listings verbatim, with the PIDs.
    2. **A clean rebuild and a real `pnpm tauri:dev` launch** (never `tauri dev`, which serves a
       stale static bundle from `frontendDist`). Sidecar liveness proven from `gamelib.log` growth
       plus the `ps` listing from precondition 1 — never from the `tauri:dev` terminal, whose
       sidecar `console.*` output is structurally invisible. Record the build's completion timestamp:
       it is needed by precondition 12's staleness check.
    3. **Preserve the log before trusting it.** Copy `~/Library/Logs/GameLib/gamelib.log` to a named
       sidecar file (`gamelib.log.34.5-g6-gate4-<sessionletter>`) and `cmp`-verify byte-identical
       **before** citing any line from it. Every excerpt in every Result slot is quoted from the
       PRESERVED copy, and the copy's path and `cmp` exit code are recorded here. `gamelib.log`
       rotated mid-gate on run 3 and the live file lost the run (F-34.5-G6-15); it was recovered only
       from `gamelib.log.old`. If the run spans more than one session, preserve each session
       separately and label which session each excerpt came from.
    4. **The secret-store arm is `keyring`, VERIFIED not assumed.** `GAMELIB_DEV_SECRET_VAULT` must
       be **unset for the whole run**. The guard is an exact-string `=== '1'` match, so verify the
       arm rather than the export: `grep "\[bootstrap\] secret stores: " <preserved log>` must end
       **`keyring`**, not `dev-vault`, and zero `[dev-secret-vault]` lines may appear anywhere in the
       log. State in this precondition, not in a footnote, that **a `dev-vault` reading is NO
       evidence for `U-34.5-01` and `U-34.5-10` — not weaker evidence, none** — because such a run
       issues no real `keyring_get` at all. A `dev-vault` reading is a DEVIATION from D-CYCLE6-D and
       must be recorded as one; the run may continue, but both rows then stay OPEN whatever the
       verdict. Gate 3 spanned two sessions on *different* arms because an export silently failed to
       reach the sidecar — so if the run spans sessions, this check is repeated per session and each
       session's arm is recorded.
    5. **Signed OUT of GOG and Amazon — verified, not assumed.** Do not carry a prior run's aside
       state forward; plan 34.5-20's SUMMARY records a session ending with GOG signed back IN. Fresh
       `ls`/`test -f` on every original and backup path, recorded verbatim, covering `nile_store/`
       **and** `nile_config/`. Any re-aside uses a NEW dated suffix with a `cmp` proof of
       byte-identity before the original is removed — never delete blind. Epic is out of scope
       (precondition 10) and no Epic sign-out is required or performed.
    6. **The G-1 fix is present in THIS build, proven from the preserved log**:
       `[bootstrap] appRoot resolved=… source=GAMELIB_APP_ROOT` (a `process.cwd` source does NOT
       satisfy this even if the path looks right), `[bootstrap] publicDir resolved=… exists=true`,
       and all four `[bootstrap] runner binary … exists=true` lines. A
       `[bootstrap] SIDECAR ASSET ROOT DEFECT` line anywhere **BLOCKS the run**.
    7. **`GAMELIB_SHELL_EXE` read back from the SIDECAR's own receipt line**
       (`[bootstrap] GAMELIB_SHELL_EXE received=…`), never the Rust shell's `eprintln!`, which
       reaches only the terminal and structurally cannot distinguish "set on the child env" from
       "visible to `pathShim`". An `<UNSET>` reading **BLOCKS item 3**. Carry the dev-versus-packaged
       caveat verbatim: a dev PASS never retires the packaged half of Assumption A2 and never retires
       residual `R-34.5-G1-PKG`.
    8. **The full suite battery at the HEAD the gate runs on**, exit codes recorded VERBATIM and any
       non-zero explained rather than rounded to green: `npm run test:ci`, `npx tsc --noEmit`,
       `cd src-tauri && cargo check`, `cargo test`, `python3 ported-channels-gate.py`, and
       `python3 preload-surface-gate.py` (plan 34.5-49's new anti-rot gate). **The floor is the
       MEASURED count recorded in the wave-1/2 SUMMARYs** — cite the SUMMARY by filename and copy its
       number; do not transcribe a count from a prior gate document, from this contract's own prose,
       or from any plan. A count at or below that floor means HEAD is stale and must be explained
       before proceeding; a count above it also needs an explanation.
    9. **The cycle-6 fixes are present in this build, proven from source at HEAD**, with
       comment-filtered greps (`grep -v '^\s*//' | grep -v '^\s*\*' | grep -c …`) so a doc comment
       cannot satisfy a code assertion, and with a bare `== 0` gate on an unfiltered file forbidden.
       Enumerate one check per plan: the sidecar `getInstallInfo` registration (34.5-43); the Rust
       argv/`--no-gui`/`gamelib://` helpers, the single-instance guard, and `getInstallInfo` present
       in `LONG_RUNNING_CHANNELS` (34.5-44); `createFromBuffer` on `electronStub`'s `nativeImage`
       (34.5-45); the runner-aware `handleLaunch` dispatch (34.5-46); the threaded `origin=` and the
       post-login navigation (34.5-47); `callOrDeclare` / `declaredUnavailable` at the EOS call sites
       (34.5-48). Record beside this precondition, in its own sentence, that **presence is not proof
       of effect** — that is what the four items are for.
    10. **The Epic descope is in force (D-CYCLE6-A).** Restate the item-count section's five binding
        consequences here, and record the `deferred-items.md` item number. This precondition's RESULT
        slot is where the operator confirms the run was executed as a four-item gate. It exists
        because Phase 34.4.1's gate passed 4/4 while a struck precondition turned an untested hole
        into an implicit pass — a silently missing item is precisely the failure mode
        `34.5-UNTESTED-ITEMS.md`'s preamble was written to prevent.
    11. **Item-3 preflight — the non-event guard.** Record this machine's global
        `addDesktopShortcuts`, `addStartMenuShortcuts` and `addSteamShortcuts` values, read from the
        live config. On gate 3 all three were `false`, and `shortcuts.ts`'s writes are gated on
        `if (addDesktopShortcuts || fromMenu)` / `if (addStartMenuShortcuts || fromMenu)` — so the
        contract's own prescribed `addShortcut(appName, runner, false)` would have been a **guaranteed
        silent non-event** (F-34.5-G6-11). The contract must therefore prescribe an invocation that
        cannot be a non-event under the recorded settings, and state which of the two remedies it
        uses (`fromMenu=true`, or flipping the globals and recording that they were flipped). Also
        inline here: the verified currently-installed non-Steam target's `appName` and `runner`; the
        Steam `shortcuts.vdf` path with `userdata/<REDACTED>`; the quit-Steam step; and both exact
        devtools invocations. **Inline them — do not cross-reference a preflight document**, because
        run 2 referenced its groundwork and the item was skipped anyway.
    12. **Item-4 preflight — the Wine repoint.** `U-34.5-03` retires **only** on an item-4 PASS,
        never by inference. Its named blocker is now partly gone: UAT test 4 PASSED and
        `Wine-Crossover-latest` (583 MB, `Contents/MacOS/wine` present) is on disk under
        `tools/wine/`, where gate 3's precondition 11 found the directory EMPTY. **Re-verify that
        path exists at gate time rather than assuming it** — record the `ls`/`test -f` output. The
        observed target on that occasion (Alan Wake) was **uninstalled during the same session**, so
        it cannot be this item's target: the preflight must (a) enumerate currently-INSTALLED
        non-Steam games and name one, (b) repoint that game's `wineVersion` at the `wine`-type
        `Wine-Crossover-latest` and record the before/after `wineVersion.type` values, and (c) name
        the specific macOS-permitted Wine action, its UI path, its dispatching channel and
        registration kind, its literal evidence log lines with their emitting source files, and its
        observable in-app effect. Record explicitly: the live non-Steam Wine execution observed
        during the UAT (`wineboot --init`, `winepath`, a real `unins000.exe` run to completion) is
        evidence **for a future attempt and explicitly not a substitute for one**, and research
        **Pitfall 2** is therefore **CONTRADICTED BY DIRECT EVIDENCE but NOT STRUCK** — it is struck
        only by an item-4 PASS.

    Leave every RESULT slot EMPTY.

    **`## Known holes (ledger Rule 3)`** — a table with **one row per OPEN row in the live
    `34.5-UNTESTED-ITEMS.md`**, built from Task 1's measured roster, never from a list in this plan.
    Columns: ledger id + creating plan, one-line subject, which gate item (if any) would retire it,
    and whether THIS run can retire it with the reason. Rules the table must encode:
    - A row with no item that touches it is marked **NOT COVERED BY THIS GATE** with its future owner
      named. Naming it is the point — it is what stops it becoming an implicit pass.
    - Each retirable row quotes its own "Retired by" cell into the table, so the operator gathers the
      exact observation deliberately rather than in hindsight.
    - `U-34.5-01` and `U-34.5-10` are retirable **only if precondition 4 records the `keyring` arm**;
      state that dependency in their rows rather than leaving it inferred.
    - `U-34.5-02` retires only on an item-3 PASS covering **both** `exe` call sites
      (`nonesteamgame.ts:258` **and** `shortcuts.ts:227`) — one alone never suffices, and gate 3
      wrote a byte-correct `Exe` at the first while never reaching the second.
    - `U-34.5-03` retires only on an item-4 PASS (precondition 12).
    - `U-34.5-04` (Amazon verification-code field legibility) and `U-34.5-05` (title-bar origin
      CHANGING within one flow) are gathered as transcriptions inside items 2 and 1 respectively;
      a log-observed nav-host change is **not** the title-bar transcription `U-34.5-05` names.
    - `U-34.5-14`: if plan 34.5-49 retired it, record that the extent question is **ANSWERED** and
      quote the audit's measured numbers from `34.5-PRELOAD-SURFACE-AUDIT.md` — the total preload
      channel count, the bucket-listed count, and the unlisted count — plus the four channels it
      flagged as **security findings rather than bucket rows** (`runWineCommandForGame`,
      `moveInstall`, `importGame`, `oauthCaptureLogin`). Do **not** repeat "extent UNKNOWN". If
      34.5-49 left it OPEN, record why, from that plan's SUMMARY.
    - Every row created by waves 1 and 2 appears, referenced as an **(id, creating plan)** pair, with
      any duplicate-id collision from Task 1 Step B carried forward as a note in the table.
  </action>
  <verify>
    <automated>grep -q "^verdict: null" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md &amp;&amp; grep -q "^items_total: 4" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md &amp;&amp; test $(git status --porcelain | grep -Ec "34\.5-LIVE-GATE(-RERUN(-2)?)?\.md$") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-LIVE-GATE-RERUN-3.md` exists with `verdict: null`, `items_total: 4`, `status: pending`, `blocking: true`, `executed_by: 34.5-51-PLAN.md`, and null `items_passed` / `items_failed` / `items_blocked` / `items_not_attempted` fields.
    - Exactly twelve numbered preconditions are present, each with an empty RESULT slot; none is prose-only — each states a command, a file check, or a recorded reading.
    - D-08's no-partial-pass rule and the evidence-handling rule both appear, D-08 verbatim from `34.5-CONTEXT.md`.
    - The evidence-handling rule names all six forbidden value classes (`access_token`, `refresh_token`, cookie value, session identifier, OAuth `code=` value, Steam `userdata` account id), prescribes redaction in place, carries a staged-diff sweep command, and carries item 23's published-SteamID64 exception.
    - Precondition 4 states that a `dev-vault` reading is NO evidence for `U-34.5-01`/`U-34.5-10`, requires the `grep "\[bootstrap\] secret stores: "` check against the PRESERVED log, and requires a per-session re-check.
    - Precondition 3 requires the `cmp` byte-identity proof before any citation, and every Result slot's excerpts are sourced from the preserved copy.
    - Precondition 11 records the three global shortcut settings and prescribes an invocation that cannot be a silent non-event under them, citing F-34.5-G6-11.
    - Precondition 12 requires re-verifying the `Wine-Crossover-latest` path, names a currently-installed non-Steam target (not Alan Wake), records before/after `wineVersion.type`, and states Pitfall 2 is contradicted-but-not-struck.
    - The Epic descope appears both as its own section and as precondition 10, naming Phase 34.7, citing the `deferred-items.md` item number, listing UAT 11 (Epic half) / 12 / 13 as travelling, and stating the descope retires nothing.
    - The known-holes table's row count equals the number of OPEN rows in `34.5-UNTESTED-ITEMS.md` at HEAD; the SUMMARY pastes both counts and shows them equal.
    - `U-34.5-14`'s row states the extent question as ANSWERED with the audit's measured numbers and the four security-finding channels, or records why 34.5-49 left it open.
    - No PASS, FAIL, BLOCKED or NOT ATTEMPTED marker is filled anywhere, and no RESULT slot contains text.
    - `git status --porcelain` shows all three prior gate documents UNMODIFIED, and `git diff --name-only` matches zero `.ts`, `.tsx` or `.rs` paths.
  </acceptance_criteria>
  <done>The contract's frame exists: four items declared, twelve mechanically checkable preconditions, the descope stated where it cannot be skimmed past, and every open ledger hole named rather than assumed covered.</done>
</task>

<task type="auto">
  <name>Task 3: Write the four items, the verdict machinery, then validate the contract against a FILLED specimen and review it in pairs</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` §§ Items 1–5, § "What this gate falsifies", § "Arithmetic rule", § "New findings" — the wording to carry forward and each item's recorded FAIL / NOT-ATTEMPTED reason. **Read; never edit.**
    - `.planning/phases/34.5-.../34.5-46-SUMMARY.md`, `34.5-47-SUMMARY.md`, `34.5-48-SUMMARY.md`, `34.5-49-SUMMARY.md` — the four observations those plans hand to this contract by name.
    - `.planning/phases/34.5-.../34.5-JSON-TRANSPORT-SWEEP.md` — plan 34.5-45's recorded verdict on the `addToSteam` return-value drop, which item 3's evidence clause is written from.
    - `.planning/phases/34.5-.../34.5-PORTED-CHANNELS.md` — plan 34.5-48's correction and its "how to find the rest" sweep subsection.
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` rows `U-34.5-02`, `-03`, `-04`, `-05`, `-07`, `-08`, `-12`, `-13`, `-15` — each row's own "Retired by" observation, which the corresponding item must require.
    - `src/backend/sidecar/oauthLoginCapture.ts`, `src/frontend/screens/WebView/useTauriOAuthLogin.ts`, `src/frontend/state/GlobalState.tsx`, `src/backend/sidecar/steamFlowRegistration.ts`, `src/backend/protocol.ts`, `src/backend/shortcuts/shortcuts.ts`, `src/backend/shortcuts/nonesteamgame/nonesteamgame.ts` — the ACTUAL log-line shapes, read at authoring time from the emitting file.
  </read_first>
  <action>
    **Part A — the four items.** Write them into `34.5-LIVE-GATE-RERUN-3.md`. For each: what changed
    since run 3, what to do, what to observe, what evidence is required, and an empty Result slot
    admitting PASS / FAIL / BLOCKED / NOT ATTEMPTED. Every Result slot stays EMPTY.

    **Items 1 and 2 (GOG and Amazon login from scratch, populated library)** — REQ-34.5-12, plus
    REQ-34.5-02 for item 2's anchor. Write a shared evidence-clause block requiring ALL of:
      (a) the `[oauthLoginCapture] runner=… status=captured` line — the backend's own report,
          necessary and NOT sufficient;
      (b) a `[useTauriOAuthLogin] runner=… phase=…` line for the same runner, with the terminal value
          a PASS requires named exactly: `idle (login completed, library refresh triggered)`;
      (c) the runner's CLI auth invocation in the log AFTER the capture timestamp (`gogdl auth` /
          `nile auth --login`);
      (d) the library **populated and RENDERED** in the UI — named game tiles visible — AND the runner
          present in the Library filter options. Gate 3's own failure was exactly here: 7 GOG titles
          persisted to `store_cache/gog_library.json` while the Library rendered nothing. A persisted
          store file is NOT clause (d); an empty library after a "successful" login is a FAIL, not a
          partial pass;
      (e) the account surface showing the signed-in username.

    **Item 1 (GOG)** additionally requires:
      (f) `refreshLibrary complete runner=gog managers=1` — a backend line from a different module
          than the capture;
      (g) ZERO `Refreshing undefined Library` lines across the session, and **every**
          `[refreshLibrary]` line carrying a non-`unknown` `origin=` value. Gate 3 satisfied the first
          half and failed the second (`runner=all origin=unknown`, F-34.5-G6-14) — plan 34.5-47 is
          the fix under test;
      (h) no `refreshLibrary failed runner=…` line anywhere in the session;
      (i) the `U-34.5-05` transcription: a before/after transcription of the login window's title-bar
          origin text CHANGING within this single flow, with `login.gog.com` → `embed.gog.com` named
          as the known candidate. A log-observed `nav host=` change does not satisfy this;
      (j) **plan 34.5-47's handed-forward observation** — the Manage Accounts panel must leave
          "logging into gog" and show the signed-in state without a restart. Record verbatim that
          34.5-47 found `WebView/index.tsx`'s `handleTauriOAuthSuccess` **already** navigates behind
          a `mountedRef` guard, shipped 2026-08-03, so the brief's "no navigation" diagnosis was
          FALSE and this may be a **stale observation** — a non-reproduction is therefore recorded as
          a non-reproduction, and is **not** a retirement of anything.

    **Item 2 (Amazon)** additionally requires:
      (k) the `www.amazon.com` host anchor holding — grep for
          `[oauthLoginCapture] runner=nile origin-mismatch origin=`; it must NOT fire on a clean
          pass. If it fires, Assumption A1 is FALSIFIED and item 2 cannot be a clean PASS even if the
          login otherwise succeeds. Record the observed origin either way, and keep the three-way
          CONFIRMED / FALSIFIED / UNTESTED recording rule verbatim from run 3's contract;
      (l) the `U-34.5-04` transcription: whether the verification-code field's text is legible
          (run 2 recorded black-on-black), transcribed by the operator.

    **Item 3 (`addToSteam` AND `addShortcut`'s macOS `.app`, BOTH `exe` call sites)** — REQ-34.5-01,
    REQ-34.5-12. Inline preconditions 11's target, path and invocations again at the item so the
    operator does not have to scroll. Require ALL of:
      - the `shortcuts.vdf` `Exe` field written by `nonesteamgame.ts:258`, quoted with
        `userdata/<REDACTED>`;
      - `<title>.app/Contents/MacOS/run.sh` written by `shortcuts.ts:227` and its body quoted, with
        NO `Error generating MacOS App` line anywhere in the preserved log — this is `U-34.5-12`'s own
        retirement condition and gate 3 never reached this call site at all;
      - `await window.api.addToSteam(...)` resolving to `true` and the GameSubMenu label flipping to
        "Remove from Steam" **without a reload** — `U-34.5-15`'s own condition. Write this clause from
        plan 34.5-45's recorded verdict in `34.5-JSON-TRANSPORT-SWEEP.md`, not from a guess about
        where the drop was;
      - `ps aux` after clicking each shortcut showing the Tauri shell binary and never a bare `node`
        process, exactly one `gamelib-shell`, and a `handleProtocol` line in the preserved log with
        the named GAME launching — `U-34.5-13`'s condition, plan 34.5-44's fix under test.
      State explicitly that **proving one call site is not sufficient**: `U-34.5-02` retires only on
      a PASS covering both, never one alone. Carry the dev-versus-packaged limit.

    **Item 4 (`runWineCommand` for a non-Steam runner)** — REQ-34.5-03, REQ-34.5-12. Inline
    precondition 12's repointed target, the named Wine action, its UI path, its dispatching channel
    and registration kind, the literal evidence lines with their emitting source files, the
    observable in-app effect, and the prerequisite checks. State which runners satisfy this item; if
    a login-independent target (e.g. `sideload`) is permitted, state in the same breath what its PASS
    does and does not retire. Restate that Pitfall 2 is struck **only** by an item-4 PASS — a
    FAIL-by-blockage retires nothing, exactly as all three prior runs recorded.

    **Part B — cross-cutting observations the contract must gather, in their own section.** These
    are handed forward by name and are observations, not items; a result here changes no verdict.
    - **From 34.5-46:** whether an uninstall makes the Library tile stop presenting as installed
      **without a restart**, with the `Finished uninstalling` line and the tile state observed in the
      same session. Record 34.5-46's own finding that `eb117d9e4` structurally covers the symptom and
      that the UAT observed it at 00:33:24 against a bundle that only built at 01:24:43 — a **stale
      observation**. Live re-observation is required, and a non-reproduction is **not** a retirement.
    - **From 34.5-48:** run the sweep, do not assert the claim. Write in the concrete command from
      `34.5-PORTED-CHANNELS.md`'s "how to find the rest" subsection — grep `src/frontend` for
      `window.api.` followed by any of the 16 deferred channel names, then check each hit for an
      enclosing `.catch` or `callOrDeclare`. The 5 SteamGridDB and 3 winetricks channels have the
      **identical shape** the EOS cluster had (a call site with no catch, console-only warn), and
      SteamGridDB becomes reachable for the first time now that 34.5-43 landed `getInstallInfo` and
      the details screen renders. Carry 34.5-48's general rule verbatim: **the marker machinery
      classifies a rejection; it does not declare anything to a user** — a deferred channel declares
      itself only where a call site catches and routes to `window.api.logError`.
    - **From 34.5-49:** record the four channels flagged as **security findings rather than bucket
      rows** — `runWineCommandForGame`, `moveInstall`, `importGame`, `oauthCaptureLogin` — and note
      whether any is exercised incidentally during this run. Quote the audit's measured counts from
      `34.5-PRELOAD-SURFACE-AUDIT.md`. **The count arithmetic in this plan's own brief does not
      reconcile** (it states 217 measured against 210 bucket-listed and calls the difference 12);
      take the numbers from the committed audit and, if they differ from 217/210/12, record the
      discrepancy as a finding rather than silently adopting either.

    **Part C — the verdict machinery**, all unfilled:
    - a **Verdict** table with one row per item and a result column admitting PASS / FAIL / BLOCKED /
      NOT ATTEMPTED;
    - **`## What this gate falsifies`** — the standing claims with their eligibility rules carried
      verbatim from run 3's contract (the 34.4.1 seam claim per-runner; Assumption A1; Assumption A2
      at both call sites, with the packaged half and `R-34.5-G1-PKG` excluded; research Pitfall 2),
      each marked STANDING, plus the rule that **nothing is struck without the corresponding ITEM
      passing** — not on the strength of a sub-clause being independently confirmed while its parent
      item fails. Add Pitfall 2's current status: contradicted by direct evidence, NOT struck;
    - the **arithmetic rule**: `items_passed + items_failed + items_blocked + items_not_attempted`
      must equal **4**. BLOCKED and NOT ATTEMPTED each count toward neither passed nor failed, and
      any shortfall from 4 PASSes must be stated explicitly in the frontmatter `notes`, never quietly
      rounded. Restate D-08 here in one line: anything short of a clean 4 of 4 means Phase 34.5 does
      not close and routes to another gap cycle inside 34.5;
    - **`## New findings (this run) — record, do not diagnose`**, instructing the operator to record
      observations without diagnosing them, as all three prior runs did.

    **Part D — validate the contract against a FILLED specimen.** Copy the finished contract to a
    scratch file outside the repository. In the copy ONLY, fill every precondition RESULT slot and
    every item Result slot with realistic specimen content: a mix of PASS / FAIL / BLOCKED / NOT
    ATTEMPTED markers, pasted-and-redacted log excerpts, `ps` listings, `cmp` exit codes, and a
    completed arithmetic line. Then run **every automated command the contract itself prescribes**
    against the FILLED specimen and confirm each still measures what it was written to measure.
    Verification/prose collisions are a real defect class in this project: a contract's own
    verification step can be satisfied or invalidated by its own prose. At minimum confirm on the
    filled specimen that the Result-slot count still reads 4, that the redaction sweep does not fire
    on the contract's own explanatory prose about redaction, that no grep intended to find a log line
    matches the contract's description of that log line instead, and that no count-based gate reads a
    number from the contract's narrative. Fix the contract, not the specimen, for anything found.
    Delete the specimen; it is never committed. Paste the specimen-run outputs and every fix made
    into the SUMMARY.

    **Part E — review in PAIRS.** Item-by-item review structurally cannot catch a contract that is
    defective through the **interaction** of two individually-valid requirements. Walk every
    (precondition, item) pair and every (item, item) pair and ask: *can satisfying A make B a silent
    non-event, unmeasurable, or ambiguous?* Record the full pair matrix with each pair's disposition
    in the SUMMARY. Two worked examples must appear in the matrix and be resolved in the contract:
    - **precondition 11 × item 3** — gate 3's own defect: this machine's global shortcut settings were
      all `false` and the contract's prescribed `addShortcut(..., false)` would have written nothing
      and raised nothing (F-34.5-G6-11). Both the precondition and the item were individually
      reasonable; their interaction was a guaranteed non-event.
    - **precondition 4 × items 1/2** — running the `keyring` arm reintroduces real Keychain prompts,
      which on gate 3 timed out at the 45s bound and produced zero successful reads. So the very
      precondition that makes `U-34.5-01`/`U-34.5-10` eligible can also block the logins items 1 and 2
      depend on. The contract must state what the operator does when a `keyring_get` times out
      mid-login: record it as an observation, continue on the same arm, and **never switch arms
      mid-run** — gate 3 spanned two sessions on different arms and made half its evidence
      uninterpretable.
  </action>
  <verify>
    <automated>test $(grep -c "^\*\*Result:\*\*" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md) -eq 4 &amp;&amp; grep -q "Arithmetic rule" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md &amp;&amp; test $(git status --porcelain | grep -Ec "34\.5-LIVE-GATE(-RERUN(-2)?)?\.md$") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Exactly four items, each with an empty Result slot admitting PASS / FAIL / BLOCKED / NOT ATTEMPTED; the Result-slot grep returns 4. No fifth item and no blank Epic placeholder exists.
    - Items 1 and 2 both carry clauses (a)–(e), with (b) naming the exact terminal `phase=` value and (d) requiring a RENDERED, populated library plus the filter option, and stating that a persisted store file does not satisfy it.
    - Item 1 carries (f)–(j) including the `U-34.5-05` title-bar transcription and 34.5-47's stale-observation caveat with the "non-reproduction is not a retirement" rule.
    - Item 2 carries (k) with the three-way recording rule and (l) the `U-34.5-04` legibility transcription.
    - Item 3 requires BOTH call sites with `nonesteamgame.ts:258` and `shortcuts.ts:227` named, the `run.sh` body, the `addToSteam` boolean plus label flip without reload, a `handleProtocol` line, exactly one `gamelib-shell`, and states that one call site never suffices.
    - Item 4 inlines the repointed target, the named Wine action, its channel and kind, its literal evidence lines with emitting files, and states Pitfall 2 is struck only by an item-4 PASS.
    - The cross-cutting observations section carries all three handed-forward items (34.5-46 uninstall-without-restart, 34.5-48 runnable sweep with the concrete command and the marker-machinery rule, 34.5-49 four security findings with the audit's measured counts), and records the brief's 217/210/12 arithmetic discrepancy if the committed audit disagrees.
    - The Verdict table, the standing-claims section with its "nothing struck without the ITEM passing" rule, the four-way arithmetic rule reconciling to 4, and the New-findings header are all present and unfilled.
    - Every log-line shape cited in any item was read from its emitting source file at authoring time; the SUMMARY lists each file it was read from.
    - Part D is evidenced: the SUMMARY contains the filled-specimen run outputs for every automated command the contract prescribes, and lists every contract fix the specimen run produced. The specimen file does not exist in the repository.
    - Part E is evidenced: the SUMMARY contains the full (precondition, item) and (item, item) pair matrix with a disposition per pair, and both worked examples appear with their resolutions written into the contract.
    - `git status --porcelain` still shows all three prior gate documents UNMODIFIED, and `git diff --name-only` matches zero `.ts`, `.tsx` or `.rs` paths.
  </acceptance_criteria>
  <done>A four-item contract exists whose every evidence clause names an observable from a component other than the one under test, whose own verification commands were proven against a filled specimen rather than an empty template, and whose requirement pairs were reviewed for the interaction defect class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior gate records → this contract | Three completed records of failed runs sit in the same directory under near-identical filenames; editing one destroys the evidence of that run |
| machine-local identifiers → contract text | Preconditions 11 and 12 inline a real Steam `userdata` path and real filesystem paths |
| live session log → committed evidence | The contract instructs an operator to paste log excerpts that may contain tokens, cookies, session ids and OAuth codes |
| a live credential origin → the contract's item 1/2 instructions | Items 1 and 2 instruct a human to enter real credentials into an embedded webview |
| the ledger's OPEN set → the known-holes table | An omitted row silently becomes an implicit pass — the exact Phase 34.4.1 failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C6-30 | Tampering | `34.5-LIVE-GATE.md` / `-RERUN.md` / `-RERUN-2.md` | mitigate | A NEW filename is used; Tasks 2 and 3 each carry a `git status --porcelain` acceptance criterion asserting all three prior documents are unmodified, and the automated verify greps the same condition |
| T-34.5-C6-31 | Information disclosure | operator-pasted log excerpts in the filled contract | mitigate | The evidence-handling rule names all six forbidden value classes, mandates redaction IN PLACE so the line shape survives, and requires a staged-diff redaction sweep with its command written into the contract and its output recorded before commit. This phase has committed a real Steam account id unredacted once and live PKCE material once — the sweep is mechanical, not advisory |
| T-34.5-C6-32 | Information disclosure | the Steam `userdata` account id inlined at precondition 11 and item 3 | mitigate | Carried from `deferred-items.md` item 23 and `T-34.5-C5-06`: the id is written `userdata/<REDACTED>`; the path STRUCTURE is the evidence, the id is not. Acceptance criteria require the redacted form |
| T-34.5-C6-33 | Repudiation | a contract citing a log line the code does not emit | mitigate | Every cited line shape is re-read from its emitting source file at authoring time with that path recorded in the SUMMARY — seven sibling plans edited these modules in waves 1–2, so a transcription from this plan or a SUMMARY would be stale by construction. This is the exact defect run 1's precondition 5 shipped |
| T-34.5-C6-34 | Repudiation | an OPEN ledger row silently assumed covered | mitigate | The known-holes table is built from a measured roster (Task 1 Step A) and its row count must equal the file's OPEN count, with both numbers pasted into the SUMMARY. Ledger Rule 3 |
| T-34.5-C6-35 | Repudiation | the Epic descope later read as an Epic pass | mitigate | Stated in the contract's own preconditions (10) AND as its own section AND as a `deferred-items.md` row AND as an appended ledger note, each stating the descope retires nothing and naming `U-34.5-06`/`U-34.5-11`'s real, unrelated retirement reasons |
| T-34.5-C6-36 | Spoofing | the operator entering credentials into an unverified origin in the embedded login window | mitigate | Items 1 and 2 require the login window's own title-bar origin to be transcribed (`U-34.5-05`), which is the anti-phishing surface plan 34.5-27 landed; item 2 additionally requires the `www.amazon.com` host anchor to hold (REQ-34.5-02, closing the inherited `T-34.4.1-44b`) |
| T-34.5-C6-37 | Repudiation | a contract whose own verification step collides with its own prose | mitigate | Task 3 Part D runs every prescribed command against a FILLED specimen and fixes the contract, not the specimen. A gate check validated only against an empty template is unproven |
| T-34.5-C6-38 | Repudiation | a defect arising from the INTERACTION of two individually-valid requirements | mitigate | Task 3 Part E reviews every (precondition, item) and (item, item) pair with a recorded disposition. Two worked examples are mandatory: F-34.5-G6-11's non-event, and the keyring-arm × login-item conflict |
| T-34.5-C6-39 | Elevation of privilege | precondition 11's remedy flipping this machine's global shortcut settings | accept | The change is local, operator-performed, reversible, and recorded in the precondition's RESULT slot; the alternative (`fromMenu=true`) is offered first. No code path is widened |
| T-34.5-C6-SC | Tampering | npm / pip / cargo installs (supply chain) | mitigate | **This plan installs no packages and touches no source file.** `34.5-RESEARCH.md`'s `## Package Legitimacy Audit` reads "Not applicable — no new packages" and stays true. Acceptance criteria require `git diff --name-only` to match zero `.ts`, `.tsx` or `.rs` paths |
</threat_model>

<verification>
- `34.5-LIVE-GATE-RERUN-3.md` exists with `verdict: null`, `items_total: 4`, twelve preconditions, a known-holes table whose row count equals the ledger's OPEN count, four empty items, the cross-cutting observations section, the verdict table, the falsification section and the four-way arithmetic rule.
- `deferred-items.md` carries the Epic descope row naming Phase 34.7 as OWNER and the 2026-08-05 operator decision as the blocker mechanism.
- `34.5-UNTESTED-ITEMS.md` carries one appended `D-CYCLE6-A` note; its row count and every Status cell are unchanged.
- `git diff --numstat` reports 0 deletions for both edited documents.
- `git status --porcelain` shows `34.5-LIVE-GATE.md`, `34.5-LIVE-GATE-RERUN.md` and `34.5-LIVE-GATE-RERUN-2.md` unmodified.
- `git diff --name-only` matches zero `.ts`, `.tsx`, `.rs` paths.
- The SUMMARY carries the ledger roster command and output, the filled-specimen run outputs, and the full pair-review matrix.
</verification>

<success_criteria>
A fourth blocking gate contract exists, authored before any live work, covering four items with Epic
explicitly descoped to Phase 34.7 in the contract's own preconditions; with the run's operating
conditions expressed as mechanically checkable preconditions rather than prose; with every evidence
clause naming an observable from a component other than the one under test; with every OPEN ledger
row named as a known hole rather than assumed covered; validated against a filled specimen rather
than an empty template; and reviewed in pairs rather than item by item.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-50-SUMMARY.md` when done
</output>
