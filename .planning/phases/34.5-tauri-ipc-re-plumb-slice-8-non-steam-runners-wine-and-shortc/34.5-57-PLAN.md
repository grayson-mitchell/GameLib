---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 57
type: execute
wave: 3
gap_closure: true
cycle: 7
depends_on: ["34.5-52", "34.5-53", "34.5-54", "34.5-55"]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-REVIEW.md
autonomous: true
requirements: [REQ-34.5-13]

must_haves:
  truths:
    - "Cycle 7's code changes are reviewed BEFORE the blocking gate runs and BEFORE the reconciliation sweep, so any finding it produces can still be ledgered."
    - "Every assertion cycle 7 added has a captured RED proof; an assertion without one is a review finding, not a pass."
    - "A blocker-severity finding halts the gate run rather than being carried into it."
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-REVIEW.md"
      provides: "The cycle-scoped code review, its findings with severities, and its halt decision"
      contains: "halt_gate:"
  key_links:
    - from: "34.5-CYCLE7-REVIEW.md"
      to: "plan 34.5-59 (the gate run)"
      via: "the halt_gate frontmatter field, read before wave 5 begins"
      pattern: "halt_gate:"
---

<objective>
Review cycle 7's own code changes (plans 34.5-52, -53, -54, -55) **before** the blocking gate runs
and **before** the propagation sweep.

Purpose: this project has measured, on a sibling phase, three consecutive cycles in which a code
review ran AFTER the reconciliation sweep and was therefore **unledgered by construction** — its
findings had nowhere to land. The fix is the ORDERING, not more diligence in the sweep. This plan
is that ordering.

Output: `34.5-CYCLE7-REVIEW.md`, with a `halt_gate` decision the gate-run plan reads before launch.
</objective>

<naming_constraint>
The file is `34.5-CYCLE7-REVIEW.md`, **cycle-scoped on purpose**. This project has a measured
instance of an automated `code_review_gate` silently overwriting `{phase}-REVIEW.md`. Do not create
or write `34.5-REVIEW.md`.
</naming_constraint>

<context>
@.planning/PROJECT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-ROUTING.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review cycle 7's diffs against this project's own measured defect classes</name>
  <files>(read-only review; no production file is modified)</files>
  <read_first>
    - .planning/phases/.../34.5-52-SUMMARY.md, 34.5-53-SUMMARY.md, 34.5-54-SUMMARY.md,
      34.5-55-SUMMARY.md (each plan's own claimed RED captures and counts)
    - The full diff of this cycle: `git diff --stat` and `git diff` restricted to the union of the
      four plans' `files_modified`
    - src-tauri/src/main.rs (the new banner helpers, their tests, and both wiring sites)
    - src/backend/storeManagers/nile/user.ts (both redacted call sites and the helper pair)
    - src/frontend/helpers/declaredUnavailable.ts and the five wrapped component files
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md Section 4
      (the two rules this project has paid for twice)
  </read_first>
  <action>
Review the cycle's diff against the defect classes this project has actually measured, not a
generic checklist. Each numbered item below is a REQUIRED review pass with a recorded verdict.

1. **Vacuous assertions.** For every test file this cycle added or edited, grep for
   `not.toMatch(`, `not.toContain(` and `expect(...).toBeTruthy()` used as a negative proof. Every
   negative fact must be asserted by collecting an explicit string and checking `toContain` against
   it, preceded by a non-vacuity anchor. Count and list every occurrence found; this project has
   shipped 7 vacuous assertions in a single phase inside files a coverage map marked COVERED.
2. **RED proofs actually captured.** Each of plans 34.5-52, -53, -54 and -55 was required to
   capture specific RED failures verbatim in its SUMMARY. Enumerate the required captures — 52:
   three (innerHTML swap, z-index raise, keydown listener); 53: three (restore site 1, restore site
   2, third raw log line); 54: three (rename marker, move early return, remove reg-add loops); 55:
   three (unwrap a site, delete a site, remove the guard token). For each of the twelve, record
   PRESENT or MISSING. **A missing capture is a `blocker`, not a `note`** — an assertion never shown
   to fail is not evidence.
3. **Restoration proofs.** Plans 34.5-54 and 34.5-55 edited production source temporarily to
   produce RED. Confirm each SUMMARY contains an empty `git diff` for the temporarily-edited file,
   and independently re-run that `git diff` now.
4. **Production call shape.** Confirm no test this cycle added reconstructs a call the production
   code does not make. Specifically: plan 34.5-53's behavioral group must drive `NileUser.login`
   and `getLoginData` as production calls them, not a hand-built logger invocation; plan 34.5-55's
   gate must read the real component sources, not a fixture copy.
5. **Grep-gate hygiene.** For every source-text gate this cycle added, confirm the pattern would
   not be satisfied by a comment. If a gate counts occurrences in a file that also documents those
   occurrences in prose, the count is self-invalidating; require comment stripping or a positional
   assertion. Check plan 34.5-54's `dxvkEvidenceLines.test.ts` specifically, since it asserts on
   literals it also names in its own header comment — confirm it reads `tools/index.ts`, not itself.
6. **Localisation.** Confirm plan 34.5-55 added only inline-default `t(key, englishDefault)` calls,
   ran no `pnpm i18n`, left the generated catalogs untouched, and listed its new keys in its
   SUMMARY for the standing catalog-drift triage. Run the Meta jest project including
   `hardcodedStringGate.test.ts` and record the result.
7. **Scope discipline.** Confirm plans 34.5-29, 34.5-30 and 34.5-31 are byte-unchanged and are not
   referenced anywhere in this cycle's diff as pending work — they are HALTED and SUPERSEDED, and
   are deliberately left unticked on disk as the record of their own halt.
8. **Additive/reversible invariant (REQ-34.5-13).** Run `npx tsc --noEmit`, `npm run test:ci`,
   `cargo check` and `cargo test`, and record every count. Confirm `electronUntouched.test.ts` and
   `sidecarRejectionGuard.test.ts` are green and were not weakened. Run
   `preload-surface-gate.py` and `ported-channels-gate.py` and record their exits.
9. **Credential hygiene over the cycle's own diff.** Run the phase's standing redaction sweep
   (`git log -p` restricted to this cycle's commits, matched against the seven value classes,
   including `code_verifier`) and record the output. The published-SteamID64 exception applies;
   anything else is a blocker.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test:ci</automated>
  </verify>
  <acceptance_criteria>
    - All nine passes are performed and each has a recorded verdict.
    - The twelve required RED captures are individually listed as PRESENT or MISSING.
    - `npx tsc --noEmit` exits 0; `cargo check` and `cargo test` exit 0; `npm run test:ci` passes at
      or above the pre-cycle count, with both counts recorded.
    - `preload-surface-gate.py` and `ported-channels-gate.py` both exit 0.
    - `git diff --stat` confirms `34.5-29-PLAN.md`, `34.5-30-PLAN.md` and `34.5-31-PLAN.md` are
      untouched by this cycle.
    - The redaction sweep output is recorded, with every hit classified as either the accepted
      SteamID64 exception or a blocker.
  </acceptance_criteria>
  <done>Every pass has a recorded verdict backed by a command output, not an impression.</done>
</task>

<task type="auto">
  <name>Task 2: Write 34.5-CYCLE7-REVIEW.md with severities and an explicit halt decision</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-REVIEW.md</files>
  <read_first>
    - The outputs collected in Task 1
    - .planning/phases/.../34.5-CYCLE7-ROUTING.md (so findings are routed by the cycle's own
      conventions and finding-id sequence)
    - .planning/phases/.../34.5-54-SUMMARY.md (which finding id is next free — `F-34.5-G6-22` unless
      that plan's SUMMARY says otherwise)
  </read_first>
  <action>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-REVIEW.md`.

**Frontmatter:** `phase`, `plan: 57`, `type: code-review`, `cycle: 7`, `created: 2026-08-13`,
`reviews: [34.5-52, 34.5-53, 34.5-54, 34.5-55]`, `findings_blocker: <n>`, `findings_major: <n>`,
`findings_note: <n>`, and **`halt_gate: true|false`** — the single field plan 34.5-59 reads before
launching. `halt_gate` is `true` if and only if `findings_blocker` is greater than zero.

**Body:**
- A short statement of WHY this review runs at wave 3 rather than after the gate: a review that runs
  after the reconciliation sweep is unledgered by construction, measured three cycles running on a
  sibling phase. Name the ordering as the fix.
- The nine passes, one section each, with the command run, its raw output (trimmed to what is
  load-bearing) and the verdict.
- A findings table: id, severity (`blocker` / `major` / `note`), file, what, why it matters,
  disposition. Allocate ids continuing the `F-34.5-G6-` sequence from the next free number.
- **The halt decision, stated explicitly.** If `halt_gate: true`: state which blocker(s) caused it,
  and that plan 34.5-59 must not launch until each is fixed and this file is amended to
  `halt_gate: false` with the fix recorded. If a blocker is fixable within two files, fix it in this
  plan and record the fix inline; otherwise record it as a cycle-8 routing candidate and keep the
  halt.
- A closing statement that this review makes no claim about live behaviour. Every pass here is
  static. A green suite is necessary and never sufficient — this phase's gate has beaten one four
  times running.

Do NOT edit `34.5-UNTESTED-ITEMS.md`, `deferred-items.md`, `STATE.md` or `ROADMAP.md` from this
plan. If this review produces a finding that creates a new untested hole, record it here and name
plan 34.5-60 as the plan that ledgers it, stating the id it should receive — that keeps the
ordering constraint (review before propagation) intact without two plans contending for the ledger
in the same wave.
  </action>
  <verify>
    <automated>grep -c "halt_gate:" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and its frontmatter contains `halt_gate:` with a literal `true` or `false`.
    - `findings_blocker` equals the number of rows in the findings table whose severity is
      `blocker`, and `halt_gate` is `true` if and only if that number is greater than zero — state
      both numbers and assert the relation in the file's own text.
    - All nine passes appear as named sections with a command and an output.
    - The file `34.5-REVIEW.md` does NOT exist (`test ! -f` on that path).
    - `git diff --stat` from this plan lists exactly one file.
    - Every finding id is unique and continues the `F-34.5-G6-` sequence without reusing `-16`
      through `-21`.
  </acceptance_criteria>
  <done>The gate-run plan has a single, unambiguous field to read before launching, and any finding this review produced has a named home before the sweep begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review findings -> the ledger | A finding produced after the reconciliation sweep has nowhere to land and silently disappears |
| review file -> automated review tooling | An automated gate has been measured overwriting `{phase}-REVIEW.md` |
| cycle diff -> public fork history | A credential committed during the cycle is permanent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C7-23 | Repudiation | review-to-sweep ordering | mitigate | This plan is wave 3; the propagation sweep is wave 6. Any finding here has three waves in which to be ledgered. Measured failure mode: three cycles of unledgered reviews on a sibling phase. |
| T-34.5-C7-24 | Tampering | the review file itself | mitigate | Cycle-scoped filename `34.5-CYCLE7-REVIEW.md`; the acceptance criteria assert `34.5-REVIEW.md` does not exist, so an automated gate cannot silently clobber this record. |
| T-34.5-C7-25 | Information disclosure | the cycle's own commits | mitigate | Pass 9 runs the phase's standing seven-class redaction sweep over the cycle's commits, with the SteamID64 exception applied explicitly and everything else classified as a blocker. |
| T-34.5-C7-26 | Spoofing | a green suite presented as a pass | mitigate | Pass 2 requires twelve individually-named RED captures; a missing one is a blocker. A test that has never been shown to fail is treated as absent, not as passing. |
| T-34.5-C7-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package. If a review fix requires one, the Package Legitimacy Gate applies and a blocking human checkpoint precedes the install. |
</threat_model>

<verification>
- `34.5-CYCLE7-REVIEW.md` exists with a resolved `halt_gate` value.
- All nine passes recorded with commands and outputs.
- Twelve RED captures individually accounted for.
- Static gates all green with counts recorded.
- `34.5-REVIEW.md` absent.
</verification>

<success_criteria>
- The review runs before the gate and before the sweep.
- Its findings have ids and a named ledger home.
- A blocker halts wave 5 rather than travelling into it.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-57-SUMMARY.md` when done.
</output>
