---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 46
type: execute
wave: 2
depends_on: []
files_modified:
  - src/backend/sidecar/steamFlowRegistration.ts
  - src/backend/sidecar/__tests__/steamFlows.test.ts
  - src/backend/__tests__/storeChangeNotifier.test.ts
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
autonomous: true
gap_closure: true
requirements: [REQ-34.5-12, REQ-34.5-13]

must_haves:
  truths:
    - "A `launch` invoke naming runner=gog dispatches to the GOG game manager and emits no steam:// URL"
    - "A `launch` invoke naming runner=steam still emits exactly the steam://rungameid/<id> openExternal frame it emits today"
    - "A `launch` invoke carrying an unknown, absent or prototype-chain runner fails closed with status 'error' and a log line naming the rejected value — it NEVER falls through to Steam"
    - "The sidecar's uninstall write to gogInstalledGamesStore reaches notifyStoreChanged AND survives pushStoreChanged's isAllowedStoreField gate"
    - "The Electron build's `launch` behaviour is unchanged (this handler is sidecar-only)"
  artifacts:
    - path: "src/backend/sidecar/steamFlowRegistration.ts"
      provides: "a runner-aware `launch` invoke handler with a fail-closed unknown-runner guard"
      contains: "libraryManagerMap"
    - path: "src/backend/sidecar/__tests__/steamFlows.test.ts"
      provides: "behavioural dispatch coverage for a NON-steam runner through the production sidecar wiring"
      contains: "runner: 'gog'"
    - path: "src/backend/__tests__/storeChangeNotifier.test.ts"
      provides: "proof that the production gogInstalledGamesStore write announces itself and is allow-listed"
      contains: "gogInstalledGamesStore"
  key_links:
    - from: "src/backend/sidecar/steamFlowRegistration.ts handleLaunch"
      to: "libraryManagerMap[runner]"
      via: "own-property guarded map index, then getGame(appName) / launchEventCallback"
      pattern: "libraryManagerMap\\["
    - from: "src/backend/storeManagers/gog/electronStores.ts installedGamesStore"
      to: "src/backend/storeChangeNotifier.ts notifyStoreChanged"
      via: "TypeCheckedStoreBackend.set announcing the write"
      pattern: "notifyStoreChanged"
---

<objective>
Close the `34.5-UAT.md` gap **"Pressing Play on a GOG game launches it through its own runner"**, and
**settle** — rather than assume — whether commit `eb117d9e4` already covers the sibling gap
**"uninstall completes in the backend but the tile still reads installed"**.

Purpose: `src/backend/sidecar/steamFlowRegistration.ts`'s `handleLaunch` (L238-251) destructures only
`appName` out of its `LaunchParams` and then does `const game = new SteamGame(appName)` —
**unconditionally, ignoring `runner` entirely**. Live-observed twice in `gamelib.log`:
`[Steam]: SteamGame: launching appId 1207659037 via steam://rungameid/1207659037` for a title whose
`runner` is `gog`. The second occurrence (00:33:42) landed *after* the 00:33:24 uninstall completed,
so it had no installed game to launch either.

This is **structurally the same runner-blind dispatch defect plan 34.5-33 fixed for `refreshLibrary`
in this same file**, in the same module, left behind because 34.5-33's routing item named only the
read flow. The module docstring says so in its own words (L9-20: `refreshLibrary` -> "the real
`libraryManagerMap[runner].refresh()` (Gap cycle 4, plan 34.5-33 — see below)" immediately followed by
`launch` -> "the real `SteamGame.launch()`", a Phase 27 walking-skeleton binding never revisited).

Output: a runner-aware, fail-closed `launch` handler; a behavioural test that exercises a NON-steam
runner through the production sidecar wiring; and a recorded, test-backed verdict on `eb117d9e4`.

**Live observable that would prove Defect A — nothing else counts.** In a live `pnpm tauri:dev`
session, pressing Play on an installed GOG title must produce a GOG launch in
`~/Library/Logs/GameLib/gamelib.log` and **zero** `steam://rungameid` lines for that title. A passing
suite proves the dispatch wiring only. This phase's live gate has beaten a fully-green suite three
times — do NOT record this defect as closed on unit evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE6-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-33-PLAN.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.planning/debug/resolved/gog-login-ui-never-updates.md
@src/backend/sidecar/steamFlowRegistration.ts

<interfaces>
<!-- Extracted from the live tree at planning time. The executor should use these directly. -->

src/common/types.ts:40 — the handler's own argument shape. `runner` is ALREADY on the wire; the
current handler simply throws it away:
  LaunchParams = { appName: string; launchArguments?: LaunchOption; runner: Runner;
                   skipVersionCheck?: boolean; args?: string[] }

src/common/types/game_manager.ts:71 — LibraryManager.getGame(id: string) => Game
src/common/types/game_manager.ts:30,44 — Game.launch(logWriter: LogWriter,
  launchArguments?: LaunchOption, args?: string[], skipVersionCheck?: boolean) => Promise<boolean>

src/backend/storeManagers/index.ts:14 — libraryManagerMap, a plain object literal with exactly the
six keys sideload | gog | legendary | nile | zoom | steam, `satisfies Record<Runner, LibraryManager>`.
ALREADY IMPORTED at steamFlowRegistration.ts:69 as the load-bearing first import.

src/backend/storeManagers/steam/library.ts:535 — SteamLibraryManager.getGame(id: string): SteamGame
src/backend/storeManagers/gog/library.ts:94 — GOGLibraryManager.getGame(id: string): GOGGame (cached)

src/backend/launcher.ts:111 + :2086 — `launchEventCallback: (args: LaunchParams) => StatusPromise`,
IS exported. Its body is the reference dispatch shape:
  const { libraryManagerMap } = await import('backend/storeManagers')
  const game = libraryManagerMap[runner].getGame(appName)
…and at :194 it creates the LogWriter the non-Steam managers require:
  const logWriter = await createGameLogWriter(appName, runner)

src/backend/storeChangeNotifier.ts — setStoreChangeNotifier(fn | null), notifyStoreChanged(payload),
notifyStoreInvalidated(store), withStoreChangeSuppressed(fn), hasStoreChangeNotifier()
src/backend/electron_store.ts:108-125 — TypeCheckedStoreBackend.set/delete/clear each announce
src/common/types/storePolicy.ts:105 — STORE_ALLOWLIST.gogInstalledGamesStore = ['installed']
src/common/types/storePolicy.ts:231 — isAllowedStoreField(storeName, key)
src/backend/storeManagers/gog/games.ts:915 uninstall(...) -> :986 installedGamesStore.set('installed', array)
</interfaces>

<facts_verified_against_the_live_tree>
These four were checked at planning time. Two of them CORRECT the outline, so read them before
following the outline's brief.

1. **`libraryManagerMap` needs no new import.** It is already this file's line-69 load-bearing first
   import. The fix therefore adds **zero** modules to the sidecar bundle. `SteamGame`'s direct import
   (L72) has exactly ONE consumer, L243 — after this change it becomes unused and must be deleted, so
   the net effect on the import graph is **negative**, not additive.

2. **The outline's `launcher.ts` import fence is STALE and no longer bites.** Plan 34.5-44's finding
   is confirmed: `src/backend/sidecar/wineToolsFlowRegistration.ts:39` already does
   `import { runWineCommand, validWine } from '../launcher'`, and `handlers.ts:87` calls
   `registerWineToolsFlows()`. `launcher.ts` is therefore ALREADY in the sidecar's import graph and
   the cost the module docstring (L22-27) warns about is already paid. **The real constraint that
   survives** is the module's *curated-import discipline*: do not grow the graph FURTHER than the fix
   needs, and never import a module into this file without saying in a comment why it is there. Update
   the docstring's L22-27 paragraph rather than leaving a claim the tree contradicts.

3. **`GOGGame.launch` dereferences its `logWriter` argument unconditionally** — `prepareLaunch(
   gameSettings, logWriter, gameInfo)` at gog/games.ts:542 and `logWriter.logError([...])` at :545.
   The current handler's `undefined as unknown as LogWriter` hack (L249, legal only because
   `SteamGame.launch` names the parameter `_logWriter` and never touches it) would therefore throw a
   TypeError the moment a GOG launch reached it. **A one-line constructor swap is NOT a working fix.**

4. **No existing test mocks the defect away** — but the coverage hole is exactly defect-shaped.
   `skeletonFlows.test.ts` Test 2 (:267) and Test 3 (:290) are the only `launch` coverage in the tree
   and both pass `runner: 'steam'`, so a runner-blind handler and a runner-aware one are
   indistinguishable to them. Its only store-manager mock is `jest.mock('../../storeManagers/steam/user')`;
   there is no `jest.mock('../../storeManagers')` and no `jest.mock('.../steam/games')`. **Nothing
   needs deleting** — this is not the `shortcutsFlows.test.ts` shape plan 34.5-45 found. Say so
   explicitly in the SUMMARY so the next reader does not re-derive it. Both tests MUST stay green
   unmodified; if either needs editing to pass, the Steam branch has regressed.
</facts_verified_against_the_live_tree>

<wrong_fix_trap>
**Do not "make the Steam path use the right id."** A non-Steam game added to Steam as a shortcut is
addressed by a **computed 64-bit shortcut id**, never by the source store's `appName` — so no
correction to the `steam://rungameid/<appName>` construction could ever have launched the right thing
for a GOG title. `SteamGame`'s `buildSteamProtocolUrl` numeric-appId guard (T-27-08) is working
correctly and is not implicated. **The defect is dispatch, before any shell invocation.** Any diff
that touches `buildSteamProtocolUrl`, `steam/games.ts`, or the shortcut-id computation is the wrong
branch and must be reverted.
</wrong_fix_trap>

<fences>
`use_worktrees=false` — every plan in this wave executes on the main tree, so an overlap is a real
collision. Do NOT edit, under any circumstance:
- `src/common/types/ipc.ts` — plan 34.5-43
- `src/backend/sidecar/electronStub.ts`, `src/backend/sidecar/shortcutsFlowRegistration.ts`,
  `src/preload/tauriTransport.ts`, `src/backend/sidecar/sidecarRpc.ts` — plan 34.5-45 (same wave)
- `src-tauri/src/main.rs` — plan 34.5-44
- anything under `src/frontend/` — plans 34.5-47 and 34.5-48 (same wave)
- `.planning/debug/resolved/gog-login-ui-never-updates.md` — it is the record of a completed debug
  session, not a working document. Record this plan's verdict in the SUMMARY and the ledger instead.
- `src/backend/sidecar/__tests__/skeletonFlows.test.ts` — must stay byte-identical; it is this
  plan's Steam-side regression detector, and editing it to make a change pass destroys that.

`34.5-UNTESTED-ITEMS.md` is shared with plan 34.5-49 this wave. Edit it **append-only**, adding one
new row under its existing table; never rewrite, reorder or reflow an existing row. Executors run
sequentially on the main tree, so an append-only edit to a distinct row is mergeable.

**You OWN `steamFlowRegistration.ts` this cycle.** Plan 34.5-45's dropped-return sweep is fenced OUT
of it for that reason. Task 1 therefore carries the audit for this file (see its `<action>`), and any
finding cross-references `U-34.5-15`.
</fences>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make handleLaunch runner-aware and fail-closed</name>

  <read_first>
    - `src/backend/sidecar/steamFlowRegistration.ts` — the whole file. `handleRefreshLibrary`
      (L93-232) is the in-file precedent you are matching: its own-property guard (L123), its
      named-runner rethrow-with-log (L140-148), and its JSON-transport `null` comment (L97-111).
    - `src/backend/launcher.ts` L111-300 — `launchEventCallback`, the reference dispatch shape and
      the `createGameLogWriter` call at :194.
    - `src/backend/storeManagers/gog/games.ts` L515-560 — proof that `logWriter` is dereferenced.
    - `src/backend/storeManagers/steam/games.ts` L1440-1450 — proof that `SteamGame.launch` names its
      first parameter `_logWriter` and never touches it.
    - `src/backend/storeManagers/index.ts` — the exact six keys of `libraryManagerMap`.
    - `src/backend/sidecar/wineToolsFlowRegistration.ts` L35-45 — confirm the `../launcher` import
      before you rewrite the docstring's fence paragraph.
  </read_first>

  <behavior>
    - `runner: 'steam'` + numeric appName -> unchanged: one `steam://rungameid/<id>` openExternal
      frame, response `{ status: 'done' }`.
    - `runner: 'steam'` + non-numeric appName -> unchanged: no openExternal frame, `{ status: 'error' }`.
    - `runner: 'gog'` -> dispatches to the GOG manager; zero `steam://` URLs emitted for that call.
    - `runner` absent / `null` / `'STEAM'` / `'constructor'` / `'toString'` / `'__proto__'` /
      `'hasOwnProperty'` -> `{ status: 'error' }`, one log line naming the rejected value, and **no**
      `openExternal` frame and **no** manager call of any kind.
  </behavior>

  <action>
    Rewrite `handleLaunch` (currently L238-251) so it dispatches on `runner`.

    Read the params as `const params = (args[0] ?? {}) as Partial<LaunchParams>` and pull `appName`,
    `runner`, `launchArguments`, `args`, `skipVersionCheck` off it. Keep treating a missing first
    argument as an empty object, as today.

    **Guard first, dispatch second.** Reject before touching the map when `appName` is not a non-empty
    string, or when `rawRunner` is not a string, or when
    `Object.prototype.hasOwnProperty.call(libraryManagerMap, rawRunner)` is false. Use the
    own-property form — never the `in` operator and never a bare `libraryManagerMap[rawRunner]` truth
    test — for the reason already documented at L118-122 of this file for `handleRefreshLibrary`
    (T-34.5-C4-10): a bare index resolves through `Object.prototype`, so `constructor` / `toString` /
    `valueOf` / `hasOwnProperty` come back as FUNCTIONS rather than `undefined` and slip past a
    fail-closed check. On rejection, call `logWarning` with a message of the exact shape
    `launch failed runner=<rawRunner> appName=<appName>: unknown runner` under `LogPrefix.Backend`,
    and `return { status: 'error' }`. **Do not throw and do not fall through to Steam** — silently
    refreshing/launching Steam on an unrecognised runner is the exact defect this plan exists to
    close, and the guard must be structurally incapable of reaching the Steam branch.

    Then branch. `runner === 'steam'` keeps TODAY'S curated behaviour byte-for-byte in effect:
    `libraryManagerMap.steam.getGame(appName)` (the map, not `new SteamGame(...)`, so the map is the
    single source of dispatch truth for every runner) followed by the same
    `await game.launch(undefined as unknown as LogWriter)` call and the same
    `{ status: launched ? 'done' : 'error' }` mapping. Keep and update the existing comment explaining
    why the `LogWriter` argument is legitimately absent on this branch — it is now load-bearing in a
    second way, since it documents exactly why the OTHER branch cannot reuse it.

    Every other runner delegates to `launchEventCallback` imported from `../launcher`, passing
    `{ appName, runner, launchArguments, args, skipVersionCheck }`, and returns its `StatusPromise`
    result directly. Reasons to state in a comment above the import, all three of them:
    (a) `launcher.ts` is already in this bundle's graph via `wineToolsFlowRegistration.ts:39`, so this
    adds no module; (b) the non-Steam managers dereference their `logWriter` argument unconditionally
    (`gog/games.ts:542,545`), and `launchEventCallback:194`'s `createGameLogWriter(appName, runner)` is
    the only thing in the tree that produces one; (c) it also supplies `sendGameStatusUpdate`, the
    install-path existence check, save sync and the before/after-launch scripts, all of which the
    renderer's tile state depends on.

    State in the same comment WHY Steam does not go through `launchEventCallback`: the Steam native
    path deliberately hands off to the Steam client via `steam://rungameid` instead of running a local
    binary, so `launchEventCallback`'s `existsSync(gameInfo.install.install_path)` precheck and its
    `askForceUninstall` branch would abort a perfectly valid Steam launch. That branch is live-proven
    working and is preserved on purpose, not by omission.

    Delete the now-unused `import SteamGame from '../storeManagers/steam/games'` (L72). Confirm with
    a comment-stripped grep that no other consumer exists in the file before deleting.

    Update the module docstring: the `launch` bullet (L17-20) and the fence paragraph (L22-27). The
    fence paragraph currently asserts this module "deliberately does NOT import `launcher.ts`'s
    `launchEventCallback`" — that is now false, and a stale in-source claim is precisely the failure
    mode 34.4's gate item 2 died on. Replace it with the surviving constraint: the graph is curated,
    `launcher.ts` was already in it before this change, and nothing may be added to this file without
    a stated reason. Add a GAP CYCLE 6 paragraph in the same voice as the existing GAP CYCLE 4 one
    (L29-39), naming this defect, its live log line, and that it is the same shape 34.5-33 fixed.

    **Dropped-return audit for this file (`U-34.5-15` cross-reference, since plan 34.5-45's sweep is
    fenced out of it).** Both handlers registered here are invoke-kind. Confirm from
    `skeletonFlows.test.ts`'s Test 2 assertion (`result: { status: 'done' }` arrives in the response
    frame) that `launch`'s return value survives the transport today, and that `refreshLibrary`
    legitimately returns `void`. Record the outcome — "no drop found in this file", or the defect and
    its fix — in the SUMMARY with an explicit `U-34.5-15` cross-reference. Do not edit any of plan
    34.5-45's files to chase it.
  </action>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Comment-stripped source assertion, run as:
      `grep -vE '^\s*(//|/\*|\*)' src/backend/sidecar/steamFlowRegistration.ts | grep -c 'new SteamGame'`
      returns **0**. (Comment stripping is mandatory — this file is majority comment and the new
      docstring paragraph will itself name `new SteamGame(appName)` when describing the defect, which
      would make an unfiltered count self-invalidating.)
    - Same stripped stream: `grep -c "hasOwnProperty.call(libraryManagerMap"` returns **2** (the
      existing `refreshLibrary` guard plus the new `launch` guard).
    - Same stripped stream: `grep -c "SteamGame"` returns **0** — the direct import is gone.
    - `grep -c "launchEventCallback" src/backend/sidecar/steamFlowRegistration.ts` returns at least 2
      (import + call site).
    - `npx jest src/backend/sidecar/__tests__/skeletonFlows.test.ts` passes with the file **unmodified**
      (`git diff --stat src/backend/sidecar/__tests__/skeletonFlows.test.ts` is empty).
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && npx jest src/backend/sidecar/__tests__/skeletonFlows.test.ts</automated>
  </verify>

  <done>
    `handleLaunch` reads `runner`, guards it with an own-property check that cannot reach the Steam
    branch on an unrecognised value, dispatches Steam through `libraryManagerMap.steam.getGame` and
    every other runner through `launchEventCallback`; the `SteamGame` direct import is deleted; the
    docstring no longer asserts a fence the tree contradicts; `skeletonFlows.test.ts` is green and
    unmodified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the dispatch behaviourally for a NON-steam runner</name>

  <read_first>
    - `src/backend/sidecar/__tests__/skeletonFlows.test.ts` L1-135 (the isolation preamble: the
      `jest.mock('os')` disposable-homedir block at L72, `jest.mock('electron', () =>
      jest.requireActual('../electronStub'))` at L87, the `electron-store` / `axios` /
      `backend/utils` / `backend/constants/environment` mocks) and L264-305 (`writeInvoke` /
      `startSidecar` / `flush` usage for the two existing `launch` tests).
    - `src/backend/sidecar/handlers.ts` L71, L111 — the production registration path
      (`registerSteamFlows()`), which is what the new test must drive.
    - `src/backend/sidecar/steamFlowRegistration.ts` as edited by Task 1.
    - `.planning/phases/.../34.5-UNTESTED-ITEMS.md` — the Rules section, for the row you add.
  </read_first>

  <behavior>
    - Test A (the defect, RED before Task 1): a `launch` invoke frame carrying
      `{ appName: '<gog-id>', runner: 'gog' }` produces **zero** frames whose `kind` is
      `openExternal` with a `steam://` URL. Against the pre-fix handler this test FAILS because
      `steam://rungameid/...` is emitted; that is the point of it.
    - Test B (the guard): a `launch` invoke frame with `runner` absent, and again with
      `runner: 'constructor'`, and again with `runner: 'STEAM'`, each responds
      `{ ok: true, result: { status: 'error' } }` and emits no `openExternal` frame.
    - Test C (Steam parity): `{ appName: '999002', runner: 'steam' }` still emits
      `steam://rungameid/999002` and responds `{ status: 'done' }` — the same assertion
      `skeletonFlows.test.ts` Test 2 makes, restated here so this file alone can detect a Steam
      regression.
  </behavior>

  <action>
    Create `src/backend/sidecar/__tests__/steamFlows.test.ts` (it does not exist today — the phase's
    only `launch` coverage lives in `skeletonFlows.test.ts`).

    **Import the production wiring; do not reconstruct the call site.** The test drives the sidecar
    exactly as `skeletonFlows.test.ts` does — write a real invoke frame into the sidecar's stdin and
    read the emitted frames — so it exercises `handlers.ts`'s `registerSteamFlows()` registration and
    the real transport. A test that imports `handleLaunch` directly and calls it with a hand-built
    argument array is a replica of the call site and will drift silently; this phase has an in-cycle
    precedent (plan 34.5-45's `shortcutsFlows.test.ts` finding, where a mock asserted the defect as
    correct). Reuse `skeletonFlows.test.ts`'s isolation preamble verbatim, including the
    `jest.mock('os', ...)` disposable-homedir redirect — without it the suite reaches the developer's
    real `~/Library/Application Support/GameLib` store files.

    For the GOG case, mock only what is needed to keep the assertion about DISPATCH rather than about
    a real launch succeeding: mock `backend/launcher`'s `launchEventCallback` to a `jest.fn()`
    resolving `{ status: 'done' }`, and assert both (a) that it was called once with an object whose
    `runner` is `'gog'` and whose `appName` matches, and (b) that no `steam://` openExternal frame was
    emitted. Do **not** mock `backend/storeManagers` or `storeManagers/steam/games` — mocking either
    would erase the very dispatch this test exists to observe, which is the exact failure shape called
    out above.

    **Prove Test A RED against the known-bad input at authoring time.** Temporarily restore the
    single-line pre-fix body (`const game = new SteamGame(appName)` plus its `launch` call), run
    `npx jest src/backend/sidecar/__tests__/steamFlows.test.ts`, confirm Test A FAILS with a
    `steam://rungameid` URL in the failure output, then restore the fix and confirm it passes. Paste
    the RED failure line into the SUMMARY. An assertion never shown to fail is not evidence — this
    project has shipped unmatchable patterns that passed vacuously.

    Add one append-only row to `34.5-UNTESTED-ITEMS.md` (Ledger Rule 2) for what stays live-only:
    **no automated test drives a real GOG game to a real launched process** — Test A proves dispatch
    reaches `launchEventCallback` with `runner='gog'` and nothing beyond it. The row's retirement
    condition is a live `pnpm tauri:dev` session where pressing Play on an installed GOG title
    produces a GOG launch in `gamelib.log` **and zero `steam://rungameid` lines for that title**.
    Name it so plan 34.5-50 can pick it up under Rule 3. Append only; do not touch existing rows.
  </action>

  <acceptance_criteria>
    - `npx jest src/backend/sidecar/__tests__/steamFlows.test.ts` exits 0 with Tests A, B and C all
      present and passing.
    - The RED proof is recorded in the SUMMARY: the verbatim failure output of Test A against the
      restored pre-fix `new SteamGame(appName)` body, showing a `steam://rungameid` URL.
    - `grep -c "runner: 'gog'" src/backend/sidecar/__tests__/steamFlows.test.ts` returns at least 1.
    - `grep -c "jest.mock('backend/storeManagers'\|jest.mock('../../storeManagers'" src/backend/sidecar/__tests__/steamFlows.test.ts`
      returns **0** — the dispatch under test is not mocked away.
    - `git diff --stat src/backend/sidecar/__tests__/skeletonFlows.test.ts` is empty.
    - `34.5-UNTESTED-ITEMS.md` gained exactly one row: `git diff --numstat` on that file shows
      deletions **0**.
  </acceptance_criteria>

  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/steamFlows.test.ts src/backend/sidecar/__tests__/skeletonFlows.test.ts</automated>
  </verify>

  <done>
    A production-wiring test file exists that fails against the pre-fix handler and passes against the
    fixed one, covers the unknown-runner guard and Steam parity, mocks nothing that would hide the
    dispatch, and the live-only remainder has its own ledger row.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Settle whether eb117d9e4 already covers the uninstall-not-reflected symptom</name>

  <read_first>
    - `.planning/debug/resolved/gog-login-ui-never-updates.md` — specifically the "Blast radius —
      CORRECTED AND MUCH WIDER" table (the `gogInstalledGamesStore` row names this exact UAT gap),
      the `## Resolution` `fix:` block listing `eb117d9e4`'s files, and the timestamps in
      `## LIVE VERIFICATION — PASSED 2026-08-03 01:39-01:40`.
    - `src/backend/storeChangeNotifier.ts` — the whole file; `setStoreChangeNotifier`,
      `withStoreChangeSuppressed`, `notifyStoreChanged`.
    - `src/backend/electron_store.ts` L100-130 — `set`/`delete`/`clear` and their announcements.
    - `src/backend/sidecar/storeWriteHandlers.ts` L233-260 — `pushStoreChanged` and its
      `isAllowedStoreField` secret-leak gate, which is where a push can still be silently dropped.
    - `src/common/types/storePolicy.ts` L105 and L231-271 — the allow-list entry and the lookup.
    - `src/backend/storeManagers/gog/games.ts` L915-990 — `uninstall`, ending in
      `installedGamesStore.set('installed', array)` at :986.
    - `src/backend/__tests__/storeChangeNotifier.test.ts` — its existing isolation preamble; match it.
  </read_first>

  <behavior>
    - Writing through the PRODUCTION `installedGamesStore` exported by
      `src/backend/storeManagers/gog/electronStores.ts` — not a locally-constructed
      `TypeCheckedStoreBackend` — emits exactly one `notifyStoreChanged` payload with
      `store === 'gogInstalledGamesStore'` and `key === 'installed'`.
    - `isAllowedStoreField('gogInstalledGamesStore', 'installed')` is `true`, so `pushStoreChanged`
      does not drop that payload at its secret-leak gate.
    - A payload for a field NOT on that store's allow-list (e.g. `'credentials'`) IS dropped —
      the negative half, so the assertion is not vacuous.
  </behavior>

  <action>
    Extend `src/backend/__tests__/storeChangeNotifier.test.ts` with a `gogInstalledGamesStore` block.

    **Verify the chain, do not assert it.** There are three links between the sidecar's uninstall and
    the renderer's tile, and the fix only covers the symptom if all three hold:
    (1) `GOGGame.uninstall` writes through `installedGamesStore.set('installed', …)`
    (gog/games.ts:986); (2) that store is a `TypeCheckedStoreBackend`, whose `set` announces via
    `notifyStoreChanged` (electron_store.ts:113); (3) `pushStoreChanged` forwards it only if
    `isAllowedStoreField('gogInstalledGamesStore', 'installed')` — and `STORE_ALLOWLIST` grants
    exactly `['installed']` for that store (storePolicy.ts:105). Link 3 is the one that could still
    silently drop the push and is the reason this task exists rather than a paper assertion.

    Import the real `installedGamesStore` from `backend/storeManagers/gog/electronStores`, install a
    capturing `jest.fn()` via `setStoreChangeNotifier`, perform a `set('installed', [...])`, and
    assert the captured payload. Clear the notifier with `setStoreChangeNotifier(null)` in `afterEach`
    — it is module-level state shared across suites, as its own doc comment warns. Replicate whatever
    isolation the existing file already uses; if it does not redirect `os.homedir()` to a per-process
    tmp directory, add that redirect using `skeletonFlows.test.ts`'s block as the model. This project
    has already had tests clobber real store files under `~/Library/Application Support` by reaching
    the real app-support dir.

    Add the negative case (`'credentials'` is dropped) so the allow-list assertion is proven
    non-vacuous rather than passing because everything passes.

    **Establish the dating fact, which is decisive and is currently only inferable.** Run
    `git log -1 --format='%cI %s' eb117d9e4` and `git log -1 --format='%cI' <the UAT session's HEAD>`
    if recoverable. The UAT uninstall was observed 2026-08-03 **00:33:24**; the resolved debug file
    records the fixed sidecar bundle being built at **01:24:43** and live-verified 01:39-01:40 the
    same night. If `eb117d9e4` post-dates 00:33, then the observed symptom was recorded against a
    build that **did not contain the fix at all**, and the UAT observation is not evidence against it.
    Record the actual command output in the SUMMARY — do not restate the inference as a finding.

    Write the verdict in the SUMMARY in three explicit parts: (a) the three-link chain, each link with
    its file:line and the test that now covers it; (b) the dating fact with its command output;
    (c) what remains UNPROVEN — that a live uninstall makes the tile flip **without a restart**.
    Nothing in this task observes the renderer. `src/frontend/**` is fenced to plans 34.5-47/48 and
    this plan must not reach into it.

    Add one append-only row to `34.5-UNTESTED-ITEMS.md` carrying (c): retirement condition is a live
    `pnpm tauri:dev` session in which a GOG uninstall completes and the Library tile stops presenting
    as installed **with no restart**, with the `Finished uninstalling` line and the tile state observed
    in the same session. Mark it as an observation plan 34.5-50 must fold into the gate-4 contract
    under Rule 3 — per the routing file, this is handed to the gate as an observation, not claimed
    closed here.
  </action>

  <acceptance_criteria>
    - `npx jest src/backend/__tests__/storeChangeNotifier.test.ts` exits 0 with the new
      `gogInstalledGamesStore` positive and negative cases present.
    - The test imports the production store: `grep -c "storeManagers/gog/electronStores" src/backend/__tests__/storeChangeNotifier.test.ts`
      returns at least 1, and no locally-constructed `new TypeCheckedStoreBackend('gogInstalledGamesStore'`
      appears in the file (`grep -c` returns 0).
    - Non-vacuity proven: temporarily neuter `isAllowedStoreField`'s allow-list branch to return
      `true` unconditionally and confirm the negative case FAILS; restore and confirm it passes. The
      RED output is pasted into the SUMMARY.
    - `git log -1 --format='%cI %s' eb117d9e4` output is pasted verbatim into the SUMMARY.
    - `ls ~/Library/Application\ Support/GameLib/gog_store/installed.json` has the same mtime before
      and after the test run (the suite did not touch real store files), or the file does not exist
      in either state.
    - `34.5-UNTESTED-ITEMS.md` `git diff --numstat` shows deletions **0** across both this task's and
      Task 2's edits.
    - `git diff --stat src/frontend` is empty.
  </acceptance_criteria>

  <verify>
    <automated>npx jest src/backend/__tests__/storeChangeNotifier.test.ts</automated>
  </verify>

  <done>
    The three-link chain from `GOGGame.uninstall` to the renderer push is covered by tests against the
    production store, the allow-list assertion is proven non-vacuous, the `eb117d9e4` dating fact is
    recorded from command output rather than inferred, and the remaining live-only half is a ledger
    row addressed to plan 34.5-50 rather than a closure claim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> sidecar RPC (`launch` invoke) | `LaunchParams` crosses JSON-encoded and fully untrusted. `runner` is a bare string used to INDEX an object literal; `appName` is a bare string used to construct a game identity and, on the Steam branch, to build a `steam://` URL handed to the OS opener. |
| sidecar -> OS (`shell.openExternal` / process spawn) | The Steam branch hands a URL to the platform opener; the non-Steam branch reaches `launchEventCallback`, which spawns a real local binary through the Wine/native pipeline. |
| sidecar store write -> renderer snapshot (`STORE_CHANGED_CHANNEL`) | Task 3's path. Sidecar-initiated store payloads reach the renderer's in-memory snapshot; the renderer's listener deliberately does NOT re-filter on the way in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-46-01 | Elevation of Privilege | `handleLaunch`'s `libraryManagerMap[runner]` index | mitigate | `runner` is a renderer-supplied string. A bare index resolves through `Object.prototype`, so `constructor`/`toString`/`valueOf`/`hasOwnProperty`/`__proto__` return FUNCTIONS rather than `undefined` and defeat a `!map[runner]` fail-closed test — the identical hazard CR-02 already hit in `isAllowedStoreField` and T-34.5-C4-10 already hit in `handleRefreshLibrary` in this same file. Guard with `Object.prototype.hasOwnProperty.call(libraryManagerMap, rawRunner)` plus a `typeof rawRunner === 'string'` precheck. Covered by Task 2 Test B (`'constructor'`). |
| T-34.5-46-02 | Tampering | mis-dispatched launch reaching the wrong runner's manager | mitigate | THE SHIPPED DEFECT. A `gog` launch reaching `SteamGame` handed an attacker-influenceable `appName` to `steam://rungameid/<appName>` — contained today only by `buildSteamProtocolUrl`'s numeric guard (T-27-08), i.e. by a downstream accident rather than by dispatch. The inverse is worse: any runner silently reaching a DIFFERENT runner's `getGame(appName)` resolves an install path from the wrong store and can spawn the wrong local binary. Fix is exact-key dispatch with no fallback branch; Task 2 Test A pins it and is proven RED. |
| T-34.5-46-03 | Elevation of Privilege | unknown-runner fallback behaviour | mitigate | An unrecognised runner MUST fail closed with `{ status: 'error' }`, never default to Steam and never throw past the handler. A default-to-Steam fallback is a confused-deputy: the renderer names a runner it is not entitled to and gets a Steam client hand-off. Task 2 Test B covers absent / `'STEAM'` (case) / prototype-chain names. |
| T-34.5-46-04 | Denial of Service | an unhandled throw inside `handleLaunch` | mitigate | `launchEventCallback` can reject (dialog paths, missing prefix, spawn failure). An unhandled rejection inside a sidecar invoke handler is what `sidecarRejectionGuard.test.ts` exists to catch. Return `{ status: 'error' }` on a caught rejection and log it under the runner's `LogPrefix` — matching `handleRefreshLibrary`'s T-34.5-C4-11 log-before-rethrow discipline, except `launch` returns a status rather than rethrowing because its caller renders the result. |
| T-34.5-46-05 | Information Disclosure | Task 3's `STORE_CHANGED_CHANNEL` push for `gogInstalledGamesStore` | accept (verify) | `pushStoreChanged`'s `isAllowedStoreField` gate is the only filter, and the renderer trusts the emitter. `STORE_ALLOWLIST.gogInstalledGamesStore = ['installed']` carries install paths and titles, not credentials; GOG credentials live on `gogConfigStore` whose allow-list omits `credentials` by design. Task 3's negative case pins that `'credentials'`-shaped fields are dropped, so the acceptance is verified rather than assumed. |
| T-34.5-46-06 | Repudiation | a rejected or mis-dispatched launch leaving no trace | mitigate | Every rejection path logs a single greppable line naming the rejected `runner` and `appName`. An un-logged rejection is indistinguishable from a hang, and the live gate reads `gamelib.log` — not the terminal — so an unlogged failure is invisible to the only evidence source the gate accepts. |
| T-34.5-46-SC | Tampering | npm/pip/cargo installs | mitigate | **No new packages.** `34.5-RESEARCH.md`'s `## Package Legitimacy Audit` reads "Not applicable — no new packages" and this plan must keep it true: every symbol it uses (`libraryManagerMap`, `launchEventCallback`, `notifyStoreChanged`, `isAllowedStoreField`) is already in the tree. If any task finds itself reaching for a dependency, STOP and raise a `checkpoint:human-verify gate="blocking-human"` before the install, per the phase's standing supply-chain threat. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0.
2. `npm run test:ci` exits 0 with no suite weakened. Record the suite/test counts and compare against
   the phase's last recorded baseline; a DROP in either count is a regression to explain, not a pass.
3. `src/backend/sidecar/__tests__/skeletonFlows.test.ts` is byte-identical to HEAD
   (`git diff --stat` empty) and green — the Steam-side regression detector.
4. `electronUntouched.test.ts` and `sidecarRejectionGuard.test.ts` green and unweakened (REQ-34.5-13,
   SEAM Invariant B).
5. `electronReachLedger.test.ts` green. This change removes one import and adds one to a module
   already in the graph, so the electron-reach count must NOT move; if it does, the fix pulled in
   something unintended and the diff is wrong.
6. `npm start` (Electron) boots and a launch still works — this handler is sidecar-only, so Electron's
   observable `launch` behaviour must be unchanged (REQ-34.5-13's behavioural invariant).
7. Both RED proofs (Task 2's pre-fix `steam://rungameid` failure, Task 3's neutered-allow-list
   failure) are pasted verbatim into the SUMMARY. An assertion never shown to fail is not evidence.
8. Fence audit: `git diff --name-only` lists ONLY the four files in `files_modified`. Any appearance
   of `src/frontend/**`, `src-tauri/src/main.rs`, `electronStub.ts`, `shortcutsFlowRegistration.ts`,
   `tauriTransport.ts`, `sidecarRpc.ts`, `ipc.ts` or `skeletonFlows.test.ts` is a wave collision.
9. `34.5-UNTESTED-ITEMS.md`: `git diff --numstat` shows deletions 0 (append-only, shared with 34.5-49).
</verification>

<success_criteria>
- `handleLaunch` dispatches on `runner` through `libraryManagerMap` with an own-property guard, and
  is structurally incapable of reaching the Steam branch for a non-`steam` runner.
- A GOG `launch` invoke emits zero `steam://` URLs — proven by a test shown RED against the pre-fix
  body and GREEN after.
- Absent / unknown / prototype-chain runners return `{ status: 'error' }` with a log line, never a
  Steam fallback and never an unhandled rejection.
- Steam's live-proven `steam://rungameid/<id>` behaviour is preserved, proven by an unmodified
  `skeletonFlows.test.ts`.
- The `SteamGame` direct import is gone and the module docstring no longer asserts a `launcher.ts`
  fence the tree contradicts.
- The `eb117d9e4` question is SETTLED with three test-covered links, a non-vacuity proof and a pasted
  `git log` dating fact — and the live half is a ledger row addressed to plan 34.5-50, not a claim.
- The `U-34.5-15` dropped-return audit for this file is recorded either way.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-46-SUMMARY.md`
when done. It MUST contain: both RED proofs verbatim; the `git log -1 --format='%cI %s' eb117d9e4`
output; the three-part `eb117d9e4` verdict; the `U-34.5-15` dropped-return audit outcome for this
file; the statement that no existing test mocked this defect away (unlike `shortcutsFlows.test.ts`);
and the two new `34.5-UNTESTED-ITEMS.md` row IDs for plan 34.5-50 to pick up under Rule 3.
</output>
</content>
</invoke>
