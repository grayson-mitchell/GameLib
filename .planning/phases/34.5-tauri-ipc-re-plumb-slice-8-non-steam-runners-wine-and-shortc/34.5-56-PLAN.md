---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 56
type: execute
wave: 3
gap_closure: true
cycle: 7
depends_on: ["34.5-52", "34.5-53", "34.5-54", "34.5-55"]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
autonomous: true
requirements: [REQ-34.5-12]
authors_gate: 34.5-LIVE-GATE-RERUN-4.md
executed_by: 34.5-59-PLAN.md

must_haves:
  truths:
    - "The fifth blocking gate contract exists, with verdict null and every RESULT slot empty, written before any live work begins."
    - "Every evidence line the contract cites is emitted by current source for the direction the contract's own steps exercise."
    - "Every OPEN ledger row the contract's items would have covered is named as a known hole (Ledger Rule 3), counted against a recount performed at authoring time."
    - "No step anywhere in the contract asks the operator to type into the Tauri DevTools console."
    - "The Structural Reachability Review is delivered as part of this plan, before publication, covering all seven defect-class tests."
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md"
      provides: "The fifth blocking live-gate contract, four items, verdict null, supersedes RERUN-3 without modifying it"
      contains: "verdict: null"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md"
      provides: "blocked_on annotations on U-34.5-29 and U-34.5-27 per D-CYCLE7-B"
      contains: "operator-supplied test fixture"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN-4.md item 4"
      to: "src/backend/tools/index.ts:369 and src/backend/launcher.ts:1520"
      via: "the corrected evidence-line list pinned by plan 34.5-54's test"
      pattern: "installing dxvk-macOS on"
    - from: "34.5-LIVE-GATE-RERUN-4.md item 1 clause (i)"
      to: "the in-page origin banner shipped by plan 34.5-52"
      via: "the retargeted U-34.5-05 transcription bar"
      pattern: "__gamelib_login_origin_banner__"
---

<objective>
Author `34.5-LIVE-GATE-RERUN-4.md` — the **fifth** blocking live-gate contract for Phase 34.5 — with
`verdict: null` and every RESULT slot empty, before any live work begins.

Purpose: four runs, four non-clean results. Two of the four contract-level defects this phase has
now paid for were authoring defects, not product defects. This contract is authored against the
project's own reference for exactly that failure class.

Output: the contract, its known-holes table, its out-of-scope table, and its Structural
Reachability Review.
</objective>

<standing_rules_this_plan_is_bound_by>
- **Decision D-E (project-standing):** the plan that authors a gate contract may never also run it.
  This plan authors; **plan 34.5-59 runs it.** This plan runs nothing live and fills no RESULT slot.
- **`34.5-LIVE-GATE.md`, `-RERUN.md`, `-RERUN-2.md` and `-RERUN-3.md` are NOT overwritten, NOT
  edited and NOT deleted.** Each is the completed record of its own run. RERUN-4 `supersedes:
  34.5-LIVE-GATE-RERUN-3.md` in frontmatter and in prose, and changes not one byte of it.
- **D-08 admits no partial pass.** Restate it verbatim from `34.5-CONTEXT.md`. Anything other than
  4 PASS / 0 FAIL / 0 BLOCKED / 0 NOT ATTEMPTED routes to gap cycle 8 inside Phase 34.5 — never a
  forced close, never an advisory note, never a deferred UAT entry, never a pre-authorised
  override. Phase 34.6's and Phase 35's dependency lines on Phase 34.5 stay untouched by this
  document.
- **No DevTools console steps, anywhere.** The Tauri DevTools console cannot be pasted into on this
  build — independently confirmed, a recurring friction, not a one-off. Any contract step that asks
  the operator to type into it is defective on arrival. Route every gesture through real UI
  controls (`34.5-CYCLE7-ROUTING.md` standing rule 5).
- **A mutating call's own report is never proof of its effect.** Every evidence clause must name at
  least one observable produced by a DIFFERENT component than the one under test.
</standing_rules_this_plan_is_bound_by>

<authoring_reference_note>
The brief that commissioned this plan describes
`.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` as carrying
**five** defect-class tests. **It currently carries SEVEN** — Tests 6 (pre-existing external-state
reachability) and 7 (UI-level reachability, distinct from backend-logic reachability) were added
after RERUN-4 of Phase 34.4.2 measured them. Author against all seven. The file's own coverage map
also states Tests 6 and 7 have caught nothing yet and that this is the first contract they apply
to — record that fact in the Review, so a clean result from them reads as "first application", not
as "validated".
</authoring_reference_note>

<context>
@.planning/PROJECT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md
@.claude/skills/spike-findings-gamelib/references/login-window-ux-macos.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the contract head — frontmatter, binding rules, capture standard, preconditions</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md</files>
  <read_first>
    - .planning/phases/.../34.5-LIVE-GATE-RERUN-3.md lines 1-480 (frontmatter shape, the D-08
      section, the evidence-handling rule T-34.5-G6-02 and its redaction-sweep regex, the binding
      evidence rule, the item-count/descope section, and all of Preconditions 1-12)
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md Section 3
      (the dual-sink evidence-capture standard — every bullet is mandatory, not guidance)
    - .planning/phases/.../34.5-52-SUMMARY.md, 34.5-53-SUMMARY.md, 34.5-54-SUMMARY.md,
      34.5-55-SUMMARY.md (what this cycle actually shipped, so "what changed since run 4" is
      measured, not assumed)
  </read_first>
  <action>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md`.

**Frontmatter:** `phase`, `plan: 56`, `executed_by: 34.5-59-PLAN.md`, `type: live-gate`,
`status: authored`, `blocking: true`, `created: 2026-08-13`, `cycle: 7`, **`verdict: null`**,
`items_total: 4`, `items_passed: null`, `items_failed: null`, `items_blocked: null`,
`items_not_attempted: null`, `supersedes: 34.5-LIVE-GATE-RERUN-3.md`, and a `notes:` block stating
in one line that every RESULT slot is empty at authoring time and is filled only by plan 34.5-59.

**Body sections, in this order:**

1. **Why this run exists.** Four runs, four FAILs, one line each naming the LAYER each failed at
   (run 1: sidecar spawn; run 2: capture-not-consumed; run 3: backend-succeeded-UI-rendered-nothing;
   run 4: a shipped feature invisible on the platform, plus an action never actually performed).
   Then a table of cycle 7's own fixes by plan number and what each CLAIMS to close — 34.5-52
   (in-page origin banner, `F-34.5-G6-16`), 34.5-53 (nile credential redaction, `F-34.5-G6-17` and
   `-20`), 34.5-54 (corrected DXVK evidence lines pinned by test, `F-34.5-G6-18`/`-19`), 34.5-55
   (SteamGridDB/winetricks honest decline, `U-34.5-25`). State plainly: **none of it is
   live-proven; that is what this contract exists to do.**
2. **D-08 — no partial pass.** Verbatim from `34.5-CONTEXT.md`, plus the note that D-08 was authored
   when the gate had five items including Epic and that D-CYCLE6-A's removal of Epic does not touch
   its no-partial-pass discipline.
3. **Evidence-handling rule.** Carry `T-34.5-G6-02` forward verbatim, INCLUDING the six value
   classes, the in-place-redaction instruction, the mandatory pre-commit sweep command with its
   4-plus-token-character rationale, and the published-SteamID64 exception from `deferred-items.md`
   item 23. **Extend the value class list to seven** by adding a PKCE `code_verifier` value, and
   extend the sweep regex with a `code_verifier=[A-Za-z0-9._+/=-]{4,}` alternative — plan 34.5-53
   established that this project logs that value class too. Re-validate the extended regex at
   authoring time against at least 4 safe specimens and 4 leak-shaped specimens, and record the
   8 results in the contract, so the regex is proven not to self-match its own documentation
   (the failure mode RERUN-3's own Part D found).
4. **The binding evidence rule.** A mutating call's own report is never proof of its effect; every
   clause names an observable from a different component. Carry RERUN-3's own two worked examples.
5. **Item count and the Epic descope (D-CYCLE6-A).** Four items, not five. Restate that descoping
   is not a pass and retires nothing, citing `deferred-items.md` item 24 by number.
6. **No-console rule.** Its own short section: the Tauri DevTools console is unusable on this build
   and no step in this contract may require it. State that run 4's item 3 struck a console step
   mid-run for exactly this reason and that the real UI route proved to be the BETTER evidence.
7. **Dual-sink evidence-capture standard.** Reproduce Section 3 of
   `live-gate-contract-authoring.md` IN FULL as binding instructions with concrete commands: one
   UTC-named session directory created once; `tee -a` into a single session-scoped `terminal.log`,
   never a bare `tee` and never `>`; the exact `=== GATE LAUNCH ${N} — <UTC> ===` delimiter text;
   per-launch `cp ~/Library/Logs/GameLib/gamelib.log <session-dir>/gamelib-launch-${N}.log` taken
   BEFORE the next launch renames it to `.old`, plus archiving any pre-existing `.old` on the first
   pass; a recorded launch ordinal per item; a closing `ls -la` plus `wc -l` inventory; and the
   exactly-ONE-app-instance assertion via `pgrep -f 'target/debug/gamelib-shell'` before each
   launch, with the PID recorded after the window appears and again at teardown, and an ABORT rule
   if the count is ever not 1.
   Also state the sink partition explicitly so no clause demands a line from a sink that cannot
   carry it: Rust `[shell]` lines reach the tee'd transcript only; sidecar `logInfo`/`logWarning`/
   `logDebug` reach `gamelib.log` only; sidecar stderr reaches the transcript prefixed
   `[sidecar:err]`; sidecar `console.*` reaches nothing.
8. **Preconditions**, numbered, ticked in order, carrying RERUN-3's own set forward and adding
   these cycle-7 ones:
   - Kill orphans first; `pgrep -f 'target/debug/gamelib-shell'` empty, re-checked.
   - `pnpm tauri:dev` (never `tauri dev` — that serves a stale static bundle).
   - The keyring arm precondition stays as RERUN-3 wrote it, with one addition: **plan 34.5-58's
     keyring session must be fully torn down before this gate's first launch** (its own
     `pgrep` check empty), and its outcome — whatever it is — cannot change this gate's required
     4/0/0/0.
   - A NEW precondition for item 4's genuine-OFF premise: see Task 2, which owns its exact wording;
     reference it here by number so it cannot be skipped.
   - A precondition asserting the four cycle-7 fix plans are present on the checked-out tree:
     `grep -c "fn login_origin_banner_script" src-tauri/src/main.rs` is 1,
     `grep -c "redactNileRegisterData" src/backend/storeManagers/nile/user.ts` is 2,
     the file `src/backend/tools/__tests__/dxvkEvidenceLines.test.ts` exists, and
     `grep -c "WINETRICKS_DECLINED_GUARD" src/frontend/components/UI/Winetricks/index.tsx` is >= 2.
     Run 4 could not distinguish "the fix is not on this build" from "the fix does not work"; this
     precondition makes that distinguishable before a single item is scored.
   - The measured static floor: `cargo test`, `cargo check`, `npx tsc --noEmit`, `npm run test:ci`
     and `preload-surface-gate.py`, each with its recorded count. State, next to it, that a green
     suite is necessary and never sufficient — this gate has beaten one four times running.
  </action>
  <verify>
    <automated>test -f .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md &amp;&amp; grep -c "verdict: null" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists with `verdict: null`, `items_total: 4`, and all four count fields `null`.
    - `grep -c "code_verifier" 34.5-LIVE-GATE-RERUN-4.md` is >= 2 (the seventh value class and the
      extended sweep regex).
    - The contract records 8 specimen results (4 safe, 4 leak-shaped) for the extended sweep regex,
      each with its expected and actual match state.
    - `grep -ci "devtools" 34.5-LIVE-GATE-RERUN-4.md` returns only occurrences inside the
      no-console rule section — verified by reading each hit; zero of them are instructions to use
      it.
    - `grep -c "tee -a" 34.5-LIVE-GATE-RERUN-4.md` is >= 1 and `grep -cE "tee [^-]" ` finds no bare
      `tee` capture instruction.
    - `git diff --stat` does NOT list `34.5-LIVE-GATE-RERUN-3.md`, `-RERUN-2.md`, `-RERUN.md` or
      `34.5-LIVE-GATE.md`.
    - `grep -c "pgrep -f 'target/debug/gamelib-shell'" 34.5-LIVE-GATE-RERUN-4.md` is >= 2.
  </acceptance_criteria>
  <done>The contract head is complete, binding, and its capture standard is stated as commands the runner can execute without interpretation.</done>
</task>

<task type="auto">
  <name>Task 2: Author the four items, the known-holes table, the out-of-scope table and the observations</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md</files>
  <read_first>
    - .planning/phases/.../34.5-LIVE-GATE-RERUN-3.md lines 480-1130 (the known-holes table, the
      known-holes resolution table, all four items with their clause lettering, and the
      cross-cutting observations)
    - .planning/phases/.../34.5-UNTESTED-ITEMS.md (the WHOLE current file — recount the OPEN rows
      yourself; do not trust any number written in this plan or in RERUN-3)
    - .planning/phases/.../34.5-54-SUMMARY.md (the corrected DXVK evidence facts and the settle-window
      requirement)
    - .planning/phases/.../34.5-52-SUMMARY.md (the banner element id and the update mechanism)
    - src/backend/tools/index.ts lines 200-530 (re-confirm every evidence line and its direction
      against source, at authoring time — this is the step whose absence produced F-34.5-G6-18)
  </read_first>
  <action>
**The four items.** Keep RERUN-3's subjects and lettering where they passed, so a reader can compare
runs. Changes required by cycle 7:

**Item 1 — GOG login from scratch, populated library.** Clauses (a)-(h) and (j) carry forward
unchanged (all PASSed). **Clause (i) is RE-TARGETED per D-CYCLE7-A** and must now read: the operator
transcribes the text of the **in-page origin banner** rendered inside the presented sheet — the
element with id `__gamelib_login_origin_banner__`, top-left corner — at two points in one flow, and
the two transcriptions differ, with `login.gog.com` then `embed.gog.com` as the known candidate
pair. State explicitly that the `NSWindow` title bar is NOT the subject any more and that a
log-observed `nav host=` change alone does not satisfy it. Add a companion clause (m): **the cancel
strip is simultaneously visible and still clickable while the banner is shown** — the banner must
never have cost the sheet its only exit. Add clause (n): the shell's own confirmation lines
`login origin banner injected for '<label>'` and `origin banner updated len=` appear in the tee'd
TRANSCRIPT (they are `eprintln!` from Rust, so the transcript is the correct sink — state the sink
next to the clause).

**Item 2 — Amazon login from scratch, `www.amazon.com` anchor.** Carry the PASSing clauses forward.
**Add clause (o), the credential-redaction check:** in the archived `gamelib-launch-${N}.log` for
the launch that covered this item, `grep -c "Got register data:"` returns 0 and
`grep -c "Got register data (redacted):"` returns at least 1; `grep -c "Register data is:"` returns
0 and `grep -c "Register data received (redacted):"` returns at least 1; and a grep for
`code_verifier` finds no line carrying a run of 4 or more token-alphabet characters after it.
State that a match here is a live credential leak and must be redacted in place before any commit,
never quoted. Keep `U-34.5-29` OUT of this item per D-CYCLE7-B — see the out-of-scope table below —
and state in the item's own text that library POPULATION is not a scored clause this run and why.

**Item 3 — shortcuts, both `exe` call sites.** Carry forward as it PASSed, driven through the real
"Add shortcut"/"Add to Steam" buttons under the game page's dots menu, never the console. Note that
`U-34.5-27` is out of scope this run per D-CYCLE7-B, and that the item must NOT be re-scoped to a
`.ico`-bearing target hunting for it.

**Item 4 — `runWineCommand` succeeds for at least one non-Steam runner.** This is the item cycle 7
must actually fix. Three changes:

  - **A genuine OFF→ON transition, proven from disk before the click (routing item 2).** Add an
    inline precondition, numbered in the Preconditions section and repeated here, requiring the
    executor to establish and RECORD a genuine OFF state before the operator opens the Wine tab:
    (i) read `~/Library/Application Support/gamelib/GamesConfig/<appName>.json` and record
    `autoInstallDxvk`'s value and the file's mtime; (ii) confirm the target prefix contains NO
    `current_<tool>` marker — on macOS the file is `current_dxvk-macOS` — and record the `ls`
    output; (iii) if either shows an already-ON state, the operator must first click the toggle OFF
    (which runs the RESTORE path and removes the marker), and the executor must then re-confirm the
    marker is gone from disk before the scored ON click. **This is a Test-6 (pre-existing external
    state) requirement: the premise "the switch is off" must be confirmed by a positive observable
    from disk, never by the switch's own on-screen appearance** — run 4 failed precisely here, on a
    week-old stale value.
    State the mechanism reason so the executor understands why the marker matters:
    `tools/index.ts:371-374` returns early with `<tool> already installed!` when the marker's
    version equals the global version, which SKIPS both the DLL copy and the `runWineCommand`
    registration loop. With the marker present, a genuine click produces no Wine command at all and
    the item is unverifiable again.
  - **The corrected evidence lines (routing item 3, as corrected by `F-34.5-G6-19`).** Replace
    RERUN-3's evidence list with:
      * Dispatch marker, install direction: `installing dxvk-macOS on...` —
        `src/backend/tools/index.ts:369`, `logInfo`, `LogPrefix.ToolInstaller`, sink
        `gamelib.log`. Proves every guard (toolkit, `-DXMT`, mac-tool-selection, valid-prefix) was
        passed and the install direction was entered.
      * Early-skip line that must NOT appear: `dxvk-macOS already installed!` —
        `tools/index.ts:372`. Its presence means the marker precondition above was not actually
        satisfied and the item is NOT ATTEMPTED, not FAIL.
      * **The line that actually retires research Pitfall 2:** at least one
        `Running Wine command: reg add HKEY_CURRENT_USER\Software\Wine\DllOverrides /v <dll> /d
        native,builtin /f` — `src/backend/launcher.ts:1520`, **`logDebug`**, sink `gamelib.log`,
        emitted by `runWineCommand` itself from the registration loops at `tools/index.ts:438` and
        `:459`. **State the correction explicitly in the contract's own text:** `F-34.5-G6-18`'s
        claim that the install direction "never calls `runWineCommand` at all" is wrong; it does,
        after the copy; and `launcher.ts:1581` was never the right citation either — the emitter is
        at `1520`. Cite `F-34.5-G6-19` and plan 34.5-54's `dxvkEvidenceLines.test.ts` as the pin.
      * Filesystem observable: `<winePrefix>/current_dxvk-macOS` EXISTS after the click and did not
        exist before it — a before/after pair, both recorded.
      * **A settle window is mandatory.** The registration loops are un-awaited
        `forEach(async ...)`, so `toggleDXVK` can resolve and the switch can flip ON before the
        `reg add` lines land. The contract must instruct: after the switch settles ON, wait at
        least 30 seconds, then archive `gamelib.log` and grep. Reading immediately is a known way
        to record a false absence.
      * **Distinguishing OFF-click noise from ON-click evidence.** If step (iii) above required an
        OFF click, that click emits its own `Running Wine command: wineboot -u` and
        `Running Wine command: reg delete ...` lines. Every item-4 grep must be scoped to lines
        AFTER the recorded timestamp of the ON click, and must match `reg add` /
        `native,builtin`, never `wineboot` and never `reg delete`. State both the timestamp scoping
        and the token discrimination as requirements, not as advice.
  - **Runner eligibility and what a PASS retires.** Keep RERUN-3's rule: a storefront-authenticated
    (`gog`) target retires BOTH the narrow literal reading of Pitfall 2 and the storefront-specific
    reading; a `sideload`-only target retires only the narrow literal reading. Add: a PASS is now
    genuinely capable of retiring Pitfall 2 at all only because of the `F-34.5-G6-19` correction —
    say so, so the causal link is on the record.
  - **UI-driven steps only**, traced to their components (`WineVersionSelector.tsx`, `AutoDXVK.tsx`,
    `SettingsButton.tsx`, `GamesSettings/index.tsx`), carried forward from RERUN-3's own verified
    step list, with the toggle-OFF-then-ON sequence inserted.

**Known holes (Ledger Rule 3).** Recount `34.5-UNTESTED-ITEMS.md` yourself at authoring time and
state the three integers explicitly — total rows, OPEN, RETIRED — and assert they reconcile. The
expected shape after plans 34.5-52 and 34.5-55 is 32 total, but **do not write 32 unless your own
recount produces it**; a transcribed number is a defect. Produce one table row per OPEN row, with
columns: Ledger ID (creating plan), Subject, Which gate item would retire it, Can THIS run retire
it. Carry RERUN-3's dispositions forward where unchanged, and add rows for `U-34.5-31` (the origin
banner's spoofability and live-unobserved state — retirable by item 1's clauses (i)/(m)/(n) taken
together) and `U-34.5-32` (the SteamGridDB/winetricks live decline — NOT COVERED BY THIS GATE, no
item drives those surfaces; owner: a future UAT session or Phase 34.6).

**Out of scope for this run (D-CYCLE7-B, plus the inherited table).** Reproduce
`34.5-CYCLE7-ROUTING.md`'s inherited out-of-scope table unchanged, and add two new rows with an
explicit `blocked_on:` field:
  - `U-34.5-29` (Amazon library population) — `blocked_on: operator-supplied test fixture` — an
    Amazon account owning at least one title. The account in use owns zero.
  - `U-34.5-27` (real GOG multi-image `.ico` decode) — `blocked_on: operator-supplied test fixture`
    — a GOG install directory containing a real `goggame-*.ico`. Item 3's own target ships a native
    `.icns` and has no `.ico` at all.
  Record the rationale D-CYCLE7-B gives, verbatim in substance: **a fifth FAIL caused by a missing
  fixture rather than a defect is worthless evidence.** State, in the same section and in the
  Arithmetic rule section, that neither row can affect the required 4/0/0/0, and that both rows stay
  OPEN — out of scope is not retirement.

**Cross-cutting observations** (observations, not items; no verdict impact):
  1. `U-34.5-22`, the GOG uninstall tile-flip without a restart (routing item 10). Run 4 recorded
     this NOT GATHERED because nothing required it. Make it **mandatory to ATTEMPT** this run: the
     runner must either gather it or record an explicit refusal naming its blocker. A silent
     omission is not an acceptable outcome for a third consecutive run. Its observable: a GOG
     uninstall completes, the `Finished uninstalling` line appears, and the Library tile stops
     presenting as installed with NO restart, all in the same session. A non-reproduction is
     recorded as a non-reproduction, never as a retirement.
  2. `U-34.5-32` incidental: if any SteamGridDB or Winetricks surface is opened during the run,
     record whether it declines visibly. Not a retirement attempt.
  3. `U-34.5-28` incidental, carried forward from RERUN-3 unchanged.
  </action>
  <verify>
    <automated>grep -c "installing dxvk-macOS on" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md</automated>
  </verify>
  <acceptance_criteria>
    - The contract contains all four items, each with an explicitly EMPTY `**Result:**` slot.
    - Item 1 clause (i) names `__gamelib_login_origin_banner__` and does NOT name the `NSWindow`
      title bar as its subject; clauses (m) and (n) exist.
    - Item 2 contains clause (o) with all four redaction greps stated as commands.
    - Item 4 contains the literals `tools/index.ts:369`, `launcher.ts:1520`, `logDebug`,
      `native,builtin`, `current_dxvk-macOS`, `F-34.5-G6-19`, and a stated settle window of at
      least 30 seconds.
    - `grep -c "launcher.ts:1581" 34.5-LIVE-GATE-RERUN-4.md` returns matches ONLY inside the
      correction prose that names it as the WRONG citation — read every hit and confirm none is a
      required-evidence citation.
    - The known-holes section states three integers (total / OPEN / RETIRED) and its table row count
      equals the stated OPEN count. The SUMMARY records the recount command and its raw output.
    - `grep -c "operator-supplied test fixture" 34.5-LIVE-GATE-RERUN-4.md` is >= 2, once each for
      `U-34.5-29` and `U-34.5-27`.
    - Both out-of-scope rows state that neither can affect the required 4/0/0/0 and that both rows
      stay OPEN.
    - Cross-cutting observation 1 is marked mandatory-to-attempt with an explicit-refusal rule.
    - `git diff --stat` still does NOT list any prior gate document.
  </acceptance_criteria>
  <done>All four items are scored against evidence current source actually emits, the DXVK item can finally produce its own proof, and every OPEN ledger row is either assigned to an item or named as uncovered.</done>
</task>

<task type="auto">
  <name>Task 3: Deliver the Structural Reachability Review (all seven tests) and annotate the ledger</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</files>
  <read_first>
    - .claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md (the WHOLE
      file — Section 2's seven tests, each worked example, the coverage map and its stated limits,
      and Section 4's two paid-for rules)
    - The draft contract produced by Tasks 1-2 (every item, sub-check, precondition, and the
      evidence-capture instruction itself)
    - .planning/phases/.../34.5-UNTESTED-ITEMS.md rows `U-34.5-27` and `U-34.5-29`
  </read_first>
  <action>
**(a) The Review, as a section of the contract itself**, headed
`## Structural Reachability Review (authoring-time, plan 34.5-56)`. It is a DELIVERABLE produced
before the contract's first live run, not a footnote. One table, one row per item / sub-check /
precondition / the evidence-capture instruction, with columns: Item-or-sub-check, Surface it names,
What it asks for, Verdict (REACHABLE / IMPOSSIBLE / CONDITIONAL), Evidence (a `file:line` or a
command). Then a short prose block per test recording what it found.

- **Test 1 — origin/scheme reachability.** Confirm no clause requires a URL a surface rejects
  before the code under test runs. `login_window_url_arg` (`src-tauri/src/main.rs:935`) rejects any
  non-`https` scheme before a window is built; confirm every login-path clause names a real `https`
  store URL.
- **Test 2 — concurrency reachability.** Confirm no clause requires two things driven at once
  through a UI whose modality forbids it. The presented sheet is application-modal to `main`; any
  clause asking for a second in-app action while the sheet is up is IMPOSSIBLE. Check item 1's new
  clauses (i)/(m)/(n) specifically: transcribing the banner and confirming the cancel strip is
  clickable are both observations of the sheet's own surface while it is up, which is reachable —
  but "clickable" must be confirmed WITHOUT clicking it mid-flow, since clicking cancels the login
  and would destroy item 1's own remaining evidence. Record how the clause resolves that.
- **Test 3 — log-line emitter reachability, WITH the sink clause.** For EVERY literal any clause
  requires, grep it in current source, record its `file:line`, AND record which sink its emitter
  writes to, then confirm the contract's mandated capture reads that sink. Rust `eprintln!`
  `[shell]` lines reach the tee'd transcript only; sidecar `logInfo`/`logWarning`/`logDebug` reach
  `gamelib.log` only; sidecar `console.*` reaches nothing. This is the test that F-34.4.2-14 exists
  for — a real emitter demanded through a sink that cannot carry it is IMPOSSIBLE just as surely as
  no emitter at all. Pay particular attention to item 1's new clause (n), which asks for `[shell]`
  lines (transcript), and item 4's evidence, which is all sidecar-side (`gamelib.log`).
- **Test 4 — absence-observability.** For every required ABSENCE, confirm the PRESENCE case would
  produce an observable signal, and record which KIND of absence each is (a falsifiable regression
  signal, or a confirmation of an existing structural guarantee). Apply it to: `dxvk-macOS already
  installed!` must not appear; `Got register data:` must not appear; `Register data is:` must not
  appear; zero `origin-mismatch` lines. For the two redaction absences, the PRESENCE case is
  directly observable because plan 34.5-53's RED proof demonstrated the un-redacted strings
  appearing — cite that as the mutation evidence.
- **Test 5 — requirement-interaction reachability. Its unit of review is the PAIR.** Record the
  reduction applied and the counts, so "found nothing" and "did not look" stay distinguishable:
  state M (state-mutating requirements) and N (evidence-bearing requirements), list both sets, and
  state that M×N pairs were considered. The hunting list is: anything that RESTARTS the app or a
  process; anything that TRUNCATES or OVERWRITES a file; anything that CLEARS state (a logout, a
  disconnect, a cookie wipe, a cache purge); anything that RESETS a store; anything that changes an
  environment variable a later item depends on; anything that consumes a one-shot resource.
  **At minimum, these pairs MUST be considered and their verdicts recorded, because each is a live
  candidate in this specific contract:**
    * {item 4's mandated toggle-OFF click} × {item 4's own `Running Wine command:` evidence} — the
      OFF click emits `wineboot -u` and `reg delete` Wine commands of its own. Resolution required:
      timestamp scoping plus `reg add`/`native,builtin` token discrimination. This pair is the
      reason both requirements exist in their current form; record it as FLAGGED-AND-RESOLVED, not
      as clear.
    * {item 4's toggle-OFF click} × {the `current_dxvk-macOS` before/after filesystem pair} — the
      OFF click is what REMOVES the marker, so the "did not exist before" half is produced by the
      other requirement rather than observed independently. Record how the contract keeps that
      honest.
    * {plan 34.5-58's keyring session} × {every item's evidence} — a separate app run. Resolution:
      it must be fully torn down before launch 1, asserted by `pgrep`.
    * {each item's own login} × {item 1's and item 2's "from scratch" premises} — a completed GOG
      login is state that could satisfy or destroy a later "logged out" premise.
    * {the per-launch `gamelib.log` archive} × {any mid-run relaunch} — `log_writer.ts:72-74`
      renames the live log to `.old` on the next launch's first write. Resolution: archive before
      the next launch, and archive any pre-existing `.old` on the first pass.
    * {the tee'd transcript instruction} × {every `[shell]`-line clause} — `tee -a`, never bare
      `tee`; this is F-34.4.2-11's own pair.
- **Test 6 — pre-existing external-state reachability. Its unit is the item's PREMISE.** Per item,
  record which external state its premise depends on and what preflight or in-run confirmation
  covers it. Name the concrete surfaces this project has: a live WKWebView cookie jar, a system
  Keychain entry, an already-running external process, an OS-level permission grant, and — new this
  cycle — **a persisted per-game config value and a Wine prefix marker file that both predate the
  session by weeks.** Apply the general rule: for every premise of the form "the user is logged out
  / the switch is off / no session exists," the contract must name a POSITIVE observable, never
  merely the absence of app-side data. Item 4's genuine-OFF precondition is this cycle's direct
  application; item 1's and item 2's "from scratch" premises need a rendered login form, not an
  empty store.
- **Test 7 — UI-level reachability, distinct from backend reachability. Its unit is the operator
  GESTURE SEQUENCE.** Trace each gesture to the component that renders the control it names, and
  record what the frontend does with that control in the state the item assumes — including whether
  navigation, conditional rendering or a disabled state removes it. Cover at minimum: the per-game
  gear/settings icon and the Wine tab (`SettingsButton.tsx`, `GamesSettings/index.tsx`); the
  Crossover/Wine Version dropdown (`WineVersionSelector.tsx`); the "Auto Install/Update DXVK on
  Prefix" toggle (`AutoDXVK.tsx`) — **including the fact that this toggle is HIDDEN for
  toolkit/CrossOver-DXMT versions and only appears once a real `wine`-type version is selected**,
  which makes the version repoint a genuine gesture PRECONDITION, not a nicety; the "Add
  shortcut"/"Add to Steam" entries under the game page's dots menu; the uninstall action for
  observation 1; and the banner/cancel-strip observation inside the sheet.

Close the Review with the reviewable clause the reference file names (T-34.4.2-42): the count of
structural impossibilities and unresolved requirement interactions actually encountered during the
run should be zero, and whatever it turns out to be is recorded per run. State explicitly that
Tests 6 and 7 have caught nothing on any prior contract and that this is their first application,
so a clean result from them is "first application", not "validated".

**(b) Ledger annotation (D-CYCLE7-B).** In `34.5-UNTESTED-ITEMS.md`, amend the Status cells of
`U-34.5-29` and `U-34.5-27` to add `blocked_on: operator-supplied test fixture` and name the exact
fixture each needs (an Amazon account owning at least one title; a GOG install directory containing
a real `goggame-*.ico`). Both rows stay **OPEN**. State in each cell that they are out of scope for
gate 5 by D-CYCLE7-B, with the recorded rationale — a fifth FAIL caused by a missing fixture rather
than a defect is worthless evidence — and that a future cycle picks them up the moment the fixture
exists. Do not retire, do not reword either row's own "Retired by" observation, and do not touch any
other row.
  </action>
  <verify>
    <automated>grep -c "Structural Reachability Review" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md</automated>
  </verify>
  <acceptance_criteria>
    - The Review section exists in the contract and contains one prose block per test, headed
      `Test 1` through `Test 7` — `grep -cE "^#+ *Test [1-7]"` returns `7`.
    - Every row of the Review table carries a Verdict of exactly REACHABLE, IMPOSSIBLE or
      CONDITIONAL, and an Evidence cell containing either a `file:line` or a runnable command.
    - Zero rows carry a Verdict of IMPOSSIBLE. If any do, the corresponding clause is rewritten or
      withdrawn with its reasoning recorded, and the Review is re-run — an IMPOSSIBLE row must never
      survive into a published contract.
    - Test 5's block states M, N and M×N as integers, lists both sets, and records a verdict for
      each of the six named mandatory pairs.
    - Test 6's block has one row per item naming that item's external-state dependency and its
      positive confirming observable.
    - Test 7's block names every component file it traced and states, for the DXVK toggle, that it
      is hidden until a `wine`-type version is selected.
    - The Review states that Tests 6 and 7 are being applied for the first time.
    - `grep -c "blocked_on: operator-supplied test fixture" .planning/phases/.../34.5-UNTESTED-ITEMS.md`
      returns `2`.
    - `U-34.5-29` and `U-34.5-27` are both still Status OPEN.
    - The ledger's total row count is unchanged by this task (this task retires nothing and opens
      nothing) — state the before and after counts in the SUMMARY.
  </acceptance_criteria>
  <done>The contract is reviewed against all seven defect-class tests before publication, with the pairing pass recorded as counts rather than as a claim, and D-CYCLE7-B is written into the ledger as a reasoned scope decision rather than an omission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract text -> operator session | A defective requirement burns a whole hardware run and produces unfalsifiable evidence |
| live session -> committed documents | Real credentials pass through the log files this contract instructs the runner to archive and quote |
| session directory (`/tmp/...`) -> the repository | Archived logs contain OAuth codes, cookies and account ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C7-18 | Information disclosure | quoted evidence in the filled contract | mitigate | Seven redaction value classes (six inherited plus `code_verifier`), in-place redaction rather than line omission, and a mandatory pre-commit sweep whose regex is re-validated at authoring time against 8 specimens. The published-SteamID64 exception is carried forward explicitly so it is not re-raised as a finding. |
| T-34.5-C7-19 | Information disclosure | the session directory's archived `gamelib.log` copies | mitigate | The session directory lives outside the repository under a UTC-named `/tmp` path and is never added to git. The contract must state that archived logs are evidence to grep, never files to commit, and that only redacted excerpts enter the document. |
| T-34.5-C7-20 | Repudiation | a contract clause whose evidence cannot exist | mitigate | The Structural Reachability Review, all seven tests, delivered before publication, with a zero-IMPOSSIBLE acceptance criterion and a recorded pairing pass. Two of this phase's four contract-level defects were exactly this class. |
| T-34.5-C7-21 | Tampering | prior gate records | mitigate | `supersedes:` rather than overwrite; `git diff --stat` asserted not to list any prior gate document, in both Task 1 and Task 2 acceptance criteria. This project has a measured instance of a review gate silently overwriting its own predecessor. |
| T-34.5-C7-22 | Elevation of privilege | the toggle-OFF step introduced for item 4 | accept | Reaching a genuine OFF state runs the RESTORE path, which executes real `reg delete` and `wineboot -u` Wine commands against the operator's own prefix. Accepted: it is the same prefix the item is about to modify anyway, it is a test prefix for a single game, and it is the only way to satisfy the genuine-transition requirement. Recorded, not silently performed. |
| T-34.5-C7-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package and edits no manifest. If a gate precondition ever requires one, the Package Legitimacy Gate applies and a blocking human checkpoint precedes the install. |
</threat_model>

<verification>
- `34.5-LIVE-GATE-RERUN-4.md` exists with `verdict: null` and four empty RESULT slots.
- No prior gate document appears in `git diff --stat`.
- The Review table has zero IMPOSSIBLE verdicts.
- The known-holes table's row count equals the recounted OPEN count, with the recount output in the
  SUMMARY.
- `blocked_on: operator-supplied test fixture` appears exactly twice in the ledger.
- No step in the contract requires the DevTools console.
</verification>

<success_criteria>
- A fifth contract exists that can, for the first time, produce falsifiable evidence for item 4.
- Item 1's clause (i) targets a feature that now exists.
- Item 2 can detect a credential leak rather than only observing one by luck.
- Ledger Rule 3 is satisfied against a recount, not a transcription.
- D-CYCLE7-B is a recorded, reasoned scope decision that cannot block 4/0/0/0 and retires nothing.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-56-SUMMARY.md` when done.
</output>
