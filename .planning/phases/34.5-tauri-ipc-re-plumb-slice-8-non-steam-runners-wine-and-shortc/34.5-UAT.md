---
status: partial
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
source: 34.5-01..42-SUMMARY.md (42 files), 34.5-PORTED-CHANNELS.md, 34.5-UNTESTED-ITEMS.md
started: 2026-08-03T00:19:00Z
updated: 2026-08-03T00:50:00Z
session_log: ~/Library/Logs/GameLib/gamelib.log (boot 00:19:10, dev-vault arm)
secret_store_arm: dev-vault
closes_phase: false
routes_to: /gsd-plan-phase 34.5 --gaps (gap cycle 6)
---

## Scope of this UAT — read before running it

Phase 34.5 has its own **blocking live gate** (`34.5-LIVE-GATE.md`,
`34.5-LIVE-GATE-RERUN.md`, `34.5-LIVE-GATE-RERUN-2.md`), run three times, verdict FAIL
(0 of 5 clean) every time. Per D-08 that gate — not this file — governs whether the phase
closes, and its findings route to gap cycle 6 (`/gsd-plan-phase 34.5 --gaps`), never to a
deferred UAT entry.

**This UAT therefore does NOT re-ask the 5 gate items.** Tests 18-22 carry their existing
gate verdicts and are recorded `blocked` (owned by the gate / gap cycle 6). Per the
verify-work workflow, blocked tests do not enter the Gaps section — they are not this
file's findings to diagnose.

**What this UAT does cover:** the ~30 of the phase's 38 ported channels that no gate item
has ever exercised live (`34.5-PORTED-CHANNELS.md` records them all at proof level `unit`).
Wine version management, the Wine/DXVK toggles, `callTool`, shortcut state and removal,
`processShortcut`, all three sign-out paths, both save-sync channels, the runtime channels,
and the honest-degradation behaviour of the 19 dropped/deferred channels.

**Test 4 is the highest-value item here.** Gate item 5 (`runWineCommand` for a non-Steam
runner — research Pitfall 2, ledger row `U-34.5-03`) has been NOT ATTEMPTED on all three
runs for one reason: no `wine`-type Wine version is downloaded on this machine, so both
configured non-Steam games sit at `wineVersion.type: "toolkit"`, which hides the DXVK
toggle and short-circuits before `runWineCommand`. Passing test 4 removes that blocker.

Two channels are deliberately not tested because they have no frontend caller at all and
are therefore not user-observable (`34.5-13` Sweep A, both pre-existing dormant conditions
inherited from Electron, not introduced by this phase): `getEpicGamesStatus` and
`getGOGLinuxInstallersLangs`. Both stay at unit proof.

## Current Test

[testing paused — 8 items outstanding]

Session ended by the developer ("wrap") after test 9, with enough findings to route. The 8
outstanding tests and what each needs before it can run:

| # | Test | Needs |
|---|------|-------|
| 5 | Remove a Wine version | Nothing — download a spare version and delete it. Do NOT delete `Wine-Crossover-latest`; tests 6/7 and gate item 5 need it. |
| 6 | DXVK / DXVK-NVAPI toggles | `getInstallInfo` fixed (details screen unreachable) |
| 7 | `callTool` — winecfg / Run EXE | `getInstallInfo` fixed |
| 8 | Shortcut state + Steam removal | A new target — Alan Wake was uninstalled and its Steam entry removed mid-session |
| 11 | Epic and Amazon sign-out | Being signed in to either |
| 12 | `egsSync` | An Epic login (gate item 1, blocked on the parked pre-auth 403) |
| 13 | Legendary save sync | An Epic login |
| 14 | GOG save sync panel | `getInstallInfo` fixed |
| 15 | Runtime download / installed check | Nothing — may be N/A on macOS |

Also carried, from test 2: `getCometVersion` is unverified (no log-panel surface). Settle it
with `await window.api.getCometVersion()` in devtools or by launching a GOG game.

## Tests

### 1. Cold Start Smoke Test
expected: |
  `ps aux | grep -E "gamelib-shell|sidecar.js"` shows nothing before you start (kill any
  orphans first — gate 3 found an orphaned pair contending for the same store files).
  Then: `npx electron-vite build` → `npm run build:sidecar` → `pnpm tauri:dev`.

  The app window opens, and `~/Library/Logs/GameLib/gamelib.log` grows with real sidecar
  application logic. In that log you should see the bootstrap asset-root lines with
  `source=GAMELIB_APP_ROOT`, all four runner binaries reported `exists=true`, and NO
  `[bootstrap] SIDECAR ASSET ROOT DEFECT` line and NO `spawn ./legendary|./gogdl|./nile
  ENOENT` errors. `ps aux` shows exactly one `gamelib-shell` and one `sidecar.js`.
result: pass

### 2. Runner version probes report real versions
expected: |
  Channels: `getLegendaryVersion`, `getGogdlVersion`, `getNileVersion`, `getCometVersion`.

  Wherever GameLib surfaces runner versions (Settings → Advanced, or the About/log panel),
  each of legendary, gogdl, nile and comet shows an actual version string — not "unknown",
  not blank, not an error. Equivalently from devtools:
  `await window.api.getLegendaryVersion()` etc. each resolve to a version string.

  This is the first live confirmation that the runner binaries are not just present on disk
  but actually executable under the sidecar.
result: pass
reported: "there are 3 in the log panel epic/legendary, gog, amazon/nile all have version numbers"
caveat: |
  PASS covers 3 of the 4 named channels — `getLegendaryVersion`, `getGogdlVersion`,
  `getNileVersion` are LIVE-PROVEN for the first time (all three runner binaries confirmed
  executable under the sidecar, not merely present on disk).

  `getCometVersion` is NOT covered by this observation and stays at unit proof. It has no
  log-panel surface: it is gated on `gameInfo.runner === 'gog'` at `launcher.ts:973` and is
  called at GOG launch time, not displayed. Settle it either with
  `await window.api.getCometVersion()` in devtools, or transitively by launching a GOG game.
  Do not read this row as covering it.

### 3. Wine Manager version list loads
expected: |
  Channels: `refreshWineVersionInfo`, `getAlternativeWine`, `wine.isValidVersion`.

  Settings → Wine Manager populates with downloadable Wine/Proton/Game-Porting-Toolkit
  versions. The list is non-empty and refreshing it re-fetches without an error toast.
  Note `refreshWineVersionInfo` was ported verbatim including its swallow-and-return-
  undefined catch — so an empty list here is a silent failure, not a "no versions
  available" state, and counts as an issue.
result: pass

### 4. Download a `wine`-type Wine version  ← unblocks gate item 5
expected: |
  Channel: `installWineVersion`.

  From Settings → Wine Manager, download a version whose type is **`wine`** (NOT a
  Game Porting Toolkit / `toolkit` entry — that is the distinction that has blocked gate
  item 5 on all three runs). A progress indicator advances, a completion notification
  fires, and afterwards `~/Library/Application Support/GameLib/tools/wine/` contains the
  downloaded version (it is currently empty — that emptiness is exactly what
  `34.5-LIVE-GATE-RERUN-2.md` precondition 11 re-verified at gate time).
result: pass
reported: "downloaded, progress bar completed, then said unziping, check the folder again."
evidence: |
  Verified on disk 2026-08-03 00:27:
  `~/Library/Application Support/GameLib/tools/wine/Wine-Crossover-latest` — 583 MB,
  unpacked bundle with `Contents/MacOS/wine` present. The directory was EMPTY at gate 3's
  precondition-11 check. `installWineVersion` is now live-proven end to end: fetch,
  progress, unzip, and a usable on-disk `wine`-type version.

  **Consequence for gate item 5 — stated precisely.** Ledger row `U-34.5-03` is NOT
  retired: per the ledger's own Rule 1 it retires ONLY on a gate item-5 PASS, never by
  inference. What HAS changed is its blocker. The explicit recorded refusal in
  `34.5-LIVE-GATE-RERUN-2.md` precondition 11 named exactly two conditions — "zero entries
  under `tools/wine/`" (now FALSE) and "both configured non-Steam games remain
  `wineVersion.type: toolkit`" (still TRUE). A future item-5 attempt additionally requires
  pointing a non-Steam game at this version; test 6 is where that happens.

### 5. Remove a Wine version
expected: |
  Channel: `removeWineVersion`.

  Removing a downloaded Wine version from the Wine Manager succeeds, the entry returns to
  its un-downloaded state in the list, and its directory disappears from `tools/wine/`.

  **RUN THIS AFTER TEST 7, not in numeric order.** Test 4 downloaded exactly one `wine`-type
  version (`Wine-Crossover-latest`) and tests 6 and 7 both need it configured on a game.
  Removing it now would re-block them and re-block gate item 5. Either download a second,
  spare version to delete instead, or come back to this once test 7 is done.
result: [pending]
deferred_until: after test 7

### 6. DXVK and DXVK-NVAPI toggles
expected: |
  Channels: `toggleDXVK`, `toggleDXVKNVAPI`, `toggleVKD3D`.

  With a non-Steam game configured to use the `wine`-type version from test 4, the DXVK
  toggle is now VISIBLE in that game's Wine settings (it is hidden under `toolkit`) and
  flipping it on installs DXVK and reports success. Same for DXVK-NVAPI.

  VKD3D is expected to be ABSENT on macOS — `tools/index.ts:228`'s platform guard excludes
  it unconditionally. Seeing no VKD3D toggle is correct behaviour, not a defect.

  If a download fails, expect a dialog box, not a silent no-op or a crash (that dialog path
  was verified safe under the sidecar by plan 34.5-09).
result: blocked
blocked_by: other
reported: "fail, alan wake was installed, cant goto details screen as not enabled, changed to wine, but unistall failed so could not test"
reason: |
  Recorded BLOCKED, not FAIL — `toggleDXVK`/`toggleDXVKNVAPI`/`toggleVKD3D` were never
  invoked, so nothing about this test's own subject was exercised or falsified. The blocker
  is F-34.5-G6-10 (`getInstallInfo` unported): the game details screen cannot be opened, so
  the Wine settings panel holding the DXVK toggle is unreachable. The developer then
  uninstalled the target game, removing it as a candidate.

  The wine-version switch itself DID succeed — `(00:31:52) 1207659037: Setting wineVersion
  to {... "name":"Wine Crossover - 23.7.1-1", "type":"wine" ...}`. So gate item 5's second
  named blocker condition ("both configured non-Steam games remain `wineVersion.type:
  toolkit`") is now false for this game as well as the first one.

  **Correction to the developer's own report, from the log:** the uninstall did NOT fail.
  `(00:33:24) Finished uninstalling`, `Alan Wake was successfully removed from Steam.`,
  `Applications shortcut removed`, `(00:33:29) refreshLibrary complete runner=gog
  managers=1`. The backend completed. What failed was the UI reflecting it — see gap
  UAT-34.5-02 below.
observed_incidentally: |
  **NON-STEAM WINE EXECUTION OBSERVED — the first time in this phase's history.** Driving
  the uninstall put GOG's Alan Wake (a NON-Steam runner) through `runWineCommand` under the
  sidecar three times, successfully, verbatim from `~/Library/Logs/GameLib/gamelib.log`:

  ```
  (00:32:52) [WARNING]: [Backend]: Required prefix files are missing, running `verifyWinePrefix` to create prefix
  (00:32:52) [INFO]:    [Backend]: Checking if wine version exists: Wine Crossover - 23.7.1-1
  (00:32:52) [DEBUG]:   [Backend]: Running Wine command: wineboot --init
  (00:33:13) [DEBUG]:   [Backend]: Running Wine command: cmd /c winepath -u /Users/graysonmitchell/GameLib/Alan Wake
  (00:33:17) [DEBUG]:   [Backend]: Running Wine command: /Users/graysonmitchell/GameLib/Alan Wake/unins000.exe /VERYSILENT /ProductId=1207659037 /galaxyclient /KEEPSAVES
  (00:33:24) [INFO]:    [Backend]: Finished uninstalling
  ```

  `wineboot --init` created a prefix (21s elapsed before the next command), `winepath`
  resolved, and a real Windows `.exe` ran to completion under Wine — for a GOG game, under
  the Tauri sidecar.

  **What this does and does not do, stated precisely.** Research Pitfall 2's standing claim
  — "no non-Steam runner has ever executed a Wine command under the sidecar" — is now
  CONTRADICTED BY DIRECT EVIDENCE. It is nonetheless NOT struck, and ledger row `U-34.5-03`
  is NOT retired: per the ledger's own Rule 1 a row retires only on the observation named in
  its own cell, which is a gate item-5 PASS, never by inference from a neighbouring
  observation. This is evidence for a future item-5 attempt, not a substitute for one.
  Record it in gap cycle 6's scoping; do not let it become an implicit pass.

### 7. `callTool` — winecfg and Run EXE
expected: |
  Channel: `callTool` (all four branches: `winecfg`, `runExe`, `winetricks`, GOG post-step).

  From a non-Steam game's Tools menu: "Wine Configuration" opens the real winecfg window,
  and "Run EXE" prompts for a file and runs it in that game's prefix. Winetricks, if
  reachable from the UI, also launches — note this one works today even though the three
  dedicated winetricks IPC channels are deferred to Phase 34.6.
result: [pending]

### 8. Shortcut state and Steam-shortcut removal
expected: |
  Channels: `shortcutsExists`, `isAddedToSteam`, `removeFromSteam`, `removeShortcut`.

  Gate 3 left a real `shortcuts.vdf` entry for GOG's *Alan Wake* (appName `1207659037`).
  **Quit Steam first** — it rewrites `shortcuts.vdf` from memory on exit and will clobber
  any write made while it is running.

  The game's submenu reflects the current shortcut state correctly, and "Remove from Steam"
  actually removes the entry from `shortcuts.vdf`. `removeShortcut` likewise removes a
  desktop shortcut without error.

  Known adjacent defect, already recorded — do NOT re-report it here: `addToSteam` returns
  `undefined` rather than a boolean (F-34.5-G6-08), so the *added* toggle never flips. This
  test is about the read and the REMOVE paths, which no gate item covers.
result: [pending]
partial_evidence: |
  NOT a pass — recorded so the evidence is not lost, and deliberately not upgraded.

  Test 6's uninstall exercised the shortcut-removal MACHINERY successfully under the
  sidecar, verbatim from `gamelib.log`:

  ```
  (00:33:24) [INFO]: [Shortcuts]: Remove Steam images for Alan Wake
  (00:33:24) [INFO]: [Shortcuts]: Alan Wake was successfully removed from Steam.
  (00:33:24) [INFO]: [Backend]:   Applications shortcut removed
  ```

  **Why this does not close the test.** The uninstall path calls `removeNonSteamGame` /
  `removeShortcuts` INTERNALLY. What the log proves is that those functions work under the
  sidecar — it does NOT prove the `removeFromSteam` and `removeShortcut` IPC CHANNELS
  dispatch correctly, because this flow never crossed them. Same distinction the phase's own
  proof-level vocabulary keeps between a function working and a channel being ported.
  Re-run this test through the UI's own "Remove from Steam" control to close it.

  Also note the target is gone: Alan Wake is uninstalled and its Steam entry removed, so
  this test needs a different game (or a re-add) when it is run.

### 9. `processShortcut` app hotkeys
expected: |
  Channel: `processShortcut`.

  The app-shell hotkeys work under Tauri. Four of the six cases are expected to be fully
  functional. Two are DECLARED DEGRADED and expected to do nothing visible:
  `ctrl+r` (reload) and `ctrl+shift+i` (devtools) reach logged no-ops in `electronStub` —
  an accepted residual (T-34.5-27), not a defect. Confirm the other four behave, and that
  the two degraded ones fail quietly rather than erroring.
result: pass
reported: "both those command no-op"
caveat: |
  PASS covers the DECLARED-DEGRADED pair only. `ctrl+r` and `ctrl+shift+i` no-op quietly —
  no error, no freeze, no window damage — which is exactly what accepted residual T-34.5-27
  predicts. The residual is confirmed behaving as declared rather than degrading into
  something worse.

  `processShortcut`'s **other four cases were NOT exercised** and stay at unit proof
  (`shortcutsFlows.test.ts`'s six-case switch coverage). Do not read this row as covering
  the channel as a whole — it confirms the two cases that were expected to do nothing, and
  says nothing about the four expected to do something.

### 10. GOG sign-out
expected: |
  Channel: `logoutGOG` (send-kind — structurally silent on failure, which is why it needs a
  human).

  You are currently signed in to GOG (gate 3 persisted 7 titles to `gog_library.json`).
  Signing out of GOG from Manage Accounts returns the account to a signed-out state, and
  `~/Library/Application Support/GameLib/gog_store/auth.json` is gone or emptied.

  Watch for this specifically: gate 2 reported being asked to approve Keychain TWICE during
  GOG sign-out (F-34.5-G6-06, never re-observed since). Note how many prompts you get.
result: pass
evidence: |
  Performed incidentally during test 16 and recorded from the log, not re-asked.

  ```
  (00:42:56) [INFO]: [Gog]: Logging user out
  ```

  followed by a complete from-scratch re-login (`status=captured` 00:43:19 →
  `Login Successful` 00:43:24). **That re-login is what makes this a PASS rather than an
  assumption**: `logoutGOG` is send-kind and structurally silent on failure, so its own
  success cannot be read from its own return. A subsequent login that had to go through the
  full OAuth capture and credential mint proves the credential really was cleared — a
  no-op logout would have left the session intact and short-circuited the re-login.
caveat: |
  The F-34.5-G6-06 sub-observation (two Keychain approval prompts on GOG sign-out) is NOT
  gathered and CANNOT be gathered from this session: it ran the `dev-vault` arm
  (`(00:19:10) [bootstrap] secret stores: dev-vault`), which issues no real `keyring_get` at
  all, so no Keychain prompt could fire regardless of whether the defect exists.
  F-34.5-G6-06 stays NOT RE-OBSERVED — same disposition it carried out of gate 3, and for
  the same reason. It needs a run with `GAMELIB_DEV_SECRET_VAULT` unset.

### 11. Epic and Amazon sign-out
expected: |
  Channels: `logoutLegendary`, `logoutAmazon`.

  Epic's sign-out was restructured by plan 34.5-06 so credential-side cleanup cannot be
  aborted by the sidecar's session stub throwing, and it is domain-scoped to the
  `epicgames.com` apex. Amazon's is an unmodified delegation to `NileUser.logout()`.

  Both return the account to a signed-out state with no error. (Amazon and GOG cookies
  persisting in the shared jar afterwards is an ACCEPTED inherited residual, T-34.5-37 —
  not a defect to report.)

  Expect this to be blocked if you are not signed in to either — say so and we move on.
result: [pending]

### 12. `egsSync` — Epic Games Store sync
expected: |
  Channel: `egsSync`.

  With Epic signed in, syncing with an installed Epic Games Store installation completes and
  reports its result. Expect this to be blocked — Epic login has never succeeded under Tauri
  (gate item 1, ledger rows U-34.5-06 / U-34.5-11).
result: [pending]

### 13. Legendary save sync
expected: |
  Channel: `syncSaves`.

  For an installed Epic game, cloud save sync runs and reports success without ever showing
  a prompt (the `-y` flag means no dialog is reachable on this path). Expect blocked on the
  same Epic login blocker as test 12.
result: [pending]

### 14. GOG save sync panel
expected: |
  Channel: `syncGOGSaves`, plus the genuinely-unported `getDefaultSavePath`.

  Open an installed GOG game's Sync Saves settings panel. **The default save location will
  NOT resolve** — this is a declared, known consequence (`34.5-PORTED-CHANNELS.md`
  correction 1, `deferred-items.md` item 4): `getDefaultSavePath` is the real caller of
  `getDefaultGogSavePaths` and is not one of this slice's 38 channels. It lands in Phase 34.6.

  What to check: that the failure is a visible, contained one (empty field, or an honest
  error) and does not crash the settings page or the app. If you set a location manually,
  `syncGOGSaves` itself should then work.
result: [pending]

### 15. Runtime download and installed check
expected: |
  Channels: `downloadRuntime`, `isRuntimeInstalled`.

  Wherever GameLib exposes runtime management, the installed-state check reports accurately
  and a download completes. On macOS these may be inapplicable (Linux-oriented runtimes) —
  if there is no reachable UI for them, mark this skipped rather than guessing.
result: [pending]

### 16. Dropped and deferred channels degrade honestly
expected: |
  19 channels are intentionally not ported: 3 DROPPED permanently (Zoom: `authZoom`,
  `getZoomUserInfo`, `logoutZoom`) and 16 DEFERRED to Phase 34.6 (8 EOS overlay, 5
  SteamGridDB artwork, 3 winetricks).

  They are supposed to reject with `UNPORTED_CHANNEL_MARKER` rather than get a new
  degradation path built for them. What matters for a human: when you touch these surfaces
  in the UI — Zoom sign-in in Manage Accounts, the EOS Overlay panel in an Epic game's
  settings, SteamGridDB artwork search in game details — you get a contained, legible
  failure, NOT a white screen, a hang, or a crash that takes the page with it.

  This is the honest-degradation check. Report anything that takes down more than itself.
result: issue
reported: "zoom isnt in manage accounts, eos epic overlay in settings says not installed and there is an install button." / "says eos is being installed, button text changes to \"cancel\". when click on cancel changes back. this seems ok for transition"
severity: major
evidence: |
  **Split result — the 3 DROPPED channels degrade honestly, the EOS DEFERRED cluster does
  not.**

  *Zoom (3 dropped channels) — HONEST.* Absent from Manage Accounts entirely. No surface
  means nothing to fail, which is the correct end state for D-02's permanent drop.

  *`[WebView]` deferral — HONEST, and the model for what the others should do.*
  `(00:43:39) [Frontend]: [WebView] in-app store/wiki browsing unavailable under Tauri
  (pathname=/store/gog) -- tracked as its own deferral (D-05)` — names itself, names its
  deferral, declines visibly.

  *EOS overlay (8 deferred channels) — NOT HONEST. This is the issue.* The panel reports
  "not installed" and offers an Install button; clicking it shows an installing state and
  swaps the button to "Cancel"; clicking Cancel reverts.

  **None of that reached the backend.** `grep -ic -E "eos|overlay"` across the entire
  session log returns **0**. Not one EOS line, and — decisively — not one
  `[GAMELIB_UNPORTED_CHANNEL]` rejection for any of the 8 EOS channels. The only unported
  channel that logged anything all session is `getInstallInfo` (16 times).

  The cancel path DID reach the backend, and errored:

  ```
  (00:45:18) [ERROR]: [Backend]: Aborting not possible. Could not find a matching abort
                                 controller for 98bc04bc842e4906993fd6d6644ffb8d
  ```

  It found no abort controller because no install was ever registered. (Strongly correlated
  with the EOS cancel by timing and by content — this is the only abort in the session and
  it follows the EOS interaction. Recorded as strongly-correlated, not as proven identical;
  a repeat with a clean log would settle it.)

  **Why this contradicts a standing phase claim.** `34.5-PORTED-CHANNELS.md` states: "The
  dropped and deferred channels required NO new code. The `UNPORTED_CHANNEL_MARKER`
  machinery (`src/common/types/sidecarTransport.ts`) already catches and declares them at
  boot — dropping or deferring means *let it reject and declare it honestly*, not *build a
  new degradation path*." Plan 34.5-13's SEAM Invariant B test asserts all 19 names are
  absent from both registries and therefore still reject.

  That test proves the REGISTRY state. It does not prove the channels are ever CALLED. For
  EOS the UI apparently never calls them — it renders an optimistic default and simulates
  progress locally. So the marker never fires, nothing is declared, and the user is invited
  to start an operation that cannot exist. **The Invariant B test and this observation are
  both true and do not contradict each other** — which is exactly why the test could not
  catch this.

  The developer's own read ("this seems ok for transition") is reasonable from the UI alone
  — and that is the defect: it is indistinguishable from a working install that happens to
  be cancellable.
caveat: |
  SteamGridDB artwork (the remaining 5 deferred channels) was NOT exercised — it is reached
  through the game details screen, which test 17's `getInstallInfo` defect blocks. Untested,
  not proven either way.

### 17. Game details page opens
expected: |
  Opening a game's details page renders — install size, install path, play buttons.

  Gate 3 found this blocked outright by `[GAMELIB_UNPORTED_CHANNEL] No handler registered
  for channel 'getInstallInfo'` (F-34.5-G6-10 / U-34.5-14) — a real channel that is unported
  AND missing from every bucket of `IPC-PORT-INVENTORY.md`, so the document that gates Phase
  35 cannot catch it.

  If the page still fails, note WHICH games it fails for (all runners? Steam too? installed
  vs not-installed?). The extent is explicitly UNKNOWN and must not be assumed to be one
  channel — scoping it is a Phase 35 precondition.
result: issue
reported: "cant goto details screen as not enabled"
severity: blocker
evidence: |
  Recorded from `gamelib.log` directly rather than by re-asking, because the log evidence is
  conclusive. F-34.5-G6-10 reproduced, with NEW scope data this session:

  | Title | Runner | Installed? | `getInstallInfo` rejections |
  |---|---|---|---|
  | Balrum | gog | no | 7 (00:31:14 ×5, 00:31:15, 00:31:17) |
  | Hero of the Kingdom: The Lost Tales 3 | gog | no | 9 (00:34:12 ×7, 00:34:13, 00:34:15) |
  | Alan Wake | gog | yes (at the time) | details screen unreachable per developer |

  So it is NOT target-specific and NOT installed-state-specific. Every attempt emits
  `[GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'getInstallInfo'` and the
  page does not render.

  **Extent still UNKNOWN and must not be narrowed to "one channel".** Only GOG titles were
  driven this session — Epic, Amazon, Steam and sideload details pages were not opened, and
  no audit of the preload surface has been done. That audit is `U-34.5-14`'s own retirement
  condition and a Phase 35 precondition.

  This defect is the blocker for tests 6, 7 and 14 of this UAT — the entire Wine/tools
  cluster is reached through the details screen.

### 18. GATE ITEM 1 — Epic login from scratch, populated library
expected: |
  A real Epic login completes from a signed-out state and produces a non-empty library.
result: blocked
blocked_by: other
reason: |
  Owned by the blocking live gate, not by this UAT. Third run (`34.5-LIVE-GATE-RERUN-2.md`,
  2026-08-02) recorded BLOCKED: 2 login windows driven, form rendered and accepted input,
  but the parked pre-authentication defect (HTTP 403 on `/id/api/email/exists`) reproduced —
  0 `status=captured`, 2 `status=timeout`. Ledger rows U-34.5-06 and U-34.5-11 stay OPEN and
  retire together or not at all. Routes to gap cycle 6.

### 19. GATE ITEM 2 — GOG login from scratch, populated library
expected: |
  A real GOG login completes from a signed-out state and the Library UI shows GOG games.
result: blocked
blocked_by: other
reason: |
  Owned by the blocking live gate. Third run recorded FAIL on a THIRD failure layer: the
  entire backend chain now works (capture → `gogdl auth` → runner-aware `refreshLibrary`
  dispatch → 7 titles persisted to `gog_library.json`) and F-34.5-G6-02 is CLOSED
  live-proven, but the Library UI never renders the persisted games (F-34.5-G6-12), and one
  `[refreshLibrary] runner=all origin=unknown` line still occurs (F-34.5-G6-14). Ledger rows
  U-34.5-07 and U-34.5-08 stay OPEN. Routes to gap cycle 6.
reproduced_by_this_uat: |
  This UAT session (2026-08-03, 00:42:56-00:46) independently reproduced item 2's failure
  and REFINED it. A full sign-out → from-scratch login cycle ran: `status=captured`
  (00:43:19), `Login Successful` (00:43:24), `phase=idle` (00:43:29), TWO
  `refreshLibrary complete runner=gog managers=1` completions (00:43:44, 00:43:54), 7 titles
  persisted. The UI showed: the login window vanishing, Manage Accounts frozen on "logging
  into gog", and a Library spinner that never resolves.

  Two clauses re-checked against this run:
  - **Clause (d) — still FAILING**, and now known to affect the Manage Accounts panel too,
    with a perpetual spinner rather than an empty list. See the gap entry "After a
    successful GOG login..." in the Gaps section.
  - **Clause (g) — still FAILING.** Exactly one `[refreshLibrary] runner=all origin=unknown`
    line occurred again (F-34.5-G6-14 reproduced). The other three origins were clean
    (`origin=mount`, `origin=game-status`, `origin=login-success`) and there were ZERO
    `Refreshing undefined Library` lines — so U-34.5-08's first half holds and its second
    half does not.

  Item 2 would still FAIL on this run. Nothing here permits striking it.

### 20. GATE ITEM 3 — Amazon login from scratch, populated library, `www.amazon.com` anchor
expected: |
  A real Amazon login completes from a signed-out state, produces a non-empty library, and
  the captured redirect's origin confirms the `www.amazon.com` host anchor.
result: blocked
blocked_by: other
reason: |
  Owned by the blocking live gate. Third run recorded NOT ATTEMPTED — the session ended
  after item 4's diagnosis before any Amazon login window opened. Research Assumption A1
  stays UNTESTED for that run (the narrow host-anchor mechanism was confirmed structurally
  by run 2, but the end-to-end claim is open). Ledger row U-34.5-04 (verification-code field
  legibility, F-34.5-G6-05) also stays ungathered. Routes to gap cycle 6.

### 21. GATE ITEM 4 — `addToSteam` and the macOS `.app` shortcut both launch GameLib
expected: |
  Both `exe` call sites (`nonesteamgame.ts:258` and `shortcuts.ts:227`) write a launch target
  that starts GameLib, and launching from Steam or the desktop starts the named GAME.
result: blocked
blocked_by: other
reason: |
  Owned by the blocking live gate. Third run was the first-ever real attempt and recorded
  FAIL, with three named root causes: `addToSteam` writes a byte-correct `Exe` but returns
  `undefined` not a boolean (F-34.5-G6-08); `addShortcut` is STRUCTURALLY DEAD for every
  macOS game because `electronStub.ts`'s `nativeImage` stub lacks `createFromBuffer`
  (F-34.5-G6-07); and running the VDF's own recorded invocation booted a second full GameLib
  instance instead of the game — `--no-gui` ignored, `gamelib://` deep link ignored
  (F-34.5-G6-09). Ledger rows U-34.5-02, U-34.5-12, U-34.5-13, U-34.5-15 all OPEN. Routes to
  gap cycle 6. Test 8 above covers the read/remove paths this item does not.

### 22. GATE ITEM 5 — `runWineCommand` succeeds for a non-Steam runner
expected: |
  A Wine command executes under the sidecar for at least one non-Steam game.
result: blocked
blocked_by: other
reason: |
  Owned by the blocking live gate. NOT ATTEMPTED on all three runs — the third as an
  explicit recorded refusal naming its blocker: zero `wine`-type versions under
  `tools/wine/`, both configured non-Steam games at `wineVersion.type: "toolkit"`, which
  hides the DXVK toggle and short-circuits before `runWineCommand` is reached. Research
  Pitfall 2 ("no non-Steam runner has ever executed a Wine command under the sidecar") stays
  STANDING. Ledger row U-34.5-03 OPEN. **Test 4 of this UAT removes the blocker** — passing
  it is what makes a future gate item 5 attempt possible.

## Summary

total: 22
passed: 6
issues: 2
pending: 8
skipped: 0
blocked: 6

gaps_recorded: 5

arithmetic: 6 + 2 + 8 + 0 + 6 = 22 = total
verdict: |
  PARTIAL — not complete, and explicitly not a phase-closing artifact. Phase 34.5's closure
  is governed by its blocking live gate (D-08, no partial-pass rule), which has FAILED three
  times. This UAT does not change that and was never scoped to.

  What it adds: 6 channel clusters live-proven for the first time, 5 new gaps for gap
  cycle 6, one standing research claim contradicted by direct evidence, and one
  phase-document claim falsified at the UI layer.

## Gaps

<!-- YAML for /gsd-plan-phase 34.5 --gaps consumption (gap cycle 6) -->

- truth: "Pressing Play on a GOG game launches it through its own runner"
  status: failed
  reason: |
    Observed twice in `gamelib.log`: `(00:32:32)` and `(00:33:42)`
    `[Steam]: SteamGame: launching appId 1207659037 via steam://rungameid/1207659037`.
    Alan Wake is a GOG title (`appName` 1207659037, `runner` gog), yet Play dispatched
    through the STEAM launcher using the raw GOG appName as a Steam `rungameid`. Wrong on
    two counts: wrong runner, and a non-Steam Steam shortcut is addressed by a computed
    64-bit shortcut id, never by the source store's appName — so even the Steam path could
    not have launched the right thing. The second occurrence is at 00:33:42, AFTER the
    uninstall completed at 00:33:24, so it also had no installed game to launch.
    Plausibly related to gate 3's F-34.5-G6-09 (launch routing) but distinct from it:
    G6-09 is the shell ignoring Electron-shaped args, this is runner mis-dispatch before
    any shell invocation.
  severity: major
  test: 6
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "After an uninstall completes, the Library UI shows the game as not installed"
  status: failed
  reason: |
    The backend uninstall fully succeeded — `(00:33:24) Finished uninstalling`,
    `Alan Wake was successfully removed from Steam.`, `Applications shortcut removed`,
    `(00:33:29) refreshLibrary complete runner=gog managers=1` — but the developer read the
    operation as having FAILED ("unistall failed so could not test") and pressed Play again
    at 00:33:42, which only makes sense if the tile still presented as installed.
    Same backend-succeeds/frontend-does-not-render family as gate 3's F-34.5-G6-12 (7 GOG
    titles persisted, zero rendered) — note `refreshLibrary` completed here too and the UI
    still did not reflect it. Third-layer frontend render defect, now observed on the
    uninstall path as well as the login path.
  severity: major
  test: 6
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "A game's details screen opens"
  status: failed
  reason: |
    `[GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'getInstallInfo'` fired
    12+ times across two different GOG titles this session — Balrum (00:31:14, x5, plus
    00:31:15 and 00:31:17) and Hero of the Kingdom: The Lost Tales 3 (00:34:12, x7, plus
    00:34:13 and 00:34:15). Developer, verbatim: "cant goto details screen as not enabled".
    This is gate 3's F-34.5-G6-10 / ledger row U-34.5-14 reproduced, and it is what BLOCKED
    test 6 of this UAT — the DXVK toggle lives behind the details screen.
    New scope data this session: it fires for MULTIPLE GOG titles, both installed (Alan
    Wake) and not-installed (Balrum, Hero of the Kingdom), so it is not target-specific.
    The extent across other runners and channels remains UNKNOWN — auditing the real
    preload surface against IPC-PORT-INVENTORY.md is a Phase 35 precondition (U-34.5-14).
  severity: blocker
  test: 17
  root_cause: "getInstallInfo is a real preload channel (src/common/types/ipc.ts:218, src/preload/api/helpers.ts:43) that is unported to the sidecar AND absent from every bucket of IPC-PORT-INVENTORY.md"
  artifacts: []
  missing: []
  debug_session: ""

- truth: "After a successful GOG login, the Manage Accounts panel updates and the Library renders the games"
  status: partially_fixed
  resolution: |
    **The Library half is FIXED and live-verified 2026-08-03** — debug session
    `.planning/debug/resolved/gog-login-ui-never-updates.md`, fix commit `eb117d9e4`.

    Root cause was NOT the teardown/unmount hypothesis this gap originally offered (that
    hypothesis is recorded below and was wrong). Phase 29 D-06's `STORE_CHANGED_CHANNEL`
    kept the renderer's synchronous store snapshot in sync with disk, but its only emit
    site covered writes the RENDERER initiated. Sidecar-initiated writes — every backend
    store manager — bypassed it, and `hydrated` was append-only, so a hydrated store's
    snapshot was frozen for the window's lifetime. Both sync read paths fail closed
    silently, which is why there was never an error.

    Live evidence: 3 `storeChanged` frames received, `loadGOGLibrary store=7`, games render
    with no restart and no Refresh press. **F-34.5-G6-12 / F-34.5-G6-03 CLOSED.**

    **The Manage Accounts half is NOT fixed** and remains open — see the separate gap below
    if raised, or the debug file's "Still open" section. It is a different defect.

    **Gate item 2 is NOT retired by this.** Only a gate RUN can do that, and clause (g)
    (`origin=unknown`, F-34.5-G6-14) is untouched. Ledger row U-34.5-07's two stated halves
    were both observed, but recording its retirement is a gate run's job, not this file's.
  original_status: failed
  reason: |
    User reported, verbatim: "no it does not, after logging into gog the window disappears
    and the manage accounts screen is NOT updated ... still said logging into gog. then when
    i goto gamelib the spinner keeps spinning and no games appear"

    Reproduces gate 3's F-34.5-G6-12 and REFINES it with two observations gate 3 did not
    record. Gate 3 logged only "the Library UI never renders the persisted games". This run
    adds:
      (a) the **Manage Accounts panel is ALSO stuck**, still showing "logging into gog"
          after the login has fully completed — so the stale-state failure is not confined
          to the Library route;
      (b) the Library shows a **perpetual spinner**, not an empty list. This is the
          diagnostically load-bearing detail: a spinner means the frontend still believes a
          refresh is IN FLIGHT. It is not "rendered zero games" — it is "never received the
          completion".

    Backend completion is not in doubt this session:
    ```
    (00:43:19) [oauthLoginCapture] runner=gog status=captured
    (00:43:24) [Gog]: Login Successful
    (00:43:29) [useTauriOAuthLogin] runner=gog phase=teardown inflight=true
    (00:43:29) [useTauriOAuthLogin] runner=gog phase=cancelled-midflight at=auth authStatus=done
    (00:43:29) [useTauriOAuthLogin] runner=gog phase=idle (login completed, library refresh triggered)
    (00:43:29) [refreshLibrary] runner=gog origin=login-success
    (00:43:44) [Gog]: refreshLibrary complete runner=gog managers=1   ← completion #1
    (00:43:54) [Gog]: refreshLibrary complete runner=gog managers=1   ← completion #2
    ```
    plus 7 titles in `store_cache/gog_library.json` (9867 bytes, rewritten 00:46).

    **Hypothesis for gap cycle 6 — NOT a diagnosis, offered because it unifies all three
    symptoms and is cheap to falsify.** `phase=teardown inflight=true` and
    `phase=cancelled-midflight at=auth authStatus=done` both fire at 00:43:29, immediately
    BEFORE `phase=idle` (this is F-34.5-G6-13, reproduced). U-34.5-09 retired on the claim
    that `useTauriOAuthLogin` now propagates THROUGH that teardown — and the log agrees, the
    hook does reach `idle`. But propagating inside the hook is not the same as the hook's
    CONSUMER still being mounted. If the teardown unmounts the component tree that rendered
    the login panel, then `phase=idle` and the refresh completion both land on a dead
    consumer, while the visible panel is a stale instance that never hears either. That
    single mechanism would explain the vanishing window, the frozen "logging into gog"
    panel, and the never-resolving Library spinner at once.

    Falsifiable cheaply: instrument whether the component consuming `useTauriOAuthLogin` is
    still mounted when `phase=idle` fires, and whether the Library's refresh-complete
    subscriber receives the two `refreshLibrary complete` events. Do NOT treat the
    hypothesis as the cause without that check — this project's own `f10-diagnosis-process-
    lesson` records what shipping a plausible-but-unverified correlation costs.
  severity: blocker
  test: 19
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "A deferred/unported channel declares itself unavailable rather than simulating success"
  status: failed
  reason: |
    The EOS overlay panel renders "not installed", offers Install, shows an installing state
    with a Cancel button, and reverts on cancel — while making ZERO backend calls. Zero
    `eos`/`overlay` lines and zero `[GAMELIB_UNPORTED_CHANNEL]` rejections for any of the 8
    deferred EOS channels across the whole session log. The cancel click then produced
    `(00:45:18) Aborting not possible. Could not find a matching abort controller for
    98bc04bc842e4906993fd6d6644ffb8d` — nothing to abort, because nothing started.
    Falsifies, at the UI layer, `34.5-PORTED-CHANNELS.md`'s claim that deferring means "let
    it reject and declare it honestly": nothing rejects and nothing is declared. Plan
    34.5-13's SEAM Invariant B test proves only that the 19 names are absent from the
    registries, not that the UI ever calls them — so it cannot catch this class of defect.
    Contrast the correct behaviour in the same session: `[WebView] in-app store/wiki
    browsing unavailable under Tauri ... tracked as its own deferral (D-05)`.
  severity: major
  test: 16
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

## Session notes (not gaps)

- **Session log PRESERVED** at `~/Library/Logs/GameLib/gamelib.log.34.5-uat-20260803`
  (28,569 bytes, `cp -p` then `cmp`-verified byte-identical, exit 0). Done per
  F-34.5-G6-15's lesson — gate 3 lost its live log to mid-run rotation and had to recover
  from `gamelib.log.old`. Every log citation in this document is quotable from that file.

- **Secret-store arm for this session: `dev-vault`**, confirmed at `(00:19:10) [bootstrap]
  secret stores: dev-vault` with the plaintext warning. Per `34.5-UNTESTED-ITEMS.md`'s own
  binding statement, this session can serve as evidence for NEITHER `U-34.5-01` (real
  Keychain path) NOR `U-34.5-10` (keyring read-timeout/approval race). Both stay OPEN and
  this run proves nothing about either. Recorded so it is never later misread.
- **Bootstrap health was clean** (test 1's evidence): `publicDir resolved=.../public
  exists=true`, all four runner binaries `exists=true` on `arm64`, `appRoot ...
  source=GAMELIB_APP_ROOT`. The G-1 fix holds.
- **Minor, unrelated to this phase — path-casing inconsistency.** `(00:31:37) Found Game
  Porting Toolkit Wine at /Users/graysonmitchell/Library/Application Support/gamelib/tools/
  ...` uses lowercase `gamelib`, while every other path this session uses `GameLib`. Works
  on a case-insensitive volume; latent on a case-sensitive one. Not this phase's code —
  noted only so it is not rediscovered as new.
- **Pre-existing third-party noise, not defects:** PCGamingWiki 403s and HLTB
  `Cannot read properties of null` for both GOG titles. External-service behaviour in
  `ExtraGameInfo`, unrelated to the 34.5 channel port.
