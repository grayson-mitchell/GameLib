---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 42
type: execute
wave: 5
depends_on: [34.5-41]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md
  - .planning/IPC-PORT-INVENTORY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
cycle: 5
requirements: [REQ-34.5-11, REQ-34.5-12, REQ-34.5-13]

must_haves:
  truths:
    - "Every document that tracks this gate agrees with the recorded verdict, and none of them softens it"
    - "No ledger row is retired without the exact observation its own row names; U-34.5-01 and U-34.5-10 remain OPEN"
    - "Phase 34.6's and Phase 35's dependency lines move ONLY on a recorded clean 5-of-5 (items_passed 5, items_failed 0, items_blocked 0, items_not_attempted 0), and stay untouched on any other arithmetic"
    - "The next command is named exactly once, in one place"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md"
      provides: "the filled Outcome section closing the cycle"
      contains: "## Outcome"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN-2.md verdict"
      to: ".planning/ROADMAP.md and .planning/STATE.md"
      via: "propagation without softening"
      pattern: "34.5-LIVE-GATE-RERUN-2"
---

<objective>
Propagate the third gate's recorded verdict into every document that tracks it, resolve the
untested-items ledger against what was actually observed, and close gap cycle 5 by naming the next
action exactly once.

**This plan is plan 34.5-31's Task 4, re-homed and extended.** Plan 34.5-31 is not edited, not
renumbered and not executed. Splitting propagation out of the gate-run plan keeps that plan's context
on the live session, where every blocking defect this phase has found was actually found.

A FAIL is recorded as a FAIL everywhere. A PASS is recorded only where the gate actually earned it.
Nothing here is softened, and nothing is retired by inference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md

**The completed `34.5-LIVE-GATE-RERUN-2.md` is the sole source of truth for this plan.** Where any
other document disagrees with it, the other document is wrong and gets corrected — never the reverse.

**`gsd-sdk` state writes are FORBIDDEN in this plan.** The standing finding is that they revert
`stopped_at`, mangle counters and splice status into historical blocks. `STATE.md` is hand-edited.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Propagate the verdict into every tracking document, without softening it</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` (the completed record — read it whole; every edit below derives from it)
    - `.planning/phases/34.5-.../34.5-PORTED-CHANNELS.md` (the 38-row ledger, its 10 gate-dependent `LIVE (item N)` proof cells, and its `## Accepted residuals` section)
    - `.planning/phases/34.5-.../deferred-items.md` (items 1-12 — do NOT renumber the existing entries)
    - `.planning/phases/34.5-.../ported-channels-gate.py` (the script that must still pass after the edit, plus its `--self-test`)
    - `.planning/IPC-PORT-INVENTORY.md` § "Phase 34.5" (the status paragraph, and the 38/3/16 arithmetic that must NOT change)
    - `.planning/ROADMAP.md` § Phase 34.5 (the status paragraph and the plan checklist, including the `[ ]` entries for 34.5-29/30/31)
  </read_first>
  <action>
    1. **`34.5-PORTED-CHANNELS.md`** — update all 10 gate-dependent `LIVE (item N)` proof cells to
       this run's result, so no cell still cites run 1 or run 2 as its CURRENT evidence. The two
       `LIVE (item 4)` rows are `addToSteam` and `addShortcut`; the `LIVE (item 5)` row is
       `runWineCommand`; the remainder belong to items 1-3. A cell whose item was recorded BLOCKED
       says BLOCKED with its cause — never PENDING, never FAIL. Update `## Accepted residuals` for
       Assumption A1, Assumption A2 (both call sites), research Pitfall 2 and `R-34.5-G1-PKG`.
       **`R-34.5-G1-PKG` stays OPEN regardless of this run's outcome**: it is the packaged asset
       root, this gate ran `pnpm tauri:dev`, a dev PASS never covers it, and its home is the
       packaging work, not Phase 34.6.
    2. **`deferred-items.md`** — append a NEW dated section recording the disposition of
       F-34.5-G6-01 through F-34.5-G6-06 against this run's evidence (closed, or still open with what
       was observed), plus every entry from the gate's "New findings (this run)" section, carried
       observation-only and undiagnosed. Do NOT renumber items 1-12; continue the numbering.
    3. **`.planning/IPC-PORT-INVENTORY.md`** — update the Phase 34.5 status paragraph to name all
       THREE gate runs and this one's verdict, pointing at `34.5-LIVE-GATE-RERUN-2.md` as the
       evidence. Do NOT touch channel membership or the 38/3/16 arithmetic — this run changes proof
       levels, not scope.
    4. **`.planning/ROADMAP.md`** § Phase 34.5 — add a gap-cycle-5 block listing plans 34.5-38
       through 34.5-42 marked `[x]`, with 34.5-41's entry annotated with the verdict. ANNOTATE (do
       not tick) the existing `[ ]` entries for 34.5-29 / 34.5-30 / 34.5-31 as HALTED and superseded,
       naming the superseding plan for each — a reader must be able to see the work was re-homed, not
       abandoned. Replace the Phase 34.5 status paragraph with this run's actual outcome and routing,
       and update the `**Plans:**` count by recounting from the phase directory rather than
       incrementing blindly.

       **Phase 34.6's and Phase 35's dependency lines are editable ONLY on a clean 5-of-5 PASS**,
       and "clean 5-of-5" is a reading of the gate's own frontmatter, not a judgement: `items_passed`
       is 5 AND `items_failed` is 0 AND `items_blocked` is 0 AND `items_not_attempted` is 0. Read
       those four counters and branch on them; never branch on item 1's entry disposition, which
       plan 34.5-40's opportunistic-attempt clause may have superseded. If and only if all four hold,
       update both dependency lines to record Phase 34.5 as satisfied, and quote the four counters in
       the SUMMARY as the evidence for doing so. On ANY other arithmetic — any FAIL, any BLOCKED, any
       NOT ATTEMPTED, or fewer than 5 PASS — leave both lines exactly as they are and prove it with
       `git diff`. This is the SAME four-counter test as Task 3 point 3, not a second formulation of
       it, and it is not a loosening of D-08: a partial or blocked result never unblocks a downstream
       phase.
  </action>
  <verify>
    <automated>python3 .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py &amp;&amp; python3 .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `ported-channels-gate.py` and its `--self-test` both exit 0 after the edit.
    - All 10 gate-dependent `LIVE (item N)` cells reflect THIS run's result; a grep for "RE-RUN (`34.5-LIVE-GATE-RERUN.md`" as a cell's current evidence finds only historical narration, never a current proof level.
    - `## Accepted residuals` carries entries for A1, A2 (both call sites), Pitfall 2 and `R-34.5-G1-PKG`, with `R-34.5-G1-PKG` recorded OPEN regardless of the verdict.
    - `deferred-items.md` records the disposition of all six F-34.5-G6 findings plus every new finding, appended under a new dated section, with items 1-12 unrenumbered.
    - `IPC-PORT-INVENTORY.md`'s Phase 34.5 paragraph names all three gate runs and this verdict; `git diff` shows no change to channel membership or the 38/3/16 arithmetic.
    - `ROADMAP.md` shows 34.5-38..42 `[x]`, 34.5-29/30/31 annotated-not-ticked with their superseding plan named, and a `**Plans:**` count recounted from the directory.
    - Phase 34.6's and Phase 35's dependency lines are keyed to the gate's four recorded counters: where `items_passed` is 5 and `items_failed`, `items_blocked` and `items_not_attempted` are each 0, `git diff .planning/ROADMAP.md` shows those lines updated to record Phase 34.5 as satisfied; on any other arithmetic the same `git diff` proves both lines untouched. The SUMMARY quotes the four counters it branched on.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Resolve the untested-items ledger against what was actually observed</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` (every row and all three Rules — Rule 1 governs this task entirely)
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` § "Known holes (ledger Rule 3)" as resolved by plan 34.5-41 Task 3, and its Item Result slots
    - `.planning/phases/34.5-.../34.5-CYCLE5-ROUTING.md` § "Ledger interaction" (the D-CYCLE5-B consequence)
    - `~/Library/Logs/GameLib/gamelib.log` — the file itself, for any row whose retirement turns on a log line
  </read_first>
  <action>
    Update `34.5-UNTESTED-ITEMS.md` row by row. **Rule 1 is absolute: a row is retired ONLY by the
    observation named in its own row, never by a passing test suite and never by inference from a
    neighbouring row.** For each row, either mark it RETIRED with the captured observation quoted
    inline and its date, or leave it OPEN and append one sentence naming what this run did and did
    not show for it.

    Bindings that are not negotiable in this task:
    - **U-34.5-01 stays OPEN** unless precondition 10 recorded a `keyring` arm AND all four of that
      row's own mechanical conditions held in the same run. Under D-CYCLE5-B's `dev-vault` arm, a
      line ending `dev-vault` proves NOTHING about the Keychain path — the row's own text says it is
      not weaker evidence, it is NO evidence.
    - **U-34.5-10 stays OPEN** for the same reason: a dev-vault run issues no real `keyring_get`, so
      plan 34.5-35's raised 45s/120s constants cannot be shown doing anything. Do not record them as
      live-proven.
    - **U-34.5-06 and U-34.5-11 retire together or not at all** — they share one live blocker. If
      the opportunistic Epic attempt reached `runner=legendary status=captured` followed by a
      `useTauriOAuthLogin` `phase=idle` line in the same session, retire both and quote the lines; if
      not, both stay OPEN.
    - **U-34.5-02 (item 4) retires only on a PASS covering BOTH call sites** (`nonesteamgame.ts:258`
      and `shortcuts.ts:227`), never one alone.
    - **U-34.5-03 (item 5) retires only on an item-5 PASS.** A NOT-ATTEMPTED or FAIL-by-blockage
      retires nothing, and research Pitfall 2's claim stays STANDING.
    - **U-34.5-07 / U-34.5-08 / U-34.5-09** retire only on their own full observations — the
      populated GOG library with `refreshLibrary complete runner=gog managers=1`, zero
      `Refreshing undefined Library` lines with every `origin=` non-`unknown` across a full GOG login,
      and multiple consecutive login attempts each reaching a `phase=` line with none silently
      returning to Manage Accounts.
    - **U-34.5-04 / U-34.5-05** retire only on the transcriptions items 3 and 1-3 required.

    **Rule 2 applies to this run's own new holes.** If the gate recorded a NOT ATTEMPTED item, a
    substituted item-5 target, a deviation at precondition 10, or any new finding that is
    live-unverified, ADD a row for it in THIS commit with its own "Retired by" observation. A finding
    that ends this cycle unverified and unrowed is exactly the failure mode this ledger exists to
    prevent.

    Finally, update the ledger's header line if the row count changed, and re-state at the top of the
    table which rows this cycle touched, so the next cycle can see the delta without diffing.
  </action>
  <verify>
    <automated>grep -c "^| U-34.5-" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md &amp;&amp; grep -q "U-34.5-01" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</automated>
  </verify>
  <acceptance_criteria>
    - Every existing row carries either RETIRED with its captured observation quoted and dated, or OPEN with one sentence on what this run showed and did not show.
    - U-34.5-01 and U-34.5-10 are OPEN, each citing the `dev-vault` arm as the reason, and neither of plan 34.5-35's constants is described anywhere as live-proven.
    - U-34.5-06 and U-34.5-11 carry the same status as each other, with the shared reason stated.
    - No row is retired citing a passing test suite, a neighbouring row, or a SUMMARY.
    - Any new hole this run created has its own row in the same commit, with a concrete "Retired by" observation.
    - The SUMMARY lists, by ID, exactly which rows were retired and which stayed open.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Close the cycle — STATE.md by hand, and the next action named exactly once</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-CYCLE5-ROUTING.md` (its `## Routing by verdict` table and the empty `## Outcome` section this task fills)
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` frontmatter (verdict, the four item counters, and `notes`)
    - `.planning/STATE.md` — frontmatter and the `## Current Position` block ONLY; do not read or touch historical blocks
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` § D-08 (the rule that decides whether the phase closes)
  </read_first>
  <action>
    1. **Fill `34.5-CYCLE5-ROUTING.md`'s `## Outcome` section.** Record the verdict and the four
       counters verbatim from the gate's frontmatter; name which of the routing table's four branches
       this run landed in — determined by those counters, never by item 1's entry disposition; and
       state in one sentence what happens next. Branch on the same four-counter test used in Task 1:
       - If `items_passed` is 5 AND `items_failed`, `items_blocked` and `items_not_attempted` are
         each 0, this is the routing table's 5-of-5 branch: record that D-08 is satisfied, that Phase
         34.5 closes on this result, and that there is no next gap cycle.
       - If items 2-5 all passed with item 1 BLOCKED, that scope is Epic-only and begins with
         unparking the pre-auth thread in `.planning/debug/epic-login-non-interactive.md` —
         including checking FIRST whether the always-on `epic_oauth_redirect_observer_script`
         `window.fetch` wrapper is implicated, per that file's own recorded NEW RISK.
       - If anything else, name the failing items.
    2. **Hand-edit `.planning/STATE.md`** — frontmatter and `## Current Position` only. Never use
       `gsd-sdk` state writes: the standing finding is that they revert `stopped_at`, mangle counters
       and splice status into historical blocks. Recount completed plans from the phase directory
       (count `34.5-*-SUMMARY.md` files) rather than incrementing blindly. Write `stopped_at` as a
       factual paragraph: the third gate ran, its verdict, which items passed/failed/blocked/not
       attempted, that plans 34.5-29/30/31 remain HALTED and superseded, and that U-34.5-01 and
       U-34.5-10 stayed open by construction. Verify with `git diff .planning/STATE.md` that ONLY the
       frontmatter and the `## Current Position` block changed.
    3. **Name the next action exactly once**, in `STATE.md`'s `## Current Position` and nowhere else:
       `/gsd-verify-work` on a clean 5-of-5 PASS — the same four-counter test as Task 1 and point 1
       above (`items_passed` 5, `items_failed` 0, `items_blocked` 0, `items_not_attempted` 0) — or
       `/gsd-plan-phase 34.5 --gaps` on anything less.
       Do not repeat it in ROADMAP.md, in the routing document, or in the SUMMARY's body — a next
       action named twice is a next action that drifts.
    4. **Final sweep before committing**: `grep -Ec "token=|code=|Cookie|userdata"` across every file
       this cycle touched under `.planning/`, confirming zero unredacted matches, and record the
       command and its output in the SUMMARY.
  </action>
  <verify>
    <automated>git diff --stat .planning/STATE.md &amp;&amp; grep -q "## Outcome" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md &amp;&amp; grep -Ec "gsd-verify-work|gsd-plan-phase 34.5 --gaps" .planning/STATE.md | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-CYCLE5-ROUTING.md`'s `## Outcome` section carries the verdict, all four counters, the branch this run landed in as determined by those counters (not by item 1's entry disposition), and what happens next in one sentence — naming phase closure under D-08 where the counters read 5 / 0 / 0 / 0, and the next cycle's scope in every other case.
    - `git diff .planning/STATE.md` shows exactly two changed regions — frontmatter and `## Current Position` — with no historical block touched.
    - The completed-plan count in `STATE.md` was recounted from the phase directory; the SUMMARY records the counting command and its output.
    - The next action appears exactly once across all files this plan touched, and the grep in the verify block confirms a count of 1 in `STATE.md`.
    - The credential sweep is recorded with its command and a zero (or fully-redacted) result.
    - The SUMMARY's first sentence states the verdict, unsoftened.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → downstream phase gating | Phases 34.6 and 35 unblock on this verdict |
| a live session's evidence → public-fork documents | Propagated excerpts may carry credential material |
| tooling → `STATE.md` | Automated state writes are known to corrupt this file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C5-18 | Elevation of privilege | a non-5/5 result unblocking Phase 34.6 or Phase 35 | mitigate | Task 1 makes those two dependency lines editable ONLY on a recorded clean 5-of-5 (`items_passed` 5 / `items_failed` 0 / `items_blocked` 0 / `items_not_attempted` 0), read from the gate's frontmatter rather than assumed from item 1's entry disposition, and pinned by a `git diff` acceptance criterion on both arms |
| T-34.5-C5-19 | Tampering | `gsd-sdk` state writes corrupting `STATE.md` | mitigate | Task 3 mandates hand-editing only, a recount from the phase directory rather than a blind increment, and a `git diff` proving only two regions changed |
| T-34.5-C5-20 | Repudiation | a ledger row retired by inference rather than by its own observation | mitigate | Task 2 requires the captured observation quoted inline per retired row, and pins U-34.5-01 / U-34.5-10 / U-34.5-06 / U-34.5-11 explicitly |
| T-34.5-C5-12 | Repudiation | a sub-clause confirmation read as retiring its parent claim | mitigate | Task 1 requires every STRUCK residual to name the ITEM that passed to earn it |
| T-34.5-G6-02 | Information disclosure | credential material propagated into public-fork documents | mitigate | Task 3's final sweep greps every touched `.planning/` file for `token=`, `code=`, `Cookie` and `userdata` and records the result |
| T-34.5-C5-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages and touches no source file |
</threat_model>

<verification>
- `ported-channels-gate.py` and `--self-test` exit 0.
- `git diff .planning/ROADMAP.md` proves Phase 34.6's and Phase 35's dependency lines untouched.
- `git diff .planning/STATE.md` shows exactly two changed regions.
- The next action is named exactly once.
- `git diff --name-only` matches zero `.ts`, `.tsx` or `.rs` paths, and zero changes to `34.5-LIVE-GATE.md` or `34.5-LIVE-GATE-RERUN.md`.
</verification>

<success_criteria>
Every document that tracks the gate agrees with the recorded verdict without softening it; the
untested-items ledger is resolved row by row against observations rather than inference, with
U-34.5-01 and U-34.5-10 still OPEN by construction; Phase 34.6's and Phase 35's dependency lines
moved if and only if the gate's four recorded counters read 5 / 0 / 0 / 0 and stayed untouched
otherwise; and the next action is named exactly once.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-42-SUMMARY.md` when done
</output>
