---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 57
subsystem: process-quality
tags: [code-review, security, credential-redaction, gate-ordering, static-verification]

# Dependency graph
requires:
  - phase: 34.5 gap cycle 7 (plans 34.5-52, -53, -54, -55)
    provides: "The four code diffs this plan reviews (login origin banner, nile credential
      redaction, DXVK evidence-line correction, SteamGridDB/winetricks honest decline)"
provides:
  - "34.5-CYCLE7-REVIEW.md -- nine-pass authoring-time defect review, halt_gate: true"
  - "F-34.5-G6-22 (blocker, NEW): PKCE code_verifier leaks unredacted via callRunner()'s
    command-line logging in launcher.ts, on every real NileUser.login() call"
  - "F-34.5-G6-23 (note): winetricksInstall send-kind exemption reasoning depends on an
    atomic three-channel port, currently true but not structurally enforced"
affects: [34.5-58, 34.5-59, 34.5-60, phase-34.6]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Review-before-sweep ordering (wave 3, not after wave 6) so findings have a ledger to land
      in before the gate consumes a stale picture -- the fix for a sibling phase's (34.9) three
      consecutive unledgered-review cycles"

key-files:
  created:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-REVIEW.md
  modified: []

key-decisions:
  - "The F-34.5-G6-22 fix is NOT applied in this plan, despite launcher.ts being a single file
    (which would technically satisfy the plan's own 'fixable within two files' allowance).
    Three constraints override that allowance here: Task 1's own <files> tag states read-only
    review, Task 2's own acceptance criteria requires git diff --stat to list exactly one file,
    and this execution session's own hard staging guard restricts commits to files this plan's
    frontmatter declares. Recorded transparently rather than silently exercising the allowance."
  - "halt_gate: true. Plan 34.5-59 (the fifth blocking live gate) must NOT launch until
    F-34.5-G6-22 is fixed in its own plan and 34.5-CYCLE7-REVIEW.md is amended to
    halt_gate: false with the fix's commit hash recorded."

requirements-completed: [REQ-34.5-13]

# Metrics
duration: ~100min
completed: 2026-08-14
---

# Phase 34.5 Plan 57: Cycle 7 Code Review Summary

**Reviewed cycle 7's four diffs (plans 34.5-52/-53/-54/-55) against this project's nine measured
authoring-time defect classes; all nine passes PASS on the cycle's own diff, but an independent
trace of the credential-logging path beyond the two call sites plan 53 named turned up a real,
currently-live PKCE `code_verifier` leak (`F-34.5-G6-22`) in a file this cycle never touched --
`halt_gate: true`, plan 34.5-59 must not run until it closes.**

## Performance

- **Duration:** ~100 min
- **Completed:** 2026-08-14
- **Tasks:** 2/2 completed
- **Files modified:** 1 (created)

## Accomplishments

- Ran all nine required review passes against the union of plans 52/53/54/55's diffs (18 files,
  1945 insertions / 85 deletions): vacuous-assertion sweep, all twelve required RED captures
  individually verified PRESENT, both temporary-edit restoration proofs independently re-run from
  HEAD, production-call-shape verification for plans 53 and 55, grep-gate self-reference check on
  `dxvkEvidenceLines.test.ts`, localisation/`hardcodedStringGate` run, scope-discipline check on
  plans 29/30/31, the full additive/reversible invariant (`tsc`, `cargo check`/`test`,
  `npm run test:ci`, both Python IPC gates, `electronUntouched`/`sidecarRejectionGuard`), and the
  phase's standing seven-class redaction sweep over the cycle's own commits (proven non-vacuous
  against two known-bad specimens first).
- All nine passes PASS on the cycle's own diff as written.
- **Independent finding beyond the plan's own checklist:** traced `NileUser.login()`'s
  `runRunnerCommand` call (the same function plan 53 redacted the two direct logger calls
  around) into the shared `callRunner()` in `src/backend/launcher.ts`, and found a third,
  pre-existing, unconditional logging call site (`logInfo([prefix, safeCommand], ...)`,
  `launcher.ts:1704`) that writes the full command-line array -- including the raw PKCE
  `code_verifier` -- to `gamelib.log` on every real login attempt. The existing redaction helper
  (`getRunnerCallWithoutCredentials`, `launcher.ts:1877-1926`) only strips `--code`/`--token`
  array members, never `--code-verifier`. `nileCredentialRedaction.test.ts`'s Group A (plan 53)
  cannot catch this because it mocks `runRunnerCommand` entirely, so the real `callRunner()`
  code path never executes under that test.
- Recorded as `F-34.5-G6-22` (blocker) in `34.5-CYCLE7-REVIEW.md`, with `halt_gate: true`.
- Recorded a second, non-blocking finding `F-34.5-G6-23` (note): the `winetricksInstall`
  send-kind exemption in `Winetricks/index.tsx` is correct only because all three winetricks
  channels are currently expected to port atomically in Phase 34.6 (per
  `IPC-PORT-INVENTORY.md`'s "Slice 9" grouping) -- not structurally enforced, flagged for that
  phase's own live verification, not fixed here.

## Task Commits

Each task was committed atomically:

1. **Task 1+2: Review cycle 7's diffs and write `34.5-CYCLE7-REVIEW.md` with severities and halt decision** - `79df3de77` (docs)

_Tasks 1 and 2 landed in a single commit: Task 1 (review, no file output of its own beyond the
Task 2 artifact) and Task 2 (writing the review file) share the same file scope
(`34.5-CYCLE7-REVIEW.md` is the only file this plan's frontmatter declares), so there was no
intermediate artifact to commit separately between them -- matching this cycle's own precedent
(plan 52's Task 3 doubling as its ledger-update commit)._

## Files Created/Modified

- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-REVIEW.md` -
  New file: nine-pass review, findings table (`F-34.5-G6-22` blocker, `F-34.5-G6-23` note),
  explicit halt decision, `halt_gate: true`.

## Nine Passes (verdicts)

| Pass | Subject | Verdict |
|------|---------|---------|
| 1 | Vacuous assertions | PASS -- none found, all negative assertions collect an explicit string first |
| 2 | Twelve required RED captures | PASS -- 12/12 PRESENT, individually verified |
| 3 | Restoration proofs (plans 54, 55) | PASS -- `tools/index.ts` byte-identical to pre-cycle, no stray injection markers anywhere in `src/` |
| 4 | Production call shape (plans 53, 55) | PASS -- both drive real class methods / read real component files |
| 5 | Grep-gate hygiene (`dxvkEvidenceLines.test.ts`) | PASS -- reads `tools/index.ts`/`launcher.ts`, never itself |
| 6 | Localisation | PASS -- no `pnpm i18n` ran, `hardcodedStringGate.test.ts` green, six new keys use `t(key, englishDefault)` |
| 7 | Scope discipline (34.5-29/30/31) | PASS -- byte-unchanged, unreferenced |
| 8 | Additive/reversible invariant | PASS -- `tsc` clean, `cargo check`/`test` clean (147/0/1), `npm run test:ci` 4832/1/0 (249 suites), both Python gates exit 0 |
| 9 | Credential hygiene over the cycle's own diff | PASS on the diff itself, but see `F-34.5-G6-22` below -- Pass 9 is scoped to files this cycle touched, and the leak lives in a file it did not |

## Decisions Made

- Did not fix `F-34.5-G6-22` inline despite `launcher.ts` technically being a single file (the
  plan's own halt-decision text allows an in-plan fix "if fixable within two files"). Overridden
  by Task 1's own read-only-review file tag, Task 2's own single-file `git diff --stat`
  acceptance criterion, and this session's hard staging guard. Recorded as a routing candidate: a
  new plan must land before wave 5 (34.5-59), most naturally inserted between 34.5-57 and
  34.5-58, or folded into 34.5-58 if that plan's author judges the fix trivial enough.
- `F-34.5-G6-23` recorded as a note, not opened as a new `34.5-UNTESTED-ITEMS.md` row -- the
  channels it concerns are already tracked there via `U-34.5-25`/`U-34.5-32` (both opened by
  plan 34.5-55).

## Deviations from Plan

None against this plan's own tasks -- both tasks executed as written, all nine passes performed,
the review file's acceptance criteria (`halt_gate:` present, `34.5-REVIEW.md` absent, exactly one
file in this plan's own diff, unique finding ids continuing from `F-34.5-G6-22`) all verified
directly.

**Not a deviation, but the point of this plan's own existence:** this review's independent trace
beyond the plan's own checklist (Pass 9's cycle-diff sweep alone would not have found it) is
exactly the kind of finding the review-before-sweep ordering exists to catch while it can still
be ledgered.

## Issues Encountered

None beyond the finding itself, which is the substantive output of this plan.

## HONESTY NOTE (binding per this plan's critical_context)

Every pass in `34.5-CYCLE7-REVIEW.md` is static: `tsc`, `cargo check`/`cargo test`,
`npm run test:ci`, two Python source-text gates, and direct source reads. Nothing here claims
live behaviour. `F-34.5-G6-22` was found by static trace (reading `launcher.ts`'s
`getRunnerCallWithoutCredentials` and `callRunner` directly), not by a live gate run -- it is
reported with the same confidence level as the source-text evidence it is: a structural leak
proven by reading the code path a mocked test cannot reach, not a live-observed log line. A live
confirmation (or a live gate run against the fix, once applied) is still owed.

## Known Stubs

None. This plan produced no application code.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: information-disclosure | `src/backend/launcher.ts:1697-1704`, `:1877-1926` | `F-34.5-G6-22` -- PKCE `code_verifier` written unredacted to `gamelib.log` on every real `NileUser.login()` call, via a pre-existing call site outside this cycle's own diff and outside its own threat register (`T-34.5-C7-06..09`). This is new security-relevant surface this review found, not one the plan's own `<threat_model>` anticipated -- flagged per the plan's threat-surface-scan instruction. |

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **`halt_gate: true` in `34.5-CYCLE7-REVIEW.md`. Plan 34.5-59 (the fifth blocking live gate)
  must NOT launch until `F-34.5-G6-22` is fixed by a new plan (inserted before wave 5, or folded
  into 34.5-58's scope) and `34.5-CYCLE7-REVIEW.md`'s frontmatter is amended to
  `halt_gate: false` with the fix's commit hash recorded.**
- `F-34.5-G6-23` is a standing note for whichever plan in Phase 34.6 ports the winetricks
  cluster -- verify `winetricksInstall` itself live, not merely the two list probes.
- Next free finding id after this plan: `F-34.5-G6-24`.

## Self-Check: PASSED

- FOUND: `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-REVIEW.md`
- FOUND: commit `79df3de77`

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-08-14*
