---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 08
type: execute
wave: 2
depends_on: ["34.5-01", "34.5-04"]
files_modified:
  - src/backend/sidecar/shortcutsFlowRegistration.ts
  - src/backend/sidecar/__tests__/shortcutsFlows.test.ts
  - src/backend/sidecar/electronStub.ts
  - src/backend/sidecar/__tests__/lifecycleStub.test.ts
autonomous: true
requirements: [REQ-34.5-05]

must_haves:
  truths:
    - "The desktop-shortcut half of the cluster is ported: `addShortcut`, `removeShortcut`, `shortcutsExists`, plus `processShortcut`. THREE of these four are send-kind — the highest send ratio of any cluster in this slice."
    - "D-09 precondition honoured: this plan runs after REQ-34.5-01, because `shortcutFiles()` calls `getPath('desktop')` on the linux and win32 branches and would throw the moment these channels were registered without it."
    - "Every send body carries the fire-and-forget guard — a send channel's rejection reaches NO caller (no reject, no timeout, no renderer log line), so the listener must guard its own promise. The Electron originals are unguarded async bodies because Electron's own `ipcMain.on` catches for them; `electronStub`'s recorder does not."
    - "PLAN-TIME FINDING, verified against source: `processShortcut` is an app-shell HOTKEY channel (`main.ts:1465`, ctrl+r/q/k/j/l/shift+i), not a game-shortcut channel. Its `ctrl+r` and `ctrl+shift+i` cases call `mainWindow?.reload()` and `mainWindow?.webContents?.openDevTools()`, and `electronStub`'s `fakeWindow`/`fakeWebContents` implement NEITHER — porting it verbatim throws a TypeError on those two hotkeys."
    - "The fix follows the Phase 34.1 Plan 04 precedent that added `fakeWindow.hide` for exactly this class of bug: add logged no-ops rather than letting a missing method throw, and DECLARE the two degraded hotkeys honestly rather than pretending they work."
    - "Test containment is load-bearing here: this is the first slice whose code path writes files OUTSIDE the app-support dir by design. Every shortcut-writing assertion targets a redirected `desktop`/`home`, never the developer's real one."
  artifacts:
    - path: "src/backend/sidecar/shortcutsFlowRegistration.ts"
      provides: "addShortcut, removeShortcut, shortcutsExists, processShortcut"
      contains: "ipcMain.on('addShortcut'"
    - path: "src/backend/sidecar/electronStub.ts"
      provides: "fakeWindow.reload and fakeWebContents.openDevTools logged no-ops"
      contains: "openDevTools"
    - path: "src/backend/sidecar/__tests__/shortcutsFlows.test.ts"
      provides: "Bidirectional registration-kind cross-check with three send channels"
  key_links:
    - from: "src/backend/sidecar/shortcutsFlowRegistration.ts"
      to: "src/backend/shortcuts/shortcuts/shortcuts.ts (shortcutFiles)"
      via: "curated named import, never shortcuts/ipc_handler.ts"
      pattern: "shortcutFiles"
    - from: "processShortcut listener"
      to: "electronStub fakeWindow.reload / fakeWebContents.openDevTools"
      via: "getMainWindow() resolving the fake window"
      pattern: "openDevTools"
---

<objective>
Port the desktop-shortcut channels and the hotkey channel, add the two `electronStub` window methods
they need, and guard all three send bodies.

Purpose: this is the send-heaviest cluster in the slice, and it is the first one whose code path
writes files outside the app-support directory by design. Both facts have already produced real
defects in this project — a silently-failing send channel (G-30-01) and a test that clobbered a real
store.

Output: four channels, two logged no-ops replacing two TypeErrors, and a test suite that cannot
touch the developer's real Desktop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-RESEARCH.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-VALIDATION.md

<interfaces>
Electron registration ground truth (verified by grep):
  src/backend/shortcuts/ipc_handler.ts:14  addListener('addShortcut',    async (event, appName, runner, fromMenu) => { getGame(...).addShortcuts(fromMenu); notify(...) })   SEND
  src/backend/shortcuts/ipc_handler.ts:33  addHandler ('shortcutsExists',      (event, appName, runner) => { const {title} = getGame(...).getGameInfo(); const [d,m] = shortcutFiles(title); return existsSync(d ?? '') || existsSync(m ?? '') })
  src/backend/shortcuts/ipc_handler.ts:41  addListener('removeShortcut', async (event, appName, runner) => { getGame(...).removeShortcuts(); notify(...) })                   SEND
  src/backend/main.ts:1465                 addListener('processShortcut', async (e, combination: string) => { switch … })                                                      SEND

Curated import targets (verified export sites):
  src/backend/shortcuts/shortcuts/shortcuts.ts:280 export block → removeShortcuts, addShortcuts, shortcutFiles
  src/backend/utils.ts → getGame, handleExit
  src/backend/dialog/dialog.ts → notify
  src/backend/ipc.ts → sendFrontendMessage  (already the sidecar's push relay)
  src/backend/main_window.ts:8 → getMainWindow

processShortcut's six cases (main.ts:1465-1492) and what each needs under the sidecar:
  ctrl+r          mainWindow?.reload()                       → fakeWindow has NO reload      (TypeError today)
  ctrl+q          handleExit()                               → already sidecar-reachable (appShellFlowRegistration's `quit`)
  ctrl+k          sendFrontendMessage('openScreen','/settings/general')  → push relay, works
  ctrl+j          sendFrontendMessage('openScreen','/download-manager')  → works
  ctrl+l          sendFrontendMessage('openScreen','/library')           → works
  ctrl+shift+i    mainWindow?.webContents?.openDevTools()    → fakeWebContents has NO openDevTools (TypeError today)

The Phase 34.1 Plan 04 precedent (src/backend/sidecar/electronStub.ts, the `fakeWindow` literal):
  `hide: (): void => {}` was added with a comment explaining that `handleExit()` called
  `mainWindow?.hide()` unconditionally and `getMainWindow()` resolves this TRUTHY fake, so the
  missing method threw and prevented `app.exit()` from ever being reached.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two missing fakeWindow methods to electronStub</name>
  <files>src/backend/sidecar/electronStub.ts, src/backend/sidecar/__tests__/lifecycleStub.test.ts</files>
  <read_first>
    - src/backend/sidecar/electronStub.ts (the `fakeWebContents` and `fakeWindow` literals and the `BrowserWindow.getAllWindows()` export — read the Phase 34.1 Plan 04 comment on `hide` verbatim; it is the template for this task's comments)
    - src/backend/main_window.ts (getMainWindow — confirm it resolves the truthy fake rather than `undefined` under the sidecar)
    - src/backend/main.ts L1465-1492 (processShortcut's six cases — the two that need these methods)
    - src/backend/sidecar/__tests__/lifecycleStub.test.ts (the existing stub-surface suite this task extends — match its case naming)
  </read_first>
  <action>
    Add `reload` to the `fakeWindow` literal and `openDevTools` to `fakeWebContents`, both as
    void-returning no-ops that log ONE warning line naming the caller and the reason. Follow the
    existing `hide` comment's format exactly: state which real call site reaches it
    (`main.ts:1465`'s `processShortcut`, cases `ctrl+r` and `ctrl+shift+i`), why the method is
    missing today (the fake window is truthy, so `mainWindow?.reload()` does not short-circuit and
    throws a TypeError), and why a logged no-op is the right answer rather than a real
    implementation (a renderer reload and a devtools open are shell concerns with no sidecar
    equivalent; routing them would be new Tauri window work, not a port).

    State plainly in the comment that these two hotkeys are DECLARED DEGRADED under Tauri and must be
    recorded as such in `34.5-PORTED-CHANNELS.md` — do not describe them as working.

    Extend `lifecycleStub.test.ts` with cases asserting both methods exist, are callable, return
    `undefined`, and do not throw; and that calling them logs exactly once each.
  </action>
  <acceptance_criteria>
    - `grep -c "openDevTools" src/backend/sidecar/electronStub.ts` returns at least 1; same for `reload`.
    - `node -e` style check is not required, but a test asserts `BrowserWindow.getAllWindows()[0].reload()` and `.webContents.openDevTools()` both return `undefined` without throwing.
    - `cd src/backend && npx jest sidecar/__tests__/lifecycleStub.test.ts` exits 0 with at least 4 new cases.
    - `git diff src/backend/sidecar/electronStub.ts` shows the existing `hide` no-op and its comment byte-unchanged.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && cd src/backend && npx jest sidecar/__tests__/lifecycleStub.test.ts</automated>
  </verify>
  <done>The two hotkey cases that would have thrown now no-op loudly, with their degraded status written down rather than hidden.</done>
</task>

<task type="auto">
  <name>Task 2: Register the three send channels and shortcutsExists</name>
  <files>src/backend/sidecar/shortcutsFlowRegistration.ts</files>
  <read_first>
    - src/backend/sidecar/shortcutsFlowRegistration.ts (the declared contract from plan 34.5-04)
    - src/backend/shortcuts/ipc_handler.ts (full file, 74 lines — the four bodies to port, including the exact `notify()` payloads and `t()` keys for `addShortcut`/`removeShortcut`; READ-ONLY, never import it)
    - src/backend/shortcuts/shortcuts/shortcuts.ts L148-175 (`shortcutFiles` and its per-platform `getPath('desktop')` branches — the reason this plan depends on 34.5-01) and L33-130 (`addShortcuts`/`removeShortcuts`)
    - src/backend/sidecar/humbleLoginFlowRegistration.ts L285-315 (the `void (async () => { try {…} catch { logSendFailure(…) } })()` send-body guard and `logSendFailure`)
    - src/backend/main.ts L1465-1492 (processShortcut's switch, verbatim)
    - src/backend/sidecar/pathShim.ts (as extended by plan 34.5-01 — confirm `desktop` is implemented before registering anything that reaches `shortcutFiles`)
  </read_first>
  <action>
    Fill `registerShortcutsFlows()` with four registrations.

    `ipcMain.handle('shortcutsExists', …)` — port the body verbatim: resolve the game via `getGame`,
    read `title` from `getGameInfo()`, call `shortcutFiles(title)`, and return
    `existsSync(desktopFile ?? '') || existsSync(menuFile ?? '')`. Unpack `appName` and `runner` from
    `args[0]`/`args[1]`.

    `ipcMain.on('addShortcut', …)` and `ipcMain.on('removeShortcut', …)` — ported bodies wrapped in
    the `void (async () => { try { … } catch (error) { logSendFailure('<channel>', error) } })()`
    guard. Keep the `notify()` calls and their `t()` keys exactly as the Electron originals have
    them. Add a comment on each explaining that the Electron original is an unguarded `async` body
    only because Electron's `ipcMain.on` catches for it, and that `electronStub`'s recorder does not,
    so the guard is required, not stylistic.

    `ipcMain.on('processShortcut', …)` — port the six-case switch verbatim inside the same guard.
    Import `getMainWindow` from `../main_window`, `handleExit` from `../utils`, and
    `sendFrontendMessage` from `../ipc`. Add a comment above the `ctrl+r` and `ctrl+shift+i` cases
    naming them as DECLARED DEGRADED under Tauri (they reach `electronStub`'s logged no-ops from
    Task 1) and pointing at `34.5-PORTED-CHANNELS.md` for the honest declaration. Do NOT delete
    those two cases and do NOT invent a Tauri reload/devtools path — that would be new shell work,
    not a port.

    Curated imports only: never import `shortcuts/ipc_handler.ts` or `main.ts`.
  </action>
  <acceptance_criteria>
    - `grep -c "ipcMain.on(" src/backend/sidecar/shortcutsFlowRegistration.ts` returns 3; `grep -c "ipcMain.handle(" ...` returns 1.
    - Each of the three `ipcMain.on` bodies contains a `try`/`catch` and a `logSendFailure` call — assert 3 occurrences of `logSendFailure`.
    - All six `processShortcut` combinations (`ctrl+r`, `ctrl+q`, `ctrl+k`, `ctrl+j`, `ctrl+l`, `ctrl+shift+i`) appear in the ported switch.
    - `grep "^import" src/backend/sidecar/shortcutsFlowRegistration.ts | grep -c "ipc_handler\|\.\./main'"` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Four channels registered with the right kinds, three send bodies that cannot produce an unhandled rejection, and two degraded hotkeys marked as such in source.</done>
</task>

<task type="auto">
  <name>Task 3: Registration-kind and containment test for the shortcuts cluster</name>
  <files>src/backend/sidecar/__tests__/shortcutsFlows.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/humbleLoginFlows.test.ts L140-175 (the bidirectional template)
    - src/backend/sidecar/__tests__/steamAuthFlows.test.ts L40-70 (the `jest.mock('os', …)` disposable-homedir block — MANDATORY here)
    - src/backend/jest.setupContainment.ts (the HOME/APPDATA/XDG redirection that runs for every backend test — this suite must assert against it, never bypass it)
    - .planning/phases/34.5-.../34.5-VALIDATION.md § "Test Infrastructure" (the explicit rule: shortcut-writing tests MUST assert against a redirected desktop/home, never the developer's real one)
    - src/backend/shortcuts/ipc_handler.ts and src/backend/main.ts L1465 (the kind ground truth: 3 SEND, 1 handle)
  </read_first>
  <action>
    Create `shortcutsFlows.test.ts`, calling `registerShortcutsFlows()` ONCE at file scope, with the
    disposable-homedir harness installed before any import that reaches the path chain.

    Assertions:
      1. Bidirectional kind: `addShortcut`, `removeShortcut` and `processShortcut` are each in
         `listenerRegistry` exactly once and absent from `handlerRegistry`; `shortcutsExists` is in
         `handlerRegistry` and absent from `listenerRegistry`. Reference REQ-34.5-05.
      2. Containment: `getPath('desktop')` as resolved inside this test returns a path UNDER the
         redirected home, and the test asserts explicitly that it does not start with the value of
         the real user's home directory captured before the redirect. Comment this as the
         `tests-clobbering-real-steam-store` rule applied to a cluster that writes outside the
         app-support dir by design.
      3. Send-body safety: invoke each of the three registered listeners directly with a dependency
         mocked to reject, and assert no unhandled rejection escapes and a failure was logged. Use
         the test runner's unhandled-rejection detection rather than only checking the log, so a
         missing guard actually fails the suite.
      4. `processShortcut` behaviour: `ctrl+k`/`ctrl+j`/`ctrl+l` each push `openScreen` with the
         expected route; `ctrl+q` reaches `handleExit`; `ctrl+r` and `ctrl+shift+i` do NOT throw and
         reach the electronStub no-ops. An unknown combination is a no-op.
      5. A forward-looking pin, commented as such: `addToSteam`, `removeFromSteam` and
         `isAddedToSteam` are NOT yet registered by this module — plan 34.5-11 flips these three
         assertions and may not delete them.
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/shortcutsFlows.test.ts` exits 0.
    - At least 8 bidirectional kind assertions exist (4 channels × both directions).
    - A containment assertion exists comparing the resolved desktop path against the pre-redirect real home and requiring they differ.
    - Three send-body rejection cases exist, one per send channel, and each fails if its guard is removed — state in the SUMMARY that you verified this by temporarily removing one guard, or explain why that check was not run.
    - The `ctrl+r` and `ctrl+shift+i` cases are asserted not to throw.
    - `git diff --name-only` does not list `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/shortcutsFlows.test.ts</automated>
  </verify>
  <done>The three send channels are kind-pinned and guard-proven, and no assertion in this suite can reach the developer's real Desktop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → send-kind channels | A send channel returns nothing; the renderer cannot distinguish success from a swallowed failure |
| sidecar → filesystem outside the app-support dir | The first slice that writes `.desktop`/`.lnk`/`.app` artifacts into the user's own home by design |
| renderer → `processShortcut` combination string | A caller-supplied string selecting a shell action, including application exit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-26 | Repudiation | `addShortcut` / `removeShortcut` failing silently | mitigate | Registered as `ipcMain.on` per source truth, each body wrapped in the fire-and-forget guard with `logSendFailure`; a per-channel rejection test fails the suite if a guard is removed. |
| T-34.5-27 | Denial of Service | `processShortcut` throwing a TypeError on `ctrl+r`/`ctrl+shift+i` | mitigate | `electronStub` gains logged `reload`/`openDevTools` no-ops (the Phase 34.1 Plan 04 `hide` precedent); tests assert both hotkeys complete without throwing. |
| T-34.5-28 | Tampering | a test writing shortcut artifacts into the developer's real home | mitigate | `jest.setupContainment.ts` stays enabled and the suite asserts the resolved desktop path differs from the pre-redirect real home — project memory records a prior suite reaching the real application-support dir. |
| T-34.5-29 | Elevation of Privilege | `processShortcut`'s `ctrl+q` reaching `handleExit()` from a caller-supplied string | accept | Unchanged from Electron; the switch matches exact literals only and has no default branch, so an unrecognised string is a no-op. The caller is this app's own frontend. |
| T-34.5-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `cd src/backend && npx jest sidecar/__tests__/shortcutsFlows.test.ts sidecar/__tests__/lifecycleStub.test.ts` exits 0.
- `grep -c "ipcMain.on(" src/backend/sidecar/shortcutsFlowRegistration.ts` returns 3.
- `grep -c "logSendFailure" src/backend/sidecar/shortcutsFlowRegistration.ts` returns 3.
</verification>

<success_criteria>
Desktop shortcuts and the hotkey channel work through the sidecar; every send body guards its own
promise; the two hotkeys that genuinely cannot work are declared degraded rather than silently
broken; and nothing in the suite can touch the developer's real Desktop.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-08-SUMMARY.md` when done
</output>
