---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 12
subsystem: ipc
tags: [tauri, sidecar, ipc, electron-migration, wine, gog, legendary, saves-sync, winetricks]

# Dependency graph
requires:
  - phase: 34.5-07
    provides: "the runnerMiscFlowRegistration.ts module with 6 of 11 declared channels already registered (runner-CLI version probes + Wine-runtime), and runnerMiscFlows.test.ts's registration-kind harness + forward-looking pin for the 5 channels this plan completes"
  - phase: 34.5-01
    provides: "pathShim.ts's documents case, the actual precondition this plan's syncGOGSaves comment cites (see Deviations for the correction to which channel really depends on it)"
provides:
  - "The last 5 of this slice's 38 channels: callTool, egsSync, getGOGLinuxInstallersLangs, syncSaves, syncGOGSaves — runnerMiscFlowRegistration.ts is now complete at 11/11"
  - "callTool's winetricks branch proven live (not gated on Phase 34.6) both by direct code inspection and a dedicated test"
  - "A source-level and deferred-items.md correction to CONTEXT.md D-09 / RESEARCH.md Pitfall 1's claim that syncGOGSaves is the live caller of save_sync.ts:146 — it is not; getDefaultSavePath is, and that channel remains genuinely unported"
affects: [34.5-13, 34.6]

# Tech tracking
tech-stack:
  added: []
  patterns: ["curated named-import registration module (no ipc_handler.ts side-effect import)", "factory-mocking curated import targets (../../storeManagers, ../../tools, ../../ipc, ../../online_monitor) rather than driving the real store-manager/tool graph in a registration-kind test"]

key-files:
  created: []
  modified:
    - src/backend/sidecar/runnerMiscFlowRegistration.ts
    - src/backend/sidecar/__tests__/runnerMiscFlows.test.ts
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md

key-decisions:
  - "Corrected CONTEXT.md D-09 / RESEARCH.md Pitfall 1's claim that syncGOGSaves's own call chain reaches save_sync.ts:146 (getDefaultGogSavePaths). Direct verification (storeManagers/gog/library.ts:94's getGame(), storeManagers/gog/games.ts's syncSaves() method, and SyncSaves/gog.tsx's getLocations()) shows syncGOGSaves only iterates the already-resolved gogSaves array it is given; the actual caller of getDefaultGogSavePaths is the separate getDefaultSavePath channel, invoked by the frontend BEFORE syncGOGSaves in its own round trip. getDefaultSavePath is not one of this slice's 38 channels and remains genuinely unported — logged to deferred-items.md for a future pass (likely 34.6)."
  - "Split Task 1 and Task 2 into two commits despite both landing in the same function, by staging the file's docstring/imports/body incrementally (verified tsc clean at each intermediate state) rather than committing the whole diff at once, to preserve one-commit-per-task traceability."
  - "documents-path test assertion implemented as the plan's explicitly-sanctioned narrower form (getPath('documents') resolves without throwing under the redirected home) rather than driving getDefaultGogSavePaths end-to-end — justified further by the deviation above: syncGOGSaves doesn't reach that function anyway, so an end-to-end drive through syncGOGSaves would have proven nothing about syncGOGSaves and duplicated pathShim.test.ts's own full coverage."

requirements-completed: [REQ-34.5-07, REQ-34.5-08]

# Metrics
duration: ~45min
completed: 2026-07-29
---

# Phase 34.5 Plan 12: Misc Module Completion — callTool, egsSync, GOG Langs, Saves-Sync Summary

**Completed the sidecar's runnerMiscFlowRegistration.ts at 11/11 channels (callTool's 4-branch Wine-tool dispatch, egsSync, getGOGLinuxInstallersLangs, syncSaves, syncGOGSaves), proved callTool's winetricks branch is live rather than Phase-34.6-gated, and corrected an inherited planning claim about which channel actually resolves GOG's DOCUMENTS save path.**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-07-29 (session start)
- **Completed:** 2026-07-29T21:15:00Z (approx.)
- **Tasks:** 3
- **Files modified:** 2 source files (1 registration module, 1 test file) + 1 planning doc (deferred-items.md)

## Accomplishments
- `registerRunnerMiscFlows()` now registers all 11 declared channels. The 5 added by this plan: `callTool` (all four branches — `winetricks`/`winecfg`/`runExe`/gog post-step — ported verbatim from `tools/ipc_handler.ts`), `egsSync`, `getGOGLinuxInstallersLangs`, `syncSaves` (including the `isEpicServiceOffline`/`isOnline` guards), `syncGOGSaves`.
- Verified and recorded, with file:line evidence, that none of `callTool`/`egsSync`/`getGOGLinuxInstallersLangs` reaches an Electron API the sidecar shim doesn't already cover: all three route through `libraryManagerMap` (curated `../storeManagers` import), `../tools` (`Winetricks`/`runWineCommandOnGame`), and `../ipc` (`sendFrontendMessage`) — no new Electron surface.
- `callTool`'s `winetricks` branch is flagged in-source (and proven by a dedicated test) as calling `Winetricks.run()` live today, NOT gated behind Phase 34.6's deferred winetricks IPC channels (Pitfall 4) — the test explicitly asserts `Winetricks.run` is invoked, not skipped/thrown.
- Verified and recorded that no dialog/prompt is reachable from either saves-sync path: legendary's `sync-saves` always sets `-y` (`storeManagers/legendary/games.ts:843-873`), and GOG's `save-sync` loop (`storeManagers/gog/games.ts:850-910`) never prompts either.
- **Correction to inherited planning claim:** CONTEXT.md's D-09 and `34.5-RESEARCH.md`'s Pitfall 1 both state `getPath('documents')` (`save_sync.ts:146`) is reached "via `syncGOGSaves`". Direct verification for this plan shows this is not accurate — `syncGOGSaves`'s handler chain (`getGame(appName).syncSaves(arg, '', gogSaves)`) never calls `getDefaultGogSavePaths`; it only iterates the already-resolved `gogSaves` array. The actual (and only) caller is the separate `getDefaultSavePath` channel, invoked by the frontend (`SyncSaves/gog.tsx`'s `getLocations()`) *before* `syncGOGSaves`, in its own round trip — and `getDefaultSavePath` remains genuinely unported after this plan (confirmed absent from every sidecar registration module). Recorded in the module's header docstring, in the `syncGOGSaves` handler's own comment, and in `deferred-items.md` item 4. The Discretion question research resolved (`documents` belongs to saves-sync, not shortcuts) still holds — only the specific "which channel's runtime path reaches the line" claim is corrected.
- Extended `runnerMiscFlows.test.ts` from 16 to 28 tests: flipped plan 34.5-07's forward-looking "not yet registered" pin to positive for all 5 new channels, added a module-completeness assertion (11 handle / 0 send, `listenerRegistry` untouched), added the three deferred winetricks channel names (`winetricksAvailable`/`winetricksInstall`/`winetricksInstalled`) to the absent-from-both-registries check, added a narrow `documents`-path pin, and added 7 `callTool` branch-dispatch tests (winetricks/winecfg/runExe/falsy-exe-no-op/gog-post-step-present/gog-post-step-absent/legendary-does-not-trigger-gog-post-step).

## Task Commits

Each task was committed atomically:

1. **Task 1: Port callTool, egsSync and getGOGLinuxInstallersLangs** - `9aa17fc18` (feat)
2. **Task 2: Port syncSaves and syncGOGSaves on top of the documents path** - `12790cdcd` (feat)
3. **Task 3: Complete the misc cluster's registration-kind coverage** - `d76415a85` (test)

_Note: this plan is not `tdd="true"`; all three tasks are `type="auto"`. Tasks 1 and 2 both modify the same file and function; the diff was staged in two verified-clean-at-tsc intermediate states (Task 1 content only, then Task 1+2 content) to preserve one-commit-per-task traceability rather than landing both in a single commit._

## Files Created/Modified
- `src/backend/sidecar/runnerMiscFlowRegistration.ts` - completed all 11 declared channels (5 added this plan); header docstring updated with the `callTool` REQ-34.1-10/D-14 reassignment note and the `documents`/D-09 correction
- `src/backend/sidecar/__tests__/runnerMiscFlows.test.ts` - extended from 16 to 28 tests: full 11-channel bidirectional registration-kind coverage, module-completeness assertion, deferred-winetricks-channel-name absence check, narrow `documents`-path pin, and 7 `callTool` branch-dispatch tests
- `.planning/phases/34.5-.../deferred-items.md` - logged the `getDefaultSavePath`-is-unported finding (item 4) and the full-suite "Jest did not exit" teardown warning (item 5, benign, exit 0, pre-existing, out of scope)

## Decisions Made
- See `key-decisions` in frontmatter: the D-09/Pitfall-1 correction, the two-commit split for Tasks 1/2 sharing one file, and the narrower `documents`-path test justification.
- Declared the test file's `mockGogGetGame`/`mockLegendaryGetGame` jest mocks as bare `jest.fn()` rather than `jest.fn(() => ({...}))` — TypeScript infers a zero-parameter signature from an inline factory implementation, which then rejects the `(...args: unknown[]) => mockGogGetGame(...args)` spread wrapper used inside the `../../storeManagers` mock factory (TS2556). Return values are configured via `.mockReturnValue()` in a `beforeEach` instead, consistent with the project's `resetMocks: true` config (which clears mock implementations, including ones set via an initial `jest.fn(impl)` factory, before every test).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Inaccurate `syncGOGSaves` → `save_sync.ts:146` reach claim, inherited from CONTEXT.md D-09 / RESEARCH.md Pitfall 1**
- **Found during:** Task 2 (reading `storeManagers/gog/games.ts`'s `syncSaves()` method in full per the task's own `read_first` instruction, and cross-checking against `SyncSaves/gog.tsx`)
- **Issue:** Both CONTEXT.md and 34.5-RESEARCH.md assert `getPath('documents')` (the sole call site, `save_sync.ts:146` inside `getDefaultGogSavePaths`) is reached "via `syncGOGSaves`". This is not correct: `syncGOGSaves`'s handler (`libraryManagerMap['gog'].getGame(appName).syncSaves(arg, '', gogSaves)`) delegates to `GOGGame.syncSaves()`, which only loops over the `gogSaves` array it's given — it never calls `getDefaultGogSavePaths`. The actual (and sole) caller of that function is the separate, still-unported `getDefaultSavePath` channel, which the frontend calls in its own round trip before `syncGOGSaves`.
- **Fix:** Documented the correction in three places: `runnerMiscFlowRegistration.ts`'s header docstring (a dedicated "documents gray area" paragraph), the `syncGOGSaves` handler's own inline comment, and `deferred-items.md` item 4 (which also flags `getDefaultSavePath` as a genuinely unported channel for a future pass). No code behavior was changed — `syncGOGSaves` is still ported byte-faithfully from `main.ts`; this is purely a documentation/planning-record correction, since inventing a call to `getDefaultGogSavePaths` inside `syncGOGSaves` that the Electron original never had would itself be a behavior change outside this plan's scope.
- **Files modified:** `src/backend/sidecar/runnerMiscFlowRegistration.ts`, `.planning/phases/34.5-.../deferred-items.md`
- **Verification:** Re-read `storeManagers/gog/library.ts:94` (`getGame()`), `storeManagers/gog/games.ts:850-910` (`syncSaves()` full method body), and `SyncSaves/gog.tsx:54-64` (`getLocations()` calling `getDefaultSavePath` separately) to confirm the trace; grepped every `src/backend/sidecar/*.ts` file for `getDefaultSavePath` (zero matches) to confirm it remains unported.
- **Committed in:** `9aa17fc18`/`12790cdcd` (docstring landed with Task 1's header rewrite and Task 2's handler comment respectively)

---

**Total deviations:** 1 auto-fixed (Rule 1 — documentation/planning-record correction, no behavior change)
**Impact on plan:** No architectural change, no new dependency, no scope creep. The correction is narrowly about which already-out-of-scope channel (`getDefaultSavePath`) is the true reach point for a dependency (`documents`) whose domain assignment (saves-sync, not shortcuts) remains exactly as research resolved it.

## Issues Encountered
- TypeScript rejected the test file's initial `jest.fn(() => ({...}))`-style mock declarations for `mockGogGetGame`/`mockLegendaryGetGame` (TS2556, spread-argument arity mismatch from an inferred zero-parameter signature). Resolved by declaring bare `jest.fn()` and configuring return values via `.mockReturnValue()` in `beforeEach` — see Decisions Made.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
All 38 of this slice's declared channels are now registered across the four sidecar registration modules (`runnerAuthFlowRegistration.ts`, `wineToolsFlowRegistration.ts`, `shortcutsFlowRegistration.ts`, `runnerMiscFlowRegistration.ts`). Plan 34.5-13 (if it exists) or the phase's closing/live-gate work is next. One item remains genuinely out of this phase's scope and is logged for Phase 34.6 consideration: `getDefaultSavePath` is unported, meaning the GOG saves-sync settings panel's location-resolution round trip (`SyncSaves/gog.tsx`'s `getLocations()`) will fail under the Tauri sidecar until that channel is added — `syncGOGSaves` itself (this plan's channel) works standalone once a caller supplies a `gogSaves` array by some other means.

Full backend suite recorded verbatim: `npm run test:ci` → **173/173 suites, 3251/3251 tests, exit 0** (baseline at plan start: 173/173 suites, 3239/3239 tests, exit 0 — net +12 tests, all in `runnerMiscFlows.test.ts`). `npx tsc --noEmit` → exit 0. A pre-existing, full-suite-only "Jest did not exit one second after the test run has completed" teardown warning appears (exit code still 0); it does not reproduce when running `runnerMiscFlows.test.ts` in isolation and is unrelated to this plan's two-file change set — logged to `deferred-items.md` item 5, not investigated further (out of scope).

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-07-29*

## Self-Check: PASSED

All created/modified files verified present on disk (`runnerMiscFlowRegistration.ts`,
`runnerMiscFlows.test.ts`, this SUMMARY.md); all task commit hashes (`9aa17fc18`, `12790cdcd`,
`d76415a85`) and the metadata commit hash (`ad28fb07e`) verified present in `git log --oneline --all`.
