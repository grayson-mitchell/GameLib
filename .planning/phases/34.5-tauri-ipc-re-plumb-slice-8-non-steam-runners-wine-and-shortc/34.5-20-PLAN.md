---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 20
type: execute
wave: 4
depends_on: [34.5-19]
gap_closure: true
closes_gaps: [G-6, G-2, G-4, G-5]
requirements: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-12]
autonomous: false
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md

must_haves:
  truths:
    - "All seven preconditions of `34.5-LIVE-GATE-RERUN.md` are satisfied and evidenced verbatim, in order, before any item is attempted"
    - "All five items are ATTEMPTED — including item 4, which was skipped last run — and each carries a filled Result slot with verbatim evidence"
    - "The verdict is recorded honestly: `items_passed + items_failed = 5`, or the shortfall is stated explicitly in the frontmatter"
    - "`34.5-LIVE-GATE.md` is never edited — the superseded FAILED run stays intact"
    - "Nothing is struck in 'What this gate falsifies' unless the corresponding item PASSed"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md"
      provides: "The executed gate: filled preconditions, five filled Result slots, a verdict"
      contains: "Overall verdict"
  key_links:
    - from: "~/Library/Logs/GameLib/gamelib.log"
      to: "34.5-LIVE-GATE-RERUN.md item Result slots"
      via: "verbatim log excerpts quoted as evidence, never paraphrased"
      pattern: "\\(\\d\\d:\\d\\d:\\d\\d\\)"
    - from: "34.5-LIVE-GATE-RERUN.md verdict"
      to: "34.5-21-PLAN.md propagation"
      via: "the recorded per-item results the propagation plan copies into the channel list"
      pattern: "Overall verdict"
---

<objective>
Execute the blocking 5-item live gate authored by plan 34.5-19, on real macOS hardware, under
`pnpm tauri:dev`, with a human driving three real OAuth logins.

This is the phase-closing condition (REQ-34.5-12, D-08). It cannot be automated, it cannot be
partially passed, and it cannot be overridden. The first run recorded 0 of 5 clean; this run exists
because the fix for its single diagnosed root cause has landed (plans 34.5-16 through 34.5-18) and
must now be proven where the failure actually lives.

Purpose: turn "the tests are green" into "a human watched three logins, a Steam shortcut, a `.app`
bundle and a Wine command work".
Output: `34.5-LIVE-GATE-RERUN.md` with every precondition and every Result slot filled, and a
recorded verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-16-SUMMARY.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-17-SUMMARY.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-18-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Satisfy and evidence all seven preconditions, in order</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md</files>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` — the contract in full; it is the instruction set for this task
    - `.planning/phases/34.5-.../34.5-LIVE-GATE.md` precondition RESULT blocks — the recording format to mirror (verbatim command output, tables for per-runner state)
    - `.planning/phases/34.5-.../34.5-18-SUMMARY.md` — the exact `[bootstrap]` log prefixes precondition 4 greps for
  </read_first>
  <action>
    Work the seven preconditions strictly in order, filling each `RESULT —` slot in
    `34.5-LIVE-GATE-RERUN.md` as it is satisfied. Do not start an item until all seven are recorded
    satisfied.

    Precondition 1: quote 34.4.1's run-3 item-1 result line verbatim.

    Precondition 2: run `ps aux | grep -E "gamelib-shell|sidecar.js"`, record it verbatim, kill any
    survivors, re-run and record the clean output.

    Precondition 3: ask the developer to run a clean rebuild and `pnpm tauri:dev`
    (`npx electron-vite build` → `npm run build:sidecar` → `cargo run --manifest-path
    src-tauri/Cargo.toml`, matching the first run's own sequence). Record both PIDs from `ps aux`
    and the growth of `~/Library/Logs/GameLib/gamelib.log` (size and line count), read from the log
    file rather than the terminal.

    Precondition 4: grep this session's `gamelib.log` for the `[bootstrap] appRoot resolved=`,
    `[bootstrap] publicDir resolved=`, and four `[bootstrap] runner binary` lines, and quote them
    verbatim. Also grep for `SIDECAR ASSET ROOT DEFECT`. If that line is present, or if any
    `exists=` field reads `false`, STOP: record the block in this precondition's RESULT slot, do not
    attempt items 1-3, and report the blockage to the developer — the fix is not in this build and
    another code plan is needed before a human drives anything.

    Precondition 5: verify the credential-aside state with `ls`/`test -f` on all four paths named in
    the contract, and verify Amazon's `nile_config`/`nile_store` hold no auth state. Record verbatim.
    If a credential has reappeared, follow the contract's `cp -p` → `cmp` → remove procedure and
    record the new backup path. Never delete before `cmp` reports byte-identity.

    Precondition 6: run all four commands, record exit codes VERBATIM alongside the `Test Suites:`,
    `Tests:` and `test result:` lines. A count LOWER than the 3447/80 floor must be explained before
    proceeding.

    Precondition 7: grep the log for `[bootstrap] GAMELIB_SHELL_EXE received=` and record the value
    verbatim, noting that this is the child's own observation. If the line reads `<UNSET>`, STOP and
    report — item 4 cannot run and `pathShim.getPath('exe')` will throw.

    Redaction rule for everything recorded here: paths, PIDs, sizes, counts and booleans only. No
    `access_token`/`refresh_token` values, no cookies, no session identifiers.
  </action>
  <verify>
    <automated>grep -c "RESULT —" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md && git status --porcelain .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - All seven `RESULT —` slots are filled, in order, each with verbatim command output or a
      verbatim log excerpt — never a paraphrase and never "green".
    - Precondition 4 quotes six `[bootstrap]` lines and records the presence or absence of
      `SIDECAR ASSET ROOT DEFECT` explicitly.
    - Precondition 6 records four exit codes verbatim plus the test-count lines.
    - Precondition 7 records the `GAMELIB_SHELL_EXE` value from the sidecar's receipt line, not from
      the terminal.
    - `git status --porcelain` reports nothing for `34.5-LIVE-GATE.md`.
    - No credential value appears anywhere in the diff.
  </acceptance_criteria>
  <done>The machine, the build and the account state are all provably in the condition the gate requires.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Developer drives all five gate items on real hardware</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md</files>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` Items 1-5 — each item's What to do / What to observe / Evidence required
    - `.planning/phases/34.5-.../34.5-LIVE-GATE.md` Items 1-5 — how the FAILED run recorded its evidence, for format parity
  </read_first>
  <what-built>
    Plans 34.5-16 through 34.5-18 landed the app-root handoff (`GAMELIB_APP_ROOT` from both Rust
    spawn paths → `electronStub.getAppPath()` → `publicDir`), the existence-checked binary
    resolution, real-filesystem coverage under sidecar-like conditions, and boot-time logging of
    what the sidecar actually received. Precondition 4 has already proven, from this session's log,
    that all four runner binaries resolve to paths that exist.
  </what-built>
  <how-to-verify>
    With the app running from Task 1's `pnpm tauri:dev` session and `tail -f
    ~/Library/Logs/GameLib/gamelib.log` open in a second window:

    1. **Item 1 — Epic:** sign in to Epic from scratch with real credentials. Confirm the login
       window opens on Epic's real origin, the login completes, and the library is POPULATED (not
       merely "signed in" with zero games).
    2. **Item 2 — GOG:** same, for GOG.
    3. **Item 3 — Amazon:** same, for Amazon. Then grep the log for
       `[oauthLoginCapture] runner=nile origin-mismatch origin=` — on a clean PASS this line must
       NOT appear. If it appears, report the origin it names: Assumption A1's `www.amazon.com`
       anchor is falsified and item 3 is not a clean PASS.
    4. **Item 4 — both `exe` call sites.** QUIT STEAM FIRST (it rewrites `shortcuts.vdf` from memory
       on exit and will clobber the write). Confirm GOG *Alan Wake* (`1207659037`) is still
       installed. In the devtools console run `window.api.addShortcut('1207659037', 'gog', false)`
       and `await window.api.addToSteam('1207659037', 'gog')`. Then report: the `Exe` value written
       into `~/Library/Application Support/Steam/userdata/<REDACTED>/config/shortcuts.vdf`; the body
       of `<shortcut>.app/Contents/MacOS/run.sh`; and, after launching each, `ps aux` output showing
       the running process is the Tauri shell binary and not a bare `node`.
    5. **Item 5 — Wine:** with one of the newly-authenticated non-Steam runners, drive a Wine tool
       action that reaches `runWineCommand` (e.g. a winecfg / DXVK toggle / wine-version action) on
       a non-Steam game. Report the log lines for the dispatch and its completion, plus the
       observable in-app effect.

    Report the outcome of each item, with the relevant `gamelib.log` lines. A FAIL is a FAIL —
    report it exactly as observed. Do NOT paste any `access_token`, `refresh_token`, cookie or
    session value.
  </how-to-verify>
  <action>
    Record the developer's reported outcome for each of the five items into its Result slot in
    `34.5-LIVE-GATE-RERUN.md`, verbatim, with the `gamelib.log` excerpts they supply. Fill exactly
    one checkbox per item. If an item is not attempted, say NOT ATTEMPTED and say why — never
    PASS, FAIL or BLOCKED by default. If a failure occurs, record the observation without
    diagnosing it in this document; diagnosis belongs in the summary and the next gap cycle.

    Item 3: record Assumption A1 as CONFIRMED (line absent, login clean), FALSIFIED (line present,
    with the observed origin) or UNTESTED (the comparison path was never reached) — one of exactly
    those three, per the contract's own rule.

    Item 4: record BOTH call sites separately. A PASS requires both; one of two is not a partial
    pass, it is a FAIL for the item.

    Item 5: if it passes, note that research Pitfall 2's claim becomes eligible to be struck by plan
    34.5-21 — but do not strike anything here.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*PASS\|\[x\] \*\*FAIL\|\[x\] PASS\|\[x\] FAIL\|NOT ATTEMPTED" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md</automated>
  </verify>
  <acceptance_criteria>
    - All five items carry a filled Result slot; none is left empty.
    - Item 4 records both `nonesteamgame.ts:258` and `shortcuts.ts:227` outcomes independently.
    - Item 3 records A1 as exactly one of CONFIRMED / FALSIFIED / UNTESTED, with the observed origin
      when the mismatch line fires.
    - Every item's evidence is a verbatim log excerpt or verbatim command output.
    - No credential value appears in the diff.
    - `34.5-LIVE-GATE.md` remains unmodified.
  </acceptance_criteria>
  <resume-signal>Report each item's outcome (PASS / FAIL / NOT ATTEMPTED) with its evidence, or describe what blocked you.</resume-signal>
  <done>Five items resolved by observation on real hardware, recorded exactly as observed.</done>
</task>

<task type="auto">
  <name>Task 3: Record the verdict and the falsification ledger</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md</files>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` — the five filled Result slots from Task 2
    - `.planning/phases/34.5-.../34.5-LIVE-GATE.md` § "Verdict" and § "What this gate falsifies" — the format and the honesty rules to mirror
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` D-08 — the no-partial-pass rule the verdict applies
  </read_first>
  <action>
    Fill the Verdict table (one row per item), set the frontmatter `verdict` to `PASS` or `FAIL`,
    `status` to `complete`, and `items_passed`/`items_failed` to the counted values. If any item is
    NOT ATTEMPTED, state in a frontmatter note that `items_passed + items_failed` is less than 5 and
    why — the first run's frontmatter note is the model.

    Write the overall verdict line. Apply D-08 as written: any FAIL means Phase 34.5 does not close
    and another gap cycle follows. Only a clean 5-of-5 closes the phase.

    Complete "What this gate falsifies": strike a standing claim ONLY where its item PASSed. For each
    of the four pre-listed claims, write either the strike with its evidence pointer, or an explicit
    statement that it remains STANDING and why. Research Pitfall 2's claim must remain standing
    unless item 5 PASSed. Assumption A2 is retired only for the DEV build even on a PASS — the
    packaged branch (`spawn_sidecar_packaged`, `main.rs:1268`) and residual `R-34.5-G1-PKG` stay
    open, per the contract's own dev-vs-packaged caveat.

    Do not touch `34.5-PORTED-CHANNELS.md`, the inventory, the ROADMAP or STATE.md here — plan
    34.5-21 owns propagation, and splitting it keeps this plan's blast radius at one file.
  </action>
  <verify>
    <automated>grep -n "^verdict:\|^status:\|^items_passed:\|^items_failed:" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md && git status --porcelain .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `verdict` is `PASS` or `FAIL` (never left `null`), `status: complete`, and the
      counts match the filled Result slots.
    - The Verdict table has five rows, one per item, each matching its Result slot exactly.
    - "What this gate falsifies" resolves all four pre-listed claims as struck-with-evidence or
      explicitly still-standing; no claim is left unaddressed.
    - Assumption A2's packaged half and `R-34.5-G1-PKG` are recorded as still open regardless of
      outcome.
    - `34.5-LIVE-GATE.md` remains unmodified.
  </acceptance_criteria>
  <done>The run has a recorded verdict, and every standing claim is either struck with evidence or explicitly still standing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live OAuth login → three real third-party accounts | Real Epic, GOG and Amazon credentials are entered and real tokens are minted to disk |
| Real credential files → filesystem move/restore | Epic and GOG `user.json`/`auth.json` are moved aside and restored around the run |
| Live evidence → committed planning documents | Log excerpts are pasted into a public fork's repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-G6-04 | Information disclosure | Evidence pasted into `34.5-LIVE-GATE-RERUN.md` and the summary | mitigate | Every task carries the redaction rule: paths, PIDs, sizes, counts and booleans only; no `access_token`, `refresh_token`, cookie or session value. Acceptance criteria on all three tasks require the diff to contain no credential value |
| T-34.5-G6-05 | Denial of service (data loss) | Credential move-aside / restore | mitigate | Precondition 5 mandates `cp -p` → `cmp` byte-identity → remove, with both paths recorded in the document before any removal |
| T-34.5-G6-06 | Tampering | Item 4(a) writing `shortcuts.vdf` while Steam is running | mitigate | The quit-Steam-first warning is in the checkpoint's `how-to-verify` and in the contract; Steam rewrites the file from memory on exit and would clobber the write, producing a false FAIL |
| T-34.5-G6-07 | Elevation of privilege | Item 4 proving a launch command that runs a binary | mitigate | The item's PASS condition is precisely that the launched process is the Tauri shell binary and never `node`, verified by `ps aux` — this item exists to catch a wrong executable being written into a launch command |
| T-34.5-G6-08 | Repudiation | Rounding a partial result to green | mitigate | D-08 restated in Task 3; the arithmetic rule is pre-written in the contract; every Result slot was authored empty |
| T-34.5-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; `files_modified` is one markdown file |
</threat_model>

<verification>
- All seven preconditions filled with verbatim evidence
- All five Result slots filled; `items_passed + items_failed = 5` or the shortfall explained
- Frontmatter `verdict` set; `34.5-LIVE-GATE.md` byte-unchanged
- No credential value in the diff
</verification>

<success_criteria>
- The gate is COMPLETE: five items resolved by observation, not by inference
- The verdict is recorded exactly as observed, with no partial-pass closure
- Standing claims are struck only where the corresponding item PASSed
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-20-SUMMARY.md` when done
</output>
