---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 51
type: execute
wave: 4
depends_on: [34.5-50]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-ROUTING.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-GATE4-PASS-ROUTING.md
autonomous: false
gap_closure: true
cycle: 6
requirements: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-04, REQ-34.5-05, REQ-34.5-12]

must_haves:
  truths:
    - "All twelve preconditions in 34.5-LIVE-GATE-RERUN-3.md carry a recorded RESULT block, in order, none filled from an assumption"
    - "The secret-store arm was VERIFIED as keyring from the preserved log before any evidence was gathered, and any dev-vault reading stopped and restarted the run rather than continuing with a caveat"
    - "The session log was copied to a named sidecar file and cmp-verified byte-identical BEFORE any line from it was cited"
    - "All four items are attempted on real macOS hardware and each carries PASS, FAIL, BLOCKED or NOT ATTEMPTED — none silently omitted, none softened"
    - "Every line pasted into a Result slot was re-verified against the PRESERVED log before being recorded, and any discrepancy was recorded with the file's version winning"
    - "items_passed + items_failed + items_blocked + items_not_attempted equals 4, with any shortfall from 4 PASSes stated explicitly rather than rounded"
    - "Every ledger row is resolved against its OWN named observation, referenced as an (id, creating plan) pair, and no standing claim is struck without the corresponding ITEM passing"
    - "The finished document carries no unredacted token, cookie, session identifier, OAuth code value or Steam userdata account id, proven by a re-grep run AFTER redaction"
    - "Exactly one routing artifact exists and it matches the recorded verdict: PASS routes to phase closure, anything else routes to gap cycle 7 inside Phase 34.5"
    - "None of the three prior gate documents is modified, overwritten or deleted"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md"
      provides: "the completed fourth blocking gate record, all Result slots filled and the four-way verdict computed"
      contains: "Overall verdict"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-ROUTING.md"
      provides: "the FAIL branch: the cycle-7 work-list, created ONLY if the verdict is not a clean 4 of 4"
      contains: "D-08"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-GATE4-PASS-ROUTING.md"
      provides: "the PASS branch: the closure record, created ONLY on a clean 4 of 4"
      contains: "4 of 4"
  key_links:
    - from: "the preserved session log (gamelib.log.34.5-g6-gate4-<sessionletter>)"
      to: "34.5-LIVE-GATE-RERUN-3.md Result slots"
      via: "re-verification of every pasted line against the preserved copy, never against memory or the operator's paste"
      pattern: "gamelib\\.log\\.34\\.5-g6-gate4"
    - from: "34.5-LIVE-GATE-RERUN-3.md known-holes table"
      to: "34.5-UNTESTED-ITEMS.md Status cells"
      via: "row-by-row resolution against each row's own named observation, keyed by (id, creating plan)"
      pattern: "U-34\\.5-[0-9]+"
    - from: "the recorded four-way arithmetic"
      to: "the routing artifact"
      via: "a mechanical branch on the four counters, never on an expectation held before the run"
      pattern: "items_passed"
---

<objective>
Execute the BLOCKING four-item live gate against `34.5-LIVE-GATE-RERUN-3.md` on real macOS hardware,
record every result, compute the four-way verdict, resolve the ledger row by row, and write the one
routing artifact the verdict earns.

This is the fourth attempt. Runs 1, 2 and 3 all failed — 0/5, 0/5, and 0 passed / 2 failed / 1
blocked / 2 not attempted — each on a different layer: a wrong `publicDir` under the sidecar, an
OAuth capture that reached the backend and never the renderer, and a backend chain that fully
succeeded while the frontend rendered nothing. Behind this run sit seven cycle-6 fix plans
(34.5-43 through 34.5-49), **none of which is live-proven**. That is what this run is for.

**Four items, not five.** Epic is DESCOPED to Phase 34.7 per D-CYCLE6-A. The descope is not a pass
and retires nothing.

Per D-08, **anything short of a clean 4 of 4 means Phase 34.5 does not close** — no advisory mode,
no pre-authorised override, no deferred UAT entry, no partial-pass closure. The FAIL branch routes
to gap cycle 7 inside Phase 34.5 and this plan writes that routing artifact.

Every blocking defect this phase has found was found by a human driving the UI while a fully green
suite (3279, then 3387, then 3447, then 3548 tests) reported nothing wrong. That is why this plan
exists, why `autonomous` is `false`, and why it cannot be executed by an agent alone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE6-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/deferred-items.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md

**The contract is authoritative.** `34.5-LIVE-GATE-RERUN-3.md` (plan 34.5-50) governs. Where this
plan and the contract differ, **the contract wins**. Fill its Result slots; do not rewrite its items,
do not renumber them, and do not add a fifth.

**Marker strings come from the contract, not from this plan.** Read the contract's literal RESULT and
Result marker strings at Task 1 and use them verbatim in every check below. If they differ from
`**RESULT:**` and `**Result:**`, substitute the real strings and record the substitution in the
SUMMARY.

**Sidecar output is read from the PRESERVED COPY of `~/Library/Logs/GameLib/gamelib.log`, never from
the `pnpm tauri:dev` terminal** (`sidecar-console-and-logger-are-invisible`: stdout IS the RPC pipe,
so sidecar `console.*` is structurally invisible there). The one exception is `main.rs`'s
`eprintln!("[shell] …")` lines, which reach only the terminal — an item relying on one says so.
`gamelib.log` **rotated mid-gate on run 3** and the live file lost the run (F-34.5-G6-15); it was
recovered only from `gamelib.log.old`. Precondition 3 exists so that cannot happen again.

**Never use `tauri dev`** — it serves a stale static bundle from `frontendDist` and would silently
test the wrong frontend. Use `pnpm tauri:dev`.

**Environment for this run, per D-CYCLE6-D: `GAMELIB_DEV_SECRET_VAULT` MUST be UNSET for the whole
run.** This is the opposite of run 3's D-CYCLE5-B choice and it is deliberate. The guard is an
exact-string `=== '1'` match, so **verify the arm, never the export** — an export that silently
fails to reach the sidecar is exactly how gate 3 ended up spanning two sessions on two different
arms and made half its evidence uninterpretable.

**Ledger references are (id, creating plan) pairs, always.** Plan 34.5-50 found that `U-34.5-17` is
claimed by four concurrently-authored plans (34.5-44, 34.5-45, 34.5-46/-48 implicitly, 34.5-49).
34.5-50 allocates and records the collision; this plan must never resolve a bare `U-34.5-NN` id,
because a collision would then silently mis-resolve a row at gate time.

<interfaces>
**These are POINTERS. The contract carries the authoritative, freshly-read line shapes — use the
contract's text, and consult these files only to disambiguate a line the operator cannot find.**

- `src/backend/sidecar/bootstrap.ts` — `[bootstrap] secret stores: keyring` / `: dev-vault`;
  `[bootstrap] appRoot resolved=… source=…`; `[bootstrap] publicDir resolved=… exists=…`;
  `[bootstrap] runner binary … exists=…`; `[bootstrap] GAMELIB_SHELL_EXE received=…`;
  `[bootstrap] SIDECAR ASSET ROOT DEFECT`.
- `src/backend/sidecar/oauthLoginCapture.ts` — `[oauthLoginCapture] runner=… status=captured`,
  `runner=nile origin-mismatch origin=…`.
- `src/frontend/screens/WebView/useTauriOAuthLogin.ts` — `[useTauriOAuthLogin] runner=… phase=…`,
  terminal value `idle (login completed, library refresh triggered)`.
- `src/frontend/state/GlobalState.tsx` — `[refreshLibrary] runner=… origin=…`.
- `src/backend/sidecar/steamFlowRegistration.ts` — `refreshLibrary complete runner=… managers=…`,
  `refreshLibrary failed runner=…`, `handleLaunch`.
- `src/backend/protocol.ts` — `handleProtocol`.
- `src/backend/shortcuts/shortcuts.ts:227` (`generateMacOsApp`, the `run.sh` body) and
  `src/backend/shortcuts/nonesteamgame/nonesteamgame.ts:258` (the VDF `Exe` write) — item 3's two
  call sites. Both, never one.
- `src/frontend/helpers/declaredUnavailable.ts` — `callOrDeclare`'s decline line shape.
- `.claude/skills/spike-findings-gamelib/SKILL.md` — the sidecar-logging, login-window and WKWebView
  constraints, so a KNOWN gotcha is not mis-recorded as a new finding.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Satisfy and evidence all twelve preconditions, in order, on real hardware</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-3.md` § Preconditions — all twelve with their empty RESULT slots. **This is the file being filled and it is authoritative.** Note its literal RESULT marker string; every check in this plan uses that string verbatim.
    - `.planning/phases/34.5-.../34.5-CYCLE6-ROUTING.md` § D-CYCLE6-D — the four binding operating conditions, so the operator understands WHY each is mechanical rather than advisory.
    - `.planning/phases/34.5-.../34.5-43-SUMMARY.md` … `34.5-49-SUMMARY.md` — each SUMMARY's MEASURED suite count, which is precondition 8's floor. Cite the SUMMARY by filename; never transcribe a count from a prior gate document or from this plan.
    - `.claude/skills/spike-findings-gamelib/SKILL.md` — the sidecar-logging constraint that makes the preserved log the only admissible source.
  </read_first>
  <what-built>
    `34.5-LIVE-GATE-RERUN-3.md` (plan 34.5-50) carries twelve numbered preconditions with empty RESULT
    slots. Behind them sit the seven cycle-6 fix plans: 34.5-43 (`getInstallInfo` ported, details
    screen unblocked), 34.5-44 (Rust argv parsing, single-instance guard, deep-link delivery to
    `handleProtocol`), 34.5-45 (`nativeImage.createFromBuffer`, the dropped `addToSteam` return value),
    34.5-46 (runner-aware `launch` dispatch), 34.5-47 (`origin=` threading and post-login navigation),
    34.5-48 (the EOS cluster declining visibly), 34.5-49 (the preload-surface audit). **None of it has
    ever been observed live.**
  </what-built>
  <how-to-verify>
    Work the contract's preconditions 1 through 12 IN ORDER and record each RESULT slot verbatim. Do
    not reorder them and do not skip one because it "obviously holds" — an unstated or assumed
    precondition is exactly how a gate becomes UAT debt, and this phase's own first run did it. Fill
    each slot as it arrives; do not batch them at the end.

    The contract's own text governs. This is the sequence and the traps to expect at each step:

    1. **Kill orphans.** `ps aux | grep -E "gamelib-shell|sidecar.js"` recorded verbatim; `kill -9`
       every match including an orphaned `sidecar.js` with no `gamelib-shell` parent; re-run and
       record the clean listing. Gate 3 ran with an orphaned pair contending for the same store
       files, which makes every store-backed observation ambiguous. After launch, the same command
       must show **exactly one of each**, with PIDs recorded.
    2. **Clean rebuild, then `pnpm tauri:dev`.** Never `tauri dev`. Prove sidecar liveness from log
       growth plus the `ps` listing, never from the terminal. Record the build completion timestamp —
       precondition 12's staleness check needs it, and 34.5-46's own stale-observation trap
       (a UAT observation at 00:33:24 against a bundle that only built at 01:24:43) is why.
    3. **Preserve the log before trusting it.** Copy `~/Library/Logs/GameLib/gamelib.log` to
       `gamelib.log.34.5-g6-gate4-<sessionletter>` and `cmp`-verify byte-identical. Record the path
       and the `cmp` exit code. **Every excerpt in every Result slot from here on is quoted from the
       PRESERVED copy.** If the run spans sessions, preserve each separately and label every excerpt
       with its session letter.
    4. **The arm is `keyring`, VERIFIED.** `grep "\[bootstrap\] secret stores: "` against the
       preserved log must end **`keyring`**; zero `[dev-secret-vault]` lines may appear anywhere.
       **If it reads `dev-vault`, STOP the run and restart it** — unset the variable, relaunch,
       re-preserve the log, re-verify. Do not continue and caveat. A `dev-vault` run issues no real
       `keyring_get` at all, so it is **NO evidence** for `(U-34.5-01, its creating plan)` and
       `(U-34.5-10, its creating plan)` — not weaker evidence, none — and continuing would spend a
       whole live run buying nothing for either row. Re-check per session; **never switch arms
       mid-run.**
    5. **Signed OUT of GOG and Amazon — verified, not assumed.** Fresh `ls`/`test -f` on every
       original and backup path, recorded verbatim, covering `nile_store/` **and** `nile_config/`.
       Plan 34.5-20's SUMMARY records a prior session ending with GOG signed back IN — assume nothing
       survived. Any re-aside uses a NEW dated suffix with a `cmp` byte-identity proof before the
       original is removed. Never delete blind. **No Epic sign-out is required or performed.**
    6. **The G-1 fix is in THIS build:** `source=GAMELIB_APP_ROOT` (a `process.cwd` source does NOT
       satisfy it even if the path looks right), `publicDir … exists=true`, all four runner-binary
       `exists=true` lines. A `SIDECAR ASSET ROOT DEFECT` line anywhere is a **hard STOP**.
    7. **`GAMELIB_SHELL_EXE` from the SIDECAR's own receipt line**, never the shell's `eprintln!` —
       the terminal line structurally cannot distinguish "set on the child env" from "visible to
       `pathShim`". An `<UNSET>` reading **BLOCKS item 3**. A dev PASS never retires the packaged
       half of Assumption A2 nor `R-34.5-G1-PKG`.
    8. **The full suite battery at the HEAD the gate runs on**, exit codes VERBATIM: `npm run
       test:ci`, `npx tsc --noEmit`, `cargo check`, `cargo test`, `ported-channels-gate.py`,
       `preload-surface-gate.py`. Compare against the floor MEASURED in the wave-1/2 SUMMARYs, cited
       by filename. A count at or below the floor means HEAD is stale; a count above it also needs an
       explanation. Explain any deviation in writing rather than rounding it to green.
    9. **The cycle-6 fixes are present at HEAD**, by comment-filtered grep
       (`grep -v '^\s*//' | grep -v '^\s*\*' | grep -c …`) so a doc comment cannot satisfy a code
       assertion. Any absence is a **hard STOP** — this is not the build the cycle produced, and no
       result may be attributed to a fix that is not present. Record beside it that **presence is not
       proof of effect**; that is what the four items are for.
    10. **The Epic descope is in force.** Confirm in this slot that the run was executed as a
        FOUR-item gate, and record the `deferred-items.md` item number. Phase 34.4.1's gate passed
        4/4 while a struck precondition turned an untested hole into an implicit pass — a silently
        missing item is precisely the failure mode this slot exists to prevent.
    11. **Item-3 preflight — the non-event guard.** Record this machine's live `addDesktopShortcuts`,
        `addStartMenuShortcuts` and `addSteamShortcuts` values. On gate 3 all three were `false` and
        the prescribed `addShortcut(appName, runner, false)` was a **guaranteed silent non-event**
        (F-34.5-G6-11). Use the contract's prescribed remedy and record which one (`fromMenu=true`,
        or flipping the globals and recording that they were flipped). Confirm the target `appName`
        and `runner`, the `shortcuts.vdf` path with `userdata/<REDACTED>`, the quit-Steam step, and
        both devtools invocations — all inlined in the contract, so nothing needs discovering
        mid-run.
    12. **Item-4 preflight — the Wine repoint.** Re-verify `tools/wine/Wine-Crossover-latest` and its
        `Contents/MacOS/wine` exist NOW rather than assuming it from UAT test 4; record the `ls`/
        `test -f` output. Name a currently-INSTALLED non-Steam game — **not Alan Wake**, which was
        uninstalled during the observing session. Repoint it at the `wine`-type version and record
        the before/after `wineVersion.type` values.

    Hard STOPs: precondition **4** (a `dev-vault` reading — stop and restart), **6** (asset-root
    defect), **9** (a missing cycle-6 fix). Precondition **7**'s `<UNSET>` blocks item 3 only. If a
    hard STOP fires, halt here, record which one, and do not proceed to Task 2.
  </how-to-verify>
  <action>
    Present the contract's twelve preconditions to the developer IN ORDER and collect each RESULT
    block's verbatim command output. Fill the contract's own RESULT slots as they arrive. Never fill a
    slot from an assumption, from a prior run's document, or from this plan's prose. If a hard STOP
    fires, halt the plan and report which one; if precondition 4 reads `dev-vault`, instruct a full
    restart rather than accepting the reading.
  </action>
  <acceptance_criteria>
    - All twelve RESULT slots in `34.5-LIVE-GATE-RERUN-3.md` are filled with verbatim command output, in the contract's order, and no placeholder text (`(unfilled)`, `TBD`, `<to be filled>`) remains anywhere in the document.
    - Precondition 3 records the preserved log's path and a `cmp` exit code of 0, and the preserved filename matches `gamelib.log.34.5-g6-gate4-*`.
    - Precondition 4's recorded grep output ends `keyring`, and `grep -c "\[dev-secret-vault\]"` against the preserved log returns 0. If any session read `dev-vault`, the SUMMARY records the restart, not a caveat.
    - Precondition 1 records both listings: empty before launch, exactly one `gamelib-shell` and one `sidecar.js` with PIDs after.
    - Precondition 6 records the literal `source=GAMELIB_APP_ROOT` reading and a zero count for `SIDECAR ASSET ROOT DEFECT`.
    - Precondition 8's numbers are compared against the floor cited from a named wave-1/2 SUMMARY filename, with every deviation explained in writing in either direction.
    - Precondition 11 records the three global shortcut settings as read from live config and names which non-event remedy was used.
    - Precondition 12 records a `Wine-Crossover-latest` re-verification performed during THIS session, names an installed non-Steam target that is not Alan Wake, and records before/after `wineVersion.type`.
    - If a hard STOP fired, the SUMMARY names it and NO item result is recorded anywhere.
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -ciE "\(unfilled\)|^TBD$|&lt;to be filled&gt;" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md) -eq 0 &amp;&amp; grep -q "gamelib.log.34.5-g6-gate4" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md &amp;&amp; grep -qE "secret stores: keyring" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md</automated>
  </verify>
  <resume-signal>Paste all twelve RESULT blocks with their verbatim command output. Then type "preconditions satisfied", or name which one blocked and why.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive all four items on real hardware, and gather the three cross-cutting observations</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-3.md` §§ Items 1–4 (every evidence clause, verbatim — **this is the script**), § "Cross-cutting observations", § "Known holes (ledger Rule 3)" (so each row's own named observation is gathered deliberately rather than reconstructed in hindsight), § "The binding evidence rule".
    - `.planning/phases/34.5-.../34.5-CYCLE6-ROUTING.md` § "Standing rules this cycle inherits" — Rules 1 and 4 in particular.
    - `.claude/skills/spike-findings-gamelib/SKILL.md` — the `navigator.clipboard` no-op, the WKWebView layout gotchas and the login-window constraints, so a known gotcha is not mis-recorded as a new finding.
  </read_first>
  <what-built>
    A `pnpm tauri:dev` build with all twelve preconditions satisfied, running on the `keyring` arm,
    carrying the seven cycle-6 fixes. Item 3's two call sites and item 4 have **never** produced a
    result of any kind in this phase's history — three runs, three silent skips or blockages.
  </what-built>
  <how-to-verify>
    Work the items in the contract's order — **1 GOG, 2 Amazon, 3 shortcuts, 4 `runWineCommand`**. For
    each, gather EVERY evidence clause the item lists, not a subset. There is no fifth item and no
    Epic placeholder; if one appears, the wrong contract is being run.

    **Item 1 (GOG login from scratch, populated library).** All of clauses (a)–(e) plus (f)–(j).
    The two clauses run 2's evidence lacked entirely are the `[useTauriOAuthLogin] runner=… phase=…`
    line with its exact terminal value and the library **populated and RENDERED** with the runner
    present in the Library filter options. Gate 3's own failure was here: 7 GOG titles persisted to
    `store_cache/gog_library.json` while the Library rendered nothing. **A persisted store file is not
    the rendered-library clause**, and an empty library after a "successful" login is a FAIL, not a
    partial pass. Clause (g) — zero `Refreshing undefined Library` lines and every `[refreshLibrary]`
    line carrying a non-`unknown` `origin=` — is plan 34.5-47's fix under test; gate 3 satisfied the
    first half and failed the second (F-34.5-G6-14). Clause (i) is the `(U-34.5-05, creating plan)`
    transcription: the login window's title-bar origin text **CHANGING within this single flow**,
    transcribed by eye. A log-observed `nav host=` change does not satisfy it.
    **Expect Keychain prompts on the `keyring` arm.** On gate 3 a `keyring_get` timed out at the 45s
    bound and produced zero successful reads. If one times out mid-login: record it as an observation,
    continue on the **same arm**, and never switch arms mid-run.

    **Item 2 (Amazon login from scratch, populated library).** The same (a)–(e), plus (k) the
    `www.amazon.com` host anchor — grep for `runner=nile origin-mismatch origin=`; it must NOT fire on
    a clean pass. If it fires, **Assumption A1 is FALSIFIED and item 2 cannot be a clean PASS even if
    the login otherwise succeeds.** Record the observed origin either way, using the contract's
    three-way CONFIRMED / FALSIFIED / UNTESTED recording rule. Clause (l) is the
    `(U-34.5-04, creating plan)` transcription: whether the verification-code field's text is legible
    (run 2 recorded black-on-black).

    **Item 3 (shortcuts — BOTH `exe` call sites).** Quit Steam FIRST. Exercise
    `nonesteamgame.ts:258` (the `shortcuts.vdf` `Exe` field, quoted with `userdata/<REDACTED>`) AND
    `shortcuts.ts:227` (`<title>.app/Contents/MacOS/run.sh`, body quoted, with **zero** `Error
    generating MacOS App` lines anywhere in the preserved log). Gate 3 wrote a byte-correct `Exe` at
    the first while **never reaching the second at all** — so proving one call site is not sufficient
    and `(U-34.5-02, creating plan)` retires only on a PASS covering both. Also required: `addToSteam`
    resolving to `true` with the GameSubMenu label flipping to "Remove from Steam" **without a
    reload**; and after clicking each shortcut, a `ps aux` showing the Tauri shell binary and never a
    bare `node`, exactly one `gamelib-shell`, and a `handleProtocol` line naming the GAME.

    **Item 4 (`runWineCommand` for a non-Steam runner) — ATTEMPT IT.** NOT ATTEMPTED on all three
    prior runs, so research Pitfall 2 has never actually been tested. Precondition 12's repointed
    target, the named Wine action, its UI path, its dispatching channel and registration kind, and its
    literal evidence lines are inlined in the contract; nothing should need discovering mid-run. **The
    live non-Steam Wine execution observed during the UAT (`wineboot --init`, `winepath`, a real
    `unins000.exe` run) is evidence for a future attempt and explicitly not a substitute for one.**
    If a login-independent fallback target is named in the contract, use it rather than abandoning the
    item — but record in the same breath what its PASS does and does not retire.

    **The three cross-cutting observations** — gathered, not skipped. A result here changes no verdict.
    - **Uninstall without restart (34.5-46):** does the Library tile stop presenting as installed
      without a restart, with `Finished uninstalling` and the tile state observed in the SAME session?
      `eb117d9e4` likely already covers it, and 34.5-46 found the original UAT observation was stale
      (observed at 00:33:24 against a bundle that built at 01:24:43). **A non-reproduction is not a
      retirement** — record it as a non-reproduction.
    - **Manage Accounts panel (34.5-47):** does it leave "logging into gog" and show the signed-in
      state without a restart? 34.5-47 found `WebView/index.tsx`'s `handleTauriOAuthSuccess` already
      navigates behind a `mountedRef` guard, shipped 2026-08-03, so the "no navigation" diagnosis was
      FALSE and this may be a **stale observation**. Record what is seen; retire nothing.
    - **SteamGridDB / winetricks honest-degradation sweep (34.5-48):** run the concrete sweep command
      the contract inlines from `34.5-PORTED-CHANNELS.md` — do not assert the claim. SteamGridDB is
      reachable for the first time now that 34.5-43 landed `getInstallInfo`. The 5 SteamGridDB and 3
      winetricks channels have the **identical shape** the EOS cluster had. **The marker machinery
      classifies a rejection; it does not declare anything to a user** — a deferred channel declares
      itself only where a call site catches and routes to `window.api.logError`.

    Recording rules, all five binding:
    - **Record what happened, not what should have happened.** Do not soften, do not round, do not
      describe a partial result as a pass.
    - **A mutating call's own report is never proof of its effect.** A backend `status=captured` is
      necessary and not sufficient — run 2 had it for two runners and both items still FAILED. Every
      clause must land an observable from a **different component** than the one under test.
    - **Record NEW observations WITHOUT diagnosing them.** Diagnosis belongs to the next cycle. All
      three prior runs followed this rule and all three produced actionable findings because of it.
    - **Do not claim a ledger row retired at the console.** Rows are resolved in Task 3 against the
      exact observation each row's own cell names, keyed by (id, creating plan).
    - **If an item genuinely cannot be attempted, record NOT ATTEMPTED with its specific reason** —
      not FAIL, not BLOCKED, and never silence. A fourth silent skip of item 4 is a criterion failure.
  </how-to-verify>
  <action>
    Walk the developer through items 1 through 4 in the contract's order, then the three cross-cutting
    observations, collecting every evidence clause each item names. Record results verbatim as they
    arrive, quoting only from the preserved log copy and redacting in place. Do not soften a FAIL, do
    not infer an unobserved clause, and do not diagnose any new observation. If the developer declines
    an item, record NOT ATTEMPTED with their stated reason.
  </action>
  <acceptance_criteria>
    - Exactly four item Result slots are filled, each reading PASS, FAIL, BLOCKED or NOT ATTEMPTED, with every evidence clause its own contract section names.
    - Item 1 carries clauses (a)–(j), including the exact terminal `phase=` value, a RENDERED populated library with the runner in the filter options, and the `U-34.5-05` title-bar transcription by eye.
    - Item 2 carries clauses (a)–(e), (k) with the three-way anchor recording, and (l) the code-field legibility transcription.
    - Item 3 carries evidence from BOTH `nonesteamgame.ts:258` and `shortcuts.ts:227`, or explicitly records which call site was not exercised and why; the `run.sh` body is quoted, `Error generating MacOS App` count is recorded, the `addToSteam` boolean and label flip are recorded, and `ps aux` plus a `handleProtocol` line are recorded per shortcut click.
    - Item 4 carries a real result — PASS, FAIL, or NOT ATTEMPTED with a specific stated reason. A fourth silent skip fails this criterion.
    - All three cross-cutting observations are recorded, each as an observation with no verdict weight, and any non-reproduction is labelled a non-reproduction rather than a retirement.
    - New observations appear in the contract's "New findings (this run)" section with no diagnosis attached.
    - Every quoted line is attributed to the preserved log copy and its session letter; no credential value appears in any pasted evidence.
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -cE "^\*\*Result:\*\* (PASS|FAIL|BLOCKED|NOT ATTEMPTED)" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md) -eq 4</automated>
  </verify>
  <resume-signal>Paste, per item, the result and every evidence clause the contract names, plus the three cross-cutting observations. Then type "items recorded".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Re-verify every pasted line against the preserved log, then record the verdict, the arithmetic, the falsification ledger and the redaction proof</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-3.md` — the whole file. Its Verdict table, falsification section, known-holes table and arithmetic rule are authoritative.
    - The preserved log copy `gamelib.log.34.5-g6-gate4-<sessionletter>` recorded at precondition 3 — **the file itself, not the operator's paste**.
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` § D-08 — the no-partial-pass rule, verbatim.
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` — every row's own "Retired by" cell, at its current state, with each row's creating plan.
    - `.planning/phases/34.5-.../deferred-items.md` item 23 — the standing redaction rule and the 37 already-published SteamID64 files that are expected sweep matches.
    - `.planning/phases/34.5-.../34.5-50-SUMMARY.md` — the `U-34.5-17` collision record and how 34.5-50 allocated it.
  </read_first>
  <action>
    **Step A — re-verify before recording.** For every line pasted into any Result slot, grep the
    **preserved log copy** for it and confirm it matches. Plan 34.5-20's SUMMARY set the precedent:
    it caught a claimed "4 legendary timeouts" that the log showed as **3**. If a pasted line
    disagrees with the file, **record the FILE's version and note the discrepancy explicitly** — do
    not quietly adopt either. Record in the SUMMARY how many lines were re-verified and how many
    discrepancies were found, including zero.

    **Step B — the Verdict table and frontmatter.** Fill the Verdict table with one row per item, its
    result and a one-line reason. Set frontmatter `status: complete`, `verdict`, `items_passed`,
    `items_failed`, `items_blocked`, `items_not_attempted`, the precondition counters, and
    `completed`.

    **Step C — the arithmetic, applied literally.** `items_passed + items_failed + items_blocked +
    items_not_attempted` must equal **4**. BLOCKED and NOT ATTEMPTED count toward neither passed nor
    failed. Any shortfall from 4 PASSes is **stated explicitly in `notes`** — as all three prior runs
    did for their own shortfalls — never quietly rounded to make the arithmetic look clean.

    **Step D — D-08, branched on the RECORDED counters and nothing else.**
    - **If `items_passed` is 4 AND `items_failed`, `items_blocked` and `items_not_attempted` are each
      0** — a genuine clean 4 of 4. `notes` names it as such, states D-08 is satisfied and Phase 34.5
      DOES close on this result, and names the PASS routing artifact Task 4 writes.
    - **In every other case** — any FAIL, any BLOCKED, any NOT ATTEMPTED, or fewer than 4 PASS —
      `notes` states plainly that Phase 34.5 does **NOT** close and that the result routes to **gap
      cycle 7 inside Phase 34.5**, naming the FAIL routing artifact Task 4 writes.
    This conditional is not a loosening of D-08 and not a partial-pass escape hatch: D-08 is unchanged
    and still binds, and no result short of 4 / 0 / 0 / 0 may be described anywhere as closing the
    phase. It removes one failure mode only — recording a false "does not close" over a 4-of-4 the run
    genuinely earned. **Never an advisory note, never a deferred UAT entry, never a pre-authorised
    override.**

    **Step E — resolve "What this gate falsifies".** Every standing claim is marked STRUCK or
    STANDING, and **nothing is struck without the corresponding ITEM passing** — not on the strength
    of a sub-clause being independently confirmed while its parent item fails. Run 2 had to draw
    exactly this distinction for Assumption A1 (confirmed at the mechanism level, claim still STANDING
    because its item failed); draw it again if it arises and spell it out so a future reader cannot
    conflate the two. **Research Pitfall 2 is CONTRADICTED BY DIRECT EVIDENCE but NOT STRUCK** — it is
    struck only by an item-4 PASS. A FAIL-by-blockage retires nothing.

    **Step F — resolve the known-holes table row by row, by (id, creating plan) pair.** For each row:
    RETIRED with the exact observation its own cell names, or STILL OPEN with why. **Ledger Rule 1:
    never by a passing suite and never by inference from a neighbouring row.** Specifically:
    `(U-34.5-01, …)` and `(U-34.5-10, …)` are retirable only if precondition 4 recorded the `keyring`
    arm AND their own observations were gathered; `(U-34.5-02, …)` only on an item-3 PASS covering
    **both** call sites; `(U-34.5-03, …)` only on an item-4 PASS; `(U-34.5-04, …)` and
    `(U-34.5-05, …)` only on their transcriptions. Never resolve a bare id — plan 34.5-50 recorded
    that `U-34.5-17` is claimed by four concurrently-authored plans, and a bare-id resolution would
    silently close the wrong row. Then propagate each resolution into `34.5-UNTESTED-ITEMS.md`'s own
    Status cells (retirements only; no row deleted, no preamble edit, no renumbering of another plan's
    committed row) and record any collision that Step F could not resolve as a numbered
    `deferred-items.md` finding.

    **Step G — "What this run nevertheless proved."** As all three prior runs did: distinguish what
    genuinely advanced from what did not, honestly, in both directions. Item 4 carrying a result of
    any kind is itself a first for this phase; say so if it happened, and say so just as plainly if it
    did not.

    **Step H — redact, then RE-GREP AFTER redacting.** Sweep the staged diff for `access_token`,
    `refresh_token`, cookie values, session identifiers, OAuth `code=` values and Steam `userdata`
    account ids. Redact **in place** (`code=<REDACTED>`, `userdata/<REDACTED>`) so the line's shape
    survives as evidence. Then **run the sweep AGAIN over the redacted text** and paste both outputs
    into the SUMMARY. The second sweep is not ceremony: this phase has committed a real Steam account
    id unredacted once and live PKCE material once, and in one case **the replacement text
    reintroduced the id** — a pre-redaction sweep would have passed and the commit would still have
    leaked. Per `deferred-items.md` item 23, the 37 already-published SteamID64 files are expected
    matches and are not re-raised; any NEW unredacted account id, token, cookie, session id or OAuth
    code **blocks the commit**.

    **Step I — the three prior gate documents are records.** `34.5-LIVE-GATE.md`,
    `34.5-LIVE-GATE-RERUN.md` and `34.5-LIVE-GATE-RERUN-2.md` are not edited, not overwritten and not
    deleted. Each is the completed record of its own failed run; anyone citing one as evidence of a
    PASS is citing the wrong document.
  </action>
  <acceptance_criteria>
    - Frontmatter `verdict` is `PASS` or `FAIL`; `items_passed`, `items_failed`, `items_blocked` and `items_not_attempted` are integers reconciling to 4, with the reconciliation stated in `notes`.
    - The SUMMARY records how many pasted lines were re-verified against the preserved log copy and every discrepancy found, with the file's version recorded as authoritative in each case.
    - The Verdict table has one row per item with its result and a one-line reason; all four rows present.
    - `notes` branches only on the four recorded counters. Where they are not 4 / 0 / 0 / 0 it states plainly that Phase 34.5 does not close and routes to gap cycle 7 inside 34.5. No result short of 4 / 0 / 0 / 0 is described anywhere as closing the phase, and no advisory-note, deferred-UAT or override wording appears.
    - Every standing claim is resolved STRUCK or STANDING; every STRUCK claim names the ITEM that passed to earn it; Pitfall 2 is STRUCK only if item 4 PASSed.
    - Every known-holes row is resolved by (id, creating plan) pair against its own named observation; `U-34.5-01`/`U-34.5-10` state the keyring-arm dependency explicitly; `U-34.5-02` is retired only on both call sites.
    - `34.5-UNTESTED-ITEMS.md` carries the propagated Status changes with zero deleted rows (`git diff --numstat` deletions attributable only to Status-cell edits, itemised in the SUMMARY).
    - The post-redaction re-grep output is pasted into the SUMMARY and contains zero new unredacted account ids, tokens, cookies, session ids or OAuth codes; both sweep outputs (before and after) appear.
    - `git status --porcelain` shows `34.5-LIVE-GATE.md`, `34.5-LIVE-GATE-RERUN.md` and `34.5-LIVE-GATE-RERUN-2.md` UNMODIFIED, and `git diff --name-only` matches zero `.ts`, `.tsx` or `.rs` paths.
  </acceptance_criteria>
  <verify>
    <automated>G=.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-3.md; grep -qE "^verdict: (PASS|FAIL)$" $G &amp;&amp; test $(grep -oE "userdata/[0-9]+" $G | wc -l) -eq 0 &amp;&amp; test $(grep -oE "(access_token|refresh_token|code)=[A-Za-z0-9._-]{6,}" $G | wc -l) -eq 0 &amp;&amp; test $(git status --porcelain | grep -Ec "34\.5-LIVE-GATE(-RERUN(-2)?)?\.md$") -eq 0</automated>
  </verify>
  <done>The fourth gate carries a computed four-way verdict whose every pasted line was re-verified against the preserved log, whose ledger rows are resolved by (id, creating plan) against their own named observations, and whose redaction was proven by a sweep run AFTER redacting.</done>
</task>

<task type="auto">
  <name>Task 4: Write the one routing artifact the verdict earned — PASS closure or gap cycle 7</name>
  <read_first>
    - `34.5-LIVE-GATE-RERUN-3.md` frontmatter as written by Task 3 — the four counters are the ONLY input to this branch.
    - `.planning/phases/34.5-.../34.5-CYCLE6-ROUTING.md` — the shape to mirror for a cycle routing document: the work-list by cluster, the "explicitly OUT of scope" table, and the "standing rules this cycle inherits" list.
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` § D-08.
    - `34.5-LIVE-GATE-RERUN-3.md` § "New findings (this run)" — the undiagnosed observations that become the next cycle's work-list.
  </read_first>
  <action>
    Branch **mechanically on the four counters Task 3 recorded**, never on an expectation held before
    the run. Write **exactly one** artifact; the other filename must not exist.

    **FAIL branch (anything other than 4 / 0 / 0 / 0)** — write
    `34.5-CYCLE7-ROUTING.md`. It must state: that Phase 34.5 does NOT close, quoting D-08 verbatim
    and quoting the four counters; that this routes to **gap cycle 7 inside Phase 34.5**, not to an
    advisory note, not to a deferred UAT entry, not to a pre-authorised override, and not to Phase
    34.6 or 35 (whose dependency lines stay untouched); a work-list built from the gate's own
    undiagnosed "New findings" plus every item that did not PASS, **each named by its existing
    finding or ledger id as an (id, creating plan) pair**; an "explicitly OUT of scope" table carrying
    forward the Epic descope to Phase 34.7, the 16 deferred channel ports to Phase 34.6, and
    `R-34.5-G1-PKG` to packaging; and the standing rules inherited from `34.5-CYCLE6-ROUTING.md`,
    Rule 1 first. **Diagnose nothing** — cycle 7's planning does that. Record observations as
    observations.

    **PASS branch (exactly 4 / 0 / 0 / 0)** — write `34.5-GATE4-PASS-ROUTING.md`. It must state: the
    four counters and that this is a genuine clean 4 of 4; that D-08 is satisfied and Phase 34.5
    closes; **what the PASS does NOT cover** — Epic (descoped to Phase 34.7, retiring nothing), the
    packaged half of Assumption A2 and `R-34.5-G1-PKG` (a dev PASS never retires them), the 16
    deferred channel ports (Phase 34.6), and every ledger row Task 3 recorded STILL OPEN, listed by
    (id, creating plan); and only then that Phase 34.6's and Phase 35's dependency lines become
    editable. A PASS that overstates its own reach is the failure mode Phase 34.4.1's 4/4 produced;
    the not-covered list is what prevents it.

    In both branches, keep the three prior gate documents untouched, and carry item 23's redaction
    rule into whatever is quoted.
  </action>
  <acceptance_criteria>
    - Exactly one of `34.5-CYCLE7-ROUTING.md` / `34.5-GATE4-PASS-ROUTING.md` exists, and which one exists matches the recorded counters: the PASS file exists if and only if `items_passed` is 4 and the other three counters are 0.
    - The written artifact quotes the four counters verbatim from the gate's frontmatter and quotes D-08.
    - On the FAIL branch: the artifact names gap cycle 7 inside Phase 34.5 as the destination, contains no advisory-note / deferred-UAT / override wording, carries a work-list of (id, creating plan) pairs with no diagnosis attached, and leaves Phase 34.6 and Phase 35 dependency lines unedited.
    - On the PASS branch: the artifact carries an explicit not-covered list naming the Epic descope, the packaged half of Assumption A2, `R-34.5-G1-PKG`, the 16 deferred channels, and every STILL OPEN ledger row by (id, creating plan).
    - `git status --porcelain` shows the three prior gate documents UNMODIFIED; `git diff --name-only` matches zero `.ts`, `.tsx` or `.rs` paths; the post-redaction sweep is re-run over the new artifact and its output pasted into the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>D=.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc; P=$(grep -E "^items_passed:" $D/34.5-LIVE-GATE-RERUN-3.md | awk '{print $2}'); F=$(grep -E "^items_failed:" $D/34.5-LIVE-GATE-RERUN-3.md | awk '{print $2}'); B=$(grep -E "^items_blocked:" $D/34.5-LIVE-GATE-RERUN-3.md | awk '{print $2}'); N=$(grep -E "^items_not_attempted:" $D/34.5-LIVE-GATE-RERUN-3.md | awk '{print $2}'); test $((P+F+B+N)) -eq 4 &amp;&amp; if [ "$P" = "4" ] &amp;&amp; [ "$F" = "0" ] &amp;&amp; [ "$B" = "0" ] &amp;&amp; [ "$N" = "0" ]; then test -f $D/34.5-GATE4-PASS-ROUTING.md &amp;&amp; test ! -f $D/34.5-CYCLE7-ROUTING.md; else test -f $D/34.5-CYCLE7-ROUTING.md &amp;&amp; test ! -f $D/34.5-GATE4-PASS-ROUTING.md; fi</automated>
  </verify>
  <done>The verdict has a single routing consequence on disk, computed from the counters rather than from an expectation, and a PASS cannot be read as covering more than it earned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live session → repository documents | Real GOG and Amazon session artefacts, Keychain reads and a Steam `userdata` path cross into a **public-fork** record |
| operator paste → recorded evidence | A human summary can drift from what the log says; run 2's evidence contained a miscount caught only by re-verification |
| the live `gamelib.log` → the cited log | The live file rotates mid-run; a citation from it can point at a file that no longer holds the run |
| the four recorded counters → phase closure | Phases 34.6 and 35 unblock on this verdict; a softened FAIL closes a phase that has failed four times |
| prior gate records → this run | Three completed records of failed runs sit under near-identical filenames; editing one destroys that run's evidence |
| the ledger's OPEN set → resolution | A row resolved by inference, or by a bare colliding id, retires an untested hole |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C6-40 | Information disclosure | operator-pasted log excerpts in the filled gate record | mitigate | Task 3 Step H sweeps for all six forbidden value classes, redacts IN PLACE so the line shape survives, and **re-greps AFTER redacting** with both outputs pasted into the SUMMARY. This phase leaked a real Steam account id once and live PKCE material once, and in one case the replacement text reintroduced the id — a pre-redaction sweep alone would have passed. The automated verify greps `userdata/[0-9]+` and `(access_token\|refresh_token\|code)=` in their unredacted forms only |
| T-34.5-C6-41 | Information disclosure | the preserved log sidecar file itself | mitigate | The preserved copy lives beside `~/Library/Logs/GameLib/`, **never inside the repository**; only redacted excerpts are committed. Precondition 3 records its path and `cmp` code, and the acceptance criteria require the filename pattern rather than the file's contents |
| T-34.5-C6-42 | Information disclosure | the Steam `userdata` account id in precondition 11 and item 3 | mitigate | Carried from `deferred-items.md` item 23 and `T-34.5-C5-06`: written `userdata/<REDACTED>`. The path STRUCTURE is the evidence; the id is not. The 37 already-published SteamID64 files are declared expected sweep matches so they cannot mask a genuine new leak |
| T-34.5-C6-43 | Repudiation | an operator summary adopted as fact | mitigate | Task 3 Step A re-verifies EVERY pasted line against the **preserved copy** and records the count plus every discrepancy, the file's version winning. Precedent: plan 34.5-20 caught a claimed "4 legendary timeouts" the log showed as 3 |
| T-34.5-C6-44 | Repudiation | a citation from a log that rotated mid-run | mitigate | Precondition 3's `cmp` byte-identity proof is required BEFORE any citation (F-34.5-G6-15); multi-session runs preserve and label per session |
| T-34.5-C6-45 | Repudiation | evidence gathered on the `dev-vault` arm credited to the keyring rows | mitigate | Precondition 4 verifies the ARM from the log, not the export. A `dev-vault` reading **stops and restarts the run**; it is NO evidence for `(U-34.5-01, …)`/`(U-34.5-10, …)`. Arms are never switched mid-run — gate 3 spanned two sessions on two arms and made half its evidence uninterpretable |
| T-34.5-C6-46 | Repudiation | a ledger row resolved by inference, or a bare colliding id resolving the wrong row | mitigate | Ledger Rule 1 is restated as an acceptance criterion; every resolution is an (id, creating plan) pair, because plan 34.5-50 recorded `U-34.5-17` claimed by four concurrently-authored plans |
| T-34.5-C6-47 | Tampering | a softened verdict closing a four-times-failed phase | mitigate | The arithmetic must reconcile to 4 with the shortfall stated; Task 3 Step D branches only on the four recorded counters; Task 4's automated verify recomputes the branch from the frontmatter and asserts the artifact on disk matches it, so a narrative "close" cannot outvote the numbers |
| T-34.5-C6-48 | Tampering | `34.5-LIVE-GATE.md` / `-RERUN.md` / `-RERUN-2.md` | mitigate | Tasks 3 and 4 each carry a `git status --porcelain` criterion and automated grep asserting all three are unmodified; a new filename is used throughout |
| T-34.5-C6-49 | Spoofing | credentials entered into an unverified origin in the embedded login window | mitigate | Item 1 requires the login window's title-bar origin transcribed by eye (`U-34.5-05`, the anti-phishing surface plan 34.5-27 landed); item 2 requires the `www.amazon.com` anchor to hold with a zero-count `origin-mismatch` grep (REQ-34.5-02, closing the inherited `T-34.4.1-44b`) |
| T-34.5-C6-50 | Elevation of privilege | a wrong `exe` value in a Steam VDF launch command | mitigate | Item 3 requires reading the written `Exe` field and the `run.sh` body back, plus a `ps aux` proving the launched process is the Tauri shell binary and never bare `node`, plus exactly one `gamelib-shell` and a `handleProtocol` line naming the game |
| T-34.5-C6-51 | Elevation of privilege | a "fix" applied opportunistically mid-run to rescue a failing item | mitigate | This plan authorises observation only. Acceptance criteria require `git diff --name-only` to match zero `.ts`, `.tsx` or `.rs` paths; a code change mid-run would make the gate a test of an unreviewed build |
| T-34.5-C6-52 | Elevation of privilege | precondition 11's remedy flipping this machine's global shortcut settings | accept | Local, operator-performed, reversible, and recorded in the precondition's RESULT slot; `fromMenu=true` is offered first. No code path is widened |
| T-34.5-C6-SC | Tampering | npm / pip / cargo installs (supply chain) | mitigate | **This plan installs no packages.** It rebuilds from existing lockfiles only. `34.5-RESEARCH.md`'s `## Package Legitimacy Audit` reads "Not applicable — no new packages" and stays true |
</threat_model>

<verification>
- All twelve preconditions carry recorded RESULT blocks, with precondition 4 reading `keyring` and precondition 3 recording a `cmp` exit code of 0 against a `gamelib.log.34.5-g6-gate4-*` copy.
- All four items carry a result; `items_passed + items_failed + items_blocked + items_not_attempted` reconciles to 4, with any shortfall stated in `notes`.
- Every pasted line was re-verified against the preserved log copy; the SUMMARY records the count and every discrepancy.
- Every known-holes row is resolved by (id, creating plan) against its own named observation, and the resolutions are propagated into `34.5-UNTESTED-ITEMS.md` without deleting a row.
- The post-redaction re-grep output is pasted into the SUMMARY and shows zero new unredacted account ids, tokens, cookies, session ids or OAuth codes.
- Exactly one routing artifact exists and matches the counters.
- `34.5-LIVE-GATE.md`, `34.5-LIVE-GATE-RERUN.md` and `34.5-LIVE-GATE-RERUN-2.md` are byte-unchanged; `git diff --name-only` matches zero `.ts`, `.tsx` or `.rs` paths.
</verification>

<success_criteria>
The fourth blocking gate has run on real macOS hardware on the verified `keyring` arm against a
byte-identical preserved log; all four items carry a recorded result with every evidence clause their
contract sections name; every pasted line was re-verified against the preserved log before being
recorded; the verdict is computed by the contract's own four-way arithmetic under D-08 with nothing
softened and no shortfall rounded; no standing claim or ledger row is retired without the exact
observation it names, resolved by (id, creating plan); the committed record carries no credential
value, proven by a sweep run AFTER redacting; and exactly one routing artifact exists — phase closure
on a clean 4 of 4, or gap cycle 7 inside Phase 34.5 on anything else.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-51-SUMMARY.md` when done
</output>
