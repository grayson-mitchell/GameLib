---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 49
type: execute
wave: 2
depends_on: [34.5-43]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PRELOAD-SURFACE-AUDIT.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py
  - .planning/IPC-PORT-INVENTORY.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
autonomous: true
gap_closure: true
requirements: [REQ-34.5-11]

must_haves:
  truths:
    - "The number of channels exposed in src/preload/ but absent from every bucket list in IPC-PORT-INVENTORY.md is stated as an explicit integer, and the method that produced it is re-runnable by a reader"
    - "Every unlisted channel is assigned to a named bucket — none is left unbucketed"
    - "The extraction is multi-line-aware, and the audit proves it by reporting what a naive single-line regex would have missed"
    - "The reverse direction is reported too: bucket-listed names with no preload exposure are named, not silently dropped"
    - "A gate script re-derives the surface from source at run time and fails when the inventory stops covering it"
    - "Every assertion in that gate is demonstrated to REJECT a known-bad input, with the demonstration output pasted"
    - "This plan buckets and ports nothing — no file under src/ is modified"
    - "D-01: each newly-discovered unlisted channel is bucketed case-by-case on its real usage — never by a blanket rule such as 'all unlisted go to 34.6'. D-01's case-by-case keep/drop judgement is exactly what a bucket assignment is, so every assignment must carry its own one-line justification."
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PRELOAD-SURFACE-AUDIT.md"
      provides: "the per-channel enumeration, the explicit unlisted count, the bucket assignment for each, and the reproducible method"
      contains: "Unlisted channel count"
      min_lines: 120
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py"
      provides: "an anti-rot gate that re-derives the preload surface from source and set-differences it against the inventory, with a --self-test proving each assertion rejects its bad input"
      contains: "--self-test"
    - path: ".planning/IPC-PORT-INVENTORY.md"
      provides: "bucket lines that now enumerate the full preload surface"
      contains: "34.5-PRELOAD-SURFACE-AUDIT.md"
  key_links:
    - from: "src/preload/api/*.ts makeHandlerInvoker / makeListenerCaller call sites"
      to: ".planning/IPC-PORT-INVENTORY.md bucket lines"
      via: "preload-surface-gate.py set-difference, re-derived from source at run time"
      pattern: "make(HandlerInvoker|ListenerCaller)"
    - from: ".planning/phases/34.5-.../34.5-PRELOAD-SURFACE-AUDIT.md"
      to: ".planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md"
      via: "one appended ledger row per newly-created untested hole (Ledger Rule 2)"
      pattern: "U-34\\.5-1[7-9]"
---

<objective>
Execute **D-CYCLE6-C** — the full preload-surface audit. Diff **every** channel exposed under
`src/preload/` against `.planning/IPC-PORT-INVENTORY.md`'s bucket lists, state the count of unlisted
channels **explicitly, zero or otherwise**, assign every one of them to a bucket, and pin the result
with an anti-rot gate whose every assertion is proven to reject its bad input.

Purpose — stated so it is not mistaken for tidying. `U-34.5-14`'s retirement condition, verbatim from
its own cell:

> A completed diff of every channel exposed in `src/preload/` against the inventory's three buckets,
> with the count of unlisted channels stated explicitly (zero or otherwise) and every one of them
> assigned to a bucket.

This is a **Phase 35 precondition**, not a 34.5 nicety. `IPC-PORT-INVENTORY.md`'s own header rule —
"Phase 35 must not run while any channel below is unported" — **cannot catch a channel it never
listed**. `ported-channels-gate.py` verifies only that the ported/dropped/deferred split reconciles
**internally**; it is structurally incapable of finding an omission. `getInstallInfo` is the one
omission we happened to trip over live, **not necessarily the only one**, and the inventory's own ⚠
paragraph says the extent is UNKNOWN and "must not be assumed to be one".

**Planning-time measurement — treat as a floor to be re-derived, not as a number to transcribe.** A
multi-line-aware extraction over `src/preload/` at planning time (2026-08-11, pre-34.5-43-execution
tree) found **217 distinct invoke/send channels**, of which **12 appear in no bucket line of
`IPC-PORT-INVENTORY.md`**. `getInstallInfo` is one of the 12 and is plan 34.5-43's to bucket, leaving
**11 for this plan**. The executor MUST re-derive this against the post-34.5-43 tree and report what
it actually measures; if the measurement disagrees with 11, **the measurement wins** and the
disagreement is recorded rather than reconciled away.

Output: `34.5-PRELOAD-SURFACE-AUDIT.md` (the enumeration and the method), a bucketed
`IPC-PORT-INVENTORY.md`, appended `34.5-UNTESTED-ITEMS.md` rows for any new hole, and
`preload-surface-gate.py` with a self-test.

**Ordering obligation.** This plan's bucketing output must be **committed before plan 34.5-50 authors
the gate-4 contract**, because that contract's Ledger-Rule-3 known-holes section enumerates the OPEN
rows this plan may create. 34.5-50 sits in wave 3 and depends on this plan; do not let this plan's
commit slip past it.

**This plan buckets and does NOT port.** A discovered channel becomes a bucket entry plus, where it
creates a new untested hole, a `34.5-UNTESTED-ITEMS.md` row in the **same commit** (Ledger Rule 2).
No file under `src/` or `src-tauri/` is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE6-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.planning/IPC-PORT-INVENTORY.md

**Run `graphify query "preload api channel exposure makeHandlerInvoker makeListenerCaller"` before
opening raw source.** The index is backend-weighted and will orient you but will not enumerate the
preload surface — for the enumeration you are reading and parsing `src/preload/` directly, which is
the point of the audit.

<interfaces>
**Everything below was established by reading and measuring the live tree at planning time. Trust
these over any count in `34.5-PLAN-OUTLINE.md`, which used a different (mention-based) counting
method and disagrees.**

**1. The exposure mechanism.** `src/preload/ipc.ts` defines exactly three factories:
- `makeListenerCaller<ChannelName extends keyof SyncIPCFunctions>(channel)` — line 17. **send** kind.
- `makeHandlerInvoker<ChannelName extends keyof AsyncIPCFunctions>(channel)` — line 38. **invoke** kind.
- `frontendListenerSlot<ChannelName extends keyof FrontendMessages>(channel)` — line 50. **push** kind
  (backend→frontend). `IPC-PORT-INVENTORY.md`'s own Method section says push relays are counted
  **outside** the tally, per the SEAM.md convention Phase 30 established.

Every call site passes a **string literal**, and the type parameter is always inferred — there is
**no** `makeHandlerInvoker<...>('x')` angle-bracket form anywhere in the tree. A regex written for
that form matches **zero** call sites and passes vacuously. This was verified at planning time.

**2. The naive-regex trap is REAL here, and measured.** A single-line regex
(`make(HandlerInvoker|ListenerCaller)\('name'\)`) finds **206** distinct channels. A multi-line-aware
extraction (the `(` and the literal on different lines, matched with `re.S` and whitespace-tolerant)
finds **217**. The under-count is **11 channels, 5.1%**. The multi-line call sites are concentrated
in exactly three modules — `humble.ts` (7 sites: lines 11, 22, 25, 28, 31, 38, 41), `steam.ts` (3
sites: 14, 21, 33) and `storeSearch.ts` (1 site: 5) — because Prettier wrapped the longer names. Any
extractor that is not multi-line-aware will silently drop those 11 and report a clean audit.

**3. The bucket-membership parse must be scoped to BUCKET LINES, not to any backtick in the
document.** Parsing every `` `name` `` anywhere in `IPC-PORT-INVENTORY.md` yields 255 names and makes
`getInstallInfo` look **listed** — because the ⚠ `F-34.5-G6-10` paragraph names it while explaining
that it is missing. That is a live vacuity trap in this exact document: **the prose that records an
omission would mask it.** Scoping to lines carrying **5 or more** backticked names finds exactly
**10 bucket lines** holding **210 distinct names**, which agrees with the document's own
`## Totals` → "Unique channels | 210". Use bucket-line scoping; state the rule in the audit.

**4. Planning-time measurement (pre-34.5-43 tree, 2026-08-11).**
- Distinct invoke channels: 157. Distinct send channels: 60. Union: **217**. No name is both.
- Distinct push (`frontendListenerSlot`) channels: **29**, of which **24** are absent from bucket
  lines — expected, since push relays are out-of-tally by the Method convention. They are reported,
  not bucketed.
- Per-module raw call-site counts (multi-line-aware): `misc.ts` 71, `helpers.ts` 42, `humble.ts` 26,
  `library.ts` 23, `steam.ts` 21, `settings.ts` 20, `wine.ts` 17, `menu.ts` 12, `downloadmanager.ts`
  8, `storeSearch.ts` 3, `zoom.ts` 3. **These are call sites including push slots, not distinct
  invoke/send channels — the two numbers differ and the audit must not conflate them.**
- **Unlisted (in preload invoke/send, in no bucket line): 12** —
  `connectivity-changed`, `frontendReady`, `get-connectivity-status`, `getAchievements`,
  `getDefaultSavePath`, `getGogDiscounts`, `getInstallInfo`, `getPlaytimeFromRunner`, `importGame`,
  `moveInstall`, `oauthCaptureLogin`, `runWineCommandForGame`.
  `getInstallInfo` is plan 34.5-43's; **11 remain for this plan** on a post-34.5-43 tree.
- **Reverse direction — 5 bucket-listed names with NO preload invoke/send exposure:**
  `getEpicGamesStatus`, `health`, `openPatreonPage`, `openReleases`, `openWikiLink`. None of them is
  exposed as a push slot either. `getEpicGamesStatus` already has a recorded disposition (routing
  file, out-of-scope table: "No frontend caller at all"). The other four do not. This direction is
  **not** `U-34.5-14`'s retirement condition and must not be allowed to expand scope — report it,
  do not chase it.

**5. Prior-art bucket decisions that already exist — do not re-litigate them, cite them.**
- `getDefaultSavePath` → **Phase 34.6**, per `34.5-PORTED-CHANNELS.md` correction 1 and
  `deferred-items.md` item 4. It has an owner; it was simply never written into a bucket line.
- `getInstallInfo` → **Phase 34.5 ported**, by plan 34.5-43 (wave 1). Already handled upstream.
- The 3 Zoom channels are permanently dropped (D-02); the 16 EOS/SteamGridDB/winetricks channels are
  Phase 34.6 (D-03/D-05). Those buckets exist and are available as destinations.

**6. The arithmetic baseline you inherit from plan 34.5-43 (wave 1, runs before you).** 34.5-43
changes the Phase 34.5 section heading from `(57 channels)` to `(58 channels)`, its reconciliation
sentence to `39 ported + 3 dropped + 16 deferred = 58`, and `## Totals` → "Unique channels" from 210
to 211. **Verify this against the tree you are handed rather than assuming it** — if 34.5-43's
numbers differ from what its plan predicted, the tree wins. Your own edits are on top of that.
34.5-43 also adds ledger row `U-34.5-16` and plan 34.5-44 (wave 1) adds `U-34.5-17`..`U-34.5-20`, so
**hardcode no id — re-read `34.5-UNTESTED-ITEMS.md` off disk and take the next free `U-34.5-NN`.**

**7. `ported-channels-gate.py` is NOT yours.** Plan 34.5-43 owns it this cycle. Your gate is a **new,
separate file**, `preload-surface-gate.py`, asserting a different invariant: that the inventory's
bucket lines **cover the real preload surface**. Do not edit, extend or import
`ported-channels-gate.py`. Read it for its self-test idiom only — specifically `ASSERTION_COUNT`, the
assertion registry, and `self_test()`'s runtime 1:1 equality between assertion count and case count,
which is the pattern your file must reproduce.

**8. `34.5-UNTESTED-ITEMS.md` is edited by three plans this cycle** — 34.5-43 (wave 1, adds
`U-34.5-16`), plan 34.5-46 (wave 2) and this one. Plan 34.5-46 mandates **append-only, distinct-row
edits with a `git diff --numstat` deletions-0 acceptance criterion**; adopt the identical discipline
so the two are mergeable on the sequential main tree (`use_worktrees=false`).

**9. File fences.** Do not edit any file under `src/` or `src-tauri/`. `src/frontend/**` belongs to
plans 34.5-47/48, `src/backend/sidecar/**` to 34.5-43/45/46, `src-tauri/src/main.rs` to 34.5-44. This
plan is an audit; a port discovered here is recorded, never performed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the real preload surface and write 34.5-PRELOAD-SURFACE-AUDIT.md</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PRELOAD-SURFACE-AUDIT.md</files>
  <read_first>
    - `src/preload/ipc.ts` lines 1-75 — the three factories, their `keyof` type parameters, and the fact that every call site passes a bare string literal with no explicit type argument. This is why an angle-bracket-form regex matches nothing.
    - `src/preload/api/humble.ts` lines 1-45, `src/preload/api/steam.ts` lines 1-40, `src/preload/api/storeSearch.ts` lines 1-15 — the eleven Prettier-wrapped multi-line call sites. Confirm them by eye before writing an extractor; they are the entire difference between 206 and 217.
    - `src/preload/api/misc.ts` lines 110-180 — commented-out `ipcRenderer.invoke('storeGet', ...)` / `ipcRenderer.send('storeNew', ...)` lines and a comment mentioning `makeHandlerInvoker`. These are the known-bad inputs a comment-blind extractor swallows. Your extractor must strip block and line comments before matching.
    - `.planning/IPC-PORT-INVENTORY.md` — the whole file. Specifically the `## Method (reproducible)` section (its rule that push relays and `rustInvoke` dialog channels are tallied outside), `## Totals` (19-25), all ten bucket lines, and the ⚠ `F-34.5-G6-10` paragraph, which names `getInstallInfo` in backticks and is exactly why any-backtick parsing is unsound.
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` row `U-34.5-14` (line 49) — the retirement condition this task's output must satisfy word for word.
    - `.planning/phases/34.5-.../34.5-CYCLE6-ROUTING.md` § D-CYCLE6-C and § "Explicitly OUT of scope" — the fence, and the existing dispositions for `getDefaultSavePath` and `getEpicGamesStatus`.
  </read_first>
  <action>
    Write a **multi-line-aware, comment-stripping** extractor and record it verbatim inside the audit
    document so a reader can re-run it and obtain the same set. Requirements on the extractor, each of
    which exists because the naive alternative demonstrably fails on this tree: strip `/* */` blocks
    and `//` line comments before matching; match across newlines between the factory name, its
    opening paren and the string literal; classify by factory into invoke / send / push; walk
    `src/preload/**/*.ts` excluding `__tests__`; and emit distinct sets, not occurrence counts.

    **Reconcile the extracted count against a manual count on at least one module before trusting
    it.** Use `helpers.ts` — hand-count its `makeHandlerInvoker`/`makeListenerCaller` call sites by
    reading the file and state that integer in the audit next to the extractor's figure for the same
    file. If they disagree, the extractor is wrong; fix the extractor, never the manual count. Record
    both numbers even when they agree — an unstated reconciliation is not a reconciliation.

    Parse bucket membership from `IPC-PORT-INVENTORY.md` by **bucket lines only** — lines carrying 5
    or more backticked names. State this rule in the audit **with its justification**: any-backtick
    parsing yields 255 names and makes `getInstallInfo` appear listed, because the ⚠ paragraph that
    exists to record its absence names it in backticks. Cross-check the parse by asserting the
    bucket-line name count equals the `## Totals` → "Unique channels" figure on the tree you are
    handed; if those disagree, say so in the audit rather than picking one.

    Compute and state, each as an explicit integer even when zero:
    (a) distinct invoke channels; (b) distinct send channels; (c) their union — the audited surface;
    (d) distinct push channels, declared **out of tally** with the Method-section rule quoted as the
    reason; (e) **the unlisted count** — union minus bucket-line names — which is the number
    `U-34.5-14` demands; (f) the reverse-direction count, bucket-listed names with no preload
    exposure, reported and explicitly declared out of `U-34.5-14`'s scope so it cannot expand this
    plan; (g) what a single-line-only regex would have found, and the delta, as evidence the
    extraction is not the naive one.

    **Enumerate at the defect's granularity — per channel, never per module — and set-difference.**
    The audit's table has one row per unlisted channel, not one row per module. This project has been
    bitten twice by the coarser-unit failure: a threat register whose ranges hid three threats with
    zero evidence behind correct arithmetic, and a per-file coverage map that hid seven vacuous
    `not.toMatch` assertions inside files it marked COVERED. A per-module summary would repeat it.

    For **each** unlisted channel, record in its own row: the exact source location
    (`src/preload/api/<file>.ts:<line>`); its kind (invoke or send); whether a handler is registered
    under `src/backend/sidecar/` (grep for `ipcMain.handle`/`ipcMain.on` with that channel name) and
    whether one is registered under `src/backend/` via `addHandler`/`addListener`; the resulting port
    status; and the **proposed bucket** with a one-line rationale. Do not decide port status from the
    channel's name or from this plan's prose — grep for it. Where a prior decision already exists,
    cite it rather than inventing a new one: `getDefaultSavePath` → Phase 34.6 per
    `34.5-PORTED-CHANNELS.md` correction 1 / `deferred-items.md` item 4.

    Flag, as a distinct column or marked subsection, any unlisted channel that **crosses a trust
    boundary carrying a filesystem path, a command string, or credential material** — these are
    findings, not mere bucket entries, and must be named as such (see `<threat_model>` T-34.5-C6-49-03
    for the specific candidates identified at planning time). Naming a channel here does not authorise
    fixing it; it authorises recording it so a later phase cannot claim it was never seen.

    Write the audit with YAML frontmatter matching this phase's document house style
    (`phase`, `plan: 49`, `type: audit`, `status`, `authored`, `authored_by`). Open with the
    `U-34.5-14` retirement condition quoted verbatim, so a reader can check the artifact against the
    condition without opening a second file. Close with a "What this audit does NOT prove" section
    stating plainly that it enumerates the surface **as exposed in preload** — it does not prove any
    channel works, does not prove the buckets' own port claims, and retires no ledger row other than
    `U-34.5-14`.
  </action>
  <verify>
    <automated>bash -c 'cd .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc && test -f 34.5-PRELOAD-SURFACE-AUDIT.md && grep -v "^#" 34.5-PRELOAD-SURFACE-AUDIT.md | grep -c "Unlisted channel count"'</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-PRELOAD-SURFACE-AUDIT.md` exists and contains the literal string `Unlisted channel count` followed by an explicit integer on the same line. A range, an approximation, or a word ("several", "a handful") fails this criterion.
    - The document quotes `U-34.5-14`'s retirement condition verbatim, including the clause "zero or otherwise".
    - The document states seven explicit integers: distinct invoke, distinct send, their union, distinct push, unlisted count, reverse-direction count, and the single-line-regex count. Each is a bare integer, not a range.
    - The document states BOTH the extractor's figure for `src/preload/api/helpers.ts` AND a hand-counted figure for the same file, and says explicitly whether they agree.
    - The unlisted table has exactly one row per unlisted channel — `grep -c '^| `' ` over that table's rows equals the stated unlisted count. A module-level summary in place of per-channel rows fails.
    - Every unlisted row's bucket cell is non-empty. `grep` the table for `| |` (an empty cell) returns nothing.
    - Every unlisted row cites a `src/preload/api/` path with a line number.
    - The document states the bucket-line parsing rule AND its justification, naming the ⚠ `F-34.5-G6-10` paragraph as the reason any-backtick parsing is unsound.
    - The document declares push channels out of tally and quotes the `## Method` section rule that makes them so.
    - The document declares the reverse-direction finding OUT of `U-34.5-14`'s scope.
    - The extractor source is embedded in the document, is comment-stripping, and is multi-line-aware. Running it on a clean checkout reproduces the stated sets — the SUMMARY records the run output verbatim.
    - `git diff --name-only` shows zero paths under `src/` and zero under `src-tauri/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Bucket every unlisted channel in IPC-PORT-INVENTORY.md and open ledger rows for the new holes</name>
  <files>.planning/IPC-PORT-INVENTORY.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</files>
  <read_first>
    - `.planning/IPC-PORT-INVENTORY.md` on the **post-34.5-43 tree** — re-read it, do not work from this plan's quoted figures. Specifically: `## Totals` (19-25); the `## Already ported` line (34); every phase bucket line; the `## Phase 34.5` heading and its reconciliation bullets (113-135); the ⚠ `F-34.5-G6-10` paragraph (160-171), which 34.5-43 amends and which this plan amends again; and `## Phase 34.6` (177-191).
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` — the preamble's three Rules and the exact column shape of rows `U-34.5-14` and `U-34.5-15`, plus whatever `U-34.5-16` looks like after 34.5-43 lands, so the appended rows match the table byte-shape.
    - `.planning/phases/34.5-.../34.5-PORTED-CHANNELS.md` correction 1, and `.planning/phases/34.5-.../deferred-items.md` item 4 — the existing recorded owner for `getDefaultSavePath`.
    - `.planning/phases/34.5-.../34.5-CYCLE6-ROUTING.md` § "Explicitly OUT of scope" — the table this task must not contradict, and the source of `getEpicGamesStatus`'s existing disposition.
  </read_first>
  <action>
    Add every unlisted channel from Task 1 to a bucket line in `.planning/IPC-PORT-INVENTORY.md`, in
    the section its measured port status and concern place it. **Bucketing into Phase 34.6 or Phase 35
    is permitted; leaving one unbucketed is not.** Preserve each bucket line's existing alphabetical
    ordering and its backtick-comma formatting exactly — `preload-surface-gate.py` (Task 3) parses
    these lines, and so does the reader.

    Where a channel is already registered on the sidecar, add it to `## Already ported` and adjust
    that section's stated count. Where it is unported and belongs to an existing deferred cluster, add
    it to `## Phase 34.6`. Where it is unported and belongs to none, create a clearly-titled new
    bucket section for late-discovered channels with an explicit owner phase — do not smuggle a
    late-discovered channel into an existing phase's list where it would silently inflate that
    phase's historical count. **Every count you touch, you restate**: update `## Totals`' "Unique
    channels", the ported/unported split, and any per-section channel count in a heading. Show the
    arithmetic in a dated bullet rather than leaving a reader to re-derive it.

    Amend — **never delete** — the ⚠ `F-34.5-G6-10` paragraph. After 34.5-43 it says the extent is
    UNKNOWN and must not be assumed to be one; this plan is the thing that **makes it known**. Rewrite
    the closing sentences to state the measured number, name `34.5-PRELOAD-SURFACE-AUDIT.md` as the
    artifact that established it, and state what the audit does and does not close: it closes the
    enumeration question, and it closes nothing about whether any enumerated channel works. Keep the
    historical record of how the gap was found — the paragraph is provenance, and deleting the warning
    because it was answered would recreate the blind spot `U-34.5-14` exists to record.

    Add a short `## Preload-surface coverage` section stating the invariant in one sentence — every
    channel exposed via `makeHandlerInvoker`/`makeListenerCaller` under `src/preload/` appears in
    exactly one bucket line — naming `preload-surface-gate.py` as its enforcement and the audit as its
    provenance, and stating that push (`frontendListenerSlot`) channels are out of tally by the
    `## Method` convention. This is what makes the header rule "Phase 35 must not run while any
    channel below is unported" mean something for the first time.

    Append new rows to `34.5-UNTESTED-ITEMS.md` per **Ledger Rule 2** — a plan creating a new untested
    hole adds its row in the same commit. **Re-read `34.5-UNTESTED-ITEMS.md` off disk and take the
    next free `U-34.5-NN`; hardcode no id** — 34.5-43 takes `-16`, 34.5-44 takes `-17`..`-20`, and
    34.5-45/46/48 also append this cycle. At minimum
    one row is required, and its Item cell must contain the literal marker phrase
    `unported channel declines visibly under Tauri` so the row is provable by its subject rather than
    by a bare id: whether each newly-bucketed **unported** channel is actually reached and
    rejected honestly under Tauri, or whether it fails silently the way the EOS cluster does — the
    UAT proved that a deferred channel can produce **zero** `[GAMELIB_UNPORTED_CHANNEL]` lines while
    the UI simulates success, so "it is bucketed as deferred" is not evidence that it declines
    visibly. Write the retirement condition as a **mechanically checkable live observation**, in the
    style of `U-34.5-01`'s four numbered conditions, not as prose. Do not retire any row from this
    task; `U-34.5-14` retires only once Task 3's gate is green and the audit exists, and you record
    that in Task 3.

    **Append-only discipline, mandatory** — plan 34.5-46 edits this same ledger in this same wave on
    the same tree. Add rows at the end of the table; change no existing row's text; delete no line.
    `git diff --numstat` for `34.5-UNTESTED-ITEMS.md` must show **0 deletions**. The one permitted
    exception is the preamble's row-count sentence ("**15 rows as of 2026-08-02**"), which is a
    counter, not a row — if you touch it, expect a non-zero deletion count on that single line and
    state in the SUMMARY exactly which line it was and why, so the exception cannot be used to hide a
    row edit. Prefer appending a new dated "Update" paragraph over rewriting the existing counter.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/graysonmitchell/Projects/GameLib && python3 -c "
import re,sys,pathlib
doc=pathlib.Path(\".planning/IPC-PORT-INVENTORY.md\").read_text().splitlines()
b=set()
for ln in doc:
    n=re.findall(r\"\`([A-Za-z0-9_.-]+)\`\",ln)
    if len(n)>=5: b|=set(n)
pat=re.compile(r\"\b(makeHandlerInvoker|makeListenerCaller)\s*\(\s*.([^.]+).\",re.S)
s=set()
for p in sorted(x for x in pathlib.Path(\"src/preload\").rglob(\"*.ts\") if \"__tests__\" not in str(x)):
    t=p.read_text(); t=re.sub(r\"/\*.*?\*/\",\"\",t,flags=re.S); t=re.sub(r\"^\s*//.*$\",\"\",t,flags=re.M)
    for k,c in pat.findall(t): s.add(c)
m=sorted(s-b); print(\"unlisted after bucketing:\",len(m),m); sys.exit(1 if m else 0)"'</automated>
  </verify>
  <acceptance_criteria>
    - The Task 2 verify command exits 0 — every preload invoke/send channel now appears in at least one bucket line. A non-zero exit prints the survivors by name.
    - `.planning/IPC-PORT-INVENTORY.md` contains the literal string `34.5-PRELOAD-SURFACE-AUDIT.md`.
    - A `## Preload-surface coverage` section exists and names `preload-surface-gate.py`.
    - The ⚠ `F-34.5-G6-10` paragraph still exists and now states an explicit integer for the measured omission count. `grep -c "F-34.5-G6-10" .planning/IPC-PORT-INVENTORY.md` is at least 1. A diff that deletes the paragraph fails this criterion.
    - `## Totals` → "Unique channels" equals the number of distinct names across all bucket lines. Verify by running the same bucket-line parse used above and comparing; state both integers in the SUMMARY.
    - Every count changed in the document is accompanied by a dated bullet showing the arithmetic. A changed integer with no stated derivation fails.
    - **The new row is proven by its SUBJECT, not by its id.** `grep -cF "unported channel declines visibly under Tauri" 34.5-UNTESTED-ITEMS.md` is exactly 1 (not merely ≥1 — an exact count also catches a duplicate row appended by a re-run); that hit is a table row whose "Created by" cell contains `34.5-49`, whose Status is `OPEN`, and whose retirement condition is written as mechanically checkable live conditions, not prose. Run the identical grep BEFORE writing the row, confirm **0**, and record both outputs in the SUMMARY — a bare `U-34.5-17`-or-higher id check would pass on a row another plan committed this cycle and cannot discriminate "this plan did its job" from "this plan did nothing". State the allocated id verbatim in the SUMMARY.
    - `git diff --numstat -- .planning/phases/34.5-*/34.5-UNTESTED-ITEMS.md` shows deletions of 0, OR shows exactly 1 deletion which the SUMMARY identifies by line content as the preamble row counter.
    - `git diff --name-only` shows zero paths under `src/` and zero under `src-tauri/`, and does not list `ported-channels-gate.py` (plan 34.5-43 owns it) or any `src/backend/sidecar/` file.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Ship preload-surface-gate.py with a --self-test proving every assertion rejects its bad input</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py</files>
  <read_first>
    - `.planning/phases/34.5-.../ported-channels-gate.py` — read for its **idiom only**, not to edit: the assertion registry, `ASSERTION_COUNT`, `self_test()`'s runtime 1:1 equality between assertion count and self-test case count, the synthetic-document builders (`_valid_synthetic_row`, `_valid_synthetic_inventory_doc`) and `main()`'s summary print. Reproduce this structure in the new file so the two gates read alike and a future editor cannot weaken one by habit.
    - `.planning/phases/34.5-.../34.5-PRELOAD-SURFACE-AUDIT.md` (Task 1's output) — the extractor the gate must reuse, and the exact figures it must be able to reproduce.
    - `src/preload/api/humble.ts` lines 11-45 — the multi-line call sites. The gate's own extractor must find these, and one self-test case must prove it would fail if they were missed.
  </read_first>
  <action>
    Create `preload-surface-gate.py` as a **new, standalone** file in the phase directory. Do not edit
    or import `ported-channels-gate.py`; it asserts a different invariant and belongs to plan 34.5-43
    this cycle. This gate asserts one thing `ported-channels-gate.py` structurally cannot: that
    `IPC-PORT-INVENTORY.md`'s bucket lines **cover the real preload surface**, re-derived from source
    at run time rather than from any transcribed list.

    Implement at minimum these checks, each independently registered so `--self-test` can target it
    one at a time:
    1. **Coverage** — every distinct invoke/send channel extracted from `src/preload/` appears in at
       least one bucket line. Fails naming the survivors.
    2. **Multi-line awareness** — the extractor finds the known Prettier-wrapped call sites; assert
       the extracted count is at least the audited figure, so a regression to a single-line regex
       (which loses 11) trips it.
    3. **Comment blindness** — the extractor ignores commented-out call sites; `misc.ts`'s
       commented `ipcRenderer.invoke('storeGet', ...)` block is the live specimen.
    4. **Bucket-line scoping** — bucket membership is parsed only from lines with 5 or more
       backticked names, so a channel named solely in a warning paragraph does not count as listed.
       This check is the one that would have caught `getInstallInfo` masquerading as listed.
    5. **Totals reconciliation** — the distinct bucket-line name count equals `## Totals`' "Unique
       channels" integer.
    6. **Provenance** — the inventory still names `34.5-PRELOAD-SURFACE-AUDIT.md` and still carries
       the `F-34.5-G6-10` marker, so the record of how the omission was found cannot be quietly
       deleted once it stops being embarrassing.

    Give the file a `--self-test` mode. **Every assertion must have at least one case proving it
    REJECTS a known-bad input**, constructed by mutating a synthetic valid fixture — not by asserting
    a pattern happens to match. Reproduce `ported-channels-gate.py`'s runtime 1:1 equality between
    `ASSERTION_COUNT` and the self-test case count, and satisfy it by adding cases, never by editing
    the equality. Print, on success, a line naming both integers, in the same shape
    `ported-channels-gate.py` prints.

    **A gate whose assertions cannot fail is worse than no gate.** This project shipped seven vacuous
    `not.toMatch` assertions inside files a coverage map marked COVERED, and an unmatchable regex
    passes vacuously — which is precisely how the angle-bracket-form regex would behave against this
    tree, matching zero call sites and reporting a clean surface. The self-test is the discriminator
    against that class, and it only counts if its output is **pasted into the SUMMARY**, not claimed
    to pass.

    Prove the coverage check RED against the **real** tree, not only against a synthetic: temporarily
    remove one channel name from a real bucket line in `IPC-PORT-INVENTORY.md`, run the gate, confirm
    it exits non-zero and names that exact channel, restore the line, confirm exit 0. Record both
    outputs verbatim in the SUMMARY. A synthetic-only proof leaves open the possibility that the real
    parse never reaches the assertion at all.

    Add a header docstring stating the invariant, naming `U-34.5-14` and D-CYCLE6-C as provenance,
    and stating in its own words that **weakening an assertion to make this file pass defeats the
    reason it exists** — the same standing instruction `ported-channels-gate.py` carries.

    Finally, record the `U-34.5-14` disposition. This plan's three artifacts — the completed diff, the
    explicit unlisted count, and every unlisted channel bucketed — are that row's stated retirement
    condition in full. Update the row in `34.5-UNTESTED-ITEMS.md` **only if** all three exist and the
    gate is green, citing the audit file and the gate's passing output as the observation. Per **Ledger
    Rule 1** a row retires only by the observation named in its own cell: `U-34.5-14`'s cell names a
    completed diff, not a live session, so this retirement is legitimate — and it is the **only** row
    this plan may retire. Retiring it does not touch `U-34.5-15`, `U-34.5-16`, or any row created by
    Task 2, and it makes no claim that any enumerated channel works. State that boundary explicitly in
    the retired row's own cell so a later reader cannot widen it.
  </action>
  <verify>
    <automated>bash -c 'cd .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc &amp;&amp; python3 preload-surface-gate.py &amp;&amp; python3 preload-surface-gate.py --self-test'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 preload-surface-gate.py` exits 0 and prints the measured distinct invoke, send, union, push and unlisted integers — the last being 0.
    - `python3 preload-surface-gate.py --self-test` exits 0 and prints a line naming both the assertion count and the self-test case count, asserting they are equal 1:1.
    - `grep -v '^#' preload-surface-gate.py | grep -c "ASSERTION_COUNT"` is at least 2 (the definition and the runtime equality check).
    - Every registered assertion has at least one self-test case. The script itself enforces this at runtime and exits non-zero if it does not hold — confirm by temporarily incrementing `ASSERTION_COUNT` by one without adding a case, observing a non-zero exit, then reverting. Record both outputs in the SUMMARY.
    - The real-tree RED proof is recorded: the verbatim non-zero output naming the removed channel, and the verbatim exit-0 output after restoring. A self-test-only proof does not satisfy this criterion.
    - The extractor inside the gate reproduces Task 1's audited figures exactly. If it does not, the discrepancy is investigated and stated in the SUMMARY — never averaged, rounded, or reconciled by adjusting the audit.
    - The gate file's header docstring names `U-34.5-14` and `D-CYCLE6-C` and carries the do-not-weaken instruction.
    - `34.5-UNTESTED-ITEMS.md` row `U-34.5-14` reads `RETIRED` with the date, citing `34.5-PRELOAD-SURFACE-AUDIT.md` and the gate's passing output, and its cell explicitly states that retirement covers enumeration only and makes no claim about any channel working.
    - `git diff --name-only` lists no file outside this plan's `files_modified`; specifically zero paths under `src/`, zero under `src-tauri/`, and not `ported-channels-gate.py`.
    - `python3 ported-channels-gate.py` and `python3 ported-channels-gate.py --self-test` both still exit 0 — plan 34.5-43's gate is untouched and unbroken by the inventory edits made in Task 2.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → preload → sidecar/main | Every channel enumerated by this audit is a renderer-reachable entry point; the audit's output IS the map of that boundary |
| audit document → public fork repository | `34.5-PRELOAD-SURFACE-AUDIT.md` is a complete, machine-readable enumeration of the application's IPC attack surface, committed to a public repo |
| planning documents → Phase 35 completion gate | `IPC-PORT-INVENTORY.md`'s bucket lines are the gate deciding whether the Electron cutover may run; a name missing from them is a channel the gate cannot see |
| gate script → future editors | An assertion that cannot fail silently converts this gate into decoration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C6-49-01 | Information disclosure | `34.5-PRELOAD-SURFACE-AUDIT.md` publishing a complete IPC entry-point map in a public fork | accept | The channel names are already public: `src/preload/api/*.ts` ships in the repo and in the shipped bundle, and any user can enumerate `window.api` from devtools. The audit adds organisation, not access. The mitigation is a real one and applies to the CONTENT: Task 1 forbids pasting argument VALUES, real `appName`s, real paths, tokens or account identifiers into the document — names and source locations only. Enumerating what exists is safe; publishing a worked exploitation path is not |
| T-34.5-C6-49-02 | Information disclosure | a channel NAME leaking sensitive structure through the audit | accept | Reviewed at planning time against the 12 unlisted names. None encodes a secret. Two encode capability rather than data (`runWineCommandForGame`, `oauthCaptureLogin`) and are covered by T-34.5-C6-49-03. Note the already-listed `getPrivateBranchPassword`/`setPrivateBranchPassword` pair names a secret in its channel name — pre-existing, out of this plan's scope to rename, recorded here so it is not rediscovered as new |
| T-34.5-C6-49-03 | Elevation of privilege | unlisted channels that cross the renderer boundary carrying a path, a command string or credential material, entering the record as bucket entries with no security note | mitigate | Task 1 requires these be flagged as **findings**, not mere bucket rows. Candidates identified at planning time: `runWineCommandForGame` (renderer-supplied command string reaching a Wine process), `moveInstall` and `importGame` (renderer-supplied filesystem paths — and `path.join` is not containment, a lesson this project already recorded in Phase 18), `getDefaultSavePath` (discloses install-path structure), `oauthCaptureLogin` (carries an OAuth `code=` and redirect URL — live credential material, and this phase has already committed live PKCE material once). Flagging authorises recording only; no fix is performed here |
| T-34.5-C6-49-04 | Repudiation | the ⚠ `F-34.5-G6-10` paragraph being deleted once the audit answers it | mitigate | Task 2 forbids deletion and requires the amended paragraph to retain the `F-34.5-G6-10` marker and state the measured integer. Gate check 6 independently asserts both the marker and the audit filename remain present, with a self-test case proving it rejects their removal. The paragraph is provenance: deleting it because it stopped being embarrassing recreates exactly the blind spot `U-34.5-14` records |
| T-34.5-C6-49-05 | Tampering | a future editor adding a preload channel without adding it to a bucket line — the original defect, recurring | mitigate | `preload-surface-gate.py` re-derives the surface **from source at run time**, so it fails on the next unlisted channel rather than reconciling a transcribed list against itself. This is the specific structural gap that made `ported-channels-gate.py` incapable of finding `getInstallInfo` |
| T-34.5-C6-49-06 | Tampering | the gate's own extractor silently regressing to a naive single-line or comment-blind regex, reporting a clean surface while missing 11 channels | mitigate | Dedicated checks 2 and 3 with their own self-test cases, plus a mandatory real-tree RED proof. Measured at planning time: the single-line form finds 206 of 217, and the angle-bracket form finds **zero** and would pass vacuously |
| T-34.5-C6-49-07 | Tampering | a bucket entry created here being read later as evidence the channel is ported or works | mitigate | Task 1's closing "What this audit does NOT prove" section and Task 3's constrained `U-34.5-14` retirement cell both state the boundary explicitly. Task 2 opens a ledger row for the honest-decline question, because the UAT proved a deferred channel can produce **zero** `[GAMELIB_UNPORTED_CHANNEL]` lines while the UI simulates success |
| T-34.5-C6-49-08 | Denial of service | this plan's inventory edits colliding with plan 34.5-43's on the sequential main tree | mitigate | `depends_on: [34.5-43]` serialises them; Task 2 requires re-reading the post-43 tree rather than working from this plan's quoted baseline; Task 3 re-runs `ported-channels-gate.py` to prove 43's gate still passes after 49's edits |
| T-34.5-C6-49-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages — the extractor and the gate use the Python 3 standard library only (`re`, `pathlib`, `sys`, `argparse`), matching `ported-channels-gate.py`'s existing dependency profile. `34.5-RESEARCH.md`'s `## Package Legitimacy Audit` reads "Not applicable — no new packages" and stays true. Any install would require slopcheck plus a blocking human checkpoint before the install command runs |
</threat_model>

<verification>
- `python3 preload-surface-gate.py` and `python3 preload-surface-gate.py --self-test` both exit 0, with the self-test's full output pasted into the SUMMARY.
- `python3 ported-channels-gate.py` and `python3 ported-channels-gate.py --self-test` both still exit 0 — plan 34.5-43's gate must survive this plan's inventory edits untouched.
- `npm run test:ci` exits 0 and its verbatim `Tests:` line is recorded. This plan changes no source, so any delta against the phase's last recorded count is a signal that something outside the fence was touched — investigate rather than absorb it.
- `git diff --name-only` lists exactly the four files in `files_modified`, and specifically zero paths under `src/`, zero under `src-tauri/`, and not `ported-channels-gate.py`.
- `git diff --numstat -- .planning/phases/34.5-*/34.5-UNTESTED-ITEMS.md` shows 0 deletions, or exactly 1 identified in the SUMMARY as the preamble row counter.
- The audit's stated unlisted count and the gate's re-derived count agree. If they do not, the SUMMARY states both and explains the discrepancy — it is never averaged away.
- No secret material appears in any file this plan writes: no `access_token`, `refresh_token`, cookie value, session identifier, OAuth `code=` value, PKCE verifier, real `appName`, or Steam `userdata` account id. Channel NAMES and source LOCATIONS only.
</verification>

<success_criteria>
`U-34.5-14`'s retirement condition is satisfied in full and by its own words: a completed diff of every
channel exposed in `src/preload/` against `IPC-PORT-INVENTORY.md`'s bucket lists exists as
`34.5-PRELOAD-SURFACE-AUDIT.md`; the count of unlisted channels is stated as an explicit integer,
enumerated **per channel** rather than per module; every one of them is assigned to a named bucket;
and `preload-surface-gate.py` re-derives the surface from source so the omission class cannot recur —
with every one of its assertions demonstrated to reject a known-bad input, including a RED proof
against the real tree, both outputs pasted.

The extent of the inventory's incompleteness moves from **UNKNOWN** to a measured integer, and the ⚠
`F-34.5-G6-10` paragraph survives to record how it was found. The `## Preload-surface coverage`
invariant makes the inventory's header rule — "Phase 35 must not run while any channel below is
unported" — mean something for the first time, because the set it quantifies over is now provably the
real one.

**What this plan does NOT establish.** It ports nothing, fixes nothing, and proves nothing about
whether any enumerated channel works under Tauri. `U-34.5-14` is the only row it may retire, on the
strength of a completed diff — which is exactly the observation that row's own cell names. Every other
row stays exactly as it was, and the new rows this plan opens retire only on their own live
observations. A green gate here is necessary and never sufficient: this phase's live gate has beaten a
green suite three times.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-49-SUMMARY.md` when done
</output>
</content>
</invoke>
