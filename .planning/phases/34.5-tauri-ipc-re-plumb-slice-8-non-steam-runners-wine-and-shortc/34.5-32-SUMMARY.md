---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 32
subsystem: planning-records
tags: [gap-cycle-4, routing, ledger, halt-documentation]
dependency-graph:
  requires: []
  provides:
    - "34.5-CYCLE4-ROUTING.md — disposition of halted plans 29/30/31 and the cycle-4 scope map"
    - "34.5-UNTESTED-ITEMS.md — standing ledger of explicitly-untested items"
  affects:
    - "future cycle-4 plans 34.5-33..37 (each routes here)"
    - "any future gate-authoring decision (must read the ledger)"
tech-stack:
  added: []
  patterns:
    - "pre-registered contract discipline extended to routing documents (BINDING DECISION quoted verbatim, not paraphrased)"
key-files:
  created:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE4-ROUTING.md
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
  modified: []
decisions: []
metrics:
  duration: "~25 minutes"
  completed: 2026-08-01
---

# Phase 34.5 Plan 32: Cycle-4 routing + standing untested-items ledger Summary

Two new planning-record documents: `34.5-CYCLE4-ROUTING.md` records that plans 34.5-29/30/31 are
halted by `BINDING DECISION: fix-first` and maps all six discriminator Routing scope items to
cycle-4 plans 34.5-33 through 34.5-37; `34.5-UNTESTED-ITEMS.md` opens a standing ledger of six
explicitly-untested items, each retirable only by a named live observation.

## What Was Built

**Task 1 — `34.5-CYCLE4-ROUTING.md`.** Quotes `BINDING DECISION: fix-first` verbatim from
`34.5-G6-EPIC-DISCRIMINATOR.md` § `## Routing`. Documents plan 34.5-29 as halted by TWO independent
gates — its own objective-level self-halt instruction ("STOP IMMEDIATELY. Do not run Task 1, 2 or
3") and, separately, its Task 1's `R1-FALSIFIED` verdict branch ("make NO change to `USER_AGENTS`")
— and states that its Tasks 2/3 (the item-4/5 preflight) are deliberately deferred to the
gate-authorising cycle rather than lost, because a preflight written now would go stale again before
that gate runs. Documents 34.5-30 as halted-not-authored (it would have written
`34.5-LIVE-GATE-RERUN-2.md`) and 34.5-31 as halted-not-run (it would have executed that contract).
Maps all six Routing scope items — GOG library never lands, `Refreshing undefined Library`, keyring
race, capture-without-propagation race, Epic further diagnosis, and the developer-scoped dev secret
vault — to plans `34.5-33` through `34.5-37` across two waves, each with a stated live observation
that would prove it. Closes with an explicit "what this cycle does NOT do" list and the standing
green-suite-is-not-proof rule.

**Task 2 — `34.5-UNTESTED-ITEMS.md`.** Opens with the Phase 34.4.1 struck-precondition/domain-scoping
precedent as the reason this ledger exists. Seeds six rows (`U-34.5-01` through `U-34.5-06`):
Keychain-path-unproven while the dev secret vault (plan 34.5-36) is active; gate item 4 (`addToSteam`
+ macOS `.app` shortcut, both `exe` call sites) never attempted; gate item 5 (`runWineCommand`) never
attempted; Amazon verification-code field legibility not reached; login-window origin display
changing WITHIN a single flow not transcribed (per-page display is confirmed, the change itself is
not); Epic's success path never reached (R1 falsified, R2 unconfirmed). Each row's "Retired by" cell
names a concrete log line, transcription, or gate item — never "tests pass." Closes with three rules,
including that a passing test suite never retires a row.

## Deviations from Plan

None — plan executed exactly as written. Both tasks wrote new files only; no existing plan, summary,
or gate document was touched.

## Authentication Gates

None encountered — this plan writes only planning-record markdown, no code, no live runs.

## Verification

- `34.5-CYCLE4-ROUTING.md` automated verify: PASS (contains `BINDING DECISION: fix-first`; all of
  `34.5-33`..`34.5-37` present; `git diff --name-only` against `34.5-29/30/31-PLAN.md` is empty).
- `34.5-UNTESTED-ITEMS.md` automated verify: PASS (contains `KEYCHAIN PATH UNPROVEN`; all of
  `U-34.5-01`..`U-34.5-06` present; 6+ `OPEN` occurrences).
- Plan-level `<verification>`: `git status --porcelain` on the phase directory shows exactly the two
  new files added, zero modified files. No `.ts`/`.tsx`/`.rs` file appears in this plan's commits.
  No `34.5-LIVE-GATE-RERUN-2.md` exists anywhere in the repository after this plan.

## Self-Check: PASSED

- FOUND: `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE4-ROUTING.md`
- FOUND: `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md`
- FOUND: commit `ea25984a2` in `git log --oneline --all`
- FOUND: commit `0646f1d61` in `git log --oneline --all`

## Next Phase Readiness

Cycle 4's remaining five plans (`34.5-33` through `34.5-37`, two waves) are unblocked to proceed —
each has a named live observation in the scope map above. The blocking five-item gate remains NOT
authored and NOT scheduled; plans 34.5-29/30/31 stay halted in place as the record of their own
authoring/non-execution. `34.5-UNTESTED-ITEMS.md` is open for any future plan that creates a new
untested hole to append its own row in the same commit.
