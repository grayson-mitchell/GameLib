---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 07
type: execute
wave: 2
depends_on: ["34.5-04"]
files_modified:
  - src/backend/sidecar/runnerMiscFlowRegistration.ts
  - src/backend/sidecar/__tests__/runnerMiscFlows.test.ts
autonomous: true
requirements: [REQ-34.5-06, REQ-34.5-09]

must_haves:
  truths:
    - "The four runner CLI version channels are ported — `getLegendaryVersion`, `getGogdlVersion`, `getNileVersion`, `getCometVersion` — as pure version-probe reads with no seam dependency."
    - "D-04 is recorded in source: `getCometVersion` is GOG's, not Zoom's. `comet` is the GOG Galaxy Communication replacement, gated on `gameInfo.runner === 'gog'` at `launcher.ts:973`. An option label during the phase discussion wrongly bundled it with Zoom; Zoom is exactly 3 channels, so the drop candidate set was 19, not 20."
    - "The two runtime channels are ported — `downloadRuntime`, `isRuntimeInstalled` — pure Node `axiosClient` download plus tar extract to `runtimePath` (`toolsPath/runtimes`), which resolves through `pathShim`'s ALREADY-implemented `userData`. No wave-1 precondition."
    - "All six are `addHandler`/invoke; this half of the misc module registers zero send channels."
    - "Curated imports only: `utils/ipc_handler.ts` and `wine/runtimes/ipc_handler.ts` are never imported — `utils/ipc_handler.ts` also registers `abort`, `getSystemInfo`, `copySystemInfoToClipboard` and `hasExecutable`, which belong to already-completed slices and must not be double-registered."
  artifacts:
    - path: "src/backend/sidecar/runnerMiscFlowRegistration.ts"
      provides: "The 4 version-probe channels and the 2 runtime channels"
      contains: "getCometVersion"
    - path: "src/backend/sidecar/__tests__/runnerMiscFlows.test.ts"
      provides: "Bidirectional registration-kind cross-check for this cluster"
  key_links:
    - from: "src/backend/sidecar/runnerMiscFlowRegistration.ts"
      to: "src/backend/utils/helperBinaries.ts"
      via: "curated named imports of the four version functions, never utils/ipc_handler.ts"
      pattern: "helperBinaries"
    - from: "src/backend/sidecar/runnerMiscFlowRegistration.ts"
      to: "src/backend/wine/runtimes/runtimes.ts"
      via: "curated named imports of download/isInstalled, never wine/runtimes/ipc_handler.ts"
      pattern: "runtimes"
---

<objective>
Port the six channels in this slice that have no seam precondition at all: the four runner CLI
version probes and the two Wine-runtime channels.

Purpose: they are independent, cheap and unblock nothing else — which makes them the right work to
run in parallel with the auth and Wine clusters rather than queued behind them. They also carry
D-04's correction, which needs to land somewhere on the record in source.

Output: six invoke channels in the misc module, with the version/runtime half done and the
other five channels left to plan 34.5-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-RESEARCH.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md

<interfaces>
Electron registration ground truth (verified by grep):
  src/backend/utils/ipc_handler.ts:18  addHandler('getLegendaryVersion', getLegendaryVersion)
  src/backend/utils/ipc_handler.ts:19  addHandler('getGogdlVersion',     getGogdlVersion)
  src/backend/utils/ipc_handler.ts:20  addHandler('getCometVersion',     getCometVersion)
  src/backend/utils/ipc_handler.ts:21  addHandler('getNileVersion',      getNileVersion)
  src/backend/wine/runtimes/ipc_handler.ts:4  addHandler('downloadRuntime',    async (e, runtime_name) => download(runtime_name))
  src/backend/wine/runtimes/ipc_handler.ts:6  addHandler('isRuntimeInstalled', async (e, runtime_name) => isInstalled(runtime_name))

Curated import targets (verified export sites):
  src/backend/utils/helperBinaries.ts → getCometVersion, getGogdlVersion, getLegendaryVersion, getNileVersion
  src/backend/wine/runtimes/runtimes.ts:92 export block → download, isInstalled

What must NOT be imported (they register channels owned by other slices or by Phase 34.6):
  utils/ipc_handler.ts        → also registers abort, getSystemInfo, copySystemInfoToClipboard, hasExecutable
  wine/runtimes/ipc_handler.ts → nothing extra today, but the curated-import rule is unconditional
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the four runner CLI version channels and the two runtime channels</name>
  <files>src/backend/sidecar/runnerMiscFlowRegistration.ts</files>
  <read_first>
    - src/backend/sidecar/runnerMiscFlowRegistration.ts (the declared contract from plan 34.5-04 — its docstring lists all 11 channels this module will eventually own; this plan fills 6 of them)
    - src/backend/utils/ipc_handler.ts L1-30 (the four version registrations and where their functions come from — note the file also imports `clipboard` from `electron` at line 1, which is exactly why it must not be side-effect-imported)
    - src/backend/utils/helperBinaries.ts (the four version functions' signatures and what they actually shell out to)
    - src/backend/wine/runtimes/runtimes.ts (the `download`/`isInstalled` implementations and the `runtimePath` they write to — confirm it derives from `toolsPath`, which resolves through `pathShim`'s already-implemented `userData`, so this cluster has no wave-1 dependency)
    - src/backend/launcher.ts L965-985 (the `gameInfo.runner === 'gog'` gate around comet — the evidence for D-04's correction that `getCometVersion` is GOG's)
    - src/backend/sidecar/settingsFlowRegistration.ts (the grab-bag module shape: several small independent handlers in one file)
  </read_first>
  <action>
    Add six `ipcMain.handle` registrations to `registerRunnerMiscFlows()`.

    The four version probes register the imported function directly, exactly as the Electron source
    does — `ipcMain.handle('getLegendaryVersion', ...)` delegating to `getLegendaryVersion`, and
    likewise for `getGogdlVersion`, `getCometVersion`, `getNileVersion`. Import all four by name from
    `../utils/helperBinaries`. Do not add caching, timeouts or error wrapping the Electron path does
    not have.

    Above the `getCometVersion` registration, add a comment recording D-04 on the record: `comet` is
    the GOG Galaxy Communication replacement, gated on `gameInfo.runner === 'gog'` at
    `launcher.ts:973`; it is GOG's channel, not Zoom's; an option label during the 34.5 discussion
    wrongly bundled it with Zoom, and Zoom is exactly three channels (`authZoom`,
    `getZoomUserInfo`, `logoutZoom`), all DROPPED permanently by D-02.

    The two runtime channels delegate to `download(runtime_name)` and `isInstalled(runtime_name)`
    imported by name from `../wine/runtimes/runtimes`, unpacking `args[0]` as the runtime name.
    Add a comment stating this cluster has no wave-1 precondition because `runtimePath` resolves
    through `pathShim`'s already-implemented `userData`, so a reader does not add a spurious
    dependency on plan 34.5-01.

    Do not touch the five channels plan 34.5-12 owns (`callTool`, `egsSync`,
    `getGOGLinuxInstallersLangs`, `syncSaves`, `syncGOGSaves`) — leave their docstring entries alone.
  </action>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle(" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns 6.
    - `grep -c "ipcMain.on(" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns 0.
    - `grep "^import" src/backend/sidecar/runnerMiscFlowRegistration.ts | grep -c ipc_handler` returns 0.
    - `grep -c "launcher.ts:973" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns at least 1 — D-04's evidence is cited, not asserted.
    - Neither `syncSaves`, `syncGOGSaves`, `callTool`, `egsSync` nor `getGOGLinuxInstallersLangs` appears in an `ipcMain.` call in this file yet.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Six independent invoke channels are registered from curated imports, and D-04's correction is recorded where a future reader will actually find it.</done>
</task>

<task type="auto">
  <name>Task 2: Bidirectional registration-kind test for the version and runtime channels</name>
  <files>src/backend/sidecar/__tests__/runnerMiscFlows.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/humbleLoginFlows.test.ts L140-175 (Describe 1 — the bidirectional template; skip Describes 2-4)
    - src/backend/sidecar/runnerMiscFlowRegistration.ts (as written by Task 1)
    - src/backend/utils/ipc_handler.ts and src/backend/wine/runtimes/ipc_handler.ts (the kind ground truth — all six are `addHandler`)
    - src/backend/sidecar/__tests__/steamAuthFlows.test.ts L40-70 (the disposable-homedir harness — apply it if any assertion reaches the real config/tools path chain)
  </read_first>
  <action>
    Create `runnerMiscFlows.test.ts`, calling `registerRunnerMiscFlows()` ONCE at file scope.

    Assert bidirectional kind for the six channels registered so far: each of `getLegendaryVersion`,
    `getGogdlVersion`, `getCometVersion`, `getNileVersion`, `downloadRuntime`, `isRuntimeInstalled`
    is present in `handlerRegistry` and absent from `listenerRegistry`. Reference REQ-34.5-06 in the
    version tests and REQ-34.5-09 in the runtime tests.

    Add a curated-import guard asserting the module's source text contains no `import` of
    `utils/ipc_handler` or `wine/runtimes/ipc_handler`, and that the four channels
    `utils/ipc_handler.ts` also registers (`abort`, `getSystemInfo`, `copySystemInfoToClipboard`,
    `hasExecutable`) are NOT registered by this module — those belong to already-completed slices and
    a double registration would be a real defect.

    Add a forward-looking pin, commented as such: the five channels plan 34.5-12 owns are NOT yet
    registered by this module. Write it as an explicit "not yet" assertion listing them, so the
    reader of a failing test in wave 3 knows immediately that the expectation moved rather than that
    something broke. Plan 34.5-12 will flip these five assertions; it may not delete them.

    Apply the disposable-homedir `jest.mock('os', …)` harness if any assertion reaches the real
    config or tools-path chain. Never disable `jest.setupContainment.ts`.
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/runnerMiscFlows.test.ts` exits 0.
    - At least 12 kind assertions exist (6 channels × both directions), with REQ IDs in the test names.
    - An assertion proves `abort`, `getSystemInfo`, `copySystemInfoToClipboard` and `hasExecutable` are absent from both registries after `registerRunnerMiscFlows()`.
    - The forward-looking "not yet registered" pin names all five of plan 34.5-12's channels.
    - `git diff --name-only` does not list `src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts`.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/runnerMiscFlows.test.ts</automated>
  </verify>
  <done>All six channels are kind-pinned in both directions, no already-ported channel was double-registered, and wave 3's remaining work is declared in the test rather than left implicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → `downloadRuntime` runtime name | A caller-supplied identifier that selects a remote archive to download and extract |
| remote CDN → local filesystem (`runtimePath`) | `download()` fetches and tar-extracts remote content into the app's tools directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-23 | Tampering | `download(runtime_name)`'s remote fetch and tar extract | accept | Unchanged from Electron — same `axiosClient`, same URL construction, same extraction. This phase relocates the code onto the sidecar transport and does not widen the boundary; re-architecting it is out of scope (research § Security Domain, "relocates, does not widen"). |
| T-34.5-24 | Tampering | double-registration of an already-ported channel via an `ipc_handler` import | mitigate | Curated named imports only; the test asserts `abort`/`getSystemInfo`/`copySystemInfoToClipboard`/`hasExecutable` are absent from both registries after registration. |
| T-34.5-25 | Repudiation | a version probe silently returning stale or empty data | accept | Pure pass-through to the same functions the Electron build calls; no caching or wrapping is added, so behaviour is identical and any defect is inherited, not introduced. |
| T-34.5-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `cd src/backend && npx jest sidecar/__tests__/runnerMiscFlows.test.ts` exits 0.
- `grep -c "ipcMain.on(" src/backend/sidecar/runnerMiscFlowRegistration.ts` returns 0.
- No `ipc_handler` import in the module.
</verification>

<success_criteria>
The four CLI version probes and the two runtime channels answer on the sidecar, D-04's correction is
recorded in source with its evidence line, and nothing from an already-completed slice was
re-registered.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-07-SUMMARY.md` when done
</output>
