---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 30
type: execute
wave: 6
depends_on: [34.5-29]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md
autonomous: true
gap_closure: true
requirements: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-12]

must_haves:
  truths:
    - "A gate contract exists with verdict: null, written before any live work of the third run begins"
    - "It covers all five items including the two never attempted, each with evidence that is observable downstream of the thing it measures"
    - "Neither prior gate document is modified — each remains the record of its own failed run"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md"
      provides: "the third blocking gate contract, unfilled"
      contains: "verdict: null"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN-2.md"
      to: "34.5-G6-ITEM45-PREFLIGHT.md"
      via: "items 4 and 5 consume the verified preflight"
      pattern: "ITEM45-PREFLIGHT"
---

<objective>
Author `34.5-LIVE-GATE-RERUN-2.md`: the CONTRACT for the third blocking five-item live gate, written
with `verdict: null` BEFORE any live work of that run begins.

Purpose: this is the discipline plan 34.5-19 followed for run 2 and plan 34.5-03 followed for run 1,
and it is the only reason either run's evidence is trustworthy. A contract authored after the fact
can be shaped by what happened; a contract authored before it cannot. This plan writes the contract
and fills in NOTHING.

Output: a new file. **`34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` are NOT overwritten, NOT
edited, and NOT deleted** — each is the completed record of its own FAILED run (0/5 and 0/5), and
anyone citing either as evidence of a PASS is citing the wrong document.

**This plan runs nothing live.** Plan 34.5-31 executes this contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-FINDINGS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the frontmatter, the framing, and the preconditions</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` (whole file — the shape to mirror: frontmatter, the seven numbered preconditions, the evidence-handling rule, the arithmetic rule)
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR.md` § Routing (whether the gate was judged safe to author, and on what basis)
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` D-08 (the no-partial-pass rule, verbatim)
  </read_first>
  <action>
    Create `34.5-LIVE-GATE-RERUN-2.md` with frontmatter: `phase`, `plan: 30`,
    `executed_by: 34.5-31-PLAN.md`, `type: live-gate`, `status: pending`, `blocking: true`,
    `created`, `verdict: null`, `items_total: 5`, `items_passed: null`, `items_failed: null`,
    `items_not_attempted: null`, `supersedes: 34.5-LIVE-GATE-RERUN.md`.

    Open with **why this run exists**, naming what changed since run 2 and what did not:
    - Run 1 failed 0/5 on `spawn ./{legendary,gogdl,nile} ENOENT` (wrong `publicDir` under the
      sidecar). CLOSED and proven closed by run 2's precondition 4.
    - Run 2 failed 0/5 on a DIFFERENT, downstream defect family: the OAuth capture succeeded at the
      backend for `gog` and `nile` but never reached the renderer, and Epic's form was never
      interactive. Name each fix this gap cycle landed against it, by plan number: 34.5-23 (the 60 s
      invoke bound and the loud transport-failure path), 34.5-26 (the post-login library refresh that
      the raw-channel bypass skipped), 34.5-24/28/29 (Epic's discriminator and whatever fix it
      selected), 34.5-25 (the keyring double-prompt), 34.5-27 (the login-window origin display and
      interface style).
    - State plainly that NONE of that is live-proven yet, and that this contract exists to do so.

    Restate **D-08's no-partial-pass rule verbatim**: *any item recorded FAIL means this phase does
    not close — no advisory mode, no pre-authorized override, no deferred UAT entry, no partial-pass
    closure.* Add that a third FAIL routes to another gap cycle inside Phase 34.5, not a forced close.

    Restate the **evidence-handling rule (T-34.5-G6-02)** verbatim, binding on every Result slot.

    Write the preconditions as explicit NUMBERED steps — an unstated precondition is exactly how a
    gate gets skipped and becomes UAT debt (Phases 21, 30, 34.2, and this phase's own first run).
    Carry forward run 2's seven, updated, plus the new ones this cycle requires:

    1. **Phase 34.4.1's blocking gate PASSED on item 1** — satisfied by `34.4.1-LIVE-GATE-RERUN-3.md`
       (4/4). Carry the verbatim quote, and carry the warning that `34.4.1-LIVE-GATE.md` (2/4) and
       `-RERUN.md` (3/4) may never be cited as evidence of a PASS.
    2. **A clean machine** — `ps aux | grep -E "gamelib-shell|sidecar.js"` before launching, kill
       everything, re-check. An orphaned sidecar has now been found before BOTH prior runs; two live
       pairs contend for the same store files and make the evidence ambiguous.
    3. **A clean rebuild and a real `pnpm tauri:dev` launch**, with sidecar liveness proven from
       `gamelib.log` growth plus a `ps` listing showing both PIDs — never from the `tauri:dev`
       terminal.
    4. **The G-1 fix present in THIS build, proven from the log**: `[bootstrap] appRoot resolved=…
       source=GAMELIB_APP_ROOT` (a `process.cwd` source does NOT satisfy this even if the path looks
       right), `[bootstrap] publicDir resolved=… exists=true`, and all four
       `[bootstrap] runner binary … exists=true` lines. A `[bootstrap] SIDECAR ASSET ROOT DEFECT`
       line anywhere BLOCKS the run.
    5. **Signed OUT of Epic, GOG and Amazon — verified, not assumed.** Do NOT carry run 2's aside
       state forward: plan 34.5-20's SUMMARY explicitly records that session ending with GOG signed
       back IN and Amazon's password reset. Require fresh `ls`/`test -f` on every original and backup
       path, recorded verbatim, and require any re-aside to use a NEW dated suffix with a `cmp` proof
       before the original is removed. Check `nile_store/` AND `nile_config/`.
    6. **`npm run test:ci`, `npx tsc --noEmit`, `cd src-tauri && cargo check`, `cargo test`** — all
       four at the HEAD the gate runs on, exit codes recorded VERBATIM, any non-zero explained rather
       than rounded to green. Set the floor from this cycle's landed counts (run 2 recorded 3463 jest
       / 84 Rust; every plan 34.5-23 through 34.5-29 added tests, so the count MUST be higher — a
       count at or below 3463/84 means HEAD is stale and must be explained before proceeding). Also
       run `python3 ported-channels-gate.py` → exit 0.
    7. **`GAMELIB_SHELL_EXE` read back from the SIDECAR's own receipt line** in `gamelib.log`:
       `[bootstrap] GAMELIB_SHELL_EXE received=…`, never the Rust shell's `eprintln!` at `main.rs`
       (which reaches only the terminal and structurally cannot distinguish "set on the child env"
       from "visible to `pathShim`"). An `<UNSET>` reading BLOCKS item 4. Carry the dev-versus-packaged
       caveat verbatim: a dev PASS never retires the packaged half of Assumption A2, and never retires
       residual `R-34.5-G1-PKG`.
    8. **NEW — the gap-cycle fixes are present in this build, proven from source at HEAD, not from a
       SUMMARY.** Require: `oauthCaptureLogin` present in `main.rs`'s `LONG_RUNNING_CHANNELS`
       (comment-filtered grep), `capture-transport-failed` present in `useTauriOAuthLogin.ts`, and the
       completion-callback wiring present at `WebView/index.tsx`. Run 2's precondition 4 established
       the pattern: prove the fix is in the binary under test before attributing any result to it.
    9. **NEW — item 4 and item 5 preflight consumed.** `34.5-G6-ITEM45-PREFLIGHT.md` exists and its
       target verification is re-confirmed AT GATE TIME (the install may have changed since plan
       34.5-29 ran). Items 4 and 5 were NOT ATTEMPTED on both prior runs; this precondition exists so
       a third skip requires an explicit refusal rather than a drift.

    Leave every RESULT slot EMPTY.
  </action>
  <verify>
    <automated>grep -q "^verdict: null" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md &amp;&amp; git diff --name-only HEAD | grep -Ec "34.5-LIVE-GATE(-RERUN)?\.md" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-LIVE-GATE-RERUN-2.md` exists with `verdict: null`, `status: pending`, `blocking: true`, `executed_by: 34.5-31-PLAN.md`.
    - Nine numbered preconditions are present, including the two new ones (8 and 9).
    - D-08's no-partial-pass rule and the T-34.5-G6-02 evidence-handling rule both appear verbatim.
    - Precondition 5 explicitly refuses to carry run 2's aside state forward and cites plan 34.5-20's SUMMARY for why.
    - Precondition 6's floor is stated as strictly greater than 3463 jest / 84 Rust, with the reason.
    - `git status --porcelain` shows `34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` UNMODIFIED.
    - Every Result slot is empty — no PASS, FAIL or NOT ATTEMPTED marker is filled anywhere.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the five items, with evidence that is observable downstream</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` §§ Items 1-5 (the wording to carry forward, and each item's recorded FAIL/NOT-ATTEMPTED reason)
    - `.planning/phases/34.5-.../34.5-G6-ITEM45-PREFLIGHT.md` (the verified item-4 target, the redacted Steam path, the two devtools invocations, the named item-5 Wine action and its evidence lines)
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR.md` (what item 1 should now expect, per its verdict)
    - `src/backend/sidecar/oauthLoginCapture.ts` and `src/frontend/screens/WebView/useTauriOAuthLogin.ts` — read the ACTUAL log-line shapes at authoring time so this contract cannot cite a line that does not exist in the shipped code
  </read_first>
  <action>
    Write all five items. For each: what changed since run 2, what to do, what to observe, and what
    evidence is required.

    **The binding evidence rule for every item — state it once at the top of the Items section and
    apply it to all five: a mutating call's own report is never proof of its effect.** Run 2 recorded
    `status=captured` from the backend for GOG and Amazon and it proved nothing; both items still
    FAILED. Every evidence clause below must therefore name at least one observable produced by a
    DIFFERENT component than the one being tested — a UI state, a file on disk, a log line emitted by
    another module, or a `ps` listing.

    **Items 1-3 (Epic / GOG / Amazon login from scratch, populated library)** — REQ-34.5-12, plus
    REQ-34.5-02 for item 3's anchor. For each, require ALL of:
      (a) the `[oauthLoginCapture] runner=… status=captured` line (backend's own report — necessary,
          not sufficient);
      (b) the `[useTauriOAuthLogin] runner=… phase=…` line for the same runner. **Its absence is the
          exact signature of run 2's defect** — six backend terminal outcomes produced zero such
          lines. This is a DIFFERENT component reporting, which is why it counts;
      (c) the runner's CLI auth invocation appearing in the log AFTER the capture timestamp
          (`legendary`/`gogdl auth`/`nile auth --login` as applicable);
      (d) the library **populated** in the UI — named games visible — AND the runner present in the
          Library filter options. An empty library after a "successful" login is a FAIL, not a
          partial pass: run 2's developer report named the missing filter option specifically;
      (e) the account surface showing the signed-in username.
      Item 3 additionally keeps sub-clause (b) from run 2: grep for
      `[oauthLoginCapture] runner=nile origin-mismatch origin=` — it must NOT fire on a clean pass;
      if it does, Assumption A1 is FALSIFIED and item 3 cannot be a clean PASS even if login
      otherwise succeeds. Record the observed origin either way, and keep run 2's three-way
      CONFIRMED / FALSIFIED / UNTESTED recording rule verbatim.
      For item 1, note what the discriminator's verdict predicts, and add: **a prediction is not a
      result.** Record what actually happens.

    **Item 4 (`addToSteam` AND the macOS `.app` shortcut, BOTH `exe` call sites)** — REQ-34.5-01,
    REQ-34.5-12. Inline the verified target, the redacted Steam `shortcuts.vdf` path, the quit-Steam
    step, and the two exact devtools invocations from `34.5-G6-ITEM45-PREFLIGHT.md` — do not merely
    reference the file, because run 2 referenced its groundwork and the item was still skipped. State
    that proving ONE call site is not sufficient (research Correction 3: two independent writers of
    the same value, either can regress alone). Evidence: the written VDF `Exe` field, the `run.sh`
    body, and `ps aux` after clicking each, showing the Tauri shell binary and never a bare `node`
    process. Carry the dev-versus-packaged limit.

    **Item 5 (`runWineCommand` for a non-Steam runner)** — REQ-34.5-03, REQ-34.5-12. Inline the
    specific macOS-permitted Wine action the preflight named, its UI path, its dispatching channel and
    registration kind, the literal evidence log lines, the observable in-app effect, and the
    prerequisite checks. Restate that Pitfall 2's claim is retired ONLY by an item-5 PASS — a
    FAIL-by-blockage retires nothing, exactly as both prior runs recorded.

    Then write, all unfilled:
    - a **Verdict** table with one row per item;
    - a **What this gate falsifies** section listing the four standing claims with their eligibility
      rules verbatim from run 2 (the 34.4.1 seam claim per-runner; Assumption A1; Assumption A2 at
      both call sites with the packaged half and `R-34.5-G1-PKG` excluded; Pitfall 2), each marked
      STANDING, plus the rule that nothing is struck without the corresponding ITEM passing — not on
      the strength of a sub-clause being independently confirmed while its parent item FAILs, which
      is precisely the distinction run 2 had to draw for A1;
    - the **arithmetic rule**: `items_passed + items_failed` must equal 5 on a clean completion; a NOT
      ATTEMPTED item counts toward NEITHER and the shortfall must be stated explicitly in the
      frontmatter `notes`, never quietly rounded.

    Add a **New findings (this run)** section header with the instruction to record observations
    WITHOUT diagnosing them, as both prior runs did — diagnosis belongs to the next cycle.

    Leave every Result slot EMPTY. This plan authors; plan 34.5-31 fills.
  </action>
  <verify>
    <automated>grep -c "Result:" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md | grep -qx 5 &amp;&amp; grep -q "arithmetic" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five items, each with an empty Result slot; `grep -c "Result:"` returns 5.
    - The binding evidence rule ("a mutating call's own report is never proof of its effect") is stated once and every item's evidence clause names at least one observable from a different component than the one under test.
    - Items 1-3 each require all five evidence clauses (a)-(e), with (b)'s absence identified as run 2's defect signature and (d) requiring both populated games AND the filter option.
    - Item 3 keeps the origin-mismatch sub-clause with its three-way CONFIRMED/FALSIFIED/UNTESTED rule.
    - Item 4 INLINES the verified appName, the redacted Steam path, the quit-Steam step and both devtools invocations — not a bare cross-reference.
    - Item 5 INLINES the named Wine action, its channel and kind, its literal evidence lines, and its prerequisites.
    - Every log-line shape cited in any item was read from the emitting source file at authoring time; the SUMMARY names each file it was read from.
    - The Verdict table, the four standing claims with eligibility rules, the "nothing struck without the ITEM passing" rule, the arithmetic rule and the New findings header are all present and unfilled.
    - `git status --porcelain` still shows both prior gate documents UNMODIFIED.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior gate records → this contract | Overwriting or editing a completed record would destroy the evidence of a failed run |
| machine-local identifiers → contract text | Item 4 inlines a real Steam userdata path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-G6-34 | Tampering | `34.5-LIVE-GATE.md` / `34.5-LIVE-GATE-RERUN.md` | mitigate | A NEW filename is used; both tasks' acceptance criteria require `git status --porcelain` to show the two prior documents unmodified, checked after each commit |
| T-34.5-G6-35 | Repudiation | a contract citing a log line that does not exist | mitigate | Every cited line shape must be read from the emitting source file at authoring time and the source path recorded — this is the exact gate-contract defect run 1's precondition 5 shipped and run 2 had to close |
| T-34.5-G6-31 | Information disclosure | Steam `userdata` account id inlined into item 4 | mitigate | Carried from plan 34.5-29: the id is committed REDACTED; the path structure is the evidence, the id is not |
| T-34.5-G6-36 | Repudiation | a sub-clause confirmation read as retiring its parent claim | mitigate | The falsification section carries run 2's explicit rule that nothing is struck without the corresponding ITEM passing, with A1 named as the worked example |
| T-34.5-G6-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages and touches no source file |
</threat_model>

<verification>
- `34.5-LIVE-GATE-RERUN-2.md` exists, `verdict: null`, nine preconditions, five empty items, verdict table, falsification section, arithmetic rule.
- `git status --porcelain` shows the two prior gate documents unmodified.
- `git diff --name-only` matches zero `.ts`, `.tsx`, `.rs` paths.
</verification>

<success_criteria>
A third blocking gate contract exists, authored before any live work, covering all five items —
including the two never attempted — with preconditions that prove the fixes are in the build under
test, and evidence clauses that require an observable from a component other than the one being
measured.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-30-SUMMARY.md` when done
</output>
