---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 40
executed_by: 34.5-41-PLAN.md
type: live-gate
status: complete
blocking: true
created: 2026-08-02
completed: 2026-08-02
verdict: FAIL
items_total: 5
items_passed: 0
items_failed: 2
items_blocked: 1
items_not_attempted: 2
preconditions_satisfied: [1, 4, 5, 6, 7, 8, 9, 10, 11]
preconditions_blocked: []
preconditions_pending: [2, 3]
supersedes: 34.5-LIVE-GATE-RERUN.md
notes: |
  CONTRACT authored by plan 34.5-40 (wave 3), before any live work of this run begins. Executed by
  plan 34.5-41 (Tasks 2 and 3; Task 1's pre-gate-preparable preconditions were recorded and
  committed separately in e30bcad34). LIVE RUN COMPLETE 2026-08-02, on real macOS hardware,
  developer-driven, across two sessions (session A 21:23:55-21:33:12, session B 21:42:53-22:00:24)
  with different secret-store arms (session A used `keyring` -- a DEVIATION from D-CYCLE5-B;
  session B used `dev-vault`).

  ARITHMETIC: items_passed(0) + items_failed(2) + items_blocked(1) + items_not_attempted(2) = 5 =
  items_total. Stated explicitly per the contract's own arithmetic rule -- BLOCKED and NOT
  ATTEMPTED count toward neither passed nor failed, and this shortfall (0 of 5 clean) is not
  rounded away.

  VERDICT: FAIL. items_passed is 0, not 5 -- this is NOT the clean 5-of-5 case, so per D-08, Phase
  34.5 does NOT close on this result. Per `34.5-CYCLE5-ROUTING.md` Section "Routing by verdict",
  this outcome lands in the "any of items 2,3,4,5 FAIL" row (items 2 and 4 both FAILED) -- next
  action `/gsd-plan-phase 34.5 --gaps` (gap cycle 6).

  Item 1 (Epic): opportunistic attempt made per the clause (two login windows driven), form
  rendered and accepted input, but the PARKED pre-authentication defect (403 on
  `/id/api/email/exists`) reproduced itself before a distinguishable PASS/FAIL outcome could be
  produced -- stays BLOCKED, not converted to FAIL. Two extra observations recorded: 403 (not
  timeout, a distinct phenomenon per the debug file's own framing); observer-script injection NOT
  OBSERVED.

  Item 2 (GOG): FAIL, but on a THIRD, new failure layer, downstream of both prior runs' own root
  causes. Backend capture, frontend propagation (F-34.5-G6-02 appears CLOSED), CLI auth, dispatch
  and persistence (7 games landed in gog_library.json) all succeed -- but the Library UI never
  renders the runner's games. U-34.5-09 retires (mid-flight-teardown propagation proven live,
  twice). `origin=unknown` still occurs once (F-34.5-G6-14), violating the item's own clause (g).

  Item 3 (Amazon): NOT ATTEMPTED -- session ended after item 4's diagnosis. Assumption A1 stays
  UNTESTED, not confirmed or falsified.

  Item 4 (shortcuts): FAIL, first attempt ever for this item. Both `exe` call sites were exercised.
  `addToSteam` writes the correct Exe value but returns `undefined` instead of a boolean
  (F-34.5-G6-08). `addShortcut` is structurally dead under the sidecar -- `nativeImage` stub lacks
  `createFromBuffer` (F-34.5-G6-07), blocking the macOS `.app` call site for every game, not just
  this one. The one reachable launch path boots a second full GameLib instance instead of the game
  (F-34.5-G6-09). A contract/preflight defect was found live: this machine's global shortcut
  settings are all false, so the contract's own literal item-4 invocation would have been a
  guaranteed silent non-event (F-34.5-G6-11).

  Item 5 (Wine): NOT ATTEMPTED, first attempt ever for this item, blocking prerequisite unmet and
  re-verified at gate time -- zero wine-type Wine versions downloaded.

  Standing claims: all four remain STANDING -- zero items PASSed, so nothing is struck, per the
  gate's own "nothing struck without the item passing" rule.

  Known holes: U-34.5-09 RETIRES (its own narrow observable was met twice). All other OPEN rows
  (U-34.5-01/02/03/04/05/06/07/08/10/11) stay OPEN, each against its own row's stated retirement
  bar -- see "Known holes -- RESOLVED" table.

  Preconditions: this plan's scope is Tasks 2 and 3 only (item results, verdict, arithmetic,
  falsification and known-holes resolution). Preconditions 1, 5, 6, 8, 9 and 11 were satisfied
  during pre-gate preparation (commit e30bcad34). This run's own item-level log evidence
  independently confirms preconditions 4, 7 and 10 held at both sessions' starts (bootstrap
  appRoot/publicDir/runner-binary lines with source=GAMELIB_APP_ROOT, GAMELIB_SHELL_EXE received
  (not <UNSET>), and the secret-store arm recorded each session) -- no hard STOP fired at any
  point. Preconditions 2 and 3 (the clean-machine kill sweep and the rebuild/launch PID-pair
  evidence) were NOT independently re-verified in writing by this plan and are recorded PENDING
  rather than assumed SATISFIED -- this plan did not re-open Task 1's own checkpoint, which left
  them explicitly EMPTY "as scoped" per e30bcad34's own commit message.

  Credential grep check (T-34.5-G6-02): `grep -Ec "token=|code=|Cookie|userdata"` against the
  finished document returns non-zero, but every match is either a redaction example
  (`access_token=<REDACTED>` in the evidence-handling-rule prose), a benign substring inside an
  unrelated identifier (`sessionCookie` read-key name in dev-vault log lines, which never carries a
  value), a field NAME rather than a value (`LaunchOptions`, `code-field legibility`), or the
  literal string `userdata` used only in its REDACTED path form
  (`userdata/<REDACTED>/config/shortcuts.vdf`) -- confirmed by manual review, recorded in the
  SUMMARY.
---

# Phase 34.5 Plan 40 — Blocking Live-Gate RE-RUN 2 (REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-04, REQ-34.5-05, REQ-34.5-12)

This document is a **CONTRACT**, written before any live work begins — the same discipline plan
34.5-03 followed for run 1 and plan 34.5-19 followed for run 2. It is the third such contract for
this phase's blocking five-item live gate.

**This plan supersedes plan 34.5-30**, which was HALTED by cycle 3's `BINDING DECISION: fix-first`
and never authored. Plan 34.5-30 is not edited, not renumbered and not executed; it stays on disk
as the record of its own halt. Its authorisation gate is spent — cycle 5's own authorisation
(`34.5-CYCLE5-ROUTING.md`, D-CYCLE5-A) replaces it, and this plan runs unconditionally under it.

**`34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` are NOT overwritten, NOT edited and NOT
deleted.** Each is the completed record of its own FAILED run (0/5 and 0/5). Anyone citing either
document as evidence of a PASS on any item is citing the wrong document.

**This plan runs nothing live.** Plan 34.5-41 executes this contract.

---

## Why this run exists

- **Run 1** (`34.5-LIVE-GATE.md`, plan 34.5-15, 2026-08-01) failed 0/5 on `spawn
  ./{legendary,gogdl,nile} ENOENT` at sidecar startup — `publicDir` resolved to a nonexistent
  `src-tauri/public` path (the `publicdir-getapppath-chunking` gotcha family, its third
  recurrence at the time). **CLOSED**, and proven closed by run 2's own precondition 4
  (`gamelib.log` showing `source=GAMELIB_APP_ROOT`, `publicDir exists=true`, all four runner
  binaries `exists=true`, no `SIDECAR ASSET ROOT DEFECT` line).
- **Run 2** (`34.5-LIVE-GATE-RERUN.md`, plan 34.5-19, executed by plan 34.5-20, 2026-08-01) failed
  0/5 on a DIFFERENT, downstream defect family: the OAuth capture succeeded at the backend for
  `gog` and `nile` (`status=captured` logged for both) but never reached the renderer — no
  follow-up runner-CLI auth invocation fired, and the frontend's own `[TauriLoginPanel]
  captured-blocked` line never appeared for either runner. Epic's login form never became
  interactive at all (0 of 3 attempts captured; the form rendered but presented no interactive
  elements). Items 4 and 5 were NOT ATTEMPTED (confirmed explicitly by the developer).
- **Fixes landed against run 2's findings since, by plan number:**
  - **34.5-23** — the 60s invoke bound / 300s sidecar deadline mismatch (`oauthCaptureLogin` and
    the completion-callback channel added to `LONG_RUNNING_CHANNELS` in `main.rs`), plus the loud
    `capture-transport-failed` path in `useTauriOAuthLogin.ts` instead of a silent transport
    rejection.
  - **34.5-26** — the post-login library refresh the raw channel bypass previously skipped:
    `useTauriOAuthLogin.ts` now hands a `status === 'done'` response to an injected
    `onLoginSuccess` completion callback (`createOAuthLoginCompletion`), wired by
    `GlobalState.tsx` as `completeOAuthLogin` and passed into the hook by
    `WebView/index.tsx:78-80`.
  - **34.5-27** — login-window origin display (the title bar reads the navigated hostname) and
    interface style, addressing F-34.5-G6-04 (no URL/origin shown).
  - **34.5-33** — runner-aware `refreshLibrary` dispatch (`libraryManagerMap[runner]` instead of
    an unconditional Steam-only call) plus the `origin=` attribution at
    `GlobalState.tsx:1012`; the same-day live-found `null`-vs-`undefined` JSON-transport
    regression in `handleRefreshLibrary` (`steamFlowRegistration.ts:112-117`, now excluding BOTH
    `undefined` and `null`) was found and fixed in the same session.
  - **34.5-34** — cancellation (effect teardown) no longer discards a completed login: the
    `safeSetState` helper (`useTauriOAuthLogin.ts:152-156`) gates state updates only, never the
    irreversible `onLoginSuccess` call or the perishable OAuth code exchange.
  - **34.5-35** — the keyring read bound raised `KEYRING_READ_TIMEOUT` 8s→45s
    (`src-tauri/src/main.rs:1100`) and the failure memo `KEYRING_FAILURE_MEMO_MS` 15s→120s
    (`src/backend/sidecar/keyringTokenStore.ts:55`), addressing F-34.5-G6-06 (double Keychain
    prompt on GOG sign-out).
  - **34.5-36** — the dev-only plaintext secret vault (`src/backend/sidecar/devSecretVault.ts`),
    gated on an exact-string `GAMELIB_DEV_SECRET_VAULT === '1'` match, to remove Keychain prompts
    as a confounder from this cycle's gate run.
  - **The 2026-08-02 Epic post-auth exfil fix** (commit `c857ade8e`, ledger row `U-34.5-11`) — a
    `.on_navigation` intercept plus a production `epic_oauth_redirect_observer_script` in
    `src-tauri/src/main.rs`, closing the CONFIRMED post-authentication root cause
    (`.planning/debug/epic-login-non-interactive.md`, `Resolution.root_cause`: WKWebView silently
    refuses Epic's client-side navigation to its own `redirectUrl`).

**None of the above is live-proven.** Every fix above is verified ONLY statically this cycle
(`cargo check`/`cargo test`, `npx tsc --noEmit`, `npm run test:ci` — see precondition 6's cited
measured baseline). This contract exists to do so.

---

## D-08 — no partial pass

Restated verbatim from `34.5-CONTEXT.md`:

> **D-08:** **Blocking live gate covering all three logins** (Epic, GOG, Amazon) on macOS — same
> standard as every prior slice, no advisory mode, no pre-authorized override. Rejected: one
> runner live + two declared unit-proven-only; auth-only gating with Wine/shortcuts deferred to
> UAT.

Restated from run 2's own binding language, unchanged: *any item recorded FAIL means this phase
does not close — no advisory mode, no pre-authorized override, no deferred UAT entry, no
partial-pass closure.*

A third non-clean result (anything other than 5 PASS / 0 FAIL / 0 BLOCKED / 0 NOT ATTEMPTED)
routes to another gap cycle inside Phase 34.5 — **never a forced close**. Phase 34.6's and Phase
35's dependency lines on Phase 34.5 are editable ONLY on a clean 5-of-5 PASS recorded in this
document's own frontmatter.

---

## Evidence-handling rule (T-34.5-G6-02)

Restated verbatim from run 2's contract, binding on every Result slot below. This document lives
in the public fork's repository. When pasting `gamelib.log` excerpts, VDF/config contents, or any
other captured evidence into a Result slot: never paste an `access_token`, `refresh_token`, cookie
value, session identifier, or the Steam `userdata` account id. Quote only the specific log lines
an item or precondition names. If a required line contains one of these values inline, redact it
in place (e.g. `access_token=<REDACTED>`) rather than omitting the whole line — the surrounding
structure (which field was populated, that a value was non-empty) is still evidence; the value
itself is not needed to prove a PASS.

---

## The binding evidence rule

Stated once, applying to all five items: **a mutating call's own report is never proof of its
effect.** Run 2 recorded backend `status=captured` for GOG and Amazon and it proved nothing — both
items still FAILED, because the capture was never consumed downstream. Every evidence clause below
therefore names at least one observable produced by a DIFFERENT component than the one under test:
a UI state, a file on disk, a log line emitted by another module, or a `ps` listing.

---

## Item 1 enters BLOCKED — cause, and the opportunistic-attempt clause

Per D-CYCLE5-A, carried from `34.5-CYCLE5-ROUTING.md`:

**BLOCKED is a fourth result value**, distinct from PASS, FAIL and NOT ATTEMPTED: an item whose
precondition is known-unsatisfiable at authoring time for a reason recorded OUTSIDE this gate. It
counts toward NEITHER `items_passed` nor `items_failed`, and the shortfall is stated in the
frontmatter `notes` when the run completes.

**The cause, named:**

- The **POST-authentication** root cause is CONFIRMED and its fix IMPLEMENTED but STATIC-ONLY
  (commit `c857ade8e`, ledger row `U-34.5-11`) — WKWebView silently refuses Epic's client-side
  navigation to its own `redirectUrl`. Per
  `.planning/debug/epic-login-non-interactive.md`'s `Resolution.verification`: `cargo
  check`/`cargo test` clean, `npx tsc --noEmit` clean, `npm run test:ci` clean at the time — but
  explicitly "STATIC/COMPILE/UNIT PROOF ONLY", with the live gate itself "OWED, not attempted, not
  partially covered by anything above."
- The **PRE-authentication** half is the actual blocker and is PARKED by explicit developer
  decision (`developer_override_2026_08_02T19_00_00`). Its measured shape, recorded rather than
  a slogan: across the Tauri arm's runs, `/id/api/email/exists` returned **403 x1**, **`xhr.timeout`
  x2**, and **success x1** — and NO Tauri run has ever completed an Epic login end to end. The 403
  and the two `xhr.timeout` outcomes are NOT the same phenomenon (the debug file's own
  `three_outcome_reframe` section: the timeouts fit a distinct shape from the 403, which remains a
  separate, still-unexplained phenomenon) — do not collapse them into one description when
  recording what happens this run.
- The debug file's own **NEW RISK**, recorded verbatim, must be checked FIRST when the live gate
  runs: *"the production observer script wraps `window.fetch` using the SAME wrapping technique
  `DEV_LOGIN_DIAGNOSTIC_INIT_SCRIPT` uses...and that script's patched network primitives are this
  file's OWN live suspect for CAUSING the 403 the pre-auth thread is investigating....if patched
  `fetch` is confirmed to be what [the anti-bot system]'s fingerprinting keys on, shipping this fix
  unconditionally could make the pre-auth 403 permanent for every Epic login attempt rather than
  opt-in....it is recorded here as an open risk for the live gate this cycle cannot run, and as a
  candidate explanation to check FIRST if/when that live gate still shows a 403 after this fix
  ships."*

**Opportunistic-attempt clause:** a BLOCKED entry is NOT permission to skip a working path. **If
the Epic login form renders and accepts input, the operator MUST attempt item 1 for real and
record PASS or FAIL instead of BLOCKED.** In either case, record two additional observations:
(a) whether `/id/api/email/exists` returns 403, times out, or succeeds; and (b) whether the
always-on `epic_oauth_redirect_observer_script` was injected — check this FIRST per the debug
file's own NEW RISK above. **This is DIAGNOSIS ONLY: no change to `USER_AGENTS`, `EPIC_LOGIN_URL`
or `matchOAuthRedirect` is authorised by this gate.**

**Consequence, stated in one line:** with item 1 entering BLOCKED, this run cannot reach 5 of 5
unless the opportunistic-attempt clause converts it to a PASS — which is why every other item still
matters, and why the routing table in `34.5-CYCLE5-ROUTING.md` marks the 5-of-5 branch
EXPECTED-unreachable, not unreachable-by-construction.

---

## Known holes (ledger Rule 3)

Per `34.5-UNTESTED-ITEMS.md`'s own Rule 3: *"any OPEN row that the gate's own items would have
covered must be named in the gate contract as a known hole rather than silently assumed
covered."* One row per OPEN ledger row, whether THIS run can retire it:

| Ledger ID | Item | Which gate item would retire it | Can THIS run retire it? |
|---|---|---|---|
| U-34.5-01 | Real macOS Keychain read/write path for `steam-refresh-token`/`humble-session`/`humble-csrf` | None of this gate's items exercise the keyring directly | **CANNOT be retired this run.** Per D-CYCLE5-B, `GAMELIB_DEV_SECRET_VAULT=1` is exported for this run, so no real `keyring_get` is issued and the Keychain path is never exercised. Ledger's own binding statement: *"a `dev-vault` run proves NOTHING about the Keychain path and stays OPEN regardless of that run's outcome."* Stays OPEN whatever this run's verdict. |
| U-34.5-02 | Gate item 4 (`addToSteam` + macOS `.app` shortcut, both `exe` call sites) | Item 4 | Retirable ONLY by an item-4 PASS covering BOTH call sites (never one alone). |
| U-34.5-03 | Gate item 5 (`runWineCommand` for a non-Steam runner) | Item 5 | Retirable ONLY by an item-5 PASS. |
| U-34.5-04 | Amazon verification-code field legibility (F-34.5-G6-05) | Item 3 | Retirable by a live Amazon login reaching the code field with its text transcribed as legible or not. |
| U-34.5-05 | Login-window origin display CHANGING within a single flow | Items 1-3 | Retirable by a transcribed before/after pair of the title-bar text within one flow — GOG's `login.gog.com` → `embed.gog.com` is the known candidate if item 2 provides it. |
| U-34.5-06 | Epic's success path end to end (`status=captured` → `phase=idle`) | Item 1 | **Retires together with U-34.5-11 or not at all** — they share the same live blocker (item 1's BLOCKED entry). Retirable only via the opportunistic-attempt clause succeeding. |
| U-34.5-07 | Runner-aware `refreshLibrary` dispatch actually populating a non-Steam library live | Item 2 | Retirable by a live session in which, following a successful GOG login, `gamelib.log` shows `refreshLibrary complete runner=gog managers=1` (`steamFlowRegistration.ts:151`) AND the Library UI displays actual GOG game tiles. |
| U-34.5-08 | `origin=` field correctly attributing a live `refreshLibrary` call, and zero `Refreshing undefined Library` lines across a full login | Items 2-3 | Retirable by a live session with zero `Refreshing undefined Library` lines across a full GOG login, and every `[refreshLibrary]` line (`GlobalState.tsx:1012`) carrying a non-`unknown` `origin=` value. |
| U-34.5-09 | `useTauriOAuthLogin.ts` propagating a captured code / completed login through a mid-flight teardown | Items 2-3 | Retirable by multiple consecutive identical GOG login attempts in one live session, each producing a `[useTauriOAuthLogin] phase=idle` line after backend `status=captured`, with zero attempts silently returning the developer to Manage Accounts. |
| U-34.5-10 | Keyring read-timeout/human-approval race (34.5-35's raised 45s/120s constants) | None of this gate's items exercise the keyring directly | **CANNOT be retired this run**, for the same reason as U-34.5-01 (D-CYCLE5-B, dev vault enabled) — the raised constants sit on a code path this run deliberately does not exercise. Its own row: *"a run using the dev-only secret vault CANNOT serve as evidence for plan 34.5-35's item-3 claim."* Stays OPEN whatever this run's verdict. |
| U-34.5-11 | The Epic OAuth-redirect-capture exfil mechanism (commit `c857ade8e`) delivering a captured redirect code end to end | Item 1 | **Retires together with U-34.5-06 or not at all** — same shared live blocker. Retirable only via the opportunistic-attempt clause succeeding. |

### Known holes — RESOLVED (recorded by plan 34.5-41, after the live run)

Per Ledger Rule 1: a row is retired ONLY by the exact observation its own row names, never by a
passing item overall and never by inference from a neighbouring row. Resolved against
`gamelib.log.34.5-g6-gate3-sessionA`/`...sessionB` directly.

| Ledger ID | Resolution | Exact observation (or why not) |
|---|---|---|
| U-34.5-01 | **STILL OPEN** | Session A used the `keyring` arm (a DEVIATION from D-CYCLE5-B — see precondition 10 below), but its two `SidecarKeyringSlotStore` reads (`humble-session`, `steam-refresh-token`) both ended `keyring_get failed: keyring:timeout` — the retirement bar requires "at least one successful" real-slot read, which did not happen. Session B used `dev-vault`, which structurally cannot exercise this path at all. Neither session meets the row's own four-part retirement bar. |
| U-34.5-02 | **STILL OPEN** | Item 4 FAILED (see Verdict). Retirable only by an item-4 PASS covering both call sites; item 4 did not PASS. |
| U-34.5-03 | **STILL OPEN** | Item 5 NOT ATTEMPTED (blocking prerequisite unmet, re-verified at gate time). Retirable only by an item-5 PASS. |
| U-34.5-04 | **STILL OPEN** | Item 3 NOT ATTEMPTED — the Amazon code field was never reached. |
| U-34.5-05 | **STILL OPEN** | A nav-origin change IS log-observed for GOG (`nav host=login.gog.com` at 21:30:02 → `nav host=embed.gog.com` at 21:30:30, session A), but the row's own retirement condition specifically requires a **transcribed title-bar TEXT** before/after pair, which item 2's evidence explicitly states was not captured this run. A log-level nav host is a different observable than a transcribed title bar; the row stays OPEN on its own stated terms. |
| U-34.5-06 | **STILL OPEN** — retires together with U-34.5-11 or not at all | Item 1 stayed BLOCKED; zero `runner=legendary status=captured` lines occurred (confirmed by direct grep against session A, count 0). |
| U-34.5-07 | **STILL OPEN** | Compound condition: `refreshLibrary complete runner=gog managers=1` (`steamFlowRegistration.ts:151`) DID fire, five times (21:30:53, 21:31:03, 21:32:02, 21:32:12, 21:33:12, session A) — but the row also requires "the Library UI displays actual GOG game tiles," which the operator observed did NOT happen ("no games showed"). Half the compound condition is met; the row's own text requires both, so it stays OPEN. Backend data did persist (7 GOG titles in `gog_library.json`, confirmed independently by this plan), but that is not the observation the row names. |
| U-34.5-08 | **STILL OPEN** | Compound condition: zero `Refreshing undefined Library` lines — confirmed TRUE (`grep -c` against session A returns 0). But "every `[refreshLibrary]` line carrying a non-`unknown` origin=" — confirmed FALSE: `(21:32:47) [refreshLibrary] runner=all origin=unknown` fires in session A. One clause of the compound condition fails, so the row stays OPEN. |
| U-34.5-09 | **RETIRED** | The row's own narrow observable — "multiple consecutive identical GOG login attempts in one live session, each producing a `[useTauriOAuthLogin] phase=idle` line after backend `status=captured`, with zero attempts silently returning the developer to Manage Accounts" — occurred exactly as specified, twice, in session A: attempt 1 (`status=captured` 21:30:30 → `phase=idle` 21:30:41) and attempt 2 (`status=captured` 21:31:41 → `phase=idle` 21:31:51). Neither attempt was silently discarded. This is a narrower, mechanism-specific claim (mid-flight-teardown propagation survives) than item 2's overall result, which FAILED for a different, downstream reason (the UI library not rendering) — the row's own text names only the propagation observable, which this run satisfies in full, so it retires independently of item 2's overall FAIL. |
| U-34.5-10 | **STILL OPEN** | Same reasoning as U-34.5-01: session A's keyring-arm reads both TIMED OUT rather than succeeding under human approval, so the "only ONE Keychain approval prompt" success observation this row names was never produced — a timeout is not an approval. Session B used `dev-vault` and cannot exercise this path. |
| U-34.5-11 | **STILL OPEN** — retires together with U-34.5-06 or not at all | Same shared blocker as U-34.5-06; the opportunistic-attempt clause did not resolve to PASS. |

---

## Preconditions

**Tick off in order. An unstated precondition is exactly how a gate gets skipped and becomes UAT
debt (Phases 21, 30, 34.2 and this phase's own first two runs).**

1. **Phase 34.4.1's blocking gate PASSED on item 1.** Satisfied by `34.4.1-LIVE-GATE-RERUN-3.md`
   (4/4). Verdict-table item-1 result line, quoted verbatim: `| 1 — login from scratch | -02, -12,
   GAP-02, GAP-09 | ✅ **PASS** | 75B keyring store, no sessionCookie, keyring-install line
   present; **WR-07 closed**, **F-4 closed** |`. Overall run-3 verdict: `**4 of 4 PASS — the gate
   is met. Phase 34.4.1 closes.**`. **`34.4.1-LIVE-GATE.md` (2/4) and `34.4.1-LIVE-GATE-RERUN.md`
   (3/4) hold only superseded FAILED runs and may never be cited as evidence of a PASS.**

   **RESULT — RECORDED during PRE-GATE PREPARATION, 2026-08-02 (not the live session).**

   Verified by grepping `34.4.1-LIVE-GATE-RERUN-3.md` directly, not transcribed from memory:
   ```
   $ grep -n "login from scratch" 34.4.1-LIVE-GATE-RERUN-3.md
   1314:| 1 — login from scratch | `-02`, `-12`, `GAP-02`, `GAP-09` | ✅ **PASS** | 75B keyring store, no `sessionCookie`, keyring-install line present; **WR-07 closed**, **F-4 closed** |

   $ grep -n "4 of 4 PASS" 34.4.1-LIVE-GATE-RERUN-3.md
   1307:# **4 of 4 PASS — the gate is met. Phase 34.4.1 closes.**
   ```
   Both lines quoted verbatim from `34.4.1-LIVE-GATE-RERUN-3.md` (the 4/4 document), never from
   `34.4.1-LIVE-GATE.md` (2/4) or `34.4.1-LIVE-GATE-RERUN.md` (3/4). **Precondition SATISFIED.**

2. **A clean machine.** Run `ps aux | grep -E "gamelib-shell|sidecar.js"` BEFORE launching
   anything, and kill anything already running. Record the command's output verbatim both before
   the kill and after. An orphaned sidecar was found before BOTH prior runs — two live pairs
   contend for the same store files and make evidence ambiguous.

   **RESULT — [ ]**

3. **A clean rebuild and a real `pnpm tauri:dev` launch.** Confirm the sidecar is alive via
   `~/Library/Logs/GameLib/gamelib.log` growing AND a `ps` listing showing both the shell and
   sidecar processes simultaneously. **Sidecar output is read from that log file, NEVER from the
   `pnpm tauri:dev` terminal** — the terminal shows only the shell process's own console (stdout
   is the RPC pipe), never proof the sidecar's application logic ran
   (`sidecar-console-and-logger-are-invisible`). Keep `tail -f
   ~/Library/Logs/GameLib/gamelib.log` open in a second window for the whole run. Record the exact
   rebuild sequence and its output, and the `ps aux` lines showing both PIDs alive together.

   **RESULT — [ ]**

4. **The G-1 fix is present in THIS build — proven from the log, not from git.** Quote verbatim
   from THIS session's `gamelib.log`:
   - `[bootstrap] appRoot resolved=<path> source=<GAMELIB_APP_ROOT|process.cwd>` — `source=` must
     read `GAMELIB_APP_ROOT`, never `process.cwd` (a `process.cwd` source means the Rust-side
     handoff did not reach the sidecar this run, and this precondition is NOT satisfied even if
     the resolved path looks correct).
   - `[bootstrap] publicDir resolved=<path> exists=<bool>` — `exists=` must read `true`.
   - All four `[bootstrap] runner binary <name> path=<path> exists=<bool>` lines, one each for
     `legendary`, `gogdl`, `nile`, `comet` — every `exists=` must read `true`.

   These three line shapes are quoted directly from `src/backend/sidecar/bootstrap.ts`, read at
   this plan's authoring time (lines 207, 213, 241):
   ```
   [bootstrap] appRoot resolved=${electronStub.app.getAppPath()} source=${appRootSource}
   [bootstrap] publicDir resolved=${publicDir} exists=${publicDirExists}
   [bootstrap] runner binary ${runnerBinaryName} path=${resolvedRunnerPath} exists=${runnerExists}
   ```
   **A `[bootstrap] SIDECAR ASSET ROOT DEFECT` line anywhere in this session's `gamelib.log`
   BLOCKS the run** (`bootstrap.ts:248`). Items 1-3 (and therefore item 5) must NOT be attempted
   while that line is present. If it fires, treat this precondition as NOT satisfied, stop, and
   route back to a gap cycle.

   **RESULT — [ ]**

5. **Signed OUT of Epic, GOG and Amazon — verified, not assumed.** Do NOT carry any prior run's
   aside state forward: plan 34.5-20's SUMMARY records that session ending with GOG signed back IN
   and Amazon's password reset. Require fresh `ls`/`test -f` on every original and backup path,
   recorded verbatim. If a credential HAS reappeared at its original path, move it aside again by
   the same recoverable procedure: `cp -p` to a NEW, dated suffix (never reuse a prior aside's own
   suffix for a second aside), `cmp` the copy against the original to prove byte-identity, and only
   then remove the original — never delete blind. Check `nile_store/` AND `nile_config/`. Epic is
   signed out too, so the opportunistic item-1 attempt is a genuine from-scratch attempt if it
   happens.

   **RESULT — RECORDED during PRE-GATE PREPARATION, 2026-08-02 (not the live session). Record
   only — nothing moved, re-asided, or deleted; that decision belongs to the live run.**

   ```
   Epic (legendary):
   $ test -f ".../legendaryConfig/legendary/user.json"                       -> ABSENT
   $ test -f ".../legendaryConfig/legendary/user.json.pre-34.5-15-gate.bak"  -> EXISTS (6266 bytes, mtime Jul 29 07:21:19 2026)
   -> Epic is SIGNED OUT at its original path. Backup survives, untouched.

   GOG (gog_store):
   $ test -f ".../gog_store/auth.json"                       -> EXISTS (478 bytes, mtime Aug  2 17:06:00 2026)
   $ test -f ".../gog_store/auth.json.pre-34.5-15-gate.bak"  -> EXISTS (478 bytes, mtime Jul 29 21:27:32 2026)
   -> GOG credential HAS REAPPEARED at its original path — GOG is currently SIGNED IN. This is
      exactly the risk this precondition names, citing 34.5-20's SUMMARY ("session ending with GOG
      signed back IN"): confirmed still true as of now. NOT re-asided by this preparation — the
      contract's own re-aside procedure (new dated suffix, `cmp` byte-identity, only then remove
      the original) is explicitly the live operator's call, not this preparation's.

   Amazon (nile — both nile_store/ AND nile_config/nile/ checked):
   $ test -f ".../nile_config/nile/user.json"                       -> ABSENT
   $ test -f ".../nile_config/nile/user.json.pre-34.5-15-gate.bak"  -> ABSENT
   $ ls ".../nile_store/"                                            -> EMPTY (no files at all)
   -> Amazon is SIGNED OUT with NO backup of any kind at either location. Consistent with the
      plan's note that this session ended with Amazon's password reset — nothing survived.
   ```

   **Precondition NOT fully satisfied as authored — GOG is signed IN, not signed out.** This is a
   real, current-state finding the live session must act on before item 2 (GOG "from scratch")
   can proceed: the developer needs to re-aside `gog_store/auth.json` (new dated suffix + `cmp`
   proof) during their own live precondition-5 pass. Epic and Amazon are both genuinely signed out
   and need no live action for this precondition's purpose.

6. **`npm run test:ci`, `npx tsc --noEmit`, `cd src-tauri && cargo check`, `cargo test`, `python3
   ported-channels-gate.py`** — all at the HEAD the gate runs on, exit codes recorded VERBATIM, any
   non-zero explained rather than rounded to green.

   **The floor is the MEASURED baseline in `34.5-CYCLE5-ROUTING.md` § "Measured baseline"**,
   measured at HEAD `c8f314205`: `npm run test:ci` exit 0 (**181 suites passed / 181 total, 3553
   tests passed / 3553 total**); `npx tsc --noEmit` exit 0; `cd src-tauri && cargo check` exit 0;
   `cargo test` exit 0 (**92 passed; 0 failed; 1 ignored**); `python3 ported-channels-gate.py`
   exit 0, and its `--self-test` exit 0. Copy these numbers in as the floor rather than
   transcribing a number from run 2 (3463 jest / 84 Rust) or from any SUMMARY — that routing
   document's own honest caveat applies here too: the jest count (3553) carries an **unexplained
   +5 delta** versus `.planning/debug/epic-login-non-interactive.md`'s own recorded 3548, and
   `git diff --stat c857ade8e..HEAD` (that debug entry's HEAD through this cycle's measured HEAD)
   shows zero changes under `src/` or `src-tauri/` — only `.planning/` documents. The routing
   document records this as an open discrepancy, not a fabricated explanation, and this
   precondition must not launder it away.

   **A count AT OR BELOW 3553 jest / 92 Rust (0 failed, 1 ignored) at gate time means HEAD is
   stale and must be explained before the run proceeds. A count ABOVE this floor also needs an
   explanation, because no plan in this cycle (34.5-38 through 34.5-42) lands any tests.**

   **RESULT — RECORDED during PRE-GATE PREPARATION, 2026-08-02 (not the live session).**

   HEAD at measurement time: `645a57a8c` — NOT `c8f314205` (the routing document's measured
   baseline HEAD). Four commits landed since (`0c70cc3bf`..`645a57a8c`, plans 34.5-38/39/40);
   `git diff --stat c8f314205..645a57a8c` confirms every changed path is under `.planning/`
   (`ROADMAP.md`, `STATE.md`, four new 34.5-38/39/40 planning docs) — **zero** changes under
   `src/` or `src-tauri/`. Matches the routing document's own prediction exactly, so the floor
   should hold unchanged.

   ```
   $ npm run test:ci
   Test Suites: 181 passed, 181 total
   Tests:       3553 passed, 3553 total
   Snapshots:   0 total
   exit 0

   $ npx tsc --noEmit
   (clean — only an unrelated npm advisory line: "Unknown project config \"node-linker\"")
   exit 0

   $ cd src-tauri && cargo check
   Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.40s
   exit 0

   $ cd src-tauri && cargo test
   test result: ok. 92 passed; 0 failed; 1 ignored; 0 measured; 0 filtered out; finished in 0.06s
   exit 0

   $ python3 ported-channels-gate.py
   OK: all 38 channels declared exactly once with correct send/invoke kind, every proof level
   permitted with an explicit PENDING/PASS/FAIL state on every LIVE cell, per-module counts are
   11/9/7/11, the 38+3+16=57 arithmetic reconciles against IPC-PORT-INVENTORY.md with no name in
   two buckets, the residuals and obligations sections carry their required citations, and
   live-gate item 4 is scoped to exactly {addToSteam, addShortcut} (REQ-34.5-11).
   exit 0

   $ python3 ported-channels-gate.py --self-test
   SELF-TEST OK: every assertion rejects its corresponding bad input.
   exit 0
   ```

   **Comparison against `34.5-CYCLE5-ROUTING.md` § "Measured baseline": EXACT MATCH on every
   number** — 181/181 suites, 3553/3553 jest tests, tsc clean, cargo check clean, cargo test
   92 passed/0 failed/1 ignored, gate script clean, self-test clean. No deviation in either
   direction: the count is neither at-or-below the floor (stale HEAD) nor above it (unexplained
   new tests) — it is identical, consistent with HEAD moving by doc-only commits. **Precondition
   SATISFIED. Must still be re-run at live gate time per the contract's own instruction — this
   entry does not substitute for that.**

7. **`GAMELIB_SHELL_EXE` read back from the SIDECAR's own receipt line** in `gamelib.log`
   (`[bootstrap] GAMELIB_SHELL_EXE received=${shellExeReceived}`, `bootstrap.ts:165`), never the
   Rust shell's `eprintln!` (which reaches only the terminal and structurally cannot distinguish
   "set on the child env" from "visible to `pathShim`"). **An `<UNSET>` reading
   (`bootstrap.ts:170`, warning level) BLOCKS item 4.** Carry the dev-versus-packaged caveat
   verbatim: this value comes from a **dev** build (`src-tauri/target/debug/gamelib-shell`); a
   packaged build resolves through `spawn_sidecar_packaged` (`main.rs:1268`) to a different path
   from a different code path. **A dev PASS on item 4 never retires the packaged half of
   Assumption A2, and never retires residual `R-34.5-G1-PKG`** (`34.5-APP-ROOT-SWEEP.md` § 3,
   `34.5-16-SUMMARY.md`) — both stay open regardless of this run's outcome.

   **RESULT — [ ]**

8. **NEW — the cycle-3 fixes are present in this build, proven from source at HEAD, not from a
   SUMMARY.** Use comment-filtered greps (`grep -v '^\s*//' | grep -v '^\s*\*'`) so a doc comment
   cannot satisfy a code assertion:
   - `oauthCaptureLogin` present in `main.rs`'s `LONG_RUNNING_CHANNELS` array (confirmed present
     at authoring time, `main.rs:152`, alongside a second long-running channel added at `:156`).
   - `capture-transport-failed` present in `useTauriOAuthLogin.ts` (confirmed present at authoring
     time, line 212: `phase=error capture-transport-failed message=${message}`).
   - The completion-callback wiring present at `WebView/index.tsx` (confirmed present at authoring
     time: `completeOAuthLogin` destructured from context at line 49, passed as the hook's second
     argument at line 80: `useTauriOAuthLogin(<runner arg>, completeOAuthLogin)`).

   **RESULT — RECORDED during PRE-GATE PREPARATION, 2026-08-02 (not the live session).**
   Comment-filtered greps (`grep -v '^\s*//' | grep -v '^\s*\*'`) run against HEAD `645a57a8c`.

   ```
   $ grep -v '^\s*//' src-tauri/src/main.rs | grep -v '^\s*\*' | grep -n "oauthCaptureLogin"
   66:    "oauthCaptureLogin",
   -> PRESENT, inside the LONG_RUNNING_CHANNELS array (declared main.rs:57).

   $ grep -v '^\s*//' src/frontend/screens/WebView/useTauriOAuthLogin.ts | grep -v '^\s*\*' | grep -n "capture-transport-failed"
   137:          `[useTauriOAuthLogin] runner=${activeRunner} phase=error capture-transport-failed message=${message}`
   -> PRESENT. (Actual file path is src/frontend/screens/WebView/useTauriOAuthLogin.ts, confirmed
      by `find`; content matches the plan's citation.)

   $ grep -v '^\s*//' src/frontend/screens/WebView/index.tsx | grep -v '^\s*\*' | grep -n "completeOAuthLogin"
   49:  const { epic, gog, amazon, zoom, humble, connectivity, completeOAuthLogin } =
   78-81: useTauriOAuthLogin(<runner arg>, completeOAuthLogin) call — completeOAuthLogin passed as
          the hook's second argument.
   -> PRESENT. Line numbers have drifted slightly from the plan's authoring-time citation (line 80
      for both the destructure and the argument); the destructure is at line 49 and the argument
      passage spans the call at lines 78-81 — content and wiring intact, drift is cosmetic.
   ```

   **No hard STOP.** All three cycle-3 fixes are present in this build, confirmed from source at
   HEAD, not from a SUMMARY. Precondition SATISFIED.

9. **NEW — the cycle-4 fixes are present in this build, proven from source at HEAD.** Record
   beside this precondition that **presence is NOT proof of effect** — the last two constants sit
   on a code path this run deliberately does not exercise (precondition 10 below):
   - The `handleRefreshLibrary` gate in `steamFlowRegistration.ts` excluding BOTH `undefined` and
     `null` (confirmed present at authoring time, line 117: `if (rawRunner !== undefined &&
     rawRunner !== null && rawRunner !== 'all')`).
   - `[refreshLibrary] runner=${runnerScope} origin=${origin}` at `GlobalState.tsx:1012` (confirmed
     present at authoring time).
   - The cancellation-aware `safeSetState` helper in `useTauriOAuthLogin.ts` (confirmed present at
     authoring time, lines 152-156).
   - `KEYRING_READ_TIMEOUT` at 45s in `src-tauri/src/main.rs` (confirmed present at authoring time,
     line 1100: `const KEYRING_READ_TIMEOUT: Duration = Duration::from_secs(45);`).
   - `KEYRING_FAILURE_MEMO_MS` at 120s in `keyringTokenStore.ts` (confirmed present at authoring
     time, line 55: `const KEYRING_FAILURE_MEMO_MS = 120_000`).

   **RESULT — RECORDED during PRE-GATE PREPARATION, 2026-08-02 (not the live session). Presence
   is NOT proof of effect — recorded per this precondition's own instruction, since the last two
   items sit on a keyring-read code path this run's `GAMELIB_DEV_SECRET_VAULT=1` deliberately
   bypasses.**

   ```
   $ grep -v '^\s*//' src/backend/sidecar/steamFlowRegistration.ts | grep -v '^\s*\*' | grep -n "rawRunner !== undefined"
   19:  if (rawRunner !== undefined && rawRunner !== null && rawRunner !== 'all') {
   -> PRESENT, excludes BOTH undefined and null.

   $ grep -v '^\s*//' src/frontend/state/GlobalState.tsx | grep -v '^\s*\*' | grep -n "\[refreshLibrary\] runner="
   936:      `[refreshLibrary] runner=${runnerScope} origin=${origin}`
   -> PRESENT. (Line drifted from the plan's authoring-time citation of line 1012 to line 936;
      content and presence confirmed, drift is cosmetic.)

   $ grep -v '^\s*//' src/frontend/screens/WebView/useTauriOAuthLogin.ts | grep -v '^\s*\*' | grep -n "safeSetState"
   84:    function safeSetState(next: TauriOAuthLoginState): void {
   (called at lines 91, 118, 140, 156, 162, 168, 177, 229, 235, 249, 253)
   -> PRESENT. (Helper is defined at line 84, not the plan's cited 152-156; the helper and its
      cancellation-gating call sites are all confirmed present, drift is cosmetic.)

   $ grep -v '^\s*//' src-tauri/src/main.rs | grep -v '^\s*\*' | grep -n "KEYRING_READ_TIMEOUT"
   625:const KEYRING_READ_TIMEOUT: Duration = Duration::from_secs(45);
   -> PRESENT, at 45s. (Line drifted from the plan's cited 1100 to 625; value confirmed.)

   $ grep -v '^\s*//' src/backend/sidecar/keyringTokenStore.ts | grep -v '^\s*\*' | grep -n "KEYRING_FAILURE_MEMO_MS"
   17:const KEYRING_FAILURE_MEMO_MS = 120_000
   -> PRESENT, at 120s (120_000ms). (Declaration line drifted from the plan's cited 55 to 17;
      value confirmed 120_000.)
   ```

   **No hard STOP.** All five cycle-4 fixes are present in this build, confirmed from source at
   HEAD. As required: this presence is NOT claimed as proof of effect — the raised
   `KEYRING_READ_TIMEOUT`/`KEYRING_FAILURE_MEMO_MS` constants sit on the keyring-read path this
   run's dev-secret-vault mode does not exercise (see precondition 10, a live-session precondition
   left EMPTY here). Precondition SATISFIED.

10. **NEW — the secret-store arm is RECORDED, not assumed.** Per D-CYCLE5-B, `GAMELIB_DEV_SECRET_VAULT=1`
    is exported for this run. Record: the exact env-var value used; the
    `[bootstrap] secret stores: dev-vault` line from `gamelib.log` (`bootstrap.ts:356`); and the
    `[dev-secret-vault] INSTALLED — secrets are stored in PLAINTEXT at <path> — NEVER use in
    production` warning (`devSecretVault.ts:273-276`). **Satisfying this precondition is exactly
    what keeps U-34.5-01 and U-34.5-10 OPEN** — recorded here rather than left inferred. A
    `[bootstrap] secret stores: keyring` reading instead (`bootstrap.ts:367`) means the vault did
    NOT install (the guard is an exact-string `=== '1'` match, `devSecretVault.ts:232`) — that is
    a deviation from D-CYCLE5-B and must be RECORDED as such, not silently accepted; if it
    happens, the run may continue, and U-34.5-01 and U-34.5-10 then become eligible if their own
    observations (see the known-holes table above) are gathered.

    **RESULT — [ ]**

11. **NEW — the item-4/item-5 preflight is consumed AND re-confirmed at gate time.**
    `34.5-G6-ITEM45-PREFLIGHT.md` exists (authored by plan 34.5-39), and its target verification is
    RE-RUN now (the install may have changed since plan 34.5-39): re-check
    `gog_store/installed.json` and the on-disk `~/GameLib/Alan Wake` directory for item 4's target
    (`appName` `1207659037`), and re-check `tools/wine/` for item 5's prerequisite (a `wine`-type
    Wine version download — NOT MET as of the preflight; must be downloaded via the Wine Manager
    screen before item 5 can be attempted for real). This precondition exists so a third skip of
    items 4/5 requires an explicit, recorded refusal rather than a drift — items 4 and 5 were NOT
    ATTEMPTED on both prior runs.

    **RESULT — RECORDED during PRE-GATE PREPARATION, 2026-08-02 (not the live session). The
    contract still requires this re-confirmed AGAIN at gate time — this entry does not substitute
    for that.**

    Item-4 target (GOG Alan Wake), re-verified now:
    ```
    $ cat ".../gog_store/installed.json"
    appName 1207659037, runner gog, install_path "/Users/graysonmitchell/GameLib/Alan Wake",
    installedDLCs [] — unchanged from the preflight.

    $ ls -la "$HOME/GameLib/Alan Wake"                          -> 31 entries, incl. alanwake.exe (3,043,680 bytes)
    $ test -f "$HOME/GameLib/Alan Wake/alanwake.exe"            -> EXISTS
    ```
    Target UNCHANGED from plan 34.5-39's preflight. Still VERIFIED installed, both via
    `installed.json` and an on-disk cross-check.

    Steam userdata / `shortcuts.vdf` (path REDACTED per T-34.5-C5-06):
    ```
    $ ls "$HOME/Library/Application Support/Steam/userdata/"    -> exactly one account-id dir
    $ test -f "userdata/<REDACTED>/config/shortcuts.vdf"        -> ABSENT
    ```
    Matches the preflight's own pre-run state exactly — `shortcuts.vdf` still does not exist. Its
    first appearance after `addToSteam` remains available as item 4(a)'s evidence.

    Item-5 prerequisite state, re-checked now (per this plan's own additional instruction):
    ```
    $ find "$HOME/Library/Application Support/GameLib/tools/wine" -maxdepth 1
    /Users/graysonmitchell/Library/Application Support/GameLib/tools/wine
    (directory exists; ZERO entries beneath it — still empty, no wine-type Wine version downloaded)

    $ grep wineVersion.type in each non-empty GamesConfig/*.json
    1207659037.json  -> "name": "Game-Porting-Toolkit-latest", "type": "toolkit"
    251570.json      -> "name": "Game-Porting-Toolkit-latest", "type": "toolkit"
    (every other GamesConfig/*.json on this machine is empty — no Wine version assigned at all)
    ```
    **STILL TRUE, unchanged from plan 34.5-39's finding.** Zero `wine`-type Wine versions
    downloaded; both of this machine's two configured non-Steam games remain `type: "toolkit"`
    (Game Porting Toolkit), which both hides the DXVK toggle in the UI (`AutoDXVK.tsx:24-30`) and
    short-circuits before `runWineCommand` is reached (`tools/index.ts:210-217`). **Item 5's
    primary/stronger GOG-target reading remains UNATTEMPTABLE without a live setup step** —
    downloading a `wine`-type Wine version via the Wine Manager screen, then explicitly assigning
    it to a non-Steam game. The login-independent `sideload` fallback target (per the preflight's
    own analysis) is unaffected by this finding and remains available regardless of items 1-3's
    outcome.

    Informational only (not part of this precondition; recorded because it materially affects the
    developer's own live precondition-2 kill sweep — precondition 2 itself was NOT touched by this
    preparation, per the scope fence): `ps aux | grep -E "gamelib-shell|sidecar.js"` shows a
    CURRENTLY RUNNING pair — `gamelib-shell` PID 54152 and `sidecar.js` PID 54211, started ~5:11PM
    today. Not killed. The developer should check whether this is their own already-open session
    before running precondition 2's kill sweep live.

---

## Items

**Tick off in order, after every precondition above is satisfied. Each item's Result slot is empty
below and is filled ONLY by plan 34.5-41, never by this authoring plan.**

### Item 1 — Epic login (REQ-34.5-12) — enters BLOCKED per D-CYCLE5-A; opportunistic attempt required if the form is interactive

**Disposition:** this item enters as **BLOCKED**, per the "Item 1 enters BLOCKED" section above.
Restated inline so the operator does not have to hunt for it:

- The cause: the CONFIRMED, STATIC-ONLY-fixed post-auth root cause (`U-34.5-11`) plus the PARKED
  pre-auth blocker (`developer_override_2026_08_02T19_00_00`, measured 403 x1 / `xhr.timeout` x2 /
  success x1, zero completed Tauri Epic logins ever).
- **Opportunistic-attempt clause: if the Epic login form renders and accepts input this run, the
  operator MUST attempt a real login and record PASS or FAIL — not BLOCKED.** Check FIRST whether
  `epic_oauth_redirect_observer_script`'s `window.fetch` wrapper was injected (the debug file's own
  prime suspect for the 403). Record: (a) whether `/id/api/email/exists` returns 403, times out, or
  succeeds; (b) whether the observer script injected.
- **A prediction is not a result** — record what actually happens, not what was expected.
- Diagnosis-only fence: no change to `USER_AGENTS`, `EPIC_LOGIN_URL` or `matchOAuthRedirect` is
  authorised by this gate, regardless of outcome.

**What to observe if the opportunistic attempt is made:** the login window opens on Epic's real
login origin, the form accepts credential entry, the sidecar's `oauthCaptureLogin` channel reports
`status=captured` for `legendary` (`oauthLoginCapture.ts:231`), a
`[useTauriOAuthLogin] runner=legendary phase=idle (login completed, library refresh triggered)`
line appears (`useTauriOAuthLogin.ts:320`), and the library is populated with Epic titles.

**Evidence required (if attempted):** all five of items 2/3's clauses (a)-(e) below, applied to
`legendary`, PLUS the two pre-auth observations above.

**LIVE RUN result — recorded by plan 34.5-41, re-verified against `gamelib.log.34.5-g6-gate3-sessionA`
(21:23:55–21:33:12), the preserved copy of this run's rotated log:**

The opportunistic-attempt clause was exercised: Epic was opened, and the operator drove it — TWO
separate login-capture windows, not one:
```
(21:26:10) [oauthLoginCapture] runner=legendary label=loginwin-0-18c7f2d41e3924c8-23d1ebe4
(21:26:10) [oauthLoginCapture] runner=legendary nav host=www.epicgames.com
(21:27:27) [useTauriOAuthLogin] runner=legendary phase=teardown inflight=true
(21:27:33) [oauthLoginCapture] runner=legendary label=loginwin-1-18c7f2e784f67ec8-9581313e
(21:27:33) [oauthLoginCapture] runner=legendary nav host=www.epicgames.com
(21:31:10) [oauthLoginCapture] runner=legendary status=timeout
(21:32:33) [oauthLoginCapture] runner=legendary status=timeout
```
Confirmed by direct grep against the log file: exactly TWO `status=timeout` lines (`grep -c` → 2)
and ZERO `status=captured` lines for `legendary` (`grep -c` → 0).

**Two required extra observations:**
(a) `/id/api/email/exists` — operator devtools observation, NOT present in `gamelib.log` (this is a
    browser-devtools-network observation, not a sidecar log line, so it cannot be independently
    re-verified from the log file the way the lines above were): returned **403**. Recorded as its
    own distinct outcome — per `.planning/debug/epic-login-non-interactive.md`'s own
    `three_outcome_reframe` instruction, this 403 is NOT collapsed into the same bucket as the two
    `xhr.timeout` backend results above; they are different phenomena.
(b) whether `epic_oauth_redirect_observer_script` was injected — checked FIRST per the debug file's
    own NEW RISK: **NOT OBSERVED**. `grep -c "observer_script"` against the full session-A log
    returns 0 — no line, positive or negative, addresses it. Recorded as NOT OBSERVED, never as
    absent or present.

**Disposition, and why it stays BLOCKED rather than converting to FAIL:** the form did render and
accept input — the email-exists check firing at all proves credential entry began — but the SAME
parked pre-authentication defect (`developer_override_2026_08_02T19_00_00`) intercepted the flow
immediately afterward (the 403 is that defect's own signature reproducing itself), before a full
credentialed login attempt could be driven through to a state distinguishable from the entry cause
itself. Both timeouts are consistent with the flow having stalled at that same point on both
attempts. Recording this as FAIL would claim a completed, independently-adjudicated login attempt
that did not, in fact, happen — the parked blocker fired again, unchanged, exactly as its own
measured shape (403 x1 / `xhr.timeout` x2 / success x1, prior to this run) predicts it can. Per this
run's own arithmetic (Task 3), item 1 counts toward `items_blocked`, not `items_failed`.

**Diagnosis-only fence respected:** no change was made to `USER_AGENTS`, `EPIC_LOGIN_URL` or
`matchOAuthRedirect` during this run.

**Result:** [ ] PASS   [ ] FAIL   [x] **BLOCKED**   [ ] NOT ATTEMPTED — *converted-disposition check
performed per the opportunistic-attempt clause; the entry disposition (BLOCKED) is CONFIRMED, not
merely defaulted, because the live attempt reproduced the exact parked cause rather than producing a
distinguishable pass/fail outcome of its own.*

### Items 1-3 (Epic / GOG / Amazon) — common evidence clauses (REQ-34.5-12; REQ-34.5-02 anchors item 3)

For each of items 1 (if opportunistically attempted), 2 and 3, require ALL of:

  (a) the `[oauthLoginCapture] runner=<runner> status=captured` line (`oauthLoginCapture.ts:231`)
      — the backend's own report, necessary and NOT sufficient;
  (b) a `[useTauriOAuthLogin] runner=<runner> phase=...` line for the SAME runner
      (`useTauriOAuthLogin.ts`, multiple call sites). **Its absence is the exact signature of run
      2's defect** — six backend terminal outcomes across items 2/3 produced zero such lines. This
      is a DIFFERENT component reporting, which is why it counts. **The specific terminal value a
      PASS requires:** `phase=idle (login completed, library refresh triggered)`
      (`useTauriOAuthLogin.ts:320`);
  (c) the runner's CLI auth invocation appearing in the log AFTER the capture timestamp
      (`legendary` / `gogdl auth` / `nile auth --login` as applicable);
  (d) the library **populated** in the UI — named games visible — AND the runner present in the
      Library filter options. An empty library after a "successful" login is a FAIL, not a partial
      pass; run 2's developer report named the missing GOG filter option specifically;
  (e) the account surface showing the signed-in username.

### Item 2 — GOG login completed FROM SCRATCH, populated library (REQ-34.5-12)

**What changed since run 2:** 34.5-33's runner-aware `refreshLibrary` dispatch and the same-day
`null`-vs-`undefined` JSON-transport fix (`steamFlowRegistration.ts:117`); 34.5-34's
cancellation-aware `safeSetState`; 34.5-26's completion-callback wiring.

**What to do:** With precondition 5 satisfied for GOG (no stored credential), drive a real GOG
login through the seam, authenticating with real credentials.

**Evidence required:** clauses (a)-(e) above, applied to `gog`, PLUS:

  (f) `refreshLibrary complete runner=gog managers=1` in `gamelib.log`
      (`steamFlowRegistration.ts:151`) — a backend line emitted by a DIFFERENT module than the
      capture, following the login;
  (g) ZERO `Refreshing undefined Library` lines across the whole session, and every
      `[refreshLibrary]` line (`GlobalState.tsx:1012`) carrying a non-`unknown` `origin=` value;
  (h) no `refreshLibrary failed runner=...` line anywhere in the session
      (`steamFlowRegistration.ts:125,144`) — the `null`-transport regression fixed 2026-08-01
      emitted `refreshLibrary failed runner=null: unknown runner` at mount, so its absence is part
      of item 2's evidence.

Also require, for U-34.5-09: **at least TWO consecutive GOG login attempts** in the same session,
each producing its own `[useTauriOAuthLogin] phase=...` line, with zero attempts silently
returning the developer to Manage Accounts.

**LIVE RUN result — recorded by plan 34.5-41, re-verified against `gamelib.log.34.5-g6-gate3-sessionA`
directly:**

(a) `status=captured` — TWICE, confirmed:
```
(21:30:30) [oauthLoginCapture] runner=gog status=captured
(21:31:41) [oauthLoginCapture] runner=gog status=captured
```

(b) `[useTauriOAuthLogin] runner=gog phase=idle (login completed, library refresh triggered)` —
TWICE, confirmed. **This is the exact clause run 2's evidence lacked entirely across six backend
terminal outcomes — F-34.5-G6-02 appears CLOSED:**
```
(21:30:41) [useTauriOAuthLogin] runner=gog phase=idle (login completed, library refresh triggered)
(21:31:51) [useTauriOAuthLogin] runner=gog phase=idle (login completed, library refresh triggered)
```

**ANOMALY, recorded without diagnosis:** a `phase=cancelled-midflight at=auth authStatus=done` line
fires immediately BEFORE each `phase=idle` line, both times:
```
(21:30:41) [useTauriOAuthLogin] runner=gog phase=cancelled-midflight at=auth authStatus=done
(21:30:41) [useTauriOAuthLogin] runner=gog phase=idle (login completed, library refresh triggered)
(21:31:51) [useTauriOAuthLogin] runner=gog phase=cancelled-midflight at=auth authStatus=done
(21:31:51) [useTauriOAuthLogin] runner=gog phase=idle (login completed, library refresh triggered)
```

(c) `gogdl auth` CLI invocations after capture — confirmed, and MORE extensively than initially
summarized: invocations begin at (21:30:36), immediately after the FIRST capture (21:30:30), and
continue through (21:30:41), (21:30:45), (21:30:53), (21:30:58); a second cluster begins at
(21:31:46), immediately after the SECOND capture (21:31:41), continuing through (21:31:51),
(21:31:56), (21:32:02), (21:32:07), (21:32:47), (21:32:52), (21:32:58), (21:33:03), (21:33:08).
**Discrepancy note:** the evidence handed to this plan stated invocations began "from (21:31:46)
onward" only; the file itself shows an earlier, equally-valid cluster starting at (21:30:36)
following the FIRST capture. Recording the file's fuller version per this plan's own instruction to
prefer the file over a summarized claim when they disagree.

(d) **UI library NOT populated — operator observation ("no games showed"). This clause FAILS.**

(e) account surface username: **NOT RECORDED** this run.

(f) `refreshLibrary complete runner=gog managers=1` — confirmed FIVE times, exact timestamps:
(21:30:53), (21:31:03), (21:32:02), (21:32:12), (21:33:12).

(g) ZERO `Refreshing undefined Library` lines, confirmed (`grep -c` → 0). BUT clause (g) also
requires every `[refreshLibrary]` line to carry a non-`unknown` `origin=` — this does NOT hold:
`(21:32:47) [refreshLibrary] runner=all origin=unknown` fires in session A (confirmed by direct
grep). **Clause (g) FAILS on its second half.**

(h) ZERO `refreshLibrary failed runner=` lines anywhere in the session, confirmed (`grep -c` → 0).

**U-34.5-09's two-consecutive-attempts requirement: MET.** Two full attempts occurred (21:30 and
21:31), each producing its own `phase=...` sequence ending `phase=idle`, with zero silent returns to
Manage Accounts — see the "Known holes — RESOLVED" table above, where this retires U-34.5-09
independently of this item's overall result.

`[refreshLibrary] runner=gog origin=login-success` lines confirmed at (21:30:41) and (21:31:51).

**Backend data DID land — this is a FRONTEND RENDER failure, not data loss.**
`~/Library/Application Support/GameLib/store_cache/gog_library.json`: confirmed independently by
this plan — 9867 bytes, mtime 21:33, containing exactly 7 games (Galaxy Common Redistributables,
Endless Sky, Empire Earth Gold Edition, Fallout: London, Alan Wake, Hero of the Kingdom: The Lost
Tales 3, Balrum). The failure sits downstream of a fully successful backend chain — a THIRD distinct
failure layer for this phase, after run 1's spawn/publicDir defect and run 2's capture-to-UI
propagation defect, both now closed.

**U-34.5-05 evidence:** the log shows a nav-origin change for GOG, `nav host=login.gog.com`
(21:30:02) → `nav host=embed.gog.com` (21:30:30). This is a LOG-OBSERVED nav-origin change, not the
title-bar TEXT transcription U-34.5-05's row requires — that transcription was not captured this run
(see the "Known holes — RESOLVED" table: U-34.5-05 stays OPEN on its own terms).

**Result:** [ ] PASS   [x] **FAIL**   [ ] BLOCKED   [ ] NOT ATTEMPTED — *clause (d) fails (library
never renders in the UI) and clause (g) fails on its second half (`origin=unknown` occurs once);
backend capture, propagation, dispatch and persistence all succeed, but the item as a whole is a
FAIL per the binding rule that a mutating call's own success report is not proof of user-visible
effect.*

### Item 3 — Amazon login completed FROM SCRATCH, populated library, AND the `www.amazon.com` anchor confirmed (REQ-34.5-02, REQ-34.5-12)

**What changed since run 2:** same 34.5-33/34/26 fixes as item 2, applied to the `nile` runner.

**What to do:** With precondition 5 satisfied for Amazon (no stored credential), drive a real
Amazon Games login through the seam.

**Evidence required:** clauses (a)-(e) above, applied to `nile`, PLUS run 2's sub-clause (b): grep
for `[oauthLoginCapture] runner=nile origin-mismatch origin=` (`oauthLoginCapture.ts:150`) — it
must NOT fire on a clean pass; if it does, Assumption A1 is FALSIFIED and item 3 cannot be a clean
PASS even if login otherwise succeeds. Record the observed origin either way, using run 2's
three-way recording rule verbatim: a clean login completing with the line never firing records A1
as **CONFIRMED**; the line firing records A1 as **FALSIFIED**; the redirect-comparison path never
being reached for any other reason records A1 as **UNTESTED**.

Additionally required:
  - **U-34.5-04:** a transcription of whether the verification-code field's text is legible (run 2
    recorded black-on-black).
  - **U-34.5-05:** a before/after transcription of the login window's title-bar origin text
    CHANGING within a single flow, with GOG's `login.gog.com` → `embed.gog.com` named as the known
    candidate if item 2 provides it first.

**LIVE RUN result — recorded by plan 34.5-41:**

**NOT ATTEMPTED.** The operator ended the session after item 4's root-cause diagnosis (session B
ran 21:42:53–22:00:24; the item-4 shortcut work, its failure, and its root-cause read (the
`nativeImage` sidecar stub) consumed the remainder of that session). No `nile`/Amazon login window
was opened, no `[oauthLoginCapture] runner=nile` line appears anywhere in either preserved log
(confirmed by direct grep: zero matches in both session files), and no `origin-mismatch` grep result
exists to report either way.

**Consequence for the research claims this item exists to test:**
- **Assumption A1 (`www.amazon.com` host anchor) stays UNTESTED** — not confirmed, not falsified.
  The redirect-comparison path was never reached this run, which is the third of the item's own
  three-way recording rule ("the redirect-comparison path never being reached for any other reason
  records A1 as UNTESTED").
- **U-34.5-04 (code-field legibility) stays OPEN** — not gathered, because the code field was never
  reached.

**Result:** [ ] PASS   [ ] FAIL   [ ] BLOCKED   [x] **NOT ATTEMPTED** — *reason: session ended after
item 4's diagnosis before item 3 could be driven; recorded as an explicit refusal, not a silent
skip.*

### Item 4 — `addToSteam` AND the macOS `.app` shortcut, BOTH `exe` call sites (REQ-34.5-01, REQ-34.5-12)

**INLINED from `34.5-G6-ITEM45-PREFLIGHT.md` (plan 34.5-39)** — proving groundwork was referenced
before and the item still got skipped, so it is inlined here rather than merely cross-referenced.

**Target (re-verify at gate time per precondition 11):** GOG *Alan Wake*, `appName` `1207659037`,
`runner` `gog`, installed at `~/GameLib/Alan Wake`. Verified installed via both
`gog_store/installed.json` and a direct `ls` of the install directory (`alanwake.exe` present) as
of the preflight — re-check both again now. **Item 4 needs NO fresh GOG login** — both call sites
are proven runner/login-independent by source read (`nonesteamgame.ts:258-268`,
`shortcuts.ts:227-229`): the `Exe`/launch-command value is a pure function of `getPath('exe')` plus
packaging-format env vars, never `gameInfo.runner` or any auth/session state.

**Steam userdata path (REDACTED per T-34.5-C5-06):** `userdata/<REDACTED>/config/shortcuts.vdf`.
Confirmed ABSENT pre-run by the preflight — its first appearance after `addToSteam` is itself part
of item 4(a)'s evidence.

**Operator warning — quit Steam FIRST, before either invocation.** Steam holds `shortcuts.vdf` in
memory and rewrites it from memory on exit; a write performed while Steam is running will be
clobbered.

**Exact devtools-console invocations:**
```js
window.api.addShortcut('1207659037', 'gog', false)   // send-kind, fire-and-forget
await window.api.addToSteam('1207659037', 'gog')     // invoke-kind, returns boolean
```
`addShortcut` is **send-kind** (`shortcutsFlowRegistration.ts:206`) — a thrown error inside it
reaches no caller (no reject, no timeout, no console line); its evidence must come from the
filesystem (the `.app`'s `run.sh` body) and from `gamelib.log`'s own
`[shortcutsFlowRegistration] addShortcut failed:` line if it fails — never from the absence of a
UI error. `addToSteam` is **invoke-kind** (`shortcutsFlowRegistration.ts:168`) — a thrown error
rejects directly to the `await` in the devtools console.

**Proving ONE call site is not sufficient (research Correction 3).** Both MUST be exercised:

| Call site | What to read back | PASS looks like |
|---|---|---|
| `addToSteam` → `nonesteamgame.ts:258` | The `Exe` field inside the (REDACTED-path) `shortcuts.vdf` | Non-empty, holds the Tauri shell binary's own path. Then, after launching from Steam: a `ps aux` line showing the Tauri shell binary, **never** a bare `node` process. |
| `addShortcut` → `shortcuts.ts:227` | The body of `<shortcut>.app/Contents/MacOS/run.sh` | Reads `<shell exe path> --no-gui gamelib://launch?appName=1207659037&runner=gog`. Then, after double-clicking the `.app`: a `ps aux` line showing the Tauri shell binary, **never** a bare `node` process. |

**Dev-versus-packaged limit, carried verbatim:** this value comes from a **dev** build
(`src-tauri/target/debug/gamelib-shell`, per precondition 7). **Item 4 passing in dev does NOT
transfer to the packaged artifact**; residual `R-34.5-G1-PKG` stays OPEN regardless of this item's
outcome.

**LIVE RUN result — session B, 21:42:53–22:00:24, recorded by plan 34.5-41. First attempt for this
item in the phase's history (`34.5-LIVE-GATE.md` and `-RERUN.md` both recorded item 4 NOT
ATTEMPTED).**

**Precondition 7 held for this session:**
```
(21:42:53) [bootstrap] GAMELIB_SHELL_EXE received=/Users/graysonmitchell/Projects/GameLib/src-tauri/target/debug/gamelib-shell
```
Not `<UNSET>` — item 4 was unblocked.

**`shortcuts.vdf`:** confirmed ABSENT pre-run (precondition 11's re-check). Confirmed PRESENT
post-run. **Discrepancy note:** re-reading the file directly at this plan's Task 3 execution finds
533 bytes, mtime `Aug 2 22:10`; the evidence handed to this plan stated 472 bytes / mtime `22:00:22`.
The log's own `Alan Wake was successfully added to Steam` line is timestamped (22:00:22), consistent
with the smaller/earlier reading; the current on-disk state (533 bytes / 22:10) postdates the gate
session's own log tail (which ends 22:00:24) by roughly ten minutes and most plausibly reflects
Steam's own known rewrite-on-exit behavior (this item's own operator warning: "Steam holds
`shortcuts.vdf` in memory and rewrites it from memory on exit") rather than a second `addToSteam`
call — no second `Adding shortcuts`/`addToSteam` log line exists in either preserved session log.
Recording the file's current version rather than the handed-in figures, and recording the
discrepancy explicitly rather than silently adopting either number.

**VDF read-back (userdata account id REDACTED):**
```
Exe        = "/Users/graysonmitchell/Projects/GameLib/src-tauri/target/debug/gamelib-shell"
StartDir   = "/Users/graysonmitchell/Projects/GameLib/src-tauri"
LaunchOptions = --no-gui --no-sandbox "gamelib://launch?appName=1207659037&runner=gog"
icon       = /Users/graysonmitchell/GameLib/Alan Wake/goggame-1207659037.ico
```
`Exe` **matches precondition 7's `GAMELIB_SHELL_EXE received=` value exactly** — confirmed by direct
comparison of the two strings above.

**`addToSteam` call site — PASS on the narrow `Exe`-correctness sub-clause, but the call itself is
defective:** `await window.api.addToSteam('1207659037','gog')` returned `undefined`, not a boolean —
confirmed as a real defect: `src/frontend/screens/Game/GameSubMenu/index.tsx:219-220` does
`.addToSteam(appName, runner).then((added) => setAddedToSteam(added))`, so the UI's "Added to Steam"
toggle can never flip true even when the underlying write succeeds, as it did here. **This also
proves the contract's binding evidence rule in reverse: a call's silence (no thrown rejection, no
boolean) is not proof of non-effect** — the VDF write succeeded despite the malformed return.

**`addShortcut` call site — structurally dead, root cause confirmed by source read:**
```
(21:59:52) Adding shortcuts for Alan Wake
(21:59:52) Generating macOS shortcut
(21:59:52) [ERROR] Error generating MacOS App
(21:59:52) [ERROR] Error converting icon to icns: TypeError: import_electron20.nativeImage.createFromBuffer is not a function
    at convertPngToICNS (build/main/sidecar.js:18007:48)
    at async generateMacOsApp (build/main/sidecar.js:17958:22)
    at async addShortcuts (build/main/sidecar.js:17890:9)
(21:59:52) Temporary MacOS App removed
```
Confirmed at source: `src/backend/sidecar/electronStub.ts:651-655` exports
`nativeImage = { createFromPath: () => ({}), createFromDataURL: () => ({}), createEmpty: () => ({}) }`
— no `createFromBuffer`, and the three members that DO exist return bare `{}`, so
`convertPngToICNS`'s next chained call (`.resize({width:512}).crop(...).toPNG()`,
`src/backend/shortcuts/shortcuts/shortcuts.ts:259-263`) would throw regardless. The stub's own
comment reads "safe no-ops; out of scope per 27-CONTEXT" (`electronStub.ts:649`). **This makes
`addShortcut` → `generateMacOsApp` → the `run.sh`/`getPath('exe')` call site
(`shortcuts.ts:226-228`) structurally unreachable under the Tauri sidecar for EVERY macOS game — not
target-specific, not icon-format-specific.** Confirmed: no `.app` exists at
`~/Applications/Alan Wake.app` (verified directly, `ls` returns "No such file or directory").

**"Proving one call site is not sufficient" — both call sites WERE exercised, and both are
defective, in different ways:** `addToSteam` writes correctly but reports incorrectly (`undefined`);
`addShortcut` never writes at all (throws before `run.sh` is created).

**Launch from Steam — FAILED**, reproduced manually by the orchestrator running
`./target/debug/gamelib-shell --no-gui --no-sandbox "gamelib://launch?appName=1207659037&runner=gog"`
directly: it booted a WHOLE SECOND GameLib instance (its own sidecar, its own devtools window, its
own store re-init) rather than launching the game. It ignored `--no-gui`, ignored the `gamelib://`
deep link, and performed no single-instance detection. Zero log lines and no new game process
resulted from the Steam-side launch attempt itself.

**Critical distinction, drawn explicitly so it cannot be conflated:** the contract's literal PASS
clause for `addToSteam` — "a `ps aux` line showing the Tauri shell binary, never a bare `node`
process" — is TECHNICALLY SATISFIED (the correct binary IS what starts). But it launches the
LAUNCHER, not the game. **A confirmed sub-clause does not upgrade a failed parent item.**

**Contract/preflight defect found, of the same class the preflight caught for item 5's
toolkit/VKD3D traps:** this machine's global settings are `addDesktopShortcuts: false`,
`addStartMenuShortcuts: false`, `addSteamShortcuts: false` — confirmed by direct read of
`~/Library/Application Support/GameLib/config.json`. `shortcuts.ts:65` and `:71` gate the desktop
and start-menu writes on `if (addDesktopShortcuts || fromMenu)` / `if (addStartMenuShortcuts ||
fromMenu)`; `:43` gates the Steam VDF write on `if (addSteamShortcuts)` ALONE, with no `fromMenu`
override. **The contract's own prescribed invocation, `window.api.addShortcut('1207659037', 'gog',
false)` (`fromMenu=false`), would have been a GUARANTEED NON-EVENT on this machine** — no write, no
error, silent — because `addSteamShortcuts` is false and there is no `fromMenu` escape for that
specific gate. The operator used `true` for the third argument instead of the contract's literal
`false`, which is why any evidence exists at all for this item. Recorded as a DEVIATION from the
contract's own literal invocation, and as a defect in the contract/preflight discovered live.

**`getInstallInfo` unported, blocking the game page entirely:**
`(21:55:31) [GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'getInstallInfo'`.
**Discrepancy note:** the evidence handed to this plan stated this fired "×5" at (21:55:31); direct
`grep` against the preserved session-B log finds **8** occurrences at that exact timestamp, and
**18** occurrences across the whole session. Recording the file's count (8 at 21:55:31, 18 total)
rather than the handed-in "×5," per this plan's own re-verification instruction.

**Dev-vs-packaged limit stands:** `src-tauri/target/debug/gamelib-shell`; `R-34.5-G1-PKG` stays
OPEN.

**Result:** [ ] PASS   [x] **FAIL**   [ ] BLOCKED   [ ] NOT ATTEMPTED — *both `exe` call sites were
exercised (satisfying "proving one is not sufficient"); `addToSteam` writes correctly but returns
`undefined` instead of a boolean; `addShortcut` is structurally dead under the sidecar
(`nativeImage.createFromBuffer` missing); and the one launch attempt that WAS reachable
(`addToSteam`'s VDF) booted a second full GameLib instance instead of the game. The Exe-field
sub-clause is confirmed correct but does not upgrade the parent item's FAIL.*

### Item 5 — `runWineCommand` succeeds for at least one NON-Steam runner (REQ-34.5-03, REQ-34.5-12)

**INLINED from `34.5-G6-ITEM45-PREFLIGHT.md` (plan 34.5-39).** Last run this FAILED BY BLOCKAGE
only — nothing about `runWineCommand`'s own code was exercised or falsified. Research Pitfall 2's
standing claim ("no non-Steam runner has ever executed a Wine command under the sidecar") must NOT
be struck until THIS item records a PASS.

**The one macOS-permitted Wine action:** the DXVK auto-install/restore toggle, dispatched via
**`toggleDXVK`** (`src/backend/sidecar/wineToolsFlowRegistration.ts:186-193`, `ipcMain.handle` —
**invoke-kind**; none of the 9 Wine channels are send-kind, so a thrown error rejects directly to
the caller). VKD3D is excluded on macOS before any file I/O runs
(`tools/index.ts:228`) — a "toggle VKD3D" instruction would be a guaranteed non-event.

**UI path:** per-game Settings → Wine tab → "Auto Install/Update DXVK on Prefix" toggle
(`AutoDXVK.tsx`, mounted at `GamesSettings/index.tsx:187`).

**Runner-eligibility question — resolved, not silently substituted.** `sideload` is a real,
login-independent non-Steam runner (`src/common/types.ts:195`) whose launch path
(`storeManagerCommon/games.ts:254`) calls `runWineCommand` directly for any non-native sideloaded
app, regardless of runner identity. It literally satisfies item 5's own wording ("at least one
NON-Steam runner") and REQ-34.5-03's framing — neither requires the runner to be authenticated.
**Decision, per the preflight's flag to this plan:**
  - **Primary, stronger target: GOG (`gog` runner), if item 2 reaches a real signed-in,
    library-populated state this cycle.** This is what the item's own procedural wording and
    Pitfall 2's own framing (`34.5-RESEARCH.md:467-471`, specifically about storefront-authenticated
    runners) point to, and it retires the *specific* historically-untested gap.
  - **Fallback, login-independent target: `sideload`**, usable regardless of item 1/2/3's outcome.
  - **What each PASS does and does not retire, stated in the same breath:** a GOG-target PASS
    retires BOTH the narrow literal reading of Pitfall 2's claim AND the storefront-specific
    reading its prose actually describes. **A `sideload`-only PASS retires ONLY the narrow literal
    reading** ("at least one non-Steam runner") — it does NOT retire the storefront-specific
    reading, because `sideload` was never blocked by the specific defect Pitfall 2 was scoped
    around (it has no login channel to reject in the first place). Record explicitly which target
    was used and which reading is retired.

**Prerequisites, with verifying commands (NOT MET as of the preflight — re-check at gate time):**

| Prerequisite | Verifying command | State as of preflight |
|---|---|---|
| A `wine`-type (not `toolkit`/`crossover`/`proton`) Wine version downloaded | `find "$HOME/Library/Application Support/GameLib/tools/wine" -maxdepth 1` | NOT MET — zero versions present; must be downloaded via the Wine Manager screen (`window.api.installWineVersion(...)`) BEFORE this item can be attempted. |
| An installed non-Steam game with that Wine version explicitly assigned | `cat "$HOME/Library/Application Support/GameLib/GamesConfig/<appName>.json"`, inspect `wineVersion.type` | NOT MET for any currently-installed game — Alan Wake (`1207659037`) and `251570` are both `type: "toolkit"` (Game Porting Toolkit), which BOTH hides the toggle in the UI (`AutoDXVK.tsx:24-30`) AND short-circuits `DXVK.installRemove` before `runWineCommand` is reached (`tools/index.ts:210-217`) — reusing Alan Wake would be a second guaranteed non-event, for a different reason than the VKD3D-on-macOS trap. |
| A prepared/initialized Wine prefix (`.update-timestamp` present) | `ls "<winePrefix>/.update-timestamp"` | Not applicable until the two above are met. |

**If the prerequisites are STILL unmet at gate time:** the disposition is **NOT ATTEMPTED**, and
the operator must record explicitly which prerequisite blocked it (never a silent skip) — a third
skip of item 5 requires an explicit, recorded refusal, not a drift, per precondition 11.

**Evidence lines, quoted from their emitting source at authoring time:**
  - Dispatch (proves the toolkit/DXMT/mac-tool-selection/valid-prefix guards were all passed):
    `tools/index.ts:369` — `installing dxvk-macOS on... <prefix>` (with `isMac` forcing `tool =
    'dxvk-macOS'`).
  - Early-skip line that must NOT appear (its presence means `runWineCommand` was never reached):
    `tools/index.ts:372` — `dxvk-macOS already installed!`.
  - **The definitive evidence — from `launcher.ts`, fired by `runWineCommand` itself regardless of
    caller:** `launcher.ts:1581` — `Running Wine command: <commandParts joined>`. For the DXVK
    backup path this fires twice, `commandParts` beginning `reg add
    HKEY_CURRENT_USER\Software\Wine\DllOverrides ...`. No special logging flag needs to be enabled.
  - No explicit textual "success" line exists for the backup path — the function ends by silently
    writing the version marker file (`tools/index.ts:515`); do not cite a completion log line the
    action does not produce.

**Observable in-app effect (independent of the log evidence, per the binding evidence rule):**
clicking the toggle sets `installingDxvk` true (label "Please wait...", switch fades), then on a
truthy `toggleDXVK` response the switch flips ON (`AutoDXVK.tsx:42`) and stays there after a
reload. Filesystem-observable: `<winePrefix>/current_dxvk-macOS` should exist with the installed
DXVK version string after a successful backup.

**LIVE RUN result — recorded by plan 34.5-41. First attempt for this item in the phase's history
(`34.5-LIVE-GATE.md` and `-RERUN.md` both recorded item 5 NOT ATTEMPTED).**

**Prerequisite re-verified at gate time (precondition 11), unchanged from the preflight:**
```
$ find "$HOME/Library/Application Support/GameLib/tools/wine" -maxdepth 1
(directory exists; ZERO entries beneath it)
```
No `wine`-type Wine version has been downloaded. Both of this machine's two configured non-Steam
games (`1207659037` GOG Alan Wake, `251570`) remain `wineVersion.type: "toolkit"` (Game Porting
Toolkit) — confirmed by precondition 11's own re-check. This is a hard blocking prerequisite per the
item's own prerequisites table: the `toolkit` type both hides the DXVK toggle in the UI
(`AutoDXVK.tsx:24-30`) and short-circuits `runWineCommand` before it is ever reached
(`tools/index.ts:210-217`).

**Consequence for the target-selection decision:** the primary, stronger target (GOG, contingent on
item 2 reaching a real signed-in, library-populated state) is moot regardless of item 2's outcome,
because the blocking prerequisite (a `wine`-type Wine version assigned to a non-Steam game) was
never satisfied — this is unrelated to item 2's own FAIL. The fallback, login-independent `sideload`
target was also not attempted, for the same reason: no `wine`-type Wine version exists on this
machine to assign to ANY game, sideloaded or not.

**Consequence for the research claim this item exists to test:** research Pitfall 2's standing claim
("no non-Steam runner has ever executed a Wine command under the sidecar") is **NOT retired** — a
FAIL-by-blockage does not retire it, and this is not even a FAIL-by-blockage, it is an explicit,
recorded NOT ATTEMPTED per the item's own instruction ("the disposition is NOT ATTEMPTED, and the
operator must record explicitly which prerequisite blocked it").

**Result:** [ ] PASS   [ ] FAIL   [ ] BLOCKED   [x] **NOT ATTEMPTED** — *reason: blocking prerequisite
unmet and re-verified at gate time — zero `wine`-type Wine versions downloaded; both configured
non-Steam games remain `type: "toolkit"`. This is the third time item 5 has gone unexercised, but
the first time the reason is an explicit, recorded refusal (precondition 11) rather than a silent
skip.*

---

## Verdict

| Item | Subject | Result |
|------|---------|--------|
| 1 | Epic login completed FROM SCRATCH, populated library | **BLOCKED** — opportunistic attempt made (2 windows driven), but the parked pre-auth defect (403 on `/id/api/email/exists`) reproduced itself before a distinguishable PASS/FAIL outcome; 0 `status=captured`, 2 `status=timeout` |
| 2 | GOG login completed FROM SCRATCH, populated library | **FAIL** — backend capture, propagation (`phase=idle` ×2, closing F-34.5-G6-02), CLI auth, dispatch and persistence (7 games in `gog_library.json`) all succeed, but the UI library never renders; a THIRD distinct failure layer, downstream of both prior runs' causes |
| 3 | Amazon login completed FROM SCRATCH, populated library + `www.amazon.com` anchor confirmed | **NOT ATTEMPTED** — session ended after item 4's diagnosis; Assumption A1 stays UNTESTED |
| 4 | `addToSteam` AND macOS `.app` shortcut both launch GameLib, not node (both `exe` call sites) | **FAIL** — `addToSteam` writes the correct `Exe` but returns `undefined` not boolean; `addShortcut` is structurally dead (missing `nativeImage.createFromBuffer` in the sidecar stub); the one reachable launch path boots a second GameLib instance, not the game |
| 5 | `runWineCommand` succeeds for at least one non-Steam runner | **NOT ATTEMPTED** — blocking prerequisite unmet and re-verified at gate time: zero `wine`-type Wine versions downloaded; both configured non-Steam games remain `type: "toolkit"` |

**Overall verdict: FAIL — 0 of 5 clean (0 PASS / 2 FAIL / 1 BLOCKED / 2 NOT ATTEMPTED). Per D-08,
Phase 34.5 does NOT close on this result.**

---

## What this gate falsifies

Four standing claims, each STANDING until this run's own item-level results say otherwise. **The
rule, stated plainly: nothing is struck without the corresponding ITEM passing** — not on the
strength of a sub-clause being independently confirmed while its parent item fails, exactly the
distinction run 2 had to draw for Assumption A1.

1. **That the 34.4.1 login seam carries a real Epic/GOG/Amazon session end-to-end** — eligible on a
   PASS of items 1, 2, and 3 (each independently; a PASS on one does not retire the claim for the
   others). Item 1 is eligible only via the opportunistic-attempt clause. **STANDING** until an
   item PASSes.
2. **The `www.amazon.com` host anchor (research Assumption A1)** — eligible on a PASS of item 3,
   AND ONLY if the origin-mismatch grep line does not fire. A login PASS with the mismatch line
   firing does NOT retire this claim — it FALSIFIES it instead. **STANDING.**
3. **`GAMELIB_SHELL_EXE` correctness at both `exe` call sites (research Assumption A2)** — eligible
   on a PASS of item 4, covering BOTH call sites. A dev PASS never retires the packaged half of A2,
   and never retires residual `R-34.5-G1-PKG` — both stay open regardless of this item's result.
   **STANDING.**
4. **Research Pitfall 2's non-Steam Wine claim** ("no non-Steam runner has ever executed a Wine
   command under the sidecar") — eligible on a PASS of item 5 only, and eligible ONLY to the depth
   the target used actually retires (see item 5's own "what each PASS does and does not retire"
   clause above — a `sideload`-only PASS retires the narrow literal reading, not the
   storefront-specific reading). A FAIL-by-blockage does NOT retire this claim. **STANDING.**

No claim is struck by this run's authoring — this section records the eligibility rule only. If a
future gate re-run (this run itself, executed by plan 34.5-41) PASSes any of items 1-5, THAT is the
point at which the corresponding claim above becomes eligible to be struck, recorded by plan
34.5-41 or 34.5-42, never by this authoring plan.

### Resolution — recorded by plan 34.5-41, after the live run

**Zero items PASSed this run (0 PASS / 2 FAIL / 1 BLOCKED / 2 NOT ATTEMPTED). Per this section's own
binding rule, nothing may be struck without the corresponding ITEM passing — so all four standing
claims remain STANDING.**

1. **34.4.1 login seam end-to-end (Epic/GOG/Amazon)** — **STANDING.** Items 1, 2 and 3 all fell short
   of PASS: item 1 stayed BLOCKED (opportunistic attempt reproduced the parked cause rather than
   passing), item 2 FAILED (UI library render), item 3 was NOT ATTEMPTED. Note the same
   confirmed-sub-clause-vs-failed-parent distinction run 2 drew for Assumption A1 applies here to
   item 2: GOG's backend chain (capture → propagation → CLI auth → dispatch → persistence, 7 games
   landed) is FULLY CONFIRMED working end-to-end at the backend layer — but that confirmation does
   NOT upgrade the claim, because the item's own PASS clause (d) requires the library populated IN
   THE UI, which failed. The claim is about the login seam carrying a session end-to-end in a way the
   user experiences, not about backend persistence alone.
2. **`www.amazon.com` host anchor (Assumption A1)** — **STANDING**, and specifically **UNTESTED**
   this run (not confirmed, not falsified) — item 3 was NOT ATTEMPTED, so the redirect-comparison
   path was never reached at all. This is the third of the item's own three-way recording rule.
3. **`GAMELIB_SHELL_EXE` correctness at both `exe` call sites (Assumption A2)** — **STANDING.** Item
   4 FAILED. Drawing the confirmed-sub-clause-vs-failed-parent distinction explicitly, the way run 2
   had to draw it for A1: the `Exe` field's VALUE is independently confirmed correct (it matches
   `GAMELIB_SHELL_EXE received=` exactly, byte for byte) — but that sub-clause confirmation does NOT
   upgrade item 4 to a PASS, because `addToSteam` itself returns the wrong type (`undefined`, not a
   boolean) and the `addShortcut` call site never produces a working `.app` at all (structurally
   dead on `nativeImage.createFromBuffer`). A correct VALUE inside a WRITE that itself misreports its
   own outcome, sitting beside a call site that cannot write at all, is not the item PASSing. The
   packaged half of A2 and residual `R-34.5-G1-PKG` remain moot — this run never reached the point
   where the dev/packaged distinction would matter, because the dev attempt itself did not PASS.
4. **Research Pitfall 2's non-Steam Wine claim** — **STANDING.** Item 5 was NOT ATTEMPTED (blocking
   prerequisite unmet), which is explicitly named in this section's own text as NOT sufficient to
   retire the claim ("A FAIL-by-blockage does NOT retire this claim" — a fortiori, a NOT ATTEMPTED
   does not either). No target — GOG or `sideload` — was driven.

---

## Arithmetic rule

**`items_passed + items_failed + items_blocked + items_not_attempted` must equal 5 when the run
completes.** BLOCKED and NOT ATTEMPTED each count toward NEITHER `items_passed` NOR
`items_failed`, and the shortfall must be stated explicitly in the frontmatter `notes` field when
the run completes — never quietly rounded to make the arithmetic look clean. Per
`34.5-CYCLE5-ROUTING.md`'s binding note: no criterion in this cycle may presuppose the phase does
not close; the routing table's 5-of-5 branch is recorded EXPECTED-unreachable, not
unreachable-by-construction, precisely because item 1's default BLOCKED disposition is subject to
the opportunistic-attempt clause.

### Resolution — recorded by plan 34.5-41, after the live run

`items_passed=0`, `items_failed=2` (items 2, 4), `items_blocked=1` (item 1), `items_not_attempted=2`
(items 3, 5). `0 + 2 + 1 + 2 = 5 = items_total` — the arithmetic reconciles; no shortfall was rounded
away. This is stated again, explicitly, in the frontmatter `notes` field.

**Per D-08, computed mechanically off these four RECORDED counters (never off item 1's entry
disposition):** `items_passed` is 0, not 5 — this is NOT the clean 5-of-5 case. **Phase 34.5 does
NOT close on this result.** Per `34.5-CYCLE5-ROUTING.md` § "Routing by verdict," this outcome lands
in the row **"any of items 2,3,4,5 FAIL"** (items 2 and 4 both FAILED) — the routing action for that
row is `/gsd-plan-phase 34.5 --gaps` (gap cycle 6). This is a DIFFERENT routing row than "any item
NOT ATTEMPTED" alone would select, because this run also carries two independent FAILs; the FAIL row
takes precedence as the more specific, blocking finding.

---

## New findings (this run) — record, do not diagnose

Per both prior runs' own practice: record observations WITHOUT diagnosing them. Diagnosis belongs
to the SUMMARY and the next gap cycle if one is needed. Use the ID prefix `F-34.5-G6-` continuing
from the existing sequence (currently `-01` through `-06`).

- **F-34.5-G6-07.** `src/backend/sidecar/electronStub.ts`'s `nativeImage` export lacks
  `createFromBuffer`, and its three existing members (`createFromPath`, `createFromDataURL`,
  `createEmpty`) each return a bare `{}`.
- **F-34.5-G6-08.** `await window.api.addToSteam(appName, runner)` returns `undefined`, not a
  boolean, while its underlying `shortcuts.vdf` write succeeds.
- **F-34.5-G6-09.** The Tauri shell binary, launched directly with `--no-gui --no-sandbox` and a
  `gamelib://launch?...` argument, ignores both `--no-gui` and the deep link, and boots a second
  full GameLib instance instead of launching the referenced game; no single-instance detection was
  observed.
- **F-34.5-G6-10.** The `getInstallInfo` IPC channel is both unported (`[GAMELIB_UNPORTED_CHANNEL]
  No handler registered for channel 'getInstallInfo'`, 8 occurrences at (21:55:31) alone, 18 total
  across session B) AND absent from `.planning/IPC-PORT-INVENTORY.md` entirely, despite being a real
  channel (`common/types/ipc.ts:218`, `preload/api/helpers.ts:43`).
- **F-34.5-G6-11.** This machine's global settings (`addDesktopShortcuts: false`,
  `addStartMenuShortcuts: false`, `addSteamShortcuts: false`) mean the contract's own literal
  item-4 console invocation (`window.api.addShortcut('1207659037', 'gog', false)`, `fromMenu=false`)
  would have produced a guaranteed non-event — no write, no error, silent — because
  `shortcuts.ts:43` gates the Steam-VDF write on `addSteamShortcuts` alone, with no `fromMenu`
  escape for that specific branch.
- **F-34.5-G6-12.** GOG's backend chain (capture → CLI auth → dispatch → persistence) fully
  succeeds — 7 games landed in `gog_library.json` — while the Library UI renders nothing for that
  runner: a third distinct failure layer, downstream of both prior runs' own root causes.
- **F-34.5-G6-13.** A `[useTauriOAuthLogin] runner=gog phase=cancelled-midflight at=auth
  authStatus=done` line fires immediately before each successful `phase=idle` line, both times this
  run.
- **F-34.5-G6-14.** `[refreshLibrary] runner=all origin=unknown` still occurs
  ((21:32:47), session A) despite the runner-aware dispatch and `origin=` attribution work landed
  this cycle.
- **F-34.5-G6-15.** `~/Library/Logs/GameLib/gamelib.log` rotated mid-gate; the live file lost this
  run's session-A record entirely. Evidence was recovered from `gamelib.log.old` and preserved
  separately as `gamelib.log.34.5-g6-gate3-sessionA`/`...sessionB`.

---

## What this run nevertheless proved

Stated honestly in both directions, as both prior runs did.

**What genuinely advanced:**
- **Items 4 and 5 each carry a real result for the first time in this phase's history.** Both prior
  runs recorded them NOT ATTEMPTED with no diagnostic content at all. This run's item 4 produced a
  FAIL with three independently-confirmed structural defects (F-34.5-G6-07/08/09); item 5 produced a
  NOT ATTEMPTED with its blocking prerequisite explicitly named and re-verified at gate time, not
  silently skipped.
- **F-34.5-G6-02 appears CLOSED.** The exact clause run 2's evidence lacked entirely — a
  `[useTauriOAuthLogin]` `phase=...` line for a runner whose backend reported `status=captured` — now
  fires twice for GOG, ending `phase=idle (login completed, library refresh triggered)` both times.
- **U-34.5-09 retires.** The mid-flight-teardown propagation mechanism (plan 34.5-34) is now
  live-proven: two consecutive GOG login attempts, each surviving teardown and reaching `phase=idle`
  after `status=captured`, with zero silent returns to Manage Accounts.
- **The GOG backend chain is now fully proven end-to-end at the persistence layer** — capture →
  runner-CLI auth → runner-aware `refreshLibrary` dispatch → `gog_library.json` written with 7 real
  games — a genuine advance versus run 2, where nothing downstream of capture ever fired at all.
- **The defect surface for GOG has narrowed and moved, three times running.** Run 1 failed at
  process spawn (publicDir). Run 2 failed at capture-to-propagation (nothing consumed the capture).
  This run's GOG backend chain is now fully sound; the sole remaining defect is a frontend render
  layer that neither of the first two runs could even reach. Each cycle's fix work correctly closed
  the PREVIOUS run's defect and, each time, uncovered a new one further downstream — consistent with
  this phase's own standing lesson that a green test suite catches nothing a human driving the UI
  would have found.
- **Item 4's failure, though a FAIL, surfaced three structural defects with blast radius wider than
  this one item** (F-34.5-G6-07/08/09) — the `nativeImage` stub gap blocks EVERY macOS `.app`
  shortcut for EVERY game, not just Alan Wake; the shell's ignorance of `--no-gui`/deep-links and
  lack of single-instance detection affects every non-Steam launch-from-Steam attempt.
- **A gate contract defect of its own was found live** (F-34.5-G6-11): the contract's own literal
  item-4 console invocation would have been a silent non-event on this exact machine's settings, of
  the same class the preflight caught for item 5's toolkit/VKD3D traps. Preflight discipline
  correctly caught one trap; this one it did not.

**What did NOT advance, stated just as plainly:**
- **Epic (item 1) is exactly where it was.** The opportunistic attempt was made and reproduced the
  identical parked cause (a 403 on `/id/api/email/exists`) rather than producing new information
  about it. U-34.5-06 and U-34.5-11 remain OPEN, unretired, exactly as before this run.
- **U-34.5-01 and U-34.5-10 remain OPEN**, and this run adds a small negative data point rather than
  progress toward retiring them: session A's `keyring` arm (itself a deviation from D-CYCLE5-B) ran
  two real `keyring_get` reads, and BOTH timed out rather than succeeding — the raised 45s/120s
  constants from plan 34.5-35 were exercised on a real Keychain path for the first time this cycle,
  and did not produce the single-approval-prompt success either row requires.
- **Amazon (item 3) and Wine (item 5) both remain formally UNTESTED/NOT ATTEMPTED for their own
  underlying research claims** (Assumption A1, Pitfall 2) — items 4 and 5 finally getting SOME result
  does not mean every open question under this gate got one.
- **The phase is not materially closer to closing on D-08's own terms.** This is the THIRD 0/5-clean
  result in a row (run 1: 0/5, run 2: 0/5, this run: 0/5 by strict count, though items now carry
  BLOCKED/FAIL/NOT ATTEMPTED dispositions instead of run 2's near-uniform FAIL). D-08 does not credit
  partial progress; only a clean 5-of-5 closes the phase, and this run did not produce one.
- **A new process risk was found and must be carried forward**, not just an app defect: the log
  rotation that nearly lost this run's own evidence (F-34.5-G6-15) is a gate-tooling gap, not a
  GameLib defect, and the next gate run should preserve `gamelib.log` copies proactively rather than
  relying on post-hoc recovery from `gamelib.log.old`.
