---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 11
type: execute
wave: 3
depends_on: ["34.5-01", "34.5-08"]
files_modified:
  - src/backend/sidecar/shortcutsFlowRegistration.ts
  - src/backend/sidecar/__tests__/shortcutsFlows.test.ts
autonomous: true
requirements: [REQ-34.5-05]

must_haves:
  truths:
    - "The add-to-Steam half completes the shortcuts cluster at 7: `addToSteam`, `removeFromSteam`, `isAddedToSteam` — all `addHandler`/invoke."
    - "D-10: this plan carries the `exe`-in-VDF risk. `nonesteamgame.ts:258` writes `\"${app.getPath('exe')}\"` into the Steam shortcut's `Exe` field as the LAUNCH COMMAND. A wrong value is a semantic divergence between builds that is only observable when the shortcut is clicked."
    - "ATTRIBUTION CORRECTED at plan time: the SECOND `exe` call site, `shortcuts.ts:227` (the macOS `.app` `run.sh`), does NOT belong to this plan. `generateMacOsApp` (`shortcuts.ts:176`) holds it, its only caller is `addShortcuts()` at `shortcuts.ts:111`, and the only IPC entry into `addShortcuts` is the `addShortcut` channel (`shortcuts/ipc_handler.ts:15`) — plan 34.5-08. Research Correction 3 and CONTEXT.md D-09 both filed it here; that is wrong. This plan owns `:258`; plan 34.5-08 owns `:227` and pins it with its own test."
    - "REQ-34.5-01 is a hard precondition: without `pathShim`'s `exe` case these channels throw. With it, an unset `GAMELIB_SHELL_EXE` throws LOUDLY rather than writing an empty launch command — and this plan pins that for `addToSteam`, because the call site has no existence check (Electron's `app.getPath('exe')` could not meaningfully fail, so none was ever needed)."
    - "The two pins are complementary and neither may be deleted in favour of the other: `addToSteam` is invoke-kind so an unset `GAMELIB_SHELL_EXE` REJECTS to the caller (pinned here), while `addShortcut` is send-kind so the same condition surfaces as a `logSendFailure` entry with no written `run.sh` (pinned in plan 34.5-08 Task 3 assertion 5). Same property, two observable shapes."
    - "`steam-shortcut-editor` is not reimplemented. This phase changes only the value fed to the writer, never the writer."
    - "Test containment stays load-bearing: VDF writes go to a redirected home, never the developer's real Steam userdata directory."
  artifacts:
    - path: "src/backend/sidecar/shortcutsFlowRegistration.ts"
      provides: "addToSteam, removeFromSteam, isAddedToSteam — completing the cluster at 7"
      contains: "addNonSteamGame"
    - path: "src/backend/sidecar/__tests__/shortcutsFlows.test.ts"
      provides: "Extended coverage to all 7 shortcuts channels plus the addToSteam unset-exe loud-throw pin"
  key_links:
    - from: "src/backend/sidecar/shortcutsFlowRegistration.ts"
      to: "src/backend/shortcuts/nonesteamgame/nonesteamgame.ts"
      via: "curated named imports of addNonSteamGame/removeNonSteamGame/isAddedToSteam, never shortcuts/ipc_handler.ts"
      pattern: "addNonSteamGame"
    - from: "pathShim getPath('exe')"
      to: "the Steam shortcut VDF Exe field (nonesteamgame.ts:258)"
      via: "GAMELIB_SHELL_EXE, throwing loudly when unset"
      pattern: "GAMELIB_SHELL_EXE"
---

<objective>
Complete the shortcuts cluster with the three add-to-Steam channels, and pin the property that makes
them safe: an unavailable shell path fails loudly instead of producing a Steam entry that launches
nothing.

Purpose: these three write an executable path into a VDF that Steam later runs. The value they write
comes from wave-1 seam 1, and the only place its correctness is fully observable is the live gate —
so everything that CAN be pinned automatically must be. The macOS `.app` half of live-gate item 4
belongs to plan 34.5-08's `addShortcut`; this plan covers the Steam half.

Output: seven of seven shortcuts channels, and a test that fails if an empty `Exe` could ever be
written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-RESEARCH.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-01-SUMMARY.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-08-SUMMARY.md

<interfaces>
Electron registration ground truth (verified by grep, src/backend/shortcuts/ipc_handler.ts):
  L60  addHandler('addToSteam',      async (event, appName, runner) => addNonSteamGame(getGame(appName, runner)))
  L65  addHandler('removeFromSteam', async (event, appName, runner) => { await removeNonSteamGame(getGame(appName, runner)) })
  L70  addHandler('isAddedToSteam',  async (event, appName, runner) => isAddedToSteam(getGame(appName, runner)))

Curated import targets (verified export sites):
  src/backend/shortcuts/nonesteamgame/nonesteamgame.ts:522 export block → addNonSteamGame, removeNonSteamGame, isAddedToSteam
  src/backend/utils.ts → getGame

The two `exe` call sites and who owns each (corrected at plan time — do not re-derive this from
research Correction 3 or CONTEXT.md D-09, both of which assign `:227` to this plan):
  nonesteamgame.ts:258  newEntry.Exe = `"${app.getPath('exe')}"`   ← Steam shortcut VDF launch command
                        Reached by `addNonSteamGame`, called by THIS plan's `addToSteam`.
                        Also reachable from `addShortcuts` at `shortcuts.ts:43-45` when the global
                        `addSteamShortcuts` setting is on — so plan 34.5-08's `addShortcut` shares it.
  shortcuts.ts:227      `${app.getPath('exe')} --no-gui gamelib://launch?appName=…&runner=…`  ← macOS .app run.sh
                        Held by `generateMacOsApp` (`shortcuts.ts:176`), whose only caller is
                        `addShortcuts` at `shortcuts.ts:111`. NOT reachable from `addNonSteamGame`.
                        Owned by plan 34.5-08 (`addShortcut`), which registers its source comment and
                        its own loud-throw test. This plan must not duplicate or relocate either.

pathShim contract from plan 34.5-01:
  getPath('exe') returns process.env.GAMELIB_SHELL_EXE, or THROWS
  "[sidecar/pathShim] getPath('exe') is unavailable — GAMELIB_SHELL_EXE was not set by the Tauri shell at spawn time"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the three add-to-Steam channels</name>
  <files>src/backend/sidecar/shortcutsFlowRegistration.ts</files>
  <read_first>
    - src/backend/sidecar/shortcutsFlowRegistration.ts (as written by plan 34.5-08 — the four existing registrations, the send-guard shape, and the `addShortcut` comment block that already records the `shortcuts.ts:227` site; read it so this task's comment complements rather than contradicts it)
    - src/backend/shortcuts/ipc_handler.ts L55-74 (the three bodies verbatim; READ-ONLY, never import it)
    - src/backend/shortcuts/nonesteamgame/nonesteamgame.ts L240-275 and L500-522 (the `newEntry.Exe` write at L258 with its surrounding flatpak/AppImage/PORTABLE_EXECUTABLE_FILE branches, and the export block)
    - src/backend/sidecar/pathShim.ts (as extended by plan 34.5-01 — confirm the `exe` case throws rather than returning `''` before registering anything that reaches it)
    - .planning/phases/34.5-.../34.5-RESEARCH.md § "Don't Hand-Roll" (the `steam-shortcut-editor` row: this phase changes the value fed to the writer, never the writer)
  </read_first>
  <action>
    Add three `ipcMain.handle` registrations to `registerShortcutsFlows()`, porting each body
    verbatim: resolve the game with `getGame(appName, runner)` from `args[0]`/`args[1]`, then call
    `addNonSteamGame(game)`, `await removeNonSteamGame(game)` and `isAddedToSteam(game)`
    respectively. Preserve each Electron body's return value exactly — note `removeFromSteam`
    awaits and returns `undefined`, which is deliberate.

    Import the three functions by name from `../shortcuts/nonesteamgame/nonesteamgame` and `getGame`
    from `../utils`. Never import `shortcuts/ipc_handler.ts`.

    Add a comment above `addToSteam` recording, with line citations, that this channel's write path
    reaches `nonesteamgame.ts:258` where `getPath('exe')` becomes the Steam shortcut's launch
    command; that the site has no existence check because Electron's `app.getPath('exe')` could not
    meaningfully fail; that the sidecar's version CAN fail and therefore throws loudly per
    `pathShim`'s convention, rejecting to the caller because this channel is invoke-kind; and that
    the SECOND `exe` site, `shortcuts.ts:227`, belongs to `addShortcut` (plan 34.5-08) and is
    documented at that registration, not here. Point at plan 34.5-08's comment rather than repeating
    its content. State that live-gate item 4 exercises BOTH channels.

    Do NOT modify, move or delete the `shortcuts.ts:227` comment plan 34.5-08 wrote at the
    `addShortcut` registration.

    Do not add retry, fallback or default-value logic around `getPath('exe')`. A thrown error
    surfacing to the caller is the intended behaviour — the alternative is a Steam entry that
    launches nothing.
  </action>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle(" src/backend/sidecar/shortcutsFlowRegistration.ts` returns 4.
    - `grep -c "ipcMain.on(" src/backend/sidecar/shortcutsFlowRegistration.ts` returns 3 (unchanged from plan 34.5-08).
    - `grep -c "nonesteamgame.ts:258" src/backend/sidecar/shortcutsFlowRegistration.ts` returns at least 1, and the match sits inside the comment block attached to the `addToSteam` registration — record the line numbers of the match and of the `ipcMain.handle('addToSteam'` call in the SUMMARY.
    - `grep -c "shortcuts.ts:227" src/backend/sidecar/shortcutsFlowRegistration.ts` still returns at least 1 and `git diff` shows plan 34.5-08's `addShortcut` comment block unmodified — this plan must not relocate the second call site's documentation.
    - No `catch` around `getPath('exe')` and no default/fallback string appears on the add-to-Steam path.
    - `grep "^import" src/backend/sidecar/shortcutsFlowRegistration.ts | grep -c ipc_handler` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All seven shortcuts channels are registered, and each `exe` risk is documented at the registration that actually reaches it.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the addToSteam loud-throw property and extend cluster coverage to all seven channels</name>
  <files>src/backend/sidecar/__tests__/shortcutsFlows.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/shortcutsFlows.test.ts (as written by plan 34.5-08 — EXTEND its arrays, keep the disposable-homedir harness, the containment assertion, and its assertion-5 `addShortcut` `GAMELIB_SHELL_EXE` cases; those are the send-kind counterpart to this task and must not be deleted, merged or weakened)
    - src/backend/sidecar/__tests__/pathShim.test.ts (plan 34.5-01's suite — the env save/restore discipline to reuse, so this suite does not leak `GAMELIB_SHELL_EXE` into others)
    - src/backend/shortcuts/nonesteamgame/nonesteamgame.ts L240-275 (the VDF entry construction, so the assertion targets the real field name `Exe`)
    - .planning/phases/34.5-.../34.5-VALIDATION.md § "Test Infrastructure" (VDF writes must target a redirected home)
  </read_first>
  <action>
    Extend `HANDLE_CHANNELS` with `addToSteam`, `removeFromSteam` and `isAddedToSteam`, flipping the
    forward-looking "not yet registered" pin plan 34.5-08 left — flip it, do not delete it. The
    cluster is now complete at 7 (4 handle, 3 send); assert both counts explicitly.

    Add the `addToSteam` loud-throw pin, which is the security-relevant test of this plan and the
    invoke-kind counterpart to plan 34.5-08's send-kind `addShortcut` pin. Add a comment naming that
    relationship so a future reader does not treat either as redundant.
      1. With `GAMELIB_SHELL_EXE` UNSET, invoking `addToSteam`'s registered handler rejects with an
         error whose message contains `GAMELIB_SHELL_EXE was not set`, and NO shortcut file is
         written — assert by checking the redirected Steam userdata path is unchanged before and
         after (file list or mtime), not merely that the promise rejected.
      2. With `GAMELIB_SHELL_EXE` set to the EMPTY STRING, the same rejection occurs — empty and
         unset must behave identically.
      3. With `GAMELIB_SHELL_EXE` set to a plausible path, the constructed VDF entry's `Exe` field
         contains that exact value (quoted as `nonesteamgame.ts:258` quotes it) and never an empty
         string, `undefined`, or the node interpreter path. Assert explicitly that the written value
         is NOT equal to `process.execPath` — D-10's rejected alternative.
      Save and restore `process.env.GAMELIB_SHELL_EXE` around every case.

    Note the kind difference in the test names or comments: `addToSteam` REJECTS, while
    `addShortcut` (plan 34.5-08) logs via `logSendFailure` and returns nothing. Asserting a rejection
    for the send channel, or a log-only outcome for this one, would be asserting the wrong contract.

    Keep every write under the redirected home. Add a comment stating that this suite is the first in
    the repo whose subject writes Steam VDF and `.app` artifacts by design, and that
    `jest.setupContainment.ts` must never be bypassed to make an assertion easier.
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/shortcutsFlows.test.ts` exits 0.
    - The suite asserts both directions for all 7 channels (14 assertions minimum) and asserts the 4/3 handle/send split explicitly.
    - Three `addToSteam` `GAMELIB_SHELL_EXE` cases exist (unset, empty, set) and the unset/empty cases assert NO file was written, not merely that the call rejected.
    - Plan 34.5-08's `addShortcut` assertion-5 cases are still present and still passing — `git diff` shows them unmodified, and the SUMMARY states how many `GAMELIB_SHELL_EXE` cases the file now holds in total across both channels.
    - An assertion exists that the written `Exe` value is not equal to `process.execPath`.
    - `process.env.GAMELIB_SHELL_EXE` is restored after the suite — add an assertion or `afterAll` check and state in the SUMMARY that a following suite is unaffected.
    - No assertion targets a path outside the redirected home.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/shortcutsFlows.test.ts sidecar/__tests__/pathShim.test.ts</automated>
  </verify>
  <done>An unavailable shell path can no longer produce a written-but-broken Steam shortcut, and the property is proven by inspecting the filesystem rather than trusting the rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar → Steam shortcut VDF | A value written here becomes a command Steam executes, outside this app's control |
| Rust shell → sidecar (`GAMELIB_SHELL_EXE`) | The single source of the value crossing that boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-39 | Tampering | an empty or unset `exe` interpolated into a Steam VDF `Exe` field | mitigate | `pathShim` throws loudly (plan 34.5-01); this plan adds no catch, fallback or default; and the test asserts NO file is written on the unset and empty paths, verified against the filesystem rather than the promise state. |
| T-34.5-40 | Tampering | the node interpreter written as the launch command | mitigate | Test asserts the written `Exe` value differs from `process.execPath` — D-10's explicitly rejected alternative. |
| T-34.5-41 | Tampering | only one of the two `exe` call sites covered | mitigate | Ownership is now correct per call site: `nonesteamgame.ts:258` is documented and pinned here; `shortcuts.ts:227` is documented and pinned in plan 34.5-08 (its true owner, via `addShortcut` → `generateMacOsApp`). Live-gate item 4 requires BOTH channels to be exercised. Each plan's acceptance criteria assert the other's comment and cases remain present. |
| T-34.5-42 | Tampering | a test writing a VDF into the developer's real Steam userdata | mitigate | `jest.setupContainment.ts` stays enabled; the disposable-homedir harness is inherited from plan 34.5-08's suite; assertions target the redirected path only. |
| T-34.5-43 | Tampering | reimplementing the VDF writer | accept | `steam-shortcut-editor` is already a dependency and already correct; this phase changes only the value fed to it (research § "Don't Hand-Roll"). |
| T-34.5-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `cd src/backend && npx jest sidecar/__tests__/shortcutsFlows.test.ts` exits 0 with 7-channel coverage.
- `grep -c "ipcMain.handle(" src/backend/sidecar/shortcutsFlowRegistration.ts` returns 4; `ipcMain.on(` returns 3.
- `grep -c "nonesteamgame.ts:258" src/backend/sidecar/shortcutsFlowRegistration.ts` returns at least 1; `grep -c "shortcuts.ts:227" ...` still returns at least 1.
- No fallback string exists on the `getPath('exe')` path.
</verification>

<success_criteria>
Add-to-Steam works through the sidecar, the failure mode that would be invisible until a user clicks
a broken Steam entry is a loud, test-pinned rejection that writes nothing at all, and each of the two
`exe` call sites is owned by the plan whose channel actually reaches it.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-11-SUMMARY.md` when done
</output>
