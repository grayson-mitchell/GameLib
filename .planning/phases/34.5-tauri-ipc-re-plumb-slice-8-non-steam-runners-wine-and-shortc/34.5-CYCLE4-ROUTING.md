---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 32
type: routing
cycle: 4
blocking: false
---

# Phase 34.5 gap cycle 4 — routing and disposition

## Authorisation

`BINDING DECISION: fix-first`, quoted verbatim from `34.5-G6-EPIC-DISCRIMINATOR.md` § `## Routing`:

> BINDING DECISION: fix-first
>
> Consequences: Plans 34.5-29 and 34.5-30 are NOT authorised to proceed to authoring or running the
> blocking five-item gate this cycle and MUST halt at their own authorisation gates pending a further
> gap cycle. Phase 34.5 does not reach its blocking gate this cycle. The developer additionally scoped
> a dev-only secret-vault item (above) into that same gap cycle.

Stated plainly: gap cycle 4 is a FIX cycle. The blocking five-item live gate is NOT authored and NOT
run this cycle. Phase 34.5 does not reach its gate in cycle 4.

## Disposition of plans 34.5-29 / 34.5-30 / 34.5-31

### 34.5-29 — HALTED — NOT EXECUTED

Record BOTH of the following separately. They are independent protections; a reader must not assume
the second follows from the first.

**(a) Self-halt in its own `<objective>`.** Plan 34.5-29's own authoring text instructs the executor
to read the discriminator's `BINDING DECISION:` line before doing anything, and states verbatim:

> `fix-first` or `escalate` → **STOP IMMEDIATELY.** Do not run Task 1, 2 or 3. Report to the
> orchestrator that plan 34.5-28's decision withheld authorisation, quote the line, and end the plan.

`fix-first` is exactly the decision recorded. Plan 34.5-29 self-halts on its own written contract
before Task 1 opens.

**(b) The R1-FALSIFIED branch, independent of (a).** Even setting the self-halt aside, plan 34.5-29's
Task 1 branches strictly on the discriminator's frontmatter `verdict`. The recorded verdict is
`R1-FALSIFIED`, and Task 1's own action text for that branch instructs verbatim:

> **`verdict: R1-FALSIFIED`** — make NO change to `USER_AGENTS`. R2 survived but is not confirmed.

Even had the self-halt in (a) not existed, the verdict-branch logic in (b) independently forbids
shipping a user-agent-only change to `USER_AGENTS.legendary`.

**Conclusion.** Plan 34.5-29 cannot ship a user-agent-only Epic fix, by two independent gates — its
own objective-level halt, and its own task-level verdict branch. It is left in place unmodified as
the record of its own authoring; this document does not edit, renumber, or delete it.

**What IS superseded.** Plan 34.5-29 Task 1's premise — "apply the Epic fix that plan 34.5-28's
discriminator SELECTED" — is void, because the discriminator selected no fix (`R1-FALSIFIED`, R2
unconfirmed). Epic's diagnosis is re-homed to plan `34.5-37` this cycle (see the scope map below).

**What is PRESERVED but deferred.** Plan 34.5-29's Tasks 2 and 3 — the item-4/item-5 preflight that
would produce `34.5-G6-ITEM45-PREFLIGHT.md` (verified target, exact devtools invocations, quit-Steam
warning, and a named reachable Wine action for item 5) — are still wanted work. They are deliberately
NOT re-homed into cycle 4. They are to be re-homed into whichever future cycle authorises the blocking
gate, because a preflight written now would be stale again by the time that gate actually runs — this
is the precise reason items 4 and 5 have already been skipped twice (once per prior gate run), each
time because groundwork recorded days earlier had gone stale by the time the item was reached.

### 34.5-30 — HALTED — NOT AUTHORED

Plan 34.5-30 would have authored `34.5-LIVE-GATE-RERUN-2.md`, the third gate contract (written with
`verdict: null`, before any live work, per the same discipline plans 34.5-03 and 34.5-19 followed for
runs 1 and 2). Plan 34.5-30's own `<objective>` carries the identical authorisation-gate text as
34.5-29: `fix-first` or `escalate` → STOP, author nothing, create no
`34.5-LIVE-GATE-RERUN-2.md`. Halted by the binding decision. **NOT authored this cycle.**

### 34.5-31 — HALTED — NOT RUN

Plan 34.5-31 would have executed the third blocking five-item gate against
`34.5-LIVE-GATE-RERUN-2.md` on real macOS hardware and propagated its verdict into
`.planning/ROADMAP.md`, `.planning/STATE.md`, `34.5-PORTED-CHANNELS.md`, and
`.planning/IPC-PORT-INVENTORY.md`. It depends on `34.5-30` producing a contract that, per the
disposition above, was never authored. Halted by the binding decision. **NOT run this cycle.**

## Cycle 4 scope map

| Item | Routing item # | Cycle-4 plan | Wave | What live observation would prove it |
|------|-----------------|--------------|------|----------------------------------------|
| GOG library never lands after successful auth | 1 | `34.5-33` | 1 | A GOG library sync producing a completion line analogous to Steam's `Steam library sync complete: N games`, replacing the current repeating `No cache found, getting data from gog...` with no terminal line; the GOG filter in Library showing actual game tiles, not just the empty filter option |
| `Refreshing undefined Library` from a second call site | 2 | `34.5-33` | 1 | `gamelib.log` showing zero `Refreshing undefined Library` lines across a full GOG login session, with the offending call site identified and passing a real runner name instead of `undefined` |
| Keyring read timeout races human Keychain approval | 3 | `34.5-35` | 1 | A single session in which only ONE macOS Keychain approval prompt appears for a given `steam-refresh-token` (or other slot) read, not two, across a full login flow that previously triggered two |
| Captured + authenticated but no propagation (a race) | 4 | `34.5-34` | 1 | Multiple consecutive identical GOG login attempts in one session each producing a `useTauriOAuthLogin` `phase=idle` line after backend `status=captured`, with zero attempts silently dumping the developer back to Manage Accounts |
| Epic (F-34.5-G6-01) further diagnosis, explicitly NOT a UA fix | 5 | `34.5-37` | 2 | Running `EPIC_LOGIN_URL` under Electron (`npm start`) and observing whether the same "greyed out"/`client_id required` symptom reproduces there — separating "broken by the Tauri port" from "broken by an Epic server-side change unrelated to this project's port" |
| Dev-only local secret vault (developer-scoped) | — (developer-scoped addition) | `34.5-36` | 2 | The vault installing only under an explicit env var, logging a loud warning line on install and on every read/write, and refusing to install in a production build — verified by attempting to build a production bundle with the env var set and observing a refusal, not a silent vault install |
| Routing / ledger entry (this plan) | — | `34.5-32` | 1 | N/A — this plan authors no code and runs no live observation; it records where the other five plans route and opens the standing untested-items ledger |

## What this cycle does NOT do

- Does NOT author `34.5-LIVE-GATE-RERUN-2.md`.
- Does NOT run a third gate.
- Does NOT close Phase 34.5.
- Does NOT retire gate items 4 or 5.
- Does NOT claim any of the four defects (GOG library, `Refreshing undefined`, keyring race,
  capture-without-propagation) closed on unit evidence.

Per D-08 (`34.5-CONTEXT.md`), quoted in one line: no partial pass, no advisory mode, no
pre-authorized override.

## Standing rule carried into every cycle-4 plan

A green test suite is not evidence of live behaviour. This phase failed its gate twice while suites
of 3279, 3387 and 3447 tests were green. Every cycle-4 fix plan states the LIVE observable that would
prove it, and none may record a defect as closed on unit evidence alone.
