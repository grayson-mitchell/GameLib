---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 28
type: execute
wave: 4
depends_on: [34.5-23, 34.5-24, 34.5-25, 34.5-26, 34.5-27]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md
autonomous: false
gap_closure: true
requirements: [REQ-34.5-04, REQ-34.5-12]

must_haves:
  truths:
    - "The Epic discriminator records a verdict selecting R1, falsifying R1, or stopping and escalating — never a third invented reading"
    - "One non-Steam runner is observed reaching a populated library, live, before the full five-item gate is attempted"
    - "The origin is visible in the login window and the Amazon code field is legible, checked by eye"
    - "If the gate is not safe to author, dispatch actually HALTS — it is not merely recommended against in a SUMMARY"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md"
      provides: "the filled discriminator verdict and the smoke observation"
      contains: "verdict:"
  key_links:
    - from: "gamelib.log"
      to: "the discriminator verdict"
      via: "nav host= and useTauriOAuthLogin phase= lines"
      pattern: "nav host="
---

<objective>
A SHORT, developer-driven live checkpoint that does two things the expensive five-item gate cannot do
for us: it executes the pre-registered Epic discriminator so plan 34.5-29 fixes a diagnosed defect
rather than a guessed one, and it smoke-proves that ONE non-Steam login now reaches a populated
library before the full gate is committed to.

Purpose: two full gate runs have now failed, each costing a complete developer session, and each time
the failure was a defect nobody had diagnosed. This checkpoint is deliberately cheap — one runner for
the smoke half — so that if the capture-to-library chain is still broken we find out here instead of
in the middle of item 2 of a five-item gate.

**This is NOT the blocking gate.** It records no PASS/FAIL for any of the five gate items, retires no
standing claim, and cannot close the phase. Its verdict feeds plan 34.5-29. The blocking gate is plan
34.5-31, against the fresh contract plan 34.5-30 authors.

Output: `34.5-G6-EPIC-DISCRIMINATOR.md` filled in, plus a recorded smoke observation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-FINDINGS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md

**Evidence-handling rule, binding on every Result slot below (T-34.5-G6-02, carried verbatim from
`34.5-LIVE-GATE-RERUN.md`):** this document lives in the public fork's repository. Never paste an
`access_token`, `refresh_token`, cookie value, session identifier, or the Steam `userdata` account
id. Quote only the specific log lines a task names. If a required line contains one of these values
inline, redact it in place (`access_token=<REDACTED>`) rather than omitting the whole line.

**Sidecar output is read from `~/Library/Logs/GameLib/gamelib.log`, NEVER from the `pnpm tauri:dev`
terminal.** The sidecar's stdout IS the RPC pipe, so the terminal can never prove the sidecar's
application logic ran (standing finding `sidecar-console-and-logger-are-invisible`). The one
deliberate exception is the `eprintln!("[shell] …")` lines from `main.rs`, which reach the terminal
ONLY — where a task relies on one, it says so explicitly.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Preflight — clean machine, clean build, credential state verified not assumed</name>
  <what-built>
    Plans 34.5-23 through 34.5-27 landed: `oauthCaptureLogin` exempted from the 60 s invoke bound, a
    loud `capture-transport-failed` path in the renderer, hostname-only navigation logging, a
    `GAMELIB_OAUTH_UA_LEGENDARY` override, a keyring in-flight dedupe plus bounded failure memo, the
    captured code routed through the post-login library refresh, and the login window's origin
    display plus light interface style.
  </what-built>
  <how-to-verify>
    1. `ps aux | grep -E "gamelib-shell|sidecar.js"` BEFORE launching anything. Record verbatim. Kill
       everything it lists (`kill -9 <pids>`) and re-run until only the grep process remains. An
       ORPHANED sidecar from an earlier session has now been found TWICE, once before each prior gate
       run; two live pairs contend for the same store files, which makes every later observation
       ambiguous.
    2. Rebuild in order, recording each exit code verbatim: `npx electron-vite build`, then
       `npm run build:sidecar`, then `cargo run --manifest-path src-tauri/Cargo.toml`.
    3. Confirm BOTH processes alive simultaneously with one `ps aux` listing; paste it.
    4. Open `tail -f ~/Library/Logs/GameLib/gamelib.log` in a second window; keep it open all session.
    5. From `gamelib.log` — not the terminal — quote `[bootstrap] appRoot resolved=… source=…` and
       confirm `source=GAMELIB_APP_ROOT`; quote `[bootstrap] publicDir resolved=… exists=…` and
       confirm `exists=true`; confirm
       `grep -c "SIDECAR ASSET ROOT DEFECT" ~/Library/Logs/GameLib/gamelib.log` returns 0. If that
       line is present, STOP — run no task below.
    6. Verify credential state by `ls`, recorded verbatim. **Do not assume the 2026-08-01 aside state
       survived** — plan 34.5-20's SUMMARY records that session ending with GOG signed back IN and
       Amazon's password reset. If a credential is present where this checkpoint needs it absent,
       move it aside recoverably: `cp -p` to a NEW dated suffix (never reuse
       `.pre-34.5-15-gate.bak`), `cmp` the copy against the original to prove byte-identity, then
       remove the original. Never delete blind.
       - Epic: `~/Library/Application Support/GameLib/legendaryConfig/legendary/user.json`
       - GOG: `~/Library/Application Support/GameLib/gog_store/auth.json`
       - Amazon: `~/Library/Application Support/GameLib/nile_store/` and `.../nile_config/`
  </how-to-verify>
  <action>
    Present the numbered steps above to the developer, one block at a time, and collect their
    verbatim output. Do not paraphrase their answers into the record. Where a step names a command,
    the command's literal output is the evidence — not a summary of it. If any hard STOP fires
    (a `SIDECAR ASSET ROOT DEFECT` line present, a build exiting non-zero), halt the plan here and
    report the blockage rather than proceeding to Task 2.
  </action>
  <resume-signal>Paste the `ps` output before and after the kill, the three build exit codes, the three `[bootstrap]` lines from `gamelib.log`, the `SIDECAR ASSET ROOT DEFECT` count, and the credential `ls` output. Then type "preflight clean" or describe what blocked.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the two-arm Epic discriminator, and smoke one non-Steam login end to end</name>
  <what-built>
    `34.5-G6-EPIC-DISCRIMINATOR.md` (authored with `verdict: null` by plan 34.5-24) names two
    readings and a decision rule fixed in advance. `oauthLoginCapture.ts` now logs
    `[oauthLoginCapture] runner=… nav host=…` for every page a login window reaches, and reads
    `GAMELIB_OAUTH_UA_LEGENDARY` to override Epic's user agent without a rebuild. The capture is no
    longer bounded at 60 s, its failures are loud, and a successful auth now triggers the library
    refresh.
  </what-built>
  <how-to-verify>
    **Part 1 — Epic discriminator. Both arms run against the SAME build.** Do not rebuild between
    them; a rebuild makes the arms non-comparable and voids the experiment.

    *Arm A — stock Epic user agent.* With `GAMELIB_OAUTH_UA_LEGENDARY` UNSET, open the Epic login
    from Manage Accounts. Record:
      (a) Is the form INTERACTIVE — can you click a field, type into it, and does the submit control
          respond? Answer yes/no plainly; "it looked fine" is not an answer.
      (b) The full ordered sequence of `[oauthLoginCapture] runner=legendary nav host=` lines from
          `gamelib.log`, verbatim.
      (c) The login window's TITLE BAR text, transcribed exactly as displayed.
      Let the attempt reach its own conclusion or close the window; record which.

    *Arm B — Chrome-shaped user agent.* Quit the app. Relaunch the SAME build with
    `GAMELIB_OAUTH_UA_LEGENDARY` set to `Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/200.0` —
    the exact agent GOG and Amazon already use successfully. Confirm from `gamelib.log` that
    `[oauthLoginCapture] runner=legendary user-agent-override len=…` appears; if it does not, the
    override did not take effect and arm B is VOID — fix the invocation and retry before recording
    anything. Then repeat (a), (b) and (c).

    Record BOTH arms' `nav host=` sequences regardless of interactivity — "sat on one hostname for
    the whole attempt" versus "reached a second hostname" is independent evidence that neither
    reading predicts identically, and the contract requires it unconditionally.

    **Part 2 — smoke ONE non-Steam login end to end.** Use GOG (its capture already succeeded at the
    backend on 2026-08-01, so it isolates the consume path rather than the capture path). Sign out if
    signed in, then log in with real credentials. Record:
      (d) The `[oauthLoginCapture] runner=gog status=…` line and its timestamp.
      (e) Every `[useTauriOAuthLogin] runner=gog phase=…` line. **Zero such lines is the exact
          signature of the defect this cycle exists to close** — the 2026-08-01 session had six
          backend terminal outcomes and not one of these lines. If none appear, say so plainly.
      (f) Whether a `gogdl auth` CLI invocation appears in the log AFTER the capture timestamp.
      (g) By eye: does the Library show GOG games, and does "GOG" appear in the Library filter
          options? Answer both yes/no. This is the observable downstream effect — the backend's own
          `status=captured` report proved nothing in either prior run and does not count here.
      (h) Whether the account surface shows a signed-in GOG username.
      (i) How many macOS Keychain approval prompts appeared during the sign-OUT, if you signed out.

    **Part 3 — two by-eye checks** (these have no automated equivalent; the app is the only place
    they exist):
      (j) Did every login window's title bar show the site's ORIGIN, and did it CHANGE when the flow
          moved between hosts? Transcribe before and after.
      (k) If you reach an Amazon verification-code field at any point, is the text you type legible
          without highlighting it? If you do not reach one, record "not reached" — do not guess.
  </how-to-verify>
  <action>
    Walk the developer through parts 1, 2 and 3 in order and collect their verbatim answers plus the
    log lines each part names. Do not answer any observation on their behalf and do not infer an
    unobserved result — a part the developer did not reach is recorded "not reached", never guessed.
    If arm B's `user-agent-override len=` line is absent, tell the developer arm B is VOID and have
    them retry the invocation before any recording happens.
  </action>
  <resume-signal>Paste answers to (a) through (k), with the verbatim log lines each names. Then type "checkpoint recorded".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the decision rule, fill the verdict, and route plan 34.5-29</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR.md` (whole file — this is the file being modified; its decision-rule table is authoritative and must not be renegotiated now the outcome is known)
    - `~/Library/Logs/GameLib/gamelib.log` — re-verify the developer's pasted lines against the file directly
    - `.planning/phases/34.5-.../34.5-G6-FINDINGS.md` — the conclusion this verdict must not silently contradict
  </read_first>
  <action>
    Fill the Result slots and set the frontmatter `verdict` strictly from the decision-rule table the
    contract fixed BEFORE the experiment:

    - A non-interactive, B interactive → `verdict: R1` (user-agent gated); R2 falsified for this defect.
    - Both non-interactive → `verdict: R1-FALSIFIED`. R2 is NOT thereby confirmed — it is merely
      surviving. Record the additional observation that would confirm it, and route plan 34.5-29 to a
      further diagnosis task rather than to a fix.
    - Both interactive → `verdict: NOT-REPRODUCED`. A defect that vanishes without a targeted change
      is unexplained, not fixed. Name plans 34.5-23/26 as candidate timing changes and require the
      blocking gate to re-attempt Epic from scratch regardless.
    - A interactive, B non-interactive → `verdict: NEITHER-READING-FITS`. STOP and escalate to the
      orchestrator. Do not invent a third reading to rescue the experiment.

    Re-verify every pasted log line against `gamelib.log` yourself before recording it. Plan 34.5-20
    found and corrected a factual error in gathered evidence exactly this way (a claimed "4 legendary
    timeouts" the log showed as 3). If a pasted line disagrees with the file, record the FILE's
    version and note the discrepancy explicitly.

    Then add a `## Smoke observation (part 2)` section recording (d) through (i) verbatim, and a
    `## By-eye checks (part 3)` section recording (j) and (k). For each, state plainly whether the
    corresponding plan's fix is CONFIRMED-LIVE, FALSIFIED, or NOT-OBSERVED:
    - (e)+(g) together confirm or falsify plans 34.5-23 and 34.5-26. Note that (g) is the binding
      half: `[useTauriOAuthLogin]` lines appearing while the library stays empty means layer 1 closed
      and layer 2 did not.
    - (i) confirms or falsifies plan 34.5-25. Two prompts again means FALSIFIED.
    - (j) confirms or falsifies plan 34.5-27 Task 1; (k) confirms, falsifies, or leaves NOT-OBSERVED
      plan 34.5-27 Task 2.

    Finish with a `## Routing` section naming exactly what plan 34.5-29 must do, and — critically —
    whether the blocking gate (plans 34.5-30/31) is safe to author yet. **If the smoke half shows the
    library still unpopulated, say so and recommend another fix plan BEFORE the gate contract.**
    Authoring and running a five-item gate against a build whose one-runner smoke test already failed
    would burn a third developer session to learn what this task already knows.

    Redact per the evidence-handling rule. Epic's redirect carries its authorization code in a query
    string; if a pasted line contains one, redact in place.
  </action>
  <verify>
    <automated>grep -qE "^verdict: (R1|R1-FALSIFIED|NOT-REPRODUCED|NEITHER-READING-FITS)$" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md &amp;&amp; grep -q "## Routing" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `verdict` is exactly one of the four contract values; no fifth value is invented.
    - Every arm's `nav host=` sequence is recorded verbatim, for BOTH arms, regardless of interactivity.
    - The SUMMARY records that each pasted line was re-checked against `gamelib.log`, and names any discrepancy found and which version was kept.
    - `## Smoke observation (part 2)` records (d)–(i) and labels plans 34.5-23, 34.5-25 and 34.5-26 each CONFIRMED-LIVE, FALSIFIED or NOT-OBSERVED — never "probably works" or "looks right".
    - `## By-eye checks (part 3)` records (j) and (k) and labels plan 34.5-27's two tasks the same way.
    - `## Routing` names plan 34.5-29's required work and states explicitly whether the blocking gate is safe to author, with a reason.
    - No `access_token`, `refresh_token`, cookie value, session identifier or Steam `userdata` account id appears in the file — verified by grepping the finished file for `token=`, `code=`, `Cookie`, `userdata` before committing.
    - This plan records NO PASS/FAIL against any of the five blocking gate items and retires NO standing claim — confirmed by `grep -c "Item [1-5]" ` returning 0 for gate-item verdict markers.
    - `## Routing` ends with a line reading `BINDING DECISION: <option-id>` once Task 4 completes; plans 34.5-29 and 34.5-30 both read that line and refuse to run without it.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: HALT — decide whether the blocking gate is safe to author</name>
  <decision>
    Given this checkpoint's recorded verdict and smoke observation, does the cycle proceed to plan
    34.5-29 → 34.5-30 → 34.5-31 (the third blocking five-item live gate), or stop here and fix more
    first?
  </decision>
  <context>
    This task exists because a recommendation is not a control. Plans 34.5-29 and 34.5-30 are both
    `autonomous: true`, and an autonomous task writing "STOP and escalate" into its own SUMMARY does
    NOT halt the next wave's dispatch — the only structural gate that pauses an autonomous chain is a
    blocking `checkpoint:*` task inside a plan. Without this task, a `NEITHER-READING-FITS` verdict or
    an unpopulated library in the smoke half would flow straight through to the full gate anyway,
    burning the third developer session this checkpoint exists to protect. This project has already
    burned two.

    Present to the developer, from Task 3's own record, verbatim and without softening:
      - the frontmatter `verdict`;
      - each of plans 34.5-23, 34.5-25, 34.5-26 and 34.5-27 labelled CONFIRMED-LIVE, FALSIFIED or
        NOT-OBSERVED;
      - the answer to (g) — were GOG games visible in the Library AND was GOG present in the filter
        options? This is the binding signal: it is the observable downstream effect, and it is the
        one that both prior gate runs failed on;
      - the `## Routing` section's own recommendation.

    Decision rule, stated so the executor does not improvise:
      - `verdict: R1` or `NOT-REPRODUCED`, AND (g) answered yes to both halves → option `proceed` is
        the expected choice.
      - (g) answered no to either half → option `fix-first` is the expected choice, whatever the Epic
        verdict says. A five-item gate cannot pass items 2 and 3 on a build whose one-runner smoke
        test already showed an unpopulated library.
      - `verdict: NEITHER-READING-FITS` → option `escalate` is the REQUIRED choice. This is its one
        unambiguous defined action; the executor does not get to pick another.
      - `verdict: R1-FALSIFIED` → either `fix-first` or `proceed-knowing-epic-fails` is defensible;
        present both with the trade-off rather than choosing for the developer.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed to the blocking gate</name>
      <pros>The cycle's fixes are live-confirmed on at least one runner; the gate is the remaining work</pros>
      <cons>Costs a full developer session; a missed defect burns it</cons>
    </option>
    <option id="fix-first">
      <name>Plan another fix cycle before authoring the gate contract</name>
      <pros>Protects the developer session that plans 34.5-30/31 would consume; the correct choice whenever the smoke half failed</pros>
      <cons>Adds plans and delays closure; Phase 34.5 stays open longer</cons>
    </option>
    <option id="proceed-knowing-epic-fails">
      <name>Proceed, accepting that item 1 (Epic) is expected to FAIL</name>
      <pros>Items 2-5 still get exercised, including items 4 and 5 which have NEVER been attempted across two runs</pros>
      <cons>Guarantees a non-5/5 verdict, so the phase cannot close on this run; only worth it for the evidence items 4 and 5 would produce</cons>
    </option>
    <option id="escalate">
      <name>Stop and escalate to the orchestrator</name>
      <pros>The mandatory choice when the discriminator's own decision rule was contradicted — an experiment whose outcome fits neither pre-registered reading means the model is wrong, not the experiment</pros>
      <cons>Requires a planning pass before any further execution</cons>
    </option>
  </options>
  <action>
    Present the four options with the recorded evidence above and the decision rule, then STOP and
    wait. Do not select on the developer's behalf, do not proceed on silence, and do not soften a
    `fix-first` or `escalate` indication into `proceed` because the remaining plans already exist.
    Record the chosen option id verbatim in this plan's SUMMARY, and append it to
    `34.5-G6-EPIC-DISCRIMINATOR.md`'s `## Routing` section as the binding decision that plans
    34.5-29 and 34.5-30 must read before doing anything.
  </action>
  <resume-signal>Select exactly one: `proceed`, `fix-first`, `proceed-knowing-epic-fails`, or `escalate`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live session log → repository document | Real Epic/GOG session artefacts cross from private disk into a public-fork file |
| developer-reported observation → recorded verdict | A human summary can drift from what the log actually says |
| operator-set `GAMELIB_OAUTH_UA_LEGENDARY` → live storefront | An override is forwarded verbatim to a real login origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-G6-02 | Information disclosure | pasted `gamelib.log` excerpts in the discriminator doc | mitigate | Only the lines a task names may be quoted; in-place redaction is required; Task 3's acceptance criteria require a pre-commit grep for `token=`, `code=`, `Cookie`, `userdata` |
| T-34.5-G6-27 | Repudiation | a developer summary adopted as fact | mitigate | Task 3 re-verifies every pasted line against `gamelib.log` directly and records any discrepancy, following the precedent that caught the "4 vs 3 timeouts" error in plan 34.5-20 |
| T-34.5-G6-28 | Tampering | a checkpoint result being read as a blocking-gate PASS | mitigate | The objective states this is not the gate; Task 3's acceptance criteria forbid recording any gate-item verdict and forbid retiring any standing claim |
| T-34.5-G6-40 | Elevation of privilege | the chain proceeding to the costly gate against this checkpoint's own finding | mitigate | Task 4 is a BLOCKING `checkpoint:decision`, the only structural construct that actually pauses dispatch. Its chosen option is written into `## Routing` as `BINDING DECISION:`, and plans 34.5-29/34.5-30 each refuse to run without it |
| T-34.5-G6-12 | Spoofing | UA override reaching a live storefront | accept | Carried from plan 34.5-24: settable only by a process already running as the user, cannot widen the host-anchored redirect matcher, and its use is recorded by a `user-agent-override len=` line |
| T-34.5-G6-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages; it rebuilds from the existing lockfiles only |
</threat_model>

<verification>
- `34.5-G6-EPIC-DISCRIMINATOR.md` carries one of the four contract verdicts, both arms' evidence, the smoke observation, the by-eye checks, and a routing recommendation.
- No source file is modified by this plan — `git diff --name-only` matches zero `.ts`, `.tsx`, `.rs` paths.
</verification>

<success_criteria>
Epic's failure mode is decided by a rule fixed before the experiment rather than chosen after it; one
non-Steam login has been driven end to end against the fixed build with the library state checked by
eye; and the cycle knows, before spending a developer session on the five-item gate, whether that
gate is worth authoring yet.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-28-SUMMARY.md` when done
</output>
