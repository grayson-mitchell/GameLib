---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 37
type: execute
wave: 2
depends_on: [34.5-32]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR-2.md
autonomous: false
gap_closure: true
requirements: [REQ-34.5-04, REQ-34.5-12]

must_haves:
  truths:
    - "The contract's readings and decision rule are committed before any observation is made"
    - "The experiment separates 'broken by the Tauri port' from 'broken independently of this project'"
    - "No Epic fix is shipped by this plan — the verdict selects a route, not a patch"
    - "The observation requires no real Epic credential to be entered"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR-2.md"
      provides: "the pre-registered Electron-vs-Tauri discriminator for F-34.5-G6-01 and its filled verdict"
      contains: "verdict"
  key_links:
    - from: "34.5-G6-EPIC-DISCRIMINATOR.md verdict R1-FALSIFIED"
      to: "34.5-G6-EPIC-DISCRIMINATOR-2.md"
      via: "the concrete next discriminator its own Result section named"
      pattern: "EPIC_LOGIN_URL"
---

<objective>
Close Routing item 5: run the concrete next discriminator that the first Epic contract's own Result
section named — load `EPIC_LOGIN_URL` under Electron (`npm start`) as well as under Tauri
(`pnpm tauri:dev`), to separate "broken by the Tauri port" from "broken by something outside this
project entirely."

Purpose: the first discriminator returned `R1-FALSIFIED` — a user-agent change did not make Epic's
login form interactive, on either arm. R2 (a Chromium-only web API throwing under WKWebView) merely
SURVIVED; it was not confirmed, and the contract explicitly forbade shipping it as a fix on the
strength of elimination alone. Two further facts point away from a Tauri-specific cause and were
recorded as evidence rather than promoted to a reading: `EPIC_LOGIN_URL` is
`https://www.epicgames.com/id/login?responseType=code` (`src/frontend/screens/WebView/loginRoutes.ts:45`)
carrying no `client_id`, Arm B's Chrome-shaped agent produced a literal
`Parameter "client_id" is required` error page, and `git show c76875e83^:src/frontend/screens/WebView/index.tsx`
shows the identical URL literal predating the Tauri port. An Electron arm is the cheapest way to tell
these apart, and it costs one short developer session rather than a full gate run.

Output: `34.5-G6-EPIC-DISCRIMINATOR-2.md`, authored with `verdict: null` before any observation, then
filled from the live result.

**This plan ships no Epic fix.** Its verdict selects a ROUTE. Do not change `USER_AGENTS`, do not
change `EPIC_LOGIN_URL`, and do not change `matchOAuthRedirect` in this plan. Plan 34.5-29's already
recorded R1-FALSIFIED branch ("make NO change") stands.

**This is not the blocking gate.** `BINDING DECISION: fix-first` forbids authoring or running the
five-item live gate this cycle. This is a single-question diagnosis observation costing minutes, of
the same kind that prevented a third doomed gate session. Do not extend it into gate items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-FINDINGS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE4-ROUTING.md
@.claude/skills/spike-findings-gamelib/SKILL.md

**Run `graphify query "EPIC_LOGIN_URL loginRoutes WebView"` and `graphify explain "oauthLoginCapture"` before opening raw source. This plan reads source; it modifies none.**

<interfaces>
- `EPIC_LOGIN_URL` — `src/frontend/screens/WebView/loginRoutes.ts:45`, value
  `https://www.epicgames.com/id/login?responseType=code`. No `client_id`, no `redirectUrl`.
- `src/frontend/screens/WebView/index.tsx` — the Electron `<webview>` path, which is what `npm start`
  exercises. It sets its own user agent at L229-237; that Chromium `<webview>` supplies its own
  product tokens, which is where `USER_AGENTS.legendary`'s token-less string was copied from.
- `src/backend/sidecar/oauthLoginCapture.ts` — the Tauri path. `resolveUserAgent()` is called only at
  line 284 inside `.open(loginUrl, ...)`, so the `user-agent-override len=` receipt line structurally
  cannot be emitted before the login window opens (recorded in the first contract's Result).
- `nav host=` logging was added by plan 34.5-24 Task 1 and is available on the Tauri arm only. The
  Electron arm has no equivalent; its evidence is the visible page and the title bar.

Prior evidence this contract must not contradict, from `34.5-G6-EPIC-DISCRIMINATOR.md` § Result:
- Arm A (stock Epic agent, Tauri): non-interactive form, single hostname `www.epicgames.com`.
- Arm B (Chrome-shaped agent, Tauri): `Parameter "client_id" is required` error page, same single
  hostname, full 300 s `status=timeout`.
- Verdict `R1-FALSIFIED`; R2 surviving but unconfirmed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 34.5-G6-EPIC-DISCRIMINATOR-2.md with verdict null, before any observation</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR.md` — the WHOLE file; it is the structural template (frontmatter `verdict`, "the two readings", a per-reading prediction table, a decision rule covering every outcome combination, empty Result slots, an evidence-handling rule) and its Result section supplies this contract's starting evidence
    - `src/frontend/screens/WebView/loginRoutes.ts` around line 45 — `EPIC_LOGIN_URL`'s exact literal, to quote it correctly rather than from memory
    - `src/frontend/screens/WebView/index.tsx` lines 220-245 — the Electron `<webview>` user-agent site, so the contract states accurately what differs between the two arms besides the shell
    - `.planning/phases/34.5-.../34.5-CYCLE4-ROUTING.md` — to cite this plan's row in the cycle-4 scope map
  </read_first>
  <action>
    Create `34.5-G6-EPIC-DISCRIMINATOR-2.md` with frontmatter carrying `phase`, `plan: 37`,
    `type: discriminator`, `finding: F-34.5-G6-01`, `verdict: null`, `blocking: false`. `verdict` MUST
    be the literal `null` when this task commits — it is filled by Task 3 and by nothing else.

    Open with a paragraph stating that this document is a CONTRACT written before the experiment runs,
    that it exists because the first contract returned `R1-FALSIFIED` with R2 merely surviving, and
    that its own Result section named this exact experiment as the concrete next discriminator.

    Carry forward, verbatim and attributed, the first contract's Result evidence: Arm A's
    non-interactive form and single `www.epicgames.com` hostname; Arm B's
    `Parameter "client_id" is required` error page, same hostname, `status=timeout` at 300 s; and the
    recorded fact that `git show c76875e83^:src/frontend/screens/WebView/index.tsx` carries the
    identical `EPIC_LOGIN_URL` literal, so the URL predates the Tauri port.

    Pre-register exactly two readings and no more:

    **E1 — broken by the Tauri port.** Something about the Tauri/WKWebView login-window seam prevents
    Epic's login page from becoming interactive. R2 from the first contract (a Chromium-only web API
    throwing under WKWebView) is one mechanism of this shape; the standing project record already has
    a confirmed instance in `queryLocalFonts` throwing under WKWebView. This reading does not commit to
    a specific API.

    **E2 — broken independently of this project.** Epic's login page no longer works when loaded at
    `EPIC_LOGIN_URL` as written, regardless of shell — for example because the flow now requires a
    `client_id` the inherited URL literal does not carry. This reading is supported, not established,
    by Arm B's literal error message and by the URL predating the port.

    Write a per-reading prediction table with columns `Reading | Electron (npm start) | Tauri (pnpm
    tauri:dev)`: E1 predicts interactive under Electron and non-interactive under Tauri; E2 predicts
    non-interactive under BOTH.

    Write a decision rule covering all four outcome combinations, fixed in advance:
    Electron interactive AND Tauri non-interactive selects **E1**, and routes to instrumenting the
    embedded window's own console/script-error signal — the additional observation the first contract
    already named as the one that would confirm R2 — not to shipping a guess.
    Both non-interactive selects **E2**, and routes to a further contract comparing `EPIC_LOGIN_URL`
    against upstream Heroic's current value and against legendary's own documented auth flow. State
    explicitly that E2 being selected does NOT authorise editing `EPIC_LOGIN_URL` inline: a URL change
    is a new hypothesis and gets its own pre-registered contract, exactly as R1 and R2 did.
    Both interactive records **NOT REPRODUCED**, and routes to re-running the Tauri arm before
    concluding anything — a defect that vanishes without an identified change is unexplained, not
    fixed.
    Electron non-interactive AND Tauri interactive records **NEITHER READING FITS**; STOP and escalate.
    Do not invent a third reading inline.

    Add a `## How to run each arm` section with numbered steps for both arms: which command starts the
    shell, how to reach the Epic login window, and what to record. State the single measured variable
    plainly — whether the form accepts keyboard and mouse input — and require the title bar and any
    visible error text to be transcribed on both arms. Require the Tauri arm's `nav host=` sequence to
    be read back from `~/Library/Logs/GameLib/gamelib.log`, and state that the Electron arm has no
    equivalent line so its evidence is the visible page plus the title bar.

    Add a precondition that the developer must NOT enter real Epic credentials on either arm. The
    measured variable is whether the form is interactive; a completed login is neither required nor
    wanted, and not entering credentials removes the whole credential-handling risk from this
    experiment. Include the evidence-handling rule (T-34.5-G6-02) verbatim in its own section.

    Leave every Result slot EMPTY. Commit this task on its own, before Task 2 runs — a contract edited
    after the observation is not a pre-registration.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR-2.md; test -f $F &amp;&amp; grep -q "^verdict: null" $F &amp;&amp; grep -q "E1" $F &amp;&amp; grep -q "E2" $F &amp;&amp; grep -q "NEITHER READING FITS" $F &amp;&amp; grep -q "NOT REPRODUCED" $F'</automated>
  </verify>
  <acceptance_criteria>
    - The file exists with frontmatter `verdict: null` at the moment of this task's commit, confirmed by `git show <sha>:<path> | grep '^verdict: null'`.
    - Exactly two readings are pre-registered, labelled `E1` and `E2`, and neither commits to a specific throwing API.
    - The prediction table states E1 predicts interactive-under-Electron and non-interactive-under-Tauri, and E2 predicts non-interactive under both.
    - The decision rule covers all four outcome combinations by name, and the E2 branch explicitly forbids editing `EPIC_LOGIN_URL` inline.
    - The precondition forbidding real Epic credentials is present, with its reason.
    - The evidence-handling rule (T-34.5-G6-02) is present.
    - Every Result slot is empty; `grep -c "R1-FALSIFIED\|E1 SELECTED\|E2 SELECTED" $F` returns matches only inside the decision-rule and carried-evidence sections, never inside a Result slot.
    - `git diff --name-only` for this task lists exactly one file, and no `.ts`, `.tsx` or `.rs` file is modified.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run both arms on real hardware</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR-2.md` § `## How to run each arm` — as committed by Task 1, followed exactly as written
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR-2.md` § the precondition forbidding real Epic credentials
  </read_first>
  <what-built>
    Task 1 committed a pre-registered contract with two readings (E1, broken by the Tauri port; E2,
    broken independently of this project), a prediction table, and a decision rule covering all four
    outcome combinations — all fixed before any observation, with `verdict: null`.
  </what-built>
  <action>
    Drive both shells by hand and record what the Epic login form does in each, following the
    committed contract's `## How to run each arm` steps exactly. The single measured variable is
    whether the form accepts keyboard and mouse input. Do not enter a real Epic credential on either
    arm, do not edit the contract, and do not attempt any other gate item — this observation answers
    one question and nothing else.
  </action>
  <how-to-verify>
    1. Confirm Epic is signed out before starting. Re-verify; do not assume.
    2. **Arm E — Electron.** Run `npm start`. Open Manage Accounts and click Epic. Wait for the login
       page to render, allowing at least 60 seconds — the 2026-08-01 session recorded a GOG form
       taking over 30 seconds to appear, so a slow render is not a blank page.
    3. Record for Arm E: does the form accept keyboard and mouse input, yes or no? Transcribe the
       window title bar exactly. Transcribe any visible error text exactly.
    4. **Do not enter real Epic credentials.** Clicking into a field to test focus and typing a single
       throwaway character is the whole interactivity test.
    5. Quit the app.
    6. **Arm T — Tauri.** Run `pnpm tauri:dev` with `GAMELIB_OAUTH_UA_LEGENDARY` UNSET (the stock
       agent, matching the gate's original configuration). Open Manage Accounts and click Epic. Apply
       steps 3 and 4 identically.
    7. For Arm T only, after the attempt settles, read back
       `grep -n "runner=legendary" ~/Library/Logs/GameLib/gamelib.log` and copy every `label=`,
       `nav host=` and `status=` line for that attempt, in order.
    8. Supply all of the above verbatim. Redact in place per the evidence-handling rule if any line
       carries a token, cookie, session id or Steam `userdata` account id — redact, never omit the
       line.
  </how-to-verify>
  <acceptance_criteria>
    - Both arms were attempted, and each has an explicit interactive yes/no answer. An arm that was not run is recorded as NOT RUN, never inferred from the other arm.
    - The title bar text is transcribed for both arms, and any visible error text is transcribed verbatim.
    - The Tauri arm's `nav host=` sequence is supplied from `gamelib.log`, or its absence is stated with the reason.
    - No real Epic credential was entered on either arm, confirmed by the developer.
    - No supplied line contains an unredacted token, cookie value, session identifier or Steam `userdata` account id.
  </acceptance_criteria>
  <resume-signal>Supply both arms' observations verbatim, or type "not run" to halt this plan.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fill the verdict and record the route it selects</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR-2.md` — the committed contract, in full; its decision rule is applied as written and is NOT to be rewritten to fit the observation
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR.md` § "Which outcome combination was observed" — the precedent for how a verdict section is written, including how a contract defect is recorded as evidence rather than silently corrected
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` — row `U-34.5-06` (Epic's success path), to state correctly whether this observation retires it
  </read_first>
  <action>
    Fill the Result section of `34.5-G6-EPIC-DISCRIMINATOR-2.md` from Task 2's supplied evidence, and
    set the frontmatter `verdict` to whichever of `E1`, `E2`, `NOT-REPRODUCED` or
    `NEITHER-READING-FITS` the committed decision rule selects. Apply the rule as written. Do not edit
    the readings, the prediction table or the decision rule — if the observation exposes a defect in
    any of them, record that defect inside the Result section as evidence, exactly as the first
    contract recorded its own `len=52` arithmetic error rather than silently correcting the
    pre-registered text.

    Re-verify every pasted log line against `~/Library/Logs/GameLib/gamelib.log` (or `gamelib.log.old`
    if the session rotated) before writing it, and state whether any discrepancy was found. Redact in
    place per the evidence-handling rule.

    Add a `## Routing` section naming the concrete next step the selected verdict maps to, quoting the
    decision rule's own branch text. Under `E1`, that is instrumenting the embedded window's
    console/script-error signal to confirm or refute R2 — not a fix. Under `E2`, that is a further
    contract comparing `EPIC_LOGIN_URL` against upstream Heroic's current value; state explicitly that
    this does NOT authorise editing the URL in this or any plan until that contract runs.

    State plainly whether ledger row `U-34.5-06` (Epic's success path) is retired by this observation.
    It is not — this experiment measures form interactivity, not a completed login — so record it as
    still OPEN and say why, rather than leaving a reader to infer it.

    Ship NO code. Do not modify `USER_AGENTS`, `EPIC_LOGIN_URL`, `matchOAuthRedirect`, or any `.ts`,
    `.tsx` or `.rs` file. Record in the SUMMARY that this plan deliberately shipped no code and which
    verdict it recorded.
  </action>
  <verify>
    <automated>bash -c 'D=.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc; F=$D/34.5-G6-EPIC-DISCRIMINATOR-2.md; grep -qE "^verdict: (E1|E2|NOT-REPRODUCED|NEITHER-READING-FITS)$" $F &amp;&amp; grep -q "## Routing" $F &amp;&amp; grep -q "U-34.5-06" $F &amp;&amp; test -z "$(git diff --name-only -- \*.ts \*.tsx \*.rs)"'</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `verdict` is exactly one of `E1`, `E2`, `NOT-REPRODUCED`, `NEITHER-READING-FITS`, and the Result section names which outcome combination was observed and quotes the decision-rule branch that selects it.
    - Both arms' Result slots are filled, or an unrun arm is recorded as NOT RUN with its consequence for the verdict stated.
    - Every pasted log line is recorded as re-verified against `gamelib.log`, with any discrepancy stated.
    - The readings, prediction table and decision rule are byte-identical to Task 1's commit — verified by `git diff <task-1-sha> -- <path>` showing changes only below the `## Result` heading.
    - A `## Routing` section exists naming the next step, and under `E2` it explicitly states that editing `EPIC_LOGIN_URL` is not authorised without a further contract.
    - `U-34.5-06` is recorded as still OPEN with the reason stated.
    - `git diff --name-only` shows zero `.ts`, `.tsx` and `.rs` files across this entire plan.
    - The SUMMARY records the verdict verbatim and states that no code shipped.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → live Epic login page | A real credential origin is loaded in an embedded window |
| live observation → pre-registered contract | An observed result meets a decision rule that must not be rewritten to fit it |
| session log → public-fork document | `gamelib.log` excerpts are pasted into a repository that is publicly forked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C4-50 | Information disclosure | a real Epic credential entered into a page whose behaviour is under investigation | mitigate | Task 1 pre-registers a precondition forbidding real credentials; the measured variable is interactivity, and a single throwaway character establishes it. Task 2's acceptance criteria require the developer to confirm no credential was entered |
| T-34.5-C4-51 | Information disclosure | tokens, cookies or the Steam `userdata` account id pasted into a public-fork document | mitigate | The evidence-handling rule (T-34.5-G6-02) is carried into the contract verbatim and enforced by Task 2's and Task 3's acceptance criteria: redact in place, never omit the line |
| T-34.5-C4-52 | Tampering | the contract's readings or decision rule rewritten after the observation | mitigate | Task 1 commits with `verdict: null` on its own commit; Task 3's acceptance criteria require `git diff` against that commit to show changes only below `## Result` |
| T-34.5-C4-53 | Repudiation | an unrun arm silently inferred from the other | mitigate | Task 2 requires an explicit NOT RUN record per arm, and Task 3 must state its consequence for the verdict |
| T-34.5-C4-54 | Elevation of privilege | shipping an Epic fix this experiment did not select | mitigate | Every task forbids code changes; Task 3's automated verify asserts zero `.ts`/`.tsx`/`.rs` files in the diff, and the `E2` branch explicitly withholds authorisation to edit `EPIC_LOGIN_URL` |
| T-34.5-C4-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. Any install would require `slopcheck` plus a blocking human checkpoint first |
</threat_model>

<verification>
- `git diff --name-only` across the whole plan shows exactly one file, `34.5-G6-EPIC-DISCRIMINATOR-2.md`, and zero source files.
- Task 1's commit contains `verdict: null`; Task 3's commit contains a resolved verdict — two commits, verified with `git log --oneline` on that path.
- No file named `34.5-LIVE-GATE-RERUN-2.md` was created, and no gate item was recorded PASS or FAIL by this plan.
- `npm run test:ci` and `npx tsc --noEmit` are unaffected because no source file changed; run both once and record them to confirm the tree is still green after the wave.
</verification>

<success_criteria>
A pre-registered Electron-versus-Tauri contract exists, committed with `verdict: null` before any
observation; both arms were run on real hardware; the recorded verdict follows the committed
decision rule; and the routing names the next diagnostic step rather than a patch, with
`EPIC_LOGIN_URL` explicitly not authorised for editing.

**CORRECTED 2026-08-20 by `/gsd-secure-phase 34.5` (threat `T-34.5-C4-50`, accepted risk
`AR-34.5-02`).** This criterion previously read *"both arms were run on real hardware **without a
real Epic credential**"*. That clause was **false as written** and is struck rather than reworded
around: Arm E (Electron) completed a full real Epic login — `legendary auth --code <redacted>` →
`Game list updated, got 15 games & DLCs` — against this experiment's own no-credentials
precondition (`34.5-G6-EPIC-DISCRIMINATOR-2.md:141-145`).

The deviation was self-disclosed in `34.5-37-SUMMARY.md` at the time; only this block was never
updated to match, leaving the PLAN asserting the opposite of its own Result section. The verdict is
unaffected — the decision rule required "Electron interactive AND Tauri non-interactive", and a
completed login proves interactivity *a fortiori*. Do not read the struck clause as a standard this
experiment met.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-37-SUMMARY.md` when done
</output>
