---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md
  - .planning/ROADMAP.md
  - .planning/IPC-PORT-INVENTORY.md
autonomous: true
requirements: [REQ-34.5-11, REQ-34.5-12]

must_haves:
  truths:
    - "The live gate is a CONTRACT written before any live work, not a report written after it — `34.5-LIVE-GATE.md` exists with all 5 items pending and all preconditions unticked before the first channel is ported."
    - "The gate's preconditions are explicit NUMBERED steps, because an unstated precondition is how a gate gets skipped and becomes UAT debt (Phases 21/30/34.2)."
    - "Precondition 1 is the 34.4.1 sequencing constraint: 34.5 must not ship a real OAuth credential path on a seam whose own blocking live gate never ran."
    - "D-05: Phase 34.6 EXISTS in ROADMAP.md before Phase 35, so the 16 deferred channels are tracked rather than silently becoming accepted loss — Phase 35's own entry requires the IPC re-plumb to be COMPLETE."
    - "The inventory's 57 reconciles arithmetically as 38 ported + 3 dropped + 16 moved, keeping the Phase 35 completion gate checkable."
    - "The 3 dropped and 16 deferred channels need NO new code — `UNPORTED_CHANNEL_MARKER` already catches them and declares them honestly."
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md"
      provides: "The blocking 5-item gate contract with empty result slots"
      contains: "blocking: true"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 34.6 entry inserted between Phase 34.5 and Phase 35"
      contains: "Phase 34.6"
    - path: ".planning/IPC-PORT-INVENTORY.md"
      provides: "Phase 34.5's 57 reconciled as 38/3/16 plus a Phase 34.6 section listing the 16"
      contains: "Phase 34.6"
  key_links:
    - from: ".planning/ROADMAP.md § Phase 35"
      to: "Phase 34.6"
      via: "the Depends-on line naming the completed IPC re-plumb"
      pattern: "34.6"
---

<objective>
Write this phase's blocking live gate as an empty contract, and make the deferral real: insert
Phase 34.6 into the roadmap and reconcile the inventory so the 57-channel slice adds up as 38
ported, 3 dropped and 16 moved.

Purpose: VALIDATION.md requires the gate doc to exist before any code plan, and D-05 states the
deferral is not free — Phase 35 requires the re-plumb to be COMPLETE, so an untracked deferral
silently becomes accepted loss.

Output: a gate that cannot be quietly skipped, and a roadmap whose arithmetic still holds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-VALIDATION.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md
@.planning/IPC-PORT-INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 34.5-LIVE-GATE.md as an empty, blocking contract</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md (the full 390-line shape to copy: YAML frontmatter, numbered Preconditions with stated evidence forms, per-item What to do / What to observe / Evidence required / Result, the Verdict table, and the "What this gate falsifies" section)
    - .planning/phases/34.5-.../34.5-VALIDATION.md § "Manual-Only Verifications" (the 5 items and their exact wording — this table is BINDING and must not be paraphrased into something weaker)
    - .planning/REQUIREMENTS.md REQ-34.5-12 (the phrasing of each item, including item 3's redirect-origin capture and item 4's "BOTH `exe` call sites")
    - .planning/STATE.md L37-72 (34.4.1-08's held state: which of its own preconditions are satisfied, and the Humble-credential-moved-aside situation — relevant to sequencing precondition 1)
  </read_first>
  <action>
    Create `34.5-LIVE-GATE.md` following `34.4.1-LIVE-GATE.md`'s structure exactly.

    Frontmatter: `status: pending`, `blocking: true`, `items_total: 5`, `items_passed: 0`,
    `items_failed: 0`, `verdict: pending`, plus the phase/slug fields the analog uses.

    `## Preconditions` — a NUMBERED list, ticked in order, each stating exactly how it will be
    evidenced (screenshot, log transcript, file content, command output). At minimum:
      1. **Phase 34.4.1's own blocking live gate has recorded PASS on at least item 1** (a real login
         completing from scratch) in `34.4.1-LIVE-GATE.md`. Evidence: quote the recorded result line.
         State plainly that this phase may be BUILT before this precondition is met but MUST NOT run
         gate items 1-3 (the real OAuth credential paths) until it is — 34.5 must not ship a real
         credential path on a seam whose live gate never ran.
      2. A clean rebuild and a real `pnpm tauri:dev` launch, with the sidecar confirmed alive via
         `~/Library/Logs/GameLib/gamelib.log` growing and a `ps` listing showing shell + sidecar.
         State explicitly that sidecar output is read from that log file and NOT from the
         `tauri:dev` terminal, because stdout is the RPC pipe.
      3. The operator is signed OUT of Epic, GOG and Amazon before items 1-3, with any existing
         credential moved aside recoverably and the restore path written down.
      4. `npm run test:ci`, `npx tsc --noEmit`, `cd src-tauri && cargo check`, `cargo test` — all
         four run and their exit codes recorded VERBATIM, not rounded to green.
      5. The `GAMELIB_SHELL_EXE` value the shell actually set is read back from `gamelib.log` and
         written into this document before item 4 runs.

    `### Item N` sections, one per the five VALIDATION.md rows, each with **What to do** /
    **What to observe** / **Evidence required** / **Result: [ ] PASS [ ] FAIL** and empty slots:
      1. Epic login from scratch reaches a signed-in, POPULATED library.
      2. GOG login from scratch reaches a signed-in, POPULATED library.
      3. Amazon login from scratch reaches a signed-in, POPULATED library, AND the captured
         redirect's origin is logged and compared against the `www.amazon.com` anchor
         (research Assumption A1). Name the greppable evidence-log prefix plan 34.5-02 added.
      4. `addToSteam` AND the macOS `.app` desktop shortcut both launch GameLib, not node —
         BOTH `exe` call sites exercised (`nonesteamgame.ts:258` and `shortcuts.ts:227`).
      5. `runWineCommand` succeeds for at least one NON-Steam runner, closing the never-proven gap.

    `## Verdict` table (empty), and a `## What this gate falsifies` section naming the exact standing
    claims a PASS retires — at minimum "no non-Steam runner has ever executed a Wine command under
    the sidecar" and "the `www.amazon.com` anchor is unverified against GameLib's own traffic".

    Every result slot stays EMPTY. This plan writes the contract; plan 34.5-15 executes it.
  </action>
  <acceptance_criteria>
    - The file exists and its frontmatter contains `blocking: true`, `items_total: 5`, `items_passed: 0`, `verdict: pending`.
    - `grep -c "^### Item" 34.5-LIVE-GATE.md` returns 5.
    - `grep -c "PASS" 34.5-LIVE-GATE.md` returns at least 5 and every occurrence is inside an unticked `[ ]` slot — `grep -c "\[x\]" 34.5-LIVE-GATE.md` returns 0.
    - The Preconditions section is a numbered list with at least 5 entries, and entry 1 names `34.4.1-LIVE-GATE.md` explicitly.
    - `grep -c "gamelib.log" 34.5-LIVE-GATE.md` returns at least 1 and the document states the `tauri:dev` terminal is NOT the sidecar's output.
    - `grep -c "shortcuts.ts:227" 34.5-LIVE-GATE.md` returns at least 1 — item 4 covers both `exe` call sites, not just `nonesteamgame.ts`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md && grep -c "^### Item" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md</automated>
  </verify>
  <done>The gate exists as an unfilled contract with numbered preconditions, five empty items, and the 34.4.1 sequencing constraint stated as step 1 rather than as a footnote.</done>
</task>

<task type="auto">
  <name>Task 2: Insert Phase 34.6 into ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md L1443-1512 (Phase 34.4.1's entry as the INSERTED-phase format template, Phase 34.5's entry, and Phase 35's entry with its "Depends on: Phases 34.1–34.5" line that must be updated)
    - .planning/phases/34.5-.../34.5-CONTEXT.md D-03 and D-05 (which 16 channels defer and the exact rationale per cluster — EOS is Epic/core-value but not needed to install or launch; SteamGridDB is an enhancement behind a user-supplied API key; winetricks is Linux-centric power-user tooling)
    - .planning/IPC-PORT-INVENTORY.md § "Phase 34.5" (the 57-name list the 16 are drawn from)
  </read_first>
  <action>
    Insert a `### Phase 34.6: Tauri IPC re-plumb slice 9 — EOS overlay, SteamGridDB artwork and
    winetricks (INSERTED)` entry between Phase 34.5's entry and Phase 35's, matching Phase 34.4.1's
    INSERTED-phase formatting exactly (Goal / Requirements / Depends on / Blocks / Plans / a
    `Plans:` checklist stub).

    Content requirements:
      - Goal: port the **16 channels** deferred by Phase 34.5's D-03 — EOS overlay (8):
        `disableEosOverlay`, `enableEosOverlay`, `getEosOverlayStatus`, `getLatestEosOverlayVersion`,
        `installEosOverlay`, `isEosOverlayEnabled`, `removeEosOverlay`, `updateEosOverlayInfo`;
        SteamGridDB artwork (5): `steamgriddb.getGrids`, `steamgriddb.getHeroes`,
        `steamgriddb.hasApiKey`, `steamgriddb.searchGame`, `steamgriddb.setApiKey`; winetricks (3):
        `winetricksAvailable`, `winetricksInstall`, `winetricksInstalled`. State that additive and
        reversible still applies.
      - Record that `winetricksInstall` is `addListener`/send-kind (`tools/ipc_handler.ts`) so the
        next slice inherits the send-channel warning rather than rediscovering it.
      - Record that `callTool`'s `winetricks` branch already works from Phase 34.5 via
        `Winetricks.run()` on the shared `tools/index.ts` object — 34.6 is about the three dedicated
        IPC channels, not about making winetricks work at all.
      - `**Requirements:** TBD — mint at /gsd-plan-phase 34.6`
      - `**Depends on:** Phase 34.5` and `**Blocks:** Phase 35 (the IPC re-plumb must be COMPLETE before cutover)`
      - `**Plans:** 0 plans` with a `Plans:` stub reading `- [ ] TBD (run /gsd-plan-phase 34.6 to break down)`
      - A short provenance note: inserted by Phase 34.5 plan 03 per 34.5 D-03/D-05, dated.

    Then update Phase 35's `**Depends on:**` line so it reads `Phases 34.1–34.6` instead of
    `34.1–34.5`, leaving the rest of that line's reasoning intact.

    Do not renumber, reword or delete any existing phase entry.
  </action>
  <acceptance_criteria>
    - `grep -c "^### Phase 34.6" .planning/ROADMAP.md` returns 1, and its line number is greater than Phase 34.5's heading line and less than Phase 35's.
    - All 16 deferred channel names appear in the new entry — verify each of the 16 with a grep and record the count in the SUMMARY.
    - Phase 35's Depends-on line contains `34.6`.
    - `git diff --stat .planning/ROADMAP.md` shows insertions plus the single Phase 35 line change, and no deletions of existing phase headings: `git diff .planning/ROADMAP.md | grep -c "^-### Phase"` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^### Phase 34.6" .planning/ROADMAP.md && git diff .planning/ROADMAP.md | grep -c "^-### Phase" | grep -q "^0$"</automated>
  </verify>
  <done>Phase 34.6 exists between 34.5 and 35, carries all 16 channel names, and Phase 35's completion gate now names it.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile IPC-PORT-INVENTORY.md's Phase 34.5 arithmetic</name>
  <files>.planning/IPC-PORT-INVENTORY.md</files>
  <read_first>
    - .planning/IPC-PORT-INVENTORY.md (full file, 106 lines — the per-phase section format, the existing reassignment notes under Phases 34.1/34.4/34.4.1 as the tone and structure to match, and the Totals table)
    - .planning/phases/34.5-.../34.5-CONTEXT.md § "Phase Boundary" (the 38-by-cluster table) and D-02/D-03
    - .planning/REQUIREMENTS.md REQ-34.5-11 (the reconciliation obligation: 57 = 38 ported + 3 dropped + 16 moved, "arithmetically checkable")
  </read_first>
  <action>
    Under `## Phase 34.5 — Slice 8`, leave the 57-name list itself INTACT (it remains the audit
    trail) and add a dated scope note directly beneath the heading, in the same voice as the existing
    Phase 34.1/34.4 reassignment notes, recording:
      - Re-scoped 2026-07-29 by 34.5 D-01/D-02/D-03: **57 = 38 ported + 3 dropped + 16 moved**.
      - The 3 DROPPED permanently (`authZoom`, `getZoomUserInfo`, `logoutZoom`) — never ported, zoom
        sign-in dies at the Phase 35 cutover, rationale is D-02 (absent from GameLib's stated core
        value). No new code: `UNPORTED_CHANNEL_MARKER` already declares them honestly.
      - The 16 MOVED to the new `## Phase 34.6` section, listed by name and cluster.
      - `getCometVersion` is GOG's, not zoom's (D-04) and IS ported in 34.5 — record the correction
        so a future reader does not re-derive it.
      - `callTool` arrived from 34.1 (D-14) and `isLoggedIn` from 34.4 (D-03), both already
        reflected in the 57.

    Add a new `## Phase 34.6 — Slice 9 — EOS overlay, SteamGridDB and winetricks (16 channels)`
    section after Phase 34.5's, listing the 16 names in sorted order and stating it runs BEFORE
    Phase 35.

    Update the `## Totals` table only if the ported count changes as a factual matter; if it does
    not change at this point in time, leave it and say so in the SUMMARY rather than editing a number
    to look tidy.
  </action>
  <acceptance_criteria>
    - `grep -c "^## Phase 34.6" .planning/IPC-PORT-INVENTORY.md` returns 1.
    - The Phase 34.5 scope note contains the literal arithmetic `38` , `3` and `16` and the sum statement.
    - The 57-name list under Phase 34.5 is byte-unchanged: `git diff .planning/IPC-PORT-INVENTORY.md` shows no deletion inside that list line.
    - Each of the 3 dropped names appears in the scope note; each of the 16 deferred names appears in the Phase 34.6 section — record both counts in the SUMMARY.
    - The SUMMARY states whether the Totals table was changed and why.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Phase 34.6" .planning/IPC-PORT-INVENTORY.md</automated>
  </verify>
  <done>The slice's 57 reconciles as 38 + 3 + 16, the 16 have a named home, and the original list survives as the audit trail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → Phase 35 cutover decision | Phase 35 deletes the Electron path based on these documents; an unreconciled count is how channels get stranded |
| gate contract → gate execution | A gate authored after the work it verifies can be shaped to pass; authoring it first is the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-08 | Repudiation | a live gate written after the fact | mitigate | The gate doc is created in wave 1 with every result slot empty and `[x]` grep-asserted absent; plan 34.5-15 may only fill slots, never author items. |
| T-34.5-09 | Repudiation | an untracked deferral becoming accepted loss | mitigate | Phase 34.6 is a real ROADMAP entry that Phase 35's Depends-on line now names, so the cutover cannot proceed while it is open (D-05). |
| T-34.5-10 | Tampering | inventory arithmetic edited to look tidy | mitigate | The 57-name list is grep-asserted byte-unchanged; the reconciliation is an additive note, and the SUMMARY must state explicitly whether the Totals table changed. |
| T-34.5-SC | Tampering | npm installs | accept | Docs-only plan; zero packages, zero source files touched. |
</threat_model>

<verification>
- `34.5-LIVE-GATE.md` exists with 5 items, 0 ticked, and 5+ numbered preconditions.
- `### Phase 34.6` exists in ROADMAP.md between 34.5 and 35; Phase 35's Depends-on names it.
- `## Phase 34.6` exists in IPC-PORT-INVENTORY.md; Phase 34.5's 57-name list is byte-unchanged.
- No source file is modified by this plan: `git diff --name-only` shows only `.planning/` paths.
</verification>

<success_criteria>
The blocking gate exists as an unfilled contract before a single channel is ported, and the 16
deferred channels have a tracked home that Phase 35's own completion gate depends on.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-03-SUMMARY.md` when done
</output>
