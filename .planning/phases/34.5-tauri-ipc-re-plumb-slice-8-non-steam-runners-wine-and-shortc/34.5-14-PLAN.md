---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 14
type: execute
wave: 5
depends_on: ["34.5-03", "34.5-13"]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py
autonomous: true
requirements: [REQ-34.5-11]

must_haves:
  truths:
    - "All 38 ported channels are declared in one table with port kind (`sidecar invoke` / `sidecar send`) and an HONEST proof level — `unit`, `unit + LIVE (item N)`, or ported-with-a-declared-caveat. Never a bare 'done'."
    - "Where an automated test proves only wiring, the artifact says so plainly. No reader may take 'wired and unit-proven' for 'seen working'."
    - "Live-gate item 4 covers BOTH `exe` call sites and therefore BOTH channels: `addToSteam` (`nonesteamgame.ts:258`, the Steam VDF `Exe`) and `addShortcut` (`shortcuts.ts:227`, the macOS `.app` `run.sh`, reached via `addShortcuts` → `generateMacOsApp`). Both rows read `unit + LIVE (item 4)`. Attributing item 4 to `addToSteam` alone would understate what the gate proves — the gate's own instructions exercise both."
    - "The document is written BEFORE the live gate runs, so every gate-dependent cell reads PENDING until plan 34.5-15 records a result. A cell may not claim a live result the gate has not recorded."
    - "Obligation closed: `T-34.4.1-44b` (nile's host-free redirect match) is closed by REQ-34.5-02, cited to the `oauthLoginCapture.ts` change."
    - "Declared caveats, not hidden: `processShortcut`'s `ctrl+r` and `ctrl+shift+i` reach `electronStub` logged no-ops and are DEGRADED under Tauri; `callTool`'s `winetricks` branch WORKS despite the winetricks IPC channels being deferred to 34.6; Amazon/GOG cookies persisting in the shared jar after sign-out is the inherited, accepted T-34.4.1-47 residual."
    - "The 3 dropped and 16 deferred channels need NO new code — `UNPORTED_CHANNEL_MARKER` already catches them. The doc states this rather than implying a degradation path was built."
    - "A self-tested gate script binds the declaration to the inventory's arithmetic and to plan 34.5-13's measured registration counts, so the document cannot drift from what actually shipped."
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md"
      provides: "The 38-row declared channel list with kinds and honest proof levels"
      min_lines: 120
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py"
      provides: "Self-tested checks binding the declaration to the inventory and the measured counts"
  key_links:
    - from: ".planning/phases/34.5-.../34.5-PORTED-CHANNELS.md"
      to: ".planning/IPC-PORT-INVENTORY.md § Phase 34.5"
      via: "set-equality and 38+3+16=57 arithmetic cross-check in ported-channels-gate.py"
      pattern: "IPC-PORT-INVENTORY"
---

<objective>
Declare exactly what shipped, at what proof level, and prove the declaration cannot drift from the
code.

Purpose: every prior slice closed with an honest per-channel ledger (34.1-11, 34.2-13, 34.3-12,
34.4-14, 34.4.1-10). The value is entirely in the honesty — a table reading "done" for a channel
only a unit test has touched is worse than no table at all.

Output: 38 declared rows, a named caveat for every degraded behaviour, and a script that fails if
the document and the inventory disagree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-13-SUMMARY.md
@.planning/IPC-PORT-INVENTORY.md

<interfaces>
The declared-list doc shape to copy (34.4.1-PORTED-CHANNELS.md, 251 lines):
  ## What shipped                     — narrative
  ## Obligations closed and opened    — cite the source change for each
  ## Accepted residuals               — with threat / assumption IDs
  <the per-channel table>             — columns EXACTLY:
      | Channel | Kind | Registration module | Proof level | Riders |
      Kind ∈ { `sidecar invoke`, `sidecar send` }
      Proof level ∈ { `unit`, `unit + LIVE (item N)`, a named declared caveat }
  ## Automated vs live proof boundary — what jest/cargo prove vs what only the gate proves

The 5 live-gate items (34.5-LIVE-GATE.md, written by plan 34.5-03):
  1 Epic login from scratch → signed-in, populated library
  2 GOG login from scratch → signed-in, populated library
  3 Amazon login from scratch → populated library, AND the captured redirect origin compared
    against the www.amazon.com anchor
  4 Add a game to Steam AND create a desktop shortcut, then launch both — covering BOTH exe call
    sites and therefore BOTH channels:
      addToSteam   → nonesteamgame.ts:258 (Steam VDF `Exe`)
      addShortcut  → shortcuts.ts:227 (macOS .app run.sh, via addShortcuts → generateMacOsApp)
    Plan-time correction: research Correction 3 and CONTEXT.md D-09 file `:227` under `addToSteam`;
    `generateMacOsApp`'s only caller is `addShortcuts` at `shortcuts.ts:111`, so `:227` is
    `addShortcut`'s. Plans 34.5-08 and 34.5-11 each own one site.
  5 runWineCommand succeeds for at least one NON-Steam runner

The 38 by module (34/4 handle/send): runnerAuth 11 (send: logoutGOG) · wineTools 9 (send: none) ·
shortcuts 7 (send: addShortcut, removeShortcut, processShortcut) · runnerMisc 11 (send: none)

The gate-script precedent: .planning/phases/34.4.1-.../ported-channels-gate.py — 8 self-tested
checks, a `--self-test` mode with deliberately-broken fixtures, exits 0 on both the real document
and the self-test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 34.5-PORTED-CHANNELS.md with all 38 rows and honest proof levels</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md (full file — section order, exact table columns, proof-level vocabulary, and the tone of its residuals section)
    - .planning/phases/34.5-.../34.5-13-SUMMARY.md (the MEASURED registration counts and the reach-ledger delta — proof levels must reflect what was measured, not what was planned)
    - the SUMMARYs of plans 34.5-05 through 34.5-12 (each cluster's actual outcome, including any deviation from its plan — a deviation that is not declared here is a deviation that disappears)
    - .planning/phases/34.5-.../34.5-08-PLAN.md and 34.5-11-PLAN.md (the `exe` call-site ownership split: `addShortcut` owns `shortcuts.ts:227`, `addToSteam` owns `nonesteamgame.ts:258` — both are live-gate item 4)
    - .planning/phases/34.5-.../34.5-LIVE-GATE.md (the 5 items, so each `unit + LIVE (item N)` cell cites the right number)
    - .planning/IPC-PORT-INVENTORY.md § "Phase 34.5" and § "Phase 34.6" (the 38/3/16 reconciliation plan 34.5-03 wrote)
  </read_first>
  <action>
    Write `34.5-PORTED-CHANNELS.md` following the 34.4.1 shape exactly.

    `## What shipped` — 38 of 57 ported; 3 dropped permanently (Zoom, D-02); 16 deferred to the newly
    inserted Phase 34.6 (D-03/D-05). State that the dropped and deferred required NO new code because
    the `UNPORTED_CHANNEL_MARKER` machinery already catches and declares them, and that plan
    34.5-13's test asserts all 19 still reject.

    `## Obligations closed and opened` — CLOSED: `T-34.4.1-44b` (nile's host-free redirect match),
    cited to the `oauthLoginCapture.ts` change in plan 34.5-02. Also record the three CONTEXT.md
    corrections research made and which plan acted on each: D-15's dialog citation (plan 34.5-09
    verified the real site is already safe and built no fix), D-10's second `exe` call site, and
    D-12's `nile_config` label (plan 34.5-10). For the second `exe` site, record the FURTHER
    plan-time correction: `shortcuts.ts:227` is reached by `addShortcut` (via `addShortcuts` →
    `generateMacOsApp`, whose only caller is `shortcuts.ts:111`), NOT by `addToSteam` as research
    Correction 3 and CONTEXT.md D-09 both stated — the shim landed in plan 34.5-01, the `:227` site
    is owned and pinned by plan 34.5-08, and the `:258` site by plan 34.5-11.
    Record the resolved Discretion question — `documents` belongs to saves-sync, not shortcuts
    (plan 34.5-12). OPENED: nothing that is not listed as a residual below.

    `## Accepted residuals` — at minimum these four, each with its threat or assumption ID:
      - `processShortcut`'s `ctrl+r` and `ctrl+shift+i` are DEGRADED under Tauri; they reach
        `electronStub`'s logged `reload`/`openDevTools` no-ops. Say so plainly; do not describe the
        channel as fully working.
      - Amazon and GOG cookies persist in the app-wide jar after sign-out — the inherited, accepted
        T-34.4.1-47 residual. Electron never cleared them either. Any future per-runner clear MUST
        be domain-scoped, never blanket.
      - The `www.amazon.com` anchor is MEDIUM confidence until live-gate item 3 confirms it
        (research Assumption A1).
      - `std::env::current_exe()`'s macOS bundle behaviour is unproven until live-gate item 4
        (research Assumption A2). Note that item 4 tests it through both `addToSteam` and
        `addShortcut`, so a partial pass on one channel does not retire the assumption.

    The per-channel table — 38 rows, columns exactly
    `| Channel | Kind | Registration module | Proof level | Riders |`. Assign proof levels honestly:
      - `unit + LIVE (item 1|2|3)` for the auth channels each respective login exercises.
      - `unit + LIVE (item 4)` for BOTH `addToSteam` AND `addShortcut` — two rows, not one. Put the
        call site each one proves in its `Riders` cell (`nonesteamgame.ts:258` and
        `shortcuts.ts:227` respectively) so the split is legible without reading the plans.
      - `unit + LIVE (item 5)` for `runWineCommand`.
      - `unit` for everything a jest suite alone covers.
      - A named caveat in `Riders` for every degraded or flagged behaviour, including `callTool`'s
        winetricks branch working despite the 34.6 deferral, and `processShortcut`'s two degraded
        hotkeys.
    Every gate-dependent cell must ALSO read PENDING, because the gate has not run when this
    document is written. Do not pre-write a PASS.

    `## Automated vs live proof boundary` — state exactly what jest and cargo prove (registration
    kind in both directions, argument forwarding, the `exe` loud-throw on BOTH channels — rejecting
    for invoke-kind `addToSteam`, `logSendFailure`-with-no-file-written for send-kind `addShortcut` —
    test containment, the measured reach baseline) and what ONLY the gate proves (a real login
    completing, a real Steam entry and a real `.app` each launching the right binary, a non-Steam
    runner really executing a Wine command). Name the standing claim each gate item would retire.
  </action>
  <acceptance_criteria>
    - The table has exactly 38 data rows — count them and record the count in the SUMMARY.
    - Every Kind cell is exactly `sidecar invoke` or `sidecar send`, and exactly 4 rows read `sidecar send`: `logoutGOG`, `addShortcut`, `processShortcut`, `removeShortcut`.
    - Exactly two rows carry `LIVE (item 4)`, and they are the `addToSteam` and `addShortcut` rows; each names its own `exe` call site in `Riders`.
    - Every cell containing `LIVE (item` also contains `PENDING` — no gate-dependent cell claims a result.
    - No proof-level cell reads a bare `done`.
    - The residuals section names all four required items, each with its threat or assumption ID.
    - `grep -c "T-34.4.1-44b" 34.5-PORTED-CHANNELS.md` returns at least 1, inside the obligations-closed section.
    - The obligations section states that `shortcuts.ts:227` belongs to `addShortcut`, correcting research Correction 3 — `grep -c "shortcuts.ts:227" 34.5-PORTED-CHANNELS.md` returns at least 2 (obligations + the `addShortcut` row's Riders cell).
    - The 38 declared channel names are set-equal to the 38 registered by plans 34.5-05..12 — verify by scripting the comparison and record the result in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "sidecar send" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md</automated>
  </verify>
  <done>Thirty-eight rows declare what shipped and at what proof level, both `exe` call sites are attributed to the channel that reaches them, every degraded behaviour is named, and no cell claims a live result the gate has not recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Write a self-tested gate script binding the declaration to reality</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py</files>
  <read_first>
    - .planning/phases/34.4.1-.../ported-channels-gate.py (full file — its 8 checks, the `--self-test` mode with deliberately-broken fixtures, and its exit-code convention)
    - .planning/phases/34.5-.../34.5-PORTED-CHANNELS.md (as written by Task 1)
    - .planning/IPC-PORT-INVENTORY.md § "Phase 34.5" and § "Phase 34.6"
    - src/backend/sidecar/__tests__/runnerSliceRegistration.test.ts (plan 34.5-13's per-module counts — the numbers this script cross-checks against)
  </read_first>
  <action>
    Write `ported-channels-gate.py` in the 34.4.1 precedent's shape, with a `--self-test` mode that
    feeds each check a deliberately-broken fixture and asserts it FAILS. Checks:

      1. Row count is exactly 38.
      2. Every Kind cell is `sidecar invoke` or `sidecar send`, and the send set equals
         `{logoutGOG, addShortcut, processShortcut, removeShortcut}` — set equality, not a count.
      3. Every Proof level cell matches the permitted vocabulary; no bare `done`.
      4. Every cell containing `LIVE (item` also contains either `PENDING` or a recorded `PASS`/
         `FAIL`. Implement the two states explicitly (a mode flag or an accepted-forms list) and
         document which, so plan 34.5-15 can flip PENDING to a result without the script rejecting
         the document.
      5. Arithmetic cross-check against `IPC-PORT-INVENTORY.md`: the 38 declared, plus the 3 dropped
         named in § Phase 34.5's scope note, plus the 16 listed in § Phase 34.6, equal the
         inventory's 57 Phase-34.5 names as a SET, with no name appearing in two buckets.
      6. Per-module row counts are 11 / 9 / 7 / 11.
      7. The residuals section names each of the four required residual items.
      8. The obligations-closed section cites `T-34.4.1-44b`.
      9. The set of channels whose Proof level contains `LIVE (item 4)` equals
         `{addToSteam, addShortcut}` — set equality, not a count — and each of those two rows'
         `Riders` cell names a distinct `exe` call site (`nonesteamgame.ts:258` for `addToSteam`,
         `shortcuts.ts:227` for `addShortcut`). This check exists because the natural mistake is to
         attribute item 4 to `addToSteam` alone, which understates what the gate proves.

    Run it against the real document and with `--self-test`; both must exit 0. If a check cannot be
    made to fail in self-test, that check is not real — either fix it or delete it, and say which in
    the SUMMARY.
  </action>
  <acceptance_criteria>
    - `python3 .planning/phases/34.5-.../ported-channels-gate.py` exits 0 against the real document.
    - `python3 .planning/phases/34.5-.../ported-channels-gate.py --self-test` exits 0, and every check has at least one deliberately-broken fixture it rejects.
    - The 38 + 3 + 16 = 57 set arithmetic check is present and passes, including the no-name-in-two-buckets assertion.
    - Check 9 is present, uses set equality against `{addToSteam, addShortcut}`, and its self-test fixture (item 4 attributed to `addToSteam` alone) is rejected.
    - Check 4 accepts both the PENDING state and a recorded PASS/FAIL state, and the SUMMARY states how that is implemented.
    - The SUMMARY lists each check, whether it passed against the real document on the first attempt, and any check removed with the reason.
  </acceptance_criteria>
  <verify>
    <automated>python3 .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py && python3 .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py --self-test</automated>
  </verify>
  <done>The declaration is machine-checked against the inventory's arithmetic, the measured counts and the two-channel scope of live-gate item 4, and every check is proven capable of failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared artifact → future reader / Phase 35 | The cutover decision is made from these documents; an over-claiming proof level is how UAT debt is created |
| declaration → shipped code | A document that drifts from the registry is worse than none, because it is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-55 | Repudiation | a proof level claiming live verification the gate never recorded | mitigate | Every `LIVE (item N)` cell must also read PENDING when written; the gate script enforces it and its self-test proves the check can fail. |
| T-34.5-56 | Repudiation | the declaration drifting from the registered channel set | mitigate | Set-equality against the 38 registered channels plus the 38+3+16=57 inventory arithmetic, both scripted. |
| T-34.5-57 | Repudiation | a degraded behaviour omitted from the residuals | mitigate | The four required residuals are named checks in the gate script, not prose the author may forget. |
| T-34.5-58 | Tampering | a check that cannot fail | mitigate | `--self-test` feeds every check a broken fixture; a check with no failing fixture is deleted rather than kept as decoration. |
| T-34.5-67 | Repudiation | live-gate item 4 credited to one channel, understating its coverage and leaving `addShortcut`'s `.app` path looking unit-only | mitigate | Gate-script check 9 asserts set equality `{addToSteam, addShortcut}` for `LIVE (item 4)` and requires each row's `Riders` cell to name its own `exe` call site; the self-test fixture attributing item 4 to `addToSteam` alone must be rejected. |
| T-34.5-SC | Tampering | npm installs | accept | Docs and one script; zero packages, zero source files touched. |
</threat_model>

<verification>
- The table has exactly 38 rows, 4 of them `sidecar send`.
- Exactly two rows carry `LIVE (item 4)`: `addToSteam` and `addShortcut`.
- Every `LIVE (item N)` cell also reads PENDING.
- `ported-channels-gate.py` exits 0 on both the real document and `--self-test`.
- `git diff --name-only` shows only `.planning/` paths — no source file is touched by this plan.
</verification>

<success_criteria>
A reader can tell, per channel, exactly what is proven and by what — and a script fails if that
statement ever stops matching the code, the inventory, or the true scope of the live gate.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-14-SUMMARY.md` when done
</output>
