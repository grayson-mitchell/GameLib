---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 32
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE4-ROUTING.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
autonomous: true
gap_closure: true
requirements: [REQ-34.5-11, REQ-34.5-12]

must_haves:
  truths:
    - "A reader who opens plan 34.5-29, 30 or 31 can discover from a committed document that it is halted and why"
    - "No executor can be routed into shipping a user-agent-only Epic fix"
    - "Every item this project knows is UNTESTED has one row in one ledger, including the one a dev-vault run will create"
    - "The blocking five-item live gate is not authored and not scheduled by this cycle"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE4-ROUTING.md"
      provides: "the disposition of plans 29/30/31 and the cycle-4 plan map"
      contains: "BINDING DECISION: fix-first"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md"
      provides: "the standing ledger of explicitly-untested items"
      contains: "KEYCHAIN PATH UNPROVEN"
  key_links:
    - from: "34.5-G6-EPIC-DISCRIMINATOR.md ## Routing"
      to: "34.5-CYCLE4-ROUTING.md"
      via: "the six scope items, each mapped to a cycle-4 plan number"
      pattern: "34\\.5-3[2-7]"
---

<objective>
Record where gap cycle 4 routes each of the six items the discriminator's `## Routing` section named,
and open the standing ledger of explicitly-untested items the rest of this cycle will write to.

Purpose: three authored-but-unexecuted plans (34.5-29/30/31) sit in the phase directory under a
binding halt, and nothing outside the discriminator document says so. Plan 34.5-29's Task 1 was
written to apply "the Epic fix the discriminator SELECTED" — and the discriminator selected NO fix
(R1 FALSIFIED, R2 unconfirmed). Separately, the dev-only secret vault this cycle builds will leave
the real Keychain path UNPROVEN in any run that uses it, and Phase 34.4.1's gate has already
demonstrated what happens when a struck precondition leaves something untested and nothing tracks it.
Both need a durable home before any code lands.

Output: `34.5-CYCLE4-ROUTING.md` and `34.5-UNTESTED-ITEMS.md`.

**This plan writes NO code and authors NO gate.** `BINDING DECISION: fix-first` forbids authoring or
running the blocking five-item live gate this cycle. This plan records that constraint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-28-SUMMARY.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md

**Hard constraint for every task here:** do NOT edit, renumber or delete `34.5-29-PLAN.md`,
`34.5-30-PLAN.md`, `34.5-31-PLAN.md`, any `*-SUMMARY.md`, `34.5-LIVE-GATE.md` or
`34.5-LIVE-GATE-RERUN.md`. Each is the record of its own authoring or its own run. This plan adds two
NEW documents and changes nothing that already exists.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 34.5-CYCLE4-ROUTING.md — disposition of plans 29/30/31 and the cycle-4 map</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR.md` § `## Routing` and § `## Result` — the six scope items and the `BINDING DECISION: fix-first` line, verbatim
    - `.planning/phases/34.5-.../34.5-29-PLAN.md` — read the WHOLE file; its `<objective>` carries a self-halt gate and its Task 1 carries a four-way verdict branch. Both facts are load-bearing for what this document must say
    - `.planning/phases/34.5-.../34.5-30-PLAN.md` and `34.5-31-PLAN.md` — frontmatter and `<objective>` only, enough to state what each would have done
    - `.planning/phases/34.5-.../34.5-28-SUMMARY.md` § "Next Phase Readiness"
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` § D-08 (the no-partial-pass rule, quoted in section 4)
  </read_first>
  <action>
    Create `34.5-CYCLE4-ROUTING.md` with frontmatter carrying `phase`, `plan: 32`, `type: routing`,
    `cycle: 4`, `blocking: false`. Write these five sections.

    **`## Authorisation`** — quote the `BINDING DECISION: fix-first` line verbatim with its source
    file and section name. State plainly: gap cycle 4 is a FIX cycle; the blocking five-item live gate
    is NOT authored and NOT run this cycle; Phase 34.5 does not reach its gate in cycle 4.

    **`## Disposition of plans 34.5-29 / 34.5-30 / 34.5-31`** — one subsection each, each headed with
    status `HALTED — NOT EXECUTED`, each carrying its own detail:

    For 34.5-29, record BOTH of these separately, because they are independent protections and a
    reader must not assume the second follows from the first: (a) its `<objective>` already self-halts
    on `fix-first` — quote its own instruction "STOP IMMEDIATELY. Do not run Task 1, 2 or 3"; and (b)
    even if it were run, its Task 1 branch for `verdict: R1-FALSIFIED` instructs "make NO change to
    `USER_AGENTS`". Conclude explicitly: plan 34.5-29 cannot ship a user-agent-only Epic fix, by two
    independent gates, so it is left in place unmodified as the record of its own authoring. Then
    state what IS superseded: its Task 1 premise ("apply the Epic fix the discriminator SELECTED") is
    void because the discriminator selected no fix — Epic's diagnosis is re-homed to plan `34.5-37`
    this cycle. Then state what is PRESERVED but deferred: its Tasks 2 and 3 (the item-4/5 preflight
    producing `34.5-G6-ITEM45-PREFLIGHT.md`) are still wanted work, are deliberately NOT re-homed into
    cycle 4, and are to be re-homed into whichever cycle authorises the blocking gate — because a
    preflight written now would be stale again by the time the gate runs, which is the precise reason
    those two items have already been skipped twice.

    For 34.5-30: it would have authored `34.5-LIVE-GATE-RERUN-2.md`, the third gate contract. Halted
    by the binding decision; NOT authored this cycle. For 34.5-31: it would have run the third
    blocking gate. Halted; NOT run this cycle.

    **`## Cycle 4 scope map`** — a table with one row per scope item and columns
    `Item | Routing item # | Cycle-4 plan | Wave | What live observation would prove it`. Use exactly
    these plan numbers: GOG library never lands after successful auth → `34.5-33`, wave 1;
    `Refreshing undefined Library` from a second call site → `34.5-33`, wave 1; keyring read timeout
    races human Keychain approval → `34.5-35`, wave 1; capture+authenticated but no propagation (a
    race) → `34.5-34`, wave 1; Epic (F-34.5-G6-01) further diagnosis, explicitly NOT a UA fix →
    `34.5-37`, wave 2; dev-only local secret vault (developer-scoped) → `34.5-36`, wave 2. Add a row
    for `34.5-32` (this plan) as the routing/ledger entry.

    **`## What this cycle does NOT do`** — an explicit list: does not author
    `34.5-LIVE-GATE-RERUN-2.md`; does not run a third gate; does not close Phase 34.5; does not retire
    gate items 4 or 5; does not claim any of the four defects closed on unit evidence. Quote D-08 from
    `34.5-CONTEXT.md` in one line — no partial pass, no advisory mode, no pre-authorized override.

    **`## Standing rule carried into every cycle-4 plan`** — one paragraph: a green test suite is not
    evidence of live behaviour. This phase failed its gate twice while suites of 3279, 3387 and 3447
    tests were green. Every cycle-4 fix plan states the LIVE observable that would prove it, and none
    may record a defect as closed on unit evidence alone.
  </action>
  <verify>
    <automated>bash -c 'D=.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc; F=$D/34.5-CYCLE4-ROUTING.md; test -f $F &amp;&amp; grep -q "BINDING DECISION: fix-first" $F &amp;&amp; for p in 33 34 35 36 37; do grep -q "34.5-$p" $F || exit 1; done &amp;&amp; test -z "$(git diff --name-only -- $D/34.5-29-PLAN.md $D/34.5-30-PLAN.md $D/34.5-31-PLAN.md)"'</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-CYCLE4-ROUTING.md` exists and contains the literal string `BINDING DECISION: fix-first`, attributed to `34.5-G6-EPIC-DISCRIMINATOR.md` § `## Routing`.
    - All five of `34.5-33`, `34.5-34`, `34.5-35`, `34.5-36`, `34.5-37` appear in the scope-map table, each with a wave number and a live-observation cell that is not empty and not the word "tests".
    - The 34.5-29 subsection contains BOTH the self-halt quote ("STOP IMMEDIATELY") AND the R1-FALSIFIED branch quote ("make NO change"), and states the conclusion that 29 cannot ship a UA-only fix by two independent gates.
    - The 34.5-29 subsection states that Tasks 2/3 (`34.5-G6-ITEM45-PREFLIGHT.md`) are deferred to the gate-authorising cycle, with the staleness reason given.
    - `git diff --name-only` reports zero changes to `34.5-29-PLAN.md`, `34.5-30-PLAN.md`, `34.5-31-PLAN.md`, any `*-SUMMARY.md`, `34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md`.
    - No file named `34.5-LIVE-GATE-RERUN-2.md` exists anywhere in the phase directory after this task.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Open 34.5-UNTESTED-ITEMS.md — the standing explicitly-untested ledger</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR.md` § "Smoke observation (part 2)" and § "By-eye checks (part 3)" — the NOT-OBSERVED / NOT CLEANLY OBSERVED / NOT REACHED items recorded there
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR.md` § `## Routing`, the developer-scoped secret-vault paragraph — the source of the KEYCHAIN-PATH-UNPROVEN cost statement
    - `.planning/phases/34.5-.../deferred-items.md` — the existing deferral ledger; this new document must NOT duplicate its rows, it tracks a different thing (untested, versus deferred work)
    - `.planning/phases/34.5-.../34.5-PORTED-CHANNELS.md` — read its proof-level column convention so this ledger's vocabulary matches the phase's existing one
  </read_first>
  <action>
    Create `34.5-UNTESTED-ITEMS.md` with frontmatter `phase`, `plan: 32`, `type: ledger`,
    `status: open`.

    Open with a one-paragraph statement of purpose that names the precedent it exists to prevent:
    Phase 34.4.1's gate passed 4/4 while domain-scoping went untested, because a struck precondition
    removed the only thing that would have exercised it and no artifact tracked the hole. This ledger
    is where a hole goes so it cannot become an implicit pass.

    Then a table with columns
    `ID | Item | Why untested | Created by | Retired by (the exact observation that would close it) | Status`.
    Seed it with these rows, each `Status: OPEN`:

    - `U-34.5-01` — the real macOS Keychain read/write path for `steam-refresh-token`,
      `humble-session` and `humble-csrf`. Why untested: any run with the dev-only secret vault enabled
      (plan `34.5-36`) bypasses the keyring entirely. The cell MUST contain the literal phrase
      `KEYCHAIN PATH UNPROVEN`. Retired by: a live run with `GAMELIB_DEV_SECRET_VAULT` unset in which
      the sidecar log shows a `SidecarKeyringSlotStore` read succeeding for at least one slot, and
      shows no `dev-secret-vault` install line. Created by: plan 34.5-36.
    - `U-34.5-02` — gate item 4 (`addToSteam` plus the macOS `.app` shortcut, both `getPath('exe')`
      call sites). Why untested: NOT ATTEMPTED on both gate runs. Retired by: a gate item-4 PASS
      covering BOTH call sites, never one.
    - `U-34.5-03` — gate item 5 (`runWineCommand` for a non-Steam runner). Why untested: NOT ATTEMPTED
      on both gate runs; research Pitfall 2 stands. Retired by: a gate item-5 PASS.
    - `U-34.5-04` — Amazon verification-code field legibility (F-34.5-G6-05, plan 34.5-27 Task 2). Why
      untested: NOT REACHED in the 2026-08-01 checkpoint. Retired by: a live Amazon login reaching the
      code field with the field's text legible.
    - `U-34.5-05` — login-window origin display CHANGING within a single flow. Why untested: per-page
      origin display is CONFIRMED-LIVE, but a before/after transcription of the title-bar text
      changing within one flow was never captured. Retired by: a transcribed before/after pair from
      one flow (GOG's `login.gog.com` → `embed.gog.com` is the known candidate).
    - `U-34.5-06` — Epic's success path end to end. Why untested: Epic has never reached
      `status=captured`; R1 falsified, R2 unconfirmed. Retired by: one `runner=legendary
      status=captured` followed by a `useTauriOAuthLogin` `phase=idle` line in the same session.

    Close with a `## Rules` section stating: (1) a row is retired ONLY by the observation named in its
    own row, never by a passing test suite and never by inference from a neighbouring row; (2) any
    plan that creates a new untested hole MUST add its row in the same commit that creates the hole;
    (3) this ledger is read at every gate-authoring decision, and any OPEN row that the gate's own
    items would have covered must be named in the gate contract as a known hole rather than silently
    assumed covered.
  </action>
  <verify>
    <automated>bash -c 'D=.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc; F=$D/34.5-UNTESTED-ITEMS.md; test -f $F &amp;&amp; grep -q "KEYCHAIN PATH UNPROVEN" $F &amp;&amp; for i in 01 02 03 04 05 06; do grep -q "U-34.5-$i" $F || exit 1; done &amp;&amp; test "$(grep -c "OPEN" $F)" -ge 6'</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-UNTESTED-ITEMS.md` exists with all six seeded rows `U-34.5-01` through `U-34.5-06`, each with a non-empty "Retired by" cell naming a concrete observation (a log line, a transcription, or a named gate item) — never "tests pass".
    - The `U-34.5-01` row contains the literal phrase `KEYCHAIN PATH UNPROVEN` and names `GAMELIB_DEV_SECRET_VAULT` as the variable whose absence is part of its retirement condition.
    - The purpose paragraph names Phase 34.4.1's struck-precondition/domain-scoping precedent explicitly.
    - The `## Rules` section states all three rules, including that a passing test suite never retires a row.
    - `deferred-items.md` is unmodified — confirmed by `git diff --name-only` showing it absent from the change set.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → future executor | A halted plan left undescribed is an instruction a future executor may follow |
| untested state → closure decision | An untracked hole becomes an implicit pass at the next gate |
| live evidence → public-fork document | Log excerpts pasted into a repository that is publicly forked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C4-01 | Repudiation | plans 34.5-29/30/31 sitting halted with no external record | mitigate | Task 1 writes `34.5-CYCLE4-ROUTING.md` naming each as `HALTED — NOT EXECUTED`, and records the two independent gates that stop 29 shipping a UA fix |
| T-34.5-C4-02 | Repudiation | an untested item silently read as covered at the next gate | mitigate | Task 2's ledger, with a per-row retirement observation and the rule that a passing suite retires nothing |
| T-34.5-C4-03 | Tampering | this plan rewriting an existing run record to fit the new cycle | mitigate | Both tasks create NEW files only; Task 1's automated verify asserts `git diff --name-only` is empty for plans 29/30/31 |
| T-34.5-C4-04 | Information disclosure | credential material copied into the new documents while quoting evidence | mitigate | Neither task quotes log evidence containing values — only `label=`/`status=`/`phase=` shapes and plan prose. The evidence-handling rule (T-34.5-G6-02) applies: redact in place, never paste an access/refresh token, cookie, session id or Steam `userdata` account id |
| T-34.5-C4-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. Any install would require `slopcheck` plus a blocking human checkpoint first |
</threat_model>

<verification>
- Both new documents exist and pass their own automated greps.
- `git status --porcelain` shows exactly two added files under the phase directory and no modified files under it.
- No `.ts`, `.tsx` or `.rs` file appears in this plan's change set.
</verification>

<success_criteria>
The halt on plans 29/30/31 is recorded outside the discriminator document with the two independent
reasons plan 29 cannot ship a UA fix; every one of the six Routing scope items maps to a named
cycle-4 plan and wave; and a standing ledger exists with six seeded untested items, each retirable
only by a named live observation.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-32-SUMMARY.md` when done
</output>
