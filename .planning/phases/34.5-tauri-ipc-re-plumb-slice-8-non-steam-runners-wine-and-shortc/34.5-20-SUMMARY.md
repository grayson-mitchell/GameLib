---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 20
subsystem: testing
tags: [live-gate, sidecar, tauri, oauth, legendary, gogdl, nile, oauthLoginCapture, TauriLoginPanel, wine, shortcuts]

# Dependency graph
requires:
  - phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
    provides: "Plans 34.5-16..18's publicDir/runner-binary fix (G-1) and plan 34.5-19's re-run contract (34.5-LIVE-GATE-RERUN.md)"
provides:
  - "A completed (not passing) blocking 5-item live-gate RE-RUN for Phase 34.5's Epic/GOG/Amazon
    OAuth logins, dual exe call-site shortcuts, and non-Steam Wine command"
  - "Proof that the G-1 publicDir/runner-binary defect is closed (items 2 and 3 both reach
    status=captured at the backend for the first time — the first run never got past ENOENT)"
  - "A new, undiagnosed downstream defect family: a successful backend OAuth capture is never
    consumed into a completed, UI-visible, library-populating login for GOG and Amazon; Epic's
    login form never becomes interactive at all"
  - "Assumption A1 (the www.amazon.com host anchor) CONFIRMED against one real captured redirect,
    proven structurally from matchOAuthRedirect's own code"
  - "Six new findings (F-34.5-G6-01..06) for the next gap cycle to scope, recorded observation-only"
affects: [34.5-gap-cycle-2, phase-34.6]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A sub-clause (Assumption A1) can be independently CONFIRMED by code-structural proof even
      while its parent item FAILs — the two verdicts are recorded distinctly and the falsification
      section spells out why the sub-clause confirmation does not retire the broader claim"
    - "Live-gate items are recorded FAIL/NOT ATTEMPTED verbatim, never softened, per D-08's
      no-partial-pass rule — mirrors 34.4.1's and 34.5-15's own gate discipline"

key-files:
  created: []
  modified:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md

key-decisions:
  - "Corrected a pre-supplied reading of the log (orchestrator-gathered evidence claimed '4 legendary
    timeouts'; direct grep of gamelib.log shows exactly 3 opens / 3 timeouts / 0 captures) rather
    than adopting the unverified count — re-verified from the log directly per the task's own
    instruction not to trust gathered evidence blindly"
  - "Assumption A1 recorded as CONFIRMED, not UNTESTED, based on a structural proof read directly
    from matchOAuthRedirect's source: a nile status=captured outcome is only reachable via an exact
    www.amazon.com hostname match, so a captured outcome with zero origin-mismatch lines is itself
    proof the comparison path was reached and matched cleanly — this is a stronger basis than 'the
    line happened to be absent'"
  - "Item 3 recorded FAIL despite A1 being CONFIRMED, and the falsification section's claim #2 left
    explicitly STANDING despite A1's confirmation — the contract's own eligibility rule requires the
    whole item to PASS, and the two verdicts (sub-clause vs. parent item) are not the same question"
  - "items_passed + items_failed = 3, not 5 — items 4 and 5 NOT ATTEMPTED, confirmed explicitly by
    the developer when asked; arithmetic and reasons stated in the frontmatter per the contract's
    own no-quiet-rounding rule"
  - "Verdict computed and recorded without diagnosing the new downstream defect in this document —
    per the plan's own instruction, diagnosis is deferred to the next gap cycle; six findings
    (F-34.5-G6-01..06) recorded observation-only for that cycle to scope"

patterns-established: []

requirements-completed: []

# Metrics
duration: 55min
completed: 2026-08-01
---

# Phase 34.5 Plan 20: Blocking Live-Gate RE-RUN — Execution and Record Summary

**Live-gate re-run executed on real macOS hardware, verdict FAIL 0/5 (3 FAIL, 2 NOT ATTEMPTED) —
the G-1 publicDir/runner-binary defect is proven closed (items 2/3 both reach `status=captured` for
the first time ever), but a new downstream defect blocks all three login items: Epic's form never
becomes interactive, and GOG/Amazon's successful backend captures are never consumed into a
completed, UI-visible, library-populated login. Phase 34.5 does NOT close.**

## Performance

- **Duration:** 55 min (this continuation session, Tasks 2-3; Task 1 was executed and committed by
  a prior agent in the same plan, commit `8ea770e2f`)
- **Started:** 2026-08-01 (session start, Task 2 recording)
- **Completed:** 2026-08-01 (Task 3 commit `fe799e29b`)
- **Tasks:** 3 (Task 1 previously completed; Tasks 2-3 completed this session)
- **Files modified:** 1 (`34.5-LIVE-GATE-RERUN.md`)

## Accomplishments

- Recorded all 5 blocking live-gate items with verbatim `gamelib.log` evidence, cross-checked
  directly against the log file rather than trusting the developer's report or the orchestrator's
  gathered summary at face value:
  - **Item 1 (Epic): FAIL.** 3 login-window opens, 3 `status=timeout`, 0 `status=captured` — the
    login form renders but is non-interactive ("greyed out"). Corrected a pre-supplied "4 timeouts"
    reading to the log's actual count of 3.
  - **Item 2 (GOG): FAIL.** Backend reaches `status=captured` (11:15:45) for the first time this
    phase, but no follow-up `gogdl auth` CLI call and no frontend `[TauriLoginPanel]
    captured-blocked` transition ever fire afterward; the UI stays on the pre-capture "Signing
    in…" panel and the library is never populated.
  - **Item 3 (Amazon): FAIL,** same downstream-of-capture pattern as item 2 (`status=captured` at
    11:10:10, no follow-up `nile auth --login`, no frontend transition). **Assumption A1
    (`www.amazon.com` anchor) is CONFIRMED** — proven structurally from `matchOAuthRedirect`'s own
    code (`src/backend/sidecar/oauthLoginCapture.ts:103-127`): a `nile` `status=captured` outcome
    is only reachable via an exact hostname match, so a captured outcome plus a zero-count
    `origin-mismatch` grep is itself proof the comparison path was reached and matched cleanly.
    This does not retire item 3's own FAIL (library never populated) or the broader "session
    end-to-end" claim (which requires the whole item to PASS).
  - **Items 4 and 5: NOT ATTEMPTED**, confirmed explicitly by the developer when asked. Reasons
    recorded: item 4's groundwork target depends on a working in-session GOG login that item 2 did
    not deliver; item 5 requires an authenticated non-Steam runner that no item delivered.
- Added a "New findings (this run)" register with six defects (F-34.5-G6-01..06) surfaced by the
  developer's report beyond the five items' own PASS/FAIL scope, each with evidence, diagnosis
  explicitly deferred to the next gap cycle: Epic's greyed-out form; the capture-without-UI-update
  pattern on GOG/Amazon; the unpopulated GOG library and missing filter option; no origin/URL shown
  in the login window (a usability and phishing-resistance concern); black-on-black text in
  Amazon's verification-code field; double Keychain prompts on GOG sign-out.
- Computed and recorded the verdict: frontmatter `verdict: FAIL`, `items_passed: 0`,
  `items_failed: 3`, `items_not_attempted: 2`, with the arithmetic (3 + 2 = 5) stated explicitly.
- Resolved "What this gate falsifies" for all four pre-listed standing claims: none struck. Claim 2
  (the `www.amazon.com` anchor) required special care — its own sub-clause (A1) is CONFIRMED within
  item 3's Result, but the claim's eligibility rule requires the whole item to PASS, so the claim
  stays explicitly STANDING with the distinction spelled out in the document to prevent the two
  verdicts being conflated by a future reader.
- Verified `34.5-LIVE-GATE.md` (the superseded first-run document) remains byte-unchanged after
  every edit (`git status --porcelain` empty on that path, checked after both commits).
- Verified no credential value (`access_token`, `refresh_token`, cookie, session identifier) appears
  in either commit's diff — checked directly with a targeted grep before committing.

## Task Commits

Each task was committed atomically:

1. **Task 1: Satisfy and evidence all seven preconditions, in order** - `8ea770e2f` (docs) —
   completed by a prior agent; all seven preconditions SATISFIED, including precondition 4's proof
   that the G-1 fix (`source=GAMELIB_APP_ROOT`, `publicDir exists=true`, all four runner binaries
   `exists=true`, no `SIDECAR ASSET ROOT DEFECT`) is present in the build under test.
2. **Task 2: Developer drives all five gate items on real hardware** - `a3b22cadb` (docs) — recorded
   the developer's real-hardware run: 3 FAIL log excerpts with A1's CONFIRMED sub-clause, 2
   NOT-ATTEMPTED with reasons, plus a six-item new-findings register.
3. **Task 3: Record the verdict and the falsification ledger** - `fe799e29b` (docs) — frontmatter
   verdict FAIL, Verdict table, "What this gate falsifies" (no strikes, with the A1/claim-2
   distinction spelled out).

## Files Created/Modified

- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md` — all 5 item Result slots filled with verbatim log evidence, a "New findings" register added, frontmatter/Verdict/falsifies sections updated to FAIL

## Decisions Made

- Re-verified the orchestrator-gathered log evidence directly from `gamelib.log` rather than
  adopting it as-is, per the task's own instruction — found and corrected one discrepancy (legendary
  timeouts: 3 actual vs. 4 claimed) and confirmed the rest (nile/gog capture counts, the zero-count
  `origin-mismatch` grep).
- Went beyond the plan's explicit read list to read `TauriLoginPanel.tsx` directly, discovering that
  its `phase === 'blocked'` branch logs `[TauriLoginPanel] captured-blocked: runner=... ` on a
  successful capture reaching the panel — a line that never appears in the log for either GOG or
  Amazon despite both reaching `status=captured` at the backend. This is the load-bearing evidence
  for the "downstream of capture" defect pattern recorded in both items' Results and in the new
  findings register; without reading that component's source directly, this defect would have been
  recorded only as "library wasn't populated," a much weaker and less actionable finding.
- Recorded Assumption A1 as CONFIRMED using a structural code-proof (read directly from
  `oauthLoginCapture.ts`) rather than the more common-but-weaker "the line happened to be absent"
  reading — the contract explicitly warned against picking the flattering reading when both fit;
  the structural proof removes the ambiguity rather than picking a side.

## Deviations from Plan

None — plan executed exactly as written for Tasks 2-3. The recording rules given in this
continuation's prompt (redaction, A1 classification discipline, the "confirm or falsify the
gathered evidence yourself" instruction) were followed, and one discrepancy in the gathered evidence
was found and corrected as instructed rather than silently adopted.

## Issues Encountered

None blocking. The main investigative work (reading `oauthLoginCapture.ts` and
`TauriLoginPanel.tsx` directly, and grepping `gamelib.log` for the frontend's own
`captured-blocked` log line) was necessary to distinguish "the capture worked but nothing consumed
it" from "the capture itself is flaky" — the log alone (backend-only) would have supported either
reading; the frontend source read resolved it.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

**Phase 34.5 is NOT complete.** The blocking live-gate re-run FAILED (0 of 5 items PASS), and per
D-08 this phase does not close. Plan 34.5-21 is expected to propagate this verdict (per this plan's
own scope boundary — `34.5-PORTED-CHANNELS.md`, the inventory, ROADMAP, and STATE.md are
deliberately untouched by this plan). Gap-cycle candidates for the next planning pass, in order of
what a fix must not get wrong:

1. **The downstream-of-capture defect (blocks items 2 and 3, and by extension item 1's own distinct
   failure mode is a separate but related concern).** `captureOAuthLogin()` resolves
   `{status: 'captured', code, redirectUrl}` correctly for both `gog` and `nile` — proven by the
   backend log. Nothing in the current wiring appears to consume that resolved value: no follow-up
   `gogdl auth`/`nile auth --login` CLI invocation fires afterward, and
   `TauriLoginPanel.tsx`'s own `phase === 'blocked'` branch (which would log
   `[TauriLoginPanel] captured-blocked: runner=...`) never fires for either runner. Whether the gap
   is in `useTauriOAuthLogin.ts` (the hook that is supposed to drive `state.phase` from the
   `oauthCaptureLogin` promise) or a missing consumer entirely was not diagnosed in this document —
   left for the gap cycle, per this plan's own instruction not to diagnose here.
2. **Epic's login form never becomes interactive** ("greyed out", 0/3 captures across three
   attempts) — a distinct defect from items 2/3's pattern, since Epic never even reaches a captured
   redirect. Needs its own root-cause investigation; possibly a UA/embed-mode issue specific to
   `legendary`'s login flow, given the other two runners' windows were interactive.
3. **Six usability/UX findings (F-34.5-G6-01..06)** in the gate document's own "New findings"
   section — most notably F-34.5-G6-04 (no origin/URL shown in the login window, a
   phishing-resistance concern) and F-34.5-G6-05 (black-on-black verification-code text under
   WKWebView).
4. **Items 4 and 5 remain genuinely unproven** — neither `addToSteam`/`shortcuts.ts`'s dual
   `exe`-call-site behavior nor `runWineCommand` against an authenticated non-Steam runner has ever
   been live-exercised. Both need a login-completing fix (items 1-3) before they can be attempted
   again.

Credential-aside state note carried forward: Epic and GOG credentials were signed OUT coming into
this run (precondition 5) but the developer's session ended this run with GOG signed back IN
(captured, even if not reflected in the UI) and Amazon's password having been reset via a real
Amazon password-reset flow. A future gate re-run's own precondition 5 must re-verify current
credential state from scratch rather than assume this run's aside state persisted — it plausibly
did not, especially for GOG and Amazon.

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-08-01*

## Self-Check: PASSED

- FOUND: `34.5-LIVE-GATE-RERUN.md`
- FOUND: `34.5-20-SUMMARY.md`
- FOUND commit `8ea770e2f` (Task 1)
- FOUND commit `a3b22cadb` (Task 2)
- FOUND commit `fe799e29b` (Task 3)
- `34.5-LIVE-GATE.md` confirmed byte-unchanged (`git status --porcelain` empty)
