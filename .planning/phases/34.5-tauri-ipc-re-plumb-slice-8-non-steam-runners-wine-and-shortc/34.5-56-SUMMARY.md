---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 56
subsystem: qa-gate-authoring
tags: [live-gate, contract-authoring, dxvk, nile, redaction, structural-reachability-review]

# Dependency graph
requires:
  - phase: 34.5 gap cycle 7 (plans 34.5-52, -53, -54, -55)
    provides: "The four code fixes this contract's four items must live-verify"
  - phase: 34.5 gap cycle 7 (plan 34.5-57)
    provides: "F-34.5-G6-22 (blocker) and halt_gate: true, the precondition this contract's own Precondition 0 encodes"
provides:
  - "34.5-LIVE-GATE-RERUN-4.md -- the fifth blocking live-gate contract, authored UNFILLED (verdict: null), executed_by 34.5-59"
  - "A gate-time HALT-GATE precondition (Precondition 0) that mechanically blocks launch while F-34.5-G6-22/halt_gate: true is open"
  - "D-CYCLE7-B fixture-blocked annotations on U-34.5-29/U-34.5-27 in 34.5-UNTESTED-ITEMS.md"
affects: [34.5-59, 34.5-60]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Every evidence-line citation re-verified against current source at authoring time (not transcribed from a prior contract or the routing document) -- caught and corrected one stale grep form for the banner function during this plan's own authoring"

key-files:
  created:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md
  modified:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md

key-decisions:
  - "Precondition 0 (the HALT-GATE check) is placed before every other precondition, not folded into precondition 4's keyring section, because F-34.5-G6-22 blocks the whole run (item 2 specifically, and by extension D-08's all-four-in-one-session requirement), not merely a keyring-arm sub-condition."
  - "The banner-function fix-presence grep in precondition 9 was corrected from an unanchored form (which returns 11 on current source, matching ten unrelated #[cfg(test)] function names) to an anchored, full-signature form (which returns 1) -- verified live against current source rather than transcribed, per this cycle's own repeated 'wrong evidence line' lesson."
  - "Tasks 1-2 were authored as one continuous pass (the contract head, items, known-holes and out-of-scope tables are interdependent) and committed as a single commit; Task 3's ledger annotation was committed separately, matching the plan's own file-level task boundary (the Review section itself was authored in the same pass as Tasks 1-2 since splitting an already-cohesive single document after the fact would have added no real atomicity)."

requirements-completed: [REQ-34.5-12]

# Metrics
duration: ~70min
completed: 2026-08-14
---

# Phase 34.5 Plan 56: Fifth Blocking Live-Gate Contract (RERUN-4) Summary

**Authors `34.5-LIVE-GATE-RERUN-4.md` -- the fifth blocking live-gate contract, verdict null, every RESULT slot empty -- with a mechanical gate-time precondition that blocks the run while `F-34.5-G6-22`'s `halt_gate: true` is open, corrected DXVK/banner/redaction evidence lines, and a Structural Reachability Review (all seven tests) delivered before publication.**

## Performance

- **Duration:** ~70 min
- **Completed:** 2026-08-14
- **Tasks:** 3/3 completed (authored as two commits at the file-level boundary, see Decisions)
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments

- `34.5-LIVE-GATE-RERUN-4.md` created: frontmatter `verdict: null`, `items_total: 4`, all four
  count fields `null`, `supersedes: 34.5-LIVE-GATE-RERUN-3.md` (no prior gate document touched --
  confirmed by `git diff --stat` listing only the two files this plan's own frontmatter declares).
- **Precondition 0** encodes the `halt_gate`/`F-34.5-G6-22` block as a mechanical check
  (`grep -n "halt_gate:" 34.5-CYCLE7-REVIEW.md`) that must read `halt_gate: false` with a fix
  commit hash recorded, before Precondition 1 may even begin -- independently re-confirmed against
  current source (`launcher.ts:1697-1704`/`:1888`) at this document's own authoring time, not
  taken on the review's word alone.
- Evidence-handling rule extended to a seventh redaction value class (`code_verifier`), with the
  extended sweep regex re-validated at authoring time against 4 safe and 4 leak-shaped specimens --
  real terminal output captured verbatim in the contract (all 8 behaved as expected: safe
  specimens exit 1, leak-shaped specimens exit 0 and match).
- Item 1 clause (i) re-targeted from the structurally-impossible `NSWindow` title bar to the
  in-page origin banner (`__gamelib_login_origin_banner__`); new clauses (m) (cancel strip
  simultaneously visible+clickable, confirmed WITHOUT clicking mid-flow) and (n) (the shell's own
  `[shell]` confirmation lines, correctly routed to the transcript sink, never `gamelib.log`).
- Item 2 clause (o) adds the four-grep credential-redaction live-confirmation check plus a
  `code_verifier` leak grep, closing the live-confirmation obligation `34.5-53-SUMMARY.md` deferred
  to this contract.
- Item 4's evidence corrected per `F-34.5-G6-19`: dispatch marker `tools/index.ts:369`, the
  `runWineCommand` emitter `launcher.ts:1520` (`logDebug`, re-confirmed at this authoring, not
  `:1581`), `native,builtin` reg-add literals, `current_dxvk-macOS` before/after filesystem pair,
  a mandatory >=30s settle window, and precondition 12(c)'s genuine-OFF-state check (reading
  `autoInstallDxvk` + the marker file from disk BEFORE the operator opens the Wine tab) -- the
  direct fix for run 4's own `U-34.5-30` defect (a week-old stale ON value silently absorbing the
  click that never happened).
- Known-holes table recounted at authoring time, not transcribed: **32 total rows, 18 OPEN, 14
  RETIRED** (recount command and raw output below). 18-row table, one row per OPEN item.
- D-CYCLE7-B out-of-scope table added for `U-34.5-29` (Amazon library population) and `U-34.5-27`
  (real GOG `.ico` decode), both `blocked_on: operator-supplied test fixture`, both staying OPEN.
- Cross-cutting observation 1 (`U-34.5-22`, GOG uninstall tile-flip) marked MANDATORY TO ATTEMPT
  this run, with an explicit-refusal rule for a third consecutive silent omission.
- Structural Reachability Review delivered as a section of the contract itself: all seven tests
  (Tests 1-7), a table with zero IMPOSSIBLE verdicts, Test 5's pairing pass stated as
  M=4 (state-mutating) × N=5 (evidence-bearing) = 20 pairs considered, with the six specific pairs
  the plan named all recorded FLAGGED-AND-RESOLVED. Tests 6 and 7 explicitly noted as being applied
  for the first time.
- `34.5-UNTESTED-ITEMS.md`: `U-34.5-29` and `U-34.5-27` both amended with a
  `blocked_on: operator-supplied test fixture` disposition, staying OPEN, per D-CYCLE7-B. Ledger
  total unchanged: 32 rows before, 32 rows after.

## Task Commits

1. **Tasks 1-2: author the contract head, evidence rules, preconditions, items, known-holes,
   out-of-scope and cross-cutting observations (Structural Reachability Review authored in the
   same continuous pass, see Decisions)** - `b1456701b` (docs)
2. **Task 3: annotate `U-34.5-29`/`U-34.5-27` with D-CYCLE7-B fixture-blocked disposition** -
   `61080c23d` (docs)

## Files Created/Modified

- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md` - new file, 987 lines, the fifth blocking live-gate contract
- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md` - two rows amended, ledger total unchanged (32)

## Recount command and raw output (Ledger Rule 3, known-holes table)

```
$ grep -oE '^\| U-34\.5-[0-9]+' 34.5-UNTESTED-ITEMS.md | wc -l
32
```

Cross-checked programmatically against each row's own FINAL Status-cell marker (several rows carry
an earlier `OPEN` before a later `RETIRED` update, so the recount takes the LAST marker in each
row, not the first):

```
OPEN: 18 ['01', '03', '04', '05', '10', '18', '20', '21', '22', '23', '24', '26', '27', '28', '29', '30', '31', '32']
RETIRED: 14 ['02', '06', '07', '08', '09', '11', '12', '13', '14', '15', '16', '17', '19', '25']
```

18 matches the known-holes table's own row count in the contract exactly.

## Source facts re-verified at authoring time (not transcribed)

All literal citations in the contract were re-grepped against current source before being written
in, per this cycle's own "wrong evidence line" lesson (`F-34.5-G6-18`→`-19`, `launcher.ts:1581`→
`:1520`):

- `tools/index.ts:369` — `logInfo([\`installing ${tool} on...\`, prefix], LogPrefix.ToolInstaller)` — confirmed exact line via `grep -n`.
- `launcher.ts:1520` — `logDebug(['Running Wine command:', commandParts.join(' ')], LogPrefix.Backend)` — confirmed exact line.
- `launcher.ts:1697-1704`/`:1888` — `getRunnerCallWithoutCredentials`'s `sensitiveArg` loop (`['--code', '--token']`) — confirmed `--code-verifier` is not a member, corroborating `F-34.5-G6-22`.
- `main.rs:1606`/`:3704-3706`/`:3803-3806` — the banner element id, and the two `eprintln!` confirmation lines — confirmed present and unchanged since 34.5-52 landed.
- **Corrected during this plan's own authoring:** `grep -c "fn login_origin_banner_script" src-tauri/src/main.rs` returns `11` on current source (matches ten `#[cfg(test)]` function names sharing the substring), not `1` as an unanchored form would suggest. The corrected, anchored form `grep -c "^fn login_origin_banner_script(origin: &str) -> String {" src-tauri/src/main.rs` returns `1`. The contract's own precondition 9 uses the corrected form and documents why the unanchored form is wrong.
- `redactNileRegisterData` count in `nile/user.ts` — confirmed `2` (definition + one call site).
- `WINETRICKS_DECLINED_GUARD` count in `Winetricks/index.tsx` — confirmed `2`.
- `src/backend/tools/__tests__/dxvkEvidenceLines.test.ts` — confirmed exists.

## Decisions Made

- Precondition 0 (HALT-GATE) placed first, before Precondition 1, rather than folded into an
  existing precondition — `F-34.5-G6-22` blocks the entire run (D-08's all-four-in-one-session
  requirement), not a single sub-condition.
- The banner fix-presence grep corrected from the plan's own literally-specified (but stale) form
  to an anchored form that actually isolates the real function definition — documented inline in
  the contract itself as a deliberate correction, not silently substituted.
- Tasks 1-2 committed together (one continuous authoring pass across the contract head, evidence
  rules, items, known-holes, out-of-scope table, cross-cutting observations, and the Structural
  Reachability Review, since these sections are interdependent and were drafted as one document);
  Task 3's ledger annotation committed separately, matching the actual file-level change boundary.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] The banner fix-presence grep specified in the plan's own task text is stale
against current source**
- **Found during:** Task 1, verifying precondition 9's four cycle-7 fix-presence greps against
  current source before writing them into the contract.
- **Issue:** `grep -c "fn login_origin_banner_script" src-tauri/src/main.rs` (the unanchored form)
  returns `11`, not `1` — it also matches ten `#[cfg(test)]` test-function names sharing the same
  substring (e.g. `fn login_origin_banner_script_never_uses_innerhtml`), all added by plan 34.5-52.
- **Fix:** Used an anchored, full-signature form,
  `grep -c "^fn login_origin_banner_script(origin: &str) -> String {" src-tauri/src/main.rs`, which
  correctly isolates the one real function definition and returns `1`. Documented the correction
  inline in the contract's own precondition 9, per this cycle's own "wrong evidence line" defect
  class this plan was explicitly instructed to avoid repeating.
- **Files modified:** `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md` (same file/commit).
- **Verification:** Both grep forms re-run live and their outputs (11 vs. 1) recorded verbatim in this SUMMARY above.
- **Committed in:** `b1456701b`.

**2. [Rule 1 - Bug] The "devtools" acceptance criterion's own literal check (occurrences confined
to the no-console section) was violated by a legitimate cross-reference in item 3's evidence text**
- **Found during:** Task 1's own self-verification pass, running the acceptance-criteria greps
  before considering the document complete.
- **Issue:** Item 3's evidence text originally referenced "NOT the DevTools console — see the
  No-console rule above," which is a correct, non-instructional mention, but placed the literal
  word `DevTools` outside the No-console rule section, failing the plan's own literal
  "only occurrences inside the no-console rule section" acceptance check.
- **Fix:** Reworded to "per the No-console rule above — never a typed console invocation," removing
  the literal word `DevTools` from that location while preserving the identical meaning and the
  cross-reference.
- **Files modified:** Same file, same commit.
- **Verification:** `grep -ni "devtools"` now returns exactly 3 hits, all within lines 252-260 (the
  No-console rule section).
- **Committed in:** `b1456701b`.

**3. [Rule 1 - Bug] The ledger update paragraph's own summary text accidentally duplicated the
literal `blocked_on:` string, breaking the exact-count acceptance check on `34.5-UNTESTED-ITEMS.md`**
- **Found during:** Task 3's own self-verification pass.
- **Issue:** The plan's own acceptance criterion requires `blocked_on: operator-supplied test
  fixture` to appear **exactly 2** times in the ledger file (once per amended row). A summary
  paragraph added at the top of the file (documenting the recount, per this file's own convention
  of dated update paragraphs) repeated the same literal phrase a third time.
- **Fix:** Reworded the summary paragraph to describe the annotation without repeating the exact
  literal string, while still naming both rows and both fixtures in full.
- **Files modified:** `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md`.
- **Verification:** `grep -c "blocked_on: operator-supplied test fixture"` now returns exactly `2`.
- **Committed in:** `61080c23d`.

---

**Total deviations:** 3 auto-fixed, all Rule 1 (self-caught defects in this plan's own draft,
found and fixed during its own verification pass before commit). None affected the contract's
substance — all three are corrections to citation accuracy or self-verification-check compliance.

## Issues Encountered

None beyond the three auto-fixed items above.

## HONESTY NOTE (binding per this plan's critical_context)

**This plan proves nothing live.** It authors a contract — a document describing what MUST be
observed, with every evidence line re-verified against current SOURCE (not against a live run) at
authoring time. Nothing in `34.5-LIVE-GATE-RERUN-4.md` claims any of the four items has passed,
that the banner renders, that the nile redaction holds under a real login, or that the DXVK toggle
produces the corrected evidence live. The `verdict: null` frontmatter and every empty `**Result:**`
slot are the explicit, structural statement of that fact. Plan 34.5-59 is the only artifact that
can fill any of it in, and per Precondition 0, it cannot even begin until `F-34.5-G6-22` is fixed
and `34.5-CYCLE7-REVIEW.md`'s `halt_gate` reads `false`.

## Known Stubs

None. No hardcoded empty values, placeholder text, or unwired data sources — this plan produces
planning/QA-gate documents, not application code.

## Threat Flags

None beyond what the plan's own threat register (`T-34.5-C7-18` through `-22`, `T-34.5-C7-SC`)
already names and mitigates. The evidence-handling rule's own redaction-sweep specimens
(deliberately synthetic, sentinel-style values like `abcdefghij1234567890` used to prove the regex
is non-vacuous) were re-checked against the mandatory pre-commit sweep before commit and confirmed
to be the ONLY matches — synthetic self-test specimens, not real credentials, mirroring the same
pattern `34.5-53-SUMMARY.md`'s own `SENTINEL_CODE_ac91f3` RED-proof captures use.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `34.5-LIVE-GATE-RERUN-4.md` is ready for plan 34.5-59 to execute, but is BLOCKED by its own
  Precondition 0 until `F-34.5-G6-22` is fixed (a new plan, per `34.5-CYCLE7-REVIEW.md`'s own
  routing candidate — between 34.5-57 and 34.5-58, or folded into 34.5-58) and
  `34.5-CYCLE7-REVIEW.md`'s `halt_gate` is amended to `false` with the fix's commit hash recorded.
- `34.5-UNTESTED-ITEMS.md` reconciles at 32 rows, unchanged in count, with `U-34.5-29`/`U-34.5-27`
  now explicitly fixture-blocked rather than silently uncovered.
- No blocker introduced by this plan itself beyond the pre-existing, independently-discovered
  `F-34.5-G6-22`.

## Self-Check: PASSED

- FOUND: `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-4.md`
- FOUND: `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md`
- FOUND: commit `b1456701b` (Tasks 1-2)
- FOUND: commit `61080c23d` (Task 3)

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-08-14*
