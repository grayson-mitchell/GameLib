---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 19
subsystem: testing
tags: [live-gate, gate-contract, publicDir, getAppPath, oauth, wine, shortcuts, gap-closure]

# Dependency graph
requires:
  - phase: 34.5-16
    provides: "GAMELIB_APP_ROOT handoff — the fix this re-run's precondition 4 must prove is present in the build"
  - phase: 34.5-17
    provides: "Existence-checked binary fallback + real-filesystem sidecar coverage — the second half of the G-1 closure"
  - phase: 34.5-18
    provides: "Boot-time [bootstrap] GAMELIB_SHELL_EXE received= and asset-root self-check log lines — the exact evidence source this re-run's preconditions 4 and 7 cite"
provides:
  - "34.5-LIVE-GATE-RERUN.md — an authored, unrun re-run contract for all 5 blocking live-gate items, verdict: null, superseding 34.5-LIVE-GATE.md without editing it"
  - "A precondition (4) that proves the G-1 fix is present in the exact build under test, from the sidecar's own boot log, before any login is attempted"
  - "A closed gate-contract defect (precondition 7 vs the old precondition 5) — GAMELIB_SHELL_EXE is now read from gamelib.log, not the pnpm tauri:dev terminal"
affects: [34.5-20, 34.5-21]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Re-run gate documents supersede without overwriting — 34.5-LIVE-GATE.md stays byte-unchanged (verified by git status --porcelain after every commit in this plan), mirroring Phase 34.4.1's own three-document gate history"
    - "Preconditions cite log-line shapes read directly from source at authoring time (src/backend/sidecar/bootstrap.ts, src/backend/sidecar/oauthLoginCapture.ts), never copied from a SUMMARY, so the contract cannot cite a line that does not exist in shipped code"

key-files:
  created:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md
  modified: []

key-decisions:
  - "The re-run goes in a NEW document (34.5-LIVE-GATE-RERUN.md), never overwriting 34.5-LIVE-GATE.md — verified after every commit in this plan via git status --porcelain reporting nothing for the superseded document"
  - "Precondition 4's source= field check (GAMELIB_APP_ROOT vs process.cwd) is a hard gate, not advisory — a process.cwd source means the Rust-side handoff did not reach the sidecar this run, even if the resolved path happens to look correct by coincidence"
  - "Item 3's Assumption A1 recording rule was strengthened relative to the first run's contract: a clean login with the origin-mismatch line never firing is recorded CONFIRMED (not merely 'not falsified'), distinguishing that outcome from a genuinely unreached code path (UNTESTED)"
  - "Precondition 6's exit-code floor is stated as a range (3447/80 as the original floor, 3463/84 as the gap-cycle's own accumulated baseline per 34.5-16/17/18's SUMMARYs) rather than a single number, so a re-run at a stale HEAD between those two points is also flagged for explanation, not just a regression below the floor"

patterns-established: []

requirements-completed: []

# Metrics
duration: 35min
completed: 2026-08-01
---

# Phase 34.5 Plan 19: Author the Live-Gate Re-Run Contract Summary

**Authored `34.5-LIVE-GATE-RERUN.md` — a 5-item, 7-precondition contract with every Result slot
empty and `verdict: null`, closing the two defects that blocked the first run's contract itself
(an unsatisfiable precondition 5, and no proof the G-1 fix is in the build) — without running
anything live and without touching the superseded `34.5-LIVE-GATE.md`.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-01 (session start, after reading all 8 required context files)
- **Completed:** 2026-08-01
- **Tasks:** 2 completed, plus 1 Rule 2 fix commit
- **Files modified:** 1 created

## Accomplishments

- Created `34.5-LIVE-GATE-RERUN.md` with frontmatter (`verdict: null`, `status: pending`,
  `blocking: true`, `items_passed: 0`, `items_failed: 0`, `supersedes: 34.5-LIVE-GATE.md`) and a
  body stating explicitly that this document supersedes but does not edit `34.5-LIVE-GATE.md`
  (which holds the FAILED first run of 2026-08-01), and that D-08's no-partial-pass rule applies
  unchanged.
- Authored 7 numbered preconditions, each with an empty `RESULT —` slot:
  1. Carries forward Phase 34.4.1's own gate PASS (quoted verbatim from
     `34.4.1-LIVE-GATE-RERUN-3.md`), with the standing warning that `34.4.1-LIVE-GATE.md` and
     `-RERUN.md` hold only superseded FAILED/partial runs.
  2. Clean-machine check (`ps aux | grep -E "gamelib-shell|sidecar.js"`), citing the orphaned
     process finding from the first run's own precondition 2.
  3. Clean rebuild + `pnpm tauri:dev`, sidecar confirmed alive via `gamelib.log` growing, never
     the terminal.
  4. **The G-1 fix proof** — quotes all four `[bootstrap]` log-line shapes verbatim from
     `src/backend/sidecar/bootstrap.ts` (read directly at authoring time, not copied from
     `34.5-18-SUMMARY.md`), requires `source=GAMELIB_APP_ROOT` (not `process.cwd`) and
     `exists=true` on `publicDir` and all four runner binaries, and states a
     `SIDECAR ASSET ROOT DEFECT` line BLOCKS items 1-3 outright.
  5. Credential-aside verification with `ls`/`test -f` on both original and backup paths for
     Epic/GOG, plus a re-verify (not assume) instruction for Amazon's `nile_config`/`nile_store`.
  6. Full suite re-run with exit codes recorded verbatim; the 2026-08-01 floor (3447 jest / 80
     Rust) stated explicitly as a floor, with the gap-cycle's own higher accumulated baseline
     (3463/84, from 34.5-16/17/18's SUMMARYs) also named so a stale-but-not-regressed HEAD is
     flagged too.
  7. **Closes the old precondition 5 defect** — cites `[bootstrap] GAMELIB_SHELL_EXE received=`
     (read directly from `bootstrap.ts` source), states this is the preferred closure option
     `34.5-LIVE-GATE.md` named, carries the dev-vs-packaged caveat and the `R-34.5-G1-PKG`
     parallel-limit warning forward verbatim.
- Authored 5 items, each with an empty `[ ] PASS   [ ] FAIL` slot, carrying `34.5-LIVE-GATE.md`'s
  wording forward rather than re-deriving it:
  - Items 1-3 each name what changed since the failed run (the app-root handoff) and point at
    precondition 4 as proof.
  - Item 3 carries the `[oauthLoginCapture] runner=nile origin-mismatch origin=` grep (confirmed
    present verbatim in `src/backend/sidecar/oauthLoginCapture.ts` at authoring time) and adds a
    CONFIRMED/FALSIFIED/UNTESTED three-way recording rule for Assumption A1, strengthening the
    first run's own two-way (confirmed-or-not) framing.
  - Item 4 carries the unconsumed groundwork from `34.5-LIVE-GATE.md`'s "Item 4 partial progress"
    (the Alan Wake target, both `exe` call sites, both `window.api` invocations, the quit-Steam
    warning) and adds one instruction the old contract lacked: re-verify the target is still
    installed before starting, since a fresh GOG login (item 2) re-populates the library.
  - Item 5 states plainly that last run's FAIL was by blockage only, and that research Pitfall 2's
    claim is not retired without a PASS here.
- Closed with a reserved Verdict table (all cells empty), a "What this gate falsifies" section
  binding four standing claims to their specific items with an explicit no-strike-without-PASS
  rule (and the dev-only limit on Assumption A2/`R-34.5-G1-PKG`), and an arithmetic rule for a
  not-attempted item mirroring the first run's own frontmatter discipline.
- **Rule 2 fix, discovered during self-review before writing this SUMMARY:** the plan's own
  `threat_model` names `T-34.5-G6-02` (information disclosure via pasted log excerpts) with a
  `mitigate` disposition requiring an explicit instruction that secrets are never pasted into a
  Result slot. Neither task commit included this instruction. Added a standalone
  "Evidence-handling rule" paragraph ahead of the Preconditions section, binding on every Result
  slot, naming the specific value types to redact (`access_token`/`refresh_token`, cookies,
  session identifiers, the Steam `userdata` account id) and the redact-in-place convention.
- `python3 ported-channels-gate.py` re-run after every commit in this plan (3 total) — exits 0
  each time, confirming this doc-only plan touched no channel declaration.
- `git status --porcelain` on `34.5-LIVE-GATE.md` confirmed empty after every commit — zero bytes
  changed in the superseded document across the whole plan.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author the re-run contract's frontmatter and seven preconditions** - `010b97bf6`
   (docs)
2. **Task 2: Author the five items, item 4's carried-forward groundwork, and the recording
   rules** - `cf5533832` (docs)
3. **Rule 2 fix: add missing T-34.5-G6-02 evidence-redaction rule** - `4369d5166` (docs) —
   discovered during pre-SUMMARY self-review of the plan's own threat model, not part of either
   task's literal action text but required by the threat register's `mitigate` disposition.

## Files Created/Modified

- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md`
  — created across three commits (frontmatter + preconditions; items + closing sections; the
  T-34.5-G6-02 redaction rule). 451 lines total. `verdict: null` throughout, every Result slot
  empty, no PASS/FAIL recorded anywhere.

## Decisions Made

- Split the file's authoring into two atomic commits matching the plan's own Task 1/Task 2
  structure, even though both were drafted together — Task 1's content (frontmatter through the
  end of precondition 7) was isolated with `awk` before the first commit, then the full content
  restored for Task 2's commit, mirroring this phase's established one-commit-per-task convention
  (e.g. `34.5-17`, `34.5-18`).
- Item 3's recording rule was strengthened beyond the plan's literal instruction: the plan asked
  for "confirmed" vs "UNTESTED" as the two outcomes; a third outcome (a clean login where the
  mismatch line correctly never fires) was added as explicitly CONFIRMED rather than leaving it
  ambiguous between "not falsified" and "confirmed" — this is a direct reading of the plan's own
  action text ("If a clean login completes and the line never fires...") rather than a deviation
  from it.
- Precondition 6's floor was stated as a range (3447/80 to 3463/84) rather than the single number
  the plan's action text quoted (3447 jest / 80 Rust), because the three prior plans in this gap
  cycle (34.5-16/17/18) had each already raised the baseline before this plan started — using only
  the older, lower number would let a stale-but-not-regressed HEAD pass this precondition
  incorrectly.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 — missing critical functionality] Added the T-34.5-G6-02 evidence-redaction rule.**
- **Found during:** Post-task self-review of the plan's own `<threat_model>` block, before writing
  this SUMMARY.
- **Issue:** The plan's threat register names `T-34.5-G6-02` (information disclosure via log
  excerpts pasted into the committed planning document) with a `mitigate` disposition, requiring
  the contract to instruct that `access_token`/`refresh_token` values, cookies, session
  identifiers, and the Steam `userdata` account id are never pasted, and that only the log lines
  an item names are quoted. Neither Task 1's nor Task 2's action text mentioned this instruction
  explicitly, and it was absent from the document after both tasks committed.
- **Fix:** Added a standalone "Evidence-handling rule (T-34.5-G6-02)" paragraph immediately after
  the "Why this run exists" narrative and before the Preconditions section, stating the rule is
  binding on every Result slot and naming the redact-in-place convention (`access_token=<REDACTED>`
  rather than omitting the whole line).
- **Files modified:** `34.5-LIVE-GATE-RERUN.md`.
- **Verification:** `git status --porcelain` on `34.5-LIVE-GATE.md` still empty;
  `ported-channels-gate.py` still exits 0; the two prior grep-based acceptance checks (5 `[ ] PASS`
  slots, `1207659037` present) re-confirmed unchanged.
- **Committed in:** `4369d5166`.
- **Impact on plan:** None on the two tasks' own acceptance criteria (both were independently
  satisfied before this fix) — this closes a threat-model requirement the plan's task-level
  acceptance criteria did not separately enumerate.

---

**Total deviations:** 1 auto-fixed (Rule 2, above). 0 Rule 1/3/4 triggers.
**Impact on plan:** None on scope or correctness — every acceptance criterion in both tasks, plus
the threat-model's own mitigation requirement, is independently verified (see Self-Check below).

## Issues Encountered

None beyond the Rule 2 gap documented above.

## User Setup Required

None — no external service configuration required. This plan authored a document only; no live
session was run.

## Next Phase Readiness

**This plan does NOT close any gap (G-2, G-3, G-4, G-6) by itself.** It produces the contract that
plan 34.5-20 must execute live to close them. `34.5-LIVE-GATE-RERUN.md` is complete, internally
consistent, and unrun:

- All 7 preconditions and 5 items have empty Result slots; frontmatter carries `verdict: null`,
  `items_passed: 0`, `items_failed: 0`.
- Precondition 4 is the load-bearing check plan 34.5-20 must satisfy before attempting items 1-3 —
  if `gamelib.log` shows a `SIDECAR ASSET ROOT DEFECT` line or a `process.cwd` app-root source,
  the run must stop and route back to a gap cycle rather than proceed.
- `34.5-LIVE-GATE.md` remains byte-unchanged and continues to hold the record of the superseded
  0/5 FAILED first run — verified via `git status --porcelain` after all three commits in this
  plan.
- Precondition 5's credential-aside state (Epic/GOG backups from plan 34.5-15) is assumed to
  persist but is NOT re-verified by this plan — plan 34.5-20 must actually run the `ls`/`test -f`
  checks this contract specifies, not assume the state described here.
- Item 4's groundwork (Alan Wake target, both call sites) is carried forward but not re-verified —
  plan 34.5-20 must re-check the install still exists before attempting item 4, per this contract's
  own added instruction.

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-08-01*

## Self-Check: PASSED

- FOUND: `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md`
- FOUND commit `010b97bf6` (Task 1)
- FOUND commit `cf5533832` (Task 2)
- FOUND commit `4369d5166` (Rule 2 fix)
- CONFIRMED: `34.5-LIVE-GATE.md` byte-unchanged (`git status --porcelain` empty)
- CONFIRMED: `verdict: null` present exactly once (frontmatter)
- CONFIRMED: exactly 7 preconditions with empty `RESULT —` slots
- CONFIRMED: exactly 5 items with empty `[ ] PASS   [ ] FAIL` slots
- CONFIRMED: `python3 ported-channels-gate.py` exits 0
