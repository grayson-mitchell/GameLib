---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 28
subsystem: auth
tags: [oauth, epic, gog, keyring, tauri, wkwebview, discriminator]

# Dependency graph
requires:
  - phase: 34.5-24
    provides: "the F-34.5-G6-01 discriminator contract (two readings, decision rule, instrumentation) and the GAMELIB_OAUTH_UA_LEGENDARY override"
  - phase: 34.5-23
    provides: "oauthCaptureLogin exempted from the 60s invoke bound"
  - phase: 34.5-25
    provides: "keyring in-flight dedupe plus bounded failure memo"
  - phase: 34.5-26
    provides: "captured OAuth code routed through the post-login library-refresh completion path"
  - phase: 34.5-27
    provides: "login window origin display plus light interface style, Amazon code-field legibility fix"
provides:
  - "F-34.5-G6-01 discriminator executed live: R1 (user-agent gating) FALSIFIED, R2 not confirmed, routed to a further diagnosis cycle rather than a guessed fix"
  - "one non-Steam runner (GOG) driven end to end live, exposing that F-34.5-G6-02/03's capture-to-hook chain is fixed but the hook-to-populated-library chain is not"
  - "four new diagnosed defects for the next gap cycle: GOG library never lands, Refreshing undefined Library, keyring read timeout races human approval, capture+auth-but-no-propagation race"
  - "binding developer decision (fix-first) recorded structurally in the discriminator's ## Routing section so plans 34.5-29/30 cannot silently skip it"
affects: [34.5-29, 34.5-30, 34.5-31, epic-login, gog-login, keyring]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pre-registered discriminator contracts (readings + decision rule fixed before the experiment) prevent shipping the nicer of two readings that both fit one measurement"
    - "Structural BINDING DECISION line inside a plan's own output document, read by downstream plans as an authorisation gate, rather than relying on an autonomous plan's SUMMARY recommendation alone"

key-files:
  created: []
  modified:
    - ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md"

key-decisions:
  - "R1 (user-agent gated Epic login) is FALSIFIED live: both stock-agent and Chrome-shaped-agent arms produced a non-interactive form/error. R2 is not thereby confirmed per the contract's own text."
  - "BINDING DECISION: fix-first — plans 34.5-29/30 must NOT proceed to authoring or running the blocking five-item gate this cycle; a further gap cycle is required first"
  - "Developer additionally scoped a dev-only local secret vault (env-gated, loud-warned, production-refused) into the same gap cycle to remove Keychain prompts as a confounder in future gate runs, with the keychain path explicitly tracked as untested while the vault is in use"

patterns-established: []

requirements-completed: [REQ-34.5-04, REQ-34.5-12]

# Metrics
duration: 25min
completed: 2026-08-01
---

# Phase 34.5 Plan 28: Epic OAuth Discriminator + GOG Smoke Checkpoint Summary

**Live-ran the pre-registered Epic UA discriminator (R1 falsified, R2 unconfirmed) and a GOG smoke test that proved the capture→hook propagation fix works but exposed the library never actually populates; developer chose fix-first, halting the blocking gate for a further gap cycle.**

## Performance

- **Duration:** ~25 min (Tasks 3-4 only; Tasks 1-2 were live developer-driven checkpoints executed outside this agent's runtime)
- **Started:** 2026-08-01 (continuation agent spawn)
- **Completed:** 2026-08-01T07:39:36Z
- **Tasks:** 4/4 (Tasks 1-2 human-verify checkpoints completed by the developer with evidence supplied verbatim to this agent; Tasks 3-4 executed by this agent)
- **Files modified:** 1 (`34.5-G6-EPIC-DISCRIMINATOR.md`)

## Accomplishments

- Filled the F-34.5-G6-01 discriminator's Result section from live two-arm evidence, re-verifying every pasted log line directly against `gamelib.log`/`gamelib.log.old` with no discrepancies found; verdict `R1-FALSIFIED` per the contract's own pre-registered decision rule.
- Identified and recorded a contract arithmetic defect (the `len=52` precondition can never match the 54-byte literal UA string) without silently correcting the pre-registered section — recorded as evidence in the Result section instead.
- Recorded the GOG smoke observation and by-eye origin/Amazon checks, labelling each of plans 34.5-23/25/26/27 CONFIRMED-LIVE, FALSIFIED, or NOT-OBSERVED per the contract's own instruction.
- Recorded the developer's binding `fix-first` decision structurally in `## Routing`, so plans 34.5-29/30 cannot silently proceed to the blocking gate without reading it.

## Task Commits

Tasks 1-2 were live human-verify checkpoints — no code/doc commits from this agent (the developer drove the app directly; evidence was collected by the orchestrator and supplied to this agent verbatim).

1. **Task 3+4: Fill discriminator verdict, smoke observation, by-eye checks, Routing, and BINDING DECISION** - `442d36285` (docs)

**Note on commit granularity:** the plan's protocol calls for Task 3 and Task 4 to land in separate commits. Because the developer's Task 4 decision (`fix-first`) was already made and supplied to this agent alongside Task 3's evidence — and because Task 3's own acceptance criteria require the `## Routing` section (which the `BINDING DECISION:` line is appended to) to be written as part of filling the Result — both tasks' content was written to the same section of the same file in a single pass. Splitting this into two commits would have required an artificial intermediate edit-and-revert with no real diff between them. This was judged the more honest choice over manufacturing a churn commit; documented here as a deviation.

**Plan metadata:** _(pending — this SUMMARY's own commit)_

## Files Created/Modified

- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md` - Result section filled (both arms, verdict, smoke observation, by-eye checks, Routing, BINDING DECISION). No source files (`.ts`/`.tsx`/`.rs`) touched, per the plan's own verification requirement.

## Decisions Made

- **R1-FALSIFIED, not NOT-REPRODUCED or NEITHER-READING-FITS.** Both arms (stock Epic UA, Chrome-shaped UA) produced a non-interactive result — Arm A a greyed-out form, Arm B an explicit `Parameter "client_id" is required` error page. This is the "Both arms non-interactive" outcome combination the contract fixed in advance, selecting `R1-FALSIFIED` with R2 left unconfirmed (merely surviving), per the contract's own text. Routed to a further diagnosis cycle rather than shipping R2 as a guessed fix.
- **Arm B accepted as VALID despite the `len=54` vs. contract's `len=52` mismatch.** The mismatch is the contract's own arithmetic error (the literal UA string is verifiably 54 bytes, confirmed two independent ways), not evidence the override failed to take effect — the receipt line's presence is what matters, and it is present.
- **`fix-first` recorded as the binding decision**, per the developer's own already-made choice, presented in the objective context with the full evidence set. Plans 34.5-29/30 halt at their authorisation gates; the blocking gate (34.5-30/31) is not authored this cycle.

## Deviations from Plan

### Auto-fixed Issues

None — Tasks 3 and 4 required no code fixes; this plan modifies only the discriminator planning document per its own scope (`autonomous: false`, `files_modified` restricted to the discriminator doc).

### Process Deviation (not a Rule 1-4 auto-fix)

**1. Tasks 3 and 4 committed together rather than as two separate commits**
- **Found during:** Task 4 (recording the binding decision)
- **Issue:** the plan's execution protocol calls for one commit per task. Task 4's entire scope (append `BINDING DECISION: fix-first` to `## Routing`) is a sub-part of the same section Task 3 was already required to write as part of its own acceptance criteria (`## Routing` naming plan 34.5-29's required work, with the binding-decision line appended once Task 4 completes).
- **Resolution:** wrote both tasks' content in a single edit and a single commit (`442d36285`), since the developer's Task 4 decision was already known at the time Task 3 was executed (supplied verbatim in this agent's spawn context) and no meaningful intermediate state existed between "Task 3 done, Task 4 pending" and "both done."
- **Files modified:** `34.5-G6-EPIC-DISCRIMINATOR.md` (same file, single commit)
- **Impact:** cosmetic only — the plan's acceptance criteria (verdict recorded, Routing present, BINDING DECISION line present, no gate-item PASS/FAIL recorded) are all satisfied; git history has one fewer commit than the ideal but no information or auditability was lost, since the underlying evidence for the decision (four options + full record) predates both tasks and is fully documented in this agent's spawn context.

---

**Total deviations:** 1 process deviation (commit granularity), 0 auto-fixes.
**Impact on plan:** No scope creep, no code changes outside the plan's own file-modification scope.

## Issues Encountered

- **Contract arithmetic defect found in the discriminator itself** (`len=52` precondition, actual literal is 54 bytes). Not a bug this plan's task set is scoped to fix (the discriminator's pre-registered "experiment" section is explicitly off-limits to edits per this plan's own hard constraints) — recorded as evidence in the Result section instead, with the substantive precondition (override receipt line present) confirmed satisfied by other means.
- **Instruction in the contract ("grep for the receipt line before clicking Epic") is structurally impossible** — `resolveUserAgent()` is only called inside `.open(loginUrl, ...)`, so the receipt line cannot exist before the window opens. Recorded as a note rather than an edit to the pre-registered section.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Blocking gate (plans 34.5-30/31) is NOT authorised to proceed this cycle.** `BINDING DECISION: fix-first` is recorded in `34.5-G6-EPIC-DISCRIMINATOR.md`'s `## Routing` section. Plan 34.5-29 must address, in the order recorded in Routing:

1. GOG library never lands after successful auth (`No cache found, getting data from gog...` repeats without a completion line, unlike Steam's `fetched N owned games` → `sync complete` pair in the same session).
2. `Refreshing undefined Library` fires from a caller other than the one plan 34.5-26 fixed (which correctly logs `Refreshing gog Library`).
3. Keyring read timeout (8s, `src-tauri/src/main.rs:640`) races human Keychain approval time, falsifying plan 34.5-25 live (two prompts observed for `steam-refresh-token` in one session).
4. A race where a fully captured-and-authenticated GOG login sometimes does NOT produce a `useTauriOAuthLogin` line (attempt 2) and sometimes does (attempt 3), on an otherwise identical code path.
5. Epic (F-34.5-G6-01) further diagnosis, per this checkpoint's own R1-FALSIFIED/R2-unconfirmed verdict — NOT a UA-only fix. Concrete next discriminator named in Routing: run `EPIC_LOGIN_URL` under Electron (`npm start`) to separate "broken by the Tauri port" from "broken by an Epic server-side change."

Plus a developer-scoped item for the same gap cycle: a dev-only, env-gated, loud-warned, production-refused local secret vault to remove Keychain prompts as a confounder from future gate runs — with the explicit cost that the real Keychain path stays untested while the vault is used, and must be tracked as such (the precedent 34.4.1's gate already fell into once).

**Win, live-proven for the first time outside the test suite:** F-34.5-G6-02/03 (the 60s invoke-bound exemption plus the capture-to-library-refresh completion path) is CLOSED for GOG — `useTauriOAuthLogin` lines now fire on both the timeout and success paths, where the 2026-08-01 session had zero across six terminal outcomes. Limit stated honestly: proven for GOG only; Epic's own success path remains unproven because Epic never captures.

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-08-01*

## Self-Check: PASSED

- FOUND: `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md`
- FOUND: commit `442d36285` in `git log --oneline --all`
- FOUND: this SUMMARY.md on disk
