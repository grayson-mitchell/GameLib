---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 40
type: execute
wave: 3
depends_on: [34.5-38, 34.5-39]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md
autonomous: true
gap_closure: true
cycle: 5
requirements: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-04, REQ-34.5-05, REQ-34.5-12]

must_haves:
  truths:
    - "A third gate contract exists with verdict: null, written before any live work of that run begins"
    - "It covers all five items, including the two never attempted, each with evidence observable downstream of the thing it measures"
    - "Item 1 enters BLOCKED with its cause named, and BLOCKED is defined as counting toward neither passed nor failed"
    - "Every OPEN ledger row the gate's own items touch is named in the contract as a known hole, never silently assumed covered"
    - "Neither prior gate document is modified — each remains the record of its own failed run"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md"
      provides: "the third blocking gate contract, unfilled"
      contains: "verdict: null"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN-2.md"
      to: "34.5-G6-ITEM45-PREFLIGHT.md"
      via: "items 4 and 5 inline the verified preflight rather than referencing it"
      pattern: "ITEM45-PREFLIGHT"
---

<objective>
Author `34.5-LIVE-GATE-RERUN-2.md`: the CONTRACT for the third blocking five-item live gate, written
with `verdict: null` BEFORE any live work of that run begins.

Purpose: this is the discipline plan 34.5-19 followed for run 2 and plan 34.5-03 followed for run 1,
and it is the only reason either run's evidence is trustworthy. A contract authored after the fact
can be shaped by what happened; one authored before it cannot. This plan writes the contract and
fills in NOTHING.

**This plan supersedes plan 34.5-30**, which was HALTED by cycle 3's `BINDING DECISION: fix-first`
and never authored. Plan 34.5-30 is not edited, not renumbered and not executed; it stays on disk as
the record of its own halt. Its authorisation gate is spent — cycle 5's own authorisation
(`34.5-CYCLE5-ROUTING.md`, D-CYCLE5-A) replaces it, and this plan runs unconditionally under it.

Output: a new file. **`34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` are NOT overwritten, NOT
edited and NOT deleted** — each is the completed record of its own FAILED run (0/5 and 0/5), and
anyone citing either as evidence of a PASS is citing the wrong document.

**This plan runs nothing live.** Plan 34.5-41 executes this contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md

**Run `graphify query "<question>"` before opening raw source** (project rule, `CLAUDE.md`). This
plan opens source only to read the ACTUAL log-line shapes it cites, which it must do rather than
copying them from a SUMMARY.

<interfaces>
Log-line shapes verified by direct source read at planning time. **Re-read each from its own file
before citing it** — a contract that cites a line the code does not emit is the exact defect run 1
shipped at its own precondition 5.

- `src/backend/sidecar/oauthLoginCapture.ts`
  - `:298` `[oauthLoginCapture] runner=${runner} label=${openedLabel}`
  - `:255` `[oauthLoginCapture] runner=${runner} nav host=${navHost}`
  - `:231` `[oauthLoginCapture] runner=${runner} status=${outcome.status}`
  - `:150` `[oauthLoginCapture] runner=nile origin-mismatch origin=${parsed.origin}` (warning level)
  - `:89`  `[oauthLoginCapture] runner=${runner} user-agent-override len=${override.length}`
- `src/frontend/screens/WebView/useTauriOAuthLogin.ts` — `[useTauriOAuthLogin] runner=${activeRunner} phase=<value>`
  with these terminal/observable values: `idle (login completed, library refresh triggered)` (:320),
  `timeout` (:239), `cancelled` (:233), `blocked channel=<channel>` (:350), `error` (:254),
  `error capture-transport-failed message=<msg>` (:212), `teardown inflight=<bool>` (:371), and four
  `cancelled-midflight at=<login-url|capture-transport|capture|auth>` lines (:180, :202, :224, :309).
- `src/frontend/state/GlobalState.tsx:1012` — `[refreshLibrary] runner=${runnerScope} origin=${origin}`
- `src/backend/sidecar/steamFlowRegistration.ts`
  - `:151` `refreshLibrary complete runner=${runner} managers=1`
  - `:214` `refreshLibrary complete runner=all managers=${managers.length}`
  - `:125` `refreshLibrary failed runner=${rawRunner}: unknown runner`
  - `:144` `refreshLibrary failed runner=${runner}:` (with the error appended)
- `src/backend/sidecar/bootstrap.ts`
  - `:356` `[bootstrap] secret stores: dev-vault`
  - `:367` `[bootstrap] secret stores: keyring`
  - plus the boot self-check lines `[bootstrap] appRoot resolved=… source=…`,
    `[bootstrap] publicDir resolved=… exists=…`, `[bootstrap] runner binary … path=… exists=…`,
    `[bootstrap] GAMELIB_SHELL_EXE received=…`, and the `[bootstrap] SIDECAR ASSET ROOT DEFECT` line
- `src/backend/sidecar/devSecretVault.ts` — `[dev-secret-vault] INSTALLED — …` (:274),
  `[dev-secret-vault] read key=<slot>` (:146), `[dev-secret-vault] write key=<slot>` (:154),
  `[dev-secret-vault] REFUSED — …` (:245/:253/:264). The enabling variable is an exact-string
  `=== '1'` match on `GAMELIB_DEV_SECRET_VAULT`; `0`, `false` or unset do NOT enable it.

Cycle-4 constants whose presence the contract must prove at HEAD (all recorded UNIT-PROVEN ONLY):
- `KEYRING_READ_TIMEOUT` 8s→45s in `src-tauri/src/main.rs`
- `KEYRING_FAILURE_MEMO_MS` 15s→120s in `src/backend/sidecar/keyringTokenStore.ts`
- the `handleRefreshLibrary` gate in `steamFlowRegistration.ts` excluding BOTH `undefined` and `null`
- the cancellation-aware `safeSetState` helper in `useTauriOAuthLogin.ts`

Cycle-3 fixes whose presence run 2's own precondition-4 pattern requires proving at HEAD:
- `oauthCaptureLogin` present in `src-tauri/src/main.rs`'s `LONG_RUNNING_CHANNELS`
- `capture-transport-failed` present in `useTauriOAuthLogin.ts`
- the completion-callback wiring present at `src/frontend/screens/WebView/index.tsx`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontmatter, framing, the known-holes table, and eleven numbered preconditions</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` (whole file — the shape to mirror: frontmatter, the seven numbered preconditions with their RESULT blocks, the evidence-handling rule, the arithmetic rule)
    - `.planning/phases/34.5-.../34.5-CYCLE5-ROUTING.md` (this cycle's authorisation, D-CYCLE5-A/B verbatim, the four-branch routing table, and the MEASURED baseline that becomes precondition 6's floor)
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` (all 11 rows and Rule 3 — every OPEN row this gate's items would have covered must be NAMED here as a known hole)
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` § D-08 (the no-partial-pass rule, verbatim)
    - `src/backend/sidecar/bootstrap.ts` (the boot self-check and secret-store receipt lines, read at authoring time so no cited line is invented)
    - `src/backend/sidecar/devSecretVault.ts` (the exact-string env-var guard and the `[dev-secret-vault]` line shapes)
  </read_first>
  <action>
    Create `34.5-LIVE-GATE-RERUN-2.md` with frontmatter: `phase`, `plan: 40`,
    `executed_by: 34.5-41-PLAN.md`, `type: live-gate`, `status: pending`, `blocking: true`, `created`,
    `verdict: null`, `items_total: 5`, `items_passed: null`, `items_failed: null`,
    `items_blocked: null`, `items_not_attempted: null`, `supersedes: 34.5-LIVE-GATE-RERUN.md`.

    **`## Why this run exists`** — name what changed since run 2 and what did not:
    - Run 1 failed 0/5 on `spawn ./{legendary,gogdl,nile} ENOENT` (wrong `publicDir` under the
      sidecar). CLOSED and proven closed by run 2's precondition 4.
    - Run 2 failed 0/5 on a DIFFERENT, downstream defect family: the OAuth capture succeeded at the
      backend for `gog` and `nile` but never reached the renderer, and Epic's window never became
      interactive. Name each fix landed against it since, by plan number: 34.5-23 (the 60 s invoke
      bound and the loud transport-failure path), 34.5-26 (the post-login library refresh the raw
      channel bypass skipped), 34.5-27 (login-window origin display and interface style), 34.5-33
      (runner-aware `refreshLibrary` plus the `origin=` attribution, and the live-found `null`
      transport regression fixed the same day), 34.5-34 (cancellation no longer discards a completed
      login), 34.5-35 (the keyring read bound raised to 45s and the failure memo to 120s), 34.5-36
      (the dev-only secret vault), and the 2026-08-02 Epic post-auth exfil fix (commit `c857ade8e`,
      `U-34.5-11`).
    - State plainly that NONE of it is live-proven, and that this contract exists to do so.

    **`## D-08 — no partial pass`** — restate verbatim from `34.5-CONTEXT.md`. Add that a third
    non-clean result routes to another gap cycle inside Phase 34.5, never a forced close, and that
    Phase 34.6's and Phase 35's dependency lines are editable ONLY on a clean 5-of-5 PASS.

    **`## Evidence-handling rule (T-34.5-G6-02)`** — restate verbatim from run 2's contract, binding
    on every Result slot: never paste an `access_token`, `refresh_token`, cookie value, session
    identifier, or the Steam `userdata` account id; quote only the lines an item names; redact in
    place rather than omitting a whole line.

    **`## The binding evidence rule`**, stated once and applying to all five items: **a mutating
    call's own report is never proof of its effect.** Run 2 recorded backend `status=captured` for
    GOG and Amazon and it proved nothing — both items still FAILED. Every evidence clause must
    therefore name at least one observable produced by a DIFFERENT component than the one under test:
    a UI state, a file on disk, a log line emitted by another module, or a `ps` listing.

    **`## Item 1 enters BLOCKED — cause, and the opportunistic-attempt clause`** — per D-CYCLE5-A,
    carried from `34.5-CYCLE5-ROUTING.md`:
    - Define BLOCKED as a fourth result value, distinct from PASS, FAIL and NOT ATTEMPTED: an item
      whose precondition is known-unsatisfiable at authoring time for a reason recorded OUTSIDE this
      gate. It counts toward NEITHER `items_passed` nor `items_failed`, and the shortfall is stated
      in `notes`.
    - Name the cause: the PARKED pre-authentication defect
      (`.planning/debug/epic-login-non-interactive.md`, `developer_override_2026_08_02T19_00_00`),
      measured as 403 x1 / `xhr.timeout` x2 / success x1 on `/id/api/email/exists` across the Tauri
      arm's runs, with zero completed Tauri Epic logins ever. Record that the POST-auth root cause is
      confirmed and its fix implemented but STATIC-ONLY (`U-34.5-11`).
    - **Opportunistic-attempt clause:** a BLOCKED entry is NOT permission to skip a working path. If
      the Epic login form renders and accepts input, the operator MUST attempt item 1 and record
      PASS or FAIL instead. In either case, record two extra observations: (a) whether
      `/id/api/email/exists` returns 403, times out, or succeeds; and (b) whether the always-on
      `epic_oauth_redirect_observer_script` was injected — the debug file's own NEW RISK says its
      `window.fetch` wrapper is the prime suspect for the pre-auth rejection and "must be checked
      FIRST". State that this is DIAGNOSIS ONLY: no change to `USER_AGENTS`, `EPIC_LOGIN_URL` or
      `matchOAuthRedirect` is authorised by this gate.
    - State the consequence in one line: with item 1 BLOCKED, **this run cannot reach 5 of 5, so the
      phase cannot close this cycle** — which is why every other item still matters.

    **`## Known holes (ledger Rule 3)`** — a table with one row per OPEN row in
    `34.5-UNTESTED-ITEMS.md` (U-34.5-01 through U-34.5-11 and any row plan 34.5-39 added), each with:
    which gate item would retire it, and whether THIS run can retire it. Fill in explicitly:
    - **U-34.5-01 and U-34.5-10 CANNOT be retired by this run** — D-CYCLE5-B sets
      `GAMELIB_DEV_SECRET_VAULT=1`, so no real `keyring_get` is issued, the Keychain path is never
      exercised, and plan 34.5-35's raised 45s/120s constants cannot be demonstrated doing anything.
      Quote the ledger's own binding statement. These two rows stay OPEN whatever this run's verdict.
    - **U-34.5-06 and U-34.5-11 retire together or not at all** — they share the same live blocker
      and item 1 is BLOCKED, so neither retires this run unless the opportunistic attempt succeeds.
    - U-34.5-02 (item 4), U-34.5-03 (item 5), U-34.5-04 (Amazon code-field legibility, item 3),
      U-34.5-05 (origin display CHANGING within one flow, items 1-3), U-34.5-07 and U-34.5-08 (GOG
      library populated / `origin=` attribution, item 2), U-34.5-09 (capture propagation through a
      teardown, items 2-3) — each retirable ONLY by the exact observation its own row names, quoted
      into this table so the operator can gather it deliberately rather than in hindsight.

    **`## Preconditions`** — eleven explicit NUMBERED steps, each with an empty `RESULT` slot. An
    unstated precondition is exactly how a gate gets skipped and becomes UAT debt (Phases 21, 30,
    34.2 and this phase's own first run). Carry run 2's seven forward, updated, plus four new:

    1. **Phase 34.4.1's blocking gate PASSED on item 1** — satisfied by `34.4.1-LIVE-GATE-RERUN-3.md`
       (4/4). Carry the verbatim quote, and the warning that `34.4.1-LIVE-GATE.md` (2/4) and
       `-RERUN.md` (3/4) may never be cited as evidence of a PASS.
    2. **A clean machine** — `ps aux | grep -E "gamelib-shell|sidecar.js"` before launching, kill
       everything, re-check. An orphaned sidecar was found before BOTH prior runs; two live pairs
       contend for the same store files and make the evidence ambiguous.
    3. **A clean rebuild and a real `pnpm tauri:dev` launch**, with sidecar liveness proven from
       `gamelib.log` growth plus a `ps` listing showing both PIDs — never from the `tauri:dev`
       terminal (`sidecar-console-and-logger-are-invisible`).
    4. **The G-1 fix present in THIS build, proven from the log**:
       `[bootstrap] appRoot resolved=… source=GAMELIB_APP_ROOT` (a `process.cwd` source does NOT
       satisfy this even if the path looks right), `[bootstrap] publicDir resolved=… exists=true`,
       and all four `[bootstrap] runner binary … exists=true` lines. A
       `[bootstrap] SIDECAR ASSET ROOT DEFECT` line anywhere BLOCKS the run.
    5. **Signed OUT of Epic, GOG and Amazon — verified, not assumed.** Do NOT carry any prior run's
       aside state forward: plan 34.5-20's SUMMARY records that session ending with GOG signed back
       IN and Amazon's password reset. Require fresh `ls`/`test -f` on every original and backup
       path, recorded verbatim; any re-aside uses a NEW dated suffix with a `cmp` proof of
       byte-identity before the original is removed — never delete blind. Check `nile_store/` AND
       `nile_config/`. Epic is signed out too, so the opportunistic item-1 attempt is a genuine
       from-scratch attempt if it happens.
    6. **`npm run test:ci`, `npx tsc --noEmit`, `cd src-tauri && cargo check`, `cargo test`,
       `python3 ported-channels-gate.py`** — all at the HEAD the gate runs on, exit codes recorded
       VERBATIM, any non-zero explained rather than rounded to green. **The floor is the MEASURED
       baseline in `34.5-CYCLE5-ROUTING.md` § "Measured baseline"** — copy those numbers in and cite
       the section; do not transcribe a number from run 2 or from any SUMMARY. A count at or below
       that floor means HEAD is stale and must be explained before proceeding; a count above it also
       needs an explanation, because no plan in this cycle lands tests.
    7. **`GAMELIB_SHELL_EXE` read back from the SIDECAR's own receipt line** in `gamelib.log`
       (`[bootstrap] GAMELIB_SHELL_EXE received=…`), never the Rust shell's `eprintln!` (which
       reaches only the terminal and structurally cannot distinguish "set on the child env" from
       "visible to `pathShim`"). An `<UNSET>` reading BLOCKS item 4. Carry the dev-versus-packaged
       caveat verbatim: a dev PASS never retires the packaged half of Assumption A2, and never
       retires residual `R-34.5-G1-PKG`.
    8. **NEW — the cycle-3 fixes are present in this build, proven from source at HEAD, not from a
       SUMMARY**: `oauthCaptureLogin` in `main.rs`'s `LONG_RUNNING_CHANNELS`,
       `capture-transport-failed` in `useTauriOAuthLogin.ts`, and the completion-callback wiring at
       `WebView/index.tsx`. Use comment-filtered greps (`grep -v '^\s*//' | grep -v '^\s*\*'`) so a
       doc comment cannot satisfy a code assertion.
    9. **NEW — the cycle-4 fixes are present in this build, proven from source at HEAD**: the
       `handleRefreshLibrary` gate in `steamFlowRegistration.ts` excluding BOTH `undefined` and
       `null`; `[refreshLibrary] runner=… origin=…` at `GlobalState.tsx:1012`; the cancellation-aware
       `safeSetState` helper in `useTauriOAuthLogin.ts`; `KEYRING_READ_TIMEOUT` at 45s in
       `src-tauri/src/main.rs`; `KEYRING_FAILURE_MEMO_MS` at 120s in `keyringTokenStore.ts`. Record
       beside this precondition that presence is NOT proof of effect — the last two are on a code
       path this run deliberately does not exercise (see precondition 10).
    10. **NEW — the secret-store arm is RECORDED, not assumed.** Per D-CYCLE5-B, `GAMELIB_DEV_SECRET_VAULT=1`
        is exported for this run. Record: the exact env-var value used; the
        `[bootstrap] secret stores: dev-vault` line from `gamelib.log`; and the
        `[dev-secret-vault] INSTALLED …` warning. **Satisfying this precondition is exactly what keeps
        U-34.5-01 and U-34.5-10 OPEN**, and the contract says so here rather than leaving it inferred.
        A `[bootstrap] secret stores: keyring` reading instead means the vault did NOT install (the
        guard is an exact-string `=== '1'` match) — that is a deviation from D-CYCLE5-B and must be
        RECORDED as such, not silently accepted; if it happens, the run may continue, and U-34.5-01
        and U-34.5-10 then become eligible if their own observations are gathered.
    11. **NEW — the item-4/item-5 preflight is consumed AND re-confirmed at gate time.**
        `34.5-G6-ITEM45-PREFLIGHT.md` exists, and its target verification is RE-RUN now (the install
        may have changed since plan 34.5-39). Items 4 and 5 were NOT ATTEMPTED on both prior runs;
        this precondition exists so a third skip requires an explicit, recorded refusal rather than a
        drift.

    Leave every RESULT slot EMPTY.
  </action>
  <verify>
    <automated>grep -q "^verdict: null" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md &amp;&amp; git status --porcelain | grep -Ec "34.5-LIVE-GATE(-RERUN)?\.md" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-LIVE-GATE-RERUN-2.md` exists with `verdict: null`, `status: pending`, `blocking: true`, `executed_by: 34.5-41-PLAN.md`, and an `items_blocked` frontmatter field.
    - Eleven numbered preconditions are present, each with an empty RESULT slot; the four new ones (8, 9, 10, 11) are all present.
    - D-08's no-partial-pass rule and the T-34.5-G6-02 evidence-handling rule both appear verbatim.
    - Precondition 6's floor cites `34.5-CYCLE5-ROUTING.md` § "Measured baseline" by name and carries its actual numbers; no run-2 count appears as this run's floor.
    - Precondition 10 names `GAMELIB_DEV_SECRET_VAULT=1`, the `dev-vault` receipt line, and states that satisfying it is what keeps U-34.5-01 and U-34.5-10 OPEN.
    - The known-holes table has one row per OPEN ledger row, each naming the item that would retire it and whether this run can; U-34.5-01, U-34.5-10, U-34.5-06 and U-34.5-11 are all explicitly marked not-retirable-this-run with their reason.
    - The item-1 BLOCKED section defines BLOCKED, names the cause with its measured shape, and carries the opportunistic-attempt clause plus the diagnosis-only fence.
    - `git status --porcelain` shows `34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` UNMODIFIED.
    - No PASS, FAIL, BLOCKED or NOT ATTEMPTED marker is filled in any Result slot anywhere.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: The five items, the verdict table, the falsification ledger and the arithmetic rule</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` §§ Items 1-5 (the wording to carry forward and each item's recorded FAIL / NOT-ATTEMPTED reason), § "What this gate falsifies" (the four standing claims and their eligibility rules), § "Arithmetic rule"
    - `.planning/phases/34.5-.../34.5-G6-ITEM45-PREFLIGHT.md` (the verified item-4 target, the redacted Steam path, both devtools invocations, the named item-5 Wine action, its channel and kind, its literal evidence lines and prerequisites, and its runner-eligibility finding)
    - `src/backend/sidecar/oauthLoginCapture.ts` and `src/frontend/screens/WebView/useTauriOAuthLogin.ts` — read the ACTUAL log-line shapes at authoring time
    - `src/backend/sidecar/steamFlowRegistration.ts` and `src/frontend/state/GlobalState.tsx` (the `refreshLibrary complete runner=… managers=…` and `[refreshLibrary] runner=… origin=…` shapes — item 2's cross-component evidence)
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` rows U-34.5-02..09 (each row's own "Retired by" observation, which the corresponding item must require)
  </read_first>
  <action>
    Write all five items into `34.5-LIVE-GATE-RERUN-2.md`. For each: what changed since run 2, what to
    do, what to observe, and what evidence is required. Every Result slot stays EMPTY.

    **Items 1-3 (Epic / GOG / Amazon login from scratch, populated library)** — REQ-34.5-12, plus
    REQ-34.5-02 for item 3's anchor. For each, require ALL of:
      (a) the `[oauthLoginCapture] runner=… status=captured` line — the backend's own report,
          necessary and NOT sufficient;
      (b) a `[useTauriOAuthLogin] runner=… phase=…` line for the same runner. **Its absence is the
          exact signature of run 2's defect** — six backend terminal outcomes produced zero such
          lines. This is a DIFFERENT component reporting, which is why it counts. Name the specific
          terminal value a PASS requires: `phase=idle (login completed, library refresh triggered)`;
      (c) the runner's CLI auth invocation appearing in the log AFTER the capture timestamp
          (`legendary` / `gogdl auth` / `nile auth --login` as applicable);
      (d) the library **populated** in the UI — named games visible — AND the runner present in the
          Library filter options. An empty library after a "successful" login is a FAIL, not a
          partial pass; run 2's developer report named the missing filter option specifically;
      (e) the account surface showing the signed-in username.

    **Item 1 (Epic)** additionally carries its BLOCKED entry disposition, the opportunistic-attempt
    clause and the two extra observations from Task 1's item-1 section, restated inline so the
    operator does not have to hunt for them. Add: **a prediction is not a result** — record what
    actually happens.

    **Item 2 (GOG)** additionally requires the cycle-4 observables, because they are exactly what
    ledger rows U-34.5-07 and U-34.5-08 name:
      (f) `refreshLibrary complete runner=gog managers=1` in `gamelib.log`
          (`steamFlowRegistration.ts:151`) — a backend line emitted by a different module than the
          capture, following the login;
      (g) ZERO `Refreshing undefined Library` lines across the whole session, and every
          `[refreshLibrary]` line carrying a non-`unknown` `origin=` value
          (`GlobalState.tsx:1012`);
      (h) no `refreshLibrary failed runner=…` line anywhere in the session — the `null`-transport
          regression fixed on 2026-08-01 emitted `refreshLibrary failed runner=null: unknown runner`
          at mount, so its absence is part of item 2's evidence.
    Also require, for U-34.5-09: at least TWO consecutive GOG login attempts in the same session,
    each producing its own `[useTauriOAuthLogin] … phase=…` line, with zero attempts silently
    returning the developer to Manage Accounts.

    **Item 3 (Amazon)** keeps run 2's sub-clause (b): grep for
    `[oauthLoginCapture] runner=nile origin-mismatch origin=` — it must NOT fire on a clean pass; if
    it does, Assumption A1 is FALSIFIED and item 3 cannot be a clean PASS even if login otherwise
    succeeds. Record the observed origin either way, and keep run 2's three-way
    CONFIRMED / FALSIFIED / UNTESTED recording rule verbatim. Add the U-34.5-04 observation as a
    required transcription: whether the verification-code field's text is legible (run 2 recorded
    black-on-black), and the U-34.5-05 observation: a before/after transcription of the login
    window's title-bar origin text CHANGING within a single flow, with GOG's
    `login.gog.com` → `embed.gog.com` named as the known candidate if item 2 provides it first.

    **Item 4 (`addToSteam` AND the macOS `.app` shortcut, BOTH `exe` call sites)** — REQ-34.5-01,
    REQ-34.5-12. **INLINE** the verified target, the redacted Steam `shortcuts.vdf` path, the
    quit-Steam step, both exact devtools invocations, and the preflight's login-dependency finding
    (including its named fallback target, if it found one) — do not merely reference
    `34.5-G6-ITEM45-PREFLIGHT.md`, because run 2 referenced its groundwork and the item was still
    skipped. State that proving ONE call site is not sufficient (research Correction 3). Evidence: the
    written VDF `Exe` field, the `run.sh` body, and `ps aux` after clicking each, showing the Tauri
    shell binary and never a bare `node` process. Carry the dev-versus-packaged limit.

    **Item 5 (`runWineCommand` for a non-Steam runner)** — REQ-34.5-03, REQ-34.5-12. **INLINE** the
    specific macOS-permitted Wine action the preflight named, its UI path, its dispatching channel and
    registration kind, the literal evidence log lines with their emitting source files, the observable
    in-app effect, and the prerequisite checks. If the preflight flagged a runner-eligibility
    wording problem, RESOLVE it here explicitly: state which runners satisfy this item, and if a
    login-independent target (e.g. `sideload`) is permitted, state in the same breath what its PASS
    does and does not retire. Restate that Pitfall 2's claim is retired ONLY by an item-5 PASS — a
    FAIL-by-blockage retires nothing, exactly as both prior runs recorded.

    Then write, all unfilled:
    - a **Verdict** table with one row per item and a result column admitting PASS / FAIL / BLOCKED /
      NOT ATTEMPTED;
    - a **What this gate falsifies** section listing the four standing claims with their eligibility
      rules verbatim from run 2 (the 34.4.1 seam claim, per-runner; Assumption A1; Assumption A2 at
      both call sites with the packaged half and `R-34.5-G1-PKG` excluded; research Pitfall 2), each
      marked STANDING, plus the rule that nothing is struck without the corresponding ITEM passing —
      not on the strength of a sub-clause being independently confirmed while its parent item fails,
      which is exactly the distinction run 2 had to draw for A1;
    - the **arithmetic rule**, extended for this run: `items_passed + items_failed + items_blocked +
      items_not_attempted` must equal 5. BLOCKED and NOT ATTEMPTED each count toward neither passed
      nor failed, and the shortfall must be stated explicitly in the frontmatter `notes`, never
      quietly rounded;
    - a **New findings (this run)** header instructing the operator to record observations WITHOUT
      diagnosing them, as both prior runs did — diagnosis belongs to the next cycle.
  </action>
  <verify>
    <automated>grep -c "^\*\*Result:\*\*" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md | grep -qx 5 &amp;&amp; grep -q "Arithmetic rule" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five items, each with an empty Result slot admitting PASS / FAIL / BLOCKED / NOT ATTEMPTED; the Result-slot count grep returns 5.
    - Items 1-3 each require all five evidence clauses (a)-(e), with (b) naming the specific terminal `phase=` value a PASS requires and (d) requiring both populated games AND the filter option.
    - Item 2 additionally carries clauses (f), (g) and (h) with their source-cited line shapes, plus the two-consecutive-attempts requirement for U-34.5-09.
    - Item 3 keeps the origin-mismatch sub-clause with its three-way recording rule and adds the U-34.5-04 and U-34.5-05 transcriptions.
    - Item 4 INLINES the verified appName, the redacted Steam path, the quit-Steam step, both devtools invocations and the preflight's login-dependency finding — not a bare cross-reference.
    - Item 5 INLINES the named Wine action, its channel and kind, its literal evidence lines with emitting files, its prerequisites, and an explicit resolution of the runner-eligibility question.
    - Every log-line shape cited in any item was read from its emitting source file at authoring time; the SUMMARY names each file it was read from.
    - The Verdict table, the four standing claims with eligibility rules, the "nothing struck without the ITEM passing" rule, the four-way arithmetic rule and the New-findings header are all present and unfilled.
    - `git status --porcelain` still shows both prior gate documents UNMODIFIED, and `git diff --name-only` matches zero `.ts`, `.tsx` or `.rs` paths.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior gate records → this contract | Editing a completed record would destroy the evidence of a failed run |
| machine-local identifiers → contract text | Item 4 inlines a real Steam `userdata` path |
| a live credential origin → the contract's item-1 instructions | Item 1 instructs a human to enter real credentials into an embedded webview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C5-10 | Tampering | `34.5-LIVE-GATE.md` / `34.5-LIVE-GATE-RERUN.md` | mitigate | A NEW filename is used; both tasks' acceptance criteria require `git status --porcelain` to show the two prior documents unmodified |
| T-34.5-C5-11 | Repudiation | a contract citing a log line the code does not emit | mitigate | Every cited line shape must be read from its emitting source file at authoring time with that path recorded — the exact defect run 1's precondition 5 shipped and run 2 had to close |
| T-34.5-C5-06 | Information disclosure | Steam `userdata` account id inlined into item 4 | mitigate | Carried from plan 34.5-39: the id is committed REDACTED; the path structure is the evidence, the id is not |
| T-34.5-C5-12 | Repudiation | a sub-clause confirmation read as retiring its parent claim | mitigate | The falsification section carries run 2's explicit rule that nothing is struck without the corresponding ITEM passing, with A1 named as the worked example |
| T-34.5-C5-13 | Spoofing | the operator entering credentials into an unverified origin in the embedded login window | mitigate | Items 1-3 require the login window's own title-bar origin to be transcribed (U-34.5-05), which is the anti-phishing surface plan 34.5-27 landed; item 3 additionally requires the `www.amazon.com` host anchor to hold (REQ-34.5-02, closing the inherited `T-34.4.1-44b`) |
| T-34.5-C5-14 | Repudiation | a known-untestable row silently assumed covered by this run | mitigate | Task 1's known-holes table names every OPEN ledger row with whether this run can retire it, per ledger Rule 3, with U-34.5-01/10/06/11 explicitly marked not-retirable and why |
| T-34.5-C5-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages and touches no source file |
</threat_model>

<verification>
- `34.5-LIVE-GATE-RERUN-2.md` exists with `verdict: null`, eleven preconditions, a known-holes table, five empty items, the verdict table, the falsification section and the four-way arithmetic rule.
- `git status --porcelain` shows both prior gate documents unmodified.
- `git diff --name-only` matches zero `.ts`, `.tsx`, `.rs` paths.
</verification>

<success_criteria>
A third blocking gate contract exists, authored before any live work, covering all five items —
including the two never attempted and the one entering BLOCKED with a named cause — with
preconditions that prove the fixes are in the build under test, evidence clauses that require an
observable from a component other than the one being measured, and every open ledger hole named
rather than assumed covered.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-40-SUMMARY.md` when done
</output>
