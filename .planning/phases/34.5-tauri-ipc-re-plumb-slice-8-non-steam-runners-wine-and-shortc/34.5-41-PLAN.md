---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 41
type: execute
wave: 4
depends_on: [34.5-40]
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md
autonomous: false
gap_closure: true
cycle: 5
requirements: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-04, REQ-34.5-05, REQ-34.5-12]

must_haves:
  truths:
    - "All eleven preconditions carry a recorded RESULT block, in order, none filled from an assumption"
    - "Items 2, 3, 4 and 5 are each attempted on real macOS hardware and carry a recorded result — items 4 and 5 for the first time in this phase's history"
    - "Item 1 is recorded BLOCKED with its cause, or PASS/FAIL if the opportunistic attempt became possible — never silently omitted"
    - "The verdict is computed by the contract's own four-way arithmetic, with no FAIL softened and no shortfall rounded"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md"
      provides: "the completed third blocking gate record"
      contains: "Overall verdict"
  key_links:
    - from: "the live session's gamelib.log"
      to: "34.5-LIVE-GATE-RERUN-2.md Result slots"
      via: "re-verification of every pasted line against the file itself"
      pattern: "gamelib.log"
---

<objective>
Execute the BLOCKING five-item live gate against `34.5-LIVE-GATE-RERUN-2.md` on real macOS hardware,
record every result, and compute the verdict.

This is the third attempt. Runs 1 and 2 both failed 0/5 on two different root causes, and items 4 and
5 have never been attempted at all — twice skipped because groundwork written days earlier had gone
stale by the time the item was reached. Plan 34.5-39 wrote that groundwork one wave ago and the
contract's precondition 11 re-confirms it at gate time, so that excuse is gone.

**This plan supersedes plan 34.5-31's Tasks 1-3.** Plan 34.5-31 is not edited, not renumbered and not
executed; it stays on disk as the record of its own halt. Its Task 4 (propagation) is re-homed to
plan 34.5-42 so this plan's context stays on the live run.

Per D-08, **any item recorded FAIL means Phase 34.5 does not close** — no advisory mode, no
pre-authorized override, no deferred UAT entry, no partial-pass closure. Per D-CYCLE5-A, item 1
enters BLOCKED, so **5 of 5 is unreachable this run and the phase cannot close this cycle**; what is
at stake is whether items 2-5 finally land, and whether items 4 and 5 get a result of any kind for
the first time.

Every blocking defect this phase has found was found by a human driving the UI while a fully green
test suite (3279, then 3387, then 3447, then 3548 tests) reported nothing wrong. That is why this
plan exists and why it cannot be automated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md

**The contract is authoritative.** `34.5-LIVE-GATE-RERUN-2.md` governs; where this plan and the
contract differ, the contract wins. Fill its Result slots; do not rewrite its items.

**Evidence-handling rule (T-34.5-G6-02), binding on every Result slot:** never paste an
`access_token`, `refresh_token`, cookie value, session identifier, OAuth `code=` value, or the Steam
`userdata` account id. Quote only the lines an item names; redact in place rather than omitting a
whole line.

**Sidecar output is read from `~/Library/Logs/GameLib/gamelib.log`, NEVER from the `pnpm tauri:dev`
terminal** (`sidecar-console-and-logger-are-invisible`). The one exception is `main.rs`'s
`eprintln!("[shell] …")` lines, which reach only the terminal — an item relying on one says so.

**Environment for this run, per D-CYCLE5-B:** export `GAMELIB_DEV_SECRET_VAULT=1` before launching.
The value must be the exact string `1` — `devSecretVault.ts`'s guard is an exact-string `=== '1'`
match, so `true`, `yes` or `0` all leave the keyring path active. This choice is deliberate and its
cost is recorded: **ledger rows U-34.5-01 and U-34.5-10 cannot be retired by this run.**
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Satisfy and evidence all eleven preconditions, in order</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` § Preconditions (all eleven, with their empty RESULT slots — this is the file being filled)
    - `.planning/phases/34.5-.../34.5-CYCLE5-ROUTING.md` § "Measured baseline" (precondition 6's floor)
    - `.planning/phases/34.5-.../34.5-G6-ITEM45-PREFLIGHT.md` (precondition 11 re-confirms this document's target verification)
    - `.claude/skills/spike-findings-gamelib/SKILL.md` (the sidecar-logging and login-window constraints that make the log the only admissible source)
  </read_first>
  <what-built>
    `34.5-LIVE-GATE-RERUN-2.md` (plan 34.5-40) carries eleven numbered preconditions with empty RESULT
    slots. Behind them sit this phase's cycle-3 and cycle-4 fixes (plans 34.5-23 through 34.5-37) and
    the 2026-08-02 Epic post-auth exfil fix, none of which has ever been exercised live.
  </what-built>
  <how-to-verify>
    Work the contract's preconditions 1 through 11 IN ORDER and record each RESULT slot verbatim. Do
    not reorder them and do not skip one because it "obviously holds" — an unstated or assumed
    precondition is exactly how a gate becomes UAT debt, and this phase's own first run did it.

    The literal sequence, with the exact commands:

    1. Quote `34.4.1-LIVE-GATE-RERUN-3.md`'s item-1 PASS line and its 4/4 overall verdict. Do not
       cite `34.4.1-LIVE-GATE.md` (2/4) or `-RERUN.md` (3/4).
    2. `ps aux | grep -E "gamelib-shell|sidecar.js"` — record verbatim, `kill -9` every match
       (including any orphaned `sidecar.js` with no `gamelib-shell` parent — found before BOTH prior
       runs), re-run and record the clean listing.
    3. `npx electron-vite build`, then `npm run build:sidecar`, then export
       `GAMELIB_DEV_SECRET_VAULT=1` and launch `pnpm tauri:dev`. Keep
       `tail -f ~/Library/Logs/GameLib/gamelib.log` open in a second window for the whole run. Record
       both PIDs alive together from `ps aux`, plus the log's line count at two separate timestamps to
       prove growth.
    4. From THIS session's `gamelib.log`: the `[bootstrap] appRoot resolved=… source=` line (the
       `source=` field must read `GAMELIB_APP_ROOT`, never `process.cwd`), the
       `[bootstrap] publicDir resolved=… exists=true` line, and all four
       `[bootstrap] runner binary … exists=true` lines. Then
       `grep -c "SIDECAR ASSET ROOT DEFECT" ~/Library/Logs/GameLib/gamelib.log` — it must return 0.
       **A non-zero count is a hard STOP.**
    5. `ls`/`test -f` on every Epic, GOG and Amazon credential path and its backup, recorded
       verbatim; check `nile_store/` AND `nile_config/`. Any re-aside uses a NEW dated suffix with
       `cmp` proving byte-identity before the original is removed. Never delete blind. Plan 34.5-20's
       SUMMARY records the prior session ending with GOG signed back IN and Amazon's password reset —
       assume nothing survived.
    6. `npm run test:ci`, `npx tsc --noEmit`, `cd src-tauri && cargo check`, `cargo test`, and
       `python3 .planning/phases/34.5-.../ported-channels-gate.py` — exit codes VERBATIM, compared
       against `34.5-CYCLE5-ROUTING.md` § "Measured baseline". Explain any deviation in either
       direction rather than rounding it to green.
    7. `grep "GAMELIB_SHELL_EXE received=" ~/Library/Logs/GameLib/gamelib.log` — the SIDECAR's own
       receipt line, never the shell's terminal `eprintln!`. **An `<UNSET>` reading is a hard STOP
       for item 4** (but not for items 1-3 or 5).
    8. Comment-filtered greps proving the cycle-3 fixes are at HEAD: `oauthCaptureLogin` in
       `main.rs`'s `LONG_RUNNING_CHANNELS`, `capture-transport-failed` in `useTauriOAuthLogin.ts`,
       the completion-callback wiring in `WebView/index.tsx`. **Any absence is a hard STOP** — this
       build is not the one the cycle produced, and no result may be attributed to a fix that is not
       present.
    9. The same for the cycle-4 fixes: the `handleRefreshLibrary` gate excluding both `undefined` and
       `null`; `[refreshLibrary] runner=… origin=…` at `GlobalState.tsx:1012`; the `safeSetState`
       helper; `KEYRING_READ_TIMEOUT` at 45s; `KEYRING_FAILURE_MEMO_MS` at 120s. Same hard STOP on
       absence. Record beside it that presence is not proof of effect.
    10. `grep "secret stores: " ~/Library/Logs/GameLib/gamelib.log` — record which arm this run used.
        `dev-vault` is the expected reading under D-CYCLE5-B and is what keeps U-34.5-01 and
        U-34.5-10 OPEN. A `keyring` reading is a DEVIATION and must be recorded as such — the run may
        continue, but the ledger consequence changes and plan 34.5-42 must be told.
    11. Re-run `34.5-G6-ITEM45-PREFLIGHT.md`'s own target-verification commands NOW and record their
        output. If the target no longer exists, use the preflight's named fallback and record the
        substitution — do not skip items 4 or 5 over it.

    Three preconditions carry a hard STOP: **4** (asset-root defect blocks items 1-3 and therefore 5),
    **8 and 9** (a missing fix means the wrong build). Precondition **7**'s `<UNSET>` blocks item 4
    only. If a hard STOP fires, halt here and report which one — do not proceed to Task 2.
  </how-to-verify>
  <action>
    Present the contract's eleven preconditions to the developer in order and collect each RESULT
    block's verbatim command output. Fill the contract's own RESULT slots as they arrive; do not
    batch them at the end, and never fill a slot from an assumption. If precondition 4, 8 or 9 fires
    its hard STOP, halt the plan and report which one blocked.
  </action>
  <acceptance_criteria>
    - All eleven RESULT slots in `34.5-LIVE-GATE-RERUN-2.md` are filled with verbatim command output, in order.
    - Precondition 4's evidence includes the literal `source=GAMELIB_APP_ROOT` reading and a zero count for `SIDECAR ASSET ROOT DEFECT`.
    - Precondition 6's numbers are compared against `34.5-CYCLE5-ROUTING.md`'s measured baseline, with any deviation explained in writing.
    - Precondition 10 records which secret-store arm the run used, quoted from the log, and states the ledger consequence that follows from it.
    - Precondition 11 records a target re-verification performed during THIS session, not a citation of plan 34.5-39's earlier run.
    - If any hard STOP fired, the SUMMARY names it and no item result is recorded anywhere.
  </acceptance_criteria>
  <resume-signal>Paste all eleven RESULT blocks with their verbatim command output. Then type "preconditions satisfied", or name which one blocked and why.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Drive items 2, 3, 4 and 5 on real hardware, and dispose of item 1</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` §§ Items 1-5 (each item's evidence clauses, verbatim — this is the script)
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` § "Known holes (ledger Rule 3)" (which observation retires which ledger row, so it is gathered deliberately rather than in hindsight)
    - `.planning/phases/34.5-.../34.5-G6-ITEM45-PREFLIGHT.md` (items 4 and 5's invocations, prerequisites and evidence lines — already inlined in the contract, read here as the fuller record)
    - `.claude/skills/spike-findings-gamelib/SKILL.md` (the `navigator.clipboard` no-op, the WKWebView layout gotcha, and the login-window constraints — so a known gotcha is not mis-recorded as a new finding)
  </read_first>
  <what-built>
    A `pnpm tauri:dev` build with all eleven preconditions satisfied, carrying this phase's cycle-3
    and cycle-4 fixes and the Epic post-auth exfil mechanism — none of which has ever been observed
    live.
  </what-built>
  <how-to-verify>
    Work the items in the contract's order. For each, gather EVERY evidence clause the item lists —
    not a subset.

    **Item 1 (Epic).** Its entry disposition is BLOCKED with the parked pre-auth defect as its cause.
    Open Epic anyway, once, and observe: if the login form renders and accepts input, the
    opportunistic-attempt clause applies — drive the login and record PASS or FAIL like any other
    item. Either way record the two extra observations the contract names: whether
    `/id/api/email/exists` returns 403, times out, or succeeds, and whether the always-on
    `epic_oauth_redirect_observer_script` was injected. **Diagnosis only** — no change to
    `USER_AGENTS`, `EPIC_LOGIN_URL` or `matchOAuthRedirect` is authorised here.

    **Items 2 and 3 (GOG, Amazon).** Five clauses each, plus item 2's three cycle-4 clauses and its
    two-consecutive-attempts requirement, plus item 3's origin-mismatch sub-clause and the
    code-field-legibility and title-bar-origin transcriptions. The two clauses run 2's evidence
    lacked entirely are the `[useTauriOAuthLogin] runner=… phase=…` line and the library populated
    in the UI **with the runner present in the Library filter options** — neither may be inferred.

    **Items 4 and 5 — ATTEMPT THEM.** Both have been NOT ATTEMPTED on both prior runs, so neither
    research Assumption A2 nor research Pitfall 2 has ever been tested. Their targets, invocations,
    prerequisites and evidence lines are inlined in the contract; nothing should need discovering
    mid-run. Item 4 requires quitting Steam FIRST and exercising BOTH `exe` call sites — proving one
    is not sufficient. If the contract's item-4 or item-5 section names a login-independent fallback
    target, use it rather than abandoning the item when an upstream login fails. If an item genuinely
    cannot be attempted, record NOT ATTEMPTED with the specific reason; do not record it as FAIL or
    BLOCKED, and do not leave it silent.

    Recording rules, all four binding:
    - **Record what happened, not what should have happened.** Do not soften, do not round, do not
      describe a partial result as a pass.
    - **A mutating call's own report is not proof of its effect.** A backend `status=captured` is
      necessary and not sufficient; run 2 had it for two runners and both items still FAILED.
    - **Record any NEW observation in the "New findings (this run)" section WITHOUT diagnosing it.**
      Both prior runs followed this rule and both produced actionable findings because of it.
    - **Do not claim a ledger row retired at the console.** Rows are resolved in Task 3 against the
      exact observation each row's own text names.
  </how-to-verify>
  <action>
    Walk the developer through items 1 through 5 in the contract's order, collecting every evidence
    clause each item names. Record results verbatim as they arrive. Do not soften a FAIL, do not infer
    an unobserved clause, and do not diagnose any new observation. If the developer declines an item,
    record NOT ATTEMPTED with their stated reason rather than FAIL or BLOCKED.
  </action>
  <acceptance_criteria>
    - Every item has a recorded result and every evidence clause its own contract section names, with `gamelib.log` lines quoted verbatim and redacted in place where required.
    - Items 4 and 5 each carry a real result (PASS, FAIL, or NOT ATTEMPTED with a specific stated reason) — a third silent skip is a criterion failure.
    - Item 4's evidence covers BOTH call sites or explicitly records which one was not exercised and why.
    - Item 1 carries BLOCKED with its cause, or PASS/FAIL from the opportunistic attempt, plus both extra observations.
    - New observations are recorded in the "New findings (this run)" section without diagnosis.
    - No credential value appears anywhere in the pasted evidence.
  </acceptance_criteria>
  <resume-signal>Paste, per item, the result and every evidence clause the contract names. Add any new observations to the findings list. Then type "items recorded".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verdict, the arithmetic, and the falsification ledger</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN-2.md` (whole file — this is the file being modified; its Verdict table, falsification section, known-holes table and arithmetic rule are authoritative)
    - `~/Library/Logs/GameLib/gamelib.log` — re-verify every pasted line against the file directly
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` § D-08 (the no-partial-pass rule, verbatim)
    - `.planning/phases/34.5-.../34.5-CYCLE5-ROUTING.md` § "Routing by verdict" (which branch this run lands in)
  </read_first>
  <action>
    Fill the Result slots from Task 2's evidence, then the Verdict table, then the frontmatter.

    **Re-verify every pasted log line against `gamelib.log` yourself before recording it.** Plan
    34.5-20 found and corrected a factual error in gathered evidence exactly this way (a claimed "4
    legendary timeouts" the log showed as 3). If a pasted line disagrees with the file, record the
    FILE's version and note the discrepancy explicitly.

    Set frontmatter `status: complete`, `verdict`, `items_passed`, `items_failed`, `items_blocked`,
    `items_not_attempted`, `preconditions_satisfied`/`_blocked`/`_pending`, and `completed`.

    **Apply the four-way arithmetic rule literally.** `items_passed + items_failed + items_blocked +
    items_not_attempted` must equal 5. BLOCKED and NOT ATTEMPTED count toward neither passed nor
    failed, and the shortfall must be stated explicitly in the frontmatter `notes` — as both prior
    runs did for their own shortfalls — never quietly rounded to make the arithmetic look clean.

    **Apply D-08 literally.** Only 5 of 5 PASS closes the phase. With item 1 entered BLOCKED that is
    unreachable this run, so the frontmatter `notes` must state plainly that Phase 34.5 does not
    close on this result and name the routing branch from `34.5-CYCLE5-ROUTING.md` § "Routing by
    verdict" that this outcome lands in.

    Resolve "What this gate falsifies" for all four standing claims. **Nothing is struck without the
    corresponding ITEM passing** — not on the strength of a sub-clause being independently confirmed
    while its parent item fails. Run 2 had to draw exactly this distinction for Assumption A1
    (confirmed at the mechanism level, claim still STANDING because item 3 failed on the library
    half); draw it again if it arises, and spell the distinction out so a future reader cannot
    conflate the two.

    Resolve the known-holes table: for each row, record RETIRED (with the exact observation captured)
    or STILL OPEN (with why). **U-34.5-01 and U-34.5-10 are recorded STILL OPEN regardless of this
    run's outcome**, unless precondition 10 recorded the `keyring` deviation AND their own
    observations were gathered — in which case say so explicitly rather than retiring them silently.
    U-34.5-06 and U-34.5-11 retire together or not at all.

    Write a "What this run nevertheless proved" section, as both prior runs did, distinguishing what
    genuinely advanced from what did not — honestly, in both directions. Items 4 and 5 finally
    carrying a result of any kind is itself a first for this phase; say so if it happened, and say so
    just as plainly if it did not.

    Before committing: grep the finished document for `token=`, `code=`, `Cookie` and `userdata` and
    confirm no credential value is present.
  </action>
  <verify>
    <automated>grep -qE "^verdict: (PASS|FAIL)$" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md &amp;&amp; git status --porcelain | grep -Ec "34.5-LIVE-GATE(-RERUN)?\.md" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `verdict` is `PASS` or `FAIL`; `items_passed`, `items_failed`, `items_blocked` and `items_not_attempted` are all integers reconciling to 5, with the reconciliation stated in `notes`.
    - All five Result slots are filled, each with the evidence clauses its item names, re-verified against `gamelib.log` directly.
    - The Verdict table has one row per item with its result and a one-line reason.
    - All four standing claims are resolved STRUCK or STANDING, and every STRUCK claim names the ITEM that passed to earn it.
    - The known-holes table is resolved row by row; U-34.5-01 and U-34.5-10 are recorded STILL OPEN with the dev-vault reason, and U-34.5-06/U-34.5-11 are resolved together.
    - `notes` states plainly that the phase does not close on this result and names the routing branch it lands in.
    - `git status --porcelain` shows `34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` UNMODIFIED.
    - `grep -Ec "token=|code=|Cookie|userdata" ` on the finished document returns 0, or every match is verifiably redacted; the check is recorded in the SUMMARY.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live session → repository documents | Real Epic/GOG/Amazon session artefacts cross into a public-fork record |
| developer report → recorded verdict | A human summary can drift from what the log says |
| dev secret vault → real credentials | For this run, secrets are stored in PLAINTEXT on disk by design |
| gate verdict → downstream phase gating | Phases 34.6 and 35 unblock on this verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-G6-02 | Information disclosure | pasted `gamelib.log` excerpts in the gate record | mitigate | Only the lines an item names may be quoted, redacted in place; Task 3's acceptance criteria require a pre-commit grep for `token=`, `code=`, `Cookie`, `userdata` |
| T-34.5-C5-15 | Information disclosure | `GAMELIB_DEV_SECRET_VAULT=1` writing real storefront secrets to a plaintext file | accept | Developer-scoped, knowingly accepted under D-CYCLE5-B. `devSecretVault.ts` refuses to install in a packaged build, creates the file owner-only, and logs a loud `INSTALLED — secrets are stored in PLAINTEXT` warning plus a line per read/write. The residual is a dev-machine-local plaintext file; the mitigation for production is the refusal, which is already in place |
| T-34.5-C5-16 | Repudiation | a developer summary adopted as fact | mitigate | Task 3 re-verifies every pasted line against `gamelib.log` directly and records any discrepancy, following the precedent that caught the "4 vs 3 timeouts" error in run 2 |
| T-34.5-C5-13 | Spoofing | credentials entered into an unverified origin in the embedded login window | mitigate | Items 1-3 require the login window's title-bar origin to be transcribed; item 3 requires the `www.amazon.com` anchor to hold with a zero-count `origin-mismatch` grep |
| T-34.5-C5-05 | Elevation of privilege | a wrong `exe` value in a Steam VDF launch command | mitigate | Item 4's evidence requires reading the written `Exe` field and `run.sh` body back plus a `ps aux` proving the launched process is the Tauri shell binary and never bare `node` |
| T-34.5-C5-17 | Elevation of privilege | an Epic "fix" applied opportunistically mid-run | mitigate | Task 2's item-1 instructions are diagnosis-only and name the three identifiers that must not change; the run authorises observation, never a code change |
| T-34.5-C5-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages; it rebuilds from existing lockfiles only |
</threat_model>

<verification>
- All eleven preconditions carry recorded RESULT blocks.
- All five items carry a result; `items_passed + items_failed + items_blocked + items_not_attempted` reconciles to 5.
- `34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` are byte-unchanged.
- The finished document contains no credential value.
</verification>

<success_criteria>
The third blocking gate has run on real hardware; items 2, 3, 4 and 5 each carry a recorded result
with every evidence clause their contract sections name; item 1 is disposed of as BLOCKED with its
cause or attempted opportunistically; the verdict is computed by the contract's own four-way
arithmetic under D-08 with nothing softened; and no standing claim or ledger row is retired without
the exact observation it names.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-41-SUMMARY.md` when done
</output>
