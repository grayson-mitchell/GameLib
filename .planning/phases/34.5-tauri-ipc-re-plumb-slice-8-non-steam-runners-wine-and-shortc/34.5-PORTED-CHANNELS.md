# Phase 34.5 — Declared Ported-Channel List

**Purpose:** The enumerated set plan 34.5-15's blocking live gate and Phase 35 both start from,
mirroring how `34.4.1-PORTED-CHANNELS.md` served the same role for the prior slice. It describes
what actually shipped in plans 34.5-01 through 34.5-13, verified row by row against every prior
plan's own `SUMMARY.md`, never against planning-time intent. **If the measurement disagrees with
the prediction, the measurement wins** — the same discipline `34.4.1-PORTED-CHANNELS.md` stated
and this document inherits.

**This document was originally written BEFORE plan 34.5-15's live gate ran.** That FIRST live gate
ran 2026-08-01 (developer-driven on real macOS hardware) and FAILED: 0 of 5 items PASS, 4 FAIL
(items 1, 2, 3, 5), 1 NOT ATTEMPTED (item 4) — all three OAuth runner binaries failed to spawn under
the sidecar (`spawn ./{legendary,gogdl,nile} ENOENT`), because `publicDir` resolved to a nonexistent
`src-tauri/public`. See `34.5-LIVE-GATE.md` for that FAILED first run's full evidence; it is
byte-unchanged and remains the record of that run — never cite it as evidence of a PASS.

Gap-cycle plans 34.5-16/17/18 fixed that root cause (G-1) at the code level, and plan 34.5-19
authored a RE-RUN contract, executed by plan 34.5-20 on 2026-08-01: **`34.5-LIVE-GATE-RERUN.md`**.
The re-run also FAILED, 0 of 5 items PASS (3 FAIL — items 1, 2, 3 — and 2 NOT ATTEMPTED — items 4,
5) — but the G-1 fix IS proven closed (items 2 and 3 both reached `status=captured` at the backend
for the first time ever; the first run never got past the `ENOENT` spawn failure). The re-run
surfaced a NEW, downstream defect: Epic's login form never becomes interactive (0/3 captures), and
GOG/Amazon's successful backend captures are never consumed into a completed, UI-visible,
library-populated login. Every `LIVE (item N)` cell below now reflects the RE-RUN's recorded
result — FAIL for items 1/2/3's cells, PENDING for items 4 and 5's cells (both genuinely NOT
ATTEMPTED this run, each Riders cell says why). **No `LIVE (item N)` cell here may be read as "seen
working"; a FAIL cell means the opposite was observed, and a PENDING cell means not attempted, not
proven either way.** See `34.5-LIVE-GATE-RERUN.md` for full evidence and the new downstream-defect
diagnosis. Phase 34.5 does NOT close on this result — another gap cycle is required (D-08's
no-partial-pass rule, unchanged).

---

## What shipped

**39 of 58 inventoried channels are ported**, registered across five sidecar registration
modules: `runnerAuthFlowRegistration.ts` (11), `wineToolsFlowRegistration.ts` (9),
`shortcutsFlowRegistration.ts` (7), `runnerMiscFlowRegistration.ts` (11) — 11+9+7+11 = 38, measured
by plan 34.5-13's completeness test (`runnerSliceRegistration.test.ts` Describe 6), not merely
predicted by this phase's own CONTEXT.md — plus `gameDetailsFlowRegistration.ts` (1: `getInstallInfo`,
late-discovered, F-34.5-G6-10, ported by plan 34.5-43), for 11+9+7+11+1 = 39.

**3 channels are DROPPED permanently** (D-02): `authZoom`, `getZoomUserInfo`, `logoutZoom`. Zoom
sign-in dies at the Phase 35 cutover — accepted, because Zoom is absent from GameLib's stated core
value ("Epic, GOG, Amazon, and Steam") and is purely inherited from Heroic.

**16 channels are DEFERRED to the newly-inserted Phase 34.6** (D-03/D-05): EOS overlay (8:
`disableEosOverlay`, `enableEosOverlay`, `getEosOverlayStatus`, `getLatestEosOverlayVersion`,
`installEosOverlay`, `isEosOverlayEnabled`, `removeEosOverlay`, `updateEosOverlayInfo`), SteamGridDB
artwork (5: `steamgriddb.getGrids`, `steamgriddb.getHeroes`, `steamgriddb.hasApiKey`,
`steamgriddb.searchGame`, `steamgriddb.setApiKey`), and winetricks (3: `winetricksAvailable`,
`winetricksInstall`, `winetricksInstalled`).

**The dropped and deferred channels required NO new code.** The `UNPORTED_CHANNEL_MARKER` machinery
(`src/common/types/sidecarTransport.ts`) already catches and declares them at boot — dropping or
deferring means *let it reject and declare it honestly*, not *build a new degradation path*. Plan
34.5-13's SEAM Invariant B test (`runnerSliceRegistration.test.ts` Describe 7) directly asserts all
19 names are absent from both `handlerRegistry` and `listenerRegistry`, so they still reject with
`UNPORTED_CHANNEL_MARKER` — measured, not assumed.

**39 + 3 + 16 = 58**, reconciling exactly against `IPC-PORT-INVENTORY.md`'s "Phase 34.5" section
(the authoritative work-list, counted at 57 during `34.5-CONTEXT.md`'s own discussion, then 58 once
plan 34.5-43 bucketed the late-discovered `getInstallInfo`, F-34.5-G6-10).

**None of the 39 has passed a live gate.** Every one is proven registered by automated test
(bidirectional registration-kind, curated-import discipline, argument-forwarding, or a real-filesystem
loud-throw pin, depending on the channel). Plan 34.5-15's blocking 5-item live gate ran on 2026-08-01
and FAILED — 0 of 5 items PASS. Its own precondition 1 (Phase 34.4.1's own gate recording PASS on
item 1) was satisfied first (34.4.1's third gate run, 4/4 PASS), unblocking items 1-3, but all three
then failed on an unrelated defect: the `legendary`/`gogdl`/`nile` runner binaries fail to spawn
under the sidecar (`publicDir` resolves to a nonexistent path — full diagnosis in
`34.5-LIVE-GATE.md`'s "Root cause" section). Item 5 failed by blockage on items 1-3; item 4 was not
attempted.

**The RE-RUN, `34.5-LIVE-GATE-RERUN.md` (executed by plan 34.5-20, 2026-08-01), also FAILED — 0 of
5 items PASS.** Its own precondition 4 proved the `publicDir` defect above is CLOSED (`source=
GAMELIB_APP_ROOT`, all four runner binaries `exists=true`, no `SIDECAR ASSET ROOT DEFECT` line) —
and items 2 (GOG) and 3 (Amazon) both reached `status=captured` at the backend for the first time
this phase, something the first run never achieved. What blocked a PASS this time is a NEW,
downstream defect: item 1 (Epic)'s login form never becomes interactive (0 of 3 captures across
three attempts); items 2 and 3's successful backend captures are never consumed into a completed,
UI-visible, library-populated login (no follow-up runner-CLI auth call, no frontend
`captured-blocked` transition, for either runner). Items 4 and 5 were NOT ATTEMPTED, confirmed
explicitly by the developer. Proof levels below never claim more than what has actually been
measured — a FAIL is recorded as a FAIL, not softened back to "unit," and a PENDING cell means
genuinely not attempted, never "seen working."

### Two corrections this declaration must not paper over

1. **`getDefaultSavePath` is genuinely unported, and GOG saves-sync does not work end-to-end under
   the sidecar until Phase 34.6 ports it.** CONTEXT.md's D-09 and `34.5-RESEARCH.md`'s Pitfall 1
   both claim `syncGOGSaves` reaches `save_sync.ts:146`'s `getPath('documents')` (via
   `getDefaultGogSavePaths`). Plan 34.5-12 verified this is wrong: `syncGOGSaves`'s own handler
   chain (`libraryManagerMap['gog'].getGame(appName).syncSaves(arg, '', gogSaves)`) only iterates
   the already-resolved `gogSaves` array it is handed — it never calls `getDefaultGogSavePaths`. The
   real and only caller of `getDefaultGogSavePaths` is the separate `getDefaultSavePath` channel,
   invoked by the frontend (`SyncSaves/gog.tsx`'s `getLocations()`) *before* `syncGOGSaves`, in its
   own round trip. `getDefaultSavePath` is **not one of this slice's 38 channels** — confirmed
   absent from every sidecar registration module (`grep -rn getDefaultSavePath src/backend/sidecar/`
   returns nothing) — and remains genuinely unported after this phase. The Discretion question
   research resolved (`documents` belongs to the saves-sync domain, not shortcuts) still holds; only
   the specific claim about which channel's *runtime call path* reaches the line is corrected.
   Practical consequence, stated plainly: **the frontend's GOG saves-sync settings panel does not
   resolve its default location under the sidecar today** — `syncGOGSaves` itself works standalone
   once a caller supplies a `gogSaves` array by some other means, but the panel's own
   location-resolution round trip fails. Logged as deferred item 4 in `deferred-items.md`, flagged
   for Phase 34.6.
2. **`save_sync.ts` is NOT a new electron-reach entry point.** Plan 34.5-13's own prediction (echoed
   from CONTEXT.md/RESEARCH.md) that adding this slice's four registration modules as
   `electronReachLedger.test.ts` entry points would newly confirm `save_sync.ts` as
   electron-reaching did not hold — measured directly, the electron-importing module count stayed
   at 34 (unchanged from the pre-34.5 baseline). `save_sync.ts` is imported by exactly one
   first-party file in the whole repo: `src/backend/main.ts`, which has never been one of the
   ledger's entry points and is not reached transitively by any of this slice's four new modules.
   Recorded here so this document's own channel-count claims are never read as implying a
   reach-ledger change that did not actually happen.

---

## Obligations closed and opened

**CLOSED: `T-34.4.1-44b`** (nile's host-free redirect match, forwarded from Phase 34.4.1 as an
obligation on this phase, not a closed item). Plan 34.5-02 (`oauthLoginCapture.ts`, commit
`6c5d9f203`) host-anchored the `nile` case of `matchOAuthRedirect` on `www.amazon.com` — the same
shape `legendary`'s (`localhost`) and `gog`'s (`embed.gog.com`) sibling cases already used — closing
the obligation exactly as REQ-34.5-02 required, before `authAmazon` (plan 34.5-10) was allowed to
mint any real credential from a captured code. Zoom's identical host-free residual is **void, not
closed** — D-02 drops Zoom permanently, so there is no channel left for it to protect.

**Three CONTEXT.md/RESEARCH.md corrections research made, and which plan acted on each:**

- **D-15's dialog citation (plan 34.5-09 verified the real site is already safe and built no
  fix).** CONTEXT.md's D-15 cited `tools/index.ts:794` as the dialog call site needing scrutiny
  under the sidecar's fire-and-forget dialog constraint. Plan 34.5-09 verified by direct read that
  `tools/index.ts:794` sits inside `Winetricks.checkDependencies` and calls no dialog at all (only a
  caller-supplied `appendMessage` callback and a `logWarning`) — that function's only call site
  (`tools/index.ts:665`, inside `Winetricks.runWithArgs`) belongs to the winetricks cluster,
  deferred to Phase 34.6 and unreachable from any of this phase's 38 channels. The actually
  reachable dialog site for this phase's DXVK/VKD3D toggle channels is `tools/index.ts:137`
  (`showDialogBoxModalAuto`, called from `installOrUpdateTool` on a download failure) — verified
  safe by a full file:line trace down to `electronStub.ts`'s `dialog.showErrorBox`, which
  structurally cannot reject (its own `try`/`catch` around `requestRustInvoke` only ever logs via
  `console.warn`), and pinned by a `jest.isolateModules()`-sandboxed test against the REAL
  (non-mocked) `electronStub.dialog` fallback. No Rust or `dialog.ts` change was needed.
- **D-10's second `exe` call site (`shortcuts.ts:227`).** Recorded here with the FURTHER plan-time
  correction that supersedes CONTEXT.md D-09 and `34.5-RESEARCH.md`'s Correction 3, both of which
  filed `shortcuts.ts:227` under the `addToSteam` cluster. Plan 34.5-01 (the `pathShim.ts` `exe`
  case, blocked-precondition landing) recorded the corrected attribution in source; plan 34.5-08
  owns and pins the `:227` site — reached ONLY via `addShortcut` → `addShortcuts` →
  `generateMacOsApp`, whose only caller is `shortcuts.ts:111`, never `addNonSteamGame`. The OTHER
  `exe` call site, `nonesteamgame.ts:258` (the Steam VDF `Exe` field), is owned and pinned by plan
  34.5-11 under `addToSteam`. `shortcuts.ts:227` also appears in `addToSteam`'s riders cell below via
  cross-reference only — it is `addShortcut`'s call site, not `addToSteam`'s.
- **D-12's `nile_config` label (plan 34.5-10).** CONTEXT.md D-12 and `34.5-RESEARCH.md` both
  originally labeled nile's CLI-owned credential directory `nile_store`. Plan 34.5-10 verified by
  direct read (`nile/constants.ts:4`, wired via `NILE_CONFIG_PATH` at `nile/library.ts:478-490`)
  that the CLI-credential directory is actually `nile_config` — `nile_store`
  (`nile/electronStores.ts:13`) is the *profile* `configStore` cwd, the other half of D-12's own
  distinction. Both resolve through `pathShim`'s already-implemented `userData`, so no new path work
  was needed; only the label was wrong. Corrected in `runnerAuthFlowRegistration.ts`'s own source
  comment (`grep -c "nile_config"` returns ≥1 there).

**Resolved Discretion question:** `documents` belongs to the saves-sync domain, not shortcuts (plan
34.5-12, confirming plan 34.5-01's original resolution) — see the correction above for the one
caveat on which channel actually reaches it.

**OPENED:** nothing beyond what is listed in `## Accepted residuals` below.

---

## Accepted residuals

- **`processShortcut`'s `ctrl+r` and `ctrl+shift+i` are DEGRADED under Tauri (T-34.5-27).** They
  reach `electronStub.ts`'s `fakeWindow.reload()` and `fakeWebContents.openDevTools()` — logged
  no-ops added by plan 34.5-08, not a functioning renderer reload or devtools toggle. This is an
  accepted gap, not a hidden one: a shell-level reload/devtools-open has no sidecar/WebView
  equivalent, and inventing one would be new work, not a port. `processShortcut`'s other four cases
  (documented in `shortcutsFlows.test.ts`'s six-case switch coverage) are unaffected.
- **Amazon and GOG cookies persist in the shared app-wide cookie jar after sign-out — the inherited,
  accepted `T-34.4.1-47` residual (T-34.5-37).** Spike 015 (Phase 34.4.1) found no per-window cookie
  isolation by default; Electron's own `NileUser.logout()`/`GOGUser.logout()` never cleared these
  cookies either, so this phase adding a clear would be a behaviour CHANGE, not a faithful port.
  Epic's sign-out (plan 34.5-06) IS domain-scoped (`epicgames.com` apex, mirroring `humble/user.ts`'s
  own convention) because Epic's cleanup previously aborted mid-wipe under the sidecar's session
  stub — a real defect this phase fixed, unrelated to the Amazon/GOG residual. Any future
  Amazon/GOG cookie clear MUST be domain-scoped the same way, never a blanket wipe.
- **The `www.amazon.com` anchor's mechanism is CONFIRMED, but the broader research uncertainty this
  entry tracks stays OPEN (research Assumption A1).** The FIRST run FAILED before it could confirm
  or falsify the anchor at all (`nile` `ENOENT` before any Amazon login attempt). The RE-RUN
  (`34.5-LIVE-GATE-RERUN.md` item 3) reached `status=captured` at the backend with a zero-count
  `origin-mismatch` grep across the full session — CONFIRMED structurally from `matchOAuthRedirect`'s
  own code, since a `nile` `captured` outcome is only reachable via an exact `www.amazon.com`
  hostname match. **This does not retire Assumption A1 as a standing claim**: the claim's own
  eligibility rule requires a PASS of item 3 as a whole (matched redirect AND populated library), and
  item 3 FAILED on the library half (no follow-up `nile auth --login` CLI invocation, no frontend
  `captured-blocked` transition). **THE THIRD RUN (`34.5-LIVE-GATE-RERUN-2.md` item 3) did not touch
  this at all — item 3 was NOT ATTEMPTED** (session ended after item 4's diagnosis before any
  `nile`/Amazon login window opened), so the third of the item's own three-way recording rule
  applies: the redirect-comparison path was never reached, and Assumption A1 is UNTESTED for this
  run specifically — not confirmed, not falsified, not re-confirmed. The narrow host-anchor mechanism
  remains confirmed from run 2's own evidence; the broader end-to-end claim stays open pending a
  future item-3 PASS.
- **`std::env::current_exe()`'s macOS bundle behaviour — the packaged half remains unproven, but a
  DEV PASS on the `Exe` VALUE itself is now confirmed for BOTH call sites (research Assumption A2,
  still OPEN overall).** Item 4 was NOT ATTEMPTED in EITHER the first run (developer elected to skip
  it) or the RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 4). **The THIRD run
  (`34.5-LIVE-GATE-RERUN-2.md` item 4) is the first-ever real attempt, and it exercised BOTH call
  sites: the sub-clause that the `Exe`/launch-command VALUE itself resolves to
  `GAMELIB_SHELL_EXE received=` (byte-for-byte, confirmed by direct comparison) is now independently
  CONFIRMED for `nonesteamgame.ts:258`'s write — but this sub-clause confirmation does NOT retire
  Assumption A2 or upgrade item 4 to a PASS**, because `addToSteam` itself returns the wrong type
  (`undefined`, not a boolean, F-34.5-G6-08) and `shortcuts.ts:227`'s call site
  (`addShortcut`/`generateMacOsApp`) never reaches a value comparison at all — it throws first, on
  the missing `nativeImage.createFromBuffer` (F-34.5-G6-07), before any `.app`/`run.sh` is written.
  So the VALUE-correctness sub-clause is confirmed at ONE of the two call sites, not both, and the
  parent item still FAILED regardless — this run does not retire Assumption A2. The packaged half of
  A2 remains entirely untouched by any run to date (every attempt, including this one, used the
  **dev** build, `src-tauri/target/debug/gamelib-shell`) — see the `R-34.5-G1-PKG` entry below,
  which covers the same dev/packaged split for the asset root and stays OPEN regardless of item 4's
  outcome.
- **Research Pitfall 2's non-Steam Wine claim remains STANDING, unretired by any of the three runs.**
  "No non-Steam runner has ever executed a Wine command under the sidecar" is eligible to be struck
  only on a PASS of item 5. The first run FAILED BY BLOCKAGE (items 1-3 failed upstream at the
  runner-binary spawn defect, so item 5 was never reached). The RE-RUN (`34.5-LIVE-GATE-RERUN.md`
  item 5) recorded NOT ATTEMPTED instead — the developer confirmed explicitly that item 5 was not
  attempted, because none of items 1-3 delivered an authenticated, library-populated non-Steam runner
  to drive it against. **The THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 5) is STILL NOT ATTEMPTED —
  but for the first time an EXPLICIT, RECORDED refusal (precondition 11) rather than a silent skip**:
  the blocking prerequisite (a `wine`-type Wine version downloaded) was re-verified unmet at gate
  time — zero entries under `tools/wine/`, and both of this machine's two configured non-Steam games
  remain `wineVersion.type: "toolkit"` (Game Porting Toolkit), which both hides the DXVK toggle and
  short-circuits before `runWineCommand` is reached. Neither the primary GOG target (contingent on a
  populated item-2 login this run did not deliver either) nor the fallback `sideload` target was
  driven — the blocking prerequisite is independent of item 2's own result. No run has yet exercised
  or falsified anything about `runWineCommand`'s own code; the claim stays open pending a future
  item-5 PASS.
- **`R-34.5-G1-PKG` — the packaged Tauri build's asset root is still unresolved (2026-08-01,
  `34.5-APP-ROOT-SWEEP.md` § 3). Stays OPEN regardless of the third gate's outcome — explicitly
  restated per this cycle's own instruction.** The G-1 fix this gap cycle landed (`GAMELIB_APP_ROOT`
  handoff, plan 34.5-16) is proven correct for a **dev** build only: `electronStub.app.isPackaged`
  stays `false` under the sidecar, so `publicDir` always appends `'public'`, which exists at the repo
  root in dev. In a packaged build the handed-down root is Tauri's `resource_dir()`, which has no
  `public/` child — appending `'public'` there would resolve to a path that does not exist, the same
  shape of defect this cycle just closed for dev. Plan 34.5-18's boot self-check makes this loud
  (a `[bootstrap] SIDECAR ASSET ROOT DEFECT` line) rather than a silent `ENOENT` six layers
  downstream, but does not resolve it. **All three gate runs' preconditions and items were dev-only
  (`pnpm tauri:dev`, most recently confirmed by `34.5-LIVE-GATE-RERUN-2.md` precondition 7's
  `GAMELIB_SHELL_EXE received=.../src-tauri/target/debug/gamelib-shell`)** — none covers this
  residual, and this run's item-4 FAIL means even the dev half never reached a PASS to make the
  dev/packaged distinction relevant. Its future home is the packaging work (building and
  self-checking a real `.app` bundle's resource layout), not Phase 34.6's channel port, since no
  additional IPC channel is involved.

---

## The 39 channels

| Channel | Kind | Registration module | Proof level | Riders |
|---|---|---|---|---|
| `getEpicGamesStatus` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit | No frontend caller exists at all (`grep -rn getEpicGamesStatus src/preload/` returns nothing) — dormant, inherited from the Electron original unchanged, not introduced or regressed by this phase (34.5-13 Sweep A). |
| `getUserInfo` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit + LIVE (item 1) — PENDING | `LegendaryUser.getUserInfo()`; called after a real Epic login to populate the signed-in profile — the "populated library" half of item 1's own definition. First run FAILED on `spawn ./legendary ENOENT` (now fixed, G-1). RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 1) also FAILED: 3 login-window opens, 3 `status=timeout`, 0 `status=captured` — the login form renders "greyed out" and non-interactive. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 1) recorded BLOCKED, not FAIL, not PENDING-untouched** — the opportunistic-attempt clause was exercised (2 login windows driven, form rendered and accepted input) but the same parked pre-auth 403 on `/id/api/email/exists` reproduced itself before a distinguishable pass/fail outcome (0 `status=captured`, 2 `status=timeout`). This channel has still never been reached live across all three runs. |
| `isLoggedIn` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit + LIVE (item 1) — PENDING | `LegendaryUser.isLoggedIn()`; the state check the real login flow calls before/after driving `login`. First run FAILED on `spawn ./legendary ENOENT` (now fixed, G-1). RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 1) also FAILED — 0 of 3 captures, non-interactive login form. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 1) recorded BLOCKED, not FAIL, not PENDING-untouched** — see `login`'s Riders cell below for the full evidence (this row shares the identical disposition and cause). |
| `login` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit + LIVE (item 1) — PENDING | THE Epic credential mint (`LegendaryUser.login(sid)`) — the direct subject of live-gate item 1. First run FAILED on `legendary` binary spawn `ENOENT` (root cause: `publicDir` resolved to a nonexistent `src-tauri/public`; CLOSED by plans 34.5-16/17/18). RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 1, precondition 4 proves the fix present) **also FAILED**, on a NEW defect: the embedded login window opened 3 separate times with the correct title but presented a "greyed out", non-interactive form — 0 of 3 attempts ever produced a matched redirect. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 1) recorded BLOCKED, not FAIL, not PENDING-untouched, per D-CYCLE5-A**: the opportunistic-attempt clause WAS exercised — two separate login-capture windows driven, form rendered and accepted input (the email-exists check firing proves credential entry began) — but the SAME parked pre-authentication defect (403 on `/id/api/email/exists`, `developer_override_2026_08_02T19_00_00`) reproduced itself immediately afterward, before a distinguishable pass/fail outcome could be produced: 0 `status=captured`, 2 `status=timeout` this run. U-34.5-06 and U-34.5-11 stay OPEN — they retire together or not at all, and neither retired this run. The mint call has still never been reached live across all three runs. See `34.5-LIVE-GATE-RERUN-2.md` item 1. |
| `logoutLegendary` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit | Sign-out path, restructured (plan 34.5-06) to make credential-side cleanup unabortable even when the sidecar's session stub throws — a real Rule-1 fix, not merely ported. Not exercised by any of the 5 live-gate items (all cover sign-IN, not sign-out). |
| `authGOG` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit + LIVE (item 2) — FAIL | THE GOG credential mint (`GOGUser.login(code)`) — the direct subject of live-gate item 2. First run FAILED on `gogdl` binary spawn `ENOENT` at sidecar startup, before any GOG interaction was attempted (same root cause as item 1; CLOSED by plans 34.5-16/17/18). RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 2) **also FAILED**, but past the spawn defect this time: the OAuth redirect WAS captured at the backend (`status=captured`, 68s after open), yet no follow-up `gogdl auth` CLI invocation and no frontend `[TauriLoginPanel] captured-blocked` transition ever fire afterward — the UI stays on the pre-capture "Signing in..." panel and the library is never populated. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 2) FAILS AGAIN, but on a THIRD, new failure layer, downstream of both prior causes**: this channel — the mint call itself — IS now reached live (`authGOG` fires, `status=captured` TWICE, `gogdl auth` CLI invocations follow both captures, `[useTauriOAuthLogin] runner=gog phase=idle (login completed, library refresh triggered)` fires twice — F-34.5-G6-02 CLOSED), and the backend chain persists 7 real games to `gog_library.json`. The item as a whole still FAILS because the Library UI never renders those games (clause (d)) and one `[refreshLibrary] runner=all origin=unknown` line still occurs (clause (g), F-34.5-G6-14). U-34.5-09 (mid-flight-teardown propagation) RETIRES on this run's evidence, independent of the item's overall FAIL. See `34.5-LIVE-GATE-RERUN-2.md` item 2. |
| `logoutGOG` | sidecar send | `runnerAuthFlowRegistration.ts` | unit | Send-kind, structurally silent on failure by design (`sidecar-send-channels-fail-silently`) — guarded in a try/catch, never a bare `.catch()`, per plan 34.5-06's own verification of `GOGUser.logout()`'s sync/async shape. Not exercised by any live-gate item. |
| `getAmazonLoginData` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit + LIVE (item 3) — PENDING | `NileUser.getLoginData()`; the first step of the real Amazon login flow item 3 drives. First run FAILED on `spawn ./nile ENOENT` (now fixed, G-1). RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 3) also FAILED downstream of a successful capture — see `authAmazon`'s Riders cell below for that run's full evidence. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 3) is NOT ATTEMPTED** — see `authAmazon`'s Riders cell below for the reason. |
| `authAmazon` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit + LIVE (item 3) — PENDING | THE Amazon credential mint (`NileUser.login(data)`), protected by plan 34.5-02's `www.amazon.com` host anchor — the direct subject of live-gate item 3's second half (the anchor confirmation). Trust-boundary-validated against `NileRegisterData`'s real shape before minting; never logs the rejected payload. First run FAILED on `nile` binary spawn `ENOENT` (now fixed, G-1); the anchor confirmation was never reached that run. RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 3) reached `status=captured` at the backend (11:10:10, second attempt) with a **zero-count `origin-mismatch` grep** — Assumption A1 (`www.amazon.com` anchor) was recorded **CONFIRMED**, proven structurally from `matchOAuthRedirect`'s own code, though the item still failed on the library half. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 3) recorded NOT ATTEMPTED, not FAIL, not PENDING-untouched** — the operator ended the session after item 4's root-cause diagnosis (session B) before a `nile`/Amazon login window was ever opened; zero `[oauthLoginCapture] runner=nile` lines appear anywhere in either preserved session log. **Consequence for the research claims:** Assumption A1 stays UNTESTED this run (not confirmed, not falsified — the redirect-comparison path was never reached), per the item's own three-way recording rule. U-34.5-04 (code-field legibility) stays OPEN, not gathered. See `34.5-LIVE-GATE-RERUN-2.md` item 3. |
| `getAmazonUserInfo` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit + LIVE (item 3) — PENDING | `NileUser.getUserData()`; called after a real Amazon login to populate the signed-in profile — the "populated library" half of item 3's own definition. First run FAILED before this channel was reached (spawn `ENOENT`, now fixed). RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 3) reached backend `status=captured` but the downstream-of-capture defect (see `authAmazon`'s Riders cell) meant this channel was still never reached live that run. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 3) is NOT ATTEMPTED** — see `authAmazon`'s Riders cell above for the reason (session ended after item 4's diagnosis before item 3 was driven). |
| `logoutAmazon` | sidecar invoke | `runnerAuthFlowRegistration.ts` | unit | Unmodified delegation to `NileUser.logout()` (verified no Electron `session` usage — no restructuring needed, unlike `logoutLegendary`). Not exercised by any live-gate item. No cookie clear added — see `## Accepted residuals`. |
| `getAlternativeWine` | sidecar invoke | `wineToolsFlowRegistration.ts` | unit | Wine probe, delegates to `GlobalConfig.get().getAlternativeWine()`. |
| `installWineVersion` | sidecar invoke | `wineToolsFlowRegistration.ts` | unit | Ported verbatim including the `progressOfWineManager` push and `notify()` calls. |
| `refreshWineVersionInfo` | sidecar invoke | `wineToolsFlowRegistration.ts` | unit | Ported verbatim including the swallow-and-return-undefined catch behaviour. |
| `removeWineVersion` | sidecar invoke | `wineToolsFlowRegistration.ts` | unit | Ported verbatim. |
| `runWineCommand` | sidecar invoke | `wineToolsFlowRegistration.ts` | unit + LIVE (item 5) — PENDING | D-14 wave-1 seam. Its Steam path was already proven transitively via the ported install/launch channels; its non-Steam path — the direct subject of live-gate item 5 — has never been observed executing under the sidecar until the gate runs (research Pitfall 2). First run FAILED BY BLOCKAGE (items 1-3 all failed at the runner-binary spawn defect). RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 5): NOT ATTEMPTED, developer confirmed explicitly, because none of items 1-3 delivered an authenticated, library-populated non-Steam runner to drive it against. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 5) is STILL NOT ATTEMPTED, not FAIL, not PENDING-untouched — but for the first time an EXPLICIT, RECORDED refusal rather than a silent skip** (precondition 11): the blocking prerequisite (a `wine`-type Wine version downloaded) was re-verified unmet at gate time — zero entries under `tools/wine/`, and both of this machine's two configured non-Steam games remain `wineVersion.type: "toolkit"` (Game Porting Toolkit), which both hides the DXVK toggle and short-circuits before `runWineCommand` is reached. Neither the primary GOG target nor the fallback `sideload` target was driven. Research Pitfall 2's claim is still NOT retired — nothing about `runWineCommand`'s own code has been exercised or falsified across all three runs. See `34.5-LIVE-GATE-RERUN-2.md` item 5. |
| `toggleDXVK` | sidecar invoke | `wineToolsFlowRegistration.ts` | unit | Ported verbatim from `main.ts:999`; the reachable dialog on a download failure (`tools/index.ts:137`) is verified safe (D-15 closure, see above). |
| `toggleDXVKNVAPI` | sidecar invoke | `wineToolsFlowRegistration.ts` | unit | Ported verbatim from `main.ts:1007`. |
| `toggleVKD3D` | sidecar invoke | `wineToolsFlowRegistration.ts` | unit | Ported verbatim from `main.ts:1015`; a dedicated regression test pins the `'vkd3d'` literal against a copy-paste-from-`'dxvk'` mistake (T-34.5-31), since macOS's platform guard (`tools/index.ts:228`) already excludes VKD3D unconditionally without a duplicate branch in this module. |
| `wine.isValidVersion` | sidecar invoke | `wineToolsFlowRegistration.ts` | unit | Wine probe, delegates to `validWine()`. |
| `shortcutsExists` | sidecar invoke | `shortcutsFlowRegistration.ts` | unit | Ported verbatim from `shortcuts/ipc_handler.ts`'s `addHandler('shortcutsExists', ...)`. |
| `addToSteam` | sidecar invoke | `shortcutsFlowRegistration.ts` | unit + LIVE (item 4) — FAIL | Reaches `nonesteamgame.ts:258` (the Steam shortcut VDF `Exe` field) via `addNonSteamGame`. Pinned by a byte-identical loud-throw test against a real, unmocked `shortcuts.vdf` write on unset/empty `GAMELIB_SHELL_EXE` (plan 34.5-11) — the invoke-kind half of the exe-in-VDF safety property. First run: NOT ATTEMPTED. RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 4): still NOT ATTEMPTED. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 4) is the first-ever real attempt for this channel, and it FAILS**: the VDF `Exe` field write itself is confirmed CORRECT — it matches `GAMELIB_SHELL_EXE received=` byte-for-byte, retiring the sub-clause of Assumption A2 that covers this call site — but `await window.api.addToSteam('1207659037','gog')` returned `undefined`, not a boolean (F-34.5-G6-08), so the UI's "Added to Steam" toggle can never flip true even on a successful write. Launching from Steam via the resulting shortcut also booted a second full GameLib instance rather than the game (F-34.5-G6-09) — a confirmed correct `Exe` VALUE does not upgrade the parent item's FAIL. Dev-build only; `R-34.5-G1-PKG` stays OPEN regardless. See `34.5-LIVE-GATE-RERUN-2.md` item 4. |
| `removeFromSteam` | sidecar invoke | `shortcutsFlowRegistration.ts` | unit | Delegates to `removeNonSteamGame`. |
| `isAddedToSteam` | sidecar invoke | `shortcutsFlowRegistration.ts` | unit | Delegates to the module-level `isAddedToSteam` helper. |
| `addShortcut` | sidecar send | `shortcutsFlowRegistration.ts` | unit + LIVE (item 4) — FAIL | Reaches `shortcuts.ts:227` (the macOS `.app` `run.sh` launch command) via `addShortcuts` → `generateMacOsApp` — corrected attribution, NOT `addToSteam` as CONTEXT.md D-09/research Correction 3 originally filed it (see `## Obligations closed and opened`). Pinned by a real, unmocked `.app`-folder write test asserting `run.sh` specifically is absent (not the whole directory) on unset/empty `GAMELIB_SHELL_EXE` (plan 34.5-08) — the send-kind half of the exe-in-VDF/run.sh safety property. Also reachable from `nonesteamgame.ts:258` via the `addSteamShortcuts` setting branch (`shortcuts.ts:43-45`), sharing that call site with `addToSteam`. Send-kind, guarded fire-and-forget body. First run: NOT ATTEMPTED. RE-RUN (`34.5-LIVE-GATE-RERUN.md` item 4): still NOT ATTEMPTED. **THIRD run (`34.5-LIVE-GATE-RERUN-2.md` item 4) is the first-ever real attempt for this channel, and it is STRUCTURALLY DEAD under the sidecar for EVERY macOS game, not target-specific**: `src/backend/sidecar/electronStub.ts`'s `nativeImage` export lacks `createFromBuffer`, and its three existing members (`createFromPath`/`createFromDataURL`/`createEmpty`) each return a bare `{}` (F-34.5-G6-07) — `convertPngToICNS`'s next chained call throws regardless, confirmed live: `[ERROR] Error converting icon to icns: TypeError: import_electron20.nativeImage.createFromBuffer is not a function`. No `.app` was ever written. A contract/preflight defect was also found live on this call site: this machine's `addSteamShortcuts: false` setting means the contract's own literal invocation (`fromMenu=false`) would have been a guaranteed silent non-event (F-34.5-G6-11) — the operator deviated to `true` to produce any evidence at all. Dev-build only; `R-34.5-G1-PKG` stays OPEN regardless — moot here since the dev attempt itself did not PASS. See `34.5-LIVE-GATE-RERUN-2.md` item 4. |
| `removeShortcut` | sidecar send | `shortcutsFlowRegistration.ts` | unit | Delegates to `removeShortcuts`. Send-kind, guarded fire-and-forget body. |
| `processShortcut` | sidecar send | `shortcutsFlowRegistration.ts` | unit | App-shell hotkey channel (`main.ts:1465`), no `pathShim` dependency. **DEGRADED**: its `ctrl+r`/`ctrl+shift+i` cases reach logged no-ops, not a working reload/devtools toggle — see `## Accepted residuals`. Its other four cases are fully functional. Send-kind, guarded fire-and-forget body. |
| `getCometVersion` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | GOG's channel (D-04 correction — `comet` is the GOG Galaxy Communication replacement, gated on `gameInfo.runner === 'gog'` at `launcher.ts:973`), not Zoom's as an earlier discussion draft wrongly bundled it. |
| `getGogdlVersion` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | Ported verbatim. |
| `getLegendaryVersion` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | Ported verbatim; becoming real required swapping 5 pre-existing "genuinely still unported" Invariant B canary tests to `winetricksInstall` (Rule 1 fix, plan 34.5-07). |
| `getNileVersion` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | Ported verbatim. |
| `callTool` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | All four branches (`winetricks`/`winecfg`/`runExe`/GOG post-step) ported verbatim. **Declared caveat:** the `winetricks` branch calls `Winetricks.run()` on the shared `tools/index.ts` object and WORKS today, live, despite the three dedicated winetricks IPC channels (`winetricksAvailable`/`winetricksInstall`/`winetricksInstalled`) being deferred to Phase 34.6 (research Pitfall 4) — proven by a dedicated test asserting `Winetricks.run` is actually invoked, not skipped or thrown. |
| `egsSync` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | Ported verbatim; verified to route only through `libraryManagerMap`/`../ipc`, no new Electron surface. |
| `getGOGLinuxInstallersLangs` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | Ported verbatim. No frontend caller currently exists despite a working preload wrapper (34.5-13 Sweep A) — pre-existing/inherited dormant condition, not introduced by this phase. |
| `syncSaves` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | Legendary's save-sync; verified its `-y` flag means no dialog/prompt is ever reachable on this path. |
| `syncGOGSaves` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | GOG's save-sync; verified its own loop never prompts either. **Declared caveat:** does NOT depend on `getPath('documents')` as originally predicted (see `## What shipped` corrections) — that dependency belongs to the separate, still-unported `getDefaultSavePath` channel. `syncGOGSaves` itself works once handed a `gogSaves` array by some other means, but the frontend's own location-resolution round trip (`SyncSaves/gog.tsx`) does not complete under the sidecar until Phase 34.6 ports `getDefaultSavePath`. |
| `downloadRuntime` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | Ported verbatim. |
| `isRuntimeInstalled` | sidecar invoke | `runnerMiscFlowRegistration.ts` | unit | Ported verbatim. |
| `getInstallInfo` | sidecar invoke | `gameDetailsFlowRegistration.ts` | unit | F-34.5-G6-10. Late-discovered (2026-08-11, plan 34.5-43) — absent from every inventory bucket until this port; a real preload channel (`common/types/ipc.ts:217`, `preload/api/helpers.ts:43`) registered ONLY on the Electron side (`main.ts:842`) until now. 12+ live rejections across two GOG titles (both installed and not-installed) in one UAT session blocked the game-details page outright. All four frontend callers go through the single choke point `frontend/helpers/index.ts:88` (`GamePage/index.tsx:262`, `InstallModal/DownloadDialog/index.tsx:319`, `ModifyInstallModal/GOG/index.tsx:143`, `DLCList/DLC/index.tsx:53`) — none of the four needed editing. Its 4th/5th positional args are build then branch, per the wrapper and `main.ts:844` (both real ends agree); `ipc.ts`'s declared parameter NAMES disagreed and are corrected in this same plan. **F-34.5-G6-10 does NOT close on this unit evidence** — it closes when a live session observes zero `getInstallInfo` `[GAMELIB_UNPORTED_CHANNEL]` lines on a details-page open. |

**Total: 39.** `sidecar send` accounts for exactly 4 rows (`logoutGOG`, `addShortcut`,
`removeShortcut`, `processShortcut`); `sidecar invoke` accounts for the other 35. Per module:
`runnerAuthFlowRegistration.ts` 11, `wineToolsFlowRegistration.ts` 9, `shortcutsFlowRegistration.ts`
7, `runnerMiscFlowRegistration.ts` 11, `gameDetailsFlowRegistration.ts` 1 (11+9+7+11+1 = 39), the
first four matching plan 34.5-13's measured completeness proof exactly and the fifth added by plan
34.5-43 (F-34.5-G6-10).

**Exactly two rows carry `LIVE (item 4)`: `addToSteam` and `addShortcut`, both now FAIL.** Each names
its own `exe` call site in its Riders cell (`nonesteamgame.ts:258` and `shortcuts.ts:227`
respectively). Crediting item 4 to `addToSteam` alone would understate what the gate proves — the
gate's own instructions (`34.5-LIVE-GATE.md`, `34.5-LIVE-GATE-RERUN.md` and
`34.5-LIVE-GATE-RERUN-2.md`, all Item 4) exercise both independently, because they are two
independent writers of the same `GAMELIB_SHELL_EXE`-derived value and either can regress
independently of the other. Item 4 was NOT ATTEMPTED across the first two runs — first run by
developer choice, RE-RUN because no item 1-3 delivered a working login to build its groundwork
target on — and got its first-ever real attempt in the THIRD run (`34.5-LIVE-GATE-RERUN-2.md`),
where both call sites were exercised and both are defective, in different ways: `addToSteam` writes
the correct `Exe` value but returns `undefined` instead of a boolean (F-34.5-G6-08); `addShortcut`
never writes at all, throwing before `run.sh` is created (`nativeImage.createFromBuffer` missing,
F-34.5-G6-07). "Proving one call site is not sufficient" (research Correction 3) is satisfied — both
were driven — but neither call site PASSED.

---

## Automated vs live proof boundary

**What the jest/tsc/cargo suites prove:** that all 38 channels dispatch with the correct kind
(`invoke` vs `send`), cross-checked bidirectionally against `handlerRegistry`/`listenerRegistry` for
every module; that the 4 send-kind channels' fire-and-forget bodies never leak an unhandled
rejection (verified by manually removing the guard on the shortcuts module and confirming the suite
fails); that `login`/`authGOG`/`authAmazon` validate their credential payload at the trust boundary
and never log a rejected value; that `authAmazon`'s credential mint is provably dependent on the
`www.amazon.com` host anchor (an integration test importing the REAL, unmocked
`matchOAuthRedirect` proves a URL that would have matched the old host-free `nile` matcher now
yields `null` and never reaches `authAmazon` — T-34.5-34, the anchor-plus-mint pair failing
together if either half regresses); that BOTH `exe` call sites (`nonesteamgame.ts:258` via
`addToSteam`, `shortcuts.ts:227` via `addShortcut`) throw loudly — proven against a real, unmocked
filesystem write with byte-identical-content assertions on unset/empty `GAMELIB_SHELL_EXE`, not
merely a rejected-promise assertion — and write the exact resolved value when it is set; that no
`ipc_handler.ts`/`main.ts` side-effect import leaks into any of the four registration modules
(curated-import discipline); that all 19 dropped-or-deferred channel names are absent from both
registries and therefore still reject with `UNPORTED_CHANNEL_MARKER` (SEAM Invariant B); that the
one reachable dialog in the Wine cluster (`tools/index.ts:137`) cannot propagate a rejection through
the real, non-mocked `electronStub.dialog` fallback; that `callTool`'s `winetricks` branch actually
invokes `Winetricks.run()`, not merely compiles without throwing; and that the measured electron-reach
baseline stayed at 34 modules (not 35 — `save_sync.ts` is not a new entry, correcting this phase's
own prediction). `181/181` test suites, `3553/3553` tests, `npx tsc --noEmit` clean, `cargo check`/
`cargo test` clean (92 passed, 0 failed, 1 ignored) — measured at `34.5-CYCLE5-ROUTING.md`'s own
"Measured baseline" (HEAD `c8f314205`) and re-confirmed unchanged at `34.5-LIVE-GATE-RERUN-2.md`
precondition 6 (HEAD `645a57a8c`, four doc-only commits later, zero changes under `src/` or
`src-tauri/`) — superseding the stale `179/179`/`3463/3463`/`84/84` figures this document
previously recorded after gap cycle 3, which themselves superseded the `173/173`/`3279/3279`/`40/40`
figures recorded before that.

**What the FIRST live gate (`34.5-LIVE-GATE.md`, executed by plan 34.5-15, ran 2026-08-01) found:**
it did NOT retire any of the standing claims it was built to test. A real Epic login never reached
the `epicgames.com` credential-entry step's outcome because the `legendary` runner binary failed to
spawn first (item 1 FAIL); the same for GOG (`gogdl`, item 2 FAIL) and Amazon (`nile`, item 3 FAIL —
Assumption A1 stayed UNTESTED because the redirect-comparison code was never reached);
`GAMELIB_SHELL_EXE`'s correctness at both `exe` call sites was NOT tested at all (item 4 NOT
ATTEMPTED, Assumption A2 stayed open); and `runWineCommand`'s non-Steam path was NOT observed
executing (item 5 FAIL by blockage). It surfaced a single, fully-diagnosed root cause — a wrong
`publicDir` resolution under the sidecar (`paths.ts:73` + `electronStub.ts:207`) — that blocked
items 1, 2, 3 and transitively 5.

**What the RE-RUN (`34.5-LIVE-GATE-RERUN.md`, executed by plan 34.5-20, ran 2026-08-01) actually
found:** the `publicDir` root cause above is CLOSED and live-proven (precondition 4: `source=
GAMELIB_APP_ROOT`, all four runner binaries `exists=true`, no `SIDECAR ASSET ROOT DEFECT` line) —
and the OAuth redirect-capture mechanism itself now works for `gog` and `nile` (real credentials,
real redirects, real matched captures, backend `status=captured` for both, something the first run
never achieved). Assumption A1 (the `www.amazon.com` host anchor) is CONFIRMED against one real
captured redirect. But it still did NOT retire the broader "session end-to-end" claim for any of
the three runners: item 1 (Epic) FAILED with 0 of 3 captures — the login form itself never becomes
interactive; items 2 (GOG) and 3 (Amazon) FAILED downstream of a successful capture — nothing
consumes `{status: 'captured', code, redirectUrl}` into a completed, UI-visible, library-populated
login (no follow-up runner-CLI auth call, no frontend `[TauriLoginPanel] captured-blocked`
transition, for either runner). Items 4 and 5 were NOT ATTEMPTED, confirmed explicitly by the
developer — Assumption A2 and research Pitfall 2's non-Steam Wine claim both stay open. No
`unit + LIVE (item N)` cell in the table above may be read as "seen working"; every FAIL cell means
the opposite was observed, and PENDING cells (item 4's two rows, item 5's one row) mean exactly what
they say — not attempted, not proven either way.

**What the THIRD run (`34.5-LIVE-GATE-RERUN-2.md`, executed by plan 34.5-41, propagated by plan
34.5-42, ran 2026-08-02) found:** both prior root causes stay CLOSED, live-proven a second time —
`publicDir`/spawn (precondition 4) and capture-to-propagation (`[useTauriOAuthLogin] runner=gog
phase=idle (login completed, library refresh triggered)` fires twice, F-34.5-G6-02 CLOSED). GOG's
entire backend chain now works end to end for the first time this phase (capture → CLI auth →
runner-aware `refreshLibrary` dispatch → persisted `gog_library.json`, 7 real games) — but item 2
still FAILS on a THIRD, new failure layer: the Library UI never renders those persisted games, and
one `[refreshLibrary] runner=all origin=unknown` line still occurs. Item 1 (Epic) entered BLOCKED
per D-CYCLE5-A; the opportunistic-attempt clause was exercised (2 login windows, form rendered and
accepted input) but the same parked pre-auth 403 reproduced itself before a distinguishable
pass/fail outcome — stays BLOCKED, not FAIL, not PASS. Item 3 (Amazon) was NOT ATTEMPTED (session
ended after item 4's diagnosis) — Assumption A1 stays UNTESTED this run specifically. Items 4 and 5
each carry a real result for the first time in this phase's history: item 4 FAIL (both `exe` call
sites exercised; `addToSteam` writes the correct value but returns `undefined`, F-34.5-G6-08;
`addShortcut` is structurally dead on the missing `nativeImage.createFromBuffer`, F-34.5-G6-07;
launching from Steam booted a second GameLib instance, F-34.5-G6-09); item 5 NOT ATTEMPTED (blocking
prerequisite unmet and re-verified at gate time — an explicit recorded refusal, not a drift). Zero
items PASSed, so per the gate document's own rule nothing is struck without the corresponding item
PASSing: all four standing claims (login seam end-to-end, the `www.amazon.com` anchor,
`GAMELIB_SHELL_EXE` correctness at both `exe` call sites, the non-Steam Wine claim) remain STANDING
— including where a sub-clause (GOG's backend chain; `addToSteam`'s `Exe` value) was independently
confirmed while its parent item still FAILed, mirroring run 2's own precedent for Assumption A1.
U-34.5-09 (mid-flight-teardown propagation) RETIRES, independent of item 2's overall FAIL. Per D-08,
Phase 34.5 STILL does not close — gap cycle 6 is next. No `unit + LIVE (item N)` cell in the table
above may be read as "seen working"; every FAIL cell means the opposite was observed, and PENDING
cells now also cover this run's BLOCKED (item 1) and NOT ATTEMPTED (items 3, 5) dispositions — see
each row's own Riders cell for which of "genuinely untouched," "BLOCKED," or "NOT ATTEMPTED" applies,
since the structured vocabulary cannot distinguish them on its own.

**A THIRD live gate ran 2026-08-02** (`34.5-LIVE-GATE-RERUN-2.md`, executed by plan 34.5-41,
propagated by plan 34.5-42): **FAIL, 0 of 5 clean** (`items_passed=0`, `items_failed=2` — items 2
GOG and 4 shortcuts; `items_blocked=1` — item 1 Epic, entered BLOCKED per D-CYCLE5-A rather than
attempted cold; `items_not_attempted=2` — items 3 Amazon and 5 Wine). Both prior root causes (run
1's `publicDir`/spawn defect, run 2's capture-to-propagation defect) are now proven CLOSED a second
time: GOG's full backend chain (capture → CLI auth → runner-aware `refreshLibrary` dispatch →
persisted library JSON, 7 games) works end to end for the first time this phase, and
`[useTauriOAuthLogin] runner=gog phase=idle (login completed, library refresh triggered)` fires
twice — **F-34.5-G6-02 is CLOSED**. But a THIRD, new failure layer blocks item 2: the Library UI
never renders the persisted games. Item 1 (Epic) stays BLOCKED — the opportunistic-attempt clause
WAS exercised (two login windows driven, form rendered and accepted input) but the same parked
pre-authentication defect (403 on `/id/api/email/exists`) reproduced itself before a distinguishable
pass/fail outcome. Items 4 and 5 each carry a real result for the first time in this phase's
history: item 4 FAIL (three new structural sidecar defects — the `nativeImage` stub lacks
`createFromBuffer`, `addToSteam` returns `undefined` not a boolean, the shell boots a second GameLib
instance instead of launching the game); item 5 NOT ATTEMPTED (no `wine`-type Wine version
downloaded, re-verified at gate time — an explicit recorded refusal, not a drift). The proof-level
vocabulary this document uses has no distinct BLOCKED or NOT ATTEMPTED token — per the convention
already established for items 4/5's prior NOT ATTEMPTED dispositions, both BLOCKED and NOT ATTEMPTED
map to the structural `PENDING` value below, with each Riders cell stating the real disposition
explicitly so it is never misread as "untouched." Every `LIVE (item N)` cell below now reflects THIS
run's recorded result. Per D-08, **Phase 34.5 STILL does not close** — gap cycle 6 is next
(`/gsd-plan-phase 34.5 --gaps`). See `34.5-LIVE-GATE-RERUN-2.md` for full evidence and
`34.5-42-SUMMARY.md` for this propagation pass.
