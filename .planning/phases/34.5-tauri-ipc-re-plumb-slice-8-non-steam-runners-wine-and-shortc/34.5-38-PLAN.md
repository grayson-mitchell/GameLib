---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 38
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md
autonomous: true
gap_closure: true
cycle: 5
requirements: [REQ-34.5-12, REQ-34.5-13]

must_haves:
  truths:
    - "A cycle-5 routing record exists, naming what happens next for EVERY verdict branch — including the branch where item 1 is BLOCKED and the phase still does not close"
    - "Plans 34.5-29, 34.5-30 and 34.5-31 are dispositioned in writing, in a NEW file, without any of the three being edited"
    - "The floor the third gate contract will assert for its test-count precondition is a number MEASURED at this cycle's HEAD, never transcribed from a prior gate document"
    - "The dev-secret-vault trade (D-CYCLE5-B) is recorded as the reason U-34.5-01 and U-34.5-10 cannot be retired by this cycle's run"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md"
      provides: "cycle-5 authorisation, disposition of the three halted plans, per-verdict routing, and the measured build baseline"
      contains: "D-CYCLE5-A"
  key_links:
    - from: "34.5-CYCLE5-ROUTING.md"
      to: "34.5-LIVE-GATE-RERUN-2.md"
      via: "the authorisation this cycle's gate contract is written under"
      pattern: "LIVE-GATE-RERUN-2"
---

<objective>
Open gap cycle 5 with a written record: what authorises it, what happens to the three plans cycle 3
halted, and — measured rather than assumed — the build-health numbers the third gate contract will
assert as its own floor.

Purpose: cycle 4's own routing document (`34.5-CYCLE4-ROUTING.md`) states that the blocking
five-item gate is authored and run by "whichever future cycle authorises the blocking gate." **This
is that cycle.** Nothing else in this cycle may proceed on an oral authorisation; it goes in a file
first, exactly as cycle 4's did.

Output: `34.5-CYCLE5-ROUTING.md`.

**This plan writes NO code, runs NO live session, and edits NONE of plans 34.5-01..37.** Plans
34.5-29 / 34.5-30 / 34.5-31 stay on disk byte-unchanged as the record of their own halt; this
document supersedes and re-homes their work by reference, never by editing them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE4-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md

**Run `graphify query "<question>"` before opening any raw source file** (project rule, `CLAUDE.md`).
This plan opens source only to read the WR-08 guard if the suite reports a failure.

<interfaces>
Binding developer decisions for this cycle, recorded 2026-08-02, to be quoted verbatim into the
routing document:

- **D-CYCLE5-A — Gate-first; Epic item 1 enters as BLOCKED.** This cycle authors
  `34.5-LIVE-GATE-RERUN-2.md` (with `verdict: null`, written before any live work, the discipline
  plans 34.5-03 and 34.5-19 followed for runs 1 and 2) and RUNS the gate. Gate item 1 (Epic) is
  entered as **BLOCKED**, with the parked pre-authentication defect named as its cause — it is not
  PASS, not FAIL, and not silently omitted. Items 2, 3, 4 and 5 get a real attempt; 4 and 5 for the
  first time ever. Epic diagnosis is NOT this cycle's blocker and must not gate the contract
  authoring or the run.
- **D-CYCLE5-B — the gate run uses the dev secret vault (`GAMELIB_DEV_SECRET_VAULT=1`).** The
  developer accepted this trade knowingly. Per `34.5-UNTESTED-ITEMS.md`'s own binding statement,
  ledger rows **U-34.5-01** and **U-34.5-10** CANNOT be retired by this run and MUST stay OPEN.

Prior-cycle numbers that are NOT to be transcribed as this cycle's floor — they are recorded here
only so the SUMMARY can state what moved: run 2's precondition 6 recorded 3463 jest / 84 Rust;
`.planning/debug/epic-login-non-interactive.md`'s `verification` block records 3548 jest / 92 Rust
(1 ignored) at the 2026-08-02 post-auth-fix commit, with ONE named pre-existing failure in
`src/backend/__tests__/longRunningChannels.test.ts` (the WR-08 stripper-integrity guard against
`DEV_LOGIN_DIAGNOSTIC_INIT_SCRIPT`'s raw-string delimiter lines). Commits `d22fe0dfd` and `50a807a56`
subsequently landed a raw-string-aware fix for that guard, so its current state is UNKNOWN and must
be MEASURED, not assumed either way.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 34.5-CYCLE5-ROUTING.md — authorisation, dispositions, and every verdict branch</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-CYCLE4-ROUTING.md` (whole file — the shape to mirror, and the source of the "PRESERVED but deferred" instruction this cycle consumes)
    - `.planning/phases/34.5-.../34.5-29-PLAN.md` (Tasks 2 and 3 specifically — the preflight being re-homed; read to cite, never to edit)
    - `.planning/phases/34.5-.../34.5-30-PLAN.md` and `34.5-31-PLAN.md` (their objectives and task lists — read to cite what is superseded, never to edit)
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` § D-08 (the no-partial-pass rule, verbatim)
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` (all 11 rows and all 3 Rules — Rule 3 binds this cycle's gate contract)
    - `.planning/debug/epic-login-non-interactive.md` frontmatter `status` and `root_cause_scope`, its `## Constraints` section, and the `developer_override_2026_08_02T19_00_00` block at the top of `## Current Focus` (read these four regions; do NOT read the whole 420 KB file)
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR-2.md` § `## Routing` (the E1 branch — diagnosis only, no fix)
  </read_first>
  <action>
    Create `34.5-CYCLE5-ROUTING.md` with frontmatter `phase`, `plan: 38`, `type: routing`, `cycle: 5`,
    `blocking: false`, `created`. Then these sections.

    **`## Authorisation`** — quote D-CYCLE5-A and D-CYCLE5-B verbatim from this plan's `<interfaces>`
    block. State plainly, in one line: gap cycle 5 is a GATE cycle — the blocking five-item live gate
    IS authored and IS run this cycle, and cycle 4's own deferral of that work terminates here. Quote
    `34.5-CYCLE4-ROUTING.md`'s own sentence naming the future cycle ("They are to be re-homed into
    whichever future cycle authorises the blocking gate") as the hand-off being taken up.

    **`## Disposition of plans 34.5-29 / 34.5-30 / 34.5-31`** — one subsection each. All three remain
    on disk, unmodified, unexecuted:
    - `34.5-29` — Task 1 (the Epic user-agent fix) is **VOID**, by two independent gates already
      recorded in `34.5-CYCLE4-ROUTING.md`: its own `fix-first` self-halt, and its own
      `R1-FALSIFIED` verdict branch. It stays void this cycle for a THIRD, newer reason: the second
      discriminator's `verdict: E1` Routing section authorises instrumentation and diagnosis only,
      and `.planning/debug/epic-login-non-interactive.md`'s `## Constraints` forbid changing
      `USER_AGENTS`, `EPIC_LOGIN_URL` or `matchOAuthRedirect`. Tasks 2 and 3 (the item-4/item-5
      preflight producing `34.5-G6-ITEM45-PREFLIGHT.md`) are **RE-HOMED to plan 34.5-39** this cycle.
    - `34.5-30` — **SUPERSEDED by plan 34.5-40**, which authors `34.5-LIVE-GATE-RERUN-2.md`.
    - `34.5-31` — **SUPERSEDED by plans 34.5-41 (run + verdict) and 34.5-42 (propagation)**.
    State explicitly that no edit, renumber or deletion is applied to any of the three, and that a
    reader must not infer from their `[ ]` state in ROADMAP.md that their work went undone.

    **`## Why the preflight is re-homed HERE and not earlier`** — quote `34.5-CYCLE4-ROUTING.md`'s
    own stated reason (a preflight written earlier "would be stale again by the time that gate
    actually runs — this is the precise reason items 4 and 5 have already been skipped twice"). Record
    that plan 34.5-39 runs in the wave immediately before the contract, and that the contract carries
    a precondition re-confirming the preflight target AT GATE TIME.

    **`## Item 1 (Epic) enters BLOCKED — the cause, named`** — per D-CYCLE5-A. Name the cause
    precisely, and do not overstate it:
    - The POST-authentication root cause is CONFIRMED and its fix is IMPLEMENTED but STATIC-ONLY
      (commit `c857ade8e`, ledger row `U-34.5-11`) — WKWebView silently refuses Epic's client-side
      navigation to its own `redirectUrl`; the `.on_navigation` exfil intercept plus
      `epic_oauth_redirect_observer_script` were built to close it and have never run live.
    - The PRE-authentication half is the blocker and is PARKED by explicit developer decision
      (`developer_override_2026_08_02T19_00_00`). Record its measured shape rather than a slogan:
      across the Tauri arm's runs, `/id/api/email/exists` returned 403 x1, `xhr.timeout` x2 and
      success x1, and NO Tauri run has ever completed an Epic login. Cite the debug file's own
      `pre_auth_defect_reframed_as_intermittent` and its instruction not to treat "403" and "timeout"
      as interchangeable.
    - Record the debug file's own NEW RISK verbatim: the production observer script wraps
      `window.fetch` using the same technique that file's own investigation suspects of CAUSING the
      pre-auth rejection, and that risk "must be checked FIRST" when the live gate runs.
    - State the consequence in one line, and state it as an EXPECTATION rather than a certainty: a
      BLOCKED item 1 is not a PASS, so the phase is **expected not to reach 5/5 this cycle**. Record
      in the same breath why that is not a guarantee: plan 34.5-40's opportunistic-attempt clause
      REQUIRES the operator to drive item 1 for real if Epic's login form renders and accepts input,
      and the measured shape above contains one success in four pre-auth attempts. This branch is
      therefore expected-unreachable, NOT unreachable-by-construction, and no downstream plan may
      treat item 1's BLOCKED entry disposition as its settled final result.

    **`## Routing by verdict`** — a table with one row per branch, each naming the exact next action.
    Cover all four, and do not leave any branch implicit:
    | Branch | Outcome | Next action |
    | items 2,3,4,5 all PASS, item 1 BLOCKED | Phase 34.5 does NOT close (D-08) | a further cycle scoped to Epic ONLY — unparking the pre-auth thread; Phase 34.6's and Phase 35's dependency lines stay untouched |
    | any of items 2,3,4,5 FAIL | Phase 34.5 does NOT close | `/gsd-plan-phase 34.5 --gaps` — gap cycle 6 |
    | any item NOT ATTEMPTED | counts toward neither passed nor failed | the shortfall is stated in the contract's `notes`; the item stays on the ledger; gap cycle 6 |
    | 5 of 5 PASS | closes the phase (D-08) | **EXPECTED to be unreachable this cycle, NOT unreachable by construction.** Item 1 ENTERS as BLOCKED and a BLOCKED item is not a PASS, but plan 34.5-40's opportunistic-attempt clause requires the operator to drive item 1 for real if Epic's login form renders and accepts input (measured shape: 403 x1 / `xhr.timeout` x2 / **success x1**), in which case item 1 resolves PASS or FAIL like any other item. This branch is taken if and only if the gate's RECORDED frontmatter reads `items_passed: 5` with `items_failed: 0`, `items_blocked: 0` and `items_not_attempted: 0` — then D-08 is satisfied, Phase 34.5 closes, and the next action is `/gsd-verify-work`. |
    Then restate D-08 verbatim from `34.5-CONTEXT.md`, and add: there is no escape hatch in this
    cycle that lets a partial result close the phase.

    Close the table with a binding note for downstream plans, stated in these terms: the 5-of-5 row
    is recorded as EXPECTED-unreachable, never as unreachable-by-construction. No plan in this cycle
    may write an acceptance criterion that presupposes the phase does not close; any criterion whose
    behaviour depends on non-closure MUST key off the four RECORDED counters
    (`items_passed` / `items_failed` / `items_blocked` / `items_not_attempted`) rather than off item
    1's entry disposition. State plainly that this is **not** a loosening of D-08 and **not** a
    partial-pass escape hatch: D-08 is unchanged and still binds, anything short of 5 PASS / 0 FAIL /
    0 BLOCKED / 0 NOT ATTEMPTED still means Phase 34.5 does not close, and no partial or blocked
    result may unblock a downstream phase. The conditional exists to prevent the OPPOSITE error only
    — recording a false "does not close" over a 5-of-5 the run genuinely earned.

    **`## Ledger interaction (Rule 2 and Rule 3)`** — per D-CYCLE5-B, record that this run uses
    `GAMELIB_DEV_SECRET_VAULT=1`, and that this choice alone keeps `U-34.5-01` (the real macOS
    Keychain path) and `U-34.5-10` (the keyring read-timeout/human-approval race, plan 34.5-35's
    45s/120s constants) OPEN. State the binding consequence: **no plan in this cycle may claim either
    row retired, and no plan may treat plan 34.5-35's raised constants as live-proven.** Point at the
    gate contract (plan 34.5-40) as the place Rule 3's "name it as a known hole" obligation is
    discharged.

    **`## Standing rule carried into every cycle-5 plan`** — a green test suite is not evidence of
    live behaviour. Name the counts this phase has been green at while failing its gate: 3279, 3387,
    3447, 3548. Every plan in this cycle states the LIVE observable that would prove its claim, and
    none records anything closed on unit evidence alone.

    Leave a `## Outcome` heading at the end with the single line `Filled by plan 34.5-42 after the
    gate runs.` — nothing else. This plan fills no outcome.
  </action>
  <verify>
    <automated>grep -q "D-CYCLE5-A" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md &amp;&amp; grep -q "U-34.5-10" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md &amp;&amp; git status --porcelain | grep -Ec "34.5-(29|30|31)-PLAN.md" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `34.5-CYCLE5-ROUTING.md` exists with `cycle: 5` in its frontmatter and all seven named sections present.
    - D-CYCLE5-A and D-CYCLE5-B appear verbatim under `## Authorisation`.
    - All three of 34.5-29 / 34.5-30 / 34.5-31 have their own disposition subsection, and `git status --porcelain` shows none of the three PLAN files modified.
    - The item-1 BLOCKED section names the post-auth fix as static-only (`U-34.5-11`, commit `c857ade8e`) AND the pre-auth blocker's measured shape (403 x1 / `xhr.timeout` x2 / success x1, zero completed Tauri logins) AND the `window.fetch`-wrapper risk to check first.
    - The routing table has exactly four branches, each naming a concrete next action. The 5/5 branch is marked EXPECTED-unreachable and explicitly NOT unreachable-by-construction, names plan 34.5-40's opportunistic-attempt clause as the reason it stays reachable in principle, and states the exact arithmetic that would make it real (`items_passed: 5`, `items_failed: 0`, `items_blocked: 0`, `items_not_attempted: 0`).
    - The note closing the routing table forbids downstream acceptance criteria that presuppose non-closure, requires them to key off the four recorded counters, and states that this is not a loosening of D-08 and not a partial-pass escape hatch.
    - D-08 is quoted verbatim from `34.5-CONTEXT.md`.
    - The ledger section states that U-34.5-01 and U-34.5-10 stay OPEN, names `GAMELIB_DEV_SECRET_VAULT=1` as the reason, and forbids treating 34.5-35's constants as live-proven.
    - The `## Outcome` section exists and contains only the hand-off line to plan 34.5-42.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Measure the build-health baseline at HEAD and record it as the gate's floor</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-CYCLE5-ROUTING.md` (the file being appended to — Task 1's output)
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` precondition 6 (the shape of the recording, and the reason a floor must be explained rather than rounded)
    - `.planning/debug/epic-login-non-interactive.md` § `## Resolution`, the `verification` block ONLY (the last recorded counts and the named WR-08 pre-existing failure)
    - `src/backend/__tests__/longRunningChannels.test.ts` — read ONLY if the suite reports a failure in that file, to identify which assertion fired
  </read_first>
  <action>
    Append a `## Measured baseline (this cycle's HEAD)` section to `34.5-CYCLE5-ROUTING.md`. Every
    number in it is measured now; none is copied from a prior document.

    Record, each with its VERBATIM exit code and the verbatim summary line, never rounded to "green":
    1. `git rev-parse --short HEAD` — the commit every number below belongs to.
    2. `npm run test:ci` — record the `Test Suites:` and `Tests:` lines verbatim.
    3. `npx tsc --noEmit`.
    4. `cd src-tauri && cargo check`.
    5. `cd src-tauri && cargo test` — record the `test result:` line verbatim.
    6. `python3 .planning/phases/34.5-.../ported-channels-gate.py` and the same script with
       `--self-test`.

    **If any command exits non-zero, DO NOT round it to green and DO NOT assume it is the known
    WR-08 failure.** Follow the standing `flake-baselines-can-be-undiagnosed-bugs` rule: re-run the
    failing suite alone with `npx jest <path> --silent`, record that result too, and determine
    whether the failure is pre-existing at HEAD by `git stash`/`git stash pop` if any working-tree
    change exists. Specifically: the WR-08 stripper-integrity guard in
    `src/backend/__tests__/longRunningChannels.test.ts` was reported failing against
    `DEV_LOGIN_DIAGNOSTIC_INIT_SCRIPT` on 2026-08-02, and commits `d22fe0dfd` / `50a807a56` then
    landed a raw-string-aware fix — so its present state is genuinely unknown. Record which it is;
    do not predict it.

    Close the section with an explicit sentence naming these numbers as **the floor plan 34.5-40's
    precondition 6 asserts**, and stating the rule that a count at or below this floor at gate time
    means HEAD is stale and must be explained before the run proceeds. Add the corollary that a
    count ABOVE this floor is expected and healthy only if a plan in this cycle landed tests — and no
    plan in this cycle lands any, so a HIGHER count also needs an explanation.

    Also record the working-tree state at measurement time (`git status --porcelain`, summarised by
    file count, with any `.planning/` paths listed) — a baseline measured on a dirty tree is a
    different number from one measured on a clean tree, and the gate must know which it got.
  </action>
  <verify>
    <automated>grep -q "## Measured baseline" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md &amp;&amp; grep -qE "Tests:[^|]*[0-9]{4}" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE5-ROUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - All six commands are recorded with their verbatim exit code and summary line, plus the short HEAD sha.
    - Any non-zero exit carries an isolated single-suite re-run result AND a stated determination of pre-existing-vs-regression; no non-zero exit is described as "green" or "known flake" without that determination.
    - The `ported-channels-gate.py` run and its `--self-test` are both recorded, each exiting 0 (or the failure explained).
    - The section names these numbers as plan 34.5-40 precondition 6's floor and states both the stale-HEAD rule and the higher-count corollary.
    - The working-tree state at measurement time is recorded.
    - `git status --porcelain` still shows plans 34.5-29 / 34.5-30 / 34.5-31 unmodified.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior halted plans → this cycle's record | Editing a halted plan would destroy the evidence of its own halt |
| measured build numbers → a blocking gate's precondition | A transcribed or rounded number lets a stale build pass a gate precondition |
| parked security investigation → gate authorisation | Item 1's blocker is a live anti-bot rejection on a real credential origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C5-01 | Tampering | `34.5-29/30/31-PLAN.md` | mitigate | Every disposition is written into a NEW file; both tasks' acceptance criteria require `git status --porcelain` to show all three unmodified |
| T-34.5-C5-02 | Repudiation | a rounded or transcribed test-count floor | mitigate | Task 2 records verbatim exit codes and summary lines, and requires an isolated re-run plus a pre-existing-vs-regression determination for any non-zero exit |
| T-34.5-C5-03 | Repudiation | a verdict branch with no stated routing, read later as implicit permission to close | mitigate | Task 1's routing table must carry all four branches, including the explicitly-unreachable 5/5 branch, each with a concrete next action |
| T-34.5-C5-04 | Information disclosure | Epic session artefacts quoted into a public-fork document | mitigate | This plan quotes only statuses, timings and counts from the debug file — no `code=`, `access_token`, cookie or session identifier is copied |
| T-34.5-C5-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages; it only runs existing test/build commands from committed lockfiles |
</threat_model>

<verification>
- `34.5-CYCLE5-ROUTING.md` exists with all sections, the four-branch routing table, and the measured baseline.
- `git status --porcelain` shows zero modifications to `34.5-29-PLAN.md`, `34.5-30-PLAN.md`, `34.5-31-PLAN.md`.
- `git diff --name-only` matches zero `.ts`, `.tsx` or `.rs` paths.
</verification>

<success_criteria>
Gap cycle 5 has a written authorisation naming itself as the gate cycle, a disposition for each of
the three halted plans that edits none of them, a routing outcome for every verdict branch including
the one where item 1 is BLOCKED, and a build-health floor measured at this cycle's own HEAD.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-38-SUMMARY.md` when done
</output>
