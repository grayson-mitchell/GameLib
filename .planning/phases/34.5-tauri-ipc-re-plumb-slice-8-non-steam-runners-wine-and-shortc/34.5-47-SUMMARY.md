---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 47
subsystem: frontend
tags: [react, logging, log-injection, tdd, oauth, tauri, discriminator, gsd-checkpoint]

# Dependency graph
requires:
  - phase: 34.5 (plan 34.5-33)
    provides: origin threaded through GlobalState.tsx's own six internal refreshLibrary call sites
  - phase: 34.5 (plan 34.5-34)
    provides: mid-flight-teardown propagation fix in useTauriOAuthLogin.ts (U-34.5-09, retired)
  - phase: 34.5 (debug session, fix eb117d9e4)
    provides: sidecar-initiated store writes now announce to the renderer (Library-render half of Cluster 3)
  - phase: 34.5 (commit 343a0f6d9)
    provides: handleTauriOAuthSuccess's navigate('/login') after a Tauri OAuth login
provides:
  - Fixed-literal origin threaded through all 15 production refreshLibrary call sites (6 internal + 9 external)
  - Source-text gate extended to cover the 9 external sites + a literal-only security rule (T-34.5-47-01), RED-proven against a real reverted edit
  - A three-way discriminator design + live-run methodology for the Manage Accounts "logging into gog" symptom
  - A live, developer-driven non-reproduction result for that symptom on current HEAD (READING NONE)
  - Ledger updates for U-34.5-07, U-34.5-08, and a new row U-34.5-23 for the deferred origin union-type narrowing
affects: [34.5 gate re-run, any future refreshLibrary call site, Manage Accounts / WebView OAuth flow, GlobalState.tsx origin instrumentation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Fixed single-quoted string literal origin at every refreshLibrary call site — never a variable, template literal, or computed value (log-injection / forged-attribution mitigation)"
    - "Source-text structural gate (readFileSync + stripSourceComments) for components that cannot be imported under the DOM-less frontend jest project"
    - "Discriminator-first diagnosis: design a live-observable trace that distinguishes N mutually exclusive readings BEFORE writing any fix"

key-files:
  created:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-47-DISCRIMINATOR.md
  modified:
    - src/frontend/screens/Login/index.tsx
    - src/frontend/components/UI/NavShell/components/NavTabs/index.tsx
    - src/frontend/components/UI/NavShell/__tests__/NavTabsComponent.test.tsx
    - src/frontend/components/UI/ActionIcons/index.tsx
    - src/frontend/components/UI/ErrorComponent/index.tsx
    - src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx
    - src/frontend/screens/Settings/components/ClearCache.tsx
    - src/frontend/screens/Settings/components/EgsSettings.tsx
    - src/frontend/screens/ConsoleMode/index.tsx
    - src/frontend/screens/Library/components/InstallModal/SideloadDialog/index.tsx
    - src/frontend/types.ts
    - src/frontend/state/__tests__/GlobalStateRefreshLibraryOrigin.test.ts
    - src/frontend/screens/WebView/index.tsx
    - src/frontend/screens/WebView/components/TauriLoginPanel.tsx
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md

key-decisions:
  - "frontend/types.ts's own local RefreshOptions type (feeding ContextType.refreshLibrary, distinct from GlobalState.tsx's local widening of common/types.ts's RefreshOptions) needed origin?: string added too, undiscovered by planning's interfaces block — a Rule 3 blocking-issue fix, not a Rule 4 architectural change"
  - "RefreshLibraryOptions['origin'] stays a plain string in this plan (both copies) — union narrowing deferred to U-34.5-23 per the plan's own file-ownership-collision reasoning"
  - "Defect B: recorded as READING NONE (live trace matches none of three hypothesized broken-behaviour signatures) rather than forced into one of the three — a non-reproduction, explicitly NOT a retirement of U-34.5-07 (Ledger Rule 1/3: only a gate run retires a ledger row)"
  - "Did not commit the raw gamelib.log snapshot used for byte-verification into the repo — cmp-verified locally, relevant lines transcribed into the discriminator doc, copy discarded, consistent with this project's standing caution against committing credential-adjacent runtime artifacts"

patterns-established:
  - "When a live-observable requires real user credentials the executor cannot obtain, pause via a human-action checkpoint AFTER completing every automatable step (static analysis, instrumentation, process-hygiene checks, dev-server start) rather than fabricating or skipping the observation"

requirements-completed: [REQ-34.5-04, REQ-34.5-12]

# Metrics
duration: ~21min active work (excludes an out-of-band pause for a developer-driven live GOG login via a mid-plan checkpoint)
completed: 2026-08-11
---

# Phase 34.5 Plan 47: Origin attribution + Manage Accounts discriminator Summary

**Threaded a fixed-literal `origin` through the 9 remaining production `refreshLibrary` call sites (15/15 total, log-injection-proofed by an executable gate), and ran a live three-way discriminator for the Manage Accounts "logging into gog" symptom that resolved as a non-reproduction on current HEAD.**

## Performance

- **Duration:** ~21 min of active agent work across 5 commits, plus an out-of-band pause for a developer-driven live GOG login triggered via a mid-plan `checkpoint:human-action`
- **Started:** 2026-08-11T22:05:09+12:00 (first task commit)
- **Completed:** 2026-08-11T22:25:50+12:00 (final task commit) — SUMMARY/state commits follow
- **Tasks:** 3 (Task 3 spanned a checkpoint pause and two commits: WIP instrumentation, then resolution)
- **Files modified:** 15 (14 source/test files + the shared ledger)

## Accomplishments

- All 15 production `refreshLibrary` call sites (6 internal + 9 external) now thread a unique, fixed single-quoted string-literal `origin` — a surviving `origin=unknown` line is now, by construction, evidence of a non-frontend emitter rather than an unattributed mystery.
- The single existing origin source-text gate was extended (not duplicated) to cover all 15 sites plus a literal-only security rule (`everyOriginIsAFixedStringLiteral`, T-34.5-47-01), RED-proven against a real reverted edit (not just synthetics) and against 5 synthetic regressions (deleted key, template literal, bare identifier, double-quoted string, embedded-newline log-injection payload).
- Built and ran a three-way discriminator for the Manage Accounts "logging into gog" symptom, including a Step 3a staleness check that found the brief's own "no navigation" premise was already false against the live tree (fix commit `343a0f6d9` predates the UAT observation by over an hour).
- A real, developer-driven live GOG login through Manage Accounts under the running `pnpm tauri:dev` session produced a probe trace matching none of the three hypothesized broken-behaviour readings — recorded honestly as `READING NONE` / non-reproduction, not forced into a false positive.

## Task Commits

Each task was committed atomically:

1. **Task 1: Thread fixed-literal `origin` through the nine external `refreshLibrary` call sites** — `061c7d521` (feat)
2. **Task 2: Extend the origin source-text gate to the nine external sites + literal-only rule (TDD)** — `48a0e0c41` (test)
3. **Task 3: Build and run the Defect B discriminator, apply the fix its outcome selects** — `9f7e50b86` (wip, probes + Step 3a/3b design, paused at a `checkpoint:human-action`), then `f638a5efe` (docs, resolution: probes removed, READING NONE recorded, ledger updated) after the developer performed the live GOG login and reported the outcome

**Plan metadata:** (this commit) — `docs: complete 34.5-47 plan`

## Files Created/Modified

- `src/frontend/screens/Login/index.tsx` — `origin: 'login-screen'`
- `src/frontend/components/UI/NavShell/components/NavTabs/index.tsx` — `origin: 'nav-tabs-games-tab'`
- `src/frontend/components/UI/NavShell/__tests__/NavTabsComponent.test.tsx` — updated the one existing test asserting `refreshLibrary`'s exact call args (Rule 1)
- `src/frontend/components/UI/ActionIcons/index.tsx` — `origin: 'action-icons-refresh-button'`
- `src/frontend/components/UI/ErrorComponent/index.tsx` — `origin: 'error-component-retry'`
- `src/frontend/components/UI/RedeemSteamKeyDialog/index.tsx` — `origin: 'redeem-steam-key'`
- `src/frontend/screens/Settings/components/ClearCache.tsx` — `origin: 'clear-cache'`
- `src/frontend/screens/Settings/components/EgsSettings.tsx` — `origin: 'egs-sync'`
- `src/frontend/screens/ConsoleMode/index.tsx` — `origin: 'console-mode-mount'`
- `src/frontend/screens/Library/components/InstallModal/SideloadDialog/index.tsx` — `origin: 'sideload-add-app'`
- `src/frontend/types.ts` — added `origin?: string` to the local `RefreshOptions` type feeding `ContextType.refreshLibrary` (Rule 3 blocking-issue fix)
- `src/frontend/state/__tests__/GlobalStateRefreshLibraryOrigin.test.ts` — extended with a new describe block: 9-row `it.each`, `everyOriginIsAFixedStringLiteral`, 7 self-tests (1 positive control + 5 negative controls + 1 default-immunity control)
- `src/frontend/screens/WebView/index.tsx` — Defect B discriminator probes added then fully removed; net diff vs pre-plan is zero functional change
- `src/frontend/screens/WebView/components/TauriLoginPanel.tsx` — same (probe 4 added then removed)
- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-47-DISCRIMINATOR.md` — new; Step 3a/3b/3c/3d design and results, `## Observed: READING NONE`
- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md` — `U-34.5-07` and `U-34.5-08` cells updated (append-only), new row `U-34.5-23` added

## Decisions Made

- `frontend/types.ts`'s own local `RefreshOptions` type (distinct from `common/types.ts`'s `RefreshOptions` that `GlobalState.tsx` widens) needed `origin?: string` too — undiscovered by planning's `<interfaces>` block, found only when `tsc` failed on all nine call sites. Fixed under Rule 3 (blocking issue), plain optional string to match the existing non-union choice.
- Kept `RefreshLibraryOptions['origin']` (both copies) as a plain string per the plan's explicit fence; the union-type narrowing is deferred to new ledger row `U-34.5-23`, naming the same file-ownership-collision blocker the plan itself pre-registered.
- Recorded the live discriminator result as `READING NONE` rather than force-fitting it into READING 1/2/3 — the probe trace matches the FIXED shape, not any of the three broken-behaviour signatures. Explicitly did not treat this as a retirement of `U-34.5-07` or gate item 2 (only a formal gate run can do that).
- Did not track the raw `gamelib.log` byte-verification copy in the repo — cmp-verified it locally, transcribed the relevant lines into the discriminator doc, then discarded the copy, consistent with this project's standing caution about committing credential-adjacent runtime logs.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added `origin?: string` to `frontend/types.ts`'s local `RefreshOptions`**
- **Found during:** Task 1 (`pnpm tsc --noEmit` failed on all nine call sites: "Object literal may only specify known properties, and 'origin' does not exist in type 'RefreshOptions'")
- **Issue:** External call sites go through `useContext(ContextProvider)`'s `refreshLibrary`, typed by `ContextType` in `frontend/types.ts` against a SEPARATE local `RefreshOptions` type — not `common/types.ts`'s `RefreshOptions` that `GlobalState.tsx`'s own `RefreshLibraryOptions` widens. Neither the plan's `<interfaces>` block nor `GlobalState.tsx`'s own header comment named this second type.
- **Fix:** Added `origin?: string` to `frontend/types.ts`'s `RefreshOptions`, with a comment cross-referencing `GlobalState.tsx`'s own non-union rationale.
- **Files modified:** `src/frontend/types.ts`
- **Verification:** `pnpm tsc --noEmit` exits 0; full `pnpm jest` run afterward 4707/4708 passing (1 pre-existing skip).
- **Committed in:** `061c7d521` (Task 1 commit)

**2. [Rule 1 - Stale test] Updated `NavTabsComponent.test.tsx`'s exact-args assertion**
- **Found during:** Task 1 verification
- **Issue:** The one pre-existing test asserting `refreshLibrary`'s exact call shape (`toHaveBeenCalledWith({ runInBackground: true })`) failed once `origin: 'nav-tabs-games-tab'` was added, since the test predates this plan.
- **Fix:** Updated the expectation to include the new `origin` field.
- **Files modified:** `src/frontend/components/UI/NavShell/__tests__/NavTabsComponent.test.tsx`
- **Verification:** Suite passes (285/285 in that directory).
- **Committed in:** `061c7d521` (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 Rule 3 blocking-issue, 1 Rule 1 stale-test)
**Impact on plan:** Both necessary for correctness (compilation) and to keep the touched test suite honest. No scope creep — neither file was in the plan's fenced list, and both changes are minimal, directly caused by Task 1's own edits.

## Issues Encountered

**Task 3's live-run step (Step 3c) could not be performed autonomously.** This executor has no test/dev GOG credentials, and every prior live login in this phase's history is developer-driven, never agent-driven. Resolved by completing every automatable step first (Step 3a staleness analysis, Step 3b discriminator design + probe instrumentation, process-hygiene check, starting the dev server) and then pausing at a `checkpoint:human-action`. The developer performed the real GOG sign-out/sign-in and reported the outcome ("returns to manage accounts on its own"), which this executor then used to pull the probe trace from `gamelib.log`, complete the discriminator's `## Observed` section, remove the probes, and finish the task.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- The origin attribution instrumentation (all 15 call sites) is complete and gate-covered; a future live gate run can now either see zero `origin=unknown` lines, or — if one survives — treat it as a forced identification of a non-frontend emitter (the two standing candidates are named in `U-34.5-08`).
- `U-34.5-07` (GOG library populate + Manage Accounts panel) and `U-34.5-23` (origin union-type narrowing) remain OPEN, each with a precisely stated retirement bar, ready for the phase's next formal gate run.
- `34.5-47-DISCRIMINATOR.md` is a reusable template for any future "backend succeeded, frontend didn't tell the truth" investigation in this phase — three mutually-exclusive readings, a Step 3a staleness check before any fix, and an honest READING NONE outcome when the trace doesn't fit any hypothesis.
- No blockers for the next plan in this wave.

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-08-11*

## Self-Check: PASSED

- FOUND: `34.5-47-DISCRIMINATOR.md`
- FOUND: `src/frontend/types.ts`
- FOUND: `src/frontend/state/__tests__/GlobalStateRefreshLibraryOrigin.test.ts`
- FOUND: `34.5-UNTESTED-ITEMS.md`
- FOUND commits: `061c7d521`, `48a0e0c41`, `9f7e50b86`, `f638a5efe`
