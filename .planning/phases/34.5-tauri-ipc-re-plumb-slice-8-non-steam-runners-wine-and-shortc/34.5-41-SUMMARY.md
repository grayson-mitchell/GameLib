---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 41
subsystem: testing
tags: [live-gate, sidecar, tauri, oauth, gogdl, legendary, nile, oauthLoginCapture, useTauriOAuthLogin, refreshLibrary, addToSteam, addShortcut, nativeImage, wine, shortcuts, keyring]

# Dependency graph
requires:
  - phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
    provides: "Plan 34.5-40's gate contract (34.5-LIVE-GATE-RERUN-2.md, verdict null), plan 34.5-39's item-4/5 preflight, and this plan's own Task 1 pre-gate preparation (commit e30bcad34)"
provides:
  - "A completed (not passing) third blocking 5-item live-gate for Phase 34.5's Epic/GOG/Amazon OAuth logins, dual exe call-site shortcuts, and non-Steam Wine command"
  - "Proof that F-34.5-G6-02 (backend capture never propagating to a frontend phase=idle line) is CLOSED for GOG — the exact clause run 2 lacked entirely now fires twice"
  - "Proof that GOG's entire backend chain (capture -> CLI auth -> runner-aware refreshLibrary dispatch -> persisted library JSON) works end to end for the first time this phase — a THIRD, new, frontend-render-only failure layer discovered downstream of it"
  - "First-ever results for items 4 and 5 in this phase's history: item 4 FAIL (nativeImage sidecar stub structurally blocks the macOS .app shortcut for every game; addToSteam misreports its own return type; launching from Steam boots a second GameLib instance instead of the game); item 5 NOT ATTEMPTED (no wine-type Wine version downloaded, re-verified at gate time)"
  - "U-34.5-09 (mid-flight-teardown login propagation) retired — live-proven twice in one session"
  - "Nine new findings (F-34.5-G6-07..15) recorded observation-only for gap cycle 6 to scope"
affects: [34.5-gap-cycle-6, phase-34.6, phase-35]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A sub-clause can be independently confirmed correct (addToSteam's Exe field value; GOG's backend persistence chain) while its parent item still FAILs — the item verdict and the sub-clause verdict are recorded as distinct questions, mirroring run 2's own precedent for Assumption A1"
    - "A ledger row's own narrowly-worded retirement observable can retire independently of the gate item's overall result, when the row's text names a narrower mechanism than the item's full evidence-clause set (U-34.5-09 retired despite item 2's overall FAIL)"
    - "Evidence handed into an execution plan is re-verified against the source log/filesystem directly before being recorded, and any discrepancy is recorded rather than silently adopted or silently corrected without a note"

key-files:
  created:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-41-SUMMARY.md
  modified:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md

key-decisions:
  - "Item 1 (Epic) recorded BLOCKED, not converted to FAIL by the opportunistic-attempt clause, even though a real attempt was driven (two login windows) and the form rendered and accepted input — because the observed outcome (403 on /id/api/email/exists, then two backend timeouts) is the parked pre-auth defect reproducing itself, not a distinguishable new pass/fail outcome on Epic's own merits. Converting to FAIL would have overclaimed a completed adjudicated attempt that did not, in fact, happen."
  - "Item 2 (GOG) recorded FAIL despite F-34.5-G6-02 closing and the entire backend chain (capture, CLI auth, dispatch, persistence -- 7 games landed in gog_library.json) succeeding, because the item's own clause (d) requires the library populated IN THE UI, which the operator observed did not happen. Backend success does not upgrade a UI-visible failure -- the same distinction run 2 drew for Assumption A1, applied here to a different sub-clause/parent pair."
  - "U-34.5-09 retired independently of item 2's overall FAIL result -- its own row names a narrower mechanism (mid-flight-teardown propagation survives) than item 2's full evidence-clause set, and that narrower observable was fully satisfied twice. Ledger Rule 1 ('retired ONLY by the observation named in its own row') was read literally rather than assumed to require the parent item's overall PASS."
  - "Two evidence discrepancies were found and corrected against the source directly, per this plan's own re-verification mandate (following plan 34.5-20's '4 vs 3 timeouts' precedent): the getInstallInfo unported-channel count (evidence handed in said 5 occurrences at 21:55:31; direct grep of the preserved log shows 8 at that timestamp, 18 total across the session) and shortcuts.vdf's post-run size/mtime (evidence said 472 bytes / 22:00:22; direct read at Task 3 execution time found 533 bytes / 22:10, most plausibly a later Steam rewrite-on-exit, not a second addToSteam call -- no second write log line exists)."
  - "Frontmatter preconditions_pending recorded as [2, 3] rather than assumed satisfied -- this plan's scope (Tasks 2 and 3 only, per the executing agent's explicit brief) did not re-open Task 1's own checkpoint, which left those two preconditions' RESULT text empty 'as scoped' per commit e30bcad34. Item-level log evidence independently confirms preconditions 4, 7 and 10 held at both sessions' starts, so those three are recorded satisfied; 2 and 3 (the clean-machine kill sweep and rebuild/launch PID-pair evidence) were never independently re-verified in writing and are recorded PENDING rather than rounded to SATISFIED."
  - "Verdict computed strictly off the four recorded counters (items_passed=0) rather than off item 1's entry disposition, per 34.5-CYCLE5-ROUTING.md's own binding instruction -- this is NOT the 5-of-5 case, so D-08 blocks closure regardless of item 1's cause being well-understood and pre-named."

patterns-established: []

requirements-completed: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-04, REQ-34.5-05, REQ-34.5-12]

# Metrics
duration: 45min
completed: 2026-08-02
---

# Phase 34.5 Plan 41: Third Blocking Live-Gate — Execution and Record Summary (Tasks 2-3)

**Third live-gate run executed on real macOS hardware, verdict FAIL 0/5 (2 FAIL, 1 BLOCKED, 2 NOT
ATTEMPTED) — the run 1 (publicDir/spawn) and run 2 (capture-to-propagation) defect families are both
now proven CLOSED, but a THIRD, new failure layer blocks GOG (backend fully succeeds, 7 games
persisted, but the Library UI never renders them) and item 4's shortcuts flow surfaces three
structural sidecar defects on its first-ever attempt. Phase 34.5 does NOT close; routes to gap cycle
6.**

## Performance

- **Duration:** ~45 min (this session, Tasks 2-3 only; Task 1's pre-gate-preparable preconditions
  were executed and committed separately by a prior agent, commit `e30bcad34`; the live sessions
  themselves — 21:23:55–22:00:24 — were driven by the developer, not by this agent)
- **Started:** 2026-08-02 (session start, Task 2/3 recording against the handed-in evidence package
  and the two preserved `gamelib.log` sessions)
- **Completed:** 2026-08-02
- **Tasks:** 2 (Task 2: item results and evidence; Task 3: verdict, arithmetic, falsification and
  known-holes resolution — executed together as a single documentation pass since both operate on
  the same already-completed live session's evidence)
- **Files modified:** 1 (`34.5-LIVE-GATE-RERUN-2.md`)

## Accomplishments

- All five item Result slots filled in `34.5-LIVE-GATE-RERUN-2.md`, each re-verified directly
  against the preserved `gamelib.log.34.5-g6-gate3-sessionA`/`...sessionB` log files (not the
  rotated, evidence-losing live log) or against the live filesystem state where the item's evidence
  is filesystem-based (VDF read-back, `gog_library.json`, `.app` absence, source-code stub reads).
- The "Known holes (ledger Rule 3)" table resolved row by row in a new "Known holes — RESOLVED"
  subsection: U-34.5-09 RETIRED; all ten other OPEN rows stay OPEN, each against its own row's
  stated retirement bar, not by inference from a neighbouring row or from item 2's overall FAIL.
- The "What this gate falsifies" section's four standing research claims resolved: all four remain
  STANDING (zero items PASSed this run), with the confirmed-sub-clause-vs-failed-parent distinction
  drawn explicitly for claim #1 (GOG's backend success) and claim #3 (the `Exe` field's correct
  value inside `addToSteam`'s otherwise-defective call).
- Nine new findings recorded (`F-34.5-G6-07` through `-15`), observation-only, no diagnosis, for the
  next gap cycle to scope — covering the `nativeImage` sidecar stub gap, `addToSteam`'s wrong return
  type, the shell's ignorance of `--no-gui`/deep-links, `getInstallInfo`'s absence from the IPC
  inventory, a contract/preflight defect (global shortcut settings would make the contract's own
  literal invocation a silent non-event), the GOG backend-succeeds/UI-fails split, the
  `cancelled-midflight` anomaly preceding each successful `phase=idle`, the persisting
  `origin=unknown` line, and the mid-gate log rotation itself.
- Frontmatter fully updated: `status: complete`, `verdict: FAIL`, all four item counters (`0`/`2`/
  `1`/`2`, reconciling to 5), `preconditions_satisfied`/`_blocked`/`_pending` lists, `completed`
  date, and a `notes` block stating the D-08 non-closure and naming the exact routing branch
  (`34.5-CYCLE5-ROUTING.md`'s "any of items 2,3,4,5 FAIL" row → `/gsd-plan-phase 34.5 --gaps`).

## Task Commits

Task 2 and Task 3 were executed together as a single documentation pass (both operate on the same
already-completed live session's evidence and the same target document) and committed as one:

1. **Tasks 2-3: Fill item results, compute verdict, resolve falsification/known-holes tables** —
   `19a5b4d1e` (fix — the commit is scoped as `fix` rather than `docs` because it records a blocking
   phase-gate FAIL that changes downstream routing, not documentation-only content)

**Plan metadata:** *(this SUMMARY's own commit, made immediately after this document)*

_Note: this is a `type: execute` plan whose Tasks 2 and 3 are both `checkpoint`/`auto` documentation
tasks operating on a single already-completed live run's evidence — there is no TDD RED/GREEN/
REFACTOR cycle here._

## Files Created/Modified

- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md` — all five item Result slots filled; Known holes table resolved (new subsection); falsification section resolved; arithmetic resolved; nine new findings recorded; "What this run nevertheless proved" section added; frontmatter completed (status, verdict, four counters, precondition lists, notes).
- `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-41-SUMMARY.md` — this document.

**Confirmed unchanged (T-34.5-C5-* discipline):** `34.5-LIVE-GATE.md`, `34.5-LIVE-GATE-RERUN.md`,
and plans `34.5-29`/`34.5-30`/`34.5-31`/`34.5-38`/`34.5-39`/`34.5-40` and their SUMMARYs — verified
via `git diff --stat` against each, zero output.

## Decisions Made

See `key-decisions` in frontmatter for the full list. The most consequential:

1. **Item 1 stays BLOCKED, not FAIL**, despite a genuine opportunistic attempt being driven — the
   observed outcome (403, then timeout) reproduces the parked cause rather than adjudicating Epic's
   login on its own merits.
2. **Item 2 is FAIL despite the entire backend chain succeeding** — the item's own clause (d)
   requires UI-visible population, which did not happen; backend success is recorded as a real
   advance (in "What this run nevertheless proved") without being allowed to upgrade the item's
   verdict.
3. **U-34.5-09 retires on its own narrower observable**, independent of item 2's overall FAIL —
   Ledger Rule 1 was applied literally rather than requiring the parent item's overall PASS.
4. **Two evidence discrepancies were caught and corrected** by re-verifying the handed-in evidence
   package against the log/filesystem directly, per this plan's own re-verification mandate.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug in evidence, not code] Corrected a mis-stated `getInstallInfo` occurrence count**
- **Found during:** Task 3, item 4 re-verification
- **Issue:** The evidence handed to this plan stated the `[GAMELIB_UNPORTED_CHANNEL] No handler
  registered for channel 'getInstallInfo'` line fired "×5" at (21:55:31).
- **Fix:** Direct `grep` against `gamelib.log.34.5-g6-gate3-sessionB` found **8** occurrences at
  that exact timestamp and **18** total across the session. The file's version was recorded, with
  the discrepancy noted explicitly in the item-4 evidence text.
- **Files modified:** `34.5-LIVE-GATE-RERUN-2.md` (item 4 section)
- **Verification:** `grep -c "21:55:31.*getInstallInfo"` → 8; `grep -c "getInstallInfo"` → 18.
- **Committed in:** `19a5b4d1e`

**2. [Rule 1 - Bug in evidence, not code] Corrected `shortcuts.vdf`'s post-run size/mtime**
- **Found during:** Task 3, item 4 re-verification
- **Issue:** The evidence handed to this plan stated the post-run `shortcuts.vdf` was 472 bytes,
  mtime `22:00:22`.
- **Fix:** A direct read at this plan's execution time found 533 bytes, mtime `Aug 2 22:10` — later
  than the gate session's own log tail (which ends 22:00:24). Recorded the file's current version,
  and recorded the most plausible non-speculative explanation (Steam's own documented
  rewrite-on-exit behavior, which this item's own operator warning already names) without asserting
  it as fact — no second `addToSteam`/`Adding shortcuts` log line exists to support a second write.
- **Files modified:** `34.5-LIVE-GATE-RERUN-2.md` (item 4 section)
- **Verification:** `ls -la` on the live file; grep of both session logs confirms only one shortcut
  operation occurred.
- **Committed in:** `19a5b4d1e`

---

**Total deviations:** 2 auto-fixed (both Rule 1 — evidence-fidelity corrections, not code changes).
**Impact on plan:** Neither correction changes any item's verdict or the overall arithmetic; both
are exactly the kind of discrepancy this plan's own re-verification mandate exists to catch (per
plan 34.5-20's "4 vs 3 timeouts" precedent), and both are now recorded in place rather than silently
adopted or silently fixed without a note.

## Issues Encountered

None beyond the two evidence discrepancies above (which are documented as deviations, not issues,
since they were anticipated by the plan's own instructions and resolved per its own procedure).

## User Setup Required

None — no external service configuration required. This plan is documentation-only against an
already-completed live session; it drove no live app itself.

## Credential Grep Check (T-34.5-G6-02)

`grep -Ec "token=|code=|Cookie|userdata"` against the finished `34.5-LIVE-GATE-RERUN-2.md` returns
non-zero. Every match was manually reviewed and confirmed to be one of: a redaction example
(`access_token=<REDACTED>` inside the evidence-handling-rule's own prose), a benign field/key NAME
rather than a value (`sessionCookie` as a dev-vault read-KEY name, which never carries the value
itself; `LaunchOptions` field name), or the literal string `userdata` used only in its already-
REDACTED path form (`userdata/<REDACTED>/config/shortcuts.vdf`). No credential value, session
identifier, access/refresh token, or unredacted Steam userdata account id appears anywhere in the
finished document.

## Next Phase Readiness

**Phase 34.5 does NOT close.** Per `34.5-CYCLE5-ROUTING.md` § "Routing by verdict," this run's
result (items 2 and 4 both FAIL) lands in the **"any of items 2,3,4,5 FAIL"** row — next action is
`/gsd-plan-phase 34.5 --gaps` (gap cycle 6). Phase 34.6's and Phase 35's dependency lines on Phase
34.5 remain untouched; they unblock only on a clean 5-of-5 PASS, which this run did not produce.

**What gap cycle 6 inherits, concretely:**
- A GOG frontend-render defect, scoped to the Library UI failing to display a runner whose backend
  data is fully and correctly persisted — a narrower, better-understood target than either of the
  first two runs' defects.
- Three item-4 structural defects with wider blast radius than the one item (F-34.5-G6-07/08/09).
- Epic (item 1) unchanged — still blocked by the same parked pre-auth 403, now confirmed to
  reproduce identically on a second live attempt.
- Items 3 and 5 (Amazon, Wine) both still needing a genuine first live attempt.
- U-34.5-01/06/10/11 still open on the keyring/Epic axis; U-34.5-02/03/04/05/07/08 still open on the
  shortcuts/Wine/GOG-UI/Amazon axis.

**Blockers/concerns for the next cycle:** none new beyond what this run's own findings already name
— see the "New findings" and "What this run nevertheless proved" sections of
`34.5-LIVE-GATE-RERUN-2.md` for the full, undiagnosed observation set gap cycle 6 should scope from.

---
*Phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc*
*Completed: 2026-08-02*
