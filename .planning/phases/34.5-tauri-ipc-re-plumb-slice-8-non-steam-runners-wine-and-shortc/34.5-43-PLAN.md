---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 43
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/gamedetails/dispatch.ts
  - src/backend/main.ts
  - src/backend/sidecar/gameDetailsFlowRegistration.ts
  - src/common/types/ipc.ts
  - src/backend/sidecar/__tests__/gameDetailsFlows.test.ts
  - src/backend/sidecar/__tests__/electronReachLedger.test.ts
  - .planning/IPC-PORT-INVENTORY.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
autonomous: true
gap_closure: true
requirements: [REQ-34.5-10, REQ-34.5-11, REQ-34.5-13]

must_haves:
  truths:
    - "Opening a game's details page under Tauri no longer emits [GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'getInstallInfo'"
    - "A getInstallInfo invoke reaches the runner named in its own argument, not a hardcoded runner"
    - "The 4th and 5th positional arguments arrive as build then branch — the order every real caller sends — never transposed"
    - "An unknown or failing runner resolves to null rather than rejecting, exactly as the Electron handler does"
    - "IPC-PORT-INVENTORY.md's arithmetic reconciles at 39 ported + 3 dropped + 16 deferred = 58, and its own gate script proves it"
    - "The electron-reach baseline is a MEASURED number recorded at execution time, not a transcribed prediction"
  artifacts:
    - path: "src/backend/gamedetails/dispatch.ts"
      provides: "the extracted getInstallInfo body both main.ts and the sidecar registration call"
      contains: "export async function getInstallInfo"
    - path: "src/backend/sidecar/gameDetailsFlowRegistration.ts"
      provides: "the sidecar ipcMain.handle registration for getInstallInfo"
      contains: "'getInstallInfo'"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py"
      provides: "a 10-check gate reconciling 39+3+16=58 with a self-test case per check"
      contains: "gameDetailsFlowRegistration.ts"
  key_links:
    - from: "src/preload/api/helpers.ts:43 makeHandlerInvoker('getInstallInfo')"
      to: "the sidecar handlerRegistry entry for 'getInstallInfo'"
      via: "ipcMain.handle in gameDetailsFlowRegistration.ts"
      pattern: "ipcMain\\.handle\\(\\s*'getInstallInfo'"
    - from: "src/backend/gamedetails/dispatch.ts getInstallInfo"
      to: "libraryManagerMap[runner].getInstallInfo"
      via: "the already-present libraryManagerMap import at dispatch.ts:52"
      pattern: "libraryManagerMap\\[runner\\]\\.getInstallInfo"
---

<objective>
Port `getInstallInfo` to the sidecar and reconcile it into `.planning/IPC-PORT-INVENTORY.md`,
`34.5-PORTED-CHANNELS.md` and `ported-channels-gate.py`. Closes **F-34.5-G6-10** and the FIRST HALF
of ledger row **U-34.5-14** — the full preload-surface audit that is the row's own retirement
condition belongs to plan 34.5-49 in wave 2, and this plan must not claim it.

Purpose: `getInstallInfo` is a real preload channel (`src/common/types/ipc.ts:217`, exposed at
`src/preload/api/helpers.ts:43` as `makeHandlerInvoker('getInstallInfo')`) registered ONLY on the
Electron side (`src/backend/main.ts:842`). Under Tauri every details-screen open emits
`[GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'getInstallInfo'` and the page does not
render — 12+ rejections across two GOG titles in one UAT session, both installed and not-installed,
so it is neither target-specific nor install-state-specific. The details screen is in turn the
blocker for UAT tests 6 (DXVK/DXVK-NVAPI toggles), 7 (`callTool`) and 14 (GOG save-sync panel), and
for SteamGridDB artwork. This single port unblocks the whole Wine/tools cluster.

Output: a `getInstallInfo` handler registered on the sidecar; the inventory, the declared-channel
document and its anti-rot gate all reconciled to 39 ported channels; a MEASURED electron-reach
baseline; and one new `34.5-UNTESTED-ITEMS.md` row for the hole this port opens.

**Live observable that would prove this — nothing else counts.** In a live `pnpm tauri:dev` session,
opening a GOG title's details page produces ZERO
`[GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'getInstallInfo'` lines in
`~/Library/Logs/GameLib/gamelib.log` and the page renders its size/version fields. A passing test
suite proves the DISPATCH wiring only. This phase's live gate has beaten a green suite THREE times —
do not record F-34.5-G6-10 as closed on unit evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE6-ROUTING.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.claude/skills/spike-findings-gamelib/SKILL.md

**Run `graphify explain "gameDetailsFlowRegistration"` and
`graphify query "getInstallInfo library manager install info"` before opening raw source.**

<interfaces>
**Three corrections to the plan brief, established by reading the live tree at planning time. Trust
these, not the brief.**

1. **`gameDetailsFlowRegistration.ts` has NO `import '../storeManagers'` first-import line.** That
   load-bearing discipline lives in `runnerAuthFlowRegistration.ts:65`,
   `downloadQueueFlowRegistration.ts:79`, `installFlowRegistration.ts:95` and
   `humbleFlowRegistration.ts:116` — never in this module. `gameDetailsFlowRegistration.ts` reaches
   `libraryManagerMap` transitively through `../gamedetails/dispatch` (which does
   `import { libraryManagerMap } from '../storeManagers'` at its own line 52). **Do not add a
   `../storeManagers` import to the registration module.** Extending `dispatch.ts` keeps that
   property and adds ZERO new module edges anywhere.

2. **`gameDetailsFlowRegistration.ts` is ALREADY one of `ENTRY_POINTS`** in
   `electronReachLedger.test.ts:47` (added by Phase 34.2 Plan 11). This plan adds no new entry point;
   what it must still do is RE-MEASURE, because the ledger's contract is a measurement, not a
   prediction (REQ-34.5-10).

3. **The declared argument order in `ipc.ts` is WRONG relative to every real caller, and tsc cannot
   see it.** `src/common/types/ipc.ts:217` declares
   `getInstallInfo(appName, runner, installPlatform, branch?, build?)`, but
   `src/frontend/helpers/index.ts:88`'s wrapper is
   `(appName, runner, installPlatform, build?, branch?)` and forwards positionally as
   `window.api.getInstallInfo(appName, runner, handleRunnersPlatforms(...), build, branch)`, and
   `src/backend/main.ts:844`'s handler unpacks
   `(event, appName, runner, installPlatform, build, branch)`. Both real ends agree on **build then
   branch**; only the type's parameter NAMES disagree. Positions 4 and 5 are both `string | undefined`,
   so `npx tsc --noEmit` is structurally incapable of catching a transposition here. A porter who
   reads `ipc.ts` and believes it will silently swap GOG's build and branch.

**The Electron handler being ported, `src/backend/main.ts:842-864`:**
- `addHandler('getInstallInfo', async (event, appName, runner, installPlatform, build, branch) => ...)`
- Body: `await libraryManagerMap[runner].getInstallInfo(appName, installPlatform, { branch, build })`
  — note the manager takes an OPTIONS OBJECT as its third argument, not two more positionals.
- `if (info === undefined) return null` — `undefined` is normalized to `null`.
- `catch (error) { logError(error, runner === 'legendary' ? LogPrefix.Legendary : LogPrefix.Gog); return null }`
  — it NEVER rejects. Every failure, including an unknown runner producing a TypeError on
  `undefined.getInstallInfo`, resolves to `null`.

**The manager-side contract, `src/common/types/game_manager.ts:84`:**
`getInstallInfo(appName: string, installPlatform: InstallPlatform, options: { branch?, build?, lang?, retries? }) => Promise<InstallInfo | undefined>`.
Implemented by `gog/library.ts:624`, `legendary/library.ts:221`, `nile/library.ts:326`,
`steam/library.ts:771`, `zoom/library.ts:210`, and a `logWarning`-and-return-undefined stub at
`sideload/library.ts:127`.

**`src/backend/gamedetails/dispatch.ts` already imports everything the extracted body needs** —
`libraryManagerMap` (line 52), `logError`/`LogPrefix` (line 59), and a `import type { ... } from 'common/types'`
block (lines 42-49) that `InstallInfo` (`common/types.ts:860`) and `InstallPlatform`
(`common/types.ts:627`) can join. No new import statement is required in any production file.

**The 4 frontend callers — the count this plan is required to state.** All four go through the single
choke point `src/frontend/helpers/index.ts:88`, so satisfying that wrapper satisfies all four:
- `src/frontend/screens/Game/GamePage/index.tsx:262` — `(appName, runner, installPlatform)`, 3 args
- `src/frontend/screens/Library/components/InstallModal/DownloadDialog/index.tsx:319` — `(appName, runner, platformToInstall, selectedBuild, branch)`, 5 args
- `src/frontend/screens/Game/ModifyInstallModal/GOG/index.tsx:143` — `(app_name, 'gog', platform, undefined, branch || gameInfo.install.branch)` — **this is the transposition detector**: it passes `undefined` for build and a real value for branch, so a swap silently sends a branch name into the build slot
- `src/frontend/components/UI/DLCList/DLC/index.tsx:53` — `(app_name, runner, mainAppInfo.install.platform)`, 3 args

There is a FIFTH, backend-internal caller — `src/backend/downloadmanager/downloadqueue.ts:223` —
which calls `libraryManagerMap[...].getInstallInfo(...)` directly and does NOT cross the IPC
boundary. It is unaffected by this plan and must not be edited.

**The gate script's current shape, `ported-channels-gate.py` (runs clean today, exit 0, self-test
9/9):** `ALL_CHANNELS` = 38 across four module lists (11/9/7/11); `SEND_CHANNELS` = 4,
`INVOKE_CHANNELS` = 34; `DROPPED_CHANNELS` = 3, `DEFERRED_CHANNELS` = 16; `PHASE_34_5_HEADING`
hard-codes the literal `(57 channels)`; `_valid_synthetic_inventory_doc()` embeds that same literal;
`ASSERTION_COUNT` is 9 and `self_test()` asserts it equals its own case count at runtime.

**`src-tauri/src/main.rs`'s `LONG_RUNNING_CHANNELS` (line 134) does NOT contain `getInstallInfo`,
and this plan MUST NOT add it.** `main.rs` is owned by plan 34.5-44 in this same wave — editing it
here is a real collision (`use_worktrees=false`, every plan runs on the main tree). The exposure is
real: `INVOKE_TIMEOUT` is 60s, `gog/library.ts:624` spawns `gogdl` over the network and
`legendary/library.ts:260` has an internal retry that re-enters `getInstallInfo`. This plan's
obligation is to RECORD the hole as a ledger row (Task 3), not to close it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract getInstallInfo into gamedetails/dispatch.ts and register it on the sidecar</name>
  <files>src/backend/gamedetails/dispatch.ts, src/backend/main.ts, src/backend/sidecar/gameDetailsFlowRegistration.ts, src/common/types/ipc.ts, src/backend/sidecar/__tests__/gameDetailsFlows.test.ts</files>
  <read_first>
    - `src/backend/main.ts` lines 820-870 — the exact handler being ported, including its `undefined`→`null` normalization and its never-reject catch. Also read lines 138-142 to see the existing `./gamedetails/dispatch` import list this task extends
    - `src/backend/gamedetails/dispatch.ts` — the WHOLE file (252 lines). Every existing export is the shape the new one must match: thin, `libraryManagerMap`-driven, no ipc knowledge
    - `src/backend/sidecar/gameDetailsFlowRegistration.ts` — the WHOLE file, especially the docstring's rule that a `send` channel registered with `ipcMain.handle` (or the reverse) fails 100% silently, and the existing `ipcMain.handle('getGameInfo', async (_event, ...args) => ...)` argument-unpacking shape at lines 161-165
    - `src/common/types/ipc.ts` lines 210-225 — the declared signature whose parameter NAMES are wrong
    - `src/frontend/helpers/index.ts` lines 85-100 — the single wrapper all four frontend callers go through; the ground truth for positional order
    - `src/common/types/game_manager.ts` lines 84-95 — the manager-side options-object contract
    - `src/backend/sidecar/__tests__/gameDetailsFlows.test.ts` lines 260-300 (the `jest.mock('backend/storeManagers', ...)` factory) and lines 524-600 (the invoke describe block and its `handlerRegistry` idiom) — the new tests must extend these, not build a parallel harness
  </read_first>
  <behavior>
    - Invoking the registered `getInstallInfo` handler with `['appName', 'gog', 'windows', 'BUILD_SENTINEL', 'BRANCH_SENTINEL']` calls the **gog** manager's `getInstallInfo` with `('appName', 'windows', { build: 'BUILD_SENTINEL', branch: 'BRANCH_SENTINEL' })` — the sentinels are distinct strings so a transposition fails the assertion rather than passing silently.
    - Invoking with `['appName', 'legendary', 'windows']` and no 4th/5th argument calls the **legendary** manager with an options object whose `build` and `branch` are both `undefined`, and does NOT dispatch to gog or steam.
    - A manager resolving `undefined` makes the handler resolve `null`, never `undefined`.
    - A manager that REJECTS makes the handler resolve `null` and never reject — parity with `main.ts`'s catch.
    - A runner string with no entry in `libraryManagerMap` makes the handler resolve `null` and never reject.
    - The channel is registered as INVOKE: `handlerRegistry.has('getInstallInfo')` is true and `listenerRegistry.has('getInstallInfo')` is false.
    - The handler no longer returns `UNPORTED_CHANNEL_MARKER`.
  </behavior>
  <action>
    Add one new exported async function `getInstallInfo` to `src/backend/gamedetails/dispatch.ts`,
    moving `main.ts:844-863`'s body verbatim. Signature:
    `getInstallInfo(appName: string, runner: Runner, installPlatform: InstallPlatform, build?: string, branch?: string): Promise<InstallInfo | null>`.
    Note the parameter order is **build then branch** — the order both real ends already use. Add
    `InstallInfo` and `InstallPlatform` to the EXISTING `import type { ... } from 'common/types'`
    block at lines 42-49; do not add a new import statement, and do not import anything from
    `../storeManagers` (line 52 already provides `libraryManagerMap`) or from `../logger` (line 59
    already provides `logError` and `LogPrefix`). Preserve all three behaviours exactly: the options
    object handed to the manager is `{ branch, build }`; `info === undefined` returns `null`; the
    catch calls `logError` with the same `runner === 'legendary' ? LogPrefix.Legendary : LogPrefix.Gog`
    selector and returns `null`. Do NOT add any new log line that echoes `appName`, `build` or
    `branch` — see T-34.5-C6-04.

    Replace `main.ts:842-864`'s inline handler body with a delegating call to the newly imported
    `getInstallInfo`, adding the name to the existing `./gamedetails/dispatch` import list that ends
    at line 142. Keep `addHandler`'s own parameter list byte-identical
    (`event, appName, runner, installPlatform, build, branch`) so the Electron path's observable
    behaviour is provably unchanged (REQ-34.5-13's behavioural-invariant clause).

    Register the channel in `src/backend/sidecar/gameDetailsFlowRegistration.ts` — **NOT** in
    `steamFlowRegistration.ts`, which plan 34.5-46 owns in wave 2, and **NOT** in any of the four
    34.5 registration modules. Use `ipcMain.handle` (INVOKE), never `ipcMain.on`: `main.ts:842` is
    `addHandler`, and this module's own docstring records that getting the kind wrong produces no
    runtime signal whatsoever. Mirror the module's existing unpacking shape exactly — an
    `async (_event: unknown, ...args: unknown[])` arrow that casts each positional out of `args`.
    Cast `args[3]` to the BUILD and `args[4]` to the BRANCH, and put a comment immediately above the
    registration recording that `ipc.ts`'s declared parameter names say the opposite and are wrong,
    citing `frontend/helpers/index.ts:88` and `main.ts:844` as the two agreeing ground truths. Update
    the module docstring's channel census from "18 game-details/settings/override channels (15 invoke
    + 3 send)" to 19 (16 invoke + 3 send), and add a `getInstallInfo` bullet to the invoke list naming
    `main.ts:842` and F-34.5-G6-10 as the reason it arrived late.

    Correct `src/common/types/ipc.ts:217`'s parameter names from `branch?, build?` to `build?, branch?`
    so the declared contract matches reality. This is a rename only — both are `string | undefined`,
    so no call site changes and no behaviour changes. It exists so the next porter cannot be misled
    the same way.

    Add tests to `src/backend/sidecar/__tests__/gameDetailsFlows.test.ts` covering every bullet in
    `<behavior>`. Pull the handler out of the REAL `handlerRegistry` after the file's existing
    `registerGameDetailsFlows()` call — do not construct a handler inline, and do not reconstruct the
    preload call site: a test that rebuilds the call shape is a replica and drifts silently. Assert on
    the manager mock's recorded call arguments, never on the handler's return value alone. Extend the
    file's existing `jest.mock('backend/storeManagers', ...)` factory with a `getInstallInfo` jest.fn
    per runner rather than adding a second factory.

    Add ONE source-text cross-check test, in the same file, that reads the real
    `src/frontend/helpers/index.ts` and the real `src/backend/sidecar/gameDetailsFlowRegistration.ts`
    off disk and asserts the wrapper forwards `build` before `branch` while the registration reads
    `args[3]` as build and `args[4]` as branch. **This assertion must be proven RED at authoring
    time**: temporarily transpose the two casts in the registration module, confirm the test fails,
    revert, confirm it passes, and record both observations verbatim in the SUMMARY. An unmatchable
    pattern passes vacuously and is worth nothing.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/gameDetailsFlows.test.ts --silent &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function getInstallInfo" src/backend/gamedetails/dispatch.ts` is exactly 1.
    - `grep -rn "ipcMain.handle(" src/backend/sidecar/ | grep getInstallInfo` returns exactly ONE match, and it is in `gameDetailsFlowRegistration.ts`.
    - `grep -rn "ipcMain.on(" src/backend/sidecar/ | grep -c getInstallInfo` is 0 — the channel is INVOKE, and a send registration would fail 100% silently.
    - `grep -n "libraryManagerMap" src/backend/main.ts | grep -c getInstallInfo` is 0 — the inline body is gone from `main.ts`, replaced by a delegating call.
    - `grep -c "storeManagers" src/backend/sidecar/gameDetailsFlowRegistration.ts` counts only comment lines: re-run as `grep -v '^\s*\*' src/backend/sidecar/gameDetailsFlowRegistration.ts | grep -v '^\s*//' | grep -c "from '../storeManagers'"` and confirm 0. No `../storeManagers` import may be added to this module.
    - `sed -n '217,223p' src/common/types/ipc.ts` shows `build?: string` declared BEFORE `branch?: string`.
    - A test invokes the handler retrieved from `handlerRegistry.get('getInstallInfo')` with the two distinct sentinels and asserts the gog manager mock received `('appName', 'windows', { build: 'BUILD_SENTINEL', branch: 'BRANCH_SENTINEL' })`. Asserted on `mock.calls`, not on the resolved value.
    - A test asserts a rejecting manager makes the handler RESOLVE `null` (use `await expect(...).resolves.toBeNull()`, not a try/catch), and a test asserts an unknown runner string does the same.
    - A test asserts `handlerRegistry.has('getInstallInfo')` is `true` AND `listenerRegistry.has('getInstallInfo')` is `false`.
    - A test asserts the handler's resolved value is not `UNPORTED_CHANNEL_MARKER`.
    - The source-text cross-check test exists, and the SUMMARY records the verbatim failure message observed when the two casts were transposed, plus the verbatim pass after reverting. A cross-check without a recorded RED proof does not satisfy this criterion.
    - `npx tsc --noEmit` exits 0.
    - The SUMMARY states in its own words that these tests prove DISPATCH only, and that F-34.5-G6-10 and `U-34.5-14` stay OPEN until the live gate observes zero `getInstallInfo` `[GAMELIB_UNPORTED_CHANNEL]` lines on a details-page open.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Regenerate the electron-reach baseline BY MEASUREMENT (REQ-34.5-10)</name>
  <files>src/backend/sidecar/__tests__/electronReachLedger.test.ts</files>
  <read_first>
    - `src/backend/sidecar/__tests__/electronReachLedger.test.ts` — the WHOLE file. Specifically: the header docstring's growth-only/subset contract; `ENTRY_POINTS` at line 44 (note line 47 — `gameDetailsFlowRegistration.ts` is ALREADY an entry point); the `BASELINE_ELECTRON_REACHING_MODULES` header comment block at lines 195-202, which records each prior phase's before/after measurement and the standing "temporary-print-statement procedure"; the `reachability sanity` test near line 503 whose floor is `> 224` and whose comment says the floor is never lowered
    - `.planning/REQUIREMENTS.md` line 787 (REQ-34.5-10) — the requirement's own words: "Every prior slice's measured baseline disagreed with its own planning-time prediction at least once, so a transcribed number is a defect, not a shortcut"
    - `34.5-13-SUMMARY.md` — the precedent for how a before/after measurement is recorded when the answer turns out to be "no change"
  </read_first>
  <action>
    Run the standing temporary-print-statement procedure the `BASELINE_ELECTRON_REACHING_MODULES`
    header comment describes: add a one-off print of `electronImportingFiles.size`,
    `visitedFiles.size` and the sorted module list; run the suite; capture the numbers; remove the
    print. Do this TWICE — once against the pre-Task-1 tree (`git stash` the Task 1 diff, or measure
    from the merge base) and once against the post-Task-1 tree — so the delta is a measured
    difference, not a single reading compared against a remembered figure.

    **The planning-time prediction is that both readings are IDENTICAL**, because Task 1 adds zero
    new import statements to any production file and `gameDetailsFlowRegistration.ts` is already an
    entry point. **This prediction is not evidence and must not be transcribed.** If the measurement
    disagrees, the measurement wins: add the new path(s) to `BASELINE_ELECTRON_REACHING_MODULES` with
    a comment naming the specific edge that introduced them, and anti-vacuity-protect any genuinely
    new set member by adding it to the `requiredModules` array in the anti-degradation test.

    Record the before/after readings in a new comment block appended to the
    `BASELINE_ELECTRON_REACHING_MODULES` header comment, in the same house style the four prior
    phases used, labelled `Phase 34.5 gap cycle 6 Plan 43 (REQ-34.5-10, 34.3 D-10 / 34.4 D-10
    standing obligation)`. State the measured electron-importing module count and the measured
    `visitedFiles.size` for both readings explicitly, even when they are equal — "measured, unchanged"
    IS the deliverable, and it is only worth something if the numbers are printed.

    Raise the `reachability sanity` floor only if the measured `visitedFiles.size` grew enough to
    leave the current `> 224` floor no longer meaningful. **Never lower it**, per that test's own
    standing instruction.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/electronReachLedger.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/sidecar/__tests__/electronReachLedger.test.ts` contains a comment block naming `Plan 43` and `REQ-34.5-10` that states BOTH a before and an after reading, each with an explicit integer for the electron-importing module count and an explicit integer for `visitedFiles.size`.
    - `grep -c "console.log" src/backend/sidecar/__tests__/electronReachLedger.test.ts` is 0 — the temporary measurement print is removed.
    - `grep -n "toBeGreaterThan(224)" src/backend/sidecar/__tests__/electronReachLedger.test.ts` still matches, OR the floor is HIGHER than 224 with a comment recording the new measured size. A lower value fails this criterion.
    - `npx jest src/backend/sidecar/__tests__/electronReachLedger.test.ts` exits 0.
    - The SUMMARY records the two readings verbatim and states explicitly whether the measurement agreed with or contradicted the plan's prediction. A SUMMARY that says "as predicted, unchanged" without printing the two numbers does not satisfy this criterion.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reconcile the inventory, the declared-channel list and the gate script to 39+3+16=58, and open the timeout ledger row</name>
  <files>.planning/IPC-PORT-INVENTORY.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/ported-channels-gate.py, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</files>
  <read_first>
    - `.planning/phases/34.5-.../ported-channels-gate.py` — the WHOLE file. Specifically: `ALL_CHANNELS` and its four module lists (lines 39-106); `SEND_CHANNELS`/`INVOKE_CHANNELS` (110-113); `MODULE_CHANNELS`/`MODULE_EXPECTED_COUNTS` (91-102); `PHASE_34_5_HEADING`'s hard-coded `(57 channels)` literal (298-301); `check_kind_correctness`'s hard-coded "expected 34" message (233-237); `_valid_synthetic_row` (557-581); `_valid_synthetic_inventory_doc`'s embedded `(57 channels)` literal (609-634); `ASSERTION_COUNT` (541-542) and `self_test`'s runtime 1:1 case-count equality check (781-786); `main()`'s final summary print (811-818)
    - `.planning/IPC-PORT-INVENTORY.md` — the `## Totals` table (19-25), the `## Phase 34.5` heading and its 57-name backtick line (113-135), and the ⚠ `F-34.5-G6-10` paragraph (160-171)
    - `.planning/phases/34.5-.../34.5-PORTED-CHANNELS.md` — `## What shipped` (34-60), `## The 38 channels` (254-301) including the per-module tally paragraph at 297-301, and the two-row `LIVE (item 4)` paragraph at 303-316 which this task must NOT disturb
    - `.planning/phases/34.5-.../34.5-UNTESTED-ITEMS.md` — the preamble's three Rules and the table shape of rows `U-34.5-14`/`U-34.5-15`, so the new row matches
    - `.planning/phases/34.5-.../34.5-CYCLE6-ROUTING.md` § D-CYCLE6-C — the fence this task must stay behind: it buckets ONE channel, it does not perform the preload-surface audit
  </read_first>
  <action>
    Extend `ported-channels-gate.py` DELIBERATELY. Never weaken an assertion to make it pass —
    deleting or loosening a check here defeats the reason the file exists, and the script's own
    docstring says so. Concretely: add a fifth module list containing `getInstallInfo` keyed to
    `gameDetailsFlowRegistration.ts` with an expected count of 1; assert its length; make
    `ALL_CHANNELS` 39 and update both the length and the duplicate-set assertions; `SEND_CHANNELS`
    stays 4 so `INVOKE_CHANNELS` becomes 35 — update that assertion AND
    `check_kind_correctness`'s hard-coded "expected 34" message string, which is not driven by the
    constant and would otherwise become a lie. Change `PHASE_34_5_HEADING`'s literal from
    `(57 channels)` to `(58 channels)`, and the same literal inside
    `_valid_synthetic_inventory_doc()`. Give `_valid_synthetic_row` a `getInstallInfo` branch
    producing the `gameDetailsFlowRegistration.ts` module, a `unit` proof level, and a Riders cell
    containing `F-34.5-G6-10`.

    Add a TENTH check, `check_late_discovery_provenance`, asserting that the `getInstallInfo` row
    exists, is attributed to `gameDetailsFlowRegistration.ts`, and carries the literal `F-34.5-G6-10`
    in its Riders cell. Its purpose is anti-rot: without it a future editor can quietly absorb this
    row into the original 38 and erase the record that `IPC-PORT-INVENTORY.md` was not exhaustive —
    which is `U-34.5-14`'s entire subject. Register it in `SINGLE_TEXT_ASSERTIONS`, bump
    `ASSERTION_COUNT` to 10, and add exactly ONE new self-test case that strips `F-34.5-G6-10` from
    the synthetic row and confirms the check rejects it. The script's own runtime 1:1 equality between
    case count and assertion count must still hold — do not satisfy it by editing the equality, only
    by adding the case.

    Update `.planning/IPC-PORT-INVENTORY.md`. Change the Phase 34.5 heading to `(58 channels)`; insert
    `` `getInstallInfo` `` into that section's backtick channel-list line (between `getGogdlVersion`
    and `getLatestEosOverlayVersion`); change the "38 ported + 3 dropped + 16 moved" reconciliation
    sentence to 39 + 3 + 16 = 58 and add a dated bullet explaining that the 39th was
    F-34.5-G6-10's late-discovered channel, ported by plan 34.5-43 into
    `gameDetailsFlowRegistration.ts` because it is a game-details channel by concern, not one of this
    slice's four runner modules. Raise `## Totals`' "Unique channels" from 210 to 211 and restate the
    ported/unported split consistently, with a one-line note that the delta is the previously
    unlisted channel, not a newly created one.

    **Do NOT delete or soften the ⚠ `F-34.5-G6-10` paragraph.** Amend it in place to say the channel
    is now ported and bucketed, that it was ONE previously unlisted channel of an UNKNOWN total, and
    that the audit establishing that total is plan 34.5-49's (D-CYCLE6-C) and remains a Phase 35
    precondition. Deleting the warning because its example got fixed would recreate exactly the blind
    spot `U-34.5-14` exists to record.

    Update `34.5-PORTED-CHANNELS.md`: retitle `## The 38 channels` to `## The 39 channels`; add the
    `getInstallInfo` row (kind `sidecar invoke`, module `gameDetailsFlowRegistration.ts`, proof level
    `unit`, Riders naming F-34.5-G6-10, the 12+ live rejections across two GOG titles, the four
    frontend call sites, `main.ts:842` as the Electron original, and the fact that it was absent from
    every inventory bucket); update `## What shipped`'s "38 of 57" and the per-module tally paragraph
    to 11+9+7+11+1 = 39 of 58. Leave the `LIVE (item 4)` two-row paragraph untouched — this channel
    carries no live-gate item, and check 9 will fail if it is credited with one.

    Add ONE new row to `34.5-UNTESTED-ITEMS.md` per **Ledger Rule 2** (a plan creating a new untested
    hole adds its row in the same commit). Number it `U-34.5-16`. Subject: whether a real
    `getInstallInfo` invoke completes inside `src-tauri/src/main.rs`'s 60s `INVOKE_TIMEOUT` for a GOG
    or Epic title under the sidecar. Evidence cell: the channel is absent from `LONG_RUNNING_CHANNELS`
    (`main.rs:134`), `gog/library.ts:624` spawns `gogdl` over the network, and
    `legendary/library.ts:260` re-enters `getInstallInfo` on a retry; a timeout returns
    `Err("sidecar invoke timed out")` at 60s AND the late real response is dropped by the reader
    thread as an unknown id, so the outcome can never arrive — the same shape F-34.5-G6-02 recorded
    for `oauthCaptureLogin`. Retirement condition: a live details-page open on a real GOG title that
    renders its size/version fields with no `sidecar invoke timed out` line for this channel in the
    preserved log. Status: OPEN, created by plan 34.5-43. State in the row that this plan
    deliberately did NOT edit `main.rs`, naming plan 34.5-44's same-wave ownership of that file as
    the reason.
  </action>
  <verify>
    <automated>bash -c 'cd .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc &amp;&amp; python3 ported-channels-gate.py &amp;&amp; python3 ported-channels-gate.py --self-test'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 ported-channels-gate.py` exits 0, and its final summary line says 39 channels, per-module counts 11/9/7/11/1, and 39+3+16=58 — the print must not still claim 38 or 57.
    - `python3 ported-channels-gate.py --self-test` exits 0 and prints `All 10 check(s) proved capable of rejecting the input they exist to reject (10 self-test case(s), 1:1)`.
    - `grep -c "ASSERTION_COUNT == 10" ported-channels-gate.py` is at least 1, and no `assert` statement present before this task was deleted — confirm by `git diff` showing only additions and literal-value updates in the assertion registry, no removals.
    - The new self-test case is proven to reject: temporarily strip `F-34.5-G6-10` from the real `getInstallInfo` row in `34.5-PORTED-CHANNELS.md`, run `python3 ported-channels-gate.py`, confirm it exits non-zero with the provenance failure message, restore, confirm exit 0. Record both verbatim outputs in the SUMMARY.
    - `grep -c "58 channels" .planning/IPC-PORT-INVENTORY.md` is at least 1; `grep -c "57 channels" .planning/IPC-PORT-INVENTORY.md` is 0.
    - `grep -c "getInstallInfo" .planning/IPC-PORT-INVENTORY.md` is at least 2 (the channel-list line plus the amended ⚠ paragraph).
    - The ⚠ paragraph still contains the literal strings `UNKNOWN` and `must not be assumed to be one`, and now also names plan `34.5-49`. A diff that deletes the paragraph fails this criterion.
    - `grep -c "## The 39 channels" 34.5-PORTED-CHANNELS.md` is 1; `grep -c "## The 38 channels" 34.5-PORTED-CHANNELS.md` is 0.
    - `grep -c "U-34.5-16" 34.5-UNTESTED-ITEMS.md` is at least 1, and the row's Status cell contains `OPEN`.
    - `git diff --name-only src-tauri/` is EMPTY — this plan touches no Rust file, and `main.rs` belongs to plan 34.5-44 this wave.
    - The SUMMARY states explicitly that this task buckets ONE channel and does NOT perform D-CYCLE6-C's preload-surface audit, which is plan 34.5-49's, and that `U-34.5-14` therefore stays OPEN.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar invoke | A renderer-supplied `runner` string indexes `libraryManagerMap`, and a renderer-supplied `appName`/`build`/`branch` are forwarded into a CLI-spawning manager |
| sidecar → runner CLI (`gogdl`/`legendary`/`nile`) | `appName`, `installPlatform`, `build` and `branch` become process arguments and network request parameters |
| sidecar → `gamelib.log` → gate/finding documents | Anything logged on this path gets pasted into public-fork planning artifacts |
| planning documents → Phase 35 completion gate | `IPC-PORT-INVENTORY.md`'s arithmetic is the gate that decides whether the Electron cutover may run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-C6-01 | Tampering | `libraryManagerMap[runner]` indexed by a renderer-supplied string in the extracted `dispatch.ts` body | accept | `main.ts:856`'s existing catch already resolves `null` for every failure including a prototype-chain key like `constructor` producing a TypeError on `undefined.getInstallInfo`. Adding a membership check here would CHANGE Electron's observable behaviour and violate REQ-34.5-13's behavioural invariant. Task 1 pins the accepted behaviour with a test asserting an unknown runner RESOLVES `null` and never rejects |
| T-34.5-C6-02 | Elevation of privilege | the sidecar registration widening the reachable surface beyond the Electron handler | mitigate | The sidecar handler must be a byte-equivalent delegation to the SAME extracted function `main.ts` calls — one shared body, not two. Acceptance criteria require exactly one `export async function getInstallInfo` in `dispatch.ts` and zero `libraryManagerMap`-bearing `getInstallInfo` lines left in `main.ts`, so the two builds cannot drift apart into differing exposure |
| T-34.5-C6-03 | Information disclosure | the `InstallInfo` payload crossing the IPC boundary | accept | Verified at planning time: `GogInstallInfo` is `{ game: { app_name, launch_options, owned_dlc, title, version, branches, buildId }, manifest }` (`common/types/gog.ts:6-25`) — no on-disk install path, no token, no account id. `InstallInfo`'s other members (`Legendary`/`Nile`/`Zoom`) are the same manifest-and-metadata shape. The payload is identical to what the Electron build already returns to the same renderer |
| T-34.5-C6-04 | Information disclosure | a new log line echoing `appName`, `build` or `branch` into `gamelib.log` | mitigate | `gog/library.ts:638` composes its cache key from `privateBranchesStore.get(appName)` — a **private-branch PASSWORD** — so anything in this call's neighbourhood is adjacent to a secret. Task 1 forbids adding any log line echoing the arguments; only `main.ts:857`'s pre-existing `logError(error, ...)` is carried over, unchanged, for parity |
| T-34.5-C6-05 | Information disclosure | `privateBranchPassword` embedded in `installInfoStore`'s on-disk cache key (`gog/library.ts:638`) | accept | Pre-existing Electron behaviour, inherited unchanged; out of this plan's scope to alter, and changing it would break cache-key parity between the two builds. Recorded here so it is not rediscovered as new. The gate operator must treat `installInfoStore` contents as secret-bearing when preserving logs |
| T-34.5-C6-06 | Denial of service | `getInstallInfo` exceeding `main.rs`'s 60s `INVOKE_TIMEOUT` with no `LONG_RUNNING_CHANNELS` exemption | transfer | Cannot be closed here — `src-tauri/src/main.rs` is plan 34.5-44's this wave. Task 3 transfers it to the record as new ledger row `U-34.5-16`, retiring only on a live observation. A 60s timeout also DROPS the late real response as an unknown id, so the outcome can never arrive — the same shape F-34.5-G6-02 recorded for `oauthCaptureLogin` |
| T-34.5-C6-07 | Repudiation | the ⚠ `F-34.5-G6-10` inventory warning being deleted because its named example got fixed | mitigate | Task 3 forbids deletion and requires the amended paragraph to retain the literals `UNKNOWN` and `must not be assumed to be one`. New gate check 10 independently pins `F-34.5-G6-10` into the `getInstallInfo` row's Riders cell, with its own self-test case proving it rejects the stripped input |
| T-34.5-C6-08 | Tampering | a future editor weakening a gate assertion to make 39+3+16 pass | mitigate | Task 3's acceptance criteria require `git diff` on `ported-channels-gate.py` to show additions and literal-value updates only, no assertion removals, and require the script's own runtime 1:1 case-count equality to be satisfied by ADDING a case, never by editing the equality |
| T-34.5-C6-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages; `34.5-RESEARCH.md`'s `## Package Legitimacy Audit` reads "Not applicable — no new packages" and stays true. Any install would require slopcheck plus a blocking human checkpoint before the install command runs |
</threat_model>

<verification>
- `npm run test:ci` exits 0; the verbatim `Tests:` line is recorded in the SUMMARY and any delta against the phase's last recorded count (`181/181` suites, `3553/3553` tests, per `34.5-PORTED-CHANNELS.md`'s automated-proof section) is explained rather than absorbed.
- `npx tsc --noEmit` exits 0.
- `python3 ported-channels-gate.py` and `python3 ported-channels-gate.py --self-test` both exit 0, and the self-test reports 10 cases against 10 checks.
- `npx jest src/backend/__tests__/longRunningChannels.test.ts` still exits 0 — this plan adds NO channel to `LONG_RUNNING_CHANNELS`, so that test's exact-set assertion must be untouched. A failure here means `main.rs` was edited in violation of plan 34.5-44's wave-1 ownership.
- `npx jest src/backend/sidecar/__tests__/electronUntouched.test.ts src/backend/sidecar/__tests__/gameDetailsImportGate.test.ts` exits 0 — SEAM Invariant A and the curated-import gate are unweakened.
- `git diff --name-only` lists no file outside this plan's `files_modified`, and specifically zero paths under `src-tauri/`, zero `steamFlowRegistration.ts` (plan 34.5-46), zero `src/backend/protocol.ts` or `src/backend/sidecar/bootstrap.ts` (plan 34.5-44), and zero frontend files — all four frontend callers are satisfied through the existing `frontend/helpers/index.ts:88` choke point without editing any of them.
- Electron parity (REQ-34.5-13): `npm start` boots and `src/backend/main.ts`'s `addHandler('getInstallInfo', ...)` still exists with its original six-parameter signature, now delegating. Confirmed by reading the diff hunk, not inferred from a green suite.
</verification>

<success_criteria>
`getInstallInfo` is registered exactly once on the sidecar, as an INVOKE channel, in
`gameDetailsFlowRegistration.ts`, sharing one extracted body with the Electron handler so the two
builds cannot drift; its 4th and 5th arguments are build-then-branch, pinned by a cross-check proven
RED against a transposed input; the electron-reach baseline carries two printed measurements rather
than a prediction; `39 + 3 + 16 = 58` reconciles across `IPC-PORT-INVENTORY.md`,
`34.5-PORTED-CHANNELS.md` and a 10-check gate whose every assertion is proven to reject its bad
input; and the unknown extent of the inventory's incompleteness is still recorded as UNKNOWN, with
`U-34.5-14` OPEN pending plan 34.5-49's audit and new row `U-34.5-16` OPEN for the 60s-timeout hole.

F-34.5-G6-10 does NOT close on this plan. It closes when a live session opens a GOG title's details
page with zero `[GAMELIB_UNPORTED_CHANNEL] No handler registered for channel 'getInstallInfo'` lines
in a preserved `gamelib.log`.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-43-SUMMARY.md` when done
</output>
