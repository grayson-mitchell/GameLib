---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 09
type: execute
wave: 3
depends_on: ["34.5-05"]
files_modified:
  - src/backend/sidecar/wineToolsFlowRegistration.ts
  - src/backend/sidecar/__tests__/wineToolsFlows.test.ts
autonomous: true
requirements: [REQ-34.5-03]

must_haves:
  truths:
    - "The three DXVK/VKD3D toggle channels complete the 9-channel Wine cluster: `toggleDXVK`, `toggleDXVKNVAPI`, `toggleVKD3D`."
    - "D-13: the platform guards are ported AS-IS and trusted. On macOS `tools/index.ts:228` already permits `dxvk` ONLY and already excludes VKD3D; `:245` already maps to `dxvk-macOS`; `:219` already recognises `-DXMT` DirectX→Metal builds. Re-deciding platform scope would mean re-implementing correct upstream behaviour."
    - "RESEARCH Correction 1 / Pitfall 3, VERIFY DO NOT REBUILD: the one dialog reachable from this cluster is `showDialogBoxModalAuto` at `tools/index.ts:137` (`installOrUpdateTool`, reached via `DXVK.installRemove` → `DXVK.getLatest()`). It is ALREADY SAFE — its primary path is the fire-and-forget `sendFrontendMessage('showDialog', …)` push, and its `catch` fallback lands on `electronStub.dialog`, which is Rust-backed and non-throwing. D-15's original citation (`tools/index.ts:794`) is WRONG: that line has no dialog at all and sits in `Winetricks.checkDependencies`, a DEFERRED cluster unreachable from any of the 38 channels."
    - "No task in this plan may 'fix' `showDialogBoxModalAuto`, add a safe-decline sentinel to it, or add a new `dialog_open`-style Rust round-trip. The existing wiring already satisfies D-15's underlying safety concern; the deliverable is a recorded verification, not a change."
    - "All three are `addHandler`/invoke, completing a 9-for-9 handle cluster with zero send channels."
  artifacts:
    - path: "src/backend/sidecar/wineToolsFlowRegistration.ts"
      provides: "toggleDXVK, toggleDXVKNVAPI, toggleVKD3D — the cluster's final three channels"
      contains: "toggleVKD3D"
    - path: "src/backend/sidecar/__tests__/wineToolsFlows.test.ts"
      provides: "Extended bidirectional coverage to all 9 Wine channels"
  key_links:
    - from: "src/backend/sidecar/wineToolsFlowRegistration.ts"
      to: "src/backend/tools/index.ts DXVK.installRemove"
      via: "curated named import of DXVK, never tools/ipc_handler.ts"
      pattern: "DXVK.installRemove"
---

<objective>
Complete the Wine cluster with the three DXVK/VKD3D toggles, and put on the record — with evidence,
not assertion — that the one dialog they can reach is already safe.

Purpose: these three are the only 34.5 channels that reach a dialog call site at all, and CONTEXT.md
D-15 pointed at the wrong one. Research already verified the correct site is non-throwing; this plan
confirms that against source and declines to build a fix nothing needs.

Output: nine of nine Wine channels registered, and a verification record that closes D-15 for this
phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-RESEARCH.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-05-SUMMARY.md

<interfaces>
Electron registration ground truth (verified by grep, src/backend/main.ts):
  L999   addHandler('toggleDXVK',      async (event, { appName, action }) => GameConfig.get(appName).getSettings().then(async (gameSettings) => DXVK.installRemove(gameSettings, 'dxvk', action)))
  L1007  addHandler('toggleDXVKNVAPI', … DXVK.installRemove(gameSettings, 'dxvk-nvapi', action))
  L1015  addHandler('toggleVKD3D',     … DXVK.installRemove(gameSettings, 'vkd3d', action))

Curated import targets:
  src/backend/tools/index.ts:162 → `DXVK`   (also exports installOrUpdateTool at :72, Winetricks at :528, runWineCommandOnGame at :874)
  src/backend/config.ts → `GameConfig`

The dialog chain to VERIFY (not to change):
  toggleDXVK/NVAPI/VKD3D → DXVK.installRemove → (cache miss) DXVK.getLatest()
    → Promise.all(tools.map(installOrUpdateTool)) → tools/index.ts:137 showDialogBoxModalAuto(…)
    → primary: sendFrontendMessage('showDialog', …)   (fire-and-forget, cannot reject)
    → catch fallback: electronStub.dialog.showErrorBox / showMessageBox  (electronStub.ts ~L320-382,
      both non-throwing async functions wired to RUST_DIALOG_MESSAGE with their own internal .catch())
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the three DXVK/VKD3D toggle channels</name>
  <files>src/backend/sidecar/wineToolsFlowRegistration.ts</files>
  <read_first>
    - src/backend/sidecar/wineToolsFlowRegistration.ts (as written by plan 34.5-05 — the six existing registrations, the D-13 guard comment, and the module docstring's channel list)
    - src/backend/main.ts L995-1022 (the three handler bodies verbatim, including the `GameConfig.get(appName).getSettings().then(…)` shape)
    - src/backend/tools/index.ts L162-260 (`DXVK.installRemove` and `DXVK.getLatest`, plus the platform guards at :219 `-DXMT`, :228 macOS-permits-dxvk-only, :245 `dxvk-macOS` — read them, cite them, change nothing)
    - .planning/phases/34.5-.../34.5-RESEARCH.md § "Corrections to CONTEXT.md" Correction 1 and § "Common Pitfalls" Pitfall 3
  </read_first>
  <action>
    Add three `ipcMain.handle` registrations to `registerWineToolsFlows()`, porting each Electron
    body verbatim: unpack `{ appName, action }` from `args[0]`, resolve
    `GameConfig.get(appName).getSettings()`, and call `DXVK.installRemove(gameSettings, <tool>,
    action)` with `'dxvk'`, `'dxvk-nvapi'` and `'vkd3d'` respectively.

    Import `DXVK` by name from `../tools` and `GameConfig` from `../config`. Never import
    `tools/ipc_handler.ts` — it also registers `runWineCommandForGame` and the three DEFERRED
    winetricks channels.

    Above `toggleVKD3D`, add a comment recording D-13 concretely: on macOS the guard at
    `tools/index.ts:228` already permits `dxvk` only and already excludes VKD3D, so this channel is
    registered unconditionally and the platform decision stays where upstream Heroic made it. Do NOT
    add a `process.platform` branch in the registration module — that would duplicate and eventually
    diverge from the guard.
  </action>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle(" src/backend/sidecar/wineToolsFlowRegistration.ts` returns 9.
    - `grep -c "ipcMain.on(" src/backend/sidecar/wineToolsFlowRegistration.ts` returns 0.
    - `grep -c "'dxvk-nvapi'" src/backend/sidecar/wineToolsFlowRegistration.ts` returns 1; same for `'vkd3d'`.
    - `grep -c "process.platform\|isMac\|isLinux" src/backend/sidecar/wineToolsFlowRegistration.ts` counts only comment lines — no platform branch is introduced in the registration module.
    - `grep "^import" src/backend/sidecar/wineToolsFlowRegistration.ts | grep -c ipc_handler` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All nine Wine channels are registered, and the platform decision stays exactly where upstream made it.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the reachable dialog is already safe, and record the evidence</name>
  <files>src/backend/sidecar/wineToolsFlowRegistration.ts, src/backend/sidecar/__tests__/wineToolsFlows.test.ts</files>
  <read_first>
    - src/backend/tools/index.ts L72-143 (`installOrUpdateTool` in full, including the `showDialogBoxModalAuto` call at L137 and the failure branch that reaches it)
    - src/backend/tools/index.ts L776-799 (`Winetricks.checkDependencies` — confirm for yourself that L794 calls only `appendMessage` and `logWarning`, has no dialog of any kind, and that its only caller is `Winetricks.runWithArgs`; this is D-15's mis-citation and confirming it is part of this task's deliverable)
    - src/backend/dialog/dialog.ts (`showDialogBoxModalAuto` — the `sendFrontendMessage('showDialog', …)` primary path and the `catch` fallback)
    - src/backend/sidecar/electronStub.ts § dialog (~L320-382 — `showErrorBox` and `showMessageBox`, their `RUST_DIALOG_MESSAGE` wiring and their internal `.catch()` defaults)
    - .claude/skills/spike-findings-gamelib/SKILL.md and the project's `sidecar-dialog-reject-crashes` rule (the general landmine D-15 invokes — real in general, and this task establishes it does not apply here)
  </read_first>
  <action>
    This task produces EVIDENCE and a comment, not a behavioural change. Do not modify
    `dialog/dialog.ts`, `tools/index.ts` or `electronStub.ts`.

    Trace the chain by reading source and record in the SUMMARY, with file:line citations for each
    hop: `toggleDXVK`/`toggleDXVKNVAPI`/`toggleVKD3D` → `DXVK.installRemove` → `DXVK.getLatest()` →
    `installOrUpdateTool` → `showDialogBoxModalAuto` (`tools/index.ts:137`) →
    `sendFrontendMessage('showDialog', …)` and, on the catch branch, `electronStub.dialog.*`. State
    explicitly whether each of `showErrorBox` and `showMessageBox` can reject, quoting the code that
    makes them non-throwing.

    Record the D-15 correction in the SUMMARY as a verified finding: `tools/index.ts:794` calls only
    `appendMessage` and `logWarning`, has no dialog, and lives in `Winetricks.checkDependencies`,
    whose only caller is `Winetricks.runWithArgs` — the winetricks cluster, DEFERRED to Phase 34.6
    and unreachable from any of this phase's 38 channels.

    Add a comment in `wineToolsFlowRegistration.ts` above the toggle registrations summarising the
    finding in two or three lines and pointing at this plan's SUMMARY for the full trace, so a future
    reader does not re-derive it or "fix" a safe path.

    Add a test to `wineToolsFlows.test.ts` that pins the safety property rather than the trace: drive
    `showDialogBoxModalAuto` with the frontend push mocked to REJECT, and assert the returned promise
    does not reject and no unhandled rejection escapes. This is the property D-15 actually cares
    about, and pinning it means a future change to the dialog path fails a test instead of crashing
    the app.
  </action>
  <acceptance_criteria>
    - The SUMMARY contains the full chain with a file:line citation for every hop, and an explicit statement of whether `showErrorBox`/`showMessageBox` can reject.
    - The SUMMARY states the D-15 correction as verified by direct read, naming what `tools/index.ts:794` actually calls.
    - `git diff --name-only` does NOT list `src/backend/dialog/dialog.ts`, `src/backend/tools/index.ts` or `src/backend/sidecar/electronStub.ts` — no fix was built for a path that does not need one.
    - `grep -rc "dialog_open" src/common/types/sidecarTransport.ts` is unchanged — no new Rust dialog round-trip was added.
    - A test exists that fails if `showDialogBoxModalAuto` ever propagates a rejection.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/wineToolsFlows.test.ts</automated>
  </verify>
  <done>The reachable dialog's safety is proven from source and pinned by a test, D-15's mis-citation is corrected on the record, and no unnecessary fix was built.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the Wine cluster's registration-kind coverage to all nine channels</name>
  <files>src/backend/sidecar/__tests__/wineToolsFlows.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/wineToolsFlows.test.ts (as written by plan 34.5-05 — EXTEND its channel arrays, do not restart the file)
    - src/backend/main.ts L999, L1007, L1015 (the kind ground truth for the three new channels — all `addHandler`)
    - src/backend/sidecar/wineToolsFlowRegistration.ts (as completed by Task 1)
  </read_first>
  <action>
    Extend the existing `HANDLE_CHANNELS` array with `toggleDXVK`, `toggleDXVKNVAPI` and
    `toggleVKD3D` so the bidirectional assertions now cover all nine. Leave `SEND_CHANNELS` empty and
    add an explicit assertion that it is empty, commented as a deliberate property of this cluster —
    9 of 9 invoke, zero send.

    Extend the existing curated-import guard's negative list so it now also asserts that the three
    DEFERRED winetricks channel names remain absent from both registries after all nine
    registrations, not just after the first six.

    Add a behaviour test for one toggle: mock `DXVK.installRemove` and assert `toggleVKD3D` forwards
    the resolved game settings and the literal tool name `'vkd3d'`, proving the three registrations
    are not copy-paste-identical in the wrong direction (a wrong tool literal is silent and would
    install the wrong component).
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/wineToolsFlows.test.ts` exits 0.
    - The suite asserts both directions for all 9 channels (18 assertions minimum) and asserts `SEND_CHANNELS` is empty.
    - A test asserts `toggleVKD3D` passes the literal `'vkd3d'` and would fail if it passed `'dxvk'`.
    - `winetricksAvailable`, `winetricksInstall`, `winetricksInstalled` and `runWineCommandForGame` are asserted absent from both registries.
    - `cd src/backend && npx jest sidecar/__tests__/runnerSliceRegistration.test.ts` exits 0, and `git diff --name-only` does not list it.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/wineToolsFlows.test.ts sidecar/__tests__/runnerSliceRegistration.test.ts</automated>
  </verify>
  <done>All nine Wine channels are kind-pinned in both directions, and each toggle is proven to carry its own tool literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → toggle payload (`appName`, `action`) | Selects which component is installed or removed into a game's Wine prefix |
| remote release feed → local DLL copy | `installOrUpdateTool` downloads and unpacks DXVK/VKD3D/DXMT archives into the prefix |
| sidecar → dialog surface | A fire-and-forget push whose failure path must not become an unhandled rejection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-30 | Denial of Service | a dialog rejection in a fire-and-forget path crashing the app | mitigate | Verified from source that `showDialogBoxModalAuto`'s primary path cannot reject and its fallback lands on non-throwing Rust-backed stubs; pinned by a test that drives the push to reject and asserts nothing escapes. No new code — the existing wiring already satisfies the concern (research Correction 1 / Pitfall 3). |
| T-34.5-31 | Tampering | the wrong tool literal installed into a prefix | mitigate | A behaviour test asserts `toggleVKD3D` forwards `'vkd3d'` and fails on `'dxvk'` — a copy-paste error here is otherwise completely silent. |
| T-34.5-32 | Elevation of Privilege | platform guards re-decided or duplicated in the registration module | mitigate | D-13: guards cited by line in a comment; `grep` asserts no `process.platform`/`isMac`/`isLinux` branch was introduced outside comments. |
| T-34.5-33 | Tampering | a DEFERRED winetricks channel registered via `tools/ipc_handler.ts` | mitigate | Curated named imports only; the negative registry assertion now covers all nine registrations. |
| T-34.5-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `cd src/backend && npx jest sidecar/__tests__/wineToolsFlows.test.ts` exits 0 with 9-channel coverage.
- `git diff --name-only` lists neither `dialog/dialog.ts`, `tools/index.ts` nor `electronStub.ts`.
- `grep -c "ipcMain.handle(" src/backend/sidecar/wineToolsFlowRegistration.ts` returns 9.
</verification>

<success_criteria>
The Wine cluster is complete at nine channels, the one reachable dialog is proven safe by reading
rather than assumed unsafe by inheritance, and D-15's incorrect citation is corrected on the record
without a single unnecessary line of remediation code.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-09-SUMMARY.md` when done
</output>
