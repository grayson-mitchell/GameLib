---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 56
executed_by: 34.5-59-PLAN.md
type: live-gate
status: authored
blocking: true
created: 2026-08-14
cycle: 7
verdict: null
items_total: 4
items_passed: null
items_failed: null
items_blocked: null
items_not_attempted: null
supersedes: 34.5-LIVE-GATE-RERUN-3.md
notes: |
  Authored 2026-08-14 by plan 34.5-56. Every RESULT slot in this document is EMPTY at authoring
  time and is filled ONLY by plan 34.5-59, never by this authoring plan (Decision D-E: the plan
  that authors a gate contract may never also run it).
---

# Phase 34.5 Plan 56 — Blocking Live-Gate RE-RUN 4 (fifth gate; four items)

This document is a **CONTRACT**, written before any live work begins — the same discipline plans
34.5-03, 34.5-19, 34.5-40 and 34.5-50 followed for runs 1 through 4. It is the **fifth** such
contract for this phase's blocking live gate.

**`34.5-LIVE-GATE.md`, `34.5-LIVE-GATE-RERUN.md`, `34.5-LIVE-GATE-RERUN-2.md` and
`34.5-LIVE-GATE-RERUN-3.md` are NOT overwritten, NOT edited and NOT deleted.** Each is the
completed record of its own run (0/5, 0/5, 0 PASS/2 FAIL/1 BLOCKED/2 NOT ATTEMPTED,
2 PASS/1 FAIL/0 BLOCKED/1 NOT ATTEMPTED). This document `supersedes: 34.5-LIVE-GATE-RERUN-3.md` in
its own frontmatter and in this prose, and changes not one byte of any prior gate document.

**This plan (34.5-56) runs nothing live.** Plan 34.5-59 executes this contract. Every `**Result:**`
slot below is EMPTY at authoring time and stays that way until this document is next edited by
34.5-59 (Decision D-E, restated below in full).

---

## PRECONDITION 0 — the HALT-GATE. Read this before anything else in this document.

**`34.5-CYCLE7-REVIEW.md` (plan 34.5-57, created 2026-08-14) carries `halt_gate: true` on blocker
`F-34.5-G6-22`.** Independently re-confirmed against current source at THIS document's own
authoring time (2026-08-14): `NileUser.login()` (`src/backend/storeManagers/nile/user.ts:122`)
dispatches through `runRunnerCommand`, which reaches the shared `callRunner()`
(`src/backend/launcher.ts`). `callRunner()` builds `safeCommand` via `getRunnerCallWithoutCredentials`
(`launcher.ts:1697-1704`) and then unconditionally logs it: `logInfo([prefix, safeCommand],
runner.logPrefix)` at `launcher.ts:1703-1704`. `getRunnerCallWithoutCredentials`
(`launcher.ts:1877-1926`) redacts only the array member immediately following an exact `--code` or
`--token` string (`for (const sensitiveArg of ['--code', '--token'])`, `launcher.ts:1888`,
re-confirmed verbatim at this authoring). **`--code-verifier` is a different array member and is
never matched by that loop** — the raw PKCE `code_verifier` value is written to `gamelib.log` in
cleartext on every real `NileUser.login()` call, success or failure, through a call site plan
34.5-53's own redaction never touched (`nileCredentialRedaction.test.ts`'s Group A `jest.mock`s
`runRunnerCommand` entirely, so this exact path is structurally untested by that suite).

**This gate CANNOT launch while this condition holds.** Any item that would mint a real OAuth
credential over this seam — item 2 (Amazon/`nile` login) specifically, and by extension the whole
run, since D-08 requires all four items in one clean session — is BLOCKED by construction until
the leak is closed.

**The mechanical check plan 34.5-59 must run, and record the raw output of, before any launch:**

```
grep -n "halt_gate:" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CYCLE7-REVIEW.md
```

If this reads `halt_gate: true`, **STOP. Do not proceed to precondition 1. Do not launch the app.**
Report back that this gate is still blocked on `F-34.5-G6-22` and route to whichever plan was
inserted to fix `launcher.ts:1888` (per `34.5-CYCLE7-REVIEW.md`'s own routing candidate: a new plan
between 34.5-57 and 34.5-58, or folded into 34.5-58's own scope). Only if the grep reads
`halt_gate: false`, **with a fix commit hash recorded in that same file** (per that document's own
halt-decision text — "this file is amended to `halt_gate: false` with the fix's commit hash
recorded here"), may this contract's precondition 1 begin. Record the commit hash found there in
this precondition's own RESULT slot when 34.5-59 fills it in.

**Result:** _EMPTY — filled only by plan 34.5-59._

---

## Why this run exists

Four runs, four FAILs, each on a different layer, restated for the reader of a fifth attempt:

| Run | Document | Verdict | Layer that failed |
|---|---|---|---|
| 1 | `34.5-LIVE-GATE.md` | 0/5 | Sidecar spawn — `spawn ./{legendary,gogdl,nile} ENOENT`, `publicDir` unresolved |
| 2 | `34.5-LIVE-GATE-RERUN.md` | 0/5 | OAuth capture reached the backend for GOG/Amazon but nothing consumed it into a completed, UI-visible login |
| 3 | `34.5-LIVE-GATE-RERUN-2.md` | 0 PASS/2 FAIL/1 BLOCKED/2 NOT ATTEMPTED | GOG's backend chain fully succeeded while the Library UI rendered nothing — a THIRD, downstream layer |
| 4 | `34.5-LIVE-GATE-RERUN-3.md` | 2 PASS/1 FAIL/0 BLOCKED/1 NOT ATTEMPTED | A shipped anti-phishing feature invisible on the platform (AppKit sheets render no title bar), plus an action (the DXVK toggle) never actually performed — a stale ON value absorbed the click that never happened |

**Cycle 7's own fixes, by plan number, and what each CLAIMS to close — none of it is live-proven
until this gate runs:**

| Plan | Claims to close |
|---|---|
| 34.5-52 | `F-34.5-G6-16` (code half): in-page origin banner (`login_origin_banner_script`/`_update_script`) injected on macOS alongside the mandated cancel strip — the substitute for the title bar an AppKit sheet cannot render |
| 34.5-53 | `F-34.5-G6-17`/`F-34.5-G6-20` (code half): both nile credential-logging call sites (`NileUser.login`'s `logDebug`, `NileUser.getLoginData`'s `logInfo`) now route through `redactNileLoginData`/`redactNileRegisterData` — but see Precondition 0 above: a THIRD, unfixed site (`callRunner()`) still leaks the same value class |
| 34.5-54 | `F-34.5-G6-19`: corrects `F-34.5-G6-18`'s own overstated claim — the DXVK install/ON direction DOES call `runWineCommand` (via the un-awaited `reg add` registration loops), so a genuine click CAN retire research Pitfall 2. Evidence set pinned by `dxvkEvidenceLines.test.ts`, RED-proven |
| 34.5-55 | `U-34.5-25`: SteamGridDB (7 sites) and winetricks (3 sites) now route through `callOrDeclare()` with a visible decline, correcting the fourth gate's own defective sweep census (8 → 10, `F-34.5-G6-21`) |
| 34.5-57 | Code review of the union of 52-55's diffs — found `F-34.5-G6-22` (blocker, see Precondition 0), which is the reason this contract cannot be run yet |

**State plainly: none of it is live-proven; that is what this contract exists to do — once
Precondition 0 clears.**

---

## D-08 — no partial pass

Restated verbatim from `34.5-CONTEXT.md`, and every prior contract's own restatement:

> **D-08:** **Blocking live gate covering all three logins** (Epic, GOG, Amazon) on macOS — same
> standard as every prior slice, no advisory mode, no pre-authorized override. Rejected: one runner
> live + two declared unit-proven-only; auth-only gating with Wine/shortcuts deferred to UAT.

D-08 was authored when the gate had five items including Epic. It is restated verbatim because it
is binding language, not because it still names the current item set — the Epic item it describes
was removed by D-CYCLE6-A (see below); D-08's own no-partial-pass discipline is unaffected by that
removal and applies to the four items that remain, exactly as every prior contract since RERUN-3
has recorded.

A **fifth** non-clean result (anything other than 4 PASS / 0 FAIL / 0 BLOCKED / 0 NOT ATTEMPTED)
routes to **another gap cycle inside Phase 34.5** — never a forced close, never an advisory note,
never a deferred UAT entry, never a pre-authorised override. Phase 34.6's and Phase 35's dependency
lines on Phase 34.5 stay untouched by this document and are editable ONLY on a clean 4-of-4 PASS
recorded in this document's own frontmatter.

---

## Evidence-handling rule (T-34.5-G6-02, extended for `code_verifier`)

Restated and extended from every prior contract's own version, binding on every Result slot and
every pasted excerpt below. This document lives in a public fork's repository.

**Never paste, in any Result slot or excerpt, any of the following SEVEN value classes** (six
inherited plus a new seventh, added this cycle because `F-34.5-G6-17`/`-20`/`-22` all establish
that this project logs PKCE `code_verifier` material, not merely OAuth `code`):

1. an `access_token` value
2. a `refresh_token` value
3. a cookie value
4. a session identifier
5. an OAuth `code=` value
6. a Steam `userdata` account id
7. **a PKCE `code_verifier` value** (new this cycle)

Quote only the specific log lines an item or precondition names. If a required line contains one of
these values inline, **redact it in place** (e.g. `access_token=<REDACTED>`,
`code_verifier=<REDACTED>`) rather than omitting the whole line — the surrounding structure (which
field was populated, that a value was non-empty) is still evidence; the value itself is not needed
to prove a PASS.

**The mandatory pre-commit sweep**, extended with the seventh value class. Before committing the
FILLED contract, run this over the staged diff and paste its output into the run's own record:

```
git diff --cached -- 34.5-LIVE-GATE-RERUN-4.md | grep -nE "access_token=[A-Za-z0-9._+/=-]{4,}|refresh_token=[A-Za-z0-9._+/=-]{4,}|Cookie: [A-Za-z0-9._+/=-]{4,}|session(id|Cookie)=[A-Za-z0-9._+/=-]{4,}|code=[A-Za-z0-9_-]{6,}|userdata/[0-9]|code_verifier=[A-Za-z0-9._+/=-]{4,}"
```

**Re-validated at THIS document's own authoring time (2026-08-14) against 4 safe specimens and 4
leak-shaped specimens, so the extended regex is proven neither vacuous nor self-matching before it
is ever relied on live.** Real terminal output, all 8 cases:

```
=== SAFE (1): bare prose mention, no assignment ===
$ echo 'the PKCE code_verifier value is redacted before logging' | grep -nE "<full regex>"
exit=1  (no match, as expected)

=== SAFE (2): the pattern's own documentation text ===
$ echo 'code_verifier=[A-Za-z0-9._+/=-]{4,}' | grep -nE "<full regex>"
exit=1  (no match — the character immediately after "code_verifier=" in the pattern's own text is
         "[", which is not in the token-alphabet class, so the pattern cannot self-match its own
         source — same non-self-matching property RERUN-3's Part D established for the other six
         classes, now re-proven for the seventh)

=== SAFE (3): a properly redacted value ===
$ echo 'code_verifier=<REDACTED>' | grep -nE "<full regex>"
exit=1  (no match — "<" is not in the token-alphabet class)

=== SAFE (4): below the 4-char floor ===
$ echo 'code_verifier=ab' | grep -nE "<full regex>"
exit=1  (no match — only 2 token characters follow "=", below the 4-character floor)

=== LEAK (1): real-shaped code_verifier value ===
$ echo 'code_verifier=abcdefghij1234567890' | grep -nE "<full regex>"
1:code_verifier=abcdefghij1234567890
exit=0  (MATCHES, as required)

=== LEAK (2): code_verifier embedded mid-line in a quoted shell command ===
$ echo 'nile register --code_verifier=Q3JlYXRlZFRoaXNTZW50aW5lbA --serial 12345' | grep -nE "<full regex>"
1:nile register --code_verifier=Q3JlYXRlZFRoaXNTZW50aW5lbA --serial 12345
exit=0  (MATCHES, as required)

=== LEAK (3): access_token, pre-existing class, sanity re-check ===
$ echo 'access_token=abc123XYZ' | grep -nE "<full regex>"
1:access_token=abc123XYZ
exit=0  (MATCHES — the six inherited classes are unaffected by the extension)

=== LEAK (4): code_verifier at exactly the 4-char boundary ===
$ echo 'code_verifier=abcd' | grep -nE "<full regex>"
1:code_verifier=abcd
exit=0  (MATCHES — the floor is >= 4, not > 4)
```

`<full regex>` above is the literal pattern quoted in the mandatory-sweep command block. All 8
specimens behaved exactly as expected: 4 safe → no match, 4 leak-shaped → match. The extended
pattern is non-vacuous and does not self-match its own documentation.

**The published-SteamID64 exception, carried verbatim from `deferred-items.md` item 23.** The 37
already-published files containing the SteamID64 profile-URL number are an **expected, accepted**
sweep match by developer decision (redact-forward, not rewrite-history) — do NOT re-raise a hit
against one of those 37 files as a new finding. Any **NEW** unredacted account id, token, cookie,
session id, OAuth code, or `code_verifier` — in this document or any other file this run touches —
is still in scope and still blocks the commit.

---

## The binding evidence rule

Stated once, applying to all four items, carried verbatim from every prior contract: **a mutating
call's own report is never proof of its effect.** Run 2 recorded backend `status=captured` for GOG
and Amazon and it proved nothing. Run 3's backend persisted 7 GOG titles while the Library rendered
none. Run 4's `autoInstallDxvk: true` and the switch's own on-screen ON state proved to be a
week-old stale value, not evidence of anything that session. **Every evidence clause below
therefore names at least one observable produced by a DIFFERENT component than the one under
test:** a UI state, a file on disk, a log line emitted by another module, or a `ps`/`pgrep` listing.

---

## Item count and the Epic descope (D-CYCLE6-A)

**This gate has FOUR items, not five.** Per `34.5-CYCLE6-ROUTING.md` § D-CYCLE6-A and
`deferred-items.md` item 24:

| Item | Subject |
|---|---|
| 1 | GOG login completed from scratch, populated library |
| 2 | Amazon login completed from scratch, populated library, `www.amazon.com` anchor confirmed |
| 3 | `addToSteam` AND the macOS `.app` shortcut, BOTH `exe` call sites |
| 4 | `runWineCommand` succeeds for at least one non-Steam runner |

Phase 34.7 is the receiving owner of Epic's interactive login; the blocker mechanism is the operator
decision of 2026-08-05. **Descoping is not a pass and retires nothing** — `U-34.5-06` and
`U-34.5-11` both retired 2026-08-03, before this descope was ever recorded, each for its own named
reason, and neither retirement is credited to the descope.

---

## No-console rule

**The Tauri DevTools console cannot be pasted into on this build — independently confirmed by the
operator during gate 4's own item 3 ("devtools console is crappy and cant cut and paste into it,
sigh"), a recurring, known friction, not a one-off.** No step in this contract may require typing
into it. Gate 4's item 3 struck a console-invocation step mid-run for exactly this reason, and the
real UI route (the "⋮" dots-menu buttons) proved to be the BETTER evidence — the button's own
resolved-value handling (`handleAddToSteam()`'s real `await ... .then((added) =>
setAddedToSteam(added))`) sidesteps the console's own unreliability entirely, since nothing is
typed into it. Every item and precondition below routes through real UI controls or through
files/logs read directly from disk — never a DevTools prompt.

---

## Dual-sink evidence-capture standard (Section 3 of `live-gate-contract-authoring.md`, reproduced in full as binding instructions)

- **One session directory per gate run**, created ONCE before the first launch, named with a UTC
  timestamp: `mkdir -p /tmp/gamelib-gate-$(date -u +%Y%m%dT%H%M%SZ)/`.
- **The terminal transcript is APPENDED, never truncated.** `tee -a <session-dir>/terminal.log` into
  a single session-scoped file for every launch. **Never a bare `tee`, never a `>` redirect.**
- **A delimiter line before each launch**, exact text: `echo "=== GATE LAUNCH ${N} —
  $(date -u +%Y-%m-%dT%H:%M:%SZ) ===" | tee -a <session-dir>/terminal.log`, so a reader can
  `grep -n '^=== GATE LAUNCH'` to find each launch's section boundary.
- **`gamelib.log` archived per launch**, `cp ~/Library/Logs/GameLib/gamelib.log
  <session-dir>/gamelib-launch-${N}.log`, taken **before the next launch's first write renames it
  to `.old`** (`src/backend/logger/log_writer.ts:72-74`). Archive any pre-existing `.old` file on
  the very first pass too.
- **Every item records which launch ordinal covered it.**
- **A closing inventory**: `ls -la <session-dir>/` plus `wc -l <session-dir>/*`, run at session end,
  so a missing or zero-length capture is visible immediately.
- **Exactly ONE app instance for the whole run — asserted, not assumed.** Before EVERY launch:
  `pgrep -f 'target/debug/gamelib-shell'` must be EMPTY. After the window appears, record the
  single PID into the transcript. At teardown, record it again. **If the count is ever not 1, the
  launch is ABORTED and re-run — its evidence is not scorable.**

**Sink partition, stated explicitly so no clause below demands a line from a sink that cannot carry
it:**
- Rust `[shell]`-prefixed `eprintln!` lines (e.g. the banner-injection confirmation lines, item 1's
  clause (n) below) reach the tee'd terminal transcript ONLY — they never reach `gamelib.log`.
- Sidecar `logInfo`/`logWarning`/`logDebug` calls reach `~/Library/Logs/GameLib/gamelib.log` ONLY —
  they never reach the tee'd terminal transcript (`sidecar-console-and-logger-are-invisible`).
- Sidecar STDERR reaches the tee'd transcript, prefixed `[sidecar:err]`.
- Sidecar `console.*` calls reach **nothing at all**.

---

## Preconditions

**Tick off in order after Precondition 0 clears. An unstated precondition is exactly how a gate
gets skipped and becomes UAT debt.**

**1. Kill orphans first.** `pgrep -f 'target/debug/gamelib-shell'` AND
`pgrep -f 'sidecar.js'` must both be EMPTY before launching anything; kill anything found and
re-check. After launch, `pgrep -f 'target/debug/gamelib-shell'` must show **exactly one** PID and
`pgrep -f 'sidecar.js'` must show exactly one PID — record both listings verbatim, with PIDs.

**Result:** _EMPTY — filled only by plan 34.5-59._

**2. A clean rebuild and a real `pnpm tauri:dev` launch** (never `tauri dev`, which serves a stale
static bundle — project memory `tauri-dev-serves-stale-static-bundle`). Sidecar liveness proven
from `gamelib.log` growth PLUS the precondition-1 `ps`/`pgrep` listing — never from the terminal's
own sidecar output (structurally invisible). Record the rebuild sequence, its output, and the build
completion timestamp.

**Result:** _EMPTY — filled only by plan 34.5-59._

**3. Preserve the log before trusting it, using the Section 3 session-directory standard above.**
Confirm the session directory was created once, the delimiter text matches exactly, and every
archived `gamelib-launch-${N}.log` was copied BEFORE the next launch. If the run spans more than
one session, preserve each session separately and label which session every excerpt below came
from.

**Result:** _EMPTY — filled only by plan 34.5-59._

**4. The secret-store arm is `keyring`, VERIFIED not assumed. Plan 34.5-58's own keyring session
must be fully torn down before this gate's first launch.** Verify the arm from the preserved log:
`grep "\[bootstrap\] secret stores: " <preserved log>` must end `keyring`, never `dev-vault`, and
**zero** `[dev-secret-vault]` lines may appear anywhere. A `dev-vault` reading is NO evidence for
`U-34.5-01`/`U-34.5-10` — the run may continue, but those two rows stay OPEN regardless of this
run's verdict. **Additionally, before this gate's first launch:** confirm plan 34.5-58's own
session process is gone — `pgrep -f 'target/debug/gamelib-shell'` must be EMPTY at the moment this
precondition is checked (the same command precondition 1 uses), so that any keyring-related outcome
34.5-58 produced cannot bleed into this gate's own evidence. Whatever 34.5-58's own outcome was, it
CANNOT change this gate's required 4/0/0/0. **Never switch secret-store arms mid-run** (a keyring
timeout during item 1/2's own login is recorded as a finding for both this precondition AND the
affected item's own Result slot, never silently absorbed into either alone).

**Result:** _EMPTY — filled only by plan 34.5-59._

**5. Signed OUT of GOG and Amazon — verified, not assumed. Epic requires no sign-out action** (out
of scope per precondition 10). Fresh `ls`/`test -f` on every original and backup path: **GOG —
`gog_store/auth.json` (the credential file) AND `gog_store/config.json`'s cached `isLoggedIn` flag**
(gate 4's own precondition 5 found this SECOND surface independently cached `{"isLoggedIn": true}`
and required a separate aside — check both, not only `auth.json`). **Amazon — `nile_store/` AND
`nile_config/nile/user.json`.** If a credential HAS reappeared at its original path, move it aside
by the SAME recoverable procedure gate 4 used: `cp -p` to a NEW, dated suffix (never reuse a prior
aside's own suffix), `cmp` the copy against the original to prove byte-identity, and only then
remove the original — **never delete blind.**

**Result:** _EMPTY — filled only by plan 34.5-59._

**6. The G-1 sidecar-asset-root fix is present in THIS build, proven from the preserved log.**
Quote verbatim: `[bootstrap] appRoot resolved=<path> source=GAMELIB_APP_ROOT` (never
`process.cwd`); `[bootstrap] publicDir resolved=<path> exists=true`; all four
`[bootstrap] runner binary <name> ... exists=true` lines (`legendary`, `gogdl`, `nile`, `comet`).
A `[bootstrap] SIDECAR ASSET ROOT DEFECT` line anywhere **BLOCKS the run.**

**Result:** _EMPTY — filled only by plan 34.5-59._

**7. `GAMELIB_SHELL_EXE` read back from the SIDECAR's own receipt line**
(`[bootstrap] GAMELIB_SHELL_EXE received=<path>`), never the Rust shell's own terminal output. An
`<UNSET>` reading **BLOCKS item 3.** Dev-vs-packaged caveat carried verbatim: this is a **dev**
build; a dev PASS on item 3 never retires the packaged half of Assumption A2 or residual
`R-34.5-G1-PKG`, both of which stay open regardless of this item's outcome.

**Result:** _EMPTY — filled only by plan 34.5-59._

**8. The full suite battery at the HEAD the gate runs on**, exit codes recorded VERBATIM: `npm run
test:ci`, `npx tsc --noEmit`, `cd src-tauri && cargo check`, `cargo test`, `python3
ported-channels-gate.py`, `python3 preload-surface-gate.py`. **The floor is the MEASURED count from
the most recent wave-3 SUMMARY** — `34.5-55-SUMMARY.md`: `npm run test:ci` **249/249 suites,
4832/4833 tests (1 pre-existing skip)**; `cargo test` **147 passed, 0 failed, 1 ignored** (per
`34.5-52-SUMMARY.md`, unaffected by 53-55, none of which touch Rust). Cite the SUMMARY by filename
and copy its number — do not transcribe from a prior gate document. A count below this floor means
HEAD is stale; a count above needs an explanation naming which plan between the floor and this
gate's own HEAD added tests (34.5-57's own review is docs-only and adds none; any 34.5-58 fix for
`F-34.5-G6-22` should be explained here if it adds tests, per its own SUMMARY once it exists). A
green suite is necessary and never sufficient — this gate has beaten one four times running.

**Result:** _EMPTY — filled only by plan 34.5-59._

**9. Cycle-6 AND cycle-7 fixes present in this build, proven from source at HEAD**, with
comment-filtered greps so a doc comment cannot satisfy a code assertion. One check per plan,
**each re-confirmed against current source at THIS document's own authoring time (2026-08-14)** —
not transcribed from a prior contract or from the routing document, per this cycle's own repeated
"wrong evidence line" lesson (`F-34.5-G6-18`/`-19`):

Cycle-6 (carried forward, unchanged mechanism from RERUN-3's own precondition 9):
`getInstallInfo` registered (34.5-43); Rust argv/single-instance/`gamelib://` helpers (34.5-44);
`createFromBuffer`/quoted `run.sh` (34.5-45); runner-aware `handleLaunch` (34.5-46); `origin=`
threading (34.5-47); `callOrDeclare`/EOS decline (34.5-48); preload-surface audit (34.5-49).

Cycle-7 (new this contract — **the exact grep for the banner function is CORRECTED here from a
plausible-looking but wrong form**, per this document's own duty to verify every citation against
current source rather than transcribe it):

- `grep -c "^fn login_origin_banner_script(origin: &str) -> String {" src-tauri/src/main.rs` → must
  be `1`. **Note: an UN-anchored `grep -c "fn login_origin_banner_script"` (no `^` anchor, no
  parameter signature) returns `11` on current source, not `1`, because it also matches the ten
  `#[cfg(test)] fn login_origin_banner_script_...` test-function names that share the same
  substring — re-verified at this document's own authoring time by running both forms. The
  anchored, full-signature form is the one that isolates the real function definition and is the
  only form this precondition may cite.**
- `grep -c "redactNileRegisterData" src/backend/storeManagers/nile/user.ts` → must be `2` (the
  helper's own `function redactNileRegisterData` definition at line 63, plus its one call site at
  line 126 — re-confirmed at authoring time).
- `test -f src/backend/tools/__tests__/dxvkEvidenceLines.test.ts` → must exist.
- `grep -c "WINETRICKS_DECLINED_GUARD" src/frontend/components/UI/Winetricks/index.tsx` → must be
  `>= 2` (re-confirmed at authoring time: exactly `2`, the `const WINETRICKS_DECLINED_GUARD =
  declined` declaration and its one `if (WINETRICKS_DECLINED_GUARD) return` guard use).

This precondition makes "the fix is not on this build" distinguishable from "the fix does not
work" before a single item is scored — run 4 could not make that distinction for item 4.

**Record beside this precondition, in its own sentence: presence is NOT proof of effect — that is
what the four items below are for.**

**Result:** _EMPTY — filled only by plan 34.5-59._

**10. The Epic descope is in force (D-CYCLE6-A).** This precondition's RESULT slot is where the
operator confirms the run was executed as a **four-item** gate — no fifth, blank Epic slot filled
in or left dangling.

**Result:** _EMPTY — filled only by plan 34.5-59._

**11. Item-3 preflight — the non-event guard.** Record this machine's global
`addDesktopShortcuts`/`addStartMenuShortcuts`/`addSteamShortcuts` values from the live
`config.json`. If `addSteamShortcuts` is `false`, this contract prescribes `fromMenu=true` for the
Steam-VDF half of item 3's `addShortcut` invocation (via the real "Add shortcut" button, which
already calls `window.api.addShortcut(appName, runner, true)` — see item 3 below). Record the
verified currently-installed non-Steam target's `appName` and `runner` (re-check at gate time — do
not assume a prior run's target is still installed). Record the Steam `shortcuts.vdf` path with the
`userdata` account id REDACTED. **Quit Steam FIRST**, before either invocation — Steam holds
`shortcuts.vdf` in memory and rewrites it from memory on exit.

**Result:** _EMPTY — filled only by plan 34.5-59._

**12. Item-4 preflight — the Wine repoint AND the genuine-OFF premise (Test-6 requirement, this
cycle's own direct application).** `U-34.5-03` retires **only** on an item-4 PASS. This preflight
must, live, at gate time:

(a) Re-verify a `wine`-type Wine version exists under `tools/wine/` — do not assume from a prior
    session.
(b) Enumerate currently-INSTALLED non-Steam games and name one; repoint that game's `wineVersion`
    at the `wine`-type version, recording the before/after `wineVersion.type` values from
    `GamesConfig/<appName>.json`.
(c) **Establish and RECORD a genuine OFF state before the operator ever opens the Wine tab — this
    is the fix for run 4's own defect (`U-34.5-30`), and it must be a POSITIVE observable from
    disk, never the switch's own on-screen appearance:**
    - Read `~/Library/Application Support/gamelib/GamesConfig/<appName>.json` and record
      `autoInstallDxvk`'s current value AND the file's mtime.
    - Confirm the target prefix contains NO `current_dxvk-macOS` marker file — `ls
      <winePrefix>/current_dxvk-macOS` must fail (file absent). Record the `ls` output verbatim.
    - **If either check shows an already-ON state** (a `true` config value, or the marker file
      present), the operator must first click the toggle OFF (which runs the RESTORE path and
      removes the marker via `wineboot -u`), and the executor must then RE-CONFIRM the marker is
      gone from disk before the scored ON click. Do not proceed to the scored click until this
      re-confirmation is recorded.
    - **Why the marker matters, stated so the executor understands the mechanism, not just the
      procedure:** `src/backend/tools/index.ts:371-374` returns early with `<tool> already
      installed!` when the marker's version equals the global version — this SKIPS both the DLL
      copy and the `runWineCommand` registration loop entirely. With the marker present, a genuine
      click produces NO Wine command at all and the item is unverifiable again, exactly as it was
      in run 4.

**Result:** _EMPTY — filled only by plan 34.5-59._

---

## Known holes (Ledger Rule 3)

**Recount performed against `34.5-UNTESTED-ITEMS.md` at THIS document's own authoring time
(2026-08-14), not transcribed from any prior contract or from the plan's own brief.** Command and
raw output (recorded verbatim in this plan's own SUMMARY as required):

```
$ grep -oE '^\| U-34\.5-[0-9]+' 34.5-UNTESTED-ITEMS.md | wc -l
32
```

Cross-checked row by row against each row's own final Status-cell disposition (an `**OPEN**` or
`**RETIRED**` marker recorded LAST in each row, since several rows carry a historical `OPEN` before
a later `RETIRED` update): **32 total rows. 18 OPEN. 14 RETIRED.** This does NOT match the plan's
own guessed shape of "32" for the total alone — the total does match (32), but the OPEN/RETIRED
split is this document's own independent recount, not assumed.

The table below has 18 rows, matching the OPEN count exactly.

| Ledger ID (creating plan) | Subject | Which gate item would retire it | Can THIS run retire it? |
|---|---|---|---|
| `U-34.5-01` (34.5-36) | Real macOS Keychain read/write path for `steam-refresh-token`/`humble-session`/`humble-csrf` | None of items 1-4 exercise the keyring directly | **Only via precondition 4's own four-part bar** (unset vault, `keyring` arm logged, zero `[dev-secret-vault]` lines, at least one successful `SidecarKeyringSlotStore` read) — a condition of the RUN, not of any single item. |
| `U-34.5-03` (both prior runs) | Item 4's own subject (`runWineCommand` for a non-Steam runner) | Item 4 | Retirable ONLY by an item-4 PASS, driven against the genuine-OFF precondition (12) above. |
| `U-34.5-04` (34.5-27) | Amazon verification-code field legibility | Item 2, clause (l) | Retirable by item 2's own transcription IF a code field appears this run; NOT-APPLICABLE (not a pass) if no 2FA challenge fires, per every prior run's own recording convention. |
| `U-34.5-05` (34.5-27, RE-TARGETED 34.5-52) | The in-page origin banner's before/after transcription within one flow | Item 1, clauses (i)/(m)/(n) | Retirable by item 1's own banner transcription — NOT the `NSWindow` title bar, which is structurally impossible on macOS (`F-34.5-G6-16`). |
| `U-34.5-10` (34.5-35) | Keyring read-timeout/human-approval race — only ONE Keychain prompt for a slot across a full login | None of items 1-4 exercise the keyring directly | **Only via precondition 4's own bar**, same dependency as `U-34.5-01`. |
| `U-34.5-18` (34.5-44, D-44-A) | Single-instance guard on non-unix (Windows) targets | None — this run is macOS-only by construction | **NOT COVERED BY THIS GATE.** Owner: whichever future work first ships/tests a Windows build. |
| `U-34.5-20` (34.5-44) | A WARM (already-running) protocol launch hiding the main window | None — no `rustInvoke`-style hide channel exists | **NOT COVERED BY THIS GATE.** Owner: whichever future plan adds the missing hide channel. |
| `U-34.5-21` (34.5-46) | A real GOG game actually LAUNCHING via the Play button (not a shortcut) | None — none of items 1-4 press Play on an installed title | **NOT COVERED BY THIS GATE**, a genuine gap in this gate's own coverage. Owner: a future UAT session or gate cycle. |
| `U-34.5-22` (34.5-46) | Whether a live GOG uninstall makes the Library tile stop presenting as installed WITHOUT a restart | None of items 1-4 directly — gathered as cross-cutting observation #1 below | **Observation only, no verdict impact — but MANDATORY TO ATTEMPT this run** (see Cross-cutting observations). |
| `U-34.5-23` (34.5-47) | `RefreshLibraryOptions['origin']` union-type narrowing | None — a type-system decision, not a live observable | **NOT COVERED BY THIS GATE.** Owner: a future plan, or an explicit decision to keep the source-text gate as sole enforcement. |
| `U-34.5-24` (34.5-48) | The EOS overlay decline proven live | None of items 1-4 touch Settings → Advanced | **NOT COVERED BY THIS GATE.** EOS is Phase 34.6 territory. |
| `U-34.5-26` (34.5-48) | Two new i18n keys absent from generated locale catalogs | None — blocked on the standing i18n catalog-drift triage | **NOT COVERED BY THIS GATE.** Owner: the standing triage (project memory `localisation-standing-requirement`). |
| `U-34.5-27` (34.5-45) | Real (DIB/BMP-encoded, multi-image) GOG `.ico` decode via `sips` | Item 3, IF the target has a real `.ico` | **OUT OF SCOPE this run (D-CYCLE7-B) — `blocked_on: operator-supplied test fixture`.** See the out-of-scope table below. Item 3's own target (Endless Sky) ships a native `.icns`, no `.ico` at all. |
| `U-34.5-28` (34.5-49) | Whether the 8 newly-bucketed unported channels decline visibly under Tauri | None of items 1-4 directly exercise most of these 8 | **NOT retirable this run.** Full retirement requires all 8 driven deliberately in one session; cross-cutting observation #3 records whichever fire incidentally. |
| `U-34.5-29` (34.5-51) | Amazon/nile library population (named tiles rendering) | Item 2's own clause (d), IF the account owns a title | **OUT OF SCOPE this run (D-CYCLE7-B) — `blocked_on: operator-supplied test fixture`.** See the out-of-scope table below. The account in use owns zero Amazon games. |
| `U-34.5-30` (34.5-51) | Whether `toggleDXVK`'s install/backup action actually runs under this sidecar | Item 4 | Retirable by an item-4 PASS driven against the genuine-OFF precondition (12), citing the corrected evidence set (`F-34.5-G6-19`). |
| `U-34.5-31` (34.5-52) | The in-page banner's spoofability and live-unobserved state | Item 1, clauses (i)/(m)/(n) taken TOGETHER | Retirable only if all three clauses PASS in the same session — the banner visible, the cancel strip simultaneously visible and clickable, and the text changing on a real origin transition. |
| `U-34.5-32` (34.5-55) | Whether the SteamGridDB/winetricks declines this cycle shipped actually render live | None of items 1-4 drive Settings → SteamGridDB or the Winetricks panel | **NOT COVERED BY THIS GATE.** Owner: a future UAT session or Phase 34.6 — recorded as cross-cutting observation #2 (incidental only) below. |

---

## Out of scope for this run (D-CYCLE7-B, plus the inherited table)

**Inherited unchanged from `34.5-CYCLE6-ROUTING.md`/`34.5-CYCLE7-ROUTING.md`:**

| Item | Owner | Why |
|---|---|---|
| Epic login, `egsSync`, legendary save sync, the alt-login 403 | Phase 34.7 | D-CYCLE6-A, operator decision 2026-08-05. |
| The 16 deferred channel ports (8 EOS, 5 SteamGridDB, 3 winetricks — porting, not the honesty sweep) | Phase 34.6 | 34.5 D-03/D-05. |
| `getDefaultSavePath` | Phase 34.6 | `34.5-PORTED-CHANNELS.md` correction 1 / `deferred-items.md` item 4. |
| `R-34.5-G1-PKG` — the packaged Tauri build's asset root | packaging work | Item 3's PASS this gate is a **dev-build-only** result. |
| `getEpicGamesStatus`, `getGOGLinuxInstallersLangs` | none | No frontend caller at all. |
| Path-casing inconsistency, PCGamingWiki 403s, HLTB null-property errors | note only / none | Not this phase's own code. |

**New this cycle (D-CYCLE7-B), each with an explicit `blocked_on:` field:**

| Item | `blocked_on:` |
|---|---|
| `U-34.5-29` (Amazon library population) | **`blocked_on: operator-supplied test fixture`** — an Amazon account owning at least one title. The account in use owns zero. |
| `U-34.5-27` (real GOG multi-image `.ico` decode) | **`blocked_on: operator-supplied test fixture`** — a GOG install directory containing a real `goggame-*.ico`. Item 3's own target ships a native `.icns` and has no `.ico` at all. |

**Rationale (D-CYCLE7-B, verbatim in substance): a fifth FAIL caused by a missing fixture rather
than a defect is worthless evidence.** Neither row can affect the required 4/0/0/0 this run, and
**both rows stay OPEN** — out of scope is not retirement.

---

## Cross-cutting observations

Observations, not items — a result here changes no verdict and does not gate the arithmetic below.

**1. GOG uninstall tile-flip without a restart (`U-34.5-22`).** Run 4 recorded this NOT GATHERED —
no uninstall action appeared anywhere in the preserved log, and the executor did not separately ask
the operator to drive one. **This run makes it MANDATORY TO ATTEMPT — a third consecutive silent
omission is not an acceptable outcome.** The runner must EITHER gather it (drive a real GOG
uninstall in the same session as items 1-4, record the `Finished uninstalling` line and the tile
state in the same session, with NO restart between them) OR record an EXPLICIT REFUSAL naming its
own blocker (e.g. "no GOG title was installed this session to uninstall"). A silent omission a
third time is itself a defect in how this contract is being run, and must be called out as such in
the run's own notes if it recurs. A non-reproduction is recorded as a non-reproduction, never as a
retirement.

**2. SteamGridDB/winetricks live decline (`U-34.5-32`, incidental only — NOT a retirement
attempt).** If any SteamGridDB or Winetricks surface is opened during this run for any reason
(e.g. while demonstrating item 1's library), record whether it declines visibly (the unavailable
message, no editable-and-inert Save, no empty component list rendered as if genuinely empty) and
whether `grep -c "GAMELIB_DECLARED_UNAVAILABLE" <preserved log>` shows a corresponding line. This
observation alone cannot retire `U-34.5-32`, which requires a deliberate session driving all of its
own named surfaces.

**3. The 8 newly-bucketed unported channels (`U-34.5-28`, incidental only).** Record whether any of
`frontendReady`, `getAchievements`, `getDefaultSavePath`, `getGogDiscounts`,
`getPlaytimeFromRunner`, `importGame`, `moveInstall`, `runWineCommandForGame` fire incidentally
during this run's own items (`frontendReady` will, at boot; the rest require driving their own
respective UI surfaces, none of which items 1-4 require). Not an attempt to retire the row.

---

## Items

**Tick off in order, after Precondition 0 clears and every other precondition above is satisfied.
Each item's Result slot is empty below and is filled ONLY by plan 34.5-59.**

### Items 1-2 (GOG / Amazon) — common evidence clauses (REQ-34.5-12; REQ-34.5-02 anchors item 2)

For EACH of items 1 and 2, require ALL of:

(a) the `[oauthLoginCapture] runner=<runner> status=captured` line
    (`src/backend/sidecar/oauthLoginCapture.ts:235-238`);
(b) a `[useTauriOAuthLogin] runner=<runner> phase=idle (login completed, library refresh
    triggered)` line for the SAME runner (`src/frontend/screens/WebView/useTauriOAuthLogin.ts:381-382`);
(c) the runner's CLI auth invocation appearing in the log AFTER the capture timestamp;
(d) the library **populated AND RENDERED** in the UI — named game tiles visible — AND the runner
    present in the Library filter options. A persisted store file alone is NOT clause (d);
(e) the account surface showing the signed-in username.

### Item 1 — GOG login completed FROM SCRATCH, populated library (REQ-34.5-12)

**What changed since run 4:** 34.5-52's in-page origin banner, replacing the structurally-impossible
title-bar transcription this row's clause (i) previously named.

**What to do:** with precondition 5 satisfied for GOG, drive a real GOG login through the seam.

**Evidence required:** clauses (a)-(e) above, applied to `gog`, PLUS clauses (f)-(j) carried forward
unchanged from RERUN-3 (all PASSed in run 4):

(f) `refreshLibrary complete runner=gog managers=1` in `gamelib.log`
    (`src/backend/sidecar/steamFlowRegistration.ts:187`);
(g) ZERO `Refreshing undefined Library` lines across the whole session, AND every `[refreshLibrary]`
    line carrying a non-`unknown` `origin=` value;
(h) no `refreshLibrary failed runner=...` line anywhere in the session;
(j) the Manage Accounts panel leaves "logging into gog" and shows the signed-in state without a
    restart.

**PLUS, this cycle's own changes:**

(i) **RE-TARGETED per D-CYCLE7-A.** The operator transcribes the text of the **in-page origin
    banner** rendered inside the presented sheet — the element with id
    `__gamelib_login_origin_banner__`, top-left corner (`src-tauri/src/main.rs:1606`,
    `login_origin_banner_script`) — at two points in one flow, and the two transcriptions differ,
    with `login.gog.com` then `embed.gog.com` named as the known candidate pair (the same
    transition GOG's own `nav host=` log line has independently shown in every prior run). **State
    explicitly: the `NSWindow` title bar is NOT the subject any more** — it is structurally
    impossible on macOS (`F-34.5-G6-16`) — **and a log-observed `nav host=` change alone does NOT
    satisfy this clause**, exactly as it never satisfied the old title-bar version of this row.
(m) **The cancel strip is simultaneously visible and still clickable while the banner is shown —
    the banner must never have cost the sheet its only exit.** Per this document's own Structural
    Reachability Review (Test 2, below), confirm "still clickable" WITHOUT actually clicking it
    mid-flow — clicking cancels the login and would destroy this item's own remaining evidence.
    Confirm by visual inspection (the cancel strip's own affordance is rendered and not visually
    occluded by the banner — the banner's own `z-index: '2147482999'` is strictly below the cancel
    strip's `'2147483000'` by construction, per `34.5-52-SUMMARY.md`'s own RED-proven test) rather
    than by an actual click.
(n) The shell's own confirmation lines appear in the tee'd TRANSCRIPT (they are Rust `eprintln!`
    calls, so the transcript — never `gamelib.log` — is the correct sink, per the dual-sink
    standard above): `[shell] humble_login_open: login origin banner injected for '<label>'`
    (`src-tauri/src/main.rs:3704-3706`) at least once, and `[shell] humble_login_open: origin
    banner updated len=<n>` (`main.rs:3803-3806`) at least once for the origin transition named in
    clause (i).

**Result:** _EMPTY — filled only by plan 34.5-59._

### Item 2 — Amazon login completed FROM SCRATCH, populated library, `www.amazon.com` anchor confirmed (REQ-34.5-02, REQ-34.5-12)

**What changed since run 4:** 34.5-53's nile credential redaction (subject to Precondition 0's own
gate — this item cannot be scored while `F-34.5-G6-22` is open, since a real `nile` login is
exactly the seam that leaks).

**What to do:** with precondition 5 satisfied for Amazon, drive a real Amazon Games login through
the seam.

**Interaction with item 1, resolved here (pair-review finding, item 1 × item 2, carried from
RERUN-3):** if item 1's clause (j) shows Manage Accounts did NOT leave "logging into gog" cleanly,
reload the Manage Accounts screen (a full navigation) before starting Amazon's login, and record
that a reload was needed.

**Evidence required:** clauses (a)-(e) above, applied to `nile`, PLUS:

(k) grep for `[oauthLoginCapture] runner=nile origin-mismatch origin=`
    (`oauthLoginCapture.ts:149-150`) — must NOT fire on a clean pass. Record the observed origin
    either way (three-way recording rule: never firing → Assumption A1 CONFIRMED; firing → A1
    FALSIFIED; the redirect-comparison path never reached for any other reason → A1 UNTESTED);
(l) the `U-34.5-04` transcription: whether the verification-code field's text is legible, IF one
    appears — NOT-APPLICABLE, not a pass, if no 2FA challenge fires;
(o) **the credential-redaction check (new this cycle, F-34.5-G6-17/-20/-22's own live-confirmation
    obligation, per `34.5-53-SUMMARY.md`'s own success criteria).** In the archived
    `gamelib-launch-${N}.log` for the launch that covered this item, ALL FOUR of:
    - `grep -c "Got register data:" <archived log>` returns `0`
    - `grep -c "Got register data (redacted):" <archived log>` returns `>= 1`
    - `grep -c "Register data is:" <archived log>` returns `0`
    - `grep -c "Register data received (redacted):" <archived log>` returns `>= 1`
    - AND a `code_verifier` grep against the whole archived log,
      `grep -nE "code_verifier[\"']?\s*[:=]\s*[\"'][A-Za-z0-9_-]{10,}" <archived log>`, finds NO
      line carrying a run of 10 or more token-alphabet characters after `code_verifier` — a match
      here is a live credential leak (per Precondition 0's own `F-34.5-G6-22` finding — this exact
      check is what would catch a regression, or catch the fix's own incompleteness, at
      `callRunner()`'s logging site) and must be redacted in place before any commit, never quoted.

**State explicitly:** `U-34.5-29` (Amazon library population) is OUT OF SCOPE this run per
D-CYCLE7-B — the item's own clause (d) is recorded as "nothing to render" if the account owns zero
games, exactly as run 4 recorded it, and this is NOT folded into the item's overall PASS/FAIL.

**Result:** _EMPTY — filled only by plan 34.5-59._

### Item 3 — `addToSteam` AND `addShortcut`'s macOS `.app`, BOTH `exe` call sites (REQ-34.5-01, REQ-34.5-12)

Carried forward unchanged from RERUN-3, which PASSed this item cleanly. Inline precondition 11's
target, path and invocations again here so the operator does not have to scroll. Require ALL of:

- the `shortcuts.vdf` `Exe` field written by `nonesteamgame.ts:258`, quoted with the account id path
  REDACTED, matching precondition 7's `GAMELIB_SHELL_EXE received=` value exactly;
- `<title>.app/Contents/MacOS/run.sh` written by `shortcuts.ts:227`, quoted shape confirmed (the exe
  path double-quoted, the whole `gamelib://` URL single-quoted and percent-encoded), with NO `Error
  generating MacOS App` line;
- the `U-34.5-27` real-`.ico` check: IF the item-3 target's install directory contains a real
  `goggame-<appId>.ico`, confirm a non-empty `Contents/Resources/shortcut.icns`. **This run's own
  target is expected to have no such file (D-CYCLE7-B, out of scope) — record that absence
  explicitly, do not silently pass over it.**
- `window.api.addToSteam(<appName>, <runner>)` resolving `true` (via the real "Add to Steam" button,
  `GameSubMenu/index.tsx`'s `handleAddToSteam()`, per the No-console rule above — never a typed
  console invocation) and the label flipping to "Remove from Steam" WITHOUT a reload;
- `ps aux`/`pgrep -f 'target/debug/gamelib-shell'` after launching the shortcut showing exactly ONE
  `gamelib-shell` process throughout, and a `[ProtocolHandler]: Received gamelib://launch?...` line
  with the named GAME launching (a REAL, installed game, never a smoke-test nonexistent appName).

**Operator steps, click-by-click, through the game's own page → the "⋮" (three-dot) icon → "Add
shortcut" then "Add to Steam" → navigate away and back to confirm the shortcut survived → launch
from the created shortcut → confirm only the game itself started, never a second GameLib window.**

**Result:** _EMPTY — filled only by plan 34.5-59._

### Item 4 — `runWineCommand` succeeds for at least one non-Steam runner (REQ-34.5-03, REQ-34.5-12)

**This is the item cycle 7 must actually fix.** Three changes from RERUN-3:

**(1) A genuine OFF→ON transition, proven from disk before the click.** Precondition 12(c) above is
this item's own inline precondition, repeated here so the item cannot be scored without it having
been satisfied: the marker/config-value state must be established and RECORDED as genuinely OFF
before the operator opens the Wine tab, per precondition 12's own three sub-steps.

**(2) The corrected evidence lines (`F-34.5-G6-19`, pinned by `dxvkEvidenceLines.test.ts`,
RED-proven — replacing RERUN-3's own wrong citation):**

- Dispatch marker, install direction: `installing dxvk-macOS on...` — `src/backend/tools/index.ts:369`,
  `logInfo`, `LogPrefix.ToolInstaller`, sink `gamelib.log`. Proves the toolkit/DXMT/mac-tool-selection/
  valid-prefix guards were all passed and the install direction was entered.
- Early-skip line that must NOT appear (its presence means the marker precondition above was not
  actually satisfied and the item is NOT ATTEMPTED, not FAIL): `dxvk-macOS already installed!` —
  `tools/index.ts:372`.
- **The line that actually retires research Pitfall 2:** at least one `Running Wine command: reg
  add HKEY_CURRENT_USER\Software\Wine\DllOverrides /v <dll> /d native,builtin /f` —
  `src/backend/launcher.ts:1520`, **`logDebug`** (re-confirmed at THIS document's own authoring
  time: `logDebug(['Running Wine command:', commandParts.join(' ')], LogPrefix.Backend)` at exactly
  line 1520), sink `gamelib.log`, emitted by `runWineCommand` itself from the DLL-registration
  loops at `tools/index.ts:438` (64-bit) and `:459` (32-bit). **State the correction explicitly:**
  `F-34.5-G6-18`'s own claim that the install direction "never calls `runWineCommand` at all" was
  WRONG; it does, after the DLL copy. `launcher.ts:1581` — **previously mis-cited by RERUN-3 as
  "the definitive evidence... fired regardless of caller"** — was never the right citation for the
  direction this item's own steps exercise; that line is only reached on the RESTORE (toggle-OFF)
  direction. Do not cite `launcher.ts:1581` for the ON-click evidence.
- Filesystem observable: `<winePrefix>/current_dxvk-macOS` EXISTS after the click and did NOT exist
  before it — a before/after pair, both recorded (per precondition 12(c)'s own `ls` check).
- **A settle window is mandatory: at least 30 seconds.** The DLL-registration loops are un-awaited
  `dlls32.forEach(async (dll) => { await runWineCommand(...) })` — a synchronous `forEach` around an
  async callback — so `toggleDXVK` can resolve and the switch can flip ON before the `reg add` lines
  land in the log. Instruct: after the switch settles ON, wait at least 30 seconds, THEN archive
  `gamelib.log` and grep. Reading immediately is a known way to record a false absence.

**(3) Distinguishing OFF-click noise from ON-click evidence.** If precondition 12(c) required an
OFF click first, that click emits its own `Running Wine command: wineboot -u` and `Running Wine
command: reg delete ...` lines. **Every item-4 grep must be scoped to lines AFTER the recorded
timestamp of the SCORED ON click**, and must match `reg add`/`native,builtin`, never `wineboot` and
never `reg delete`.

**Runner eligibility, resolved, not silently substituted.** Primary target: GOG (or any
storefront-authenticated runner), contingent on item 1 reaching a real signed-in,
library-populated state this run — a storefront-runner PASS retires BOTH the narrow literal
reading of Pitfall 2 AND the storefront-specific reading. Fallback, login-independent target:
`sideload` — retires ONLY the narrow literal reading. **State explicitly which target was used, and
that a PASS is now genuinely capable of retiring Pitfall 2 at all only because of the
`F-34.5-G6-19` correction** — say so, so the causal link is on the record.

**UI-driven steps only, no console, traced to their components:** the per-game gear/settings icon
(`SettingsButton.tsx`) → the "Wine" tab (`GamesSettings/index.tsx`) → the "Crossover/Wine Version"
dropdown (`WineVersionSelector.tsx`), selecting `Wine-Crossover-latest` → the "Auto Install/Update
DXVK on Prefix" toggle (`AutoDXVK.tsx`), which is HIDDEN until a real `wine`-type version is
selected — clicking it OFF-if-needed then ON, per precondition 12(c)'s own sequence.

**If the prerequisite is STILL unmet at gate time:** disposition is **NOT ATTEMPTED**, with an
explicit, recorded refusal naming its blocker — never a silent skip.

**Result:** _EMPTY — filled only by plan 34.5-59._

---

## Verdict

**EMPTY — filled only by plan 34.5-59.**

| Item | Subject | Result |
|------|---------|--------|
| 1 | GOG login completed FROM SCRATCH, populated library | _EMPTY_ |
| 2 | Amazon login completed FROM SCRATCH, populated library + `www.amazon.com` anchor confirmed | _EMPTY_ |
| 3 | `addToSteam` AND macOS `.app` shortcut, both `exe` call sites | _EMPTY_ |
| 4 | `runWineCommand` succeeds for at least one non-Steam runner | _EMPTY_ |

**Overall verdict:** `null` at authoring time. Filled only when 34.5-59 completes the run.

---

## Arithmetic rule

**`items_passed + items_failed + items_blocked + items_not_attempted` must equal 4 when the run
completes.** BLOCKED and NOT ATTEMPTED each count toward NEITHER `items_passed` NOR `items_failed`.
Any shortfall from 4 PASSes must be stated explicitly in the frontmatter `notes` field — never
quietly rounded to make the arithmetic look clean.

**Restated from D-08 in one line:** anything short of a clean 4 of 4 means Phase 34.5 does NOT
close, and routes to another gap cycle inside Phase 34.5.

---

## New findings (this run) — record, do not diagnose

**None at authoring time — this document is authored before any live work begins.** Continue the ID
prefix `F-34.5-G6-`, currently at `-23` (the highest allocated so far, `34.5-CYCLE7-REVIEW.md`'s own
`F-34.5-G6-23`) — the next finding plan 34.5-59 records, if any, is `F-34.5-G6-24`.

---

## Structural Reachability Review (authoring-time, plan 34.5-56)

**Delivered as part of this plan, before publication, per Decision D-E — this is a DELIVERABLE, not
a footnote.** Applies all seven defect-class tests from
`.claude/skills/spike-findings-gamelib/references/live-gate-contract-authoring.md` to every item,
sub-check, precondition, and the evidence-capture instruction itself.

| Item/sub-check/precondition | Surface it names | What it asks for | Verdict | Evidence |
|---|---|---|---|---|
| Item 1, overall (GOG login URL) | `login_window_url_arg` https-only gate | A real `https://` GOG login URL | REACHABLE | `src-tauri/src/main.rs:935`-area https-only arg validation, re-confirmed present; GOG's own login URL is `https://login.gog.com/...` |
| Item 1, clause (i)/(m)/(n) | The presented AppKit sheet, the in-page banner, the cancel strip | Transcribe banner text twice, confirm cancel strip visible+clickable WITHOUT clicking, confirm `[shell]` confirmation lines | CONDITIONAL | `main.rs:1597-1670` (`login_origin_banner_script`/`_update_script`); resolved by NOT clicking the cancel strip mid-flow (visual-only confirmation) — see Test 2 below |
| Item 2, clause (o) | The credential-redaction sweep | Four greps against the archived `gamelib.log`, plus the `code_verifier` grep | CONDITIONAL on Precondition 0 clearing first | `src/backend/storeManagers/nile/user.ts` lines 63/126 (redaction helper + call site); `launcher.ts:1697-1704`/`:1888` (the THIRD, still-open site — Precondition 0 blocks item 2 until this closes) |
| Item 2, overall (Amazon login URL) | `login_window_url_arg` https-only gate | A real `https://www.amazon.com/...` login URL | REACHABLE | Same gate as item 1; Amazon's own login URL is `https://www.amazon.com/ap/signin` |
| Item 3, gesture sequence | `GameSubMenu`'s "⋮" dots menu | Real "Add shortcut"/"Add to Steam" button clicks | REACHABLE | `src/frontend/screens/Game/GameSubMenu/index.tsx:164-172` (`handleShortcuts`), `:211-223` (`handleAddToSteam`) — both real, mounted, clickable controls; confirmed live-driven successfully in run 4 |
| Item 4, gesture sequence | Per-game Settings → Wine tab → DXVK toggle | Real UI clicks; toggle only visible after selecting a `wine`-type version | CONDITIONAL | `WineVersionSelector.tsx`, `AutoDXVK.tsx` (toggle conditionally rendered), `SettingsButton.tsx`, `GamesSettings/index.tsx` — see Test 7 below for the conditional-rendering trace |
| Item 4, OFF-click × ON-click evidence pair | `gamelib.log`'s Wine-command lines | Distinguish OFF-click noise from the scored ON-click evidence | CONDITIONAL, resolved | Timestamp scoping (grep only after the recorded ON-click time) + token discrimination (`reg add`/`native,builtin` vs. `wineboot`/`reg delete`) — see Test 5 below |
| Precondition 1 (orphan kill) | Process table | Zero orphans before, exactly one pair after | REACHABLE | `pgrep -f 'target/debug/gamelib-shell'`, `pgrep -f 'sidecar.js'` |
| Precondition 4 (keyring arm + 34.5-58 teardown) | Secret-store bootstrap log line, process table | `keyring` arm logged; 34.5-58's process gone | REACHABLE | `[bootstrap] secret stores: keyring` (grep against preserved log); `pgrep -f 'target/debug/gamelib-shell'` empty at precondition-4 check time |
| Precondition 9 (cycle-6/7 fix presence) | Source at HEAD | Anchored greps against real function/constant names | REACHABLE | All four cycle-7 greps re-run and re-confirmed at this document's own authoring time (see precondition 9's own text; the banner grep was corrected from an unanchored to an anchored form during this authoring) |
| Precondition 12(c) (genuine-OFF premise) | `GamesConfig/<appName>.json`, the Wine prefix directory | A positive disk observable of OFF state, not the switch's own appearance | REACHABLE | `GamesConfig/<appName>.json`'s `autoInstallDxvk` field + mtime; `ls <winePrefix>/current_dxvk-macOS` (must fail/absent) |
| Evidence-capture instruction (dual-sink standard) | `tee -a`, `gamelib.log` archiving, `pgrep` instance assertion | Every literal cited by an item routed through the sink that can actually carry it | REACHABLE | Section 3's own sink-partition table, re-applied per-clause in items 1/2/4 above (clause (n) → transcript; clauses (a)/(f)/(o)/item-4 evidence → `gamelib.log`) |

**Zero rows carry a Verdict of IMPOSSIBLE.**

### Test 1 — Origin/scheme reachability

Every login-path clause in this contract (item 1's GOG flow, item 2's Amazon flow) targets the
store's own real `https://` login URL — `login_window_url_arg`'s https-only gate
(`src-tauri/src/main.rs`, re-confirmed present at authoring time) is satisfied by construction for
both. No DummyStore or other non-`https` fixture is named anywhere in this contract. **REACHABLE
for both login items.**

### Test 2 — Concurrency reachability

The presented login window is application-modal to `main` via `beginSheet:` (Phase 34.4.2's own
CR-01 fix, unchanged this cycle). No clause in this contract asks for a second in-app action to be
driven while the sheet is up. Item 1's new clauses (i)/(m)/(n) are observations of the sheet's OWN
surface while it is up — reachable — but clause (m)'s "still clickable" check is resolved, per its
own text above, by visual confirmation ONLY (the cancel strip is rendered, not visually occluded,
and its z-index ordering is unit-proven) rather than by an actual click, because clicking the
cancel strip would cancel the login and destroy the item's own remaining evidence (clauses (d)
through (j), (n)). **No IMPOSSIBLE concurrency requirement found.**

### Test 3 — Log-line emitter reachability, WITH the sink clause

Every literal this contract cites was re-grepped against current source at THIS document's own
authoring time, with its emitter's sink identified:

| Literal | file:line | Emitter | Sink | Contract's own mandated capture reads this sink? |
|---|---|---|---|---|
| `login origin banner injected for '<label>'` | `main.rs:3704-3706` | Rust `eprintln!` | tee'd transcript | Yes — clause (n) explicitly names the transcript, not `gamelib.log` |
| `origin banner updated len=<n>` | `main.rs:3803-3806` | Rust `eprintln!` | tee'd transcript | Yes, same clause |
| `installing dxvk-macOS on...` | `tools/index.ts:369` | sidecar `logInfo` | `gamelib.log` | Yes — item 4's evidence names `gamelib.log`, archived per Section 3 |
| `Running Wine command: ...` | `launcher.ts:1520` | sidecar `logDebug` | `gamelib.log` | Yes, same |
| `Got register data (redacted):` / `Register data received (redacted):` | `nile/user.ts:126`/similar | sidecar `logDebug`/`logInfo` | `gamelib.log` | Yes — item 2 clause (o) names the archived log |
| `oauthLoginCapture ... status=captured` | `oauthLoginCapture.ts:235-238` | sidecar `logInfo` | `gamelib.log` | Yes |

Every literal's emitter exists at the cited line, and every clause names the sink that literal's
emitter actually writes to. **No sink-mismatch defect found** (contrast `F-34.4.2-14`, where three
required lines were sidecar-emitted but demanded of the transcript). Item 1's clause (n) is the one
row this contract deliberately routes to the TRANSCRIPT rather than `gamelib.log`, because its
emitter is a Rust `eprintln!`, not a sidecar logger call — correctly distinguished.

### Test 4 — Absence-observability

Required absences in this contract: `dxvk-macOS already installed!` (item 4); `Got register data:`
and `Register data is:` (item 2, clause (o)); zero `origin-mismatch` lines (item 2, clause (k));
zero `Refreshing undefined Library` lines (item 1, clause (g)). For each, the PRESENCE case would
produce a real, observable signal — none of these are absences over a code path that logs nothing
on success. The two redaction absences are the strongest case: plan 34.5-53's own RED proof
(`34.5-53-SUMMARY.md`, captures (a) and (b)) directly demonstrated the un-redacted strings
appearing when the fix was reverted — the mutation evidence already exists and is cited here rather
than re-derived. `dxvk-macOS already installed!`'s absence is a **regression-signal** kind of
absence (its presence would mean the genuine-OFF premise silently failed); the other three are
**confirmations of an existing structural guarantee** established by 34.5-47's/34.5-53's own
source-text gates. **No unfalsifiable absence check found.**

### Test 5 — Requirement-interaction reachability

**Unit of review is the PAIR.** State M (state-mutating requirements) and N (evidence-bearing
requirements) explicitly:

**M (4 state-mutating requirements):**
- M1: item 4's precondition-12(c) toggle-OFF click, if the marker precondition requires it
- M2: plan 34.5-58's own keyring session (a separate, prior app run)
- M3: each item's own real login (GOG, Amazon) — a genuine session-state change
- M4: the mid-run archive-then-relaunch cycle (`gamelib.log` renamed to `.old` on the next launch's
  first write)

**N (5 evidence-bearing requirements):**
- N1: item 4's `Running Wine command:`/`reg add` `gamelib.log` lines
- N2: the `current_dxvk-macOS` marker's before/after filesystem pair
- N3: every `[shell]`-line clause (item 1's clauses (n); the orphan/instance `pgrep` counts)
- N4: item 1/2's "from scratch" login clauses (a)-(k)
- N5: precondition 4's `SidecarKeyringSlotStore` read evidence

**M×N = 20 pairs considered** (every M requirement against every N requirement — the practical
reduction this file's own Section 2 prescribes, not the full requirement-count² cross product).
**Six of the twenty are live candidates in this specific contract and are recorded with a verdict
below; the remaining fourteen do not interact** (verified by inspection: no other M requirement
mutates state any other N requirement reads — e.g. M2's keyring session cannot touch N1/N2/N3/N4,
since it is a wholly separate, already-torn-down app run; M3's logins cannot touch N1/N2, which are
Wine-only).

1. **{M1: item 4's toggle-OFF click} × {N1: item 4's own `Running Wine command:` evidence} —
   FLAGGED-AND-RESOLVED, not clear.** The OFF click emits its own `wineboot -u`/`reg delete` Wine
   commands. Resolution: timestamp scoping (grep only after the recorded ON-click time) plus token
   discrimination (`reg add`/`native,builtin` vs. `wineboot`/`reg delete`), both stated as
   requirements in item 4's own text, not as advice.
2. **{M1: item 4's toggle-OFF click} × {N2: the `current_dxvk-macOS` before/after filesystem pair} —
   FLAGGED-AND-RESOLVED.** The OFF click is what REMOVES the marker, so the "did not exist before"
   half of the pair is produced by the OTHER requirement (M1) rather than observed independently.
   Resolution: precondition 12(c) records the marker's absence AFTER the OFF click and BEFORE the
   scored ON click as its own explicit sub-step, so the "before" half is honestly attributed to the
   OFF click's own effect, not silently presented as a pre-existing clean state.
3. **{M2: plan 34.5-58's keyring session} × {N5: precondition 4's keyring-read evidence} —
   FLAGGED-AND-RESOLVED.** Resolution: precondition 4 requires `pgrep -f
   'target/debug/gamelib-shell'` to be EMPTY (34.5-58's session fully torn down) before this gate's
   own first launch, so 34.5-58's own reads cannot be double-counted as this gate's evidence.
4. **{M3: each item's own login} × {N4: item 1/2's own "from scratch" premises} —
   FLAGGED-AND-RESOLVED.** A completed GOG login is state that could satisfy or destroy item 2's
   own "from scratch" premise if run out of order, or a stale sign-in from a PRIOR session could
   silently satisfy item 1's own premise. Resolution: precondition 5 re-verifies BOTH
   `gog_store/auth.json` AND `gog_store/config.json`'s cached `isLoggedIn` flag (gate 4's own
   second-surface discovery), plus `nile_store/`/`nile_config/nile/user.json`, immediately before
   each login attempt — a positive disk check, not an assumption carried from a prior run.
5. **{M4: the per-launch archive} × {N1/N3: any mid-run relaunch's evidence} —
   FLAGGED-AND-RESOLVED.** `log_writer.ts:72-74` renames the live log to `.old` on the next
   launch's first write. Resolution: Section 3's own per-launch archiving rule (archive BEFORE the
   next launch, archive any pre-existing `.old` on the first pass) is restated as binding, not
   optional, in the dual-sink standard above.
6. **{the tee'd transcript instruction} × {every `[shell]`-line clause, i.e. item 1's clause (n)} —
   FLAGGED-AND-RESOLVED.** Resolution: `tee -a`, never a bare `tee` or a `>` redirect — stated
   explicitly in Section 3 above, this project's own `F-34.4.2-11` pair.

### Test 6 — Pre-existing external-state reachability

**Unit of review is the item's PREMISE.** Per item, the external-state dependency and its positive
confirming observable:

| Item | External-state dependency | Positive confirming observable |
|---|---|---|
| 1 (GOG login) | A live WKWebView cookie jar and/or `gog_store/auth.json` / `gog_store/config.json`'s cached `isLoggedIn` flag could hold a stale signed-in session from a prior run | Precondition 5's fresh `ls`/`test -f`/`cat` on BOTH GOG surfaces, PLUS a rendered GOG login FORM actually appearing on screen (not merely absent files — an emptied app-side store does not prove a logged-out session, per this file's own general rule) |
| 2 (Amazon login) | `nile_store/`/`nile_config/nile/user.json` and/or the WKWebView cookie jar for `amazon.com` | Precondition 5's fresh check on both Amazon surfaces, PLUS a rendered Amazon login FORM |
| 3 (shortcuts) | A pre-existing `shortcuts.vdf` entry, a running Steam process holding the file open, or a leftover `.app` bundle at `~/Applications/<title>.app` from a prior run | Precondition 11's confirmed-absent check on `shortcuts.vdf`, plus "Quit Steam FIRST" and a `ps`/`lsof` check that nothing holds the file open (per gate 4's own re-investigation of the `ipcserver` orphan) |
| 4 (DXVK toggle) | The persisted `autoInstallDxvk` config value AND the `current_dxvk-macOS` prefix marker — BOTH can predate the session by weeks, the exact failure this run exists to fix | Precondition 12(c)'s genuine-OFF premise: read the config value + mtime, confirm the marker absent from disk, click OFF-if-needed, RE-confirm the marker gone, before the scored click |

**This is the first live contract to which Test 6 applies** (per the reference file's own coverage
map: "Tests 6 and 7 have caught nothing yet"). Item 4's genuine-OFF precondition is this cycle's
direct, deliberate application of Test 6's general rule — for every premise of the form "the switch
is off," name a POSITIVE observable, never merely the switch's own on-screen appearance.

### Test 7 — UI-level reachability, distinct from backend-logic reachability

**Unit of review is the operator GESTURE SEQUENCE.** Every gesture this contract names is traced to
the component that renders its control:

- Item 1's login form and banner: the presented AppKit sheet itself (not a React component — a
  native window built in `src-tauri/src/main.rs`'s `humble_login_open` arm) plus the injected
  `login_origin_banner_script`/cancel-strip scripts.
- Item 3's "Add shortcut"/"Add to Steam": `src/frontend/screens/Game/GameSubMenu/index.tsx`, mounted
  from the game page's own "⋮" (three-dot) icon (`DotsMenu.tsx`).
- Item 4's settings path: the per-game gear/settings icon → `SettingsButton.tsx` → the Wine tab →
  `GamesSettings/index.tsx` → the Crossover/Wine Version dropdown, `WineVersionSelector.tsx` → the
  "Auto Install/Update DXVK on Prefix" toggle, `AutoDXVK.tsx`.
- Cross-cutting observation 1's uninstall gesture: whichever uninstall control the Library tile or
  game page renders (traced live, at gate time, since no specific component is pinned by this
  contract for an observation-only clause).
- Cross-cutting observation 2's incidental SteamGridDB/Winetricks surfaces:
  `SteamGridDbApiKey.tsx`/`SteamGridDBPicker/index.tsx`/`Winetricks/index.tsx` (34.5-55's own
  wrapped call sites), reached only if opened incidentally.

**The AutoDXVK toggle's own conditional rendering is stated explicitly, per this test's own
requirement: it is HIDDEN for toolkit/CrossOver-DXMT versions and appears ONLY once a real
`wine`-type version (e.g. `Wine-Crossover-latest`) is selected in `WineVersionSelector.tsx`.** This
makes the version-repoint step (precondition 12(b)) a genuine gesture PRECONDITION for item 4, not
a nicety that could be skipped — without it, the toggle this item's own steps ask the operator to
click does not exist on screen at all. **This is the first live contract to which Test 7 applies**
(same coverage-map statement as Test 6).

**Closing statement, per the reference file's own reviewable clause (T-34.4.2-42):** the count of
structural impossibilities and unresolved requirement interactions actually encountered DURING THE
RUN should be zero. This authoring-time review found zero IMPOSSIBLE rows and six FLAGGED-AND-
RESOLVED pairs, all resolved by a stated mechanism (timestamp scoping, positive disk observables,
teardown assertions, append-only archiving). Whatever the run itself encounters is recorded per run
by plan 34.5-59, whatever that count turns out to be — this review does not claim completeness
beyond what it has actually tested, and Tests 6/7 specifically are being applied for the FIRST TIME
by this contract, so a clean result from them reads as "first application," not as "validated."
