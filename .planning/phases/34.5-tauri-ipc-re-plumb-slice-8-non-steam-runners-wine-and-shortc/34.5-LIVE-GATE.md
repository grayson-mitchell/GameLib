---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 03
executed_by: 34.5-15-PLAN.md
type: live-gate
status: pending
blocking: true
created: 2026-07-29
completed: null
verdict: pending
items_total: 5
items_passed: 0
items_failed: 0
notes: contract written before any live work (wave 1, plan 03), per REQ-34.5-12/D-08. No item
  has been attempted yet. Result slots are filled ONLY by plan 34.5-15 (wave 6) at gate time.
---

# Phase 34.5 Plan 03 — Blocking Live Gate (REQ-34.5-12)

This document is a **contract**, written before any live work begins (wave 1, plan 03), not a
report. Per **D-08** (carrying 34.3's D-13, 34.4's D-08, and 34.4.1's D-09/D-10), this gate is
**BLOCKING**: any FAIL in the 5 named items below means the phase does not close and a gap cycle
inside 34.5 is the next step — no advisory mode, no pre-authorized override, no deferred UAT
entry.

34.4.1's own gate (`34.4.1-LIVE-GATE.md`) has just demonstrated that a live gate catches what a
fully-green 3095-test suite cannot (the D-06 relaunch-guard blocker, the item-3(b) unobservability
finding). This gate exists for the same reason, scoped to 34.5's three OAuth logins, the
`GAMELIB_SHELL_EXE` handoff, and the never-proven non-Steam Wine path.

---

## Preconditions

**Tick off in order. An unstated precondition is exactly how a gate gets skipped and becomes UAT
debt (Phases 21, 30, 34.2) — 34.4.1's own D-09/D-10 lesson, carried forward.**

1. **Phase 34.4.1's own blocking live gate has recorded PASS on at least item 1** (a real Humble
   login completing from scratch) in `34.4.1-LIVE-GATE.md`. Evidence: quote the recorded result
   line for Item 1 from that document verbatim here before proceeding.

   **This phase may be BUILT before this precondition is met** — wave 1 through wave 5 of 34.5 do
   not require it. **But this gate's items 1-3 (the real Epic/GOG/Amazon OAuth credential paths)
   MUST NOT be run** until this precondition is satisfied: 34.5 must not ship a real credential
   path on a seam whose own blocking live gate never ran. As of this document's authoring
   (2026-07-29), `34.4.1-08` is HELD at Task 2 (the live gate itself) — this precondition is
   **currently unmet** and item 1 quoted below is a placeholder, not evidence.

   Recorded result line (fill in at gate time): _______________________________________________

2. A clean rebuild and a real `pnpm tauri:dev` launch, with the sidecar confirmed alive via
   `~/Library/Logs/GameLib/gamelib.log` growing and a `ps` listing showing shell + sidecar
   processes running simultaneously. **Sidecar output is read from that log file and NOT from the
   `pnpm tauri:dev` terminal** — the terminal shows the shell process's own console, not proof the
   sidecar's *application* logic ran, because stdout is the RPC pipe (per the standing
   `sidecar-console-and-logger-are-invisible` finding). Keep `tail -f
   ~/Library/Logs/GameLib/gamelib.log` open in a second window for the whole gate run.

3. The operator is signed OUT of Epic, GOG and Amazon before items 1-3 run. If a stored credential
   for any of the three exists, it must be moved aside **recoverably**, not deleted blind: copy it
   to at least one backup location, verify the backup is byte-identical (e.g. via `cmp`), only
   then remove the original, and write both the original path and the restore path down in this
   document before proceeding. A gate that starts from an already-authenticated session cannot
   prove REQ-34.5-12's "from scratch" claim for that runner.

4. `npm run test:ci`, `npx tsc --noEmit`, `cd src-tauri && cargo check`, `cargo test` — all four
   are run and their exit codes are recorded VERBATIM in this document (not rounded to "green"),
   along with a one-line explanation of any non-zero exit that is a known pre-existing flake
   rather than a regression introduced by this phase.

5. The `GAMELIB_SHELL_EXE` value the Rust shell actually set for the sidecar (REQ-34.5-01, wave-1
   seam 1 from plan `34.5-01`) is read back from `gamelib.log` and written into this document
   verbatim before item 4 below is attempted — item 4 is the live proof that this value is not
   just present but *correct* (points at the Tauri shell binary, not `node`).

   `GAMELIB_SHELL_EXE` value observed (fill in at gate time): ____________________________________

---

## Items

### Item 1 — Epic login completed FROM SCRATCH, populated library (REQ-34.5-12)

**What to do:** With precondition 3 satisfied for Epic (no stored credential), drive a real Epic
Games Store login through `authAmazon`/`authGOG`/`login`'s Epic counterpart (the `legendary`
runner's OAuth flow via the seam 34.4.1 built), authenticating with real credentials.

**What to observe:** The login window opens on Epic's real login origin, credential entry
succeeds against the live site, the sidecar's `oauthCaptureLogin` channel reports a matched
redirect, and the resulting library is **populated** (not merely "signed in" with zero games) —
an empty library after login is not a PASS, since it could indicate the profile-data write to
`configStore` silently failed.

**Evidence required:** `gamelib.log` lines showing the OAuth redirect capture succeeding for the
`legendary`/Epic runner, and the app's own UI reflecting a signed-in, non-empty library afterward.

**Result:** _ [ ] PASS   [ ] FAIL _ — evidence:

---

### Item 2 — GOG login completed FROM SCRATCH, populated library (REQ-34.5-12)

**What to do:** With precondition 3 satisfied for GOG (no stored credential), drive a real GOG
login through the seam, authenticating with real credentials.

**What to observe:** The login window opens on GOG's real login origin, credential entry succeeds
against the live site, the sidecar's `oauthCaptureLogin` channel reports a matched redirect for
the `gog` runner, and the resulting library is **populated**.

**Evidence required:** `gamelib.log` lines showing the OAuth redirect capture succeeding for the
`gog` runner, and the app's own UI reflecting a signed-in, non-empty library afterward.

**Result:** _ [ ] PASS   [ ] FAIL _ — evidence:

---

### Item 3 — Amazon login completed FROM SCRATCH, populated library, AND the `www.amazon.com` anchor confirmed against one real redirect (REQ-34.5-02, REQ-34.5-12)

**What to do:** With precondition 3 satisfied for Amazon (no stored credential), drive a real
Amazon Games login through the seam, authenticating with real credentials. This is also the ONLY
real proof of REQ-34.5-02's `matchOAuthRedirect('nile', …)` host anchor (`www.amazon.com`,
MEDIUM confidence, verified only against upstream `nile` source, never against GameLib's own
traffic — research Assumption A1).

**What to observe:** The login window opens on Amazon's real login origin, credential entry
succeeds against the live site, the sidecar's `oauthCaptureLogin` channel reports a matched
redirect for the `nile` runner, and the resulting library is **populated**. Additionally: capture
the real redirect's origin and confirm it matches `www.amazon.com` as `matchOAuthRedirect`
assumes.

**Evidence required:**
  (a) `gamelib.log` lines showing the OAuth redirect capture succeeding for the `nile` runner, and
      the app's own UI reflecting a signed-in, non-empty library afterward.
  (b) The captured redirect's origin, read back from the greppable evidence-log line plan
      `34.5-02` added — grep `gamelib.log` for the
      `[oauthLoginCapture] runner=nile origin-mismatch origin=` prefix. **A PASS on the login
      itself means this line should NOT fire** (the origin matched and was accepted silently); if
      it DOES fire, the `www.amazon.com` anchor is FALSIFIED and this item cannot be a clean PASS
      even if login otherwise succeeds — record the actual origin observed either way.

**Result:** _ [ ] PASS   [ ] FAIL _ — evidence:

---

### Item 4 — `addToSteam` AND the macOS `.app` desktop shortcut both launch GameLib, not node — BOTH `exe` call sites (REQ-34.5-01, REQ-34.5-12)

**What to do:** Using precondition 5's confirmed `GAMELIB_SHELL_EXE` value: (a) add a non-Steam
game to Steam via `addToSteam` and launch it from Steam; (b) separately, create a macOS `.app`
desktop shortcut via the shortcuts flow and double-click it. **Both call sites must be
exercised** — `nonesteamgame.ts:258` (the Steam-shortcut VDF launch command) and
`shortcuts.ts:227` (the desktop `.app` bundle), per research Correction 3. Proving only one is not
sufficient — they are two independent writers of the same value and either can regress
independently.

**What to observe:** In both cases, the launched process is the Tauri shell binary (GameLib),
never the `node` interpreter. A wrong value at either call site produces a shortcut/VDF entry
that launches the wrong executable.

**Evidence required:** For each call site, the actual launch command/target written (VDF launch
field for `addToSteam`, `.app`'s `Info.plist`/launcher script target for the desktop shortcut) and
confirmation the running process after launch is GameLib, not node (e.g. `ps aux` showing the
Tauri shell binary, not a bare `node` process).

**Result:** _ [ ] PASS   [ ] FAIL _ — evidence:

---

### Item 5 — `runWineCommand` succeeds for at least one NON-Steam runner (REQ-34.5-03, REQ-34.5-12)

**What to do:** After a successful login above (items 1-3), run a Wine tool action (e.g. a
winecfg/DXVK toggle/wine-version action reachable via `runWineCommand`) against a non-Steam game
from one of the newly-authenticated runners.

**What to observe:** The Wine command executes successfully under the sidecar. This closes the
gap named by research Pitfall 2: install/launch for non-Steam runners are ported, but no
non-Steam game has ever reached `runWineCommand` under the sidecar without a working login —
until this gate, the wave-1 seam-3 work (D-14) was unit-proven only, never live-proven.

**Evidence required:** `gamelib.log` lines showing the Wine command dispatch and its successful
completion, plus the observable in-app effect (e.g. the toggle taking effect, or the tool
launching) for the specific non-Steam runner exercised.

**Result:** _ [ ] PASS   [ ] FAIL _ — evidence:

---

## Verdict

| Item | Subject | Result |
|------|---------|--------|
| 1 | Epic login completed FROM SCRATCH, populated library | _pending_ |
| 2 | GOG login completed FROM SCRATCH, populated library | _pending_ |
| 3 | Amazon login completed FROM SCRATCH, populated library + `www.amazon.com` anchor confirmed | _pending_ |
| 4 | `addToSteam` AND macOS `.app` shortcut both launch GameLib, not node (both `exe` call sites) | _pending_ |
| 5 | `runWineCommand` succeeds for at least one non-Steam runner | _pending_ |

**Overall verdict:** _pending — not yet attempted._

**Gate failure is BLOCKING.** Any item recorded FAIL means this phase does not close. Findings
become a gap cycle inside 34.5 (`/gsd-plan-phase 34.5 --gaps` or equivalent), not a deferred UAT
entry, not an advisory note, and not a pre-authorized override. There is no partial-pass closure
for this gate.

---

## What this gate falsifies

If items 1-3 PASS, three standing claims about this seam's readiness for real credential traffic
are retired: that the 34.4.1 login seam (built and unit-proven, but only Humble-live-tested) also
carries a real Epic/GOG/Amazon session end-to-end; and that REQ-34.5-02's `www.amazon.com` host
anchor (MEDIUM confidence, verified only against upstream `nile` source) holds against GameLib's
own live traffic. If item 4 PASSes, it retires the standing claim that `GAMELIB_SHELL_EXE`
(REQ-34.5-01) is merely present in the environment rather than *correct* at both of its two
independent call sites. If item 5 PASSes, it retires the standing claim (research Pitfall 2) that
"no non-Steam runner has ever executed a Wine command under the sidecar" — the wave-1 seam-3 work
was unit-proven only until this gate runs it live.
