---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 29
type: execute
wave: 5
depends_on: [34.5-28]
files_modified:
  - src/backend/sidecar/oauthLoginCapture.ts
  - src/backend/sidecar/__tests__/oauthLoginCapture.test.ts
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md
autonomous: true
gap_closure: true
requirements: [REQ-34.5-01, REQ-34.5-03, REQ-34.5-04, REQ-34.5-05, REQ-34.5-12]

must_haves:
  truths:
    - "Epic's login form is interactive, by the mechanism the discriminator selected — not by a guess"
    - "Gate items 4 and 5 have a named, verified target and exact invocation steps before the gate runs"
    - "Nothing in this plan claims a live behaviour on the strength of a passing test"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md"
      provides: "verified preflight for the two never-attempted gate items"
      contains: "shortcuts.vdf"
  key_links:
    - from: "34.5-G6-EPIC-DISCRIMINATOR.md verdict"
      to: "the Epic fix in oauthLoginCapture.ts"
      via: "the branch the verdict selected"
      pattern: "USER_AGENTS"
---

<objective>
Apply the Epic fix that plan 34.5-28's discriminator SELECTED, and remove the two remaining reasons
gate items 4 and 5 have never been attempted across two full runs.

Purpose: items 4 (`addToSteam` plus the macOS `.app` shortcut, both `exe` call sites — research
Assumption A2) and 5 (`runWineCommand` for a non-Steam runner — research Pitfall 2) are the only two
gate items with NO result of any kind, ever. Both times they were skipped because the developer ran
out of a working login to build on and because the item's own groundwork was days stale by the time
it was reached. Neither standing claim can be retired until they actually run. This plan removes the
"we did not get to it" failure mode by verifying the target and writing the exact invocations down
before the gate opens.

Output: the Epic fix (branch-selected, never guessed) and `34.5-G6-ITEM45-PREFLIGHT.md`.

**Branch discipline is binding.** Task 1 reads the discriminator's verdict and does what that verdict
says — including doing nothing and escalating. Do not apply a fix the verdict did not select.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-EPIC-DISCRIMINATOR.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.claude/skills/spike-findings-gamelib/SKILL.md

**Run `graphify explain "oauthLoginCapture"`, `graphify query "addToSteam nonesteamgame shortcuts vdf exe"` and `graphify explain "runWineCommand"` before opening raw source.**

<interfaces>
From `src/backend/sidecar/oauthLoginCapture.ts`:
- `const USER_AGENTS: Record<OAuthRunner, string>` (line 53) — `legendary` is the only entry without a browser product token; the others are `Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/200.0`
- `resolveUserAgent(runner)` — the override seam plan 34.5-24 added
- `matchOAuthRedirect('legendary', url)` requires `parsed.hostname === 'localhost'` plus a non-empty `code` param. **Changing the user agent must not change this matcher.**

Gate item 4's two independent `exe` call sites (research Correction 3 — proving one is NOT sufficient):
- `src/backend/shortcuts/nonesteamgame/nonesteamgame.ts:258` — `getPath('exe')` written into the Steam shortcut VDF's `Exe` field
- `src/backend/shortcuts/shortcuts/shortcuts.ts:227` — `getPath('exe')` written into `generateMacOsApp`'s `run.sh` body

Item 4's unconsumed groundwork, carried from `34.5-LIVE-GATE-RERUN.md` rather than rediscovered:
- Target: GOG *Alan Wake*, `appName` `1207659037`, installed at `~/GameLib/Alan Wake`
- Expected writes:
  | Call site | File | Expected content |
  |---|---|---|
  | `nonesteamgame.ts:258` | `~/Library/Application Support/Steam/userdata/<id>/config/shortcuts.vdf` | `Exe` = `"<shell exe path>"` |
  | `shortcuts.ts:227` | `<shortcut>.app/Contents/MacOS/run.sh` | body reads `<shell exe path> --no-gui gamelib://launch?appName=1207659037&runner=gog` |
- Both channels are reachable from the devtools console:
  `window.api.addShortcut('1207659037', 'gog', false)` (listener, fire-and-forget) and
  `await window.api.addToSteam('1207659037', 'gog')` (handler, returns boolean)
- **Operator warning: quit Steam FIRST.** Steam holds `shortcuts.vdf` in memory and rewrites it from memory on exit, clobbering any write made while it is running.
- `shortcuts.vdf` did NOT exist when the groundwork was recorded — the file appearing at all is part of item 4(a)'s evidence.

From `src/backend/launcher.ts`:
- `runWineCommand()` at line 1504 — the item-5 seam. Its Steam path is proven transitively via already-ported `install`/`launch`; no NON-Steam runner has ever reached it under the sidecar (research Pitfall 2, STANDING).
- `src/backend/tools/index.ts` platform guards: `:73` (`tool.os !== process.platform`), `:228` (macOS permits `dxvk` only — VKD3D already excluded), `:245` (`dxvk-macOS`), `:219` (`-DXMT` builds).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the Epic fix the discriminator selected — and only that</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-G6-EPIC-DISCRIMINATOR.md` — read the frontmatter `verdict` FIRST, before anything else; it selects which branch below applies
    - `src/backend/sidecar/oauthLoginCapture.ts` lines 50-64 and 226-255 (whole `USER_AGENTS` block and the `activeSeam.open` site — this is the file being modified)
    - `src/backend/sidecar/__tests__/oauthLoginCapture.test.ts` (whole file — the suite this task extends)
    - `.claude/skills/spike-findings-gamelib/SKILL.md` § "The UA override is mandatory — Tauri's default macOS UA has no browser product token"
  </read_first>
  <behavior>
    - Under `verdict: R1`: `resolveUserAgent('legendary')` with no override returns the Chrome-shaped agent, and `matchOAuthRedirect('legendary', …)` is byte-identically unchanged.
    - Under any other verdict: `oauthLoginCapture.ts` is unmodified and the suite is unchanged.
    - In every branch, the `GAMELIB_OAUTH_UA_LEGENDARY` override still works and still logs its length.
  </behavior>
  <action>
    Branch strictly on `34.5-G6-EPIC-DISCRIMINATOR.md`'s frontmatter `verdict`:

    **`verdict: R1`** — change `USER_AGENTS.legendary` in `src/backend/sidecar/oauthLoginCapture.ts`
    to the Chrome-shaped agent arm B proved interactive:
    `Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/200.0`. In the doc comment, record WHY the
    original value existed (it was copied verbatim from `WebView/index.tsx` L229-237, where it runs
    inside a Chromium `<webview>` that already supplies its own product tokens — a bare
    `EpicGamesLauncher` string is a token-less agent under WKWebView, which the standing spike record
    names as the mandatory-to-avoid case), and record the live evidence that selected the change,
    citing the discriminator document and its two arms by name. Do NOT delete the old string from the
    comment — a future reader must be able to see what was changed and why, or this becomes a mystery
    edit at the next upstream merge.

    **`verdict: R1-FALSIFIED`** — make NO change to `USER_AGENTS`. R2 survived but is not confirmed.
    Write the further-diagnosis task the discriminator's own routing section named into
    `34.5-G6-FINDINGS.md` as a new open finding with its next observation, and record in the SUMMARY
    that this task deliberately shipped no code. Escalate to the orchestrator that Epic is expected to
    FAIL item 1 of the blocking gate, so the gate is authored knowing that.

    **`verdict: NOT-REPRODUCED`** — make NO change. Record in `34.5-G6-FINDINGS.md` that Epic's defect
    is unexplained rather than fixed, and that the blocking gate must attempt item 1 from scratch with
    no expectation either way. An unexplained disappearance is not a closure.

    **`verdict: NEITHER-READING-FITS`** — STOP. Make no change, write no fix, and escalate. Do not
    invent a third reading.

    In ALL branches: do not touch `matchOAuthRedirect`. Epic's `localhost` host anchor and nile's
    `www.amazon.com` anchor (REQ-34.5-02, and the ONLY thing closing the inherited `T-34.4.1-44b`)
    are security controls, not styling. A user-agent change must not be allowed to drift into the
    matcher.

    Under `verdict: R1`, extend `oauthLoginCapture.test.ts`: assert the value handed to the seam stub's
    `open` for `legendary` with no override, and re-assert every `matchOAuthRedirect` case unchanged —
    especially that a non-`localhost` host still returns `null` for `legendary` and a non-
    `www.amazon.com` host still returns `null` for `nile`.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/oauthLoginCapture.test.ts --silent &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY quotes the discriminator's frontmatter `verdict` verbatim and names which branch was taken, before describing any change.
    - Under `R1`: `grep -c "EpicGamesLauncher" src/backend/sidecar/oauthLoginCapture.ts` counts only comment occurrences — verified by re-running the grep with `grep -v '^\s*\*' | grep -v '^\s*//'` and confirming the code-only count is 0.
    - Under `R1`: a test asserts the seam stub's recorded `open` argument for `legendary` equals the Chrome-shaped agent — asserted on the stub's recorded argument, not on `resolveUserAgent`'s return value alone.
    - Under any non-`R1` verdict: `git diff --name-only` shows NO change to `src/backend/sidecar/oauthLoginCapture.ts`, and the SUMMARY states plainly that no code shipped and why.
    - In all branches: tests re-assert `matchOAuthRedirect('legendary', 'https://evil.example/?code=x')` returns `null` and `matchOAuthRedirect('nile', …)` rejects a non-`www.amazon.com` origin.
    - `npx jest src/backend/sidecar/__tests__/oauthLoginCapture.test.ts` exits 0; count recorded.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify item 4's target and write its exact invocation steps</name>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` item 4 (the unconsumed groundwork, verbatim — including the quit-Steam warning and the two expected writes)
    - `src/backend/shortcuts/nonesteamgame/nonesteamgame.ts` around line 258 (the VDF `Exe` write — read the surrounding function, not just the line)
    - `src/backend/shortcuts/shortcuts/shortcuts.ts` around line 227 (`generateMacOsApp`'s `run.sh` body)
    - `src/backend/sidecar/pathShim.ts` (the `exe` case and its loud-throw contract when `GAMELIB_SHELL_EXE` is unset)
  </read_first>
  <action>
    Item 4 has been NOT ATTEMPTED on both gate runs. Its groundwork was recorded on 2026-07-29 and is
    now days stale; its own text warns the target may not have survived. Remove every reason it can be
    skipped again by verifying the target NOW and writing the steps down.

    Create `34.5-G6-ITEM45-PREFLIGHT.md` with a `## Item 4 — both exe call sites` section containing:

    1. **Target verification, run now, recorded verbatim.** Is GOG *Alan Wake* (`appName`
       `1207659037`) still installed? Check BOTH `~/Library/Application Support/GameLib/gog_store/installed.json`
       AND the on-disk directory `~/GameLib/Alan Wake`. If it is NOT installed, pick a different
       installed non-Steam game, record its real `appName` and `runner` read out of `installed.json`,
       and use it consistently in every step below. Do not carry a stale appName forward — that is
       precisely what made this item skippable twice.
    2. **The exact Steam userdata path**, resolved on this machine rather than transcribed: list
       `~/Library/Application Support/Steam/userdata/` and record the account-id directory that
       exists. Record the full `shortcuts.vdf` path. **Redact the account id in the committed document**
       (`userdata/<REDACTED>/config/shortcuts.vdf`) per the evidence-handling rule — it is a Steam
       account identifier.
    3. **Whether `shortcuts.vdf` currently exists**, and if so its size and mtime. Its first
       appearance is itself part of item 4(a)'s evidence, so its prior state must be on record.
    4. **The two exact devtools-console invocations**, with the verified appName substituted in:
       `window.api.addShortcut('<appName>', '<runner>', false)` and
       `await window.api.addToSteam('<appName>', '<runner>')`.
    5. **The quit-Steam warning**, restated as a numbered step BEFORE the invocations, with its reason
       (Steam holds `shortcuts.vdf` in memory and rewrites it from memory on exit).
    6. **What to read back for each call site**, and what a PASS looks like: the VDF `Exe` field for
       `addToSteam`, the `run.sh` body for the desktop shortcut, and in both cases a `ps aux` line
       after clicking, showing the launched process is the Tauri shell binary and NEVER a bare `node`
       process. State explicitly that proving ONE call site is not sufficient — they are two
       independent writers of the same `GAMELIB_SHELL_EXE`-derived value and either can regress alone
       (research Correction 3).
    7. **The dev-versus-packaged limit**, carried verbatim so a dev PASS is never later read as
       covering packaging: `current_exe()` resolves to `src-tauri/target/debug/gamelib-shell` in dev;
       a packaged build resolves through `spawn_sidecar_packaged` to a path inside the `.app` bundle.
       Residual `R-34.5-G1-PKG` stays open regardless of this item's outcome.
  </action>
  <verify>
    <automated>grep -q "shortcuts.vdf" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md &amp;&amp; grep -q "R-34.5-G1-PKG" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - The document records a VERIFIED installed non-Steam game with its real `appName` and `runner`, read out of `installed.json` and cross-checked against the on-disk directory, with both commands and their output quoted.
    - The Steam `userdata` account-id directory was resolved on this machine, and the committed document shows it REDACTED.
    - `shortcuts.vdf`'s current existence, size and mtime are recorded (or its absence stated).
    - Both devtools invocations appear with the verified appName substituted, not a placeholder.
    - The quit-Steam step precedes the invocations and carries its reason.
    - The read-back and PASS criteria are stated per call site, with the explicit rule that proving one is not sufficient.
    - The dev-versus-packaged limit and `R-34.5-G1-PKG` are recorded as staying open.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Name a concrete, reachable item-5 Wine action against a non-Steam game</name>
  <read_first>
    - `src/backend/launcher.ts` around line 1504 (`runWineCommand` — its signature and its callers)
    - `src/backend/tools/index.ts` lines 60-90, 210-260 and 630-650 (the platform guards: `tool.os !== process.platform`, the macOS `dxvk`-only rule with VKD3D already excluded, `dxvk-macOS`, `-DXMT`, and `isMac ? macEnvs : linuxEnvs`)
    - `src/backend/sidecar/wineToolsFlowRegistration.ts` (which of the 9 Wine channels are registered and by what kind)
    - `.planning/phases/34.5-.../34.5-LIVE-GATE-RERUN.md` item 5 (the standing Pitfall 2 claim and why a FAIL-by-blockage retires nothing)
  </read_first>
  <action>
    Item 5 has never been attempted either, both times because no authenticated non-Steam runner
    existed to drive it against. Add a `## Item 5 — runWineCommand for a non-Steam runner` section to
    `34.5-G6-ITEM45-PREFLIGHT.md` that makes the action unambiguous.

    Establish from source, and record with citations:

    1. **Which Wine action is actually reachable on macOS** for a non-Steam game. The platform guards
       at `tools/index.ts` permit `dxvk` on macOS and already exclude VKD3D — so a "toggle VKD3D"
       instruction would be a guaranteed non-event and would prove nothing. Name a specific, permitted
       action and the exact UI path to reach it (which settings panel, which control).
    2. **Which channel that action dispatches**, by name, and whether it is handle-kind or send-kind.
       This matters: a send-kind channel fails 100% SILENTLY under the sidecar — no reject, no
       timeout, no log line — so if the chosen action rides a send channel, name the separate
       observable that proves it ran, because its own absence of error proves nothing.
    3. **The exact `gamelib.log` lines that constitute evidence** of dispatch and successful
       completion — quote the literal log-line shapes from the source that emits them, read at
       planning time, so the gate cannot cite a line that does not exist. (This is the same
       gate-contract defect the first run hit at its own precondition 5: it asked for a value nothing
       ever wrote to `gamelib.log`.)
    4. **The observable in-app effect** — what the user should SEE change. A log line alone is the
       dispatcher's own report; per this cycle's binding rule, a mutating call's own report is never
       proof of its effect.
    5. **A prerequisite check**: does the chosen action require an installed non-Steam game, a
       downloaded Wine version, or a prepared prefix? List each prerequisite with the command that
       verifies it, so the gate does not stall discovering a missing dependency mid-run.

    State plainly that Pitfall 2's standing claim ("no non-Steam runner has ever executed a Wine
    command under the sidecar") is retired ONLY by an item-5 PASS — not by this preflight, and not by
    a green test suite.
  </action>
  <verify>
    <automated>grep -q "## Item 5" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md &amp;&amp; grep -q "Pitfall 2" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-G6-ITEM45-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - One specific macOS-permitted Wine action is named, with its UI path and the source citation proving it is not excluded by `tools/index.ts`'s guards.
    - The dispatching channel is named with its registration kind; if send-kind, a separate observable is named because silent failure is the default there.
    - The evidence log lines are quoted as literal shapes read from the emitting source file at planning time, with that file's path.
    - The observable in-app effect is stated separately from the log evidence.
    - Every prerequisite is listed with a verifying command.
    - The document states Pitfall 2 is retired only by an item-5 PASS.
    - `npm run test:ci` exits 0 and `npx tsc --noEmit` exits 0; verbatim `Tests:` line recorded.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user agent → live storefront | The agent string is sent to a real credential origin |
| `getPath('exe')` → Steam VDF / `run.sh` | A shell-derived path becomes an executed launch command |
| machine-local identifiers → repository document | The Steam `userdata` account id is a real account identifier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-G6-29 | Tampering | a UA change drifting into `matchOAuthRedirect` | mitigate | Task 1's acceptance criteria re-assert both host anchors (`localhost` for Epic, `www.amazon.com` for nile — REQ-34.5-02, closing `T-34.4.1-44b`) in every branch, including the branches that ship no code |
| T-34.5-G6-30 | Elevation of privilege | a wrong `exe` value written into a Steam VDF launch command | mitigate | Item 4's preflight requires reading the written `Exe` field and the `run.sh` body back, and a `ps aux` proving the launched process is the shell binary and never bare `node`. `pathShim`'s `exe` case throws loudly when `GAMELIB_SHELL_EXE` is unset rather than writing an empty string |
| T-34.5-G6-31 | Information disclosure | Steam `userdata` account id in a public-fork document | mitigate | Task 2 resolves the id on-machine but commits it REDACTED; the path structure is the evidence, the id is not |
| T-34.5-G6-32 | Repudiation | a send-kind Wine channel's silence read as success | mitigate | Task 3 requires the channel's registration kind to be named and, if send-kind, a separate observable to be named, because `sidecar-send-channels-fail-silently` means absence of error is not evidence |
| T-34.5-G6-33 | Repudiation | shipping an Epic fix the discriminator did not select | mitigate | Task 1 branches strictly on the recorded verdict, three of the four branches ship no code, and the SUMMARY must quote the verdict before describing any change |
| T-34.5-G6-SC | Tampering | npm/cargo installs | mitigate | This plan installs no packages. Any install requires `slopcheck` plus a blocking human checkpoint first |
</threat_model>

<verification>
- `npm run test:ci`, `npx tsc --noEmit` exit 0; counts recorded verbatim, any non-zero exit explained.
- `python3 .planning/phases/34.5-.../ported-channels-gate.py` exits 0 — channel membership is unchanged by this plan.
- `git diff --name-only` shows zero `src-tauri/` paths.
</verification>

<success_criteria>
Epic's fix is whatever the pre-registered discriminator selected — including nothing, if that is what
it selected — with both redirect host anchors re-proven unchanged; and gate items 4 and 5 each have a
verified target, exact invocations, named evidence lines that provably exist in source, and their
prerequisites checked, so neither can be skipped for a third time.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-29-SUMMARY.md` when done
</output>
