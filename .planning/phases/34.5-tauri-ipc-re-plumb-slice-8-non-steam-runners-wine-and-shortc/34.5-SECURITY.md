---
phase: 34.5
slug: tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
status: blocked
threats_open: 3
threats_total: 362
threats_closed: 359
threats_accepted: 3
threats_open_first_pass: 8
asvs_level: 2
block_on: high
created: 2026-08-20
register_authored_at_plan_time: true
audit_unit: "(plan, threat_id) — never threat_id alone, never a range"
shards: [BASE, G6, C4, C5, C6, C7]
---

# Phase 34.5 — Security

> Per-phase security contract: threat register, accepted risks, audit trail.
>
> **State B run** — no SECURITY.md existed; the register was reconstructed from the
> `<threat_model>` blocks of 60 of the phase's 61 PLAN files and audited by six parallel
> `gsd-security-auditor` runs, one per gap cycle.

---

## How this register was built, and why the unit matters

**362 audit units across 281 distinct threat IDs.** The register is keyed by
**`(plan, threat_id)`**, never by threat ID alone and never by range.

Two census errors were caught while building it, both of which would have produced a
falsely-clean result:

1. **Namespace blindness.** A first pass grepping `T-34\.5-[0-9]+` found **67** IDs. The real
   register is **281**, because the gap cycles use namespaced IDs (`T-34.5-G6-*`, `-C4-*`,
   `-C5-*`, `-C6-*`, `-C7-*`) that the pattern cannot match. Auditing on 67 would have skipped
   **214 IDs — 76% of the phase.**
2. **Range-shaped coverage claims.** The SUMMARY files assert coverage as ranges
   (*"T-34.5-18 through T-34.5-22, all mitigate"*). A range is not per-ID evidence; it is the
   exact shape that lets an uncovered ID hide inside a passing statement. Every range was
   expanded and every ID scored on its own named mitigation.

30 threat IDs recur across plans with **different mitigation text each time** (`T-34.5-SC`
alone appears in 21 plans), which is why the ID alone is the wrong key.

**Also noted:** 41 of 61 SUMMARY files have no `## Threat Flags` section at all. Absence of a
flag was treated as *not* evidence of closure.

---

## Threat Register — by shard

| Shard | Plans | Rows | Closed | Open |
|---|---|---:|---:|---:|
| BASE | 34.5-01..48 | 115 | 113 | 2 |
| G6 | 34.5-19..44 | 77 | 76 | 1 |
| C4 | 34.5-32..37 | 37 | 36 | 1 |
| C5 | 34.5-38..42 | 29 | 28 | 1 |
| C6 | 34.5-43..51 | 55 | 52 | 3 |
| C7 | 34.5-52..60 | 49 | 49 | 0 |
| **Total** | | **362** | **354** | **8** |

Arithmetic: `113+76+36+28+52+49 = 354` closed; `2+1+1+1+3+0 = 8` open; `354+8 = 362`.
Recounted from the shard tables, not carried forward.

**This table is the FIRST-PASS result and is deliberately left unedited** — it is what the six
auditors returned on 2026-08-20 before any disposition. After the developer's disposition pass
the same day the live counts are **358 closed / 4 open / 3 accepted** (`354 + 4 dispositioned =
358`; `8 − 4 = 4`). The frontmatter carries the live numbers; this table carries the audit's.
Do not "fix" one to match the other — they answer different questions.

### Where the per-row evidence lives

Evidence coverage is **asymmetric** and is stated rather than smoothed over — three shards
wrote evidence files, three returned their tables inline only:

| Shard | Per-row evidence file |
|---|---|
| G6 | `34.5-SECURITY-EVIDENCE-G6.md` (76-row table with file:line citations) |
| C5 | `34.5-SECURITY-EVIDENCE-C5.md` |
| C6 | `34.5-SECURITY-EVIDENCE-C6.md` |
| BASE | **none written** — 115 rows verified, evidence grouped by plan in the audit return only |
| C4 | **none written** — 37 rows, full per-row table in the audit return only |
| C7 | **none written** — 49 rows, full per-row table in the audit return only |

For BASE, C4 and C7 the closed-row citations are **not durably recorded in this repo**. The
open-row evidence for those shards is reproduced in full below, so nothing actionable is lost;
what is lost is the ability to re-read a *closed* row's proof without re-running the audit.
Re-run `/gsd-secure-phase 34.5` with a write-evidence instruction if that matters later.

---

## Open Threats — 3 rows, 2 root causes

*(Was 8 rows / 5 causes at first audit. R1 and R5 were dispositioned by the developer on
2026-08-20 — see "Dispositioned 2026-08-20" below and the Accepted Risks Log. R3 was CLOSED by a
fix on 2026-08-20 (quick-260820-fyl) — not dispositioned as accepted risk, the code defect it
named was actually resolved.)*

### R2 — A promised `grep` assertion that does not exist (2 rows, Elevation of Privilege)

| Threat ID | Plan |
|---|---|
| `T-34.5-17` | 34.5-05 |
| `T-34.5-32` | 34.5-09 |

The register states: *"`grep` asserts no `process.platform`/`isMac`/`isLinux` branch was
introduced outside comments"* in `src/backend/sidecar/wineToolsFlowRegistration.ts`.

The invariant is **true today** — a comment-stripped scan finds zero live occurrences (only
comment references at lines 207, 218, 222). **The assertion does not exist.** Searched
`wineToolsFlows.test.ts`, `runnerSliceRegistration.test.ts`, `testContainment.test.ts`,
`sidecarRejectionGuard.test.ts`. The `stripSourceComments` + `not.toMatch(/process\.platform/)`
pattern exists only in `humbleLoginFlows.test.ts`, guarding a **different module**
(`humble/loginWindowSeam.ts`) for a **different requirement** (REQ-34.4.1-13).

A future edit reintroducing a platform branch ships with zero CI signal. Classified OPEN on the
rule that *a grep assertion must be able to fail against a known-bad input* — here there is no
assertion to fail. "True today by inspection" is a coincidence, not a control.

### R3 — CLOSED 2026-08-20 — Keyring cache resurrects a signed-out session (1 row, Elevation of Privilege)

`T-34.5-G6-14` — plan 34.5-25. **The only open row that is a live code defect.**
Independently re-verified during this audit, not taken on the auditor's word.

Declared mitigation: *"setToken/clearToken invalidate the success cache, the failure memo
**AND the in-flight promise**."*

Implementation does the first two only. `keyringTokenStore.ts:435-440` clears `cachedToken`,
`cachedAvailable`, `failedTokenAt`, `failedTokenReason`; its own doc comment states it
*"Does NOT touch an in-flight `pendingToken`/`pendingAvailable` promise."*

The consequence is at `keyringTokenStore.ts:347`:

```ts
const value = typeof result === 'string' ? result : ''
this.cachedToken = { value }        // unconditional — no generation/epoch check
```

A read in flight when `clearToken()` fires resolves afterwards and writes its **pre-signout**
value back into `cachedToken`. Tests at `keyringTokenStore.test.ts:599,733` exercise
invalidation **sequentially only**; nothing drives a concurrent in-flight `getToken()`.

Two facts make this practical rather than theoretical: the race window is bounded by
`KEYRING_READ_TIMEOUT` = **45 seconds**, and `34.5-KEYRING-ARM-SESSION.md` recorded keyring
reads racing Keychain approval at roughly **9:1**. "Sign out while a Keychain prompt is still
pending" is a plausible sequence.

The code's rationale — mirroring `bounded_keyring_read`'s *"abandoned, not cancelled"* on the
Rust side — is defensible; you cannot un-send the request. **But it was never adopted as this
row's disposition, and the register still claims the in-flight promise is invalidated.**
Resolve by adding a generation counter so a stale resolve cannot write, or by amending the
register and logging an accepted risk. Do not leave the two disagreeing.

**CLOSED 2026-08-20 (quick-260820-fyl).** The fix taken: a monotonic `cacheEpoch` on
`SidecarKeyringSlotStore`, bumped by `invalidateCache()`, captured at entry by
`fetchToken()`/`fetchAvailable()`, gating FOUR write sites — the success cache (`cachedToken`),
the failure-memo arm (`failedTokenAt`/`failedTokenReason`), the failure-memo clear, and the
availability cache (`cachedAvailable`). This is one more write site than this finding's own
audit cited — the audit named only `cachedToken` at `keyringTokenStore.ts:347`; the failure-memo
arm/clear and the availability cache carried the identical unconditional-write defect and are
closed by the same fix.

Verified by three concurrent tests in `keyringTokenStore.test.ts` (`in-flight read superseded by
clearToken() -- cache epoch guard (quick-260820-fyl, T-34.5-G6-14)`), each RED-proven by removing
only its own guard and confirming the mapped test fails before restoring it — recorded verbatim
in `.planning/quick/260820-fyl-fix-t-34-5-g6-14-keyring-cacheepoch-gene/SUMMARY.md`. The two
pre-existing sequential invalidation tests this finding cited as insufficient (`keyringTokenStore.test.ts:599,733`)
still pass unchanged; they are joined, not replaced.

What was explicitly NOT done, so this register does not overclaim in the other direction: the
in-flight request is still not cancelled (it cannot be un-sent), and a caller already joined to
it before `clearToken()` fired still receives the pre-signout value it asked for — only the
durable cache is protected. The declared mitigation wording is corrected from "invalidate ...
the in-flight promise" to **"a stale in-flight result can no longer be written back over cleared
state."**

Commit: `f339137c6` (`fix(quick-260820-fyl): gate keyring cache writes behind a generation epoch
(T-34.5-G6-14)`).

### R4 — A `transfer` whose target retired without its own observation (1 row, DoS)

`T-34.5-C6-06` — plan 34.5-43, disposition **`transfer`** → ledger row `U-34.5-16`.

A transfer is only closed if the target still carries the threat. `U-34.5-16`'s stated
retirement bar is a **live details-page open with no `sidecar invoke timed out` line for
`getInstallInfo`**. It is marked RETIRED in `34.5-UNTESTED-ITEMS.md` on **structural proof
only** — `LONG_RUNNING_CHANNELS` membership plus `cargo test`/`jest`. No live `getInstallInfo`
observation appears in `34.5-LIVE-GATE-RERUN-3.md` or `-RERUN-4.md`.

This is a transferred threat closed against a bar it did not meet. Either run the live
observation `U-34.5-16` demands, or amend the row to state that structural proof was accepted
as sufficient — but do not leave it claiming an unmet live observation.

---

## Dispositioned 2026-08-20 — R1 (3 rows) and R5 (1 row)

Developer decision, taken after reviewing the evidence row by row. **4 rows leave the open
count: 8 → 4.** Both write to the Accepted Risks Log below.

### R1 — CLOSED. The audit's own framing was wrong; corrected here.

**Rows:** `T-34.5-C5-13` (34.5-41), `T-34.5-C6-36` (34.5-50), `T-34.5-C6-49` (34.5-51).

The first audit pass reported these as *"login-origin transcription never live-obtained"* and
cited `src-tauri/src/main.rs:3659` (`initial_visible = false`) as the cause. **Both claims are
wrong and are struck.** `main.rs:3659` is the deliberate macOS sheet-attachment behaviour and
has nothing to do with this. The origin display **has** been live-confirmed.

All three rows carry the same two clauses. Scored separately rather than as one verdict:

| Clause | Verdict |
|---|---|
| `www.amazon.com` host anchor holds (REQ-34.5-02, inherited `T-34.4.1-44b`) | **SATISFIED** — gate 5 item 2 PASS, research Assumption A1 CONFIRMED |
| Login-window origin displayed and transcribed (`U-34.5-05`) | **SATISFIED in part** — Epic showed `www.epicgames.com`, GOG showed `login.gog.com`; at gate 5 the operator transcribed `https://login.gog.com` and **three screen frames** show the banner rendered with the cancel strip simultaneously visible, unoccluded and clickable |
| …the *before/after pair* — banner text CHANGING within one flow | **UNMEASURABLE** — see below |

**Why UNMEASURABLE and not OPEN.** The banner re-texts from `on_page_load`
(`main.rs:3822-3843`), but the GOG sheet tears down in the *same second* as the
`login.gog.com` → `embed.gog.com` transition — `nav host=embed.gog.com` and `status=captured`
share a timestamp across all three flows — so the banner never re-texts and the second half of
the pair cannot be produced. This is the **second consecutive retarget of this same observation
landing on an impossibility**: D-CYCLE7-A had already moved it off the `NSWindow` title bar
because `F-34.5-G6-16` established that AppKit sheets render no title-bar UI at all.

Given two independent impossibility findings, the observation is the defect, not the product.
Holding these rows OPEN would assert the product failed a test it cannot be given — the same
reasoning already applied to `U-34.5-10`. **Rows CLOSED; the unmeasurable half is recorded as
accepted risk `AR-34.5-01`.**

`U-34.5-05` and `U-34.5-31` stay OPEN in the ledger by their own rules — a ledger row retires
only on its own named observation, and a half-pair is not that. **The ledger and this register
now disagree deliberately, and that is not drift:** the ledger tracks *was this observed*, this
register tracks *is this risk dispositioned*. Do not "reconcile" them by retiring the ledger
rows.

### R1b — the residual that actually matters, now logged: the banner is spoofable

Not one of the 8 register rows — it is ledger row `U-34.5-31`, which would otherwise have
carried a real, undispositioned risk out of this phase inside a ledger nobody reads as a
security artifact. Logged here as `AR-34.5-03`.

The banner renders **inside the attacker-controlled document**. Reading
`login_origin_banner_script` (`main.rs:1597-1646`), the concrete mechanism is z-index headroom:

```
banner.style.zIndex = '2147482999'      // origin banner
                      '2147483000'      // cancel strip
                      '2147483647'      // CSS maximum
```

That ordering protects the cancel strip from the banner, but leaves **648 values above both**.
A hostile page setting `z-index: 2147483001` covers the banner *and* the cancel strip. The
debounced `MutationObserver` re-ensures the element **exists** and re-texts it; it never checks
**occlusion or z-order**. `pointerEvents: none` means the banner cannot even detect
interference. (`window.top !== window` does correctly limit the script to the top frame, and
`textContent` from the shell-resolved origin means page content can never write the string.)

Never tested against a page that actively fights it — the shipped mitigations are proven only
by Rust `#[cfg(test)]` assertions over the emitted script text.

### R5 — ACCEPTED, and the false success criterion corrected in place

`T-34.5-C4-50` — plan 34.5-37. Accepted as `AR-34.5-02`.

Two facts reduce the residual below what the raw finding suggests:

- **The credential went into the CONTROL arm, not the suspect one.** Arm E is **Electron** —
  the client already shipping. The arm under investigation was Arm T (Tauri), which never
  reached `status=captured` and took two full 300s timeouts. The precondition's own stated
  hazard — *"a page whose behaviour is actively under investigation"* — barely applies to the
  arm that received the credential.
- **The finding is over-satisfied, not weakened.** The pre-registered rule needed "Electron
  interactive AND Tauri non-interactive"; a completed login proves interactivity *a fortiori*.
  Nothing leaked — the one-time code is redacted, verified independently by `T-34.5-C4-51`
  (closed).

**Action taken, not just recorded:** `34.5-37-PLAN.md`'s `<success_criteria>` asserted *"both
arms were run on real hardware without a real Epic credential"* — false, and sitting in the
block a reader consults to judge whether the experiment was sound. The SUMMARY disclosed the
deviation at the time; the PLAN never did. The clause is **struck in place** with a pointer to
the deviation and to `AR-34.5-02`.

---

## Adjacent findings — outside the register, not counted in the open rows

### A1 — Plan 34.5-61: HIGH severity, zero threat model, undisposed residual

`34.5-61` is the **only plan in the phase with no `<threat_model>` block**, and its own
frontmatter carries `security_severity: high`. It fixed **three** credential-logging leaks in
`getRunnerCallWithoutCredentials` (`launcher.ts:1877+`), all verified present at HEAD:

| Finding | Leak |
|---|---|
| `F-34.5-G6-22` | nile PKCE `code_verifier` via `callRunner()` — a **different call site** than the two plan 34.5-53's mitigation covered |
| `F-34.5-G6-24` | GOG private-branch `--password` — user-chosen, reused secret, never previously redacted |
| `F-34.5-G6-25` | A PowerShell `-ArgumentList` regex that never matched the real backtick-quoted shape, so redaction **silently no-op'd on Windows** for `--code`/`--token` too |

Fix verified: `sensitiveArg` now covers `['--code','--token','--code-verifier','--password','--serial','--client-id']`; `launcher_callRunner.test.ts` 10/10.

**The residual has no disposition anywhere** — no `accept`, no `transfer`, no accepted-risk
entry. It exists only as `U-34.5-33`, OPEN: the GOG private-branch-password fix and the
PowerShell branch have never run on hardware (every gate in this phase ran on macOS; the
PowerShell branch is Windows-only and unit-probed only).

`F-34.5-G6-22` also vindicates the per-`(plan, ID)` keying: plan 53's mitigation for
`T-34.5-C7-09` was correctly implemented and **still missed the sibling call site**.

### A2 — `F-34.5-G6-17`: nile DEBUG-level raw OAuth `code`

Reported by the C6 shard as uncovered by any C6 threat ID, self-disclosed by plan 34.5-51's
SUMMARY and deferred to gap cycle 7's register. C7's audit found plan 34.5-53 redacting nile
credentials at `nile/user.ts:94,126`. **Whether that closes `F-34.5-G6-17` specifically was not
confirmed by either shard** — treat as needs-confirmation, not closed.

### A3 — Steam account id in a published commit message (no action; decision already binding)

The C5 shard flagged a real Steam account id (`userdata/35601368`) in the message of commit
`d48b4d964`. Publication state was checked rather than assumed:

```
d48b4d964  2026-08-03  docs(34.5-42): close gap cycle 5 ...
  contained in: remotes/gamelib/fix/steam-native-install-stability   ← PUBLISHED
```

The binding developer decision of 2026-08-02 is **redact-forward only**: published files and
git history are deliberately left untouched; only a *new unredacted account id in an
unpublished file* remains blocking. This commit is published, so it falls inside that
decision. **Not a new finding, not re-raised.**

The one genuinely new fact: the 2026-08-02 scrub was **content-only** and never examined commit
messages, so this instance survived in a form the remediation did not cover. Worth adding
commit messages to future credential sweeps — a pattern improvement, not a remediation.

---

## Accepted Risks Log

Three risks accepted 2026-08-20 by the developer, covering R1 (3 rows), R1b (ledger row, not a
register row) and R5 (1 row). **The 4 remaining open rows — R2 ×2, R3, R4 — are NOT accepted.**

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| `AR-34.5-01` | `T-34.5-C5-13` (34.5-41), `T-34.5-C6-36` (34.5-50), `T-34.5-C6-49` (34.5-51) — the before/after half of `U-34.5-05` | **UNMEASURABLE, not unmitigated.** Origin display is live-confirmed (Epic `www.epicgames.com`, GOG `login.gog.com`; gate 5 transcribed `https://login.gog.com` with 3 corroborating screen frames, cancel strip visible and clickable) and the `www.amazon.com` anchor clause PASSED at gate 5. Only the *change-within-one-flow* pair is missing, and it cannot be produced: the GOG sheet tears down in the same second as the origin transition, so `on_page_load`'s re-text loses the race. Second consecutive retarget of this observation to land on an impossibility (the first was the `NSWindow` title bar, `F-34.5-G6-16`). Holding it open would assert a failure the product was never given a chance to avoid. | Developer | 2026-08-20 |
| `AR-34.5-02` | `T-34.5-C4-50` (34.5-37) | Real Epic credential entered on Arm E against the experiment's own no-credentials precondition. Accepted because the credential went into the **control** arm (Electron — the shipping client), not the arm under investigation (Tauri); the verdict is over-satisfied rather than weakened (a completed login proves interactivity *a fortiori*); nothing reached the repo (one-time code redacted, verified by `T-34.5-C4-51`); and the deviation was self-disclosed in the SUMMARY at the time. **Remediation applied, not just accepted:** the false clause in `34.5-37-PLAN.md`'s `<success_criteria>` is struck in place. | Developer | 2026-08-20 |
| `AR-34.5-03` | Ledger row `U-34.5-31` (no register row — logged here so it does not leave the phase undispositioned) | The in-page origin banner is spoofable by the document it renders inside. Concrete mechanism: `zIndex 2147482999` (banner) / `2147483000` (cancel strip) leaves **648 values below the CSS maximum**, so a hostile page at `2147483001` covers both; the `MutationObserver` re-ensures existence and text but never checks occlusion or z-order, and `pointerEvents: none` means the banner cannot detect interference. Never tested against a page that fights it. Accepted because exploitation requires hostile content **already executing inside the login sheet**, at which point the attacker has largely won regardless — this is defence-in-depth that does not survive an active attacker, not a primary control. Cheap hardening if revisited: raise the banner to `2147483647` and the cancel strip just below. | Developer | 2026-08-20 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-08-20 | 362 | 354 | 8 | `/gsd-secure-phase 34.5` — six parallel `gsd-security-auditor` shards |
| 2026-08-20 | 362 | 358 | 4 | Developer disposition pass — R1 (3 rows) closed UNMEASURABLE as `AR-34.5-01`; R5 (1 row) accepted as `AR-34.5-02` with the false success criterion struck in `34.5-37-PLAN.md`; `U-34.5-31` logged as `AR-34.5-03`. R2/R3/R4 deliberately left OPEN. |
| 2026-08-20 | 362 | 359 | 3 | quick-260820-fyl — R3 (`T-34.5-G6-14`) closed by implementing the cache-epoch guard (option (a) from the G6 recommendation), with three RED-proven concurrency tests. R2/R4 remain OPEN. |

---

## Gate

**PHASE 34.5 SECURITY BLOCKED — `threats_open: 3`.**

Phase 34.5 is closed for delivery purposes (fifth blocking live gate, 4/0/0/0, 2026-08-19),
but that gate scored four live items; it did not discharge this register. The two instruments
are independent and this one is not satisfied.

Remaining: **R2** (2 rows — a promised `grep` assertion that does not exist), **R4** (1 row — a
`transfer` whose target retired on a bar it did not meet). None is accepted.

No next-phase routing is emitted while `threats_open > 0`.
