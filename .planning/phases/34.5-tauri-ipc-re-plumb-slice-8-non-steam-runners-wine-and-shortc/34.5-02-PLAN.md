---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/sidecar/oauthLoginCapture.ts
  - src/backend/sidecar/__tests__/oauthLoginCapture.test.ts
autonomous: true
requirements: [REQ-34.5-02]

must_haves:
  truths:
    - "D-11: nile's OAuth redirect matcher is HOST-ANCHORED before any credential flow exists — the anchor lands in wave 1, not folded into the Amazon auth plan, because 34.4.1's host-free match was only harmless while the receiving channel rejected."
    - "T-34.4.1-44b is closed for nile. Zoom's half of the obligation is VOID (D-02 drops zoom permanently) and no zoom code is touched."
    - "The `www.amazon.com` value is MEDIUM confidence (research Assumption A1 — verified against upstream `imLinguin/nile`, not against GameLib's vendored binary), so this plan ships an origin-evidence log line that lets live-gate item 3 confirm or falsify it against one real redirect."
    - "The evidence log records the redirect URL's ORIGIN ONLY. The authorization code and the full query string are never logged (ASVS V5/V6 — a captured code is a credential)."
    - "`legendary` and `gog` are already host-anchored and are not modified — three consistent host-anchored cases in one switch after this plan, not one exception."
  artifacts:
    - path: "src/backend/sidecar/oauthLoginCapture.ts"
      provides: "Host-anchored nile case in matchOAuthRedirect"
      contains: "www.amazon.com"
    - path: "src/backend/sidecar/__tests__/oauthLoginCapture.test.ts"
      provides: "Rejection cases for a non-amazon origin carrying the openid.oa2.authorization_code param"
  key_links:
    - from: "src/backend/sidecar/oauthLoginCapture.ts matchOAuthRedirect('nile', url)"
      to: "the captured authorization code consumed by authAmazon (plan 34.5-10)"
      via: "hostname equality check before the searchParams read"
      pattern: "hostname !== 'www.amazon.com'"
---

<objective>
Land wave-1 seam 2: host-anchor nile's OAuth redirect matcher so an authorization code can only be
captured from Amazon's own origin, and leave behind the evidence hook that lets the live gate
confirm the anchor value against one real redirect.

Purpose: this phase is the first to MINT a real credential from that captured value. The inherited
`T-34.4.1-44b` obligation states plainly that the anchor must land before the credential flow
exists, not alongside it.

Output: three host-anchored cases in one switch, and a way to falsify the chosen host without
guessing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-RESEARCH.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PATTERNS.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md

<interfaces>
The sibling cases already in the same switch (src/backend/sidecar/oauthLoginCapture.ts) — the
templates to copy, NOT to modify:

  case 'legendary':  if (parsed.hostname !== 'localhost') return null
  case 'gog':        if (parsed.hostname !== 'embed.gog.com' || parsed.pathname !== '/on_login_success') return null
                     const code = parsed.searchParams.get('code'); if (!code) return null
                     return { code, redirectUrl: url }

The case being replaced (host-free, inherited from the Electron original):

  case 'nile':       const code = parsed.searchParams.get('openid.oa2.authorization_code')
                     if (!code) return null
                     return { code, redirectUrl: url }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Host-anchor the nile case and add the origin-evidence log</name>
  <files>src/backend/sidecar/oauthLoginCapture.ts</files>
  <read_first>
    - src/backend/sidecar/oauthLoginCapture.ts (full file — all four `matchOAuthRedirect` cases, the URL parsing/guard code above the switch, and the existing T-34.4.1-44b comment block on the nile case that this plan retires)
    - .planning/phases/34.5-.../34.5-PATTERNS.md § "src/backend/sidecar/oauthLoginCapture.ts" (the gog case verbatim as the shape to copy, and the exact recommended nile replacement)
    - .planning/phases/34.5-.../34.5-RESEARCH.md § "Architecture Patterns" Pattern 2 and § "Assumptions Log" A1 (why `www.amazon.com`, and why it must be confirmed empirically before it is trusted)
    - .planning/ROADMAP.md § "Phase 34.5" (the inherited-obligation note naming T-34.4.1-44b — this plan is what closes it)
  </read_first>
  <action>
    Replace the `nile` case body with a host-anchored form matching its `gog` sibling's shape:
    reject when `parsed.hostname !== 'www.amazon.com'`, then read
    `parsed.searchParams.get('openid.oa2.authorization_code')`, then return
    `{ code, redirectUrl: url }`. Do not add a pathname constraint — upstream nile's
    `openid.return_to` is the bare origin, so a pathname check would be an invented constraint.

    Replace the existing T-34.4.1-44b comment block with one that records: the obligation is CLOSED
    for nile by this line; zoom's half is void (D-02 drops zoom permanently); the `www.amazon.com`
    value derives from `imLinguin/nile`'s `openid.return_to` and is MEDIUM confidence until live-gate
    item 3 confirms it; and if a real capture shows a country-TLD variant (e.g. `amazon.co.uk`) the
    correct response is a documented suffix match, not the removal of the anchor.

    Add the evidence hook: when the runner is `nile` AND the URL carries an
    `openid.oa2.authorization_code` param BUT the hostname does not match, log one warning line
    containing `parsed.origin` and nothing else before returning `null`. The line MUST NOT contain
    the code, the full URL, or `parsed.search` — a captured authorization code is a credential.
    Prefix the line so it is greppable from `~/Library/Logs/GameLib/gamelib.log` during the live
    gate; state in a comment that this line is the sole automated evidence path for Assumption A1
    and must survive until the gate records item 3.

    Do not touch the `legendary` or `gog` cases. Do not touch or re-add any zoom case.
  </action>
  <acceptance_criteria>
    - `grep -c "www.amazon.com" src/backend/sidecar/oauthLoginCapture.ts` returns at least 1.
    - `grep -c "openid.oa2.authorization_code" src/backend/sidecar/oauthLoginCapture.ts` is unchanged from before this task.
    - `git diff src/backend/sidecar/oauthLoginCapture.ts` shows zero changed lines inside the `legendary` and `gog` cases.
    - The evidence log statement references `parsed.origin` and does NOT reference `code`, `url`, or `parsed.search` on the same statement — verify by reading the emitted line, not by assuming.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>nile only accepts a code from `www.amazon.com`, and a rejected candidate leaves an origin-only breadcrumb the live gate can read.</done>
</task>

<task type="auto">
  <name>Task 2: Extend oauthLoginCapture.test.ts with the anchor's rejection cases</name>
  <files>src/backend/sidecar/__tests__/oauthLoginCapture.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/oauthLoginCapture.test.ts (full file — the existing 39 assertions across all four runner shapes, and the naming convention its cases use; EXTEND this file, never restart it)
    - src/backend/sidecar/oauthLoginCapture.ts (as edited by Task 1)
    - .planning/phases/34.5-.../34.5-VALIDATION.md § "Per-Task Verification Map" row for REQ-34.5-02 (the secure behaviour this test must assert: a non-`www.amazon.com` origin carrying the param is REJECTED)
  </read_first>
  <action>
    Add nile cases to the existing suite without deleting or weakening any current case:
      1. A `https://www.amazon.com/?openid.oa2.authorization_code=<value>` URL returns
         `{ code: '<value>', redirectUrl: <the url> }`.
      2. An attacker-shaped origin carrying the identical param — use at least
         `https://evil.example/?openid.oa2.authorization_code=…` and
         `https://amazon.com.evil.example/?openid.oa2.authorization_code=…` (a suffix-confusion
         shape) — returns `null`.
      3. A near-miss legitimate-looking origin, `https://amazon.com/?openid.oa2.authorization_code=…`
         (no `www.`), returns `null`. Add a comment that this case is deliberately pinned to the
         exact host and is the case live-gate item 3 may falsify.
      4. `https://www.amazon.com/` with NO code param returns `null`.
      5. The evidence log fires exactly once for case 2 and the logged payload contains the origin
         but does NOT contain the code value — spy on the logger/console method the implementation
         uses and assert on the emitted string.

    Also add a guard case asserting the `legendary` and `gog` behaviours are unchanged by re-running
    one existing positive assertion for each, so a future edit to this switch cannot silently break
    a sibling.
  </action>
  <acceptance_criteria>
    - `cd src/backend && npx jest sidecar/__tests__/oauthLoginCapture.test.ts` exits 0.
    - The suite gains at least 6 new assertions and deletes zero existing ones (compare test counts before/after and record both in the SUMMARY).
    - A case exists asserting `matchOAuthRedirect('nile', 'https://amazon.com.evil.example/?openid.oa2.authorization_code=abc')` returns `null`.
    - A case asserts the evidence log's emitted string does NOT include the literal code value used in that case.
  </acceptance_criteria>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/oauthLoginCapture.test.ts</automated>
  </verify>
  <done>The anchor is proven to reject suffix-confusion and near-miss origins, and the evidence log is proven not to leak the code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote web content → `matchOAuthRedirect` | Any page the login window navigates to can put arbitrary query params on its own URL; only the origin is attacker-hard |
| `matchOAuthRedirect` → `authAmazon` credential mint | From this phase onward the captured value is exchanged for a real Amazon credential, which is exactly why the anchor precedes the mint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-05 | Spoofing | `matchOAuthRedirect`'s `nile` case | mitigate | Exact-hostname check `parsed.hostname !== 'www.amazon.com'` executed BEFORE the param read (ASVS V5). Suffix-confusion (`amazon.com.evil.example`) and missing-subdomain (`amazon.com`) shapes are both test-pinned. |
| T-34.5-06 | Information Disclosure | the origin-evidence log line | mitigate | Logs `parsed.origin` only; a test asserts the emitted string does not contain the code value. Never logs `url`, `parsed.search`, or the param. |
| T-34.5-07 | Spoofing | a wrong anchor value silently breaking Amazon login | mitigate | Live-gate item 3 captures one real redirect and confirms the origin before the anchor is trusted; the evidence log is the mechanism that makes the failure diagnosable rather than silent (research Assumption A1). |
| T-34.5-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `cd src/backend && npx jest sidecar/__tests__/oauthLoginCapture.test.ts` exits 0.
- `npx tsc --noEmit` exits 0.
- `git diff` shows the `legendary` and `gog` cases untouched.
</verification>

<success_criteria>
nile's matcher accepts a code only from `www.amazon.com`; the inherited `T-34.4.1-44b` obligation is
closed for the one runner that still carried it; and the chosen host can be confirmed or falsified
from a log line rather than from an assumption.
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-02-SUMMARY.md` when done
</output>
