---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 19
type: execute
wave: 3
depends_on: [34.5-17, 34.5-18]
gap_closure: true
closes_gaps: [G-3, G-2, G-4, G-6]
requirements: [REQ-34.5-01, REQ-34.5-02, REQ-34.5-03, REQ-34.5-12]
autonomous: true
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md

must_haves:
  truths:
    - "A NEW gate document exists with `verdict: null` at authoring time, written before any live work — the superseded FAILED run stays untouched in `34.5-LIVE-GATE.md`"
    - "Precondition 5's defect is closed: the re-run cites the sidecar's own boot log line, so it no longer contradicts the read-from-the-log-file precondition"
    - "A precondition proves the G-1 fix is present in the exact build under test, before any login is driven"
    - "All five items are covered, including item 4, which was NOT ATTEMPTED last run"
    - "The recording rules for A1, A2 and research Pitfall 2 are written down before the run, so a PASS cannot later be read as proving more than it did"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md"
      provides: "The re-run contract: preconditions, 5 items, empty result slots, verdict skeleton"
      contains: "verdict: null"
  key_links:
    - from: "34.5-LIVE-GATE-RERUN.md precondition 7"
      to: "src/backend/sidecar/bootstrap.ts"
      via: "citing the `[bootstrap] GAMELIB_SHELL_EXE received=` log line as the evidence source"
      pattern: "GAMELIB_SHELL_EXE received="
    - from: "34.5-LIVE-GATE-RERUN.md"
      to: "34.5-LIVE-GATE.md"
      via: "explicit supersession pointer, never an overwrite"
      pattern: "34.5-LIVE-GATE.md"
---

<objective>
Write the re-run contract BEFORE the live work, exactly as `34.5-LIVE-GATE.md` was written before
the first run (wave 1, plan 03) and as Phase 34.4.1 did across its three gate documents.

Two hard rules from the gap scope: the re-run goes in a NEW document,
`34.5-LIVE-GATE-RERUN.md`, with `verdict: null` at authoring time; and `34.5-LIVE-GATE.md` is NOT
overwritten — it holds the superseded FAILED run and stays as the record.

This plan runs nothing live. It fixes the contract so the run is possible: the old precondition 5
is unsatisfiable as written (it asks for a value from `gamelib.log` that only ever appears on the
shell's terminal), and no precondition currently proves the G-1 fix is present in the build under
test.

Purpose: a human's live session is expensive; the contract must be correct before it starts.
Output: `34.5-LIVE-GATE-RERUN.md`, authored and unrun.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-CONTEXT.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-PORTED-CHANNELS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-17-SUMMARY.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-18-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the re-run contract's frontmatter and seven preconditions</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md</files>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE.md` IN FULL — frontmatter shape, the five preconditions and their RESULT-block convention, and the precondition-5 defect analysis with its two named closures
    - `.planning/phases/34.5-.../34.5-15-SUMMARY.md` § "Next Phase Readiness" — the credential-backup state and the orphan-process finding
    - `.planning/phases/34.5-.../34.5-18-SUMMARY.md` — the four verbatim `[bootstrap]` log prefixes this contract must cite
    - `.planning/phases/34.5-.../34.5-CONTEXT.md` D-08 — the no-partial-pass blocking rule
    - `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-3.md` — the precedent for a re-run document that supersedes without overwriting (its header and verdict table are enough; do not read the whole file)
  </read_first>
  <action>
    Create `34.5-LIVE-GATE-RERUN.md`. Frontmatter: `phase`, `plan: 19`,
    `executed_by: 34.5-20-PLAN.md`, `type: live-gate`, `status: pending`, `blocking: true`,
    `created: <today>`, `verdict: null`, `items_total: 5`, `items_passed: 0`, `items_failed: 0`,
    `supersedes: 34.5-LIVE-GATE.md`. Open the body by stating that this is a CONTRACT written before
    any live work, that it supersedes but does NOT replace `34.5-LIVE-GATE.md` (which holds the
    FAILED first run of 2026-08-01 and must not be edited), and that D-08's no-partial-pass rule
    applies unchanged — any FAIL means the phase does not close and another gap cycle follows, with
    no advisory mode and no pre-authorized override.

    Write seven numbered preconditions, each ending with an empty `RESULT —` slot for plan 34.5-20
    to fill:

    1. **Phase 34.4.1's own blocking live gate has recorded PASS on item 1.** Satisfied on
       2026-08-01 by `34.4.1-LIVE-GATE-RERUN-3.md` (4/4 PASS); carry the verbatim quoted result line
       across from `34.5-LIVE-GATE.md` precondition 1, together with its warning that
       `34.4.1-LIVE-GATE.md` holds only the superseded FAILED run and must never be quoted as
       evidence of a PASS.
    2. **A clean machine.** `ps aux | grep -E "gamelib-shell|sidecar.js"` is run BEFORE launching and
       anything already running is killed. State why: an orphaned shell/sidecar pair from an earlier
       session was alive during the last run, contending for `gog_store`, `nile_store` and keyring
       slots. Record the command output verbatim both before the kill and after.
    3. **A clean rebuild and a real `pnpm tauri:dev` launch**, with the sidecar confirmed alive via
       `~/Library/Logs/GameLib/gamelib.log` growing and a `ps` listing showing both processes
       simultaneously. Sidecar output is read from that log file, NEVER from the `pnpm tauri:dev`
       terminal (stdout is the RPC pipe — the standing `sidecar-console-and-logger-are-invisible`
       finding). Keep `tail -f` on the log for the whole run.
    4. **The G-1 fix is present in THIS build — proven from the log, not from git.** Quote verbatim
       from this session's `gamelib.log`: the `[bootstrap] appRoot resolved=` line with its
       `source=` field, the `[bootstrap] publicDir resolved=` line showing `exists=true`, and all
       four `[bootstrap] runner binary <name> path=... exists=true` lines. State that a
       `[bootstrap] SIDECAR ASSET ROOT DEFECT` line anywhere in the log BLOCKS the run — items 1-3
       must not be attempted while it is present. Use the exact prefixes recorded in
       `34.5-18-SUMMARY.md`; if they differ from the strings above, the summary wins and this
       contract is corrected to match.
    5. **Signed OUT of Epic, GOG and Amazon — verified, not assumed.** The Epic and GOG credentials
       were moved aside during the 2026-08-01 run and the backups are expected to still be in place:
       originals `~/Library/Application Support/GameLib/legendaryConfig/legendary/user.json` and
       `~/Library/Application Support/GameLib/gog_store/auth.json`; backups the same paths with a
       `.pre-34.5-15-gate.bak` suffix. This contract ASSUMES that aside state persists and REQUIRES
       it be verified with `ls`/`test -f` on both original paths and both backup paths, recorded
       verbatim. If a credential has reappeared, move it aside again by the same recoverable
       procedure: `cp -p` to a NEW suffix, `cmp` to prove byte-identity, and only then remove the
       original — never delete blind. Amazon was already signed out; re-verify
       `~/Library/Application Support/GameLib/nile_config/` and `nile_store/` hold no auth state
       rather than assuming it.
    6. **`npm run test:ci`, `npx tsc --noEmit`, `cd src-tauri && cargo check`, `cargo test`** — all
       four run at the HEAD the gate actually runs on, exit codes recorded VERBATIM (not rounded to
       "green"), with a one-line explanation of any non-zero exit that is a known pre-existing flake
       rather than a regression. State that the 2026-08-01 readings (3447 jest tests, 80 Rust tests)
       are a floor, not a target: this gap cycle adds suites, so a higher count is expected and a
       LOWER one must be explained before the run proceeds.
    7. **`GAMELIB_SHELL_EXE` read back from `gamelib.log`, from the SIDECAR's own receipt line.**
       Cite `[bootstrap] GAMELIB_SHELL_EXE received=` — the value the child actually observed — and
       state explicitly that this closes the gate-contract defect recorded in `34.5-LIVE-GATE.md`
       precondition 5 by the PREFERRED of its two named options, so preconditions 3 and 7 no longer
       contradict each other. Carry the dev-vs-packaged caveat across verbatim: this value comes
       from a dev build where `current_exe()` resolves to `src-tauri/target/debug/gamelib-shell`,
       while a packaged build resolves through a different branch (`spawn_sidecar_packaged`,
       `main.rs:1268`), so an item-4 PASS in dev does NOT transfer to the packaged artifact. Add the
       parallel limit for this cycle: residual `R-34.5-G1-PKG` (the packaged asset root, recorded in
       `34.5-APP-ROOT-SWEEP.md` § 3) is likewise NOT covered by a dev PASS.
  </action>
  <verify>
    <automated>test -f .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md && grep -c "^verdict: null" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md && git status --porcelain .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - The new file exists; its frontmatter carries `verdict: null`, `status: pending`,
      `blocking: true`, `items_passed: 0`, `items_failed: 0`, `supersedes: 34.5-LIVE-GATE.md`.
    - `git status --porcelain` reports NOTHING for `34.5-LIVE-GATE.md` — zero bytes changed in the
      superseded run's document.
    - Exactly seven numbered preconditions, each with an empty `RESULT —` slot.
    - Precondition 7 cites `[bootstrap] GAMELIB_SHELL_EXE received=` and states it closes the old
      precondition 5's defect via the preferred option.
    - Precondition 4 names all four `[bootstrap]` prefixes and states that a
      `SIDECAR ASSET ROOT DEFECT` line blocks the run.
    - Precondition 5 requires verification of the credential-aside state with recorded commands and
      states the restore procedure for both backups.
    - No item Result slot is filled and no verdict appears anywhere in the document.
  </acceptance_criteria>
  <done>The preconditions are satisfiable, mutually consistent, and prove the fix is in the build before a human logs into anything.</done>
</task>

<task type="auto">
  <name>Task 2: Author the five items, item 4's carried-forward groundwork, and the recording rules</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md</files>
  <read_first>
    - `.planning/phases/34.5-.../34.5-LIVE-GATE.md` Items 1-5 in full, including "Item 4 partial progress" and item 3's sub-clause (b)
    - `.planning/phases/34.5-.../34.5-PORTED-CHANNELS.md` § "Accepted residuals" — the current standing wording for Assumptions A1 and A2
    - `.planning/phases/34.5-.../34.5-RESEARCH.md` — Pitfall 2 and Assumptions A1/A2 as originally written
    - `src/backend/sidecar/oauthLoginCapture.ts` around line 118 — the exact `[oauthLoginCapture] runner=nile origin-mismatch origin=` line item 3(b) greps for
  </read_first>
  <action>
    Append the five items to the same document, each with What to do / What to observe / Evidence
    required / an EMPTY Result slot (`[ ] PASS   [ ] FAIL`), carrying `34.5-LIVE-GATE.md`'s wording
    forward rather than re-deriving it.

    Items 1-3 — Epic, GOG and Amazon each log in FROM SCRATCH and reach a signed-in, POPULATED
    library (an empty library is not a PASS: it could mean the `configStore` profile write silently
    failed). Add to each one sentence naming what changed since the failed run: the runner binary now
    resolves through the handed-down app root, and precondition 4 proves that before the item is
    attempted. For item 3, carry sub-clause (b) verbatim — grep `gamelib.log` for
    `[oauthLoginCapture] runner=nile origin-mismatch origin=`; a clean login means that line does NOT
    fire; if it DOES fire, Assumption A1's `www.amazon.com` anchor is FALSIFIED and item 3 cannot be
    a clean PASS even if the login otherwise succeeds. Record the observed origin either way, and
    record A1 as UNTESTED if the comparison code path is never reached.

    Item 4 — `addToSteam` AND the macOS `.app` desktop shortcut, exercising BOTH `exe` call sites
    (`nonesteamgame.ts:258`, the Steam shortcut VDF `Exe` field; and `shortcuts.ts:227`, the `.app`'s
    `run.sh` body) per research Correction 3. Proving one is not sufficient — they are two
    independent writers of the same value and either can regress alone. Carry the unconsumed
    groundwork across from "Item 4 partial progress" rather than rediscovering it: the target (GOG
    *Alan Wake*, `appName` `1207659037`, installed at `~/GameLib/Alan Wake`); the two expected writes
    (`~/Library/Application Support/Steam/userdata/35601368/config/shortcuts.vdf`'s `Exe` field, and
    `<shortcut>.app/Contents/MacOS/run.sh`'s body, which must read
    `<shell exe> --no-gui gamelib://launch?appName=1207659037&runner=gog`); the two `window.api`
    invocations from the devtools console (`window.api.addShortcut('1207659037', 'gog', false)`, a
    fire-and-forget listener; and `await window.api.addToSteam('1207659037', 'gog')`, a handler
    returning a boolean); and the operator warning that **Steam must be quit first** or it rewrites
    `shortcuts.vdf` from memory on exit and clobbers the write. Add one instruction the old contract
    lacked: verify the target is still installed (`gog_store/installed.json` plus the on-disk
    directory) before starting — a fresh GOG login re-populates the library and this groundwork is
    days old. Note that `shortcuts.vdf` did not exist when the groundwork was written, so the file
    appearing at all is part of item 4(a)'s evidence. Evidence for both halves: the actual value
    written at each call site, plus `ps aux` confirming the launched process is the Tauri shell
    binary and never a bare `node`.

    Item 5 — `runWineCommand` succeeds for at least one NON-Steam runner. State plainly that last
    run this FAILED BY BLOCKAGE only: nothing about `runWineCommand`'s own code was exercised or
    falsified, and research Pitfall 2's standing claim ("no non-Steam runner has ever executed a Wine
    command under the sidecar") must NOT be struck until this item records a PASS.

    Close with three sections, all empty of results:
    - **Verdict** — a five-row table skeleton plus a reserved line for the overall verdict.
    - **What this gate falsifies** — pre-list the four standing claims eligible to be struck ONLY on
      a PASS of the corresponding item: the 34.4.1 seam carrying a real Epic/GOG/Amazon session
      end-to-end (items 1-3); the `www.amazon.com` anchor (item 3, Assumption A1);
      `GAMELIB_SHELL_EXE` correctness at both call sites (item 4, Assumption A2); and research
      Pitfall 2's non-Steam Wine claim (item 5). State the rule: nothing is struck without the
      corresponding item PASSing, and a dev PASS never retires the packaged half of A2 or residual
      `R-34.5-G1-PKG`.
    - **Arithmetic rule** — `items_passed + items_failed` must equal 5; if any item is NOT ATTEMPTED
      it counts toward neither, and the shortfall must be stated explicitly in the frontmatter note
      (as the first run did), never quietly rounded.
  </action>
  <verify>
    <automated>grep -c "\[ \] PASS" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md && grep -c "1207659037" .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN.md && git status --porcelain .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - The document contains exactly five items, each with an unfilled `[ ] PASS   [ ] FAIL` slot
      (`grep -c "\[ \] PASS"` returns 5).
    - Item 4 names both call sites, both expected writes, both `window.api` invocations, the
      `1207659037` target, the quit-Steam warning, and the re-verify-installed instruction.
    - Item 3 names the `[oauthLoginCapture] runner=nile origin-mismatch origin=` grep and both the
      falsified and untested recording rules.
    - Item 5 states that Pitfall 2's claim is not struck without a PASS.
    - "What this gate falsifies" lists four claims, each bound to its item, plus the dev-only limit.
    - `git status --porcelain` still reports nothing for `34.5-LIVE-GATE.md`.
    - `python3 .planning/phases/34.5-.../ported-channels-gate.py` still exits 0 (this plan touches no
      channel declaration, and the gate must stay green across every plan in this cycle).
  </acceptance_criteria>
  <done>The contract is complete and unrun: a human can execute it top to bottom without interpretation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live gate instructions → real credential files on disk | The contract directs a human to move real Epic/GOG credentials aside and restore them |
| Live gate evidence → committed planning documents | Log excerpts pasted into the repository could carry secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.5-G6-01 | Denial of service (data loss) | Precondition 5's credential move-aside | mitigate | The contract mandates `cp -p` then `cmp` byte-identity verification BEFORE any removal, records both the original and restore path in the document, and forbids deleting blind — carried unchanged from the first run's own procedure, which worked |
| T-34.5-G6-02 | Information disclosure | Log excerpts and evidence pasted into `34.5-LIVE-GATE-RERUN.md` | mitigate | The contract instructs that `access_token`/`refresh_token` values, cookies, session identifiers and the Steam `userdata` account id are never pasted; quote only the log lines the item names, and redact any value that is not a path or a boolean |
| T-34.5-G6-03 | Repudiation | Recording a partial pass as closure | mitigate | D-08's no-partial-pass rule is restated in the document body, the arithmetic rule is pre-written, and every Result slot is authored EMPTY so a filled slot is always an act of recording, never a default |
| T-34.5-SC | Tampering | npm/pip/cargo installs | mitigate | Documentation-only plan; `files_modified` contains one markdown file and no manifest or lockfile |
</threat_model>

<verification>
- `34.5-LIVE-GATE-RERUN.md` exists with `verdict: null` and five empty Result slots
- `34.5-LIVE-GATE.md` is byte-unchanged (`git status --porcelain` silent for it)
- `python3 ported-channels-gate.py` exits 0
</verification>

<success_criteria>
- The contract is authored before any live work and is internally consistent
- Precondition 7 makes the old precondition-5 defect closed rather than reworded around
- Every one of G-2, G-4 and G-5's observation rules is written down before the run, not after
</success_criteria>

<output>
Create `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-19-SUMMARY.md` when done
</output>
