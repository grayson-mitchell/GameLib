---
phase: 34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc
plan: 40
subsystem: gate-contract
tags: [live-gate, gap-cycle-5, tauri, non-steam-runners, wine, shortcuts]
dependency-graph:
  requires:
    - 34.5-38-SUMMARY.md (cycle authorisation, D-CYCLE5-A/B, measured baseline)
    - 34.5-39-SUMMARY.md (item 4/5 preflight — verified GOG Alan Wake target, redacted Steam path, Wine prerequisite gap)
  provides:
    - 34.5-LIVE-GATE-RERUN-2.md (the third blocking gate contract, verdict null, unfilled)
  affects:
    - 34.5-41-PLAN.md (executes this contract)
    - 34.5-42-PLAN.md (propagates the outcome)
tech-stack:
  added: []
  patterns:
    - "contract-before-run discipline (verdict: null authored strictly before any live work)"
    - "four-way result value per item (PASS/FAIL/BLOCKED/NOT ATTEMPTED) with an explicit arithmetic invariant"
key-files:
  created:
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md
  modified: []
decisions:
  - "Item 5's runner-eligibility trade (GOG-vs-sideload, flagged by name in 34.5-G6-ITEM45-PREFLIGHT.md) resolved: GOG is the primary target if item 2 PASSes this cycle (retires both the narrow literal reading of Pitfall 2 and the storefront-specific reading); sideload is the login-independent fallback, but a sideload-only PASS retires only the narrow literal reading, not the storefront-specific one — both readings and their retirement scope are stated explicitly in the item-5 section so a future reader cannot conflate them."
metrics:
  duration: "~55 minutes"
  completed: 2026-08-02
---

# Phase 34.5 Plan 40: Author the third blocking live-gate contract (gap cycle 5) Summary

Authored `34.5-LIVE-GATE-RERUN-2.md` — the CONTRACT for the third five-item blocking live gate,
`verdict: null`, written strictly before any live work of that run begins, filling in nothing.

## What Was Built

A new gate-contract document, in two commits mirroring the plan's two tasks:

**Task 1** — frontmatter (`verdict: null`, `items_passed/failed/blocked/not_attempted: null`,
`executed_by: 34.5-41-PLAN.md`), a "Why this run exists" recap naming every fix landed since run 2
by plan number (34.5-23, -26, -27, -33, -34, -35, -36, and the 2026-08-02 Epic post-auth exfil
commit `c857ade8e`), D-08's no-partial-pass rule restated verbatim, the T-34.5-G6-02
evidence-handling rule, a new "binding evidence rule" (a mutating call's own report is never proof
of its effect — every item must name an observable from a different component), item 1's BLOCKED
entry disposition with the opportunistic-attempt clause from D-CYCLE5-A, a known-holes table (one
row per `34.5-UNTESTED-ITEMS.md` OPEN row, naming which gate item would retire it and whether this
run can), and 11 numbered preconditions (7 carried forward from run 2, 4 new: cycle-3 fixes present
at HEAD, cycle-4 fixes present at HEAD, the dev-secret-vault arm recorded, and the item-4/5
preflight re-confirmed at gate time).

**Task 2** — the five items with inlined evidence. Item 1 (Epic) carries the BLOCKED disposition
inline plus the opportunistic-attempt clause restated for the operator. Items 1-3 share five common
evidence clauses (a)-(e), with clause (b) naming the specific terminal
`phase=idle (login completed, library refresh triggered)` value a PASS requires (run 2's six
backend `status=captured` outcomes across items 2/3 produced zero matching frontend lines — this is
the exact defect signature the clause exists to catch). Item 2 (GOG) adds three more clauses (f)-(h)
tied to the cycle-4 `refreshLibrary`/`origin=` fixes, plus a two-consecutive-attempts requirement
for U-34.5-09. Item 3 (Amazon) keeps the origin-mismatch three-way recording rule and adds the
U-34.5-04 (code-field legibility) and U-34.5-05 (title-bar origin change) transcriptions. Item 4
inlines the 34.5-39 preflight's verified GOG *Alan Wake* target, the redacted Steam userdata path,
the quit-Steam warning, both devtools invocations, and the dev-vs-packaged limit. Item 5 inlines the
preflight's `toggleDXVK` action, its channel/kind, its literal evidence lines with emitting files,
its three unmet prerequisites with verifying commands, and resolves the runner-eligibility question
the preflight flagged by name (see Decisions above). The document closes with an unfilled Verdict
table, four standing claims with their "nothing struck without the ITEM passing" eligibility rules,
and the four-way arithmetic rule (`items_passed + items_failed + items_blocked +
items_not_attempted = 5`).

## Approach

Every log-line shape cited was read directly from its emitting source file at this plan's authoring
time, per the plan's own binding rule (run 1's own precondition-5 defect — citing a line nothing
emits — is the exact failure mode this rule exists to prevent):

- `src/backend/sidecar/oauthLoginCapture.ts` — `status=`, `label=`, `nav host=`,
  `origin-mismatch origin=`, `user-agent-override len=` lines (confirmed at lines 89, 150, 231, 255,
  298).
- `src/frontend/screens/WebView/useTauriOAuthLogin.ts` — all `phase=` terminal values including the
  four `cancelled-midflight at=...` sites and `capture-transport-failed` (confirmed at lines 180,
  202, 212, 224, 233, 239, 245, 254, 309, 320, 332, 337, 350, 371) and the `safeSetState` helper
  (lines 152-156).
- `src/backend/sidecar/steamFlowRegistration.ts` — `refreshLibrary complete`/`failed` lines
  (confirmed at lines 125, 144, 151, 214) and the `handleRefreshLibrary` gate excluding both
  `undefined` and `null` (confirmed at line 117).
- `src/frontend/state/GlobalState.tsx:1012` — the `[refreshLibrary] runner=... origin=...` line
  (confirmed).
- `src/backend/sidecar/bootstrap.ts` — the boot self-check, `GAMELIB_SHELL_EXE received=`, and
  `secret stores: dev-vault|keyring` lines (confirmed at lines 165, 170, 207, 213, 241, 248, 356,
  367).
- `src/backend/sidecar/devSecretVault.ts` — the exact-string `=== '1'` guard (line 232) and the
  `[dev-secret-vault] INSTALLED`/`read key=`/`write key=` lines (confirmed at lines 146, 154, 273).
- `src-tauri/src/main.rs` — `KEYRING_READ_TIMEOUT: Duration = Duration::from_secs(45)` (line 1100)
  and `oauthCaptureLogin` present in `LONG_RUNNING_CHANNELS` (line 152).
- `src/backend/sidecar/keyringTokenStore.ts:55` — `KEYRING_FAILURE_MEMO_MS = 120_000`.
- `src/frontend/screens/WebView/index.tsx:49,78-80` — `completeOAuthLogin` destructured from
  context and passed as `useTauriOAuthLogin`'s second argument.

Prior context was consumed rather than rediscovered: `34.5-LIVE-GATE-RERUN.md` supplied the shape
to mirror (frontmatter fields, precondition numbering, evidence-handling rule, arithmetic rule);
`34.5-CYCLE5-ROUTING.md` supplied D-CYCLE5-A/B verbatim and the measured baseline (precondition 6's
floor: 181/181 jest suites, 3553/3553 tests, `tsc` clean, `cargo check` clean, 92/0/1 cargo test,
`ported-channels-gate.py` + `--self-test` both exit 0, at HEAD `c8f314205`); `34.5-UNTESTED-ITEMS.md`
supplied all 11 ledger rows and Rule 3's obligation; `34.5-G6-ITEM45-PREFLIGHT.md` supplied items
4/5's verified targets and the runner-eligibility trade this plan was explicitly asked to resolve.

## Deviations from Plan

None — plan executed exactly as written. Both tasks' acceptance criteria were verified mechanically
after writing: `verdict: null` present; both prior gate documents (`34.5-LIVE-GATE.md`,
`34.5-LIVE-GATE-RERUN.md`) show zero changes in `git status --porcelain`; `34.5-29/30/31-PLAN.md`
untouched; 11 numbered preconditions each with an empty `**RESULT — [ ]**` slot; 5 items each with
an empty `**Result:**` slot (grep count 5); the four new preconditions (8-11) all present; the
known-holes table names all 11 ledger rows with U-34.5-01/06/10/11 explicitly marked
not-retirable-this-run (or retirable-only-together) with their reason; `git diff --name-only`
matches zero `.ts`/`.tsx`/`.rs` paths (this plan ships no code).

One deviation worth naming even though it did not require a code fix: the PreToolUse write hook
flagged a prompt-injection pattern (`act\s+as\s+(?:a|an|the)`) on the Task 1 write. Investigated
before proceeding — the match was a false positive spanning a word boundary in "named in the gate
cont**ract as a** known hole," a verbatim quote of `34.5-UNTESTED-ITEMS.md`'s own Rule 3 text, not
an embedded instruction. No content was changed in response.

## Known Stubs

None. This plan writes a static planning document with every result field intentionally empty by
design (`verdict: null` is the point of a contract-before-run gate, not an omission).

## Threat Flags

None beyond what the plan's own `<threat_model>` already registers (T-34.5-C5-06 redaction,
T-34.5-C5-10 through -14 tampering/repudiation/spoofing mitigations) — no new network endpoint,
auth path, file-access pattern, or schema change was introduced; this plan is documentation-only.

## Self-Check: PASSED

- `FOUND: .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-LIVE-GATE-RERUN-2.md`
- `FOUND: 1f4932a43` (Task 1 commit)
- `FOUND: f81dadd9f` (Task 2 commit)
- `git status --porcelain` confirms `34.5-LIVE-GATE.md` and `34.5-LIVE-GATE-RERUN.md` are absent
  from the diff (unmodified); `34.5-29/30/31-PLAN.md` likewise absent.
