---
phase: 36-login-to-steam-crossfade-and-explicit-login-in-flight-mitiga
plan: 02
type: execute
wave: 2
depends_on: ['36-01']
files_modified:
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md
  - .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: ['REQ-36-05']
user_setup: []

must_haves:
  truths:
    - "The living threat register records that T-34.4.2-39/-41's mitigation basis changed from an incidental navigation unmount to an explicit, stated guard — with the new evidence source named."
    - "The register states plainly that the WITHDRAWN live-discharge disposition itself did NOT change, only its basis."
    - "The Epic/SIDLogin hole (F-36-01) and the inert platform-floor gap (F-36-02) are recorded as named, accepted, still-open findings rather than silently absorbed."
    - "Truth 8 in 34.4.2-VERIFICATION.md carries a forward pointer, so a reader landing on that closed snapshot is not misled by a superseded basis."
    - "Phase 36's requirement IDs exist in REQUIREMENTS.md and ROADMAP.md no longer reads `Requirements: TBD`."
  artifacts:
    - path: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md"
      provides: "Fourteenth update to §5 recording the T-34.4.2-39/-41 basis change"
      contains: "Fourteenth update"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-36-01 through REQ-36-05"
      contains: "REQ-36-01"
  key_links:
    - from: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-VERIFICATION.md"
      to: ".planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md"
      via: "forward pointer on Truth 8"
      pattern: "Fourteenth update"
---

<objective>
Record the threat-model change the operator approved on 2026-08-20, and mint this phase's requirement
IDs.

Purpose: ROADMAP.md obligation 3. Retiring the navigation retires the mechanism the register
currently cites as the sole UI-pinned discharge basis for T-34.4.2-39 and T-34.4.2-41. A register
still naming the unmount would describe something no longer true — the same failure mode obligation 2
guards against for the test file.

Output: a Fourteenth update appended to `34.4.2-PLATFORM-SCOPE.md` §5; a forward pointer on Truth 8
in `34.4.2-VERIFICATION.md`; REQ-36-01..05 minted; ROADMAP.md's Phase 36 Requirements line filled in.

This plan changes no `src` or `src-tauri` source. It is documentation only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-login-to-steam-crossfade-and-explicit-login-in-flight-mitiga/36-RESEARCH.md
@.planning/phases/36-login-to-steam-crossfade-and-explicit-login-in-flight-mitiga/36-01-SUMMARY.md

<verified_register_facts>
<!-- VERIFIED BY DIRECT READ 2026-08-20. Do NOT re-derive — but DO re-check the tail before writing,
     per this repo's ledgered lesson that HEAD can move mid-session against a concurrent agent. -->

THE REGISTER FILE (there is no single THREAT-REGISTER.md in this project):
  .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md

  §5 "Threat register roll-up" begins at line 427. §6 "Package legitimacy" begins at line 925 — the
  new subsection goes at the END of §5, immediately BEFORE the §6 heading.

  Convention is APPEND-AND-SUPERSEDE: dated, bold-headed ordinal subsections, each a narrative
  preamble followed by a four-column pipe table
  | Threat ID | Status-at-entry-of-this-update | Update-this-round (bold) | Evidence/notes |
  Older tables are RETAINED verbatim, never edited or deleted.

  Ordinal headings verified present: Second (L481), Third (L507), Fourth (L530), Fifth (L561),
  Sixth (L591), Seventh (L637), Eighth (L657), Ninth (L689), Tenth (L729), Eleventh (L763),
  Twelfth (L803), Thirteenth (L875). The next ordinal is therefore Fourteenth.

  The current T-34.4.2-39 / T-34.4.2-41 row (Thirteenth update, 2026-08-06, plan 34.4.2-24) reads:
    | T-34.4.2-39 / T-34.4.2-41 | DISPOSITION CHANGED under D-G1: live-discharge path WITHDRAWN, not
    merely blocked. Basis unit-proven + UI-pinned, permanently. | **Unchanged -- still WITHDRAWN,
    unit-proven + UI-pinned, permanently.** Item 5 not attempted (it withdrawn, not scored) was not
    re-instated. | Plan 14's mutation-proven unit test + plan 22's
    `loginInFlightUiReachability.test.tsx` remain sole discharge basis, per D-G1. |

THE SECONDARY REFERENCE (NOT the append target):
  34.4.2-VERIFICATION.md lines 216-221 hold "Truth 8", a CLOSED gate's locked scorecard entry for the
  same two threats. Historical snapshot, not a living register. Do NOT rewrite its verdict — amending
  a closed gate's score is prohibited in this project. Add only a forward-pointing note.

REQUIREMENTS.md convention (verified against the v0.8 section): checkbox list items of the form
`- [ ] **REQ-{phase}-{NN}**: ...`, grouped under a per-phase paragraph naming the phase and where the
IDs were minted from.
</verified_register_facts>

<locked_decision_documentation_only>
RESEARCH.md Open Question 1 asked whether this update should also REOPEN live discharge for
T-34.4.2-39/-41's original item-5 contract, now that the UI no longer structurally forecloses it (the
prior "WITHDRAWN, permanently" disposition was reasoned on F-34.4.2-17 grounds — that the scenario
was UI-unreachable by construction, and that construction is exactly what phase 36 removes).

RESOLVED BY THE OPERATOR: DOCUMENTATION-ONLY. Record the basis change. Note factually that live
discharge is no longer structurally impossible. Do NOT author a new live-gate contract, do NOT
re-instate item 5, do NOT change the WITHDRAWN disposition. Reopening live discharge would expand
this phase into a new live-gate effort, named OUT in the scope fence.

Record this as a DELIBERATE NON-GOAL inside the Fourteenth update's own narrative preamble — not only
in a plan artifact — so a future reader sees the question was asked and answered, not overlooked.
</locked_decision_documentation_only>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the Fourteenth update to 34.4.2-PLATFORM-SCOPE.md §5</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md</files>
  <action>
FIRST, re-read the tail of §5 and confirm the highest ordinal present is still "Thirteenth update".
If a Fourteenth already exists, use the next free ordinal and say so in SUMMARY.md. Do not take the
ordinal from this plan text without checking — this repo has a ledgered lesson about a stale init
snapshot and a HEAD that moved mid-session.

Append a new subsection at the END of §5, after the Thirteenth update's table and Summary paragraph
and BEFORE the `## 6. Package legitimacy` heading. Match the existing subsections exactly: a bold
dated heading naming the ordinal, today's date, and this plan (phase 36, plan 36-02) with a short
reason such as "login-in-flight mitigation basis change: incidental unmount retired, explicit guard
installed". Then a narrative preamble, then the four-column pipe table. Do not edit, reformat, or
delete any earlier table.

The narrative preamble must state, at the level of detail the Thirteenth update's own preamble uses:
  - WHAT changed mechanically: `loginweb/steam` was removed from the router and `SteamLogin` became a
    co-mounted overlay on `/login`, so clicking a login tile no longer unmounts the six-tile group.
  - WHY that matters to these two threats: the unmount WAS the entire frontend mitigation, per the
    pre-rewrite `loginInFlightUiReachability.test.tsx` header.
  - WHAT REPLACED IT: an explicit, named `loginInFlight` boolean feeding all six tiles' `disabled`
    prop (consumed by `Runner.handleLogin()`'s early return), plus `pointer-events: none` and
    React-18 string-form `inert` on `.loginContentWrapper`.
  - THAT THE DISPOSITION ITSELF IS UNCHANGED: still `mitigate`, still WITHDRAWN from live discharge,
    still unit-proven plus UI-pinned. Only the UI-pinned half's BASIS changed. State this explicitly
    so no reader can mistake a basis change for a discharge.
  - THE DELIBERATE NON-GOAL, in substance from the locked decision above: live discharge is no longer
    structurally foreclosed by the UI, and is nonetheless NOT reopened in phase 36.
  - THE HONEST LIMITATION: the replacement gate is a source-text gate in a `testEnvironment: 'node'`
    jest project. It proves source shape, never a real click or a rendered document tree. A green
    suite has never caught one of this phase-family's blocking live defects.

The table must contain at least these rows.

  Row `T-34.4.2-39 / T-34.4.2-41`: status-at-entry quoting the Thirteenth update's disposition;
  update-this-round in bold stating the BASIS change with the disposition unchanged; evidence naming
  the rewritten `loginInFlightUiReachability.test.tsx`, its per-assertion mutation-proven
  falsifiability record in `36-01-SUMMARY.md`, and plan 14's unit test as still standing.

  Row `F-36-01 (NEW)`: the Epic-under-Tauri / SIDLogin hole. Status-at-entry "n/a — did not exist as a
  named finding before this phase". Disposition `accept — OPEN, deliberately not closed`. Evidence:
  Epic's tile opens SIDLogin via `primaryLoginAction` without setting `loginInFlight`; its only guard
  is the incidental `.SIDLoginModal` backdrop at `z-index: 8` over `.loginContentWrapper`'s
  `z-index: 1`, which is pointer-only with no focus trap; it is a PRE-EXISTING hole already carved out
  by name in the pre-rewrite gate's own header (lines 29-33); left open because Epic is a standing
  project deferral named OUT in phase 36's scope fence; carried forward in the rewritten gate's
  exception clause.

  Row `F-36-02 (NEW)`: the `inert` platform-floor gap. `inert` requires Safari 15.5 (May 2022), which
  ships with macOS 12.4; this app's `oldMac` gate floors at macOS 12.0, so the 12.0–12.3 slice lacks
  the a11y-tree/focus-order layer. Disposition `accept`. State the residual accurately and do NOT
  overstate it: the tiles are non-focusable `<div>`s with no `tabIndex`, no `role`, and are not
  `<button>`s — a premise now pinned by a dedicated assertion against `Runner/index.tsx` in the
  rewritten gate — so practical impact for the tiles is near zero; the two genuinely focusable
  controls in the panel (`goToLibrary`, `LanguageSelector`) are covered on that slice by MUI's Dialog
  focus trap, active because `Dialog.tsx` overrides none of `disableAutoFocus` /
  `disableEnforceFocus` / `disableRestoreFocus`. Note that `aria-hidden` was deliberately NOT added as
  a fallback because the container holds focusable children.

  Row `T-34.4.2-SC`: follow the existing row's convention. This plan changes no `src` or
  `src-tauri/src` source; run `git diff --stat -- src src-tauri/src` scoped to this plan's own changes
  and record the empty result.

Close with a bolded Summary sentence in the style of the existing ones: no threat moves to a fresh
CLOSED disposition in this update, and two new findings enter the register as accepted-and-open.
  </action>
  <verify>
    <automated>grep -c 'Fourteenth update' .planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-PLATFORM-SCOPE.md</automated>
  </verify>
  <done>§5 ends with a Fourteenth update carrying the narrative preamble and a table with rows for T-34.4.2-39/-41, F-36-01, F-36-02 and T-34.4.2-SC; every prior ordinal subsection is byte-unchanged; §6 still follows it.</done>
</task>

<task type="auto">
  <name>Task 2: Forward pointer on Truth 8, plus mint REQ-36-01..05</name>
  <files>.planning/phases/34.4.2-macos-login-window-ux-modal-child-window-attachment-in-field/34.4.2-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
In `34.4.2-VERIFICATION.md`, locate Truth 8 (around lines 216-221 — confirm by reading, do not trust
the line number). Add a short, clearly-marked forward-pointing note immediately after the existing
entry, stating that its "unit-proven plus UI-pinned, never live-discharged" basis was SUPERSEDED as to
its UI-pinned half by phase 36, and naming the Fourteenth update in `34.4.2-PLATFORM-SCOPE.md` §5 as
the current record.

DO NOT alter Truth 8's verdict, score, or any existing wording. It is a closed gate's locked
scorecard; amending a closed gate's score is prohibited. Add only. This addition exists because this
repo has a ledgered lesson that a status doc can sit two gate runs stale and nothing detects it —
before writing, grep this file for the previous run's artifact name to confirm you are amending the
current entry and not a superseded one.

In `.planning/REQUIREMENTS.md`, add a phase-36 group to the v0.8 section following the existing
convention exactly (a paragraph naming the phase and stating the IDs were minted during
`/gsd-plan-phase 36` from ROADMAP.md's Phase 36 section and its four numbered obligations, since no
36-CONTEXT.md exists; then unchecked checkbox items). Mint these five:

  REQ-36-01 — The Steam sign-in surface is a co-mounted overlay on `/login`, not a sibling route.
  `loginweb/steam` is removed from the router; `SteamLogin` dismisses through a callback prop; zero
  `navigate(` calls remain in `SteamLogin/index.tsx`; all eight censused dismissal paths funnel
  through one handler.

  REQ-36-02 — An explicit `loginInFlight` guard replaces the incidental navigation unmount. All six
  Runner tiles carry `disabled={oldMac || loginInFlight}`; `.loginContentWrapper` carries
  `pointer-events: none`, React-18 string-form `inert`, and a conditional `tabIndex={-1}` while the
  Steam flow is open; the flag clears synchronously on every exit path.

  REQ-36-03 — Clicking the Steam tile plays one continuous motion: `.loginContentWrapper` slides up
  and out via a CSS transition while the Steam Dialog slides up in at the same 500ms duration, with
  `.loginBackground` remaining painted underneath throughout.

  REQ-36-04 — `loginInFlightUiReachability.test.tsx` is rewritten — header prose and assertions — to
  pin the explicit guard, carries an Epic/SIDLogin exception clause and a FILLED-SPECIMEN guard, and
  every assertion has a recorded mutation-proven RED observation.

  REQ-36-05 — The threat register records T-34.4.2-39/-41's basis change from incidental unmount to
  explicit guard, with the WITHDRAWN live-discharge disposition explicitly unchanged, and with F-36-01
  and F-36-02 entered as named accepted-and-open findings.

In `.planning/ROADMAP.md`, Phase 36's `**Requirements**:` line, `**Plans:** 3 plans` count and the
three-entry plan list were ALREADY written by the planning pass that produced these plans. VERIFY they
are present and that the five REQ IDs on the Requirements line match the five you just minted in
REQUIREMENTS.md exactly — do not rewrite the section. Note that there are FIVE requirement IDs across
THREE plans; that is correct and not a mismatch. If they disagree, fix REQUIREMENTS.md to match
ROADMAP.md rather than the reverse, and note the discrepancy in SUMMARY.md.

Leave the Goal, the coupled-security-work block, and the Anchors block untouched — they are the
operator's own text and are still accurate.
  </action>
  <verify>
    <automated>grep -c 'REQ-36-0' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Truth 8 carries an additive forward pointer with its verdict untouched; REQUIREMENTS.md contains REQ-36-01 through REQ-36-05 in the established format; ROADMAP.md's Phase 36 section names all five requirement IDs and lists three plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Documentation-only plan. No runtime code, no IPC, no `src-tauri`, no sidecar, no dependency change. The only risk class is misrecording, addressed below. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07 | Repudiation | the threat register itself | mitigate | The register is append-and-supersede: prior tables are retained verbatim so a basis change cannot erase the history it supersedes. Task 1 forbids editing any earlier ordinal subsection, and the verify step confirms the Thirteenth update still exists alongside the Fourteenth. |
| T-36-08 | Repudiation | a status doc silently lagging the current state | mitigate | This repo has a ledgered lesson that a propagation plan can miss a status doc and nothing detects it — two docs sat two gate runs stale for a week. Task 2 adds the forward pointer on Truth 8 for exactly that reason, and instructs a grep for the previous run's artifact before writing. |
| T-36-09 | Tampering | amending a closed gate's score | mitigate | Task 2 forbids altering Truth 8's verdict or wording. The forward pointer is purely additive. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — documentation only, nothing installed. Confirm with `git diff --exit-code pnpm-lock.yaml`. |
</threat_model>

<verification>
- `grep -c 'Fourteenth update' 34.4.2-PLATFORM-SCOPE.md` returns 1, and `grep -c 'Thirteenth update'` still returns its pre-existing count (no earlier subsection was disturbed).
- `F-36-01` and `F-36-02` both appear in `34.4.2-PLATFORM-SCOPE.md`.
- `## 6. Package legitimacy` still follows §5.
- `REQ-36-01` through `REQ-36-05` appear in `.planning/REQUIREMENTS.md`.
- `.planning/ROADMAP.md` Phase 36 no longer contains `Requirements**: TBD` or `TBD (run /gsd-plan-phase 36`.
- `git diff --stat -- src src-tauri/src` is empty for this plan's changes.
</verification>

<success_criteria>
- §5 of `34.4.2-PLATFORM-SCOPE.md` ends with a Fourteenth update whose narrative preamble states the basis change, the unchanged WITHDRAWN disposition, the documentation-only non-goal, and the source-gate limitation.
- F-36-01 (Epic/SIDLogin) and F-36-02 (`inert` platform floor) are entered as accepted-and-open findings.
- Truth 8 in `34.4.2-VERIFICATION.md` is amended additively only.
- REQ-36-01..05 minted; ROADMAP.md Phase 36 Requirements and Plans lines are filled in.
</success_criteria>

<output>
Create `.planning/phases/36-login-to-steam-crossfade-and-explicit-login-in-flight-mitiga/36-02-SUMMARY.md` when done.
Record the ordinal actually used (in case a concurrent session had already claimed Fourteenth).
</output>
