---
phase: 15-store-overlay-expiration-alerts
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/humble/expirationAlerts.ts
  - src/backend/humble/electronStores.ts
  - src/backend/humble/__tests__/expirationAlerts.test.ts
  - public/locales/en/translation.json
autonomous: true
gap_closure: true
requirements: [HSTORE-03]

must_haves:
  truths:
    - "The expiration dedup store is keyed by composite `gamekey:machineName`, matching the Phase 14 convention used by humbleAuditStore/humbleLocalRedeemedStore (D-90/D-92)"
    - "The same machineName appearing across two orders with DIFFERENT expirations each fires exactly once and does NOT re-fire on an identical re-sync"
    - "Existing transition semantics are preserved: null->date fires; same-date re-seen is a no-op; changed-date re-fires; first-ever sync seeds baseline silently (locked decision 3)"
    - "Upgrading from the machineName-only store does not fire a spurious notification storm for already-notified keys"
    - "The four humble.notification.* i18n keys used by buildDigestCopy are registered in public/locales/en/translation.json"
  artifacts:
    - path: "src/backend/humble/expirationAlerts.ts"
      provides: "composite-key dedup (`${key.gamekey}:${key.machineName}`) + one-time legacy backfill; digest copy unchanged"
      contains: "gamekey"
    - path: "src/backend/humble/electronStores.ts"
      provides: "humbleNotifiedExpirationStore comment reflects composite `gamekey:machineName` keying"
      contains: "gamekey:machineName"
    - path: "public/locales/en/translation.json"
      provides: "humble.notification.{expiringTitleSingle,expiringBodySingle,expiringTitlePlural,expiringBodyPlural} keys"
      contains: "notification"
  key_links:
    - from: "src/backend/humble/expirationAlerts.ts"
      to: "src/backend/humble/electronStores.ts"
      via: "humbleNotifiedExpirationStore get/set keyed by composite gamekey:machineName"
      pattern: "gamekey.*machineName"
    - from: "src/backend/humble/expirationAlerts.ts"
      to: "public/locales/en/translation.json"
      via: "i18next.t('humble.notification.*')"
      pattern: "humble\\.notification"
---

<objective>
Close WR-01 and WR-02 (HSTORE-03 warnings):

WR-01 — `humbleNotifiedExpirationStore` and `detectAndNotifyExpirationTransitions`
key the last-notified expiration by `machineName` alone. `getKeys()` flattens
keys across all orders with no de-dup, so a duplicate `machineName` across two
orders with different expirations re-fires the notification indefinitely,
defeating HSTORE-03's single-fire intent. Phase 14 already established a
composite `gamekey:machineName` key for the sibling disconnect-exempt stores
(humbleAuditStore, humbleLocalRedeemedStore) precisely for this collision class.
User decision: FIX (do not override). Switch to the composite key and add a
one-time legacy backfill so the upgrade does not fire a notification storm.

WR-02 — `buildDigestCopy` references four `humble.notification.*` keys that were
never registered in `public/locales/en/translation.json`; they work only via
inline English fallbacks. Register them (hand-insert only — the `discounts.badge.*`
convention from this same phase), WITHOUT running a full `pnpm i18n` scan.

Purpose: make the dedup collision-safe and the OS notification localizable.
Output: composite-keyed dedup + migration, registered notification i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-store-overlay-expiration-alerts/15-VERIFICATION.md
@.planning/phases/15-store-overlay-expiration-alerts/15-REVIEW.md

<interfaces>
<!-- Contracts the executor needs. Do not re-explore the codebase for these. -->

From src/backend/humble/electronStores.ts:149-152 (existing):
```typescript
const humbleNotifiedExpirationStore = new CacheStore<
  { expiration: string },
  string
>('humble_notified_expiration', null)
```
CacheStore API in use elsewhere: `.get(key)`, `.set(key, value)`, `.has(key)`.
The store value/key TYPES do not change — only the KEY STRING changes from
`machineName` to `${gamekey}:${machineName}`.

From src/common/types/humble.ts — HumbleKey carries `gamekey: string`,
`machineName: string`, `title: string`, `expiration: string | null`.

Phase 14 precedent (src/backend/humble/library.ts:46-48) — composite builder:
```typescript
const compositeKey = (gamekey: string, machineName: string): string =>
  `${gamekey}:${machineName}`
```
(This helper is module-private to library.ts. expirationAlerts.ts operates on
HumbleKey[] which carry both fields, so build the composite inline:
`` `${key.gamekey}:${key.machineName}` ``. Do NOT export/import library.ts internals.)

Current detection loop (src/backend/humble/expirationAlerts.ts:25-38) reads/writes
the store with `key.machineName`. Digest copy (lines 78-111) already uses the four
`humble.notification.*` keys with inline English fallbacks — leave the copy text
and fallbacks unchanged; only the i18n registration is missing.

translation.json structure: keys are sorted; there is NO top-level `humble` block
yet (only `humbleKeys` at line 548). `discounts.badge.owned/keyAvailable` ARE
registered (lines 299-303) — follow that hand-registration convention.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Composite-key the expiration dedup store + legacy backfill (RED first)</name>
  <files>src/backend/humble/expirationAlerts.ts, src/backend/humble/electronStores.ts, src/backend/humble/__tests__/expirationAlerts.test.ts</files>
  <behavior>
    Change `detectAndNotifyExpirationTransitions` to key `humbleNotifiedExpirationStore` by the composite `${key.gamekey}:${key.machineName}` for every get and set (replace both uses of `key.machineName` at lines ~28 and ~34).
    Add a one-time legacy backfill BEFORE the transition loop: for each key, if the composite entry is absent (`!store.has(composite)`) AND a legacy `machineName`-only entry exists (`store.has(key.machineName)`), copy the legacy value to the composite key (`store.set(composite, store.get(key.machineName)!)`). This preserves "already notified" state on upgrade so the upgrade does not re-fire. Legacy keys contain no colon and composite keys always do, so they never collide. (Do NOT delete legacy entries — leaving them is harmless and keeps the backfill idempotent.)
    New/updated test cases:
    - Duplicate machineName across two orders (different gamekeys) with DIFFERENT expirations: BOTH fire once on first observation; a second identical sync fires NEITHER (the re-fire-indefinitely bug is fixed). This test MUST fail against the current machineName-only code.
    - Duplicate machineName across two orders where only ONE order's key changes date on re-sync: only that one re-fires.
    - Legacy backfill: pre-seed the mock store with a machineName-only entry equal to a key's current expiration; run detection (non-suppressed); assert NO notification fires for that key (treated as already-notified), and the composite entry now exists.
    - Preserve the existing suite's semantics: null->date fires, same-date no-op, changed-date re-fires, first-ever sync (suppressNotifications) seeds silently, settings-gate/Notification-unsupported/SteamDeck suppression still advance the store. Update any existing assertions that inspect the store by `machineName` to use the composite key.
  </behavior>
  <action>
    Implement the composite keying and backfill per <behavior> in expirationAlerts.ts. Build the composite inline (`` `${key.gamekey}:${key.machineName}` ``) — do not import library.ts's private helper. Keep the "always advance persisted state even under suppression" logic (locked decision 3) intact, just keyed by composite.
    Update src/backend/humble/electronStores.ts:140-152 comment to state the store is now keyed by composite `gamekey:machineName` (matching humbleAuditStore/humbleLocalRedeemedStore), citing the WR-01 non-collision lesson; note the one-time legacy backfill in expirationAlerts.ts. The CacheStore type parameters are unchanged.
    In expirationAlerts.test.ts, write the failing duplicate-machineName and backfill tests FIRST, then implement. Update existing machineName-keyed assertions to composite. The store mock is already Map-backed, so `.has()`/`.get()`/`.set()` on composite strings work without mock changes.
  </action>
  <verify>
    <automated>npx jest expirationAlerts.test.ts && pnpm codecheck</automated>
  </verify>
  <done>Store is keyed by `gamekey:machineName` everywhere in the detection path; duplicate-machineName-across-orders no longer re-fires; legacy machineName entries are backfilled so upgrade fires no storm; all prior transition/gate/suppression semantics still pass; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Register the four humble.notification.* i18n keys</name>
  <files>public/locales/en/translation.json</files>
  <action>
    Hand-insert a new top-level `"humble"` block containing a `"notification"` object with exactly these four keys (values matching the inline English fallbacks in buildDigestCopy so behavior is identical):
    - `expiringBodyPlural`: "{{count}} Humble keys gained expiration dates"
    - `expiringBodySingle`: "{{title}}'s Humble key now expires on {{date}}"
    - `expiringTitlePlural`: "Humble keys expiring"
    - `expiringTitleSingle`: "Humble key expiring"
    Place the `"humble"` block at its alphabetically-correct position — immediately BEFORE the existing `"humbleKeys"` block (line ~548), since "humble" sorts before "humbleKeys". Keep keys within `notification` alphabetically ordered to match the file's convention (as seen in discounts.badge). Match existing indentation exactly.
    Do NOT run `pnpm i18n` / the full extraction scan — per prior phase-15 SUMMARY constraint it pulls in ~140 files of pre-existing orphaned-key drift out of scope. Insert only these four keys by hand, exactly as the `discounts.badge.*` keys were added this phase.
    Do NOT change the `i18next.t(...)` calls or their inline fallbacks in expirationAlerts.ts — registration alone closes WR-02.
  </action>
  <verify>
    <automated>node -e "const t=require('./public/locales/en/translation.json').humble.notification; ['expiringTitleSingle','expiringBodySingle','expiringTitlePlural','expiringBodyPlural'].forEach(k=>{if(!t[k])throw new Error('missing '+k)}); console.log('ok')" && node -e "JSON.parse(require('fs').readFileSync('public/locales/en/translation.json','utf8'));console.log('valid json')"</automated>
  </verify>
  <done>public/locales/en/translation.json contains humble.notification with all four keys at the alphabetically-correct position; file is valid JSON; no unrelated orphaned-key churn introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Humble sync cache -> OS notification | Cached HumbleKey fields cross into an OS-level toast and the dedup store |
| Persisted dedup store -> notification decision | On-disk last-notified state gates whether a toast fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Denial of Service | detectAndNotifyExpirationTransitions dedup | mitigate | Composite `gamekey:machineName` key removes the cross-order machineName collision that re-fired notifications indefinitely (the exact WR-01 storm); legacy backfill prevents an upgrade-time storm |
| T-15-06-02 | Information Disclosure | buildDigestCopy notification body | mitigate | Copy reads only display-safe `title`/`expiration` (Pitfall 5); revealedKeyValue/keyindex never touched — unchanged by this plan and preserved |
| T-15-06-03 | Tampering | translation.json edit | accept | Hand-insert of four literal keys; JSON validity asserted in verify; no code path or install touched |
</threat_model>

<verification>
- `npx jest expirationAlerts.test.ts` — all suites green, including new duplicate-machineName and backfill tests.
- `pnpm codecheck` — no type errors.
- `humble.notification.*` keys resolve from translation.json (verify script) and file remains valid JSON.
</verification>

<success_criteria>
- Duplicate machineName across orders no longer re-fires the expiration notification.
- Upgrade from machineName-only store fires no spurious notification for already-notified keys.
- All four OS-notification i18n keys are translator-visible; English behavior unchanged.
- HSTORE-03 dedup intent ("does not repeat") holds under the collision condition.
</success_criteria>

<output>
Create `.planning/phases/15-store-overlay-expiration-alerts/15-06-SUMMARY.md` when done.
</output>
