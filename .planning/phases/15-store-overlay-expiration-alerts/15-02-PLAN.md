---
phase: 15-store-overlay-expiration-alerts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/types.ts
  - src/backend/config.ts
  - src/frontend/screens/Settings/components/NotifyHumbleExpirations.tsx
  - src/frontend/screens/Settings/components/index.ts
  - src/frontend/screens/Settings/sections/GeneralSettings/index.tsx
  - src/backend/humble/electronStores.ts
  - src/backend/humble/__tests__/electronStores.test.ts
autonomous: true
requirements: [HSTORE-03]

must_haves:
  truths:
    - "A 'Notify when Humble keys gain expiration dates' toggle appears in Settings → General, on by default (D-93)"
    - "The notify preference persists and is readable by the backend via GlobalConfig.get().getSettings() (D-93)"
    - "A disconnect-exempt per-key store records the last-notified expiration date, keyed by machineName, surviving app restarts and Humble disconnect/reconnect (D-92)"
  artifacts:
    - path: "src/common/types.ts"
      provides: "AppSettings.notifyHumbleExpirations: boolean field"
      contains: "notifyHumbleExpirations"
    - path: "src/backend/config.ts"
      provides: "getFactoryDefaults() notifyHumbleExpirations: true default"
      contains: "notifyHumbleExpirations: true"
    - path: "src/frontend/screens/Settings/components/NotifyHumbleExpirations.tsx"
      provides: "ToggleSwitch component defaulting to true"
      contains: "notifyHumbleExpirations"
    - path: "src/backend/humble/electronStores.ts"
      provides: "humbleNotifiedExpirationStore CacheStore export (disconnect-exempt)"
      contains: "humbleNotifiedExpirationStore"
  key_links:
    - from: "src/frontend/screens/Settings/sections/GeneralSettings/index.tsx"
      to: "NotifyHumbleExpirations"
      via: "component registration + barrel export"
      pattern: "NotifyHumbleExpirations"
    - from: "src/frontend/screens/Settings/components/NotifyHumbleExpirations.tsx"
      to: "AppSettings.notifyHumbleExpirations"
      via: "useSetting('notifyHumbleExpirations', true)"
      pattern: "useSetting\\('notifyHumbleExpirations'"
---

<objective>
Lay the notification foundation for HSTORE-03: a default-ON Settings toggle
("Notify when Humble keys gain expiration dates") wired through the standard 3-file
settings pattern, and a disconnect-exempt `humbleNotifiedExpirationStore` that persists
the last-notified expiration per key. These are the two dependencies Plan 15-03's
detection/dispatch logic reads from.

Purpose: HSTORE-03's "optional OS notifications" preference surface + the durable state
that makes D-92 transition-dedup survive restarts and reconnects.
Output: AppSettings field + factory default, the toggle component + registration, and
the new CacheStore export with test coverage.

Decision note: references Phase 15 CONTEXT decisions D-92/D-93 (collide with Phase 14's
historical D-77..D-80 numerically — here they mean the Phase 15 decisions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-store-overlay-expiration-alerts/15-CONTEXT.md
@.planning/phases/15-store-overlay-expiration-alerts/15-RESEARCH.md
@.planning/phases/15-store-overlay-expiration-alerts/15-PATTERNS.md
@.planning/phases/15-store-overlay-expiration-alerts/15-UI-SPEC.md

<interfaces>
From src/common/humble/electronStores.ts (existing pattern):
- `new CacheStore<{ giftedAt: number }, string>('humble_gifted_at', null)` — machineName-keyed, disconnect-exempt.
- Existing final `export { ... }` block lists all stores.

From src/frontend/hooks/useSetting.ts:
- `useSetting<T extends keyof AppSettings>(key: T, fallback: NonNullable<AppSettings[T]>)`.

Backend reads settings via `GlobalConfig.get().getSettings()` (see main.ts:381 `settings.analyticsOptIn`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings toggle — AppSettings field + factory default + component + registration + i18n</name>
  <files>src/common/types.ts, src/backend/config.ts, src/frontend/screens/Settings/components/NotifyHumbleExpirations.tsx, src/frontend/screens/Settings/components/index.ts, src/frontend/screens/Settings/sections/GeneralSettings/index.tsx</files>
  <read_first>
    - src/common/types.ts (line ~97: `analyticsOptIn: boolean` field in AppSettings)
    - src/backend/config.ts (line ~328: `analyticsOptIn: false` in getFactoryDefaults())
    - src/frontend/screens/Settings/components/AnalyticsOptIn.tsx (full file — verbatim structure to clone)
    - src/frontend/screens/Settings/components/index.ts (barrel export list)
    - src/frontend/screens/Settings/sections/GeneralSettings/index.tsx (AnalyticsOptIn import ~line 25, usage ~line 73; TraySettings/DiscordRPC placement group)
    - src/frontend/hooks/useSetting.ts (fallback mechanics — keyof AppSettings typing)
    - 15-UI-SPEC.md (Copywriting Contract: locked toggle label + help copy)
  </read_first>
  <action>
    Add `notifyHumbleExpirations: boolean` to the `AppSettings` interface in `src/common/types.ts`, directly beside `analyticsOptIn: boolean`. Add `notifyHumbleExpirations: true` to `getFactoryDefaults()` in `src/backend/config.ts`, beside `analyticsOptIn: false` — MUST default `true` (D-93 on-by-default). Create `src/frontend/screens/Settings/components/NotifyHumbleExpirations.tsx` by cloning `AnalyticsOptIn.tsx` verbatim, changing: `htmlId="notifyHumbleExpirations"`, `useSetting('notifyHumbleExpirations', true)` (default true), title `t('setting.notifyHumbleExpirations', 'Notify when Humble keys gain expiration dates')` (locked copy — do not paraphrase), help `t('help.notifyHumbleExpirations', "Get an OS notification when a Humble key you haven't claimed gains an expiration date.")`. Add `NotifyHumbleExpirations` to the `src/frontend/screens/Settings/components/index.ts` barrel export following its existing export-list pattern. Register `<NotifyHumbleExpirations />` in `GeneralSettings/index.tsx` near `<TraySettings />`/`<DiscordRPC />` (nearest notification/behavior-adjacent group). Run `pnpm i18n` to register the two new keys.
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm codecheck` passes (AppSettings key exists so `useSetting('notifyHumbleExpirations', true)` type-checks).
    - `grep -c "notifyHumbleExpirations: boolean" src/common/types.ts` == 1.
    - `grep -c "notifyHumbleExpirations: true" src/backend/config.ts` == 1.
    - `grep -c "useSetting('notifyHumbleExpirations', true)" src/frontend/screens/Settings/components/NotifyHumbleExpirations.tsx` == 1.
    - `grep -c "NotifyHumbleExpirations" src/frontend/screens/Settings/sections/GeneralSettings/index.tsx` ≥ 1 (imported + rendered).
    - `setting.notifyHumbleExpirations` and `help.notifyHumbleExpirations` exist in `public/locales/en/translation.json`.
  </acceptance_criteria>
  <done>The default-ON toggle renders in Settings → General and its value is persisted to AppSettings, readable by the backend.</done>
</task>

<task type="auto">
  <name>Task 2: humbleNotifiedExpirationStore CacheStore + test extension</name>
  <files>src/backend/humble/electronStores.ts, src/backend/humble/__tests__/electronStores.test.ts</files>
  <read_first>
    - src/backend/humble/electronStores.ts (lines ~80-94: humbleGiftedAtStore + its disconnect-exempt doc-comment; the final `export { ... }` block ~lines 136-145)
    - src/backend/humble/user.ts (HumbleUser.disconnect() — confirm which stores it clears; the new store must NOT be added to that clear list)
    - src/backend/humble/__tests__/electronStores.test.ts (existing describe/test structure for humbleGiftedAtStore/humbleOwnershipOverrideStore — extend, do not replace)
  </read_first>
  <action>
    In `src/backend/humble/electronStores.ts`, add `const humbleNotifiedExpirationStore = new CacheStore<{ expiration: string }, string>('humble_notified_expiration', null)` following the `humbleGiftedAtStore` shape exactly. Keyed by `machineName` (never `gamekey`). Add a doc-comment matching the file's disconnect-exempt rationale block stating it is NEVER cleared by `HumbleUser.disconnect()` (per RESEARCH A4 — a reconnect must not re-notify already-known expirations) and kept as its own on-disk electron-store file. Add `humbleNotifiedExpirationStore` to the file's final `export { ... }` block alongside the existing exports. Confirm `HumbleUser.disconnect()` does NOT clear it (it must survive disconnect like humbleRevealedStore/humbleGiftedAtStore). Extend `electronStores.test.ts` with cases: set/get roundtrip keyed by machineName; value shape `{ expiration: string }`; and (if the test file already asserts disconnect-survival for a sibling store) an assertion that this store is not among the disconnect-cleared stores.
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/electronStores.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/backend/humble/__tests__/electronStores.test.ts` passes including the new cases.
    - `grep -c "humbleNotifiedExpirationStore" src/backend/humble/electronStores.ts` ≥ 2 (declaration + export).
    - `grep -c "humble_notified_expiration" src/backend/humble/electronStores.ts` == 1.
    - `grep -c "humbleNotifiedExpirationStore" src/backend/humble/user.ts` == 0 (not wiped on disconnect).
  </acceptance_criteria>
  <done>A machineName-keyed, disconnect-exempt store persists last-notified expiration dates with test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted electron-store on disk | Notified-state file holds per-key metadata across sessions |
| Renderer settings → backend config | The notify preference flows to the main process notification gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Information Disclosure | humbleNotifiedExpirationStore contents | mitigate | Store value shape is `{ expiration: string }` (an ISO date) keyed by `machineName` only — NEVER stores raw key values, `revealedKeyValue`, or `keyindex` (matches C4/T-14-02 discipline; only the display-safe fields are persisted). |
| T-15-02-02 | Tampering | On-disk store growing unbounded over years of syncs | accept | Mirrors existing humbleRevealedStore/humbleGiftedAtStore which have no pruning either — no new risk class introduced (low-severity availability tradeoff). |
| T-15-02-03 | Tampering | Package installs | n/a | Zero npm/pip/cargo installs this plan — no supply-chain surface. |
</threat_model>

<verification>
- `npx jest src/backend/humble/__tests__/electronStores.test.ts` green.
- `pnpm codecheck` + `pnpm lint` clean.
- Toggle visible + default ON in Settings → General (verified live in Plan 15-03's checkpoint alongside firing behavior).
</verification>

<success_criteria>
- HSTORE-03 (foundation): the "optional" notification preference exists (default ON) and is backend-readable; last-notified expiration state persists per key and survives disconnect.
</success_criteria>

<output>
Create `.planning/phases/15-store-overlay-expiration-alerts/15-02-SUMMARY.md` when done.
</output>
