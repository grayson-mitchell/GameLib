---
phase: 15-store-overlay-expiration-alerts
plan: 03
type: execute
wave: 2
depends_on: [15-02]
files_modified:
  - src/backend/humble/expirationAlerts.ts
  - src/backend/humble/__tests__/expirationAlerts.test.ts
  - src/backend/humble/library.ts
autonomous: true
requirements: [HSTORE-03]

must_haves:
  truths:
    - "When a key's expiration goes null→date on a non-first sync, one OS digest notification fires (D-90, D-92)"
    - "Re-syncing the same expiration date never re-fires; a changed date (date→different-date) fires again (D-92)"
    - "A first-ever sync (no prior sync snapshot) seeds the notified-state baseline WITHOUT firing any notification — no notification storm on fresh Humble connect (D-92, locked decision 3)"
    - "The digest names the game for a single affected key; uses a plural summary for 2+ keys (D-90)"
    - "Clicking the notification focuses the window AND navigates to /humble-keys/waiting (D-91)"
    - "Notifications never fire when the notifyHumbleExpirations setting is off, when unsupported, or in Steam Deck Game Mode (D-93)"
  artifacts:
    - path: "src/backend/humble/expirationAlerts.ts"
      provides: "detectAndNotifyExpirationTransitions + digest-copy builder"
      exports: ["detectAndNotifyExpirationTransitions"]
    - path: "src/backend/humble/__tests__/expirationAlerts.test.ts"
      provides: "Unit coverage for transition dedup, first-sync baseline, digest copy, click nav, settings gate"
      contains: "detectAndNotifyExpirationTransitions"
    - path: "src/backend/humble/library.ts"
      provides: "runSync() hook calling detection after recomputeOwnership()"
      contains: "detectAndNotifyExpirationTransitions"
  key_links:
    - from: "src/backend/humble/library.ts"
      to: "src/backend/humble/expirationAlerts.ts"
      via: "call after recomputeOwnership() before runSync return"
      pattern: "detectAndNotifyExpirationTransitions\\("
    - from: "src/backend/humble/expirationAlerts.ts"
      to: "/humble-keys/waiting"
      via: "sendFrontendMessage('openScreen', ...) in notification click handler"
      pattern: "sendFrontendMessage\\('openScreen'"
    - from: "src/backend/humble/expirationAlerts.ts"
      to: "humbleNotifiedExpirationStore"
      via: "get/set last-notified expiration per machineName"
      pattern: "humbleNotifiedExpirationStore"
---

<objective>
Detect expiration transitions at the end of every Humble sync and fire a single per-sync
digest OS notification for newly-expiring keys, with transition-based dedup (D-92) and
first-sync baseline suppression (locked user decision 3 — no notification storm on fresh
connect). Clicking the notification focuses the app and navigates to the Keys-waiting view.

Purpose: HSTORE-03's "OS notification alerts the user when a previously non-expiring key
gains an expiration on sync — does not repeat on subsequent syncs for the same key."
Output: The pure-ish `expirationAlerts.ts` service (unit-tested with mocked Notification/
IPC/store) plus the one-line `runSync()` hook.

Decision note: references Phase 15 CONTEXT decisions D-90/D-91/D-92 (numeric collision with
Phase 14's historical D-77..D-80 — here they mean the Phase 15 decisions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-store-overlay-expiration-alerts/15-CONTEXT.md
@.planning/phases/15-store-overlay-expiration-alerts/15-RESEARCH.md
@.planning/phases/15-store-overlay-expiration-alerts/15-PATTERNS.md
@.planning/phases/15-store-overlay-expiration-alerts/15-UI-SPEC.md
@.planning/phases/15-store-overlay-expiration-alerts/15-02-SUMMARY.md

<interfaces>
From src/backend/dialog/dialog.ts (notify() precedent — clone the guard, not the call):
- Guard: `if (Notification.isSupported() && !isSteamDeckGameMode) { ... }`
- `isSteamDeckGameMode` from `backend/constants/environment`.
- `getMainWindow()` from `../main_window`; `sendFrontendMessage` from `../ipc`.

From src/backend/humble/library.ts:
- `sendFrontendMessage` already imported (line 2).
- `getKeys(): HumbleKey[]` (line ~457); `recomputeOwnership()` (line ~486); the runSync
  hook point is immediately after the `recomputeOwnership()` call, before `return { status: ... }` (~line 997).

From src/backend/humble/electronStores.ts (added in 15-02):
- `humbleNotifiedExpirationStore` — CacheStore<{ expiration: string }, string>, machineName-keyed.

Backend settings: `GlobalConfig.get().getSettings().notifyHumbleExpirations` (added in 15-02).

Navigation target route: `/humble-keys/waiting` (App.tsx route; Sidebar's handleGoToScreen
listener already consumes `openScreen` app-wide — zero new IPC plumbing).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: expirationAlerts.ts detection + digest + first-sync baseline (RED→GREEN)</name>
  <files>src/backend/humble/expirationAlerts.ts, src/backend/humble/__tests__/expirationAlerts.test.ts</files>
  <read_first>
    - src/backend/dialog/dialog.ts (notify() guard + click handler pattern to clone)
    - src/backend/constants/environment.ts (isSteamDeckGameMode export)
    - src/backend/humble/electronStores.ts (humbleNotifiedExpirationStore signature from 15-02)
    - src/common/types/humble.ts (HumbleKey display-safe fields: machineName, title, expiration; NEVER read HumbleKeyInternal)
    - src/common/humble/urgencyBadge.ts (locale date-format helper used by UrgencyBadge — reuse for the digest date, do not invent a new formatter)
    - src/backend/humble/__tests__/electronStores.test.ts (CacheStore mocking pattern for backend tests)
    - src/backend/main.ts (line ~381 GlobalConfig.get().getSettings() accessor; lines ~1473 sendFrontendMessage('openScreen', ...) precedent)
    - 15-UI-SPEC.md (Copywriting Contract: digest title/body single + plural i18n keys and locked wording)
    - 15-CONTEXT.md (D-90 digest, D-91 click nav, D-92 dedup) + planning_context locked user decision 3 (first-sync suppression)
  </read_first>
  <behavior>
    - Test: key expiration null→date, non-baseline sync → pushed to newlyExpiring, store updated, one notification fired.
    - Test: same date re-seen (store already has that expiration) → no-op, no fire (D-92 dedup).
    - Test: date→different-date → fires again, store updated to new date (D-92 "moved deadline is new info").
    - Test: baseline/first sync (suppressNotifications true) → ALL current expirations seeded into store, ZERO notifications fired (locked decision 3 — fresh-connect storm prevention).
    - Test: single affected key → digest body uses `humble.notification.expiringBodySingle` naming the game title; 2+ keys → `humble.notification.expiringBodyPlural` with count (D-90).
    - Test: notification click handler calls `getMainWindow()?.show()` then `sendFrontendMessage('openScreen', '/humble-keys/waiting')` (D-91).
    - Test: `notifyHumbleExpirations` setting false → store still updated (dedup state advances) but no notification fires; likewise when `Notification.isSupported()` is false or `isSteamDeckGameMode` (Pitfall 4).
    - Test: digest copy reads only `title`/`expiration` — assert no `revealedKeyValue`/`keyindex` access (Pitfall 5).
  </behavior>
  <action>
    Create `src/backend/humble/expirationAlerts.ts` exporting `detectAndNotifyExpirationTransitions(keys: HumbleKey[], opts: { suppressNotifications: boolean }): void`. Iterate keys: for each, compare `key.expiration` against `humbleNotifiedExpirationStore.get(key.machineName)?.expiration ?? null`; when `current !== null && current !== last`, record the key as newly-expiring AND `humbleNotifiedExpirationStore.set(key.machineName, { expiration: current })` (always advance the persisted state so dedup works even under suppression). After the loop: if `opts.suppressNotifications` is true, return WITHOUT firing (first-sync baseline is now seeded — locked decision 3). Otherwise, if `newlyExpiring.length === 0` return; if `GlobalConfig.get().getSettings().notifyHumbleExpirations !== true` return (D-93 gate); if `!Notification.isSupported() || isSteamDeckGameMode` return (Pitfall 4 guard, cloned verbatim from dialog.ts). Build digest copy via a private `buildDigestCopy(newlyExpiring)`: single-key form `t('humble.notification.expiringBodySingle', "{{title}}'s Humble key now expires on {{date}}", { title, date })` with title/formatted-expiration from urgencyBadge's existing date helper; plural form `t('humble.notification.expiringBodyPlural', '{{count}} Humble keys gained expiration dates', { count })`; titles singular/plural via `humble.notification.expiringTitle` variants — build strings with `i18next.t()` in the main process (precedent: `epic.offline-notification-title` in utils.ts). Construct `new Notification({ title, body })`, attach `.on('click', () => { getMainWindow()?.show(); sendFrontendMessage('openScreen', '/humble-keys/waiting') })`, call `.show()`. Never read `HumbleKeyInternal` fields for copy. Write the test file mocking `electron` (Notification), `../main_window`, `../ipc`, the store, and `../config` (GlobalConfig.get().getSettings()).
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/expirationAlerts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/backend/humble/__tests__/expirationAlerts.test.ts` passes all behavior cases above.
    - `grep -c "suppressNotifications" src/backend/humble/expirationAlerts.ts` ≥ 2 (param + baseline branch) — first-sync suppression is structurally present.
    - `grep -c "Notification.isSupported() && !isSteamDeckGameMode\|isSteamDeckGameMode" src/backend/humble/expirationAlerts.ts` ≥ 1 (guard cloned).
    - `grep -c "sendFrontendMessage('openScreen', '/humble-keys/waiting')" src/backend/humble/expirationAlerts.ts` == 1.
    - `grep -c "revealedKeyValue\|keyindex\|HumbleKeyInternal" src/backend/humble/expirationAlerts.ts` == 0 (Pitfall 5).
  </acceptance_criteria>
  <done>Transition detection fires a correct per-sync digest with dedup, first-sync baseline suppression, settings/OS guards, and click-navigation — fully unit-covered with mocks.</done>
</task>

<task type="auto">
  <name>Task 2: Wire detection into runSync() after recomputeOwnership()</name>
  <files>src/backend/humble/library.ts</files>
  <read_first>
    - src/backend/humble/library.ts (lines ~449-497: getKeys()/recomputeOwnership(); the runSync structure; the setSyncState calls; the ~line 990-997 recomputeOwnership() hook point + return)
    - src/backend/humble/syncFence.ts (isStale/currentSyncGeneration — so the hook respects the same mid-sync-disconnect fence)
    - src/backend/humble/electronStores.ts (humbleSyncStore / whatever holds the prior syncedAt — to compute first-sync baseline)
    - src/backend/humble/expirationAlerts.ts (the Task-1 signature)
  </read_first>
  <action>
    At the START of `runSync()` (before any per-order fetch mutates sync state), capture `hadPriorSyncSnapshot` = whether a prior successful sync snapshot exists (read the existing sync store's `syncedAt`/equivalent; null/absent means first-ever sync for this connection). Immediately AFTER the existing `recomputeOwnership()` call (~line 997) and before `return { status: ... }`, call `detectAndNotifyExpirationTransitions(getKeys(), { suppressNotifications: !hadPriorSyncSnapshot })`. Add a doc-comment (matching the recomputeOwnership() comment style) explaining WHY it runs after recomputeOwnership() and after the isStale() fence — so a disconnect mid-sync never fires a notification — and WHY first-sync is suppressed (locked decision 3: a fresh connect where every expiring key transitions null→date must not fire a storm; the baseline is seeded silently). Import `detectAndNotifyExpirationTransitions` from `./expirationAlerts`. Do NOT fire on a fully-failed sync where recomputeOwnership itself no-ops (the existing gate already covers Steam-disconnected/empty).
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/library.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/backend/humble/__tests__/library.test.ts` passes (no regressions to existing sync tests).
    - `grep -c "detectAndNotifyExpirationTransitions" src/backend/humble/library.ts` ≥ 1 and it appears AFTER the `recomputeOwnership()` call (verify ordering by line numbers).
    - `grep -c "hadPriorSyncSnapshot\|suppressNotifications" src/backend/humble/library.ts` ≥ 1 (first-sync baseline computed and passed).
    - `pnpm codecheck` passes.
  </acceptance_criteria>
  <done>Every sync end runs transition detection with correct first-sync suppression; existing sync behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend main → OS notification daemon | Notification body text crosses to the OS toast surface |
| Backend → renderer navigation | `openScreen` IPC message triggers a route change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Information Disclosure | Digest notification body | mitigate | Copy built exclusively from the display-safe `HumbleKey` fields `title`/`expiration` via `i18next.t()`; the module never imports or reads `HumbleKeyInternal`/`revealedKeyValue`/`keyindex` (asserted by grep == 0 in acceptance). Matches C4/T-14-02 discipline (Pitfall 5). |
| T-15-03-02 | Tampering | Notification click navigation target | mitigate | The click handler navigates to a FIXED internal route literal `/humble-keys/waiting` — no user-controlled or product-derived URL is ever passed to `openScreen`; no external URL/shell open. |
| T-15-03-03 | Denial of Service | Notification storm on fresh connect | mitigate | First-sync baseline suppression (`suppressNotifications: !hadPriorSyncSnapshot`) seeds the notified-state store silently on the first sync; per-sync digest batching (D-90) prevents per-key floods on later syncs. |
| T-15-03-04 | Tampering | Package installs | n/a | Zero npm/pip/cargo installs — Electron built-in `Notification` only; no supply-chain surface. |
</threat_model>

<verification>
- `npx jest src/backend/humble/__tests__/expirationAlerts.test.ts` + `library.test.ts` green.
- `pnpm test:ci` full suite green; `pnpm codecheck` + `pnpm lint` clean.
- Manual (from 15-VALIDATION Manual-Only): trigger a sync where a key gains an expiration → observe one OS toast → click → app focuses on Keys-waiting; run sync again with same data → no second toast; fresh connect → no storm.
</verification>

<success_criteria>
- HSTORE-03: a previously non-expiring key gaining an expiration on sync fires exactly one digest OS notification (respecting the settings toggle), never repeats for the same date, and its click lands on the Keys-waiting view; a fresh connect fires no storm.
</success_criteria>

<output>
Create `.planning/phases/15-store-overlay-expiration-alerts/15-03-SUMMARY.md` when done.
</output>
