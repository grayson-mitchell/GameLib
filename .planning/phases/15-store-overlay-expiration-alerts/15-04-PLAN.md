---
phase: 15-store-overlay-expiration-alerts
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/humble/viewFilters.ts
  - src/backend/humble/__tests__/viewFilters.test.ts
  - src/frontend/screens/Humble/Keys/Waiting/index.tsx
autonomous: true
requirements: [HSTORE-03]

must_haves:
  truths:
    - "Keys within the urgency window (getUrgencyTier !== null) are lifted into a pinned 'Expiring soon' section at the top of the Keys-waiting view, sorted soonest-first (D-86, D-87)"
    - "A key in the pinned section is removed from the normal list — never rendered twice (move, don't duplicate) (D-88)"
    - "When zero keys are within the window, the pinned section renders nothing at all — no heading, no placeholder (D-89)"
    - "The pinned heading is a static, non-interactive element (no collapse/expand button) (D-86)"
  artifacts:
    - path: "src/common/humble/viewFilters.ts"
      provides: "Pure partitionWaitingByUrgency helper returning { pinned, rest }"
      contains: "partition"
    - path: "src/backend/humble/__tests__/viewFilters.test.ts"
      provides: "Unit coverage for partition membership + empty-window case"
      contains: "partition"
    - path: "src/frontend/screens/Humble/Keys/Waiting/index.tsx"
      provides: "Pinned section render above the normal list"
      contains: "expiringSoon"
  key_links:
    - from: "src/frontend/screens/Humble/Keys/Waiting/index.tsx"
      to: "src/common/humble/viewFilters.ts"
      via: "partition of selectKeysWaiting output by getUrgencyTier"
      pattern: "partition"
    - from: "src/frontend/screens/Humble/Keys/Waiting/index.tsx"
      to: "humbleKeys.expiringSoon"
      via: "static pinned-section heading rendered only when pinned.length > 0"
      pattern: "expiringSoon"
---

<objective>
Add a pinned "Expiring soon" section at the top of the existing Keys-waiting view. Keys
whose urgency tier is live (≤30 days, D-87) are MOVED out of the normal list into the
pinned section, sorted soonest-first. The section is hidden entirely when empty. No 4th
tab, no new sidebar entry, no new threshold logic — reuse Phase 13's urgency helpers
unchanged.

Purpose: HSTORE-03's "'expiring soon' surface lists keys nearing expiration sorted by urgency."
Output: A pure `partitionWaitingByUrgency` helper (unit-tested) plus the Waiting view render.

Decision note: references Phase 15 CONTEXT decisions D-86..D-89 (numeric collision with
Phase 14's historical D-77..D-80 — here they mean the Phase 15 decisions).

KNOWN-ISSUE INHERITANCE (locked user decision 2): The pinned rows reuse the existing
`UrgencyBadge`/`getUrgencyCountdownParts` countdown copy AS-IS. That helper has a
pre-existing 24-48h countdown defect (Phase 13 CR-01) that renders "2 days left" for the
25-47h window. Fixing it is OUT of Phase 15 scope — it belongs to `/gsd:plan-phase 13 --gaps`.
Do NOT add a fix task here; do NOT let phase verification flag the inherited countdown copy
as a Phase 15 regression. This plan only reuses the tier thresholds and countdown component
verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-store-overlay-expiration-alerts/15-CONTEXT.md
@.planning/phases/15-store-overlay-expiration-alerts/15-RESEARCH.md
@.planning/phases/15-store-overlay-expiration-alerts/15-PATTERNS.md
@.planning/phases/15-store-overlay-expiration-alerts/15-UI-SPEC.md

<interfaces>
From src/common/humble/viewFilters.ts:
- `selectKeysWaiting(keys: HumbleKey[]): HumbleKey[]` — already sorted soonest-first via compareWaiting.

From src/common/humble/urgencyBadge.ts:
- `getUrgencyTier(state, expiration): 'danger' | 'warning' | null` — null means "outside the window".

From src/frontend/screens/Humble/Keys/Waiting/index.tsx (current):
- `getUrgencyTier` already imported (line ~7), used per-row (line ~147).
- `const keys = selectKeysWaiting(humble?.keys ?? [])` (line ~33).
- Existing `<HumbleKeyRow>` map block (~lines 138-178) to reuse verbatim for both sections.

Heading chrome to clone STATIC parts only from
src/frontend/screens/Humble/Keys/components/HumbleKeyGroup/index.tsx:
- `.humbleKeyGroupHeading` / `.humbleKeyGroupLabel` / `.humbleKeyGroupCount` CSS classes.
- UI-SPEC lock: render as a static `<h2>`-equivalent — NO `<button>`, NO chevron, NO
  aria-expanded, NO onClick.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure partitionWaitingByUrgency helper + test extension (RED→GREEN)</name>
  <files>src/common/humble/viewFilters.ts, src/backend/humble/__tests__/viewFilters.test.ts</files>
  <read_first>
    - src/common/humble/viewFilters.ts (module-doc + selectKeysWaiting/compareWaiting; add the sibling helper here)
    - src/common/humble/urgencyBadge.ts (getUrgencyTier signature + tier thresholds — reuse unchanged, D-87)
    - src/backend/humble/__tests__/viewFilters.test.ts (existing describe structure + makeKey factory — extend, do not replace)
    - src/common/types/humble.ts (HumbleKey.state/expiration)
  </read_first>
  <behavior>
    - Test: given a mixed selectKeysWaiting output, `pinned` contains exactly the keys with `getUrgencyTier(state, expiration) !== null`, in the input order (soonest-first inherited from selectKeysWaiting).
    - Test: `rest` contains exactly the keys with `getUrgencyTier(...) === null`; pinned ∪ rest == input, pinned ∩ rest == ∅ (no key in both — D-88).
    - Test: when no key is within the urgency window, `pinned` is `[]` (empty-window → section will be hidden, D-89).
    - Test: a key exactly at the ≤30-day and ≤7-day boundaries lands in pinned (thresholds reused unchanged from getUrgencyTier).
  </behavior>
  <action>
    Add `export function partitionWaitingByUrgency(waiting: HumbleKey[]): { pinned: HumbleKey[]; rest: HumbleKey[] }` to `src/common/humble/viewFilters.ts`, following the file's existing module-doc / no-React-no-i18n-no-I/O convention. Implement as a single pass (not two independent filters) that pushes each key into `pinned` when `getUrgencyTier(key.state, key.expiration) !== null` else `rest` — preserving the input order so both arrays inherit selectKeysWaiting's soonest-first sort. Import `getUrgencyTier` from `./urgencyBadge`. Do NOT introduce any new threshold constant (D-87 — reuse getUrgencyTier only). Extend `viewFilters.test.ts` with a `describe('partitionWaitingByUrgency')` block covering the behavior cases above, reusing the existing `makeKey` factory (set `expiration` to controlled ISO offsets from a fixed `now`).
  </action>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/viewFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/backend/humble/__tests__/viewFilters.test.ts` passes including the new partition cases.
    - `grep -c "partitionWaitingByUrgency" src/common/humble/viewFilters.ts` == 1 (single exported helper).
    - `grep -c "getUrgencyTier" src/common/humble/viewFilters.ts` ≥ 1 (reuses the existing tier helper).
    - No new numeric threshold literal for days is introduced in viewFilters.ts (partition delegates entirely to getUrgencyTier).
  </acceptance_criteria>
  <done>A single-pass pure partition splits waiting keys into pinned/rest with disjoint, order-preserving membership and full unit coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Pinned "Expiring soon" section render in Keys-waiting view + i18n</name>
  <files>src/frontend/screens/Humble/Keys/Waiting/index.tsx</files>
  <read_first>
    - src/frontend/screens/Humble/Keys/Waiting/index.tsx (full current file — the selectKeysWaiting call ~line 33, the getUrgencyTier import ~line 7, the HumbleKeyRow map block ~lines 138-178)
    - src/common/humble/viewFilters.ts (partitionWaitingByUrgency from Task 1)
    - src/frontend/screens/Humble/Keys/components/HumbleKeyGroup/index.tsx (lines ~52-73: heading chrome — clone STATIC parts only)
    - src/frontend/screens/Humble/Keys/index.css (`.humbleKeyGroupHeading`/`.humbleKeyGroupLabel`/`.humbleKeyGroupCount` — reuse; the `--space-md` gap between pinned and rest per UI-SPEC Spacing)
    - src/frontend/screens/Humble/Keys/components/UrgencyBadge/index.tsx (the null-tier no-render convention — reused unchanged)
    - 15-UI-SPEC.md (Component Inventory row for the pinned section: static heading, not collapsible; Copywriting Contract `humbleKeys.expiringSoon`)
  </read_first>
  <action>
    Replace the single `selectKeysWaiting(...)` derivation with `const { pinned, rest } = partitionWaitingByUrgency(selectKeysWaiting(humble?.keys ?? []))` (import `partitionWaitingByUrgency` from `common/humble/viewFilters` — reuse the existing `getUrgencyTier` import, do not add a duplicate). Render the existing `<HumbleKeyRow>` map block TWICE: first a pinned section (only when `pinned.length > 0`) wrapped by a static heading, then the normal `rest` list. The pinned heading MUST be a static non-interactive element (e.g. `<div className="humbleKeyGroupHeading">` or an `<h2>`-equivalent) reusing `.humbleKeyGroupLabel` with copy `t('humbleKeys.expiringSoon', 'Expiring soon')` and `.humbleKeyGroupCount` showing `pinned.length` — NO `<button>`, NO chevron/FontAwesome chevron, NO `aria-expanded`, NO onClick (UI-SPEC deviation from HumbleKeyGroup). Wrap the ENTIRE pinned block (heading + list) in `{pinned.length > 0 && (...)}` so nothing renders when empty (D-89) — not a zero-row heading, not a `display:none` container. Rows in both sections reuse `HumbleKeyRow` + `UrgencyBadge` completely unchanged (the inherited 24-48h countdown copy is intentionally NOT touched — locked user decision 2). Add `--space-md` vertical gap between the pinned section and the rest list per UI-SPEC. Run `pnpm i18n` to register `humbleKeys.expiringSoon`.
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
    <human-check>On the Humble Keys → Waiting view with at least one key expiring within 30 days: a static "Expiring soon" heading with a count appears at the top, listing those keys soonest-first; each such key appears ONLY in that section (not also below); with no keys within 30 days, no "Expiring soon" heading renders at all; the heading has no expand/collapse affordance.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm codecheck` passes.
    - `grep -c "partitionWaitingByUrgency" src/frontend/screens/Humble/Keys/Waiting/index.tsx` == 1.
    - `grep -c "humbleKeys.expiringSoon" src/frontend/screens/Humble/Keys/Waiting/index.tsx` ≥ 1; `humbleKeys.expiringSoon` exists in `public/locales/en/translation.json`.
    - The pinned heading is not a `<button>` and has no `aria-expanded`/`onClick`/chevron (assert via source: no new `<button` or `aria-expanded` in the pinned block).
    - The pinned block is guarded by `pinned.length > 0` (grep the conditional).
    - `getUrgencyTier` is imported once (no duplicate import added).
  </acceptance_criteria>
  <done>The pinned Expiring-soon section moves in-window keys to the top with a static heading, renders nothing when empty, and never duplicates a row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Already-loaded humble.keys (renderer) → view | Pure client-side partition of already-fail-soft data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Information Disclosure | Pinned rows reuse HumbleKeyRow | mitigate | Rows reuse the existing `HumbleKeyRow`/`UrgencyBadge` components unchanged — they already render only display-safe fields (title, state, urgency countdown); no new field access, so no new disclosure surface. |
| T-15-04-02 | Tampering | Duplicate-row / partition drift | mitigate | Single-pass `partitionWaitingByUrgency` guarantees pinned ∩ rest == ∅ (unit-asserted, D-88) — a key can never render twice or leak into the wrong section. |
| T-15-04-03 | Tampering | Package installs | n/a | Zero npm/pip/cargo installs — pure composition of existing helpers/components. |
</threat_model>

<verification>
- `npx jest src/backend/humble/__tests__/viewFilters.test.ts` green.
- `pnpm codecheck` + `pnpm lint` clean.
- Manual: pinned section behavior on a real account (see Task 2 human-check).
- NOTE: the inherited 24-48h countdown copy defect (Phase 13 CR-01) is a known, out-of-scope
  issue for Phase 15 (locked user decision 2) — not a Phase 15 regression.
</verification>

<success_criteria>
- HSTORE-03: the Keys-waiting view surfaces expiring-soon keys in a pinned, soonest-first section that hides when empty and never duplicates a row; zero new threshold logic.
</success_criteria>

<output>
Create `.planning/phases/15-store-overlay-expiration-alerts/15-04-SUMMARY.md` when done.
</output>
