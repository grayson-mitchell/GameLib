---
phase: 28-tauri-keyring-real-safestorage-via-the-keyring-crate
plan: 05
type: execute
wave: 3
depends_on: ["28-04"]
files_modified:
  - src/backend/sidecar/__tests__/electronUntouched.test.ts
autonomous: true
requirements: [REQ-28-02, REQ-28-04]

must_haves:
  truths:
    - "Electron's stored refresh token is byte-identical before and after the sidecar exercises its full token path."
    - "The sidecar's token path leaves no new key, no modified key, and no deleted key in the shared configStore."
    - "A regression that reintroduces a configStore token write from the sidecar fails an automated check, not a human review."
  artifacts:
    - path: "src/backend/sidecar/__tests__/electronUntouched.test.ts"
      provides: "Byte-comparison proof of D-04 plus a repo-wide by-construction source gate"
      min_lines: 90
  key_links:
    - from: "src/backend/sidecar/__tests__/electronUntouched.test.ts"
      to: "src/backend/storeManagers/steam/electronStores.ts configStore"
      via: "unmocked real store read/snapshot/restore"
      pattern: "electronStores"
    - from: "src/backend/sidecar/__tests__/electronUntouched.test.ts"
      to: "src/backend/sidecar/keyringTokenStore.ts"
      via: "source-level gate asserting no configStore/TOKEN_STORE_KEY reach"
      pattern: "TOKEN_STORE_KEY"
---

<objective>
Land the automated half of this phase's proof pair (REQ-28-04): an integration test that proves
Electron's stored token is byte-identical before and after the sidecar runs its whole token path,
plus a repo-wide source gate that turns D-04's "by construction" claim into a failing test rather
than a code-review convention.

Purpose: REQ-28-02 explicitly requires the Electron token be *provably* untouched, and REQ-28-04
requires the proof be a reproducible artifact. The manual half (real Keychain round-trip, Deny
click, cross-build check) lands in plan 28-06.

Output: `src/backend/sidecar/__tests__/electronUntouched.test.ts`.

**Convention warning (load-bearing):** this repo's sidecar tests operate on the developer's REAL
`~/Library/Application Support/GameLib/…` directory — `pathShim.ts` has no `HOME`/`XDG_CONFIG_HOME`/
`APPDATA` override for darwin. This test MUST snapshot and restore `steamConfigStore.json`'s
contents in a `finally` block, exactly like `skeletonFlows.test.ts` Test 4 does, or it will destroy
the developer's real Steam session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-tauri-keyring-real-safestorage-via-the-keyring-crate/28-CONTEXT.md
@.planning/phases/28-tauri-keyring-real-safestorage-via-the-keyring-crate/28-PATTERNS.md
@.planning/phases/28-tauri-keyring-real-safestorage-via-the-keyring-crate/28-VALIDATION.md
@.planning/phases/28-tauri-keyring-real-safestorage-via-the-keyring-crate/28-03-SUMMARY.md
@.planning/phases/28-tauri-keyring-real-safestorage-via-the-keyring-crate/28-04-SUMMARY.md
@src/backend/sidecar/__tests__/skeletonFlows.test.ts
</context>

<interfaces>
<!-- Contracts from the earlier plans. Do not re-derive. -->

- `configStore` (Steam): `import { configStore as steamConfigStore } from '../../storeManagers/steam/electronStores'` — real, unmocked, backed by the developer's real config directory.
- Token key literal: `refreshToken` (`TOKEN_STORE_KEY` in `src/backend/storeManagers/steam/constants.ts:15`).
- `getTokenStore()` / `setTokenStore()` from `src/backend/storeManagers/steam/tokenStore.ts` (plan 28-03).
- `SidecarKeyringTokenStore` from `src/backend/sidecar/keyringTokenStore.ts` (plan 28-04) — its only outward reach is `requestRustInvoke`.
- `requestRustInvoke` from `src/backend/sidecar/sidecarRpc.ts` (plan 28-01) — mock it here; there is no Rust process in Jest.

Cleanup shape to copy — `skeletonFlows.test.ts` Test 4 (L222-261): set real store values, run, assert, `finally { steamConfigStore.clear() }`. **Do not copy `clear()` blindly** — this test must RESTORE the pre-existing value, not clear the store.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the Electron-untouched byte-comparison proof</name>
  <files>src/backend/sidecar/__tests__/electronUntouched.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/skeletonFlows.test.ts (L1-45 docstring on the real-config-dir convention; L222-261 Test 4's write/assert/finally shape)
    - src/backend/storeManagers/steam/electronStores.ts (the `configStore` export)
    - src/backend/sidecar/keyringTokenStore.ts (plan 28-04)
    - src/backend/storeManagers/steam/tokenStore.ts (plan 28-03)
    - .planning/phases/28-.../28-PATTERNS.md § "electronUntouched.test.ts" (L369-394)
  </read_first>
  <behavior>
    - After `SidecarKeyringTokenStore.setToken('sidecar-only-token')` runs against a fake keyring responder, the real `configStore`'s `refreshToken` value is `===` (strict, byte-identical string comparison) to the value captured before the call.
    - The same holds for `getToken()`, `clearToken()`, and `isAvailable()` — each exercised individually and then all four in sequence.
    - The same holds when every keyring channel REJECTS (`keyring:unavailable:…`) — the failure path writes nothing to `configStore` either.
    - The full serialized `configStore` snapshot (all keys, JSON-stringified) is identical before and after the sequence — no key added, changed, removed; not just `refreshToken`.
    - When `setTokenStore(new SidecarKeyringTokenStore())` is active, `getTokenStore().setToken(...)` (the path `user.ts` actually calls) also leaves `configStore` byte-identical — proving the seam, not just the class, is safe.
  </behavior>
  <action>
    Create the test file with a module docstring in `skeletonFlows.test.ts`'s style that states: this
    suite reads and writes the developer's REAL config directory; it snapshots and restores
    `steamConfigStore`'s pre-existing contents in `afterEach`/`finally`; it exists to satisfy
    REQ-28-02 / D-04.

    Setup: capture `JSON.stringify(steamConfigStore.raw_store)` (or the equivalent full-store read
    the `TypeCheckedStoreBackend` exposes) into a `before` snapshot. If the store already holds a
    `refreshToken`, leave it in place and use it as the byte-comparison subject; if it does not,
    seed a sentinel value (`'electron-owned-sentinel-<uuid>'`) so the comparison has a subject.
    Restore the full snapshot in a `finally`/`afterEach` — restore, never `clear()`.

    `jest.mock('../sidecarRpc')` with a programmable fake `requestRustInvoke` (resolve or reject per
    channel), matching plan 28-04's Task 1 fake.

    Implement each `<behavior>` bullet as its own `it(...)`, asserting with `toBe` on the string
    values (byte-identical, not `toEqual` on parsed objects) and on the full serialized snapshot.

    Add a final `it(...)` implementing the by-construction source gate: read
    `src/backend/sidecar/keyringTokenStore.ts` and `src/backend/sidecar/bootstrap.ts` with
    `readFileSync`, strip comment lines (`/^\s*(\/\/|\*|\/\*)/`) before matching, and assert neither
    stripped source matches `/TOKEN_STORE_KEY|TOKEN_PREFIX|configStore/`. Comments must be stripped
    first — an explanatory comment naming the forbidden identifier must not fail its own gate.

    Add a second source-gate `it(...)` asserting that `src/backend/sidecar/electronStub.ts`'s
    `safeStorage.isEncryptionAvailable` does not return `true` (match the stripped source against
    `/isEncryptionAvailable:\s*\(\):\s*boolean\s*=>\s*true/` and expect no match) — a regression
    guard on the exact lie SEAM.md §2 records.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/electronUntouched.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/backend/sidecar/__tests__/electronUntouched.test.ts` exits 0.
    - File is >= 90 lines; `grep -c "it(" …/electronUntouched.test.ts` returns >= 7.
    - `grep -c "finally\|afterEach" …/electronUntouched.test.ts` returns >= 1 and `grep -c "steamConfigStore.clear()" …/electronUntouched.test.ts` returns 0 (restore, not clear).
    - `grep -c "toBe(" …/electronUntouched.test.ts` returns >= 4 (strict byte comparison, not structural equality).
    - `grep -c "readFileSync" …/electronUntouched.test.ts` returns >= 1 (the source gate exists).
    - `grep -c "\-\-watch" …/electronUntouched.test.ts` returns 0.
    - After the run, `node -e "const s=require('fs').readFileSync(require('os').homedir()+'/Library/Application Support/GameLib/steamConfigStore.json','utf8');process.exit(0)"` succeeds and the file's `refreshToken` value is unchanged from before the run (verify by diffing a manual copy taken before invoking jest).
    - `npm run test:ci` exits 0.
  </acceptance_criteria>
  <done>An automated, reproducible proof exists that the sidecar token path — success and failure — leaves the shared Electron store byte-identical, backed by a comment-stripped source gate against regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar process → shared `configStore` file | The write-direction corruption trap documented in SEAM.md §2 |
| test process → developer's real config directory | This suite reads and mutates the developer's real Steam session file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | sidecar writing `TOKEN_STORE_KEY` into the shared store | mitigate | Byte-comparison assertions across all four `TokenStore` operations plus their failure paths, and a full-store snapshot comparison so an unrelated key write is also caught; backed by a comment-stripped source gate that fails the suite if `configStore`/`TOKEN_STORE_KEY` reappears in the sidecar modules. |
| T-28-09 | Spoofing | regression re-introducing `isEncryptionAvailable: () => true` | mitigate | Dedicated source-gate assertion against that exact literal. |
| T-28-11 | Denial of Service (data loss) | this test destroying the developer's real Steam session | mitigate | Full pre-run snapshot captured and RESTORED in `finally`/`afterEach`; `clear()` is explicitly forbidden by an acceptance criterion. Grep gate confirms no `steamConfigStore.clear()` call exists in this file. |
| T-28-04 | Information Disclosure | a real refresh token echoed into Jest output | mitigate | Assertions compare values with `toBe` but the suite runs under `npm run test:ci`'s `--silent`; no test logs the token value, and any seeded sentinel is synthetic. |
</threat_model>

<verification>
- `npx jest src/backend/sidecar/__tests__/electronUntouched.test.ts` green.
- `npm run test:ci` green (full suite, no cross-test pollution of the real config dir).
- `cd src-tauri && cargo build` clean.
- Manual sanity: `steamConfigStore.json`'s `refreshToken` value is identical before and after a full `npm run test:ci` run.
</verification>

<success_criteria>
- Electron's stored token survives the sidecar token path byte-identical, on both success and failure paths.
- The full store snapshot is unchanged (no collateral key writes).
- A comment-stripped source gate fails if the sidecar regains `configStore`/`TOKEN_STORE_KEY` reach or the stub regains its `isEncryptionAvailable: true` lie.
- The developer's real config directory is restored after the suite.
</success_criteria>

<output>
Create `.planning/phases/28-tauri-keyring-real-safestorage-via-the-keyring-crate/28-05-SUMMARY.md` when done
</output>
