---
phase: 39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec
plan: 05
type: execute
wave: 3
depends_on: [39-04]
files_modified:
  - src/backend/humble/user.ts
  - src/backend/humble/__tests__/user.test.ts
autonomous: true
requirements:
  - REQ-39-03

must_haves:
  truths:
    - "`HumbleUser.getLiveCsrfToken()` reads the csrf cookie through the seam unconditionally — the `session.fromPartition` fallback below its `if (seam !== null)` guard is gone"
    - "`checkHealthAndFlagExpiry()`'s csrf_cookie backfill takes the seam path unconditionally — the `seam === null` arm that called `session.fromPartition` is gone"
    - "Every test that drove either function without a seam installed is re-pointed at the seam path or deleted as already-covered, and each deletion is named"
    - "The three `session.fromPartition(HUMBLE_LOGIN_PARTITION)` occurrences belonging to plan 39-06 are untouched"
    - "`pnpm test --selectProjects Backend` is green with collected suite and test counts recorded"
  artifacts:
    - path: "src/backend/humble/user.ts"
      provides: "getLiveCsrfToken and the health-check csrf backfill on a single, seam-only path"
      contains: "getLoginWindowSeamOrThrow"
  key_links:
    - from: "src/backend/humble/user.ts"
      to: "src/backend/humble/loginWindowSeam.ts"
      via: "getLiveCsrfToken and checkHealthAndFlagExpiry both call the throwing accessor"
      pattern: "getLoginWindowSeamOrThrow"
---

<objective>
Collapse the two independent `humble/user.ts` seam sites that do not share a closure with anything
else: `getLiveCsrfToken()`'s inverted guard and `checkHealthAndFlagExpiry()`'s csrf-backfill branch.

Purpose: these two are separable from the `watchForLogin` cluster (plan 39-06) because each declares
its own `seam` local inside its own function. Doing them in their own plan keeps 39-06's much larger
test rewrite from having to carry them, and keeps the backend suite green at this wave boundary.
Output: two functions on one path, two test describes re-pointed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-RESEARCH.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-PATTERNS.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-04-SUMMARY.md

<line_numbers_are_stale_by_design>
Every line number below was verified against `ed1fdf71d` on 2026-09-02, BEFORE plan 39-04 deleted
`disconnect()`'s five-step arm from this same file. They will have shifted. Locate each site by its
ENCLOSING FUNCTION NAME and its predicate, never by line number alone.

This project has a standing record that a finding can name the wrong file with the right line
numbers. Read the site before editing at it and confirm the predicate is actually there.
</line_numbers_are_stale_by_design>

<interfaces>
The accessor plan 39-02 created, in `src/backend/humble/loginWindowSeam.ts`:
```typescript
export function getLoginWindowSeamOrThrow(): LoginWindowSeam
```

Site 1 — `HumbleUser.getLiveCsrfToken()`, was `humble/user.ts:177-211`:
```typescript
static async getLiveCsrfToken(): Promise<string | undefined> {
  const seam = getLoginWindowSeam()
  if (seam !== null) {
```
The guard is inverted relative to the other sites: the SEAM path is the `if` arm, and the dead code
is the `try { session.fromPartition(...) }` fallback that follows it (was lines 193-211). Collapsing
this one deletes the code AFTER the block, not inside it. Read it carefully — an executor pattern-
matching on "delete the `if` arm" will delete the surviving path.

Site 2 — `checkHealthAndFlagExpiry()`'s csrf_cookie backfill, was `humble/user.ts:873-891`:
```typescript
const seam = getLoginWindowSeam()
if (seam === null) {
  // ── Electron path (unchanged) ─────────────────────────────────────
  try {
    const csrfSes = session.fromPartition(HUMBLE_LOGIN_PARTITION)
```
Standard shape: the `seam === null` arm is dead, the `else` arm (marked `// ── Tauri seam path ──`,
was line 891) survives.
</interfaces>

<sites_this_plan_must_not_touch>
`humble/user.ts` still carries three `session.fromPartition(HUMBLE_LOGIN_PARTITION)` occurrences
that belong to plan 39-06 — inside `watchForLogin()` (the `ses` local and its `setUserAgent`),
inside `checkCookie()`, and inside `finishLogin()`'s csrf capture. All three share the closure-scoped
`seam` local declared once in `watchForLogin()`, which is why they must collapse together in one
plan and not here.

Also do not touch: the ternary in `watchForLogin`'s notify call, the `if (seam !== null)` window-open
guard, or the `if (seam !== null && seamLabel !== null)` guard inside the nested `settle()`. All
three read the same `watchForLogin` local.

Do not drop the `session` import or `HUMBLE_LOGIN_PARTITION` from the constants import in this plan.
Both are still used by 39-06's sites. Plan 39-06 owns their removal.
</sites_this_plan_must_not_touch>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse getLiveCsrfToken and the health-check csrf backfill</name>
  <files>src/backend/humble/user.ts</files>
  <read_first>
    - `src/backend/humble/user.ts` — locate and read `static async getLiveCsrfToken()` in full, including the doc comment above it and the `session.fromPartition` fallback that follows the `if (seam !== null)` block (was lines 170-215)
    - `src/backend/humble/user.ts` — locate and read `checkHealthAndFlagExpiry()` in full, including the WR-03/S-09 comment block explaining why the backfill previously called `session.fromPartition()` unconditionally with no guard (was lines 858-905)
    - `src/backend/humble/loginWindowSeam.ts` — the accessor's name and error message
  </read_first>
  <action>
    Site 1, `getLiveCsrfToken()`: replace `const seam = getLoginWindowSeam()` with the throwing
    accessor, remove the `if (seam !== null) { ... }` WRAPPER while keeping its body unconditional,
    and delete the `session.fromPartition` fallback that follows it. Note the inversion: here the
    surviving code is inside the `if`, and the dead code is after it. Also delete the comment at the
    top of the fallback that reads "if it fell through to session.fromPartition below" — it
    describes control flow that no longer exists.

    Site 2, `checkHealthAndFlagExpiry()`'s backfill: replace `const seam = getLoginWindowSeam()` with
    the throwing accessor, delete the `seam === null` arm in full (the `try { const csrfSes =
    session.fromPartition(...) }` block and its catch), and promote the `else` arm's body to
    unconditional.

    Rewrite the surrounding dual-build commentary at both sites. Specifically, the S-09 paragraph
    that says the backfill "previously called session.fromPartition() unconditionally with NO
    getLoginWindowSeam() guard anywhere in this function -- under Tauri this silently no-op'd every
    health check" describes a historical defect against a build that no longer exists. Keep the
    still-true half — that the backfill opens a temporary HIDDEN window through the seam, the same
    shape `disconnect()`'s cookie step uses — and drop the Electron comparison.

    Do NOT drop the `session` import or `HUMBLE_LOGIN_PARTITION` from `./constants` in this plan.
    Three occurrences belonging to plan 39-06 still need both.
  </action>
  <verify>
    <automated>pnpm codecheck; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "session.fromPartition(HUMBLE_LOGIN_PARTITION)" src/backend/humble/user.ts` returns exactly 3. If it returns 4, one of this plan's two sites was missed; if it returns fewer than 3, this plan deleted a site belonging to plan 39-06. Either way, stop.
    - `grep -c 'getLoginWindowSeamOrThrow' src/backend/humble/user.ts` returns at least 2.
    - `grep -c 'getLoginWindowSeam()' src/backend/humble/user.ts` returns exactly 1 — only `watchForLogin()`'s declaration, which plan 39-06 owns, remains.
    - Reading `getLiveCsrfToken()` back: it contains no `if (seam` predicate and its body still performs the seam cookie read that was inside the original `if` arm — quote the surviving lines in the SUMMARY, because this is the one site where a mechanical "delete the guarded arm" edit deletes the wrong half.
    - `pnpm codecheck` exits 0.
  </acceptance_criteria>
  <done>Both functions read the csrf cookie through the seam unconditionally; exactly three `session.fromPartition(HUMBLE_LOGIN_PARTITION)` occurrences remain for plan 39-06.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point the getLiveCsrfToken and health-check test describes</name>
  <files>src/backend/humble/__tests__/user.test.ts</files>
  <read_first>
    - `src/backend/humble/__tests__/user.test.ts` — the `checkHealthAndFlagExpiry()` describe (was lines 720-879) and the `getLiveCsrfToken() (WR-03)` describe (was lines 1114-1195), both in full
    - The already-existing seam-path coverage: the `checkHealthAndFlagExpiry() — csrf_cookie backfill seam path (S-09, Plan 18)` describe (was line 1635) — read it before deleting anything, because some of the Electron-path tests may already have a seam-path twin there
    - The fake-seam fixture as plan 39-04 left it (module-scope mocks or an `installFakeSeam()` helper), plus its `beforeEach` defaults
    - The Task 1 diff
  </read_first>
  <action>
    Every test in those two describes that drives `HumbleUser.getLiveCsrfToken()` or
    `HumbleUser.checkHealthAndFlagExpiry()` without a seam installed now reaches the throwing
    accessor. For each, choose deliberately between:
    (i) re-pointing it at the seam path — install the fake seam plan 39-04 lifted to module scope,
        replace `mockCookiesGet.mockResolvedValue([{ value: 'x' }])` setups with the seam's
        `mockSeamCookies.mockResolvedValue({ total: n, matched: [...] })` shape, and replace
        `expect(mockFromPartition).toHaveBeenCalledWith('persist:humble')` assertions with the
        equivalent seam assertion; or
    (ii) deleting it as already covered by the S-09 seam-path describe — but only after reading that
        describe and confirming the specific behaviour really is covered there.

    Note the shape difference, because it is the most likely source of a silently weakened
    assertion: the Electron mock resolved an ARRAY of `{ value }` cookie objects; the seam resolves
    `{ total, matched }` where `total` is the unfiltered jar size that `classifyCookieRead()` uses as
    its liveness proof. A test re-pointed with `{ total: 0, matched: [] }` where it previously
    supplied a found cookie is not the same test. Set `total` to a value consistent with what the
    original test was simulating.

    Scope the seam install to these two describes only. Do NOT add a file-level `beforeEach` that
    installs a seam — the `startLogin` describes above still exercise the pre-collapse
    `watchForLogin` path until plan 39-06 lands, and a global install would silently change their
    behaviour and mask a real regression.

    If a test outside these two describes fails, stop and diagnose. That means Task 1 changed
    behaviour rather than removing dead code.
  </action>
  <verify>
    <automated>pnpm test --selectProjects Backend; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test --selectProjects Backend` exits 0 and its `Tests:` line reports zero failures. Record the exact `Test Suites:` and `Tests:` counts in the SUMMARY and confirm `user.test.ts` appears in the collected run. `--selectProjects` is case-sensitive and exits 0 on an unrecognised project name, so the exit code alone is not evidence.
    - The SUMMARY states the pre-change and post-change `Tests:` totals, names every deleted test, and for each deletion names the specific test in the S-09 seam-path describe that covers it.
    - For every re-pointed test, the SUMMARY records the `total` value chosen for `mockSeamCookies` and why it matches what the original Electron-path setup was simulating.
    - `grep -cE 'expect\(mockFromPartition\)' src/backend/humble/__tests__/user.test.ts` returns a number strictly lower than its pre-change value, and the SUMMARY states both numbers.
    - `pnpm codecheck` exits 0 and `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>Both describes are green against the seam path, no deletion is unexplained, and no re-pointed test silently weakened its own setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collapse edit -> the surviving code path | `getLiveCsrfToken()`'s guard is INVERTED relative to every other site in this workstream. A mechanical edit deletes the live path instead of the dead one. |
| Electron cookie mock -> seam cookie mock | The two mocks have different shapes. A careless translation produces a test that passes while simulating something the original never simulated. |

No new dependency, no network surface, no user input, no auth change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-19 | Tampering | `getLiveCsrfToken()`'s inverted guard | mitigate | The action calls out the inversion explicitly and acceptance requires the surviving seam read to be quoted back in the SUMMARY, so deleting the wrong half cannot pass as done. |
| T-39-20 | Tampering | collapsing sites owned by plan 39-06 | mitigate | Acceptance pins `session.fromPartition(HUMBLE_LOGIN_PARTITION)` at exactly 3 remaining occurrences and `getLoginWindowSeam()` at exactly 1, so both over- and under-collapse are caught by a number rather than by review. |
| T-39-21 | Tampering | the cookie-read liveness proof | mitigate | `classifyCookieRead()` distinguishes a genuinely empty jar from a dead cookie API using the unfiltered `total`. A re-pointed test that supplies `total: 0` where the original supplied a found cookie silently changes the verdict under test. Acceptance requires the chosen `total` and its justification to be recorded per test. |
| T-39-22 | Repudiation | deleting a test as "already covered" | mitigate | Deletion is only permitted after reading the S-09 seam-path describe, and acceptance requires each deletion to name the specific covering test. "Covered elsewhere" without a name is how coverage is lost. |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `pnpm codecheck` exits 0.
- `pnpm lint` exits 0.
- `pnpm test --selectProjects Backend` exits 0 with recorded suite and test counts.
- `grep -c "session.fromPartition(HUMBLE_LOGIN_PARTITION)" src/backend/humble/user.ts` returns 3.
- `grep -c 'getLoginWindowSeam()' src/backend/humble/user.ts` returns 1.
</verification>

<success_criteria>
- `getLiveCsrfToken()` and `checkHealthAndFlagExpiry()`'s backfill each take one path.
- Exactly the sites this plan owns were collapsed — proven by count, not by inspection.
- Every deleted test names its covering twin; every re-pointed test records its mock translation.
</success_criteria>

<output>
Create `.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-05-SUMMARY.md` when done.
</output>
