---
phase: 39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec
plan: 03
type: execute
wave: 2
depends_on: [39-02]
files_modified:
  - src/backend/humble/adapter.ts
  - src/backend/humble/library.ts
  - src/backend/humble/__tests__/adapter.test.ts
  - src/backend/humble/__tests__/library.test.ts
  - src/backend/sidecar/__tests__/netStub.test.ts
autonomous: true
requirements:
  - REQ-39-03

must_haves:
  truths:
    - "`humblePostRequest()` always routes through the seam — the `viaElectronNet` alternate is gone and so is `humblePostRequestViaElectronNet()`, the ~74-line function that was its only consumer"
    - "`adapter.ts` no longer imports `net` or `HUMBLE_LOGIN_PARTITION` if the deletion left them unused — an unused import is exactly the shape a fresh lint warning takes, and this plan must not hand plan 39-08 a warning it created"
    - "`library.ts`'s `revealTransportLabel` is the fixed string `'login-window seam transport'`; the `'electron-net transport'` alternate no longer exists anywhere in `src/`"
    - "Every test that asserted the electron-net transport label or the electron-net POST path is rewritten against the surviving seam path or retired, and the reason is recorded — including `sidecar/__tests__/netStub.test.ts`'s Group 2, which drives `revealKey` through the real `net` stub precisely BECAUSE no seam is installed"
    - "`pnpm test --selectProjects Backend` is green with its collected suite and test counts recorded"
  artifacts:
    - path: "src/backend/humble/adapter.ts"
      provides: "single-transport humblePostRequest with the orphaned electron-net implementation removed"
      contains: "getLoginWindowSeamOrThrow"
    - path: "src/backend/humble/library.ts"
      provides: "unconditional transport label"
      contains: "login-window seam transport"
  key_links:
    - from: "src/backend/humble/adapter.ts"
      to: "src/backend/humble/loginWindowSeam.ts"
      via: "humblePostRequest calls the throwing accessor and passes the seam straight to humblePostRequestViaSeam"
      pattern: "humblePostRequestViaSeam"
---

<objective>
Collapse the two dead seam predicates that live outside `humble/user.ts`: `adapter.ts`'s
`humblePostRequest` transport ternary (and the whole electron-net implementation behind it) and
`library.ts`'s cosmetic transport-label ternary.

Purpose: these two sites were missed entirely by the original WR-01 census. `adapter.ts`'s is the
largest single deletion in the workstream — the dead alternate drags a ~74-line function with it.
`library.ts`'s is one line, but the string it produces is asserted by tests, so it cannot be
dropped silently.
Output: one transport, one label, four files touched, no unused imports left behind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-RESEARCH.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-PATTERNS.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-02-SUMMARY.md

<interfaces>
The accessor plan 39-02 created, in `src/backend/humble/loginWindowSeam.ts`:
```typescript
export function getLoginWindowSeamOrThrow(): LoginWindowSeam
```
It throws a descriptive `Error` when no seam is installed. `getLoginWindowSeam()` and
`setLoginWindowSeam()` are unchanged and still exported.

The site being collapsed, `src/backend/humble/adapter.ts:270-279` (verified 2026-09-02):
```typescript
function humblePostRequest(
  path: string,
  body: string,
  csrfToken?: string
): Promise<HumbleRawResponse> {
  const seam = getLoginWindowSeam()
  return seam
    ? humblePostRequestViaSeam(seam, path, body, csrfToken)
    : humblePostRequestViaElectronNet(path, body, csrfToken)
}
```
Its orphaned callee spans `adapter.ts:358-431` (74 lines) and opens with
`function humblePostRequestViaElectronNet(...)` / `return new Promise((resolve, reject) => {` /
`const request = net.request({`.

The site being collapsed, `src/backend/humble/library.ts:1210-1213` (verified 2026-09-02):
```typescript
const revealTransportLabel =
  getLoginWindowSeam() !== null
    ? 'login-window seam transport'
    : 'electron-net transport'
```
</interfaces>

<a_third_test_file_the_research_did_not_name>
Found at plan time by sweeping every file referencing `getLoginWindowSeam`, `LoginWindowSeam` or
`session.fromPartition` across `src/`, `meta/` and `.planning/` — neither `39-RESEARCH.md` nor
`39-PATTERNS.md` names it.

`src/backend/sidecar/__tests__/netStub.test.ts` Group 2 (the describe at line 168,
`humblePostRequest reaches its own on('error') handler through the real stub (D-06 integration,
REQ-34.4-11)`) drives the exported `revealKey` from `humble/adapter.ts` against the REAL, unmocked
hardened `net` stub. Its own header comment at lines 29-31 states the mechanism explicitly: "This
test only exercises the Electron path (no seam is ever installed in this suite) —
`getLoginWindowSeam()` returns `null`, so `humblePostRequest` still falls through to this exact
`net.request` stub."

Task 1 removes that fall-through. After it lands, `revealKey` reaches the throwing accessor and the
suite fails with an unrelated error. This is REQ-39-03 fallout, it belongs to this plan, and Task 3
owns it.

The requirement that test was guarding — D-06 / REQ-34.4-11, that a transport error surfaces its
real cause rather than a 15-second timeout message — is NOT retired by the transport collapse. It
still applies to the seam transport. Do not delete the assertion; re-point it.
</a_third_test_file_the_research_did_not_name>

<measured_import_facts>
Verified against the live tree 2026-09-02 — check each one after your edit rather than trusting
this list, because it was taken before the deletion:
- `adapter.ts:2` — `import { net } from 'backend/platform'`. `net.` is referenced at line 364,
  inside `humblePostRequestViaElectronNet`. Deleting that function makes the import unused.
- `adapter.ts:9` — `HUMBLE_LOGIN_PARTITION` in the `./constants` import group. Its only code use
  is `adapter.ts:367`, also inside `humblePostRequestViaElectronNet`.
- `adapter.ts:14` — `import { getLoginWindowSeam, type LoginWindowSeam } from './loginWindowSeam'`.
  The type import is still needed by `humblePostRequestViaSeam`'s signature.
- `library.ts:45` — `import { getLoginWindowSeam } from './loginWindowSeam'`. Line 1211 is its only
  use; collapsing the ternary makes the import unused.
- `tsconfig.json` sets neither `noUnusedLocals` nor `noUnusedParameters`, so `pnpm codecheck` will
  NOT catch a leftover unused import. ESLint's `no-unused-vars` will. Do not rely on tsc here.
</measured_import_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse humblePostRequest to a single transport and delete the orphaned electron-net implementation</name>
  <files>src/backend/humble/adapter.ts</files>
  <read_first>
    - `src/backend/humble/adapter.ts` lines 1-20 (the import block, including `net` at line 2, `HUMBLE_LOGIN_PARTITION` at line 9, and the seam import at line 14), lines 185-300 (the doc comments at 194/254/259/283 and `humblePostRequest` at 270-279), and lines 330-435 (`humblePostRequestViaElectronNet` at 358-431 plus the doc comment at 341)
    - `src/backend/humble/loginWindowSeam.ts` — the `getLoginWindowSeamOrThrow` signature and error message plan 39-02 landed
  </read_first>
  <action>
    Replace `humblePostRequest`'s body with an unconditional
    `return humblePostRequestViaSeam(getLoginWindowSeamOrThrow(), path, body, csrfToken)`. Delete
    the ternary and the `const seam = getLoginWindowSeam()` local. Change the import at line 14 to
    bring in `getLoginWindowSeamOrThrow` alongside `type LoginWindowSeam`; drop
    `getLoginWindowSeam` from that import unless another site in this file still uses it (grep
    before deciding).

    Delete `humblePostRequestViaElectronNet` in full — lines 358-431, from the `function` keyword
    through its closing brace — now that its only caller is gone. Delete its own doc comment
    (around line 341) with it; a doc comment for a deleted function is not a record, it is debris.

    Then remove any import the deletion orphaned. Expect `net` (line 2) and
    `HUMBLE_LOGIN_PARTITION` (line 9) to become unused, but VERIFY with a grep over the
    post-deletion file rather than deleting on the strength of this plan's pre-deletion census.
    `pnpm codecheck` will not catch a leftover unused import; `pnpm lint` will, and leaving one
    hands plan 39-08 a fresh warning that this plan created.

    Finally, correct the dual-build doc comments that survive at roughly lines 194, 254, 259 and
    283. They describe a two-transport world: `net.request` versus the seam, "Electron's own
    Chromium network stack", "under Tauri there is no `net.request`/partition equivalent". Rewrite
    each to describe the single transport that ships. Keep any statement that is still true about
    the seam's request SHAPE (csrf header, form body) — only the transport-choice framing is
    stale.
  </action>
  <verify>
    <automated>pnpm codecheck && pnpm lint; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'humblePostRequestViaElectronNet' src/backend/humble/adapter.ts` returns 0.
    - `grep -rc 'humblePostRequestViaElectronNet' src --include='*.ts'` returns 0 across the whole tree.
    - `grep -c 'getLoginWindowSeamOrThrow' src/backend/humble/adapter.ts` returns at least 1.
    - `grep -cE "seam\s*\?" src/backend/humble/adapter.ts` returns 0.
    - `grep -vE '^\s*\*|^\s*//' src/backend/humble/adapter.ts | grep -c 'net\.'` returns 0, and if that holds, `grep -c "import { net } from 'backend/platform'" src/backend/humble/adapter.ts` returns 0.
    - `grep -vE '^\s*\*|^\s*//' src/backend/humble/adapter.ts | grep -c 'HUMBLE_LOGIN_PARTITION'` returns 0, and if that holds the identifier is absent from the `./constants` import group.
    - `pnpm codecheck` exits 0 and `pnpm lint` exits 0.
    - The per-file ESLint warning count for `src/backend/humble/adapter.ts` is no higher after this task than before it. Capture both with `npx eslint src/backend/humble/adapter.ts --format json` before and after, and record the two numbers in the SUMMARY.
  </acceptance_criteria>
  <done>One transport survives, the 74-line orphan is gone, no import was orphaned, and the file's own warning count did not rise.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse library.ts's transport-label ternary to the fixed string</name>
  <files>src/backend/humble/library.ts</files>
  <read_first>
    - `src/backend/humble/library.ts` lines 40-50 (the `getLoginWindowSeam` import at line 45) and lines 1195-1225 (the `revealTransportLabel` ternary at 1210-1213, the comment at 1207, and every consumer of the label)
  </read_first>
  <action>
    Replace the ternary at lines 1210-1213 with the fixed string
    `'login-window seam transport'`. This label is only ever logged, never branched on, so the
    behavioural risk is zero — but the string `'electron-net transport'` is asserted by tests in
    `library.test.ts`, which Task 3 handles, so do not treat this as a free change.

    Rewrite the comment at line 1207 that explains the label is computed from
    `getLoginWindowSeam()` "at the moment of logging" — that rationale existed to explain why the
    label could differ between calls. It cannot any more.

    Remove the now-unused `getLoginWindowSeam` import at line 45 if line 1211 was its only use;
    grep the post-edit file to confirm rather than assuming.
  </action>
  <verify>
    <automated>pnpm codecheck && pnpm lint; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "electron-net transport" src/backend/humble/library.ts` returns 0.
    - `grep -c "login-window seam transport" src/backend/humble/library.ts` returns 1.
    - `grep -cE 'getLoginWindowSeam\(\)\s*!==\s*null' src/backend/humble/library.ts` returns 0.
    - If `grep -vE '^\s*\*|^\s*//' src/backend/humble/library.ts | grep -c 'getLoginWindowSeam'` returns 0, then `grep -c "from './loginWindowSeam'" src/backend/humble/library.ts` returns 0.
    - `pnpm codecheck` exits 0 and `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>The label is unconditional, the dead string is absent from the file, and no orphaned import remains.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point the three test files that asserted the electron-net path</name>
  <files>src/backend/humble/__tests__/adapter.test.ts, src/backend/humble/__tests__/library.test.ts, src/backend/sidecar/__tests__/netStub.test.ts</files>
  <read_first>
    - `src/backend/humble/__tests__/adapter.test.ts` lines 18-40 (the electron-net mock introduced at line 24), lines 160-172 (the `setLoginWindowSeam` import at 166), and lines 1035-1145 in full (the describe holding the `setLoginWindowSeam(null)` calls at 1063 and 1138 and the fake-seam installs at 1067/1081/1094/1107/1122)
    - `src/backend/humble/__tests__/library.test.ts` lines 205-215 and 260-270 (the electron-net commentary and the `setLoginWindowSeam` import at 265), lines 2435-2450 (the partition cookie-jar commentary at 2444), and lines 2745-2850 in full (the two tests at 2776 and 2831 that assert the `'electron-net transport'` label, and the seam-installed control at 2808)
    - `src/backend/sidecar/__tests__/netStub.test.ts` lines 1-60 (the header explaining that Group 2 relies on no seam being installed), lines 95-105 (the `net` and `revealKey` imports), and lines 165-215 (Group 2's single `it`)
    - The Task 1 and Task 2 diffs
  </read_first>
  <action>
    Three files, one commit, one concern: retire every assertion about a transport and a label that
    no longer exist, without weakening what those tests were really guarding.

    In `adapter.test.ts`: the describe around lines 1042-1140 exercises the reveal POST across
    both transports. Delete the cases that drive the electron-net path via
    `setLoginWindowSeam(null)` and assert against the electron-net mock; keep and, where needed,
    extend the seam-path cases so the reveal POST's CONTENT contract (csrf header, form body,
    non-2xx handling) still has coverage — that contract survives the transport collapse and
    losing it would be a real regression hidden behind a green suite. If the electron-net mock
    declared around line 24 becomes entirely unreferenced, delete it; if any surviving test still
    uses it, leave it and say why in the SUMMARY.

    In `library.test.ts`: the test at line 2776
    (`round 6: calling-adapter log line names the electron-net transport and carries no
    fullCookieJarPresent field`) and the regression test at line 2831
    (`F-8: with no seam installed, the calling-adapter log line still names the electron-net
    transport`) both assert a string the source can no longer emit. The second is a
    no-seam-installed regression test for a branch that is gone — delete it. The first should be
    rewritten to assert the label the source now emits unconditionally, keeping its second half
    (that the log line carries no `fullCookieJarPresent` field) intact, because that half is about
    cookie-secrecy hygiene and has nothing to do with the transport. The seam-installed control at
    2808 asserting the label does NOT contain `'electron-net transport'` becomes tautological once
    the string is unreachable — fold it into the rewritten test rather than leaving a control that
    can no longer fail.

    Also update the two stale explanatory comments at `library.test.ts:210` and `:2444` and the
    one at `:2753` that frame the electron-net transport as a live alternative.

    In `netStub.test.ts`: Group 2's single `it` (`revealKey rejects with the seam-naming cause, not
    the pre-fix timeout message, and settles without REQUEST_TIMEOUT_MS ever being advanced`) reaches
    the real `net` stub only because `getLoginWindowSeam()` returned `null`. That route is gone. The
    REQUIREMENT it guards is not: D-06 / REQ-34.4-11 says a transport failure must surface its real
    cause rather than a 15-second timeout message, and that is just as true of the seam transport.
    Re-point the test — install a fake seam whose `revealPost` rejects with a named transport error,
    drive `revealKey`, and assert the rejection carries that cause and settles WITHOUT
    `REQUEST_TIMEOUT_MS`'s fake timer being advanced. Keep the fake-timers-with-real-`setImmediate`
    combination the existing test uses; that detail is what makes the assertion meaningful rather
    than a restatement of "it rejected".

    Rewrite the file's header comment (lines 20-50) in the same commit. Several of its paragraphs —
    the "no seam is ever installed in this suite" mechanism, the `app.userAgentFallback` workaround
    that exists because `standardBrowserUserAgent()` is called inside the electron-net path — become
    false or moot. A header that explains a mechanism the file no longer uses is how the next reader
    is misled. If Group 1 (the direct `net` stub tests, unrelated to `humblePostRequest`) still
    stands on its own, keep it and say so; if it does not, say that instead of quietly deleting it.

    Do not touch any other test in any of these files. If a test fails that this action did not name,
    stop and diagnose it — an unexpected failure here means Task 1 or Task 2 changed behaviour,
    not just dead code, and that is a finding, not an obstacle.
  </action>
  <verify>
    <automated>pnpm test --selectProjects Backend; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "electron-net transport" src --include='*.ts'` returns 0 across the whole tree, tests included.
    - `grep -rc 'humblePostRequestViaElectronNet' src --include='*.ts'` returns 0.
    - `pnpm test --selectProjects Backend` exits 0 and its `Tests:` line reports zero failures. Record the exact `Test Suites:` and `Tests:` counts in the SUMMARY and confirm `adapter.test.ts`, `library.test.ts` and `netStub.test.ts` all appear in the collected run — `--selectProjects` is case-sensitive and exits 0 on an unrecognised project name, so the exit code alone is not evidence.
    - The recorded post-change `Tests:` total is lower than the pre-change total by exactly the number of tests this task deleted, and that number is stated explicitly in the SUMMARY with each deleted test named.
    - `pnpm codecheck` exits 0 and `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>All three files are green, no assertion references a transport or label the source cannot emit, D-06/REQ-34.4-11's guard is re-pointed rather than dropped, and every deleted test is named with its reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| "unreachable" claim -> a 74-line deletion | `humblePostRequestViaElectronNet` is the single largest deletion in this workstream. If its caller were reachable, the failure surfaces at runtime on a reveal POST, not at compile time. |
| test rewrite -> retained coverage | Deleting the tests that drove the removed transport can silently delete the coverage of the request CONTENT contract those same tests carried. |

No new dependency, no network surface, no user input, no auth change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09 | Tampering | deletion of `humblePostRequestViaElectronNet` | mitigate | Deleted only after a tree-wide `grep -rc` proves zero remaining callers, and only because its sole call site was the ternary alternate this task removes. The replacement path uses the throwing accessor, so an unexpectedly-null seam raises a named error rather than falling through to a deleted branch. |
| T-39-10 | Tampering | test coverage of the reveal POST's content contract | mitigate | The action forbids deleting the seam-path cases and requires the csrf-header/form-body/non-2xx assertions to survive. Acceptance requires the exact count of deleted tests and each deleted test named, so a coverage drop cannot hide inside a green suite. |
| T-39-11 | Tampering | orphaned imports raising the lint baseline | mitigate | `tsconfig.json` has no `noUnusedLocals`, so tsc is blind to this. Acceptance greps the comment-stripped file for `net.` and `HUMBLE_LOGIN_PARTITION` before asserting the import's absence, and requires the per-file ESLint warning count to be recorded before and after. |
| T-39-12 | Repudiation | a tautological surviving control | mitigate | The `not.toContain('electron-net transport')` control at `library.test.ts:2808` can no longer fail once the string is unreachable. The action requires folding it in rather than leaving a green assertion that measures nothing. |
| T-39-13 | Repudiation | `netStub.test.ts` Group 2's D-06/REQ-34.4-11 guard | mitigate | This test was invisible to the phase research and would have failed as an "unexplained" red after Task 1. It is named here as REQ-39-03 fallout and re-pointed at the seam transport rather than deleted, so the requirement it guards (a transport error surfaces its real cause, not a 15s timeout) keeps a live assertion. Its stale header is rewritten in the same commit so the next reader is not told a mechanism the file no longer uses. |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `pnpm codecheck` exits 0.
- `pnpm lint` exits 0.
- `pnpm test --selectProjects Backend` exits 0 with recorded suite/test counts and all three
  touched test files confirmed present in the collected run.
- `grep -rc 'electron-net transport' src --include='*.ts'` and
  `grep -rc 'humblePostRequestViaElectronNet' src --include='*.ts'` both return 0.
</verification>

<success_criteria>
- One POST transport, one transport label, zero references to the removed alternates anywhere in
  `src/`.
- No import orphaned by the deletions.
- Every deleted test is named in the SUMMARY with the reason it can no longer exist.
</success_criteria>

<output>
Create `.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-03-SUMMARY.md` when done.
</output>
