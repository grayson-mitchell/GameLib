---
phase: 39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py
  - .planning/IPC-PORT-INVENTORY.md
autonomous: true
requirements:
  - REQ-39-02

must_haves:
  truths:
    - "`python3 .planning/phases/34.5-*/preload-surface-gate.py` exits 0 — the floor equals the union the extractor actually measures today (206), not a number chosen to make the gate pass"
    - "`IPC-PORT-INVENTORY.md`'s `## Totals` -> `Unique channels` equals its own bucket-line name count, so the masked check-5 defect is fixed in the same edit as the floor and the next run cannot produce a fresh red that looks like a regression this phase introduced"
    - "The 18 names removed from the bucket lines are exactly the window-chrome cluster measured as documented-but-absent-from-code; no live channel loses its documentation, and `check_coverage` still finds zero code-channels missing from the doc"
    - "Both provenance markers (`34.5-PRELOAD-SURFACE-AUDIT.md`, `F-34.5-G6-10`) survive the edit"
    - "The floor is still capable of failing — demonstrated, not asserted"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py"
      provides: "the preload-surface gate with a floor re-derived from the live extractor"
      contains: "AUDITED_UNION_FLOOR"
    - path: ".planning/IPC-PORT-INVENTORY.md"
      provides: "bucket lines and Totals row reconciled against the live preload surface"
      contains: "Unique channels"
  key_links:
    - from: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py"
      to: ".planning/IPC-PORT-INVENTORY.md"
      via: "check_totals_reconciliation compares the doc's stated Unique channels against its own parsed bucket-line names"
      pattern: "AUDITED_UNION_FLOOR = 206"
---

<objective>
Disposition the preload-surface planning gate: re-derive its stale floor from the union the
extractor actually measures, and fix the third, currently-masked defect that the floor repair would
otherwise unmask as a fresh red gate.

Purpose: this gate runs in CI and is hard-red. Its 217 floor predates Phase 35's removal of the
window-chrome IPC channels. The 206 the extractor reports is correct; the floor is stale. This is
one of the two gates standing between `meta/runPlanningGates.py` and 7/7.
Output: a floor and an inventory that agree with each other and with the live preload surface.

This plan does NOT touch the seam-parity-sweep gate. That gate's disposition depends on work this
phase has not done yet — see `<why_the_other_gate_is_not_here>` below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-RESEARCH.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-PATTERNS.md

<why_the_other_gate_is_not_here>
`runPlanningGates.py` is 5/7. The other failing gate,
`.planning/phases/34.4.1-.../seam-parity-sweep-gate.py`, cannot be dispositioned yet.

Found at plan time, and recorded here because neither `39-RESEARCH.md` nor `39-PATTERNS.md`
surfaces it: that gate carries a module-level floor list, `EXPECTED_AXIS_A_SITES`, naming eight
`getLoginWindowSeam()` call sites that MUST still exist —
`humble/adapter.ts:275`, `humble/user.ts:178`, `:274`, `:740`, `:1034`,
`sidecar/oauthLoginCapture.ts:195`, `storeManagers/legendary/user.ts:137`, and
`sidecar/humbleLoginFlowRegistration.ts:457`. REQ-39-03 deletes seven of those eight. Repairing
that gate now would make it pass, and then this same phase's own dead-seam collapse would break it
again — with a failure that looks like a regression the collapse introduced rather than an
inherited pin that the collapse correctly invalidates.

Its disposition is therefore deferred to plan `39-08`, which runs after the collapse and decides
its label (RE-POINT for the moved `electronStub.ts` path, plus RE-DERIVE or INVERT for the Axis A
floor list) against a tree where the sites are already gone. The research's claim that the
gate-disposition workstream "is fully independent" and can run in parallel with the dead-seam
collapse is FALSE for this gate; it is true for the one this plan fixes.

Consequence for this plan: `runPlanningGates.py` will still report 6/7, not 7/7, when this plan
lands. That is expected and correct. Do not attempt to reach 7/7 here.
</why_the_other_gate_is_not_here>

<measured_facts>
Produced by invoking the gate's own functions against the working tree at `ed1fdf71d`, 2026-09-02.
These supersede every figure in ROADMAP.md's Phase 39 section.

- `invoke` = 154, `send` = 52, union = 206, `push` = 27 (push is out of tally).
- `AUDITED_UNION_FLOOR` = 217 at line 39. The 206 is correct; the 217 is stale.
- `check_coverage`: `union - bucket_names` is EMPTY. Zero live channels are undocumented. The gap
  runs the other way — `bucket_names - union` is exactly these 18 names:
  `closeWindow`, `createNewWindow`, `gamepadAction`, `getEpicGamesStatus`, `health`,
  `isFrameless`, `isFullscreen`, `isMaximized`, `isMinimized`, `maximizeWindow`,
  `minimizeWindow`, `openPatreonPage`, `openReleases`, `openWikiLink`, `setFullscreen`,
  `setZoomFactor`, `showAboutWindow`, `unmaximizeWindow`.
  A coherent window-chrome cluster retired when Tauri took over window decoration — this matches
  Phase 35's own record (`main.ts`'s deletion removed 136 IPC channel registrations) and this
  project's recorded `windowcontrols-grid-area-retired-by-3410` finding.
- MASKED DEFECT: `parse_bucket_names` returns 224 distinct names; `## Totals` states 225.
  `check_totals_reconciliation` (check 5) has never executed, because `check_multiline_awareness`
  (check 2) calls `fail()` -> `sys.exit(1)` first — `run_all_checks()`'s literal ordering, lines
  251-258.
- `parse_any_backtick_names` returns 299; `check_bucket_line_scoping` requires the bucket set to
  remain a STRICT subset of it.
- `parse_bucket_names` harvests names ONLY from lines carrying five or more backticked names.
  Deleting names can push a bucket line below that threshold, silently dropping that line's
  SURVIVING names from the census.
</measured_facts>

<disposition_vocabulary>
From `.planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md:1164`
(D-35-14-02). Use these four labels; do not invent new ones.
- RE-POINT — the pinned artifact moved, the invariant is still true elsewhere.
- RE-DERIVE — a census's stated number and its list must be updated together, never just the number.
- INVERT — the gate asserted "X still exists" and X's deliberate removal is what a later phase does.
- RETIRE — the artifact the gate reads no longer exists at all.

This gate is RE-DERIVE. It is not RETIRE: `src/preload/` still exists and is still load-bearing.
</disposition_vocabulary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the floor and reconcile the inventory Totals row in one edit</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py, .planning/IPC-PORT-INVENTORY.md</files>
  <read_first>
    - `.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py` lines 30-80 (constants, `INVENTORY_PATH`, `AUDITED_UNION_FLOOR` at line 39), lines 105-135 (`parse_bucket_names`, `parse_any_backtick_names`, `parse_totals_unique`, `fail`), and lines 140-260 (all six checks plus `run_all_checks`'s ordering)
    - `.planning/IPC-PORT-INVENTORY.md` in full — especially the `## Totals` table at lines 40-51, the explanatory paragraph beneath it, and every bucket line (any line carrying five or more backticked names)
  </read_first>
  <action>
    One logical edit spanning two files, landing as one commit. Splitting them moves the gate from
    "fails on check 2" to "fails on check 5" and looks like a regression this phase introduced.

    In `preload-surface-gate.py`: set `AUDITED_UNION_FLOOR = 206` at line 39. Rewrite the trailing
    parenthetical in `check_multiline_awareness`'s failure message — it currently says "measured at
    206 on the audited tree, 11 short", which described the OLD failing condition and becomes false
    the instant the floor is 206. Replace it with the new provenance: re-derived 2026-09-02 from the
    live extractor (invoke 154, send 52, union 206) after Phase 35 retired the window-chrome IPC
    channels. Do not change any other check, do not change `ASSERTION_COUNT`, and do not touch the
    self-test block — line 334 parameterises off `AUDITED_UNION_FLOOR` and adapts on its own.

    In `.planning/IPC-PORT-INVENTORY.md`: delete these 18 names, and only these 18, from the bucket
    lines carrying them — `closeWindow`, `createNewWindow`, `gamepadAction`, `getEpicGamesStatus`,
    `health`, `isFrameless`, `isFullscreen`, `isMaximized`, `isMinimized`, `maximizeWindow`,
    `minimizeWindow`, `openPatreonPage`, `openReleases`, `openWikiLink`, `setFullscreen`,
    `setZoomFactor`, `showAboutWindow`, `unmaximizeWindow`. Then set
    `| Unique channels | 225 |` to the post-deletion bucket-name count. That count is expected to be
    206; VERIFY it by re-running `parse_bucket_names` and use whatever it actually returns.

    Two hazards specific to this edit:
    - `parse_bucket_names` only harvests from lines with five or more backticked names. If deleting
      a stale name drops a line to four, that line's SURVIVING names silently leave the census and
      `check_coverage` will fail. After the deletion, confirm every line you edited still carries at
      least five backticked names; if one does not, merge it into an adjacent bucket line for the
      same category rather than padding it with filler.
    - `check_provenance` requires the literal strings `34.5-PRELOAD-SURFACE-AUDIT.md` and
      `F-34.5-G6-10` to survive somewhere in the document. Do not delete or reflow the paragraphs
      carrying them.

    Leave the `Ported to sidecar` and `Unported` rows alone. This phase measured neither, and
    inventing numbers for them is worse than the existing note. Extend the explanatory paragraph
    below the Totals table with a dated line stating that as of 2026-09-02 only the
    `Unique channels` row was re-derived (from the live extractor), and that the other two rows
    remain the Phase 34.1-era snapshot they already describe themselves as.
  </action>
  <verify>
    <automated>python3 ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/preload-surface-gate.py"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .planning/phases/34.5-*/preload-surface-gate.py` exits 0, with no line matching `^GATE FAILED:` and no line matching `Traceback (most recent call last)`.
    - `grep -c 'AUDITED_UNION_FLOOR = 206' .planning/phases/34.5-*/preload-surface-gate.py` returns 1; `grep -c 'AUDITED_UNION_FLOOR = 217'` returns 0.
    - A direct probe, run and pasted verbatim into the SUMMARY: import the gate module under a non-`__main__` name via `importlib.util` (its `main()` is guarded by `if __name__ == "__main__"`, so importing it does not run and does not rewrite anything), then print `len(parse_bucket_names(text))`, `parse_totals_unique(text)`, `len(invoke|send)`, `sorted(parse_bucket_names(text) - (invoke|send))`, and `sorted((invoke|send) - parse_bucket_names(text))`. Expected: the first three all equal 206, and both set differences are empty lists.
    - `grep -c '34.5-PRELOAD-SURFACE-AUDIT.md' .planning/IPC-PORT-INVENTORY.md` and `grep -c 'F-34.5-G6-10' .planning/IPC-PORT-INVENTORY.md` each return at least 1.
    - Mutation controls, both run and recorded in the SUMMARY, then reverted: (a) set `AUDITED_UNION_FLOOR = 207` and confirm the gate fails on check 2; (b) restore 206, set the Totals row to 205, and confirm the gate fails on check 5 with the `must reconcile exactly` message. Both must go red. A floor that cannot fail is not a floor, and a reconciliation that cannot fail is not a reconciliation.
  </acceptance_criteria>
  <done>The gate exits 0; floor, bucket-line census, and stated Totals all equal the same freshly measured number; both provenance markers survive; both mutation controls demonstrated red and reverted.</done>
</task>

<task type="auto">
  <name>Task 2: Record the disposition and re-run the full gate runner to confirm the expected 6/7</name>
  <files>.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-GATE-DISPOSITIONS.md</files>
  <read_first>
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md` from line 1164 (the D-35-14-02 disposition table — reuse its `| Gate | Pinned invariant | Disposition |` column shape and its four-label vocabulary)
    - `meta/runPlanningGates.py` (how it discovers gates by the `-gate.py` suffix, and its `MINIMUM_EXPECTED_GATES = 7` hard floor)
    - Task 1's own SUMMARY notes
  </read_first>
  <action>
    Create `39-GATE-DISPOSITIONS.md` in the phase directory. Plan `39-08` will APPEND the
    seam-parity gate's row to this same file after the dead-seam collapse; write it so that append
    is natural (a table that grows by a row, not prose that has to be rewritten).

    Three sections:
    1. A disposition table in the D-35-14-02 column shape. One row now:
       `34.5/preload-surface-gate.py` | its pinned invariant | **RE-DERIVE** | the evidence
       (`check_coverage` clean in the code->doc direction, 18 documented-but-absent names forming a
       coherent window-chrome cluster, floor and list moved together). Leave an explicit
       "pending — see plan 39-08" row for `34.4.1/seam-parity-sweep-gate.py` so its absence reads
       as deferred rather than overlooked.
    2. "The masked check-5 defect": state that the Totals row said 225 against 224 bucket-line
       names, that `check_totals_reconciliation` had never executed because check 2 exits first,
       and that this phase fixed it in the same edit as the floor precisely so the next run would
       not produce a fresh-looking red. Record the before and after numbers.
    3. "Why the seam-parity gate is deferred": reproduce the `EXPECTED_AXIS_A_SITES` finding from
       this plan's `<why_the_other_gate_is_not_here>` block, naming all eight pinned sites and
       which seven REQ-39-03 removes. Also record the constraint that `runPlanningGates.py` hard-
       fails if fewer than `MINIMUM_EXPECTED_GATES = 7` gates are discovered, so RETIRE-by-deletion
       is not available for that gate — whatever its disposition, the file stays on disk.

    Then run the full runner. Expect `6/7`. Record the exact output line. Do NOT repair the second
    gate here.

    Do not put lint figures or dead-seam census figures in this document. Those are other
    workstreams with their own records, and mixing them is exactly what the phase's commit-hygiene
    rule exists to prevent.
  </action>
  <verify>
    <automated>python3 meta/runPlanningGates.py; echo "exit=$? (non-zero and 6/7 is the EXPECTED outcome of this plan)"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 meta/runPlanningGates.py` prints a final line exactly matching `6/7 planning gates passed.` — one better than the 5/7 baseline, and not 7/7.
    - Its output contains exactly one line matching `^\[FAIL\]`, and that line names `seam-parity-sweep-gate.py`. It contains zero `[FAIL]` lines naming `preload-surface-gate.py`.
    - `.planning/phases/39-*/39-GATE-DISPOSITIONS.md` exists, is at least 40 lines, contains a markdown table with a `Disposition` column, and `grep -c 'RE-DERIVE'` returns at least 1.
    - The document contains the literal strings `225`, `EXPECTED_AXIS_A_SITES`, and `MINIMUM_EXPECTED_GATES`.
    - `grep -ciE 'max-warnings|4190' .planning/phases/39-*/39-GATE-DISPOSITIONS.md` returns 0 — the record must not bleed into the lint workstream.
  </acceptance_criteria>
  <done>The runner reports 6/7 with the sole remaining failure correctly attributed, and the disposition record exists with the deferred gate's reason written down rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script -> CI verdict -> human record | A green gate is read downstream as proof an invariant holds. Anything that makes the gate pass without the invariant holding crosses this boundary undetected. |
| planning document -> gate input | `IPC-PORT-INVENTORY.md` is BOTH the thing checked and an input to the check. One edit can move both sides at once and produce a self-consistent lie. |

No new dependency, no network surface, no user input, no auth change is introduced by this plan.
The genuine risk is integrity-of-verification, and only that.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | `AUDITED_UNION_FLOOR` | mitigate | The floor is set to the value the extractor measures (206), not to any value that merely passes. Mutation control (a) requires the gate to go RED at 207. A floor lowered to zero would pass silently; the control is what makes that impossible to land unnoticed. |
| T-39-02 | Tampering | `IPC-PORT-INVENTORY.md` bucket lines | mitigate | Only the 18 measured documented-but-absent names may be deleted, and the `check_coverage` set difference in both directions is asserted empty by an explicit probe rather than inferred from the gate's exit code. |
| T-39-03 | Tampering | provenance markers | mitigate | `check_provenance` exists so the record of how the preload gap was found cannot be quietly deleted once it stops being embarrassing. Acceptance asserts both markers survive. |
| T-39-04 | Tampering | the five-backtick bucket-line threshold | mitigate | Deleting names can push a line below the parser's threshold, silently removing its SURVIVING names from the census and producing a document that looks complete but is not. The action names the hazard; the two-direction set-difference probe catches it. |
| T-39-05 | Repudiation | the masked check-5 defect | mitigate | Fixing the floor alone would unmask a pre-existing failure a later reader would attribute to this phase. Floor and Totals land in one commit, and the before/after numbers are recorded so the attribution survives. |
| T-39-06 | Repudiation | the deferred seam-parity gate | mitigate | Leaving it red without a written reason is indistinguishable from missing it. Its `EXPECTED_AXIS_A_SITES` coupling and the `MINIMUM_EXPECTED_GATES` constraint are both recorded in `39-GATE-DISPOSITIONS.md` before the collapse begins. |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages; there are no package-manager install tasks, so no legitimacy audit is required. |
</threat_model>

<verification>
- `python3 .planning/phases/34.5-*/preload-surface-gate.py` exits 0 when invoked directly, so a
  runner-level result cannot mask a per-gate skip.
- `python3 meta/runPlanningGates.py` reports 6/7 with the single failure attributed to
  `seam-parity-sweep-gate.py`.
- Both mutation controls were demonstrated red and reverted.
- `git diff --stat` for this plan's commits touches only the two gate-related files and the new
  disposition record — no `src/`, no `package.json`, no lint artifact.
</verification>

<success_criteria>
- The preload-surface gate's disposition is RE-DERIVE, and its floor, bucket-line list, and stated
  Totals all equal the same freshly measured number.
- The masked 225-vs-224 defect is fixed and recorded, not merely un-hidden.
- The seam-parity gate's deferral is written down with its cause, not left to be rediscovered.
- Nothing under `src/` was touched.
</success_criteria>

<output>
Create `.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-01-SUMMARY.md` when done.
</output>
