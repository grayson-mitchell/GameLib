---
phase: 39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/humble/loginWindowSeam.ts
  - src/backend/sidecar/oauthLoginCapture.ts
  - src/backend/sidecar/oauthLoginFlowRegistration.ts
  - src/backend/sidecar/__tests__/oauthLoginCapture.test.ts
autonomous: true
requirements:
  - REQ-39-03

must_haves:
  truths:
    - "A single non-null-guaranteeing accessor, `getLoginWindowSeamOrThrow()`, exists in `loginWindowSeam.ts` and throws a descriptive error rather than returning `null`, so every collapsed call site fails loudly instead of dereferencing null"
    - "`getLoginWindowSeam()` and `setLoginWindowSeam()` keep their existing signatures — the deliberately-kept smoke-test guard at `humbleLoginFlowRegistration.ts:457` and every test that installs or clears a fake seam must keep compiling unchanged"
    - "`captureOAuthLogin()` no longer carries a `getLoginWindowSeam() === null` early return, and the `const activeSeam = seam` rebind that existed only to survive that narrowing is gone with it"
    - "`loginWindowSeam.ts`'s header no longer asserts that an Electron build exists and never installs a seam — it describes the single-shell (Tauri sidecar) reality"
    - "`oauthLoginFlowRegistration.ts`'s doc comment no longer references `captureOAuthLogin()`'s removed null check"
    - "`pnpm test --selectProjects Backend` is green, with the collected-suite count asserted, not just the exit code"
  artifacts:
    - path: "src/backend/humble/loginWindowSeam.ts"
      provides: "the throwing accessor every collapse site consumes, plus a header that describes the shipped build"
      contains: "getLoginWindowSeamOrThrow"
    - path: "src/backend/sidecar/oauthLoginCapture.ts"
      provides: "captureOAuthLogin with the unreachable unsupported-return removed"
      contains: "getLoginWindowSeamOrThrow"
  key_links:
    - from: "src/backend/sidecar/oauthLoginCapture.ts"
      to: "src/backend/humble/loginWindowSeam.ts"
      via: "import of getLoginWindowSeamOrThrow instead of getLoginWindowSeam"
      pattern: "getLoginWindowSeamOrThrow"
---

<objective>
Introduce the one new accessor the whole dead-seam collapse depends on, and use it to make the
first, smallest collapse — `captureOAuthLogin()`'s early return.

Purpose: every later plan in this workstream collapses a `getLoginWindowSeam() === null` predicate.
They all need the same replacement accessor, and creating it once here means no later plan has to
invent a contract. `oauthLoginCapture.ts` is the right first consumer because its dead branch is
small, self-contained, and its test fallout is a single test.
Output: `getLoginWindowSeamOrThrow()`, one collapsed call site, two corrected doc comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-RESEARCH.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-PATTERNS.md

<the_premise_this_workstream_rests_on>
Verified 2026-09-02, not assumed. `getLoginWindowSeam()` reads one module-scoped
`let installed: LoginWindowSeam | null = null` at `loginWindowSeam.ts:204`. The ONLY production
call to `setLoginWindowSeam()` in `src/` is `humbleLoginFlowRegistration.ts:340`,
`setLoginWindowSeam(createRustLoginWindowSeam())`, called unconditionally. Its enclosing
`registerHumbleLoginFlows()` runs at module scope — `handlers.ts:197` calls it directly, and
`handlers.ts` is statically imported by `bootstrap.ts` before any IPC handler can receive a call.
Since Phase 35 there is exactly one shell. `getLoginWindowSeam()` cannot return `null` by the time
any handler-reachable code runs.
</the_premise_this_workstream_rests_on>

<interfaces>
Current accessor, `src/backend/humble/loginWindowSeam.ts:202-214`:
```typescript
let installed: LoginWindowSeam | null = null

export function setLoginWindowSeam(seam: LoginWindowSeam | null): void {
  installed = seam
}

export function getLoginWindowSeam(): LoginWindowSeam | null {
  return installed
}
```

The structural sibling this repo already uses for a hard, descriptive failure —
`src/backend/platform/index.ts:609-621`'s `safeStorage`, whose members throw
`'safeStorage is not available in the sidecar — use getTokenStore() (see steam/tokenStore.ts)'`.
There is NO existing throwing-accessor idiom and NO existing `someAccessor()!` non-null-assertion
call-site idiom anywhere in `src/backend` (searched exhaustively 2026-09-02), so this is a
green-field choice, not a deviation from an established pattern.
</interfaces>

<gate_status_note>
`meta/runPlanningGates.py` is deliberately NOT expected to improve during this plan, and running it
here proves nothing about this plan's correctness.

`.planning/phases/34.4.1-.../seam-parity-sweep-gate.py` carries a module-level floor list,
`EXPECTED_AXIS_A_SITES`, naming eight `getLoginWindowSeam()` call sites that must still exist. One
of them is `src/backend/sidecar/oauthLoginCapture.ts:195` — the exact site Task 2 collapses. That
gate is already hard-red for an unrelated reason (a `FileNotFoundError` on the `git mv`'d
`electronStub.ts`), so this collapse cannot make it any redder, but it also must not be "fixed"
before the collapse finishes. Its disposition is plan `39-08`'s job, deliberately sequenced after
every collapse has landed. Do not repair it here, and do not treat its red state as a defect this
plan introduced.
</gate_status_note>

<deliberate_exclusion>
`src/backend/sidecar/humbleLoginFlowRegistration.ts:457-460`'s `const seam = getLoginWindowSeam();
if (!seam) { smokeLog(...) }` matches the same predicate family and is DELIBERATELY KEPT. It sits
inside a block gated by `process.env.GAMELIB_LOGIN_SEAM_SMOKE === '1'`, is a "did registration
actually run" sanity check for a debugging harness, and its own comment says to keep it
permanently. This is the single strongest reason to ADD a new accessor rather than change
`getLoginWindowSeam()`'s return type: changing the existing signature would make this guard a
type error and force its removal.
</deliberate_exclusion>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getLoginWindowSeamOrThrow() and rewrite the module's stale dual-build header</name>
  <files>src/backend/humble/loginWindowSeam.ts</files>
  <read_first>
    - `src/backend/humble/loginWindowSeam.ts` in full (262 lines) — especially the header doc at lines 1-25 and the holder/accessor block at lines 200-215
    - `src/backend/platform/index.ts` lines 605-625 — the `safeStorage` throw-on-use shape whose error-message style this accessor should match
  </read_first>
  <action>
    Add one exported function to `loginWindowSeam.ts`, immediately after `getLoginWindowSeam()`:

    `getLoginWindowSeamOrThrow(): LoginWindowSeam` — returns `installed` when non-null, otherwise
    throws an `Error` with a message that names the cause and the fix, in the same style as
    `platform/index.ts`'s `safeStorage` throws. The message must say that no login-window seam is
    installed, that `registerHumbleLoginFlows()` in
    `src/backend/sidecar/humbleLoginFlowRegistration.ts` is what installs it at sidecar startup,
    and that reaching this accessor before that runs is a wiring bug, not a supported state.
    Give it a doc comment stating explicitly that it is the accessor every production call site
    uses, and that `getLoginWindowSeam()` survives only for `setLoginWindowSeam(null)`-driven
    tests and for the deliberately-kept smoke-test guard in `humbleLoginFlowRegistration.ts`.

    Prefer the throwing wrapper over a bare `getLoginWindowSeam()!` at each call site: `!` produces
    a generic `Cannot read properties of null` at whichever member happens to be touched first,
    which is exactly the kind of failure this project has repeatedly had to trace backwards.

    DO NOT change `getLoginWindowSeam()`'s or `setLoginWindowSeam()`'s signatures or bodies.

    Then rewrite the header doc comment (lines 1-25). Two specific false statements must go:
    the sentence at lines 5-8 asserting that `humble/user.ts` "is compiled into BOTH the Electron
    main process and the Tauri Node sidecar" and keeps calling `session.fromPartition` under
    Electron; and the sentence at lines 17-19 asserting "The Electron build never calls
    `setLoginWindowSeam()` at all, so `getLoginWindowSeam()` always returns `null` there". Replace
    them with the single-shell reality: there is one build, the sidecar always installs a seam at
    startup, and the module's remaining value is the interface plus the pure `classifyCookieRead`
    classifier. Keep every other structural constraint the header records (no `'electron'` import,
    no `backend/sidecar` import) — those are still true and still load-bearing.
  </action>
  <verify>
    <automated>pnpm codecheck; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function getLoginWindowSeamOrThrow' src/backend/humble/loginWindowSeam.ts` returns 1.
    - `grep -c 'export function getLoginWindowSeam(): LoginWindowSeam | null' src/backend/humble/loginWindowSeam.ts` returns 1 — the original accessor is unchanged.
    - `grep -c 'export function setLoginWindowSeam(seam: LoginWindowSeam | null): void' src/backend/humble/loginWindowSeam.ts` returns 1.
    - `grep -vE '^\s*\*|^\s*//' src/backend/humble/loginWindowSeam.ts | grep -c 'throw new Error'` returns at least 1 — the throw is in code, not only described in a comment.
    - `grep -ci 'The Electron build never calls' src/backend/humble/loginWindowSeam.ts` returns 0.
    - `grep -ci 'compiled into BOTH' src/backend/humble/loginWindowSeam.ts` returns 0.
    - `pnpm codecheck` exits 0.
  </acceptance_criteria>
  <done>The throwing accessor exists and compiles; both original accessors are untouched; the header no longer describes an Electron build.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse captureOAuthLogin's unreachable early return and its two doc comments</name>
  <files>src/backend/sidecar/oauthLoginCapture.ts, src/backend/sidecar/oauthLoginFlowRegistration.ts</files>
  <read_first>
    - `src/backend/sidecar/oauthLoginCapture.ts` lines 30-40 (the `getLoginWindowSeam` import at line 34) and lines 185-260 (the function head, the `seam === null` early return at 195-198, the `const activeSeam = seam` rebind at 205, and every subsequent use of `activeSeam` in the nested `settle`/`poll` declarations)
    - `src/backend/sidecar/oauthLoginFlowRegistration.ts` lines 15-40 (the doc comment at line 27 referencing the removed check)
    - `src/backend/humble/loginWindowSeam.ts` after Task 1 — the new accessor's exact name and error message
  </read_first>
  <action>
    In `oauthLoginCapture.ts`: change the import at line 34 from `getLoginWindowSeam` to
    `getLoginWindowSeamOrThrow`. Replace `const seam = getLoginWindowSeam()` at line 195 with a
    call to the throwing accessor, delete the `if (seam === null) { return Promise.resolve({
    status: 'unsupported' }) }` block at 195-198 entirely, and delete the `const activeSeam = seam`
    rebind at line 205 together with its four-line explanatory comment. Rename every downstream
    `activeSeam` reference to the single local the accessor now returns. The rebind existed only
    because TypeScript's control-flow narrowing of `seam` did not survive into the nested
    `settle`/`poll` function declarations; once the accessor's return type is non-null at the point
    of declaration there is nothing to narrow and nothing to rebind.

    Check whether `OAuthCaptureOutcome`'s `'unsupported'` variant still has any producer after this
    deletion. If it does not, leave the type alone and note that in the SUMMARY — widening or
    narrowing that union is outside this phase's scope fence, and the flow-registration layer may
    still surface it.

    In `oauthLoginFlowRegistration.ts`: rewrite the doc sentence at line 27 that reads "Under
    Electron, `captureOAuthLogin()`'s own `getLoginWindowSeam() === null` check resolves ...". It
    describes a build that does not exist and a check that no longer exists.

    Framing note, binding on how you write both comments and the SUMMARY: do NOT describe this
    collapse in the "Electron behaviour preserved" language the `humble/user.ts` sites will use.
    This file's own sibling doc already framed its Electron case as hypothetical and defensive —
    it was registered in the Tauri-only sidecar module graph and "would be harmless even if
    somehow invoked there". The branch is unreachable now, same as the others, but it was never a
    genuine preserved-behaviour path, and a record that says otherwise misstates what the removed
    code was for.
  </action>
  <verify>
    <automated>pnpm codecheck; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getLoginWindowSeam()' src/backend/sidecar/oauthLoginCapture.ts` returns 0.
    - `grep -c 'activeSeam' src/backend/sidecar/oauthLoginCapture.ts` returns 0.
    - `grep -cE 'seam\s*===\s*null' src/backend/sidecar/oauthLoginCapture.ts` returns 0.
    - `grep -c 'getLoginWindowSeamOrThrow' src/backend/sidecar/oauthLoginCapture.ts` returns at least 1.
    - `grep -ci 'Under Electron' src/backend/sidecar/oauthLoginFlowRegistration.ts` returns 0.
    - `pnpm codecheck` exits 0.
  </acceptance_criteria>
  <done>The unreachable early return and its narrowing workaround are gone; both doc comments describe the shipped build; tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the one oauthLoginCapture test that asserted the removed branch</name>
  <files>src/backend/sidecar/__tests__/oauthLoginCapture.test.ts</files>
  <read_first>
    - `src/backend/sidecar/__tests__/oauthLoginCapture.test.ts` lines 235-255, 588-605, 726-740 (the three places a fake seam is installed and cleared) and lines 828-878 in full (the `registerOAuthLoginFlows() — registration kind (REQ-34.4.1-08)` describe, which holds all three no-seam tests)
    - The Task 2 diff for `oauthLoginCapture.ts`
  </read_first>
  <action>
    Exactly one test in this file asserted the deleted branch: the last `it` in the
    `registerOAuthLoginFlows() — registration kind (REQ-34.4.1-08)` describe, titled
    `forwards a valid { runner, url } pair to captureOAuthLogin (unsupported when no seam
    installed)`, which calls `setLoginWindowSeam(null)` and expects
    `{ status: 'unsupported' }`. That outcome is no longer producible.

    Rewrite it rather than deleting it, so the assertion it was really making — that the flow
    registration forwards a valid `{ runner, url }` pair through to `captureOAuthLogin` — keeps a
    home: install the same fake seam this file already builds for its other describes, drive the
    handler with a valid `gog` / `https://auth.gog.com/auth` pair, and assert the seam's `open`
    was called with that url. If the fake-seam fixture is scoped to another describe, hoist it or
    build a minimal local one; do not weaken the assertion to "did not throw".

    The other two `setLoginWindowSeam(null)` calls in the same describe belong to the unknown-runner
    and non-https tests. Those reject during validation and never reach the seam, so they are
    unaffected — read them and confirm that before changing anything, rather than assuming from
    the title.

    Leave the `setLoginWindowSeam(fakeSeam)` / `setLoginWindowSeam(null)` pairs at lines 241/245,
    594/601 and 732/736 alone. `setLoginWindowSeam` is still exported and still takes `null`;
    clearing the holder between tests remains correct.
  </action>
  <verify>
    <automated>pnpm test --selectProjects Backend -- src/backend/sidecar/__tests__/oauthLoginCapture.test.ts; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status: 'unsupported'" src/backend/sidecar/__tests__/oauthLoginCapture.test.ts` returns 0.
    - `pnpm test --selectProjects Backend` exits 0 AND its output line matching `Tests:` reports zero failures; record the exact `Suites:`/`Tests:` counts in the SUMMARY. `--selectProjects` is case-sensitive and exits 0 on an unrecognised project name, so a bare exit code is not evidence — the collected counts must be non-zero and must include `oauthLoginCapture.test.ts` in the run list.
    - `pnpm codecheck` exits 0.
    - `pnpm lint` exits 0 (this plan must not introduce a lint ERROR; the warning count is not this plan's business and is measured in plan 39-08).
  </acceptance_criteria>
  <done>The backend suite is green with the collected counts recorded, and no test asserts an outcome the source can no longer produce.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| "this branch is dead" claim -> deleted code | The only thing standing between a correct deletion and a silently broken login flow is the premise that `setLoginWindowSeam()` is always called before any handler runs. |
| test suite -> "the collapse was safe" | A green suite is read as proof the deletion changed nothing. A suite that no longer exercises the surviving path proves nothing. |

No new dependency, no network surface, no user input, no auth change is introduced by this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Tampering | deletion of the `seam === null` arm in `captureOAuthLogin` | mitigate | The premise was re-derived, not inherited: one `setLoginWindowSeam()` production call site, unconditional, at module scope, statically imported before any IPC handler can run. The replacement accessor THROWS rather than returning null, so if the premise is ever falsified the failure is loud and named at the accessor rather than silent at a member access. |
| T-39-07 | Tampering | `getLoginWindowSeam()`'s signature | mitigate | The signature is deliberately NOT changed. Changing it would force removal of the smoke-test guard at `humbleLoginFlowRegistration.ts:457`, which this phase's binding constraints explicitly keep. Acceptance asserts both original accessors survive byte-compatible. |
| T-39-08 | Repudiation | the rewritten test | mitigate | The removed test is replaced by one asserting the SURVIVING path (seam `open` called with the forwarded url), not deleted outright and not weakened to a did-not-throw. Acceptance requires the `Tests:` counts to be recorded, because `--selectProjects` exits 0 on a typo'd project name. |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `pnpm codecheck` exits 0.
- `pnpm test --selectProjects Backend` exits 0 with recorded, non-zero suite and test counts.
- `pnpm lint` exits 0 (errors only; warnings are plan 39-08's concern).
- `grep -rn 'getLoginWindowSeam()' src/backend/sidecar/humbleLoginFlowRegistration.ts` still shows
  the deliberately-kept smoke-test guard, unmodified.
</verification>

<success_criteria>
- `getLoginWindowSeamOrThrow()` exists, throws descriptively, and is the accessor every later plan
  in this workstream will import.
- `captureOAuthLogin()` has no null-seam branch and no narrowing rebind.
- `loginWindowSeam.ts` and `oauthLoginFlowRegistration.ts` no longer describe an Electron build.
- The smoke-test guard at `humbleLoginFlowRegistration.ts:457` is untouched.
</success_criteria>

<output>
Create `.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-02-SUMMARY.md` when done.
</output>
