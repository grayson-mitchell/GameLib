---
phase: 39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec
plan: 04
type: execute
wave: 2
depends_on: [39-02]
files_modified:
  - src/backend/humble/user.ts
  - src/backend/storeManagers/legendary/user.ts
  - src/backend/sidecar/__tests__/seamBranchParity.test.ts
  - src/backend/humble/__tests__/user.test.ts
  - src/backend/sidecar/__tests__/humbleFlows.test.ts
autonomous: true
requirements:
  - REQ-39-03

must_haves:
  truths:
    - "Both `disconnect()` wipe-step sites — `humble/user.ts:1034` and `storeManagers/legendary/user.ts:221` — build their `wipeSteps` array unconditionally from the seam; the five-step `session.fromPartition` arms are gone from both"
    - "`seamBranchParity.test.ts`, whose entire purpose is comparing an Electron wipe branch against a Tauri wipe branch, is dispositioned explicitly — not left to fail, and not deleted without a record"
    - "The two `disconnect()` tests that drove the removed branch (`humble/__tests__/user.test.ts`'s five-step regression and `sidecar/__tests__/humbleFlows.test.ts`'s REQ-34.4-09 ordering proof) are re-pointed at the surviving path, and the security invariant each guarded — that the credential-side cleanup runs even when every cookie-side step fails — still has a live assertion"
    - "`legendary/user.ts` no longer imports `session` from `backend/platform` if the collapse orphaned it"
    - "`pnpm test --selectProjects Backend` is green with collected suite and test counts recorded"
  artifacts:
    - path: "src/backend/humble/user.ts"
      provides: "disconnect() with a single, unconditional seam-driven wipeSteps array"
      contains: "getLoginWindowSeamOrThrow"
    - path: "src/backend/storeManagers/legendary/user.ts"
      provides: "Epic disconnect() with a single, unconditional seam-driven wipeSteps array"
      contains: "getLoginWindowSeamOrThrow"
  key_links:
    - from: "src/backend/storeManagers/legendary/user.ts"
      to: "src/backend/humble/loginWindowSeam.ts"
      via: "the throwing accessor replaces the seam-null branch in disconnect()"
      pattern: "getLoginWindowSeamOrThrow"
---

<objective>
Collapse the two `disconnect()` wipe-step sites — the Humble one and its verbatim Epic twin — and
disposition the 648-line regression guard whose only reason to exist was comparing those two
branches against each other.

Purpose: these two sites are the highest-stakes deletions in the workstream. Both are logout paths,
both carry an explicit ASVS security note about the credential-side cleanup running unconditionally,
and both are watched by a dedicated parity test plus a planning gate. They are collapsed together
because they are structurally identical and because `seamBranchParity.test.ts` breaks on whichever
one lands first — splitting them would make that file a shared dependency across two waves.
Output: one wipe path per store, an explicitly dispositioned parity guard, and two re-pointed tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-RESEARCH.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-PATTERNS.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-02-SUMMARY.md

<interfaces>
The accessor plan 39-02 created, in `src/backend/humble/loginWindowSeam.ts`:
```typescript
export function getLoginWindowSeamOrThrow(): LoginWindowSeam
```
`getLoginWindowSeam()` and `setLoginWindowSeam()` are unchanged and still exported.

`humble/user.ts:1034-1046` (verified 2026-09-02) — the Humble site:
```typescript
const seam = getLoginWindowSeam()
let wipeSteps: Array<[string, () => Promise<unknown>]>
if (seam === null) {
  const ses = session.fromPartition(HUMBLE_LOGIN_PARTITION)
  wipeSteps = [ /* five clear* steps */ ]
} else {
```

`storeManagers/legendary/user.ts:221-224` (verified 2026-09-02 — RESEARCH.md's citation of
"167-177" is the drifted number it flagged; 221 is the current true location):
```typescript
const seam = getLoginWindowSeam()

if (seam === null) {
  const ses = session.fromPartition('persist:epicstore')
```
Same shape as the Humble site: five-step Electron wipe in the `if` arm, seam wipe in the `else`.
</interfaces>

<a_regression_guard_the_research_did_not_name>
Found at plan time by sweeping every file referencing `getLoginWindowSeam`, `LoginWindowSeam` or
`session.fromPartition` across `src/`, `meta/` and `.planning/`. Neither `39-RESEARCH.md` nor
`39-PATTERNS.md` names it, and it is the single largest piece of collateral in this workstream.

`src/backend/sidecar/__tests__/seamBranchParity.test.ts` (648 lines, Phase 34.4.1 plan 10 Task 2,
REQ-34.4.1-11 / REQ-34.4.1-GAP-04) exists to assert capability-category PARITY between the Electron
wipe branch and the Tauri wipe branch at exactly the two sites this plan collapses. It reads the
real source from disk at test time. Three of its describes stop being meaningful the moment either
branch is removed:
- `registry completeness: the number of wipeSteps-shaped dual-branch sites found in src/backend
  matches SITES.length exactly` (line 608) — after the collapse there are zero dual-branch sites.
- `this plan does not touch humble/__tests__/user.test.ts (the untouched-five-step regression stays
  exactly as written)` (line 636) — it pins the very test Task 3 rewrites.
- `every Electron-only category is either present in Tauri, covered by a dated KNOWN_GAP entry, or a
  validated DECLARED entry` (line 405) — there is no Electron-only category once there is no
  Electron branch.
Its `DECLARED` mechanism additionally requires specific threat-id and category-term strings to be
present in the REAL source of the compared file — including the `authCache`/`hostResolver`
paragraphs inside the very comment blocks this collapse rewrites.

This file is NOT a defect. It caught a real asymmetry (F-6) that a green suite had tested around.
It is the correct kind of guard for a dual-build world, and this phase ends that world. Task 2
dispositions it deliberately; it must not be left failing, and it must not be silently deleted.
</a_regression_guard_the_research_did_not_name>

<a_second_test_file_the_research_did_not_name>
`src/backend/sidecar/__tests__/humbleFlows.test.ts`, the last describe in the file
(`humbleDisconnect — D-05 ordering proof (real disconnect(), not automocked)`, the `it` at line
1035, REQ-34.4-09 / T-34.4-24). It bypasses the file-wide automock via `jest.resetModules()` plus
`jest.requireActual('../../humble/user')` and drives the REAL `HumbleUser.disconnect()` with NO seam
installed, deliberately against the real `session.fromPartition()` no-op stub — its own comment
says the stub returns `{}`, so every wipe step throws "is not a function" and is swallowed by the
per-step try/catch. That is the point: it proves the three Humble store clears happen even when
every cookie-side step fails.

After Task 1, `disconnect()` reaches the throwing accessor and this test fails. The invariant it
guards is a SECURITY invariant (`user.ts`'s own ASVS note: a sign-out that revoked the session but
left `userInfo` behind is worse than one that left a stray cookie), and it survives the collapse
unchanged. Re-point it; do not delete it.
</a_second_test_file_the_research_did_not_name>

<gate_status_note>
`.planning/phases/34.4.1-.../seam-parity-sweep-gate.py` pins both of this plan's sites in its
`EXPECTED_AXIS_A_SITES` floor list (`humble/user.ts:1034`, `legendary/user.ts:137`) and both files
in `EXPECTED_SILENT_DROP_SITES` (findings S-07 and S-10, the F-6 authCache/hostResolver drops). That
gate is already hard-red and stays red through this plan by design. Its disposition — which will
have to address the Axis A floor list and the silent-drop registry, not just the moved
`electronStub.ts` path — is plan `39-08`'s job. Do not touch it here, and do not read its red state
as a defect this plan introduced.
</gate_status_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse both disconnect() wipe-step sites to a single seam-driven path</name>
  <files>src/backend/humble/user.ts, src/backend/storeManagers/legendary/user.ts</files>
  <read_first>
    - `src/backend/humble/user.ts` lines 985-1075 in full — the ASVS/dual-build doc comment at 991-993, the `getLoginWindowSeam()` call at 1034, the five-step `session.fromPartition` arm, the surviving seam arm, and the per-step try/catch that follows
    - `src/backend/storeManagers/legendary/user.ts` lines 1-20 (the `session` import at line 7 and the seam import at line 15), lines 165-320 in full — the ASVS comment at 167-176, the Electron/Tauri path paragraphs at 176-190, the collapse site at 221, both wipe arms, and the load-bearing step ORDER comment in the seam arm
    - `src/backend/humble/loginWindowSeam.ts` — the accessor plan 39-02 landed
  </read_first>
  <action>
    Collapse both sites the same way. In each: replace `const seam = getLoginWindowSeam()` with the
    throwing accessor, delete the `if (seam === null) { ... }` arm in full (the five-step
    `session.fromPartition` wipe and the `const ses = ...` it depends on), and promote the `else`
    arm's `wipeSteps` array to an unconditional assignment. Keep the per-step try/catch and the
    unconditional credential-side cleanup that follows EXACTLY as they are — those are the security
    boundary, not the branch.

    Preserve, verbatim, the ORDER comment inside the Epic seam arm explaining that
    `clearEpicStorage` must run FIRST and `clearEpicCookies` must run LAST. That ordering was
    established by the `epic-cookie-clear-read-divergence` debug session, it is load-bearing, and it
    has nothing to do with the branch being removed.

    Rewrite the dual-build framing in the surrounding comments — `humble/user.ts` around lines
    991-993, `legendary/user.ts` at 176-190 and 634. Two specific things must go: the "Electron path
    (seam === null): the ORIGINAL five session.fromPartition clear calls" paragraph, and the
    "Tauri path (seam !== null): there is no session.fromPartition() shape under the sidecar
    (electronStub.ts's stub returns `{}`)" paragraph. Both describe a choice that no longer exists.

    Two things in those comments must NOT be lost, because they document real, still-open residual
    risk rather than the removed branch: (a) the F-6 / T-34.5-20 note that `authCache` and
    `hostResolver` have no in-page-JS equivalent and are therefore not cleared at all under the seam
    path; and (b) the T-34.5-20 note that the app-wide cookie jar is shared with Humble/GOG/Amazon,
    so the Epic clear must stay domain-scoped and never become a blanket wipe. Rewrite them as
    standing limitations of the single wipe path rather than as a comparison against a deleted one.
    Deleting them would silently retire two recorded threats.

    Then drop `session` from `legendary/user.ts:7`'s import if the collapse orphaned it. Check
    `humble/user.ts` too but expect it to still need `session` — the `watchForLogin`/`checkCookie`/
    `finishLogin` sites are plan 39-06's, not this plan's. Grep the comment-stripped file rather
    than assuming either way; `tsconfig.json` has no `noUnusedLocals`, so `pnpm codecheck` will not
    tell you.
  </action>
  <verify>
    <automated>pnpm codecheck; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'seam\s*===\s*null' src/backend/storeManagers/legendary/user.ts` returns 0.
    - `grep -c 'getLoginWindowSeamOrThrow' src/backend/storeManagers/legendary/user.ts` returns at least 1.
    - `grep -c "session.fromPartition('persist:epicstore')" src/backend/storeManagers/legendary/user.ts` returns 0.
    - `grep -c "session.fromPartition(HUMBLE_LOGIN_PARTITION)" src/backend/humble/user.ts` returns exactly 4 — the disconnect occurrence at line 1037 is gone, the four belonging to plans 39-05 and 39-06 remain. If it returns anything else, stop: either this task deleted a site it does not own, or the file drifted.
    - `grep -n 'clearEpicStorage' src/backend/storeManagers/legendary/user.ts` and `grep -n 'clearEpicCookies'` both still return a match, and the ORDER-IS-LOAD-BEARING comment is still present between them.
    - `grep -ci 'authCache' src/backend/storeManagers/legendary/user.ts` returns at least 1 and `grep -ci 'T-34.5-20' src/backend/storeManagers/legendary/user.ts` returns at least 1 — the two recorded residual threats survived the comment rewrite.
    - `grep -cvE '^\s*\*|^\s*//' /dev/null; grep -vE '^\s*\*|^\s*//' src/backend/storeManagers/legendary/user.ts | grep -c 'session\.'` returns 0, and if so `grep -c "import { session } from 'backend/platform'" src/backend/storeManagers/legendary/user.ts` returns 0.
    - `pnpm codecheck` exits 0.
  </acceptance_criteria>
  <done>Both logout paths build one unconditional wipeSteps array; the security boundary, the step ordering, and the two recorded residual threats all survive; no orphaned import remains.</done>
</task>

<task type="auto">
  <name>Task 2: Disposition seamBranchParity.test.ts</name>
  <files>src/backend/sidecar/__tests__/seamBranchParity.test.ts</files>
  <read_first>
    - `src/backend/sidecar/__tests__/seamBranchParity.test.ts` in full (648 lines) — the header explaining what F-6 was and why a green suite was not evidence of parity, the `CATEGORY_MAP`, `SITES`, `KNOWN_GAP` and `DECLARED` registries, and all six describes (lines 340, 341, 379, 405-472, 473, 607, 636)
    - The Task 1 diff for both `user.ts` files
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md` from line 1164 (D-35-14-02's four-label disposition vocabulary: RE-POINT, RE-DERIVE, INVERT, RETIRE)
  </read_first>
  <action>
    This file compares a branch that no longer exists against a branch that does. Choose ONE
    disposition from the D-35-14-02 vocabulary and apply it in full. Do not leave the file failing
    and do not `git rm` it without a record.

    The two defensible choices, in preference order:

    INVERT (preferred). The file asserted "for every Electron-only capability category there is a
    Tauri counterpart, a dated KNOWN_GAP, or a validated DECLARED entry". Flip it: assert that ZERO
    dual-branch wipe sites remain in `src/backend` (its `SITES` registry becomes the assertion, not
    the input), and keep the `DECLARED` machinery pointed at what actually still matters — that the
    `authCache` and `hostResolver` categories are STILL not covered by the surviving seam path, and
    that both files still say so in source. That preserves the file's real value (the F-6 lesson
    that an uncovered capability must be declared in source, not merely absent) while dropping the
    parity comparison that has nothing left to compare. If you take this path, rewrite the header:
    it currently explains a defect in a dual-build world, and a reader in six months needs to know
    the file changed shape deliberately.

    RETIRE. Only if INVERT cannot be made non-vacuous — that is, if after inverting, every remaining
    assertion would pass against an empty `src/backend`. If you retire, `git rm` the file and record
    in the SUMMARY, explicitly: which assertions died with it, that F-6's authCache/hostResolver
    residual is now carried only by the source comments Task 1 preserved, and that no other test
    asserts it. A retirement whose lost coverage is not enumerated is indistinguishable from an
    accident.

    Whichever you choose, delete the final describe at line 636 (`this plan does not touch
    humble/__tests__/user.test.ts`). It pins a test that Task 3 rewrites by design, and leaving it
    would make Task 3 look like a violation of a standing contract rather than the point of the
    phase.

    Do not weaken an assertion to make it pass. If an anti-vacuity test in the `describe` at line
    473 can no longer fail after your change, that is the signal to retire it, not to keep it green.
  </action>
  <verify>
    <automated>pnpm test --selectProjects Backend -- seamBranchParity; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Either `src/backend/sidecar/__tests__/seamBranchParity.test.ts` exists and its suite passes with at least one assertion that can still fail, OR the file is absent from `git ls-files` and the SUMMARY enumerates every assertion retired with it.
    - If the file survives: `grep -c 'humble/__tests__/user.test.ts' src/backend/sidecar/__tests__/seamBranchParity.test.ts` returns 0 — the describe pinning that file is gone.
    - If the file survives: a mutation control, run and recorded in the SUMMARY then reverted — reintroduce a synthetic dual-branch wipe site (or an undeclared dropped category, whichever the inverted assertion targets) and confirm the suite goes RED. A guard that cannot fail is not a guard, and this project has recorded that failure mode repeatedly.
    - The SUMMARY names the chosen disposition using exactly one of RE-POINT / RE-DERIVE / INVERT / RETIRE, and states why the other three do not apply.
    - `pnpm test --selectProjects Backend` collects a non-zero suite count and the `Tests:` line reports zero failures.
  </acceptance_criteria>
  <done>The parity guard is either inverted into a still-falsifiable assertion or retired with its lost coverage enumerated; the disposition label and its justification are recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point the two disconnect tests that drove the removed branch</name>
  <files>src/backend/humble/__tests__/user.test.ts, src/backend/sidecar/__tests__/humbleFlows.test.ts</files>
  <read_first>
    - `src/backend/humble/__tests__/user.test.ts` lines 20-60 (the module-level `mockCookiesGet` / `mockClearStorageData` / `mockFromPartition` mock scaffolding), lines 899-1010 (the `disconnect() — D-07 full partition wipe` describe), and lines 1196-1300 (the existing fake-seam fixture: `mockSeamOpen`, `mockSeamCookies`, `mockSeamClearCookies`, `mockSeamClearStorage`, `mockSeamCookiesForDomain`, `mockSeamClose`, `mockSeamRevealPost`, the `fakeSeam` object, and its `beforeEach` defaults), plus lines 1765-1930 (the existing `disconnect() — seam path` describe, which already covers the surviving behaviour)
    - `src/backend/sidecar/__tests__/humbleFlows.test.ts` lines 1010-1120 (the D-05 ordering-proof describe in full, including the `jest.resetModules()` / `jest.requireActual` mechanism and the comment explaining why the real `session.fromPartition()` no-op is load-bearing for it)
    - The Task 1 diff
  </read_first>
  <action>
    Two files, one commit.

    In `humble/__tests__/user.test.ts`: the test at line 1828, `with no seam installed, the original
    five Electron wipe steps still run instead`, asserts a path that no longer exists — delete it.
    Then check the `disconnect() — D-07 full partition wipe` describe at 899-1010: every test in it
    that drives `HumbleUser.disconnect()` without installing a seam now reaches the throwing
    accessor. For each, decide between (i) re-pointing it at the seam path by installing the
    existing `fakeSeam` fixture, or (ii) deleting it as a duplicate of a test already in the
    `disconnect() — seam path` describe at 1765. Do not delete on a hunch — read the seam-path
    describe first and only delete what is genuinely already covered there.

    The `fakeSeam` fixture is currently declared INSIDE the describe at line 1196. Lift the mock
    functions, the `fakeSeam` object and its `beforeEach` defaults to module scope (or into a small
    `installFakeSeam()` helper near the top of the file) so the disconnect describe can use them
    too. Do NOT install the seam in a file-level `beforeEach` — the `startLogin` describes above
    still exercise the pre-collapse `watchForLogin` path until plan 39-06 lands, and a global
    install would silently change their behaviour now. Scope the install to the describes this task
    touches.

    In `sidecar/__tests__/humbleFlows.test.ts`: the D-05 ordering proof at line 1035 must keep
    proving what it proves — the three Humble store clears run even when every cookie-side step
    fails. Install a fake seam inside that `it` (after `jest.resetModules()`, alongside the existing
    `jest.doMock` calls) whose wipe methods all reject, then assert the same three
    `configStore` / `humbleLibraryStore` / `humbleSyncStore` clears still happened and
    `disconnect()` still resolves. Update the surrounding comment: it currently explains that the
    real `electronStub` `session.fromPartition()` returns `{}` and therefore every step throws.
    That mechanism is gone; the new one is an all-rejecting seam. Keep the `jest.requireActual`
    paragraph — that explanation is about the automock boundary and is still exactly right.

    If any test outside these two describes fails, stop and diagnose. An unexpected failure means
    Task 1 changed behaviour rather than removing dead code, and that is a finding.
  </action>
  <verify>
    <automated>pnpm test --selectProjects Backend; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'the original five Electron wipe steps still run instead' src/backend/humble/__tests__/user.test.ts` returns 0.
    - `grep -cE 'expect\(mockClearStorageData\)' src/backend/humble/__tests__/user.test.ts` returns 0 — no surviving assertion depends on the deleted five-step wipe.
    - `pnpm test --selectProjects Backend` exits 0 and its `Tests:` line reports zero failures. Record the exact `Test Suites:` and `Tests:` counts in the SUMMARY, and confirm `user.test.ts`, `humbleFlows.test.ts` and (if it survived Task 2) `seamBranchParity.test.ts` all appear in the collected run. `--selectProjects` is case-sensitive and exits 0 on an unrecognised project name, so the exit code alone is not evidence.
    - The SUMMARY states the pre-change and post-change `Tests:` totals and names every deleted test with its reason.
    - The D-05 ordering proof still asserts all three store clears and still asserts `disconnect()` resolves — quote the surviving assertions in the SUMMARY. This is a security invariant; a green suite that no longer checks it is the failure this criterion exists to prevent.
    - `pnpm codecheck` exits 0 and `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>Both logout test surfaces are re-pointed at the surviving path, the credential-cleanup security invariant still has a live assertion, and every deletion is named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logout code path -> user's credential state | `disconnect()` is a security operation. A collapse that accidentally removes the unconditional credential-side cleanup leaves a revoked session with `userInfo` still on disk. |
| deleted parity guard -> retired threat | `seamBranchParity.test.ts` and the comments it validates are the only places the F-6 `authCache`/`hostResolver` residual is written down. Deleting either silently retires a recorded, still-open threat. |
| test rewrite -> retained security assertion | Both re-pointed tests exist because a green suite had previously tested AROUND a real defect. |

No new dependency, no network surface, no user input, no auth flow change is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-14 | Elevation of Privilege | `disconnect()` credential-side cleanup | mitigate | The per-step try/catch and the unconditional `configStore.delete('userInfo')` + `clearCache` that follow the wipe loop are explicitly out of scope for the edit; the action names them as the security boundary. Task 3's acceptance requires the D-05 ordering proof to keep asserting all three store clears against an all-rejecting seam. |
| T-39-15 | Repudiation | the F-6 `authCache`/`hostResolver` residual and the T-34.5-20 shared-jar note | mitigate | Task 1 forbids deleting either paragraph and requires rewriting them as standing limitations rather than branch comparisons; acceptance greps for `authCache` and `T-34.5-20` surviving in source. Task 2's INVERT option keeps the `DECLARED` machinery pointed at exactly these categories. |
| T-39-16 | Tampering | Epic wipe-step ordering | mitigate | The `clearEpicStorage`-first / `clearEpicCookies`-last order was established by a live debug session (storage clearing re-seeds Epic's cookies). Acceptance asserts both step labels and the ORDER-IS-LOAD-BEARING comment survive. |
| T-39-17 | Tampering | `seamBranchParity.test.ts` disposition | mitigate | Weakening assertions to keep the suite green is forbidden by the action; a surviving file must pass a mutation control proving it can still go red, and a retired file must have its lost coverage enumerated. |
| T-39-18 | Tampering | collapsing sites this plan does not own | mitigate | `humble/user.ts` carries five other seam sites belonging to plans 39-05 and 39-06. Acceptance pins `session.fromPartition(HUMBLE_LOGIN_PARTITION)` at exactly 4 remaining occurrences, so an over-eager collapse is caught by a number rather than by review. |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `pnpm codecheck` exits 0.
- `pnpm lint` exits 0.
- `pnpm test --selectProjects Backend` exits 0 with recorded suite and test counts, and every file
  this plan touched confirmed present in the collected run.
- `grep -c "session.fromPartition(HUMBLE_LOGIN_PARTITION)" src/backend/humble/user.ts` returns 4.
- `git log --oneline -3` shows the source collapse, the parity-guard disposition, and the test
  re-point as separate commits.
</verification>

<success_criteria>
- Both `disconnect()` implementations build one unconditional wipeSteps array from the seam.
- `seamBranchParity.test.ts` carries an explicit, labelled disposition — inverted and still
  falsifiable, or retired with enumerated losses.
- The credential-cleanup security invariant and the two recorded residual threats all still have
  live assertions or live source records.
- No site belonging to plans 39-05 or 39-06 was touched.
</success_criteria>

<output>
Create `.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-04-SUMMARY.md` when done.
</output>
