---
phase: 39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec
plan: 08
type: execute
wave: 6
depends_on: [39-07]
files_modified:
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py
  - .planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-GATE-DISPOSITIONS.md
autonomous: true
requirements:
  - REQ-39-02

must_haves:
  truths:
    - "`python3 meta/runPlanningGates.py` prints `7/7 planning gates passed.` and exits 0"
    - "The seam-parity-sweep gate reaches a real verdict against the live tree — the `[PASS]` line is present and no Python traceback appears anywhere in the runner output"
    - "Its `ELECTRON_STUB_PATH` points at `src/backend/platform/index.ts`, the artifact's actual post-`git mv` location, and the safeStorage shape it parses is still there"
    - "Its Axis A machinery — `EXPECTED_AXIS_A_SITES`, `EXPECTED_SILENT_DROP_SITES`, `SITE_PROFILES`, and the report-drift check — is dispositioned deliberately against a tree where seven of its eight pinned sites no longer exist, using the D-35-14-02 four-label vocabulary"
    - "Whatever remains of the gate can still fail — proven by mutation, not asserted"
    - "The gate file stays on disk regardless of disposition: `runPlanningGates.py` hard-fails below `MINIMUM_EXPECTED_GATES = 7` discovered gates, so RETIRE-by-deletion is unavailable"
  artifacts:
    - path: ".planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py"
      provides: "the seam-parity gate, re-pointed and re-dispositioned for a single-shell tree"
      contains: "EXPECTED_AXIS_A_SITES"
    - path: ".planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-GATE-DISPOSITIONS.md"
      provides: "both gates' dispositions in one record, the seam-parity row filled in"
      contains: "RE-POINT"
  key_links:
    - from: ".planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py"
      to: "src/backend/platform/index.ts"
      via: "ELECTRON_STUB_PATH, consumed by parse_electron_stub_safestorage() and iter_source_files()"
      pattern: "\"platform\""
---

<objective>
Close the second planning gate and get `runPlanningGates.py` back to 7/7 — against the post-collapse
tree, which is the only tree where its disposition can be decided honestly.

Purpose: this gate has two independent problems. One is a stale path (an artifact moved by Phase 35).
The other is that its Axis A census pins eight `getLoginWindowSeam()` call sites, seven of which
this phase's own REQ-39-03 correctly deleted. Repairing only the path would produce a gate that
passes for five minutes and then fails on a census this phase invalidated on purpose.
Output: a gate that reaches a verdict, a disposition for its Axis A machinery, and 7/7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-RESEARCH.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-PATTERNS.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-SEAM-DISPOSITIONS.md
@.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-07-SUMMARY.md

<problem_1_the_stale_path>
Measured 2026-09-02 at `ed1fdf71d`, and re-confirmed at plan time: all 15 of the gate's own
self-tests PASS, then it dies at line 963 with
`FileNotFoundError: .../src/backend/sidecar/electronStub.ts`, thrown from
`parse_electron_stub_safestorage()`, called from `classify_axis_b()` at line 1013.

`ELECTRON_STUB_PATH` is declared once at line 73 and consumed twice: line 172
(`iter_source_files()`'s self-exclusion) and line 963. One re-point fixes both.

The artifact exists at `src/backend/platform/index.ts` — the same file, `git mv`'d by Phase 35 plans
35-13/35-15, with continuous history under `git log --follow`. The specific export the gate parses,
`safeStorage`, is at `index.ts:609-621` with exactly the hardcoded-false / throws shape
`parse_electron_stub_safestorage()`'s regex classifier (lines 973-984) is built to detect.

Disposition for this half: **RE-POINT**. Same shape as D-35-14-02's `packagingConfig.test.ts`
re-point (symlink plugin moved to `vite.config.ts`, gate re-pointed, no logic change).

Because the gate currently crashes rather than calling `fail()`, nothing downstream of
`parse_electron_stub_safestorage()` inside `classify_axis_b()` has run against the live tree since
the move. Re-pointing could surface a second, previously invisible failure. The task is not done
when the path is edited; it is done when the gate reaches a verdict.
</problem_1_the_stale_path>

<problem_2_the_axis_a_census_this_phase_invalidated>
Found at plan time, named in neither `39-RESEARCH.md` nor `39-PATTERNS.md`, and recorded in
`39-GATE-DISPOSITIONS.md` by plan 39-01 as the reason this gate was deferred.

The gate carries a module-level floor list:
```python
EXPECTED_AXIS_A_SITES = [
    "src/backend/humble/adapter.ts:275",
    "src/backend/humble/user.ts:178",
    "src/backend/humble/user.ts:274",
    "src/backend/humble/user.ts:740",
    "src/backend/humble/user.ts:1034",
    "src/backend/sidecar/oauthLoginCapture.ts:195",
    "src/backend/storeManagers/legendary/user.ts:137",
    "src/backend/sidecar/humbleLoginFlowRegistration.ts:457",
]
```
REQ-39-03 deleted seven of those eight. Only the last — the deliberately-kept smoke-test guard —
survives, and `39-SEAM-DISPOSITIONS.md` records why.

Three further pieces of the same machinery are affected, and all must be inspected before choosing
a disposition:
- `EXPECTED_SILENT_DROP_SITES` pins `humble/user.ts`, `storeManagers/legendary/user.ts` and
  `steamgrid/secureKey.ts` as files that silently drop capability (findings S-07, S-10, S-12 — the
  F-6 authCache/hostResolver drops and F-1's steamgrid leg). The first two were dual-branch
  comparisons; the third (`secureKey.ts`) is a safeStorage finding untouched by this phase and must
  keep working.
- `SITE_PROFILES` classifies specific sites by anchor strings found in their source. Plans 39-04
  through 39-06 rewrote the comment blocks those anchors live in, so `classify_site_profile()`'s
  anchor-count check may now fail loudly by design.
- `run_axis_a()` hard-`fail()`s on any `getLoginWindowSeam()` call site that matches none of its
  mechanical tiers and has no `SITE_PROFILES` entry. The one surviving site — the smoke guard —
  must either match a tier, carry a profile, or be explicitly excluded.
- The gate also performs a report-drift check against `34.4.1-SEAM-PARITY-SWEEP.md`. Read what that
  check does before editing anything; a re-derived findings list will not match a pinned report.

Axis B (the safeStorage parity axis, `EXPECTED_AXIS_B_SAFESTORAGE_IMPORTERS`) is a different
concern and is NOT invalidated by this phase — `secretStore.ts`, `steamgrid/secureKey.ts` and
`steam/tokenStore.ts` are untouched. Do not collapse Axis B along with Axis A.

Constraint: `meta/runPlanningGates.py` sets `MINIMUM_EXPECTED_GATES = 7` and hard-fails if fewer
gates are discovered by its `-gate.py` suffix scan. Deleting this file is therefore not an option
regardless of disposition — a retired Axis A must be retired IN PLACE.
</problem_2_the_axis_a_census_this_phase_invalidated>

<disposition_vocabulary>
From `.planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md:1164`
(D-35-14-02). Use these four labels; do not invent new ones.
- RE-POINT — the pinned artifact moved, the invariant is still true elsewhere.
- RE-DERIVE — a census's stated number and its list must be updated together, never just the number.
- INVERT — the gate asserted "X still exists" and X's deliberate removal is exactly what a later
  phase does. **This is the shape Axis A's floor list most likely is now.** D-35-14-02's own
  `artifactTargets.test.ts` precedent did exactly this, with the test's own comment recording who
  owned the removal: "Plan 35-14 owns these. If they vanish, someone must have planned doing it
  deliberately."
- RETIRE — the artifact the gate reads no longer exists at all.
</disposition_vocabulary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point ELECTRON_STUB_PATH and drive the gate to a verdict</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py</files>
  <read_first>
    - `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py` lines 30-90 (the header comment and the constants block, `ELECTRON_STUB_PATH` at line 73), lines 148-180 (`fail()` and `iter_source_files()`), lines 950-1030 (`parse_electron_stub_safestorage()` and `classify_axis_b()`), and `main()` at lines 1520-1600
    - `src/backend/platform/index.ts` lines 600-625 — the `safeStorage` export the gate parses
  </read_first>
  <action>
    Change the constant at line 73 from `SRC_DIR / "backend" / "sidecar" / "electronStub.ts"` to
    `SRC_DIR / "backend" / "platform" / "index.ts"`. Do not rename the constant — both consumers key
    off it and renaming widens the diff for no benefit. Add a one-line provenance comment above it:
    the file was moved by Phase 35 plans 35-13/35-15, and `git log --follow -- src/backend/platform/index.ts`
    carries the continuous history.

    Then run the gate and read its output. This is the whole task; the edit is the easy part. Three
    outcomes are possible:
    (a) it passes — proceed to Task 2 for the Axis A disposition anyway, since a pass may only mean
        Axis A's floor check runs before the sites it pins are enumerated;
    (b) it emits one or more `GATE FAILED: ...` lines — expected. Record every one verbatim before
        touching anything. Task 2 dispositions them;
    (c) a different traceback appears — treat the crash itself as a defect and diagnose it here; a
        gate that crashes has no verdict.

    Do not weaken, comment out, or skip any check in this task. Task 2 is where dispositions happen,
    and separating the two is what keeps "the path was stale" distinguishable from "the census was
    invalidated" in the commit history.
  </action>
  <verify>
    <automated>python3 ".planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'ELECTRON_STUB_PATH = ' .planning/phases/34.4.1-*/seam-parity-sweep-gate.py` shows a path ending in `"platform" / "index.ts"`, and `grep -c 'electronStub.ts' .planning/phases/34.4.1-*/seam-parity-sweep-gate.py` counts only comment/prose occurrences, none in that assignment.
    - The gate's output contains NO line matching `FileNotFoundError`.
    - The gate's self-test banner still reports `All 15 check(s) proved capable of rejecting the input they exist to reject.`
    - Every `GATE FAILED:` line the run produces is recorded verbatim in the SUMMARY, with the check that emitted it identified by function name. If there are none, that fact is recorded explicitly — "nothing was hiding behind the crash" is a finding the research flagged as a genuine open question.
    - No check was weakened, skipped or commented out in this task's commit — `git diff` for the commit touches only the constant and its new comment.
  </acceptance_criteria>
  <done>The path is re-pointed with recorded provenance, the FileNotFoundError is gone, and every remaining failure is captured verbatim before any disposition is applied.</done>
</task>

<task type="auto">
  <name>Task 2: Disposition the Axis A census against the post-collapse tree</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py</files>
  <read_first>
    - `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py` — `EXPECTED_AXIS_A_SITES` and its long provenance comment, `EXPECTED_SILENT_DROP_SITES`, `SITE_PROFILES` (from line 566), `find_axis_a_call_sites()`, `run_axis_a()` (from line 836) including its hard-`fail()` for unmatched sites, `classify_site_profile()`'s anchor-count check, and the report-drift check
    - `.planning/phases/39-.../39-SEAM-DISPOSITIONS.md` — the 13-site table and the deliberate exclusion
    - `.planning/phases/35-electron-cutover-remove-the-electron-build/deferred-items.md` from line 1164 — specifically the `artifactTargets.test.ts` INVERT precedent
    - Task 1's recorded `GATE FAILED:` output
  </read_first>
  <action>
    Disposition Axis A. Take each failure Task 1 recorded, decide its label from the four-label
    vocabulary, and apply it. INVERT is the expected answer for the floor list, but derive it from
    what the gate actually reported rather than assuming.

    Concretely, for `EXPECTED_AXIS_A_SITES`: the list asserted that seven dual-build discriminator
    sites still exist. Their removal is exactly what REQ-39-03 did on purpose. Invert it — assert
    that the ONLY surviving `getLoginWindowSeam()` call site in `src/` is
    `sidecar/humbleLoginFlowRegistration.ts`'s smoke-test guard, and that the other seven are gone.
    Follow the `artifactTargets.test.ts` precedent and record IN THE GATE ITSELF who owns the
    removal: "Phase 39 / REQ-39-03 owns these deletions. If a seam predicate reappears, someone must
    have planned doing it deliberately." Preserve the existing provenance comment above the list —
    its record of three prior line-number refreshes is exactly the drift history that justified
    re-deriving this phase's census on the predicate rather than on a file-and-line list.

    For `EXPECTED_SILENT_DROP_SITES`: findings S-07 and S-10 (the F-6 authCache/hostResolver drops in
    `humble/user.ts` and `legendary/user.ts`) were expressed as a Tauri branch dropping capability
    relative to an Electron branch. There is no longer a comparison. But the underlying residual is
    still real — those two categories are still not cleared — and plan 39-04 was required to keep
    saying so in source. Decide whether the gate can still assert that (re-express it as "these files
    still declare the uncleared categories") or whether it must be retired in place with the residual
    now carried only by `39-SEAM-DISPOSITIONS.md` and the source comments. Either is defensible;
    silently dropping it is not. S-12 (`steamgrid/secureKey.ts`) is untouched by this phase and must
    keep working.

    For `SITE_PROFILES` and `run_axis_a()`'s hard-`fail()` on unmatched sites: only one site survives.
    Ensure it is handled explicitly — a profile entry, a tier match, or a named exclusion — and that
    the hard-`fail()` still fires for a site that is genuinely new. That hard stop is the gate's own
    anti-silent-skip mechanism and must not become unreachable.

    Do NOT touch Axis B. `EXPECTED_AXIS_B_SAFESTORAGE_IMPORTERS` and the safeStorage classification
    are a separate concern that this phase did not change.

    If the report-drift check against `34.4.1-SEAM-PARITY-SWEEP.md` fails because the findings list
    changed, regenerate the report the way the gate expects and record that you did — do not disable
    the check. A report whose number outlives its list is the exact failure D-35-14-02's RE-DERIVE
    label exists for.

    Every check you keep must still be capable of failing. If inverting an assertion leaves it
    trivially true, retire it in place and say so, rather than keeping a green line that measures
    nothing.
  </action>
  <verify>
    <automated>python3 ".planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/seam-parity-sweep-gate.py"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .planning/phases/34.4.1-*/seam-parity-sweep-gate.py` exits 0 with no `GATE FAILED:` line and no traceback.
    - `grep -c 'humbleLoginFlowRegistration' .planning/phases/34.4.1-*/seam-parity-sweep-gate.py` returns at least 1 — the one surviving site is named in the gate, not merely absent from a deleted list.
    - `grep -c 'REQ-39-03' .planning/phases/34.4.1-*/seam-parity-sweep-gate.py` returns at least 1 — the gate records who owns the removal, per the `artifactTargets.test.ts` precedent.
    - `grep -c 'secureKey' .planning/phases/34.4.1-*/seam-parity-sweep-gate.py` returns at least 1 — S-12 and Axis B survived the Axis A disposition.
    - The self-test banner still reports a check count, and that count is recorded in the SUMMARY alongside the pre-change count of 15. If it dropped, the SUMMARY names each removed self-test and the assertion it covered.
    - Mutation controls, run and recorded verbatim in the SUMMARY then reverted, one per assertion this task kept or inverted: for the inverted floor list, reintroduce a `getLoginWindowSeam() === null` predicate in a source file and confirm the gate goes RED; for any surviving silent-drop assertion, delete the declaring comment from source and confirm the gate goes RED. An assertion with no demonstrated red is retired, not kept.
    - `git diff` for this task's commit touches only `seam-parity-sweep-gate.py` and, if the report-drift check required it, `34.4.1-SEAM-PARITY-SWEEP.md`. No `src/` file is modified.
  </acceptance_criteria>
  <done>Axis A is dispositioned with a label per failure, every surviving assertion is mutation-proven capable of failing, Axis B is untouched, and the gate exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Reach 7/7 and complete the gate disposition record</name>
  <files>.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-GATE-DISPOSITIONS.md</files>
  <read_first>
    - `.planning/phases/39-.../39-GATE-DISPOSITIONS.md` as plan 39-01 left it, including the placeholder row for this gate and the "why the seam-parity gate is deferred" section
    - `meta/runPlanningGates.py` — its `-gate.py` suffix discovery and `MINIMUM_EXPECTED_GATES = 7`
    - Tasks 1 and 2's recorded output
  </read_first>
  <action>
    Run the full runner and confirm 7/7. A per-gate pass does not prove the runner is green — the
    runner discovers gates by filename suffix and has its own minimum-count check, and a third gate
    could have been collaterally broken by this phase's source deletions.

    Then fill in `39-GATE-DISPOSITIONS.md`'s placeholder row and add two sections:
    - The seam-parity gate's row, in the same `| Gate | Pinned invariant | Disposition |` shape, with
      TWO labels if the repair needed two (RE-POINT for the moved artifact, plus whatever Task 2
      chose for Axis A) and the evidence for each.
    - A section recording that this gate's Axis A census was invalidated by this phase's own
      REQ-39-03, that this is why the gate was deferred from plan 39-01 to here, and what the
      inverted or retired assertions now say. Name the one surviving pinned site.
    - A section recording the `MINIMUM_EXPECTED_GATES = 7` constraint and that it is why RETIRE-by-
      deletion was unavailable, so a future phase facing a genuinely dead gate knows it must retire
      in place or lower the floor deliberately.

    Update the "why the seam-parity gate is deferred" section plan 39-01 wrote so it reads as
    resolved rather than pending — but do not delete it. The reason the deferral happened is part of
    the record.

    No lint figures in this document. Plan 39-09 owns those.
  </action>
  <verify>
    <automated>python3 meta/runPlanningGates.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 meta/runPlanningGates.py` prints a final line exactly matching `7/7 planning gates passed.` and exits 0.
    - Its output contains zero lines matching `^\[FAIL\]` and zero lines matching `Traceback (most recent call last)`.
    - `.planning/phases/39-*/39-GATE-DISPOSITIONS.md` contains rows for both gates with no `pending` placeholder remaining, and `grep -c 'RE-POINT'` and `grep -c 'RE-DERIVE'` each return at least 1.
    - The document contains the literal strings `EXPECTED_AXIS_A_SITES`, `MINIMUM_EXPECTED_GATES`, and `humbleLoginFlowRegistration`.
    - `grep -ciE 'max-warnings|4190' .planning/phases/39-*/39-GATE-DISPOSITIONS.md` returns 0.
    - `pnpm test --selectProjects Backend` still exits 0 — this plan touched no `src/` file, and confirming that is how a collateral break gets caught rather than assumed absent.
  </acceptance_criteria>
  <done>The runner reports 7/7, both gates carry a labelled disposition with evidence, and the deferral is recorded as resolved rather than erased.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict -> CI -> human record | A green gate is read as proof an invariant holds. A gate whose assertions were weakened to accommodate a deletion is a green light that measures nothing. |
| this phase's own deletions -> this phase's own gate | The unusual risk here: the same phase that deletes the pinned sites also decides whether the pin was legitimate. That is a conflict of interest, and the mitigation is that the deletion's justification lives in a separate document written before the gate was touched. |

No new dependency, no network surface, no user input, no auth change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-34 | Tampering | `EXPECTED_AXIS_A_SITES` | mitigate | The floor is INVERTED, not deleted: the gate keeps asserting something falsifiable (only the smoke guard survives) and records that REQ-39-03 owns the deletions. Acceptance requires a mutation control — reintroduce a predicate, the gate must go RED. Simply emptying the list would pass and assert nothing. |
| T-39-35 | Repudiation | the F-6 authCache/hostResolver residual (S-07, S-10) | mitigate | This residual is a real, still-open limitation that predates the phase. It may only be retired from the gate if the SUMMARY records that it is thereafter carried by `39-SEAM-DISPOSITIONS.md` and the source comments plan 39-04 was required to preserve. A silent drop would retire a threat by accident. |
| T-39-36 | Repudiation | the gate's own self-test count | mitigate | The gate proves its 15 checks can each reject bad input. A disposition that removes checks also removes self-tests. Acceptance requires the before and after counts, and each removed self-test named with the assertion it covered. |
| T-39-37 | Tampering | `run_axis_a()`'s hard-fail on unmatched sites | mitigate | That hard stop is the gate's own anti-silent-skip mechanism, added because a silently-skipped site is how a third F-1/F-6-shaped defect would slip through. The action requires it to remain reachable for a genuinely new site. |
| T-39-38 | Tampering | Axis B collateral | mitigate | Axis B (safeStorage importers, `secureKey.ts`'s S-12) is untouched by this phase. The action forbids collapsing it alongside Axis A, and acceptance greps for `secureKey` surviving. |
| T-39-39 | Tampering | the report-drift check | mitigate | Disabling it is forbidden; the report must be regenerated the way the gate expects and the regeneration recorded. A census whose number outlives its list is the exact failure the RE-DERIVE label exists for. |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `python3 .planning/phases/34.4.1-*/seam-parity-sweep-gate.py` exits 0 when invoked directly.
- `python3 meta/runPlanningGates.py` prints `7/7 planning gates passed.` and exits 0.
- Every surviving or inverted assertion has a demonstrated mutation-RED recorded in the SUMMARY.
- The self-test check count is recorded before and after.
- `pnpm test --selectProjects Backend` still exits 0; no `src/` file was modified by this plan.
</verification>

<success_criteria>
- `runPlanningGates.py` is 7/7 for the first time since the Electron cutover.
- The seam-parity gate reaches a verdict, with its stale path re-pointed and its invalidated census
  inverted or retired in place — never deleted, never silently emptied.
- Axis B and the S-12 steamgrid finding survive untouched.
- Both gates' dispositions, and the reason the second was deferred, are on the record.
</success_criteria>

<output>
Create `.planning/phases/39-repo-wide-lint-debt-drive-pnpm-lint-to-exit-0-after-the-elec/39-08-SUMMARY.md` when done.
</output>
