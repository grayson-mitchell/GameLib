---
phase: 20-aggregated-store-search-cheapshark
plan: 06
type: execute
wave: 4
depends_on: ["20-03", "20-04", "20-05"]
files_modified:
  - src/frontend/screens/StoreSearch/index.tsx
  - src/frontend/screens/StoreSearch/hooks/useDebouncedStoreSearch.ts
  - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx
  - src/frontend/App.tsx
  - public/locales/en/translation.json
  - src/frontend/screens/StoreSearch/__tests__/useDebouncedStoreSearch.test.ts
  - src/frontend/screens/StoreSearch/__tests__/StoreSearchScreen.test.tsx
autonomous: true
requirements: [STORESEARCH-01, STORESEARCH-02, STORESEARCH-05, STORESEARCH-06, STORESEARCH-08]
must_haves:
  truths:
    - "A new top-level sidebar entry (sibling of Deals, always visible) navigates to /store-search (STORESEARCH-01)"
    - "Search is debounced ~400ms, requires min 3 chars, and a superseding query cancels the stale one via a generation counter — one request per pause (STORESEARCH-02, D-11)"
    - "The container resolves ownership badges once via resolveStoreSearchBadges from GlobalState reading exactly the five sources steam/gog/epic/amazon libraries + Humble keys, with sideloadedLibrary explicitly excluded, passing resolved literals to each row (STORESEARCH-05/06, D-05)"
    - "Three visually distinct states exist: explanatory prompt, no-results, and inline retryable provider-failed — the search box stays usable on failure (STORESEARCH-08, D-14 fail-soft)"
  artifacts:
    - path: "src/frontend/screens/StoreSearch/index.tsx"
      provides: "Container: debounce wiring, badge resolution, three-state rendering, row list"
    - path: "src/frontend/screens/StoreSearch/hooks/useDebouncedStoreSearch.ts"
      provides: "Debounce + min-3-char + generation-counter cancel hook"
    - path: "public/locales/en/translation.json"
      provides: "All storeSearch.* i18n keys"
  key_links:
    - from: "src/frontend/screens/StoreSearch/index.tsx"
      to: "src/common/discounts/badges.ts resolveStoreSearchBadges"
      via: "useMemo over results reading GlobalState libraries"
      pattern: "resolveStoreSearchBadges"
    - from: "src/frontend/App.tsx"
      to: "src/frontend/screens/StoreSearch"
      via: "lazy route registration for /store-search"
      pattern: "screens/StoreSearch"
    - from: "src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx"
      to: "/store-search"
      via: "new SidebarItem sibling of /discounts"
      pattern: "store-search"
---

<objective>
Assemble the store-search screen container: the debounce hook (400ms, min 3 chars, generation-counter cancel), badge resolution computed once from `GlobalState`, the three-state fail-soft rendering (prompt / no-results / provider-failed), plus the sidebar entry, the `/store-search` route, and the full `storeSearch.*` i18n key block.

Purpose: The debounce respects a free keyless API — one request per pause, never per keystroke (D-11). The three-state contract distinguishes "no results" from "provider failed" so a user never retries a search that was never going to work, or abandons one that would have (D-14). The badge-once-in-container pattern is what keeps ownership logic unit-testable (RESEARCH Pattern 2).
Output: `index.tsx`, `useDebouncedStoreSearch.ts`, sidebar + route edits, `translation.json` keys, and two tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-CONTEXT.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-RESEARCH.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-PATTERNS.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-UI-SPEC.md

<interfaces>
<!-- From Plan 03: resolveStoreSearchBadges(result:{title,steamAppId?}, libraries:{steam,gog,epic,amazon}, keysWaiting) => { owned, keyAvailable } -->
<!-- From Plan 04: window.api.searchStores(query) => Promise<StoreSearchResult[]> -->
<!-- From Plan 05: StoreSearchRow (takes result + resolved { owned, keyAvailable } prop) -->
<!-- GlobalState/ContextProvider slices (GlobalState.tsx): epic.library, gog.library, amazon.library, steam.library (items have app_name), humble.keys — EXCLUDE sideloadedLibrary + zoom (D-05) -->
<!-- Analog: Discounts/index.tsx — badge-once useMemo (lines ~495-512), load+3-state try/catch (lines ~203-247, ~620-632), SearchBar reuse (DiscountFilters:285-289) -->
<!-- Analog: SidebarLinks/index.tsx:199-203 (/discounts SidebarItem, faTags) ; App.tsx:159-162 (discounts lazy route) -->
<!-- Debounce: NO codebase precedent — RESEARCH Pattern 3 (setTimeout 400 + min-3 + generation counter). AbortController does NOT cancel across the IPC hop — use a requestId/generation ref to ignore stale resolutions (RESEARCH Pitfall 5). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useDebouncedStoreSearch hook (debounce + min-3 + generation-counter cancel) + unit test</name>
  <files>src/frontend/screens/StoreSearch/hooks/useDebouncedStoreSearch.ts, src/frontend/screens/StoreSearch/__tests__/useDebouncedStoreSearch.test.ts</files>
  <read_first>
    - .planning/phases/20-aggregated-store-search-cheapshark/20-RESEARCH.md (Pattern 3 — the exact setTimeout+min-3 sketch; Pitfall 5 — generation-counter guard; why AbortController does not cross the IPC hop)
    - .planning/phases/20-aggregated-store-search-cheapshark/20-PATTERNS.md (section "useDebouncedStoreSearch.ts" + "No Analog Found")
    - src/backend/humble/library.ts (the currentSyncGeneration() supersede-detection idiom that inspires the generation counter — do not copy, adapt)
    - .planning/phases/20-aggregated-store-search-cheapshark/20-UI-SPEC.md (Search input — spinner-in-searchButton-slot while pending, no spinner under 3 chars, cancellation is invisible)
  </read_first>
  <behavior>
    - Rapid setQuery('p'),('po'),('por'),('port') within one 400ms window fires window.api.searchStores exactly once, with the final value ('port')
    - A query trimmed to <3 chars fires no request and exposes an "initial/prompt" status (not loading, not error, no results)
    - When an earlier request resolves AFTER a newer query has been fired, the stale results are ignored (generation guard) — only the latest query's results are exposed
    - A rejected searchStores exposes an 'error' status while leaving the query editable
    - Exposes a `loading` flag true while a request is in-flight OR the debounce timer is pending (for the SearchBar spinner)
  </behavior>
  <action>
    Create `useDebouncedStoreSearch.ts` exposing `{ query, setQuery, results, status, loading, retry }` where `status` is one of `'prompt' | 'loading' | 'results' | 'empty' | 'error'`. Debounce: a `useEffect` on `query` that trims, gates on `length >= 3` (else set status 'prompt' and clear results), and `setTimeout(..., 400)` before committing a `debouncedQuery`; clear the timer on cleanup. Fetch: a `useEffect` on `debouncedQuery` that increments a `generationRef` (useRef number), captures the current generation in a closure, sets loading, calls `window.api.searchStores(debouncedQuery)`, and on resolve/reject IGNORES the outcome if `generationRef.current !== capturedGeneration` (RESEARCH Pitfall 5 — AbortController cannot cancel the IPC-bridged promise, the generation guard is what prevents a stale render). On success set results + status 'results'/'empty'; on failure `window.api.logError(String(err))` + status 'error' (do not surface the raw error). `retry()` re-fires the current debouncedQuery. Keep `loading` true while the debounce timer is pending OR a request is in-flight so the SearchBar can swap its search icon for a spinner. Create `useDebouncedStoreSearch.test.ts` using `jest.useFakeTimers()` + a mocked `window.api.searchStores`, asserting the five behaviors above (including the out-of-order stale-resolution guard).
  </action>
  <verify>
    <automated>npx jest src/frontend/screens/StoreSearch/__tests__/useDebouncedStoreSearch.test.ts --selectProjects frontend</automated>
  </verify>
  <acceptance_criteria>
    - rapid sequential setQuery within 400ms results in exactly one window.api.searchStores call (fake-timers test asserts)
    - query <3 chars fires no request and yields status 'prompt'
    - a stale (superseded) resolution is ignored via the generation ref (test simulates out-of-order resolve)
    - a rejected request yields status 'error' and the raw error goes to window.api.logError, not the returned state
    - the hook uses a generation/requestId ref, not AbortController, for cross-IPC supersede (grep: no AbortController in the hook)
    - useDebouncedStoreSearch.test.ts green
  </acceptance_criteria>
  <done>The debounce/min-3/generation-cancel hook exists and is unit-tested including the out-of-order race.</done>
</task>

<task type="auto">
  <name>Task 2: StoreSearch container (badge-once + three-state + SearchBar) + sidebar entry + route + i18n</name>
  <files>src/frontend/screens/StoreSearch/index.tsx, src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx, src/frontend/App.tsx, public/locales/en/translation.json, src/frontend/screens/StoreSearch/__tests__/StoreSearchScreen.test.tsx</files>
  <read_first>
    - src/frontend/screens/Discounts/index.tsx (badge-once useMemo ~495-512; load+3-state try/catch ~203-247 and ~620-632; useContext(ContextProvider) slice destructuring; SearchBar wiring)
    - src/frontend/state/GlobalState.tsx (the epic/gog/amazon/steam/humble slice shapes — confirm humble.keys and steam.library item app_name field; confirm sideloadedLibrary + zoom exist so you can consciously EXCLUDE them, D-05)
    - src/frontend/components/UI/Sidebar/components/SidebarLinks/index.tsx (lines 199-203 /discounts SidebarItem — add sibling directly after; the free-solid import block near line 13 — add faMagnifyingGlassDollar to it, no second import line; note there is NO conditional gate for Deals — this entry is also ungated)
    - src/frontend/App.tsx (lines 159-162 discounts lazy route — add /store-search sibling in the same children array via makeLazyFunc(import('./screens/StoreSearch')))
    - .planning/phases/20-aggregated-store-search-cheapshark/20-UI-SPEC.md (Three-State Contract table — the distinct visual containers for prompt vs no-results vs provider-failed; Copywriting Contract — every t() key + default string; Sidebar entry + Screen shell specs)
    - src/frontend/components/UI/SearchBar/index.tsx (uncontrolled input, onInputChanged fires per keystroke — container debounces; swap searchButton icon for spinner while pending)
    - public/locales/en/translation.json (where to add the storeSearch.* block — mirror the discounts.* keys location)
  </read_first>
  <action>
    Create `StoreSearch/index.tsx` (container, default export for the lazy route). Wire `useDebouncedStoreSearch` (Task 1). Reuse `SearchBar` verbatim with placeholder `t('storeSearch.placeholder','Search for a game title…')`, swapping its `searchButton`-slot icon for an inline spinner while `loading` (same DOM slot, not a second element). Destructure `const { epic, gog, amazon, steam, humble } = useContext(ContextProvider)` — DO NOT read sideloadedLibrary or zoom (D-05 exclusion). Compute a `Map<gameId, { owned, keyAvailable }>` via `useMemo` calling `resolveStoreSearchBadges(result, { steam: steam.library, gog: gog.library, epic: epic.library, amazon: amazon.library }, humble.keys)` once per results array (RESEARCH Pattern 2 — resolved once, passed to each `StoreSearchRow` as a literal; the row never recomputes). Render exactly three DISTINCT visual states below the SearchBar per the UI-SPEC Three-State Contract — they MUST NOT share one `<p className="__message">`: (a) status 'prompt' (or <3 chars) → centered explanatory block with the 48px muted faMagnifyingGlassDollar icon + `t('storeSearch.prompt', ...)`; (b) status 'empty' → centered block WITHOUT icon + `t('storeSearch.noResults','No results for "{{query}}"...',{query})`; (c) status 'error' → an inline banner (left-accent danger bar, positioned under the still-enabled SearchBar) with `t('storeSearch.error.provider',...)` + an inline `t('storeSearch.error.retry','Retry search')` button wired to the hook's `retry()` — the SearchBar stays focusable (fail-soft, D-14). Render `<h2>` `t('storeSearch.title','Store Search')`, no region selector. On 'results' render the `StoreSearchRow` list passing each row its resolved badge literal.
    Add the sidebar `<SidebarItem url="/store-search" icon={faMagnifyingGlassDollar} label={t('storeSearch.sidebar','Store Search')} dataTour="sidebar-store-search" />` as a sibling immediately after the `/discounts` entry (no conditional gate), importing `faMagnifyingGlassDollar` into the existing free-solid import statement. Register the route `{ path: 'store-search', lazy: makeLazyFunc(import('./screens/StoreSearch')) }` beside the discounts route in App.tsx. Add the full `storeSearch.*` key block to `public/locales/en/translation.json` (sidebar, title, placeholder, prompt, noResults, error.provider, error.retry, badge.ownedOn, badge.ownedOnMulti, badge.ownedOnOverflow, badge.keyAvailable, loadingStores, aria.expand, aria.viewDeal) with the exact default strings from the UI-SPEC Copywriting Contract.
    Create `StoreSearchScreen.test.tsx` (React Testing Library, mocking window.api.searchStores + ContextProvider) asserting: the initial prompt state renders (with the search-icon block), a successful empty result renders the no-results block (no icon), and a rejected search renders the error banner with a working Retry button while the SearchBar input remains present/enabled — and that the error banner and no-results block are structurally different containers.
  </action>
  <verify>
    <automated>npx jest src/frontend/screens/StoreSearch --selectProjects frontend && npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - a /store-search SidebarItem exists as a sibling of /discounts with faMagnifyingGlassDollar and no conditional gate
    - App.tsx registers the /store-search lazy route
    - container reads steam/gog/epic/amazon/humble from ContextProvider and does NOT reference sideloadedLibrary or zoom (grep returns 0 in index.tsx)
    - resolveStoreSearchBadges is called inside a useMemo once per results array; StoreSearchRow receives a resolved prop (grep confirms resolveStoreSearchBadges in index.tsx, not in StoreSearchRow)
    - the prompt, no-results, and error states are three distinct containers (error is a left-accent banner, not the shared __message paragraph) — test asserts distinct DOM
    - the SearchBar remains enabled in the error state (fail-soft) — test asserts input present after a rejected search
    - all storeSearch.* keys added to en/translation.json; `node -e "JSON.parse(require('fs').readFileSync('public/locales/en/translation.json'))"` succeeds
    - StoreSearchScreen.test.tsx green; npm run codecheck exits 0
  </acceptance_criteria>
  <done>The full screen is navigable, debounced, badge-resolved from GlobalState, and renders three distinct fail-soft states; i18n keys registered; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → debounced IPC call | user-typed text becomes a search request |
| GlobalState libraries → badge resolution | renderer-side ownership data drives the "you own this" verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Denial (CheapShark reputation) | debounced search | mitigate | D-11 debounce (400ms) + min-3-char gate + generation-counter supersede is the load-bearing rate control — one request per pause. This must not be weakened; the hook test asserts exactly-one-call-per-window. |
| T-20-03 | Spoofing (false badge) | resolveStoreSearchBadges call site | mitigate | Container passes only steam/gog/epic/amazon + humble.keys (D-05) — sideloadedLibrary (highest fuzzy false-positive risk) is deliberately excluded; the exclusion is grep-asserted. |
| T-20-02 | Injection | rendering query/results | mitigate | Query and result strings render as React text children (auto-escaped); the error state shows only a translated generic message, never the raw error/stack (raw goes to window.api.logError). |
| T-20-SC | Tampering | npm installs | accept | Zero new packages this plan. |
</threat_model>

<verification>
- `npx jest src/frontend/screens/StoreSearch --selectProjects frontend` green
- `npm run codecheck` exits 0
- Sidebar + route present; debounce one-call-per-window; three distinct states; sideloaded excluded
</verification>

<success_criteria>
The store-search screen is reachable from a new top-level sidebar entry, debounces to one request per pause, resolves store-attributed ownership badges once from GlobalState (excluding sideloaded), and renders three visually distinct fail-soft states — with i18n keys registered and the behavior unit-tested.
</success_criteria>

<output>
Create `.planning/phases/20-aggregated-store-search-cheapshark/20-06-SUMMARY.md` when done.
</output>
