---
phase: 20-aggregated-store-search-cheapshark
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/types/storeSearch.ts
  - src/common/discounts/storeMapping.ts
  - src/common/types/ipc.ts
  - src/backend/__tests__/storeMapping.test.ts
autonomous: true
requirements: [STORESEARCH-01, STORESEARCH-03, STORESEARCH-04]
must_haves:
  truths:
    - "A provider-neutral type vocabulary exists for store-search results and per-store deals, so a second provider (ITAD) can be added later without reshaping the consumer (D-11/D-12)"
    - "Every price-bearing type carries an explicit currencyCode field, making the CheapShark-is-USD-only debt visible in the type system rather than implicit (D-13)"
    - "The CheapShark numeric storeID -> GameLib Runner mapping is a single tested constant"
    - "The three new IPC channels are declared in AsyncIPCFunctions so preload invokers type-check"
  artifacts:
    - path: "src/common/types/storeSearch.ts"
      provides: "StoreSearchResult, StoreSearchDeal, StoreSearchStore types with currencyCode"
      exports: ["StoreSearchResult", "StoreSearchDeal", "StoreSearchStore"]
    - path: "src/common/discounts/storeMapping.ts"
      provides: "CheapShark storeID -> Runner constant + resolver"
    - path: "src/common/types/ipc.ts"
      provides: "searchStores/getStoreSearchDeals/getStoreSearchStoreMap channel signatures"
  key_links:
    - from: "src/common/types/ipc.ts"
      to: "src/common/types/storeSearch.ts"
      via: "channel return-type references"
      pattern: "StoreSearchResult|StoreSearchDeal|StoreSearchStore"
---

<objective>
Define the provider-neutral type vocabulary and IPC contract for the store-search feature: result/deal/store types (with an explicit per-price `currencyCode`), the CheapShark `storeID`→Runner mapping constant, and the three `AsyncIPCFunctions` channel signatures.

Purpose: These are the contracts every downstream plan (backend adapter, preload bridge, frontend screen) builds against. Defining them first removes the scavenger hunt — executors receive the exact shapes. The explicit `currencyCode` field is the type-system enforcement of D-13 (CheapShark USD-only must be visible, never implicit).
Output: `common/types/storeSearch.ts` (new), `common/discounts/storeMapping.ts` (new) + test, and the three new channels in `common/types/ipc.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-CONTEXT.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-RESEARCH.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-PATTERNS.md

<interfaces>
<!-- Live-verified CheapShark shapes (RESEARCH Code Examples) that the adapter maps FROM.
     These are NOT the GameLib types — the adapter (Plan 04) converts these into the GameLib types below. -->
// GET /games?title= -> { gameID, steamAppID?, cheapest (decimal string), cheapestDealID (already %-encoded), external (title), thumb (CDN url) }
// GET /games?id=    -> { info:{title,steamAppID?,thumb}, deals:[{ storeID, dealID (already %-encoded), price, retailPrice, savings }] }
// GET /stores       -> [{ storeID, storeName, isActive:0|1, images:{logo,icon,banner} }]
// Confirmed storeIDs: '1'=Steam=Runner steam, '7'=GOG=Runner gog, '25'=Epic=Runner legendary, '4'=Amazon=Runner nile (inactive on CheapShark)

<!-- Existing analog: src/common/types/discounts.ts CatalogLocaleSettings { countryCode; locale; currencyCode } -->
<!-- Existing analog: src/common/types/ipc.ts AsyncIPCFunctions.getGogDiscounts:
     getGogDiscounts: (locale, hideOwned?, wishlistOnly?) => Promise<CatalogProduct[]> (near line 486) -->
<!-- Runner union lives in common/types (imported widely); reuse it, do not redefine store names as free strings. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: storeSearch.ts types + storeMapping.ts constant + mapping unit test</name>
  <files>src/common/types/storeSearch.ts, src/common/discounts/storeMapping.ts, src/backend/__tests__/storeMapping.test.ts</files>
  <read_first>
    - src/common/types/discounts.ts (full file — the CatalogProduct/CatalogPrice/CatalogLocaleSettings structural template to mirror)
    - .planning/phases/20-aggregated-store-search-cheapshark/20-RESEARCH.md (Code Examples section — the live-verified CheapShark response shapes; Open Question 1 — where the storeID->Runner mapping lives)
    - .planning/phases/20-aggregated-store-search-cheapshark/20-PATTERNS.md (section "src/common/types/storeSearch.ts")
    - src/common/types/index.ts or wherever the `Runner` union is exported (grep "type Runner" src/common/types) — import Runner, do not restring store names
  </read_first>
  <action>
    Create `src/common/types/storeSearch.ts` mirroring `discounts.ts`'s structure with three exported interfaces:
    (1) `StoreSearchResult` — `{ gameId: string; title: string; steamAppId?: string; thumb: string; cheapestPrice: string; currencyCode: string; buyUrl: string }` where `cheapestPrice` is the decimal string and `buyUrl` is the pre-built verbatim redirect URL for the cheapest deal (built backend-side in Plan 04 so the dealID-verbatim rule + its regression test stay in the adapter).
    (2) `StoreSearchDeal` — one per-store row: `{ storeId: string; storeName: string; runner?: Runner; price: string; retailPrice: string; currencyCode: string; buyUrl: string }`.
    (3) `StoreSearchStore` — `{ storeId: string; storeName: string; runner?: Runner; isActive: boolean }`.
    Every price-bearing type MUST carry `currencyCode: string` (D-13/STORESEARCH-04) — it is `'USD'` in practice but present in the type so the USD-only debt is explicit and downstream ITAD migration flips one field. Add a doc comment on `currencyCode` citing D-13. Do NOT leak a literal `'USD'` union type — keep it `string` so ITAD needs no type change.
    Create `src/common/discounts/storeMapping.ts` exporting `CHEAPSHARK_STORE_TO_RUNNER: Record<string, Runner>` = `{ '1': 'steam', '7': 'gog', '25': 'legendary', '4': 'nile' }` and a `resolveRunner(storeId: string): Runner | undefined` helper. Import `Runner` from the common types module (do not restring). Add a header comment: Amazon (`'4'`) is mapped for forward-compat but is `isActive:0` on CheapShark today (RESEARCH Pitfall 2) — it will simply never match at runtime, which does not affect the "Owned on Amazon" badge (that reads GameLib's own amazon.library).
    Create `src/backend/__tests__/storeMapping.test.ts` asserting each of the four mappings by value so the constant cannot silently drift (RESEARCH Open Question 1 explicitly wants this).
  </action>
  <verify>
    <automated>npx jest src/backend/__tests__/storeMapping.test.ts --selectProjects backend && npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/common/types/storeSearch.ts exports StoreSearchResult, StoreSearchDeal, StoreSearchStore
    - every price-bearing interface has a `currencyCode: string` field (grep confirms currencyCode appears on StoreSearchResult and StoreSearchDeal)
    - `grep -c "'USD'" src/common/types/storeSearch.ts` returns 0 (no literal USD union baked into shared types — stays `string`)
    - CHEAPSHARK_STORE_TO_RUNNER maps '1'->steam, '7'->gog, '25'->legendary, '4'->nile
    - storeMapping.test.ts asserts all four mappings and passes green
    - npm run codecheck exits 0
  </acceptance_criteria>
  <done>Provider-neutral types + tested store mapping exist; currency debt is visible in the type system.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the three store-search IPC channels in AsyncIPCFunctions</name>
  <files>src/common/types/ipc.ts</files>
  <read_first>
    - src/common/types/ipc.ts (the AsyncIPCFunctions block near line 486 — the getGogDiscounts entry to mirror; and the import block at top to add the storeSearch type import)
    - src/common/types/storeSearch.ts (created in Task 1 — the return types to reference)
  </read_first>
  <action>
    In `src/common/types/ipc.ts`, add an import for `StoreSearchResult`, `StoreSearchDeal`, `StoreSearchStore` from `common/types/storeSearch`. Inside the `AsyncIPCFunctions` interface, beside the existing `getGogDiscounts` entry, add three channel signatures: `searchStores: (query: string) => Promise<StoreSearchResult[]>`, `getStoreSearchDeals: (gameId: string) => Promise<StoreSearchDeal[]>`, `getStoreSearchStoreMap: () => Promise<Record<string, StoreSearchStore>>`. These are the keys `makeHandlerInvoker<keyof AsyncIPCFunctions>` (Plan 04 preload bridge) and `addHandler` (Plan 04 backend) both key off — they must exist here or neither side type-checks. Do NOT add the buy-handoff as a new channel: the external-URL open reuses the existing `openExternalUrl` SyncIPC listener (D-08, RESEARCH — no new channel for the handoff).
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - AsyncIPCFunctions contains searchStores, getStoreSearchDeals, getStoreSearchStoreMap with the exact signatures above
    - the three channel return types reference StoreSearchResult/StoreSearchDeal/StoreSearchStore
    - no new channel named for the buy handoff (grep for 'openStoreSearchUrl' or similar returns 0 — openExternalUrl is reused)
    - npm run codecheck exits 0
  </acceptance_criteria>
  <done>The three store-search channels are declared; the IPC contract type-checks end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CheapShark JSON → GameLib types | Untrusted third-party JSON is shaped into these types by the Plan 04 adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Tampering / Injection | Untrusted CheapShark fields flowing into typed payloads | mitigate | Types are the contract but not the guard — the adapter (Plan 04) validates/coerces; here, keep `buyUrl` a plain string built by GameLib (never a raw URL field trusted from CheapShark) so Plan 04 can enforce the fixed host prefix. currencyCode stays a GameLib-applied constant, never read from CheapShark. |
| T-20-SC | Tampering | npm installs | accept | Zero new packages this plan. No install task, no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx jest src/backend/__tests__/storeMapping.test.ts --selectProjects backend` green
- `npm run codecheck` exits 0
- currencyCode present on all price types; no literal 'USD' union in shared types
</verification>

<success_criteria>
The provider-neutral type vocabulary, the tested storeID→Runner mapping, and the three IPC channel signatures all exist and type-check, ready for the backend adapter (Plan 04) and frontend (Plans 05/06).
</success_criteria>

<output>
Create `.planning/phases/20-aggregated-store-search-cheapshark/20-02-SUMMARY.md` when done.
</output>
