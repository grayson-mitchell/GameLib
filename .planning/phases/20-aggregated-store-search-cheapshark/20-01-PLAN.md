---
phase: 20-aggregated-store-search-cheapshark
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/matching/titleMatch.ts
  - src/backend/humble/dedup.ts
  - src/backend/humble/constants.ts
  - src/backend/__tests__/titleMatch.test.ts
autonomous: true
requirements: [STORESEARCH-05, STORESEARCH-06]
must_haves:
  truths:
    - "The fuzzy title matcher (normalizeTitle/titleSimilarity/isDlcFalsePositiveRisk/fuzzyMatch) lives in one shared common/ module with no Humble/Steam type dependency"
    - "backend/humble/dedup.ts consumes the shared matcher instead of defining the primitives locally; recomputeOwnership() still works and existing dedup tests stay green"
    - "HUMBLE_FUZZY_MATCH_THRESHOLD remains a single 0.85 constant (D-02) with one source of truth"
  artifacts:
    - path: "src/common/matching/titleMatch.ts"
      provides: "Store-agnostic pure fuzzy title matcher lifted from dedup.ts"
      exports: ["normalizeTitle", "titleSimilarity", "isDlcFalsePositiveRisk", "fuzzyMatch", "HUMBLE_FUZZY_MATCH_THRESHOLD"]
    - path: "src/backend/__tests__/titleMatch.test.ts"
      provides: "Unit coverage for the lifted matcher"
  key_links:
    - from: "src/backend/humble/dedup.ts"
      to: "src/common/matching/titleMatch.ts"
      via: "import of normalizeTitle/titleSimilarity/isDlcFalsePositiveRisk/fuzzyMatch"
      pattern: "from 'common/matching/titleMatch'"
    - from: "src/backend/humble/constants.ts"
      to: "src/common/matching/titleMatch.ts"
      via: "re-export of HUMBLE_FUZZY_MATCH_THRESHOLD"
      pattern: "HUMBLE_FUZZY_MATCH_THRESHOLD"
---

<objective>
Lift the store-agnostic fuzzy title-matching primitives out of `backend/humble/dedup.ts` into a new pure `common/matching/titleMatch.ts` module so Phase 20's store-search badge resolver can reuse them without writing a second matcher (CONTEXT D-02, RESEARCH Pitfall 4).

Purpose: A single shared matcher means the 85% threshold and DLC false-positive guard behave identically on the Humble dedup surface and the new store-search surface. Two matchers would drift; a second threshold would be invented precision (D-02).
Output: `common/matching/titleMatch.ts` (new), a refactored `dedup.ts` that imports from it, a re-exporting `constants.ts`, and a unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-CONTEXT.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-RESEARCH.md
@.planning/phases/20-aggregated-store-search-cheapshark/20-PATTERNS.md

<interfaces>
<!-- Extracted from src/backend/humble/dedup.ts — these five identifiers move to common/matching/titleMatch.ts verbatim. -->
normalizeTitle(title: string): string          // lowercase, strip ™®©, strip (parentheticals), strip EDITION_SUFFIXES, collapse whitespace
titleSimilarity(a: string, b: string): number  // 1 - distance/maxLen over normalized titles (fastest-levenshtein)
isDlcFalsePositiveRisk(a: string, b: string): boolean  // DLC_KEYWORDS containment guard
fuzzyMatch(humbleTitle: string, steamTitle: string): boolean  // DLC-guard then titleSimilarity >= HUMBLE_FUZZY_MATCH_THRESHOLD
// Supporting private consts also move: EDITION_SUFFIXES, DLC_KEYWORDS
// backend/humble/constants.ts currently: export const HUMBLE_FUZZY_MATCH_THRESHOLD = 0.85
// dedup.ts KEEPS: recomputeOwnership() — Humble-specific (HumbleKey[], D-42 override predicate). Do NOT move it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create common/matching/titleMatch.ts with the lifted pure matcher + unit test</name>
  <files>src/common/matching/titleMatch.ts, src/backend/__tests__/titleMatch.test.ts</files>
  <read_first>
    - src/backend/humble/dedup.ts (lines 1-114 — the exact function bodies + EDITION_SUFFIXES/DLC_KEYWORDS to lift; read the header comments explaining the length-sensitive ratio and the ReDoS guard)
    - src/backend/humble/constants.ts (line 57 — the current HUMBLE_FUZZY_MATCH_THRESHOLD = 0.85 definition)
    - src/backend/humble/__tests__/dedup.test.ts (the existing pure-function assertions to mirror; confirms the "Into the Breach (Steam)" parenthetical fixture and DLC-guard cases)
    - .planning/phases/20-aggregated-store-search-cheapshark/20-PATTERNS.md (section "src/common/matching/titleMatch.ts" — the lift instructions and the "do NOT import HumbleKey/GameInfo" leak warning)
  </read_first>
  <behavior>
    - normalizeTitle('The Witcher 3™') strips the trademark and lowercases
    - normalizeTitle('Into The Breach (Steam)') === normalizeTitle('Into the Breach') (parenthetical strip is load-bearing)
    - titleSimilarity of identical normalized titles === 1
    - fuzzyMatch('Portal 2', 'Portal 2') === true; fuzzyMatch('Portal', 'Portal 2') respects the 0.85 threshold
    - isDlcFalsePositiveRisk('Terraria', 'Terraria Soundtrack DLC') === true (DLC keyword in longer, not shorter)
    - fuzzyMatch returns false whenever isDlcFalsePositiveRisk is true, regardless of score
  </behavior>
  <action>
    Create `src/common/matching/titleMatch.ts` as a pure, I/O-free module. Move verbatim from `dedup.ts`: the `EDITION_SUFFIXES` and `DLC_KEYWORDS` const arrays, and the four functions `normalizeTitle`, `titleSimilarity`, `isDlcFalsePositiveRisk`, `fuzzyMatch` — all exported. Import `distance` from `fastest-levenshtein` (already a dependency). Define `export const HUMBLE_FUZZY_MATCH_THRESHOLD = 0.85` HERE as the single source of truth (D-02 — keep this exact name, do NOT rename it despite the module living in common/ now). Preserve the existing header comments documenting the length-sensitive ratio rationale and the T-12-01/T-12-05 ReDoS guard (regex only ever interpolates the trusted literal keyword arrays, never untrusted title strings). The module MUST NOT import `HumbleKey`, `GameInfo`, or anything from `backend/` — signatures stay `string`/`number`/`boolean` only (RESEARCH Pitfall 4 leak check). Then create `src/backend/__tests__/titleMatch.test.ts` covering the behaviors above; you may port the relevant pure-function assertions from the existing `dedup.test.ts` since the moved functions are behavior-identical.
  </action>
  <verify>
    <automated>npx jest src/backend/__tests__/titleMatch.test.ts --selectProjects backend</automated>
  </verify>
  <acceptance_criteria>
    - src/common/matching/titleMatch.ts exports normalizeTitle, titleSimilarity, isDlcFalsePositiveRisk, fuzzyMatch, and HUMBLE_FUZZY_MATCH_THRESHOLD
    - `grep -n "from 'backend" src/common/matching/titleMatch.ts` returns nothing (no backend import)
    - `grep -n "HumbleKey\|GameInfo" src/common/matching/titleMatch.ts` returns nothing (no store-specific types leaked)
    - HUMBLE_FUZZY_MATCH_THRESHOLD === 0.85 defined in titleMatch.ts
    - titleMatch.test.ts passes green with the six behaviors above asserted
  </acceptance_criteria>
  <done>The shared matcher exists in common/, is pure, and is unit-tested green.</done>
</task>

<task type="auto">
  <name>Task 2: Point dedup.ts and constants.ts at the shared matcher; keep dedup tests green</name>
  <files>src/backend/humble/dedup.ts, src/backend/humble/constants.ts</files>
  <read_first>
    - src/backend/humble/dedup.ts (full file — the four functions to delete-and-import, and recomputeOwnership() which STAYS)
    - src/backend/humble/constants.ts (line 57 — HUMBLE_FUZZY_MATCH_THRESHOLD current definition)
    - src/backend/humble/__tests__/dedup.test.ts (must stay green unchanged)
    - src/common/matching/titleMatch.ts (created in Task 1 — the new import source)
  </read_first>
  <action>
    In `src/backend/humble/dedup.ts`, delete the local definitions of `EDITION_SUFFIXES`, `DLC_KEYWORDS`, `normalizeTitle`, `titleSimilarity`, `isDlcFalsePositiveRisk`, and `fuzzyMatch`, and instead `import { normalizeTitle, titleSimilarity, isDlcFalsePositiveRisk, fuzzyMatch } from 'common/matching/titleMatch'`. If other modules import these functions FROM dedup.ts, keep them working by re-exporting from dedup.ts (`export { fuzzyMatch, ... } from 'common/matching/titleMatch'`) — verify with a grep before deciding. `recomputeOwnership()` and its D-42 override semantics STAY in dedup.ts unchanged (it takes `HumbleKey[]`/`GameInfo[]` — Humble-specific, per RESEARCH Pitfall 4). In `src/backend/humble/constants.ts`, replace the local `HUMBLE_FUZZY_MATCH_THRESHOLD = 0.85` definition with a re-export from the common module (`export { HUMBLE_FUZZY_MATCH_THRESHOLD } from 'common/matching/titleMatch'`) so the single-source-of-truth (D-02) is preserved and every existing caller that imports the threshold from `constants.ts` keeps compiling.
  </action>
  <verify>
    <automated>npx jest src/backend/humble --selectProjects backend && npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function normalizeTitle\|function titleSimilarity\|function fuzzyMatch\|function isDlcFalsePositiveRisk" src/backend/humble/dedup.ts` returns 0 (definitions removed)
    - dedup.ts imports the four functions from 'common/matching/titleMatch'
    - recomputeOwnership is still defined in dedup.ts (`grep -c "recomputeOwnership" src/backend/humble/dedup.ts` >= 1)
    - constants.ts re-exports HUMBLE_FUZZY_MATCH_THRESHOLD from the common module (no second `= 0.85` literal in constants.ts)
    - existing src/backend/humble/__tests__/dedup.test.ts passes unchanged
    - npm run codecheck exits 0
  </acceptance_criteria>
  <done>dedup.ts and constants.ts consume the shared matcher; no behavior change; all Humble tests + codecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| title string → regex | Titles from libraries/keys flow into normalizeTitle's regex construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Denial (ReDoS) | normalizeTitle regex construction | mitigate | Preserve dedup.ts's existing T-12-01/T-12-05 guard: regex only ever interpolates the trusted, hardcoded EDITION_SUFFIXES/DLC_KEYWORDS literals — never an untrusted title string. Assert no config/IPC-sourced value reaches `new RegExp(...)`. |
| T-20-SC | Tampering | npm installs | accept | Zero new packages this plan; `fastest-levenshtein` already vetted/in-tree. No install task, no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx jest src/backend/__tests__/titleMatch.test.ts src/backend/humble --selectProjects backend` green
- `npm run codecheck` exits 0
- No backend imports or store-specific types in the new common module
</verification>

<success_criteria>
The fuzzy matcher is a single shared common/ module, consumed by both dedup.ts (unchanged behavior) and available for Plan 03's badge resolver, with the 0.85 threshold single-sourced (D-02).
</success_criteria>

<output>
Create `.planning/phases/20-aggregated-store-search-cheapshark/20-01-SUMMARY.md` when done.
</output>
