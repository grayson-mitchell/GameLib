# Phase 34.4 Security Evidence — Shard A (Plans 01–05)

Audited by: gsd-security-auditor (shard A)
Date: 2026-08-23
Scope: `(plan, threat_id)` rows for 34.4-01-PLAN.md through 34.4-05-PLAN.md (31 rows). Shard B (plans 06–10) is audited separately.
Register origin: authored at plan time (`register_authored_at_plan_time: true`) — this audit verifies each declared mitigation exists in shipped code; it does not scan blindly for new vulnerabilities.
ASVS level: 2 (default). block_on: high (default).

Legend: CLOSED = mitigation verified present and non-vacuous in cited/located code. OPEN = mitigation absent or unverifiable.

---

## Plan 34.4-01 (`src/backend/sidecar/steamAuthFlowRegistration.ts`, `src/backend/sidecar/__tests__/steamAuthFlows.test.ts`)

### T-34.4-01 — Information Disclosure — Steam password/SteamGuard code passing through `steamStartCredentials`/`steamSubmitGuard`
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `steamAuthFlowRegistration.ts:162-178` — both handlers are bare pass-throughs (`SteamUser.startCredentialLogin(username, password)` / `SteamUser.submitSteamGuardCode(args[0] as string)`), no `console.*`/log call in either body. Verified directly by source inspection (stronger evidence than the plan's "assert in review" bar). `34.4-REVIEW.md` (files_reviewed includes `steamAuthFlowRegistration.ts`, 0 critical findings) corroborates no such call was flagged.

### T-34.4-02 — Information Disclosure — refresh token from credential login — transfer to Phase 28 `getTokenStore()`/`SidecarKeyringTokenStore` seam
- Disposition: transfer
- Status: **CLOSED**
- Evidence: `steamAuthFlowRegistration.ts` imports `SteamUser` from `../storeManagers/steam/user` unchanged; no new `getTokenStore()`/`setToken` call site is added by this plan's diff (`git show --stat 625cd665 8f127f06` touches only `steamAuthFlowRegistration.ts` and its test). The transfer target's existing seam is exercised and proven by `steamAuthFlows.test.ts` Test 4 ("token seam") at lines ~370-430, which asserts a `rustInvoke`/`RUST_KEYRING_SET` frame carries the token via `getTokenStore().setToken(...)` and that `sidecar:store-snapshot` never surfaces a `refreshToken` field on `steamConfigStore`. This is the pre-existing, already-audited seam; this plan adds no parallel path.

### T-34.4-03 — Repudiation — `logoutSteam` registered with wrong kind (G-30-01 defect class)
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `steamAuthFlowRegistration.ts:197` — `ipcMain.on('logoutSteam', ...)` (send, not handle). Cross-checked against `main.ts:918` — `addListener('logoutSteam', async () => SteamUser.logout())` (plan cites `main.ts:939`; actual current line is 918 — citation drift, not a code defect). Bidirectional proof in `steamAuthFlows.test.ts:469-496`: positive test asserts a `writeSend` frame causes `SteamUser.logout` to be called exactly once and produces no response frame; negative test asserts a `writeInvoke` frame does NOT resolve `ok: true` and `SteamUser.logout` is NOT called. Both directions exercised — not a one-sided check.

### T-34.4-04 — Denial of Service — unguarded rejection from fire-and-forget `SteamUser.logout()` crashing sidecar
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `steamAuthFlowRegistration.ts:198-200` — `.catch((error) => console.warn('[steamAuthFlowRegistration] logoutSteam failed:', error))`, never rethrown. Proven non-vacuous in `steamAuthFlows.test.ts:499-534`: mocks `SteamUser.logout` to reject, asserts `console.warn` called with the module-attributed prefix, asserts a subsequent `health` invoke still resolves `ok: true` (RPC loop keeps serving), and asserts `unhandledRejection` was never fired (`process.on('unhandledRejection', ...)` listener array stays empty).

### T-34.4-05 — Information Disclosure — test run writing a real Steam token into developer's real config dir
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `steamAuthFlows.test.ts` top-of-file `jest.mock('os', ...)` redirects `homedir()` to a disposable per-process tmp dir; `jest.mock('../../storeManagers/steam/user')` automocks `SteamUser`. File is classified in the containment ledger: `src/backend/sidecar/__tests__/testContainment.test.ts:770` lists `'steamAuthFlows.test.ts'`. `jest.setupContainment.ts` is registered via `setupFiles` in `src/backend/jest.config.js` (confirmed via `testContainment.test.ts` header, lines 49-64), applying structurally to every backend test file.

### T-34.4-SC (plan 01) — Tampering — npm/pip/cargo installs
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `git show --stat 625cd665` and `git show --stat 8f127f06` (Plan 01's two task commits) — no `package.json`, `pnpm-lock.yaml`, `Cargo.toml`, or `Cargo.lock` hunks in either commit.

---

## Plan 34.4-02 (`src/backend/sidecar/steamAuthFlowRegistration.ts` extended, same test file)

### T-34.4-06 — Tampering/Input Validation — `redeemSteamKey` malformed/hostile payload
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `steamAuthFlowRegistration.ts:209-219` — guard rejects non-`'steam'` store, non-string key, or empty key with `{ store: 'steam', outcome: 'error', message: 'invalid-request' }` *before* `SteamUser.redeemKey` is called. Four not-called rejection tests in `steamAuthFlows.test.ts:870-906` (empty-string key, null payload, and two more variants) each assert `expect(SteamUser.redeemKey).not.toHaveBeenCalled()`.

### T-34.4-07 — Information Disclosure — Steam product key leaking into log line or rejection message
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `steamAuthFlows.test.ts:908-940` — test seeds `SECRET_KEY = 'SUPER-SECRET-KEY-VALUE-DO-NOT-LEAK'`, spies `console.log`/`console.warn`/`console.error`, drives a rejection case, and asserts `JSON.stringify(response)` and the concatenated text of all three spies' call args do NOT contain the secret. Non-vacuous: a distinctive planted value, not an empty-string check.

### T-34.4-08 — Elevation of Privilege — `steamBottleProvision`/`steamClientSetupStart` spawning installer processes from IPC — accept (Electron parity)
- Disposition: accept
- Status: **CLOSED**
- Evidence: `main.ts:925` (`addHandler('steamBottleProvision', async (event, args) => provisionBottle(args))`) and `main.ts:953` (`addHandler('steamClientSetupStart', async () => startGuidedClientInstall())`) delegate to the exact same `provisionBottle`/`startGuidedClientInstall` functions in `storeManagers/steam/bottle.ts` / `steam/clientSetup.ts` that `steamAuthFlowRegistration.ts:233-237` and `:325-327` delegate to. No reimplementation — same module, same `child_process` orchestration, both runtimes. `34.4-PORTED-CHANNELS.md:105-108` confirms both are declared unit-only (no live run this phase, D-08).

### T-34.4-09 — Repudiation — `steamBottleStatus` reporting stale/fabricated bottle state
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `steamAuthFlowRegistration.ts:248-254` reproduces the two `get_nodefault('provisioned') ?? false` / `get_nodefault('bottleName') ?? DEFAULT_STEAM_BOTTLE_NAME` reads exactly, no `loggedIn` field. Tests at `steamAuthFlows.test.ts` (~lines 595-630): default-object test, real-stored-values test, and an explicit `expect(response?.result).not.toHaveProperty('loggedIn')` assertion (17-17/WR-02 regression guard).

### T-34.4-10 — Denial of Service — live bottle provision/key redemption run during automated verification
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `steamAuthFlows.test.ts` mocks `provisionBottle` (`jest.mock('../../storeManagers/steam/bottle')`) and `SteamUser.redeemKey` (automocked via `jest.mock('../../storeManagers/steam/user')`) at file scope — every test case runs against the mock. `34.4-PORTED-CHANNELS.md:90` states both channels are "deliberately excluded" from the D-08 live gate; lines 105 and 112 declare both `unit`-only (not `unit + LIVE`).

### T-34.4-SC (plan 02) — Tampering — npm/pip/cargo installs
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `git show --stat 321dacf9` and `git show --stat e1d0ba04` — no package/lockfile/Cargo hunks.

---

## Plan 34.4-03 (`src/backend/sidecar/settingsFlowRegistration.ts`, `src/backend/sidecar/__tests__/settingsFlows.test.ts`)

### T-34.4-11 — Tampering — private-branch password bypassing `privateBranchesStore`'s layout via a hand-rolled key scheme
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `settingsFlowRegistration.ts:271-280` — both `getPrivateBranchPassword`/`setPrivateBranchPassword` route through `libraryManagerMap['gog'].getGame(...).getBranchPassword()`/`.setBranchPassword(...)` (which internally hit `storeManagers/gog/games.ts:1397/1401`, the sole owner of `privateBranchesStore`). Confirmed by direct grep: `settingsFlowRegistration.ts`'s import list (lines 59-96) contains no `privateBranchesStore`/`gog/electronStores` import — repo-wide, only `storeRegistration.ts` and `gog/games.ts`/`gog/library.ts` import `privateBranchesStore` directly. Note: the plan's claim of "asserted by a grep acceptance criterion" was not found materialized as an automated jest gate in `settingsFlows.test.ts` (no test asserts absence of the import string) — the underlying property was verified by the auditor directly via `grep -rn privateBranchesStore src/backend/`, but there is no regression-guarding automated test for this specific sub-claim. Recorded as a minor gap in rigor, not a code defect — closed on direct evidence.

### T-34.4-12 — Information Disclosure — password leaking into sidecar log line or response frame
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `settingsFlows.test.ts` ("T-34.4-12: no emitted response frame or stderr diagnostic contains submitted branch password", ~lines 694-724) — spies `process.stderr.write`, seeds a distinctive secret, drives `setPrivateBranchPassword`, asserts neither any response frame (`JSON.stringify(frame)`) nor any stderr call contains the secret.

### T-34.4-13 — Tampering — channels reaching the wrong runner's library manager (`'steam'` misattribution)
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `settingsFlows.test.ts:682-693` ("REQ-34.4-06: both channels reach libraryManagerMap.gog.getGame and NEVER libraryManagerMap.steam.getGame") — asserts `mockedGogGetGame` called and `expect(mockedSteamGetGame).not.toHaveBeenCalled()`. Hand RED-proof claim (flipping `'gog'` to `'steam'`) not independently re-run by this auditor (destructive edit forbidden), but the not-called assertion structurally would fail under that flip since `mockedSteamGetGame` would then be the one invoked.

### T-34.4-14 — Elevation of Privilege — side-effect-importing a feature `ipc_handler.ts` and registering channels this slice does not own
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `settingsFlowRegistration.ts:78` (`import '../storeManagers'`) and `:81` (`import { libraryManagerMap } from '../storeManagers'`) are both pre-existing imports (not new for this plan — `libraryManagerMap` is already used by `requestGameSettings`/`isNative` earlier in the same file). `storeManagers/index.ts` registers no IPC channels itself (constructs managers only) — confirmed by grep, no `ipcMain`/`addHandler`/`addListener` call in that file.

### T-34.4-SC (plan 03) — Tampering — npm/pip/cargo installs
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `git show --stat e3343206` and `git show --stat f7f0e540` — no package/lockfile/Cargo hunks.

---

## Plan 34.4-04 (`src/backend/sidecar/humbleFlowRegistration.ts`, `src/backend/sidecar/__tests__/humbleFlows.test.ts`)

### T-34.4-15 — Elevation of Privilege — side-effect-importing `humble/ipc_handler.ts`, violating SEAM.md Invariant B
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `humbleFlowRegistration.ts` imports only `HumbleUser`, `HumbleLibrary`, `runHumbleValidation` (curated) — no `humble/ipc_handler` import anywhere in the file (confirmed by grep for `ipc_handler` in the file: only the module docstring names it in prose). Source gate in `humbleFlows.test.ts:532-604` applies `stripSourceComments` FIRST, then checks three regex forms (named `import ... from`, bare `import '...'`, `require(...)`). Self-tests prove non-vacuity in both directions: a synthetic named-import source is detected (`:566-578`), a synthetic bare side-effect-import source is detected (`:580-589`) — this second form specifically matters because it mirrors this codebase's own `import '../storeManagers'` idiom, which a `from`-only regex would miss — and an anti-vacuity self-test confirms a docblock-only mention of `humble/ipc_handler.ts` does NOT trip the gate (`:591-604`), ruling out the false-positive failure mode. Negative-scope registry check at `:472-503` confirms `registerHumbleFlows()` registers exactly the 16 channels it owns and none of the 6 login-flow channels.

### T-34.4-16 — Information Disclosure — raw Humble key value leaking via `humbleGetRevealedKeyValue`
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `humbleFlowRegistration.ts` handler body for `humbleGetRevealedKeyValue` (in the "Key-state group") is a bare delegate to `HumbleLibrary.getRevealedKeyValue(gamekey, machineName)` with no logging. Proven in `humbleFlows.test.ts:317-353` — spies `console.warn`/`console.log`, drives the channel with a distinctive fake secret value, asserts `expect(warnSpy).not.toHaveBeenCalled()` and `expect(logSpy).not.toHaveBeenCalled()`.

### T-34.4-17 — Repudiation — `handle`/`send` kind mismatch silently dropping a key-state mutation
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `humbleFlows.test.ts:472-503` ("registerHumbleFlows() has registered all 16 Humble channels this slice owns: 15 as ipcMain.handle ... 1 (humbleDisconnect) as ipcMain.on exactly once") — iterates a 15-item `HANDLE_CHANNELS` list (includes `humbleMarkRedeemed`, `humbleUndoRedeemed`) asserting `isolationHandlerRegistry.has(channel)` per channel, and separately asserts the `humbleDisconnect` listener registry length is exactly 1. This is a per-channel loop, not an aggregate count, so a single kind-mismatched channel would fail the specific assertion for that channel.

### T-34.4-18 — Tampering — `gamekey`/`machineName` transposed at the transport boundary
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `humbleFlows.test.ts:274-353` — `humbleMarkRedeemed`/`humbleUndoRedeemed`/`humbleGetRevealedKeyValue` tests each use distinguishable values (`'GAMEKEY-DISTINCT'` vs `'MACHINENAME-DISTINCT'`, `-2`/`-3` suffixed variants) and assert `toHaveBeenCalledWith(gamekey, machineName)` in that exact order — a transposition would fail these assertions since the values are not interchangeable placeholders.

### T-34.4-19 — Information Disclosure — test run reaching developer's real Humble stores
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `humbleFlows.test.ts:77-78` — `jest.mock('../../humble/user')` and `jest.mock('../../humble/library')` automock both modules at file scope. File classified in containment ledger: `testContainment.test.ts:746` lists `'humbleFlows.test.ts'`. Relies on the same structural `setupFiles`/`jest.setupContainment.ts` floor as T-34.4-05.

### T-34.4-SC (plan 04) — Tampering — npm/pip/cargo installs
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `git show --stat 575414e7` and `git show --stat 62637712` — no package/lockfile/Cargo hunks.

---

## Plan 34.4-05 (`src/backend/sidecar/humbleFlowRegistration.ts` extended, same test file)

### T-34.4-20 — Tampering — `humbleSetOwnershipOverride` on an exact-AppID-match key overriding ground truth (D-42/T-12-03)
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: handler body finds the target key via `HumbleLibrary.getKeys().find(...)` and returns early with `console.warn` (never calling `setOwnershipOverride`) unless `targetKey.matchConfidence === 'fuzzy'`. Proven by `humbleFlows.test.ts:658-703` — one test asserts a non-fuzzy (`'exact'`) match is rejected (`setOwnershipOverride` not called, warning names only the machineName, and does not contain the planted `FAKE_KEY_VALUE`/`FAKE_GIFT_URL`), a second asserts an unknown machineName is also rejected.

### T-34.4-21 — Tampering — `humbleRecordGiftLinkOpened` called on an ineligible key (D-59/D-57)
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: handler checks `!targetKey || !targetKey.ownedElsewhere || targetKey.state !== 'UNREVEALED'` before calling `recordGiftLinkOpened`. Three separate not-called rejection tests at `humbleFlows.test.ts:791-861` — unknown machineName, not-`ownedElsewhere`, and state !== `'UNREVEALED'` — each independently asserts `recordGiftLinkOpened` is never called.

### T-34.4-22 — Repudiation — `humbleRecordGiftLinkOpened` registered as a listener per CONTEXT.md's incorrect claim
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: handler is `ipcMain.handle('humbleRecordGiftLinkOpened', ...)` in `humbleFlowRegistration.ts`, corrected inline comment documents the CONTEXT.md error explicitly. Bidirectionally proven in `humbleFlows.test.ts:745-773`: positive test asserts a `writeInvoke` produces a response frame; negative test asserts a `writeSend` never calls `recordGiftLinkOpened`.

### T-34.4-23 — Repudiation/Session — `humbleDisconnect` registered with wrong kind
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `ipcMain.on('humbleDisconnect', ...)` (send) in `humbleFlowRegistration.ts`, cross-checked against `humble/ipc_handler.ts:121` (`addListener`). Bidirectional kind proof at `humbleFlows.test.ts:864-890`: positive (send calls `disconnect` once, no response frame) and negative (invoke does not resolve `ok: true`, `disconnect` not called).

### T-34.4-24 — Session Management — stored encrypted Humble cookie surviving sign-out because no-op'd partition wipe aborted the sequence
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `src/backend/humble/user.ts:942-985` (`disconnect()`) — `configStore.clear()` runs synchronously first (line 953, ahead of any partition-wipe logic), followed by `humbleLibraryStore.clear()`/`humbleSyncStore.clear()`, and the subsequent partition-wipe steps are wrapped individually in try/catch that only logs (never throws). Proven — not merely asserted — by `humbleFlows.test.ts:1035-1120` ("D-05 ordering proof"), which uses `jest.requireActual('../../humble/user')` to drive the REAL `disconnect()` (bypassing the file-wide automock), spies the three real store `.clear()` methods, and confirms all three fire exactly once even though every one of 5 partition-wipe steps throws against the real `electronStub` D-09 no-op stub (asserted via `expect(warnSpy).toHaveBeenCalledTimes(5)` with each call matching `/wipe step/i`).

### T-34.4-25 — Information Disclosure — rejection warning leaking a Humble key value or gift URL (C4)
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: both `humbleSetOwnershipOverride` and `humbleRecordGiftLinkOpened` rejection paths log only `machineName`. Tests at `humbleFlows.test.ts:658-680` and `:791-812` seed `FAKE_KEY_VALUE`/`FAKE_GIFT_URL` (distinctive, non-empty) on the fake key object and assert the warning call's joined text does not contain either.

### T-34.4-26 — Elevation of Privilege (minor) — dev-only `humbleRunValidation` reachable in a packaged Tauri build (`electronStub.app.isPackaged` hardcoded `false`)
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `humbleFlowRegistration.ts` `isPackagedSidecar()` uses `require('node:sea').isSea()`, guarded try/catch defaulting to `true` (treat as packaged, i.e. do NOT register) on any failure, with a `console.warn` fallback log. Three tests at `humbleFlows.test.ts:946-1034` cover all three branches: dev signal (`isSea() === false`) → registered; packaged signal (`isSea() === true`) → not registered; undeterminable (`node:sea` throws) → not registered (safe default) with the warning logged. `34.4-PORTED-CHANNELS.md:129` confirms this was the primary approach and worked (fallback declared-divergence path was not needed), matching the plan's disjunctive mitigation claim.

### T-34.4-SC (plan 05) — Tampering — npm/pip/cargo installs; `node:sea` is a Node builtin
- Disposition: mitigate
- Status: **CLOSED**
- Evidence: `git show --stat 212a80c8` and `git show --stat f4315a72` — no package/lockfile/Cargo hunks. `node:sea` is used via bare `require('node:sea')` (`humbleFlowRegistration.ts`), a Node.js builtin module — not an npm dependency, no `package.json` entry required or present.

---

## Reconciliation

31/31 rows resolved. **31 CLOSED, 0 OPEN.**

| Plan | Rows | Closed | Open |
|------|------|--------|------|
| 34.4-01 | 6 | 6 | 0 |
| 34.4-02 | 6 | 6 | 0 |
| 34.4-03 | 5 | 5 | 0 |
| 34.4-04 | 6 | 6 | 0 |
| 34.4-05 | 8 | 8 | 0 |
| **Total** | **31** | **31** | **0** |

## Threat Flags (SUMMARY.md `## Threat Flags`)

- 34.4-01-SUMMARY.md, 34.4-02-SUMMARY.md, 34.4-03-SUMMARY.md: no `## Threat Flags` section present in these three summaries (informational — not a gap, since no new attack surface was claimed by the executor for these plans).
- 34.4-04-SUMMARY.md:176 — "None — plan's threat register (T-34.4-15..19, T-34.4-SC) covers exactly the surface this plan touches". No unmapped surface.
- 34.4-05-SUMMARY.md:167 — "None — plan's own threat register (T-34.4-20..26, T-34.4-SC) covers exactly the surface this plan touches". No unmapped surface.
- No unregistered flags found in shard A.

## Adjacent findings (out of scope for open/closed totals)

- `34.4-REVIEW.md` WR-01/WR-02/WR-03 concern `src/frontend/state/SteamSignOut.ts` and `src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx` — none map to a threat ID in this shard's register (they are functional/UX defects: an unguarded rejection breaking a documented contract, an internal runner codename leaking into user-facing text, and an i18next default-value interpolation bug). WR-01 is marked ✅ FIXED in the review; WR-02/WR-03 status not re-verified by this auditor (outside the 31-row scope). Flagging for awareness only — not counted as open/closed in this shard.
- `T-34.4-03`/`T-34.4-23` plan citations for `main.ts` line numbers (`:939`, prior citations) are stale relative to current `main.ts` (`logoutSteam` is now at `main.ts:918`, having shifted due to unrelated intervening edits). The mitigation itself is intact and verified against the current file; only the plan's line-number citation has drifted. Not a blocker.
