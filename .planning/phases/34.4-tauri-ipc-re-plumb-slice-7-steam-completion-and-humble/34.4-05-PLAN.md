---
phase: 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble
plan: 05
type: execute
wave: 2
depends_on: ["34.4-04"]
files_modified:
  - src/backend/sidecar/humbleFlowRegistration.ts
  - src/backend/sidecar/__tests__/humbleFlows.test.ts
autonomous: true
requirements: [REQ-34.4-07, REQ-34.4-08, REQ-34.4-09]
must_haves:
  truths:
    - "humbleSetOwnershipOverride / humbleClearOwnershipOverride / humbleGetOwnershipOverrides and humbleRecordGiftLinkOpened resolve on the Tauri sidecar"
    - "humbleRecordGiftLinkOpened is registered as a HANDLE (humble/ipc_handler.ts:72 addHandler), correcting CONTEXT.md's claim that it is a send — registering it as a listener would reproduce the G-30-01 silent-drop defect class"
    - "humbleDisconnect is registered as a SEND (humble/ipc_handler.ts:121 addListener) and its stored-credential wipe is proven to run BEFORE, and independently of, the no-op'd session.fromPartition loop (D-05)"
    - "The server-side re-validation guards in humbleSetOwnershipOverride (non-fuzzy rejection) and humbleRecordGiftLinkOpened (ineligible rejection) are ported verbatim — the renderer's view is never trusted"
    - "humbleRunValidation's registration reflects a conscious dev-vs-packaged decision, resolved via a genuine signal or explicitly declared, never silently shipping electronStub.app.isPackaged's hardcoded false"
    - "All 16 of this slice's Humble channels are now registered; the 6 Phase 34.4.1 channels remain unregistered"
  artifacts:
    - path: "src/backend/sidecar/humbleFlowRegistration.ts"
      provides: "All 16 Humble channels this slice owns"
      contains: "humbleDisconnect"
    - path: "src/backend/sidecar/__tests__/humbleFlows.test.ts"
      provides: "Kind proofs for the corrected humbleRecordGiftLinkOpened handle and the humbleDisconnect send, plus the D-05 ordering proof"
      contains: "humbleRunValidation"
  key_links:
    - from: "src/backend/sidecar/humbleFlowRegistration.ts"
      to: "HumbleUser.disconnect()"
      via: "ipcMain.on('humbleDisconnect') with a rejection guard, never ipcMain.handle"
      pattern: "ipcMain\\.on\\('humbleDisconnect'"
    - from: "src/backend/sidecar/humbleFlowRegistration.ts"
      to: "src/backend/humble/validation.ts"
      via: "runHumbleValidation() behind a dev-only guard"
      pattern: "runHumbleValidation"
---

<objective>
Complete the Humble half of this slice with its six remaining channels — the three ownership
overrides, `humbleRecordGiftLinkOpened`, `humbleDisconnect`, and `humbleRunValidation` — added to the
`humbleFlowRegistration.ts` that plan 34.4-04 created.

Purpose: every hard decision in the Humble half lands here.

1. **`humbleRecordGiftLinkOpened` is a `handle`, not a `send`.** `34.4-CONTEXT.md`'s Discretion
   section names it as one of three `send` channels. Research corrected that against source
   (`humble/ipc_handler.ts:72` is `addHandler`, `common/types/ipc.ts:350` types it `Promise<void>` in
   the `AsyncIPCFunctions` shape, `preload/api/humble.ts:31` calls `makeHandlerInvoker`) and the
   orchestrator confirmed it. Registering it as a listener would reproduce exactly the
   `sidecar-send-channels-fail-silently` defect class this phase's live gate exists to catch.
2. **`humbleDisconnect` is the one genuine `send`, and ships as a precisely declared partial (D-05).**
   Its stored-cookie delete is the real sign-out and the actual security boundary; only the Chromium
   partition wipe no-ops under Tauri, against a partition that never held anything.
3. **`humbleRunValidation` cannot simply reuse Electron's `!app.isPackaged` guard.**
   `electronStub.app.isPackaged` is hardcoded `false`, so porting the guard verbatim would register a
   dev-only channel in every Tauri build. This plan resolves that or declares it — never ships it
   silently.
4. **Two server-side re-validation guards are trust boundaries, not boilerplate.** The renderer must
   never be trusted to have gated its own button.

Output: 16 Humble channels registered, three corrected/declared decisions pinned by test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-CONTEXT.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-RESEARCH.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-PATTERNS.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-04-SUMMARY.md

<interfaces>
<!-- Exact Electron-parity source, read from src/backend/humble/ipc_handler.ts and main.ts on 2026-07-27. -->

**Kinds, cross-checked against source:**
- `humbleSetOwnershipOverride` (`ipc_handler.ts:39`) — **addHandler / handle**
- `humbleClearOwnershipOverride` (`ipc_handler.ts:55`) — **addHandler / handle**
- `humbleGetOwnershipOverrides` (`ipc_handler.ts:63`) — **addHandler / handle**
- `humbleRecordGiftLinkOpened` (`ipc_handler.ts:72`) — **addHandler / handle** ← the CORRECTION
- `humbleDisconnect` (`ipc_handler.ts:121`) — **addListener / SEND** ← the only send in the Humble half
- `humbleRunValidation` (`main.ts:970`) — **addHandler / handle**, registered only when `!app.isPackaged`

**`humbleSetOwnershipOverride` body (`ipc_handler.ts:39-54`)** — the Phase 12 Plan 04 / D-42 /
T-12-03 trust boundary. Its comment states this is "the ONE authoritative server-side check that an
override target is actually a fuzzy match — the renderer must never be trusted to have gated its own
button. A compromised/buggy renderer calling this on an exact-match machineName is rejected + logged,
never persisted (an exact AppID match is ground truth per D-44 and is never overridable)." Body:
find the key in `HumbleLibrary.getKeys()` whose `machineName` matches; if not found OR
`matchConfidence !== 'fuzzy'`, emit a warning naming only the `machineName` and **return without
calling `HumbleLibrary.setOwnershipOverride`**; otherwise call it.

**`humbleClearOwnershipOverride` (`ipc_handler.ts:55-57`)** → `HumbleLibrary.clearOwnershipOverride(machineName)`.
**`humbleGetOwnershipOverrides` (`ipc_handler.ts:63-65`)** → `HumbleLibrary.getAllOwnershipOverrides()`.

**`humbleRecordGiftLinkOpened` body (`ipc_handler.ts:72-91`)** — the Phase 13 Plan 02 / D-59 / D-57
trust boundary, mirroring the above: find the key by `machineName`; if not found, or
`!targetKey.ownedElsewhere`, or `targetKey.state !== 'UNREVEALED'`, warn (**machineName only, never a
URL or token — constraint C4**) and return without calling
`HumbleLibrary.recordGiftLinkOpened`; otherwise call it.

**`humbleDisconnect` (`ipc_handler.ts:117-125`)** — the WR-02 rider is in the source comment: "never
discard the disconnect promise silently — a rejection here (e.g. `session.fromPartition` throwing)
must not become an unhandled rejection in the main process. The credential wipe itself runs first
inside `disconnect()` and is synchronous." Body: `HumbleUser.disconnect().catch(err => <warn>)`.

**`HumbleUser.disconnect()`'s real body (`humble/user.ts:566-608`)** — the basis for D-05, verified by
direct read. Order: `invalidateSyncGeneration()`; then the SYNCHRONOUS credential wipe
`configStore.clear()`, `humbleLibraryStore.clear()`, `humbleSyncStore.clear()`; then
`session.fromPartition(HUMBLE_LOGIN_PARTITION)` and a best-effort wipe loop
(`clearStorageData`, `clearCache`, `clearAuthCache`, `clearHostResolverCache`, `clearData`), **each
step individually try/caught** so one rejected Electron session call cannot abort the rest
(`user.ts:588`'s own comment). Under Tauri that loop resolves against `electronStub.session`'s Phase
29 D-09 logged no-op — but the credential wipe, the real security boundary, already committed.

**`humbleRunValidation` (`main.ts:965-970`)**: `if (!app.isPackaged) { addHandler('humbleRunValidation',
async () => runHumbleValidation()) }`. `runHumbleValidation` is `export async function
runHumbleValidation(): Promise<HumbleValidationReport>` at `src/backend/humble/validation.ts:22`.
`humble/ipc_handler.ts:13-16`'s own docstring records why it is registered in `main.ts` and not in
`registerHumbleIpcHandlers()`: "kept out of this always-on function so it can never be accidentally
exposed in a packaged build."

**RESEARCH.md Open Question 1 — RESOLVED at plan time, use this finding rather than re-investigating:**
`src-tauri/src/main.rs` was read directly. `use_dev_sidecar()` (L737-739) reduces to
`cfg!(debug_assertions)`. `spawn_sidecar_dev()` (L745-767) runs
`Command::new("node").arg(&entry).stdin/stdout/stderr(Stdio::piped()).spawn()`. `spawn_sidecar_packaged()`
(L775-800) converts `app.shell().sidecar("gamelib-sidecar")` into a plain `Command` and spawns it the
same way. **Neither path calls `.env(...)`** — there is NO env var or CLI flag distinguishing the two
today, so the guard cannot be resolved through the Electron `app` shim or a spawn-time environment
signal without a Rust change (which this slice forbids).

**However, a genuine Node-only signal DOES exist:** the packaged sidecar is a Node Single Executable
Application. `package.json:35` defines `build:sidecar-sea` (`pnpm build:sidecar && esbuild --bundle
--platform=node --target=node22 meta/buildSidecarSea.ts | node`), producing the `externalBin` the
packaged path spawns, whereas `build:sidecar` (L34) produces the plain `build/main/sidecar.js` the dev
path runs under `node`. The project runs Node v26.2.0 and `require('node:sea').isSea` is a function
(verified at plan time). `node:sea` is a builtin, so esbuild's `--packages=external` leaves it as a
plain builtin require — this is NOT the alias/relative `sync-require-alias-unresolved-in-build`
hazard.

Diagnostics convention: `humble/ipc_handler.ts` uses `logWarning([...], LogPrefix.Backend)` from
`backend/logger`. Sibling sidecar registration modules use a module-prefixed diagnostic. Either is
acceptable here; what is not acceptable is dropping the warning, or widening it to carry a key value
or gift URL (C4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the ownership-override trio, the corrected humbleRecordGiftLinkOpened handle, and the humbleDisconnect send</name>
  <files>src/backend/sidecar/humbleFlowRegistration.ts</files>
  <read_first>
    - `src/backend/sidecar/humbleFlowRegistration.ts` — plan 34.4-04's output. You are extending it; read its docstring (which you must extend) and its existing 10 registrations.
    - `src/backend/humble/ipc_handler.ts` L33-92 and L117-125 — the exact parity bodies for the ownership trio, `humbleRecordGiftLinkOpened`, and `humbleDisconnect`, including the D-42/T-12-03 and D-59/D-57 comments explaining why the server-side re-validation exists.
    - `src/backend/humble/user.ts` L560-610 — `disconnect()`'s real body: the synchronous credential wipe first, then the individually-try/caught partition-wipe loop, and L588's comment.
    - `src/backend/sidecar/electronStub.ts` L547-564 — the `session` stub's Phase 29 D-09 docstring, which names `humble/user.ts` by name. This is D-05's basis; read it in full.
    - `src/backend/humble/library.ts` L1353-1420 — the exact method names on the `HumbleLibrary` object (`setOwnershipOverride`, `clearOwnershipOverride`, `getAllOwnershipOverrides`, `recordGiftLinkOpened`).
    - `src/backend/sidecar/loggerFlowRegistration.ts` — the sibling module's call-site rejection-guard shape for a fire-and-forget async listener.
  </read_first>
  <action>
Add five registrations to `registerHumbleFlows()`.

**Four `ipcMain.handle` registrations.** `humbleClearOwnershipOverride` and
`humbleGetOwnershipOverrides` are one-line delegates.

`humbleSetOwnershipOverride` and `humbleRecordGiftLinkOpened` each carry a **server-side
re-validation guard that must be ported verbatim** — it is the authoritative check, and the renderer
is never trusted to have gated its own button. Reproduce each condition exactly:
- `humbleSetOwnershipOverride`: look up the key in `HumbleLibrary.getKeys()` by `machineName`; reject
  when the key is missing OR `matchConfidence !== 'fuzzy'`. An exact AppID match is ground truth
  (D-44) and is never overridable.
- `humbleRecordGiftLinkOpened`: same lookup; reject when the key is missing OR `!ownedElsewhere` OR
  `state !== 'UNREVEALED'`.
Each rejection path emits a warning naming **only the `machineName`** — never a key value, never a
gift URL (constraint C4) — and returns **without** calling the mutating `HumbleLibrary` method. Do
not collapse the two guards into a shared helper: they check different predicates and each carries
its own documented rationale; keep both bodies traceable to their `ipc_handler.ts` source lines.

**`humbleRecordGiftLinkOpened` is `ipcMain.handle`, NOT `ipcMain.on`.** Add an inline comment
recording the correction and its three independent confirmations (`humble/ipc_handler.ts:72` is
`addHandler`; `common/types/ipc.ts:350` types it `Promise<void>` in the `AsyncIPCFunctions` shape;
`preload/api/humble.ts:31` uses `makeHandlerInvoker`), so a future reader who finds CONTEXT.md's
prose does not "fix" it back.

**One `ipcMain.on` registration: `humbleDisconnect`.** This is `addListener` at
`humble/ipc_handler.ts:121` — a **send**. Call `HumbleUser.disconnect()` and attach a `.catch(...)`
carrying the WR-02 rider forward (never discard the disconnect promise silently; a rejection must not
become an unhandled rejection). Never rethrow — `sidecar-dialog-reject-crashes`.

Add a **D-05 declaration comment** above the `humbleDisconnect` registration stating precisely, in
this order: (a) `HumbleUser.disconnect()` clears the encrypted-cookie store SYNCHRONOUSLY and FIRST
(`configStore.clear()`, `humbleLibraryStore.clear()`, `humbleSyncStore.clear()`) — that is the real
sign-out and the actual security boundary, and it is fully functional under Tauri; (b) only the
subsequent best-effort `session.fromPartition(HUMBLE_LOGIN_PARTITION)` wipe loop no-ops, against
`electronStub.session`'s accepted Phase 29 D-09 stub, and under Tauri that partition never held
anything — so surfacing a "your browser session may survive" warning would be actively FALSE (34.3
D-01's rider against inflating a precautionary no-op into a shipped defect); (c) **once Phase 34.4.1
creates a real browser context, `humbleDisconnect` MUST be revisited to clear it too**, or 34.4.1
silently inherits a half-finished sign-out.

Extend the module docstring with all five channels, their `ipc_handler.ts:LINE` citations and kinds,
the `humbleRecordGiftLinkOpened` correction, and the D-05 partial declaration.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit && npx prettier --check src/backend/sidecar/humbleFlowRegistration.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ipcMain.on('humbleDisconnect'" src/backend/sidecar/humbleFlowRegistration.ts` returns 1, and `grep -c "ipcMain.handle('humbleDisconnect'" ...` returns 0
    - `grep -c "ipcMain.handle('humbleRecordGiftLinkOpened'" ...` returns 1, and `grep -c "ipcMain.on('humbleRecordGiftLinkOpened'" ...` returns 0
    - The `humbleSetOwnershipOverride` body contains the literal `'fuzzy'` comparison and returns before any `setOwnershipOverride(` call in that body
    - The `humbleRecordGiftLinkOpened` body contains both an `ownedElsewhere` check and an `'UNREVEALED'` comparison, and returns before any `recordGiftLinkOpened(` call in that body
    - The `humbleDisconnect` listener body contains a `.catch(` and no `throw`
    - A comment above `humbleDisconnect` names all three D-05 points: the synchronous credential wipe running first, the `electronStub.session` D-09 no-op, and the Phase 34.4.1 revisit obligation
    - A comment at `humbleRecordGiftLinkOpened` cites `humble/ipc_handler.ts:72`, `common/types/ipc.ts:350` and `preload/api/humble.ts:31`
    - `npx tsc --noEmit` and `npx prettier --check` exit 0
  </acceptance_criteria>
  <done>Five channels are registered with kinds cross-checked against source, both server-side re-validation guards are ported verbatim, and D-05's partial is declared in code where the next reader will find it.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve or declare humbleRunValidation's dev-vs-packaged guard</name>
  <files>src/backend/sidecar/humbleFlowRegistration.ts</files>
  <read_first>
    - `src/backend/main.ts` L963-972 — the `if (!app.isPackaged)` guard and its D-12/T-10-16 comment.
    - `src/backend/humble/ipc_handler.ts` L13-16 — the docstring recording WHY this channel is deliberately kept out of the always-on registration function.
    - `src/backend/sidecar/electronStub.ts` around L170-185 — the `app` stub's hardcoded `isPackaged: false`, which is why the Electron guard cannot be reused verbatim.
    - `src/backend/humble/validation.ts` L1-40 — `runHumbleValidation`'s exact signature and what it actually does against the real adapter and the real stored encrypted cookie.
    - `package.json` L33-35 — `tauri:dev`, `build:sidecar` and `build:sidecar-sea`, confirming that the packaged sidecar is a Node SEA binary and the dev sidecar is a plain script run under `node`.
    - `src/backend/sidecar/bootstrap.ts` — how the sidecar reads any process-level signal today, so the new signal is placed consistently.
  </read_first>
  <action>
Register `humbleRunValidation` behind a guard that reflects a **conscious decision**, and record that
decision in code. Reusing `app.isPackaged` verbatim is forbidden: `electronStub`'s `app` shim
hardcodes `isPackaged: false`, so the guard would always pass and the dev-only validation trigger
would exist in every Tauri build, packaged or not — a real (if minor) elevation-of-privilege
divergence from Electron.

**Primary approach — a genuine Node-only packaged signal.** The packaged sidecar is a Node Single
Executable Application (`package.json:35`'s `build:sidecar-sea`), while the dev sidecar is a plain
script run under `node` (`build:sidecar`, L34). `require('node:sea').isSea()` therefore distinguishes
them with zero Rust change and zero new dependency. Implement a small, well-commented helper that
returns whether this sidecar process is a packaged SEA build, and gate the `humbleRunValidation`
registration on its negation, mirroring `main.ts:969`'s `!app.isPackaged` semantics. Wrap the
`node:sea` access so an older or unavailable runtime cannot throw at registration time; on any
failure to determine, choose the SAFE default (treat the build as packaged, i.e. do NOT register the
dev-only channel) and log that fallback once. Import `node:sea` as a builtin — do not add a package,
and do not reach for a relative/alias `require` (`sync-require-alias-unresolved-in-build` does not
apply to builtins, but keep the import style consistent with the file).

**Verify empirically before committing to it.** Confirm at execution time that `node -e
"console.log(typeof require('node:sea').isSea)"` prints `function` on this machine's Node, and that
the helper returns `false` under `node build/main/sidecar.js`. This is 34.3 D-05's standing lesson:
an API inference is not a measurement. If `node:sea` proves unusable at execution time for a concrete,
recorded reason:

**Sanctioned fallback — declare the divergence explicitly.** Register `humbleRunValidation`
unconditionally, add a comment stating that the sidecar has no packaged-vs-dev signal today (citing
the direct `main.rs` read: `use_dev_sidecar()` at L737-739 reduces to `cfg!(debug_assertions)`, and
neither `spawn_sidecar_dev()` L745-767 nor `spawn_sidecar_packaged()` L775-800 calls `.env(...)`), and
state the accepted residual risk: a dev-only validation trigger is reachable in a packaged Tauri
build, mitigated by there being **no UI entry point** for it — it is a manual dev trigger with no
renderer affordance. Plan 34.4-09 must then carry a matching declared row in
`34.4-PORTED-CHANNELS.md`.

Whichever path you take, record it in your SUMMARY as an explicit decision with its evidence, and
state which one plan 34.4-09's declared row must describe. **What is refused is porting the guard
verbatim and saying nothing.**

Import `runHumbleValidation` from `'../humble/validation'` directly — never `humble/ipc_handler.ts`.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && node -e "console.log('isSea type:', typeof require('node:sea').isSea)" && npx tsc --noEmit && npx prettier --check src/backend/sidecar/humbleFlowRegistration.ts</automated>
  </verify>
  <acceptance_criteria>
    - `humbleRunValidation` appears exactly once as `ipcMain.handle('humbleRunValidation'` in `humbleFlowRegistration.ts`
    - `grep -c "app.isPackaged" src/backend/sidecar/humbleFlowRegistration.ts` returns 0 — the Electron guard is not reused verbatim
    - `runHumbleValidation` is imported from `'../humble/validation'`, and no import references `humble/ipc_handler`
    - EITHER a packaged-signal helper exists whose comment names `node:sea`, `build:sidecar-sea` and the safe-default-on-failure behavior, AND the SUMMARY records the empirical check (`typeof require('node:sea').isSea === 'function'`, and the helper returning `false` under the dev sidecar entry) — OR an unconditional registration exists with a declared-divergence comment citing `main.rs` L737-739 / L745-767 / L775-800 and the no-UI-entry-point mitigation
    - The SUMMARY names which of the two paths was taken, with its evidence, and states what plan 34.4-09's declared row must say
    - `npx tsc --noEmit` exits 0 and `git diff --stat src-tauri/` produces no output
  </acceptance_criteria>
  <done>`humbleRunValidation` is registered behind a guard that reflects a recorded decision, and the `electronStub.app.isPackaged` divergence is either resolved or explicitly declared — never silently shipped.</done>
</task>

<task type="auto">
  <name>Task 3: Kind proofs, guard proofs, and the D-05 ordering proof</name>
  <files>src/backend/sidecar/__tests__/humbleFlows.test.ts</files>
  <read_first>
    - `src/backend/sidecar/__tests__/humbleFlows.test.ts` — plan 34.4-04's output. Reuse its scaffold and helpers; extend, do not rewrite.
    - `src/backend/sidecar/humbleFlowRegistration.ts` — your Task 1 and Task 2 output.
    - `src/backend/humble/__tests__/user.test.ts` — how `disconnect()` is already covered at the logic level, so this suite does not duplicate it and instead proves ordering and independence at the seam.
    - `src/backend/sidecar/electronStub.ts` L547-564 — the `session` stub, so the ordering test observes the real no-op rather than a fabricated one.
  </read_first>
  <action>
Extend `humbleFlows.test.ts` with the following, each `it(...)` prefixed by its requirement ID.

**REQ-34.4-07 — ownership trio + the gift-link correction.**
- Round-trip each of the three ownership channels and `humbleRecordGiftLinkOpened`.
- **Guard proof, `humbleSetOwnershipOverride`:** with `HumbleLibrary.getKeys()` mocked to return a key
  whose `matchConfidence` is `'exact'`, assert `setOwnershipOverride` was **not** called and a warning
  was emitted; with `'fuzzy'`, assert it **was** called once with the machineName. A test that only
  checks the happy path would pass with the guard deleted.
- **Guard proof, `humbleRecordGiftLinkOpened`:** three rejection cases — key missing,
  `ownedElsewhere` false, `state` not `'UNREVEALED'` — each asserting `recordGiftLinkOpened` was NOT
  called; plus the eligible case asserting it was called once.
- **Warning-content assertion:** in every rejection case, assert the emitted diagnostic contains the
  machineName and contains **no** key value and no URL (constraint C4). Seed the mocked key with a
  distinctive fake key value and a distinctive fake URL so the negative assertion is meaningful.
- **Kind proof for the correction:** assert `humbleRecordGiftLinkOpened` resolves as an **invoke**
  (a response frame comes back), and that it is NOT registered as a listener. This is the assertion
  that pins the research correction against CONTEXT.md's prose.

**REQ-34.4-09 — `humbleDisconnect`, kind and D-05 ordering.**
- **Kind proof, positive:** a `writeSend` frame for `humbleDisconnect` calls `HumbleUser.disconnect`
  exactly once.
- **Kind proof, negative:** a `writeInvoke` for `humbleDisconnect` does not produce a successful
  response frame carrying a disconnect result.
- **Rejection guard:** with `HumbleUser.disconnect` rejecting, drive the send, `flush()`, assert the
  process did not crash, that a subsequent `health` invoke still responds, and that a module-attributed
  diagnostic was emitted. Install an `unhandledRejection` listener in the test so an escaped rejection
  fails the test rather than the worker.
- **The D-05 ordering proof — this is the load-bearing one.** Do NOT automock
  `HumbleUser.disconnect` for this case. Exercise the real `disconnect()` against the real
  `electronStub.session` no-op with the three Humble stores observable (spy on their `clear` methods,
  or assert their post-state), and prove BOTH: (a) all three store clears happened; (b) they happened
  even though the `session.fromPartition` wipe loop no-op'd — i.e. the credential wipe is not
  contingent on the partition steps. Then force a partition step to reject and assert the store
  clears STILL happened, which is the actual claim D-05 makes and `user.ts:588`'s comment requires.
  If the real `disconnect()` cannot be driven from this suite without reaching real developer data,
  drive it in isolation with the stores mocked at the `electronStores` boundary — but do not weaken
  the assertion to "disconnect was called".

**REQ-34.4-08 — `humbleRunValidation`.** Pin whichever path Task 2 took: if a packaged-signal helper
exists, test both branches (registered when the signal says dev, not registered when it says packaged,
and the safe default when the signal cannot be determined); if the divergence was declared instead,
assert the channel is registered unconditionally and that the declaring comment exists in source
(strip comments with `src/backend/testUtils/stripSourceComments.ts` is NOT appropriate here since the
assertion is about a comment — instead assert on the raw source, and add a self-test proving the gate
rejects a source lacking the declaration).

**Scope re-check:** re-run plan 34.4-04's negative-scope guard and confirm the six Phase 34.4.1
channels are STILL unregistered after this plan's five additions, and that the module now registers
exactly 16 channels.

**RED proofs (mandatory, by hand, then restore).** Two, each recorded verbatim in the SUMMARY:
1. Flip `humbleDisconnect` from `ipcMain.on` to `ipcMain.handle` — the send-kind tests must fail.
2. Delete `humbleSetOwnershipOverride`'s `matchConfidence !== 'fuzzy'` early return — the non-fuzzy
   rejection test must fail on its not-called assertion.
Restore via `git checkout` after each and confirm `git diff --stat` on the module is empty against
your Task 2 commit.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib/src/backend && npx jest sidecar/__tests__/humbleFlows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest sidecar/__tests__/humbleFlows.test.ts` passes; record the new test count against plan 34.4-04's
    - `humbleRecordGiftLinkOpened` has a test asserting it resolves as an invoke and is not a listener
    - `humbleDisconnect` has both a positive send-kind test and a negative not-an-invoke test
    - The D-05 ordering test asserts the three Humble store clears happened even when a `session.fromPartition` step rejects — not merely that `disconnect` was called
    - `humbleSetOwnershipOverride` has an exact-match rejection test asserting `setOwnershipOverride` was NOT called; `humbleRecordGiftLinkOpened` has three distinct rejection tests each asserting `recordGiftLinkOpened` was NOT called
    - Every rejection test asserts the emitted diagnostic contains the machineName and does NOT contain the seeded fake key value or URL
    - `humbleRunValidation` coverage matches whichever path Task 2 took, and includes a self-test if it is a source-text gate
    - The negative-scope guard still passes: all six Phase 34.4.1 channels unregistered, and the module registers exactly 16 channels
    - Both hand RED proofs are recorded in the SUMMARY with verbatim failure output, and the module is restored byte-identical afterwards
    - Full backend `npx jest` shows no newly-red suite other than the expected `electronReachLedger.test.ts` (plan 34.4-08's work)
  </acceptance_criteria>
  <done>The two corrected/declared kinds are pinned by tests that fail when flipped, both server-side trust boundaries are proven by not-called assertions, and D-05's independence claim is proven rather than asserted in prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar IPC | `humbleSetOwnershipOverride` and `humbleRecordGiftLinkOpened` receive a `machineName` the renderer may have chosen without a legitimate UI gate |
| sign-out boundary | `humbleDisconnect` is the only way a Tauri user can end their Humble session |
| build boundary | `humbleRunValidation` is a dev-only trigger that must not exist in a packaged build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4-20 | Tampering | a compromised or buggy renderer calling `humbleSetOwnershipOverride` on an exact-AppID-match key, overriding ground truth (D-42/D-44, T-12-03) | mitigate | The authoritative server-side `matchConfidence !== 'fuzzy'` re-validation is ported verbatim and returns before the mutation; proven by a not-called rejection test and hand RED-proved by deleting the early return (ASVS V5) |
| T-34.4-21 | Tampering | `humbleRecordGiftLinkOpened` called on an ineligible key, corrupting gift bookkeeping (D-59/D-57) | mitigate | All three server-side predicates (`exists`, `ownedElsewhere`, `state === 'UNREVEALED'`) ported verbatim, each with a not-called rejection test |
| T-34.4-22 | Repudiation | `humbleRecordGiftLinkOpened` registered as a listener per CONTEXT.md's incorrect claim, silently dropping every call | mitigate | Registered as `ipcMain.handle` cross-checked against three independent sources (`ipc_handler.ts:72`, `common/types/ipc.ts:350`, `preload/api/humble.ts:31`), asserted as an invoke in test, and the correction recorded in an inline comment so it is not "fixed" back |
| T-34.4-23 | Repudiation / Session | `humbleDisconnect` registered with the wrong kind, leaving a Tauri user unable to sign out of Humble at all | mitigate | Registered as `ipcMain.on` per `ipc_handler.ts:121`; asserted in both directions and hand RED-proved by a kind flip; covered live by gate item 2's family (plan 34.4-10) — ASVS V3 |
| T-34.4-24 | Session Management | the stored encrypted Humble cookie surviving a sign-out because the no-op'd partition wipe aborted the sequence | mitigate | `disconnect()`'s synchronous credential wipe runs FIRST and each partition step is individually try/caught (`user.ts:588`). The D-05 ordering test proves the store clears still happen when a partition step rejects — the claim is proven, not asserted (ASVS V3) |
| T-34.4-25 | Information Disclosure | a rejection warning leaking a Humble key value or gift URL (constraint C4) | mitigate | Both rejection paths log the machineName only; tests seed a distinctive fake key value and URL and assert neither appears in the emitted diagnostic |
| T-34.4-26 | Elevation of Privilege (minor) | the dev-only `humbleRunValidation` channel reachable in a packaged Tauri build because `electronStub.app.isPackaged` is hardcoded `false` | mitigate | Resolved via a genuine Node SEA signal (`node:sea`'s `isSea()`, with a safe default of "treat as packaged" when undeterminable), verified empirically at execution time; or, if that proves unusable, explicitly declared with the `main.rs` evidence and the no-UI-entry-point mitigation, plus a declared row in `34.4-PORTED-CHANNELS.md`. Never silently shipped |
| T-34.4-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages; `node:sea` is a Node builtin |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx prettier --check` on both touched files exits 0
- `cd src/backend && npx jest sidecar/__tests__/humbleFlows.test.ts` passes
- `humbleFlowRegistration.ts` registers exactly 16 channels: 15 `ipcMain.handle` and 1 `ipcMain.on` (`humbleDisconnect`)
- The six Phase 34.4.1 channels remain unregistered (negative-scope guard still green)
- `git diff --stat src-tauri/` is empty — zero new Rust arms
- `src/backend/humble/ipc_handler.ts` and `src/backend/main.ts` are byte-unchanged
- `electronReachLedger.test.ts` remains expected-red until plan 34.4-08
</verification>

<success_criteria>
- 6 channels registered, kinds cross-checked against source, with the `humbleRecordGiftLinkOpened` correction pinned by test
- `humbleDisconnect` ships as a `send` with D-05's partial declared in code and its independence proven
- `humbleRunValidation`'s dev/packaged divergence is resolved or declared, with recorded evidence
- Two hand RED proofs recorded
</success_criteria>

<output>
Create `.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-05-SUMMARY.md` when done
</output>
