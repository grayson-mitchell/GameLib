---
phase: 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/sidecar/humbleFlowRegistration.ts
  - src/backend/sidecar/handlers.ts
  - src/backend/sidecar/__tests__/humbleFlows.test.ts
  - src/backend/sidecar/__tests__/testContainment.test.ts
autonomous: true
requirements: [REQ-34.4-07]
must_haves:
  truths:
    - "A new humbleFlowRegistration.ts exists and is wired into handlers.ts, registering Humble channels on the Tauri sidecar"
    - "The 10 library/sync and key-state Humble channels resolve real values instead of rejecting with UNPORTED_CHANNEL_MARKER"
    - "humble/ipc_handler.ts is NEVER imported — the module curated-imports humble/user.ts and humble/library.ts directly, so the 6 channels Phase 34.4.1 owns stay unregistered and keep rejecting non-fatally (SEAM.md Invariant B, D-02)"
    - "The load-bearing import '../storeManagers'-first circular-dependency fix is present in the new module, because humble/library.ts:41-42 reaches storeManagers/steam/*"
    - "All 10 channels are registered as handle, cross-checked against humble/ipc_handler.ts's own addHandler calls"
  artifacts:
    - path: "src/backend/sidecar/humbleFlowRegistration.ts"
      provides: "registerHumbleFlows() — the curated Humble channel registration module"
      min_lines: 60
      exports: ["registerHumbleFlows"]
    - path: "src/backend/sidecar/__tests__/humbleFlows.test.ts"
      provides: "Registration-kind and transport coverage for the ported Humble channels, plus a negative-scope guard for the 6 deferred ones"
      min_lines: 120
  key_links:
    - from: "src/backend/sidecar/handlers.ts"
      to: "src/backend/sidecar/humbleFlowRegistration.ts"
      via: "import { registerHumbleFlows } + call site alongside registerClipboardFlows()/registerLoggerFlows()"
      pattern: "registerHumbleFlows\\(\\)"
    - from: "src/backend/sidecar/humbleFlowRegistration.ts"
      to: "src/backend/humble/library.ts"
      via: "HumbleLibrary.sync / getKeys / getSyncState / markRedeemed / undoRedeemed / getRevealedKeyValue / getClaimAnnotations / getAllGiftedAt"
      pattern: "HumbleLibrary\\."
    - from: "src/backend/sidecar/humbleFlowRegistration.ts"
      to: "src/backend/humble/user.ts"
      via: "HumbleUser.getUserDetails / checkHealthAndFlagExpiry"
      pattern: "HumbleUser\\."
---

<objective>
Create `humbleFlowRegistration.ts` — the new curated Humble registration module — wire it into
`handlers.ts`, and register the **10 library/sync and key-state channels**: `humbleSync`,
`humbleGetKeys`, `humbleGetSyncState`, `humbleGetUserInfo`, `humbleCheckHealth`,
`humbleGetRevealedKeyValue`, `humbleMarkRedeemed`, `humbleUndoRedeemed`,
`humbleGetClaimAnnotations`, `humbleGetGiftedAt`. Create the new `humbleFlows.test.ts` alongside.

Purpose: this is the sharpest instance of the curated-import rule in the whole port sequence.
`humble/ipc_handler.ts` registers **21** channels, of which this slice owns 15 and Phase 34.4.1 owns
6 (`humbleStartLogin` L21, `humbleReconnect` L23, `humbleGetLoginUserAgent` L25, `humbleRevealKey`
L102, `humbleStopLogin` L126, `humbleLoginNavigated` L127). A single side-effect import of that file
would register all 21, prematurely claiming channels 34.4.1 owns and violating SEAM.md Load-Bearing
Invariant B — and would drag `backend/ipc` into the sidecar's module graph. Import `humble/user.ts`
and `humble/library.ts` **directly**.

Output: a new registration module, its `handlers.ts` wiring, and a new test suite whose
negative-scope guard proves the 6 deferred channels stayed unregistered.

Plan 34.4-05 (next wave, same two files) adds the remaining 6 Humble channels — the three ownership
overrides, `humbleRecordGiftLinkOpened`, `humbleDisconnect` (the one `send`), and
`humbleRunValidation`. Do not register those here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-CONTEXT.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-RESEARCH.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-PATTERNS.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-VALIDATION.md

<interfaces>
<!-- Exact Electron-parity source, read from src/backend/humble/ipc_handler.ts on 2026-07-27. -->

**All 10 channels in this plan are `addHandler` → `ipcMain.handle`. None is a `send`.**

From `src/backend/humble/ipc_handler.ts` — the parity bodies (each is a thin delegate):
- L22 `humbleGetUserInfo` → `HumbleUser.getUserDetails()`
- L24 `humbleCheckHealth` → `HumbleUser.checkHealthAndFlagExpiry()`
- L30 `humbleSync` → `HumbleLibrary.sync()`
- L31 `humbleGetKeys` → `HumbleLibrary.getKeys()`
- L32 `humbleGetSyncState` → `HumbleLibrary.getSyncState()`
- L92 `humbleGetGiftedAt` → `HumbleLibrary.getAllGiftedAt()`
- L105-107 `humbleMarkRedeemed` → `HumbleLibrary.markRedeemed(params.gamekey, params.machineName)`
- L108-110 `humbleUndoRedeemed` → `HumbleLibrary.undoRedeemed(params.gamekey, params.machineName)`
- L111-113 `humbleGetRevealedKeyValue` → `HumbleLibrary.getRevealedKeyValue(params.gamekey, params.machineName)`
- L114-116 `humbleGetClaimAnnotations` → `HumbleLibrary.getClaimAnnotations()`

**DO NOT register (Phase 34.4.1 owns these, D-02):** `humbleStartLogin` (L21), `humbleReconnect`
(L23), `humbleGetLoginUserAgent` (L25), `humbleRevealKey` (L102), `humbleStopLogin` (L126),
`humbleLoginNavigated` (L127).
**Not in this plan (plan 34.4-05 owns them):** `humbleSetOwnershipOverride` (L39),
`humbleClearOwnershipOverride` (L55), `humbleGetOwnershipOverrides` (L63),
`humbleRecordGiftLinkOpened` (L72), `humbleDisconnect` (L121, the one `addListener`),
`humbleRunValidation` (`main.ts:970`).

Export shapes (confirmed by direct read):
- `HumbleUser` is `export class HumbleUser` at `src/backend/humble/user.ts:136` (static methods).
- `HumbleLibrary` is `export const HumbleLibrary = { ... }` at `src/backend/humble/library.ts:1353`
  — an **object literal**, not a class. Import it as a named value.

**The circular-dependency fix IS required in this new module.** `humble/library.ts:41-42` statically
imports `steamLibraryStore` from `backend/storeManagers/steam/electronStores` and `SteamUser` from
`backend/storeManagers/steam/user`. That is the same `storeManagers/steam/*` reach that made the fix
load-bearing in `steamAuthFlowRegistration.ts`. Copy the idiom from
`steamAuthFlowRegistration.ts:28-42`: `import { ipcMain } from './electronStub'` first, then
`import '../storeManagers'` with its explanatory comment, then the direct `humble/*` imports. The
fix is **per-file, not once-per-bundle** — each curated registration module is its own independent
entry point into the bundle's module graph, and the failure it prevents
(`SteamLibraryManager is not a constructor`) is esbuild-bundle-only, so ts-jest passing proves
nothing about it.

Wiring site — `src/backend/sidecar/handlers.ts`: imports at L50-67 (existing pattern:
`import { registerClipboardFlows } from './clipboardFlowRegistration'` at L63,
`import { registerLoggerFlows } from './loggerFlowRegistration'` at L64); call sites at L86-152, in
order `registerSteamFlows()` L86 … `registerShellFilesFlows()` L128, `registerClipboardFlows()` L136,
`registerLoggerFlows()` (~L146), then `ensureStoresRegistered()` then `registerStoreWriteHandlers()`.
Place `registerHumbleFlows()` alongside the other `register*Flows()` calls and **before**
`ensureStoresRegistered()`. There is no ordering constraint among the `register*Flows()` calls — they
own distinct channel names and have no cross-module runtime dependency at registration time — but
record that in a comment the way L128-145's existing comments do. Also extend `handlers.ts`'s
module docstring (L8-25 enumerates each registration module) with a Humble entry.

**All 9 Humble stores are already registered** at `storeRegistration.ts:88-97` (wired L151-155,
L177-185) — zero new store plumbing.

**Reach-ledger note (do not edit the ledger here — plan 34.4-08 owns that file):** this module's
import graph reaches three electron-importing modules — `humble/user.ts:1`
(`import { safeStorage, session } from 'electron'`), `humble/adapter.ts:2`
(`import { net } from 'electron'`), and `humble/expirationAlerts.ts:1`
(`import { Notification } from 'electron'`, reached two hops via `humble/library.ts:22`). Expect
`electronReachLedger.test.ts` to go RED after this plan until plan 34.4-08 regenerates the baseline.
Record that expected-red in your SUMMARY; do not "fix" it by editing the ledger.

**Test scaffold to mirror** — `src/backend/sidecar/__tests__/steamAuthFlows.test.ts` in full: its
mock kit (`jest.mock('electron', () => jest.requireActual('../electronStub'))` L64,
`jest.mock('electron-store', ...)` L65, `jest.mock('axios', ...)` L76,
`jest.mock('backend/constants/environment', ...)` L98) and its helpers `collectFrames` (L124),
`flush` (L147), `startSidecar` (L154), `writeInvoke` (L163). Copy `writeSend` from
`settingsFlows.test.ts:249`. `steamAuthFlows.test.ts` predates the global containment fix and carries
its own `jest.mock('os', ...)` — **a new file does not need it**: `src/backend/jest.setupContainment.ts`
runs via `setupFiles` for every backend test file and redirects HOME/APPDATA/XDG structurally
(Phase 34.2 gap cycle 4). Never disable or bypass it.

`src/backend/sidecar/__tests__/testContainment.test.ts` maintains a
`STRUCTURALLY_CONTAINED_SUITES` classification list. Plan 34.3-06 set the precedent: a new sidecar
suite using the same os/electron/electron-store mock kit gets classified there. Check whether the
new file needs an entry and add it if so.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create humbleFlowRegistration.ts with the 10 clean channels, and wire it into handlers.ts</name>
  <files>src/backend/sidecar/humbleFlowRegistration.ts, src/backend/sidecar/handlers.ts</files>
  <read_first>
    - `src/backend/sidecar/steamAuthFlowRegistration.ts` — the shape template: module docstring structure ("what this registers / what it deliberately does NOT / why"), the L28-42 circular-dep-fix comment block to adapt, and the `registerXFlows()` export shape.
    - `src/backend/humble/ipc_handler.ts` — the whole file (128 lines). This is the parity source for every body. Read it to copy bodies from; **never import it**.
    - `src/backend/humble/library.ts` L1-60 and L1353-1400 — the import block (confirming the `storeManagers/steam/*` reach at L41-42 and the `./expirationAlerts` import at L22) and the `HumbleLibrary` object's exact method names.
    - `src/backend/humble/user.ts` L1-10 and L136-160 — the electron import at L1 and `HumbleUser`'s class/static-method shape.
    - `src/backend/sidecar/handlers.ts` — the whole file. Read the L8-25 module docstring, the L50-67 import block, and the L86-152 call-site region with its per-module explanatory comments.
    - `src/backend/sidecar/storeRegistration.ts` L85-100 — confirm all 9 Humble stores are already registered, so no store plumbing is added.
  </read_first>
  <action>
**Create `src/backend/sidecar/humbleFlowRegistration.ts`** exporting `registerHumbleFlows(): void`.

Import order is load-bearing and must be exactly: `import { ipcMain } from './electronStub'`; then
`import '../storeManagers'` carrying an adapted version of `steamAuthFlowRegistration.ts:28-42`'s
comment (state the concrete reason it is needed HERE: `humble/library.ts:41-42` statically imports
`steamLibraryStore` and `SteamUser` from `backend/storeManagers/steam/*`, so this module is another
independent entry point into the same eager-`libraryManagerMap` construction hazard, and the fix is
per-file); then `import { HumbleUser } from '../humble/user'` and
`import { HumbleLibrary } from '../humble/library'`.

Use static top-level ESM imports only — a synchronous `require()` of a relative or alias path
survives the build as an unresolvable literal (`sync-require-alias-unresolved-in-build`).

Write a module docstring in `steamAuthFlowRegistration.ts`'s three-part shape:
1. What this registers (Phase 34.4, REQ-34.4-07), naming each channel with its
   `humble/ipc_handler.ts:LINE` citation and its kind.
2. What it **deliberately does NOT** register and why: the six channels Phase 34.4.1 owns —
   `humbleStartLogin` (L21), `humbleReconnect` (L23), `humbleGetLoginUserAgent` (L25),
   `humbleRevealKey` (L102), `humbleStopLogin` (L126), `humbleLoginNavigated` (L127) — which stay
   unregistered on the sidecar and keep rejecting non-fatally with `UNPORTED_CHANNEL_MARKER` per
   SEAM.md Load-Bearing Invariant B.
3. Why it curated-imports `humble/user.ts` and `humble/library.ts` directly and **must never**
   side-effect-import `humble/ipc_handler.ts`: that file registers all 21 channels as an import
   side effect, so importing it would claim the six 34.4.1 owns and drag `backend/ipc` in.
   State also that plan 34.4-05 adds the remaining six 34.4-owned channels to this same file.

Then add the 10 `ipcMain.handle` registrations, each reproducing its `ipc_handler.ts` body verbatim
in behavior. Cross-check each against that file's own `addHandler` call before writing it — a
`handle`/`send` mismatch fails 100% silently (`sidecar-send-channels-fail-silently`, Phase 31
Pitfall 2). The three `params`-taking channels (`humbleMarkRedeemed`, `humbleUndoRedeemed`,
`humbleGetRevealedKeyValue`) each destructure `gamekey` and `machineName` off the single transport
arg and pass them as two positional arguments, in that order.

Type transport args as `unknown` and narrow at the boundary rather than asserting a renderer-supplied
type blindly. **Never log a key value** — `humbleGetRevealedKeyValue` is the project's declared C4
narrow-exposure channel (`ipc_handler.ts:96-101`: the ONLY channel that ever transmits a raw key
value, on explicit on-demand read only, never broadcast). Add no logging to it, and register none of
these channels on any generic frontend `storeGet` bridge (WR-09/T-11-02).

**Wire into `handlers.ts`:** add `import { registerHumbleFlows } from './humbleFlowRegistration'`
alongside the L50-67 imports, add a `registerHumbleFlows()` call site alongside the other
`register*Flows()` calls and **before** `ensureStoresRegistered()`, with a comment in the style of
L128-145 recording the phase/plan/REQ, the channel group, and that there is no ordering constraint
relative to the other registration calls. Extend `handlers.ts`'s L8-25 module docstring with a Humble
entry matching the existing entries' format.

**Do not** create any new Rust arm — `git diff --stat src-tauri/` must be empty. **Do not** edit
`electronReachLedger.test.ts`; plan 34.4-08 owns it and the RED it goes on this plan is expected.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit && npx prettier --check src/backend/sidecar/humbleFlowRegistration.ts src/backend/sidecar/handlers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/sidecar/humbleFlowRegistration.ts` exists and exports `registerHumbleFlows`
    - `grep -c "ipcMain.handle(" src/backend/sidecar/humbleFlowRegistration.ts` returns 10, and `grep -c "ipcMain.on(" ...` returns 0
    - All ten channel names appear as `ipcMain.handle('<name>'` string matches
    - `grep -c "ipc_handler" src/backend/sidecar/humbleFlowRegistration.ts` counts only docstring/comment mentions — no `import` statement referencing `humble/ipc_handler` exists (verify with `grep -n "^import" src/backend/sidecar/humbleFlowRegistration.ts`)
    - None of `humbleStartLogin`, `humbleReconnect`, `humbleGetLoginUserAgent`, `humbleRevealKey`, `humbleStopLogin`, `humbleLoginNavigated` appears inside an `ipcMain.handle(` or `ipcMain.on(` call in the new file
    - `import '../storeManagers'` appears in the new file, above the `humble/*` imports and below the `./electronStub` import, with a comment naming `humble/library.ts:41-42` as the concrete reason
    - `grep -c "require(" src/backend/sidecar/humbleFlowRegistration.ts` returns 0
    - `handlers.ts` contains both `import { registerHumbleFlows }` and a `registerHumbleFlows()` call, and the call appears before `ensureStoresRegistered()` in file order
    - `git diff --stat src-tauri/` produces no output
    - `npx tsc --noEmit` and `npx prettier --check` on both files exit 0
  </acceptance_criteria>
  <done>A curated Humble registration module exists, is wired into the sidecar, registers exactly the 10 clean channels, and structurally cannot register the 6 channels Phase 34.4.1 owns.</done>
</task>

<task type="auto">
  <name>Task 2: Create humbleFlows.test.ts with transport, kind, and negative-scope coverage</name>
  <files>src/backend/sidecar/__tests__/humbleFlows.test.ts, src/backend/sidecar/__tests__/testContainment.test.ts</files>
  <read_first>
    - `src/backend/sidecar/__tests__/steamAuthFlows.test.ts` — the full-real-shim test pattern to mirror: mock kit, `collectFrames`/`flush`/`startSidecar`/`writeInvoke` helpers, and the `it('REQ-... ...')` naming convention.
    - `src/backend/sidecar/__tests__/settingsFlows.test.ts` L249-257 — the `writeSend` helper's frame shape.
    - `src/backend/sidecar/__tests__/clipboardFlows.test.ts` — plan 34.3-06's negative-scope guard: a registration snapshot taken BEFORE the register call, proving no registration leaked for channels the module must not own. Mirror that technique for the 6 deferred channels.
    - `src/backend/sidecar/humbleFlowRegistration.ts` — your Task 1 output.
    - `src/backend/sidecar/__tests__/testContainment.test.ts` — the `STRUCTURALLY_CONTAINED_SUITES` list and its classification rule.
    - `src/backend/jest.setupContainment.ts` — the structural containment this suite inherits and must not bypass.
  </read_first>
  <action>
Create `src/backend/sidecar/__tests__/humbleFlows.test.ts`. Mirror `steamAuthFlows.test.ts`'s
real-shim, over-the-wire pattern: drive real registered handlers through the sidecar's stdio JSON-RPC
loop with `writeInvoke`/`writeSend`, and assert on response frames — not on internal function calls
alone. Automock `../../humble/user` and `../../humble/library` (their own logic is already covered by
`src/backend/humble/__tests__/`); this suite proves **registration and transport**, not Humble logic.

Do **not** copy `steamAuthFlows.test.ts`'s `jest.mock('os', ...)` block — that file predates
`jest.setupContainment.ts`, which now provides HOME/APPDATA/XDG redirection structurally for every
backend suite. Do not disable or bypass that setup file.

Coverage required (all under `REQ-34.4-07`-prefixed `it(...)` titles):

1. **Per-channel round-trip, all 10.** Each resolves the automocked return value and is NOT
   `UNPORTED_CHANNEL_MARKER`. Assert the delegate was called exactly once.
2. **Argument fidelity for the three `params` channels.** `humbleMarkRedeemed`,
   `humbleUndoRedeemed` and `humbleGetRevealedKeyValue` must each receive `gamekey` first and
   `machineName` second as separate positional arguments. Use distinguishable values so a
   transposition fails — identical placeholders would let a transposed call pass.
3. **Kind assertion, per channel.** Each of the 10 must be reachable as an **invoke** and must NOT
   behave as a listener-only registration. A `handle`/`send` mismatch is 100% silent at runtime, so
   assuming the kind is exactly the defect class this suite exists to catch.
4. **Negative-scope guard (the load-bearing one).** Snapshot the registered-channel set BEFORE
   calling `registerHumbleFlows()`, call it, then assert that none of `humbleStartLogin`,
   `humbleReconnect`, `humbleGetLoginUserAgent`, `humbleRevealKey`, `humbleStopLogin`,
   `humbleLoginNavigated` became registered. Additionally drive an invoke for `humbleRevealKey`
   over the wire and assert it still rejects non-fatally with `UNPORTED_CHANNEL_MARKER` and that the
   RPC loop keeps serving (a following `health` invoke responds) — SEAM.md Invariant B for the six
   channels Phase 34.4.1 owns.
5. **Curated-import guard.** Assert by source inspection that `humbleFlowRegistration.ts` has no
   import statement referencing `humble/ipc_handler`. Read the file's source in the test and apply
   `src/backend/testUtils/stripSourceComments.ts` **first** so a docstring mentioning
   `humble/ipc_handler.ts` (which the module legitimately contains) cannot make this gate vacuous —
   this is the exact defect class that took Phase 34.2 four gap cycles. Add a self-test proving the
   gate rejects a synthetic source that DOES import it.

Add the new suite to `testContainment.test.ts`'s `STRUCTURALLY_CONTAINED_SUITES` if its mock kit
matches an already-classified shape (34.3-06's precedent for `clipboardFlows.test.ts`).

**RED proof (mandatory, by hand, then restore):** temporarily add an
`import '../humble/ipc_handler'` line to `humbleFlowRegistration.ts`, confirm the negative-scope
guard AND the curated-import guard both fail, then restore via `git checkout` and confirm
`git diff --stat` on the module is empty against your Task 1 commit. Record the verbatim failure
output in the SUMMARY.

**Expected RED elsewhere:** `electronReachLedger.test.ts` will fail after this plan (three new
electron-reaching modules enter the graph). That is expected and is plan 34.4-08's job — record it,
do not fix it here.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib/src/backend && npx jest sidecar/__tests__/humbleFlows.test.ts sidecar/__tests__/testContainment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/sidecar/__tests__/humbleFlows.test.ts` exists and `npx jest sidecar/__tests__/humbleFlows.test.ts` passes
    - All 10 channels have a round-trip test asserting a real value and not `UNPORTED_CHANNEL_MARKER`
    - The three `params` channels have argument-fidelity tests using distinguishable `gamekey`/`machineName` values that would fail if transposed
    - A negative-scope test snapshots registrations BEFORE `registerHumbleFlows()` and asserts all six 34.4.1 channels stayed unregistered
    - A wire-level test asserts `humbleRevealKey` still rejects with `UNPORTED_CHANNEL_MARKER` and that a subsequent `health` invoke responds
    - The curated-import source gate strips comments via `src/backend/testUtils/stripSourceComments.ts` before asserting, and carries a self-test proving it rejects a synthetic importing source
    - The hand RED proof (adding `import '../humble/ipc_handler'`) is recorded in the SUMMARY with verbatim output showing BOTH guards failing, and the module is restored byte-identical afterwards
    - No `jest.mock('os', ...)` block is copied into the new file, and nothing disables `jest.setupContainment.ts`
    - Full backend `npx jest` shows exactly one newly-red suite — `electronReachLedger.test.ts` — and that expected-red is recorded in the SUMMARY as plan 34.4-08's work
  </acceptance_criteria>
  <done>The 10 clean Humble channels have transport and kind coverage, and a non-vacuous, self-tested guard proves the 6 channels Phase 34.4.1 owns stayed unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar IPC | `humbleGetRevealedKeyValue` transmits a raw product key value across this boundary — the project's declared C4 narrow-exposure channel |
| sidecar → humblebundle.com | `humbleSync` / `humbleCheckHealth` perform axios GETs carrying the stored `_simpleauth_sess` cookie |
| sidecar → the 9 Humble stores | key state, ownership overrides and the encrypted session cookie |
| module graph | `humble/ipc_handler.ts` is a registration side-effect boundary — importing it would claim channels this slice does not own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4-15 | Elevation of Privilege | side-effect-importing `humble/ipc_handler.ts` and registering the 6 channels Phase 34.4.1 owns, violating SEAM.md Invariant B | mitigate | Curated imports of `humble/user.ts` and `humble/library.ts` only, enforced by a comment-stripped source gate with its own self-test, plus a before/after registration-snapshot negative-scope test and a wire-level `UNPORTED_CHANNEL_MARKER` assertion. Hand RED-proved by temporarily adding the forbidden import |
| T-34.4-16 | Information Disclosure | a raw Humble key value leaking into a sidecar log line via `humbleGetRevealedKeyValue` | mitigate | The registration adds no logging at all and is a bare delegate; `ipc_handler.ts:96-101`'s C4/T-14-02 constraint (on-demand read only, never broadcast, never on a generic `storeGet` bridge) is carried across unchanged. Asserted by review that the handler body contains no `console.*`/`log*` call |
| T-34.4-17 | Repudiation | a `handle`/`send` kind mismatch silently dropping a key-state mutation (`humbleMarkRedeemed`/`humbleUndoRedeemed`) | mitigate | All 10 kinds cross-checked against `humble/ipc_handler.ts`'s own `addHandler` calls and asserted per channel in the test, not assumed (`sidecar-send-channels-fail-silently`) |
| T-34.4-18 | Tampering | `gamekey`/`machineName` transposed at the transport boundary, mutating the wrong key's redeemed state | mitigate | Argument-fidelity tests use distinguishable values for the two parameters so a transposition fails; identical placeholders would make the assertion vacuous |
| T-34.4-19 | Information Disclosure | a test run reaching the developer's real `~/Library/Application Support/GameLib` Humble stores (`tests-clobbering-real-steam-store`) | mitigate | `jest.setupContainment.ts` runs structurally via `setupFiles` for every backend suite; `humble/user` and `humble/library` are automocked; the new suite is classified in `testContainment.test.ts` |
| T-34.4-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx prettier --check` on all touched files exits 0
- `cd src/backend && npx jest sidecar/__tests__/humbleFlows.test.ts` passes
- `humbleFlowRegistration.ts` has exactly 10 `ipcMain.handle` calls and zero `ipcMain.on` calls
- No import statement in `humbleFlowRegistration.ts` references `humble/ipc_handler`
- `handlers.ts` imports and calls `registerHumbleFlows()` before `ensureStoresRegistered()`
- `git diff --stat src-tauri/` is empty — zero new Rust arms
- `src/backend/main.ts` and `src/backend/humble/ipc_handler.ts` are byte-unchanged — the Electron build's behavior is untouched
- `electronReachLedger.test.ts` going red is EXPECTED after this plan and is plan 34.4-08's work — record it, do not edit that file here
</verification>

<success_criteria>
- A new curated `humbleFlowRegistration.ts` exists, is wired into `handlers.ts`, and registers exactly 10 channels
- The 6 Phase 34.4.1 channels are proven unregistered by a non-vacuous, self-tested guard
- The `import '../storeManagers'` circular-dep fix is present with a concrete reason
- Zero new Rust, zero new store plumbing
</success_criteria>

<output>
Create `.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-04-SUMMARY.md` when done
</output>
