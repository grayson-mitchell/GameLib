---
phase: 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble
plan: 09
type: execute
wave: 4
depends_on: ["34.4-01", "34.4-02", "34.4-03", "34.4-04", "34.4-05", "34.4-06", "34.4-07", "34.4-08"]
files_modified:
  - .planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-PORTED-CHANNELS.md
  - .planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/ported-channels-gate.py
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
autonomous: true
requirements: [REQ-34.4-14, REQ-34.4-16]
must_haves:
  truths:
    - "All 31 ported channels are declared in one table with their port kind and their HONEST proof level — no reader can take 'wired and unit-proven' for 'seen working'"
    - "Every rider this phase accepted is named explicitly in the document: humbleDisconnect's declared partial and its 34.4.1 revisit note, redeemSteamKey and steamBottleProvision as unit-only, the two GOG-corrected rows, humbleRunValidation's packaged-guard decision, and humbleRecordGiftLinkOpened's corrected handle kind"
    - "SEAM.md's Incremental-Port Checklist steps 5/6 are closed: a new §1 subsection exists for this cluster and §3's deferred item 5 (D-02) is marked closed"
    - "SEAM.md §3 row 3's '<webview> login story — target Phase 34.4' is re-targeted to Phase 34.4.1, since D-01 moved that seam"
    - "The D-01/D-02/D-03/D-07 scope-surgery artifacts are VERIFIED INTACT, not re-done — IPC-PORT-INVENTORY.md's 31/6/57 split and Phase 34.4.1's ROADMAP seed are already correct"
    - "A re-runnable gate script enforces the declared list, and each of its assertions is self-tested against a synthetic violation so the gate cannot be vacuous"
  artifacts:
    - path: ".planning/phases/34.4-.../34.4-PORTED-CHANNELS.md"
      provides: "The declared 31-row ported-channel list with honest proof levels and every accepted rider"
      min_lines: 120
    - path: ".planning/phases/34.4-.../ported-channels-gate.py"
      provides: "A re-runnable, self-tested doc-shape gate covering REQ-34.4-14 and REQ-34.4-16"
      min_lines: 150
  key_links:
    - from: ".planning/phases/34.4-.../ported-channels-gate.py"
      to: ".planning/IPC-PORT-INVENTORY.md"
      via: "the 31 channel names, the 34.4.1 heading and the slice-8 count are asserted intact (REQ-34.4-16)"
      pattern: "Phase 34.4.1"
---

<objective>
Close the phase's documentation obligations: declare all 31 ported channels with honest proof levels,
close SEAM.md's Incremental-Port Checklist steps 5/6, verify (do not re-do) the scope-surgery
artifacts, and enforce all of it with a re-runnable, self-tested gate script.

Purpose: this project has a specific, recorded failure it is defending against — "the conflation that
let G-30-02 be declared fixed twice while the live build hung." Where an automated test proves only
wiring, the artifact must say so plainly. `redeemSteamKey` and `steamBottleProvision` are ported and
unit-proven but **never live-run** (D-08). `humbleDisconnect` is ported but a **declared partial**
(D-05). Two channels were **miscategorized** as Steam and are actually GOG. One channel's `send`/
`handle` kind was **wrong in CONTEXT.md**. Every one of those has to be legible to the next reader
without them having to reconstruct it from four planning documents.

REQ-34.4-16 is a **confirmation gate, not an editing task**: research and the orchestrator both
verified that `IPC-PORT-INVENTORY.md` and Phase 34.4.1's ROADMAP entry already carry the correct
content. It must not be satisfied by rewriting content that is already right.

Output: `34.4-PORTED-CHANNELS.md`, a `ported-channels-gate.py` with anti-vacuity self-tests, and the
SEAM.md §3→§1 move.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-CONTEXT.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-RESEARCH.md
@.planning/IPC-PORT-INVENTORY.md

<interfaces>
<!-- Read directly on 2026-07-27. -->

**Templates to mirror, not reinvent:**
- `.planning/phases/34.3-.../34.3-PORTED-CHANNELS.md` (163 lines). Structure: a header, a
  `## Framing corrections this document owes` section (L20), then `## The 29 channels` (L71) with a
  five-column table `| Channel | Kind | Backed by | Proof level | Notes |` (L73-74). Kind values in
  use: `sidecar send`, `sidecar invoke`. Proof-level values in use: `unit`, `unit + LIVE (item N)`.
- `.planning/phases/34.3-.../ported-channels-gate.py` (305 lines). Structure: a docstring documenting
  the `--self-test` mode (L26); a hardcoded channel-name list sourced from the inventory (L39-42); a
  rider-token list every one of which must appear somewhere in the document (L77-79); a row-leading
  backticked-channel regex deliberately anchored so a channel merely MENTIONED in prose cannot
  satisfy it (L96-99); `check_*` functions (L108-179); an `ASSERTIONS` list (L180) run by
  `run_all_assertions` (L189); and anti-vacuity self-tests (L194-235) that feed a synthetic document
  violating exactly one assertion each **through the same `check_*` functions** used against the real
  document, never a reimplementation. `main()` at L285 dispatches on `--self-test` in `sys.argv`.

**The 31 channels**, exactly as `.planning/IPC-PORT-INVENTORY.md`'s `## Phase 34.4 — Slice 7 — Steam
completion and Humble (31 channels)` section lists them:
`getPrivateBranchPassword`, `getSteamInstallSize`, `getSteamSyncedAt`, `getSteamUserInfo`,
`humbleCheckHealth`, `humbleClearOwnershipOverride`, `humbleDisconnect`, `humbleGetClaimAnnotations`,
`humbleGetGiftedAt`, `humbleGetKeys`, `humbleGetOwnershipOverrides`, `humbleGetRevealedKeyValue`,
`humbleGetSyncState`, `humbleGetUserInfo`, `humbleMarkRedeemed`, `humbleRecordGiftLinkOpened`,
`humbleRunValidation`, `humbleSetOwnershipOverride`, `humbleSync`, `humbleUndoRedeemed`,
`isSteamBottleProvisioned`, `logoutSteam`, `redeemSteamKey`, `setPrivateBranchPassword`,
`steamBottleProvision`, `steamBottleStatus`, `steamClientSetupRecheck`, `steamClientSetupStart`,
`steamPollCredential`, `steamStartCredentials`, `steamSubmitGuard`.

**Kinds (only two `send` channels in the whole slice):** `logoutSteam` and `humbleDisconnect` are
`sidecar send`. **All 29 others are `sidecar invoke`** — including `humbleRecordGiftLinkOpened`, the
corrected one.

**SEAM.md structure** (`.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md`):
- §1 "Ported (real, wired, hardware-provable)" starts L13. Existing per-phase cluster subsections
  follow one heading form, e.g. L322 `### App shell + window chrome cluster (real, Phase 34.1) —
  CLOSED, moved out of §3`, L381 (Phase 34.2), L430 (Phase 34.3). Mirror this form exactly.
- §3 "Deferred (out of scope this phase, the incremental-port backlog)" starts L521, a table from
  L530. **Row 5** (L534) is the login-channel row — deferred item 5, D-02: it records the QR branch
  closed in Phase 30 and lists `steamStartCredentials`/`steamSubmitGuard`/`steamPollCredential`/
  `getSteamUserInfo`/`logoutSteam` as still deferred. **This slice closes exactly those.**
- **Row 3** (L532, `BrowserWindow`) currently says the remaining `<webview>` login story targets
  **"Phase 34.4"**. D-01 moved that seam to the new **Phase 34.4.1** — this row is now wrong and must
  be re-targeted.
- §"Incremental-Port Checklist" starts L693 — steps 5/6 are the declare-and-move obligations.

**REQ-34.4-16 — verify intact, do NOT re-edit.** Already-correct content, confirmed by direct read:
`IPC-PORT-INVENTORY.md` L62 `## Phase 34.4 — Slice 7 — Steam completion and Humble (31 channels)`,
L77 `## Phase 34.4.1 — the embedded-browser login seam (6 channels)` listing
`humbleGetLoginUserAgent`, `humbleLoginNavigated`, `humbleReconnect`, `humbleRevealKey`,
`humbleStartLogin`, `humbleStopLogin`, and L88 `## Phase 34.5 — Slice 8 — non-Steam runners, Wine and
shortcuts (57 channels)` with its "Gained `isLoggedIn` from slice 7" note. `.planning/ROADMAP.md`'s
Phase 34.4.1 entry already carries the full D-07 seed (three candidate mechanisms; the
CSP/`X-Frame-Options` landmine; 34.3 D-02's shared-capability-grant warning; the **unverified** Tauri
webview cookie API; the falsified-fingerprint reveal-POST constraint; the per-platform
WKWebView/WebKitGTK/WebView2 engine risk; the `humbleDisconnect` revisit note).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 34.4-PORTED-CHANNELS.md — 31 rows with honest proof levels and every rider</name>
  <files>.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-PORTED-CHANNELS.md</files>
  <read_first>
    - `.planning/phases/34.3-.../34.3-PORTED-CHANNELS.md` — the full 163-line template: header, framing-corrections section, the five-column table and the kind/proof-level vocabulary.
    - All eight prior SUMMARYs in this phase directory (`34.4-01` … `34.4-08`) — every row's "Backed by" and "Proof level" must reflect what was ACTUALLY built and tested, not what was planned. In particular read 34.4-05's SUMMARY for which path `humbleRunValidation` took, and 34.4-08's for the measured reach-ledger result.
    - `.planning/IPC-PORT-INVENTORY.md` § "Phase 34.4 — Slice 7" — the authoritative 31-name list.
    - `.planning/phases/34.4-.../34.4-VALIDATION.md` § "Manual-Only Verifications" — the five live-gate items, so proof-level cells reference the right item numbers.
  </read_first>
  <action>
Create `34.4-PORTED-CHANNELS.md` following `34.3-PORTED-CHANNELS.md`'s structure.

**Opening prose — the scope surgery, stated plainly.** Open by recording that this slice was
deliberately re-scoped from the inventory's original 38 to 31: `isLoggedIn` moved to Phase 34.5
(D-03, it is `LegendaryUser.isLoggedIn()` — Epic auth, filed here only because the inventory groups
by file), and 6 Humble browser-auth channels moved to the newly-inserted Phase 34.4.1 (D-01/D-02,
because the `<webview>`/`session.fromPartition` seam is shared with Phase 34.5's Epic/GOG/Amazon
logins and does not belong inside a store slice).

**A `## Framing corrections this document owes` section**, mirroring 34.3's. Name all four corrections
this phase made to its own upstream planning documents, since declaring corrections rather than
silently absorbing them is this project's discipline:
1. `humbleRecordGiftLinkOpened` is a `handle`, not a `send` (CONTEXT.md's Discretion section claimed
   otherwise; `humble/ipc_handler.ts:72`, `common/types/ipc.ts:350`, `preload/api/humble.ts:31`).
2. `getPrivateBranchPassword`/`setPrivateBranchPassword` are **GOG**, not Steam
   (`main.ts:1510-1515` → `libraryManagerMap['gog']`).
3. The electron-reach ledger gained **three** new baseline entries, not two — the third,
   `humble/expirationAlerts.ts`, is a two-hop edge via `humble/library.ts:22`. Report the number and
   identity plan 34.4-08 actually MEASURED, and note any discrepancy from this prediction.
4. `settingsFlowRegistration.ts` and `steamAuthFlowRegistration.ts` were **not** already
   `electronReachLedger.test.ts` entry points, contrary to RESEARCH.md; plan 34.4-08 added them.

**The `## The 31 channels` table**, five columns `| Channel | Kind | Backed by | Proof level | Notes |`.
- `Kind` is `sidecar send` for exactly `logoutSteam` and `humbleDisconnect`; `sidecar invoke` for the
  other 29.
- `Proof level` must be **honest**, drawn from what the SUMMARYs actually record: `unit`,
  `unit + LIVE (item N)` where a live-gate item covers it, and `unit only, declared` for the two
  channels D-08 deliberately excludes from the gate. Do not write `unit + LIVE` for anything the gate
  does not actually cover. Plan 34.4-10 runs the gate AFTER this plan, so any `LIVE` cell is a
  forward reference — say so explicitly in the document (34.3-08 hit exactly this and declared its
  live cells PENDING; do the same, and state that plan 34.4-10's SUMMARY is where the result lands).
- `Notes` must carry every accepted rider, per channel:
  - `logoutSteam`: the G-30-01 channel; a `send` whose failure mode produces no reject, no timeout and
    no console line; jest can assert the delegation but not the sign-out — hence live-gate item 2.
  - `humbleDisconnect`: **declared partial (D-05)** — "ported" means the app-side session IS cleared
    (the stored encrypted cookie delete is synchronous, runs first, and is the real security
    boundary); only the Chromium partition wipe no-ops against `electronStub.session`'s accepted
    Phase 29 D-09 stub, against a partition that under Tauri never held anything. **Plus the handoff
    note: once Phase 34.4.1 creates a real browser context, `humbleDisconnect` MUST be revisited to
    clear it too**, or 34.4.1 silently inherits a half-finished sign-out.
  - `redeemSteamKey`: **unit-proven only, declared, never live-redeemed** (D-08 — a redemption is
    irreversible and cannot be re-run on failure). Phase 26's 5 deferred live-key UAT items stay
    deferred and are explicitly NOT folded in. Its WR-03 input-validation trust boundary is ported
    verbatim.
  - `steamBottleProvision`: **unit-proven only, declared, never live-provisioned** (D-08 — it
    downloads a real CrossOver bottle; that belongs in a macOS hardware-UAT pass).
  - `getPrivateBranchPassword`/`setPrivateBranchPassword`: the corrected GOG classification, backed
    by `libraryManagerMap['gog']`, registered in `settingsFlowRegistration.ts`, not a Steam module.
  - `humbleRecordGiftLinkOpened`: the corrected `handle` kind, with its three confirming sources.
  - `humbleRunValidation`: whichever decision plan 34.4-05 recorded — either the resolved packaged
    signal (naming the mechanism) or the explicitly declared divergence (dev-only trigger reachable in
    a packaged Tauri build, mitigated by having no UI entry point). Do not guess; read 34.4-05's
    SUMMARY.
  - `steamBottleStatus`: the one genuinely inline body in the slice, reproduced without duplicating
    bottle logic; reports only `provisioned` + `bottleName` (the always-false `loggedIn` signal was
    removed by 17-17/WR-02, and bottled-Steam auth stays opaque per D-04). Live-gate item 5.

**A closing section recording the Humble Claim Wizard's real Tauri state**, which REQ-34.4-14
explicitly requires: the wizard's Copy button now works under Tauri thanks to Phase 34.3's clipboard
seam — **but only for an already-revealed key**, since `humbleRevealKey` itself is deferred to Phase
34.4.1. Stating this prevents a reader concluding the claim flow is end-to-end functional.

**A section recording D-06 and D-04**, which are not channels but are this phase's deliverables:
`electronStub.net.request`'s hardening (what it emits, why asynchronously, and that Phase 34.4.1 owns
the real seam), and the honest "not available on this build" login panel (what a Tauri user now sees,
and that the Electron branch was proven structurally unreachable rather than asserted).

**Close with the standard honesty rider**: where an automated test proves only wiring, this document
says so plainly — no reader may take "wired and unit-proven" for "seen working", the conflation that
let G-30-02 be declared fixed twice while the live build hung.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && test -f .planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-PORTED-CHANNELS.md && grep -c '^| `' .planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-PORTED-CHANNELS.md</automated>
  </verify>
  <acceptance_criteria>
    - The document contains exactly 31 table rows leading with a backticked channel name, and the set of those names equals the inventory's 31 exactly (no extras, no omissions)
    - Exactly two rows carry the kind `sidecar send`, and they are `logoutSteam` and `humbleDisconnect`
    - `humbleRecordGiftLinkOpened`'s row carries `sidecar invoke`
    - `redeemSteamKey`'s and `steamBottleProvision`'s rows both carry a proof level indicating unit-only/declared, and their notes name D-08
    - `humbleDisconnect`'s note contains both the "the stored encrypted cookie IS deleted" claim and the Phase 34.4.1 revisit obligation
    - A framing-corrections section names all four corrections, with the reach-ledger one reporting the MEASURED result from 34.4-08's SUMMARY, not the prediction
    - `humbleRunValidation`'s note matches the decision recorded in 34.4-05's SUMMARY
    - A section records the Humble Claim Wizard's real Tauri state (Copy works, reveal deferred)
    - A section records D-06's stub hardening and D-04's panel
    - Any `LIVE` proof-level cell is explicitly marked as a forward reference to plan 34.4-10's gate result
  </acceptance_criteria>
  <done>Every ported channel's kind, backing and honest proof level is legible in one place, with every accepted rider named rather than left in a reader's head.</done>
</task>

<task type="auto">
  <name>Task 2: Write a re-runnable, self-tested ported-channels gate script</name>
  <files>.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/ported-channels-gate.py</files>
  <read_first>
    - `.planning/phases/34.3-.../ported-channels-gate.py` — the full 305-line template. Read the docstring's `--self-test` contract, the row-leading regex and WHY it is anchored (L96-99), each `check_*` function, the `ASSERTIONS` list, and especially the self-test section (L194-235): each synthetic document violates exactly one assertion and is fed through the SAME `check_*` functions, never a reimplementation, plus a base-document sanity check that the clean synthetic passes.
    - `.planning/phases/34.4-.../34.4-PORTED-CHANNELS.md` — your Task 1 output, the document being gated.
    - `.planning/IPC-PORT-INVENTORY.md` — the source of truth for the 31/6/57 split the REQ-34.4-16 assertions verify.
  </read_first>
  <action>
Write `ported-channels-gate.py`, adapting 34.3's script rather than inventing a new design. It must
run clean with `python3 ported-channels-gate.py` and prove itself with
`python3 ported-channels-gate.py --self-test`.

**REQ-34.4-14 assertions, against `34.4-PORTED-CHANNELS.md`:**
1. All 31 inventory channel names appear as **row-leading** backticked cells. Keep 34.3's anchored
   regex so a channel merely mentioned in prose can never satisfy this.
2. The table has **exactly 31** such rows — no extras, catching a duplicate or an accidentally-added
   out-of-scope channel.
3. **Negative-scope assertion:** none of the six Phase 34.4.1 channels (`humbleStartLogin`,
   `humbleReconnect`, `humbleStopLogin`, `humbleLoginNavigated`, `humbleGetLoginUserAgent`,
   `humbleRevealKey`) nor `isLoggedIn` appears as a row-leading channel. They may legitimately appear
   in prose (the document explains the split), so the anchoring matters in both directions.
4. Kind correctness: exactly two rows carry `sidecar send`, and they are `logoutSteam` and
   `humbleDisconnect`. This is the assertion that pins the corrected
   `humbleRecordGiftLinkOpened` classification.
5. Rider tokens: a list of required substrings every one of which must appear somewhere in the
   document. Include at minimum tokens for — D-08's unit-only declarations for `redeemSteamKey` and
   `steamBottleProvision`; D-05's `humbleDisconnect` partial AND its Phase 34.4.1 revisit obligation;
   the GOG correction; the `humbleRecordGiftLinkOpened` kind correction; `humbleRunValidation`'s
   packaged-guard decision; the Claim Wizard's reveal-deferred state; and the "wired and unit-proven
   is not seen working" honesty rider. Follow 34.3's own comment convention: this list may be
   extended, never shrunk without also removing the prose it pins.
6. Proof-level vocabulary: every row's proof-level cell matches an allowed set, so a row cannot ship
   with an empty or improvised proof level.

**REQ-34.4-16 assertions, against `.planning/IPC-PORT-INVENTORY.md` — verification, not editing:**
7. The slice-7 heading states 31 channels and its list contains exactly the 31 names.
8. A `## Phase 34.4.1` heading exists and its list contains exactly the six browser-auth channels.
9. The slice-8 heading states 57 channels and its list contains `isLoggedIn`.
These assertions must FAIL if someone later "helpfully" edits the inventory back to 38/56 — that is
the whole point of making REQ-34.4-16 a gate rather than a task.

**Anti-vacuity self-tests are mandatory and are the reason this script is trustworthy.** For EVERY
assertion above, feed a synthetic document (or synthetic inventory text) that violates exactly that
assertion through the **same** `check_*` function used against the real file, and confirm it rejects.
Include 34.3's base-document sanity check: the clean synthetic must pass a full run first, or the
self-tests are measuring nothing. A `check_*` function without a corresponding self-test is not
acceptable — Phase 34.2 spent four gap cycles on exactly this defect class, and its root cause was a
gate that passed against text that only NAMED the pattern.

Both `python3 ported-channels-gate.py` and `python3 ported-channels-gate.py --self-test` must exit 0.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib/.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble && python3 ported-channels-gate.py && python3 ported-channels-gate.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `python3 ported-channels-gate.py` exits 0 against the real `34.4-PORTED-CHANNELS.md`
    - `python3 ported-channels-gate.py --self-test` exits 0 and its output shows one "correctly rejected" line per assertion
    - The count of `check_*` functions equals the count of self-test cases — no assertion lacks a self-test
    - The self-tests call the same `check_*` functions used against the real document, not reimplementations (verify by reading; the base-document sanity check must also be present)
    - The channel-row regex is anchored to the row-leading position, and a self-test proves a prose-only mention does not satisfy it
    - Assertions 7-9 read `.planning/IPC-PORT-INVENTORY.md` and would fail if the 31/6/57 split were reverted — proven by a self-test feeding a synthetic 38-channel inventory
    - The script makes NO edit to `IPC-PORT-INVENTORY.md` or `ROADMAP.md` — `git diff --stat` on both is empty after running it
  </acceptance_criteria>
  <done>The declared list and the scope-surgery artifacts are enforced by a script that has proven, per assertion, that it rejects the corresponding violation.</done>
</task>

<task type="auto">
  <name>Task 3: Close SEAM.md's Incremental-Port Checklist steps 5/6</name>
  <files>.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</files>
  <read_first>
    - `.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` — the whole file, or at minimum: §1's existing per-phase cluster subsections at L322 (34.1), L381 (34.2) and L430 (34.3) for the exact heading form and internal structure to mirror; §3's table from L521, especially **row 3** (L532, `BrowserWindow`, which mis-targets "Phase 34.4") and **row 5** (L534, the login channel / deferred item 5 this slice closes); and the Incremental-Port Checklist at L693.
    - `.planning/phases/34.4-.../34.4-PORTED-CHANNELS.md` — your Task 1 output, which the new SEAM subsection points at rather than duplicates.
    - `.planning/phases/34.4-.../34.4-CONTEXT.md` § canonical_refs — the D-01/D-02 framing for what moved to Phase 34.4.1.
  </read_first>
  <action>
Make three edits to SEAM.md. Mirror the existing subsection shape exactly; do not invent a new one.

**Edit 1 — add a §1 subsection.** Heading:
`### Steam completion and Humble cluster (real, Phase 34.4) — CLOSED, moved out of §3`, placed after
the Phase 34.3 subsection (L430's block) so §1's clusters stay in phase order. Populate it in the
same structure the 34.1/34.2/34.3 subsections use: a short intro naming the slice and the channel
count; the breakdown (13 genuinely-Steam channels, 2 corrected-to-GOG, 16 Humble); the two `send`
channels named explicitly (`logoutSteam`, `humbleDisconnect`) since kind is the recurring hazard in
this sequence; and the accepted-gap riders — `humbleDisconnect`'s declared partial with its 34.4.1
revisit obligation, `redeemSteamKey`/`steamBottleProvision` as unit-proven-only per D-08, and
`humbleRunValidation`'s packaged-guard decision. State the claim level explicitly, as the 34.1 and
34.3 subsections do: this slice ran a **blocking 5-item live gate** (D-08) covering credential login,
`logoutSteam`, user-info/synced-at, Humble sync+keys and bottle-status truthfulness, all under
`tauri:dev` with deliberately no packaged item (D-10, and say WHY: nothing here depends on the
dev-vs-packaged sidecar spawn path). Point at `34.4-PORTED-CHANNELS.md` for the per-row detail rather
than duplicating the table. Note that plan 34.4-10 runs the gate after this plan, so the gate result
is recorded in `34.4-LIVE-GATE.md`.

**Edit 2 — close §3 row 5 (deferred item 5, D-02).** That row currently records the QR branch as
closed in Phase 30 and lists `steamStartCredentials`/`steamSubmitGuard`/`steamPollCredential`/
`getSteamUserInfo`/`logoutSteam` as still deferred. All five are ported by this slice. Update the row
to **fully closed, Phase 34.4**, pointing at the new §1 subsection, in the same "**Fully closed,
Phase N.**" voice rows 2 and 9 already use.

**Edit 3 — re-target §3 row 3 (`BrowserWindow`).** It currently says the remaining `<webview>` login
story (navigation interception, OAuth redirect capture, session/cookie access) targets **"Phase
34.4"**. D-01 moved that seam to the newly-inserted **Phase 34.4.1**, which runs BEFORE Phase 34.5
because 34.5's Epic/GOG/Amazon logins depend on the identical seam. Re-target the row and add the
one-line reason. Leaving it pointing at 34.4 would make SEAM.md — the governing document — assert
something this very phase decided against, which is exactly the stale-invariant failure this slice is
also fixing in `steamAuthFlowRegistration.ts`'s docstring.

**Do not** touch any other §3 row, and do not weaken §"Load-Bearing Invariants" B — every channel not
ported here, including the six Phase 34.4.1 owns, still rejects non-fatally.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && grep -c "Phase 34.4" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md && grep -n "Steam completion and Humble cluster" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</automated>
  </verify>
  <acceptance_criteria>
    - SEAM.md §1 contains a heading matching `### Steam completion and Humble cluster (real, Phase 34.4) — CLOSED, moved out of §3`, placed after the Phase 34.3 subsection
    - That subsection names both `send` channels, the 13/2/16 breakdown, all three accepted riders, and the blocking 5-item live gate with D-10's no-packaged-item reason
    - §3's login-channel row (deferred item 5) now reads as fully closed by Phase 34.4 and points at the new §1 subsection
    - §3's `BrowserWindow` row no longer targets "Phase 34.4" for the `<webview>` login story; it targets Phase 34.4.1 with a stated reason
    - `git diff .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` shows changes confined to the new §1 subsection and §3 rows 3 and 5 — no other row and no invariant text is altered
    - Load-Bearing Invariant B's text is byte-unchanged
  </acceptance_criteria>
  <done>SEAM.md's Incremental-Port Checklist steps 5/6 are closed for this slice, deferred item 5 is retired, and the governing document no longer mis-targets the `<webview>` seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared artifact → future reader | the only durable record of what "ported" actually means per channel; an inflated claim here becomes an invisible defect later |
| this phase → Phase 34.4.1 and Phase 35 | the inventory split and the SEAM row targets are the scope contract those phases inherit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4-40 | Repudiation | a proof level inflated from "unit-proven" to "working", the exact conflation that let G-30-02 be declared fixed twice while the live build hung | mitigate | Every row's proof level is drawn from what the SUMMARYs actually record; `redeemSteamKey` and `steamBottleProvision` carry explicit unit-only/declared levels; the honesty rider is a gate-enforced rider token; `LIVE` cells are marked as forward references to plan 34.4-10 |
| T-34.4-41 | Repudiation | a vacuous gate script — an assertion that passes against text merely NAMING the pattern (the defect that cost Phase 34.2 four gap cycles) | mitigate | Every `check_*` function has a matching self-test feeding a synthetic violation through the same function, plus a base-document sanity check; the channel regex is anchored to the row-leading position with its own prose-only-mention self-test |
| T-34.4-42 | Tampering | a future edit silently reverting the 31/6/57 inventory split, re-absorbing Phase 34.4.1's channels or `isLoggedIn` | mitigate | Assertions 7-9 read the inventory directly and fail on a reverted split, proven by a self-test feeding a synthetic 38-channel inventory. REQ-34.4-16 is deliberately a gate, not an editing task |
| T-34.4-43 | Repudiation | `humbleDisconnect`'s partial being read as complete, so Phase 34.4.1 silently inherits a half-finished sign-out | mitigate | The D-05 partial AND the explicit 34.4.1 revisit obligation are both gate-enforced rider tokens, and both appear in the SEAM §1 subsection as accepted-gap riders |
| T-34.4-44 | Tampering | SEAM.md — the governing document — continuing to assert the `<webview>` login story targets Phase 34.4 after D-01 moved it | mitigate | §3 row 3 is re-targeted to Phase 34.4.1 with a stated reason; a stale governing-document invariant is the same failure class this slice fixes in `steamAuthFlowRegistration.ts`'s docstring |
| T-34.4-45 | Tampering | REQ-34.4-16 satisfied by rewriting already-correct content, destroying the D-07 seed | mitigate | The gate script asserts the content is intact and makes no edit; `git diff --stat` on `IPC-PORT-INVENTORY.md` and `ROADMAP.md` must be empty after running it |
| T-34.4-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages; the gate script uses only the Python standard library |
</threat_model>

<verification>
- `34.4-PORTED-CHANNELS.md` exists with exactly 31 row-leading channel rows matching the inventory
- `python3 ported-channels-gate.py` and `python3 ported-channels-gate.py --self-test` both exit 0
- Every `check_*` function has a matching self-test; the base-document sanity check is present
- SEAM.md has the new §1 subsection, a closed deferred item 5, and a re-targeted `BrowserWindow` row
- `git diff --stat .planning/IPC-PORT-INVENTORY.md .planning/ROADMAP.md` produces no output — REQ-34.4-16 is verified, not re-done
- No source file is touched by this plan (`git diff --stat src/ src-tauri/` is empty)
</verification>

<success_criteria>
- All 31 channels declared with honest proof levels and every accepted rider named
- A self-tested, re-runnable gate enforces both the declared list and the scope-surgery artifacts
- SEAM.md's steps 5/6 closed, deferred item 5 retired, `BrowserWindow` row re-targeted to 34.4.1
- The already-correct inventory and ROADMAP seed are left byte-unchanged
</success_criteria>

<output>
Create `.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-09-SUMMARY.md` when done
</output>
