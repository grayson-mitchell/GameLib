# Phase 34.4: Tauri IPC re-plumb slice 7 — Steam completion and Humble - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-27
**Phase:** 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble
**Areas discussed:** Slice scope + reassignments, The Humble login surface, The key-reveal network seam, Sign-off — live gate or unit-only?

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| The Humble login surface | 34.1 D-12 parked the `<webview>` story here; `electronStub.session.fromPartition` is a logged no-op | ✓ |
| The key-reveal network seam | `humbleRevealKey` POSTs through Electron `net.request` because Cloudflare 403s axios's TLS fingerprint | ✓ |
| Slice scope + reassignments | Does the Humble browser-auth cluster stay? Does `isLoggedIn` move to 34.5? | ✓ |
| Sign-off — live gate or unit-only? | 34.3's blocking gate caught 2 defects jest missed; this slice needs real accounts | ✓ |

**User's choice:** all four.

---

## Slice scope + reassignments

### Where should the embedded-browser login seam be built?

| Option | Description | Selected |
|--------|-------------|----------|
| Split it into its own phase | 34.4 ports 17 Steam + the non-Chromium Humble channels; browser-auth cluster carves into a new phase that also unblocks 34.5's three logins | ✓ |
| Build it here, all 38 ship in 34.4 | Slice 7 owns the whole login story; 34.5 inherits a working seam. Complete, but the largest platform-work slice yet | |
| Port all 38 at parity, declare login degraded | Fastest, closes the Phase 35 channel gate — but ships structurally dead channels | |

**User's choice:** Split it into its own phase.
**Notes:** Rejected options carried into CONTEXT.md D-01 with rationale. The deciding argument was that `WebView/index.tsx:467` renders ONE `<webview>` serving Humble, Epic, GOG and Amazon — so the seam is cross-cutting and 34.5 would hit the identical wall.

### Where exactly does the split line fall for `humbleDisconnect` and `humbleRevealKey`?

| Option | Description | Selected |
|--------|-------------|----------|
| Disconnect stays, RevealKey moves | 15 ported / 6 deferred; disconnect's real job (deleting the stored cookie) works in the sidecar | ✓ |
| Both move with the login cluster | Cleanest boundary, but a Tauri user then cannot sign out of Humble | |
| Both stay in 34.4 | Solves Cloudflare now, independently of login — but drags the hardest problem into a slice just narrowed | |

**User's choice:** Disconnect stays, RevealKey moves.
**Notes:** Channel accounting corrected afterwards — the inventory's Humble set is 22, not 21 (`humbleUndoRedeemed` was missed on the first count), so the final split is 16 Humble ported / 6 deferred.

### `isLoggedIn` reassignment

| Option | Description | Selected |
|--------|-------------|----------|
| Reassign to 34.5 | Follows 34.1 D-14's `callTool` precedent; requires an IPC-PORT-INVENTORY edit | ✓ |
| Keep it in 34.4 | One-line delegation, cheap to port — but lands an Epic channel in the Steam slice | |

**User's choice:** Reassign to 34.5.

### How should the carved-out seam be tracked?

| Option | Description | Selected |
|--------|-------------|----------|
| Insert Phase 34.4.1 now, before 34.5 | The deferral gets a real destination; placement before 34.5 matters because 34.5's logins depend on the same seam | ✓ |
| Insert Phase 34.4.1 after 34.5 | One consolidated "all embedded logins" phase; costs a second deferral round in 34.5 | |
| Record as a deferred idea only | Lightest touch, but leaves the Phase 35 gate with 6 unported channels and no owner | |

**User's choice:** Insert Phase 34.4.1 now, before 34.5.

---

## The Humble login surface

### What should a Tauri user see at the Humble login screen before 34.4.1?

| Option | Description | Selected |
|--------|-------------|----------|
| An honest "not available on this build" surface | Replace the blank `<></>` return on the `isTauri() && !webviewPreloadPath` branch | ✓ |
| Leave the blank screen, declare it | Purely a backend port — but a blank screen with no diagnostic is the shape 34.3 D-01 called out | |
| Hide the Humble login entry point under Tauri | Cleanest UX, but hides a shipped feature behind a build check Phase 35 must unwind | |

**User's choice:** An honest "not available on this build" surface.
**Notes:** Rider recorded in D-04 — the branch is structurally unreachable in Electron, and that must be proven in test rather than asserted.

### How much of 34.4.1's login mechanism should 34.4 pre-commit?

| Option | Description | Selected |
|--------|-------------|----------|
| Record candidates + landmines, decide nothing | Seed 34.4.1 with the three candidates and the known landmines; its research picks the winner | ✓ |
| Pre-commit to the second-window approach | Fastest — but rests on an unverified Tauri cookie API, and 34.3 D-05 is the standing lesson | |
| Record nothing — leave it fully open | Minimal coupling, but throws away scouting already paid for | |

**User's choice:** Record candidates + landmines, decide nothing.

### `humbleDisconnect`'s no-op partition wipe

| Option | Description | Selected |
|--------|-------------|----------|
| Port it, declare the partial precisely | The stored encrypted cookie IS deleted — the real security boundary; only the partition wipe no-ops | ✓ |
| Port it and make the stub call fail loudly | More honest in principle — but under Tauri there is no browser session to survive, so the warning would be false | |
| Defer it to 34.4.1 after all | Cleaner boundary, but a Tauri user cannot sign out of Humble at all | |

**User's choice:** Port it, declare the partial precisely.

---

## The key-reveal network seam

**Correction issued during discussion:** the initial scout characterised `humblePostRequest` against the sidecar stub as hanging forever. It does not — `adapter.ts:277-287` arms its own `REQUEST_TIMEOUT_MS` and rejects. The real problem is a *misleading* rejection cause.

### Does 34.4 harden `electronStub.net.request`?

| Option | Description | Selected |
|--------|-------------|----------|
| Make it fail fast and legibly | Emit `'error'` asynchronously naming the missing seam; `humblePostRequest` already attaches `request.on('error')` | ✓ |
| Leave it, declare it in 34.4.1's seed | Zero risk — but leaves a booby trap in a file this slice already edits | |
| Remove `net.request` from the stub entirely | Maximally obvious — but the shape `electronStub.session`'s docstring rejected, and how `sidecar-dialog-reject-crashes` happened | |

**User's choice:** Make it fail fast and legibly.

### What should 34.4 record for 34.4.1 about the reveal POST?

| Option | Description | Selected |
|--------|-------------|----------|
| The constraint plus the per-platform engine risk | Rounds 1–6 falsified cookie/header fidelity; plus WKWebView/WebKitGTK/WebView2 are three fingerprints and macOS is furthest from what works | ✓ |
| Record the constraint only | Shorter seed — but the platform split is the most likely late surprise | |
| Point at the debug session, summarize nothing | Artifacts are thorough — but a bare pointer is how 34.2's defect went four cycles | |

**User's choice:** The constraint plus the per-platform engine risk.

---

## Sign-off — live gate or unit-only?

### What sign-off does 34.4 require?

| Option | Description | Selected |
|--------|-------------|----------|
| Blocking gate, ~5 items, nothing irreversible | Carries 34.3 D-11/D-13, scoped to reversible items; `redeemSteamKey` and `steamBottleProvision` stay unit-proven | ✓ |
| Blocking gate including a live key redemption | Retires Phase 26's deferred UAT — but burns a key irreversibly and can't be re-run | |
| Unit-proven only, carry 34.1 D-15 forward | Cheapest, and most channels have assertable return shapes — but leaves `logoutSteam`'s known silent-send failure unproven | |

**User's choice:** Blocking gate, ~5 items, nothing irreversible.
**Notes:** The deciding evidence was `logoutSteam` = the G-30-01 channel, whose `send` failure mode is structurally invisible to assertions.

### The Humble gate items need an Electron-captured cookie

| Option | Description | Selected |
|--------|-------------|----------|
| Name it explicitly as a gate precondition | "Log in under `npm start` FIRST, then relaunch under `tauri:dev`" as a numbered setup step | ✓ |
| Drop the Humble items from the live gate | Simpler gate — but the whole Humble port would ship with zero live evidence | |
| Verify Humble against a hand-seeded cookie | Repeatable — but tests the store read, not the end-to-end path | |

**User's choice:** Name it explicitly as a gate precondition.

### Gate build split

| Option | Description | Selected |
|--------|-------------|----------|
| `tauri:dev` for all five items | Nothing here depends on the dev-vs-packaged spawn path; record WHY so the omission reads as a decision | ✓ |
| Add one packaged-build item | More coverage — at the cost of a full package cycle per gate re-run | |

**User's choice:** `tauri:dev` for all five items.

---

## Claude's Discretion

Recorded in CONTEXT.md `<decisions>` → "Claude's Discretion":
- Extraction shape for the 15 `main.ts` Steam registrations (almost none needed).
- Whether the credential-login trio extends `steamAuthFlowRegistration.ts` or gets its own
  module — but that file's now-false docstring must be updated either way.
- Wording and placement of the D-04 frontend panel.
- The curated-import discipline's application to `humble/ipc_handler.ts` (which registers 6
  channels this slice must NOT register).
- Send-vs-handle cross-check for `logoutSteam`, `humbleDisconnect`, `humbleRecordGiftLinkOpened`.
- Standing rider: expect ZERO new Rust arms; stop and ask if one appears necessary.

## Deferred Ideas

- **Phase 34.4.1** — the embedded-browser login seam + the 6 deferred Humble channels, with the
  full candidate/landmine seed (D-07).
- `isLoggedIn` → Phase 34.5.
- Phase 26's 5 deferred live-key UAT items — not folded into this gate.
- Live `steamBottleProvision` — a macOS hardware-UAT concern, not a port slice's gate.
- `cargo check` / `cargo test` in CI — carried unchanged from 34.3.
- The other Electron-only frontend webview surfaces (`Sidebar`, `WebviewControls`).
- Live cross-build divergence / convergence — Phase 35.

### Reviewed todos (not folded)
Five surfaced; none folded. Two scored 0.9 and are genuine subject-matter matches rather than
the keyword noise Phases 30–34.3 saw — the macOS Steam-bridge productionization todo, and the
GPTK/Wine broken-bottle todo (flagged to whoever runs gate item 5).
</content>
