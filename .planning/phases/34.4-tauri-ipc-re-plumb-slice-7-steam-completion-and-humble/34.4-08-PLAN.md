---
phase: 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble
plan: 08
type: execute
wave: 3
depends_on: ["34.4-01", "34.4-02", "34.4-03", "34.4-04", "34.4-05", "34.4-06", "34.4-07"]
files_modified:
  - src/backend/sidecar/__tests__/electronReachLedger.test.ts
autonomous: true
requirements: [REQ-34.4-10, REQ-34.4-15]
must_haves:
  truths:
    - "electronReachLedger.test.ts's ENTRY_POINTS covers this slice's whole ported surface, so the committed baseline remains a trustworthy Phase 35 cutover work-list"
    - "BASELINE_ELECTRON_REACHING_MODULES is REGENERATED by actually running computeElectronReach(), never transcribed from planning-time prose — the ledger's own explicit rule"
    - "humble/user.ts, humble/adapter.ts and humble/expirationAlerts.ts appear in the measured set; the third is a genuine two-hop edge (humbleFlowRegistration.ts -> humble/library.ts:22 -> expirationAlerts.ts) that a depth-1 regex could not see"
    - "The growth-only/subset semantics, the anti-vacuity requiredModules list and the reachability-sanity floor are preserved, never weakened"
    - "The additive/reversible invariant holds behaviorally: the full backend suite, tsc --noEmit and cargo check are all green, and electronUntouched.test.ts is unweakened"
  artifacts:
    - path: "src/backend/sidecar/__tests__/electronReachLedger.test.ts"
      provides: "The extended entry-point list and the regenerated baseline"
      contains: "humbleFlowRegistration"
  key_links:
    - from: "src/backend/sidecar/__tests__/electronReachLedger.test.ts"
      to: "src/backend/sidecar/humbleFlowRegistration.ts"
      via: "ENTRY_POINTS entry, whose traversal surfaces the three new electron-importing modules"
      pattern: "humbleFlowRegistration"
---

<objective>
Extend the electron-reach ledger to cover this slice's ported surface, and regenerate its baseline by
**running the traversal** — then prove the phase's additive/reversible invariant structurally.

Purpose: the committed baseline IS the Phase 35 cutover work-list. That only holds if the entry-point
set actually covers everything the sidecar registers. This slice adds one brand-new registration
module and extends two existing ones, and the Humble half pulls three electron-importing modules into
the graph — one of which (`humble/expirationAlerts.ts`) is a **two-hop** edge that CONTEXT.md
undercounted and that a depth-1 scan structurally cannot see. The ledger's own docstring exists to
record exactly that limitation.

The file's explicit rule is "regenerate at execution time, not from planning-time prose". Phase 34.3
Plan 07 followed it by adding a temporary measurement print statement, capturing the real output, and
removing the statement. Do the same. The three module names below are this phase's research finding
and should match what you measure — but if the measurement disagrees, **the measurement wins** and the
discrepancy goes in your SUMMARY.

Output: an accurate ledger, plus the phase's structural green sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-CONTEXT.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-RESEARCH.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-04-SUMMARY.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-05-SUMMARY.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-07-SUMMARY.md

<interfaces>
<!-- Read directly from src/backend/sidecar/__tests__/electronReachLedger.test.ts on 2026-07-27. -->

Current `ENTRY_POINTS` (L44-56) — **seven** entries, exactly:
`src/backend/gamedetails/dispatch.ts`, `src/backend/gamedetails/overrides.ts`,
`src/backend/sidecar/gameDetailsFlowRegistration.ts`,
`src/backend/sidecar/enrichmentFlowRegistration.ts`,
`src/backend/sidecar/shellFilesFlowRegistration.ts`,
`src/backend/sidecar/clipboardFlowRegistration.ts`,
`src/backend/sidecar/loggerFlowRegistration.ts`.

**CORRECTION to 34.4-RESEARCH.md, verified at plan time.** RESEARCH.md states that
`settingsFlowRegistration.ts` is "already one of `electronReachLedger.test.ts`'s `ENTRY_POINTS`
(added in Phase 34.2)". It is **not** — see the seven-entry list above. Neither is
`steamAuthFlowRegistration.ts`. Both are extended by this slice (plans 34.4-01/02/03), so both are
uncovered surface today. Do not carry RESEARCH.md's claim forward.

`BASELINE_ELECTRON_REACHING_MODULES` begins at L73 with a comment block recording that Phase 34.3
Plan 07 regenerated it at execution time (2026-07-26), gaining exactly one module
(`src/backend/logger/uploader.ts`), and noting the accepted 192-vs-194 total-file-count discrepancy
against that phase's planning-time note. Entries are sorted, repo-relative strings.

Measurement machinery: `computeElectronReach(ENTRY_POINTS, options)` is defined at L202 and called at
L269 into `reachResult`. Anti-vacuity guards: `expect(measured.size).toBeGreaterThan(0)` at L293; a
`requiredModules` list at L295 iterated at L307 (the anti-degradation list — every named module must
still be measured); and a reachability-sanity floor `expect(reachResult.visitedFiles.size)
.toBeGreaterThan(150)` at L317, raised from 100 by Phase 34.3 Plan 07 with the then-measured value
(202) recorded in a comment.

**The semantics that must not be weakened:** the ledger enforces a **growth-only / subset** property —
the measured electron-importing set must be a subset of the committed baseline. Extra baseline entries
never cause a false failure; a NEW measured module not in the baseline does. `electronImportingFiles`
means files with a **direct bare `'electron'` specifier**, not a transitive-reach measure — which is
why the four Steam modules (`steam/user.ts`, `steam/bottle.ts`, `steam/clientSetup.ts`,
`steam/constants.ts`) contribute nothing new despite being newly reached.

**The three new edges this phase's research identified by direct import-line read (2026-07-27):**
- `src/backend/humble/user.ts:1` — `import { safeStorage, session } from 'electron'`
- `src/backend/humble/adapter.ts:2` — `import { net } from 'electron'`
- `src/backend/humble/expirationAlerts.ts:1` — `import { Notification } from 'electron'`, reached
  **two hops**: `humbleFlowRegistration.ts` → `humble/library.ts:22`
  (`import { detectAndNotifyExpirationTransitions } from './expirationAlerts'`) → `expirationAlerts.ts`.
  CONTEXT.md's D-10 section named only the first two.

Research Assumption A3, carried forward: `steam/bottle.ts` is already present in the baseline despite
a direct grep finding no bare `'electron'` import in it. Treated as harmless stale cruft — the subset
check can never fail on an extra baseline entry. Do not remove it as part of this plan; if your
regeneration confirms it is genuinely absent from the measured set, note that in the SUMMARY as an
observation, not a change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ENTRY_POINTS and regenerate the baseline by running the traversal</name>
  <files>src/backend/sidecar/__tests__/electronReachLedger.test.ts</files>
  <read_first>
    - `src/backend/sidecar/__tests__/electronReachLedger.test.ts` — the whole file (~335 lines). In particular: the L1-36 header docstring (growth-only/subset semantics, the anti-vacuity rationale, and the "regenerate at execution time, not from planning-time prose" rule), `ENTRY_POINTS` L44-56, the baseline comment block and list from L58, `computeElectronReach` at L202, and the guards at L293/L295-307/L317.
    - `.planning/phases/34.3-.../34.3-07-SUMMARY.md` — the exact procedure Phase 34.3 Plan 07 used: a temporary measurement print statement, captured then removed, with the before/after measured sets recorded. Follow that procedure, not a reimplementation of it.
    - `src/backend/sidecar/humbleFlowRegistration.ts` — plans 34.4-04/05's output, the new entry point.
    - `src/backend/sidecar/steamAuthFlowRegistration.ts` and `src/backend/sidecar/settingsFlowRegistration.ts` — plans 34.4-01/02/03's output, the two extended modules that are not yet entry points.
    - `src/backend/humble/library.ts` L1-45 — confirm the `./expirationAlerts` import at L22 and the `storeManagers/steam/*` imports at L41-42 that make the two-hop edge and the Steam reach real.
  </read_first>
  <action>
**Step 1 — extend `ENTRY_POINTS`** with this slice's ported surface. Add
`src/backend/sidecar/humbleFlowRegistration.ts` (required by REQ-34.4-10), and also add
`src/backend/sidecar/steamAuthFlowRegistration.ts` and
`src/backend/sidecar/settingsFlowRegistration.ts`, which this slice extends and which the corrected
reading shows are **not** currently entry points. Covering them is what makes the committed baseline
a trustworthy Phase 35 work-list rather than a partial one. Add a comment in the style of the
existing Phase 34.3 block, naming Phase 34.4, REQ-34.4-10/D-10, and stating why each was added
(one new module; two extended modules previously uncovered — noting explicitly that this corrects
34.4-RESEARCH.md's claim that `settingsFlowRegistration.ts` was already listed).

**Step 2 — MEASURE, do not transcribe.** Following 34.3-07's recorded procedure: capture the measured
`electronImportingFiles` set BEFORE your `ENTRY_POINTS` change and AFTER it, using a temporary
one-off measurement print statement inside this test file, run the suite to capture the output, then
**remove the temporary statement**. Compute the exact set difference. Do not hand-write the new
baseline from this plan's prose — the three module names in `<interfaces>` are a prediction to check
your measurement against, not a list to paste.

**Step 3 — update `BASELINE_ELECTRON_REACHING_MODULES`** with exactly the measured additions, keeping
the list sorted and repo-relative. Add a comment block in the existing style recording: the date, the
plan, the before/after counts, the exact set difference, and for each new entry its import line and
its reach path (for `humble/expirationAlerts.ts`, spell out the two-hop path
`humbleFlowRegistration.ts -> humble/library.ts:22 -> ./expirationAlerts` and why a depth-1 scan
cannot see it).

**If the measurement disagrees with the prediction — the measurement wins.** Record the discrepancy
prominently in your SUMMARY (which modules appeared or did not, and your best explanation). Adding
the two Steam/settings entry points may legitimately surface modules neither this plan nor RESEARCH.md
anticipated; that is precisely what this exercise exists to find, and it is a finding, not a failure.

**Step 4 — extend the anti-degradation `requiredModules` list (L295)** with the new edges, so a future
refactor that quietly removes one of them fails a test instead of silently shrinking the measured set.

**Step 5 — re-check the reachability-sanity floor (L317).** The three added entry points will grow
`visitedFiles.size`. Record the newly measured value in a comment (as 34.3-07 recorded 202) and raise
the floor only if the measured value comfortably clears a higher one. **Never lower it**, and never
raise it above the measured value.

**Do not weaken anything.** The growth-only/subset semantics, the four pre-existing tests, and the
anti-vacuity guards must all survive unchanged in intent. If a pre-existing test must change, change
it openly with a stated reason in both the code comment and the SUMMARY (REQ-34.4-15).

**RED proof (mandatory, by hand, then restore):** temporarily remove one of the newly-added baseline
entries and confirm the subset test fails naming exactly that module; restore and confirm green.
Record the verbatim failure output in the SUMMARY. A baseline extension that stays green when an
entry is removed is a vacuous gate.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib/src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ENTRY_POINTS` contains `humbleFlowRegistration.ts`, `steamAuthFlowRegistration.ts` and `settingsFlowRegistration.ts` in addition to the seven pre-existing entries
    - `npx jest sidecar/__tests__/electronReachLedger.test.ts` passes, including all four pre-existing tests
    - The baseline comment block records the date, the plan, the before/after measured counts, and the exact set difference — and the SUMMARY states that the list was produced by running `computeElectronReach()`, with the captured output quoted
    - No temporary measurement print statement remains in the committed file (`grep -c "console.log" src/backend/sidecar/__tests__/electronReachLedger.test.ts` returns the same value as before this plan)
    - `src/backend/humble/user.ts`, `src/backend/humble/adapter.ts` and `src/backend/humble/expirationAlerts.ts` each appear in `BASELINE_ELECTRON_REACHING_MODULES`, OR the SUMMARY explains with measured evidence why one did not appear
    - The `requiredModules` anti-degradation list gained the new edges
    - The reachability-sanity floor is unchanged or raised, never lowered, and the newly measured `visitedFiles.size` is recorded in a comment
    - The hand RED proof (removing one new baseline entry) is recorded with verbatim output, and the file is restored green afterwards
    - The list is sorted and every entry is repo-relative
  </acceptance_criteria>
  <done>The ledger covers this slice's whole ported surface, its baseline is a measurement rather than a transcription, and removing any new entry makes it go red.</done>
</task>

<task type="auto">
  <name>Task 2: Structural proof of the additive/reversible invariant across the whole phase</name>
  <files>src/backend/sidecar/__tests__/electronReachLedger.test.ts</files>
  <read_first>
    - `src/backend/sidecar/__tests__/electronUntouched.test.ts` — the additive/reversible guard, behavioral since Phase 34.1. Understand exactly what it asserts before running it, so a pass is meaningful rather than incidental.
    - All seven prior SUMMARYs in this phase directory (`34.4-01` … `34.4-07`) — confirm each plan's own verification actually passed, and collect the recorded pre/post test counts so this task's totals can be compared against a real baseline.
    - `.planning/phases/34.4-.../34.4-VALIDATION.md` § "Sampling Rate" — the per-wave contract this task satisfies.
  </read_first>
  <action>
Run the phase's full structural sweep and record every result in the SUMMARY with its exact command
and exit status. This is the wave-level gate that plans 34.4-09 and 34.4-10 depend on; do not proceed
past a red result — record it and report.

1. `cd src/backend && npx jest` — the full backend suite. Compare against the recorded pre-phase
   baseline. The only acceptable failures are the two documented pre-existing ones
   (`rustInvokeChannel.test.ts`, a Phase 34.1-era baseline; and the wine `rest.test.ts` path-depth
   bug). The known non-deterministic `library.ts` leaked-timer flake may land on an arbitrary suite —
   if a suite fails, re-run it in isolation before recording it as real, exactly as prior plans in
   this phase sequence did. Record the final suite/test totals.
2. `npx tsc --noEmit` — must exit 0.
3. `cd src-tauri && cargo check --quiet` — must exit 0. Also confirm `git diff --stat src-tauri/` is
   **empty** for the whole phase: **zero new Rust arms** is this slice's standing rider (34.2), and
   if any Rust changed, stop and report why rather than accepting it.
4. `npx prettier --check` over every file this phase has touched — `.github/workflows/lint.yml:19`
   runs `pnpm prettier`, so a dirty file fails CI.
5. `cd src/frontend && npx jest` — the frontend project, covering plan 34.4-07's panel.
6. `cd src/backend && npx jest sidecar/__tests__/electronUntouched.test.ts` — must pass, and
   `git diff --stat` on that file must be **empty**. If it was modified by any plan in this phase,
   that is a REQ-34.4-15 violation: stop and report rather than accepting it.
7. Confirm the Electron-side parity sources are byte-unchanged for the whole phase:
   `git diff --stat src/backend/main.ts src/backend/humble/ipc_handler.ts` must be empty. This slice
   ports transport; it does not change either build's behavior.

Record all seven results in the SUMMARY as a table with commands and outcomes. Plan 34.4-10's live
gate will re-run several of these on both builds; this task is the structural half.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit && cd src-tauri && cargo check --quiet && cd ../src/backend && npx jest 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a table of all seven checks with the exact command and its outcome
    - Full backend jest totals are recorded, and every failing suite is either one of the two documented pre-existing failures or was reproduced-then-explained after an isolated re-run
    - `npx tsc --noEmit` exits 0
    - `cargo check --quiet` exits 0 AND `git diff --stat src-tauri/` produces no output for the whole phase
    - `npx prettier --check` on every file this phase touched exits 0
    - The frontend jest project passes
    - `electronUntouched.test.ts` passes and `git diff --stat` on it is empty
    - `git diff --stat src/backend/main.ts src/backend/humble/ipc_handler.ts` produces no output
  </acceptance_criteria>
  <done>The phase's structural invariants are proven green in one place, so the declared-list plan and the live gate measure the right tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar module graph → Electron | every remaining `import ... from 'electron'` reachable from a registered channel; the ledger is the only inventory of this boundary |
| committed baseline → Phase 35 | the baseline IS the cutover work-list; an incomplete one silently under-scopes that phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4-35 | Repudiation | a baseline transcribed from planning-time prose rather than measured, silently omitting a real edge | mitigate | Regenerated by running `computeElectronReach()` via a temporary, removed measurement statement (34.3-07's recorded procedure); the SUMMARY quotes the captured output; when measurement and prediction disagree, the measurement wins |
| T-34.4-36 | Repudiation | an incomplete `ENTRY_POINTS` making the committed baseline an under-scoped Phase 35 work-list | mitigate | Extended with all three of this slice's registration modules, including the two the corrected reading shows were never listed (`steamAuthFlowRegistration.ts`, `settingsFlowRegistration.ts`) |
| T-34.4-37 | Repudiation | a vacuous ledger — a baseline extension that stays green when an entry is removed | mitigate | Hand RED proof: remove one new entry, confirm the subset test fails naming exactly that module, restore. Plus the `requiredModules` anti-degradation list and the reachability-sanity floor, both extended and never weakened |
| T-34.4-38 | Information Disclosure | an unmeasured two-hop electron edge (`humble/expirationAlerts.ts` via `humble/library.ts:22`) shipping unnoticed into the Tauri build | mitigate | Explicitly predicted, measured, added to the baseline and to `requiredModules`, with its two-hop path spelled out in a comment recording why a depth-1 scan cannot see it |
| T-34.4-39 | Tampering | the additive/reversible invariant quietly broken by a weakened `electronUntouched.test.ts` or a changed Electron parity source | mitigate | Task 2 asserts `electronUntouched.test.ts` passes AND is byte-unchanged, and that `main.ts` / `humble/ipc_handler.ts` are byte-unchanged for the whole phase; any needed contract change must be raised, not absorbed |
| T-34.4-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages; `cargo check` is run but `src-tauri/` must be byte-unchanged |
</threat_model>

<verification>
- `cd src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts` passes with all four pre-existing tests intact
- The baseline was regenerated by running the traversal, with the captured output quoted in the SUMMARY
- No temporary measurement statement remains in the committed file
- The hand RED proof is recorded
- Full backend jest, `tsc --noEmit`, `cargo check`, `prettier --check`, and the frontend jest project are all green
- `git diff --stat src-tauri/`, `git diff --stat src/backend/main.ts src/backend/humble/ipc_handler.ts`, and `git diff --stat` on `electronUntouched.test.ts` are all empty
</verification>

<success_criteria>
- `ENTRY_POINTS` covers this slice's whole ported surface (3 additions)
- The baseline is a measurement, not a transcription, and includes the two-hop `expirationAlerts.ts` edge
- Growth-only/subset semantics, anti-vacuity guards and the sanity floor are preserved or strengthened
- The phase's structural additive/reversible invariant is proven green in one recorded place
</success_criteria>

<output>
Create `.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-08-SUMMARY.md` when done
</output>
