---
phase: 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble
plan: 02
subsystem: auth
tags: [tauri, sidecar, ipc, steam, steam-user, jest, trust-boundary]

# Dependency graph
requires:
  - phase: 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble
    plan: 01
    provides: steamAuthFlowRegistration.ts extended with the credential/SteamGuard/TOTP trio and the session/identity trio (6 channels), plus the corrected module docstring this plan extends further
provides:
  - "7 newly-registered Steam channels on the Tauri sidecar, completing all 13 genuinely-Steam channels: steamBottleProvision, isSteamBottleProvisioned, steamBottleStatus (macOS CrossOver bottle trio), steamClientSetupStart, steamClientSetupRecheck (guided Steam-client install pair), redeemSteamKey, getSteamInstallSize (misc pair)"
  - "redeemSteamKey's WR-03 main-process trust boundary ported verbatim: non-'steam' store, non-string key, or empty key returns { store: 'steam', outcome: 'error', message: 'invalid-request' } before SteamUser.redeemKey is ever reached, proven by four not-called rejection tests, hand RED-proved"
  - "steamBottleStatus reproduces the one genuinely inline body among the 15 Steam-labeled channels (main.ts:948-953) without re-deriving from isBottleProvisioned() or re-adding the removed loggedIn field"
  - "14 new tests (28 total, up from 14) covering bottle trio, client-setup pair, and redeem/install-size, all naming their REQ ID"
affects: [34.4-09, 34.4-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Module-boundary mocking for a transport-only suite: jest.mock(...) the underlying storeManagers/steam/{bottle,clientSetup,games} modules wholesale (automock) rather than mocking SteamUser's individual methods, since this suite proves registration and transport, not the mocked modules' own logic (already covered by storeManagers/steam/__tests__/)"
    - "Not-called assertion as the load-bearing trust-boundary proof: a rejection test that only checks the returned message would still pass if the guard were moved after the delegation; asserting SteamUser.redeemKey was never called is what makes the RED proof meaningful"

key-files:
  created: []
  modified:
    - src/backend/sidecar/steamAuthFlowRegistration.ts
    - src/backend/sidecar/__tests__/steamAuthFlows.test.ts

key-decisions:
  - "steamBottleProvision's transport arg is passed through as Parameters<typeof provisionBottle>[0] rather than a hand-written inline type, avoiding a stale duplicate of provisionBottle's own opts shape (bottleName/wineVersion) that would silently drift if the real signature changes"
  - "getPrivateBranchPassword/setPrivateBranchPassword (main.ts:1510-1515) are explicitly named in the docstring as NOT registered by this file -- they are a GOG channel despite the inventory filing them under Steam, and belong to settingsFlowRegistration.ts per 34.4-PATTERNS.md's classification correction. Not this plan's scope; documented so the next reader does not treat the omission as an oversight."
  - "steamBottleConfigStore.clear() added to the shared afterEach alongside the existing steamConfigStore.clear() -- both stores are isolated to the disposable per-process tmp config dir established by this file's os mock, never real user data."

requirements-completed: [REQ-34.4-03, REQ-34.4-04, REQ-34.4-05]

# Metrics
duration: ~40min
completed: 2026-07-27
---

# Phase 34.4 Plan 02: Steam bottle trio, client-setup pair, redeem/install-size Summary

**Ported the macOS CrossOver bottle trio, the guided Steam-client install pair, and the redeemSteamKey/getSteamInstallSize pair onto the Tauri sidecar, completing all 13 genuinely-Steam channels in `steamAuthFlowRegistration.ts`, with the WR-03 trust boundary ported byte-faithful and hand RED-proved non-vacuous.**

## Performance

- **Duration:** ~40 min
- **Completed:** 2026-07-27
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- 7 new Steam channels registered on the Tauri sidecar: `steamBottleProvision`, `isSteamBottleProvisioned`, `steamBottleStatus`, `steamClientSetupStart`, `steamClientSetupRecheck`, `redeemSteamKey`, `getSteamInstallSize` — bringing the total to 16 registrations (3 QR + 6 credential/session + 7 here), all 13 genuinely-Steam channels in scope for this slice now on the sidecar
- `steamBottleStatus` reproduces the one genuinely inline body among the 15 Steam-labeled channels exactly: both `get_nodefault(...) ?? fallback` reads, no re-derivation from `isBottleProvisioned()`, no `loggedIn` field (17-17/WR-02, D-04)
- `redeemSteamKey`'s WR-03 main-process trust boundary ported verbatim from `main.ts:906-917`: malformed payload rejected with the literal `invalid-request` shape before `SteamUser.redeemKey` is ever called; never logs the key value on either path
- Module docstring extended (not just the credential/session paragraphs from plan 01) to name all 7 new channels with `main.ts:LINE` citations, and to explicitly state `getPrivateBranchPassword`/`setPrivateBranchPassword` are NOT registered here (a GOG channel, corrected classification)
- 14 new tests (28 total, up from 14) covering the bottle trio, client-setup pair, and redeem/install-size — each redeem rejection case asserts `SteamUser.redeemKey` was never called, proven non-vacuous by a hand RED flip
- Zero new Rust arms, zero new store plumbing (all three stores already registered by Phase 29 D-15), zero new reach-ledger growth (`steam/games.ts` already present in `BASELINE_ELECTRON_REACHING_MODULES` from a prior slice)

## Task Commits

Each task was committed atomically:

1. **Task 1: Register the bottle trio, client-setup pair, and redeem/install-size pair** - `e1d0ba04` (feat)
2. **Task 2: Bottle/client-setup/redeem coverage, including the redeem rejection cases** - `321dacf9` (test)

## Files Created/Modified
- `src/backend/sidecar/steamAuthFlowRegistration.ts` - added 7 registrations (all `ipcMain.handle`), 5 new imports (`provisionBottle`/`isBottleProvisioned` from `steam/bottle`, `startGuidedClientInstall`/`ensureSteamClientReady` from `steam/clientSetup`, `getSteamInstallSize` from `steam/games`, `steamBottleConfigStore` added to the existing `electronStores` import, `DEFAULT_STEAM_BOTTLE_NAME` from `steam/constants`), rewrote the module docstring to name all 13 Steam channels now registered plus what stays out of scope
- `src/backend/sidecar/__tests__/steamAuthFlows.test.ts` - added `jest.mock(...)` for `steam/bottle`, `steam/clientSetup`, `steam/games` (automocked, module boundary), three new `describe` blocks (bottle trio, client-setup pair, redeem/install size), extended the shared `afterEach` to also clear `steamBottleConfigStore`

## Decisions Made
- `steamBottleProvision`'s arg is cast via `Parameters<typeof provisionBottle>[0]` rather than a hand-duplicated `{ bottleName?: string; wineVersion?: WineInstallation }` type, so the cast can never silently drift from the real function signature.
- Added an explicit "no emitted log line or response frame contains the submitted key value" test (spying on `console.log`/`warn`/`error`) beyond the plan's four not-called rejection cases, directly proving the T-34.4-07 Information Disclosure mitigation rather than relying on code inspection alone.

## Deviations from Plan

**1. [Rule 1 - minor, acceptance-criterion literal vs. spirit] `grep -c "loggedIn"` returns 2, not 0.**
- **Found during:** Task 1 acceptance-criteria verification.
- **Issue:** The plan's literal acceptance criterion (`grep -c "loggedIn" ... returns 0`) is satisfied only in spirit. The rewritten module docstring necessarily contains the string `loggedIn` twice, both in prose explicitly stating the field is deliberately NOT present (17-17/WR-02) — no `loggedIn` **registration** or object property exists anywhere in the file.
- **Fix:** None needed — confirmed by the more precise, code-level acceptance criterion: `steamBottleStatus`'s actual resolved object has no `loggedIn` property (proven by a dedicated test in Task 2, and by direct read of the handler body, which builds `{ provisioned, bottleName }` only).
- **Files affected:** `src/backend/sidecar/steamAuthFlowRegistration.ts` (docstring prose only).
- **Commit:** `e1d0ba04`.

No other deviations — plan executed as written.

## Issues Encountered

None. The hand RED proof (relocating `redeemSteamKey`'s validation guard to after the `SteamUser.redeemKey` delegation) failed exactly the five expected tests:

```
● redeem / install size › REQ-34.4-05 redeemSteamKey invoke rejects a non-"steam" store before reaching SteamUser.redeemKey
● redeem / install size › REQ-34.4-05 redeemSteamKey invoke rejects a non-string key before reaching SteamUser.redeemKey
● redeem / install size › REQ-34.4-05 redeemSteamKey invoke rejects an empty-string key before reaching SteamUser.redeemKey
● redeem / install size › REQ-34.4-05 redeemSteamKey invoke rejects a missing/null payload before reaching SteamUser.redeemKey
● redeem / install size › REQ-34.4-05 no emitted log line or response frame in the rejection cases contains the submitted key value

Test Suites: 1 failed, 1 total
Tests:       5 failed, 21 skipped, 2 passed, 28 total
```

The two remaining `redeem`-filtered tests (valid payload, `getSteamInstallSize`) still passed, as expected — only the guard's absence was exercised. Reverted via `git checkout -- src/backend/sidecar/steamAuthFlowRegistration.ts`; `git diff --stat e1d0ba04 -- src/backend/sidecar/steamAuthFlowRegistration.ts` confirmed empty (byte-identical to the Task 1 commit).

A pre-existing, already-documented SDK gotcha (`gsd-sdk state writes corrupt STATE.md`) was avoided entirely this run by hand-editing `STATE.md` rather than invoking the `state.*`/`roadmap.*` write verbs (per the known_gotcha directive in this session's prompt).

## User Setup Required

None - no external service configuration required.

## Verification Evidence

- `npx tsc --noEmit` exits 0 (both touched files)
- `npx prettier --check` on both touched files exits 0
- `cd src/backend && npx jest sidecar/__tests__/steamAuthFlows.test.ts` — 28/28 passing (baseline after plan 01 was 14/14)
- Full backend sweep: `cd src/backend && npx jest` — 2449/2452 tests, 115/118 suites; the 3 failing suites are pre-existing, unrelated to this plan's files, confirmed by re-running each in isolation:
  - `rustInvokeChannel.test.ts` — fails identically in isolation (documented Phase 34.1 tray-icon baseline, `tray_set_icon` extra rustInvoke frame)
  - `wine/manager/downloader/__tests__/utilities/rest.test.ts` — fails identically in isolation (pre-existing absolute-path-substring environment mismatch, unrelated file)
  - `sidecar/__tests__/lifecycleStub.test.ts` — **passes** in isolation (25/25); the full-suite failure is cross-test pollution from a leaked timer in `steam/library.ts`'s `pollInstallOnce`, matching the previously-documented "known separate library.ts leaked-timer jest exit-1" issue
- `cd src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts` — 4/4 passing, no new reach-ledger growth (`steam/games.ts`'s `import { shell } from 'electron'` was already present in `BASELINE_ELECTRON_REACHING_MODULES` from an earlier slice, so importing it here adds no new edge)
- `cd src/backend && npx jest sidecar/__tests__/gameDetailsImportGate.test.ts` — 49/49 passing, unaffected (this gate scans `settingsFlowRegistration.ts`/`main.ts`, not `steamAuthFlowRegistration.ts`)
- `git diff --stat -- src-tauri/` empty (no Rust files touched)
- `git diff --stat -- src/backend/main.ts` empty (`main.ts` byte-unchanged — Electron build behavior unaffected)

## Next Phase Readiness
- `steamAuthFlowRegistration.ts` now registers all 13 genuinely-Steam channels in scope for this slice (13/13). Only `getPrivateBranchPassword`/`setPrivateBranchPassword` (GOG, routed to `settingsFlowRegistration.ts`) and `isLoggedIn` (Epic, deferred to Phase 34.5 per D-03) remain unregistered under the "Steam" heading, both by deliberate design.
- The live gate (plan 34.4-10) still owes items 2, 4, 5 involving these channels: `logoutSteam` actually signing out (plan 01's territory), `steamBottleStatus`/`isSteamBottleProvisioned` reporting truthfully on macOS. `redeemSteamKey` and `steamBottleProvision` stay unit-proven and declared per D-08 — never live-run.
- `34.4-PORTED-CHANNELS.md` (plan 34.4-09) still owes declared rows for all 13 Steam channels, including the two D-08 unit-only rows for `redeemSteamKey`/`steamBottleProvision`.
- No blockers.

---
*Phase: 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble*
*Completed: 2026-07-27*

## Self-Check: PASSED

- FOUND: `src/backend/sidecar/steamAuthFlowRegistration.ts`
- FOUND: `src/backend/sidecar/__tests__/steamAuthFlows.test.ts`
- FOUND: commit `e1d0ba04` (Task 1)
- FOUND: commit `321dacf9` (Task 2)
