---
phase: 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble
plan: 08
subsystem: infra
tags: [tauri, sidecar, electron-reach-ledger, structural-proof, additive-reversible-invariant]

# Dependency graph
requires:
  - phase: 34.4-01
    provides: steamAuthFlowRegistration.ts extended with the credential/session trio (not yet an entry point before this plan)
  - phase: 34.4-02
    provides: steamAuthFlowRegistration.ts extended with the bottle/client-setup/redeem surface
  - phase: 34.4-03
    provides: settingsFlowRegistration.ts extended with the two GOG private-branch-password channels
  - phase: 34.4-04
    provides: humbleFlowRegistration.ts (new module), 10 Humble channels — the plan whose SUMMARY explicitly deferred the ENTRY_POINTS edit to this plan
  - phase: 34.4-05
    provides: humbleFlowRegistration.ts extended to all 16 owned Humble channels
  - phase: 34.4-06
    provides: hardened electronStub.net.request (D-06) — no reach-ledger impact, confirmed by measurement
  - phase: 34.4-07
    provides: WebviewUnavailablePanel.tsx (D-04) — frontend-only, no reach-ledger impact
provides:
  - electronReachLedger.test.ts's ENTRY_POINTS extended to 10 entries (humbleFlowRegistration.ts, steamAuthFlowRegistration.ts, settingsFlowRegistration.ts)
  - BASELINE_ELECTRON_REACHING_MODULES regenerated by measurement (30 -> 34 modules)
  - Phase-wide structural proof of the additive/reversible invariant (tsc, cargo check, full backend+frontend jest, prettier, electronUntouched.test.ts, byte-unchanged parity sources)
affects: [phase-35-electron-cutover, 34.4-09, 34.4-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "regenerate a committed baseline by actually running the measurement tooling at execution time (temporary print statement, captured, removed), never by transcribing planning-time prose — repeated a third time (34.2 Plan 11, 34.3 Plan 07, this plan)"
    - "when a measurement disagrees with a plan's own prediction, the measurement wins and the discrepancy is documented inline, not silently reconciled"

key-files:
  created: []
  modified:
    - src/backend/sidecar/__tests__/electronReachLedger.test.ts
    - src/backend/storeManagers/steam/library.ts

key-decisions:
  - "The plan's own <interfaces> section and 34.4-RESEARCH.md predicted exactly THREE new electron-importing modules (humble/user.ts, humble/adapter.ts, humble/expirationAlerts.ts). The actual measurement found a FOURTH: src/backend/humble/userAgent.ts (imports `app` from 'electron' at its own line 1). Per the plan's explicit rule ('if the measurement disagrees with the prediction, the measurement wins'), all four were added to the baseline, with the fourth flagged as an unpredicted finding rather than silently folded in as if it had been expected."
  - "Fixed a pre-existing, out-of-scope prettier violation in src/backend/storeManagers/steam/library.ts (introduced by the out-of-band timer-leak fix, commit 3658b204, which was never run through prettier) discovered by Task 2's phase-wide prettier sweep. Whitespace/line-wrap only, zero logic change, verified via library.test.ts (166/166 unchanged). This was necessary to satisfy Task 2's acceptance criterion that prettier --check exits 0 on every file this phase touched, and CI (.github/workflows/lint.yml:19) would otherwise fail on this file regardless of this plan's own changes."

requirements-completed: [REQ-34.4-10, REQ-34.4-15]

# Metrics
duration: ~35min
completed: 2026-07-27
---

# Phase 34.4 Plan 08: Electron-Reach Ledger Extension + Structural Additive/Reversible Proof Summary

**Extended `electronReachLedger.test.ts`'s `ENTRY_POINTS` with this slice's three registration modules and regenerated `BASELINE_ELECTRON_REACHING_MODULES` by actually running `computeElectronReach()` — the measurement disagreed with the plan's own prediction (4 new modules, not 3: `humble/userAgent.ts` was an unpredicted discovery), then proved the whole phase's additive/reversible invariant green in one place.**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-07-27
- **Tasks:** 2 completed
- **Files modified:** 2 (`electronReachLedger.test.ts`, `storeManagers/steam/library.ts` — the latter a Rule 1 formatting fix, not this plan's own scope)

## Accomplishments

- `ENTRY_POINTS` extended from 7 to 10 entries: `humbleFlowRegistration.ts` (never added by plans 34.4-04/05, which explicitly deferred that edit here), `steamAuthFlowRegistration.ts` and `settingsFlowRegistration.ts` (both extended by this slice but, per the corrected reading, were never already entry points despite 34.4-RESEARCH.md's claim otherwise)
- Regenerated `BASELINE_ELECTRON_REACHING_MODULES` by running the real traversal via a temporary, removed `console.log` measurement statement (mirroring 34.3-07's exact procedure): **before 30 modules / `visitedFiles.size` 202, after 34 modules / `visitedFiles.size` 217**
- **Measurement disagreed with the plan's own prediction.** The plan's `<interfaces>` section and 34.4-RESEARCH.md named exactly three new modules. The measurement found a fourth: `src/backend/humble/userAgent.ts` (`import { app } from 'electron'` at its own line 1), reached two-hop via `humbleFlowRegistration.ts -> humble/user.ts:16 -> ./userAgent` and independently three-hop via `humbleFlowRegistration.ts -> humble/library.ts:12 -> ./adapter -> ./userAgent`. Neither RESEARCH.md's canonical_refs nor 34.4-CONTEXT.md's D-10 section named this module — both enumerated only the two direct electron imports (`user.ts`, `adapter.ts`) and the one transitive edge through `library.ts` (`expirationAlerts.ts`), missing this second, independently-electron-importing transitive dependency shared by both `user.ts` and `adapter.ts`. All four modules were added to the baseline with per-entry reach-path comments; the Steam and settings additions contributed zero new electron reach, exactly as predicted (all four Steam modules and `settingsFlowRegistration.ts`'s `libraryManagerMap` import zero electron)
- Extended the anti-degradation `requiredModules` list with all four new edges; raised the reachability-sanity floor from 150 to 200 (measured 217, never lowered)
- Hand RED-proofed: removed the `humble/userAgent.ts` baseline entry, confirmed the growth-tripwire test failed naming exactly that module, restored, confirmed all 4 tests green again
- Removed the temporary measurement statement (`grep -c "console.log"` returns 0, matching the pre-plan value)
- Task 2's full structural sweep: `tsc --noEmit` clean, `cargo check --quiet` clean with `git diff --stat src-tauri/` empty (zero new Rust, standing rider held), full backend jest run twice (2468/2470 both times, only the two documented pre-existing baselines failing — confirms the leaked-timer flake fix from `3658b204` is genuinely stable now), frontend jest 213/213 clean, `electronUntouched.test.ts` passes with `git diff --stat` empty on that file, `main.ts`/`humble/ipc_handler.ts` both byte-unchanged for the whole phase
- Found and fixed one out-of-scope-but-CI-blocking defect during Task 2's prettier sweep: `storeManagers/steam/library.ts` (touched by the out-of-band `3658b204` fix, never run through prettier) failed `prettier --check`. Fixed via `prettier --write` (whitespace/line-wrap only), verified `library.test.ts` unchanged (166/166)

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend ENTRY_POINTS and regenerate the baseline by running the traversal** - `4d3e555e` (test)
2. **Task 2 fix (Rule 1): prettier formatting on `library.ts`, found during the phase-wide sweep** - `08990737` (style)

Task 2 itself performed no source edits beyond the Rule 1 fix above — it is a verification/proof task, recorded here with all seven check results.

## Task 2: Structural Sweep Results

| # | Check | Command | Result |
|---|-------|---------|--------|
| 1 | Full backend jest | `cd src/backend && npx jest` | 2468/2470 tests, 116/118 suites — run twice, identical both times. The only 2 failures are the documented pre-existing baselines: `rustInvokeChannel.test.ts` (Phase 34.1-era) and wine `rest.test.ts` (path-depth-dependent). No flaky third failure landed on either run — confirms `3658b204`'s leaked-timer fix is stable. |
| 2 | `tsc --noEmit` | `npx tsc --noEmit` | Exit 0 |
| 3 | `cargo check` + Rust diff | `cd src-tauri && cargo check --quiet` + `git diff --stat src-tauri/` | Exit 0; diff-stat empty (zero new Rust arms this whole phase, standing rider held) |
| 4 | `prettier --check` on every phase-touched file | `npx prettier --check <18 files>` | Exit 0 after fixing `storeManagers/steam/library.ts` (see Deviations) |
| 5 | Frontend jest | `cd src/frontend && npx jest` | 213/213 tests, 27/27 suites passing (incl. plan 34.4-07's `WebviewUnavailablePanel.test.tsx`) |
| 6 | `electronUntouched.test.ts` + diff-stat | `cd src/backend && npx jest sidecar/__tests__/electronUntouched.test.ts` + `git diff --stat` on that file | 11/11 tests passing; diff-stat empty (file untouched for the whole phase) |
| 7 | Electron parity sources byte-unchanged | `git diff --stat src/backend/main.ts src/backend/humble/ipc_handler.ts` (scoped to the whole phase, since `caebe13f1`) | Empty — both files byte-identical to pre-phase state |

## Files Created/Modified

- `src/backend/sidecar/__tests__/electronReachLedger.test.ts` — `ENTRY_POINTS` extended 7→10; `BASELINE_ELECTRON_REACHING_MODULES` regenerated 30→34 (measured, not transcribed); `requiredModules` extended with the 4 new edges; reachability floor raised 150→200 (measured 217)
- `src/backend/storeManagers/steam/library.ts` — prettier formatting only (Rule 1 fix, out-of-scope defect found during Task 2's sweep, not this plan's own edit target)

## Decisions Made

- All four measured additions (not the predicted three) were added to the baseline — the plan's own rule ("if the measurement disagrees with the prediction, the measurement wins") was applied literally, and the fourth (`humble/userAgent.ts`) is documented as an explicit, flagged discrepancy rather than silently absorbed as if predicted.
- The out-of-scope prettier violation in `library.ts` was fixed (Rule 1) rather than deferred, because Task 2's own acceptance criterion requires `prettier --check` to exit 0 on every phase-touched file, and CI (`.github/workflows/lint.yml:19`) enforces the same gate — leaving it unfixed would fail CI on a file this phase's commit range already includes, regardless of authorship.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `storeManagers/steam/library.ts` failed `prettier --check`, introduced by an out-of-band fix commit not run through prettier**

- **Found during:** Task 2, step 4 (phase-wide prettier sweep).
- **Issue:** Commit `3658b204` ("stop leaked steam library poll timers from crashing jest workers", landed between plan 34.4-05 and this plan, out-of-band per this plan's `<prior_wave_context>`) reformatted several call sites in a way that did not match this repo's prettier config (line-wrap width). `.github/workflows/lint.yml:19` runs `pnpm prettier` in CI, so this would fail the pipeline on the next push regardless of any change this plan made.
- **Fix:** `npx prettier --write src/backend/storeManagers/steam/library.ts` — whitespace/line-wrap only, zero logic change (confirmed via `git diff`, which shows only reflow of existing call sites, no token changes).
- **Files modified:** `src/backend/storeManagers/steam/library.ts`
- **Verification:** `prettier --check` now exits 0 on the file; `library.test.ts` re-run, 166/166 unchanged; full backend jest re-run after the fix, still 2468/2470 (identical to before the fix).
- **Commit:** `08990737` (style)

### Not fixed / discrepancy (measurement disagreement, per plan's explicit rule)

**`humble/userAgent.ts` was not among the plan's three predicted new modules.** This is not a defect — it is the finding this plan's execution methodology exists to surface (per the plan's own text: "Adding the two Steam/settings entry points may legitimately surface modules neither this plan nor RESEARCH.md anticipated; that is precisely what this exercise exists to find, and it is a finding, not a failure."). Recorded in full above and inline in the committed baseline's comment block.

## Verification

- `cd src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts` — 4/4 passing, both before and after the fix, and immediately after the hand RED proof's restore
- Hand RED proof: removed `'src/backend/humble/userAgent.ts'` from the baseline; growth-tripwire test failed with `A NEW electron-importing module has entered the sidecar's reach graph and is NOT in the committed baseline: src/backend/humble/userAgent.ts` (verbatim, see below); restored; re-ran, green again
- `grep -c "console.log" src/backend/sidecar/__tests__/electronReachLedger.test.ts` = 0 (matches the pre-plan value; no temporary statement remains)
- `npx prettier --check` on all 18 non-`.planning` files this phase touched (per `git diff --stat caebe13f1..HEAD --name-only`, excluding `.claude/settings.json`/`.gitignore` which belong to unrelated interleaved quick tasks, not any 34.4 plan) — exits 0 after the Deviation 1 fix
- `npx tsc --noEmit` — exit 0
- `cd src-tauri && cargo check --quiet` — exit 0; `git diff --stat src-tauri/` — empty
- Full backend `npx jest`, run twice: 2468/2470 tests, 116/118 suites both times, identical failing-suite set (the 2 documented pre-existing baselines)
- Frontend `npx jest`: 213/213 tests, 27/27 suites
- `electronUntouched.test.ts`: 11/11 passing; `git diff --stat` on that file empty for the whole phase
- `git diff --stat src/backend/main.ts src/backend/humble/ipc_handler.ts` (scoped since `caebe13f1`, the phase's first commit) — empty

### Hand RED Proof (verbatim)

```
FAIL Backend sidecar/__tests__/electronReachLedger.test.ts
  electronReachLedger (Phase 34.2 Plan 11 — REQ-34.2-03, gap #3 / WR-02)
    ✕ growth tripwire: every electron-importing module measured today is present in the committed baseline
    ✓ anti-degradation: the measured set is non-empty and contains every known load-bearing electron-reaching edge
    ✓ reachability sanity: the walk actually traverses the graph rather than stopping at depth 1
    ✓ the gap-#3 edge is pinned as a known, documented fact: dispatch.ts reaches dialog.ts, which imports electron directly

  ● electronReachLedger (Phase 34.2 Plan 11 — REQ-34.2-03, gap #3 / WR-02) › growth tripwire: every electron-importing module measured today is present in the committed baseline

    A NEW electron-importing module has entered the sidecar's reach graph and is NOT in the committed baseline: src/backend/humble/userAgent.ts. This is not automatically a bug -- electronStub.ts's Module._load hook safely rescues transitive electron reach at runtime -- but the baseline must be updated deliberately, after review, never silently. If this addition was reviewed and accepted, add the new path(s) to BASELINE_ELECTRON_REACHING_MODULES above with a comment explaining why.

Test Suites: 1 failed, 1 total
Tests:       1 failed, 3 passed, 4 total
```

## Known Stubs

None — this plan touches only a static-analysis test file and a formatting fix; no UI-rendering data path is affected.

## Threat Flags

None — this plan's threats (T-34.4-35..39, T-34.4-SC) cover exactly the ledger/baseline/invariant surface it touches; no new network endpoint, auth path, file access pattern, or schema change was introduced. The Rule 1 prettier fix is formatting-only with no behavioral surface.

## Self-Check

- `src/backend/sidecar/__tests__/electronReachLedger.test.ts` — FOUND, contains `humbleFlowRegistration` and `userAgent.ts`
- Commit `4d3e555e` — FOUND (`git log --oneline --all | grep 4d3e555e`)
- Commit `08990737` — FOUND (`git log --oneline --all | grep 08990737`)

## Self-Check: PASSED
