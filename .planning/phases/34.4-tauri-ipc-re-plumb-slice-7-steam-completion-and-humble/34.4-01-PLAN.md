---
phase: 34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/sidecar/steamAuthFlowRegistration.ts
  - src/backend/sidecar/__tests__/steamAuthFlows.test.ts
autonomous: true
requirements: [REQ-34.4-01, REQ-34.4-02]
must_haves:
  truths:
    - "Under the Tauri sidecar, steamStartCredentials / steamSubmitGuard / steamPollCredential resolve real values from SteamUser instead of rejecting with UNPORTED_CHANNEL_MARKER"
    - "logoutSteam is registered as a SEND (ipcMain.on / addListener), matching main.ts:939 — the G-30-01 channel kind, cross-checked against source not prose (D-08's deciding evidence)"
    - "getSteamUserInfo and getSteamSyncedAt resolve real shapes over the sidecar wire protocol"
    - "steamAuthFlowRegistration.ts's docstring no longer asserts the false invariant that these five channels are 'deliberately NOT registered'"
    - "steamAuthFlows.test.ts's pre-existing Test 5 (which asserted logoutSteam is unported) is openly rewritten with a stated reason, never silently deleted (REQ-34.4-15 discipline)"
    - "The 6 channels deferred to Phase 34.4.1 and isLoggedIn (D-03, Phase 34.5) are NOT registered by this plan"
  artifacts:
    - path: "src/backend/sidecar/steamAuthFlowRegistration.ts"
      provides: "9 registered Steam channels (3 pre-existing QR + 6 new), with a corrected docstring"
      contains: "steamStartCredentials"
    - path: "src/backend/sidecar/__tests__/steamAuthFlows.test.ts"
      provides: "Round-trip + registration-kind coverage for the 6 new channels"
      contains: "logoutSteam"
  key_links:
    - from: "src/backend/sidecar/steamAuthFlowRegistration.ts"
      to: "src/backend/storeManagers/steam/user.ts"
      via: "SteamUser.startCredentialLogin / submitSteamGuardCode / pollCredentialLogin / getUserDetails / logout"
      pattern: "SteamUser\\.(startCredentialLogin|submitSteamGuardCode|pollCredentialLogin|getUserDetails|logout)"
    - from: "src/backend/sidecar/steamAuthFlowRegistration.ts"
      to: "steamSyncStore"
      via: "getSteamSyncedAt reads syncedAt from the already-registered sync store"
      pattern: "steamSyncStore\\.get"
---

<objective>
Port the Steam **credential/SteamGuard/TOTP login trio** and the **session/identity trio** onto the
Tauri sidecar by extending the existing `steamAuthFlowRegistration.ts` — the QR half of this exact
same login flow, already proven in Phase 30. This closes the login half of SEAM.md deferred item 5
(Phase 30 D-02's QR-only scope).

Purpose: `logoutSteam` is the single most important channel in this whole slice. It is the G-30-01
channel — a `send` whose failure mode (`sidecar-send-channels-fail-silently`) produces **no reject,
no timeout and no console line**. Getting its registration kind right, and asserting that kind in a
test rather than assuming it, is the reason Phase 34.4 has a blocking live gate at all (D-08).

Output: 6 newly-registered channels, a docstring that is no longer a false invariant, and test
coverage that asserts each channel's `handle`/`send` kind explicitly.

**Scope discipline:** this plan registers exactly 6 channels. It must NOT register `isLoggedIn`
(reassigned to Phase 34.5 by D-03 — it is `LegendaryUser.isLoggedIn()`, Epic auth), and it must not
touch anything in the bottle / client-setup / redeem group (plan 34.4-02 owns those, same file,
next wave).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-CONTEXT.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-PATTERNS.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-VALIDATION.md

<interfaces>
<!-- Exact Electron-parity source, read from src/backend/main.ts on 2026-07-27. -->
<!-- Use these directly — do not re-derive them by exploring the codebase. -->

The six registrations this plan ports, with their EXACT `addHandler`/`addListener` kind
(`src/backend/main.ts`):

- L899 `addHandler('steamPollCredential', async () => SteamUser.pollCredentialLogin())` — **handle**
- L900-902 `addHandler('steamStartCredentials', async (event, { username, password }) => SteamUser.startCredentialLogin(username, password))` — **handle**
- L903-905 `addHandler('steamSubmitGuard', async (event, code) => SteamUser.submitSteamGuardCode(code))` — **handle**
- L918 `addHandler('getSteamUserInfo', async () => SteamUser.getUserDetails())` — **handle**
- L922 `addHandler('getSteamSyncedAt', () => steamSyncStore.get('syncedAt') ?? null)` — **handle** (note: synchronous body, still a handle)
- L939 `addListener('logoutSteam', async () => SteamUser.logout())` — **SEND / addListener**, never `.handle`

`main.ts`'s own comment above L939 records why it is fire-and-forget: `SteamUser.logout()` is async
(D-09 gap fix — it clears the refresh token through the TokenStore seam, which may RPC to Rust in
the sidecar build), and this file's convention for async void listeners is fire-and-forget.

Store import (already registered by `storeRegistration.ts:46-50` — **zero new store plumbing**):
`steamSyncStore` is a named export of `src/backend/storeManagers/steam/electronStores.ts`
(export block at L72-78, alongside `configStore`, `steamLibraryStore`, `steamMetadataStore`,
`steamBottleConfigStore`).

`SteamUser` is already imported at `steamAuthFlowRegistration.ts:43` — no new Steam-user import
needed. `steam/user.ts` imports **zero** Electron, so this plan adds no electron-reach-ledger edge.

**The load-bearing circular-dependency fix already exists in this file** at
`steamAuthFlowRegistration.ts:28-42` (`import '../storeManagers'` FIRST). It is per-file, not
per-registration — this file already pays it once. Do not duplicate or reorder it.

**Pre-existing test that this plan invalidates** — `steamAuthFlows.test.ts:320-331`:
`it('Test 5 (Invariant B guard): logoutSteam (deliberately unported) still rejects non-fatally, and
the RPC loop keeps serving', ...)`. It writes an invoke for `logoutSteam` and asserts a non-fatal
rejection. That contract is **legitimately changed by this plan** and must be rewritten openly with
a stated reason, per REQ-34.4-15. Keep the Invariant B guard's intent by re-pointing it at a channel
that IS still genuinely unported this slice — use `humbleRevealKey` (deferred to Phase 34.4.1 by
D-02), not a channel another 34.4 plan ports.

Test-file scaffold already present in `steamAuthFlows.test.ts` — reuse unchanged, do not rebuild:
`jest.mock('os', ...)` (L52), `jest.mock('electron', () => jest.requireActual('../electronStub'))`
(L64), `jest.mock('electron-store', ...)` (L65), `jest.mock('axios', ...)` (L76),
`jest.mock('backend/utils', ...)` (L91), `jest.mock('backend/constants/environment', ...)` (L98),
`jest.mock('../../storeManagers/steam/user')` (L108, automocked). Helpers: `collectFrames()` (L124),
`flush()` (L147), `startSidecar()` (L154), `writeInvoke(input, id, channel, args)` (L163). There is
**no `writeSend` helper in this file yet** — `settingsFlows.test.ts:249` has one
(`function writeSend(...)`) to copy the frame shape from.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the credential trio and session/identity trio, and correct the false docstring</name>
  <files>src/backend/sidecar/steamAuthFlowRegistration.ts</files>
  <read_first>
    - `src/backend/sidecar/steamAuthFlowRegistration.ts` — the whole file (63 lines). You are extending it; read the existing QR-trio shape, the L28-42 circular-dep-fix comment block, and the L18-24 docstring paragraph you must rewrite.
    - `src/backend/main.ts` L895-960 — the Electron-parity source for all six registrations, including the comment above `addListener('logoutSteam', ...)`.
    - `src/backend/storeManagers/steam/user.ts` L38-60, L244-260, L430-560 — `SteamUser`'s class shape, `logout()`, `pollCredentialLogin()`, `startCredentialLogin()`, and the guard_required/poll contract documented around L49-51 that the frontend depends on.
    - `src/backend/storeManagers/steam/electronStores.ts` L71-78 — the exact export block naming `steamSyncStore`.
    - `src/backend/sidecar/appShellFlowRegistration.ts` L1-60 — the module-docstring density and `main.ts:LINE` citation style this project uses for a multi-channel registration module; mirror that format when rewriting the docstring.
  </read_first>
  <action>
Extend `registerSteamAuthFlows()` with exactly six new registrations, each reproducing its
`main.ts` body, and rewrite the module docstring.

**Registrations — the kind is load-bearing, cross-check each against `main.ts` before writing it:**

Five `ipcMain.handle` registrations: `steamPollCredential` → `SteamUser.pollCredentialLogin()`;
`steamStartCredentials` → destructure `{ username, password }` from the first arg and call
`SteamUser.startCredentialLogin(username, password)`; `steamSubmitGuard` → pass the first arg to
`SteamUser.submitSteamGuardCode(code)`; `getSteamUserInfo` → `SteamUser.getUserDetails()`;
`getSteamSyncedAt` → `steamSyncStore.get('syncedAt') ?? null`.

One `ipcMain.on` registration: `logoutSteam` → `SteamUser.logout()`. This is `addListener` at
`main.ts:939` — **`ipcMain.on`, never `ipcMain.handle`.** Because it is fire-and-forget with no
promise for the transport to settle, the returned promise must not be discarded silently: attach a
`.catch(...)` that writes a module-attributed warning (prefix `[steamAuthFlowRegistration]`),
mirroring `humble/ipc_handler.ts:121-125`'s own WR-02 rider and `loggerFlowRegistration.ts`'s
call-site rejection-guard shape. Do NOT let it reject — `sidecar-dialog-reject-crashes`: an
unguarded fire-and-forget rejection can crash the sidecar.

**Argument shape:** the sidecar's `ipcMain` shim passes transport args after the event object, the
same way the existing QR handlers receive them. Follow whatever signature the pre-existing
registrations in this file and in `settingsFlowRegistration.ts` use for arg-taking handlers — do not
invent a new one. Type transport args as `unknown` and narrow at the boundary rather than asserting
a renderer-supplied type blindly.

**New import:** add `steamSyncStore` from `'../storeManagers/steam/electronStores'`. Place it after
the existing `import { SteamUser } from '../storeManagers/steam/user'` line, never above the
load-bearing `import '../storeManagers'` at L42.

**Docstring rewrite (mandatory, not tidying).** The paragraph at L18-24 currently asserts that
`steamStartCredentials`, `steamSubmitGuard`, `steamPollCredential`, `getSteamUserInfo` and
`logoutSteam` are "Deliberately does NOT register ... per Phase 30 D-02 (QR only)". Five of those
five become registered by this plan, so that sentence becomes a false invariant a future slice would
read and trust. Replace it with a paragraph that: names Phase 34.4 and REQ-34.4-01/REQ-34.4-02;
lists each newly-registered channel with its `main.ts:LINE` citation and its kind; states explicitly
that `logoutSteam` is a **send** (`main.ts:939`) and is the G-30-01 channel; and states what this
file still deliberately does NOT register — the bottle/client-setup/redeem group is plan 34.4-02's
(same file, next wave), and `isLoggedIn` belongs to Phase 34.5 per D-03 because it is
`LegendaryUser.isLoggedIn()` (Epic auth), not Steam. Preserve the existing Phase 30 QR paragraph
(L4-16) and the L28-42 circular-dep-fix comment verbatim.

**Do not** register `isLoggedIn`, `checkSteamInstalled` (already registered), or any channel from
plan 34.4-02's group.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit && npx prettier --check src/backend/sidecar/steamAuthFlowRegistration.ts && grep -c "ipcMain.handle\|ipcMain.on" src/backend/sidecar/steamAuthFlowRegistration.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ipcMain.on('logoutSteam'" src/backend/sidecar/steamAuthFlowRegistration.ts` returns exactly one line, and `grep -c "ipcMain.handle('logoutSteam'" ...` returns 0
    - The file registers exactly 9 channels total: `grep -c "ipcMain\.\(handle\|on\)(" ` returns 9
    - All five of `steamPollCredential`, `steamStartCredentials`, `steamSubmitGuard`, `getSteamUserInfo`, `getSteamSyncedAt` appear as `ipcMain.handle('<name>'` string matches
    - `grep -c "isLoggedIn" src/backend/sidecar/steamAuthFlowRegistration.ts` returns 0
    - The `logoutSteam` listener body contains a `.catch(` with a `[steamAuthFlowRegistration]` prefixed warning, and contains no `throw`
    - Running `grep -v '^ \*' src/backend/sidecar/steamAuthFlowRegistration.ts | grep -c "Deliberately does NOT register"` — the old five-channel sentence text no longer claims `steamStartCredentials`/`steamSubmitGuard`/`steamPollCredential`/`getSteamUserInfo`/`logoutSteam` are unregistered (read the rewritten docstring and confirm by eye; the phrase may legitimately survive pointing at the bottle group and `isLoggedIn`)
    - The docstring cites `main.ts:939` and the word `send` for `logoutSteam`
    - `import '../storeManagers'` is still the first non-`./electronStub` import, its comment block at L28-42 is byte-unchanged
    - `npx tsc --noEmit` exits 0 and `npx prettier --check` on the file exits 0
  </acceptance_criteria>
  <done>Six channels are registered with the correct kind, `logoutSteam` is a guarded `ipcMain.on`, and the module docstring is a true statement about what this file registers.</done>
</task>

<task type="auto">
  <name>Task 2: Round-trip and registration-kind tests, including the open rewrite of Test 5</name>
  <files>src/backend/sidecar/__tests__/steamAuthFlows.test.ts</files>
  <read_first>
    - `src/backend/sidecar/__tests__/steamAuthFlows.test.ts` — the whole file. Reuse its mock scaffold (L47-119) and helpers (`collectFrames` L124, `flush` L147, `startSidecar` L154, `writeInvoke` L163) unchanged; read Test 4's token-seam convention (L245+) and Test 5 (L320-331), which this task rewrites.
    - `src/backend/sidecar/__tests__/settingsFlows.test.ts` L249-257 — the `writeSend(...)` helper's exact frame shape, since `steamAuthFlows.test.ts` has none yet.
    - `src/backend/sidecar/steamAuthFlowRegistration.ts` — your Task 1 output, so assertions match what was actually written.
    - `.planning/phases/34.4-.../34.4-VALIDATION.md` § "Test Infrastructure" — the containment rule: `jest.setupContainment.ts` runs for every backend test file and must never be disabled or bypassed.
  </read_first>
  <action>
Add new `describe` blocks to `steamAuthFlows.test.ts` covering the six channels this plan registered,
and rewrite the now-false Test 5.

**Add a `writeSend` helper** to this file, copying the frame shape from
`settingsFlows.test.ts:249-257`. It is required for the `logoutSteam` proof and does not exist here
yet.

**Describe block — credential + SteamGuard trio (REQ-34.4-01).** Drive each of
`steamStartCredentials`, `steamSubmitGuard`, `steamPollCredential` through `writeInvoke` against the
real registered handler and assert: (a) the response frame resolves a real value and is NOT
`UNPORTED_CHANNEL_MARKER`; (b) the automocked `SteamUser.startCredentialLogin` was called with the
destructured `username` and `password` (not the raw payload object), `submitSteamGuardCode` with the
bare code string, and `pollCredentialLogin` with no arguments; (c) each was called exactly once.
Include one case that pins the guard_required/poll contract shape the frontend depends on
(`steam/user.ts:49-51`) by having the automock resolve a `guard_required`-shaped value and asserting
it survives the wire round-trip unchanged.

**Describe block — session/identity trio (REQ-34.4-02).** For `getSteamUserInfo` and
`getSteamSyncedAt`, assert the invoke round-trips a real value (and that `getSteamSyncedAt` resolves
`null` when the store has no `syncedAt`, matching `?? null`).

For `logoutSteam`, the load-bearing assertions are:
1. **Kind proof, positive direction:** a frame written via `writeSend` for `logoutSteam` causes
   `SteamUser.logout` to be called exactly once. This is the assertion jest CAN make; the live gate
   (plan 34.4-10 item 2) makes the one it cannot.
2. **Kind proof, negative direction:** `logoutSteam` must NOT be reachable as an invoke handler. A
   `writeInvoke` for `logoutSteam` must not produce a successful response frame carrying a
   `SteamUser.logout` result. This is the assertion that would have caught G-30-01's inverse.
3. **Rejection guard:** make the automocked `SteamUser.logout` return a rejected promise, drive the
   send, `flush()`, and assert the process did not crash, that the RPC loop keeps serving (write a
   `health` invoke afterwards and assert it responds), and that a `[steamAuthFlowRegistration]`
   diagnostic was emitted. Install an `unhandledRejection` listener in the test so an escaped
   rejection fails the test rather than the worker.

**Rewrite Test 5 (`steamAuthFlows.test.ts:320-331`) openly, with a stated reason.** It currently
asserts `logoutSteam` is "deliberately unported" and rejects non-fatally — that contract is
legitimately changed by this plan. Do not delete it silently. Keep the SEAM.md Invariant B guard it
exists to provide by re-pointing it at `humbleRevealKey`, which IS still genuinely unported this
slice (deferred to Phase 34.4.1 by D-02) and which no other 34.4 plan registers. Add a comment above
it recording: the original channel (`logoutSteam`), why the assertion changed (Phase 34.4 REQ-34.4-02
ports it as a send), and where the replacement proof now lives (this file's own send-kind
assertions plus live-gate item 2). Do not pick `humbleStartLogin`/`humbleReconnect`/`humbleStopLogin`/
`humbleLoginNavigated`/`humbleGetLoginUserAgent` arbitrarily — `humbleRevealKey` is the clearest
deferred marker; any of the six is defensible, but state which and why.

Name each new `it(...)` with its requirement ID prefix (`REQ-34.4-01 ...`, `REQ-34.4-02 ...`),
following `appShellFlows.test.ts`'s established convention in this project.

**RED proof (mandatory, by hand, then restore):** temporarily change the `logoutSteam` registration
in `steamAuthFlowRegistration.ts` from `ipcMain.on` to `ipcMain.handle`, confirm the send-kind test
fails, then restore via `git checkout` and confirm `git diff --stat` on that file is empty relative
to your Task 1 commit. Record the observed failure output in the SUMMARY. This is the whole point of
the test — if it stays green under a deliberate kind flip, it is vacuous.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib/src/backend && npx jest sidecar/__tests__/steamAuthFlows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest sidecar/__tests__/steamAuthFlows.test.ts` passes with a higher test count than the pre-plan baseline (record both numbers in the SUMMARY)
    - A test asserts `SteamUser.logout` is called exactly once when a `logoutSteam` **send** frame is written
    - A test asserts `logoutSteam` does NOT resolve as an invoke handler
    - A test drives a rejecting `SteamUser.logout` and asserts the RPC loop still answers a subsequent `health` invoke
    - Tests exist naming `REQ-34.4-01` and `REQ-34.4-02` in their `it(...)` titles
    - The rewritten Test 5 targets `humbleRevealKey` and carries a comment naming the original channel, the reason for the change, and where the replacement proof lives
    - The hand RED proof is recorded in the SUMMARY with the verbatim failure output, and `git diff --stat src/backend/sidecar/steamAuthFlowRegistration.ts` is empty against the Task 1 commit afterwards
    - No `jest.mock` or config change disables or bypasses `src/backend/jest.setupContainment.ts`
    - Full backend sweep `cd src/backend && npx jest` shows no suite that was green before this plan is now red (the known pre-existing failures — `rustInvokeChannel.test.ts` and wine `rest.test.ts` — do not count)
  </acceptance_criteria>
  <done>The six channels have round-trip coverage, `logoutSteam`'s send kind is asserted in both directions and proven non-vacuous by a hand RED flip, and Test 5's changed contract is documented rather than silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar IPC | `steamStartCredentials`/`steamSubmitGuard` carry a Steam username, password and SteamGuard/TOTP code across an untrusted-input boundary |
| sidecar → Steam CM network | `steam-session` performs the real credential handshake |
| sidecar → Rust keyring (Phase 28) | the resulting refresh token is persisted through `getTokenStore()`, never a generic store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4-01 | Information Disclosure | Steam password / SteamGuard code passing through the new `steamStartCredentials` and `steamSubmitGuard` handlers | mitigate | Handlers are pure pass-throughs to `SteamUser.startCredentialLogin`/`submitSteamGuardCode` — no logging of the payload, no persistence, no interpolation of `args` into any log line. Assert in review that neither registration body contains a `console.*`/`log*` call carrying the transport args (ASVS V2) |
| T-34.4-02 | Information Disclosure | the refresh token produced by a successful credential login | transfer | Persistence is `steam/user.ts`'s existing `getTokenStore()` seam (Phase 28 `SidecarKeyringTokenStore`), unchanged and already proven by the ported QR flow. This plan adds no new token-write path. Test 4's existing token-seam assertion in this suite stays green |
| T-34.4-03 | Repudiation | `logoutSteam` registered with the wrong kind, silently dropping a sign-out (the G-30-01 defect class) | mitigate | Registered as `ipcMain.on` cross-checked against `main.ts:939`; asserted in BOTH directions by Task 2 and proven non-vacuous by a hand kind-flip RED proof; live-gate item 2 (plan 34.4-10) closes the residual that jest structurally cannot reach |
| T-34.4-04 | Denial of Service | an unguarded rejection from the fire-and-forget `SteamUser.logout()` crashing the sidecar | mitigate | The listener attaches a `.catch(...)` writing a module-attributed diagnostic and never rethrows (`sidecar-dialog-reject-crashes`); Task 2 drives a rejecting logout and asserts the RPC loop keeps serving |
| T-34.4-05 | Information Disclosure | a test run writing a real Steam token into the developer's real `~/Library/Application Support/GameLib` (`tests-clobbering-real-steam-store`) | mitigate | `jest.setupContainment.ts` redirects HOME/APPDATA/XDG for every backend test file structurally; this plan must not disable or bypass it, and `../../storeManagers/steam/user` stays automocked |
| T-34.4-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages — zero new dependencies this phase (RESEARCH.md § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx prettier --check` on both touched files exits 0
- `cd src/backend && npx jest sidecar/__tests__/steamAuthFlows.test.ts` passes
- Full backend `npx jest` introduces no new red suite versus the recorded pre-plan baseline
- `logoutSteam` is `ipcMain.on`, cross-checked against `main.ts:939`, asserted in both directions, and hand RED-proved
- No Rust file is touched (`git diff --stat src-tauri/` is empty) — zero new Rust arms this slice
- The Electron build's behavior is unchanged: `src/backend/main.ts` is byte-unchanged by this plan
</verification>

<success_criteria>
- 6 channels registered with the correct `handle`/`send` kind
- `steamAuthFlowRegistration.ts`'s docstring is a true statement
- Test 5's changed contract is rewritten openly, with the reason and replacement proof recorded
- The send-kind assertion is proven non-vacuous by a deliberate kind flip
</success_criteria>

<output>
Create `.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-01-SUMMARY.md` when done
</output>
