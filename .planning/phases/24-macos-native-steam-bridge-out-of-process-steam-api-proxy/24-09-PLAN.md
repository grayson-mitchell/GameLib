---
phase: 24-macos-native-steam-bridge-out-of-process-steam-api-proxy
plan: 09
type: execute
wave: 4
depends_on: ["24-08"]
files_modified:
  - src/frontend/state/SteamBridgeSetup.ts
  - src/frontend/screens/Game/GamePage/components/SteamBridgeSetup/index.tsx
  - src/frontend/state/GlobalState.tsx
  - src/frontend/state/__tests__/SteamBridgeSetup.test.ts
  - public/locales/en/translation.json
autonomous: true
requirements: [R7]
must_haves:
  truths:
    - "A steamBridgeSetupRequired backend signal opens an explicit error dialog (D-05) — not a silent strand"
    - "The dialog offers to fall back to the bottled-Steam path vs cancel (no silent auto-fallback, no dead-end)"
    - "The fallback action routes through the EXISTING Phase 17 non-bridge branch; if that bottle was never provisioned it offers on-demand provisionBottle (D-11)"
    - "The handler is a standalone testable function wired once in GlobalState (SteamBottleSetup precedent)"
  artifacts:
    - path: "src/frontend/state/SteamBridgeSetup.ts"
      provides: "zustand store + handleSteamBridgeSetupRequiredSignal (D-05 seam)"
      min_lines: 30
    - path: "src/frontend/screens/Game/GamePage/components/SteamBridgeSetup/index.tsx"
      provides: "fall-back / cancel dialog component (D-05/D-11)"
  key_links:
    - from: "src/frontend/state/GlobalState.tsx"
      to: "handleSteamBridgeSetupRequiredSignal"
      via: "IPC listener registration for steamBridgeSetupRequired"
      pattern: "steamBridgeSetupRequired"
    - from: "SteamBridgeSetup dialog fallback action"
      to: "existing non-bridge install()/launch() branch (+ provisionBottle on demand)"
      via: "re-route + D-11 on-demand provision"
      pattern: "fallback"
---

<objective>
Build the R7/D-05 failure surface. When the backend fires `steamBridgeSetupRequired` (from 24-06/24-08), the frontend opens an EXPLICIT error dialog (D-05 — no silent auto-fallback, no dead-end error) offering to fall back to the proven Phase 17/22 bottled-Steam path, or cancel. The fallback action re-routes that AppID through the EXISTING non-bridge `install()`/`launch()` branch (Pitfall 4 — do not build a separate fallback path; reuse the existing guard chain); if the Phase 17 `GameLibSteam` bottle was never provisioned, the dialog offers to provision it on demand via `provisionBottle` (D-11) and then routes through it. Follows the `SteamBottleSetup` zustand-store + standalone-testable-handler precedent exactly (a third guided-setup surface, not a new invocation shape).

Purpose: guarantee a bridge failure never silently strands the user (R7 acceptance).
Output: `SteamBridgeSetup.ts` store + dialog component + GlobalState wiring + i18n + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-SPEC.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-RESEARCH.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-PATTERNS.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-08-SUMMARY.md

**MANDATORY graphify rule:** run `graphify query "SteamBottleSetup GlobalState steamBottleSetupRequired dialog"` before reading raw source.

**Analogs (read via graphify first):**
- `src/frontend/state/SteamBottleSetup.ts` (52 lines, full) — zustand `create<State>()` + standalone `handleSteamBottleSetupRequiredSignal` (testable without mounting GlobalState). See 24-PATTERNS.md "SteamBridgeSetup.ts".
- `src/frontend/screens/Game/GamePage/components/SteamBottleSetup*` — the sibling dialog component + how it consumes the store's `isOpen`.
- `src/frontend/state/GlobalState.tsx` — where `steamBottleSetupRequired` listener is registered once.
- `doc/frontend_testing.md` — no-jsdom frontend test constraint (test the handler function, not a mounted component).

<interfaces>
<!-- State: { isOpen, appName?, reason?, fallbackAvailable: boolean, open(appName, opts), close() } -->
<!-- handleSteamBridgeSetupRequiredSignal(_e, { appName, reason, fallbackAvailable }) -> useSteamBridgeSetup.getState().open(...) -->
<!-- Wire to the NEW steamBridgeSetupRequired IPC event registered in 24-08 (ipcBridge.ts). -->
<!-- Fallback button: invoke the existing non-bridge install/launch path for appName; D-11 -> if Phase 17 bottle absent, provisionBottle then route. -->
<!-- i18n: add keys under a steam.bridge.* namespace (title/message/fallback/cancel) — never change existing key paths. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SteamBridgeSetup zustand store + standalone signal handler</name>
  <read_first>
    - src/frontend/state/SteamBottleSetup.ts (full — zustand + standalone handler precedent; graphify-orient first)
    - doc/frontend_testing.md (no-jsdom: test the handler, not a mounted component)
    - 24-08 SUMMARY (steamBridgeSetupRequired payload shape)
  </read_first>
  <behavior>
    - `handleSteamBridgeSetupRequiredSignal(_e, { appName, reason, fallbackAvailable })` opens the store with those fields.
    - `useSteamBridgeSetup.getState().isOpen` is false initially and true after the handler fires.
    - `close()` resets `isOpen` to false and clears `appName`.
    - `fallbackAvailable` defaults sensibly when omitted (does not throw).
  </behavior>
  <action>
    Create `src/frontend/state/SteamBridgeSetup.ts` following `SteamBottleSetup.ts` exactly: a zustand `create<SteamBridgeSetupState>()` store with `{ isOpen, appName?, reason?, fallbackAvailable, open, close }` and an exported standalone `handleSteamBridgeSetupRequiredSignal(_e, payload)` that calls `open(...)` (so it is unit-testable without mounting GlobalState). Add `src/frontend/state/__tests__/SteamBridgeSetup.test.ts` covering the behaviors above.
  </action>
  <verify>
    <automated>pnpm jest src/frontend/state/__tests__/SteamBridgeSetup.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm jest .../SteamBridgeSetup.test.ts` exits 0.
    - Handler-fires-open, close-resets, and omitted-fallbackAvailable tests pass.
    - Store follows the SteamBottleSetup shape (zustand create + standalone exported handler).
  </acceptance_criteria>
  <done>Store + testable handler; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Dialog component + GlobalState wiring + fallback/D-11 action + i18n</name>
  <read_first>
    - src/frontend/screens/Game/GamePage/components/SteamBottleSetup* (dialog component consuming isOpen — graphify-orient first)
    - src/frontend/state/GlobalState.tsx (steamBottleSetupRequired listener registration site)
    - 24-RESEARCH.md Pitfall 4 (fallback = re-route existing non-bridge branch) + D-11 (on-demand provisionBottle)
    - public/locales/en/translation.json (i18n key convention — add, never rename existing keys)
  </read_first>
  <action>
    Create `src/frontend/screens/Game/GamePage/components/SteamBridgeSetup/index.tsx` consuming `useSteamBridgeSetup` — an explicit error dialog (D-05) with a "Fall back to bottled Steam" primary action and a "Cancel" action (no silent auto-fallback, no dead-end). The fallback action invokes the EXISTING non-bridge install/launch path for `appName` (reuse the existing frontend install/launch invocation the non-bridge flow already uses — do NOT add a bespoke fallback IPC path; Pitfall 4); per D-11, if the Phase 17 bottle is not provisioned the existing guard chain's `steamBottleSetupRequired`/guided-setup (incl. `provisionBottle` on demand) handles it. Register `handleSteamBridgeSetupRequiredSignal` as the `steamBridgeSetupRequired` IPC listener once in `GlobalState.tsx` (mirroring the `steamBottleSetupRequired` registration), and mount the dialog component where `SteamBottleSetup` mounts. Add `steam.bridge.*` i18n keys (title/message/fallback/cancel) to `public/locales/en/translation.json` (add-only; run the i18n extraction if the repo requires it).
  </action>
  <verify>
    <automated>pnpm codecheck 2>&1 | tail -1 && node -e "JSON.parse(require('fs').readFileSync('public/locales/en/translation.json','utf8')); console.log('en locale valid json')"</automated>
  </verify>
  <acceptance_criteria>
    - `steamBridgeSetupRequired` is registered exactly once in `GlobalState.tsx` (grep asserts) and the dialog component is mounted alongside `SteamBottleSetup`.
    - The fallback action routes through the existing non-bridge install/launch path (no new bespoke fallback IPC channel — grep asserts it reuses the existing invoker).
    - `public/locales/en/translation.json` parses as valid JSON and contains the new `steam.bridge.*` keys (existing key paths unchanged).
    - `pnpm codecheck` (tsc + eslint) clean on the touched files.
  </acceptance_criteria>
  <done>Explicit fallback dialog wired; D-05/D-11 satisfied; codecheck + locale valid. (Runtime visual UAT is confirmed in 24-10.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend steamBridgeSetupRequired signal → frontend dialog | IPC payload drives a user-facing recovery action |
| fallback action → existing bottled-Steam path | Must not silently strand or dead-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-12 | Repudiation/DoS | silent strand on bridge failure | mitigate | D-05 explicit dialog (fall back vs cancel); no silent auto-fallback, no dead-end — R7 acceptance |
| T-24-14 | Tampering | bespoke fallback path drift | mitigate | Reuse the existing non-bridge branch + its guard chain (Pitfall 4); D-11 on-demand provision via existing `provisionBottle` — one code path, not two |
</threat_model>

<verification>
- `pnpm jest src/frontend/state/__tests__/SteamBridgeSetup.test.ts --silent` green.
- `pnpm codecheck` clean; en locale valid JSON.
- `pnpm test:ci` (wave-merge) green.
</verification>

<success_criteria>
A bridge failure surfaces an explicit fall-back-or-cancel dialog that re-routes through the proven bottled-Steam path (provisioning it on demand if needed, D-11) — never a silent hang or dead-end (R7 clean-fallback acceptance).
</success_criteria>

<output>
Create `.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-09-SUMMARY.md` when done.
</output>
