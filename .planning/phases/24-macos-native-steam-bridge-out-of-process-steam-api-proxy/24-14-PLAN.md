---
phase: 24-macos-native-steam-bridge-out-of-process-steam-api-proxy
plan: 14
type: execute
wave: 3
gap_closure: true
depends_on: ["24-11", "24-12", "24-13"]
files_modified:
  - .planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-UAT.md
autonomous: true
requirements: [R5, R6]
must_haves:
  truths:
    - "24-UAT.md Gates 2, 3, 4 are re-pointed from ⛔ BLOCKED to PENDING with a fresh retest template — the gap fixes (24-11/24-12/24-13) are cited as the reason the blockers are lifted"
    - "The retest preconditions require a packaged .app REBUILT after the gap fixes and a CLEAN reinstall of the acceptance game through GameLib (so the full fixed bridge install→shim→launch path runs), and the messy pre-fix install state is cleared first"
    - "Each re-pointed gate keeps its original Steps/Expected-result and adds an explicit per-fix verification hook (shim overwrites the game dll; install badge stays installed; launch fires a real exe) so the human recorder can attribute a pass/fail to a specific fix"
    - "The Summary Table + status line reflect Gates 2-4 as PENDING retest (not BLOCKED), Gate 1 remains PASS, and Gate 0 remains PASS"
  artifacts:
    - path: ".planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-UAT.md"
      provides: "Re-pointed Gates 2-4 retest template + updated verdict/summary reflecting the gap-cycle fixes"
      contains: "PENDING"
  key_links:
    - from: "24-UAT.md Gates 2-4 Result fields"
      to: "PENDING retest (post gap-cycle 24-11/24-12/24-13)"
      via: "BLOCKED → PENDING with fresh recording template + rebuild/clean-reinstall preconditions"
      pattern: "PENDING"
---

<objective>
Re-point `24-UAT.md` Gates 2–4 (R5/R6) from ⛔ BLOCKED back to PENDING for a fresh human-hardware retest, now that the gap cycle (24-11 shim overwrite, 24-12 install-poll reads the bridge bottle, 24-13 install-completion + launch existence-gate) has closed the bridge install→shim→launch integration cluster (D-UAT-24-04/05/02/03-cascade). These are human-hardware gates: this plan does NOT run them — it updates the prepared recording template so the developer can re-run Gates 2–4 on real Apple-Silicon hardware after the fixes ship, and can attribute each result to a specific fix.

Purpose: give the retest a clean, fix-aware template so a re-run produces an honest PASS/FAIL recording (per the 21-UAT.md/23-UAT.md precedent), and the phase can reach R5/R6 acceptance.
Output: updated `24-UAT.md` (Gates 2–4 PENDING + retest preconditions + per-fix verification hooks + refreshed verdict/summary).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-UAT.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point 24-UAT.md Gates 2-4 to a fresh, fix-aware retest</name>
  <read_first>
    - .planning/phases/.../24-UAT.md (the WHOLE file — Gates 2/3/4 blocks, the UAT Findings section D-UAT-24-02/03/04/05, the VERDICT, and the Summary Table)
    - The gap PLANs just written: 24-11-PLAN.md (shim overwrite by identity), 24-12-PLAN.md (bridge AcfSource), 24-13-PLAN.md (install-poll wiring + sticky-flag clear + launch existence-gate) — cite them as the closure of each finding
    - .planning/phases/.../24-SPEC.md (R5/R6 acceptance criteria the gates prove)
  </read_first>
  <action>
    Edit `24-UAT.md` (do NOT touch Gate 0 or Gate 1 results, both PASS): (1) In each of Gate 2, Gate 3, Gate 4, change the `**Result:**` line from BLOCKED back to `PENDING (retest after gap cycle 24-11/24-12/24-13)`, keeping the original Preconditions/Steps/Expected-result. (2) Add to each gate's Preconditions a "Retest preconditions (post gap-cycle)" note requiring: the packaged `.app` REBUILT after 24-11/24-12/24-13 land (so the fixes are in the bundle); the messy pre-fix install state cleared first (the pre-fix install left the game's own steam_api.dll + a wrong install record; since uninstall was itself affected, clear the GameLibSteamBridge bottle + library record for the acceptance appId manually before retest); a CLEAN reinstall of the acceptance game (Avernum 6 / Hoard) THROUGH GameLib so the full fixed bridge path runs (provision → depot into bridge bottle → shim OVERWRITES the game's own dll → install badge stays installed → launch fires the real exe). (3) Add a "Per-fix verification hooks" bullet list to each relevant gate so the recorder can attribute pass/fail: (D-UAT-24-04) after install, the `steam_api.dll` next to the game exe in the bridge bottle is the 805888-byte bridge shim, NOT the game's ~118368-byte copy (size/`grep 54550` check); (D-UAT-24-05) the Install badge STAYS "Installed" and does not revert (poll detected the bridge-bottle StateFlags=4 ACF); (D-UAT-24-02/03) Play launches the real exe (non-empty launch.log, game reaches main menu) and a prior single failure no longer poisons the session. (4) Update the VERDICT paragraph to note the gap cycle closed D-UAT-24-04/05/02/03-cascade and Gates 2–4 are re-pointed to PENDING retest. (5) Update the Summary Table rows for Gates 2/3/4 from ⛔ BLOCKED to `PENDING (retest)` with a note citing the gap plans, and update the closing "Gate status" line to reflect Gates 2–4 PENDING retest (Gate 1 PASS, Gate 0 PASS). Keep the existing D-UAT findings text intact (they are the historical record) — only the Result/verdict/summary lines move from BLOCKED to PENDING.
  </action>
  <verify>
    <automated>grep -c "PENDING" .planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Gates 2, 3, 4 each show `**Result:** PENDING` (retest), and NONE of them still reads BLOCKED as its live Result (`grep -n "BLOCKED" 24-UAT.md` returns only historical/summary-note context, not a live Gate Result line).
    - Each of Gates 2/3/4 has a "Retest preconditions (post gap-cycle)" note naming: rebuild the .app after 24-11/24-12/24-13, clear the messy pre-fix bridge-bottle state, clean reinstall through GameLib.
    - Each relevant gate has per-fix verification hooks for D-UAT-24-04 (shim overwrites the game dll: 805888 vs 118368 bytes), D-UAT-24-05 (badge stays Installed), D-UAT-24-02/03 (launch fires a real exe, session not poisoned).
    - Gate 0 and Gate 1 results are unchanged (still PASS).
    - The Summary Table shows Gates 2/3/4 as PENDING (retest) citing the gap plans; the "Gate status" line reflects Gates 2–4 PENDING retest.
  </acceptance_criteria>
  <done>24-UAT.md Gates 2–4 are re-pointed to a fresh, fix-aware PENDING retest; verdict + summary updated; Gate 0/1 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT recording integrity | A gate must not be silently marked passed without a real hardware run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-21 | Repudiation | UAT gate falsely recorded as passed | mitigate | Gates re-pointed to PENDING (not PASS) — a human hardware run must record the actual outcome + evidence; per-fix hooks make an unearned pass detectable |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — documentation-only edit to 24-UAT.md |
</threat_model>

<verification>
- `grep -n "PENDING" 24-UAT.md` shows Gates 2/3/4 re-pointed; `grep -n "BLOCKED" 24-UAT.md` shows no live Gate Result line still BLOCKED.
- Gate 0/Gate 1 PASS records unchanged.
- Human retest is run later via `/gsd-execute-phase 24` (Gates 2–4) or `/gsd-verify-work 24` on real Apple-Silicon hardware; any FAIL routes to a further gap cycle.
</verification>

<success_criteria>
Gates 2–4 are ready to re-run on real hardware with a clean, fix-aware template that attributes each result to the specific gap fix — restoring a path to R5/R6 acceptance. The phase completes only when the human re-run records Gates 1–4 all PASS (21-UAT.md/23-UAT.md precedent).
</success_criteria>

<output>
Create `.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-14-SUMMARY.md` when done.
</output>
