---
phase: 24-macos-native-steam-bridge-out-of-process-steam-api-proxy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/storeManagers/steam/bridge/protocol.ts
  - src/backend/storeManagers/steam/bridge/__tests__/protocol.test.ts
  - native/steam-bridge/helper/bridge_helper.c
autonomous: true
requirements: [R2]
must_haves:
  truths:
    - "A native arm64 helper dlopens libsteam_api.dylib and calls SteamAPI_InitFlat exactly ONCE"
    - "The helper binds loopback-only (INADDR_LOOPBACK), never INADDR_ANY"
    - "The helper serves >=2 sequential requests over a single persistent connection without re-initializing"
    - "The helper carries a single generic identity-only AppID (480/Spacewar) for its whole lifetime (D-04)"
    - "protocol.ts encodes/decodes the shared binary frame that shim and helper both speak"
  artifacts:
    - path: "native/steam-bridge/helper/bridge_helper.c"
      provides: "R2 persistent-channel native host helper (D-03/D-04)"
    - path: "src/backend/storeManagers/steam/bridge/protocol.ts"
      provides: "Shared wire-frame encode/decode + constants (documents the C wire layout)"
      min_lines: 40
    - path: "src/backend/storeManagers/steam/bridge/__tests__/protocol.test.ts"
      provides: "Frame round-trip + max-frame-bound tests"
  key_links:
    - from: "native/steam-bridge/helper/bridge_helper.c"
      to: "libsteam_api.dylib"
      via: "dlopen + SteamAPI_InitFlat once at startup"
      pattern: "InitFlat"
    - from: "native/steam-bridge/helper/bridge_helper.c"
      to: "127.0.0.1 loopback listener"
      via: "INADDR_LOOPBACK bind"
      pattern: "INADDR_LOOPBACK"
---

<objective>
Build the R2 native arm64 host helper and the shared wire protocol. The helper `dlopen`s the real `libsteam_api.dylib`, calls `SteamAPI_InitFlat` ONCE at startup (D-04 — the flat init takes no AppID; the helper carries one generic identity-only AppID `480`/Spacewar for its whole life, resolved via `steam_appid.txt` in its working dir), holds the inited `ISteamUser`/`ISteamFriends` pointers, and serves marshaled requests over a single persistent `127.0.0.1` loopback-only connection.

This upgrades spike 005b's connect-per-call `bridge_server.c` to D-03's persistent-channel design, replacing the human-readable PING/WHOAMI text protocol with the fixed binary frame from 24-RESEARCH.md Pattern 3. `protocol.ts` is the TypeScript-side definition of that frame (used by the readiness probe in 24-06 and by tests/docs — the real wire code lives in the C on both ends, this documents and validates the layout).

Purpose: the lowest-level runtime building block — the one live Steam identity source every bridge game shares.
Output: `bridge_helper.c` (persistent, loopback-only, init-once) + `protocol.ts` + frame tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-SPEC.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-RESEARCH.md
@.claude/skills/spike-findings-gamelib/references/macos-steam-bridge.md

**MANDATORY graphify rule:** run `graphify query`/`explain`/`path` before reading any raw source.

**Authoritative "how" sources (native C — no in-repo analog; read directly):**
- `.claude/skills/spike-findings-gamelib/sources/005b-bottle-to-host-tcp/bridge_server.c` — the ACTUAL code every 005/006/007/008 spike ran. Source of the D-04 finding (`InitFlat(err)` with NO AppID argument) and the `INADDR_LOOPBACK` bind. MUST be upgraded from connect-per-call to persistent-channel (D-03).
- 24-RESEARCH.md Pattern 1 (D-04 resolution — why single generic AppID 480, why init-once), Pattern 3 (the binary frame layout — SOURCE OF TRUTH), Code Examples (init-once, no-AppID-parameter).

<interfaces>
<!-- Wire frame — SHARED with the generated shim (Plan 24-01). SOURCE OF TRUTH: 24-RESEARCH.md Pattern 3. -->
<!-- Request:  [4B LE total_len][4B LE request_id][2B LE interface_ordinal][2B LE method_slot][N argblob] -->
<!-- Response: [4B LE total_len][4B LE request_id][1B status: 0=ok,1=err/no-live-session][N return blob] -->
<!-- interface ordinals: 0=flat API, 1=ISteamUser, 2=ISteamFriends. slot = manifest declaration order. -->
<!-- request_id correlates responses on the persistent connection (future pipelining; only >=2 sequential needed this phase). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: protocol.ts — shared wire-frame encode/decode + bounds</name>
  <read_first>
    - 24-RESEARCH.md Pattern 3 (frame layout — the authoritative spec)
    - src/backend/storeManagers/steam/clientSetup.ts (for the LogPrefix.Steam + result-shape house style; graphify-orient first)
  </read_first>
  <behavior>
    - encodeRequest(request_id, interfaceOrdinal, slot, argBlob) → Buffer whose first 4 LE bytes equal the total frame length.
    - decodeResponse(buffer) round-trips a value written by encodeResponse (request_id + status + return blob recovered exactly).
    - A frame whose declared length exceeds MAX_FRAME_BYTES is rejected by the decoder (throws / returns error) — never allocates past the cap (T-24-03).
    - A truncated frame (fewer bytes than declared length) is reported as incomplete, not silently accepted.
  </behavior>
  <action>
    Create `src/backend/storeManagers/steam/bridge/protocol.ts` exporting the frame constants (offsets, `INTERFACE_ORDINAL` map `{ flat:0, user:1, friends:2 }`, a `MAX_FRAME_BYTES` cap) and pure `encodeRequest`/`decodeRequest`/`encodeResponse`/`decodeResponse` functions using little-endian `Buffer` reads/writes exactly per 24-RESEARCH.md Pattern 3. The decoder MUST bounds-check the declared length against `MAX_FRAME_BYTES` before allocating and MUST report truncation. This TS module documents and validates the layout the C on both ends implements; it is imported by the 24-06 readiness probe. Write `__tests__/protocol.test.ts` covering the behaviors above.
  </action>
  <verify>
    <automated>pnpm jest src/backend/storeManagers/steam/bridge/__tests__/protocol.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm jest .../protocol.test.ts` exits 0.
    - Round-trip test: `decodeResponse(encodeResponse(...))` recovers request_id/status/blob byte-for-byte.
    - Oversized-length frame is rejected without allocating past `MAX_FRAME_BYTES`.
    - `INTERFACE_ORDINAL` maps flat=0, user=1, friends=2 matching the generator's ordinals (24-01).
  </acceptance_criteria>
  <done>protocol.ts exports encode/decode + bounds, tests green.</done>
</task>

<task type="auto">
  <name>Task 2: bridge_helper.c — persistent, loopback-only, init-once native helper</name>
  <read_first>
    - .claude/skills/spike-findings-gamelib/sources/005b-bottle-to-host-tcp/bridge_server.c (the connect-per-call original to upgrade)
    - 24-RESEARCH.md Pattern 1 (D-04: InitFlat once, no AppID, single 480 identity), Pattern 3 (frame), Code Examples
    - .claude/skills/spike-findings-gamelib/references/macos-steam-bridge.md (loopback-only, AppID-before-init, SDK pinning constraints)
  </read_first>
  <action>
    Create `native/steam-bridge/helper/bridge_helper.c`: `dlopen` the real `libsteam_api.dylib`, call `SteamAPI_InitFlat(err)` EXACTLY ONCE at startup (D-04 — no AppID parameter; identity resolves from a `steam_appid.txt` containing `480` in the helper's working directory, per Pattern 1), resolve and hold `SteamAPI_SteamUser()`/`SteamAPI_SteamFriends()` (or the flat accessor equivalents) for the process lifetime. Bind a listener on `INADDR_LOOPBACK` only (never `INADDR_ANY` — R2 acceptance + T-24-01), `accept` a connection, and serve requests in a persistent read-loop that decodes the Pattern 3 request frame, dispatches by interface ordinal + slot (identity methods: `ISteamUser::GetSteamID`→real SteamID64 as bytes, `BLoggedOn`, `ISteamFriends::GetPersonaName`), and writes the Pattern 3 response frame — WITHOUT re-initializing between requests (serve ≥2 sequential requests on one connection, R2 acceptance). Enforce the `MAX_FRAME_BYTES` cap on inbound frame length before reading the body (T-24-03). Reject/abort on a malformed frame rather than reading unbounded. Keep the SteamID64 as raw bytes end-to-end (never parse to a JS number — Constraint: strings/64-bit end to end). This is compiled by clang at packaging time (Plan 24-07), not here.
  </action>
  <verify>
    <automated>grep -q "INADDR_LOOPBACK" native/steam-bridge/helper/bridge_helper.c && ! grep -q "INADDR_ANY" native/steam-bridge/helper/bridge_helper.c && grep -c "InitFlat" native/steam-bridge/helper/bridge_helper.c | grep -qv '^0$' && echo "loopback-only, init present"</automated>
  </verify>
  <acceptance_criteria>
    - Source contains `INADDR_LOOPBACK` and does NOT contain `INADDR_ANY` (R2 non-loopback-bind-absent acceptance; T-24-01).
    - `SteamAPI_InitFlat` is called exactly once (init is outside the accept/read loop — structural: init call precedes the `listen`/accept loop, not inside it).
    - The read loop decodes/encodes the 24-RESEARCH.md Pattern 3 frame and bounds inbound length against a max-frame cap before reading the body.
    - No `dlopen` of a second `libsteam_api.dylib` handle; one identity for the process lifetime (D-04).
  </acceptance_criteria>
  <done>Persistent-channel, loopback-only, init-once helper source committed (compiled later in 24-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bottle PE32 shim → helper (127.0.0.1 TCP) | Any local process/user can connect to the loopback listener |
| inbound wire frames | Attacker-controllable frame length/body on the persistent channel |
| helper → libsteam_api.dylib / live Mac Steam | Helper proxies the real signed-in account's identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure/Spoofing | helper socket bind | mitigate | `INADDR_LOOPBACK` only, never `INADDR_ANY` (verify grep asserts both); R2 acceptance requires a non-loopback bind to be absent |
| T-24-02 | Information Disclosure | unauthenticated localhost peers | accept | Loopback-only bind is the sole mitigation named by SPEC + blueprint; no locked decision calls for a handshake/nonce. Record as accepted residual risk at secure-phase (do not silently expand scope) |
| T-24-03 | DoS/Tampering | inbound frame length/body | mitigate | Enforce `MAX_FRAME_BYTES` cap in protocol.ts decoder AND in the C read loop before allocating/reading the body; reject malformed frames |
| T-24-SC | Tampering | libsteam_api.dylib load path | mitigate | `dlopen` the OS/Steam-provided canonical dylib path only; no attacker-supplied load path |
</threat_model>

<verification>
- `pnpm jest src/backend/storeManagers/steam/bridge/__tests__/protocol.test.ts --silent` green.
- Helper source: `INADDR_LOOPBACK` present, `INADDR_ANY` absent, single `InitFlat`.
- `pnpm codecheck` clean on `protocol.ts`.
</verification>

<success_criteria>
The native helper binds loopback-only, initializes once against the live Mac Steam, and serves ≥2 sequential requests over one persistent connection without re-initializing (R2 automated + structural rows; the live-Steam init and real-SteamID64 round-trip are the human-HW rows deferred to 24-10). protocol.ts encodes/decodes the shared frame with a max-frame bound.
</success_criteria>

<output>
Create `.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-02-SUMMARY.md` when done.
</output>
