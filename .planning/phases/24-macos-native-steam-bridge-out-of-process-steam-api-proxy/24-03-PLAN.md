---
phase: 24-macos-native-steam-bridge-out-of-process-steam-api-proxy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/storeManagers/steam/bridge/bridge-allowlist.json
  - src/backend/storeManagers/steam/bridge/allowlist.ts
  - src/backend/storeManagers/steam/bridge/__tests__/allowlist.test.ts
autonomous: true
requirements: [R4]
must_haves:
  truths:
    - "The bridge-eligibility allowlist is a bundled JSON file keyed by AppID (D-01)"
    - "Each entry carries at least AppID + title metadata to drive shim generation/validation (D-02)"
    - "A malformed allowlist entry throws at load (fail-loud) — curated/reviewed posture"
    - "Avernum 4 and Hoard (the acceptance set) are present in the shipped allowlist"
  artifacts:
    - path: "src/backend/storeManagers/steam/bridge/bridge-allowlist.json"
      provides: "Bundled curated AppID allowlist (D-01/D-02)"
      contains: "version"
    - path: "src/backend/storeManagers/steam/bridge/allowlist.ts"
      provides: "zod-validated loader + has(appId) lookup"
      min_lines: 30
    - path: "src/backend/storeManagers/steam/bridge/__tests__/allowlist.test.ts"
      provides: "Schema-validation + lookup tests"
  key_links:
    - from: "src/backend/storeManagers/steam/bridge/allowlist.ts"
      to: "bridge-allowlist.json"
      via: "readFileSync + JSON.parse + zod .parse() at module load"
      pattern: "bridge-allowlist"
---

<objective>
Ship the R4 bridge-eligibility allowlist: a bundled JSON file keyed by AppID (D-01 — chosen over a hardcoded constant and over a remote/CI index; the Phase 19 CI-index pattern is a documented future upgrade path, NOT built now) plus a zod-validated loader exposing a `has(appId)` lookup. Entries carry AppID + human-readable title + room for a per-game export-set note (D-02). Validation follows the exact `crossoverIndexSchema` precedent; a malformed entry throws at load (fail-loud — this is a small, developer-curated list, unlike the 1000+-entry CrossOver index).

Purpose: the routing decision's data source (consumed by `isBridgeEligible()` in 24-08).
Output: `bridge-allowlist.json` (with Avernum 4 + Hoard) + `allowlist.ts` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-SPEC.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-RESEARCH.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-PATTERNS.md

**MANDATORY graphify rule:** run `graphify query`/`explain`/`path` before reading any raw source.

**Analog (read via graphify first, then the file):**
- `src/backend/crossover_index/schema.ts` — the exact zod-validation precedent (`z.object` + `version: z.literal(1)` guard + typed inference). See 24-PATTERNS.md "allowlist.ts" section for the full pattern.

<interfaces>
<!-- AppId numeric guard reused project-wide (bottle.ts:806, clientSetup.ts:40) — reuse the SAME constant, do not redefine: -->
<!-- const NUMERIC_APP_ID = /^\d+$/ -->

<!-- Allowlist JSON shape (D-01/D-02): -->
<!-- { "version": 1, "games": [ { "appId": "1477890", "title": "Avernum 4", "note"?: "2 steam_api imports" }, ... ] } -->
<!-- Acceptance set AppIDs: confirm exact AppIDs for Avernum 4 and Hoard from the spike sources -->
<!-- (.claude/skills/spike-findings-gamelib/sources/007-real-game-avernum/ and .../008-gating-game-hoard/). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: zod-validated allowlist loader + bundled JSON (D-01/D-02)</name>
  <read_first>
    - src/backend/crossover_index/schema.ts (zod precedent — graphify-orient first)
    - src/backend/storeManagers/steam/clientSetup.ts (NUMERIC_APP_ID convention, LogPrefix.Steam house style)
    - .claude/skills/spike-findings-gamelib/sources/007-real-game-avernum/README.md and .../008-gating-game-hoard/README.md (confirm the two acceptance-set AppIDs + import counts)
    - 24-PATTERNS.md "allowlist.ts" section (D-01/D-02 apply-notes)
  </read_first>
  <behavior>
    - Loading a well-formed `bridge-allowlist.json` parses to a typed object and `has("<known appId>")` returns true.
    - `has("<absent appId>")` returns false.
    - `has(appId)` for a non-numeric appId returns false (NUMERIC_APP_ID guard) — never throws on lookup.
    - A JSON with a missing required field (e.g. entry without `appId`) or wrong `version` throws at parse time (fail-loud), not silently drops.
    - Both Avernum 4 and Hoard AppIDs are present and `has()` returns true for each.
  </behavior>
  <action>
    Create `src/backend/storeManagers/steam/bridge/bridge-allowlist.json` (`version: 1`, a `games` array with `{ appId, title, note? }` entries for Avernum 4 and Hoard using their real AppIDs from the spike sources — D-01/D-02). Create `src/backend/storeManagers/steam/bridge/allowlist.ts` defining `bridgeAllowlistSchema` following `crossoverIndexSchema`'s shape (`z.object({ version: z.literal(1), games: z.array(z.object({ appId: z.string().regex(/^\d+$/), title: z.string().min(1), note: z.string().optional() })).min(1) })`), `readFileSync` + `JSON.parse` + `.parse()` at module load (throws loud on malformed — D-02 curated posture, `.min(1)` not `.min(1000)`), and export a `bridgeAllowlist` object with a `has(appId: string): boolean` method that numeric-guards the appId with the shared `NUMERIC_APP_ID` regex before lookup. Export the inferred `BridgeAllowlist` type. Bundle the JSON so it is available in the packaged app (co-located in the same directory — reads at runtime via `join(__dirname, 'bridge-allowlist.json')` or the project's bundled-asset convention).
  </action>
  <verify>
    <automated>pnpm jest src/backend/storeManagers/steam/bridge/__tests__/allowlist.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm jest .../allowlist.test.ts` exits 0.
    - Test asserts `bridgeAllowlist.has(<Avernum4 appId>)===true` and `has(<Hoard appId>)===true`.
    - Test asserts a malformed JSON (missing `appId`, or `version:2`) causes `.parse()` to throw (fed via a fixture / direct schema call).
    - Test asserts `has("not-a-number")===false` (guard) and `has(<absent numeric>)===false`.
    - `allowlist.ts` reuses the shared `NUMERIC_APP_ID = /^\d+$/` value (not a divergent regex).
  </acceptance_criteria>
  <done>Loader + bundled JSON + tests green; acceptance set present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled allowlist JSON → routing decision | Static asset drives a bridge-vs-fallback routing decision |
| appId (IPC-adjacent) → allowlist.has() | Untrusted appId flows into the lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | bridge-allowlist.json integrity | mitigate | zod `bridgeAllowlistSchema` with `version: z.literal(1)` + required-field bounds; malformed → throws at load (fail-loud); test asserts rejection |
| T-24-07 | Tampering | appId into `has()` | mitigate | `NUMERIC_APP_ID = /^\d+$/` guard before lookup (reuse project-wide constant); non-numeric returns false, never crashes routing |
</threat_model>

<verification>
- `pnpm jest src/backend/storeManagers/steam/bridge/__tests__/allowlist.test.ts --silent` green.
- `pnpm codecheck` clean on `allowlist.ts`.
</verification>

<success_criteria>
Allowlist JSON is zod-validated (malformed rejected), keyed by AppID (D-01/D-02), contains the Avernum 4 + Hoard acceptance set, and exposes a guarded `has(appId)` lookup (R4 allowlist-validation row).
</success_criteria>

<output>
Create `.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-03-SUMMARY.md` when done.
</output>
