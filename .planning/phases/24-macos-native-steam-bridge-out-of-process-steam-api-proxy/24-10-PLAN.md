---
phase: 24-macos-native-steam-bridge-out-of-process-steam-api-proxy
plan: 10
type: execute
wave: 5
depends_on: ["24-07", "24-08", "24-09"]
files_modified:
  - .planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-UAT.md
autonomous: false
requirements: [R1, R5, R6]
must_haves:
  truths:
    - "A packaged .app (not a dev yarn run) launches an allowlisted game via the BUNDLED helper — no externally staged binary (R5)"
    - "Avernum 4 launches from GameLib via the bridge and reaches playable single-player with the real SteamID64 + correct persona (R6)"
    - "Hoard launches from GameLib via the bridge and reaches playable single-player with the real SteamID64 + correct persona (R6)"
    - "Each acceptance game's bottle contains no steam.exe / Windows Steam client (R6)"
    - "A 006-style vtable round-trip gate runs the GENERATED shim against the PRODUCTION helper and confirms a C++ virtual GetSteamID() returns the real signed-in SteamID64 — the ONLY thing that proves R1's runtime ABI acceptance independent of whether the acceptance games ever call the vtable (review findings #5b/#13)"
    - "R6 evidence separates 'SteamAPI_Init succeeded through the bridge' from 'real SteamID64 + persona were SERVED through the bridge' — a 2-import game (Avernum 4) may satisfy the former without the latter, so identity proof leans on the round-trip gate (review finding #6)"
  artifacts:
    - path: ".planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-UAT.md"
      provides: "Recorded PASS/FAIL for the human-HW-gated R5/R6 acceptance set (21-UAT.md/23-UAT.md precedent)"
  key_links:
    - from: "packaged GameLib .app"
      to: "bundled steam-bridge-helper"
      via: "runtime spawn from the app bundle (no staged binary)"
      pattern: "bundled helper"
    - from: "generated steam_api.dll shim (24-01 source, 24-07 built)"
      to: "production steam-bridge-helper (24-02)"
      via: "006-style vtable GetSteamID() round-trip → real SteamID64 (R1 runtime acceptance)"
      pattern: "vtable round-trip"
---

<objective>
Record the human-hardware-gated acceptance for R1 (runtime vtable round-trip), R5, and R6 on the developer's own Apple-Silicon Mac (per 24-VALIDATION.md — these cannot be automated: they require a packaged build, live signed-in Mac Steam, real games, and dev HW). This plan produces `24-UAT.md` (mirroring the `21-UAT.md`/`23-UAT.md` precedent) with explicit PASS/FAIL gates. It adds a 006-style vtable round-trip gate (review findings #5b/#13) — running the GENERATED shim against the PRODUCTION helper to prove a C++ virtual `GetSteamID()` returns the real SteamID64 — because the acceptance games (Avernum 4 = 2 imports) may never exercise the vtable identity path the generator exists to produce, leaving R1's runtime ABI otherwise unvalidated until here. Every prior plan's automated suite must be green (including 24-07's zig-cc compile gate) before this runs.

Purpose: prove the bridge works end-to-end through GameLib from a packaged build with real Steam identity — the phase's ultimate acceptance bar.
Output: `24-UAT.md` with recorded outcomes for the four human gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-SPEC.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-VALIDATION.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-07-SUMMARY.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-08-SUMMARY.md
@.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-09-SUMMARY.md

**MANDATORY graphify rule:** any code exploration during triage of a failed gate must start from `graphify query`/`explain`/`path`.

**Precedent:** `.planning/phases/21-steam-native-install/21-UAT.md` and `.planning/phases/23-steam-full-ownership-install-stateflags-4/23-UAT.md` — the recorded-gate format to follow.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate + build packaged app + author 24-UAT.md gate scaffold</name>
  <read_first>
    - .planning/phases/21-steam-native-install/21-UAT.md (gate format)
    - 24-VALIDATION.md Manual-Only Verifications (the four human rows)
    - package.json dist:mac/release:mac scripts (24-07 wired build-steam-bridge into them)
  </read_first>
  <action>
    Confirm the full automated suite is green (`pnpm test:ci`) and `pnpm codecheck` is clean — this human gate does not run until every prior plan's tests pass. Build the packaged app via the `dist:mac` script (which now runs `build-steam-bridge` first per 24-07, producing the bundled arm64 helper + PE shim). Author `.planning/phases/.../24-UAT.md` with FOUR explicit gates (below: the vtable round-trip R1 gate + R5 packaged-helper + R6 Avernum 4 + R6 Hoard), each with exact steps and a PASS/FAIL + evidence field, following the 21-UAT.md/23-UAT.md structure.
  </action>
  <verify>
    <automated>pnpm test:ci --silent 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:ci` exits 0 (full suite green) and `pnpm codecheck` clean.
    - A packaged `.app` is produced by `dist:mac` and the bundled helper exists at `<app>/Contents/.../build/bin/arm64/darwin/steam-bridge-helper`.
    - `24-UAT.md` exists with the four gates below (round-trip R1, R5, R6 Avernum 4, R6 Hoard), each with steps + PASS/FAIL + evidence fields.
  </acceptance_criteria>
  <done>Suite green, packaged build produced, UAT scaffold authored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>R1 runtime vtable round-trip (review findings #5b/#13). The GENERATED shim (24-01 source, built by 24-07's zig cc) is exercised against the PRODUCTION helper (24-02) via a 006-style harness. This is the only gate that proves the hand-authored ABI (`__thiscall`, `ret N`, sret, slot offsets) is correct at RUNTIME through a real C++ virtual call — independent of whether Avernum 4 / Hoard ever call the vtable identity methods.</what-built>
  <how-to-verify>
    1. On your Apple-Silicon Mac with the native Steam client signed in, ensure the packaged/built bridge helper is running (or start it) and the generated `steam_api.dll` shim from 24-07's build is available.
    2. Run the 006-style round-trip harness (resurrected from `.claude/skills/spike-findings-gamelib/sources/006-cpp-vtable-abi/`) that loads the GENERATED shim, obtains the `ISteamUser` interface, and makes a C++ VIRTUAL `GetSteamID()` call through the vtable (not just a flat `SteamAPI_*` export).
    3. Confirm the returned SteamID64 equals your REAL signed-in SteamID64 (string-compared end to end — never parsed through a JS number).
    4. Optionally repeat for `ISteamFriends::GetPersonaName()` to confirm a second slot/`ret N` is correct.
  </how-to-verify>
  <acceptance_criteria>
    - A C++ virtual `GetSteamID()` through the GENERATED shim's vtable returns the real signed-in SteamID64 (proves slot offset + `__thiscall` + `ret N` + EDX:EAX 64-bit return are correct at runtime — R1 acceptance).
    - The value matches the live account's SteamID64 exactly (string equality), served through the production helper.
    - Record PASS/FAIL + the observed SteamID64 (and persona if run) in 24-UAT.md. A FAIL here localizes an ABI/generator bug BEFORE the game gates (findings #5b/#13).
  </acceptance_criteria>
  <resume-signal>Type "approved" (record PASS) or describe the failure (record FAIL → /gsd-plan-phase 24 --gaps).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>R5 packaged-build bundled-helper acceptance. The packaged GameLib `.app` (from `dist:mac`, not a dev `yarn`/`vite` run) bundles the arm64 helper and locates/launches it from the app bundle at runtime.</what-built>
  <how-to-verify>
    1. On your Apple-Silicon Mac with the native Steam client installed and signed in, quit any dev GameLib instance and remove any externally staged `steam-bridge-helper` from your PATH / working dirs.
    2. Launch the PACKAGED `.app` produced in Task 1.
    3. Launch an allowlisted game (Avernum 4 or Hoard) so the bridge helper is spawned.
    4. Confirm (Activity Monitor / logs) that the running `steam-bridge-helper` process path resolves INSIDE the packaged `.app` bundle (not a dev/staged path).
  </how-to-verify>
  <acceptance_criteria>
    - A packaged `.app` (not a dev run) launches an allowlisted game through the bridge using the BUNDLED helper.
    - No externally staged helper binary is present or used — the helper path is inside the `.app` bundle.
    - Record PASS/FAIL + the observed helper process path in 24-UAT.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" (record PASS) or describe the failure (record FAIL → routes to /gsd-plan-phase 24 --gaps).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>R6 Avernum 4 single-player launch parity through the bridge (real Steam identity, no bottled Windows Steam client).</what-built>
  <how-to-verify>
    1. From the packaged GameLib, install (if needed) and launch Avernum 4 on macOS.
    2. Confirm the game reaches main menu / playable single-player.
    3. Confirm (part A) logs show `SteamAPI_Init`/`SteamAPI_InitFlat` succeeded THROUGH the bridge (the flat init crossed the loopback channel to the helper).
    4. Confirm (part B — identity served) IF Avernum 4 makes an identity call, logs show the real signed-in SteamID64 + correct persona crossed the bridge. NOTE (finding #6): Avernum 4 imports only 2 symbols (Init/Shutdown) and may NEVER request SteamID/persona — in that case part B is legitimately N/A here, and the real-identity proof is the separate vtable round-trip gate above (which does not depend on the game calling the vtable). Do not record a FAIL for part B solely because a 2-import game never asked for identity.
    5. Inspect Avernum 4's bridge bottle: confirm NO `steam.exe` / Windows Steam client is present (e.g. `ls` the bottle's `drive_c/Program Files (x86)/Steam/`).
  </how-to-verify>
  <acceptance_criteria>
    - Avernum 4 reaches playable single-player launched from GameLib via the bridge.
    - Part A: `SteamAPI_Init` succeeded through the bridge (logged). Part B: real SteamID64 + persona served through the bridge IF the game requests them; else N/A with the vtable round-trip gate standing as the identity proof (finding #6).
    - The game's bottle contains no `steam.exe` / Windows Steam client.
    - Record PASS/FAIL per part + evidence (init-through-bridge log, SteamID64/persona if served, bottle listing) in 24-UAT.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" (record PASS) or describe the failure (record FAIL → /gsd-plan-phase 24 --gaps).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>R6 Hoard single-player launch parity through the bridge (real Steam identity, no bottled Windows Steam client). Hoard exercises a larger interface-accessor surface (7 imports) than Avernum 4 (2) — a real vtable `ret N` regression would show here first (Pitfall 2).</what-built>
  <how-to-verify>
    1. From the packaged GameLib, install (if needed) and launch Hoard on macOS.
    2. Confirm the game reaches main menu / playable single-player.
    3. Confirm (part A) logs show `SteamAPI_Init` succeeded THROUGH the bridge, AND (part B) — Hoard's 7-import surface makes an identity call likely — logs show the real signed-in SteamID64 + correct persona served through the bridge. If Hoard exercises a vtable identity accessor, a `ret N` regression would surface here first (Pitfall 2), corroborating the round-trip gate.
    4. Inspect Hoard's bridge bottle: confirm NO `steam.exe` / Windows Steam client is present.
  </how-to-verify>
  <acceptance_criteria>
    - Hoard reaches playable single-player launched from GameLib via the bridge.
    - Part A: `SteamAPI_Init` succeeded through the bridge. Part B: real SteamID64 + persona served through the bridge (Hoard's larger import surface is the game most likely to actually exercise identity — finding #6).
    - The game's bottle contains no `steam.exe` / Windows Steam client.
    - Record PASS/FAIL per part + evidence in 24-UAT.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" (record PASS) or describe the failure (record FAIL → /gsd-plan-phase 24 --gaps).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged app → bundled helper | Runtime must resolve the helper from the signed app bundle, not a staged binary |
| live Mac Steam identity served through the bridge | Real SteamID64/persona crosses the loopback channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-11 | Tampering | bundled helper provenance | mitigate | Verify the running helper path is inside the packaged `.app` (R5 gate) — no externally staged/attacker-substituted binary |
| T-24-02 | Information Disclosure | real SteamID64 over loopback | accept | Accepted residual loopback-only risk (recorded at secure-phase); acceptance run confirms the identity is the user's own live session |
| T-24-SC | Tampering | packaging supply chain | mitigate | Helper + shim built from committed source via pinned zig (24-07); packaged build is the artifact under test |
</threat_model>

<verification>
- `pnpm test:ci` full suite green (and 24-07's zig-cc compile gate passed) before any human gate.
- Packaged `.app` produced; bundled helper present in the bundle.
- 24-UAT.md records PASS/FAIL + evidence for all FOUR gates (vtable round-trip R1, R5, R6 Avernum 4, R6 Hoard).
</verification>

<success_criteria>
R1: a C++ virtual GetSteamID() through the generated shim's vtable returns the real SteamID64 against the production helper (runtime ABI acceptance). R5: a packaged build runs an allowlisted game via the bundled helper on the dev's Mac. R6: both Avernum 4 and Hoard reach playable single-player through the bridge, with 'init through the bridge' and 'identity served' recorded separately, and no Windows Steam client in the bottle. Any FAIL routes to `/gsd-plan-phase 24 --gaps`; the phase is not complete until all four gates PASS (21-UAT.md/23-UAT.md precedent).
</success_criteria>

<output>
Create `.planning/phases/24-macos-native-steam-bridge-out-of-process-steam-api-proxy/24-10-SUMMARY.md` when done. The recorded gates live in `24-UAT.md`.
</output>
