---
phase: 21-steam-native-install
plan: 12
type: execute
wave: 8
depends_on: ['21-05', '21-06', '21-07', '21-08', '21-09', '21-10', '21-11']
files_modified:
  - .planning/phases/21-steam-native-install/21-UAT.md
autonomous: false
requirements: [SNI-01, SNI-04, SNI-08]
must_haves:
  truths:
    - "A native GameLib depot install is adopted by real Steam (StateFlags 1026→4, zero re-download) and the game launches — including a confirmed hard-DRM title"
    - "A large (10GB+) real game downloads with bounded memory and byte-correct files (streaming-to-disk proven at scale)"
    - "A real multi-depot game downloads all depots with a correct summed total and no file collisions"
    - "The bottled Windows Steam adopts a GameLib-written 1026 manifest identically to native Steam (D-15)"
  artifacts:
    - path: ".planning/phases/21-steam-native-install/21-UAT.md"
      provides: "recorded real-machine validation results for the five MUST-VALIDATE items"
  key_links:
    - from: "GameLib depot install"
      to: "real Steam client verify-repair pass"
      via: "1026 manifest adoption on a real machine"
      pattern: "1026"
---

<objective>
Close the five MUST-VALIDATE items that are genuinely empirical and cannot be resolved by unit tests
(RESEARCH Validation Architecture: these rows are manual-only, real-machine). This plan is entirely
human-verify checkpoints run against a real, authenticated Steam account: native .acf adoption on a
confirmed hard-DRM title, streaming-to-disk at 10GB+ scale, a real multi-depot game, and bottled Steam
adoption on macOS. Each result is recorded in 21-UAT.md. No plan may claim SNI-01/SNI-08 complete
until these pass — they are the difference between "unit-green" and "actually installs games."

Purpose: SNI-01 (streaming@scale, multi-depot), SNI-04 (adoption/recovery on real Steam), SNI-08
(bottle adoption) — the real-machine gate.
Output: 21-UAT.md with recorded pass/fail + evidence for each MUST-VALIDATE item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-steam-native-install/21-RESEARCH.md
@.planning/phases/21-steam-native-install/21-CONTEXT.md
@.planning/spikes/MANIFEST.md

<interfaces>
- RESEARCH §Validation Architecture "Phase Requirement → Test Map" manual-only rows + §Open Questions
  1/2/3 + §Assumptions Log A1/A2/A3 define exactly what to test and why each is empirical.
- Spike 001 procedure (native adoption): write 1026 manifest, restart Steam, observe StateFlags flip
  to 4 with zero re-download, launch via steam://rungameid. Re-run against a GameLib install.
- Assumption A3 (bottle): re-run spike 001's procedure with the target = getBottleSteamappsDir()
  instead of native defaultSteamPath.
- Assumption A1 (streaming): download a real 10GB+ depot; profile RSS stays bounded (O(concurrency ×
  chunk)); verify byte-correctness (SHA1 vs Steam's own install where feasible).
- Assumption A2 (multi-depot): a real multi-depot game (e.g. Wasteland 3 / Dead Island); confirm all
  depots download, summed total is correct, and no cross-depot file collision corrupts the install.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Native adoption + hard-DRM launch on real Steam</name>
  <action>Pause for the developer to run the native-adoption + hard-DRM + cancel-recovery validation on real Steam per the steps below; record results in 21-UAT.md and resume on approval.</action>
  <what-built>The full native path (Plans 04-09): opt-in ON, depot download into a registered Steam library, 1026 manifest write, poller watching for Steam's flip.</what-built>
  <how-to-verify>
    1. With the opt-in ON, install a game via GameLib on a machine with a real authenticated Steam account. Watch the DownloadManager queue show real percent/speed/ETA.
    2. Let it complete; restart/focus Steam; confirm Steam verifies and flips StateFlags 1026→4 with (near) zero re-download, and the game appears Installed in Steam.
    3. Launch the game via GameLib (steam://rungameid) and confirm it runs.
    4. Repeat step 1-3 with a CONFIRMED hard-DRM title (e.g. a Denuvo/VMProtect-wrapped game you own) — RESEARCH Open Question 3. Confirm it launches after GameLib-owned install.
    5. Cancel a mid-download install; confirm it finalizes to 1026 and Steam repairs on next launch (D-04). Record results in 21-UAT.md.
  </how-to-verify>
  <acceptance_criteria>
    - Steam flips 1026→4 with no meaningful re-download for a GameLib-installed game.
    - A confirmed hard-DRM title launches after a GameLib-owned install (Open Question 3 closed).
    - Cancel → 1026 → Steam repair path works end to end (D-04).
    - Results recorded in 21-UAT.md with the titles/appIds used.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Streaming-to-disk at 10GB+ scale + real multi-depot game</name>
  <action>Pause for the developer to run the large-game streaming (memory-profiled) and real multi-depot validation per the steps below; record results in 21-UAT.md and resume on approval.</action>
  <what-built>The streaming download loop (Plan 05, positional fs.write) + multi-depot summing (Plan 04).</what-built>
  <how-to-verify>
    1. Install a large (10GB+) real game with the opt-in ON. While it downloads, monitor GameLib's main-process memory (Activity Monitor / task manager / process RSS). Confirm RSS stays bounded (does not grow with the largest file / total size) — Assumption A1.
    2. After completion, spot-check byte-correctness of a few large files (SHA1 vs a known-good Steam install where feasible).
    3. Install a real MULTI-DEPOT game (e.g. Wasteland 3 or Dead Island). Confirm the queue total reflects the summed depots (D-03), all depots download, and Steam adopts + verifies with no forced full re-download (no cross-depot file collision corruption) — Assumption A2.
    4. Record memory profile + titles in 21-UAT.md.
  </how-to-verify>
  <acceptance_criteria>
    - Large-game RSS stays bounded during download (streaming proven at scale, A1).
    - Downloaded large files are byte-correct on spot-check.
    - A real multi-depot game installs with a correct summed total and no collision corruption (A2).
    - Results recorded in 21-UAT.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Bottled Steam adoption on macOS (D-15)</name>
  <action>Pause for the developer to validate bottled Windows Steam adoption of a GameLib-written 1026 manifest per the steps below; record results (or capture divergence for gap closure) in 21-UAT.md and resume on approval.</action>
  <what-built>The D-15 bottle path (Plan 11): depot download into getBottleSteamappsDir with os:'windows', bottle-scoped poller.</what-built>
  <how-to-verify>
    1. On macOS with a provisioned CrossOver bottle (Phase 17) and the opt-in ON, install a bottle-eligible (Windows-only) Steam game via GameLib. Confirm the Windows depot downloads into the bottle's steamapps/ and the queue shows real progress.
    2. Launch/focus the bottled Windows Steam; confirm it adopts the GameLib-written 1026 manifest and verify-repairs it to 4 the same as native (Assumption A3 — re-run of spike 001 against the bottle).
    3. Launch the game through the bottle and confirm it runs.
    4. Record the result in 21-UAT.md. If bottled Steam does NOT adopt identically, capture the divergence for a gap plan (this is the one D-15 risk RESEARCH flagged as inference, not tested).
  </how-to-verify>
  <acceptance_criteria>
    - Bottled Windows Steam adopts a GameLib-written 1026 manifest and flips to 4 (A3 confirmed) OR the divergence is captured for gap closure.
    - The bottle game launches after a GameLib-owned depot install.
    - Result recorded in 21-UAT.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real depot bytes → disk → Steam | End-to-end integrity of untrusted CDN content through to a launchable game |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-22 | Tampering | real-world install integrity | mitigate | Real-machine validation confirms chunk+whole-file SHA1 (Plans 01/05) actually yield byte-correct, Steam-adoptable, launchable installs — the empirical backstop to the unit-level controls |
| T-21-02 | Denial of Service | large-game memory | mitigate | Task 2 memory profiling confirms the streaming fix bounds RSS at scale (A1) |
| T-21-SC | Tampering | lzma decompression at scale | accept | Approved package; real-machine byte-correctness on a large depot is the practical confirmation |
</threat_model>

<verification>
- All three human-verify checkpoints approved; 21-UAT.md records titles/appIds, memory profile, and pass/fail.
- Any divergence (esp. bottle adoption A3) is captured for a follow-up gap plan rather than silently passed.
</verification>

<success_criteria>
- Native adoption + hard-DRM launch, streaming@scale, multi-depot, and bottle adoption all validated on real hardware and recorded in 21-UAT.md — the MUST-VALIDATE gate is closed (or divergences captured for gap closure).
</success_criteria>

<output>
Create/append `.planning/phases/21-steam-native-install/21-UAT.md` with recorded results, and create
`.planning/phases/21-steam-native-install/21-12-SUMMARY.md` when done.
</output>
