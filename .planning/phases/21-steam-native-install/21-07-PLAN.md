---
phase: 21-steam-native-install
plan: 07
type: execute
wave: 5
depends_on: ['21-06', '21-03']
files_modified:
  - src/backend/storeManagers/steam/games.ts
  - src/backend/storeManagers/steam/__tests__/games.test.ts
autonomous: true
requirements: [SNI-07]
must_haves:
  truths:
    - "With the opt-in ON, SteamGame.install() routes native installs through the depot.ts orchestrator instead of steam://install"
    - "With the opt-in OFF, install() behaves byte-for-byte as today (steam://install handoff unchanged)"
    - "SteamGame.stop() aborts an in-flight depot download via the appId's AbortController (was a no-op)"
    - "install() calls well-defined seams ensureSteamClientReady() and resolveSteamInstallTarget() so Plans 09/10 fill them without re-editing games.ts"
  artifacts:
    - path: "src/backend/storeManagers/steam/games.ts"
      provides: "opt-in branch point in install() + real stop() abort"
  key_links:
    - from: "src/backend/storeManagers/steam/games.ts install()"
      to: "src/backend/storeManagers/steam/depot.ts downloadSteamDepots"
      via: "isSteamNativeInstallEnabled() gate"
      pattern: "downloadSteamDepots|isSteamNativeInstallEnabled"
---

<objective>
Wire the opt-in branch into SteamGame.install()/stop(). When the D-13 setting is ON, native installs
go through depot.ts (Plan 04-06); when OFF, today's steam://install path is untouched (D-13 safety
valve). Convert stop() from a no-op into a real AbortController-driven cancel for the depot path
(D-02). Establish the control-flow seams — `ensureSteamClientReady(appId)` (Plan 10) and
`resolveSteamInstallTarget(appId, args)` (Plan 09) — as imported calls with minimal in-place stubs so
those plans implement them in their own files without re-touching games.ts (keeps Wave 6 parallel).
The D-15 bottle branch is Plan 11.

Purpose: SNI-07 (D-12 all-OS, D-13 opt-in, D-14 no-fallback native branch).
Output: install()/stop() branch + extended games.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-steam-native-install/21-PATTERNS.md

<interfaces>
- Current install() (games.ts L527-585) + stop() no-op (L890-895): PATTERNS §games.ts reproduces both.
  Existing structure: isBottleEligible() branch (bottle) else steam://install + startInstallPolling.
- New native branch: if isSteamNativeInstallEnabled() (Plan 03) → ensureSteamClientReady(appId) (Plan 10
  seam) → resolveSteamInstallTarget(appId, args) (Plan 09 seam, returns { targetSteamappsDir, installdir })
  → createAbortController(appId) → downloadSteamDepots(appId, { targetSteamappsDir, installdir, os: hostOs,
  signal }) → on outcome, startInstallPolling(appId) to watch Steam flip 1026→4. When OFF → existing
  steam://install path verbatim.
- downloadSteamDepots + finalizeToSteam: Plan 04-06 depot.ts.
- Abort: src/backend/utils/aborthandler/aborthandler.ts createAbortController/callAbortController;
  downloadqueue.ts stopCurrentDownload calls .stop(false).
- buildSteamProtocolUrl numeric guard (games.ts L50) — keep for the surviving steam://rungameid launch.
- InstallArgs/InstallResult: src/common/types.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opt-in branch in install() with Plan 09/10 seams</name>
  <read_first>
    - src/backend/storeManagers/steam/games.ts (install() L527, isBottleEligible L612, buildSteamProtocolUrl L50)
    - src/backend/storeManagers/steam/nativeInstallSetting.ts (isSteamNativeInstallEnabled — Plan 03)
    - src/backend/storeManagers/steam/depot.ts (downloadSteamDepots signature — Plan 04-06)
    - src/backend/storeManagers/steam/__tests__/games.test.ts (mock-factory conventions, existing install/bottle cases)
  </read_first>
  <behavior>
    - Test: opt-in OFF → install() takes the existing steam://install path (shell.openExternal with the steam://install URL) and does NOT call downloadSteamDepots — byte-for-byte unchanged.
    - Test: opt-in ON, non-bottle → install() calls ensureSteamClientReady, then resolveSteamInstallTarget, then downloadSteamDepots with the resolved target + host os + an AbortSignal, then startInstallPolling(appId).
    - Test: a non-numeric appId is still rejected via the existing guard before any handoff.
  </behavior>
  <action>In games.ts install(), add a branch guarded by isSteamNativeInstallEnabled() (Plan 03). When ON and NOT bottle-eligible: call `ensureSteamClientReady(this.appId)` (import from a new steam/clientSetup.ts — add a minimal stub returning ready:true here that Plan 10 replaces; do NOT implement the installer flow in this plan), then `resolveSteamInstallTarget(this.appId, _args)` (import from a new steam/installLocation.ts — minimal stub returning Steam's first library steamapps dir + the app's installdir that Plan 09 replaces), then register `createAbortController(this.appId)` and call `downloadSteamDepots(this.appId, { targetSteamappsDir, installdir, os: <host os>, signal })`. Map its outcome to InstallResult and call startInstallPolling(this.appId) so the poller watches Steam flip 1026→4. When the setting is OFF, leave the existing steam://install + startInstallPolling path exactly as-is (D-13). Do not touch the isBottleEligible bottle branch (Plan 11 owns D-15). Keep the /^\d+$/ appId guard. Add a comment tagging the seams `// Plan 09` / `// Plan 10` / `// Plan 11 (bottle)`.</action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/games.test.ts -t "install" --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isSteamNativeInstallEnabled" src/backend/storeManagers/steam/games.ts` returns >= 1.
    - `grep -c "downloadSteamDepots" src/backend/storeManagers/steam/games.ts` returns >= 1.
    - Opt-in-OFF test asserts the steam://install path is taken and downloadSteamDepots is NOT called.
    - Opt-in-ON test asserts ensureSteamClientReady → resolveSteamInstallTarget → downloadSteamDepots call order.
    - The two new seam files (clientSetup.ts, installLocation.ts) exist with typed stub signatures matching what Plans 09/10 will implement.
  </acceptance_criteria>
  <done>install() branches on the opt-in: ON routes native to depot.ts via the two seams, OFF is unchanged; install tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stop() real abort for the depot path (D-02)</name>
  <read_first>
    - src/backend/storeManagers/steam/games.ts (stop() no-op L890-895)
    - src/backend/downloadmanager/downloadqueue.ts (stopCurrentDownload L293-297 — calls .stop(false))
    - src/backend/utils/aborthandler/aborthandler.ts (callAbortController/deleteAbortController)
  </read_first>
  <behavior>
    - Test: when a depot download is in flight for this.appId, stop() calls callAbortController(this.appId) (triggering the depot loop's D-02 cancel + Plan 06 finalize).
    - Test: when no depot download is in flight (native steam:// path / bottle path), stop() remains a safe no-op (Steam owns process lifecycle) and does not throw.
  </behavior>
  <action>Convert SteamGame.stop() from an unconditional no-op into: if a depot download AbortController is registered for this.appId (registered at install start in Task 1), call callAbortController(this.appId) so depot.ts's loop aborts and finalizeToSteam (Plan 06) writes the 1026 handoff; otherwise keep the existing no-op behavior (log that Steam owns the process lifecycle). This mirrors downloadqueue.ts's own stopCurrentDownload → callAbortController(appName) call site. Do not delete the controller here if depot.ts owns its cleanup; follow the aborthandler create/call/delete lifecycle used elsewhere.</action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/games.test.ts -t "stop\|cancel" --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "callAbortController" src/backend/storeManagers/steam/games.ts` returns >= 1.
    - stop()-with-in-flight-download test asserts callAbortController(this.appId) is invoked.
    - stop()-with-no-download test asserts no throw and no abort call (no-op preserved).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>stop() aborts an in-flight depot download and stays a safe no-op otherwise; stop tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DownloadManager cancel → backend | Cancel must reliably reach the in-flight depot loop |
| Setting state → install path | OFF must fully preserve the legacy path (no partial new-path leakage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Injection | games.ts appId in handoff | mitigate | Existing `/^\d+$/` buildSteamProtocolUrl guard retained on the surviving steam:// path; depot path validated in depot.ts |
| T-21-08 | Unexpected behavior | opt-in default regression | mitigate | Opt-in-OFF test asserts the legacy steam://install path is byte-for-byte unchanged and depot path never runs |
| T-21-15 | Denial of Service | stuck/uncancellable download | mitigate | stop() wired to callAbortController(appId), mirroring the proven downloadqueue cancel path; abort triggers Plan 06 finalize |
</threat_model>

<verification>
- `npx jest src/backend/storeManagers/steam/__tests__/games.test.ts --silent` green (install + stop cases).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Opt-in ON routes native to depot.ts through 09/10 seams; OFF unchanged; stop() aborts the depot loop.
- Seam files exist with typed stubs for Plans 09/10 to implement without re-editing games.ts.
</success_criteria>

<output>
Create `.planning/phases/21-steam-native-install/21-07-SUMMARY.md` when done.
</output>
