---
phase: 21-steam-native-install
plan: 10
type: execute
wave: 7
depends_on: ['21-07', '21-09']
files_modified:
  - src/backend/storeManagers/steam/clientSetup.ts
  - src/backend/storeManagers/steam/__tests__/clientSetup.test.ts
  - src/frontend/screens/Game/GamePage/components/SteamClientSetup.tsx
  - src/common/types/ipc.ts
  - src/backend/storeManagers/steam/ipc.ts
  - public/locales/en/translation.json
autonomous: false
requirements: [SNI-06]
must_haves:
  truths:
    - "When the Steam client is not installed, a native install triggers a guided, consent-gated install of the official Steam client (download + run non-silently on Win/macOS, link-out on Linux) — D-10"
    - "When Steam is installed but never run (no libraryfolders.vdf yet), the user is prompted to launch Steam once rather than GameLib authoring Steam config — D-11"
    - "After the client is ready, the install proceeds to the depot download"
    - "ensureSteamClientReady() replaces Plan 07's stub and gates the native install branch"
  artifacts:
    - path: "src/backend/storeManagers/steam/clientSetup.ts"
      provides: "ensureSteamClientReady + guided native Steam-client install orchestration"
      exports: ["ensureSteamClientReady"]
  key_links:
    - from: "src/backend/storeManagers/steam/games.ts install()"
      to: "src/backend/storeManagers/steam/clientSetup.ts ensureSteamClientReady"
      via: "gate before depot download (Plan 07 seam)"
      pattern: "ensureSteamClientReady"
---

<objective>
Implement D-10/D-11: the native depot download requires a real Steam client (to adopt the manifest,
launch, and own updates). When Steam is absent, run a consent-gated guided install of the official
Steam client (reusing Phase 17's consent-then-background-task pattern), then proceed. When Steam is
installed but never launched (no libraryfolders.vdf), prompt the user to launch Steam once (D-11 —
lower risk than GameLib synthesizing Steam config). This fills Plan 07's ensureSteamClientReady seam.
This plan is non-autonomous: the actual installer download+run and the prompt-to-launch are
human-facing consent flows with a verification checkpoint.

Purpose: SNI-06 (D-10 guided client install, D-11 prompt-to-launch).
Output: clientSetup.ts + SteamClientSetup.tsx consent UI + event + i18n; a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-steam-native-install/21-PATTERNS.md

<interfaces>
- ensureSteamClientReady(appId) seam — stubbed in Plan 07 (games.ts import from steam/clientSetup.ts).
  Implement: (1) if the Steam client is present and libraryfolders.vdf exists → ready; (2) if Steam is
  absent → guided install (D-10); (3) if Steam present but no libraryfolders.vdf → prompt-to-launch
  (D-11). Return a readiness result that install() gates on (do not proceed to depot download until ready).
- Consent + background-task pattern: SteamBottleSetup.tsx (PATTERNS §Guided Steam-client install) —
  Phase = 'consent' | 'provisioning' | 'error'; consent Dialog blocks, then a non-blocking banner.
- Backend installer download+run: provisionBottle() in bottle.ts (fetch installer once, run non-silently);
  downloadFile/spawnAsync from backend/utils are the concrete "fetch external installer, run non-silent"
  utilities. Target the NATIVE Steam installer (SteamSetup.exe on Windows; the macOS Steam.dmg/app;
  link-out to the distro download page on Linux — distro packaging is not reliably automatable, D-10).
- New frontend event: sendFrontendMessage('steamClientSetupRequired', { appName }) (distinct from the
  existing 'steamBottleSetupRequired'), opening SteamClientSetup.tsx.
- Steam presence detection: reuse getSteamLibraries()/known Steam path probes already in the codebase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ensureSteamClientReady — presence detection + D-11 prompt-to-launch</name>
  <read_first>
    - src/backend/storeManagers/steam/clientSetup.ts (Plan 07 stub — replace it)
    - src/backend/utils.ts (getSteamLibraries + Steam path probes)
    - src/backend/storeManagers/steam/bottle.ts (provisionBottle download-installer-then-run pattern, isBottleReady structure)
    - .planning/phases/21-steam-native-install/21-PATTERNS.md (§Guided Steam-client install)
  </read_first>
  <behavior>
    - Test: Steam present + libraryfolders.vdf present → ensureSteamClientReady returns ready without any install/prompt.
    - Test: Steam present but libraryfolders.vdf absent → returns a 'needs-launch' result and emits steamClientSetupRequired (D-11), does NOT synthesize libraryfolders.vdf.
    - Test: Steam absent → returns a 'needs-install' result and emits steamClientSetupRequired with the guided-install affordance (D-10).
  </behavior>
  <action>Replace Plan 07's stub in `src/backend/storeManagers/steam/clientSetup.ts` with `ensureSteamClientReady(appId): Promise<{ status: 'ready' | 'needs-install' | 'needs-launch' }>`. Detect Steam presence via existing Steam path probes; detect libraryfolders.vdf via getSteamLibraries(). Return 'ready' when both present. When Steam present but no libraryfolders.vdf, emit sendFrontendMessage('steamClientSetupRequired', { appName: appId, reason: 'launch-once' }) and return 'needs-launch' — do NOT author Steam config from scratch (D-11). When Steam absent, emit the same event with reason 'install' and return 'needs-install'. Add the IPC types (common/types/ipc.ts) + a handler (steam/ipc.ts) the frontend calls to trigger/confirm the guided install and to re-check readiness after the user acts. install() (Plan 07 seam) must not proceed to depot download unless status is 'ready'.</action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/clientSetup.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - The three readiness branches (ready / needs-launch / needs-install) are tested.
    - needs-launch test asserts NO libraryfolders.vdf is written by GameLib (D-11).
    - steamClientSetupRequired event fires with a distinct reason for install vs launch.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>ensureSteamClientReady detects presence, prompts-to-launch (D-11) or triggers guided install (D-10), never synthesizes Steam config; readiness tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Guided native Steam-client install (D-10) + consent UI</name>
  <read_first>
    - src/frontend/screens/Game/GamePage/components/SteamBottleSetup.tsx (consent+phase-machine shape to copy)
    - src/backend/storeManagers/steam/bottle.ts (provisionBottle — downloadFile/spawnAsync non-silent run)
    - public/locales/en/translation.json (setup copy keys)
  </read_first>
  <action>Implement the D-10 guided install backend in clientSetup.ts: on user consent, download the official native Steam installer (SteamSetup.exe on Windows; the Steam macOS package on macOS) via downloadFile and run it NON-silently via spawnAsync so the user sees the real installer; on Linux, open the official Steam download/instructions page via openExternalUrl (distro packaging is not reliably automatable — D-10). Create `src/frontend/screens/Game/GamePage/components/SteamClientSetup.tsx` mirroring SteamBottleSetup.tsx's Phase = 'consent' | 'provisioning' | 'error' machine and non-blocking banner (never fight the native installer for focus), opened by the steamClientSetupRequired event. After the installer completes and the user has launched Steam once, re-check ensureSteamClientReady and, when 'ready', proceed to the depot install. Add en-locale copy for consent, provisioning, launch-once prompt, and error.</action>
  <verify>
    <automated>npx tsc --noEmit && node -e "JSON.parse(require('fs').readFileSync('public/locales/en/translation.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - clientSetup.ts runs the installer via spawnAsync NON-silently (grep confirms no silent/quiet flag on the Steam installer spawn).
    - Linux branch uses openExternalUrl to the official Steam download page (no attempt to auto-install a distro package).
    - SteamClientSetup.tsx exists with the consent → provisioning → error phase machine and a non-blocking banner.
    - en translation.json parses and contains the new setup keys.
  </acceptance_criteria>
  <done>Consent-gated guided native Steam-client install (Win/macOS run installer, Linux link-out) with a non-blocking consent UI; proceeds to depot install once ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify guided setup + prompt-to-launch flows</name>
  <action>Pause for the developer to exercise the D-10 guided install and D-11 prompt-to-launch flows on a real machine per the steps below; record the outcome and resume on approval.</action>
  <what-built>Backend ensureSteamClientReady (presence detection, D-11 prompt-to-launch, D-10 guided install) + the SteamClientSetup.tsx consent UI + steamClientSetupRequired event.</what-built>
  <how-to-verify>
    1. With Steam NOT installed (or a test harness simulating absence), start a Steam native install with the opt-in ON; confirm the consent dialog appears, and on consent the official Steam installer downloads and runs non-silently (Win/macOS) or the Steam download page opens (Linux).
    2. With Steam installed but libraryfolders.vdf absent, start an install; confirm the "launch Steam once" prompt appears and GameLib does NOT write libraryfolders.vdf itself.
    3. After Steam is ready, confirm the install proceeds to the depot download.
  </how-to-verify>
  <acceptance_criteria>
    - Guided install flow reaches a running native installer (Win/macOS) / opens the download page (Linux) after consent.
    - Prompt-to-launch appears for the never-launched case; no GameLib-authored Steam config.
    - Install continues to depot download once the client is ready.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External installer download → execution | GameLib downloads and runs a native installer binary |
| GameLib → Steam config | GameLib must not forge Steam's own config files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-20 | Tampering | Steam installer download source | mitigate | Download the installer only from Valve's official URL over HTTPS; run non-silently so the user sees the real, signed installer (consent-gated, no silent background exec) |
| T-21-21 | Elevation of Privilege | GameLib forging libraryfolders.vdf | mitigate | D-11 prompt-to-launch instead of authoring Steam config; test asserts no libraryfolders.vdf is written by GameLib |
| T-21-05 | Injection | appId through the setup IPC | mitigate | numeric appId guard at the IPC/handler entry |
</threat_model>

<verification>
- `npx jest src/backend/storeManagers/steam/__tests__/clientSetup.test.ts --silent` green.
- `npx tsc --noEmit` clean; en translation.json valid.
- Human-verify checkpoint (Task 3) approved.
</verification>

<success_criteria>
- D-10 consent-gated guided native Steam-client install (Win/macOS run, Linux link-out).
- D-11 prompt-to-launch, no GameLib-authored Steam config.
- ensureSteamClientReady seam fully implemented; install proceeds only when ready.
</success_criteria>

<output>
Create `.planning/phases/21-steam-native-install/21-10-SUMMARY.md` when done.
</output>
