---
phase: 21-steam-native-install
plan: 08
type: execute
wave: 5
depends_on: ['21-06']
files_modified:
  - src/backend/storeManagers/steam/library.ts
  - src/backend/storeManagers/steam/__tests__/library.test.ts
autonomous: true
requirements: [SNI-04]
must_haves:
  truths:
    - "On startup, an interrupted GameLib depot download is finalized to a 1026 manifest (Plan 06) so Steam adopts it on its next launch — never re-downloaded, never auto-driving Steam/CrossOver (D-05)"
    - "The ACF poller (pollInstallOnce/readAcfState/startInstallPolling) still requires ZERO changes for D-05/D-15 reuse — it only watches, never drives"
    - "After startup finalize, startInstallPolling watches for Steam to flip StateFlags 1026→4"
  artifacts:
    - path: "src/backend/storeManagers/steam/library.ts"
      provides: "init() startup-resume rewired to finalize-then-watch (D-05)"
  key_links:
    - from: "src/backend/storeManagers/steam/library.ts init()"
      to: "src/backend/storeManagers/steam/depot.ts finalizeToSteam"
      via: "startup resume finalizes partial to 1026 then watches"
      pattern: "finalizeToSteam"
---

<objective>
Resolve the folded todo (steam-startup-download-resume-autoopens-crossover) via D-05: on startup, an
interrupted GameLib depot download must be finalized to a 1026 manifest and left for Steam to adopt on
its next launch — it must NEVER silently re-drive the Steam client or Steam-in-CrossOver. Rewire
init()'s existing scanDownloadingAppIds → startInstallPolling loop so a GameLib-owned partial calls
finalizeToSteam (Plan 06) first, then watches. Regression-guard the RESEARCH claim that the poller
itself (pollInstallOnce/readAcfState) needs zero changes.

Purpose: SNI-04 (D-05 startup arm of the single recovery mechanism) + closes the Phase 18 UAT folded todo.
Output: init() rewire + library.test.ts regression cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-steam-native-install/21-RESEARCH.md
@.planning/phases/21-steam-native-install/21-PATTERNS.md
@.planning/phases/21-steam-native-install/21-CONTEXT.md

<interfaces>
- init() resume site (library.ts L102-121, PATTERNS §library.ts): for each scanDownloadingAppIds()
  appId → currently startInstallPolling(appId). D-05: must finalizeToSteam(appId) FIRST (write 1026,
  stop), THEN startInstallPolling to watch Steam flip to 4. NEVER re-invoke downloadSteamDepots on
  startup (Pitfall 4 — no silent network/Wine re-drive).
- finalizeToSteam: Plan 06 depot.ts.
- pollInstallOnce L907-1001 / readAcfState L781-839 / startInstallPolling L1022 / scanDownloadingAppIds
  L1302: RESEARCH Pattern 4 + Pitfall 4 assert these need ZERO changes (they only watch). This plan
  regression-guards that, it does not modify them.
- How to tell a GameLib-owned partial from a Steam-owned in-progress install: a GameLib partial is one
  whose files exist but with no Steam-written completion; if the distinction is ambiguous, prefer the
  D-05 conservative action (finalize to 1026 + watch) which is safe either way — Steam's verify pass
  reconciles. Do not auto-launch anything.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire init() startup resume to finalize-then-watch (D-05)</name>
  <read_first>
    - src/backend/storeManagers/steam/library.ts (init() L83-121, scanDownloadingAppIds L1302, startInstallPolling L1022)
    - src/backend/storeManagers/steam/depot.ts (finalizeToSteam — Plan 06)
    - .planning/phases/21-steam-native-install/21-RESEARCH.md (Pitfall 4 — never re-drive on startup)
    - src/backend/storeManagers/steam/__tests__/library.test.ts (existing init/poller test conventions)
  </read_first>
  <behavior>
    - Test: on init with an interrupted GameLib depot download present, finalizeToSteam(appId) is called (writes 1026) and downloadSteamDepots is NOT called (no re-download).
    - Test: after finalize, startInstallPolling(appId) is called to watch for Steam's 1026→4 flip.
    - Test: init never calls tellBottledSteamToInstall / shell.openExternal / runWineCommand on startup for a resumed appId (no silent auto-drive of Steam/CrossOver — the folded-todo regression guard).
  </behavior>
  <action>In library.ts init(), rewire the scanDownloadingAppIds() resume loop: for each in-progress appId found on disk, call finalizeToSteam(appId) (Plan 06 — write the honest 1026 manifest over whatever landed and stop) BEFORE startInstallPolling(appId). Never re-invoke downloadSteamDepots or any Steam/CrossOver dispatch on startup (Pitfall 4 / D-05). Keep the existing try/catch resilience. Gate the finalize on the native-install feature being the relevant owner where determinable; when ambiguous, the finalize-and-watch action is safe regardless (Steam reconciles). Leave pollInstallOnce/readAcfState/startInstallPolling bodies unchanged.</action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/library.test.ts -t "init\|resume\|finalize" --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "finalizeToSteam" src/backend/storeManagers/steam/library.ts` returns >= 1, inside init()'s resume loop.
    - `grep -c "downloadSteamDepots" src/backend/storeManagers/steam/library.ts` returns 0 (no startup re-download).
    - The no-auto-drive test asserts init calls none of tellBottledSteamToInstall / shell.openExternal / runWineCommand for a resumed appId.
    - finalize-then-startInstallPolling ordering asserted.
  </acceptance_criteria>
  <done>Startup resume finalizes a partial to 1026 and watches — never re-downloads, never auto-drives Steam/CrossOver; init tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression-guard the "poller unchanged" claims (D-05/D-15 reuse)</name>
  <read_first>
    - src/backend/storeManagers/steam/library.ts (readAcfState L781, pollInstallOnce L907, buildBottleInstalledMap L850)
    - .planning/phases/21-steam-native-install/21-RESEARCH.md (Pattern 4 — bottle poller unmodified; Wave 0 Gaps library.test.ts row)
  </read_first>
  <behavior>
    - Test: readAcfState correctly reads a GameLib-written 1026 manifest as state 'downloading' (StateFlags & 4 == 0), and a Steam-flipped 4 manifest as 'installed' — proving the write side (Plan 02) round-trips against the unchanged read side.
    - Test: startInstallPolling with { source: 'bottle' } scans the bottle steamapps root and reads a hand-written 1026 manifest the same as native (D-15 poller reuse) — no code change required.
  </behavior>
  <action>Add regression tests to library.test.ts (do NOT modify pollInstallOnce/readAcfState/startInstallPolling/buildBottleInstalledMap) that feed a GameLib-shaped 1026 manifest (matching Plan 02's writeAppManifest output) through readAcfState and assert it reads as 'downloading', and that a StateFlags=4 manifest reads as 'installed'. Add a bottle-source test asserting the poller reads a hand-written manifest in the bottle steamapps path identically. These lock in RESEARCH Pattern 4/Pitfall 4's "already works unmodified" claim so a future change can't silently break D-05/D-15 reuse.</action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/library.test.ts -t "acf\|poller\|bottle\|stateflags" --silent</automated>
  </verify>
  <acceptance_criteria>
    - readAcfState test asserts 1026 → 'downloading' and 4 → 'installed'.
    - A bottle-source (source:'bottle') poller read test passes against a hand-written manifest.
    - No diff to pollInstallOnce/readAcfState/startInstallPolling/buildBottleInstalledMap bodies (only tests added).
  </acceptance_criteria>
  <done>Regression tests lock the unchanged read-side poller against GameLib-written 1026 manifests for both native and bottle sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Startup state → Steam/CrossOver | An interrupted download must not auto-trigger external clients without user action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-16 | Elevation of Privilege / unexpected side-effect | init() silent auto-drive (folded todo) | mitigate | Startup resume calls finalizeToSteam + startInstallPolling only; grep-gated absence of downloadSteamDepots/tellBottledSteamToInstall/shell.openExternal/runWineCommand on the resume path |
| T-21-13 | Tampering | startup finalize racing Steam | mitigate | finalize uses Plan 02's atomic write; poller only watches (unchanged), never drives |
</threat_model>

<verification>
- `npx jest src/backend/storeManagers/steam/__tests__/library.test.ts --silent` green.
- `grep -c "downloadSteamDepots" src/backend/storeManagers/steam/library.ts` == 0.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- D-05 startup finalize-then-watch, no re-download, no auto-drive.
- Poller-unchanged claim regression-guarded for native + bottle sources.
</success_criteria>

<output>
Create `.planning/phases/21-steam-native-install/21-08-SUMMARY.md` when done.
</output>
