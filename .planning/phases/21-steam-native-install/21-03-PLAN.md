---
phase: 21-steam-native-install
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx
  - src/frontend/screens/Settings/components/index.ts
  - src/backend/storeManagers/steam/nativeInstallSetting.ts
  - public/locales/en/translation.json
  - src/backend/storeManagers/steam/__tests__/nativeInstallSetting.test.ts
autonomous: true
requirements: [SNI-07]
must_haves:
  truths:
    - "A user can toggle 'Download Steam games directly in GameLib' in Settings, default OFF"
    - "The backend can read the opt-in setting from a single accessor used at the install() branch point"
    - "The toggle carries risk-framing copy (young-but-de-risked feature) per D-13"
  artifacts:
    - path: "src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx"
      provides: "D-13 opt-in toggle (useSetting + ToggleSwitch + InfoIcon)"
      min_lines: 30
    - path: "src/backend/storeManagers/steam/nativeInstallSetting.ts"
      provides: "isSteamNativeInstallEnabled() single backend accessor"
      exports: ["isSteamNativeInstallEnabled"]
  key_links:
    - from: "src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx"
      to: "GlobalConfig setting 'enableSteamNativeInstall'"
      via: "useSetting"
      pattern: "useSetting\\('enableSteamNativeInstall'"
---

<objective>
Ship the D-13 opt-in setting end to end: a Settings toggle (default OFF) and a single backend
accessor the install() branch point (Plan 07) reads. When OFF, nothing about today's behavior
changes — the setting is the primary safety valve for a young-but-de-risked feature. This plan owns
ONLY the setting infrastructure; the actual branch in SteamGame.install() is Plan 07 (keeps files
non-overlapping so this ships in Wave 1).

Purpose: D-12 (all three OSes — no platform gate) + D-13 (opt-in) + the read seam D-14's
no-fallback branch consumes.
Output: `EnableSteamNativeInstall.tsx`, `isSteamNativeInstallEnabled()`, i18n copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-steam-native-install/21-PATTERNS.md

<interfaces>
<!-- Exact analog: copy its storage mechanism precisely (GlobalConfig useSetting, NOT the steam configStore). -->
- Toggle analog: src/frontend/screens/Settings/components/DownloadProtonToSteam.tsx (full file, 44 lines)
  — useSetting('downloadProtonToSteam', false) + ToggleSwitch + InfoIcon + `if (!isDefault) return <></>`.
- Backend read of a GlobalConfig setting: follow how downloadProtonToSteam is read on the backend
  (GlobalConfig.get().getSettings()) — the new accessor wraps the SAME mechanism.
- Register the component alongside its siblings in the Settings components barrel/index.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-13 opt-in toggle component + i18n</name>
  <read_first>
    - src/frontend/screens/Settings/components/DownloadProtonToSteam.tsx (exact shape to copy)
    - src/frontend/screens/Settings/components/index.ts (barrel — export the new component the same way)
    - .planning/phases/21-steam-native-install/21-PATTERNS.md (§Settings toggle — D-13)
    - public/locales/en/translation.json (where setting.* / help.* keys live)
  </read_first>
  <action>Create `src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx` mirroring DownloadProtonToSteam.tsx exactly: `useSetting('enableSteamNativeInstall', false)`, a `ToggleSwitch` titled from `t('setting.steam-native-install', 'Download Steam games directly in GameLib')`, an `InfoIcon` whose text (`help.steam_native_install`) carries D-13 risk-framing copy ("Downloads Steam games in GameLib with real progress and cancel. When off, Steam handles installs. Newer path — Steam still verifies and owns updates."), and the `if (!isDefault) return <></>` guard. Do NOT add an OS gate (D-12: all three desktop OSes). Register the component in the Settings components barrel and place it in the same Settings section where DownloadProtonToSteam renders. Add the two en-locale keys.</action>
  <verify>
    <automated>npx tsc --noEmit && node -e "JSON.parse(require('fs').readFileSync('public/locales/en/translation.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useSetting('enableSteamNativeInstall'" src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx` returns 1 with default `false`.
    - `grep -c "isLinux\|isWin\|isMac" src/frontend/screens/Settings/components/EnableSteamNativeInstall.tsx` returns 0 (no OS gate, per D-12).
    - `setting.steam-native-install` and `help.steam_native_install` exist in en translation.json and it parses as valid JSON.
    - Component is exported from the Settings components barrel.
  </acceptance_criteria>
  <done>Toggle renders in Settings, default OFF, no OS gate, risk-framed copy, valid i18n.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single backend accessor for the opt-in setting</name>
  <read_first>
    - src/frontend/screens/Settings/components/DownloadProtonToSteam.tsx (confirm the GlobalConfig key it writes)
    - src/backend/storeManagers/steam/games.ts (where install() will call this accessor — Plan 07)
    - src/backend/storeManagers/steam/electronStores.ts (steam-dir module conventions)
  </read_first>
  <behavior>
    - Test: isSteamNativeInstallEnabled() returns false when the GlobalConfig setting is unset (default OFF).
    - Test: returns true when the setting is true; false when explicitly false.
  </behavior>
  <action>Create `src/backend/storeManagers/steam/nativeInstallSetting.ts` exporting `isSteamNativeInstallEnabled(): boolean` that reads the `enableSteamNativeInstall` GlobalConfig setting via the SAME mechanism the backend uses for downloadProtonToSteam (GlobalConfig.get().getSettings()), defaulting to false when absent. This is the single chokepoint the Plan 07 branch and Plan 11 bottle branch both consult — do not duplicate the read inline. Keep it side-effect-free and synchronous.</action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/nativeInstallSetting.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - isSteamNativeInstallEnabled default-OFF test passes (unset → false).
    - The accessor reads `enableSteamNativeInstall` (grep confirms the key name matches the frontend useSetting key).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>isSteamNativeInstallEnabled() reads the same key the toggle writes, default OFF, unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User setting → install branch | The opt-in gates whether the young depot-download path runs at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Elevation of Privilege / unexpected behavior | default-on regression | mitigate | Accessor and toggle both default to `false`; a default-OFF unit test guards against an accidental opt-out of the safety valve |
| T-21-09 | Tampering | setting key drift | accept | Frontend useSetting key and backend accessor key are both `enableSteamNativeInstall`; a grep acceptance criterion keeps them in sync (low risk, single string) |
</threat_model>

<verification>
- `npx jest src/backend/storeManagers/steam/__tests__/nativeInstallSetting.test.ts --silent` green.
- `npx tsc --noEmit` clean; en translation.json valid JSON.
</verification>

<success_criteria>
- Toggle ships default-OFF with risk-framed copy, no OS gate.
- Backend accessor is the single read seam, default OFF, tested.
</success_criteria>

<output>
Create `.planning/phases/21-steam-native-install/21-03-SUMMARY.md` when done.
</output>
