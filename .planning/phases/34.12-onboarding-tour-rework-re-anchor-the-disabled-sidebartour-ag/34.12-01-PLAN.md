---
phase: 34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/components/UI/NavShell/components/NavItem/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx
  - src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts
autonomous: true
requirements: [D-05, D-06]

must_haves:
  truths:
    - "A caller can pass data-tour to NavItem and the string reaches the rendered element's props"
    - "The tour-disabled gate still runs green at HEAD after the passthrough lands"
    - "No NavShell source file may reintroduce a legacy sidebar-* tour selector"
  artifacts:
    - path: "src/frontend/components/UI/NavShell/components/NavItem/index.tsx"
      provides: "'data-tour'?: string prop, destructured and forwarded onto both the button and NavLink branches"
      contains: "data-tour"
    - path: "src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx"
      provides: "Layer-bridge assertions: NavItem forwards data-tour on both branches"
      contains: "data-tour"
    - path: "src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts"
      provides: "Transitional gate: no NavShell source file carries a data-tour=\"sidebar-*\" token"
      contains: "sidebar-"
  key_links:
    - from: "src/frontend/components/UI/NavShell/components/NavItem/index.tsx"
      to: "rendered <button> / <NavLink> props"
      via: "data-tour={dataTour} on both JSX branches"
      pattern: "data-tour=\\{dataTour\\}"
---

<objective>
Re-add the `data-tour` passthrough that the 34.10 port deliberately dropped from `NavItem`,
and narrow `tourDisabled.test.ts` so it stays green — and still meaningful — once `data-tour`
tokens start appearing in `NavShell/` source.

Purpose: four of D-05's twelve anchors (Wine Manager, Accessibility, Documentation, Ko-fi)
sit on `NavItem` rows. `NavItemProps` has no index signature and no `...rest` spread, so
those four anchors are impossible until this lands. This plan is the blocking prerequisite
for plans 03, 04 and 05.

Output: `NavItem` accepts and forwards `'data-tour'`; `NavItem.test.tsx` carries the
bridging assertion of the two-layer positive gate; `tourDisabled.test.ts` is rewritten from
a blanket `data-tour` absence scan into a `sidebar-*` selector absence scan that survives
the whole phase and is deleted by plan 06 once the census subsumes it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-CONTEXT.md
@.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-PATTERNS.md
@.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-VALIDATION.md

<interfaces>
Current `NavItemProps` (`NavItem/index.tsx:27-37`) — a CLOSED interface. No index
signature, no rest spread. The prop being added is the only change to its shape:

  interface NavItemProps {
    label: string
    labelElement?: ReactNode
    url?: string
    icon?: FontAwesomeIconProps['icon']
    isActiveFallback?: boolean
    onClick?: MouseEventHandler
    className?: string
    elementType?: 'a' | 'button'
    active?: boolean
  }

Two in-repo precedents for the prop's NAME — both agree on the literal hyphenated key,
neither uses camelCase `dataTour`:

  // src/frontend/screens/Library/components/AddGameButton/index.tsx:6-9
  interface AddGameButtonProps { 'data-tour'?: string }
  function AddGameButton({ 'data-tour': dataTour }: AddGameButtonProps = {}) { ... }

  // src/frontend/components/UI/ActionIcons/index.tsx:24-29, 46
  interface ActionIconsProps { 'data-tour'?: string }
  export default React.memo(function ActionIcons({ 'data-tour': dataTour }: ActionIconsProps = {}) {
    return <div className="ActionIcons" data-tour={dataTour}>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 'data-tour' passthrough to NavItem and prove the forward on both branches</name>
  <files>src/frontend/components/UI/NavShell/components/NavItem/index.tsx, src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/NavShell/components/NavItem/index.tsx (full file — the closed interface, the docstring paragraph that must be rewritten, and BOTH JSX branches)
    - src/frontend/screens/Library/components/AddGameButton/index.tsx (naming precedent 1 — literal `'data-tour'?: string`, destructured as `'data-tour': dataTour`)
    - src/frontend/components/UI/ActionIcons/index.tsx lines 20-50 (naming precedent 2 — same convention on a React.memo component)
    - src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx (full file — the direct-invocation idiom, the react-router-dom / FontAwesome mocks, and the ts-jest mock-ordering comment)
  </read_first>
  <action>
    Add `'data-tour'?: string` to `NavItemProps` — the literal hyphenated key, NOT a camelCase
    `dataTour` prop. The repo has exactly one convention for this and it is used twice
    (`AddGameButton/index.tsx:6`, `ActionIcons/index.tsx:25`); match it.

    Destructure it in the parameter list as `'data-tour': dataTour` and forward
    `data-tour={dataTour}` onto BOTH JSX branches — the `<button>` in the
    `case 'button':` arm and the `<NavLink>` in the `default:` arm. Only one branch renders
    per instance, but D-01's launcher row uses the button branch while the Wine Manager /
    Accessibility / Documentation rows use the NavLink branch, so both are live call sites in
    this phase. Do not add it to one branch only.

    Rewrite the docstring paragraph at `NavItem/index.tsx:14-18` — the one that currently
    says "The old onboarding-tour anchor prop is dropped entirely: per D-13 the onboarding
    tour is disabled by this phase ... re-adding it would ship anchors for a tour that does
    not run." That statement is now false. Replace it with a statement that the prop is
    re-added by phase 34.12 because the tour is rebuilt against this shell, naming the four
    D-05 rows that consume it (Wine Manager, Accessibility, Documentation, Ko-fi) and the
    D-01 launcher row. Do not leave the stale paragraph in place alongside the new prop.

    Then add two NEW assertions to `NavItem.test.tsx`, in the existing `describe('NavItem')`
    block, using that file's existing direct-invocation call shape:
      - button branch: invoke NavItem with `{ label: 'Take the tour', elementType: 'button',
        'data-tour': 'nav-launcher' }` and assert `element.props['data-tour']` equals
        `'nav-launcher'`.
      - NavLink branch: invoke NavItem with `{ label: 'Wine Manager', url: '/wine-manager',
        'data-tour': 'nav-wine' }` and assert `element.props['data-tour']` equals
        `'nav-wine'`.
    These two assertions ARE the "bridging assertion" of the Positive Gate Contract — they
    prove the primitive does not silently eat the prop, which the per-parent Layer-A
    assertions in plans 03/05 cannot prove on their own.

    New `jest.mock(...)` calls, if any are needed, go textually BEFORE the import they mock —
    this project's ts-jest setup does not hoist `jest.mock` (stated verbatim at
    `SettingsPanel.test.tsx:79-85`). The existing file already orders its mocks this way.
  </action>
  <verify>
    <automated>pnpm jest --selectProjects Frontend NavShell/__tests__/NavItem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'data-tour'?: string" src/frontend/components/UI/NavShell/components/NavItem/index.tsx` returns exactly 1 line.
    - `grep -c 'data-tour={dataTour}' src/frontend/components/UI/NavShell/components/NavItem/index.tsx` returns exactly 2 (one per JSX branch).
    - `grep -c 'dataTour' src/frontend/components/UI/NavShell/components/NavItem/index.tsx` is at least 3 (destructure + two forwards).
    - `grep -n 'dropped entirely' src/frontend/components/UI/NavShell/components/NavItem/index.tsx` returns NO lines (the stale docstring paragraph is gone).
    - `pnpm jest --selectProjects Frontend NavShell/__tests__/NavItem.test.tsx` exits 0 with at least 2 more passing tests than before this task.
    - `pnpm codecheck` exits 0 (proves the new prop is accepted by TypeScript at call sites, not force-cast).
    - RED-PROOF, RECORDED IN THE SUMMARY: delete the `data-tour={dataTour}` forward from the `<NavLink>` branch only, re-run `pnpm jest --selectProjects Frontend NavShell/__tests__/NavItem.test.tsx`, confirm the NavLink-branch assertion FAILS and the button-branch assertion still PASSES, then restore and confirm green. Paste both the failing test name and the restored pass count into the SUMMARY. A RED proof that fails BOTH assertions means the two branches were not tested independently — fix the test, not the proof.
  </acceptance_criteria>
  <done>`NavItem` accepts `'data-tour'` and forwards it on both branches; `NavItem.test.tsx` proves each branch separately; `pnpm codecheck` clean; RED proof recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow tourDisabled.test.ts from a blanket data-tour ban to a sidebar-* selector ban</name>
  <files>src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts</files>
  <read_first>
    - src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts (full file — the 33-line docstring, the two describe blocks, the `walk`/`readStripped` helpers, and the `it.each` per-file failure-naming shape)
    - src/frontend/components/UI/NavShell/components/NavItem/index.tsx (as modified by Task 1 — this file is the first NavShell source file to contain the `data-tour` token)
    - .planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-VALIDATION.md § "Positive Gate Contract"
  </read_first>
  <action>
    Task 1 has just made this file's `expect(source).not.toMatch(/data-tour/)` assertion RED —
    `NavItem/index.tsx` is inside `NAVSHELL_DIR` and now contains the token. Rewrite the gate
    in place at the SAME path (do not rename, do not delete — plan 06 deletes it once the
    anchor census subsumes it).

    In the first describe block, replace the blanket `expect(source).not.toMatch(/data-tour/)`
    with `expect(source).not.toMatch(/data-tour="sidebar-/)`. Keep the three other assertions
    in that block unchanged (`/SidebarTour/`, `/SIDEBAR_TOUR_ID/`, `/TourButton/`) — plan 04
    renames the component to `NavShellTour` and the id constant to `NAV_TOUR_ID`, and D-01's
    launcher row is a `NavItem`, not a `TourButton`, so all three stay satisfiable through the
    rest of the phase. Keep the `it.each` per-file shape so failures still name the offending
    file. Keep the non-empty-walk sanity test at `:68-70`.

    Leave the SECOND describe block ("SidebarTour.tsx has no live importer") completely
    untouched in this plan. Plan 04 is the plan that makes it vacuous (by moving the file) and
    plan 04 owns its removal.

    Rewrite the file docstring. The current one explains why the tour is OFF. The new one must
    state: (a) this is a TRANSITIONAL gate owned by phase 34.12, (b) the property it now
    measures is D-06's rename — no `sidebar-*` selector may survive in the new shell — not
    "the tour is disabled", (c) it is scheduled for deletion by plan 34.12-06, whose
    `navTourAnchorCensus.test.ts` subsumes it with a strictly stronger per-id check, and
    (d) why the `__tests__/` exclusion in the walk is still necessary (a file searching for a
    token cannot include itself in its own target set). Do not leave any sentence claiming the
    tour cannot start — that claim stops being true in plan 04.
  </action>
  <verify>
    <automated>pnpm jest --selectProjects Frontend NavShell/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'not.toMatch(/data-tour/)' src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts` returns 0.
    - `grep -c 'data-tour="sidebar-' src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts` returns at least 1.
    - `grep -v '^ \*' src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts | grep -c 'SidebarTour.tsx has no live importer'` returns 1 (the second describe block is still present and untouched).
    - `pnpm jest --selectProjects Frontend NavShell/__tests__/` exits 0 — the whole NavShell suite, not just this file, proving Task 1's change did not leave any other gate red.
    - RED-PROOF (both directions), RECORDED IN THE SUMMARY: (a) add `data-tour="sidebar-menu"` to the `<Tabs>` element in `NavShell/components/NavTabs/index.tsx`, re-run the suite, confirm this gate FAILS and the failure message names `NavTabs/index.tsx` specifically; remove it and confirm green. (b) Confirm the gate is NOT vacuous by checking the sanity test reports a non-zero file count — run with `--verbose` and paste the "scans at least one NavShell source file" line. Both outputs go in the SUMMARY.
    - `grep -n 'cannot start\|is disabled\|tour is off' src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts` returns no lines in the docstring making a now-false claim.
  </acceptance_criteria>
  <done>The gate is green at HEAD with `NavItem`'s passthrough in place, measures D-06's `sidebar-*` absence, names the offending file on failure, and its docstring states its transitional status and its deletion owner (plan 34.12-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by this plan) | This plan touches one React presentational primitive and one Node-environment jest gate. No network, no IPC, no filesystem write, no user input crosses any boundary here. |

Honest scope note (from RESEARCH.md's Architectural Responsibility Map): phase 34.12 has
**no backend / IPC / API surface at all**. The register below is deliberately short rather
than padded with severities this surface cannot produce.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.12-01-01 | Tampering | `NavItem`'s new `'data-tour'` prop | accept | The prop's only callers are literal string constants in first-party JSX (`SettingsPanel`, plans 03/05). It is never populated from user input, IPC payloads or catalog data. A `data-*` attribute carries no behaviour; React escapes attribute values. No mitigation warranted. |
| T-34.12-01-02 | Information disclosure | `data-tour` values rendered into the DOM | accept | The twelve values are static UI-region names (`nav-wine`, `nav-quit`, …). They disclose nothing a user cannot already read off the visible nav. |
| T-34.12-01-SC | Tampering | npm/pip/cargo installs | N/A — no installs | This plan installs no packages. RESEARCH.md confirms `intro.js ^7.2.0` / `intro.js-react ^1.0.0` are already pinned and already running; no Package Legitimacy Audit is triggered. If an executor finds itself reaching for `pnpm add`, STOP — that is out of plan scope and needs a blocking human checkpoint. |
</threat_model>

<verification>
- `pnpm jest --selectProjects Frontend NavShell/__tests__/` exits 0.
- `pnpm codecheck` exits 0.
- Both RED proofs (Task 1 branch-isolated forward, Task 2 sidebar-* detection) are recorded verbatim in the SUMMARY, each showing the failing test name and the restored green.
</verification>

<success_criteria>
- `NavItem` accepts `'data-tour'?: string` and forwards it onto both the `<button>` and `<NavLink>` branches.
- `NavItem.test.tsx` proves each branch's forward independently (two assertions, not one combined).
- `tourDisabled.test.ts` is green at HEAD, bans `data-tour="sidebar-*"` in NavShell source, and its docstring names plan 34.12-06 as its deletion owner.
- No other suite in the repo went red: `pnpm test` is not required here, but `pnpm jest --selectProjects Frontend` must be clean.
</success_criteria>

<output>
Create `.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-01-SUMMARY.md` when done.
</output>
