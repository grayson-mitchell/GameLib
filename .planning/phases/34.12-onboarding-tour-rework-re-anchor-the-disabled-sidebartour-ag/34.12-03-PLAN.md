---
phase: 34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag
plan: 03
type: execute
wave: 2
depends_on: ["34.12-01"]
files_modified:
  - src/frontend/components/UI/NavShell/components/NavTabs/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/NavTabsComponent.test.tsx
  - src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx
  - src/frontend/components/UI/NavShell/components/QuitButton/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/QuitButton.test.tsx
  - src/frontend/components/UI/NavShell/components/HeroicVersion/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/HeroicVersion.test.tsx
autonomous: true
requirements: [D-05]

must_haves:
  truths:
    - "The tier-1 tab strip carries a tour anchor"
    - "Each of the four tier-1 tabs carries its own distinct tour anchor"
    - "The Downloads ring carries a single tour anchor (the old duplicate is gone by construction)"
    - "The Quit button carries a tour anchor"
    - "The version block carries a tour anchor"
  artifacts:
    - path: "src/frontend/components/UI/NavShell/components/NavTabs/index.tsx"
      provides: "nav-menu on <Tabs>; nav-manage-accounts / nav-library / nav-stores / nav-settings on the four <Tab>s"
      contains: "data-tour=\"nav-menu\""
    - path: "src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx"
      provides: "nav-downloads on the single <Link>"
      contains: "data-tour=\"nav-downloads\""
    - path: "src/frontend/components/UI/NavShell/components/QuitButton/index.tsx"
      provides: "nav-quit on the <button>"
      contains: "data-tour=\"nav-quit\""
    - path: "src/frontend/components/UI/NavShell/components/HeroicVersion/index.tsx"
      provides: "nav-version on .heroicVersionContainer"
      contains: "data-tour=\"nav-version\""
    - path: "src/frontend/components/UI/NavShell/__tests__/QuitButton.test.tsx"
      provides: "First unit coverage for QuitButton, including its anchor"
    - path: "src/frontend/components/UI/NavShell/__tests__/HeroicVersion.test.tsx"
      provides: "First unit coverage for HeroicVersion via the hooks harness + React.memo unwrap, including its anchor"
  key_links:
    - from: "src/frontend/components/UI/NavShell/components/NavTabs/index.tsx"
      to: "the rendered <a> elements MUI produces via component={NavLink}"
      via: "data-* prop forwarding through Tab -> NavLink"
      pattern: "data-tour=\"nav-(menu|library|stores|settings|manage-accounts)\""
---

<objective>
Land eight of D-05's twelve anchors — every one that does NOT go through `SettingsPanel` —
each with its own Layer-A correctness assertion tied to the specific element that carries it.

Purpose: five of these (`nav-menu`, `nav-library`, `nav-stores`, `nav-settings`,
`nav-manage-accounts`) also empirically settle RESEARCH.md's MEDIUM-confidence claim A1, that
MUI `Tabs`/`Tab` forward arbitrary `data-*` props. Two of them (`nav-quit`, `nav-version`)
require test files that do not exist in the repo at all, which VALIDATION.md lists as Wave 0
gaps. And `nav-downloads` closes the historical duplicate-selector defect by construction:
the retired sidebar had two elements carrying `sidebar-downloads`; `DownloadsRing` is one
element, so there is nothing to fix — only to confirm.

Output: eight anchors, four extended/created test suites, twelve separate per-anchor
assertions (five for the tabs strip, one each for Downloads / Quit / Version, plus the
identity checks that tie each to its element).

## What this plan does NOT do

Do not write a "fix" for the duplicate `sidebar-downloads` defect. Per CONTEXT.md's
Specific Ideas: it does not need a fix so much as a check. Task 2's uniqueness assertion IS
that check. Adding deduplication code would be inventing work.

Do not touch `SettingsPanel`, `NavItem`, `NavShell/index.tsx` or `tourDisabled.test.ts` —
they belong to plans 01, 04 and 05 and this plan runs in parallel with plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-CONTEXT.md
@.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-PATTERNS.md
@.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-VALIDATION.md

<interfaces>
D-05's anchor mapping for the eight sites this plan owns. The old selector is listed only so
the executor can confirm the 1:1 correspondence; the old strings must NOT appear in the new
source.

  | old selector              | new anchor value      | carrying element (file:line at HEAD)                        |
  |---------------------------|-----------------------|-------------------------------------------------------------|
  | sidebar-menu              | nav-menu              | <Tabs className="NavTabs">, NavTabs/index.tsx:89             |
  | sidebar-library           | nav-library           | <Tab value="games">, NavTabs/index.tsx:100-110               |
  | sidebar-stores            | nav-stores            | <Tab value="stores">, NavTabs/index.tsx:111-120              |
  | sidebar-settings          | nav-settings          | <Tab value="settings">, NavTabs/index.tsx:121-130            |
  | sidebar-manage-accounts   | nav-manage-accounts   | <Tab value="accounts">, NavTabs/index.tsx:90-99              |
  | sidebar-downloads         | nav-downloads         | <Link to="/download-manager">, DownloadsRing/index.tsx:76-83 |
  | sidebar-quit              | nav-quit              | <button className="NavItem">, QuitButton/index.tsx:37-38     |
  | sidebar-version           | nav-version           | <div className="heroicVersionContainer">, HeroicVersion:76-77|

Note the Accounts tab is FIRST in JSX order (`value="accounts"`), before games/stores/
settings — do not assume the D-05 table's numbering is the render order.

`HeroicVersion`'s default export is `React.memo`-wrapped:
`export default React.memo(function HeroicVersion() {...})` (`HeroicVersion/index.tsx:35`).
A memo result is a plain object `{ $$typeof, type, compare }`, NOT a callable function. The
real render function is at `.type`. No existing test in the repo needs this unwrap, so it is
not visible in any analog.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five tab-strip anchors, each proven on its own element</name>
  <files>src/frontend/components/UI/NavShell/components/NavTabs/index.tsx, src/frontend/components/UI/NavShell/__tests__/NavTabsComponent.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/NavShell/components/NavTabs/index.tsx (full file — the `<Tabs>` wrapper and all four `<Tab>` elements with their `value` props)
    - src/frontend/components/UI/NavShell/__tests__/NavTabsComponent.test.tsx (full file — the `@mui/material` mock, `collectElements`, `callNavTabs`, `tabsOf`, and the existing four-tab order assertions)
  </read_first>
  <action>
    Add `data-tour="nav-menu"` to the `<Tabs className="NavTabs" ...>` element, and one
    `data-tour` prop to each `<Tab>`, keyed by that tab's existing `value`:
      value="accounts" -> data-tour="nav-manage-accounts"
      value="games"    -> data-tour="nav-library"
      value="stores"   -> data-tour="nav-stores"
      value="settings" -> data-tour="nav-settings"
    No component code change is needed — MUI forwards unrecognised props to its root, and
    `component={NavLink}` forwards through to the rendered `<a>`. Do not add a prop-forwarding
    shim; do not change `NavTabs`' signature.

    Extend `NavTabsComponent.test.tsx` with SIX separate `it` blocks — one per anchor plus one
    uniqueness check. Do not write a single combined "all five anchors present" assertion:
    that measures the wrong property and is the failure mode this phase exists to avoid.
      - `it('the Tabs strip carries data-tour="nav-menu"')`: `callNavTabs().props['data-tour']`
        equals `'nav-menu'`, and `root.type` is the imported `Tabs` binding (identity).
      - Four `it` blocks, one per tab, each locating its tab via
        `tabsOf(callNavTabs()).find((tab) => tab.props.value === '<value>')` and asserting
        `tab.props['data-tour']` equals the exact expected string. Locate by `value`, not by
        array index — index-based location cannot distinguish "the right anchor on the right
        tab" from "the tabs were reordered".
      - `it('no two tab-strip anchors share a value')`: collect the five anchor values from the
        rendered tree and assert `new Set(values).size === 5`. This is the in-component half
        of the duplicate defence; plan 06's census covers the repo-wide half.

    RESEARCH.md flags the MUI forwarding claim at MEDIUM confidence (A1) because the two-hop
    Tab -> NavLink -> <a> chain was not empirically rendered in this installed version
    combination. These assertions prove the prop reaches `Tab`'s props, which is the half this
    jest project can prove. The remaining half — that MUI passes it to the DOM — is covered by
    plan 07's live verification, step-by-step. State that split explicitly in the test file's
    docstring so a future reader does not mistake this suite for a DOM-level proof.
  </action>
  <verify>
    <automated>pnpm jest --selectProjects Frontend NavShell/__tests__/NavTabsComponent.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-tour="nav-' src/frontend/components/UI/NavShell/components/NavTabs/index.tsx` returns exactly 5.
    - `grep -c 'sidebar-' src/frontend/components/UI/NavShell/components/NavTabs/index.tsx` returns 0.
    - `pnpm jest --selectProjects Frontend NavShell/__tests__/NavTabsComponent.test.tsx` exits 0 with at least 6 more passing tests than before.
    - `pnpm jest --selectProjects Frontend NavShell/__tests__/tourDisabled.test.ts` exits 0 — plan 01's narrowed gate must still be green with five `data-tour` tokens now in `NavTabs/index.tsx`. If this fails, plan 01's narrowing did not land; STOP and report rather than editing the gate here.
    - RED-PROOF, RECORDED IN THE SUMMARY: swap the `data-tour` values on the `stores` and `settings` tabs (each still appears exactly once, so a count-based check would still pass). Re-run. Confirm exactly TWO `it` blocks fail — the stores one and the settings one — each naming its tab. Confirm the uniqueness check still PASSES (proving it measures uniqueness only, not correctness, exactly as the two-layer contract requires). Restore and confirm green. Paste the two failing test names into the SUMMARY.
  </acceptance_criteria>
  <done>Five anchors on five distinct elements, six separate assertions, the swap RED proof shows per-tab failure isolation, and plan 01's gate is still green.</done>
</task>

<task type="auto">
  <name>Task 2: Downloads and Quit anchors, plus the repo's first QuitButton test</name>
  <files>src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx, src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx, src/frontend/components/UI/NavShell/components/QuitButton/index.tsx, src/frontend/components/UI/NavShell/__tests__/QuitButton.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx (the exported `DownloadsRing` function and its single `<Link>` root)
    - src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx (full file — the react-router-dom `Link` mock and the hand-rolled useState/useEffect harness with its `__beginRender` / `__resetMount` helpers)
    - src/frontend/components/UI/NavShell/components/QuitButton/index.tsx (full file — the `useContext(ContextProvider)` call, the `showDialogModal` handler, and the docstring paragraph claiming the anchor prop is "dropped entirely")
    - src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx (the plain direct-invocation idiom — `QuitButton` needs no hooks harness, only a `useContext` mock)
    - src/frontend/components/UI/NavShell/__tests__/SettingsPanel.test.tsx lines 30-50 (the `useContext` and `react-i18next` mock conventions)
  </read_first>
  <action>
    Add `data-tour="nav-downloads"` to `DownloadsRing`'s single `<Link to="/download-manager">`
    as a sibling prop of `aria-label` / `aria-current`. There is exactly one `<Link>` in this
    component — that single-element fact IS the resolution of the historical duplicate
    `sidebar-downloads` defect. Do not add deduplication logic.

    Add `data-tour="nav-quit"` to `QuitButton`'s `<button className="NavItem">`. Rewrite the
    docstring sentence at `QuitButton/index.tsx:12-14` that says "the old onboarding-tour
    anchor prop is dropped entirely (per D-13 the tour is disabled this phase)" — that claim
    is now false. Replace it with a statement that phase 34.12 re-anchors this button as the
    tour's `nav-quit` step.

    Extend `DownloadsRing.test.tsx` with ONE new `it` block asserting the mounted tree's root
    `<Link>` element carries `props['data-tour'] === 'nav-downloads'`, and a second asserting
    the tree contains exactly ONE element whose `data-tour` value is `'nav-downloads'` (the
    in-component uniqueness half of the historical-defect check). Reuse the file's existing
    `mount()` helper rather than re-invoking the component directly — `DownloadsRing` uses
    `useState`/`useEffect` and will throw "Invalid hook call" without the harness.

    Create `src/frontend/components/UI/NavShell/__tests__/QuitButton.test.tsx`. No test file
    for `QuitButton` exists today. `QuitButton` uses `useContext` and `useTranslation` but no
    `useState`/`useEffect`, so it needs NO hooks harness — invoke it directly, exactly the way
    `NavItem.test.tsx` invokes `NavItem`. Mock `react`'s `useContext` to return a context value
    exposing a `showDialogModal: jest.fn()`, and mock `react-i18next` with the standard
    `t: (_key, defaultValue) => defaultValue`. Every `jest.mock(...)` goes textually BEFORE the
    import it mocks — ts-jest does not hoist. Assert as separate `it` blocks:
      - the returned element's `type` is the string `'button'`;
      - `element.props['data-tour']` equals `'nav-quit'`;
      - `element.props.className` is `'NavItem'` (proves the anchor landed on the row element,
        not on some inner wrapper);
      - invoking `element.props.onClick()` calls `showDialogModal` once with an object whose
        `title` is the resolved Quit label (proves the anchor addition did not disturb the
        existing behaviour, which had no prior test at all).
  </action>
  <verify>
    <automated>pnpm jest --selectProjects Frontend NavShell/__tests__/DownloadsRing.test.tsx NavShell/__tests__/QuitButton.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-tour="nav-downloads"' src/frontend/components/UI/NavShell/components/DownloadsRing/index.tsx` returns exactly 1.
    - `grep -c 'data-tour="nav-quit"' src/frontend/components/UI/NavShell/components/QuitButton/index.tsx` returns exactly 1.
    - `grep -n 'dropped entirely' src/frontend/components/UI/NavShell/components/QuitButton/index.tsx` returns no lines.
    - `test -f src/frontend/components/UI/NavShell/__tests__/QuitButton.test.tsx` succeeds and the suite reports at least 4 passing tests.
    - `pnpm jest --selectProjects Frontend NavShell/__tests__/` exits 0.
    - RED-PROOF #1, RECORDED IN THE SUMMARY: remove `data-tour="nav-quit"` from the `<button>`; re-run; confirm the `QuitButton.test.tsx` anchor assertion FAILS while its `onClick` behaviour assertion still PASSES (proving the two are independent); restore and confirm green.
    - RED-PROOF #2, RECORDED IN THE SUMMARY: add a second `data-tour="nav-downloads"` onto the `<span className="DownloadsRing__count">` inside `DownloadsRing`; re-run; confirm the in-component uniqueness assertion FAILS while the root-`<Link>` assertion still PASSES; restore and confirm green. This is the historical duplicate-selector defect reproduced deliberately and caught — record it as such.
  </acceptance_criteria>
  <done>Both anchors landed on their single carrying elements, `QuitButton` has its first test suite covering anchor AND behaviour, and the duplicate-selector RED proof is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Version anchor, plus the repo's first HeroicVersion test through the memo unwrap</name>
  <files>src/frontend/components/UI/NavShell/components/HeroicVersion/index.tsx, src/frontend/components/UI/NavShell/__tests__/HeroicVersion.test.tsx</files>
  <read_first>
    - src/frontend/components/UI/NavShell/components/HeroicVersion/index.tsx (full file — the `React.memo(function HeroicVersion() {...})` export, the four `useState` calls, the two `useEffect` calls, the `useContext(ContextProvider)` call, the `ChangelogModal` import, the colocated `index.scss` import, the outer `<div className="heroicVersionContainer">`, and the docstring paragraph at lines 16-19 claiming the anchor is permanently gone)
    - src/frontend/components/UI/NavShell/__tests__/DownloadsRing.test.tsx lines 40-105 (the hand-rolled useState/useEffect harness to copy VERBATIM — `stateSlots`/`stateCursor`/`effectDeps`/`effectCleanups`, `depsChanged`, `__beginRender`, `__resetMount`)
    - src/frontend/components/UI/NavShell/__tests__/SettingsPanel.test.tsx lines 30-50 (the `useContext` mock convention — this test needs it IN THE SAME `jest.mock('react', ...)` factory as the hooks harness, because `HeroicVersion` calls all three hooks and the two analog files each mock only one)
  </read_first>
  <action>
    Add `data-tour="nav-version"` to `HeroicVersion`'s outer `<div className="heroicVersionContainer">`
    (`HeroicVersion/index.tsx:76-77`). Rewrite the docstring paragraph at lines 16-19 — the one
    saying "the tour-anchor attribute this container used to carry [is] gone. Re-adding either
    would offer an entry point into a tour whose anchors no longer resolve." That is now false.
    Replace it with a statement that phase 34.12 re-anchors this container as the tour's
    `nav-version` step, and note that the tour's launcher is a `SettingsPanel` row (D-01) — NOT
    a help icon restored here, which D-01 explicitly rejected as the second easy-to-miss entry
    point. Preserve the surrounding paragraphs about the mount-triggered effects (REQ-34.10-12)
    unchanged; they are still true and load-bearing.

    Create `src/frontend/components/UI/NavShell/__tests__/HeroicVersion.test.tsx`. No unit test
    for this component exists. Three things this file needs that no single analog provides:
      1. The hooks harness copied from `DownloadsRing.test.tsx` (`useState` x4, `useEffect` x2
         will otherwise throw "Invalid hook call").
      2. `useContext` mocked in the SAME `jest.mock('react', ...)` factory as the harness —
         `DownloadsRing.test.tsx` mocks only useState/useEffect and `SettingsPanel.test.tsx`
         mocks only useContext; this component needs all three.
      3. The `React.memo` unwrap. The default export is a memo OBJECT, not a function.
         `HeroicVersion()` will throw "is not a function". Access the real render function via
         the export's `.type` property and call THAT. Add a comment recording this, since it is
         the one gotcha with no precedent in the repo.
    Also mock the colocated `index.scss` import as `{}` (no CSS transform in this jest
    project) and stub the `ChangelogModal` import — it is not the subject of this test and
    pulls its own dependency tree. All mocks textually before their imports.

    Assert as separate `it` blocks:
      - the returned root element's `type` is the string `'div'` and its `className` is
        `'heroicVersionContainer'`;
      - `root.props['data-tour']` equals `'nav-version'`;
      - exactly ONE element in the returned tree carries a `data-tour` prop at all (the anchor
        is on the container, not sprayed onto inner nodes).
  </action>
  <verify>
    <automated>pnpm jest --selectProjects Frontend NavShell/__tests__/HeroicVersion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-tour="nav-version"' src/frontend/components/UI/NavShell/components/HeroicVersion/index.tsx` returns exactly 1.
    - `grep -n 'no longer resolve\|are both gone' src/frontend/components/UI/NavShell/components/HeroicVersion/index.tsx` returns no lines.
    - `test -f src/frontend/components/UI/NavShell/__tests__/HeroicVersion.test.tsx` succeeds and the suite reports at least 3 passing tests.
    - `grep -c '\.type' src/frontend/components/UI/NavShell/__tests__/HeroicVersion.test.tsx` is at least 1 AND the file contains a comment naming `React.memo` as the reason (prove the unwrap is deliberate, not accidental).
    - `pnpm jest --selectProjects Frontend NavShell/__tests__/` exits 0 — in particular `NavShell.test.tsx` and `destinationCoverage.test.tsx`, which already reference `HeroicVersion`, must not have regressed.
    - `pnpm codecheck` exits 0.
    - RED-PROOF, RECORDED IN THE SUMMARY: move `data-tour="nav-version"` from the outer `<div className="heroicVersionContainer">` onto the inner element that renders the version string; re-run; confirm the `root.props['data-tour']` assertion FAILS while the "exactly one element carries a data-tour prop" assertion still PASSES — this is the "right count, wrong element" case the census alone cannot catch, and it is the whole reason Layer A exists. Restore and confirm green. Paste the failing test name into the SUMMARY.
  </acceptance_criteria>
  <done>The version container carries `nav-version`, the docstring no longer asserts the anchor is permanently gone, the first `HeroicVersion` unit suite exists with the memo unwrap documented, and the "right count wrong element" RED proof is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Eight static JSX attribute additions and three jest suites. No network, IPC, filesystem or user input crosses any boundary in this plan. |

RESEARCH.md's Architectural Responsibility Map is explicit that phase 34.12 has no backend /
IPC / API surface. Stating that plainly rather than inventing severities this surface cannot
produce.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.12-03-01 | Information disclosure | The eight new `data-tour` attributes in the DOM | accept | Values are static UI-region names (`nav-menu`, `nav-quit`, …). They expose no user, account, library or filesystem data. A `data-*` attribute is inert markup. |
| T-34.12-03-02 | Tampering | `data-tour` as a stable selector handle for injected scripts | accept | Anything able to run script in the WebView can already query by `className` (`.NavItem`, `.DownloadsRing`, `.heroicVersionContainer`), all of which are stable today. Adding a `data-*` handle grants no capability that is not already available. |
| T-34.12-03-03 | Denial of service | `QuitButton`'s `showDialogModal` handler, now covered by a test for the first time | accept | No change to the handler. The new test is a strict improvement in coverage of an untested quit path; it introduces no new call site. |
| T-34.12-03-SC | Tampering | npm/pip/cargo installs | N/A — no installs | This plan adds no dependency. The `HeroicVersion` hooks harness is hand-rolled precisely so no jsdom / react-test-renderer install is needed (`src/frontend/jest.config.js` docstring records this as a deliberate constraint). If an executor reaches for `pnpm add`, STOP — out of plan scope, requires a blocking human package-legitimacy checkpoint. |
</threat_model>

<verification>
- `pnpm jest --selectProjects Frontend` exits 0.
- `pnpm codecheck` exits 0.
- `grep -rc 'data-tour="nav-' src/frontend/components/UI/NavShell/components/ | grep -v ':0'` lists exactly four files (NavTabs, DownloadsRing, QuitButton, HeroicVersion) with counts 5/1/1/1.
- All four RED proofs recorded in the SUMMARY with failing test names and restored green counts.
</verification>

<success_criteria>
- Eight anchors present, each on the element named in the interfaces table, none on a wrapper or inner node.
- Twelve or more new assertions, each tied to one anchor on one identified element — no combined "all anchors present" check anywhere.
- `tourDisabled.test.ts` (plan 01's narrowed form) still green.
- No `sidebar-*` string remains in any file this plan touches.
</success_criteria>

<output>
Create `.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-03-SUMMARY.md` when done.
</output>
