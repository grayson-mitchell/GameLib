---
phase: 34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag
plan: 06
type: execute
wave: 4
depends_on: ["34.12-02", "34.12-03", "34.12-04", "34.12-05"]
files_modified:
  - src/frontend/screens/Library/components/LibraryHeader/index.tsx
  - src/frontend/screens/Library/index.tsx
  - src/frontend/screens/Humble/Keys/index.tsx
  - src/frontend/components/UI/NavShell/__tests__/navTourAnchorCensus.test.ts
  - src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts
  - .planning/ROADMAP.md
autonomous: true
requirements: [D-05, D-06, D-09, IN-01]

must_haves:
  truths:
    - "No tour selector value appears on more than one element anywhere in the frontend"
    - "No dead tour selector survives anywhere in the frontend"
    - "The tour-disabled gate is gone and a strictly stronger gate stands in its place"
    - "The ROADMAP no longer instructs a future reader to clean up components that do not exist"
  artifacts:
    - path: "src/frontend/components/UI/NavShell/__tests__/navTourAnchorCensus.test.ts"
      provides: "Layer B of the positive gate: per-id uniqueness census + dead-id absence"
      contains: "toHaveLength(1)"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected IN-01 description naming the surviving cleanup targets"
      contains: "humble-keys"
  key_links:
    - from: "src/frontend/components/UI/NavShell/__tests__/navTourAnchorCensus.test.ts"
      to: "every .tsx file under src/frontend"
      via: "readdirSync recursive walk + comment-stripped source scan"
      pattern: "walk\\(FRONTEND_DIR"
---

<objective>
Close the phase's verification loop: sweep the three dead `data-tour` attributes that survive
at HEAD, land Layer B of the positive gate (the per-id uniqueness census), delete the
transitional `tourDisabled.test.ts` now that something strictly stronger replaces it, and
correct the ROADMAP entry that describes IN-01 as work on two components that no longer exist.

Purpose: Layer A (plans 03 and 05) proves each anchor is on the right element but cannot prove
uniqueness — the exact defect class the original duplicate `sidebar-downloads` belonged to.
Layer B proves uniqueness but cannot prove correctness. Only the composition closes both.

Output: three dead attributes removed, one new census suite, `tourDisabled.test.ts` deleted,
and a ROADMAP that no longer sends a future reader after phantom components.

## Decision this plan resolves (Claude's Discretion, per CONTEXT.md)

**`library-header`'s duplicate attribute IS removed** — both occurrences
(`LibraryHeader/index.tsx:99` and `Library/index.tsx:1122`, confirmed at those exact lines at
HEAD). CONTEXT.md left this open. Removing is correct because the value is referenced by ZERO
steps in either tour, so it is dead either way, and leaving a duplicated dead selector in place
is precisely the ambiguity the phase exists to eliminate. Keeping it would also force the
census to carry a permanent exemption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-CONTEXT.md
@.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-RESEARCH.md
@.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-VALIDATION.md

<interfaces>
The three dead attributes to remove (verified at HEAD during planning):

  src/frontend/screens/Library/components/LibraryHeader/index.tsx:99
    <h5 className="libraryHeader" data-tour="library-header">
  src/frontend/screens/Library/index.tsx:1122
    <div className="library-section-header" data-tour="library-header">
  src/frontend/screens/Humble/Keys/index.tsx:130
    <div className="humbleKeysScreen" data-tour="humble-keys">

The file-walking helpers to reuse verbatim (`tourDisabled.test.ts:34-61`, the file this plan
deletes — copy them across BEFORE deleting):

  import { readFileSync, readdirSync } from 'fs'
  import { join, relative } from 'path'
  import { stripSourceComments } from 'backend/testUtils/stripSourceComments'
  const REPO_ROOT = join(__dirname, '..', '..', '..', '..', '..', '..')
  const FRONTEND_DIR = join(REPO_ROOT, 'src/frontend')
  function walk(dir, extensions) { ...readdirSync recursive, filter isFile + extension... }
  function readStripped(path) { return stripSourceComments(readFileSync(path, 'utf8')) }

The thirteen LIVE `nav-*` values after plans 03/04/05, and the two live `library-*` values
after plan 02:

  nav-menu  nav-library  nav-stores  nav-settings  nav-downloads  nav-wine
  nav-manage-accounts  nav-accessibility  nav-docs  nav-community  nav-quit  nav-version
  nav-launcher                     <- deliberate 13th, NOT a step selector (plan 05)
  library-views-collections        <- plan 02
  library-facets                   <- plan 02

Values that must be ABSENT everywhere after this plan:
  every `data-tour="sidebar-*"`, `library-header`, `humble-keys`,
  `library-categories`, `library-filters`

Values that are LIVE and untouched by this phase (do NOT assert uniqueness on these —
`library-view-toggle` legitimately appears twice inside one file on mutually exclusive
render branches, and `library-add-game` is a prop default plus an explicit call site):
  library-game-card  library-search  library-view-toggle  library-sort-az
  library-sort-installed  library-refresh  library-add-game
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the three dead data-tour attributes the IN-01 sweep actually leaves behind</name>
  <files>src/frontend/screens/Library/components/LibraryHeader/index.tsx, src/frontend/screens/Library/index.tsx, src/frontend/screens/Humble/Keys/index.tsx</files>
  <read_first>
    - src/frontend/screens/Library/components/LibraryHeader/index.tsx around line 99 (the `<h5 className="libraryHeader">`) and around line 144/154 (the `AddGameButton` / `ActionIcons` call sites, so the executor can see which `data-tour` props are LIVE and must not be touched)
    - src/frontend/screens/Library/index.tsx around line 1122 (the `<div className="library-section-header">`)
    - src/frontend/screens/Humble/Keys/index.tsx around line 130 (the `<div className="humbleKeysScreen">`)
    - src/frontend/screens/Library/components/LibraryTour.tsx (as modified by plan 02 — confirm none of the three values is referenced by any step before removing)
    - src/frontend/components/UI/NavShell/components/NavShellTour/index.tsx (plan 04's output — same confirmation for the shell tour)
  </read_first>
  <action>
    Delete the `data-tour` attribute — the attribute only, not the element, not the className —
    from exactly three sites: `LibraryHeader/index.tsx`'s `<h5 className="libraryHeader">`,
    `Library/index.tsx`'s `<div className="library-section-header">`, and
    `Humble/Keys/index.tsx`'s `<div className="humbleKeysScreen">`.

    Before deleting, confirm by grep that neither `NavShellTour/index.tsx` nor `LibraryTour.tsx`
    references `library-header` or `humble-keys` in any step's `element` field. If either DOES,
    STOP and report — that would mean an earlier plan wired a step at one of these and the
    inventory is wrong.

    Do NOT touch any other `data-tour` in these files or anywhere else. In particular leave
    alone: `AddGameButton`'s `data-tour={dataTour || 'library-add-game'}` default and the
    explicit `<AddGameButton data-tour="library-add-game" />` at `LibraryHeader/index.tsx:144`;
    `ActionIcons`' `'data-tour'?: string` passthrough prop and the five literal
    `library-*` values inside it; and every live `library-*` value listed in the interfaces
    block. Those are working steps of `LibraryTour`, which is out of scope beyond D-09's two
    re-anchors.

    `ActionIcons` is passed no `data-tour` value by its `LibraryHeader` call site, so it
    currently renders `data-tour={undefined}` (React omits the attribute). RESEARCH.md noted
    this for completeness and explicitly placed it outside IN-01's scope. Leave it. Record in
    the SUMMARY that it was seen and deliberately not changed, so a future sweep does not
    rediscover it as a finding.
  </action>
  <verify>
    <automated>pnpm jest --selectProjects Frontend</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'data-tour="library-header"' src/frontend/ | grep -v ':0'` produces NO output.
    - `grep -rc 'data-tour="humble-keys"' src/frontend/ | grep -v ':0'` produces NO output.
    - `grep -c 'className="libraryHeader"' src/frontend/screens/Library/components/LibraryHeader/index.tsx` returns 1 (the element and its class survived; only the attribute went).
    - `grep -c 'className="humbleKeysScreen"' src/frontend/screens/Humble/Keys/index.tsx` returns 1.
    - `grep -rc 'data-tour="library-add-game"\|data-tour="library-search"\|data-tour="library-refresh"' src/frontend/ | grep -v ':0' | wc -l` returns a non-zero count (the live ids were NOT swept up).
    - `pnpm jest --selectProjects Frontend` exits 0 and `pnpm codecheck` exits 0.
    - The SUMMARY records the grep output proving `library-header` / `humble-keys` are referenced by zero steps in either tour file, run BEFORE the deletion.
  </acceptance_criteria>
  <done>Three dead attributes gone, every live `library-*` id untouched, `ActionIcons`' undefined-passthrough deliberately left with a recorded rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Land the uniqueness census and delete the transitional gate it replaces</name>
  <files>src/frontend/components/UI/NavShell/__tests__/navTourAnchorCensus.test.ts, src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts (deleted)</files>
  <read_first>
    - src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts (as narrowed by plan 01 and trimmed by plan 04 — copy its `walk`/`readStripped`/`REPO_ROOT` helper block and its non-empty-walk sanity test BEFORE deleting the file)
    - .planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-VALIDATION.md § "Positive Gate Contract" (the binding statement of what Layer B measures and what it must NOT become)
    - src/frontend/components/UI/NavShell/components/NavShellTour/index.tsx (plan 04 — the twelve step selectors, so the census's literal id list can be cross-checked by hand against them)
    - src/frontend/components/UI/NavShell/components/SettingsPanel/index.tsx (plan 05 — `nav-launcher`, the thirteenth value that is deliberately not a step)
    - src/backend/testUtils/stripSourceComments.ts (what the comment stripper does, so the executor knows a `data-tour` mentioned in a comment is correctly not counted)
  </read_first>
  <action>
    Create `src/frontend/components/UI/NavShell/__tests__/navTourAnchorCensus.test.ts`.
    This is Layer B of the Positive Gate Contract. It measures UNIQUENESS, not correctness —
    Layer A (the per-row/per-tab/per-component assertions in plans 02, 03 and 05) owns
    correctness. Say that explicitly in the file docstring, and name the failure mode each
    layer alone misses: Layer A cannot see a duplicate; Layer B cannot see "exactly one
    occurrence, on the wrong element". Neither is redundant.

    Copy the `walk` / `readStripped` / `REPO_ROOT` / `FRONTEND_DIR` helper block from
    `tourDisabled.test.ts` verbatim before deleting that file. Using the comment-stripped
    source matters: a `data-tour` value named in a docstring must not be counted, and plan 05
    deliberately writes such a comment about `nav-launcher`.

    Write the id lists as LITERAL arrays in the test. Do NOT derive them from
    `NavShellTour`'s steps or from any exported manifest — deriving would make the gate
    self-sealing: dropping a step from the component would silently shrink the expectation and
    the gate would still pass. A literal list is the contract.

    Assert:
      1. `it.each(NAV_TOUR_STEP_IDS)` over the TWELVE step ids: the number of `.tsx` files
         under `src/frontend` whose stripped source contains `data-tour="{id}"` is exactly 1.
      2. A separate `it` for `nav-launcher`: exactly 1 file. Keep it out of the step-id array
         and comment why — it is a launcher affordance, not a tour step, and conflating them
         would make the array's length assertion below wrong.
      3. `it.each` over `['library-views-collections', 'library-facets']`: exactly 1 file each.
      4. `it.each(DEAD_TOUR_IDS)` over `['library-header', 'humble-keys',
         'library-categories', 'library-filters']`: exactly 0 files.
      5. A separate `it` asserting NO `.tsx` file under `src/frontend` contains any
         `data-tour="sidebar-` prefix at all — this is the property plan 01's transitional gate
         carried, now repo-wide instead of NavShell-only, which is why deleting that gate is a
         strengthening rather than a loss.
      6. `expect(NAV_TOUR_STEP_IDS).toHaveLength(12)` — a manifest-integrity guard so a future
         edit cannot quietly shrink the census.
      7. Non-empty-walk sanity: `expect(walk(FRONTEND_DIR, ['.tsx']).length).toBeGreaterThan(100)`.
         The repo has 322 `.tsx` files under `src/frontend` today; a walk returning zero would
         make every "exactly 0" assertion pass vacuously.

    Then DELETE `src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts`. It is now
    fully subsumed: its surviving `data-tour="sidebar-` check is covered repo-wide by assertion
    5, and its `/TourButton/` check is covered by plan 05's proof that the launcher is a
    `NavItem`. Deleting it only AFTER assertions 1-7 are green is what satisfies CONTEXT.md's
    "replace it — do not simply delete it".
  </action>
  <verify>
    <automated>pnpm jest --selectProjects Frontend NavShell/__tests__/navTourAnchorCensus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/frontend/components/UI/NavShell/__tests__/navTourAnchorCensus.test.ts` succeeds; `test -f src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts` FAILS.
    - The census suite reports at least 20 passing tests (12 + 1 + 2 + 4 + 1 + 1 + 1).
    - `grep -c 'toHaveLength(1)' src/frontend/components/UI/NavShell/__tests__/navTourAnchorCensus.test.ts` returns at least 1, and the file contains NO assertion of the form "contains the substring data-tour" without an id — grep for `includes('data-tour')` and confirm 0 matches. The vacuous form is banned by CONTEXT.md.
    - `pnpm jest --selectProjects Frontend` exits 0.
    - RED-PROOF #1 (duplicate detection — the historical defect), RECORDED IN THE SUMMARY: add a second `data-tour="nav-downloads"` onto any other element in a different file (e.g. the `<aside className="NavShell__tier2">` in `NavShell/index.tsx`); re-run; confirm EXACTLY ONE `it.each` case fails and its name names `nav-downloads`; confirm the other eleven still pass. Restore and confirm green.
    - RED-PROOF #2 (dead-id detection), RECORDED IN THE SUMMARY: re-add `data-tour="library-header"` to `LibraryHeader/index.tsx`; re-run; confirm the `library-header` absence case FAILS while the other three dead-id cases pass. Restore and confirm green.
    - RED-PROOF #3 (sidebar prefix), RECORDED IN THE SUMMARY: add `data-tour="sidebar-menu"` anywhere under `src/frontend`; re-run; confirm assertion 5 FAILS; restore and confirm green.
    - RED-PROOF #4 (not vacuous), RECORDED IN THE SUMMARY: temporarily point `FRONTEND_DIR` at an empty temp directory; re-run; confirm the non-empty-walk sanity test FAILS (rather than every other assertion silently passing). Restore and confirm green. This is the check that stops a broken walk from turning the whole census green.
    - The SUMMARY states which specific assertion of the census replaces each assertion of the deleted `tourDisabled.test.ts`, line by line. A deletion with no stated replacement mapping is not acceptable.
  </acceptance_criteria>
  <done>The census exists with per-id assertions, a literal non-derived manifest, dead-id and sidebar-prefix absence, and a non-vacuity guard; `tourDisabled.test.ts` is deleted with a written assertion-by-assertion replacement mapping; four RED proofs recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the ROADMAP's IN-01 description so the phantom work item does not outlive the phase</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md § "Phase 34.12" (the work-list bullets, specifically the "34.10 IN-01" bullet describing dead props in `CategoryFilter`/`LibraryFilters`, and the "Gate that must be retired" paragraph)
    - .planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-RESEARCH.md § "34.10 IN-01 dead-`data-tour` sweep" (the correction and its evidence)
  </read_first>
  <action>
    ROADMAP.md's Phase 34.12 entry says IN-01 is "dead `data-tour` props in
    `CategoryFilter`/`LibraryFilters` that `Dropdown` never reads". Neither component exists at
    HEAD — phase 34.11 deleted them outright rather than leaving dead props on them. Verify
    this yourself first with a `find` for both names across `src/frontend`; if either turns up,
    STOP and report rather than editing.

    Rewrite that bullet in place. It must state: (a) the original IN-01 description named two
    components 34.11 deleted, so the literal item had nothing to clean; (b) the surviving
    IN-01-shaped cleanup was `library-header` (two occurrences) plus a previously uncatalogued
    dead `data-tour="humble-keys"`; (c) all three were removed by plan 34.12-06. Keep the
    `IN-01` label so the trace back to 34.10 survives — do not renumber or delete the bullet,
    which would break the reference chain.

    Also update the "Gate that must be retired as part of this phase" paragraph: name
    `navTourAnchorCensus.test.ts` as the replacement that landed, and note that the replacement
    is two-layer (per-component correctness assertions distributed across the anchor sites,
    plus this repo-wide uniqueness census), so a future reader does not go looking for a single
    file that carries the whole gate.

    Do NOT edit any other phase's entry, and do not touch the `**Plans:**` line or the plan
    checklist — those are maintained by the planning tooling.
  </action>
  <verify>
    <automated>grep -c 'CategoryFilter' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `find src/frontend -iname '*CategoryFilter*' -o -iname '*LibraryFilters*' | wc -l` returns 0 — the correction's premise, re-verified at execution time and pasted into the SUMMARY.
    - The Phase 34.12 section of ROADMAP.md contains the strings `humble-keys` and `library-header`, and the `IN-01` label is still present.
    - `awk '/^### Phase 34.12:/,/^### Phase 35:/' .planning/ROADMAP.md | grep -c 'navTourAnchorCensus'` returns at least 1.
    - `awk '/^### Phase 34.12:/,/^### Phase 35:/' .planning/ROADMAP.md | grep -c 'CategoryFilter'` returns at least 1 ONLY in the sentence explaining that the component no longer exists — read the matched line and confirm it is the correction, not a surviving instruction to clean it up. Quote that line in the SUMMARY.
    - `git diff --stat .planning/ROADMAP.md` shows changes confined to the Phase 34.12 section (no other `### Phase` heading's body in the diff hunks).
  </acceptance_criteria>
  <done>The IN-01 bullet describes what was actually cleaned and why the original description was wrong, the retired-gate paragraph names its two-layer replacement, and the diff touches only Phase 34.12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo source tree -> census test's filesystem walk | The census reads every `.tsx` file under `src/frontend` from disk at test time. Its input is the repo itself, at source trust level. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.12-06-01 | Denial of service | The census's recursive `readdirSync` walk over 322+ `.tsx` files | accept | Bounded, synchronous, repo-local, and identical to the walk `tourDisabled.test.ts` has run in CI since 34.10 with no measured cost problem. VALIDATION.md's feedback-latency budget is < 20s for the scoped run; this walk is a small fraction of it. |
| T-34.12-06-02 | Repudiation | A future edit silently shrinking the census's id manifest | mitigate | Assertion 6 pins `NAV_TOUR_STEP_IDS` at exactly 12 as a literal array, and the docstring forbids deriving it from `NavShellTour`. Removing a step from the component therefore fails the census rather than shrinking it. Additionally, RED-PROOF #4 pins non-vacuity so a broken walk cannot turn the whole suite green. |
| T-34.12-06-03 | Information disclosure | Removal of three dead `data-tour` attributes | accept | Strictly reduces DOM surface. No new exposure. |
| T-34.12-06-SC | Tampering | npm/pip/cargo installs | N/A — no installs | This plan adds no dependency; the census reuses `backend/testUtils/stripSourceComments`, already present and already used by the file it replaces. If an executor reaches for `pnpm add`, STOP — out of plan scope, requires a blocking human package-legitimacy checkpoint. |
</threat_model>

<verification>
- `pnpm test` (all five jest projects) exits 0 — this is the phase's last autonomous plan, so the full suite runs here, not just the frontend project.
- `pnpm codecheck` exits 0.
- `grep -rc 'data-tour="sidebar-' src/ | grep -v ':0'` produces no output.
- All four census RED proofs plus the pre-deletion zero-reference grep recorded in the SUMMARY.
- The SUMMARY carries the assertion-by-assertion mapping from deleted `tourDisabled.test.ts` to the census.
</verification>

<success_criteria>
- Zero dead `data-tour` values under `src/frontend`; every live one accounted for.
- Each of the twelve step ids and `nav-launcher` occupies exactly one source file.
- `tourDisabled.test.ts` is deleted and every assertion it carried has a named successor.
- ROADMAP.md's IN-01 bullet and retired-gate paragraph describe reality.
</success_criteria>

<output>
Create `.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-06-SUMMARY.md` when done.
</output>
