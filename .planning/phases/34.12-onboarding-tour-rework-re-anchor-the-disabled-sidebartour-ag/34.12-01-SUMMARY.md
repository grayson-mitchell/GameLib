---
phase: 34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag
plan: 01
subsystem: ui
tags: [react, typescript, jest, navshell, onboarding-tour]

# Dependency graph
requires:
  - phase: 34.10
    provides: "NavShell tier-2 row primitive (NavItem) and the tourDisabled.test.ts source gate that proved the retired tour could not start"
provides:
  - "NavItem accepts and forwards 'data-tour'?: string on both its <button> and <NavLink> JSX branches"
  - "tourDisabled.test.ts narrowed from a blanket data-tour ban to a sidebar-* selector ban, so it stays meaningful (and green) as D-05's anchors land"
affects: [34.12-03, 34.12-04, 34.12-05, 34.12-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Literal-hyphenated-key 'data-tour'?: string prop convention (matches AddGameButton/index.tsx, ActionIcons/index.tsx) used instead of camelCase dataTour"

key-files:
  created: []
  modified:
    - src/frontend/components/UI/NavShell/components/NavItem/index.tsx
    - src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx
    - src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts

key-decisions:
  - "Prop named literally 'data-tour' (not camelCase dataTour), matching the repo's only existing precedent, used twice"
  - "tourDisabled.test.ts rewritten in place (not renamed/deleted) so plan 34.12-06 can delete it once navTourAnchorCensus.test.ts subsumes it"

patterns-established:
  - "Transitional gate lifecycle: a gate's docstring names its own deletion owner (plan 34.12-06) so a future executor knows removing it is intentional, not neglect"

requirements-completed: [D-05, D-06]

# Metrics
duration: ~20min
completed: 2026-08-22
---

# Phase 34.12 Plan 01: NavItem data-tour passthrough + tourDisabled gate narrowing Summary

**Re-added the `'data-tour'` prop to `NavItem` (forwarded on both the `<button>` and `<NavLink>` branches) and narrowed `tourDisabled.test.ts` from a blanket `data-tour` absence scan to a `sidebar-*` selector absence scan, unblocking the four D-05 anchors that sit on `NavItem` rows without ever leaving HEAD red.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments

- `NavItemProps` gained `'data-tour'?: string`, destructured and forwarded onto both the `case 'button':` and `default:` (NavLink) JSX branches — unblocking D-05's Wine Manager, Accessibility, Documentation and Ko-fi anchors (NavLink branch) and D-01's launcher row (button branch)
- `NavItem.test.tsx` gained two independent bridging assertions proving each branch forwards the prop on its own, not just in combination
- `tourDisabled.test.ts`'s per-file `it.each` assertion narrowed from `not.toMatch(/data-tour/)` to `not.toMatch(/data-tour="sidebar-/)`, converting a gate that Task 1 would otherwise have broken into one that still holds meaning for the rest of the phase (D-06's rename)
- Docstring on `tourDisabled.test.ts` rewritten to state its transitional status and name plan 34.12-06 as its deletion owner
- Sequencing trap flagged in planning was avoided: HEAD is green at every commit boundary, including after Task 1 landed the first `data-tour` token in NavShell source

## Task Commits

Each task was committed atomically:

1. **Task 1: Add the 'data-tour' passthrough to NavItem and prove the forward on both branches** - `11b278b04` (feat)
2. **Task 2: Narrow tourDisabled.test.ts from a blanket data-tour ban to a sidebar-* selector ban** - `9ccbbb38a` (test)

_Note: within-task commit only; no plan-metadata commit is made by this executor per the orchestrator's shared-artifact prohibition — STATE.md/ROADMAP.md are owned by the orchestrator._

## Files Created/Modified

- `src/frontend/components/UI/NavShell/components/NavItem/index.tsx` - Added `'data-tour'?: string` to `NavItemProps`, destructured as `'data-tour': dataTour`, forwarded via `data-tour={dataTour}` on both the `<button>` and `<NavLink>` branches; rewrote the stale docstring paragraph claiming the prop was dropped for good
- `src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx` - Added two new `it` blocks: one invoking the button branch and asserting `element.props['data-tour']` forwards, one invoking the NavLink branch and asserting the same, independently
- `src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts` - Narrowed the per-file assertion from a blanket `data-tour` ban to a `data-tour="sidebar-` ban; rewrote the file docstring to state the gate's transitional status, what it now measures (D-06's rename), and its deletion owner (plan 34.12-06); left the second describe block (`SidebarTour.tsx has no live importer`) completely untouched, as instructed

## Decisions Made

- Matched the repo's existing `'data-tour'?: string` literal-key convention exactly (`AddGameButton/index.tsx:6`, `ActionIcons/index.tsx:25`) rather than introducing a camelCase `dataTour` prop — there is exactly one convention in this codebase and it is used twice
- Kept `tourDisabled.test.ts` at its existing path and existing two-describe-block structure rather than splitting or renaming it, per the plan's explicit instruction that plan 34.12-06 owns its eventual deletion

## Deviations from Plan

None - plan executed exactly as written. Both tasks' acceptance criteria were verified via the exact grep/jest/tsc commands specified in the plan, and both RED-proofs were run in both directions as required.

## RED-Proof Evidence (recorded per plan's verification requirement)

**Task 1 — branch-isolated forward proof:**

Mutation: removed `data-tour={dataTour}` from the `<NavLink>` branch only (button branch left untouched).

Result:
```
FAIL src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx
  ● NavItem › NavLink branch forwards data-tour onto the rendered NavLink

    expect(received).toBe(expected) // Object.is equality
    Expected: "nav-wine"
    Received: undefined

Tests: 1 failed, 1940 skipped, 1 passed, 1942 total
```
The button-branch assertion ("button branch forwards data-tour onto the rendered `<button>`") passed; only the NavLink-branch assertion failed — confirming the two branches are tested independently, not conflated.

Restored, re-ran with `-t "forwards data-tour"`:
```
PASS Frontend src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx
Tests: 1940 skipped, 2 passed, 1942 total
```
Both assertions pass after restore.

**Task 2 — sidebar-\* detection proof (both directions):**

(a) Mutation: added `data-tour="sidebar-menu"` to the `<Tabs>` element in `NavShell/components/NavTabs/index.tsx`.

Result:
```
FAIL src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts
  ● NavShell source files never reference the disabled tour ›
    src/frontend/components/UI/NavShell/components/NavTabs/index.tsx contains no
    SidebarTour, SIDEBAR_TOUR_ID, TourButton or sidebar-* data-tour reference

Test Suites: 1 failed, 118 passed, 119 total
```
The failure names `NavTabs/index.tsx` specifically (per-file `it.each` naming preserved).

Removed the mutation, re-ran the full NavShell suite:
```
Test Suites: 119 passed, 119 total
Tests:       1942 passed, 1942 total
```

(b) Non-vacuity check, run with `-t "sanity check"`:
```
PASS Frontend src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts
  NavShell source files never reference the disabled tour
    ✓ scans at least one NavShell source file (sanity check the walk found something) (2 ms)
Tests: 1941 skipped, 1 passed, 1942 total
```
The walk finds and scans real files (16 non-`__tests__` `.tsx` files under `NavShell/` at time of this plan); the gate is not vacuously green.

## Issues Encountered

None. One minor self-correction during Task 2: the acceptance criterion `grep -n 'cannot start\|is disabled\|tour is off' ... returns no lines in the docstring making a now-false claim` initially matched a line in the new docstring itself (a negation — "no longer asserts... 'the tour is disabled'" — which is not a false claim, but contains the literal substring). Reworded that sentence to avoid the literal trigger phrase while preserving the same meaning, so the grep-based acceptance check is unambiguous rather than relying on a human reading the negation correctly. The one remaining match (line 94, the second describe block's test title, "...proves the tour cannot start") is untouched per the plan's explicit instruction to leave that block alone, and remains literally true — nothing imports `SidebarTour.tsx`.

## Next Phase Readiness

- Plans 03, 04 and 05 are unblocked: `NavItem` now accepts and forwards `'data-tour'`, so the Wine Manager, Accessibility, Documentation and Ko-fi anchors can be added without further primitive changes
- `tourDisabled.test.ts` remains green and meaningful (bans `sidebar-*` tokens) through the rest of the phase; plan 34.12-04's `NavShellTour`/`NAV_TOUR_ID` rename and plan 34.12-06's `navTourAnchorCensus.test.ts` are both accounted for in its current docstring and assertions
- No blockers

## Self-Check: PASSED

All claimed files and commits verified present:
- FOUND: `src/frontend/components/UI/NavShell/components/NavItem/index.tsx`
- FOUND: `src/frontend/components/UI/NavShell/__tests__/NavItem.test.tsx`
- FOUND: `src/frontend/components/UI/NavShell/__tests__/tourDisabled.test.ts`
- FOUND: `.planning/phases/34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag/34.12-01-SUMMARY.md`
- FOUND commit: `11b278b04` (Task 1)
- FOUND commit: `9ccbbb38a` (Task 2)

---
*Phase: 34.12-onboarding-tour-rework-re-anchor-the-disabled-sidebartour-ag*
*Completed: 2026-08-22*
