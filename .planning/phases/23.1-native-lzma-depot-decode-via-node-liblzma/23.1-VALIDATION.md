---
phase: 23.1
slug: native-lzma-depot-decode-via-node-liblzma
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-17
---

# Phase 23.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest (`ts-jest` preset) |
| **Config file** | `jest.config.js` |
| **Quick run command** | `pnpm test -- src/backend/storeManagers/steam/__tests__/decompressPool.test.ts` |
| **Full suite command** | `pnpm test` |
| **Estimated runtime** | ~60-120 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test -- <touched test file>`
- **After every plan wave:** Run `pnpm test` (full suite)
- **Before `/gsd:verify-work`:** Full suite must be green, PLUS the manual live-hardware SEA-binary spike from Wave 1 (jest cannot close this)
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

*No `phase_req_ids` are mapped to this inserted phase (goal states "none mapped — TBD"). Planning finished 2026-08-18 across 5 plans in 4 waves; this table reflects the actual plan/task assignments.*

| Behavior | Plan | Wave | Test Type | Automated Command | File Exists | Status |
|----------|------|------|-----------|--------------------|-------------|--------|
| Native `.node` addon extracts + `dlopen()`s correctly inside a real compiled SEA binary from an eval'd worker (mechanism proof, throwaway harness) | 01 | 1 | manual-only / live-hardware, gated by checkpoint | N/A — spike harness under `.planning/spikes/023-sea-native-addon-dlopen/` | ✅ existing spike plan | ⬜ pending |
| `buildSidecarSea.ts`'s new native-asset embedding step (mirrors `bundleWorkerForSea()`/`buildSeaConfig()`); adopts `lzma-native@8.0.6` exact-pinned | 02 | 2 | unit + build | `pnpm test -- meta/__tests__/buildSidecarSea.test.ts && pnpm build:sidecar-sea && node -e "...assets keys length===2..."` | ✅ existing test file/pattern, extended with new asset-wiring assertions | ⬜ pending |
| `lzmaLoader.ts` resolves native-first with a loud, logged pure-JS fallback if the native module fails to load | 04 (Task 1) | 3 | unit | `pnpm test -- decompressPool.test.ts -t "lzmaLoader" && pnpm codecheck` | ✅ new file + new test block | ⬜ pending |
| `decodeChunk` VZ branch round-trips via `lzma-native` to the same bytes as the current pure-JS path, with `decompress.ts`'s container parsing provably untouched (comment-only diff) | 04 (Task 2) | 3 | unit | `pnpm test -- decompressPool.test.ts && pnpm codecheck && test $(git diff --unified=0 decompress.ts \| grep -E '^[+-][^+-]' \| grep -vE '^[+-]\s*(//\|/?\*)' \| wc -l) -eq 0` | ✅ existing test file, extended | ⬜ pending |
| Native addon bytes are physically present in the compiled SEA binary (byte-offset proof, not string-tool inference) | 05 (Task 1) | 4 | auto | see plan 23.1-05 Task 1 `<automated>` — `Buffer.indexOf` probe against the compiled sidecar binary | ✅ build-output assertion | ⬜ pending |
| Native `.node` addon `dlopen()`s correctly in the SHIPPING production binary (real depot install, real eval'd worker) | 05 (Task 2) | 4 | manual-only / live-hardware, blocking checkpoint | N/A — real `gamelib-sidecar-<triple>` binary + real Steam depot install | ❌ no jest-closable test possible | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.* `decompressPool.test.ts` and `meta/__tests__/buildSidecarSea.test.ts` already exist with directly extensible pure-function test patterns (`resolveWorkerSpec`/`buildSeaConfig`). No new test scaffolding or framework install needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Native `.node` addon `process.dlopen()`s successfully from inside a `new Worker(source, { eval: true })` worker in a real compiled SEA binary — mechanism proof | Plan 01, Wave 1 | No jest-closable test can exercise a real compiled SEA binary's native dlopen from an eval'd worker context — this combination has zero precedent in this codebase or in Node's own SEA docs | Spike harness under `.planning/spikes/023-sea-native-addon-dlopen/`: build a real SEA binary, launch it, and confirm dlopen succeeds from the eval'd-worker context |
| Real Steam depot-chunk throughput on actual target hardware/triples | Plan 01, Wave 1 | Only synthetic/darwin-arm64 benchmark data exists so far; the ~47x speedup must be confirmed against a real captured LZMA depot chunk | Decode a real captured Steam depot LZMA chunk with `lzma-native` and compare wall-clock throughput against the current pure-JS `lzma` baseline |
| Native `.node` addon `dlopen()`s correctly in the shipping production binary against a real Steam depot install | Plan 05, Wave 4, Task 2 (blocking checkpoint) | Confirms the mechanism proven in Plan 01's throwaway harness also holds in the actual packaged sidecar + `DecompressPool`/`decompressWorker.ts` integration, not just the spike | Build the production SEA sidecar with the native asset embedded, launch it for real, trigger a depot decode that exercises the native path, and confirm via logs/`stats().nativeWorkers` that the native addon loaded and decoded correctly (not the pure-JS fallback) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (checkpoint tasks in plans 01 and 05 correctly have none)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (confirmed by gsd-plan-checker run 2026-08-18)
- [x] Wave 0 covers all MISSING references (existing infra sufficient — no Wave 0 gaps)
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-18 (gsd-plan-checker VERIFICATION PASSED, 0 blockers)
