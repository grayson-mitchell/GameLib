---
phase: 23.1
slug: native-lzma-depot-decode-via-node-liblzma
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-08-17
---

# Phase 23.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest (`ts-jest` preset) |
| **Config file** | `jest.config.js` |
| **Quick run command** | `pnpm test -- src/backend/storeManagers/steam/__tests__/decompressPool.test.ts` |
| **Full suite command** | `pnpm test` |
| **Estimated runtime** | ~60-120 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test -- <touched test file>`
- **After every plan wave:** Run `pnpm test` (full suite)
- **Before `/gsd:verify-work`:** Full suite must be green, PLUS the manual live-hardware SEA-binary spike from Wave 1 (jest cannot close this)
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

*No `phase_req_ids` are mapped to this inserted phase (goal states "none mapped — TBD"). The planner assigns Task IDs; this table is the behavior→test contract those tasks must satisfy.*

| Behavior | Plan | Wave | Test Type | Automated Command | File Exists | Status |
|----------|------|------|-----------|--------------------|-------------|--------|
| `decodeChunk` VZ branch round-trips via `lzma-native` to the same bytes as the current pure-JS path | TBD | TBD | unit | `pnpm test -- decompressPool.test.ts -t "VZ"` | ✅ existing test file, needs new case(s) | ⬜ pending |
| `DecompressPool` falls back to inline pure-JS decode if native module fails to load | TBD | TBD | unit | `pnpm test -- decompressPool.test.ts -t "fallback"` | ✅ existing pattern present — extend for native-load failure | ⬜ pending |
| Native `.node` addon extracts + `dlopen()`s correctly inside a real compiled SEA binary, INCLUDING from an eval'd worker | TBD | 1 (spike) | manual-only / live-hardware | N/A — real `gamelib-sidecar-<triple>` binary + real worker spawn | ❌ no jest-closable test possible | ⬜ pending |
| `buildSidecarSea.ts`'s new native-asset embedding step (mirrors `bundleWorkerForSea()`/`buildSeaConfig()`) | TBD | TBD | unit | `pnpm test -- meta/__tests__/buildSidecarSea.test.ts` | ✅ existing test file/pattern, needs new asset-wiring assertions | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.* `decompressPool.test.ts` and `meta/__tests__/buildSidecarSea.test.ts` already exist with directly extensible pure-function test patterns (`resolveWorkerSpec`/`buildSeaConfig`). No new test scaffolding or framework install needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Native `.node` addon `process.dlopen()`s successfully from inside a `new Worker(source, { eval: true })` worker in a real compiled SEA binary | N/A (TBD phase_req_ids) | No jest-closable test can exercise a real compiled SEA binary's native dlopen from an eval'd worker context — this combination has zero precedent in this codebase or in Node's own SEA docs | Build the SEA sidecar with the native asset embedded, launch it for real, trigger a depot decode that exercises the native path, and confirm via logs/output that the native `.node` addon loaded and decoded correctly (not the pure-JS fallback) |
| Real Steam depot-chunk throughput on actual target hardware/triples | N/A (TBD phase_req_ids) | Only synthetic/darwin-arm64 benchmark data exists so far; the ~47x speedup must be confirmed against a real captured LZMA depot chunk per CONTEXT.md's Wave 1 spike recommendation | Decode a real captured Steam depot LZMA chunk with `lzma-native` and compare wall-clock throughput against the current pure-JS `lzma` baseline |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (existing infra sufficient — no Wave 0 gaps)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
