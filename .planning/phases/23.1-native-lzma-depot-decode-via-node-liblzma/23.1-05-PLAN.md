---
phase: 23.1-native-lzma-depot-decode-via-node-liblzma
plan: 05
type: execute
wave: 4
depends_on: ["03", "04"]
files_modified:
  - .planning/debug/humankind-depot-full-stall.md
autonomous: false
# No phase_req_ids are mapped to this inserted urgent-work phase (ROADMAP
# "Requirements: TBD"). Source of truth is the phase goal + CONTEXT.md/
# RESEARCH.md decisions, per the planning brief.
requirements: []

must_haves:
  truths:
    - "A REAL compiled gamelib-sidecar binary, built by the real pnpm build:sidecar-sea pipeline, contains the native addon bytes in its SEA blob"
    - "A real Steam depot install run on live hardware reports native decode engaged in its own gamelib.log — not inferred, quoted"
    - "The real end-to-end install wall-clock for a reference title is measured and compared against this debug arc's recorded baseline"
    - "If native decode did NOT engage, that fact is recorded with the log evidence rather than the phase being closed on a green jest suite"
  artifacts:
    - path: ".planning/debug/humankind-depot-full-stall.md"
      provides: "Live-gate record: quoted log lines, measured wall-clock, and the resolution or continuation of the open debug investigation"
      contains: "23.1"
  key_links:
    - from: "src-tauri/binaries/gamelib-sidecar-<triple>"
      to: "the embedded lzma-native prebuild"
      via: "SEA blob asset embedded by buildSeaConfig()/writeSeaConfig()"
      pattern: "lzma_native.node"
    - from: "a live depot install"
      to: "gamelib.log"
      via: "lzmaLoader's one-time decoder log line and DecompressPool.stats().nativeWorkers"
      pattern: "nativeWorkers"
---

<objective>
Close the one property in this phase that no jest test can reach: whether the native
LZMA addon actually loads and decodes inside a REAL compiled SEA sidecar during a REAL
Steam depot install, and whether that produces a real throughput improvement.

Purpose: 23.1-VALIDATION.md declares this behavior manual-only and jest-unclosable, and
this debug arc has now three times shipped a change that was green in the suite and
inert on the shipped binary — including one fix that ran unnoticed through five live
runs because nothing logged which path had been taken. A green `pnpm test` is not
evidence that this phase worked.

Output: a live-gate record appended to `.planning/debug/humankind-depot-full-stall.md`
carrying quoted log evidence and a measured wall-clock, and either the resolution of
that open investigation or an explicit statement of what remains.

Depends on plans 23.1-03 and 23.1-04 both being complete — the build half and the
runtime half must both be in place before a live run means anything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23.1-native-lzma-depot-decode-via-node-liblzma/23.1-CONTEXT.md
@.planning/phases/23.1-native-lzma-depot-decode-via-node-liblzma/23.1-VALIDATION.md
@.planning/phases/23.1-native-lzma-depot-decode-via-node-liblzma/23.1-03-SUMMARY.md
@.planning/phases/23.1-native-lzma-depot-decode-via-node-liblzma/23.1-04-SUMMARY.md
@.planning/debug/humankind-depot-full-stall.md

<interfaces>
<!-- Reference values reproduced from CONTEXT.md and the debug file. -->

Reference titles used throughout this debug arc:
    HUMANKIND                       appId 1124300
    Age of Wonders: Planetfall      appId 718850

Recorded pre-phase baseline (CONTEXT.md, live-measured on this hardware):
    network fetch   ~150-280 ms/chunk, flat -- NOT the bottleneck
    decode          pure-JS `lzma`, ~5-7 MB/s per thread -- THE bottleneck
    HUMANKIND       ~1.5 h end-to-end vs Steam's own ~5 min
    pool sizing     min(os.cpus().length, 16) -- confirmed correct, out of scope

Synthetic expectation to beat, NOT to trust (RESEARCH.md, darwin-arm64, synthetic payload):
    pure-JS   ~7 MB/s     native   ~332 MB/s     speedup  47.2x
23.1-01-SUMMARY carries the real-chunk speedup measured on this machine -- use that
number as the expectation, not the synthetic one.

Build + run commands:
    pnpm test                 full jest suite
    pnpm codecheck            typecheck + lint
    pnpm build:sidecar-sea    produces src-tauri/binaries/gamelib-sidecar-<triple>
    pnpm tauri:dev            NOTE: `tauri dev` alone serves a STALE static bundle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight — full suite, real SEA build, and blob-level proof the addon shipped</name>
  <read_first>
    - .planning/phases/23.1-native-lzma-depot-decode-via-node-liblzma/23.1-03-SUMMARY.md (the shipped alias flag string and the shim's exported symbol names)
    - .planning/phases/23.1-native-lzma-depot-decode-via-node-liblzma/23.1-04-SUMMARY.md (the exact log line text emitted on each decoder branch — the live gate greps for these verbatim, so they must be known before the binary is built)
    - .planning/phases/23.1-native-lzma-depot-decode-via-node-liblzma/23.1-02-SUMMARY.md (the `LZMA_NATIVE_ASSET_KEY` literal and the observed on-disk prebuild `.node` filename)
  </read_first>
  <files>(no source files modified — this task builds and asserts)</files>
  <action>
    Run the gates in this order and stop at the first failure rather than pressing on:

    (a) `pnpm codecheck` and `pnpm test` (full suite). Note in the SUMMARY the git SHA
    these ran against — a subagent's green suite count is stamped to its own base, and
    this repo has had HEAD move mid-session before.

    (b) `pnpm build:sidecar-sea` from a clean `build/` directory. Delete `build/` first:
    this repo has repeatedly been bitten by stale output caches, including a
    file-to-directory transition that poisoned every non-cleaned cache, so a cold build
    is the only one whose result means anything here.

    (c) Assert the emitted `build/sea-config.json` has exactly two `assets` keys and that
    the native one points at an existing file.

    (d) Assert the compiled binary at `src-tauri/binaries/gamelib-sidecar-<triple>`
    actually carries the addon bytes, not merely that the config said to embed them.
    Compare the size of the freshly compiled binary against a build of the same commit
    with the native asset omitted, or — simpler and sufficient — read the prebuild's
    first 64 bytes and confirm that exact byte sequence occurs inside the compiled
    binary. Do this with a small Node one-liner using `Buffer.indexOf`, not `strings` or
    `grep` (the payload is binary and both tools will lie about it). This is the step
    that distinguishes "the build script intended to embed it" from "the blob contains
    it"; every prior defect in this arc lived precisely in that gap.

    (e) Record the compiled binary's size and mtime, and the resolved triple, so the
    human gate in Task 2 can confirm it is running the binary this task produced and not
    a stale one.

    Do not modify any source file in this task. If any assertion fails, stop and report
    which one — do not proceed to the human gate with a binary that failed its
    pre-flight.
  </action>
  <acceptance_criteria>
    - `pnpm codecheck` exits 0
    - `pnpm test` exits 0 with zero failing suites
    - `build/` was removed before `pnpm build:sidecar-sea`, and that command exits 0
    - `node -e "const c=require('./build/sea-config.json'); const k=Object.keys(c.assets); if(k.length!==2) process.exit(1)"` exits 0
    - The native asset path named in `build/sea-config.json` exists on disk
    - A Node `Buffer.indexOf` check confirms the first 64 bytes of the prebuild `.node` file occur inside `src-tauri/binaries/gamelib-sidecar-<triple>` — asserted with an explicit non-zero-index result, not merely a truthy one
    - The SUMMARY records the git SHA, resolved triple, binary size, and binary mtime
  </acceptance_criteria>
  <verify>
    <automated>pnpm codecheck && pnpm test && rm -rf build && pnpm build:sidecar-sea && node -e "const fs=require('fs');const c=require('./build/sea-config.json');const k=Object.keys(c.assets);if(k.length!==2){console.error('assets:',k);process.exit(1)}const nk=k.find(x=>x.endsWith('.node'));const probe=fs.readFileSync(c.assets[nk]).subarray(0,64);const bins=fs.readdirSync('src-tauri/binaries').filter(f=>f.startsWith('gamelib-sidecar-'));if(!bins.length){console.error('no sidecar binary');process.exit(1)}const bin=fs.readFileSync('src-tauri/binaries/'+bins[0]);const at=bin.indexOf(probe);if(at<0){console.error('addon bytes NOT found in compiled binary');process.exit(1)}console.log('addon bytes found at offset',at,'in',bins[0],'size',bin.length)"</automated>
  </verify>
  <done>The full suite is green, a cold SEA build succeeded, and the native addon's bytes are proven present inside the compiled sidecar binary by offset — not inferred from the build script's intent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-hardware gate — run a real Steam depot install and confirm native decode engaged</name>
  <read_first>
    - .planning/phases/23.1-native-lzma-depot-decode-via-node-liblzma/23.1-04-SUMMARY.md (the verbatim log line text to grep for, and the `stats()` field name)
    - .planning/debug/humankind-depot-full-stall.md (the recorded baseline timings and the shape prior live rounds were recorded in)
  </read_first>
  <what-built>
    A cold-built `gamelib-sidecar-<triple>` binary whose SEA blob is byte-proven (Task 1,
    step d) to contain `lzma-native`'s prebuilt addon, wired so that each pooled decode
    worker `dlopen()`s that addon at spawn via the `--alias:node-gyp-build` shim and
    reports its resolved decoder kind in its ready handshake, with a one-time log line on
    both the native-success and pure-JS-fallback branches and a `nativeWorkers` count in
    `DecompressPool.stats()`.

    Everything automatable has already been automated. What remains is the property
    23.1-VALIDATION.md declares jest-unclosable: whether this works on real hardware
    against real Steam depot bytes.
  </what-built>
  <how-to-verify>
    1. Confirm you are running the binary Task 1 built: check
       `src-tauri/binaries/gamelib-sidecar-<triple>`'s size and mtime against the values
       recorded in Task 1's output. If they differ, rebuild before continuing.
    2. Launch GameLib against that sidecar. Use `pnpm tauri:dev` if running from source —
       NOT bare `tauri dev`, which serves a stale static bundle.
    3. Start a real Steam native depot install of a reference title. Prefer HUMANKIND
       (appId 1124300) because this debug arc's ~1.5h baseline was measured on it; Age of
       Wonders: Planetfall (appId 718850) is the smaller alternative if you want a faster
       round-trip first.
    4. While it downloads, watch `gamelib.log` and answer these, quoting the actual lines:
       a. Does the native-decoder success line appear (the verbatim text recorded in
          23.1-04-SUMMARY), or the pure-JS fallback warning?
       b. Does `DecompressPool.stats()` report `nativeWorkers` equal to the pool size, or
          `0`, or something in between? A partial count means some workers fell back and
          is a REAL finding, not noise.
       c. Is `inlineFallback` still `false`? (If it is `true`, the pool never spawned and
          this phase's change is inert regardless of what the decoder lines say — that is
          the exact failure mode quick task 260817-pkx fixed and it must not have
          regressed.)
    5. Let the install run to completion, or long enough to measure a stable
       bytes-per-second, and record the wall-clock. Compare against the ~1.5h HUMANKIND
       baseline in `.planning/debug/humankind-depot-full-stall.md`.
    6. Confirm the installed game actually launches, or at minimum that the install
       reported success with no sha1/size verification failures in the log — a fast
       decode that produces wrong bytes would be caught by `decodeChunk()`'s integrity
       gate and would surface as decode errors, so their ABSENCE is part of the evidence.
    7. If the native line did NOT appear: capture the fallback warning's cause message
       verbatim. Do not approve on the grounds that the install still worked — it would
       still work with a completely inert change, which is precisely how this arc lost
       five live rounds.
  </how-to-verify>
  <acceptance_criteria>
    - The operator has quoted, verbatim, the decoder line that appeared in `gamelib.log`
    - The operator has stated the observed `nativeWorkers` value and the `inlineFallback` value
    - The operator has stated the measured wall-clock or throughput and the title used
    - The operator has confirmed the absence or presence of sha1/size verification failures
  </acceptance_criteria>
  <resume-signal>Type "approved" with the quoted log lines, the nativeWorkers/inlineFallback values, and the measured timing — or describe what appeared instead</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the live-gate evidence in the open debug investigation</name>
  <read_first>
    - .planning/debug/humankind-depot-full-stall.md (the whole file — match the existing per-round record format and the frontmatter's `status:` field exactly; do not invent a new section shape)
  </read_first>
  <files>.planning/debug/humankind-depot-full-stall.md</files>
  <action>
    Append a new round record to `.planning/debug/humankind-depot-full-stall.md` in the
    same shape as the existing rounds, covering phase 23.1: the change made (native
    `lzma-native` decode replacing pure-JS `lzma` for VZ chunks, embedded as a SEA asset
    and `dlopen()`ed per worker), the pre-flight evidence from Task 1 (git SHA, triple,
    binary size, the byte-offset at which the addon was found inside the compiled
    binary), and the live-gate evidence from Task 2 verbatim — the quoted log lines, the
    `nativeWorkers` and `inlineFallback` values, and the measured wall-clock beside the
    recorded ~1.5h HUMANKIND baseline.

    Then update the file's `status:` frontmatter honestly against what was actually
    observed:
      - Native decode engaged AND throughput materially improved -> resolve the
        investigation, stating the confirmed multiplier and naming the new binding
        constraint (CONTEXT.md predicts network fetch at ~150-280ms/chunk becomes the
        ceiling again — say whether the measurement agrees).
      - Native decode engaged but throughput did NOT materially improve -> leave the
        investigation open and record that the phase's premise (decode is THE bottleneck)
        now has a counter-measurement, which is a finding worth more than a closed ticket.
      - Native decode did NOT engage -> leave the investigation open, record the fallback
        cause message verbatim, and state which of the three layers (SEA asset embedding,
        the `--alias:node-gyp-build` shim, or the loader) the evidence implicates.

    Do not write "COMPLETE", "resolved", or any success language that the quoted evidence
    does not support. A record that overstates is worse than no record — this file is the
    ground truth future sessions will trust.

    Also note in the record whether the pure-JS `lzma` package and its
    `patches/lzma.patch` entry are still required (they are: the fallback path depends on
    them), so a future cleanup pass does not delete the safety net.
  </action>
  <acceptance_criteria>
    - `.planning/debug/humankind-depot-full-stall.md` contains a new round section referencing phase `23.1`
    - That section quotes the decoder log line verbatim and states the `nativeWorkers` and `inlineFallback` values observed
    - That section states the measured wall-clock/throughput beside the recorded baseline
    - The file's `status:` frontmatter matches one of the three outcomes above and is consistent with the quoted evidence
    - `grep -c "lzma" .planning/debug/humankind-depot-full-stall.md` returns at least 1 and the record explicitly states the pure-JS fallback dependency must not be removed
  </acceptance_criteria>
  <verify>
    <automated>grep -q "23.1" .planning/debug/humankind-depot-full-stall.md && grep -q "nativeWorkers" .planning/debug/humankind-depot-full-stall.md && grep -q "inlineFallback" .planning/debug/humankind-depot-full-stall.md</automated>
  </verify>
  <done>The open debug investigation carries a round record for phase 23.1 whose stated status is entailed by the quoted live evidence, and the pure-JS fallback's continued necessity is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Steam CDN -> live install | A real live run pulls attacker-influenceable depot bytes through the new native decoder for the first time |
| gamelib.log -> the debug record | Log excerpts are copied verbatim into a committed planning artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.1-05-01 | Information Disclosure | log excerpts quoted into `.planning/debug/humankind-depot-full-stall.md` | mitigate | Only the decoder-kind line, the `stats()` counters, and timing values are quoted. Depot keys, CDN auth tokens, and decrypted bytes are never logged by `decompressWorker.ts` (T-21-15-02) or by `lzmaLoader.ts` (asserted by plan 23.1-04's acceptance criteria), so there is nothing sensitive in the lines this task copies. A SteamID64 appearing in surrounding log context is a known, accepted redact-forward case for this repo |
| T-23.1-05-02 | Tampering | live decode of real CDN bytes through native code | mitigate (residual accepted) | `decodeChunk()`'s sha1/size integrity gate runs unchanged on every chunk, and the human gate explicitly asks whether any verification failures appeared — their absence across a full multi-GB title is stronger evidence of decode correctness than any unit fixture. The residual native-memory-safety risk is the one already accepted in plans 23.1-01 and 23.1-04 |
| T-23.1-05-03 | Repudiation | a phase closed on a green suite rather than live evidence | mitigate | Task 1's byte-offset assertion and Task 2's blocking human gate exist for exactly this. Task 3 forbids success language the quoted evidence does not entail, and the gate explicitly instructs the operator not to approve merely because the install completed |
</threat_model>

<verification>
1. `pnpm codecheck` and `pnpm test` both exit 0 at a recorded git SHA
2. A cold `pnpm build:sidecar-sea` exits 0 and the addon's bytes are found at a specific offset inside the compiled sidecar binary
3. The blocking live-hardware gate has been answered with quoted log lines, not a summary
4. `.planning/debug/humankind-depot-full-stall.md` carries a phase-23.1 round record whose `status:` is entailed by that evidence
</verification>

<success_criteria>
- The phase's central claim — that native decode engages inside a real compiled SEA sidecar during a real install — is either proven with quoted evidence or explicitly recorded as unproven, with the implicated layer named
- The throughput change is measured against this arc's own recorded baseline, not against a synthetic benchmark
- No success language exists anywhere that the live evidence does not support
</success_criteria>

<output>
Create `.planning/phases/23.1-native-lzma-depot-decode-via-node-liblzma/23.1-05-SUMMARY.md` when done.
The SUMMARY MUST record: (1) the git SHA the gates ran against, (2) the quoted decoder
log line and the `nativeWorkers`/`inlineFallback` values, (3) the measured wall-clock
beside the baseline, (4) the resulting `status:` of the debug investigation, and (5) if
native decode did not engage, which layer the evidence implicates and what the next
investigation step should be.
</output>
