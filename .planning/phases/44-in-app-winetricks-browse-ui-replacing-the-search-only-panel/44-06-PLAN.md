---
phase: 44-in-app-winetricks-browse-ui-replacing-the-search-only-panel
plan: 06
type: execute
wave: 5
depends_on:
  - 44-05
files_modified:
  - public/locales/*/gamelib.json
  - public/locales/*/translation.json
  - meta/i18nCatalogPresenceBaseline.json
autonomous: true
requirements:
  - REQ-44-08
  - REQ-44-18
must_haves:
  truths:
    - "D-08: the locale fill happens inside Phase 44, not as a follow-on — the phase cannot be called done with the gamelib gate red"
    - "D-10: `pnpm machine-fill-gamelib` is re-measured ONCE before any hand-fill, and the result is recorded either way"
    - "D-10: every hand-filled value passes a pre-flight validator BEFORE any file is touched — locale-set check, bidirectional placeholder parity, glossary survival, no empty values"
    - "D-09: the fill contains no `winetricksBrowse.category.*` key"
    - "D-20: the orphaned `translation.json` `winetricks.*` keys are removed across 47 locale dirs — not 49; `br` and `sl` have no `translation.json` at all"
    - "D-20: `winetricks.install`, `winetricks.openGUI` and `winetricks.loading-available` survive the removal"
    - "REQ-44-18: the removal is structural, not a blind line delete — a key that is last in its object strands a trailing comma on the preceding line"
  artifacts:
    - path: "public/locales/en/gamelib.json"
      provides: "the source catalog all 48 targets are filled against"
      contains: "winetricksBrowse"
  key_links:
    - from: "public/locales/*/gamelib.json"
      to: "meta/__tests__/gamelibCatalogParity.test.ts"
      via: "the parity gate reads every catalog against the English source"
      pattern: "winetricksBrowse"
    - from: "public/locales/*/translation.json"
      to: "src/"
      via: "no surviving consumer for any removed key"
      pattern: "winetricks\\."
---

<objective>
Close the two locale-catalog obligations: fill the 13 `winetricksBrowse` keys into the 48
non-English `gamelib.json` catalogs until `lint-translations:gamelib` and `gamelibCatalogParity` are
green, and remove the 6 orphaned `winetricks.*` keys from the 47 `translation.json` catalogs that
have one.

Purpose: D-08 makes this in-phase and non-deferrable on a measured basis — `quick-260915-t13`
measured 17 unfilled `gamelib` keys producing 816 findings across 2 failing tests. The `gamelib`
namespace IS gated, unlike the 84 ungated `translation.json` `humbleKeys.*` keys still open in todo
`816`. Deferring the fill only moves the red somewhere easier to forget.

Output: 48 filled `gamelib.json` files, 47 pruned `translation.json` files, and two green gates.
</objective>

<planner_decisions>
## Three numbers and one key-count correction this plan operates on

**The fill is 13 keys, not 11.** Plan 44-02 added two keys the UI-SPEC's own contract required but
its copy table omitted — `searchPlaceholder` (because `SearchBar`'s `placeholder` prop is required
and D-20 deletes the key currently supplying it) and `resultsHeading` (because UI-SPEC Interaction
Contract §2 mandates a results header). Corrected arithmetic:

| Figure | Value |
|---|---|
| New keys | 13 |
| Locale dirs with `gamelib.json` | 49 |
| Written by plan 44-02 (English) | 13 |
| **Written by THIS plan (non-English)** | **13 × 48 = 624** |
| Total across the phase | 13 × 49 = 637 |

D-09's "11 × 48 = 528" and `44-PATTERNS.md`'s "11 × 49 = 539" are both superseded. 528 was the
non-English fill under the 11-key count; 539 was the total under it. Neither is the number to
assert against.

**The removal is 6 keys, not 5.** D-20 lists `search`, `no-components`, `installed`, `nothingYet`,
`installing`. `44-PATTERNS.md` measured the `translation.json` `winetricks` block at **9** keys, not
8 — the 9th is a bare `"loading": "Loading"` whose only consumer was `Winetricks/index.tsx:210`
inside the `installedWrapper` that D-11 retired in plan 44-05. RESEARCH Open Question 1 recommends
adding it; plan 44-05's Task 2 census confirms zero surviving consumers. **Planner decision: remove
it as a 6th key.** Shipping it across 47 locale dirs with no consumer is the exact orphaned-key
pattern D-20 exists to close, just missed for this one.

Removal set (6): `search`, `no-components`, `installed`, `nothingYet`, `installing`, `loading`.
Survivor set (3): `install`, `openGUI`, `loading-available`.
6 + 3 = 9, which reconciles against the measured block size.

**The trailing-comma trap does NOT recur for this key, but the structural method is still required.**
`44-PATTERNS.md` ran a direct check of all 47 `translation.json` dirs this session: **0 of 47** are
missing the `winetricks` key, and **0 of 47** hold it last in their top-level object. So the specific
trap measured for the prior `accessibility.disable_smooth_scrolling` removal (which stranded commas
in `da`, `id`, `nl`) does not arm here. Do **not** re-plan defensive work against those three
locales specifically. Do still implement removal as a structural edit rather than a blind line
delete — "last in object" is a per-locale, per-key fact, and the keys being removed here are
*within* the `winetricks` object, where last-key position is a live possibility even though the
object itself is never last. Verify by re-parsing every file.
</planner_decisions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-CONTEXT.md
@.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-PATTERNS.md
@.planning/quick/260915-t13-fill-17-humblekeys-across-48-locales/260915-t13-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

The pre-flight validator to reuse, `meta/machineFillGamelib.ts:285` (verified signature):

```ts
export function validateTranslation(
  source: string,
  target: string,
  glossary: string[]
): string[]   // returns human-readable problems; never throws
```
It checks bidirectional placeholder parity and glossary-term survival. D-10 requires running this
(or its equivalent) before writing any hand-filled value. Reuse it directly rather than
re-deriving the checks — re-deriving risks missing a trap already discovered.

The 13 English source values (shipped by plan 44-02, `public/locales/en/gamelib.json` →
`winetricksBrowse`): `curatedGroup`, `searchPlaceholder`, `resultsHeading` (`{{total}}`),
`cachedTag`, `installedTag`, `needsGuiTag`, `installingRow`, `installFailedTag`, `retry`,
`emptyHeading`, `emptyBody`, `zeroResultHeading` (`{{query}}`), `clearSearch`.

Population facts, verified 2026-09-16:
- `ls public/locales/*/gamelib.json | wc -l` → **49**
- `ls public/locales/*/translation.json | wc -l` → **47** (`br` and `sl` have no `translation.json`)
- `ls -d public/locales/*/ | wc -l` → **49**

Gates: `pnpm lint-translations:gamelib`, `meta/__tests__/gamelibCatalogParity.test.ts`,
`meta/__tests__/lintTranslations.test.ts`, `meta/i18nCatalogPresenceBaseline.json`,
`meta/i18nGlossary.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure pnpm machine-fill-gamelib once, and record the verdict either way</name>
  <files>(no files modified unless the script succeeds — measurement first)</files>
  <read_first>
    - `meta/machineFillGamelib.ts` — the whole script, especially its own D-08 note about deliberately not bulk-running all 48 locales, and the `validateTranslation` export at line 285.
    - `.planning/quick/260915-t13-fill-17-humblekeys-across-48-locales/260915-t13-SUMMARY.md` — the measured precedent: the 108-char `sk-ant-` key in `~/.gamelib.env` returned HTTP 401 at that time, and the fill was completed by hand.
    - `meta/i18nGlossary.json` — the glossary terms `validateTranslation` checks survival for. Confirm whether `Winetricks` is among them before filling; two of the 13 values contain it verbatim.
  </read_first>
  <action>
D-10 requires re-measuring the machine-fill path **once** before assuming it still fails. A key can
be rotated, and one command is cheaper than an assumption.

Run `pnpm machine-fill-gamelib` (consult the script's own `--help`/argument handling first; it may
need the namespace or key prefix named). Record verbatim: the exact command, the exit code, and the
first ~20 lines of output including any HTTP status.

Then branch:
- **If it succeeds** for some or all locales, let it write, then still run `validateTranslation`
  over every value it produced before accepting them (Task 2's validator step is not skipped just
  because a machine produced the strings). Record which locales it covered.
- **If it 401s or otherwise fails**, record that verbatim and proceed to Task 2's hand-fill. Hand
  filling is sanctioned and precedented twice — `260908-iq8` and `260915-t13`.

Do not retry more than once and do not go looking for a different key. The decision D-10 records is
"re-measure once, then hand-fill", not "make the machine path work".

Also confirm in this task whether `Winetricks` is a glossary term in `meta/i18nGlossary.json`. If it
is, every filled value for `needsGuiTag` and `emptyBody` must carry it verbatim, and
`validateTranslation` will fail the value otherwise. Record the answer — Task 2 depends on it.
  </action>
  <verify>
    <automated>pnpm machine-fill-gamelib; echo "machine-fill exit=$?"; node -e "const g=require('./meta/i18nGlossary.json');console.log('Winetricks in glossary:', JSON.stringify(g).includes('Winetricks'))"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the exact command run, its exit code, and the verbatim first lines of output including any HTTP status.
    - The SUMMARY states plainly which path the plan took: machine-filled (and for which locales) or hand-filled.
    - The SUMMARY answers whether `Winetricks` is a glossary term, with the check that produced the answer.
    - If the script wrote nothing, `git status --porcelain public/locales/` is empty at the end of this task.
  </acceptance_criteria>
  <done>The machine-fill path is re-measured exactly once with the verdict recorded verbatim, and the glossary question is answered before any value is written.</done>
</task>

<task type="auto">
  <name>Task 2: Fill 13 winetricksBrowse keys into 48 non-English gamelib.json catalogs, validator-first</name>
  <files>public/locales/*/gamelib.json, meta/i18nCatalogPresenceBaseline.json</files>
  <read_first>
    - `public/locales/en/gamelib.json` — the 13 source values, and the file's structural conventions (indentation, key ordering, trailing newline).
    - `public/locales/de/gamelib.json` and `public/locales/ja/gamelib.json` — two structurally different targets. Check where a sibling top-level key sits and whether indentation matches `en`.
    - `meta/machineFillGamelib.ts` — `validateTranslation`'s signature and exactly what it returns.
    - `meta/__tests__/gamelibCatalogParity.test.ts` — what the parity gate asserts, so the fill is aimed at the real contract.
    - `meta/i18nCatalogPresenceBaseline.json` — whether it pins a count this addition moves; plan 44-02's Task 2 should have recorded the answer. If it did not, determine it here.
    - `.planning/quick/260915-t13-.../260915-t13-SUMMARY.md` — the pre-flight validator sequence D-10 reuses.
  </read_first>
  <action>
Add the `winetricksBrowse` block with all 13 keys to each of the **48 non-English**
`public/locales/<locale>/gamelib.json` files. `en` already has it from plan 44-02 — do not rewrite it.

**Validator BEFORE writing, not after (D-10).** Build the complete set of 48 × 13 = 624 candidate
values first, in memory or in a scratch file under the session scratchpad, then run the pre-flight
checks over the whole set, then write. D-10's wording is explicit: the validator is "run before any
file is touched". Checks:
1. **Locale-set check** — the target locale list is exactly the 49 dirs minus `en`, derived by
   globbing `public/locales/*/gamelib.json`, never a hardcoded list. Assert 48.
2. **Bidirectional placeholder parity** — via `validateTranslation` from `meta/machineFillGamelib.ts`.
   `resultsHeading` must contain `{{total}}` and `zeroResultHeading` must contain `{{query}}` in
   every locale, and neither may introduce a placeholder the English source lacks. A translation
   that "helpfully" renames `{{total}}` to a localised word breaks at runtime silently.
3. **Glossary survival** — per Task 1's answer, if `Winetricks` is a glossary term it must appear
   verbatim in every locale's `needsGuiTag` and `emptyBody`.
4. **No empty values** — no key may be `""`, whitespace-only, or absent.
5. **No `{{count}}`** anywhere in the added block, in any locale. It is a reserved i18next
   interpolation name that switches a key into plural resolution and would demand per-locale
   `_one`/`_few`/`_many`/`_other` variants (Arabic alone has six categories).

Record the validator's output — the count of problems found and fixed — in the SUMMARY. A run that
reports zero problems on the first pass across 624 strings deserves a second look at whether the
validator was actually invoked.

**Editing mechanics.** `public/locales/` is in `.prettierignore`. Do **not** round-trip any file
through a JSON serialiser (`json.load` → `json.dump`, `jq`, `JSON.stringify`) — a serialiser cycle
reformats all 48 files and buries the real diff under thousands of whitespace-only lines. Edit the
raw text in place (string splice or a targeted regex insertion at the correct top-level position),
matching each file's own existing indentation, and re-parse afterwards purely as verification.

Add `winetricksBrowse` as a **top-level sibling** in every file — never nested inside the existing
`winetricks` key, which holds unrelated build-availability copy. Match each file's existing key
ordering convention; do not re-sort any file.

**`meta/i18nCatalogPresenceBaseline.json`:** only touch it if plan 44-02's measurement identified a
pinned number that this addition legitimately moves, and only to reflect the new true count. Do not
add an allowlist entry, do not widen a baseline to admit a failure, and do not silence a finding you
have not understood. A ratchet that grows to admit whatever failed is not a ratchet — the same rule
CLAUDE.md states for the todo-frontmatter gate.

**No `winetricksBrowse.category.*` keys (D-09).** Category headers render the parser's own strings.
  </action>
  <verify>
    <automated>pnpm lint-translations:gamelib && npx jest --selectProjects Meta --passWithNoTests --silent gamelibCatalogParity</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint-translations:gamelib` exits 0 with zero findings.
    - `npx jest --selectProjects Meta --passWithNoTests --silent gamelibCatalogParity` exits 0.
    - `node -e "const fs=require('fs'),p='public/locales';let n=0,bad=[];for(const d of fs.readdirSync(p)){const f=p+'/'+d+'/gamelib.json';if(!fs.existsSync(f))continue;const j=JSON.parse(fs.readFileSync(f,'utf8'));const b=j.winetricksBrowse;if(!b||Object.keys(b).length!==13){bad.push(d);continue}if(Object.keys(b).some(k=>k.startsWith('category')))bad.push(d+':category');if(Object.values(b).some(v=>!String(v).trim()))bad.push(d+':empty');if(!/\{\{total\}\}/.test(b.resultsHeading))bad.push(d+':total');if(!/\{\{query\}\}/.test(b.zeroResultHeading))bad.push(d+':query');if(/\{\{count\}\}/.test(JSON.stringify(b)))bad.push(d+':count');n++}if(bad.length)throw new Error('bad locales: '+bad.join(','));if(n!==49)throw new Error('expected 49 catalogs, saw '+n);console.log('OK 49 catalogs x 13 keys')"` exits 0.
    - `git diff --name-only public/locales/ | grep -c gamelib.json` returns 48 (en was committed by plan 44-02).
    - Diff-size sanity: `git diff --numstat public/locales/ | awk '$3 ~ /gamelib.json/ {a+=$1} END {print a}'` returns a total added-line count within 10% of 48 × 15 = 720. A total in the thousands means a serialiser round-trip happened; revert and redo in place.
    - The SUMMARY records the pre-flight validator's problem count and how each was resolved.
  </acceptance_criteria>
  <done>All 49 `gamelib.json` catalogs carry 13 validated `winetricksBrowse` keys with placeholders intact, no empties, no `category.*`, no `{{count}}` — and both gamelib gates are green with a diff confined to the added blocks.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the 6 orphaned winetricks.* keys from 47 translation.json catalogs, structurally</name>
  <files>public/locales/*/translation.json</files>
  <read_first>
    - `public/locales/en/translation.json` lines 1405-1425 — the full 9-key `winetricks` block. Confirm all 9 keys are present before removing 6.
    - `public/locales/da/translation.json`, `public/locales/id/translation.json`, `public/locales/nl/translation.json` — the three locales that stranded trailing commas on the PRIOR removal (a different key). `44-PATTERNS.md` measured that the trap does not recur for `winetricks`, but spot-check the block's internal key ordering in each.
    - `.planning/phases/44-.../44-05-SUMMARY.md` — plan 44-05's D-20(a) grep census, which must show zero surviving consumers for all 6 keys before this task removes anything.
    - `.planning/phases/44-.../44-PATTERNS.md` §"`translation.json` key removal" — the verified population facts (0 of 47 missing the key, 0 of 47 hold it last).
  </read_first>
  <action>
**Precondition, checked first.** Re-run plan 44-05's census and confirm zero surviving consumers, via `grep -rEn "winetricks\.(search|no-components|installed|nothingYet|installing)" src/` and `grep -rn "winetricks\.loading'" src/`. Both must return zero hits. If either returns a hit, **stop and report** — removing a key with a
live consumer ships a raw key string into the UI. D-20(a) exists precisely to prevent that, and
`winetricks.installing` had a live consumer until plan 44-05 deleted it.

**Remove exactly 6 keys** from the `winetricks` object in each of the 47 `public/locales/<locale>/translation.json`
files: `search`, `no-components`, `installed`, `nothingYet`, `installing`, `loading`. The sixth is
this plan's recorded addition to D-20's list — see `<planner_decisions>`.

**Keep exactly 3**: `install`, `openGUI`, `loading-available`. All three are reused by the new panel
and must survive. `loading-available` is a distinct key from `loading`; a regex that matches
`"loading"` loosely will eat both. Match the full quoted key name with its closing quote and colon.

**Derive the population, do not assume it.** Glob `public/locales/*/translation.json` and assert the
result is 47 — `br` and `sl` have no `translation.json` at all. Never hardcode a locale list.

**Structural removal, not a blind line delete.** For each key being removed, before deleting its
line, check whether the next non-empty line begins with `}` — i.e. whether this key is last inside
the `winetricks` object. If it is, the preceding sibling's trailing comma must also be removed or
the file stops parsing. `44-PATTERNS.md` measured that the *object itself* is never last in any of
the 47 top-level catalogs, so the outer-object variant of this trap does not arm here — but
last-key-inside-`winetricks` is a live possibility for `search` (alphabetically last in the English
block) and must be handled generically rather than by a hardcoded locale list.

**Never round-trip through a JSON serialiser.** `public/locales/` is `.prettierignore`'d. Edit raw
text in place and re-parse every one of the 47 files afterwards purely as verification.

Also check whether any other gate artifact enumerates these keys (e.g. a presence baseline or a
churn guard) and record the answer. If one does, note it — do not edit it silently.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='public/locales';const gone=['search','no-components','installed','nothingYet','installing','loading'];const keep=['install','openGUI','loading-available'];let n=0,bad=[];for(const d of fs.readdirSync(p)){const f=p+'/'+d+'/translation.json';if(!fs.existsSync(f))continue;n++;const j=JSON.parse(fs.readFileSync(f,'utf8'));const w=j.winetricks||{};for(const k of gone)if(k in w)bad.push(d+':still-has:'+k);for(const k of keep)if(!(k in w))bad.push(d+':lost:'+k);if(Object.keys(w).length!==3)bad.push(d+':size='+Object.keys(w).length)}if(bad.length)throw new Error(bad.join(','));if(n!==47)throw new Error('expected 47 translation.json, saw '+n);console.log('OK 47 catalogs, winetricks block is exactly the 3 survivors')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command above exits 0 and prints the OK line — all 47 files parse, all 6 keys are gone, all 3 survivors remain, and each block is exactly 3 keys.
    - `grep -rEn "winetricks\.(search|no-components|installed|nothingYet|installing)" src/` returns 0 hits and `grep -rn "winetricks\.loading'" src/` returns 0 hits, re-run AFTER the removal.
    - `grep -rn "winetricks\.install'\|winetricks\.openGUI\|winetricks\.loading-available" src/` returns at least one hit each.
    - `git diff --name-only public/locales/ | grep -c translation.json` returns 47.
    - Diff-size sanity: `git diff --numstat public/locales/ | awk '$3 ~ /translation.json/ {d+=$2} END {print d}'` returns a deleted-line total within 10% of 47 × 6 = 282. A total in the thousands means a serialiser round-trip; revert and redo in place.
    - `pnpm lint-translations` (full, both namespaces) exits 0, and `npx jest --selectProjects Meta --passWithNoTests --silent` reports no newly-failing suite versus baseline.
    - The SUMMARY names any other gate artifact that enumerates these keys, or states plainly that none does and how that was checked.
  </acceptance_criteria>
  <done>All 47 `translation.json` catalogs carry exactly the 3 surviving `winetricks` keys, the 6 orphans are gone with no parse breakage and no serialiser reformat, and no consumer in `src/` references a removed key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → renderer (i18n catalogs) | Static JSON compiled into the app, resolved client-side by `react-i18next` at render time. No runtime fetch. |
| external translation service → repo (machine-fill path only) | If `pnpm machine-fill-gamelib` succeeds, third-party-generated strings enter 48 catalogs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-17 | Tampering | machine-generated locale values | mitigate | Task 2 runs `validateTranslation` over every value regardless of origin, before any file is written — machine output is not trusted more than hand output. Placeholder parity is checked bidirectionally so a generated string cannot introduce an interpolation the English source lacks, and glossary survival is checked so product names are not translated away. |
| T-44-18 | Tampering | 48-file bulk edit | mitigate | Every acceptance criterion pins the diff size against a derived expectation (48 × 15 added, 47 × 6 deleted). A serialiser round-trip or an unrelated edit smuggled into a 48-file change would blow past the 10% tolerance and fail. All 95 touched files are re-parsed as JSON after editing. |
| T-44-19 | Information Disclosure | `{{query}}` / `{{total}}` interpolation in 49 locales | accept | Both render the user's own input or a local count back into their own dialog. React escapes interpolated values and no `dangerouslySetInnerHTML` exists in this phase. |
| T-44-20 | Denial of Service | removing a key with a live consumer | mitigate | Task 3 re-runs the D-20(a) consumer census as a blocking precondition and stops rather than removing if any hit survives. A removed-but-consumed key renders a raw key string into the UI — the exact failure D-20(a)'s grep obligation exists to prevent. |
| T-44-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. `44-RESEARCH.md` §"Package Legitimacy Audit" records zero new dependencies for this phase. `pnpm machine-fill-gamelib` runs an existing committed script; it installs nothing. |
</threat_model>

<verification>
```bash
pnpm lint-translations:gamelib
pnpm lint-translations
npx jest --selectProjects Meta --passWithNoTests --silent
node -e "..."   # the two structural assertions in Tasks 2 and 3
git diff --numstat public/locales/ | head -5   # diff-size sanity
```
Do NOT gate on `pnpm test:ci` — it exits 1 at HEAD with zero failing tests. See
`44-VALIDATION.md` §Sampling Rate.
</verification>

<success_criteria>
- `pnpm machine-fill-gamelib` was re-measured exactly once and the verdict recorded verbatim (D-10).
- All 49 `gamelib.json` catalogs carry 13 validated `winetricksBrowse` keys; `lint-translations:gamelib` and `gamelibCatalogParity` are green (D-08).
- No `winetricksBrowse.category.*` key exists in any locale (D-09); no `{{count}}` exists in the added block.
- All 47 `translation.json` catalogs carry exactly the 3 surviving `winetricks` keys; the 6 orphans including `loading` are gone (D-20, extended).
- No consumer in `src/` references a removed key; all three survivors still have consumers.
- Every touched file re-parses as JSON; no diff exceeds its derived size tolerance.
- No baseline or allowlist was widened to admit a failure.
</success_criteria>

<output>
Create `.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-06-SUMMARY.md` when done.
Record verbatim: the `machine-fill-gamelib` command, exit code and output; the pre-flight validator's
problem count and resolutions; the post-removal consumer census; the two diff-size totals against
their derived expectations; and whether `meta/i18nCatalogPresenceBaseline.json` needed an edit and why.
</output>
