---
phase: 44-in-app-winetricks-browse-ui-replacing-the-search-only-panel
plan: 03
type: execute
wave: 2
depends_on:
  - 44-01
  - 44-02
files_modified:
  - src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx
  - src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.scss
  - src/frontend/components/UI/Winetricks/WinetricksBrowse/__tests__/rowStates.test.tsx
  - src/frontend/components/UI/Winetricks/WinetricksBrowse/__tests__/winetricksInstallMouseRace.test.tsx
autonomous: true
requirements:
  - REQ-44-11
  - REQ-44-17
  - REQ-44-19
  - REQ-44-23
  - REQ-44-26
must_haves:
  truths:
    - "D-12: an installed row shows a badge and no reinstall affordance"
    - "D-19: every clickable row action captures intent on mousedown with a suppressNextClick guard, and keyboard activation via click still works"
    - "C-4 / ROADMAP fence 3: a Needs-GUI verb renders NO Install button in any state and instead offers Open GUI"
    - "C-2 / ROADMAP fence 2: the value bound to every action handler is that row's own resolved verb string — never a value read out of search state or a tracked highlight"
    - "C-3: every row state pairs its colour with an icon and a text label; colour is never the only signal"
    - "D-24: every colour declaration uses a token-survival fallback chain and the theme-adaptive --success/--danger aliases, never raw --status-*"
  artifacts:
    - path: "src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx"
      provides: "the per-row component with the six-state action slot"
      min_lines: 90
    - path: "src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.scss"
      provides: "row and action-slot styling with fixed height/width and (0,3,0)+ specificity"
    - path: "src/frontend/components/UI/Winetricks/WinetricksBrowse/__tests__/winetricksInstallMouseRace.test.tsx"
      provides: "the D-19 port of the 35-25 mouse-race coverage, retargeted at Row"
    - path: "src/frontend/components/UI/Winetricks/WinetricksBrowse/__tests__/rowStates.test.tsx"
      provides: "one assertion per row state, including C-4's no-Install-button invariant"
  key_links:
    - from: "src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx"
      to: "src/common/winetricks/deriveRowState.ts"
      via: "import of deriveRowState"
      pattern: "deriveRowState"
    - from: "src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx"
      to: "public/locales/en/gamelib.json"
      via: "useTranslation('gamelib') reading winetricksBrowse.*"
      pattern: "winetricksBrowse\\."
---

<objective>
Build the per-row component: a fixed-height row rendering title, verb caption, cached badge, and a
metrically-pinned action slot whose contents are selected by `deriveRowState()` from plan 44-01.
Port the `35-25` mouse-race coverage onto it per D-19.

Purpose: the row is where every scope fence lands. C-2 is satisfied structurally here (the handler
closes over `props.component.verb`, so there is no "current selection" to be out of sync with),
C-4 is satisfied by not rendering an Install button at all for the 8 unattended-incapable verbs,
and the UI-SPEC's action-slot metric parity is what stops rows shifting under the pointer when the
post-install refetch resolves.

Output: `Row/index.tsx`, `Row/index.scss`, and two test files under `WinetricksBrowse/__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-CONTEXT.md
@.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-UI-SPEC.md
@.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-PATTERNS.md
@.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

Shipped by plan 44-01, `src/common/winetricks/deriveRowState.ts`:

```ts
export type WinetricksRowState =
  | 'available' | 'installing' | 'installingElsewhere'
  | 'installed' | 'needsGui' | 'errored'
export type VerbErrorMap = Readonly<Record<string, true>>
export function deriveRowState(input: {
  verb: string
  installed: readonly string[]
  installing: boolean
  installingComponent: string
  erroredVerbs: VerbErrorMap
}): WinetricksRowState
```

Shipped by plan 44-01, `src/common/winetricks/verbs.ts`:
`CURATED_WINETRICKS_VERBS`, `NEEDS_GUI_WINETRICKS_VERBS`, `resolveCuratedComponents()`.

`src/common/types.ts:826-831`:

```ts
export interface WinetricksComponent {
  verb: string; title: string; category: string; cached: boolean
}
```

Shipped by plan 44-02, `public/locales/en/gamelib.json` → `winetricksBrowse`:
`curatedGroup`, `searchPlaceholder`, `resultsHeading`, `cachedTag`, `installedTag`,
`needsGuiTag`, `installingRow`, `installFailedTag`, `retry`, `emptyHeading`, `emptyBody`,
`zeroResultHeading`, `clearSearch`.

Reused from `translation.json` (default namespace) and NOT removed by D-20:
`winetricks.install`, `winetricks.openGUI`, `winetricks.loading-available`.

The mousedown-capture pattern being ported, `WinetricksSearch/index.tsx:79-104` (verified):

```tsx
const suppressNextClick = useRef(false)
onMouseDown={(e) => { e.preventDefault(); suppressNextClick.current = true; install(c.verb) }}
onClick={() => { if (suppressNextClick.current) { suppressNextClick.current = false; return }
                 install(c.verb) }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Row/index.tsx — the six-state action slot</name>
  <files>src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx</files>
  <read_first>
    - `src/frontend/components/UI/Winetricks/WinetricksSearch/index.tsx` — the whole file, especially the 27-line MOUSE-CLICK RACE FIX comment block at lines 52-78 and the handlers at 79-104. That comment records a live-measured root cause; it must be carried forward (adapted, not deleted) into the new Row, because plan 44-05 deletes this file.
    - `src/common/winetricks/deriveRowState.ts` and `src/common/winetricks/verbs.ts` — shipped by plan 44-01.
    - `src/frontend/components/UI/NavShell/components/FilterFacetGroup/index.tsx` — the shipped row primitive's structure (`classNames` usage, `count !== undefined` rather than truthiness, and the header comment explaining why it holds no `t()` call). Read it for shape; do NOT import `FilterFacetRow` — it is a `role="checkbox"` toggle and D-05 explicitly rejects reusing it.
    - `.planning/phases/44-.../44-UI-SPEC.md` §"States > Row-level" (the 7 rows) and §"Interaction Contract" points 3 and 5, and the "CTA note" paragraph in §"Copywriting Contract".
    - `public/locales/en/gamelib.json` — the `winetricksBrowse` block shipped by plan 44-02.
  </read_first>
  <action>
Create `src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx`, a default-exported
function component.

**Props** (declare a `type Props` in the file):
`component: WinetricksComponent`, `installed: readonly string[]`, `installing: boolean`,
`installingComponent: string`, `erroredVerbs: VerbErrorMap`, `onInstall: (verb: string) => void`,
`onOpenGui: () => void`. No `selectedVerb`, no `isSelected`, no index — C-2 is satisfied by the
absence of any selection concept, not by a runtime check.

**Structure**, left to right, inside a single root element with `className="WinetricksBrowse__row"`:
1. A title element `className="WinetricksBrowse__rowTitle"` rendering `component.title`, carrying a
   stable `id` derived from the verb (e.g. `` id={`wtb-title-${component.verb}`} ``). The id is
   load-bearing — it is the `aria-labelledby` target below.
2. A verb caption element `className="WinetricksBrowse__rowVerb"` rendering `component.verb`.
3. When `component.cached === true`, a badge `className="WinetricksBrowse__tag WinetricksBrowse__tag--cached"`
   pairing a FontAwesome icon with `tGamelib('winetricksBrowse.cachedTag', 'Cached (installs offline)')`.
   The cached badge renders **alongside whichever action state applies**, not as a state of its own.
4. A fixed-width action slot `className="WinetricksBrowse__actionSlot"` whose contents come from
   `deriveRowState({ verb: component.verb, installed, installing, installingComponent, erroredVerbs })`.

**The six action-slot branches** — use a `switch` over the derived state so a future seventh member
is a compile error, not a silent fall-through:
- `needsGui` — **NO Install button, in any state, ever (C-4).** Render the
  `winetricksBrowse.needsGuiTag` badge (icon + text, `--text-secondary` family — neutral, not accent,
  not danger: this is routing, not failure) plus a secondary button labelled
  `t('winetricks.openGUI', 'Open Winetricks GUI')` wired to `onOpenGui`.
- `installing` — a spinner icon plus `winetricksBrowse.installingRow`. No button.
- `installingElsewhere` — the Install button rendered but `disabled`, carrying a `title` attribute
  explaining why (to the effect of "finishing the current install first"). A guarded no-op with an
  explanation, never a silent dead button.
- `installed` — a static `winetricksBrowse.installedTag` badge, icon + text, colour from the
  theme-adaptive `--success` alias. **No reinstall affordance (D-12)** — `winetricksInstall` is
  technically re-invocable; the decision is product, not capability.
- `errored` — the `winetricksBrowse.installFailedTag` badge (icon + text, `--danger` alias) plus a
  `winetricksBrowse.retry` button wired to `onInstall(component.verb)`.
- `available` — the Install button, `t('winetricks.install', 'Install')`, enabled.

**Every clickable action (Install, Retry, Open GUI) uses the D-19 pattern.** One
`const suppressNextClick = useRef(false)` for the row; a shared `activate(fn)` helper returning the
`{ onMouseDown, onClick }` pair so all three buttons share one implementation rather than three
copies. `onMouseDown` calls `e.preventDefault()`, sets the ref, and fires the action; `onClick`
returns early once if the ref is set, otherwise fires the action so keyboard `Enter`/`Space`
activation still works. Carry a condensed version of `WinetricksSearch/index.tsx:52-78`'s root-cause
comment into this file, citing Phase 35 Plan 25 (`366e719bb`) and UI-SPEC Interaction Contract §5 —
the technique is kept because it is free and correct for any clickable list row, independent of
whether the original remount path is still reachable after C-1 ships.

**Accessible names without a new string.** Every action button gets
`` aria-labelledby={`wtb-<action>-label-${component.verb} wtb-title-${component.verb}`} `` where the
first id belongs to a `<span>` inside the button holding the visible verb label. This resolves the
accessible name to "Install Visual C++ 2019 libraries" from two already-localised fragments,
satisfying the UI-SPEC CTA note's requirement that the accessible name carry the object while the
visible label stays the bare verb — and it adds **zero** new locale keys, which is why plan 44-02
declined `44-PATTERNS.md`'s proposed 12th `installAriaLabel` key. Record that reasoning in a comment.

**Never name an element `.button`.** `Dropdown/index.scss:4-6`'s `.dropdownContainer .button` rule
sits at (0,2,0) and matches ANY descendant carrying that class. Use
`WinetricksBrowse__installButton`, `WinetricksBrowse__retryButton`, `WinetricksBrowse__guiButton`.

Import `'./index.scss'` at the top, matching the convention in every sibling component.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "WinetricksBrowse" || echo "no type errors in WinetricksBrowse"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*[/*]' src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx | grep -c "aria-labelledby"` returns at least 3 (one per action button), comment lines stripped.
    - `grep -c "className=\"button\"\|className={'button'}\|'button'" src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx` returns 0 — no element is named `.button` verbatim.
    - `grep -v '^\s*[/*]' src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx | grep -c "onMouseDown"` returns at least 1 and the helper it belongs to is used by all three actions (verified by reading, and pinned by the tests in Task 3).
    - `grep -c "suppressNextClick" src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx` returns at least 3.
    - The file contains no `status-success` / `status-danger` string and no inline `style=` colour.
    - `grep -c "366e719bb\|35-25\|Plan 25" src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.tsx` returns at least 1 — the root-cause provenance survived the port.
    - `npx tsc --noEmit` reports no error originating in this file.
  </acceptance_criteria>
  <done>`Row/index.tsx` renders all six action-slot states via a `switch` on `deriveRowState`, never renders an Install button for a Needs-GUI verb, binds every handler to `component.verb`, uses one shared mousedown-capture helper for all three actions, and resolves accessible names via `aria-labelledby` with no new locale key.</done>
</task>

<task type="auto">
  <name>Task 2: Build Row/index.scss — metric parity, token-survival chains, and (0,3,0)+ specificity</name>
  <files>src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.scss</files>
  <read_first>
    - `src/frontend/components/UI/NavShell/components/FilterFacetGroup/index.scss` — the whole file. Specifically: the CR-01/CR-03 fallback chains around lines 43-46 and their consumers at 88 and 231, and the `260815-mk1` out-specification fix pattern at lines 190-211. Copy the chain technique; do NOT edit this file and do NOT de-scope it from `.NavShell__tier2Portal` (D-05).
    - `src/frontend/components/UI/Dropdown/index.scss` — lines 4-6 (`.dropdownContainer .button`, (0,2,0)) and 104-109 (`.dropdownContainer .dropdown button`, (0,2,1)). These are the rules the new row styling must out-specify.
    - `src/frontend/components/UI/Winetricks/index.scss` — the existing `.progressDialog.winetricksDialog` scope this styling nests under.
    - `src/frontend/components/UI/NavShell/components/FilterFacetGroup/__tests__/facetGroupBadgeStyles.test.ts` — the in-repo precedent for asserting against COMPILED CSS rather than source nesting. Task 3 adds an equivalent for this stylesheet.
    - `.planning/phases/44-.../44-UI-SPEC.md` §"Layout" (the action-slot height/width paragraph) and §"Color".
    - `.planning/phases/44-.../44-RESEARCH.md` Pitfalls 3 and 5.
  </read_first>
  <action>
Create `src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.scss`, scoped under
`.winetricksDialog` so nothing leaks app-wide (D-05; this repo has paid twice for cross-context CSS
leak — `Dropdown` styling its panel's contents, and the unscoped `.MuiTabs-root` incident).

**Fixed row metrics (UI-SPEC Layout, the checker's iteration-2 addition).** Give
`.WinetricksBrowse__row` a single fixed `height: 44px` and `min-width: 0`. Give
`.WinetricksBrowse__actionSlot` BOTH a fixed `min-width` (never narrower than the Install button's
own width) AND a fixed `height` with the same `line-height` and vertical padding across every one
of the six treatments. This is load-bearing, not polish: the Installing→Installed swap fires at the
exact instant the post-install stale-while-revalidate refresh resolves, and if that swap is not
metrically identical the rows below shift **vertically** under a pointer that was about to act on a
different row — the vertical twin of the horizontal hazard Interaction Contract §5 guards.
`min-width: 0` on the row and on the flexible title/verb container as well; this codebase has hit
flex-child overflow without it before.

**Token-survival fallback chains (CR-01/CR-03, D-24).** Declare the chains file-locally — the
project convention per `FilterFacetGroup/index.scss`'s own header is that every consumer carries its
own copy so a census gate can assert they match. Never consume a custom property bare: an undefined
custom property with no fallback drops the ENTIRE declaration at computed-value time, silently, in
whichever theme block lacks it. `--navbar-active` is declared in only 4 of 11 theme blocks.
Required chains, copied not invented:
- active/hover colour: `var(--navbar-active, var(--accent-overlay, var(--accent)))`
- inactive text: `var(--navbar-inactive, var(--navbar-accent))`
- hover text: `var(--text-hover, var(--accent))`

**Status colour aliases (Pitfall 5).** Installed badge text/icon uses `var(--success)`; Errored badge
text/icon uses `var(--danger)`. **Never `--status-success` / `--status-danger`** — those are raw
constants with no light-theme override and measure 2.27:1 on `nord-light`, a WCAG failure this repo
has already shipped and fixed once. Reserve `--status-*` for fills only, and this stylesheet needs
none.

**Non-colour signal (C-3).** Every badge rule must be paired with an icon element and text in the
markup from Task 1; the stylesheet must not express any state through colour alone (no rule whose
only difference from its sibling is `color`/`background-color` without an accompanying icon slot).

**Out-specification (Pitfall 3, recurred twice already — `FilterFacetRow` `260815-mk1`, `NavItem`
`260815-nmq`).** Row and button rules must reach at least (0,3,0) so they beat
`.dropdownContainer .dropdown button` at (0,2,1) regardless of import order — this stylesheet and
`Dropdown/index.scss` are imported by different components and neither can rely on loading second.
Use three-class selectors, e.g.
`.WinetricksBrowse .WinetricksBrowse__group .WinetricksBrowse__row { ... }` and
`.WinetricksBrowse .WinetricksBrowse__row .WinetricksBrowse__installButton { ... }`. Explicitly
reset the leaked `margin-inline-start: 0.5rem` from `Dropdown`'s generic rule rather than
inventing a new value. Do NOT trust nesting to produce the intended specificity — Task 3 verifies
the emitted selectors against compiled CSS.

Use the existing `styles/_spacing.scss` tokens (`--space-3xs`/`2xs`/`xs`/`sm`) rather than raw px
for internal gaps; do not invent a parallel scale.
  </action>
  <verify>
    <automated>npx sass --style=expanded --no-source-map src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.scss /tmp/wtb-row.css && grep -c "status-danger\|status-success" /tmp/wtb-row.css; echo "expected 0 above"</automated>
  </verify>
  <acceptance_criteria>
    - `npx sass --style=expanded --no-source-map src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.scss /tmp/wtb-row.css` exits 0.
    - `grep -c "var(--status-" /tmp/wtb-row.css` returns 0.
    - `grep -c "var(--success" /tmp/wtb-row.css` and `grep -c "var(--danger" /tmp/wtb-row.css` each return at least 1.
    - Every `var(--` occurrence in the compiled output that names a theme-variable custom property has a comma fallback: `grep -oE 'var\(--[a-z0-9-]+\)' /tmp/wtb-row.css | sort -u` returns nothing except properties declared file-locally in this same stylesheet (list them in the SUMMARY with that justification).
    - At least one compiled selector for the row and one for each action button carries 3 or more class tokens: `grep -E '^\.[A-Za-z].*\..*\..*\{' /tmp/wtb-row.css | head` shows them; paste the matched selectors into the SUMMARY.
    - `grep -c "height: 44px" /tmp/wtb-row.css` returns at least 1 and the action-slot rule carries both a `min-width` and a `height`.
    - `git status --porcelain src/frontend/components/UI/NavShell/` is empty — `FilterFacetGroup` was copied from, not edited (D-05).
  </acceptance_criteria>
  <done>`Row/index.scss` compiles, uses only theme-adaptive aliases with fallback chains, pins both dimensions of the action slot, and emits selectors at (0,3,0)+ verified against compiled CSS rather than source nesting — with `FilterFacetGroup/` untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Port the mouse-race test to Row and add per-state coverage</name>
  <files>src/frontend/components/UI/Winetricks/WinetricksBrowse/__tests__/winetricksInstallMouseRace.test.tsx, src/frontend/components/UI/Winetricks/WinetricksBrowse/__tests__/rowStates.test.tsx</files>
  <read_first>
    - `src/frontend/components/UI/Winetricks/WinetricksSearch/__tests__/winetricksInstallMouseRace.test.tsx` — the WHOLE file (~344 lines). This is the mandatory harness template. Lines 33-37 (`react-i18next` mock), 50-118 (the `jest.mock('react', …)` state-slot/cursor hook reimplementation with `__beginRender`/`__resetMount`), 127-146 (`mount()`/`reinvoke()`), 153-163 (`walk()` element-graph traversal), 165-178 (`findInstallButton`).
    - `src/frontend/jest.config.js` — read its header comment. `testEnvironment: 'node'`. There is NO DOM. `jest-environment-jsdom` and `react-test-renderer` are NOT installed.
    - `src/frontend/components/UI/NavShell/components/FilterFacetGroup/__tests__/facetGroupBadgeStyles.test.ts` — the compiled-CSS assertion precedent for the third assertion group below.
    - `.planning/phases/44-.../44-VALIDATION.md` §"Test Infrastructure" and its `pnpm test:ci` warning.
  </read_first>
  <action>
**Do not write `render()`, `fireEvent`, or `screen.getByRole`. They cannot run here.** Copy the
hand-rolled harness from the source file named above: the `react` module mock over manual state-slot
arrays with a cursor, the `mount()`/`reinvoke()` helpers that invoke the component as a plain
function, the `walk()` element-graph traversal that finds elements by `props.className`/`type`, and
the `react-i18next` mock whose `t` returns the supplied default value.

**File 1 — `winetricksInstallMouseRace.test.tsx` (the D-19 port).** Retarget `mount()`/`reinvoke()`
at the new `Row` component instead of `WinetricksSearchBar`. Carry the source file's own explanatory
header across, updated to say it is a port and naming both its origin
(`WinetricksSearch/__tests__/`) and its reason (D-19: the file encodes the `35-25` finding and the
technique survives into the new Row). Assertions to preserve and retarget:
- A `mousedown` on the Install button invokes `onInstall` exactly once with that row's verb.
- The subsequent `click` after that `mousedown` does NOT invoke `onInstall` a second time.
- A `click` with no preceding `mousedown` (the keyboard-activation path) DOES invoke `onInstall`
  once — this is the assertion that proves the `suppressNextClick` guard did not break keyboard use.
- `mousedown`'s handler calls `preventDefault` on its event.
Extend the port with the same three assertions against the **Retry** button (state `errored`) and
the **Open GUI** button (state `needsGui`), since D-19 requires the technique on every clickable row
action, not just Install.

**File 2 — `rowStates.test.tsx`.** One `describe` per derived state, each mounting `Row` with props
that force that state and walking the returned element graph:
- `available`: an enabled Install button exists; no Installed badge; no spinner.
- `available` + `component.cached === true`: the cached badge exists AND the Install button still
  exists (cached is a modifier, not a state).
- `installing` (this row): the installing text exists; **no** Install button exists.
- `installingElsewhere`: an Install button exists AND carries `disabled: true` AND a non-empty
  `title` prop. Assert the `title` is non-empty — a disabled button with no explanation is the
  silent-dead-button failure the UI-SPEC names.
- `installed`: the Installed badge exists; **no** Install button and **no** Retry button exists
  (D-12 — badge only).
- `errored`: the failure badge AND a Retry button exist; the Retry button is bound to this row's verb.
- `needsGui`: **no Install button exists**, an Open GUI button exists, and the Needs-GUI badge
  exists. Then add the C-4 invariant test: parametrise over all 8 verbs in
  `NEEDS_GUI_WINETRICKS_VERBS` × the cross-product of `{installed: yes/no} × {installing: yes/no} ×
  {errored: yes/no}` and assert an Install button is absent in **every** combination. C-4 says
  "regardless of loading/installed/cached state"; a single-case test would not prove it.
- Accessible names: for each action button, assert `props['aria-labelledby']` is a non-empty string
  containing the row's title element id, and that a descendant span carries that id's counterpart.

Also add a third assertion group (or a separate `.test.ts` if the harness mocks interfere) asserting
the **compiled** CSS of `Row/index.scss`, following the `facetGroupBadgeStyles.test.ts` precedent:
compile with the `sass` API, assert no `var(--status-` occurrence, and assert at least one row
selector and one button selector carry ≥3 class tokens. Asserting compiled output rather than source
nesting is what makes the Pitfall-3 guard real.

Run with `npx jest --selectProjects Frontend --passWithNoTests --silent`. `--selectProjects` is
case-sensitive and fails **open**, so `--passWithNoTests` is mandatory — without it a typo'd project
name exits 0 having run nothing. **Do NOT gate on `pnpm test:ci`**: it exits 1 at HEAD with zero
failing tests, from a leaked 60s `rustInvoke('store_embed_open')` timer that kills Node after the
green summary prints.
  </action>
  <verify>
    <automated>npx jest --selectProjects Frontend --passWithNoTests --silent src/frontend/components/UI/Winetricks/WinetricksBrowse</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --selectProjects Frontend --passWithNoTests --silent src/frontend/components/UI/Winetricks/WinetricksBrowse` exits 0 and reports at least 2 suites.
    - `grep -c "render(\|fireEvent\|screen\.get" src/frontend/components/UI/Winetricks/WinetricksBrowse/__tests__/*.tsx` returns 0 — no RTL-shaped API is used.
    - `grep -c "jest.mock('react'" src/frontend/components/UI/Winetricks/WinetricksBrowse/__tests__/winetricksInstallMouseRace.test.tsx` returns 1.
    - The C-4 parametrised test executes at least 8 × 8 = 64 cases: `npx jest --selectProjects Frontend --passWithNoTests src/frontend/components/UI/Winetricks/WinetricksBrowse -t "needsGui"` reports a case count ≥ 64.
    - Negative control, run and recorded verbatim in the SUMMARY: temporarily delete the `needsGui` branch from `Row/index.tsx`'s switch so a Needs-GUI verb falls through to `available`, re-run, confirm the C-4 test turns RED and names the verb, then restore. A passing test that has never failed proves nothing.
    - `npx jest --selectProjects Frontend --passWithNoTests --silent` (full project) reports no newly-failing suite versus the pre-task baseline; record both suite/test totals in the SUMMARY.
  </acceptance_criteria>
  <done>Both test files run under the hand-rolled no-DOM harness, the mouse-race coverage is ported and extended to all three row actions, C-4 is proven across all 64 state combinations, the compiled-CSS guard is in place, and the C-4 assertion is proven non-vacuous by a recorded revert-to-red control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar (`winetricksInstall`) | The `verb` this row binds to its handlers crosses into a `winetricks -q <verb>` shell invocation via the already-shipped send-kind IPC. |
| renderer → sidecar (`callTool({tool:'winetricks'})`) | The Open GUI action launches the zenity GUI. Existing, unchanged call; the row is a new caller of it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Tampering | `Row`'s action handlers | mitigate | Every handler closes over `props.component.verb`, which originates in `listAvailable()`'s parsed 567-verb output. No handler reads search-box text, a tracked highlight, or any free-text value, so the new UI cannot widen what reaches `winetricks -q` beyond the already-parsed set. This is C-2 satisfied by construction rather than by a runtime check, and it is pinned by the "bound to this row's verb" assertions in Task 3. |
| T-44-07 | Denial of Service | `needsGui` branch | mitigate | The 8 unattended-incapable verbs render no Install button in any of the 64 state combinations (Task 3's parametrised C-4 test) and are routed to the zenity GUI instead, so none can reach `-q` where winetricks hangs or fails silently. |
| T-44-08 | Elevation of Privilege | `onOpenGui` | transfer | Launching the zenity GUI uses the existing unchanged `callTool({tool:'winetricks'})` path with no new argument surface — the row supplies no parameters to it. Privilege semantics are whatever that shipped path already has; this plan neither widens nor narrows them. |
| T-44-09 | Repudiation | row Errored state | accept | Error attribution is a UI affordance derived from log text, not an audit record. A misattributed Errored badge is a display defect, not a security finding; the authoritative log remains visible verbatim in `ProgressDialog`. |
| T-44-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. `44-RESEARCH.md` §"Package Legitimacy Audit" records zero new dependencies for this phase, and D-07 explicitly rejects adding a virtualisation library. Any `pnpm add` here is a scope violation to escalate, not to satisfy. |
</threat_model>

<verification>
```bash
npx jest --selectProjects Frontend --passWithNoTests --silent src/frontend/components/UI/Winetricks/WinetricksBrowse
npx sass --style=expanded --no-source-map src/frontend/components/UI/Winetricks/WinetricksBrowse/Row/index.scss /tmp/wtb-row.css
npx jest --selectProjects Frontend --passWithNoTests --silent      # full project, compare to baseline
pnpm lint                                                          # assert the TWO counts, not the exit code
```
`pnpm lint` has ~1 free warning slot repo-wide (production 1123/1124, tests 638/638). It prints two
`problems (` lines; record both numerically. Do NOT raise either ceiling.
</verification>

<success_criteria>
- `Row/index.tsx` renders all six action-slot states, never an Install button for a Needs-GUI verb, and binds every handler to `component.verb`.
- Every clickable action uses one shared mousedown-capture + `suppressNextClick` helper, with keyboard activation proven still working.
- Accessible names resolve the row title via `aria-labelledby` with zero new locale keys.
- `Row/index.scss` compiles, uses `--success`/`--danger` aliases with fallback chains, pins both action-slot dimensions, and emits (0,3,0)+ selectors verified against compiled CSS.
- `FilterFacetGroup/` is untouched (D-05).
- Both test files run under the no-DOM harness; the C-4 invariant is proven across 64 combinations and proven non-vacuous by a recorded revert-to-red control.
- `pnpm lint`'s two counts are unchanged or lower.
</success_criteria>

<output>
Create `.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-03-SUMMARY.md` when done.
Record verbatim: the Task 3 C-4 negative-control failure output, the compiled selectors proving
(0,3,0)+, the list of file-locally-declared custom properties justifying any bare `var()`, and both
`pnpm lint` counts.
</output>
