---
phase: 44-in-app-winetricks-browse-ui-replacing-the-search-only-panel
plan: 08
type: execute
wave: 6
depends_on:
  - 44-06
  - 44-07
files_modified:
  - .planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-LIVE-GATE.md
autonomous: false
requirements:
  - REQ-44-23
  - REQ-44-24
must_haves:
  truths:
    - "D-21: the phase's verification posture is component tests PLUS one live gate — neither alone"
    - "D-22.1: a real mouse-click install runs to completion with the browse list staying mounted through BOTH the install-start and install-completion transitions"
    - "D-22.2: the Installed badge appears in place on that same row afterwards, with no list reflow and no scroll jump"
    - "D-23: Needs-GUI routing and pointer-driven browse/search were NOT selected for the live gate and rest on component tests alone — recorded so no later document claims coverage this phase does not have"
    - "D-24: theme checking is the existing undefined-custom-property gate PLUS a measured spot-check in one dark and one light theme"
  artifacts:
    - path: ".planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-LIVE-GATE.md"
      provides: "the recorded live-gate run: measurements, verdict, and explicit non-coverage"
      contains: "D-22"
  key_links:
    - from: ".planning/phases/44-.../44-LIVE-GATE.md"
      to: "src/frontend/components/UI/Winetricks/WinetricksBrowse/__tests__/remountSafety.test.tsx"
      via: "the live gate measures under a real pointer what the node-environment test asserts structurally"
      pattern: "remountSafety"
---

<objective>
Run and record the phase's two verification obligations that no automated test can discharge: D-22's
two-measurement live gate under a real mouse in a real WKWebView, and D-24's theme spot-check in one
dark and one light theme.

Purpose: D-21 rejected tests-only on a measured basis — **every** prior defect on this exact surface
(mouse-dead Install, the remount race, the hover highlight) was invisible to the suite and only
appeared under a real pointer in a real WKWebView. The Frontend jest project has no DOM, no pointer
and no compositor; reflow and scroll position are not observable without layout.

Output: `44-LIVE-GATE.md` with measured results, an explicit verdict, and an explicit statement of
what this gate does **not** cover.

**This plan is `autonomous: false`.** Both tasks are blocking human checkpoints. They require the
operator's Mac, a real pointer, a real Wine game with a bottle, and a real winetricks install. An
agent cannot execute them and must not report an outcome it did not observe.
</objective>

<planner_decisions>
## What is and is not measured here, and why that is written down

D-22 names **exactly two** measurements and D-23 explicitly records two things that were considered
and **not** selected. That asymmetry is deliberate and must survive into the artifact: the point of
D-23 is that no later document gets to claim live coverage this phase does not have.

| Behaviour | Live-gated? | Basis |
|---|---|---|
| Mouse-click install completes with the list mounted through both transitions | **Yes** (D-22.1) | the phase's central claim |
| Installed badge appears in place, no reflow, no scroll jump | **Yes** (D-22.2) | proves the refetch renders as a state change, not a remount |
| Needs-GUI routing for the 8 unattended-incapable verbs | **No** (D-23) | component tests only — plan 44-03's 64-case C-4 parametrisation |
| Pointer-driven browse/search | **No** (D-23) | component tests only — plan 44-04 |
| Theme token survival | gate | the existing undefined-custom-property gate (D-24) |
| Theme contrast | **Yes, spot-check** (D-24) | the gate cannot see a contrast failure; this repo has shipped a 1.46:1 pairing every gate passed |

**Measure, do not eyeball.** Two recorded lessons bind this run. "No layout shift" has been asserted
and been wrong twice on this repo; pixel-measure rather than judging. And a colour contract has
nearly failed correct code when scored by eye; measure the contrast ratio rather than deciding it
looks fine.

**Run the negative control.** A live gate whose instrument was never shown to be capable of
reporting a failure proves nothing. Before scoring D-22.1, confirm the observation method can
actually detect a blanked list — e.g. by watching the same region during a transition known to
unmount something, or by checking that the chosen capture cadence is fast enough to catch a ~4ms
teardown. Record what the control was and what it showed.
</planner_decisions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-CONTEXT.md
@.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-VALIDATION.md
@.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: D-22 live gate — mouse-click install, list stays mounted, badge appears in place</name>
  <files>.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-LIVE-GATE.md</files>
  <read_first>
    - `.planning/phases/44-.../44-VALIDATION.md` §"Manual-Only Verifications" — the two rows and their stated test instructions.
    - `.planning/phases/44-.../44-CONTEXT.md` D-21, D-22, D-23.
    - `.planning/phases/44-.../44-05-SUMMARY.md` — what `remountSafety.test.tsx` already proves structurally, so this run measures the thing the test cannot: real pointer, real compositor, real timing.
    - `src/frontend/components/UI/Winetricks/WinetricksBrowse/index.tsx` — the class names to watch (`WinetricksBrowse__region`, `WinetricksBrowse__row`, `WinetricksBrowse__revalidating`).
  </read_first>
  <action>
Operator-run. Follow `<how-to-verify>` below step by step and write the results into
`44-LIVE-GATE.md`. Concretely: build and launch the **packaged** app (not `tauri:dev`), establish a
negative control that your observation method can actually detect a blanked list region, then take
two measurements — (1) does the browse region survive install START and install COMPLETION, scored
as two independent observations; (2) what are the numeric scroll-offset and row-Y deltas before and
after the Installed badge appears. Record numbers, not adjectives, and add the mandatory
"Not covered by this gate (D-23)" section naming Needs-GUI routing and pointer-driven browse/search
as resting on component tests alone. An agent must not fill in an outcome it did not observe.
  </action>
  <verify>
    <human-check>Both D-22 measurements scored with numeric results and an explicit PASS/FAIL each, recorded in `44-LIVE-GATE.md`.</human-check>
    <automated>test -f .planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-LIVE-GATE.md && grep -q "D-23" .planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-LIVE-GATE.md && echo "live-gate record present with D-23 non-coverage section"</automated>
  </verify>
  <what-built>
Plans 44-01 through 44-07 shipped: a browse-first categorised Winetricks panel with a curated group,
per-category disclosure groups, per-row install/installed/cached/errored/needs-GUI states, and — the
central claim — removal of **both** mount gates (`!installing` and `!loadingInstalled`) so the list
survives an install start and an install completion, with the post-install `listInstalled()` refetch
rendering as a stale-while-revalidate overlay rather than a remount.

`remountSafety.test.tsx` asserts this structurally under a node-environment harness with no DOM, no
pointer and no compositor, and its two revert-to-red transcripts are recorded in `44-05-SUMMARY.md`.
This checkpoint measures the same claim under the conditions where every prior defect on this surface
actually appeared.
  </what-built>
  <how-to-verify>
**Setup**
1. Build and launch the packaged app (not `tauri:dev` — the dev shell serves from Vite and has its
   own teardown behaviour; the claim is about the shipped build).
2. Open a Wine/CrossOver game's settings and open the Winetricks panel.
3. Note the app theme in use and record it.

**Negative control — do this BEFORE scoring anything**
4. Establish that your observation method can detect a disappearing list at all. State what method
   you used (screen recording at a named frame rate, a repeated pixel probe, the Web Inspector's
   element panel, etc.) and demonstrate it reports *something* when the region genuinely changes.
   Record the control and its result. If the method cannot resolve a ~4ms teardown, say so and
   choose a different one — a gate that cannot see its own failure mode is worse than no gate.

**Measurement 1 — D-22.1: the list stays mounted through both transitions**
5. **Browse, do not search.** Expand a category with a real mouse click. This matters: the search
   path renders the flat pane, and the claim is about the grouped browse list.
6. Scroll partway into the expanded category so there is a measurable scroll offset. **Record that
   offset numerically** (pixels or a named row at a named screen position).
7. With a real mouse, click Install on a small, fast verb.
8. Watch the browse region continuously from the click until the install reports completion. Record,
   as two separate observations:
   - **at install START** — did the list region ever blank, flicker, or lose its rows?
   - **at install COMPLETION** — same question, at the moment the row flips out of Installing.
   Score each independently. `35-25` closed only half this defect precisely because only half was
   checked.
9. Record whether the thin revalidation indicator appeared during the post-install refetch and
   whether the rows remained visible underneath it at the same time.

**Measurement 2 — D-22.2: the badge appears in place, no reflow, no scroll jump**
10. **Before** completion, capture the scroll offset and the on-screen Y position of the row being
    installed and of two rows below it.
11. **After** the Installed badge appears, capture the same three positions and the scroll offset.
12. **Pixel-measure the deltas. Do not eyeball them.** "No layout shift" has been asserted and been
    wrong twice on this repo. Report each delta as a number. A non-zero vertical delta on the rows
    below means the Installing→Installed action-slot swap is not metrically identical, which is the
    hazard the UI-SPEC's fixed action-slot height exists to prevent.
13. Confirm the badge appeared **on that same row, in place** — not that the list re-sorted, re-rendered
    from the top, or scrolled.

**Verdict**
14. Write `44-LIVE-GATE.md` with: the build identifier, the theme, the verb installed, the
    negative control and its result, both measurements with **numbers** (not adjectives), and a
    per-measurement PASS / FAIL verdict.
15. Include a section headed **"Not covered by this gate (D-23)"** stating explicitly that Needs-GUI
    routing for the 8 unattended-incapable verbs and pointer-driven browse/search were **not**
    selected for live coverage and rest on component tests alone. This section is required by D-23
    and exists so no later document claims coverage this phase does not have.
16. If anything FAILS: do not soften it. Record the failure with its measurement, and file a todo in
    `.planning/todos/pending/` carrying `severity:`, `platform:`, `ready:` in that order, bare and
    lowercase (CI-enforced — `pnpm planning-gates` fails otherwise).
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/44-.../44-LIVE-GATE.md` exists and names the build identifier, the theme and the verb installed.
    - It records a negative control with its method and result — not just an assertion that the method works.
    - D-22.1 is scored as **two independent observations** (install START and install COMPLETION), each with its own verdict. A single combined verdict does not satisfy D-22.1.
    - D-22.2 reports **numeric** deltas for the scroll offset and for at least two rows' Y positions, before and after the badge appears. Adjectives ("no visible shift") are not acceptable in place of numbers.
    - The file contains a section headed "Not covered by this gate (D-23)" naming both excluded behaviours.
    - Every measurement carries a PASS or FAIL verdict; no measurement is left unscored or recorded as "looked fine".
    - If any verdict is FAIL, a corresponding todo exists in `.planning/todos/pending/` and `pnpm planning-gates` exits 0 against its frontmatter.
  </acceptance_criteria>
  <done>`44-LIVE-GATE.md` records the build, theme, verb, a negative control with its result, D-22.1 scored as two independent observations, D-22.2 with numeric scroll and row-position deltas, a PASS/FAIL per measurement, and a "Not covered by this gate (D-23)" section.</done>
  <resume-signal>Type "approved" with the two verdicts, or describe what failed and at which measurement.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-24 theme spot-check — one dark theme, one light theme, contrast measured</name>
  <files>.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-LIVE-GATE.md</files>
  <read_first>
    - `.planning/phases/44-.../44-CONTEXT.md` D-24 and the `<code_context>` note that `--status-*` are raw constants with no light-theme override while `--success`/`--danger` are the theme-adaptive aliases.
    - `.planning/phases/44-.../44-03-SUMMARY.md` and `44-04-SUMMARY.md` — the compiled-CSS assertions and the list of file-locally-declared custom properties justifying any bare `var()`. This spot-check looks for what those assertions structurally cannot see.
    - `.planning/phases/44-.../44-UI-SPEC.md` §"Color" — the accent-reservation list and the C-3 non-colour-signal requirement.
  </read_first>
  <action>
Operator-run. Follow `<how-to-verify>` below and append a "D-24 theme spot-check" section to
`44-LIVE-GATE.md`. Concretely: open the browse panel in one named dark theme and one named light
theme, drive every row state onto screen (Available, Available + Cached, Installing this row,
Installing elsewhere, Installed, Needs GUI, Errored), and **measure** the contrast ratio of each
badge/status text and icon against the row background rather than judging it by eye. Record a table
of state × theme × measured ratio × C-3 icon-plus-text verdict, confirm no declaration silently
dropped (headers, carets, count badges, hover, `:focus-visible` rings all render in both themes),
and confirm category headers read as raw uppercased parser strings (`DLLS`) per D-09. Mark any state
you could not reach live by name rather than omitting it. An agent must not fill in a ratio it did
not measure.
  </action>
  <verify>
    <human-check>A measured contrast ratio recorded for every reachable row state in both named themes, with a C-3 verdict each.</human-check>
    <automated>grep -q "D-24 theme spot-check" .planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-LIVE-GATE.md && pnpm planning-gates</automated>
  </verify>
  <what-built>
Plans 44-03 and 44-04 shipped `Row/index.scss` and `WinetricksBrowse/index.scss`, both using
token-survival fallback chains and the theme-adaptive `--success`/`--danger` aliases rather than the
raw `--status-*` constants, verified against **compiled** CSS rather than source nesting.

D-24 pairs that gate with a human spot-check because the undefined-custom-property gate cannot see a
contrast failure: this repo has shipped a 1.46:1 pairing that every gate passed.
  </what-built>
  <how-to-verify>
1. Open the Winetricks browse panel in **one dark theme** and then in **one light theme**. Name both
   themes in the record. Prefer a light theme known to lack `--navbar-active` and prefer `nord-light`
   if available, since that is the theme where `--status-danger` was measured at 2.27:1.
2. Drive the panel into each visually-distinct row state so every one is actually on screen:
   Available, Available + Cached, Installing (this row), Installing (elsewhere), Installed,
   Needs GUI, Errored. Reaching Errored may require deliberately installing a verb that fails; if you
   cannot reach it live, say so explicitly rather than scoring it unobserved.
3. For each state, in each theme, **measure** the contrast ratio of the badge/status text and its icon
   against the row background. Record the ratio as a number. Do not judge it by eye — a colour
   contract on this repo has already nearly failed correct code when scored visually.
4. Confirm **C-3** holds in both themes: every state pairs its colour with an icon **and** a text
   label. Colour must never be the only signal. Note any state where the icon or the label is
   missing or invisible.
5. Confirm no declaration silently dropped: check that group headers, carets, count badges, hover and
   `:focus-visible` rings all render in both themes. A dropped declaration from an undefined custom
   property with no fallback is silent and total — the element simply has no colour, not a wrong one.
6. Confirm the category headers render the parser's raw strings uppercased (`DLLS`, not
   `DLLS & LIBRARIES`) — this is D-09's recorded consequence, and seeing it is the confirmation that
   the decision shipped as decided, not a defect to file.
7. Append a **"D-24 theme spot-check"** section to `44-LIVE-GATE.md` with a table: state × theme ×
   measured contrast ratio × C-3 pass/fail. Give a verdict per row.
8. If any measured ratio falls below the WCAG AA threshold for its text size, record it with the
   number and file a todo in `.planning/todos/pending/` carrying `severity:`, `platform:`, `ready:`
   in that order, bare and lowercase (CI-enforced).
  </how-to-verify>
  <acceptance_criteria>
    - `44-LIVE-GATE.md` carries a "D-24 theme spot-check" section naming both themes explicitly.
    - The section contains a table with a **numeric** measured contrast ratio for every row state in both themes. Any state not reachable live is marked as such, by name, rather than silently omitted or scored unobserved.
    - Every row carries a C-3 pass/fail for the icon-plus-text pairing.
    - The record confirms group headers, carets, count badges, hover and focus rings all render in both themes.
    - The record confirms category headers read as the raw uppercased parser strings (D-09's recorded consequence).
    - Any sub-threshold ratio has a corresponding todo in `.planning/todos/pending/` with correct frontmatter, and `pnpm planning-gates` exits 0.
  </acceptance_criteria>
  <done>`44-LIVE-GATE.md` carries a "D-24 theme spot-check" section naming both themes, with a measured numeric contrast ratio and a C-3 verdict for every reachable row state, any unreachable state named explicitly, and a todo filed for any sub-threshold ratio.</done>
  <resume-signal>Type "approved" with both theme names and the lowest measured ratio, or describe which state/theme failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → sidecar → shell | The live run exercises the real `winetricksInstall` path end to end: a verb string reaches a `winetricks -q <verb>` invocation on the operator's machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-25 | Tampering | the live install this gate performs | accept | The run installs a real winetricks verb into a real bottle on the operator's own machine, using the unchanged shipped IPC path. That is the behaviour under test, not a new exposure. Choose a small, well-known verb; the gate does not require an exotic one. |
| T-44-26 | Repudiation | a verdict recorded without a measurement | mitigate | Every acceptance criterion demands a number or an explicit "not reachable, here is why". Adjectives are rejected in place of measurements, and D-22.1 must be scored as two independent observations so a half-measured pass cannot be recorded as a whole one — the exact error `35-25` made. |
| T-44-27 | Information Disclosure | screen recordings / captures from the live run | mitigate | Any capture from a real profile may embed session state, paths or account identifiers. Register captures for disposal and do not commit raw recordings into `.planning/`; commit the measurements, not the frames. This repo has twice left unredacted captures on disk after a run that specified deletion in writing. |
| T-44-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. This plan builds and runs the existing app and writes one markdown record. |
</threat_model>

<verification>
```bash
test -f .planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-LIVE-GATE.md
grep -c "D-23" .planning/phases/44-.../44-LIVE-GATE.md      # the non-coverage section is required
pnpm planning-gates                                          # green, including any todo filed by a FAIL
```
</verification>

<success_criteria>
- `44-LIVE-GATE.md` exists with the build identifier, theme, verb, and a negative control with its result.
- D-22.1 is scored as two independent observations (install START, install COMPLETION), each with its own verdict.
- D-22.2 reports numeric scroll-offset and row-position deltas before and after the badge appears.
- A "Not covered by this gate (D-23)" section names Needs-GUI routing and pointer-driven browse/search as resting on component tests alone.
- A "D-24 theme spot-check" section gives numeric contrast ratios per row state in one dark and one light theme, with a C-3 verdict each.
- Every measurement carries a PASS or FAIL; nothing is recorded as "looked fine".
- Any FAIL has a correspondingly-filed todo with CI-valid frontmatter, and `pnpm planning-gates` is green.
</success_criteria>

<output>
Create `.planning/phases/44-in-app-winetricks-browse-ui-replacing-the-search-only-panel/44-08-SUMMARY.md` when done,
alongside `44-LIVE-GATE.md`. Record the overall verdict and, if any measurement failed, exactly which
one and the number that failed it.
</output>
