---
phase: 18-macos-32-bit-detection-badge-crossover-routing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/common/types.ts
  - src/backend/storeManagers/steam/electronStores.ts
  - scripts/steam-appinfo-dump.cjs
  - src/backend/storeManagers/steam/__tests__/fixtures/appinfo-32bit.json
  - src/backend/storeManagers/steam/__tests__/fixtures/appinfo-64bit.json
  - src/backend/storeManagers/steam/__tests__/fixtures/appinfo-no-osarch.json
  - src/backend/storeManagers/steam/__tests__/fixtures/appinfo-false-flag.json
autonomous: false
requirements: [MAC32-01]

must_haves:
  truths:
    - "GameInfo carries a mac_arch signal that can be '32' | '64' | 'unknown'"
    - "SteamMetadataCacheEntry can persist the arch verdict plus its provenance and verified flag"
    - "Real captured getProductInfo appinfo payloads exist as test fixtures, including a missing-osarch case and the false-flag titles"
  artifacts:
    - path: "src/common/types.ts"
      provides: "GameInfo.mac_arch optional field"
      contains: "mac_arch"
    - path: "src/backend/storeManagers/steam/electronStores.ts"
      provides: "SteamMetadataCacheEntry mac_arch / mac_arch_verified / mac_arch_source fields"
      contains: "mac_arch"
    - path: "src/backend/storeManagers/steam/__tests__/fixtures/appinfo-no-osarch.json"
      provides: "Real captured payload with no osarch key (the false-flag-safe fixture)"
  key_links:
    - from: "src/backend/storeManagers/steam/electronStores.ts"
      to: "src/common/types.ts"
      via: "mac_arch type union shared between cache entry and GameInfo"
      pattern: "'32' \\| '64' \\| 'unknown'"
---

<objective>
Establish the arch-signal contracts that every downstream plan in Phase 18 consumes, and capture the real PICS `getProductInfo` appinfo payloads that gate the parser (MAC32-01 pre-work).

Purpose: The `mac_arch` type must exist on both `GameInfo` and `SteamMetadataCacheEntry` before the parser (Plan 02), the Mach-O check (Plan 03), and the badge (Plan 04) can compile against it. The parser cannot be locked until it is validated against real captured payloads — the field path/casing was corroborated only from community sources, not a canonical Valve schema (see RESEARCH.md Assumption A1 and the pre-work todo).
Output: `mac_arch` on `GameInfo`; `mac_arch` / `mac_arch_verified` / `mac_arch_source` on `SteamMetadataCacheEntry`; a runnable dump harness; and four fixture JSON files (32-bit-only, 64-bit, no-osarch, false-flag) under the Steam test `fixtures/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-macos-32-bit-detection-badge-crossover-routing/18-CONTEXT.md
@.planning/phases/18-macos-32-bit-detection-badge-crossover-routing/18-RESEARCH.md
@.planning/phases/18-macos-32-bit-detection-badge-crossover-routing/18-PATTERNS.md
@.planning/todos/pending/steam-getproductinfo-appinfo-dump.md

<interfaces>
<!-- Contracts to mirror. Extracted from the codebase — use directly, no exploration needed. -->

From src/common/types.ts (existing, GameInfo fields ~line 220):
  is_mac_native?: boolean
  is_linux_native?: boolean
  is_delisted?: boolean          // has a /** */ doc comment explaining rationale
  steamPlatformsCaptured?: boolean

From src/backend/storeManagers/steam/electronStores.ts (existing, SteamMetadataCacheEntry ~line 35-51):
  art_cover: string
  art_square: string
  extra: ExtraInfo
  is_mac_native?: boolean
  is_linux_native?: boolean
  is_delisted?: boolean
  platformsCaptured?: boolean

From src/backend/storeManagers/steam/user.ts (existing auth plumbing to reuse in the dump harness):
  SteamUser.getClient()  // returns the connected steam-user client or null
  // steam-user getProductInfo(apps: number[], packages: number[]): Promise<ProductInfo>
  // ProductInfo.apps[appid].appinfo.config.launch[N].config.{oslist, osarch}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the mac_arch signal to GameInfo and SteamMetadataCacheEntry</name>
  <files>src/common/types.ts, src/backend/storeManagers/steam/electronStores.ts</files>
  <read_first>
    - src/common/types.ts lines ~215-228 (the is_mac_native / is_delisted / steamPlatformsCaptured field block — mirror its optional-field + doc-comment style)
    - src/backend/storeManagers/steam/electronStores.ts lines 35-51 (SteamMetadataCacheEntry — the exact field block to extend)
    - 18-PATTERNS.md "electronStores.ts field addition" and "common/types.ts" sections (locked field shapes + doc-comment text)
  </read_first>
  <action>
    In src/common/types.ts, add an optional `mac_arch?: '32' | '64' | 'unknown'` field to `GameInfo`, immediately after `is_mac_native?` and before `is_linux_native?`, with a `/** */` doc comment matching the style of the adjacent `is_delisted?`/`steamPlatformsCaptured?` fields. The comment must state the false-flag-safe invariant: absent key = unknown, and a missing/blank Steam `osarch` is NEVER coerced to '32'.
    In src/backend/storeManagers/steam/electronStores.ts, add three optional fields to `SteamMetadataCacheEntry` adjacent to `is_mac_native?`/`platformsCaptured?`, using the flat-field convention already used by this interface (NOT a nested `{ arch, source }` object): `mac_arch?: '32' | '64' | 'unknown'` (PICS pre-install hint, corrected post-install), `mac_arch_verified?: boolean` (true once the post-install lipo/file check has run — prevents re-shelling on every launch), and `mac_arch_source?: 'osarch' | 'macho'` (provenance per the CONTEXT.md-locked cache shape; forward-compat for a future Phase-19 community export — keep even though nothing reads it yet). Each field gets a `/** */` doc comment. Do NOT build any export/contribution flow (explicitly out of scope, CONTEXT D-5).
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mac_arch" src/common/types.ts` shows `mac_arch?: '32' | '64' | 'unknown'` on GameInfo
    - `grep -n "mac_arch\|mac_arch_verified\|mac_arch_source" src/backend/storeManagers/steam/electronStores.ts` shows all three new fields on SteamMetadataCacheEntry
    - `npm run codecheck` exits 0 (tsc clean — no other file references break from the additive optional fields)
    - No nested object shape introduced; fields are flat optionals like the existing is_mac_native
  </acceptance_criteria>
  <done>GameInfo and SteamMetadataCacheEntry both expose the mac_arch signal; tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build the getProductInfo appinfo dump harness</name>
  <files>scripts/steam-appinfo-dump.cjs</files>
  <read_first>
    - src/backend/storeManagers/steam/user.ts (how the app constructs/authenticates the steam-user client; refresh-token flow) — mirror the login shape
    - .planning/todos/pending/steam-getproductinfo-appinfo-dump.md (the exact capture requirements: three sample titles, record casing/nesting, absent-vs-empty-string distinction)
    - 18-RESEARCH.md "Code Examples — getProductInfo call shape" (SDK signature) and Pitfall 2 (enablePicsCache: false does NOT block getProductInfo — no client reconfiguration needed)
  </read_first>
  <action>
    Create a standalone Node script `scripts/steam-appinfo-dump.cjs` that: (1) constructs a `steam-user` client and authenticates using `steam-session` interactive login (QR or credentials), mirroring the auth flow in user.ts — self-contained, no Electron/safeStorage dependency required; (2) accepts one or more numeric AppIDs as argv; (3) guards each argv value with the `/^\d+$/` numeric-appid check (reuse the pattern from buildSteamProtocolUrl in games.ts) before passing to `getProductInfo([appIdNum], [])`; (4) writes `result.apps[appIdNum].appinfo` (the full appinfo object, or at minimum `appinfo.config.launch`) as pretty-printed JSON to a path given by a `--out` argv flag; (5) logs the exact key path + casing observed for `oslist` and `osarch`, and whether the no-osarch case is an absent key vs empty string. Do NOT hardcode credentials. This is a one-off developer harness; keep it out of the app bundle (scripts/ dir).
  </action>
  <verify>
    <automated>node -c scripts/steam-appinfo-dump.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/steam-appinfo-dump.cjs` exits 0 (script parses)
    - Script contains a `/^\d+$/` guard on argv AppIDs before the getProductInfo call
    - Script calls `getProductInfo([appIdNum], [])` and writes `apps[appIdNum].appinfo` to the `--out` path
    - Script does NOT reconfigure `enablePicsCache` on the client (per Pitfall 2)
  </acceptance_criteria>
  <done>A runnable, self-authenticating dump harness exists that captures raw appinfo for supplied AppIDs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Capture real appinfo fixtures (human-run dump)</name>
  <action>Human runs scripts/steam-appinfo-dump.cjs against real 32-bit-only, 64-bit, no-osarch, and false-flag mac titles and commits the four fixture JSON files. Claude cannot authenticate to Steam interactively, so this manual capture gates the parser.</action>
  <what-built>
    Task 1 added the `mac_arch` contracts; Task 2 built `scripts/steam-appinfo-dump.cjs`. The remaining step requires a real authenticated Steam session (which Claude cannot perform interactively), so the human runs the harness against real titles and commits the captured fixtures.
  </what-built>
  <how-to-verify>
    1. Run the harness for a KNOWN 32-bit-only mac title:
       `node scripts/steam-appinfo-dump.cjs <appId> --out src/backend/storeManagers/steam/__tests__/fixtures/appinfo-32bit.json`
    2. Run it for a KNOWN 64-bit mac title → `appinfo-64bit.json`
    3. Run it for an OLD mac title suspected to have NO osarch tag → `appinfo-no-osarch.json`
    4. Run it for one documented false-flag title (A Hat in Time, Metro: Last Light, or BattleBlock Theater — supply its AppID) → `appinfo-false-flag.json`. This title has a mac depot but no explicit osarch "64" and MUST parse to 'unknown', not '32'.
    5. Inspect each JSON: confirm `config.launch[N].config` has lowercase `oslist` and (where present) `osarch`; note whether the no-osarch case is an absent key or an empty string, and whether legacy `"osx"` or current `"macos"` appears. Record these findings for Plan 02's parser tests.
    6. Commit the four fixture files under `src/backend/storeManagers/steam/__tests__/fixtures/`.
    If a real 32-bit or false-flag title's payload is unobtainable, hand-author the fixture from the documented shape (osarch absent for the false-flag case) and note it in the SUMMARY as synthetic — but the no-osarch and at least one real mac capture MUST be genuine.
  </how-to-verify>
  <resume-signal>Type "approved" once the four fixtures are committed, or describe what the real payloads revealed (casing/nesting corrections) so Plan 02's parser can be adjusted.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network (Steam CM) → dump harness | PICS appinfo KeyValues arrive from Steam; shape/types are not guaranteed |
| CLI argv → getProductInfo | AppIDs supplied on the command line reach an RPC call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | scripts/steam-appinfo-dump.cjs argv → getProductInfo | mitigate | Guard each argv AppID with `/^\d+$/` before `parseInt` and the RPC call (reuse buildSteamProtocolUrl's T-03-01 pattern) |
| T-18-01-02 | Information Disclosure | captured fixture JSON | accept | Fixtures contain public store metadata only (launch config, arch tags) — no credentials/tokens; harness never persists auth material to disk |
| T-18-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase — steam-user/steam-session/@node-steam/vdf already direct deps (RESEARCH.md Package Legitimacy Audit: gate N/A) |
</threat_model>

<verification>
- `npm run codecheck` exits 0 after Task 1.
- `node -c scripts/steam-appinfo-dump.cjs` exits 0 after Task 2.
- Four fixture JSON files present under `src/backend/storeManagers/steam/__tests__/fixtures/` after the checkpoint, including `appinfo-no-osarch.json` (missing-key case) and `appinfo-false-flag.json`.
</verification>

<success_criteria>
- `GameInfo.mac_arch` and `SteamMetadataCacheEntry.{mac_arch,mac_arch_verified,mac_arch_source}` exist and compile.
- The dump harness runs and produces real appinfo fixtures.
- The four fixtures are committed and their osarch/oslist casing findings are recorded for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/18-macos-32-bit-detection-badge-crossover-routing/18-01-SUMMARY.md` when done. Record the observed osarch/oslist casing, the absent-vs-empty-string result for the no-osarch case, and whether any fixture is synthetic.
</output>
