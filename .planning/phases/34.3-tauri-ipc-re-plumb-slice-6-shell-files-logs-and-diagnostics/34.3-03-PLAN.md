---
phase: 34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
  - src-tauri/src/main.rs
  - src/common/types/sidecarTransport.ts
autonomous: true
requirements: [REQ-34.3-03, REQ-34.3-06, REQ-34.3-08, REQ-34.3-13]
user_setup: []
must_haves:
  truths:
    - "The Rust shell can write text to the real OS clipboard when the sidecar asks it to"
    - "The Rust shell can read text from the real OS clipboard and return it to the sidecar"
    - "No clipboard-manager permission is granted to the webview — capabilities/default.json is unchanged"
    - "A future reader of the app_relaunch arm learns from the code itself that RunEvent::Exit already fires and no orphan-sidecar fix is needed"
    - "cargo check and cargo test both pass locally"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "tauri-plugin-clipboard-manager dependency"
      contains: "tauri-plugin-clipboard-manager"
    - path: "src-tauri/src/main.rs"
      provides: "clipboard_write_text / clipboard_read_text dispatch arms, 2 pure helpers, plugin registration, the D-05 verified-no-fix comment, and #[cfg(test)] coverage"
      contains: "clipboard_write_text"
    - path: "src/common/types/sidecarTransport.ts"
      provides: "RUST_CLIPBOARD_WRITE_TEXT / RUST_CLIPBOARD_READ_TEXT constants, both in RUST_INVOKE_CHANNELS"
      exports: ["RUST_CLIPBOARD_WRITE_TEXT", "RUST_CLIPBOARD_READ_TEXT"]
  key_links:
    - from: "src-tauri/src/main.rs dispatch_rust_channel"
      to: "app.clipboard()"
      via: "tauri_plugin_clipboard_manager::ClipboardExt"
      pattern: "app\\.clipboard\\(\\)"
    - from: "src/common/types/sidecarTransport.ts RUST_INVOKE_CHANNELS"
      to: "the two new clipboard channel names"
      via: "aggregate array membership — a missing entry makes requestRustInvoke pre-reject before the transport"
      pattern: "RUST_CLIPBOARD_(WRITE|READ)_TEXT"
---

<objective>
Build the slice's one genuinely new platform seam in Rust: add
`tauri-plugin-clipboard-manager`, register it, and add exactly two
`dispatch_rust_channel` arms (`clipboard_write_text`, `clipboard_read_text`) that call
`app.clipboard()` from the Rust side with **zero** renderer capability grant. Mint the two channel
constants in `sidecarTransport.ts`. Record the D-05 relaunch-teardown verification as a code
comment. Prove the new Rust with `#[cfg(test)]` tests over extracted pure helpers.

Purpose: `electronStub.clipboard.writeText` is a logged no-op and `readText()` returns `''`; three
channels in this slice need a real clipboard, and `clipboardWriteText`'s callers include
`HumbleClaimWizard/index.tsx:120,366`, which copy a revealed Humble game key — the guided-claim
flow that is milestone v0.3's core deliverable. **Framing (carry into the SUMMARY):** this is not a
latent shipped defect. The only backend consumers of `clipboard.writeText`/`readText` are this
slice's own three channels, so nothing has been silently degrading in production. The no-op was
purely precautionary — it is the direct descendant of Phase 31 D-04, which wrote "deferred to
Phase 33"; Phase 33 never collected it. This plan closes a two-phase-old promise.

Output: one new crate dependency, two new Rust arms, two new pure helpers with real behavioral
tests, two new transport constants, and one verified-finding comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-CONTEXT.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-RESEARCH.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. -->

Crate API (verified by direct source read of `tauri-plugin-clipboard-manager` 2.3.2 —
34.3-RESEARCH.md Q2):
- `pub fn init<R: Runtime>() -> TauriPlugin<R>` — **contains NO `.js_init_script(...)`**, injects
  nothing into the webview, and its `permissions/default.toml` declares `permissions = []`
- `pub trait ClipboardExt<R: Runtime> { fn clipboard(&self) -> &Clipboard<R>; }` — `use tauri_plugin_clipboard_manager::ClipboardExt;`
- `Clipboard::write_text(impl Into<String>) -> Result<(), Error>`
- `Clipboard::read_text() -> Result<String, Error>` — the plugin's own `desktop.rs` docstring warns
  this **must not be called on the main thread** (Linux deadlock). Satisfied by construction here:
  `dispatch_rust_channel` always runs on a `thread::spawn`'d worker (`main.rs:748-873`).

Existing arm template — `main.rs:553-562` (`shell_open_path`) and `main.rs:355-420` (`keyring_*`):
arg extraction via `args.first().and_then(|v| v.as_str()).ok_or("<channel>:bad-args")?`, plugin call,
`.map_err(|e| e.to_string())?`, `Ok(Value::Null)`.

Existing plugin chain — `main.rs:910-914`, five `.plugin(...)` lines, bare-version deps at
`Cargo.toml:16-23`.

Existing `#[cfg(test)] mod tests` — `main.rs:1063+`, `use super::*;`, plain `#[test] fn` assertions
over pure functions, with a docstring recording that this project's CI runs **no cargo step at all**
so these tests are hand-run.

Transport constants — `src/common/types/sidecarTransport.ts`: individual `export const RUST_* = '<snake_case>' as const`
declarations at L134-218, then the aggregate `RUST_INVOKE_CHANNELS` array at L225-239 and
`export type RustInvokeChannel = (typeof RUST_INVOKE_CHANNELS)[number]` at L242.

Q1 finding (34.3-RESEARCH.md, HIGH confidence, direct read of tauri 2.11.5 as pinned by
`Cargo.lock`): `AppHandle::restart()`'s "skip the events" branch applies **only** when called on the
main thread. `dispatch_rust_channel` always runs on a spawned worker, so `app_relaunch` always takes
the branch that sets `restart_on_exit = true` and requests exit through the real event loop — and
`main.rs`'s own `RunEvent::Exit` callback (L1042, Phase 34 WR-03's `shutdown_child()`) runs BEFORE
the process re-execs. **There is no orphan window. Add NO `state.shutdown_child()` call to the
`app_relaunch` arm.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the clipboard plugin and the two dispatch arms</name>
  <files>src-tauri/Cargo.toml, src-tauri/Cargo.lock, src-tauri/src/main.rs, src/common/types/sidecarTransport.ts</files>
  <read_first>
    - `src-tauri/Cargo.toml` — the existing `tauri-plugin-*` bare-version lines (L16-23) whose style
      the new line must match.
    - `src-tauri/src/main.rs` lines 40-50 — the existing `use tauri_plugin_*::*Ext;` imports.
    - `src-tauri/src/main.rs` lines 340-360 — `dispatch_rust_channel`'s signature and its first arm.
    - `src-tauri/src/main.rs` lines 536-600 — the `shell_show_item_in_folder` / `shell_open_path` /
      `app_exit` / `app_relaunch` arms: the exact arg-extraction idiom, error-mapping idiom, and
      docstring density to copy.
    - `src-tauri/src/main.rs` lines 900-920 — the `.plugin(...)` chain.
    - `src-tauri/capabilities/default.json` — READ THE `description` FIELD IN FULL. It states that
      Tauri v2 capabilities gate webview→Rust IPC only and are not consulted for Rust-side plugin
      calls, why `dialog:allow-open` was refused (WR-03), and that `"windows": ["main"]` shares
      grants with untrusted remote content. **This file must not be modified by this plan.**
    - `.planning/phases/34.3-.../34.3-RESEARCH.md` § "Q2" and § "Pattern 1".
  </read_first>
  <action>
Add `tauri-plugin-clipboard-manager = "2"` to `src-tauri/Cargo.toml`'s `[dependencies]`, adjacent to
the other `tauri-plugin-*` lines, same bare-version style. Run a build/check so `Cargo.lock` records
the resolved version; confirm it resolves to 2.3.x and record the exact resolved version in the
SUMMARY.

**Assumption A2 confirmation step (one line, do not skip):** after the crate is vendored, run
`grep -rn "js_init_script" ~/.cargo/registry/src/*/tauri-plugin-clipboard-manager-*/src/lib.rs` and
confirm it returns nothing. Record the command and its empty result in the SUMMARY. If it DOES
return a hit, stop and escalate — the D-02 zero-capability-grant stance would need a narrowly
scoped exception (this is the `tauri-plugin-notification` landmine that crashed GameLib's startup).

In `src-tauri/src/main.rs`:
- add `use tauri_plugin_clipboard_manager::ClipboardExt;` alongside the existing plugin `use` lines
- add `.plugin(tauri_plugin_clipboard_manager::init())` to the builder chain, same style, no builder args
- add two arms to `dispatch_rust_channel`, placed immediately after the `shell_open_path` arm:
  - `"clipboard_write_text"` — extract the text via the new `clipboard_text_arg` helper (Task 2),
    call `app.clipboard().write_text(text).map_err(|e| e.to_string())?`, return `Ok(Value::Null)`
  - `"clipboard_read_text"` — call `app.clipboard().read_text()`, map the error to a `String`, and
    shape the success value through the new `clipboard_read_value` helper (Task 2)
- give each arm a `//` line-comment docstring matching the neighbouring arms' density: name the
  phase/plan/REQ, name `electronStub.clipboard.writeText` / the `clipboardReadText` sidecar handler
  as the caller, state that these are the ONLY new `dispatch_rust_channel` arms the whole slice adds,
  and — on the read arm — state that it is safe here specifically because
  `dispatch_rust_channel` always runs on a `thread::spawn`'d worker thread, never the main/reader
  thread, citing the plugin's own Linux-deadlock warning, so a future refactor that moves dispatch
  onto the reader thread does not silently reintroduce that deadlock.

**Do NOT add any `clipboard-manager:*` entry to `src-tauri/capabilities/default.json`.** Verified
safe by 34.3-RESEARCH.md Q2: the plugin has no `js_init_script`, so unlike
`tauri-plugin-notification` it needs no grant to avoid a startup crash. Granting clipboard *read* to
the `"windows": ["main"]` scope would hand a credential-exfiltration primitive to the untrusted
remote content that renders in that same window.

**Do NOT add `state.shutdown_child()` to the `app_relaunch` arm** — see Task 3.

Mint the two channel constants in `src/common/types/sidecarTransport.ts`:
`export const RUST_CLIPBOARD_WRITE_TEXT = 'clipboard_write_text' as const` and
`export const RUST_CLIPBOARD_READ_TEXT = 'clipboard_read_text' as const`, each with a short doc
comment in the style of the neighbouring `RUST_SHELL_*`/`RUST_APP_*` declarations, AND add both to
the aggregate `RUST_INVOKE_CHANNELS` array. The array membership is load-bearing, not cosmetic: a
channel string missing from it makes `requestRustInvoke` **pre-reject before ever reaching the
transport** — a failure mode `lifecycleStub.test.ts:102-113` exists because this codebase already
hit it.
  </action>
  <verify>
    <automated>cd src-tauri && cargo check --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tauri-plugin-clipboard-manager" src-tauri/Cargo.toml` returns 1
    - `grep -c "tauri-plugin-clipboard-manager" src-tauri/Cargo.lock` is ≥1 and the resolved version is recorded in the SUMMARY
    - `grep -rn "js_init_script" ~/.cargo/registry/src/*/tauri-plugin-clipboard-manager-*/src/lib.rs` returns nothing (recorded in the SUMMARY)
    - `grep -v '^\s*//' src-tauri/src/main.rs | grep -c '"clipboard_write_text" =>'` returns 1
    - `grep -v '^\s*//' src-tauri/src/main.rs | grep -c '"clipboard_read_text" =>'` returns 1
    - `grep -v '^\s*//' src-tauri/src/main.rs | grep -c "tauri_plugin_clipboard_manager::init()"` returns 1
    - `git diff --stat src-tauri/capabilities/default.json` shows zero changes
    - `grep -c "clipboard" src-tauri/capabilities/default.json` returns 0
    - `grep -v '^\s*//' src-tauri/src/main.rs | grep -c "shutdown_child()"` returns 1 (the pre-existing `RunEvent::Exit` call site only — NOT a second call inside `app_relaunch`)
    - `grep -c "RUST_CLIPBOARD_WRITE_TEXT\|RUST_CLIPBOARD_READ_TEXT" src/common/types/sidecarTransport.ts` returns ≥4 (2 declarations + 2 array entries)
    - `cd src-tauri && cargo check --quiet` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The clipboard plugin is installed and registered, both arms compile and call `app.clipboard()` from the worker thread, the capability file is untouched, and both channel constants are in `RUST_INVOKE_CHANNELS`.</done>
</task>

<task type="auto">
  <name>Task 2: Extract two pure helpers and prove them with #[cfg(test)] tests</name>
  <files>src-tauri/src/main.rs</files>
  <read_first>
    - `src-tauri/src/main.rs` lines 1048-1120 — the existing `#[cfg(test)] mod tests` block and its
      docstring, which records that this project's CI runs no cargo step. Copy that docstring's
      structure and its honesty about what a hand-run test does and does not prove.
    - `src-tauri/src/main.rs` — the two arms added in Task 1.
    - `.planning/phases/34.3-.../34.3-PATTERNS.md` § "Rust #[cfg(test)] mod extension" — it flags
      that the existing test module targets a pure function while the new arms need an `AppHandle`,
      and that the fix is to extract the testable surface. This task implements that.
  </read_first>
  <action>
The two new arms call `app.clipboard()`, which needs a live `AppHandle` and therefore cannot be
driven from a plain `#[test]`. Extract the arms' non-plugin logic into two **pure** module-level
functions and test those — this is the design call REQ-34.3-08 requires, and the plan states it
explicitly so a reviewer does not read the absence of an arm-level test as an oversight: a
`#[cfg(test)]` unit test structurally cannot construct a Tauri runtime, and a test that only
asserted a passthrough compiles would not be proof.

Add `fn clipboard_text_arg(args: &[Value]) -> Result<&str, String>` returning
`args.first().and_then(|v| v.as_str()).ok_or_else(|| "clipboard_write_text:bad-args".to_string())`,
and make the `clipboard_write_text` arm call it. This is the channel's entire ASVS V5 input
validation.

Add `fn clipboard_read_value(read: Result<String, String>) -> Result<Value, String>` mapping
`Ok(text)` to `Ok(Value::String(text))` and propagating `Err` unchanged, and make the
`clipboard_read_text` arm call it with `app.clipboard().read_text().map_err(|e| e.to_string())`.

Extend `#[cfg(test)] mod tests` with a new set of `#[test] fn`s. What they assert (state this in the
SUMMARY too, so the proof value is legible):

For `clipboard_text_arg` — the distinction between *absent* and *empty* is real behavior worth
pinning, and the RED direction is stated: an implementation using `unwrap_or("")` instead of
`ok_or` would flip the four rejection cases to `Ok`.
- `clipboard_text_arg(&[])` is `Err` containing `clipboard_write_text:bad-args`
- `clipboard_text_arg(&[Value::Null])` is `Err`
- `clipboard_text_arg(&[json!(1)])` is `Err`
- `clipboard_text_arg(&[Value::Bool(true)])` is `Err`
- `clipboard_text_arg(&[json!("")])` is `Ok("")` — an empty string is a VALID clipboard write and
  must not be conflated with a missing argument
- `clipboard_text_arg(&[json!("hi")])` is `Ok("hi")`
- extra trailing args are ignored: `clipboard_text_arg(&[json!("hi"), json!("ignored")])` is `Ok("hi")`

For `clipboard_read_value` — the RED direction is that an implementation returning `Value::Null` for
an empty clipboard would fail the first case, and that distinction matters because the JS handler
coerces non-strings to `''` and would mask it.
- `clipboard_read_value(Ok(String::new()))` is `Ok(Value::String(""))`, **not** `Ok(Value::Null)`
- `clipboard_read_value(Ok("hi".into()))` is `Ok(Value::String("hi"))`
- `clipboard_read_value(Err("boom".into()))` is `Err("boom")`

Run `cd src-tauri && cargo test` manually. Then **hand-verify the RED proof for both helpers** by
temporarily applying the two stated regressions (`unwrap_or("")` in `clipboard_text_arg`;
`Value::Null` for the empty read in `clipboard_read_value`), confirming the expected tests fail,
and reverting. Record both RED runs verbatim in the SUMMARY — this is D-07's whole mechanism,
because CI compiles none of this.

Extend the `mod tests` docstring with a paragraph naming Phase 34.3 Plan 03 / REQ-34.3-08, stating
which helpers are covered, restating that CI runs no cargo step so these are hand-run, and stating
plainly that the arms' plugin calls themselves are proven by REQ-34.3-11's live gate item 3, not by
these tests.
  </action>
  <verify>
    <!-- NOTE (sampling): this one task's automated verify is deliberately the SLOWER full
         `cargo test` run (~60s cold / ~5s warm per 34.3-VALIDATION.md), not the `cargo check`
         used for per-commit sampling everywhere else in this phase. That is intentional and
         not a Nyquist latency regression: this is the task that CREATES the Cargo tests, so
         `cargo check` would compile them without ever running them, and the whole point of
         D-07 is that this project's CI runs no cargo step at all — if these tests are not
         actually executed here, nothing else in the pipeline will execute them. -->
    <automated>cd src-tauri && cargo test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd src-tauri && cargo test` exits 0 and its summary line reports at least 6 more tests than before this plan
    - `grep -v '^\s*//' src-tauri/src/main.rs | grep -c "fn clipboard_text_arg"` returns 1
    - `grep -v '^\s*//' src-tauri/src/main.rs | grep -c "fn clipboard_read_value"` returns 1
    - Both arms call the helpers (the arm bodies contain `clipboard_text_arg(` and `clipboard_read_value(`)
    - A `#[test]` asserts `clipboard_text_arg(&[json!("")])` is `Ok("")`
    - A `#[test]` asserts `clipboard_read_value(Ok(String::new()))` yields `Value::String` and not `Value::Null`
    - The SUMMARY records two hand-verified RED runs with the exact regression applied and the exact failing test names
  </acceptance_criteria>
  <done>Both arms' non-plugin logic is pure, tested, and hand-RED-proved, with the honest boundary of what a Cargo test can prove stated in the code.</done>
</task>

<task type="auto">
  <name>Task 3: Record the D-05 relaunch-teardown verification as a code comment</name>
  <files>src-tauri/src/main.rs</files>
  <read_first>
    - `src-tauri/src/main.rs` lines 565-585 — the existing `app_exit` and `app_relaunch` arm
      docstrings IN FULL, including the sentence stating `restart()` never returns.
    - `src-tauri/src/main.rs` lines 195-220 — `shutdown_child`'s definition and its WR-03 docstring.
    - `src-tauri/src/main.rs` lines 1030-1050 — the `RunEvent::Exit` handler that calls
      `shutdown_child()`.
    - `src-tauri/src/main.rs` lines 745-880 — `start_reader`, confirming `dispatch_rust_channel` runs
      on a `thread::spawn`'d worker.
    - `.planning/phases/34.3-.../34.3-RESEARCH.md` § "Q1" IN FULL — the evidence chain this comment
      must summarise faithfully.
  </read_first>
  <action>
Add a `//` line-comment block above the existing `"app_relaunch" =>` arm recording the verified
finding, appended to (not replacing) the arm's existing docstring. Content, in the same density and
citation style as the neighbouring arms:

- Phase 34.3 D-05 / REQ-34.3-06 is **RESOLVED with no code change needed.**
- `dispatch_rust_channel` always runs on a `thread::spawn`'d worker (cite `start_reader`), so this
  `app.restart()` call is always `restart()`'s *background-thread* branch — it sets
  `restart_on_exit = true` and requests exit through the real event loop.
- That fires `RunEvent::Exit`, and this file's own `.run(...)` closure calls `shutdown_child()` on
  exactly that event, **before** the process re-execs.
- Therefore there is no orphan-sidecar window for this codebase's calling pattern, and **no
  `state.shutdown_child()` call belongs here** — adding one would be dead code inviting the next
  reader to wonder why it is duplicated.
- Verified against tauri 2.11.5's vendored source (the version `Cargo.lock` pins); see
  `34.3-RESEARCH.md` Q1. `restart()`'s "skip the events" branch applies only on the main thread.
- Empirical confirmation is REQ-34.3-11's live-gate item 4 (packaged build: `resetHeroic` leaves
  exactly ONE sidecar process), **not** a Rust unit test — a `#[cfg(test)]` test structurally cannot
  drive a live winit/tao event loop.
- Note the one residual: `request_exit`'s delivery guarantee inside `tauri-runtime-wry` was not
  traced; it is the same mechanism every existing `app.exit()` call already depends on, and item 4
  is where that residual closes.

**Use `//` line comments only — no `/* */` block comment.** `src/backend/__tests__/tauriShellSource.test.ts`'s
`loadMainRsCode` helper strips comments by dropping lines that start with `//`; a block comment
would survive stripping and could make an existing or new positive-existence gate pass vacuously on
prose alone. Plan 34.3-07 hardens that stripper, but this comment must be safe under the current one.

Do not change the `app_relaunch` arm's body. Do not change the `app_exit` arm.
  </action>
  <verify>
    <automated>cd src/backend && npx jest __tests__/tauriShellSource.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src-tauri/src/main.rs | grep -A2 '"app_relaunch" =>' | grep -c "shutdown_child"` returns 0
    - `git diff src-tauri/src/main.rs` shows the `app_relaunch` arm's body is still exactly `app.restart();`
    - `grep -c "34.3-RESEARCH.md Q1\|34.3-RESEARCH" src-tauri/src/main.rs` returns ≥1
    - `grep -c "/\*" src-tauri/src/main.rs` is unchanged from its pre-plan value of 1
    - `cd src/backend && npx jest __tests__/tauriShellSource.test.ts` exits 0 (the existing WR-03 gate still passes)
    - `cd src-tauri && cargo check --quiet` exits 0
  </acceptance_criteria>
  <done>The verified finding lives in the code where the question would next be asked, with no dead defensive call added and no existing source gate broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview → Rust (Tauri capability-gated IPC) | `capabilities/default.json` is scoped `"windows": ["main"]`, the window that also renders untrusted remote content (store-search today, Humble login in 34.4) with no `<webview>` isolation boundary |
| sidecar → Rust (`rustInvoke`, stdio) | Not capability-gated; args arrive as `serde_json::Value` |
| Rust → OS clipboard | `arboard` via `tauri-plugin-clipboard-manager` |
| crates.io → build | New third-party dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.3-08 | Information Disclosure | renderer-reachable clipboard **read** as a credential-exfiltration primitive | mitigate | ASVS V4: add **zero** `clipboard-manager:*` entries to `capabilities/default.json`. All clipboard access is Rust-side via `app.clipboard()`, unreachable from the webview. Verified safe by 34.3-RESEARCH.md Q2 (the plugin has no `js_init_script`, so no grant is needed to avoid a startup crash). Task 1 asserts `grep -c clipboard capabilities/default.json` returns 0 |
| T-34.3-09 | Tampering | `clipboard_write_text` arg handling | mitigate | ASVS V5: `clipboard_text_arg` validates `args[0]` is a JSON string before use, matching the existing `keyring_*`/`shell_open_path` `.and_then(\|v\| v.as_str()).ok_or(...)` pattern. Task 2 pins all four rejection cases with `#[test]`s and a hand-verified RED proof |
| T-34.3-10 | Information Disclosure | `clipboard_read_text` returning host clipboard contents into the app | accept | Its only sidecar-reachable caller is `clipboardReadText`, whose only frontend consumer is the Epic SID login paste (`SIDLogin/index.tsx:137`) — a user-initiated paste. Electron parity: `clipboard.readText()` is reachable on the same channel today |
| T-34.3-11 | Denial of Service | Linux clipboard-read deadlock if `read_text()` runs on the main thread | mitigate | Satisfied by construction (`dispatch_rust_channel` always runs on a spawned worker). Task 1 requires the safety property be stated in the arm's comment so a future refactor cannot silently regress it |
| T-34.3-12 | Tampering | orphaned sidecar retaining an authenticated Steam session across a relaunch | accept (verified not present) | Q1 verified `RunEvent::Exit` already fires for this call pattern and `shutdown_child()` runs before re-exec. No fix added; Task 3 records the verification; REQ-34.3-11 item 4 (packaged build) closes the residual empirically |
| T-34.3-SC | Tampering | crates.io install of `tauri-plugin-clipboard-manager` | mitigate | `[VERIFIED]` in 34.3-RESEARCH.md's Package Legitimacy Audit: slopcheck `[OK]`, first-party `tauri-apps/plugins-workspace` — the same repo already backing five plugins in this `Cargo.toml`. No `[ASSUMED]`/`[SUS]` package in this plan, so no legitimacy checkpoint is required. Task 1's `js_init_script` grep is the one execution-time re-confirmation (Assumption A2) |
</threat_model>

<verification>
- `cd src-tauri && cargo check --quiet` exits 0
- `cd src-tauri && cargo test` exits 0, with the new clipboard-helper tests present in the output
- `cd src/backend && npx jest __tests__/tauriShellSource.test.ts __tests__/tauriConf.test.ts __tests__/longRunningChannels.test.ts` exits 0
- `cd src/backend && npx jest sidecar/__tests__/lifecycleStub.test.ts` exits 0 (its `RUST_INVOKE_CHANNELS` allowlist test must still pass after the array grows)
- `npx tsc --noEmit` exits 0
- `npx prettier --check` on `src/common/types/sidecarTransport.ts` exits 0
- `git diff --stat src-tauri/capabilities/default.json` is empty
- REQ-34.3-13: `npm start` (Electron) still launches — the Electron build does not load `src-tauri/`, and `sidecarTransport.ts` only gains additive exports
</verification>

<success_criteria>
- `tauri-plugin-clipboard-manager` resolved, registered, and confirmed free of `js_init_script`
- Exactly two new `dispatch_rust_channel` arms, both calling `app.clipboard()`
- Zero capability grants added
- Zero `shutdown_child()` calls added to `app_relaunch`
- Two pure helpers with 10 behavioral `#[test]`s and two hand-verified RED proofs recorded in the SUMMARY
- Both channel constants declared AND present in `RUST_INVOKE_CHANNELS`
</success_criteria>

<output>
Create `.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-03-SUMMARY.md` when done
</output>
