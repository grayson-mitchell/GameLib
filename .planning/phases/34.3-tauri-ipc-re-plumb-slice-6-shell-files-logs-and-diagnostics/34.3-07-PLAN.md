---
phase: 34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics
plan: 07
type: execute
wave: 4
depends_on: ["34.3-01", "34.3-02", "34.3-03", "34.3-04", "34.3-06"]
files_modified:
  - src/backend/sidecar/__tests__/electronReachLedger.test.ts
  - src/backend/__tests__/tauriShellSource.test.ts
autonomous: true
requirements: [REQ-34.3-08, REQ-34.3-10, REQ-34.3-13]
must_haves:
  truths:
    - "The committed electron-reach baseline covers this slice's entry points, so it remains a truthful Phase 35 cutover work-list"
    - "A newly-added electron-importing module in this slice's reach graph fails the ledger loudly instead of slipping in silently"
    - "Deleting the Rust clipboard test module during a later refactor fails a jest gate"
    - "The main.rs source gate cannot pass vacuously on comment prose"
  artifacts:
    - path: "src/backend/sidecar/__tests__/electronReachLedger.test.ts"
      provides: "ENTRY_POINTS extended with this slice's registration modules and a regenerated baseline"
      contains: "shellFilesFlowRegistration"
    - path: "src/backend/__tests__/tauriShellSource.test.ts"
      provides: "REQ-34.3-08 source-existence gate for the clipboard arms + helpers + Cargo test module, over a block-comment-safe stripper"
      contains: "clipboard_text_arg"
  key_links:
    - from: "src/backend/sidecar/__tests__/electronReachLedger.test.ts ENTRY_POINTS"
      to: "src/backend/logger/uploader.ts"
      via: "loggerFlowRegistration -> logger/uploader -> import { app } from 'electron' — the new two-hop edge D-10 names"
      pattern: "loggerFlowRegistration"
    - from: "src/backend/__tests__/tauriShellSource.test.ts loadMainRsCode"
      to: "src/backend/testUtils/stripSourceComments.ts"
      via: "block-comment stripping layered under the existing line-prefix filter"
      pattern: "stripSourceComments"
---

<objective>
Close the two automated-proof requirements that depend on every other code plan being finished:

1. **REQ-34.3-10** — extend `electronReachLedger.test.ts` with this slice's entry points and
   **regenerate** the committed baseline by running the tooling.
2. **REQ-34.3-08 (jest half)** — add a source-existence gate pinning the continued existence of the
   Rust clipboard arms, their two pure helpers, and their `#[cfg(test)]` tests, and harden the gate's
   comment-stripper so the new positive assertions cannot pass on prose.

Purpose (ledger): `logger/uploader.ts:1` does `import { app } from 'electron'`, so porting the log
channels adds a module to the electron-reach graph. `ENTRY_POINTS` currently lists only 34.2's four
game-details files, yet the ledger's own docstring states its committed baseline **"IS the Phase 35
cutover work-list"** — a claim that only holds if it covers the whole ported surface.

Purpose (Rust gate): this project's CI runs **no cargo step at all** — `.github/workflows/*.yml`
contains neither `cargo test` nor `cargo check`. Adding one is deliberately deferred (it is the
highest-value infrastructure fix available and this slice is its natural forcing function, but it is
CI work riding inside a port slice). So the Rust proof story is: hand-run `cargo test` with the RED
proof recorded in plan 34.3-03's SUMMARY, plus this jest gate pinning that the test module still
exists. The gate cannot and does not prove the tests still pass — only `cargo test` does. Say so in
the gate's own docstring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-CONTEXT.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-PATTERNS.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-03-SUMMARY.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. -->

`electronReachLedger.test.ts` structure (verified):
- `ENTRY_POINTS: string[]` at L44-49 — currently 4 absolute `join(REPO_ROOT, ...)` paths
- `BASELINE_ELECTRON_REACHING_MODULES: string[]` at L57-87 — 29 sorted repo-relative paths, preceded
  by a comment recording that it was regenerated at plan-execution time
- `computeElectronReach(ENTRY_POINTS, options): ElectronReachResult` with
  `{ visitedFiles: Set<string>, electronImportingFiles: Set<string> }`
- four tests: the growth tripwire (L250), the anti-degradation test with a `requiredModules` list
  (L273), the reachability-sanity test asserting `visitedFiles.size > 100` (L295), and the pinned
  gap-#3 `dispatch.ts → dialog.ts` edge test (L303)
- semantics, quoted from its own docstring: *"This is deliberately a growth-only (subset) tripwire,
  not a strict-equality pin… The committed `BASELINE_ELECTRON_REACHING_MODULES` array below IS the
  Phase 35 cutover work-list."*

`src/backend/__tests__/tauriShellSource.test.ts` structure (verified):
- `MAIN_RS_PATH` at L21-29, `loadMainRsCode()` at L38-45, then six describe blocks including
  `loadMainRsCode comment-stripping helper (self-test)` (L48), the WR-01 dispatch gate (L56), the
  WR-03 lifecycle gate (L74), and three REQ-34.1-07 tray gates
- its `loadMainRsCode` today drops lines whose trimmed form starts with `//`, then applies a naive
  `line.replace(/\/\/.*$/, '')` trailing pass. **It does not strip `/* */` block comments at all.**

`src/backend/testUtils/stripSourceComments.ts:38` — `export function stripSourceComments(source: string): string`,
the single shared implementation from quick task 260726-q8f. Two-stage: block comments first
(`/\/\*[\s\S]*?\*\//g`), then the line-prefix filter UNCHANGED. Its docstring records why the naive
trailing-comment regex was removed as WR-08 (it truncates code lines containing a quoted `"steam://…"`)
and states that a caller needing trailing-comment stripping may layer a LOCAL pass on top of this
util's output — which is exactly what `loadMainRsCode` needs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ENTRY_POINTS and regenerate the electron-reach baseline</name>
  <files>src/backend/sidecar/__tests__/electronReachLedger.test.ts</files>
  <read_first>
    - `src/backend/sidecar/__tests__/electronReachLedger.test.ts` — READ IN FULL, especially the
      36-line header docstring (growth-only/subset semantics, why depth-1 gates are insufficient, the
      regenerate-at-execution-time rule, and the anti-vacuity tests) and all four test bodies.
    - `src/backend/sidecar/shellFilesFlowRegistration.ts`, `src/backend/sidecar/clipboardFlowRegistration.ts`,
      `src/backend/sidecar/loggerFlowRegistration.ts` — the three modules becoming entry points.
    - `src/backend/logger/uploader.ts` line 1 — the `import { app } from 'electron'` that is the new edge.
  </read_first>
  <action>
Add three absolute paths to `ENTRY_POINTS`, in the same `join(REPO_ROOT, ...)` style:
`src/backend/sidecar/shellFilesFlowRegistration.ts`,
`src/backend/sidecar/clipboardFlowRegistration.ts`,
`src/backend/sidecar/loggerFlowRegistration.ts`.

**Regenerate `BASELINE_ELECTRON_REACHING_MODULES` by RUNNING THE TOOLING, not by transcribing any
list from a planning document.** That is the ledger's own written instruction and it is why the
existing array carries a "regenerated at plan-execution time" comment. Concretely: run the suite
with a temporary `console.log([...reachResult.electronImportingFiles].sort())` (or an equivalent
one-off script that calls the same `computeElectronReach`), capture the real measured set, sort it,
and commit that. Remove the temporary logging before finishing. Record in the SUMMARY: the exact
count before and after, and the exact list of newly-added paths — a diff a reviewer can check.

For **each** newly-added baseline path, append an inline comment naming why it entered the graph and
which entry point pulled it in (the array's existing convention). `src/backend/logger/uploader.ts` is
the expected new edge (D-10 names it); if the measured set contains additional new paths, that is
information, not a failure — add them with their reason. If a path you expected does NOT appear,
investigate before committing: a resolver that silently stops traversing would make the growth
tripwire pass vacuously, which is the exact failure mode this file's anti-vacuity tests exist to
rule out.

Update the comment above the array to record the new regeneration date and that Phase 34.3 Plan 07
extended it, keeping the existing note about the accepted 192-vs-194 file-count discrepancy.

Preserve all four existing tests and their semantics:
- the growth tripwire stays a **subset** assertion (never convert it to strict equality — Phase 35 is
  expected to shrink this set and a strict pin would go red on every legitimate improvement)
- extend the anti-degradation test's `requiredModules` list with at least `src/backend/logger/uploader.ts`
  so the new edge itself is anti-vacuity-protected
- re-check the reachability-sanity threshold: `visitedFiles.size` will grow with three new entry
  points, so `toBeGreaterThan(100)` still holds; do **not** lower it, and consider whether raising it
  to a value comfortably below the new measured size makes the guard stronger — if you raise it,
  record the measured size in the comment so the number is not a mystery later
- leave the pinned `dispatch.ts → dialog.ts` gap-#3 test untouched
  </action>
  <verify>
    <automated>cd src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FlowRegistration" src/backend/sidecar/__tests__/electronReachLedger.test.ts` shows all three new entry points present in `ENTRY_POINTS` (7 entries total)
    - `BASELINE_ELECTRON_REACHING_MODULES` contains `src/backend/logger/uploader.ts`
    - The anti-degradation `requiredModules` list contains `src/backend/logger/uploader.ts`
    - The growth tripwire test still uses a `filter`/subset check, not `toEqual` on the whole sorted set
    - `cd src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts` exits 0
    - No `console.log` remains in the file (`grep -c "console.log" ` returns 0)
    - The SUMMARY records the before/after counts and the exact newly-added paths
  </acceptance_criteria>
  <done>The committed baseline is a measured fact covering this slice's whole ported surface, with every new entry annotated and the growth-only semantics intact.</done>
</task>

<task type="auto">
  <name>Task 2: Harden loadMainRsCode against block comments</name>
  <files>src/backend/__tests__/tauriShellSource.test.ts</files>
  <read_first>
    - `src/backend/__tests__/tauriShellSource.test.ts` lines 1-60 — `MAIN_RS_PATH`, `loadMainRsCode`,
      and the existing self-test describe block whose shape the new self-test must match.
    - `src/backend/testUtils/stripSourceComments.ts` — READ IN FULL, especially the docstring's
      two-stage ordering rationale, the WR-08 warning about the naive trailing regex, and the explicit
      permission for a caller to layer a LOCAL trailing pass on top of this util's output.
    - `src/backend/__tests__/longRunningChannels.test.ts` lines 24-80 — the sibling main.rs gate with
      the same local stripper, and its `loadMainRsCode(source?)` parameterisation that lets self-tests
      drive the real code path with synthetic input instead of reimplementing the algorithm.
  </read_first>
  <action>
Change `tauriShellSource.test.ts`'s `loadMainRsCode` to:
- accept an optional `source?: string` parameter (mirroring `longRunningChannels.test.ts`'s helper) so
  self-tests drive the real code path with synthetic input
- call `stripSourceComments` imported from `../testUtils/stripSourceComments` FIRST — this is what
  removes `/* */` block comments, whose interior lines do not themselves start with a comment marker
  and therefore survive the current line-prefix filter entirely
- then apply the existing local trailing-`//` pass on that output, keeping it local per the shared
  util's own instruction. Add a comment recording the accepted WR-08 tradeoff: this local pass can
  truncate a code line containing a quoted `//`-bearing string literal, which is acceptable here
  because none of this file's assertions match against such a literal — and if a future assertion
  does, the pass must be reconsidered rather than the assertion weakened.

Update `loadMainRsCode`'s docstring to state the two-stage order and why block-comment stripping is
now load-bearing: Phase 34.3 adds positive-existence assertions (`fn clipboard_text_arg` etc.) whose
tokens also appear in `main.rs` prose, and this same file's existing WR-03 gate positively asserts
`RunEvent::Exit` / `.kill()` / `try_state::<Arc<SidecarState>>` — all of which appear in comments too.

Extend the existing `loadMainRsCode comment-stripping helper (self-test)` describe with two new
cases that drive the helper with synthetic `source`:
- a `/* … */` block comment whose interior line contains `fn clipboard_text_arg` does NOT survive
  stripping (this is the case the pre-change helper fails — state that in the test's own comment as
  the RED direction)
- a `//`-prefixed line naming `clipboard_write_text` does NOT survive stripping (regression guard for
  the retained line filter)

Keep the existing self-test case unchanged.
  </action>
  <verify>
    <automated>cd src/backend && npx jest __tests__/tauriShellSource.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stripSourceComments" src/backend/__tests__/tauriShellSource.test.ts` returns ≥2 (import + call)
    - `loadMainRsCode` accepts an optional `source` parameter
    - A self-test proves a block comment containing `fn clipboard_text_arg` is stripped
    - A self-test proves a `//` line containing `clipboard_write_text` is stripped
    - The original self-test case (`Kept alive so the child is not reaped`) still exists and passes
    - All six pre-existing describe blocks still pass
    - `cd src/backend && npx jest __tests__/tauriShellSource.test.ts` exits 0
  </acceptance_criteria>
  <done>The gate's stripper handles both comment forms and proves it does, so Task 3's positive assertions cannot pass on prose.</done>
</task>

<task type="auto">
  <name>Task 3: Add the REQ-34.3-08 source-existence gate for the Rust clipboard work</name>
  <files>src/backend/__tests__/tauriShellSource.test.ts</files>
  <read_first>
    - `src/backend/__tests__/tauriShellSource.test.ts` — the file as edited by Task 2, including its
      `REQ-34.1-07 main.rs tray_set_icon dispatch arm` describe block (L129-174), which is the exact
      analog: a dispatch-arm existence gate with a stated scope boundary.
    - `src/backend/__tests__/longRunningChannels.test.ts` lines 1-30 — the docstring convention for
      stating what a source gate does and does NOT prove, and the division of responsibility between
      the JS contents gate and the Rust behavioral tests.
    - `src-tauri/src/main.rs` — the arms, the two pure helpers, and the `#[cfg(test)]` test function
      names added by plan 34.3-03. The gate must pin the ACTUAL names, so read them rather than
      guessing.
  </read_first>
  <action>
Add a `describe('REQ-34.3-08 main.rs clipboard seam (Phase 34.3 Plan 03, D-01/D-02/D-07)')` block to
`src/backend/__tests__/tauriShellSource.test.ts`, with every assertion run against
`loadMainRsCode()`'s comment-stripped output:

Positive-existence assertions:
- the stripped code contains `"clipboard_write_text" =>` and `"clipboard_read_text" =>`
- the stripped code contains `fn clipboard_text_arg` and `fn clipboard_read_value`
- the stripped code contains `tauri_plugin_clipboard_manager::init()` and
  `use tauri_plugin_clipboard_manager::ClipboardExt`
- the stripped code contains a literal `fn` declaration for **each** `#[test]` function plan 34.3-03
  added (read their real names from `main.rs` and list them in a local constant array in the test
  file, asserting each is present) — this is what pins the Cargo test module's continued existence
  against a later refactor deleting it

Negative-scope assertions, each with a comment stating what it protects:
- the stripped code does NOT contain a `shutdown_child()` call inside the `app_relaunch` arm. Assert
  this narrowly (e.g. a regex over the `"app_relaunch" =>` arm's own body, or an assertion that the
  file contains exactly ONE `shutdown_child()` call site), not as a blanket substring check —
  `shutdown_child` legitimately exists as a method and is legitimately called from the
  `RunEvent::Exit` handler. State in the comment that REQ-34.3-06 resolved to no-fix.
- `src-tauri/capabilities/default.json` contains no `clipboard` string. Read that file in the test
  and assert it, so the zero-capability-grant stance (T-34.3-08, the ASVS V4 control) is pinned by a
  test rather than only by a plan sentence.

Add a describe-level docstring stating plainly, in `longRunningChannels.test.ts`'s voice: this gate
proves CONTINUED EXISTENCE of the Rust source shapes, not that the Cargo tests still pass; this
project's CI runs no cargo step at all, so `cd src-tauri && cargo test` is hand-run and its
hand-verified RED proof is recorded in `34.3-03-SUMMARY.md`; adding a cargo step to CI is a
deliberately deferred idea, not an oversight.
  </action>
  <verify>
    <automated>cd src/backend && npx jest __tests__/tauriShellSource.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/backend && npx jest __tests__/tauriShellSource.test.ts` exits 0
    - The new describe block contains ≥8 passing cases
    - A local constant array in the test file lists the real `#[test] fn` names from `main.rs` and each is asserted present
    - An assertion pins that `shutdown_child()` has exactly one call site (the `RunEvent::Exit` handler) and is not inside the `app_relaunch` arm
    - An assertion reads `src-tauri/capabilities/default.json` and proves it contains no `clipboard` string
    - Deleting the `#[cfg(test)]` clipboard tests from `main.rs` makes this suite RED — verify by temporarily deleting one `#[test] fn`, observing the failure, and reverting; record the observed failure in the SUMMARY
  </acceptance_criteria>
  <done>The Rust clipboard seam's shape, its test module's existence, and the zero-capability-grant stance are all pinned by an automated, non-vacuous gate with a RED proof recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → CI/test gates | These gates are the only automated defence for Rust source shape and for the electron-reach surface Phase 35 depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.3-25 | Repudiation | a vacuous source gate passing on comment prose, so a deleted Rust test or a removed arm goes unnoticed | mitigate | Task 2 layers the shared block-comment stripper under the existing line filter and proves both forms are stripped with synthetic-input self-tests; Task 3 requires a hand-verified RED proof by deleting a `#[test] fn` |
| T-34.3-26 | Repudiation | a resolver that silently stops traversing making the ledger's growth tripwire pass against an empty/undersized set | mitigate | The four existing anti-vacuity tests are preserved; the anti-degradation `requiredModules` list gains the new edge; Task 1 forbids committing a baseline that lacks an expected path without investigating |
| T-34.3-27 | Tampering | the electron-reach baseline being transcribed from planning prose instead of measured, making the Phase 35 work-list fiction | mitigate | Task 1 requires regeneration by running `computeElectronReach`, with before/after counts and the exact added paths recorded in the SUMMARY |
| T-34.3-28 | Information Disclosure | a future `clipboard-manager:*` capability grant slipping into `default.json` unnoticed and exposing clipboard read to untrusted remote content | mitigate | Task 3 pins the absence of any `clipboard` string in `capabilities/default.json` as an automated assertion |
| T-34.3-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages |
</threat_model>

<verification>
- `cd src/backend && npx jest sidecar/__tests__/electronReachLedger.test.ts __tests__/tauriShellSource.test.ts` exits 0
- `cd src/backend && npx jest` — full backend suite; no previously-green suite goes red
- `cd src/backend && npx jest __tests__/longRunningChannels.test.ts __tests__/tauriConf.test.ts __tests__/stripSourceComments.test.ts` exits 0 (the sibling gates and the shared util's own suite)
- `npx tsc --noEmit` exits 0
- `npx prettier --check` on both touched files exits 0
- The SUMMARY records: baseline count before/after, the added paths, and the observed RED failure from deleting a `#[test] fn`
</verification>

<success_criteria>
- `ENTRY_POINTS` has 7 entries; baseline regenerated by running the tooling and every new path annotated
- Growth-only/subset semantics and all four anti-vacuity tests preserved
- `loadMainRsCode` strips block comments via the shared util, proven by self-tests
- REQ-34.3-08 gate pins both arms, both helpers, the plugin registration, every new `#[test] fn` name, the single `shutdown_child()` call site, and the empty clipboard capability set
</success_criteria>

<output>
Create `.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-07-SUMMARY.md` when done
</output>
