---
phase: 34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics
plan: 08
type: execute
wave: 5
depends_on: ["34.3-01", "34.3-02", "34.3-03", "34.3-04", "34.3-05", "34.3-06", "34.3-07"]
files_modified:
  - .planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-PORTED-CHANNELS.md
  - .planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/ported-channels-gate.py
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
  - .planning/todos/pending/uploaded-log-delete-button-lies.md
  - .planning/todos/pending/log-upload-has-no-redaction.md
autonomous: true
requirements: [REQ-34.3-12, REQ-34.3-13]
must_haves:
  truths:
    - "A reader can see all 29 ported channels, their port kind, and their honest proof level in one artifact"
    - "Every accepted risk and declared-dead path in this slice is written down, not left in a reviewer's head"
    - "SEAM.md's deferred backlog no longer lists clipboard as untouched, and §1 records the closed cluster"
    - "The two out-of-scope defects are filed as todos so they survive this phase"
    - "A future edit that drops a required declaration from the artifact fails a re-runnable gate"
  artifacts:
    - path: ".planning/phases/34.3-.../34.3-PORTED-CHANNELS.md"
      provides: "The declared ported-channel list for slice 6 — SEAM Incremental-Port Checklist step 5"
      min_lines: 90
    - path: ".planning/phases/34.3-.../ported-channels-gate.py"
      provides: "Re-runnable doc-shape gate asserting every required row and rider is present"
      min_lines: 60
  key_links:
    - from: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md §3 row 9"
      to: "§1 'Shell, files, logs and diagnostics cluster (real, Phase 34.3) — CLOSED, moved out of §3'"
      via: "Incremental-Port Checklist step 5's row move"
      pattern: "Phase 34.3\\) — CLOSED"
    - from: ".planning/phases/34.3-.../ported-channels-gate.py"
      to: "34.3-PORTED-CHANNELS.md"
      via: "non-zero exit when a required channel row or rider token is missing"
      pattern: "sys.exit"
---

<objective>
Produce the slice's declared artifact and close SEAM.md's Incremental-Port Checklist steps 5 and 6:
`34.3-PORTED-CHANNELS.md` with all 29 rows, a re-runnable doc-shape gate, the SEAM §3→§1 row move,
and the two out-of-scope defects filed as todos.

Purpose: this is REQ-34.3-12, not optional bookkeeping. The 34.1/34.2 declared lists exist because
"wired and unit-proven" was once conflated with "seen working" — the conflation that let G-30-02 be
declared fixed twice while the live build hung. Every row here states its honest proof level, and
every accepted risk is written down where a Phase 35 reader will find it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/IPC-PORT-INVENTORY.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-CONTEXT.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-PATTERNS.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-01-SUMMARY.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-02-SUMMARY.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-03-SUMMARY.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-04-SUMMARY.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-05-SUMMARY.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-06-SUMMARY.md
@.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-07-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. -->

The authoritative 29-channel list — `IPC-PORT-INVENTORY.md` § "Phase 34.3 — Slice 6":
`checkDiskSpace`, `clearAchievementCache`, `clearCache`, `clipboardReadText`, `clipboardWriteText`,
`copySystemInfoToClipboard`, `deleteUploadedLogFile`, `getShellPath`, `getUploadedLogFiles`,
`logInfo`, `openDiscordLink`, `openExternalUrl`, `openFolder`, `openGithubSponsorsPage`,
`openKofiPage`, `openLoginPage`, `openPatreonPage`, `openSidInfoPage`, `openSupportPage`,
`openWeblate`, `openWikiLink`, `openWinePrefixFAQ`, `pathExists`, `removeFolder`, `resetHeroic`,
`showConfigFileInFolder`, `showItemInFolder`, `showLogFileInFolder`, `uploadLogFile`.

**`logError` is NOT one of the 29** — it was ported early by 34.2 gap plan 34.2-16. The inventory
carries this warning explicitly. Do not add it as a row.

Port-kind vocabulary, completed by 34.1 and used unchanged: `sidecar send`, `sidecar invoke`,
`rustInvoke`, `renderer-side (Tauri JS)`.

Analogs to copy:
- `.planning/phases/34.2-.../34.2-PORTED-CHANNELS.md` — structure, the L1-16 honest-bookkeeping prose
  framing, and the L48-60 table row shape
- `.planning/phases/34.2-.../currency-gate.py` — READ ITS L1-30 DOCSTRING. Copy the "extend the token
  lists, never delete an assertion to make the gate pass" discipline and the fail-loud structure. Do
  NOT copy its per-gap-cycle numbering scaffolding — this phase has no gap cycle yet.
- `SEAM.md` §1 headings at ~L322 (`App shell + window chrome cluster (real, Phase 34.1) — CLOSED,
  moved out of §3`) and ~L381 (the 34.2 equivalent), and §3's row 9 at ~L474 whose "untouched" list
  currently includes `clipboard`.
- `.planning/todos/pending/*.md` — todo file format: frontmatter (`created`, `title`, `area`, `files`)
  then `## Problem` / `## Solution`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 34.3-PORTED-CHANNELS.md with all 29 rows and every rider</name>
  <files>.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-PORTED-CHANNELS.md</files>
  <read_first>
    - `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-PORTED-CHANNELS.md`
      lines 1-70 — the prose framing and the table row shape.
    - `.planning/IPC-PORT-INVENTORY.md` § "Phase 34.3 — Slice 6" and its `logError` warning note.
    - All seven prior SUMMARYs in this phase directory — the proof level of each channel must come
      from what the SUMMARYs actually recorded, not from what this plan predicted.
    - `.planning/phases/34.3-.../34.3-CONTEXT.md` § "Specific Ideas" — the two framing corrections
      this artifact must honor.
  </read_first>
  <action>
Create `.planning/phases/34.3-.../34.3-PORTED-CHANNELS.md`.

Opening prose section (mirroring 34.2's framing) must state:
- 15 of the 29 channels are near-free — they reduce to `openUrlOrFile`/`showItemInFolder` in
  `utils.ts`, already in the sidecar graph, reaching Rust arms that already existed. A reader who
  budgets this slice as "29 delegations" has misread it in the opposite direction from 34.2's warning.
- The slice's real content is three things: a new clipboard platform seam, a JS-side relaunch/quit
  ordering fix, and this series' **first blocking live gate**.
- **Framing correction 1:** the clipboard no-op was **NOT a latent shipped defect.** Its only backend
  consumers are this slice's own three channels, so nothing had been silently degrading in
  production. It is the direct descendant of Phase 31 D-04, which chose a logged no-op and wrote
  "deferred to Phase 33"; Phase 33 never collected it. This slice closes a two-phase-old promise —
  completion, not churn. Do not write a 34.2-style "already broken in production" narrative.
- **Framing correction 2:** `resetHeroic` is **narrower than its UI copy claims.** It deletes
  `config.json` + `GamesConfig/` only; the Steam token + library store, all 10 Humble stores, the
  download manager, and the Wine and wiki/crossover caches all survive as siblings in `appFolder`.
  The dialog's "removes all Settings and Caching" copy already overclaims in Electron — inherited,
  out-of-scope UI copy, not a defect this slice introduced.
- **D-05 resolved to NO FIX.** Research verified (HIGH confidence, direct read of tauri 2.11.5 as
  pinned by `Cargo.lock`) that `AppHandle::restart()` fires `RunEvent::Exit` for this codebase's
  worker-thread dispatch pattern, so `shutdown_child()` already runs before the process re-execs.
  The orphan sidecar was an inference, not a measured defect. State that plainly — do not present it
  as an established defect that was fixed.
- **Why this slice has a live gate when 34.1/34.2 did not.** State the difference specifically rather
  than inheriting wording: 34.2 could honestly claim "data in / data out with assertable return
  shapes"; most of this slice's channels' entire purpose is an observable OS side effect jest cannot
  see, and it also ships two Rust arms CI never compiles plus a destructive channel whose failure
  mode is only visible in the process table.

Then a table with **exactly 29 rows**, columns `Channel | Kind | Backed by | Proof level | Notes`.
Proof level must be one of `unit`, `unit + LIVE (D-11 item N)`, or `ported at parity — declared
caveat`, and must reflect what the SUMMARYs actually recorded. Required per-row content:
- the 12 link openers: note that they share the one `openUrlOrFile` code path and that live-gate item
  1 proves that path once (this is why the gate is 5 items, not a 29-channel UAT)
- `showItemInFolder` / `showLogFileInFolder` / `showConfigFileInFolder`: reveal path, item 2
- `clipboardWriteText`: `unit + LIVE (item 3)`; note that write failures are **log-only at Electron
  parity** (D-03) and that the **Humble key-copy silent-failure risk is a KNOWN ACCEPTED RISK, not
  an oversight** — name `HumbleClaimWizard/index.tsx:120,366`
- `clipboardReadText`: `unit + LIVE (item 3)`; note that the sidecar handler awaits Rust directly and
  that `electronStub.clipboard.readText` remains a **documented, deliberately-dead** sync no-op (D-04)
- `copySystemInfoToClipboard`: note the curated `utils/systeminfo` import
- `resetHeroic`: `unit + LIVE (packaged build, item 4)`; note the real blast radius and that the
  relaunch/quit ordering guard lives in `electronStub`, not in `utils.ts`
- `clearCache`: note the dialog rides the existing `showDialog` frontend push with **no fabricated
  event**, and that a dialog failure is logged and non-fatal
- `deleteUploadedLogFile`: **"ported" does NOT mean "delete works"** — hardcoded `token = '1'`
  (`uploader.ts:74-77`) means it cannot delete anything **in EITHER build**; equally dead pre-existing
  upstream Heroic defect, not port-introduced (explicitly distinguish from 34.2 D-07, which was dead
  only under Tauri); the live UI button is filed as a separate todo
- `uploadLogFile`: `unit + LIVE (item 5)`; note the **filed-not-audited** log-redaction security todo
  (D-09) and that the user explicitly declined even a bounded audit
- `getUploadedLogFiles`: note its expiry-pruning side effect on a read channel, preserved at parity
- `checkDiskSpace`: note the zod `Path.parse` input validation is preserved (ASVS V5)
- `removeFolder`: note the two-element-array argument shape

Then a short "Accepted risks and declared caveats" section listing, as numbered items: the Humble
key-copy silent failure; `deleteUploadedLogFile`'s both-builds deadness; the `readText` documented-dead
stub; the filed-not-audited log redaction; and `resetHeroic`'s overclaiming dialog copy.

Then an "Automated vs live proof boundary" section stating what the jest suites prove (dispatch,
argument shape, registration kind, failure-mode non-fatality) and what only the live gate proves (a
browser actually opened, Finder actually revealed, the clipboard actually round-tripped, exactly one
sidecar process survives a reset, a real dpaste URL comes back). No reader may take "wired and
unit-proven" for "seen working."
  </action>
  <verify>
    <automated>test $(grep -c '^| `' .planning/phases/34.3-*/34.3-PORTED-CHANNELS.md) -eq 29</automated>
  </verify>
  <acceptance_criteria>
    - The table has exactly 29 channel rows and every channel from the inventory list appears exactly once
    - `grep -c 'logError' .planning/phases/34.3-*/34.3-PORTED-CHANNELS.md` — any occurrence is prose stating it is NOT in this slice; there is no `logError` table row
    - The file contains the phrase `NOT a latent shipped defect` (or equivalent explicit wording) about the clipboard no-op
    - The file states `resetHeroic` deletes `config.json` + `GamesConfig/` only
    - The file states D-05 resolved to no fix and names tauri 2.11.5
    - The file contains a `KNOWN ACCEPTED RISK` rider for the Humble key copy
    - The file states `deleteUploadedLogFile` is dead in EITHER build and distinguishes it from 34.2 D-07
    - The file states log redaction is filed and NOT audited
    - Every row's proof level is one of the three permitted values
  </acceptance_criteria>
  <done>All 29 channels are declared with honest proof levels, and every accepted risk and framing correction this phase owes is written down.</done>
</task>

<task type="auto">
  <name>Task 2: Write the re-runnable doc-shape gate</name>
  <files>.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/ported-channels-gate.py</files>
  <read_first>
    - `.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/currency-gate.py`
      — READ IN FULL. Copy its maintenance-instruction docstring discipline and its fail-loud
      structure; do not copy its gap-cycle numbering scaffolding.
    - `.planning/phases/34.3-.../34.3-PORTED-CHANNELS.md` — the artifact under test, as written in Task 1.
    - `.planning/IPC-PORT-INVENTORY.md` § "Phase 34.3 — Slice 6" — the 29 channel names the gate's
      constant list must match.
  </read_first>
  <action>
Create `.planning/phases/34.3-.../ported-channels-gate.py`, an executable python3 script that exits
non-zero with a loud message when `34.3-PORTED-CHANNELS.md` loses a required declaration.

Assertions:
1. All 29 channel names from a module-level `CHANNELS` constant appear as table rows (match on the
   row-leading backticked name, not a bare substring — a channel name mentioned only in prose must
   not satisfy the row assertion).
2. Exactly 29 channel rows exist — a 30th row or a missing row both fail. This is the guard against
   `logError` being re-added or a channel being quietly dropped.
3. `logError` does **not** appear as a table row.
4. Every required rider token appears, from a `REQUIRED_RIDER_TOKENS` constant: the clipboard
   not-a-latent-defect framing, the Humble key-copy accepted risk, `deleteUploadedLogFile`'s
   both-builds deadness, the `readText` documented-dead stub, the filed-not-audited redaction todo,
   `resetHeroic`'s narrow blast radius, and D-05's resolved-no-fix finding.
5. Every row's proof-level cell is one of the three permitted values (parse the 4th column).
6. **Anti-vacuity self-tests**, run when the script is invoked with `--self-test`: feed each assertion
   a synthetic document that violates it and confirm the assertion fails. A gate that cannot be shown
   to reject a bad input is not a gate — this is the lesson from 34.2 gap cycle 4's round-4 blocker
   (14 comment strippers that all passed vacuously). Run the assertions against the synthetic input
   through the SAME code path, never a reimplementation.

Docstring must state, in `currency-gate.py`'s voice: what the gate is for, that the correct
maintenance action when it fails is to restore the missing declaration or extend the token list —
**never to delete an assertion to make it pass** — and that if a rider genuinely no longer needs
tracking, its token is removed in the SAME commit that removes the prose discussing it, so the two
cannot drift.

Run both `python3 ported-channels-gate.py` and `python3 ported-channels-gate.py --self-test` and
record both outputs in the SUMMARY.
  </action>
  <verify>
    <automated>python3 .planning/phases/34.3-*/ported-channels-gate.py && python3 .planning/phases/34.3-*/ported-channels-gate.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .planning/phases/34.3-*/ported-channels-gate.py` exits 0
    - `python3 .planning/phases/34.3-*/ported-channels-gate.py --self-test` exits 0 and reports one passing self-test per assertion
    - The script's `CHANNELS` constant has exactly 29 entries matching the inventory list
    - An assertion fails if a 30th row is added (proven by the self-test)
    - An assertion fails if any `REQUIRED_RIDER_TOKENS` entry is removed from the artifact (proven by the self-test)
    - The docstring contains the never-delete-an-assertion instruction
  </acceptance_criteria>
  <done>The declared list is protected by a gate that has been shown to reject the inputs it exists to reject.</done>
</task>

<task type="auto">
  <name>Task 3: Move SEAM.md's rows §3→§1 and file the two out-of-scope todos</name>
  <files>.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md, .planning/todos/pending/uploaded-log-delete-button-lies.md, .planning/todos/pending/log-upload-has-no-redaction.md</files>
  <read_first>
    - `.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` § "Incremental-Port Checklist" steps
      1-6 (L629-656) — the six steps this phase must have executed, steps 5 and 6 being this task.
    - `SEAM.md` § 1 — the Phase 34.1 and Phase 34.2 CLOSED cluster subsections (~L322 and ~L381):
      their heading format, intro, channel-kind breakdown, and accepted-gap riders.
    - `SEAM.md` § 3 row 9 (~L474) — the deferred backlog row whose "untouched" list includes `clipboard`.
    - `SEAM.md` § "Load-Bearing Invariants" A and B (L660-700) — confirm nothing this slice did
      weakens either, and say so in the new §1 subsection.
    - `.planning/todos/pending/tray-dark-light-icons-are-identical.md` — a short todo in this
      project's exact format, to copy the frontmatter and section shape.
    - `.planning/IPC-PORT-INVENTORY.md` § "Phase 34.3 — Slice 6" and `.planning/ROADMAP.md`'s Phase
      34.3 section — confirm both already read 29 (the ROADMAP's stale "30" was corrected at plan
      time); if either still says 30, correct it.
  </read_first>
  <action>
In `SEAM.md`:
- Add a §1 subsection `### Shell, files, logs and diagnostics cluster (real, Phase 34.3) — CLOSED,
  moved out of §3`, populated with the same structure the 34.1/34.2 subsections use: a short intro
  naming the 29 channels and the three new pieces (clipboard seam, relaunch/quit guard, first
  blocking live gate); a channel-kind breakdown (counts of `sidecar send` vs `sidecar invoke`, and
  the two new `rustInvoke` arms); the accepted-gap riders (`deleteUploadedLogFile`'s both-builds
  deadness, the Humble clipboard-write-failure risk, the `readText` documented-dead stub, the
  filed-not-audited redaction todo); and a sentence confirming Invariants A and B still hold —
  specifically that no channel outside this slice's 29 gained a registration, so every unported
  channel still rejects non-fatally.
- Edit §3 row 9 to remove `clipboard` from its "untouched" list, leaving `protocol` and `ipcMain`
  (remaining) — both unaffected by this slice. Do not delete the row.
- Add a line to the new §1 subsection recording that checklist step 3 (giving an Electron API real
  behavior bound to a real Tauri command rather than leaving a silent no-op) was executed for
  `clipboard`, and that step 4 (new persisted config) was a no-op because `uploadedLogFileStore` and
  `gogAchievementStore` were already registered by Phase 29 D-15.

File two todos in `.planning/todos/pending/`, each in this project's format (frontmatter with
`created`, `title`, `area`, `files`, then `## Problem` / `## Solution`):

`uploaded-log-delete-button-lies.md` — area `logs`. Problem: `UploadedLogFilesList/index.tsx:60`
wires a live delete button to `deleteUploadedLogFile`, but `logger/uploader.ts:74-77` hardcodes
`const token = '1'` with the comment "dpaste.com does not support deleting files… I'll hide the
delete option" — the option was never hidden. The channel POSTs a bogus token and either fails or
falsely reports success while the paste stays public. **Affects both builds** — inherited upstream
Heroic defect, not port-introduced (Phase 34.3 ported it at parity and declared it, D-08). Solution:
either hide the button as originally intended, or move to a paste host with a real delete API.

`log-upload-has-no-redaction.md` — area `security`. Problem: `uploadLogFile` sends up to 10 MiB of
log content to dpaste.com as a public paste with a 2-day expiry, and `src/backend/logger/` has no
redaction anywhere. GameLib adds Steam refresh tokens and revealed Humble key values that upstream
Heroic never had. The threat surface is identical in both builds and predates the Tauri port; the
upload sits behind a confirm dialog (`LogFileUploadDialog`). Solution: **a future owner must START by
determining whether a live credential can actually reach a log line** — that bounded audit was
explicitly declined by the user for Phase 34.3 (D-09) and NOT performed, so this todo carries no
finding, only the open question. Then add redaction if the audit finds a real path.
  </action>
  <verify>
    <automated>grep -q "Phase 34.3) — CLOSED" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md && test -f .planning/todos/pending/uploaded-log-delete-button-lies.md && test -f .planning/todos/pending/log-upload-has-no-redaction.md</automated>
  </verify>
  <acceptance_criteria>
    - `SEAM.md` contains a §1 subsection heading matching `Shell, files, logs and diagnostics cluster (real, Phase 34.3) — CLOSED, moved out of §3`
    - The new subsection names all four accepted-gap riders and states Invariants A and B hold
    - `SEAM.md` §3 row 9 no longer lists `clipboard` as untouched, and still lists `protocol` and `ipcMain`
    - `.planning/todos/pending/uploaded-log-delete-button-lies.md` exists with valid frontmatter and both sections
    - `.planning/todos/pending/log-upload-has-no-redaction.md` exists, and its text states plainly that NO audit was performed and the question is open
    - `.planning/IPC-PORT-INVENTORY.md` and `.planning/ROADMAP.md` both read 29 for Phase 34.3 (verified, corrected only if needed)
  </acceptance_criteria>
  <done>SEAM.md's checklist steps 5 and 6 are executed, and both deliberately-out-of-scope defects survive this phase as filed todos rather than as reviewer memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase → Phase 35 | `34.3-PORTED-CHANNELS.md` and SEAM.md §1 are the durable record Phase 35 will act on; an inaccurate record is an operational risk, not a documentation nit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.3-29 | Repudiation | "ported" being read as "works" for `deleteUploadedLogFile` or `resetHeroic`, repeating the conflation that let G-30-02 be declared fixed twice while the live build hung | mitigate | Per-row honest proof levels, an explicit "Automated vs live proof boundary" section, and a gate token pinning each caveat |
| T-34.3-30 | Repudiation | a later edit silently dropping a required declaration or re-adding `logError` as a 30th row | mitigate | `ported-channels-gate.py`'s exact-29-rows assertion, `logError`-absent assertion, and rider-token list — each proven to reject its bad input by `--self-test` |
| T-34.3-31 | Information Disclosure | the unredacted-log-upload threat being lost because no audit was performed | mitigate (tracking only) | Filed as `.planning/todos/pending/log-upload-has-no-redaction.md` with the open question stated explicitly and no finding implied. **No audit is performed — D-09, explicit user instruction** |
| T-34.3-32 | Repudiation | SEAM.md §3 still advertising `clipboard` as untouched, misleading a future slice into rebuilding the seam | mitigate | Task 3's §3 row edit plus the §1 CLOSED subsection, both asserted |
| T-34.3-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages |
</threat_model>

<verification>
- `test $(grep -c '^| `' .planning/phases/34.3-*/34.3-PORTED-CHANNELS.md) -eq 29`
- `python3 .planning/phases/34.3-*/ported-channels-gate.py` exits 0
- `python3 .planning/phases/34.3-*/ported-channels-gate.py --self-test` exits 0
- `grep -q "Phase 34.3) — CLOSED" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md`
- Both todo files exist under `.planning/todos/pending/`
- `cd src/backend && npx jest` — no source file was touched by this plan, so the suite is unchanged and green
- REQ-34.3-13: this plan touches documentation only; both builds are unaffected
</verification>

<success_criteria>
- 29 rows, no `logError` row, every proof level honest and every rider present
- A doc-shape gate that passes AND has been shown to reject each violation it guards
- SEAM.md checklist steps 5 and 6 executed
- Two todos filed, with the redaction todo stating plainly that no audit was performed
</success_criteria>

<output>
Create `.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-08-SUMMARY.md` when done
</output>
