---
phase: 34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics
plan: 08
subsystem: infra
tags: [tauri, documentation, ipc, declared-channel-list, seam-checklist, doc-shape-gate]

# Dependency graph
requires:
  - phase: 34.3-01
    provides: shellFilesFlowRegistration.ts's 18 channels (12 openers + showConfigFileInFolder/showItemInFolder/removeFolder + checkDiskSpace/getShellPath/pathExists)
  - phase: 34.3-02
    provides: clearCache/clearAchievementCache/resetHeroic registration + resetHeroic's real, narrower-than-advertised blast radius
  - phase: 34.3-03
    provides: the Rust clipboard seam (arms, pure helpers, 10 tests) and the D-05 verified no-fix finding
  - phase: 34.3-04
    provides: logInfo/showLogFileInFolder/uploadLogFile/deleteUploadedLogFile/getUploadedLogFiles registration + D-08/D-09 declarations
  - phase: 34.3-05
    provides: electronStub.clipboard.writeText's real forwarding, readText's documented-dead stub, the relaunchInFlight race guard (D-06)
  - phase: 34.3-06
    provides: clipboardFlowRegistration.ts registering all 3 clipboard channels
  - phase: 34.3-07
    provides: the regenerated electron-reach ledger and the REQ-34.3-08 Rust source-existence gate
provides:
  - 34.3-PORTED-CHANNELS.md declaring all 29 slice-6 channels with kind/backed-by/honest proof level and every accepted-risk/framing-correction rider
  - ported-channels-gate.py, a re-runnable doc-shape gate with anti-vacuity self-tests
  - SEAM.md's Incremental-Port Checklist steps 5/6 closed (§3→§1 row move, clipboard no longer "untouched")
  - Two filed todos surviving this phase's two declared out-of-scope defects
affects: [34.3-09 (the live gate that supplies this document's LIVE proof cells), phase-35-electron-cutover]

# Tech tracking
tech-stack:
  added: []
  patterns: [row-leading backtick-anchored table-row regex (never a bare substring match) for a doc-shape gate, anti-vacuity self-tests run through the same assertion functions as production, "pending, not observed" framing for proof levels that name a not-yet-run live gate item]

key-files:
  created:
    - .planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-PORTED-CHANNELS.md
    - .planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/ported-channels-gate.py
    - .planning/todos/pending/uploaded-log-delete-button-lies.md
    - .planning/todos/pending/log-upload-has-no-redaction.md
  modified:
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md

key-decisions:
  - "Proof-level cells naming a live-gate item (unit + LIVE (item N)) are declared as the row's TARGET proof level, explicitly marked pending — not yet observed — because plan 34.3-09 (the blocking live gate) runs strictly after this plan (depends_on includes 34.3-08) and had not executed as of this document's authorship. A dedicated 'Live-gate status as of this document' paragraph and a repeated reminder in the proof-boundary section prevent a reader from mistaking a declared target for an observed result — the same overclaiming this whole artifact exists to prevent."
  - "All 12 link openers, plus the 3 reveal-path channels, are assigned unit + LIVE (item 1) / (item 2) respectively, per the plan's explicit instruction that live-gate items 1 and 2 prove those shared code paths once rather than 29 times — not a per-channel live observation."
  - "The gate's ROW_PATTERN anchors on a row-LEADING backticked name (^\\|\\s*`name`\\s*\\|) so a channel merely mentioned in prose (e.g. logInfo's note that it mirrors the already-ported logError) can never satisfy a row-presence assertion — verified by the self-test's logError-as-actual-row case, which is distinct from a prose mention."
  - "check_exact_row_count only counts rows whose name is in CHANNELS or is exactly 'logError' (the one name the gate must actively reject), so the count assertion stays meaningful even if unrelated backticked-name tables ever appear elsewhere in the document."

requirements-completed: [REQ-34.3-12, REQ-34.3-13]

# Metrics
duration: ~70min
completed: 2026-07-26
---

# Phase 34.3 Plan 08: Declared Ported-Channel List + SEAM Closure Summary

**Declared all 29 of slice 6's channels with honest, verified-against-SUMMARY proof levels in `34.3-PORTED-CHANNELS.md`, backed it with a re-runnable doc-shape gate whose 5 assertions are each proven to reject a synthetic bad input, closed SEAM.md's Incremental-Port Checklist steps 5/6, and filed the two out-of-scope defects (the lying delete button, the unaudited log-redaction gap) as todos.**

## Performance

- **Duration:** ~70 min
- **Completed:** 2026-07-26
- **Tasks:** 3
- **Files modified:** 5 (4 created, 1 modified)

## Accomplishments

- Wrote `34.3-PORTED-CHANNELS.md`: 29-row table (`grep -c '^| \`'` = 29 exactly), zero `logError` row, both framing corrections (clipboard no-op was never a latent shipped defect; `resetHeroic`'s real blast radius is `config.json` + `GamesConfig/` only), the D-05 verified-no-fix finding naming `tauri = 2.11.5`, the Humble key-copy `KNOWN ACCEPTED RISK` rider, `deleteUploadedLogFile`'s both-builds-dead declaration distinguished from 34.2's D-07, and the filed-not-audited log-redaction statement — all verified present in the file by direct grep before committing.
- Wrote `ported-channels-gate.py`: 5 assertions (all 29 channels present as rows, exactly 29 rows, `logError` absent as a row, all 9 required rider tokens present, every proof-level cell one of the 3 permitted values), each proven by `--self-test` to reject a synthetic violation through the exact same `check_*` functions used against the real document.
- Closed SEAM.md's checklist steps 5 and 6: added the `### Shell, files, logs and diagnostics cluster (real, Phase 34.3) — CLOSED, moved out of §3` subsection (channel-kind breakdown, 4 accepted-gap riders, Invariants A/B confirmation, checklist-step-3/4 record, D-05 finding, the deliberate live-gate claim-level break), and edited §3 row 9 so `clipboard` is recorded CLOSED rather than "untouched," leaving `protocol`/`ipcMain` (remaining).
- Filed `uploaded-log-delete-button-lies.md` (area: logs) and `log-upload-has-no-redaction.md` (area: security), both in this project's established todo format, so the two deliberately out-of-scope defects survive this phase.
- Confirmed `IPC-PORT-INVENTORY.md` and `ROADMAP.md` already both read 29 for Phase 34.3 (no correction needed — the stale "30" this plan's `read_first` warned about was already corrected at plan-authoring time).

## Task Commits

Each task was committed atomically:

1. **Task 1: Write 34.3-PORTED-CHANNELS.md** - `902965b7` (docs)
2. **Task 2: Write the re-runnable doc-shape gate** - `484c13ce` (test)
3. **Task 3: Move SEAM.md's rows §3→§1 and file the two out-of-scope todos** - `e6b54d34` (docs)

## Files Created/Modified

- `.planning/phases/34.3-.../34.3-PORTED-CHANNELS.md` - New declared ported-channel list; 29-row table, budget-correction framing, accepted-risks section, automated-vs-live proof boundary section
- `.planning/phases/34.3-.../ported-channels-gate.py` - New executable doc-shape gate with a self-test mode; `CHANNELS` (29 entries) and `REQUIRED_RIDER_TOKENS` (9 entries) constants, a docstring mirroring `34.2-currency-gate.py`'s never-delete-an-assertion discipline (deliberately without gap-cycle scaffolding, since this phase has none yet)
- `.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` - New §1 subsection for the Phase 34.3 cluster; §3 row 9 edited to move `clipboard` from "untouched" to "CLOSED"
- `.planning/todos/pending/uploaded-log-delete-button-lies.md` - New todo, area `logs`
- `.planning/todos/pending/log-upload-has-no-redaction.md` - New todo, area `security`

## Decisions Made

- **Live-gate proof-level cells are explicitly marked pending, not observed.** Plan `34.3-09` (the blocking 5-item live gate) depends on this plan and had not run as of this document's writing. Rather than silently writing `unit + LIVE (item N)` as if it were an already-achieved fact — which would repeat the exact "wired and unit-proven read as seen working" conflation this whole artifact exists to prevent — the document states plainly, in its own opening section and again in the "Automated vs live proof boundary" section, that these cells name the item that WILL supply the live half of that row's proof once `34.3-09` completes and `34.3-LIVE-GATE.md` exists. This is stricter than the plan's literal `<action>` instruction demanded, but consistent with the plan's own repeated insistence (D-11's rationale, the objective section, the required "Automated vs live proof boundary" content) that no reader may take wiring for observation.
- **The 12 link openers and 3 reveal-path channels share one proof-level assignment each** (`unit + LIVE (item 1)` / `(item 2)`), per the plan's explicit instruction that the live gate proves the shared `openUrlOrFile`/`showItemInFolder` code path once, not per-channel — this is why the gate is 5 items, not a 29-channel UAT, and the document says so.
- **The gate script deliberately does not copy 34.2's `currency-gate.py` gap-cycle numbering scaffolding**, per the plan's own instruction — this phase has no gap cycle. If one opens later, the correct action is a new, separate section mirroring 34.2's per-cycle pattern, not retrofitting cycle numbers onto this file now.

## Deviations from Plan

None — plan executed exactly as written, with one deliberate strengthening (see "Live-gate proof-level cells" decision above, which is a stricter honesty stance than the plan's literal action text, not a deviation from its intent).

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `34.3-PORTED-CHANNELS.md` and its gate are ready for `34.3-09` (the blocking live gate) to read and, once the 5 items are run and recorded in `34.3-LIVE-GATE.md`, this document's `unit + LIVE (item N)` cells become true observations rather than declared targets — no further edit to this document is required for that transition, since the cells already name the exact item each row depends on.
- SEAM.md's Incremental-Port Checklist steps 1-6 are all executed for this slice; the document is ready for Phase 35's cutover work-list to cite.
- Both filed todos are self-contained and require no phase-34.3-specific context to action later.
- No blockers. Full backend `npx jest --selectProjects Backend` (run twice, since this plan touched zero source/test files and only needed to confirm the baseline is unchanged): both runs show the identical, previously-documented pair of pre-existing failures — `sidecar/__tests__/rustInvokeChannel.test.ts` (Phase 34.1-era baseline) and `wine/manager/downloader/__tests__/utilities/rest.test.ts` (environment-path-depth-dependent, `deferred-items.md`). One intermediate run additionally showed `humble/__tests__/user.test.ts` failing with a stack trace pointing into unrelated `storeManagers/steam/library.ts` code — a one-off, non-reproducing flake (this project's documented `library.ts` leaked-timer/test-pollution class), not caused by this plan's files; a clean re-run passed that suite with no changes made. 2399-2400/2401 tests, 114-115/116 suites passing depending on the flake's presence in a given run; neither of the two consistent failures touches any file this plan modified (all documentation).

## Self-Check: PASSED

- FOUND: `.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/34.3-PORTED-CHANNELS.md`
- FOUND: `.planning/phases/34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics/ported-channels-gate.py`
- FOUND: `.planning/todos/pending/uploaded-log-delete-button-lies.md`
- FOUND: `.planning/todos/pending/log-upload-has-no-redaction.md`
- FOUND: `.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` contains `Phase 34.3) — CLOSED`
- FOUND commit: `902965b7`
- FOUND commit: `484c13ce`
- FOUND commit: `e6b54d34`

---
*Phase: 34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics*
*Completed: 2026-07-26*
