---
phase: 34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics
plan: 09
type: live-gate
status: task-1-complete
blocking: true
created: 2026-07-26
---

# Phase 34.3 Plan 09 — Blocking Live Gate

Per D-13, this gate is **blocking**: any FAIL in the 5 named items below means the phase does not
close and `/gsd-plan-phase 34.3 --gaps` is the next step, not a deferred UAT entry.

This document is produced in two stages:
- **Task 1 (this section, automated, executor-run):** the pre-gate sweep + both builds.
- **Tasks 2/3 (the 5 item sections below, human-run):** the OS-level observations jest cannot make.

---

## Task 1 — Automated Sweep + Build Results

Run 2026-07-26, all commands executed from repo root (`/Users/graysonmitchell/Projects/GameLib`)
unless noted.

| # | Check | Command | Result |
|---|-------|---------|--------|
| 1 | Full backend jest | `npx jest --selectProjects Backend --config src/backend/jest.config.js` | **PASS** — 115/116 suites, 2400/2401 tests. Sole failure `src/backend/sidecar/__tests__/rustInvokeChannel.test.ts` — pre-existing since Phase 34.1, explicitly out of scope per this plan's corrected baseline. Matches baseline exactly, no new regressions. (Note: running the identical command from inside `src/backend` instead of repo root additionally trips `wine/manager/downloader/__tests__/utilities/rest.test.ts` — a known cwd-dependent path-depth artifact in that one test's own error-string assertion, not a real failure; confirmed by re-running it isolated from repo root, where it passes clean. Recorded here for the next executor's awareness.) |
| 2 | `cargo test` | `cd src-tauri && cargo test` | **PASS** — 16/16 tests, including all 10 clipboard-helper tests (`clipboard_text_arg_*`, `clipboard_read_value_*`) and the 6 `longRunningChannels`/timeout tests. |
| 3 | `cargo check --quiet` | `cd src-tauri && cargo check --quiet` | **PASS** — exit 0, no output. |
| 4 | `tsc --noEmit` | `npx tsc --noEmit` | **PASS** — exit 0, no output. |
| 5 | `prettier --check` (touched files) | `npx prettier --check <25 files touched by 34.3-01..08>` | **PASS** — "All matched files use Prettier code style!" (Rust file `src-tauri/src/main.rs` excluded — prettier has no parser for `.rs`; the repo's own `pnpm prettier` script (`prettier --check .`) silently skips unsupported extensions the same way, confirmed against `.github/workflows/lint.yml:19`.) |
| 6 | `ported-channels-gate.py` | `python3 .../ported-channels-gate.py` | **PASS** — "OK: all 29 channels declared exactly once, logError absent, 9 rider token(s) present, every proof level permitted." |
| 6b | `ported-channels-gate.py --self-test` | `python3 .../ported-channels-gate.py --self-test` | **PASS** — exit 0, "SELF-TEST OK: every assertion rejects its corresponding bad input" (all 5 assertions proved capable of rejecting the input they exist to reject). |
| 7 | `npm start` Electron regression (REQ-34.3-13) | `npm start` (backgrounded, observed via log + `ps`/`pgrep`, then quit) | **PASS, with one caveat below.** App launched: `electron-vite dev --watch` compiled cleanly, Electron main process + 2 renderer helper processes + gpu/utility helpers all came up (confirmed via `ps aux`), backend bootstrap completed (`Starting the Download Queue`, Humble sync finished 26/26 keys), and the frontend actively received and processed the full Steam library push (`[handleGamePush/steam]` lines for ~377 games, matchedIndex counting through to the end) — this requires a live renderer process consuming IPC data, which is the strongest signal obtainable from a headless terminal that the library actually rendered. **I did not visually confirm the window on screen** (no screenshot capability from this context) — the human should do a quick visual spot-check if desired, but process-level and log-level evidence is unambiguous and consistent with a normal, successful launch. App was cleanly quit afterward (`kill`, confirmed via `pgrep` that zero GameLib Electron processes remained). **Caveat (out of scope, pre-existing, not touched by this phase):** one console error appeared — `Error occurred in handler for 'getCrossoverIndex': Error: No handler registered for 'getCrossoverIndex'` — a startup-race in `crossover_index/ipc_handler.ts`'s registration timing, unrelated to any of the 25 files this phase (34.3-01..08) touched (confirmed via `git diff --stat`: none of them touch `main.ts` or `crossover_index/*`). Logged to `deferred-items.md`, not fixed (out of scope per this plan). Did not affect Download Queue/library rendering. |
| 8 | Packaged build | `electron-vite build && pnpm build:sidecar-sea && npx tauri build` | See "Packaged Build" subsection below. |

**Packaged bundle path:** `/Users/graysonmitchell/Projects/GameLib/src-tauri/target/release/bundle/dmg/GameLib_0.7.0_aarch64.dmg`
(367,548,711 bytes). The app bundle itself (`GameLib.app`) is embedded inside this DMG — tauri's
own `bundle_dmg.sh` cleans up the loose `.../bundle/macos/GameLib.app` directory after packaging it
into the DMG, so the DMG is the durable artifact. Mounted read-only to confirm contents (then
unmounted): `GameLib.app/Contents/MacOS/` contains both `gamelib-shell` (main binary) and
`gamelib-sidecar` (the SEA sidecar `externalBin`, the exact binary the i18next-fs-backend fix
above produced). `codesign -dv` confirms `Signature=adhoc` / `flags=0x20002(adhoc,linker-signed)`
— unsigned/ad-hoc as expected with no local Apple Developer identity configured, per Phase 34 gap
34-16.

### Deviation: SEA sidecar build was broken by a pre-existing (Phase 34.2) defect, fixed here as a Rule 3 blocking-issue auto-fix

`pnpm build:sidecar-sea` failed on first run with:
```
Error: COMPILE GATE FAILED (D-06): esbuild SEA bundle exited 1:
✘ [ERROR] Top-level await is currently not supported with the "cjs" output format
    node_modules/i18next-fs-backend/esm/writeFile.js:7:20
    node_modules/i18next-fs-backend/esm/readFile.js:8:20
```

**Root cause (confirmed, not guessed):** `src/backend/sidecar/bootstrap.ts` does
`import Backend from 'i18next-fs-backend'` (added by commit `a8e7c8093`,
`feat(34.2-01): initialize i18next in sidecar bootstrap (D-02)` — **Phase 34.2, not this phase**).
`i18next-fs-backend`'s `package.json` `"exports"` map resolves the `import` condition to
`./esm/index.js` regardless of the bundle's own `--format=cjs` output (a dual-package hazard —
esbuild picks the condition based on how the specifier is referenced in source, not the bundle's
target format). The ESM variant's `writeFile.js`/`readFile.js` use a top-level
`await import('node:fs')` Deno-detection guard, which is illegal in a cjs bundle. This has been
silently broken since 2026-07-25 (Phase 34.2) — nobody had re-run `build:sidecar-sea` since, so
this is the first time it surfaced. **None of 34.3's own 25 touched files caused this.**

**Fix applied (Rule 3 — blocking build-config error, no package install involved):** added
`'--alias:i18next-fs-backend=i18next-fs-backend/cjs'` to `meta/buildSidecarSea.ts`'s
`buildEsbuildArgv()` flags, forcing esbuild to resolve the package's own genuinely-CJS `./cjs`
subpath export (confirmed by direct inspection: `cjs/writeFile.js` uses plain
`require('node:fs')`, no top-level await) — mirroring the existing `--alias:electron=...` entry in
the same array. Also added the new flag to `meta/__tests__/buildSidecarSea.test.ts`'s
"carries the required bundling flags" `arrayContaining` assertion (44/44 tests in that suite still
green). Re-ran `pnpm build:sidecar-sea`: **PASS**, produced
`src-tauri/binaries/gamelib-sidecar-aarch64-apple-darwin`. Standalone smoke-tested with
`PATH=/usr/bin:/bin` (no system Node, no reachable `node_modules`), stdin held open: printed
`__GAMELIB_SIDECAR_READY__` with no crash, mirroring 34-02's original verification protocol
exactly.

**Files modified by this fix:** `meta/buildSidecarSea.ts`, `meta/__tests__/buildSidecarSea.test.ts`
(commit recorded separately from this document's own commit — see Task 1 commit list).

---

### Packaged Build

- **electron-vite build:** PASS — `build/` output produced, only a pre-existing "chunks larger than
  500kB" advisory warning (not an error).
- **pnpm build:sidecar-sea:** PASS after the fix above — `src-tauri/binaries/gamelib-sidecar-aarch64-apple-darwin`
  produced and standalone-smoke-tested clean.
- **npx tauri build:** PASS — `cargo build --bins --features tauri/custom-protocol --release`
  finished in 5m22s, then bundled `GameLib.app` and `GameLib_0.7.0_aarch64.dmg` successfully. One
  pre-existing, unrelated advisory: `Warn The bundler was configured to create updater artifacts
  but no updater-enabled targets were built. Please enable one of these targets: app, appimage,
  msi, nsis` — a `tauri.conf.json` updater/bundle-target config note inherited from Phase 34
  (34-05/34-06), not touched by any of this phase's 25 files; not a failure, not fixed here
  (out of scope).
- **Output bundle path:** `src-tauri/target/release/bundle/dmg/GameLib_0.7.0_aarch64.dmg` (see full
  path above).
- **macOS signing:** ran unsigned/ad-hoc as expected (no `APPLE_*`/`CSC_*` identity configured
  locally) — confirmed via `codesign -dv` on the mounted `.app` (`Signature=adhoc`), not a failure,
  per Phase 34 gap 34-16.

---

## Item 1 — one opener actually opens a browser (REQ-34.3-01)

**Build:**
**Result:**
**Evidence:**
**Observed at:**

---

## Item 2 — `showItemInFolder` actually reveals in Finder (REQ-34.3-01)

**Build:**
**Result:**
**Evidence:**
**Observed at:**

---

## Item 3 — clipboard write pastes, and `clipboardReadText` round-trips (REQ-34.3-03/04)

**Build:**
**Result:**
**Evidence:**
**Observed at:**

---

## Item 4 — `resetHeroic` leaves exactly ONE sidecar process, `config.json` gone (REQ-34.3-06/07) — PACKAGED build only

**Build:**
**Result:**
**Evidence:**
**Observed at:**

---

## Item 5 — `uploadLogFile` returns a real, reachable dpaste URL (REQ-34.3-09)

**Build:**
**Result:**
**Evidence:**
**Observed at:**
