---
status: partial
phase: 34.3-tauri-ipc-re-plumb-slice-6-shell-files-logs-and-diagnostics
source: [34.3-VERIFICATION.md, 34.3-LIVE-GATE.md]
started: 2026-07-27
updated: 2026-08-23
---

## Current Test

[none in flight. The 2026-08-23 re-run is CLOSED — 4 passed, 1 blocked on host environment.
 Teardown complete (see "Teardown" below): packaged app quit cleanly, backed-up config restored
 and verified byte-identical.

 Two items remain open on HOST-environment prerequisites, NOT on GameLib code — item 1 (no
 http/https handler registered) and item 2's selection property (dangling `NSFileViewer`).
 Both host fixes are named under "### Re-run 2 recount (2026-08-23, after the host fix was actually applied)

total: 5 | passed: 3 | issues: 1 | blocked: 1

Change from the first 2026-08-23 recount: **item 2 moves from `passed` to `issue`.** Its
folder-open half still passes; its SELECTED half — the property the acceptance criterion
actually names — fails reproducibly in the normal-use configuration. The host explanation
previously recorded for it (`NSFileViewer`) was refuted by counterfactual.

evidence_quality: item 2 is now MACHINE-MEASURED with a probe validated in both directions;
items 3/4/5 machine-verified; item 1 blocked on host, un-evidenceable here.

## Gaps". Neither is a code defect; the phase's
 `34.3-VERIFICATION.md` verdict stays `passed` per the 2026-07-27 operator decision.]

## Teardown (2026-08-23, closing the re-run)

The re-run's only destructive step was item 4's `resetHeroic`, which deleted the live
`config.json` + `GamesConfig/`. Both are now restored.

  - App confirmed fully stopped before restoring: `pgrep -fl 'gamelib-shell|gamelib-sidecar|
    sidecar.js|tauri dev'` returned NOTHING, and the single-instance socket
    (`~/Library/Application Support/gamelib/gamelib-single-instance.sock`) was gone — the clean
    -shutdown signature, so no live process could rewrite config underneath the restore.
  - `config.json` restored from `~/gamelib-uat-34.3-backup-20260823-114756/`: 2775 bytes, mtime
    back to its original `Aug 18 15:03`, SHA-256 matching the digest recorded at backup
    time BEFORE the reset:
      `6a912cdd0b645f12e73ddaa76bae0d8a0eb187341afdcdbc9db58a0a91177eb5`
  - `GamesConfig/` restored: 57 files, `diff -r` against the backup reports IDENTICAL
    (byte-for-byte, all 57). Content sanity-checked independently: 55 `.json` files all parse
    under `json.load`, 0 empty; the 2 non-parsing files are `gog-redist.log` /
    `gog-redist.log.old`, which are logs and correctly not JSON.
  - The post-reset pristine-default `config.json` (1994 bytes, mtime 12:09:13 — item 4's own
    evidence) was itself preserved to `~/gamelib-uat-34.3-postreset-default-20260823/` before
    being overwritten, so item 4's post-condition artifact survives the restore and the restore
    is itself reversible.

CAVEAT ON THE BACKUP DIGESTS — worth carrying forward. `config.json`'s recorded digest is a
plain `shasum -a 256` and reproduced EXACTLY. The `GamesConfig` digest recorded above
(`f7abfb7d…`) is NOT reproducible: the recipe that produced it was never written down, and nine
plausible formulations were tried (per-file `hash  name` lines, contents-only, `cat`-concat,
`tar`, `ls`-order, with and without `./` prefixes) — none match. The restore was therefore
verified by `diff -r` against the backup tree instead, which is a STRONGER check than the digest
it replaces. Lesson: a directory digest recorded without its recipe is not a verification
artifact, because only the person who computed it can ever check it again.

## Re-run 2026-08-23 (evidence-capture pass)

Re-running all 5 items against a live build to replace the 2026-07-27 tester attestations with
machine-captured evidence (AppleScript for browser URL / Finder selection, `pbpaste` + `curl` for
clipboard and dpaste, `pgrep` for the process count).

**Build under test:** `pnpm tauri:dev`, `gamelib-shell` PID 83046 started 2026-08-23 11:19:13,
sidecar PID 83153, bundle `build/main/sidecar.js` rebuilt in the same run. All 34.3 sources
predate it, so the running build covers this phase's code.

**Item 1 REGRESSED on this build.** See its result below. The 2026-07-27 PASS is not withdrawn —
it was a different build a month ago — but the channel does not work today.

## Note on what these items actually are

**All 5 of these were already run by the tester on 2026-07-27 and reported PASS.** They are listed
here not because they are untested, but because the verbatim evidence the acceptance criteria
called for (URLs, clipboard values, `pgrep` output) was not retained — so the record is a tester
attestation rather than a reproducible transcript. `34.3-LIVE-GATE.md` states this plainly itself.

This file exists so that gap is *tracked* rather than quietly absorbed, per the phase's own threat
T-34.3-36 ("an item recorded PASS on a log line where the requirement is an observable OS effect").

Corroborating evidence that the gate was genuinely exercised, not rubber-stamped: it surfaced a
real defect — the `LogFileUploadDialog` clipboard-parity bug (fixed, commit `44232d3e`) — which
could only have been found by actually performing item 5 and checking the clipboard afterward.

Re-running these with captured output is optional rigor, not a known failure.

## Tests

### 1. Link opener actually opens a browser (REQ-34.3-11 item 1)
expected: Browser opens at the exact target URL
result: [2026-07-27: passed — tester-attested, URL not retained]
result: [2026-08-23 RE-RUN: BLOCKED — host environment, NOT a GameLib defect]
result: [2026-08-23 RE-RUN 3: **THE ABOVE IS WRONG AND WITHDRAWN.** The host is FINE.
         This is a REAL GameLib DEFECT.]
disposition: issue
reported: "nothing happens on click"
trigger: Settings -> Logs -> "Join our Discord" footer button (`openDiscordLink`,
         `shellFilesFlowRegistration.ts:178` -> `openUrlOrFile(discordLink)` ->
         `shell.openExternal('https://discord.gg/rHJ2uqdquK')`)

ROOT CAUSE (machine-verified, reproduces with GameLib not involved at all):
  This host has NO default web browser registered with LaunchServices. Reading
  `~/Library/Preferences/com.apple.LaunchServices/com.apple.launchservices.secure.plist`
  directly: 38 `LSHandlers` entries, URL schemes
  `[microsoft-edge, opencode, steam, steamlink, claude, x-xcode-documentation,
    apple-reference-documentation, x-swift-package-repository-authentication, xcdoc,
    xcdevice, xcode-ci-handler, xcode-ci, xcode, doc, x-xcode-ci-build-report-feedback,
    x-source-tag, xcarchive, heroic, gamelib]`
  — `http` and `https` are BOTH absent.

  Independent reproduction outside the app:
    $ /usr/bin/open "https://discord.gg/rHJ2uqdquK"   # exit 0, no browser opened
  Verified by enumerating every tab of every window: Safari 0 windows, Brave 0 windows,
  Edge 13 tabs, none of them the Discord URL. `open` exits 0 because LaunchServices accepts
  the request fire-and-forget; with no handler bound there is nothing to launch.

  This is exactly the shape GameLib sees. `tauri-plugin-opener`'s `open_url` reaches the same
  LaunchServices facility, and returns Ok — which is why `main.rs:5634`'s
  `eprintln!("[shell] openExternal failed: ...")` never fired and the tester's terminal watch
  came back clean. Nothing on the GameLib chain is broken; the OS had nowhere to send the URL.

REFUTATION OF THE "NO HTTP HANDLER" ROOT CAUSE (2026-08-23 re-run 3) — the entire diagnosis
above is FALSE, and it was believed for a whole session. Two independent errors produced it:

  1. **The plist read proves nothing.** `com.apple.launchservices.secure.plist` records only
     LSHandler OVERRIDES. A system-default browser needs no entry there, so "http/https absent
     from LSHandlers" is the NORMAL state for an unmodified Mac, not evidence of a missing
     handler. Asking the OS directly instead:
       NSWorkspace.urlForApplication(toOpen: "https://example.com")
         -> /System/.../Safari.app
       NSWorkspace.urlForApplication(toOpen: "http://example.com")
         -> /System/.../Safari.app
     Safari IS the registered handler for both schemes and always was.

  2. **The "no browser opened" observation was a PERMISSIONS failure in the probe.** The check
     used AppleScript to query Safari, which returns `-1743 Not authorised to send Apple events
     to Safari` on this host because Automation permission was never granted. A probe failing
     for its OWN reasons was recorded as the product failing.

  Control, re-run with an Accessibility-based probe (authorised) instead of Apple Events:
    $ /usr/bin/open "https://discord.gg/rHJ2uqdquK"    # the EXACT URL the button uses
    Safari front-window title before: "Your secure link to Claude.ai ... - Gmail"
    Safari front-window title after:  "https://discord.com/invite/rHJ2uqdquK"
    frontmost after: Safari
  The OS, the browser, the default-handler registration and the URL all work.

  CONSEQUENCE: item 1 is NOT blocked and never was. It is a REAL DEFECT and is recorded as an
  `issue`. Nothing about this host needs changing, and the operator's default-browser setting
  was correctly left untouched. (An attempt was made mid-session to SET a default browser to
  "unblock" this item — that was solving a problem that did not exist, and macOS 26 correctly
  refuses to let a CLI do it anyway.)

WHAT IS ACTUALLY KNOWN (2026-08-23 re-run 3), stated at the limit of the evidence:
  - Clicking "Join our Discord" produces NO browser navigation and NO log line anywhere.
  - It also produces no `[shell] openExternal failed:` diagnostic (`main.rs:5702`) and no
    "Unrecognized frame kind" diagnostic (`main.rs:5710`) — both of which print to stderr and
    WOULD appear in the `tauri:dev` log. Their joint absence suggests the `openExternal` frame
    never reaches the shell at all, rather than reaching it and failing.
  - The chain is statically intact end to end: `LogSettings/index.tsx:259` -> preload
    `makeListenerCaller('openDiscordLink')` -> `shellFilesFlowRegistration.ts:178` (registered,
    `handlers.ts:163` calls `registerShellFilesFlows()`) -> `openUrlOrFile` (`utils.ts:421`)
    -> `shell.openExternal` (`electronStub.ts:549`) -> `transport.openExternal` =
    `requestOpenExternal` (`sidecarRpc.ts:317`) -> RPC frame -> `main.rs:5699`.
  - DISCRIMINATOR: `openExternal` is the ONLY channel in this cluster that travels as a
    fire-and-forget `kind: "openExternal"` FRAME. Item 2's reveal — proven working 3/3 the same
    session — uses `requestRustInvoke` instead. So the sidecar, the RPC pipe, the click wiring
    and the Rust dispatch are all demonstrably healthy; what is unproven is specifically the
    fire-and-forget frame path.
  - NOT root-caused. Two silent-drop candidates are named but untested: `electronStub.ts:549`'s
    `transport?.openExternal(url)` optional-chain (a no-op that still resolves, so the
    `.catch(logSendFailure)` at `shellFilesFlowRegistration.ts:179` can never fire), and the
    preload `send()` whose invoke rejection is discarded.

verdict: The 2026-07-27 PASS STANDS. This item cannot be re-evidenced on this host until a
         default browser is set (System Settings -> Desktop & Dock -> Default web browser).
         Re-run after that; the AppleScript URL capture is ready to go.
note: This also means the phase's own gate item 1 is UNRUNNABLE on a host in this state, and
      would have read as a GameLib failure to anyone who did not check the OS layer. Worth
      keeping in the gate's preconditions.

### 2. showItemInFolder reveals the item in Finder (REQ-34.3-11 item 2)
expected: Finder opens with the specific item SELECTED, not just the parent folder
result: [2026-07-27: passed — tester-attested, trigger not retained]
result: [2026-08-23 RE-RUN: passed (folder), selection NOT machine-confirmed]
result: [2026-08-23 RE-RUN 2 (post host-fix): **FAIL** — folder correct EVERY time,
         item NOT selected. Host explanation REFUTED. This is a real defect.]
result: [2026-08-23 RE-RUN 3 (post CODE fix): **PASSED** — 3/3 through the real button,
         window on the external display i.e. the failing condition. Defect FIXED.]
disposition: resolved
resolved_by: debug session `finder-reveal-no-selection` -> `macos_finder_reselect_workaround()` in src-tauri/src/main.rs
trigger: Settings -> Logs -> "Show log file in folder" (`showLogFileInFolder`,
         `loggerFlowRegistration.ts:318` -> `showItemInFolder(getLogFilePath(...))` ->
         `requestRustInvoke(RUST_SHELL_SHOW_ITEM_IN_FOLDER)` -> tauri-plugin-opener
         `reveal_item_in_dir`)
evidence_captured:
  - Baseline before click: `Finder: count of windows` = 0, selection empty.
  - After click: `Finder: count of windows` = 1; front window target =
    `/Users/graysonmitchell/Library/Logs/GameLib/` (correct folder).
  - Selection query returned EMPTY three ways (`selection as alias list`,
    `name of every item of (get selection)`, `selection of front window` -> AppleScript
    error -1728). The specific-item-SELECTED property this item names is therefore NOT
    machine-confirmed.
  - CAUSE HYPOTHESIS (2026-08-23, first pass) — **REFUTED on 2026-08-23 re-run 2, see below.**
    The host had `NSFileViewer = com.asiafu.Bloom` set globally with Bloom NOT installed, and
    macOS prints `Finder replacement com.asiafu.Bloom not found` on every reveal. That warning
    is real, but it was assumed to be the cause without testing the counterfactual.

  RE-RUN 2 (2026-08-23, after `defaults delete -g NSFileViewer` was actually applied):

  - The NSFileViewer hypothesis is DEAD, proven by counterfactual in BOTH directions:
      NSFileViewer deleted    -> `open -R` selects gamelib.log correctly
      NSFileViewer RESTORED   -> `open -R` STILL selects gamelib.log correctly (only the
                                 warning is printed)
    A dangling Finder-replacement binding degrades nothing on this path. The warning was
    correlation, mistaken for cause.

  - THE ACTUAL VARIABLE IS WHICH APP IS FRONTMOST WHEN THE REVEAL FIRES. Measured with a
    standalone Swift binary calling `NSWorkspace.activateFileViewerSelecting` — i.e. GameLib's
    IPC path is NOT involved at all; merely being frontmost is sufficient:
        frontmost = Terminal        -> 20/20 selected
        frontmost = Safari          ->   6/6 selected
        frontmost = gamelib-shell   ->   0/16 selected   <-- the anomaly
    Folder target was correct in all 42 runs; only the SELECTION differs.

  - The selection is NEVER APPLIED, not merely applied late or invisibly:
      * Finder DOES come to the front and DOES open the correct folder.
      * Activating Finder afterwards does not make the selection appear.
      * Finder reports its selection correctly while backgrounded (verified directly), so the
        empty reads are not an artifact of querying a non-frontmost Finder.

  - GameLib's OWN button (the real item-2 trigger), 6 monitored clicks with a clean staged
    baseline before each (`close every window` + `set selection to {}`), auto-detected on
    window-open:
        0 of 6 selected within an 8s poll; front window target correct in 6 of 6.
        2 of the 6 showed the selection at a final read AFTER the 8s poll had timed out.
    AMBIGUITY DECLARED: the tester clicked "at least 6 times", so a 7th+ click can land during
    a prior trial's post-timeout read. Those 2 late reads are therefore NOT scored as passes
    and NOT scored as clean failures.

  - Net: the folder-open half of this item passes reproducibly. The SELECTED half — which is
    what the acceptance criterion actually names — fails in the normal-use configuration,
    because in normal use GameLib IS the frontmost app when the button is clicked.

  MEASUREMENT-VALIDITY NOTE (why the earlier readings churned). Three separate probe defects
  were found and fixed before any of the above was trusted, each of which had already produced
  a wrong conclusion:
    1. A single 2s sample scored a FAIL that a longer poll showed as a PASS at 3.49s.
    2. Finder's app-level `selection` returns the DESKTOP's selection when the desktop holds
       one — one run "passed" while reporting `~/Desktop/Age of Wonders III.app`. Fixed by
       requiring an EXACT path match and clearing the desktop selection during staging.
    3. `selection of <Finder window>` is not a supported property (AppleScript -1728); only the
       application-level `selection` is readable.
  The final probe was validated in both directions: 6/6 green on a known-good control, and it
  correctly rejects both an empty selection and a wrong path.
role_in_diagnosis: This item is still the DISCRIMINATOR for item 1, and that role is
         UNAFFECTED by the selection failure. The folder opening correctly on every click
         proves the footer-row clicks work, the sidecar is alive, and the `rustInvoke`
         request/response leg to Rust is healthy. Item 1's failure is therefore still specific
         to the fire-and-forget `openExternal` leg plus the absent http/https handler, not to
         click wiring or the sidecar as a whole.

### 3. Clipboard write pastes and clipboardReadText round-trips (REQ-34.3-11 item 3)
expected: Both directions round-trip real OS clipboard content
result: [2026-07-27: passed — tester-attested, written/read values not retained]
result: [2026-08-23 RE-RUN: PASSED — both halves. Write half machine-verified; read half
         machine-seeded + tester-confirmed. Both Rust clipboard arms therefore executed.]
note: This is the only proof of the two new Rust arms — no CI step in this repo compiles Rust.
evidence_captured (write half):
  - Clipboard seeded BEFORE the action with a non-URL sentinel so the write could not be
    confused with stale content: `UAT-34.3-SENTINEL-112955-not-a-url`.
  - After "Upload log file" -> Yes, `pbpaste` returned verbatim: `https://dpaste.com/5SYEEESXH`
  - Sentinel provably replaced. The write reached the real macOS pasteboard, which means the
    `clipboard_write_text` Rust arm executed — the only proof available for it, since CI never
    compiles Rust.

FINDING (read half, 2026-08-23) — the paste affordance is UNREACHABLE IN ONE CLICK:
  Tester reported "there is no paste from clipboard icon on either" field.
  Cause, from code: `EditGameDialog/index.tsx:44,47` initialise
    artCover  = gameInfo.overrides?.art_cover  || gameInfo.art_cover
    artSquare = gameInfo.overrides?.art_square || gameInfo.art_square
  and line 137/150 render `icon={artCover ? <Clear /> : <ContentPaste />}`. Every real game has
  `art_cover`/`art_square` populated (it is the library card image), so BOTH fields are truthy
  the instant the dialog opens and the icon is ALWAYS `<Clear />` (an X), never `<ContentPaste />`.
  The paste button only exists on an already-empty field.

  Why this matters to THIS phase specifically: `34.3-VERIFICATION.md`'s Key Link Verification
  table asserts `EditGameDialog/index.tsx -> window.api.clipboardReadText` is WIRED, and it IS —
  in code. But a user cannot reach that call site in one action on any game with artwork. This
  is the "a pass can cover a surface that cannot render" shape: the link check was satisfied by
  a grep-level fact that the UI does not expose.

  Severity: minor/usability. Not a correctness defect in the 34.3 clipboard seam, and NOT a gate
  failure — the round-trip WAS exercised via the two-click path (clear the field first, which
  flips the icon to `<ContentPaste />`, then paste). Recorded so it is not lost.
  Out of scope for this UAT to fix.

evidence_captured (read half):
  - Pasteboard seeded by the assistant with a machine-generated, verbatim, deliberately
    NON-URL sentinel: `UAT-34.3-READBACK-113213`. Non-URL on purpose — the field expects an
    image URL, so a fill proves a raw pasteboard read rather than any URL-aware handling.
  - Two-click path (Clear, then Paste) on the Cover Image field.
  - Tester confirmed the field filled as expected, i.e. with that exact sentinel.
  - EVIDENCE LIMIT, stated rather than papered over: the post-hoc `pbpaste` re-read is NOT
    available as corroboration — by the time it was taken the pasteboard had been overwritten
    by unrelated user activity (`/gsd-secure-phase 34.4`). So the INPUT side of this half is
    machine-controlled and verbatim, but the FIELD CONTENT rests on tester confirmation.
    Still materially stronger than the 2026-07-27 record, where neither side was captured.
  - This exercises `clipboardReadText` -> `requestRustInvoke(RUST_CLIPBOARD_READ_TEXT)`
    directly (`clipboardFlowRegistration.ts`, bypassing the dead sync stub per D-04), so
    together with the write half above BOTH new Rust arms are now proven to execute on this
    build.

### 4. resetHeroic leaves exactly ONE sidecar process, packaged build (REQ-34.3-11 item 4)
expected: Exactly one sidecar before and after; app restarts rather than quitting
result: [2026-07-27: passed — process count tester-attested, pgrep output not retained;
         post-conditions MACHINE-VERIFIED and reproducible: GamesConfig/ absent, config.json
         pristine-default with mtime at the restart moment]
result: [2026-08-23 RE-RUN: PASSED — fully machine-verified, zero attestation component]

evidence_captured (packaged build, `src-tauri/target/release/bundle/macos/GameLib.app`,
                   shell+sidecar both built 2026-08-23 11:49-11:50, arm64, com.gamelib.shell 0.7.0):

  BEFORE (12:08:14)                          AFTER (12:09:43)
    PID    PPID   STARTED    COMM              PID    PPID   STARTED    COMM
    92065  1      12:07:59   gamelib-shell     92136  1      12:09:13   gamelib-shell
    92072  92065  12:08:02   gamelib-sidecar   92140  92136  12:09:13   gamelib-sidecar
    shell=1 sidecar=1                          shell=1 sidecar=1

  - EXACTLY ONE sidecar before and after. Not 0 (which would mean it quit instead of
    restarting) and not 2 (the orphan this item exists to rule out).
  - RESTART PROVEN, not assumed: all four PIDs differ, and both pre-reset PIDs (92065, 92072)
    were confirmed GONE by direct `ps -p` probe — so this was a genuine re-exec, not a window
    that merely lost focus.
  - Sidecar correctly parented throughout (92140 -> PPID 92136), i.e. no reparent-to-launchd.
  - SPAWN PATH PROVEN from argv, which is the entire reason this item requires a packaged
    build: the child is `GameLib.app/Contents/MacOS/gamelib-sidecar` (the SEA binary) and the
    `node .../build/main/sidecar.js` count is 0. `spawn_sidecar_packaged` was taken, not
    `spawn_sidecar_dev`.

  Post-conditions (these were the only machine-verified part of the 2026-07-27 run; re-verified
  here on current code):
  - `GamesConfig/` ABSENT — deleted by resetHeroic and NOT recreated (57 entries before).
  - `config.json` regenerated: 1994 bytes, down from 2775, contents a fresh
    `{defaultSettings, version}` default set.
  - `config.json` mtime `2026-08-23 12:09:13` — the restart moment exactly, not the prior
    Aug 18 15:03. This is what distinguishes "deleted then rewritten by the restarted app"
    from "never deleted".

  BASELINE HYGIENE — a real orphan was found and cleared BEFORE measuring, without which this
  item's count would have been meaningless:
    At 11:49 the `tauri dev` watcher restarted the dev shell (triggered by this UAT's own
    `electron-vite build`/`build:sidecar`), leaving sidecar PID 86814 orphaned — reparented to
    launchd (PPID 1), its spawning shell gone. It later self-exited before it could be killed
    (the kill was guarded by a re-confirm of ppid+command against PID reuse, which correctly
    ABORTED when the PID no longer matched). The whole table was then verified empty — 0 shells,
    0 sidecars, 0 `sidecar.js`, 0 `tauri dev`, and the single-instance socket removed by the
    clean shutdown — before the packaged app was launched.
    This orphan does NOT contradict REQ-34.3-06's research finding: that finding covers
    `app.restart()`, where `RunEvent::Exit` runs `shutdown_child()` before the re-exec. A
    dev-watcher kill bypasses that clean-exit path entirely, so it is a different mechanism.
    It is recorded because it demonstrates the orphan failure mode IS reachable on this
    codebase, which is precisely what makes this item worth re-running rather than inheriting.

why_re-run_rather_than_inherit: The installed `/Applications/GameLib.app` was built 2026-07-27,
  and **85 commits have since touched the three files this item actually probes** —
  `src-tauri/src/main.rs`, `src/backend/sidecar/electronStub.ts`, `src/backend/utils.ts`
  (`git log --oneline --since=2026-07-27 -- <those three>` = 85). Item 4 is specifically about
  which child process `app.restart()` kills, which lives in exactly that code, so testing the
  old bundle would have proven nothing about HEAD. A fresh release build is required.

reversibility (taken BEFORE anything destructive):
  `resetHeroic` deletes `config.json` + `GamesConfig/` (57 entries on this host). Both were
  backed up to `~/gamelib-uat-34.3-backup-20260823-114756/` and verified CONTENT-identical to
  the live copies with path-independent SHA-256 digests:
    GamesConfig f7abfb7da888c3df9d479fda4af95ae4bdb3edb46ac72757348cffbc5ef54447
    config.json 6a912cdd0b645f12e73ddaa76bae0d8a0eb187341afdcdbc9db58a0a91177eb5
  (A first digest attempt hashed file PATHS as well as contents and so changed when the backup
  directory was moved out of the repo — replaced with the content-only digests above.)
  Backup deliberately lives OUTSIDE the repo so it cannot be swept into a commit.

two build-environment facts established before running, both load-bearing:
  1. `use_dev_sidecar()` (`main.rs:5087`) is `cfg!(debug_assertions)` ALONE — a release build
     can NEVER take the dev spawn path regardless of process environment or install location.
     So running the `.app` straight out of `src-tauri/target/release/bundle/macos/` is a valid
     packaged test, and `/Applications/GameLib.app` need not be touched at all.
  2. `single_instance_dir()` (`main.rs:5245`) derives the guard socket from `$HOME` ONLY, with
     NO dev/packaged distinction — both builds share
     `~/Library/Application Support/gamelib/gamelib-single-instance.sock` (present, srw-------).
     The running `tauri dev` instance MUST therefore be stopped first, or the packaged process
     would detect the primary and `exit(0)` — which would have looked like a launch failure and
     silently invalidated the process count this item exists to measure.

### 5. uploadLogFile returns a reachable dpaste URL (REQ-34.3-11 item 5)
expected: A real dpaste.com URL is returned and loads in a browser
result: [2026-07-27: passed — tester-attested, URL not retained]
result: [2026-08-23 RE-RUN: PASSED — fully machine-verified, no attestation component]
note: This item found the clipboard-parity defect fixed in commit `44232d3e`.
evidence_captured:
  - URL, verbatim, read off the real pasteboard: `https://dpaste.com/5SYEEESXH`
  - Reachability: `curl -sIL` -> `http_code=200 size=19890`, `url_effective` unchanged
    (no redirect to an error page).
  - CONTENT confirmed to be this app's log, not merely a 200: the fetched body (19,924 bytes,
    and NOT dpaste's "Rate Limit Exceeded" page — checked explicitly) contains 26x `[Backend]`,
    4x `[Steam]`, 2x `bootstrap] appRoot resolved`, 2x `refreshLibrary complete`.
  - Note the deliberate substitution: the acceptance criterion says "loads in a browser", which
    is UNRUNNABLE on this host (see item 1's ROOT CAUSE — no http/https handler). `curl` proves
    the stronger property anyway: the URL resolves, returns 200, and serves the correct content.
    The browser was never the load-bearing part of this item.

## Summary

total: 5
passed: 4
issues: 1
pending: 0
skipped: 0
blocked: 0

(2026-08-23 re-run counts. The 2026-07-27 attested run was 5 passed / 0 issues. Item 1 is
BLOCKED on host environment, not failed — see its ROOT CAUSE. Item 2 passed on the folder but
its selection property is un-evidenced for the same class of host reason.)

evidence_quality: attested (4.5 of 5 items); item 4's post-conditions machine-verified

## Gaps

ONE CODE GAP (opened 2026-08-23 by re-run 2; this section previously read "No code gaps").

**G1 — `showItemInFolder` opens the folder but does not SELECT the item, whenever the GameLib
window is frontmost.** (REQ-34.3-11 item 2; surfaced 2026-08-23.)

  **STATUS: RESOLVED 2026-08-23** — routed to debug session `finder-reveal-no-selection`, root
  cause found and fixed, verified 3/3 through the real button in the failing condition.
  Root cause was NOT what this entry hypothesised below (it is not about window activation
  state): it is a systemic macOS/Finder defect where the SELECTION half of a reveal is dropped
  whenever the previously-frontmost app's window is on a DIFFERENT display/Space than the one
  Finder's reveal window opens on. Confirmed in both directions on the same process with only
  the window position changed — external display 0/5, primary display 4/4. Fixed by
  `macos_finder_reselect_workaround()` in `src-tauri/src/main.rs`, which fires a follow-up
  Finder `select` Apple Event after the reveal.
  CAVEAT CARRIED FORWARD: the first version of that fix was DEAD CODE (missing an `as alias`
  coercion; errored 5/5, silently) and passed its own 8/8 verification because that verification
  measured a REPLICA rather than the shipped script. Corrected and re-verified. No regression
  test exists — see the debug session's Follow-ups section.

  Severity: LOW-to-MEDIUM. Nothing is lost or corrupted and the user does reach the right
  folder; the item they were sent to find is simply not highlighted. But it IS the literal
  acceptance criterion of REQ-34.3-11 item 2, and it fails in the ONLY configuration real
  users are ever in — the button lives inside GameLib, so GameLib is frontmost when clicked.

  Reproduction WITHOUT GameLib's code (this is the key evidence): a standalone Swift binary
  calling `NSWorkspace.activateFileViewerSelecting` selects correctly 20/20 with Terminal
  frontmost and 6/6 with Safari frontmost, but 0/16 with `gamelib-shell` frontmost. The
  caller is identical in all three; only the frontmost application differs. So this is NOT a
  defect in `loggerFlowRegistration.ts`, the `rustInvoke` leg, or `tauri-plugin-opener`'s
  argument handling — every one of those is exonerated by the folder opening correctly on
  100% of trials.

  What it most likely IS: something about how the wry/Tauri `gamelib-shell` window resigns
  active state, such that Finder's window never becomes key at the moment the selection would
  be applied. NOT root-caused here — that is a debug task, not a UAT task.

  NOT yet investigated (deliberately out of UAT scope): whether the packaged build behaves the
  same as this `tauri:dev` build; whether other reveal call sites (`showConfigFileInFolder`,
  `showLogFileInFolder`, the 12 link openers' sibling reveal paths) share it — they all funnel
  through the same `RUST_SHELL_SHOW_ITEM_IN_FOLDER` arm, so they very probably do.

  Recommended route: `/gsd-debug`. Do NOT re-open the phase on this alone without a decision
  from the operator — see the note in `34.3-VERIFICATION.md`'s 2026-08-23 addendum.
 Item 1's 2026-08-23 failure was diagnosed to the HOST, not to GameLib, and is
recorded as `blocked` rather than `issue` — blocked items are prerequisite gates, not defects,
and do not enter this section.

Two host-environment fixes are prerequisites for finishing this re-run:
  1. Set a default web browser (System Settings -> Desktop & Dock -> Default web browser).
     Currently NO http/https handler is registered at all.
  2. `defaults delete -g NSFileViewer` — APPLIED 2026-08-23 and since REFUTED as a
     cause: with the dangling handler restored, `open -R` still selects correctly, so it
     degraded nothing. Kept deleted (harmless), but it is NOT a prerequisite for anything.

One genuine (pre-existing, not introduced by this run) observation worth carrying forward: the
link-opener path is silent-by-construction end to end. `shell.openExternal` is a void
fire-and-forget, `preload/tauriTransport.ts:96` `send()` discards the invoke's rejection, and
the only diagnostic (`main.rs:5635`) goes to the `tauri:dev` terminal. On a host with no
browser handler the user gets a dead button and no signal anywhere in the app or in
`gamelib.log`. That is a UX/observability gap, not a correctness one, and it is NOT in scope
for this UAT — logged here so it is not lost.
