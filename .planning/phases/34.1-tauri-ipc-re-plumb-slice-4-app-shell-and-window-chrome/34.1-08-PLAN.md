---
phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome
plan: 08
type: execute
wave: 4
depends_on: ["34.1-01", "34.1-02", "34.1-03", "34.1-04", "34.1-05", "34.1-06", "34.1-07"]
files_modified:
  - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-PORTED-CHANNELS.md
  - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md
  - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VALIDATION.md
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
autonomous: true
requirements: [REQ-34.1-11, REQ-34.1-12]
must_haves:
  truths:
    - "A reader can see, per channel, exactly what shipped, by which of the THREE port kinds, and at what proof level."
    - "A reader cannot come away believing anyone looked at the window chrome, the tray, the frameless toggle, or gamepad navigation running."
    - "SEAM.md's deferred backlog no longer lists what this slice closed, and its ported section lists it."
    - "Every declared logged no-op, declared-empty return and re-deferral in this slice is named in one place."
  artifacts:
    - path: ".planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-PORTED-CHANNELS.md"
      provides: "The declared 33-channel list with the third port kind and the honest sign-off"
      contains: "renderer-side (Tauri JS)"
    - path: ".planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md"
      provides: "The deferred live-UAT item list, all deferred by D-15, none gating"
    - path: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      provides: "§3 rows moved to §1 (Incremental-Port Checklist steps 5/6)"
  key_links:
    - from: ".planning/IPC-PORT-INVENTORY.md"
      to: "34.1-PORTED-CHANNELS.md"
      via: "the Phase 35 completion gate reads the declared list against the slice list"
      pattern: "33"
---

<objective>
Close the slice the way SEAM.md's Incremental-Port Checklist steps 5 and 6 require: declare
what shipped, move the closed rows out of the deferred backlog, and record the deferred
live-UAT items.

Purpose, and the reason this plan is not paperwork: D-15 accepts that this phase closes
**unit-proven with ALL live UAT deferred**, and CONTEXT.md names the resulting tension in
unusually blunt terms. This slice's headline deliverable is *visual and interactive* — the
frameless toggle applying, the buttons actually moving the window, the tray appearing and
its colour changing, gamepad navigation still working, macOS traffic lights versus custom
buttons looking right — and jest structurally cannot prove any of it. **The phase will
close without anyone having looked at it.** That is the same class of claim that let
G-30-02 be declared fixed twice while the live build hung. The artifact this plan produces
is the only place that fact gets recorded, and it must not be softened.

Output: `34.1-PORTED-CHANNELS.md`, `34.1-HUMAN-UAT.md`, a populated `34.1-VALIDATION.md`,
and a SEAM.md §3→§1 move.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/IPC-PORT-INVENTORY.md
@.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-CONTEXT.md
@.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VALIDATION.md
@.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md

<interfaces>
<!-- The 33 channels and their port kind, as planned. The executor must reconcile this
     against what the seven preceding SUMMARY files actually record, and flag any drift. -->

renderer-side (Tauri JS) — 13:
  minimizeWindow, maximizeWindow, unmaximizeWindow, closeWindow, isMaximized,
  isMinimized, isFullscreen, setFullscreen, isFrameless, setZoomFactor   (plan 34.1-03, D-01/D-02)
  gamepadAction                                                          (plan 34.1-05, D-10)
  createNewWindow, showAboutWindow                                       (plan 34.1-07, D-12)

sidecar invoke — 8:
  getCustomThemes, getThemeCSS, getCustomCSS, getHeroicVersion,
  getLatestReleases, getCurrentChangelog, isIntelMac, getWebviewPreloadPath  (plan 34.1-04)

sidecar send — 11:
  changeLanguage, notify, quit, openReleases, openCustomThemesWiki, openWebviewPage,
  abort, lock, unlock, setTitleBarOverlay                                    (plan 34.1-04)
  changeTrayColor                                                            (plan 34.1-06, D-11)

already live via bootstrap, verified not re-registered — 1:
  set-connectivity-online                                                    (plan 34.1-04)

Total 13 + 8 + 11 + 1 = 33.  `callTool` is NOT here — reassigned to Phase 34.5 (D-14).

SEAM.md structure:
  §1 "Ported (real, wired, hardware-provable)" — each closed cluster is an `###` subsection
     titled e.g. "Lifecycle cluster: ... (real, Phase 33) — CLOSED, moved out of §3" at L270.
  §3 "Deferred (out of scope this phase, the incremental-port backlog)" at L347 — a table
     whose rows this slice partially closes:
       row 1 `app` (tray/protocol registration, "target Phase 34/35")
       row 3 `BrowserWindow` (full window management, "target Phase 34/35")
       row 6 `nativeImage` ("only needed once tray/notifications need custom icons")
       row 9 `protocol`/`powerSaveBlocker`/`clipboard`/`Tray`/`ipcMain` — the `Tray` entry
     and the "IPC re-plumb (the headline cost)" paragraph, whose running "28 wired/re-routed
     total" tally this slice advances.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 34.1-PORTED-CHANNELS.md with the third port kind and the honest sign-off</name>
  <files>.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/33-tauri-lifecycle-cluster/33-PORTED-CHANNELS.md (or whatever the Phase 33 directory is named — the declared-list format to follow, including its per-row proof-level column and its re-deferral section)
    - .planning/phases/32-*/32-PORTED-CHANNELS.md (the Phase 32 declared-list discipline)
    - .planning/phases/34.1-*/34.1-01-SUMMARY.md through 34.1-07-SUMMARY.md (ALL SEVEN — this document must reflect what actually shipped, not what was planned; reconcile every row against its plan's summary and flag any drift explicitly)
    - .planning/phases/34.1-*/34.1-CONTEXT.md (D-02's third-kind requirement and D-15's verbatim honest-tension wording)
    - .planning/IPC-PORT-INVENTORY.md (the corrected 33-channel slice-4 list)
  </read_first>
  <action>
Create `34.1-PORTED-CHANNELS.md` following the Phase 32/33 declared-list format, with these
mandatory sections:

**1. Port kinds.** State that this phase introduces a THIRD kind to the vocabulary and
define all three:
  - `sidecar invoke/send` — the channel is registered on the sidecar's `ipcMain` and the
    renderer reaches it over the Tauri transport (Phases 30-33's kind)
  - `rustInvoke` — the sidecar asks the Rust shell via `dispatch_rust_channel` (Phase 28+)
  - `renderer-side (Tauri JS)` — **new this phase (D-02)** — the preload/`window.api`
    implementation calls a Tauri JS API directly with no transport hop; the sidecar
    registers nothing for the channel, and `UNPORTED_CHANNEL_MARKER` is never reached
    because the preload short-circuits first
  State explicitly that the Phase 35 completion gate reads "the channel has real behavior
  under Tauri", not "the sidecar owns it" — so renderer-side counts as ported.

**2. The 33 rows.** One table, one row per channel, columns:
`channel | kind | backed by | proof level | notes`. Populate from `<interfaces>` above,
CORRECTED against the seven summaries. Proof level is `unit` for everything —
D-15 defers all live UAT — except `changeTrayColor`, whose Rust half is `compile-gate only`
(`cargo check`). Never write `verified` or `working` in this column.

**3. Declared logged no-ops and declared-empty returns.** One row each, with the reason:
  - `lock` / `unlock` — `powerSaveBlocker`, carried forward from Phase 33 D-08; the
    user-visible symptom is that the system may sleep during long depot downloads
  - `setTitleBarOverlay` — no native overlay left to target under D-06
  - `getWebviewPreloadPath` — declared-empty; the `<webview>` login story is Phase 34.4's
  - `isFullscreen` — declared-false under Tauri; the Electron body reads two Node-only
    env/CLI signals (`isSteamDeckGameMode || isCLIFullscreen`) with no webview equivalent.
    State plainly that this is NOT a window-fullscreen query and was deliberately not
    "improved" into one.

**4. Declared divergences from the Electron build.** At minimum:
  - D-06: when frameless is on, GameLib's own buttons render on ALL platforms under Tauri,
    where Electron uses native overlay controls on macOS/Windows. Declared, not discovered.
  - D-05 superset: the Tauri path applies `framelessWindow` live, where Electron's own
    Settings label says "requires restart".
  - D-10: gamepad directional navigation is computed geometrically by GameLib rather than
    by Chromium's spatial navigation, which the platform webviews do not provide.
  - Whether the frameless drag region works at all depends on whether the platform webview
    exposes `-webkit-app-region` in computed styles; plan 34.1-03 implements a `.Sidebar`
    fallback, and neither path has been observed running. Record this as an open question,
    not as a resolved one.

**5. Re-deferrals.** Name each and its target phase:
  - tray scope beyond the bounded minimum — recent-games submenu, protocol launch entries,
    About/Reload/Debug items, macOS dock menu, `languageChanged` rebuilds, `noTrayIcon` and
    `exitToTray` honouring, `nativeImage` per-platform resizing → Phase 35
  - the `<webview>` login story (navigation interception, OAuth redirect capture,
    session/cookie access) → Phase 34.4
  - real `powerSaveBlocker` parity → Phase 35 or a maintained Tauri v2 wake-lock plugin
  - real `setTitleBarOverlay` behavior → only if D-06 is ever revisited
  - `getLatestReleases` / `openReleases` still pointing at `heroicGithubURL` rather than the
    GameLib fork, now inconsistent with Phase 34's fork-pointed updater feed → Phase 35
  - `callTool` → Phase 34.5 (D-14), with the inventory correction noted

**6. Sign-off — the honest statement.** Reproduce D-15's tension without softening it. The
section must state, in these terms:
  > Every automated test in this phase proves **wiring**: that the right Tauri API was
  > called with the right arguments. **None proves the window on screen actually did the
  > thing.** Nobody has run this build and looked at it. The frameless toggle applying, the
  > titlebar buttons moving the window, the tray appearing and changing colour, gamepad
  > navigation working, and how macOS traffic lights versus custom buttons look are all
  > **unobserved**. `cargo check` proves the tray code compiles; it proves nothing about a
  > tray existing. Do not read "wired and unit-proven" as "seen working" — that is exactly
  > how G-30-02 was declared fixed twice while the live build hung.

Do not add mitigating phrasing ("should work", "expected to", "high confidence") anywhere
in section 6.

**7. Reconciliation.** State the channel count (33), confirm it matches
`.planning/IPC-PORT-INVENTORY.md`'s corrected slice-4 heading, and confirm `callTool` is
absent. If any of the seven summaries records a channel shipping differently from the plan
(different kind, deferred, or absent), say so explicitly in this section rather than
silently writing the planned value into the table.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=".planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-PORTED-CHANNELS.md"; test -f "$f"; grep -q "renderer-side (Tauri JS)" "$f"; grep -q "None proves the window on screen actually did the thing" "$f"; grep -q "G-30-02" "$f"; grep -q "34.5" "$f"; grep -vE "^\s*(#|>)" "$f" | grep -qi "callTool" && { echo "callTool listed as ported"; exit 1; }; n=$(grep -cE "^\| \`[a-zA-Z.\-]+\` \|" "$f"); echo "rows=$n"; test "$n" -ge 33; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and contains all seven numbered sections.
    - The channel table has exactly 33 rows and the set of channel names equals the corrected Phase 34.1 list in `.planning/IPC-PORT-INVENTORY.md`. Verify the set equality, not just the count.
    - `renderer-side (Tauri JS)` appears as a defined kind and is used by exactly 13 rows.
    - No row's proof-level cell contains `verified`, `working`, `confirmed`, or `hardware`.
    - Section 6 contains the literal sentence `None proves the window on screen actually did the thing.` and mentions `G-30-02`.
    - Section 6 contains none of `should work`, `expected to work`, `high confidence`, `we believe`.
    - `callTool` appears only in the re-deferral section (as reassigned to 34.5), never as a ported row.
  </acceptance_criteria>
  <done>The declared list exists with the third port kind, and the honest sign-off is recorded in terms a later reader cannot misread.</done>
</task>

<task type="auto">
  <name>Task 2: Record the deferred live-UAT items and populate the validation map</name>
  <files>.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md, .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/34.1-*/34.1-VALIDATION.md (the § Manual-Only Verifications table and the § Validation Sign-Off checklist to complete)
    - .planning/phases/33-*/33-HUMAN-UAT.md and .planning/phases/34-*/34-HUMAN-UAT.md (the deferred-UAT artifact format used by prior phases in this series)
    - .planning/phases/34.1-*/34.1-01-SUMMARY.md through 34.1-07-SUMMARY.md (each plan's own "DEFERRED to UAT" verification notes)
    - .planning/phases/34.1-*/34.1-PORTED-CHANNELS.md (Task 1's output — the two documents must not contradict each other)
  </read_first>
  <action>
Create `34.1-HUMAN-UAT.md` in the prior phases' format. Every item is **deferred — none
gates phase closure** (D-15), and the document must open by saying so, together with the
reason it is worth writing anyway: these are the only checks that can prove the slice's
actual deliverable.

Items, each with concrete reproduction steps and an explicit expected result:
  1. Window buttons — `pnpm tauri:dev`, click minimize / maximize / unmaximize / close in
     GameLib's own titlebar (frameless on) and confirm the real OS window responds.
  2. Frameless toggle — toggle `framelessWindow` in Settings; confirm the chrome changes,
     that it applies without a restart, and that on a fresh profile the window starts
     DECORATED (the Electron default).
  3. No startup flip — restart with `framelessWindow` on; confirm the window does not
     visibly flip from decorated to undecorated during load.
  4. Frameless drag — drag the sidebar to move the window and double-click it to toggle
     maximize. **Explicitly flagged as the highest-risk unobserved item**: it depends on
     whether the platform webview exposes `-webkit-app-region` in computed styles, with a
     `.Sidebar` selector fallback; neither path has been run.
  5. Zoom — change the zoom setting and confirm the UI scales, including on a sub-1200px
     display where the DPI compensation branch applies.
  6. Tray — confirm a tray icon appears at startup, that clicking it shows/focuses the
     window, that both menu items work, and that toggling the dark-tray-icon setting swaps
     the image within ~500ms.
  7. Gamepad — with a controller connected, navigate the `/console` routes: d-pad and left
     stick move focus in the expected direction, tab/shiftTab traverse, B/back navigates,
     the right stick scrolls in the expected direction (the sign convention most likely to
     be inverted), and the stick clicks activate the element under the cursor.
  8. Child windows — click a ProtonDB / AppleWiki / AreWeAntiCheatYet link and confirm a
     real window opens with the page; open About and confirm it shows the REAL version
     string, not `unknown` or `0.0.0`.
  9. macOS traffic lights vs custom buttons — the deferred aesthetic call (D-06/D-15);
     look at it on macOS with `framelessWindow` on and decide.
 10. Electron parity — `npm start` still launches and the themes / language / About /
     releases / changelog surfaces still behave, confirming the D-07 `main.ts` extraction
     changed no behavior.

Close the document with the same honest statement Task 1's section 6 carries, so neither
artifact can be read in isolation and give the wrong impression.

Then update `34.1-VALIDATION.md`:
  - populate the § Per-Task Verification Map with one row per plan task (plans 34.1-01
    through 34.1-08), each row naming its `<automated>` command and its status
  - mark the § Wave 0 Requirements checkboxes against the test files that actually exist
  - complete the § Validation Sign-Off checklist honestly — including
    `cargo check` green for the Rust-side tray, and the deferred-UAT items being recorded
    verbatim
  - set `nyquist_compliant: true` in the frontmatter ONLY if every task in every plan has
    an `<automated>` verify and no three consecutive tasks lack one; otherwise leave it
    false and say why in the § Validation Sign-Off approval line
  </action>
  <verify>
    <automated>bash -c 'set -e; d=".planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome"; test -f "$d/34.1-HUMAN-UAT.md"; grep -q "deferred" "$d/34.1-HUMAN-UAT.md"; grep -qi "traffic light" "$d/34.1-HUMAN-UAT.md"; grep -q "None proves the window on screen actually did the thing" "$d/34.1-HUMAN-UAT.md"; n=$(grep -cE "^\s*[0-9]+\." "$d/34.1-HUMAN-UAT.md"); test "$n" -ge 10; grep -q "nyquist_compliant:" "$d/34.1-VALIDATION.md"; grep -q "34.1-06" "$d/34.1-VALIDATION.md"; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `34.1-HUMAN-UAT.md` exists with at least 10 numbered items, each carrying reproduction steps and an expected result.
    - The document states in its opening that every item is deferred and none gates closure.
    - Item 4 (frameless drag) is explicitly flagged as the highest-risk unobserved item.
    - `34.1-VALIDATION.md`'s Per-Task Verification Map has one row per task across plans 34.1-01..08, each naming a concrete command.
    - `34.1-VALIDATION.md`'s Wave 0 checkboxes reflect files that actually exist on disk (verify each referenced test path with `test -f`).
    - The § Validation Sign-Off approval line is filled in with a real assessment, not left as `pending`.
  </acceptance_criteria>
  <done>The deferred live checks are written down in enough detail to be run later, and the validation contract is complete and honest.</done>
</task>

<task type="auto">
  <name>Task 3: Move the closed rows from SEAM.md §3 into §1 (checklist steps 5/6)</name>
  <files>.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</files>
  <read_first>
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md (§1's Phase 33 subsection at ~L270 for the "CLOSED, moved out of §3" heading and body style; §3's deferred table at ~L347 including rows 1, 3, 6 and 9; and the "IPC re-plumb (the headline cost)" paragraph with its running wired tally)
    - .planning/phases/34.1-*/34.1-PORTED-CHANNELS.md (Task 1's output — SEAM.md's new subsection summarizes it and must not contradict it)
    - .planning/phases/34.1-*/34.1-CONTEXT.md (SEAM.md is named the governing document; the Incremental-Port Checklist step 5 is what this task performs)
  </read_first>
  <action>
Add a new `###` subsection at the end of SEAM.md §1, titled in the established style:
`### App shell + window chrome cluster (real, Phase 34.1) — CLOSED, moved out of §3`.
Its body must, in SEAM.md's own voice:
  - name the 33 channels by kind, pointing at `34.1-PORTED-CHANNELS.md` for the per-row detail
  - introduce the third port kind `renderer-side (Tauri JS)` into SEAM.md's vocabulary,
    since it is now part of how this seam works and later slices will meet it
  - name what is closed on the Rust side: the `tray-icon` Cargo feature, the bounded
    `TrayIcon`, and the single new `tray_set_icon` `dispatch_rust_channel` arm (bringing
    the arm count from 10 to 11)
  - name the new capability grants (the twelve window/webview commands plus
    `core:webview:allow-create-webview-window`) and the fail-closed child-window boundary
    (`windows: ["main"]`, child labels match nothing)
  - state plainly that this is the FIRST slice to modify `src/backend/main.ts`, so the
    additive/reversible invariant is now BEHAVIORAL rather than textual, and that
    `electronUntouched.test.ts` was inspected, found to be Phase 28's keyring/`configStore`
    byte-identity proof with no assertion about `main.ts`, left unmodified, and supplemented
    with a NEW `appShellImportGate.test.ts`
  - state that the whole cluster is unit-proven with ALL live UAT deferred (D-15), pointing
    at `34.1-HUMAN-UAT.md`

Then update §3's deferred table in place — edit the affected rows' "What's needed to port"
cells, do not delete rows other slices still need:
  - row 1 `app`: mark tray registration CLOSED (Phase 34.1, bounded — see the re-deferral
    list) and leave updater hooks / protocol registration deferred
  - row 3 `BrowserWindow` (full window management): mark **partially closed, Phase 34.1** —
    real multi-window now exists via `WebviewWindow` for `createNewWindow`/`showAboutWindow`;
    what remains deferred is the `<webview>` login story (navigation interception,
    session/cookie access), target **Phase 34.4**
  - row 6 `nativeImage`: mark partially addressed — the tray uses compile-time
    `include_bytes!` images rather than `nativeImage`; per-platform resizing stays deferred
  - row 9: mark `Tray` CLOSED (Phase 34.1, bounded); leave `powerSaveBlocker` as the
    existing logged no-op and `protocol`/`clipboard`/`ipcMain` untouched
  - the "IPC re-plumb (the headline cost)" paragraph: advance the running tally from
    "28 wired/re-routed total" to "61 wired/re-routed total" (28 + this slice's 33),
    naming Phase 34.1 and `34.1-PORTED-CHANNELS.md` in the same style as the Phase 30/31/32
    clauses, and noting that `callTool` moved to the Phase 34.5 slice (D-14).

Do NOT touch §"Load-Bearing Invariants" or §"Incremental-Port Checklist" — this slice
followed them, it does not amend them.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"; grep -q "App shell + window chrome cluster (real, Phase 34.1) — CLOSED" "$f"; grep -q "renderer-side (Tauri JS)" "$f"; grep -q "61 wired/re-routed total" "$f"; grep -q "34.1-PORTED-CHANNELS.md" "$f"; grep -q "34.1-HUMAN-UAT.md" "$f"; awk "/^## 3\./,/^## Accepted Constraints/" "$f" | grep -q "Phase 34.1"; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - SEAM.md §1 ends with the new `### App shell + window chrome cluster (real, Phase 34.1) — CLOSED, moved out of §3` subsection.
    - That subsection names the third port kind, the `tray_set_icon` arm, the capability grants, the fail-closed child-window boundary, the `main.ts` behavioral-invariant change, and the `electronUntouched.test.ts` finding.
    - §3 rows 1, 3, 6 and 9 each mention Phase 34.1 with an accurate closed/partially-closed status.
    - The headline-cost paragraph reads `61 wired/re-routed total` and names Phase 34.1 and `callTool`'s move to 34.5.
    - `## Load-Bearing Invariants` and `## Incremental-Port Checklist` are byte-unchanged: `git diff HEAD -- .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md | grep -A2 -B2 "Load-Bearing Invariants\|Incremental-Port Checklist"` shows no `+`/`-` lines inside those sections.
    - `34.1-PORTED-CHANNELS.md`'s channel set and SEAM.md's new subsection do not contradict each other on any channel's port kind.
  </acceptance_criteria>
  <done>SEAM.md's ported section owns what this slice closed, its deferred backlog no longer claims it, and the running tally reconciles with the inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this slice's security posture → the Phase 35 cutover reader | `34.1-PORTED-CHANNELS.md` and SEAM.md are what a future agent reads to decide whether the cutover is safe. Their accuracy IS a security control. |
| declared no-ops → downstream assumptions | A channel silently believed working when it is a logged no-op leads to features shipped on nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.1-32 | Repudiation | The declared list softening "wired and unit-proven" into language a later reader takes as "seen working" — the precise failure mode that let G-30-02 be declared fixed twice while the live build hung | mitigate | Task 1 mandates a verbatim sign-off sentence and Task 1's acceptance criteria assert both its presence and the ABSENCE of hedging phrases (`should work`, `high confidence`, …). Task 2 repeats it in the UAT artifact so neither document can be read in isolation and mislead. |
| T-34.1-33 | Repudiation | A capability grant, a new Rust arm, or a fail-closed boundary shipping without appearing in SEAM.md, so the Phase 35 security review misses it | mitigate | Task 3 requires the new SEAM subsection to name the twelve window/webview grants, `core:webview:allow-create-webview-window`, the `tray_set_icon` arm (10 → 11), and the child-window `windows: ["main"]` boundary explicitly. |
| T-34.1-34 | Tampering | The declared list being written from the PLANS rather than from what shipped, hiding a drift between intent and reality | mitigate | Task 1 requires reading all seven SUMMARY files and reconciling row by row, with a dedicated section 7 for recording any drift rather than silently writing the planned value. |
| T-34.1-35 | Repudiation | The Phase 35 completion gate failing to reconcile because the declared count and the inventory disagree | mitigate | Task 1 asserts set equality (not just count equality) against `.planning/IPC-PORT-INVENTORY.md`'s corrected slice-4 list; Task 3 advances SEAM.md's running tally to match. |
| T-34.1-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan touches only `.planning/` documentation and installs nothing. |
</threat_model>

<verification>
- `34.1-PORTED-CHANNELS.md` lists 33 channels whose name set equals the inventory's corrected slice-4 list.
- `34.1-HUMAN-UAT.md` has at least 10 deferred items, none gating.
- `34.1-VALIDATION.md`'s per-task map is populated and its sign-off is filled in.
- SEAM.md §1 has the new closed subsection; §3 rows 1/3/6/9 and the headline-cost tally are updated; the Invariants and Checklist sections are byte-unchanged.
- `pnpm jest` full suite still green (this plan changes no code).
</verification>

<success_criteria>
- Three port kinds are defined and every one of the 33 channels is assigned one.
- The honest sign-off is present verbatim in two places, with hedging language provably absent.
- SEAM.md's ported/deferred split and running tally reconcile with the inventory.
- Every logged no-op, declared-empty return, divergence and re-deferral is named in one document.
</success_criteria>

<output>
Create `.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-08-SUMMARY.md` when done
</output>
