---
phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome
plan: 15
type: execute
wave: 4
gap_closure: true
depends_on: [34.1-14]
files_modified:
  - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md
  - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/todos/pending/tray-dark-light-icons-are-identical.md
autonomous: true
requirements: [REQ-34.1-11]

must_haves:
  truths:
    - "`gsd-sdk query audit-uat` reports Phase 34.1 with a NON-ZERO item count equal to the number of genuinely open items"
    - "Every item resolved by this gap cycle has been MOVED out of `human_verification`, not annotated in place"
    - "`34.1-VERIFICATION.md`'s `status` is still `human_needed`"
    - "Every compound UAT item is split at its branch boundary, so an un-run half cannot disappear behind a pass"
    - "The four hardware/platform-gated items each carry a greppable `blocked_by` and none is faked as agent-verifiable"
  artifacts:
    - path: ".planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md"
      provides: "an audit-tool-correct ledger"
      contains: "human_verification_resolved"
    - path: ".planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md"
      provides: "the three-session narrative with per-sub-item results"
      contains: "sessions:"
  key_links:
    - from: "34.1-14-SUMMARY.md"
      to: "34.1-HUMAN-UAT.md / 34.1-VERIFICATION.md"
      via: "verbatim transcription of live results"
      pattern: "closed_by"
    - from: "34.1-VERIFICATION.md human_verification"
      to: "gsd-sdk query audit-uat"
      via: "parseVerificationItems only emits items when status is human_needed"
      pattern: "status: human_needed"
---

<objective>
Write plan 34.1-14's live results back into the ledger and reconcile the phase's planning artifacts
to the post-gap-cycle truth.

Purpose: this step has already failed on this project twice, in opposite directions.
`34.1-VERIFICATION.md`'s own `ledger_defect_note` records that until 2026-08-13 the file still
listed all 10 items as awaiting human verification a month after the 2026-07-25 session resolved 8
of them, so `gsd-sdk query audit-uat` over-counted 34.1 by 8 items for a month. Its
`audit_tool_note` records the opposite failure: setting `status` to `gaps_found` makes the phase
yield ZERO items and vanish from the audit entirely, verified live on 2026-08-13 when 34.1 dropped
from 10 open items to 0. A verify session that does not write back leaves the audit reporting the
pre-session state indefinitely.

Output: an audit-tool-correct ledger, a reconciled ROADMAP/REQUIREMENTS/STATE, and a closed todo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md
@.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md

<the_two_silent_failure_modes>
Both are documented in `34.1-VERIFICATION.md`'s own `audit_tool_note`, both were verified live, and
both are SILENT — the tool reports success either way. Honour them as acceptance criteria, not as
prose:

1. **`status` MUST stay `human_needed`.** `sdk/dist/query/uat.js` admits a VERIFICATION.md when
   status is `human_needed` OR `gaps_found` (line 306), but `parseVerificationItems` only emits
   items when status is `human_needed` (line 221). So `gaps_found` is admitted and then always
   yields ZERO items, and the phase disappears from the audit. Do NOT "improve" this field to
   reflect that gaps exist.

2. **Resolved items must be MOVED, not annotated.** The tool counts EVERY entry in
   `human_verification` regardless of any `result:` field. An entry left in place with
   `result: "PASS"` still counts as open. Move it into `human_verification_resolved`, a plain YAML
   key the tool does not read, which exists purely to preserve history without over-counting.
</the_two_silent_failure_modes>

<the_compound_item_lesson>
UAT test 5 recorded a PASS across TWO sessions while the branch it was written to cover, the
sub-1200px DPI-compensation path, was never exercised by either. The recorded lesson: a compound
test item resolves to a single pass/fail, and the un-run half disappears. Split compound items at
the branch boundary.

Apply it to every item this plan touches, not only test 5. Test 6 is the other obvious one: its
four sub-items collapsed into a single "issue" result on 2026-07-25, which is exactly why sub-items
1-3 then sat un-re-run for three weeks while the document read as though the whole item had been
handled.
</the_compound_item_lesson>

<gsd_sdk_state_write_hazard>
`gsd-sdk` state-WRITE commands have corrupted `STATE.md` on this project repeatedly — most recently
`state.record-session` silently changed `completed_plans` 247 to 248 while setting `percent` 92 to
65. **Edit `STATE.md` by hand**, then re-read it and verify every numeric field against the actual
counts before finishing. Do not run a state-write command as a shortcut. Read-only queries
(`gsd-sdk query audit-uat`, `gsd-sdk query state.load`) are safe.
</gsd_sdk_state_write_hazard>

<blocker_records_rot>
A `blocked_by` / `parked_to_phase` record rots silently — phase 27 sat blocked across four phases
that had removed its cause. Write every `blocked_by` here so a grep can falsify it: name the
concrete missing thing (`a Windows or Linux machine`, `a game controller`, `a sub-1200px-wide
display`), not a vague category word. Where an item is unblocked by a specific artifact, cite that
artifact as a file path.
</blocker_records_rot>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe live results into 34.1-HUMAN-UAT.md, splitting every compound item</name>
  <files>.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-14-SUMMARY.md
      (the source of every result transcribed here — transcribe verbatim, do not paraphrase)
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md
      (the whole file — the frontmatter `sessions:` list, the ten test entries, the `## Gaps` block,
      `## D-06 reversal`, and the `## Summary` counts)
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-09-SUMMARY.md
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-11-SUMMARY.md
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-12-SUMMARY.md
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-13-SUMMARY.md
  </read_first>
  <action>
    1. Add a THIRD entry to the frontmatter `sessions:` list for plan 34.1-14's session, with its
       date, its scope (items 1, 2, 6, 9 plus the frameless-toggle presentation decision), and its
       outcome counts. Keep both existing entries byte-unchanged.

    2. SPLIT every compound item this cycle touched, at its branch boundary:
       - Test 1 becomes 1a and 1b. 1a is Windows/Linux: GameLib's own buttons sit at the window's
         top edge and minimize/maximize/restore/close the real OS window. NOT ATTEMPTED,
         `blocked_by: a Windows or Linux machine`. Note that plan 34.1-09 re-anchored
         `grid-area: content` to `grid-area: navbar` and that
         `src/frontend/components/UI/NavShell/__tests__/windowControlsPlacement.test.ts` gates the
         placement statically by recomputing the row from `.App`'s own `grid-template-areas`.
         1b is macOS: GameLib's own buttons are ABSENT and the native traffic lights are present,
         from 34.1-14 Task 1 items A1/A2, recorded per theme.
       - Test 5 becomes 5a and 5b. 5a is zoom levels 200% / 60% / 100%, PASS, re-confirmed
         2026-08-13. 5b is the sub-1200px-wide display / DPI-compensation branch, NEVER VERIFIED in
         any session, `blocked_by: a sub-1200px-wide display`. Preserve the existing `lesson:` line
         on 5b.
       - Test 6 becomes 6a tray icon appears at startup, 6b clicking the tray shows/focuses the
         window, 6c the "Show GameLib" and "Quit" menu items, 6d the dark-tray-icon colour swap.
         Record 34.1-14 Task 3 steps 1 through 6 against these four individually.
       - Test 9 becomes separate observations from 34.1-14 Task 1, at the granularity that plan's
         block D actually produced. At minimum: native traffic lights present (all three stress
         themes — `midnightMirage`, `gruvbox_dark`, `dracula`); with frameless OFF the nav tabs
         have NO 78px gap; with frameless ON the nav tabs clear the 78px reserve; and each of the
         eight mechanism items L2-L9 as its own line — overlay applies at runtime (L2), ON-transition
         visual quality (L3), overlaid-state tab clearance (L4), visible restores at runtime (L5),
         OFF-transition visual quality (L6), no startup flip with frameless ON (L7), no startup
         change with frameless OFF (L8), and unfocused drag plus title overdraw in overlaid state
         (L9). Do NOT merge L7 and L8, and do NOT merge L2 and L5 — they are opposite directions of
         the same setter and either can fail alone. This is the compound-item lesson applied to the
         items this cycle actually produced.

    3. Update the `## Gaps` block. Do NOT delete a resolved gap entry — this document's own
       convention is to preserve superseded results rather than erase them, as test 9's
       `superseded_result:` shows. For each of the four `status: failed` gaps: if 34.1-14
       adjudicated it PASS, change `status:` to `closed`, add `closed_by:` naming the plan(s), and
       add `closed_evidence:` quoting the live observation. If 34.1-14 recorded a FAIL, leave
       `status: failed` and update `reason:` and `missing:` to the current truth instead. Never
       close a gap on static evidence alone.

    4. Append any NEW gap 34.1-14 discovered, in the existing
       `truth: / status: / reason: / severity: / test: / artifacts: / missing:` shape. In
       particular: if the tray swap is STILL a no-op against byte-distinct assets, that is a NEW
       gap naming the `rustInvoke` leg and `app.tray_by_id(TRAY_ICON_ID)` returning `None`
       (`src-tauri/src/main.rs:3179-3188`) as the two remaining candidates. It is NOT a reopening
       of the asset gap, which the byte-distinctness and luminance-ordering evidence closes on its
       own terms.

    5. Record the macOS frameless-toggle decision from 34.1-14 Task 2 as a dated decision paragraph
       in the same style as `## D-06 reversal`, including the user's verbatim reasoning.

       **This paragraph is the one this gap cycle's revision exists to protect.** An earlier draft
       of plans 34.1-10 and 34.1-14 asserted that Tauri v2 exposes no runtime setter for
       `titleBarStyle` and that macOS overlay was therefore permanent. That claim is FALSE:
       `Window.setTitleBarStyle(style: 'visible' | 'transparent' | 'overlay')` exists at
       `node_modules/@tauri-apps/api/window.d.ts:1185`, is implemented as
       `invoke('plugin:window|set_title_bar_style', ...)` in `window.js:1541`, and plan 34.1-10
       granted its capability. So:
         - The paragraph MUST record which PATH (A or B) 34.1-14 Task 2 determined, and the
           observations that determined it, before recording the chosen option.
         - It MUST NOT contain any wording asserting that Tauri lacks a runtime titlebar API, that
           the overlaid state is permanent, or that `framelessWindow` is inherently decoration-inert
           on macOS. If 34.1-14 landed on PATH B, record the SPECIFIC observed failure verbatim
           (e.g. "toggling ON produced no visible change") as the reason, and cite it as an open
           mechanism question, not a settled platform limit.
       If an implementation option was chosen, state explicitly that it is NOT yet implemented and
       needs a follow-up gap-cycle plan.

    6. Under test 10 (Electron parity), add that it is now OWED AND URGENT: plan 34.1-11
       deliberately stops `WindowControls` rendering on macOS under BOTH shells, so the Electron
       macOS "both sets of buttons" behaviour changed by design, and it must be re-run before
       Phase 35 removes the Electron build and the comparison becomes impossible forever.

    7. Under test 7 (gamepad), restate it as STILL UNVERIFIED with
       `blocked_by: a game controller`, keeping both named risks (the right-stick scroll sign
       convention, most likely inverted; Up/Left directional focus from a cold start with nothing
       focused) and the cross-reference to Phase 34.10's independently-tracked never-measured
       gamepad focus-scroll.

    8. Under test 8 (child windows), restate it as STILL UNVERIFIED with its security-relevant
       claim intact: `createNewWindow` loads renderer-supplied REMOTE content into a child
       `WebviewWindow` with no `<webview>` isolation boundary, and the zero-command-access design
       (capability scoped to the `main` window only) has only ever been confirmed statically. Do
       NOT weaken that scoping and do NOT record it as lower risk because plan 34.1-10 gated the
       capability file as unchanged — a static gate is not a live confirmation.

    9. Rewrite `## Summary`'s counts to match the split item set exactly. RECOUNT from the entries;
       do not adjust the old numbers arithmetically.
  </action>
  <verify>
    <automated>grep -c "blocked_by:" .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The frontmatter `sessions:` list has at least 3 entries and both pre-existing entries are
      byte-unchanged (confirm with `git diff` on that region).
    - Tests 1, 5, 6 and 9 each appear as split sub-items with an individual result per sub-item.
    - `grep -c "blocked_by:"` on the file returns at least 4.
    - `grep "blocked_by:"` output shows each value naming a concrete missing thing — the strings
      `a Windows or Linux machine`, `a game controller` and `a sub-1200px-wide display` all appear.
      A bare category word such as `physical-device` or `platform` alone is not acceptable.
    - No gap entry was deleted: the `## Gaps` block still contains all four original `truth:` lines.
    - Every gap entry has a `status:` of exactly `closed` or `failed`; a `closed` entry also has
      `closed_by:` and `closed_evidence:`.
    - `## Summary`'s `total` equals the number of test entries after splitting, and its category
      counts sum to `total`.
    - Test 8's security-relevant claim about remote content in a child `WebviewWindow` is present
      and unweakened.
  </acceptance_criteria>
  <done>The UAT document records three sessions at per-sub-item granularity, and no un-run half is hiding behind a pass.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile 34.1-VERIFICATION.md against the audit tool's two silent failure modes</name>
  <files>.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md
      (the whole frontmatter — `status`, `audit_tool_note`, `ledger_defect_note`,
      `live_uat_sessions`, `human_verification`, `human_verification_resolved`,
      `open_items_after_2026_08_13`)
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md
      (post-Task-1 — the source of truth this file must agree with)
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-14-SUMMARY.md
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-13-SUMMARY.md
      (the macOS template-image open question this task adds as a ledger item)
  </read_first>
  <action>
    1. LEAVE `status: human_needed` EXACTLY AS IT IS. Gaps existing does not change it. Leave
       `audit_tool_note` and `ledger_defect_note` byte-unchanged — they are the standing warnings
       that keep the next editor from breaking this.

    2. Add a third entry to `live_uat_sessions` for plan 34.1-14's session, matching Task 1's
       frontmatter entry.

    3. For every entry in `human_verification` that plan 34.1-14 adjudicated as resolved, MOVE the
       whole entry into `human_verification_resolved`, rewriting its `result:` to state the date
       and the adjudicating evidence. Do NOT annotate it in place. Candidates, subject to 34.1-14's
       actual results: the macOS half of item 1; the tray creation / click / menu-item sub-items;
       the macOS traffic-lights item if all three themes passed; and the dark-tray-icon swap ONLY
       if 34.1-14 Task 3 steps 4 and 5 actually showed a change.

    4. REWRITE `human_verification` so it contains ONLY genuinely open items, each with `test:`,
       `expected:`, `why_human:`, `result:` and, where applicable, `blocked_by:`. It must include:
       - Windows/Linux `WindowControls` placement, `blocked_by: a Windows or Linux machine`
       - the sub-1200px DPI-compensation branch, `blocked_by: a sub-1200px-wide display`
       - gamepad, `blocked_by: a game controller`
       - child windows and the About version round-trip, with its security-relevant claim intact
       - the Electron parity re-run, marked as owed before Phase 35 and explicitly citing the
         deliberate macOS `WindowControls` behaviour change plan 34.1-11 made to the Electron build
       - any NEW item 34.1-14 produced, such as a still-no-op tray swap
       - the macOS template-image open question from plan 34.1-13: does the non-template icon read
         correctly on BOTH a light and a dark macOS menu bar? Record why a template image was NOT
         adopted (it is auto-inverted by the OS, which would make `darkTrayIcon` a macOS no-op and
         re-create the very symptom G3 fixed, by a different mechanism).

    5. Replace the `open_items_after_2026_08_13` block with a dated block for this cycle, RECOUNTED
       from the entries above rather than adjusted arithmetically from the old one.

    6. Add a short prose section under `### Human Verification Required` recording that gap cycle
       plans 34.1-09 through 34.1-15 closed G1's static half, G2, G3's asset half and G4, and
       naming what each still depends on for final closure.
  </action>
  <verify>
    <automated>gsd-sdk query audit-uat > /tmp/gsd-audit-341.json && grep -c '"phase": "34.1"' /tmp/gsd-audit-341.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: human_needed" 34.1-VERIFICATION.md` returns 1. The field was NOT changed
      to `gaps_found`.
    - `gsd-sdk query audit-uat` output contains a result object whose `phase` is `34.1`. If 34.1 is
      absent, the `status` field was broken — restore it and re-run.
    - That result object's `items` array is NON-EMPTY. A zero-length array is the `gaps_found`
      failure mode even if the field looks right; investigate before proceeding.
    - The number of entries under `human_verification:` equals the length of that `items` array.
      Record both numbers in the SUMMARY.
    - `human_verification_resolved` gained at least one entry, and every entry it gained was
      REMOVED from `human_verification` (verify with `git diff` — a moved entry shows as a deletion
      in one block and an addition in the other, not as an in-place edit).
    - `audit_tool_note` and `ledger_defect_note` are byte-unchanged (`git diff` shows no hunk
      touching them).
    - Every `blocked_by:` value in the file names a concrete missing thing, matching Task 1's.
  </acceptance_criteria>
  <done>The audit tool reports 34.1 with a truthful, non-zero open-item count, and history is preserved without over-counting.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP, REQUIREMENTS, STATE, and close the tray todo</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/todos/pending/tray-dark-light-icons-are-identical.md</files>
  <read_first>
    - .planning/ROADMAP.md (the `### Phase 34.1:` section around line 1296 — its `**Plans:**` count
      line and its eight-entry plan checklist; also read a neighbouring phase's section to copy the
      formatting of a multi-gap-cycle plan list, e.g. Phase 34.2's 30-plan block)
    - .planning/REQUIREMENTS.md (lines 536-547 — the twelve REQ-34.1 entries, all already `[x]`;
      and line 904's per-phase tally)
    - .planning/STATE.md (the frontmatter `progress:` block — `total_plans`, `completed_plans`,
      `percent` — and `stopped_at` / `last_activity`)
    - .planning/todos/pending/tray-dark-light-icons-are-identical.md (the todo to close)
    - .planning/phases/34.1-.../34.1-13-SUMMARY.md (the evidence that closes it)
  </read_first>
  <action>
    1. ROADMAP.md, Phase 34.1 section:
       - Change `**Plans:** 8/8 plans complete` to the new total, counting the seven gap plans
         (34.1-09 through 34.1-15). State it as `15/15 plans complete` only if all seven have
         SUMMARY files; otherwise state the true fraction.
       - Append seven `- [x] 34.1-NN-PLAN.md — <one-line objective>` entries to the plan checklist,
         in the same style as the existing eight.
       - Add a short gap-cycle note beneath the list recording that the cycle was driven by live
         UAT findings from three sessions, not by static verification, and that
         `34.1-VERIFICATION.md` remains `status: human_needed` by design.

    2. REQUIREMENTS.md: the twelve REQ-34.1 entries stay `[x]` — this cycle re-plans none of them,
       it repairs regressions and a reversed decision against requirements that were already
       satisfied. Do NOT mint new REQ IDs.

       **Plan 34.1-10 (wave 1) already amended REQ-34.1-02's text** from twelve to thirteen pinned
       capability identifiers, adding `core:window:allow-set-title-bar-style`. That amendment is
       deliberate and load-bearing — `capabilitiesDefault.test.ts` and the capability file's own
       description prose were moved with it. Do NOT revert it, and do NOT "correct" the count back
       to twelve. Verify it is still present before touching the file.

       Append a dated line to the Phase 34.1 provenance note (near line 904) recording that plans
       34.1-09..15 closed live-UAT gaps G1-G4 without adding requirements, that REQ-34.1-02's grant
       list grew by one identifier, and naming the requirement whose SCOPE changed in substance:
       REQ-34.1-03. Record its supersession precisely — the two halves are NOT superseded the same
       way, and conflating them would write the false constraint into the permanent record:
         - SUPERSEDED on macOS: the clause "GameLib's own titlebar buttons render on all three
           platforms, one code path, no per-platform branch". On macOS the OS draws the controls,
           so GameLib's own buttons no longer render there — plan 34.1-11's unconditional macOS
           gate.
         - AMENDED IN MECHANISM, NOT IN INTENT: the clause "the renderer reads the setting and
           calls `getCurrentWindow().setDecorations(!frameless)`". On macOS that call is replaced by
           `setTitleBarStyle('overlay'|'visible')`, because `setDecorations(false)` strips the
           native traffic lights. The requirement's actual intent — `framelessWindow` is honoured at
           RUNTIME and genuinely affects decorations, on every platform — is UPHELD, not superseded.
           Windows/Linux keep `setDecorations` unchanged.
       Do not rewrite the requirement text — record both notes, dated, beneath it, in the same way
       the phase's other superseded decisions are preserved. Do NOT write that the setting is
       decoration-inert on macOS unless 34.1-14 Task 2 landed on PATH B, and then only with the
       specific observation quoted as the reason.

    3. STATE.md — BY HAND, per `<gsd_sdk_state_write_hazard>`:
       - Update `stopped_at` and `last_activity` to describe this gap cycle's completion.
       - Update `progress.total_plans` and `progress.completed_plans` by adding the seven new plans
         to each (only the completed ones to `completed_plans`).
       - Recompute `percent` as `round(completed_plans / total_plans * 100)` and verify the
         arithmetic by hand.
       - Update `last_updated`.
       - RE-READ the file after writing and confirm every numeric field against the actual counts.
         A prior `state.record-session` run silently moved `percent` from 92 to 65 while
         incrementing `completed_plans` by one.

    4. Close the tray todo: move
       `.planning/todos/pending/tray-dark-light-icons-are-identical.md` to
       `.planning/todos/completed/` (use `git mv`), and append a dated resolution section to it
       naming plan 34.1-13, the new md5s, the luminance measurements, and — unsoftened — that the
       LIVE swap behaviour is adjudicated separately by UAT item 6d, which the asset fix does not
       by itself close.

       If plan 34.1-14 Task 3 step 4/5 showed NO change, do NOT move the todo. Instead update it in
       place: the asset defect is fixed, a second fault remains, and the two remaining candidates
       are the `rustInvoke` leg and `tray_by_id` returning `None`.
  </action>
  <verify>
    <automated>grep -n "Phase 34.1:" .planning/ROADMAP.md && grep -c "34.1-1[0-5]-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "34.1-0[9]-PLAN.md\|34.1-1[0-5]-PLAN.md" .planning/ROADMAP.md` returns 7.
    - ROADMAP's Phase 34.1 `**Plans:**` line states a total matching the number of `34.1-*-PLAN.md`
      files on disk (verify with `ls .planning/phases/34.1-*/34.1-*-PLAN.md | wc -l`).
    - REQUIREMENTS.md still shows exactly twelve REQ-34.1 entries and no new IDs were minted
      (`grep -c "REQ-34.1-" .planning/REQUIREMENTS.md` unchanged except for the added provenance
      line's references).
    - REQUIREMENTS.md contains a dated note under REQ-34.1-03 recording BOTH halves separately:
      the one-code-path / no-per-platform-branch clause SUPERSEDED on macOS, and the
      `setDecorations` clause AMENDED IN MECHANISM (to `setTitleBarStyle`) with its runtime-honoured
      intent UPHELD.
    - REQ-34.1-02 still names `core:window:allow-set-title-bar-style` and still says thirteen —
      plan 34.1-10's amendment was not reverted. `grep -c "allow-set-title-bar-style" .planning/REQUIREMENTS.md`
      returns at least 1.
    - No file this plan writes contains a claim that Tauri lacks a runtime `titleBarStyle` setter,
      that macOS overlay is permanent, or that `framelessWindow` is inherently decoration-inert on
      macOS. Verify with a grep across the six owned files before finishing.
    - STATE.md: `progress.completed_plans / progress.total_plans * 100`, rounded, equals
      `progress.percent`. Show the arithmetic in the SUMMARY.
    - STATE.md was edited by hand — no `gsd-sdk` state-write command appears in the SUMMARY's
      command log.
    - The tray todo is either in `.planning/todos/completed/` with a dated resolution section, or
      still in `pending/` with an updated second-fault description. Exactly one of these, matching
      34.1-14's actual result.
    - `git status --porcelain .planning/` shows only the six files this plan owns.
  </acceptance_criteria>
  <done>Every planning artifact agrees with what actually happened, and the numbers were checked by hand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger -> downstream tooling | `gsd-sdk query audit-uat` and every future planner read these files as ground truth. A wrong ledger silently mis-scopes later phases. |
| ledger -> future readers | A closed item that was never observed becomes indistinguishable from one that was. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.1-15-01 | Repudiation | `34.1-VERIFICATION.md` `status` field | mitigate | Setting `status: gaps_found` makes `parseVerificationItems` emit zero items, so the phase VANISHES from `audit-uat` while the file still looks correct — verified live, 34.1 went from 10 items to 0. Task 2's acceptance criteria assert `status: human_needed` is unchanged AND that the audit returns a non-empty `items` array for 34.1, which is the only check that catches the failure rather than the field. |
| T-34.1-15-02 | Repudiation | resolved entries annotated in place | mitigate | The tool counts every entry in `human_verification` regardless of `result:`, so an in-place annotation over-counts forever. Task 2 requires a MOVE and verifies it via `git diff` shape (deletion in one block, addition in the other), not by reading the final file. |
| T-34.1-15-03 | Tampering | `STATE.md` numeric fields | mitigate | `gsd-sdk` state-write commands have corrupted this file repeatedly, most recently moving `percent` 92 to 65 while incrementing `completed_plans`. Task 3 mandates hand-editing, a post-write re-read, and an explicit arithmetic check recorded in the SUMMARY; the acceptance criteria forbid a state-write command appearing in the command log. |
| T-34.1-15-04 | Repudiation | stale `blocked_by` records | mitigate | A blocker record rots silently — phase 27 sat blocked across four phases that had removed its cause. Both tasks require every `blocked_by` to name a concrete, greppable missing thing so a later reader can falsify it in one command, rather than a category word that reads true forever. |
| T-34.1-15-05 | Information Disclosure | transcribed terminal output | mitigate | Plan 34.1-14 may have captured `pnpm tauri:dev` terminal output containing home-directory paths or, historically on this project, a Steam account identifier. Transcribe only redacted forms, and record in the SUMMARY what was redacted. |
| T-34.1-15-07 | Repudiation | ledgering a false technical constraint | mitigate | This plan writes the permanent record. An earlier draft of plans 34.1-10/34.1-14 carried a verified-FALSE claim that Tauri exposes no runtime `titleBarStyle` setter (it does — `@tauri-apps/api/window.d.ts:1185`), and this plan would have transcribed a product decision made on that premise into `34.1-HUMAN-UAT.md`'s dated decision paragraph and REQUIREMENTS.md's REQ-34.1-03 note, where it would outlive every plan that produced it. Task 1 step 5 and Task 3 step 2 both forbid that wording explicitly, require the determined PATH and its evidence to be recorded before the outcome, and require any negative result to be recorded as the specific observation and raised as an open mechanism question. An acceptance criterion greps the owned files for the forbidden claim. |
| T-34.1-15-06 | Elevation of Privilege | weakening test 8's recorded claim | mitigate | Test 8 carries the phase's only live-unconfirmed security claim: remote content in a child `WebviewWindow` with no isolation boundary, held safe only by capabilities scoped to the `main` window. Task 1 step 8 and Task 2 step 4 both require it restated unweakened, and explicitly forbid downgrading it on the strength of plan 34.1-10's STATIC capability gate. |
| T-34.1-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages and touches no source file. No RESEARCH.md Package Legitimacy Audit entry is required and no legitimacy checkpoint applies. |
</threat_model>

<verification>
- `gsd-sdk query audit-uat` returns a `34.1` result whose `items` array is non-empty, and whose
  length equals the number of `human_verification` entries.
- `grep -c "^status: human_needed"` on `34.1-VERIFICATION.md` returns 1.
- `grep "blocked_by:"` on both ledger files shows only concrete, greppable missing things.
- No owned file asserts that Tauri lacks a runtime `titleBarStyle` setter or that the macOS overlaid
  state is permanent.
- STATE.md's `percent` matches `round(completed_plans / total_plans * 100)`.
- `ls .planning/phases/34.1-*/34.1-*-PLAN.md | wc -l` matches ROADMAP's `**Plans:**` total.
- `git status --porcelain .planning/` shows only this plan's six owned files (plus the todo's move).
- No source file under `src/`, `meta/`, `public/` or `src-tauri/` was modified.
</verification>

<success_criteria>
The audit tool reports 34.1 truthfully with a non-zero count; resolved items were moved rather than
annotated; every compound item is split at its branch boundary with a greppable blocker; STATE.md's
arithmetic was checked by hand; nothing unobserved is recorded as observed; and no false technical
constraint is written into the permanent record.
</success_criteria>

<output>
Create `.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-15-SUMMARY.md` when done.
Include: the `human_verification` entry count and the `audit-uat` `items` length side by side; the
STATE.md arithmetic shown explicitly; the list of entries MOVED into `human_verification_resolved`;
the final open-item list with each `blocked_by`; the tray todo's disposition and why; and any
redactions applied to transcribed terminal output.
</output>
