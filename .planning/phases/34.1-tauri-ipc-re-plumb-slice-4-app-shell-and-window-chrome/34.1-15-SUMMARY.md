---
phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome
plan: 15
subsystem: infra
tags: [uat-ledger, audit-uat, gap-closure, documentation, state-reconciliation]

# Dependency graph
requires:
  - phase: 34.1-14
    provides: "the 28/28 PASS live macOS gate result, all observation slots filled, Section C decision (PATH A / A1)"
provides:
  - "34.1-HUMAN-UAT.md with three sessions recorded, tests 1/5/6/9 split into 26 individually-adjudicated entries, gaps G1-G4 updated + G5 opened"
  - "34.1-VERIFICATION.md reconciled: status stays human_needed, 5 items moved to human_verification_resolved, 7 genuinely open items remain each with a concrete blocked_by"
  - "ROADMAP/REQUIREMENTS/STATE hand-reconciled to the post-gap-cycle truth; tray todo re-scoped (not closed) to the Windows/Linux remainder"
affects: ["Phase 35 (owes the Electron-parity re-run before removing the Electron build)", "any future phase reading 34.1's VERIFICATION.md or the tray todo"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Compound UAT items split at their branch boundary (1a/1b, 5a/5b, 6a-6d, 9a-9l) so a section-level pass cannot hide an un-run sub-item"
    - "Gap closure recorded as closed_by/closed_evidence with platform-scoped caveats (windows_linux_remainder) rather than a blanket closed/open binary when a fix is genuinely platform-partial"

key-files:
  created:
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-15-SUMMARY.md
  modified:
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md
    - .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/todos/pending/tray-dark-light-icons-are-identical.md

key-decisions:
  - "Gap G1 (Windows/Linux WindowControls placement) stays status: failed, not closed, despite the static fix (plan 34.1-09) landing weeks ago — this hardware is macOS-only and cannot render WindowControls at all (gated off there), so the live confirmation the gap's own truth statement requires has never happened. Reworded reason/missing to reflect 'statically fixed, never live-confirmed' rather than 'known broken', without upgrading status."
  - "Gap G3 (tray icon byte-identical) marked status: closed with an explicit windows_linux_remainder field and heavy caveat text, rather than left status: failed — the macOS half genuinely IS closed on live evidence (D4/D5/D5b), and the acceptance criteria only permit closed|failed as literal status values, so the platform split is carried in prose fields alongside the required binary status rather than by leaving a live-confirmed fix marked failed."
  - "The tray todo is updated in place (re-scoped to Windows/Linux), NOT moved to completed/ — critical_constraints in this plan's own brief explicitly overrides the plan text's simpler move/don't-move framing (written before 34.1-14's D4/D5/D5b corrected test 6's expected result from 'swap visibly' to 'vestigial by design'), because the Windows/Linux half remains genuinely, unambiguously broken (byte-identical MD5s, unchanged)."
  - "STATE.md completed_plans corrected +7 (300->307), not +7-minus-something — git history confirms NO prior STATE.md commit ever incremented completed_plans for plans 34.1-09 through 34.1-14 despite all six having SUMMARY.md on disk (commit 44391ff1f's 'update tracking after wave 1' touched only prose fields, not the progress block). total_plans stays unchanged at 316, confirmed via commit eeb5495c0's own diff showing the 7-plan bump (308->315) happened at PLANNING time, not execution time."

requirements-completed: [REQ-34.1-11]

# Metrics
duration: ~55min
completed: 2026-08-14
---

# Phase 34.1 Plan 15: Gap Cycle 1 Ledger Reconciliation Summary

**Transcribed plan 34.1-14's 28/28 PASS live macOS gate into the phase's two ledgers (`34.1-HUMAN-UAT.md`, `34.1-VERIFICATION.md`), closing gaps G2 and G4 in full and G3 on macOS only, then hand-reconciled ROADMAP/REQUIREMENTS/STATE and re-scoped (not closed) the tray-icon todo to its genuinely-still-open Windows/Linux remainder.**

## Performance

- **Duration:** ~55 min
- **Completed:** 2026-08-14
- **Tasks:** 3/3
- **Files modified:** 6 (1 created — this SUMMARY — plus the 6 files listed above; no source files under `src/`, `meta/`, `public/`, or `src-tauri/` touched)

## Accomplishments

- `34.1-HUMAN-UAT.md` now records three live sessions (2026-07-25, 2026-08-13, 2026-08-14) with tests 1, 5, 6, and 9 each split at their branch boundary into 26 total individually-adjudicated entries (was 10 bundled items) — no un-run half hides behind a section-level pass
- `34.1-VERIFICATION.md`'s `status` field stays `human_needed` and both standing-warning fields (`audit_tool_note`, `ledger_defect_note`) are byte-unchanged (verified via `git diff`, no hunk touches them)
- `gsd-sdk query audit-uat` returns a non-empty 7-item `items` array for phase `34.1`, matching the 7 entries in `human_verification` 1:1
- 5 resolved items MOVED (not annotated) into `human_verification_resolved`, verified via `git diff` shape (deletion in one block, addition in the other): macOS window buttons, frameless-toggle Settings-row copy, tray creation/click/menu items, macOS vestigial dark-tray-icon, macOS titlebar mechanism (L2-L9)
- STATE.md's ledger gap closed by hand: plans 34.1-09 through 34.1-14 had SUMMARY.md on disk but were never reflected in `completed_plans` — corrected 300 → 307, `percent` 94 → 97, arithmetic shown below
- Tray todo re-scoped in place to the Windows/Linux remainder — NOT closed, since that half is genuinely still broken

## Task Commits

1. **Task 1: Transcribe live results into 34.1-HUMAN-UAT.md, splitting every compound item** — `b975087a4` (docs)
2. **Task 2: Reconcile 34.1-VERIFICATION.md against the audit tool's two silent failure modes** — `31432c834` (docs)
3. **Task 3: Reconcile ROADMAP, REQUIREMENTS, STATE, and re-scope the tray todo** — `3e9a9e109` (docs)

**Plan metadata / SUMMARY:** committed separately below (final metadata commit).

## Files Created/Modified

- `.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md` — 3rd session added to frontmatter (byte-preserving the first two); tests 1/5/6/9 split into 1a/1b, 5a/5b, 6a-6d, 9a-9l; Section C decision (PATH A/A1) written up in full; new non-blocking UX finding + gap G5 recorded; Summary counts recounted (26 total: 21 passed, 1 partial, 3 blocked, 1 skipped)
- `.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md` — 3rd `live_uat_sessions` entry; `human_verification` rewritten to 7 genuinely open items each with `blocked_by`; `human_verification_resolved` gained 5 entries; `open_items_after_2026_08_13` replaced with a recounted `open_items_after_2026_08_14` block; new prose paragraph under `### Human Verification Required` naming each gap's closure state and remaining dependency
- `.planning/ROADMAP.md` — `**Plans:**` line 13/15 → 15/15; 34.1-14/34.1-15 bullets ticked with their actual outcomes; gap-cycle-1 closing note appended
- `.planning/REQUIREMENTS.md` — REQ-34.1-03 gets a dated, two-half supersession note (macOS `WindowControls` rendering SUPERSEDED; `setDecorations`→`setTitleBarStyle` mechanism AMENDED with runtime-honoured intent UPHELD); phase-level provenance line updated; no REQ ID added/deleted/renumbered
- `.planning/STATE.md` — `stopped_at`/`last_activity`/`last_updated`/Current-focus prose updated (prepended, prior content preserved as "Earlier:"); `progress.completed_plans` 300→307, `progress.percent` 94→97, `progress.total_plans` unchanged at 316, with a new dated hand-correction comment explaining the arithmetic and citing the git-history evidence for each number
- `.planning/todos/pending/tray-dark-light-icons-are-identical.md` — title/frontmatter re-scoped to Windows/Linux; `status:` field added stating the re-scope explicitly; new dated "Resolution" section recording the macOS closure (with live evidence) and why the Windows/Linux half stays open and pending

## human_verification count vs. audit-uat items length

| Source | Count |
|---|---|
| `34.1-VERIFICATION.md` `human_verification:` entries | 7 |
| `gsd-sdk query audit-uat` → phase `34.1` → `items` array length | 7 |

Verified equal by direct inspection of `/tmp/gsd-audit-341.json`: both list, in order, Windows/Linux window buttons, the sub-1200px zoom branch, the Windows/Linux tray-asset remainder, the macOS tray dark-appearance residual, gamepad, child windows, and the owed Electron-parity re-run.

## Entries MOVED into human_verification_resolved (verified via git diff shape, not final-file reading)

1. **macOS window buttons** — GameLib's own buttons absent, native traffic lights present and functional (test 1b) — PASS 2026-08-14, Sections A/B/C
2. **Frameless-window Settings row copy** — accurate label, no confirmation dialog (test 2's copy half) — PASS 2026-08-14, D7/D8
3. **Tray creation/click/menu items** — icon appears, click shows/focuses, Show GameLib/Quit (test 6a/6b/6c) — PASS 2026-08-14, D1/D2/D3/D6 (re-confirming the 2026-07-25 partial pass)
4. **macOS dark-tray-icon vestigial-by-design** — template auto-tints, toggle produces no visible change (test 6d, macOS scope only) — PASS 2026-08-14, D4/D5/D5b
5. **macOS titlebar mechanism** — traffic lights, 78px reserve, WindowControls gating, all eight L2-L9 items (test 9) — PASS 2026-08-14, 28/28 including individually re-elicited L7/L8

Each was verified moved (not annotated) by inspecting the `git diff` shape: a deletion under the old `human_verification` block and a distinct addition under `human_verification_resolved`, never an in-place `result:` edit.

## STATE.md arithmetic, shown explicitly

- **Prior state:** `total_plans: 316`, `completed_plans: 300`, `percent: 94`
- **total_plans correction:** none. Confirmed via `git show eeb5495c0 -- .planning/STATE.md`, which shows the 7-plan gap-cycle-1 bump (`total_plans` 308→315) happened at PLANNING time (commit `eeb5495c0`, "plan gap cycle 1"), before any of the 7 plans executed. `total_plans` 315→316 happened later for an unrelated reason (plan 34.5-61, a NEW mid-cycle plan in a different phase). So all 7 of 34.1's gap-cycle plans (34.1-09..15) were already counted in `total_plans` before this session started.
- **completed_plans correction:** +7 → 307. `git log --oneline -- .planning/STATE.md | grep -i "34.1-"` shows STATE.md commits only for the original 8-plan phase (34.1-01..08) and one gap-cycle tracking commit (`44391ff1f`, "update tracking after wave 1") that touched only `stopped_at`/`last_activity`/Current-focus prose — NOT the `progress` block — despite its own commit message naming four completed plans (34.1-09/10/12/13). No STATE.md commit ever incremented `completed_plans` for 34.1-09 through 34.1-14, even though all six have `SUMMARY.md` on disk. This plan closes that same class of ledger gap (a session that produces real completions but never writes them back) inside STATE.md itself, not just in `34.1-VERIFICATION.md`. Correction: `300 + 7 = 307` (34.1-09, 10, 11, 12, 13, 14 — already SUMMARY-complete before this session — plus 34.1-15, this plan's own SUMMARY landing now).
- **percent:** `round(completed_plans / total_plans * 100) = round(307 / 316 * 100) = round(97.1518...) = 97`.

## Final open-item list, each with its blocked_by

| # | Item | blocked_by |
|---|---|---|
| 1 | Windows/Linux `WindowControls` placement (window buttons) | a Windows or Linux machine |
| 2 | Sub-1200px DPI-compensation zoom branch | a sub-1200px-wide display |
| 3 | Windows/Linux tray icon byte-identical remainder | distinct dark/light tray icon artwork (Windows/Linux) — a design/asset task, not a hardware gap |
| 4 | macOS tray template dark/black-menu-bar rendering, direct observation | none named — a light-touch accessibility-toggle check, not a hardware/asset blocker |
| 5 | Gamepad navigation (`/console` routes, all twelve action cases) | a game controller |
| 6 | Child windows + About version round-trip (security-relevant claim, unweakened) | none named — simply not yet attempted, not a hardware gap |
| 7 | Electron parity re-run (`npm start`), owed before Phase 35 removes the Electron build | a dedicated Electron-parity re-run session — a scheduling debt, not a hardware gap |

## Tray todo disposition and why

**Updated in place, kept in `.planning/todos/pending/` — NOT moved to `completed/`.** The macOS half is genuinely closed on live evidence (plan 34.1-13's AppKit template image, live-confirmed legible and correctly vestigial by plan 34.1-14's D4/D5/D5b), but the Windows/Linux half remains completely unfixed — `icon-dark.png`/`icon-light.png` (+ `@2x`/`@3x`) are still byte-identical there, unchanged by any plan in this gap cycle. The plan's own text offered a binary "moved-if-fixed / update-in-place-if-still-broken-everywhere" framing written before 34.1-14 corrected test 6's expected result (from "swap visibly" to "vestigial by design on macOS"); this plan's `critical_constraints` explicitly overrides that framing for the platform-split case this gap cycle actually produced. The todo's frontmatter now carries a `status:` field stating the re-scope explicitly, and a new dated "Resolution" section records the macOS closure evidence, the new template asset's MD5, and exactly what remains needed for Windows/Linux (distinct dark artwork, a design pass, explicitly not an auto-invert).

## Redactions applied to transcribed terminal output

None. This plan transcribed only already-published SUMMARY/UAT content (no raw terminal capture was performed by this plan itself); no home-directory paths or account identifiers were introduced. `34.1-HUMAN-UAT.md`'s own "Redactions applied to transcribed terminal output" section (unchanged by this plan) still correctly states none were needed as of plan 34.1-14's session.

## Decisions Made

See `key-decisions` in the frontmatter above (four decisions: G1 stays `failed`-with-reworded-reason rather than closed; G3 marked `closed` with an explicit `windows_linux_remainder` caveat field rather than downgraded to `failed`; the tray todo updated in place rather than moved, per `critical_constraints` overriding the plan's simpler binary; the STATE.md `completed_plans` correction is +7, evidenced by git history rather than assumed).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] STATE.md's own `completed_plans` counter had the same class of ledger gap this plan exists to fix in `34.1-VERIFICATION.md`**
- **Found during:** Task 3, while computing the STATE.md arithmetic the plan's acceptance criteria require to be "shown by hand"
- **Issue:** `git log --oneline -- .planning/STATE.md` showed no commit ever incrementing `completed_plans` for plans 34.1-09 through 34.1-14, despite all six having `SUMMARY.md` on disk (commit `44391ff1f`'s own message named four completed plans but its diff touched only prose fields). A naive "+1 for this plan's own SUMMARY" correction (matching the pattern used elsewhere for single-plan increments) would have left `completed_plans` under-counted by 6.
- **Fix:** Corrected by +7 instead of +1, with the full accounting (six already-complete plans plus this plan's own completion) shown explicitly in both the STATE.md comment and this SUMMARY's "STATE.md arithmetic" section above.
- **Files modified:** `.planning/STATE.md`
- **Verification:** `git log --oneline -- .planning/STATE.md | grep -i "34.1-"` re-run after the fix still shows no prior commit incrementing the counter for 34.1-09..14, confirming the correction accounts for genuinely-uncounted completions rather than double-counting.
- **Committed in:** `3e9a9e109` (Task 3's own commit).

---

**Total deviations:** 1 auto-fixed (Rule 1 — a bug in the state this plan inherited, not introduced by this plan).
**Impact on plan:** Necessary for STATE.md's numeric fields to be honest; no scope creep — the fix stayed inside STATE.md's `progress` block, the exact field Task 3 was already responsible for.

## Issues Encountered

None beyond the STATE.md ledger gap documented above.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Phase 34.1 gap cycle 1 is CLOSED OUT** (15/15 plans executed on disk), but **the phase itself does not close** — `34.1-VERIFICATION.md` correctly stays `status: human_needed` with 7 genuinely open items, none of which any session has resolved.
- **Highest-priority carried-forward item: the Electron parity re-run (UAT test 10), owed before Phase 35 removes the Electron build entirely** — after that phase, the comparison this item measures becomes permanently impossible. No future plan should let this slip further.
- Windows/Linux `WindowControls` placement, the sub-1200px DPI branch, and the Windows/Linux tray-asset defect all require hardware or artwork this project does not currently have; each carries a concrete `blocked_by` in both ledgers so a future session can act the moment the blocker clears.
- Gamepad and child-window/About-version verification remain entirely unattempted across all three sessions to date — the largest genuinely-untested surfaces left in this phase.
- No blockers to closing this plan.

---
*Phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome*
*Completed: 2026-08-14*

## Self-Check: PASSED

- FOUND: `.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md` (modified, verified via git diff)
- FOUND: `.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-VERIFICATION.md` (modified, verified via git diff)
- FOUND: `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/todos/pending/tray-dark-light-icons-are-identical.md` (all modified)
- CONFIRMED commit `b975087a4` (Task 1) present in `git log --oneline -3`
- CONFIRMED commit `31432c834` (Task 2) present in `git log --oneline -3`
- CONFIRMED commit `3e9a9e109` (Task 3) present in `git log --oneline -3`
- CONFIRMED `gsd-sdk query audit-uat` returns a non-empty 7-item array for phase `34.1`, matching `human_verification`'s 7 entries exactly
- CONFIRMED `git status --porcelain .planning/` before this SUMMARY's own commit showed only the six files this plan owns
- CONFIRMED no `gsd-sdk query state.*` verb was invoked at any point in this plan's execution
