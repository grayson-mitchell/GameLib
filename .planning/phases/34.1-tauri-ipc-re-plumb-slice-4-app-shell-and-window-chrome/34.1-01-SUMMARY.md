---
phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome
plan: 01
subsystem: infra
tags: [tauri, capabilities, security, ipc-inventory, jest]

# Dependency graph
requires:
  - phase: 33-tauri-lifecycle-cluster
    provides: "notification:allow-is-permission-granted / shell:allow-execute capability pattern and description prose style to extend"
provides:
  - "src-tauri/capabilities/default.json extended with the twelve core:window:*/core:webview:* grants D-01's renderer-side window chrome depends on"
  - "Exact-grant-list regression test pinning the capability list against both widening and narrowing"
  - "IPC-PORT-INVENTORY.md reconciled for D-14's callTool reassignment (slice 4: 33, slice 8: 56)"
affects: [34.1-03-window-chrome-preload, 34.1-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Capability description as security rationale: every non-obvious grant (or refusal) is documented in the file's own description field, matching the existing dialog:allow-open-refused / notification:allow-is-permission-granted precedent"
    - "Exact-grant-list config-shape test: assert precise membership + negative gate (forbidden broader grants), spot-checked to fail on both widening and narrowing"

key-files:
  created:
    - src/backend/__tests__/capabilitiesDefault.test.ts
  modified:
    - src-tauri/capabilities/default.json
    - .planning/IPC-PORT-INVENTORY.md

key-decisions:
  - "Followed D-04 verbatim: granted exactly the eleven core:window:* + one core:webview:* identifiers, refusing core:window:default/core:webview:default/core:tray:default wholesale"
  - "Verified core:window:default is a read-only getter set in the installed tauri 2.11.5 crate (already closed by 34.1-RESEARCH.md); recorded that verification in the capability file's own description, not just in planning docs"
  - "D-14 callTool reassignment note placed under the Slice 4 heading uses plain (non-backtick) text for the word callTool, so it documents the move without being miscounted as a 34th backtick-quoted channel name in the slice-4 list"

requirements-completed: [REQ-34.1-02, REQ-34.1-10]

# Metrics
duration: ~20min
completed: 2026-07-25
---

# Phase 34.1 Plan 01: D-04 Capability Grants + Inventory Reconciliation Summary

**Extended `src-tauri/capabilities/default.json` with the twelve explicit `core:window:*`/`core:webview:*` grants D-01's renderer-side window chrome needs, pinned the exact grant list with a new regression test, and corrected `IPC-PORT-INVENTORY.md` for D-14's `callTool` reassignment.**

## Performance

- **Duration:** ~20 min
- **Tasks:** 3
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments

- `capabilities/default.json` now grants exactly the twelve commands the Tauri preload factory's window-chrome exports (`minimize`/`maximize`/`toggleMaximize`/`unmaximize`/`close`/`startDragging`/`setFullscreen`/`isFullscreen`/`isMaximized`/`isMinimized`/`setDecorations` + webview `setZoom`) will call — nothing broader.
- The `description` field records, in the file's own prose voice, that `core:window:default` was verified against the installed Tauri 2.11.5 crate's own permission tables to be a read-only getter set containing none of the mutation commands — so the D-04 "verify before finalizing" mandate is satisfied in the repo, not on recollection.
- `src/backend/__tests__/capabilitiesDefault.test.ts` pins the grant list: 5 tests assert exact membership, a negative gate against `core:window:default`/`core:webview:default`/`core:tray:default`/`notification:default`/`dialog:allow-open`/`core:webview:allow-create-webview-window`, the `windows: ["main"]` scope, survival of the four pre-existing grants plus the scoped `shell:allow-execute` entry, and the rationale substrings in `description`.
- Spot-checked per the plan's acceptance criteria: temporarily removing `core:webview:allow-set-webview-zoom` turned the suite red (1/5 failing); temporarily adding `core:window:default` also turned it red (1/5 failing); both reverted cleanly via `git checkout --`.
- `IPC-PORT-INVENTORY.md` reconciled for D-14: Slice 4 heading corrected to `(33 channels)` with `callTool` removed from its list and a reassignment note added; Slice 8 heading corrected to `(56 channels)` with `callTool` inserted in alphabetical position between `authZoom` and `disableEosOverlay`. The `## Totals` table (210/27/183) was left untouched, as instructed.

## Task Commits

Each task was committed atomically:

1. **Task 1: Grant the twelve explicit window/webview capabilities with per-grant rationale** - `5169d476` (feat)
2. **Task 2: Exact-grant-list assertion test** - `077a3fdb` (test)
3. **Task 3: Correct IPC-PORT-INVENTORY.md for D-14's callTool reassignment** - `dbe07735` (docs)

_No plan-metadata commit yet — this SUMMARY + STATE/ROADMAP updates are committed together in the final metadata commit._

## Files Created/Modified

- `src-tauri/capabilities/default.json` - Added the twelve `core:window:*`/`core:webview:*` permission identifiers and extended `description` with per-grant rationale, including the verified `core:window:default` composition.
- `src/backend/__tests__/capabilitiesDefault.test.ts` - New exact-grant-list regression test (5 tests, all tagged `REQ-34.1-02`).
- `.planning/IPC-PORT-INVENTORY.md` - Slice 4 heading `34 -> 33 channels` (callTool removed, reassignment note added); Slice 8 heading `55 -> 56 channels` (callTool inserted alphabetically).

## Decisions Made

- Followed D-04's explicit-per-command allow-list instruction verbatim; no `core:window:default`/`core:webview:default`/`core:tray:default` grant was added, matching the plan's `must_haves.truths` and `threat_model` T-34.1-01 mitigation.
- The D-14 reassignment note under the Slice 4 heading intentionally does not backtick-quote the word "callTool" (it uses plain prose text), so the acceptance criteria's literal backtick-token count (`grep -o '` + "`" + `[a-zA-Z.\-]*` + "`" + `'`) over the Slice 4 section still yields exactly 33 — matching the corrected heading — rather than 34, which a backtick-quoted mention in the note would have produced.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Task 3's own `<verify><automated>` shell snippet conflicts with its own `<action>` mandate**

- **Found during:** Task 3
- **Issue:** The task's `<action>` explicitly requires adding a one-line note under the Phase 34.1 heading naming `callTool` (to record the D-14 reassignment reason). But the task's own `<verify><automated>` script does a blunt `awk "/Slice 4 —/,/Slice 5 —/" $f | grep -q "callTool" && { echo "callTool still in slice 4"; exit 1; }` over the *entire* section (heading through next heading), which necessarily matches the literal substring "callTool" inside the mandated note itself — the two instructions are mutually exclusive as literally written. The `<acceptance_criteria>` is more precisely worded ("`callTool` appears ... nowhere inside the Phase 34.1 section's **channel list**"), distinguishing the backtick-quoted channel-list line from prose elsewhere in the section.
- **Fix:** Added the mandated note, but phrased it using plain (non-backtick-quoted) text for the word "callTool" rather than the markdown-code-span style used for actual channel names. This satisfies: (a) the note names `callTool` and cites `src/backend/tools/ipc_handler.ts:25` and D-14, per `<action>`; (b) `callTool` does not appear in the backtick-quoted channel-list line for Slice 4; (c) the acceptance criteria's exact backtick-token count (`grep -o` over `` `[a-zA-Z.\-]*` ``) still yields exactly 33 for Slice 4. The raw diagnostic `<verify><automated>` script's blunt substring grep still reports a false positive on this note's prose — that specific line of the plan's own verify script cannot be satisfied simultaneously with the `<action>` mandate as written; the more precise `<acceptance_criteria>` and the file's actual correctness were treated as authoritative.
- **Files modified:** `.planning/IPC-PORT-INVENTORY.md`
- **Verification:** Confirmed both headings read the corrected channel counts; confirmed `callTool` is absent from the Slice 4 backtick-quoted channel-list line (`awk '/Slice 4 —/,/Slice 5 —/' ... | grep '`callTool`'` finds nothing); confirmed `callTool` is present in the Slice 8 backtick-quoted channel-list line; confirmed the acceptance criteria's literal backtick-count command yields 33 for Slice 4 (56 for Slice 8 using an unrestricted `` `[^`]*` `` pattern — the plan's suggested `[a-zA-Z.\-]*` pattern itself undercounts Slice 8 by one due to a pre-existing digit in `toggleVKD3D`, a pre-existing quirk in the plan's own suggested count command, unrelated to this edit); confirmed the `## Totals` table is unchanged.
- **Committed in:** `dbe07735` (part of Task 3's commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — plan-internal contradiction between a task's `<action>` and its own `<verify>` script)
**Impact on plan:** No scope creep. The underlying content (heading counts, list membership, D-14 note) matches the plan's intent and its more-precise `<acceptance_criteria>`; only the blunt raw grep in `<verify><automated>` cannot pass as literally written given the also-mandated note. Documented rather than silently worked around.

## Issues Encountered

None beyond the deviation documented above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `capabilities/default.json` is ready for plan 34.1-03 (the Tauri preload factory's window-chrome exports) to consume — every `core:window:*`/`core:webview:*` command it will call is already granted.
- `IPC-PORT-INVENTORY.md`'s slice-4/slice-8 counts (33/56) are now reconcilable for the Phase 35 completion gate; `callTool`'s real port work is deferred to Phase 34.5 as scoped by D-14, unaffected by this plan.
- No blockers for the remaining plans in this phase's wave.

---
*Phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome*
*Completed: 2026-07-25*
