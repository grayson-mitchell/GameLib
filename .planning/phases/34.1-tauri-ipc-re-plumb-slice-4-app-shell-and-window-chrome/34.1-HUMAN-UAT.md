---
status: complete
# 2026-08-22 (LATEST, seventh session): testing -> complete. Item 8b RAN and PASSED. See its
# entry below. ZERO items open in this phase again.
#
# PRIOR (2026-08-22, LATER): complete -> testing. Item 8b was REOPENED. It was closed as STRUCTURALLY
# UNREACHABLE, which was correct: the About window had no Tauri entry point. Quick task
# `260822-tv4` added one (Settings tab -> About), so the item is runnable and is being run.
# `34.1-VERIFICATION.md` was flipped to `human_needed` in the SAME commit as this line -- the two
# files must not diverge again (see the note immediately below, which records the last time they
# did).
#
# PRIOR (2026-08-22, earlier): diagnosed -> complete. All 32 individual items are discharged or relocated; the
# four that remain genuinely open are hardware-gated and owned by Phase 38 (38-W01, 38-W02,
# 38-C01..C04), not by this ledger. Matches `34.1-VERIFICATION.md`'s `status: passed`, which was
# flipped in the same closure and which this file lagged behind for one commit -- the exact
# "propagation misses a status doc" failure this project has hit before.
phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome
source: [34.1-CONTEXT.md D-15, 34.1-VALIDATION.md, 34.1-PORTED-CHANNELS.md]
started: 2026-07-25
updated: 2026-08-22T19:05:00Z
sessions:
  - date: 2026-07-25
    scope: all 10 items
    outcome: 7 pass, 1 issue, 2 skipped
  - date: 2026-08-13
    scope: "items 1-5 only (re-run during /gsd-audit-uat group 1); items 6-8 and 10 NOT re-run"
    outcome: "1 REGRESSION (item 1), 1 decision REVERSAL (item 9), 1 pass downgraded to partial (item 5), 1 new copy defect (item 2), 3 re-confirmed"
  - date: 2026-08-14
    scope: "plan 34.1-14's live macOS gate -- items 1 (macOS half), 2 (Settings row visual confirmation), 6 (tray sub-items), 9 (macOS titlebar/L2-L9, three stress themes), plus the frameless-toggle-on-macOS decision (Section C)"
    outcome: "28/28 observations PASS (0 FAIL); Section C decision: PATH A / option A1 (keep-functional, no further code); one non-blocking UX finding raised and ticketed as two todos (commit 3be0730ef)"
  - date: 2026-08-14 (second session, same day)
    scope: "item 10 ONLY -- the OWED Electron parity re-run, via `/gsd-verify-work 34.1`. Scope
      chosen deliberately: of the 7 items open after the first 2026-08-14 session, four are
      hardware-blocked (1a, 5b, 6d-remainder, 7 -- but see 2026-08-22: 5b was NOT
      hardware-blocked, that classification was an unread predicate) and item 10 was the only one carrying a hard
      deadline (Phase 35 deletes the Electron build). Items 8 and the tray dark-appearance
      residual were runnable but explicitly NOT selected -- they remain open and un-run."
    outcome: "Item 10 split into 7 sub-items (10a-10g). 6 PASS, 1 STRUCTURALLY UNREACHABLE.
      The deliberate macOS WindowControls change CONFIRMED under Electron (10b). Two findings:
      the historical 'releases' pass is VACUOUS (10f -- getLatestReleases() has returned [] since
      2026-07-02, before the first session), and the changelog modal renders stale '1.0.0' copy
      against a 0.7.0 app (10g -- new gap G6, ticketed)."
  - date: 2026-08-22
    scope: "item 5b ONLY -- the sub-1200px DPI-compensation branch, unverified since 2026-07-25.
      Selected because the 'blocked_by: a sub-1200px-wide display' record turned out to be
      WRONG: the branch keys on window.screen.availWidth, which follows the window across
      displays, so an external monitor in a scaled sub-1200 mode arms it. NOTE this
      retroactively corrects the 2026-08-14 (second session) scope line above, which listed
      5b among the four 'hardware-blocked' items -- it was never hardware-blocked."
    outcome: "PASS. Branch ARMED for the first time in five sessions (availWidth=1024,
      k=0.8533, k != 1), 8 levels swept, every logged factor matched prediction exactly,
      rendering clean at 200% / 100% / 60%. One near-miss caught mid-run: a 200% visual
      observation with NO matching f=2 log line, caused by the 500ms zoom debounce -- the
      re-run with a deliberate release paired the observation to the factor. No production
      code changed; instrumentation removed, file back to pristine."
  - date: 2026-08-22 (second session, same day)
    scope: "the two remaining RUNNABLE items -- item 8 (child windows) and the macOS
      dark-menu-bar tray residual. Both had NO hardware blocker; item 8 had been deliberately
      not selected on 2026-08-14 despite being the only item with no blocker of any kind."
    outcome: "Both discharged. Item 8 SPLIT: 8a external links PASS on mechanism with a new
      title defect (gap G7 -- every external child window is titled 'Tauri App'; the WR-07
      comment asserts a document-title fallback that does not exist, and its test asserts the
      CALL SITE so it stays green regardless). 8b About window STRUCTURALLY UNREACHABLE under
      Tauri -- the only caller of showAboutWindow is the ELECTRON tray, caught by a reachability
      check BEFORE an operator was sent to look. Tray residual PASS: template glyph renders
      white against a genuinely opaque dark menu bar, closing the half 34.1-14 had inferred by
      symmetry. Two todos filed. After this session ONE item remains and it is a design/asset
      task, not a test."
---

## Current Test

[testing complete -- re-run 2026-08-13: 6 passed, 2 issues, 1 partial, 1 reversed decision;
live macOS gate 2026-08-14: 28/28 PASS, closing G2 and G4 in full and G3/G1 on macOS. Plan
34.1-15 was the terminal step of gap cycle 1. Electron parity re-run 2026-08-14 (second
session): item 10 discharged, 6 PASS + 1 structurally unreachable, gap G6 opened.
Item 5b discharged 2026-08-22 (fifth session): PASS, branch armed for the first time.
ZERO items remain open in THIS phase as of 2026-08-22 (sixth session). 8 was discharged
(8a PASS, 8b structurally unreachable), the macOS dark-menu-bar tray residual PASSED as 6e,
and every remaining item is hardware-gated and now owned by Phase 38: 1a -> 38-W01,
6d-remainder -> 38-W02 (its ARTWORK blocker was fixed in code first, see 6d), 7 -> 38-C01..C04.
34.1-VERIFICATION.md is `status: passed` accordingly.]

> ⚠ **This document now records FOUR sessions.** A `result:` line reading "pass — verified live
> 2026-07-25" WITHOUT a "RE-CONFIRMED" marker for a later date means that item has NOT been
> re-run since the date it names. Item 1 proves that matters: it passed on 2026-07-25 and
> REGRESSED under Phase 34.10 with nothing turning red. Items 7 and 8 remain in that never-re-run (in this
> case, never-run-at-all) state after all five sessions — see their entries below for
> concrete, greppable blockers. **The sub-1200px half of item 5 has LEFT that set:** 5b was
> run and passed 2026-08-22. Its exit carries its own warning, because the blocker that held
> it for four sessions was never real — "a sub-1200px-wide display" described a predicate
> nobody had read (`window.screen.availWidth`, which follows the window to whatever display
> it currently occupies). Before accepting any remaining `blocked_by` line here as a hardware
> fact, read the predicate it claims to describe.
>
> ⚠ **And a stronger warning the fourth session earned:** a `pass` does not establish that the
> surface it names can render at all. Item 10f had been recorded as part of a passing item since
> 2026-07-25 while `getLatestReleases()` had already returned `[]` unconditionally since
> 2026-07-02. Three sessions of splitting compound items did not surface it, because splitting
> asks which halves RAN, not whether the whole was RUNNABLE. Check source-level reachability
> before sending an operator to look at anything.

> **Session activated 2026-07-25 via `/gsd-verify-work 34.1`.** The items below were written as
> deferred under D-15; this session is now walking them live. Results are recorded per test.

Every item in this document was **deferred** per the phase's own CONTEXT decision D-15. They are
the only checks that can prove this slice's actual deliverable: the frameless toggle applying, the
titlebar buttons moving the window, the tray appearing and changing colour, gamepad navigation
working, and how macOS traffic lights versus custom buttons look. Every automated test this phase
produced proves wiring — that the right Tauri API was called with the right arguments — and jest
structurally cannot prove any of the above.

## Tests

**Compound-item lesson, applied throughout this document:** UAT test 5 recorded a pass across two
sessions while the branch it was written to cover, the sub-1200px DPI-compensation path, was never
exercised by either — the recorded lesson is that a compound test item resolves to a single
pass/fail and the un-run half disappears. Every item this gap cycle touched (1, 5, 6, 9) is split
at its branch boundary below, each half with its own individual result.

1a. **Window buttons — Windows/Linux.** With `framelessWindow` on, GameLib's own custom-titlebar
    buttons sit at the window's top edge and minimize/maximize/restore/close the real OS window.
    Expected: each click causes the real OS window to minimize / maximize / restore / close,
    exactly as the equivalent native title-bar button would.
    result: **NOT ATTEMPTED — no session has run this on real Windows/Linux hardware.**
    blocked_by: a Windows or Linux machine
    status_of_the_fix: plan 34.1-09 re-anchored `WindowControls/index.scss:2` from the stale
      sidebar-era `grid-area: content` to `grid-area: navbar`, closing the identified root cause,
      and added `src/frontend/components/UI/NavShell/__tests__/windowControlsPlacement.test.ts`,
      which gates the placement STATICALLY by recomputing the expected row from `.App`'s own
      live `grid-template-areas` rather than a pinned literal — so a future grid rewrite cannot
      silently displace `WindowControls` again without the gate going red. This is strong static
      evidence the regression is fixed. **It has never been confirmed by looking at a real
      Windows or Linux window** — this hardware is exclusively macOS, and `WindowControls` is
      gated OFF entirely on macOS (plan 34.1-11), so it structurally cannot render here.
    severity: major (unconfirmed live)
    history: pass — verified live 2026-07-25 (pre-34.10 shell), REGRESSED under 34.10, statically
      re-fixed by plan 34.1-09 (2026-08-13), still never re-observed live.

1b. **Window buttons — macOS.** With `framelessWindow` on, GameLib's own titlebar buttons are
    ABSENT (hidden unconditionally, per the D-06 reversal / gap G4) and the native macOS traffic
    lights are present and fully functional instead.
    Expected: no GameLib-drawn window buttons anywhere; native traffic lights present, correctly
    coloured, and each one (minimize/zoom/close) behaves exactly as a native macOS window button
    would.
    result: **PASS**, operator, live, 2026-08-14. Recorded across all three stress themes
      (`midnightMirage`, `gruvbox_dark`, `dracula`) — see test 9 below for the per-theme
      breakdown (A1/A2, B1/B2, C1/C2). Traffic lights explicitly named present by the operator
      ("traffic lights present"); each traffic light individually clicked and behaved natively
      (A4, inherited from the section verdict — see test 9).
    entangled_with: "gap G4 / test 9 — this IS the macOS half of what G4 restores; recorded here
      too because test 1's original truth statement covers window-button behaviour specifically"

2. **Frameless toggle** — toggle `framelessWindow` on in Settings.
   Expected: the window chrome changes (native decorations disappear, GameLib's own titlebar
   appears, or on macOS the overlaid titlebar with native traffic lights) without requiring a
   restart. On a fresh profile with no settings yet written, the window starts DECORATED — the
   Electron default — not frameless. The toggle's own label and behaviour must agree — no
   "(requires restart)" claim, no confirmation dialog gating the on-path.
   result: **PASS, fully closed.** The behavioural half passed live 2026-07-25, RE-CONFIRMED
     2026-08-13 and again structurally exercised throughout 2026-08-14's Section B (L2/L5 both
     PASS — the toggle visibly changes the titlebar both directions without a restart). The copy
     half (gap G2) was code-fixed by plan 34.1-12 (dialog removed, "(requires restart)" dropped,
     moved to the `gamelib` i18n namespace) and then **visually confirmed live** by plan 34.1-14:
     D7 — operator read the Settings row label; verified against source
     (`public/locales/en/gamelib.json:66`, `UseFramelessWindow.tsx:29-30`) both reading exactly
     "Use frameless window", no parenthetical. D8 — operator confirmed NO "Experimental feature
     ahead" confirmation dialog appears when toggling on.
   history: pass — verified live 2026-07-25, RE-CONFIRMED 2026-08-13 (behaviour). Copy gap G2
     opened 2026-08-13, code-fixed by 34.1-12, live-confirmed CLOSED by 34.1-14 D7/D8
     (2026-08-14). See `## Gaps` below for the closure record.

3. **No startup flip** — with `framelessWindow` already on from a prior session, restart the app
   (`pnpm tauri:dev`).
   Expected: the window does not visibly flip from decorated to undecorated partway through
   load — decorations are applied pre-paint, not after the first frame.
   result: pass — verified live 2026-07-25, RE-CONFIRMED 2026-08-13 ("done, no flip"). Confirms
   the CR-02 fix. Not directly re-run in this form during 2026-08-14, but its non-regression
   claim was independently re-covered under the NEW macOS mechanism by test 9's L7/L8
   (individually re-elicited startup-flip checks in both toggle states, both PASS) — see below.

4. **Frameless drag — highest-risk unobserved item.** With `framelessWindow` on, drag the
   sidebar to move the window; double-click the sidebar to toggle maximize/restore.
   Expected: dragging the sidebar moves the real OS window; double-clicking it maximizes or
   restores it, mirroring native titlebar drag/double-click behavior.
   result: pass — verified live 2026-07-25, RE-CONFIRMED 2026-08-13 ("drag works").
   **The highest-risk item in the slice passed, and survived the 34.10 shell rewrite.**
   Both drag and double-click-to-maximize work against a real webview, so the
   `-webkit-app-region` path (or its selector fallback) does function under WKWebView.
   surface_correction: the 2026-07-25 run dragged the `.Sidebar`; Phase 34.10 retired the left
     sidebar, so the 2026-08-13 re-run dragged the **NavShell navbar** — the active Tauri path
     is `tauriWindowChrome.ts`'s `closest('.NavShell__navbar')` fallback (already re-anchored in
     commit `a150733fc`). Test 9's A5 (2026-08-14, navbar drag + double-click, macOS, overlaid
     state) independently re-covers this same mechanism under the new overlay titlebar and also
     PASSED (inherited from the section verdict).

5a. **Zoom — levels.** Change the zoom slider while the app is running through 200% / 60% / 100%.
    Expected: the UI visibly scales to match each new zoom level.
    result: pass — verified live 2026-07-25, RE-CONFIRMED 2026-08-13 across 200% / 60% / 100%.
    Not re-run 2026-08-14 (out of that plan's scope).

5b. **Zoom — sub-1200px DPI-compensation branch.** On a sub-1200px-wide display, the
    level→scale-factor conversion (`Math.pow(1.2, level)`) must also account for the DPI
    compensation branch.
    Expected: the UI visibly scales correctly on the narrow display, including the DPI branch.
    result: **PASS**, operator, live, 2026-08-22 (fifth session). FIRST TIME ARMED in five
      sessions. The window was started on a wide display and dragged onto a 1024x768 external
      display; the branch then reported `availWidth=1024`, `k=0.8533333333333334` -- k != 1, so
      the DPI-compensation path EXECUTED rather than falling through to the `k = 1` no-op that
      makes this item indistinguishable from 5a. Eight distinct levels swept (60/70/100/110/120/
      140/170/200%); every logged factor matched the value predicted from
      `Math.pow(1.2, (f * k - 1) / 0.2)` before the run:

        f=0.6 -> 0.6409110504255824    f=1.1 -> 0.9456223998715025    f=1.7 -> 1.5080708350223149
        f=0.7 -> 0.6927583321426815    f=1.2 -> 1.0221196781312254    f=2.0 -> 1.9044682466672116
        f=1.0 -> 0.8748503157415362    f=1.4 -> 1.1941798701158977

      Operator's rendering verdict at 200% -- the level where the compensated factor (1.905)
      diverges most from the uncompensated one (2.488), and the only level where the difference
      is large enough to SEE rather than infer: "200% looked fine, no clipping or overflow".
      100% and 60% likewise rendered clean.
    artifact: eight `[UAT-5b]` lines in `~/Library/Logs/GameLib/gamelib.log` (15:40:18-15:43:15,
      2026-08-22), emitted by temporary instrumentation placed AT the branch in
      `src/preload/api/tauriWindowChrome.ts` and removed immediately after the run (the file is
      back to pristine; nothing was committed). The instrumentation was routed through the
      `logInfo` listener rather than `console.*` on purpose -- see `method_note`.
    method_note: this item was blocked for four sessions on "a sub-1200px-wide display", which
      read as a hardware blocker. It was not one. The branch keys on
      `window.screen.availWidth`, NOT on window width and NOT on the display being internal --
      and `availWidth` in the Tauri webview tracks the display the window CURRENTLY OCCUPIES
      (established by this run: the window was started elsewhere and moved). So the branch is
      armable on any Mac with a second display set to a scaled mode under 1200 logical px. Four
      sessions of "blocked on hardware" were four sessions of not having read the predicate.
    trap_avoided: the operator's first 200% observation had NO matching `f=2` log line. The zoom
      slider's LABEL updates state synchronously (`GlobalState.tsx:395`) while `setZoomFactor` is
      debounced 500ms (`:397`), so dragging THROUGH 200% without releasing shows a UI labelled
      "200%" still rendered at the previously-applied factor. Had that observation been recorded,
      this item would have carried a pass for a frame that was never at 200%. The re-run with a
      deliberate release produced `f=2` three times and paired the observation to the factor.
      Generalisation: for any debounced control, the gate artifact must be emitted by the
      COMMITTED value, and the operator's look must happen after that emission -- never key a
      visual observation to a control's own label.
    lesson: a compound test item ("including on a sub-1200px display") resolves to a single
      pass/fail, and the un-run half disappears. Split at the branch boundary (this entry is that
      split). Re-reading the 2026-07-25 entry, it recorded three zoom LEVELS but never named a
      narrow display, so this branch was unverified from the very first session, not just the
      most recent two. Now closed: the split half has its own arming proof (`k != 1`), its own
      artifact, and its own rendering verdict.

6a. **Tray — icon appears at startup.** Start the app and observe the system tray/menu bar.
    Expected: a GameLib tray icon is present.
    result: **PASS**, operator, live. Sub-item 1 verified 2026-07-25 ("1-3 yes"); RE-CONFIRMED
      2026-08-14 (D1, operator: "d1: pass").

6b. **Tray — click shows/focuses the window.** Click the tray icon.
    Expected: the main window shows / takes focus.
    result: **PASS**, operator, live. Verified 2026-07-25 ("1-3 yes"); RE-CONFIRMED 2026-08-14
      (D2, operator: "D2: pass").

6c. **Tray — "Show GameLib" and "Quit" menu items.** Open the tray menu; use "Show GameLib"
    (shows/focuses the window) and, separately, "Quit" (exits the app).
    Expected: "Show GameLib" shows/focuses the window; "Quit" exits the app.
    result: **PASS**, operator, live. Verified 2026-07-25 ("1-3 yes"); RE-CONFIRMED 2026-08-14
      (D3 "Show GameLib" — operator: "D3 pass"; D6 "Quit" — operator: "d6 - pass").

6d. **Tray — dark-tray-icon setting.** Toggle the dark-tray-icon setting on, then off.
    Expected (as originally written, pre-G3): the tray image visibly swaps within ~500ms.
    Expected (CORRECTED 2026-08-14, per plan 34.1-13's accepted macOS fix): on macOS the toggle
    is now VESTIGIAL BY DESIGN — a template image lets AppKit auto-invert the glyph per the
    *effective menu-bar appearance*, so `darkTrayIcon` has zero visible effect there; "nothing
    visibly changes" is the CORRECT macOS result, not a defect.
    result: **PASS on macOS** (vestigial-by-design confirmed) — **Windows/Linux CODE-FIXED
      2026-08-22, NOT YET OBSERVED.** The byte-identical-asset defect that made this unrunnable
      even WITH hardware is closed: `meta/trayIconVariants.ts` now generates
      `icon-tray-{dark,light}{,@2x,@3x}.png` from the same hue-segmented mask as the macOS
      template, and both the generator and `trayIconAssets.test.ts` refuse an identical pair
      (RED-proven both ways). What remains is purely "watch the swap on a Windows or Linux
      tray", so the item was RELOCATED to **Phase 38 item 38-W02** — it is now hardware-gated
      and nothing else, which is that phase's entire charter.
      Note the ordering that mattered here: this item correctly did NOT move to Phase 38 with
      the six hardware items, because at that time it would have FAILED on a Windows machine
      too. Relocating it then would have parked a real defect behind a hardware excuse.
    macOS evidence (2026-08-14): D4 — glyph legible as a monochrome cat silhouette at menu-bar
      size (operator: "can see the silhoutte"). D5 — glyph auto-tints against the EFFECTIVE
      menu-bar appearance and stays legible; operator's macOS light-mode menu bar happened to be
      rendering translucent-over-a-coloured-wallpaper (effectively dark), so the glyph correctly
      rendered white, "like all the others" — the load-bearing discriminator proving the template
      flag is honoured, not a miss (see `34.1-14-SUMMARY.md`'s "D5 -- why 'stays white'..."
      section for the full reasoning). D5b — toggling the setting on/off produced NO visible
      change on macOS, confirmed as the expected vestigial result (operator: "Nothing visibly
      changes").
    windows_linux_remainder: `public/icon-dark.png`/`icon-light.png` (+ `@2x`/`@3x`) remain
      byte-identical on Windows/Linux, which still use the original two-file colour selection
      path (unchanged by plan 34.1-13, explicitly out of that plan's redirect scope).
    blocked_by: distinct dark/light tray icon artwork (Windows/Linux) — a design/asset task, not
      a hardware-availability blocker; see the re-scoped pending todo,
      `.planning/todos/pending/tray-dark-light-icons-are-identical.md`.

6e. **Tray — macOS template glyph against an OPAQUE dark menu bar.** The residual D5 left behind:
    2026-08-14 observed white ink against a TRANSLUCENT effectively-dark menu bar and inferred the
    opaque-dark rendering by symmetry.
    Expected: the template glyph renders legibly in white ink against a genuinely opaque dark
    menu bar.
    result: **PASS**, operator, live, 2026-08-22 (sixth session). System already in Dark
      appearance with transparency on (verified from the machine: `AppleInterfaceStyle = Dark`,
      `com.apple.universalaccess reduceTransparency` unset); enabling Reduce Transparency made the
      menu bar genuinely opaque and the cat silhouette rendered **white** and legible. Operator:
      "cat is white was white before". All three menu-bar states now rest on DIRECT observation
      rather than symmetry: light appearance (D5, 2026-08-14), translucent dark (D4/D5,
      2026-08-14), opaque dark (this run).
    no_change_is_the_correct_result: "was white before" is a PASS, not a null observation. Reduce
      Transparency changes menu-bar OPACITY, not its effective appearance — a dark bar stays dark,
      so the template tint has nothing to re-tint to and no visible change is expected. The gate's
      value is that the white rendering was observed against the opaque state, which had never
      been seen. (The session's own gate step asked whether the glyph "re-tinted live"; that
      question was mis-posed for this toggle and is answered N/A rather than FAIL. Live tint
      TRACKING was already the load-bearing D5 discriminator on 2026-08-14.)
    method_correction: `34.1-VERIFICATION.md`'s prior `why_human` for this item prescribed
      "Reduce Transparency **in Light appearance**" to force an opaque dark menu bar. That is
      wrong — Light appearance plus Reduce Transparency yields an opaque LIGHT bar, which is not
      the unobserved state. Dark appearance plus Reduce Transparency is what forces opaque dark.
      Corrected in place rather than silently re-ticked.

7. **Gamepad** — connect a game controller. Navigate the `/console` routes using: d-pad and left
   stick (all four directions), Tab/Shift+Tab, the B/back button, the right stick (scroll), and
   the stick clicks (activate).
   Expected: d-pad and left-stick moves focus in the expected direction with no wrap. Tab/
   Shift+Tab traverse focusable elements forward/backward. B/back navigates back. The right
   stick scrolls the page — **explicitly the sign convention most likely to be inverted**, check
   the scroll direction matches the stick direction, not the reverse. Stick clicks (left/right
   click equivalents) activate the element currently under focus/cursor.
   result: **STILL UNVERIFIED.** Skipped 2026-07-25 (no controller); not reached 2026-08-13; out
     of scope for 2026-08-14 (macOS live gate did not require a controller and none was
     available).
   blocked_by: a game controller
   note: `gamepadAction` (D-10) is the one channel in this slice whose entire implementation is a
     from-scratch rewrite — Electron injected synthetic input via `webContents.sendInputEvent`,
     which has no Tauri equivalent, so all twelve action cases plus a hand-written geometric
     nearest-in-direction focus algorithm were reimplemented as pure DOM dispatch. It is the
     largest untested surface left in the phase. Two specific risks to check when a controller is
     available: (a) the right-stick scroll sign convention, most likely to be inverted; (b)
     Up/Left directional focus from a cold start with nothing focused, which was broken as
     WR-02/WR-03 and fixed unit-only during code review. Cross-reference: Phase 34.10's
     independently-tracked "never-measured gamepad focus-scroll" is the SAME unmeasured surface,
     not a coincidence — no phase since has had a controller available either.

8a. **Child windows — external links.** Click an external-content link from a game's detail page.
    Expected: a real, separate OS window opens showing that page, titled by the REMOTE PAGE, not
    by the app.
    result: **PASS on the mechanism; TITLE DEFECT raised (gap G7).** Operator, live, 2026-08-22.
      Two windows opened in one session from Avowed's **Install info** tab — the "Crossover
      emulation" row (codeweavers.com) and the "Wine emulation" row (applegamingwiki.com). Both
      opened as real, separate OS windows showing the remote page. Both coexisted, which
      exercises unique label generation: the pre-fix bug there was SILENT (the renderer-side
      counter restarted at 0 after a main-window reload, re-requested the taken label
      `external-1`, Tauri rejected it, and the click simply did nothing), so a second window
      opening at all is the non-vacuous half of this check.
    defect: **both windows are titled "Tauri App"** — see gap G7. Mechanism passes; the title
      expectation fails.
    sub_check_not_run: the RELOAD variant of the label-collision regression (open a child window,
      reload the MAIN window, open another) was not run — the sequential-open case was. Recorded
      here rather than folded into the pass, per this document's own compound-item lesson.
    affordance_note: **the item's original wording named affordances that do not exist on macOS.**
      ProtonDB is Linux-gated in both places it appears (`AppleWikiInfo.tsx:46`
      `showProton = is.linux && ...`; `GameSubMenu/index.tsx:520` `!isSideloaded && isLinux`), and
      the score row lives behind an "Extra info" tab that only renders when `hasWikiInfo` is true
      (`GamePage/index.tsx:429`) — for Avowed it is false, correctly, because the lookup returned
      no `howlongtobeat`, no pcgamingwiki score and no `steamInfo`. The macOS-reachable
      `createNewWindow` affordances are `AppleWikiInfo`'s Crossover row
      (`showCrossover = is.mac && !!codeweavers`) and its Wine row (`showWine`), both on the
      DEFAULT Install info tab. Establishing this took an instrumented read of
      `getWikiGameInfo`'s actual return (`[UAT-8]` line in gamelib.log, 16:48:59) after two wrong
      predictions from render-gate reading alone.
    capability_isolation_NOT_covered: the security claim is **still static-only**. Confirming a
      child window has zero Tauri command access requires executing script inside that window,
      and GameLib's DevTools console accepts no input. This run establishes that the window
      opens, loads remote content, and is not titled with the app's name — it does NOT establish
      the capability boundary. The code reviewer's static verification (labels generated, never
      url-derived; `main` cannot be re-created; capability scoped `"windows": ["main"]`) remains
      the only evidence, and must not be read as live-confirmed.

8b. **Child windows — About window.** Open the About window and read its version string.
    result: **PASS — verified live 2026-08-22 (seventh session), under Tauri, on macOS.**
      Route: Settings tab -> About (between Documentation and Ko-fi, circled-i icon), the entry
      point added by quick task `260822-tv4`. Three observations, all matching prediction:
        (a) a separate OS window opened, titled "About GameLib", ~420x380, centered, not
            resizable;
        (b) a SECOND click on About REFOCUSED that same window -- no second window was created,
            exercising `WebviewWindow.getByLabel('about')`;
        (c) version string read `0.7.0`.
    preflight: bundle freshness was asserted BEFORE the operator looked, not assumed --
      572 files under `build/` newer than the fix commit `dea20f7be`, and the new key
      `about.navLabel` present in the shipped `build/assets/App-D6RNtBr3.js`. A bare `tauri dev`
      serves the last `electron-vite build`, so without this check a pass can score a fix the
      running app never contained.
    ⚠ OBSERVATION (c) IS VACUOUS FOR THE `0.0.0` REGRESSION, BY CONSTRUCTION — declared BEFORE
      the run, not as an excuse after it. T-34.1-17 fixed `getHeroicVersion()` returning `0.0.0`
      because `process.env.npm_package_version` is unset in the PACKAGED SEA sidecar.
      `pnpm tauri:dev` runs a plain bundled `sidecar.js` with the variable set, so pre-fix and
      post-fix code both report `0.7.0` here. Only a packaged build discriminates. What (c) DOES
      prove is that the version is not `unknown` -- i.e. the 1s `ABOUT_VERSION_TIMEOUT_MS` race
      resolved against a live sidecar rather than timing out.
    ticketed_from_this_run: the operator asked for About to become an in-app animated modal
      matching the Steam login overlay instead of a bare OS window. Raised as
      `.planning/todos/pending/2026-08-22-about-should-be-an-in-app-modal-matching-steam-login.md`.
      NOT a regression and NOT a condition on this pass -- polish, severity low. Note that if it
      lands, sub-observation (b) above stops having a subject: a Dialog has no second-window
      concept.

    PRIOR RESULT (2026-08-22, sixth session) — SUPERSEDED, kept because it was correct when
    written and its reasoning is the reusable part:
    result: **STRUCTURALLY UNREACHABLE UNDER TAURI — not pass, not defect.** Established
      2026-08-22 by source-level reachability check BEFORE an operator was sent to look.
      `tauriShowAboutWindow` is fully implemented (`tauriChildWindows.ts:98`) and
      `public/about.html` exists, but the ONLY caller of `showAboutWindow` in the tree is
      `src/backend/tray_icon/tray_icon.ts:124` — the **Electron** tray menu. There is no frontend
      call site, and Tauri's tray deliberately omits it (`main.rs:18`, `:5852`: "Deliberately out
      of scope: recent-games submenu, About/Reload/Debug, the macOS dock menu"). No user action
      can invoke it under Tauri.
    same_class_as_10f: item 10f was recorded inside a passing item for six weeks while
      `getLatestReleases()` had already returned `[]` unconditionally since before the session
      that recorded it. This is the same shape, caught one step EARLIER — before the operator
      looked rather than after. The mechanism was this document's own warning: check source-level
      reachability before sending an operator to look at anything.
    version_string_note: even with an entry point, `pnpm tauri:dev` could NOT falsify the `0.0.0`
      mode. That bug was `process.env.npm_package_version` being unset in the packaged SEA
      sidecar; dev runs a plain bundled `sidecar.js` with the variable set, so pre-fix and
      post-fix code both report correctly. Only a packaged build discriminates. Expected string
      is currently `0.7.0`. Item 10e's Electron pass stands and is unaffected.
    ticketed: `.planning/todos/pending/2026-08-22-about-window-is-unreachable-under-tauri.md`

9. **macOS titlebar: traffic lights, chrome reserve, and mechanism (L2-L9)** — on macOS, with
   `framelessWindow` toggled through both states across three stress themes.
   Expected: this is the D-06/D-15 deferred aesthetic call, REVERSED 2026-08-13 (see
   `## D-06 reversal` below) into a concrete contract: native traffic lights present in BOTH
   titlebar states, a 78px reserve that applies ONLY while overlaid, `WindowControls` hidden
   unconditionally on macOS, and eight individually-checkable mechanism questions (L2-L9) about
   the runtime `setTitleBarStyle` transition. Split at the granularity plan 34.1-14's protocol
   actually produced — **do NOT merge L7 and L8, and do NOT merge L2 and L5**, they are opposite
   directions of the same setter and either can fail alone.
   result: **28/28 individual observations PASS**, operator, live, 2026-08-14. Full per-item
     breakdown below. **Provenance, stated plainly:** Sections A and B were reported at SECTION
     granularity by the operator ("Section A - traffic lights present, changed to Dracula, test
     passes" / "Section B - Pass, with one UX tidy up needed"), with only the items the operator
     named explicitly (A1, C1, L7, L8) individually adjudicated; the remaining Section A/B rows
     below INHERIT the section verdict rather than carrying invented per-item wording. Section D
     (tray) was reported item by item — see test 6 above. This inheritance is the SAME risk this
     document's compound-item lesson names; it is NOT fully retired for the inherited rows, only
     for L7/L8, which were deliberately individually re-elicited because each requires its own
     quit-and-relaunch cycle that is easy to skip inside a section-level "pass."

   9a. Native traffic lights present, correctly coloured, in a normal native title bar —
       `midnightMirage` / `gruvbox_dark` / `dracula`. PASS. `midnightMirage`/`dracula` named
       explicitly (A1, C1); `gruvbox_dark` (B1) inherits the section verdict.
   9b. No GameLib-drawn window buttons duplicate the traffic lights — all three themes (A2/B2/C2).
       PASS (inherited from the section verdict).
   9c. With `framelessWindow` OFF, the leftmost nav tab has NO 78px leading gap — all three
       themes (A3/B3/C3). PASS (inherited from the section verdict).
   9d. **L2** — toggling `framelessWindow` ON visibly changes the titlebar (normal strip
       disappears, traffic lights float over content) WITHOUT a restart. PASS (inherited).
   9e. **L3** — the ON transition does not flicker, jump, resize, or show a black/white frame.
       PASS (inherited).
   9f. **L4** — with `framelessWindow` ON (overlaid), the leftmost nav tab clears the traffic
       lights (starts ~78px in, nothing overlapping/clipped). PASS (inherited). This is the "nav
       tabs clear the 78px reserve" observation.
   9g. **L5** — toggling `framelessWindow` back OFF restores the normal native title bar. PASS
       (inherited). **First-class finding that did NOT materialise: overlay is NOT one-way at
       runtime on this hardware** — the plan pre-wrote a finding for the opposite outcome.
   9h. **L6** — the OFF transition does not flicker, jump, resize, or show a stray frame. PASS
       (inherited).
   9i. **L7** — quit-and-relaunch with `framelessWindow` ON: no visible flip from a normal title
       bar to the overlaid one partway through load. **PASS, INDIVIDUALLY CONFIRMED.** Operator:
       "L7 no flipping." This is the branch where `tauri.conf.json`'s creation-time
       `titleBarStyle: "Visible"` and the runtime value DISAGREE — the branch most likely to
       show a flip. It does not.
   9j. **L8** — quit-and-relaunch with `framelessWindow` OFF: no flip at all. **PASS,
       INDIVIDUALLY CONFIRMED.** Operator: "L8 no flip." Creation-time and runtime values agree
       in this branch.
   9k. **L9(a)** — with `framelessWindow` ON and the app UNFOCUSED, dragging the navbar still
       moves the window (Tauri's own docs name unfocused-drag-fails as a known overlay-mode
       caveat). PASS (inherited).
   9l. **L9(b)** — with `framelessWindow` ON, the window TITLE text does not draw over the navbar.
       PASS (inherited).

10. **Electron parity** — `npm start` (the Electron build, not Tauri). Exercise the themes,
    language, About, releases, and changelog surfaces.
    Expected: all of these surfaces behave exactly as they did before this phase — confirming
    the D-07 `main.ts` extraction changed no observable Electron behavior — EXCEPT the deliberate
    macOS `WindowControls` change introduced by plan 34.1-11.
    result: **RE-RUN COMPLETE, 2026-08-14 (second session). 6 PASS, 1 STRUCTURALLY UNREACHABLE.**
      The OWED re-run is discharged. Split into 10a-10g below at the branch boundaries this
      document's compound-item lesson requires — the original single-line item bundled five
      unrelated surfaces plus a window-chrome claim behind one `pass`, which is precisely how
      10f's vacuity survived a full session unnoticed.
    incident (not a phase defect, historical): the first `npm start` attempt on 2026-07-25 failed
      with `dyld: Library not loaded` — repaired by re-extracting the cached Electron zip,
      unrelated to 34.1. **Did NOT recur** on 2026-08-14; `node_modules/electron/dist/Electron.app`
      was intact and both launches booted clean (exit 0).

    10a. **Electron window chrome — decorated (`framelessWindow` OFF).** Normal native macOS
         title bar with traffic lights; no GameLib-drawn window buttons.
         result: **PASS**, operator, live, 2026-08-14 ("e1 - pass").

    10b. **Electron window chrome — frameless (`framelessWindow` ON): the deliberate macOS
         change.** Native traffic lights present; GameLib's own `WindowControls` ABSENT.
         Expected: on 2026-07-25 this showed **both** sets of buttons — native traffic lights
         (from Electron's `main_window.ts:48` `titleBarStyle: 'hidden'`, which on macOS hides the
         chrome but keeps the lights) PLUS GameLib's own `WindowControls` (because
         `main_window.ts:16` counts `'hidden'` as frameless). Plan 34.1-11 removed the duplicate.
         result: **PASS**, operator, live, 2026-08-14 ("e2 passes"). **This is the load-bearing
           observation of the entire re-run** — it is the one thing plan 34.1-11 deliberately
           changed under BOTH shells, and the only reason the 2026-07-25 baseline was no longer
           a valid comparison. Confirmed: Electron now matches Tauri on macOS.
         mechanism_confirmed_in_source: `src/frontend/App.tsx:79` computes
           `showOverlayControls = isFrameless && !hasNativeOverlayControls && !isMac`. The
           `!isMac` term is UNCONDITIONAL and keyed on `platform`, not on a shell/runtime
           detection check (`App.tsx:73-76` says so explicitly) — which is exactly why the gate
           reaches the Electron build at all. Read before the observation, not after.
         restart_note: Electron reads `framelessWindow` at window-CREATION time
           (`main_window.ts:43-48`), so toggling it does not change the live window there; a
           quit-and-relaunch is required. Confirmed live — the operator's on/off/on toggle
           sequence at 17:50:52-56 changed nothing until relaunch. This is a genuine per-shell
           behavioural difference from Tauri (where L2/L5 proved it applies live, test 9d/9g),
           NOT a regression, and NOT a contradiction of the gap-G2 copy fix: the "(requires
           restart)" claim was removed because it is false on the shell GameLib is shipping.

    10c. **Themes.** Switch themes (incl. `midnightMirage` + one other); each applies immediately,
         no broken/unstyled state.
         result: **PASS**, operator, live, 2026-08-14 ("e3 - pass").

    10d. **Language.** Switch language and back to English; UI strings translate and revert.
         result: **PASS**, operator, live, 2026-08-14 ("e4 - pass").

    10e. **About — real version string.** About window shows the real version, never `unknown`
         or `0.0.0`.
         result: **PASS**, operator, live, 2026-08-14 ("e5 - pass"). Confirms the 34.1-04 fix
           (T-34.1-17: `electronStub.app.getVersion()` reading `package.json` for the packaged
           SEA case where `npm_package_version` is unset) holds under Electron too.

    10f. **Releases surface.** The "Update Available!" block with Stable/Beta links.
         result: **STRUCTURALLY UNREACHABLE BY DESIGN — not a pass, not a defect.**
           `getLatestReleases()` (`src/backend/utils.ts:869-875`) returns `[]` unconditionally.
           `HeroicVersion/index.tsx:107` gates the whole block on that array being non-empty, so
           it cannot render. The suppression is deliberate and commented: GameLib's version vs
           Heroic's 2.22+ would always fire a false update notice pointing users at *Heroic*
           downloads. To be re-enabled only when GameLib's own release pipeline ships and the
           function is repointed at GameLib's releases.
         **finding — the historical pass was VACUOUS.** The suppression landed in commit
           `054f97408` on **2026-07-02**, which is BEFORE the 2026-07-25 session that recorded
           "releases" as passing. That session could not have observed this surface either; the
           pass covered a branch that was never exercisable on the date it was recorded.
         lesson: same class as this document's compound-item lesson, one level further out. A
           compound item hides an un-run HALF; this hid an un-runnable WHOLE, because the item
           named five surfaces at once and a single `pass` absorbed the one that renders nothing.
           The discriminator that would have caught it is cheap and was never applied: for each
           named surface, ask *can this render at all in the current build* before asking *does
           it look right*.
         do_not_re_test: no operator action can move this until the suppression is lifted. Do
           not carry it forward as an open verification item.

    10g. **Changelog surface.** Settings tab → tier-2 column footer → click "GameLib Version:
         {version}" → changelog modal opens.
         result: **PASS on the mechanism; CONTENT DEFECT raised (gap G6).** Operator, live,
           2026-08-14: "e7 - pass, modal shows 1.0.0". The modal opens and renders correctly.
           **The content is wrong**: it is headed "GameLib 1.0.0" / `gamelib-v1.0.0` while the
           running app is **0.7.0** — and 10e confirmed every other version surface reads 0.7.0
           correctly, so this is isolated to `public/changelog.json`. See gap G6 below.
         discoverability_note: the operator could not initially find this surface or 10f. Neither
           is a separate screen — both live in `HeroicVersion`, mounted in the NavShell tier-2
           column (`NavShell/index.tsx:101`) where 34.10-07 relocated them when the sidebar was
           retired. The version slot carries `hidden={activeTab !== 'settings'}`
           (`NavShell/index.tsx:99`), so it is visible ONLY on the Settings tab. Recorded because
           "the tester could not find it" is the kind of observation that reads as a defect and
           is not one — but it does mean the 2026-07-25 "changelog" pass was made against the
           RETIRED sidebar footer, a different location, and this is the first confirmation the
           relocated entry point actually works.

## D-06 reversal — decided 2026-08-13

The 2026-07-25 session closed D-06 as "custom buttons on every platform, divergence accepted,
stop investing in frameless." **That decision is REVERSED.** It is preserved above as
`superseded_result` rather than deleted, because the reasoning that produced it was sound on the
information available at the time — what changed is that the information was incomplete.

**The decision:** on macOS, GameLib adopts an overlaid titlebar with **native traffic lights**.

- `src-tauri/tauri.conf.json` — set `"titleBarStyle": "Overlay"` on the `main` window. Its
  window block currently carries ONLY `label`/`title`/`width`/`height`/`resizable` — there is no
  titlebar key of any kind today.
- `--traffic-light-inset` — `0px` → **78px**.
- `WindowControls` — renders on **Windows/Linux only**; hidden on macOS.

**Why the traffic lights disappeared — an unmade decision, not a regression.** Electron and Tauri
were never reconciled:

- Electron sets `windowProps.titleBarStyle = 'hidden'` (`src/backend/main_window.ts:48`), which
  on macOS hides the chrome but **KEEPS the traffic lights**. `main_window.ts:16` also counts
  `titleBarStyle === 'hidden'` as frameless, so `WindowControls` renders too. That is the "both
  sets of buttons" the user remembered — and it IS the duplication D-06 was raised about.
- Tauri only ever calls `.setDecorations(!resolved)`
  (`src/preload/api/tauriWindowChrome.ts:279`). On macOS `setDecorations(false)` strips **all**
  decorations, traffic lights included. Tauri's actual equivalent of Electron's `hidden` is the
  config key `titleBarStyle: "Overlay"`, which was never set.

**The generalisable lesson: a cross-shell port that swaps the MECHANISM silently changes the
PRODUCT decision.** `setDecorations(false)` is not the Tauri translation of
`titleBarStyle: 'hidden'` — it is a different, stricter behaviour. No test failed, no reviewer
flagged it, and the divergence was then formally "accepted" in a UAT session by a user who could
not see what had been lost. Port reviews must ask *what did the old mechanism do that the new one
does not*, not merely *does the new one work*.

**78px is not a guess.** `Skill("sketch-findings-gamelib")` lists it as one of exactly TWO
constraints that **override aesthetics**: *"78px at the window's top-left belongs to the macOS
traffic lights whenever the Tauri titlebar is transparent or overlaid. No top-mounted bar starts
at x=0."* Every sketch's Annotate toggle renders this reserve.

**`--traffic-light-inset: 0px` was never a design position.** `NavShell/index.scss:24-33`
documents it as correct *because nothing overlaid the titlebar*, and explicitly warns: *"Do NOT
set this defensively to the sketches' reserved value: an un-overlaid native title bar makes that
reservation wrong, not merely unnecessary."* Overlay mode is precisely the trigger that flips it.
The same comment anticipated this exact moment: *"This exists as a token so that adopting overlay
mode later is a value change, not a re-layout."*

**`shellTokens.test.ts:54-56` pins the value at exactly `0px` and WILL go red.** It is
correct-as-written for the old mode — retune it with the decision, do not delete it.

**Consequence for the 2026-07-25 "stop investing in frameless" resolution:** that stance is also
superseded on macOS. Overlay mode moves work in the opposite direction — toward the OS providing
the controls rather than GameLib re-implementing them — so it reduces bespoke frameless machinery
on macOS rather than adding polish to it. The "candidate for removal" note still stands for
Windows/Linux, where custom buttons remain the only option.

**Live confirmation, 2026-08-14 (plan 34.1-14):** this entire decision is now the closed gap G4
— see test 9 above (28/28 PASS across the three stress themes plus all eight L-items) and
`## Gaps` below.

## Frameless-toggle-on-macOS decision (Section C) — decided 2026-08-14

Plan 34.1-10's mechanism reversal and plan 34.1-11's frontend gating (both above) left one open
question: given macOS now HAS a working runtime titlebar setter, should the `framelessWindow`
toggle stay exactly as built on macOS, or does the observed behaviour call for a constrained
option (hide the toggle, or add macOS-specific copy)? Plan 34.1-14 Section C answered this live.

**Determined path: PATH A.** Determined FROM, and only from, the Section B (L2-L9) answers above:
L2 PASS (toggling ON visibly changes the titlebar with no restart, test 9d), L5 PASS (toggling
OFF restores the normal native title bar, test 9g), and neither L3 nor L6 showed unacceptable
flicker (tests 9e/9h). PATH A's precondition — "L2 worked AND L5 worked AND neither L3 nor L6
showed unacceptable flicker" — is met on all four counts.

**Worth stating because the plan pre-wrote a finding for its opposite: overlay is NOT one-way at
runtime on this hardware.** L5 (test 9g) was the observation that would have forced PATH B, and
it passed.

**Selected option id: A1 (keep-functional).** The toggle stays exactly as built — it changes
macOS decorations the same way it changes Windows/Linux decorations, with native traffic lights
present in both states. No further code.

**Operator's reasoning (verbatim, per the recorded transcript):** A2 (revise the Settings copy)
was offered and declined. The copy had just been rewritten by plan 34.1-12 (dialog and the false
"(requires restart)" claim removed, closing gap G2), and the operator read the live label at
step D7 without flagging it as unclear — so there was no observed confusion for A2 to fix.

**Required phrasing check (mandated by this plan and by plan 34.1-15's own threat register,
T-34.1-15-07):** this outcome is recorded as PATH A on the strength of what was observed. It is
NOT phrased as, and must never be summarised as, "Tauri has no runtime API for this" — the
`setTitleBarStyle` setter demonstrably exists (`@tauri-apps/api` 2.11.1,
`node_modules/@tauri-apps/api/window.d.ts:1185`) and demonstrably works in both directions on
this hardware. An earlier planning revision (plans 34.1-10/34.1-14, first draft) asserted the
opposite and was falsified by the plan-checker before it reached the operator; that false
constraint must not be reintroduced through a careless summary of this decision, here or anywhere
downstream.

## Section D — Tray + copy: additional context

Test 6 above carries the full per-sub-item breakdown (6a-6d). One additional note, preserved from
plan 34.1-14's own summary: D7's reported text ("user frameless window") differs from source by
one character (`Use` vs a mis-transcribed `user`) — verified directly against
`public/locales/en/gamelib.json:66` and `UseFramelessWindow.tsx:29-30`, both reading exactly
`Use frameless window`. Recorded as a transcription slip in the operator's report, not a defect;
the substantive claim (no `(requires restart)` parenthetical) is confirmed by both source
locations regardless.

## Non-blocking UX finding raised during Section B (2026-08-14)

Operator, verbatim: *"When going to full screen mode the traffic lights disappear from the
titlebar (normal mac behavior), however that makes the titlebar look too large. Also I think you
could change titlebar to have tabs on same line as traffic lights. That can go on todo list but
test passes."*

Both captured as todos (commit `3be0730ef`), explicitly classified by the operator as NOT
blocking this gate — and tracked as new gap **G5** below:

1. `.planning/todos/pending/fullscreen-traffic-light-reserve-not-collapsed.md` — **a real
   defect with a one-token fix.** `index.scss:151` lacks the `:not(.fullscreen)` exclusion that
   its own sibling rule at `index.scss:179` already carries, and `App.tsx:118` already sets the
   class. In fullscreen macOS hides the lights, so the 78px reserve becomes dead space.
   **`shellTokens.test.ts` could not have caught this**: its model of "scoped correctly" is
   two-state (`.macOverlayTitlebar` vs not), and this is a three-state problem. Fourth time on
   this project a live gate has beaten a green suite — and again not by asking the same question
   harder, but by asking one the suite's state model did not contain.
2. `.planning/todos/pending/nav-tabs-on-the-traffic-light-line.md` — design proposal, routed to
   `/gsd-sketch` rather than direct implementation because the 78px inset comes from settled
   sketch decisions and Phase 34.12 is already queued against the same nav shell.

## Manual-Only Verifications

None beyond the ten items above — this document's whole purpose is the deferred-live-UAT list;
there is no separate "manual, not deferred" category for this slice per D-15.

## Summary

total: 32
passed: 26
partial: 1
blocked: 3
skipped: 1
structurally_unreachable: 1
pending: 0
issues: 0

Counts as of the **2026-08-14 Electron-parity re-run (second session that day)**, RECOUNTED from
the split entries (1a/1b, 2, 3, 4, 5a/5b, 6a/6b/6c/6d, 7, 8, 9a-9l, 10a-10g = 32 individual
entries), not adjusted arithmetically from the prior 26-entry tally. The delta is item 10 being
split into seven (26 − 1 + 7 = 32).

- **passed (26):** 1b, 2, 3, 4, 5a, 6a, 6b, 6c, 9a, 9b, 9c, 9d, 9e, 9f, 9g, 9h, 9i, 9j, 9k, 9l,
  10a, 10b, 10c, 10d, 10e, 10g
- **partial (1):** 6d (PASS on macOS by design; UNFIXED on Windows/Linux — a genuine, known,
  still-open code/data defect, not an unverified one)
- **blocked (3):** 1a (a Windows or Linux machine), 5b (a sub-1200px-wide display), 7 (a game
  controller)
- **skipped (1):** 8 (child windows — not attempted in any session; not blocked by unavailable
  hardware, simply not yet run. It WAS runnable during the 2026-08-14 parity session and was
  deliberately not selected — this is now the only open item with no blocker of any kind.)
- **structurally_unreachable (1):** 10f (releases — `getLatestReleases()` returns `[]`
  unconditionally by deliberate suppression; no operator action can move it)

**A new category was needed, and that is itself the finding.** 10f is not a pass, not a failure,
and not blocked — it is unrunnable by design. The prior tally had no way to express that, which
is why a vacuous pass sat in the `passed` column from 2026-07-25 until it was read at the source
level. Note that `issues: 0` remains accurate on the strict definition used throughout this
document (an item whose observed behaviour contradicted its expectation): 10g's defect is in
CONTENT, not behaviour, and is carried in `## Gaps` as G6 rather than by flipping 10g's result.

**Historical note, not a numeric category:** test 9's underlying decision (D-06) was REOPENED AND
REVERSED on 2026-08-13 after a 2026-07-25 "pass" that did not see the traffic-light loss. That
reversal is now fully closed — see `## D-06 reversal` and gap G4 below — and every current test 9
sub-item reads PASS against the REVERSED (not the original) expectation.

**What the 2026-08-14 session settled that no prior session could:** the phase's own load-bearing
open questions from gap cycle 1 — does the macOS runtime titlebar setter actually work in both
directions on real hardware (yes, L2/L5 both PASS), does the overlay state trap the user with no
way back (no, L5 PASS refutes the pre-written opposite finding), does the macOS tray template
render legibly against the real effective menu-bar appearance (yes, D4/D5), and does the
frameless-toggle-on-macOS decision resolve to PATH A or PATH B (PATH A, live-determined) — are
all now answered by a real operator on a real running build, not inferred from source.

## Gaps

- truth: "Every external child window is titled 'Tauri App', not by the remote page"
  status: failed
  id: G7
  reason: >
    `tauriChildWindows.ts:78-86` omits `title` deliberately and defends the choice in a comment
    asserting 'Tauri falls back to the loaded document's own title. Do not fix this by adding a
    title back.' That premise is FALSE — Tauri falls back to its own built-in default 'Tauri App'
    (verified against the installed crate at tauri-utils-2.9.2/src/config.rs:2375, not from
    memory); Tauri v2 does not sync a window title from document.title at all. Observed live on
    TWO windows in one session, 2026-08-22 (codeweavers.com and applegamingwiki.com).
  why_no_gate_caught_it: >
    `childWindows.test.ts:189` asserts `expect(options.title).toBeUndefined()` — that no title was
    passed AT THE CALL SITE. It cannot assert what the window displays, and stays green
    regardless. A verification checking a callsite rather than a behaviour. It survived because no
    child window had ever been observed running: the code reviewer verified WR-07 statically and
    UAT item 8 sat unrun for five sessions.
  security_impact: >
    NONE. WR-07's concern was that titling renderer-supplied REMOTE content 'GameLib' presents an
    attacker-controlled page under the app's own name — that is NOT realized, the window is not
    titled GameLib. Label generation and capability scoping are untouched. What fails is the
    stated Electron-parity goal plus user-facing polish (the framework name leaks into every
    external window).
  severity: low
  test: 8a
  recommended_fix: >
    Title the window with the URL's HOSTNAME (codeweavers.com). Not the app's name, so WR-07
    holds; no framework leak; and strictly better anti-phishing than a page-controlled title,
    because an attacker controls <title> but not the host they are served from. Whatever is
    chosen, replace the callsite assertion with one naming the resulting title, or the next reader
    inherits the same green-and-wrong gate.
  ticketed: ".planning/todos/pending/2026-08-22-external-child-windows-are-titled-tauri-app.md"

- truth: "With framelessWindow on, GameLib's own titlebar buttons sit at the window's top edge and minimize/maximize/restore/close the real OS window"
  status: failed
  reason: "STATICALLY FIXED, NEVER LIVE-CONFIRMED. Original: user reported 'windows buttons are missing!' — on follow-up, 'actually has windows like buttons on right UNDER the title bar, but there are no mac traffic lights. on a previous version there were both sets of buttons'. Root cause fixed by plan 34.1-09 (2026-08-13): WindowControls/index.scss:2 re-anchored from grid-area: content to grid-area: navbar, gated by a new premise-asserting test (windowControlsPlacement.test.ts) that recomputes the expected row from .App's own live grid-template-areas. This is strong static evidence. It has NEVER been observed on a real Windows or Linux window in any of the three sessions to date — this project's hardware is macOS-only, and WindowControls is gated OFF entirely on macOS (see gap G4), so it structurally cannot render here regardless of who runs the gate."
  severity: major (static fix believed correct; live confirmation genuinely outstanding)
  test: 1a
  regression: true
  regressed_from: "pass, 2026-07-25 live session (this same document)"
  regressed_by: "Phase 34.10 (navigation shell) — completed 2026-08-09, rewrote src/frontend/App.css grid-template-areas"
  fixed_by: "plan 34.1-09 (2026-08-13) -- static only"
  blocked_by: "a Windows or Linux machine"
  root_cause: "`WindowControls/index.scss:2` declared `grid-area: content`, an area name
    whose POSITION changed. The retired sidebar grid put `content` in the top row, so
    `align-self: start` placed the buttons at the window's top edge. 34.10's grid inserts a 56px
    `navbar` row above it, so `content` now starts below the titlebar. Nothing errored — the
    area name still resolves, it just means somewhere else."
  artifacts:
    - path: "src/frontend/components/UI/WindowControls/index.scss:2"
      issue: "FIXED 2026-08-13 -- now grid-area: navbar (was: content, a sidebar-era area that 34.10 moved below the navbar)"
    - path: "src/frontend/components/UI/NavShell/__tests__/windowControlsPlacement.test.ts"
      issue: "NEW 2026-08-13 -- premise-asserting gate deriving the expected row from .App's live grid-template-areas, RED-proven against the actual regression input"
    - path: "src/frontend/App.css:27-31"
      issue: "the 34.10 grid rewrite that changed what 'content' means; consumer audit now complete (34.1-09 Task 2), no other live consumer found"
  ruled_out:
    - "Render gate: App.tsx:47 showOverlayControls computes true (navigator.windowControlsOverlay is undefined under WKWebView); component renders at App.tsx:125"
    - "Portal wrapper: Tier2PortalProvider (NavShell/Tier2PortalContext.tsx:56-60) emits NO DOM element, so .windowControls IS still a direct grid item of .App"
    - "CSS tokens: --overlay-controls-height and --overlay-controls-width both declared at index.scss:38-41; not a no-fallback custom-property collapse"
  missing:
    - "A live Windows or Linux window to confirm the re-anchored buttons actually sit at the window's top edge and each click actually minimizes/maximizes/restores/closes the real OS window"
  entangled_with: "test 9 / the D-06 reversal — the macOS half of the ORIGINAL truth statement (test 1b) IS live-confirmed via test 9's traffic-light/no-duplicate-buttons observations; only the Windows/Linux half of test 1 remains open"
  debug_session: null

- truth: "The frameless-window Settings toggle describes its own behaviour accurately"
  status: closed
  closed_by: "plan 34.1-12 (2026-08-13, code fix) + plan 34.1-14 (2026-08-14, D7/D8 live confirmation)"
  closed_evidence: "D7 (operator read the live Settings row; verified against public/locales/en/gamelib.json:66 and UseFramelessWindow.tsx:29-30, both reading exactly 'Use frameless window', no parenthetical) and D8 (operator: 'd8 - pass' -- no 'Experimental feature ahead' confirmation dialog appears on toggle-on). See test 2 above."
  reason: "User reported (2026-08-13): 'frameless toggle works without restart. so text should not say needs restart, and warning should go'"
  severity: minor
  test: 2
  root_cause: "UI copy predated the on-toggle re-apply this phase shipped. The toggle applied
    live (test 2's behavioural half PASSED, twice), but the label carried Electron's 'requires
    restart' caveat and an experimental-feature confirmation dialog gated the on-path."
  artifacts:
    - path: "public/locales/en/gamelib.json:66"
      issue: "FIXED 2026-08-13 -- settings.framelessWindowDescription now reads exactly 'Use frameless window', moved to the fork-owned gamelib namespace"
    - path: "src/frontend/screens/Settings/components/UseFramelessWindow.tsx"
      issue: "FIXED 2026-08-13 -- collapsed to a plain ToggleSwitch; showDialogModal/ContextProvider/useContext removed"
  fixed_by: "plan 34.1-12 (code), plan 34.1-14 D7/D8 (live confirmation)"
  note: "The behaviour under test PASSED from the start — this was documentation contradicting the code, now closed on both the code side and the live-observation side."

- truth: "Toggling the dark-tray-icon setting swaps the tray image within roughly 500ms"
  status: closed
  closed_by: "plan 34.1-13 (2026-08-13, macOS AppKit template image) + plan 34.1-14 (2026-08-14, D4/D5/D5b live confirmation) -- CLOSED ON MACOS ONLY"
  closed_evidence: "D4 (operator: 'can see the silhoutte' -- template glyph legible as a monochrome cat silhouette). D5 (operator: 'on macos lightmode the header is blue (not white) the icon stays white like all the others' -- the glyph auto-tints to match the EFFECTIVE menu-bar appearance and is indistinguishable in tinting behaviour from the system's own template icons, the discriminating evidence that set_icon_with_as_template is honoured at runtime). D5b (operator: 'Nothing visibly changes' when toggling the setting -- the CORRECT vestigial-by-design result, not a miss)."
  windows_linux_remainder: "NOT CLOSED. public/icon-dark.png and public/icon-light.png (+ @2x/@3x) remain byte-identical on Windows/Linux, which still use the original two-file colour-selection path, explicitly out of plan 34.1-13's redirect scope. See test 6d above and the re-scoped pending todo (tray-dark-light-icons-are-identical.md) -- NOT closed as a todo, only re-scoped."
  reason: "User reported (2026-07-25): 1-3 yes, dark tray icon does nothing"
  severity: major (macOS: resolved by design change; Windows/Linux: still open, unchanged severity)
  test: 6d
  root_cause: "`public/icon-dark.png` and `public/icon-light.png` were (and, on Windows/Linux,
    still are) BYTE-IDENTICAL. On macOS, plan 34.1-13 superseded the swap-based fix entirely with
    a monochrome AppKit template image that AppKit auto-tints per effective appearance, making
    the dark/light DISTINCTION itself unnecessary on that platform -- darkTrayIcon is now
    vestigial there by design, not fixed by making the two assets differ."
  origin: "Commit 34fc2f5f 'rebrand app identity from Heroic to GameLib' (2026-06-27) overwrote
    both tray slots with a single image; 0d81f046 (gamer-cat artwork) flattened them again.
    Upstream Heroic had distinct icons (328ebd15 / 8289b074). This predates Phase 34.1 by about
    a month."
  artifacts:
    - path: "public/icon-tray-template.png"
      issue: "NEW 2026-08-13 -- macOS-only monochrome AppKit template, hue-segmented from icon-dark.png, live-confirmed legible 2026-08-14"
    - path: "src-tauri/src/main.rs"
      issue: "wired to icon_as_template/set_icon_with_as_template, cfg(target_os = \"macos\")-gated with a decode-failure fallback to the (still identical) colour pair"
    - path: "public/icon-dark.png (+ @2x, @3x) / public/icon-light.png (+ @2x, @3x)"
      issue: "STILL byte-identical -- Windows/Linux only, unchanged by plan 34.1-13, remains the genuinely open remainder"
  missing:
    - "Windows/Linux: regenerate icon-dark.png/@2x/@3x as a genuinely distinct dark treatment of the gamer-cat artwork (needs a design pass; do not auto-invert branded artwork -- an RGB-inversion attempt was tried and rejected, see 34.1-13-SUMMARY.md)"
  test_gap: "Every existing test asserts on the SELECTOR, never the PAYLOAD, for the Windows/Linux colour-pair path -- unchanged by this closure."
  debug_session: ".planning/debug/tray-icon-swap-noop.md"

- truth: "On macOS the titlebar shows native traffic lights over an overlaid titlebar, and the nav tabs clear them"
  status: closed
  closed_by: "plan 34.1-10 (2026-08-13, mechanism: setTitleBarStyle) + plan 34.1-11 (2026-08-13, frontend: 78px reserve + WindowControls gating) + plan 34.1-14 (2026-08-14, live confirmation across all three stress themes plus all eight L-mechanism items)"
  closed_evidence: "Section A/B/C, live, 2026-08-14: 'traffic lights present' (A1, midnightMirage) and 'changed to Dracula, test passes' (C1, dracula); gruvbox_dark (B1) inherits the section verdict. All eight L-items PASS, including L7/L8 individually re-elicited ('L7 no flipping', 'L8 no flip') and L5 ('overlay is NOT one-way at runtime' -- the first-class finding the protocol was watching for did not materialise). See test 9 above for the full 9a-9l breakdown."
  reason: "User reported (2026-08-13): 'there are no mac traffic lights. on a previous version there were both sets of buttons'. D-06 reopened and REVERSED 2026-08-13 -- see `## D-06 reversal`."
  severity: major
  test: 9
  decision_reversal: true
  supersedes: "the 2026-07-25 acceptance of D-06's custom-buttons-everywhere divergence"
  root_cause: "The Tauri port used `.setDecorations(false)` where Electron used
    `titleBarStyle: 'hidden'`. On macOS those are NOT equivalent: Electron's variant keeps the
    traffic lights, Tauri's strips every decoration. Tauri's actual equivalent — the window
    config key `titleBarStyle: \"Overlay\"` — was never set. A mechanism swap silently changed
    the product decision; no test failed and no reviewer flagged it."
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      issue: "FIXED 2026-08-13 -- titleBarStyle: \"Visible\" (creation-time state); runtime driven by setTitleBarStyle"
    - path: "src/preload/api/tauriWindowChrome.ts"
      issue: "FIXED 2026-08-13 -- applyFramelessDecorations drives setTitleBarStyle('overlay'|'visible') on macOS; setDecorations structurally unreachable there"
    - path: "src/frontend/components/UI/NavShell/index.scss"
      issue: "FIXED 2026-08-13 -- --traffic-light-inset retuned 0px -> 78px, scoped to .App.macOverlayTitlebar"
    - path: "src/frontend/components/UI/NavShell/__tests__/shellTokens.test.ts"
      issue: "RETUNED 2026-08-13, not deleted, to the new 78px value plus a scoping gate"
    - path: "src/backend/main_window.ts:16,48"
      issue: "the Electron reference behaviour (titleBarStyle: 'hidden' + WindowControls) that produced the 'both sets of buttons' the user remembers -- now deliberately diverged from on macOS (see UAT test 10's OWED re-run)"
  missing: []
  entangled_with: "test 1 (the macOS half, test 1b, is live-confirmed here) and Section C's frameless-toggle-on-macOS decision (PATH A / A1) -- see the dedicated section above"

- truth: "In macOS fullscreen, the 78px traffic-light reserve collapses (no dead leading space once the OS hides the lights)"
  status: failed
  reason: "User reported (2026-08-14, non-blocking, during Section B): 'When going to full screen mode the traffic lights disappear from the titlebar (normal mac behavior), however that makes the titlebar look too large.' Explicitly classified by the operator as NOT blocking the gate."
  severity: minor
  test: "new, discovered during test 9 (Section B)"
  root_cause: "`index.scss:151` lacks the `:not(.fullscreen)` exclusion that its own sibling rule
    at `index.scss:179` already carries, and `App.tsx:118` already sets the `.fullscreen` class.
    macOS hides the traffic lights in fullscreen, but the reserve rule doesn't know to collapse
    with them -- a THREE-state problem (`.macOverlayTitlebar` x `.fullscreen`) that
    `shellTokens.test.ts`'s existing two-state model (`.macOverlayTitlebar` vs not) could not
    have caught."
  artifacts:
    - path: "src/frontend/components/UI/NavShell/index.scss:151"
      issue: "missing :not(.fullscreen), unlike its sibling rule at :179"
  missing:
    - "Add :not(.fullscreen) to index.scss:151, matching :179's existing pattern"
    - "A three-state test (matching shellTokens.test.ts's own style) asserting the reserve collapses specifically in fullscreen"
  test_gap: "shellTokens.test.ts's scoping model is two-state (.macOverlayTitlebar vs not); this is a three-state case the model structurally cannot represent without extension."
  todo: ".planning/todos/pending/fullscreen-traffic-light-reserve-not-collapsed.md (opened commit 3be0730ef)"
  debug_session: null

- truth: "The changelog modal describes the version the user is actually running"
  status: failed
  reason: "User reported (2026-08-14, Electron parity re-run): 'e7 - pass, modal shows 1.0.0'. The modal opens and renders correctly -- the mechanism passes -- but it is headed 'GameLib 1.0.0' / tag_name gamelib-v1.0.0 while package.json is at 0.7.0. Test 10e confirmed in the SAME session that every other version surface reads 0.7.0 correctly, which isolates this to public/changelog.json rather than to version resolution."
  severity: minor
  test: 10g
  root_cause: "`getCurrentChangelog()` (`src/backend/utils.ts:877-891`) reads
    `public/changelog.json` verbatim off disk and returns it as the `Release` object the modal
    renders. Nothing derives or validates it against `package.json`. The file was written
    2026-07-02 and still carries the pre-renumber identity. The 2026-07-20 v1.x -> 0.x renumber
    swept the project with a lookahead-guarded regex `v1\\.([0-6])(?!\\d)(?!\\.\\d)` DELIBERATELY
    written to preserve literal version strings -- `gamelib-v1.0.0` is named in the renumber
    record as one of the strings that guard protected. The guard is correct for git tags and
    dependency pins and wrong here, because this particular 1.0.0 is not a version reference, it
    is user-facing display copy."
  lesson: "A renumber sweep that protects 'version strings' as a category will silently skip the
    subset of them that are RENDERED TO USERS. Protection and correctness diverge exactly where a
    version string doubles as content. Related in kind to gap G3's data defect: in both cases the
    code is correct, the asset it reads is not, and every test asserts on the mechanism rather
    than the payload."
  artifacts:
    - path: "public/changelog.json"
      issue: "tag_name/name/html_url and the `## GameLib 1.0.0` heading inside `body` all still say 1.0.0; the body's feature list was authored for a 1.0.0 that never released"
    - path: "src/backend/utils.ts:877-891"
      issue: "getCurrentChangelog() reads the file verbatim with no cross-check against package.json"
  missing:
    - "Rewrite public/changelog.json for 0.7.0 (tag_name, name, html_url, and the heading inside body); confirm the feature list describes what 0.7.0 actually ships"
    - "Decide whether this file should be generated rather than hand-maintained -- it is the only version surface not derived from package.json, which is why it drifted alone"
    - "Add a gate asserting changelog.json's tag_name/name agree with package.json's version (a two-line check that would have caught the drift the day it started)"
  not_covered_by: "The known-open renumber carryforward is the `gamelib-v1.0` GIT TAG on the
    `gamelib` remote (a pushed-ref rewrite, pending user confirmation). Different artifact. No
    pending todo covered public/changelog.json before this one."
  todo: ".planning/todos/pending/changelog-modal-shows-stale-1-0-0.md (opened 2026-08-14)"
  debug_session: null

- truth: "The releases surface shows available GameLib updates"
  status: not_applicable
  reason: "STRUCTURALLY UNREACHABLE BY DESIGN, and the historical pass covering it was VACUOUS. `getLatestReleases()` (`src/backend/utils.ts:869-875`) returns `[]` unconditionally; `HeroicVersion/index.tsx:107` gates the entire 'Update Available!' block on that array being non-empty. The suppression is deliberate and commented -- GameLib's version vs Heroic's 2.22+ would always fire a false update notice pointing users at HEROIC downloads."
  severity: none (intentional suppression, correctly implemented)
  test: 10f
  vacuous_prior_result: "The suppression landed in commit `054f97408` on 2026-07-02 -- BEFORE the
    2026-07-25 session that recorded 'releases' as part of a passing item 10. That session could
    not have observed this surface either. The pass covered a branch that was never exercisable
    on the date it was recorded, and it survived unchallenged through two subsequent sessions."
  lesson: "Same class as this document's compound-item lesson, one level further out. A compound
    item hides an un-run HALF; this hid an un-runnable WHOLE, because item 10 named five surfaces
    at once and a single `pass` absorbed the one that renders nothing. The missing discriminator
    is cheap and was never applied: for each named surface, ask *can this render at all in the
    current build* BEFORE asking *does it look right*. Source-level reachability is a
    precondition for a live observation to mean anything."
  re_enable_when: "GameLib's own release pipeline ships and getLatestReleases() is repointed at
    GameLib's GitHub releases. Until then no operator action can move this."
  missing: []
  do_not_carry_forward: true

## The unavoidable gap, restated

Every automated test in this phase proves **wiring**: that the right Tauri API was called with
the right arguments. As of 2026-08-14, the macOS half of that gap has been substantially closed
by a real live operator on real hardware — 28/28 titlebar/tray observations PASS — and the
**Electron parity re-run is now discharged** (test 10a-10g, 6 PASS + 1 structurally unreachable),
removing the item that carried a hard Phase 35 deadline.

What remains UNOBSERVED, after four sessions:

- **test 1a** — Windows/Linux window-button placement. `blocked_by`: a Windows or Linux machine.
- **test 5b** — the sub-1200px DPI-compensation zoom branch. `blocked_by`: a narrow display.
- **test 6d (Windows/Linux half)** — tray dark/light swap. `blocked_by`: distinct artwork (a
  design task).
- **test 7** — gamepad navigation. `blocked_by`: a game controller.
- **macOS tray template against an OPAQUE dark menu bar** — the second half of the 34.1-14 visual
  sweep, inferred by symmetry rather than observed. `blocked_by`: nothing — needs System Settings
  → Accessibility → Display → Reduce Transparency. Runnable today.
- **test 8** — child windows and the About version round-trip. `blocked_by`: **nothing at all.**
  It was runnable during the 2026-08-14 parity session and was deliberately not selected. It is
  now the only item with no blocker of any kind, and it still carries an unconfirmed
  security-relevant claim: child `WebviewWindow`s load renderer-supplied REMOTE content with no
  `<webview>` isolation boundary, and the zero-command-access design has only ever been checked
  statically.

Each carries a concrete, greppable `blocked_by` above rather than a vague category word — and
where the blocker is *nothing*, that is stated as such rather than left to read as unavailability.
Read this document together with `34.1-PORTED-CHANNELS.md` §6 — neither artifact should be read in
isolation, because either one alone could leave a later reader with the wrong impression.

**One correction this session forced on the document's own method.** Three sessions of splitting
compound items at their branch boundaries still did not catch test 10f, because splitting asks
*which halves ran* and 10f's failure was that its whole could never run. Reachability is a
separate question from coverage, it is answerable at the source level in seconds, and it should be
asked of every named surface before an operator is sent to look at it.
