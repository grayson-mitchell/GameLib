---
phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome
plan: 09
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/frontend/components/UI/WindowControls/index.scss
  - src/frontend/screens/Settings/index.css
  - src/frontend/screens/WebView/index.css
  - src/frontend/components/UI/NavShell/__tests__/windowControlsPlacement.test.ts
autonomous: true
requirements: [REQ-34.1-01, REQ-34.1-03]

must_haves:
  truths:
    - "With framelessWindow on (Windows/Linux), GameLib's own titlebar buttons render in the SAME grid row as the NavShell navbar, at the window's top edge — not below it"
    - "Exactly one stylesheet under src/frontend declares `grid-area: content`, and it is `.App .content` in App.css"
    - "A test recomputes WindowControls' grid row from `.App`'s own `grid-template-areas`, so a future grid rewrite turns it RED instead of silently displacing the buttons again"
  artifacts:
    - path: "src/frontend/components/UI/WindowControls/index.scss"
      provides: "WindowControls anchored to the navbar row"
      contains: "grid-area: navbar"
    - path: "src/frontend/components/UI/NavShell/__tests__/windowControlsPlacement.test.ts"
      provides: "premise-asserting placement gate + complete grid-area:content census"
      min_lines: 90
  key_links:
    - from: "src/frontend/components/UI/WindowControls/index.scss"
      to: "src/frontend/App.css grid-template-areas"
      via: "windowControlsPlacement.test.ts recomputes the row index from the live matrix"
      pattern: "grid-template-areas"
    - from: "src/frontend/components/UI/WindowControls/index.scss"
      to: "src/frontend/components/UI/NavShell/index.scss .NavShell__navbar"
      via: "both resolve to the same row of .App's grid"
      pattern: "grid-area:\\s*navbar"
---

<objective>
Close gap G1 (UAT test 1, REGRESSION, major): `WindowControls` renders BELOW the titlebar
because `WindowControls/index.scss:2` still declares `grid-area: content`, an area name whose
POSITION Phase 34.10 moved. Re-anchor it to the navbar row, audit and prune every other
`grid-area: content` consumer, and add a gate that asserts the PREMISE (WindowControls and the
navbar share a coordinate space) rather than a consequence of it.

Purpose: the existing guard (`appShellLayout.test.ts:171`) is non-vacuous, correctly written,
green — and guards a collision that can no longer occur, because its layout premise moved out
from under it. Re-anchoring restores that guard's premise AND adds one that cannot be
invalidated the same way, because it derives the row index from `.App`'s own grid definition.

Output: WindowControls in the navbar row on Windows/Linux, a complete `grid-area: content`
census with the two dead consumers removed, and a new premise-asserting gate.

**Scope boundary — the macOS half of this fix lives in plan 34.1-11**, which gates
`WindowControls` off entirely on macOS per the D-06 reversal. This plan is the "move it"
half; 34.1-11 is the "hide it on macOS" half. Do not add any platform branch here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-HUMAN-UAT.md

<already_ruled_out>
The UAT diagnosis eliminated these three candidates with evidence. Do NOT re-walk them, do NOT
write tasks against them, do NOT "double-check" them:

1. **Render gate** — `App.tsx:47` `showOverlayControls = isFrameless && !hasNativeOverlayControls`
   computes TRUE under Tauri (`navigator.windowControlsOverlay` is undefined under WKWebView).
   The component DOES render, at `App.tsx:125`.
2. **Portal wrapper** — `NavShell/Tier2PortalContext.tsx:56-60`'s `Tier2PortalProvider` returns a
   bare `Context.Provider` with NO DOM element, so `.windowControls` IS still a direct grid item
   of `.App`.
3. **CSS token collapse** — `--overlay-controls-height` and `--overlay-controls-width` are both
   declared at `src/frontend/index.scss:38-41`. Not a no-fallback custom-property collapse.
</already_ruled_out>

<interfaces>
`.App`'s grid, as Phase 34.10 left it (`src/frontend/App.css:26-32`) — this is the coordinate
system every `grid-area` in the shell resolves against:

  grid-template-columns: min-content 1fr;
  grid-template-areas:
    'offline offline'   <- row 0
    'navbar navbar'     <- row 1  (56px, --navbar-height)
    'tier2 content'     <- row 2
    'tier2 controller'; <- row 3

`content` is row 2, column 2. Under the RETIRED sidebar grid it was row 1, which is why
`align-self: start` used to place the buttons at the window's top edge.

Direct grid items of `.App`, read from `App.tsx:99-126` (the non-console branch):
  <OfflineMessage/>            -> .offline-message  -> grid-area: offline   (App.css:113)
  <NavShell/>                  -> a FRAGMENT emitting .NavShell__navbar (grid-area: navbar,
                                  NavShell/index.scss:38) and .NavShell__tier2 (grid-area:
                                  tier2, NavShell/index.scss:176)
  <main className="content">   -> .content          -> grid-area: content   (App.css:63)
  <div className="controller"> -> .controller       -> grid-area: controller(App.css:104)
  {showOverlayControls && <WindowControls/>} -> .windowControls              <- THIS PLAN

Route components (`.Settings`, `.WebView`) are rendered by `<Outlet/>` INSIDE
`<main className="content">`, which is `display: flex`. They are flex children, not grid items.

Project test idiom for stylesheet gates (there is NO jsdom and NO CSS engine in the `Frontend`
jest project — see `shellTokens.test.ts`'s own docstring): read the file, run it through
`stripSourceComments` from `backend/testUtils/stripSourceComments`, then scan. `extractBlock`
(a nested-brace-aware block extractor anchored with `^...\{` multiline) is defined in
`appShellLayout.test.ts:86-101` — copy that helper rather than writing a naive
`/\.sel\s*\{[^}]*\}/`, which truncates at the first nested `}`.
</interfaces>

@src/frontend/components/UI/WindowControls/index.scss
@src/frontend/App.css
@src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor .windowControls to the navbar row</name>
  <files>src/frontend/components/UI/WindowControls/index.scss</files>
  <read_first>
    - src/frontend/components/UI/WindowControls/index.scss (the file being modified)
    - src/frontend/App.css (lines 23-58 — the grid this must resolve against)
    - src/frontend/components/UI/NavShell/index.scss (lines 37-56 and 129-137 — the navbar's own
      grid-area, and the D-05 trailing-reserve rule this change makes meaningful again)
    - src/frontend/components/UI/WindowControls/index.tsx (what actually renders — a single
      `<div className="windowControls">` with three SvgButtons)
  </read_first>
  <action>
    In the `.windowControls` rule:

    1. Change `grid-area: content;` to `grid-area: navbar;`. `.NavShell__navbar` also occupies
       that area — grid items are ALLOWED to overlap, and the existing `z-index: 100` already
       puts `.windowControls` above it. This overlap is the design D-05 assumed: the reserve
       rule `.frameless:not(.fullscreen) .NavShell__navbar { padding-inline-end: calc(...) }`
       (`NavShell/index.scss:133-137`) exists precisely so the navbar's nav-right cluster does
       not sit under these buttons.
    2. DELETE `position: sticky;` and `top: 0px;`. Both are dead in the new position: a sticky
       grid item's containing block is its own grid area, and `navbar` is a single
       `min-content` row exactly as tall as the navbar — zero sticky travel. `appShellLayout
       .test.ts:332` already gates `.NavShell__navbar` against exactly this wrong-but-plausible
       declaration for the same reason.
    3. KEEP `justify-self: right;`, `align-self: start;`, `z-index: 100;`,
       `-webkit-app-region: no-drag;` and the whole nested `button { ... }` block unchanged.
    4. Replace the rule's leading context with a comment that records: the area name is
       `navbar`, NOT `content`; `content` was correct only under the RETIRED sidebar grid where
       it was the top row; Phase 34.10 (`App.css:27-31`) inserted a `navbar` row above it and
       nothing errored because the name still resolved — it just meant somewhere else; and that
       `windowControlsPlacement.test.ts` now derives this row from `.App`'s own
       `grid-template-areas` so the same silent displacement cannot recur. Reference gap G1 /
       UAT test 1.

    Do NOT add any platform branch — macOS gating is plan 34.1-11's task.
    Do NOT touch `NavShell/index.scss` — plan 34.1-11 owns that file.
  </action>
  <verify>
    <automated>npx prettier --check src/frontend/components/UI/WindowControls/index.scss && grep -v '^\s*//' src/frontend/components/UI/WindowControls/index.scss | grep -c 'grid-area: navbar'</automated>
  </verify>
  <acceptance_criteria>
    - `src/frontend/components/UI/WindowControls/index.scss` contains the literal declaration
      `grid-area: navbar;` in the `.windowControls` block (comment-stripped source).
    - The comment-stripped source contains NO `grid-area: content`.
    - The comment-stripped source contains NO `position: sticky` and NO `top: 0px`.
    - `justify-self: right;`, `align-self: start;`, `z-index: 100;` and
      `-webkit-app-region: no-drag;` are all still present.
    - The nested `button { ... }` block is byte-identical to its pre-change form (verify with
      `git diff` — the diff must touch only the four declarations named above plus comments).
    - `npx prettier --check` on the file exits 0.
  </acceptance_criteria>
  <done>`.windowControls` resolves to `.App`'s `navbar` row, the same row as `.NavShell__navbar`.</done>
</task>

<task type="auto">
  <name>Task 2: Census and prune every other grid-area: content consumer</name>
  <files>src/frontend/screens/Settings/index.css, src/frontend/screens/WebView/index.css</files>
  <read_first>
    - src/frontend/screens/Settings/index.css (lines 1-15 — the `.Settings` rule being modified)
    - src/frontend/screens/WebView/index.css (lines 1-10 — the `.WebView` rule being modified)
    - src/frontend/App.tsx (lines 80-133 — proves which elements are direct grid items of `.App`
      and which are `<Outlet/>` descendants of `<main className="content">`)
    - src/frontend/App.css (lines 60-101 — `.App .content` is `display: flex`, so its children
      are flex items and `grid-area` on them is inert)
  </read_first>
  <action>
    Do NOT scope this task to the two files named in the gap report. The recorded project lesson
    is that an audit whose unit is coarser than the defect's unit cannot find the defect —
    ENUMERATE, then set-difference.

    1. Enumerate the complete consumer set with a comment-excluding scan:
       `grep -rn "grid-area:[[:space:]]*content" src/ --include="*.css" --include="*.scss" | grep -v '^\S*:[0-9]*: *//'`
       At authoring time this yields exactly four real declarations (`App.css:63`,
       `Settings/index.css:8`, `WebView/index.css:2`, `WindowControls/index.scss:2` — the last
       already fixed by Task 1) plus one prose mention inside a comment at
       `NavShell/index.scss:132`. If the scan returns anything not in that set, handle it under
       the same rule below and record it in the SUMMARY — do not silently ignore it.

    2. Classify each hit by whether its selector's element is a DIRECT child of
       `<div className="App">` in `App.tsx`. Only a direct child is a grid item; `grid-area` on
       anything else is inert dead code.
         - `App.css:63` `.App .content` -> `<main className="content">` at `App.tsx:103`.
           DIRECT GRID ITEM, correct. LEAVE UNCHANGED.
         - `Settings/index.css:8` `.Settings` -> rendered by `<Outlet/>` (`App.tsx:115`) inside
           `<main className="content">`. NOT a grid item. DEAD.
         - `WebView/index.css:2` `.WebView` -> same. NOT a grid item. DEAD.

    3. DELETE the `grid-area: content;` declaration from `.Settings` in
       `src/frontend/screens/Settings/index.css` and from `.WebView` in
       `src/frontend/screens/WebView/index.css`. Change nothing else in either rule — in
       particular `.Settings` keeps its `grid-template-columns: 1fr 5fr` (also inert on a
       `display: flex` box, but pre-existing and out of scope) and `.WebView` keeps
       `position: relative; height: 100%`.

    4. In `NavShell/index.scss`'s comment at line 132, do NOT edit it here — plan 34.1-11 owns
       that file and will correct the now-false claim that WindowControls "keeps
       `grid-area: content`". Record the handoff in this plan's SUMMARY.

    Record the full four-row census table in the SUMMARY: path, selector, direct-grid-item
    yes/no, disposition.
  </action>
  <verify>
    <automated>npx jest src/frontend --silent 2>&1 | tail -5 && npx prettier --check src/frontend/screens/Settings/index.css src/frontend/screens/WebView/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "grid-area:[[:space:]]*content" src/ --include="*.css" --include="*.scss"` returns
      exactly TWO lines: `src/frontend/App.css:63` and the prose mention inside the comment at
      `src/frontend/components/UI/NavShell/index.scss:132`.
    - `src/frontend/screens/Settings/index.css` no longer contains `grid-area`.
    - `src/frontend/screens/WebView/index.css` no longer contains `grid-area`.
    - `git diff --stat` for these two files shows exactly one deleted line each.
    - `npx prettier --check` on both files exits 0.
    - The SUMMARY contains a four-row census table with an explicit direct-grid-item column.
  </acceptance_criteria>
  <done>Exactly one live `grid-area: content` declaration remains in the tree, and it is the one that belongs there.</done>
</task>

<task type="auto">
  <name>Task 3: Premise-asserting placement gate</name>
  <files>src/frontend/components/UI/NavShell/__tests__/windowControlsPlacement.test.ts</files>
  <read_first>
    - src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts (the `extractBlock`
      helper at lines 86-101, the `readStripped` idiom at 76-78, and the SANITY-case convention
      at lines 159-169 / 186-193 — copy all three patterns)
    - src/frontend/components/UI/NavShell/__tests__/shellTokens.test.ts (the
      `collectStylesheets` recursive walker at lines 21-32 and the `countMatches` census idiom
      at 40-47)
    - src/frontend/App.css (the grid the parser must read)
    - src/frontend/components/UI/WindowControls/index.scss (post-Task-1 state)
    - src/frontend/components/UI/NavShell/index.scss (lines 37-40 — `.NavShell__navbar`'s own
      `grid-area: navbar`)
  </read_first>
  <action>
    Create `windowControlsPlacement.test.ts` as a SOURCE-TEXT gate (there is no jsdom or CSS
    engine in this jest project). Every scan runs through `stripSourceComments` first, so prose
    can neither satisfy nor break a gate.

    The design rule this file exists to embody: `appShellLayout.test.ts:171` was non-vacuous,
    correctly written and green, and still guarded nothing once its layout premise moved. So do
    NOT assert a consequence of co-location — DERIVE the row index from `.App`'s own
    `grid-template-areas` and compare.

    Implement these helpers:
      - `readStripped(path)` and `extractBlock(source, openPattern)` — copied from
        `appShellLayout.test.ts`.
      - `gridAreaMatrix(): string[][]` — extracts `.App`'s block from `App.css`, pulls the
        `grid-template-areas:` value, and splits it into a row-major matrix of area names
        (each quoted string is one row; whitespace-separated tokens are the columns). Throw a
        descriptive Error if the declaration is absent, so a grid rewrite fails loudly rather
        than silently returning an empty matrix.
      - `rowOf(matrix, areaName): number` — the index of the first row containing `areaName`.
        Throw if the name appears in no row.
      - `declaredGridArea(path, selectorPattern): string` — extracts the block and returns the
        value of its `grid-area:` declaration. Throw if absent.

    Write these gates:

    1. `rowOf(matrix, declaredGridArea(WindowControls/index.scss, /^\.windowControls\s*\{/m))`
       EQUALS `rowOf(matrix, declaredGridArea(NavShell/index.scss, /^\.NavShell__navbar\s*\{/m))`.
       Failure message must name gap G1 / UAT test 1 and print both area names and both row
       indices.
    2. SANITY for gate 1: using the SAME live `gridAreaMatrix()`, assert
       `rowOf(matrix, 'content') !== rowOf(matrix, 'navbar')`. This proves the comparator
       genuinely distinguishes rows AND that the pre-fix pairing would have failed gate 1 — it
       is the known-bad input, taken from the real regression, not an invented fixture.
    3. Census: scan every `.css`/`.scss` file under `src/frontend` (recursive, comment-stripped)
       for `grid-area:\s*content\s*;` and assert the set of matching FILE PATHS equals exactly
       `['App.css']` (compare basenames or repo-relative paths — pick one and be consistent).
       Failure message must state the enumerate-and-set-difference rule and list the offenders.
    4. SANITY for gate 3: assert the same scanner finds AT LEAST ONE match overall (i.e.
       `App.css`). A census gate that silently matches nothing passes vacuously; this is the
       negative control.
    5. Assert `.windowControls`'s block does NOT declare `position: sticky` — a sticky grid item
       is constrained to its own grid area, and `navbar` is a single `min-content` row with zero
       travel, so the declaration would be wrong-but-plausible. Mirrors
       `appShellLayout.test.ts:332`'s reasoning for `.NavShell__navbar`.
    6. Assert `.windowControls`'s block still declares `justify-self: right` and `z-index: 100`
       — the two properties that make the intentional overlap with `.NavShell__navbar` land on
       the correct side.

    Open the file with a docstring stating: why this is a source-text gate; that gate 1 asserts
    the PREMISE rather than a consequence; and that the "prove it fails against a known-bad
    input" rule alone would NOT have caught G1, which is why gate 1 recomputes from the live
    grid instead of pinning a literal.
  </action>
  <verify>
    <automated>npx jest src/frontend/components/UI/NavShell/__tests__/windowControlsPlacement.test.ts --silent</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/frontend/components/UI/NavShell/__tests__/windowControlsPlacement.test.ts`
      exits 0 with at least 6 passing tests.
    - RED proof, recorded in the SUMMARY with the captured output: temporarily revert
      `WindowControls/index.scss` to `grid-area: content`, re-run the file, and confirm gate 1
      FAILS with a message naming G1; then restore and confirm green again.
    - RED proof for gate 3, recorded in the SUMMARY: temporarily re-add
      `grid-area: content;` to `src/frontend/screens/WebView/index.css`, re-run, confirm gate 3
      FAILS and names the offending path; then restore and confirm green.
    - The file imports `stripSourceComments` from `backend/testUtils/stripSourceComments` and
      applies it before every scan.
    - `gridAreaMatrix()` throws (not returns empty) when `grid-template-areas` is absent —
      exercised by at least one assertion or documented in the SUMMARY as manually confirmed.
    - `npx tsc --noEmit` exits 0.
    - `npx prettier --check` on the new file exits 0.
  </acceptance_criteria>
  <done>A future rewrite of `.App`'s grid cannot silently displace WindowControls again — the gate recomputes the row from the grid itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan changes only CSS placement and adds a test. No new input crosses any boundary, no new IPC surface, no new capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.1-09-01 | Tampering | `src/frontend/App.css` `grid-template-areas` | mitigate | A future grid rewrite silently displaces window controls again (the exact 34.10 failure). `windowControlsPlacement.test.ts` gate 1 derives the row index from the live matrix, so any rewrite that separates `.windowControls` from `.NavShell__navbar` turns it RED. |
| T-34.1-09-02 | Denial of Service | `src/frontend/screens/Settings/index.css`, `src/frontend/screens/WebView/index.css` | mitigate | Deleting `grid-area: content` from a rule that WAS load-bearing would blank the Settings screen or the store WebView. Proven inert first by reading `App.tsx:103-118` (both render via `<Outlet/>` inside `<main className="content">`, a `display: flex` box). Confirmed live by UAT gate item in plan 34.1-14: Settings renders and a store WebView renders. |
| T-34.1-09-03 | Spoofing | `.windowControls` overlapping `.NavShell__navbar` | accept | Both elements are app-owned chrome rendered by GameLib's own React tree in the `navbar` row. No remote or renderer-supplied content occupies that row — remote content is confined to the `content` row, which after Task 2 is the only place `grid-area: content` resolves. No spoofing surface introduced. |
| T-34.1-09-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. No entry required in RESEARCH.md's Package Legitimacy Audit; no legitimacy checkpoint applies. |
</threat_model>

<verification>
- `npx jest src/frontend --silent` exits 0 (no other frontend suite regressed).
- `npx jest src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts --silent` exits 0
  — note this suite's gate at line 171 was PASSING while guarding nothing; after Task 1 the
  collision it describes is real again, so it must still pass, and now for the right reason.
- `npx tsc --noEmit` exits 0.
- `npx prettier --check` on all four modified/created files exits 0.
- `grep -rn "grid-area:[[:space:]]*content" src/ --include="*.css" --include="*.scss"` returns
  exactly two lines (App.css:63 and the NavShell comment).
</verification>

<success_criteria>
`.windowControls` and `.NavShell__navbar` resolve to the same row of `.App`'s grid, proven by a
gate that recomputes that row from `App.css` itself; exactly one live `grid-area: content`
declaration remains in `src/frontend`; both RED proofs are captured in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-09-SUMMARY.md` when done.
Include: the four-row `grid-area: content` census table; both captured RED-proof outputs; and the
handoff note that `NavShell/index.scss:129-137`'s D-05 comment still claims WindowControls "keeps
`grid-area: content`" and is corrected by plan 34.1-11.
</output>
