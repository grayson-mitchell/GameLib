---
phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome
plan: 12
subsystem: ui
tags: [i18n, i18next, settings, react, gamelib-namespace]

# Dependency graph
requires:
  - phase: 34.8
    provides: the gamelib i18n namespace split-brain design (D-06), `meta/i18nCatalogChurnGuard.ts`, `i18next-parser.config.js`'s `tGamelib` lexer support (D-03)
provides:
  - Plain frameless-window ToggleSwitch with copy that matches actual (live-apply) behaviour
  - settings.framelessWindowDescription key in the fork-owned gamelib i18n catalog
  - A non-vacuous jest source-text gate pinning the copy, the namespace, and the removed dialog
affects: [34.1-14 (live gate confirms the Settings row on-screen)]

# Tech tracking
tech-stack:
  added: []
  patterns: ["gamelib: namespace call-site idiom (const { t: tGamelib } = useTranslation('gamelib'))"]

key-files:
  created:
    - src/frontend/screens/Settings/components/__tests__/framelessWindowCopy.test.ts
  modified:
    - src/frontend/screens/Settings/components/UseFramelessWindow.tsx
    - public/locales/en/gamelib.json

key-decisions:
  - "Reverted unrelated Phase-34.11-carryforward catalog drift (translation.json +46/-6 keys, gamepage.json +7 reshuffle, login.json +1 key) after pnpm i18n, keeping only the gamelib.json addition -- none of the reverted churn referenced frameless-window, confirming it predates this change per the plan's own carve-out"

patterns-established:
  - "Source-text jest gate with stripSourceComments + a REAL known-bad specimen (the retired translation.json key) for non-vacuity proof, following NavShell/__tests__/shellTokens.test.ts"

requirements-completed: [REQ-34.1-03]

# Metrics
duration: 10min
completed: 2026-08-13
---

# Phase 34.1 Plan 12: Frameless-window toggle copy fix Summary

**Collapsed the frameless-window Settings toggle to a plain ToggleSwitch, moved its description into the fork-owned `gamelib` i18n catalog with accurate copy, and added a non-vacuous jest gate proven against the real retired defective string.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-08-13T09:45:00Z (approx.)
- **Completed:** 2026-08-13T09:53:13Z
- **Tasks:** 3/3 completed
- **Files modified:** 3 (1 modified component, 1 modified catalog, 1 created test)

## Accomplishments

- `UseFramelessWindow.tsx` no longer shows an "Experimental feature ahead" confirmation dialog on toggle-on, and no longer claims "(requires restart)" -- the toggle applies live, matching the behaviour UAT test 2 confirmed twice
- The corrected description string (`settings.framelessWindowDescription` = `"Use frameless window"`) lives in `public/locales/en/gamelib.json`, the fork-owned namespace; zero bytes of `translation.json` changed
- `framelessWindowCopy.test.ts` gates the copy, the removed dialog, and the namespace against regression, with a non-vacuity proof driven off the real retired `translation.json` string rather than a hand-built fixture

## Task Commits

Each task was committed atomically:

1. **Task 1: Collapse the toggle and retarget its description to the gamelib namespace** - `dc0dcaa60` (fix)
2. **Task 2: Regenerate the catalog through the project's i18n tooling, with churn proof** - `9cfefe7a2` (feat)
3. **Task 3: Copy-accuracy gate, proved against the real known-bad specimen** - `e66510d2c` (test)

**Plan metadata:** (final commit, created below)

## Files Created/Modified

- `src/frontend/screens/Settings/components/UseFramelessWindow.tsx` - Collapsed to a plain `ToggleSwitch`; removed `showDialogModal`/`ContextProvider`/`useContext`; retargeted to `useTranslation('gamelib')` + `tGamelib('gamelib:settings.framelessWindowDescription', 'Use frameless window')`; kept the `window.isSteamDeckGameMode` early return unchanged; added an explanatory comment recording gap G2 and why the string moved namespaces
- `public/locales/en/gamelib.json` - Regenerated via `pnpm i18n`; added `settings.framelessWindowDescription: "Use frameless window"`
- `src/frontend/screens/Settings/components/__tests__/framelessWindowCopy.test.ts` - New source-text jest gate (5 tests, no jsdom / no render): catalog copy has no restart claim; the same regex proven to fire on the real retired `translation.json` string; component free of `showDialogModal`/`ContextProvider`; component references the `gamelib:` key and no upstream `setting.frameless-window.` key; no tuple-form `t([...])` call

## i18n Catalog Churn Evidence (Task 2)

**Pre-run baseline** (`git status --porcelain public/locales/` before running `pnpm i18n`): empty (clean tree) -- confirmed before proceeding, per the plan's stop-if-dirty instruction.

**Post-`pnpm i18n` status** (before cleanup): `public/locales/en/gamelib.json`, `public/locales/en/gamepage.json`, `public/locales/en/login.json`, `public/locales/en/translation.json` all showed as modified (translation.json: 46 added keys/6 unreferenced-relocated; gamepage.json: 7 added keys + sort-order reshuffle; login.json: 1 added key). None of the three non-gamelib diffs mentioned "frameless" anywhere -- verified with `git diff <file> | grep -i frameless` returning no matches on all three. This is the known pre-existing Phase-34.11-carryforward catalog-drift issue named in this plan's briefing, unrelated to this task's call site.

**Recovery:** `git checkout -- public/locales/en/translation.json public/locales/en/gamepage.json public/locales/en/login.json`, keeping only the `gamelib.json` addition.

**Final status:** `git status --porcelain public/locales/` shows `public/locales/en/gamelib.json` only. `pnpm i18n-churn-guard` exits 0 ("clean -- no upstream public/locales/ catalog changed"). `git diff --exit-code public/locales/en/translation.json` exits 0.

**Value gate 2 matched** (Task 3's non-vacuity proof): `translation.json`'s `setting['frameless-window'].description` still reads exactly `"Use frameless window (requires restart)"` -- the real defective string, retained (not deleted) by `keepRemoved: true`. The `confirmation.title` (`"Experimental feature ahead"`) and `confirmation.message` keys are likewise unchanged. All three `setting.frameless-window.*` keys are now unreferenced by any call site but deliberately retained.

## RED Proofs Captured (Task 3)

**RED proof 1 (gate 1 -- catalog copy):** Temporarily set `gamelib.json`'s `settings.framelessWindowDescription` to `"Use frameless window (requires restart)"` and re-ran the suite. Result: 1 failed / 4 passed -- `expect(description).not.toMatch(/requires restart/i)` failed with `Received string: "Use frameless window (requires restart)"`. Restored via `git checkout -- public/locales/en/gamelib.json`; re-run confirmed 5/5 green.

**RED proof 2 (gate 3 -- removed dialog):** Temporarily re-added a `showDialogModal({})` call inside `handleChange` in `UseFramelessWindow.tsx` and re-ran the suite. Result: 1 failed / 4 passed -- `expect(source).not.toMatch(/showDialogModal/)` failed. Restored the original file (`git diff` confirmed zero diff after restore); re-run confirmed 5/5 green.

**Gate 2 (non-vacuity) passed without modification** on every run -- it reads the real retired string from `translation.json` directly (see matched value above), proving the `/restart/i` predicate fires on the exact input that shipped the original defect rather than an invented fixture.

## Decisions Made

- Reverted the unrelated Phase-34.11-carryforward catalog drift in `translation.json`, `gamepage.json`, and `login.json` after running `pnpm i18n`, keeping only the `gamelib.json` addition this plan's call site produced. Verified via `git diff <file> | grep -i frameless` (no matches in any of the three) that none of that drift traces to this task's call site, consistent with the plan's explicit carve-out ("if you see drift that predates your change, report it as pre-existing... your OWN new key must be clean").

## Deviations from Plan

None - plan executed exactly as written. The one adjustment (reverting unrelated catalog drift before regenerating gamelib.json) was explicitly anticipated and authorized by the plan's own Task 2 instructions and this executor's `critical_context`/`localisation_requirement` briefing, not an unplanned deviation under Rules 1-4.

## Issues Encountered

- `pnpm i18n` regenerated pre-existing unrelated key drift in three upstream catalogs (translation.json, gamepage.json, login.json) alongside the intended gamelib.json addition. Resolved per plan instructions by reverting the three unrelated files and keeping only the gamelib.json change; confirmed via grep that none of the reverted content referenced "frameless" (i.e., it did not originate from this task's call site).
- `pnpm lint-translations:gamelib` logs (non-fatal) `ENOENT` stack traces for every non-`en` locale directory, because `gamelib.json` only exists under `public/locales/en/`. This is a pre-existing structural fact (no other locale directory has ever had a `gamelib.json`), not something this plan introduced or is scoped to fix. The command still exits 0, satisfying the plan's acceptance criterion.
- The repo's `post-checkout` husky hook (`pnpm i && pnpm download-helper-binaries`) fired automatically during the RED-proof `git checkout --` recovery steps and its `download-helper-binaries` half failed on an unrelated pre-existing placeholder-sentinel issue (macOS onedir asset digest, tracked separately per its own error message referencing plan 34.9-09). This did not affect any file this plan touches and is out of scope here.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The toggle's on-screen appearance (no dialog on toggle-on, corrected label text) is not verified here by design -- plan 34.1-14's live gate confirms the Settings row visually.
- No blockers for subsequent plans in this phase.

---
*Phase: 34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome*
*Completed: 2026-08-13*

## Self-Check: PASSED

- FOUND: src/frontend/screens/Settings/components/UseFramelessWindow.tsx
- FOUND: public/locales/en/gamelib.json
- FOUND: src/frontend/screens/Settings/components/__tests__/framelessWindowCopy.test.ts
- FOUND: .planning/phases/34.1-tauri-ipc-re-plumb-slice-4-app-shell-and-window-chrome/34.1-12-SUMMARY.md
- FOUND commit: dc0dcaa60 (Task 1)
- FOUND commit: 9cfefe7a2 (Task 2)
- FOUND commit: e66510d2c (Task 3)
- FOUND commit: 9c6931d1b (SUMMARY docs commit)
