---
phase: 10-humble-auth-adapter-scaffold
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - src/common/types/ipc.ts
  - src/backend/humble/ipc_handler.ts
  - src/preload/api/humble.ts
  - src/preload/api/index.ts
  - src/backend/main.ts
autonomous: true
requirements: [HACCT-01, HACCT-02, HACCT-03]

must_haves:
  truths:
    - "The renderer can invoke humbleStartLogin, humblePollLogin, humbleGetUserInfo, humbleReconnect, humbleDisconnect through typed IPC"
    - "The backend can push humbleAuthState to the renderer as a FrontendMessages listener slot"
    - "All Humble IPC channel names are unprefixed camelCase (no colon), consistent with steamStartQR"
  artifacts:
    - path: "src/common/types/ipc.ts"
      provides: "Humble channel signatures in AsyncIPCFunctions + humbleAuthState in FrontendMessages"
      contains: "humbleStartLogin"
    - path: "src/backend/humble/ipc_handler.ts"
      provides: "registerHumbleIpcHandlers() calling HumbleUser methods"
      exports: ["registerHumbleIpcHandlers"]
    - path: "src/preload/api/humble.ts"
      provides: "preload bridge invokers + humbleAuthState listener slot"
      contains: "makeHandlerInvoker"
  key_links:
    - from: "src/backend/main.ts"
      to: "registerHumbleIpcHandlers"
      via: "single call during main startup"
      pattern: "registerHumbleIpcHandlers"
    - from: "src/preload/api/index.ts"
      to: "src/preload/api/humble.ts"
      via: "Humble module aggregation export"
      pattern: "Humble"
---

<objective>
Wire the `HumbleUser` service to the renderer: typed IPC channel signatures, the backend handler
registration, the preload bridge, and the single `registerHumbleIpcHandlers()` call from `main.ts`.

Purpose: The frontend (Plan 04) and the validation gate (Plan 05) call these channels; they cannot be
built until the typed IPC surface exists. Contracts-first: channel signatures land here so Plan 04
consumes `window.api.humble*` without guessing.

Output: modified `src/common/types/ipc.ts`, new `ipc_handler.ts` + `preload/api/humble.ts`, and
registration wiring in `preload/api/index.ts` and `main.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-humble-auth-adapter-scaffold/10-RESEARCH.md
@.planning/phases/10-humble-auth-adapter-scaffold/10-PATTERNS.md
@.planning/phases/10-humble-auth-adapter-scaffold/10-02-SUMMARY.md

<interfaces>
From src/backend/humble/user.ts (Plan 02):
  HumbleUser.startLogin(), pollLogin(), getUserDetails(), checkHealthAndFlagExpiry(),
  reconnect(), disconnect(), isLoggedIn()
From src/common/types/humble.ts (Plan 01):
  HumbleUserData, HumbleAuthState
Analogs to mirror (read directly):
  src/common/types/ipc.ts lines 214-230 (steam AsyncIPCFunctions block), line 128 (logoutSteam listener),
    lines 388-432 (FrontendMessages interface — connectivity-changed shape)
  src/backend/main.ts lines 860-874 (steam addHandler/addListener block)
  src/preload/api/steam.ts (full file — makeHandlerInvoker/makeListenerCaller)
  src/preload/api/library.ts (frontendListenerSlot usage for push messages)
  src/preload/api/index.ts (module aggregation)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Humble IPC signatures and backend handler registration</name>
  <files>src/common/types/ipc.ts, src/backend/humble/ipc_handler.ts</files>
  <read_first>
    - src/common/types/ipc.ts (AsyncIPCFunctions steam block ~214-230, logoutSteam ~128, FrontendMessages ~388-432)
    - src/backend/main.ts (steam addHandler block ~860-874, and how sendFrontendMessage/addHandler/addListener are imported)
    - src/backend/humble/user.ts (methods to bind)
    - .planning/phases/10-humble-auth-adapter-scaffold/10-PATTERNS.md (ipc_handler + ipc.ts sections; camelCase correction)
  </read_first>
  <action>
    In `src/common/types/ipc.ts`: import `HumbleUserData` from `./humble`; add to `AsyncIPCFunctions`:
    `humbleStartLogin: () => Promise<{ status: 'done' | 'waiting' | 'error'; username?: string }>`,
    `humblePollLogin: () => Promise<{ status: 'done' | 'waiting' | 'error'; username?: string }>`,
    `humbleGetUserInfo: () => Promise<HumbleUserData | undefined>`,
    `humbleReconnect: () => Promise<{ status: 'done' | 'waiting' | 'error'; username?: string }>`,
    `humbleCheckHealth: () => Promise<void>`. Add to the sync/listener section (next to `logoutSteam`):
    `humbleDisconnect: () => void`. Add to `FrontendMessages`:
    `humbleAuthState: (state: { isLoggedIn: boolean; username?: string; expired?: boolean }) => void`
    — MUST NOT include the cookie. Use unprefixed camelCase only (no colon — Pitfall 6).
    Create `src/backend/humble/ipc_handler.ts` exporting `registerHumbleIpcHandlers()` that calls
    `addHandler`/`addListener` (import from the same module main.ts uses) binding each channel to the
    matching `HumbleUser` method: humbleStartLogin→startLogin, humblePollLogin→pollLogin,
    humbleGetUserInfo→getUserDetails, humbleReconnect→reconnect, humbleCheckHealth→checkHealthAndFlagExpiry,
    humbleDisconnect (listener)→disconnect.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit 2>&1 | grep -E "humble|ipc" | head; grep -c "humble:" src/common/types/ipc.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/common/types/ipc.ts` contains `humbleStartLogin`, `humbleDisconnect`, `humbleAuthState`
    - `grep -c "humble:" src/common/types/ipc.ts` returns 0 (no colon-namespaced channels)
    - `src/backend/humble/ipc_handler.ts` exports `registerHumbleIpcHandlers` and references all six channels
    - `npx tsc --noEmit` clean for ipc.ts and ipc_handler.ts
  </acceptance_criteria>
  <done>Typed Humble channels declared; backend handler registration function compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add preload bridge and register handlers in main.ts</name>
  <files>src/preload/api/humble.ts, src/preload/api/index.ts, src/backend/main.ts</files>
  <read_first>
    - src/preload/api/steam.ts (invoker/caller pattern to copy)
    - src/preload/api/library.ts (frontendListenerSlot for push handlers)
    - src/preload/api/index.ts (aggregation import/export pattern)
    - src/backend/main.ts (where steam handlers register; import site for registerHumbleIpcHandlers)
  </read_first>
  <action>
    Create `src/preload/api/humble.ts` mirroring `steam.ts`: `humbleStartLogin`, `humblePollLogin`,
    `humbleGetUserInfo`, `humbleReconnect`, `humbleCheckHealth` via `makeHandlerInvoker`;
    `humbleDisconnect` via `makeListenerCaller`; and a push listener slot
    `handleHumbleAuthState = frontendListenerSlot('humbleAuthState')` (mirrors library.ts
    `handleRefreshLibrary`). In `src/preload/api/index.ts` add `import * as Humble from './humble'`
    and include `...Humble` (or `Humble`) in the exported aggregate exactly as `Steam` is included.
    In `src/backend/main.ts` import `registerHumbleIpcHandlers` from `backend/humble/ipc_handler` and
    call it once in the same startup region the Steam handlers register (near main.ts ~860).
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit 2>&1 | grep -E "humble|preload" | head; grep -c "registerHumbleIpcHandlers" src/backend/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/preload/api/humble.ts` contains `humbleStartLogin`, `humbleDisconnect`, `handleHumbleAuthState`
    - `src/preload/api/index.ts` contains `Humble`
    - `grep -c "registerHumbleIpcHandlers" src/backend/main.ts` returns >= 1 (imported and called)
    - `npx tsc --noEmit` clean; `npm run codecheck` (or `npx tsc --noEmit && npx eslint src/backend/humble src/preload/api/humble.ts`) passes
  </acceptance_criteria>
  <done>Renderer can call window.api.humble* channels and receive humbleAuthState pushes; main registers handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC invoke) | Renderer-initiated auth actions cross into the main process |
| main → renderer (IPC push) | humbleAuthState pushed to the renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Information Disclosure | IPC payload shape | mitigate | humbleAuthState typed as `{ isLoggedIn; username?; expired? }` only — the cookie is structurally excluded from the channel signature |
| T-10-10 | Tampering | channel-name drift | mitigate | camelCase-only channel names; grep gate asserts zero colon-namespaced Humble channels (Pitfall 6) |
| T-10-11 | Elevation | unbounded renderer-triggered login windows | accept | Single-user desktop app; login is user-initiated from Manage Accounts, no untrusted remote caller |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across common/backend/preload.
- `grep -c "humble:" src/common/types/ipc.ts` == 0.
- `registerHumbleIpcHandlers` imported and called once in main.ts.
</verification>

<success_criteria>
- Typed Humble IPC surface exists end-to-end (types → handler → preload → main registration).
- humbleAuthState push channel available to the renderer, cookie-free by type.
</success_criteria>

<output>
Create `.planning/phases/10-humble-auth-adapter-scaffold/10-03-SUMMARY.md` when done.
</output>
