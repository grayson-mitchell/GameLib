---
phase: 10-humble-auth-adapter-scaffold
plan: 04
type: execute
wave: 4
depends_on: [10-03]
files_modified:
  - src/frontend/helpers/electronStores.ts
  - src/frontend/state/GlobalState.tsx
  - src/frontend/state/ContextProvider.tsx
  - src/frontend/screens/Login/index.tsx
  - src/frontend/screens/Login/components/HumbleConnect/index.tsx
  - src/frontend/App.tsx
  - src/frontend/assets/humble-logo.svg
  - public/locales/en/login.json
autonomous: true
requirements: [HACCT-01, HACCT-02, HACCT-03]

must_haves:
  truths:
    - "A Humble Bundle tile appears on Manage Accounts alongside Epic/GOG/Amazon/Steam"
    - "Clicking Connect opens the Humble login window (via humbleStartLogin), and on success the tile shows the account identity"
    - "When the session expires the tile flips to a Session expired — Reconnect state and a one-time toast fires, without breaking the rest of Manage Accounts"
    - "Disconnect shows a confirmation dialog, then removes the Humble account"
    - "If encryption is degraded (Linux no keyring), the user sees a reduced-encryption warning"
  artifacts:
    - path: "src/frontend/state/GlobalState.tsx"
      provides: "humble context slice { username?; expired? }, humbleLogin/humbleDisconnect, humbleAuthState listener"
      contains: "humble"
    - path: "src/frontend/screens/Login/index.tsx"
      provides: "Humble Runner tile with connected/expired/disconnected states"
      contains: "humble"
    - path: "src/frontend/screens/Login/components/HumbleConnect/index.tsx"
      provides: "minimal route that calls humbleStartLogin on mount then navigates back (keeps Runner unchanged per D-01)"
    - path: "public/locales/en/login.json"
      provides: "login.humble, login.humble_reconnect, humble disconnect + degraded-encryption strings"
  key_links:
    - from: "src/frontend/screens/Login/index.tsx"
      to: "window.api.humbleStartLogin"
      via: "/humble-connect route mounted from the Humble Runner tile loginUrl"
      pattern: "humble"
    - from: "src/frontend/state/GlobalState.tsx"
      to: "window.api.handleHumbleAuthState"
      via: "listener registration updating expired state"
      pattern: "handleHumbleAuthState"
    - from: "src/frontend/state/GlobalState.tsx"
      to: "window.api.humbleDisconnect"
      via: "confirmation dialog then disconnect"
      pattern: "humbleDisconnect"
---

<objective>
Surface the Humble account in the frontend: a Manage Accounts Runner tile with connected / expired /
disconnected states, the `humble` context slice, the `humbleAuthState` expiry listener + one-time
reconnect toast, the disconnect confirmation dialog, and the degraded-encryption warning.

Purpose: Delivers the user-facing half of HACCT-01/02/03. Reuses the existing `Runner` component
unchanged (D-01) by routing Connect through a minimal `/humble-connect` route that calls the login IPC.

Output: modified GlobalState/ContextProvider/Login/App/electronStores, a new HumbleConnect route
component, a Humble logo asset, and i18n strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-humble-auth-adapter-scaffold/10-CONTEXT.md
@.planning/phases/10-humble-auth-adapter-scaffold/10-PATTERNS.md
@.planning/phases/10-humble-auth-adapter-scaffold/10-03-SUMMARY.md

<interfaces>
Preload (Plan 03): window.api.humbleStartLogin(), humblePollLogin(), humbleGetUserInfo(),
  humbleReconnect(), humbleCheckHealth(), humbleDisconnect(), handleHumbleAuthState(cb)
Analogs to mirror (read directly):
  src/frontend/helpers/electronStores.ts lines 156-158 (steamConfigStore frontend reader) + export block ~203
  src/frontend/state/GlobalState.tsx: steam slice decl (~79-82), init (~221-223), steamLogin/steamLogout
    (~684-710), context assembly (~1216-1220), handleShowDialogModal (~509-513), push-listener
    registrations (window.api.handleRefreshLibrary ~961, handleMetadataChanged ~969)
  src/frontend/state/ContextProvider.tsx line ~34 (steam default context slice)
  src/frontend/screens/Login/index.tsx: steam <Runner> usage (~170-179), steamLoginPath (~24)
  src/frontend/screens/Login/components/Runner/index.tsx (props: loginUrl, isLoggedIn, user, logoutAction, buttonText, disabled)
  src/frontend/App.tsx: route table (loginweb/steam ~160, loginweb/:runner ~164)
Humble slice shape (auth-only, no library this phase): humble: { username?: string | null; expired?: boolean }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add humble context slice, store reader, expiry listener, and disconnect dialog</name>
  <files>src/frontend/helpers/electronStores.ts, src/frontend/state/GlobalState.tsx, src/frontend/state/ContextProvider.tsx</files>
  <read_first>
    - src/frontend/helpers/electronStores.ts (steamConfigStore reader + export block)
    - src/frontend/state/GlobalState.tsx (steam slice, steamLogin/steamLogout, handleShowDialogModal, push-listener registrations, componentDidMount)
    - src/frontend/state/ContextProvider.tsx (steam default slice)
    - src/frontend/screens/Login/components/Runner/index.tsx (logoutAction contract)
  </read_first>
  <action>
    In `src/frontend/helpers/electronStores.ts` add
    `const humbleConfigStore = new TypeCheckedStoreFrontend('humbleConfigStore', { cwd: 'humble_store' })`
    and add it to the final export block (mirror steamConfigStore).
    In `src/frontend/state/GlobalState.tsx`: add a `humble: { username?: string | null; expired?: boolean }`
    slice to state (mirror the steam slice but WITHOUT a `library` field); initialize `username` from
    `humbleConfigStore.get_nodefault('userData')?.username` and `expired: false`. Add `humbleLogin`
    (setState username on `{status:'done'}`, no refreshLibrary call) and `humbleDisconnect` (call
    `this.handleShowDialogModal` with a confirmation dialog — buttons Confirm/Cancel; on confirm call
    `window.api.humbleDisconnect()` and setState `humble: { username: null, expired: false }`). Register a
    push listener in componentDidMount: `window.api.handleHumbleAuthState((e, state) => this.setState({ humble: { username: state.username ?? this.state.humble.username, expired: !!state.expired } }))`,
    and when `state.expired` becomes true, fire a one-time reconnect toast (D-09) via the existing
    dialog/notify mechanism — de-duplicate so it fires once per expiry transition (guard on prior expired
    value). Also read `humbleConfigStore.get_nodefault('encryptionDegraded')` and expose it in the slice
    (e.g. `encryptionDegraded?: boolean`) so the tile can render the reduced-encryption warning
    (success criterion 5). Add `humble` to the context assembly object with `login`/`logout` bound.
    In `src/frontend/state/ContextProvider.tsx` add the matching `humble` default slice.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit 2>&1 | grep -E "GlobalState|ContextProvider|electronStores" | head; grep -c "handleHumbleAuthState" src/frontend/state/GlobalState.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/frontend/helpers/electronStores.ts` contains `humbleConfigStore` and `cwd: 'humble_store'`
    - `src/frontend/state/GlobalState.tsx` contains `humble`, `handleHumbleAuthState`, `humbleDisconnect`, and a confirmation dialog before disconnect
    - `src/frontend/state/ContextProvider.tsx` contains a `humble` default slice
    - `npx tsc --noEmit` clean for these three files
  </acceptance_criteria>
  <done>Humble frontend state, store reader, expiry listener, one-time toast, and disconnect-with-confirmation exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Humble Runner tile, /humble-connect route, logo, and i18n</name>
  <files>src/frontend/screens/Login/index.tsx, src/frontend/screens/Login/components/HumbleConnect/index.tsx, src/frontend/App.tsx, src/frontend/assets/humble-logo.svg, public/locales/en/login.json</files>
  <read_first>
    - src/frontend/screens/Login/index.tsx (steam Runner usage, steamLoginPath, context destructure)
    - src/frontend/screens/Login/components/Runner/index.tsx (loginUrl → navigate behavior)
    - src/frontend/App.tsx (route table; how loginweb/steam is declared)
    - public/locales/en/login.json (existing login.* keys + format)
  </read_first>
  <action>
    Create `src/frontend/screens/Login/components/HumbleConnect/index.tsx`: a minimal route component that
    on mount calls `window.api.humbleStartLogin()` (or `humbleReconnect()` when arriving in expired state),
    awaits the result, calls the context `humble.login(result)`, then `navigate('/login')` back (D-01:
    keeps Runner unmodified). Add `export const humbleLoginPath = '/humble-connect'`.
    Add a `humble-logo.svg` asset under `src/frontend/assets/` (a simple Humble Bundle mark; SVG using
    currentColor is acceptable). In `src/frontend/App.tsx` register a `path: 'humble-connect'` route
    rendering HumbleConnect (place it as a specific route BEFORE the `loginweb/:runner` catch-all, mirroring
    the Steam-route ordering precedent to avoid WebView capture).
    In `src/frontend/screens/Login/index.tsx`: destructure `humble` from context; add a `<Runner>` for
    Humble mirroring the Steam tile — `class="humble"`, icon `HumbleLogo`, `loginUrl={humbleLoginPath}`,
    `isLoggedIn={Boolean(humble?.username)}`, `user={humble?.username ?? undefined}`,
    `logoutAction={humble?.logout ?? (() => Promise.resolve())}`, and conditional
    `buttonText={humble?.expired ? t('login.humble_reconnect', 'Session expired — Reconnect') : t('login.humble', 'Humble Bundle Login')}`.
    When `humble?.encryptionDegraded`, render the reduced-encryption warning text near the tile.
    Add the new keys to `public/locales/en/login.json`: `humble`, `humble_reconnect`,
    `humble_disconnect_title`, `humble_disconnect_message`, `humble_expired_toast`,
    `humble_encryption_degraded`. Use existing semantic CSS tokens for any new styling (no hard-coded hex/px).
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx tsc --noEmit 2>&1 | grep -E "Login|App|HumbleConnect" | head; grep -c "humble" src/frontend/screens/Login/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/frontend/screens/Login/index.tsx` renders a `<Runner class="humble"` tile with `humbleLoginPath` and conditional expired buttonText
    - `src/frontend/screens/Login/components/HumbleConnect/index.tsx` calls `humbleStartLogin` on mount and navigates back
    - `src/frontend/App.tsx` declares the `humble-connect` route before the `loginweb/:runner` catch-all
    - `public/locales/en/login.json` contains keys `humble`, `humble_reconnect`, `humble_encryption_degraded`
    - `npm run codecheck` (tsc + eslint) passes
  </acceptance_criteria>
  <done>Humble tile appears on Manage Accounts with connect/expired/disconnect states and localized strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer state ← IPC push | humbleAuthState updates the tile; must never receive the cookie |
| user → disconnect | Destructive action gated by a confirmation dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Information Disclosure | frontend state / logs | mitigate | Slice holds only username/expired/encryptionDegraded — the cookie never reaches the renderer (enforced by the Plan 03 channel type) |
| T-10-13 | Repudiation / accidental disconnect | disconnect action | mitigate | Confirmation dialog (D-03) before humbleDisconnect wipes the account |
| T-10-14 | Information Disclosure | degraded encryption on Linux | mitigate | `encryptionDegraded` flag renders a user-visible reduced-encryption warning (success criterion 5) |
</threat_model>

<verification>
- `npm run codecheck` passes (tsc + eslint).
- Manual: Humble tile renders on Manage Accounts; expired state and toast are wired (full UX verified in Plan 05 UAT).
- New i18n keys present in `public/locales/en/login.json`.
</verification>

<success_criteria>
- Humble appears as a standard Runner tile with connected/expired/disconnected states.
- Expiry pushes a one-time reconnect toast without breaking Manage Accounts; disconnect confirms then removes.
- Degraded-encryption warning is surfaced to the user.
</success_criteria>

<output>
Create `.planning/phases/10-humble-auth-adapter-scaffold/10-04-SUMMARY.md` when done.
</output>
