---
phase: 10-humble-auth-adapter-scaffold
plan: 06
type: execute
wave: 7
depends_on: [10-05]
files_modified:
  - src/common/types/humble.ts
  - src/backend/humble/validation.ts
  - src/backend/humble/adapter.ts
  - .planning/phases/10-humble-auth-adapter-scaffold/10-VALIDATION.md
autonomous: false
requirements: [HACCT-01, HACCT-02, HACCT-03]

must_haves:
  truths:
    - "The dev-only trigger runs the REAL adapter with the REAL stored cookie from Electron main and returns a redacted report (D-12)"
    - "10-VALIDATION.md PASS requires gamekeys 200 + at least one order detail 200 + zod parse incl. steam_app_id; the identity endpoint result is recorded but ADVISORY and cannot fail the gate (D-13 revised)"
    - "Axios is the primary transport; if confirmed blocked, ses.fetch() on the persist:humble partition behind the SAME adapter signatures is validated instead (D-14 revised)"
    - "The phase does not complete until ONE transport passes; 10-VALIDATION.md records PASS, redacted (no cookie values, no key values) (D-15)"
  artifacts:
    - path: "src/backend/humble/validation.ts"
      provides: "runHumbleValidation() with identity demoted to advisory; overall PASS computed from gamekeys + order detail + steam_app_id only"
      contains: "runHumbleValidation"
    - path: ".planning/phases/10-humble-auth-adapter-scaffold/10-VALIDATION.md"
      provides: "recorded live-API pass/fail report (redacted), canonical ref for Phase 11"
      contains: "PASS"
  key_links:
    - from: "src/backend/humble/validation.ts"
      to: "adapter.getGamekeys / getOrderDetail (gate) + getAccountIdentity (advisory)"
      via: "real stored cookie decrypted via HumbleUser.getCredentials()"
      pattern: "getGamekeys|getOrderDetail"
    - from: "src/backend/humble/adapter.ts humbleRequest"
      to: "session.fromPartition('persist:humble').fetch (D-14 fallback)"
      via: "single transport seam swapped only if axios is confirmed blocked"
      pattern: "humbleRequest"
---

<objective>
Finish the D-12 live validation gate under the revised criteria: identity is advisory, the gate is
gamekeys + order-detail + steam_app_id (D-13 revised), and the D-14 fallback is `ses.fetch()` on the
persist:humble partition (not a hidden-BrowserWindow proxy). Run the real-account UAT of the full
HACCT-01/02/03 UX through the new embedded WebView surface, and record the redacted PASS in
`10-VALIDATION.md` — the hard blocker before Phase 11.

Purpose: Phase 11 cannot be planned on an unproven transport. This gate proves the C5 adapter
actually reaches the Humble API from Electron's main process with the real stored cookie, and doubles
as the manual UAT for the whole phase.

Output: an updated `validation.ts` + a possible `ses.fetch()` transport fallback, and a signed-off
`10-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-humble-auth-adapter-scaffold/10-CONTEXT.md
@.planning/phases/10-humble-auth-adapter-scaffold/10-05-SUMMARY.md

<interfaces>
Existing (already wired — do not re-create):
- runHumbleValidation(): Promise<HumbleValidationReport>  (src/backend/humble/validation.ts)
- HumbleValidationReport { transport; timestamp; overall:'pass'|'fail'; endpoints[]; gamekeyCount; steamAppIdPresent }
- HumbleValidationEndpointResult { path; status; schemaValid }
- adapter humbleRequest(path, cookie) — the single transport seam (D-14 swap point)
- adapter.getGamekeys / getOrderDetail / getAccountIdentity
- IPC humbleRunValidation is registered in backend/main.ts ONLY behind `if (!app.isPackaged)` (T-10-16)
- preload export window.api.humbleRunValidation

D-13 revised PASS criteria (identity is NOT criterion #4 anymore):
  1. GET /api/v1/user/order returns 200 (gamekeys)
  2. at least one GET /api/v1/order/{gamekey} detail returns 200
  3. responses zod-parse, incl. tpkd_dict.all_tpks[n].steam_app_id presence
  The account-identifier endpoint result is recorded but ADVISORY — cannot fail overall.
D-14 revised fallback: if axios is confirmed blocked, swap humbleRequest to ses.fetch() on the
  persist:humble partition (same function signatures), record transport accordingly.
Analog for the report artifact: .planning/phases/08-new-steam-surfaces/08-VALIDATION.md (frontmatter + table shape).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demote identity to advisory in the gate, and prepare the ses.fetch transport fallback seam</name>
  <files>src/common/types/humble.ts, src/backend/humble/validation.ts, src/backend/humble/adapter.ts</files>
  <read_first>
    - src/backend/humble/validation.ts (current runHumbleValidation — overall currently counts the identity endpoint)
    - src/backend/humble/adapter.ts (humbleRequest single seam; getGamekeys/getOrderDetail/getAccountIdentity)
    - src/common/types/humble.ts (HumbleValidationReport / HumbleValidationEndpointResult / AdapterResult)
    - .planning/phases/10-humble-auth-adapter-scaffold/10-CONTEXT.md (D-02, D-13 revised, D-14 revised, D-15, D-16)
    - .planning/phases/08-new-steam-surfaces/08-VALIDATION.md (report frontmatter + table shape)
  </read_first>
  <action>
    In validation.ts, change the overall verdict so it does NOT depend on the identity endpoint (D-13 revised).
    Compute `overall = 'pass'` iff: getGamekeys returned 200/ok AND at least one getOrderDetail returned 200/ok
    AND steamAppIdPresent is true; otherwise 'fail'. Still call getAccountIdentity and still push its result into
    the endpoints[] list, but mark it advisory: add an `advisory?: boolean` field to
    HumbleValidationEndpointResult in src/common/types/humble.ts and set advisory:true on the identity endpoint
    entry so 10-VALIDATION.md records the identity result without it being able to fail the gate (consistent with
    D-02's generic-"Connected" fallback). Keep the report fully redacted (gamekeyCount + steamAppIdPresent
    booleans/counts only, never cookie/key values). Update the `transport` union in HumbleValidationReport from
    `'axios' | 'browserwindow-proxy'` to `'axios' | 'session-fetch'` to match the D-14-revised fallback.

    In adapter.ts, keep axios as the primary transport in humbleRequest (D-14: axios stays primary). Prepare the
    seam for the fallback WITHOUT changing the default path: leave humbleRequest issuing the axios GET, but factor
    it so a session-fetch implementation can be dropped in behind the identical signature
    `humbleRequest(path, cookie): Promise<unknown>` if Task 2's live run shows axios blocked. Do NOT implement or
    activate the ses.fetch() path in this task — it is built only if the gate shows axios is blocked (Task 2
    conditional). Keep the Plan 01 adapter tests green after any refactor.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx jest src/backend/humble/__tests__/adapter.test.ts --no-coverage 2>&1 | tail -8; grep -c "advisory" src/common/types/humble.ts; grep -c "session-fetch" src/common/types/humble.ts; grep -c "steamAppIdPresent" src/backend/humble/validation.ts; npx tsc --noEmit 2>&1 | grep -E "humble|validation" | head</automated>
  </verify>
  <acceptance_criteria>
    - runHumbleValidation's overall verdict is computed from gamekeys + order-detail + steamAppIdPresent only; the identity endpoint entry carries advisory:true and cannot change overall
    - HumbleValidationEndpointResult has an `advisory?: boolean` field; HumbleValidationReport.transport is `'axios' | 'session-fetch'`
    - The report contains no field carrying the cookie or raw gamekey values (still redacted)
    - `npx jest src/backend/humble/__tests__/adapter.test.ts --no-coverage` exits 0 after any humbleRequest refactor
    - `npx tsc --noEmit` clean for the modified files
  </acceptance_criteria>
  <done>The gate treats identity as advisory and the transport seam is ready for a same-signature ses.fetch() swap; adapter tests stay green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live validation gate + full HACCT UX UAT (embedded WebView) + record 10-VALIDATION.md</name>
  <read_first>
    - .planning/phases/10-humble-auth-adapter-scaffold/10-CONTEXT.md (D-12, D-13 revised, D-14 revised, D-15, D-16)
    - src/backend/humble/validation.ts (runHumbleValidation output shape)
    - .planning/phases/08-new-steam-surfaces/08-VALIDATION.md (frontmatter + table shape to follow)
  </read_first>
  <what-built>
    The dev-only in-app trigger (window.api.humbleRunValidation, behind `!app.isPackaged`) exercises the real
    Humble adapter with the real stored encrypted cookie from Electron main, plus the full Manage Accounts
    Humble connect / persist / expire-reconnect / disconnect flow — now via the embedded /loginweb/humble WebView
    surface built in Plan 10-05.
  </what-built>
  <action>
    Pause for the user to run the live validation gate and the full HACCT UX on a real Humble account (steps
    below), then record the redacted result in 10-VALIDATION.md with a PASS verdict against the D-13-revised
    criteria. If the live run shows axios blocked (403/transport failure despite a valid cookie that logs in via
    the WebView), swap adapter humbleRequest to a `session.fromPartition('persist:humble').fetch()` implementation
    behind the identical signature (D-14 revised), set the report transport to 'session-fetch', and re-run until
    one transport passes. Do not resume until 10-VALIDATION.md records PASS.
  </action>
  <how-to-verify>
    Run the app in dev mode with a REAL Humble Bundle account:
    1. Manage Accounts → click the Humble "Humble Bundle Login" tile. Confirm the login opens INSIDE the embedded
       WebView at /loginweb/humble (NOT a popup window). Complete email/password + reCAPTCHA + Humble Guard code
       in the WebView. Confirm it returns to Manage Accounts on success and the tile shows your account name — or
       a generic "Connected" label if identity is unavailable (D-02, HACCT-01).
    2. Start a second login and navigate away / back to /login before completing it — confirm silent cancel (tile
       stays disconnected, no error toast) (D-06).
    3. Fully quit and relaunch — confirm no re-login is required (encrypted session persisted, persist:humble
       kept) (HACCT-02). On Linux without a keyring, confirm the reduced-encryption warning appears (success
       criterion 5).
    4. Trigger the dev-only validation (window.api.humbleRunValidation() in devtools, or the dev menu item).
       Confirm the report shows: /api/v1/user/order 200, at least one /api/v1/order/{gamekey} 200, zod-parse PASS,
       steam_app_id present in at least one order → overall PASS. The identity endpoint row is recorded as
       ADVISORY and does not affect the verdict. If axios is blocked (403), the executor builds the ses.fetch()
       fallback (D-14) and you re-run until PASS.
    5. Simulate/await expiry (or clear the cookie from the persist:humble partition) — confirm the tile flips to
       "Session expired — Reconnect", a one-time toast fires, and the rest of Manage Accounts is intact
       (HACCT-02). Click Reconnect → confirm it reopens the /loginweb/humble WebView with the partition kept (D-11).
    6. Click Disconnect → confirm the confirmation dialog, then the account is removed and session data wiped
       (persist:humble cleared) (HACCT-03).
    Finally, record the redacted report in
    `.planning/phases/10-humble-auth-adapter-scaffold/10-VALIDATION.md` following the 08-VALIDATION.md frontmatter
    + table shape, with `status: approved`, an explicit PASS verdict, the three D-13-revised criteria results, the
    advisory identity result, and the validated transport (axios or session-fetch). NO cookie values, NO key
    values in the file.
  </how-to-verify>
  <verify>
    <human-check>User confirms all six UAT steps and that 10-VALIDATION.md records a PASS verdict for the three D-13-revised criteria (identity advisory), redacted.</human-check>
  </verify>
  <acceptance_criteria>
    - 10-VALIDATION.md contains a PASS verdict and rows for the three D-13-revised criteria plus the advisory identity result
    - 10-VALIDATION.md frontmatter has `status: approved` and does NOT contain any `_simpleauth_sess` value or raw gamekey
    - The validated transport (axios or session-fetch) is identified in the report
    - All six UAT steps are confirmed by the user
  </acceptance_criteria>
  <done>Live gate passes on one transport, identity advisory; full HACCT UX verified through the embedded WebView; 10-VALIDATION.md recorded with PASS.</done>
  <resume-signal>Type "approved" once 10-VALIDATION.md is recorded with a PASS verdict, or describe the failures (which D-13 criterion failed, whether the ses.fetch fallback is needed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation.ts → Humble API | Authenticated live requests using the real stored cookie from Electron main |
| validation report → disk (10-VALIDATION.md) | Report must be redacted before it is written/committed |
| dev trigger surface | Must not ship enabled in production builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Information Disclosure | 10-VALIDATION.md contents | mitigate | Report redacts cookie + all key/gamekey values (D-15); acceptance criterion greps the file for `_simpleauth_sess` and raw keys |
| T-10-16 | Elevation / abuse | dev-only trigger in production | mitigate | humbleRunValidation stays wired only behind `!app.isPackaged` in backend/main.ts, never in packaged builds (D-12) |
| T-10-17 | Tampering | humbleRequest transport swap regressing 401/403 split | mitigate | Plan 01 adapter tests re-run after any humbleRequest refactor and must stay green |
| T-10-18 | Repudiation | unproven transport advancing to Phase 11 | mitigate | Phase does not complete until one transport passes; PASS verdict in 10-VALIDATION.md is the blocking gate (D-14) |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (ses.fetch and session are Electron built-ins; RESEARCH.md audit: N/A); no install tasks |
</threat_model>

<verification>
- `npx jest src/backend/humble/__tests__/*.test.ts --no-coverage` exits 0.
- `pnpm test` full suite green before sign-off.
- `10-VALIDATION.md` exists with a PASS verdict and is redacted (no cookie/key values).
</verification>

<success_criteria>
- The live gate proves one working transport from Electron main with the real stored cookie (D-13 revised, identity advisory).
- Full HACCT-01/02/03 UX verified end-to-end on a real account through the embedded WebView surface.
- 10-VALIDATION.md recorded as the canonical, redacted Phase 11 reference.
</success_criteria>

<output>
Create `.planning/phases/10-humble-auth-adapter-scaffold/10-06-SUMMARY.md` when done.
</output>
