---
phase: 10-humble-auth-adapter-scaffold
plan: 05
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - src/backend/humble/validation.ts
  - src/backend/humble/adapter.ts
  - src/common/types/humble.ts
  - src/common/types/ipc.ts
  - src/backend/humble/ipc_handler.ts
  - src/preload/api/humble.ts
  - src/backend/main.ts
  - .planning/phases/10-humble-auth-adapter-scaffold/10-VALIDATION.md
autonomous: false
requirements: [HACCT-01, HACCT-02, HACCT-03]

must_haves:
  truths:
    - "A dev-only in-app trigger exercises the REAL adapter with the REAL stored encrypted cookie from the Electron main process (not a Node script) (D-12 dev-only in-app debug trigger exercising the real adapter in Electron main)"
    - "The trigger emits a redacted structured report: endpoints hit, status codes, schema-parse results, steam_app_id presence — no cookie values, no key values (D-15 redacted structured gate evidence recorded in 10-VALIDATION.md)"
    - "The phase does not complete until ONE transport (axios or the D-14 BrowserWindow proxy fallback) is empirically validated against the live Humble API"
    - "10-VALIDATION.md records a PASS verdict against all four D-13 criteria"
  artifacts:
    - path: "src/backend/humble/validation.ts"
      provides: "runHumbleValidation(): live gate exercising getAccountIdentity/getGamekeys/getOrderDetail, redacted report assembly"
      contains: "runHumbleValidation"
    - path: ".planning/phases/10-humble-auth-adapter-scaffold/10-VALIDATION.md"
      provides: "recorded live-API pass/fail report (redacted), canonical ref for Phase 11"
      contains: "PASS"
  key_links:
    - from: "src/backend/humble/validation.ts"
      to: "adapter.getGamekeys / getOrderDetail / getAccountIdentity"
      via: "real stored cookie decrypted via HumbleUser.getCredentials"
      pattern: "getGamekeys|getOrderDetail|getAccountIdentity"
    - from: "src/backend/main.ts"
      to: "humbleRunValidation"
      via: "dev-only trigger surface (menu item or IPC)"
      pattern: "humbleRunValidation"
---

<objective>
Build the D-12 dev-only validation trigger and run the live-API gate (D-13): prove the C5 adapter
transport actually reaches the Humble API from Electron's main process with the stored encrypted cookie,
record the redacted result in `10-VALIDATION.md`, and — if axios is blocked — build the D-14 BrowserWindow
webRequest-proxy fallback in-phase until ONE transport passes.

Purpose: Phase 11 cannot be planned on an unproven transport. This gate is a hard phase-completion
blocker, not a nice-to-have. It also serves as the manual UAT for the full HACCT-01/02/03 UX.

Output: `validation.ts` + IPC trigger, a possible adapter transport-fallback, and a signed-off
`10-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-humble-auth-adapter-scaffold/10-CONTEXT.md
@.planning/phases/10-humble-auth-adapter-scaffold/10-RESEARCH.md
@.planning/phases/10-humble-auth-adapter-scaffold/10-VALIDATION.md
@.planning/phases/10-humble-auth-adapter-scaffold/10-04-SUMMARY.md

<interfaces>
From src/backend/humble/adapter.ts (Plan 01): getGamekeys, getOrderDetail, getAccountIdentity → AdapterResult
From src/backend/humble/user.ts (Plan 02): HumbleUser.getCredentials() → decrypted cookie
D-13 PASS criteria (all four required):
  1. GET /api/v1/user/order returns 200 (gamekeys list)
  2. at least one GET /api/v1/order/{gamekey} detail returns 200
  3. responses parse against adapter zod schemas, incl. tpkd_dict.all_tpks[n].steam_app_id presence
  4. the account-identifier endpoint (D-02) works
D-14 fallback: if axios+cookie+header is blocked, implement a BrowserWindow webRequest proxy transport
  behind the SAME adapter function signatures and validate that instead.
Analog for report artifact: .planning/phases/08-new-steam-surfaces/08-VALIDATION.md (frontmatter + table shape)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the dev-only validation trigger and redacted report generator</name>
  <files>src/backend/humble/validation.ts, src/backend/humble/adapter.ts, src/common/types/humble.ts, src/common/types/ipc.ts, src/backend/humble/ipc_handler.ts, src/preload/api/humble.ts, src/backend/main.ts</files>
  <read_first>
    - src/backend/humble/adapter.ts (transport call sites — factor a single request helper for D-14 swappability)
    - src/backend/humble/user.ts (getCredentials for the real stored cookie)
    - src/backend/humble/ipc_handler.ts (registration pattern from Plan 03)
    - src/backend/main.ts (dev-menu / addHandler registration region)
    - .planning/phases/08-new-steam-surfaces/08-VALIDATION.md (report shape to follow)
    - .planning/phases/10-humble-auth-adapter-scaffold/10-RESEARCH.md (D-12/D-13/D-14, Open Question 2 identity endpoint)
  </read_first>
  <action>
    Refactor `src/backend/humble/adapter.ts` so all three functions route their HTTP through a single
    internal transport helper (e.g. `humbleRequest(path, cookie)`) — this is the seam the D-14 fallback
    swaps without touching call sites. Keep behavior and the Plan 01 adapter tests green.
    Create `src/backend/humble/validation.ts` exporting `runHumbleValidation()`: decrypt the stored cookie
    via `HumbleUser.getCredentials()`; call `getAccountIdentity`, `getGamekeys`, then `getOrderDetail` on
    the first returned gamekey; assemble a `HumbleValidationReport` with, per endpoint: path, result status
    (200 / session_expired / access_denied / schema_error), zod-parse pass/fail, and a boolean
    `steam_app_id_present` computed from the order detail's `tpkd_dict.all_tpks`. The report MUST be
    redacted — never include the cookie value or any raw gamekey/key values (represent gamekeys as a count
    or a masked token). Return the report; do NOT write files from this function (the human checkpoint
    writes 10-VALIDATION.md).
    Add `HumbleValidationReport` to `src/common/types/humble.ts` (redacted shape only). Add IPC channel
    `humbleRunValidation: () => Promise<HumbleValidationReport>` to `AsyncIPCFunctions` in
    `src/common/types/ipc.ts`; register it in `ipc_handler.ts`; export the invoker in
    `src/preload/api/humble.ts`. Add a dev-only trigger surface in `src/backend/main.ts` — a menu item or a
    handler wired only when the repo's existing dev flag is set (e.g. `!app.isPackaged`), per D-12 discretion.
    D-14 conditional: if the Task 2 live run shows axios blocked (403/transport failure despite a valid
    cookie), implement a `humbleRequest` variant that issues the request from a real Humble origin via a
    BrowserWindow session/webRequest, behind the same signature, and re-run the gate until one transport
    passes. Do NOT complete the phase on an unproven transport.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && npx jest src/backend/humble/__tests__/adapter.test.ts --no-coverage 2>&1 | tail -8; npx tsc --noEmit 2>&1 | grep -E "humble|validation" | head; grep -c "runHumbleValidation" src/backend/humble/validation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/backend/humble/validation.ts` exports `runHumbleValidation` and references getAccountIdentity, getGamekeys, getOrderDetail
    - The report shape contains `steam_app_id_present` and does NOT contain a field carrying the cookie or raw gamekey values
    - `src/common/types/ipc.ts` contains `humbleRunValidation`; `src/preload/api/humble.ts` exports it; `src/backend/main.ts` wires it behind a dev-only guard
    - `npx jest src/backend/humble/__tests__/adapter.test.ts --no-coverage` still exits 0 after the transport refactor
    - `npx tsc --noEmit` clean for the modified files
  </acceptance_criteria>
  <done>Dev-only trigger runs the real adapter with the real stored cookie and returns a redacted report; transport is swappable for the D-14 fallback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live validation gate + full HACCT UX UAT + record 10-VALIDATION.md</name>
  <what-built>
    A dev-only in-app trigger that exercises the real Humble adapter with the real stored encrypted cookie
    from Electron main, plus the full Manage Accounts Humble connect / persist / expire-reconnect /
    disconnect flow from Plans 01-04.
  </what-built>
  <action>
    Pause for the user to run the live validation gate and the full HACCT UX on a real Humble account
    (steps below), then record the redacted result in 10-VALIDATION.md with a PASS verdict. If the live run
    shows axios blocked, return to Task 1 to build the D-14 BrowserWindow-proxy transport and re-run until
    one transport passes. Do not resume until 10-VALIDATION.md records PASS.
  </action>
  <how-to-verify>
    Run the app in dev mode with a REAL Humble Bundle account:
    1. Open Manage Accounts → click the Humble "Humble Bundle Login" tile. Complete email/password +
       reCAPTCHA + Humble Guard emailed code INSIDE the login window. Confirm the window auto-closes on
       success and the tile shows your account identity (HACCT-01).
    2. Close the login window BEFORE logging in on a second attempt — confirm it is a silent cancel (tile
       stays disconnected, no error toast) (D-06).
    3. Fully quit and relaunch the app — confirm no re-login is required (encrypted session persisted)
       (HACCT-02). Note the OS: on Linux without a keyring, confirm the reduced-encryption warning appears
       (success criterion 5).
    4. Trigger the dev-only validation (menu item or `window.api.humbleRunValidation()` in devtools).
       Confirm the returned report shows: /api/v1/user/order 200, at least one /api/v1/order/{gamekey}
       200, zod-parse PASS, steam_app_id present in at least one order, and the account-identifier endpoint
       working (all four D-13 criteria). If axios is blocked (403), the executor builds the D-14 fallback
       and you re-run until PASS.
    5. Simulate/await expiry (or clear the cookie via the humble-login partition) — confirm the tile flips
       to "Session expired — Reconnect", a one-time toast fires, and the rest of Manage Accounts is intact
       (HACCT-02). Click Reconnect → confirm the login window reopens (partition kept, D-11).
    6. Click Disconnect → confirm the confirmation dialog appears, then the account is removed and session
       data is wiped (HACCT-03).
    Finally, record the redacted report in `.planning/phases/10-humble-auth-adapter-scaffold/10-VALIDATION.md`
    following the 08-VALIDATION.md frontmatter + table shape, with an explicit PASS verdict and the four
    D-13 criteria results. NO cookie values, NO key values in the file.
  </how-to-verify>
  <verify>
    <human-check>User confirms all six UAT steps and that 10-VALIDATION.md records a PASS verdict for all four D-13 criteria, redacted.</human-check>
  </verify>
  <acceptance_criteria>
    - `10-VALIDATION.md` contains a PASS verdict and rows for all four D-13 criteria
    - `10-VALIDATION.md` frontmatter has `status: approved` and does NOT contain any `_simpleauth_sess` value or raw gamekey
    - The validated transport (axios or D-14 proxy) is identified in the report
    - All six UAT steps above are confirmed by the user
  </acceptance_criteria>
  <done>Live gate passes on one transport; full HACCT UX verified; 10-VALIDATION.md recorded with PASS.</done>
  <resume-signal>Type "approved" once 10-VALIDATION.md is recorded with a PASS verdict, or describe the failures (which D-13 criterion failed, whether the D-14 fallback is needed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation.ts → Humble API | Authenticated live requests using the real stored cookie |
| validation report → disk (10-VALIDATION.md) | Report artifact must be redacted before it is written/committed |
| dev trigger surface | Must not ship enabled in production builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Information Disclosure | 10-VALIDATION.md contents | mitigate | Report redacts cookie + all key/gamekey values (D-15); acceptance criterion greps the file for `_simpleauth_sess` and raw keys |
| T-10-16 | Elevation / abuse | dev-only trigger in production | mitigate | Trigger wired only behind a dev guard (`!app.isPackaged`/dev flag), never in packaged builds (D-12) |
| T-10-17 | Tampering | adapter transport refactor regressing 401/403 split | mitigate | Plan 01 adapter tests re-run after the humbleRequest refactor and must stay green |
| T-10-18 | Repudiation | unproven transport advancing to Phase 11 | mitigate | Phase does not complete until one transport passes; PASS verdict in 10-VALIDATION.md is the blocking gate (D-14) |
</threat_model>

<verification>
- `npx jest src/backend/humble/__tests__/*.test.ts --no-coverage` exits 0 (adapter + user).
- `pnpm test` full suite green before sign-off.
- `10-VALIDATION.md` exists with a PASS verdict and is redacted (no cookie/key values).
</verification>

<success_criteria>
- The live gate proves one working transport from Electron main with the real stored cookie (D-13).
- Full HACCT-01/02/03 UX verified end-to-end on a real account.
- 10-VALIDATION.md recorded as the canonical, redacted Phase 11 reference.
</success_criteria>

<output>
Create `.planning/phases/10-humble-auth-adapter-scaffold/10-05-SUMMARY.md` when done.
</output>
