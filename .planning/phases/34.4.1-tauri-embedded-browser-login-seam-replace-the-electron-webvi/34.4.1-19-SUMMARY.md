---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 19
subsystem: docs
tags: [gap-cycle, declaration, requirements, ported-channels, reconciliation]

# Dependency graph
requires:
  - phase: 34.4.1-10
    provides: "the seam-parity sweep + seamBranchParity.test.ts regression guard this plan's gap-cycle section describes"
  - phase: 34.4.1-13
    provides: "F-1 closed at the code level (keyring-backed Humble secret store + migration) -- this plan resets humbleStartLogin/humbleReconnect's proof-level riders to reflect it changed the store's shape"
  - phase: 34.4.1-16
    provides: "F-6 closed at the code level (origin-scoped storage-clear capability wired into both disconnect() and the cross-phase Legendary logout() edit) -- described in this plan's new gap-cycle section"
  - phase: 34.4.1-17
    provides: "the F-5 jar census and F-8 transport-label fix -- both described in this plan's new gap-cycle section and reflected in the humbleReconnect/humbleRevealKey riders"
  - phase: 34.4.1-18
    provides: "F-2/F-3/F-4 + S-09/D-GAP-03 closure, and the two sweep-document staleness carry-forwards this plan re-forwards rather than fixing"
provides:
  - "34.4.1-PORTED-CHANNELS.md's gap-cycle section: what plans 10-18 shipped, stated as code-level closures never upgraded to live proof"
  - "humbleStartLogin/humbleReconnect reset to honest forward references pending plan 20's re-observation against the new keyring-backed store"
  - "humbleRevealKey's PROVEN claim explicitly NOT withdrawn, with a rider distinguishing the F-8 wording fix from a behavior change"
  - "REQ-34.4.1-GAP-01..06 minted in REQUIREMENTS.md, each naming the existing requirement it extends and the finding it closes"
  - "REQ-34.4.1-02/-05/-06/-12 reopened to UNCHECKED with dated reopening riders naming the gap plan and the gate item each is pending on"
  - "An explicit GATED-updates checklist (below) for plan 20 Task 3 to consume directly"
affects: ["34.4.1-20 (blocking live-gate re-run -- consumes this plan's GATED checklist)", "34.5 (reads 34.4.1-PORTED-CHANNELS.md as fact, per this document's own stated purpose)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Reset-to-forward-reference: when the code underneath a 'unit + LIVE (item N)' row changes after the live gate that produced it, the row is not silently left claiming a stale PASS -- it is explicitly reset to a forward reference with a rider stating what changed and what plan 20 must re-observe, never deleted or reworded to imply continuity it does not have."
    - "Distinguish a withdrawn claim from an unchanged one under a code change: humbleRevealKey's PROVEN claim was NOT withdrawn (the reveal mechanism is unchanged) even though its own rider's quoted log line went stale (F-8's wording fix) -- the two are recorded as different facts, not conflated into a blanket 're-run everything.'"

key-files:
  created: []
  modified:
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/deferred-items.md

key-decisions:
  - "GAP-01/GAP-04/GAP-06 are checked (unit/cargo-proven, no live-gate dependency: the keyring allowlist design, the sweep+regression-guard instrument, and F-1b's evidenced NOT-REACHABLE declaration). GAP-02/GAP-03/GAP-05 stay unchecked because their own claim -- Humble's session actually lands in the Keychain, disconnect actually stops auto-sign-in, the census arithmetic actually comes out clean on a real jar -- is exactly what plan 20's live-gate re-run must observe. This split mirrors which existing requirement each GAP extends (checked GAP IDs extend requirements that were never reopened; unchecked GAP IDs extend REQ-34.4.1-02/-06, which were reopened)."
  - "seam-parity-sweep.py's S-07/S-10/S-11 categorization staleness (routed here by plans 17/18) was NOT fixed. This plan's own files_modified frontmatter names exactly three files, none of them the sweep script or its generated document, and fixing categories_for_labels() plus regenerating the sweep would be scope expansion this plan's own objective explicitly forbids ('change nothing whose truth the gate has not yet established'). Logged to deferred-items.md and re-forwarded to an as-yet-unassigned future plan instead -- neither this plan nor plan 20 owns that file."
  - "REQ-34.4.1-05 was reopened despite item 4 (reveal) having PASSED live and that PASS not being withdrawn. The interfaces this plan was given explicitly name -05 among the four reopened IDs, and the rider makes the reasoning legible: a wording-only diagnostic change (F-8) after the gate ran means the box should not be checked on the strength of a PASS measured before that change, even though the underlying claim is unaffected. Recorded as a deliberate, conservative choice rather than silently deviating from the given interface list."

requirements-completed: []

# Metrics
duration: ~55min
completed: 2026-07-30
---

# Phase 34.4.1 Plan 19: Declare the gap cycle + mint REQ-34.4.1-GAP-01..06 Summary

**`34.4.1-PORTED-CHANNELS.md` now carries a dated gap-cycle section describing what plans 10-18 shipped, with `humbleStartLogin`/`humbleReconnect` reset to honest forward references (their code changed since the original live gate proved them) and `humbleRevealKey`'s PROVEN claim explicitly preserved but annotated for a wording-only fix; `REQUIREMENTS.md` gained six new `REQ-34.4.1-GAP-*` entries and reopened four existing IDs to UNCHECKED with dated riders — nothing here claims plan 20 has not yet observed.**

## Performance

- **Duration:** ~55 min
- **Completed:** 2026-07-30
- **Tasks:** 2/2 (plan tasks) + 1 logged, unfixed carry-forward (deferred-items.md)
- **Files modified:** 3 (all `.planning/` documents; zero source files touched)

## Accomplishments

- Added a new "Gap cycle 2026-07-30 (plans 10-18)" section to `34.4.1-PORTED-CHANNELS.md`, verified row-by-row against plans 10-18's own SUMMARYs rather than planning-time intent: the D-GAP-01 multi-slot keyring allowlist (F-1) with both rejected alternatives recorded on the record (a dedicated channel family; a free-form account argument that would have reopened T-28-03); F-1b's evidenced DECLARED-dormant disposition (two independent re-measurement instruments, both agreeing with the original sweep); the F-6a/F-6 origin-scoped storage-clear capability, including the deliberate cross-phase edit into Phase 34.5's own `storeManagers/legendary/user.ts`; the F-5 jar census and F-8 transport-label fixes; and F-2/F-3/F-4/S-09's closure.
- Added `T-34.4.1-73` (the two permanently-uncloseable `clearAuthCache`/`clearHostResolverCache` categories) to `## Accepted residuals`, alongside the existing `T-34.4.1-47` entry.
- Restated beside the struck "34.4 D-05's `humbleDisconnect` partial is CLOSED" entry that it stays gated on plan 20's live-gate item 3 actually passing — the gap cycle's code-level closure does not un-strike it by itself.
- Reset `humbleStartLogin` and `humbleReconnect`'s riders in the "7 channels" table: their proof-level cells remain in the permitted `unit + LIVE (item N)` form (unchanged, per the gate script's controlled vocabulary), but the prose now states plainly that the prior live PASS was measured against the OLD plaintext-storing code, which plan 13 replaced — item 1/2 must be re-observed by plan 20 against the new keyring-backed store, and the mechanism/plaintext-fix-in-code are recorded as two separate, non-equivalent claims.
- Updated `humbleRevealKey`'s rider to record that its PROVEN claim is **NOT** withdrawn, while noting plan 17 replaced the hardcoded `"electron-net transport"` log literal the original gate's evidence quoted — a future reader comparing logs across the two gate runs will not mistake that wording change for a behavior change.
- Minted six `REQ-34.4.1-GAP-01..06` entries in `REQUIREMENTS.md`, each naming the existing requirement it extends and the finding it closes, attributed to `D-GAP-01`/`D-GAP-02` and the live gate rather than to a pre-existing CONTEXT.md decision. Three are checked (GAP-01 keyring allowlist, GAP-04 sweep+regression-guard, GAP-06 SteamGridDB DECLARED-dormant) because their own evidence is unit/cargo-proven independent of any live observation. Three stay unchecked (GAP-02 session-encrypted-at-rest, GAP-03 storage-clear-capability's live efficacy, GAP-05 census-arithmetic-on-a-real-jar) because their own claim is exactly what plan 20 must observe.
- Reopened `REQ-34.4.1-02`/`-05`/`-06`/`-12` to UNCHECKED with a dated rider on each naming the live-gate finding that reopened it, the gap-cycle plan that re-closed it at the code level, and the explicit gate item plan 20 must re-run before the box may be re-checked.
- Verified `IPC-PORT-INVENTORY.md`'s diff stays empty throughout (never touched — that edit is plan 20's, gated on item 3), and `ROADMAP.md`'s Phase 34.4.1 plan list already ran 01-20 with the six GAP requirement IDs listed (verified intact from planning time, not re-edited).
- Logged, but deliberately did NOT fix, the `seam-parity-sweep.py` S-07/S-10/S-11 categorization staleness that plans 17/18 routed here — see Deviations below.

## Task Commits

Each task was committed atomically:

1. **Task 1: Declare the gap cycle in 34.4.1-PORTED-CHANNELS.md, with proof levels no stronger than the evidence** - `2a93d25fe` (docs)
2. **Task 2: Mint the REQ-34.4.1-GAP IDs and bring ROADMAP.md's plan list current** - `a8fa6bde2` (docs)

A third commit (`00d81abc7`, docs) re-forwards a carry-forward to `deferred-items.md` — see Deviations.

## Files Created/Modified

- `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md` — new "Gap cycle 2026-07-30" section, `T-34.4.1-73` accepted-residual entry, a restated gating note beside the struck D-05 entry, and reset riders on the `humbleStartLogin`/`humbleReconnect`/`humbleRevealKey` table rows.
- `.planning/REQUIREMENTS.md` — six new `REQ-34.4.1-GAP-01..06` entries; `REQ-34.4.1-02`/`-05`/`-06`/`-12` reopened to UNCHECKED with dated riders.
- `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/deferred-items.md` — new entry re-forwarding the `seam-parity-sweep.py` S-07/S-10/S-11 staleness carry-forward (not fixed by this plan).

## Decisions Made

See `key-decisions` in frontmatter. Most consequential: the checked/unchecked split across the six new GAP IDs mirrors exactly which existing requirement each extends and whether that requirement was itself reopened — a mechanical, legible rule rather than a case-by-case judgment call that could be second-guessed later.

## Deviations from Plan

### Logged, not fixed (out of declared scope)

**1. [Scope-boundary] `seam-parity-sweep.py`'s S-07/S-10/S-11 categorization staleness, routed here by plans 17/18, was not fixed**
- **Found during:** Re-reading `deferred-items.md` before starting Task 1 (per this plan's own `<read_first>` instruction).
- **Issue:** Plans 17/18 explicitly wrote "plan 19 owns reconciling S-07/S-10/S-11's disposition text" into `deferred-items.md`, but `34.4.1-19-PLAN.md`'s own frontmatter `files_modified` list names exactly `34.4.1-PORTED-CHANNELS.md`, `REQUIREMENTS.md` and `ROADMAP.md` — the sweep script and its generated document are not among them, and this plan's own objective states it must "change nothing whose truth the gate has not yet established" and must not expand its own scope.
- **Disposition:** Recorded in prose inside `34.4.1-PORTED-CHANNELS.md`'s new gap-cycle section (so a reader of the declared record is not misled by the sweep document's stale table) and re-logged to `deferred-items.md` with an explicit note that no plan currently owns the fix — neither this plan nor plan 20 (whose own `files_modified` list is `34.4.1-LIVE-GATE-RERUN.md`, `34.4.1-LIVE-GATE.md`, `34.4.1-PORTED-CHANNELS.md` and `IPC-PORT-INVENTORY.md`) touches `seam-parity-sweep.py`.
- **Files modified:** `.planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md` (prose only), `.planning/phases/34.4.1-.../deferred-items.md`.
- **Committed in:** `2a93d25fe` (prose note) and `00d81abc7` (deferred-items.md re-forward).

---

**Total deviations:** 0 auto-fixed; 1 logged-and-re-forwarded (out of declared file scope, per the scope-boundary rule).
**Impact on plan:** None on this plan's own deliverables — the carry-forward remains visible and explicitly unassigned rather than silently dropped, for whichever future plan picks it up.

## Issues Encountered

None beyond the logged carry-forward above.

## Verification Results

- `python3 ported-channels-gate.py` — exit 0: "OK: all 7 channels declared exactly once with correct send/invoke kind, every proof level permitted, the 6 humble* channels match IPC-PORT-INVENTORY.md's Phase 34.4.1 section exactly ... IPC-PORT-INVENTORY.md is verified unmodified, SEAM.md's checklist steps are closed, and oauthCaptureLogin never claims a live proof."
- `python3 ported-channels-gate.py --self-test` — exit 0, 8/8 self-test cases each proved their assertion rejects its corresponding bad input.
- `python3 seam-parity-sweep.py --self-test` — exit 0, 11/11 checks proved capable of rejecting their corresponding bad input (this plan touched no line the sweep script's own self-test depends on).
- `git diff --stat -- .planning/IPC-PORT-INVENTORY.md` — empty throughout all three commits.
- `grep -c "~~\*\*34.4 D-05" 34.4.1-PORTED-CHANNELS.md` — 1 (still struck).
- `grep -c "REQ-34.4.1-GAP-" .planning/REQUIREMENTS.md` — 11 (≥6 required; extra hits are cross-references from other entries' own prose, e.g. "see REQ-34.4.1-GAP-02").
- `grep -n "REQ-34.4.1-12"` — shows an UNCHECKED box, with the gate's actual FAIL 2/4 result recorded in its own inline note.
- `REQ-34.4.1-02`, `-05` and `-06` — all UNCHECKED with a stated reopening reason.
- `git diff --name-only` across this plan's three commits — only `.planning/REQUIREMENTS.md`, `.planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md`, `.planning/phases/34.4.1-.../deferred-items.md` — no source file, no `IPC-PORT-INVENTORY.md`.
- `npm run test:ci` — 176 suites / 3387 tests, all passing, exit 0. No source changed by this plan, so this is a no-regression confirmation only (matches plan 18's own baseline exactly: 176/3387).

### Acceptance criteria (from plan)

- Task 1: all five acceptance criteria satisfied — gate script exits 0 both modes, inventory diff empty, struck entry still struck, no proof-level cell claims an un-re-observed live item (`humbleStartLogin`/`humbleReconnect` explicitly carry the pending forward-reference form), `oauthCaptureLogin`'s proof level is still exactly `unit`, and the document names F-1b's measured disposition plus the Legendary cross-phase obligation.
- Task 2: all acceptance criteria satisfied — `REQ-34.4.1-GAP-` count ≥6, each GAP entry names its extended requirement and closed finding, `REQ-34.4.1-12` shows UNCHECKED, `REQ-34.4.1-02`/`-05`/`-06` are unchecked with a stated reopening reason, `ROADMAP.md`'s plan list already includes entries through `34.4.1-20`, inventory diff empty.

## Known Stubs

None. This plan touches only planning/requirements documents — no UI/data-rendering surface, no code path.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern, or schema change at a trust boundary was introduced — this plan edits only planning-record prose and requirement checkboxes.

## User Setup Required

None — no external service configuration required.

## GATED updates — explicit checklist for plan 20 Task 3

Everything below remains gated exactly as this plan found it. Plan 20 Task 3 should consume this
list directly rather than re-deriving it from `34.4.1-LIVE-GATE.md`:

| Record update | Gated on | Currently |
|---|---|---|
| `IPC-PORT-INVENTORY.md` L73-75's `humbleDisconnect` declared-partial note | item 3 PASS (plan 20) | Untouched; verified-intact per `ported-channels-gate.py` check 5 |
| `34.4.1-PORTED-CHANNELS.md`'s struck "34.4 D-05 CLOSED" entry | item 3 PASS (plan 20) | STRUCK and REOPENED; a note beside it restates the gate |
| `humbleStartLogin`'s rider (currently a forward reference) | item 1 re-run PASS: no `sessionCookie` field, `encryptionDegraded: false`, plan 13's install log line present | Forward reference, prior PASS recorded as history against the old plaintext code |
| `humbleReconnect`'s rider (currently a forward reference) | item 2 re-run PASS against the NEW keyring-backed store, paired reading taken inside the item (F-7) | Forward reference, prior PASS recorded as history against the old plaintext store |
| `humbleRevealKey`'s rider | item 4 regression re-run confirming the derived transport label and no other behavior change | PROVEN claim already stands, NOT withdrawn; only the wording-fix note is pending confirmation |
| `REQ-34.4.1-02` checkbox | item 1 re-run PASS | UNCHECKED, dated reopening rider present |
| `REQ-34.4.1-05` checkbox | item 4 regression re-run PASS | UNCHECKED, dated reopening rider present |
| `REQ-34.4.1-06` checkbox | item 3 re-run PASS (both halves) | UNCHECKED, dated reopening rider present |
| `REQ-34.4.1-12` checkbox | 4/4 overall PASS | UNCHECKED, dated rider records the FAIL 2/4 result |
| `REQ-34.4.1-GAP-02` checkbox | item 1 re-run PASS | UNCHECKED |
| `REQ-34.4.1-GAP-03` checkbox | item 3 re-run PASS | UNCHECKED |
| `REQ-34.4.1-GAP-05` checkbox | item 3(b) re-run PASS specifically (census arithmetic on a real jar) | UNCHECKED |
| `seam-parity-sweep.py`'s S-07/S-10/S-11 categorization | **not gated on plan 20 at all** — unassigned, needs its own future plan | Logged in `deferred-items.md`, not owned by any current plan |

## Next Phase Readiness

- Plan 20 can proceed directly from this document's GATED-updates checklist rather than re-deriving it from `34.4.1-LIVE-GATE.md`'s own "Record updates this result PERMITS and FORBIDS" table.
- Every claim this plan touched is either (a) unchanged and still fully evidenced (humbleRevealKey's PROVEN status, GAP-01/-04/-06's unit/cargo evidence) or (b) explicitly reset to a pending forward reference naming exactly what plan 20 must observe. Nothing was upgraded on the strength of this gap cycle's own green 3387/3387 suite.
- No blockers. The one open carry-forward (`seam-parity-sweep.py`'s stale categorization) is visible and explicitly unassigned, not silently dropped.

---
*Phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi*
*Plan: 19*
*Completed: 2026-07-30*

## Self-Check: PASSED

- FOUND: .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
- FOUND: .planning/REQUIREMENTS.md
- FOUND: .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/deferred-items.md
- FOUND commit: 2a93d25fe (Task 1)
- FOUND commit: a8fa6bde2 (Task 2)
- FOUND commit: 00d81abc7 (deferred-items.md re-forward)
