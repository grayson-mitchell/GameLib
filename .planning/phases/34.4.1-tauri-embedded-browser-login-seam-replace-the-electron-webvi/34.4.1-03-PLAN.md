---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 03
type: execute
wave: 3
depends_on: ["34.4.1-01", "34.4.1-02"]
files_modified:
  - src/backend/humble/user.ts
  - src/backend/humble/__tests__/user.test.ts
autonomous: true
requirements: [REQ-34.4.1-02, REQ-34.4.1-03]

must_haves:
  truths:
    - "D-01/D-02: `watchForLogin()`'s poll/throttle/revalidate/deadline state machine is preserved STRUCTURALLY — only its `session.fromPartition()` + `setUserAgent` calls are replaced by seam calls, so every existing behavior (anonymous-cookie candidacy, throttle bypass on forced revalidation, silent-cancel on stop, WR-03 deadline) survives unchanged."
    - "A Humble login can be started from the Tauri build: the watch opens a real login window on HUMBLE_LOGIN_URL with the standard Chrome UA, polls its cookie jar, and closes the window when the watch settles."
    - "D-03 (bad case named and closed off): the cookie read is spike-proven to work on macOS, but if it ever returns UNDECIDABLE the watch fails LOUDLY with `{ status: 'error' }` and a log line — it never spins silently for the full 10-minute deadline."
    - "REQ-34.4.1-03: the navigation relay is fed by `on_page_load` main-frame events drained from Rust, never by `on_navigation`, so a third-party ad iframe cannot re-arm the login deadline indefinitely."
    - "Electron's path is byte-for-byte the old behavior: when `getLoginWindowSeam()` returns null the function takes the original `session.fromPartition` branch."
  artifacts:
    - path: "src/backend/humble/user.ts"
      provides: "Seam-backed watchForLogin + csrf capture with an Electron fallback branch"
      contains: "getLoginWindowSeam"
    - path: "src/backend/humble/__tests__/user.test.ts"
      provides: "Seam-path coverage: UNDECIDABLE abort, nav-event forced revalidation, window close on settle, Electron-path regression"
  key_links:
    - from: "src/backend/humble/user.ts watchForLogin"
      to: "LoginWindowSeam.open/cookies/takeEvents/close"
      via: "getLoginWindowSeam() injected by the sidecar at startup"
      pattern: "getLoginWindowSeam\\(\\)"
    - from: "classifyCookieRead"
      to: "settle({ status: 'error' })"
      via: "UNDECIDABLE verdict aborts the watch instead of polling"
      pattern: "UNDECIDABLE"
---

<objective>
Rewire `HumbleUser.watchForLogin()` (and the csrf capture beside it) to drive the new login-window
seam when one is installed, keeping the existing state machine and the Electron path intact.

Purpose: this is the plan that makes gate item 1 — a Humble login completed FROM SCRATCH under
Tauri — physically possible, falsifying 34.4 D-09's "there is no Tauri path to create a Humble
session".

Output: a dual-path `watchForLogin` + csrf capture, with new tests for the seam path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-CONTEXT.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PATTERNS.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-02-SUMMARY.md
@.claude/skills/spike-findings-gamelib/references/tauri-login-webview-cookies.md

<interfaces>
From src/backend/humble/loginWindowSeam.ts (plan 34.4.1-02):
  getLoginWindowSeam(): LoginWindowSeam | null
  LoginWindowSeam.open(url, { visible, userAgent }): Promise<string>   // returns the window label
  LoginWindowSeam.cookies(label, host, names): Promise<{ total: number; matched: [{name,domain,value}] }>
  LoginWindowSeam.takeEvents(label): Promise<Array<{ event: 'started'|'finished'; url: string }>>
  LoginWindowSeam.close(label): Promise<boolean>
  classifyCookieRead({ total, everProvedLive }): 'SUPPORTED_NONEMPTY' | 'SUPPORTED_BUT_EMPTY'
                                               | 'UNSUPPORTED_OR_ERROR' | 'UNDECIDABLE'

Existing, unchanged in this plan:
  src/backend/humble/constants.ts: HUMBLE_BASE_URL = 'https://www.humblebundle.com'
                                   HUMBLE_LOGIN_URL = 'https://www.humblebundle.com/login'
                                   HUMBLE_LOGIN_PARTITION = 'persist:humble'
  src/backend/humble/user.ts:      LoginResult = { status: 'done'|'waiting'|'error'; username?: string }
                                   COOKIE_POLL_INTERVAL_MS, VALIDATION_THROTTLE_MS, LOGIN_WATCH_TIMEOUT_MS
                                   HumbleUser.finishLogin(), HumbleUser.activeWatch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive watchForLogin through the login-window seam</name>
  <files>src/backend/humble/user.ts</files>
  <read_first>
    - src/backend/humble/user.ts L40-70 (poll/throttle/deadline constants and LoginResult) and L204-374 IN FULL — including the removed-`getFullCookieHeader` comment at L204-218, which is the round-5/6 history explaining why a hand-built cookie header was abandoned; do not reintroduce that pattern for any new logging
    - src/backend/humble/loginWindowSeam.ts (as written by plan 34.4.1-02)
    - .planning/phases/34.4.1-.../34.4.1-RESEARCH.md Pattern 3 (the liveness-proof poll) and Pitfalls 1/2
    - .claude/skills/spike-findings-gamelib/references/tauri-login-webview-cookies.md § "The killer" and § "Persistence and isolation" (the poller must be anchored to a webview that OUTLIVES the poll — closing the window destroys the handle even though the cookies survive)
    - src/backend/humble/__tests__/user.test.ts (the existing login-watch coverage this must not break)
  </read_first>
  <action>
    In `watchForLogin()`, replace the two lines at ~L256/L260 (`session.fromPartition(...)` and
    `ses.setUserAgent(standardBrowserUserAgent())`) with a branch on
    `const seam = getLoginWindowSeam()`:

    - seam === null (Electron): keep the EXISTING `session.fromPartition` + `setUserAgent` code and
      the existing `ses.cookies.get({ url: HUMBLE_BASE_URL, name: '_simpleauth_sess' })` read
      verbatim. Electron behavior must not change.
    - seam !== null (Tauri sidecar): open the window with
      `seam.open(HUMBLE_LOGIN_URL, { visible: true, userAgent: standardBrowserUserAgent() })` and
      keep the returned label for the life of the watch. The window must stay open for the whole
      poll — closing it destroys the cookie handle (spike 015) — and must be closed exactly once,
      inside `settle()`, with a floated `.catch()` that logs and never throws (WR-06 float
      discipline, and a throw inside `settle` would strand the promise).

    `checkCookie(forceValidation)` gains a seam path that preserves the existing control flow
    exactly (the `settled || validationInFlight` guards, the `lastRejectedValue`/`lastRejectedAt`
    throttle, `finishLogin`'s three outcomes) and differs ONLY in how the candidate value is
    obtained:
      1. `const read = await seam.cookies(label, 'www.humblebundle.com', ['_simpleauth_sess'])`,
         with the throw path recorded as `total: null`.
      2. `const verdict = classifyCookieRead({ total, everProvedLive })` where `everProvedLive` is a
         closure-scoped boolean set true the first time `total > 0`.
      3. 'UNDECIDABLE' -> `logWarning` naming the verdict and the label, then
         `settle({ status: 'error' })` and return. NEVER continue polling.
         'UNSUPPORTED_OR_ERROR' -> `logWarning` and settle `{ status: 'error' }` as well.
         'SUPPORTED_BUT_EMPTY' -> return (the genuine "not logged in yet" case).
         'SUPPORTED_NONEMPTY' -> take `matched[0].value` as the candidate and fall into the existing
         throttle + `finishLogin` logic unchanged.
      Use the host `'www.humblebundle.com'` deliberately: the Rust arm does a proper domain-SUFFIX
      match, so the apex-domain `_simpleauth_sess` cookie IS returned — this is the exact case
      `cookies_for_url()` silently drops, and a comment must say so at the call site.
    NEVER log a cookie value; log at most name/verdict/length.

    Navigation relay (REQ-34.4.1-03): on every poll tick, before the cookie read, drain
    `await seam.takeEvents(label)`; if any entry has `event === 'finished'`, call the same
    `armDeadline()` + forced-revalidation path `notifyLoginNavigated()` uses today. These events come
    from Rust's `on_page_load` hook, which is main-frame only — do NOT add any `on_navigation`
    consumer, because iframes would re-arm the deadline indefinitely and defeat
    `LOGIN_WATCH_TIMEOUT_MS`.

    `stopLogin()`/`notifyLoginNavigated()` keep their current bodies and signatures; the window
    close happens through `settle()`, which `stop()` already calls.
  </action>
  <acceptance_criteria>
    - `grep -c "cookies_for_url" src/backend/humble/user.ts` returns 0.
    - The Electron branch is intact: `grep -c "session.fromPartition(HUMBLE_LOGIN_PARTITION)" src/backend/humble/user.ts` returns >= 1.
    - `grep -c "UNDECIDABLE" src/backend/humble/user.ts` returns >= 1 and it is followed by a settle, not a `return` alone.
    - `npx jest src/backend/humble/__tests__/user.test.ts` exits 0 with the PRE-EXISTING cases unchanged (no test deleted or weakened — `git diff` on that file shows additions only in this task).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx jest src/backend/humble/__tests__/user.test.ts</automated>
  </verify>
  <done>The watch opens/polls/closes a real login window under Tauri, aborts loudly on UNDECIDABLE, re-arms only on main-frame page loads, and leaves the Electron path untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Route the csrf capture through the seam and cover the seam path in tests</name>
  <files>src/backend/humble/user.ts, src/backend/humble/__tests__/user.test.ts</files>
  <read_first>
    - src/backend/humble/user.ts L176-202 (`getLiveCsrfToken`) and L430-455 (finishLogin's opportunistic `csrf_cookie` capture)
    - src/backend/humble/__tests__/user.test.ts (existing structure, mocking style, and how it fakes the Electron session)
    - .planning/phases/34.4.1-.../34.4.1-VALIDATION.md § "Per-Task Verification Map"
  </read_first>
  <action>
    (a) In `finishLogin`'s csrf capture block, take the same seam branch: when a seam is installed
    and the caller passes the live window label through, read `csrf_cookie` via
    `seam.cookies(label, 'www.humblebundle.com', ['csrf_cookie'])` instead of
    `session.fromPartition`. Absence stays NON-FATAL exactly as today (no error, nothing stored,
    login still completes). Thread the label in as an explicit parameter — do not reach for a module
    global.

    (b) In `getLiveCsrfToken()`, add a guard so the sidecar does not hit the `{}` stub: when a seam
    is installed there is no live login window at reveal time, so return the stored snapshot
    (`HumbleUser.getCsrfToken()`) directly with a one-line debug log, instead of throwing a TypeError
    into the existing catch. Behavior under Electron is unchanged.

    (c) Extend `src/backend/humble/__tests__/user.test.ts` with a `describe('login window seam path')`
    block using a fake `LoginWindowSeam` installed via `setLoginWindowSeam()` in `beforeEach` and
    cleared in `afterEach`. Cases, at minimum:
      - opens the window once with `HUMBLE_LOGIN_URL`, `visible: true`, and the standard Chrome UA
      - a first read with `total === 0` settles `{ status: 'error' }` and does NOT tick again
        (UNDECIDABLE), asserted by the poll timer never firing a second `cookies()` call
      - `total > 0` with no matched cookie keeps polling (SUPPORTED_BUT_EMPTY), and a later matched
        value reaches `finishLogin`
      - a `{ event: 'finished' }` nav event triggers a forced revalidation and re-arms the deadline
      - `close(label)` is called exactly once when the watch settles, for BOTH the done and the
        stop/silent-cancel paths
      - a `close()` rejection is swallowed (the watch still resolves)
      - Electron regression: with NO seam installed, the existing session-based path still runs
        (assert `session.fromPartition` was called), which is the additive/reversible invariant
        proven rather than asserted
    Use fake timers for the poll; mock `electron-store` — never `requireActual` it.
  </action>
  <acceptance_criteria>
    - `npx jest src/backend/humble/__tests__/user.test.ts` exits 0 with >= 7 new cases in the seam describe.
    - Hand RED-proof recorded in the SUMMARY: change the UNDECIDABLE branch to `return` instead of
      `settle({ status: 'error' })`, confirm the "does not tick again" case FAILS, restore, re-verify.
    - `grep -c "setLoginWindowSeam" src/backend/humble/__tests__/user.test.ts` returns >= 2 (install + teardown).
    - `npm run test:ci` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/backend/humble/__tests__/user.test.ts</automated>
  </verify>
  <done>csrf capture works on both paths, and the seam path's liveness, relay, close-once and Electron-fallback behaviors are all test-proven with a RED-proof on the discriminator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| platform cookie jar → sidecar | `_simpleauth_sess` and `csrf_cookie` values leave the jar and enter Node memory |
| Humble login page (untrusted remote) → watch state machine | Page navigations influence the watch deadline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.1-15 | Information Disclosure | cookie candidate handling in `checkCookie` | mitigate | Values are passed only to `finishLogin`/`getGamekeys` and the existing encrypt-then-store path; never logged, never included in `sendFrontendMessage` (the standing T-10-05 rule). Any new log line carries name/verdict/length only. |
| T-34.4.1-16 | Denial of Service | the poll loop | mitigate | UNDECIDABLE and UNSUPPORTED_OR_ERROR both SETTLE the watch instead of continuing; the existing `LOGIN_WATCH_TIMEOUT_MS` deadline and the `VALIDATION_THROTTLE_MS` never-hammer throttle (C5) are preserved unchanged. |
| T-34.4.1-17 | Repudiation | deadline re-arming | mitigate | Only `on_page_load` main-frame `finished` events re-arm the deadline; `on_navigation` (iframe-inclusive) has no consumer anywhere in this phase, asserted by grep in plan 34.4.1-06's sweep. |
| T-34.4.1-18 | Elevation of Privilege | an orphaned authenticated login window | mitigate | `settle()` closes the window on every exit path (done, waiting, error, stop, deadline); the close-once behavior is test-asserted for both the success and the silent-cancel paths. |
| T-34.4.1-19 | Spoofing | candidate cookie treated as proof of login | accept (existing control) | Unchanged from Electron: a cookie value is only a CANDIDATE; `finishLogin()`'s gamekeys-endpoint validation (D-16) remains the sole authentication boundary (ASVS V2). |
| T-34.4.1-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `npx jest src/backend/humble/__tests__/user.test.ts` and `npm run test:ci` green.
- `npx tsc --noEmit` clean.
- `npm start` (Electron): the Humble login flow still completes through the unchanged partition path.
- `git diff src/backend/humble/user.ts` shows the Electron branch preserved, not replaced.
</verification>

<success_criteria>
`HumbleUser.watchForLogin()` can complete a real Humble login on the Tauri build by opening,
polling and closing a Rust-owned login window, fails loudly rather than silently when the cookie API
is undecidable, re-arms its deadline only on main-frame navigations, and behaves identically to
before under Electron.
</success_criteria>

<output>
Create `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-03-SUMMARY.md` when done
</output>
