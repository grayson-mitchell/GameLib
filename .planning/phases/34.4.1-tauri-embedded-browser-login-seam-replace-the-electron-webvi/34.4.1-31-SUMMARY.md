---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 31
status: complete
completed: 2026-08-23
wave: 1
gap_cycle: 3
commit: 4688c2b0f
---

# Plan 34.4.1-31 — SUMMARY

**NEW-01 closed. `seam-parity-sweep.py` exits 0 again.**

## The plan's premise was wrong, in a way worth recording

Plan 31 was written expecting an **unclassified site** needing a new `SITE_PROFILES` entry, and
warned against "reaching for the classification that silences the gate."

It was **line drift**. The entry for `humble/library.ts::revealTransportLabel` already existed at
`line_hint: 1202`; the site is now at `1211`, and `run_axis_a()`'s ±5 window cannot absorb a 9-line
shift. So the honest fix was **narrower** than the plan assumed — no new classification, no
judgement call about parity relevance.

Fixing one hint surfaced the next. **All three `SITE_PROFILES` hints and seven of the eight
`EXPECTED_AXIS_A_SITES` entries had drifted at once**: `264→274`, `436→457`, `1202→1211`, and the
floor's `272→275`, `168→178`, `264→274`, `646→740`, `943→1034`, `157→195`, `436→457`.

## Renumbered by enclosing function, not by position

The walk now finds a **fifth** `humble/user.ts` site the floor does not list — `:873`,
`checkHealthAndFlagExpiry`'s S-09 guard. Positional matching would have mis-assigned every entry
after it. Each site was re-located by its enclosing function and confirmed to be the same call site
in the same function.

That fifth site stays **out** of the floor, per Plan 18's own note that this list is *"a FLOOR … not
an exhaustive site list"*. Adding it would quietly convert a floor into an inventory.

`legendary/user.ts:137` never drifted — the control proving this was not a blanket rewrite.

This is the same line-number maintenance Plan 18 and gap-cycle-2 plan 28 each performed once. It is
**not** a weakening of the expectation the gate forbids ("fix the walk, never the expectation"): no
site was added, removed, or moved to a different function.

## An attribution error, recorded on purpose

A first draft blamed `fbbfa852e style: apply prettier repo-wide` **alone**, on the strength of
`git diff -w` still showing changes. **That test is invalid** — `-w` ignores whitespace *within* a
line and does not ignore reflowing, so it cannot distinguish a formatting sweep from real work in
either direction. Corrected before commit to the honest attribution: the **~8 commits** touching
these files since the Plan 18 refresh (34.4.2 and 34.5 work plus the prettier sweep), collectively.

## Verification — three states, real exit codes

`cmd | tail` reports `tail`'s status, not the script's — a mistake made while first diagnosing this
gate, so every measurement below used `cmd > file; echo $?`.

| state | expected | measured |
|---|---|---|
| after the fix | exit 0 | **exit 0**, 13 findings written |
| one hint reverted `1211`→`1202` | exit 1 | **exit 1**, same hard stop |
| restored | exit 0 | **exit 0** |
| `--self-test` | exit 0 | **exit 0** — "every check rejects its corresponding bad input" |

Regenerating `34.4.1-SEAM-PARITY-SWEEP.md` risked the recorded *regenerating-an-artifact-breaks-the-pins*
failure (once turned 1 failure into 5). Checked: `seamBranchParity.test.ts` pins this surface and
passes **28/28** afterwards. `pnpm planning-gates` **6/6**.

## Deliberately NOT done

**The sweep is still not wired into CI.** That is the reason a red gate survived 23 days. Out of
scope here, and named rather than left as an undeclared note: it would need adding to
`meta/runPlanningGates.py`'s discovery, plus a decision on whether the three pre-existing
`SILENTLY-DROPPED` entries (`S-07`, `S-10`, `S-12`) are acceptable at red or must be dispositioned
first — they are reported today but do not fail the run.
