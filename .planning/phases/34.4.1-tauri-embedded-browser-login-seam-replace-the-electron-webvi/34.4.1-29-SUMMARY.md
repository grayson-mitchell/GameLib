---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 29
type: execute
wave: 7
gap_closure: true
gap_cycle: 2
completed: 2026-07-31
verdict: PASS
items_total: 4
items_passed: 4
items_failed: 0
phase_closes: true
one_liner: |
  The third blocking live gate passed 4 of 4, closing F-6 — a disconnect that had twice reported
  success while deleting nothing — on behavioural evidence rather than on the mutating call's own
  report, and the gated record updates it had been holding back were released; the domain-scoping
  property it was also supposed to prove turned out to be unmeasurable by the contract's own
  construction, and is recorded untested rather than passed.
requirements_closed:
  - REQ-34.4.1-02
  - REQ-34.4.1-05
  - REQ-34.4.1-06
  - REQ-34.4.1-12
  - REQ-34.4.1-GAP-02
  - REQ-34.4.1-GAP-03
  - REQ-34.4.1-GAP-05
  - REQ-34.4.1-GAP-07
  - REQ-34.4.1-GAP-08
  - REQ-34.4.1-GAP-09
  - REQ-34.4.1-GAP-12
  - REQ-34.4.1-GAP-13
requirements_open:
  - F-9 (RPC timeout; co-occurrence UNDETERMINED)
  - domain-scoping of the cookie clear (UNTESTED, D-29-07)
  - Epic logout observation (→ Phase 34.5, D-29-08)
---

# Phase 34.4.1 Plan 29: Third blocking live gate — Summary

**Verdict: 4 of 4 PASS. Phase 34.4.1 closes.** Run 1 FAILED 2/4, run 2 FAILED 3/4, run 3 passes 4/4.

## Per-item table

| Item | Requirements | Verdict | Decisive evidence |
|---|---|---|---|
| Pre-check | `GAP-13` live half | **PASS** | Manage Accounts renders first-time (F-10 no reproduce); **zero** `unsupported URL` on a full re-render, vs ~150 pre-fix |
| 1 — login from scratch | `-02`, `-12`, `GAP-02`, `GAP-09` | **PASS** | 75B keyring store, no `sessionCookie`, `encryptionDegraded: false`, install line present at 21:58:28 |
| 2 — survives relaunch | `-12` | **PASS** | store byte-identical, both PIDs changed, fresh bootstrap line, **29/29 authenticated gamekey sync** |
| 3 — disconnect | `-06`, `-12`, `GAP-03/05/07/08` | **PASS** | census 34/34/0; reported count agrees with independent re-read; re-login genuinely fresh |
| 4 — `humbleRevealKey` | `-05`, `-12` | **PASS** | `login-window seam transport` ×1, `electron-net transport` ×0; real key returned; secret absent from logs |

No item recorded FAIL. D-10's binding rule was satisfied with no advisory mode, no override and no
partial-pass closure.

## Every number item 3 produced

```
Humble disconnect: cookie census before(total=34, matched=34, verdict=SUPPORTED_NONEMPTY)
                                after(total=0,  matched=0,  verdict=SUPPORTED_BUT_EMPTY)
                                deleted=34 survivingNonHumble=0
Humble disconnect: cleared 34 humblebundle.com cookie(s)
Humble disconnect: cleared storage — localStorage=29, sessionStorage=0, indexedDB=1, caches=0, serviceWorkers=0
```

| Required check | Observed | Verdict |
|---|---|---|
| `matched-after` is 0 | 0 | PASS |
| jar shrank by exactly `matched-before` | 34 → 0 | PASS |
| `deleted` equals `matched-before` | 34 = 34 | PASS |
| `survivingNonHumble` non-zero | **0** | **VACUOUS — see below** |
| read path: `T0` / `V0` | 34 / `SUPPORTED_NONEMPTY` | PASS |

**Behavioural half — the measurement that actually closed F-6:**

| | Poll lines | Rejections | Duration |
|---|---|---|---|
| Run 2 (FAILED) | 0 | 0 | ~3 sec |
| Genuine-login reference | ~3 | ~17 | ~1.3 min |
| **This run** | **12** | **68** | **6 min 17 sec** |

All 68 rejections carry reason `session_expired`. A session that survived the disconnect cannot
produce that reason, so the rejections are themselves evidence the old session was **gone**, not
merely unrecognised. Corroborated outside the log: both Keychain slots went `NOT FOUND` after the
disconnect and returned with `mdat` matching login acceptance to the second.

## WR-07 and F-4, in the operator's own words

**WR-07 — title bar.** *"title bar changed, but saw the change, i.e. tauri app, then corrected while
watching."* Exact final text: **`Humble Bundle - Log In`** — 22 characters, an exact match to plan
24's `title change applied len=22` diagnostic. Run 2's stuck-at-`Tauri app` failure is reversed.

**F-4 half (a), unobserved in BOTH prior runs.** *"comes to the fron and takes focus."* PASS.

**F-4 half (b).** *"when navigate away and click on the app the window does not... it is independant
from the app."* This is a precise natural-language restatement of `persistent_pin=false` — the
requested presentation line was present and matched observed behaviour on both halves. **Not a
defect**; the platform offers one-shot focus *or* a persistent pin, and this plan chose the former.

## Which gated records moved, and the rule that decided each

Item 3 PASSED, so the gated set was released. Applied **exactly** what the verdict permits:

| Record | Moved? | Rule |
|---|---|---|
| `IPC-PORT-INVENTORY.md` D-05 partial | **YES** — resolved to a closed declaration citing this run's census | item 3 PASS |
| `34.4.1-PORTED-CHANNELS.md` struck D-05 entry | **YES** — restored with a dated note naming both defects | item 3 PASS |
| `REQ-34.4.1-06/-12/GAP-03/GAP-05/GAP-07/GAP-08` | **YES** — checked with dated riders | item 3 PASS + 4/4 |
| `REQ-34.4.1-02`, `GAP-02` | **YES** | item 1 PASS |
| `REQ-34.4.1-05` | **YES** | item 4 PASS |
| `REQ-34.4.1-GAP-09` | **YES** | **both** halves observed — the plan forbade checking on a title-only pass |
| `REQ-34.4.1-GAP-12` | **YES** | pre-check rendered first-time **AND** plan 25 recorded a *measured* fix — a non-reproduction alone would not have sufficed |
| `REQ-34.4.1-GAP-13` | **YES** | zero `unsupported URL` live, closing the half plan 27 could not close by unit test |
| **domain-scoping claim** | **NO** | untested — see below |
| **Epic logout** | **NO** | shared-code-path argument only |

## The one thing the gate could not measure, and why

`survivingNonHumble=0` is **vacuous, not passing**: `before total=34` equalled `matched=34`, so no
non-Humble cookie existed for the delete to spare, and the zero is arithmetically forced.

**Root cause is this phase's own contract, not the operator or the code.** Precondition 6 of
`34.4.1-LIVE-GATE-RERUN-3.md` STRUCK the planted non-Humble cookie as moot, reasoning that plan 17's
census *"supplies the evidence without it."* That reasoning conflates the **measuring apparatus**
with the **thing measured** — a census cannot report `survivingNonHumble > 0` against a single-origin
jar however correct its counting. Item 1's from-scratch reset then guaranteed the single-origin
condition. **The contract told the operator not to plant a cookie and then required an outcome only
a planted cookie could produce.** No action available during the run could have satisfied it.

Recorded as **UNTESTED** — neither PASS (nothing was measured) nor FAIL (nothing suggests
over-deletion). The next cycle must **unstrike precondition 6**; the planted cookie and the census
are complements, not alternatives.

## Epic cross-check disposition

**No Epic test session was available.** The evidence for the shared Rust arm's second caller is the
**shared-code-path argument only, not an observation**. No `clearEpicCookies` line was seen; the
removed-nothing warning was neither observed to fire nor not to fire. No new Epic OAuth login was
opened (D-04). Honest statement: **expected fixed by construction, unobserved in practice.** No
document may call it verified. → Phase 34.5.

## It took three live gate runs — and what each caught that a green suite could not

| Run | Suite | Verdict | What it caught |
|---|---|---|---|
| 1 (07-30) | 3279/3279 green | FAIL 2/4 | **F-1** — session persisted in PLAINTEXT; **F-6** — disconnect cleared cookies only, re-login auto-signed in |
| 2 (07-31) | 3387/3387 green | FAIL 3/4 | **F-6 Defect A + B** — census read the jar in the *wrong direction*; delete reported an *attempted* count for an operation WebKit never performed |
| 3 (07-31) | 3447/3447 green | **PASS 4/4** | Confirmed all fixes live; found **D-29-01** (Manage Accounts stale after sign-in), **D-29-02** (`/api/v1/user/info` HTML 404), **D-29-03** (successful reveal logs nothing) |

**Every blocking defect in this phase was found by a human driving the UI. None by automation.** The
suite was fully green for all three runs. Run 2's own process lesson — *a gap analysis that
enumerates what is MISSING will not question what is PRESENT* — is what pointed plans 22/23 at the
step that already existed, and is why run 3 could pass.

## Deviations

**`seam-parity-sweep.py` edited though not in `files_modified`.** Task 3's acceptance criteria
require its `--self-test` to exit 0; it was failing at HEAD because plan 28 added two check
functions and their self-test cases without bumping `expected_case_count` from 11. **The guard that
exists to prove every check can reject was the one check nobody was running.** Fixed as a one-line
constant bump (11 → 13) with an explanatory comment. Precedent for a recorded deviation of this
shape: plan 25 and `App.css`. Filed as `D-29-10`.

**Item 4's verdict was initially written above its prior-run baseline rather than into the canonical
`Result:` slot.** Caught by the plan's own automated verify (`grep -c "Result:"`), not by
inspection, and repaired. Repositioning surfaced two points otherwise lost: run 2's item 4 ran
against an **auto-restored** session (the very failure the gate exists to catch), and D-07's
Cloudflare-403 branch was **not exercised** this run.

## Process failure recorded against this run

**F-7 recurred.** Item 2 was skipped and its live session destroyed by item 3's disconnect before
its readings were taken — the exact hazard this document warned about **in writing, in advance**. It
was recovered at zero extra credential cost by reusing item 3(a)'s genuine login, and the recovered
result is a legitimate PASS.

**The lesson is not "warn harder."** A prose warning was written, was read, and did not hold. Item 2
is the only item whose validity depends on nothing else happening first, and the only one with no
on-screen affordance of its own. **Its "before" snapshot must become an executor-captured artifact
gated before the disconnect affordance is described to the operator.** Mechanism, not warning.

**Corollary, earned twice:** the relaunch rotated `gamelib.log` and destroyed item 1's login and item
3's entire census and poll trace from that file. Every number had already been transcribed into the
gate document and committed (`c26e42512`, `0ab647d34`) before the rotation. Recording verbatim
as-you-go is what made this survivable.

## Method notes worth preserving

- **"i am signed in" was not interpreted.** On its own that report is equally consistent with a
  genuine login and with run 2's auto-sign-in failure — the precise thing under test. The verdict
  rests on the poll trace.
- **Two hypotheses were built and refuted**, both retained rather than deleted: `len=0` as the cause
  of the `Tauri app` flash (disposed of by ordering), and a title/applied-title divergence (disposed
  of by an exact 22-character match). Per F-10's costly lesson the discriminator was built each
  time instead of shipping the nicer reading — at zero live-run cost.
- **A root-cause attribution was corrected mid-run:** `survivingNonHumble=0` was first blamed on the
  operator's from-scratch choice, then traced to the contract's own struck precondition 6.
- **A clean grep of the wrong source is not evidence of absence.** "F-9 watch CLEAN" was recorded
  from a `gamelib.log` grep that structurally could not see shell `eprintln!` output; a generic RPC
  timeout had in fact fired.

## Verification

| Check | Result |
|---|---|
| `npm run test:ci` | **178 suites / 3447 tests, 0 failed** |
| `npx tsc --noEmit` | exit 0 |
| `cargo test` | **80 passed, 0 failed, 1 ignored** |
| `ported-channels-gate.py` + `--self-test` | both OK |
| `seam-parity-sweep.py` + `--self-test` | both OK (after the D-29-10 fix) |
| `wkwebview-silent-noop-sweep.py --self-test` | OK |
| spike016 functional scaffolding | 0 hits (the 1 literal hit is plan 23's design-rationale comment, per precondition 4) |
| secret scan across all edited documents | 0 hits |

## Findings filed

`D-29-01`..`D-29-10` in `deferred-items.md`, each naming **the file a future plan must declare** —
because plan 28 closed a sweep item only after it had been correctly re-forwarded twice without
being done. **A carry-forward without an owning plan that declares the file is a note, not a task.**

## Next

- **Phase 34.5 plan 15's live gate is UNBLOCKED** — its precondition was this gate.
- **Phase 34.6 has no directory and no plans**, so Phase 35's stated precondition is still silently
  false.
- `/gsd-secure-phase 34.4.1` is owed (no SECURITY.md for this phase).
