---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 30
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_cycle: 3
files_modified:
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/deferred-items.md
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-3.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-34.4.1-GAP-11]
must_haves:
  truths:
    - "Findings that are already closed are recorded as closed, with the evidence that closes them"
    - "A requirement checkbox never contradicts its own rider"
    - "A record that disagrees with the filesystem is corrected toward the filesystem"
---

# Plan 34.4.1-30 — Ledger truth pass: close what is already closed, fix what disagrees

**Wave 1. No code changes. This plan touches `.planning/` only.**

Read `34.4.1-GAP-CYCLE-3-ANALYSIS.md` first — it carries the evidence each task below records.

---

## Task 1 — Record D-29-01 as CLOSED, with its evidence

`D-29-01` (Manage Accounts does not self-refresh after successful sign-in) was fixed after it was
filed. Append a dated closure to its entry in `deferred-items.md`. **Do not delete the original
entry** — the house convention is to append, and the finding's history is the useful part.

The closure must cite, verbatim:

- `src/frontend/screens/WebView/components/HumbleLoginSurface.tsx:58-67`, whose comment describes
  D-29-01's exact symptom **in the past tense as the rationale for the replacing design**: *"the
  promise settled, but nothing told `TauriLoginPanel` … so the user was left staring at a lying
  in-progress message forever."*
- the current success branch: `await humble.login(result); onDone()`.
- attribution: Phase 34.4.2 (`F-34.4.2-19`) and quick task `260808-gl6`.

**State explicitly that `.planning/debug/resolved/manage-accounts-slow-update.md` is NOT this
defect** — it is `status: fixed` for the GOG/OAuth-capture path (2026-08-03, phase 34.5). It is
corroborating context for the defect class, not D-29-01's closure. Recording it as the closure
would be the same conflation D-29-02's discriminator was written to prevent.

## Task 2 — Record D-29-10 as CLOSED-AT-FILING

`D-29-10`'s own body already says *"Fixed as a recorded deviation… Filed so the pattern is
visible."* It was never an open task. Mark it so, so no future cycle re-plans it. Preserve the
transferable lesson: **a self-test's own bookkeeping needs a test too, or it silently stops
running.**

## Task 3 — D-29-09: fix the ONE contradicting checkbox

Set **`REQ-34.4.1-GAP-11`** to `[ ]` in `.planning/REQUIREMENTS.md`. Its own body ends *"This box
stays UNCHECKED — live-only"* and it carries no `**CLOSED 2026-…**` record.

**Change exactly one checkbox.** The analysis document's hand sweep found this is the only genuine
contradiction among all 26 `REQ-34.4.1-*` blocks; `REQ-34.4.1-13` and `REQ-34.4.1-GAP-06` are false
positives caused by the gap-cycle section preamble being absorbed into the preceding block, and
**must not be touched**.

Record in `deferred-items.md` that D-29-09's real extent was one requirement, not many, and that
the tooling could not have found it — *plan-phase gap-analysis is blind to every decimal-phase REQ
ID*, and this phase is doubly decimal.

**Verification, both directions.** Re-run the hand sweep after the edit and assert:

| control | expectation |
|---|---|
| `REQ-34.4.1-04` (unit-provable, no rider) | not flagged |
| `REQ-34.4.1-GAP-11` | no longer flagged, because its box is now `[ ]` |

A sweep that flags nothing because it can no longer flag anything is vacuous. **Prove it still
flags by temporarily re-checking `GAP-11`, confirming the flag returns, then reverting.**

## Task 4 — Reconcile two records that disagree with the filesystem

1. **ROADMAP** says `**Plans:** 28/29 plans executed`. All 29 `34.4.1-NN-PLAN.md` files have
   matching `-SUMMARY.md` files on disk. Correct to `29/29`, and note the correction rather than
   silently overwriting.
2. **`34.4.1-LIVE-GATE-RERUN-3.md` frontmatter reads `status: pending` / `verdict: null`** while its
   body records the 4/4 PASS that closed the phase. Correct the frontmatter to match the body.

Flag in `deferred-items.md` that this frontmatter-vs-body rot is **a recurring shape, not a
one-off** — `34.4-VERIFICATION.md` records the identical staleness against `34.4-LIVE-GATE.md`.
A live-gate contract's frontmatter rots independently of its body because nothing reads it.

---

## Acceptance

- `deferred-items.md` carries dated closures for D-29-01 and D-29-10 with the cited evidence.
- Exactly one checkbox changed in `REQUIREMENTS.md`; the sweep's discriminating power is re-proven
  after the edit, not merely asserted.
- ROADMAP reads `29/29`; `LIVE-GATE-RERUN-3.md` frontmatter matches its body.
- **No file under `src/` or `src-tauri/` is modified by this plan.**

## Commit discipline

Two unrelated renames are already staged, and nine `src/` files are modified by a concurrent
session. Use `git commit --only <paths>` with the paths from `files_modified`. **Never `git stash`,
`git reset`, or `git stash pop`.**
