---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 20
type: execute
wave: 10
depends_on: ["34.4.1-19"]
gap_closure: true
files_modified:
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN.md
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE.md
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
  - .planning/IPC-PORT-INVENTORY.md
autonomous: false
requirements: [REQ-34.4.1-12, REQ-34.4.1-02, REQ-34.4.1-05, REQ-34.4.1-06]
must_haves:
  truths:
    - "A from-scratch Tauri Humble login persists its session ENCRYPTED, not in plaintext"
    - "A disconnect is followed by a genuinely fresh login, not an auto-signed-in page"
    - "The disconnect's domain scope is proven by evidence taken inside the operation"
    - "The session still survives a relaunch and the reveal still works — no regression"
    - "Every record update gated on item 3 is applied only if item 3 actually passed"
  artifacts:
    - path: ".planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN.md"
      provides: "The re-run contract, written with empty result slots BEFORE any live work"
      contains: "Verdict"
  key_links:
    - from: "34.4.1-LIVE-GATE-RERUN.md"
      to: "34.4.1-LIVE-GATE.md"
      via: "explicit supersession of the F-1/F-6 findings"
      pattern: "F-1|F-6"
---

<objective>
Re-run the blocking live gate. This is the only evidence that closes F-1 and F-6.

Purpose: **REQ-34.4.1-12**, and the gate's own rule — *"any item recorded FAIL means this phase
does not close ... no advisory mode, no pre-authorized override, no partial-pass closure."*
Nothing in plans 10-19 proves F-1 or F-6 fixed. Both defects were invisible to a fully-green
3279/3279 suite and 40/40 `cargo test`; a second green suite is not different evidence. This is
now the **third** consecutive slice where a live gate caught what automation could not.

Scope: **items 1 and 3 re-run IN FULL. Items 2 and 4 re-run to confirm no regression** — item 2's
store contents changed (the secret moved to the keyring) and item 4's transport label changed, so
neither can simply be carried forward.

Output: a recorded verdict, and the record updates that verdict permits — no more.

**Two process lessons from the last run, to be honoured rather than repeated:**
- **F-7:** item 2's before/after measurement was scheduled AROUND the item, and the next item
  destroyed the "after". It could not be reconstructed. **Every paired reading is taken INSIDE the
  item that needs it**, before the next item begins.
- **Log rotation:** `gamelib.log` rotated three times during the last run and nearly took item 1's
  and item 4's evidence with it. Task 1 must preserve rotations, and the operator must not rely on
  a byte offset noted earlier in the session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md

<interfaces>
**Evidence the gap cycle built specifically so this gate can read it.** Each plan's SUMMARY records
the exact format; read them rather than guessing at a grep:
- plan 13 — a `gamelib.log` line at sidecar startup confirming the Humble secret store is
  keyring-backed. **Its absence means the install silently degraded to the plaintext default, and
  item 1 fails regardless of what the store file shows.**
- plan 15/16 — the per-category storage-clear report emitted during disconnect.
- plan 17 — the disconnect jar census line: jar total before/after, Humble matched before/after,
  deleted count, surviving non-Humble count. **This is item 3(b)'s entire proof.**
- plan 18 — the new poll log shape (one line per status change plus a suppressed count), replacing
  item 1's original ~37 identical warnings.

**Item 1's evidence requirement, which the last run violated.** The store must hold an ENCRYPTED
session. Under the new design that means `~/Library/Application Support/GameLib/humble_store/config.json`
has **no `sessionCookie` field at all** (it lives in the Keychain) and `encryptionDegraded: false`.
Compare against the last run's measured failure:
```
size 353, mtime 2026-07-30T05:39:40.925Z
encryptionDegraded   boolean, TRUE        <-- the defect
sessionCookie        str, len=208         <-- plaintext
csrfToken            str, len=29
```
Record field types and lengths only, never values (T-34.4.1-39).

**Item 3's pass condition, both halves:**
(a) the clear runs and reports a real non-zero count, AND a re-login presents Humble's own login
    form — not an auto-signed-in page. Last run: 3 seconds from `cleared 27 humblebundle.com
    cookie(s)` to a persisted valid session with ZERO `session_expired` lines, where a genuine
    from-scratch login produced 36 across 2.5 minutes. **That contrast is the measurement.**
(b) the census arithmetic holds: Humble matched count goes to 0, the jar shrank by exactly the
    matched count, and a non-zero surviving non-Humble count is reported.

**Precondition 6 (plant a non-Humble cookie) is MOOT and must not be attempted.** Plan 17's census
supplies 3(b)'s evidence without it. Left in the record rather than deleted, per this phase's
strike-don't-delete convention.

**Credential restore paths from the last run's precondition 3** — the operator is currently signed
out of Humble because the gate moved the store aside:
```
~/Library/Application Support/GameLib/humble_store/config.json.pre-34.4.1-08-gate.bak
/private/tmp/claude-501/.../scratchpad/humble_store-config.json.backup-20260729
```
Both were verified byte-identical (484 bytes, Jul 27 12:27). Note the backup is an **Electron-era**
store (`encryptionDegraded: false`, prefixed ciphertext of lengths 314/58), so restoring it is
also a live test of plan 13's migration reading an Electron-format value — worth doing
deliberately rather than by accident, and worth recording either way.

Baseline at planning time: `npm run test:ci` exit 0 (173/173 suites, 3279/3279) and
`cd src-tauri && cargo test` exit 0 (40/40). Documented intermittent: a `rustInvoke` mock
frame-leak that can land on an ARBITRARY suite with `rustInvoke timed out after 60000ms:
keyring_get`. It did not reproduce in the gate-time runs, but one clean run does not falsify an
intermittent — **do not treat its reappearance as a new regression without isolating the suite
first.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the re-run contract, satisfy the automatable preconditions, and protect the evidence</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN.md</files>
  <read_first>
    - `.planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md` in full — Preconditions 1-6, all four
      items' "What to do / What to observe / Evidence required" blocks, the item-3 amendment, and
      the Verdict section. **The re-run contract inherits this structure; it does not invent one.**
    - The SUMMARYs of plans 13, 15, 16, 17 and 18 — the exact log line formats this gate greps for.
    - Project memory `phase-33-complete` — sidecar logs go to `~/Library/Logs/GameLib/gamelib.log`,
      NOT the `tauri:dev` terminal, and Electron `app.whenReady()` initializers are not auto-run in
      the sidecar, so anything that looks uninitialised there is a real finding.
  </read_first>
  <action>
    Write `34.4.1-LIVE-GATE-RERUN.md` as a CONTRACT with empty result slots, before any live work —
    the same D-09/D-10 discipline the original gate followed. It carries the same four items and
    the same blocking rule, with these amendments recorded up front:
    - Item 1's evidence requirement restated for the keyring design: no `sessionCookie` field in
      the store, `encryptionDegraded: false`, AND plan 13's install line present in the log.
    - Item 2's paired reading taken INSIDE item 2 (F-7), with the "after" captured before item 3
      begins. Say so in the item, not in a note elsewhere.
    - Item 3(b) proven by plan 17's census, with precondition 6 struck as moot.
    - Items 2 and 4 marked as regression re-runs, with their prior PASS results cited so a
      divergence is visible rather than absorbed.

    Then satisfy the automatable preconditions and record the evidence:
    1. Remove stale `src-tauri/target/debug/build/gamelib-shell-*` artifacts and confirm a real
       rebuild, exactly as the last run's Task 1 did.
    2. Re-measure the automated baseline at current HEAD (`npm run test:ci`, `cargo test`) and
       record both verbatim — **not rounded to green.** If the documented `rustInvoke` frame-leak
       flake appears, isolate the affected suite and record the isolation result rather than
       declaring a regression.
    3. Record the current Humble credential state and the two restore paths, verifying both
       backups still exist and are byte-identical to each other.
    4. **Protect the evidence against rotation.** Set up a capture that preserves rotated
       `gamelib.log` segments for the whole gate run, so a rotation mid-item cannot destroy an
       item's evidence the way it nearly did last time. Record the capture location in the
       contract and instruct the operator to take a FRESH baseline offset at launch — any offset
       noted earlier is void.

    Do not drive any of the four items in this task. This is preparation and contract-writing;
    plan 08's run-and-record boundary is inherited. If any precondition cannot be satisfied,
    STOP and report it rather than proceeding with a weaker version of the gate.
  </action>
  <verify>
    <automated>npm run test:ci; cd src-tauri && cargo test; ls -la ~/Library/Application\ Support/GameLib/humble_store/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `34.4.1-LIVE-GATE-RERUN.md` exists with all four items present and every result slot EMPTY.
    - The contract restates item 1's encrypted-storage requirement in terms of the keyring design
      and names plan 13's install log line.
    - The contract marks item 2's paired reading as taken inside item 2, citing F-7.
    - The contract strikes precondition 6 as moot and cites plan 17's census as 3(b)'s evidence.
    - The baseline is recorded verbatim with its exact exit codes and counts, including any flake
      and its isolation result.
    - Both credential backup paths are confirmed present and byte-identical.
    - A rotation-preserving log capture is in place and its location is recorded in the contract.
    - `git diff --stat -- .planning/IPC-PORT-INVENTORY.md` is EMPTY at the end of this task.
  </acceptance_criteria>
  <done>The re-run contract is written before any live work, the automatable preconditions are
  evidenced, and the log evidence is protected against the rotation that nearly destroyed it last
  time.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING live gate re-run — items 1 and 3 in full, items 2 and 4 for regression</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN.md</files>
  <read_first>
    - `34.4.1-LIVE-GATE-RERUN.md` — Task 1's contract: the four items, their evidence
      requirements, the amendments, and the preserved-log capture location.
    - `.planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md` — the original run's per-item results and
      measured numbers, which this run is scored against for contrast.
    - The SUMMARYs of plans 13, 15, 17 and 18 — the exact log line formats to look for.
  </read_first>
  <action>
    Drive all four items of `34.4.1-LIVE-GATE-RERUN.md` live under `pnpm tauri:dev` with the
    developer at the keyboard, recording each item's result and evidence INTO that document as the
    item completes — never afterwards from memory (F-7). This task is run-and-record: do not fix
    anything discovered here. A defect found mid-gate is a finding for the next cycle, and
    inventing a fix at gate time would ship unproven code, which is exactly what plan 08 declined
    to do with F-1.
  </action>
  <what-built>
    Plans 10-19 closed both blocking defects at the code level and added the instruments this gate
    reads:
    - **F-1** — the Humble session cookie now goes to the OS Keychain through a multi-slot keyring
      allowlist (`humble-session` / `humble-csrf`), never to the Steam token's slot. An existing
      plaintext credential migrates with a verified readback before the plaintext is deleted.
    - **F-6** — disconnect now clears the origin's `localStorage`, `sessionStorage`, IndexedDB,
      caches and service workers in addition to its cookies, scoped by same-origin policy rather
      than by a filter. The identical defect in Legendary's logout was closed with it.
    - **F-5** — disconnect emits a before/after cookie-jar census that proves domain scope from
      inside the operation, so 3(b) no longer needs a planted control cookie.
    - **F-2/F-3/F-4/F-8** — state-change poll logging, a named endpoint on identity failure, a
      sized/centered/raised login window (still with NO hardcoded title, WR-07), and a reveal
      transport label derived from the seam.
    - A branch-parity guard now fails when an Electron and Tauri branch diverge again.

    A fully-green suite and `cargo test` prove none of this works live. That is the whole reason
    this gate exists, and it has now caught what automation could not on three consecutive slices.
  </what-built>
  <how-to-verify>
    Work `34.4.1-LIVE-GATE-RERUN.md` (Task 1's contract) top to bottom and record results IN it as
    you go. Read sidecar output from `~/Library/Logs/GameLib/gamelib.log`, never the `pnpm
    tauri:dev` terminal. Take a FRESH baseline offset at launch.

    **Preconditions:** tick off the contract's list in order. You hold real Humble credentials
    (precondition 2) and must confirm an UNREVEALED key exists after item 1's login, before item 4
    (precondition 4). **Do NOT plant a non-Humble cookie — precondition 6 is moot.**

    **Decide and record your starting state before item 1.** Either:
      (a) start from ABSENT (the from-scratch path), or
      (b) restore the Electron-era backup first, which additionally exercises plan 13's migration
          against a real prefixed-ciphertext value.
    Both are legitimate; (b) tests more. Whichever you choose, record it — the store's starting
    contents change what item 1's evidence means.

    **Item 1 — login FROM SCRATCH (REQ-34.4.1-02).** Open the login window, authenticate for real
    including any Humble Guard / OTP step, and let the sidecar detect the session.
    - The window should now be large enough for its content and should NOT get lost behind other
      applications when you switch to a password manager (F-4). Report if either is still wrong.
    - Confirm the window's title bar shows Humble's own document title, never "GameLib" (WR-07).
    - Confirm plan 13's keyring-install line is present in the log. **If it is absent, item 1
      FAILS regardless of the store contents** — its absence means the install silently degraded
      back to the plaintext default.
    - Confirm the login poll now logs one line per status change with a suppressed count, instead
      of ~37 identical warnings (F-2).
    - **Evidence: the store file's field types and lengths only, never values.** PASS requires NO
      `sessionCookie` field and `encryptionDegraded: false`.

    **Item 2 — survives quit-and-relaunch (REQ-34.4.1-12), regression re-run.** Capture the store
    snapshot immediately after item 1. Fully quit (not just close the window), relaunch, confirm
    the relaunch independently (fresh process, log lines, `ps aux`), then capture the "after"
    snapshot. **Take both readings inside this item, before starting item 3 — last time item 3
    overwrote the "after" and it could not be reconstructed (F-7).** Signed-in state must survive
    with no re-auth polls. Prior result: PASS.

    **Item 3 — disconnect (REQ-34.4.1-06). THE ITEM THAT FAILED.** From the signed-in state,
    trigger Humble disconnect.
    - (a) Confirm the clear ran and reported a non-zero cookie count, AND the per-category storage
      clear report is present. Then **attempt a fresh login and confirm you land on Humble's own
      login form, not an auto-signed-in page.** Last time this took 3 seconds and zero
      `session_expired` lines; a genuine login produced 36 across 2.5 minutes. If you are signed
      straight back in again, F-6 is NOT fixed — record it exactly as observed.
    - (b) Find plan 17's census line and check the arithmetic: Humble matched goes to 0, the jar
      shrank by exactly the matched count, and a non-zero surviving non-Humble count is reported.
      A blanket wipe would show the surviving count at 0 and must FAIL this.
    - Record any category reported `unsupported` — that is information, not a failure.

    **Item 4 — `humbleRevealKey` (REQ-34.4.1-05), regression re-run.** Using an unrevealed key,
    attempt a real reveal. A 403 remains a VALID declarable outcome. Confirm the transport label
    now names the SEAM transport, not `electron-net` (F-8). Never record the revealed value. Prior
    result: PASS with a real key returned.

    **Scoring rule, unchanged and binding:** any item recorded FAIL means this phase does not
    close. No advisory mode, no pre-authorized override, no partial-pass closure. If something is
    unclear or unobservable, record it as such — do not round it up to a PASS. That honesty is
    what made the last run useful.
  </how-to-verify>
  <acceptance_criteria>
    - All four items have a recorded result in `34.4.1-LIVE-GATE-RERUN.md`, each with the evidence
      its contract requires — no empty slots and no inferred results.
    - Item 1's evidence includes the store's field list (types and lengths only) and an explicit
      statement about the keyring-install log line's presence.
    - Item 2's before AND after snapshots are both recorded, both taken during item 2.
    - Item 3 records the cookie count, the per-category storage report, the census arithmetic, and
      the observed re-login outcome in the operator's own words.
    - Item 4 records the transport label observed and the outcome (key returned or a classified
      failure), with no revealed value written down.
    - A Verdict section states the overall result and, on any FAIL, what the next gap cycle is.
    - No secret value appears anywhere in the document (T-34.4.1-39).
  </acceptance_criteria>
  <verify>
    <human-check>All four items in `34.4.1-LIVE-GATE-RERUN.md` carry a recorded result and its
    required evidence, and the developer has signalled the verdict.</human-check>
    <automated>grep -c "Result:" .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN.md</automated>
  </verify>
  <done>All four items are recorded with the evidence their contract requires, and the developer
  has signalled the verdict or named the failures.</done>
  <resume-signal>
    Record each item's result in `34.4.1-LIVE-GATE-RERUN.md` as you go, then reply with the
    verdict — e.g. "4/4 PASS" or "item 3 FAIL: [what you saw]". Partial observations are welcome
    mid-run; the phase closes only on 4/4.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verdict and apply ONLY the record updates it permits</name>
  <files>
    .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN.md
    .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE.md
    .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
    .planning/IPC-PORT-INVENTORY.md
  </files>
  <read_first>
    - `34.4.1-LIVE-GATE-RERUN.md` — Task 2's recorded results. **The measurement wins over every
      expectation in this plan.**
    - `.planning/phases/34.4.1-.../34.4.1-19-SUMMARY.md` — the explicit checklist of what remains
      gated and on what.
    - `.planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md` § "Record updates this result PERMITS and
      FORBIDS" — the original gating table, which this re-run resolves.
    - `.planning/IPC-PORT-INVENTORY.md` L73-75 — the `humbleDisconnect` declared-partial note,
      the ONE inventory edit this phase is permitted, gated on item 3.
    - `.planning/phases/34.4.1-.../ported-channels-gate.py` — check 5 compares the inventory
      against HEAD, so an uncommitted edit fails it. Commit, then re-run.
  </read_first>
  <action>
    Write the Verdict into `34.4.1-LIVE-GATE-RERUN.md`: a per-item table, the overall result, and
    a findings register for anything new. Add a dated cross-reference in `34.4.1-LIVE-GATE.md`
    pointing to the re-run and stating which of F-1..F-8 it resolved — strike, do not delete,
    per this phase's convention.

    Then apply the gated updates, and ONLY those the result permits:

    **If item 3 PASSED:**
    - Update `IPC-PORT-INVENTORY.md` L73-75's `humbleDisconnect` declared-partial note to record
      that Phase 34.4.1 closed it, naming this gate run as the evidence. This is the one inventory
      edit the phase is permitted. **Commit it, then re-run `ported-channels-gate.py`** — check 5
      compares against HEAD and will fail on an uncommitted edit; that is the check working, not a
      bug.
    - Un-strike `34.4.1-PORTED-CHANNELS.md`'s "34.4 D-05's `humbleDisconnect` partial is CLOSED"
      entry, dated, citing the live evidence.

    **If item 3 FAILED:** change neither. The note stays exactly as it is and the entry stays
    struck. Writing "closed" into the inventory on anything less than a live pass is the precise
    failure mode this gate exists to prevent, and it has already been avoided once.

    **If item 1 PASSED:** resolve `humbleStartLogin`'s forward reference to PROVEN, and record
    that F-1 is closed with live evidence. **If item 1 FAILED:** record why, and F-1 stays open —
    which also means Phase 34.5-15 stays held, since its items mint real OAuth credentials over
    this seam.

    Resolve items 2 and 4's regression results into their rows the same way.

    Update `REQUIREMENTS.md`'s reopened IDs to match the outcome. Check a box only for a
    requirement the gate actually proved.

    On any FAIL, write the next gap cycle's recommended scope into the Verdict, in the same shape
    the original gate did — ordered by what a fix must not get wrong, not by severity alone.
  </action>
  <verify>
    <automated>cd .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi && python3 ported-channels-gate.py --self-test && python3 ported-channels-gate.py && python3 seam-parity-sweep.py --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `python3 ported-channels-gate.py` exits 0 and `--self-test` exits 0.
    - `python3 seam-parity-sweep.py --self-test` exits 0.
    - `34.4.1-LIVE-GATE-RERUN.md` has a Verdict table with a result for all four items.
    - `34.4.1-LIVE-GATE.md` carries a dated cross-reference naming which findings were resolved.
    - If item 3 PASSED: `git log -1 --stat -- .planning/IPC-PORT-INVENTORY.md` shows the note
      updated and committed, and `git diff --stat -- .planning/IPC-PORT-INVENTORY.md` is empty.
    - If item 3 FAILED: `git diff --stat -- .planning/IPC-PORT-INVENTORY.md` is empty AND
      `git log -1 --format=%H -- .planning/IPC-PORT-INVENTORY.md` is unchanged from before this
      plan — the file was neither edited nor committed.
    - No proof level claims a live result the re-run did not record.
    - `npm run test:ci` exits 0 and `cd src-tauri && cargo test` exits 0.
  </acceptance_criteria>
  <done>The verdict is recorded, the gated record updates match the measured result exactly, and
  the phase's closure status is an honest consequence of what was observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Humble origin -> app | real credentials over a real network during the gate |
| gate evidence -> planning record | a mis-scored item becomes a shipped defect marked reviewed |
| OS Keychain -> sidecar | first live exercise of the multi-slot allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.1-39 (carried) | Information Disclosure | gate evidence | mitigate | Field types and lengths only; never a cookie value, a csrf token, or a revealed key. Asserted by the acceptance criteria and by the contract's own instruction. |
| T-34.4.1-86 | Repudiation | mis-scored item | mitigate | Each item's pass condition is written into the contract BEFORE the run, with the prior run's measured numbers cited for contrast. An unobservable result is recorded as unobservable, never rounded to PASS — the discipline that made the last run useful. |
| T-34.4.1-87 | Denial of Service | Steam sign-out during the gate | mitigate | The allowlist's first live exercise could in principle disturb the Steam slot. Item 1 should be followed by a confirmation that Steam is still signed in; record it. Rust's allowlist plus plan 13's grep gate and tests make this unlikely, but "unlikely" is not "observed". |
| T-34.4.1-88 | Denial of Service | credential loss during migration | mitigate | Plan 13 never deletes the plaintext before a verified readback; both pre-gate backups remain on disk and their paths are restated in the contract. A failed migration leaves the user signed in. |
| T-34.4.1-89 | Repudiation | evidence lost to log rotation | mitigate | Task 1 sets up a rotation-preserving capture and voids any earlier byte offset. Last run, three rotations nearly destroyed items 1 and 4's evidence. |
| T-34.4.1-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages and changes no source. |
</threat_model>

<verification>
- All four items recorded with their required evidence in `34.4.1-LIVE-GATE-RERUN.md`.
- `python3 ported-channels-gate.py` and `--self-test` exit 0.
- `python3 seam-parity-sweep.py --self-test` exits 0.
- `npm run test:ci` exits 0 and `cd src-tauri && cargo test` exits 0, both recorded verbatim.
- The `IPC-PORT-INVENTORY.md` edit is present if and only if item 3 passed.

**The phase closes only on 4/4.** Anything less is a further gap cycle inside 34.4.1, scoped in
the Verdict — not a deferred UAT entry, not an advisory note, not an override.
</verification>

<success_criteria>
- The re-run contract was written before any live work, with empty result slots.
- Items 1 and 3 were re-run in full; items 2 and 4 confirmed no regression.
- Every paired measurement was taken inside its own item (F-7).
- Log evidence survived any rotation.
- Record updates gated on item 3 were applied if and only if item 3 passed.
- The verdict is an honest consequence of what was observed, and the phase's closure follows it.
</success_criteria>

<output>
Create `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-20-SUMMARY.md` when done.

State the verdict, whether Phase 34.4.1 closes, and the consequence for **Phase 34.5-15** — whose
precondition 1 is 34.4.1's gate, and whose own gate items mint real OAuth credentials over this
seam. If F-1 is now closed with live evidence, say so explicitly: that was the stated reason to
fix F-1 before 34.5-15 runs.
</output>
