---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 06
subsystem: auth
tags: [tauri, humble, cookies, disconnect, electron-reach-ledger, label-safety, sweep]

# Dependency graph
requires:
  - phase: 34.4.1-01/02
    provides: "Rust humble_login_clear_cookies dispatch arm + LoginWindowSeam.clearCookies (D-08)"
  - phase: 34.4.1-03
    provides: "loginWindowSeam.ts seam interface, watchForLogin()/finishLogin() rewired onto it"
  - phase: 34.4.1-04
    provides: "revealPost seam path, humble_reveal_post's on_navigation exfil interception"
  - phase: 34.4.1-09
    provides: "oauthLoginFlowRegistration.ts, the four OAuth runners genuinely wired"
provides:
  - "humbleDisconnect's declared 34.4 D-05 partial fully closed with a domain-scoped, hidden-window cookie clear"
  - "Electron-reach ledger regenerated by measurement with both this phase's registration modules as entry points"
  - "Extended T-34.1-27 label-discipline coverage in childWindows.test.ts, cross-linked to the Rust-side tests"
  - "Both Discretion sweeps (stale isTauri() guard, bare/aliased import) run against the phase's complete diff with recorded evidence"
affects: [34.4.1-07, 34.4.1-08, 34.5]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Guarded-step wipe array branches on getLoginWindowSeam() === null (Electron five-step session.fromPartition path, verbatim) vs seam-installed (single 'clearHumbleCookies' step: open hidden window -> clearCookies(label, 'humblebundle.com') -> close in finally)"
    - "electronReachLedger.test.ts's 'measurement wins over prediction' discipline reapplied a third time: temporary print statement, captured before/after counts, removed before commit"

key-files:
  created: []
  modified:
    - src/backend/humble/user.ts
    - src/backend/humble/__tests__/user.test.ts
    - src/backend/sidecar/__tests__/electronReachLedger.test.ts
    - src/preload/__tests__/childWindows.test.ts

key-decisions:
  - "disconnect()'s seam-path cookie clear opens a HIDDEN window (visible: false) on HUMBLE_BASE_URL solely to obtain a live webview handle -- Tauri has no windowless jar access -- and closes it unconditionally in a finally, so a rejecting clearCookies or a rejecting close() can never leak the window or throw out of disconnect()"
  - "The domain scope passed to clearCookies is exactly 'humblebundle.com' (never a blanket clear_all_browsing_data), matching the Rust arm's own suffix-match filter and threat T-34.4.1-30's mitigation"
  - "electronReachLedger.test.ts's baseline stayed at 34 electron-importing modules after adding both new entry points -- the measurement agreed with the prediction that humble/user.ts and humble/adapter.ts (already 34.4-baselined) are the only reached modules; only visitedFiles.size grew (219 -> 222), and the reachability floor was raised accordingly (200 -> 220)"
  - "Sweep A found zero stale isTauri() guards: the two new isTauri() checks in WebView/index.tsx and useTauriOAuthLogin.ts are deliberate, correctly-scoped new guards (their own comments explicitly reference and avoid 34.4 gate item 2's failure mode); every isTauri() hit in preload/api/misc.ts is the pre-existing correct transport-routing pattern"
  - "Sweep C's four anti-pattern greps each returned exactly the expected doc-comment-only hits (cookies_for_url, clear_all_browsing_data, navigator.clipboard all appear only in comments explicitly warning against them) and the one real on_navigation usage is confirmed to be exactly plan 04's reveal-exfil interception, not a relay"

requirements-completed: [REQ-34.4.1-06, REQ-34.4.1-09, REQ-34.4.1-11]

# Metrics
duration: 70min
completed: 2026-07-29
---

# Phase 34.4.1 Plan 06: Close humbleDisconnect's D-05 partial + phase-wide guardrail sweeps Summary

**Domain-scoped `clearCookies('humblebundle.com')` through a hidden Tauri window closes 34.4's declared disconnect partial; the electron-reach ledger was regenerated by measurement (34 modules, unchanged) with both of this phase's registration modules as entry points; and both Discretion sweeps (stale `isTauri()` guards, bare/aliased imports) ran clean against the phase's complete diff, with `npm start` and `pnpm tauri:dev` both confirmed compiling and running.**

## Performance

- **Duration:** ~70 min
- **Tasks:** 3
- **Files modified:** 4 (2 source/test pairs)

## Accomplishments

- `HumbleUser.disconnect()`'s Tauri seam path now opens a hidden window on `HUMBLE_BASE_URL`, calls `seam.clearCookies(label, 'humblebundle.com')`, logs only the deleted count, and closes the window unconditionally in a `finally` -- closing 34.4 D-05's declared partial (`.planning/IPC-PORT-INVENTORY.md`'s note) for real, not by re-declaration.
- Six new `disconnect()` test cases prove: credential-store-before-cookie-clear ordering, the exact `'humblebundle.com'` scope, survival of a rejecting `clearCookies`, survival of a rejecting `seam.close()`, survival of a rejecting `seam.open()`, and the untouched five-step Electron path when no seam is installed.
- `electronReachLedger.test.ts`'s `ENTRY_POINTS` now includes both `humbleLoginFlowRegistration.ts` (plan 02) and `oauthLoginFlowRegistration.ts` (plan 09), regenerated by an actual `computeElectronReach()` run (not hand-written from prose): 34 electron-importing modules before and after (unchanged -- the measurement agreed with the prediction), `visitedFiles.size` grew 219 -> 222. The reachability floor was raised from 200 to 220.
- `childWindows.test.ts` gained four new T-34.1-27 label-discipline cases (pairwise-unique labels across five generations, the same url never repeating a label across five calls, no label in a varied-url batch ever equalling `main`/`about`, and no label containing any substring of a `humblebundle.com` url's host) with a comment cross-linking them to the Rust-side `next_login_window_label()` tests in `main.rs`.
- Sweep A (stale `isTauri()` guard): every `isTauri()` hit in the phase's touched files was classified. All 6 ported channels (`humbleGetLoginUserAgent`, `humbleLoginNavigated`, `humbleReconnect`, `humbleRevealKey`, `humbleStartLogin`, `humbleStopLogin`) confirmed reachable from a real caller with no intervening early return.
- Sweep B (bare/aliased import build regression): `npm start` and `pnpm tauri:dev` both confirmed to compile and run to completion (Electron renderer dev server up on `localhost:5173`, app running with library data loaded; Tauri's Rust shell compiled, sidecar spawned and signalled READY).
- Sweep C anti-pattern greps: all four patterns (`cookies_for_url`, `clear_all_browsing_data`, `navigator.clipboard` in WebView, `on_navigation` outside the plan-04 reveal-exfil arm) confirmed clean, with results recorded below.
- Full green check: `npm run test:ci` (3094 passed / 1 failed -- the pre-existing baseline `rustInvokeChannel.test.ts` flake, confirmed by a second consecutive run to be exactly the baseline set and nothing more), `npx tsc --noEmit` clean, `cargo check` clean, `cargo test` 37/37 passed.

## Task Commits

Each task was committed atomically:

1. **Task 1: Domain-scoped cookie clear on humbleDisconnect** - `d5dd150c6` (feat)
2. **Task 2: Regenerate the electron-reach ledger and extend the label-safety suite** - `8458db8af` (test)
3. **Task 3: Run both Discretion sweeps and the full phase-wide green check** - no code changes; both sweeps found zero defects, so nothing to commit beyond the evidence recorded in this SUMMARY.

## Files Created/Modified

- `src/backend/humble/user.ts` - `disconnect()` gains a seam branch: Electron's original five-step `session.fromPartition` wipe is untouched; the Tauri seam path opens a hidden window, calls `clearCookies(label, 'humblebundle.com')`, logs the deleted count, and closes the window in a `finally`.
- `src/backend/humble/__tests__/user.test.ts` - 6 new disconnect-seam-path test cases (call order, exact domain scope, three independent rejection-survival cases, and the Electron-path regression check), nested inside the existing "login window seam path" describe block so the fake seam fixture is reused.
- `src/backend/sidecar/__tests__/electronReachLedger.test.ts` - Two new `ENTRY_POINTS` (`humbleLoginFlowRegistration.ts`, `oauthLoginFlowRegistration.ts`), a measured (not predicted) baseline-comment update documenting the 34/219 -> 34/222 before/after, a `requiredModules` comment explaining why no new anti-vacuity entry was needed (zero new electron edges), and the reachability floor raised 200 -> 220.
- `src/preload/__tests__/childWindows.test.ts` - 4 new T-34.1-27 label-discipline test cases plus a comment block cross-linking them to `main.rs`'s `#[cfg(test)] mod tests` for `next_login_window_label()`.

## Decisions Made

- The hidden-window-per-clear pattern mirrors `humblePostRequestViaSeam`'s existing on-demand-window shape (open, use, close in `finally`) rather than introducing a new lifecycle -- consistent with the phase's established pattern for one-shot seam operations that need a live webview handle but must never leave one idle.
- No new `requiredModules` anti-vacuity entry was added to `electronReachLedger.test.ts` because the measurement found zero new electron-importing modules from either of this phase's two new entry points -- documented explicitly with a comment rather than silently omitted, so a future reader does not mistake the omission for an oversight.
- See frontmatter `key-decisions` for the full list.

## Deviations from Plan

None - plan executed exactly as written. All three tasks' acceptance criteria were met without needing Rule 1-4 auto-fixes; both Discretion sweeps found zero defects to fix.

## Sweep A Evidence (stale `isTauri()` guard classification)

Enumerated via `git diff --name-only f58feec80...HEAD` (the phase's base commit, the last commit before `docs(state): record phase 34.4.1 context session`) against `src/*` and `src-tauri/*`.

| Hit | File | Classification |
|---|---|---|
| `import { isTauri } from '../../../preload/tauriTransport'` + `if (isTauri() && isLoginPathname(pathname))` / `if (isTauri())` | `src/frontend/screens/WebView/index.tsx:11,465,474` | (ii) Deliberate new guard -- comment at line 466 explicitly cites REQ-34.4.1-07/-08 and explains the old blanket "not available" message is now a lie for login routes; routes to `TauriLoginPanel` (Humble) or `WebviewUnavailablePanel` (store/wiki, D-05 deferral) |
| `import { isTauri } from '../../../preload/tauriTransport'` + `if (!isTauri() \|\| !isOAuthRunner(runner))` | `src/frontend/screens/WebView/useTauriOAuthLogin.ts:3,59` | (ii) Deliberate new guard -- doc comment explicitly says "No other gate exists (a stale premise in a guard's own comment is exactly how 34.4's gate item 2 shipped a registered-but-unreachable channel)" |
| `isTauri() ? tauriX() : xIpc()` (11 call sites) | `src/preload/api/misc.ts` | (i) Correct transport-routing guard -- same pattern as `preload/ipc.ts`/`preload/tauriTransport.ts`, pre-existing and untouched by this phase's feature logic |
| `(Task 3's hook guards on isTauri() first)` | `src/backend/sidecar/oauthLoginFlowRegistration.ts:25` | Comment only, not code -- describes the frontend hook's own guard (already classified above) |

**Zero (iii) stale guards found.** All 6 ported channels' call sites confirmed reachable with no intervening early return:

| Channel | Call site | Reachability |
|---|---|---|
| `humbleGetLoginUserAgent` | `WebView/index.tsx:194`, inside a `useEffect` gated only by `if (runner !== 'humble') return` | Hooks run unconditionally on mount (declared above the `if (!webviewPreloadPath)` platform-branch return at line 464); no `isTauri()` blocks it |
| `humbleLoginNavigated` | `WebView/index.tsx:370`, inside `onHumbleLoginNavigate` gated only by `if (runner === 'humble')` | Same -- wired to `did-navigate`/`did-navigate-in-page` webview listeners, unconditional on platform |
| `humbleReconnect` | `WebView/index.tsx:213`, ternary on `humble.expired` inside the same `runner === 'humble'`-gated effect | Reachable |
| `humbleRevealKey` | `Humble/Keys/components/HumbleClaimWizard/index.tsx:112`, inside `handleReveal()` gated only by a `busy` double-click guard | Reachable via user button click, no platform gate at all |
| `humbleStartLogin` | `WebView/index.tsx:214`, same effect as `humbleReconnect` | Reachable |
| `humbleStopLogin` | `WebView/index.tsx:228`, effect cleanup function | Reachable, runs unconditionally on unmount |

## Sweep B Evidence (bare/aliased import build regression)

1. `npm start` (Electron renderer dev server): compiled clean. Captured lines:
   - `build the electron main process successfully`
   - `build the electron preload files successfully`
   - `dev server running for the electron renderer process at: http://localhost:5173/`
   - App ran to `(...)[INFO]: [Backend]: Frontend Ready` and `Humble sync finished: gamekeys=26 fetched=6/6 ... ok=6`. (One pre-existing, out-of-scope error unrelated to this phase: `No handler registered for 'getCrossoverIndex'` -- not touched by this plan's diff.)
2. `pnpm tauri:dev`: compiled clean. Captured lines:
   - `Compiling gamelib-shell v0.7.0 ...` / `Finished \`dev\` profile [unoptimized + debuginfo] target(s) in 5.20s`
   - `[shell] sidecar process spawned OK`
   - `[shell] sidecar signalled READY (__GAMELIB_SIDECAR_READY__)`
3. Bare/aliased-import grep across this phase's changed frontend/preload files (`TauriLoginPanel.tsx`, `WebviewUnavailablePanel.tsx`, `WebView/index.tsx`, `loginRoutes.ts`, `useTauriOAuthLogin.ts`, `SteamSignOut.ts`, `preload/api/misc.ts`) found imports from `frontend/`, `common/`, and `backend/` prefixes only -- all three are registered Vite aliases in `electron.vite.config.ts`'s `srcAliases` (`['backend', 'frontend', 'common']`), and both dev-server runs above compiled them successfully. No `preload/` bare import found (would have been the 34.4-10-class regression) and no fix was needed.

## Sweep C Evidence (anti-pattern greps)

| Grep | Result | Detail |
|---|---|---|
| `grep -rc "cookies_for_url" src src-tauri/src` | 1 non-zero file, 0 real usages | `src/common/types/sidecarTransport.ts:254` -- doc comment: "Never `cookies_for_url()` -- see ... tauri-login-webview-cookies.md" |
| `grep -rc "clear_all_browsing_data" src src-tauri/src` | 1 non-zero file, 0 real usages | Same file, line 280 -- doc comment: "never `clear_all_browsing_data()`" |
| `grep -n "on_navigation" src-tauri/src/main.rs` | 5 hits, 1 real usage | Lines 489/555/952-953 are comments; the ONE real `.on_navigation(move |url| {...})` at line 1118 is confirmed to be exactly plan 34.4.1-04's reveal-exfil interception inside the `humble_reveal_post` arm -- it inspects `url.host_str()` against `REVEAL_EXFIL_HOST`, extracts the `data` query param, and returns `false` to cancel navigation. Not a relay. |
| `grep -rc "navigator.clipboard" src/frontend/screens/WebView` | 4 non-zero files, 0 real usages | `TauriLoginPanel.tsx:54` and `WebviewUnavailablePanel.tsx:29` are doc comments warning against it (per `navigator-clipboard-noops-under-tauri`); the other 2 hits are their own tests' doc comments |

## Full Green Check

- `npm run test:ci`: first run showed 2 failures (the baseline `rustInvokeChannel.test.ts` frame-leak plus a spuriously-timing-out `Humble/Keys/Waiting/index.test.tsx`). Both isolated re-runs (`npx jest src/frontend/screens/Humble/Keys/Waiting/__tests__/index.test.tsx` alone, and `npx jest src/backend/downloadmanager/__tests__/downloadqueue.test.ts` alone) passed cleanly. A second full `npm run test:ci` run reproduced exactly the documented 1-failure baseline (`rustInvokeChannel.test.ts` only) with 3094 passed -- confirming the second failure on the first run was the same class of pre-existing cross-test frame-leak flakiness the baseline already documents, not a regression introduced by this plan's diff. Total test count (3095) is baseline (3085) + 10 new tests added by this plan (6 in `user.test.ts` + 4 in `childWindows.test.ts`), exactly as expected.
- `npx tsc --noEmit`: clean, exit 0.
- `cd src-tauri && cargo check`: clean, `Finished` in 19.54s.
- `cd src-tauri && cargo test`: 37/37 passed, 0 failed.

## Issues Encountered

The first `npm run test:ci` run showed a second, non-baseline failure (`Humble/Keys/Waiting/index.test.tsx` timing out on a `rustInvoke` mock). Investigated per the deviation-rules scope boundary: isolated re-runs of that test and the known-flaky `downloadqueue.test.ts` both passed clean, and a second full-suite run reproduced exactly the 1-failure baseline. Concluded this is the same pre-existing cross-test frame-leak class documented in the baseline (shared `rustInvoke` mock state leaking across test files depending on execution order), not a regression from this plan's diff -- consistent with the baseline's own framing that `rustInvokeChannel.test.ts`'s failure is "out of scope" for the same underlying cause. No code change was made in response (nothing in this plan's diff touches the sidecar RPC mock machinery).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `humbleDisconnect`'s D-05 partial is fully closed -- plan 34.4.1-07 (which the plan's `<read_first>` notes owns verifying `.planning/IPC-PORT-INVENTORY.md`'s note) can now record the closure without carrying the partial forward again.
- The electron-reach ledger and its Phase 35 cutover work-list are current through this phase's complete diff (all 9 plans, waves 1-5).
- Both Discretion sweeps ran clean with recorded evidence -- no known live-gate risk from a stale guard or a broken bundler-only import going into the phase's live gate (`34.4.1-LIVE-GATE.md`).
- No blockers.

---
*Phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi*
*Completed: 2026-07-29*

## Self-Check: PASSED

- FOUND: src/backend/humble/user.ts
- FOUND: src/backend/humble/__tests__/user.test.ts
- FOUND: src/backend/sidecar/__tests__/electronReachLedger.test.ts
- FOUND: src/preload/__tests__/childWindows.test.ts
- FOUND: .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-06-SUMMARY.md
- FOUND: d5dd150c6 (Task 1 commit)
- FOUND: 8458db8af (Task 2 commit)
