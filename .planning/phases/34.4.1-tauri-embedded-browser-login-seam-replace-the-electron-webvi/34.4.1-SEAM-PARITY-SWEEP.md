# 34.4.1 Seam-Parity Sweep

Generated by `seam-parity-sweep.py` (34.4.1 gap cycle, plan 10 — REQ-34.4.1-11/REQ-34.4.1-GAP-04). Regenerate with `python3 seam-parity-sweep.py`; do not hand-edit this file — fix the script instead.

**Total findings: 12** (8 Axis A dual-branch sites, 1 supplementary Axis A finding(s) from the unguarded-session.fromPartition check, 3 Axis B safeStorage importer(s)). **5 classified SILENTLY-DROPPED.**

## Findings

| ID | Axis | Site | Electron capability | Tauri capability | Classification | Disposition |
|----|------|------|----------------------|-------------------|-----------------|--------------|
| S-01 | A | `src/backend/humble/adapter.ts:272` | humblePostRequestViaElectronNet(): returns keys ['data'], throws ['Error', 'HumbleTransportHttpError'] | humblePostRequestViaSeam(): returns keys ['data'], throws ['Error', 'HumbleTransportHttpError'] | DECLARED | correct — no action (identical downstream contract, id T-10-02 present) |
| S-02 | A | `src/backend/humble/user.ts:185` | returns: ['HumbleUser.getCsrfToken()', 'cookies[0].value'] | returns: ['HumbleUser.getCsrfToken()'] | DECLARED | correct — no action (T-34.4.1-18) |
| S-03 | A | `src/backend/humble/user.ts:281` | ses.setUserAgent(standardBrowserUserAgent()) on the persist:humble partition | seam.open(url, { userAgent: standardBrowserUserAgent() }) -- same UA string, different delivery API | DECLARED | correct — no action (D-01/D-02; UA parity verified by anchor count, not a capability drop) |
| S-04 | A | `src/backend/humble/user.ts:614` | configStore keys written: ['csrfToken'] | configStore keys written: ['csrfToken'] | DECLARED | correct — no action (identical sink keys) |
| S-05 | A | `src/backend/humble/user.ts:794` | wipeSteps: ['clearStorageData', 'clearCache', 'clearAuthCache', 'clearHostResolverCache', 'clearData']  (categories: ['authCache', 'cache', 'cookies', 'hostResolver', 'storage']) | wipeSteps: ['clearHumbleCookies']  (categories: ['cookies']) | SILENTLY-DROPPED | gap-cycle item (F-6) — dropped categories: ['authCache', 'cache', 'hostResolver', 'storage'] (see 34.4.1-LIVE-GATE.md) |
| S-06 | A | `src/backend/sidecar/humbleLoginFlowRegistration.ts:358` | n/a — inert unless GAMELIB_LOGIN_SEAM_SMOKE=1 (debug-only diagnostic hook, not a shipped capability) | opens a diagnostic window against example.com, waits ~2s, closes it -- proves the seam construction path, not a user-facing feature | DECLARED | correct — no action (env-gated diagnostic harness; no production capability on either branch to drop) |
| S-07 | A | `src/backend/sidecar/oauthLoginCapture.ts:157` | returns: ["Promise.resolve({ status: 'unsupported' })", "{ status: 'unsupported' })"] | returns: ['for (const event of events) {', 'new Promise<OAuthCaptureOutcome>((resolve) => {', 'pollInFlight = true', 'settled = true', '}'] | DECLARED | correct — no action (D-04) |
| S-08 | A | `src/backend/storeManagers/legendary/user.ts:107` | wipeSteps: ['clearStorageData', 'clearCache', 'clearAuthCache', 'clearHostResolverCache', 'clearData']  (categories: ['authCache', 'cache', 'cookies', 'hostResolver', 'storage']) | wipeSteps: ['clearEpicCookies']  (categories: ['cookies']) | SILENTLY-DROPPED | gap-cycle item (F-6's verbatim twin, per 34.4.1-LIVE-GATE.md gap-cycle scoping) — dropped categories: ['authCache', 'cache', 'hostResolver', 'storage'] (see 34.4.1-LIVE-GATE.md) |
| S-09 | A | `src/backend/humble/user.ts:734 (unguarded session.fromPartition, new finding)` | session.fromPartition(...) reads/writes the real Electron partition unconditionally | hits electronStub.ts's session.fromPartition() stub (returns {}), caught by the surrounding try/catch and logged as non-fatal — the capability silently no-ops | SILENTLY-DROPPED | gap-cycle item (NEW — not in 34.4.1-10-PLAN.md's <interfaces> expected-site list; found only by this supplementary guard check, not the getLoginWindowSeam() enumeration) |
| S-10 | B | `src/backend/humble/user.ts` | safeStorage.isEncryptionAvailable() real Keychain probe; encryptString/decryptString real Keychain calls | isEncryptionAvailable() hardcoded false; encryptString() throws; decryptString() throws | SILENTLY-DROPPED | gap-cycle item (see 34.4.1-LIVE-GATE.md F-1/F-1b; plan 34.4.1-13) |
| S-11 | B | `src/backend/steamgrid/secureKey.ts` | safeStorage.isEncryptionAvailable() real Keychain probe; encryptString/decryptString real Keychain calls | isEncryptionAvailable() hardcoded false; encryptString() throws; decryptString() throws | SILENTLY-DROPPED | gap-cycle item (see 34.4.1-LIVE-GATE.md F-1/F-1b; plan 34.4.1-13) |
| S-12 | B | `src/backend/storeManagers/steam/tokenStore.ts` | safeStorage.isEncryptionAvailable() real Keychain probe; encryptString/decryptString real Keychain calls | isEncryptionAvailable() hardcoded false; encryptString() throws; decryptString() throws | DECLARED | correct — no action (D-09; module doc comment documents the bypass/alternate seam) |

## steamgrid/secureKey.ts (F-1b) sidecar-reachability

`steamgrid/ipc_handler.ts` (the only importer of `secureKey.ts`'s `encryptApiKey`/`decryptApiKey`) is imported by: ['src/backend/main.ts']. `src/backend/sidecar/handlers.ts` (the sidecar's registration-module import list) does NOT mention `steamgrid`. `src/backend/sidecar/__tests__/electronReachLedger.test.ts`'s committed `BASELINE_ELECTRON_REACHING_MODULES` (a 34-entry, independently-verified transitive-reach measurement from all sidecar entry points) does NOT contain any `steamgrid/*` path. **Conclusion: `steamgrid/secureKey.ts` is NOT reachable from the sidecar's curated import graph today** — it is only reached from `src/backend/main.ts` (Electron's own entry point, never imported by the sidecar's `bootstrap.ts`/`handlers.ts` chain). F-1b therefore stays dormant, not live, under the current Tauri build; it becomes live the moment any future plan wires `steamgrid` into a sidecar registration module without first migrating it off direct `safeStorage` (this is exactly D-GAP-02's stated reason for planning F-1/F-1b's keyring-slot design together, not sequentially).

## Enumeration completeness

Axis A live walk found 8 `getLoginWindowSeam()` call site(s) (expected >= 8 from 34.4.1-10-PLAN.md `<interfaces>`).
Axis B live walk found 3 non-test `safeStorage` importer(s) (expected >= 3 from `<interfaces>`).
