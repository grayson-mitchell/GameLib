# 34.4.1 Seam-Parity Sweep

Generated by `seam-parity-sweep.py` (34.4.1 gap cycle, plan 10 — REQ-34.4.1-11/REQ-34.4.1-GAP-04). Regenerate with `python3 seam-parity-sweep.py`; do not hand-edit this file — fix the script instead.

**Total findings: 13** (10 Axis A dual-branch sites, 0 supplementary Axis A finding(s) from the unguarded-session.fromPartition check, 3 Axis B safeStorage importer(s)). **3 classified SILENTLY-DROPPED.**

## Findings

| ID | Axis | Site | Electron capability | Tauri capability | Classification | Disposition |
|----|------|------|----------------------|-------------------|-----------------|--------------|
| S-01 | A | `src/backend/humble/adapter.ts:275` | humblePostRequestViaElectronNet(): returns keys ['data'], throws ['Error', 'HumbleTransportHttpError'] | humblePostRequestViaSeam(): returns keys ['data'], throws ['Error', 'HumbleTransportHttpError'] | DECLARED | correct — no action (identical downstream contract, id T-10-02 present) |
| S-02 | A | `src/backend/humble/library.ts:1211` | logs the fixed 'electron-net transport' label on the reveal-POST diagnostic line | logs 'login-window seam transport' -- a LABEL choice derived from the SAME getLoginWindowSeam() condition humblePostRequest (adapter.ts) branches its REAL dispatch on; this line does not itself dispatch anything | DECLARED | correct — no action (Phase 34.4.1 gap-cycle plan 17, F-8: a diagnostic label choice, not a dropped capability -- the reveal POST's actual transport dispatch lives in adapter.ts's own getLoginWindowSeam() branch, unaffected by this line) |
| S-03 | A | `src/backend/humble/user.ts:178` | returns: ['HumbleUser.getCsrfToken()', 'cookies[0].value'] | returns: ['HumbleUser.getCsrfToken()'] | DECLARED | correct — no action (T-34.4.1-18) |
| S-04 | A | `src/backend/humble/user.ts:274` | ses.setUserAgent(standardBrowserUserAgent()) on the persist:humble partition | seam.open(url, { userAgent: standardBrowserUserAgent() }) -- same UA string, different delivery API | DECLARED | correct — no action (D-01/D-02; UA parity verified by anchor count, not a capability drop) |
| S-05 | A | `src/backend/humble/user.ts:740` | configStore keys written: ['csrfToken'] | configStore keys written: ['csrfToken'] | DECLARED | correct — no action (identical sink keys) |
| S-06 | A | `src/backend/humble/user.ts:873` | configStore keys written: ['csrfToken'] | configStore keys written: ['csrfToken'] | DECLARED | correct — no action (identical sink keys) |
| S-07 | A | `src/backend/humble/user.ts:1034` | wipeSteps: ['clearStorageData', 'clearCache', 'clearAuthCache', 'clearHostResolverCache', 'clearData']  (categories: ['authCache', 'cache', 'cookies', 'hostResolver', 'storage']) | wipeSteps: ['clearHumbleCookies', 'clearHumbleStorage']  (categories: ['cache', 'cookies', 'storage']) | SILENTLY-DROPPED | gap-cycle item (F-6) — dropped categories: ['authCache', 'hostResolver'] (see 34.4.1-LIVE-GATE.md) |
| S-08 | A | `src/backend/sidecar/humbleLoginFlowRegistration.ts:457` | n/a — inert unless GAMELIB_LOGIN_SEAM_SMOKE=1 (debug-only diagnostic hook, not a shipped capability) | opens a diagnostic window against example.com, waits ~2s, closes it -- proves the seam construction path, not a user-facing feature | DECLARED | correct — no action (env-gated diagnostic harness; no production capability on either branch to drop) |
| S-09 | A | `src/backend/sidecar/oauthLoginCapture.ts:195` | returns: ["Promise.resolve({ status: 'unsupported' })", "{ status: 'unsupported' })"] | returns: ['for (const event of events) {', 'new Promise<OAuthCaptureOutcome>((resolve) => {', 'pollInFlight = true', 'settled = true', '}'] | DECLARED | correct — no action (T-34.5-G6-11) |
| S-10 | A | `src/backend/storeManagers/legendary/user.ts:137` | wipeSteps: ['clearStorageData', 'clearCache', 'clearAuthCache', 'clearHostResolverCache', 'clearData']  (categories: ['authCache', 'cache', 'cookies', 'hostResolver', 'storage']) | wipeSteps: ['clearEpicCookies', 'clearEpicStorage']  (categories: ['cache', 'cookies', 'storage']) | SILENTLY-DROPPED | gap-cycle item (F-6's verbatim twin, per 34.4.1-LIVE-GATE.md gap-cycle scoping) — dropped categories: ['authCache', 'hostResolver'] (see 34.4.1-LIVE-GATE.md) |
| S-11 | B | `src/backend/humble/secretStore.ts` | safeStorage.isEncryptionAvailable() real Keychain probe; encryptString/decryptString real Keychain calls | isEncryptionAvailable() hardcoded false; encryptString() throws; decryptString() throws | DECLARED | correct — no action (T-34.4.1-56; module doc comment documents the bypass/alternate seam) |
| S-12 | B | `src/backend/steamgrid/secureKey.ts` | safeStorage.isEncryptionAvailable() real Keychain probe; encryptString/decryptString real Keychain calls | isEncryptionAvailable() hardcoded false; encryptString() throws; decryptString() throws | SILENTLY-DROPPED | gap-cycle item (see 34.4.1-LIVE-GATE.md F-1/F-1b; plan 34.4.1-13) |
| S-13 | B | `src/backend/storeManagers/steam/tokenStore.ts` | safeStorage.isEncryptionAvailable() real Keychain probe; encryptString/decryptString real Keychain calls | isEncryptionAvailable() hardcoded false; encryptString() throws; decryptString() throws | DECLARED | correct — no action (D-09; module doc comment documents the bypass/alternate seam) |

## steamgrid/secureKey.ts (F-1b) sidecar-reachability

`steamgrid/ipc_handler.ts` (the only importer of `secureKey.ts`'s `encryptApiKey`/`decryptApiKey`) is imported by: ['src/backend/main.ts']. `src/backend/sidecar/handlers.ts` (the sidecar's registration-module import list) does NOT mention `steamgrid`. `src/backend/sidecar/__tests__/electronReachLedger.test.ts`'s committed `BASELINE_ELECTRON_REACHING_MODULES` (a 34-entry, independently-verified transitive-reach measurement from all sidecar entry points) does NOT contain any `steamgrid/*` path. **Conclusion: `steamgrid/secureKey.ts` is NOT reachable from the sidecar's curated import graph today** — it is only reached from `src/backend/main.ts` (Electron's own entry point, never imported by the sidecar's `bootstrap.ts`/`handlers.ts` chain). F-1b therefore stays dormant, not live, under the current Tauri build; it becomes live the moment any future plan wires `steamgrid` into a sidecar registration module without first migrating it off direct `safeStorage` (this is exactly D-GAP-02's stated reason for planning F-1/F-1b's keyring-slot design together, not sequentially).

## Enumeration completeness

Axis A live walk found 10 `getLoginWindowSeam()` call site(s) (expected >= 8 from 34.4.1-10-PLAN.md `<interfaces>`).
Axis B live walk found 3 non-test `safeStorage` importer(s) (expected >= 3 from `<interfaces>`).
