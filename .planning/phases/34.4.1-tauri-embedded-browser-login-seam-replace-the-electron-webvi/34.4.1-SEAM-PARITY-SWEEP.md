# 34.4.1 Seam-Parity Sweep

Generated by `seam-parity-sweep.py` (34.4.1 gap cycle, plan 10 — REQ-34.4.1-11/REQ-34.4.1-GAP-04). Regenerate with `python3 seam-parity-sweep.py`; do not hand-edit this file — fix the script instead.

**Total findings: 4** (1 Axis A dual-branch sites, 0 supplementary Axis A finding(s) from the unguarded-session.fromPartition check, 3 Axis B safeStorage importer(s)). **1 classified SILENTLY-DROPPED.**

## Findings

| ID | Axis | Site | Electron capability | Tauri capability | Classification | Disposition |
|----|------|------|----------------------|-------------------|-----------------|--------------|
| S-01 | A | `src/backend/sidecar/humbleLoginFlowRegistration.ts:447` | n/a — inert unless GAMELIB_LOGIN_SEAM_SMOKE=1 (debug-only diagnostic hook, not a shipped capability) | opens a diagnostic window against example.com, waits ~2s, closes it -- proves the seam construction path, not a user-facing feature | DECLARED | correct — no action (env-gated diagnostic harness; no production capability on either branch to drop) |
| S-02 | B | `src/backend/humble/secretStore.ts` | safeStorage.isEncryptionAvailable() real Keychain probe; encryptString/decryptString real Keychain calls | isEncryptionAvailable() hardcoded false; encryptString() throws; decryptString() throws | DECLARED | correct — no action (T-34.4.1-56; module doc comment documents the bypass/alternate seam) |
| S-03 | B | `src/backend/steamgrid/secureKey.ts` | safeStorage.isEncryptionAvailable() real Keychain probe; encryptString/decryptString real Keychain calls | isEncryptionAvailable() hardcoded false; encryptString() throws; decryptString() throws | SILENTLY-DROPPED | gap-cycle item (see 34.4.1-LIVE-GATE.md F-1/F-1b; plan 34.4.1-13) |
| S-04 | B | `src/backend/storeManagers/steam/tokenStore.ts` | safeStorage.isEncryptionAvailable() real Keychain probe; encryptString/decryptString real Keychain calls | isEncryptionAvailable() hardcoded false; encryptString() throws; decryptString() throws | DECLARED | correct — no action (D-09; module doc comment documents the bypass/alternate seam) |

## steamgrid/secureKey.ts (F-1b) sidecar-reachability

`steamgrid/ipc_handler.ts` (the only importer of `secureKey.ts`'s `encryptApiKey`/`decryptApiKey`) is imported by: (none found). `src/backend/sidecar/handlers.ts` (the sidecar's registration-module import list) does NOT mention `steamgrid`. `src/backend/sidecar/__tests__/electronReachLedger.test.ts`'s committed `BASELINE_ELECTRON_REACHING_MODULES` (a 34-entry, independently-verified transitive-reach measurement from all sidecar entry points) does NOT contain any `steamgrid/*` path. **Conclusion: `steamgrid/secureKey.ts` is NOT reachable from the sidecar's curated import graph today** — it has no importer at all -- `src/backend/main.ts` (Electron's own entry point) was deleted outright by Phase 35 plan 35-14's Electron-cutover commit (5643c7583), and no other module imports it. F-1b therefore stays dormant, not live, under the current Tauri build; it becomes live the moment any future plan wires `steamgrid` into a sidecar registration module without first migrating it off direct `safeStorage` (this is exactly D-GAP-02's stated reason for planning F-1/F-1b's keyring-slot design together, not sequentially).

## Enumeration completeness

Axis A live walk found 1 `getLoginWindowSeam()` call site(s) (expected == ['src/backend/sidecar/humbleLoginFlowRegistration.ts:447'] -- INVERTED Phase 39 Plan 08, REQ-39-03 owns the other seven's deletion; see EXPECTED_AXIS_A_SURVIVOR_SET's own comment).
Axis B live walk found 3 non-test `safeStorage` importer(s) (expected >= 3 from `<interfaces>`).
