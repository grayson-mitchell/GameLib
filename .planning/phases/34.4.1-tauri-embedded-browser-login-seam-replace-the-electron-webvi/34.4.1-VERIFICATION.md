---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
verified: 2026-08-23T00:00:00Z
status: human_needed
score: "Gate run 4 (2026-08-23): 4 of 4 scoreable items PASS, including item 3(b) which was unscoreable in all three prior runs. D-29-07 and D-29-04 CLOSED live. Item 4 PASS. Item 2 UNSCOREABLE on macOS -- a defect in THIS contract, re-scoped to Windows/Linux. 4 items remain open."
record: 34.4.1-LIVE-GATE-RERUN-4.md

# ─────────────────────────────────────────────────────────────────────────────
# WHY THIS FILE DID NOT EXIST UNTIL 2026-08-23
#
# Phase 34.4.1 closed 2026-07-31 on a 4/4 blocking live gate, with 29/29 plans
# summarised and ROADMAP marked COMPLETE -- and `/gsd-verify-work` never ran, so no
# VERIFICATION.md was ever written.
#
# `gsd-sdk query audit-uat` reads THIS FILE. With it absent, the phase contributed
# ZERO to every cross-phase sweep for 23 days, while ten D-29-* findings sat in
# `deferred-items.md` (which has no frontmatter and is invisible to both the audit
# and the explorer rollup) with no todo files and, for two of them, no owner at all.
#
# A missing file is worse than a stale one: a stale file is at least in someone's
# enumeration list. Nothing enumerates a directory that has no VERIFICATION.md.
# ─────────────────────────────────────────────────────────────────────────────

audit_tool_note: >
  Three SILENT failure modes govern how this file must be written. All three are measured, not
  theoretical.

  (1) `status` MUST stay `human_needed`, never `gaps_found`. `sdk/dist/query/uat.js` admits a
  VERIFICATION.md when status is `human_needed` OR `gaps_found` (line 306), but
  `parseVerificationItems` only EMITS items when status === 'human_needed' (line 221) -- so
  `gaps_found` is admitted and then always yields ZERO items, and the phase disappears from the
  report entirely. Measured live on Phase 34.1: the flip dropped it from 10 open items to 0.

  (2) Resolved items must be MOVED to `human_verification_resolved`, never annotated in place. The
  tool counts EVERY entry in `human_verification` regardless of any `result:` field.

  (3) An EMPTY `human_verification: []` while status is still `human_needed` makes the tool scrape
  the document BODY and report prose headings as phantom open items. If this array ever empties,
  flip `status` in the same edit.

# ─────────────────────────────────────────────────────────────────────────────
# OPEN -- these are what `audit-uat` should report for 34.4.1. Was 6 before gate run 4; now 4.
# ─────────────────────────────────────────────────────────────────────────────
human_verification:
  - test: "D-29-05 -- the login window's provisional title. RE-SCOPED TO WINDOWS/LINUX 2026-08-23: run 4 established this is UNSCOREABLE ON macOS, where the login window is an AppKit sheet that structurally renders NO title bar (main.rs:1551, F-34.5-G6-16). Plan 33's fix sets an NSWindow title macOS never displays. macOS satisfies the anti-phishing intent via the in-page origin banner instead (observed live: banner shows the origin from the very start, len=28)."
    record: 34.4.1-LIVE-GATE-RERUN-4.md
    requirement: REQ-34.4.1-09
    blocker: "needs Windows or Linux hardware -- a platform where the login window HAS a title bar. Cannot be closed on macOS by any run."
  - test: "REQ-34.4.1-GAP-11 -- keyring_get returns a classified, bounded-timeout error instead of silently consuming the sidecar's whole 60s RPC budget. Its own body reads 'This box stays UNCHECKED -- live-only'; un-checked 2026-08-23 by plan 30."
    record: 34.4.1-LIVE-GATE-RERUN-4.md
    requirement: REQ-34.4.1-GAP-11
    blocker: "live-only; explicitly an OBSERVABILITY requirement, NOT a root-cause fix for F-9"
  - test: "D-29-02 -- post-login /api/v1/user/info returns a 232-byte HTML 404. Two candidates (path moved / interstitial answering) BOTH fit every offline observation; an unauthenticated probe cannot discriminate. Non-blocking: finishLogin gates on getGamekeys, never on getAccountIdentity."
    record: 34.4.1-LIVE-GATE-RERUN-4.md
    requirement: REQ-34.4.1-02
    blocker: "Run 4: REPRODUCED byte-identically (status=404, text/html, bodyLength=232) and confirmed NON-BLOCKING live -- login completed with 'login already accepted'. Which explanation holds (path moved vs interstitial) remains UNDETERMINED and is NOT rounded to either."
  - test: "D-29-06 / F-9 -- a generic RPC timeout fired live ('response for unknown/timed-out id=1575'). Co-occurrence with a cookie operation is UNDETERMINED and must NOT be rounded to 'no'. Capture the TERMINAL scrollback, not gamelib.log: shell eprintln! never reaches the log file."
    record: 34.4.1-LIVE-GATE-RERUN-4.md
    requirement: REQ-34.4.1-GAP-11
    blocker: "id=1575 carries no channel name; needs the originating request located in the same scrollback. Run 4: NOT OBSERVED -- no 'response for unknown/timed-out id=' in the terminal. Recorded as not-observed, NOT as absent."

# ─────────────────────────────────────────────────────────────────────────────
# RELOCATED -- owned by another phase. NOT resolved. A distinct key is used so a
# future reader cannot mistake "relocated" for "passed", and so the audit stops
# counting it here and starts counting it there.
# ─────────────────────────────────────────────────────────────────────────────
human_verification_relocated:
  - test: "Epic logout's cookie clear (clearEpicCookies) -- UNOBSERVED. Epic's logout calls the SAME arm Humble's disconnect proved fixed, but that is an inference from shared code, NOT a measurement. No document may call it verified on that basis."
    moved_to: "Phase 34.6"
    note: >
      D-29-08 named Phase 34.5, and that was already false: 34.5-26-SUMMARY.md:316 disclaims it,
      34.6 inherited only the Epic LOGIN and egsSync legs from the parked 34.7, 34.7 is ON HOLD, and
      clearEpicCookies appears in no phase folder except this one's. Re-homed to 34.6 on 2026-08-23
      as an EXPLICIT addition to its inherited list. Tracked at
      .planning/todos/pending/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md
      PENDING OPERATOR SIGN-OFF -- the owner was proposed by plan 34, not confirmed by a human.

# ─────────────────────────────────────────────────────────────────────────────
# RESOLVED -- moved OUT of human_verification per failure mode (2) above.
# ─────────────────────────────────────────────────────────────────────────────
human_verification_resolved:
  - test: "Live gate run 4, item 4 -- humbleRevealKey's real outcome and D-29-03's completion line (REQ-34.4.1-05)"
    result: >
      PASS 2026-08-23. 'Humble adapter: /humbler/redeemkey reveal succeeded keyPresent=true
      durationMs=2409 contentType=none' -- D-29-03's line, which did not exist before this cycle.
      Real key returned and displayed; cache state UNREVEALED -> REVEALED; transport label
      'login-window seam transport' (F-8 no regression). Redaction PASS: the key value is absent
      from gamelib.log AND the terminal, with no keyLength= side channel -- verified with a POSITIVE
      CONTROL first (the check was proven able to find the value in a synthetic leaked string), and
      the value was never printed. Run 3's item 4 rested entirely on the operator's screen because
      the log went silent on success; this run scored it from the log.
  - test: "Live gate run 4, item 3(b) -- the cookie clear's domain scoping (D-29-07, REQ-34.4.1-GAP-05)"
    result: >
      PASS 2026-08-23, NON-VACUOUSLY, the first time in four runs. before(total=76, matched=37) --
      total != matched, so 39 foreign cookies genuinely existed to be spared; after(total=39,
      matched=0), deleted=37, survivingNonHumble=39, and 76-37=39 reconciles. GOG remained connected
      after the disconnect (isLoggedIn true, userData intact, auth.json 478B), operator-confirmed
      visually -- identity evidence, not just a tally. Unstriking precondition 6 is what made the
      item scoreable; run 3 had 34==34 and no possible pass. D-29-07 CLOSED.
  - test: "Live gate run 4, items 1 and 3(a)/3(c) -- login from scratch, disconnect, fresh re-login"
    result: >
      PASS 2026-08-23. Item 1: keyring-backed store (session len=208, csrf len=29), no plaintext
      sessionCookie, encryptionDegraded false, and Manage Accounts self-updated with NO navigate-away
      -- D-29-01 live-confirmed. 3(a): matched-after=0, 37 deleted, storage cleared, both keyring
      slots deleted. 3(c): operator got Humble's login form, not a signed-in page.
  - test: "D-29-04 -- an empty document title is applied to the login window unguarded"
    result: >
      PASS 2026-08-23, live and machine-verified. Terminal shows applied len=22 -> SKIPPED len=0
      (empty document title; previous title retained) -> applied len=42, with 'applied len=0' count
      = 0. This is run 3's exact 22->0->42 sequence, now guarded. D-29-04 CLOSED.
  - test: "Original 4-item blocking live gate (login from scratch / survives relaunch / disconnect / revealKey)"
    result: "PASS 4/4 on the third attempt, 2026-07-31 (run 1 FAIL 2/4 -> run 2 FAIL 3/4 -> run 3 PASS 4/4). Record: 34.4.1-LIVE-GATE-RERUN-3.md. NOTE: item 3's domain-scoping HALF was vacuous -- see the open 3(b) entry above."
  - test: "D-29-01 -- Manage Accounts does not self-refresh after a successful sign-in"
    result: "CLOSED 2026-08-23 on SOURCE evidence. HumbleLoginSurface.tsx:58-67 describes the symptom in the past tense as the rationale for the design that replaced it; the success branch runs 'await humble.login(result); onDone()'. Fixed by Phase 34.4.2 (F-34.4.2-19) + quick task 260808-gl6. Live re-confirmation is folded into run 4 item 1."
  - test: "D-29-03 -- a SUCCESSFUL humbleRevealKey logged nothing"
    result: "CLOSED 2026-08-23 (plan 32). One INFO line on the success path; two tests, each RED-proven. Live half is the open item 4 above."
  - test: "D-29-09 -- REQUIREMENTS checkboxes contradicting their own riders"
    result: "CLOSED 2026-08-23 (plan 30). Real extent was ONE requirement (REQ-34.4.1-GAP-11), not many; two apparent others were false positives from the gap-cycle preamble. Sweep proven to discriminate in both directions."
  - test: "D-29-10 -- the sweep's anti-vacuity guard was itself failing"
    result: "CLOSED AT FILING, per its own text. Recorded 2026-08-23 so no future cycle re-plans it."
  - test: "NEW-01 -- seam-parity-sweep.py was RED on main for 23 days"
    result: "CLOSED 2026-08-23 (plan 31). Line drift, not an unclassified site: all three SITE_PROFILES hints and 7 of 8 floor entries had drifted. Renumbered by enclosing function. Proven to still fail in three states."
  - test: "D-29-04 / D-29-05 -- empty-title application and the 'Tauri app' flash"
    result: "SOURCE half CLOSED 2026-08-23 (plan 33), with four red-proofs. An existing WR-07 gate was AMENDED because it banned '.title(' outright and so could not distinguish the prohibited hard-coded title from the correct origin-derived one. Live half is the open item 2 above."

overrides_applied: 0
live_uat_sessions:
  - date: 2026-07-30
    scope: "blocking gate, all 4 items"
    outcome: "FAIL 2/4 -- F-1 (plaintext session), F-6 (disconnect does not disconnect)"
  - date: 2026-07-31
    scope: "gap cycle 1 re-run, items 1+3 in full, 2+4 regression"
    outcome: "FAIL 3/4 -- F-1 CLOSED live-proven; item 3 FAIL, cookie delete silently deleted nothing"
  - date: 2026-07-31
    scope: "gap cycle 2 re-run, all 4 items"
    outcome: "PASS 4/4 -- phase closed. Item 3(b)'s domain-scoping half was VACUOUS, not passing."
  - date: null
    scope: "gap cycle 3 run 4 -- item 3(b) with precondition 6 UNSTRUCK, plus items 1/2/4 and observations 5(a)/5(b)"
    outcome: "NOT YET RUN -- contract written with empty slots 2026-08-23"
---

# Phase 34.4.1 — Verification

## Goal

Replace the Electron-only embedded-browser login path (`<webview partition="persist:…">` plus
`session.fromPartition()` cookie capture) with something that works under Tauri, and port the six
browser-auth channels carved out of Phase 34.4.

## Requirements

All 26 (`REQ-34.4.1-01..13`, `REQ-34.4.1-GAP-01..13`) carry evidence. **25 are checked; one —
`REQ-34.4.1-GAP-11` — is deliberately `[ ]`**, because its own body says *"This box stays UNCHECKED
— live-only"* and no live run has observed it.

Swept **by hand**, with a control, because `plan-phase`'s gap analysis is not phase-scoped and is
blind to every decimal-phase REQ ID — and this phase is doubly decimal (`N.M.P`), so every ✓/✗ the
tool prints for it is unreliable.

## What this phase's history should be read for

**A fully green test suite is not evidence of a live-observable claim here.** The suite was green —
3279/3279, then 3387/3387 — for all three prior gate runs **while F-1 and both of F-6's defects were
live**. Every blocking defect in this phase was found by a human driving the UI; none by automation.

That is why six items remain open above rather than being closed on source evidence, even where the
source evidence is strong.

## The one that is not like the others

Item 3(b) is open **not because it failed, but because it was never scoreable.** Run 3's contract
struck the planted non-Humble cookie as moot, then required an outcome only that cookie could
produce. `survivingNonHumble=0` against `before(total=34, matched=34)` was arithmetically forced.

**The contract was the defect, not the operator and not the code.** Run 4 unstrikes precondition 6.
