---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 07
type: execute
wave: 5
depends_on: ["34.4.1-06"]
files_modified:
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
autonomous: true
requirements: [REQ-34.4.1-10, REQ-34.4.1-13]

must_haves:
  truths:
    - "D-03/D-10: all 6 channels are declared in one table with port kind and an HONEST proof level — unit-proven, live-gate-proven, or ported-with-a-declared-caveat. No reader may take 'wired and unit-proven' for 'seen working', the conflation that let G-30-02 be declared fixed twice while the live build hung."
    - "D-04: the phase's acceptable bad case is stated UP FRONT, not discovered at review — a working mechanism that unblocks 34.5 with Humble's login trio declared-degraded is a legitimate outcome, and the document says which outcome actually occurred."
    - "D-09/REQ-34.4.1-13: macOS is live-proven; Linux and Windows are declared per-platform by crate reading, naming the specific unverified surfaces (`cookies_for_url()`'s domain-match on WebKitGTK/WebView2, the UA fingerprint gap, `data_store_identifier`'s macOS-14+ gating) so the omission reads as a decision."
    - "SEAM.md's Incremental-Port Checklist steps 5/6 are closed: the browser-auth rows move from §3's deferred backlog into §1, and Load-Bearing Invariant B is left byte-unchanged."
    - "IPC-PORT-INVENTORY.md's Phase 34.4.1 section (L77-86) and the `humbleDisconnect` partial note (L73-75) are VERIFIED INTACT, not re-edited — research confirmed both are already correct."
  artifacts:
    - path: ".planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md"
      provides: "The 6-row declared list with kinds, proof levels, riders and per-platform declarations"
      contains: "humbleRevealKey"
    - path: ".planning/phases/34.4.1-.../ported-channels-gate.py"
      provides: "Self-tested gate asserting the declared list's shape and the inventory's untouched state"
    - path: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      provides: "§1 subsection for the login seam; §3 backlog rows retired"
  key_links:
    - from: "34.4.1-PORTED-CHANNELS.md"
      to: "34.4.1-LIVE-GATE.md"
      via: "every live-proof cell references the gate item number that proves it"
      pattern: "item [1-4]"
---

<objective>
Declare what actually shipped: six channels, their port kinds, their honest proof levels, their
accepted riders, and what remains unverified on Linux and Windows — then close SEAM.md's checklist
steps and prove the inventory was left alone.

Purpose: this is the artifact a future reader trusts instead of re-deriving. Its value is entirely
in its honesty, so every claim must trace to something a prior SUMMARY actually records.

Output: `34.4.1-PORTED-CHANNELS.md`, a self-tested gate script, and a two-hunk SEAM.md edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-PORTED-CHANNELS.md
@.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
@.planning/IPC-PORT-INVENTORY.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-01-SUMMARY.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-02-SUMMARY.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-03-SUMMARY.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-04-SUMMARY.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-05-SUMMARY.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 34.4.1-PORTED-CHANNELS.md</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md</files>
  <read_first>
    - .planning/phases/34.4-.../34.4-PORTED-CHANNELS.md (the document to mirror — five-column table, per-row proof levels, per-row riders, and its own "if the measurement disagrees with the prediction, the measurement wins" line)
    - all six 34.4.1-NN-SUMMARY.md files listed in this plan's context (every proof level must trace to what a SUMMARY actually records — never to what a plan intended)
    - .planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md (for the item numbers to forward-reference)
    - .claude/skills/spike-findings-gamelib/references/tauri-login-webview-cookies.md § "Constraints" (the exact per-platform unverified surfaces to name)
  </read_first>
  <action>
    Create `34.4.1-PORTED-CHANNELS.md` mirroring `34.4-PORTED-CHANNELS.md`'s structure, with one row
    per channel: `humbleStartLogin`, `humbleReconnect`, `humbleGetLoginUserAgent`, `humbleRevealKey`
    (all `sidecar invoke` / handle) and `humbleStopLogin`, `humbleLoginNavigated` (both
    `sidecar send`). Columns: channel, port kind, registration module, proof level, riders.

    Proof levels must be honest and drawn from the SUMMARYs:
      - `unit` where only a jest/cargo test covers it,
      - `unit + LIVE (item N)` where a live-gate item proves it — written as a FORWARD REFERENCE
        until plan 34.4.1-08 records the actual result, exactly as 34.4-09 did,
      - `ported, declared <caveat>` where something is knowingly degraded.
    Per-row riders to state explicitly:
      - `humbleGetLoginUserAgent` is a pure function with zero browser dependency — recorded so the
        phase is not mis-scored as "0 of 6" in D-03's bad case.
      - `humbleRevealKey` carries D-07's time-boxed attempt and the possibility of a declared 403.
      - `humbleStopLogin`/`humbleLoginNavigated` are send-kind and therefore structurally silent on
        failure; say which test proves their registration kind.

    Include these sections:
      1. `## What shipped` — the mechanism in three sentences: Rust arms driven by the sidecar
         (D-02 resolved), one runner-agnostic window, navigation observation via `on_page_load`.
      2. `## The outcome, stated plainly` — name D-04's acceptable bad case up front and then say
         which case actually occurred, with evidence. If the cookie read worked, say the bad case did
         not materialize and why that is now known rather than hoped.
      3. `## Per-platform status (D-09 / REQ-34.4.1-13)` — macOS: live-proven (pending/actual gate
         result). Linux (WebKitGTK) and Windows (WebView2): verified by reading `tauri` 2.11.5 /
         `wry` 0.55.1 sources and DECLARED, naming the three specific unverified surfaces —
         `cookies_for_url()`'s domain-match correctness (the macOS defect is unverified elsewhere),
         the UA fingerprint gap, and `data_store_identifier`'s macOS-14+/iOS-17+ gating. Also record
         the platform-independent proof that DOES exist: `classifyCookieRead`'s truth table has no
         platform branch, so no platform can silently poll on a dead API.
      4. `## Obligations closed and opened` — closes 34.4 D-05's `humbleDisconnect` partial (D-08);
         records that 34.4 D-09's "there is no Tauri path to create a Humble session" precondition is
         PENDING STRIKE, to be struck by plan 34.4.1-08 once gate item 1 passes; names what Phase
         34.5 inherits (the navigation-observation seam, unchanged) and what it still must port
         (`login`, `authGOG`, `authAmazon`, `authZoom`).
      5. `## Deferred, unchanged` — in-app store/wiki browsing (D-05), the remaining
         `Electron.WebviewTag` references in `WebviewControls`/`Sidebar` (Phase 35), `cargo check`/
         `cargo test` in CI.
  </action>
  <acceptance_criteria>
    - The file exists with exactly 6 channel rows; `grep -c "^| humble" 34.4.1-PORTED-CHANNELS.md` returns 6.
    - All five required section headings are present.
    - Every proof-level cell is one of `unit`, `unit + LIVE (item N)`, or begins with `ported, declared`.
    - The three per-platform unverified surfaces are each named by string.
    - No claim in the file lacks a traceable source: the SUMMARY lists, per row, which SUMMARY the proof level came from.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md && grep -c "^| \`humble" .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md</automated>
  </verify>
  <done>Six rows, honest proof levels traced to SUMMARYs, the bad case named up front, and Linux/Windows declared with their specific unverified surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Close SEAM.md's checklist steps and verify the inventory is intact</name>
  <files>.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</files>
  <read_first>
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md — §1 (the per-phase ported subsections, ending with the Phase 34.4 one 34.4-09 added), §3 (the deferred backlog table, including the BrowserWindow/login row 34.4-09 re-targeted to "Phase 34.4.1"), the Incremental-Port Checklist steps 5/6, and Load-Bearing Invariants A and B
    - .planning/phases/34.4-.../34.4-09-SUMMARY.md (the precedent: a diff confined to exactly 2 hunks with Invariant B byte-unchanged)
    - .planning/IPC-PORT-INVENTORY.md L62-90 (READ ONLY)
  </read_first>
  <action>
    Edit SEAM.md in exactly two places:
      1. Add a new §1 subsection after the Phase 34.4 subsection: "Embedded-browser login seam
         (real, Phase 34.4.1)". Name the 6 channels, the five-plus-one new `dispatch_rust_channel`
         arms, the seam-injection shape (`loginWindowSeam.ts` + `humbleLoginFlowRegistration.ts`),
         and state that navigation observation — not the cookie read — is what Phase 34.5 inherits.
         Record that this phase deliberately reverses the 34.2/34.4 "zero new Rust arms" rider with a
         stated reason, exactly as 34.3 D-03 did for clipboard.
      2. Retire the §3 deferred row(s) covering the login/`BrowserWindow`/`session.fromPartition`
         seam, since they are now shipped. Where a row is only PARTLY retired, say what remains
         rather than deleting it.
    Do not touch Load-Bearing Invariant B (unported channels stay non-fatal) — the four OAuth auth
    channels still depend on it. Do not touch §"Accepted Constraints".

    Then VERIFY (do not edit) `.planning/IPC-PORT-INVENTORY.md`: its Phase 34.4.1 section at L77-86
    lists exactly the 6 channels this phase ported, and the `humbleDisconnect` partial note at
    L73-75 exists. Research confirmed both are already correct, so the obligation is to prove they
    were not disturbed: `git diff --stat .planning/IPC-PORT-INVENTORY.md` must be EMPTY at the end of
    this plan. If the note at L73-75 now reads as stale because REQ-34.4.1-06 closed the partial,
    record that as a follow-up observation in the SUMMARY — do NOT edit the file in this plan; that
    is plan 34.4.1-08's call once the gate proves the clear works.
  </action>
  <acceptance_criteria>
    - `git diff --stat .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` shows exactly 2 hunks.
    - `git diff .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md | grep -c "^-.*Invariant B"` returns 0 (Invariant B byte-unchanged).
    - `git diff --stat .planning/IPC-PORT-INVENTORY.md` is EMPTY.
    - The SUMMARY quotes the verified inventory lines (the 6-channel list and the partial note) to prove they were read, not assumed.
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat .planning/IPC-PORT-INVENTORY.md | wc -l | grep -q '^0$' && git diff --stat .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</automated>
  </verify>
  <done>SEAM.md's §1 records the login seam and §3 no longer defers it, in a two-hunk diff, with the inventory provably untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Write a self-tested gate for the declared list</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py</files>
  <read_first>
    - .planning/phases/34.4-.../ported-channels-gate.py (the script to mirror — its `check_*` function shape, its one-self-test-per-check rule, and the count-and-assert-equal-at-runtime discipline)
    - .planning/phases/34.4-.../34.4-09-SUMMARY.md (the two Rule-1 gate-logic bugs it found in its OWN gate before commit: a case-sensitive token mismatch and a whole-section-vs-whole-line extraction that produced a false positive against the real document)
  </read_first>
  <action>
    Write `ported-channels-gate.py` mirroring 34.4's, with one `check_*` function per assertion and
    exactly one self-test case per check, counted and asserted equal at runtime. Checks, at minimum:
      1. `34.4.1-PORTED-CHANNELS.md` exists and contains exactly 6 channel rows.
      2. The 6 channel names match the `IPC-PORT-INVENTORY.md` Phase 34.4.1 section exactly (set
         equality, both directions — a missing row and an extra row are different failures).
      3. Every row's port kind is `sidecar invoke` or `sidecar send`, and the four/two split matches
         the source-verified kinds.
      4. Every row's proof level matches one of the three permitted forms.
      5. `IPC-PORT-INVENTORY.md` is unmodified relative to HEAD (`git diff --stat` empty).
      6. SEAM.md contains a §1 subsection naming Phase 34.4.1 and no longer defers the login seam in §3.
    Two Rule-1 traps to avoid, both of which bit 34.4's gate: extract WHOLE LINES, not whole
    sections, when matching backticked channel names (the inventory's own prose names deferred
    channels while explaining they moved), and compare tokens case-insensitively where the documents
    legitimately differ in case. Run the gate against the REAL documents before committing and fix
    any false positive in the GATE, not in the document.

    Never use a bare `grep -c` on an uncommented file for any check — comment lines are
    self-invalidating (the repo-wide defect quick task 260726-q8f fixed). Strip or exclude comment
    lines explicitly.
  </action>
  <acceptance_criteria>
    - `python3 .planning/phases/34.4.1-.../ported-channels-gate.py` exits 0 against the real documents.
    - `python3 .planning/phases/34.4.1-.../ported-channels-gate.py --self-test` exits 0.
    - The self-test count equals the `check_*` count, asserted at runtime by the script itself.
    - The SUMMARY records any gate-logic bug found while running it against the real documents, and states that the fix was made in the gate rather than the document.
  </acceptance_criteria>
  <verify>
    <automated>python3 .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py && python3 .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py --self-test</automated>
  </verify>
  <done>The declared list is machine-checked against the inventory and SEAM.md, and the checker is itself self-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future implementer | A proof level read as stronger than it is causes a future phase to skip verification it needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.1-35 | Repudiation | inflated proof levels | mitigate | Every proof cell traces to a named SUMMARY; live cells are explicit forward references until plan 34.4.1-08 records the real result. This is the G-30-02 lesson: "wired and unit-proven" was twice read as "seen working". |
| T-34.4.1-36 | Tampering | silent edits to the inventory | mitigate | `git diff --stat .planning/IPC-PORT-INVENTORY.md` must be empty, asserted by both the plan's verify and the gate script's check 5. |
| T-34.4.1-37 | Repudiation | an undeclared platform gap | mitigate | Linux and Windows are declared per-platform with three named unverified surfaces, so the omission reads as a decision rather than an oversight (D-09). |
| T-34.4.1-38 | Tampering | a gate that passes vacuously | mitigate | One self-test per check, counted and asserted equal at runtime; whole-line extraction and comment stripping instead of a bare `grep -c`. |
| T-34.4.1-SC | Tampering | package installs | accept | Documentation-only plan; zero packages. |
</threat_model>

<verification>
- `python3 ported-channels-gate.py` and `--self-test` both exit 0.
- `git diff --stat .planning/IPC-PORT-INVENTORY.md` empty; SEAM.md diff is exactly 2 hunks.
- `npm run test:ci` still green (no source touched by this plan).
</verification>

<success_criteria>
A future reader can learn from one table exactly which of the six channels work, how strongly each
is proven, what is degraded, what is unverified on which platform, and what Phase 34.5 inherits —
with a self-tested script that keeps the table honest against the inventory.
</success_criteria>

<output>
Create `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-07-SUMMARY.md` when done
</output>
