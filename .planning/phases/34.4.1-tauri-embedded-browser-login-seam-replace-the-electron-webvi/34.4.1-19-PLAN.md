---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 19
type: execute
wave: 9
depends_on: ["34.4.1-18"]
gap_closure: true
files_modified:
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-34.4.1-10, REQ-34.4.1-13]
must_haves:
  truths:
    - "The gap cycle's changes are declared before the gate runs, not after it"
    - "No proof level is upgraded on the strength of unit tests alone"
    - "Every record update gated on item 3 passing is still ungated and untouched"
    - "The new REQ-34.4.1-GAP IDs exist and name the existing requirements they extend"
  artifacts:
    - path: ".planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md"
      provides: "Declared state of the 7 channels after the gap cycle, with honest forward references"
      contains: "unit + LIVE (item"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-34.4.1-GAP-01..06 minted, each naming what it extends"
      contains: "REQ-34.4.1-GAP-"
  key_links:
    - from: ".planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md"
      to: "ported-channels-gate.py"
      via: "controlled proof-level vocabulary"
      pattern: "unit \\+ LIVE|ported, declared"
---

<objective>
Declare what the gap cycle changed, BEFORE the gate re-runs — and change nothing whose truth
depends on the gate's outcome.

Purpose: this phase's own discipline, established by plans 07 and 08. Plan 07 declared; plan 08
resolved. This plan is the second declaration pass, and the plan-20 gate is the second resolution.
The failure mode it guards against is named in the gate itself: *"a declared-partial marked closed
on the strength of unit tests, while the live behavior is broken."* That is precisely how F-6
survived a fully-green suite.

Output: `34.4.1-PORTED-CHANNELS.md` describing the post-gap-cycle state with honest forward
references; the new `REQ-34.4.1-GAP-*` IDs minted against `REQUIREMENTS.md`; `ROADMAP.md`'s plan
list current.

**What this plan MUST NOT do, restated because it is the whole point:**
- It must not update `IPC-PORT-INVENTORY.md` L73-75's `humbleDisconnect` declared-partial note.
  That edit is gated on gate item 3 actually passing. `ported-channels-gate.py` check 5 asserts
  the file is unmodified, and that check must keep passing at the end of this plan.
- It must not un-strike `34.4.1-PORTED-CHANNELS.md`'s "34.4 D-05's `humbleDisconnect` partial is
  CLOSED" entry. It is STRUCK and REOPENED and may only be un-struck once item 3 passes live.
- It must not upgrade any proof-level cell to claim a live result that has not been observed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md

<interfaces>
**The controlled proof-level vocabulary, enforced by `ported-channels-gate.py:71`.** Exactly three
forms are permitted in the proof-level cell, and prose is not one of them:
```
unit
unit + LIVE (item N)
ported, declared ...
```
The gate's other binding assertions: exactly 7 channel rows (6 `humble*` + 1
`oauthCaptureLogin`); exactly `humbleStopLogin` + `humbleLoginNavigated` carry kind
`sidecar send` and the other four `sidecar invoke`; `oauthCaptureLogin` must carry
`(new, not an inventory row)` and its proof level must be exactly `unit`, never `unit + LIVE`;
the 6 `humble*` names must set-match `IPC-PORT-INVENTORY.md`'s Phase 34.4.1 section;
`IPC-PORT-INVENTORY.md` must be unmodified relative to HEAD; and SEAM.md's §1 must name Phase
34.4.1 while §3 no longer carries the stale deferral phrasing.

The rows whose live claims this gap cycle INVALIDATES and which must return to forward
references, because the code underneath them changed:
- `humbleStartLogin` — currently `unit + LIVE (item 1)` describing a run where the session landed
  in plaintext. The mechanism claim survives; the storage changed, so item 1 must be re-observed.
- `humbleReconnect` — currently `unit + LIVE (item 1)`, resolved by item 2's persistence proof.
  The store's contents changed (the secret moved to the keyring), so item 2 must be re-observed.
- `humbleRevealKey` — currently `unit + LIVE (item 4)` and genuinely PROVEN. Item 4 re-runs only
  to confirm no regression; this row's claim is not withdrawn, but the transport LABEL it cites
  changed in plan 17 and the rider must say so.

New REQ IDs to mint, each naming the existing requirement it extends:
```
REQ-34.4.1-GAP-01  multi-slot keyring allowlist              extends REQ-28-02/-03 (TokenStore seam)
REQ-34.4.1-GAP-02  Humble session encrypted at rest, Tauri   extends REQ-34.4.1-02
REQ-34.4.1-GAP-03  origin-scoped storage-clear capability    extends REQ-34.4.1-06
REQ-34.4.1-GAP-04  seam-parity sweep + regression guard      extends REQ-34.4.1-11
REQ-34.4.1-GAP-05  disconnect domain-scope observability     extends REQ-34.4.1-06 (closes F-5)
REQ-34.4.1-GAP-06  SteamGridDB key at rest                   extends REQ-34.4.1-GAP-01 (F-1b)
```
Reopened existing IDs: REQ-34.4.1-02, -05, -06, -12.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the gap cycle in 34.4.1-PORTED-CHANNELS.md, with proof levels no stronger than the evidence</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md</files>
  <read_first>
    - `.planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md` in full — its stated discipline
      (*"If the measurement disagrees with the prediction, the measurement wins"*), the
      strike-don't-delete convention, the "GATE RESULT — 2026-07-30" block, the struck/reopened
      D-05 entry, and the `## Accepted residuals` section.
    - `.planning/phases/34.4.1-.../ported-channels-gate.py` — every assertion, so the edit is made
      to satisfy the gate honestly rather than discovered to violate it afterwards.
    - The summaries of plans 10 through 18 — this document describes what SHIPPED, verified row by
      row against each plan's SUMMARY, never against planning-time intent.
    - `.planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md` § "Record updates this result PERMITS and
      FORBIDS" — the table of what is gated on what.
  </read_first>
  <action>
    Add a dated gap-cycle section describing what plans 10-18 changed and what it means for each
    row. Follow the document's existing register: state what is proven, by what, and what is not.

    Reset the proof-level cells for the rows whose underlying behavior changed, back to
    `unit + LIVE (item N)` forward references — the same convention this document already uses for
    a pending gate. Do not invent a new vocabulary form; the gate permits exactly three and prose
    in that cell fails check 4. `humbleRevealKey`'s live claim from item 4 is NOT withdrawn (the
    reveal itself is unchanged), but its rider must record that plan 17 replaced the hardcoded
    transport label, so a future reader comparing logs across the two gate runs is not confused by
    a line that changed wording without changing meaning.

    Record in the prose sections, not as new table rows:
    - The multi-slot keyring allowlist and the two Humble slots (F-1's fix), including the
      explicit statement that D-GAP-01's rejected alternatives were rejected and why, so the
      design is not re-litigated by a future phase.
    - The new origin-scoped storage-clear capability (F-6's fix), and the two capability
      categories that remain DECLARED because they have no in-page equivalent
      (`clearAuthCache`, `clearHostResolverCache`) — this belongs in `## Accepted residuals`
      alongside T-34.4.1-47.
    - The census instrument (F-5's fix) and the fact that precondition 6's planted control cookie
      is now unnecessary because the evidence no longer depends on it.
    - The cross-phase edit to `storeManagers/legendary/user.ts`, forwarded as an obligation to
      Phase 34.5 in the same register T-34.4.1-44b already uses.
    - F-1b's disposition as plan 14 measured it.

    Do NOT un-strike the D-05 entry. Do NOT touch `IPC-PORT-INVENTORY.md`. Add a line beside the
    struck entry restating that it is gated on item 3, so a reader of this new section does not
    have to scroll back to learn why it is still struck.

    Keep the row count at exactly 7 and every kind cell unchanged — the new Rust arm is a seam
    capability, not a ported IPC channel, and adding a row for it would break check 2 and
    misrepresent the inventory.
  </action>
  <verify>
    <automated>cd .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi && python3 ported-channels-gate.py --self-test && python3 ported-channels-gate.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 ported-channels-gate.py` exits 0.
    - `python3 ported-channels-gate.py --self-test` exits 0.
    - `git diff --stat -- .planning/IPC-PORT-INVENTORY.md` is EMPTY.
    - The struck "34.4 D-05's `humbleDisconnect` partial is CLOSED" entry is still struck —
      `grep -c "~~\*\*34.4 D-05" 34.4.1-PORTED-CHANNELS.md` returns at least 1.
    - No proof-level cell claims a live item that has not been re-observed since the gap cycle:
      `humbleStartLogin` and `humbleReconnect` carry `unit + LIVE (item N)` as forward references
      with the pending status stated in their riders.
    - `oauthCaptureLogin`'s proof level is still exactly `unit`.
    - The document names F-1b's measured disposition and the Legendary cross-phase obligation.
  </acceptance_criteria>
  <done>The declared record describes the post-gap-cycle code accurately, claims nothing the gate
  has not yet proven, and every gated update is still gated.</done>
</task>

<task type="auto">
  <name>Task 2: Mint the REQ-34.4.1-GAP IDs and bring ROADMAP.md's plan list current</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` L540-556 — the existing REQ-34.4.1-01..13 entries. Match their
      format exactly: checkbox, bold ID, the requirement, and a trailing `*(D-xx)*` attribution.
      Note REQ-34.4.1-12's entry is unchecked and carries an inline status note; the reopened IDs
      need the same honest treatment.
    - `.planning/ROADMAP.md` — the Phase 34.4.1 entry, its Goal, Plans count and plan list.
    - The summaries of plans 10-18 for what each GAP ID actually delivered.
  </read_first>
  <action>
    Add the six `REQ-34.4.1-GAP-NN` entries listed in `<interfaces>` to `REQUIREMENTS.md` in the
    Phase 34.4.1 block, each stating plainly which existing requirement it extends and which
    finding it closes. Attribute them to D-GAP-01/D-GAP-02 and to the live gate rather than to a
    CONTEXT decision that predates them — they were minted from a gate result, and the record
    should say so.

    Update the four reopened IDs (REQ-34.4.1-02, -05, -06, -12) to reflect that the live gate
    reopened them and which gap plan re-closes each. Do not silently re-check a box: REQ-34.4.1-12
    stays UNCHECKED until plan 20's gate records a pass, and -02/-05/-06 stay unchecked until the
    items that prove them are re-observed. An unchecked box with a stated reason is the honest
    state; a checked box on unit evidence is the exact conflation this gap cycle exists to
    correct.

    Update `ROADMAP.md`'s Phase 34.4.1 entry: the plan count and the plan list now run 01-20.
    Mark 01-09 complete and 10-20 as the gap cycle. Leave the phase's status as not-closed.
  </action>
  <verify>
    <automated>grep -c "REQ-34.4.1-GAP-" .planning/REQUIREMENTS.md && grep -c "34.4.1-20-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REQ-34.4.1-GAP-" .planning/REQUIREMENTS.md` returns at least 6.
    - Each of the six GAP entries names the existing REQ it extends and the finding ID it closes.
    - `grep -n "REQ-34.4.1-12" .planning/REQUIREMENTS.md` shows an UNCHECKED box.
    - REQ-34.4.1-02, -05 and -06 are unchecked with a stated reopening reason.
    - `.planning/ROADMAP.md`'s Phase 34.4.1 plan list includes entries through `34.4.1-20`.
    - `git diff --stat -- .planning/IPC-PORT-INVENTORY.md` is EMPTY.
  </acceptance_criteria>
  <done>The requirement record shows what the gap cycle added, what the gate reopened, and what
  remains unproven — with no box checked ahead of its evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record -> future phase | Phase 34.5 and Phase 35 both read these documents as fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.1-83 | Repudiation | premature proof-level upgrade | mitigate | Rows whose behavior changed return to `unit + LIVE (item N)` forward references; `ported-channels-gate.py` check 4 enforces the controlled vocabulary and check 7 pins `oauthCaptureLogin` at `unit`. A live claim cannot be written as prose to dodge the check. |
| T-34.4.1-84 | Tampering | gated record updates | mitigate | `IPC-PORT-INVENTORY.md` stays unmodified (gate check 5, run in this plan's verification), and the struck D-05 entry stays struck. Both are asserted by acceptance criteria, not left to care. |
| T-34.4.1-85 | Repudiation | silently re-checked requirement boxes | mitigate | REQ-34.4.1-02/-05/-06/-12 stay unchecked with stated reasons until the gate re-observes them. |
| T-34.4.1-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages and changes no source. |
</threat_model>

<verification>
- `python3 ported-channels-gate.py` exits 0.
- `python3 ported-channels-gate.py --self-test` exits 0.
- `python3 seam-parity-sweep.py --self-test` exits 0.
- `git diff --stat -- .planning/IPC-PORT-INVENTORY.md` is EMPTY.
- `npm run test:ci` exits 0 — no source changed, so this is a no-regression confirmation only.
- `git diff --name-only` shows only `.planning/` files changed by this plan.
</verification>

<success_criteria>
- The gap cycle is declared before the gate, in the document the gate and Phase 34.5 both read.
- No proof level, checkbox or struck entry is advanced ahead of live evidence.
- The six new REQ IDs exist and each names what it extends.
- Every item-3-gated record update remains untouched and still gated.
</success_criteria>

<output>
Create `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-19-SUMMARY.md` when done.

List every record update that remains GATED and what it is gated on, so plan 20 Task 3 has an
explicit checklist to work from rather than re-deriving it from the gate document.
</output>
