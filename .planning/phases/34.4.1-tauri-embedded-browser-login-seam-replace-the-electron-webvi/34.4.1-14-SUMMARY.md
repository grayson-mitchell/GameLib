---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 14
subsystem: auth
tags: [seam, safeStorage, steamgrid, evidence, gap-closure, tauri, electron]

# Dependency graph
requires:
  - phase: 34.4.1-11
    provides: "Compile-time keyring slot allowlist (keyring_account() in src-tauri/src/main.rs)
      and the exported KEYRING_SLOT_* constants — deliberately did NOT add a SteamGridDB slot,
      and named plan 14 as the plan responsible for that decision"
  - phase: 34.4.1-12
    provides: "HumbleSecretStore seam shape (secretStore.ts) — the pattern this plan would have
      reused had F-1b been reachable"
  - phase: 34.4.1-13
    provides: "Keyring-backed secret store + write→readback→compare→delete migration ordering —
      the pattern this plan would have reused had F-1b been reachable"
provides:
  - "F-1b closed by evidenced declaration: steamgrid/secureKey.ts is confirmed NOT REACHABLE
    from the sidecar's curated import graph, via two independent instruments re-run at
    execution time (not inherited from plan 10's prose)"
  - "In-source comment in secureKey.ts naming F-1b, the measurement, and the exact trigger
    condition that would make it a live exposure"
  - "34.4.1-SEAM-PARITY-SWEEP.md's F-1b section extended with the plan 14 re-check, its two
    commands, and a named forward obligation for whichever future plan ports steamgrid into
    the sidecar"
affects: [34.4.1-20, 34.5]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Re-check-wins-on-disagreement: a gap-closure plan re-measures a prior sweep's finding
      with fresh instruments rather than trusting inherited prose, and records agreement or
      disagreement explicitly either way"
    - "DECLARED (not SILENTLY-DROPPED) dormancy: a safeStorage-shaped module that is provably
      unreached keeps its Electron-only behavior unchanged, but gains an in-source comment
      naming the evidence and the trigger condition that would flip the disposition"

key-files:
  created: []
  modified:
    - src/backend/steamgrid/secureKey.ts
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-SEAM-PARITY-SWEEP.md

key-decisions:
  - "No Rust allowlist slot added for SteamGridDB — the re-check confirmed steamgrid is still
    unreached from the sidecar, so an allowlist entry would address a slot nothing can write
    to yet (same reasoning plan 11 already recorded when it declined to add the slot)"
  - "No new test file created for the NOT REACHABLE branch — Task 2's own acceptance criteria
    required 'test count unchanged' for this branch, so secureKey.test.ts was deliberately
    NOT authored (it does not exist in this repo yet); only ipc_handler.ts/secureKey.ts's
    behavior needed to stay unchanged, which a comment-only edit already guarantees"

requirements-completed: [REQ-34.4.1-GAP-06]

# Metrics
duration: ~20min
completed: 2026-07-30
---

# Phase 34.4.1 Plan 14: F-1b evidenced declaration (steamgrid API key) Summary

**F-1b closed by declaration, not migration: two independent re-measurements (the committed electron-reach ledger and a direct grep of the sidecar's curated `register*Flows` import graph) both confirm `steamgrid/secureKey.ts` is still unreachable from the sidecar, so its direct `safeStorage` import is left unchanged and annotated in-source with the evidence and the trigger that would make it live.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-07-30T09:28:07Z
- **Tasks:** 2 completed
- **Files modified:** 2 (both modified, 0 created)

## Accomplishments

- Re-ran `npx jest src/backend/sidecar/__tests__/electronReachLedger.test.ts` (4/4 passing) as
  the primary instrument: the committed `BASELINE_ELECTRON_REACHING_MODULES` (35 entries,
  grown by plan 12's `humble/secretStore.ts` addition since plan 10's sweep) still contains
  zero `steamgrid/*` paths, and the ledger's own live traversal — which regenerates at every
  invocation rather than reading planning-time prose — found no new `steamgrid` edge.
- Ran a corroborating direct check: `grep -rn "steamgrid" src/backend/sidecar/ --include="*.ts"`
  (non-test) returns zero matches, and `handlers.ts`'s full curated 19-entry `register*Flows`
  import list has no `steamgrid` entry. The only first-party importer of
  `steamgrid/ipc_handler.ts` (which itself is the only importer of `secureKey.ts`'s
  `encryptApiKey`/`decryptApiKey`) is `src/backend/main.ts:1557` — Electron's own entry point,
  never on the sidecar's `bootstrap.ts` → `handlers.ts` chain.
- Both instruments agree with plan 10's original sweep finding. **No disagreement to record**
  — this re-check's purpose (falsifying an inherited claim if wrong) found the inherited claim
  correct.
- Updated `34.4.1-SEAM-PARITY-SWEEP.md`'s F-1b section with a new "Plan 14 re-check" subsection:
  the two commands run, their results, the disposition (DECLARED, not migrated), and a named
  forward obligation for whichever future plan ports a SteamGridDB channel into the sidecar
  (must migrate onto the `HumbleSecretStore`-shaped seam and add a Rust allowlist slot BEFORE
  porting the channel, not after).
- Added a 20-line in-source comment to `src/backend/steamgrid/secureKey.ts` immediately above
  its `import { safeStorage } from 'electron'` line, naming F-1b, both measurement instruments,
  the DECLARED-dormant disposition, and the exact trigger condition — so the next reader of
  this file's `safeStorage` import finds the analysis instead of re-deriving it.
- No behavior changed on either build: `secureKey.ts`/`ipc_handler.ts` are byte-identical apart
  from the added comment block. D-GAP-01's "one-line allowlist addition" claim was not
  exercised in this plan (nothing needed migrating), but its converse — that the allowlist
  design makes NOT touching the Rust side the correct, low-risk choice when a slot has no
  writer yet — held exactly as plan 11 anticipated.

## Task Commits

Each task was committed atomically:

1. **Task 1: Settle F-1b's reachability with evidence and record the disposition** - `30d91ba5e` (docs)
2. **Task 2: Close F-1b at the level the evidence supports (NOT REACHABLE branch)** - `7e57f2cfb` (docs)

## Files Created/Modified

- `src/backend/steamgrid/secureKey.ts` - added a 20-line in-source comment above the
  `safeStorage` import naming F-1b, the two measurement instruments, the DECLARED-dormant
  disposition, and the trigger condition that would make it live; zero behavioral change
- `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-SEAM-PARITY-SWEEP.md` -
  extended the F-1b section with the plan 14 re-check subsection (commands, results,
  disposition, forward obligation)

## Decisions Made

- Did not add a SteamGridDB slot to the Rust keyring allowlist. The re-check confirmed
  `steamgrid` is still unreached from the sidecar, so an allowlist entry would address a slot
  nothing can write to yet — untested capability, exactly as plan 11 already reasoned when it
  declined to add this slot and named plan 14 as the plan that would decide.
- Did not create `src/backend/steamgrid/__tests__/secureKey.test.ts`. The plan's frontmatter
  listed it as a candidate file, but Task 2's own acceptance criteria for the NOT REACHABLE
  branch required "test count unchanged" — since this file does not exist yet in the repo,
  authoring it would have changed the count. The behavior-unchanged guarantee this branch
  needs is already provided by the comment-only diff to `secureKey.ts`.

## Deviations from Plan

None - plan executed exactly as written. Task 1 measured NOT REACHABLE (matching plan 10's
original sweep, with no disagreement), and Task 2 executed the NOT REACHABLE branch of its
`<behavior>` specification precisely: comment-only change, no functional edit to
`ipc_handler.ts`, no new test file.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Verification Results

- `npx jest src/backend/sidecar/__tests__/electronReachLedger.test.ts` — 4/4 passing (Task 1 gate)
- `grep -c "F-1b" .../34.4.1-SEAM-PARITY-SWEEP.md` — 5 (Task 1 gate; >=1 required)
- `git diff --name-only` after Task 1 — only the sweep doc touched, no source file (Task 1 gate)
- `npx jest src/backend/steamgrid/` — 6/6 passing, unchanged from pre-plan count (Task 2 gate)
- `npm run test:ci` — 176/176 suites, 3347/3347 tests, exit 0 — **no regression** against the
  176/3347 baseline plan 13 established (identical counts: this plan added zero tests)
- `npx tsc --noEmit` — exits 0
- `cd src-tauri && cargo test` — 50/50 passing, unchanged (no Rust file touched this plan)
- `grep -c "F-1b" src/backend/steamgrid/secureKey.ts` — 3 (>=1 required for both branches)
- `git diff --stat -- src/backend/steamgrid/` — comment-only (`secureKey.ts` +24 lines,
  `ipc_handler.ts` untouched), as required for the NOT REACHABLE branch
- `git diff --stat -- src-tauri/` — empty (no Rust change, as expected — allowlist untouched)
- `git diff --stat -- .planning/IPC-PORT-INVENTORY.md` — empty

## Requirements Traceability Note

`requirements.mark-complete REQ-34.4.1-GAP-06` reported `not_found`. This is the SAME
pre-existing gap already recorded in plan 13's own Requirements Traceability Note and
STATE.md's "Carry-forward for plan 19" note (the gap-cycle `REQ-34.4.1-GAP-0*` IDs were never
appended to `REQUIREMENTS.md`'s traceability table) -- not a new issue introduced by this plan.
Not fixed here; plan 19 owns the reconciliation per that existing note.

## Next Phase Readiness

- F-1b is closed as a measured, evidenced fact — not an inherited "same-shape, unverified" note.
  Plan 20's blocking live-gate re-run can cite this plan's two commands and their outputs
  directly rather than re-deriving reachability from scratch.
- The forward obligation is named in two places (the sweep doc and the source comment itself),
  so whichever future plan eventually ports a SteamGridDB channel into the sidecar inherits the
  obligation rather than rediscovering the plaintext-exposure risk live — mirroring how
  `34.4.1-PORTED-CHANNELS.md` forwarded T-34.4.1-44b to Phase 34.5.
- D-GAP-01's "one-line allowlist addition" claim remains untested in the migrate direction
  (nothing needed migrating this plan), but its declare-vs-migrate branching worked as designed:
  the allowlist design let this plan cleanly choose "no Rust change" without any structural
  cost, which is the property D-GAP-01 was chosen for.
- No blockers. This was one of two parallel wave-5 gap-closure plans (alongside plan 15); both
  can be verified independently against plan 20's live gate.

---
*Phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi*
*Completed: 2026-07-30*

## Self-Check: PASSED

Verified `src/backend/steamgrid/secureKey.ts` exists and contains the F-1b comment block
(`grep -c "F-1b"` → 3). Verified `.planning/phases/.../34.4.1-SEAM-PARITY-SWEEP.md` exists and
contains the plan 14 re-check section (`grep -c "F-1b"` → 5). Both task commit hashes
(`30d91ba5e`, `7e57f2cfb`) confirmed present via `git log --oneline --all | grep`.
