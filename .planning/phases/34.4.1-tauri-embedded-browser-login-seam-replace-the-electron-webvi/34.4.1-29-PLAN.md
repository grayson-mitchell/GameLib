---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 29
type: execute
wave: 7
depends_on: ["34.4.1-28"]
gap_closure: true
gap_cycle: 2
files_modified:
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-3.md
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN.md
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
  - .planning/IPC-PORT-INVENTORY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/deferred-items.md
autonomous: false
requirements: [REQ-34.4.1-12, REQ-34.4.1-10, REQ-34.4.1-02, REQ-34.4.1-05, REQ-34.4.1-06, REQ-34.4.1-13, REQ-34.4.1-GAP-02, REQ-34.4.1-GAP-03, REQ-34.4.1-GAP-05, REQ-34.4.1-GAP-07, REQ-34.4.1-GAP-08, REQ-34.4.1-GAP-09, REQ-34.4.1-GAP-12, REQ-34.4.1-GAP-13]
must_haves:
  truths:
    - "A disconnect is followed by a genuinely fresh login — Humble's own login form, not an auto-signed-in page"
    - "The census arithmetic comes out clean on a real jar, against CORRECTED counts"
    - "Other origins' cookies survive the disconnect — it is still domain-scoped"
    - "The login window's title bar shows Humble's own document title, and the window's raise behaviour is finally observed"
    - "Every record update gated on an item-3 PASS is applied only if item 3 actually passed"
  artifacts:
    - path: ".planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-3.md"
      provides: "The third re-run contract, written with empty result slots BEFORE any live work"
      contains: "Verdict"
  key_links:
    - from: "34.4.1-LIVE-GATE-RERUN-3.md"
      to: ".planning/IPC-PORT-INVENTORY.md"
      via: "the GATED record updates, applied only on a recorded item-3 PASS"
      pattern: "humbleDisconnect"
---

<objective>
Run the blocking live gate a **third** time. This is the only evidence that can close F-6, WR-07 and
F-4, and the only thing that can close Phase 34.4.1.

Purpose: **REQ-34.4.1-12** and D-10's binding rule, restated unchanged by every gate document since
— *"any item recorded FAIL means this phase does not close … no advisory mode, no pre-authorized
override, no partial-pass closure."* The gate has now run twice and failed twice (2 of 4, then 3 of
4). Nothing in plans 21-28 proves F-6 fixed: research Item 1 establishes that the defect lives
inside WebKit's own closed-source cookie-store matching, which a `#[cfg(test)] mod tests` structurally
cannot reach (`main.rs:1864-1867`'s standing convention). **A green suite has now missed live-blocking
defects twice — 3279/3279, then 3387/3387.** A third green suite is not different evidence.

Scope: **item 3 re-runs IN FULL** — it is the item that failed. **Items 1, 2 and 4 re-run for
regression** — item 1's window presentation changed (plan 24), item 2's keyring arm gained a timeout
wrapper (plan 26), and item 4 runs on a session whose teardown path was rewritten (plan 23). None
can be carried forward on a prior PASS.

Four things this run must do that neither prior run did:
1. **Score item 3 against CORRECTED census counts.** Research Item 8 #1: if Defect A had not been
   fixed, the census's `matched`/`survivingNonHumble` numbers would remain undercounted and any
   PASS would be unreliable. Plan 22 fixed it; this contract states the corrected counts as an
   explicit precondition rather than assuming it.
2. **Re-verify the READ path, not only the delete path.** Research Item 8 #2 applies this cycle's own
   process lesson reflexively: both cookie arms were touched, so a run that only checks the delete
   could ship a broken read without noticing.
3. **Cross-check Epic's logout**, which research confirmed calls the exact same Rust arm — shared
   code, not merely similar shape. If no Epic test session exists, declare the shared-code-path
   argument explicitly as the evidence rather than implying an observation that did not happen.
4. **Observe F-4's raised half and WR-07's positive half**, both of which have gone unobserved or
   failed across two runs, against plan 24's machine records so the operator's report can be
   corroborated rather than standing alone.

**This plan owns the GATED record updates.** `IPC-PORT-INVENTORY.md` and
`34.4.1-PORTED-CHANNELS.md` were withheld by plan 20 Task 3 because item 3 did not pass.
`34.4.1-19-SUMMARY.md` § "GATED updates — explicit checklist" is the authoritative list and Task 3
consumes it directly rather than re-deriving it.

Output: a recorded verdict, and the record updates that verdict permits — no more.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-RESEARCH-GAP-CYCLE-2.md

**Project rule (CLAUDE.md):** run `graphify query`/`graphify explain` before grepping raw source.

<interfaces>
**The evidence this gap cycle built specifically so this gate can read it.** Each plan's SUMMARY
records the exact format — read them rather than guessing at a grep:
- **plan 21** — `34.4.1-SPIKE-016-FINDINGS.md`: the live answers to `with_webview` synchrony, thread
  identity, the two census/clear direction counts, and the retry experiment.
- **plan 22** — the census log line format, byte-identical to plan 17's so this gate's grep still
  finds it, now fed by the correctly-directed `humble_login_cookies_for_domain` read.
- **plan 23** — the arm's measured delete count (a post-removal re-read delta, never an attempted
  count), and Epic's new `clearEpicCookies` count line plus its removed-nothing warning.
- **plan 24** — two greppable substrings: the title-change diagnostic (fires when
  `on_document_title_changed` fires, logging a LENGTH only) and the presentation-configuration line
  (what was requested for the visible window: size, centring, one-shot focus, no always-on-top).
- **plan 26** — the `keyring:timeout` classification and the RPC timeout message that now names its
  channel.
- **plan 27** — the library should render with **zero** `unsupported URL` errors; the count is this
  gate's confirmation of `REQ-34.4.1-GAP-13`'s live half.
- **plan 25** — whether F-10 was fixed or declared narrowed-but-open, and the classification.

**Item 3's pass condition, both halves, unchanged in substance from the last run:**
(a) the clear reports a real non-zero count AND the per-category storage report is present AND a
    fresh login lands on Humble's own login form rather than an auto-signed-in page. Last run:
    signed straight back in with **zero** poll lines, versus a genuine login's ~3 lines carrying
    ~17 rejections over ~1.3 min. **That qualitative contrast is the measurement** — the obsolete
    literal "36" was retired by plan 18's F-2 collapse of poll logging and must not be reused.
(b) the census arithmetic holds: `matched-after == 0`, the jar shrank by exactly `matched-before`,
    `deleted == matched-before`, and `survivingNonHumble` is **non-zero**. Last run all three
    equalities failed and the discrepancy warning fired by name:
    `before(total=25, matched=23) after(total=24, matched=23) deleted=25 survivingNonHumble=1`.

**`Ok(())` is not proof, and neither is a returned count on its own.** Plan 23's count is now
measured by a post-removal re-read, but the census remains the independent oracle. **If the reported
count and the census arithmetic ever disagree, the census wins** and the item FAILS — a mutating
call's own report can never outrank an independent re-read.

**Precondition 6 (plant a non-Humble cookie) remains MOOT** and must not be attempted. Plan 17's
census supplies 3(b)'s evidence without it. Struck, not deleted, per this phase's convention.

**The GATED-updates checklist** lives in `34.4.1-19-SUMMARY.md` § "GATED updates — explicit
checklist for plan 20 Task 3" (a 13-row table). Plan 20 applied only the non-gated rows. Still
gated on an item-3 PASS: `IPC-PORT-INVENTORY.md` L73-75's `humbleDisconnect` declared-partial note;
`34.4.1-PORTED-CHANNELS.md`'s struck "34.4 D-05 CLOSED" entry; and the `REQ-34.4.1-06`,
`REQ-34.4.1-12`, `REQ-34.4.1-GAP-03`, `REQ-34.4.1-GAP-05` checkboxes. This cycle adds
`REQ-34.4.1-GAP-07`, `-GAP-08`, `-GAP-09`, `-GAP-12` and `-GAP-13`'s live half to the gated set.

**The row that is NOT gated on this gate and is now closed elsewhere:** the checklist's
`seam-parity-sweep.py` S-07/S-10/S-11 row, marked "not gated on plan 20 at all — unassigned". Plan
28 closed it. Do not re-forward it.

**Baseline discipline.** Record `npm run test:ci` and `cd src-tauri && cargo test` verbatim with
their exact counts and exit codes — **not rounded to green**. Documented intermittents that must be
isolated rather than declared regressions: the `rustInvoke` mock frame-leak that can land on an
arbitrary suite with `rustInvoke timed out after 60000ms: keyring_get`; `depot.test.ts`'s
full-suite-only flake; and `helperProcess.test.ts`'s full-suite-only flake. All three have recorded
single-file isolation results in `deferred-items.md`; re-run the isolation before calling any of
them a regression (project memory `flake-baselines-can-be-undiagnosed-bugs`).

**Log rotation nearly destroyed the last run's evidence** — `gamelib.log` rotated three times.
A rotation-preserving capture is mandatory, and the operator must take a FRESH baseline offset at
launch; any offset noted earlier in the session is void.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the third re-run contract with empty result slots, and evidence the automatable preconditions</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-3.md</files>
  <read_first>
    - `34.4.1-LIVE-GATE-RERUN.md` in full — the four items as written, the four corrections that run
      made to its own record (the wrong precondition 3, the single-not-double backup, the obsolete
      "36 vs 0" comparator, F-9's corrected scope), the Verdict section and the findings register.
    - `34.4.1-20-PLAN.md` Task 1 — the preparation shape this task inherits: clean rebuild, verbatim
      baseline, credential-state record, rotation-preserving capture.
    - `34.4.1-20-SUMMARY.md` — the recorded lessons, especially that the rotation-preserving capture
      survived three real rotations and preserved evidence that would otherwise have been lost.
    - `34.4.1-19-SUMMARY.md` § "GATED updates — explicit checklist" — the 13-row table Task 3
      consumes.
    - The SUMMARYs of plans 21 through 28 — every log-line format, greppable substring and recorded
      number this gate reads. Do not guess a grep; take it from the SUMMARY that recorded it.
    - `34.4.1-RESEARCH-GAP-CYCLE-2.md` § Item 8 — the five things this run must do differently, and
      #6's STATE.md housekeeping instruction.
    - `deferred-items.md` — the three documented intermittents and their isolation results.
  </read_first>
  <action>
    Write `34.4.1-LIVE-GATE-RERUN-3.md` with all four items present and **every result slot empty**,
    before any live work. Carry forward the prior contract's structure so the operator is not
    learning a new document, and state at the top that this supersedes `34.4.1-LIVE-GATE-RERUN.md`
    for the items it re-runs, without deleting that document's recorded history.

    The contract must add, beyond the prior run's four items:
    - **An explicit precondition that Defect A is fixed**, naming plan 22's corrected census read,
      and stating that if it is not, item 3 cannot be scored at all — an undercounting
      `matched-before` makes all three equalities unreliable even when they appear to hold.
    - **A read-path re-verification step inside item 3**: the census's `total` must be non-zero and
      its verdict `SUPPORTED_NONEMPTY` on the BEFORE read, proving the read arm still works after
      both arms were touched. A census that silently reports zeros would look like a clean pass.
    - **An Epic logout cross-check**, with two acceptable outcomes recorded honestly: an observed
      Epic logout (record the count line and the removed-nothing warning's presence or absence), or
      an explicit declaration that no Epic test session was available and the evidence is the
      shared-code-path argument only. **Do not open a new Epic OAuth login for this** — D-04 scopes
      the mechanism's proof to Humble, and a logout of an already-authenticated session is the only
      in-scope form.
    - **WR-07's positive observation**, paired with plan 24's title-change diagnostic substring: the
      operator reads the title bar AND the log is checked for the diagnostic. If the diagnostic
      fired but the title bar still reads `Tauri app`, that is a materially different finding from
      the diagnostic never firing, and the contract must ask for both.
    - **F-4's raised-half observation**, as research Item 4 specifies: (a) does the window come to
      the front and take keyboard focus the moment it is created, and (b) after switching to another
      application and back, does it need an extra click to refocus or show any stale
      still-focused state. Paired with plan 24's presentation-configuration log line so what was
      REQUESTED is on the record beside what was SEEN.
    - **An F-10 pre-check before item 1**: navigate to Manage Accounts on a cold start and record
      whether it renders first time. It blocked a gate item once; discovering it again mid-run
      wastes a session that needs the developer, real credentials and a 2FA step.
    - **An `unsupported URL` count** from the library render, for `REQ-34.4.1-GAP-13`'s live half.
    - **An F-9 watch**: whether any `keyring:timeout` or generic RPC timeout appears, and — research
      Item 5's specific question — whether it ever co-occurs with a cookie operation in the same
      session. Not passing or failing anything; F-9 is open and unassigned for closure.

    Then satisfy the automatable preconditions and record the evidence:
    1. Remove stale `src-tauri/target/debug/build/gamelib-shell-*` artifacts and confirm a real
       rebuild.
    2. Re-measure the baseline at current HEAD (`npm run test:ci`, `cd src-tauri && cargo test`) and
       record both verbatim, with exact counts and exit codes. If any of the three documented
       intermittents appears, isolate the suite and record the isolation result rather than
       declaring a regression.
    3. Record the current Humble credential state and any restore paths, verifying what actually
       exists on disk rather than transcribing the prior run's paths — the last run found one of
       its two claimed backups pointed into a nonexistent scratchpad.
    4. Confirm plan 21's throwaway spike arm is absent from the build:
       `grep -c "spike016\|SPIKE 016" src-tauri/src/main.rs` must be `0`.
    5. Set up a rotation-preserving `gamelib.log` capture for the whole run, record its location in
       the contract, and instruct the operator to take a FRESH baseline offset at launch.

    Do not drive any item in this task. This is preparation and contract-writing; plan 08/20's
    run-and-record boundary is inherited. If any precondition cannot be satisfied, STOP and report
    rather than proceeding with a weaker gate.
  </action>
  <verify>
    <automated>npm run test:ci; cd src-tauri && cargo test; grep -c "spike016\|SPIKE 016" src/main.rs</automated>
  </verify>
  <acceptance_criteria>
    - `34.4.1-LIVE-GATE-RERUN-3.md` exists with all four items present and every result slot EMPTY.
    - The contract states the corrected-census precondition and says item 3 cannot be scored without
      it.
    - The contract contains a read-path re-verification step inside item 3.
    - The contract contains the Epic cross-check with both acceptable outcomes spelled out.
    - The contract asks for WR-07's title-bar reading AND plan 24's diagnostic substring separately.
    - The contract carries F-4's two-part raised-half observation and names plan 24's
      presentation-configuration substring.
    - The contract carries the F-10 pre-check, the `unsupported URL` count and the F-9 watch.
    - Precondition 6 is struck as moot, not deleted.
    - The baseline is recorded verbatim with exact counts and exit codes, including any intermittent
      and its isolation result.
    - `grep -c "spike016\|SPIKE 016" src-tauri/src/main.rs` is `0`.
    - A rotation-preserving capture is in place and its location is recorded in the contract.
    - `git diff --stat -- .planning/IPC-PORT-INVENTORY.md .planning/phases/34.4.1-*/34.4.1-PORTED-CHANNELS.md` is EMPTY at the end of this task.
  </acceptance_criteria>
  <done>The contract is written before any live work, the automatable preconditions are evidenced,
  and the log evidence is protected against the rotation that nearly destroyed it last time.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING live gate re-run #3 — item 3 in full, items 1/2/4 for regression, plus WR-07, F-4, F-10 and the Epic cross-check</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-3.md</files>
  <read_first>
    - `34.4.1-LIVE-GATE-RERUN-3.md` — Task 1's contract: the four items, their evidence
      requirements, the added observations and the preserved-log capture location.
    - `34.4.1-LIVE-GATE-RERUN.md` — the last run's per-item results and measured numbers, which this
      run is scored against for contrast.
    - The SUMMARYs of plans 21-28 — the exact log-line formats and greppable substrings to look for.
    - `34.4.1-SPIKE-016-FINDINGS.md` — what was already established live, so this run does not
      re-derive it.
  </read_first>
  <what-built>
    Plans 21-28 closed both halves of F-6 at the code level and added the instruments this gate
    reads:
    - **F-6 Defect A** — the disconnect census now reads through a purpose-built,
      correctly-directed arm, so subdomain- and leading-dot-scoped Humble cookies are counted
      instead of silently missed. The login poll's own arm keeps the direction spike 014a proved it
      needs, pinned by test.
    - **F-6 Defect B** — on macOS the clear no longer hands WebKit a reconstructed cookie object.
      It reaches the live `WKWebsiteDataStore` through Tauri's documented `with_webview()` escape
      hatch and removes only the data records whose `displayName` matches the target domain. The
      returned count is **measured by re-reading the jar after removal** — the attempted count that
      could never report failure is gone on every platform.
    - **The same Rust arm's second, already-shipped caller** (Epic's logout) now logs its own count
      and warns when a clear removes nothing.
    - **WR-07** — the visible login window's title tracks the loaded document through
      `on_document_title_changed`, and the comment claiming a grep gate proved WR-07 is corrected.
    - **F-4** — no code change (the platform offers only one-shot focus or a persistent pin), but the
      window's requested presentation is now logged so your observation has something to be checked
      against.
    - **F-10** — diagnosed live and either fixed or recorded narrowed-but-open; no pending state on
      `/login` can now render an empty window.
    - **F-9** — a Keychain read that stalls now fails fast as `keyring:timeout` instead of consuming
      the sidecar's whole 60 s budget.
    - **Housekeeping** — `queryLocalFonts` degrades instead of throwing; no image is requested
      against the unregistered `imagecache://` scheme; two sweep scripts, one new.

    A fully-green suite and `cargo test` prove none of the live behaviour works. That is the whole
    reason this gate exists, and it has now caught what automation could not on three consecutive
    slices.
  </what-built>
  <how-to-verify>
    Work `34.4.1-LIVE-GATE-RERUN-3.md` top to bottom and record results IN it as you go. Read
    sidecar output from `~/Library/Logs/GameLib/gamelib.log`; read Rust shell `eprintln!` output from
    the `pnpm tauri:dev` terminal (the sidecar's stdout is the RPC pipe, so shell diagnostics do not
    reach the log file). Take a FRESH baseline offset at launch — any offset noted earlier is void.

    **Pre-check, before item 1 — F-10.** Cold start, navigate to Manage Accounts for the first time.
    Record whether it renders. If it is blank, record it and retry; do not abandon the run, but the
    finding is recorded either way. Also record the `unsupported URL` error count from the library
    render (expected: zero after plan 27).

    **Item 1 — login FROM SCRATCH (REQ-34.4.1-02, regression).** Authenticate for real including any
    Humble Guard / OTP step.
    - **WR-07:** read the title bar. Record its exact text. Then check the log for plan 24's
      title-change diagnostic. Record BOTH: a diagnostic that fired while the bar still reads
      `Tauri app` is a different finding from a diagnostic that never fired.
    - **F-4, both parts:** (a) does the window come to the front and take keyboard focus the moment
      it appears? (b) switch to another application — a password manager is the realistic case —
      and come back: does the login window need an extra click to refocus, or show any stale
      still-focused state? Record both in your own words. Also record plan 24's
      presentation-configuration line from the terminal.
    - Confirm plan 13's keyring-install line is present. **Its absence means the install silently
      degraded to the plaintext default and item 1 FAILS regardless of the store contents.**
    - Evidence: the store file's field types and lengths only, never values (T-34.4.1-39). PASS
      requires NO `sessionCookie` field and `encryptionDegraded: false`.

    **Item 2 — survives quit-and-relaunch (REQ-34.4.1-12, regression).** Capture the store snapshot
    immediately after item 1, fully quit, relaunch, confirm the relaunch independently (fresh PID,
    fresh bootstrap log line), then capture the "after" snapshot. **Take both readings inside this
    item, before starting item 3** — last time item 3 overwrote the "after" and it could not be
    reconstructed (F-7). Watch for `keyring:timeout` (plan 26's new classification) around the
    relaunch.

    **Item 3 — disconnect (REQ-34.4.1-06). THE ITEM THAT FAILED TWICE.** From the signed-in state,
    trigger Humble disconnect.
    - **(b) first, because it is the objective half.** Find the census line and check the
      arithmetic: `matched-after` is 0, the jar shrank by exactly `matched-before`, `deleted` equals
      `matched-before`, and `survivingNonHumble` is **non-zero**. Also record the BEFORE read's
      `total` and verdict — a `total` of 0 or an `UNDECIDABLE`/`SUPPORTED_BUT_EMPTY` verdict means
      the READ path broke and the item fails even if the deletion worked. Record all four numbers
      and both verdicts. If the discrepancy warning fired, record which named equality failed.
    - **(a) then the behavioural half.** Confirm the per-category storage report is present (each
      category a number or the literal `unsupported`, never a coerced `0`), then **attempt a fresh
      login and confirm you land on Humble's own login form, not an auto-signed-in page.** Last time
      this took 3 seconds with zero poll lines, where a genuine login produced ~3 lines carrying ~17
      rejections over ~1.3 min. **That contrast is the measurement.** If you are signed straight
      back in again, F-6 is NOT fixed — record it exactly as observed, do not round it up.
    - **If the reported count and the census disagree, the census wins.** A mutating call's own
      report never outranks an independent re-read.
    - **Epic cross-check.** If you have an authenticated Epic session, log out and record the
      `clearEpicCookies` count line and whether the removed-nothing warning fired. If you do not,
      write exactly that: no Epic test session was available and the evidence for the shared arm's
      second caller is the shared-code-path argument only, not an observation. **Do not open a new
      Epic OAuth login** — that is Phase 34.5's scope.

    **Item 4 — `humbleRevealKey` (REQ-34.4.1-05, regression).** Using an unrevealed key, attempt a
    real reveal. A Cloudflare 403 remains a VALID declarable outcome (D-07). Confirm the transport
    label names the seam transport. **Never record the revealed value.**

    **Scoring rule, unchanged and binding:** any item recorded FAIL means this phase does not close.
    No advisory mode, no pre-authorized override, no partial-pass closure. If something is unclear
    or unobservable, record it as such — do not round it up to a PASS. That honesty is what made
    both prior runs useful.

    This task is run-and-record: **do not fix anything discovered here.** A defect found mid-gate is
    a finding for the next cycle. Inventing a fix at gate time would ship unproven code, which is
    exactly what plans 08 and 20 both declined to do.
  </how-to-verify>
  <action>
    Drive all four items of `34.4.1-LIVE-GATE-RERUN-3.md` live under `pnpm tauri:dev` with the
    developer at the keyboard, recording each item's result and evidence INTO that document as the
    item completes — never afterwards from memory (F-7). Fix nothing.
  </action>
  <verify>
    <human-check>All four items in `34.4.1-LIVE-GATE-RERUN-3.md` carry a recorded result and the
    evidence their contract requires, including the WR-07, F-4, F-10, Epic and `unsupported URL`
    observations, and the developer has signalled the verdict.</human-check>
    <automated>grep -c "Result:" .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-3.md</automated>
  </verify>
  <acceptance_criteria>
    - All four items have a recorded result with their required evidence — no empty slots, no
      inferred results.
    - Item 1's evidence includes the store's field list (types and lengths only), an explicit
      statement about the keyring-install log line, the title bar's exact text, plan 24's
      title-change diagnostic presence, and F-4's two-part raised-half observation.
    - Item 2's before AND after snapshots are both recorded, both taken during item 2.
    - Item 3 records: all four census numbers plus both verdicts, the BEFORE read's `total` and
      verdict as the read-path check, the per-category storage report, the measured clear count, and
      the re-login outcome in the operator's own words.
    - The Epic cross-check records either an observation or an explicit "no Epic session available;
      shared-code-path argument only".
    - Item 4 records the transport label and the outcome, with no revealed value written down.
    - The F-10 pre-check result and the `unsupported URL` count are recorded.
    - Any `keyring:timeout` or RPC timeout is recorded, with whether it co-occurred with a cookie
      operation.
    - A Verdict section states the overall result and, on any FAIL, what the next gap cycle is.
    - No secret value appears anywhere in the document (T-34.4.1-39).
  </acceptance_criteria>
  <done>All four items are recorded with the evidence their contract requires, plus the four
  observations that went unobserved or unmade across the two prior runs, and the developer has
  signalled the verdict or named the failures.</done>
  <resume-signal>
    Record each item's result in `34.4.1-LIVE-GATE-RERUN-3.md` as you go, then reply with the
    verdict — e.g. "4/4 PASS, title read 'Humble Bundle', window raised, census 23/23/0/2" or
    "item 3 FAIL: [what you saw]". Partial observations are welcome mid-run; the phase closes only
    on 4/4.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verdict and apply ONLY the record updates it permits, including the GATED set</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN-3.md, .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE-RERUN.md, .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md, .planning/IPC-PORT-INVENTORY.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/deferred-items.md</files>
  <read_first>
    - `34.4.1-LIVE-GATE-RERUN-3.md` — Task 2's recorded results. **The measurement wins over every
      expectation in every other document, including this plan's own objective.**
    - `34.4.1-19-SUMMARY.md` § "GATED updates — explicit checklist for plan 20 Task 3" — the 13-row
      table. Consume it directly. Note that its `seam-parity-sweep.py` row was closed by plan 28 and
      must not be re-forwarded.
    - `.planning/IPC-PORT-INVENTORY.md` around L73-75 — the `humbleDisconnect` declared-partial note,
      untouched since plan 19 and verified-intact by `ported-channels-gate.py` check 5.
    - `34.4.1-PORTED-CHANNELS.md` — the struck "34.4 D-05 CLOSED" entry and the gap-cycle section's
      prose recording the sweep staleness (now closed by plan 28).
    - `.planning/REQUIREMENTS.md` — `REQ-34.4.1-06`, `-12`, `-GAP-03`, `-GAP-05` (still unchecked,
      gated on item 3) and this cycle's `-GAP-07`, `-GAP-08`, `-GAP-09`, `-GAP-12`, `-GAP-13`.
    - `.planning/ROADMAP.md` — the Phase 34.4.1 entry, its `**Requirements**:` line, and the GAP
      CYCLE 2 plan list plan 21 added. Check whether plan 27's `REQ-34.4.1-GAP-13` was added to the
      `**Requirements**:` line or handed off here (plan 27's SUMMARY records which).
    - `.planning/STATE.md` — and project memory `gsd-sdk-state-writes-corrupt-state-md`: hand-correct,
      never via an automated state write.
    - `34.4.1-20-PLAN.md` Task 3 — the record-only, verdict-permits-nothing-more discipline this
      task inherits.
  </read_first>
  <action>
    Record the verdict in `34.4.1-LIVE-GATE-RERUN-3.md` — the per-item table, the overall result, a
    findings register updated from the prior run's, and, on any FAIL, an explicit statement of what
    the next gap cycle must address. Strike any statement in `34.4.1-LIVE-GATE-RERUN.md` that this
    run falsified, using this phase's strike-don't-delete convention.

    **Apply exactly the updates the verdict permits, and nothing else.**

    **If item 3 PASSED (both halves) and the overall verdict is 4/4:**
    - `.planning/IPC-PORT-INVENTORY.md` — resolve the `humbleDisconnect` declared-partial note at
      L73-75 to a closed declaration, citing this run's census numbers.
    - `34.4.1-PORTED-CHANNELS.md` — restore the struck "34.4 D-05 CLOSED" entry with a dated note
      recording that it took three gate runs and naming what was actually wrong (a census reading
      the jar in the wrong direction, and a delete that reported an attempted count for an operation
      WebKit never performed). Update the gap-cycle section's sweep-staleness prose to record plan
      28's closure.
    - Check `REQ-34.4.1-06`, `-12`, `-GAP-03`, `-GAP-05`, `-GAP-07`, `-GAP-08`, and `-GAP-13`, each
      with a dated rider citing the specific observation that closed it.
    - Check `REQ-34.4.1-GAP-09` only if BOTH halves were observed — the title bar showed Humble's
      own title AND F-4's raised behaviour was reported. A title PASS with F-4 still unobserved
      leaves it unchecked with a rider saying so; that half has now gone unobserved three times and
      must not be quietly absorbed.
    - Check `REQ-34.4.1-GAP-12` only if the F-10 pre-check rendered first time AND plan 25 recorded
      a classified fix. A non-reproduction is not a closure.
    - Update `.planning/ROADMAP.md`: mark plans 21-29 `[x]`, add `REQ-34.4.1-GAP-13` to the
      `**Requirements**:` line if plan 27 handed it off, and mark the phase complete.
    - Hand-correct `.planning/STATE.md`: the phase closes, the milestone position advances, plan
      counters include this cycle's nine plans.

    **If ANY item FAILED:** apply NOTHING from the gated set. Leave `IPC-PORT-INVENTORY.md` and
    `34.4.1-PORTED-CHANNELS.md` untouched and every gated checkbox unchecked, exactly as plan 20
    Task 3 did. Record the failure, update the findings register, log any new finding in
    `deferred-items.md`, and hand-correct `.planning/STATE.md` to name the next action
    (`/gsd-plan-phase 34.4.1 --gaps`). **The binding rule allows no partial-pass closure and this
    task may not invent one**, however close the run came.

    In either case: log every finding this run produced into `deferred-items.md` if no plan owns it,
    including the F-9 watch result and the Epic cross-check's disposition. Do not upgrade any proof
    level beyond what Task 2 recorded — a shared-code-path argument is not an observation, and
    saying so is the point.
  </action>
  <verify>
    <automated>python3 .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py && python3 .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py --self-test && npm run test:ci</automated>
  </verify>
  <acceptance_criteria>
    - `34.4.1-LIVE-GATE-RERUN-3.md` has a Verdict section with a per-item table and an overall
      result, plus an updated findings register.
    - **If any item FAILED:** `git diff --stat -- .planning/IPC-PORT-INVENTORY.md .planning/phases/34.4.1-*/34.4.1-PORTED-CHANNELS.md`
      is EMPTY, and `grep -c "^- \\[x\\] \\*\\*REQ-34.4.1-06\\*\\*\\|^- \\[x\\] \\*\\*REQ-34.4.1-12\\*\\*" .planning/REQUIREMENTS.md`
      is `0`.
    - **If the verdict is 4/4:** the `humbleDisconnect` note in `.planning/IPC-PORT-INVENTORY.md` is
      resolved and cites this run's census numbers; `34.4.1-PORTED-CHANNELS.md`'s struck
      "34.4 D-05 CLOSED" entry is restored with a dated note naming both defects; and
      `REQ-34.4.1-06`, `-12`, `-GAP-03`, `-GAP-05`, `-GAP-07`, `-GAP-08`, `-GAP-13` are each checked
      with a dated rider citing a specific observation.
    - `REQ-34.4.1-GAP-09` is checked ONLY if both the title-bar reading and F-4's raised-half
      observation were recorded; otherwise it stays unchecked with a rider stating which half is
      still unobserved.
    - `REQ-34.4.1-GAP-12` is checked ONLY if the F-10 pre-check rendered first time AND plan 25
      recorded a classified fix.
    - Every statement in `34.4.1-LIVE-GATE-RERUN.md` that this run falsified is STRUCK, not deleted.
    - `.planning/STATE.md` is hand-edited (no automated state write) and names the correct next
      action for the recorded verdict.
    - `.planning/ROADMAP.md`'s `**Requirements**:` line for Phase 34.4.1 contains every
      `REQ-34.4.1-GAP-07 … GAP-13` id.
    - `python3 ported-channels-gate.py` and `--self-test` both exit 0 after the edits.
    - `python3 seam-parity-sweep.py --self-test` exits 0 and the plan-28 closure prose in
      `34.4.1-PORTED-CHANNELS.md` matches it.
    - `npm run test:ci` exits 0; `npx tsc --noEmit` exits 0; `cd src-tauri && cargo test` exits 0.
    - No secret value appears in any edited document.
  </acceptance_criteria>
  <done>The verdict is recorded honestly, and exactly the records the verdict permits have moved —
  no more, and none of the gated set on anything less than an item-3 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live observation -> planning record | what the operator saw becomes the project's permanent truth about whether logout works |
| real Humble credentials -> gate session | genuine credentials, a 2FA step and a real key are exercised on the developer's own machine |
| gated record updates -> Phase 34.5 and beyond | `34.4.1-PORTED-CHANNELS.md` is read as fact by later phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.1-129 | Repudiation | a partial-pass closure recorded as a phase close | mitigate | D-10's binding rule is restated in the contract, in the checkpoint's scoring paragraph and in Task 3's action, and the FAIL branch's acceptance criteria assert by grep that the gated documents are untouched and the gated boxes unchecked. This is the third run; the rule has held twice and must hold a third time however close the result. |
| T-34.4.1-130 | Repudiation | a mutating call's own report accepted as proof of effect | mitigate | The contract states that if the reported clear count and the census arithmetic disagree, the census wins and the item FAILS. Plan 23's count is measured by a post-removal re-read, but the census remains an independent oracle rather than a corroborating one. |
| T-34.4.1-131 | Repudiation | scoring item 3 against undercounted census numbers | mitigate | An explicit precondition that plan 22's corrected read is in place, with the contract stating item 3 cannot be scored without it. Research Item 8 #1. |
| T-34.4.1-132 | Repudiation | the read path silently breaking while the delete path passes | mitigate | Item 3's evidence requires the BEFORE read's `total` and verdict; a zero total or a non-`SUPPORTED_NONEMPTY` verdict fails the item even if the deletion appeared to work. Research Item 8 #2, applied because BOTH arms were touched this cycle. |
| T-34.4.1-133 | Repudiation | Epic's shared-arm caller implied to be verified when it was not | mitigate | Two acceptable outcomes are spelled out in advance, and the "no session available" branch requires writing that the evidence is the shared-code-path argument only. Research Item 8 #3. |
| T-34.4.1-134 | Information Disclosure | a secret, cookie value or revealed key written into the gate record | mitigate | T-34.4.1-39, restated at every evidence instruction: field types and lengths only, counts only, never a revealed value. Asserted as an acceptance criterion on both Task 2 and Task 3. |
| T-34.4.1-135 | Denial of Service | log rotation destroying an item's evidence mid-run | mitigate | A rotation-preserving capture is a Task 1 precondition with its location recorded in the contract, and the operator takes a fresh baseline offset at launch. The last run's capture survived three real rotations and preserved evidence that would otherwise have been lost. |
| T-34.4.1-136 | Tampering | an automated state write corrupting `STATE.md` | mitigate | `STATE.md` is hand-edited, per project memory `gsd-sdk-state-writes-corrupt-state-md` (reverts `stopped_at`, mangles counters, splices status into historical blocks). Asserted in the acceptance criteria. |
| T-34.4.1-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. It runs existing suites and edits planning records. |
</threat_model>

<verification>
- `34.4.1-LIVE-GATE-RERUN-3.md` has all four items recorded with their required evidence and a
  Verdict section.
- On any FAIL: `git diff --stat -- .planning/IPC-PORT-INVENTORY.md .planning/phases/34.4.1-*/34.4.1-PORTED-CHANNELS.md`
  is EMPTY and no gated checkbox moved.
- `python3 ported-channels-gate.py` and `--self-test` both exit 0.
- `python3 seam-parity-sweep.py --self-test` and `python3 wkwebview-silent-noop-sweep.py --self-test`
  both exit 0.
- `npm run test:ci` exits 0; `npx tsc --noEmit` exits 0; `cd src-tauri && cargo test` exits 0.
- `grep -c "spike016\|SPIKE 016" src-tauri/src/main.rs` is `0`.
- No secret value appears in any edited document.
</verification>

<success_criteria>
- All four gate items are run live and recorded with the evidence their contract requires.
- WR-07's title bar, F-4's raised half, F-10's first-navigation render and the `unsupported URL`
  count are all observed rather than inferred — three of the four went unobserved across two prior
  runs.
- Item 3 is scored against corrected census counts, with the read path independently re-verified.
- The Epic cross-check is either observed or honestly declared as an argument.
- Exactly the record updates the verdict permits are applied, and none of the gated set on anything
  less than an item-3 PASS.
</success_criteria>

<output>
Create `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-29-SUMMARY.md` when done.

Record: the verdict and the per-item table; every number item 3 produced; which gated records moved
and which did not, with the rule that decided each; the F-4 and WR-07 observations in the operator's
own words; the Epic cross-check's disposition; and — if the phase closes — an explicit note that it
took three live gate runs, and what each one caught that a green suite could not. If it does not
close, name the next gap cycle's blocking finding as precisely as this cycle's research named F-6's.
</output>
