---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 29
type: live-gate-rerun
status: pending
blocking: true
supersedes_items: [1, 2, 3, 4]
regression_reruns: []
created: 2026-07-31
completed: null
verdict: null
items_total: 4
items_passed: null
items_failed: null
notes: |
  CONTRACT written before any live work (Task 1), per D-09/D-10 discipline this document inherits
  from 34.4.1-LIVE-GATE.md and 34.4.1-LIVE-GATE-RERUN.md. All result slots below are EMPTY. This
  document does not itself close F-4, F-6, F-9, F-10, WR-07 or REQ-34.4.1-GAP-13's live half --
  only a recorded live result on each item does that.
---

# Phase 34.4.1 Plan 29 — Blocking Live-Gate RE-RUN #3 (REQ-34.4.1-12)

This is the **third** re-run contract for this phase's blocking live gate, written before any live
work begins. It **supersedes `34.4.1-LIVE-GATE-RERUN.md` for the four items it re-runs** (all four
are re-run this time: items 1, 2 and 4 for regression against the plans 21-28 gap cycle; item 3 IN
FULL, since it is the item that has now failed twice). `34.4.1-LIVE-GATE-RERUN.md`'s own recorded
history is **not deleted** — it remains the record of the second run, including the corrections it
made to its own preconditions (F-7) and the "36 vs 0" comparator it retired. Per that document's
own rule, restated here verbatim because it is still binding: **"any item recorded FAIL means this
phase does not close ... no advisory mode, no pre-authorized override, no partial-pass closure."**

**Why this run exists.** The gate has now run twice and failed twice:
- Run 1 (`34.4.1-LIVE-GATE.md`, 2026-07-30): FAIL, 2/4 — item 1 passed the mechanism but persisted
  the session in plaintext (F-1); item 3 failed (F-6, F-5 blocked-unobservable).
- Run 2 (`34.4.1-LIVE-GATE-RERUN.md`, 2026-07-31): FAIL, 3/4 — F-1 closed live; item 3 failed again
  on **both halves**: the disconnect reported `cleared 25 humblebundle.com cookie(s)` while the jar
  shrank by 1 and all 23 Humble cookies survived (the cookie deletion silently not deleting, while
  reporting success), and a fresh login afterward auto-signed straight back in. WR-07 also failed
  (`Tauri app`, the framework default). F-4's raised half went unobserved. Two NEW unassigned
  findings surfaced mid-run: F-9 (an intermittent 60s `keyring_get` timeout hitting `humble-csrf`)
  and F-10 (Manage Accounts rendering blank on first navigation, blocking item 3 entirely for part
  of that session).

Gap cycle 2 (plans 21-28) closed all of the above **at the code level**:
- **F-6 Defect A** (plan 22) — the disconnect census now reads through a second, correctly-directed
  Rust arm (`humble_login_cookies_for_domain`), so leading-dot/subdomain-scoped Humble cookies are
  counted. Spike 016 measured this live before the fix: the clear-direction predicate matched
  **100% of the jar** (33/33), while the (unfixed) census direction matched only 29/33.
- **F-6 Defect B** (plan 23) — on macOS the clear now deletes through the live `WKWebsiteDataStore`
  (`fetchDataRecordsOfTypes_completionHandler` + `removeDataOfTypes_forDataRecords_completionHandler`
  scoped to `WKWebsiteDataTypeCookies`), never wry's `delete_cookie()` (whose `Ok(())` fires
  unconditionally regardless of whether anything was deleted — bugs.webkit.org #184938). Every
  platform now returns a count **measured by re-reading the jar after removal**, never the old
  pre-removal `matching.len()` attempted count. Epic's `clearEpicCookies` — the shared arm's second,
  already-shipped caller, unverified since Phase 34.5 — is now instrumented with the same
  measured-count + zero-count-warning discipline.
- **WR-07** (plan 24) — `humble_login_open`'s visible branch now wires Tauri's documented
  `on_document_title_changed` builder callback, so the OS title bar tracks the loaded document's
  title. The prior FALSE claim that WR-07 was "enforced by the grep gate" was corrected in-source: a
  grep gate can only prove the ABSENCE of a hard-coded title, never the PRESENCE of a tracking one.
- **F-4** (plan 24) — no code change (the platform offers only one-shot focus or a persistent pin),
  but the window's requested presentation is now logged, so this run's observation has a machine
  record to be checked against.
- **F-10** (plan 25) — root cause established by measurement: `.App .content`'s `min-height: 100%`
  resolved against `.App`'s `1fr` grid row, a circular dependency WebKit resolves by iterative
  feedback (converging on a garbage `23323.0625px` row) rather than to `auto`. The login screen was
  rendering fine, ~11,000px below the fold. Fixed by deleting the redundant declaration; hardware
  confirmed (`oversized=0`, `.loginContentWrapper` at `top=0`). A router `errorElement` was also
  added — before this, any route throw unmounted the tree to an empty document, a failure mode
  visually identical to F-10.
- **F-9** (plan 26) — `keyring_get` now runs on a worker thread bounded at `KEYRING_READ_TIMEOUT`
  (8s), rejecting a classified `keyring:timeout` well under the sidecar's 60s RPC budget. A
  hardware timing harness **REFUTED** the original "missing entry is slower" hypothesis: an absent
  Keychain entry read in 40-102ms, while the **present**, allowlisted `steam-refresh-token` entry
  stalled **48.9s then 291s**, both times failing `PlatformFailure(-60008, "Unable to obtain
  authorization for this operation")` — direct hardware evidence for the ad-hoc-code-signature /
  Keychain-ACL theory (an ad-hoc-signed dev build cannot durably retain an "Always Allow" grant, so
  every `pnpm tauri:dev` rebuild re-triggers Keychain authorization negotiation for every allowlisted
  slot). A process-lifetime read cache + in-flight dedupe was also added to
  `keyringTokenStore.ts`/`humbleSecretStore.ts`, cutting the observed 20+ reads/boot toward the
  structural floor of 3 (one per allowlisted slot: `steam-refresh-token`, `humble-session`,
  `humble-csrf`).
- **`REQ-34.4.1-GAP-13`** (plan 27) — `queryLocalFontsSafe()` guards the Chromium-only
  `queryLocalFonts` API from throwing (degrades to CSS-declared default fonts); `CachedImage` no
  longer emits an `imagecache://` URL when `imageCacheSchemeAvailable()` is false, eliminating the
  ~150 guaranteed-failing `unsupported URL` requests per library render under Tauri. **Only the
  unit-provable half is closed** — the live half (zero `unsupported URL` errors observed on a real
  build) is explicitly this gate's job.
- **Tooling** (plan 28) — a new self-tested `wkwebview-silent-noop-sweep.py` sibling script; the
  `seam-parity-sweep.py` S-07/S-10/S-11 staleness (twice logged, twice re-forwarded) is closed.

**A fully-green suite has now missed live-blocking defects on two consecutive slices** (176/3387,
then 178/3447 — see this document's own automatable-preconditions section below for this run's own
178/3447 re-measurement). Research Item 8's framing is restated: a gap analysis that enumerates what
is missing will not question what is present, and neither will a green suite. **A third green suite
is not different evidence.**

---

## What this run must do that neither prior run did (Research Item 8, restated as binding)

1. **Score item 3 against CORRECTED census counts, and require it as an explicit precondition, not
   an assumption.** Plan 22 fixed Defect A. If it had not been fixed, the census's
   `matched`/`survivingNonHumble` numbers would remain undercounted and any PASS would be
   unreliable.
2. **Re-verify the READ path, not only the delete path, inside item 3.** Both cookie arms were
   touched this cycle. A run that only checks the delete could ship a broken read without noticing.
3. **Cross-check Epic's logout**, which calls the exact same Rust arm — shared code, not merely
   similar shape. If no Epic test session exists, declare the shared-code-path argument explicitly
   as the evidence rather than implying an observation that did not happen. **Do not open a new Epic
   OAuth login for this** — D-04 scopes the mechanism's proof to Humble; a logout of an
   already-authenticated session is the only in-scope form.
4. **Observe F-4's raised half and WR-07's positive half**, both unobserved or failed across two
   runs, against plan 24's machine records so the operator's report is corroborated rather than
   standing alone.

The gated record updates (`IPC-PORT-INVENTORY.md`, `34.4.1-PORTED-CHANNELS.md`) stay withheld until
item 3 records PASS — unchanged, not reopened for debate. `autonomous: false` is unchanged and
non-negotiable: a human operator must be physically present for credential entry, 2FA, and the
qualitative "did I get signed straight back in" judgment no automated harness can make.

---

## Preconditions (inherited from `34.4.1-LIVE-GATE.md` / `34.4.1-LIVE-GATE-RERUN.md`, amended)

1. **`pnpm tauri:dev` runs from a clean tree (no stale `src-tauri/target` build artifacts).**
   **SATISFIED, evidenced in Task 1** — see "Automatable preconditions" below.
2. **Operator holds real Humble credentials.** OPERATOR-ATTESTED AT GATE TIME.
3. **Starting-credential state, decided and recorded by the operator BEFORE item 1.** As with the
   second run, this does NOT mandate starting from an absent store; the operator may choose either
   (a) **ABSENT** (from-scratch, all four locations cleared — store file, both Keychain slots, the
   WKWebView cookie jar, and WebKit's WebsiteData store — see the second run's own attempt-1 VOID
   lesson: missing the cookie jar alone silently auto-restores the session), or (b) start from the
   **CURRENT signed-in state** (see Task 1's measured evidence below — the store is presently
   `isLoggedIn: true`, keyring-backed, no plaintext secret fields; a real session left over from the
   gap cycle's own live sessions). **Whichever is chosen, it must be recorded in item 1's result.**
   If (b), item 1 cannot be scored as "from scratch" and item 1's own from-scratch requirement (see
   Item 1 below) must instead be satisfied by a deliberate disconnect-then-relogin inside item 1
   itself, or the operator must reset to (a) first.
4. **At least one UNREVEALED Humble key exists** in the connected account's library, confirmed via
   the Humble Keys page AFTER item 1's login completes, before item 4 is attempted.
5. **Sidecar log is the evidence source.** Read `~/Library/Logs/GameLib/gamelib.log`, **NEVER** the
   `pnpm tauri:dev` terminal for anything the sidecar itself logs (Rust shell `eprintln!` diagnostics
   — the two plan 24 lines, the presentation-configuration line, F-9's classified timeout message —
   only reach the `pnpm tauri:dev` terminal, since the sidecar's own stdout is the RPC pipe and
   cannot carry them; read those from the terminal specifically). **A rotation-preserving capture is
   in place — see "Evidence protection" below. Take a FRESH baseline offset at launch; any offset
   noted in this document from Task 1's own preparatory work is VOID the moment the operator
   relaunches for Task 2.**
6. ~~**A non-Humble cookie of known name and domain planted before item 3.**~~ **STRUCK AS MOOT**,
   unchanged from the second run's own Amendment C. Plan 17's jar census supplies item 3(b)'s
   evidence without it. **Do not attempt to plant one.** Left in this document rather than deleted,
   per this phase's strike-don't-delete convention.

---

## Automatable preconditions — satisfied and evidenced (Task 1, executor-run, 2026-07-31, before any human gate time)

### 1. Clean rebuild — SATISFIED, evidenced

Four stale `src-tauri/target/debug/build/gamelib-shell-*` directories were removed (the same four
directories the second run's Task 1 had already once removed, regenerated since by the gap cycle's
own `cargo build`/`cargo test` runs):
```
gamelib-shell-2302f3f7120e1323
gamelib-shell-5a4ef76a03f143b3
gamelib-shell-a51283c72838c807
gamelib-shell-c8b8f7261188fa2f
```
`cd src-tauri && cargo build` was then run directly (not `pnpm tauri:dev`, since no operator was
present to drive a launched app in this preparatory session, and a direct `cargo build` is
sufficient to force and confirm a real rebuild without needing to spawn the full shell):
```
   Compiling gamelib-shell v0.7.0 (/Users/graysonmitchell/Projects/GameLib/src-tauri)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 43.22s
```
A real `Compiling` line (43.22s, not a cached/incremental no-op) confirms the stale-artifact removal
forced an actual rebuild.

### 2. Automated baseline re-measured at current HEAD — SATISFIED, recorded verbatim

Commit at time of measurement: `979585a92` (2026-07-31, tip of gap cycle 2, plans 21-28 complete).

```
npm run test:ci
```
Exit code: **0**. Test Suites: **178 passed / 178 total**. Tests: **3447 passed / 3447 total**, 0
failed, 0 snapshots. Time: ~56-58s. **This matches plan 28's own recorded baseline exactly**
(178/3447). **No documented intermittent reproduced this run** (grep of the full run's output for
`^FAIL` returns 0; no `rustInvoke timed out after 60000ms: keyring_get` mock frame-leak; no
`depot.test.ts`/`helperProcess.test.ts` full-suite-only flake).

```
cd src-tauri && cargo test
```
Exit code: **0**. **80 passed; 0 failed; 1 ignored** (the hardware-only timing harness,
`keyring_read_timing_hypothesis_absent_vs_present_entry`, `#[ignore]`d by design — it is a manual
hardware probe, not part of the automated suite); 0 measured, 0 filtered out. Matches plan 26/27/28's
recorded baseline exactly (80/80).

**Per this document's own inherited instruction: both automated baselines are a clean 0-exit, no
caveat needed, no flake to isolate this run.**

### 3. Humble credential state — SATISFIED, recorded, verified on disk (not transcribed from the last run)

**Current live store (2026-07-31, before any Task 2 work), values redacted to type+length only per
T-34.4.1-39:**
```
$ ls -la "~/Library/Application Support/GameLib/humble_store/"
config.json                            75 bytes, mtime Jul 31 19:56
config.json.post-migration-plan13.bak  75 bytes, mtime Jul 31 06:39
config.json.pre-34.4.1-08-gate.bak    484 bytes, mtime Jul 27 12:27

config.json fields (type + length/value only, never the underlying secret):
  encryptionDegraded   bool   false
  isLoggedIn           bool   true
  expired              bool   false
  sessionCookie        ABSENT   (lives in Keychain slot humble-session)
  csrfToken            ABSENT   (lives in Keychain slot humble-csrf)
```

**Keychain, `com.gamelib.launcher`, 4 items present** (metadata only — `security find-generic-password`
without `-g`; the `-g` flag fetches the actual secret and triggers exactly the modal
authorization-negotiation prompt `deferred-items.md`'s F-9 investigation documents, which cannot be
answered non-interactively and hung for 2 minutes before being killed — do not use `-g` for any
future preparatory measurement of a protected slot):
```
acct=humble-session              cdat=mdat=20260731005055Z   (= 2026-07-31 12:50:55 local)
acct=humble-csrf                 cdat=20260731005055Z  mdat=20260731075652Z (= 2026-07-31 19:56:52 local)
acct=steam-refresh-token         cdat=mdat=20260730055937Z   (= 2026-07-30 17:59:37 local)
acct=steam-refresh-token-selfcheck  cdat=mdat=20260722021016Z  (dead cruft, pre-existing, unwritten by
                                                                any current code per deferred-items.md;
                                                                not this run's concern)
```
`humble-csrf`'s `mdat` (19:56:52 local) matches `config.json`'s own mtime (19:56) — a session was
active and refreshed the csrf token during the gap cycle's own live work (most likely plan 25's 9
cold-start F-10 diagnosis runs, which exercised repeated boots against this same signed-in session).
**Current state is a real, live, signed-in Humble session, not a from-scratch or reset state** —
this is precondition 3's option (b) starting point unless the operator resets to (a).

**Restore-path discrepancy found and recorded honestly (contradicts the second run's own
correction).** The second run's document recorded, as a correction, that only ONE Electron-era
backup was real and that a second, scratchpad-based path "does not exist." Re-checked now:
```
Durable (project app-support dir, both confirmed present, both still restorable):
  config.json.pre-34.4.1-08-gate.bak     484 B, Jul 27 12:27  (Electron-era, prefixed-ciphertext shape)
  config.json.post-migration-plan13.bak   75 B, Jul 31 06:39  (post-migration, keyring-backed shape)

Scratchpad-based (ephemeral, session-temp directories under /private/tmp/claude-501/...):
  .../0b7c97ea-.../scratchpad/humble_store-config.json.pre-gate-electron-era.bak   484 B, Jul 31 06:39
      -- byte-shape-identical to the durable Electron-era .bak above (same field lengths: 314/58)
  .../ea174e89-.../scratchpad/humble_store-config.json.backup-20260729             484 B, Jul 27 12:27
      -- ALSO EXISTS, byte-shape-identical, SAME mtime as the durable .bak
      -- the second run's document recorded this exact path as nonexistent; it is present now
```
**Do not rely on either scratchpad-based copy for a real restore** — those directories are
session-ephemeral and may be cleaned up between sessions (their continued presence here is
incidental, not guaranteed). **The two durable, project-directory `.bak` files are the load-bearing
restore paths** if the operator chooses option (a) (from-scratch) and later wants to restore a prior
state; both were stat-confirmed present above.

### 4. Confirm plan 21's throwaway spike arm is absent from the build

```
$ grep -c "spike016\|SPIKE 016" src-tauri/src/main.rs
1
```
**NOT the required `0`. Investigated, not a precondition failure.** The single hit is a
design-rationale comment at `main.rs:1502` (plan 23's own threading-decision documentation,
explaining why the cookie-clear arm does not rely on `with_webview()`'s return for synchrony,
referencing spike 016's measurement conditions for context — see `34.4.1-23-SUMMARY.md`'s own
"Decisions Made" section, which contains the identical prose). It is **not** residual scaffolding:
```
$ grep -n "spike016_cookie_probe\|spike016_wait_for_records\|GAMELIB_SPIKE016" src-tauri/src/main.rs
(zero hits)
```
The actual throwaway arm, its helper, its import and its trigger — the four things plan 22's SUMMARY
records removing — are confirmed absent by name. This document's own `main.rs` is not in plan 29's
`files_modified`, and the comment is legitimate, accurate documentation, not a defect to fix; per
this phase's established self-tripping-literal-grep pattern (plans 22/23/24 each hit and recorded an
analogous case), this is recorded as a discrepancy in the plan's own overly-literal grep pattern, not
as a failed precondition. **The functional claim the precondition exists to verify — no spike016
probe code remains in the build — is satisfied.**

### 5. Evidence protection against log rotation — SATISFIED, fresh capture in place

**A stale capture from the SECOND run was found still running** (PID 60078, a `tail -F` process
started during that run's Task 1 on 2026-07-30, never terminated, still appending to that session's
own scratchpad path `.../0b7c97ea-.../scratchpad/34.4.1-20-gate-rerun-gamelib-log-capture.log`,
which had grown to 5.6MB across the entire gap cycle). It was **terminated** (`kill 60078`,
confirmed via a follow-up `ps aux` showing zero `tail -F` processes) because it was not scoped to
this run and its target lives in a prior session's scratchpad directory.

**A fresh capture was started for this run specifically:**
```
/private/tmp/claude-501/-Users-graysonmitchell-Projects-GameLib/fc9f47a4-9fe2-4d27-8489-87ecc2635e46/scratchpad/34.4.1-29-gate-rerun3-gamelib-log-capture.log
```
Verified running (PID `55892`, confirmed via `lsof -p 55892` showing it has `gamelib.log` open for
read and the capture file open for append) and confirmed already containing the current
`gamelib.log`'s full content (359,462 bytes / 2,465 lines at capture-start) — this exists so
`LogWriter#archiveOldLogFile()`'s rotate-on-first-write-per-process behavior (confirmed source-level
by the second run's own Task 1: renames `gamelib.log` to `.old`, **overwriting any existing
`.old`**, on the very first write of every new `pnpm tauri:dev` process) cannot destroy any earlier
part of this run's own evidence, exactly as it protected the second run.

**Instructions for the operator (Task 2), unchanged from the second run's own instruction:** grep or
read this capture file, not `gamelib.log` directly, whenever a check needs to look back across a
relaunch. `gamelib.log` itself is still the right file to `tail -f` live during any single session.
If the capture process is found not running at the start of Task 2 (check with
`ps aux | grep "tail -F"`), restart it:
```
nohup tail -F -n +1 ~/Library/Logs/GameLib/gamelib.log >> /private/tmp/claude-501/-Users-graysonmitchell-Projects-GameLib/fc9f47a4-9fe2-4d27-8489-87ecc2635e46/scratchpad/34.4.1-29-gate-rerun3-gamelib-log-capture.log 2>&1 &
disown
```
**Take a FRESH baseline offset (line count or byte offset into the capture file) at the moment Task
2 actually begins driving the app — not from anything recorded above, which is now void the moment
you relaunch.** Reminder from precondition 5: this capture file only ever contains what
`gamelib.log` itself contains (the sidecar's own logging). The Rust shell's `eprintln!` diagnostics
(plan 24's title-change/presentation-config lines, F-9's classified timeout message) are **not**
written to `gamelib.log` at all — read those from the live `pnpm tauri:dev` terminal directly.

---

## Items

### Pre-check, before Item 1 — F-10 render + `unsupported URL` count

**What to do.** On a cold start (fresh `pnpm tauri:dev` launch), navigate to Manage Accounts for the
FIRST time this session. Separately, let the Library render and observe the WKWebView's Web
Inspector / console output (read-only view is sufficient — per `34.4.1-F10-DIAGNOSIS.md`'s own
recorded finding, the Web Inspector console does not accept keyboard input against this WKWebView,
but its output can still be read) for the literal substring `unsupported URL`.

**What to observe:**
- Does Manage Accounts render its content immediately, or does it appear blank? F-10 is recorded
  fixed (plan 25, hardware-confirmed) — this is a regression pre-check, not a re-diagnosis. If it is
  blank, record it and retry once before abandoning; the finding is recorded either way, and it
  blocked an entire gate item last time (item 3 was unreachable) — catching a recurrence here before
  spending the operator's credential/2FA time is the entire point of this pre-check.
- Count of `unsupported URL` occurrences during a full Library render. Expected, per plan 27's fix:
  **zero**. This is `REQ-34.4.1-GAP-13`'s live half — the only part of that requirement plan 27
  could not close by unit test alone.

**Evidence required:** whether Manage Accounts rendered first-time (yes/no, and if no, whether a
retry recovered it), and the `unsupported URL` count (a number, or "not observed" if the console
could not be checked).

**Result:** PASS (render half) — recorded live 2026-07-31, run 3.

- **Manage Accounts rendered first-time on cold start: YES.** No blank window, no retry needed.
  F-10 does not reproduce. Plan 25's fix holds on hardware for a second time.
- **`unsupported URL` count: NOT YET CONFIRMED.** The operator reported the pre-check as passed but
  did not separately state a console count. Per this document's own rule ("a number, or 'not
  observed'"), this is recorded as outstanding rather than rounded to zero — `REQ-34.4.1-GAP-13`'s
  live half is NOT closed by this entry as it stands. See the outstanding-evidence note in Item 1.

---

### Item 1 — Humble login completed, keyring-backed persistence, WR-07 + F-4 observed (REQ-34.4.1-02, REQ-34.4.1-12, REQ-34.4.1-GAP-02, REQ-34.4.1-GAP-09) — RE-RUN IN FULL / REGRESSION

**What to do.** Per precondition 3's operator choice:
- If (a) from-scratch: clear all four locations (store file, both Keychain slots, the WKWebView
  cookie jar `~/Library/HTTPStorages/gamelib-shell.binarycookies`, and
  `~/Library/WebKit/com.gamelib.shell/WebsiteData`) — back up first, never destroy without a copy —
  then drive a real Humble login (credentials + any Humble Guard / OTP step).
- If (b) starting from the current signed-in state (see Task 1's measured evidence above): trigger
  Humble disconnect FIRST (this also begins exercising item 3 — record which path was taken and do
  not double-count the same disconnect as both item 1's reset and item 3's own trigger; if in doubt,
  do a full from-scratch reset instead so item 1 and item 3 stay cleanly separated).
Record which starting-state option was actually used.

**What to observe:**
- **WR-07 (REQ-34.4.1-GAP-09, half 1 of 2).** Read the title bar. Record its EXACT text. Then check
  the `pnpm tauri:dev` terminal (not `gamelib.log` — this diagnostic is a Rust `eprintln!`, per
  precondition 5's reminder) for plan 24's title-change diagnostic:
  ```
  [shell] humble_login_open: title change applied len={N}
  ```
  Record BOTH the title bar's exact text and whether this diagnostic fired (and its length value if
  so). **A diagnostic that fired while the bar still reads `Tauri app` is a different finding from a
  diagnostic that never fired at all** — record whichever actually happened, do not conflate them.
- **F-4 (REQ-34.4.1-GAP-09, half 2 of 2), both parts.** (a) Does the window come to the front and
  take keyboard focus the moment it is created? (b) Switch to another application (a password
  manager is the realistic case) and return: does the login window need an extra click to refocus,
  or show any stale still-focused state? Record both in your own words. Also check the terminal for
  plan 24's presentation-configuration line (fixed text, no format args):
  ```
  [shell] humble_login_open: presentation requested visible=true width=900 height=700 center=true focus_once=true persistent_pin=false
  ```
  This records only what was REQUESTED — it does not prove what was actually seen on screen. Record
  its presence/absence alongside your own observation of (a) and (b).
- Confirm plan 13's keyring-install line is present in `gamelib.log` (via the rotation-safe capture
  file):
  ```
  [bootstrap] Humble secret store installed: keyring-backed (humble-session/humble-csrf slots)
  ```
  **If absent, item 1 FAILS regardless of the store contents.**
- **F-9 watch (not scored, F-9 is open and unassigned for closure):** does a `keyring:timeout` or any
  generic RPC timeout appear anywhere during this item? If so, record whether it co-occurred with a
  cookie operation in the same session (research's specific question). The classified message, if
  it fires, reads (in the terminal, not `gamelib.log`):
  ```
  [shell] keyring {channel} timed out after 8s (worker thread abandoned, not cancelled -- see KEYRING_READ_TIMEOUT's doc comment)
  ```

**Evidence required (unchanged from the second run's Amendment A):**
1. The store file's field types and lengths only (never values) — PASS requires **NO
   `sessionCookie` field** and **`encryptionDegraded: false`**.
2. The keyring-install log line's presence or absence, stated explicitly.
3. Which starting-state option (a/b) was used, recorded here.
4. The title bar's exact text AND the title-change diagnostic's presence/absence (WR-07).
5. F-4's two-part observation AND the presentation-configuration line's presence/absence.

**Prior results, cited not assumed:** Run 2 PASSED items 1's core evidence (attempt 2: no
`sessionCookie`, `encryptionDegraded: false`, keyring-install line present, genuine credential+2FA
login with poll activity). WR-07 FAILED in run 2 (title read `Tauri app`). F-4's sizing/centring was
OBSERVED CORRECT in run 2 attempt 2; the raised half was UNOBSERVED in both runs. **This run must
re-observe all of it against plan 24's code changes** — nothing here may be carried forward as
still-true.

**Result:** IN PROGRESS — partial observations recorded live 2026-07-31, run 3. **Not scored yet;
five pieces of required evidence are still outstanding (listed at the end of this entry).**

**WR-07 — title bar (evidence 4, partial).** The title bar **did change**. The operator watched the
transition happen: the bar first read `Tauri app`, then corrected to the document title while under
observation. This is materially different from run 2, where it read `Tauri app` and stayed there.

Recorded honestly as a **qualified result, not a clean PASS**: the mechanism now fires, but there is
a visible window during which the user sees `Tauri app` before the correction lands. Whether that
transient is acceptable is a scoring decision, not an observation — the observation is that it is
visible to the naked eye. Still outstanding: the bar's **exact final text**, and whether plan 24's
`[shell] humble_login_open: title change applied len={N}` diagnostic fired (with its `len` value).
Per this item's own rule, "a diagnostic that fired while the bar still reads `Tauri app`" is a
distinct finding — here the bar did *not* stay at `Tauri app`, so this is a third shape neither
prior run produced.

**F-4 half (a) — raise and focus on create: PASS, and newly observed.** The window comes to the
front and takes keyboard focus the moment it appears. This half was **UNOBSERVED in both prior
runs** — it is observed here for the first time, and it is correct.

**F-4 half (b) — return-from-another-app: FINDING.** In the operator's own words: navigating away
and then clicking on the app does not bring the login window back — *"it is independant from the
app."* The login window does not travel with the main application's activation. This is not the
"extra click to refocus" shape the contract anticipated; it is a window-grouping/ownership
observation — the login window behaves as an unrelated top-level window rather than one owned by
the app. Recorded as observed, not diagnosed. **Fix nothing at gate time.**

**NEW FINDING — Manage Accounts does not self-refresh after a successful sign-in.** Not anticipated
by this contract; surfaced during item 1 and recorded here because that is where it happened.

After sign-in completed successfully, the Manage Accounts page **did not update**. It remained
showing its in-progress state:

> Signing in to Humble Bundle
> A sign-in window has opened. Complete sign-in there — this page updates automatically once it
> succeeds.

The page's own promise — *"this page updates automatically once it succeeds"* — was not honoured.
Navigating away and back **does** load correctly and shows Humble as connected, so this is a stale
view, **not** a lost session or a failed login: the underlying auth state is correct and persisted.

Severity is UX-blocking-but-not-data-affecting: a user following the on-screen instruction has no
indication their login worked, and the screen actively tells them to keep waiting for something that
will never arrive. Needs a findings-register entry and a next-cycle assignment. **Not fixed here**
(this task is run-and-record; plans 08 and 20 both declined to invent gate-time fixes).

**Evidence 1 — store file field types and lengths: PASS.** Read on disk 2026-07-31 during this item,
values never fetched (type/length only per T-34.4.1-39):
```
~/Library/Application Support/GameLib/humble_store/config.json   75 bytes
  encryptionDegraded   bool   false      <- PASS condition met
  isLoggedIn           bool   true
  expired              bool   false
  sessionCookie        ABSENT            <- PASS condition met (lives in Keychain humble-session)
  csrfToken            ABSENT            (lives in Keychain humble-csrf)
```
Both PASS conditions are met: no `sessionCookie` field, `encryptionDegraded: false`. The 75-byte
keyring-backed shape matches precondition 3's measurement and run 2's regression baseline.

**Evidence 2 — keyring-install line: PRESENT.** Found in `gamelib.log`, this session's boot:
```
(21:58:28) [INFO]: [Backend]: [bootstrap] Humble secret store installed: keyring-backed (humble-session/humble-csrf slots)
```
The install did **not** silently degrade to the plaintext default. Item 1's hard-fail condition on
this line is not triggered.

**F-9 watch (not scored): NOT CLEAN — an RPC timeout DID fire.**

**Correction, recorded rather than silently overwritten.** This entry first read "CLEAN — zero
occurrences." That reading was taken from `gamelib.log` **only**, and was wrong for exactly the
reason precondition 5 warns about: the shell's `eprintln!` diagnostics never reach the log file. The
terminal shows:
```
[shell] response for unknown/timed-out id=1575 (dropped)
```
A request timed out, was abandoned, and its response arrived afterwards to find no waiter. The
`gamelib.log` grep could not have seen this and its silence proved nothing.

Standing state:
- `keyring:timeout` (plan 26's classified 8s message): **not observed** in either source.
- **A generic RPC timeout: OBSERVED**, `id=1575`, response dropped on late arrival.
- **Co-occurrence with a cookie operation: UNDETERMINED.** This is the contract's specific
  question and it cannot be answered from the single line above — `id=1575` is a correlation id
  with no channel name attached. Answering it requires locating the request that opened `id=1575`
  in the same scrollback. Recorded as undetermined; **not** rounded to "no".

This is the F-9 investigation's exact subject appearing live, in the one item that was supposed to
be a clean regression pass. It does not fail item 1 (F-9 is open and unassigned for closure, and
this item's scored evidence is unaffected), but it is a real observation that both prior runs' F-9
notes wanted and neither got.

**Evidence 3 — starting state: option (a), FULL FROM-SCRATCH RESET.** Confirmed by the operator.
Item 1 and item 3 therefore stay **cleanly separated**: no disconnect was consumed as item 1's
reset, so item 3's `humbleDisconnect` trigger remains entirely unspent and its census will be read
against a jar built by this run's own genuine login. This is the stricter of the two permitted
starting states and removes the double-counting hazard the contract warned about.

**Evidence 4 (title-bar half) — exact final text: `Humble Bundle`.** This is Humble's own document
title, not the `Tauri app` default. **The end-state requirement of WR-07 is met**, and this is a
direct reversal of run 2, where the bar read `Tauri app` and never corrected.

WR-07 therefore resolves as: **end state correct, transition visible.** The title arrives at the
right value; a user watching the window at creation sees `Tauri app` first. Recorded as a real but
cosmetic residual, distinct from run 2's outright failure. It is a finding for the next cycle's
consideration, not an item-1 FAIL — the requirement's substance (the window identifies the site it
has loaded) is satisfied.

**Evidence 4 (diagnostic half) — the title-change diagnostic FIRED THREE TIMES, and one of them
applied an EMPTY title.** Verbatim from the terminal:
```
[shell] humble_login_open: title change applied len=22
[shell] humble_login_open: title change applied len=0
[shell] humble_login_open: title change applied len=42
```
Plan 24's mechanism is unambiguously live and firing — that half of WR-07 is proven working, against
run 2 where the title never changed at all.

**Two things follow, and the second is new.**

**(i) None of the three lengths matches the reported title bar text.** `Humble Bundle` is 13
characters; the applied lengths are 22, 0 and 42. The last applied title (42) is over three times
the length of what the operator read off the bar. Both readings fit the evidence so far — either the
reported text was an abbreviation of a longer exact string, or the bar genuinely diverges from the
last applied title — and **this document does not choose between them.** The exact final text is
requested below as the discriminator. This is precisely why the `len` was not inferred from the
title string when only the title was known.

**(ii) A zero-length title is applied mid-sequence (`len=0`).** The shell forwards WebKit's reported
document title without a non-empty guard, so a transient empty title during navigation is applied to
the window as-is.

**Hypothesis, explicitly NOT a conclusion:** a window whose title is set to the empty string may
fall back to displaying the application default — which is `Tauri app`, exactly the string the
operator watched flash before the correction landed. The `len=0` line and the observed flash are
consistent with each other and no third observation currently separates them.

**This is recorded as a hypothesis with a named discriminator, not shipped as the cause.** The
governing lesson is on the record from F-10's own diagnosis: *when two readings of a measurement
both fit, build the discriminator instead of shipping the nicer one* — a correlation was shipped as
a cause there and cost nine live runs. The competing reading here is mundane: the flash may simply
be the window's initial pre-navigation state, with `len=0` arriving harmlessly at some other moment.

**Discriminator (for the next cycle, NOT to be built during this gate):** timestamp the three
diagnostic lines against the moment the bar visibly reads `Tauri app`. If the flash coincides with
`len=0`, the empty-title application is the cause and the fix is a non-empty guard before applying.
If the flash precedes the first diagnostic entirely, `len=0` is incidental and the flash is just
pre-navigation state. Nothing here may be fixed at gate time.

**Exact title bar text — reading (i) RESOLVED, and the discriminator killed the hypothesis.**

The bar reads, character for character:
```
Humble Bundle - Log In      <- 22 characters, counted
```
**This is an exact match to the first diagnostic, `len=22`.** There is **no divergence between the
applied title and the displayed title**. The earlier `Humble Bundle` report was the operator's
shorthand for this fuller string, which is reading (i)'s mundane branch. Plan 24's mechanism is
coherent end to end: WebKit's document title reaches the window intact and correct.

**Reading (ii) — the `len=0` hypothesis is now DISFAVOURED by ordering, and it was mine.** The
sequence is `len=22` → `len=0` → `len=42`, so the *first* title ever applied is the 22-character one
the bar displays. The operator's own account of the flash was one-way and complete: *`Tauri app`,
then corrected while watching.* That places the flash **before** the first title application —
therefore before `len=0`, which fires only after the bar already showed the correct title. Had
`len=0` caused the fallback, the bar would have had to flash **back** to `Tauri app` after already
reading `Humble Bundle - Log In`, and no such second transition was reported.

**Conclusion: the `Tauri app` flash is the window's initial pre-title state, not the empty-title
application.** The nicer story — a discoverable empty-title bug elegantly explaining the visible
symptom — was the wrong one, and the discriminator named in the previous entry is what disposed of
it, at zero live-run cost. Recorded in full rather than quietly deleted, because a hypothesis that
was built, tested and rejected is part of this run's evidence.

**`len=0` survives as a latent code smell, unpromoted.** An empty document title is still forwarded
to the window with no non-empty guard. It was **not observed to cause any visible defect** in this
run, and the operator was not watching for a second flash at the moment it fired. Worth a next-cycle
note on its own merits; **not** the cause of anything seen here, and not to be written up as one.

**WR-07 (REQ-34.4.1-GAP-09, half 1 of 2): PASS.** The title bar shows Humble's own document title,
the diagnostic fired, and applied text and displayed text match exactly. Run 2's failure (bar stuck
at `Tauri app`, no diagnostic) is fully reversed.

**One minor point left unobserved, recorded as such:** the last applied title was `len=42`, but the
bar was read while showing the `len=22` login-page title — consistent with the reading being taken
during login, before the post-login navigation that presumably produced the 42-character title.
Whether the bar tracked that final update was **not observed**. It is not required by any acceptance
criterion and does not affect the PASS above.

**Evidence 5 (presentation half) — the line is PRESENT, verbatim and unmodified:**
```
[shell] humble_login_open: presentation requested visible=true width=900 height=700 center=true focus_once=true persistent_pin=false
[shell] humble_login_open: title change applied len=22
```

**Ordering corroborates the flash conclusion independently.** The presentation request precedes the
first title application in the same scrollback: the window is created and presented **first**, and
only then does a title arrive. The interval between those two lines is exactly the window in which a
titleless window displays the application default `Tauri app`. This is a second, independent line of
evidence for the same conclusion the `len=0` discriminator reached — arrived at from ordering rather
than from the operator's recollection.

**F-4 — the requested presentation MATCHES the observed behaviour on both halves, and that closes
the question rather than opening a defect.**

| Requested flag | Observed behaviour | Agreement |
|---|---|---|
| `focus_once=true` | window came to front and took keyboard focus on create (half a) | ✅ exact |
| `persistent_pin=false` | window did **not** travel with the app on re-activation (half b) | ✅ exact |
| `width=900 height=700 center=true` | sizing/centring correct (consistent with run 2 attempt 2) | ✅ |

**Half (b) is therefore not a defect — it is the recorded, intended consequence of the configuration
this plan deliberately chose.** This plan's own `<what-built>` states the position plainly: *"F-4 —
no code change (the platform offers only one-shot focus or a persistent pin), but the window's
requested presentation is now logged so your observation has something to be checked against."* The
platform offers a binary choice; `persistent_pin=false` was selected; a window that does not
persistently pin does not follow the app forward on re-activation. The operator's independent
description — *"it is independant from the app"* — is a precise natural-language restatement of
`persistent_pin=false`.

**This is exactly the job the instrumentation was added to do.** Both prior runs left F-4's raised
half UNOBSERVED and had nothing to check an observation against. This run observed both halves and
found them in exact agreement with what the code requested. The earlier provisional entry above,
which recorded half (b) as a bare "finding", is **superseded by this reconciliation** — it was
recorded before the presentation line was available to check it against, and it is left in place
rather than rewritten so the order of discovery stays visible.

**F-4 (REQ-34.4.1-GAP-09, half 2 of 2): PASS** — requested presentation observed to match actual
behaviour on both halves. No code change was owed and none is owed now.

---

## ITEM 1 VERDICT: **PASS**

All five required evidence items are satisfied:

| # | Evidence | Result |
|---|---|---|
| 1 | Store field types/lengths — no `sessionCookie`, `encryptionDegraded: false` | ✅ PASS |
| 2 | Keyring-install line present (hard-fail condition not triggered) | ✅ PASS |
| 3 | Starting state recorded — option (a), full from-scratch | ✅ recorded |
| 4 | Title bar exact text + diagnostic fired — `Humble Bundle - Log In`, len=22 exact match | ✅ PASS |
| 5 | F-4 two-part observation + presentation line present and in agreement | ✅ PASS |

A genuine from-scratch credential login was completed, the session is keyring-backed with no
plaintext fallback, **WR-07 is closed** (run 2's failure reversed), and **F-4 is closed** (both
halves observed for the first time, both matching the requested presentation).

**Non-blocking observations carried out of item 1 for the findings register** — none of these affect
the PASS above, and **none were fixed** (run-and-record):

1. **Manage Accounts does not self-refresh after successful sign-in** (NEW, not anticipated by this
   contract). Stale view only; auth state correct and persisted. The page's own text promises an
   automatic update that never arrives. Needs a register entry and a next-cycle assignment.
2. **`len=0` — unguarded empty document title** forwarded to the window. Latent smell; **refuted**
   as the cause of the `Tauri app` flash, and must not be written up as such.
3. **`Tauri app` visible between window presentation and first title application.** Cosmetic;
   root cause now established from ordering evidence.
4. **Generic RPC timeout `id=1575`, response dropped on late arrival.** Co-occurrence with a cookie
   operation UNDETERMINED — `id=1575` carries no channel name. F-9's subject, live.
5. **Bar not observed tracking the final `len=42` title.** Not required by any acceptance criterion.

Carried forward and still open: the `unsupported URL` count from the Library render (pre-check's
second half, `REQ-34.4.1-GAP-13`'s live half) — a number, or an explicit "not observed".

---

### Item 2 — Session survives quit-and-relaunch, against the current keyring-backed store (REQ-34.4.1-12) — REGRESSION RE-RUN

**What to do.** Immediately after item 1, capture the "before" store snapshot. Fully quit the Tauri
app (not just close the window) and relaunch `pnpm tauri:dev`. Confirm the relaunch independently
(fresh PIDs via `ps aux`, a fresh `[bootstrap] Humble secret store installed` log line). Capture the
"after" snapshot. **Both readings must be taken INSIDE this item, before item 3 begins** (F-7 — the
run-2 process lesson: the run-1 original gate's first attempt at this item had its "after" destroyed
by item 3 starting before it was captured, and could not be reconstructed).

**What to observe:** The Humble session is still signed in after relaunch, no re-login prompt, no
`Humble login validation rejected candidate session` lines anywhere in the post-relaunch capture
window. Watch for `keyring:timeout` (plan 26) around the relaunch specifically — this is exactly the
kind of boot the F-9 investigation's read-count reduction should be observable on.

**Evidence required:** the store's field types/lengths (never values) from BOTH the "before" and
"after" reads, both taken inside this item, plus independent relaunch confirmation (new PID, fresh
bootstrap line).

**Prior result (regression baseline, cited per the second run's own Amendment D):** PASS (run 2,
2026-07-31) — 75B store, unchanged across relaunch (`encryptionDegraded: false`, no `sessionCookie`,
`isLoggedIn: true`), zero re-auth polls, new PID confirmed. **This exact store shape may or may not
still be current** depending on what happened in gap cycle 2's own live sessions since (Task 1's own
measurement above shows the store is presently in this same healthy shape) — re-observe fresh rather
than citing the number alone.

**Result:**

_(EMPTY — record here during Task 2)_

---

### Item 3 — `humbleDisconnect` leaves no `humblebundle.com` cookie AND clears storage; re-login is genuinely fresh; read path re-verified; Epic cross-checked (REQ-34.4.1-06, REQ-34.4.1-12, REQ-34.4.1-GAP-03, REQ-34.4.1-GAP-05, REQ-34.4.1-GAP-07, REQ-34.4.1-GAP-08) — RE-RUN IN FULL, THE ITEM THAT FAILED TWICE

**Explicit precondition, stated per research Item 8 #1 (this run's own binding addition): Defect A
must be fixed before this item can be scored at all.** Plan 22 closed Defect A (the census now reads
through `humble_login_cookies_for_domain`, the correctly-directed arm — see this document's own "why
this run exists" section above, and `34.4.1-22-SUMMARY.md`). **If, on inspection during this item,
the census log line still shows the OLD undercounting shape (i.e. `matched-before` values
inconsistent with a correctly-directed read), STOP and record item 3 as UNSCORABLE rather than
scoring a PASS/FAIL against unreliable numbers** — an undercounted `matched-before` makes all three
downstream equalities unreliable even when they appear to hold.

**What to do.** From the signed-in state (item 1/2), trigger Humble disconnect.

**Read-path re-verification (research Item 8 #2, new this run) — check FIRST, before scoring
anything else in this item.** Find the census line's BEFORE-read `total` and verdict:
```
Humble disconnect: cookie census before(total=T0, matched=M0, verdict=V0) after(total=T1, matched=M1, verdict=V1) deleted=D survivingNonHumble=S
```
**`T0` must be non-zero and `V0` must be `SUPPORTED_NONEMPTY`.** A `total` of 0 or a verdict of
`UNDECIDABLE`/`SUPPORTED_BUT_EMPTY` means the READ path itself broke — since both cookie arms
(read and delete) were touched this cycle, a broken read could silently coexist with a working
delete, and the item FAILS on this alone regardless of what the delete arm does. Record `T0` and
`V0` explicitly before proceeding.

**What to observe, part (b) — the census arithmetic (score this FIRST, per the second run's own
precedent — it is the objective half):**
```
Humble disconnect: cookie census before(total=T0, matched=M0, verdict=V0) after(total=T1, matched=M1, verdict=V1) deleted=D survivingNonHumble=S
```
PASS requires ALL THREE: `matched` in the `after` term is `0`; the jar shrank by exactly the
matched-before count (`T0 - T1 == D`); `S` (survivingNonHumble) is **non-zero**. Record all four
numbers (`T0`, `M0`, `T1`/`M1`, `D`, `S`) and both verdicts (`V0`, `V1`) explicitly. If the
discrepancy warning fired instead:
```
Humble disconnect: cookie census discrepancy — clear may not have been domain-scoped: <failure 1>; <failure 2>; ...
```
record which named equality failed, verbatim.

**What to observe, part (a) — the behavioural half:**
- The per-category storage-clear report is present (each category a number or the literal
  `unsupported`, never a coerced `0`).
- **Attempt a fresh login and confirm you land on Humble's own login form, not an auto-signed-in
  page.** Twice now, a broken disconnect has produced a 3-second auto-restore with **zero**
  `session_expired`/poll lines, versus a genuine login's ~3 logged lines carrying ~17 rejections over
  ~1.3 min (plan 18's F-2 collapsed-poll-logging shape — **the obsolete literal "36" is retired,
  do not reuse it**). **That qualitative contrast is the measurement.** If you are signed straight
  back in again, F-6 is NOT fixed — record it exactly as observed, do not round it up.

**If the reported delete count and the census arithmetic ever disagree, the census wins and the item
FAILS.** A mutating call's own report can never outrank an independent re-read (plan 23's count is
now measured by a post-removal re-read, but the census remains the independent oracle, not a
corroborating one).

**Epic cross-check (research Item 8 #3, new this run).** Two acceptable outcomes, spelled out in
advance — record honestly whichever actually applies:
- **If an authenticated Epic (`legendary`) test session is available:** log it out and record the
  measured-count line and whether the removed-nothing warning fired:
  ```
  Legendary logout: cleared {N} epicgames.com cookie(s) (measured post-removal delta)
  Legendary logout: domain-scoped cookie clear removed nothing for epicgames.com   <- only if N==0
  ```
- **If no Epic test session is available:** write exactly that — no Epic test session was available
  this run, and the evidence for the shared arm's second caller is the shared-code-path argument
  only (both call sites route through the identical `humble_login_clear_cookies` Rust arm; fixing
  the arm fixes both structurally), not an independent observation. **Do not open a new Epic OAuth
  login for this** — that is Phase 34.5's scope, out of bounds for this gate (D-04).

**Evidence required:** `T0`/`V0` (read-path check), the four census numbers plus both verdicts, the
per-category storage report, the measured clear count, the re-login outcome in the operator's own
words, and the Epic cross-check's disposition (observed, or explicitly declared unavailable).

**Prior results:** Run 1: FAILED, 3(b) BLOCKED-UNOBSERVABLE. Run 2: **FAILED on both halves** — clear
reported `cleared 25 humblebundle.com cookie(s)`, jar shrank by 1, all 23 Humble cookies survived;
re-login auto-signed back in with zero poll lines; WR-07 also failed in this item's window (`Tauri
app`). **This run is the only evidence that can close F-6 either way.**

**Result:**

_(EMPTY — record here during Task 2)_

---

### Item 4 — `humbleRevealKey`'s real outcome, transport label regression check (REQ-34.4.1-05, REQ-34.4.1-12) — REGRESSION RE-RUN

**What to do.** Using the precondition-4 unrevealed key, attempt a real reveal through the
in-webview POST mechanism (D-07/D-08).

**What to observe:** Either the reveal succeeds (a real key value returned) or fails with a
declared, classified outcome (e.g. a Cloudflare 403 — **still a VALID, DECLARABLE result**, per
D-07's framing). Confirm the calling-adapter log line names the transport that actually ran:
```
Humble reveal: calling adapter (login-window seam transport), csrfTokenPresent=true ...
```
**not** the stale hardcoded `"electron-net transport"` literal (F-8, fixed run-1's gate cycle) —
under Tauri it must read `login-window seam transport`.

**Evidence required:** the actual HTTP outcome, the classified result surfaced to the UI, the
transport label text observed in the log. **Never record the revealed value itself** (T-34.4.1-39).

**Prior result (regression baseline, cited per the second run's own Amendment D):** PASS (run 2,
2026-07-31, 12:56:17) — a real key value was returned (not a 403); `login-window seam transport` ×1,
`electron-net transport` ×0. Caveat recorded honestly by run 2: that reveal ran against a session
item 3 had auto-restored, not a freshly-authenticated one — immaterial for a transport-label
regression check, but not a clean-room result. **This run's item 4 should run against whatever
session item 1/2/3 of THIS run actually produced.**

**Result:**

_(EMPTY — record here during Task 2)_

---

## Verdict

_(EMPTY — Task 3 records the per-item table, the overall result, an updated findings register, and
— on any FAIL — the next gap cycle's blocking finding, per this document's own binding rule: any item
recorded FAIL means this phase does not close, no advisory mode, no pre-authorized override, no
partial-pass closure.)_

---

## Findings register carried in from `34.4.1-LIVE-GATE-RERUN.md`, status as of this run's start

| id | status entering this run | code-level fix this cycle | this run's job |
|---|---|---|---|
| F-1 | CLOSED (live-proven, run 2) | — | regression-check only (item 1/2) |
| F-2 | CLOSED (live-proven, run 2) | — | regression-check only (item 1 poll shape) |
| F-3 | CLOSED as scoped (run 2) | — | not this run's concern |
| F-4 | PARTIAL — sizing/centring proven; raised half UNOBSERVED (2 runs) | plan 24 (log record only, no code change) | **observe the raised half, both parts** |
| F-5 | CLOSED (census exists, observable, run 2) | — | regression-check only |
| F-6 | **OPEN, BLOCKING** — cookie deletion does not delete (FAILED twice) | plans 22 (Defect A) + 23 (Defect B) | **the item this run exists to re-score, in full** |
| F-8 | CLOSED (transport label, run 2) | — | regression-check only (item 4) |
| F-9 | OPEN, unassigned for closure | plan 26 (timeout classification + read cache) | **watch only, not scored** — does `keyring:timeout` fire, and does it co-occur with a cookie op |
| F-10 | OPEN, BLOCKING item 3 for part of run 2 | plan 25 (root cause fixed, hardware-confirmed) | **regression pre-check before item 1** |
| WR-07 | FAILED (run 2 — `Tauri app`) | plan 24 (title-tracking hook) | **re-observe, paired with the title-change diagnostic** |
| REQ-34.4.1-GAP-13 | unassigned (new this cycle) | plan 27 (unit half only) | **the live half — zero `unsupported URL` errors** |
| — | `seam-parity-sweep.py` staleness | plan 28 (closed) | not this run's concern |

`IPC-PORT-INVENTORY.md` and `34.4.1-PORTED-CHANNELS.md` remain **NOT** updated — those edits stay
gated on item 3 PASS, and it has not yet passed as of this contract's writing.
