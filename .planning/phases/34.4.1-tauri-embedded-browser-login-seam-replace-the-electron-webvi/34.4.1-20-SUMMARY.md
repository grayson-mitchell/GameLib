---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 20
completed: 2026-07-31
gate_verdict: FAIL
items_passed: 3
items_failed: 1
phase_closes: false
---

# 34.4.1-20 — Blocking live-gate re-run

**Plan complete; phase does NOT close.** All three tasks executed and the gate ran in full on real
hardware against the real Humble origin. The verdict is **FAIL, 3 of 4 clean** — item 3 failed, and
this gate's binding no-partial-pass rule keeps Phase 34.4.1 open.

Full evidence: `34.4.1-LIVE-GATE-RERUN.md` (frontmatter `verdict: FAIL`).

## What was done

- **Task 1** (`146af9c71`) — re-run contract written with empty result slots before any live work;
  clean rebuild; rotation-preserving `tail -F` capture (which then survived three real rotations and
  preserved evidence that would otherwise have been lost).
- **Task 2** — all four items run live.
- **Task 3** — verdict recorded; **only** the updates the verdict permits were applied.
  `IPC-PORT-INVENTORY.md` and `34.4.1-PORTED-CHANNELS.md` were **NOT** touched — those edits were
  gated on item 3 passing.

## Results

| Item | Result |
|---|---|
| 1 — login from scratch, keyring-backed | **PASS** (attempt 2; attempt 1 VOID) |
| 2 — survives quit-and-relaunch | **PASS** |
| 3 — disconnect actually disconnects | **FAIL** (a and b) |
| 4 — reveal + transport label | **PASS** |

**F-1 CLOSED, live-proven on the write path** — Keychain slots deleted pre-boot and recreated by a
credential+2FA login; store carries no `sessionCookie`/`csrfToken` and `encryptionDegraded: false`.
Stronger than migration evidence. F-2, F-3, F-5, F-8 also confirmed closed live.

**F-6 NOT CLOSED.** The four wipe steps the gap cycle *built* all work live (localStorage=32,
IndexedDB=1, Keychain cleared, store `{}`). The **pre-existing** cookie deletion reports
`cleared 25 humblebundle.com cookie(s)` while the jar shrinks by 1 and **all 23 Humble cookies
survive**; re-login auto-signs back in with zero poll lines.

## Corrections made to the gate record itself

1. **Precondition 3 was wrong when written** — it recorded a 353 B plaintext store and asserted the
   migration "has not yet had the chance to run", but the rebuild launch recorded in precondition 1
   of the same document had already triggered it. **This is F-7, the exact error this plan was
   written to avoid.** Corrected, with the destroyed migration evidence recovered and preserved.
2. **Only one recovery backup existed, not two** — the second path pointed into a nonexistent
   session scratchpad, so the claimed `cmp` verification could not have run.
3. **Item 3's "36 vs 0" comparator is obsolete** — plan 18's F-2 fix collapsed poll logging, so a
   genuine login now emits ~3 lines carrying ~17 rejections. Item 3 was scored on the qualitative
   contrast instead, using attempt 1's accidental auto-restore as the modern baseline.
4. **F-9's scope was corrected** from Steam-only to also hitting `humble-csrf`.

## Lessons

- **A gap analysis that enumerates what is MISSING will not question what is PRESENT.** F-6's
  five-vs-one framing aimed every plan at the four absent steps; nothing verified the one already
  there, and that one was broken.
- **Plan 17's census earned its place on its first live run** — it was built to make an
  unobservable claim observable and immediately caught a real defect a green 3387-test suite could
  not.
- **Third instance of resolves-without-doing-the-work under WKWebView** (after
  `navigator.clipboard`, and now `queryLocalFonts` throwing). Argues for a sweep of that class.

## Carried forward (see LIVE-GATE-RERUN.md § Findings register)

Blocking next cycle: **F-6** (cookie deletion), **WR-07** (title reads `Tauri app`), **F-4** raised
half unobserved. Unassigned: **F-9** (60 s `keyring_get` timeout on `humble-csrf`), **F-10**
(Manage Accounts blank on first navigation to the lazy `/login` route), `queryLocalFonts` in
`Accessibility/index.tsx:63`, ~150 percent-encoded Steam artwork URLs.

## Self-Check: PASSED

Gate executed as specified; verdict recorded honestly; no proof level upgraded beyond evidence; no
record update applied that the verdict did not permit.
