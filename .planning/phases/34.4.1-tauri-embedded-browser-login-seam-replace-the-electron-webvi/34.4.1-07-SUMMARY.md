---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 07
subsystem: docs
tags: [documentation, gate-script, seam-map, honesty, proof-levels]

# Dependency graph
requires:
  - phase: 34.4.1-01
    provides: "5 Rust dispatch_rust_channel arms (humble_login_open/cookies/take_events/close/clear_cookies), 34.4.1-LIVE-GATE.md's 4 blocking items"
  - phase: 34.4.1-02
    provides: "LoginWindowSeam interface, classifyCookieRead, 6 registered browser-auth channels, Assumption A4 live-hardware validation"
  - phase: 34.4.1-03
    provides: "watchForLogin()/finishLogin() wired onto the seam"
  - phase: 34.4.1-04
    provides: "humble_reveal_post Rust arm, LoginWindowSeam.revealPost()"
  - phase: 34.4.1-05
    provides: "TauriLoginPanel, loginRoutes.ts route classifier"
  - phase: 34.4.1-06
    provides: "humbleDisconnect's D-05 partial CLOSED (domain-scoped clear), electron-reach ledger regenerated, both Discretion sweeps clean"
  - phase: 34.4.1-09
    provides: "oauthCaptureLogin channel, matchOAuthRedirect() proven against all 4 real redirect shapes"
provides:
  - "34.4.1-PORTED-CHANNELS.md — the 7-row declared list (6 Humble browser-auth + oauthCaptureLogin) with honest, SUMMARY-traced proof levels, per-platform Linux/Windows declarations, and accepted residuals"
  - "SEAM.md's new §1 subsection recording the login seam as real, and §3's BrowserWindow row's login-story portion retired"
  - "ported-channels-gate.py — 8-check self-tested gate against the declared list, the inventory, and SEAM.md"
affects: [34.4.1-08, Phase-34.5]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Row-leading backtick regex + whole-cell split for declared-channel-list gates, mirroring 34.4's ported-channels-gate.py almost line-for-line"
    - "Pure check_* functions taking only text (or two text args) so every assertion has a directly-testable self-test counterpart, including the git-diff-based inventory-unmodified check (split into a subprocess wrapper + a pure text-taking check function)"

key-files:
  created:
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
    - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py
  modified:
    - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md

key-decisions:
  - "Verified every claim against the actual code (humbleLoginFlowRegistration.ts, oauthLoginFlowRegistration.ts, main.rs's dispatch_rust_channel match arms, loginWindowSeam.ts's classifyCookieRead) rather than inheriting the phase-provided <what_actually_shipped> claims — confirmed 6 Rust dispatch arms (5 login + 1 reveal_post), 4 handle + 2 send Humble channels, oauthCaptureLogin as a handle channel, 37 passing cargo tests, and classifyCookieRead's platform-independent truth table, all by direct source read."
  - "Did NOT edit IPC-PORT-INVENTORY.md's now-stale humbleDisconnect partial note (L73-75) even though plan 06 closed that partial — recorded the staleness as a follow-up observation in 34.4.1-PORTED-CHANNELS.md instead, per this plan's explicit instruction that the edit is plan 34.4.1-08's call once the live gate proves the clear works."
  - "SEAM.md's §3 BrowserWindow row was edited to say 'Mostly closed' (not 'Fully closed') because the in-app store/wiki <webview> replacement remains a separate, still-deferred item (D-05) — only the login-story portion of that row is retired."

patterns-established:
  - "The gate script's check_inventory_unmodified is split into a pure text-taking function (self-testable with a synthetic diff string) plus a subprocess wrapper in main() that supplies the real git diff --stat output -- keeps the git dependency out of the self-test path entirely."

requirements-completed: [REQ-34.4.1-10, REQ-34.4.1-13]

# Metrics
duration: ~110min
completed: 2026-07-29
---

# Phase 34.4.1 Plan 07: Declare What Shipped Summary

**`34.4.1-PORTED-CHANNELS.md` declares all 7 channels (6 Humble browser-auth + the new
`oauthCaptureLogin`) with proof levels traced to source and to each prior plan's SUMMARY — only
Assumption A4 (the window construct/close primitive) is live-hardware-proven; the actual login
flow, cookie read, reveal POST, and disconnect clear stay explicit forward references to plan
34.4.1-08's still-pending live gate. SEAM.md's §1/§3 checklist steps are closed in a two-hunk diff
with Invariant B untouched and the inventory verified unmodified. An 8-check self-tested gate
script proves the declared list, the inventory, and SEAM.md all agree — and demonstrably CAN
fail, not just pass.**

## Performance

- **Duration:** ~110 min
- **Tasks:** 3/3 completed
- **Files created:** 2 (`34.4.1-PORTED-CHANNELS.md`, `ported-channels-gate.py`)
- **Files modified:** 1 (`SEAM.md`)

## Accomplishments

- Wrote `34.4.1-PORTED-CHANNELS.md`: 7-row table (6 `humble*` + 1 `oauthCaptureLogin`), six
  required prose sections (`What shipped` / `The outcome, stated plainly` / `Per-platform status`
  / `Obligations closed and opened` / `Accepted residuals` / `Deferred, unchanged`), every proof
  level traced to a named plan and verified against the actual code (not inherited from the
  phase-provided claim list) before being written down.
- Named D-04/D-03's original acceptable bad case (from `34.4.1-DISCUSSION-LOG.md` Q3 — "if the
  cookie-jar read doesn't work on macOS, the phase closes on the 4 [OAuth runners] with Humble
  declared degraded") up front, then recorded that it did NOT materialize at the unit-proof level
  — while being explicit that the live, from-scratch login itself remains unobserved pending plan
  08's gate item 1.
- Declared Linux (WebKitGTK) and Windows (WebView2) per-platform status by naming three specific
  unverified surfaces from `tauri-login-webview-cookies.md`'s own "Constraints" section
  (`cookies_for_url()`'s domain-match correctness, the UA fingerprint gap, `data_store_identifier`'s
  macOS-14+/iOS-17+ gating), and recorded the one platform-independent proof that DOES exist:
  `classifyCookieRead()`'s truth table has no platform branch (verified by direct source read).
- Recorded 34.4 D-05's `humbleDisconnect` partial as CLOSED (plan 06, not carried forward) and
  34.4 D-09's "there is no Tauri path to create a Humble session" statement as PENDING STRIKE —
  explicitly NOT struck by this document, since that strike is plan 34.4.1-08's responsibility once
  gate item 1 passes, per REQ-34.4.1-10 and the gate's own "What this gate falsifies" section.
- Declared both accepted residuals: T-34.4.1-47 (shared app-wide cookie jar across future Epic/GOG/
  Amazon/Zoom login windows, tolerable because D-08's clear is domain-scoped) and T-34.4.1-44b
  (nile/zoom host-free redirect param matching — explicitly distinguished from the genuinely
  host-anchored T-34.4.1-44a — forwarded as an obligation Phase 34.5 MUST close before minting a
  real credential from a captured value).
- Edited SEAM.md in exactly two hunks: a new `### Embedded-browser login seam (real, Phase
  34.4.1)` §1 subsection (naming the 7 channels, the 6 new `dispatch_rust_channel` arms taking the
  running count 13→19, the deliberate reversal of the 34.2/34.4 zero-new-Rust-arms rider with a
  stated reason, and the navigation-observation-not-cookie-read inheritance for Phase 34.5), and a
  retirement of §3's `BrowserWindow` row's login-story portion (kept the still-deferred store/wiki
  browsing and `Electron.WebviewTag` removal items explicit, not silently dropped).
- Verified, without editing, `IPC-PORT-INVENTORY.md`'s Phase 34.4.1 section (L77-86) and the
  `humbleDisconnect` partial note (L73-75) — both confirmed intact via `git diff --stat` empty.
  Recorded the L73-75 note's now-stale wording (plan 06 already closed what it says "must be
  revisited") as a follow-up observation for plan 08, not silently corrected here.
- Wrote `ported-channels-gate.py`: 8 `check_*` functions (row presence, exact row count with a
  6/1 split, invoke/send kind correctness including `oauthCaptureLogin`'s special kind-cell text,
  permitted proof-level forms, a set-equality cross-check against the inventory with
  `oauthCaptureLogin`'s exclusion asserted explicitly, an inventory-untouched check via `git diff
  --stat`, SEAM.md's checklist-closure check, and `oauthCaptureLogin`'s never-live proof-level
  pin), each with exactly one self-test case, count asserted equal to the check count at runtime.
  Both the real-document run and `--self-test` exit 0 on the first attempt.

## Task Commits

1. **Task 1: Write 34.4.1-PORTED-CHANNELS.md** — `093b9ef83` (docs)
2. **Task 2: Close SEAM.md's checklist steps and verify the inventory is intact** — `6c7fa4d15` (docs)
3. **Task 3: Write a self-tested gate for the declared list** — `5d567ccb9` (test)

**Plan metadata:** (this commit, immediately following)

## Files Created/Modified

- `.planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md` — new, the 7-channel declared list with
  honest proof levels, per-platform declarations, obligations, and accepted residuals.
- `.planning/phases/27-tauri-shell-walking-skeleton/SEAM.md` — 2-hunk diff: new §1 subsection +
  §3 `BrowserWindow` row edit.
- `.planning/phases/34.4.1-.../ported-channels-gate.py` — new, 8-check self-tested gate script.

## Decisions Made

See `key-decisions` in frontmatter. Most consequential: every claim in
`34.4.1-PORTED-CHANNELS.md` was verified against the actual code before being written — the
channel registration modules (`humbleLoginFlowRegistration.ts`, `oauthLoginFlowRegistration.ts`),
`main.rs`'s `dispatch_rust_channel` match arms (confirmed exactly 6 Humble-related arms:
`humble_login_open`/`cookies`/`take_events`/`close`/`clear_cookies`/`reveal_post`), a live
`cargo test` run (37/37 passing), and `loginWindowSeam.ts`'s `classifyCookieRead()` source (no
platform branch) — rather than inheriting the phase-provided `<what_actually_shipped>` summary
verbatim, per this plan's own explicit instruction to verify every claim independently.

## Deviations from Plan

### Auto-fixed Issues

None — no Rule 1/2/3 auto-fixes were needed. This is a documentation-only plan; no source code
was touched, so no bug, missing functionality, or blocking issue of the kind those rules cover
arose during execution.

### Task-boundary note (not a Rule 1-4 deviation)

None. All three tasks were executed in the order and shape the plan specified, one commit per
task, with no reordering or combining required.

## Issues Encountered

- **`pnpm test:ci`'s full-suite run showed a second, non-baseline-named failure the first time**:
  `gameDetailsFlows.test.ts` timing out on `keyring_get` after 60s, alongside the documented
  baseline `rustInvokeChannel.test.ts` failure. Investigated per the deviation-rules scope
  boundary (this plan touches zero source files, so a regression from THIS plan's diff was ruled
  out on inspection alone): re-ran `gameDetailsFlows.test.ts` in isolation
  (`npx jest --config src/backend/jest.config.js src/backend/sidecar/__tests__/gameDetailsFlows.test.ts`)
  and it passed cleanly, 31/31. This matches the exact cross-test frame-leak class prior plans in
  this phase (03, 06) already documented — a shared `rustInvoke` mock state leaking across test
  files depending on jest's execution order, not a regression. No code change was made in
  response; nothing in this plan's diff touches test infrastructure or the sidecar RPC mock.

## Test Suite / Codecheck (test_baseline diff)

- `pnpm codecheck` (`tsc --noEmit`) → clean, 0 errors.
- `pnpm test:ci` → **2 failed, 3093 passed, 3095 total, 166 suites** (first full run). Failure 1:
  `rustInvokeChannel.test.ts` — the documented, expected baseline failure (Phase 34.1 tray
  cross-test frame leak, out of scope). Failure 2: `gameDetailsFlows.test.ts`, isolated re-run
  confirmed clean (31/31) — the same load-dependent cross-test frame-leak flake class the
  baseline already names for `downloadqueue.test.ts`/`shellFilesFlows.test.ts`, now also observed
  hitting `gameDetailsFlows.test.ts` on this run. No new *class* of failure; no regression traced
  to this plan's diff (zero source files touched by this plan). Diffed against the stated
  baseline (1 expected failure + isolation-clean flakes) — matches.

## Known Stubs

None — this plan is documentation-only (a declared-channel list, a gate script, and a SEAM.md
edit); no UI/data-rendering surface is touched, and no code stub was introduced or modified.

## Threat Flags

None — no new network endpoint, auth path, file access pattern, or schema change was introduced
at a trust boundary. This plan's own threat register (T-34.4.1-35/36/37/38) is entirely about
documentation honesty (inflated proof levels, silent inventory edits, undeclared platform gaps, a
vacuous gate), all mitigated as designed: every proof cell traces to a named SUMMARY or a direct
source verification performed during this plan's execution; `IPC-PORT-INVENTORY.md`'s
`git diff --stat` is empty (verified both by the plan's own commands and by the gate script's
`check_inventory_unmodified`); Linux/Windows are declared with three named unverified surfaces;
and the gate's 8 self-test cases each prove the corresponding check rejects the bad input it
exists to reject (the RED direction demonstrated, not merely a passing run).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `34.4.1-08` can now run its blocking 4-item live gate against a document that already states,
  plainly, which of the 7 channels' claims are pending its result — no row in
  `34.4.1-PORTED-CHANNELS.md` needs to be re-derived from scratch; the forward-reference cells
  (`unit + LIVE (item N)`) are exactly the ones plan 08 must fill in.
  `34.4.1-LIVE-GATE.md`'s evidence slots were NOT touched by this plan (left empty per this
  plan's explicit instruction) — they remain plan 08's to fill.
  34.4 D-09's "there is no Tauri path to create a Humble session" statement remains PENDING
  STRIKE, to be struck by plan 08 once — and only once — gate item 1 records PASS.
- Phase 34.5 can now read `SEAM.md`'s new §1 subsection and `34.4.1-PORTED-CHANNELS.md`'s
  "Obligations closed and opened" / "Accepted residuals" sections as its own starting work-list,
  including the forwarded T-34.4.1-44b host-anchoring obligation for `nile`/`zoom`.
- `IPC-PORT-INVENTORY.md`'s stale `humbleDisconnect` partial note (L73-75) is a known, recorded
  follow-up for plan 08 — not a blocker for this plan, not silently dropped.
- No blockers.

---
*Phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi*
*Plan: 07*
*Completed: 2026-07-29*

## Self-Check: PASSED

- FOUND: .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
- FOUND: .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py
- FOUND: .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md (modified, verified via git diff)
- FOUND: 093b9ef83 (Task 1 commit)
- FOUND: 6c7fa4d15 (Task 2 commit)
- FOUND: 5d567ccb9 (Task 3 commit)
