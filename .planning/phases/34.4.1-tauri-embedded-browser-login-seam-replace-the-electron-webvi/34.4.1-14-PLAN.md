---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 14
type: execute
wave: 5
depends_on: ["34.4.1-13"]
gap_closure: true
files_modified:
  - src/backend/steamgrid/secureKey.ts
  - src/backend/steamgrid/ipc_handler.ts
  - src/backend/steamgrid/__tests__/secureKey.test.ts
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-SEAM-PARITY-SWEEP.md
autonomous: true
requirements: [REQ-34.4.1-GAP-06]
must_haves:
  truths:
    - "Whether the SteamGridDB API key is exposed under Tauri is settled by measurement, not assumption"
    - "If exposed, it is stored in the OS keyring like Humble's session — using the same allowlist design"
    - "If not exposed, the reason is recorded with evidence and carries a forward obligation"
  artifacts:
    - path: ".planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-SEAM-PARITY-SWEEP.md"
      provides: "F-1b's disposition, resolved with evidence"
      contains: "F-1b"
  key_links:
    - from: "src/backend/steamgrid/secureKey.ts"
      to: "src/backend/humble/secretStore.ts"
      via: "the same seam pattern, proving the design scales as D-GAP-01 claimed"
      pattern: "SecretStore|safeStorage"
---

<objective>
Resolve **F-1b**: `src/backend/steamgrid/secureKey.ts:1` has the identical direct-`safeStorage`
import shape that produced F-1, holding the SteamGridDB API key. The gate recorded it as
"same-shape, unverified — not exercised by this gate" and explicitly asked for it to be checked
in the same pass.

Purpose: D-GAP-01's stated benefit for choosing the allowlist over dedicated per-secret channels
was that *"Secret #3 (`steamgrid/secureKey.ts`, F-1b) becomes a one-line allowlist addition rather
than another duplicated channel family."* This plan is where that claim is either cashed in or
shown to be unnecessary — and either outcome is a legitimate result, provided it is measured.

Output: F-1b closed, either by migrating the key onto the same seam, or by an evidenced
declaration that it is not reachable under the sidecar — never by assumption in either direction.

**This plan does not decide the answer in advance.** Plan 10's sweep measured reachability; this
plan acts on what it measured. If the sweep's finding and this plan's own re-check disagree, the
re-check wins and the disagreement is recorded — the same "if the measurement disagrees with the
prediction, the measurement wins" rule `34.4.1-PORTED-CHANNELS.md` already states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-SEAM-PARITY-SWEEP.md

**Project rule (CLAUDE.md):** run `graphify query "<question>"` before grepping or reading raw
source files.

<interfaces>
The affected module, in full (`src/backend/steamgrid/secureKey.ts`) — note it is the SAME
four-part shape as `humble/user.ts`'s pre-plan-12 helpers:
```
import { safeStorage } from 'electron'            // L1
const CIPHERTEXT_PREFIX = 'sgdb:v1:'
function encryptionAvailable(): boolean           // L7  — try/catch -> false
export function isEncryptedValue(stored): boolean
export function encryptApiKey(plain): string      // L19 — plaintext branch at L21, prefix+base64 at L27
export function decryptApiKey(stored): string     // L32 — legacy plaintext read at L34
```

Its only consumer (`src/backend/steamgrid/ipc_handler.ts`):
```
L5   import { encryptApiKey, decryptApiKey, isEncryptedValue } from './secureKey'
L17  if (!isEncryptedValue(stored)) {              // opportunistic re-encrypt of a legacy value
L18    const reEncrypted = encryptApiKey(stored)
L19    if (isEncryptedValue(reEncrypted)) { ... }
L25  return decryptApiKey(stored)
L32  const stored = trimmed ? encryptApiKey(trimmed) : ''
```
Note L17-19: an existing opportunistic upgrade path already exists here. Under the sidecar's dead
stub, `encryptApiKey` returns the plaintext and `isEncryptedValue(reEncrypted)` is false, so the
upgrade silently no-ops — the same "success path still runs, nothing happened" shape as F-1.

The two questions this plan answers, in order:
  1. Is `steamgrid/ipc_handler.ts` (or `secureKey.ts` by any other route) reachable from the
     sidecar's curated import graph? If nothing in the sidecar imports it, the key is never
     written by the Tauri build and F-1b is a latent shape, not a live exposure.
  2. If reachable — migrate onto the same seam pattern plan 12/13 established.

The evidence source for question 1 is `src/backend/sidecar/__tests__/electronReachLedger.test.ts`,
which already baselines the sidecar's transitive `electron`-reaching module set. If
`steamgrid/secureKey.ts` is in that ledger, it is reached. That is a measurement the repo already
maintains — use it rather than reasoning about imports by hand.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settle F-1b's reachability with evidence and record the disposition</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-SEAM-PARITY-SWEEP.md</files>
  <read_first>
    - `.planning/phases/34.4.1-.../34.4.1-SEAM-PARITY-SWEEP.md` — plan 10's finding for
      `steamgrid/secureKey.ts`, including whatever evidence it recorded for reachability.
    - `.planning/phases/34.4.1-.../34.4.1-11-SUMMARY.md` — whether plan 11 added a SteamGridDB
      slot to the Rust allowlist, and which line of the sweep decided it.
    - `src/backend/sidecar/__tests__/electronReachLedger.test.ts` — read the header docstring in
      full: growth-only/subset semantics, the anti-vacuity tests, and the standing rule to
      **regenerate at execution time, not from planning-time prose**.
    - `src/backend/sidecar/handlers.ts` and the `*FlowRegistration.ts` modules — the curated
      import graph that determines what the sidecar actually loads.
    - `src/backend/steamgrid/ipc_handler.ts` in full.
  </read_first>
  <action>
    Determine, by measurement rather than inspection-by-eye, whether `steamgrid/secureKey.ts` is
    reachable from the sidecar. Use the reach ledger as the primary instrument and a direct check
    of the curated registration modules as the corroborating one. Record BOTH results; if they
    disagree, that disagreement is itself a finding worth writing down.

    Update the F-1b row in `34.4.1-SEAM-PARITY-SWEEP.md` with the measured answer, the command(s)
    that produced it, and the resulting disposition:
      - REACHABLE -> Task 2 migrates it; the row's disposition names this plan.
      - NOT REACHABLE -> the row is DECLARED with the evidence, plus an explicit forward
        obligation: the moment any plan ports a SteamGridDB channel into the sidecar, this shape
        becomes a live plaintext exposure and must be migrated first. Name that obligation so a
        future phase inherits it rather than rediscovering it live, exactly as
        `34.4.1-PORTED-CHANNELS.md` forwarded T-34.4.1-44b to Phase 34.5.

    Do not edit any source file in this task. This task produces a decision with evidence; Task 2
    acts on it.
  </action>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/electronReachLedger.test.ts && grep -c "F-1b" .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-SEAM-PARITY-SWEEP.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/backend/sidecar/__tests__/electronReachLedger.test.ts` exits 0.
    - The F-1b row in `34.4.1-SEAM-PARITY-SWEEP.md` states REACHABLE or NOT REACHABLE — not a
      hedge — and names the command whose output decided it.
    - If NOT REACHABLE, the row contains an explicit forward obligation naming the trigger
      condition (a SteamGridDB channel being ported to the sidecar).
    - `git diff --name-only` shows no source file changed by this task.
  </acceptance_criteria>
  <done>F-1b's status is a measured fact with a named instrument, not an inherited "same-shape,
  unverified" note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close F-1b at the level the evidence supports</name>
  <files>
    src/backend/steamgrid/secureKey.ts
    src/backend/steamgrid/ipc_handler.ts
    src/backend/steamgrid/__tests__/secureKey.test.ts
  </files>
  <behavior>
    IF Task 1 measured REACHABLE:
    - The SteamGridDB key is written to and read from the keyring slot under Tauri, never
      `configStore` plaintext.
    - An existing plaintext key migrates with the same write -> readback -> compare -> delete
      ordering plan 13 established; any failure leaves the plaintext in place.
    - Electron's behavior is unchanged: same `sgdb:v1:` prefix, same `safeStorage` ciphertext,
      same legacy-plaintext read.
    - The opportunistic re-encrypt path (`ipc_handler.ts:17-19`) either succeeds or is a declared
      no-op — it must not silently appear to upgrade a key it did not upgrade.

    IF Task 1 measured NOT REACHABLE:
    - `secureKey.ts` gains an in-source comment naming F-1b, the measurement, and the trigger
      condition that would make it live — so the next reader of `import { safeStorage } from
      'electron'` in this file finds the analysis instead of repeating it.
    - No behavior changes on either build.
  </behavior>
  <read_first>
    - `.planning/phases/34.4.1-.../34.4.1-SEAM-PARITY-SWEEP.md` — Task 1's recorded disposition.
      **This is the input that decides which branch of this task runs.**
    - `src/backend/humble/secretStore.ts` and `src/backend/sidecar/humbleSecretStore.ts` — the
      seam + keyring pattern to reuse. Reusing it, rather than inventing a second shape, is the
      point: D-GAP-01 chose the allowlist specifically so this would be cheap.
    - `src/backend/steamgrid/secureKey.ts` and `src/backend/steamgrid/ipc_handler.ts` in full.
    - `src/backend/steamgrid/__tests__/secureKey.test.ts` if it exists — extend, do not restart.
    - `.planning/phases/34.4.1-.../34.4.1-13-SUMMARY.md` — the migration ordering and the failure
      modes plan 13 proved, so this task reuses proven rules rather than re-deriving them.
  </read_first>
  <action>
    Read Task 1's disposition and act on it.

    On REACHABLE: migrate `secureKey.ts` onto the same seam shape plan 12 built for Humble —
    a store-shaped seam with the Electron `safeStorage` implementation as the default and a
    keyring-backed implementation installed by the sidecar over the allowlisted SteamGridDB slot.
    Reuse plan 13's migration ordering exactly (write, read back, compare, only then delete),
    including its rule that any failure leaves the plaintext untouched. If plan 11 did not add
    the SteamGridDB slot (because the sweep said not reachable and this task's re-check says
    otherwise), add it to the Rust allowlist here — it is the one-line addition the design was
    chosen for — and say so prominently in the summary, because it means an earlier measurement
    was wrong.

    On NOT REACHABLE: change no behavior. Add the in-source comment described in `<behavior>`,
    naming F-1b, the instrument that measured it, and the trigger condition. This is the
    DECLARED-versus-SILENTLY-DROPPED distinction plan 10 defined, applied to this file: the
    reduction becomes a recorded decision rather than a latent surprise.

    Under no circumstances write the SteamGridDB key to the `steam-refresh-token` or
    `humble-session` slots.
  </action>
  <verify>
    <automated>npx jest src/backend/steamgrid/ && npm run test:ci && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/backend/steamgrid/` exits 0.
    - `npm run test:ci` exits 0 with no regression against the 173/3279 baseline.
    - `npx tsc --noEmit` exits 0.
    - `cd src-tauri && cargo test` exits 0 (relevant only if the allowlist was touched).
    - `grep -c "F-1b" src/backend/steamgrid/secureKey.ts` returns at least 1 — in BOTH branches,
      the file records what was decided about it.
    - REACHABLE branch only: a test proves the plaintext key survives a rejecting keyring write.
    - REACHABLE branch only:
      `grep -v '^\s*//' src/backend/steamgrid/secureKey.ts | grep -c "steam-refresh-token\|humble-session"`
      returns 0.
    - NOT REACHABLE branch only: `git diff --stat -- src/backend/steamgrid/` shows comment-only
      changes, and `npx jest src/backend/steamgrid/` test count is unchanged.
  </acceptance_criteria>
  <done>F-1b is closed — either the key is in the keyring, or the file itself records why it is
  not and what would change that.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered API key -> disk | the SteamGridDB key's at-rest storage |
| sidecar -> OS Keychain | reached only if F-1b is measured reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.1-62 | Information Disclosure | SteamGridDB API key at rest | mitigate (if reachable) / accept-with-declaration (if not) | Migrated to the keyring on the same seam as Humble; or, if the sidecar cannot reach this module, recorded in-source with the trigger that would make it live. A third-party API key is a lower-value secret than a session cookie, which is why an evidenced declaration is an acceptable disposition here and was NOT acceptable for F-1. |
| T-34.4.1-63 | Denial of Service | slot cross-contamination | mitigate | The SteamGridDB path may address only its own slot; a grep gate asserts the other two slot names never appear in this module, and Rust's allowlist is the backstop. |
| T-34.4.1-64 | Repudiation | silent no-op re-encrypt | mitigate | `ipc_handler.ts:17-19`'s opportunistic upgrade currently appears to run while doing nothing under the dead stub. Either it genuinely upgrades, or the no-op is declared in source — it may not stay silent. |
| T-34.4.1-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. |
</threat_model>

<verification>
- `npm run test:ci` exits 0.
- `npx tsc --noEmit` exits 0.
- `cd src-tauri && cargo test` exits 0.
- `npx jest src/backend/sidecar/__tests__/electronReachLedger.test.ts` exits 0, regenerated at
  execution time if a new module edge appeared.
- `git diff --stat -- .planning/IPC-PORT-INVENTORY.md` is EMPTY.
</verification>

<success_criteria>
- F-1b's reachability is a measured fact with a named instrument.
- The disposition matches the measurement, and the source file records it either way.
- No non-SteamGridDB slot is ever addressed from this module.
- D-GAP-01's "one-line allowlist addition" claim is either demonstrated or shown unnecessary.
</success_criteria>

<output>
Create `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-14-SUMMARY.md` when done.

State which branch ran and the evidence that chose it. If Task 1's re-check contradicted plan
10's sweep, say so loudly — the sweep is the instrument plans 11-18 were scoped against, and a
wrong reading in it may affect other rows too.
</output>
