---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 05
type: execute
wave: 3
depends_on: ["34.4.1-02"]
files_modified:
  - src/frontend/screens/WebView/loginRoutes.ts
  - src/frontend/screens/WebView/index.tsx
  - src/frontend/screens/WebView/components/TauriLoginPanel.tsx
  - src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx
  - src/frontend/screens/WebView/__tests__/loginRoutes.test.ts
  - src/frontend/screens/WebView/components/__tests__/TauriLoginPanel.test.tsx
  - src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx
autonomous: true
requirements: [REQ-34.4.1-07, REQ-34.4.1-08]
# REQ-34.4.1-08 is SHARED with plan 34.4.1-09 (wave 4): this plan ships the declared-blocked
# surface, plan 09 ships the capture that makes it a state rather than a constant.

must_haves:
  truths:
    - "D-06: the `!webviewPreloadPath` branch is SPLIT — login routes stop rendering `WebviewUnavailablePanel` and drive the new mechanism; `/store/*`, `/wiki` and `store-page?store-url=` keep a panel whose copy names the REAL gap (in-app store browsing), not a login gap."
    - "D-05: in-app store and wiki browsing stays out of scope, and the split keeps that deferral visible to USERS via an Open-in-browser affordance, not only to the roadmap."
    - "D-04/REQ-34.4.1-08 (HALF of it — the DECLARED half): the four OAuth runners are served by the SAME runner-agnostic mechanism (nothing in the Rust arms or the seam knows what Humble is) and their login routes state plainly that `login`/`authGOG`/`authAmazon`/`authZoom` land in Phase 34.5 — declared, not silently broken, and never a blank screen. D-04 says the four get wired AND declared-blocked; the WIRED half is plan 34.4.1-09's, which replaces this panel's static copy with real per-runner capture state. This plan must therefore leave the surface wired-ready: no copy here may assert that no sign-in was attempted."
    - "34.4 D-04's rider holds: Electron's behavior does not change, and that is PROVEN by test (the Electron `<></>` branch and the route classifier are both asserted), not asserted in prose."
    - "No copy affordance uses `navigator.clipboard`, which resolves WITHOUT writing under Tauri's WKWebView."
  artifacts:
    - path: "src/frontend/screens/WebView/loginRoutes.ts"
      provides: "LOGIN_PATHNAMES + isLoginPathname() — the pure route classifier the branch split turns on"
      exports: ["LOGIN_PATHNAMES", "isLoginPathname"]
    - path: "src/frontend/screens/WebView/components/TauriLoginPanel.tsx"
      provides: "Humble in-progress surface + OAuth declared-blocked surface naming Phase 34.5"
    - path: "src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx"
      provides: "Store/wiki-only panel with reworded copy and an Open-in-browser button"
  key_links:
    - from: "src/frontend/screens/WebView/index.tsx"
      to: "isLoginPathname"
      via: "the !webviewPreloadPath Tauri branch chooses login vs store surface"
      pattern: "isLoginPathname"
    - from: "src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx"
      to: "window.api.openExternalUrl"
      via: "Open in browser button"
      pattern: "openExternalUrl"
---

<objective>
Split 34.4 D-04's catch-all "not available on this build" branch so login routes drive the new
mechanism while store and wiki routes keep an honest, differently-worded panel with a way out to the
system browser.

Purpose: once Humble login works under Tauri, showing it a login-unavailable panel would be a lie;
and the store routes were never this phase's job (D-05) but their users still deserve an accurate
message and an escape hatch.

Output: a pure route classifier, a split branch, a new login panel, and a reworded store panel — all
test-covered, with Electron's shape unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-CONTEXT.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PATTERNS.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-02-SUMMARY.md

<interfaces>
src/frontend/screens/WebView/loginRoutes.ts  (NEW — pure, no React, no window.api)
  export const LOGIN_PATHNAMES: readonly string[] = [
    '/loginEpic', '/loginGOG',
    '/loginweb/legendary', '/loginweb/gog', '/loginweb/nile', '/loginweb/zoom', '/loginweb/humble'
  ]
  export function isLoginPathname(pathname: string): boolean

src/frontend/screens/WebView/components/TauriLoginPanel.tsx  (NEW)
  props: { runner?: Runner }   // same Runner union WebviewUnavailablePanel already takes

src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx  (MODIFIED)
  props: { url?: string }      // `runner` prop is REMOVED — this panel is store/wiki-only now
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the route classifier and split the !webviewPreloadPath branch</name>
  <files>src/frontend/screens/WebView/loginRoutes.ts, src/frontend/screens/WebView/index.tsx, src/frontend/screens/WebView/__tests__/loginRoutes.test.ts</files>
  <read_first>
    - src/frontend/screens/WebView/index.tsx L75-120 (the `urls` map at L83-97 — the authoritative list of login vs store/wiki pathnames — plus the `store-page` match at L108-114) and L448-476 (the branch being split, and the humble-UA guard immediately after it)
    - src/frontend/screens/WebView/index.tsx L185-217 (the humble login-watch effect — note it runs on MOUNT regardless of what is rendered, so the Humble route already drives `humbleStartLogin`/`humbleStopLogin` once plan 34.4.1-02 registers them)
    - .planning/phases/34.4.1-.../34.4.1-PATTERNS.md § "src/frontend/screens/WebView/index.tsx — D-06 branch split"
    - src/preload/api/helpers.ts L10-23 (the isTauri routing pattern)
  </read_first>
  <action>
    Create `src/frontend/screens/WebView/loginRoutes.ts` exporting `LOGIN_PATHNAMES` and
    `isLoginPathname(pathname)` exactly as typed in `<interfaces>`. Derive the list from the `urls`
    map in `index.tsx` (all `/loginweb/*` plus the legacy `/loginEpic` and `/loginGOG`); everything
    else — `/store/epic|gog|amazon|zoom|steam`, `/wiki`, and any `store-page` pathname — is the store
    half. Keep it pure: no React, no `window.api`, no imports from `index.tsx` (import direction is
    index -> loginRoutes, never the reverse).

    In `index.tsx`, rewrite ONLY the `if (!webviewPreloadPath)` block at L448-469:
      - `isTauri() && isLoginPathname(pathname)` -> `return <TauriLoginPanel runner={runner} />`
      - `isTauri()` (store/wiki) -> keep a `window.api.logInfo` line, reworded to say in-app store
        browsing is unavailable and naming the deferral (D-05), then
        `return <WebviewUnavailablePanel url={startUrl} />`
      - the Electron `return <></>` branch and its comment stay EXACTLY as they are, except for
        updating the trailing "The real fix ... is Phase 34.4.1's (D-01)" sentence, which is now
        shipped — replace it with a sentence naming what actually landed. Do not merge the two
        branches; the separation is what lets a test assert Electron's shape never changes.
    Leave every other part of the file untouched — the `<webview>` element (L488-503), the
    `WebviewControls` block, the adtraction workaround and the store `sessionStorage` restore are all
    out of scope (D-05).

    Add `src/frontend/screens/WebView/__tests__/loginRoutes.test.ts` covering: every entry of the
    `urls` map classified correctly (all 7 login pathnames true; all 6 store/wiki pathnames false), a
    `store-page` pathname false, an unknown pathname false, and a case-sensitivity case
    (`/LoginWeb/humble` false — the router emits lowercase).
  </action>
  <acceptance_criteria>
    - `npx jest src/frontend/screens/WebView/__tests__/loginRoutes.test.ts` exits 0 with >= 15 assertions.
    - `grep -c "WebviewUnavailablePanel" src/frontend/screens/WebView/index.tsx` returns 1 usage inside the store branch only (verified by reading the branch).
    - The Electron branch is unchanged in shape: `git diff src/frontend/screens/WebView/index.tsx` shows the `return <></>` line untouched.
    - `npx tsc --noEmit` exits 0.
    - `npm start` launches the Electron renderer without a bundler error (the bare-import class of regression 34.4-10 caught) — record the observed dev-server line in the SUMMARY. Every new import in `index.tsx` must resolve under BOTH `electron.vite.config.ts` and the Tauri build; prefer a relative import over an alias.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx jest src/frontend/screens/WebView/__tests__/loginRoutes.test.ts</automated>
  </verify>
  <done>Login and store routes take different branches under Tauri, the classifier is unit-proven against the real `urls` map, and the Electron branch is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Build TauriLoginPanel — Humble in-progress, OAuth declared-blocked</name>
  <files>src/frontend/screens/WebView/components/TauriLoginPanel.tsx, src/frontend/screens/WebView/components/__tests__/TauriLoginPanel.test.tsx</files>
  <read_first>
    - src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx (the whole 77-line file — structure, `t()` usage with inline defaults, class names, and the docstring naming this phase as its retirement plan)
    - src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx (the testing style to mirror)
    - .planning/phases/34.4.1-.../34.4.1-CONTEXT.md D-04 and the `<specifics>` bullet on the bad case being legitimate only if declared
    - src/frontend/screens/Login/components/SIDLogin/index.tsx (the shipped non-embedded Epic login — evidence a paste/browser handoff is acceptable UX here; referenced for tone, not copied)
  </read_first>
  <action>
    Create `TauriLoginPanel.tsx` rendering one of two surfaces based on `runner`:

    (a) `runner === 'humble'` — the IN-PROGRESS surface. Copy states that a sign-in window has
    opened, that the user should complete sign-in there, and that this page updates automatically
    when it succeeds. It must NOT claim login is unavailable. Do not add a "cancel" button: leaving
    the route already issues `humbleStopLogin` via the existing effect's cleanup (the D-06 silent
    cancel), and a second cancel path would need its own contract.

    (b) `runner` is `legendary`/`gog`/`nile`/`zoom` (or undefined on a login route) — the
    DECLARED-BLOCKED surface. Copy names the runner and states plainly that its backend sign-in
    channel is not ported yet and lands in Phase 34.5. Name the exact channel per runner
    (`login` for Epic/legendary, `authGOG`, `authAmazon` for nile, `authZoom`) in a developer-legible
    line, and emit one `window.api.logInfo` line on mount saying the same — "logged, never silent"
    (34.1 D-13 house style). This is D-04's declaration made visible to the user instead of a blank
    screen or a silent hang.

    WORDING CONSTRAINT (this is what makes the component reusable by plan 34.4.1-09 instead of
    rewritten by it): the copy must be about the BACKEND CHANNEL being unported, never about
    sign-in not having been attempted. Plan 09 wires a real capture in front of this surface, so
    anything like "sign-in is not available on this build" or "nothing was attempted" becomes false
    one wave later. Write it as "your sign-in cannot be completed yet: `authGOG` is not ported —
    Phase 34.5" and it stays true both before and after plan 09.

    Structure the component so plan 09 can supply per-runner state without restructuring it: accept
    an OPTIONAL `state` prop today (default `undefined` = the static declared-blocked copy above),
    so plan 09 adds cases rather than rewriting the render tree.

    Both surfaces reuse `WebviewUnavailablePanel`'s existing class names/markup shape so no new CSS
    is needed. All strings go through `t()` with inline English defaults, matching the existing file.
    Never call `navigator.clipboard` — if any copy affordance is ever added it must use
    `window.api.clipboardWriteText` (WKWebView resolves `navigator.clipboard.writeText` WITHOUT
    writing).

    Add `TauriLoginPanel.test.tsx`: the humble variant renders the in-progress copy and does NOT
    render the word "unavailable"; each of the four OAuth runners renders its own channel name and
    the Phase 34.5 statement; `window.api.logInfo` is called exactly once per mount for the OAuth
    variant and not for the humble variant; `navigator.clipboard` is never touched.
  </action>
  <acceptance_criteria>
    - `npx jest src/frontend/screens/WebView/components/__tests__/TauriLoginPanel.test.tsx` exits 0 with a case per runner (5 total).
    - `grep -c "navigator.clipboard" src/frontend/screens/WebView/components/TauriLoginPanel.tsx` returns 0.
    - The four channel names appear in the component: `grep -c "authGOG\|authAmazon\|authZoom" src/frontend/screens/WebView/components/TauriLoginPanel.tsx` returns >= 3.
    - Runner-agnosticism check (NECESSARY, NOT SUFFICIENT, for REQ-34.4.1-08):
      `grep -ci "humblebundle" src-tauri/src/main.rs` returns 0 — every URL the Rust arms open comes
      from the sidecar, so the Rust layer is CAPABLE of serving all five runners. Record it in the
      SUMMARY with that exact framing. It proves capability, not invocation: it does NOT show that
      the four OAuth flows actually call the seam. The executable evidence for "wired" is plan
      34.4.1-09's four redirect-shape tests; do not close REQ-34.4.1-08 on this grep alone.
    - The declared-blocked copy contains no claim that sign-in was not attempted — verify by reading
      the rendered strings and quote them in the SUMMARY (plan 09 puts a real capture in front of
      this surface one wave later).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx jest src/frontend/screens/WebView/components/__tests__/TauriLoginPanel.test.tsx</automated>
  </verify>
  <done>Humble's login route shows an honest in-progress surface; the four OAuth routes show an honest, logged, Phase-34.5-naming block instead of a blank screen.</done>
</task>

<task type="auto">
  <name>Task 3: Reword WebviewUnavailablePanel for store/wiki and add Open in browser</name>
  <files>src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx, src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx</files>
  <read_first>
    - src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx (whole file, especially the docstring at L7-34 that names 34.4.1 D-01 as its own retirement plan for the login case)
    - src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx (existing cases — EXTEND, and if a case must change, change it openly with a stated reason in the SUMMARY)
    - a current call site of `window.api.openExternalUrl` in the frontend (for the exact call shape; never `shell.openExternal`)
  </read_first>
  <action>
    Change `WebviewUnavailablePanel` to the store/wiki-only surface D-06 specifies:
      - Replace the `runner` prop with an optional `url` prop (the resolved `startUrl`).
      - Reword the heading and body to name the REAL gap — in-app store and wiki browsing is not
        available on this build — and drop the login-specific "Signing in to X is unavailable" and
        "use the Electron build to sign in" copy, which is now wrong for this surface (login works).
      - Add an "Open in browser" button that calls `window.api.openExternalUrl(url)` when `url` is
        present, and renders nothing when it is absent. Keep the existing class-name conventions.
      - Update the file docstring: state that the login case moved to `TauriLoginPanel` in Phase
        34.4.1, that this panel now covers only `/store/*`, `/wiki` and `store-page`, and that the
        deferral is tracked as its own future work (D-05) — do not leave the old "the real fix is
        Phase 34.4.1's" sentence pointing at a phase that shipped.
    Extend the existing test file: the reworded copy no longer mentions signing in; the button
    calls `openExternalUrl` with the exact url passed; no button renders without a url; a snapshot or
    text assertion covers the store wording. Any existing case that asserted login-specific copy is
    UPDATED, not deleted, with the reason recorded in the SUMMARY (`electronUntouched.test.ts` and
    this suite must not be weakened silently).
  </action>
  <acceptance_criteria>
    - `npx jest src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx` exits 0.
    - `grep -c "openExternalUrl" src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx` returns >= 1.
    - `grep -ci "sign in\|signing in\|login" src/frontend/screens/WebView/components/WebviewUnavailablePanel.tsx` returns 0 outside the docstring (verify by reading; the docstring may reference the moved login case).
    - `npm run test:ci` exits 0.
    - `npx jest src/backend/sidecar/__tests__/electronUntouched.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/frontend/screens/WebView/components/__tests__/WebviewUnavailablePanel.test.tsx src/backend/sidecar/__tests__/electronUntouched.test.ts</automated>
  </verify>
  <done>The store/wiki panel names the real gap and offers a way out to the system browser, with its tests updated openly rather than weakened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route/query input → external browser | `store-page?store-url=` can put a caller-supplied URL behind the Open-in-browser button |
| renderer → user perception | The panel copy is the only signal a user has about whether a login can succeed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.1-26 | Spoofing / Tampering | Open-in-browser button on `store-page?store-url=` | mitigate | The button forwards the already-resolved `startUrl` through the ported `window.api.openExternalUrl`, which routes to the existing Rust `open_external` arm; no new URL-handling path is introduced and `shell.openExternal` is never called directly. The existing `validStoredUrl` host validation on stored URLs is untouched. |
| T-34.4.1-27 | Repudiation | a silently blocked OAuth login | mitigate | The declared-blocked surface names the runner, the exact unported channel and Phase 34.5, and emits one `logInfo` line per mount — the user never sees a blank screen and a developer never has to guess (SEAM Invariant B made visible). Plan 34.4.1-09 strengthens this from a static declaration to a post-capture one; the copy written here must already be true under both. |
| T-34.4.1-28 | Information Disclosure | copy affordances under WKWebView | mitigate | No `navigator.clipboard` usage in either panel (grep-asserted); any future copy button must use `window.api.clipboardWriteText`, which is proven in both builds. |
| T-34.4.1-29 | Tampering | Electron behavior drift | mitigate | The Electron `<></>` branch is left byte-identical and the classifier is unit-tested; `electronUntouched.test.ts` stays green and is not weakened. |
| T-34.4.1-SC | Tampering | npm installs | accept | Zero new packages; only already-imported React/i18next machinery is used. |
</threat_model>

<verification>
- `npm run test:ci` green.
- `npx tsc --noEmit` clean.
- `npm start` (Electron) renders the login and store routes exactly as before — no bundler error, no visual change.
- `pnpm tauri:dev`: `/loginweb/humble` shows the in-progress surface, `/store/steam` shows the reworded store panel with a working Open-in-browser button.
</verification>

<success_criteria>
Login routes and store routes tell the user the truth under Tauri: Humble says a sign-in window is
open, the four OAuth runners say precisely which channel is missing and when it lands, and the store
routes say in-app browsing is deferred while offering the system browser — with Electron unchanged
and proven so. The OAuth surface is written to stay true after plan 34.4.1-09 puts a real capture
in front of it.
</success_criteria>

<output>
Create `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-05-SUMMARY.md` when done
</output>
