---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 34
status: complete
completed: 2026-08-23
wave: 2
gap_cycle: 3
commit: 8319ad642
---

# Plan 34.4.1-34 — SUMMARY

**Two unassigned notes became tracked items. No code.**

## Why it was needed

`grep -rl "34\.4\.1" .planning/todos/` was **empty in both `pending/` and `completed/`**. All ten
`D-29-*` findings lived only in `deferred-items.md`, which carries no frontmatter and is therefore
invisible to `gsd-sdk query audit-uat` **and** to the explorer rollup.

## D-29-08 — confirmed ORPHANED, with dates

| claim | evidence |
|---|---|
| 34.5 does not own it | `34.5-26-SUMMARY.md:316` — *"NOT specifically re-exercised … not independently live-verified here."* |
| 34.6 inherited only login + `egsSync` | its ROADMAP scope names 34.5 UAT tests 11 and 12; logout is absent |
| 34.7 cannot own it | ON HOLD, premise died |
| nothing else references it | `clearEpicCookies` in no phase folder but 34.4.1's own |

Third recurrence of the records-rot-silently shape. **Proposed owner 34.6**, added to its inherited
list as an explicit act — the existing inheritance demonstrably did not cover logout.

The record preserves the epistemics: Epic's logout calls the **same** arm Humble's disconnect proved
fixed, but that is **an inference from shared code, not a measurement** — the exact distinction that
let run 2's failure hide behind a green suite. **No document may call it verified on that basis.**

## D-29-06 / F-9 — UNDETERMINED kept as UNDETERMINED

Not rounded to "no". `id=1575` carries no channel name; settling it needs the originating request
located in the same scrollback.

Its transferable half is preserved: the finding was first recorded *"F-9 watch CLEAN"* on a
`gamelib.log` grep that **could not have seen it**, because shell `eprintln!` never reaches the log
file. **A clean grep of the wrong source is not evidence of absence.**

Cross-linked to `REQ-34.4.1-GAP-11` with an explicit warning that observability is not a root-cause
fix — do not close one by satisfying the other.

## Also delivered

`deferred-items.md` now carries an **ownership map for all 11 items** (ten `D-29-*` plus `NEW-01`),
each with an owning plan and a declared file — satisfying that file's own rule that a carry-forward
without an owning plan that declares the file is a note, not a task.
