---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 06
type: execute
wave: 4
depends_on: ["34.4.1-03", "34.4.1-04", "34.4.1-05"]
files_modified:
  - src/backend/humble/user.ts
  - src/backend/humble/__tests__/user.test.ts
  - src/backend/sidecar/__tests__/electronReachLedger.test.ts
  - src/preload/__tests__/childWindows.test.ts
autonomous: true
requirements: [REQ-34.4.1-06, REQ-34.4.1-09, REQ-34.4.1-11]

must_haves:
  truths:
    - "D-08: 34.4 D-05's `humbleDisconnect` partial is CLOSED, not re-declared — under Tauri the login cookies land in the app-wide jar, and disconnect now clears `humblebundle.com` entries specifically so a re-login genuinely starts fresh."
    - "D-08: the clear is DOMAIN-SCOPED and never blanket — the same jar will hold Epic/GOG/Amazon cookies once 34.5 lands, and `clear_all_browsing_data()` would sign the user out of storefronts they never touched."
    - "The real security boundary is unchanged: the encrypted credential is deleted FIRST (configStore.clear() before any wipe step), and the cookie clear is a best-effort guarded step that cannot abort or precede it (ASVS V3)."
    - "REQ-34.4.1-11: the additive/reversible invariant holds BEHAVIORALLY — `npm start` and `pnpm tauri:dev` both work, the reach ledger is REGENERATED by measurement (never from planning prose), and `electronUntouched.test.ts` / `sidecarRejectionGuard.test.ts` stay green and unweakened."
    - "Both Discretion sweeps ran as real checks against THIS phase's own diff: no stale `isTauri()` guard short-circuits a newly-ported path (34.4's gate item 2 failure mode), and no bare/aliased import breaks the Electron renderer build (34.4-10's regression, invisible to tsc and jest)."
  artifacts:
    - path: "src/backend/humble/user.ts"
      provides: "disconnect() gains a domain-scoped clearHumbleCookies wipe step on the seam path"
      contains: "clearCookies"
    - path: "src/backend/sidecar/__tests__/electronReachLedger.test.ts"
      provides: "humbleLoginFlowRegistration.ts added as an entry point with a measured baseline"
      contains: "humbleLoginFlowRegistration"
    - path: "src/preload/__tests__/childWindows.test.ts"
      provides: "Extended T-34.1-27 label-safety coverage for this phase's window labels"
  key_links:
    - from: "src/backend/humble/user.ts disconnect()"
      to: "LoginWindowSeam.clearCookies"
      via: "guarded wipeStep, after configStore.clear()"
      pattern: "clearHumbleCookies"
---

<objective>
Close `humbleDisconnect`'s declared partial with a domain-scoped cookie clear, then run the phase's
guardrails: regenerate the electron-reach ledger by measurement, extend the label-safety suite, and
execute both Discretion sweeps against this phase's own diff.

Purpose: D-08 exists specifically to stop a partial being carried for a second phase running, and
34.4's gate item 2 proved that a fully-green test suite can coexist with a completely unreachable
ported channel.

Output: a closed disconnect obligation and a phase whose invariants are measured, not assumed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-CONTEXT.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PATTERNS.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-03-SUMMARY.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-04-SUMMARY.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-05-SUMMARY.md
@.planning/IPC-PORT-INVENTORY.md

<interfaces>
From src/backend/humble/loginWindowSeam.ts (plans 02/04):
  LoginWindowSeam.open(url, { visible, userAgent }): Promise<string>
  LoginWindowSeam.clearCookies(label, domain): Promise<number>   // returns cookies deleted
  LoginWindowSeam.close(label): Promise<boolean>
  getLoginWindowSeam(): LoginWindowSeam | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain-scoped cookie clear on humbleDisconnect</name>
  <files>src/backend/humble/user.ts, src/backend/humble/__tests__/user.test.ts</files>
  <read_first>
    - src/backend/humble/user.ts L564-623 (disconnect() in full — the credential-first ordering, the guarded `wipeSteps` array, and the deliberate store-exclusion comment that must NOT be weakened)
    - .planning/IPC-PORT-INVENTORY.md L73-75 (the declared-partial note this closes — READ ONLY, plan 34.4.1-07 verifies it, this plan does not edit it)
    - .planning/phases/34.4.1-.../34.4.1-RESEARCH.md Pitfall 3 and the D-08 code example
    - .claude/skills/spike-findings-gamelib/references/tauri-login-webview-cookies.md § "Persistence and isolation" (there is no windowless jar access — a clear needs a live webview handle)
  </read_first>
  <action>
    In `disconnect()`, leave everything above the wipe loop untouched — `invalidateSyncGeneration()`,
    `configStore.clear()`, `humbleLibraryStore.clear()`, `humbleSyncStore.clear()` and the
    store-exclusion comment are the real security boundary and this phase does not change them.

    Add a seam branch around the wipe block:
      - `getLoginWindowSeam() === null` (Electron): the existing `session.fromPartition` +
        five-step `wipeSteps` array, verbatim.
      - seam installed (Tauri): a single `['clearHumbleCookies', ...]` step in the SAME guarded
        array/loop shape, which opens a HIDDEN window on `HUMBLE_BASE_URL` (a live webview handle is
        the only way to reach the jar), calls `seam.clearCookies(label, 'humblebundle.com')`, logs
        the returned COUNT (never any cookie name or value), and closes the window in a `finally` so
        no path leaves it open. A failure logs and continues, exactly like the existing steps — the
        disconnect already succeeded once the credential store was cleared.
      Never call anything resembling a blanket wipe: `clear_all_browsing_data` must not appear
      anywhere in the TS or Rust source (threat T-34.4.1-03).

    Extend `user.test.ts` with disconnect cases on the seam path: the credential store is cleared
    BEFORE the cookie step runs (assert call order); `clearCookies` is called with exactly
    `'humblebundle.com'`; a rejecting `clearCookies` does not throw out of `disconnect()`; the hidden
    window is closed even when the clear rejects; and with no seam installed the original five
    Electron wipe steps still run.
  </action>
  <acceptance_criteria>
    - `npx jest src/backend/humble/__tests__/user.test.ts` exits 0 with >= 5 new disconnect cases.
    - `grep -rc "clear_all_browsing_data" src src-tauri/src` returns 0 for every file.
    - Call-order assertion exists: the test proves `configStore.clear` ran before `clearCookies`.
    - `git diff src/backend/humble/user.ts` shows the store-exclusion comment block unchanged.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx jest src/backend/humble/__tests__/user.test.ts</automated>
  </verify>
  <done>Disconnect deletes the credential first and then clears only `humblebundle.com` cookies through a short-lived hidden window, with order and scope both test-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the electron-reach ledger and extend the label-safety suite</name>
  <files>src/backend/sidecar/__tests__/electronReachLedger.test.ts, src/preload/__tests__/childWindows.test.ts</files>
  <read_first>
    - src/backend/sidecar/__tests__/electronReachLedger.test.ts L1-70 (the header docstring's growth-only/subset semantics, the anti-vacuity tests, and the "regenerate at execution time, not from planning-time prose" rule; plus the ENTRY_POINTS array and the Phase 34.4 comment convention)
    - .planning/phases/34.4-.../34.4-08-SUMMARY.md (the precedent: the MEASUREMENT disagreed with the plan's prediction and the measurement won — a fourth, unpredicted module appeared)
    - src/preload/__tests__/childWindows.test.ts L75-170 (the existing T-34.1-27 label cases — EXTEND, do not restart)
    - src/preload/api/tauriChildWindows.ts L36-59 (nextExternalWindowLabel)
  </read_first>
  <action>
    (a) Add `src/backend/sidecar/humbleLoginFlowRegistration.ts` to `ENTRY_POINTS` with a
    Phase-34.4.1 comment in the existing convention. Then REGENERATE
    `BASELINE_ELECTRON_REACHING_MODULES` by actually running `computeElectronReach()` via a
    temporary measurement statement that you remove before committing — do NOT hand-write the new
    entries from this plan's prose. Record in the SUMMARY: the before/after module count, the
    before/after `visitedFiles.size`, and whether the measurement AGREED with the prediction that
    `humble/user.ts` and `humble/adapter.ts` (already baselined by 34.4) are the only reached
    modules. If the measurement disagrees, the MEASUREMENT WINS: add whatever it found, with a
    reach-path comment, and flag the delta as a planning-time gap rather than absorbing it silently.
    Extend the `requiredModules` anti-degradation list with the new edge(s) and never lower the
    reachability floor.

    (b) Extend `childWindows.test.ts` with cases proving this phase's label discipline survives:
    labels from two successive generations differ; the same URL never yields the same label twice;
    no generated label equals `main` or `about`; no generated label contains any substring of the
    input URL's host (T-34.1-27). Mirror the existing case names rather than inventing a new style,
    and add a comment linking these to the Rust-side `next_login_window_label()` tests in
    `src-tauri/src/main.rs` so the two halves of the same rule are discoverable from each other.
  </action>
  <acceptance_criteria>
    - `npx jest src/backend/sidecar/__tests__/electronReachLedger.test.ts` exits 0.
    - `grep -c "humbleLoginFlowRegistration" src/backend/sidecar/__tests__/electronReachLedger.test.ts` returns >= 1.
    - The SUMMARY states the measured before/after module count and `visitedFiles.size`, and explicitly says whether the measurement matched the prediction.
    - No temporary measurement statement remains: `grep -c "computeElectronReach()" src/backend/sidecar/__tests__/electronReachLedger.test.ts` matches the count that existed before this task (record both).
    - `npx jest src/preload/__tests__/childWindows.test.ts` exits 0 with >= 4 added cases and zero deleted cases.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/backend/sidecar/__tests__/electronReachLedger.test.ts src/preload/__tests__/childWindows.test.ts</automated>
  </verify>
  <done>The ledger's baseline is measured rather than predicted, and the never-derived-from-a-URL label rule is asserted on both sides of the seam.</done>
</task>

<task type="auto">
  <name>Task 3: Run both Discretion sweeps and the full phase-wide green check</name>
  <files>src/backend/humble/user.ts</files>
  <read_first>
    - .planning/phases/34.4.1-.../34.4.1-RESEARCH.md § "Discretion Findings" (a) — the pre-existing `GlobalState.tsx` guard is already gone, so this is a FRESH sweep of this phase's own diff, not a remediation
    - .planning/STATE.md L39-76 (34.4-10's account of gate item 2's failure and the bare-import build regression — the two failure modes these sweeps target)
    - .planning/phases/34.4.1-.../34.4.1-VALIDATION.md § "Sampling Rate"
  </read_first>
  <action>
    Run these as real checks and record every command and its OUTPUT in the SUMMARY. This task
    produces evidence, not code — if a sweep finds something, fix it here and say so.

    Sweep A — stale `isTauri()` guard on the login path (34.4 gate item 2's failure mode: a guard
    whose own comment asserted a premise a later plan had falsified, leaving a ported channel
    unreachable while every test stayed green):
      1. `git diff --name-only main...HEAD` (or the phase's base commit) to enumerate THIS phase's
         touched files.
      2. `grep -rn "isTauri" <those files>` plus `grep -rn "isTauri" src/frontend/state/GlobalState.tsx
         src/frontend/screens/WebView src/frontend/screens/Humble`.
      3. For every hit, classify it in the SUMMARY as (i) a correct transport-routing guard in
         `preload/ipc.ts`/`preload/tauriTransport.ts`/`preload/api/helpers.ts`, (ii) a deliberate new
         guard this phase added, or (iii) a STALE feature guard that short-circuits a channel this
         phase just ported. Any (iii) is a defect: fix it and record the fix.
      4. Specifically confirm each of the 6 ported channels is reachable from its caller with no
         early return in between — name the call site for each in the SUMMARY.

    Sweep B — bare/aliased import build regression (34.4-10 caught one invisible to `tsc --noEmit`,
    because `tsconfig.json`'s `baseUrl` masks it, and to every jest suite, because none run a real
    bundler — but fatal to `npm start`):
      1. `npm start` and confirm the Electron renderer dev server compiles; capture the line.
      2. `pnpm tauri:dev` builds far enough to serve the frontend; capture the line.
      3. `grep -rn "^import .*from '\(preload\|common\|backend\|frontend\)/" ` across this phase's
         changed frontend files and confirm each alias has a matching Vite alias, or convert it to a
         relative import.

    Sweep C — anti-pattern greps, each recorded with its output:
      - `grep -rc "cookies_for_url" src src-tauri/src` == 0
      - `grep -rc "clear_all_browsing_data" src src-tauri/src` == 0
      - `grep -rn "on_navigation" src-tauri/src/main.rs` — the ONLY permitted use is the reveal
        exfil interception from plan 34.4.1-04; assert no navigation RELAY consumes it
      - `grep -rc "navigator.clipboard" src/frontend/screens/WebView` == 0

    Finally: `npm run test:ci`, `npx tsc --noEmit`, and `cd src-tauri && cargo check && cargo test` —
    all green, all recorded.
  </action>
  <acceptance_criteria>
    - `npm run test:ci` exits 0; `npx tsc --noEmit` exits 0; `cd src-tauri && cargo check && cargo test` exit 0.
    - The SUMMARY contains a classification line for EVERY `isTauri()` hit found, and a named call site for each of the 6 ported channels.
    - The SUMMARY records the actual `npm start` and `pnpm tauri:dev` output lines proving both builds compile.
    - All four Sweep C greps are recorded with their numeric results, and `on_navigation` has no relay consumer.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:ci && npx tsc --noEmit && cd src-tauri && cargo check && cargo test</automated>
  </verify>
  <done>Both builds compile, both suites are green, every `isTauri()` guard on the login path is classified, and the four anti-pattern greps are clean and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app-wide cookie jar → per-storefront disconnect | One storefront's sign-out acts on a jar shared with every other storefront |
| credential store → cookie jar | Two independent deletions whose ORDER decides whether a failure can strand a secret on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.1-30 | Denial of Service (to the user's other sessions) | `disconnect()`'s cookie step | mitigate | Domain-scoped `clearCookies(label, 'humblebundle.com')` only; `clear_all_browsing_data` grep-asserted absent repo-wide. The live gate additionally proves a planted non-Humble cookie SURVIVES (item 3). |
| T-34.4.1-31 | Information Disclosure | credential vs cookie deletion ordering | mitigate | `configStore.clear()` stays FIRST and unconditional (ASVS V3, the WR-02/T-10-07 rule); the cookie clear is a guarded best-effort step that cannot abort it. Order is asserted by test, not by comment. |
| T-34.4.1-32 | Repudiation | a ported-but-unreachable channel | mitigate | Sweep A classifies every `isTauri()` guard on the login path and names the reachable call site for each of the 6 channels — the exact defect that made 34.4's gate item 2 fail while all automation stayed green. |
| T-34.4.1-33 | Tampering | an import that breaks only the real bundler | mitigate | Sweep B runs both dev servers, because `tsc --noEmit` and jest structurally cannot see this class of regression. |
| T-34.4.1-34 | Information Disclosure | cookie logging during the clear | mitigate | Only the deleted COUNT is logged; no name, domain or value (the removed-`getFullCookieHeader` discipline in `user.ts` L204-218). |
| T-34.4.1-SC | Tampering | npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `npm run test:ci`, `npx tsc --noEmit`, `cargo check`, `cargo test` all green.
- `npm start` and `pnpm tauri:dev` both compile and run.
- The reach-ledger baseline is measured and its delta reported honestly.
</verification>

<success_criteria>
`humbleDisconnect` is fully ported with a domain-scoped clear that cannot touch another storefront's
cookies, the reach ledger reflects measurement rather than prediction, and both sweeps that caught
real defects in the previous phase have been run against this phase's own diff with their output on
the record.
</success_criteria>

<output>
Create `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-06-SUMMARY.md` when done
</output>
