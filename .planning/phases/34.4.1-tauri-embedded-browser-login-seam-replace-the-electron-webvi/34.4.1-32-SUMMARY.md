---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 32
status: complete
completed: 2026-08-23
wave: 2
gap_cycle: 3
commit: f7eeb918f
---

# Plan 34.4.1-32 — SUMMARY

**D-29-03 CLOSED. D-29-02 deliberately NOT closed — the planned outcome.**

## D-29-03 — reveal success is now observable

`revealKey()` emits exactly one INFO line on success:
`Humble adapter: /... reveal succeeded  keyPresent=true durationMs=<n> contentType=<t>`.

Live-gate item 4's central outcome — *did the real network call work* — is now machine-checkable
rather than resting entirely on the operator's screen.

**Two tests, each RED-proven against the defect it guards:**

| test | mutation | result |
|---|---|---|
| completion line exists exactly once, with `durationMs` | delete the `logInfo` call | **FAILS** ✓ |
| revealed key reaches no log sink | interpolate the key into the line | **FAILS** ✓ |

The redaction test audits **all three sinks**, not just the one this call uses — a redaction check
driven through a single caller has found exactly one leak here four times where a census found more.
It also asserts no `keyLength=`: a length is a side channel on a secret whose value is the point.

**No HTTP status, as a DECISION.** D-29-03 asked for "status and duration". `HumbleRawResponse`
carries only `{ data, contentType }`; adding status means widening the shared transport type across
**both** branches — precisely the seam-parity surface F-1 and F-6 hid in — for an observability
nicety. Recorded in-source so a reader sees a decision, not a missing half.

## D-29-02 — handed to plan 35, not guessed

The plan instructed: *if it cannot be determined without a live authenticated session, say so and
stop*. It cannot. Both candidate explanations (path moved / interstitial answering) fit every
offline observation equally, and an unauthenticated probe cannot discriminate — both would answer
anonymous requests with HTML. `adapter.ts:673` records the path as empirically confirmed in Plan 05,
which rules out "never existed" but neither live candidate.

Guessing would repeat the failure that cost F-10 nine live runs.

## No duplicate test written

The plan's acceptance asked for a test that `getAccountIdentity` cannot block login acceptance.
**`user.test.ts:337` and `:353` already pin exactly that** (gamekeys ok + identity non-ok/throwing
still resolves login `done`). Verified passing rather than duplicated.

## Verification

humble suite **564/564**, backend **4082 passed / 0 failed**, tsc 0, eslint 0 errors. Seam sweep
still exit 0 with an unchanged report and `seamBranchParity` 28/28 — plan 31 warned this plan could
shift a line hint; it did not.
