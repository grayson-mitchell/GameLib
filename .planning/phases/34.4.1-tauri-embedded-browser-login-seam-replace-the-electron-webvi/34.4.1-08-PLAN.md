---
phase: 34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi
plan: 08
type: execute
wave: 6
depends_on: ["34.4.1-07"]
files_modified:
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE.md
  - .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
  - .planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-LIVE-GATE.md
  - .planning/IPC-PORT-INVENTORY.md
autonomous: false
requirements: [REQ-34.4.1-02, REQ-34.4.1-05, REQ-34.4.1-06, REQ-34.4.1-10, REQ-34.4.1-12]

must_haves:
  truths:
    - "D-10: the blocking 4-item gate is RUN under `pnpm tauri:dev` on macOS, from the explicit numbered preconditions written in wave 1 — not improvised at gate time, which is how a gate becomes UAT debt (Phases 21/30/34.2)."
    - "D-10 item 1: a Humble login is completed FROM SCRATCH with no Electron-captured session — the headline deliverable, and the direct falsification of 34.4 D-09's 'there is no Tauri path to create a Humble session'."
    - "D-10 item 2: the session survives quit-and-relaunch, proven at the PERSISTENCE layer (an absent-to-present store transition), the stronger method 34.4-10 established over a UI check."
    - "D-08/D-10 item 3: disconnect leaves no `humblebundle.com` cookie AND the planted non-Humble cookie survives — a blanket wipe would pass a naive item 3 while being the exact defect D-08 forbids."
    - "D-07/D-10 item 4: `humbleRevealKey`'s real outcome is recorded either way; a 403 is a valid declarable result, not a phase failure."
    - "D-09: macOS is the live platform, as with every prior slice (34.3 D-12, 34.4 D-10); gate failure is BLOCKING and becomes a gap cycle inside 34.4.1 — no advisory mode, no pre-authorized override."
  artifacts:
    - path: ".planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md"
      provides: "Filled results, per-item evidence, and the overall verdict"
      contains: "Verdict"
    - path: ".planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md"
      provides: "Forward-referenced live proof cells replaced with actual results"
  key_links:
    - from: "34.4.1-LIVE-GATE.md item 1 result"
      to: "34.4-LIVE-GATE.md's manual precondition"
      via: "the struck-from-the-record statement that no Tauri path to a Humble session exists"
      pattern: "34.4 D-09"
---

<objective>
Run the phase's blocking 4-item live gate on real hardware against the real Humble origin, record
what actually happened, and update the record — including striking a statement a previous phase had
to declare true and this one falsifies.

Purpose: every prior slice that skipped or softened this step turned it into UAT debt, and 34.4's
gate caught a completely unreachable ported channel that every unit test, `tsc`, `cargo check` and
the reach ledger had called green.

Output: a filled gate document, corrected proof levels, and either a closed phase or a named gap
cycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-PORTED-CHANNELS.md
@.planning/phases/34.4-tauri-ipc-re-plumb-slice-7-steam-completion-and-humble/34.4-LIVE-GATE.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-06-SUMMARY.md
@.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Satisfy and evidence every numbered precondition before spending human gate time</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md § "Preconditions" (written in wave 1 — follow it literally; if a precondition turns out to be wrong or missing, amend it openly and say so)
    - .planning/phases/34.4-.../34.4-LIVE-GATE.md (how the previous gate recorded evidence)
    - .planning/phases/34.4.1-.../34.4.1-06-SUMMARY.md (both builds' compile evidence)
  </read_first>
  <action>
    Work through the numbered preconditions and record evidence for each INTO the gate document
    before any human step:
      1. Build and launch: `pnpm tauri:dev` from a clean build; capture the launch line and confirm
         the sidecar started. Sidecar output is read from `~/Library/Logs/GameLib/gamelib.log`, NOT
         the `tauri:dev` terminal.
      2. Confirm the from-scratch starting state: show the Humble credential store's contents
         (absent, or `isLoggedIn:false` with no `sessionCookie`) and paste the evidence. If a
         credential exists, disconnect or move the store aside FIRST and re-show it — item 1 is
         meaningless from a warm session.
      3. Confirm an UNREVEALED Humble key exists for item 4, and record its gamekey/machineName
         (redacted as needed) so the item is reproducible.
      4. Plant or identify a NON-Humble cookie of known name and domain in the app-wide jar (any
         other site loaded once through a child window is sufficient) and record it — item 3's
         domain-scope proof depends on it.
      5. Re-run the fast automated checks as a sanity baseline: `npm run test:ci`,
         `cd src-tauri && cargo test`. Record both exit codes.
    If any precondition cannot be satisfied, STOP and report it rather than proceeding with a weaker
    version of the gate.
  </action>
  <acceptance_criteria>
    - Every numbered precondition in the gate document has evidence pasted beneath it.
    - The from-scratch credential state is shown as raw store contents, not asserted in prose.
    - The planted non-Humble cookie's name and domain are recorded.
    - `npm run test:ci` and `cargo test` exit codes are recorded as 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:ci && cd src-tauri && cargo test</automated>
  </verify>
  <done>Every precondition is satisfied and evidenced in the document, so no gate item can later be attributed to a bad starting state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the blocking 4-item live gate</name>
  <files>.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-LIVE-GATE.md</files>
  <read_first>
    - .planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md (the full item list and required evidence, written in wave 1)
    - .planning/phases/34.4.1-.../34.4.1-08-PLAN.md Task 1's recorded precondition evidence
    - .claude/skills/spike-findings-gamelib/references/tauri-login-webview-cookies.md (the failure shapes to watch for while driving the UI)
  </read_first>
  <action>
    Present the four gate items to the developer with the exact steps below, wait for them to drive
    the real UI under `pnpm tauri:dev`, and transcribe each item's result and evidence into
    `34.4.1-LIVE-GATE.md` as it is reported. Do not proceed past a failed item by reinterpreting it;
    a failure is BLOCKING and becomes a gap cycle inside 34.4.1.
  </action>
  <acceptance_criteria>
    - All four items have a recorded PASS or FAIL with the evidence the gate document requires.
    - Item 2's evidence is a persistence-layer store transition, not a UI observation.
    - Item 3's evidence includes BOTH "no humblebundle.com cookie remains" AND "the planted non-Humble cookie survives".
    - Item 4's evidence records the actual reveal outcome, including a 403 body classification if that is what happened.
    - No cookie value, session cookie or revealed key value appears anywhere in the transcript.
  </acceptance_criteria>
  <done>All four items are recorded with evidence and the developer has signalled approval or named the failures.</done>
  <what-built>
    A Tauri-native embedded-browser login seam: five Rust `dispatch_rust_channel` arms that open a
    fail-closed child window, observe its main-frame navigations and read/delete cookies with a
    correct domain-suffix match; a sidecar module registering all six Humble browser-auth channels;
    a `watchForLogin()` that drives that window instead of `session.fromPartition`; a hidden,
    on-demand reveal window that POSTs from the page's own JS context; a domain-scoped cookie clear
    on disconnect; and a split login/store panel in the WebView screen.
  </what-built>
  <how-to-verify>
    All four items run under `pnpm tauri:dev` on macOS. Sidecar log:
    `tail -f ~/Library/Logs/GameLib/gamelib.log` (NOT the `tauri:dev` terminal). Record the result of
    every item in `34.4.1-LIVE-GATE.md`, pass or fail, with the evidence the document asks for.

    1. HUMBLE LOGIN FROM SCRATCH. With no stored Humble credential (precondition 2), open
       Settings/Humble and start the sign-in. Expect: a real login window opens on
       humblebundle.com; the WebView route shows the in-progress surface, not an "unavailable"
       panel; you complete sign-in in that window; the app transitions to connected and the login
       window closes by itself. Record: the window appearing, the transition, and the store now
       holding an encrypted `sessionCookie` with `isLoggedIn:true`.
    2. SESSION SURVIVES QUIT-AND-RELAUNCH. Fully quit the app (Cmd+Q), relaunch `pnpm tauri:dev`,
       and confirm Humble is still connected. Prove it at the PERSISTENCE layer — show the store's
       `isLoggedIn`/`sessionCookie` state before and after — not by a UI glance.
    3. DISCONNECT IS DOMAIN-SCOPED. Disconnect Humble. Confirm (a) the credential store is cleared,
       (b) no `humblebundle.com` cookie remains, and (c) the planted non-Humble cookie from
       precondition 4 SURVIVES. Then start a fresh login and confirm it genuinely starts logged out
       (Humble's own login form, not an auto-signed-in page).
    4. REVEAL OUTCOME. Open the Humble Claim Wizard for the unrevealed key from precondition 3 and
       reveal it. Record the ACTUAL outcome: a revealed key value, or a 403/Cloudflare challenge, or
       any other error — with the sidecar log lines. A 403 is a VALID, DECLARABLE result and not a
       phase failure. Do NOT re-run rounds 1-5 of the `humble-reveal-key-fails` investigation
       whatever happens.

    While driving the UI, watch for the failure shapes this project has been bitten by: a login that
    "keeps trying" with a silent log (the UNDECIDABLE poll), a surface that shows nothing at all
    (an unreachable channel), and any window whose title reads `GameLib` while showing remote
    content.
  </how-to-verify>
  <resume-signal>Type "approved" with the per-item results, or describe what failed and on which item</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the outcome and strike what the gate falsified</name>
  <files>.planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md, .planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md, .planning/phases/34.4-.../34.4-LIVE-GATE.md, .planning/IPC-PORT-INVENTORY.md</files>
  <read_first>
    - .planning/phases/34.4.1-.../34.4.1-LIVE-GATE.md (as filled during Task 2)
    - .planning/phases/34.4.1-.../34.4.1-PORTED-CHANNELS.md (the forward-referenced live cells to replace)
    - .planning/phases/34.4-.../34.4-LIVE-GATE.md (locate the manual precondition asserting there is no Tauri path to create a Humble session)
    - .planning/IPC-PORT-INVENTORY.md L73-75 (the `humbleDisconnect` declared-partial note)
  </read_first>
  <action>
    1. Finalize `34.4.1-LIVE-GATE.md`: per-item PASS/FAIL, the evidence, and an overall verdict. If
       any item failed, state plainly that the failure is BLOCKING and enumerate the findings as
       candidate gap-cycle items — do not soften a failure into a caveat.
    2. Update `34.4.1-PORTED-CHANNELS.md`: replace every forward-referenced `unit + LIVE (item N)`
       cell with what the gate actually proved. Where an item failed, downgrade the row honestly and
       name the gap. Record item 4's real reveal outcome in the `humbleRevealKey` row and in the
       "outcome, stated plainly" section — including, if it 403'd, that the channel ships
       declared-degraded per D-07.
    3. ONLY IF gate item 1 PASSED: strike 34.4 D-09's manual precondition in
       `.planning/phases/34.4-.../34.4-LIVE-GATE.md` — the statement that there is no Tauri path to
       create a Humble session. Strike it in place with a dated one-line note pointing at this
       phase's gate item 1 rather than deleting the history.
    4. ONLY IF gate item 3 PASSED: update `.planning/IPC-PORT-INVENTORY.md` L73-75's
       `humbleDisconnect` declared-partial note to record that Phase 34.4.1 closed it, keeping the
       history readable. This is the ONE edit to that file this phase is permitted, and it is
       gated on the live proof — plan 34.4.1-07 deliberately left it alone.
    5. If the gate did not pass 4/4, report the failing items to the orchestrator as a gap cycle
       inside 34.4.1 and stop — do not mark the phase complete.
  </action>
  <acceptance_criteria>
    - `34.4.1-LIVE-GATE.md` has a recorded result for all four items and an overall verdict.
    - No `unit + LIVE (item N)` cell remains as an unresolved forward reference in `34.4.1-PORTED-CHANNELS.md`.
    - If item 1 passed: `git diff .planning/phases/34.4-.../34.4-LIVE-GATE.md` shows the precondition struck with a dated pointer, not deleted.
    - If item 3 passed: `git diff .planning/IPC-PORT-INVENTORY.md` shows exactly the L73-75 note updated and nothing else.
    - `python3 .planning/phases/34.4.1-.../ported-channels-gate.py` still exits 0 after the edits (its inventory-untouched check may need to move to a per-line assertion — if so, change the GATE deliberately and say why).
  </acceptance_criteria>
  <verify>
    <automated>python3 .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py && python3 .planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/ported-channels-gate.py --self-test</automated>
  </verify>
  <done>The gate's real results are on the record, the declared list reflects them, and the two statements this phase falsified are struck with dated pointers rather than quietly deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real credentials → live third-party origin | The gate signs in for real, against a Cloudflare-fronted production site |
| gate evidence → project record | A softened or skipped item becomes silent UAT debt that a later phase inherits as fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.4.1-39 | Information Disclosure | evidence pasted into the gate document | mitigate | Cookie values, session cookies and revealed key values are NEVER pasted; record presence/absence, name, domain and length only. Redact gamekey/machineName where practical. |
| T-34.4.1-40 | Repudiation | a softened gate result | mitigate | Per-item PASS/FAIL with required evidence and an explicit statement that failure is BLOCKING and becomes an in-phase gap cycle; no advisory mode and no pre-authorized override. |
| T-34.4.1-41 | Denial of Service (to the user's own data) | item 3's disconnect | accept | Disconnect intentionally deletes the Humble credential; the operator re-logs in as part of the same item. Non-Humble sessions must survive, which is exactly what item 3 asserts. |
| T-34.4.1-42 | Tampering | item 4's irreversible reveal | accept | Revealing a Humble key is irreversible AT HUMBLE but not destructive — per Phase 14's D-30 realignment `redeemed_key_val` means revealed, not activated, so unlike 34.4 D-08's refused `redeemSteamKey` this is legitimately live-testable. |
| T-34.4.1-43 | Spoofing | a phishing-shaped login window | mitigate | The operator is instructed to check that no window shows `GameLib` as its title while displaying remote content (WR-07). |
| T-34.4.1-SC | Tampering | package installs | accept | Zero packages; this plan runs and records. |
</threat_model>

<verification>
- All four gate items have recorded results with evidence.
- `python3 ported-channels-gate.py` and `--self-test` exit 0 after the record updates.
- `npm run test:ci` and `cargo test` were green immediately before the human gate.
</verification>

<success_criteria>
The mechanism is proven on real hardware against the real origin: a Humble session created from
scratch under Tauri, surviving a restart, cleanly and narrowly disconnected, with the reveal
attempt's true outcome recorded — or a blocking, honestly-named set of gap-cycle findings.
</success_criteria>

<output>
Create `.planning/phases/34.4.1-tauri-embedded-browser-login-seam-replace-the-electron-webvi/34.4.1-08-SUMMARY.md` when done
</output>
