---
phase: 46-windows-single-instance-guard-and-gamelib-deep-link-registra
plan: 07
type: execute
wave: 2
depends_on: ["46-06"]
gap_closure: true
files_modified:
  - .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md
  - .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE.md
  - .planning/todos/pending/2026-08-29-windows-single-instance-guard-and-deep-link-registration.md
  - .planning/todos/completed/2026-08-29-windows-single-instance-guard-and-deep-link-registration.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [REQ-46-10, REQ-46-11, REQ-46-01, REQ-46-02]

must_haves:
  truths:
    - "Against the INSTALLED 46-06 debug NSIS build, a bare second launch from a terminal AND from the Start menu each restore and focus a minimized primary. Both are measured (IsIconic False, foreground handle equals the primary's), not just described"
    - "The primary's console shows the new sentinel-receipt line and a raise-result line for each bare second launch"
    - "Checks 1-2 (warm ping delivery, 1/1 counts with the sidecar parented by the single shell), Check 4 (two near-simultaneous launches give 1/1) and Check 5 (a force-killed primary does not block the next launch) are re-run on this build with numeric observations"
    - "46-LIVE-GATE.md's FAIL record is preserved (additions only). The re-run lives in 46-LIVE-GATE-RERUN.md"
    - "The todo, ledger row U-34.5-18 and the REQ-46-10 traceability close ONLY on an all-PASS verdict. A FAIL leaves all three open and is reported as a gap"
  artifacts:
    - path: ".planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md"
      provides: "Numeric PASS/FAIL record of P0, Checks 1, 2, 3a, 3b, 4, 5 and supplementary S1 against the 46-06 build"
      contains: "Check 3b"
  key_links:
    - from: "46-LIVE-GATE-RERUN.md verdict"
      to: "todo move + U-34.5-18 row + REQ-46-10 traceability"
      via: "Task 3 runs the closure branch only on `Verdict: PASS`"
      pattern: "Verdict: PASS"
---

<objective>
Re-run the Windows live gate (REQ-46-10) against the 46-06 build on this Windows 11 machine. Then do the closure bookkeeping that 46-05 could not (REQ-46-11), gated on the verdict.

The re-run covers:
- Check 3, the failing check. It is split into 3a (terminal launch) and 3b (Start-menu launch, the real user path, which exercises the foreground-lock grant), each measured with `IsIconic` and `GetForegroundWindow`.
- Checks 4 and 5, never run before.
- Checks P0, 1 and 2, re-confirmed on the new build.
- One non-gating supplementary check on the tray left-click, which 46-06 also changed.

Purpose: the only evidence that closes Check 3 is a real minimized window being restored on Windows. No automated suite can observe that.
Output: `46-LIVE-GATE-RERUN.md`. On PASS: the todo moves to completed, U-34.5-18 is CLOSED, REQ-46-10 (and the stale REQ-46-09 row) is Complete, and a pointer is appended to `46-LIVE-GATE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE.md
@.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-06-SUMMARY.md
@.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-05-SUMMARY.md
@.planning/todos/pending/2026-08-29-windows-single-instance-guard-and-deep-link-registration.md

**Orchestrator constraints (binding on every task):**
- Do NOT invoke any `gsd-sdk query state.*` verb, and do not edit `.planning/STATE.md`. The orchestrator owns it.
- Agents never kill processes. Force-kill and orphan cleanup in Check 5 are OPERATOR actions, performed and reported by the operator.
- Every file written gets `npx prettier --check --ignore-unknown <exact paths>`.
- In Git Bash, count installer.nsi lines with `grep -cF 'Classes\gamelib'`.
- Any UAT-style `expected:` line is inline, never `expected: |`. Any new todo in `.planning/todos/pending/` carries `severity:`/`platform:`/`ready:` in that order, bare and lowercase.

**Operational facts measured in the first gate (46-LIVE-GATE.md), which the gate script MUST use:**
- The installed exe is `%LOCALAPPDATA%\GameLib\gamelib-shell.exe` (absolute: `C:\Users\grays\AppData\Local\GameLib\gamelib-shell.exe`), and its process name is `gamelib-shell`, NOT `GameLib`.
- The debug build spawns the sidecar as `node <repo>\build\main\sidecar.js`, NOT `gamelib-sidecar.exe`. Count it as `(Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -like '*sidecar.js*' }).Count`.
- After install, the registry value is `"C:\Users\grays\AppData\Local\GameLib\gamelib-shell.exe" "%1"`.
- The debug build has a console, so `[shell]` lines appear in the PowerShell window that launched the process. A process launched from Explorer or the Start menu gets its own transient console.
- `<appid>` 2706020 (All Will Fall) and runner `steam` were used before. This re-run uses the side-effect-free `ping` route throughout, so no game is launched.

**Log lines the 46-06 build emits (from 46-06):**
- Primary, on sentinel: `[shell] received single-instance focus sentinel -- raising the main window`, then `[shell] focus sentinel raise: unminimize=ok, show=ok, set_focus=ok` (any `err=` value is recorded verbatim).
- Secondary: `[shell] another GameLib instance is already running -- sending focus sentinel to it and exiting`, then either `[shell] granted foreground rights to the running instance (pid=N)` or a `[shell] WARN: could not grant foreground rights ...` line.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight (build freshness, installer.nsi, binary carries the fix) and write the 46-LIVE-GATE-RERUN.md contract</name>
  <files>.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md</files>
  <read_first>
    - .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-06-SUMMARY.md (setup .exe path/size/mtime and the build sha)
    - .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE.md (the whole file: format, check tables, the FAIL record. This file is NOT edited in this task)
    - .planning/REQUIREMENTS.md (Grep `REQ-46-10` and Read that requirement's five checks)
  </read_first>
  <action>
    **Freshness.**
    - Run `git diff --stat <46-06 build sha>..HEAD -- src-tauri/ src/`. It must be empty.
    - If it is not empty, STOP and report. Do not rebuild here: a rebuild is 46-06 Task 3's job, and a stale build would make this whole gate test the wrong code.
    - Re-assert `grep -cF 'Classes\gamelib' src-tauri/target/debug/nsis/x64/installer.nsi` = 6 and `grep -caF 'received single-instance focus sentinel -- raising the main window' src-tauri/target/debug/gamelib-shell.exe` at least 1.

    **Write `46-LIVE-GATE-RERUN.md`.** Do not spell the first script's stale process names anywhere in this file, not even in prose. Refer to 46-LIVE-GATE.md's "Other observations" instead, so the stale-name grep below stays exact. Mirror 46-LIVE-GATE.md's layout: frontmatter, a build-under-test table, then per check a PowerShell block, and an `| Item | Expected | Observed | Result |` table whose Observed and Result cells are `_pending_`.
    - Frontmatter: `status: pending`, `phase`, `plan: 46-07`, `supersedes: 46-LIVE-GATE.md (FAIL at Check 3, 2026-09-24)`, `build_sha`, `build_type`, `built_at`, `operator: Grayson Mitchell`.
    - Open with a notice stating two things. First, nothing in the file may be filled in by an agent from inference: Observed cells hold only operator-reported numbers, True/False values or verbatim console lines. Second, `46-LIVE-GATE.md` is the preserved record of the first run and is not edited.
    - Add a `## What changed since the first run` paragraph naming the 46-06 fix: unminimize in the Windows sentinel arm, sentinel logging, the secondary foreground grant, and tray unminimize.

    **The sections, each with its exact commands.** Use the literal installed path via `$exe = "$env:LOCALAPPDATA\GameLib\gamelib-shell.exe"`.

    **`## Helpers` (run once in PowerShell window B).**
    - `Add-Type -Namespace GL -Name W -MemberDefinition '[DllImport("user32.dll")] public static extern bool IsIconic(IntPtr h); [DllImport("user32.dll")] public static extern IntPtr GetForegroundWindow();'`
    - A `$sidecars` scriptblock `{ (Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -like '*sidecar.js*' }).Count }`
    - `$shells` = `{ (Get-Process gamelib-shell -ErrorAction SilentlyContinue).Count }`

    **`## P0`.**
    - Quit any running GameLib normally (tray, then Quit), and confirm `& $shells` and `& $sidecars` both print 0.
    - Run the 46-06 setup .exe (absolute path from 46-06-SUMMARY).
    - `reg query "HKCU\Software\Classes\gamelib\shell\open\command" /ve` is expected to be `"C:\Users\grays\AppData\Local\GameLib\gamelib-shell.exe" "%1"`.
    - `(Get-Item $exe).LastWriteTime` must be later than the 46-06 build time. This proves the reinstall replaced the old binary.
    - `Select-String -Path $exe -Pattern 'received single-instance focus sentinel' -SimpleMatch -Quiet` must be expected `True`.

    **`## Check 1` (warm ping re-confirm).**
    - In PowerShell window A: `& "$env:LOCALAPPDATA\GameLib\gamelib-shell.exe"`, and wait for the library to render.
    - In B: `& $shells; & $sidecars` (expected 1 and 1), then `Start-Process 'gamelib://ping?phase=46&check=1'`.
    - Expected: A prints `[shell] delivered single-instance deep link to sidecar: ok`, no new window appears, and the counts stay 1/1.

    **`## Check 2`.**
    - 5 s after the ping: the counts, plus `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -like '*sidecar.js*' } | Select ProcessId,ParentProcessId` and `(Get-Process gamelib-shell).Id`.
    - Expected: the sidecar's ParentProcessId equals the single gamelib-shell Id.

    **`## Check 3a` (bare second launch from a terminal).**
    - In B: `$h = (Get-Process gamelib-shell).MainWindowHandle`. Capture it BEFORE minimizing, and it must be nonzero.
    - Minimize the GameLib window, then `[GL.W]::IsIconic($h)`, expected `True`.
    - Then run ONE line, so the terminal does not retake focus before sampling: `& $exe; "exit=$LASTEXITCODE"; Start-Sleep 2; "iconic=$([GL.W]::IsIconic($h))"; "foreground=$([GL.W]::GetForegroundWindow() -eq $h)"`.
    - Rows:
      - the secondary's `another GameLib instance ...` line;
      - the secondary's grant line (verbatim, either form; informational);
      - exit, expected 0;
      - iconic, expected False;
      - foreground, expected True;
      - window A's `received single-instance focus sentinel` line;
      - window A's `focus sentinel raise:` line, expected all three `ok`;
      - the operator's own eyes: window visible and in front, Yes;
      - counts 1/1.

    **`## Check 3b` (bare second launch from the Start menu, the real user path).**
    - Minimize GameLib again and confirm `[GL.W]::IsIconic($h)` is `True`.
    - In B run `Start-Sleep 15; "iconic=$([GL.W]::IsIconic($h))"; "foreground=$([GL.W]::GetForegroundWindow() -eq $h)"; & $shells; & $sidecars`. Within the 15 s, launch GameLib from the Start menu shortcut, without clicking back into B. A transient console window for the secondary may flash and close; that is expected for a debug build.
    - Rows:
      - iconic, expected False;
      - foreground, expected True;
      - window A's two sentinel lines (the raise line all `ok`);
      - counts 1/1.

    **`## Check 4`.**
    - Quit GameLib normally, then counts 0/0.
    - `Start-Process $exe; Start-Process $exe` in ONE line.
    - After 15 s: `& $shells; & $sidecars` expected 1 and 1, and `(Get-Process gamelib-shell | Where-Object MainWindowHandle -ne 0).Count` expected 1.

    **`## Check 5`.**
    - OPERATOR runs `Stop-Process -Name gamelib-shell -Force`.
    - Record `& $sidecars`. A surviving orphan is pre-existing (no job object) and does NOT fail the check. If it is nonzero, the operator cleans it up with `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -like '*sidecar.js*' } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }` and records that.
    - Relaunch in A with `& $exe`. Expected: a normal primary with a window, and counts 1/1.
    - Then in B, `Start-Process 'gamelib://ping?phase=46&check=5'`. Expected: A prints `delivered single-instance deep link to sidecar: ok`, no second window appears, and the counts stay 1/1.

    **`## Supplementary S1` (NON-GATING, the tray left-click that 46-06 changed).** Minimize, left-click the tray icon, then `[GL.W]::IsIconic($h)` (re-capture `$h` after the Check 5 relaunch). Expected False. State in the section that S1's result does not enter the verdict, and that a FAIL here files a todo instead.

    **Ending.**
    - `## Verdict` with `Verdict: _pending_`. The rule: PASS only if P0, 1, 2, 3a, 3b, 4 and 5 all match Expected. The Check 5 orphan observation, the informational grant line and S1 do not by themselves fail it. Otherwise `FAIL (first failing: <check>)`.
    - `## If any check FAILS`, carrying the same instructions as 46-LIVE-GATE.md's section: do not ship; record the console output; stop running further checks; the operator chooses between another `--gaps` cycle and reverting 46-04's `607089433`; the todo and U-34.5-18 stay open.
  </action>
  <verify>
    <automated>test "$(grep -cF 'Classes\gamelib' src-tauri/target/debug/nsis/x64/installer.nsi)" = "6" && test "$(grep -caF 'received single-instance focus sentinel -- raising the main window' src-tauri/target/debug/gamelib-shell.exe)" -ge 1 && test "$(grep -cE '^## (P0|Check 1|Check 2|Check 3a|Check 3b|Check 4|Check 5|Supplementary S1|Verdict|If any check FAILS)' .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md)" = "10" && npx prettier --check --ignore-unknown .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md</automated>
  </verify>
  <acceptance_criteria>
    - The heading count in `<verify>` is exactly 10
    - `grep -c 'gamelib-shell' 46-LIVE-GATE-RERUN.md` is at least 5, and `grep -cE "Get-Process GameLib\b|gamelib-sidecar\.exe" 46-LIVE-GATE-RERUN.md` prints 0. The stale process names from the first script are gone
    - `grep -c 'IsIconic' 46-LIVE-GATE-RERUN.md` is at least 4, and `grep -c 'GetForegroundWindow' 46-LIVE-GATE-RERUN.md` is at least 3
    - `grep -c '_pending_' 46-LIVE-GATE-RERUN.md` is at least 20, and the file contains `Verdict: _pending_`
    - `git diff --stat <46-06 build sha>..HEAD -- src-tauri/ src/` is empty, and its output is quoted in the file's build table
    - `git diff --quiet .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE.md` exits 0 (the first run's record is untouched in this task)
  </acceptance_criteria>
  <done>The operator has a copy-pasteable re-gate script, bound to the proven-fresh 46-06 build, using the correct process names and objective window-state measurements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: OPERATOR: install the 46-06 build and run P0, Checks 1, 2, 3a, 3b, 4, 5 and S1 on this Windows 11 machine</name>
  <files>.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md</files>
  <read_first>
    - .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md (the script written by Task 1)
  </read_first>
  <action>Operator-only. Installing software, minimizing and restoring windows, using the Start menu and force-killing processes are human actions in this workflow. Claude must not run the installer or kill anything. Follow 46-LIVE-GATE-RERUN.md top to bottom and report each Observed value.</action>
  <what-built>
    - Plan 46-06 fixed the Windows focus-sentinel raise: `unminimize()` before `show()`/`set_focus()`, with receipt and result logging.
    - The secondary now grants foreground rights to the owner-verified primary PID before writing.
    - The tray "show" and left-click handlers also unminimize.
    - A fresh debug NSIS setup .exe carries all of this. Its path and sha are in 46-LIVE-GATE-RERUN.md.
  </what-built>
  <how-to-verify>
    1. Quit any running GameLib (tray, then Quit). Run the P0 commands in 46-LIVE-GATE-RERUN.md, including the setup .exe, `reg query`, the exe LastWriteTime and the `Select-String ... -Quiet` check.
    2. Run the `## Helpers` block once in PowerShell window B.
    3. Run Checks 1, 2, 3a, 3b, 4 and 5 in order, exactly as written. Report numbers and True/False values as printed, and the `[shell]` lines verbatim from BOTH windows.
    4. Checks 3a and 3b: the sampling commands must run without you clicking back into window B first. 3a's one-liner handles this. In 3b, start the 15-second sampler, then use the Start menu.
    5. Check 5: you perform the force-kill and any orphan cleanup yourself, and report whether an orphan sidecar survived.
    6. S1 is informational. Report it either way.
    7. If any gating check deviates from Expected, stop there and report it.
  </how-to-verify>
  <verify>
    <human-check>Every gating row in 46-LIVE-GATE-RERUN.md has an operator-reported Observed value, or the first failing check is named</human-check>
  </verify>
  <done>The operator has run the re-gate and reported results. Task 3 records them.</done>
  <resume-signal>Paste the observed values per check, or describe the first failing check</resume-signal>
  <acceptance_criteria>
    - The operator has reported Observed values for P0 and Checks 1, 2, 3a, 3b, 4 and 5, or has named the first failing check
    - Every count is a number, and every iconic/foreground value is the literal True or False printed by PowerShell
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Record the verdict; on all-PASS close the todo, U-34.5-18 and REQ-46-10 traceability; on FAIL leave them open</name>
  <files>.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md, .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE.md, .planning/todos/pending/2026-08-29-windows-single-instance-guard-and-deep-link-registration.md, .planning/todos/completed/2026-08-29-windows-single-instance-guard-and-deep-link-registration.md, .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md
    - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md (Grep `| U-34.5-18 |` and Read that row. Grep `CLOSED` and Read one closed row for the closure-wording convention)
    - .planning/todos/completed/ (Read the tail of one recently completed todo for its resolution-footer convention)
    - .planning/REQUIREMENTS.md (Grep `| REQ-46-` for the traceability rows ~504-514, and `- \[ \] \*\*REQ-46-` for the unticked boxes)
    - .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-01-SUMMARY.md (frontmatter `requirements-completed`, which lists REQ-46-09)
  </read_first>
  <action>
    Transcribe the operator's report into every Observed and Result cell of 46-LIVE-GATE-RERUN.md: verbatim numbers, True/False values and console lines. Set `Verdict: PASS` only under the rule the file states. Otherwise set `Verdict: FAIL (first failing: <check>)`. Set frontmatter `status:` to `passed` or `failed`.

    Append (do not edit) a `## Superseded by re-run (<date>)` section to the END of `46-LIVE-GATE.md`. It is a two-to-three-line pointer to `46-LIVE-GATE-RERUN.md` naming the re-run verdict. Do this in both the PASS and FAIL branches. Every existing line of 46-LIVE-GATE.md stays byte-identical.

    **ON PASS:**
    1. Append a `## Resolution (phase 46, <date>)` section to the todo. Cite the 46-02/46-03/46-04/46-06 commit shas, the installer.nsi 0→6 evidence, the first gate's Check 3 FAIL, the 46-06 fix, and the re-run verdict with its Check 3a/3b iconic/foreground values. Then `git mv` it from `.planning/todos/pending/` to `.planning/todos/completed/`.
    2. In 34.5-UNTESTED-ITEMS.md, update the `U-34.5-18` row's status cell to CLOSED, following the file's existing closure wording. Cite phase 46, `46-LIVE-GATE.md` (first run, FAIL) and `46-LIVE-GATE-RERUN.md` (PASS). Do not delete the original text.
    3. In REQUIREMENTS.md:
       - flip `| REQ-46-10 | Phase 46 | Pending |` to `| REQ-46-10 | Phase 46 | Complete (46-LIVE-GATE-RERUN.md) |`, and tick `- [ ] **REQ-46-10**` to `- [x]`;
       - flip the stale `| REQ-46-09 | Phase 46 | Pending |` row to `Complete (plan 46-01)` and tick its box, citing 46-01-SUMMARY's `requirements-completed`. This is records hygiene; the evidence already exists.
       - Touch no other requirement.
    4. If Check 5 observed an orphan sidecar and no pending todo already covers "force-killed shell leaves the sidecar running on Windows" (Grep `.planning/todos/pending/` for `job object` and `orphan`), file one with frontmatter `severity: minor`, `platform: windows`, `ready: code`, in that order. Name the missing Windows job object as the mechanism.
    5. If S1 (tray left-click) observed iconic=True, file a todo (`severity: minor`, `platform: windows`, `ready: live-gate`) recording it. This does not change the verdict.

    **ON FAIL:**
    - Change NONE of the closure files. The todo stays in pending, U-34.5-18 stays open, and the REQ rows are unchanged.
    - Record the failing check's verbatim console output in a `## Recorded failure (<date>)` section of 46-LIVE-GATE-RERUN.md, and record the operator's chosen branch.
    - Report the result as a gap for `/gsd-plan-phase 46 --gaps`.

    Do not edit STATE.md or ROADMAP.md: the orchestrator owns them. Commit without `--no-verify`.
  </action>
  <verify>
    <automated>grep -E "^Verdict: (PASS|FAIL)" .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md && test "$(grep -c _pending_ .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md)" = "0" && python .planning/todos/todo-frontmatter-gate.py && pnpm planning-gates && npx prettier --check --ignore-unknown .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE-RERUN.md .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE.md .planning/REQUIREMENTS.md .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</automated>
  </verify>
  <acceptance_criteria>
    - 46-LIVE-GATE-RERUN.md has 0 `_pending_` tokens and exactly one `Verdict:` line
    - `git diff HEAD~1 -- .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-LIVE-GATE.md | grep '^-' | grep -v '^---'` prints nothing (46-LIVE-GATE.md additions only)
    - If PASS:
      - `test -f .planning/todos/completed/2026-08-29-windows-single-instance-guard-and-deep-link-registration.md && test ! -f .planning/todos/pending/2026-08-29-windows-single-instance-guard-and-deep-link-registration.md`
      - the `U-34.5-18` row contains `CLOSED` and `46-LIVE-GATE-RERUN.md`
      - `grep -c "| REQ-46-.. | Phase 46 | Pending |" .planning/REQUIREMENTS.md` prints 0, and `grep -c -- "- \[ \] \*\*REQ-46-" .planning/REQUIREMENTS.md` prints 0
    - If FAIL: the todo is still in pending/, the U-34.5-18 row does not contain the word CLOSED for this phase, and `git diff HEAD~1 --stat` touches only 46-LIVE-GATE-RERUN.md and 46-LIVE-GATE.md
    - `python .planning/todos/todo-frontmatter-gate.py` and `pnpm planning-gates` exit 0
    - `git diff --quiet HEAD~1 -- .planning/STATE.md` exits 0
  </acceptance_criteria>
  <done>The phase's closure state matches the live evidence. Nothing closes without a measured PASS, and the first run's FAIL stays on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine registry and install dir | The installer rewrites HKCU `Software\Classes\gamelib` and `%LOCALAPPDATA%\GameLib`. The operator performs the install |
| planning record ↔ reality | Closing U-34.5-18 or the todo without a measured PASS would recreate the "unowned platform gap" the todo exists to prevent, and would overwrite the first run's honest FAIL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-11 | Denial of Service | shipping registration while Check 3 is unfixed | mitigate | The gate is blocking. The FAIL branch forbids shipping and offers a revert of `607089433` (D-05) |
| T-46-14 | Repudiation | closure without evidence | mitigate | Closure runs ONLY on `Verdict: PASS`, with numeric/True-False Observed values. Check 3 is measured by `IsIconic`/`GetForegroundWindow`, not by adjective |
| T-46-21 | Repudiation | stale binary under test | mitigate | The Task 1 freshness diff must be empty. In P0, the installed exe's LastWriteTime must be after the build, and the exe must contain the 46-06 log literal (`Select-String -Quiet` True) |
| T-46-22 | Repudiation | overwriting the first run's FAIL | mitigate | The re-run is a new file. 46-LIVE-GATE.md is additions-only (a pointer), checked by a diff with no `-` lines |
| T-46-12 | Tampering | stale Electron-era HKCU key | mitigate | P0 `reg query` must show the `%LOCALAPPDATA%` path |
| T-46-15 | Denial of Service | orphan sidecar after force-kill (pre-existing, no job object) | accept | Out of scope. Recorded in Check 5, cleaned up by the operator, and filed as a todo if observed |
</threat_model>

<verification>
- 46-LIVE-GATE-RERUN.md is complete with measured values and a verdict. 46-LIVE-GATE.md is preserved, with an appended pointer.
- Closure artefacts changed if and only if the verdict is PASS. STATE.md is untouched.
</verification>

<success_criteria>
REQ-46-10 is re-recorded with evidence against the fixed build, including an objective minimized-window restore measurement from both launch paths. REQ-46-11's closure obligations are fulfilled on PASS, or explicitly withheld on FAIL.
</success_criteria>

<output>
Create `.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-07-SUMMARY.md` when done. State the verdict, each check's observed values (including the 3a/3b iconic/foreground values and the grant line), and which closure actions ran.
</output>
