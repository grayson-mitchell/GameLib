---
phase: 46-windows-single-instance-guard-and-gamelib-deep-link-registra
plan: 04
type: execute
wave: 4
depends_on: ["46-03"]
files_modified:
  - src-tauri/tauri.windows.conf.json
  - src/backend/__tests__/windowsDeepLinkSuppression.test.ts
  - src/backend/__tests__/tauriShellSource.test.ts
  - src-tauri/src/main.rs
autonomous: true
requirements: [REQ-46-05, REQ-46-06, REQ-46-11]

must_haves:
  truths:
    - "The Windows platform overlay no longer suppresses gamelib://. The merged Windows config declares schemes ['gamelib'], the same as macOS and Linux"
    - "The override removal and the suppression-test inversion land in ONE commit, and that commit comes after the guard commits of 46-02/46-03"
    - "The generated installer.nsi for this commit contains the 6 Classes\\gamelib lines (WriteRegStr x4, ReadRegStr x1, DeleteRegKey x1)"
    - "Runtime register_all() stays Linux-only on purpose (decision point a). The code comment and a REQ-46-06-named test say so"
    - "No comment in main.rs or the two test headers still claims Windows has no guard or is unregistered"
  artifacts:
    - path: "src-tauri/tauri.windows.conf.json"
      provides: "Windows overlay with only bundle.resources (no plugins key)"
    - path: "src/backend/__tests__/windowsDeepLinkSuppression.test.ts"
      provides: "Inverted Tests A/E, with controls B/C/D unchanged"
      contains: "['gamelib']"
    - path: "src/backend/__tests__/tauriShellSource.test.ts"
      provides: "REQ-46-06 register_all decision pin"
      contains: "REQ-46-06"
  key_links:
    - from: "src-tauri/tauri.windows.conf.json"
      to: "src-tauri/target/debug/nsis/x64/installer.nsi"
      via: "Tauri CLI merges plugins.deep-link.desktop into its bundler deep_link_protocols, and the NSIS template writes Software\\Classes\\gamelib"
      pattern: "Classes\\\\gamelib"
---

<objective>
Un-suppress `gamelib://` on Windows now that the guard exists and is unit- and source-gated. This happens strictly after 46-02/46-03, per the todo's "Then, and only then" and D-05.
- Delete the `plugins.deep-link` override, and invert the suppression tests in the SAME commit (REQ-46-05).
- Record and pin the runtime `register_all()` decision at its research default, installer-only (REQ-46-06, decision point (a)).
- Rewrite the registration comment block and the test headers (REQ-46-11).
- Prove at installer-script level that registration now happens.

Purpose: without this, the live gate's external `gamelib://` open cannot reach GameLib at all.
Output: config and test change; `installer.nsi` evidence; a debug NSIS setup .exe ready for the 46-05 live gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/todos/pending/2026-08-29-windows-single-instance-guard-and-deep-link-registration.md
@.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-RESEARCH.md
@.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-03-SUMMARY.md

<interfaces>
Current src-tauri/tauri.windows.conf.json (full):
{ "$schema": ..., "bundle": { "resources": { "../build/bin/x64/win32/": "build/bin/x64/win32", "../build/bin/arm64/win32/": "build/bin/arm64/win32", "../build/bin/legendary.LICENSE": "build/bin/legendary.LICENSE" } }, "plugins": { "deep-link": { "desktop": { "schemes": [] } } } }

windowsDeepLinkSuppression.test.ts: Test A (~127) asserts own `schemes` === []. Test E (~154) asserts mergedWindows schemes === [] and macOS/Linux === ['gamelib']. Tests B/C/D are controls. Test D's shape (`expect(conf.plugins?.['deep-link']).toBeUndefined()`) is the template for the inverted Test A.

main.rs registration comment: the `// Runtime register() is LINUX-ONLY ...` block directly above `#[cfg(target_os = "linux")]` / `register_all()` in .setup(). Its "Windows is NOT registered at EITHER stage" bullet is the text being retired.

tauriShellSource.test.ts: the Phase 35 plan 07 describe header (~2268-2290) point 3, "Runtime register() is unreachable on Windows (T-35-28 / D-05 / U-34.5-18) ... acquire_single_instance() is #[cfg(unix)]", is now stale. cfgGuardAboveRegisterAll() is the helper to reuse.

Expected installer.nsi lines (quick 260922-nx4 control run, verbatim apart from line numbers):
  WriteRegStr SHCTX "Software\Classes\gamelib" "URL Protocol" ""
  WriteRegStr SHCTX "Software\Classes\gamelib" "" "URL:${BUNDLEID} protocol"
  WriteRegStr SHCTX "Software\Classes\gamelib\DefaultIcon" "" "$\"$INSTDIR\${MAINBINARYNAME}.exe$\",0"
  WriteRegStr SHCTX "Software\Classes\gamelib\shell\open\command" "" "$\"$INSTDIR\${MAINBINARYNAME}.exe$\" $\"%1$\""
  ReadRegStr $R7 SHCTX "Software\Classes\gamelib\shell\open\command" ""
  DeleteRegKey SHCTX "Software\Classes\gamelib"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ONE commit that deletes the Windows override, inverts Tests A/E, pins REQ-46-06, and rewrites the stale comments</name>
  <files>src-tauri/tauri.windows.conf.json, src/backend/__tests__/windowsDeepLinkSuppression.test.ts, src/backend/__tests__/tauriShellSource.test.ts, src-tauri/src/main.rs</files>
  <read_first>
    - src-tauri/tauri.windows.conf.json (whole file)
    - src/backend/__tests__/windowsDeepLinkSuppression.test.ts (whole file, 173 lines: header, mergePatch, Tests A-E)
    - src/backend/__tests__/tauriShellSource.test.ts (~2268-2400: the Phase 35 plan 07 describe header and the register_all tests)
    - src-tauri/src/main.rs (Grep `Runtime \`register()\` is LINUX-ONLY`, then Read from there through the `#[cfg(target_os = "linux")]` block's closing brace. Grep `Platform reality (the plugin's own README)` and Read that comment)
    - src/backend/__tests__/packagingConfig.test.ts (Grep `windows`: confirm nothing there asserts on the `plugins` key of the Windows overlay)
    - .planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-RESEARCH.md (sections "Q6" and "Q7")
  </read_first>
  <action>
    Preconditions, checked first: `git log --oneline` shows the 46-02 and 46-03 feat commits, and `pnpm exec jest src/backend/__tests__/tauriShellSource.test.ts -t "Phase 46"` passes. If either is false, STOP. The override must not be removed before the guard exists (D-05, T-46-11).

    1. **tauri.windows.conf.json.** Delete the entire `"plugins"` key, so the file holds only `$schema` and `bundle`. Leave `bundle.resources` byte-identical. The file becomes the same shape as the macOS/Linux overlays with respect to `plugins`: absent. Deletion is chosen over `["gamelib"]` because an absent key lets the base config stay the single source of truth.

    2. **windowsDeepLinkSuppression.test.ts.**
       - Rename Test A to `Test A (inverted, phase 46): tauri.windows.conf.json declares NO plugins["deep-link"] key -- Windows inherits the base schemes now that a single-instance guard exists`, asserting `expect(windowsConf.plugins?.['deep-link']).toBeUndefined()`.
       - Invert Test E's Windows line to `.toEqual(['gamelib'])` and rename it to say all three platforms merge to `["gamelib"]`.
       - Keep Tests B, C and D unchanged.
       - Rename the describe to `Windows gamelib:// deep-link registration (quick-260922-nx4 suppression, lifted by phase 46)`.
       - Rewrite the file header. It keeps the nx4 history (why the override existed, and the NSIS mechanism), then states that phase 46 shipped the Windows guard (mutex + named pipe, `src-tauri/src/main.rs`). The override was deleted in the same commit as this inversion. Test A now guards against the suppression being re-added silently, which would quietly kill Windows deep links, and Test E pins parity. It also points at REQ-46-05.

    3. **tauriShellSource.test.ts.**
       - In the Phase 35 plan 07 describe header, rewrite point 3. Runtime `register_all()` stays Linux-only, and Windows now registers at INSTALL time via NSIS. The reason is no longer the missing guard. It is phase 46 decision point (a), REQ-46-06: runtime registration exists for installs that bypass an installer (AppImage), and GameLib ships Windows only as NSIS.
       - Update the comment inside `self-test (RED proof): a register_all() gated on WINDOWS` if it cites the missing guard.
       - Add a test named `REQ-46-06 (decision point a, operator-overridable): Windows relies on the NSIS installer alone -- register_all() stays #[cfg(target_os = "linux")]`, asserting `cfgGuardAboveRegisterAll()` equals `'#[cfg(target_os = "linux")]'`, with a RED self-test using an `any(target_os = "linux", windows)` gate. Place a comment above it with the override procedure: widen the cfg to `#[cfg(any(target_os = "linux", windows))]`, keep the `CI=e2e` guard, and flip this expected string.
       - Do NOT change the existing T-35-28 tests' assertions.

    4. **main.rs, comments only.**
       - Rewrite the "Windows is NOT registered at EITHER stage" bullet in the `Runtime register() is LINUX-ONLY` block. Windows now registers at INSTALL time. The NSIS template writes HKCU `Software\Classes\gamelib` under the default `currentUser` install mode, and re-installing overwrites the stale Electron-era key. The guard in this file (phase 46: mutex + named pipe) hands every external open to the running instance. Runtime `register_all()` stays Linux-only by decision (REQ-46-06, decision point (a), operator-overridable; RESEARCH Q7).
       - Replace the side-effect sentence about `handle_cli_arguments` matching no scheme with RESEARCH Q6's result. Now that Windows schemes are non-empty, the plugin's own cold-start argv emit happens during plugin init, BEFORE this `.setup()` registers `on_open_url`, so it has no listener and is dropped. Argv delivery stays exclusively `sidecar_forward_args` -> `protocol_url_arg`. This is pinned by the REQ-46-08 ordering gate and holds for tauri 2.11.5 / tauri-plugin-deep-link 2.4.9; re-verify on upgrade.
       - Drop the "Lifting this gap means (1)(2)(3)" text, since it is done. Keep the pointer to ledger U-34.5-18, noting that it closes with the phase 46 live gate.
       - In the `Platform reality` comment, make sure "On Linux and Windows the OS spawns a NEW process" now names the Windows pipe path as the warm route.
       - Comments only: `git diff` of main.rs must touch no code line.

    Run `pnpm exec prettier --write` on both test files, and eslint on both. Commit all four files in ONE commit, `feat(46-04): register gamelib:// on Windows -- drop the nx4 schemes override now that the single-instance guard exists`, without `--no-verify`.
  </action>
  <verify>
    <automated>pnpm exec jest src/backend/__tests__/windowsDeepLinkSuppression.test.ts src/backend/__tests__/tauriShellSource.test.ts src/backend/__tests__/packagingConfig.test.ts && cd src-tauri && cargo check --bin gamelib-shell</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const c=require('./src-tauri/tauri.windows.conf.json'); if (c.plugins!==undefined) process.exit(1); console.log(JSON.stringify(c.bundle.resources))"` exits 0 and prints the three unchanged resource mappings
    - `pnpm exec jest src/backend/__tests__/windowsDeepLinkSuppression.test.ts` passes 5/5, and `grep -c "toEqual(\[\])" src/backend/__tests__/windowsDeepLinkSuppression.test.ts` prints 0
    - `pnpm exec jest src/backend/__tests__/tauriShellSource.test.ts -t "REQ-46-06"` runs at least 2 tests (pin + RED), all passing, and the whole file passes
    - `pnpm exec jest src/backend/__tests__/packagingConfig.test.ts` passes
    - `git show --stat HEAD` lists exactly these 4 files: tauri.windows.conf.json, windowsDeepLinkSuppression.test.ts, tauriShellSource.test.ts, main.rs. The override removal and the test inversion are in the SAME commit
    - `git show HEAD -- src-tauri/src/main.rs | grep '^[+-]' | grep -v '^[+-][+-]' | grep -v '^[+-] *//' | grep -v '^[+-] *$'` prints nothing (comments-only change)
    - `grep -n "Windows is NOT registered at EITHER stage\|so Windows has no guard" src-tauri/src/main.rs src/backend/__tests__/*.ts` prints nothing
    - `git log --format=%s -8` shows the 46-02 and 46-03 feat commits BELOW (older than) this commit
  </acceptance_criteria>
  <done>The Windows overlay no longer suppresses the scheme, and the inverted tests pin the new state. The register_all decision is recorded and pinned. No stale "no guard / unregistered" claim remains.</done>
</task>

<task type="auto">
  <name>Task 2: Build the packaged debug NSIS bundle and prove installer.nsi registers gamelib:// (installer NOT run)</name>
  <files>(no tracked files: build artefacts under src-tauri/target and build/ are gitignored; evidence goes to 46-04-SUMMARY.md)</files>
  <read_first>
    - .planning/todos/pending/2026-08-29-windows-single-instance-guard-and-deep-link-registration.md (section "Addendum 2026-09-22": the proven method, the expected exit-1-at-signing, and the 6-line control table)
    - .planning/todos/pending/2026-09-22-windows-packaged-build-breaks-on-darwin-runner-symlinks.md (Layers 0-3 and the local repairs)
    - package.json (scripts `tauri:dev:packaged`, `build:sidecar`, `build:decompress-worker-dev`, `tauri`)
  </read_first>
  <action>
    Run the packaged-build prep chain from the repo root, as in `tauri:dev:packaged` but NSIS-only: `pnpm exec vite build && pnpm build:sidecar && pnpm build:decompress-worker-dev && pnpm tauri build --debug --bundles nsis`. Run it with `run_in_background` and poll for completion, because a cold debug build can exceed the 10-minute foreground limit.

    Known local conditions (darwin-symlink todo; do NOT fix them in this plan):
    - If `vite build` fails with the preserve-runner-symlinks `skipped (destination parent missing)` message or the assembleRendererDist `bundleKeys is empty` error, apply that todo's documented local repair once: delete the generated `build/bin/arm64/darwin` directory, then re-run the chain.
    - If it still fails, STOP and report the first error (the todo explains that vite masks it).

    Exit 1 at the updater-artifact signing step (`TAURI_SIGNING_PRIVATE_KEY` absent) is EXPECTED and happens after `installer.nsi` and the setup .exe are written. Any other non-zero exit is a failure.

    Then:
    - `grep -n 'Classes\\gamelib' src-tauri/target/debug/nsis/x64/installer.nsi` must print exactly 6 lines matching the six `<interfaces>` lines.
    - Also run `grep -c 'URL Protocol' src-tauri/target/debug/nsis/x64/installer.nsi` and record the count.
    - Record the setup .exe path (expected: `src-tauri/target/debug/bundle/nsis/GameLib_<version>_x64-setup.exe`), its size, its mtime, and `git rev-parse HEAD` at build time in the SUMMARY. Plan 46-05's operator installs exactly this file.
    - Do NOT run the installer. Do NOT touch the registry.
  </action>
  <verify>
    <automated>test "$(grep -c 'Classes\\gamelib' src-tauri/target/debug/nsis/x64/installer.nsi)" = "6" && ls src-tauri/target/debug/bundle/nsis/*-setup.exe</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Classes\\gamelib' src-tauri/target/debug/nsis/x64/installer.nsi` prints 6 (it was 0 in the nx4 override run)
    - The `shell\open\command` WriteRegStr line contains the quoted `$\"%1$\"` argument form (RESEARCH Q6/Pitfall 3)
    - `src-tauri/target/debug/bundle/nsis/` contains a `*-setup.exe` whose mtime is later than this plan's Task 1 commit time
    - The SUMMARY records: the exact command chain, the exit code and the step it exited at, whether the darwin local repair was needed, the 6 grepped lines verbatim, the `URL Protocol` count, the setup .exe path/size/mtime, and the HEAD sha
    - `git status --porcelain` shows no tracked-file modifications from the build
  </acceptance_criteria>
  <done>Installer-level proof that the Windows NSIS installer now registers gamelib://, and a built setup .exe identified by HEAD sha, ready for the live gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS protocol handler → GameLib.exe argv | Any web page or app can open a `gamelib://` URL, and after install it launches our exe with the URL as `%1` |
| installer → HKCU registry | Install-time write of `Software\Classes\gamelib` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-11 | Denial of Service / Tampering | registering the handler before a guard exists (D-05: every open would start a second app + sidecar) | mitigate | Task 1 precondition: the 46-02/46-03 commits exist and the Phase 46 jest block passes, else STOP. The commit-order acceptance check. The live gate (46-05) must pass before the todo and ledger close |
| T-34.5-G6-20 | Tampering | untrusted URL arriving via argv/pipe | mitigate | Unchanged: argv goes through sidecar_forward_args → protocol_url_arg, and warm delivery goes through the pipe → protocol_url_arg (46-03) |
| T-46-09 | Tampering | cold-start double dispatch once schemes are non-empty | mitigate | RESEARCH Q6 timing proof, the REQ-46-08 ordering gate (46-03), and the main.rs comment carrying the version pin and a re-verify-on-upgrade note |
| T-46-12 | Tampering | stale Electron-era HKCU key pointing at `C:\Program Files\GameLib\GameLib.exe` | accept | NSIS `WriteRegStr` overwrites it unconditionally on install (RESEARCH Q7). The 46-05 precondition verifies the registry value after install |
| T-46-13 | Tampering | argv splitting of URLs containing `&` | accept | The NSIS command is quoted `"%1"` (acceptance-checked in installer.nsi). The live gate exercises a `&` URL |
| T-46-SC | Tampering | build-time supply chain | accept | No new packages. The build uses the existing lockfiles |
</threat_model>

<verification>
- Jest windowsDeepLinkSuppression (5/5 inverted), tauriShellSource (incl. REQ-46-06) and packagingConfig are green.
- The single commit contains the override removal and the test inversion.
- installer.nsi has 6 `Classes\gamelib` lines, and a setup .exe exists, tied to the HEAD sha.
</verification>

<success_criteria>
REQ-46-05 and REQ-46-06 are satisfied. REQ-46-11's comment rewrites are complete. The live-gate precondition artefact exists.
</success_criteria>

<output>
Create `.planning/phases/46-windows-single-instance-guard-and-gamelib-deep-link-registra/46-04-SUMMARY.md` when done, including the installer.nsi evidence table (this build vs the nx4 override run: 0 → 6) and the setup .exe identity.
</output>
