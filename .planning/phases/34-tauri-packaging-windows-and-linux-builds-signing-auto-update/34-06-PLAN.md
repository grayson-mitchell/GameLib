---
phase: 34-tauri-packaging-windows-and-linux-builds-signing-auto-update
plan: 06
type: execute
wave: 4
depends_on: [34-01, 34-02, 34-05]
files_modified:
  - .github/workflows/release-tauri.yml
autonomous: true
requirements: [REQ-34-04, REQ-34-06]
must_haves:
  truths:
    - "A v* tag push (or workflow_dispatch) builds Windows + Linux + macOS (arm64 & x64) via tauri-action"
    - "Each leg builds the SEA sidecar before tauri-action bundles it"
    - "The release is created as a DRAFT marked PRERELEASE with per-platform artifacts + latest.json"
    - "Missing signing secrets gracefully skip signing and NEVER fail the job (macOS native skip; Windows conditional override)"
    - "The workflow never references Heroic-Games-Launcher"
  artifacts:
    - path: ".github/workflows/release-tauri.yml"
      provides: "3-OS matrix tauri-action release pipeline with graceful-skip signing + draft-prerelease"
      contains: "tauri-apps/tauri-action"
  key_links:
    - from: ".github/workflows/release-tauri.yml"
      to: "pnpm build:sidecar-sea"
      via: "pre-bundle step per matrix leg"
      pattern: "build:sidecar-sea"
    - from: ".github/workflows/release-tauri.yml"
      to: "TAURI_SIGNING_PRIVATE_KEY secret"
      via: "env block feeding tauri-action for latest.json signing"
      pattern: "TAURI_SIGNING_PRIVATE_KEY"
---

<objective>
Create the `.github/workflows/release-tauri.yml` release pipeline (D-05/D-09): a
`windows-latest` + `ubuntu-24.04` + `macos-latest` (arm64 & x64) matrix that builds the SEA
sidecar, then runs `tauri-apps/tauri-action` to bundle, sign conditionally (graceful-skip when
secrets are absent — D-04), generate a minisign-signed `latest.json`, and create a DRAFT
GitHub Release marked PRERELEASE on a `v*` tag push. Greens the `releaseWorkflow` Wave-0 suite.

Purpose: CI is the only way to produce Windows/Linux artifacts (the dev has only a Mac), and
the draft+prerelease flow is the human gate that keeps a 0.x build off "Latest" and invisible
to the updater until manual publish.
Output: A single new workflow file. Live behavior is proven by the 34-07 Manual-Only gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-RESEARCH.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-PATTERNS.md

<interfaces>
<!-- 34-RESEARCH.md section "Full reference CI workflow" is the canonical source (pnpm/tsc-adapted). -->
<!-- Pattern 1 = macOS native env-var signing skip; Pattern 2 = Windows CI-conditional --config -->
<!-- override behind `if: env.WINDOWS_CERTIFICATE != ''` (Windows has NO native skip — Pitfall 4). -->
<!-- Reuse ./.github/actions/install-deps composite (34-PATTERNS.md). Pitfall 6: NO existing -->
<!-- draft-release-win.yml to model — build the Windows leg from RESEARCH directly. Pitfall 7: -->
<!-- co-triggering with draft-release-mac/linux.yml on v* is ACCEPTED — verify no artifact-name -->
<!-- collision (tauri default names vs electron-builder's -Setup-/-Portable-/-macOS-/-linux-). -->
<!-- Secrets: TAURI_SIGNING_PRIVATE_KEY(+_PASSWORD) from 34-03; APPLE_*/WINDOWS_* are a NEW set, -->
<!-- distinct from Electron's CSC_LINK/CSC_KEY_PASSWORD/TEAMID names. -->

Wave-0 gate: src/backend/__tests__/releaseWorkflow.test.ts (text assertions on this YAML).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author release-tauri.yml (matrix + SEA + graceful-skip signing + draft-prerelease)</name>
  <files>.github/workflows/release-tauri.yml</files>
  <read_first>
    - .planning/phases/34-.../34-RESEARCH.md section "Full reference CI workflow" (canonical YAML) + Pattern 1 (macOS skip) + Pattern 2 (Windows conditional) + Pitfalls 4/6/7
    - .planning/phases/34-.../34-PATTERNS.md section .github/workflows/release-tauri.yml (repo conventions: checkout@v6 + install-deps composite; env-block secrets; new Tauri secret names)
    - src/backend/__tests__/releaseWorkflow.test.ts (the gate this task greens)
    - .github/workflows/draft-release-linux.yml + draft-release-mac.yml (trigger shape + install-deps usage — do NOT copy electron-builder specifics)
  </read_first>
  <action>
    Create `.github/workflows/release-tauri.yml` from 34-RESEARCH.md's canonical workflow. Trigger `on: push: tags: ['v*']` plus `workflow_dispatch`. `permissions: contents: write`. Matrix (fail-fast:false): `macos-latest`/`--target aarch64-apple-darwin`, `macos-latest`/`--target x86_64-apple-darwin`, `ubuntu-24.04`, `windows-latest`. Steps per leg: `actions/checkout@v6`; Ubuntu system deps (`libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf` etc) gated to the ubuntu leg; `./.github/actions/install-deps`; `dtolnay/rust-toolchain@stable` (mac leg adds both darwin targets); `swatinem/rust-cache@v2` (`workspaces: './src-tauri -> target'`); `pnpm build:sidecar-sea` BEFORE tauri-action so the SEA binary exists as externalBin; a Windows-only conditional cert-import step gated `if: matrix.platform == 'windows-latest' && env.WINDOWS_CERTIFICATE != ''` (Pattern 2 — imports a base64 `.pfx` in-memory, never persisted); then `tauri-apps/tauri-action@v1` with env `GITHUB_TOKEN`, `TAURI_SIGNING_PRIVATE_KEY`, `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`, and the `APPLE_*` set, and `with: tagName: v__VERSION__`, `releaseName`, `releaseDraft: true`, `prerelease: true`, `args: ${{ matrix.args }}` plus a Windows-only `--config` signing override merged ONLY when `env.WINDOWS_CERTIFICATE != ''` (so a secrets-less run passes no signing config and ships unsigned — D-04, never fails). Reference Heroic nowhere. Add a top-of-file comment noting co-triggering with the Electron `draft-release-*` workflows on `v*` is intentional (Pitfall 7) and that Tauri vs electron-builder artifact names must not collide.
  </action>
  <acceptance_criteria>
    - `pnpm test -- --testPathPattern=releaseWorkflow` PASSES (v* trigger, workflow_dispatch, three runners, tauri-action, `releaseDraft: true`, `prerelease: true`, the `env.WINDOWS_CERTIFICATE != ''` guard, install-deps composite, no Heroic).
    - The macOS legs pass NO signing config when `APPLE_CERTIFICATE` is unset (native skip); the Windows leg passes NO `--config` signing override when `WINDOWS_CERTIFICATE` is unset — the true default is "unsigned, job green".
    - `pnpm build:sidecar-sea` runs before `tauri-action` in every leg.
    - The file is valid YAML (parses without error).
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --testPathPattern=releaseWorkflow 2>&1 | grep -Eq "PASS|passed" && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-tauri.yml')); print('YAML_OK')"</automated>
  </verify>
  <done>release-tauri.yml exists, is valid YAML, greens the releaseWorkflow suite, and encodes graceful-skip signing + draft-prerelease.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions secrets → CI job | Signing certs + minisign private key are injected into the runner at build time |
| CI runner → GitHub Release | The pipeline publishes artifacts a human later ships to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Denial of Service | secrets-less CI run | mitigate | Windows signing override gated behind `env.WINDOWS_CERTIFICATE != ''`; base config has no `certificateThumbprint`; macOS skips natively — a run with no certs ships unsigned and the job stays green (D-04) |
| T-34-05 | Information Disclosure | minisign private key + Apple/Windows certs in CI | mitigate | All read via `env:` blocks from GitHub secrets (auto-redacted in logs), never inlined into `run:` strings; the Windows `.pfx` is base64-decoded in-memory only, never written to a cached/persisted artifact |
| T-34-07 | Tampering | sidecar substitution between build + bundle | mitigate | `build:sidecar-sea` runs in the SAME leg that bundles — no cross-job artifact hand-off window |
| T-34-11 | Spoofing | artifact-name collision on the shared release | mitigate | Confirm Tauri default artifact names do not collide with electron-builder's `-Setup-/-Portable-/-macOS-/-linux-` names on the co-triggered `v*` release (Pitfall 7) |
| T-34-SC | Tampering | GitHub Actions third-party actions | accept | Pin `tauri-action@v1`, `dtolnay/rust-toolchain@stable`, `swatinem/rust-cache@v2`, `actions/checkout@v6` — official/established actions per RESEARCH |
</threat_model>

<verification>
- `pnpm test -- --testPathPattern=releaseWorkflow` — green.
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-tauri.yml'))"` — parses.
- Static review: every signing path is conditional; no Heroic reference; SEA step precedes tauri-action.
- Live matrix behavior is deferred to the 34-07 Manual-Only gate.
</verification>

<success_criteria>
A single `release-tauri.yml` builds all three platforms on a `v*` tag via tauri-action, compiles the SEA sidecar per leg, signs gracefully-or-skips without ever failing on missing certs, and produces a draft+prerelease release with per-platform artifacts + a minisign-signed `latest.json`.
</success_criteria>

<output>
Create `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-06-SUMMARY.md` when done.
</output>
