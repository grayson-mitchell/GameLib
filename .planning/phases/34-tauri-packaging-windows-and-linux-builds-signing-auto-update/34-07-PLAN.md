---
phase: 34-tauri-packaging-windows-and-linux-builds-signing-auto-update
plan: 07
type: execute
wave: 5
depends_on: [34-06]
files_modified: []
autonomous: false
requirements: [REQ-34-04, REQ-34-09]
must_haves:
  truths:
    - "A real v* test-tag push completes all three matrix legs (Windows, Linux, macOS arm64 & x64)"
    - "A Draft GitHub Release marked PRERELEASE appears with per-platform artifacts + latest.json"
    - "The release is NOT marked GitHub Latest and is invisible to the updater until manual publish"
    - "With no signing secrets set, artifacts are unsigned, an explicit `::warning::Signing skipped` line is logged per OS leg (D-04), and NO job fails"
    - "The compiled sidecar runs standalone in a Node-free environment"
  artifacts:
    - path: ".planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-07-SUMMARY.md"
      provides: "Recorded live-gate evidence (run URL, release URL, artifact list, standalone-sidecar result)"
      contains: "draft"
  key_links:
    - from: "v* test tag"
      to: "draft+prerelease GitHub Release on grayson-mitchell/GameLib"
      via: "release-tauri.yml matrix run"
      pattern: "prerelease"
---

<objective>
The Manual-Only phase-close gate (REQ-34-09). This phase is CI-native: jest cannot exercise a
GitHub Actions run, a live GitHub Release's draft/prerelease/Latest state, or a Node-free
container. A real `v*` test-tag push is the only way to prove the pipeline actually ships all
three platforms, gracefully skips signing without failing (D-04), and honors the draft+prerelease
human gate (D-09, Phase 19 prerelease-not-Latest lesson). No repo files change here.

Purpose: Close the gap between "workflow YAML asserts the right shape" and "the pipeline
actually produces installable, auto-updatable, unsigned-but-working artifacts."
Output: Recorded live-gate evidence in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-VALIDATION.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live tag-push release-pipeline gate</name>
  <files>(no repo files — live CI + GitHub Release verification)</files>
  <action>The human pushes a real v* test tag to the fork and verifies the live Actions run + draft-prerelease Release + Node-free sidecar per the steps below. Jest cannot exercise a GitHub Actions run, a live Release state, or a Node-free container — this is inherently manual.</action>
  <what-built>The full 3-OS Tauri release pipeline (34-06) plus the SEA sidecar (34-02) and updater config (34-05). This gate exercises it live on the fork repo with NO signing secrets set (the normal 0.x state).</what-built>
  <how-to-verify>
    1. Push a test tag whose NUMERIC version matches the current `tauri.conf.json` version (`0.7.0`, unbumped this phase — the `0.7.0`→`0.8.0` bump is a deferred non-blocking confirm item, CONTEXT `<deferred>`): `git tag v0.7.0-rc.test && git push gamelib v0.7.0-rc.test` (push to the `gamelib` fork remote, never `origin`/Heroic — see STATE git-remote-topology). WHY this numeric must match: 34-06 sets `tagName: v__VERSION__`, so tauri-action creates the Release at `v0.7.0` regardless of the trigger tag; pushing a divergent numeric (e.g. `v0.8.0-rc.test`) would make tauri-action target a `v0.7.0` release tag that diverges from the pushed trigger tag. If you intend to actually ship `0.8.0`, first bump `tauri.conf.json` version and push a matching `v0.8.0*` tag.
    2. In GitHub Actions on grayson-mitchell/GameLib, confirm the "Release Tauri" run: all matrix legs (windows-latest, ubuntu-24.04, macos-latest aarch64, macos-latest x86_64) complete GREEN. Confirm NO leg fails on missing signing certs (D-04) and the macOS + Windows legs each emit the explicit `::warning::Signing skipped` log line (added in 34-06).
    3. Open the created GitHub Release: confirm it is a DRAFT, marked PRERELEASE, is NOT marked "Latest", and carries per-platform artifacts (NSIS `.exe`, AppImage, `.dmg` x64 + arm64) plus `latest.json` and its `.sig`. Confirm the Tauri artifact filenames do NOT collide with any co-triggered electron-builder artifacts (Pitfall 7).
    4. Standalone sidecar smoke: on a machine/container with NO Node on PATH (or `env -i PATH=/usr/bin ./gamelib-sidecar-<triple>`), confirm the compiled sidecar starts and responds on stdio (REQ-34-03/REQ-34-09).
    5. Confirm the updater sees NOTHING while the release is still a draft (invisible until manual publish — D-09).
    6. Clean up: delete the test tag/draft release after recording evidence.
  </how-to-verify>
  <resume-signal>Type "approved" once all legs are green, the draft+prerelease release has all artifacts + latest.json, signing gracefully skipped without failure, and the sidecar ran Node-free; record the run URL + release URL + artifact list in the SUMMARY. Or describe which check failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → published GitHub Release | This gate confirms the human-publish gate genuinely stands between CI output and users |
| updater client → draft release | Confirms a draft is invisible to the updater (no premature auto-update) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Denial of Service | secrets-less CI run | verify | Live gate confirms every leg is green + unsigned artifacts produced with no cert failure (D-04) |
| T-34-12 | Elevation of Privilege / premature push | draft/prerelease/Latest state | verify | Live gate confirms Draft + prerelease + not-Latest + updater-invisible until manual publish (D-09; Phase 19 lesson) |
| T-34-13 | Tampering | unsigned artifact supply-chain exposure | accept | Unsigned 0.x is the documented, accepted state (D-03); minisign signature on `latest.json` remains the auto-update trust anchor even while OS signing is deferred |
</threat_model>

<verification>
- Live Actions run: 4 matrix legs green, no cert-failure.
- Live Release: Draft + prerelease + not-Latest, all platform artifacts + `latest.json`(+`.sig`), no filename collision.
- Standalone sidecar: runs Node-free.
- Evidence (URLs, artifact list) recorded in 34-07-SUMMARY.md; test tag/release cleaned up.
</verification>

<success_criteria>
The release pipeline is proven end-to-end on the fork: all three platforms build unsigned-but-working artifacts without failing on missing certs, the draft+prerelease human gate holds, and the compiled sidecar runs with no system Node — closing REQ-34-04 and REQ-34-09.
</success_criteria>

<output>
Create `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-07-SUMMARY.md` when done — record the Actions run URL, the Release URL, the artifact list, and the standalone-sidecar result.
</output>
