---
phase: 34-tauri-packaging-windows-and-linux-builds-signing-auto-update
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - src-tauri/Cargo.toml
  - package.json
  - meta/buildSidecarSea.ts
  - src-tauri/binaries/.gitignore
autonomous: true
requirements: [REQ-34-03, REQ-34-07]
must_haves:
  truths:
    - "The Tauri shell compiles on macOS with keyring windows-native + sync-secret-service added (additive-invariant preserved)"
    - "tauri-plugin-updater and tauri-plugin-shell are available as Rust dependencies"
    - "The JS updater/shell plugins and postject are installed"
    - "`pnpm build:sidecar-sea` produces a self-contained host-triple sidecar binary that runs without a system Node"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "keyring three-platform features + updater/shell crates"
      contains: "sync-secret-service"
    - path: "meta/buildSidecarSea.ts"
      provides: "Node SEA legacy 2-step per-OS compile script with exported pure argv-builders"
      contains: "NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2"
    - path: "package.json"
      provides: "build:sidecar-sea script + JS plugin/postject deps"
      contains: "build:sidecar-sea"
  key_links:
    - from: "meta/buildSidecarSea.ts"
      to: "src-tauri/binaries/gamelib-sidecar-<triple>"
      via: "postject inject into copied node binary"
      pattern: "gamelib-sidecar"
    - from: "package.json build:sidecar"
      to: "build/main/sidecar.js"
      via: "esbuild bundle consumed by the SEA step"
      pattern: "build:sidecar-sea"
---

<objective>
Lay the packaging foundation with no `src-tauri` shell (tauri.conf.json/main.rs/capabilities)
edits: (1) add the keyring Windows+Linux features and the updater/shell Rust crates to
`Cargo.toml`; (2) install the JS updater/shell plugins + `postject`; (3) author
`meta/buildSidecarSea.ts` and a `build:sidecar-sea` script that compiles the Phase 27 Node
sidecar into a self-contained per-OS binary via Node SEA (legacy 2-step). Turns the
`cargoFeatures` and `buildSidecarSea` Wave-0 suites green.

Purpose: The dependencies and the compiled sidecar binary are prerequisites the 34-05 shell
config (externalBin, plugin init) and the 34-06 CI workflow both consume.
Output: Updated manifests + a working SEA build script + a host-triple sidecar binary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-RESEARCH.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-PATTERNS.md

<interfaces>
<!-- Package Legitimacy Audit (34-RESEARCH.md): all 5 packages slopcheck [OK]/Approved — -->
<!-- @tauri-apps/plugin-updater 2.10.1, @tauri-apps/plugin-shell 2.3.5, postject, -->
<!-- tauri-plugin-updater 2.10.1, tauri-plugin-shell 2.3.5. No [ASSUMED]/[SUS]/[SLOP] → -->
<!-- no legitimacy checkpoint required. RE-RUN the installs for real (research reverted its own). -->

Existing sidecar bundle step (extend, do NOT replace):
  package.json "build:sidecar": esbuild ... --outfile=build/main/sidecar.js src/sidecar/index.ts

SEA build-script analog (argv-form spawn, exported pure argv-builders, fail-loud compile gate,
JEST_WORKER_ID entry guard): meta/buildSteamBridgeShims.ts
SEA reference commands: 34-RESEARCH.md Pattern 3 (sea-config.json, --experimental-sea-config,
copy node, macOS codesign --remove-signature/--sign -, postject with sentinel fuse).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cargo.toml crates + keyring features; JS deps + install</name>
  <files>src-tauri/Cargo.toml, package.json</files>
  <read_first>
    - src-tauri/Cargo.toml (current: keyring features = ["apple-native"]; tauri plugins listed)
    - .planning/phases/34-.../34-PATTERNS.md § Cargo.toml target edit (additive, no major bump; Cargo.lock resolves keyring 3.6.3)
    - .planning/phases/34-.../34-RESEARCH.md § Standard Stack Installation block + Pitfall 5 (keyring feature gap) + Package Legitimacy Audit
    - src/backend/__tests__/cargoFeatures.test.ts (the gate this task greens)
  </read_first>
  <action>
    Edit `src-tauri/Cargo.toml`: change the keyring line to `keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"] }` (additive — do NOT bump to keyring 4.x, REQ-34-07). Add `tauri-plugin-updater = "2"` and `tauri-plugin-shell = "2"` to `[dependencies]`. Install the JS side with `pnpm add @tauri-apps/plugin-updater@2.10.1 @tauri-apps/plugin-shell@2.3.5` and `pnpm add -D postject`, and run `cargo fetch` (or let `cargo build` resolve) from `src-tauri/`. Confirm the additive/reversible invariant: `cargo build` for the shell still succeeds on macOS with `sync-secret-service` present (keyring gates the Linux secret-service dep behind `cfg(target_os="linux")`, so macOS should not pull libdbus — if the mac build breaks, STOP and record the exact error rather than silently dropping the feature). Document the Linux Secret Service assumption (GNOME/KDE target; runtime absence surfaces the existing `keyring:unavailable` structured error, per 34-RESEARCH.md Pitfall 5 / A3).
  </action>
  <acceptance_criteria>
    - `Cargo.toml` keyring line contains all three features; `tauri-plugin-updater` and `tauri-plugin-shell` present.
    - `package.json` dependencies include `@tauri-apps/plugin-updater` and `@tauri-apps/plugin-shell`; devDependencies include `postject`.
    - `pnpm test -- --testPathPattern=cargoFeatures` PASSES.
    - `cargo build` (from `src-tauri/`) succeeds on the local macOS host — additive invariant intact.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --testPathPattern=cargoFeatures 2>&1 | grep -Eq "PASS|passed" && (cd src-tauri && cargo build 2>&1 | tail -1)</automated>
  </verify>
  <done>Manifests updated + installed; cargoFeatures suite green; mac cargo build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: meta/buildSidecarSea.ts + build:sidecar-sea script</name>
  <files>meta/buildSidecarSea.ts, package.json, src-tauri/binaries/.gitignore</files>
  <read_first>
    - meta/buildSteamBridgeShims.ts (spawnArgv, exported buildXArgv, compile-gate fail-loud, JEST_WORKER_ID guard — copy these conventions)
    - meta/__tests__/buildSidecarSea.test.ts (the gate this task greens — exports expected: buildSeaConfigPath, buildPostjectArgv, sidecarOutputPath)
    - .planning/phases/34-.../34-RESEARCH.md Pattern 3 (exact SEA commands) + Pitfalls 1-3 (worker_threads fallback, macOS x64 tier, module-loading)
    - .planning/phases/34-.../34-PATTERNS.md § meta/buildSidecarSea.ts
    - package.json "build:sidecar" line (the esbuild bundle this SEA step consumes; runs AFTER it)
  </read_first>
  <action>
    Create `meta/buildSidecarSea.ts` following `buildSteamBridgeShims.ts`: argv-form spawn only (never shell strings); export pure functions `buildSeaConfigPath()`, `sidecarOutputPath(triple)` (`.exe` suffix only for `*-windows-*` triples), and `buildPostjectArgv(binaryPath, blobPath)` (sentinel fuse EXACT `NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2`; `--macho-segment-name NODE_SEA` only on darwin). `main()` writes `sea-config.json` (`main: build/main/sidecar.js`, `output`, `disableExperimentalSEAWarning: true`), runs `node --experimental-sea-config`, copies `process.execPath` into `src-tauri/binaries/gamelib-sidecar-<host-triple>[.exe]`, on macOS runs `codesign --remove-signature` before and `codesign --sign -` after `postject` injection. Apply the fail-loud compile gate (non-zero exit OR missing output file both throw) — this is orthogonal to D-04's signing graceful-skip (which applies to OS code signing only, never to the sidecar compile). Copy the `JEST_WORKER_ID` entry guard verbatim. Add a one-time `console.warn` note that the `decompressPool` worker_threads path falls back to inline single-thread decode under a compiled binary (accepted throughput regression, Pitfall 1). Add `"build:sidecar-sea": "pnpm build:sidecar && esbuild --bundle --platform=node --target=node22 meta/buildSidecarSea.ts | node"` to `package.json` scripts (mirrors the `build-steam-bridge` pipe-to-node convention; runs the esbuild bundle first). Create `src-tauri/binaries/.gitignore` ignoring the compiled `gamelib-sidecar-*` binaries (per-OS artifacts are CI-built, not committed) while keeping the directory tracked.
  </action>
  <acceptance_criteria>
    - `meta/buildSidecarSea.ts` exports `buildSeaConfigPath`, `buildPostjectArgv`, `sidecarOutputPath`; contains the exact sentinel fuse string; darwin-only macho-segment + macOS codesign steps; the fail-loud gate; the `JEST_WORKER_ID` guard.
    - `pnpm test -- --testPathPattern=buildSidecarSea` PASSES.
    - `pnpm build:sidecar-sea` runs to completion and produces an executable at `sidecarOutputPath(<host triple>)`.
    - Running that binary with `PATH` scrubbed of Node responds on stdio (standalone smoke — no system Node) OR, if a clean-PATH check is not locally reproducible, the SUMMARY records it as deferred to the 34-07 Manual-Only gate.
    - `src-tauri/binaries/` is tracked but its compiled binaries are git-ignored.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --testPathPattern=buildSidecarSea 2>&1 | grep -Eq "PASS|passed" && pnpm build:sidecar-sea && test -x "$(node -e "process.stdout.write(require('./meta/buildSidecarSea').sidecarOutputPath(process.platform==='win32'?'x86_64-pc-windows-msvc':(process.platform==='darwin'?(process.arch==='arm64'?'aarch64-apple-darwin':'x86_64-apple-darwin'):'x86_64-unknown-linux-gnu')))" 2>/dev/null)"</automated>
  </verify>
  <done>SEA build script authored + green; a host-triple standalone sidecar binary is produced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/crates registries → build | New third-party packages enter the supply chain |
| compiled sidecar binary → bundled app | The SEA binary becomes a shipped, executable resource |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-SC | Tampering | npm/cargo installs (updater/shell plugins, postject) | mitigate | 34-RESEARCH.md Package Legitimacy Audit: all 5 slopcheck `[OK]`/Approved, all official Tauri/Node org packages — no `[ASSUMED]`/`[SUS]`/`[SLOP]`, so no blocking legitimacy checkpoint; pin versions (2.10.1/2.3.5) |
| T-34-07 | Tampering | compiled sidecar substitution | accept/mitigate | SEA binary is produced in the same CI leg that bundles it (34-06) — no cross-job hand-off; fail-loud compile gate ensures a missing/broken binary aborts the build rather than shipping silently |
| T-34-08 | Denial of Service | keyring feature gap on Win/Linux | mitigate | Add `windows-native`+`sync-secret-service` so token persistence works cross-platform; runtime absence returns the structured `keyring:unavailable` error, never a crash (Pitfall 5) |
</threat_model>

<verification>
- `pnpm test -- --testPathPattern="cargoFeatures|buildSidecarSea"` — both green.
- `cd src-tauri && cargo build` — succeeds on macOS (additive invariant).
- `pnpm build:sidecar-sea` — produces a host-triple binary; standalone-no-Node smoke either passes locally or is logged for 34-07.
</verification>

<success_criteria>
Rust + JS packaging dependencies installed; keyring covers all three platforms; the Node sidecar compiles to a self-contained per-OS binary; both foundation Wave-0 suites green; Electron + Tauri dev builds both still work.
</success_criteria>

<output>
Create `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-02-SUMMARY.md` when done.
</output>
