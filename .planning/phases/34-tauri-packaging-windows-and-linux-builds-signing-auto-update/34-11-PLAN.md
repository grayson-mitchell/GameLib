---
phase: 34-tauri-packaging-windows-and-linux-builds-signing-auto-update
plan: 11
type: execute
wave: 7
gap_closure: true
depends_on: [34-08]
files_modified:
  - .github/workflows/release-tauri.yml
  - src/backend/__tests__/releaseWorkflow.test.ts
  - .planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/deferred-items.md
autonomous: true
requirements: [REQ-34-03, REQ-34-04, REQ-34-06]
must_haves:
  truths:
    - "Each matrix leg tells the SEA build script which triple it is building for, so the x86_64 macOS leg produces an x86_64-named, x86_64-arch sidecar"
    - "The Windows signing certificate file is removed from the runner's working directory immediately after import, even if the import fails"
    - "The workflow's own comments describe what the code actually does — no claim of 'in-memory only' handling that the code contradicts"
    - "No artifact-upload or cache step exists in the signing job that could carry cert material off the runner"
    - "The two out-of-scope review findings (WR-04, IN-01) are recorded as tracked debt rather than silently lost"
  artifacts:
    - path: ".github/workflows/release-tauri.yml"
      provides: "Per-leg GAMELIB_SIDECAR_TARGET_TRIPLE wiring + cert.pfx cleanup + corrected comments"
      contains: "GAMELIB_SIDECAR_TARGET_TRIPLE"
    - path: "src/backend/__tests__/releaseWorkflow.test.ts"
      provides: "Regression assertions for the per-leg triple and the cert-file cleanup ordering"
      contains: "sidecar_triple"
    - path: ".planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/deferred-items.md"
      provides: "Tracked-debt entries for WR-04 (null CSP / withGlobalTauri / opener:default) and IN-01 (system.pem match)"
      contains: "WR-04"
  key_links:
    - from: ".github/workflows/release-tauri.yml matrix"
      to: "meta/buildSidecarSea.ts resolveTriple()"
      via: "GAMELIB_SIDECAR_TARGET_TRIPLE env on the 'Build self-contained sidecar (Node SEA)' step"
      pattern: "GAMELIB_SIDECAR_TARGET_TRIPLE"
    - from: "Import-PfxCertificate step"
      to: "cert.pfx removal"
      via: "Remove-Item in a finally block on the same step"
      pattern: "Remove-Item -Path cert\\.pfx"
---

<objective>
Close the CI half of **CR-01** and all of **WR-02**, then record the two explicitly out-of-scope
findings as tracked debt.

**CR-01 (CI half):** 34-08 makes `meta/buildSidecarSea.ts` honor `GAMELIB_SIDECAR_TARGET_TRIPLE`,
but nothing sets it. The `Build self-contained sidecar (Node SEA)` step currently runs
`pnpm build:sidecar-sea` unconditionally with no target input on all four legs, so both
Apple-Silicon `macos-latest` legs would still resolve to the host triple. Each leg must declare its
own triple.

**WR-02:** the Windows cert import step writes the decoded `.pfx` to `cert.pfx` in the checkout
directory and never deletes it, while the step's own comment claims the cert is handled "ONLY
in-memory (never written to a cached/persisted artifact, T-34-05)". The exposure is currently
bounded by the runner VM being destroyed, but the comment is factually wrong and any future cache,
artifact-upload, or debug-session step added to this job would turn it into a real leak.

Per user decision **GAP-D-01**, **WR-04** (`security.csp: null` + `withGlobalTauri` + broad
`opener:default`) and **IN-01** (`sidecarSeaFsShim.ts`'s loose `system.pem` match) are OUT OF SCOPE
and must NOT be implemented here — they are appended to `deferred-items.md` as tracked debt.

Purpose: make the release matrix actually build per-target sidecars, and make the workflow's cert
handling match (and improve on) its own security claim.
Output: an updated `release-tauri.yml`, new regression assertions in the existing
`releaseWorkflow.test.ts` Wave-0 suite, and appended `deferred-items.md` entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-REVIEW.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-06-SUMMARY.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-07-SUMMARY.md

<interfaces>
<!-- Contract established by 34-08 (this plan's dependency). Consume it exactly. -->

meta/buildSidecarSea.ts exports:
  export function resolveTriple(env?: NodeJS.ProcessEnv): string
    // returns env.GAMELIB_SIDECAR_TARGET_TRIPLE when it is a NON-EMPTY string, else hostTriple().
    // An unset matrix field renders as '' in GitHub Actions, which is treated as unset — so a
    // matrix leg with no sidecar_triple value degrades safely to host-triple behavior.

  The four triples the release matrix must supply:
    macos-latest   + '--target aarch64-apple-darwin'  ->  aarch64-apple-darwin
    macos-latest   + '--target x86_64-apple-darwin'   ->  x86_64-apple-darwin
    ubuntu-24.04   + ''                               ->  x86_64-unknown-linux-gnu
    windows-latest + ''                               ->  x86_64-pc-windows-msvc

<!-- Current committed shapes in .github/workflows/release-tauri.yml -->

matrix (L33-42):
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target aarch64-apple-darwin'
          - platform: 'macos-latest'
            args: '--target x86_64-apple-darwin'
          - platform: 'ubuntu-24.04'
            args: ''
          - platform: 'windows-latest'
            args: ''

SEA step (L81-82), currently with NO env: block:
      - name: Build self-contained sidecar (Node SEA)
        run: pnpm build:sidecar-sea

Windows cert import step (L98-108), whose comment carries the inaccurate claim:
      # ... ONLY in-memory (never written to a cached/persisted artifact, T-34-05).
      - name: Import Windows signing certificate (if present)
        if: matrix.platform == 'windows-latest' && env.WINDOWS_CERTIFICATE != ''
        shell: pwsh
        run: |
          $bytes = [Convert]::FromBase64String($env:WINDOWS_CERTIFICATE)
          Set-Content -Path cert.pfx -Value $bytes -AsByteStream
          $securePw = ConvertTo-SecureString -String $env:WINDOWS_CERTIFICATE_PASSWORD -AsPlainText -Force
          Import-PfxCertificate -FilePath cert.pfx -CertStoreLocation Cert:\CurrentUser\My -Password $securePw

src/backend/__tests__/releaseWorkflow.test.ts already provides:
  const RELEASE_WORKFLOW_PATH = join(__dirname, '..', '..', '..', '.github', 'workflows', 'release-tauri.yml')
  function loadReleaseWorkflow(): string        // raw text; assertions are raw-text/regex based
  8 describe blocks, 13 tests, all currently green.
</interfaces>

<toolchain_facts>
- KNOWN CLI GOTCHA (34-06-SUMMARY.md): `pnpm test -- --testPathPattern=X` drops the arg. Use
  `npx jest --testPathPattern=X`.
- KNOWN PRE-EXISTING NOISE: the full backend jest suite exits 1 from a leaked `setTimeout` in
  `src/backend/storeManagers/steam/library.ts:1153` (`deferred-items.md`). Scope runs with
  `--testPathPattern`; never gate on a green full-suite exit code.
- YAML parse check: `python3`/`pyyaml` was unavailable in this environment during 34-06; the
  equivalent used there was
  `node -e "require('js-yaml').load(require('fs').readFileSync(p,'utf-8'))"`.
  Use whichever parser is actually available and say which in the SUMMARY.
- The existing `releaseWorkflow.test.ts` asserts `expect(source).not.toContain('Heroic')` — any new
  comment text added to the workflow must avoid that word.
</toolchain_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare a per-leg sidecar triple and pass it to the SEA build step (CR-01 CI half)</name>
  <files>.github/workflows/release-tauri.yml, src/backend/__tests__/releaseWorkflow.test.ts</files>
  <read_first>
    - `.github/workflows/release-tauri.yml` (whole file — the matrix at L31-43 and the SEA step at L77-82)
    - `meta/buildSidecarSea.ts` (post-34-08 state — confirm the exact env-var name and the empty-string-is-unset semantics of `resolveTriple`)
    - `src/backend/__tests__/releaseWorkflow.test.ts` (whole file — reuse `loadReleaseWorkflow()` and its raw-text assertion style)
    - `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-REVIEW.md` § CR-01
  </read_first>
  <action>
1. Add a `sidecar_triple:` field to each of the four `matrix.include` entries, with the values from
   the `<interfaces>` block above. Use an explicit per-leg literal rather than the review's inline
   nested-ternary GHA expression — a literal matrix field is unambiguous, needs no brace escaping,
   and is directly assertable by the jest suite. This also matches 34-06's established preference
   (see its `build_args` decision) for avoiding fragile inline expressions.

2. Add an `env:` block to the existing `Build self-contained sidecar (Node SEA)` step:
   `GAMELIB_SIDECAR_TARGET_TRIPLE: ${{ matrix.sidecar_triple }}`. Do not change the step's `name`
   or its `run:` command.

3. Extend the step's existing comment to explain WHY: `macos-latest` runners are Apple-Silicon
   native, so without an explicit per-leg triple the SEA script would resolve the host triple on
   both macOS legs and the `--target x86_64-apple-darwin` leg would emit an aarch64-named,
   aarch64-arch sidecar that the bundler cannot resolve (CR-01). Reference CR-01 by name. Do not
   use the word `Heroic` anywhere (an existing test forbids it).

4. Append a describe block to `src/backend/__tests__/releaseWorkflow.test.ts` —
   `release-tauri.yml per-leg sidecar target triple (CR-01 regression guard)` — with:
   - the workflow contains `GAMELIB_SIDECAR_TARGET_TRIPLE: ${{ matrix.sidecar_triple }}`;
   - all four triples appear as `sidecar_triple:` values (`aarch64-apple-darwin`,
     `x86_64-apple-darwin`, `x86_64-unknown-linux-gnu`, `x86_64-pc-windows-msvc`);
   - exactly four `sidecar_triple:` matrix declarations exist (count occurrences of
     `sidecar_triple: '` — catches a leg being added later without a triple, without counting the
     `${{ matrix.sidecar_triple }}` reference);
   - the SEA build step carries the env wiring: assert
     `/Build self-contained sidecar \(Node SEA\)[\s\S]*?GAMELIB_SIDECAR_TARGET_TRIPLE/` so the env
     var cannot drift onto an unrelated step.
   Do not modify any existing test.
  </action>
  <verify>
    <automated>npx jest --testPathPattern=releaseWorkflow</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --testPathPattern=releaseWorkflow` exits 0 with 0 failures (13 pre-existing tests plus the new ones).
    - `grep -c "sidecar_triple: '" .github/workflows/release-tauri.yml` prints `4`.
    - `grep -c 'GAMELIB_SIDECAR_TARGET_TRIPLE' .github/workflows/release-tauri.yml` prints `1`.
    - The workflow still parses as valid YAML (js-yaml or pyyaml; the parse command exits 0).
    - `git diff .github/workflows/release-tauri.yml` shows no change to the `tauri-action` step, the `build_args` step, or the job-level `env:` block.
  </acceptance_criteria>
  <done>Each matrix leg passes its own target triple to the SEA build, so the x86_64 macOS leg builds an x86_64 sidecar.</done>
</task>

<task type="auto">
  <name>Task 2: Delete cert.pfx after import and correct the inaccurate security comment (WR-02)</name>
  <files>.github/workflows/release-tauri.yml, src/backend/__tests__/releaseWorkflow.test.ts</files>
  <read_first>
    - `.github/workflows/release-tauri.yml` lines 94-125 (the two Windows steps: cert import and `build_args`)
    - `src/backend/__tests__/releaseWorkflow.test.ts` (the existing `env.WINDOWS_CERTIFICATE != ''` describe block — extend near it)
    - `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-REVIEW.md` § WR-02
  </read_first>
  <action>
1. Restructure the `Import Windows signing certificate (if present)` step's pwsh script so the
   `.pfx` file cannot outlive the import:
   - keep the existing `FromBase64String` decode, `Set-Content -Path cert.pfx ... -AsByteStream`,
     and `ConvertTo-SecureString` lines;
   - wrap the `Import-PfxCertificate` call in a `try { ... } finally { ... }` where the `finally`
     block runs `Remove-Item -Path cert.pfx -Force -ErrorAction SilentlyContinue`. Using `finally`
     (rather than a trailing statement) means a FAILED import still deletes the cert material —
     the case that would otherwise leave it behind on a job that continues or is retried.

2. Rewrite the comment above that step to describe what the code actually does. State: the cert is
   decoded from a GitHub secret, written to `cert.pfx` in the runner workspace only for the duration
   of `Import-PfxCertificate` (the cmdlet requires a file path — there is no in-memory-only import),
   and removed in a `finally` block immediately afterwards. Keep the `T-34-05` reference. Explicitly
   delete the old "ONLY in-memory (never written to a cached/persisted artifact)" wording — it is
   the false claim the review flagged. Do not use the word `Heroic`.

3. Append a describe block to `src/backend/__tests__/releaseWorkflow.test.ts` —
   `release-tauri.yml Windows cert material cleanup (WR-02 regression guard)`:
   - the workflow contains `Remove-Item -Path cert.pfx -Force`;
   - ORDERING: assert `/Import-PfxCertificate[\s\S]*?Remove-Item -Path cert\.pfx -Force/` so the
     removal always follows the import;
   - the removal sits in a `finally` block: assert the workflow contains `finally {`;
   - the workflow no longer contains the inaccurate string `ONLY in-memory`;
   - FUTURE-LEAK GUARD (the review's actual concern): the workflow contains neither
     `actions/upload-artifact` nor `actions/cache` — so no step can carry the workspace, and with it
     a stray cert file, off the runner. Name the test so a future author who adds one of those
     actions is forced to reckon with the cert-handling question.
   Do not modify any existing test.
  </action>
  <verify>
    <automated>npx jest --testPathPattern=releaseWorkflow</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --testPathPattern=releaseWorkflow` exits 0 with 0 failures.
    - `grep -c 'Remove-Item -Path cert.pfx -Force' .github/workflows/release-tauri.yml` prints `1`.
    - `grep -c 'ONLY in-memory' .github/workflows/release-tauri.yml` prints `0`.
    - `grep -cE 'upload-artifact|actions/cache' .github/workflows/release-tauri.yml` prints `0`.
    - The workflow still parses as valid YAML (state which parser was used in the SUMMARY).
    - This task's `git diff .github/workflows/release-tauri.yml` hunk is confined to the cert-import step and its comment.
  </acceptance_criteria>
  <done>Signing cert material is removed from the runner even on a failed import, and the workflow's comment tells the truth.</done>
</task>

<task type="auto">
  <name>Task 3: Record WR-04 and IN-01 as tracked debt; note the remaining phase close-out step</name>
  <files>.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/deferred-items.md</files>
  <read_first>
    - `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/deferred-items.md` (whole file — it currently holds exactly one entry, the 34-01 `library.ts` leaked-timer note; match its `## <plan>: <title>` / **Found during** / **Symptom** / **Scope** / **Action** structure)
    - `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-REVIEW.md` §§ WR-04, IN-01
    - `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-07-SUMMARY.md` (the deferred live tag-push gate and its six repro steps)
  </read_first>
  <action>
APPEND to `deferred-items.md`. Do NOT rewrite, reformat, or remove the existing 34-01 entry.

Entry 1 — `## 34-REVIEW WR-04: null CSP + withGlobalTauri + broad opener:default (deferred)`
  - Found during: `/gsd-code-review 34` (`34-REVIEW.md`, 2026-07-24).
  - Symptom: `src-tauri/tauri.conf.json` sets `security.csp: null`, disabling CSP for the webview,
    combined with `withGlobalTauri: true` and an `opener:default` capability grant in
    `capabilities/default.json` — any future renderer-side XSS from network-supplied store/game
    metadata would have materially higher impact than under a baseline CSP.
  - Scope: pre-existing since Phase 27 (commit `83dc57a7`), not introduced by Phase 34. It touches
    the renderer and needs its own live retest cycle (a real CSP can break webview asset/API loading
    in ways only a running app reveals).
  - Action: DEFERRED by explicit user decision (GAP-D-01) when authorizing this gap cycle. Not
    folded into 34-08..34-11. Suggested remedy from the review, recorded so it need not be
    re-derived: define a real CSP (e.g. `default-src 'self'; img-src 'self' data: https:; connect-src
    'self' https:`) tuned to the renderer's actual needs, and reassess whether `opener:default`'s
    full command set is needed by renderer JS directly or can be narrowed the way
    `shell:allow-execute` was scoped to the sidecar in 34-05.

Entry 2 — `## 34-REVIEW IN-01: sidecarSeaFsShim.ts system.pem match is looser than necessary (deferred)`
  - Found during: the same review.
  - Symptom: `meta/sidecarSeaFsShim.ts:46-48`'s `isSteamSystemPemPath` matches ANY path ending in
    `system.pem`, broader than its documented intent (`@doctormckay/steam-crypto`'s bundled Steam
    public key). If another bundled module ever read a same-named file at a different path, the shim
    would silently substitute the wrong bytes.
  - Scope: build-time only, inside a controlled/trusted build step — not attacker-reachable input.
    Practical risk low (Info severity).
  - Action: DEFERRED by explicit user decision (GAP-D-01). Suggested remedy: tighten to
    `path.includes('@doctormckay/steam-crypto') && path.endsWith('system.pem')`, or the exact
    resolved path if it proves stable across esbuild bundling.

Entry 3 — `## Phase 34 close-out: the live tag-push gate is the only remaining step`
  - State plainly that with CR-01, CR-02, WR-01, WR-02, and WR-03 closed by plans 34-08 through
    34-11, the sole remaining Phase 34 item is 34-07's deferred `checkpoint:human-verify` live
    `v*` tag-push gate (REQ-34-04 live proof + REQ-34-09), whose full six-step repro procedure is
    recorded verbatim in `34-07-SUMMARY.md`.
  - Note that this gap cycle UN-BLOCKS that gate: before these fixes the `x86_64-apple-darwin` leg
    would have failed or shipped a wrong-arch sidecar (CR-01) and the `windows-latest` leg had no
    `.ico` (CR-02), so a live run would have burned a tag on a known-broken matrix.
  - Do NOT create a duplicate live-gate plan — 34-07 already owns it and is intentionally deferred
    by the user.

Nothing in this task touches source, config, or workflow files. It is documentation only.
  </action>
  <verify>
    <automated>test $(grep -c 'WR-04' .planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/deferred-items.md) -ge 1 && test $(grep -c 'IN-01' .planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/deferred-items.md) -ge 1 && test $(grep -c '34-01: Pre-existing' .planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/deferred-items.md) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `deferred-items.md` contains headings matching `WR-04`, `IN-01`, and `Phase 34 close-out`.
    - The original `## 34-01: Pre-existing library.ts leaked-timer jest crash` heading and its body are byte-identical (`git diff` shows additions only, zero deletions).
    - The WR-04 entry names `security.csp`, `withGlobalTauri`, and `opener:default`; the IN-01 entry names `meta/sidecarSeaFsShim.ts` and `@doctormckay/steam-crypto`.
    - The close-out entry names `34-07-SUMMARY.md`, `REQ-34-09`, and states that no duplicate live-gate plan is to be created.
    - No file outside `deferred-items.md` is modified by this task.
  </acceptance_criteria>
  <done>Both out-of-scope findings are tracked debt, and the phase's one remaining close-out step is documented in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions secrets -> runner filesystem | Signing cert material is decoded from a secret and briefly materialized as a file |
| runner filesystem -> outside the job | Artifact upload / cache steps are the only ways workspace content leaves the runner |
| CI matrix -> build script | The matrix decides which target triple the sidecar compile produces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Information Disclosure | Windows signing cert on the runner (WR-02) | mitigate | `Import-PfxCertificate` is wrapped in `try/finally` with `Remove-Item -Path cert.pfx -Force -ErrorAction SilentlyContinue`, so the file is deleted even when the import throws. The step comment is corrected to describe write-then-delete rather than the false "in-memory only" claim. A test asserts the removal follows the import AND that no `actions/upload-artifact` / `actions/cache` step exists in the job |
| T-34-14 | Denial of Service / Tampering | per-leg sidecar triple (CR-01) | mitigate | Each matrix leg declares an explicit `sidecar_triple`, passed to the SEA build as `GAMELIB_SIDECAR_TARGET_TRIPLE`; combined with 34-08's `lipo -archs` build gate, an arch-mismatched sidecar cannot reach a release artifact. Four matrix declarations are asserted, so a future leg added without a triple fails the suite |
| T-34-04 | Denial of Service | secrets-less CI run | accept (unchanged) | Graceful-skip signing behavior is untouched by this plan; the existing `env.WINDOWS_CERTIFICATE != ''` gate and both `::warning::Signing skipped` steps remain, and their tests still run |
| T-34-02 | Tampering | release draft/prerelease flags | accept (unchanged) | `releaseDraft: true` / `prerelease: true` are not modified; the existing D-09 regression tests continue to guard them |
| T-34-SC | Tampering | GitHub Actions third-party actions | accept (unchanged) | No new action is introduced. The pinned set (`tauri-action@v1`, `dtolnay/rust-toolchain@stable`, `swatinem/rust-cache@v2`, `actions/checkout@v6`) is untouched, and no npm/pip/cargo package is installed by this plan |
</threat_model>

<verification>
- `npx jest --testPathPattern=releaseWorkflow` green (13 pre-existing plus the new assertions).
- `.github/workflows/release-tauri.yml` parses as valid YAML.
- `grep -c "sidecar_triple: '" .github/workflows/release-tauri.yml` prints `4`.
- `grep -c 'ONLY in-memory' .github/workflows/release-tauri.yml` prints `0`.
- `deferred-items.md` gained the WR-04, IN-01, and close-out entries with the 34-01 entry intact.
- Cross-plan regression sweep: `npx jest --testPathPattern="tauriConf|cargoFeatures|releaseWorkflow|buildSidecarSea|tauriShellSource"` green.
</verification>

<success_criteria>
- Every matrix leg supplies its own `sidecar_triple` to the SEA build step, and a future leg added
  without one fails the test suite.
- The Windows cert file is deleted in a `finally` block, the misleading comment is gone, and the
  job has no artifact/cache step that could exfiltrate the workspace.
- WR-04 and IN-01 are recorded as tracked debt, not implemented.
- The phase's one remaining close-out step (34-07's deferred live gate) is documented, with no
  duplicate plan created for it.
</success_criteria>

<output>
Create `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-11-SUMMARY.md` when done
</output>
