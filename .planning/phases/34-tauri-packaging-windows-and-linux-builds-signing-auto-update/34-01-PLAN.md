---
phase: 34-tauri-packaging-windows-and-linux-builds-signing-auto-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/__tests__/tauriConf.test.ts
  - src/backend/__tests__/cargoFeatures.test.ts
  - src/backend/__tests__/releaseWorkflow.test.ts
  - meta/__tests__/buildSidecarSea.test.ts
autonomous: true
requirements: [REQ-34-01, REQ-34-02, REQ-34-05, REQ-34-06, REQ-34-07]
must_haves:
  truths:
    - "A config-shape suite fails loudly if bundle.active is not true or targets omit nsis/appimage/dmg"
    - "A negative assertion fails loudly if the updater feed ever contains Heroic-Games-Launcher"
    - "A Cargo feature suite fails loudly if keyring lacks windows-native or sync-secret-service"
    - "A workflow suite fails loudly if the release YAML loses its v* trigger or releaseDraft/prerelease flags"
    - "A workflow suite fails loudly if the release YAML loses either per-OS `::warning::Signing skipped` step (D-04 clear-warning regression guard)"
    - "The SEA build-script suite asserts per-OS argv (codesign only on macOS, .exe only on Windows, exact sentinel-fuse) without running the real toolchain"
  artifacts:
    - path: "src/backend/__tests__/tauriConf.test.ts"
      provides: "tauri.conf.json bundle + updater feed shape assertions incl. negative Heroic assertion"
      contains: "Heroic-Games-Launcher"
    - path: "src/backend/__tests__/cargoFeatures.test.ts"
      provides: "keyring feature-list assertion (apple-native + windows-native + sync-secret-service)"
      contains: "sync-secret-service"
    - path: "src/backend/__tests__/releaseWorkflow.test.ts"
      provides: "release-tauri.yml trigger + draft/prerelease + matrix assertions"
      contains: "releaseDraft"
    - path: "meta/__tests__/buildSidecarSea.test.ts"
      provides: "SEA argv-builder unit assertions"
      contains: "NODE_SEA_FUSE"
  key_links:
    - from: "src/backend/__tests__/tauriConf.test.ts"
      to: "src-tauri/tauri.conf.json"
      via: "readFileSync of ../../../src-tauri/tauri.conf.json"
      pattern: "tauri\\.conf\\.json"
---

<objective>
Create the Wave-0 config-shape test scaffolds that every downstream Phase 34 config task
verifies against. These suites assert the TARGET shape of `tauri.conf.json`, `Cargo.toml`,
`.github/workflows/release-tauri.yml`, and the SEA build script's argv. They are RED now
(the target config does not exist yet) and are turned GREEN by plans 34-02, 34-05, 34-06.

Purpose: Nyquist compliance — no Phase 34 config change lands without an automated regression
gate that already exists to catch drift (especially the never-regress-to-Heroic feed assertion).
Output: 4 new jest suites under existing jest projects (`src/backend`, `meta`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-RESEARCH.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-PATTERNS.md
@.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-VALIDATION.md

<interfaces>
<!-- jest config: `projects` = src/backend, src/common, src/frontend, src/preload, meta. -->
<!-- src-tauri is NOT a jest project — config-shape tests reading src-tauri/* MUST live under -->
<!-- src/backend/__tests__ (repo-relative path ../../../src-tauri/...). The SEA build-script -->
<!-- test lives under meta/__tests__ (meta IS a jest project). This corrects 34-PATTERNS.md, -->
<!-- which suggested src-tauri/__tests__ (jest would never run it). -->

Analog test (read-file-then-assert-shape, one-behavior-per-test):
  src/backend/storeManagers/steam/bridge/__tests__/allowlist.test.ts

Quick run: pnpm test -- --testPathPattern=tauriConf
Full run:  pnpm test:ci
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tauriConf + cargoFeatures config-shape suites</name>
  <files>src/backend/__tests__/tauriConf.test.ts, src/backend/__tests__/cargoFeatures.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/bridge/__tests__/allowlist.test.ts (read-file-then-assert-shape analog)
    - src-tauri/tauri.conf.json (current shape — bundle.active:false, targets:"all", plugins:{})
    - src-tauri/Cargo.toml (current keyring line: features = ["apple-native"])
    - .planning/phases/34-.../34-PATTERNS.md § tauriConf/cargoFeatures test map (exact assertions)
    - electron-builder.yml lines 24-31 (the never-derive-from-defaults fork-feed precedent)
  </read_first>
  <action>
    Create `tauriConf.test.ts`: `JSON.parse(readFileSync(join(__dirname,'..','..','..','src-tauri','tauri.conf.json'),'utf-8'))`.
    One behavior per test: assert `bundle.active === true`; `bundle.targets` includes `'nsis'`, `'appimage'`, `'dmg'`; `bundle.externalBin` includes `'binaries/gamelib-sidecar'`; `bundle.createUpdaterArtifacts === true`; `plugins.updater.pubkey` is a non-empty string; `plugins.updater.endpoints[0]` matches `grayson-mitchell/GameLib`. Negative: `JSON.stringify(conf)` does NOT contain `'Heroic-Games-Launcher'` (per REQ-34-05 / 260720-q5n). Do NOT assert any `certificateThumbprint`/`signCommand` key exists (must stay absent — D-04).
    Create `cargoFeatures.test.ts`: read `Cargo.toml` as text; extract the `keyring = { ... }` dependency line; assert its `features` array contains `apple-native`, `windows-native`, AND `sync-secret-service`; assert the `tauri-plugin-updater` and `tauri-plugin-shell` dependency lines are present. Text/regex assertions (no TOML parser dependency).
    These are RED now by design (the config is not yet updated) — that is the Wave-0 scaffold contract.
  </action>
  <acceptance_criteria>
    - Both files exist under `src/backend/__tests__/`.
    - `pnpm test -- --testPathPattern="tauriConf|cargoFeatures"` DISCOVERS and EXECUTES both suites (no TypeScript/collection error) and reports assertion failures naming `bundle.active`, `targets`, and the keyring features — proving the gates are wired to the right keys (RED-for-the-right-reason).
    - The Heroic negative assertion is present as a distinct `test(...)` block.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --testPathPattern="tauriConf|cargoFeatures" 2>&1 | grep -Eq "bundle.active|sync-secret-service"</automated>
  </verify>
  <done>Both suites execute and fail on assertions (not collection), referencing the target config keys.</done>
</task>

<task type="auto">
  <name>Task 2: releaseWorkflow + buildSidecarSea suites</name>
  <files>src/backend/__tests__/releaseWorkflow.test.ts, meta/__tests__/buildSidecarSea.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/bridge/__tests__/allowlist.test.ts (assertion style)
    - meta/__tests__/gen_vtables.test.ts (imports exported pure argv-builders, asserts argv/path shape without invoking the toolchain)
    - .planning/phases/34-.../34-RESEARCH.md § "Full reference CI workflow" + Pattern 3 (SEA build)
    - .planning/phases/34-.../34-PATTERNS.md § releaseWorkflow / buildSidecarSea test map
    - .github/workflows/draft-release-linux.yml (existing tag-trigger shape)
  </read_first>
  <action>
    Create `releaseWorkflow.test.ts`: `readFileSync(join(__dirname,'..','..','..','.github','workflows','release-tauri.yml'),'utf-8')` and assert on the raw text (no YAML-parser dependency required): contains `'v*'` under a tags trigger, `workflow_dispatch`, all three runner strings (`windows-latest`, `ubuntu` [24.04 or latest], `macos-latest`), `tauri-apps/tauri-action`, `releaseDraft: true`, `prerelease: true`, the Windows conditional guard `env.WINDOWS_CERTIFICATE != ''`, the two per-OS signing-skip warning steps (`env.APPLE_CERTIFICATE == ''` and `env.WINDOWS_CERTIFICATE == ''` each emitting `::warning::Signing skipped`, proving D-04's "logs a clear warning" at the config-shape level), and the `./.github/actions/install-deps` composite. Negative: does NOT contain `Heroic`. This suite is RED until 34-06 creates the workflow (a missing-file read throws — acceptable RED).
    Create `meta/__tests__/buildSidecarSea.test.ts`: import the (not-yet-existing) exported pure argv-builders from `../buildSidecarSea` (`buildSeaConfigPath`, `buildPostjectArgv`, `sidecarOutputPath`). Assert: `sidecarOutputPath('x86_64-pc-windows-msvc')` ends with `.exe`; `sidecarOutputPath('x86_64-apple-darwin')` has no `.exe`; the postject argv contains the exact sentinel fuse `NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2`; the macho-segment flag `NODE_SEA` appears only on darwin triples. RED until 34-02 authors the module.
  </action>
  <acceptance_criteria>
    - Both files exist (`src/backend/__tests__/releaseWorkflow.test.ts`, `meta/__tests__/buildSidecarSea.test.ts`).
    - `releaseWorkflow.test.ts` asserts every field named above incl. the `releaseDraft: true`/`prerelease: true` regression for D-09, the two `::warning::Signing skipped` steps gated on the empty-cert branches (D-04), and the `Heroic` negative.
    - `buildSidecarSea.test.ts` references `NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2` verbatim and asserts the `.exe`/no-`.exe` per-triple split.
    - Both suites are RED (missing target file/module) — the Wave-0 contract.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --testPathPattern="releaseWorkflow|buildSidecarSea" 2>&1 | grep -Eq "release-tauri|NODE_SEA_FUSE|buildSidecarSea"</automated>
  </verify>
  <done>Both suites exist and are wired to the target artifacts; they fail because those artifacts do not exist yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed config → build/release pipeline | These tests are the last automated gate before a config change ships to CI; a weak assertion lets a bad feed/flag through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Spoofing | updater feed endpoint | mitigate | `tauriConf.test.ts` negative assertion: `Heroic-Games-Launcher` must never appear in the feed config; positive assertion requires `grayson-mitchell/GameLib` |
| T-34-02 | Tampering | release draft/prerelease flags | mitigate | `releaseWorkflow.test.ts` asserts `releaseDraft:true` + `prerelease:true` (D-09 human-gate cannot silently regress) |
| T-34-SC | Tampering | npm/cargo installs | accept (no installs in this plan) | This plan adds only test files; no package installs. Legitimacy gate handled in 34-02 |
</threat_model>

<verification>
- `pnpm test -- --testPathPattern="tauriConf|cargoFeatures|releaseWorkflow|buildSidecarSea"` executes all four suites.
- All four are RED (asserting on not-yet-created config) — confirming they are wired, not vacuous.
- No other jest suite regresses: `pnpm test:ci` shows only the four new suites failing.
</verification>

<success_criteria>
Four new config-shape suites exist under jest-collected project dirs (`src/backend/__tests__`, `meta/__tests__`), each failing for the right reason (target artifact absent), ready to be turned green by 34-02/34-05/34-06.
</success_criteria>

<output>
Create `.planning/phases/34-tauri-packaging-windows-and-linux-builds-signing-auto-update/34-01-SUMMARY.md` when done.
</output>
