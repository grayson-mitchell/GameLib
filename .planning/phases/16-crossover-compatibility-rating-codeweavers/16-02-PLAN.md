---
phase: 16-crossover-compatibility-rating-codeweavers
plan: 02
type: execute
wave: 2
depends_on: ['16-01']
files_modified:
  - src/backend/wiki_game_info/wiki_game_info.ts
autonomous: true
requirements: [SC-02, D-02, D-07]

must_haves:
  truths:
    - "On Mac AND Linux, getWikiGameInfo fetches CodeWeavers data (not Windows) — gated isMac || isLinux (D-07)"
    - "The CodeWeavers slug is constructed from the game TITLE for every runner — appName/runner are NOT wired into the lookup (D-02)"
    - "The returned WikiInfo object includes a codeweavers field and is written to wikiGameInfoStore (cached — SC-02)"
    - "A cache entry populated before CodeWeavers existed (codeweavers null on a Mac/Linux session) is treated as stale and re-fetched (self-heal)"
  artifacts:
    - path: "src/backend/wiki_game_info/wiki_game_info.ts"
      provides: "codeweavers fetch in Promise.all, staleCrossoverData self-heal, codeweavers in returned+cached object"
      contains: "getInfoFromCodeweavers"
  key_links:
    - from: "src/backend/wiki_game_info/wiki_game_info.ts"
      to: "getInfoFromCodeweavers"
      via: "isMac || isLinux gated call in Promise.all"
      pattern: "isMac \\|\\| isLinux"
    - from: "wikiGameInfo literal"
      to: "wikiGameInfoStore.set"
      via: "codeweavers included before cache write"
      pattern: "wikiGameInfoStore\\.set"
---

<objective>
Wire the plan-01 `getInfoFromCodeweavers` service into the `getWikiGameInfo`
orchestrator so CodeWeavers data is fetched (Mac AND Linux per D-07), assembled
into the returned `WikiInfo`, and cached in the shared `wikiGameInfoStore`. Add a
self-heal guard so cache entries written before CodeWeavers existed re-fetch once
(mirrors the existing `staleAppleData` pattern). The slug is derived from the game
TITLE for all runners — the ProtonDB `runner === 'steam' ? appName : ...` precedent
is explicitly NOT used here (D-02).

Purpose: Makes the backend data actually flow into `WikiInfo` and reach the frontend.
Output: Modified `wiki_game_info.ts` (three edit sites: Promise.all entry, self-heal
guard, assemble+cache).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-crossover-compatibility-rating-codeweavers/16-CONTEXT.md
@.planning/phases/16-crossover-compatibility-rating-codeweavers/16-PATTERNS.md
@.planning/phases/16-crossover-compatibility-rating-codeweavers/16-01-SUMMARY.md

<interfaces>
<!-- From plan 01 (already implemented before this wave) -->
From src/backend/wiki_game_info/codeweavers/utils.ts:
  export function getInfoFromCodeweavers(title: string): Promise<CodeweaversInfo | null>
From src/common/types.ts:
  interface CodeweaversInfo { rating: number | null; ratingCount: number | null; slug: string }
  interface WikiInfo { ...; codeweavers: CodeweaversInfo | null; ... }

Edit sites in src/backend/wiki_game_info/wiki_game_info.ts (read in full):
  line 8    import { getInfoFromAppleGamingWiki } from './applegamingwiki/utils'   // mirror import
  lines 24-37  cachedResponse + staleAppleData self-heal guard
  lines 41-46  Promise.all([... isMac ? getInfoFromAppleGamingWiki(title) : null, isLinux ? getUmuId(...) : null])
  lines 75-84  const wikiGameInfo = { pcgamingwiki, applegamingwiki, ... }; wikiGameInfoStore.set(title, wikiGameInfo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CodeWeavers fetch + self-heal + cache into getWikiGameInfo</name>
  <files>src/backend/wiki_game_info/wiki_game_info.ts</files>
  <read_first>
    - src/backend/wiki_game_info/wiki_game_info.ts (FULL — the orchestrator being edited; note the three sites at lines 24-37, 41-46, 75-84)
    - src/backend/wiki_game_info/applegamingwiki/utils.ts lines 31-33 (the getInfoFrom* signature this call mirrors)
    - .planning/phases/16-crossover-compatibility-rating-codeweavers/16-PATTERNS.md §"wiki_game_info.ts (MOD)" (the three edit sites spelled out)
  </read_first>
  <action>
    Three edits, mirroring the existing AppleGamingWiki wiring. (1) Import: add
    `import { getInfoFromCodeweavers } from './codeweavers/utils'` next to the line-8
    applegamingwiki import. (2) Promise.all entry (lines 41-46): add a `codeweavers`
    destructured element gated on BOTH platforms per D-07 —
    `isMac || isLinux ? getInfoFromCodeweavers(title) : null`. Pass `title` ONLY — do
    NOT pass `appName`/`runner` or add a `runner === 'steam'` branch; the slug is
    title-constructed for every runner (D-02). (3) Self-heal (lines 24-37): add
    `const staleCrossoverData = (isMac || isLinux) && !cachedResponse?.codeweavers` and
    fold it into the return guard so it becomes `if (cachedResponse && !staleAppleData
    && !staleCrossoverData) { ... return cachedResponse }`. (4) Assemble+cache (lines
    75-84): add `codeweavers` to the `wikiGameInfo` object literal before
    `wikiGameInfoStore.set(title, wikiGameInfo)` so the fetched value is returned and
    persisted (SC-02). No new store is needed — the shared `wikiGameInfoStore` (WikiInfo-
    typed) now carries the field, and the self-heal handles migration of old entries.
  </action>
  <acceptance_criteria>
    - `grep -c "getInfoFromCodeweavers(" src/backend/wiki_game_info/wiki_game_info.ts` >= 1 (call present; import counts separately).
    - The CodeWeavers call is gated by `isMac || isLinux` (not `isMac` alone, not `isLinux` alone) — D-07.
    - The CodeWeavers call receives only `title` — no `appName`, no `runner`, no `runner === 'steam'` branch (D-02).
    - `staleCrossoverData` is defined and included in the early-return guard alongside `staleAppleData`.
    - `codeweavers` appears in the `wikiGameInfo` object literal that is passed to `wikiGameInfoStore.set`.
    - `pnpm codecheck` exits 0 and the existing test suite still passes.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "isMac || isLinux ? getInfoFromCodeweavers" src/backend/wiki_game_info/wiki_game_info.ts && pnpm codecheck && pnpm test src/backend/wiki_game_info</automated>
  </verify>
  <done>CodeWeavers data is fetched on Mac+Linux (title-derived, all runners), self-healed on stale caches, and included in the returned + cached WikiInfo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lookup result → wikiGameInfoStore | Third-party-derived CodeweaversInfo is persisted in the shared cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering (cache poisoning) | staleCrossoverData self-heal + cache write | mitigate | The self-heal keys on `!cachedResponse?.codeweavers`; because plan-01 returns null (not a sentinel) on fetch error, an errored lookup stores `codeweavers: null` and is re-fetched next visit rather than persisting a false miss. Only a content-classified hit or EMPTY marker (truthy) is treated as final and cached (D-09) |
| T-16-05 | Information Disclosure (over-fetch) | Promise.all gate | mitigate | Gated `isMac || isLinux` so the outbound request is never issued on Windows (D-07); combined with the shared-cache TTL this bounds request volume to on-demand reference lookups (SC-02) |
| T-16-SC | Tampering (supply chain) | npm installs | accept | N/A — no new dependencies added in this plan |
</threat_model>

<verification>
- `grep -q "isMac || isLinux ? getInfoFromCodeweavers"` — platform gate + title-only call present.
- `pnpm codecheck` — 0 errors (WikiInfo literal shape complete).
- `pnpm test src/backend/wiki_game_info` — existing orchestrator/source tests still pass.
</verification>

<success_criteria>
- getWikiGameInfo fetches CodeWeavers on Mac AND Linux, title-derived for all runners (D-02, D-07).
- The codeweavers field is returned and cached; stale pre-CodeWeavers entries self-heal (SC-02).
</success_criteria>

<output>
Create `.planning/phases/16-crossover-compatibility-rating-codeweavers/16-02-SUMMARY.md` when done.
</output>
