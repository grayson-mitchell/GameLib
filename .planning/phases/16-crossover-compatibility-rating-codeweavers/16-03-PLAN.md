---
phase: 16-crossover-compatibility-rating-codeweavers
plan: 03
type: execute
wave: 3
depends_on: ['16-01', '16-02']
files_modified:
  - src/frontend/screens/Game/GamePage/components/crossoverRating.ts
  - src/frontend/screens/Game/GamePage/components/__tests__/crossoverRating.test.ts
  - src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx
  - public/locales/en/gamepage.json
autonomous: false
requirements: [SC-01, SC-03, D-05, D-06, D-08, D-09]

must_haves:
  truths:
    - "The Crossover rating row shows the real CodeWeavers value + count (e.g. '4.5 / 5 (2 ratings)'), NOT a bucketed tier label (D-05)"
    - "The Crossover row renders on Linux even when wikiInfo.applegamingwiki is null (D-08)"
    - "A genuine miss (codeweavers.rating === null) shows 'No compatibility data available', not a number or an error (SC-03, D-09)"
    - "The Wine rating row is unchanged — still AppleGamingWiki + ratingTier() (D-06)"
  artifacts:
    - path: "src/frontend/screens/Game/GamePage/components/crossoverRating.ts"
      provides: "formatCrossoverRating(rating, ratingCount) numeric display helper"
      exports: ["formatCrossoverRating"]
    - path: "src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx"
      provides: "per-row conditional render decoupled from applegamingwiki; numeric CrossOver row; miss state"
      contains: "codeweavers"
    - path: "public/locales/en/gamepage.json"
      provides: "info.no-compatibility-data key"
      contains: "no-compatibility-data"
  key_links:
    - from: "AppleWikiInfo.tsx"
      to: "wikiInfo.codeweavers"
      via: "independent per-row conditional (not gated on applegamingwiki)"
      pattern: "wikiInfo\\.codeweavers"
    - from: "AppleWikiInfo.tsx CrossOver row"
      to: "codeweavers deep-link"
      via: "createNewWindow to BASE_URL + / + codeweavers.slug"
      pattern: "codeweavers\\.slug"
---

<objective>
Change the extra-info "Crossover rating" row to render live CodeWeavers data: a real
numeric value + rating count (D-05), rendered independently of `applegamingwiki` so it
appears on Linux (D-08), with a graceful "No compatibility data available" state for a
genuine miss (SC-03, D-09). The Wine rating row stays exactly as-is on AppleGamingWiki +
`ratingTier()` (D-06). Deep-link the CrossOver row using the constructed slug from
`codeweavers.slug` so the link works even when AppleGamingWiki data is absent.

Purpose: Delivers the user-visible outcome (success criteria 1 and 3).
Output: A numeric display helper + test, the modified `AppleWikiInfo.tsx`, one new i18n
key, and a human-verify checkpoint confirming the row on Mac and Linux.

Follow-up (OUT OF SCOPE — do not add a task): once the CrossOver row reads from
`codeweavers`, `applegamingwiki.crossoverRating` (and its `crossoverRatingRegEx`
fetch/parse in the AppleGamingWiki backend) becomes dead/unused. Removing it is
optional post-phase cleanup, not part of this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-crossover-compatibility-rating-codeweavers/16-CONTEXT.md
@.planning/phases/16-crossover-compatibility-rating-codeweavers/16-PATTERNS.md
@.planning/phases/16-crossover-compatibility-rating-codeweavers/16-01-SUMMARY.md
@.planning/phases/16-crossover-compatibility-rating-codeweavers/16-02-SUMMARY.md

<interfaces>
<!-- From plan 01 -->
From src/common/types.ts:
  interface CodeweaversInfo { rating: number | null; ratingCount: number | null; slug: string }
  interface WikiInfo { ...; codeweavers: CodeweaversInfo | null; ... }

Analogs (read for the render pattern):
  src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx (FULL, 66 lines — file being edited)
    lines 21-25  const applegamingwiki = wikiInfo.applegamingwiki; if (!applegamingwiki) return null  ← REMOVE this coupling (D-08)
    lines 39-50  CrossOver <a> row (currently ratingTier(applegamingwiki.crossoverRating))            ← REPLACE value expr (D-05)
    lines 51-60  Wine <a> row (ratingTier(applegamingwiki.wineRating))                                ← KEEP UNCHANGED (D-06)
  src/frontend/screens/Game/GamePage/components/CompatibilityInfo.tsx
    line 33  Number.isFinite(...) and line 67 {hasProtonDB && (...)}  ← per-row independent conditional pattern to adopt
  src/frontend/screens/Game/GamePage/components/appleRating.ts (sibling helper module shape for the new crossoverRating.ts)
  public/locales/en/gamepage.json  info.* block (§184 crossover-rating, §198 wine-rating, §183 clickToOpen) — add one key
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formatCrossoverRating numeric display helper + test</name>
  <files>src/frontend/screens/Game/GamePage/components/crossoverRating.ts, src/frontend/screens/Game/GamePage/components/__tests__/crossoverRating.test.ts</files>
  <read_first>
    - src/frontend/screens/Game/GamePage/components/appleRating.ts (FULL — sibling helper module shape; note ratingTier is string-tier-only, which is why a numeric helper is needed for D-05)
    - .planning/phases/16-crossover-compatibility-rating-codeweavers/16-CONTEXT.md D-05 (numeric value + count, e.g. "4.5 / 5 (2 ratings)"; do NOT bucket into tiers)
  </read_first>
  <behavior>
    - formatCrossoverRating(4.5, 2) === '4.5 / 5 (2 ratings)'.
    - formatCrossoverRating(5, 1) === '5 / 5 (1 rating)' (singular for a count of 1).
    - formatCrossoverRating(null, null) returns null (caller renders the miss i18n string instead) — the helper never fabricates a tier.
  </behavior>
  <action>
    Create crossoverRating.ts as a sibling of appleRating.ts exporting
    `formatCrossoverRating(rating: number | null, ratingCount: number | null): string | null`.
    Return null when `rating` is null (genuine miss — caller handles the i18n string).
    Otherwise return the numeric form `${rating} / 5 (${ratingCount} rating[s])`,
    pluralizing "rating" on count !== 1. Do NOT map the number to
    perfect/playable/borderline tiers (D-05 — the CrossOver row moves OFF ratingTier()).
    Write the co-located jest test first (RED) covering the three <behavior> cases.
    Keep the "/ 5" scale text out of i18n only if the existing rows do the same; if the
    row label uses i18n, pass the formatted numeric string as the value (the number
    itself is locale-neutral).
  </action>
  <acceptance_criteria>
    - `pnpm test src/frontend/screens/Game/GamePage/components/__tests__/crossoverRating.test.ts` passes all three cases.
    - formatCrossoverRating never returns a categorical tier word (perfect/playable/etc.) — asserted by the numeric-format tests.
    - `pnpm codecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test crossoverRating && pnpm codecheck</automated>
  </verify>
  <done>formatCrossoverRating produces the numeric value+count string on a hit and null on a miss, with a passing unit test.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire AppleWikiInfo row — break applegamingwiki coupling, numeric CrossOver row, miss state, i18n key</name>
  <files>src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx, public/locales/en/gamepage.json</files>
  <read_first>
    - src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx (FULL — the early-return at 21-25, CrossOver row at 39-50, Wine row at 51-60)
    - src/frontend/screens/Game/GamePage/components/CompatibilityInfo.tsx lines 32-35, 65-93 (per-row independent conditional render pattern)
    - src/frontend/screens/Game/GamePage/components/crossoverRating.ts (formatCrossoverRating from Task 1)
    - public/locales/en/gamepage.json info.* block (existing key style to match)
  </read_first>
  <action>
    Remove the `const applegamingwiki = wikiInfo.applegamingwiki; if (!applegamingwiki)
    return null` early-return (lines 21-25) so the component no longer bails when
    AppleGamingWiki is absent (D-08). Keep the top `if (!wikiInfo) return null` guard.
    Read `const codeweavers = wikiInfo.codeweavers` and `const applegamingwiki =
    wikiInfo.applegamingwiki` as independent locals. Render each row conditionally like
    CompatibilityInfo.tsx: (a) CrossOver row renders when `codeweavers` is present — if
    `codeweavers.rating !== null` show `formatCrossoverRating(codeweavers.rating,
    codeweavers.ratingCount)`; if `codeweavers.rating === null` (genuine miss) show the
    new `t('info.no-compatibility-data', 'No compatibility data available')` string
    (SC-03, D-09). Reuse the existing `iconWithText` anchor + `WineBar` icon + i18n
    `info.crossover-rating` label. Its `onClick` deep-links via `createNewWindow` to
    `https://www.codeweavers.com/compatibility/crossover/${codeweavers.slug}` (D-08
    discretion — uses the constructed slug so it is independent of applegamingwiki).
    Split the single shared `onClick` (which today uses `applegamingwiki.crossoverLink`
    for both rows) into TWO independent handlers: the CrossOver row's handler deep-links
    to the `codeweavers.slug` URL above, and the Wine row keeps its OWN handler using the
    existing `applegamingwiki` link — the shared closure must be broken so the CrossOver
    link never depends on applegamingwiki. (b)
    Wine row renders ONLY when `applegamingwiki` is present and stays byte-for-byte on
    `ratingTier(applegamingwiki.wineRating)` (D-06 — do not touch its source or renderer).
    Add `"no-compatibility-data": "No compatibility data available"` to the `info` object
    in public/locales/en/gamepage.json (alphabetical placement, matching the flat
    `"key": "Value"` style). Do NOT dangerouslySetInnerHTML the rating — React escapes
    the coerced number/string by default (T-16-05).
  </action>
  <acceptance_criteria>
    - The `if (!applegamingwiki) return null` early-return is gone; `grep -c "if (!applegamingwiki)" AppleWikiInfo.tsx` === 0.
    - `grep -c "wikiInfo.codeweavers" src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx` >= 1.
    - The Wine row still calls `ratingTier(applegamingwiki.wineRating)` (D-06 unchanged) — `grep -c "ratingTier(applegamingwiki.wineRating)"` >= 1.
    - The CrossOver row no longer calls `ratingTier(...crossoverRating)` — `grep -c "ratingTier(applegamingwiki.crossoverRating)"` === 0.
    - `public/locales/en/gamepage.json` contains `"no-compatibility-data"` under `info` and is valid JSON (`node -e "require('./public/locales/en/gamepage.json')"` exits 0).
    - `pnpm codecheck` exits 0 and `pnpm lint src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx` is clean.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./public/locales/en/gamepage.json')" && pnpm codecheck && pnpm lint src/frontend/screens/Game/GamePage/components/AppleWikiInfo.tsx</automated>
  </verify>
  <done>CrossOver row shows the numeric value+count on a hit and the miss string on a miss, renders independently of applegamingwiki, and the Wine row is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify CrossOver rating row on Mac and Linux</name>
  <action>Manual verification only — confirm the numeric hit render, graceful miss state, unchanged Wine row, and Linux-independent render per the steps below. No code changes.</action>
  <what-built>
    The extra-info "Crossover rating" row now sources live CodeWeavers data: numeric
    value + count on a hit (D-05), "No compatibility data available" on a genuine miss
    (SC-03), rendering on both Mac and Linux independently of AppleGamingWiki (D-08),
    with the Wine row unchanged (D-06).
  </what-built>
  <how-to-verify>
    1. Run the app (`pnpm start` / your usual dev launch).
    2. Open a game's page → Extra-info tab. Pick a title with a known CodeWeavers
       listing (e.g. Hades, Half-Life 2, or Grand Theft Auto V).
       EXPECT: "Crossover rating: 5 / 5 (1 rating)" (or the real value+count) — a
       NUMBER, not "Playable"/"Perfect". Clicking it opens the CodeWeavers crossover
       page for that game.
    3. Open a game that is NOT in CodeWeavers (e.g. a niche/sideloaded title, or one
       the spike showed as a soft-404).
       EXPECT: "Crossover rating: No compatibility data available" — not a number, not
       an error, not a blank pill.
    4. Confirm the "Wine rating" row still shows its AppleGamingWiki tier label
       (Perfect/Playable/Unrated) exactly as before (D-06).
    5. If on Linux (or you can test Linux): confirm the Crossover row still appears even
       though AppleGamingWiki is not fetched on Linux (D-08). On Windows: confirm no
       CrossOver fetch/row change (gated out — D-07).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CodeweaversInfo → DOM | Third-party-derived rating value/count is rendered in the game page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering (stored XSS via third-party content) | AppleWikiInfo.tsx render | mitigate | Rating is coerced to a Number in the helper and rendered as React text children (auto-escaped); no `dangerouslySetInnerHTML`. The deep-link URL is `BASE_URL + '/' + codeweavers.slug` where slug is `[a-z0-9-]` (sanitized in plan 01), so `createNewWindow` cannot be pointed at an attacker-chosen scheme/host |
| T-16-SC | Tampering (supply chain) | npm installs | accept | N/A — no new dependencies |
</threat_model>

<verification>
- `pnpm test crossoverRating` — numeric-format helper unit cases pass.
- `grep` gates: `if (!applegamingwiki) return null` removed; `wikiInfo.codeweavers` referenced; Wine row still on `ratingTier(applegamingwiki.wineRating)`; CrossOver row no longer on `ratingTier(...crossoverRating)`.
- `pnpm codecheck` + `pnpm lint` clean; gamepage.json valid JSON with the new key.
- Human-verify checkpoint: numeric hit, graceful miss, Wine unchanged, Linux-independent render.
</verification>

<success_criteria>
- CrossOver row shows the real CodeWeavers value + count, not a tier (SC-01, D-05).
- Genuine misses show the graceful "no compatibility data available" state (SC-03, D-09).
- Row renders independently of applegamingwiki (D-08); Wine row unchanged (D-06).
</success_criteria>

<output>
Create `.planning/phases/16-crossover-compatibility-rating-codeweavers/16-03-SUMMARY.md` when done.
</output>
