---
phase: 14-guided-claim-flow
plan: 05
type: execute
wave: 5
depends_on: ["14-01", "14-03", "14-04"]
files_modified:
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
  - src/frontend/screens/Humble/Keys/Waiting/index.tsx
  - src/frontend/screens/Humble/Keys/Spares/index.tsx
  - src/frontend/screens/Humble/Keys/index.css
  - public/locales/en/translation.json
autonomous: true
requirements: [HCLAIM-01, HCLAIM-02, HCLAIM-05]

must_haves:
  truths:
    - "Keys-waiting rows show a 'Claim' button (UNREVEALED) or 'Finish activation' button (REVEALED) that opens the wizard"
    - "D-67: the Claim/Finish action renders ONLY on the Keys-waiting tab — All-keys and Spares rows never render it"
    - "A key whose keyindex is unresolved shows a disabled 'Sync to enable claiming' state instead of opening a failing wizard"
    - "Fuzzy-owned Spares rows expose an 'undo override' affordance (WR-04) that clears the override"
    - "D-75: audit is surfaced ONLY as per-row 'Revealed {date}' / 'Redeemed {date}' annotations (with Undo for local redeems) — no audit-viewer surface exists"
    - "D-80: no active nudges for REVEALED-unconfirmed keys — no banners, toasts, or notifications beyond the passive row state"
  artifacts:
    - path: "src/frontend/screens/Humble/Keys/Waiting/index.tsx"
      provides: "claim/finish wiring + wizard mount + annotations"
      contains: "HumbleClaimWizard"
    - path: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      provides: "claimAction prop + undo-override affordance"
      contains: "claimAction"
  key_links:
    - from: "src/frontend/screens/Humble/Keys/Waiting/index.tsx"
      to: "HumbleClaimWizard via showDialogModal"
      via: "claimAction.onClick opens modal message=<HumbleClaimWizard/>"
      pattern: "showDialogModal"
    - from: "src/frontend/screens/Humble/Keys/Spares/index.tsx"
      to: "window.api.humbleClearOwnershipOverride"
      via: "WR-04 undo-override affordance"
      pattern: "humbleClearOwnershipOverride"
---

<objective>
Wire the claim flow into the Keys-waiting tab: add a Claim/Finish button + annotations to HumbleKeyRow, mount HumbleClaimWizard from Waiting, add the WR-04 undo-override affordance to Spares, and register all new i18n strings.

Purpose: This is the single UI entry point the C2 guard protects (D-67 — Keys-waiting only). It also closes D-71's WR-04 (a mistaken "Not the same game" override is now the sanctioned claim path, so it must be reversible). The keyindex-unresolved disabled state prevents Pitfall C from opening a wizard that would fail.
Output: HumbleKeyRow claimAction prop + undo-override, Waiting integration, Spares WR-04 wiring, scoped CSS, and translation.json entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-guided-claim-flow/14-CONTEXT.md
@.planning/phases/14-guided-claim-flow/14-PATTERNS.md
@.planning/phases/14-guided-claim-flow/14-UI-SPEC.md

<interfaces>
<!-- Existing frontend contracts. From codebase reads. -->

From HumbleKeyRow/index.tsx (existing): Props { humbleKey, urgencyTier?, giftAction? }; giftAction pattern = { giftedAt: number|null; onGift: () => void } with a ternary annotation-vs-button render; D-22 read-only-except-sanctioned-exceptions comment; the ownedElsewhere block with the fuzzy-only "Not the same game" override calling window.api.humbleSetOwnershipOverride(machineName)
From Waiting/index.tsx (existing): reads humble.keys via ContextProvider, selectKeysWaiting, maps HumbleKeyRow with key `${gamekey}:${machineName}` and urgencyTier
From Spares/index.tsx (existing): showDialogModal from ContextProvider; humbleGetGiftedAt; openExternalUrl; giftAction wiring
From ContextProvider: { humble, showDialogModal }
From window.api (Plan 03 preload): humbleGetClaimAnnotations() → Record<composite, {revealedAt?,redeemedAt?,keyindexResolved:boolean}> (an entry exists for EVERY key); humbleUndoRedeemed({gamekey,machineName}); humbleClearOwnershipOverride(machineName) (existing Phase 12); humbleGetRevealedKeyValue
From HumbleClaimWizard (Plan 04): default export, props { humbleKey, entryMode:'claim'|'finish', onDone }
HumbleKey (Plan 01): + state, ownedElsewhere, matchConfidence, steamAppId, locallyRedeemedPending?

Copy (14-UI-SPEC.md, verbatim): "Claim", "Finish activation", "Revealed {{date}}", "Redeemed {{date}}", "Undo", "Sync to enable claiming"; WR-04 undo-override affordance copy: reuse a concise "Undo — this game is not owned" style clear-override label (Claude's discretion per D-71, keep it short and reversible-sounding).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HumbleKeyRow claimAction prop + WR-04 undo-override affordance</name>
  <files>src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx</files>
  <read_first>
    - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx (full — giftAction ternary pattern, the ownedElsewhere/fuzzy override block, the D-22 comment)
    - .planning/phases/14-guided-claim-flow/14-UI-SPEC.md (Row-level entry points, button chrome = .humbleKeyGiftButton family, step-state colors)
  </read_first>
  <action>
    Add an optional prop `claimAction?: { revealedAt: number | null; redeemedAt: number | null; keyindexResolved: boolean; onClaim: () => void; onFinish: () => void; onUndoRedeem: () => void }`. Render it following the giftAction ternary convention, ONLY when supplied (Keys-waiting is the sole caller — D-67):
    - If claimAction.redeemedAt !== null: show a `humbleKeyRevealedAnnotation`-style span "Redeemed {{date}}" plus an "Undo" button calling claimAction.onUndoRedeem (D-77; the caller decides to pass this only while the redeem is a local-mark — locallyRedeemedPending).
    - Else if claimAction.revealedAt !== null: show a "Revealed {{date}}" annotation plus a "Finish activation" button calling claimAction.onFinish.
    - Else (UNREVEALED): if claimAction.keyindexResolved is false, render a DISABLED "Claim" button (or a non-interactive caption "Sync to enable claiming" per Pitfall C); otherwise render an enabled "Claim" button calling claimAction.onClaim.
    Style all buttons with the existing `.humbleKeyGiftButton` bordered family (var(--text-secondary) default, var(--accent) hover, --text-xs/--regular) — a sibling to the Gift button, not a new style.
    Add a fuzzy-only "undo override" affordance in the existing ownedElsewhere block: when matchConfidence === 'fuzzy' and the caller supplies an `onUndoOverride` (add it as part of an optional `undoOverride?: () => void` prop, OR fold into a small addition), render a reversal control next to the "Not the same game" override calling window.api.humbleClearOwnershipOverride(machineName) — this is WR-04 (a mistaken override must be reversible). Keep the existing "Not the same game" button intact; the undo is its counterpart on Spares rows.
    Update the D-22 comment to name the THIRD sanctioned exception (the claimAction prop) and the WR-04 undo-override, so the "strictly read-only" note stays accurate.
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
  </verify>
  <done>HumbleKeyRow accepts claimAction (+ undo-override) and renders Claim / Finish activation / annotations / disabled-when-unresolved per the ternary convention; D-22 comment updated.</done>
  <acceptance_criteria>
    - `grep -q "claimAction" src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx`
    - `grep -q "keyindexResolved" src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx`
    - `grep -q "humbleClearOwnershipOverride" src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx`
    - D-22 comment mentions the claimAction exception (grep for an updated exception count/wording)
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Waiting-tab wizard mount + annotations; Spares WR-04 wiring; CSS</name>
  <files>src/frontend/screens/Humble/Keys/Waiting/index.tsx, src/frontend/screens/Humble/Keys/Spares/index.tsx, src/frontend/screens/Humble/Keys/index.css</files>
  <read_first>
    - src/frontend/screens/Humble/Keys/Waiting/index.tsx (full)
    - src/frontend/screens/Humble/Keys/Spares/index.tsx (full — openGiftDialog showDialogModal pattern, giftedMap fetch)
    - src/frontend/screens/Humble/Keys/index.css (humbleKey*/humbleKeys* classes + semantic tokens)
    - src/frontend/screens/Humble/Keys/components/HumbleClaimWizard/index.tsx (props)
  </read_first>
  <action>
    In Waiting/index.tsx: destructure `{ humble, showDialogModal }` from ContextProvider. Fetch annotations on mount with a cancellable effect: `window.api.humbleGetClaimAnnotations().then(setAnnotations)` (state Record<composite, {revealedAt?,redeemedAt?}>), mirroring Spares' giftedMap effect. For each key, pass a `claimAction` built from the annotations entry at `${gamekey}:${machineName}`: revealedAt/redeemedAt from the entry, and keyindexResolved = annotations[composite]?.keyindexResolved ?? false (a real per-key signal computed by the backend from its internal keyindex field — Plan 03). An UNREVEALED key with keyindexResolved false renders the disabled "Sync to enable claiming" state and NEVER opens the wizard (Pitfall C / 14-UI-SPEC contract); the backend { status:'ineligible' } guard remains the authoritative backstop, not the primary UX. onClaim opens the wizard: showDialogModal({ showDialog:true, title: t('humbleKeys.claimWizardTitle','Claim this key'), message: <HumbleClaimWizard humbleKey={key} entryMode="claim" onDone={() => showDialogModal({ showDialog:false })} />, buttons: [] }). onFinish is the same but entryMode="finish". onUndoRedeem calls window.api.humbleUndoRedeemed({ gamekey, machineName }). Pass redeemedAt only when the key is a local redeem (locallyRedeemedPending) so the Undo affordance disappears once a server sync confirms (D-77). Import HumbleClaimWizard.
    In Spares/index.tsx: pass the WR-04 undo-override wiring to HumbleKeyRow for fuzzy-owned rows (onUndoOverride → window.api.humbleClearOwnershipOverride(key.machineName)), reusing the existing giftAction render. This closes D-71 WR-04 on the Spares surface where fuzzy-owned keys live.
    In index.css: add classes for the claim/finish button + revealed/redeemed annotations + undo affordance, all using existing semantic tokens (mirror .humbleKeyGiftButton / .humbleKeyGiftedAnnotation). No hex, no raw px.
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
  </verify>
  <done>Waiting mounts the wizard via showDialogModal with claim/finish entry modes and passes annotations; Spares exposes the WR-04 undo-override; CSS uses semantic tokens.</done>
  <acceptance_criteria>
    - `grep -q "HumbleClaimWizard" src/frontend/screens/Humble/Keys/Waiting/index.tsx`
    - `grep -q "humbleGetClaimAnnotations" src/frontend/screens/Humble/Keys/Waiting/index.tsx`
    - `grep -q 'entryMode="finish"' src/frontend/screens/Humble/Keys/Waiting/index.tsx || grep -q "entryMode='finish'" src/frontend/screens/Humble/Keys/Waiting/index.tsx`
    - `grep -q "keyindexResolved" src/frontend/screens/Humble/Keys/Waiting/index.tsx` (real disabled-state signal wired, not defaulted true)
    - `grep -q "humbleClearOwnershipOverride" src/frontend/screens/Humble/Keys/Spares/index.tsx`
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Register new humbleKeys i18n strings</name>
  <files>public/locales/en/translation.json</files>
  <read_first>
    - public/locales/en/translation.json (existing humbleKeys.* entries + JSON structure)
    - .planning/phases/14-guided-claim-flow/14-UI-SPEC.md (Copywriting Contract — the verbatim source of every string)
  </read_first>
  <action>
    Add every new humbleKeys.* key introduced by Plan 04 (wizard) and Plan 05 (row) to the en translation.json with the verbatim UI-SPEC copy as the value, keeping the existing alphabetical/grouped structure and NOT altering any existing key path. Keys to add (defaults from UI-SPEC): claimWizardTitle ("Claim this key"), claim ("Claim"), finishActivation ("Finish activation"), revealTitle ("Reveal this key?"), revealBody (the full irreversibility sentence), revealDismiss ("Don't reveal yet"), revealConfirm ("Reveal key"), ownedBlockTitle ("You already own this on Steam"), ownedBlockBody, ownedBlockGoto ("Go to Giftable spares"), yourKey ("Your key"), copyKey ("Copy key"), openSteam ("Open Steam"), redeemOnPlatform ("Redeem on {{platform}}"), markRedeemed ("Mark as redeemed"), ownedPassiveNote, ambiguousOutcome ("We couldn't confirm this finished — sync to check"), revealFailed ("Couldn't reveal this key — nothing was used up. You can try again."), tryAgain ("Try again"), cooldownRetry ("Temporarily unavailable — retry in {{N}}m"), redeemedAnnotation ("Redeemed {{date}}"), undo ("Undo"), revealedAnnotation ("Revealed {{date}}"), syncToEnableClaiming ("Sync to enable claiming"), plus the WR-04 undo-override label. Match the exact interpolation token names used in the components ({{platform}}, {{date}}, {{N}}).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('public/locales/en/translation.json','utf8')); console.log('valid json')"</automated>
  </verify>
  <done>All new humbleKeys.* strings present with verbatim UI-SPEC copy; JSON valid; no existing key changed.</done>
  <acceptance_criteria>
    - `node -e "const j=require('./public/locales/en/translation.json'); ['claim','finishActivation','revealConfirm','ownedBlockTitle','markRedeemed','redeemedAnnotation','syncToEnableClaiming'].forEach(k=>{if(!JSON.stringify(j).includes(k))throw new Error('missing '+k)}); console.log('ok')"` prints ok
    - translation.json parses as valid JSON
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer view membership→IPC | Keys-waiting is the only tab exposing the claim entry; backend re-check is the guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering / EoP | Keys-waiting claim entry | mitigate | UI restricts the claim entry to Keys-waiting (D-67), but the authoritative C2 guard is the backend re-check (Plan 03) — the UI filter is first-line only |
| T-14-10 | Tampering | keyindex-unresolved claim | mitigate | Backend returns 'ineligible' for a missing keyindex; UI shows the disabled "Sync to enable claiming" state (Pitfall C) so no failing wizard opens |
| T-14-11 | Repudiation | mistaken fuzzy override | mitigate | WR-04 undo-override affordance makes a mistaken "Not the same game" reversible (D-71), preventing a truly-owned key from being permanently claimable |
</threat_model>

<verification>
- `pnpm codecheck` exits 0.
- `node -e` JSON validity check passes.
- Full frontend suite: `pnpm jest src/frontend` exits 0.
</verification>

<success_criteria>
Keys-waiting rows expose Claim/Finish activation opening the wizard with the correct entry mode and only on that tab; unresolved-keyindex keys are disabled with the sync hint; Spares fuzzy-owned rows can undo an override (WR-04); revealed/redeemed annotations render with an Undo for local redeems; all strings are registered.
</success_criteria>

<output>
Create `.planning/phases/14-guided-claim-flow/14-05-SUMMARY.md` when done.
</output>
