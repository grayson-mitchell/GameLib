---
phase: 14-guided-claim-flow
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - src/backend/humble/library.ts
  - src/backend/humble/__tests__/library.test.ts
  - src/backend/humble/ipc_handler.ts
  - src/preload/api/humble.ts
autonomous: true
requirements: [HCLAIM-01, HCLAIM-02, HCLAIM-03, HCLAIM-04]

must_haves:
  truths:
    - "revealKey is the ONLY function that calls the adapter reveal write, behind an explicit per-key IPC action"
    - "The reveal chain re-checks ownedElsewhere server-side and hard-blocks owned games before any audit/API call (C2)"
    - "The audit record and REVEALED flag are persisted BEFORE the adapter reveal call (write-ahead, SC4)"
    - "A definitive failure rolls back the REVEALED flag; an ambiguous failure keeps it"
    - "A successful reveal/redeem/undo directly patches the cached HumbleKey.state and re-pushes humbleKeysUpdated"
  artifacts:
    - path: "src/backend/humble/library.ts"
      provides: "revealKey/markRedeemed/undoRedeemed orchestration + patchCachedState + audit helpers"
      contains: "function revealKey"
    - path: "src/backend/humble/ipc_handler.ts"
      provides: "4 server-side-revalidating handlers"
      contains: "humbleRevealKey"
    - path: "src/preload/api/humble.ts"
      provides: "invokers for the new channels"
      contains: "humbleRevealKey"
  key_links:
    - from: "src/backend/humble/library.ts revealKey"
      to: "src/backend/humble/adapter.ts revealKey"
      via: "single call site after write-ahead persistence"
      pattern: "adapterRevealKey|revealKey\\("
    - from: "src/backend/humble/ipc_handler.ts humbleRevealKey"
      to: "HumbleLibrary.revealKey"
      via: "server-side re-validated delegate returning RevealOutcome"
      pattern: "HumbleLibrary.revealKey"
---

<objective>
Implement the core claim orchestration in library.ts (reveal, mark-redeemed, undo) with the write-ahead-audit → C2-recheck → adapter-call → outcome-reconciliation chain, plus the direct cache-projection patch and the IPC/preload surface the wizard consumes.

Purpose: This is the phase's safety-critical center. It enforces C1 (single reveal call site), C2 (backend hard block, D-69/D-70), SC4 (write-ahead audit ordering), D-78 (definitive-vs-ambiguous rollback), D-79 (cooldown reuse), D-74 (revealed key persistence), and D-77 (undoable local redeem). Every rule here is a locked decision, not a judgment call.
Output: New HumbleLibrary methods + patch helper + audit/annotation helpers with exhaustive tests, four re-validating IPC handlers, and four preload invokers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-guided-claim-flow/14-CONTEXT.md
@.planning/phases/14-guided-claim-flow/14-RESEARCH.md
@.planning/phases/14-guided-claim-flow/14-PATTERNS.md

<interfaces>
<!-- Existing library/ipc/preload contracts to extend. From codebase reads. -->

From src/backend/humble/library.ts (existing):
- recomputeOwnership(): reads humbleLibraryStore.entries(), read-modify-writes each entry, then sendFrontendMessage('humbleKeysUpdated', getKeys()) — the direct-patch precedent
- setOwnershipOverride(machineName) / clearOwnershipOverride(machineName): store.set/delete then recomputeOwnership()
- recordGiftLinkOpened(machineName): humbleGiftedAtStore.set(...); getAllGiftedAt(): machineName->ms map
- getKeys(): HumbleKey[] (display-safe projection); getSyncState()/setSyncState(patch)
- cookie fetch: `const cookie = HumbleUser.getCredentials(); if (!cookie) return { status:'failed' }`
- cooldown gate: getSyncState().cooldownUntil > Date.now() → skip; set via setSyncState({ syncError:'denied', cooldownUntil: Date.now()+HUMBLE_COOLDOWN_MS })
- syncInFlight single-flight guard; export object `HumbleLibrary = { loadCached, sync, getKeys, getSyncState, recomputeOwnership, setOwnershipOverride, clearOwnershipOverride, recordGiftLinkOpened, getAllGiftedAt }`

From Plan 01 (electronStores): humbleAuditStore, humbleLocalRedeemedStore, humbleRevealedKeyValueStore (composite `gamekey:machineName` keyed); existing humbleRevealedStore (machineName-keyed, { revealedAt })
From Plan 02 (adapter): `revealKey(cookie, csrfToken, { gamekey, machineName, keyindex })`; classifyOrder now returns keyIndexByComposite; HumbleUser.getCsrfToken()
From Plan 01 (types): RevealOutcome, RedeemOutcome, ClaimAnnotation
From src/backend/humble/ipc_handler.ts (existing): humbleRecordGiftLinkOpened handler — the server-side re-validation shape to mirror (look up target key, check precondition, logWarning+return on mismatch)
From src/preload/api/humble.ts: `makeHandlerInvoker('channelName')` per channel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Library helpers — patchCachedState, keyindex map, predicates, key-value + annotation accessors</name>
  <files>src/backend/humble/library.ts</files>
  <read_first>
    - src/backend/humble/library.ts (full — recomputeOwnership ~308, setOwnershipOverride/clearOwnershipOverride, recordGiftLinkOpened/getAllGiftedAt, runSync classifyOrder call sites, getKeys, the HumbleLibrary export object ~658)
    - src/backend/humble/classify.ts (classifyOrder new signature from Plan 02: isRevealed + isLocallyRedeemed predicates + keyIndexByComposite return)
    - src/backend/humble/electronStores.ts (the 3 new stores + humbleRevealedStore)
  </read_first>
  <action>
    Add module-level `const compositeKey = (gamekey: string, machineName: string) => `${gamekey}:${machineName}``.
    Add `patchCachedState(gamekey, machineName, newState: HumbleKeyState, extra?: { locallyRedeemedPending?: boolean })`: get the humbleLibraryStore entry by gamekey; if absent return; find the key index by machineName; if -1 return; spread-replace that one key with `{ ...key, state: newState, ...(extra?.locallyRedeemedPending !== undefined ? { locallyRedeemedPending: extra.locallyRedeemedPending } : {}) }`; write the entry back; call sendFrontendMessage('humbleKeysUpdated', getKeys()). Mirror recomputeOwnership's read-modify-write-then-push shape (Pattern 4).
    Wire the new classifyOrder predicates at every classifyOrder call site in library.ts: pass `isLocallyRedeemed: (gk, mn) => humbleLocalRedeemedStore.has(compositeKey(gk, mn))` as the second predicate. Maintain a module-level `let keyIndexLookup: Record<string, string|number> = {}` merged from each classifyOrder result's keyIndexByComposite as orders commit during a sync (reset at sync start, accumulate per order). Add `lookupKeyindex(gamekey, machineName): string | number | undefined` reading keyIndexLookup[compositeKey(...)].
    Add `getRevealedKeyValue(gamekey, machineName): string | null`: return humbleRevealedKeyValueStore.get(compositeKey(...))?.key ?? null (only the plaintext key; never logged).
    Add audit helper `appendAudit(gamekey, machineName, event: string, fields: { title: string; platform: string; outcome?: string })`: read the existing array at compositeKey (default []), push `{ event, at: Date.now(), title, platform, outcome }`, write back. NEVER include a key value (C4/D-76).
    Add `getClaimAnnotations(): Record<string, ClaimAnnotation>`: for every humbleLibraryStore key, build composite entries with `revealedAt` from humbleRevealedStore.get(machineName)?.revealedAt (machineName-keyed — accepted pre-existing limitation, Open Q4) and `redeemedAt` from humbleLocalRedeemedStore.get(compositeKey(...))?.redeemedAt. Return only composites that have at least one timestamp.
    Update the HumbleLibrary export object to add: revealKey, markRedeemed, undoRedeemed (added in Task 2), getRevealedKeyValue, getClaimAnnotations, patchCachedState (patchCachedState may stay unexported if only used internally — expose the three orchestration methods + the two read accessors).
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
  </verify>
  <done>patchCachedState, keyindex lookup accumulation, getRevealedKeyValue, appendAudit, and getClaimAnnotations exist and typecheck; classifyOrder call sites pass the isLocallyRedeemed predicate.</done>
  <acceptance_criteria>
    - `grep -q "function patchCachedState" src/backend/humble/library.ts`
    - `grep -q "getRevealedKeyValue" src/backend/humble/library.ts && grep -q "getClaimAnnotations" src/backend/humble/library.ts`
    - `grep -q "isLocallyRedeemed" src/backend/humble/library.ts` (predicate wired into classifyOrder)
    - `grep -q "lookupKeyindex" src/backend/humble/library.ts`
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: revealKey / markRedeemed / undoRedeemed orchestration</name>
  <files>src/backend/humble/library.ts, src/backend/humble/__tests__/library.test.ts</files>
  <read_first>
    - src/backend/humble/library.ts (Task 1 additions + cooldown pattern in runSync ~450-505 + cookie fetch ~445)
    - src/backend/humble/__tests__/library.test.ts (how adapter + stores + SteamUser are mocked; existing sync/override tests)
    - .planning/phases/14-guided-claim-flow/14-RESEARCH.md Pattern 2 (the worked revealKey orchestration) + Architecture Patterns diagram (5a/5b/5c branches) + Pitfall B (ambiguous outcome, confirmed flag) + Pitfall D (sync race)
  </read_first>
  <behavior>
    - revealKey on an owned key (ownedElsewhere true, exact OR fuzzy) returns { status:'owned_blocked' }, writes a 'c2_block' audit event, and makes NO adapter call and sets NO REVEALED flag.
    - revealKey on a non-UNREVEALED or unknown key returns { status:'ineligible' } with no side effects.
    - revealKey with an active cooldown returns { status:'cooldown', retryAtMs } without calling the adapter.
    - On the happy path: audit 'reveal_attempt' AND humbleRevealedStore.set happen BEFORE the adapter call (write-ahead); on adapter 'ok' → store key value (confirmed:true), audit 'reveal_success', patchCachedState → 'REVEALED', return { status:'revealed', key }.
    - On adapter 'schema_error'/'access_denied'/'session_expired' (definitive) → delete the REVEALED flag (rollback), audit 'reveal_failed', return { status:'failed' } (access_denied ALSO sets the cooldown via setSyncState, reusing HUMBLE_COOLDOWN_MS).
    - On a thrown/transient error (ambiguous) → KEEP the REVEALED flag, store NO key value (or confirmed:false), audit 'reveal_ambiguous', return { status:'ambiguous' }.
    - markRedeemed on a REVEALED key writes humbleLocalRedeemedStore, audit 'mark_redeemed', patchCachedState → 'REDEEMED' with locallyRedeemedPending:true, returns { status:'ok' }; ineligible otherwise.
    - undoRedeemed deletes the local-redeemed mark, audit 'undo_redeemed', patchCachedState → 'REVEALED' (locallyRedeemedPending:false); no-op if a server redeemed value now exists.
  </behavior>
  <action>
    Add `async function revealKey(gamekey, machineName): Promise<RevealOutcome>`. Steps in exact order: (1) find the target in getKeys() by gamekey+machineName; if missing or state !== 'UNREVEALED' return { status:'ineligible' }. (2) C2 re-check (D-69/D-70): if target.ownedElsewhere === true (regardless of matchConfidence — fuzzy blocks like exact, D-70), appendAudit 'c2_block' and return { status:'owned_blocked' } — no further work. (3) cooldown gate: if getSyncState().cooldownUntil > Date.now() return { status:'cooldown', retryAtMs: cooldownUntil }. (4) resolve `const keyindex = lookupKeyindex(gamekey, machineName)`; if undefined return { status:'ineligible' } (Pitfall C — never call reveal with a missing keyindex). (5) fetch cookie via HumbleUser.getCredentials(); if absent return { status:'failed' }; csrfToken via HumbleUser.getCsrfToken(). (6) WRITE-AHEAD: appendAudit 'reveal_attempt' THEN humbleRevealedStore.set(machineName, { revealedAt: Date.now() }) — both before the network call (SC4). (7) call the adapter's revealKey(cookie, csrfToken, { gamekey, machineName, keyindex }). (8) branch per Behavior: ok → humbleRevealedKeyValueStore.set(composite, { key, confirmed:true }), audit 'reveal_success' outcome, patchCachedState 'REVEALED', return { status:'revealed', key }; schema_error/access_denied/session_expired → humbleRevealedStore.delete(machineName), audit 'reveal_failed', if access_denied setSyncState({ syncError:'denied', cooldownUntil: Date.now()+HUMBLE_COOLDOWN_MS }), return { status:'failed' }; catch/transient → audit 'reveal_ambiguous', KEEP the flag, return { status:'ambiguous' }. Guard against a concurrent sync (Pitfall D): re-read the cache entry immediately before patchCachedState (already the pattern) and, if a sync is in flight, still patch (accepted narrow window — document the choice in a comment; do NOT add a second cooldown). There must be exactly ONE call site of the adapter reveal function in the entire backend — no loop, no retry wrapper (C1, T-14-05).
    Add `async function markRedeemed(gamekey, machineName): Promise<RedeemOutcome>` and `async function undoRedeemed(gamekey, machineName): Promise<void>` per Behavior, using humbleLocalRedeemedStore + appendAudit + patchCachedState. undoRedeemed no-ops when the target key's server-redeemed value is present (server truth wins, D-77).
    Export revealKey/markRedeemed/undoRedeemed from the HumbleLibrary object.
    Extend library.test.ts with a describe block covering every Behavior bullet: mock the adapter revealKey to return each AdapterResult status and to throw; assert store writes/deletes, audit events, patch calls (spy sendFrontendMessage), the write-ahead ORDER (audit + flag recorded before the mocked adapter resolves — e.g. adapter mock asserts the flag already exists when invoked), the C2 block on both exact and fuzzy ownership, and the single-call-site invariant (a code assertion or a comment-backed test that revealKey is invoked once per user action).
  </action>
  <verify>
    <automated>pnpm jest src/backend/humble/__tests__/library.test.ts</automated>
  </verify>
  <done>All three orchestration methods implemented with write-ahead ordering, C2 hard block, D-78 rollback split, D-79 cooldown reuse, and D-77 undo; full test matrix green.</done>
  <acceptance_criteria>
    - `grep -q "function revealKey" src/backend/humble/library.ts && grep -q "function markRedeemed" src/backend/humble/library.ts && grep -q "function undoRedeemed" src/backend/humble/library.ts`
    - library.test.ts has a test proving the REVEALED flag + audit exist BEFORE the adapter call resolves (write-ahead)
    - library.test.ts has a test proving ownedElsewhere fuzzy match returns owned_blocked (C2 D-70)
    - library.test.ts has a test proving definitive failure deletes the flag AND ambiguous failure keeps it (D-78)
    - `pnpm jest src/backend/humble/__tests__/library.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: IPC handlers + preload invokers (server-side re-validation)</name>
  <files>src/backend/humble/ipc_handler.ts, src/preload/api/humble.ts</files>
  <read_first>
    - src/backend/humble/ipc_handler.ts (full — humbleSetOwnershipOverride + humbleRecordGiftLinkOpened re-validation shape, registerHumbleIpcHandlers)
    - src/preload/api/humble.ts (full — makeHandlerInvoker per channel)
    - src/common/types/ipc.ts (the 5 signatures added in Plan 01)
  </read_first>
  <action>
    In registerHumbleIpcHandlers add four handlers, each accepting `(e, params: { gamekey, machineName })` (composite params, not machineName alone). `humbleRevealKey` → return HumbleLibrary.revealKey(params.gamekey, params.machineName) directly (revealKey already performs the authoritative server-side eligibility + C2 re-check internally; the handler is a thin typed delegate returning RevealOutcome). `humbleMarkRedeemed` → HumbleLibrary.markRedeemed(...). `humbleUndoRedeemed` → HumbleLibrary.undoRedeemed(...). `humbleGetRevealedKeyValue` → HumbleLibrary.getRevealedKeyValue(params.gamekey, params.machineName) (returns string|null; this is the ONLY channel that ever transmits a key value, and only on explicit demand while the wizard is open — never broadcast). Add `humbleGetClaimAnnotations` → HumbleLibrary.getClaimAnnotations(). Do NOT register a raw store bridge for any of these (WR-09 store-isolation discipline). Add a short comment on humbleGetRevealedKeyValue noting it is the C4 narrow-exposure channel (T-14-02).
    In src/preload/api/humble.ts add one `export const <name> = makeHandlerInvoker('<name>')` line per new channel: humbleRevealKey, humbleMarkRedeemed, humbleUndoRedeemed, humbleGetRevealedKeyValue, humbleGetClaimAnnotations.
  </action>
  <verify>
    <automated>pnpm codecheck</automated>
  </verify>
  <done>Five handlers registered and five invokers exported; typecheck passes.</done>
  <acceptance_criteria>
    - `grep -q "humbleRevealKey" src/backend/humble/ipc_handler.ts && grep -q "humbleGetRevealedKeyValue" src/backend/humble/ipc_handler.ts && grep -q "humbleGetClaimAnnotations" src/backend/humble/ipc_handler.ts`
    - `grep -c "makeHandlerInvoker" src/preload/api/humble.ts` increased by 5 relative to prior (all five new invokers present)
    - `pnpm codecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main IPC | Renderer requests reveal/redeem; main process is the sole authority on ownership + eligibility |
| main→Humble API | The single reveal write call site lives here |
| main→disk | Write-ahead audit + REVEALED flag + revealed key value persisted before/around the network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering / Elevation of Privilege | C2 guard in library.revealKey | mitigate | Backend re-reads ownedElsewhere from the live key set before any audit/API call; renderer view-membership is never trusted; adversarial "renderer says unowned, backend says owned" unit test |
| T-14-02 | Information Disclosure | humbleGetRevealedKeyValue channel | mitigate | Key value exposed only via this narrow on-demand handler, never on humbleKeysUpdated/HumbleKey; annotations carry timestamps only |
| T-14-05 | Repudiation / Tampering | reveal call site | mitigate | Exactly one adapter reveal call site; no retry wrapper; ambiguous outcome never auto-resubmits (D-78) |
| T-14-01 | Information Disclosure | audit records | mitigate | AuditRecord holds identity+outcome only, never the key value (D-76) |
</threat_model>

<verification>
- `pnpm codecheck` exits 0.
- `pnpm jest src/backend/humble/__tests__/library.test.ts` exits 0.
- Full backend suite: `pnpm jest src/backend/humble` exits 0.
</verification>

<success_criteria>
The claim orchestration enforces C1 (single call site), C2 (backend hard block for exact and fuzzy), SC4 (write-ahead), D-78 (rollback split), D-79 (cooldown reuse), D-74 (key persistence), D-77 (undoable local redeem); the IPC/preload surface exposes reveal/redeem/undo/getRevealedKeyValue/getClaimAnnotations with server-side re-validation.
</success_criteria>

<output>
Create `.planning/phases/14-guided-claim-flow/14-03-SUMMARY.md` when done.
</output>
