---
phase: 25-steam-depot-download-multi-host-fan-out-throughput
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified: []
autonomous: false
requirements: [MHOST-04]

must_haves:
  truths:
    - "A real multi-depot install on macOS/Apple Silicon shows sustained hosts>1 in the chunk-stream stats log"
    - "downSpeedMiBs is materially higher than the ~1.5–2.9 MiB/s pre-fix baseline"
    - "Decode stays clean (err=0) and no cancel/abort or stall-retry regression is observed"
  artifacts:
    - path: ".planning/phases/25-steam-depot-download-multi-host-fan-out-throughput/25-03-SUMMARY.md"
      provides: "Recorded before/after throughput measurement + the chunk-stream stats evidence"
      contains: "hosts="
  key_links:
    - from: "chunk-stream stats log line"
      to: "hosts>1 sustained"
      via: "grep of ~/Library/Logs/gamelib/gamelib.log"
      pattern: "hosts="
---

<objective>
Validate on real hardware that the Phase 25 fan-out fix actually raises throughput: a real authenticated Steam CM connection + real CDN edges + a real multi-depot install on macOS/Apple Silicon, measured before/after via the EXISTING `chunk-stream stats` log line. This is the phase's acceptance gate (MHOST-04) and is inherently manual — it cannot be reproduced in jest because it depends on live Steam CDN behavior and a large real depot.

Purpose: The mechanism-level correctness (fan-out distribution, attempt>0 unchanged, circuit-breaker/abort intact) is already proven by the automated tests in Plans 25-01/25-02. What jest cannot prove is that spreading attempt-0 across top-N hosts yields a materially higher sustained download speed against real Steam infrastructure — only real hardware can. No new instrumentation is added; the existing log line already reports `hosts=`, `downSpeedMiBs`, and per-host `wl=/avgMs=/unhealthy`.
Output: a recorded before/after throughput measurement in the SUMMARY, confirming sustained `hosts>1` and higher `downSpeedMiBs` with `err=0` and no abort/stall regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-steam-depot-download-multi-host-fan-out-throughput/25-RESEARCH.md
@.planning/phases/25-steam-depot-download-multi-host-fan-out-throughput/25-VALIDATION.md
@.planning/phases/25-steam-depot-download-multi-host-fan-out-throughput/25-01-SUMMARY.md
@.planning/phases/25-steam-depot-download-multi-host-fan-out-throughput/25-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Real-hardware before/after throughput measurement</name>
  <read_first>
    - .planning/phases/25-steam-depot-download-multi-host-fan-out-throughput/25-VALIDATION.md (the Manual-Only Verifications row — the exact diagnostic command + pass condition)
    - .planning/phases/25-steam-depot-download-multi-host-fan-out-throughput/25-RESEARCH.md (the `chunk-stream stats` log line section + the ~1.5–2.9 MiB/s baseline recorded in the resolved debug session)
  </read_first>
  <what-built>
    Plans 25-01 and 25-02 shipped the fan-out fix: `pickHost` now spreads attempt-0 across the top-N healthy CDN hosts (`TOP_N_FANOUT`), and both concurrency pools thread a distinct worker slot so concurrent chunk workers — including within one large file — request from more than one host. The existing `chunk-stream stats` log line already reports `hosts=`, `downSpeedMiBs`, and per-host `wl=/avgMs=/unhealthy` every ~15s; no new logging was added.
  </what-built>
  <how-to-verify>
    Preconditions: real macOS/Apple Silicon build with the Steam native-install opt-in ON, a signed-in Steam account owning a multi-depot title (e.g. Hogwarts Legacy 990080 or Cyberpunk 2077 1091500) that is NOT already installed.

    BEFORE (baseline — optional if the ~1.5–2.9 MiB/s figure from the resolved debug session is accepted): if capturing fresh, check out the pre-Phase-25 build, install the title, and record `grep "chunk-stream stats" ~/Library/Logs/gamelib/gamelib.log | tail -20` — expect `hosts=1` and `downSpeedMiBs` ~1.5–2.9.

    AFTER (this build):
    1. Delete any partial install / stale `appmanifest_{appId}.acf` for the chosen title so it downloads fresh.
    2. Start the install and let it run for at least 2–3 `chunk-stream stats` ticks (~45s+).
    3. Run: `grep "chunk-stream stats" ~/Library/Logs/gamelib/gamelib.log | tail -20`
    4. Confirm ALL of:
       - `hosts=` is sustained > 1 (not a one-tick blip) across multiple ticks.
       - `downSpeedMiBs` is materially higher than the ~1.5–2.9 baseline.
       - per-host stats show `err=0` (decode still clean) and no unexpected `unhealthy` churn.
    5. Confirm no regression: cancel the download mid-run and verify it aborts cleanly (no crash, no host recorded as failed for the cancel), then optionally resume — the install completes and Steam adopts the manifest.

    Paste the `chunk-stream stats` before/after lines into the resume signal or note them for the SUMMARY.
  </how-to-verify>
  <acceptance_criteria>
    - Recorded `chunk-stream stats` evidence shows sustained `hosts>1` across multiple ticks on the AFTER build.
    - `downSpeedMiBs` on the AFTER build is materially higher than the ~1.5–2.9 MiB/s baseline.
    - Per-host stats show `err=0` (no decode regression) and no abnormal `unhealthy` escalation.
    - Cancel mid-run aborts cleanly with no crash and no cancel-as-failure host record; a subsequent run completes and is adopted by Steam.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the before/after `chunk-stream stats` lines, or describe the shortfall (e.g. hosts still 1, or speed unchanged) so the fan-out width / slot combination can be retuned.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN content-server host → GameLib backend | The measurement exercises the real untrusted CDN edges. Verification includes confirming the fanned-out hosts are the same vetted `getContentServers` set (no off-list host) and that integrity (`err=0`) holds across all of them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Spoofing (SSRF/host integrity) | live host set under fan-out | mitigate (verify) | The hardware check confirms every host appearing in `chunk-stream stats` is a Steam-vetted content-server edge (matches `getContentServers` output), proving fan-out did not widen the host set beyond the authenticated CM directory. |
| T-25-02 | Tampering (chunk integrity) | live decode/SHA1 | mitigate (verify) | The `err=0` acceptance condition confirms per-chunk SHA1 verification still rejects nothing spuriously and that distributing across more hosts did not weaken integrity — a mismatched chunk from any host would surface as a decode error or failed install. |
| T-25-SC | Tampering (supply chain) | dependencies | accept | No package installs in this phase. |
</threat_model>

<verification>
- Manual: `grep "chunk-stream stats" ~/Library/Logs/gamelib/gamelib.log | tail -20` shows sustained `hosts>1` and higher `downSpeedMiBs` than baseline, `err=0`.
- Automated suites from Plans 25-01/25-02 remain green (`npm test -- --testPathPattern=steam`).
</verification>

<success_criteria>
- Before/after throughput recorded; AFTER build shows sustained `hosts>1` and materially higher `downSpeedMiBs`.
- No decode (`err=0`), cancel/abort, or stall-retry regression observed on real hardware.
- Result captured in 25-03-SUMMARY.md as the MHOST-04 acceptance evidence.
</success_criteria>

<output>
Create `.planning/phases/25-steam-depot-download-multi-host-fan-out-throughput/25-03-SUMMARY.md` when done, embedding the before/after `chunk-stream stats` evidence.
</output>
