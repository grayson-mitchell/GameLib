---
phase: 34.14-steam-platform-row-depot-signal-distinguish-no-windows-build
review: 34.14-REVIEW.md
status: resolved
fixed: 2026-08-22
findings_total: 4
findings_fixed: 1
outstanding: []
---

# Fix pass for 34.14-REVIEW.md — phase 34.14-steam-platform-row-depot-signal-distinguish-no-windows-build

## Why this file exists

`34.14-REVIEW.md` carries `status: issues_found` with `warning: 1` and `info: 3`. That status
records what the review found on 2026-08-16 and, per `reviewStatus()`'s own doc comment in
`~/.vscode/extensions/gsd-phase-status/parse.js:205-209`, is "never rewritten when the fixes land" —
the review file itself is not the place to record remediation. The sanctioned way to state where a
review now stands is this fix-pass sibling: when a `REVIEW-FIX.md` exists beside a `REVIEW.md`,
`reviewStatus()` reads the fix pass's `status:` instead of falling back to the review's own (stale)
status.

Every other 34.14 gate settled on 2026-08-16 — `34.14-VERIFICATION.md` `passed` (20/20 must-haves),
`34.14-SECURITY.md` `verified` (`threats_open: 0`), `34.14-UAT.md` `passed` (the BLOCKING D-08 gate,
20 pass / 8 not-applicable / 0 fail), 5/5 plans with SUMMARYs, and `ROADMAP.md:1456` reads **PHASE
COMPLETE 2026-08-16**. The review was the only artifact left reporting otherwise, and it was
reporting a Warning that had already been fixed the same day.

This file changes no behaviour and touches no code. It is a record of where each of the four
findings in `34.14-REVIEW.md` stands, sourced from artifacts written before this task.

## Dispositions

| Finding | Severity | Disposition | Evidence |
|---|---|---|---|
| WR-01 | Warning | FIXED | Commit `527f7eea6` *"test(34.14): close WR-01 — add 'pending' mode unit coverage"* (2026-08-16, ancestor of HEAD). Independently confirmed by the phase verifier at `34.14-VERIFICATION.md:97` — ✓ PASS. Re-derived at HEAD 2026-08-22: see below. |
| IN-01 | Info | ACCEPTED — no action required | The finding's own `**Fix:**` line, `34.14-REVIEW.md:145`: the risk is already covered by a real assertion at `steamSectionGating.test.ts:856-868`, not merely by comment. |
| IN-02 | Info | ACCEPTED — no action required | The finding's own `**Fix:**` line, `34.14-REVIEW.md:168`: `installFormIpc.ts:91-95`'s in-code comment already documents the ambiguity correctly and no downstream logic distinguishes the two "uncaptured" causes. |
| IN-03 | Info | ACCEPTED — no action required | The finding's own `**Fix:**` line, `34.14-REVIEW.md:188`: the computation is inert (no non-Steam path reads `windowsDepotOffered`/`depotSignalResolved`); gating it would be a clarity nicety, not a defect. |

`findings_fixed: 1` counts WR-01 only. The three Info findings are dispositioned ACCEPTED rather
than FIXED because the review itself prescribes no change for them — counting them as "fixed" would
overstate what was done.

## WR-01 re-derived at HEAD, not copied

The review is six days old, and this repo has a standing lesson that a code-read prediction can
outlive its own fix and misdirect a session. WR-01's disposition was therefore re-checked against
the live file on 2026-08-22 rather than taken from the 2026-08-16 record:

- `ALL_MODES` at `steamPlatformRow.test.ts:45-51` now contains all five modes including `'pending'`
  (line 50) — the review's first prescribed change. It feeds the cross-product membership test at
  `:272-284`.
- `readonlyPlatformValue('pending')` is asserted directly at `:307-309` (`-> 'Mac'`) — the review's
  second prescribed change, verbatim.
- `selectSteamPlatformOptions('pending', ...)` is exercised directly at `:232-235`, and the shipped
  fix goes past what WR-01 asked for with two further `'pending'` invariants at `:237-252` (the
  Windows depot flag never leaks into a row the user cannot change) and `:254-266` (the same for the
  macOS pin).
- `npx jest --config src/frontend/jest.config.js --runInBand --silent
  src/frontend/screens/Library/components/InstallModal/__tests__/steamPlatformRow.test.ts` — 56/56
  pass, suite green.

Note for anyone grepping the history: `git log --grep WR-01` on this test file returns **two**
commits, and they are different findings. `527f7eea6` is 34.14's WR-01 (this one — missing
`'pending'` unit coverage). `dabd1ccc4` *"fix(34.15-gap): gate the Steam platform selector's mac
entry on macDepotOffered (WR-01)"* is **34.15's** WR-01, a separate behavioural fix in the
`'selectable'` branch, dispositioned by 34.15 and out of scope here.

## Badge effect

Replayed against the real extension code (`require`d from
`~/.vscode/extensions/gsd-phase-status/parse.js`, not reimplemented):

| Call | Result |
|---|---|
| `reviewStatus({status:'issues_found', critical:0}, null)` — before this file | `inprogress` |
| `reviewStatus({status:'issues_found', critical:0}, 'resolved')` — after | `complete` |
| Control: same call with the fix status withheld | `inprogress` |

The control is what makes the first two rows meaningful: the move to `complete` is caused by this
file's `status:`, not by anything else that changed in the folder. `artifactStatus('resolved')`
maps to `complete` via `parse.js:158-162`.

## Outstanding

**Nothing.** `outstanding:` is empty and this phase has no `deferred-items.md` — no 34.14 review
finding was deferred to a ledger item, and none is waiting on a named precondition or an owner.
WR-01 is FIXED; IN-01, IN-02 and IN-03 are ACCEPTED as the review itself prescribed.

This differs from the `34.9-REVIEW-FIX.md` precedent, which is `status: partial` with
`outstanding: [IN-03]` because 34.9 genuinely deferred a finding. 34.14's status is derived from its
own dispositions, not inherited from that template.

## Scope — what this file does not cover

This artifact dispositions the findings of `34.14-REVIEW.md` **only**. Phase 34.14 has no review
cycle files. `34.14-VERIFICATION.md`, `34.14-SECURITY.md` and `34.14-UAT.md` are separate gates with
their own (already settled) statuses and are referenced here only as context. 34.15's own review
findings, including its separate `WR-01`, are dispositioned by 34.15.

---

_Fix pass recorded: 2026-08-22 — quick task `260822-ryt`_
_Reviewed document: `34.14-REVIEW.md` (2026-08-16, depth: standard, 13 files, 0 critical)_
