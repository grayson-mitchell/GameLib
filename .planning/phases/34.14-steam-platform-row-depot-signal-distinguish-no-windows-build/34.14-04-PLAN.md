---
phase: 34.14-steam-platform-row-depot-signal-distinguish-no-windows-build
plan: 04
type: execute
wave: 3
depends_on: ["34.14-02", "34.14-03"]
files_modified:
  - src/frontend/screens/Library/components/InstallModal/index.tsx
  - src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-05: the seed comes from `gameInfo` and the live answer from the widened verdict, merged by ONE pure function — `index.tsx` contains zero behavioural conditionals for this decision."
    - "D-01: while the depot signal is unresolved the Install button is disabled by the EXISTING `eligibility.pending` mechanism (34.13 D-25). No second mechanism is introduced."
    - "D-06: only the 'Install with options…' route changes. `PlatformSupport.tsx` is not touched."
    - "D-07: zero new user-facing strings ship — no new mount, no new copy, no new catalog key."
    - "The second stale 'genuinely synchronous' doc-comment (`index.tsx:341-344`) is corrected with the replacement text this plan specifies."
  artifacts:
    - path: "src/frontend/screens/Library/components/InstallModal/index.tsx"
      provides: "the wiring: seed + probe + settled → resolveDepotAvailability → gating input and option projection"
      contains: "resolveDepotAvailability"
    - path: "src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts"
      provides: "the gate proving the override is CALLED and its outputs feed both consumers"
      contains: "resolveDepotAvailability("
  key_links:
    - from: "src/frontend/screens/Library/components/InstallModal/index.tsx"
      to: "src/frontend/screens/Library/components/InstallModal/steamPlatformRow.ts"
      via: "resolveDepotAvailability call, results threaded into resolveSteamSectionGating and selectSteamPlatformOptions"
      pattern: "resolveDepotAvailability\\("
---

<objective>
Wire the pure pieces together in the composition root, and lock the wiring with a source gate
— because `index.tsx` is the one file this project's test seam cannot execute.

Purpose: a pure function proven correct but never CALLED is the exact defect class
`steamEligibilityWiring.test.ts` was created to close (34.13 review B-WR-02/B-WR-03: real
behaviour landed in `index.tsx` without extending any gate, and a revert reproduced the
original defect with the whole suite still green). This plan does not repeat that.

Output: the wiring, the corrected second stale comment, and a new wiring gate with a
known-bad derived from the real source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-CONTEXT.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-PATTERNS.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-02-SUMMARY.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-03-SUMMARY.md

<test_seam_constraint>
**BLOCKING, house rule.** `index.tsx`'s first line is `import './index.scss'` and the Frontend
jest project runs `testEnvironment: 'node'` with no CSS transform. Any test importing anything
from `index.tsx` dies at that import before its first assertion. Therefore:
- NO new logic may be written inline in `index.tsx`. Every line this plan adds there is either
  a call to an already-tested pure function or a property assignment. If you find yourself
  writing a ternary that decides behaviour, it belongs in `steamPlatformRow.ts` instead.
- The wiring is proven by SOURCE GATES (`steamEligibilityWiring.test.ts`,
  `installModalSource.test.ts`), which read the file with `readFileSync` + `stripSourceComments`,
  plus the blocking manual gate in plan 05. There is no third option.
</test_seam_constraint>

<shipped_gates_this_plan_must_not_break>
These are green at HEAD and must stay green WITHOUT being weakened. Each was verified this
session; several are absent from RESEARCH.md and PATTERNS.md.

| Gate | File:line | What it forbids | How this plan complies |
|---|---|---|---|
| `steamPlatformsCaptured` count 0 in `index.tsx` | `installModalSource.test.ts:218-221` | naming the raw store field in the composition root | the seed is read through `hasSteamDepotSignalCaptured(gameInfo)` (plan 02), never by field name |
| `is_windows_native` count 0 in `index.tsx` | `installModalSource.test.ts:145-147` | a second depot comparison | unchanged — `hasSteamWindowsDepot(gameInfo)` stays the only reader |
| `hasSteamWindowsDepot(` and `selectSteamPlatformOptions(` present | `installModalSource.test.ts:140-143` | the depot gate being imported but not called | both call sites survive; only the ARGUMENT to `selectSteamPlatformOptions` changes |
| `<EligibilityLoadingRow` appears EXACTLY once | `steamEligibilityWiring.test.ts:188-191` | a second loading-row mount | this plan adds NO mount. See the rendered-form decision below. |
| `useSteamBottleEligibility(` once, `applyEligibilityPending(` once | `steamEligibilityWiring.test.ts:239-242` | a second hook call or a second wrap | unchanged |
| no `&& !pending` / `&& !eligibility.pending` anywhere | `steamEligibilityWiring.test.ts:244-247` | hand-rolled suppression appended to a gating field read | the settled signal is passed as a standalone object property `probeSettled: !eligibility.pending`, never appended to a `steamGating.` read |
| `applyLibraryFetchPending(` once, fed by `steamLibrariesSettled` | `steamEligibilityWiring.test.ts:270-276` | dropping the second wrap | the existing two-function composition chain at `index.tsx:349-352` is NOT modified by this plan |
| `steamGating.platformRow` present | `installModalSource.test.ts:116-118` | the verdict being computed but not read | unchanged |

**Rendered-form decision (Claude's Discretion, `--skip-ui`), stated so it is not "fixed" back:**
the pending row reuses the existing read-only-macOS projection and mounts NO second
`<EligibilityLoadingRow />`. Rationale, evidence-based: the pending-platform-row window is a
strict SUBSET of the `eligibility.pending` window — both are answered by the same single
round-trip — so a second mount would render two identical "Checking install options…" rows
simultaneously, every time, not as an edge case. The wait is already explained on screen by the
existing mount in the wine-region slot, and Install is already disabled (D-01 / 34.13 D-25).
This also keeps the B1 gate at exactly one and ships zero new strings (D-07).
</shipped_gates_this_plan_must_not_break>

<interfaces>
Anchors in `index.tsx`, verified at HEAD this session:

- `:113` — `const eligibility = useSteamBottleEligibility({ ... })` (so the hook's values are
  already in scope well before the depot wiring below).
- `:174-175` — `const isSteamManagedApp = runner === 'steam' && action === 'install' && Boolean(gameInfo)`.
- `:177-180` — the D-17 comment block and `const hasWindowsDepot = hasSteamWindowsDepot(gameInfo)`.
  This is the insertion point for the new derived block.
- `:300-331` — the `resolveSteamSectionGating` `useMemo`, whose object literal currently passes
  `hostPlatform, selectedPlatform, hasWindowsDepot, bottleRequired, nativeInstallOn, libraryCount`,
  with a six-entry dependency array.
- `:333-344` — the comment block ending in the SECOND stale claim (verbatim at HEAD):
  "Suppressed here instead by `applyEligibilityPending`; `platformRow` is genuinely
  synchronous and passes through untouched."
- `:349-352` — the `applyLibraryFetchPending(applyEligibilityPending(...), steamLibrariesSettled)`
  composition. NOT modified by this plan.
- `:360-373` — `legacyPlatformRowMode`, `platformRowMode`, and
  `platformRowOptions = isSteamManagedApp ? selectSteamPlatformOptions(platformRowMode, platforms, hasWindowsDepot) : availablePlatforms`.
- `:413-450` — `platformSelection()`. Its `if (platformRowMode === 'absent') return null` guard
  does NOT catch `'pending'`, which is CORRECT and intentional here — the pending row renders,
  disabled, via the existing `disabled={platformRowMode !== 'selectable' || ...}` prop and the
  existing `value={readonlyPlatformValue(platformRowMode) ?? platformToInstall}` expression.
  **No new render branch is added by this plan.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the depot resolution into the gating input and the option projection</name>
  <files>src/frontend/screens/Library/components/InstallModal/index.tsx</files>
  <read_first>
    - `src/frontend/screens/Library/components/InstallModal/index.tsx` lines 1-60, 105-200, 290-380, 405-500 (the file being modified — import block, hook, depot wiring point, gating call, comment block, render function)
    - `src/frontend/screens/Library/components/InstallModal/steamPlatformRow.ts` (as extended by plan 02 — the exact signature of `resolveDepotAvailability` and `hasSteamDepotSignalCaptured`)
    - `src/frontend/screens/Library/components/InstallModal/__tests__/installModalSource.test.ts` lines 78-260 (every gate this file must keep green)
    - `.planning/phases/34.14-.../34.14-PATTERNS.md` § `index.tsx` (the exact current text of both anchors)
  </read_first>
  <action>
1. Extend the existing `import { hasSteamWindowsDepot, selectSteamPlatformOptions, readonlyPlatformValue } from './steamPlatformRow'` with `hasSteamDepotSignalCaptured` and
   `resolveDepotAvailability`.

2. Immediately after the existing `const hasWindowsDepot = hasSteamWindowsDepot(gameInfo)` line
   (`:180`), add the derived block:
   - `const seedDepotSignalCaptured = hasSteamDepotSignalCaptured(gameInfo)` — D-05's seed. Comment
     that the raw `GameInfo` field is deliberately read through this helper and never by name
     here, both because `installModalSource.test.ts` bans the token in this file and because a
     second raw reader is the drift `steamPlatformRow.ts`'s header warns against.
   - a destructuring assignment of `resolveDepotAvailability({ ... })`'s two outputs
     (`depotSignalResolved`, `windowsDepotOffered`), passing `seedHasWindowsDepot: hasWindowsDepot`,
     `seedDepotSignalCaptured`, `probeHasWindowsDepot: eligibility.hasWindowsDepot`,
     `probeDepotSignalCaptured: eligibility.depotSignalCaptured`, and
     `probeSettled: !eligibility.pending`.
   Write `probeSettled: !eligibility.pending` as a standalone object property. Do NOT write it
   as a term appended to any `steamGating.` field read — `steamEligibilityWiring.test.ts`'s B5
   gate bans the strings `&& !pending` and `&& !eligibility.pending`, and that ban is correct.
   Comment the block with a one-line statement that ALL of D-04's fail-open and D-05's
   seed/resolve logic lives inside `resolveDepotAvailability`, that this file therefore
   contains no behavioural conditional for it, and that this is deliberate because this file
   cannot be unit-tested (no jsdom, `import './index.scss'` on line 1).

3. Thread the results into the `resolveSteamSectionGating` `useMemo` (`:300-331`):
   - pass `hasWindowsDepot: windowsDepotOffered` (the OFFER decision, which folds in D-04's
     fail-open) instead of the raw `hasWindowsDepot`;
   - add `depotSignalResolved`;
   - update the dependency array: replace `hasWindowsDepot` with `windowsDepotOffered` and add
     `depotSignalResolved`. A stale dep here means the row never leaves `'pending'` when the
     probe lands — the exact bug this phase fixes, reintroduced through a dependency array.

4. Change the `selectSteamPlatformOptions` argument at `:371-373` from `hasWindowsDepot` to
   `windowsDepotOffered`. Without this the fail-open path would set the row `'selectable'` while
   the projection still dropped the Windows entry — an offer that renders as no offer. Keep the
   surrounding comment about reading the UNFILTERED `platforms` array; extend it with one
   sentence naming this trap explicitly so a future reader does not "simplify" the argument back.

5. Leave `const hasWindowsDepot = hasSteamWindowsDepot(gameInfo)` in place. It is now the SEED
   input to `resolveDepotAvailability` rather than a direct consumer. `installModalSource.test.ts`
   requires `hasSteamWindowsDepot(` to remain present and called from executable code.

6. **Correct the second stale doc-comment** (`:341-344`). Replace the sentence "`platformRow`
   is genuinely synchronous and passes through untouched" with text stating the actual
   post-34.14 invariant: `platformRow` still passes through `applyEligibilityPending`
   untouched, but it is NOT synchronous — it is synchronous only when the depot signal was
   already captured at open (D-05's seed case); otherwise it starts as `'pending'` and is
   resolved by `resolveDepotAvailability` feeding `depotSignalResolved`/`windowsDepotOffered`
   into the memo above. Cite 34.14 D-01/D-04/D-05. This is the SECOND of the two stale
   comments (the first, `steamEligibilityProbe.ts:161-163`, is plan 03's); PATTERNS.md found
   this one — CONTEXT.md's landmine list named only the first.

7. Add NO render branch to `platformSelection()` and mount NO second `<EligibilityLoadingRow />`.
   The `'pending'` mode already renders as a disabled control through the existing `disabled`
   and `value` expressions. Record this as the deliberate rendered-form decision, citing the
   duplicate-row rationale from this plan's `<shipped_gates_this_plan_must_not_break>` block.
  </action>
  <verify>
    <automated>npx jest src/frontend/screens/Library/components/InstallModal/__tests__/installModalSource.test.ts src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest .../installModalSource.test.ts` exits 0 with ZERO modifications to that file: `git diff --name-only` must not list it. Every gate in the table above passes unchanged.
    - `npx jest .../steamEligibilityWiring.test.ts` exits 0 for all pre-existing specs (Task 2 adds new ones).
    - `grep -c "resolveDepotAvailability(" src/frontend/.../index.tsx` returns `1` — one call, not one per consumer.
    - `grep -c "windowsDepotOffered" src/frontend/.../index.tsx` returns at least `4` (destructuring, gating input, gating dep array, `selectSteamPlatformOptions` argument).
    - `grep -c "selectSteamPlatformOptions(platformRowMode, platforms, hasWindowsDepot)" src/frontend/.../index.tsx` returns `0` — the raw seed is no longer the projection argument.
    - `grep -c "&& !eligibility.pending" src/frontend/.../index.tsx` and `grep -c "&& !pending" src/frontend/.../index.tsx` both return `0`.
    - The unqualified stale claim is gone: `grep -c "genuinely synchronous and passes through untouched" src/frontend/.../index.tsx` returns `0`, and the replacement text is quoted verbatim in the SUMMARY.
    - `grep -c "<EligibilityLoadingRow" src/frontend/.../index.tsx` returns `1`.
    - `npx tsc --noEmit` reports zero errors in `index.tsx`.
  </acceptance_criteria>
  <done>The composition root calls one pure resolver once and feeds both consumers from it; every shipped source gate on this file passes unmodified; the second stale comment is corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the wiring with a source gate proven against a known-bad derived from the real source</name>
  <files>src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts</files>
  <read_first>
    - `src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts` (FULL file — the file being modified; Group B's B6/B7-RED block at lines 254-340 is the exact idiom to copy, including its recorded rationale for why "the pure suite proves it is CORRECT, these prove it is CALLED")
    - `src/frontend/screens/Library/components/InstallModal/index.tsx` (as wired in Task 1)
  </read_first>
  <action>
Append a new group to `steamEligibilityWiring.test.ts` — `describe('Group E (34.14): the depot
resolution is CALLED, once, and feeds BOTH consumers')` — following Group B's established
`stripped(...)` + `countOccurrences` + derived-known-bad structure. Specs:

- **E1:** `resolveDepotAvailability(` appears exactly ONCE in `index.tsx`'s stripped source.
  Two calls would be two independent D-04 decisions that can disagree — the drift
  `steamPlatformRow.ts`'s header warns against.
- **E2:** within the 400 characters following that call, all five input property names appear:
  `seedHasWindowsDepot`, `seedDepotSignalCaptured`, `probeHasWindowsDepot`,
  `probeDepotSignalCaptured`, `probeSettled`. A call missing the seed pair silently loses D-05;
  a call missing `probeSettled` silently loses D-04.
- **E3:** `windowsDepotOffered` — not the raw `hasWindowsDepot` — is what reaches
  `selectSteamPlatformOptions`. Assert the flattened source (whitespace collapsed, matching the
  file's own `replace(/\s+/g, ' ')` convention for surviving `prettier` reflow) contains
  `selectSteamPlatformOptions(platformRowMode, platforms, windowsDepotOffered)` and does NOT
  contain `selectSteamPlatformOptions(platformRowMode, platforms, hasWindowsDepot)`.
- **E4:** `depotSignalResolved` appears inside the `resolveSteamSectionGating(` call's object
  literal AND inside its `useMemo` dependency array. Bound the search to the memo's span
  (locate `resolveSteamSectionGating(`, then the following `],` that closes the dep array) so a
  `depotSignalResolved` written anywhere else in the file cannot satisfy it.
- **E-RED:** three known-bads DERIVED FROM THE REAL SOURCE (`source.replace(...)`), each
  asserted to trip its own gate and each asserted `not.toBe(source)` first so a no-op
  replacement cannot vacuously pass:
  (a) delete the `resolveDepotAvailability(` call → E1 fails;
  (b) swap the `selectSteamPlatformOptions` third argument back to `hasWindowsDepot` → E3 fails
      (this is the precise regression that would make fail-open render as no offer);
  (c) delete `depotSignalResolved` from the dep array → E4 fails (the stale-dep regression that
      pins the row at `'pending'` forever).

Also add **E5**, the co-occurrence proof VALIDATION.md requires for D-01: assert that
`index.tsx` passes `eligibilityPending={eligibility.pending}` (the existing `:479` prop) AND
that `probeSettled: !eligibility.pending` feeds `resolveDepotAvailability` — i.e. the Install
disable and the pending row are driven by the SAME expression, not by two flags that could
desync. Bound each match and pair it with a derived known-bad in which the prop is fed from a
different identifier.
  </action>
  <verify>
    <automated>npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityWiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All of E1-E5 pass against the real source, and all three E-RED known-bads throw/fail against their own gate. A gate whose known-bad was never executed does not count — the SUMMARY must show each E-RED spec's output.
    - Every E-RED known-bad asserts `expect(knownBad).not.toBe(source)` before exercising the gate, matching the B3-RED / B6-RED convention. A `String.replace` that matched nothing would otherwise silently make the RED proof vacuous — the exact WR-09 defect class this file's own comments record.
    - E3 and E4 operate on whitespace-flattened source, so a `prettier --write` reflow cannot fail a behaviour gate (34.13 review C-12's recorded lesson, already applied twice in this file).
    - `git diff --numstat` for `steamEligibilityWiring.test.ts` shows ZERO deletions — Group E is purely additive and no existing gate is relaxed to accommodate this phase.
    - `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/` exits 0 (all five files).
  </acceptance_criteria>
  <done>The wiring is locked by five gates with three source-derived known-bads, and D-01's single-mechanism claim is proven as a co-occurrence rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gating verdict → rendered platform options | Decides which install platforms are OFFERED to the user; the selection itself is executed by the pre-existing, unchanged install path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.14-09 | Elevation of Privilege | fail-open offering a Windows/wine install for a game with no Windows depot | accept | The offer is an option in a picker, not an action. The user must explicitly select Windows AND press Install, and Install is disabled until `eligibility.pending` clears (D-01). The resulting install runs the pre-existing bottle path, which this phase does not modify and which validates its own inputs. The failure mode is a failed download, not a privileged or destructive operation. Weighed against the certain, permanent option-withholding fail-closed produces for every offline user (the defect this phase exists to end), this is the correct trade — and it makes the modal CONSISTENT with `PlatformSupport.tsx`, which already renders the Windows glyph unconditionally for every game. |
| T-34.14-10 | Tampering | a stale `useMemo` dependency pinning the row at `'pending'` | mitigate | Task 1 step 3 updates the dependency array explicitly; Task 2's E4 gate plus its E-RED(c) known-bad prove the dep is present and that removing it fails the gate. A permanently-pending row would leave Install permanently disabled — a functional denial of the feature, caught here rather than at UAT. |
| T-34.14-11 | Information Disclosure | new renderer logging of depot state | accept | This plan adds no log statement. The only `window.api.logError` in the touched region is the pre-existing `getAlternativeWine` handler, untouched. Steam-identity redact-forward has no new surface. |
| T-34.14-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed by this phase. No install task exists in this plan. |
</threat_model>

<verification>
- `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/` exits 0 (all five files: `installModalSource`, `steamEligibilityProbe`, `steamEligibilityWiring`, `steamPlatformRow`, `steamSectionGating`).
- `npx tsc --noEmit` clean for `index.tsx`.
- `git diff --name-only` lists exactly `index.tsx` and `steamEligibilityWiring.test.ts` — `installModalSource.test.ts` must NOT appear.
- `git diff -- src/frontend/screens/Game/GamePage/components/PlatformSupport.tsx` is empty (D-06 route fence).
</verification>

<success_criteria>
- One call to `resolveDepotAvailability`, its two outputs feeding the gating input and the option projection respectively.
- The Install disable and the pending row are driven by the same `eligibility.pending` expression, proven by co-occurrence.
- Every shipped source gate on `index.tsx` passes unmodified; the new Group E is purely additive with three source-derived known-bads.
- No second loading-row mount, no new render branch, no new string, `PlatformSupport.tsx` untouched.
</success_criteria>

<output>
Create `.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-04-SUMMARY.md` when done.
Record: the verbatim replacement text for the second stale comment; each E-RED spec's output;
confirmation that `installModalSource.test.ts` is unmodified and green; and the exact wiring
lines added to `index.tsx`.
</output>
