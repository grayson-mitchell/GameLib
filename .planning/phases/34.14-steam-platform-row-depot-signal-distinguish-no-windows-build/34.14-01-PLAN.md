---
phase: 34.14-steam-platform-row-depot-signal-distinguish-no-windows-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/types/steam.ts
  - src/backend/storeManagers/steam/installFormIpc.ts
  - src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts
  - src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts
  - src/backend/sidecar/__tests__/steamAuthFlows.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-02: the depot signal reaches the renderer by WIDENING the existing SteamBottleEligibilityVerdict round-trip — no new IPC channel, no second probe, no extra fetch."
    - "D-03: the widened verdict carries TWO BOOLEANS (hasWindowsDepot, platformsCaptured), not a tri-state; both are REQUIRED so no construction site can silently omit them."
    - "D-02 (no-fork half): exactly ONE `export async function getSteamBottleEligibilityVerdict` exists in the repo; both Electron and the Tauri sidecar pick up the widening from that single body."
    - "The timeout/offline path is distinguishable: a verdict computed after ensurePlatformsCaptured() timed out reports platformsCaptured:false, never a bare hasWindowsDepot:false."
  artifacts:
    - path: "src/common/types/steam.ts"
      provides: "SteamBottleEligibilityVerdict widened with two required booleans"
      contains: "hasWindowsDepot"
    - path: "src/backend/storeManagers/steam/installFormIpc.ts"
      provides: "the single shared handler body, reading steamMetadataStore after checkBottleEligibility() resolves"
      contains: "steamMetadataStore"
  key_links:
    - from: "src/backend/storeManagers/steam/installFormIpc.ts"
      to: "src/backend/storeManagers/steam/electronStores.ts"
      via: "steamMetadataStore.get(appName)"
      pattern: "steamMetadataStore\\.get\\("
---

<objective>
Widen `SteamBottleEligibilityVerdict` with the two booleans D-03 names, and populate them in
the ONE shared IPC handler body, reading `steamMetadataStore` immediately after
`checkBottleEligibility()` has already forced and awaited `ensurePlatformsCaptured()`.

Purpose: D-02's whole mechanism. The eligibility probe already performs the exact fetch that
captures the depot signal; this plan makes that already-paid-for answer visible to the
renderer instead of throwing it away.

Output: a widened wire type, a widened handler, and four updated test files whose
deep-equality / typed-literal assertions this widening would otherwise break silently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-CONTEXT.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-PATTERNS.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-VALIDATION.md

<settled_findings>
Do NOT re-litigate these. Each was verified against HEAD by RESEARCH.md and independently
re-confirmed by PATTERNS.md and by this plan's own authoring session:

1. **Quick-install additivity is CONFIRMED SAFE, not assumed.** `startSteamQuickInstall`
   (`state/InstallGameModal.ts`) never constructs or reads a `SteamBottleEligibilityVerdict`;
   it routes through the plain-`boolean` `checkBottleEligibility()` / `isBottleEligible()`
   seam inside `SteamGame`. Grep of `InstallGameModal.ts` for
   `checkBottleEligibility`/`isBottleEligible` returns zero matches. Widening the verdict
   cannot disturb that path.
2. **`checkBottleEligibility()`'s `Promise<boolean>` signature does NOT change.** That is
   precisely why (1) holds. Read `steamMetadataStore` directly, after the await.
3. **`games.ts` needs NO changes at all.** Verify, do not touch.
4. **The two new fields are REQUIRED, not optional.** D-03 says "two booleans". Required is
   what makes the compiler force every construction site — including the rejected-appName
   early return — to supply them.
5. **ts-jest in this repo is TRANSPILE-ONLY.** A type error does NOT fail `jest`. Type-level
   pins are only enforced by `pnpm typecheck` / `tsc --noEmit`. Run it; do not assume a green
   jest run means the types are right.
</settled_findings>

<interfaces>
Current state at HEAD (`src/common/types/steam.ts:55-59`):

```typescript
export interface SteamBottleEligibilityVerdict {
  eligible: boolean
  wineVersion?: WineInstallation
  bottleName?: string
}
```

Current handler at HEAD (`installFormIpc.ts:66-95`), abbreviated:
- rejected-appName early return at `:84` is `return { eligible: false }`
- `const eligible = await new SteamGame(appName).checkBottleEligibility()`
- two `steamBottleConfigStore.get_nodefault(...)` reads
- `return { eligible, wineVersion, bottleName }`

`steamMetadataStore` is exported from `./electronStores` (same module `steamBottleConfigStore`
already comes from) and is NOT currently imported by `installFormIpc.ts`.
`SteamMetadataCacheEntry` declares `is_windows_native?: boolean` (`electronStores.ts:55`) and
`platformsCaptured?: boolean` (`electronStores.ts:102` — note: NOT in CONTEXT.md's cited
27-70 range).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen SteamBottleEligibilityVerdict with two required booleans and repair every typed literal</name>
  <files>src/common/types/steam.ts, src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts, src/backend/sidecar/__tests__/steamAuthFlows.test.ts, src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts</files>
  <read_first>
    - `src/common/types/steam.ts` (the file being modified — read lines 45-65 for the existing doc-comment convention)
    - `.planning/phases/34.14-.../34.14-PATTERNS.md` § `src/common/types/steam.ts` (the exact current text and the doc-comment convention to extend)
    - `src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts` lines 184-226 (the three typed literals + the A-07 compile-pin idiom)
    - `src/backend/sidecar/__tests__/steamAuthFlows.test.ts` lines 683-745 (two `jest.mocked(...).mockResolvedValue({...})` verdict literals)
    - `src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts` lines 155-200 (four typed verdict literals)
  </read_first>
  <action>
Add two REQUIRED fields to `SteamBottleEligibilityVerdict` in `src/common/types/steam.ts`:
`hasWindowsDepot: boolean` and `platformsCaptured: boolean`. Place them after `eligible` and
before the two optional fields. Give each a doc-comment following the existing convention in
that interface's header (cite the decision ID and the round-trip rationale inline, the way
`wineVersion`/`bottleName` are documented):
  - `hasWindowsDepot` — "D-03 (34.14): the backend's `steamMetadataStore` read of
    `is_windows_native === true`, captured AFTER `checkBottleEligibility()` has already
    awaited `ensurePlatformsCaptured()`. MEANINGLESS on its own: this field must never be
    read without `platformsCaptured === true` also holding — see D-03's single-seam
    obligation, discharged by read order inside `resolveSteamSectionGating`."
  - `platformsCaptured` — "D-03 (34.14): whether the appdetails fetch actually landed.
    `false` covers offline, an errored fetch, AND the `METADATA_FETCH_TIMEOUT_MS` expiry —
    `ensurePlatformsCaptured()` returns unconditionally at its deadline, so without this
    field a timed-out probe is indistinguishable from a confirmed absent Windows depot,
    which is the exact defect Phase 34.14 exists to end."

Then repair every typed construction site the widening breaks. Exact list (all confirmed at
HEAD; there are no others — the exhaustive consumer search is in PATTERNS.md § Verification
Predicates):
  - `steamInstallFormContracts.test.ts:192` — `const minimal: SteamBottleEligibilityVerdict = { eligible: true }`.
    The enclosing `it()` title at `:191` asserts the literal "accepts `{ eligible: true }` alone",
    which this phase deliberately makes FALSE. Retitle it to state the post-34.14 contract
    ("requires eligible, hasWindowsDepot and platformsCaptured; wineVersion/bottleName stay
    optional") and add both fields to the literal.
  - `steamInstallFormContracts.test.ts:196-208` — the `full` literal: add both fields.
  - `steamInstallFormContracts.test.ts:222` — `const minimal: ... = { eligible: false }`: add both fields.
  - `steamAuthFlows.test.ts:687-689` and `:740-741` — the two
    `jest.mocked(getSteamBottleEligibilityVerdict).mockResolvedValue({...})` literals: add both
    fields. Do NOT touch the `toMatchObject` expectations below them — `toMatchObject` is a
    subset match and stays correct as written.
  - `steamEligibilityProbe.test.ts:163,164,188,195` — four typed verdict literals: add both fields.

Add ONE new compile pin in `steamInstallFormContracts.test.ts`, immediately after the existing
`A-07 COMPILE PIN` spec, following that spec's exact `@ts-expect-error` idiom: a literal
supplying only `{ eligible: true }` must be a type error. Name it so its intent is unambiguous
("34.14 COMPILE PIN: hasWindowsDepot and platformsCaptured are REQUIRED — an optional pair
would let a construction site silently reintroduce the undefined-vs-false collapse").

Do NOT change `src/common/types/ipc.ts:301` — it references the type by name and needs no edit.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "steam\.ts|steamInstallFormContracts|steamAuthFlows|steamEligibilityProbe" ; test $? -ne 0</automated>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'hasWindowsDepot: boolean' src/common/types/steam.ts` returns `1`; `grep -c 'platformsCaptured: boolean' src/common/types/steam.ts` returns `1`. Neither is declared with a `?`: `grep -c 'hasWindowsDepot?:' src/common/types/steam.ts` returns `0`.
    - `npx tsc --noEmit` reports ZERO errors in `src/common/types/steam.ts`, `steamInstallFormContracts.test.ts`, `steamAuthFlows.test.ts`, `steamEligibilityProbe.test.ts`. (RED proof for this criterion: before adding the fields to the six test literals, `tsc --noEmit` MUST report errors in at least `steamInstallFormContracts.test.ts` and `steamAuthFlows.test.ts` — capture that output in the SUMMARY. An assertion never proven red is not a gate.)
    - The new `@ts-expect-error` compile pin is present and `tsc --noEmit` does NOT report an "unused '@ts-expect-error' directive" for it — that error appearing is itself the proof the fields became optional again.
    - `steamInstallFormContracts.test.ts` contains ZERO occurrences of the string `accepts { eligible: true } alone` (the retired, now-false claim).
    - `npx jest src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts` exits 0.
  </acceptance_criteria>
  <done>The wire type carries two required booleans; every typed literal in the repo compiles; a compile pin fails the moment either field is made optional.</done>
</task>

<task type="auto">
  <name>Task 2: Populate the two booleans in the ONE shared handler body</name>
  <files>src/backend/storeManagers/steam/installFormIpc.ts, src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts</files>
  <read_first>
    - `src/backend/storeManagers/steam/installFormIpc.ts` (full file — the file being modified)
    - `.planning/phases/34.14-.../34.14-PATTERNS.md` § `installFormIpc.ts` (exact current body, import block, and header-comment convention)
    - `src/backend/storeManagers/steam/electronStores.ts` lines 27-30, 45-60, 95-130 (the `steamMetadataStore` export and the two field contracts)
    - `src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts` lines 1-60 (the `jest.mock('../electronStores', ...)` factory) and lines 113-160 (the two `toEqual({ eligible: false })` assertions)
    - `src/backend/storeManagers/steam/games.ts` lines 1628-1631 and 1655-1681 (`checkBottleEligibility` / `ensurePlatformsCaptured` — read to CONFIRM they need no change, per settled finding 3)
  </read_first>
  <action>
In `installFormIpc.ts`:

1. Add `steamMetadataStore` to the existing `import { steamBottleConfigStore } from './electronStores'`
   line. It is exported from that same module's bottom export block.

2. Widen the rejected-appName early return at `:84` from `return { eligible: false }` to
   `return { eligible: false, hasWindowsDepot: false, platformsCaptured: false }`. Add a
   comment above it stating exactly this: an invalid `appName` is a REJECTED REQUEST, not an
   "unknown depot" state — `eligible: false` short-circuits routing before the depot question
   is ever asked, so this `platformsCaptured: false` must NOT be read as a D-04 fail-open
   trigger. Without that note a future reader will assume every `platformsCaptured: false`
   return means "offer Windows".

3. After the existing `const eligible = await new SteamGame(appName).checkBottleEligibility()`
   line, add a `steamMetadataStore` read in the same shape as the two
   `steamBottleConfigStore.get_nodefault(...)` lines directly below it:
   `const cached = steamMetadataStore.get(appName)`, then
   `const hasWindowsDepot = cached?.is_windows_native === true` and
   `const platformsCaptured = cached?.platformsCaptured === true`. Both comparisons are
   `=== true` — `undefined` means "never captured" on BOTH fields and must never coerce.
   Add a comment stating that no new fetch and no new await occur here: the `checkBottleEligibility()`
   line above has ALREADY driven `ensurePlatformsCaptured()` to completion or to its
   `METADATA_FETCH_TIMEOUT_MS` deadline (D-02's entire mechanism), so this is a read of an
   already-paid-for result.

4. Return `{ eligible, hasWindowsDepot, platformsCaptured, wineVersion, bottleName }`.

5. Extend the module header comment (lines 1-9) with one sentence recording that 34.14 widened
   this body with the depot pair, and that the widening lands on BOTH runtimes at once
   precisely because this file is never forked.

In `installFormIpc.test.ts`:

6. Extend the `jest.mock('../electronStores', ...)` factory to also export
   `steamMetadataStore: { get: jest.fn() }`. Establish its default return in the existing
   `beforeEach` (the file's own header notes `resetMocks: true`, so implementations must be
   re-established per test).

7. Update BOTH `expect(result).toEqual({ eligible: false })` assertions (`:137` in the
   `T-34.13-07-01` non-numeric block, `:154` in the `WR-10` non-string block) to the full new
   shape `{ eligible: false, hasWindowsDepot: false, platformsCaptured: false }`. These are
   deep-equality assertions and are mandatory edits, not optional.

8. Add a new `describe('34.14 D-02/D-03: the depot pair is READ from steamMetadataStore, never
   hardcoded')` block with three specs:
   - captured cache entry `{ is_windows_native: true, platformsCaptured: true }` →
     verdict `hasWindowsDepot: true, platformsCaptured: true`.
   - captured-and-absent entry `{ is_windows_native: false, platformsCaptured: true }` →
     `hasWindowsDepot: false, platformsCaptured: true` (34.13's fence — the genuinely-absent case).
   - absent/uncaptured entry (`steamMetadataStore.get` returns `undefined`) →
     `hasWindowsDepot: false, platformsCaptured: false` (the timeout/offline case).
   These three together are the RED proof required by VALIDATION.md: an implementation that
   hardcodes either field cannot make all three pass, because specs 1 and 3 differ on both fields.
  </action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "steamMetadataStore" src/backend/storeManagers/steam/installFormIpc.ts` returns at least `2` (import + read).
    - `grep -v '^\s*[*/]' src/backend/storeManagers/steam/installFormIpc.ts | grep -c "return { eligible: false }"` returns `0` — the un-widened early return is gone. (Comment lines are filtered because `installFormIpc.ts:45`'s prose legitimately quotes that literal; an unfiltered count would be self-invalidating.)
    - `npx jest src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts` exits 0 with the three new specs present in the output.
    - RED proof, captured in the SUMMARY: with the `steamMetadataStore` read replaced by hardcoded `false` literals, the new captured-entry spec FAILS. Run it, record the failure text, then restore.
    - `git diff --name-only` shows ZERO changes to `src/backend/storeManagers/steam/games.ts` — settled finding 3, verified rather than assumed.
    - `checkBottleEligibility` still declares `Promise<boolean>`: `grep -c "async checkBottleEligibility(): Promise<boolean>" src/backend/storeManagers/steam/games.ts` returns `1`.
  </acceptance_criteria>
  <done>The handler returns the depot pair read from the metadata store; the three new specs prove the fields track the store rather than a constant; `games.ts` is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the no-fork invariant as a checkable predicate</name>
  <files>src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts</files>
  <read_first>
    - `src/backend/main.ts` lines 55-65 and 935-955 (the Electron import + registration)
    - `src/backend/sidecar/steamAuthFlowRegistration.ts` lines 125-135 and 292-305 (the Tauri import + dispatch — this is the REAL Tauri import site, not "sidecar main.ts")
    - `.planning/phases/34.14-.../34.14-PATTERNS.md` § Verification Predicates (the confirmed call-site inventory)
  </read_first>
  <action>
Do not edit `main.ts` or `steamAuthFlowRegistration.ts` — the widening reaches both by
construction. Instead, make the no-fork invariant CHECKABLE and record the evidence.

Run and record in the SUMMARY:
`grep -rn "export async function getSteamBottleEligibilityVerdict" src/ | wc -l` — must be
exactly `1`, and the single hit must be `src/backend/storeManagers/steam/installFormIpc.ts`.

Then add a permanent guard rather than a one-off check. Extend
`src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts` with a
`describe('D-02: the shared handler body is never forked')` block containing one spec that
reads the repo source (follow the `readFileSync` + `stripSourceComments` idiom already used
by `steamInstallFormContracts.test.ts`, importing `stripSourceComments` from
`backend/testUtils/stripSourceComments`) and asserts:
  - exactly ONE file under `src/` contains `export async function getSteamBottleEligibilityVerdict`,
    and it is `installFormIpc.ts`;
  - `main.ts` and `steamAuthFlowRegistration.ts` each contain the IDENTIFIER
    `getSteamBottleEligibilityVerdict` but NOT the `export async function` definition of it.
Pair it with a non-vacuity spec that runs the same matcher against an inline specimen
containing a second `export async function getSteamBottleEligibilityVerdict` and asserts the
matcher counts 2 — proving the gate bites.
  </action>
  <verify>
    <automated>npx jest src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "export async function getSteamBottleEligibilityVerdict" src/ | wc -l` outputs `1`.
    - The new no-fork spec and its non-vacuity partner both appear in the jest output and pass.
    - The non-vacuity spec is proven: it asserts a count of `2` against a hand-built specimen containing a second definition. A gate that only ever sees the real (count-1) source cannot fail and is not a gate.
    - `git diff --stat` shows ZERO lines changed in `src/backend/main.ts` and `src/backend/sidecar/steamAuthFlowRegistration.ts`.
  </acceptance_criteria>
  <done>The single-body invariant is enforced by a test with a proven-non-vacuous matcher, not by a comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → backend (`isSteamBottleEligible` IPC) | `appName` is untrusted renderer input crossing into `new SteamGame(...)` and template-literal paths |
| backend → renderer (widened verdict) | Two new booleans flow OUTWARD; not a privilege-escalation direction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.14-01 | Tampering | `getSteamBottleEligibilityVerdict(appName)` | mitigate | Pre-existing and UNCHANGED: the `typeof appName !== 'string'` + `NUMERIC_APP_ID` guard runs BEFORE any widened logic. Task 2 explicitly widens the rejected-appName early return rather than moving or relaxing the guard. `installFormIpc.test.ts`'s existing `T-34.13-07-01` / `WR-10` specs continue to prove no `SteamGame` is constructed on rejection. |
| T-34.14-02 | Information Disclosure | the widened verdict's two new fields | accept | Both are booleans derived from public Steam store `appdetails` platform data already cached on disk. Neither carries a SteamID64, a token, or a path, so the standing Steam-identity redact-forward rule has no new surface here. Task 2 adds NO new log statement; the only `logWarning` in this function is the pre-existing rejection notice, which logs no appName value. |
| T-34.14-03 | Tampering | a hostile/malformed `appdetails` response driving `platformsCaptured` | accept | An attacker able to forge `appdetails` can at worst force `platformsCaptured: false`, which drives D-04's fail-open in a LATER plan — offering a Windows/wine install option the user must still explicitly select. It cannot force an install, cannot select a path, and cannot make a destructive action happen without a further deliberate user action against an already-disabled-until-settled Install button (D-01). Forcing `platformsCaptured: true` with `is_windows_native: true` yields the same offer. No branch here writes to disk or spawns a process. |
| T-34.14-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this phase installs ZERO packages (RESEARCH.md § Package Legitimacy Audit: "Not applicable"). No slopcheck checkpoint is required because no install task exists. If any task in this plan ever adds one, that is a scope violation to be raised, not executed. |
</threat_model>

<verification>
- `npx jest src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts src/backend/storeManagers/steam/__tests__/steamInstallFormContracts.test.ts` exits 0.
- `npx tsc --noEmit` reports no errors in any file this plan touched.
- `git diff --name-only` matches this plan's `files_modified` exactly — no `games.ts`, no `main.ts`, no `steamAuthFlowRegistration.ts`, no frontend file.
</verification>

<success_criteria>
- `SteamBottleEligibilityVerdict` carries `hasWindowsDepot: boolean` and `platformsCaptured: boolean`, both required, both documented with their decision IDs.
- `getSteamBottleEligibilityVerdict` populates both from `steamMetadataStore` after the existing await, with no added fetch and no added await.
- Three new backend specs prove the pair tracks the store (captured/true, captured/false, uncaptured) — the RED proof against a hardcoded implementation is recorded.
- The no-fork invariant is a passing, non-vacuous test rather than a claim.
</success_criteria>

<output>
Create `.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-01-SUMMARY.md` when done.
Record: the RED-proof outputs (tsc errors before the literal repairs; the hardcoded-implementation
failure), the no-fork grep count, and confirmation that `games.ts` is untouched.
</output>
