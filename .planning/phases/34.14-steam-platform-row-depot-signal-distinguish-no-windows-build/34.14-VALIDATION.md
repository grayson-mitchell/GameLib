---
phase: 34.14
slug: steam-platform-row-depot-signal-distinguish-no-windows-build
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-16
---

# Phase 34.14 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `34.14-RESEARCH.md` § Validation Architecture (line 687+).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29 + `ts-jest` (multi-project root config) |
| **Config file** | `jest.config.js` — `projects: ['src/backend', 'src/common', 'src/frontend', 'src/preload', 'meta']`. `src/frontend/jest.config.js` is `testEnvironment: 'node'` — **no jsdom, no CSS transform** |
| **Quick run command** | `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamPlatformRow.test.ts src/frontend/screens/Library/components/InstallModal/__tests__/steamSectionGating.test.ts src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts` |
| **Full suite command** | `pnpm test:ci` (`jest --runInBand --silent`) |
| **Estimated runtime** | quick ~15s · full suite several minutes |

**Test-seam constraint (BLOCKING, house pattern).** `index.tsx`'s first line is
`import './index.scss'` and the Frontend jest project has no CSS transform — any test
importing from `index.tsx` dies at that import. **All new pure logic MUST live in
`steamSectionGating.ts`, `steamPlatformRow.ts`, `steamEligibilityProbe.ts`, or a new
sibling module following the same zero-runtime-import convention.** Logic inlined in
`index.tsx` cannot be tested at all.

---

## Sampling Rate

- **After every task commit:** Run the quick run command above
- **After every plan wave:** Run `pnpm test:ci`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~15 seconds (quick), full suite at wave boundaries

---

## Per-Task Verification Map

> Populated by the planner. Every task must map to an automated command **or** be
> listed under Manual-Only Verifications below with a stated reason.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _TBD — planner fills_ | | | | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## What Must Be Proven

Three branches, from CONTEXT.md `D-04` and `D-01`:

| # | Condition | Required outcome | Status today |
|---|-----------|------------------|--------------|
| **(a)** | `platformsCaptured === true && hasWindowsDepot !== true` | Windows **OMITTED** — 34.13's fence, byte-identical | Already proven; must stay proven |
| **(b)** | `platformsCaptured === false`, probe terminally settled | Windows **OFFERED** (fail open) | **NEW — the defect being fixed** |
| **(c)** | Depot signal unresolved | Install **DISABLED**, joining `eligibility.pending` (34.13 `D-25`), not a second mechanism | Mechanism exists (`index.tsx:479`); the *co-occurrence* is unproven |

For (c), the test must prove `platformRow === 'pending'` and `eligibility.pending === true`
hold **simultaneously** for the uncaptured-seed case — i.e. that they are the same window,
not two windows that can desync.

---

## Matrix Expansion

`steamSectionGating.test.ts` currently sweeps **96 combinations across 8 rows**. Two valid
expansions — **the planner MUST pick one and state it in the plan**:

1. **Naive doubling** — `platformsCaptured` as a uniform 6th axis → **192 combinations,
   9 rows**. Includes the meaningless `(hasWindowsDepot: true, platformsCaptured: false)`
   combination in the primary sweep (48 of 192).
2. **Recommended — exclude the meaningless combination** from the generator wherever
   `hasWindowsDepot: true` → **144 combinations, 9 rows**, plus a
   `describe('defensive normalisation…')` block (matching the file's existing pattern at
   `:654`) proving the excluded combination still resolves safely — `platformRow` stays
   `'pending'` and never leaks to `'selectable'` off a stale `hasWindowsDepot: true` alone.

Either way, the `ALL_COMBINATIONS.length` assertion (`steamSectionGating.test.ts:327-338`,
currently pinning exactly 96) must be updated, and the per-platform sub-counts
(currently darwin/win32/linux = 48/24/24) recomputed and re-asserted.

---

## The RED Proof

No assertion counts as a gate until it has been **proven to fail against a known-bad input**.

| Assertion | Known-bad input it MUST fail against |
|-----------|--------------------------------------|
| **Row 9 (pending)** — the phase's single most important RED proof | **Today's shipped, unmodified `resolveSteamSectionGating`.** Run the new Row 9 test against HEAD *before* any code change and confirm it FAILS. If it passes against current code, it proves nothing. |
| `hasSteamWindowsDepot` saboteurs | `treatsAbsentAsAvailable` / `includesWindowsRegardless` / `readonlyRowLeaksDepot` must keep failing it **exactly as today, unmodified**. These staying green with the same saboteurs is the *negative proof that the fix landed at the right layer* — per RESEARCH.md OQ2, `steamPlatformRow.ts`'s depot function needs **zero changes**. |
| Widened `getSteamBottleEligibilityVerdict` | An implementation that omits `platformsCaptured` from the returned object — proving the field is genuinely read from `steamMetadataStore` and not hardcoded. Assert it differs between a mocked captured cache entry and a mocked uncaptured/absent one. |

**Known test breakage to fix (found by research, absent from CONTEXT.md):**
`installFormIpc.test.ts:137,154` assert `toEqual({ eligible: false })` — deep equality that
breaks the moment the verdict widens. These are on the phase's mandatory test-touch list.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The cold-cache window actually renders | `D-08` | No pure-function test proves a real freshly-opened dialog against a real uncaptured library renders the pending row, disables the real Install button, and resolves. Same situation 34.13-13's blocking manual gate already covers. | See forcing procedure below |
| Fail-open under network failure | `D-04` | Requires a real terminal probe failure (offline / blocked `appdetails`), not a mocked one | See forcing procedure below, run 2 |

### UAT forcing procedure (D-08 — verified executable)

The cold-cache window is **not reproducible on demand from a warm library**, so the gate
must force it:

1. Quit GameLib completely.
2. Edit `~/Library/Application Support/GameLib/store_cache/steam_metadata.json` and delete
   **one** appId key (targets a single game so the rest of the library stays warm; ships no
   test seam in product code). Electron and Tauri share this identical file.
3. Relaunch, open that game's **"Install with options…"**.

- **Run 1 — network UP:** expect pending row → resolves captured → Windows entry appears,
  Install enables.
- **Run 2 — network DOWN / `appdetails` blocked:** expect pending row → resolves
  uncaptured → **fail open, Windows offered anyway** (D-04).

**Note:** a single probe fires at open — **reconnecting mid-dialog does not re-probe.** Do
not author a gate step that assumes it does.

### Both runtimes

`installFormIpc.ts` is ONE shared handler body imported by both Electron (`main.ts:944`)
and the Tauri sidecar, deliberately so the two are "physically incapable of drifting."
Jest covers the shared body once. The manual gate should be exercised under **both**
runtimes, or the plan must state explicitly why one suffices.

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements — Jest 29 + `ts-jest` is configured,
and all three target test files already exist:
- `src/frontend/screens/Library/components/InstallModal/__tests__/steamPlatformRow.test.ts`
- `src/frontend/screens/Library/components/InstallModal/__tests__/steamSectionGating.test.ts`
- `src/backend/storeManagers/steam/__tests__/installFormIpc.test.ts`

No framework install needed. No Wave 0 stubs required.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or appear under Manual-Only Verifications
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Row 9 test proven RED against unmodified HEAD **before** the fix lands
- [ ] `ALL_COMBINATIONS.length` and per-platform sub-counts updated to the chosen total
- [ ] `installFormIpc.test.ts:137,154` deep-equality assertions updated
- [ ] `steamPlatformRow.test.ts` saboteurs unchanged and still failing their targets
- [ ] No watch-mode flags
- [ ] Feedback latency < 20s (quick run)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
