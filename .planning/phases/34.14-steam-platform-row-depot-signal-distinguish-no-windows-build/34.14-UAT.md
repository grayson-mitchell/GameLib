---
phase: 34.14-steam-platform-row-depot-signal-distinguish-no-windows-build
plan: 05
gate: D-08
status: human_needed
target_game_name: "TBD — fill in Step 0.2"
target_game_appid: "TBD — fill in Step 0.3"
target_game_genuinely_has_windows_build: "TBD — fill in Step 0.2 (yes/no/unsure — needed to classify a 'Windows absent' result correctly)"
created: 2026-08-16
filled_by: "human (developer), not the executor — this file is pre-created empty per the checkpoint task's own instruction: 'The human fills cells; they do not author the ledger.'"
---

# Phase 34.14 — D-08 Blocking UAT Ledger

**Do not self-certify.** This ledger exists to record what was actually observed on screen
under four forced runs (Electron × network-up/blocked, Tauri × network-up/blocked) — not what
the plans predicted. Follow the full forcing procedure in
`34.14-05-PLAN.md`'s Task 2 `<how-to-verify>` block verbatim (steps 0-32). Executor readiness
checks already confirmed both launch paths build and start cleanly on this working tree
(see "Executor pre-flight" below) — you do not need to verify that yourself.

**Executor pre-flight (done 2026-08-16, before this file was created):**
- `npm start`: launched cleanly, reached `Frontend Ready`, Steam library synced (378 games from
  cache), then fully quit. One pre-existing, unrelated error observed and NOT investigated (out
  of this phase's scope): `getCrossoverIndex` has no registered IPC handler under Electron —
  logged here for completeness, not a blocker for this gate.
- `pnpm tauri:dev`: full pipeline (electron-vite build → `build:sidecar` → `cargo build` →
  launch) completed cleanly — Rust compiled in 4.93s, binary ran. **IMPORTANT:** a GameLib Tauri
  instance was ALREADY RUNNING on this machine before this session started (`target/debug/
  gamelib-shell`, PID 67010, up since before this session). The app's own single-instance guard
  correctly detected it and this readiness-check invocation exited immediately ("another GameLib
  instance is already running — sending focus sentinel to it and exiting") rather than opening a
  second window. **Before you begin Run 1, fully quit that pre-existing instance (and any other
  running GameLib/Electron window) — the forcing procedure requires the app to be FULLY QUIT
  before every cache edit, and a stray already-running instance will silently defeat Step A.**
- Neither readiness check touched `~/Library/Application Support/GameLib/store_cache/
  steam_metadata.json`, `/etc/hosts`, or any other file this ledger's forcing procedure edits.

**Never paste into the Tauri DevTools console** — it is unusable there. All observations below
are made by looking at the running app, not by running code in it.

---

## Target Game

| Field | Value |
|---|---|
| Game name | Terraria |
| Steam AppID | 105600 |
| Does it genuinely ship a Windows build? (yes / no / unsure) | **yes** — Terraria ships Windows, macOS and Linux builds. Confirmed independently from disk during Run 1: after Step A forced a cold refetch, the rewritten cache entry read `is_windows_native: true, is_mac_native: true, is_linux_native: true`. So an absent-Windows observation on ANY run is unambiguously a defect, never a property of the game. |

### Pre-existing cache condition found before Run 1 (recorded, not a phase regression)

Before any forcing edit, all 380 cached entries were surveyed. **370 had `platformsCaptured: true`
and ZERO had `is_windows_native` at all** — including Terraria's. That combination is the worst
case for the row: the signal reads as *captured* (so D-04's fail-open correctly does NOT engage)
while `hasSteamWindowsDepot` is false (so Windows is omitted). This is pre-D-17 residue written by
an older build — the current `games.ts:704` write path does persist `is_windows_native`, and the
Run 1 refetch proved it. Flagged because it means existing users carry a cache state that this
phase's fail-open deliberately does not rescue; it needs a migration or a cache-version bump, which
is OUT OF SCOPE here and should be filed separately.

---

## Result Ledger

Mark each `Result` cell `pass`, `fail`, or `blocked`. Leave nothing as `pending` when you finish —
if a step could not be attempted, mark it `blocked` and say why in Notes, do not leave it empty.

### Run 1 — Electron, network UP

| # | Observation | Expected | Result (pass/fail/blocked) | Notes |
|---|---|---|---|---|
| 1.1 | "Checking install options…" row appears when the dialog opens | Row appears | **blocked** | Row never appeared. NOT a fail — the cold-cache window closed before the dialog could be opened. Step A deleted entry `105600` with the app fully quit, but the appdetails refetch completed during library sync / game-page navigation, so the dialog opened against a WARM cache and the `'pending'` state was never entered. Verified from disk, not inferred: immediately after this run the entry was back with `is_windows_native: true, platformsCaptured: true, mac_arch: 'unknown'`. On a healthy connection this window is sub-perceptual. Pending-state evidence must come from Runs 2/4, where the fetch burns the full 15s timeout. |
| 1.2 | Install button disabled while that row is showing | Disabled | **blocked** | Unarbitrated — depends on 1.1's row, which never rendered. Install was enabled immediately. Deliberately NOT recorded as pass: a pass here would certify a transition that never occurred. |
| 1.3 | Within ~20s, the row disappears and the Platform selector becomes usable | Settles within ~20s | **blocked** | Unarbitrated for the same reason as 1.2 — there was no un-settled state to settle FROM. The selector was usable from the instant the dialog opened. |
| 1.4 | Platform selector offers Windows as well as macOS after settling | Windows offered (if this game genuinely ships one — see Target Game table) | **pass** | Operator observed Windows selectable in the Platform selector. Corroborated from disk: the refetched cache entry carries `is_windows_native: true`. |
| 1.5 | Install button becomes enabled after settling | Enabled | **pass** | Operator could install straight away; Install was enabled. (Enabled-throughout rather than disabled→enabled, per 1.2.) |
| 1.6 | Exactly ONE "Checking install options…" row is ever shown at a time (no duplicate) | No duplicate row | **blocked** | Operator confirmed no duplicate row — but also no row at all, so the "exactly one at a time" property is vacuously satisfied and arbitrates nothing. Recorded as blocked rather than pass on purpose. Carried to Runs 2/4. |
| 1.7 | Dialog never asserts, in words, that this game is macOS-only | No such copy anywhere in the dialog | **pass** | Operator confirmed no such copy. This is a GENUINE pass, not vacuous: the dialog rendered fully and completely, so the copy would have been visible had it existed. D-07 satisfied on the Electron/network-up path. |

### Run 2 — Electron, `appdetails` BLOCKED

| # | Observation | Expected | Result (pass/fail/blocked) | Notes |
|---|---|---|---|---|
**Blocking method used (differs from the plan, deliberately — see Procedure Defects below):** only
`store.steampowered.com` was blocked, NOT `api.steampowered.com`. The plan's dual block took out
`steam-user`'s CM login and emptied the entire Steam library, making the target game unreachable.
Confirmed at dispatch: `store.steampowered.com` → `127.0.0.1`, curl exit 7; `api.steampowered.com`
→ real IP, HTTP 200; Steam library synced 378 games. Independently corroborated from the app log
that the depot fetch was attempted for the target and failed:
`(11:19:51) [WARNING] [Steam]: Steam metadata fetch failed for appId 105600: Error: connect ECONNREFUSED 127.0.0.1:443`

| # | Observation | Expected | Result (pass/fail/blocked) | Notes |
|---|---|---|---|---|
| 2.0 | *(added during the run — not anticipated by the ledger)* Platform icons in the dialog's game title, and the default selected platform | — | **finding, not pass/fail** | Title showed the **Windows symbol ONLY** (Run 1, warm cache, showed Apple + Windows), and the selector **defaulted to Windows**; macOS remained manually selectable. Fully explained: Step A deletes the ENTIRE cache entry, so `is_mac_native` is destroyed along with `is_windows_native`. `getDefaultplatform()` (`index.tsx:240-246`) is `if (isMac && gameInfo?.is_mac_native) return 'Mac'; return 'Windows'` → absent mac flag falls through to Windows; the icon row uses `Boolean(gameInfo?.is_mac_native)` (`index.tsx:165`) → no Apple icon. **This is the MIRROR IMAGE of the bug this phase fixed:** 34.14 taught the code that absent `is_windows_native` ≠ "no Windows build", but nothing teaches it that absent `is_mac_native` ≠ "no Mac build", so the title row now asserts *Windows-only* — a claim the app cannot support. Pre-existing logic, newly visible. Filed as a separate todo; NOT counted as a 34.14 gap. |
| 2.1 | "Checking install options…" row appears when the dialog opens | Row appears | **blocked** | Row did not appear. NOT a fail, and NOT the same cause as Run 1: here the fetch DID fire and DID fail (log line above), but `/etc/hosts` → `127.0.0.1` makes the connection **refused in milliseconds** rather than hanging, so the 15s `METADATA_FETCH_TIMEOUT_MS` path is never taken and the pending window stays sub-perceptual. This is a defect in the plan's blocking METHOD, not in the code. Needs a blackhole address (e.g. TEST-NET-3 `203.0.113.1`) that drops packets silently instead of refusing. See Procedure Defects. |
| 2.2 | Install button disabled while that row is showing | Disabled | **blocked** | Unarbitrated — depends on 2.1's row, which never rendered. Install was enabled from the start. Not recorded as pass: that would certify a transition that never occurred. |
| 2.3 | Within ~20s (the metadata fetch times out at 15s), it settles | Settles within ~20s | **blocked** | Operator reports everything settled in **<1 second**. Consistent with instant ECONNREFUSED, not with the 15s timeout path. There was no un-settled state to settle from, so this arbitrates nothing. |
| 2.4 | **KEY OBSERVATION (D-04 fail-open):** after it settles, is Windows STILL offered in the Platform selector? | Windows OFFERED — this is the phase's headline behaviour; if absent here, the gate FAILS | **pass** | **Windows offered.** This is the phase's headline behaviour confirmed on real hardware: the depot fetch demonstrably failed (log line above), so `platformsCaptured` was `false`, and D-04's fail-open engaged and offered Windows rather than concluding "no Windows build". Strengthened by the Run 1 contrast — the same game with a warm, captured cache also offered Windows, so the fail-open path and the captured path agree. |
| 2.5 | Install button becomes enabled after settling | Enabled | **pass** | Install enabled and usable. (Enabled-throughout rather than disabled→enabled, per 2.2.) |
| 2.6 | Exactly ONE "Checking install options…" row is ever shown at a time (no duplicate) | No duplicate row | **blocked** | Operator marked n/a — no row ever rendered, so "exactly one at a time" is vacuously satisfied and arbitrates nothing. Same disposition as Run 1. |
| 2.7 | Dialog never asserts, in words, that this game is macOS-only | No such copy anywhere in the dialog | **pass** | Operator confirmed no macOS-only wording. GENUINE pass, not vacuous: the dialog rendered fully, so the copy would have been visible had it existed. D-07 satisfied on the Electron/blocked path — and this is the run where such copy was MOST likely to appear, since the depot signal was genuinely absent. |

### Run 3 — Tauri, network UP

| # | Observation | Expected | Result (pass/fail/blocked) | Notes |
|---|---|---|---|---|
| 3.1 | "Checking install options…" row appears when the dialog opens | Row appears | pending | |
| 3.2 | Install button disabled while that row is showing | Disabled | pending | |
| 3.3 | Within ~20s, the row disappears and the Platform selector becomes usable | Settles within ~20s | pending | |
| 3.4 | Platform selector offers Windows as well as macOS after settling | Windows offered (if this game genuinely ships one — see Target Game table) | pending | |
| 3.5 | Install button becomes enabled after settling | Enabled | pending | |
| 3.6 | Exactly ONE "Checking install options…" row is ever shown at a time (no duplicate) | No duplicate row | pending | |
| 3.7 | Dialog never asserts, in words, that this game is macOS-only | No such copy anywhere in the dialog | pending | |

### Run 4 — Tauri, `appdetails` BLOCKED

| # | Observation | Expected | Result (pass/fail/blocked) | Notes |
|---|---|---|---|---|
| 4.1 | "Checking install options…" row appears when the dialog opens | Row appears | pending | |
| 4.2 | Install button disabled while that row is showing | Disabled | pending | |
| 4.3 | Within ~20s (the metadata fetch times out at 15s), it settles | Settles within ~20s | pending | |
| 4.4 | **KEY OBSERVATION (D-04 fail-open):** after it settles, is Windows STILL offered in the Platform selector? | Windows OFFERED — this is the phase's headline behaviour; if absent here, the gate FAILS | pending | |
| 4.5 | Install button becomes enabled after settling | Enabled | pending | |
| 4.6 | Exactly ONE "Checking install options…" row is ever shown at a time (no duplicate) | No duplicate row | pending | |
| 4.7 | Dialog never asserts, in words, that this game is macOS-only | No such copy anywhere in the dialog | pending | |

---

## D-01 cross-check (Install-disable co-occurs with the pending row)

For all four runs above, confirm rows `*.2` (Install disabled while checking) and `*.1`
("Checking install options…" row visible) were observed to hold **simultaneously**, not as two
separate windows that could desync. State this explicitly per run:

| Run | Install-disable and pending-row co-occurred simultaneously? | Notes |
|---|---|---|
| 1 (Electron, up) | pending | |
| 2 (Electron, blocked) | pending | |
| 3 (Tauri, up) | pending | |
| 4 (Tauri, blocked) | pending | |

---

## Cleanup Confirmation (mandatory — do not skip)

| Step | Done? |
|---|---|
| `/etc/hosts` block (the two appended lines) removed | pending |
| DNS cache flushed after removal (`sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder`) | pending |
| `steam_metadata.json` restored from `~/Desktop/steam_metadata.backup.json` | pending |
| Relaunched once afterward and confirmed the library renders normally (no lingering forced-cache-miss effects) | pending |

---

## Overall Verdict

**Status:** `human_needed` (unchanged until every row above is marked and this section is filled in by the developer).

Set to one of:
- `passed` — every row above is `pass`, cleanup fully confirmed done.
- `gaps_found` — one or more rows are `fail` or `blocked`; itemise each below with enough detail
  to plan a gap cycle (which run, which observation number, what was actually seen vs. expected).

### Gaps (fill in only if status is `gaps_found`)

_(fill in per failed/blocked row — run #, observation #, what was seen, hypothesis if any)_
