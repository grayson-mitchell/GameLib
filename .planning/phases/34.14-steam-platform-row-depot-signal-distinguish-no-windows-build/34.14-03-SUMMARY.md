---
phase: 34.14-steam-platform-row-depot-signal-distinguish-no-windows-build
plan: 03
subsystem: ui
tags: [steam, install-modal, eligibility-probe, react-hook, jest, pure-function]

# Dependency graph
requires:
  - phase: 34.14-01
    provides: "SteamBottleEligibilityVerdict.hasWindowsDepot / .platformsCaptured wire fields"
  - phase: 34.14-02
    provides: "steamPlatformRow.ts's resolveDepotAvailability / hasSteamDepotSignalCaptured pure gating layer that consumes this plan's hasWindowsDepot/depotSignalCaptured output"
provides:
  - "EligibilityState's 'resolved' variant widened with hasWindowsDepot/depotSignalCaptured"
  - "useSteamBottleEligibility's return widened with the same two fields"
  - "the corrected 'platformRow is not always synchronous' doc-comment (first of two stale comments 34.14 owns)"
affects: ["34.14 wave-3 consumer plan(s) that wire index.tsx to resolveDepotAvailability/resolveSteamSectionGating"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Widen a discriminated union's payload variant in place, mapping the wire field name to a locally-scoped state-machine name at the single point the verdict enters (D-02 same-round-trip pattern)"
    - "RED-proof-first spec authoring for a pass-through mapping: two inputs whose expected outputs differ on both fields, captured failing against a hardcoded stub before committing"

key-files:
  created: []
  modified:
    - "src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts"
    - "src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts"

key-decisions:
  - "D-02/D-03 (locked at planning time, not re-litigated): the widened verdict's two booleans ride the EXISTING round trip -- no second probe, no second IPC call site, no change to the effect's dependency array"
  - "State field named depotSignalCaptured (not platformsCaptured) to match the gating vocabulary steamPlatformRow.ts/steamSectionGating.ts already use; mapped 1:1 from verdict.platformsCaptured at the one entry point"

requirements-completed: []

# Metrics
duration: ~15min
completed: 2026-08-16
---

# Phase 34.14 Plan 03: Widen the Eligibility Probe State Machine Summary

**`EligibilityState`'s resolved variant and `useSteamBottleEligibility`'s hook return now both carry `hasWindowsDepot`/`depotSignalCaptured`, mapped from the verdict on the same existing IPC round trip, with the failure and non-probing paths both proven to report "settled and uncaptured" -- the exact input `resolveDepotAvailability` (34.14-02) fails open on -- and the stale "platformRow is genuinely synchronous" doc-comment corrected to describe the post-34.14 pending/resolved split.**

## Performance

- **Duration:** ~15 min commit-to-commit
- **Tasks:** 2 (both `type="auto"`, no checkpoints)
- **Files modified:** 2 (`steamEligibilityProbe.ts`, `steamEligibilityProbe.test.ts`)

## Accomplishments

- `EligibilityState`'s `'resolved'` variant widened with `hasWindowsDepot: boolean` and `depotSignalCaptured: boolean`, populated in `applyEligibilityResponse` from `response.verdict.hasWindowsDepot`/`response.verdict.platformsCaptured` -- the existing `state.appName !== response.appName` guard covers the whole widened object, unchanged
- `applyEligibilityFailure` and the non-probing branch of `initialEligibilityState` both resolve `hasWindowsDepot: false, depotSignalCaptured: false` ("we never found out", not a fail-closed depot verdict), each with an in-code comment naming `resolveDepotAvailability`/D-04 as the consumer of that shape
- `useSteamBottleEligibility`'s return object widened with the same two fields, following the existing `bottleRequired` ternary's exact shape
- The stale doc-comment claiming `platformRow` is "genuinely synchronous" replaced with the corrected post-34.14 invariant: synchronous only on the D-05 seed path; otherwise `resolveSteamSectionGating` returns `'pending'` from its own `depotSignalResolved` input until this probe settles
- Five new specs prove: (1) both new fields travel through on two inputs differing on both fields (RED-proof captured against a hardcoded `false`/`false` stub, see below), (2) the stale cross-game guard covers the widened payload via `toBe` referential identity, (3) the failure path resolves settled-and-uncaptured, (4) the non-probing branch reports the same shape, (5) the hook's return carries exactly four documented keys via `Object.keys().sort()` -- a structural guard against a future undocumented 5th pending flag (D-01)
- Probe timing, the cross-game guard, effect cleanup, dependency array (`[platform, runner, appName, action]`), and the single `window.api.isSteamBottleEligible(` call site are all provably unchanged (`git diff` shows no hunk inside `applyEligibilityPending`, `applyLibraryFetchPending`, `shouldProbeEligibility`, the effect body, or the source-gate `describe` block)

## Task Commits

1. **Task 1: Widen EligibilityState and the hook return with the depot pair** - `5f7cb8827` (feat)
2. **Task 2: Prove the widened state machine, including the failure path D-04 depends on** - `d9ff5d6ae` (test) -- append-only, 127 insertions / 0 deletions

**Plan metadata:** committed alongside STATE.md/ROADMAP.md updates per the standard `<final_commit>` step.

## Files Created/Modified

- `src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts` - `EligibilityState`'s `'resolved'` variant, `applyEligibilityResponse`, `applyEligibilityFailure`, `initialEligibilityState`'s non-probing branch, and the hook's return type/body all widened with the depot pair; the `platformRow` synchronicity doc-comment corrected
- `src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts` - append-only: a new `describe('34.14: the depot pair travels through the widened round trip')` block with 5 specs

## Verbatim Replacement Text (corrected doc-comment, per plan's `<output>` requirement)

Replacing the HEAD text (`` `platformRow` is deliberately NOT suppressed -- it is genuinely synchronous (D-03/D-17/D-19), and hiding it would make the dialog look empty on open, the D-20 corner this phase already decided not to create. ``):

```
`platformRow` is still deliberately NOT suppressed here, but as of 34.14
it is NOT synchronous either -- it is synchronous only when the depot
signal was already captured at open (34.14 D-05's seed case, the common
path, where an already-captured game never flashes a pending state);
otherwise `resolveSteamSectionGating` returns the `'pending'` mode from
its own `depotSignalResolved` input (34.14 D-01/D-03), and
`resolveDepotAvailability` resolves that mode once this probe settles
(34.14 D-04). Suppressing `platformRow` here would still make the dialog
look empty on open -- the D-20 corner 34.13 declined to create -- and
would ALSO collapse the pending/`readonly-macos` distinction that
Phase 34.14 exists to create.
```

`grep -c "it is genuinely" steamEligibilityProbe.ts` returns `0` and `grep -c "genuinely" steamEligibilityProbe.ts` also returns `0` -- the replacement dropped the word entirely rather than reusing it under a qualifier. Both are within the acceptance criterion's accepted branches.

## RED-Proof Evidence (Task 2 spec 1, per plan's `<output>` requirement)

With `applyEligibilityResponse`'s two derived fields temporarily hardcoded to `false, false` (bypassing `response.verdict.hasWindowsDepot`/`response.verdict.platformsCaptured`):

```
● 34.14: the depot pair travels through the widened round trip › applyEligibilityResponse
  carries BOTH hasWindowsDepot and depotSignalCaptured through from the verdict

  expect(received).toMatchObject(expected)

  - Expected  - 2
  + Received  + 2

    Object {
  -   "depotSignalCaptured": true,
  -   "hasWindowsDepot": true,
  +   "depotSignalCaptured": false,
  +   "hasWindowsDepot": false,
    }

Tests: 1 failed, 26 skipped, 27 total
```

The implementation was restored immediately after capturing this output; `git diff` against the committed Task 1 state showed zero drift (confirmed via `git diff --stat`), and the full suite was re-verified green (145/145) before Task 2's commit.

## Decisions Made

Followed the plan's locked design decisions exactly:
- The two booleans ride the existing round trip -- no second probe, no second IPC call site, no change to the effect's dependency array
- State field named `depotSignalCaptured`, distinct from the wire field `platformsCaptured`, matching the gating layer's vocabulary
- `eligibility.pending` remains the single pending mechanism; no second pending flag was added (structurally guarded by the new `Object.keys().sort()` spec)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The one pre-existing, out-of-scope `tsc` error in `index.tsx` (missing `depotSignalResolved` in a `SteamSectionGatingInput` literal, introduced by 34.14-02's widening of that input type) is unchanged by this plan -- `index.tsx` is explicitly not touched here and is deferred to a later wave-3 consumer plan, consistent with 34.14-02-SUMMARY.md's stated readiness note.

## Next Phase Readiness

- Both the pure gating layer (34.14-02) and the probe state machine (this plan) are complete and independently proven.
- The widened hook return (`pending`, `bottleRequired`, `hasWindowsDepot`, `depotSignalCaptured`) is ready for the wave-3 consumer plan to wire into `index.tsx`: seed `depotSignalResolved`/`hasWindowsDepot` from `gameInfo` (D-05), thread this hook's live `hasWindowsDepot`/`depotSignalCaptured` plus `!pending` (`probeSettled`) through `resolveDepotAvailability`, and pass the result into `resolveSteamSectionGating`.
- No blockers.

## Self-Check: PASSED

Verified on disk / in git history:
- `src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts` - FOUND, contains `depotSignalCaptured`.
- `src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts` - FOUND, contains `34.14: the depot pair travels through the widened round trip`.
- Commit `5f7cb8827` (Task 1) - FOUND in `git log --oneline`.
- Commit `d9ff5d6ae` (Task 2) - FOUND in `git log --oneline`.
- `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/` - 5 suites, 145/145 tests passed.

---
*Phase: 34.14-steam-platform-row-depot-signal-distinguish-no-windows-build*
*Completed: 2026-08-16*
