---
phase: 34.14-steam-platform-row-depot-signal-distinguish-no-windows-build
plan: 03
type: execute
wave: 2
depends_on: ["34.14-01"]
files_modified:
  - src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts
  - src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-02: the widened verdict's two booleans travel to the renderer on the EXISTING round-trip — no second probe, no second IPC call site, no change to the effect's dependency array."
    - "D-01: `eligibility.pending` remains the SINGLE mechanism gating Install; this plan adds no second pending flag."
    - "The timeout/offline path is preserved end-to-end: a failed probe resolves with the captured flag false, which is what lets D-04 fail open instead of silently reproducing the defect."
    - "The two stale doc-comments claiming `platformRow` is 'genuinely synchronous' are corrected with the replacement text the plan specifies (this plan owns the first of the two)."
  artifacts:
    - path: "src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts"
      provides: "EligibilityState.resolved widened with the depot pair; hook returns it"
      contains: "depotSignalCaptured"
  key_links:
    - from: "src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts"
      to: "src/common/types/steam.ts"
      via: "SteamBottleEligibilityVerdict.hasWindowsDepot / .platformsCaptured"
      pattern: "verdict\\.platformsCaptured"
---

<objective>
Carry the two new verdict booleans through the existing eligibility state machine, from the
one IPC call site to the hook's return value, and correct the shipped doc-comment that asserts
the premise this phase falsifies.

Purpose: D-02 says the signal rides the existing round-trip. This is the module that owns that
round-trip. Widening the `resolved` phase's payload is the whole change — the stale-response
guard, the effect cleanup, and the single-probe-per-open behaviour all stay exactly as shipped.

Output: a widened `EligibilityState`, a widened hook return, and a corrected doc-comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-CONTEXT.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-PATTERNS.md
@.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-01-SUMMARY.md

<settled_findings>
- **The existing `state.appName !== response.appName` reducer guard already covers the widened
  payload.** It compares the whole response's identity before replacing state, so a wider
  `resolved` variant needs NO new guard code (T-34.13-11-02 stays intact). Do not add one.
- **A single probe fires per dialog-open.** `useEffect`'s dependency array is exactly
  `[platform, runner, appName, action]`; none of those change on a network-state change, so
  reconnecting mid-dialog does NOT re-probe. This is load-bearing for the UAT gate in plan 05
  and must not be "improved" here.
- **This module's source gate bans the token `steamPlatformsCaptured`** (with the `steam`
  prefix) from its executable source. The verdict's own field name `platformsCaptured` and the
  state field name `depotSignalCaptured` are both permitted. The gate strips comments first, so
  doc-comments may name anything.
</settled_findings>

<interfaces>
Current state at HEAD (`steamEligibilityProbe.ts`):

```typescript
// lines 36-38
export type EligibilityState =
  | { phase: 'pending'; appName: string }
  | { phase: 'resolved'; appName: string; bottleRequired: boolean }
```

`initialEligibilityState` (lines 80-95) returns `{ phase: 'pending', appName }` when
`shouldProbeEligibility(...)` holds, else `{ phase: 'resolved', appName, bottleRequired: false }`.

`applyEligibilityResponse` (lines 105-117) returns
`{ phase: 'resolved', appName: response.appName, bottleRequired: response.verdict.eligible }`
after the appName guard.

`applyEligibilityFailure` (lines 126-138) returns
`{ phase: 'resolved', appName: failure.appName, bottleRequired: false }` — the fail-CLOSED
precedent D-04 deliberately diverges from (the divergence itself is implemented in plan 02's
`resolveDepotAvailability`, not here).

Hook return (lines 304-307):
```typescript
  return {
    pending: isEligibilityPending(state),
    bottleRequired: state.phase === 'resolved' ? state.bottleRequired : false
  }
```

**THE STALE COMMENT, lines 161-163, verbatim at HEAD:**
```
 * `platformRow` is deliberately NOT suppressed -- it is genuinely
 * synchronous (D-03/D-17/D-19), and hiding it would make the dialog look
 * empty on open, the D-20 corner this phase already decided not to create.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen EligibilityState and the hook return with the depot pair</name>
  <files>src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts</files>
  <read_first>
    - `src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts` (FULL file — the file being modified)
    - `.planning/phases/34.14-.../34.14-PATTERNS.md` § `steamEligibilityProbe.ts` (the exact current shape of every function being widened)
    - `src/common/types/steam.ts` (as widened by plan 01 — the source of the two field names)
  </read_first>
  <action>
1. Widen the `'resolved'` variant of `EligibilityState` with two fields alongside
   `bottleRequired`: `hasWindowsDepot: boolean` and `depotSignalCaptured: boolean`. Keep the
   `'pending'` variant unchanged — a pending state carries no depot answer by definition, and
   `index.tsx` will supply the `gameInfo` seed for that window (D-05).

   Name the state field `depotSignalCaptured`, not `platformsCaptured`, so the state machine's
   vocabulary matches the gating vocabulary `steamPlatformRow.ts`/`steamSectionGating.ts` use.
   Map it from `verdict.platformsCaptured` at the one place the verdict enters.

2. `applyEligibilityResponse`: after the existing unchanged appName guard, populate both new
   fields from the verdict — `hasWindowsDepot: response.verdict.hasWindowsDepot` and
   `depotSignalCaptured: response.verdict.platformsCaptured`. Do NOT add a second guard; the
   existing one covers the whole object.

3. `applyEligibilityFailure`: resolve with `hasWindowsDepot: false, depotSignalCaptured: false`.
   Add a comment stating that this is NOT a fail-closed depot decision — it records "we never
   found out", and D-04's fail-open is applied downstream by `resolveDepotAvailability`
   (`steamPlatformRow.ts`), which reads `probeSettled: true` together with
   `depotSignalCaptured: false` and OFFERS Windows. Without this note the pairing reads as a
   contradiction of D-04 rather than its input.

4. `initialEligibilityState`: the non-probing branch (`shouldProbeEligibility` false) resolves
   immediately, so it must supply `hasWindowsDepot: false, depotSignalCaptured: false`. Comment
   that this branch is only reached off the Steam-install-on-non-Windows/Linux route, where
   `isSteamManagedApp` is false in `index.tsx` and the Steam gating verdict is never consulted
   — so the resulting "settled and uncaptured" shape can never reach D-04's fail-open on a
   surface that renders it.

5. Widen the hook's return object with `hasWindowsDepot` and `depotSignalCaptured`, following
   the existing `bottleRequired` line's exact shape (`state.phase === 'resolved' ? state.X : false`).
   Do NOT change the effect, its dependency array, its cleanup, or the single
   `window.api.isSteamBottleEligible(` call site.

6. **Correct the stale doc-comment at lines 161-163.** Replace it — do not delete it — with
   text stating the actual post-34.14 invariant:
   `platformRow` is still deliberately NOT suppressed here, but it is NOT synchronous either.
   It is synchronous only when the depot signal was already captured at open (D-05's seed case,
   the common path, where an already-captured game never flashes a pending state); otherwise
   `resolveSteamSectionGating` returns the `'pending'` mode from its own `depotSignalResolved`
   input, and `resolveDepotAvailability` resolves that mode once this probe settles. Suppressing
   `platformRow` here would still make the dialog look empty on open — the D-20 corner 34.13
   declined to create — and would ALSO collapse the pending/`readonly-macos` distinction that
   Phase 34.14 exists to create. Cite 34.14 D-01/D-04/D-05 in the replacement so the next reader
   can trace it.
  </action>
  <verify>
    <automated>npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "genuinely" src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts` returns `0` if the replacement drops the word, or the surviving occurrence is inside the NEW text and is qualified by "only when the depot signal was already captured at open". State which in the SUMMARY, quoting the replacement verbatim. The unqualified claim "it is genuinely synchronous" must be gone: `grep -c "it is genuinely" src/frontend/.../steamEligibilityProbe.ts` returns `0`.
    - `applyEligibilityPending` is byte-identical to HEAD — `git diff` shows no hunk inside that function. It still does not suppress `platformRow`, and `steamEligibilityWiring.test.ts`'s B4 gate (`applyEligibilityPending(` appears exactly once in `index.tsx`) is unaffected by this plan.
    - The effect's dependency array is unchanged: `grep -c "\[platform, runner, appName, action\]" src/frontend/.../steamEligibilityProbe.ts` returns `1`.
    - The IPC call site count is unchanged: the module's own existing source-gate spec ("window.api.isSteamBottleEligible( appears exactly once") passes untouched.
    - The module's source gate passes unmodified: `npx jest .../steamEligibilityProbe.test.ts -t "source gates"` exits 0, and `git diff` shows no change to that describe block.
    - `npx tsc --noEmit` reports zero errors in `steamEligibilityProbe.ts`.
  </acceptance_criteria>
  <done>The resolved state and the hook return carry the depot pair; the probe's timing, guard and call-site behaviour are untouched; the stale synchronicity claim is corrected rather than removed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the widened state machine, including the failure path D-04 depends on</name>
  <files>src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts</files>
  <read_first>
    - `src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts` (FULL file — the file being modified; plan 01 already updated its four typed verdict literals, so read the current state, not HEAD~)
    - `src/frontend/screens/Library/components/InstallModal/steamEligibilityProbe.ts` (as widened in Task 1)
  </read_first>
  <action>
Append specs (append only — change nothing pre-existing beyond plan 01's literal repairs):

1. `applyEligibilityResponse` carries BOTH new fields through from the verdict: feed a verdict
   with `{ eligible: false, hasWindowsDepot: true, platformsCaptured: true }` and assert the
   resolved state reports `hasWindowsDepot: true, depotSignalCaptured: true`. Then feed
   `{ eligible: true, hasWindowsDepot: false, platformsCaptured: false }` and assert both are
   `false`. Two distinct inputs producing two distinct outputs is what makes this a gate rather
   than a restatement — a pass-through that hardcoded either field would fail one of them.

2. The stale-cross-game guard still holds over the WIDENED payload: apply a response whose
   `appName` differs from the state's, with `hasWindowsDepot: true, platformsCaptured: true`,
   and assert the returned state is REFERENTIALLY the prior state (`toBe`, not `toEqual`) — so
   a wider payload cannot leak one game's depot answer onto another game's dialog.

3. `applyEligibilityFailure` resolves settled-and-uncaptured: assert `phase: 'resolved'`,
   `bottleRequired: false`, `hasWindowsDepot: false`, `depotSignalCaptured: false`. Title it so
   the D-04 relationship is explicit — this is the INPUT to fail-open, not a fail-closed depot
   verdict.

4. `initialEligibilityState`'s non-probing branch reports the same settled-and-uncaptured shape.

5. A spec asserting the hook's RETURN shape carries exactly the four documented keys
   (`pending`, `bottleRequired`, `hasWindowsDepot`, `depotSignalCaptured`) via
   `Object.keys(...).sort()`, mirroring the `Object.keys(verdict)` convention
   `steamSectionGating.test.ts` already uses. This is the structural guard against a fifth,
   undocumented pending flag being added later in violation of D-01. Exercise the pure
   `isEligibilityPending`/state functions to build the object under test rather than rendering
   the hook — this project's frontend jest project has no jsdom and cannot mount a component.
  </action>
  <verify>
    <automated>npx jest src/frontend/screens/Library/components/InstallModal/__tests__/steamEligibilityProbe.test.ts src/frontend/screens/Library/components/InstallModal/__tests__/steamPlatformRow.test.ts src/frontend/screens/Library/components/InstallModal/__tests__/steamSectionGating.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five new spec groups pass. Spec 1 uses two inputs whose expected outputs differ on both fields — record in the SUMMARY that a hardcoded `false` implementation fails the first input (run it, capture the failure, restore).
    - Spec 2 uses `toBe` (referential identity), not `toEqual`. A `toEqual` here would pass against an implementation that rebuilt an equal-but-new state and would not prove the guard short-circuited.
    - `git diff --numstat` for `steamEligibilityProbe.test.ts` shows deletions ONLY on the four verdict-literal lines plan 01 repaired; no pre-existing `it()` body is otherwise modified.
    - `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/` exits 0 (all five files in the directory).
  </acceptance_criteria>
  <done>The widened payload is proven to travel, proven not to leak across games, and proven to record the failure path as settled-and-uncaptured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC response → renderer state | An `isSteamBottleEligible` response is applied to component state; responses may arrive out of order or for a superseded game |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.14-07 | Spoofing | a late/out-of-order verdict applied to the wrong game | mitigate | Pre-existing and UNCHANGED: the `state.appName !== response.appName` reducer guard rejects any response not matching the state's game, and the effect's `cancelled` flag is a second, independent control. Task 2 spec 2 proves the guard covers the WIDENED payload by referential identity, closing the specific new risk that a wider object leaks a depot answer across games. |
| T-34.14-08 | Denial of Service | a probe that never settles leaving Install permanently disabled | accept | Bounded upstream by `ensurePlatformsCaptured`'s `METADATA_FETCH_TIMEOUT_MS` (15s) plus the promise's `.catch` → `applyEligibilityFailure`. Both terminate. Worst case is a 15s disabled Install on a cold-cache game, which is the same bound 34.13 D-25 already shipped and accepted; this plan adds no new unbounded wait and no new await. |
| T-34.14-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed by this phase. No install task exists in this plan. |
</threat_model>

<verification>
- `npx jest src/frontend/screens/Library/components/InstallModal/__tests__/` exits 0.
- `npx tsc --noEmit` clean for both modified files.
- `git diff` shows no change to `applyEligibilityPending`, `applyLibraryFetchPending`, `shouldProbeEligibility`, the effect body, or the module's source-gate describe block.
</verification>

<success_criteria>
- `EligibilityState`'s resolved variant and the hook's return both carry `hasWindowsDepot` and `depotSignalCaptured`.
- The failure and non-probing paths both report settled-and-uncaptured, which is what D-04 consumes.
- The first of the two stale "genuinely synchronous" comments is replaced with the corrected invariant.
- Probe timing, cross-game guard, cleanup and call-site count are provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/34.14-steam-platform-row-depot-signal-distinguish-no-windows-build/34.14-03-SUMMARY.md` when done.
Record: the verbatim replacement text for the corrected doc-comment; the hardcoded-implementation
RED capture from Task 2 spec 1; and confirmation that the dependency array and IPC call-site count
are unchanged.
</output>
