---
phase: 34.11-library-filtering-search-views-collections-and-cross-store-f
plan: 06
type: execute
wave: 3
depends_on: ["34.11-02", "34.11-04"]
files_modified:
  - src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx
  - src/frontend/components/UI/NavShell/components/FilterViewList/index.scss
  - src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx
  - src/frontend/components/UI/NavShell/components/FilterCollectionList/index.scss
  - src/frontend/components/UI/NavShell/__tests__/FilterViewList.test.tsx
  - src/frontend/components/UI/NavShell/__tests__/FilterCollectionList.test.tsx
  - meta/i18nGateScope.json
autonomous: true
requirements:
  - REQ-34.11-05
  - REQ-34.11-08
user_setup: []

must_haves:
  truths:
    - "The panel offers exactly four Views — All games / Installed / Recently played / Favourites — as single-select rows, and choosing one deselects the previous one (D-05, REQ-34.11-05)."
    - "The chosen View row reads distinctly from the three unchosen ones, via plan 02's extended NavItem button branch rather than a forked row component (REQ-34.11-11 consumed here)."
    - "Collections rows are the verbatim output of customCategories.listCategories() plus the literal 'preset_uncategorized' pseudo-category — no migration, no derived collection, no second store (D-17, REQ-34.11-08)."
    - "Collections are single-select: at most one collection constrains the library at a time, replacing CategoryFilter's multi-select ToggleSwitch list (D-21)."
    - "'+ New collection' and 'Manage collections' both open the EXISTING CategoriesManager dialog via setShowCategories(true); no rename, delete, or membership UI is rebuilt inside the panel (D-20)."
    - "NEGATIVE (D-19): no rule, predicate, query or condition is evaluated to populate a collection. A collection's membership comes only from what the user assigned in CategoriesManager. Rule-based / smart collections are deferred, and nothing in this plan lays groundwork for them."
    - "NEGATIVE (D-18): not one CategoriesManager string changes. The panel's own header is a NEW key reading 'Collections' while the dialog it opens keeps saying 'Manage Categories'. The internal/external wording mismatch is intentional; a rename pass would touch every one of those strings and Phase 34.8's localisation gate is blocking."
    - "NEGATIVE (D-34): LibrarySearchBar is neither relocated nor reimplemented. Header already portals it into the tier-2 slot at Library/index.tsx:819, so search is ALREADY at the top of the panel. These two sections sit below it and import nothing from it."
    - "When zero collections exist the section still renders, showing the already-translated header.no_categories message rather than vanishing — the same message CategoryFilter ships today."
  artifacts:
    - path: "src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx"
      provides: "Four single-select View rows bound to libraryView / setLibraryView"
      contains: "setLibraryView"
      min_lines: 40
    - path: "src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx"
      provides: "Single-select collection rows from customCategories + the two CategoriesManager launch rows"
      contains: "setShowCategories"
      min_lines: 50
    - path: "src/frontend/components/UI/NavShell/components/FilterViewList/index.scss"
      provides: "Section styling scoped under .NavShell__tier2Portal"
    - path: "src/frontend/components/UI/NavShell/components/FilterCollectionList/index.scss"
      provides: "Collections section header + section styling scoped under .NavShell__tier2Portal"
    - path: "src/frontend/components/UI/NavShell/__tests__/FilterViewList.test.tsx"
      provides: "Direct-invocation cases for single-select, active marking, and label routing through t"
    - path: "src/frontend/components/UI/NavShell/__tests__/FilterCollectionList.test.tsx"
      provides: "Direct-invocation cases for verbatim category sourcing, single-select, empty state, and the D-18/D-19 negatives"
    - path: "meta/i18nGateScope.json"
      provides: "Both new component files registered so pnpm lint-translations:gamelib actually inspects them"
      contains: "FilterViewList"
  key_links:
    - from: "src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx"
      to: "src/frontend/screens/Library/LibraryContext"
      via: "useContext for libraryView / setLibraryView (plan 04's state)"
      pattern: "useContext\\(LibraryContext\\)"
    - from: "src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx"
      to: "src/frontend/state/ContextProvider"
      via: "customCategories.listCategories() read"
      pattern: "listCategories\\(\\)"
    - from: "src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx"
      to: "src/frontend/components/UI/NavShell/components/NavItem"
      via: "elementType='button' rows carrying plan 02's className + active props"
      pattern: "elementType=\"button\""
---

<objective>
Build the two sections that occupy the top half of the Games tier-2 filter panel, under the
already-portalled search box: the four single-select **Views** rows, and the single-select
**Collections** list with its two `CategoriesManager` launch rows.

Purpose: these are the panel's primary navigation controls — the two things a user reaches for
before touching any facet. They are also the only place in the phase where D-18, D-19 and D-34 are
decided, so this plan is where those three constraints get pinned by an automated gate rather than
left as prose.

**Scope boundary — this plan does NOT mount anything into `Header/index.tsx`.** Plan 07 builds the
facet groups in the same wave and would collide on that one file, and plan 09 (wave 4) already owns
`Header`'s rewrite as part of retiring `CategoryFilter`/`LibraryFilters` and updating
`tier2Portal.test.ts`. This plan ships two self-contained, independently unit-tested components plus
their scoped styles; plan 09 renders them. Do not add an import of either component to `Header`,
and do not delete `CategoryFilter` or `LibraryFilters` — they are still mounted and still working
until plan 09 lands.

**Shared-file note:** `meta/i18nGateScope.json` is an append-only path registry. Plan 07 will append
its own component paths to the same `files` array in the same wave. Append only your two paths in
alphabetical position and change nothing else in that file, so the two edits merge trivially.

Output: `FilterViewList` and `FilterCollectionList`, their SCSS, their tests, and their i18n gate
registration. No user-visible change yet — the components are not rendered until plan 09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-CONTEXT.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-UI-SPEC.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-VALIDATION.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-02-SUMMARY.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-04-SUMMARY.md

<interfaces>
From plan 04 (`src/frontend/types.ts`, `LibraryContextType`) — already committed by wave 2:
- `libraryView: LibraryView` / `setLibraryView: (value: LibraryView) => void`, where `LibraryView`
  is `'all' | 'installed' | 'recentlyPlayed' | 'favourites'` (declared in
  `src/frontend/screens/Library/filterEngine.ts` by plan 01)
- `currentCollection: string | null` / `setCurrentCollection: (value: string | null) => void` —
  a `customCategories` key or the literal `'preset_uncategorized'`
- `setShowCategories: (value: boolean) => void` — pre-existing; opens the `CategoriesManager` dialog

From plan 02 (`src/frontend/components/UI/NavShell/components/NavItem/index.tsx`) — already
committed by wave 1. Its props before the extension were:
`label`, `labelElement?`, `url?`, `icon?`, `isActiveFallback?`, `onClick?`, `className?`,
`elementType?: 'a' | 'button'`. Plan 02 made the `case 'button':` branch honour `className` and an
active flag through `classNames('NavItem', ...)`. **Read `34.11-02-SUMMARY.md` for the exact prop
name plan 02 shipped for the active flag (`active` vs `isActiveFallback` reuse) and use that name
verbatim** — guessing it produces a silent no-op, since a `NavItem` rendered with an unknown prop
still renders fine and just never looks selected.

From `src/frontend/state/ContextProvider` (`GlobalState.tsx:477-547`) — reuse, do not rebuild:
- `customCategories.listCategories()` → `string[]`, already sorted
- the literal `'preset_uncategorized'` is the "uncategorized" pseudo-category key
- `addCustomCategory` / `removeCustomCategory` / `renameCustomCategory` exist but are the
  `CategoriesManager` dialog's business, not this panel's (D-20)

The component being replaced, for reference on strings and structure only —
`src/frontend/components/UI/CategoryFilter/index.tsx`:
- its empty state is `t('header.no_categories', 'No custom categories. Add categories using each
  game menu.')` at L79-89 — **reuse that exact key**, do not mint a new empty-state string
- its uncategorized label is `t('header.uncategorized', 'Uncategorized')` — **reuse that exact key**
- its multi-select `ToggleSwitch` + `only` button pattern is what D-21 replaces. Do not carry the
  `only` button over: with single-select, "only" and "select" are the same gesture.

`Header/index.tsx` today renders `<LibrarySearchBar />` inside `.Header__search`, then
`<CategoryFilter />` and `<LibraryFilters />` inside `.Header__filters`. `Header` is portalled into
the tier-2 slot at `Library/index.tsx:819`. **You are not editing this file in this plan.**
</interfaces>

<i18n_contract>
Phase 34.8's localisation gate is a STANDING BLOCKING requirement. `pnpm lint-translations:gamelib`
must pass, and a green jest run does not imply a green gate.

**Namespace decision — use the `gamelib:` prefix exactly as the UI-SPEC specifies.**

> **Corrected 2026-08-09 during `/gsd-plan-phase`.** An earlier draft of this plan dropped the
> prefix and minted into the default namespace. Both of its stated grounds were falsified against
> the live tree:
> 1. *"The parser is configured with `functions: ['t']`, so the prefix breaks the tuple idiom."*
>    That was true, and **Phase 34.8-09 fixed it**. `i18next-parser.config.js:61` and `:70` now read
>    `functions: ['t', 'tGamelib']` for the `ts` and `tsx` lexers. The config's own comment records
>    why: the aliased-hook idiom is universal across every `gamelib:` call site (17 files), and under
>    the old default the lexer "silently could not see any `tGamelib(...)` call — 16 of 48
>    retrofitted keys measured absent from the generated catalog before this fix."
> 2. *"34.10's nav-shell gate forbids the prefix."* That assertion
>    (`NavTabsComponent.test.tsx:257-264`) reads a single hard-coded path,
>    `NavShell/components/NavTabs/index.tsx`. It is a local constraint on that one component and
>    **cannot fire on any file this plan creates**.
>
> The `gamelib:` namespace is the established, parser-supported, catalogued pattern: **90 call sites**
> across `src/`, a real catalog at `public/locales/en/gamelib.json`, and the shipping idiom
> `t('gamelib:humble.lessThanAMinute', 'less than a minute')`.

Get the translate function via the aliased-hook idiom used at every other `gamelib:` call site:
`const { t: tGamelib } = useTranslation('gamelib')`. **The copy strings themselves are unchanged
from the UI-SPEC.**

Keys minted by this plan (`gamelib` namespace, exact default text from the UI-SPEC Copywriting table):

| Key | Default text |
|---|---|
| `gamelib:library.filterPanel.viewAll` | `All games` |
| `gamelib:library.filterPanel.viewInstalled` | `Installed` |
| `gamelib:library.filterPanel.viewRecentlyPlayed` | `Recently played` |
| `gamelib:library.filterPanel.viewFavourites` | `Favourites` |
| `gamelib:library.filterPanel.collections` | `Collections` |
| `gamelib:library.filterPanel.newCollection` | `+ New collection` |
| `gamelib:library.filterPanel.manageCollections` | `Manage collections` |

Keys REUSED verbatim (do not re-mint, do not edit): `header.uncategorized`, `header.no_categories`.

Strings that must NOT change: every `categories-manager.*` key, including
`t('categories-manager.title', 'Manage Categories')` (D-18).
</i18n_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FilterViewList — four single-select View rows</name>
  <read_first>
    - .planning/phases/34.11-.../34.11-02-SUMMARY.md (the EXACT active-flag prop name plan 02 shipped on the button branch — do not guess it)
    - src/frontend/components/UI/NavShell/components/NavItem/index.tsx (post-plan-02 props and the `case 'button':` branch)
    - src/frontend/components/UI/NavShell/components/NavItem/index.scss (confirm the row rules are already scoped under `.NavShell__tier2 .NavItem`, so these rows inherit padding/hover/active for free)
    - src/frontend/components/UI/NavShell/components/NavTabs/index.tsx L87-118 (the shipped `label={t('key', 'Default')}` idiom for this component family)
    - src/frontend/screens/Library/components/GameCard/__tests__/CrossoverBadge.test.tsx L1-50 (the module-level `react-i18next` mock + direct-invocation pattern you must copy)
    - .planning/phases/34.11-.../34.11-CONTEXT.md D-05, D-34
  </read_first>
  <files>src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx, src/frontend/components/UI/NavShell/components/FilterViewList/index.scss, src/frontend/components/UI/NavShell/__tests__/FilterViewList.test.tsx</files>
  <behavior>
    - Given `libraryView === 'all'`, the `All games` row is the only row rendered with the active flag true.
    - Given `libraryView === 'favourites'`, the `Favourites` row is the only active row.
    - Clicking the `Installed` row calls `setLibraryView('installed')` exactly once and calls it with a single scalar — never an array, never a spread of a previous selection (this is what makes it single-select rather than a toggle set, D-05).
    - Clicking the row that is already active still calls `setLibraryView` with that same value — selecting the current view is idempotent, it does NOT clear back to `'all'`.
    - Exactly four rows render, in the order All games, Installed, Recently played, Favourites.
    - Every row label is the return value of a `t(key, default)` call; no bare string literal reaches a `label` prop.
  </behavior>
  <action>
Create `src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` exporting a default
function component `FilterViewList` taking no props.

Read `libraryView` and `setLibraryView` from `LibraryContext`
(`import LibraryContext from 'frontend/screens/Library/LibraryContext'`, then `useContext`). Get the
translate function via the aliased-hook idiom `const { t: tGamelib } = useTranslation('gamelib')` —
the `gamelib` namespace, per `<i18n_contract>`.

Declare the four rows as a module-scope constant array of `{ value: LibraryView; key: string;
defaultText: string }`, in the order `all`, `installed`, `recentlyPlayed`, `favourites`, using the
seven-key table in `<i18n_contract>`. Keeping the array at module scope is what lets the unit test
assert the row count and order without a DOM. Import the `LibraryView` type from
`frontend/screens/Library/filterEngine` — do not redeclare the union.

Map that array to `NavItem`s with `elementType="button"`, `label={t(row.key, row.defaultText)}`,
`className="FilterViewList__row"`, the plan-02 active flag set to `libraryView === row.value`, and
`onClick={() => setLibraryView(row.value)}`. Pass NO `icon` — Views rows are text-only (UI-SPEC
discretion item 3, already resolved). Give each row a stable `key` of `row.value`.

Wrap the rows in `<nav className="FilterViewList">` — a `nav` rather than a bare `div` because these
four rows are the panel's primary navigation control and the tier-2 column has no other landmark.

Create `src/frontend/components/UI/NavShell/components/FilterViewList/index.scss` and import it from
the component. **Scope every selector under `.NavShell__tier2Portal`** — an unscoped selector in this
codebase has already leaked app-wide once (34.10's `.MuiTabs-root` incident), and these class names
are generic enough to collide. The file declares only the section box: `display: flex;
flex-direction: column;` and `margin-block-end: var(--space-sm)` (the 12px between-sections gap from
the UI-SPEC spacing table). Declare NOTHING about row padding, hover, active colour or font weight —
`NavItem/index.scss` already supplies all of those under `.NavShell__tier2 .NavItem`, and
redeclaring them here would fork the tier-2 row spec into two places. Do not add any pixel
`max-height` — `.NavShell__tier2Portal` already ships `height: 100%; overflow-y: auto;
overflow-x: hidden` at `NavShell/index.scss:172-178`.

Create `src/frontend/components/UI/NavShell/__tests__/FilterViewList.test.tsx` covering every
`<behavior>` bullet. **No jsdom exists** (`src/frontend/jest.config.js` sets
`testEnvironment: 'node'`; there is no `@testing-library/react` and no `react-test-renderer`), so
use the direct-invocation pattern from `CrossoverBadge.test.tsx`: `jest.mock('react-i18next', ...)`
returning a `t` that echoes its `defaultValue`, `jest.mock('react', ...)` (or mock the
`LibraryContext` module) so `useContext` returns a controllable fake context object, then call
`FilterViewList({})` as a plain function and walk the returned element's `props.children` array to
inspect each `NavItem` element's `props`. Assert the click behaviour by invoking
`child.props.onClick` directly and checking the `setLibraryView` jest mock's calls — including
`expect(setLibraryView).toHaveBeenCalledWith('installed')` and that the argument is not an array.
  </action>
  <acceptance_criteria>
    - `grep -c "elementType=\"button\"" src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` returns 1
    - Exactly four view keys are minted: `grep -c "gamelib:library.filterPanel.view" src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` returns 4
    - No bare label literal: `grep -c "label={tGamelib(" src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` returns 1 and `grep -cE "label=\"" src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` returns 0
    - The `gamelib:` prefix is PRESENT on every minted key — the namespace is parser-visible via `functions: ['t', 'tGamelib']`: `grep -c "t('gamelib:" src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` returns 0 and `grep -c "tGamelib('gamelib:" src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` returns 4
    - The aliased hook is used: `grep -c "useTranslation('gamelib')" src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` returns 1
    - D-34 negative — this component does not touch search: `grep -c "LibrarySearchBar\|filterText" src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` returns 0
    - SCSS is scoped, ignoring comment lines: `grep -v '^\s*//' src/frontend/components/UI/NavShell/components/FilterViewList/index.scss | grep -c "NavShell__tier2Portal"` returns at least 1
    - No pixel height bound: `grep -v '^\s*//' src/frontend/components/UI/NavShell/components/FilterViewList/index.scss | grep -c "max-height"` returns 0
    - The row spec is not forked: `grep -v '^\s*//' src/frontend/components/UI/NavShell/components/FilterViewList/index.scss | grep -cE "font-weight|--navbar-active"` returns 0
    - `pnpm jest src/frontend/components/UI/NavShell/__tests__/FilterViewList.test.tsx --silent` exits 0 with at least 6 assertions passing
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest src/frontend/components/UI/NavShell/__tests__/FilterViewList.test.tsx --silent</automated>
  </verify>
  <done>Four single-select View rows render as extended NavItems, the active one is distinguishable, and single-select is proven by a test that fails if a selection is ever accumulated rather than replaced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FilterCollectionList — single-select collections plus the two CategoriesManager launchers</name>
  <read_first>
    - src/frontend/components/UI/CategoryFilter/index.tsx (whole file, ~100 lines — the exact `header.no_categories` and `header.uncategorized` keys, the `'preset_uncategorized'` literal, and the `setShowCategories(true)` launch you are reusing)
    - src/frontend/state/GlobalState.tsx around L477-547 (`customCategories.listCategories()` and its siblings — confirm you only need the read)
    - src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx (Task 1's output — this component mirrors its structure exactly, so the two sections cannot drift)
    - .planning/phases/34.11-.../34.11-CONTEXT.md D-17, D-18, D-19, D-20, D-21
    - .planning/phases/34.11-.../34.11-UI-SPEC.md the "Discretion resolved — Collections section header wording" block and the Copywriting table rows for `newCollection` / `manageCollections`
  </read_first>
  <files>src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx, src/frontend/components/UI/NavShell/components/FilterCollectionList/index.scss, src/frontend/components/UI/NavShell/__tests__/FilterCollectionList.test.tsx</files>
  <behavior>
    - Given `listCategories()` returns `['RPG', 'Backlog']`, exactly those two names render as rows, in that order, with no name rewritten, sorted, filtered or prefixed (D-17 verbatim sourcing).
    - An `Uncategorized` row always renders after the category rows, carrying the value `'preset_uncategorized'`.
    - Given `currentCollection === 'RPG'`, the `RPG` row is the only active row; given `currentCollection === null`, no collection row is active.
    - Clicking a collection row calls `setCurrentCollection` with that single name — never an array, never appended to a prior selection (single-select, D-21).
    - Clicking the already-active collection row calls `setCurrentCollection(null)`, so a user can leave the collection without a separate clear control.
    - Given `listCategories()` returns `[]`, the section still renders its header and the `header.no_categories` message, and still renders both the `+ New collection` and `Manage collections` rows.
    - Clicking `+ New collection` calls `setShowCategories(true)`; clicking `Manage collections` calls `setShowCategories(true)`. Neither adds, renames or deletes a category itself.
  </behavior>
  <action>
Create `src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` exporting a
default function component `FilterCollectionList` taking no props.

Read `customCategories` from `ContextProvider` (`import ContextProvider from
'frontend/state/ContextProvider'`), and `currentCollection`, `setCurrentCollection`,
`setShowCategories` from `LibraryContext`. This component needs BOTH namespaces: newly minted panel
keys come from `const { t: tGamelib } = useTranslation('gamelib')`, while the two REUSED shipped
keys (`header.no_categories`, `header.uncategorized`) must keep coming from the default namespace via
`const { t } = useTranslation()` — they are existing translated strings and re-minting them under
`gamelib:` would orphan every locale that already has them.

Render, in order, inside `<section className="FilterCollectionList">`:

1. A header element with `className="FilterCollectionList__header"` whose text is
   `tGamelib('gamelib:library.filterPanel.collections', 'Collections')`. **This is a NEW panel-only key, not a
   rename** (D-18): the dialog these rows open keeps its own `categories-manager.*` strings, so the
   panel says "Collections" while the dialog says "Manage Categories". That mismatch is the decided
   outcome, not a bug to reconcile — do not touch any `categories-manager.*` string, and do not
   rename `customCategories`, `setCurrentCustomCategories`, `CategoriesManager` or any other
   identifier in the plumbing.
2. When `customCategories.listCategories()` is empty: a `<span
   className="FilterCollectionList__empty">` containing `t('header.no_categories', 'No custom
   categories. Add categories using each game menu.')` — the SAME already-translated key
   `CategoryFilter/index.tsx:79-89` ships. Do not mint a new empty-state string. The section is
   never hidden outright (UI-SPEC discretion item 2, resolved: show the message).
3. One `NavItem` per entry of `customCategories.listCategories()`, used **verbatim** — do not sort,
   dedupe, trim, title-case or filter the returned names (D-17: the list is already sorted upstream,
   and any transform here would be an undeclared second source of truth).
4. An `Uncategorized` `NavItem` labelled `t('header.uncategorized', 'Uncategorized')` whose value is
   the literal `'preset_uncategorized'`.
5. `+ New collection` — an `NavItem` labelled
   `tGamelib('gamelib:library.filterPanel.newCollection', '+ New collection')`, `onClick` calling
   `setShowCategories(true)`.
6. `Manage collections` — an `NavItem` labelled
   `tGamelib('gamelib:library.filterPanel.manageCollections', 'Manage collections')`, `onClick` calling
   `setShowCategories(true)`.

Rows 3-6 all use `elementType="button"` with `className="FilterCollectionList__row"` and no `icon`
(text-only, UI-SPEC discretion item 3). Rows 5 and 6 are actions, not selections — they never carry
the active flag. Rows 3 and 4 carry the plan-02 active flag set to `currentCollection === value`.

Selection handler: a single `selectCollection(value: string)` that calls
`setCurrentCollection(currentCollection === value ? null : value)`. One scalar in, one scalar out —
there is no array anywhere in this component.

**D-20 boundary — the two action rows only LAUNCH the existing dialog.** Do not call
`addCustomCategory`, `removeCustomCategory` or `renameCustomCategory`, do not render an inline text
input, and do not build a rename or delete affordance. `+ New collection` and `Manage collections`
differ in label and panel position, not in behaviour; both open `CategoriesManager`, which is where
all collection management already lives and stays.

**D-19 boundary — manual collections only.** Membership comes from what the user assigned in
`CategoriesManager` and nowhere else. Do not add a rule, predicate, query, condition, filter
expression or "smart collection" concept, and do not leave a type, field or TODO that anticipates
one. Rule-based collections are a deferred feature that should be designed as a saved-filter
feature in its own right, not smuggled in as an unused branch here.

Create `src/frontend/components/UI/NavShell/components/FilterCollectionList/index.scss`, imported by
the component, with **every selector scoped under `.NavShell__tier2Portal`**. Declare only:
`.FilterCollectionList` — `display: flex; flex-direction: column;` plus
`margin-block-end: var(--space-sm)`; `.FilterCollectionList__header` — `font-size: var(--text-sm);
font-weight: var(--bold); line-height: 1;` plus `padding: var(--space-2xs)
var(--tier2-row-padding-inline);` so the header's inline edge aligns with the row labels beneath it;
`.FilterCollectionList__empty` — `font-size: var(--text-sm); padding: var(--space-2xs)
var(--tier2-row-padding-inline);`. Add no colour declaration to the header or empty message: both
inherit the panel's foreground, and introducing a colour here would need an entry in all eleven
theme blocks. Add no row padding, hover, active colour or font weight — `NavItem/index.scss` owns
those. Add no pixel `max-height`.

Create `src/frontend/components/UI/NavShell/__tests__/FilterCollectionList.test.tsx` covering every
`<behavior>` bullet, using the same no-jsdom direct-invocation pattern as Task 1 with
`listCategories` as a jest mock you can re-point per case. Add two source-text gates in the same
file (`fs.readFileSync` + regex, the `tier2Portal.test.ts` style): one asserting the component source
contains no `addCustomCategory|removeCustomCategory|renameCustomCategory` reference (D-20), and one
asserting it contains no `rule|predicate|smartCollection` identifier (D-19). Both gates must be
proven to fail against a known-bad string before you accept them — an unmatchable pattern passes
vacuously.
  </action>
  <acceptance_criteria>
    - Verbatim sourcing: `grep -c "listCategories()" src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` returns 1, and `grep -cE "\.sort\(|\.filter\(|toLowerCase|trim\(" src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` returns 0
    - Single-select, no arrays: `grep -c "setCurrentCollection" src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` returns at least 1 and `grep -cE "setCurrentCollection\(\[" src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` returns 0
    - D-20 negative: `grep -cE "addCustomCategory|removeCustomCategory|renameCustomCategory" src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` returns 0, and `grep -c "setShowCategories(true)" ...` returns 2
    - D-19 negative: `grep -ciE "smartcollection|ruleset|predicate" src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` returns 0
    - D-18 negative: `git diff --name-only HEAD -- src/frontend/components/UI/CategoriesManager` is empty, and `grep -rc "categories-manager.title" src/frontend/components/UI/CategoriesManager` is unchanged from HEAD
    - Reused keys are reused, not re-minted: `grep -c "header.no_categories" src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` returns 1 and `grep -c "header.uncategorized" ...` returns 1
    - New keys carry the namespace: `grep -cE "gamelib:library\.filterPanel\.(collections|newCollection|manageCollections)" src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` returns 3, and all three are reached through `tGamelib(`: `grep -c "tGamelib('gamelib:" ...` returns 3
    - Both hooks are present, because this component mixes namespaces: `grep -c "useTranslation('gamelib')" ...` returns 1 and `grep -cE "useTranslation\(\)" ...` returns 1
    - The two reused keys are NOT prefixed: `grep -c "gamelib:header\." ...` returns 0
    - SCSS scoped and unforked: `grep -v '^\s*//' src/frontend/components/UI/NavShell/components/FilterCollectionList/index.scss | grep -c "NavShell__tier2Portal"` returns at least 1; the same filtered stream returns 0 for `max-height` and 0 for `--navbar-active`
    - The two source gates in the test file are proven non-vacuous: temporarily point each regex at a known-bad literal, confirm the test FAILS, then restore
    - `pnpm jest src/frontend/components/UI/NavShell/__tests__/FilterCollectionList.test.tsx --silent` exits 0 with at least 9 assertions passing
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest src/frontend/components/UI/NavShell/__tests__/FilterCollectionList.test.tsx --silent</automated>
  </verify>
  <done>Collections render verbatim from customCategories as single-select rows, both action rows only launch the existing CategoriesManager, and D-18/D-19/D-20 are each pinned by an assertion rather than by prose.</done>
</task>

<task type="auto">
  <name>Task 3: Register both components with the i18n gate and pin the D-34 search invariant</name>
  <read_first>
    - meta/i18nGateScope.json (the `files` array — note it is alphabetically ordered and carries a `baseCommit`/`generatedAt` header you must not edit)
    - src/frontend/components/UI/NavShell/__tests__/NavTabsComponent.test.tsx L250-275 (the shipped source-gate style for nav-shell i18n assertions, including the `not.toMatch(/gamelib:/)` precedent)
    - src/frontend/components/UI/Header/index.tsx (17 lines — the file you are asserting about but NOT editing)
    - .planning/phases/34.11-.../34.11-CONTEXT.md D-34
  </read_first>
  <files>meta/i18nGateScope.json, src/frontend/components/UI/NavShell/__tests__/FilterViewList.test.tsx</files>
  <action>
Append exactly two paths to the `files` array in `meta/i18nGateScope.json`, in their correct
alphabetical positions among the existing `src/frontend/components/UI/NavShell/components/…`
entries:
`src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` and
`src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx`.

Change nothing else in that file — not `baseCommit`, not `baseVersion`, not `generatedAt`, not
`generatedBy`, and no existing path. Do not run `pnpm gen-i18n-gate-scope` to do this: regenerating
would rewrite the header fields and produce a diff that hides the two lines that actually matter.
Plan 07 appends its own paths to the same array in this wave, so keep the edit to two added lines.

Then run `pnpm lint-translations:gamelib` and fix any finding it reports. The historical trap here —
that the parser's default `functions: ['t']` cannot see a translate call reached through an alias
such as `tGamelib` — was FIXED by Phase 34.8-09: `i18next-parser.config.js:61,70` now declare
`functions: ['t', 'tGamelib']` for the `ts` and `tsx` lexers, which is exactly what makes the
`gamelib:` namespace safe to use here. Two things still to check rather than assume: the alias must
literally be named `tGamelib` (any other alias is invisible again), and the generated catalog
`public/locales/en/gamelib.json` must actually gain the new keys. A green jest run does NOT mean
this gate passes — run it explicitly and confirm the keys landed in the catalog.

Finally, add one D-34 source gate to `FilterViewList.test.tsx` (the file already exists from Task 1;
this is an appended `describe` block, not a new file). D-34 is decided nowhere else in the phase, so
it needs one assertion that would catch a regression: read `src/frontend/components/UI/Header/
index.tsx` with `fs.readFileSync` and assert it still renders `<LibrarySearchBar />` and still
imports it from `'../LibrarySearchBar'`. Search is ALREADY at the top of the panel because `Header`
is portalled into the tier-2 slot at `Library/index.tsx:819` — this plan neither moves it nor
reimplements it, and plan 09's rewrite of `Header` must not drop it while replacing everything
around it. Add a one-line comment above the gate naming D-34 and stating that the search box is
inherited, not built. Prove the gate is non-vacuous by temporarily changing the expected literal to
a string that is not in the file and confirming the test fails, then restore it.
  </action>
  <acceptance_criteria>
    - `grep -c "FilterViewList/index.tsx" meta/i18nGateScope.json` returns 1 and `grep -c "FilterCollectionList/index.tsx" meta/i18nGateScope.json` returns 1
    - Only additions, exactly two lines: `git diff --numstat meta/i18nGateScope.json` shows `2` added and `0` deleted
    - The generated header is untouched: `git diff meta/i18nGateScope.json | grep -c "baseCommit\|generatedAt\|baseVersion"` returns 0
    - `pnpm lint-translations:gamelib` exits 0
    - `grep -c "LibrarySearchBar" src/frontend/components/UI/NavShell/__tests__/FilterViewList.test.tsx` returns at least 1
    - The D-34 gate is non-vacuous: with the expected literal corrupted the test FAILS; restored, it passes
    - Header itself is unedited by this plan: `git diff --name-only HEAD -- src/frontend/components/UI/Header` is empty
    - `pnpm jest src/frontend/components/UI/NavShell --silent` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint-translations:gamelib && pnpm jest src/frontend/components/UI/NavShell --silent</automated>
  </verify>
  <done>Both new components are inside the i18n gate's scope and the gate is green, and D-34's "search is inherited, not rebuilt" invariant is held by a proven-failing assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `configStore` (`games.customCategories`) → renderer render tree | Collection names are **user-authored free text** persisted by the app and read back here to become rendered row labels and i18n interpolation inputs. This is the only untrusted-ish string that crosses into this plan's output. |
| `LibraryContext` → these components | `libraryView` and `currentCollection` originate in plan 04's `localStorage`-backed state, which is writable by any local process. This plan renders from it without re-validating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.11-14 | Tampering | `FilterCollectionList` row labels | mitigate | Collection names render as JSX text children, never through `dangerouslySetInnerHTML` and never as an i18n **interpolation value** into a translated sentence. React escapes text children; an interpolated `{{name}}` inside a `Trans`/`t` call with markup enabled would not be equally safe. Each name is its own `NavItem` `label`, so there is no sentence for a name to escape from. |
| T-34.11-15 | Tampering | `currentCollection` arriving from `localStorage` via plan 04 | accept | A tampered value that matches no category simply leaves every row inactive — a cosmetic no-op, not a privilege or data effect. Plan 04 already coerces empty/absent to `null` at the initialiser, which is the correct place to validate; re-validating here would duplicate the guard in two files and let them drift. |
| T-34.11-16 | Information disclosure | the Collections section | accept | Everything rendered is data the app itself persisted for this same local user, displayed back to that user in the same window. No cross-user, cross-account or network surface exists in this plan. |
| T-34.11-17 | Elevation of privilege | the two `CategoriesManager` launch rows | mitigate | Both rows only call `setShowCategories(true)`. They hold no mutation capability at all — no `addCustomCategory` / `removeCustomCategory` / `renameCustomCategory` import exists in the file, which the D-20 acceptance grep enforces. Mutation stays behind the dialog that already owns it, so the panel cannot become a second, unreviewed write path into `games.customCategories`. |
| T-34.11-SC | Tampering | npm/pnpm/cargo installs | mitigate | **No packages are installed by this plan.** Every import is already in the tree (`react`, `react-i18next`, `classnames` via `NavItem`). If an install appears necessary, STOP — it is out of scope and requires a blocking human legitimacy checkpoint against the `RESEARCH.md` package audit. |
</threat_model>

<verification>
- `pnpm jest src/frontend/components/UI/NavShell --silent` exits 0
- `pnpm lint-translations:gamelib` exits 0 (STANDING BLOCKING requirement — a green jest run does
  not imply this)
- `npx tsc --noEmit -p tsconfig.json` exits 0
- `pnpm test:ci` exits 0
- **No visible change in the running app is expected from this plan**, because neither component is
  mounted until plan 09 rewrites `Header`. Do not report a UI change, and do not mount them early to
  produce one — plan 07 is editing `Header`'s eventual contents in the same wave.
- Multi-theme verification is DEFERRED to plan 09, which is the first point at which these sections
  are actually on screen. When it happens it must cover `gruvbox_dark` and `dracula`, never
  `midnightMirage` alone, and the app must be started with `pnpm tauri:dev` (`tauri dev` serves a
  stale static bundle).
- Untouched-file check: `git diff --name-only HEAD` must NOT list
  `src/frontend/components/UI/Header/index.tsx`,
  `src/frontend/components/UI/CategoryFilter/index.tsx`,
  `src/frontend/components/UI/LibraryFilters/index.tsx`, or anything under
  `src/frontend/components/UI/CategoriesManager`.
</verification>

<success_criteria>
- Four single-select View rows exist as extended `NavItem`s, with the active one distinguishable
  (REQ-34.11-05)
- Collections render verbatim from `customCategories.listCategories()` plus `'preset_uncategorized'`,
  single-select, with the empty state reusing `header.no_categories` (REQ-34.11-08)
- `+ New collection` and `Manage collections` only open the existing `CategoriesManager` (D-20)
- D-18, D-19 and D-34 are each held by at least one proven-non-vacuous automated assertion, not by
  prose alone
- Both new component files are registered in `meta/i18nGateScope.json` and the gate is green
- `Header`, `CategoryFilter`, `LibraryFilters` and `CategoriesManager` are all byte-identical to HEAD
</success_criteria>

<output>
Create `.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-06-SUMMARY.md` when done
</output>
