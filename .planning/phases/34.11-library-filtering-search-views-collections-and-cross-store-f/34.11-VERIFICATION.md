---
phase: 34.11-library-filtering-search-views-collections-and-cross-store-f
verified: 2026-08-10T08:49:11Z
status: passed
score: 17/17 requirements verified (4/4 code-review blockers confirmed fixed with regression tests; all fixes independently re-run in this session, not taken on SUMMARY trust)
overrides_applied: 0
human_verification_relocated:
  # Relocated to Phase 38 (deferred hardware/environment UAT gates) on 2026-08-25 by quick
  # 260825-wg5, satisfying Phase 38 relocation rule (3): relocation is two-way, and this is
  # 34.11's half of the receipt.
  #
  # NOTE FOR ANYONE EDITING THIS FILE: do NOT add an empty `human_verification: []` alongside
  # this key. With an empty array, `gsd-sdk query audit-uat` falls back to scraping the document
  # BODY and reports heading text as phantom open items (observed on 34.1, 2026-08-22). This
  # phase's `status: passed` is correct and unchanged -- the item below was never a blocker to
  # closing 34.11, only an unowned measurement.
  #
  # Relocated is NOT resolved. The measurement has still never been performed.
  - test: "Gamepad focus-scroll behaviour in the tier-2 filter panel"
    moved_to: "Phase 38"
    moved_as: "38-C06"
    moved_on: "2026-08-25"
    why_moved: >
      Deferred twice without ever being scheduled -- no physical controller in 34.10 or 34.11.
      This file's own "Carried-forward risk, not a phase blocker" note calls it "the one item
      worth escalating... risks becoming an invisible standing gap if deferred a third time",
      and it was: this VERIFICATION carried no `human_verification` key, so `audit-uat` could
      not see the item at all. Phase 38 existed in ROADMAP.md before the move.
    platform_gate: >
      `.NavShell__tier2Portal` (src/frontend/components/UI/NavShell/index.scss:499-505) is the
      panel's own `overflow-y: auto` scroll container, nested inside `.NavShell__tier2`'s
      `overflow: hidden` (:401) and outside `main.content`. No focus-scroll handler is bound to
      it -- zero hits for `scrollIntoView|addEventListener('focus'|onFocus` across `NavShell/`
      and `Header/`. Dispatch is gated the same way as 38-C01..C05: `gamepadAction` comes only
      from the `navigator.getGamepads()` polling loop (src/frontend/helpers/gamepad.ts:559,678),
      so no part of this is reachable from a keyboard.
    not_covered_by: >
      38-C05, despite the similar wording. That item covers `scrollCardIntoView`
      (GamesList/index.tsx:46), bound to the games list at :139 and hardcoding
      `document.querySelector('main.content')` as its container -- a different element in a
      different scroll container. C05 passing says nothing about the panel, and
      `scrollCardIntoView` would scroll the wrong element even if it fired there.
---

# Phase 34.11: Library Filtering, Search, Views, Collections and Cross-Store Facets — Verification Report

**Phase Goal:** Replace the Games tab's filter controls with the tier-2 panel designed in sketch 004 (hybrid: views and collections one click, facets collapsed below, removable chips above the grid, facet counts excluding their own facet).
**Verified:** 2026-08-10T08:49:11Z
**Status:** passed
**Re-verification:** No — initial verification (no prior VERIFICATION.md existed)

## Verification Method

This phase did not execute cleanly: `34.11-REVIEW.md` (2026-08-10T08:15:06Z) found 4 CRITICAL
blockers and 19 warnings **after** all 9 plans reported complete and 4462 tests passed. Five
follow-up commits (`fa694bf39`, `9ebad6dca`, `aebaccff8`, `255cc16a3`, `4b635ecc4`) claim to fix
CR-01/CR-02/CR-03/CR-04/WR-13. Per the adversarial mandate, none of those fix claims were taken on
trust. For each one this session:

1. Read the actual diff/commit and the resulting source, not just the commit message.
2. Confirmed the fix is **wired** into `Library/index.tsx` (imported and consumed), not just added as an unused module.
3. Read the new regression test and confirmed it reproduces the **real production call shape** (the specific defect class the review named: a hand-written test that cannot fail on the bug it claims to guard).
4. Ran the tests myself (`npx jest ...`) rather than reading a claimed pass count.
5. Ran `tsc --noEmit`, `eslint`, the full Frontend Jest project (72 suites), and `pnpm lint-translations:gamelib` myself.

## Goal Achievement

### Code-Review Blocker Fix Verification (CR-01..04, WR-13)

| # | Finding | Fix commit | Verified in codebase | Verified by test |
|---|---------|-----------|----------------------|-------------------|
| 1 | CR-01: facet counts computed over already-filtered list → every unselected option showed 0, D-28 inert | `fa694bf39` | ✓ New `engineWiring.ts` (`buildGridPipeline`) hoists `libraryUnion` above the grid memo; `countForStore`/`countForRunnability` and the grid all read the SAME unfiltered union (`Library/index.tsx:619-754`). Confirmed no other call site still does `filterLibrary` → `countFor` on the filtered output. | ✓ `engineWiring.test.ts`: `pipeline.countForStore('steam')` returns `3` (not `0`) with `gog` selected — the review's own empirical repro, now asserted through the real `buildGridPipeline`, not a hand-replica. 14/14 tests pass. |
| 2 | CR-02: `Favourites` view returned 0 games on any default install (`favouriteKeys` gated on a deleted-UI display memo) | `9ebad6dca` | ✓ `buildEngineDeps`/`buildFavouriteKeys` in `engineWiring.ts` derive `favouriteKeys` directly from `favouriteGames.list` (unconditional `ContextProvider` source), independent of `libraryTopSection`/`showFavouritesLibrary`. Wired at `Library/index.tsx:681-704`. | ✓ `engineWiring.test.ts`: "the Favourites view returns the favourited games with libraryTopSection disabled and show_favorites false" passes. |
| 3 | CR-03: `var(--navbar-active)` undefined in 7/11 themes, no fallback → checked-checkbox indicator and Clear-all colour dropped entirely | `aebaccff8` | ✓ All 5 flagged declarations (`FilterFacetGroup/index.scss:43`, `FilterMoreGroup/index.scss:20`, `FilterChipRow/index.scss:29`, `FilterZeroResult/index.scss:20`) now hoist `--filter-active-color: var(--navbar-active, var(--accent-overlay, var(--accent)))` once and consume the local var — the same fallback chain `NavItem/index.scss` already used. Grepped: zero live (non-comment) uses of the bare `var(--navbar-active)` form remain in the four flagged files. | ✓ `themeTokens.test.ts` census (see WR-13 below) passes. |
| 4 | CR-04: `preset_uncategorized` sentinel leaked into chip text and the zero-result sentence | `4b635ecc4` | ✓ `PRESET_UNCATEGORIZED` exported once from `filterEngine.ts`, imported by `chipLabels.ts` (special-cased to resolve `header.uncategorized` before the literal branch) and by `FilterCollectionList/index.tsx` (already did). All three sites now share one constant — no duplicate string literal. | ✓ `FilterChipRow/__tests__/index.test.tsx`: "collection:preset_uncategorized is NOT treated as user data" passes; also asserts the constant is imported by identifier, not re-declared, in `FilterCollectionList`. |
| 5 | WR-13: `--navbar-active` gate checked one theme (`gruvbox_dark`) while reading as broad coverage; stayed green throughout CR-03 | `255cc16a3` | ✓ Replaced with a census over the same 11 `themeSelectors` the pre-existing `--divider` census uses (hoisted to module scope so both use the identical list). Asserts non-vacuity, no bare form, and the exact NavItem fallback chain, for all 4 consumer files. | ✓ `themeTokens.test.ts`: 31/31 tests pass, including the new census `describe` block. |

**All 4 CRITICAL findings and the WR-13 test-quality finding are genuinely fixed, wired, and covered by regression tests that reproduce the real production call shape — re-run in this session, not read off a SUMMARY.**

### Observable Truths (REQ-34.11-01 .. 17)

| # | Requirement | Status | Evidence |
|---|-------------|--------|----------|
| 1 | REQ-34.11-01: Exactly 3 collapsed facet groups (Store/Runnability/More), no 4th, Runnability renders nothing on Windows | ✓ VERIFIED | `Header/index.tsx` renders exactly `FilterStoreFacet`, `FilterRunnabilityFacet`, `FilterMoreGroup` after the two select-one sections — no Genre import anywhere. `FilterRunnabilityFacet/index.tsx:83` returns `null` when `runnabilityRows === []`; test `FilterRunnabilityFacet.test.tsx:100` asserts the Windows case returns exactly `null`. |
| 2 | REQ-34.11-02: Store/Runnability opt-in, AND across kinds / OR within a kind | ✓ VERIFIED | `filterEngine.test.ts` (in the 968-test green run) covers empty-selection = no constraint and the AND/OR combination; `engineWiring.test.ts` confirms the same through the production call shape. |
| 3 | REQ-34.11-03: Filter pipeline is pure functions, no React/jsdom/DOM | ✓ VERIFIED | `filterEngine.ts` and `engineWiring.ts` import nothing from `react`/`.scss`/`.css`; both header comments state the constraint and it holds — `testEnvironment: 'node'` jest config confirmed. |
| 4 | REQ-34.11-04: One implementation produces grid and counts, `filterByPlatform`/`crossoverRatingFilters` retired | ✓ VERIFIED | `buildGridPipeline` is the single call site for both (CR-01 fix, table above). `libraryPipeline.test.ts` (a source gate WR-14 correctly notes is narrow) still passes; the CR-01 fix closes the gap WR-14 flagged by adding a behavioural test outside that gate. |
| 5 | REQ-34.11-05: Exactly 4 single-select Views incl. Favourites, previous combinable pair gone | ✓ VERIFIED | `FilterViewList.test.tsx` passing; Favourites now returns real games post CR-02 fix (verified above), not an empty grid. |
| 6 | REQ-34.11-06: Persistence is explicit per filter kind; search is session-only; Clear all persists | ✓ VERIFIED | `LibraryContext.tsx`/`index.tsx` `storage.setItem` calls present per filter kind; covered by the passing `filterEngine.test.ts` / component suites. Not part of the CR fix set — unaffected regression risk, still green. |
| 7 | REQ-34.11-07: Legacy opt-out localStorage value discarded, not translated | ✓ VERIFIED | `migrateStoreFacetSelection`/`migrateRunnabilityFacetSelection` in `filterEngine.ts`; covered by `filterEngine.test.ts` (passing). |
| 8 | REQ-34.11-08: Collections are verbatim `customCategories.listCategories()` + literal `preset_uncategorized`, single-select, reuses `CategoriesManager` | ✓ VERIFIED | `FilterCollectionList/index.tsx` imports the shared `PRESET_UNCATEGORIZED` constant (post CR-04 fix); `FilterCollectionList.test.tsx` passing. |
| 9 | REQ-34.11-09: Runnability tiers use `CrossoverBadge`'s exact bucketing | ✓ VERIFIED | `deriveRunnabilityTier` in `filterEngine.ts`, single implementation imported by the panel (`runnabilityRowsForHost`) — `FilterRunnabilityFacet.test.tsx` passing. |
| 10 | REQ-34.11-10: Facet option counts exclude only their own facet, `tabular-nums` | ✓ VERIFIED (was FAILED pre-fix) | This is D-28/D-29, i.e. CR-01. Confirmed fixed and re-tested above. `FilterFacetGroup/index.scss` contains `font-variant-numeric: tabular-nums`. |
| 11 | REQ-34.11-11: `NavItem` button branch supports `className` + selected state, NavLink branch unchanged | ✓ VERIFIED | `NavItem.test.tsx` passing (5 original + extended cases). |
| 12 | REQ-34.11-12: 3 theme-token defect instances fixed and gated; CR-01(navbar-active)/divider closed by live 3-theme human sweep, CR-02(app-region) source-verified-only by design | ✓ VERIFIED | Source fixes present and re-tested (CR-03/WR-13 above; `-webkit-app-region: drag` present at `NavShell/index.scss` per `themeTokens.test.ts` `navbar app-region` describe block, passing). The live 3-theme sweep (midnightMirage/gruvbox_dark/dracula, 6/6 each) is documented in `34.11-09-SUMMARY.md` with a five-attempt falsification history for the divider (commits `4b446abd9`→`fa4fe7d77`) — the artifact it produced (1px `::after` pseudo-element divider) is present in `NavShell/index.scss`. This requirement is explicitly source-verified-plus-human-adjudicated per plan 03's own design (CR-02 cannot ever be live-verified under WKWebView), and that adjudication happened, per the SUMMARY's documented iteration history rather than a bare claim. |
| 13 | REQ-34.11-13: Every descriptor kind produces a removable chip with an interpolated accessible name | ✓ VERIFIED | `chipLabelSpec` switch is exhaustive (`never`-typed default, compile-time enforced); `aria-label={tGamelib('gamelib:library.filterPanel.removeFilter', 'Remove {{filterLabel}} filter', {filterLabel: label})}` confirmed at `FilterChipRow/index.tsx:170-174`. `FilterChipRow/__tests__/index.test.tsx` passing. WR-12 (an *unmapped* descriptor value inflating the count and emptying the sentence) remains an open, narrow edge case — see Warnings. |
| 14 | REQ-34.11-14: Zero-result state names the active filters, inline Clear-all | ✓ VERIFIED | `FilterZeroResult/index.tsx` uses the same `chipLabelSpec`/`joinChipLabels` the chip row uses, so the two cannot drift; passing tests. |
| 15 | REQ-34.11-15: Fuse.js unchanged, counts computed over the fuzzy-matched set | ✓ VERIFIED (was accidentally-true pre-fix per CR-01) | `searchMatchedKeys` is now hoisted above the grid memo and fed into `engineState`, which both the grid and `countFor` read (`Library/index.tsx:637-734`). `engineWiring.test.ts` "the search constraint narrows the sibling store count" and "an empty match set is honoured" both pass — this is the same fix as REQ-34.11-10/CR-01, now satisfied on purpose. |
| 16 | REQ-34.11-16: Panel mounts in UI-SPEC order from one mount point; `CategoryFilter`/`LibraryFilters` gone from disk and unreferenced; `LibrarySearchBar` untouched | ✓ VERIFIED | `Header/index.tsx` renders `LibrarySearchBar → FilterViewList → FilterCollectionList → FilterStoreFacet → FilterRunnabilityFacet → FilterMoreGroup`. `ls` confirms both retired component directories are gone from disk; `grep -rl` across `src/` finds zero remaining **imports** of either name (only stale comments — see WR-04 below). `LibrarySearchBar/index.tsx` has no commits from this phase in its `git log`. |
| 17 | REQ-34.11-17: Gates green and non-vacuous; i18n scope consistent; multi-theme sweep one-question-per-theme; gamepad measured-and-recorded only | ✓ VERIFIED (with one honestly-disclosed carry-forward) | `tier2Portal.test.ts` rewritten with real structural + disk-existence assertions (read directly, confirmed non-vacuous). Full Frontend Jest project: **72 suites / 968 tests, all passing**, re-run in this session. `pnpm lint-translations:gamelib` exits `0` (the printed `ENOENT` stack traces are for `gamelib.json`/`login.json` files in ~15 non-English locales that are a pre-existing, phase-unrelated backlog — see MEMORY.md "Phase 34.8-09/-12" — not new debt from 34.11). `tsc --noEmit` clean. Gamepad focus-scroll: honestly recorded as **NOT MEASURED** in `34.11-09-SUMMARY.md` (no hardware available), which is exactly what this requirement's own wording asks for ("measured and recorded only" — recording the absence of measurement satisfies the letter of the requirement). **However, this is the SECOND consecutive phase (34.10, then 34.11) to defer the identical measurement with no plan to actually perform it — flagged explicitly below, not silently carried a third time.** |

**Score:** 17/17 requirements have codebase evidence supporting them, independently re-verified (not read off SUMMARY.md).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/frontend/screens/Library/filterEngine.ts` | Pure filter/count/migration engine | ✓ VERIFIED | Exports match plan 01; no React/DOM imports. |
| `src/frontend/screens/Library/engineWiring.ts` | React-free wiring layer (CR-01/CR-02 fix) | ✓ VERIFIED | Not in original plans — added post-review; exists, exported, imported and used in `Library/index.tsx`, covered by 14 own tests. |
| `src/frontend/screens/Library/facetLabels.ts` | Single label declaration site (`RunnerToStore`, `RUNNABILITY_LABELS`) | ✓ VERIFIED | Imported by both `FilterStoreFacet`/`FilterRunnabilityFacet` (panel) and `chipLabels.ts` (chips) — one source, confirmed no duplicate table. |
| `src/frontend/components/UI/NavShell/components/{FilterViewList,FilterCollectionList,FilterFacetGroup,FilterStoreFacet,FilterRunnabilityFacet,FilterMoreGroup}` | Panel sections | ✓ VERIFIED | All present, all rendered from `Header/index.tsx`, all have passing dedicated test files. |
| `src/frontend/screens/Library/components/{FilterChipRow,FilterZeroResult}` | Chip row + zero-result state | ✓ VERIFIED | Present, mounted (`Library/index.tsx`), passing tests including the CR-04 sentinel regression. |
| `src/frontend/components/UI/CategoryFilter`, `src/frontend/components/UI/LibraryFilters` | Retired | ✓ VERIFIED DELETED | Confirmed absent from disk; zero remaining imports (only stale comments, WR-04). |
| `meta/i18nGateScope.json` | Lists new panel files, provenance matches | ⚠️ STALE PROVENANCE (WR-17, open) | File lists the 8 new panel files (confirmed present via grep), and `pnpm lint-translations:gamelib` passes with them included. But `baseCommit`/`generatedAt` claim a commit at which those files did not yet exist — hand-edited without regeneration (`git log` shows 3 manual-edit commits). A future `pnpm gen-i18n-gate-scope` run would silently drop them. Not a functional defect today; a latent one. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `Library/index.tsx` | `engineWiring.ts` | `buildGridPipeline`/`buildEngineDeps` import + call | ✓ WIRED | Confirmed by direct read + grep; `gridPipeline.games`/`countForStore`/`countForRunnability` are the only values feeding the grid/count UI. |
| `engineWiring.ts` | `filterEngine.ts` | `filterLibrary`/`countFor`/`gameKey` | ✓ WIRED | Confirmed import + call. |
| `chipLabels.ts` | `filterEngine.ts` | `PRESET_UNCATEGORIZED` import | ✓ WIRED | Confirmed; same constant consumed at 3 sites (`filterEngine.ts`, `chipLabels.ts`, `FilterCollectionList/index.tsx`). |
| `FilterFacetGroup/index.scss` etc. | `themes.scss` | `var(--navbar-active, var(--accent-overlay, var(--accent)))` | ✓ WIRED | Confirmed fallback chain present at all 4 flagged consumer files, matching `NavItem/index.scss`'s existing chain. |
| `Header/index.tsx` | 6 panel section components | direct import + render | ✓ WIRED | Confirmed order matches UI-SPEC; `tier2Portal.test.ts` asserts the same order via regex against the real file. |
| `Library/index.tsx` | `Header` | `createPortal` into tier-2 target | ✓ WIRED | `tier2Portal.test.ts` "every `<Header />` usage is wrapped in `createPortal`" passes. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| CR-01 regression: sibling store count is real, not 0 | `npx jest engineWiring.test.ts` | 14/14 pass | ✓ PASS |
| CR-03/WR-13 theme-token census | `npx jest themeTokens.test.ts` | 31/31 pass | ✓ PASS |
| CR-04 sentinel chip regression | `npx jest FilterChipRow/__tests__/index.test.tsx` (part of full run) | pass | ✓ PASS |
| Type safety | `npx tsc --noEmit` | clean, no output | ✓ PASS |
| Full Frontend suite (regression check across the whole phase's surface) | `npx jest --selectProjects Frontend` | 72 suites / 968 tests pass | ✓ PASS |
| i18n gate | `pnpm lint-translations:gamelib` | exit 0 (pre-existing non-English ENOENT noise, unrelated backlog) | ✓ PASS |
| Lint | `npx eslint src/frontend/screens/Library/index.tsx` | 0 errors, 21 warnings (pre-existing classes: WR-01/02/03 unsafe-assignment/exhaustive-deps) | ✓ PASS (no new errors) |

### Requirements Coverage

| Requirement | Source Plan(s) | Status | Evidence |
|-------------|-----------------|--------|----------|
| REQ-34.11-01 | 34.11-07 | ✓ SATISFIED | See truths table #1 |
| REQ-34.11-02 | 34.11-01, 04, 05 | ✓ SATISFIED | See truths table #2 |
| REQ-34.11-03 | 34.11-01 | ✓ SATISFIED | See truths table #3 |
| REQ-34.11-04 | 34.11-01, 05 | ✓ SATISFIED | See truths table #4 |
| REQ-34.11-05 | 34.11-04, 06 | ✓ SATISFIED | See truths table #5 |
| REQ-34.11-06 | 34.11-04 | ✓ SATISFIED | See truths table #6 |
| REQ-34.11-07 | 34.11-04 | ✓ SATISFIED | See truths table #7 |
| REQ-34.11-08 | 34.11-06 | ✓ SATISFIED | See truths table #8 |
| REQ-34.11-09 | 34.11-01, 05 | ✓ SATISFIED | See truths table #9 |
| REQ-34.11-10 | 34.11-01, 07 | ✓ SATISFIED | See truths table #10 (was failed pre-fix) |
| REQ-34.11-11 | 34.11-02 (consumed by 06) | ✓ SATISFIED | See truths table #11 |
| REQ-34.11-12 | 34.11-03 (closed by 09) | ✓ SATISFIED | See truths table #12 |
| REQ-34.11-13 | 34.11-08 | ✓ SATISFIED | See truths table #13 |
| REQ-34.11-14 | 34.11-08 | ✓ SATISFIED | See truths table #14 |
| REQ-34.11-15 | 34.11-01, 05 | ✓ SATISFIED | See truths table #15 |
| REQ-34.11-16 | 34.11-09 | ✓ SATISFIED | See truths table #16 |
| REQ-34.11-17 | 34.11-09 | ✓ SATISFIED | See truths table #17 |

No orphaned requirements: every ID REQUIREMENTS.md maps to Phase 34.11 (REQ-34.11-01..17) appears in at least one plan's `requirements:` frontmatter, and every plan's declared requirements are accounted for above.

**Bookkeeping note (informational, not a gap):** `REQUIREMENTS.md`'s status table (lines 381-397) marks REQ-34.11-01, 05, 06, 07, 08, 10, 13, 14 as `Planned` while the same file's body text (lines 835-851) checks all 17 off as `[x]` complete with plan citations. The body text is the authoritative, evidenced record (it cites the closing plan and decision IDs for each); the status table appears stale/unsynced. Not a functional gap — flagged so it gets corrected before milestone close.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/frontend/components/UI/Header/index.css` | 19 | `TODO move following styles...` | ℹ️ Info | Pre-existing since 2020 (`a679ce9b3`), not introduced by this phase. Still valid per WR-19 (unscoped global selectors). |
| `src/frontend/screens/Library/index.tsx` | 621 (react-hooks/exhaustive-deps) | Missing `makeLibrary` dependency on `libraryUnion` memo (WR-01) | ⚠️ Warning | Open, not part of the CR fix batch. A login/logout that changes only username (not the `.library` array identity) leaves a stale grid and, downstream, stale counts. Independently corroborated by ESLint in this session. |
| `src/frontend/screens/Library/index.tsx` | 287-298 vs `makeLibrary` (~589) | Amazon gated on `amazon.username` in `connectedStores` vs `amazon.user_id` in `makeLibrary`/library union (WR-03) | ⚠️ Warning | Open, confirmed still present this session. Can desync Amazon's Store row from Amazon's presence in the grid. |
| `src/frontend/screens/Library/index.tsx` | ~562 | Unguarded `JSON.parse` of `localStorage['nonAvailableGames']` in `engineDeps` memo, no try/catch (WR-02) | ⚠️ Warning | Open. Corrupt localStorage can white-screen the Library on render. |
| `src/frontend/screens/Library/index.tsx` | 118-199, 323-337, 851-870; `types.ts:274-295`; `LibraryContext.tsx:7-45` | ~90 lines of dead legacy filter state survive retirement of `LibraryFilters`/`CategoryFilter` (WR-04) | ⚠️ Warning | Open. Comments now actively misleading (e.g. `types.ts:312` says fields "stay until plan 09 retires LibraryFilters/CategoryFilter" — plan 09 has landed). Cosmetic/maintainability risk, not a functional defect. |
| `src/frontend/screens/Library/components/FilterZeroResult/index.tsx` | 89-93 | A descriptor with no `chipLabelSpec` mapping inflates `activeFilterCount` and empties the sentence (`"No games match ."`) (WR-12) | ⚠️ Warning | Open, narrow edge case (unmapped store value or forged `view:all`); not reachable through normal UI given the exhaustive switch, but the guard WR-12 recommends (`if (labels.length === 0) return null`) is not present. |
| `meta/i18nGateScope.json` | 2-5 | False provenance metadata (WR-17) | ⚠️ Warning | Open, confirmed this session — `baseCommit` predates the 8 files it lists. Functionally inert today (gate passes); latent risk on next regeneration. |
| `src/frontend/screens/Library/facetLabels.ts`, `.../FilterChipRow/chipLabels.ts` | — | Canonical label sources absent from `meta/i18nGateScope.json`'s `files` list (WR-18) | ⚠️ Warning | Open, confirmed this session. Declaration sites for English copy are outside the gate that checks their consumers. |
| Various | — | WR-05 (`recentAppNames` captured once at mount, never refreshes), WR-06 (module-scope `throw` in a dev-only drift guard), WR-07 (`resolveLabel` duplicated verbatim), WR-08 (`+ New collection`/`Manage collections` identical handlers), WR-09 (stale `currentCollection` silently empties the grid), WR-10/WR-11 (alphabet-letter zero state / `Clear all` interaction, explicitly out of scope per D-08), WR-16 (i18next-injection tests run against a mock, not real i18next), WR-19 (`Header/index.css` unscoped selectors + dead background rule) | ⚠️ Warning (9 items) | Confirmed still open — none were part of the 5-commit CR fix batch, none block a declared REQ-34.11-01..17 truth. Full detail in `34.11-REVIEW.md`. |

None of the above are debt markers (`TBD`/`FIXME`/`XXX`) — grepped across every file this phase modified; zero hits. The debt-marker BLOCKER gate does not fire.

### Human Verification Required

None required to close this phase. The one verification step that structurally required a human (the 3-theme legibility sweep for REQ-34.11-12) was already performed live during plan 09 execution (documented in `34.11-09-SUMMARY.md` with a 5-attempt falsification history for the divider fix, culminating in commit `fa4fe7d77`), and the artifact it produced is present and gated in the current codebase.

**Carried-forward risk, not a phase blocker:** gamepad focus-scroll behaviour in the tier-2 panel remains genuinely untested end-to-end (no physical gamepad available in either 34.10 or 34.11). This is the SECOND consecutive phase to defer the identical measurement. Recommend a future phase — ideally one touching NavShell/tier-2 layout again, or a dedicated hardware-access session — actually performs this measurement rather than deferring it a third time.

### Gaps Summary

No blocking gaps. The phase's own code review found 4 real, user-visible CRITICAL defects after all 9 plans and 4462 tests had gone green — a fifth instance of this project's recorded pattern of a live/adversarial check beating a green suite. All 4 were genuinely fixed in 5 follow-up commits, and this verification independently re-derived and re-ran the evidence for each fix (not the SUMMARY's claim of the fix) before accepting it: read the diff, confirmed it is wired into the component that had the defect, read the new regression test to confirm it reproduces the real production call shape rather than a hand-replica, and ran it.

19 WARNING-level findings from the code review remain open (WR-01..12, 15..19 minus 13). None of them contradict a declared REQ-34.11-01..17 truth or a plan's own must-have; all are pre-existing risk classes (stale deps, unguarded JSON.parse, dead code, provenance drift, one narrow unreachable-in-practice UI edge case) that the review itself classified as Warning rather than Critical. They are listed above for visibility and should feed a follow-up cleanup pass rather than block this phase.

The gamepad focus-scroll measurement is the one item worth escalating for attention: it is honestly recorded (satisfying the letter of REQ-34.11-17) but has now gone unperformed across two consecutive phases and risks becoming an invisible standing gap if deferred a third time.

---

_Verified: 2026-08-10T08:49:11Z_
_Verifier: Claude (gsd-verifier)_
