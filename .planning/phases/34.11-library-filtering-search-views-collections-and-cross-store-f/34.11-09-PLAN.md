---
phase: 34.11-library-filtering-search-views-collections-and-cross-store-f
plan: 09
type: execute
wave: 4
depends_on: ["34.11-06", "34.11-07", "34.11-08"]
files_modified:
  - src/frontend/components/UI/Header/index.tsx
  - src/frontend/components/UI/Header/index.css
  - src/frontend/components/UI/CategoryFilter/index.tsx
  - src/frontend/components/UI/LibraryFilters/index.tsx
  - src/frontend/components/UI/NavShell/Tier2PortalContext.tsx
  - src/frontend/screens/Library/__tests__/tier2Portal.test.ts
  - meta/i18nGateScope.json
autonomous: false
requirements:
  - REQ-34.11-16
  - REQ-34.11-17
user_setup:
  - service: local-dev-machine
    why: "The multi-theme sweep and the gamepad focus-scroll measurement are visual/hardware properties. Jest runs with testEnvironment: 'node' and has no jsdom, so nothing in this phase can be seen by an automated check until a human looks at it."
    env_vars: []
    dashboard_config:
      - task: "Launch the app with `pnpm tauri:dev` (NEVER `tauri dev` — that serves a stale static bundle and would show the pre-phase panel)"
        location: "repo root, local machine"
      - task: "Switch themes to midnightMirage, then gruvbox_dark, then dracula, answering the panel-legibility question separately for each theme"
        location: "Settings → Appearance → Theme"
      - task: "Connect a physical gamepad and navigate the Games tier-2 panel to observe whether focus stays visible while the list scrolls. MEASURE ONLY — record the result, do not build a fix"
        location: "local machine, Games tab"

must_haves:
  truths:
    - "The Games tier-2 panel actually renders the phase's work: opening the Games tab shows search, then Views, then Collections, then the Store / Runnability / More filters groups — in that order (UI-SPEC Panel Structure, REQ-34.11-16)."
    - "`components/UI/CategoryFilter` and `components/UI/LibraryFilters` no longer exist on disk and nothing in `src/` imports either of them (D-21, REQ-34.11-16)."
    - "`LibrarySearchBar` is still imported and still rendered first inside `Header` — it was never moved, never reimplemented, and never deleted alongside the two components that sat beside it (D-34)."
    - "`tier2Portal.test.ts` is green: its old `CategoryFilter`/`LibraryFilters` import-order assertions are replaced by assertions describing the new panel structure, and the replacements were each observed FAILING against a known-bad input before being accepted (REQ-34.11-17)."
    - "`pnpm test:ci` is green across all five Jest projects, with no test left skipped, renamed-away or deleted to achieve it."
    - "`pnpm lint-translations:gamelib` is green and `meta/i18nGateScope.json` lists no path that does not exist on disk (REQ-34.11-17)."
    - "The panel is legible in `midnightMirage`, `gruvbox_dark` AND `dracula` — confirmed by a human, one question per theme, never inferred from `midnightMirage` alone."
    - "NEGATIVE — gamepad focus-scroll behaviour in the taller, scrollable tier-2 panel is MEASURED and recorded in the SUMMARY. No fix, workaround, scroll-into-view call or focus handler is built for it in this phase; it is a Deferred Idea."
    - "NEGATIVE — no assertion anywhere in the repo is deleted, weakened or made unreachable in order to get the suite green. Coverage after this plan is greater than or equal to coverage before it."
  artifacts:
    - path: "src/frontend/components/UI/Header/index.tsx"
      provides: "The assembled tier-2 filter panel — LibrarySearchBar first, then the six new sections in UI-SPEC order"
      contains: "FilterViewList"
      min_lines: 25
    - path: "src/frontend/screens/Library/__tests__/tier2Portal.test.ts"
      provides: "The updated source gate — new panel-structure assertions replacing the retired-component ones"
      contains: "FilterMoreGroup"
    - path: "meta/i18nGateScope.json"
      provides: "Gate scope with the two retired component paths removed and every remaining path resolving to a real file"
  key_links:
    - from: "src/frontend/components/UI/Header/index.tsx"
      to: "src/frontend/components/UI/NavShell/components/FilterViewList"
      via: "direct import + render inside .Header"
      pattern: "FilterViewList"
    - from: "src/frontend/components/UI/Header/index.tsx"
      to: "src/frontend/components/UI/NavShell/components/FilterMoreGroup"
      via: "direct import + render as the last panel section"
      pattern: "FilterMoreGroup"
    - from: "src/frontend/screens/Library/index.tsx"
      to: "src/frontend/components/UI/Header/index.tsx"
      via: "createPortal(<Header />, tier-2 target) — UNCHANGED by this plan, still the only mount path"
      pattern: "createPortal"
---

<objective>
Make the phase visible and green. Plans 06, 07 and 08 built six components and a chip row that
nothing renders; this plan mounts them, deletes the two components they replace, repairs the source
gate that the deletion breaks, and drives the phase's blocking gates — full suite, i18n, and the
live multi-theme sweep — to green.

Purpose: this is the first and only point in the phase at which a user can see any of it. Plans 06
and 07 deliberately left `Header/index.tsx` untouched to avoid a three-way wave-3 file conflict, and
both deferred multi-theme verification here on the explicit grounds that "nothing is on screen until
plan 09." That deferral is now due.

Output: a rewritten `Header`, two deleted component directories, an updated `tier2Portal.test.ts`,
a cleaned gate scope, and a human-confirmed three-theme sweep plus a recorded gamepad measurement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-CONTEXT.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-UI-SPEC.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-VALIDATION.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-06-SUMMARY.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-07-SUMMARY.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-08-SUMMARY.md

<interfaces>
Everything below is already on disk when this plan runs. Do not rebuild any of it.

**Components to mount** (all default exports, all take **no props**, all read their own state from
`LibraryContext` via `useContext`):
- `src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` — plan 06
- `src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` — plan 06
- `src/frontend/components/UI/NavShell/components/FilterStoreFacet/index.tsx` — plan 07
  (returns `null` when `connectedStores` is empty)
- `src/frontend/components/UI/NavShell/components/FilterRunnabilityFacet/index.tsx` — plan 07
  (returns `null` on a Windows host, D-12)
- `src/frontend/components/UI/NavShell/components/FilterMoreGroup/index.tsx` — plan 07

`FilterFacetGroup` is a shared primitive consumed by the three facet components — **do not import or
render it directly from `Header`**; it takes a `title` and `children` and has no standalone meaning.

`FilterChipRow` (plan 08) lives **above the grid, not in the panel** (D-08 / UI-SPEC Panel
Structure). Plan 08 already mounted it in the grid header area. **Do not add it to `Header`.**

**The file being rewritten — `src/frontend/components/UI/Header/index.tsx`, all 18 lines as of
HEAD:**
it imports `LibrarySearchBar` from `'../LibrarySearchBar'`, `CategoryFilter` from
`'../CategoryFilter'`, `LibraryFilters` from `'../LibraryFilters'` and `'./index.css'`; it exports
`default function Header()` returning `<div className="Header">` containing
`<div className="Header__search"><LibrarySearchBar /></div>` followed by
`<span className="Header__filters"><CategoryFilter /><LibraryFilters /></span>`.

**`src/frontend/components/UI/Header/index.css`** currently declares `.Header` (a vertical flex
stack with `gap: var(--space-md)`), `.Header__filters`, `.Header__filters .FormControl`,
`.Header__search`, `.Header__search .FormControl`, `.Header__summary`, and then a trailing block
marked `/* TODO move following styles to the components which use them */` holding `.iconsWrapper`,
`.refreshIcon:hover` and `@keyframes refreshing`. **That trailing block is asserted by
`tier2Portal.test.ts` (`expect(headerCss).toMatch(/@keyframes refreshing/)`) and is used by other
components — it stays.**

**How `Header` reaches the screen — UNCHANGED by this plan.** `src/frontend/screens/Library/index.tsx`
renders `createPortal(<Header />, …)` from *inside* `LibraryContext.Provider` (~L819). Every mounted
section therefore receives the real provider value rather than `initialContext`'s silent no-op
setters. **Do not touch `Library/index.tsx`** — the existing `everyHeaderUsageIsPortalled()` gate in
`tier2Portal.test.ts` already protects that arrangement and must keep passing untouched.

**Complete reference-site inventory for the two retired components** (measured against HEAD; every
one of these must be resolved before the build is clean):

| Site | Kind | Disposition |
|---|---|---|
| `src/frontend/components/UI/Header/index.tsx:2,3,13,14` | real imports + JSX | replaced in Task 1 |
| `meta/i18nGateScope.json:10` (`CategoryFilter/index.tsx`), `:16` (`LibraryFilters/index.tsx`) | gate-scope paths | removed in Task 1 |
| `src/frontend/components/UI/NavShell/Tier2PortalContext.tsx:7,19,20` | doc comment naming the three portalled children | updated in Task 1 |
| `src/frontend/types.ts:262` | comment `See LibraryFilters/index.tsx + Library/index.tsx` | stale pointer — retarget or drop the dead half |
| `src/frontend/screens/StoreSearch/helpers.ts:76` | comment, "LibraryFilters-style tuple tables" | **leave alone** — describes a style, not a file |
| `src/frontend/components/UI/ThemeSelector/themeLabels.ts:18` | comment, "the LibraryFilters idiom" | **leave alone** — same reason |
| `meta/hardcodedStringGate.ts:87,111,1029` | comments citing real-world cases the gate handles | **leave alone** — gate logic, not imports |
| `meta/__tests__/hardcodedStringGate.test.ts:141,806,818` | **inline fixtures** named `LibraryFiltersFixture` etc. | **leave alone — these are string literals inside the test, not imports. Deleting them removes real gate coverage and is exactly the failure mode this plan's threat model is about.** |
| `meta/i18nGlossary.json:31` | rationale prose | **leave alone** |

`meta/__tests__/genI18nGateScope.test.ts` imports `../i18nGateScope.json` directly. Editing the
scope file can move that test — run it explicitly after the edit rather than assuming `test:ci`
covers you by accident.
</interfaces>

<i18n_contract>
**This plan mints ZERO new keys.** Every string on the panel was already minted by plans 06 and 07
(`gamelib:library.filterPanel.*` via the aliased-hook idiom `const { t: tGamelib } =
useTranslation('gamelib')`, plus the reused default-namespace `header.*` keys). `Header` itself
renders no text of its own — it is a container.

What this plan owes the gate is **subtractive**: `meta/i18nGateScope.json` currently lists
`src/frontend/components/UI/CategoryFilter/index.tsx` and
`src/frontend/components/UI/LibraryFilters/index.tsx`, both of which this plan deletes. A scope entry
pointing at a nonexistent file is at best noise and at worst a gate error.

`pnpm lint-translations:gamelib` is a **STANDING BLOCKING requirement** (Phase 34.8). A green jest
run does NOT imply a green gate — run it explicitly. Verify by reading
`public/locales/en/gamelib.json` that the fourteen keys plans 06 and 07 minted are actually present
in the catalog; do not assume they landed just because those plans reported green.
</i18n_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the panel in Header and retire CategoryFilter / LibraryFilters</name>
  <read_first>
    - src/frontend/components/UI/Header/index.tsx (all 18 lines — the file you are rewriting)
    - src/frontend/components/UI/Header/index.css (all of it — you are pruning two rules and must not touch the `@keyframes refreshing` block that `tier2Portal.test.ts` asserts)
    - .planning/phases/34.11-.../34.11-UI-SPEC.md lines 83-123 ("Panel Structure — component inventory" — the exact assembly order and the two "Discretion resolved" notes)
    - .planning/phases/34.11-.../34.11-06-SUMMARY.md and 34.11-07-SUMMARY.md (the exact default-export names and file paths the five components actually shipped under — do not guess a path)
    - src/frontend/components/UI/NavShell/Tier2PortalContext.tsx lines 1-25 (the doc comment naming `LibrarySearchBar` / `CategoryFilter` / `LibraryFilters` that you are updating)
    - meta/i18nGateScope.json (the `files` array — alphabetically ordered, with a `baseCommit` / `baseVersion` / `generatedAt` / `generatedBy` header you must not edit)
  </read_first>
  <files>src/frontend/components/UI/Header/index.tsx, src/frontend/components/UI/Header/index.css, src/frontend/components/UI/CategoryFilter/index.tsx, src/frontend/components/UI/LibraryFilters/index.tsx, src/frontend/components/UI/NavShell/Tier2PortalContext.tsx, meta/i18nGateScope.json</files>
  <action>
Rewrite `src/frontend/components/UI/Header/index.tsx` so it assembles the tier-2 panel.

Keep the component's name, file path, default-export shape and its `import './index.css'`. Keep the
outer `<div className="Header">`. Keep `import LibrarySearchBar from '../LibrarySearchBar'` and keep
`<div className="Header__search"><LibrarySearchBar /></div>` as the **first** child, byte-identical
in structure to today. **D-34: search is inherited, not rebuilt.** `Header` is already portalled into
the tier-2 slot, so search is already at the top of the panel — moving it, wrapping it differently,
or reimplementing it would be a deviation, not an improvement. Plan 06's `FilterViewList.test.tsx`
carries a source gate asserting this import and this render survive; if you break it that gate goes
red, which is the intended alarm.

Delete the `CategoryFilter` and `LibraryFilters` imports and the entire
`<span className="Header__filters">…</span>` wrapper.

After the search div, render the five new sections as direct children of `.Header`, in exactly this
order (UI-SPEC Panel Structure, lines 85-97):

1. `<FilterViewList />`
2. `<FilterCollectionList />`
3. `<FilterStoreFacet />`
4. `<FilterRunnabilityFacet />`
5. `<FilterMoreGroup />`

Import each from its own directory under
`src/frontend/components/UI/NavShell/components/…` — use the paths recorded in the 06 and 07
SUMMARYs verbatim. Pass **no props to any of them**: every one reads `LibraryContext` itself, and a
prop passed here would be a second, silently-diverging source of truth.

Do **not** render them inside a new wrapper element. `.Header` is already a vertical flex stack with
`gap: var(--space-md)`, and each section's own SCSS already declares
`margin-block-end: var(--space-sm)`. Adding a wrapper would introduce a third spacing authority.
Do **not** import or render `FilterFacetGroup` here — it is a primitive the facet components use
internally. Do **not** render `FilterChipRow` here — D-08 puts it in the grid header above the grid,
and plan 08 already mounted it there.

Do **not** add conditional rendering around `FilterStoreFacet` or `FilterRunnabilityFacet`. Each
already returns `null` on its own when it has nothing to show (empty `connectedStores`; Windows
host). A second guard here would be a duplicate of a decision those components own.

**Prune `src/frontend/components/UI/Header/index.css`.** Remove `.Header__filters` and
`.Header__filters .FormControl` — the element they styled no longer exists. Before removing
`.Header__summary`, run `grep -rn "Header__summary" src/` and remove it only if the count is zero
outside this CSS file; if anything still uses it, leave it. Leave `.Header`, `.Header__search`,
`.Header__search .FormControl`, and the entire trailing `/* TODO move following styles… */` block
(`.iconsWrapper`, `.refreshIcon:hover`, `@keyframes refreshing`) untouched — `tier2Portal.test.ts`
asserts `@keyframes refreshing` is still present, `.Header` is still `flex-direction: column`, and
that the file contains no `position: sticky`, no `space-between` and no `frameless`. Add no new rule
here: panel-section styling lives in the components' own scoped SCSS, and a rule added in this file
would be outside `.NavShell__tier2Portal`'s scope — the exact shape of 34.10's app-wide
`.MuiTabs-root` leak.

**Delete the two retired components (D-21).** Remove the directories
`src/frontend/components/UI/CategoryFilter/` and `src/frontend/components/UI/LibraryFilters/`
entirely (each holds only `index.tsx`) using `git rm -r`. Retired means gone, not left running
alongside — a dead component that still compiles is a second implementation waiting to be
re-imported.

Then resolve the reference sites listed in `<interfaces>`:
- Remove the two now-dangling paths from `meta/i18nGateScope.json`'s `files` array. Remove exactly
  two lines, add none, and change **nothing** else — not `baseCommit`, not `baseVersion`, not
  `generatedAt`, not `generatedBy`, and no other path. **Do not run `pnpm gen-i18n-gate-scope`**: it
  rewrites the header fields and produces a diff that buries the two lines that matter. Afterwards
  run `pnpm jest meta/__tests__/genI18nGateScope.test.ts --silent`, since that test imports this
  JSON directly.
- Update the doc comment at `src/frontend/components/UI/NavShell/Tier2PortalContext.tsx:7,19,20` so
  it names the components that are actually portalled now. Rewrite the prose only — do not change a
  single line of that file's code. A comment naming two deleted components is how a future reader
  concludes the portal contract is stale and "fixes" something that is correct.
- At `src/frontend/types.ts:262`, retarget or drop the `See LibraryFilters/index.tsx +
  Library/index.tsx` half of the comment that points at a deleted file. Leave the surrounding type
  declaration alone.
- **Leave every other match alone**, specifically `StoreSearch/helpers.ts:76`,
  `ThemeSelector/themeLabels.ts:18`, `meta/hardcodedStringGate.ts:87,111,1029`,
  `meta/i18nGlossary.json:31`, and above all the inline fixtures in
  `meta/__tests__/hardcodedStringGate.test.ts:141,806,818`. Those fixtures are string literals
  *inside* the test file, not imports of the deleted module. They still compile and still test real
  gate behaviour; deleting them to make a grep look tidy would silently delete gate coverage.
  </action>
  <acceptance_criteria>
    - Both directories are gone: `test ! -e src/frontend/components/UI/CategoryFilter && test ! -e src/frontend/components/UI/LibraryFilters` succeeds
    - No live import survives anywhere in `src/`: `grep -rn "from '.*CategoryFilter'\|from '.*LibraryFilters'" src/` returns nothing (exit 1)
    - All five sections are mounted exactly once each: `grep -cE "<FilterViewList />|<FilterCollectionList />|<FilterStoreFacet />|<FilterRunnabilityFacet />|<FilterMoreGroup />" src/frontend/components/UI/Header/index.tsx` returns 5
    - Order matches the UI-SPEC: `grep -c "LibrarySearchBar[\s\S]*FilterViewList[\s\S]*FilterCollectionList[\s\S]*FilterStoreFacet[\s\S]*FilterRunnabilityFacet[\s\S]*FilterMoreGroup" <(tr '\n' ' ' < src/frontend/components/UI/Header/index.tsx)` returns 1
    - D-34 held: `grep -c "import LibrarySearchBar from '../LibrarySearchBar'" src/frontend/components/UI/Header/index.tsx` returns 1 and `grep -c "<LibrarySearchBar />" ...` returns 1
    - No props leak in: `grep -cE "<Filter[A-Za-z]+ [a-zA-Z]" src/frontend/components/UI/Header/index.tsx` returns 0
    - The chip row and the shared primitive are not mounted here: `grep -cE "FilterChipRow|FilterFacetGroup" src/frontend/components/UI/Header/index.tsx` returns 0
    - The dead CSS rules are gone but the asserted block survives: `grep -c "Header__filters" src/frontend/components/UI/Header/index.css` returns 0, and `grep -c "@keyframes refreshing" ...` returns 1
    - No new rule was added to Header's CSS: `git diff --numstat src/frontend/components/UI/Header/index.css` shows 0 added lines
    - The gate scope shrank by exactly two lines: `git diff --numstat meta/i18nGateScope.json` shows `0` added and `2` deleted, and `git diff meta/i18nGateScope.json | grep -cE "baseCommit|baseVersion|generatedAt|generatedBy"` returns 0
    - No gate-scope path is dangling: every string in the `files` array resolves to an existing file (iterate the array and `test -f` each)
    - The hardcoded-string-gate fixtures are untouched: `git diff --stat meta/__tests__/hardcodedStringGate.test.ts` is empty
    - `Library/index.tsx` is untouched: `git diff --name-only HEAD -- src/frontend/screens/Library/index.tsx` is empty
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `pnpm jest meta/__tests__/genI18nGateScope.test.ts --silent` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && pnpm jest meta/__tests__/genI18nGateScope.test.ts --silent</automated>
  </verify>
  <done>The Games tier-2 panel is assembled in Header in UI-SPEC order, CategoryFilter and LibraryFilters are deleted with every live reference resolved, and no comment, fixture or gate entry was removed beyond the ones that actually pointed at the deleted files.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite tier2Portal.test.ts's panel-structure assertions and drive the full suite plus the i18n gate green</name>
  <read_first>
    - src/frontend/screens/Library/__tests__/tier2Portal.test.ts (the whole file — you are rewriting ONE `it` block inside the first `describe` and leaving the other three describes alone. The block is `'still renders LibrarySearchBar, CategoryFilter and LibraryFilters, in that order, and imports them unchanged'` at L109-117, and its four assertions are the ones that go red the moment Task 1 lands)
    - the same file's header comment (L1-21) and its `read()` / `cssBlock()` / `everyHeaderUsageIsPortalled()` helpers (L26-83) — reuse them, do not re-invent them, and note `read()` strips comments via `stripSourceComments` so an assertion cannot accidentally match commented-out code
    - src/frontend/components/UI/Header/index.tsx (Task 1's output — the literal text your new assertions must match)
    - .planning/phases/34.11-.../34.11-VALIDATION.md the "Header retirement" and "i18n gate" rows of the Per-Task Verification Map
  </read_first>
  <files>src/frontend/screens/Library/__tests__/tier2Portal.test.ts</files>
  <action>
Replace the single `it('still renders LibrarySearchBar, CategoryFilter and LibraryFilters, in that
order, and imports them unchanged', …)` block (L109-117) with assertions describing the panel
`Header` now builds. **Replace it — do not delete it and do not `it.skip` it.** A phase that removes
a failing assertion instead of updating it has reduced coverage while reporting green, which is the
precise defect this plan's threat model exists for.

Keep the other three `describe` blocks byte-identical. In particular
`everyHeaderUsageIsPortalled()` and the `LibraryContext.Provider`-ordering assertion must keep
passing untouched: they protect the arrangement that gives the newly mounted sections a real context
rather than `initialContext`'s silent no-op setters, and that protection matters more after this
phase than before it, because five new components now depend on it.

The replacement block asserts, against `read(HEADER_TSX)`:
- `LibrarySearchBar` is still imported from `'../LibrarySearchBar'` and still rendered (D-34 —
  keep this assertion verbatim from the old block; it is the one line of it that is still true).
- All five new sections are rendered: one assertion per component name, so a single missing section
  names itself in the failure output instead of hiding inside one compound regex.
- They appear in UI-SPEC order — a single ordered regex
  `/LibrarySearchBar[\s\S]*FilterViewList[\s\S]*FilterCollectionList[\s\S]*FilterStoreFacet[\s\S]*FilterRunnabilityFacet[\s\S]*FilterMoreGroup/`,
  mirroring the shape of the ordered regex the old block used.
- The retired components are gone: `expect(tsx).not.toMatch(/CategoryFilter/)` and
  `expect(tsx).not.toMatch(/LibraryFilters/)`.

Add one more `it` to the same `describe` asserting the two component files no longer exist on disk —
`existsSync(join(REPO_ROOT, 'src/frontend/components/UI/CategoryFilter/index.tsx'))` is `false`, and
the same for `LibraryFilters`. `readFileSync` and `join` are already imported at the top of the file;
add `existsSync` to the existing `fs` import rather than adding a second import statement. This is
what makes D-21 mean "deleted" rather than "unreferenced" — a file that still exists can be
re-imported by the next person who greps for a category filter.

**Every one of these assertions must be proven non-vacuous before you accept it.** A `not.toMatch`
that can never match passes for free, and an ordered `[\s\S]*` regex passes vacuously if the source
it reads is empty or if a name is misspelled. Prove each of the two classes at least once:
1. For the positive/ordered assertions — temporarily reorder two sections in
   `Header/index.tsx` (e.g. put `<FilterMoreGroup />` before `<FilterViewList />`), confirm the
   ordered test FAILS, then restore. Then temporarily misspell one component name in the *test*
   (e.g. `FilterViewLst`), confirm it FAILS, then restore — this second step is what proves the name
   you asserted is the name that is actually in the file.
2. For the `not.toMatch` negatives — temporarily insert `// CategoryFilter` into
   `Header/index.tsx`, confirm the test FAILS (`read()` strips comments, so if it does *not* fail,
   your probe was stripped: use a live identifier such as an unused `const CategoryFilter = 1`
   instead), then restore.
3. For the existence assertions — temporarily point one path at a file that does exist (e.g.
   `Header/index.tsx`), confirm it FAILS, then restore.
Record in the SUMMARY that each was observed failing. An assertion never seen red proves nothing.

Then drive the phase's automated gates green, in this order:
- `pnpm jest src/frontend/screens/Library/__tests__/tier2Portal.test.ts --silent`
- `pnpm jest src/frontend/components/UI/NavShell --silent`
- `npx tsc --noEmit -p tsconfig.json`
- `pnpm lint-translations:gamelib` — **BLOCKING.** A green jest run does not imply this. After it
  passes, open `public/locales/en/gamelib.json` and confirm the keys plans 06 and 07 minted are
  actually in the catalog (`viewAll`, `viewInstalled`, `viewRecentlyPlayed`, `viewFavourites`,
  `collections`, `newCollection`, `manageCollections`, `storeGroup`, `runnabilityGroup`,
  `runsNatively`, `runsViaBottle`, `wontRun`, `notYetChecked`, `moreFilters`). Verify, do not assume
  — the historical failure here was 16 of 48 keys measured absent from a catalog after a green run.
- `pnpm test:ci` — all five projects.

If any suite outside this phase's files goes red, **fix the cause, do not narrow the test.** If a
test genuinely encoded the old two-component structure and cannot be made true again, update it the
way you updated `tier2Portal.test.ts` — rewrite the expectation, never delete the case.
  </action>
  <acceptance_criteria>
    - The old assertions are gone and the file did not shrink into a stub: `grep -c "CategoryFilter\|LibraryFilters" src/frontend/screens/Library/__tests__/tier2Portal.test.ts` returns at least 2 (they now appear only inside `not.toMatch` / `existsSync` negatives), and `git diff --numstat src/frontend/screens/Library/__tests__/tier2Portal.test.ts` shows more lines added than deleted
    - No test was skipped or removed to reach green: `grep -cE "it\.skip|describe\.skip|xit\(|todo" src/frontend/screens/Library/__tests__/tier2Portal.test.ts` returns 0, and the file's total `it(` count is greater than or equal to HEAD's
    - All five sections are individually asserted: `grep -cE "FilterViewList|FilterCollectionList|FilterStoreFacet|FilterRunnabilityFacet|FilterMoreGroup" src/frontend/screens/Library/__tests__/tier2Portal.test.ts` returns at least 10 (five names × individual + ordered assertions)
    - The three untouched describes survive: `grep -c "everyHeaderUsageIsPortalled(libraryTsx)" src/frontend/screens/Library/__tests__/tier2Portal.test.ts` returns 1 and `grep -c "@keyframes refreshing" ...` returns 1
    - Deletion is asserted, not just non-reference: `grep -c "existsSync" src/frontend/screens/Library/__tests__/tier2Portal.test.ts` returns at least 2
    - Non-vacuity proven for all three assertion classes (ordered/positive, `not.toMatch` negative, existence), each observed FAILING against a deliberately-introduced known-bad input and then passing once restored — recorded in the SUMMARY
    - `pnpm jest src/frontend/screens/Library/__tests__/tier2Portal.test.ts --silent` exits 0
    - `pnpm lint-translations:gamelib` exits 0, and all fourteen phase keys are present: `grep -cE "viewAll|viewInstalled|viewRecentlyPlayed|viewFavourites|collections|newCollection|manageCollections|storeGroup|runnabilityGroup|runsNatively|runsViaBottle|wontRun|notYetChecked|moreFilters" public/locales/en/gamelib.json` returns 14
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `pnpm test:ci` exits 0 across all five Jest projects
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest src/frontend/screens/Library/__tests__/tier2Portal.test.ts --silent && pnpm lint-translations:gamelib && pnpm test:ci</automated>
  </verify>
  <done>tier2Portal.test.ts describes the new panel instead of the retired one, every replacement assertion has been observed failing against a known-bad input, and the full suite plus the blocking i18n gate are green with no test skipped, deleted or narrowed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live multi-theme sweep (one question per theme) and gamepad focus-scroll measurement</name>
  <read_first>
    - .planning/phases/34.11-.../34.11-VALIDATION.md the "Manual-Only Verifications" table (all three rows)
    - .planning/phases/34.11-.../34.11-UI-SPEC.md the "Color" and "States & Interactions" sections (what "legible" means here: row label, count, checkbox box, checked accent, chip, and the tier-2 right-edge border)
    - .planning/phases/34.11-.../34.11-07-SUMMARY.md (any token SUBSTITUTION plan 07 recorded during its token-survival check — a substituted token is the highest-risk thing on screen and should be looked at first)
  </read_first>
  <what-built>
The Games tier-2 panel is now assembled and mounted: `LibrarySearchBar`, then Views, then
Collections, then the collapsed Store / Runnability / More filters groups, with `FilterChipRow`
above the grid. `CategoryFilter` and `LibraryFilters` are deleted. `pnpm test:ci` and
`pnpm lint-translations:gamelib` are green.

Everything above is automated. What follows cannot be: `src/frontend/jest.config.js` sets
`testEnvironment: 'node'` with no jsdom, so no automated check in this repo has ever seen a rendered
pixel. Theme survival and gamepad focus are the two properties that only a human at the machine can
report.
  </what-built>
  <how-to-verify>
**Start the app with `pnpm tauri:dev`.** Do NOT use `tauri dev` — `frontendDist` is a directory, not
a dev server, so `tauri dev` serves a stale static bundle and would show you the pre-phase panel
while everything looked fine. Open the Games tab so the tier-2 panel is visible, and expand the
Store, Runnability and More filters groups so their rows are on screen.

**Ask the theme question ONCE PER THEME — three separate questions, three separate answers.** Do not
ask "does it look right in all three themes?" A single combined question has produced a false pass on
this project before, and 34.10's own gate protocol requires per-theme elicitation. Switch the theme
in Settings → Appearance before each question.

For each of `midnightMirage`, then `gruvbox_dark`, then `dracula`, ask separately whether ALL of the
following are legible in that theme:
1. Views and Collections row labels — and whether the selected one is visibly distinguishable from
   the unselected ones.
2. Facet row labels and their **counts** (the count column is the newest surface — check the digits
   are readable and do not jitter as they change).
3. The facet **checkbox boxes** — both unchecked (border visible against the panel background) and
   checked (accent fill visible).
4. The **chips** above the grid, including their `×` and the dashed `Clear all` chip.
5. The **tier-2 panel's right-edge border** — the `--divider` defect erased this in two themes once
   already, which is why it is named explicitly rather than folded into "does it look right".
6. The zero-count rows at `opacity: .38` — dimmed but still readable, not invisible.

Then, with a physical gamepad connected, navigate down through the tier-2 panel until the list
scrolls, and observe whether the focused row stays visible or scrolls out of view.

**This is a MEASUREMENT, not a fix.** Gamepad focus-scroll was never measured across four gate runs
in Phase 34.10, and the panel is considerably taller and now scrollable, so this is the first real
opportunity to observe it. Record the result — including "no gamepad available, not measured" if
that is the honest outcome. **Do not build a scroll-into-view handler, a focus manager, or any
remediation**: it is an explicit Deferred Idea and building it here would be scope the user did not
approve. If focus does scroll out of view, write that down and carry it forward.
  </how-to-verify>
  <resume-signal>Answer each of the three theme questions separately ("approved" or the specific defect), then report the gamepad focus-scroll observation ("stays visible" / "scrolls out of view" / "not measured — no gamepad")</resume-signal>
  <acceptance_criteria>
    - Three distinct theme answers were elicited and recorded — `midnightMirage`, `gruvbox_dark` and `dracula` each have their own recorded response in the SUMMARY. A single combined answer does not satisfy this criterion.
    - The app was launched with `pnpm tauri:dev`; `tauri dev` was not used
    - Any theme defect found is either fixed in the owning component's scoped SCSS (never in `Header/index.css`, which is outside `.NavShell__tier2Portal`'s scope) and re-verified in all three themes, or recorded as a carry-forward with the theme it appears in named
    - The gamepad focus-scroll observation is recorded verbatim in the SUMMARY, including a "not measured" outcome if no gamepad was available
    - NEGATIVE: `git diff --stat` shows no scroll-into-view, focus-manager or gamepad-handling code added anywhere. Measurement only.
  </acceptance_criteria>
  <verify>
    <human-check>Three separate per-theme confirmations plus one recorded gamepad observation</human-check>
  </verify>
  <done>The panel has been seen and confirmed legible by a human in all three themes, elicited one theme at a time, and the gamepad focus-scroll behaviour is recorded as an observation with no remediation built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification suite → "this phase is done" claim | This plan deletes two components and rewrites the one gate that asserted their presence. Every downstream decision about whether Phase 34.11 shipped correctly rests on assertions this plan is authorised to change. The suite is the trusted authority, and this plan can edit it — that is the boundary. |
| human sweep → theme-correctness claim | Nothing automated in this repo can see a rendered pixel (`testEnvironment: 'node'`, no jsdom). The only evidence that the panel is legible in `gruvbox_dark` and `dracula` is a human's answer, so how the question is asked is a security property of the process, not a formality. |
| `LibraryContext.Provider` → the five mounted sections | The sections read every filter setter from context. If `Header` were ever rendered outside the portal-from-inside-provider arrangement, they would receive `initialContext`'s silent no-op setters: rendering fine, responding to clicks, throwing nothing, and doing nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.11-25 | Tampering | `tier2Portal.test.ts`'s rewritten assertions | mitigate | The four assertions being replaced go red the instant Task 1 lands, so the cheapest path to green is deletion — that is the threat. Task 2 mandates **replace, never delete**, forbids `it.skip`/`xit`/`todo` by grep, and requires the file's `it(` count to be greater than or equal to HEAD's with more lines added than deleted. The replacement is one assertion per component name rather than one compound regex, so a missing section names itself instead of hiding. |
| T-34.11-26 | Tampering | any new grep or regex gate added by this plan | mitigate | An unmatchable pattern and a `not.toMatch` against something that could never appear both pass for free. Task 2 requires each of the three assertion classes — ordered/positive, negative, existence — to be observed **FAILING** against a deliberately-introduced known-bad input before acceptance, including a deliberate misspelling of a component name in the test (which is what proves the asserted name matches the real one) and a note that `read()` strips comments so a commented probe is not a valid known-bad input. Proof is recorded in the SUMMARY. |
| T-34.11-27 | Repudiation | the deletion sweep across the reference inventory | mitigate | `meta/__tests__/hardcodedStringGate.test.ts` contains inline fixtures literally named `LibraryFiltersFixture`; a `grep -r LibraryFilters` cleanup would delete real, still-valid gate coverage while looking like tidying. The `<interfaces>` table classifies **every** reference site as remove / update / leave-alone, and an acceptance criterion requires `git diff --stat meta/__tests__/hardcodedStringGate.test.ts` to be empty. |
| T-34.11-28 | Information disclosure | `meta/i18nGateScope.json` after the two paths are removed | mitigate | A scope file is a coverage allowlist: silently dropping a path removes a file from i18n enforcement. Here the two removals are correct because the files are deleted, and the acceptance criteria bound the edit to exactly `0` added / `2` deleted lines with the generated header untouched, plus an existence check that every remaining path resolves to a real file — so an over-broad removal is caught rather than shipped. |
| T-34.11-29 | Spoofing | the multi-theme sweep answer | mitigate | One combined "does it look right in all themes?" question has historically produced a false pass on this project, and `midnightMirage` alone is never sufficient. Task 3 mandates three separately-elicited questions with three separately-recorded answers, names the six specific surfaces to inspect (including the `--divider`-class right-edge border that vanished in two themes once already), and requires `pnpm tauri:dev` because `tauri dev` would render a stale bundle that looks green while proving nothing. |
| T-34.11-30 | Elevation of privilege | the gamepad measurement expanding into a fix | mitigate | Gamepad focus-scroll is a Deferred Idea. Task 3 is MEASURE-ONLY, accepts "not measured — no gamepad" as an honest outcome, and carries a negative acceptance criterion that `git diff --stat` shows no scroll-into-view, focus-manager or gamepad-handling code added. Unapproved scope is not a bonus. |
| T-34.11-31 | Denial of service | the portal arrangement in `Library/index.tsx` | accept | The five mounted sections would silently no-op if `<Header />` stopped being portalled from inside `LibraryContext.Provider`. This plan does not touch `Library/index.tsx`, and `everyHeaderUsageIsPortalled()` plus the provider-ordering assertion already guard it and are required to keep passing byte-identical. Adding a second guard here would fork the invariant across two files. |
| T-34.11-SC | Tampering | npm/pnpm/cargo installs | mitigate | **No packages are installed by this plan.** Every import already exists in the tree. If an install appears necessary, STOP — it is out of scope and requires a blocking human legitimacy checkpoint against `RESEARCH.md`'s package audit. |
</threat_model>

<verification>
- `pnpm test:ci` exits 0 across all five Jest projects, with the `it(` count in
  `tier2Portal.test.ts` greater than or equal to HEAD's and no `it.skip` / `xit` / `todo` anywhere in it
- `pnpm lint-translations:gamelib` exits 0 — STANDING BLOCKING requirement; a green jest run does
  not imply it — and all fourteen phase keys are confirmed present in
  `public/locales/en/gamelib.json` by reading the catalog, not by inference
- `npx tsc --noEmit -p tsconfig.json` exits 0
- `pnpm jest meta/__tests__/genI18nGateScope.test.ts --silent` exits 0 after the scope-file edit
- `grep -rn "from '.*CategoryFilter'\|from '.*LibraryFilters'" src/` returns nothing, and neither
  directory exists on disk
- Every path in `meta/i18nGateScope.json`'s `files` array resolves to an existing file
- Every assertion this plan rewrote or added has been observed FAILING against a deliberately
  introduced known-bad input and then passing once restored — ordered regex, `not.toMatch` negative,
  and `existsSync` check each proven separately. An unmatchable pattern passes vacuously.
- The live sweep was run from `pnpm tauri:dev` (never `tauri dev`) with **three separate per-theme
  answers** recorded for `midnightMirage`, `gruvbox_dark` and `dracula`
- The gamepad focus-scroll observation is recorded, with no remediation code added
- Untouched-file check: `git diff --name-only HEAD` must NOT list
  `src/frontend/screens/Library/index.tsx`,
  `meta/__tests__/hardcodedStringGate.test.ts`, `meta/hardcodedStringGate.ts`,
  `meta/i18nGlossary.json`, `src/frontend/screens/StoreSearch/helpers.ts`, or
  `src/frontend/components/UI/ThemeSelector/themeLabels.ts`
</verification>

<success_criteria>
- The Games tier-2 panel renders search → Views → Collections → Store → Runnability → More filters,
  in UI-SPEC order, from a single mount point in `Header/index.tsx` (REQ-34.11-16)
- `CategoryFilter` and `LibraryFilters` are deleted from disk with every live reference resolved,
  and `LibrarySearchBar` survives untouched (D-21, D-34)
- `tier2Portal.test.ts` asserts the new structure — including that the two retired files no longer
  exist — with no assertion deleted, skipped or weakened (REQ-34.11-17)
- Every rewritten or added gate was observed failing against a known-bad input before being accepted
- `pnpm test:ci` and `pnpm lint-translations:gamelib` are both green
- The panel is human-confirmed legible in `midnightMirage`, `gruvbox_dark` and `dracula`, elicited
  one question per theme
- Gamepad focus-scroll in the tier-2 panel is measured and recorded, with no fix built for it
</success_criteria>

<output>
Create `.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-09-SUMMARY.md` when done
</output>
