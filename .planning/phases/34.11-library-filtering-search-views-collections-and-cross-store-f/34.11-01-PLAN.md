---
phase: 34.11-library-filtering-search-views-collections-and-cross-store-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/types.ts
  - src/frontend/screens/Library/filterEngine.ts
  - src/frontend/screens/Library/__tests__/filterEngine.test.ts
autonomous: true
requirements:
  - REQ-34.11-02
  - REQ-34.11-03
  - REQ-34.11-04
  - REQ-34.11-09
  - REQ-34.11-15
user_setup: []

must_haves:
  truths:
    - "The library filter pipeline exists as named pure functions callable without React, jsdom or a DOM (VALIDATION.md Wave 0 prerequisite)."
    - "Store and Runnability facets are opt-in: an empty selection means no constraint (D-01)."
    - "Combination is AND across facet kinds, OR within a kind (D-03)."
    - "Legacy localStorage facet values are detected and reset to empty, never translated into an equivalent opt-in selection (D-02)."
    - "Runnability tiers are derived with CrossoverBadge's exact bucketing, including its undefined-vs-null distinction (D-09, D-10, D-11, D-16)."
    - "The Runnability facet computes zero rows on a Windows host, so the whole group has nothing to render (D-12 extension)."
    - "A facet option's count is computed with every active filter EXCEPT that facet's own (D-28, D-29)."
    - "Search participates in the pipeline as a precomputed matched-key set, so counts are computed over the fuzzy-matched set (D-33 amendment, REQ-34.11-15)."
  artifacts:
    - path: "src/frontend/screens/Library/filterEngine.ts"
      provides: "Pure filter predicates, runnability derivation, countFor, active-filter descriptors, localStorage migration helpers"
      exports:
        - "filterLibrary"
        - "countFor"
        - "deriveRunnabilityTier"
        - "runnabilityRowsForHost"
        - "describeActiveFilters"
        - "migrateStoreFacetSelection"
        - "migrateRunnabilityFacetSelection"
        - "gameKey"
    - path: "src/frontend/screens/Library/__tests__/filterEngine.test.ts"
      provides: "Direct-invocation unit coverage for every predicate, the skip rule, runnability bucketing and the D-02 migration"
    - path: "src/frontend/types.ts"
      provides: "LibraryView, RunnabilityTier, FacetKind, FilterEngineState, FilterEngineDeps, ActiveFilterDescriptor"
      contains: "export type RunnabilityTier"
  key_links:
    - from: "src/frontend/screens/Library/filterEngine.ts"
      to: "src/frontend/types.ts"
      via: "type imports"
      pattern: "from 'frontend/types'"
    - from: "src/frontend/screens/Library/__tests__/filterEngine.test.ts"
      to: "src/frontend/screens/Library/filterEngine.ts"
      via: "direct function invocation, no render()"
      pattern: "from '../filterEngine'"
---

<objective>
Extract the Games library filter pipeline into named pure functions, and add the opt-in facet
types the rest of Phase 34.11 builds on.

Purpose: `34.11-VALIDATION.md`'s Wave 0 requirement. `src/frontend/screens/Library/index.tsx`'s
`gamesForAlphabetFilter` `useMemo` currently holds every filter stage inline. The Frontend jest
project runs `testEnvironment: 'node'` with no jsdom and no `react-test-renderer`, so nothing
inside that `useMemo` is reachable from a test. Without this extraction, none of the count logic,
the exclude-your-own-facet rule, the runnability derivation or the D-02 migration can be verified
at all. It is also the only way to avoid implementing the pipeline twice (once for the grid, once
for counts) — the single most likely source of a counts-vs-grid mismatch in this phase.

Output: `filterEngine.ts` (new, pure, React-free), its unit test file, and the new facet types.
This plan changes NO behaviour — `Library/index.tsx` is not touched here and still runs its own
inline pipeline. Plan 04 adopts the state model; plan 05 rewires the pipeline onto this engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-CONTEXT.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-RESEARCH.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-VALIDATION.md

<interfaces>
Contracts the executor must build against. Extracted from the codebase — do not go looking for
these, they are reproduced here.

From `src/common/types.ts` (GameInfo, the only runnability-relevant fields that exist):
- `is_mac_native?: boolean`   (L221)
- `mac_arch?: '32' | '64' | 'unknown'`   (L227)
- `is_linux_native?: boolean`   (L228)
- There is **no Windows-native field of any kind.** Confirmed by full read. Do not invent one.

From `src/frontend/types.ts` (existing, still present after this plan):
- `export interface StoresFilters { legendary; gog; nile; sideload; zoom; steam }` — all booleans
- `export interface PlatformsFilters { win; linux; mac; browser }` — all booleans
- `export interface CrossoverRatingFilters { gold; silver; bronze; wontRun; unrated }` — all booleans
- `export type FilterMode = 'off' | 'show' | 'only'`
These three interfaces are the LEGACY shapes. They stay declared for now (plan 09 retires them);
this plan only needs them to write the D-02 legacy-shape detector.

From `src/frontend/state/GlobalStateV2.ts`:
- `crossoverRatings: Record<string, number | null>` — key is `app_name`. A key that is **absent**
  from the map means the app was never looked up. A key present with value `null` means looked up,
  no data. These are two different states (D-16 honesty invariant).

From `src/frontend/screens/Library/components/GameCard/CrossoverBadge.tsx:28-43` — the exact
bucketing to reuse verbatim:
- `rating === undefined` → return null / omit entirely
- `rating === null` → `'unknown'`
- `rating >= 5` → `'gold'`; `rating === 4` → `'silver'`; `rating === 3` → `'bronze'`
- otherwise (`rating <= 2`) → `'wontRun'`

From `src/frontend/screens/Library/index.tsx`, the stages to extract (read the file; do not
re-derive from memory):
- L476-523 `makeLibrary()` — the store union, gated by `storesFilters[x] && x.username`
- L525-585 — favourites branch, DLC exclusion, `currentCustomCategories` collection filter
  (including the `preset_uncategorized` special case), `showSupportOfflineOnly`,
  `showThirdPartyManagedOnly`, `showUpdatesOnly`, `showInstalledOnly`
- L359-408 `filterByPlatform()` — the literal-platform filter this phase retires
- L601-616 — the `Fuse.js` search (`threshold: 0.4`, keys `['title','normalizedTitle']`)
- L621-676 — the `showHidden`/`showNonAvailable` tri-state block and the macOS
  `crossoverRatingFilters` block

From `src/frontend/screens/Library/components/RecentlyPlayed/index.tsx:20` — the recent-games
source: `configStore.get('games.recent', [])`, an array whose entries carry `appName`.

Test idiom (no jsdom exists — see `src/frontend/jest.config.js`):
`src/frontend/screens/Library/components/GameCard/__tests__/CrossoverBadge.test.tsx` — module-level
`jest.mock('react-i18next', ...)`, then the unit is called as a plain function and the returned
object graph is inspected. `filterEngine.ts` has no React import at all, so it needs no mocks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the opt-in facet types and the engine's state/deps shapes</name>
  <read_first>
    - src/frontend/types.ts (whole file — you are appending to an existing type module and must
      match its export style and comment conventions)
    - src/common/types.ts lines 194-247 (the GameInfo runnability fields, confirm for yourself
      that no Windows-native field exists)
    - .planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-CONTEXT.md
      (D-01 through D-12, D-26 through D-29)
  </read_first>
  <files>src/frontend/types.ts</files>
  <action>
Append the following exported types to `src/frontend/types.ts`. Do NOT remove or edit
`StoresFilters`, `PlatformsFilters`, `CrossoverRatingFilters`, `FilterMode` or `LibraryContextType`
in this plan — they stay live until plan 09 retires them, so the tree keeps compiling while
`LibraryFilters` and `CategoryFilter` still exist.

Add exactly these names:

`export type LibraryView = 'all' | 'installed' | 'recentlyPlayed' | 'favourites'` — the D-05
single-select view set. `'all'` is the default and is the only value that produces no chip (D-26).

`export type RunnabilityTier = 'native' | 'bottle' | 'wontRun' | 'notChecked'` — the D-09/D-10
runnability facet rows. `'bottle'` is the collapsed gold/silver/bronze bucket (D-10 — medal tiers
are evidence, not user-facing controls). `'notChecked'` is the `rating === null` row.

`export type StoreFacetValue = 'legendary' | 'gog' | 'nile' | 'sideload' | 'zoom' | 'steam'` —
the same six runner keys `StoresFilters` uses, expressed as a value union so an opt-in selection
can be a plain array.

`export type FacetKind = 'view' | 'collection' | 'store' | 'runnability' | 'search' | 'more'` —
the `skip` parameter's domain for the exclude-your-own-facet rule (D-28).

`export interface FilterEngineState` with fields:
`view: LibraryView`; `collection: string | null` (a `customCategories` key, or the literal
`'preset_uncategorized'`, or `null` for no constraint — D-21 makes collections single-select);
`stores: StoreFacetValue[]` (opt-in, `[]` = no constraint — D-01);
`runnability: RunnabilityTier[]` (opt-in, `[]` = no constraint — D-01);
`searchMatchedKeys: Set<string> | null` (`null` = no search active; otherwise the set of
`gameKey()` values the search matched — see Task 2 for why this is precomputed);
`showHidden: FilterMode`; `showNonAvailable: FilterMode`; `showSupportOfflineOnly: boolean`;
`showThirdPartyManagedOnly: boolean`; `showUpdatesOnly: boolean`.

`export interface FilterEngineDeps` with fields:
`hiddenAppNames: string[]`; `nonAvailableAppNames: string[]`;
`favouriteKeys: Set<string>` (keyed by `gameKey()`, i.e. `${app_name}_${runner}`, matching the
existing `favouritesIds` shape at `Library/index.tsx:472-474`);
`recentAppNames: string[]`; `customCategories: Record<string, string[]>`;
`gameUpdates: string[]`; `crossoverRatings: Record<string, number | null>`;
`hostPlatform: string` (the value of `ContextProvider`'s `platform`, i.e. `'darwin'`, `'linux'`
or `'win32'`).

`export interface ActiveFilterDescriptor` with fields:
`id: string` (stable, unique within one render — e.g. `store:gog`, `runnability:bottle`,
`view:installed`, `collection:Backlog`, `search`, `showHidden:only`);
`kind: 'view' | 'collection' | 'store' | 'runnability' | 'search' | 'showHidden' | 'showNonAvailable' | 'showSupportOfflineOnly' | 'showThirdPartyManagedOnly' | 'showUpdatesOnly'`;
`value: string` (the facet value, the collection name, the raw search term, or the FilterMode
value `'show'`/`'only'` for a tri-state).

Give each type a short comment naming the decision ID it implements (D-01, D-05, D-09, D-21, D-26)
so the mapping survives into the source, matching how `CrossoverRatingFilters` already cites D-17
at `types.ts:258-262`.
  </action>
  <acceptance_criteria>
    - `grep -c "export type LibraryView" src/frontend/types.ts` returns 1
    - `grep -c "export type RunnabilityTier" src/frontend/types.ts` returns 1
    - `grep -c "export type FacetKind" src/frontend/types.ts` returns 1
    - `grep -c "export interface FilterEngineState" src/frontend/types.ts` returns 1
    - `grep -c "export interface FilterEngineDeps" src/frontend/types.ts` returns 1
    - `grep -c "export interface ActiveFilterDescriptor" src/frontend/types.ts` returns 1
    - `StoresFilters`, `PlatformsFilters`, `CrossoverRatingFilters` and `LibraryContextType` are
      all still present and unedited: `grep -c "export interface StoresFilters" src/frontend/types.ts`
      returns 1 and `git diff src/frontend/types.ts` shows only additions, no deletions
    - `pnpm tsc --noEmit` (or the project's typecheck script) exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The new facet types compile and the three legacy filter interfaces are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Build filterEngine.ts — pure predicates, runnability derivation, countFor, migration</name>
  <read_first>
    - src/frontend/screens/Library/index.tsx (whole file — the stages you are extracting are at
      L359-408, L476-523, L525-585, L601-616, L621-676; read them, do not reconstruct them)
    - src/frontend/screens/Library/components/GameCard/CrossoverBadge.tsx (L25-43 — the bucketing
      you must reuse verbatim, including the undefined/null distinction)
    - src/frontend/types.ts (the types you added in Task 1)
    - .claude/skills/sketch-findings-gamelib/references/library-filtering.md (the "Filter Engine"
      section's `filtered(st, { skip })` shape and the "Facet counts exclude their own facet" rule)
  </read_first>
  <files>src/frontend/screens/Library/filterEngine.ts</files>
  <action>
Create `src/frontend/screens/Library/filterEngine.ts`. It must import NOTHING from `react`,
`react-dom`, `react-i18next` or any `.css`/`.scss` file — it is a pure module so the Frontend jest
project (`testEnvironment: 'node'`, no jsdom) can import and call it directly.

Export these, with exactly these names and signatures:

`gameKey(game: GameInfo): string` — returns `` `${game.app_name}_${game.runner}` ``, matching the
key shape `Library/index.tsx:472-474` already builds for `favouritesIds`.

`deriveRunnabilityTier(game: GameInfo, ratings: Record<string, number | null>, hostPlatform: string): RunnabilityTier | null`
— returns `null` when the game has no computable tier on this host, which is D-12's "hide rows it
cannot compute" applied per game. Rules, in this precedence order:
  1. If `hostPlatform === 'win32'` return `null` unconditionally. `GameInfo` carries no
     Windows-native signal, so every row is uncomputable there (D-12 extension block, operator
     decision recorded 2026-08-09). Do NOT fall back to a degenerate all-pass row.
  2. If `hostPlatform === 'linux'`: return `'native'` when `game.is_linux_native === true`,
     otherwise `null`. There is no Linux bottle/won't-run signal, so no other row exists.
  3. `hostPlatform === 'darwin'`: `game.is_mac_native === true` → `'native'` (D-09: native takes
     priority over any CrossOver rating). Otherwise `game.mac_arch === '32'` → `'bottle'`
     regardless of rating (D-11: GPTK aborts 32-bit, Phase 18 routes it to a bottle). Otherwise
     read `const rating = ratings[game.app_name]` and apply CrossoverBadge's bucketing:
     `undefined` → `null` (never looked up — omit from ALL counts, D-16 honesty invariant, and
     specifically NOT the same as `notChecked`); `null` → `'notChecked'`; `>= 5`, `=== 4`,
     `=== 3` → all collapse to `'bottle'` (D-10); `<= 2` → `'wontRun'`.

`runnabilityRowsForHost(hostPlatform: string): RunnabilityTier[]` — the rows the Runnability group
may render on this host. `'win32'` → `[]` (the caller renders no group at all, not an empty one).
`'linux'` → `['native']`. `'darwin'` → `['native', 'bottle', 'wontRun', 'notChecked']`.

Per-stage predicates, each `(game, ...) => boolean`:
`passesView(game, view, deps)` — `'all'` always true; `'installed'` is `game.is_installed`;
`'favourites'` is `deps.favouriteKeys.has(gameKey(game))`; `'recentlyPlayed'` is
`deps.recentAppNames.includes(game.app_name)`.
`passesCollection(game, collection, deps)` — `null` always true. `'preset_uncategorized'` is true
when `gameKey(game)` appears in NO value array of `deps.customCategories` (port the logic at
`Library/index.tsx:541-553`, which flattens `Object.values(customCategories)` into a set). Any
other value is true when `deps.customCategories[collection]` contains `gameKey(game)`.
`passesStore(game, stores)` — `stores.length === 0` is true (D-01 opt-in: empty means no
constraint). Otherwise `stores.includes(game.runner as StoreFacetValue)`. This is OR-within-a-kind
(D-03).
`passesRunnability(game, runnability, deps)` — `runnability.length === 0` is true. Otherwise
derive the tier and return `tier !== null && runnability.includes(tier)`. A game whose tier is
`null` is excluded whenever the facet is active, which is what "omit from all counts" means in
practice.
`passesSearch(game, searchMatchedKeys)` — `null` is true; otherwise
`searchMatchedKeys.has(gameKey(game))`.
`passesMore(game, state, deps)` — the D-06/D-07 group. Port `Library/index.tsx:570-580`'s
`showSupportOfflineOnly` (`game.canRunOffline`), `showThirdPartyManagedOnly`
(`!!game.thirdPartyManagedApp`) and `showUpdatesOnly` (`deps.gameUpdates.includes(game.app_name)`),
plus the tri-state block at L639-654 verbatim including its both-`'only'` union case: when
`showHidden === 'only' && showNonAvailable === 'only'` the predicate is
`isNonAvailable(game) || isHidden(game)`; when only one is `'only'` it is that one alone; otherwise
each `'off'` value excludes its own set and `'show'` excludes nothing. `isHidden` is
`deps.hiddenAppNames.includes(game.app_name)`; `isNonAvailable` is
`deps.nonAvailableAppNames.includes(game.app_name) || (game.runner === 'steam' && !!game.is_delisted)`
— note the Steam-delisted clause, it is part of the shipped behaviour at L627-634 and must not be
dropped. Do NOT read `localStorage` inside the engine; `nonAvailableAppNames` is passed in.

`filterLibrary(library: GameInfo[], state: FilterEngineState, deps: FilterEngineDeps, opts?: { skip?: FacetKind }): GameInfo[]`
— applies every stage whose kind is not `opts.skip`, in this order: search, view, collection,
store, runnability, more. This is AND across kinds (D-03). Also unconditionally excludes DLC
(`game.install.is_dlc`), matching `Library/index.tsx:533`; DLC exclusion is not a facet and is
never skipped.

`countFor(library, state, deps, kind: 'store' | 'runnability', value: string): number` — the
exclude-your-own-facet rule (D-28, sketch reference § 2). Implemented as
`filterLibrary(library, state, deps, { skip: kind })` then counting the survivors that match
`value` for that kind: for `'store'`, `game.runner === value`; for `'runnability'`,
`deriveRunnabilityTier(...) === value`. Do not reimplement any stage inline here.

`describeActiveFilters(state: FilterEngineState): ActiveFilterDescriptor[]` — D-26's "chips render
for everything non-default, including views". Emits nothing for `view === 'all'`, nothing for
`collection === null`, nothing for an empty `stores`/`runnability` array, nothing for a `''`/absent
search term, and nothing for a tri-state at `'off'` or a boolean at `false`. Emits one descriptor
per selected store value and per selected runnability tier. Take the raw search term as a second
parameter `searchTerm: string` so the descriptor can carry it as `value`.

`migrateStoreFacetSelection(rawNew: string | null, rawLegacy: string | null): StoreFacetValue[]`
and `migrateRunnabilityFacetSelection(rawNew: string | null, rawLegacy: string | null): RunnabilityTier[]`
— D-02. `rawNew` is the new key's raw `localStorage` string, `rawLegacy` is the legacy key's. If
`rawNew` parses to an array of valid member strings, return it filtered to valid members. In EVERY
other case return `[]`. Specifically: a legacy all-boolean object (the opt-out mask) must be
DISCARDED, never inverted into an equivalent opt-in set — D-02 forbids translation, and this is a
semantics migration, not the format migration `migrateFilterMode` at `Library/index.tsx:185-189`
performs. Malformed JSON must be caught and also yield `[]`. Do not throw for any input.

Head the file with a docstring naming: the no-React purity constraint and why (Wave 0 /
`testEnvironment: 'node'`), the single-implementation rule (the grid and the counts must both go
through `filterLibrary`, because two implementations will drift — RESEARCH Anti-Patterns), and the
fact that `searchMatchedKeys` is precomputed by the caller because `Fuse.js` stays in
`Library/index.tsx` per the operator's `/gsd-plan-phase` decision (D-33 amendment) — search is
NOT substring in this codebase and no task in this phase removes `Fuse`.
  </action>
  <acceptance_criteria>
    - The file contains no `import` of `react`, `react-dom`, `react-i18next`, `.css` or `.scss`:
      `grep -E "^import .*(react|\.css|\.scss)" src/frontend/screens/Library/filterEngine.ts | wc -l` returns 0
    - All ten public names are exported. `grep -cE "^export (function|const) (gameKey|deriveRunnabilityTier|runnabilityRowsForHost|passesView|passesCollection|passesStore|passesRunnability|passesSearch|passesMore|filterLibrary|countFor|describeActiveFilters|migrateStoreFacetSelection|migrateRunnabilityFacetSelection)" src/frontend/screens/Library/filterEngine.ts` returns 14
    - `runnabilityRowsForHost('win32')` returns an empty array (proved by Task 3's test)
    - `countFor` calls `filterLibrary` and does not re-implement any predicate:
      `grep -v '^\s*//' src/frontend/screens/Library/filterEngine.ts | grep -c "filterLibrary(" ` is at least 2
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>filterEngine.ts compiles, is React-free, and exposes every predicate plus countFor, the runnability derivation and both migration helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Unit-test the engine — skip rule, runnability bucketing, host gating, D-02 migration</name>
  <read_first>
    - src/frontend/screens/Library/filterEngine.ts (the module you are testing)
    - src/frontend/screens/Library/components/GameCard/__tests__/CrossoverBadge.test.tsx (the
      direct-invocation idiom and its `it.each` table style — copy that shape)
    - src/frontend/jest.config.js (confirm for yourself that testEnvironment is 'node' before
      writing anything that would need a DOM)
  </read_first>
  <files>src/frontend/screens/Library/__tests__/filterEngine.test.ts</files>
  <action>
Create `src/frontend/screens/Library/__tests__/filterEngine.test.ts`. Use plain direct invocation —
no `render()`, no `@testing-library/react`, no `react-test-renderer`; none of them are installed
and importing one throws module-not-found at run time.

Build a small in-file fixture factory `makeGame(overrides: Partial<GameInfo>): GameInfo` casting a
minimal object literal, and a `makeDeps(overrides)` returning a `FilterEngineDeps` with empty
defaults. Do not import real library JSON.

Cover, with these test names so the VALIDATION.md `-t` patterns resolve:

A `describe('filter engine')` block:
- an empty `stores` array imposes no constraint, and a non-empty one is OR-within-kind: `['gog']`
  keeps a `gog` game and drops a `steam` one; `['gog','steam']` keeps both (D-01, D-03)
- `stores` and `runnability` together are AND-across-kinds: a GOG game whose tier is `wontRun`
  survives `{stores:['gog']}` alone but not `{stores:['gog'], runnability:['native']}` (D-03)
- DLC is always excluded regardless of `skip`
- `passesMore`'s both-`'only'` case returns the UNION of hidden and non-available, not the
  intersection
- a Steam game with `is_delisted: true` counts as non-available even when
  `nonAvailableAppNames` is empty

A `describe('count')` block:
- `countFor(library, {stores:['gog']}, deps, 'store', 'steam')` returns the number of Steam games
  that pass every OTHER active filter — i.e. it is NOT 0 just because `gog` is selected. Assert a
  concrete non-zero number against a fixture with known contents. This is the single assertion the
  whole exclude-your-own-facet rule rests on (D-28) — write the fixture so a self-counting
  implementation would return 0 here and therefore fail.
- `countFor` for `'runnability'` likewise ignores the current `runnability` selection

A `describe('runnability')` block, as an `it.each` table mirroring CrossoverBadge.test.tsx:
- `hostPlatform:'darwin'` — `is_mac_native:true` → `'native'` even when a `wontRun` rating exists
  (D-09 precedence); `mac_arch:'32'` → `'bottle'` even when rating is 1 (D-11); rating 5/4/3 all
  → `'bottle'` (D-10); rating 2 and 1 → `'wontRun'`; rating `null` → `'notChecked'`; app absent
  from the ratings map (`undefined`) → `null`, and assert explicitly that this is NOT
  `'notChecked'` (D-16)
- `hostPlatform:'linux'` — `is_linux_native:true` → `'native'`, otherwise `null`
- `hostPlatform:'win32'` — `deriveRunnabilityTier` returns `null` for a game with every field set,
  and `runnabilityRowsForHost('win32')` returns `[]`. Name this test so it reads as the D-12
  extension decision, e.g. "renders no runnability rows at all on a Windows host".

A `describe('store facet')` block:
- a runner with no games still yields a count of 0 rather than throwing — the row-hiding decision
  for an unconnected account (D-04) lives in the component, but the engine must not crash on it

A `describe('migrat')` block (the `-t "migrat"` pattern in VALIDATION.md keys on this):
- a legacy all-`true` `storesFilters` object passed as `rawLegacy` yields `[]`, NOT six selected
  stores — assert the returned array's length is 0 and add an inline comment citing D-02's
  "discard, do not translate" rule
- a legacy `platformsFilters`/`crossoverRatingFilters` object likewise yields `[]` from
  `migrateRunnabilityFacetSelection`
- malformed JSON (`'{not json'`) yields `[]` and does not throw
- a valid new-shape array round-trips, and unknown members are filtered out

A `describe('empty')` block:
- `describeActiveFilters` returns `[]` for a fully default state (`view:'all'`, null collection,
  empty arrays, `'off'` tri-states, empty search)
- `view:'installed'` produces exactly one descriptor and `view:'all'` produces none (D-26)
- a tri-state at `'only'` produces a descriptor whose `value` is `'only'`, and one at `'show'`
  produces one whose `value` is `'show'` — the chip component turns these into the two distinct
  plain-language labels D-27 requires
  </action>
  <acceptance_criteria>
    - `pnpm jest src/frontend/screens/Library/__tests__/filterEngine.test.ts --silent` exits 0
    - `pnpm jest src/frontend/screens/Library --silent -t "migrat"` runs at least 4 tests and passes
    - `pnpm jest src/frontend/screens/Library --silent -t "runnability"` runs at least 8 tests and passes
    - `pnpm jest src/frontend/screens/Library --silent -t "count"` passes and includes an assertion
      whose expected value is a non-zero number for a facet value that is NOT currently selected
    - The test file imports none of `@testing-library/react`, `react-test-renderer`, `enzyme`:
      `grep -cE "testing-library|react-test-renderer|enzyme" src/frontend/screens/Library/__tests__/filterEngine.test.ts` returns 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest src/frontend/screens/Library --silent</automated>
  </verify>
  <done>Every engine predicate, the skip rule, all three hosts' runnability derivation and the D-02 discard-not-translate migration are covered by passing node-environment unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → renderer state | Filter values persisted by a prior app version (or edited by any local process) are re-read on every launch. Per D-02 this is explicitly untrusted input whose stale shapes must be discarded, not interpreted. |
| `configStore` (`games.customCategories`) → renderer | User-authored collection names, arbitrary strings, flow into the collection predicate and later into rendered copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.11-01 | Tampering | `migrateStoreFacetSelection` / `migrateRunnabilityFacetSelection` (`filterEngine.ts`) | mitigate | Both helpers parse inside try/catch and return `[]` for any input that is not an array of known members. A legacy opt-out object, malformed JSON, `null`, or an array of unknown strings all resolve to the empty (no-constraint) selection. No input path throws, so a corrupted key cannot crash the Library screen. |
| T-34.11-02 | Tampering | `passesMore` non-available check | mitigate | The engine never reads `localStorage` itself; `nonAvailableAppNames` is passed in as an already-parsed array by the caller, so a malformed `nonAvailableGames` value is handled once at the boundary rather than inside a per-game predicate that runs thousands of times. |
| T-34.11-03 | Denial of Service | `countFor` | accept | `countFor` runs the full pipeline once per facet option. Library sizes here are hundreds to low thousands of in-memory objects and the callers memoize (plan 07). Accepted: no remote input, no unbounded growth, and the user's own library is the only data source. |
| T-34.11-04 | Information Disclosure | `deriveRunnabilityTier` returning `null` for `rating === undefined` | mitigate | The undefined-vs-null distinction is preserved verbatim from `CrossoverBadge` so the UI never claims a compatibility verdict the app never looked up. Collapsing the two states would present fabricated compatibility information as fact (D-16 honesty invariant). |
| T-34.11-SC | Tampering | npm/pip/cargo installs | mitigate | **No packages are installed in this phase.** `34.11-RESEARCH.md` § Package Legitimacy Audit confirms every library used is already a direct dependency. If any task in this plan finds itself needing an install, STOP — that is out of scope and requires a blocking human legitimacy checkpoint. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0
- `pnpm jest src/frontend/screens/Library --silent` exits 0
- `git diff --stat src/frontend/screens/Library/index.tsx` shows no change — this plan must not
  touch the Library component
- `grep -c "Fuse" src/frontend/screens/Library/index.tsx` is unchanged from HEAD. Removing `Fuse.js`
  is explicitly OUT OF SCOPE for this phase (D-33 amendment / D-35 deferral, operator decision).
</verification>

<success_criteria>
- `filterEngine.ts` exists, is React-free, and is the single implementation of every filter stage
- `countFor` is built on `filterLibrary` with a `skip`, not on a duplicated predicate chain
- Runnability derivation is byte-for-byte faithful to `CrossoverBadge.tsx:28-43`'s bucketing
- `runnabilityRowsForHost('win32')` is `[]`
- The D-02 migration discards legacy shapes and never translates them
- No behaviour change ships in this plan: `Library/index.tsx` is untouched
</success_criteria>

<output>
Create `.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-01-SUMMARY.md` when done
</output>
