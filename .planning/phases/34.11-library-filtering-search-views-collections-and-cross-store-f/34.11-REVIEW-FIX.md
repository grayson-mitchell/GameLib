---
phase: 34.11-library-filtering-search-views-collections-and-cross-store-f
reviews: 34.11-REVIEW.md
swept: 2026-08-25T11:00:10Z
swept_by: quick 260825-vy5
swept_against: HEAD (fix/steam-native-install-stability), NOT the working tree
re-swept: 2026-08-27
re-swept_by: quick 260827-s8z
re-swept_against: HEAD (fix/steam-native-install-stability) after this quick task's own
  three commits (WR-04, WR-19, WR-07) landed on it
re-swept-2: 2026-08-27
re-swept-2_by: quick 260827-t9c
re-swept-2_against: HEAD (fix/steam-native-install-stability) after this quick task's two
  commits (`a0e7dfed7` WR-06/WR-12, `4ba13c636` WR-01/WR-05/WR-09) landed on it
re-swept-3: 2026-08-27
re-swept-3_by: quick 260827-ua7
re-swept-3_against: HEAD (fix/steam-native-install-stability) after this quick task's two
  commits (`a9b6ef51a` WR-08, `4329529e0` WR-16) landed on it
status: partial
dispositions:
  fixed: 19
  open: 3
  invalid: 1
  total: 23
criticals_open: 0
---

# Phase 34.11 — Code Review Fix Pass

Fix ledger for `34.11-REVIEW.md` (2026-08-10T08:15:06Z, 4 Critical / 19 Warning / 23 total).

`34.11-REVIEW.md`'s own `status: issues_found` is **not** edited by this pass. It records
what the review *found*; it is never rewritten when fixes land. This sibling carries the
outcome, and `reviewStatus()` reads this file's `status:` in preference to the review's.

## Verdict: `partial`

**All 4 Criticals are discharged. 3 of 19 Warnings remain open; 1 is reclassified INVALID.**

`partial` is derived from the rows below, not chosen. `all_fixed` would assert that three
findings this sweep confirmed still present in `HEAD` had been addressed — the precise
false claim that `all_fixed`-over-a-deferred-finding has produced on this project before.
None of the three contradicts a declared REQ-34.11-01..17 truth; `34.11-VERIFICATION.md`
(`status: passed`, 17/17) already stands on its own.

**2026-08-27 re-sweep (quick `260827-s8z`):** closed WR-04, WR-07 and WR-19 (mechanical,
grep-justified deletions/collapses); reclassified WR-17 **INVALID** with reproducible
generator-contract evidence (the review's recommended fix would disarm a default-deny
provenance veto and widen a blocking gate); left WR-18 **OPEN** with a measured 163/0 ->
165/35 false-positive count, because closing it needs a gate-design decision this task was
not scoped to make. Fourteen open -> ten open + one invalid, arithmetic 12 + 10 + 1 = 23
checked against the original 23-finding total.

**2026-08-27 re-sweep (quick `260827-t9c`):** closed the five remaining correctness
findings from the "Correctness — small but real" group, in two commits. `a0e7dfed7`
closed WR-06 (the module-scope `throw` is gone; the drift alarm is now an `it.each` in
`FilterRunnabilityFacet.test.tsx`) and WR-12 (`renderableActiveFilters` applied before
`activeFilterCount` is derived, plus a `labels.length === 0` guard in `FilterZeroResult`).
`4ba13c636` closed WR-01 (`makeLibrary` is now a `useCallback` declaring all twelve
identifiers it reads; `libraryUnion`'s memo depends on it alone; the
`react-hooks/exhaustive-deps` tripwire for `makeLibrary` is gone, 23 -> 22 warnings, 0
errors, nothing suppressed), WR-05 (`recentAppNames` is state refreshed by
`window.api.handleRecentGamesChanged`, with the review's `libraryStatus`-dependency
fallback explicitly rejected in a code comment) and WR-09 (`collectionIsStale()`, exported
from `engineWiring.ts` and unit-tested through all five rows including the
`preset_uncategorized` sentinel, wired into an effect that clears the persisted
`currentCollection` when it goes stale). Five of the previously-open Warnings are now
closed, leaving five still open; arithmetic 17 + 5 + 1 = 23 checked against the original
23-finding total. Both commits' diffs were grepped for `eslint-disable` and found clean.

**2026-08-27 re-sweep (quick `260827-ua7`):** closed WR-08 and WR-16, in two commits.
`a9b6ef51a` closed WR-08 by threading an `intent: 'create' | 'manage'` param through
`setShowCategories`/`LibraryContext` so `+ New collection` and `Manage collections` open
the same dialog with different behaviour (autoFocus on the new-category field only for
`'create'`), reusing the existing create flow rather than adding one — no new i18n string,
D-20 untouched. `4329529e0` closed WR-16 by adding
`FilterChipRow/__tests__/chipLabels.realI18next.test.ts`, which exercises all eleven
`ActiveFilterDescriptor` kinds (using `PRESET_UNCATEGORIZED` and `'sideload'` as the
`collection`/`store` specimens specifically, since those are the only values of those two
kinds that route through a real i18next lookup) plus injection-safety and plural-variant
cases against a fresh `i18next.createInstance()` reading the real `public/locales` catalog;
`index.test.tsx`'s existing mock-based tests are kept and retitled as structural
pass-through checks, not injection-safety proofs. The open count drops from five to three;
arithmetic 19 + 3 + 1 = 23 checked against the original 23-finding total. Both commits'
diffs were grepped for `eslint-disable` and found clean.

## Method

Swept against `HEAD` via `git show`, deliberately **not** the working tree: this branch
(`fix/steam-native-install-stability`) carries unrelated uncommitted edits to
`Library/index.tsx`, `filterEngine.ts` and `RecentlyPlayed/index.tsx` that would otherwise
be miscredited to this phase.

Each row was resolved by reading the finding's own named landmark in source. Where the
review's recommended fix is present, the row cites the commit; where it is absent, the row
cites the line that still shows the defect. Nothing here is taken from a SUMMARY.

## Criticals

| ID | Finding | Disposition | Evidence |
|---|---|---|---|
| CR-01 | Facet counts computed over the already-filtered library — every unselected option reported 0, D-28 inert | **FIXED** | `fa694bf39`. `buildGridPipeline` (`engineWiring.ts`) hoists `libraryUnion` above the grid memo; counts and grid read the same unfiltered union. `engineWiring.test.ts` asserts `countForStore('steam') === 3` with `gog` selected, through the production call shape. Independently re-run in `34.11-VERIFICATION.md`. |
| CR-02 | `Favourites` view returned zero games in the default configuration | **FIXED** | `9ebad6dca`. `buildFavouriteKeys` derives from `favouriteGames.list` directly, independent of `libraryTopSection`/`show_favorites`. Regression test passes with both disabled. |
| CR-03 | `var(--navbar-active)` had no fallback and is undefined in 7 of 11 themes — checked-checkbox indicator dropped entirely | **FIXED** | `aebaccff8`. All 5 flagged declarations hoist `--filter-active-color: var(--navbar-active, var(--accent-overlay, var(--accent)))`. Zero live bare `var(--navbar-active)` uses remain in the four files. |
| CR-04 | `preset_uncategorized` sentinel rendered as user-facing text in chips and the zero-result sentence | **FIXED** | `4b635ecc4`. `PRESET_UNCATEGORIZED` exported once from `filterEngine.ts` and imported by `chipLabels.ts` and `FilterCollectionList`. Test asserts it is imported by identifier, not re-declared. |

## Warnings

| ID | Finding | Disposition | Evidence |
|---|---|---|---|
| WR-01 | `libraryUnion` memo omits every login-gate input `makeLibrary` reads | **FIXED** | `4ba13c636` (quick `260827-t9c`). `makeLibrary` is now `useCallback(() => { ... }, [epic.username, epic.library, gog.username, gog.library, amazon.user_id, amazon.library, zoom.enabled, zoom.username, zoom.library, steam?.username, steam?.library, sideloadedLibrary])` — the review's FIRST recommended option, not its second. `libraryUnion` is `useMemo(() => makeLibrary(), [makeLibrary])`. eslint's `missing dependency: 'makeLibrary'` warning is gone (23 -> 22 warnings, 0 errors); `libraryPipeline.test.ts`'s `functionRegion` needle was updated from `'const makeLibrary = () => {'` to `'const makeLibrary = useCallback(() => {'` in the same commit so the gate did not go silently unrun. `libraryHookStaleness.test.ts` (Tests G/H/I) asserts the read set is a subset of the declared deps, non-vacuously. |
| WR-02 | Unguarded `JSON.parse` of localStorage inside render can white-screen the Library | **FIXED** | `598f71aac`. `parseNonAvailableAppNames()` at `engineWiring.ts:145-154` has `try`/`catch`, an `Array.isArray` guard and a string filter. Also closes threat `T-34.11-02` (`725ef4de0`). |
| WR-03 | `connectedStores` and `makeLibrary` gate Amazon on different fields | **FIXED** | `5472fb015`. Both sites read `amazon.user_id` (`Library/index.tsx:308`, `:642`). Pinned by `__tests__/connectedStoresParity.test.ts`, whose header cites this finding and threat `T-34.11-12`. |
| WR-04 | ~90 lines of dead legacy filter state survive the retirement of their only consumers | **FIXED** | `32d7b37b9` (quick `260827-s8z`). The three state blocks (`storesFilters`, `platformsFilters`, `crossoverRatingFilters` — `useState`, setters, `initial*` computation) are gone from `Library/index.tsx`; the six matching `LibraryContextType` members/setters and the stale `types.ts:337-339` forward-reference comment are gone; `LibraryContext.tsx`'s matching `initialContext` entries are gone. The live D-02 migration reads (`storage.getItem('storesFilters')`/`('platformsFilters')`) and the three `facetOptInMigrated` `storage.removeItem()` calls survive unchanged, confirmed by grep. `libraryPipeline.test.ts`'s rationale comment rewritten to match. |
| WR-05 | `recentAppNames` is captured once at mount and never refreshes | **FIXED** | `4ba13c636` (quick `260827-t9c`). `recentAppNames` is now `useState` seeded from `configStore.get('games.recent', [])`, refreshed by a `useEffect` subscribing to `window.api.handleRecentGamesChanged` — the identical pattern `RecentlyPlayed/index.tsx` already used. The review's fallback recommendation ("at minimum add `libraryStatus` to the dependency array") was **deliberately not taken**: `libraryStatus` changes on every download progress tick, so depending on it would recompute this (and, through `engineDeps`, invalidate downstream memos) continuously during any download. The primary recommendation (`handleRecentGamesChanged`) was taken instead; the rejection is recorded in a code comment, not buried. `libraryHookStaleness.test.ts` (Test J) asserts both `handleRecentGamesChanged` and `setRecentAppNames` are present, non-vacuously. |
| WR-06 | A module-scope `throw` in a dev-only drift check crashes the whole app on import | **FIXED** | `a0e7dfed7` (quick `260827-t9c`). The `if (process.env.NODE_ENV !== 'production') { ... throw ... }` block is deleted outright; `grep -c throw FilterRunnabilityFacet/index.tsx` is 0. (The earlier `260825-vy5` sweep's false closure was a grep anchored to `^throw`, which misses the indented statement inside the `if` block — recorded here so the next reader does not repeat the same false-closure grep.) The drift alarm is now an `it.each` over `RUNNABILITY_LABELS` in `NavShell/__tests__/FilterRunnabilityFacet.test.tsx`, a named non-fatal test failure instead of a production crash. |
| WR-07 | `resolveLabel` is duplicated verbatim between two components | **FIXED** | `89d61972a` (quick `260827-s8z`). `resolveLabel` and its `TFunc` type live once, exported from `chipLabels.ts`, imported by both `FilterChipRow/index.tsx` and `FilterZeroResult/index.tsx`. `chipLabels.ts` remains React-free (no `react`/`react-i18next` import). `FilterZeroResult`'s comment acknowledging the duplication is deleted. |
| WR-08 | `+ New collection` and `Manage collections` are functionally identical rows | **FIXED** | `a9b6ef51a` (quick `260827-ua7`). `setShowCategories` (`types.ts`) now takes an optional `intent: 'create' \| 'manage'`, threaded through `LibraryContext` as `categoriesManagerIntent`. `FilterCollectionList/index.tsx`'s `+ New collection` row calls `setShowCategories(true, 'create')`; `Manage collections` calls `setShowCategories(true, 'manage')` (option (b), per `34.11-UI-SPEC.md:281`/`:283` mandating both rows exist as distinct entry points). `CategoriesManager`'s existing create flow is reused — no new dialog, no new i18n string — and its `new-category-name` `TextInputField` gets `autoFocus={categoriesManagerIntent === 'create'}`. D-20 is preserved: neither row mutates a category, only which sub-view opens focused. `FilterCollectionList.test.tsx` asserts the two rows now call `setShowCategories` with distinct second arguments; `CategoriesManager/__tests__/newCollectionFocus.test.tsx` asserts `autoFocus` is `true` for `'create'` and falsy for `'manage'`, proven non-vacuous against the pre-fix code (`autoFocus` prop absent, assertion failed `Received: undefined`). |
| WR-09 | A stale `currentCollection` silently filters the entire library away | **FIXED** | `4ba13c636` (quick `260827-t9c`). `collectionIsStale(collection, customCategories)`, exported from `engineWiring.ts`, returns `false` for `null` and for the `PRESET_UNCATEGORIZED` sentinel, `!(collection in customCategories)` otherwise; unit-tested through all five rows in `engineWiring.test.ts` (Test K), including the `preset_uncategorized` row that a naive `!(x in list)` implementation would wrongly convict. `Library/index.tsx` wires it into a `useEffect` on `[currentCollection, customCategories.list]` that calls `setCurrentCollectionPersisted(null)` on staleness; `libraryHookStaleness.test.ts` (Test L) asserts both the call site and the clearing call are present, non-vacuously. |
| WR-10 | An alphabet-letter selection produces a zero state that names no filter | **OPEN — out of scope by design** | Explicitly excluded by decision D-08; `Library/index.tsx:917` documents the letter's absence from `activeFilterCount` as intentional. Recorded, not deferred silently. |
| WR-11 | `clearAllFilters` does not clear the alphabet letter | **OPEN** | `Library/index.tsx:992-1007` — no `setAlphabetFilterLetter(null)`. Same D-08 boundary as WR-10. |
| WR-12 | A descriptor whose label cannot be resolved inflates the count and empties the sentence | **FIXED** | `a0e7dfed7` (quick `260827-t9c`). `renderableActiveFilters()`, added to `chipLabels.ts`, filters `activeFilterDescriptors` down to those with a resolvable `chipLabelSpec` BEFORE `activeFilterCount` is derived from it in `Library/index.tsx`, closing the residual this row previously flagged. `FilterZeroResult/index.tsx` additionally gets a `labels.length === 0` guard as defence in depth. The `chipLabelSpec` null-filter the review originally recommended (`FilterZeroResult/index.tsx:92-94`) still predates the review (`726b96f93`) and was never itself the residual — that note from the prior sweep stands. |
| WR-13 | The `--navbar-active` theme gate checked one theme while the file already had a census idiom | **FIXED** | `255cc16a3`. Replaced with a census over the same 11 `themeSelectors` the `--divider` census uses; asserts non-vacuity, absence of the bare form, and the exact fallback chain, for all 4 consumer files. `themeTokens.test.ts` 31/31. |
| WR-14 | The single-implementation gate cannot detect CR-01 | **FIXED** | Closed by the CR-01 fix, not by editing the gate: `engineWiring.test.ts` adds the behavioural assertion in the real production call shape that `libraryPipeline.test.ts`'s source gate could not make. |
| WR-15 | No test exercises `engineDeps` construction — the surface CR-02 lives on | **FIXED** | `engineWiring.ts` extracts `buildEngineDeps`/`buildGridPipeline` as pure exported functions and `__tests__/engineWiring.test.ts` covers them (14/14). |
| WR-16 | The i18next-injection tests are vacuous — the implementation they test is mocked out | **FIXED** | `4329529e0` (quick `260827-ua7`). New `FilterChipRow/__tests__/chipLabels.realI18next.test.ts` runs `chipLabelSpec`/`resolveLabel` against a fresh `i18next.createInstance()` (`i18next-fs-backend`) reading the real `public/locales` catalog — all eleven `ActiveFilterDescriptor` kinds (with `PRESET_UNCATEGORIZED`/`'sideload'` as the `collection`/`store` specimens, the only values of those kinds that route through a real key lookup rather than a bare literal), `$t(...)`/`{{token}}` injection-safety cases, and a plural-variant specimen proving `resolveLabel`'s two-argument `t(key, defaultText)` shape silently discards a `key_one`/`key_other`-only catalog entry (measured correction: this reproduces under i18next 22.5.1, the folklore `{{count}}`-on-base-key shape does not). `index.test.tsx`'s `jest.mock('react-i18next', ...)` is kept for element-graph assertions; its two hostile-name tests are retitled as structural pass-through checks with a cross-reference to the new file, not removed. |
| WR-17 | `meta/i18nGateScope.json` carries false provenance metadata | **INVALID — not a defect** (quick `260827-s8z`) | `baseCommit` is `package.json`'s `upstream.baseCommit` (the upstream Heroic merge-base) by the generator's own contract (`genI18nGateScope.ts:397-402`, `:425`), so it *must* predate every fork-added file it lists -- that is the designed semantics, not staleness. The review's own reproduction (`git cat-file -e b5b5cad:.../FilterChipRow/index.tsx` fails) is the definition of "fork-added", not evidence against it. `genI18nGateScope.test.ts:146` pins `scope.baseCommit === packageJson.upstream.baseCommit` and `scope.baseVersion === packageJson.upstream.baseVersion`; `meta/i18nForkTouchedFiles.json` (regenerated 2026-08-23) carries the same `baseCommit`. The `generatedBy` marker is honest today and is **load-bearing**: `isHandCuratedProvenance()` (`genI18nGateScope.ts:286-289`) uses it as a default-deny veto that prevents the generator from overwriting the blocking gate's scope; the generator's own header (`:19-28`) records that an unconditional run previously widened the gate 160 -> 178 files and cost real work twice. Running the review's recommended `pnpm gen-i18n-gate-scope` would disarm that veto and widen the blocking gate by ~36 files. The review was written before the generator was hardened against exactly this, so its advice is stale rather than wrong-at-the-time. |
| WR-18 | The canonical English label sources are outside the i18n gate scope | **OPEN** (re-measured, quick `260827-s8z`) | Zero occurrences of `facetLabels` or `chipLabels` in `meta/i18nGateScope.json`, still true. Measured via `scanScope({ extraFiles })` (audit mode, committed scope left untouched): committed scope is 163 files / **0** violations; adding the two label files is 165 files / **35** violations (8 in `facetLabels.ts`, 27 in `chipLabels.ts`), all `object-property`/`argument` literals -- both files hold the English defaults as **data tables** consumed by `t()` at call sites already in scope, so putting the tables themselves in scope produces 35 false positives against a currently-green blocking gate. Hard pin: `genI18nGateScope.test.ts:574` asserts `scopeSnapshot.files.length` is `163`. Closing this properly needs either a spec-table-aware gate heuristic or two `meta/i18nGateAllowlist.json` entries with `expected` counts -- both design decisions outside this task's scope, deliberately not taken here. |
| WR-19 | `Header/index.css` ships unscoped global selectors and a now-dead background declaration | **FIXED** | `b65a0f7ca` (quick `260827-s8z`). `Header/index.css` now contains only `.Header`, `.Header__search`, `.Header__search .FormControl` -- the dead `background` declaration and the unrelated `.iconsWrapper`/`.refreshIcon`/`.svg-button`/duplicate `@keyframes refreshing` block are gone. `.iconsWrapper` relocated to `Settings/index.css` (its only consumer, `CustomWineProton.tsx`'s `addRemoveSvgButtons` row); `.refreshIcon:hover, .svg-button:focus-visible .refreshIcon` relocated to `ErrorComponent/index.css` (its only consumer, that component's own retry button) -- the `@keyframes refreshing` definition it referenced was already declared globally in `UpdateComponent/index.css`, so only the duplicate was removed, not the one real definition. The `NavShell__tier2Portal > .Header { background: transparent }` neutraliser (34.11-09) was removed alongside the background declaration it existed only to cancel. |

## Residual

The three open Warnings (WR-10, WR-11, WR-18) are carried to
`.planning/todos/pending/2026-08-25-phase-34-11-residual-review-warnings.md`. Two of them
(WR-10, WR-11) are out of scope by decision D-08 and close only if that decision is
revisited. One further finding (WR-17) is reclassified **invalid** rather than open --
see the table above -- and is not carried to the todo. None of the three open Warnings is a
Critical, and `criticals_open: 0` in this file's frontmatter is the machine-readable form
of that.

## Badge attribution

`34.11-REVIEW.md`'s SHA-256 is unchanged by this pass. Proven against the real
`~/.vscode/extensions/gsd-phase-status/parse.js`, with the fix status withheld as a control:

| this file's `status:` | `resolveArtifactStatus('REVIEW.md', …)` | folder `rollup()` |
|---|---|---|
| *(file absent — control)* | `blocked` | **`blocked`** |
| `partial` *(actual)* | `inprogress` | **`inprogress`** |
| `all_fixed` *(counterfactual)* | `complete` | `complete` |

The control is the load-bearing row: it shows the badge is reading **this** file rather
than moving on its own.
