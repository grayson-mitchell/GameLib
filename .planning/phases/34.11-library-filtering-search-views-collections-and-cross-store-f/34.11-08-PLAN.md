---
phase: 34.11-library-filtering-search-views-collections-and-cross-store-f
plan: 08
type: execute
wave: 4
depends_on: ["34.11-04", "34.11-05", "34.11-07"]
files_modified:
  - src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts
  - src/frontend/screens/Library/components/FilterChipRow/index.tsx
  - src/frontend/screens/Library/components/FilterChipRow/index.scss
  - src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx
  - src/frontend/screens/Library/components/FilterZeroResult/index.tsx
  - src/frontend/screens/Library/components/FilterZeroResult/index.scss
  - src/frontend/screens/Library/index.tsx
  - meta/i18nGateScope.json
autonomous: true
requirements:
  - REQ-34.11-13
  - REQ-34.11-14
user_setup: []

must_haves:
  truths:
    - "Every non-default filter is visible as a removable chip above the grid, including views — All games shows no chip, Installed does (D-26)."
    - "EVERY descriptor kind describeActiveFilters can emit produces a chip — view, collection, store, RUNNABILITY, search, both tri-states and all three More-filters booleans. A kind with no label mapping is a silent hole in the headline deliverable, not a graceful degradation (D-26, REQ-34.11-13)."
    - "A chip's × always means 'remove this constraint': a tri-state chip's × returns showHidden/showNonAvailable to 'off' and never steps the cycle to the next value (D-27)."
    - "No chip carries a count — counts live on facet options only, inside the panel (D-28)."
    - "Clear all in the chip row and Clear all filters in the empty state are the same clearing behaviour, and both persist the cleared state so it survives a relaunch (D-25)."
    - "The zero-result state names the filters responsible — 'No games match Installed + GOG + \"witcher\"' — and a generic 'No results' message is a failure of this plan (D-30)."
    - "NEGATIVE (D-08): the alphabet filter, sort (sortDescending / sortInstalled) and grid-vs-list layout are not filters. They stay in the grid header exactly where they are today, this plan neither moves nor reimplements them, and no chip is ever emitted for any of them."
    - "The chip row and the empty state resolve every label through ONE implementation, which itself imports store and runnability label text from plan 04's facetLabels.ts — so a chip provably says what the panel row says."
    - "A hostile collection name or search term reaches rendered copy as inert text — no i18next nesting, no interpolation-inside-interpolation, no dangerouslySetInnerHTML."
    - "Rendering outside LibraryContext.Provider is DIAGNOSABLE, not silent: both components detect plan 04's initialContext sentinel and emit a console.error before returning null, so a dead surface cannot be mistaken for an unfiltered library."
  artifacts:
    - path: "src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts"
      provides: "Pure, React-free descriptor→label-spec resolver shared by the chip row and the zero-result state; imports its store/runnability label text from facetLabels.ts"
      contains: "chipLabelSpec"
    - path: "src/frontend/screens/Library/components/FilterChipRow/index.tsx"
      provides: "The chip row above GamesList: one chip per ActiveFilterDescriptor plus the dashed Clear all chip"
      contains: "gamelib:library.filterPanel.removeFilter"
    - path: "src/frontend/screens/Library/components/FilterZeroResult/index.tsx"
      provides: "Zero-result empty state naming the active filters, with an inline Clear all filters action"
      contains: "gamelib:library.filterPanel.emptyBody"
    - path: "src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx"
      provides: "Wave 0 test file named in 34.11-VALIDATION.md; covers D-26/D-27/D-28/D-30 by direct invocation"
    - path: "src/frontend/screens/Library/index.tsx"
      provides: "Chip row mounted below AlphabetFilter and above GamesList; zero-result state chosen over EmptyLibraryMessage when filters are active"
      contains: "FilterZeroResult"
  key_links:
    - from: "src/frontend/screens/Library/components/FilterChipRow/index.tsx"
      to: "src/frontend/screens/Library/LibraryContext.tsx"
      via: "useContext(LibraryContext) for activeFilterDescriptors and every removal setter"
      pattern: "activeFilterDescriptors"
    - from: "src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts"
      to: "src/frontend/screens/Library/facetLabels.ts"
      via: "imports RunnerToStore and RUNNABILITY_LABELS — the same tables plan 07's panel rows use"
      pattern: "facetLabels"
    - from: "src/frontend/screens/Library/components/FilterZeroResult/index.tsx"
      to: "src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts"
      via: "same chipLabelSpec resolver, so the empty state's sentence cannot drift from the chip row"
      pattern: "chipLabelSpec"
    - from: "src/frontend/screens/Library/index.tsx"
      to: "src/frontend/screens/Library/components/FilterChipRow"
      via: "render below AlphabetFilter, above GamesList"
      pattern: "<FilterChipRow"
---

<objective>
Build the two grid-area surfaces that answer "why am I looking at 6 games instead of 214": the
`FilterChipRow` above the grid, and the zero-result empty state that names the filters responsible.

Purpose: waves 1-3 make the panel able to *set* filters. Nothing yet makes an active filter
**visible or removable from the grid**, and today a fully-filtered-out library falls through to
`EmptyLibraryMessage`, which is written for "you own no games" and says nothing about filters. Both
gaps are user-facing dead ends. This plan closes them, and is the only place in the phase where D-08
(sort/layout/alphabet are not filters) and D-30 (the empty state names names) are decided.

Output: a pure label resolver shared by both surfaces, the chip row, the zero-result state, their
mount points in `Library/index.tsx`, and the Wave 0 test file `34.11-VALIDATION.md` requires. This
plan is also the wave-4 reconciliation point for `meta/i18nGateScope.json`, the append-only registry
that plans 06 and 07 both wrote to in wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-CONTEXT.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-UI-SPEC.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-VALIDATION.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-04-SUMMARY.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-05-SUMMARY.md
@.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-07-SUMMARY.md

<interfaces>
From plan 01's `src/frontend/screens/Library/filterEngine.ts` and `src/frontend/types.ts`
(already committed by wave 1):
- `ActiveFilterDescriptor` — `{ id: string; kind: 'view' | 'collection' | 'store' | 'runnability' | 'search' | 'showHidden' | 'showNonAvailable' | 'showSupportOfflineOnly' | 'showThirdPartyManagedOnly' | 'showUpdatesOnly'; value: string }`. `id` is stable and unique within one render (`store:gog`, `view:installed`, `collection:Backlog`, `search`, `showHidden:only`, `runnability:bottle`, …). **All ten kinds are emittable and every one of them must map to a chip label in this plan.**
- `describeActiveFilters(state, searchTerm)` — emits nothing for `view === 'all'`, nothing for a
  `null` collection, nothing for an empty facet array, nothing for an empty search term, nothing for
  a tri-state at `'off'` or a boolean at `false`. This is D-26's rule, already implemented; this plan
  RENDERS its output and must not re-derive which filters are active. **This plan never calls it** —
  plan 04 already calls it once and publishes the result (below).
- types `LibraryView`, `RunnabilityTier`, `StoreFacetValue`.

From plan 04's `LibraryContext` (already committed by wave 2) — every removal handler this plan
needs already exists and already persists:
- `activeFilterDescriptors: ActiveFilterDescriptor[]` — **the actual descriptor list**, exactly as
  `describeActiveFilters` returned it this render. This is the data source for every chip and for
  the zero-result sentence. Read it straight off the context; do not reassemble `FilterEngineState`
  and do not call `describeActiveFilters` yourself.
- `activeFilterCount: number` — plan 04 guarantees it is `activeFilterDescriptors.length` from the
  same call, so branching on either is equivalent.
- `__isDefaultLibraryContext?: true` — present ONLY on `initialContext`, never on the real provider
  value. See the guard in Task 2.
- `libraryView` / `setLibraryView`, `currentCollection` / `setCurrentCollection`
- `storeFacet` / `setStoreFacet`, `runnabilityFacet` / `setRunnabilityFacet`
- `clearAllFilters(): void` — clears AND persists (D-25). Both `Clear all` controls in this plan call
  exactly this. Do not build a second clearing path.
- the shipped tri-state/boolean setters (`setShowHidden`, `setShowNonAvailable`,
  `setShowSupportOfflineOnly`, `setShowThirdPartyManagedOnly`, `setShowUpdatesOnly`) and the search
  setter — read `src/frontend/types.ts` for their exact current names before wiring, do not guess.

From plan 04's `src/frontend/screens/Library/facetLabels.ts` — the phase's single declaration site
for facet label text, already imported by plan 07's panel rows in wave 3:
- `RunnerToStore: Record<string, string>` — store brand names (`Epic Games`, `GOG`, `Amazon Games`,
  `ZOOM Platform`, `Steam`). Proper nouns; not translated.
- `RUNNABILITY_LABELS: Record<RunnabilityTier, [string, string]>` — each tier's `[key, defaultText]`
  tuple for the four keys plan 07 minted.
`chipLabels.ts` imports both. It declares neither, and it does not import from `LibraryFilters`
(plan 09 deletes that) or from any `NavShell` component.

Mount site (`src/frontend/screens/Library/index.tsx`, current line numbers at HEAD):
- `<LibraryHeader list={libraryToShow} />` at L847
- `{showAlphabetFilter && <AlphabetFilter />}` at L849
- `{libraryToShow.length === 0 && <EmptyLibraryMessage />}` at L861
- the main `<GamesList library={libraryToShow} … />` at L863-869

**Fuse.js stays.** `Library/index.tsx:601-616` builds a `Fuse` with `threshold: 0.4`. Removing or
reconfiguring it is OUT OF SCOPE for this phase (operator decision; D-35 is tagged
`[informational — DEFERRED]`). Consequence you must not treat as a bug: the games count behind both
surfaces is computed over the **fuzzy-matched** set, so results can include near-misses a substring
matcher would exclude. Write no acceptance criterion that assumes substring semantics, and add no
task touching Fuse.
</interfaces>

<i18n_contract>
Phase 34.8's localisation gate is a STANDING BLOCKING requirement. `pnpm lint-translations:gamelib`
must pass, and a green jest run does not imply a green gate.

Use the `gamelib:` prefix through the aliased-hook idiom, exactly as plan 06 does and as all 90
existing call sites do: `const { t: tGamelib } = useTranslation('gamelib')`, then
`tGamelib('gamelib:key', 'Default text')`. **The alias must literally be named `tGamelib`** —
`i18next-parser.config.js:61,70` declare `functions: ['t', 'tGamelib']` (added by Phase 34.8-09), so
any other alias is invisible to the parser again. Reused shipped keys keep coming from the default
namespace via `const { t } = useTranslation()`.

Keys MINTED by this plan (`gamelib` namespace, default text copied verbatim from the UI-SPEC
Copywriting table — do not reword):

| Key | Default text |
|---|---|
| `gamelib:library.filterPanel.clearAll` | `Clear all` |
| `gamelib:library.filterPanel.removeFilter` | `Remove {{filterLabel}} filter` |
| `gamelib:library.filterPanel.chipHiddenOnly` | `Hidden only` |
| `gamelib:library.filterPanel.chipHiddenIncluded` | `Including hidden` |
| `gamelib:library.filterPanel.chipNonAvailableOnly` | `Non-available only` |
| `gamelib:library.filterPanel.chipNonAvailableIncluded` | `Including non-available` |
| `gamelib:library.filterPanel.emptyHeading` | `No games match your filters` |
| `gamelib:library.filterPanel.emptyBody` | `No games match {{filters}}.` |
| `gamelib:library.filterPanel.emptyClearAll` | `Clear all filters` |

Keys REUSED verbatim, NOT re-minted:
- `gamelib` namespace, minted by plan 06 in this same phase: `library.filterPanel.viewInstalled`,
  `library.filterPanel.viewRecentlyPlayed`, `library.filterPanel.viewFavourites`
  (`library.filterPanel.viewAll` is never used here — `All games` renders no chip, D-26).
- `gamelib` namespace, minted by plan 07 in this same phase and reached here through
  `RUNNABILITY_LABELS`, not by retyping the key strings: `library.filterPanel.runsNatively`,
  `library.filterPanel.runsViaBottle`, `library.filterPanel.wontRun`,
  `library.filterPanel.notYetChecked`. A runnability chip is a first-class chip — a Runnability
  facet is exactly as removable from the grid as a Store facet.
- `gamelib` namespace, already shipped: `library.storeOther` (the sideload row label).
- default namespace, already shipped: `header.show_support_offline_only`,
  `header.show_third_party_managed_only`, `header.show_updates_only`.

Two of the minted keys interpolate **user-influenced** values — `{{filterLabel}}` carries a
collection name or the raw search term, and `{{filters}}` carries the whole joined list. See
`<threat_model>`.
</i18n_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chipLabels.ts — the one label resolver both surfaces share</name>
  <read_first>
    - src/frontend/types.ts (`ActiveFilterDescriptor` — read the full `kind` union and confirm you
      have a case for every member — plus `LibraryView`, `RunnabilityTier`, `StoreFacetValue` as
      plan 01 committed them)
    - src/frontend/screens/Library/facetLabels.ts (plan 04's shared module: the exact exported
      names and shapes of `RunnerToStore` and `RUNNABILITY_LABELS`. This is where store and
      runnability label text lives for the whole phase — you import it, plan 07 already imports it)
    - src/frontend/components/UI/LibraryFilters/index.tsx L24, L217 (the shipped `tGamelib` idiom
      only — do not import anything from this file)
    - src/frontend/screens/Library/components/CrossoverBadge/__tests__/CrossoverBadge.test.tsx (the no-jsdom direct-invocation test idiom)
    - .planning/phases/34.11-.../34.11-UI-SPEC.md Copywriting table rows `Chip — view` through `Empty state action`
    - .planning/phases/34.11-.../34.11-CONTEXT.md D-26, D-27, D-28
  </read_first>
  <files>src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts</files>
  <behavior>
    - `chipLabelSpec({kind:'view', value:'installed'})` returns the `gamelib` spec for `library.filterPanel.viewInstalled` with default text `Installed`.
    - No descriptor of kind `view` and value `all` ever reaches this function — `describeActiveFilters` never emits one — but if one does, the function returns `null` rather than a label, so `All games` can never produce a chip (D-26).
    - `chipLabelSpec({kind:'collection', value:'Backlog'})` returns a **literal** spec carrying `Backlog` unchanged: no key, no interpolation, no title-casing, no truncation.
    - `chipLabelSpec({kind:'search', value:'witcher'})` returns a literal spec carrying `"witcher"` — the term wrapped in straight double quotes, per the UI-SPEC `Chip — search` row.
    - `chipLabelSpec({kind:'store', value:'gog'})` returns the literal `GOG` — the value `RunnerToStore.gog` holds, read from the import rather than retyped; `value:'sideload'` returns the `gamelib` spec for `library.storeOther` with default text `Other`.
    - `chipLabelSpec({kind:'runnability', value:'bottle'})` returns the `gamelib` spec whose `key` and `defaultText` are exactly `RUNNABILITY_LABELS.bottle[0]` and `[1]` — i.e. `library.filterPanel.runsViaBottle` / `Runs via bottle`. The same holds for `native`, `wontRun` and `notChecked`: all four tiers produce a chip, none returns `null`.
    - `chipLabelSpec({kind:'showHidden', value:'only'})` returns `chipHiddenOnly` / `Hidden only`; `value:'show'` returns `chipHiddenIncluded` / `Including hidden`. Same shape for `showNonAvailable`.
    - `chipLabelSpec({kind:'showUpdatesOnly', value:'true'})` returns a **default-namespace** spec for `header.show_updates_only` — an existing shipped key, not a new one.
    - Every member of the `ActiveFilterDescriptor['kind']` union has a case: iterating the ten kinds with a representative value produces ten non-null specs. There is no kind that `describeActiveFilters` can emit and this function cannot label.
    - A descriptor whose `kind` is not in the union at all (a genuinely unknown kind, e.g. one forged from tampered persisted state) returns `null`. This is a DIFFERENT case from any of the ten supported kinds and must be tested with a kind that is genuinely unknown, not with `runnability` or any other real member.
    - Every returned spec is a plain data object with no `count` field and no numeric field of any kind (D-28: counts are a facet-option affordance, never a chip affordance).
    - `joinChipLabels(['Installed','GOG','"witcher"'])` returns `Installed + GOG + "witcher"` — the exact D-30 example string.
    - `joinChipLabels([])` returns an empty string.
    - The module imports nothing from `react` and nothing from `react-i18next`.
  </behavior>
  <action>
Create `src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` as a **pure, React-free**
module. Purity is the whole point: `src/frontend/jest.config.js` sets `testEnvironment: 'node'` and
there is no `@testing-library/react` and no `react-test-renderer`, so every behavioural assertion in
this plan has to reach a plain function. Head the file with a docstring saying so, and naming
D-26/D-27/D-28/D-30 as the decisions it implements.

Export `ChipLabelSpec`, a discriminated union of two shapes: a translated spec carrying `ns`
(`'gamelib'` or `'default'`), `key` and `defaultText`; and a literal spec carrying `literal` only.
The literal branch exists because collection names and search terms are user data, not copy — they
must never be looked up as translation keys.

Export `chipLabelSpec(descriptor: ActiveFilterDescriptor): ChipLabelSpec | null` implementing the
`<behavior>` mapping by switching on `descriptor.kind`. Map the `gamelib`-namespace keys and their
default text from the `<i18n_contract>` table verbatim.

**Store and runnability labels come from plan 04's `src/frontend/screens/Library/facetLabels.ts`.**
Import `RunnerToStore` and `RUNNABILITY_LABELS` from it. Do not declare either table here, do not
retype the brand strings, and do not retype the four runnability key strings. Plan 07's panel rows
import the same two constants, which is what makes "the chip reuses the facet row labels as-is"
(UI-SPEC) true by construction rather than by two files agreeing today and drifting tomorrow. There
is no map to move and no cross-plan edit to perform: plan 04 settled the owner in wave 2.

- `store` case: `value === 'sideload'` → the `gamelib` spec for `library.storeOther` / `Other`;
  otherwise a **literal** spec carrying `RunnerToStore[value]`. If the value is not a key of
  `RunnerToStore`, return `null` rather than rendering the raw runner id.
- `runnability` case: look the tier up in `RUNNABILITY_LABELS` and return a `gamelib` spec whose
  `key` is the tuple's first element and whose `defaultText` is its second. **This case is not
  optional and it is not a nicety.** `describeActiveFilters` emits `kind: 'runnability'` the moment
  a user ticks `Runs natively`; without this case that descriptor falls into the `null` path below,
  the chip row silently renders nothing for it, and the user has an invisible, unremovable filter —
  a direct violation of D-26 and REQ-34.11-13 that no other assertion in the phase would catch. If
  the tier is not a key of `RUNNABILITY_LABELS`, return `null`.

Return `null` — never a fabricated label — for any kind/value pair this phase does not define, so an
unrecognised descriptor renders no chip rather than a broken one. Write the switch so this fallback
is reachable **only** by kinds outside the `ActiveFilterDescriptor['kind']` union (i.e. forged or
tampered input) and by the guarded unknown-value paths above. Type the switch so that omitting a
union member is a compile error — an exhaustive `switch` with a `never`-typed default is the shape
that makes "every kind has a case" a build-time fact rather than a review-time hope.

Export `joinChipLabels(labels: string[]): string` joining with the literal separator `' + '`. This
one function is what makes the empty state's sentence provably the same list the chip row displays;
D-30's example `No games match Installed + GOG + "witcher"` is its output, not a coincidence of two
implementations agreeing.

Do not add a `count`, `total`, or `matches` field to any spec (D-28). Do not add any sort, layout or
alphabet kind (D-08) — those are not filters and this module must have no vocabulary for them.

Create the Wave 0 test file `src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx`
(the exact path `34.11-VALIDATION.md` names) and cover every `<behavior>` bullet in a
`describe('chipLabels')` block, calling the functions directly. Import `facetLabels.ts` for real (it
is pure and needs no mock) and assert the runnability and store cases **against the imported
constants**, not against retyped string literals — a test that retypes the label would keep passing
after the shared table changed. Include an `it.each` over all ten members of the `kind` union
asserting each yields a non-null spec, and a separate case passing a genuinely unknown kind (e.g.
`{ id: 'x', kind: 'notAFilterKind', value: 'x' } as unknown as ActiveFilterDescriptor`) asserting
`null`. Task 2 appends its own `describe` to this same file.
  </action>
  <acceptance_criteria>
    - The module is React-free: `grep -cE "from 'react'|from \"react\"|react-i18next" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` returns 0
    - `grep -c "export function chipLabelSpec" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` returns 1
    - `grep -c "export function joinChipLabels" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` returns 1
    - Labels are imported from the single declaration site: `grep -c "facetLabels" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` returns 1, `grep -c "RunnerToStore" ...` returns at least 1, and `grep -c "RUNNABILITY_LABELS" ...` returns at least 1
    - Nothing is retyped: `grep -cE "ZOOM Platform|Epic Games|Amazon Games|runsViaBottle|notYetChecked" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` returns 0
    - Store labels still have exactly one declaration site in new code: `grep -rl "ZOOM Platform" src/frontend --include=*.ts --include=*.tsx | grep -v "LibraryFilters/index.tsx" | wc -l` returns 1, and `grep -rl "ZOOM Platform" src/frontend --include=*.ts --include=*.tsx | grep -c "screens/Library/facetLabels.ts"` returns 1 (the survivor is plan 04's module; `LibraryFilters/index.tsx` keeps its own copy until plan 09 retires it and does not count)
    - The runnability kind is handled: `grep -c "'runnability'" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` returns at least 1
    - The switch is exhaustive by type, not by inspection: `grep -cE ": never|assertNever|exhaustive" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` returns at least 1, and `npx tsc --noEmit -p tsconfig.json` exits 0
    - The exhaustiveness gate is proven non-vacuous: temporarily delete the `runnability` case, confirm `npx tsc --noEmit -p tsconfig.json` FAILS, then restore it
    - A runnability chip is exercised behaviourally: `pnpm jest src/frontend/screens/Library/components/FilterChipRow --silent -t "runnability"` exits 0 with at least 2 assertions passing, and the test asserts against `RUNNABILITY_LABELS` rather than retyped strings (`grep -c "RUNNABILITY_LABELS" src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx` returns at least 1)
    - Unknown-kind null-safety is tested with a genuinely unknown kind, not a real one: `grep -c "notAFilterKind" src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx` returns at least 1
    - D-28 negative — no count vocabulary anywhere in the module: `grep -v '^\s*\*' src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts | grep -v '^\s*//' | grep -ciE "count|total|matches"` returns 0
    - D-08 negative — no sort/layout/alphabet vocabulary: `grep -v '^\s*\*' src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts | grep -v '^\s*//' | grep -ciE "sortdescending|sortinstalled|alphabet|layout"` returns 0
    - The four minted tri-state keys are present exactly once each: `grep -cE "chipHiddenOnly|chipHiddenIncluded|chipNonAvailableOnly|chipNonAvailableIncluded" src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` returns 4
    - The three reused `More filters` keys are default-namespace and unprefixed: `grep -c "gamelib:header\." src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts` returns 0, and `grep -cE "header\.show_(support_offline_only|third_party_managed_only|updates_only)" ...` returns 3
    - `pnpm jest src/frontend/screens/Library/components/FilterChipRow --silent` exits 0 with at least 14 assertions passing
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest src/frontend/screens/Library/components/FilterChipRow --silent</automated>
  </verify>
  <done>One pure resolver turns every ActiveFilterDescriptor kind — runnability included — into a label spec, returns null only for genuinely unknown input, carries no count vocabulary, imports its label text from the phase's single declaration site, and is unit-tested without a DOM.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FilterChipRow — removable chips above the grid, with a real accessible name on every ×</name>
  <read_first>
    - src/frontend/screens/Library/index.tsx L840-870 (the exact render slot: `<LibraryHeader>` L847, `{showAlphabetFilter && <AlphabetFilter />}` L849, `EmptyLibraryMessage` L861, `GamesList` L863) and the `LibraryContext.Provider` value object at L778-817 (confirm `activeFilterDescriptors` is provided there and `__isDefaultLibraryContext` is NOT)
    - src/frontend/types.ts (`LibraryContextType` — `activeFilterDescriptors`, `__isDefaultLibraryContext`, and the exact current names of the tri-state, boolean and search setters; do not guess them)
    - src/frontend/screens/Library/LibraryContext.tsx (`initialContext` — the sentinel and the silent no-op setters it supplies)
    - src/frontend/screens/Library/components/AlphabetFilter/index.tsx (the sibling this row sits directly beneath — match its container conventions, and confirm you are not moving it)
    - .planning/phases/34.11-.../34.11-UI-SPEC.md: the `Chip row` row of the Layout table (L165), the Spacing Scale exceptions (chip padding), the Typography row `Chip label`, the `States & Interactions` rows `Chip` and `Clear all chip`, and the `Accessibility — chip removal control` block
    - .planning/phases/34.11-.../34.11-CONTEXT.md D-08, D-25, D-26, D-27, D-28
  </read_first>
  <files>src/frontend/screens/Library/components/FilterChipRow/index.tsx, src/frontend/screens/Library/components/FilterChipRow/index.scss, src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx, src/frontend/screens/Library/index.tsx</files>
  <behavior>
    - Given `activeFilterCount === 0`, the component returns `null` — no empty row, no bare `Clear all` chip hanging over an unfiltered grid.
    - Given a context carrying `__isDefaultLibraryContext: true`, the component calls `console.error` once with a message naming `FilterChipRow` and `LibraryContext.Provider`, then returns `null`. Given a real context with zero descriptors, it returns `null` and `console.error` is NOT called — the two silent-render cases are distinguishable at the console.
    - Given descriptors for `view:installed`, `store:gog` and `search`, exactly three removal chips render plus one `Clear all` chip, in descriptor order.
    - Given a descriptor for `runnability:bottle`, a chip renders labelled `Runs via bottle` with a working `×` — a Runnability selection is as visible and as removable as a Store selection.
    - Every removal chip's `×` control is a `<button>` carrying an `aria-label` produced from `gamelib:library.filterPanel.removeFilter` with `filterLabel` interpolated to that chip's own label — so with three chips active the three labels are three distinct strings, never three copies of `Remove`.
    - Clicking the `×` on a `store:gog` chip calls `setStoreFacet` with the previous array minus `'gog'`, never with an empty array and never with a string.
    - Clicking the `×` on a `runnability:bottle` chip calls `setRunnabilityFacet` with the previous array minus `'bottle'`.
    - Clicking the `×` on a `view:installed` chip calls `setLibraryView('all')`.
    - Clicking the `×` on a `collection:Backlog` chip calls `setCurrentCollection(null)`.
    - Clicking the `×` on a `showHidden:only` chip calls the showHidden setter with `'off'` — **not** `'show'`. The same holds from `showHidden:show`: the result is `'off'`. A tri-state `×` never steps the cycle (D-27).
    - Clicking the `×` on a boolean `More filters` chip calls its setter with `false`.
    - Clicking `Clear all` calls the context's `clearAllFilters` exactly once and calls no individual setter (D-25 — one clearing path, and it persists).
    - No chip element carries a count, a number, or a numeric child (D-28).
    - A descriptor for which `chipLabelSpec` returns `null` renders no chip at all and does not throw.
  </behavior>
  <action>
Create `src/frontend/screens/Library/components/FilterChipRow/index.tsx` exporting a default function
component `FilterChipRow` taking no props. Read everything it needs from
`useContext(LibraryContext)`: `activeFilterDescriptors`, `activeFilterCount`, `clearAllFilters`, and
each removal setter.

**The descriptor list comes straight off the context. Do not call `describeActiveFilters` here and
do not reassemble `FilterEngineState`.** Plan 04 calls it once, per render, and publishes both the
array and its length; "which filters are active" is D-26's rule and it lives in `filterEngine`. A
second call site would need its own copy of the state assembly and could disagree with the panel
about what is active.

**Guard the default context first.** `initialContext` supplies silent no-op setters and an empty
`activeFilterDescriptors`, so a `FilterChipRow` rendered outside `LibraryContext.Provider` is
otherwise byte-for-byte identical to one correctly mounted over an unfiltered library: both draw
nothing. That ambiguity is the 34.10 defect — a relocated `<Header>` rendered live-looking DEAD
filters and no automated signal existed. So: if the context object carries
`__isDefaultLibraryContext`, `console.error` a message naming this component and
`LibraryContext.Provider` (state plainly that the chip row is not inside the provider and its chips
would be inert), then return `null`. Only after that guard, return `null` when
`activeFilterCount === 0`. The two paths draw the same nothing on purpose — the console line is the
discriminator, and it is what the unit test asserts on.

Otherwise render `<div className="FilterChipRow">` containing one `<span className="FilterChipRow__chip">`
per descriptor whose `chipLabelSpec` is non-null. Resolve the spec to text with the matching hook:
`tGamelib(...)` for `ns: 'gamelib'` specs, plain `t(...)` for `ns: 'default'` specs, and the raw
string for literal specs. Give each chip React `key={descriptor.id}` — `id` is already stable and
unique per render, which is why plan 01 put it there.

Inside each chip, after the label text, render the removal control as a real
`<button type="button" className="FilterChipRow__remove">` whose visible child is the `×` glyph and
whose `aria-label` is
`tGamelib('gamelib:library.filterPanel.removeFilter', 'Remove {{filterLabel}} filter', { filterLabel })`,
interpolating that chip's own resolved label. This is a hard UI-SPEC requirement, not a nicety: the
glyph is not an accessible name, and a bare `Remove` announces four identical controls when four
chips are active. Do not attach the click handler to the `<span>` and do not use a `<div role="button">`.

Then render the `Clear all` chip last, as a real `<button type="button"
className="FilterChipRow__chip FilterChipRow__clearAll">` labelled
`tGamelib('gamelib:library.filterPanel.clearAll', 'Clear all')`, calling the context's
`clearAllFilters`. It needs no `aria-label` — its text is its name. Do not reimplement clearing here;
`clearAllFilters` already clears **and persists** (D-25), and a local re-implementation would be the
exact defect D-25 was written to prevent.

Removal handler: a single `removeFilter(descriptor)` switching on `descriptor.kind`:
`'view'` → set the view to `'all'`; `'collection'` → set the collection to `null`; `'store'` /
`'runnability'` → set that facet array to the current array filtered to drop `descriptor.value`
(array in, array out — never a scalar; `runnability` routes to `setRunnabilityFacet`, `store` to
`setStoreFacet`); `'search'` → call the context's existing search setter with an empty string (read
`types.ts` for its name); `'showHidden'` / `'showNonAvailable'` → set that tri-state to the literal
`'off'`, **unconditionally and regardless of its current value** — write a one-line comment citing
D-27 stating that stepping the cycle here would be a defect, not a nicety; the three `More filters`
booleans → set to `false`. Every kind that `chipLabelSpec` can label must also be removable: a chip
whose `×` does nothing is worse than no chip.

**D-08 boundary — do not touch sort, layout or the alphabet filter.** They are not filters. They stay
in the grid header where they are, unchanged. This component must not read `sortDescending`,
`sortInstalled`, `layout`, `showAlphabetFilter` or `alphabetFilterLetter`; must not render a chip for
any of them; and must not move, wrap or reorder `<LibraryHeader />` or `<AlphabetFilter />` at their
current positions in `Library/index.tsx`.

Create `src/frontend/screens/Library/components/FilterChipRow/index.scss` and import it from the
component. This surface lives in the grid/content area, **not** in the 204px tier-2 panel, so none of
the panel's width or `.NavShell__tier2Portal` scoping applies — but every selector must still be
scoped under `.FilterChipRow` (an unscoped selector already leaked app-wide once in 34.10). Declare:
`.FilterChipRow` — `display: flex; flex-wrap: wrap; gap: var(--space-md);` plus
`padding-block: var(--space-2xs)`; `.FilterChipRow__chip` — `border-radius: 9999px` (a literal pill,
per the UI-SPEC radius note; no spacing token applies to a full-pill radius),
`padding: var(--space-2xs) var(--space-xs)`, `font-size: var(--text-sm)`,
`font-weight: var(--regular)`, `line-height: 1`, `background: var(--navbar-background)`,
`border: 1px solid var(--neutral-04)`, `color: var(--text-default)`; its `:hover` —
`border-color: var(--status-danger); color: var(--status-danger)` (the removal-preview affordance,
and the **only** place `--status-danger` is used in this phase); its `:focus-visible` —
`outline: 2px solid var(--text-hover); outline-offset: -2px`. `.FilterChipRow__clearAll` overrides
`border-style: dashed` and `color: var(--text-secondary)`, with a `:hover` of
`color: var(--navbar-active); border-color: var(--navbar-active)` — it reads as a different kind of
action from a removal, which is the sketch's own distinction. `.FilterChipRow__remove` is an
unstyled-button reset (`background: none; border: 0; padding: 0; cursor: pointer; color: inherit`)
so it inherits the chip's hover colour rather than forking it.

Mount it in `src/frontend/screens/Library/index.tsx` on its own line immediately after
`{showAlphabetFilter && <AlphabetFilter />}` (L849 at HEAD) and before the `UpdateComponent` block —
above `GamesList`, below the existing header/alphabet row, exactly as the UI-SPEC Layout table
specifies. Add the import alongside the other `./components/*` imports. Change nothing else in the
render tree.

Append a `describe('FilterChipRow')` block to the existing
`src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx` covering every
`<behavior>` bullet, using the no-jsdom direct-invocation idiom from `CrossoverBadge.test.tsx`:
`jest.mock('react-i18next', …)` with a `t` that echoes its `defaultValue` **after substituting any
`{{var}}` from the options object** (you need real substitution here, or the distinct-aria-label
assertion passes vacuously), a mocked `LibraryContext` value you can re-point per case, then call
`FilterChipRow({})` as a plain function and walk the returned element's `props.children`. Assert
removal by invoking a chip's `×` `props.onClick` directly and checking the corresponding jest mock.
The D-27 assertion must be explicit: from `showHidden:show`, assert the setter received `'off'` and
add `expect(setShowHidden).not.toHaveBeenCalledWith('only')`. The sentinel assertion needs a
`jest.spyOn(console, 'error')`: one case with `__isDefaultLibraryContext: true` asserting the spy
fired and the result is `null`, one case with a real-shaped context and zero descriptors asserting
the result is `null` and the spy did NOT fire. Those two cases together are what make "dead surface"
distinguishable from "nothing selected"; either one alone proves nothing.

Add one source gate in the same file, in the `tier2Portal.test.ts` `fs.readFileSync`+regex style,
asserting the component source contains no `sortDescending|sortInstalled|alphabetFilterLetter|layout`
reference (D-08). Prove it non-vacuous before accepting it: temporarily insert one of those
identifiers into the component, confirm the test FAILS, then remove it. An unmatchable pattern passes
vacuously and would prove nothing.
  </action>
  <acceptance_criteria>
    - The descriptor list is consumed, not re-derived: `grep -c "activeFilterDescriptors" src/frontend/screens/Library/components/FilterChipRow/index.tsx` returns at least 1 and `grep -cE "describeActiveFilters|FilterEngineState" ...` returns 0
    - The default-context guard exists and is diagnosable: `grep -c "__isDefaultLibraryContext" src/frontend/screens/Library/components/FilterChipRow/index.tsx` returns 1 and `grep -c "console.error" ...` returns 1
    - Both silent-render cases are covered and distinguished: `pnpm jest src/frontend/screens/Library/components/FilterChipRow --silent -t "provider"` exits 0, and the test file contains both a `spyOn(console, 'error')` expectation that fires and one that asserts `not.toHaveBeenCalled` (`grep -c "not.toHaveBeenCalled" src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx` returns at least 1)
    - A runnability chip renders and removes: `pnpm jest src/frontend/screens/Library/components/FilterChipRow --silent -t "runnability"` exits 0, and `grep -c "setRunnabilityFacet" src/frontend/screens/Library/components/FilterChipRow/index.tsx` returns at least 1
    - The `×` carries a real interpolated accessible name: `grep -c "gamelib:library.filterPanel.removeFilter" src/frontend/screens/Library/components/FilterChipRow/index.tsx` returns 1 and `grep -c "filterLabel" ...` returns at least 2
    - No bare removal label: `grep -cE "aria-label=\"Remove\"|aria-label={'Remove'}" src/frontend/screens/Library/components/FilterChipRow/index.tsx` returns 0
    - Both controls are real buttons: `grep -c "type=\"button\"" src/frontend/screens/Library/components/FilterChipRow/index.tsx` returns 2
    - The aliased hook is used and literally named `tGamelib`: `grep -c "useTranslation('gamelib')" ...` returns 1 and `grep -c "tGamelib('gamelib:" ...` returns at least 2
    - D-25 single clearing path: `grep -c "clearAllFilters" src/frontend/screens/Library/components/FilterChipRow/index.tsx` returns at least 1, and `grep -cE "storage\.setItem|localStorage" ...` returns 0
    - D-27 — the tri-state removal writes the literal `'off'` and never `'show'`/`'only'`: `grep -c "'off'" src/frontend/screens/Library/components/FilterChipRow/index.tsx` returns at least 1 and `grep -cE "setShowHidden\('(show|only)'\)|setShowNonAvailable\('(show|only)'\)" ...` returns 0
    - D-08 negative in the component: `grep -cE "sortDescending|sortInstalled|alphabetFilterLetter|showAlphabetFilter" src/frontend/screens/Library/components/FilterChipRow/index.tsx` returns 0
    - D-08 negative in the mount: `git diff -U0 src/frontend/screens/Library/index.tsx | grep -cE "^-.*(LibraryHeader|AlphabetFilter)"` returns 0 — neither line was moved or removed
    - Mounted in the right slot: `grep -n "FilterChipRow" src/frontend/screens/Library/index.tsx` shows a render-site line number greater than the `AlphabetFilter` render line and less than the main `GamesList` render line
    - SCSS is scoped and uses the specified tokens, ignoring comments: `grep -v '^\s*//' src/frontend/screens/Library/components/FilterChipRow/index.scss | grep -c "FilterChipRow"` returns at least 5; the same filtered stream returns at least 1 for `9999px`, at least 1 for `var(--space-2xs) var(--space-xs)`, and at least 1 for `--status-danger`
    - `--status-danger` is hover-only: `grep -v '^\s*//' src/frontend/screens/Library/components/FilterChipRow/index.scss | grep -B2 -- "--status-danger" | grep -c ":hover"` returns at least 1
    - The D-08 source gate is proven non-vacuous: with `sortDescending` temporarily inserted into the component the test FAILS; removed, it passes
    - `pnpm jest src/frontend/screens/Library/components/FilterChipRow --silent` exits 0 with at least 26 assertions passing
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm jest src/frontend/screens/Library/components/FilterChipRow --silent</automated>
  </verify>
  <done>Every non-default filter — runnability included — shows as a removable pill above the grid, each × announces which filter it removes, a tri-state × returns to off, Clear all routes through the one persisting implementation, and a chip row rendered outside the provider says so at the console instead of pretending to be an unfiltered library.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FilterZeroResult — the empty state that names the filters, plus the i18n gate and its wave-4 reconciliation</name>
  <read_first>
    - src/frontend/screens/Library/components/EmptyLibrary/index.tsx (the whole file — the state this plan must NOT regress: it speaks to "you own no games", and it stays for that case)
    - src/frontend/screens/Library/index.tsx L855-870 (the `{libraryToShow.length === 0 && <EmptyLibraryMessage />}` line you are branching)
    - src/frontend/screens/Library/components/FilterChipRow/chipLabels.ts (Task 1's `chipLabelSpec` + `joinChipLabels` — this component must import them, not re-derive labels)
    - src/frontend/screens/Library/components/FilterChipRow/index.tsx (Task 2's default-context guard — copy its shape exactly, changing only the component name in the message)
    - meta/i18nGateScope.json (the `files` array — alphabetically ordered, with a `baseCommit`/`generatedAt` header you must not edit. Plans 06 and 07 both appended to it in wave 3; you are the sole writer in wave 4 and the reconciliation point)
    - .planning/phases/34.11-.../34.11-UI-SPEC.md Typography rows `Zero-result heading`, `Zero-result body`, `Zero-result action`, and the Copywriting rows `Empty state heading` / `Empty state body` / `Empty state action`
    - .planning/phases/34.11-.../34.11-CONTEXT.md D-25, D-30
  </read_first>
  <files>src/frontend/screens/Library/components/FilterZeroResult/index.tsx, src/frontend/screens/Library/components/FilterZeroResult/index.scss, src/frontend/screens/Library/index.tsx, src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx, meta/i18nGateScope.json</files>
  <behavior>
    - Given descriptors for `view:installed`, `store:gog` and `search:witcher`, the body text is exactly `No games match Installed + GOG + "witcher".` — D-30's own example, produced from the shared resolver.
    - Given descriptors including `runnability:bottle`, the body names it as `Runs via bottle` — the same string the chip row and the panel row show.
    - The body is never a generic message: with any filter active, the rendered body contains at least one active filter's label.
    - Given `activeFilterCount === 0` the component returns `null`, so a genuinely empty library still falls through to `EmptyLibraryMessage`.
    - Given a context carrying `__isDefaultLibraryContext: true`, the component `console.error`s once naming `FilterZeroResult` and returns `null`; with a real context and zero descriptors it returns `null` silently.
    - The inline action calls the context's `clearAllFilters` exactly once and calls no individual setter (D-25 — same behaviour as the chip row's `Clear all`).
    - A collection named `Backlog $t(header.uncategorized)` renders literally, including the `$t(` run — i18next nesting does not fire on the interpolated value.
    - A collection named `Backlog {{filters}}` renders literally — an interpolation token inside an interpolated value is not re-interpolated.
    - The component contains no `dangerouslySetInnerHTML` and no `Trans` element.
  </behavior>
  <action>
Create `src/frontend/screens/Library/components/FilterZeroResult/index.tsx` exporting a default
function component `FilterZeroResult` taking no props. Read `activeFilterDescriptors`,
`activeFilterCount` and `clearAllFilters` from `useContext(LibraryContext)` exactly as
`FilterChipRow` does — including the `__isDefaultLibraryContext` guard, in the same order (sentinel
first with its `console.error`, then the `activeFilterCount === 0` early return). Same reason: an
empty-state component rendered outside the provider would otherwise be indistinguishable from one
correctly mounted over an unfiltered library.

Build the filter sentence by mapping the same descriptors through Task 1's `chipLabelSpec`,
resolving each spec with `tGamelib`/`t`/literal exactly as the chip row does, dropping `null` specs,
and joining with `joinChipLabels`. **Import both functions — do not re-derive labels here.** The
whole point of D-30 is that the sentence names the filters the user can see as chips; two
implementations would eventually print a list the chip row does not show.

Render, inside `<div className="FilterZeroResult">`:
1. a heading element with `className="FilterZeroResult__heading"` whose text is
   `tGamelib('gamelib:library.filterPanel.emptyHeading', 'No games match your filters')`;
2. a `<p className="FilterZeroResult__body">` whose text is
   `tGamelib('gamelib:library.filterPanel.emptyBody', 'No games match {{filters}}.', { filters })`,
   where `filters` is the joined string. Pass it as an interpolation **value**, rendered as a plain
   React text child. Do not use `Trans`, do not enable markup parsing, and do not use
   `dangerouslySetInnerHTML` — see `<threat_model>`;
3. a real `<button type="button" className="FilterZeroResult__action">` labelled
   `tGamelib('gamelib:library.filterPanel.emptyClearAll', 'Clear all filters')`, calling the
   context's `clearAllFilters`. Same single clearing path as the chip row; do not reimplement it.

A generic "No results" message is a **failure of this plan**, not an acceptable fallback. If the
joined string is somehow empty while `activeFilterCount > 0`, that is a bug in the resolver — fix it
rather than papering over it with generic copy.

Create `src/frontend/screens/Library/components/FilterZeroResult/index.scss`, imported by the
component, every selector scoped under `.FilterZeroResult`. Use the UI-SPEC Typography rows exactly:
`.FilterZeroResult__heading` — `font-size: var(--text-md); font-weight: var(--bold);
line-height: 1.2`; `.FilterZeroResult__body` — `font-size: var(--text-sm);
font-weight: var(--regular); line-height: 1.4` (1.4, not 1, because this string wraps when several
filters are active). Do **not** separately weight or colour the interpolated filter names — the
sentence stays one uniform run, which is why there is no `<strong>` or accent span in step 2.
`.FilterZeroResult__action` — `font-size: var(--text-sm); font-weight: var(--regular);
line-height: 1.4`, `color: var(--navbar-active)`, an unstyled-button reset, and
`text-decoration: underline` on `:hover` only; it is distinguished by colour and underline, not by
size or weight, so no third weight enters the scale. Add a `:focus-visible` outline matching the chip
row's. Add no `--status-danger` here — that token is chip-hover-only in this phase.

Mount it in `src/frontend/screens/Library/index.tsx` by branching the existing L861 line: when
`libraryToShow.length === 0`, render `<FilterZeroResult />` if `activeFilterCount > 0`, else the
existing `<EmptyLibraryMessage />`. Keep `EmptyLibraryMessage` mounted for the unfiltered case and do
not edit `EmptyLibrary/index.tsx` at all — the two states answer different questions and merging them
would regress the "you own no games / you are not signed in" copy.

Append two `describe` blocks to
`src/frontend/screens/Library/components/FilterChipRow/__tests__/index.test.tsx`: one for
`FilterZeroResult`'s `<behavior>` bullets using the same direct-invocation idiom, and one source gate
asserting the component source contains neither `dangerouslySetInnerHTML` nor `<Trans`. The two
hostile-name cases (`$t(...)` and `{{filters}}` inside a collection name) are behavioural assertions,
not source greps: drive them through a mocked `t` that performs real `{{var}}` substitution and assert
the hostile run survives verbatim in the output. Prove the source gate non-vacuous before accepting it.

**Then do the i18n gate work, last, and reconcile the registry.** Append the two new component paths
to the `files` array of `meta/i18nGateScope.json`, in their correct alphabetical positions:
`src/frontend/screens/Library/components/FilterChipRow/index.tsx` and
`src/frontend/screens/Library/components/FilterZeroResult/index.tsx`. Change nothing else in that
file — not `baseCommit`, not `baseVersion`, not `generatedAt`, not `generatedBy`, and no existing
path. Do not run `pnpm gen-i18n-gate-scope`; regenerating rewrites the header fields and buries the
lines that matter.

**This plan is the reconciliation point for that registry.** Plans 06 and 07 both appended to it in
wave 3, as parallel executors, into alphabetically adjacent positions in the same array. A lost
update there does not fail loudly — a dropped path simply means a file silently stops being scanned
by `meta/hardcodedStringGate.ts`. You are the sole writer in wave 4, after both, so you are the first
point at which the final state can be checked. Re-read the file from disk, then verify all six wave-3
paths are present:

- `src/frontend/components/UI/NavShell/components/FilterViewList/index.tsx` (plan 06)
- `src/frontend/components/UI/NavShell/components/FilterCollectionList/index.tsx` (plan 06)
- `src/frontend/components/UI/NavShell/components/FilterFacetGroup/index.tsx` (plan 07)
- `src/frontend/components/UI/NavShell/components/FilterMoreGroup/index.tsx` (plan 07)
- `src/frontend/components/UI/NavShell/components/FilterRunnabilityFacet/index.tsx` (plan 07)
- `src/frontend/components/UI/NavShell/components/FilterStoreFacet/index.tsx` (plan 07)

Any that are missing were lost in the wave-3 merge: **add them back**, and record the repair
prominently in the SUMMARY (which paths, and that the loss happened between plans 06/07 and here) so
the phase's verification does not read a repaired file as evidence that nothing went wrong. Do the
whole edit as one additive, idempotent read-modify-write over the parsed JSON — add only paths not
already present, remove nothing, leave the array sorted and duplicate-free. `facetLabels.ts` is
deliberately NOT registered: that registry drives the hardcoded-string gate and `facetLabels.ts`
exists to hold literal brand names.

Then run `pnpm lint-translations:gamelib`, fix any finding, and confirm the nine keys from
`<i18n_contract>` actually landed in `public/locales/en/gamelib.json` — a green jest run does not
imply a green gate, and the alias must literally be `tGamelib` for the parser to see these calls at
all.
  </action>
  <acceptance_criteria>
    - The shared resolver is imported, not duplicated: `grep -c "chipLabelSpec" src/frontend/screens/Library/components/FilterZeroResult/index.tsx` returns at least 1 and `grep -c "joinChipLabels" ...` returns at least 1
    - The descriptor list is consumed, not re-derived: `grep -c "activeFilterDescriptors" src/frontend/screens/Library/components/FilterZeroResult/index.tsx` returns at least 1 and `grep -cE "describeActiveFilters|FilterEngineState" ...` returns 0
    - The default-context guard matches the chip row's: `grep -c "__isDefaultLibraryContext" src/frontend/screens/Library/components/FilterZeroResult/index.tsx` returns 1 and `grep -c "console.error" ...` returns 1
    - D-30 — the body interpolates the real filter list: `grep -c "gamelib:library.filterPanel.emptyBody" src/frontend/screens/Library/components/FilterZeroResult/index.tsx` returns 1 and `grep -c "filters" ...` returns at least 2
    - A runnability filter is named in the sentence: `pnpm jest src/frontend/screens/Library/components/FilterChipRow --silent -t "Runs via bottle"` exits 0
    - No generic fallback copy exists: `grep -ciE "no results|nothing found|try again" src/frontend/screens/Library/components/FilterZeroResult/index.tsx` returns 0
    - No unsafe render path: `grep -cE "dangerouslySetInnerHTML|<Trans" src/frontend/screens/Library/components/FilterZeroResult/index.tsx` returns 0
    - D-25 single clearing path: `grep -c "clearAllFilters" src/frontend/screens/Library/components/FilterZeroResult/index.tsx` returns at least 1 and `grep -cE "setStoreFacet|setLibraryView|setCurrentCollection" ...` returns 0
    - `EmptyLibraryMessage` survives for the unfiltered case: `grep -c "EmptyLibraryMessage" src/frontend/screens/Library/index.tsx` returns at least 2 (import + render), and `git diff --name-only HEAD -- src/frontend/screens/Library/components/EmptyLibrary` is empty
    - Typography matches the UI-SPEC, ignoring comments: `grep -v '^\s*//' src/frontend/screens/Library/components/FilterZeroResult/index.scss | grep -c "var(--text-md)"` returns 1 and the same filtered stream returns 2 for `var(--text-sm)` and 2 for `line-height: 1.4`
    - The sentence is one uniform run — no accent or weight on the interpolated names: `grep -v '^\s*//' src/frontend/screens/Library/components/FilterZeroResult/index.scss | grep -cE "--status-danger|font-weight: var\(--bold\)" | head -1` returns 1 (the heading's bold only, no danger token)
    - This plan's own two paths are registered: `grep -c "FilterChipRow/index.tsx" meta/i18nGateScope.json` returns 1 and `grep -c "FilterZeroResult/index.tsx" meta/i18nGateScope.json` returns 1
    - All six wave-3 paths survived or were repaired: `grep -cE "Filter(ViewList|CollectionList|FacetGroup|MoreGroup|RunnabilityFacet|StoreFacet)/index\.tsx" meta/i18nGateScope.json` returns 6; if any had to be re-added, the SUMMARY names them
    - The shared constants module is deliberately NOT in scope: `grep -c "facetLabels" meta/i18nGateScope.json` returns 0
    - The registry edit is additive, sorted and header-clean: `git diff meta/i18nGateScope.json | grep -c "^-[^-]"` returns 0; `git diff meta/i18nGateScope.json | grep -cE "baseCommit|generatedAt|baseVersion|generatedBy"` returns 0; `node -e "const f=require('./meta/i18nGateScope.json').files; if(new Set(f).size!==f.length) process.exit(1); if(f.join()!==[...f].sort().join()) process.exit(1)"` exits 0
    - `pnpm lint-translations:gamelib` exits 0
    - All nine minted keys are in the catalog: `grep -cE "clearAll|removeFilter|chipHidden|chipNonAvailable|emptyHeading|emptyBody|emptyClearAll" public/locales/en/gamelib.json` returns at least 9
    - The nesting/interpolation behavioural cases pass: `pnpm jest src/frontend/screens/Library/components/FilterChipRow --silent -t "hostile"` exits 0
    - `pnpm jest src/frontend/screens/Library --silent` exits 0
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint-translations:gamelib && pnpm jest src/frontend/screens/Library --silent</automated>
  </verify>
  <done>A filtered-to-nothing library names the filters responsible and offers an inline Clear all filters, the genuinely-empty library still gets EmptyLibraryMessage, both new files are inside a green i18n gate, and every wave-3 registration is verified present rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `configStore` (`games.customCategories`) → i18n interpolation → rendered copy | Collection names are user-authored free text. This plan is the first place in the phase where such a name is fed into a translated **sentence** as an interpolation value (`{{filters}}`, `{{filterLabel}}`) rather than rendered as its own isolated label. |
| Search input → i18n interpolation → rendered copy | The raw `filterText` the user typed is quoted into the same two interpolation slots. Same class of value as above, arriving from a keystroke rather than from persisted state. |
| `localStorage` → plan 04 state → descriptors → this plan's render | A locally-tampered filter key can produce a descriptor with an unexpected `value`. This plan renders descriptors without re-validating them. |
| concurrent wave-3 agents → `meta/i18nGateScope.json` → this plan's read | The registry reaching this plan may have lost a path in a parallel merge, silently narrowing the hardcoded-string gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.11-18 | Tampering | `{{filters}}` / `{{filterLabel}}` interpolation of user-controlled strings | mitigate | Be precise about what i18next does and does not protect. react-i18next runs with `interpolation.escapeValue: false` (React escapes text children itself), so **i18next is not the control here — React is**. Both interpolated results are rendered as plain React text children of a `<p>` and as an `aria-label` string attribute. Neither goes through `Trans`, markup parsing, or `dangerouslySetInnerHTML`, which is what the Task 3 source gate pins. Under those two conditions a hostile name is inert markup-wise. |
| T-34.11-19 | Tampering | i18next **nesting** (`$t(...)`) reaching the interpolation slot | mitigate | i18next resolves `$t(...)` nesting against the **template**, not against interpolated values, and `interpolation.skipOnVariables` defaults to `true` in the version in this tree — so a collection named `Backlog $t(header.uncategorized)` should render literally rather than resolving another key. That is a claim, not a guarantee, and it depends on config this plan does not own, so it is pinned by a behavioural test rather than by prose: two hostile-name cases (`$t(...)` and a literal `{{filters}}` token inside a collection name) assert the run survives verbatim. If either fails, the fix is to set `skipOnVariables: true` explicitly in the i18n init and re-run — not to sanitise the name, which would corrupt a legitimate collection title. |
| T-34.11-20 | Information disclosure | the zero-result sentence and the chip labels | accept | Everything named is data the app persisted for this same local user, or text they just typed, displayed back to them in their own window. There is no cross-user, cross-account, log or network sink in this plan. Naming the filters is the explicit product requirement (D-30); withholding them would be the defect. |
| T-34.11-21 | Denial of service | a pathologically long collection name or search term | accept | The chip row is `flex-wrap: wrap` and the empty-state body has `line-height: 1.4` precisely because it wraps, so a long value degrades to more rows rather than to overflow. The value is self-inflicted — the user typed it or named the collection — and no truncation is specified by the UI-SPEC. Inventing an ellipsis rule here would add an unreviewed visual contract, and truncating the filter list would directly undercut D-30's "name the filters responsible". |
| T-34.11-22 | Tampering | a tampered `localStorage` filter value producing an unmapped descriptor | mitigate | `chipLabelSpec` returns `null` — never a fabricated or raw-value label — for a `kind` outside the union or a value outside `RunnerToStore` / `RUNNABILITY_LABELS`, and the chip row drops `null` specs without throwing. A tampered value therefore renders no chip instead of rendering attacker-chosen text into the UI. Crucially, that `null` path is NOT how a legitimate kind is handled: every one of the ten real kinds has an explicit case, enforced by an exhaustive `switch` with a `never` default, so "unsupported input" and "unimplemented feature" cannot collapse into the same silent outcome. Validation of the value itself stays at plan 04's initialisers, the one place that owns it. |
| T-34.11-23 | Spoofing | a chip row or empty state rendered outside `LibraryContext.Provider` | mitigate | `initialContext`'s silent no-op setters plus an empty descriptor list make a dead surface look exactly like a live unfiltered one — the 34.10 defect. Plan 04 marks `initialContext` with `__isDefaultLibraryContext` and both components in this plan detect it and `console.error` before returning `null`, with paired unit tests asserting the error fires in the dead case and does NOT fire in the legitimately-empty case. |
| T-34.11-24 | Tampering | a wave-3 registration lost from `meta/i18nGateScope.json` | mitigate | This plan re-reads the registry, asserts all six wave-3 paths are present, re-adds any that vanished and reports the repair in the SUMMARY. Without this the failure is silent by construction: an unregistered file is simply never scanned, and every gate still passes. |
| T-34.11-SC | Tampering | npm/pnpm/cargo installs | mitigate | **No packages are installed by this plan.** Every import already exists in the tree (`react`, `react-i18next`). If an install appears necessary, STOP — it is out of scope and requires a blocking human legitimacy checkpoint against `RESEARCH.md`'s package audit. |
</threat_model>

<verification>
- `pnpm jest src/frontend/screens/Library --silent` exits 0
- `pnpm lint-translations:gamelib` exits 0 (STANDING BLOCKING requirement from Phase 34.8 — a green
  jest run does not imply this gate is green)
- `npx tsc --noEmit -p tsconfig.json` exits 0
- `pnpm test:ci` exits 0
- **Live verification, started with `pnpm tauri:dev`** (never `tauri dev` — that serves a stale static
  bundle):
  1. Select `Installed`, tick `GOG`, and type a term in search. Confirm three chips plus a dashed
     `Clear all` chip appear above the grid and below the alphabet row.
  2. Tick a Runnability option (`Runs natively` or `Runs via bottle`). Confirm a fourth chip appears
     carrying that same label, and that its `×` clears that facet.
  3. Click one chip's `×`. Confirm that one filter drops and the others stay.
  4. Set `showHidden` to `only`, then click its chip's `×`. Confirm it goes to `off` — not to `show`
     (D-27).
  5. Filter down to zero results. Confirm the empty state **names the active filters** in its body
     sentence, not a generic message (D-30).
  6. Click `Clear all filters` in the empty state, then relaunch the app. Confirm the filters are
     still cleared (D-25 — the cleared state persisted).
  7. Confirm the sort control, layout toggle and alphabet filter are exactly where they were before
     this phase, and that none of them produced a chip (D-08).
- **Multi-theme check — never `midnightMirage` alone.** Repeat steps 1 and 5 in `gruvbox_dark` and
  `dracula`, confirming chip borders, the `--status-danger` hover, the dashed `Clear all` chip and
  the zero-result action colour (`--navbar-active`) are all legible in each.
- `grep -c "Fuse" src/frontend/screens/Library/index.tsx` is unchanged from HEAD — Fuse.js removal is
  out of scope. Results including fuzzy near-misses is expected behaviour, not a defect.
- `meta/i18nGateScope.json` final state carries plan 06's two paths, plan 07's four and this plan's
  two, sorted and duplicate-free, with the generated header untouched. Any path this plan had to
  re-add is named in the SUMMARY as a wave-3 lost update, not quietly repaired.
- Untouched-file check: `git diff --name-only HEAD` must NOT list
  `src/frontend/screens/Library/components/EmptyLibrary/index.tsx`,
  `src/frontend/screens/Library/components/AlphabetFilter/index.tsx`,
  `src/frontend/screens/Library/components/LibraryHeader/index.tsx`,
  `src/frontend/screens/Library/facetLabels.ts` (plan 04 owns it — this plan imports it),
  `src/frontend/types.ts` (plan 04 owns the whole context change; if this plan believes it needs a
  type edit there, stop — the field it wants is already on `LibraryContextType`), or anything under
  `src/frontend/components/UI/NavShell/` (plans 06/07 own those).
</verification>

<success_criteria>
- Every non-default filter, views and runnability included, renders as a removable pill above the
  grid; `All games` renders none (D-26, REQ-34.11-13)
- Every one of the ten `ActiveFilterDescriptor` kinds has an explicit label case, enforced at compile
  time; `null` is reachable only from genuinely unknown input
- Chips are rendered from `activeFilterDescriptors` off the context — `describeActiveFilters` is
  called exactly once in the app, by plan 04
- Every chip `×` is a real `<button>` with an interpolated `aria-label` naming its own filter — four
  active chips produce four distinct accessible names
- A tri-state chip's `×` returns the filter to `off`, proven by an assertion that also rejects the
  cycle-stepping behaviour (D-27)
- No chip carries a count (D-28)
- The zero-result state names the actual active filters and offers an inline `Clear all filters`
  (D-30, REQ-34.11-14); a generic message is a failure
- Both `Clear all` controls route through plan 04's single persisting `clearAllFilters` (D-25)
- A surface rendered outside the provider says so at the console and is distinguishable from an
  unfiltered library, proven by paired assertions
- D-08 is held by assertions, not prose: no sort/layout/alphabet vocabulary in either new component,
  and neither `LibraryHeader` nor `AlphabetFilter` moved
- The chip row and the empty state resolve labels through one shared module, which itself imports
  store and runnability label text from plan 04's `facetLabels.ts`
- Both new component files are in `meta/i18nGateScope.json`, all six wave-3 registrations are
  verified present, and `pnpm lint-translations:gamelib` is green
</success_criteria>

<output>
Create `.planning/phases/34.11-library-filtering-search-views-collections-and-cross-store-f/34.11-08-SUMMARY.md` when done
</output>
