---
phase: 27-tauri-shell-walking-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/backend/sidecar/bootstrap.ts
  - src/backend/sidecar/pathShim.ts
  - src/backend/sidecar/fileStore.ts
  - src/backend/sidecar/electronStub.ts
  - src/backend/sidecar/sidecarRpc.ts
  - src/backend/sidecar/handlers.ts
  - src/backend/sidecar/__tests__/bootstrap.test.ts
autonomous: true
requirements: [REQ-27-02]

must_haves:
  truths:
    - "The Node sidecar boots headless under bare node (electron absent) without an import-time fault and prints the READY sentinel"
    - "Backend modules import successfully because app.getPath and electron-store are shimmed BEFORE any backend module loads"
    - "A ping request over stdio JSON-RPC round-trips against a registered handler"
  artifacts:
    - path: "src/backend/sidecar/bootstrap.ts"
      provides: "Sidecar entrypoint: installs the electron-module shim, then imports backend, then starts the RPC server and prints READY"
      contains: "READY_SENTINEL"
    - path: "src/backend/sidecar/electronStub.ts"
      provides: "electron-module replacement (app.getPath, electron-store-free Store, ipcMain recorder, shell.openExternal→RPC)"
      contains: "getPath"
    - path: "src/backend/sidecar/fileStore.ts"
      provides: "Minimal file-backed electron-store replacement writing the same on-disk JSON the Electron build uses"
  key_links:
    - from: "src/backend/sidecar/bootstrap.ts"
      to: "electronStub.ts"
      via: "Module._load hook installed before backend import"
      pattern: "_load|Module\\._load|require"
    - from: "src/backend/sidecar/sidecarRpc.ts"
      to: "handlers.ts"
      via: "recorded ipcMain handlers dispatched by channel"
      pattern: "invoke|handle"
---

<objective>
Build the sidecar bootstrap that makes GameLib's existing Node backend run headless — the spike-009 "sharp edge." The backend faults at IMPORT time (`app.getPath()` at module scope in `paths.ts`; `electron-store` throwing at construction in `electron_store.ts`), so the entrypoint must install a path shim + a minimal store replacement + an electron-module stub BEFORE importing any backend module, then serve the transport contract over stdio and signal ready.

Purpose: REQ-27-02 — the supervised sidecar that starts headless with the import-time Electron coupling shimmed and signals ready. This is the generic transport half; 27-04 registers the two E2E flows' specific channels on top of it.

Output: `src/backend/sidecar/*` (bootstrap, shims, RPC server, a placeholder handlers registry) plus a headless-boot test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-tauri-shell-walking-skeleton/27-CONTEXT.md
@.planning/spikes/009-node-backend-headless-sidecar/README.md
@.planning/phases/27-tauri-shell-walking-skeleton/27-01-SUMMARY.md

<interfaces>
<!-- The exact import-time Electron touchpoints the sidecar must satisfy. Extracted from the codebase. -->

From src/backend/constants/paths.ts (runs at MODULE SCOPE — the first fault):
- `let configFolder = app.getPath('appData')`  (line 9)
- `export const userDataPath = app.getPath('userData')`  (line 45)
- also does a filesystem legacy-config rename at import; keep that path logic working under the shim.

From src/backend/electron_store.ts (throws at construction — the second wall):
- `import Store from 'electron-store'` then `new Store(options)` in TypeCheckedStoreBackend constructor.
- electron-store throws "You need to call .initRenderer() from the main process" under bare node → must be replaced, not shimmed (spike 009). 20 files route through this class.

From src/backend/ipc.ts (the handler-registration surface the RPC server dispatches to):
- addHandler(channel, handler)     → ipcMain.handle(channel, ...)      // req/resp
- addListener(channel, listener)   → ipcMain.on(channel, ...)          // fire-and-forget
- sendFrontendMessage(channel, ...) → mainWindow.webContents.send(...) // backend→frontend push

From src/common/types/sidecarTransport.ts (27-01): SidecarRpcRequest/Response/Notification, READY_SENTINEL, command names.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Path shim + minimal file-backed store + electron-module stub</name>
  <read_first>
    - src/backend/constants/paths.ts (every import-scope app.getPath call to satisfy)
    - src/backend/electron_store.ts (the TypeCheckedStoreBackend the file store replaces under the hood)
    - src/common/types/electron_store.ts (StoreStructure / ValidStoreName the store must type against)
    - src/backend/utils.ts (openUrlOrFile — the shell.openExternal shape the stub must forward)
  </read_first>
  <action>
    Create three shim modules. `pathShim.ts`: a `getPath(name)` resolving Electron's `appData`/`userData`/`temp`/`home` names to real OS dirs (reuse `os.homedir()` + platform appData conventions) so `paths.ts` gets the same GameLib config folder the Electron build uses. `fileStore.ts`: a minimal synchronous JSON-file-backed store implementing the `electron-store` surface the backend actually calls (`get`/`set`/`has`/`delete`/`clear`/`store` + constructor `options.cwd`/`options.name`), reading and writing the SAME on-disk JSON files the Electron build's electron-store uses (derive paths from pathShim + options) so the sidecar and any snapshot stay consistent. `electronStub.ts`: an `electron` module replacement object exposing `app` (getPath via pathShim, `isPackaged`, `getAppPath`, no-op lifecycle), a `Store`-free path for electron-store consumers, an `ipcMain` recorder (`handle`/`on`/`once` capture handlers into an exported registry keyed by channel), `safeStorage` (minimal encrypt/decrypt passthrough for the skeleton — token security via keyring is deferred per CONTEXT), and `shell.openExternal(url)` that forwards the URL to the Rust shell as an `openExternal` SidecarRpcRequest over the RPC channel (Task 2). Do not implement BrowserWindow/tray/updater — out of scope.
  </action>
  <acceptance_criteria>
    - `fileStore.ts` `get/set/has/delete` round-trip against a temp JSON file (proven in Task 3 test).
    - `grep -c "getPath" src/backend/sidecar/pathShim.ts` ≥ 1 and it maps at least `appData` and `userData`.
    - `electronStub.ts` exports an `ipcMain`-shaped recorder whose `handle`/`on` populate an exported handler registry.
    - `electronStub.ts` `shell.openExternal` routes to the RPC layer (references the `OPEN_EXTERNAL`/`openExternal` contract from sidecarTransport), not to any electron import.
    - No file under src/backend/sidecar imports from `'electron'` or `'electron-store'`.
  </acceptance_criteria>
  <verify>
    <automated>grep -rc "from 'electron'\|from \"electron\"\|'electron-store'" src/backend/sidecar/ | grep -v ':0' || echo "clean: no electron imports"</automated>
  </verify>
  <done>The three import-time walls (app.getPath, electron-store, shell) have headless replacements ready to install before backend import.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap entry + stdio JSON-RPC server + READY signal</name>
  <read_first>
    - src/backend/sidecar/electronStub.ts (the recorder registry the RPC server dispatches to — Task 1)
    - src/backend/ipc.ts (addHandler/addListener/sendFrontendMessage the backend uses)
    - src/common/types/sidecarTransport.ts (frame shapes, READY_SENTINEL, notification kind)
    - .planning/spikes/009-node-backend-headless-sidecar/README.md (Module._load hook technique used by the probes)
  </read_first>
  <action>
    Create `sidecarRpc.ts`: a stdio JSON-RPC server that reads newline-delimited `SidecarRpcRequest` frames from `process.stdin`, dispatches `invoke`/`send` to the matching handler in electronStub's recorder registry, writes `SidecarRpcResponse` (by id) to `process.stdout`, and exposes a `pushFrontendMessage(channel, ...args)` that writes a `SidecarNotification` frame — wire the electronStub's `sendFrontendMessage` and `shell.openExternal` to emit through this. Create `handlers.ts` as a placeholder registry that registers exactly one `health`/`ping` invoke handler for now (27-04 expands it with the real flow channels). Create `bootstrap.ts` (the sidecar entrypoint `build:sidecar` bundles): install a `Module._load` hook that maps BOTH module-resolution walls — `require('electron')` → `electronStub` AND `require('electron-store')` → the `fileStore` replacement (electron-store is a SEPARATE module that throws in its constructor at import scope in `electron_store.ts`; intercepting only `'electron'` leaves that second wall standing) — installed BEFORE importing anything from `backend/`; then import `./handlers` (which pulls in the backend registration path); then start the RPC server; then print `READY_SENTINEL` on stdout so the Rust shell knows the sidecar is live. Order is load-bearing — the hook must be installed before the first backend import so `paths.ts` (app.getPath) and `electron_store.ts` (new Store) both see the shims.
  </action>
  <acceptance_criteria>
    - `bootstrap.ts` installs the `Module._load`/require hook textually BEFORE any `import ... from 'backend/...'` (source-order grep: the hook install line precedes the first backend import).
    - The hook maps BOTH `'electron'`→electronStub AND `'electron-store'`→fileStore (grep shows both module names handled).
    - A headless `require` of `backend/electron_store` (the TypeCheckedStoreBackend `new Store()` path) under the installed hook does NOT throw the electron-store `initRenderer` error — proven in Task 3.
    - `bootstrap.ts` prints `READY_SENTINEL` only after the RPC server is listening.
    - A `health`/`ping` invoke request written to stdin produces a matching `SidecarRpcResponse` on stdout.
    - `sendFrontendMessage`/`shell.openExternal` from the stub produce well-formed `SidecarNotification` / `openExternal` frames on stdout.
  </acceptance_criteria>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <done>The sidecar boots headless, serves the transport contract, and signals ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Headless-boot integration test</name>
  <read_first>
    - src/backend/sidecar/bootstrap.ts (the entry under test — Task 2)
    - src/backend/storeManagers/steam/__tests__/library.test.ts (project jest conventions for steam backend)
  </read_first>
  <behavior>
    - Test 1: building + running the sidecar entry under bare `node` (electron absent) reaches READY without an uncaught exception (proves paths.ts + electron-store import-time walls are shimmed).
    - Test 2: a `health`/`ping` invoke frame written to the sidecar's stdin yields a matching `SidecarRpcResponse` on stdout within a timeout.
    - Test 3: importing a REAL backend module under the installed stub does not throw — specifically `require('backend/electron_store')` constructs a store (electron-store wall shimmed) AND `require('backend/storeManagers/steam/library')` imports headlessly (the transitive Electron coupling the live 27-05 run hits), and at least one ipcMain handler is recorded in the electronStub registry.
  </behavior>
  <action>
    Add `bootstrap.test.ts` that either (a) spawns the bundled sidecar as a child process and asserts READY + ping round-trip over stdio, or (b) — if a full child-process build is too heavy for jest — drives `bootstrap`'s exported init function in-process with the Module._load hook installed and asserts the same observable outcomes. Follow the project's real-tmpdir black-box pattern (steam library.test.ts precedent) rather than mocking node:fs, since this project's ts-jest/CJS interop makes fs exports non-mockable. Assert no `'electron'`/`'electron-store'` real import is resolved (the stub intercepts).
  </action>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=sidecar/bootstrap` passes with ≥ 3 assertions covering the three behaviors.
    - The test proves READY is reached with electron absent (no uncaught import-time fault).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --testPathPattern=sidecar/bootstrap</automated>
  </verify>
  <done>Headless boot is proven by an automated test: the backend imports and serves RPC under bare node.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust shell → sidecar stdin | request frames enter the Node process |
| sidecar → on-disk store JSON | persistence writes cross into the filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Tampering | fileStore JSON path resolution | mitigate | Resolve store paths only from pathShim + electron-store `options.cwd`/`name`; never from RPC-supplied paths, so a request cannot redirect a write outside the GameLib config dir. |
| T-27-04 | DoS | stdio frame parser | mitigate | Enforce newline-delimited framing with a max-line cap; drop/log malformed frames rather than crashing the sidecar (fail-soft, keeps READY). |
| T-27-05 | Info disclosure | safeStorage passthrough | accept | Skeleton uses a minimal passthrough for the dev build; real keyring-backed token security is explicitly deferred to a later phase per CONTEXT (no production secrets in the skeleton). |
</threat_model>

<verification>
- `npm test -- --testPathPattern=sidecar/bootstrap` green.
- `npm run codecheck` passes.
- `grep -r "from 'electron'" src/backend/sidecar/` returns nothing.
</verification>

<success_criteria>
The existing Node backend imports and runs headless as a sidecar — import-time `app.getPath` and `electron-store` walls shimmed — serves the stdio JSON-RPC contract, and signals READY, all proven by an automated test.
</success_criteria>

<output>
Create `.planning/phases/27-tauri-shell-walking-skeleton/27-02-SUMMARY.md` when done.
</output>
