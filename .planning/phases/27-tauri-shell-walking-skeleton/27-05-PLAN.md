---
phase: 27-tauri-shell-walking-skeleton
plan: 05
type: execute
wave: 4
depends_on: ["27-04"]
files_modified:
  - .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md
autonomous: false
requirements: [REQ-27-06]

must_haves:
  truths:
    - "npm run tauri:dev launches a native Tauri window on macOS showing the real GameLib UI"
    - "The Steam library list in that window is populated by the Node sidecar (real data path), not mocked"
    - "Clicking a Steam game's launch fires a real steam:// handoff via the opener parity path"
    - "The Electron build still works (npm start) — the port is additive/reversible"
    - "The ported-vs-stubbed seam boundary is documented so later phases can port the remaining ~217 endpoints incrementally"
  artifacts:
    - path: ".planning/phases/27-tauri-shell-walking-skeleton/SEAM.md"
      provides: "The seam boundary: which channels/APIs are ported vs stubbed, and the incremental-port checklist for later phases"
      contains: "Ported"
  key_links:
    - from: "SEAM.md"
      to: "the 2–4 wired channels + the deferred ~217 endpoints"
      via: "documented boundary"
      pattern: "refreshLibrary|pushGameToLibrary|launch"
---

<objective>
Prove the walking skeleton runs on real macOS and document the seam so the rearchitecture can proceed incrementally. This is the phase's payoff: a native Tauri window rendering the real GameLib UI, the Steam library populated by the live sidecar, and a real `steam://` launch — with the Electron build still working (additive/reversible).

Purpose: REQ-27-06 — the slice runs as a macOS dev build, the Electron build still works, and the ported-vs-stubbed boundary is documented. This checkpoint also confirms REQ-27-01/04/05 hold in the assembled build (they were unit/integration-proven upstream; here they are confirmed live).

Output: `SEAM.md` (the boundary map + incremental-port checklist) and a human-verified live run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-tauri-shell-walking-skeleton/27-CONTEXT.md
@.planning/spikes/009-node-backend-headless-sidecar/README.md
@.planning/phases/27-tauri-shell-walking-skeleton/27-01-SUMMARY.md
@.planning/phases/27-tauri-shell-walking-skeleton/27-02-SUMMARY.md
@.planning/phases/27-tauri-shell-walking-skeleton/27-03-SUMMARY.md
@.planning/phases/27-tauri-shell-walking-skeleton/27-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the seam-boundary document</name>
  <read_first>
    - .planning/spikes/009-node-backend-headless-sidecar/README.md (the 16-API / 44-file / 220-endpoint map — the full surface being ported incrementally)
    - src/backend/sidecar/steamFlowRegistration.ts (what is actually wired: the 2–4 channels)
    - src/backend/sidecar/electronStub.ts (which Electron APIs are shimmed vs stubbed vs deferred)
    - .planning/phases/27-tauri-shell-walking-skeleton/27-04-SUMMARY.md (the flows as built)
  </read_first>
  <action>
    Write `SEAM.md` recording the ported-vs-stubbed boundary: (1) PORTED — the transport contract, the three re-pointed factories + synchronous store snapshot bridge, the sidecar bootstrap shims (pathShim, fileStore, electronStub), and the exact channels wired for the two flows (refreshLibrary, pushGameToLibrary, launch, sidecar store snapshot). (2) STUBBED/MINIMAL — safeStorage passthrough (real keyring deferred), the fileStore covering only the stores the flows touch, shell limited to openExternal. (3) DEFERRED — the remaining ~217 IPC endpoints, the project-wide electron-store swap, the 44-file lifecycle/dialog/tray/updater/protocol cluster, session/powerSaveBlocker, Windows/Linux packaging, Electron removal. Include an incremental-port checklist keyed off spike 009's 16-API ranking so a later phase can pick the next slice. Keep it a boundary map, not a narrative.
  </action>
  <acceptance_criteria>
    - `SEAM.md` has three explicit sections (Ported / Stubbed / Deferred) and lists the four wired channels by name.
    - It references the ~217 remaining endpoints and the spike-009 16-API ranking as the incremental-port backlog.
    - `grep -c "refreshLibrary\|pushGameToLibrary\|launch" SEAM.md` ≥ 3.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md && grep -c "Ported\|Stubbed\|Deferred" .planning/phases/27-tauri-shell-walking-skeleton/SEAM.md</automated>
  </verify>
  <done>The seam boundary is documented with an incremental-port checklist for later phases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live macOS dev-build verification (skeleton end-to-end + Electron intact)</name>
  <read_first>
    - .planning/phases/27-tauri-shell-walking-skeleton/27-CONTEXT.md (the 5 success criteria)
    - package.json (tauri:dev, build:sidecar, start scripts)
  </read_first>
  <what-built>The full walking skeleton: Tauri shell (27-01) + headless sidecar (27-02) + re-pointed renderer bridge (27-03) + the two wired E2E flows (27-04). All units/integration tests pass; this confirms the assembled build on real macOS hardware — which automated tests cannot (native GUI window).</what-built>
  <how-to-verify>
    1. Build the sidecar and launch the shell: `npm run build:sidecar` then `npm run tauri:dev`. Expect a NATIVE Tauri window (not an Electron window) showing the real GameLib UI (sidebar, library grid). [REQ-27-01]
    2. With a Steam account already connected (real dev-machine Steam session), confirm the library view populates Steam games — these arrive from the Node sidecar's real steam-user path, not a mock. Watch the sidecar stdout for the READY sentinel and refresh activity. [REQ-27-04]
    3. Click an installed Steam game's Launch. Confirm Steam.app receives the `steam://rungameid/…` handoff (the game/Steam launches). [REQ-27-05]
    4. Separately run `npm start` (the existing Electron build) and confirm it still launches and works unchanged. [REQ-27-06 additive/reversible]
  </how-to-verify>
  <acceptance_criteria>
    - A native Tauri window opens on macOS rendering the real GameLib UI.
    - The Steam library list is populated by the sidecar (real data), confirmed via the sidecar log showing READY + refresh.
    - Clicking Launch produces a real steam:// handoff (Steam launches the game).
    - `npm start` still launches the working Electron build.
    - Any failure is described with the failing step number.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all four steps pass, or describe which step failed and how.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (verification + docs) | This plan runs no new untrusted-input path; it exercises the boundaries mitigated in 27-01..04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Tampering | assembled dev build | accept | Dev-only macOS build; no signing/notarization/distribution in scope (deferred per CONTEXT). Live verification confirms the mitigations from 27-01..04 hold end-to-end. |
</threat_model>

<verification>
- `SEAM.md` exists with Ported/Stubbed/Deferred sections and the wired-channel list.
- Human confirms the four live steps (native window, sidecar-populated library, steam:// launch, Electron still works).
</verification>

<success_criteria>
The walking skeleton runs on real macOS: a native Tauri window renders the real UI, the Steam library is sidecar-populated, a steam:// launch fires, the Electron build still works, and the seam boundary is documented for incremental porting.
</success_criteria>

<output>
Create `.planning/phases/27-tauri-shell-walking-skeleton/27-05-SUMMARY.md` when done.
</output>
