---
context: default
phase: 38-deferred-hardware-and-environment-uat-gates-windows-linux-ma
task: null
total_tasks: 34
status: paused
last_updated: 2026-09-06T00:31:23.645Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through actual failure in the 2026-09-06 session, not predicted.

- [ ] CONSTRAINT: **An instrument must prove its own liveness before its output counts.** A probe
      page was missing its `</script>` tag. It rendered fine, sat on its initial status string, and
      produced FOUR confident "the pad is dead" readings across two browsers and two origins —
      all void. Mitigation: any diagnostic surface must carry a visibly-changing heartbeat, and
      must be verified end-to-end (e.g. `--headless --dump-dom`) BEFORE a human is asked to read it.
- [ ] CONSTRAINT: **Never pipe a command whose exit code you intend to trust.** `git push | tail`
      reported exit 0 for a push that FAILED. The background-task notification said "completed
      (exit code 0)". Mitigation: run the bare command, or capture `${PIPESTATUS[0]}`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Inert instrument | Missing `</script>`; page loaded, script never ran, 4 false negatives | blocking | Heartbeat counter + headless dump-dom verification before human reads it |
| Piped exit code | `cmd \| tail` masks failure; harness reports success | blocking | Bare command, or `${PIPESTATUS[0]}` |
| zsh word-splitting | `FILES="a b c"; for f in $FILES` iterates ONCE with the whole string | advisory | Use bash arrays via `bash -c`, or zsh `${=VAR}` |
| Blaming the plausible culprit | Steam Input was blamed for gamepad invisibility on no controlled evidence; a 2x2 later ruled it out entirely | advisory | Build the factorial before naming a cause |
| `git checkout -- <f>` to restore | Fires this repo's post-checkout hook → `pnpm i && download-helper-binaries` → throws | blocking | Restore with `git show HEAD:<f> > <f>` |

<current_state>
macOS development is PAUSED mid-stream to move to the Windows machine and discharge Phase 38's
Windows UAT items. No phase is mid-execution. Phase 41 (i18n gate honesty) is PLANNED — 5 plans,
2 waves — but NOT started; do not read its fresh PLAN.md mtimes as work in flight.

**17 commits are stranded on `fix/steam-native-install-stability`.** `.husky/pre-push` rejects
every push because `pnpm lint` reports 4166 warnings against a `--max-warnings 4157` ratchet.
This is PRE-EXISTING: the 13 commits held before the quick task REDUCED warnings by 21
(190 → 169 across the files they touch, measured by holding the commit constant and varying the
tree), which puts origin's own tip near 4187. The gate is rejecting work that improves the metric
it measures. Phase 39 owns the debt.
</current_state>

<completed_work>

- Quick task `260906-h2k` — both graphify `PreToolUse` hooks now use a bare PATH-resolved
  `graphify` instead of `/Users/graysonmitchell/.local/bin/graphify`, so they work off this Mac.
  Commits `0e086176a`, `be57a00b5`, `537c657fa`. Positive gate passed under a stripped
  `env -i PATH=...`; negative control exited 127 as required.
- Diagnosed the controller situation end to end (see Infrastructure State).
</completed_work>

<remaining_work>

1. Push the 17 commits — needs a human decision (see Blockers).
2. Stand up the Windows dev environment.
3. **Fix the Phase 38 ledger before the sitting** — `38-C07`, `38-S01`, `38-S05`, `38-S07`,
   `38-S13` and half of `38-S15` say "electron runtime"; Phase 35 deleted that build, so they
   cannot be run as written. Linux twins `38-S03`/`S09`/`S11` and half of `S16` are dead the same
   way. Grep `test:` for `ELECTRON` across the WHOLE ledger — `38-C07` hides in the controller
   block, outside the S-series.
4. Run the Windows-observable items. `38-W04` wants a CI-produced NSIS artifact that has never
   been built — `workflow_dispatch` `release-tauri.yml` first if it must close as written.
5. Controller sitting: 7 runnable items, needs an Xbox pad or a GENUINE Switch Pro.
6. Phase 41, whenever macOS work resumes.
</remaining_work>

<decisions_made>

- graphify hooks fail LOUDLY (no `command -v` guard). `PreToolUse` only blocks on exit 2, so a
  missing binary is non-blocking noise that prompts `pipx install graphifyy`; a guard would exit 0
  and silently disable a workflow CLAUDE.md calls mandatory.
- The PowerA pad is a **Windows** asset. `nintendo.ts:69`'s widened third-party predicate is
  unreachable on macOS and only exercisable where the webview is Chromium-backed.
- Steam Input exclusivity is **ruled out** as a cause of gamepad invisibility.
</decisions_made>

<blockers>
- **Push blocked** by the lint ratchet. Options: `git push --no-verify` (recommended — CI's
  `lint.yml` still runs `pnpm lint`, so nothing is hidden) or reset `--max-warnings`. Awaiting
  the user's call. NOTE: the other three pre-push legs (`codecheck`, `prettier`,
  `i18n --fail-on-update`) NEVER RAN — lint fails first in the `&&` chain, so their state is unknown.
- `38-E01`/`38-E02` are implementation tasks before they are verification tasks.
</blockers>

## Required Reading (in order)

1. `.planning/phases/38-.../38-VERIFICATION.md` — the phase's source of truth; `audit-uat` reads
   only its `human_verification` array, and only while `status: human_needed`.
2. `.planning/phases/38-.../38-HUMAN-UAT.md` — sitting protocol; note "instrument rather than ask".
3. `.planning/ROADMAP.md` § Phase 38 — the corrected hardware premise.
4. `.planning/METHODOLOGY.md` (if present).

## Infrastructure State

- **Steam: RUNNING** on the Mac. It rewrote `~/Library/Application Support/Steam/config/libraryfolders.vdf`
  on launch (Sep 1 05:16 → Sep 6 11:54, size unchanged at 1324 B). No pre-launch copy was taken,
  so content equality is unproven. `cp` it before the next deliberate relaunch.
- **Controller: PowerA Advantage Wired for Nintendo Switch 2** (`0x20D6`/`0xA720`) plugged in.
  macOS classes it correctly (`DeviceUsagePage=1 / DeviceUsage=5`, `AppleUserHIDEventDriver`).
  **Chromium reads it; WebKit returns 0 slots.** Measured with a complete Steam-running/Steam-quit
  2x2. It cannot discharge any macOS controller item.
- Probe page at `<scratchpad>/padprobe.html`; its localhost:8777 server was STOPPED.
- No worktrees (`workflow.use_worktrees=false`).

<context>
The through-line: GameLib runs in WKWebView on macOS and WebView2 (Chromium) on Windows, and those
two engines disagree about which gamepads exist. That single fact reassigns the PowerA pad from
"the better test controller" to "a Windows-only asset", and it is the reason `nintendo.ts`'s
deliberate widening past upstream Heroic's narrow vendor match has never been exercised.

Second through-line: several Phase 38 items describe a runtime the project deleted in Phase 35.
Every gate over that ledger passes, because each item's `platform_gate` is individually correct.
Well-formed items can still point at nothing.
</context>

<next_action>
Start with: answer the push question (`--no-verify` vs ratchet reset), push the 17 commits, then
run Step 1 of the Windows setup — winget prereqs, `rustup default stable-msvc`, `corepack enable`,
and a `python3` alias.
</next_action>
