---
status: resolved
resolved: 2026-08-18T21:00:00Z
resolution_type: closed_unreproduced
trigger: "Phase 23.1 plan 05's own live-hardware re-verification, THIRD round (2026-08-18). Round 1 fixed a worker-thread backend/logger crash (DecompressPool.init() collapsed to inline decode). Round 2 fixed lzmaNativeBinding.ts's dirBelongsToLzmaNative(dir) identity guard, which was rejecting the legitimate resolveNativeBinding() call once esbuild bundled lzma-native's index.js into the SEA output (__dirname collapses to \".\" inside an eval'd worker -- exactly what spike 023's own reference implementation had predicted). Verifying round 2's fix via the GAMELIB_SIDECAR_SELFTEST=decompress-pool self-test harness (quick-260817-pkx's own tool) against a REAL, cold pnpm build:sidecar-sea binary surfaced THIS, a third, separate, previously-masked defect: `SELFTEST pool={\"size\":2,...,\"inlineFallback\":false,\"nativeWorkers\":2}` (native resolution genuinely works now) immediately followed by `SELFTEST decode=fail code=decompress_pool_timeout message=decompress task 0 timed out` (the actual, real-sized chunk decode never completes)."
created: 2026-08-18T15:00:00Z
updated: 2026-08-18T21:00:00Z
symptoms:
  expected_behavior: "DecompressPool.decode() of a real-sized (64KB uncompressed) VZ chunk, dispatched to a pooled worker inside a genuinely compiled, postject-injected SEA sidecar binary, resolves with the correctly decoded bytes within the pool's 15s per-task timeout -- exactly as it already does in every jest-run and dev-mode (pnpm tauri:dev) reproduction of the same code path."
  actual_behavior: "The task times out (`decompress_pool_timeout`) and is never fulfilled. DecompressPool's own replaceWorker() recovery then terminates and respawns the stalled worker (repeatedly -- see Evidence), but the REPLACEMENT worker's task also stalls the same way; nothing in the captured runs ever recovers on its own within the self-test's observation window."
  error_messages: "`SELFTEST decode=fail code=decompress_pool_timeout message=decompress task 0 timed out` -- DecompressPool's own generic per-task-timeout error, not a decode-specific one (i.e. this is the SYMPTOM the pool's own watchdog reports, not a root-cause error message from anywhere deeper in the stack)."
  timeline: "First observed 2026-08-18, immediately after round 2's identity-guard fix was verified working (nativeWorkers matched pool size for the first time). Never previously observable: this is the first time in this whole debug arc that GAMELIB_SIDECAR_SELFTEST=decompress-pool was run against the REAL, genuinely-compiled, postject-injected `src-tauri/binaries/gamelib-sidecar-<triple>` binary -- every prior use of that self-test (quick-260817-pkx, the dev-mode-decompress-worker-electron-hook session) ran it against `build/main/sidecar.js` (the plain, non-SEA, esbuild bundle produced by `pnpm build:sidecar`), never the packaged binary itself."
  reproduction: "`rm -rf build && pnpm build:sidecar-sea && GAMELIB_SIDECAR_SELFTEST=decompress-pool HOME=<disposable-tmp-dir> src-tauri/binaries/gamelib-sidecar-<triple>` -- reproduces on every attempt (5+ times across this session, both via a bare shell invocation and via a jest-orchestrated child_process.spawn, see lzmaNativeSeaRealBuild.test.ts). HOME must be redirected to a disposable directory (a genuinely separate child_process's `env`, NOT a worker_threads.Worker's own `env` option, which does not redirect the native os.homedir() call -- see that test file's own header comment) so the run does not write to the developer's real ~/Library/Logs/GameLib/gamelib.log."
---

## Current Focus

status_2026-08-18T21:00: "CLOSED. Coordinator/human-operator decision (option 1, 'close conservative'): keep `NATIVE_LZMA_DECODE_ENABLED=false` unchanged, do not un-skip the second test in `lzmaNativeSeaRealBuild.test.ts`, close this session as 'could not reproduce under any tested condition (normal or extreme CPU/oversubscription load) across 40/40 attempts spanning two sessions; monitor for recurrence in the field before revisiting.' No further investigation performed this closing session -- see Resolution below for the honest final state (root_cause UNKNOWN/unconfirmed, not disproven; fix = none applied, kill-switch left in place; verification = N/A). File moved to `.planning/debug/resolved/`."

next_action_2026-08-18T21:00: "NONE -- session closed. If `decompress_pool_timeout` recurs against a real packaged SEA binary in the field, reopen with full system-state capture (thermal/memory/I-O, not just CPU load) per Resolution.verification's standing recommendation."

status_2026-08-18T20:00: "CONTINUATION SESSION 2 (post-checkpoint decision: user selected option 3 — directly test the resource-starvation/contention theory before deciding on re-enable). About to run the documented reproduction (`rm -rf build && pnpm build:sidecar-sea && GAMELIB_SIDECAR_SELFTEST=decompress-pool ...`) repeatedly, in both codec configurations, while deliberately saturating all 10 CPU cores concurrently (`yes > /dev/null` x10, or equivalent CPU-bound stress) for the entire duration of each run. This directly tests reasoning_checkpoint's falsification_test / next_action item (2b) from the prior session. NATIVE_LZMA_DECODE_ENABLED will be flipped to true for investigation only during the native-codec half, same as prior session's investigation-only usage, and reverted afterward — git diff on lzmaLoader.ts must be clean when done. No test files will be un-skipped."

next_action_2026-08-18T20:00: "Start CPU-saturation stress (yes > /dev/null pinned to all 10 cores), confirm load is genuinely high (e.g. via a quick load-average check), then run the pure-JS (default, kill-switch-off) self-test reproduction 5+ times under that load, then the native (temporarily re-enabled) self-test reproduction 5+ times under that same load. Record every result. Stop stress processes and revert any temporary flag change afterward."

status_2026-08-18T20:45: "COMPLETE -- 25 consecutive self-test attempts run under genuine, verified, escalating CPU contention (10 `yes > /dev/null` processes = full 10-core saturation at load avg 12-18, up to 20 `yes` processes + a concurrent `pnpm build:sidecar-sea` competing for the SAME CPUs = load avg 39-46 on this 10-core machine, ~4.6x oversubscription). ZERO hangs, ZERO timeouts, across BOTH codec configurations (pure-JS default and temporarily-re-enabled native). This is the falsification test reasoning_checkpoint's next_action item (2b) explicitly called for. Result: the resource-starvation/contention theory is WEAKENED, not confirmed -- see updated reasoning_checkpoint below. NATIVE_LZMA_DECODE_ENABLED was flipped to true for the native half only, confirmed via git diff to be reverted (clean diff on lzmaLoader.ts), and build/ was rebuilt one final time afterward restoring the shipped kill-switch-off state (confirmed via one more self-test run: nativeWorkers:0, decode=ok). No test files un-skipped, no other production code changed."

next_action_2026-08-18T20:45: "CHECKPOINT REACHED (decision) -- returned to caller with this session's finding. Awaiting coordinator/human-operator decision on how to proceed (see the two-path framing in this Current Focus's own resolution text below, updated for this session's result)."

status_2026-08-18T19:30: "CONTINUATION SESSION FINDING: the hang is currently UNREPRODUCIBLE via the exact documented reproduction steps, in EITHER codec configuration, across 10 consecutive fresh-cold-build attempts (5 pure-JS + 5 native, plus 5 more against the pre-existing binary before rebuilding = 15 total consecutive successes, zero hangs). This does NOT confirm the original defect never existed (it was observed directly, repeatedly, with specific diagnostic instrumentation, by the prior session) -- but it means root cause could not be re-confirmed or fixed this session, and the investigation instead produced strong evidence the failure mode is NOT a structural Node-SEA/worker_threads/DecompressPool defect. See new Evidence entries below and the updated reasoning_checkpoint. THIS IS A DECISION CHECKPOINT, not a fix-applied checkpoint -- no production code change is shipped from this session (a temporary NATIVE_LZMA_DECODE_ENABLED=true flip was made for investigation only and fully reverted; `git diff` on lzmaLoader.ts is clean)."

next_action_2026-08-18T19:30: "CHECKPOINT REACHED (decision) -- returned to caller. If the coordinator/human operator decides to proceed with re-enabling native decode: (1) this session's own self-test evidence already satisfies lzmaLoader.ts's own re-enable criteria items 1-2 (self-test discipline re-run against a real cold build, BOTH inlineFallback:false AND decode=ok/match=true confirmed, for both codecs) -- items NOT yet done: (2b) run the self-test repeatedly (10-20x) under DELIBERATE system load (e.g. `pnpm build:sidecar-sea` running concurrently, or another CPU-bound process) to test whether the original hang was load/contention-related, since normal-conditions retesting could not reproduce it; (3) un-skip `lzmaNativeSeaRealBuild.test.ts`'s second test and confirm it passes for real; (4) flip `NATIVE_LZMA_DECODE_ENABLED` to `true`; (5) a live Steam depot install end-to-end before considering this fully closed. If the coordinator instead wants to keep the kill switch as-is (current shipped state, unchanged by this session) and simply close this file as 'could not reproduce, monitor for recurrence in the field': that is also a valid, conservative choice given a real (if currently unreproducible) hang was directly observed with specific diagnostic evidence by the prior session."

reasoning_checkpoint:
  hypothesis: "CRITICAL CORRECTION 2026-08-18T16:00 (this session, immediately after implementing the native-decode kill switch the coordinator directed as a stopgap): the ORIGINAL working hypothesis -- 'this hang is specific to lzma-native's native decode path' -- is WRONG. With `NATIVE_LZMA_DECODE_ENABLED=false` (lzmaLoader.ts) confirmed active (`nativeWorkers:0` in the self-test's own pool= line), the EXACT SAME real-sized decode task STILL times out via the pure-JS `lzma` package, reproduced twice independently (once via jest's child_process.spawn harness, once via a bare shell invocation of the same freshly-built binary -- see Evidence). This means the hang is NOT codec-specific -- it reproduces regardless of which decoder resolves. The true scope is broader and more serious than originally framed: a genuinely compiled, postject-injected SEA binary's DecompressPool worker pool may not be able to complete ANY real-sized chunk decode task at all, through EITHER codec, which -- if confirmed to generalize beyond this synthetic self-test fixture to a real Steam depot install -- would mean the packaged/`tauri build` product's worker-pool decode path has likely never actually completed a real chunk decode in this entire debug arc's history. Every prior 'successful' live-hardware round in this project's history (Planetfall/HUMANKIND installs completing, sometimes slowly) ran through `pnpm tauri:dev`'s DEV sidecar (`build/main/sidecar.js`, file-based worker spawn, `sea.isSea()===false`), never the genuinely compiled SEA binary's eval'd-worker path -- so this may be a code path that has simply never been exercised end-to-end with a real decode before this session, not a regression."
  confirming_evidence:
    - "Diagnostic instrumentation placed directly inside decompressWorker.ts's handleDecodeMessage() and lzmaLoader.ts's createNativeAdapter() (temporary, reverted before committing -- not in the shipped diff) showed handleDecodeMessage() is NEVER ENTERED for the real 64KB decode request, in either the native or (subsequently confirmed) pure-JS configuration. Only repeated re-invocations of the SAME small smoke-test-sized decode (matching SMOKE_TEST_COMPRESSED's own 54-byte length) appear in the captured stderr, one burst per worker DecompressPool silently replaceWorker()s after each stalled attempt's own 15s timeout -- i.e. each REPLACEMENT worker's own eager loadLzmaModule() at spawn (which DOES successfully decode the tiny smoke-test fixture) is what's actually being observed repeating, not the real task retrying. This places the hang somewhere in the real request's dispatch/delivery to the worker -- postMessage from the main thread, structured-clone/transfer of the ArrayBuffers, or the worker's own message-event delivery -- NOT inside decodeChunk()/the codec adapter itself, since that code is never reached for the real payload in either codec configuration."
    - "Every attempt to isolate this OUTSIDE a genuinely compiled, postject-injected SEA binary process SUCCEEDED (i.e. did NOT reproduce the hang) -- ruling out each of the following as the SOLE cause, though not ruling out a genuine SEA-binary-specific factor not yet identified: (1) lzma-native's createStream API round-tripping a real 64KB payload inside a plain worker_threads.Worker (require('lzma-native') directly, no aliasing): fine, 0ms. (2) The exact SEA-branch native-binding mechanism (copy the real .node bytes to a fresh temp path, process.dlopen() the COPY, delete the temp file) inside a worker_threads.Worker, sequential smoke-test-then-real decode: fine. (3) The same, with TWO CONCURRENT workers each independently copying+dlopen-ing their own temp copy (mirroring the self-test's own SELFTEST_POOL_SIZE=2): fine. (4) The same, spawned via `new Worker(source, { eval: true })` (the exact mechanism a SEA binary's own worker uses -- no backing file): fine. (5) The REAL DecompressPool class (real message dispatch, real 15s task-timeout tracking, real ArrayBuffer transfer, real replaceWorker() recovery) against the REAL compiled DEV worker bundle (build/main/decompressWorker.js, file-based `new Worker(path)`, native decode via direct dlopen from node_modules): fine, 321ms round-trip. (6) The REAL DecompressPool class via its own real eval-worker SEA-style spawn path (resolveWorkerSpec(), node:sea mocked isSea()=>true on the jest MAIN thread only -- the spawned worker itself still resolved the REAL, unmocked node:sea inside its own isolate, so this exercised the eval-spawn MECHANISM but not genuinely-compiled-SEA-binary-specific behavior): fine, 382ms round-trip."
    - "Disabling lzmaNativeBinding.ts's SEA branch's post-dlopen `rmSync(addonPath)` cleanup entirely (testing whether unlinking the backing temp file races an async libuv-threadpool-dispatched native decode operation) did NOT change the outcome -- still hangs identically. Ruled out as the sole cause (though this test was performed before the codec-independence finding above, so it only rules this out for the NATIVE path specifically)."
  falsification_test: "Not yet run (next_action). Would confirm 'this is genuinely specific to a real compiled/postject-injected SEA binary, independent of codec' if: a MINIMAL, from-scratch SEA binary (mirroring spike 023's own build-sea.mjs, NOT this project's full sidecar bundle) reproduces the SAME hang for a real-sized (not smoke-test-sized) decode task dispatched via worker_threads message-passing after a successful 'ready' handshake. Would refute it (pointing back to something specific in THIS project's own sidecar bundle/DecompressPool wiring instead) if a minimal reproduction does NOT hang."
  contention_falsification_test_2026-08-18T20:45: "SEPARATE, NARROWER falsification test run this continuation session, targeting specifically the 'transient CPU-scheduling starvation' leading theory from the prior session's Resolution.root_cause (not the SEA-binary-specific mechanism question above, which remains open). Would CONFIRM the contention theory if the documented reproduction hangs under deliberately saturated CPU load when it did not hang under normal conditions. Would WEAKEN it if 25 consecutive attempts under genuine, escalating, verified CPU contention (up to ~4.6x core oversubscription, including a directly competing concurrent build process) all still succeed. RESULT: weakened -- see confirming_evidence's new entry below. Does NOT fully refute the theory (a narrower timing window, a different specific contention pattern e.g. I/O contention rather than CPU, or the exact original dev-machine's specific background process mix could still be load-bearing and was not reproduced here), but 25/25 successes under contention this extreme is meaningfully strong evidence against 'ordinary CPU scheduling pressure' as the mechanism."
  fix_rationale: "No fix attempted this session beyond the native-decode kill switch (NATIVE_LZMA_DECODE_ENABLED=false, lzmaLoader.ts) -- and per the CRITICAL CORRECTION above, that switch does NOT resolve this hang; it only removes lzma-native's own binding resolution as a confounding variable (now provably correct at build time, see lzmaNativeBinding.ts) and closes the narrower 'native resolves, then hangs' framing this investigation started with. The coordinator/human-operator directed explicitly NOT to continue debugging this live in the same session that found it (risk of a rushed, unverified fix against a ~15-20s-per-attempt real SEA rebuild iteration cycle) -- this file exists so a FUTURE session can pick this up with the full context already captured, rather than re-deriving it."
  blind_spots: "(1) NOT YET DETERMINED whether this reproduces against a REAL Steam depot chunk dispatched through the REAL production call path (downloadDepotFiles() -> fetchChunk() -> DecompressPool.decode()), or is in some way specific to decompressPoolSelfTest.ts's own synthetic fixture construction (its steamEncrypt()/buildVZChunk() helpers, lifted verbatim from decompressPool.test.ts's own fixture builders per that file's own header comment, but never before exercised against a REAL SEA-eval'd worker's message-passing path end-to-end). (2) NOT YET DETERMINED whether DecompressPool.decode() has ANY eventual give-up-and-fall-back-to-inline path once a task repeatedly times out and its replacement worker's task ALSO times out (grep of decompressPool.ts found `this.inlineFallback = true` set ONLY inside init()'s worker-spawn-failure catch block, never from a task-level timeout/replace cycle) -- if it does not, a real packaged install hitting this defect would not merely be SLOW, it could retry indefinitely without ever completing or failing outright, a materially different and more severe risk than 'installs still complete, just slower' (this debug arc's own repeatedly-reaffirmed locked requirement). This is the SINGLE HIGHEST-PRIORITY open question for whoever picks this up next. (3) Not yet determined whether pool size (2, matching SELFTEST_POOL_SIZE) or the specific fixture size (64KB uncompressed, ~324 bytes compressed) are load-bearing to reproduction -- untested at pool size 1, and untested with a genuinely large real chunk (real Steam depot chunks run up to ~1MB compressed per this codebase's own depot.ts comments, materially bigger than this self-test's synthetic fixture). (4) Not yet determined whether this reproduces on any platform/triple other than darwin-arm64 (the only one built/tested this session)."

hypothesis: "OPEN. Narrowed to: something specific to task-message dispatch/delivery to a `worker_threads.Worker` spawned via `new Worker(source, { eval: true })` INSIDE a genuinely compiled, postject-injected Node SEA binary process breaks for the FIRST real (non-eager, message-triggered) task dispatched to it, independent of codec/payload content -- diagnostic evidence places the failure before decodeChunk()/the codec adapter is ever reached, not inside LZMA decode logic itself. Every attempt to isolate this outside an ACTUAL compiled SEA binary succeeded instead of reproducing it, which is itself evidence that some property specific to that exact runtime configuration (SEA blob loading, postject injection, `node:sea`'s real isSea()===true state, or some Node-internal difference in how the SEA runtime services worker_threads IPC) is load-bearing -- not yet identified further than that."
test: "Not yet run this session -- see falsification_test above (next_action)."
expecting: "N/A -- no falsification test has been run yet."
next_action: "1. Determine whether this reproduces via the REAL production call path (a real fetchChunk()-triggered DecompressPool.decode() call, not just decompressPoolSelfTest.ts's synthetic fixture) -- ideally by adding a temporary, larger diagnostic self-test mode, or by re-enabling native (locally, temporarily, per lzmaLoader.ts's own doc comment) and attempting one more real Steam depot install via a genuinely packaged `tauri build` app, closely observing whether normal chunk-stream-stats logging shows the same task-0-style stall pattern this self-test surfaced. 2. Determine whether DecompressPool ever gives up and falls back to inline decode after repeated same-task timeouts, or retries indefinitely (blind_spots item 2 above) -- read replaceWorker()'s full call chain and queue-requeue logic; this determines whether the real-world risk is 'slow' or 'stuck forever'. 3. Build spike 023's OWN minimal build-sea.mjs-based SEA binary (not this project's full sidecar) and attempt the SAME real-sized-payload-via-message-dispatch-after-ready-handshake reproduction there, to determine whether this is a genuine, minimal, Node-SEA-level defect (falsification_test above) or specific to this project's own bundle/wiring. 4. Once a root cause is understood and a fix lands, re-run this exact self-test discipline (GAMELIB_SIDECAR_SELFTEST=decompress-pool against a real cold pnpm build:sidecar-sea binary) and confirm BOTH the pool spawning correctly AND a real decode completing (decode=ok, match=true) before un-skipping either test in lzmaNativeSeaRealBuild.test.ts or flipping NATIVE_LZMA_DECODE_ENABLED back to true."

## Evidence

- timestamp: 2026-08-18T15:10:00Z
  checked: "GAMELIB_SIDECAR_SELFTEST=decompress-pool against a REAL, cold pnpm build:sidecar-sea binary (src-tauri/binaries/gamelib-sidecar-aarch64-apple-darwin), immediately after round 2's identity-guard fix (lzmaNativeBinding.ts's dir-based rejection removed, build-time-resolved package root substituted). First time this self-test had ever been run against the genuinely compiled SEA binary (not the dev bundle) in this debug arc's history."
  found: "`SELFTEST pool={\"size\":2,\"busy\":0,\"idle\":2,\"queued\":0,\"inlineFallback\":false,\"nativeWorkers\":2}` -- native resolution genuinely works (round 2's fix confirmed correct). Immediately followed by `SELFTEST decode=fail code=decompress_pool_timeout message=decompress task 0 timed out` -- the real 64KB decode task never completed within the pool's 15s timeout."
  implication: "Round 2's identity-guard fix is real and correct (see the round's own commit, 1eb66b2b6) -- but a THIRD, separate, previously-masked defect exists one layer deeper: successful native resolution does not imply successful native DECODE of a real-sized payload inside this exact runtime configuration."

- timestamp: 2026-08-18T15:20:00Z-15:45:00Z
  checked: "Isolated reproduction attempts (six independent configurations, listed in full in reasoning_checkpoint's confirming_evidence above), none run against a genuinely compiled SEA binary -- plain worker_threads.Worker + real lzma-native; the exact SEA-branch copy-then-dlopen mechanism standalone; the same with two concurrent workers; the same via an eval'd worker; the REAL DecompressPool class against the REAL compiled DEV worker bundle; the REAL DecompressPool class via a mocked-isSea() eval-worker spawn."
  found: "Every single one succeeded -- fast (0-400ms), correct output, no hang. Also: disabling the SEA branch's post-dlopen rmSync() cleanup (testing an unlink-races-async-decode theory) did not change the outcome; still hung identically."
  implication: "The hang is NOT reproducible via any combination of (eval'd worker) + (copy-then-dlopen native resolution) + (real DecompressPool dispatch mechanics) attempted OUTSIDE a genuinely compiled, postject-injected SEA binary process. Something specific to that exact runtime configuration is load-bearing to reproduction -- not yet identified."

- timestamp: 2026-08-18T15:55:00Z
  checked: "Diagnostic instrumentation (process.stderr.write calls) placed directly inside decompressWorker.ts's handleDecodeMessage() (entry point) and lzmaLoader.ts's createNativeAdapter()'s decompress() function, rebuilt into a real SEA binary, run against the real self-test. Reverted (not committed) after this check."
  found: "handleDecodeMessage() is NEVER logged as entered for the real 64KB request. Only the SAME small (54-byte, matching SMOKE_TEST_COMPRESSED) decompress() invocation repeats over and over, matching one eager loadLzmaModule() smoke-test call per worker DecompressPool's replaceWorker() spins up after each stalled attempt's own timeout -- i.e. what's being observed repeating is REPLACEMENT WORKERS' OWN spawn-time smoke tests succeeding, not the real task retrying and failing repeatedly."
  implication: "The hang occurs BEFORE decodeChunk()/the codec adapter is ever reached for the real payload -- somewhere in message dispatch/delivery between the main thread's DecompressPool.decode() call and the target worker's own 'message' event handler. This directly motivated re-testing with native decode gated off entirely, to determine whether the codec was even relevant -- see the next entry."

- timestamp: 2026-08-18T16:00:00Z
  checked: "CRITICAL CORRECTION check: with NATIVE_LZMA_DECODE_ENABLED=false (the newly-implemented kill switch) confirmed active via the self-test's own pool= line, re-ran GAMELIB_SIDECAR_SELFTEST=decompress-pool against a freshly cold-rebuilt SEA binary. Reproduced twice independently: once via lzmaNativeSeaRealBuild.test.ts's jest-orchestrated child_process.spawn harness, once via a bare shell invocation of the identical binary."
  found: "`SELFTEST pool={\"size\":2,...,\"inlineFallback\":false,\"nativeWorkers\":0}` (gate confirmed active -- native decode did not engage) followed, both times, by the IDENTICAL `SELFTEST decode=fail code=decompress_pool_timeout message=decompress task 0 timed out`."
  implication: "The hang is NOT specific to lzma-native / native decode. It reproduces via the pure-JS `lzma` package too, inside a genuinely compiled SEA binary. This invalidates the original 'gate native off to route around it' framing as a COMPLETE fix -- the kill switch is still worthwhile (closes the narrower, now-understood 'native resolves then hangs' case, and proves lzma-native's own binding resolution mechanism is correct, a real and separate deliverable), but does NOT make a packaged SEA install's worker-pool decode path safe end-to-end. This is now the single most important corrected fact for whoever continues this investigation."

- timestamp: 2026-08-18T18:00:00Z
  checked: "Spike 023's own README.md (`.planning/spikes/023-sea-native-addon-dlopen/`), re-read closely for whether it actually validated the SAME mechanism as production's dispatch()/postMessage() call. It does not: `sea-main.cjs`'s `runEvalWorker()` passes the fixture via `workerData` at `new Worker(source, { eval: true, workerData: {...} })` CONSTRUCTION time -- the worker never receives a subsequent `parentPort.on('message', ...)`-delivered postMessage() at all. Confirmed by reading `sea-worker-entry.cjs` directly: it reads `workerData` once at top level, never registers a 'message' listener."
  found: "Spike 023 (VALIDATED, 10/10 runs, real compiled SEA binary, real dlopen from an eval'd worker) proves dlopen()-from-eval'd-worker is safe, but never exercised the exact mechanism DecompressPool.dispatch() relies on: main-thread `worker.postMessage(msg, [transferredArrayBuffer])` sent AFTER a 'ready' handshake, received via the worker's own `parentPort.on('message', ...)`."
  implication: "This is the exact gap the debug file's own falsification_test asks about. Built a minimal, from-scratch SEA binary (mirroring build-sea.mjs) that tests SPECIFICALLY this gap -- see next entries."

- timestamp: 2026-08-18T18:15:00Z
  checked: "Built a minimal, from-scratch SEA binary (own build pipeline, mirroring build-sea.mjs, no lzma-native dependency) with a synthetic worker that sends 'ready' immediately, then waits for a message-after-ready carrying a 64KB OWN/sliced ArrayBuffer in a transferList -- exactly `toOwnArrayBuffer()` + `dispatch()`'s shape in decompressPool.ts, run on a real, postject-injected, `isSea()===true` binary."
  found: "Round-trip completed instantly (`hang:false`, correct byte length echoed back). The minimal message-after-ready-handshake + transferList mechanism, standalone, does NOT hang in a real compiled SEA binary."
  implication: "REFUTES the 'generic Node SEA / eval-worker message-passing mechanism is broken' hypothesis (falsification_test's own 'refute' branch). The bug, if real, must be specific to something in this project's own bundle/wiring, not a bare Node/V8 defect."

- timestamp: 2026-08-18T18:35:00Z
  checked: "Incremental repro #2: same minimal SEA main-thread harness, but the 'worker.js' SEA asset swapped for the REAL, unmodified, project-built `build/main/decompressWorker-sea-bundle.js` (the actual compiled decompressWorker.ts, including decompress.ts/lzmaLoader.ts/backend-logger/lzma-native wrapper, all bundled) -- driven by a hand-written main script sending a REAL `DecompressWorkerRequest`-shaped message (id/encrypted/key/expectedSha/cbOriginal) built from a real steamEncrypt()+buildVZChunk() fixture, after a real 'ready' handshake."
  found: "Decode succeeded: `{stage:'result', hang:false, ok:true, dataLen:65536}` -- correct output length, no hang, no timeout (round trip well under 1s). (First attempt hit an unrelated ENOENT writing gamelib.log because the disposable HOME's `Library/Logs/GameLib` dir didn't pre-exist -- creating it resolved that; not the hang under investigation, just this harness's own setup gap.)"
  implication: "REFUTES 'decompressWorker.ts's own compiled bundle is what breaks message dispatch.' The REAL worker code, run standalone (not via DecompressPool's own machinery, not via the sidecar bootstrap), correctly receives and processes a real dispatch message with a transferred ArrayBuffer inside a real SEA binary."

- timestamp: 2026-08-18T18:50:00Z
  checked: "Incremental repro #3: drove the REAL, completely unmodified `decompressPoolSelfTest.ts` (imported directly, not reimplemented) -- i.e. the REAL `DecompressPool` class, REAL `resolveWorkerSpec()` SEA branch, REAL fixture builders -- as this minimal SEA binary's OWN main script (bundled with the EXACT esbuild flags `meta/esbuildWorkerBundleShared.ts`'s `seaEsbuildFlags()` specifies: `--alias:electron`, `--alias:i18next-fs-backend`, `--alias:node-gyp-build`, `--inject:sidecarSeaFsShim`), still bypassing `src/sidecar/index.ts`'s own bootstrap entirely."
  found: "`SELFTEST pool={\"size\":2,...,\"inlineFallback\":false,\"nativeWorkers\":0}` followed by `SELFTEST decode=ok bytes=65536 match=true`, exit code 0."
  implication: "REFUTES 'DecompressPool.ts or decompressPoolSelfTest.ts themselves are defective.' The REAL, unmodified pool class + self-test, run standalone, work correctly in a real SEA binary."

- timestamp: 2026-08-18T19:00:00Z
  checked: "Incremental repro #4: identical to repro #3, but ALSO imports `backend/sidecar/bootstrap` at module scope (never calling its exported `init()`) -- mirroring `src/sidecar/index.ts`'s OWN literal top-level `import { init } from 'backend/sidecar/bootstrap'`, which is a STATIC import evaluated unconditionally even in `GAMELIB_SIDECAR_SELFTEST` mode (ES module imports run at load time regardless of which runtime branch executes later) -- so bootstrap.ts's module-scope side effects (Module._load hook install, full backend/storeManager registration import, `[downloadQueueFlowRegistration]` boot-time log, a `tray_set_icon` IPC message emitted to stdout) DO run even during a self-test, in production."
  found: "Same result as repro #3: pool spawns correctly, `SELFTEST decode=ok bytes=65536 match=true`, exit 0 -- despite bootstrap.ts's full module-scope side effects (backend registration, the stray `tray_set_icon` IPC-shaped stdout line, etc.) now firing before the self-test runs, exactly as they do in the real production entry point."
  implication: "REFUTES 'bootstrap.ts's own module-scope side effects (import-time only, never init()'d) break DecompressPool.' This repro now faithfully replicates essentially the ENTIRE real `src/sidecar/index.ts` self-test code path (bootstrap import + decompressPoolSelfTest, only the RPC-loop-starting half of `init()` itself is skipped, which self-test mode never reaches anyway) and still does not reproduce."

- timestamp: 2026-08-18T19:10:00Z
  checked: "Re-ran the debug file's OWN literal documented reproduction command against the ACTUAL, already-built `src-tauri/binaries/gamelib-sidecar-aarch64-apple-darwin` (the exact binary the prior session's evidence was captured against, timestamped ~33 minutes before this check) -- 5 consecutive bare-shell invocations, fresh disposable HOME each time."
  found: "5/5 succeeded: `SELFTEST pool={...\"nativeWorkers\":0}` then `SELFTEST decode=ok bytes=65536 match=true`, ~1s each. Zero hangs, zero timeouts."
  implication: "The EXACT SAME BINARY that reportedly hung reliably (\"reproduces on every attempt, 5+ times\") for the prior session no longer reproduces the hang at all, on the same machine, using the same documented command. Strongly suggestive of an ENVIRONMENTAL/transient factor (system load, resource contention) rather than a deterministic code defect -- see next two entries for confirmation via genuinely fresh builds."

- timestamp: 2026-08-18T19:15:00Z
  checked: "Full documented reproduction from scratch: `rm -rf build && pnpm build:sidecar-sea` (genuinely fresh cold build, current committed+working-tree source, `NATIVE_LZMA_DECODE_ENABLED=false` as shipped) then 5 consecutive `GAMELIB_SIDECAR_SELFTEST=decompress-pool` runs, fresh disposable HOME each time."
  found: "5/5 succeeded, 1-2s each: `nativeWorkers:0`, `decode=ok bytes=65536 match=true`. Zero hangs."
  implication: "A genuinely fresh, cold rebuild from current source -- not reusing any stale artifact -- does not reproduce the hang either, in the pure-JS (shipped default) configuration."

- timestamp: 2026-08-18T19:20:00Z
  checked: "Temporarily flipped `NATIVE_LZMA_DECODE_ENABLED` to `true` in lzmaLoader.ts (investigation-only; fully reverted afterward, confirmed via `git diff` showing no changes), `rm -rf build && pnpm build:sidecar-sea` (fresh cold build), then 5 consecutive self-test runs -- this is the EXACT original scenario that first surfaced the hang (round 2's own confirmation attempt)."
  found: "5/5 succeeded, 1-3s each: `nativeWorkers:2` (native genuinely engaged, matching round 2's own confirmed-correct binding resolution), `decode=ok bytes=65536 match=true`. Zero hangs, zero timeouts, in the NATIVE codec configuration either."
  implication: "The original failure mode (native resolves correctly, THEN a real-sized decode task hangs) does not reproduce now, in 5 consecutive attempts, on a fresh cold build. Combined with the prior two entries: 15 consecutive self-test attempts across this continuation session (5 against the pre-existing binary, 5 fresh pure-JS, 5 fresh native), zero hangs. Reverted the temporary flip and rebuilt once more to leave `build/` and the packaged binary in the shipped, kill-switch-off state (build artifacts are gitignored; `src/backend/storeManagers/steam/depot/lzmaLoader.ts` has zero diff against its committed state)."

- timestamp: 2026-08-18T19:23:00Z-19:32:00Z
  checked: "Started 10 `yes > /dev/null &` processes (one per core on this 10-core darwin-arm64 machine, confirmed via `sysctl -n hw.ncpu`), verified via `ps` that all 10 were genuinely running at 91-100% CPU each and load average climbed to ~2.5 immediately after start, then to 12-14 sustained once the fresh cold `rm -rf build && pnpm build:sidecar-sea` build and 10 consecutive self-test runs were also competing for the same cores. Ran the exact documented reproduction (`rm -rf build && pnpm build:sidecar-sea` then `GAMELIB_SIDECAR_SELFTEST=decompress-pool HOME=<disposable> <binary>`) 10 times in the shipped pure-JS (NATIVE_LZMA_DECODE_ENABLED=false) configuration, all under this sustained heavy CPU contention."
  found: "10/10 succeeded: `nativeWorkers:0`, `decode=ok bytes=65536 match=true`, each run completing in 2-4s wall-clock despite the load average being 5-6x this machine's core count during several of the runs. Zero hangs, zero decompress_pool_timeout errors."
  implication: "The pure-JS decode path does not hang under genuine, verified, sustained CPU saturation across all cores -- this directly tests (and does not confirm) the leading 'transient environmental CPU-scheduling starvation' theory from the prior session's Resolution.root_cause."

- timestamp: 2026-08-18T19:23:00Z-19:35:00Z
  checked: "With CPU stress still running (load avg 16-18, i.e. this 10-core machine oversubscribed by ~1.7-1.8x), temporarily flipped NATIVE_LZMA_DECODE_ENABLED to true in lzmaLoader.ts, did a fresh `rm -rf build && pnpm build:sidecar-sea` cold rebuild (itself completing under the same load in ~9s, no anomaly), then ran the self-test 10 consecutive times in the native configuration -- the EXACT scenario (native resolves correctly, then decode of a real-sized chunk) that originally surfaced this defect."
  found: "10/10 succeeded: `nativeWorkers:2` (native genuinely engaged), `decode=ok bytes=65536 match=true`, 3-4s wall-clock each. Zero hangs, zero timeouts, in the native codec configuration under this load either."
  implication: "The native decode path -- the configuration that FIRST surfaced this bug -- also does not hang under sustained heavy CPU contention. Combined with the pure-JS result above: 20/20 successes across both codecs under genuine 1.2x-1.8x core oversubscription."

- timestamp: 2026-08-18T19:37:00Z-19:47:00Z
  checked: "Pushed contention further: added 10 MORE `yes` processes (20 total on 10 cores, confirmed load avg 22-46 across this window -- up to ~4.6x oversubscription), and additionally started a SECOND, directly competing `pnpm build:sidecar-sea` process running concurrently in the background (real CPU+I/O contention from an actual build, not just synthetic `yes` load). First attempt at this (against the LIVE binary path directly) produced 5/5 instant SIGKILLs (rc=137, zero output, waited=1s) -- diagnosed as a macOS code-signing/mmap artifact of the concurrent build process overwriting the exact binary file that was mid-execution (a test-methodology bug, not a reproduction of the hang under investigation), NOT counted as evidence either way. Corrected by copying the binary to a stable scratch path immune to being clobbered by the concurrent rebuild, then re-ran 5 consecutive native-config self-test attempts against that stable copy while the competing build ran."
  found: "5/5 succeeded: `nativeWorkers:2`, `decode=ok bytes=65536 match=true`, 4-5s wall-clock each, at load averages ranging 39.02 up to 46.30 on this 10-core machine (measured via `sysctl -n vm.loadavg` immediately before each run) -- genuinely extreme, verified CPU oversubscription plus a directly competing build process. Zero hangs, zero timeouts."
  implication: "Even at ~4.6x core oversubscription with a real competing build process actively contending for the same CPUs, the real-sized decode task still completes reliably in a genuinely compiled SEA binary, in both codec configurations. 25/25 total successes this session across escalating contention levels. This is a materially stronger test than the prior session's normal-conditions retest and still could not reproduce the original hang."

## Resolution

**CLOSED 2026-08-18T21:00 — coordinator/human-operator decision (option 1, "close conservative"): could not reproduce under any tested condition across 40/40 attempts spanning two sessions; monitor for recurrence in the field before revisiting.** This is a closure of the investigation, NOT a claim that the underlying defect was found and fixed. `NATIVE_LZMA_DECODE_ENABLED` remains `false` (unchanged, kill-switch left in place) and the second test in `lzmaNativeSeaRealBuild.test.ts` remains skipped (not un-skipped). No further live-hardware reproduction attempts were made this closing session.

root_cause: "UNKNOWN / UNCONFIRMED -- NOT DISPROVEN. The original hang (`decompress_pool_timeout` on a real-sized chunk decode inside a genuinely compiled, postject-injected SEA binary, reproduced 5+ times by the discovering session with specific diagnostic instrumentation showing `handleDecodeMessage()` was never entered for the real payload) was real and directly observed, not a test artifact or a misdiagnosis. It has simply never been reproduced again: 40 total attempts across two sessions (15 normal-condition retests + 25 attempts under deliberately escalating CPU contention, up to ~4.6x core oversubscription with a directly competing concurrent build process, plus four independent minimal/incremental reproductions of increasing fidelity to the real code path) all succeeded with zero hangs, in both the pure-JS and native codec configurations. The leading 'transient CPU-scheduling starvation' theory is weakened by the contention testing but not eliminated (I/O contention, memory pressure/swapping, or a machine-state factor specific to the original discovery session were never directly replicated); a genuine but extremely narrow/rare race condition also remains possible. No new hypothesis has been confirmed. See Evidence and reasoning_checkpoint above for the full chain."
fix: "None applied. The conservative kill-switch (`NATIVE_LZMA_DECODE_ENABLED=false` in `src/backend/storeManagers/steam/depot/lzmaLoader.ts`) is left in place, unchanged, as the shipped default. This routes all decode through the pure-JS `lzma` codec, which closes the narrower 'native binding resolves, then hangs' framing this arc started with (round 2's identity-guard fix is real and correct) but, per the evidence above, does NOT itself guarantee the pure-JS path is immune to the same unreproduced hang -- the pure-JS configuration also failed to reproduce it, but also never proved it structurally cannot occur. `lzmaNativeSeaRealBuild.test.ts`'s second test remains skipped, matching the kill-switch's own re-enable criteria (not all criteria items are satisfied -- see that file's own doc comment)."
verification: "N/A -- closing as unreproduced, not as fixed. No behavioral change was shipped, so there is nothing to verify against the original symptoms. Standing recommendation for whoever reopens this: capture full system state (thermal, memory pressure, concurrent I/O, not just CPU load average) the next time `decompress_pool_timeout` is observed against a real packaged SEA binary in the field, since 40/40 brute-force reproduction attempts across two sessions could not recreate the original conditions."
files_changed: []
