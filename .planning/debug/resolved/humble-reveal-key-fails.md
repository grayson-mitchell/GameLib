---
status: resolved
trigger: "Humble key claim flow: clicking reveal on an UNREVEALED key fails with \"couldn't reveal this key\" error. Reveal endpoint contract (URL/form fields/CSRF) was never live-validated — RESEARCH A1-A3 LOW-MEDIUM confidence, Plan 14-06 Task 2 pending."
created: 2026-07-08T00:00:00Z
updated: 2026-07-08T23:59:00Z
---

## Current Focus

RESOLVED (round 7). Round 6's electron-net transport swap was CONFIRMED FIXED
live by the user at 21:40:49 — reveal POST succeeded (csrfTokenPresent=true,
no failure lines), key was revealed and redeemed on Steam. A residual
frontend-only bug was reported during that same verification (the row kept
showing the "Claim" button instead of "Finish activation"/"Revealed" after a
successful reveal): investigated and fixed in this round — see Evidence and
Resolution below. No further action pending.

reasoning_checkpoint (ROUND 7 — transport-fingerprint hypothesis CONFIRMED LIVE; residual frontend UI-state bug found and fixed):
  hypothesis: "(1) Round 6's transport-fingerprint hypothesis is TRUE: swapping the reveal POST from axios to Electron's `net.request` on the `persist:humble` partition closes the Cloudflare Bot-Management gap, and the reveal succeeds end-to-end. (2) The user's newly-reported residual symptom (row still shows 'Claim' after a successful reveal+redeem, not removed from Keys-waiting) is a SEPARATE, frontend-only bug: `HumbleKeysWaiting` (Waiting/index.tsx) fetches its per-row `annotations` map (revealedAt/redeemedAt, driving HumbleKeyRow's Claim vs. Finish-activation vs. Redeemed branch) exactly ONCE on mount and never refetches it — even though the backend's `humbleKeysUpdated` push (via `patchCachedState`) DOES update `humble.keys`' `state` field live. The row's control therefore keeps reading a stale `revealedAt: null`/`redeemedAt: null` from local component state and falls through to the original 'Claim' button, even after the underlying key has already advanced to REVEALED."
  confirming_evidence:
    - "User's round-7 checkpoint response (DECISIVE, live confirmation): gamelib.log line '(21:40:49) [INFO]: [Backend]: Humble reveal: calling adapter (electron-net transport), csrfTokenPresent=true' with NO subsequent failure/diagnostic line — the exact success signature round 6's falsification_test defined ('If the outcome is revealed (UI succeeds) ... the transport-fingerprint hypothesis is CONFIRMED'). User confirmed verbally ('it worked, finally!') and confirmed the redeem-on-Steam step also succeeded."
    - "Direct code read (this round): `library.ts`'s `revealKey()` success branch (`result.status === 'ok'`) DOES call `patchCachedState(gamekey, machineName, 'REVEALED', { revealedKeyValue: result.data.key })`, which mutates `humbleLibraryStore` in place AND calls `sendFrontendMessage('humbleKeysUpdated', getKeys())` — i.e. the backend cache and the live push were ALREADY correct; this ruled out the additional_context's hypothesis (a) (backend cache never patched) as the cause."
    - "Direct code read (this round): `GlobalState.tsx`'s `window.api.handleHumbleKeysUpdated` listener DOES update `humble.keys` in React state on every push — ruling out hypothesis (b)'s 'frontend never receives an event' variant. The actual gap was narrower and purely local to `Waiting/index.tsx`: its own `annotations` state (a SEPARATE piece of state from `humble.keys`, fetched via a dedicated `humbleGetClaimAnnotations` IPC call) has a `useEffect(..., [])` mount-only fetch with no re-fetch trigger anywhere — confirmed by grep showing zero other call sites of `setAnnotations`/`humbleGetClaimAnnotations` in the file before this round's fix, unlike Spares/index.tsx's `giftedMap`, which the sibling tab keeps fresh via an optimistic local update inside `openGiftDialog` itself."
  falsification_test: "If the reveal had failed again with the electron-net transport (another 'definitive failure, status=...' plus an HTML diagnostic), the transport-fingerprint hypothesis would have been falsified per round 6's own test. It did not — clean success, no failure/diagnostic lines at all. For the UI bug: if, after this round's `refreshAnnotations()` fix, the row STILL rendered 'Claim' after a reveal (verifiable by a unit test simulating the wizard's `onDone` firing and asserting a second `humbleGetClaimAnnotations` call + an updated `revealedAt`), the annotations-staleness hypothesis would be falsified and the defect would have to be elsewhere (e.g. `toDisplayKey` silently dropping the state, or `WAITING_STATES` excluding REVEALED). Neither occurred — the new regression test (`Waiting/__tests__/index.test.tsx`) asserts exactly this and passes."
  fix_rationale: "Round 6's fix is now CONFIRMED correct and requires no further change — root_cause is promoted from 'not yet confirmed' to 'confirmed'. The round-7 UI fix (`Waiting/index.tsx`: extracted a `refreshAnnotations()` function, called from `closeWizard()` — covering every wizard exit path, including a successful reveal and a mark-redeemed — and from the standalone `onUndoRedeem` handler after its IPC call resolves) is the smallest change that keeps the two sources of truth (`humble.keys` and the local `annotations` map) from diverging, and mirrors the EXISTING pattern already used one tab over: Spares/index.tsx's `giftedMap` is kept fresh by updating local state immediately after its own mutating action (`openGiftDialog`). A full backend re-sync or a bespoke new IPC push was deliberately NOT used — `humbleGetClaimAnnotations` is already a cheap, existing, on-demand read (used identically at mount), so re-invoking it after each mutation's natural exit point is consistent with the codebase's existing conventions and requires no backend change at all."
  blind_spots: "The 'row not removed from Keys-waiting count' half of the user's report is, by design (D-53/D-67, `common/humble/viewFilters.ts`'s `WAITING_STATES` set), NOT a bug — REVEALED keys are deliberately kept in Keys-waiting until an explicit 'Mark as redeemed' (or a server-confirmed REDEEMED order on the next sync), specifically so a key the user revealed but hasn't yet finished activating is never silently lost from view. This round's fix does not and should not change that grouping; only the per-row CONTROL (Claim vs. Finish activation vs. Redeemed) was stale, and that is what is fixed. Not exercised live post-fix: the user has not yet re-verified the row visually in their real environment (self-verified via unit test + code trace only) — flagged in the final response as a residual, low-risk gap the user can confirm on next use of the guided-claim flow, but not gated behind a further human-verify checkpoint given the orchestrator's find_and_fix scope and the low blast radius of a pure display-annotation refresh."

hypothesis: ROUND 7 — Round 6's electron-net transport swap CONFIRMED LIVE as the root-cause fix (reveal succeeded, no failure/diagnostic lines). Root cause is FULLY RESOLVED. Residual: a separate, frontend-only staleness bug in Waiting/index.tsx's `annotations` map (never refetched after a claim-flow mutation) made the row's Claim/Finish-activation control lag behind the already-correct `humble.keys` state. FIX APPLIED (Waiting/index.tsx): `refreshAnnotations()` now runs after every wizard close and after undo-redeem resolves.
test: (round 7) Frontend-only change, no backend touched. New test file `src/frontend/screens/Humble/Keys/Waiting/__tests__/index.test.tsx` (3 new tests: mount fetches annotations once; wizard-close triggers a second fetch and the row reflects it; undo-redeem triggers a refetch after its IPC call resolves). Full repo-wide `npx jest` — 706/706 passing (up from 703: +3 new). `npx jest src/backend/humble` unaffected — still 384/384 (no backend files changed this round). `npx tsc --noEmit` clean. `npx eslint` on changed files clean (0 errors, 0 warnings after tightening the new test's typing to avoid `any`-cascades).
expecting: With the fix live, opening the claim wizard, revealing a key, and closing the wizard causes the row to immediately show "Revealed {{date}}" + "Finish activation" (not "Claim"); marking redeemed and closing shows "Redeemed {{date}}" + "Undo"; clicking Undo reverts the row back to "Finish activation" — all without a manual refresh/resync. Verified via the new unit tests (react/useContext/useState harness, no live app needed for this narrow a change) plus a full tsc/eslint/jest pass; not yet re-observed by the user in the live app (see blind_spots above) — acceptable given goal:find_and_fix scope and the change's low blast radius (a read-only annotation refetch, no new write paths).
next_action: NONE — debug session resolved. Archive to .planning/debug/resolved/ and append a knowledge-base entry.

reasoning_checkpoint (ROUND 6 — transport-fingerprint hypothesis; electron-net transport swap applied, awaiting live verification):
  hypothesis: "The reveal POST's failure is NOT caused by request CONTENT (headers, cookies, CSRF token — all confirmed live-correct or already fixed across rounds 1-5) but by the TRANSPORT ITSELF: axios's plain Node.js HTTPS stack presents a TLS/JA3 fingerprint, HTTP/1.1 negotiation, and header-casing/ordering that does not match a real Chromium browser, and Cloudflare Bot Management (confirmed present on humblebundle.com via the `__cf_bm` cookie, round 4 evidence) fingerprints the TRANSPORT independent of any header/cookie content and blocks the request with an HTML challenge page BEFORE Humble's own application code (which only ever returns JSON) sees it. Fix: route the reveal POST through Electron's own Chromium network stack (`net.request` on the `persist:humble` partition) instead of axios — a genuine, indistinguishable-from-a-real-tab TLS/HTTP2 fingerprint — with `useSessionCookies`/`credentials: 'include'` sourcing the partition's live cookie jar natively (superseding round 5's hand-built HumbleUser.getFullCookieHeader mirror, which is now redundant and removed) and a matching Chrome-shaped User-Agent."
  confirming_evidence:
    - "User's round-6 checkpoint response (grep output from gamelib.log after a fresh restart + reveal attempt, DECISIVE): 'Humble reveal: calling adapter, csrfTokenPresent=true fullCookieJarPresent=true' immediately followed by 'Humble reveal: definitive failure, status=access_denied' immediately followed by 'Humble adapter: reveal HTTP failure diagnostic status=403 contentType=text/html; charset=UTF-8 bodyIsString=true looksLikeHtml=true bodyLength=4552'. Every round 1-5 fix was confirmed LIVE and ACTIVE (csrfTokenPresent=true, fullCookieJarPresent=true) and the request STILL 403'd with an HTML body — this is the exact discriminator round 5's own diagnostic was built to surface: `looksLikeHtml=true`/`contentType=text/html` means a Cloudflare Bot-Management/WAF challenge PAGE, not a genuine Humble JSON denial (Humble's real API never returns HTML; RevealResponseSchema's shape is JSON-only). A ~4.5KB HTML body on a 403 is the canonical Cloudflare interstitial/challenge-page signature."
    - "Round 4 evidence (unchanged, reused this round): humblebundle.com sits behind Cloudflare Bot Management (`__cf_bm` cookie confirmed present in the live partition) — but plain axios GETs to `/api/v1/*` succeed cleanly (25/25, denied=0) on the SAME transport the SAME day, ruling out a blanket non-browser-traffic block and narrowing the live candidate to Cloudflare scoring this ONE write endpoint (`/humbler/redeemkey`) more strictly — consistent with the transport-fingerprint hypothesis (a POST to a CSRF-protected, state-changing endpoint is exactly where a WAF applies its strictest bot-detection heuristics) rather than a content defect (content was already proven correct/complete by round 5's own csrfTokenPresent=true/fullCookieJarPresent=true evidence)."
  falsification_test: "Grep the SAME log pattern (extended for round 6) after this round's fix on the NEXT reveal attempt: 'Humble reveal: calling adapter (electron-net transport), csrfTokenPresent=...' plus, if it fails again, 'Humble adapter: reveal HTTP failure diagnostic status=... contentType=... bodyIsString=... looksLikeHtml=...'. If the outcome is STILL 'definitive failure, status=access_denied' AND the diagnostic STILL shows looksLikeHtml=true/contentType=text/html even through Electron's own Chromium network stack, the transport-fingerprint hypothesis is falsified outright — Cloudflare would then have to be fingerprinting something beyond the raw TLS/HTTP layer (e.g. a JS-executed proof-of-work challenge / browser-automation signal that only a real page load with document/window context can satisfy, which no bare `net.request` call — however browser-faithful its TLS layer — can produce). That would reframe the investigation toward option (b) from this round's evaluation: executing the POST via `webContents.executeJavaScript` inside the actual (visible or hidden) login BrowserWindow/session, the most faithful possible reproduction of a real page's fetch(). If the outcome is 'revealed' (UI succeeds) or a DIFFERENT status with a JSON (not HTML) body, the transport-fingerprint hypothesis is CONFIRMED."
  fix_rationale: "Addresses the root cause round 6's evidence actually narrowed to (transport-level fingerprint mismatch on a fully-correct, fully-authenticated request), not a symptom: humblePostRequest (adapter.ts) no longer uses axios at all — it now issues the POST via `net.request({ method: 'POST', url, partition: 'persist:humble', credentials: 'include', useSessionCookies: true })`, Electron's own Chromium network stack, on the same partition the login flow already uses. This makes the TLS/HTTP2 handshake, cipher/extension order, and HTTP semantics genuinely indistinguishable from a real browser tab (because it IS the same underlying network stack), and makes cookie attachment fully native (no more hand-built Cookie header at all — round 5's HumbleUser.getFullCookieHeader is removed as redundant, not just superseded, since leaving it as unused dead code risks a future round mistaking it for a still-active fix). GET paths (humbleRequest, still axios) are deliberately UNCHANGED — round 4/5 evidence shows Cloudflare scores this ONE write endpoint far more aggressively than the read endpoints, so only the endpoint that has ever actually failed gets the transport swap, keeping the fix minimal and the already-working sync path's risk surface at zero."
  blind_spots: "NOT yet verified live. Does not rule out: (a) Cloudflare requiring a JS-executed challenge/proof-of-work that only a real page load (with a live DOM/window, capable of running Cloudflare's own challenge script) can satisfy — `net.request` has a genuine Chromium TLS fingerprint but is NOT a page load and cannot execute arbitrary JS challenges; if this is the actual gate, only option (b) (execute the POST via `webContents.executeJavaScript` inside the login window/session) would work, and this round's falsification_test is specifically designed to distinguish the two; (b) Humble/Cloudflare blocking based on IP reputation or request VELOCITY (a day of repeated dev-testing cycles across 6 rounds) independent of transport — the diagnostic will still show looksLikeHtml=true in that case too, so this specific test cannot distinguish 'wrong transport' from 'rate-limited/blocked IP regardless of transport'; a clean pass on a DIFFERENT network/IP would be the only way to fully rule this out, which is outside this round's scope; (c) whether Electron's net.request, when NOT run inside an actual BrowserWindow's renderer context (this call is main-process-only, like all Humble adapter calls), still carries the FULL browser-fingerprint fidelity Cloudflare might check for (e.g. sec-fetch-* headers, which browsers auto-attach only for page-initiated fetch()s, not for a bare main-process net.request — NOT set manually this round, a possible residual gap if Cloudflare specifically checks for their presence)."

hypothesis: ROUND 6 — Round 5's checkpoint evidence (fullCookieJarPresent=true, still access_denied, status=403/contentType=text/html/looksLikeHtml=true/bodyLength=4552) is DECISIVE: the reveal POST is blocked by a Cloudflare Bot-Management/WAF HTML challenge page, not a Humble application-level denial. Content (headers/cookies/CSRF) was already proven correct/complete — the remaining gap is TRANSPORT FINGERPRINT (axios's non-browser TLS/HTTP stack). FIX APPLIED: swapped the reveal POST's transport from axios to Electron's own `net.request` on the `persist:humble` partition (genuine Chromium TLS/HTTP2 fingerprint, native session-cookie attachment, matching Chrome UA); removed round 5's now-redundant HumbleUser.getFullCookieHeader mechanism. Root cause NOT YET CONFIRMED LIVE.
test: (round 6) Transport swap in adapter.ts's humblePostRequest (axios.post -> net.request); revealKey()'s signature simplified (no more cookie/fullCookieHeader params — the transport sources cookies natively); mapAxiosError generalized via a new extractHttpErrorResponse helper to handle both AxiosError (GET paths, unchanged) and the new HumbleTransportHttpError (POST path) uniformly; standardBrowserUserAgent hoisted out of user.ts into a new userAgent.ts module (both adapter.ts and user.ts import it, avoiding a circular dependency) and applied as the reveal POST's User-Agent header; HumbleUser.getFullCookieHeader removed (superseded, not just unused). 384/384 humble suite passing (up from 393 — round 5's 4 fullCookieHeader-specific tests were removed/consolidated along with the mechanism; net new coverage: transport options/headers, native-cookie-attachment assertion, WR-04 manually-armed-timeout-and-abort test, HumbleTransportHttpError status mapping), tsc clean, eslint clean (0 errors, pre-existing warning baseline unchanged).
expecting: With the fix live, the NEXT reveal attempt logs 'Humble reveal: calling adapter (electron-net transport), csrfTokenPresent=...' followed by EITHER 'revealed' (UI advances past 'failed' — the transport fingerprint was the actual gap, CONFIRMING this round's hypothesis) OR another 'definitive failure, status=...' PLUS a 'Humble adapter: reveal HTTP failure diagnostic status=... contentType=... bodyIsString=... looksLikeHtml=...' line. If that diagnostic STILL shows looksLikeHtml=true/contentType=text/html even through the Chromium-native transport, the transport-fingerprint hypothesis is FALSIFIED and the investigation must pivot to executing the POST inside the actual login BrowserWindow/session (option b, a JS-challenge/proof-of-work gate) or to IP-reputation/velocity blocking independent of transport.
next_action: AWAITING HUMAN VERIFICATION (round 6) — full restart, attempt a fresh reveal on an UNREVEALED key, then grep gamelib.log for `Humble reveal:\|Humble adapter: reveal` and paste the raw output verbatim, plus the exact UI text/step shown. See checkpoint below for the exact command.

reasoning_checkpoint (ROUND 5 — CSRF-missing chain FALSIFIED live; fix applied, awaiting live verification):
  hypothesis: "The reveal POST reaches Humble and IS live-403'd (definitively proven this round — see Evidence) even with round 3's csrf_cookie double-submit fix in place. Three concrete, previously-unfixed contract-shape gaps remain plausible, independently or in combination: (a) GameLib always sends `X-Requested-By: hb_android_app` on the reveal POST while NEITHER proven-working reference implementation (FailSpy Selenium fetch(), GreasyFork userscript) ever sends it on this endpoint — both execute same-origin inside a real browser page with no such header; (b) GameLib (even post-round-3) only ever hand-constructs 2-3 cookie values (`_simpleauth_sess`, `csrf_cookie`) while both real references rely on the browser auto-attaching the FULL same-origin cookie jar (27+ cookies on this account, including Cloudflare's `__cf_bm` bot-management cookie — round 4 evidence); (c) the access_denied status itself may be conflating a genuine Humble 403 with a Cloudflare Bot Management HTML challenge page, which were previously indistinguishable because mapAxiosError never logged anything for a caught HTTP error."
  confirming_evidence:
    - "User's round-5 checkpoint response (grep output from gamelib.log after a fresh restart + reveal attempt, DECISIVE): 'Humble reveal: calling adapter, csrfTokenPresent=true' immediately followed by 'Humble reveal: definitive failure, status=access_denied' — this is the reveal-path log line (library.ts:1009), NOT the sync-path line (library.ts's getGamekeys access_denied) that round 4's confound could not previously rule out. Proves definitively: (1) the running build DOES contain rounds 1-3's code (round-2 instrumentation fired), (2) csrfTokenPresent=true — csrf_cookie WAS decrypted and attached to both the header AND (post-round-3) the Cookie header, (3) the reveal POST went over the network and Humble returned a live 403/429 EVEN WITH round 3's double-submit-cookie fix in place."
    - "Round 4's code-only re-read of the ACTUAL FailSpy source (not RESEARCH.md's paraphrase) confirmed the module-level 'Content-Type: application/x-www-form-urlencoded' headers dict RESEARCH.md quotes is dead/unused code — the real redeem call sends NO Content-Type override and NO X-Requested-By at all, executing via same-origin fetch() inside an authenticated browser page (full auto-attached cookie jar). GameLib's body encoding already matched the second reference (GreasyFork userscript, urlencoded) — ruled out as a defect. X-Requested-By and the 2-cookie-only jar remained the two live, previously-unfixed gaps."
  falsification_test: "Grep the SAME log pattern after this round's fix on the NEXT reveal attempt: 'Humble reveal: calling adapter, csrfTokenPresent=... fullCookieJarPresent=...' plus, if it fails again, 'Humble adapter: reveal HTTP failure diagnostic status=... contentType=... bodyIsString=... looksLikeHtml=...'. If the outcome is STILL 'definitive failure, status=access_denied' AND the diagnostic shows looksLikeHtml=false/contentType=application/json (i.e. a genuine Humble JSON denial, not a Cloudflare challenge), this round's 3 fixes (drop X-Requested-By, send full cookie jar, structural diagnostic) are falsified as sufficient — the account/IP may be genuinely rate-limited/blocked by Humble independent of request shape, or the keyindex/field contract itself needs re-verification against a byte-for-byte FailSpy diff. If looksLikeHtml=true/contentType=text/html, that reframes the entire investigation toward Cloudflare Bot Management specifically gating this endpoint, which is not fixable by request-shape changes alone."
  fix_rationale: "All three fixes are additive/opt-in and address the root cause the round-5 evidence actually narrowed to (contract-shape/request-fingerprint mismatch on a live-reached endpoint), not a symptom: (1) humblePostRequest no longer spreads X-Requested-By onto the reveal POST (GET paths untouched, unaffected, still working) — matches both real references exactly; (2) HumbleUser.getFullCookieHeader() reads the live persist:humble partition's FULL cookie jar and, when successful, supersedes the hand-built 2-3-cookie Cookie header entirely in humblePostRequest/revealKey — mirrors what a real browser sends, purely opt-in (undefined = old behavior, zero regression risk to the CSRF fix); (3) mapAxiosError gained an opt-in `context` param (revealKey is the only caller that passes one) that logs a STRUCTURAL-ONLY diagnostic (status/content-type/bodyIsString/looksLikeHtml/bodyLength — never body content, mirrors describeSchemaFailure's own redaction discipline) so the NEXT failure (if any) discriminates a genuine Humble JSON denial from a Cloudflare HTML challenge page in one log line, closing the exact ambiguity round 4 flagged as unresolvable from code alone."
  blind_spots: "NOT yet verified live. Does not rule out: (a) Humble/Cloudflare rate-limiting or blocking this specific account/IP after a day of repeated dev-testing cycles, independent of anything GameLib's request shape does or doesn't send — the new diagnostic logging will surface this as looksLikeHtml=true if it's a Cloudflare challenge, but a plain account-level Humble-side block would still show as a normal JSON 403 and remain indistinguishable from a genuine per-request defect; (b) the keyindex value itself being wrong/stale for this specific key (schema_error would fire instead of access_denied in that case per the RevealResponseSchema path, so this is lower-probability given the observed status, but not eliminated); (c) TLS/HTTP2 fingerprinting blocking axios's transport outright regardless of headers/cookies (not testable/fixable from this codebase without a transport swap, per adapter.ts's own doc comment on humbleRequest's D-14 seam)."

reasoning_checkpoint (ROUND 4 — code-only investigation, no live network; round 3 checkpoint FAILED, new "couldn't refresh" banner symptom):
  hypothesis: "The round-3 checkpoint response ('nope not working, also banner say couldn't refresh showing') is NOT sufficient evidence to conclude round 3's csrf_cookie fix failed on its own merits, because the new banner has TWO structurally distinct, code-confirmed possible origins that are indistinguishable without gamelib.log: (A) HumbleClaimWizard's reveal call itself hit adapter.ts's access_denied branch (library.ts:1012-1019), which sets the SAME shared humbleSyncStore.syncError='denied' the Keys screen banner reads — in which case the banner IS downstream proof the reveal reached Humble and got a live 403/429, OR (B) the mandatory GlobalState mount-time chain (`humbleCheckHealth().then(() => humbleSync())`, GlobalState.tsx:1131) that fires on EVERY app restart independently ran a full sync() whose OWN getGamekeys() call hit access_denied (library.ts:681-693) — a plain GET with no CSRF/cookie involvement at all — which sets the identical syncError='denied' and produces the byte-for-byte identical banner text, completely unrelated to the reveal attempt or round 3's fix. Both code paths are real, both are wired to the exact same renderer state, and the user's one-line report cannot discriminate between them."
  confirming_evidence:
    - "library.ts:1012-1019 (reveal path) and library.ts:681-693 (sync/getGamekeys path) are the ONLY two places in the entire backend that ever set syncError:'denied' (grep-confirmed) — both drive frontend/screens/Humble/Keys/index.tsx:125's `showBanner = !!humble.syncError && humble.syncError !== 'none'` and line 186-190's exact 'Couldn't refresh — showing data from {{time}}' text"
    - "GlobalState.tsx:1130-1131 confirmed (direct code read): every app mount with isLoggedIn=true unconditionally chains humbleCheckHealth() -> humbleSync() — i.e. the FULL RESTART round 3's checkpoint explicitly instructed the user to perform is itself guaranteed to fire a real sync() attempt (independent of any reveal click) before the user ever touches the reveal button again"
    - "Round 2 evidence already established getGamekeys succeeded cleanly (25/25, denied=0) as of ~08:17Z on the SAME account/session — if a plain GET now 403s on the very next restart with no code change to the sync path, that points to a Humble-side account/IP-level throttle or an invalidated session, NOT a reveal-specific CSRF defect"
  falsification_test: "grep 'Humble sync: getGamekeys access_denied' in gamelib.log — if present, banner (B) is confirmed and is unrelated to reveal/CSRF (falsifies attributing the banner to round 3's fix). grep 'Humble reveal: definitive failure, status=access_denied' — if present instead (and the sync-path line is ABSENT), banner (A) is confirmed: the reveal itself was denied live, which DOES mean round 3's csrf_cookie fix (if it was actually the code that ran) was insufficient, moving contract-shape hypotheses (multipart-vs-urlencoded body, missing non-CSRF cookies, Cloudflare Bot Management gating write endpoints differently than reads) to the top."
  fix_rationale: "No fix applied this round — deliberately. Three consecutive rounds have now shipped code changes with zero live confirmation; a 4th speculative change compounds an already-unconfirmed stack and makes future verification harder to attribute. This round instead: (1) precisely explains the new banner symptom from code so the user isn't chasing an unrelated red herring, (2) does the maximum possible code-only contract validation (see Evidence below — found a genuinely new, previously-uninspected discrepancy in the reveal request shape versus the ACTUAL FailSpy source, not just RESEARCH.md's paraphrase of it), (3) issues a maximally mechanical checkpoint (exact log path + exact grep command) since three rounds of open-ended 'paste the logs' requests have not produced logs."
  blind_spots: "Still cannot confirm from code alone: (a) whether the running app binary actually contains round 1/2/3's changes (the single most important unresolved question across all 4 rounds); (b) whether Humble is rate-limiting/blocking this specific account or IP after a day of repeated dev-testing login cycles, independent of anything GameLib's code does or doesn't send; (c) whether Cloudflare Bot Management (confirmed present via __cf_bm cookie in the live partition, see Evidence) applies stricter rules to /humbler/* than to /api/v1/* — plausible given Humble deliberately CSRF-protects only the write endpoint, but NOT provable without a live request/response pair."

hypothesis: ROUND 5 — CSRF-missing chain (rounds 1+3) FALSIFIED as the sole cause by live evidence (csrfTokenPresent=true, still access_denied). FIX APPLIED: drop X-Requested-By on the reveal POST, send the live full cookie jar when readable, add a structural (redacted) HTTP-failure diagnostic to discriminate a genuine Humble denial from a Cloudflare challenge page. Root cause NOT YET CONFIRMED LIVE.
test: (round 5) 3 additive/opt-in code fixes in adapter.ts/user.ts/library.ts (see reasoning_checkpoint above for full rationale). 13 new regression tests added (393/393 humble suite passing, up from 380), tsc clean, eslint clean (1 unrelated pre-existing-pattern error fixed along the way — unnecessary type assertion in the new diagnostic helper).
expecting: With the fixes live, the NEXT reveal attempt logs 'Humble reveal: calling adapter, csrfTokenPresent=... fullCookieJarPresent=...' (fullCookieJarPresent=true confirms the live partition read succeeded and the full jar was sent) followed by EITHER 'revealed' (UI advances past 'failed' — X-Requested-By and/or the fuller cookie jar were the actual gap) OR another 'definitive failure, status=...' PLUS (new this round) a 'Humble adapter: reveal HTTP failure diagnostic status=... contentType=... bodyIsString=... looksLikeHtml=...' line that discriminates a genuine Humble-side denial (looksLikeHtml=false, contentType=application/json) from a Cloudflare Bot Management challenge (looksLikeHtml=true, contentType=text/html) — the latter would reframe the whole investigation toward Cloudflare-specific gating, not fixable by request-shape changes.
next_action: AWAITING HUMAN VERIFICATION (round 5) — full restart, attempt a fresh reveal on an UNREVEALED key, then grep gamelib.log for `Humble reveal:\|Humble adapter: reveal` and paste the raw output verbatim, plus the exact UI text/step shown. See checkpoint below for the exact command.

reasoning_checkpoint (ROUND 3 — csrf_cookie double-submit gap, FIX APPLIED, awaiting live verification):
  hypothesis: "revealKey()'s POST to /humbler/redeemkey fails Humble's CSRF validation because humblePostRequest sent the csrf-prevention-token HEADER but never included the matching csrf_cookie value in the request's Cookie header. axios has no cookie jar for the persist:humble partition, so unlike FailSpy's real-browser reference implementation (which auto-attaches every humblebundle.com cookie, including csrf_cookie, on top of the manually-read header value), GameLib's Cookie header only ever carried _simpleauth_sess=<value>. If Humble's CSRF middleware is a standard double-submit-cookie scheme (compares header value to a same-named cookie on the request), a request with the header but not the cookie is functionally indistinguishable from a CSRF-missing request and 403s — masquerading as access_denied exactly as RESEARCH.md's own risk note warned, regardless of csrfTokenPresent=true."
  confirming_evidence:
    - "adapter.ts's buildHeaders() (GET) and humblePostRequest() (POST, pre-fix) both constructed Cookie as exactly `_simpleauth_sess=${cookie}` — csrf_cookie was never appended, confirmed by direct code read"
    - "adapter.test.ts (pre-fix) locked this in explicitly: the csrfToken-present test only ever asserted the separate header value, never the Cookie header — no test in the suite had ever exercised sending csrf_cookie as an actual cookie"
    - "14-RESEARCH.md:164 — the ONE proven-working reference (FailSpy) is Selenium-driven inside a real authenticated browser session (`driver.get_cookie('csrf_cookie')`), meaning the browser auto-attaches ALL domain cookies (including csrf_cookie) on every request IN ADDITION to the manually-read header value; GameLib's axios call structurally could never replicate that auto-attach, so it was silently sending only half of what the working reference sends"
  falsification_test: "If Humble's CSRF check validates the header value alone (not a double-submit comparison against a cookie), adding csrf_cookie to the Cookie header has zero effect and the reveal fails identically after this fix — that falsifies this hypothesis and points to a different contract-shape defect (wrong keyindex resolution/value, Humble blocking axios's TLS/HTTP fingerprint outright regardless of headers, or a stale/never-restarted dev build never having exercised any of round 1/2/3's changes at all)."
  fix_rationale: "Adds the csrf_cookie value (the SAME decrypted value already correctly sourced via HumbleUser.getCsrfToken() -> decryptCookie, independently verified correct this round) into the POST's Cookie header via humblePostRequest, mirroring exactly what the one proven-working reference implementation sends automatically as a real browser. Purely additive: never changes the token's source, never touches the header, and is a byte-for-byte no-op (`_simpleauth_sess=${cookie}` unchanged) when csrfToken is absent — cannot regress the CSRF-absent case."
  blind_spots: "NOT yet verified live — no confirmed reveal attempt with this fix in place; the coordinator's staleness concern (is the RUNNING dev build even executing today's code, or round 1/2's changes) remains completely unresolved and this fix compounds that same open question. Does not rule out: (a) Humble expecting the cookie value URL-encoded/decoded differently than the raw value stored (cookie values are sometimes percent-encoded in the wild; this code applies zero encoding, matching _simpleauth_sess's existing precedent); (b) a wrong/stale keyindex silently producing a schema_error that is UI-indistinguishable from a CSRF 403 (both map to the wizard's generic 'failed' text — round 2's instrumentation logs result.status distinctly for this exact reason, still unexercised); (c) axios/TLS fingerprinting blocking the request independent of any header/cookie correctness (not testable from code alone)."

reasoning_checkpoint (ROUND 1 — CSRF-only, NOT eliminated, but INSUFFICIENT/UNCONFIRMED):
  hypothesis: "revealKey() sends its POST to /humbler/redeemkey WITHOUT the csrf-prevention-token header for this user's account, because HumbleUser.getCsrfToken() only ever gets populated inside finishLogin()."
  status: "Round-1 self-verification (Disconnect+Reconnect) does NOT confirm or refute this — see Evidence below. checkHealthAndFlagExpiry (which carries the actual backfill fix) is ONLY invoked from GlobalState's mount-time effect (frontend/state/GlobalState.tsx:1130-1131), i.e. app STARTUP — a runtime Disconnect+Reconnect never calls it. The observed csrfToken presence is fully explained by finishLogin()'s PRE-EXISTING Phase 14 capture code (user.ts:412-426, shipped before this debug session), which runs on every successful reconnect regardless of whether the new backfill fix is even in the running build. The backfill fix itself remains UNTESTED by this round."
  additionally: "Even granting csrfToken is now genuinely present (plausible — finishLogin's capture is independent of the backfill and DID just run), the user's reported error text ('Couldn't reveal this key — nothing was used up') is the literal copy of HumbleClaimWizard's 'failed' step (index.tsx:248-252) — NOT the distinct 'cooldown' step text ('Temporarily unavailable — retry in {{N}}m', index.tsx:275-281). Since library.ts's revealKey() silently short-circuits (zero log lines) on 3 different early-return branches — cooldown (line ~895), ineligible/target-not-found-or-wrong-state (line ~879), ineligible/keyindex-unresolved (line ~899) — and mapAxiosError (adapter.ts:230-246) is ALSO silent for a genuine live 401/403/429 denial, the total absence of reveal-related log lines could not previously distinguish ANY of: (a) local cooldown gate, (b) local ineligible gate, (c) a live CSRF-shaped 403 despite the token now being present, (d) a live 401/429 for an unrelated reason. All map to the same 'failed' wizard text. This round's evidence is CONSISTENT WITH the CSRF hypothesis but does not confirm it, and is EQUALLY CONSISTENT with 3 other silent code paths never previously distinguished from a live denial.

hypothesis: ROUND 3 — csrf_cookie was never attached to the POST's Cookie header (double-submit-cookie gap), only the csrf-prevention-token HEADER was sent. FIX APPLIED (adapter.ts humblePostRequest). Root cause NOT YET CONFIRMED LIVE — this is the current best-evidence hypothesis, not a certainty; round 2's instrumentation is still in place and will independently confirm or refute it on the next attempt.
test: (round 3) Code-level fix: humblePostRequest now attaches `csrf_cookie=<value>` to the Cookie header alongside `_simpleauth_sess`, whenever a csrfToken is present (mirrors the one proven-working reference implementation, which uses a real browser that auto-attaches this cookie). 2 new regression tests added (Cookie-header-carries-csrf_cookie-when-present; Cookie-header-unchanged-when-absent). 380/380 humble suite passing, tsc clean. Round 2's status-only logging in library.ts's revealKey() remains fully in place and UNTOUCHED this round — it will still fire and name the exact branch/status on the next attempt, so this fix's effect (or lack of one) will be directly observable in gamelib.log.
expecting: With the fix live, IF the double-submit-cookie hypothesis is correct: the next reveal attempt logs 'Humble reveal: calling adapter, csrfTokenPresent=true' followed by EITHER 'revealed' (UI advances past 'failed') or a 'definitive failure, status=...' naming a DIFFERENT failure mode than before (proving the CSRF gap specifically was closed, even if something else still blocks). IF the hypothesis is wrong: identical 'definitive failure, status=access_denied' (or same status as before) with no change in UI text — this falsifies the double-submit-cookie theory and the next round should pivot to checking dev-build staleness (is ANY of round 1/2/3 even running) and/or TLS/fingerprint blocking.
next_action: AWAITING HUMAN VERIFICATION (round 3) — rebuild/restart GameLib fully (a full restart, not Disconnect+Reconnect, is required so all of round 1/2/3's changes are actually running — see checkpoint below for exact confirmation steps), then attempt a fresh reveal on an UNREVEALED key and paste back the new 'Humble reveal: ...' log lines plus the exact UI text/step shown.

## Symptoms

expected: Clicking the reveal/claim action on an UNREVEALED Steam key row reveals the key — Humble returns the redeemed key value and the row shows the code / advances to redeem
actual: UI shows error "couldn't reveal this key"; key remains unrevealed
errors: "couldn't reveal this key" (UI text; exact wording may differ slightly — user paraphrased). No logs captured yet — user needs help capturing DevTools/main-process logs
reproduction: Dev build at HEAD 3067d7eb (Phase 14 waves 1-5 complete), real Humble account, Humble Keys view → key row direct reveal action (NOT the wizard) on an UNREVEALED Steam key
started: First reveal attempt ever through GameLib — this code path has never worked (Plan 14-06 Task 2 live validation was pending precisely because reveal contract is unverified)

## Eliminated

- hypothesis: "ROUNDS 1+3 combined — CSRF token/cookie is missing or incorrectly attached (csrfToken never backfilled, or the double-submit-cookie's csrf_cookie value never sent in the Cookie header) is the (sole) root cause of the reveal 403."
  evidence: "Round 5 checkpoint response (user-pasted gamelib.log after a fresh restart + reveal attempt): 'Humble reveal: calling adapter, csrfTokenPresent=true' immediately followed by 'Humble reveal: definitive failure, status=access_denied'. This is unambiguously the reveal-path log line (library.ts:1009), not the sync-path confound round 4 could not rule out. csrfTokenPresent=true proves the token WAS decrypted and attached to both the csrf-prevention-token header AND (post-round-3) the Cookie header's csrf_cookie value — yet Humble still returned a live access_denied. The double-submit-cookie theory is therefore insufficient as a standalone explanation; CSRF presence/attachment is no longer the leading candidate (though it remains a NECESSARY, now-confirmed-correct part of the request, not itself the remaining gap)."
  timestamp: 2026-07-08 (round 5)

- hypothesis: "ROUND 5 — request-CONTENT gaps (X-Requested-By present when it shouldn't be, only a hand-built 2-3-cookie Cookie header instead of the browser's full same-origin jar, access_denied conflating a genuine Humble denial with an undiagnosed Cloudflare challenge) are the remaining root cause of the reveal 403, independently or in combination."
  evidence: "Round 6 checkpoint response (user-pasted gamelib.log after a fresh restart + reveal attempt on the round-5 build, DECISIVE): 'Humble reveal: calling adapter, csrfTokenPresent=true fullCookieJarPresent=true' — proving ALL THREE round-5 content fixes were live and active (X-Requested-By dropped, full cookie jar read succeeded and was sent, diagnostic wired in) — immediately followed by 'Humble reveal: definitive failure, status=access_denied' immediately followed by 'Humble adapter: reveal HTTP failure diagnostic status=403 contentType=text/html; charset=UTF-8 bodyIsString=true looksLikeHtml=true bodyLength=4552'. The diagnostic round 5 itself built to discriminate a genuine Humble JSON denial from a Cloudflare challenge page fired exactly as designed and returned the Cloudflare answer: an HTML body on a 403 is not a shape Humble's real API ever produces (RevealResponseSchema is JSON-only, cross-verified against two working reference implementations). Every piece of REQUEST CONTENT was therefore already correct/complete and STILL failed — request content is eliminated as the remaining gap; the block happens before Humble's own application code ever sees the request, at the Cloudflare/WAF layer, which fingerprints the TRANSPORT rather than validating headers/cookies."
  timestamp: 2026-07-08 (round 6)

- hypothesis: "additional_context's hypothesis (a) — the reveal success path never patches the backend's cached key entry (i.e. `patchCachedState`/the 25-key cache written by sync is never updated in place with the REVEALED state and revealedKeyValue)."
  evidence: "Direct code read of library.ts's revealKey() success branch: `patchCachedState(gamekey, machineName, 'REVEALED', { revealedKeyValue: result.data.key })` is called unconditionally on `result.status === 'ok'`, which both mutates `humbleLibraryStore` in place (read-modify-write, same Pattern 4 shape as recomputeOwnership) and immediately re-pushes `sendFrontendMessage('humbleKeysUpdated', getKeys())`. The backend cache and the live push were never the gap."
  timestamp: 2026-07-08 (round 7)

## Evidence

- timestamp: 2026-07-08
  checked: project context (Plan 14-06, RESEARCH A1-A3 confidence levels)
  found: the reveal/redeem HTTP contract (URL, form fields, response shape, CSRF requirement) is flagged LOW-MEDIUM confidence, cross-referenced from community tools but never called by this codebase; Plan 14-06 Task 2 (live validation with one disposable key) was never executed
  implication: prior probability strongly favors a contract mismatch (wrong endpoint/fields/CSRF) over a UI-side bug; the failing request's actual status code + response body is the highest-value evidence to capture first

- timestamp: 2026-07-08
  checked: src/backend/humble/adapter.ts (revealKey, humblePostRequest, mapAxiosError), src/backend/humble/constants.ts (HUMBLE_REDEEM_PATH), 14-RESEARCH.md "Reveal Endpoint Contract" + "Pitfall A"
  found: revealKey() POSTs to /humbler/redeemkey with form fields keytype/key/keyindex exactly matching the two independently cross-verified community contracts; csrfToken is attached to the 'csrf-prevention-token' header only when truthy (never required at type level); RESEARCH.md explicitly flags CSRF as the highest-probability first-attempt failure mode because mapAxiosError turns any 403 into access_denied, masquerading as a rate-limit cooldown rather than a distinct auth problem
  implication: the reveal contract itself (URL/fields/response shape) is well-corroborated — the more probable failure is an auth/header gap (missing CSRF token) rather than a wrong-contract guess

- timestamp: 2026-07-08
  checked: src/backend/humble/user.ts (getCsrfToken, finishLogin), src/backend/humble/library.ts:912 (revealKey csrfToken call site)
  found: getCsrfToken() is a pure configStore('csrfToken') read with no fallback; the ONLY place 'csrfToken' is ever written is finishLogin() (lines 405-426), which runs exclusively during an active login/reconnect flow — never for an already-established session (e.g. checkHealthAndFlagExpiry, which runs at every app startup for logged-in users, never touches csrfToken)
  implication: any account connected before this Phase 14 capture code existed will have getCsrfToken() permanently return undefined until the user explicitly disconnects/reconnects — a silent, self-inflicted gap independent of whether Humble's endpoint actually requires the header

- timestamp: 2026-07-08
  checked: live inspection of this machine's actual GameLib data — ~/Library/Application Support/gamelib/humble_store/config.json (mtime Jul 6 06:52, predates today's Phase 14 work) and ~/Library/Application Support/gamelib/Partitions/humble/Cookies (sqlite3 query on the persisted Chromium partition)
  found: config.json keys are exactly [encryptionDegraded, sessionCookie, isLoggedIn, expired] — no 'csrfToken' key exists for this account. The Cookies sqlite database DOES contain a live '.humblebundle.com|csrf_cookie' row (alongside _simpleauth_sess and other humblebundle.com cookies) — the value the reveal call needs is sitting in the partition, just never copied into configStore
  implication: direct, unambiguous confirmation (not inference) that THIS reproduction account has no cached csrfToken and would send every reveal POST without the CSRF header — matches the exact account/session used in the reported reproduction ("First reveal attempt ever through GameLib", account connected since Phase 10 on 2026-07-05, three days before Phase 14's csrf capture code was written today)

- timestamp: 2026-07-08 (round 2, post human-verify checkpoint)
  checked: user's checkpoint response — live main-process gamelib.log pasted verbatim, covering a Humble Disconnect+Reconnect performed WITHOUT restarting the app, followed by a manual reveal retry
  found: (1) a stale D-33/D-79 cooldown ("denial cooldown active until 2026-07-08T08:17:01.498Z") was logged at app-startup-time sync, predating the reconnect; (2) the reconnect's own login-validation loop logged 25x "session_expired" rejections over ~2min then succeeded; (3) the ensuing sync was CLEAN (denied=0, schema_error=0, ok=25/25) — per library.ts:819-833 this unconditionally overwrites cooldownUntil to `undefined` (sawAccessDenied=false), so the stale cooldown was almost certainly cleared by the time of the retry, though not independently re-verified on disk; (4) an unrelated caught AxiosError 404 (identity fetch, known D-13 behavior) logged during finishLogin, plus a SEPARATE uncaught 404 (UnhandledPromiseRejectionWarning) ~85s earlier at raw app startup whose call site could not be identified by static analysis (every Humble adapter call site that can 404 — getAccountIdentity via finishLogin/checkHealthAndFlagExpiry/validation.ts's D-12 dev-only trigger — is either try/caught or reached only via ipcMain.handle, which Electron auto-forwards rejections from rather than raising a main-process-level unhandled rejection; the dev-only humbleRunValidation trigger at main.ts:886 is the one exception with an un-caught `await getAccountIdentity` at validation.ts:54, but nothing calls it automatically at startup — left as an open, lower-priority thread, not yet explained); (5) ZERO log lines reference "reveal" anywhere after the sync finished, even though the user retried and got the same UI text
  implication: with the code AS IT WAS during this round (no status-only logging yet), absence of reveal logs could mean ANY of: local cooldown short-circuit (silent), local ineligible short-circuit (silent), OR a live 401/403/429 denial (mapAxiosError is silent for those too) — genuinely indistinguishable. Also discovered via code reading: HumbleUser.checkHealthAndFlagExpiry() (which carries the actual csrfToken backfill fix from round 1) is invoked ONLY from GlobalState's React mount effect (frontend/state/GlobalState.tsx:1130-1131) — i.e. app STARTUP, gated on isLoggedIn at mount. A runtime Disconnect+Reconnect (IPC humbleReconnect -> HumbleUser.reconnect() -> watchForLogin()/finishLogin()) NEVER calls checkHealthAndFlagExpiry. So round 1's fix was NEVER exercised by this test — the observed csrfToken presence is fully explained by finishLogin()'s own PRE-EXISTING (pre-dating this debug session) csrf_cookie capture, not the new backfill.

- timestamp: 2026-07-08 (round 2)
  checked: src/frontend/screens/Humble/Keys/components/HumbleClaimWizard/index.tsx (full component — step switch, all 6 step render branches)
  found: 'failed' step text ("Couldn't reveal this key — nothing was used up. You can try again.") is LITERALLY DISTINCT from 'cooldown' step text ("Temporarily unavailable — retry in {{N}}m") and from 'ambiguous'/'c2Block' step text. The switch in handleReveal() maps backend outcome.status: 'cooldown' -> step 'cooldown' (distinct UI); 'failed' | 'ineligible' | default -> step 'failed' (generic UI, matches user's reported text in BOTH the original bug report and this round's retry)
  implication: the user's reported text is strong (though not 100%-certain, since it's a paraphrase) evidence that outcome.status was NOT 'cooldown' — narrowing the live candidates to 'ineligible' (target/state mismatch, or unresolved keyindex) or 'failed' (no credentials, or a real adapter call returning schema_error/access_denied/session_expired). All of these produce identical, silent (no log line), identical-UI-text outcomes in the pre-round-2 code — hence the instrumentation fix below.

- timestamp: 2026-07-08 (round 3, pre-checkpoint-completion — new evidence arrived)
  checked: user's partial checkpoint response — retried reveal well after the 08:17:01.498Z cooldown expiry (~08:30Z+), same UI failure text, no new log lines yet (restart/build-currency not confirmed)
  found: coordinator raised three specific code questions to verify: (1) whether a locally-suppressed (non-network) failure re-arms/extends the shared D-33/D-79 cooldown, (2) whether HumbleUser.getCsrfToken() decrypts before library.ts attaches it to the adapter call (vs. sending an encrypted blob as the header), (3) whether the csrf header value/name matches the adapter's expectation
  implication: motivated a full re-read of library.ts's revealKey(), user.ts's getCsrfToken()/encryptCookie/decryptCookie, and adapter.ts's humblePostRequest/buildHeaders

- timestamp: 2026-07-08 (round 3)
  checked: src/backend/humble/library.ts:910-926 (cooldown gate) and :1012-1019 (access_denied branch)
  found: cooldownUntil is set ONLY on a live access_denied response from the adapter (result.status === 'access_denied', line 1012) — NEVER on a local/silent short-circuit (ineligible, keyindex-unresolved, no-credentials all return early with no cooldown write). The cooldown gate itself (line 911) is checked BEFORE the adapter call and, if active, short-circuits to `{ status: 'cooldown' }` — a DISTINCT wizard UI text ("Temporarily unavailable — retry in Nm") from the reported "couldn't reveal this key" text. Each live access_denied DOES re-arm a fresh 15-minute cooldown (Date.now() + HUMBLE_COOLDOWN_MS), so repeated live 403s would keep extending the window — but since the user's reported text matches 'failed', not 'cooldown', the reported retry (at 08:30Z+) did NOT hit this local short-circuit; it necessarily reached either the adapter (got a live definitive failure) or an earlier silent 'ineligible' branch.
  implication: rules out "still locally suppressed by an extended cooldown" as an explanation for the reported UI text — narrows remaining candidates to a live adapter failure (access_denied/session_expired/schema_error) or a silent ineligible/keyindex-unresolved gate, exactly as round 2's still-unexercised instrumentation is designed to distinguish

- timestamp: 2026-07-08 (round 3)
  checked: src/backend/humble/user.ts:186-193 (getCsrfToken), :107-146 (encryptCookie/decryptCookie), library.ts:950 (csrfToken call site), adapter.ts:195-223 (humblePostRequest, pre-fix)
  found: getCsrfToken() calls decryptCookie(stored) and returns the DECRYPTED plaintext value (or undefined) — never the encrypted `humble:v1:<base64>` blob. library.ts passes this decrypted value straight through to adapterRevealKey as csrfToken with no further transformation. humblePostRequest (pre-fix) attached it ONLY as the 'csrf-prevention-token' header value — correct name, correct decrypted value. However: the Cookie header sent alongside it was hardcoded to `_simpleauth_sess=${cookie}` ONLY — csrf_cookie was never included as an actual cookie on the request, despite 14-RESEARCH.md's only proven-working reference implementation (FailSpy, Selenium/real-browser) relying on the browser's automatic cookie-jar behavior to send csrf_cookie as a cookie IN ADDITION to reading it for a header. axios has no such automatic behavior for a partition session.
  implication: (a) decrypt-before-attach and header name/value format are CONFIRMED CORRECT — no defect there, the coordinator's encrypted-blob-as-header concern is eliminated; (b) a genuine, previously-unidentified contract gap WAS found: if Humble's CSRF check is a double-submit-cookie scheme, sending the header without the matching cookie is functionally equivalent to sending no CSRF token at all — this fully explains a persistent 403 that survives cooldown expiry and repeated retries (a structural request defect, not a rate limit)

- timestamp: 2026-07-08 (round 3)
  checked: src/backend/humble/__tests__/adapter.test.ts lines 634-651 (pre-fix)
  found: the existing "omits csrf header when csrfToken is undefined" test explicitly asserted `Cookie` header equals `_simpleauth_sess=${COOKIE}` even with no token — correct for that case — but the very next test ("sends csrf-prevention-token header when a csrfToken is provided") only checked the separate header value and never re-asserted the Cookie header, meaning the suite had silently locked in "Cookie never carries csrf_cookie" as accepted behavior without anyone deciding that was correct
  implication: confirms this is a genuine, previously untested gap (not a deliberate, verified design decision) — safe and appropriate to fix

- timestamp: 2026-07-08 (round 4)
  checked: coordinator's checkpoint response ("nope not working, also banner say couldn't refresh showing") against frontend/screens/Humble/Keys/index.tsx (banner render logic), library.ts (every setSyncState({syncError:'denied'}) call site, grep-confirmed exhaustive), frontend/state/GlobalState.tsx:1130-1131 (mount-time effect chain)
  found: (1) index.tsx:125/181-190 — the "Couldn't refresh — showing data from {{time}}" banner is a pure render of `humble.syncError !== 'none'`; it says nothing about WHICH backend call set that state. (2) Exactly two call sites ever write `syncError:'denied'` into humbleSyncStore: library.ts:681-693 (sync()'s own getGamekeys() call, a plain unauthenticated-by-CSRF GET) and library.ts:1012-1019 (revealKey()'s adapter call, the one round 3 patched). Both push the IDENTICAL state shape via emitSyncState()/humbleSyncStateChanged, so the renderer cannot and does not distinguish them. (3) GlobalState.tsx:1130-1131 — `humbleCheckHealth().then(() => humbleSync())` fires unconditionally on every mount while isLoggedIn, meaning the FULL RESTART round 3's own checkpoint instructed the user to perform necessarily triggers a real, independent sync() attempt before any reveal click. (4) Round 2's evidence already showed a clean 25/25 getGamekeys sync on this exact account ~08:17Z the same day — a same-day regression to a plain-GET 403 (if that's what happened) would point to an account/IP-level throttle unrelated to CSRF/reveal at all.
  implication: the banner text ALONE cannot be attributed to the reveal call — it is equally consistent with the mandatory startup sync independently hitting access_denied. This is a genuine confound the coordinator's own hypothesis (banner as downstream proof of a live reveal 403) did not fully account for; resolving it requires the specific gamelib.log line ('Humble sync: getGamekeys access_denied' vs 'Humble reveal: definitive failure, status=access_denied'), not just the presence of a banner.

- timestamp: 2026-07-08 (round 4)
  checked: FailSpy/humble-steam-key-redeemer ACTUAL source at github.com/FailSpy/humble-steam-key-redeemer/blob/main/humblesteamkeysredeemer.py (fetched verbatim, not RESEARCH.md's paraphrase) — the `perform_post`/`fetch_cmd` functions (lines ~104-131) that construct the real, historically-working `/humbler/redeemkey` POST
  found: the ACTUAL redeem call is executed via `driver.execute_async_script()` — i.e. a real `fetch()` call running INSIDE the authenticated humblebundle.com page context via Selenium, not a raw HTTP client call. Its body is a `FormData` object (multipart/form-data, browser-assigned boundary), NOT `application/x-www-form-urlencoded`. Its ONLY explicit header is `csrf-prevention-token` — no `Content-Type` override, no `X-Requested-By` at all for this call (the module-level `headers` dict citing `Content-Type: application/x-www-form-urlencoded` that 14-RESEARCH.md quotes is a DEAD/UNUSED variable in the actual script — grep-confirmed it is referenced nowhere except its own definition). Because the fetch executes same-origin inside the real page, the browser auto-attaches EVERY humblebundle.com cookie (not just csrf_cookie) per default `fetch()` credentials behavior — this is consistent with round 3's own reasoning but reveals it was even more literal than assumed: it's not just csrf_cookie the browser adds, it's the WHOLE cookie jar. The second reference (GreasyFork userscript) also executes same-origin in-page, sends NO csrf header at all, and uses `application/x-www-form-urlencoded` for its body — matching GameLib's current body encoding exactly.
  implication: GameLib's body encoding (urlencoded) matches the userscript reference, so that is NOT a likely defect. However neither real reference sends `X-Requested-By: hb_android_app` on the redeemkey call specifically — GameLib always does (HUMBLE_REQUIRED_HEADERS is spread into every humblePostRequest call unconditionally) — an untested, low-but-nonzero-confidence possibility that identifying as the Android app on a same-origin-only, cookie-scoped write endpoint could route into a different auth/CSRF code path server-side than either browser reference exercises. More significant: BOTH real references rely on the browser sending the FULL cookie jar, not a hand-picked subset — GameLib (even after round 3) only ever constructs `_simpleauth_sess` + `csrf_cookie`, never the rest.

- timestamp: 2026-07-08 (round 4)
  checked: live sqlite3 query against this machine's actual `~/Library/Application Support/gamelib/Partitions/humble/Cookies` (same partition round 3 already inspected for csrf_cookie's presence) — `SELECT host_key, name, is_httponly, is_secure FROM cookies WHERE host_key LIKE '%humblebundle%'`
  found: 27 distinct cookie rows exist for humblebundle.com/www.humblebundle.com beyond just `_simpleauth_sess` and `csrf_cookie` — including `__cf_bm` (Cloudflare Bot Management, is_httponly=1), `usi_found_user`, `usi_id`, `v2_humblebundlelive`, `fu`, plus various analytics cookies (`_ga`, `_fbp`, `_clck`, `OptanonConsent`, etc). GameLib's Cookie header (both pre- and post-round-3) only ever constructs 2 of these 27+ values by hand; the rest are never read or sent, mirroring what a real browser's same-origin fetch would automatically include.
  implication: `__cf_bm`'s presence directly confirms Humble Bundle sits behind Cloudflare Bot Management for at least some paths. This is a genuine candidate explanation for a persistent, contract-shape-independent 403 on the write endpoint specifically — BUT it is significantly weakened by round 2's own evidence that axios GET calls (which also never send `__cf_bm` or any of these 27 cookies beyond `_simpleauth_sess`) succeeded cleanly (25/25 orders, denied=0) on this exact axios transport that same day. If Cloudflare Bot Management were blanket-blocking non-browser axios traffic to humblebundle.com, GETs would fail too. This narrows the live possibility to Humble/Cloudflare applying STRICTER, path-specific rules to `/humbler/*` (write/CSRF-protected paths) than to `/api/v1/*` (read paths) — plausible (Humble already treats `/humbler/redeemkey` differently by requiring CSRF at all, unlike any `/api/v1/*` endpoint) but NOT confirmable without a live request/response pair (e.g. a Cloudflare challenge page body, or a response header naming the block reason) — exactly the kind of artifact `describeSchemaFailure`'s `bodyIsString=true`/contentType logging (already in place) would surface IF the response reached JSON-parsing at all, but a raw 403 with no body never reaches that code path (mapAxiosError returns before describeSchemaFailure is called for non-2xx statuses).

- timestamp: 2026-07-08 (round 5, checkpoint response — DECISIVE)
  checked: user's round-5 checkpoint response — raw gamelib.log lines pasted verbatim after a fresh restart + reveal attempt, per the round-4 checkpoint's exact grep instructions
  found: "(20:58:24) [INFO]: Humble reveal: calling adapter, csrfTokenPresent=true bkV5bPwxyfKRHpAu valiant_resurrection_storefront_steam_P8mGk" immediately followed by "(20:58:24) [WARNING]: Humble reveal: definitive failure, status=access_denied bkV5bPwxyfKRHpAu valiant_resurrection_storefront_steam_P8mGk" — same gamekey/machineName pair, same timestamp, unambiguously the reveal-path branch (library.ts:1009), not the sync-path branch round 4 could not rule out.
  implication: resolves round 4's central open confound outright. Confirms: (1) build staleness is eliminated — round 2's instrumentation is live in the running binary; (2) csrfTokenPresent=true — round 1's backfill and round 3's Cookie-header fix ARE both active and correctly attaching the token; (3) the reveal POST reached Humble over the network and got a LIVE access_denied despite carrying the csrf-prevention-token header AND the csrf_cookie double-submit value. This falsifies the CSRF-missing chain (rounds 1+3) as a SOLE explanation (see Eliminated) and promotes round 4's two dormant contract-shape candidates (X-Requested-By, full cookie jar) plus a new structural HTTP-failure diagnostic to this round's fix.

- timestamp: 2026-07-08 (round 5)
  checked: src/backend/humble/adapter.ts (humblePostRequest, mapAxiosError, revealKey), src/backend/humble/user.ts (new getFullCookieHeader), src/backend/humble/library.ts (revealKey call site) — implemented and unit-tested this round's 3 fixes
  found: (1) humblePostRequest no longer spreads the full HUMBLE_REQUIRED_HEADERS (which includes X-Requested-By: hb_android_app) onto the reveal POST — only Accept: application/json is kept; GET paths (buildHeaders) are completely untouched. (2) HumbleUser.getFullCookieHeader() reads the live persist:humble partition (session.fromPartition(HUMBLE_LOGIN_PARTITION).cookies.get({ url: HUMBLE_BASE_URL }), no name filter) and joins every cookie into a single header string; when it succeeds, this value supersedes the hand-built 2-3-cookie Cookie header entirely in humblePostRequest (opt-in via a new optional 4th param threaded through revealKey — undefined is a pure no-op, exact prior behavior). (3) mapAxiosError gained an opt-in `context` param; revealKey is the only call site that passes one ('reveal') — on any caught HTTP error it now logs status/content-type/bodyIsString/looksLikeHtml/bodyLength (never body content) via a new summarizeErrorBody helper mirroring describeSchemaFailure's redaction discipline exactly.
  implication: all 3 fixes are additive/opt-in with zero regression risk to the already-confirmed-correct CSRF attachment (rounds 1+3) or the already-working GET sync path. 13 new regression tests added (393/393 humble suite passing), tsc clean, eslint clean.

- timestamp: 2026-07-08 (round 6, checkpoint response — DECISIVE)
  checked: user's round-6 checkpoint response — raw gamelib.log lines pasted verbatim after a fresh restart + reveal attempt on the round-5 build, per the round-5 checkpoint's exact grep instructions
  found: "(21:14:35) [INFO]: Humble reveal: calling adapter, csrfTokenPresent=true fullCookieJarPresent=true" immediately followed by "(21:14:35) [WARNING]: Humble reveal: definitive failure, status=access_denied" immediately followed by "(21:14:35) [WARNING]: Humble adapter: reveal HTTP failure diagnostic status=403 contentType=text/html; charset=UTF-8 bodyIsString=true looksLikeHtml=true bodyLength=4552" — same gamekey/machineName pair, same timestamp, unambiguously the reveal path with all three round-5 fixes confirmed live and active.
  implication: resolves round 5's central open question outright. looksLikeHtml=true + contentType=text/html + a ~4.5KB body on a 403 is the canonical signature of a Cloudflare Bot-Management/WAF challenge PAGE, not a Humble JSON denial (RevealResponseSchema is JSON-only; both cross-verified reference implementations' redeem responses are always JSON). This eliminates request CONTENT (headers/cookies/CSRF, all proven live-correct this round and every round back to round 3) as the remaining gap — the block happens at the Cloudflare/WAF layer, before Humble's own application code ever inspects the request. Reframes the investigation toward TRANSPORT FINGERPRINT (axios's non-browser TLS/HTTP stack) as the leading, and only remaining untested, candidate.

- timestamp: 2026-07-08 (round 6)
  checked: src/backend/humble/adapter.ts (humblePostRequest rewritten, mapAxiosError generalized via new extractHttpErrorResponse + HumbleTransportHttpError), src/backend/humble/userAgent.ts (new, hoisted out of user.ts), src/backend/humble/user.ts (getFullCookieHeader removed), src/backend/humble/library.ts (revealKey call site simplified) — implemented and unit-tested this round's transport swap
  found: (1) humblePostRequest no longer calls axios.post at all — it issues the reveal POST via Electron's `net.request({ method: 'POST', url, partition: 'persist:humble', credentials: 'include', useSessionCookies: true })`, which resolves a response object regardless of status code (unlike axios, which throws on non-2xx) — non-2xx responses are converted into a new `HumbleTransportHttpError` (`.response.{status,headers,data}`) so mapAxiosError can pattern-match it alongside axios's own AxiosError via a new `extractHttpErrorResponse` helper; GET paths (humbleRequest, still axios) are byte-for-byte unchanged. (2) No manual Cookie header is set on this transport at all — `useSessionCookies`/`credentials: 'include'` make Chromium attach the live partition's cookie jar natively, which is why round 5's HumbleUser.getFullCookieHeader (a hand-built mirror of the same idea) is now redundant and was removed rather than left as unused/dead code. (3) `csrf-prevention-token` remains a manually-set header (unaffected — round 1/3's fix, live-confirmed correct by round 5's own csrfTokenPresent=true evidence). (4) A matching Chrome-shaped User-Agent (`standardBrowserUserAgent()`, hoisted out of user.ts into a new userAgent.ts module to avoid a circular import between adapter.ts and user.ts) is now sent on the reveal POST, consistent with the login window/partition's own UA. (5) A manually-armed setTimeout (net.request has no built-in timeout option, unlike axios's `timeout` config) aborts+rejects a stalled connection after 15s, preserving WR-04's hung-transport protection.
  implication: the transport swap is scoped to exactly the one endpoint that has ever failed (the reveal POST) — GET paths, the sync flow, and every other Humble adapter call are untouched and carry zero regression risk. 384/384 humble suite passing (adjusted from 393: round 5's 4 fullCookieHeader-specific tests were removed with the mechanism; new coverage added for the electron-net transport's options/headers/native-cookie-attachment, the HumbleTransportHttpError status mapping, and the manually-armed timeout/abort path), tsc clean, eslint clean (0 errors).

- timestamp: 2026-07-08 21:40:49 (round 7, checkpoint response — DECISIVE, final)
  checked: user's round-7 checkpoint response — live gamelib.log line pasted verbatim after this round's electron-net-transport build, plus a verbal confirmation ("it worked, finally!") that the revealed key was then successfully redeemed on Steam via the in-wizard "Open Steam" link
  found: "(21:40:49) [INFO]: [Backend]: Humble reveal: calling adapter (electron-net transport), csrfTokenPresent=true bkV5bPwxyfKRHpAu valiant_resurrection_storefront_steam_P8mGk" with NO subsequent "definitive failure"/"HTTP failure diagnostic" line — the clean-success shape round 6's falsification_test defined as CONFIRMING the transport-fingerprint hypothesis. This is the reveal-path log line (library.ts:972-980), unambiguous per every prior round's same discriminator.
  implication: root cause CONFIRMED — Cloudflare Bot Management's HTML-challenge block on the reveal POST was a genuine TLS/HTTP transport-fingerprint gate, not a request-content defect; routing the POST through Electron's own Chromium network stack (net.request on persist:humble) closes it. All content-layer fixes from rounds 1/3/5 (CSRF header, csrf_cookie double-submit, dropped X-Requested-By, structural diagnostic) remain necessary/correct — they were prerequisites the request had to satisfy before the transport swap could matter — but the transport was the decisive, final gap. Six rounds of investigation conclude here.

- timestamp: 2026-07-08 (round 7)
  checked: user's residual report (verbatim, DATA_START/DATA_END): after the successful reveal+redeem, the same row still showed the "Claim" button and stayed in the Keys-waiting count. Cross-referenced against src/backend/humble/library.ts (revealKey success branch, patchCachedState), src/frontend/state/GlobalState.tsx (handleHumbleKeysUpdated listener), src/frontend/screens/Humble/Keys/Waiting/index.tsx (annotations fetch), src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx (claimAction render branch), src/common/humble/viewFilters.ts (selectKeysWaiting/WAITING_STATES)
  found: (1) the backend cache patch + humbleKeysUpdated push were already correct (see Eliminated, round 7); (2) GlobalState.tsx's handleHumbleKeysUpdated listener already updates humble.keys in React state on every push; (3) the ACTUAL gap: Waiting/index.tsx fetches its own separate `annotations` map (revealedAt/redeemedAt per key, driving HumbleKeyRow's claimAction Claim-vs-Finish-activation-vs-Redeemed branch) via a `useEffect(..., [])` that runs exactly once at mount and is never re-invoked by any subsequent claim-flow action — unlike the sibling Spares tab's giftedMap, which openGiftDialog updates optimistically in local state the moment its own action fires. HumbleKeyRow itself was already correctly written (revealedAt !== null renders "Finish activation", not "Claim") — it was simply being fed permanently-stale annotation data. (4) Separately: the "still counted as waiting" half of the report is NOT a bug — WAITING_STATES (viewFilters.ts) deliberately includes REVEALED alongside UNREVEALED/UNPICKED (D-53) so a revealed-but-not-yet-redeemed key is never silently dropped from view; this is existing, documented, intentional behavior and was left unchanged.
  implication: fixed by adding a `refreshAnnotations()` function to Waiting/index.tsx, invoked from `closeWizard()` (covers every wizard exit path: successful reveal, mark-redeemed, sync-now, dismiss) and from the standalone `onUndoRedeem` handler after its IPC call resolves — mirrors Spares/index.tsx's existing optimistic-refresh pattern for the exact same class of state (a local per-row annotation cache sitting alongside the authoritative `humble.keys` push). No backend change required. New regression suite `src/frontend/screens/Humble/Keys/Waiting/__tests__/index.test.tsx` (3 tests) locks this in. Full repo-wide `npx jest` — 706/706 passing. `npx tsc --noEmit` clean. `npx eslint` on changed files — 0 errors, 0 warnings.

## Resolution

root_cause: CONFIRMED (round 7, final). The reveal POST to `/humbler/redeemkey` was being blocked by Cloudflare Bot Management's HTML-challenge WAF layer (status=403, contentType=text/html, looksLikeHtml=true, ~4.5KB body) BEFORE Humble's own application code ever inspected the request — a transport-fingerprint mismatch, not a request-content defect. axios's plain Node.js HTTPS stack (TLS/JA3 fingerprint, HTTP/1.1 negotiation, header casing/order) is distinguishable from a real Chromium tab, and Cloudflare gated this one write endpoint on that basis while leaving the read endpoints (`/api/v1/*`, also axios) untouched. The content-layer fixes from rounds 1/3/5 (csrfToken backfill, csrf-prevention-token header, csrf_cookie double-submit, dropped X-Requested-By, full/native cookie attachment) were all confirmed live-correct across rounds 3-6 and were NECESSARY — the request would have failed Humble's own CSRF check without them — but they were not SUFFICIENT; the transport swap in round 6 was the decisive fix. Confirmed live in round 7: gamelib.log at 21:40:49 shows the reveal succeeding cleanly via `net.request` (electron-net transport), with no failure/diagnostic line, followed by a successful Steam redemption. A separate, smaller frontend-only bug was also found and fixed this round (see fix below) — it was never part of the reveal's root cause, only a display-staleness defect surfaced during the same live verification pass.
fix: (round 1) Self-healing csrf_cookie backfill inside HumbleUser.checkHealthAndFlagExpiry(). (round 2) Self-diagnosing status-only logging on every previously-silent branch of HumbleLibrary.revealKey(). (round 3) csrf_cookie double-submit value attached alongside the csrf-prevention-token header (the header itself survives into round 6's transport, though the hand-built Cookie-header construction was superseded). (round 4) No code change — code-only FailSpy-source re-validation that produced round 5's evidence base. (round 5) Dropped X-Requested-By from the reveal POST; added HumbleUser.getFullCookieHeader() (later removed, round 6) plus a structural (redacted) HTTP-failure diagnostic that discriminated the Cloudflare HTML challenge from a genuine Humble JSON denial — the decisive diagnostic input to round 6's hypothesis. (round 6, THE DECISIVE FIX) Swapped humblePostRequest's transport from axios to Electron's own `net.request` on the `persist:humble` partition (`credentials: 'include'`, `useSessionCookies: true` for native cookie attachment, a matching Chrome-shaped User-Agent via new userAgent.ts) — a genuine Chromium TLS/HTTP2 fingerprint indistinguishable from a real browser tab; removed the now-redundant HumbleUser.getFullCookieHeader; generalized mapAxiosError via a new `extractHttpErrorResponse`/`HumbleTransportHttpError` so both transports (GET paths remain axios, unchanged) funnel through the same 401/403/429 mapping and diagnostic logging. (round 7) Frontend-only follow-up fix for a residual UI-state staleness bug found during live verification: `src/frontend/screens/Humble/Keys/Waiting/index.tsx`'s per-row `annotations` map (revealedAt/redeemedAt, driving the Claim-vs-Finish-activation-vs-Redeemed row control) was fetched once at mount and never refreshed, even though the backend's `humbleKeysUpdated` push (from `patchCachedState`, already correct pre-round-7) updated `humble.keys` live — the row kept rendering the stale "Claim" button after a successful reveal. Fixed by adding a `refreshAnnotations()` call after every wizard-close (covers reveal + mark-redeemed exit paths) and after the standalone undo-redeem IPC call resolves, mirroring the existing optimistic-refresh pattern already used by the sibling Spares tab's `giftedMap`. No backend change was needed for this part — the backend cache/push were already correct.
verification: (rounds 1-6, backend) `npx tsc --noEmit` clean; `npx jest src/backend/humble` — 384/384 passing; `npx eslint` clean (0 errors). (round 7, live) User's checkpoint response: gamelib.log line "(21:40:49) [INFO]: [Backend]: Humble reveal: calling adapter (electron-net transport), csrfTokenPresent=true" with no subsequent failure line, plus verbal confirmation the key was revealed and successfully redeemed on Steam — the exact success signature round 6's falsification_test defined as CONFIRMING the transport-fingerprint hypothesis. (round 7, UI fix) New test file `src/frontend/screens/Humble/Keys/Waiting/__tests__/index.test.tsx` (3 tests: mount fetch, wizard-close refetch, undo-redeem refetch) — all passing. Full repo-wide `npx jest` — 706/706 passing (703 pre-round-7 + 3 new). `npx tsc --noEmit` clean. `npx eslint` on all round-7-changed files — 0 errors, 0 warnings.
files_changed:
  - src/backend/humble/adapter.ts (round 6)
  - src/backend/humble/__tests__/adapter.test.ts (round 6)
  - src/backend/humble/user.ts (round 6)
  - src/backend/humble/__tests__/user.test.ts (round 6)
  - src/backend/humble/userAgent.ts (new, round 6)
  - src/backend/humble/library.ts (rounds 2/3/6)
  - src/backend/humble/__tests__/library.test.ts (rounds 2/3/6)
  - src/frontend/screens/Humble/Keys/Waiting/index.tsx (round 7)
  - src/frontend/screens/Humble/Keys/Waiting/__tests__/index.test.tsx (new, round 7)

root_cause (superseded, kept for history): STILL UNCONFIRMED (round 5) — round 5's checkpoint response DEFINITIVELY falsified the CSRF-missing chain (rounds 1+3) as a sole explanation (csrfTokenPresent=true, still a live access_denied — see Eliminated). This is significant forward progress: build staleness, sync-path confound, and CSRF-attachment correctness are now all conclusively ruled out or confirmed-correct. The remaining live candidates are contract-shape/request-fingerprint mismatches carried over from round 4's FailSpy-source re-read: (a) X-Requested-By: hb_android_app sent on the reveal POST when neither working reference sends it; (b) only 2-3 of 27+ live cookies sent, vs both references' full same-origin browser jar; (c) access_denied potentially conflating a genuine Humble denial with an undetected Cloudflare Bot Management challenge page. All 3 are now fixed/instrumented this round (see fix below) but NONE has live confirmation yet.
fix: (round 1, unchanged) Added a self-healing csrf_cookie backfill inside HumbleUser.checkHealthAndFlagExpiry(). (round 2, unchanged) Added self-diagnosing status-only logging to every previously-silent branch of HumbleLibrary.revealKey(). (round 3, unchanged, NOW LIVE-CONFIRMED CORRECT) src/backend/humble/adapter.ts: humblePostRequest attaches `csrf_cookie=<value>` to the Cookie header alongside `_simpleauth_sess=<value>` whenever a csrfToken is present. (round 4, unchanged) NO CODE CHANGE — code-only re-validation, produced the FailSpy-source evidence this round's fixes are built on. (round 5, THIS round) 3 fixes: humblePostRequest no longer sends X-Requested-By on the reveal POST (GET paths untouched); HumbleUser.getFullCookieHeader() reads the live persist:humble partition's full cookie jar and, when successful, supersedes the hand-built Cookie header for the reveal POST only (opt-in, additive); mapAxiosError gained an opt-in structural (never body-content) HTTP-failure diagnostic, wired only into revealKey's catch, to discriminate a genuine Humble denial from a Cloudflare Bot Management challenge page on the next failure.
verification: `npx tsc --noEmit` clean. `npx jest src/backend/humble` — 393/393 (13 new regression tests this round: X-Requested-By omission, fullCookieHeader override + fallback, HTTP-failure diagnostic on HTML vs JSON error bodies, diagnostic opt-in-only confirmed absent on getGamekeys, getFullCookieHeader unit tests including non-fatal-failure/empty-jar/malformed-entry/never-logs-a-value, library.ts forwarding + logging of fullCookieJarPresent). `npx eslint` clean (fixed one unrelated pre-existing-pattern error — unnecessary type assertion — introduced while writing the new diagnostic helper). Live end-to-end verification STILL PENDING — requires a NEW human-verify round with the exact grep command below.
files_changed:
  - src/backend/humble/user.ts
  - src/backend/humble/__tests__/user.test.ts
  - src/backend/humble/library.ts
  - src/backend/humble/__tests__/library.test.ts
  - src/backend/humble/adapter.ts
  - src/backend/humble/__tests__/adapter.test.ts

root_cause (superseded, kept for history): NOT YET CONFIRMED. Round 1's csrfToken-backfill-gap hypothesis remains plausible but is neither confirmed nor eliminated — the human-verify round that was meant to test it (Disconnect+Reconnect) could not actually exercise the fix, because checkHealthAndFlagExpiry (which contains the backfill) only runs at app startup (GlobalState mount), never on a runtime reconnect. Separately, code reading revealed that library.ts's revealKey() has (had, as of round 1) THREE silent local short-circuit branches (cooldown, target-ineligible, keyindex-ineligible) plus a SILENT live-denial path (mapAxiosError never logs 401/403/429) — all four converge on the exact same generic wizard UI text the user reported both originally and in this retry, meaning the evidence gathered so far cannot discriminate between "CSRF still missing/still 403" and "reveal never even reached the network."
fix: (round 1, unchanged) Added a self-healing csrf_cookie backfill inside HumbleUser.checkHealthAndFlagExpiry(). (round 2, THIS round) Added self-diagnosing status-only logging (zero behavior change) to every previously-silent branch of HumbleLibrary.revealKey() in src/backend/humble/library.ts: target-not-found-or-wrong-state ineligible (logs resolved state), keyindex-unresolved ineligible, cooldown short-circuit (logs retry timestamp), no-stored-credentials failure, a csrfTokenPresent boolean logged immediately before every adapter call, and the definitive-failure branch (logs the exact result.status: schema_error/access_denied/session_expired — closing the exact same class of silent-abort gap that fetchAndCommitOrder's sync path was already fixed for, but revealKey() never received). This is instrumentation only — it does not change any outcome, cooldown, or eligibility logic — and is required before a confirmed root cause is possible, since 4 competing hypotheses currently produce identical symptoms.
verification: `npx tsc --noEmit` clean. `npx jest src/backend/humble` — 11 suites, 378 tests, all pass (10 new regression tests added this round, asserting each new log line fires on its corresponding branch, plus one negative test confirming the D-78 'ambiguous' path deliberately does NOT log a 'definitive failure' line). Round-1's 3 backfill regression tests still pass unchanged. Live end-to-end verification still pending — requires a NEW human-verify round with a full app restart (not just Disconnect+Reconnect) so checkHealthAndFlagExpiry's backfill actually runs, followed by a fresh reveal attempt whose new log output will name the exact branch that fires.
files_changed:
  - src/backend/humble/user.ts
  - src/backend/humble/__tests__/user.test.ts
  - src/backend/humble/library.ts
  - src/backend/humble/__tests__/library.test.ts
