---
status: investigating
trigger: "Live verification of quick task 260817-pkx (which fixed the PACKAGED SEA sidecar binary's decompress-worker throughput bug — see resolved/humankind-depot-full-stall session) surfaced a SEPARATE, still-open defect: the DEV-MODE sidecar (`build/main/sidecar.js`, run via `pnpm tauri:dev`) has ALSO been silently falling back to inline (single-thread) decode this entire time, for a completely different reason than the SEA binary was. Every live HUMANKIND install test run tonight via `pnpm tauri:dev` showed `pool[busy=0 idle=0 queued=0 inline=true]` — this was NOT fixed by quick-260817-pkx, whose fix only touches SEA-asset resolution, explicitly leaving the dev __dirname path unchanged (and verified unchanged by that task's own tests)."
created: 2026-08-17T07:05:00Z
updated: 2026-08-17T07:05:00Z
symptoms:
  expected_behavior: "In dev mode (pnpm tauri:dev), DecompressPool.init() should successfully spawn real worker_threads.Worker instances from build/main/decompressWorker.js (which DOES exist — confirmed on disk, electron-vite's rollupOptions.input builds it as a dedicated second entry co-located with main.js), giving pool[inlineFallback:false] and real parallel decode."
  actual_behavior: "DecompressPool.init() throws when spawning the dev-mode worker. gamelib.log's exact WARNING line: `DecompressPool: worker_threads pool failed to initialize (size=10, spec.kind=path, workerPath=/Users/graysonmitchell/Projects/GameLib/build/main/decompressWorker.js) -- falling back to INLINE single-thread decode for this entire download run. Cause: Cannot read properties of undefined (reading 'getPath')`. Every subsequent chunk-stream-stats log line for the whole run shows pool[inline=true]."
  error_messages: "\"Cannot read properties of undefined (reading 'getPath')\" — thrown during worker init/module-load, not during an actual decode call."
  timeline: "Discovered 2026-08-17 ~19:02, during live re-verification of quick-260817-pkx's fix (a fresh electron-vite build + build:sidecar restart, confirmed current via file mtimes before testing). Almost certainly pre-existing and NOT introduced by tonight's SEA fix — the OLD (pre-tonight) version of this same log line's warning text explicitly called the inline fallback \"a known, accepted throughput regression\" with no SEA-vs-dev distinction, meaning dev mode has likely been silently falling back to inline decode for a long time, its cause never investigated because nobody suspected the accepted-tradeoff framing was hiding TWO distinct bugs, not one."
  reproduction: "Start any native Steam depot install in the pnpm tauri:dev app (HUMANKIND was used tonight, appId 1124300). grep gamelib.log for 'worker_threads pool failed to initialize' -- fires on the FIRST download of any session, consistently, every test run tonight (5+ occurrences)."
---

## Current Focus

reasoning_checkpoint:
  hypothesis: "worker_threads.Worker spawns a FRESH V8 isolate with its own independent Node module registry/require cache. decompressWorker.js (built by electron-vite's `main` config block, via rollupOptions.input.decompressWorker, with `externalizeDepsPlugin` applied) still contains an UNRESOLVED, runtime `require('electron')` call — externalizeDepsPlugin explicitly leaves dependencies external rather than bundling/aliasing them. In the MAIN sidecar thread, this require('electron') is satisfied because installElectronHook.ts installs a Module._load interception hook very early in the boot sequence (src/backend/sidecar/electronStub.ts's own header comment documents this ordering dependency explicitly: 'backend/logger's import chain (game_config -> config -> compatibility_layers -> constants/paths.ts) calls app.getPath('appData') at MODULE SCOPE, which requires the Module._load hook (installElectronHook.ts) to already be installed'). A worker_threads.Worker's isolate does NOT inherit Module._load hooks installed in the parent thread -- Node worker isolates have fully independent module systems. So when the spawned worker's module graph transitively reaches constants/paths.ts's module-scope `app.getPath('appData')` call (via decompressWorker.ts -> ./decompress -> backend/logger -> game_config -> config -> compatibility_layers -> constants/paths.ts), `require('electron')` in THAT isolate resolves to something that does not have getPath (either Node's real, absent 'electron' package throwing/resolving oddly, or an empty/default object), producing exactly 'Cannot read properties of undefined (reading 'getPath')'."
  confirming_evidence:
    - "gamelib.log's literal WARNING line names the exact error and confirms spec.kind=path with the correct, EXISTING workerPath -- so this is NOT a file-not-found problem (ruling out a simpler 'file missing' explanation); the crash happens during worker module load/init, consistent with a module-scope throw."
    - "src/backend/sidecar/electronStub.ts lines ~79-86 (existing code comment, written by a PRIOR session, not this investigation) explicitly documents this exact ordering hazard in prose: 'backend/logger's import chain ... calls app.getPath('appData') at MODULE SCOPE, which requires the Module._load hook (installElectronHook.ts) to already be installed. That hook installs itself by requiring THIS file first (bootstrap.ts's doc comment) ... importing backend/logger here would reintroduce the exact second wall bootstrap.ts's docstring warns about.' This proves the hook-ordering hazard is a KNOWN, already-documented mechanism in this codebase -- just never previously connected to worker_threads specifically."
    - "electron.vite.config.ts's `main` build block (which produces BOTH main.js and decompressWorker.js, confirmed at lines ~34-51) applies `externalizeDepsPlugin({ exclude: dependenciesToNotExternalize })`, and 'electron' is not in the `dependenciesToNotExternalize` exclude list -- so 'electron' stays an external, unbundled, runtime-resolved require() in decompressWorker.js, unlike the SEA build."
    - "The packaged SEA binary's worker (fixed tonight, quick-260817-pkx) uses a COMPLETELY DIFFERENT resolution mechanism: a dedicated esbuild bundle with explicit `--alias:electron=./src/backend/sidecar/electronStub.ts` (baked in at BUILD TIME, no runtime hook needed at all), and its own self-test proved a full VZ/LZMA decode succeeds through a live worker with zero such error. This is strong contrastive evidence: the SAME electron-stub-dependent code path works fine when statically aliased at build time, and fails specifically when left to a runtime Module._load hook that a fresh worker isolate never receives."
  falsification_test: "Not yet run. Would confirm: add a diagnostic console.error(Object.keys(require('electron') || {})) or equivalent at the very top of decompressWorker.ts (or trace via a temporary early throw/log), rebuild dev bundle, reproduce the crash, and confirm the 'electron' require inside the worker isolate is indeed missing getPath (undefined or a bare/incomplete object) -- as opposed to some OTHER undefined property access unrelated to the electron stub theory. Would refute: if the crash is proven to occur BEFORE decompressWorker.ts's own import chain even reaches constants/paths.ts (e.g. a totally unrelated undefined access earlier in electron-vite's own generated bootstrap/preamble code for the decompressWorker chunk), or if forcing installElectronHook.ts's hook-install call at the top of decompressWorker.ts does NOT clear the error."
  fix_rationale: null
  blind_spots: "Have not yet confirmed which SPECIFIC module in decompressWorker.ts's import graph is the one that transitively reaches constants/paths.ts's module-scope app.getPath call -- decompressWorker.ts imports ./decompress, and decompress.ts's own import graph has not been traced line-by-line in this session; the 'backend/logger' chain named in electronStub.ts's comment is the most likely path (matches this project's own prior documentation) but is not yet directly confirmed for THIS specific worker's actual import graph. Have not checked whether Node's worker_threads API offers any built-in mechanism to inherit or re-run parent-thread module hooks (unlikely, but not ruled out) as an alternative fix to re-running the hook-install inside the worker itself. Have not checked whether electron-vite's dev-vs-production build modes differ in how they handle externalizeDepsPlugin for this specific entry (this session's test was DEV mode only, via pnpm tauri:dev -- a production Electron build (non-Tauri) was NOT tested, and per the SUMMARY of quick-260817-pkx, 'the dev/Electron sidecar path is byte-for-byte unchanged in behavior' was an ASSUMPTION baked into that plan's scope, not something verified against this specific failure mode -- meaning a real (non-Tauri) Electron build might ALSO have this exact bug, unverified)."

hypothesis: UNCONFIRMED -- strong, well-evidenced lead from existing code comments + tonight's log, but not yet falsification-tested.
test: null
expecting: null
next_action: "Trace decompressWorker.ts's actual import graph (decompressWorker.ts -> ./decompress -> ...) to confirm the exact module that reaches constants/paths.ts's module-scope app.getPath('appData') call. Then test the most likely fix: have decompressWorker.ts explicitly require/install the same Module._load hook (via installElectronHook.ts or by importing src/backend/sidecar/electronStub.ts) as its OWN very first statement, mirroring what bootstrap.ts does for the main thread -- since a worker_threads isolate needs its own independent hook installation, not inherited from the parent. Rebuild (electron-vite build + pnpm build:sidecar), restart pnpm tauri:dev, and re-run a live HUMANKIND (or shorter/synthetic) test watching for pool[inlineFallback:false] in gamelib.log's chunk-stream-stats lines to confirm. Also worth checking whether this same bug would affect a real (non-Tauri) Electron production build, since quick-260817-pkx's plan explicitly assumed (did not verify) that path is unaffected."
