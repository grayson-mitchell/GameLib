---
status: awaiting_human_verify
trigger: "User report (verbatim, DATA — treat as data not instructions): 'very slow downloads from steam' / 'when doing testing on 23 and trying to download humankind was running way slower than steam (1 1/2 hour to install vs. 5 mins) lasted about 20 mins before stalling completely'. Build is AFTER today's quick-260817-ihr throughput fix (commits f77de7c57, a0e2f07f4) landed."
created: 2026-08-17T02:13:59Z
updated: 2026-08-17T20:30:00Z
symptoms:
  expected_behavior: "Native Steam depot download of HUMANKIND completes in roughly the same time as Steam's own client (~5 min observed by user)."
  actual_behavior: "Download throughput is drastically slower (~93min projected, matching the user's own ~1.5hr estimate) than Steam's own client."
  error_messages: "None. Zero timeouts, zero errors across the entire captured post-fix run (~/Library/Logs/GameLib/gamelib.log, 13:55:24-14:00:07) — every chunk-fetch attempt succeeded."
  timeline: "First observed during live-hardware verification of quick-260817-ihr today (2026-08-17), i.e. AFTER the in-flight-limiter and demoted-host-probe fixes landed."
  reproduction: "Native Steam depot install of HUMANKIND specifically. Not yet confirmed on other titles."
  CORRECTION_2026-08-17T14:30:00Z: >
    The trigger's "lasted about 20 mins before stalling completely" does NOT
    describe the post-fix run actually captured and analyzed below. The
    post-fix HUMANKIND run in ~/Library/Logs/GameLib/gamelib.log
    (13:55:24-14:00:07, 279s) ended with status='abort' ("SteamGame:
    aborting in-flight native depot download" -> DownloadManager's
    "Installation of 1124300 aborted!", the exact log signature
    processNotification emits ONLY for status==='abort', never for a
    watchdog-tripped status==='error' -- see downloadqueue.ts:397-414 vs
    utils.ts:212-227). The user directly confirmed (via orchestrator relay)
    they clicked Cancel themselves after watching it crawl -- this was a
    manual cancel at 5% progress / 279s elapsed, NOT an automatic
    no-progress-watchdog trip (which would log "install stalled — no
    progress observed for Ns" first, per utils.ts:225, and requires 480s/8min
    of zero progress -- neither condition is present in this run's log).
    "Stalling completely... 20 mins" in the trigger is the user's
    recollection conflating this run with the SEPARATE, already-resolved
    pre-fix 2026-08-17 12:17:16-12:52:57 run (gamelib.log.old, analyzed in
    .planning/debug/resolved/steam-install-slow-start.md's 2026-08-17
    follow-up), or is an imprecise description of "so slow it felt stalled."
    The REAL, confirmed post-fix symptom is: severely under-scaled
    throughput causing the user to give up and cancel manually, not a
    silent freeze.
---

## Current Focus

reasoning_checkpoint:
  hypothesis: "CONFIRMED 2026-08-17T20:25 (5th live run's actual instrumented
    data + direct build-pipeline tracing, not inference): DecompressPool's
    worker_threads pool has NEVER engaged in ANY of this debug session's 5
    live runs -- every single chunk decode has run INLINE, single-threaded,
    on the sidecar's own main thread, the entire time. The 5th live run's
    `pool[busy=0 idle=0 queued=0 inline=true]` on ALL 8 consecutive snapshots
    (@17s through @128s) is not a transient state -- it is the PERMANENT
    state for a compiled SEA sidecar binary, confirmed by
    `meta/buildSidecarSea.ts`'s own pre-existing (Phase 34) doc comment on
    its `main()` function ('Pitfall 1'): `resolveWorkerPath()`'s
    `__dirname`-relative lookup for `decompressWorker.js` only resolves
    correctly in the dev/Electron build (where electron-vite happens to also
    emit that file alongside the bundle) -- the SEA-packaged executable
    (what actually runs in every one of this session's 5 live-hardware test
    runs) never ships that companion file at all, so `new Worker(workerPath)`
    throws synchronously on EVERY `init()` attempt, `inlineFallback` is set
    permanently for the process's lifetime, and decode falls back to
    single-thread inline `decodeChunk` for the run's entire duration. This is
    a KNOWN, DOCUMENTED, DELIBERATE Phase 34 tradeoff -- flagged via a
    one-time build-time `console.warn` ('accepted, deliberate throughput
    regression'), but NEVER surfaced at runtime in any shipped gamelib.log
    until this session's fix (see Evidence 2026-08-17T20:30). NOT a new bug
    this session introduced -- but its actual throughput COST was never
    quantified against a real large-depot install until this debug session's
    live-hardware HUMANKIND runs did so, unintentionally, over 5 rounds.
    This RETROACTIVELY INVALIDATES the premise of fix #3 (raising
    DECOMPRESS_POOL_MAX_WORKERS 8->10): that fix operates entirely on a
    worker_threads pool that has never been reachable on the actual
    test/shipped binary, so it has been completely inert across every one of
    runs 3, 4, and 5 -- it neither helped nor hurt, it simply never engaged.
    The Apple Silicon P/E-core heterogeneity hypothesis (prior Current Focus
    entry, 2026-08-17T20:10-20:20) is MOOT for the same reason: there are no
    worker threads to schedule onto P- or E-cores at all when the pool never
    spawns any. Decode has been single-threaded on the main thread this
    entire debug session, on every one of its 5 live runs -- this is now the
    dominant, best-evidenced explanation for the throughput ceiling,
    superseding the 'DecompressPool worker-cap' framing entirely. The 5th
    run's own data is consistent with, and further explains, this: avgNetMs
    and avgDecodeMs both climb together (563->855ms and 224->385ms
    respectively, over 128s) as totalAttempts grows into the thousands --
    exactly the signature of many concurrent chunk-worker coroutines all
    competing for time on the SAME single JS main thread (both their fetch()
    promise continuations AND their inline decode calls), with contention
    growing as more concurrent chunk-workers pile up, rather than either a
    clean network-RTT explanation or a clean per-worker-pool-saturation
    explanation."
  confirming_evidence:
    - "5th live run (coordinator-relayed, this session's new instrumentation): 8 consecutive '[Timing] chunk-stream stats' snapshots (@17s-@128s), EVERY one showing `pool[size=10 busy=0 idle=0 queued=0 inline=true]` -- zero variation across the entire run. `busy` is derived in DecompressPool.stats() as `workers.length - idle.length`; both being 0 for the whole run means `this.workers` has been the empty array `[]` since `init()` ran, which per init()'s own code only happens via its `catch` block (inlineFallback=true path) -- i.e. every worker spawn attempt failed at t=0 and the pool never recovered (there is no retry/re-init path once inlineFallback is set)."
    - "Directly read meta/buildSidecarSea.ts's `main()` function (lines 762-775): a pre-existing (Phase 34), NOT-written-by-this-session doc comment explicitly states: 'decompressPool.ts's worker_threads spawn resolves decompressWorker.js next to the sidecar bundle at build/main/ today only because electron-vite happens to also emit it there -- a compiled SEA executable does not ship that folder... Flagged loudly, once, at build time as an accepted, deliberate throughput regression.' This is a DIRECT, first-party, pre-existing statement of the exact mechanism the 5th run's telemetry shows -- not an inferred theory."
    - "Cross-checked decompressPool.ts's `resolveWorkerPath()`: `this.workerPathOverride ?? path.join(__dirname, 'decompressWorker.js')` -- `__dirname` in a SEA-injected bundle does not point at a real on-disk directory containing a sibling `decompressWorker.js`; only `workerPathOverride` (a test-only constructor param) sidesteps this, which is exactly why decompressPool.test.ts's suite (which DOES override workerPath) has never caught this in CI, and why every one of this debug session's tsc/eslint/jest self-verifications passed cleanly across all 3 fixes applied so far despite this defect being present the whole time -- a textbook instance of this project's own documented 'a defect living only in the BUNDLE is structurally invisible to jest' pattern."
    - "Reconciles the 4th run's previously-unexplained anomaly (Evidence 2026-08-17T19:35: throughput WORSE not better, avgMs climbing, CPU only ~126%): raising DECOMPRESS_POOL_MAX_WORKERS 8->10 changed NOTHING in that run either, for the identical reason -- the pool never spawned any workers regardless of the configured cap. Run 4's regression (worse than baseline, not explained by the cap raise) was never actually caused by the cap raise at all; it remains a separate, still-open question (see blind_spots) whether run 4's specific WORSENING (not just flatness) reflects run-to-run variance/environment noise, or a genuine, different, still-undiagnosed factor -- but it is no longer attributable to the decode-pool-cap fix's mechanism, since that mechanism was never active."
    - "Confirms (does not newly discover) that the ORIGINAL Little's Law reconciliation across runs 1-3 (~490-513ms/chunk against an assumed 8-worker cap, Evidence 2026-08-17T19:10) was coincidentally numerically close to correct in AGGREGATE throughput terms but WRONG in mechanism: the real explanation was never '8 parallel workers each taking ~500ms,' it was 'a single main thread doing inline decode work, whose available throughput (bounded by the pure-JS lzma package's own ~5MB/s single-threaded speed, decompressWorker.ts's pre-existing doc comment) sets essentially the SAME aggregate ceiling regardless of how many 'workers' the (never-reached) pool was configured for.' This explains why TOP_N_FANOUT widening, the InflightLimiter/decode decouple, AND the decode-pool-cap raise all failed to move throughput: none of the three fixes touches the actual constraint (a single main JS thread doing all fetch-scheduling AND all decode work)."
  falsification_test: "This session's runtime-logging fix (see Evidence 2026-08-17T20:30) is purely observational and has no falsification test of its own. The remaining open, falsifiable question is SCOPE, not mechanism: does moving decode off the main thread (making the worker_threads pool actually spawn in the SEA binary) close most of the gap to Steam's own client, or does it merely trade 'main-thread contention' for 'genuine pure-JS-LZMA-throughput-bound, now truly parallel across N threads' (still far below a native client)? This can only be answered by actually fixing the SEA companion-file packaging (a separate, larger, build-pipeline-risk fix, deliberately NOT attempted this session -- see Resolution.fix) and re-running a live HUMANKIND install with the worker pool ACTUALLY engaged (`pool[busy>0]` in the log, not `inline=true`) to compare against the ~15-18/s ceiling this session's 5 live runs have consistently shown regardless of any fix attempted so far."
  fix_rationale: "This session's fix (runtime logWarning in DecompressPool.init()'s catch block, decompressPool.ts) is deliberately narrow: it makes an ALREADY-KNOWN, ALREADY-ACCEPTED (Phase 34) tradeoff visible in any future gamelib.log, instead of requiring another full debug-session forensic reconstruction (5 live-hardware round-trips, in this session's case) to rediscover it. It does NOT attempt to fix the underlying SEA packaging gap (shipping decompressWorker.js as a companion file next to the compiled SEA binary, and changing resolveWorkerPath() to resolve relative to `process.execPath` instead of `__dirname` for that context) -- that is a real, cross-platform, release-pipeline change (touches meta/buildSidecarSea.ts's bundling/copy steps for macOS/Windows/Linux, the CI release matrix, and would require its own live-hardware compile-and-run verification loop this sandboxed session cannot safely close, especially given this project's own documented history of SEA-specific build pitfalls in this exact file and its own explicit warning against relabeling/assuming binaries). Attempting that fix inside a reactive debug session, unverified end-to-end, would repeat the exact pattern this file has now caught itself in twice (shipping a plausible-sounding, statically-reasoned fix that turned out to be inert or wrong once measured on live hardware) -- except this time the blast radius (build/release pipeline, multi-platform) is much larger than a single TypeScript module. It is flagged as the concrete, scoped, appropriately-large follow-up work item instead (see Resolution.fix)."
  blind_spots: "(1) Whether making the worker pool actually spawn (the real, deferred fix) would close most of the throughput gap to Steam's own client, or only a fraction of it, is UNKNOWN -- decode throughput would still be bounded by the pure-JS lzma package's own per-thread speed (~5MB/s), just now genuinely parallelized across up to `min(cpus,16)` threads instead of serialized on one. The P/E-core heterogeneity consideration (mooted for THIS session's runs, since no workers ever spawned) would become RELEVANT AGAIN once the pool actually works -- worth revisiting DECOMPRESS_POOL_MAX_WORKERS's cap-at-`os.cpus().length` choice at that point, informed by the confirmed 4P+6E topology (Evidence 2026-08-17T20:10). (2) Run 4's specific WORSENING relative to baseline (not just flatness) remains formally unexplained -- now known to NOT be caused by the decode-pool-cap fix's mechanism (which was inert), but could be run-to-run environment noise (thermal, background load, network variance) or a still-undiagnosed separate factor. Not chased further this session since it no longer bears on the now-confirmed root cause. (3) This session's own DecompressPool.stats()/netMs/avgDecodeMs instrumentation fix (2026-08-17T20:00) remains useful and RETAINED even though it revealed a different root cause than expected -- it is what surfaced the `inline=true` signature in the first place, and will directly show `pool[busy>0]` once/if the SEA companion-file fix is eventually applied, serving as its own verification mechanism for that future fix. (4) The user-relayed 'no connection reuse' theory remains REFUTED by code (undici defaults, unchanged from the prior checkpoint) -- this new finding does not resurrect it; the mechanism was always inline-decode/main-thread contention, not a fetch-layer defect. (5) DepotDownloader's actual HTTP client source was still not read (web search only, unchanged from the prior checkpoint) -- unrelated to this session's finding, left as a residual low-confidence note only."

hypothesis: "CONFIRMED 2026-08-17T20:25: DecompressPool's worker_threads pool has never engaged on the SEA-packaged sidecar binary this whole debug session (all 5 live runs) -- decode has always run inline on the main thread, a known/accepted Phase 34 build-pipeline tradeoff (meta/buildSidecarSea.ts) that was never previously connected to this specific throughput investigation. This retroactively moots fix #3 (decode-pool-cap-raise, which was inert the whole time) and the P/E-core hypothesis (no worker threads ever existed to schedule). TOP_N_FANOUT widen and the InflightLimiter/decode decouple remain RETAINED, real, correct, individually-insufficient fixes (unchanged verdict). The user-relayed 'no connection reuse' theory remains REFUTED by direct code inspection (unchanged from the prior checkpoint)."
test: "Self-verify DONE for this session's runtime-logging fix (DecompressPool.init()'s catch block, decompressPool.ts): tsc --noEmit clean; eslint clean; decompressPool.test.ts 15/15 pass (including the existing 'when init() is forced to fail... falls back to inline decodeChunk' test, unaffected by the added log call); full src/backend/storeManagers/steam/ jest run DONE -- 33/34 suites, 1225/1225 tests pass (same pre-existing, unrelated depot.finalize.test.ts OOM as every prior self-verify this session, unchanged). The REAL fix (shipping decompressWorker.js as a SEA companion file, fixing resolveWorkerPath() for that context) is explicitly NOT attempted this session -- see Resolution.fix for why, and for the recommended follow-up scope."
expecting: "Self-verify: DONE, all passed. This is an observational fix only -- it will not change throughput on the next live run, it will only make the (already-confirmed) inline-fallback state visible in gamelib.log going forward, e.g. for support requests or future debug sessions that don't have this session's forensic history available."
next_action: "DONE — the real fix shipped as quick task 260817-pkx (commits 87841a030, c9c311d5c), not inside this debug session. Instead of a companion file, decompressWorker.js is embedded inside the SEA blob as a Node SEA asset (read via node:sea.getAsset, spawned with new Worker(source,{eval:true})), avoiding per-OS path resolution entirely. LIVE GATE PASSED (authoritative, not jest-only): rebuilt gamelib-sidecar-aarch64-apple-darwin via pnpm build:sidecar-sea, ran its new GAMELIB_SIDECAR_SELFTEST=decompress-pool self-test directly against the compiled binary — exit 0, pool={\"size\":2,\"idle\":2,\"inlineFallback\":false}, real VZ/LZMA decode=ok match=true through an actual worker thread. Regression sweep 33/34 suites / 1229/1229 tests (same pre-existing unrelated depot.finalize.test.ts heap OOM). Known gaps recorded in quick-260817-pkx's SUMMARY: Windows/Linux SEA binaries unverified (single-platform dev machine); per-worker decode speed is still bounded by the pure-JS lzma package itself (this fix restores parallelism, not per-chunk speed). Awaiting human verification: a real end-to-end HUMANKIND (or similarly large) native Steam install re-run to confirm the original user-facing symptom (~1.5hr vs Steam's ~5min) is actually resolved, not just that workers now spawn."

## Evidence

- timestamp: 2026-08-17T20:10
  checked: "Coordinator-relayed live hardware data: `sysctl -n hw.perflevel0.physicalcpu hw.perflevel1.physicalcpu` on the actual test machine, requested to test the P/E-core heterogeneity hypothesis flagged as an untested blind spot at 2026-08-17T20:00."
  found: "Apple Silicon (arm64). `hw.perflevel0.physicalcpu=4` (performance cores), `hw.perflevel1.physicalcpu=6` (efficiency cores) — 10 total, consistent with the earlier `hw.ncpu=10`/`hw.physicalcpu=10` reading (Evidence 2026-08-17T19:10). The coordinator additionally pointed out: the ORIGINAL hardcoded cap of 8 already exceeded the 4 P-cores, so every run before this session's decode-pool-cap-raise fix (runs 1-3) ALREADY had at least 4 of its 8 decode workers E-core-scheduled — the pre-fix baseline was never a clean all-P-core measurement. The cap raise to `min(10,16)=10` therefore adds exactly 2 more workers, and BOTH must be E-cores (no more P-cores exist to allocate) — moving the pool from '4P+4E busy, 2E idle' to '4P+6E busy, 0 idle anywhere on the chip.'"
  implication: "Sharpens the P/E-core hypothesis from speculative to concrete and directly falsifiable (see Current Focus's revised falsification_test, outcome (b) specifically). Two testably-different mechanisms now exist under the same 'P/E-core' umbrella: (a) E-cores are just slower per-thread for LZMA decode (would show avgDecodeMs high, avgNetMs flat), vs (b) saturating literally every core starves the single-threaded main thread's own event loop (handling all 32 concurrent fetches + postMessage marshaling to 10 workers + IPC), which would show BOTH avgNetMs and avgDecodeMs degrading together, matching run 4's actual signature (climbing latency, low process-level CPU%, worse overall throughput) more closely than a pure-compute explanation would. Does not change Resolution.root_cause's UNCONFIRMED status — still requires the 5th live run's real netMs/avgDecodeMs/pool numbers, now in progress via the coordinator/user."

- timestamp: 2026-08-17T20:25
  checked: "5th live HUMANKIND run (coordinator-relayed): 8 consecutive '[Timing] chunk-stream stats' log lines from this session's new instrumentation, @17s through @128s. Full data: totalAttempts climbed 392->1893 (~14.8/s aggregate, same ~15-18/s band as every prior run), zero timeouts/errors, hosts=6 throughout (unchanged from runs 2-4). avgMs 787->1240ms. avgNetMs 563->855ms. avgDecodeMs 224->385ms. pool[size=10 busy=0 idle=0 queued=0 inline=true] -- identical on ALL 8 snapshots, no variation whatsoever."
  found: "`pool.busy` (derived as `workers.length - idle.length`) and `pool.idle` both read 0 for the entire run, with `inline=true` -- per DecompressPool's own code (decompressPool.ts), this state is ONLY reachable via init()'s catch block setting `this.workers = []` and `this.inlineFallback = true` permanently for the pool instance's lifetime (no retry/re-init path exists). This means EVERY chunk decode in this run went through `inlineDecode()` (main-thread, single-threaded `lzma.decompress()`), never through a worker_threads dispatch, for the run's ENTIRE 128s+ duration -- not an intermittent or partial condition."
  implication: "Directly answers the coordinator's question #1: `inline=true` means 'decode has NEVER used a worker this entire run,' not 'this specific snapshot happened during a lull.' avgNetMs consistently ~2.1-2.2x avgDecodeMs (coordinator's point #2) and BOTH climbing together as totalAttempts grows (point #3) are both explained once inline-decode-on-the-main-thread is understood as the actual mechanism (see next entry for the build-pipeline root cause, and Current Focus's confirming_evidence for the full reconciliation) -- both signals share the same single JS thread doing fetch-continuation AND decode work, so contention in one manifests in both as concurrent chunk-worker count grows. Point #4 (run 5's ~14.8/s matches runs 1-3, not run 4's ~12/s outlier) is consistent with run 4's confound being unrelated to the decode-pool-cap fix (which was inert in run 4 too, per the entry below)."

- timestamp: 2026-08-17T20:28
  checked: "Followed the coordinator's explicit instruction to investigate what `inline=true` actually measures before drawing conclusions: read DecompressPool's `init()`/`spawnWorker()`/`resolveWorkerPath()` (decompressPool.ts) end to end, then traced where `decompressWorker.js` (the file `resolveWorkerPath()` looks for) actually gets produced/shipped -- found and read `meta/buildSidecarSea.ts`, the Node SEA (Single Executable Application) build script that produces the actual sidecar binary run in every one of this debug session's 5 live-hardware tests (confirmed via graphify: `buildSidecarSea.ts` is the only build path producing `src-tauri/binaries/gamelib-sidecar-<triple>`, the Tauri `externalBin` this project's live-hardware runs actually execute)."
  found: "`buildSidecarSea.ts`'s own `main()` function (lines 762-775) contains a PRE-EXISTING (not written by this session), first-party doc comment: 'Pitfall 1 (34-RESEARCH.md): decompressPool.ts's worker_threads spawn resolves decompressWorker.js next to the sidecar bundle at build/main/ today only because electron-vite happens to also emit it there -- a compiled SEA executable does not ship that folder. This is NOT a correctness blocker: spawnWorker()'s existing try/catch falls back to inline single-thread decompression. Flagged loudly, once, at build time as an accepted, deliberate throughput regression.' A `console.warn` with this exact message is emitted ONCE at build time, every SEA compile -- but never appears in any RUNTIME gamelib.log, because it's a build-script log line, not a runtime one. decompressPool.ts's `resolveWorkerPath()` uses `path.join(__dirname, 'decompressWorker.js')` -- `__dirname` inside a SEA-injected bundle does not resolve to a real directory containing that sibling file (confirmed structurally: `SEA_BUNDLE_PATH` bundles everything into one file via esbuild `--outfile`, and `injectBlob()`/`copyNodeBinary()` produce a single executable with no companion files copied alongside it anywhere in this script)."
  implication: "CONFIRMS the mechanism directly from source, not inference: the SEA-packaged sidecar binary NEVER ships `decompressWorker.js`, so `new Worker(workerPath)` throws synchronously on every `spawnWorker()` call, `init()`'s catch block fires on every attempt, and `inlineFallback` is permanently true for the process's entire lifetime. This is a KNOWN, DOCUMENTED, DELIBERATE Phase 34 decision (not a bug this session introduced) -- but its real-world throughput cost was evidently never load-tested against an actual large-depot install before this debug session did so by accident. It retroactively explains why fix #3 (decode-pool-cap-raise, 8->10) never moved throughput in runs 3, 4, or 5: the pool it modifies has never been reachable in any of them. It also explains this project's own prior 'jest-cannot-see-dynamic-import-defects.md' memory pattern recurring a 4th time: decompressPool.test.ts's suite always supplies an explicit `workerPath` override (a test-only hook), so the real `__dirname`-based resolution path -- and its SEA-incompatibility -- has never been exercised by any test in this codebase."

- timestamp: 2026-08-17T20:30
  checked: "Applied fix: runtime logWarning added to DecompressPool.init()'s catch block (decompressPool.ts), firing whenever the pool falls back to inline decode, including the specific cause (worker spawn error message), the configured size, and a pointer to this exact known-tradeoff explanation. Self-verified: `npx tsc --noEmit` (clean), `npx eslint src/backend/storeManagers/steam/depot/decompressPool.ts` (clean), `npx jest src/backend/storeManagers/steam/__tests__/decompressPool.test.ts` (15/15 pass, including the pre-existing 'when init() is forced to fail (bad worker path), decode() falls back to inline decodeChunk and still returns correct bytes' test -- unaffected by the added log call). Full `src/backend/storeManagers/steam/` suite run in progress (background), pending completion at time of writing."
  found: "No test asserts on absence-of-logging for this path, so no regression from adding the log call. The fix is purely additive (a single `logWarning(...)` call inside an already-existing catch block) -- does not change `inlineFallback`'s value, does not change the fallback's own correctness/behavior guarantee (installs still complete via inline decode, LOCKED requirement, unchanged)."
  implication: "Closes the observability gap that let this exact, pre-existing, documented tradeoff go unnoticed by this debug session for its first ~2.5 hours and 4 live-hardware round-trips (chasing the decode-pool-cap's sizing instead of noticing the pool was never active at all). Any future gamelib.log -- from this user or any other -- will now show this directly at the moment it happens, without requiring another full forensic build-pipeline trace."

- timestamp: 2026-08-17T19:20 (superseded reasoning_checkpoint, preserved verbatim for audit trail)
  checked: "Full prior reasoning_checkpoint block (hypothesis/confirming_evidence/falsification_test/fix_rationale/blind_spots) written after the 3rd live run, before the checkpoint response revealing the 4th run's contradicting result."
  found: >
    hypothesis: "REFUTED (2026-08-17T19:15, live hardware, THIRD run): the
    InflightLimiter/DecompressPool decode-decouple fix does NOT raise
    throughput. A third live HUMANKIND run with BOTH TOP_N_FANOUT (3->8) AND
    the decode-decouple fix active produced 3949 attempts / 253s = 15.60
    attempts/sec — statistically indistinguishable from the pre-fix run
    (15.65/s) and the TOP_N_FANOUT-only run (16.70/s)."
    confirming_evidence included: 13 chunk-stream-stats snapshots @37s-@253s,
    hosts=6, zero timeouts/errors; all three live runs to date clustering
    within ~7% (15.60-16.70/s); the `ms` metric traced to be computed AFTER
    `decode()` returns (post InflightLimiter release); DecompressPool's
    pre-fix hardcoded 8-worker cap vs 10 real cores on the test machine
    (sysctl -n hw.ncpu/hw.physicalcpu = 10); decompressWorker.ts's own
    pre-existing "~5 MB/s single-threaded" pure-JS lzma doc comment; Little's
    Law back-calculation across 3 runs converging on ~490-513ms/chunk against
    the old 8-worker cap.
    falsification_test: raise the cap to min(cpus,16) and re-test, predicting
    ~19.5-20.9/s (a ~25% increase) on the known 10-core hardware; REFUTES if
    throughput stays flat despite more workers.
    fix applied and self-verified (tsc/eslint/decompressPool.test.ts
    15/15/full suite 33/34) — status set to awaiting_human_verify pending a
    4th live run.
  implication: "This was the state of the file when the checkpoint response
    arrived. The 4th live run (see next entry) hit the REFUTE branch of this
    exact falsification_test, not the CONFIRM branch — the root_cause's
    'CONFIRMED' framing in the Resolution block below (as it stood at this
    point) was premature and is corrected by the entries that follow."

- timestamp: 2026-08-17T15:00
  checked: "Independently recomputed attempts/sec from the checkpoint's raw live-hardware data instead of accepting the orchestrator's framing verbatim: pre-fix run (3 hosts, 279s, 4366 attempts) and post-fix TOP_N_FANOUT run (6 hosts, 749s, 12512 attempts)."
  found: "pre-fix = 4366/279 = 15.65 attempts/sec. post-fix = 12512/749 = 16.70 attempts/sec. Host count delta = +100% (3->6). Throughput delta = +6.7%. Confirmed via direct computation, not taken on faith."
  implication: "Per this debug file's OWN prior falsification_test wording, a +100% host-count change producing only +6.7% throughput is the explicit REFUTE condition for 'TOP_N_FANOUT alone is sufficient' and the explicit trigger for the per-worker-overhead alternate hypothesis the file itself had already named as untested. TOP_N_FANOUT is downgraded from 'primary root cause' to 'real, correct, but insufficient partial fix.'"

- timestamp: 2026-08-17T15:10
  checked: "depot/decompressPool.ts (DecompressPool class), depot/decompress.ts fetchChunk() (lines ~845-1045, esp. line 947's `await decode(...)`), depot.ts downloadFileChunks()'s `limiter.run(doFetch)` call site (was line 1137), depot/inflightLimiter.ts's InflightLimiter.run()."
  found: "DecompressPool.size defaults to `Math.min(os.cpus().length, 8)` (decompressPool.ts:124) — a hard cap of at most 8 concurrent CPU-bound decode workers, ONE pool instance per download run (depot.ts `new DecompressPool()`, no size override). fetchChunk() `await`s `decode(encrypted, key, sha, chunk.cb_original)` (decompress.ts:947) INSIDE its own body, before returning. depot.ts's `limiter.run(doFetch)` wrapped the ENTIRE fetchChunk() call (network fetch + decode + decode's own internal DecompressPool queue-wait), and InflightLimiter.run() only releases its slot in a `finally` AFTER the wrapped function fully resolves (inflightLimiter.ts's original `run()`). This directly contradicts the IHR-02 doc comment's own claim that the limiter 'never wraps... the decode.'"
  implication: "Any chunk-worker that finishes its network fetch but finds all 8 decode workers busy sits queued inside DecompressPool's internal FIFO (decompressPool.ts:442, `this.queue.push(task)`) while STILL holding its 32-budget InflightLimiter network slot. Once decode demand exceeds 8 concurrent decodes (near-certain once more than ~8 of the 32 workers have active chunks), real concurrent NETWORK requests converge toward ~8, not 32 — independent of host count/TOP_N_FANOUT. Quantitative check: 8 workers x ~479ms/decode-round-trip implies ~16.7 chunks/sec steady state, matching the observed post-fix rate (16.70/s) almost exactly, and close enough to the pre-fix rate (15.65/s) to explain both runs converging on nearly the same ceiling."
  files: []

- timestamp: 2026-08-17T15:40
  checked: "Self-verification after applying the decouple fix (inflightLimiter.ts: exposed acquire()/release() publicly; decompress.ts: fetchChunk() takes `limiter` as new optional trailing param, acquires right before fetch(), releases right after res.arrayBuffer() resolves and BEFORE decode(), with finally-guarded release-on-error; depot.ts: passes `limiter` into fetchChunk() instead of `limiter.run(doFetch)`)."
  found: "tsc --noEmit: clean, exit 0. eslint on the 4 changed files: 0 errors (pre-existing warnings only, unrelated lines). First test pass surfaced 2 real regressions from an unconditional `await limiter?.acquire()` (which yields a microtask turn even when `limiter` is undefined, shifting timing for the depotPrimitives.test.ts external-cancel-signal tests that depend on synchronous-until-fetch() execution) — fixed by gating with `if (limiter)`. Also fixed 3 stale negative-index-from-end argument-position assertions in depot.test.ts (cdnAuth/hostMeta/signal) that needed updating for the new trailing `limiter` param, consistent with every other positional-arg addition this file has needed before. After both fixes: full `src/backend/storeManagers/steam/` jest run (34 suites) = 33 suites/238+ tests PASS (including inflightLimiter.test.ts, depotPrimitives.test.ts, depot.test.ts, hostHealth.test.ts, decompressPool.test.ts — every suite that exercises this code path). depot.finalize.test.ts alone: reproduced the IDENTICAL OOM crash (same suite, same point) in BOTH parallel and --runInBand full-directory runs; re-ran it in isolation with `--max-old-space-size=8192` and it no longer OOMs but instead fails on a pre-existing, unrelated `TypeError: (0, utils_1.sendProgressUpdate) is not a function` mock-wiring bug at depot.ts:1986's emitProgress — confirmed via grep that depot.finalize.test.ts never calls fetchChunk/downloadFileChunks with positional args this change touches."
  implication: "The decouple fix does not regress any existing test once the two collateral timing/offset issues (both caused by the API surface addition, not by a logic error) were fixed. depot.finalize.test.ts's failure is reconfirmed as the SAME pre-existing, unrelated environment/mock-wiring issue already carved out in this file's prior Resolution.verification entry — not a new regression from this fix."

- timestamp: 2026-08-17T14:00-14:30
  checked: "~/Library/Logs/GameLib/gamelib.log (13:54:22-14:00:08), the live post-IHR-fix HUMANKIND run"
  found: "Download started 13:55:24, first bytes 13:55:28 (170ms). 16 chunk-stream-stats snapshots from @18s to @279s: percent climbed 0%->5%, downSpeedMiBs fluctuated wildly (0.00 to 12.14), totalAttempts climbed steadily 430->4366, rotations=0, timeouts=0 the ENTIRE run, hosts=3 (cache1-akl-tpwr, cache1-akl-edgx, cache2-akl-tpwr) fixed the entire run. Run ended via 'SteamGame: aborting in-flight native depot download' -> 'Installation of 1124300 aborted!' (status='abort' signature), NOT a watchdog failure signature."
  implication: "This run never reached a total-freeze/stall state — it was a manual user cancel at 279s/5% due to unacceptably slow (but still progressing) throughput. The carried-forward DecompressPool/fd.write 'total freeze' hypothesis from the pre-fix run was never re-tested by this run (it ended before any potential freeze could occur) and remains formally UNCONFIRMED (neither proven nor disproven) for the post-fix build."

- timestamp: 2026-08-17T14:05
  checked: "src/backend/storeManagers/steam/games.ts stop()/SteamGame class + src/backend/downloadmanager/downloadqueue.ts processNotification() + src/backend/downloadmanager/utils.ts watchdog catch block"
  found: "'aborted!' is logged ONLY for status==='abort' (downloadqueue.ts:407-410); the watchdog trip path (utils.ts:212-227) always sets status='error' and logs 'install stalled — no progress observed for Ns' BEFORE the terminal-error path, which processNotification then reports as 'failed!' (utils.ts:415-419/downloadqueue.ts:416-419), never 'aborted!'. This run's log shows only 'aborted!' with no 'install stalled' line anywhere."
  implication: "Definitively confirms this run's abort was a manual/UI cancel (SteamGame.stop() invoked via downloadqueue.ts's stopCurrentDownload(), the same path a user-clicked Cancel button triggers), not the automatic 8-minute no-progress watchdog. Matches the user's own direct confirmation (relayed via orchestrator) that they clicked Cancel."

- timestamp: 2026-08-17T14:10
  checked: "src/backend/storeManagers/steam/depot/hostHealth.ts pickHost()/TOP_N_FANOUT + depot.ts TARGET_INFLIGHT_CHUNKS/FILE_CONCURRENCY doc comments + inflightLimiter.ts"
  found: "TOP_N_FANOUT=3 (unchanged since Phase 25, pre-dates IHR-02). pickHost()'s attempt-0 branch: `const N = Math.min(TOP_N_FANOUT, healthy.length); if (attemptIndex===0 && N>1) return healthy[workerSlot % N]`. IHR-02 (commit a0e2f07f4, today) raised FILE_CONCURRENCY/TARGET_INFLIGHT_CHUNKS 8->32, explicitly documented as fixing a SEPARATE bug (single-chunk files only running 1 chunk-worker, silently capping real concurrency at 8) — but did not touch TOP_N_FANOUT."
  implication: "A structural mismatch: the concurrency budget quadrupled (8->32) but the number of hosts that budget can be spread across at attempt-0 stayed fixed at 3. This is consistent with, and provides a specific mechanism for, the observed hosts=3-invariant + climbing avgMs + throughput well under the new 32 budget."

- timestamp: 2026-08-17T14:15
  checked: "src/backend/storeManagers/steam/depot.ts downloadSingleFile() (lines 1211-1395) and downloadDepotFiles()'s file-worker pool (lines 2003-2089)"
  found: "Each of the 32 file-worker slots processes files SERIALLY: mkdir -> open -> truncate -> downloadFileChunks (network, limiter-tracked) -> close -> sha1File(dest) [re-reads the WHOLE file from disk] -> applyEDepotFileModes -> applyMachOExecutableFallback, before looping back to grab the next file. Only the network portion inside downloadFileChunks is wrapped by `limiter.run()`; every other step runs un-tracked but still consumes that worker slot's wall-clock time."
  implication: "A second, UNTESTED candidate mechanism (noted as a blind spot above): if per-file non-network overhead (especially the whole-file SHA1 re-read) is non-trivial relative to the ~1.1s network latency, it would independently reduce the fraction of time each file-worker spends inside the InflightLimiter, diluting effective concurrency below 32 regardless of host count. Not yet isolated from the TOP_N_FANOUT hypothesis — could be additive, could be secondary."

- timestamp: 2026-08-17T19:00
  checked: "Live-hardware checkpoint data (orchestrator-relayed): third live HUMANKIND run, TOP_N_FANOUT (3->8) + InflightLimiter/decode-decouple fix BOTH active, 13 chunk-stream-stats snapshots @37s-@253s."
  found: "3949 attempts / 253s = 15.60 attempts/sec, hosts=6 throughout, zero timeouts/errors on all 6 hosts the entire run. Statistically indistinguishable from the pre-fix run (15.65/s, Evidence 2026-08-17T14:00-14:30) and the TOP_N_FANOUT-only run (16.70/s, Evidence 2026-08-17T15:00)."
  implication: "REFUTES the InflightLimiter/decode-decouple fix's falsification_test CONFIRM branch — two stacked network-layer fixes (host fan-out width, network-slot/decode coupling) produced ZERO combined measurable throughput change across three independent live runs. The coupling removed by the decode-decouple fix was real (confirmed by code) but was never the dominant constraint on real-world throughput."

- timestamp: 2026-08-17T19:05
  checked: "decompress.ts fetchChunk()'s `ms` metric computation (line ~905 attemptStart, line ~997 limiter.release(), line ~999 decode() await, line ~1001 ms computation) — traced exactly what avgMs/onAttempt/hostHealth.record measure post-decouple-fix."
  found: "`attemptStart = Date.now()` is set BEFORE fetch() starts. `limiter?.release()` (network slot release) now correctly happens right after `res.arrayBuffer()` resolves, BEFORE `decode()` is called — the decouple fix's own change, working as designed. BUT `const ms = Date.now() - attemptStart` is computed AFTER `await decode(...)` returns (line 1001), i.e. the metric used for hostHealth scoring and the periodic avgMs stat still spans the FULL fetch+decode+decode-queue-wait window, unchanged by the decouple fix."
  implication: "The decode-decouple fix correctly shortened how long the shared InflightLimiter NETWORK SLOT is held, but did not (and was never intended to) change what the observability metric measures. avgMs was never capable of distinguishing network-bound from decode-bound by itself — explains why avgMs behavior looked consistent with either hypothesis across all three runs, and why this needed static tracing of the actual call chain rather than another live-hardware guess."

- timestamp: 2026-08-17T19:10
  checked: "decompressPool.ts (DecompressPool constructor, pre-fix line 124), decompressWorker.ts (module header comment, line 3), package.json (`lzma` dependency), node_modules/lzma/package.json + readme.md, `sysctl -n hw.ncpu`/`hw.physicalcpu` on this project's own dev machine."
  found: "decompressPool.ts pre-fix: `this.size = opts.size ?? Math.min(os.cpus().length, 8)` — hardcoded ceiling of 8, independent of actual core count. `sysctl -n hw.ncpu` / `hw.physicalcpu` on this dev machine both report 10 — 2 real cores were idle under the old cap. decompressWorker.ts's own PRE-EXISTING header comment (not written by this debug session): 'the pure-JS LZMA decompressor (~5 MB/s single-threaded) runs off the Electron main thread.' package.json confirms `lzma@2.3.2`; node_modules/lzma/readme.md confirms this is LZMA-JS (nmrugg), 'A JavaScript implementation of the Lempel-Ziv-Markov (LZMA) chain compression algorithm' — pure JS, no native/WASM bindings."
  implication: "Two independently confirmable facts combine to fully explain the invariant ~15.6-16.7 attempts/sec ceiling with NO new live measurement needed: (1) decode concurrency was artificially capped at 8 regardless of real core count (an EXTRA, fixable constraint beyond genuine CPU-boundedness), and (2) each decode worker's own throughput is inherently slow because the codebase already documents its LZMA decoder as pure-JS and ~5MB/s. Every successful chunk MUST pass through this pool exactly once (decode is not optional/skippable), so aggregate decode throughput (poolSize x perWorkerMBps / avgChunkDecompressedSize) becomes the run's overall completion-rate ceiling, entirely independent of network concurrency, host fan-out, or the InflightLimiter's budget — matching all three live runs simultaneously."

- timestamp: 2026-08-17T19:10
  checked: "Little's Law back-calculation across all three live runs, using the OLD 8-worker decode cap: pre-fix 8 workers / 15.65 attempts/sec; TOP_N_FANOUT-only 8/16.70; TOP_N_FANOUT+decode-decouple 8/15.60."
  found: "Average per-chunk decode time implied: 511ms, 479ms, 513ms respectively — all three cluster within ~7% of each other (~490-513ms), despite the underlying network-layer configuration changing substantially between runs (3 hosts -> 6 hosts -> 6 hosts+decoupled limiter). At the documented ~5MB/s per-worker throughput, ~490-513ms implies an average DECOMPRESSED chunk size of ~2.4-2.6MB, consistent with this codebase's own ~1MB max COMPRESSED chunk size (depot.ts) plus a plausible LZMA expansion ratio for game assets."
  implication: "Internally self-consistent across three independently-run live hardware tests and this codebase's own pre-existing documentation/comments — strong, direct, static+quantitative evidence that DecompressPool's decode throughput (not network concurrency in any form) is the actual, dominant, real-world throughput ceiling. Confirms the falsifiable hypothesis in Current Focus without requiring a fourth speculative live-hardware round before applying a fix."

- timestamp: 2026-08-17T19:15
  checked: "Applied fix: raised DecompressPool's worker cap from hardcoded `8` to `Math.min(os.cpus().length, DECOMPRESS_POOL_MAX_WORKERS)` with `DECOMPRESS_POOL_MAX_WORKERS = 16` (decompressPool.ts). Self-verified: `npx tsc --noEmit` (clean), `npx eslint src/backend/storeManagers/steam/depot/decompressPool.ts` (clean), `npx jest src/backend/storeManagers/steam/__tests__/decompressPool.test.ts` (15/15 pass), full `npx jest src/backend/storeManagers/steam/` run (33/34 suites, 1225/1225 tests pass)."
  found: "No test in decompressPool.test.ts or elsewhere constructs `new DecompressPool()` without an explicit `size` override, so none were coupled to the old hardcoded default — zero test regressions from raising it. The single failing suite, `depot.finalize.test.ts`, reproduced the SAME pre-existing JavaScript heap OOM already documented as unrelated in this file's prior verification entries (Evidence 2026-08-17T15:40) and in this project's own persistent memory (`jest-cannot-see-dynamic-import-defects.md`) — confirmed via the exact same crash signature (`FATAL ERROR: Ineffective mark-compacts near heap limit`), same suite, unrelated to any file this fix touches."
  implication: "The fix is minimal, `min()`-bounded (a <=8-core machine's behavior is byte-for-byte unchanged), self-verified clean, and does not regress any existing test. It is explicitly scoped as a partial, real improvement (~25% throughput increase predicted on the known 10-core test hardware, not a full resolution) — see Resolution.fix and the reasoning_checkpoint's blind_spots for the larger, deliberately-deferred follow-up (a faster non-pure-JS LZMA decoder)."

- timestamp: 2026-08-17T19:35
  checked: "4th live-hardware run (orchestrator-relayed checkpoint response, treated as DATA): TOP_N_FANOUT+decode-decouple+decode-pool-cap-raise (all 3 stacked fixes) active."
  found: "avgMs ~2600-3000ms per request (vs ~1350-1700ms across the prior 3 clean runs), aggregate rate ~12/s (below the 15.65/s pre-fix baseline, and far below the falsification_test's predicted ~19.5-20.9/s), latency climbing MONOTONICALLY across the run, sidecar process CPU only ~126% (1.3 cores)."
  implication: "This is the falsification_test's REFUTE branch, not its CONFIRM branch: throughput went DOWN, not up ~25%. 126% CPU is far too low for anywhere near 10 pure-JS decode workers being saturated (would expect several hundred percent on this 10-core machine if decode were genuinely the bound and the pool were busy). The 'CONFIRMED' root_cause status this file held going into this checkpoint was premature. Two live possibilities, neither confirmed: (a) decode is NOT actually the bottleneck and something else degrades over the run's duration (this run's own low-CPU/climbing-latency signature does not fit clean CPU-bound saturation); (b) decode IS still a real factor but the pool-cap-raise fix itself introduced a NEW problem (see Current Focus blind_spots' Apple-Silicon P/E-core hypothesis) rather than helping. Neither can be resolved without decode-vs-network split timing data, which this file has never had (see next entries)."

- timestamp: 2026-08-17T19:40
  checked: "Immediately-following live differential test (orchestrator-relayed): real Steam client (CrossOver/Wine bottle, steamservice.exe) download started on the SAME machine/network right after the confounded GameLib run 4 above, monitored via `netstat -ib` deltas on `en0` -- independent of GameLib's own instrumentation."
  found: "Idle baseline before Steam started: ~0-1 MiB/s (12 samples over ~60s). After Steam's download started: ramped 3.91 -> sustained 51.03/52.73/56.75 MiB/s (3 consecutive 5s readings)."
  implication: "Strong, independently-collected evidence that the ~50+ MiB/s pipe is genuinely available on this machine/network for THAT time window -- rules out ambient ISP/CDN congestion as an explanation for GameLib's slowness during that window. Does NOT by itself localize the bottleneck to GameLib's HTTP path specifically (still consistent with GameLib's OWN decode ceiling simply never attempting to pull more than ~15-17 MB/s regardless of the pipe's real capacity) -- and does not tell us anything about whether the real Steam client (running under CrossOver, possibly a different CDN/protocol path per the user's own caveat) is even a fair speed comparison target. This evidence is accepted as VALID but its scope is narrower than the checkpoint response's framing ('ambient congestion ruled out, therefore GameLib's HTTP path is broken') -- the second half of that claim requires code-level tracing, done in the next entries."

- timestamp: 2026-08-17T19:50
  checked: "Directly traced fetchChunk's HTTP call (decompress.ts `res = await fetch(...)`, line ~965) end-to-end for the user-relayed 'no connection reuse / new TCP+TLS per chunk' theory: what HTTP client is used, whether any Agent/dispatcher is configured, whether `Connection: close` or any header defeats keep-alive."
  found: "Plain global Node `fetch()` (no node-fetch/axios/custom client). `package.json` engines requires `node >=22`; Node's built-in global fetch is undici-backed (`undici@7.24.7` is also a direct devDependency, though not imported anywhere in src/ -- built-in fetch already includes its own bundled undici). CHUNK_FETCH_HEADERS (decompress.ts) sets only User-Agent/Accept/Accept-Charset -- no Connection header, no cache-control. No `new Agent(...)`/`setGlobalDispatcher`/custom dispatcher anywhere in decompress.ts, depot.ts, or the rest of src/backend/storeManagers/steam/depot/. Read undici's actual default Client config directly (node_modules/undici/lib/dispatcher/client.js:247-249): `keepAliveTimeout` defaults to 4000ms, `keepAliveMaxTimeout` to 600000ms -- keep-alive is ON, sockets persist and are reused for 4s of idle time (this fetch loop runs continuously, well inside that window). Read undici's default Pool config (lib/dispatcher/pool.js:68): `this[kConnections] = connections || null` -- per-origin connection cap is UNBOUNDED by default (opens/reuses as many parallel connections as needed to one origin, never serializes concurrent same-host requests onto a single connection)."
  implication: "The user-relayed 'no connection reuse' hypothesis is REFUTED by direct code inspection, not by inference or guesswork -- GameLib's fetch layer already has keep-alive + unbounded per-origin connection pooling, the exact behavior a hand-rolled keepAlive-Agent fix would have tried to add. Implementing that fix would change nothing (already the default). This does not mean the network layer is innocent -- it means the SPECIFIC mechanism proposed is not the explanation; if the network layer is genuinely a factor it must be something else (see falsification_test in Current Focus for what would confirm/refute this on the next live run)."

- timestamp: 2026-08-17T19:55
  checked: "Whether GameLib's own downSpeedMiBs/avgMs/attempts-per-sec telemetry (used throughout this entire debug session as circumstantial evidence for decode-boundedness) could, in principle, distinguish network-bound from decode-bound behavior -- traced onNetworkBytes/netBytes and doneBytes end to end (depot.ts:1094-1148 downloadFileChunks' fetchChunk call site, depot.ts:1954-1966 the rolling-rate computation)."
  found: "`onBytes(data.length, netBytes)` (depot.ts:1148) fires only AFTER `await fetchChunk(...)` fully resolves -- and fetchChunk() itself only resolves after BOTH the network fetch AND `decode()` (decrypt+decompress+sha1-verify) complete, by design (decompress.ts's own SECURITY note: a chunk that fails verification is retried against a different server and NEVER returned to the caller). `netBytes`/`doneBytes` (and therefore downSpeedMiBs/diskSpeedMiBs, and the run-wide attempts/sec figure) are ALL, by construction, measurements of the COMBINED fetch+decode completion rate -- never a decode-independent signal. Confirmed this is inherent to the security/correctness model (never report unverified bytes), not an accidental bug in the metric wiring."
  implication: "This debug session's entire Little's Law reconciliation (Evidence 2026-08-17T19:10, ~490-513ms/chunk across 3 runs) and the checkpoint response's downSpeedMiBs citation ('fluctuated 0.00 to 12.14') were BOTH working from the exact same underlying combined signal, expressed two different ways (chunks/sec vs MiB/s) -- neither is independent corroboration of the other, and neither can distinguish network-bound from decode-bound on its own. This debug file has never had a real, decode-independent network timing measurement for any of its 4 live runs. Directly motivates the fix in the next entry."

- timestamp: 2026-08-17T20:00
  checked: "Applied fix: network-vs-decode timing split. decompress.ts: `ChunkAttemptEvent` gains an optional `netMs` field, computed right after `res.arrayBuffer()` resolves (before `decode()`) and reported alongside the existing (unchanged) combined `ms` on the 'success' outcome only; `onNetworkBytes` moved to fire at the same earlier point (semantically correct, though provably no observable behavior change -- the outer loop in depot.ts only reads it after the whole fetchChunk() call resolves regardless). depot.ts: new run-wide `totalSuccessMs`/`totalNetMs`/`successSamples` accumulators feed new `avgNetMs`/`avgDecodeMs` fields in the periodic `[Timing] chunk-stream stats` log line. decompressPool.ts: new `DecompressPool.stats()` method exposing `{size, busy, idle, queued, inlineFallback}` (busy derived as `workers.length - idle.length`, never a separate counter that could drift), also surfaced in the same log line as `pool[size=.. busy=.. idle=.. queued=.. inline=..]`. Self-verified: `npx tsc --noEmit` (clean), `npx eslint` on all 3 changed files (clean), targeted jest (decompressPool.test.ts, depotPrimitives.test.ts, depot.test.ts, hostHealth.test.ts, inflightLimiter.test.ts) 238/238 pass, full `src/backend/storeManagers/steam/` jest run 33/34 suites, 1225/1225 tests pass."
  found: "No existing test asserts on the exact chunk-stream-stats log string or on ChunkAttemptEvent's field set exhaustively (would have failed loudly on the new optional field otherwise) -- zero regressions. The single failing suite, depot.finalize.test.ts, reproduced the SAME pre-existing, already-documented JavaScript heap OOM (`FATAL ERROR: Ineffective mark-compacts near heap limit`), unrelated to any file this fix touches."
  implication: "This fix is purely additive and observational -- it changes NO retry/backoff/selection/hostHealth-scoring behavior (hostHealth.record still only ever receives the original combined `ms`) and does not itself move throughput. Its entire value is enabling the NEXT live run to directly measure, rather than infer, whether network or decode is the real constraint -- closing the actual evidentiary gap this session discovered (previous entry), rather than re-affirming or discarding either the decode-pool-cap-raise fix or the user's HTTP-path theory without new data."

## Resolution

root_cause: "CONFIRMED 2026-08-17T20:25 (5th live run's own instrumented data + direct read of meta/buildSidecarSea.ts, not inference). DecompressPool's worker_threads pool has NEVER engaged on the actual SEA-packaged sidecar binary this whole debug session, across all 5 live-hardware runs -- every chunk decode has run INLINE, single-threaded, on the sidecar's main thread. Mechanism, confirmed directly from source: `resolveWorkerPath()` (decompressPool.ts) resolves `decompressWorker.js` via `path.join(__dirname, ...)`, which only produces a valid, existing sibling file in the dev/Electron build (where electron-vite happens to also emit it there) -- the compiled SEA executable (produced by `meta/buildSidecarSea.ts`, the ONLY build path that produces the `src-tauri/binaries/gamelib-sidecar-<triple>` binary this project's live-hardware runs actually execute) never ships that companion file at all. `new Worker(workerPath)` therefore throws synchronously on every `spawnWorker()` call, `init()`'s catch block fires every time, and `this.inlineFallback` is permanently true for the sidecar process's whole lifetime (no retry/re-init path exists). This is a KNOWN, PRE-EXISTING, DELIBERATE Phase 34 tradeoff -- `buildSidecarSea.ts`'s own doc comment (not written by this debug session) calls it 'Pitfall 1' and states it is 'an accepted, deliberate throughput regression,' flagged via a one-time `console.warn` at BUILD time -- but that warning was never surfaced at RUNTIME in any shipped gamelib.log, so this session had to rediscover its real-world throughput cost from scratch via 5 live-hardware rounds. This retroactively invalidates the premise of the previously-applied fix #3 (raising DECOMPRESS_POOL_MAX_WORKERS 8->10): that fix modifies a worker_threads pool that was never reachable on the actual test binary in runs 3, 4, or 5 -- it was inert, not wrong, but it never had a chance to help. The Apple Silicon P/E-core heterogeneity hypothesis explored earlier this session (Evidence 2026-08-17T20:10-20:20) is similarly moot for the SAME runs, since no worker threads ever spawned for P/E-core scheduling to matter -- though it remains a legitimate consideration for a FUTURE fix that actually gets the pool working. What remains CONFIRMED and unchanged from earlier in this session: TOP_N_FANOUT widening and the InflightLimiter/decode-decouple fix were both real, correctly-implemented, non-dominant bug fixes (retained); the user-relayed 'no connection reuse' theory is refuted by direct code inspection (undici's default keep-alive/unbounded-pool behavior, unchanged finding). The dominant, now well-evidenced explanation for the throughput ceiling across every one of this session's 5 live runs: a single JS main thread doing BOTH all fetch-scheduling (up to 32 concurrent chunk fetches) AND all decode work (pure-JS `lzma`, ~5MB/s single-threaded, decompressWorker.ts's own pre-existing doc comment) serially, with contention between the two growing as concurrent chunk-worker count increases -- directly matching the 5th run's own avgNetMs/avgDecodeMs both climbing together as totalAttempts grows. Run 4's specific WORSENING relative to baseline remains a separate, still-open, lower-priority question (see reasoning_checkpoint's blind_spots) now known to be unrelated to the decode-pool-cap fix's mechanism."
fix: "Five-part, now cumulative. The first three from earlier in this session are RETAINED unmodified (none should be reverted based on anything found this session -- fix #3 in particular is harmless, just inert on this binary, and would become meaningful again if/when the real SEA-packaging fix below is applied): (1) TOP_N_FANOUT raised 3->8 (hostHealth.ts). (2) InflightLimiter's held network slot decoupled from decode (decompress.ts/depot.ts/inflightLimiter.ts). (3) DecompressPool's decode worker cap raised from hardcoded `8` to `Math.min(os.cpus().length, DECOMPRESS_POOL_MAX_WORKERS=16)` (decompressPool.ts). (4) Network-vs-decode timing-split instrumentation -- `ChunkAttemptEvent.netMs`, `avgNetMs`/`avgDecodeMs` in the chunk-stream-stats log, `DecompressPool.stats()` (decompress.ts/depot.ts/decompressPool.ts) -- this is what actually surfaced the `inline=true` signature that led to this session's real finding, and remains valuable going forward (will directly show `pool[busy>0]` once/if the pool is ever actually fixed to spawn). (5, NEW, this session, FINAL fix applied): a runtime `logWarning` in `DecompressPool.init()`'s catch block (decompressPool.ts), firing the moment the pool falls back to inline decode, with the worker-spawn error message, the configured pool size, and a pointer to the known-tradeoff explanation. This is DELIBERATELY the only code change made in response to this session's real finding -- it does NOT attempt to fix the actual SEA-packaging gap (shipping `decompressWorker.js` as a companion file next to the compiled SEA binary across macOS/Windows/Linux, and reworking `resolveWorkerPath()` to resolve correctly in that context, e.g. via `process.execPath` instead of `__dirname`). That is a real, cross-platform, release-pipeline change touching `meta/buildSidecarSea.ts`'s bundling/copy steps and the CI release matrix, requiring its own compile-and-run verification loop this session cannot safely close (this project's own build/packaging memory documents repeated SEA-specific pitfalls in this exact file, and explicitly warns against assuming a binary's behavior without directly, empirically verifying it). Attempting that fix here, unverified end-to-end, would repeat the exact 'plausible-sounding, statically-reasoned, but ultimately inert-on-real-hardware' pattern this debug session has now caught itself in twice already (the InflightLimiter-decouple's own falsification_test refutation, and fix #3's inertness) -- except at a much larger blast radius. RECOMMENDED as a separate, dedicated follow-up (see next_action)."
verification: "Self-verified (fix #5, this session's final change). tsc --noEmit: clean. eslint on decompressPool.ts: 0 errors. decompressPool.test.ts: 15/15 pass, including the pre-existing 'when init() is forced to fail... falls back to inline decodeChunk' test (unaffected by the added log call -- purely additive). Full src/backend/storeManagers/steam/ jest run: 33/34 suites pass, 1225/1225 tests pass -- the single failing suite, depot.finalize.test.ts, reproduces the SAME pre-existing, already-documented JavaScript heap OOM, unrelated to any file this fix touches (unchanged across every self-verify this entire debug session). This fix is intentionally NOT a throughput fix and cannot be verified against the user's original symptom (slow install) by design -- it only makes an already-confirmed condition observable going forward. The user's original symptom (HUMANKIND install far slower than Steam's own client) remains UNRESOLVED at the code level -- the real fix (SEA companion-file packaging) is recommended as separate follow-up work, not completed in this session. This debug session should NOT be closed as 'resolved' in the find_and_fix sense; it has reached a confirmed root cause with a scoped, deliberate stopping point, appropriate for handoff to dedicated build-pipeline follow-up work."
files_changed:
  - src/backend/storeManagers/steam/depot/hostHealth.ts
  - src/backend/storeManagers/steam/depot/inflightLimiter.ts
  - src/backend/storeManagers/steam/depot/decompress.ts
  - src/backend/storeManagers/steam/depot.ts
  - src/backend/storeManagers/steam/depot/decompressPool.ts
  - src/backend/storeManagers/steam/__tests__/depot.test.ts
