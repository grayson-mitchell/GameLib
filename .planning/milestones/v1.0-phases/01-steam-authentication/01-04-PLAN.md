---
phase: 01-steam-authentication
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/screens/Login/components/SteamLogin/index.tsx
  - src/backend/storeManagers/steam/user.ts
  - src/backend/storeManagers/steam/__tests__/user.test.ts
autonomous: true
requirements: [AUTH-02]
gap_closure: true

must_haves:
  truths:
    - "Entering a 5-character alphanumeric EMAIL Steam Guard code (e.g. KQM4F) completes credential login and shows the logged-in Runner tile"
    - "Lower-case or whitespace-padded guard codes are normalized (trimmed + uppercased) before being sent to Steam"
    - "Numeric authenticator/TOTP codes (e.g. 12345) still submit successfully — normalization is a no-op for digits"
    - "The Steam Guard prompt and error message reference both email and authenticator codes (guard-type aware)"
  artifacts:
    - path: "src/frontend/screens/Login/components/SteamLogin/index.tsx"
      provides: "Alphanumeric guard input + frontend normalization + guard-type-aware messaging"
      contains: "toUpperCase"
    - path: "src/backend/storeManagers/steam/user.ts"
      provides: "Defense-in-depth normalization in submitSteamGuardCode"
      contains: "trim().toUpperCase()"
    - path: "src/backend/storeManagers/steam/__tests__/user.test.ts"
      provides: "Regression coverage for the alphanumeric EmailCode path + normalization"
      contains: "KQM4F"
  key_links:
    - from: "src/frontend/screens/Login/components/SteamLogin/index.tsx"
      to: "window.api.steamSubmitGuard"
      via: "handleGuardSubmit submits normalized guardCode"
      pattern: "steamSubmitGuard"
    - from: "src/backend/storeManagers/steam/user.ts"
      to: "session.submitSteamGuardCode"
      via: "normalized code passed verbatim to steam-session"
      pattern: "session\\.submitSteamGuardCode"
---

<objective>
Close the Phase 1 UAT Test 2 gap: EMAIL Steam Guard login fails because the guard input assumes numeric TOTP-style codes, but Steam EMAIL guard codes are 5-character ALPHANUMERIC (uppercase letters + digits). The entered code is forwarded verbatim and Steam rejects mis-cased/whitespace codes as InvalidLoginAuthCode (65), surfaced as "invalid code".

Root cause is fully diagnosed (see @.planning/debug/email-steamguard-code-rejected.md): transport and steam-session layers are provably correct (steam-session auto-selects EAuthSessionGuardType.EmailCode and sends the code unmodified). The fix belongs entirely in GameLib's entry/normalization layer.

Purpose: Make credential + EMAIL Steam Guard login work end-to-end without regressing the numeric authenticator path.
Output: Alphanumeric-aware guard input, frontend + backend code normalization, guard-type-aware messaging, and regression tests exercising the EmailCode path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/email-steamguard-code-rejected.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

From src/backend/storeManagers/steam/user.ts (the backend handler — correct, but the normalization point):
```typescript
static async submitSteamGuardCode(
  code: string
): Promise<{ status: 'done' | 'error' }>
// Body calls: await session.submitSteamGuardCode(code)  // line ~398, passes code verbatim
```

From src/frontend/screens/Login/components/SteamLogin/index.tsx (the defect site):
```tsx
// handleGuardSubmit (~199-213): submits guardCode verbatim, generic error message
const result = await window.api.steamSubmitGuard(guardCode)   // ~203
// ...else branch: showError('Incorrect code. Check your authenticator and try again.')  // ~211

// Prompt text (~371): "Check your authenticator app or email for a Steam Guard code."
// Guard <input> (~387-401):
//   type="text" inputMode="numeric" maxLength={5}
//   onChange={(e) => { setGuardCode(e.target.value); if (error) setError(null) }}
// Submit button (~414): disabled={guardCode.length < 5 || loading}
```

From src/backend/storeManagers/steam/__tests__/user.test.ts (existing test harness — numeric fixtures only, ~541-578):
```typescript
// describe('submitSteamGuardCode()') uses mockSessionInstance.submitSteamGuardCode,
// sessionOnHandlers['authenticated'](), mockSteamUserInstance.logOn / steamUserOnHandlers['loggedOn'].
// beforeEach starts a credential session so this.session is set.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Treat guard code as alphanumeric + normalize + guard-type-aware messaging (frontend)</name>
  <files>src/frontend/screens/Login/components/SteamLogin/index.tsx</files>
  <action>
Fix the guard entry layer so alphanumeric EMAIL codes are accepted and normalized. Surgical edits only (stay mergeable with Heroic upstream):

1. Guard `<input>` (~387-401): Remove `inputMode="numeric"` (the field must NOT advertise numeric-only entry — change to `inputMode="text"` or drop the attribute). Keep `maxLength={5}` (email codes are 5 chars). Add visual uppercasing via inline `style={{ textTransform: 'uppercase' }}` and `autoCapitalize="characters"`. Update `aria-label` to "Steam Guard code (letters or digits)".

2. Normalize in `onChange` (~397-401): store the value uppercased and whitespace-stripped, e.g. `setGuardCode(e.target.value.replace(/\s/g, '').toUpperCase())`. This keeps state, the disabled-length check, and the displayed value all in the normalized form. Preserve the existing `if (error) setError(null)` clear-on-edit behavior.

3. Normalize again at submit in `handleGuardSubmit` (~200-203) as defense-in-depth: pass `guardCode.trim().toUpperCase()` to `window.api.steamSubmitGuard(...)`. Do NOT change the IPC channel or response handling.

4. Make messaging guard-type aware: change the prompt text (~371) and the error message (~211) so they reference BOTH email and authenticator. The error branch must read something like `showError('Incorrect code. Check your email or authenticator app and try again.')`. Do not make it email-only (authenticator/TOTP path must still read naturally).

Numeric authenticator codes (e.g. 12345) are unaffected: `.toUpperCase()` is a no-op on digits and `\s`-stripping/`trim()` only removes whitespace. Closes UAT gap missing-items 1, 2 (frontend half), and 3.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && ! grep -q 'inputMode="numeric"' src/frontend/screens/Login/components/SteamLogin/index.tsx && grep -q 'toUpperCase' src/frontend/screens/Login/components/SteamLogin/index.tsx && grep -qi 'email or authenticator' src/frontend/screens/Login/components/SteamLogin/index.tsx && npm run codecheck</automated>
  </verify>
  <done>inputMode="numeric" is gone; guardCode is uppercased/whitespace-stripped on entry and trimmed+uppercased at submit; prompt and error message reference both email and authenticator; `npm run codecheck` (lint + typecheck) passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Defense-in-depth normalization in submitSteamGuardCode + EmailCode regression tests (backend)</name>
  <files>src/backend/storeManagers/steam/user.ts, src/backend/storeManagers/steam/__tests__/user.test.ts</files>
  <behavior>
    - submitSteamGuardCode('kqm4f') passes 'KQM4F' to session.submitSteamGuardCode and resolves { status: 'done' } via the 'authenticated' event (alphanumeric EmailCode path).
    - submitSteamGuardCode('  kqm4f  ') normalizes to 'KQM4F' before forwarding (trim + uppercase + whitespace strip).
    - submitSteamGuardCode('12345') still resolves { status: 'done' } — normalization is a no-op for numeric authenticator codes (no regression to existing fixtures).
  </behavior>
  <action>
Backend (src/backend/storeManagers/steam/user.ts, submitSteamGuardCode ~387-398): Before forwarding, normalize the incoming code as defense-in-depth: derive `const normalized = code.trim().toUpperCase()` (also strip internal whitespace if practical) and call `await session.submitSteamGuardCode(normalized)` instead of the raw `code`. Do NOT alter the event-wiring, finishAuth call, or error/timeout handling. steam-session still auto-selects EmailCode — only the string shape changes.

Tests (src/backend/storeManagers/steam/__tests__/user.test.ts, inside the existing `describe('submitSteamGuardCode()')` block ~541-579): Add regression tests using the existing harness (mockSessionInstance.submitSteamGuardCode, sessionOnHandlers['authenticated'], mockSteamUserInstance.logOn / steamUserOnHandlers['loggedOn']):
  1. Alphanumeric EmailCode happy path: submit 'KQM4F' (or lowercase 'kqm4f') → expect status 'done', AND assert `mockSessionInstance.submitSteamGuardCode` was called with 'KQM4F' (the normalized form).
  2. Normalization: submit '  kqm4f  ' → assert the mock received 'KQM4F'.
  Mirror the existing happy-path test's mock setup (submitSteamGuardCode.mockImplementation firing sessionOnHandlers['authenticated'] on a timer). Keep the existing numeric '12345'/'99999' tests intact to prove no regression.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && grep -q "trim().toUpperCase()" src/backend/storeManagers/steam/user.ts && grep -q "KQM4F" src/backend/storeManagers/steam/__tests__/user.test.ts && npm test -- --testPathPattern=steam</automated>
  </verify>
  <done>submitSteamGuardCode normalizes code with trim().toUpperCase() before forwarding to session; new tests assert the EmailCode path resolves 'done' and that the session receives the normalized 'KQM4F'; existing numeric guard tests still pass; full steam suite green.</done>
</task>

</tasks>

<verification>
- `npm run codecheck` passes (lint + typecheck, no new errors).
- `npm test -- --testPathPattern=steam` passes, including the new alphanumeric EmailCode + normalization tests and all pre-existing numeric guard tests.
- Manual trace confirms the credential flow is unchanged: `steamStartCredentials` → `guard_required` → normalized `steamSubmitGuard` → done.
</verification>

<success_criteria>
- A 5-character alphanumeric EMAIL Steam Guard code (lower or mixed case, with stray whitespace) is normalized to uppercase and accepted, completing credential login.
- Numeric authenticator/TOTP codes still submit successfully (no regression).
- Prompt and error messaging reference both email and authenticator codes.
- Regression tests lock in the EmailCode path so the gap cannot silently reopen.
</success_criteria>

<output>
Create `.planning/phases/01-steam-authentication/01-04-SUMMARY.md` when done.
</output>
