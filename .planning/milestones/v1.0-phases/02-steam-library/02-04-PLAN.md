---
phase: 02-steam-library
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/frontend/state/GlobalState.tsx
  - src/frontend/screens/Library/index.tsx
  - src/frontend/helpers/library.ts
  - public/locales/en/translation.json
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]

must_haves:
  truths:
    - "handleGamePush updates state.steam.library when a 'steam' GameInfo is pushed"
    - "steamLogin triggers a non-blocking background library sync (D-01)"
    - "Steam games appear in the unified library grid mixed with Epic/GOG/Amazon/Zoom"
    - "Favourited Steam games appear in the Favourites view"
    - "The alphabet/sort filters recompute when steam.library changes"
    - "On first-ever login (username set, empty library, sync in progress) a 'Syncing your Steam library…' empty-state message is visible (D-02)"
  artifacts:
    - path: "src/frontend/helpers/library.ts"
      provides: "steamCategories export"
      contains: "steamCategories"
    - path: "src/frontend/screens/Library/index.tsx"
      provides: "steam included in makeLibrary, favourites, deps"
      contains: "steamLibrary"
  key_links:
    - from: "src/frontend/state/GlobalState.tsx"
      to: "state.steam.library"
      via: "handleGamePush steam case"
      pattern: "args.runner === 'steam'"
    - from: "src/frontend/screens/Library/index.tsx"
      to: "rendered grid"
      via: "makeLibrary spread of steamLibrary"
      pattern: "steamLibrary"
---

<objective>
Wire the backend's `pushGameToLibrary` stream into the React library view so Steam games actually render. Add the `'steam'` case to `handleGamePush`, switch `steamLogin` to a non-blocking background sync (D-01), and make the five Library/index.tsx changes plus the `steamCategories` helper so Steam games appear in the unified grid, favourites, and filters. This is the slice where the user first SEES their Steam games (LIB-01; install/playtime/art ride along from earlier waves).

Purpose: Backend data already flows; this connects it to the screen.
Output: Modified GlobalState.tsx, Library/index.tsx, helpers/library.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-steam-library/02-PATTERNS.md
@.planning/phases/02-steam-library/02-RESEARCH.md
@.planning/phases/02-steam-library/02-02-SUMMARY.md

<interfaces>
GlobalState steam state shape (src/frontend/types.ts lines 99-104): `steam: { library: GameInfo[], username?: string|null, login, logout }`.
handleGamePush gog/zoom cases live at GlobalState.tsx lines 963-1000; steamLogin at 676-688; refreshLibrary at 814 accepts `{ runInBackground?, library?, checkForUpdates? }`.
Existing `window.api.refreshLibrary('steam')` already routes to SteamLibraryManager.refresh() (same wiring as zoom).
Library/index.tsx: context destructure ~lines 52-68; storesFilters init ~91-98 (steam stub already present); makeLibrary ~399-440; favourites memo ~357-393; gamesForAlphabetFilter deps ~566-588.
helpers/library.ts category arrays at lines 472-476 (epicCategories...zoomCategories).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add steam case to handleGamePush and background-sync steamLogin</name>
  <files>src/frontend/state/GlobalState.tsx</files>
  <read_first>
    - src/frontend/state/GlobalState.tsx lines 538-560 (handleSuccessfulLogin), 676-700 (steamLogin/steamLogout), 814-832 (refreshLibrary), 963-1000 (handleGamePush gog/zoom cases)
    - .planning/phases/02-steam-library/02-PATTERNS.md (GlobalState.tsx section: steam handleGamePush case + steamLogin fix; "Don't mutate array" note)
    - .planning/phases/02-steam-library/02-RESEARCH.md (Pattern 3, Pattern 5; D-01)
  </read_first>
  <action>
    In `handleGamePush`, after the existing `args.runner === 'zoom'` block (around line 998), add an `else if (args.runner === 'steam')` branch mirroring the gog/zoom pattern: copy `[...this.state.steam.library]`, findIndex by `app_name`, replace at index if found else push, then `this.setState({ steam: { library: [...library], username: this.state.steam.username } })`. Always spread into a new array so React re-renders (RESEARCH anti-pattern: no in-place mutation). Preserve the existing steam state field shape (library, username).

    In `steamLogin` (lines 676-688), replace the `this.handleSuccessfulLogin('steam')` call with a non-blocking background sync per D-01: keep the `setState({ steam: { library: [], username: result.username } })`, then call `this.refreshLibrary({ runInBackground: true, library: 'steam' })`. Remove the leftover `console.log('logging steam')` debug line. Do not change steamLogout.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - GlobalState.tsx contains `args.runner === 'steam'` inside handleGamePush
    - The steam branch uses `[...this.state.steam.library]` and `library: [...library]` (immutable update)
    - steamLogin contains `refreshLibrary({` with `runInBackground: true` and `library: 'steam'`
    - steamLogin no longer calls `handleSuccessfulLogin('steam')`
    - `npm run codecheck` exits 0
  </acceptance_criteria>
  <done>Pushed steam GameInfo updates state.steam.library immutably; login starts a background sync without blocking the UI.</done>
</task>

<task type="auto">
  <name>Task 2: Add steamCategories and integrate steam into Library/index.tsx (5 gaps)</name>
  <files>src/frontend/helpers/library.ts, src/frontend/screens/Library/index.tsx, public/locales/en/translation.json</files>
  <read_first>
    - src/frontend/helpers/library.ts lines 472-476 (category arrays)
    - src/frontend/screens/Library/index.tsx lines 52-68, 91-98, 357-393, 399-440, 566-588
    - .planning/phases/02-steam-library/02-PATTERNS.md (Library/index.tsx Changes 1-5; Pattern 8; Pitfall 4)
    - .planning/phases/02-steam-library/02-RESEARCH.md (Pattern 4; "gate on steam?.username not library length")
  </read_first>
  <action>
    In `helpers/library.ts`: append `export const steamCategories = ['all', 'steam']` after `zoomCategories` (line 476).

    In `Library/index.tsx` make all five integration changes (single task so codecheck catches any missed dependency):
    1. Add `steam` to the `useContext(ContextProvider)` destructure (after `zoom`).
    2. Import `steamCategories` from `frontend/helpers/library` (alongside the existing `zoomCategories` import) and set the storesFilters steam entry to `steamCategories.includes(storedCategory)` (replace the inline `storedCategory === 'all' || storedCategory === 'steam'` stub).
    3. In `makeLibrary()`: after the showZoom block, push `'steam'` to `displayedStores` when `storesFilters['steam'] && steam?.username`; declare `const showSteam = steam?.username && displayedStores.includes('steam')` and `const steamLibrary = showSteam ? steam.library : []`; add `...steamLibrary` to the returned aggregation array. Gate on `steam?.username`, NOT library length (library can be empty during first sync).
    4. In the `favourites` memo: after the `zoom.library.forEach` block add `steam?.library?.forEach((game) => { if (favouriteAppNames.includes(game.app_name)) tempArray.push(game) })`, and add `steam` to that memo's dependency array.
    5. Add `steam?.library` to the `gamesForAlphabetFilter` useMemo dependency array (after `zoom.library`).
    6. First-ever-launch empty state (D-02): in the Library content area (the main game list area, NOT LibraryHeader), add a conditional branch — when `steam?.username && steam?.library?.length === 0 && refreshingInTheBackground`, render `<UpdateComponent message={t('steam.syncing', 'Syncing your Steam library…')} body={t('steam.syncingBody', 'Your games will appear here once the sync completes.')} />`. Follow the existing pattern other runners use to show a full-area loading state when their library is empty-and-loading (import `UpdateComponent` if not already imported). Add the two i18n keys `steam.syncing` and `steam.syncingBody` to `public/locales/en/translation.json` with the English strings above. This satisfies D-02's "empty state with a message" requirement for first-ever login; the LibraryHeader spinner (plan 02-05) covers the cached-refresh case.
  </action>
  <verify>
    <automated>npm run codecheck</automated>
  </verify>
  <acceptance_criteria>
    - helpers/library.ts contains `export const steamCategories = ['all', 'steam']`
    - Library/index.tsx imports `steamCategories`
    - Library/index.tsx contains `steamLibrary` and `showSteam` and spreads `...steamLibrary` in makeLibrary's return
    - Library/index.tsx favourites memo iterates `steam?.library`
    - `steam?.library` appears in the gamesForAlphabetFilter dependency array
    - Library/index.tsx contains the `steam?.library?.length === 0` condition guarding an `UpdateComponent` for the steam first-sync empty state
    - public/locales/en/translation.json contains the `steam.syncing` and `steam.syncingBody` keys
    - `npm run codecheck` exits 0 (no missing-dependency or unused-var errors)
  </acceptance_criteria>
  <done>Steam games render in the unified grid and favourites; filters recompute on steam.library change; codecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main ↔ renderer (IPC) | pushGameToLibrary GameInfo enters React state |
| renderer rendering | GameInfo fields rendered into DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02 | Tampering | steam GameInfo text fields in React | mitigate | values flow through React state into JSX text (auto-escaped); no dangerouslySetInnerHTML introduced here |
| T-2-04 | Elevation of Privilege | frontend imports | mitigate | this plan imports NO steam-user/steam-session; only GameInfo data via existing IPC |
| T-2-SC | Tampering | npm installs | accept | zero new packages in Phase 2 |
</threat_model>

<verification>
- `npm run codecheck` exits 0.
- `grep -c "args.runner === 'steam'" src/frontend/state/GlobalState.tsx` returns 1.
- `grep -c steamLibrary src/frontend/screens/Library/index.tsx` >= 1.
</verification>

<success_criteria>
After Steam login (or app start with cache), Steam games appear in the unified library grid alongside other runners, are favouritable, and respond to sort/filter — driven by the backend push stream with a non-blocking background sync.
</success_criteria>

<output>
Create `.planning/phases/02-steam-library/02-04-SUMMARY.md` when done.
</output>
