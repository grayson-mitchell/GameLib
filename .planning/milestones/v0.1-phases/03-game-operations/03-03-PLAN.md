---
phase: 03-game-operations
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/frontend/screens/Game/GamePage/index.tsx
  - src/frontend/screens/Game/GamePage/components/SettingsButton.tsx
  - src/frontend/screens/Game/GamePage/components/MainButton.tsx
  - src/frontend/screens/Game/GameSubMenu/index.tsx
autonomous: false
requirements: [GAME-02, GAME-03]
user_setup: []

must_haves:
  truths:
    - "On a Steam game's GamePage only Play, Install, and Uninstall are shown — Settings, Move install, Change location, Verify/Repair, and Force Update are hidden entirely (D-04)"
    - "Clicking Install on a not-installed Steam game hands off to Steam directly without GamerLib's install-location modal (D-04)"
    - "Clicking Uninstall on a Steam game goes straight to Steam's own uninstall dialog with no GamerLib confirmation modal (D-05)"
    - "The end-to-end Launch, Install, and Uninstall flows work against a real Steam account"
  artifacts:
    - path: "src/frontend/screens/Game/GamePage/index.tsx"
      provides: "Steam-only action surface + steam install bypass of the install modal"
      contains: "runner === 'steam'"
    - path: "src/frontend/screens/Game/GamePage/components/MainButton.tsx"
      provides: "Steam guard so not-installed install click routes to handleInstall instead of openInstallGameModal"
      contains: "steam"
    - path: "src/frontend/screens/Game/GameSubMenu/index.tsx"
      provides: "isSteam guard hiding Move/Change/Verify/Force-Update; direct Steam uninstall"
      contains: "isSteam"
    - path: "src/frontend/screens/Game/GamePage/components/SettingsButton.tsx"
      provides: "Steam runner guard returning null"
      contains: "steam"
  key_links:
    - from: "GameSubMenu Uninstall button (steam)"
      to: "window.api.uninstall"
      via: "direct call, bypassing UninstallModal (D-05)"
      pattern: "window\\.api\\.uninstall"
    - from: "MainButton install button (steam, not-installed)"
      to: "GamePage handleInstall"
      via: "runner==='steam' skips openInstallGameModal and calls handleInstall (D-04)"
      pattern: "handleInstall"
    - from: "GamePage handleInstall (steam, not-installed)"
      to: "window.api.install"
      via: "direct call, bypassing openInstallGameModal (D-04)"
      pattern: "window\\.api\\.install"
---

<objective>
Deliver the clean Steam action surface (D-04, D-05) and verify the whole Phase 3 slice end-to-end. For Steam games the GamePage and GameSubMenu show only Play, Install, and Uninstall — every Heroic action that does not apply to Steam (Settings, Move install, Change location, Verify/Repair, Force Update) is hidden entirely, not greyed-out. Install and Uninstall hand off straight to Steam without GamerLib's own modals.

This plan makes the install/uninstall slice (backend in 03-02) truly end-to-end from the UI, then a human checkpoint confirms Launch (GAME-01), Install (GAME-02), and Uninstall (GAME-03) work against a real Steam account, with focus-driven badge reconciliation (D-01).

Purpose: Final UX wiring + Phase 3 acceptance.
Output: Steam-only button surface; direct Steam hand-off for install/uninstall; signed-off end-to-end behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-operations/03-CONTEXT.md
@.planning/phases/03-game-operations/03-PATTERNS.md

<interfaces>
<!-- Contracts extracted from codebase — no exploration needed. -->

GamePage — src/frontend/screens/Game/GamePage/index.tsx:
  line 217-218: existing guard `runner !== 'sideload' && runner !== 'steam' &&` already skips getInstallInfo for steam
  line 429: `{!isBrowserGame && <SettingsButton gameInfo={gameInfo} />}` — render site to gate on runner !== 'steam'
  line 605-630: handleInstall(is_installed) — not-installed path calls handleModal() (line 612 → openInstallGameModal)

MainButton — src/frontend/screens/Game/GamePage/components/MainButton.tsx:
  lines 205-237: install button. onClick at 208-219:
    if (!is_installed && !is.queued) { openInstallGameModal({ appName, runner, gameInfo, action: 'install' }); return }  // intercepts BEFORE handleInstall
    handleInstall(is_installed)
  // IMPORTANT: for a not-installed game this opens the modal DIRECTLY and never calls handleInstall.
  // So the steam bypass MUST be added here, not only in GamePage.handleInstall.

GameSubMenu — src/frontend/screens/Game/GameSubMenu/index.tsx:
  line 88: const isSideloaded = runner === 'sideload'   // add `const isSteam = runner === 'steam'` next to it
  line 320-327: Uninstall button → onClick setShowUninstallModal(true) (NOT gated by isSideloaded)
  line 328, 338, 347, 356: blocks gated `!isSideloaded && !isThirdPartyManaged` (ForceUpdate, MoveInstall, ChangeInstall, Verify/Repair)
  line 291-298: <UninstallModal .../> rendered when showUninstallModal

UninstallModal calls — window.api.uninstall(appName, runner, deletePrefix, deleteSettings)
Install helper call shape — window.api.install({ appName, path, installDlcs, sdlList, installLanguage, runner, platformToInstall, gameInfo, build, branch })

SettingsButton — src/frontend/screens/Game/GamePage/components/SettingsButton.tsx:
  line 13: `if (!gameInfo.is_installed) { return null }` — extend with `|| gameInfo.runner === 'steam'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Steam install hand-off (GamePage + MainButton) + hide Settings</name>
  <files>src/frontend/screens/Game/GamePage/index.tsx, src/frontend/screens/Game/GamePage/components/MainButton.tsx, src/frontend/screens/Game/GamePage/components/SettingsButton.tsx</files>
  <read_first>
    - src/frontend/screens/Game/GamePage/index.tsx (the file being modified — handleInstall at 605-630, SettingsButton render at 429, existing runner!=='steam' guard at 217-218)
    - src/frontend/screens/Game/GamePage/components/MainButton.tsx (the file being modified — install onClick at lines 205-219 intercepts not-installed clicks with openInstallGameModal BEFORE handleInstall is reached)
    - src/frontend/screens/Game/GamePage/components/SettingsButton.tsx (the 27-line component to guard)
    - src/frontend/helpers/library.ts lines 40-100 (install helper shape; window.api.install params)
    - .planning/phases/03-game-operations/03-PATTERNS.md "GamePage/index.tsx", "MainButton.tsx", and "SettingsButton.tsx" sections
    - .planning/phases/03-game-operations/03-CONTEXT.md D-04 (only Play/Install/Uninstall; unsupported actions hidden entirely)
  </read_first>
  <action>
    Three coordinated edits so the not-installed Steam Install click bypasses the install-location modal (D-04):
    (1) MainButton.tsx — in the install button onClick (lines 208-219), guard the `openInstallGameModal` branch so it does NOT fire for Steam: change the condition to `if (!is_installed && !is.queued && gameInfo.runner !== 'steam')`. For Steam this falls through to `handleInstall(is_installed)`, routing the click into GamePage's steam-aware handler instead of opening the modal. (MainButton intercepts not-installed clicks before handleInstall, so the steam bypass MUST live here — modifying only GamePage.handleInstall would never run for a not-installed Steam game.)
    (2) GamePage/index.tsx — in `handleInstall(is_installed)` add a Steam branch BEFORE the existing `if (!is_installed && !isInstalling) return handleModal()` line: when `gameInfo.runner === 'steam' && !is_installed`, call `window.api.install({ appName, path: '', runner: 'steam', installDlcs: [], sdlList: [], installLanguage: 'en-US', platformToInstall: 'Windows', gameInfo })` and return — the backend SteamGame.install fires steam://install (D-04). The Play path needs no change (handlePlay → launch helper → window.api.launch → SteamGame.launch from 03-01).
    (3) SettingsButton.tsx — change the early-return guard to `if (!gameInfo.is_installed || gameInfo.runner === 'steam') { return null }` so the Settings cog never renders for Steam (D-04). Single guard inside the component is enough — do not also gate at the GamePage call site.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GamerLib && npm run codecheck 2>&1 | tail -10 && grep -rn "steam" src/frontend/screens/Game/GamePage/components/MainButton.tsx src/frontend/screens/Game/GamePage/components/SettingsButton.tsx && grep -n "runner === 'steam'" src/frontend/screens/Game/GamePage/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MainButton.tsx install onClick skips openInstallGameModal for runner==='steam' (condition includes gameInfo.runner !== 'steam') and falls through to handleInstall
    - GamePage handleInstall calls window.api.install directly for steam not-installed games (no openInstallGameModal/handleModal)
    - The steam install branch passes path:'' and runner:'steam' (backend ignores path; fires steam://install)
    - SettingsButton.tsx returns null when gameInfo.runner === 'steam'
    - Play path unchanged
    - `npm run codecheck` passes; grep finds the steam guards in all three files
  </acceptance_criteria>
  <done>Steam GamePage hides Settings and hands install straight to Steam without the location modal, via both MainButton and handleInstall.</done>
</task>

<task type="auto">
  <name>Task 2: GameSubMenu — isSteam guard + direct Steam uninstall (D-05)</name>
  <files>src/frontend/screens/Game/GameSubMenu/index.tsx</files>
  <read_first>
    - src/frontend/screens/Game/GameSubMenu/index.tsx (the file being modified — isSideloaded at 88, guarded blocks at 328/338/347/356, Uninstall button at 320, UninstallModal at 291)
    - src/frontend/components/UI/UninstallModal/index.tsx lines 89-103 (window.api.uninstall(appName, runner, deletePrefix, deleteSettings) signature)
    - .planning/phases/03-game-operations/03-PATTERNS.md "GameSubMenu/index.tsx" section
    - .planning/phases/03-game-operations/03-CONTEXT.md D-04 (hide Move/Change/Verify/Force-Update) and D-05 (uninstall delegates straight to Steam, no GamerLib confirm)
  </read_first>
  <action>
    Add `const isSteam = runner === 'steam'` next to `isSideloaded` (line 88). Add `&& !isSteam` to each of the four guarded blocks at lines 328 (Force Update), 338 (Move install), 347 (Change install location), 356 (Verify and Repair) so they are hidden for Steam (D-04). Keep the Uninstall button visible. For the Uninstall button (line 320-327): change its onClick so that for Steam it calls `window.api.uninstall(appName, runner, false, false)` directly (no prefix/settings deletion, which are Wine concepts) instead of `setShowUninstallModal(true)` — Steam shows its own confirmation dialog (D-05). For non-steam runners keep the existing `setShowUninstallModal(true)` behavior. Leave the Edit and Add/Remove-shortcut buttons as-is (they are acceptable for Steam). Do not render UninstallModal for steam (the direct call replaces it).
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GamerLib && npm run codecheck 2>&1 | tail -10 && grep -n "isSteam" src/frontend/screens/Game/GameSubMenu/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - GameSubMenu defines `const isSteam = runner === 'steam'`
    - Force Update, Move install, Change install location, Verify/Repair blocks all gated with `&& !isSteam` (hidden for Steam)
    - Uninstall button for steam calls window.api.uninstall(appName, runner, false, false) directly — no UninstallModal opened (D-05)
    - Non-steam uninstall still opens UninstallModal (unchanged)
    - `npm run codecheck` passes; grep finds isSteam usages
  </acceptance_criteria>
  <done>Steam submenu shows only the supported actions; uninstall delegates straight to Steam's own dialog.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification on a real Steam account</name>
  <files>(verification only — no files modified)</files>
  <what-built>
    Full Phase 3 slice: SteamGame.launch/install/uninstall fire steam:// URLs; focus-driven ACF re-read reconciles install badges; Steam GamePage shows only Play/Install/Uninstall with Steam-native hand-off (no GamerLib install/uninstall modals).
  </what-built>
  <how-to-verify>
    1. Run the app (`npm run dev` or the project's dev launcher) and open the Library; confirm Steam games appear.
    2. Open an INSTALLED Steam game's GamePage. Confirm only Play, Uninstall (and Edit/Shortcut) show — NO Settings cog, and the submenu has NO Move install / Change location / Verify-Repair / Force Update entries.
    3. Click Play. Confirm a brief "Opening in Steam…" toast appears and Steam launches the game. For a Windows-only game on Linux, confirm Steam (Proton) runs it — GamerLib must NOT show any Wine/Proton prompt (GAME-04).
    4. Open a NOT-installed Steam game. Click Install. Confirm NO GamerLib install-location modal appears and Steam's own install dialog opens (steam://install). Complete or cancel the install in Steam, then tab back to GamerLib — confirm the install badge updates after window focus (D-01), with no manual refresh.
    5. On an installed game, click Uninstall. Confirm NO GamerLib confirmation modal appears — Steam's own uninstall dialog opens directly (D-05). Complete uninstall in Steam, tab back, confirm the badge flips to not-installed on focus.
  </how-to-verify>
  <action>Human-only verification of the end-to-end Launch/Install/Uninstall flows against a real Steam account. No code changes — pause for sign-off after running the five checks in how-to-verify.</action>
  <verify><human-check>All five behaviors in how-to-verify pass against a real Steam account.</human-check></verify>
  <done>User confirms Launch, Install, Uninstall, and focus-driven badge reconciliation all work; GAME-01/02/03/04 accepted.</done>
  <resume-signal>Type "approved" if all five behaviors pass, or describe what failed (which step, observed vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → renderer | Button clicks in GamePage/MainButton/GameSubMenu; appName is component prop, forwarded to window.api.install/uninstall |
| renderer → IPC → backend | install/uninstall calls cross into backend SteamGame (validated by buildSteamProtocolUrl from 03-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Injection | window.api.install/uninstall → SteamGame steam:// URL | mitigate | Backend `buildSteamProtocolUrl` (03-01) enforces numeric appId; the frontend passes appName unchanged, so the backend guard remains the chokepoint. |
| T-03-05 | Spoofing / UX confusion | Direct uninstall bypassing GamerLib confirm | accept | Intentional per D-05 — Steam's own confirmation dialog is the single source of truth; no destructive action occurs without Steam's prompt. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase. No package-legitimacy gate needed. |
</threat_model>

<verification>
- `npm run codecheck` — clean.
- grep confirms steam guards in GamePage + MainButton + SettingsButton and `isSteam` in GameSubMenu.
- Human checkpoint (Task 3) signs off Launch/Install/Uninstall + focus reconciliation on a real Steam account.
</verification>

<success_criteria>
- Steam GamePage/GameSubMenu expose only Play/Install/Uninstall (+ Edit/Shortcut); all Wine-specific actions hidden.
- Install and Uninstall hand off to Steam directly without GamerLib modals (MainButton + handleInstall for install; GameSubMenu for uninstall).
- End-to-end Launch/Install/Uninstall verified on a real Steam account with focus-driven badge reconciliation.
</success_criteria>

<output>
Create `.planning/phases/03-game-operations/03-03-SUMMARY.md` when done.
</output>
