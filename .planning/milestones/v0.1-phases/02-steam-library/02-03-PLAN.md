---
phase: 02-steam-library
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/backend/storeManagers/steam/games.ts
  - src/backend/storeManagers/steam/library.ts
  - src/backend/storeManagers/steam/__tests__/games.test.ts
autonomous: true
requirements: [LIB-04]

must_haves:
  truths:
    - "SteamGame.getGameInfo() returns the current library entry synchronously and triggers a lazy metadata fetch when art_cover is empty"
    - "The lazy fetch pulls title, description, genres, and cover art from the Steam store appdetails API and CDN"
    - "Fetched metadata is cached indefinitely in steamMetadataStore and pushed to the frontend via pushGameToLibrary"
    - "Concurrent fetches for the same appId fire only one network request"
    - "SteamLibraryManager.getGameInfo() delegates lazy fetching consistently (no scavenger hunt for the executor)"
  artifacts:
    - path: "src/backend/storeManagers/steam/games.ts"
      provides: "implemented getGameInfo + fetchMetadataIfNeeded with pendingFetches dedup"
      contains: "fetchMetadataIfNeeded"
  key_links:
    - from: "src/backend/storeManagers/steam/games.ts"
      to: "Steam store API"
      via: "axios appdetails GET"
      pattern: "store.steampowered.com/api/appdetails"
    - from: "src/backend/storeManagers/steam/games.ts"
      to: "frontend"
      via: "sendFrontendMessage push of updated GameInfo"
      pattern: "sendFrontendMessage.*pushGameToLibrary"
---

<objective>
Implement lazy, cached Steam store metadata (D-04, D-05): `SteamGame.getGameInfo()` returns the in-memory entry immediately and, when artwork is missing, fires a non-blocking `fetchMetadataIfNeeded()` that hits the Steam appdetails API, populates title/description/genres/cover art, caches indefinitely, and pushes the enriched GameInfo to the frontend. Concurrency is deduplicated with `pendingFetches`. This delivers LIB-04.

Purpose: Completes the backend data layer so cards can show real names, art, and genres without a 500-call upfront storm.
Output: Implemented `games.ts`, a delegating `library.getGameInfo()` path, and real assertions in `games.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-steam-library/02-RESEARCH.md
@.planning/phases/02-steam-library/02-PATTERNS.md
@.planning/phases/02-steam-library/02-02-SUMMARY.md

<interfaces>
From 02-01: `steamMetadataStore` (CacheStore keyed by appId), `SteamMetadataCacheEntry { art_cover, art_square, extra }` from './electronStores'.
From 02-02 state.ts: `library: Map<string, GameInfo>`, `pendingFetches: Set<string>`.
sendFrontendMessage from '../../ipc'. axios already in project (1.13.5). Logger logWarning, LogPrefix.Steam.

Steam store API (verified live): GET https://store.steampowered.com/api/appdetails?appids={id} → resp.data[id].data = { name, short_description, genres: [{ id, description }] }. May be { success:false } / undefined when game delisted.
CDN artwork: https://cdn.cloudflare.steamstatic.com/steam/apps/{id}/header.jpg and capsule_616x353.jpg.

SteamGame stub (current games.ts): constructor(appId), getGameInfo() currently throws — replace it; keep other Phase-1 stub methods (install/launch/etc.) for Phase 3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SteamGame.getGameInfo() + fetchMetadataIfNeeded() with dedup</name>
  <files>src/backend/storeManagers/steam/games.ts, src/backend/storeManagers/steam/__tests__/games.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/games.ts (current stub — replace getGameInfo body, keep the rest)
    - src/backend/storeManagers/steam/state.ts (shared library Map + pendingFetches)
    - .planning/phases/02-steam-library/02-RESEARCH.md (Pattern 2 getGameInfo + fetchMetadataIfNeeded; Code Examples Steam Store API; Pitfall 5 dedup; Security Domain V5)
    - src/backend/storeManagers/steam/__tests__/games.test.ts (Wave 0 todos to convert)
  </read_first>
  <behavior>
    - getGameInfo() returns library.get(this.appId) synchronously (or {} as GameInfo when absent).
    - When the existing entry has empty art_cover, getGameInfo() invokes fetchMetadataIfNeeded once (fire-and-forget) and does not block.
    - fetchMetadataIfNeeded(): if pendingFetches already has appId, returns immediately without a second axios call.
    - On success: builds updated GameInfo with title from data.name, art_cover = CDN header.jpg, art_square = CDN capsule_616x353.jpg, extra.genres = data.genres.map(g=>g.description), extra.about.description = data.short_description (stored as plain string only — never HTML), preserving existing extra.steamPlaytimeMinutes.
    - On success: steamMetadataStore.set(appId, {art_cover,art_square,extra}), library.set(appId, updated), sendFrontendMessage('pushGameToLibrary', updated).
    - On axios rejection or resp.data[id].data undefined: logs warning, no throw, pendingFetches entry removed in finally.
  </behavior>
  <action>
    Replace `getGameInfo()` per RESEARCH Pattern 2: read `existing = library.get(this.appId)`; if absent return `{} as GameInfo`; if `!existing.art_cover` call `void this.fetchMetadataIfNeeded(existing)`; return `existing`.

    Add private async `fetchMetadataIfNeeded(current: GameInfo)`: guard with the shared `pendingFetches` Set (return if `has(appId)`, else `add(appId)` BEFORE the await — mitigates T-2-03), `axios.get(\`https://store.steampowered.com/api/appdetails?appids=${this.appId}\`)`, read `data = resp.data?.[this.appId]?.data`, return if falsy. Build `artBase = https://cdn.cloudflare.steamstatic.com/steam/apps/${this.appId}`, set art_cover=`${artBase}/header.jpg`, art_square=`${artBase}/capsule_616x353.jpg`. Build `extra` preserving `current.extra` (especially steamPlaytimeMinutes and reqs:[]) plus `about: { description: data.short_description ?? '', shortDescription: data.short_description ?? '' }` and `genres: (data.genres ?? []).map(g => g.description)`. IMPORTANT (T-2-02): treat short_description strictly as a string value; do not parse/inject it as HTML anywhere in backend. Cache via `steamMetadataStore.set(this.appId, { art_cover, art_square, extra })`, `library.set(this.appId, updated)`, `sendFrontendMessage('pushGameToLibrary', updated)`. Wrap network logic in try/catch (logWarning) and `finally { pendingFetches.delete(this.appId) }`.

    Convert all Wave 0 it.todo stubs in games.test.ts into real tests: mock axios.get to resolve a fixture for appid 570; assert synchronous return, single network call under concurrent invocation (call getGameInfo twice, assert axios.get called once), populated fields, steamMetadataStore.set called, sendFrontendMessage push, and a rejection path that does not throw.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=steam/games</automated>
  </verify>
  <acceptance_criteria>
    - games.ts contains `fetchMetadataIfNeeded` and `pendingFetches`
    - games.ts adds appId to pendingFetches before the axios await (grep order: `pendingFetches.add` appears before `axios.get`)
    - games.ts contains `cdn.cloudflare.steamstatic.com/steam/apps/` and `store.steampowered.com/api/appdetails`
    - games.ts does NOT contain `dangerouslySetInnerHTML` or any HTML parsing of short_description
    - `grep -c "it.todo" src/backend/storeManagers/steam/__tests__/games.test.ts` returns 0
    - `npm test -- --testPathPattern=steam/games` exits 0 including the dedup-asserts-single-call test
  </acceptance_criteria>
  <done>Lazy metadata fetch works with single-flight dedup, indefinite cache, and frontend push; short_description handled as plain text; all games tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate SteamLibraryManager.getGameInfo() to the lazy path and verify integration</name>
  <files>src/backend/storeManagers/steam/library.ts, src/backend/storeManagers/steam/__tests__/games.test.ts</files>
  <read_first>
    - src/backend/storeManagers/steam/library.ts (getGameInfo from plan 02)
    - src/backend/storeManagers/steam/games.ts (getGameInfo from Task 1)
    - .planning/phases/02-steam-library/02-PATTERNS.md (games.ts "Library Map sharing" note — delegate vs co-locate)
  </read_first>
  <action>
    Make `SteamLibraryManager.getGameInfo(appName)` delegate to the lazy path so a metadata fetch is triggered regardless of whether callers go through the library manager or `getGame(id)`. Implement it to construct `new SteamGame(appName).getGameInfo()` (which reads the shared Map and fires the lazy fetch), keeping the existing steamLibraryStore fallback for entries not yet in the in-memory Map. Ensure `getGame(id)` continues returning `new SteamGame(id)`. This guarantees the frontend `getGameInfo(appName,'steam')` IPC path (used by GameCard on visibility) reaches `fetchMetadataIfNeeded`.

    Add an integration test in games.test.ts: through `new SteamLibraryManager().getGameInfo(appId)` with an in-Map entry lacking art_cover, assert axios.get fires (lazy trigger propagates via delegation).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=steam</automated>
  </verify>
  <acceptance_criteria>
    - library.ts getGameInfo delegates to SteamGame.getGameInfo (grep `new SteamGame` inside getGameInfo region)
    - Integration test asserts the lazy fetch is reachable through the library manager
    - `npm test -- --testPathPattern=steam` exits 0
    - `npm run codecheck` exits 0
  </acceptance_criteria>
  <done>Library-manager and per-game entry points both trigger lazy metadata; full steam test suite green; codecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Steam store API ↔ main process | untrusted HTTP JSON (name, short_description, genres) |
| main ↔ renderer | enriched GameInfo pushed via IPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02 | Tampering | short_description from appdetails | mitigate | stored as plain string only; never HTML-parsed/injected in backend; rendered as React text in plan 05 |
| T-2-03 | Tampering | concurrent metadata fetch storm | mitigate | module-level pendingFetches Set single-flight + indefinite metadata cache (D-05) (this plan, Task 1) |
| T-2-04 | Elevation of Privilege | axios/steam network in backend | mitigate | fetch stays in src/backend; renderer only receives GameInfo data |
| T-2-SC | Tampering | npm installs | accept | zero new packages in Phase 2 |
</threat_model>

<verification>
- `npm test -- --testPathPattern=steam` exits 0 (library + games suites), no it.todo remaining.
- `npm run codecheck` exits 0.
- games.ts contains no dangerouslySetInnerHTML / HTML parsing of store text.
</verification>

<success_criteria>
Steam games gain real title, cover art, description, and genres lazily on first access, cached indefinitely, deduplicated under concurrency, and pushed to the frontend. LIB-04 backend behavior verified by green unit tests.
</success_criteria>

<output>
Create `.planning/phases/02-steam-library/02-03-SUMMARY.md` when done.
</output>
