---
phase: 02-steam-library
plan: 06
type: execute
wave: 5
depends_on: ["02-04", "02-05"]
files_modified: []
autonomous: false
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]

must_haves:
  truths:
    - "A real Steam account's games appear in the unified library grid"
    - "Install state, playtime, and metadata display correctly on real data"
  artifacts: []
  key_links: []
---

<objective>
Manual QA gate for Phase 2. The automated unit suites cover backend behavior with mocks; the success criteria for LIB-01 through LIB-04 require a real Steam account + CM connection + on-disk ACF manifests, which cannot be exercised by Jest. This plan is a single human-verify checkpoint confirming the end-to-end experience before phase verification.

Purpose: Confirm the integrated feature works with real Steam data (the part mocks cannot prove).
Output: Human sign-off; any issues become gap-closure inputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-steam-library/02-VALIDATION.md
@.planning/phases/02-steam-library/02-UI-SPEC.md
@.planning/phases/02-steam-library/02-04-SUMMARY.md
@.planning/phases/02-steam-library/02-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual QA of LIB-01..04 with a real Steam account</name>
  <files>none (verification only)</files>
  <what-built>
    Backend Steam library sync (getUserOwnedApps + ACF install detection + lazy store metadata) and the full frontend integration (unified grid, playtime, store logo, sync spinner, stale indicator, manual refresh). Automated unit suites for steam/library and steam/games pass.
  </what-built>
  <action>
    Pause for human verification. First run the automated gate, then walk the tester through the manual checks in <how-to-verify>. Do not auto-approve — record the tester's result and, on failure, capture the specific LIB id and symptom for gap closure.
  </action>
  <how-to-verify>
    Pre-step: run `npm test -- --testPathPattern=steam` and `npm run codecheck` — both must pass before manual QA.

    Then, with a real Steam account already logged in (Phase 1) on a machine with Steam installed:
    1. LIB-01 — Launch GamerLib. Confirm Steam games appear in the main library grid mixed with Epic/GOG/Amazon/Zoom games (use the 'All' category and the Steam store filter).
    2. LIB-02 — Find a game you own but have NOT installed: it should show the download/not-installed treatment (grayscale cover). Find an installed game: full colour + "Installed" badge. (Install state is read from ACF; accurate even offline.)
    3. LIB-03 — Confirm playtime shows as "N hours" for a played game, "1 hour" singular where applicable, and "Never played" for an unplayed game.
    4. LIB-04 — Scroll through a large library; confirm cards start as AppID + skeleton then fade in real title, cover art, and (on the game page) description/genres within a couple of seconds per card.
    5. D-02/D-09 — Trigger the manual Refresh button (spinner animates). Then disconnect network and relaunch: cached library still shows with a "last synced X ago" stale indicator.
  </how-to-verify>
  <verify>
    <human-check>Tester confirms LIB-01, LIB-02, LIB-03, LIB-04 visually pass on real Steam data after `npm test -- --testPathPattern=steam` and `npm run codecheck` both exit 0.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=steam` exits 0 and `npm run codecheck` exits 0 (run before sign-off)
    - Tester confirms each of LIB-01, LIB-02, LIB-03, LIB-04 visually passes, or files specific issues
  </acceptance_criteria>
  <done>Human has signed off that all four phase success criteria work on a real Steam account, or specific failures are recorded for gap closure.</done>
  <resume-signal>Type "approved" if all four behaviors pass, or list the specific failures (which LIB id, what was wrong) to feed gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Denial of Service | ACF parse | mitigate | verified in steam/library tests (corrupt-ACF skipped); manual QA confirms no crash with real manifests |
| T-2-02 | Tampering | store description rendering | mitigate | confirmed no dangerouslySetInnerHTML; manual QA observes text renders escaped |
| T-2-03 | Tampering | metadata fetch storm | mitigate | pendingFetches dedup verified in steam/games tests |
| T-2-04 | Elevation of Privilege | steam-user surface | mitigate | steam-user confined to backend; no renderer imports |
| T-2-SC | Tampering | npm installs | accept | zero new packages in Phase 2 |
</threat_model>

<verification>
Human confirmation of LIB-01..04 on real Steam data, gated behind passing automated steam suites + codecheck.
</verification>

<success_criteria>
All four phase success criteria observed working with a real Steam account, or specific failures captured for gap closure.
</success_criteria>

<output>
Create `.planning/phases/02-steam-library/02-06-SUMMARY.md` when done.
</output>
