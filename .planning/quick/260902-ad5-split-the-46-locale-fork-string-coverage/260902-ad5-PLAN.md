---
phase: quick-260902-ad5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md
  - .planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md
  - .planning/todos/pending/2026-09-01-non-english-catalogs-are-unrebranded-2117-heroic-strings.md
  - .planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md
  - .planning/quick/260902-ad5-split-the-46-locale-fork-string-coverage/260902-ad5-PLAN.md
  - .planning/quick/260902-ad5-split-the-46-locale-fork-string-coverage/260902-ad5-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The 46-locale zero-fork-string-coverage gap is owned by exactly ONE todo record"
    - "The 2026-08-28 todo is narrowed to de/fr and no longer claims the 46-locale scope"
    - "Every record in the pointer chain that references the residue points at its CURRENT owner, not only at where it was handed in the past"
    - "The closed 2026-08-06 todo is amended by APPEND ONLY, measured by diff, with no existing line altered or re-dated"
    - "The record states plainly that this split deliberately reverses part of quick 260902-9wt, at the operator's request, and does not call 260902-9wt a mistake"
    - "Everything committed is what was gated — every content gate reads `git show HEAD:<path>`"
  artifacts:
    - path: ".planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md"
      provides: "Sole owner of the 46-locale x 204-key fork-string coverage gap"
      contains: "status: pending"
    - path: ".planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md"
      provides: "de/fr-only 80-key gap, the 401 blocker, and the lint-gate blindness diagnosis"
      contains: "## Split out 2026-09-02"
    - path: ".planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md"
      provides: "Append-only forward pointer from the closed decision record to the residue's current owner"
      contains: "## Later addition — 2026-09-02, quick task 260902-ad5"
    - path: ".planning/STATE.md"
      provides: "Quick-task row 260902-ad5"
      contains: "260902-ad5"
  key_links:
    - from: ".planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md"
      to: ".planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md"
      via: "the appended `## Later addition` note naming the residue's current owner by filename"
      pattern: "2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage"
    - from: ".planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md"
      to: ".planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md"
      via: "the `## Split out 2026-09-02` section naming the successor by filename"
      pattern: "2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage"
    - from: ".planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md"
      to: ".planning/todos/pending/2026-09-01-non-english-catalogs-are-unrebranded-2117-heroic-strings.md"
      via: "the `## Related but distinct` prose distinguishing FORK strings from UPSTREAM strings"
      pattern: "2026-09-01-non-english-catalogs-are-unrebranded"
    - from: ".planning/todos/pending/2026-09-01-non-english-catalogs-are-unrebranded-2117-heroic-strings.md"
      to: ".planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md"
      via: "a bullet appended to its existing `## Related` section (the reverse direction)"
      pattern: "2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage"
---

<objective>
Split the 46-locale fork-string coverage gap out of the `2026-08-28` todo into its own
pending todo, narrow the `2026-08-28` todo back to `de`/`fr`, and forward-point the closed
`2026-08-06` decision record at the residue's new owner — so exactly ONE record owns each
gap and no pointer in the chain is stale.

Purpose: one hour ago quick `260902-9wt` WIDENED the `2026-08-28` todo to absorb the
46-locale gap. The operator has now asked for that gap as its own record. This task
therefore deliberately REVERSES part of `260902-9wt`. That reversal must be stated plainly
in the affected files. `260902-9wt` was NOT wrong and must never be described as a mistake —
the operator simply wants the bigger gap tracked separately.

**The hazard this plan exists to prevent is DUPLICATE OWNERSHIP.** Filing the new todo
without narrowing the old one leaves two records claiming the same gap — the exact defect
this session has spent five tasks correcting. **The split is only correct if it is a MOVE,
not a COPY.** Gate G2 is the thing that proves it.

**The second hazard, and the reason Task 2b exists: a pointer that is TECHNICALLY TRUE while
its target no longer owns what was handed over.** The closed `2026-08-06` todo's CLOSURE
RECORD hands the residue to the `2026-08-28` todo. That sentence was true when `260902-9wt`
wrote it and stays true as dated history — but after this split the `2026-08-28` todo owns
only the de/fr half. One append-only sentence is cheap insurance against a reader trusting
the pointer and stopping there. Gate G2b makes "append only" MEASURED rather than asserted.

Output: one new pending todo, one narrowed pending todo, one reverse cross-link, one
append-only amendment to a closed record, a quick-task SUMMARY, and one `.planning/STATE.md`
row — all in a SINGLE commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md
@.planning/todos/pending/2026-09-01-non-english-catalogs-are-unrebranded-2117-heroic-strings.md
@.planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md
@.planning/todos/pending/2026-09-02-d-35-19-15-sibling-apex-seeding-unqueued-and-unreproducible-.md

## Hard constraints (violating any of these fails the task)

**RECORDS ONLY.** Do NOT edit anything under `src/`, `meta/`, or `public/locales/`. Do NOT
run `pnpm machine-fill-gamelib` or `pnpm i18n`. Do NOT edit `.planning/ROADMAP.md`.

**A CONCURRENT SESSION IS ACTIVE.** `.planning/ROADMAP.md` (~96 uncommitted lines, Phase 40)
and exactly one uncommitted `.planning/STATE.md` line ("Phase 40 added 2026-09-02", inserted
under `### Roadmap Evolution`) are NOT ours and must not be swept into our commit.

**BANNED commands:** `git stash`, `git reset`, `git checkout -- <file>` (fires the
post-checkout hook and triggers a binary download — use `cp`), `git add -A`, `git add .`,
`gsd-sdk query commit`, and `git commit -- .planning/STATE.md` (`--only` takes the WORKING
TREE, not the index).

**Do not re-derive any measurement.** Every number in Task 1 was measured 2026-09-02 and is
supplied below verbatim. Counting locales or keys is out of scope.

## Gate rules proven on this repo TODAY — all five are mandatory

1. **Gates read `git show HEAD:<path>`, never `<path>`.** In `260902-9wt`, `git mv` staged a
   file before its content edits, nothing re-staged it, and commit `eadaae560` shipped a bare
   rename while a working-tree gate passed. It cost two extra commits. Every content gate in
   Task 3 reads HEAD.
2. **Order-independent backstop.** For every file we commit, `diff <(git show HEAD:"$f") "$f"`
   must be empty. (`.planning/STATE.md` is the one documented exception — see G9.)
3. **Frontmatter greps must be frontmatter-scoped** via `awk '/^---$/{n++; next} n==1'`, or
   they false-RED the moment prose legitimately quotes an old value as history.
4. **Negative greps must be line-anchored to headings.** A gate asserting `## Widened
   2026-09-02` is gone will false-RED if the Amendment note quotes that heading at line start.
   Anchor with `^## Widened`, and write historical references to it INLINE (backticked,
   mid-sentence) — never at the start of a line.
5. **"Append only" is a measurement, not a promise.** The binding form is
   `diff <(git show HEAD~1:"$f") <(git show HEAD:"$f")` producing ZERO `<` lines. Any gate
   that merely greps for the new text would pass on a rewrite that silently dropped a
   paragraph.
</context>

<tasks>

<task type="auto">
  <name>Task 1: File the new 46-locale todo and wire its reverse cross-link</name>
  <files>
.planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md (create)
.planning/todos/pending/2026-09-01-non-english-catalogs-are-unrebranded-2117-heroic-strings.md (append one bullet)
  </files>
  <action>
Create the new todo at exactly
`.planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md`.
The filename is load-bearing: Task 2's `## Split out` section, Task 2b's forward pointer and
Task 3's anti-duplication gate all reference it. Do not vary it.

Follow the shape of `2026-09-02-d-35-19-15-sibling-apex-seeding-unqueued-and-unreproducible-.md`
(the format exemplar for a newly-filed todo blocked on an external precondition): frontmatter,
an H1, a "why this is being filed now" section, a "what the gap actually is" section, an
explicit blocker section, a "related but distinct" section, and sources.

**Frontmatter (exact keys):**

    created: 2026-09-02
    title: "46 of 49 locales have ZERO fork-string coverage — `gamelib.json` exists only for en/de/fr, leaving 204 translatable keys x 46 locales unfilled"
    area: i18n
    status: pending
    severity: medium
    resolves_phase: ""
    found_by: "Quick task 260902-9wt (the 2026-08-06 Phase-34.8 i18n closure), split out into its own record by quick 260902-ad5 at the operator's request"
    blocked_by: "OPERATOR ACTION, not engineering — a valid RAW Anthropic API key. `ANTHROPIC_API_KEY` is set but 401s; an OAuth/subscription token will not work."
    files:
      - public/locales/
      - public/locales/en/gamelib.json
      - meta/machineFillGamelib.ts
      - meta/i18nGlossary.json
      - meta/lintTranslations.ts

`severity: medium` matches the adjacent `2026-09-01` unrebranded-catalogs todo, which has
the same class of user-visible impact. `blocked_by` prose must name the precondition and who
can supply it — the exemplar's own rule, and the correction applied to the 2026-08-23 todo:
a `blocked_by` that merely restates the symptom makes a todo permanently un-actionable.

**MANDATORY SENTINEL LINE.** The body must contain this line VERBATIM, exactly once, and it
must appear in NO other file under `.planning/todos/`:

    **Sole owner:** this todo OWNS the 46-locale x 204-key fork-string coverage gap.

Gate G10 counts files containing `OWNS the 46-locale` across the whole todo tree and requires
exactly 1. That is what turns "single owner" from prose into a measurement.

**Body — all figures measured 2026-09-02. Copy them; do not recount.**

- `public/locales/` holds **49** locale directories. Only **en**, **de** and **fr** carry a
  `gamelib.json` at all. **46 locales have never had a single fork string filled.**
- `en/gamelib.json` has **210** keys, of which **204 are translatable**. The other 6 are
  empty in English too — a separate, pre-existing English AUTHORING gap (the `redeemKey.*`
  one), and it is owned by the `2026-08-28` todo, not by this one. Say so, so a future
  reader does not pull it in here.
- Scale of this gap: **204 translatable keys x 46 locales.**

**Provenance — this is COVERAGE, not a decision.** Decision 2 of the now-closed
`.planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md`
said "an LLM covers all Heroic locales", sized at "a few hundred keys x 50 locales". That
clause was never executed beyond `de` and `fr`. The decision was adopted and BUILT; it was
simply never RUN at scale. State that distinction explicitly — it is why this is a coverage
todo and not a reopened decision.

**User-visible consequence — and it is the reason this deserves its own record.** A user
running GameLib in any of those 46 locales sees **every fork-added string in English**.

**`## Related but distinct` — the section that stops a future reader merging two todos.**
`.planning/todos/pending/2026-09-01-non-english-catalogs-are-unrebranded-2117-heroic-strings.md`
covers UPSTREAM Heroic catalogs being unrebranded (2274 "Heroic" strings, 0 "GameLib", in
`translation.json`/`gamepage.json`). **That is a different defect.** That one is about
upstream strings carrying the wrong product name; **this one is about FORK strings in
`gamelib.json` being ABSENT ENTIRELY.** Write the distinction in that shape, in prose, not
as a bare link. Note that both are plausibly served by the same glossary-aware machine fill,
and that shared tooling is exactly why they are easy to conflate.

**`## The only blocker is a valid raw Anthropic API key — an OPERATOR action.** Every
engineering prerequisite is built and tested; name each artifact so nobody re-derives them:
`meta/machineFillGamelib.ts`; the `pnpm machine-fill-gamelib` task (`package.json:57`);
hermetic tests at `meta/__tests__/machineFillGamelib.test.ts`; the glossary
`meta/i18nGlossary.json` backed by `meta/__tests__/i18nGlossary.test.ts`; and the MT-origin
sidecar manifest written to `public/locales/<locale>/gamelib.mt.json`
(`machineFillGamelib.ts:781`). Nothing here needs engineering — it needs a key.
`ANTHROPIC_API_KEY` **is** set but 401s against the Anthropic API; it is not a valid raw API
key, and an OAuth/subscription token will not work, because `machineFillGamelib.ts:824`
reads `process.env.ANTHROPIC_API_KEY` and passes it straight through.

**`## Verification discipline for whoever runs it.** Count keys per locale afterwards; do
**NOT** trust exit 0 — a batch translation job on this project has previously returned zero
results and still exited cleanly. Also record that a retry is SAFE: the prior failed run
wrote **nothing** and both catalogs verified byte-unchanged afterwards, so the D-09 contract
held.

**`## Do not hand-translate.** `emptyAlphabetLetter` is substituted into `emptyBody`
(`No games match {{filters}}.`) as a list member, so its grammatical form has to work inside
that sentence in each target language — which is exactly the judgement the glossary-aware
fill exists to make.

**`## Provenance of this record — a deliberate split, not a correction.** State plainly:
this gap was SPLIT OUT of
`.planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md`
on 2026-09-02 by quick task `260902-ad5`, at the operator's request. Quick `260902-9wt`
had widened that todo to absorb this gap one hour earlier; this split deliberately reverses
that part of `260902-9wt`. **`260902-9wt` was not wrong** — it correctly re-homed a residue
that until then was owned by NOBODY. The operator simply wants the larger gap tracked as its
own record. Do not write this as a correction, an error, or a fix. Cross-link the
`2026-08-28` todo by filename here.

Also record the full pointer chain in one sentence, so a reader can see it is complete:
closed `2026-08-06` decision record (whose CLOSURE RECORD hands the residue on, and which
Task 2b forward-points here) → `2026-08-28` todo (de/fr half, whose `## Split out 2026-09-02`
section forwards here) → this todo (46-locale half).

**Second edit — the reverse cross-link.** Append ONE bullet to the EXISTING `## Related`
section at the end of
`.planning/todos/pending/2026-09-01-non-english-catalogs-are-unrebranded-2117-heroic-strings.md`,
matching that file's `[[wikilink-without-extension]]` convention, naming this new todo and
stating the fork-vs-upstream distinction in half a sentence. Append only — change nothing
else in that file. A one-directional link is the "residual analysis checked only ONE
direction" defect; both directions are required.
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md &amp;&amp; grep -rlF 'OWNS the 46-locale' .planning/todos/ | wc -l | tr -d ' ' | grep -qx 1</automated>
  </verify>
  <done>The new todo exists at the exact required path with `status: pending`, `area: i18n` and the sentinel line; the `2026-09-01` todo has exactly one appended bullet naming it. Content gates run in Task 3 against HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow the 2026-08-28 todo back to de/fr</name>
  <files>.planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md</files>
  <action>
The file **STAYS in `pending/` with `status: pending`**, and **the filename does not change**
— the closed `2026-08-06` todo's CLOSURE RECORD cross-links to it by filename and that link
must not break. Do not `git mv` this file. Four edits, nothing else:

**(a) Retitle** (frontmatter `title:` only). It currently reads:
`"gamelib.json fork strings are unfilled in 48 of 49 locales — 80 keys missing from de/fr, 46 locales have no gamelib.json at all, machine-fill 401s, and the lint gate is structurally blind to all of it"`.
The new title must cover de/fr's 80-key gap, the 401, and the gate blindness — and **NOT**
the 46 locales. Suggested:
`"de/fr gamelib.json are missing 80 translatable keys each, machine-fill 401s, and the lint gate is structurally blind to an absent key"`.
Also set `amended_by: "quick tasks 260902-9wt, then 260902-ad5"` and leave
`amended: 2026-09-02`. Nothing else in frontmatter changes.

**(b) REPLACE the `## Widened 2026-09-02 — 46 locales have ZERO fork-string coverage` section
(currently lines 68-78) with a short `## Split out 2026-09-02` section.** Delete the widened
section outright — do not keep it and annotate it. The replacement states: the 46-locale
zero-coverage gap has MOVED to
`.planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md`
(name it by filename); **this todo now owns `de`/`fr` ONLY**; the move deliberately reverses
the widening quick `260902-9wt` performed one hour earlier, at the operator's request; and
**`260902-9wt` was not wrong** — it re-homed a residue that until then had no owner at all,
and the operator subsequently asked for the larger gap as its own record. One short
paragraph. Do not reproduce the string `## Widened` at the start of any line anywhere in this
file — gate G2 is anchored to `^## Widened`; refer to the widening inline instead. Do not
reproduce the sentinel string `OWNS the 46-locale` either; gate G10 requires it in exactly
one file.

**(c) Fix the `## The only blocker …` section's "How to apply".** `260902-9wt` changed it to
name "the full locale set (or omitted, if the script defaults to all locales)". Restore the
de/fr scope: it must read `GAMELIB_MT_LOCALES=de,fr pnpm machine-fill-gamelib`, and the
sentence "this is no longer a de/fr-only run" must go. Everything else in that paragraph —
the count-the-keys warning, the do-not-trust-exit-0 warning, the do-not-hand-translate
warning — stays. Do not quote the phrase "naming the full locale set" anywhere in the file;
gate G4 pins that string to zero.

**(d) EXTEND the existing `## Amendment note — 2026-09-02, quick task 260902-9wt` section.**
It is history — extend it, do not rewrite it, and do not delete the pre-9wt figures it
preserves. Append a short paragraph recording that `260902-ad5` later split the 46-locale
scope back out, and superseding the 9wt-era title. If you quote the 9wt-era title as history,
quote it in PROSE only — gate G3 is frontmatter-scoped precisely so that a historical
quotation does not false-RED it.

**PRESERVE VERBATIM AND UNTOUCHED** (gate G5 checks each of these against HEAD):
- the 210 / 204 / 80 / 124 key-count prose (lines 20-23)
- the five-key subset table and its "documented SUBSET of the 80" framing
- the `redeemKey.*` English-authoring-gap paragraph and the "`en` still has exactly 6 empty
  values" sentence
- the `260827-vpl` / `b04601b8a` provenance paragraph
- the gate-blindness diagnosis: `meta/lintTranslations.ts` checks a translated file's OWN
  keys, so an ABSENT key is structurally invisible
- the HTTP 401 fenced block and the `machineFillGamelib.ts:824` explanation
- the "failed run wrote nothing / D-09 contract held" sentence
- the `**Not a blocker for Phase 34.11.**` note
  </action>
  <verify>
    <automated>grep -c '^## Split out 2026-09-02' .planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md | grep -qx 1</automated>
  </verify>
  <done>The `2026-08-28` todo is de/fr-scoped: retitled, `## Widened` replaced by `## Split out 2026-09-02`, `GAMELIB_MT_LOCALES=de,fr` restored, Amendment note extended, filename and `status: pending` unchanged. Full content gates run in Task 3 against HEAD.</done>
</task>

<task type="auto">
  <name>Task 2b: Forward-point the closed 2026-08-06 record at the residue's current owner — APPEND ONLY</name>
  <files>.planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md</files>
  <action>
**Why this edit exists.** The CLOSURE RECORD written by `260902-9wt` says the residue "is
handed to" the `2026-08-28` todo. That sentence was TRUE when written and stays true as
dated history. But after Task 2 the `2026-08-28` todo owns only the de/fr half — so a reader
who trusts that pointer and stops there will not find the 46-locale owner. **A pointer that
is technically true while its target no longer owns what was handed over is the exact shape
this session has spent five tasks correcting.** One appended sentence closes it.

**APPEND ONLY. This is measured, not promised** — gate G2b runs
`diff <(git show HEAD~1:"$CLOSED") <(git show HEAD:"$CLOSED")` and requires ZERO `<` lines.

**Do NOT:**
- alter, reword, re-date or re-attribute ANY existing line — the CLOSURE RECORD's "handed to
  [the 2026-08-28 todo]" sentence stays EXACTLY as `260902-9wt` wrote it
- change `status: CLOSED`, `closed:` or `closed_by:`
- move the file, rename it, or reopen it
- reproduce the sentinel string `OWNS the 46-locale` (gate G10 pins it to one file)
- assert that this closed record still owns anything — it points, it does not own (gate G11)

**Do:**
1. **Frontmatter attribution via SEPARATE keys.** Add two NEW keys — do not edit an existing
   one. The `2026-08-28` todo's `amended` / `amended_by` pair is the precedent:

       amended: 2026-09-02
       amended_by: "quick task 260902-ad5"

   Inserting lines is still append-only in the measured sense (a diff of an insertion emits
   only `>` lines), so this does not conflict with G2b.

2. **Append a short dated section at the END of the file**, after the existing carve-outs,
   headed exactly:

       ## Later addition — 2026-09-02, quick task 260902-ad5

   It must, in one short paragraph:
   - make clear this is a **later addition to a closed record, NOT a correction of it** —
     the CLOSURE RECORD above was accurate on the day it was written and is unchanged
   - state that the residue it handed on has since been SPLIT: the **46-locale half** moved
     to `.planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md`
     (name it by filename), and the `2026-08-28` todo now owns the **de/fr half only**
   - say the split was made at the operator's request and that `260902-9wt` was not wrong —
     it re-homed a residue that until then had no owner at all
   - do NOT reopen or re-adjudicate any of the five decisions; this record stays CLOSED
  </action>
  <verify>
    <automated>grep -c '^## Later addition — 2026-09-02, quick task 260902-ad5' .planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md | grep -qx 1 &amp;&amp; git diff -- .planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md | grep -c '^-[^-]' | grep -qx 0</automated>
  </verify>
  <done>The closed record carries two new frontmatter keys and one appended dated section naming the new todo by filename; `status: CLOSED`/`closed:`/`closed_by:` and every pre-existing line are byte-identical to HEAD~1. Binding proof is gate G2b.</done>
</task>

<task type="auto">
  <name>Task 3: Write the SUMMARY, commit once via a prepared index, then gate against HEAD</name>
  <files>
.planning/quick/260902-ad5-split-the-46-locale-fork-string-coverage/260902-ad5-SUMMARY.md (create)
.planning/STATE.md (one appended row, committed via prepared index)
  </files>
  <action>
**Order is load-bearing. Make ALL edits first, stage LAST, commit ONCE.** This is the direct
countermeasure to `260902-9wt`'s `eadaae560` defect, where a path was staged before its
content edits landed and the commit shipped a bare rename while the gate read the working
tree and passed. A single commit also makes `HEAD~1` a clean base for gate G2b.

**(1) Write the SUMMARY** at
`.planning/quick/260902-ad5-split-the-46-locale-fork-string-coverage/260902-ad5-SUMMARY.md`.
It must state: this task deliberately reversed part of `260902-9wt` at the operator's
request; `260902-9wt` was not a mistake; the split is a MOVE not a COPY; and the closed
`2026-08-06` record was amended APPEND-ONLY rather than left alone, because a technically
true pointer at a target that no longer owns the thing is the failure shape this session
kept hitting. Record the three-hop pointer chain and the G1-G11 results.

**(2) Append the STATE row to the WORKING TREE.** `.planning/STATE.md`'s quick-task table
currently ends with the `| 260902-9wt |` row (line 6453), followed by a blank line and
`## Deferred Items`. Insert our row on the line immediately after the `260902-9wt` row
(`ad5` sorts after `9wt`). One row, pipe-delimited, same shape as its neighbours, ending
with `| 2026-09-02 | [260902-ad5-split-the-46-locale-fork-string-coverage](.planning/quick/260902-ad5-split-the-46-locale-fork-string-coverage/) |`.

**(3) Verify nothing foreign is staged, then stage our paths.**

    test -z "$(git diff --cached --name-only)" || { echo "INDEX NOT EMPTY — STOP"; exit 1; }
    git add .planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md \
            .planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md \
            .planning/todos/pending/2026-09-01-non-english-catalogs-are-unrebranded-2117-heroic-strings.md \
            .planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md \
            .planning/quick/260902-ad5-split-the-46-locale-fork-string-coverage/

Never `git add -A` or `git add .` — `.planning/ROADMAP.md` and the untracked Phase 40 phase
directory belong to a concurrent session.

**(4) Prepared index for `.planning/STATE.md`.** The working-tree copy carries the concurrent
session's Phase 40 line, which must NOT ship. Build the blob from HEAD plus our row only:

    T=$(mktemp -d)
    git show HEAD:.planning/STATE.md > "$T/state.new"
    # insert OUR ROW ONLY, immediately after the line beginning "| 260902-9wt |"
    BLOB=$(git hash-object -w "$T/state.new")
    git update-index --cacheinfo 100644,"$BLOB",.planning/STATE.md

Then assert the prepared blob differs from HEAD by exactly one added line and zero removals:

    diff <(git show HEAD:.planning/STATE.md) "$T/state.new" | grep -c '^>' | grep -qx 1
    diff <(git show HEAD:.planning/STATE.md) "$T/state.new" | grep -c '^<' | grep -qx 0

**(5) BARE commit — no pathspec, no `-a`, no `gsd-sdk query commit`.**

    git commit -m "docs(quick-260902-ad5): split the 46-locale fork-string coverage gap into its own todo"

**(6) Post-commit gates. Every content gate reads `git show HEAD:`, never the working tree.**

    NEW=.planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md
    OLD=.planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md
    SIB=.planning/todos/pending/2026-09-01-non-english-catalogs-are-unrebranded-2117-heroic-strings.md
    CLOSED=.planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md
    STEM=2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage

    # G1 exact commit contents — nothing foreign, nothing missing
    git show --stat --format= --name-only --no-renames HEAD | sort > "$T/actual"
    grep -c 'ROADMAP.md' "$T/actual" | grep -qx 0
    grep -c 'phases/40-' "$T/actual" | grep -qx 0
    grep -cE '^(src|meta|public)/' "$T/actual" | grep -qx 0
    for f in "$NEW" "$OLD" "$SIB" "$CLOSED" .planning/STATE.md; do
      grep -qxF "$f" "$T/actual" || { echo "MISSING FROM COMMIT: $f"; exit 1; }
    done

    # G2 ANTI-DUPLICATION — the split is a MOVE, not a COPY
    git show HEAD:"$NEW" | grep -c '46 locales' | grep -qvx 0        # new todo asserts the gap
    git show HEAD:"$OLD" | grep -c '^## Widened' | grep -qx 0         # old todo no longer claims it
    git show HEAD:"$OLD" | grep -c 'now owns the full 46-locale residue' | grep -qx 0
    git show HEAD:"$OLD" | grep -c '^## Split out 2026-09-02' | grep -qx 1
    git show HEAD:"$OLD" | grep -c "$STEM" | grep -qvx 0              # forwards to the new todo

    # G2b APPEND-ONLY on the closed record — the BINDING gate for Task 2b.
    # Zero '<' lines proves nothing was altered, reworded or re-dated.
    diff <(git show HEAD~1:"$CLOSED") <(git show HEAD:"$CLOSED") | grep -c '^<' | grep -qx 0
    diff <(git show HEAD~1:"$CLOSED") <(git show HEAD:"$CLOSED") | grep -c '^>' | grep -qvx 0
    git show HEAD:"$CLOSED" | grep -c "$STEM" | grep -qvx 0           # points at the CURRENT owner
    git show HEAD:"$CLOSED" | grep -c '^## Later addition — 2026-09-02, quick task 260902-ad5' | grep -qx 1
    git show HEAD:"$CLOSED" | awk '/^---$/{n++; next} n==1' | grep -c 'status: CLOSED' | grep -qx 1
    git show HEAD:"$CLOSED" | awk '/^---$/{n++; next} n==1' | grep -c 'closed_by: "quick task 260902-9wt"' | grep -qx 1

    # G3 frontmatter-scoped gates (prose may quote an old title as history)
    git show HEAD:"$OLD" | awk '/^---$/{n++; next} n==1' | grep -c '46 locales' | grep -qx 0
    git show HEAD:"$OLD" | awk '/^---$/{n++; next} n==1' | grep -c 'status: pending' | grep -qx 1
    git show HEAD:"$NEW" | awk '/^---$/{n++; next} n==1' | grep -c 'status: pending' | grep -qx 1
    git show HEAD:"$NEW" | awk '/^---$/{n++; next} n==1' | grep -c 'area: i18n' | grep -qx 1

    # G4 de/fr scope restored on the old todo
    git show HEAD:"$OLD" | grep -c 'GAMELIB_MT_LOCALES=de,fr' | grep -qvx 0
    git show HEAD:"$OLD" | grep -c 'naming the full locale set' | grep -qx 0

    # G5 preservation — each must survive verbatim in the old todo
    for s in 'are **204 translatable**' 'have **124** keys each' 'emptyAlphabetLetter' \
             'redeemKey.alreadyOwned' 'meta/lintTranslations.ts' 'HTTP 401' \
             'do not trust exit 0' 'hand-translate' 'Not a blocker for Phase 34.11' \
             'Amendment note — 2026-09-02, quick task 260902-9wt'; do
      git show HEAD:"$OLD" | grep -qF "$s" || { echo "LOST: $s"; exit 1; }
    done

    # G6 bidirectional sibling link
    git show HEAD:"$NEW" | grep -c '2026-09-01-non-english-catalogs-are-unrebranded' | grep -qvx 0
    git show HEAD:"$SIB" | grep -c "$STEM" | grep -qvx 0

    # G7 the "not a mistake" framing is present in all three touched records
    git show HEAD:"$NEW"    | grep -qF '260902-9wt'
    git show HEAD:"$OLD"    | grep -qF '260902-ad5'
    git show HEAD:"$CLOSED" | grep -qF '260902-ad5'

    # G8 ORDER-INDEPENDENT BACKSTOP — committed bytes == working-tree bytes
    for f in "$NEW" "$OLD" "$SIB" "$CLOSED" \
             .planning/quick/260902-ad5-split-the-46-locale-fork-string-coverage/260902-ad5-PLAN.md \
             .planning/quick/260902-ad5-split-the-46-locale-fork-string-coverage/260902-ad5-SUMMARY.md; do
      diff <(git show HEAD:"$f") "$f" || { echo "STAGED != TREE: $f"; exit 1; }
    done

    # G9 STATE.md — the ONE documented exception to G8. HEAD must differ from the
    # working tree by exactly the concurrent session's Phase 40 line: 1 addition, 0 removals.
    diff <(git show HEAD:.planning/STATE.md) .planning/STATE.md | grep -c '^>' | grep -qx 1
    diff <(git show HEAD:.planning/STATE.md) .planning/STATE.md | grep -c '^<' | grep -qx 0
    diff <(git show HEAD:.planning/STATE.md) .planning/STATE.md | grep -q 'Phase 40 added 2026-09-02'
    git show HEAD:.planning/STATE.md | grep -c '260902-ad5' | grep -qvx 0

    # G10 SINGLE OWNER, measured across the WHOLE todo tree, read from the commit.
    # Exactly one file may carry the ownership sentinel.
    git grep -lF 'OWNS the 46-locale' HEAD -- .planning/todos/ | wc -l | tr -d ' ' | grep -qx 1
    git grep -lF 'OWNS the 46-locale' HEAD -- .planning/todos/ | grep -qF "$STEM"

    # G11 NO SECOND CLAIMANT — every other record in the chain points, it does not own.
    for f in "$OLD" "$SIB" "$CLOSED"; do
      git show HEAD:"$f" | grep -cE 'this (todo|record) (now )?(OWNS|owns) the (full )?46-locale' | grep -qx 0 \
        || { echo "SECOND CLAIMANT: $f"; exit 1; }
    done
    # and the closed record must not re-assert ownership of the residue it handed on
    git show HEAD:"$CLOSED" | grep -cE '(this closed record|this todo) (still )?owns' | grep -qx 0

If ANY gate fails, fix and `git commit --amend` rather than adding a follow-up commit — the
amend keeps `HEAD~1` a valid base for G2b. Re-run the FULL gate block after amending, never
just the gate that failed.
  </action>
  <verify>
    <automated>git show HEAD:.planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md | grep -c '^## Widened' | grep -qx 0 &amp;&amp; git show HEAD:.planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md | grep -c '2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage' | grep -qvx 0 &amp;&amp; diff &lt;(git show HEAD~1:.planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md) &lt;(git show HEAD:.planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md) | grep -c '^&lt;' | grep -qx 0 &amp;&amp; git grep -lF 'OWNS the 46-locale' HEAD -- .planning/todos/ | wc -l | tr -d ' ' | grep -qx 1</automated>
  </verify>
  <done>A single commit contains all four todo records, the quick-task PLAN and SUMMARY, and one `.planning/STATE.md` row. `.planning/ROADMAP.md` and the Phase 40 directory are untouched and uncommitted. Gates G1-G11 all pass against `git show HEAD:`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| our commit → concurrent session's working tree | `.planning/ROADMAP.md` and one `.planning/STATE.md` line belong to another session; a careless `git add -A` or a pathspec commit would capture or destroy them. |
| our commit → a CLOSED, already-adjudicated record | Task 2b writes into `.planning/todos/completed/`. The risk is not disclosure but silent mutation of settled history: a rewrite that drops a paragraph while adding the new one. |

No user input, no network, no code path, and no dependency is touched by this task.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ad5-01 | Tampering | concurrent session's `.planning/ROADMAP.md` and Phase 40 STATE line | mitigate | Explicit `git add` of five paths only; STATE.md staged via `git hash-object` + `git update-index --cacheinfo` from a `git show HEAD:` base; bare `git commit` with no pathspec; gates G1 and G9 assert the result. `git add -A`, `git add .`, `git stash`, `git reset`, `git checkout -- <f>` all banned. |
| T-ad5-02 | Tampering | closed record `2026-08-06-…-defe.md` | mitigate | Gate G2b: `diff <(git show HEAD~1:"$CLOSED") <(git show HEAD:"$CLOSED")` must emit ZERO `<` lines and at least one `>` line — append-only proven by measurement, not asserted. Frontmatter gate re-reads `status: CLOSED` and the original `closed_by` verbatim. |
| T-ad5-03 | Repudiation | the todo record set | mitigate | G2 proves the 46-locale gap moved rather than copied; G10 counts ownership-sentinel files across the whole tree from the COMMIT (`git grep … HEAD`) and requires exactly 1; G11 proves no other record in the chain re-claims it; G6 proves the sibling cross-link is bidirectional; G5 proves no preserved history was silently dropped. |
| T-ad5-04 | Information Disclosure | — | accept | No secrets, no PII, no network calls. `ANTHROPIC_API_KEY` is referenced by NAME only; its value is never read, printed or committed. |
| T-ad5-SC | Tampering | npm/pip/cargo installs | accept | No package installs of any kind — records-only task, zero dependency changes, so the supply-chain surface is empty. |
</threat_model>

<verification>
Run after Task 3 and record each result in the SUMMARY:

1. **Single-owner, measured:** `git grep -lF 'OWNS the 46-locale' HEAD -- .planning/todos/`
   returns exactly one path, and it is the new todo. (G10.)
2. **Link chain traversable, four hops, walked by hand once:** closed `2026-08-06` CLOSURE
   RECORD → its appended `## Later addition` → the new todo directly; and closed
   `2026-08-06` → `2026-08-28` todo → its `## Split out 2026-09-02` → the new todo. Record
   both routes.
3. **Append-only proven:** paste the `diff <(git show HEAD~1:…) <(git show HEAD:…)` output
   for the closed record into the SUMMARY. It must contain `>` lines only.
4. **Working tree clean of foreign staging:** `git status --short` shows exactly
   ` M .planning/ROADMAP.md`, ` M .planning/STATE.md`, and the untracked
   `.planning/phases/40-…/` — and nothing of ours.
5. **No source touched:** `git show --stat --format= --name-only --no-renames HEAD | grep -c -E '^(src|meta|public)/'`
   returns `0`.
</verification>

<success_criteria>
- The 46-locale x 204-key gap is owned by exactly ONE record, proven by G10 reading the
  commit, not the working tree.
- The `2026-08-28` todo is de/fr-scoped, keeps its filename and `status: pending`, and
  forwards to the new todo by filename.
- The closed `2026-08-06` record forward-points at the CURRENT owner, stays `status: CLOSED`,
  and is provably append-only (G2b: zero `<` lines).
- No record other than the new todo claims ownership of the 46-locale gap (G11).
- Every preserved fragment listed in Task 2 survives verbatim in HEAD (G5).
- All three touched records state plainly that this split deliberately reverses part of
  `260902-9wt` at the operator's request, and none describes `260902-9wt` as a mistake.
- The sibling `2026-09-01` cross-link exists in BOTH directions.
- One commit. `.planning/ROADMAP.md` untouched; the concurrent session's Phase 40 STATE line
  still uncommitted; no file under `src/`, `meta/` or `public/locales/` modified.
- Gates G1-G11 pass, every one of them reading `git show HEAD:` or `git grep … HEAD`.
</success_criteria>

<output>
Create `.planning/quick/260902-ad5-split-the-46-locale-fork-string-coverage/260902-ad5-SUMMARY.md` when done.
</output>
