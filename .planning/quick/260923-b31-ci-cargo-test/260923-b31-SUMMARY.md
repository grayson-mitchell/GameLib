---
phase: quick-260923-b31
plan: 01
status: completed
subsystem: ci
tags: [github-actions, rust, cargo, tauri, ci-gate]

# Dependency graph
requires: []
provides:
  - "A `cargo test --bin gamelib-shell` CI job on windows-latest + macos-latest"
  - "The no-CI-cargo-test todo retired to completed/ with an honest operator follow-up"
affects: [ci, src-tauri, release-tauri]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "New standalone workflow file per gate (rust-test.yml) rather than grafting onto test.yml, so the Rust gate carries its own paths scope and its own name in the checks list"
    - "Header comment states what a CI gate does NOT catch, matching test.yml/codecheck.yml's in-situ style"

key-files:
  created:
    - .github/workflows/rust-test.yml
  modified:
    - .planning/todos/pending/2026-09-22-windows-rust-tests-not-run-in-ci.md (deleted, moved to completed/)
    - .planning/todos/completed/2026-09-22-windows-rust-tests-not-run-in-ci.md (created, with Resolution section)

key-decisions:
  - "New file (.github/workflows/rust-test.yml) rather than a job inside test.yml, per CONTEXT.md's Claude's-Discretion note"
  - "windows-latest + macos-latest only, no ubuntu leg -- src-tauri needs libwebkit2gtk system deps and the only Linux-specific Rust is the cfg(unix) libc::kill arm macOS already compiles"
  - "No positive paths: filter -- a required check that gets skipped by a paths filter never reports and would block every non-Rust-touching PR"
  - "Real pnpm exec vite build ahead of cargo test, not a stub build/renderer/index.html, because generate_context! embeds it at compile time and a fake input would hide a real asset/codegen break until release"

patterns-established:
  - "Two-clause todo Done-when (file gate + branch-protection required-check) can be partially closed from a quick task; the unclosed clause gets a named '## Resolution' subsection rather than being silently dropped or falsely marked done"

requirements-completed: [QUICK-260923-b31]

# Metrics
duration: 25min
completed: 2026-09-23
---

# Quick Task 260923-b31: Add a CI job that runs `cargo test` for the Tauri shell Summary

**Added `.github/workflows/rust-test.yml` running `cargo test --bin gamelib-shell` on windows-latest and macos-latest behind a real vite renderer build, and retired the todo it closes with an explicit note that the branch-protection "required check" half is unfinished operator work.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 2 completed
- **Files modified:** 2 (1 created, 1 moved+edited)

## Accomplishments

- Closed the CI-coverage gap the todo describes: no workflow previously ran `cargo test` or even
  `cargo check` on any OS, which is how the `store_embed_wire_contract_*` compile break sat on
  `main` unnoticed until someone ran the harness by hand.
- New job matrixes `windows-latest` + `macos-latest` with `fail-fast: false`, runs the real
  `pnpm exec vite build` (required because `tauri::generate_context!()` embeds `build/renderer` at
  compile time) ahead of `cargo test --bin gamelib-shell`, and states in its own header comment
  what it does not catch (no Linux leg, no runtime/webview coverage, unpinned `@stable` toolchain,
  the 2 `#[ignore]`d tests).
- Todo moved from `.planning/todos/pending/` to `.planning/todos/completed/` with
  `status`/`resolved`/`resolved_by` and a Resolution section that names the still-open
  branch-protection step as an operator action outside this repo, rather than claiming the todo
  fully done.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create .github/workflows/rust-test.yml** - `67e14b04b` (feat)
2. **Task 2: Retire the todo to completed/ and surface the branch-protection follow-up** - `2be2bb3ff` (docs)

_No plan-metadata commit was made in the standard GSD phase-plan style; this is a quick task and
this SUMMARY.md itself is the closing artifact._

## Files Created/Modified

- `.github/workflows/rust-test.yml` - New two-OS `cargo test --bin gamelib-shell` gate for the Tauri shell crate, behind `install-deps` and `pnpm exec vite build`.
- `.planning/todos/completed/2026-09-22-windows-rust-tests-not-run-in-ci.md` - Retired todo, with `files:` updated to include the new workflow and a `## Resolution (2026-09-23, quick 260923-b31)` section.
- `.planning/todos/pending/2026-09-22-windows-rust-tests-not-run-in-ci.md` - Removed (moved to `completed/`).

## Decisions Made

- Followed CONTEXT.md's locked decisions and Claude's-Discretion notes verbatim: separate workflow
  file, mirrored trigger/concurrency/permissions shape, and `dtolnay/rust-toolchain@stable` +
  `swatinem/rust-cache@v2` matching `release-tauri.yml` exactly (no `targets:` input, since neither
  leg here needs a cross-compile target the way the macOS release leg's
  `aarch64-apple-darwin` bundle does).
- Did not include `build-steam-bridge`: per measured_facts #6, `cargo test` consumes no bridge
  assets and `assembleRendererDist.ts` excludes `bin/` from `build/renderer`, so omitting it
  changes nothing observable by the compiler or the tests.

## Deviations from Plan

None - plan executed exactly as written. Both tasks' automated verifies passed on the first
attempt with no auto-fixes required.

## Operator Follow-up (NOT resolvable by this task)

The todo's Done-when has two clauses: "That job exists in a workflow under
`.github/workflows/`" (done, this task) **and** "is a required check" (not done, and not
doable from a file in this repo).

Making `Rust test / cargo-test (windows-latest)` and `Rust test / cargo-test (macos-latest)`
required status checks is a GitHub branch-protection (or ruleset) setting under
**Settings -> Branches** for `main`, changed by a repo admin through the GitHub UI or `gh api`
against that endpoint -- this task was explicitly instructed not to attempt it, and no commit in
this repo can perform it.

Order matters: GitHub's required-check picker only lists check names it has already observed at
least once, so the sequence has to be (1) merge this workflow to `main`, (2) let it run at least
once (push or PR), (3) an operator then adds both `cargo-test` check names as required in branch
protection. Until step 3 happens, the job runs and reports on every push/PR but does not block a
merge.

## Self-Check

- `.github/workflows/rust-test.yml` exists: verified via `node -e "require('js-yaml').load(...)"` parse (PASS).
- `.planning/todos/completed/2026-09-22-windows-rust-tests-not-run-in-ci.md` exists and
  `.planning/todos/pending/2026-09-22-windows-rust-tests-not-run-in-ci.md` does not: verified via
  `test -f` / `test ! -e` in the Task 2 automated verify (PASS, `TODO_RETIRED_OK`).
- Commit `67e14b04b` exists: `git log --oneline` shows it as HEAD~1.
- Commit `2be2bb3ff` exists: `git log --oneline` shows it as HEAD.
- `pnpm planning-gates` (12/12 gates): PASS.
- `git diff --stat -- src-tauri/` is empty: confirmed, no Rust source touched.

## Self-Check: PASSED
