---
phase: quick-260903-ly4
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - meta/machineFillGamelib.ts
  - meta/__tests__/machineFillGamelib.test.ts
  - .planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md
  - .planning/quick/260903-ly4-relax-the-target-side-trailing-word-boun/260903-ly4-SUMMARY.md

must_haves:
  truths:
    - "In the committed `meta/machineFillGamelib.ts`, `containsTermVerbatim` and `containsTermSourcePresence` are NO LONGER byte-identical: the source-side matcher keeps both boundaries, the target-side matcher keeps ONLY the leading `(?<![A-Za-z0-9_])` lookbehind."
    - "`validateTranslation` returns `[]` for Estonian `Steami`/`Steamis`, Finnish `Steamin`, Hungarian `Steamet` and Scandinavian-genitive `GameLibs` targets whose English source carries the bare glossary term."
    - "`validateTranslation` still returns exactly one problem when the target genuinely localises the term away (`Steam` -> `Пар`), and still returns a problem when the term appears only as the tail of an unrelated word (`MegaSteam`) -- the leading lookbehind survives."
    - "The four new accept-case assertions were demonstrated RED against the UNMODIFIED validator before the fix, with the verbatim jest failure output recorded in the SUMMARY."
    - "The regression pin for the leading lookbehind was mutation-proven capable of failing (removing the lookbehind turns it RED)."
    - "The 3-file meta suite is green with a test count strictly greater than the 255 baseline, and the delta is reported."
    - "A scratchpad-only no-regression sweep over every filled string in `public/locales/*/gamelib.json` reports `patched <= current` with zero strings moving from problem-free to problem-bearing, and its pair count proves it was non-vacuous."
    - "The owning todo records the 2026-09-03 +185 batch, the 57-string trailing-lookahead attribution, and that live confirmation is an OPERATOR step -- and stays `status: pending`."
    - "The concurrent session's files (`.planning/ROADMAP.md`, `.planning/STATE.md`, the untracked `.planning/phases/40-*/` directory) are untouched and uncommitted."
  artifacts:
    - path: "meta/machineFillGamelib.ts"
      provides: "Asymmetric glossary matchers + the 260903-ly4 comment block stating the accepted false-pass cost"
      contains: "260903-ly4"
    - path: "meta/__tests__/machineFillGamelib.test.ts"
      provides: "Inflection accept-cases + localisation/lookbehind regression pins in the `glossary preservation` describe block"
      contains: "Steami"
    - path: ".planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md"
      provides: "2026-09-03 batch numbers, the 57-string diagnosis, and the operator-run caveat; still pending"
      contains: "5bc76a74c"
    - path: ".planning/quick/260903-ly4-relax-the-target-side-trailing-word-boun/260903-ly4-SUMMARY.md"
      provides: "RED-before output verbatim, GREEN-after counts, sweep numbers, and the explicit not-verified-here statement"
  key_links:
    - from: "meta/machineFillGamelib.ts containsTermVerbatim"
      to: "validateTranslation glossary loop"
      via: "negated survival check inside the `for (const term of glossary)` loop"
      pattern: "containsTermVerbatim"
    - from: "meta/__tests__/gamelibCatalogParity.test.ts"
      to: "meta/machineFillGamelib.ts validateTranslation"
      via: "parity gate replays the validator over every committed locale catalog"
      pattern: "validateTranslation"
---

<objective>
`validateTranslation`'s TARGET-side glossary matcher forbids a glossed brand term
from taking any suffix. English brands do not inflect, so the defect is invisible
in English; in Estonian (`Steami`/`Steamis`/`Steamiga`), Finnish (`Steamin`),
Hungarian (`Steamet`) and the North Germanic bare-`s` genitive (`GameLibs`) it
silently rejects the correct translation while the fill run still exits 0. That is
the same root shape as the `Browser` defect fixed under quick `260903-itr` on
2026-09-03: a validator encoding ENGLISH morphology and applying it to languages
that do not share it.

Purpose: unblock the last 57 outstanding fork-string fills (measured 2026-09-02:
all 57 have an English source containing a glossary term; et misses 31 of 33
Steam-bearing strings vs 2 of 176 others).

Output: an asymmetric matcher pair, six new hermetic assertions, an offline
no-regression sweep, and a records append. NOT a live fill run -- see
`<not_verifiable_here>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

Scope fence, non-negotiable. Touch ONLY:
- `meta/machineFillGamelib.ts`
- `meta/__tests__/machineFillGamelib.test.ts`
- `.planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md` (append only)
- this quick task's own `260903-ly4-SUMMARY.md`

Do NOT edit `meta/i18nGlossary.json`, anything under `public/locales/`, anything
under `src/`, `.planning/ROADMAP.md`, `.planning/STATE.md`, or any other pending
todo. Do NOT run `pnpm machine-fill-gamelib`.

SCRATCHPAD (throwaway files live here, never in the repo):
`/private/tmp/claude-501/-Users-graysonmitchell-Projects-GameLib/56b28ade-489f-4734-9bdc-c1a67bdea353/scratchpad`

<interfaces>
Measured from the working tree before planning -- use these directly, do not
re-explore.

`meta/machineFillGamelib.ts` (the two matchers, currently BYTE-IDENTICAL):

- `:206` `function escapeRegExp(value: string): string`
- `:210-228` the `260903-itr` comment block -- match its voice for the new one
- `:229` `function containsTermSourcePresence(text: string, term: string): boolean`
  body: `new RegExp(`(?<![A-Za-z0-9_])${escapeRegExp(term)}(?![A-Za-z0-9_])`)`
- `:236-237` two-line comment, then
- `:238` `function containsTermVerbatim(text: string, term: string): boolean`
  body: identical regex to the above
- `:251` `export function validateTranslation(source, target, glossary): string[]`
- `:276-285` the glossary loop:
  `if (containsTermSourcePresence(source, term) && !containsTermVerbatim(target, term))`
  pushing `translation drops or alters glossary term "<term>" ...`

`meta/__tests__/machineFillGamelib.test.ts`:
- `:194` `describe('glossary preservation', () => {` -- the block to extend
- `:222-229` the existing `'Open in browser'` / `'Ouvrir dans le navigateur'`
  case (the `260903-itr` proof) -- must stay green, do not edit it
- imports at `:1-12` already include `validateTranslation`

Module-load hazard, measured: `meta/machineFillGamelib.ts` ends with
`if (!process.env.JEST_WORKER_ID) { void main() }`. IMPORTING this module from a
plain node script FIRES `main()`. `ANTHROPIC_API_KEY` in this environment is the
truthy 10-char placeholder `sk-ant-...`, so the missing-key guard does NOT trip.
Any scratchpad script that imports it MUST run with `JEST_WORKER_ID=1` set in the
process environment (set at launch, not inside the file -- ESM imports hoist above
any in-file assignment). Its only imports are `node:fs` and `node:path`, so an
esbuild bundle of a scratchpad entry needs no `node_modules`.

Consumers of `validateTranslation`, measured (there are no others):
- `meta/machineFillGamelib.ts` itself (`fillLocale`)
- `meta/__tests__/machineFillGamelib.test.ts`
- `meta/__tests__/gamelibCatalogParity.test.ts:75` -- replays the validator over
  EVERY committed locale catalog and asserts `failures` is `[]`. It is currently
  GREEN, which means the current committed-catalog problem count is already 0:
  the rejected translations were never written to disk, so they cannot appear in
  the sweep. Expect the sweep to report 0 -> 0. Report the actual numbers.

`containsTermVerbatim` / `containsTermSourcePresence` have no consumers outside
`meta/machineFillGamelib.ts` and its own suite.

Measured baseline (2026-09-03, this working tree):
`npx jest meta/__tests__/machineFillGamelib.test.ts meta/__tests__/i18nGlossary.test.ts meta/__tests__/gamelibCatalogParity.test.ts`
-> `Test Suites: 3 passed`, `Tests: 255 passed, 255 total`.

`.prettierignore` excludes `.planning` and `public/locales/`, so prettier applies
to the two `meta/` files ONLY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the inflection and regression assertions, and prove them RED against the UNMODIFIED validator</name>
  <files>meta/__tests__/machineFillGamelib.test.ts</files>
  <behavior>
    Added inside the existing `describe('glossary preservation')` block at
    `:194`, AFTER the existing `260903-itr` cases (do not edit those).

    EXPECTED RED before the fix (these are the proof the fix is non-vacuous):
    - et agglutination: source `'Connect your Steam account'`, glossary
      `['Steam']`, targets containing `Steami`, `Steamis` and `Steamiga`
      (e.g. `'Ühenda oma Steami konto'`, `'Sinu konto on Steamis'`,
      `'Ühenda konto Steamiga'`) -> each `expect(problems).toEqual([])`
    - fi: target containing `Steamin` (e.g. `'Yhdistä Steamin tilisi'`) -> `[]`
    - hu: target containing `Steamet` (e.g. `'Csatlakoztasd a Steamet'`) -> `[]`
    - Scandinavian bare-`s` genitive: source EXACTLY
      `"GameLib's Tauri build does not yet embed a browser view for the store and wiki pages."`,
      glossary `['GameLib']`, target containing `GameLibs` -> `[]`

    EXPECTED GREEN both before and after (regression pins, must never flip):
    - a target that genuinely TRANSLATES the term still fails: source
      `'Connect your Steam account'`, target rendering Steam as `Пар`, glossary
      `['Steam']` -> exactly ONE problem, message matching `/Steam/`
    - the leading lookbehind still holds: target where the term appears only as
      the TAIL of an unrelated word (e.g. `'Verbinde dein MegaSteam-Konto'`),
      glossary `['Steam']` -> exactly ONE problem
    - the existing `'Open in browser'` / `'Ouvrir dans le navigateur'` case and
      every other existing test stay green
  </behavior>
  <action>
    Add ONLY test code in this task. Do NOT touch `meta/machineFillGamelib.ts`
    yet -- the RED demonstration is worthless if the fix is already in the tree.

    Head each new assertion with a short comment naming `260903-ly4` and the
    language whose morphology it encodes, in the voice of the `260903-itr`
    comment already at `:214-221`.

    Then, with the validator still UNMODIFIED:

    1. `git diff --stat` MUST list `meta/__tests__/machineFillGamelib.test.ts`
       and nothing else. If `meta/machineFillGamelib.ts` appears, stop -- the
       fix leaked in early and the RED run proves nothing.
    2. Run `npx jest meta/__tests__/machineFillGamelib.test.ts` and capture the
       output VERBATIM to the scratchpad
       (`.../scratchpad/260903-ly4-red.txt`). This output goes into the SUMMARY
       unedited in Task 3.
    3. Confirm the failure set is exactly the accept-cases listed above and that
       the two regression pins and all 255 pre-existing tests PASSED in the same
       run. A pin that is red here is a wrong pin, not a discovered defect --
       fix the pin before continuing.

    Record the RED failure count.
  </action>
  <verify>
    <automated>npx jest meta/__tests__/machineFillGamelib.test.ts 2>&1 | tee /private/tmp/claude-501/-Users-graysonmitchell-Projects-GameLib/56b28ade-489f-4734-9bdc-c1a67bdea353/scratchpad/260903-ly4-red.txt | grep -E "Tests:.*failed"</automated>
  </verify>
  <done>The run FAILS, the failures are exactly the et/fi/hu/Scandinavian accept-cases, both regression pins pass, `git diff --stat` shows only the test file, and the verbatim output is saved to the scratchpad.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relax ONLY the target-side trailing boundary, document the accepted cost, and mutation-prove the lookbehind pin</name>
  <files>meta/machineFillGamelib.ts</files>
  <behavior>
    After this change every assertion from Task 1 is GREEN, and the 3-file meta
    suite total is strictly greater than 255 with zero failures.
  </behavior>
  <action>
    Change `containsTermVerbatim` (`:238`) ONLY. Drop the TRAILING
    `(?![A-Za-z0-9_])` lookahead; KEEP the leading `(?<![A-Za-z0-9_])`
    lookbehind:

    the regex becomes the leading-lookbehind form followed by
    `escapeRegExp(term)` with nothing after it.

    Leave `containsTermSourcePresence` (`:229`) EXACTLY as it is -- both
    boundaries, byte-unchanged. The source is English and must stay strict, and
    it already handles the English genitive correctly because an apostrophe is
    not in `[A-Za-z0-9_]`, so `GameLib's` already matches the term `GameLib`.
    After the edit the two functions MUST have visibly different regexes.

    Replace the thin two-line comment at `:236-237` with a block in the same
    voice as the `260903-itr` block above it, tagged `260903-ly4`, stating:
    - what the strict trailing boundary rejected: any inflected or suffixed form
      of a glossed term
    - the measured evidence (2026-09-02, real catalogs, after the +185 batch):
      57 outstanding fills, ZERO of which have a glossary-term-free English
      source; et 31 of 33 Steam-bearing strings missed vs 2 of 176 others (94%
      vs 1%); fi 9 of 33 vs 5 of 176; hu 4 of 33 vs 0; hr 2 vs 0; sl 1 vs 0; and
      da, nb_NO and sv each missing the SAME single string,
      `webview.unavailable.body`, whose English opens `GameLib's` -- three
      independent North Germanic locales failing on the one English genitive
    - the ACCEPTED COST, stated plainly and NOT as harmless: a relaxed trailing
      boundary makes the survival check weaker for short glossary terms -- `Mac`
      is now also satisfied by German `Macht`/`Machen`, and `GE` by any all-caps
      word starting `GE`. This deliberately trades a FALSE-REJECT (silently
      discards correct translations while the run exits 0 -- the failure mode
      that has now bitten this project twice) for a FALSE-PASS on the survival
      check (at worst one bad translation reaches a catalog already marked
      MT-origin in its `gamelib.mt.json` sidecar). The direction is chosen, not
      accidental.
    - that this is the asymmetry's OPPOSITE direction to `260903-itr`: that fix
      tightened the SOURCE side, this one relaxes the TARGET side.

    Then, in order:

    1. `npx jest meta/__tests__/machineFillGamelib.test.ts meta/__tests__/i18nGlossary.test.ts meta/__tests__/gamelibCatalogParity.test.ts`
       -- all green. Report the new total and the delta from 255.
    2. MUTATION-PROVE the leading-lookbehind pin (a pin never seen to fail is
       not a pin). Back the file up with `cp meta/machineFillGamelib.ts
       .../scratchpad/260903-ly4-backup.ts`, then ALSO delete the leading
       lookbehind from `containsTermVerbatim`, re-run the suite, and confirm the
       `MegaSteam` pin goes RED. Restore with `cp` from the backup -- NEVER
       `git checkout -- <file>`, which fires this repo's post-checkout hook and
       triggers a binary download. Confirm restoration with
       `diff .../scratchpad/260903-ly4-backup.ts meta/machineFillGamelib.ts`
       being empty, then re-run the suite green.
    3. `npx prettier --check meta/machineFillGamelib.ts meta/__tests__/machineFillGamelib.test.ts`.
       If it fails, `npx prettier --write` those TWO paths only -- never
       repo-wide (this repo is prettier-red repo-wide) -- then re-run the suite,
       because a prettier rewrap can defeat a comment-anchored construct.
  </action>
  <verify>
    <automated>npx jest meta/__tests__/machineFillGamelib.test.ts meta/__tests__/i18nGlossary.test.ts meta/__tests__/gamelibCatalogParity.test.ts 2>&1 | grep -E "Tests: +[0-9]+ passed, [0-9]+ total" && npx prettier --check meta/machineFillGamelib.ts meta/__tests__/machineFillGamelib.test.ts && test "$(grep -c 'A-Za-z0-9_' meta/machineFillGamelib.ts)" -ge 3</automated>
  </verify>
  <done>All three suites green with a total above 255 and zero failures; the two matchers have different regexes; the `MegaSteam` pin was seen RED under mutation and the file was restored byte-identically; prettier is clean on both files.</done>
</task>

<task type="auto">
  <name>Task 3: Offline no-regression sweep over all filled strings, records append, and a path-scoped commit</name>
  <files>.planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md, .planning/quick/260903-ly4-relax-the-target-side-trailing-word-boun/260903-ly4-SUMMARY.md</files>
  <action>
    A. THE SWEEP (the important gate). Everything below lives in the scratchpad,
    NOTHING in the repo.

    1. `git show HEAD:meta/machineFillGamelib.ts > <scratchpad>/baselineValidator.ts`
       and `git show HEAD:meta/i18nGlossary.json > <scratchpad>/glossary.json`.
       Use `git show`, never `git checkout`.
    2. Write `<scratchpad>/sweep.ts` that:
       - imports `validateTranslation as baseline` from
         `<scratchpad>/baselineValidator.ts` and `validateTranslation as patched`
         from the ABSOLUTE working-tree path
         `/Users/graysonmitchell/Projects/GameLib/meta/machineFillGamelib.ts`
       - reads the glossary terms from `<scratchpad>/glossary.json`
       - reads `/Users/graysonmitchell/Projects/GameLib/public/locales/en/gamelib.json`
         and every `public/locales/<locale>/gamelib.json` except `en`, flattening
         nested objects to dotted key paths
       - for every key whose English source is non-empty AND whose target value
         is non-empty, runs both validators
       - prints: total pairs examined, baseline problem count, patched problem
         count, and the list of any pair whose status CHANGED in either direction
       All repo paths must be ABSOLUTE: the compiled bundle runs from a private
       tmpdir, so relative paths resolve to the wrong place.
    3. ANTI-VACUITY. The script must `process.exit(1)` if the pair count is below
       9000 (expected ~9,975). A sweep that silently iterated nothing would
       otherwise report a cheerful 0 -> 0. It must ALSO self-test both directions
       in the same run, asserting: `baseline('Connect your Steam account',
       'Verbinde dein Dampf-Konto', ['Steam'])` has >= 1 problem (the validator
       is wired up at all), and `baseline(..., 'Ühenda oma Steami konto', ...)`
       has >= 1 problem while `patched(...)` has 0 (the two imports are genuinely
       different builds and not the same file twice).
    4. Run it as:
       `JEST_WORKER_ID=1 env -u ANTHROPIC_API_KEY node meta/runTs.cjs --bundle --platform=node --target=node21 <scratchpad>/sweep.ts`
       `JEST_WORKER_ID=1` is MANDATORY -- without it, importing the module fires
       `main()`, and the placeholder `ANTHROPIC_API_KEY` is truthy enough to get
       past the missing-key guard.
    5. ASSERT `patched <= baseline` and that no pair moves from problem-free to
       problem-bearing. Dropping a lookahead is a monotone relaxation:
       `containsTermVerbatim` can only match MORE, so the problem set can only
       SHRINK. If the measurement shows anything else, STOP and investigate --
       do not explain it away.
       EXPECTED RESULT, and why it is not a failure: `gamelibCatalogParity.test.ts`
       already replays this exact validator over every committed catalog and is
       green, so both counts are expected to be 0 -- the rejected translations
       were never written to disk and therefore cannot appear in a sweep of
       committed catalogs. Report the ACTUAL numbers and how many of the pairs
       change status (expected: none); list them if any do.

    B. RECORDS. Append ONE new dated section to
    `.planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md`
    recording:
    - the 2026-09-03 batch landed +185 (commit `5bc76a74c`), 9,790 -> 9,975
      filled, 242 -> 57 outstanding, 40 of 48 locales now complete
    - the remaining 57 are attributed to this trailing-lookahead defect, with the
      per-locale evidence (et 31/33 vs 2/176, fi 9/33 vs 5/176, hu 4/33, hr 2,
      sl 1, and the shared `webview.unavailable.body` miss across da/nb_NO/sv)
      and the fact that ZERO of the 57 have a glossary-term-free English source
    - that the fix is hermetically proven (this quick task) but awaits an
      OPERATOR machine-fill run for live confirmation
    Keep `status: pending` -- do NOT close it, do NOT edit its frontmatter, do
    NOT touch any other todo. Do NOT duplicate or alter its verbatim
    `**Sole owner:**` sentinel line; after the append,
    `grep -c 'Sole owner' <that file>` must still be 1.

    C. PLANNING GATES. Run `python3 meta/runPlanningGates.py` BEFORE and AFTER
    the append and require no NEW failures. A pre-existing failure introduced by
    the concurrent session's untracked `.planning/phases/40-*/` directory is not
    ours to fix -- record it, do not chase it.

    D. SUMMARY, then COMMIT. Write the SUMMARY first (it must contain the Task 1
    RED output verbatim, the GREEN counts and delta from 255, the sweep numbers,
    and the `<not_verifiable_here>` statement below reproduced in full), then
    make ONE commit naming all four paths EXPLICITLY:

    `git commit meta/machineFillGamelib.ts meta/__tests__/machineFillGamelib.test.ts .planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md .planning/quick/260903-ly4-relax-the-target-side-trailing-word-boun/260903-ly4-SUMMARY.md -m "fix(meta): let a glossed term inflect in the target language"`

    FORBIDDEN: `git add -A`, `git stash`, `git reset`, `git checkout -- <file>`,
    `gsd-sdk query commit` (stages the whole tree), `--no-verify`, `git push`.

    Then verify the COMMIT, not the working tree -- a gate that read the working
    tree passed on 2026-09-02 while the commit shipped something else:
    - for each of the four paths, `diff <(git show HEAD:"$f") "$f"` must be EMPTY
    - `git show --stat HEAD` must list exactly those four paths
    - `git status --porcelain` must STILL show `.planning/ROADMAP.md` and
      `.planning/STATE.md` as modified and `.planning/phases/40-*/` as untracked
      -- the concurrent session's work must be neither committed nor disturbed
  </action>
  <verify>
    <automated>for f in meta/machineFillGamelib.ts meta/__tests__/machineFillGamelib.test.ts .planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md .planning/quick/260903-ly4-relax-the-target-side-trailing-word-boun/260903-ly4-SUMMARY.md; do diff <(git show HEAD:"$f") "$f" || exit 1; done && test "$(git show --stat --format= --name-only HEAD | wc -l | tr -d ' ')" = "4" && test "$(grep -c 'Sole owner' .planning/todos/pending/2026-09-02-46-locales-have-zero-gamelib-json-fork-string-coverage.md)" = "1" && git status --porcelain | grep -q "40-in-app-store-and-wiki-browsing"</automated>
  </verify>
  <done>Sweep reports a non-vacuous pair count with `patched <= baseline` and zero problem-free -> problem-bearing flips; the todo carries the new section, one `Sole owner` line and `status: pending`; planning gates show no new failures; exactly four paths are committed and each matches `git show HEAD:` byte-for-byte; the concurrent session's files remain modified/untracked.</done>
</task>

</tasks>

<not_verifiable_here>
Reproduce this verbatim in the SUMMARY.

`ANTHROPIC_API_KEY` in this agent environment is the 10-character placeholder
literal `sk-ant-...`, not a usable key. The live 57-to-0 proof CANNOT be run
here. This task did NOT run `pnpm machine-fill-gamelib` and makes NO claim that
the 57 outstanding strings are filled.

Live confirmation is an OPERATOR step: re-export a real key and run
`GAMELIB_MT_LOCALES=et,fi,hu,hr,sl,da,nb_NO,sv pnpm machine-fill-gamelib`, then
COUNT the outstanding strings afterwards rather than trusting exit 0 -- this
script has silently rejected translations and still exited 0 twice now.
</not_verifiable_here>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM translation output -> committed locale catalog | untrusted model output crosses into shipped files; `validateTranslation` is the only check on that crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ly4-01 | Tampering | `containsTermVerbatim` survival check | accept | The relaxed trailing boundary admits false passes for short terms (`Mac` satisfied by `Macht`, `GE` by any all-caps `GE*`). Accepted deliberately: the alternative is the current false-REJECT that discards correct translations at exit 0. Blast radius is one MT-origin catalog string already labelled in its `gamelib.mt.json` sidecar. Stated in the source comment, not hidden. |
| T-ly4-02 | Tampering | leading `(?<![A-Za-z0-9_])` lookbehind | mitigate | Keep the lookbehind; pin it with the `MegaSteam` regression assertion and mutation-prove that pin RED in Task 2. |
| T-ly4-03 | Elevation of Privilege | scratchpad sweep importing `machineFillGamelib.ts` | mitigate | Module-load fires `main()` unless `JEST_WORKER_ID` is set; the placeholder API key is truthy and clears the missing-key guard. Task 3 mandates `JEST_WORKER_ID=1` plus `env -u ANTHROPIC_API_KEY` so an accidental `main()` fails loudly instead of writing catalogs. |
| T-ly4-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this task -- no dependency is added or changed. |
</threat_model>

<verification>
- `npx jest meta/__tests__/machineFillGamelib.test.ts meta/__tests__/i18nGlossary.test.ts meta/__tests__/gamelibCatalogParity.test.ts` green; total > 255, delta reported.
- Task 1 RED output captured verbatim before any source edit; `git diff --stat` proved the source was untouched at that moment.
- The `MegaSteam` lookbehind pin seen RED under mutation, then restored via `cp` (never `git checkout --`).
- Scratchpad sweep: non-vacuous pair count (>= 9000), `patched <= baseline`, zero problem-free -> problem-bearing flips, both self-tests pass.
- `npx prettier --check` clean on the two `meta/` files.
- `python3 meta/runPlanningGates.py` shows no new failures versus its pre-append run.
- Every committed path verified with `diff <(git show HEAD:"$f") "$f"` empty; `git show --stat HEAD` lists exactly four paths.
- Concurrent session untouched: `.planning/ROADMAP.md` and `.planning/STATE.md` still modified, `.planning/phases/40-*/` still untracked.
</verification>

<success_criteria>
The two glossary matchers are asymmetric in the committed file: source-side keeps
both boundaries, target-side keeps only the leading lookbehind. Estonian, Finnish,
Hungarian and Scandinavian-genitive inflections of a glossed term now validate
clean; a genuine localisation of the term and a mid-word match still fail. The
accepted false-pass cost is written into the source in plain terms. The owning
todo records the 2026-09-03 numbers, the 57-string diagnosis, and that live
confirmation is an operator step -- and remains `status: pending`.
</success_criteria>

<output>
Create `.planning/quick/260903-ly4-relax-the-target-side-trailing-word-boun/260903-ly4-SUMMARY.md` when done.
</output>
