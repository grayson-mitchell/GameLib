---
phase: quick-260819-ch5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/locales/en/gamelib.json
  - src/frontend/screens/Game/GamePage/components/MainButton.tsx
  - src/frontend/screens/Game/GamePage/components/GameStatus.tsx
  - src/frontend/hooks/constants.ts
  - src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamIncomplete.test.tsx
  - src/frontend/screens/Game/GamePage/components/__tests__/steamResumeCopyCensus.test.ts
  - src/frontend/hooks/__tests__/hasStatus.reconcile.test.ts
  - src/backend/storeManagers/steam/library.ts
  - src/backend/storeManagers/steam/__tests__/library.test.ts
autonomous: true
requirements: [QUICK-260819-CH5]

must_haves:
  truths:
    - "The GamePage primary button for an incomplete native Steam install renders 'Resume Install', not 'Finish in Steam'"
    - "The GamePage status line for that same state renders 'Install incomplete — resume the download in GameLib'"
    - "The game card / library list / submenu label for statusContext 'steam-incomplete' renders 'Resume Install'"
    - "No source file under src/ resolves the key status.steamFinishInSteam or emits the literal 'Finish in Steam' outside comments"
    - "steam-waiting-for-restart and steam-paused still render 'Restart Steam to finish' and 'Paused' respectively"
    - "Every assertion on the new copy measures the RENDERED catalog value, not a t() inline default"
  artifacts:
    - path: "public/locales/en/gamelib.json"
      provides: "steam.status.resumeInstall and steam.status.resumeInstallHint"
      contains: "resumeInstall"
    - path: "src/frontend/screens/Game/GamePage/components/__tests__/steamResumeCopyCensus.test.ts"
      provides: "Repo-wide residual stale-copy gate with a RED derivation"
      min_lines: 60
  key_links:
    - from: "src/frontend/screens/Game/GamePage/components/MainButton.tsx"
      to: "public/locales/en/gamelib.json"
      via: "t('gamelib:steam.status.resumeInstall', ...)"
      pattern: "gamelib:steam\\.status\\.resumeInstall"
    - from: "src/frontend/hooks/constants.ts"
      to: "public/locales/en/gamelib.json"
      via: "getStatusLabel notInstalled branch"
      pattern: "gamelib:steam\\.status\\.resumeInstall"
    - from: "src/frontend/screens/Game/GamePage/components/GameStatus.tsx"
      to: "public/locales/en/gamelib.json"
      via: "getInstallLabel not-installing tail"
      pattern: "gamelib:steam\\.status\\.resumeInstallHint"
    - from: "src/frontend/hooks/__tests__/hasStatus.reconcile.test.ts"
      to: "src/frontend/screens/Game/GamePage/components/__tests__/faithfulTranslate.ts"
      via: "makeFaithfulT replaces the defaults-returning fakeT"
      pattern: "makeFaithfulT"
---

<objective>
Retire the stale "Finish in Steam" copy shown for an incomplete on-disk native
Steam install (`install.steamResumePending` true / statusContext
`'steam-incomplete'`) and replace it with copy that tells the truth: GameLib
resumes the download itself.

The copy dates from Phase 21 (D-UAT-09, plan 21-17), when GameLib genuinely
could not finish a native Steam install. Phase 23 shipped GameLib's own native
depot downloader (`reconcilePartialState` / `resumeInterruptedSteamInstall`),
and the operator confirmed live on hardware 2026-08-19 that clicking this
surface starts GameLib's own depot download and correctly reuses the verified
portion of a partial install.

Purpose: the action is already correct — only the words are wrong. Users are
being told to go do something in Steam that GameLib already does for them.
Output: three render call sites repointed at two new fork-owned i18n keys, the
two existing suites converted to measure the RENDERED string, and a new
repo-wide census gate that fails if any stale-copy site survives.

**This is a COPY/labelling change only. No click handler, no routing, no
install dispatch, no backend logic changes.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

Read before editing (each once — do not re-read a range already in context):
@src/frontend/screens/Game/GamePage/components/MainButton.tsx
@src/frontend/screens/Game/GamePage/components/GameStatus.tsx
@src/frontend/hooks/constants.ts
@src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamIncomplete.test.tsx
@src/frontend/screens/Game/GamePage/components/__tests__/faithfulTranslate.ts
@src/frontend/screens/Game/GamePage/components/__tests__/mainButtonCatalogDefaults.test.ts
@src/frontend/screens/Game/GamePage/components/__tests__/labelSuiteI18nCensus.test.ts
@src/frontend/hooks/__tests__/hasStatus.reconcile.test.ts

<census>
<!-- COMPLETE census of every site carrying the stale framing, taken during
     planning. There are exactly three RENDER sites, one catalog entry, and
     two test suites. Backend hits are comment prose only. -->

RENDER SITES (all three must change):
  1. src/frontend/screens/Game/GamePage/components/MainButton.tsx:248
     `{t('status.steamFinishInSteam', 'Finish in Steam')}`
     — the big GamePage primary button, inside a <span className="buttonWithIcon">
       preceded by <Warning />. Guarded by the branch at lines 238-251
       (runner === 'steam' && !is_installed && install?.steamResumePending
        && !is.installing && !is.queued).

  2. src/frontend/hooks/constants.ts:58, inside getStatusLabel's `notInstalled` entry
     `runner === 'steam' && statusContext === 'steam-incomplete'
        ? t('gamepage:status.steamFinishInSteam', 'Finish in Steam')
        : t('gamepage:status.notinstalled')`
     — this is the label consumed by the game CARD, library list and submenu.
       Fed by hasStatus.ts:179-193, which threads `'steam-incomplete'` into
       statusContext when `newGameInfo?.install?.steamResumePending`.

  3. src/frontend/screens/Game/GamePage/components/GameStatus.tsx:142-144
     `if (runner === 'steam' && !is_installed && statusContext === 'steam-incomplete')
        return t('status.steamFinishInSteam', 'Finish in Steam')`
     — the italic explanatory status line under the title. Its siblings here are
       full sentences ("This game is not installed", "Setting up Steam…"), so it
       gets the longer hint string, not the button string.

CATALOG:
  public/locales/en/gamepage.json:389  "steamFinishInSteam": "Finish in Steam"
  — **MUST NOT BE EDITED OR DELETED.** See <hard_constraints>.

TESTS THAT ASSERT THE OLD STRING:
  src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamIncomplete.test.tsx
    lines 5, 144, 154, 165, 176, 179, 187, 190, 197
  src/frontend/hooks/__tests__/hasStatus.reconcile.test.ts
    lines 163-172 (and its `fakeT`, which is a defaults-returning mock — see Task 2)

COMMENT PROSE ONLY (no code, no rendered copy):
  src/backend/storeManagers/steam/library.ts:832, :925
  src/backend/storeManagers/steam/__tests__/library.test.ts:1386
  MainButton.tsx:231-237, constants.ts:52-55, GameStatus.tsx:139-141,
  MainButton.steamIncomplete.test.tsx:1-8

NOT AFFECTED — do not touch:
  status.steamWaitingRestart ("Restart Steam to finish") — a DISTINCT state
  status.steamPaused ("Paused") — a DISTINCT state
  status.steamInstalling, status.settingUpBottle
  ConsoleMode/.../LaunchOverlay/index.tsx:172 `console.steam.launched`
    ("Launched in Steam") — unrelated, correct as-is
</census>

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; do NOT go
     exploring for these. -->

From src/frontend/screens/Game/GamePage/components/__tests__/faithfulTranslate.ts:
```typescript
// Reproduces i18next's REAL precedence: an explicit `ns:` prefix wins over the
// bound namespace; the inline default is consulted only when the key does not
// resolve; a key resolving nowhere falls back to the key itself.
// Resolves against gamepage.json, gamelib.json and translation.json.
export function makeFaithfulT(
  boundNamespace?: string
): (key: string, defaultValue?: string) => string

export function faithfulReactI18next(): {
  useTranslation: (ns?: string) => {
    t: (key: string, defaultValue?: string) => string
  }
}
```

From src/frontend/hooks/constants.ts:
```typescript
type StatusArgs = {
  status: Status
  t: TFunction<'gamepage', undefined>   // no CustomTypeOptions augmentation
  runner: Runner                        // exists in this repo, so the key
  statusContext?: string                // param is plain `string` — a
  percent?: number                      // `gamelib:`-prefixed key typechecks
  size?: string
}
export function getStatusLabel(args: StatusArgs): string
```

From src/backend/testUtils/stripSourceComments (already used by the two
existing gates in the same directory, resolvable from frontend suites via the
existing `backend/` module alias):
```typescript
export function stripSourceComments(source: string): string
```

Current shape of public/locales/en/gamelib.json (fork-owned namespace):
```json
{ "steam": { "bottle": { ... }, "install": { ...14 keys... } } }
```
`steam.status` does NOT exist yet — you are creating it.
</interfaces>
</context>

<hard_constraints>
**1. THE SILENT-NO-OP TRAP (this repo has been burned by it repeatedly).**
Renaming a label by editing the `t()` DEFAULT argument is a SILENT NO-OP when
the key already exists. `status.steamFinishInSteam` EXISTS in
`public/locales/en/gamepage.json`, so changing only the inline default string
changes nothing at runtime — it looks right in the diff, passes review, and
does nothing on screen. **The fix requires NEW KEYS, not edited defaults.**

**2. `public/locales/en/gamepage.json` MUST NOT BE TOUCHED — not edited, not
deleted from.** `meta/i18nCatalogChurnGuard.ts` (D-04/D-05/D-06) throws
`UpstreamChurnError` on ANY changed path under `public/locales/` that does not
end in `/gamelib.json` or `/gamelib.mt.json`, and it runs live against the
working tree inside `pnpm test:ci`. The old `status.steamFinishInSteam` entry
therefore STAYS as a dead, unreferenced key. That is expected and correct.
All new copy goes in `public/locales/en/gamelib.json`, referenced with an
explicit `gamelib:` namespace prefix — the pattern
`t('gamelib:steam.install.withOptionsLabel', …)` already proven in
MainButton.tsx and already handled by both the faithful mock and the catalog
gate.

**3. Do NOT run these:** `pnpm gen-i18n-gate-scope`, `pnpm gen-i18n-scope:rewrite`,
`meta/machineFillGamelib.ts`, `pnpm i18n`. The first two silently widen a
blocking gate and erase a hand-curated provenance marker (this has cost real
work twice). None are needed for a hand-added fork key.

**4. Do NOT touch any behaviour:** no click handler, no `handleInstall`, no
`openSteamInstallOptions`, no `quickInstallSteamGame`, no install routing, no
backend logic. The `<Warning />` icon in MainButton stays as-is — the state IS
abnormal; only the words were wrong.

**5. Do NOT merge, rename or alter** the `steam-waiting-for-restart` or
`steam-paused` statusContext values, or their copy. They are DISTINCT states.

**6. Non-English bundles** keep the stale translated copy until the next
translation sync. That is a known, accepted consequence — record it in the
SUMMARY, do NOT hand-edit other locales.

**7. A dev app instance is running against this tree.** Do not kill it. Do not
use `git stash`, `git clean`, or `git reset` under any circumstances — an
executor `git stash` has stranded a concurrent session's work here twice.

**8. Out of scope, separately filed:** the report that "Install options…" opened
no options and proceeded straight to install. Do not investigate or fix it.

**No package-manager installs occur in this plan**, so no Package Legitimacy
Gate applies.
</hard_constraints>

<approved_copy>
Two keys, because the two surfaces have different shapes. Use these EXACT
strings — inline `t()` defaults must match the catalog byte-for-byte or the
`mainButtonCatalogDefaults` gate fails by name.

| Key | Value | Surfaces |
|-----|-------|----------|
| `gamelib:steam.status.resumeInstall` | `Resume Install` | MainButton primary button; getStatusLabel (card / list / submenu) |
| `gamelib:steam.status.resumeInstallHint` | `Install incomplete — resume the download in GameLib` | GameStatus explanatory line |

Notes:
- The em dash `—` (U+2014) is deliberate and matches existing gamelib.json copy
  ("Not enough space in your Steam library — choose another below…").
- Title Case on the button matches its siblings (`Install`, `Remove from Queue`).
  Do NOT encode casing changes in the key text; casing belongs in CSS.
- `Resume Install` is short enough for the `min-width: 200px` `mainBtn` box.
</approved_copy>

<tasks>

<task type="auto">
  <name>Task 1: Add the fork-owned keys and repoint all three render sites</name>
  <files>public/locales/en/gamelib.json, src/frontend/screens/Game/GamePage/components/MainButton.tsx, src/frontend/screens/Game/GamePage/components/GameStatus.tsx, src/frontend/hooks/constants.ts, src/backend/storeManagers/steam/library.ts, src/backend/storeManagers/steam/__tests__/library.test.ts</files>
  <action>
Add a `status` sibling to `steam.bottle` / `steam.install` in
`public/locales/en/gamelib.json` with exactly the two keys and values from
`<approved_copy>`. Keep the file's existing alphabetical key ordering and its
two-space indentation; `gamelib.json` is the ONLY locale file you may modify
(hard constraint 2).

Repoint the three render sites from `<census>`, changing ONLY the key and the
inline default:

  1. `MainButton.tsx:248` → `{t('gamelib:steam.status.resumeInstall', 'Resume Install')}`.
     Leave the surrounding `<span className="buttonWithIcon">` and `<Warning />`
     untouched. Leave the branch condition at 238-244 untouched.

  2. `constants.ts:58` → `t('gamelib:steam.status.resumeInstall', 'Resume Install')`.
     The `gamelib:` prefix is REQUIRED here even though the surrounding entries
     use `gamepage:` — the explicit prefix selects the namespace regardless of
     the `TFunction<'gamepage'>` binding, and there is no `CustomTypeOptions`
     augmentation in this repo so it typechecks (see `<interfaces>`). Leave the
     `: t('gamepage:status.notinstalled')` else-branch alone.

  3. `GameStatus.tsx:143` → `return t('gamelib:steam.status.resumeInstallHint',
     'Install incomplete — resume the download in GameLib')`. First confirm
     `GameStatus.tsx`'s `useTranslation` binding is `'gamepage'` (its siblings
     call `t('status.installed')` unprefixed) — the `gamelib:` prefix is what
     crosses the namespace.

Then update the STALE COMMENT PROSE that documents the old label, so the next
reader is not told the UI says something it no longer says. Change ONLY text
inside comments — if an edit would alter any non-comment byte, skip that edit
and note it:
  - `MainButton.tsx` 231-237, `constants.ts` 52-55, `GameStatus.tsx` 139-141:
    replace the "reads as 'needs Steam to finish'" framing with the true one —
    an incomplete on-disk native install resumes in GameLib via
    `resumeInterruptedSteamInstall`; the D-UAT-09 branch survives, only its copy
    changed (quick-260819-ch5). Keep the D-UAT-09 (21-17) provenance reference
    and keep MainButton's existing note that the click handler already routes to
    `handleInstall` and needs no change.
  - `src/backend/storeManagers/steam/library.ts:832` and `:925`, and
    `src/backend/storeManagers/steam/__tests__/library.test.ts:1386`: these
    comments name the old `"Finish in Steam"` label. Update the quoted label to
    `"Resume Install"`. **Comment text only — not one byte of executable code in
    these three backend locations.**

Do NOT delete or edit `status.steamFinishInSteam` in `gamepage.json`; it becomes
a dead key by design.
  </action>
  <verify>
    <automated>node -e "const g=require('./public/locales/en/gamelib.json'); const s=g.steam.status; if(s.resumeInstall!=='Resume Install') throw new Error('resumeInstall value mismatch: '+JSON.stringify(s.resumeInstall)); if(s.resumeInstallHint!=='Install incomplete — resume the download in GameLib') throw new Error('resumeInstallHint value mismatch: '+JSON.stringify(s.resumeInstallHint)); console.log('catalog OK')" && git diff --name-only -- public/locales/ | grep -v 'gamelib\.json$' | (! grep . ) && echo "no upstream locale churn" && pnpm codecheck && pnpm i18n-churn-guard && pnpm lint-translations:gamelib</automated>
  </verify>
  <done>Both keys exist in `gamelib.json` with the exact approved values; all three render sites resolve `gamelib:steam.status.*`; `git diff --name-only -- public/locales/` lists `gamelib.json` and nothing else; `pnpm codecheck`, `pnpm i18n-churn-guard` and `pnpm lint-translations:gamelib` all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert both existing suites to measure the RENDERED string</name>
  <files>src/frontend/screens/Game/GamePage/components/__tests__/MainButton.steamIncomplete.test.tsx, src/frontend/hooks/__tests__/hasStatus.reconcile.test.ts</files>
  <behavior>
    - MainButton with `{is_installed:false, install:{steamResumePending:true}}`, not installing, not queued → rendered text contains `Resume Install`, and does NOT contain `Finish in Steam` or `Play`.
    - MainButton with `is_installed:true` → still contains `Play`, does not contain `Resume Install`.
    - MainButton mid-install with statusContext `steam-waiting-for-restart` → still `Restart Steam to finish`, no `Resume Install`.
    - MainButton mid-install with statusContext `steam-paused` → still `Installing…`, no `Resume Install`.
    - MainButton not-installed with no `steamResumePending` → neither `Resume Install` nor `Play`.
    - `getStatusLabel({status:'notInstalled', runner:'steam', statusContext:'steam-incomplete'})` → exactly `Resume Install`.
    - `getStatusLabel({status:'notInstalled', runner:'gog', statusContext:'steam-incomplete'})` → the plain notinstalled copy, unchanged.
    - `getStatusLabel({status:'notInstalled', runner:'steam', statusContext:undefined})` → the plain notinstalled copy, unchanged.
    - `getStatusLabel` for `steam-waiting-for-restart` → `Restart Steam to finish`; for `steam-paused` → `Paused`. Both unchanged.
    - Sentinel: a `t` that returned its inline default instead of the catalog value cannot make the `Resume Install` assertions pass by accident.
  </behavior>
  <action>
**A — `MainButton.steamIncomplete.test.tsx`.** This suite already routes through
`faithfulReactI18next()`, so it already measures the rendered catalog value —
keep that wiring exactly as-is (`labelSuiteI18nCensus.test.ts` enforces both
`faithfulTranslate` and `faithfulReactI18next()` appear in this file, and its
non-vacuity check names this file by name; do not rename or move it).

Retarget every assertion from `'Finish in Steam'` to `'Resume Install'`,
including the `it(...)` titles and the file's docstring. Then ADD a standing
regression pin to the first test: `expect(text).not.toContain('Finish in Steam')`
and `expect(text).not.toContain('in Steam')` — the second is the stronger one,
since it fails for any resurrection of the "go do it in Steam" framing. Keep
the `steam-waiting-for-restart` case's existing `toContain('Restart Steam to
finish')`; note that `'Restart Steam to finish'` does not contain the substring
`'in Steam'`, so the new pin only belongs on the resume-pending case.

**B — `hasStatus.reconcile.test.ts`. This is the real hole, not a rename.** Its
`fakeT` is a defaults-returning mock (proof: the existing assertion at line 178
expects the literal `'gamepage:status.notinstalled'` — the raw KEY — because
that call site passes no default). That is precisely the trap shape
`labelSuiteI18nCensus` exists to forbid, and this suite sits OUTSIDE that
census's scan directory, so it would have stayed green through any catalog
change. A test asserting the default would not have caught the original bug.

Replace `fakeT` with the shared faithful mock:
`import { makeFaithfulT } from '../../screens/Game/GamePage/components/__tests__/faithfulTranslate'`
(a bare `.ts` helper in a `__tests__` dir is never collected as a suite — see
its docstring; do NOT move or copy the helper). Bind it with
`makeFaithfulT('gamepage')`, since every `getStatusLabel` key is explicitly
namespace-prefixed and the `gamelib:` prefix must resolve through the gamelib
catalog. Cast at the call site the same way `fakeT` was cast.

Converting the mock changes what the OTHER assertions in this describe block
measure — they now compare against real catalog values. Re-run and repair each
one against `gamepage.json`/`gamelib.json`; expect `'gamepage:status.notinstalled'`
to become the real sentence and the `Downloading 42%` / `Paused` /
`Restart Steam to finish` cases to keep passing. Do not weaken any assertion to
a `toContain` to make it pass — fix the expected literal.

Retarget the D-UAT-09 case at lines 163-172 to expect exactly `'Resume Install'`,
and add a sentinel test proving the mock is faithful:
`expect(makeFaithfulT('gamepage')('gamelib:steam.status.resumeInstall',
'INLINE-DEFAULT-SENTINEL')).toBe('Resume Install')` plus
`.not.toBe('INLINE-DEFAULT-SENTINEL')`.

Add a comment in this suite recording WHY the mock changed, referencing the
ledgered "editing a `t()` default is a silent no-op when the key exists" trap.
  </action>
  <verify>
    <automated>pnpm test -- src/frontend/screens/Game/GamePage/components/__tests__ src/frontend/hooks/__tests__/hasStatus.reconcile.test.ts</automated>
  </verify>
  <done>Both suites pass. `MainButton.steamIncomplete.test.tsx` asserts `Resume Install` and pins `not.toContain('in Steam')` on the resume-pending case. `hasStatus.reconcile.test.ts` resolves `t` through `makeFaithfulT` with a passing inline-default sentinel, and no assertion in it compares against a `t()` default. `mainButtonCatalogDefaults.test.ts` and `labelSuiteI18nCensus.test.ts` still pass unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add a repo-wide residual stale-copy census gate</name>
  <files>src/frontend/screens/Game/GamePage/components/__tests__/steamResumeCopyCensus.test.ts</files>
  <behavior>
    - Scanning every `.ts`/`.tsx` under `src/` (comments stripped) finds ZERO occurrences of `steamFinishInSteam` and ZERO occurrences of the literal `Finish in Steam`.
    - The scan is non-vacuous: it visits several hundred files, and it DOES find `gamelib:steam.status.resumeInstall` in both `MainButton.tsx` and `hooks/constants.ts`, and `gamelib:steam.status.resumeInstallHint` in `GameStatus.tsx`.
    - RED: injecting the real pre-fix line `t('status.steamFinishInSteam', 'Finish in Steam')` into a scanned source string makes the gate's own predicate report a violation.
    - Comment prose mentioning the old label does NOT trip the gate (comments are stripped before matching).
    - `gamelib.json` still carries both new keys with the exact approved values, and `gamepage.json` still carries the now-dead `status.steamFinishInSteam` (proving the churn guard was respected rather than dodged by deletion).
  </behavior>
  <action>
Create `steamResumeCopyCensus.test.ts` in the GamePage components `__tests__`
directory, alongside the two existing gates whose vocabulary it shares.

Why this gate exists — put this in the file's docstring: the reported surface
was the big button, but the same stale copy lived at three independent call
sites across three directories. This repo's dominant defect shape is "fix the
reported file, miss the sibling", and an audit coarser than the defect's unit
cannot find it. A per-file assertion cannot see a residual site; only a
filesystem-enumerated census can.

Implementation requirements:
  - Walk `src/` recursively from the repo root for `.ts`/`.tsx`, skipping
    `node_modules` and this file itself (it necessarily contains both marker
    strings, in its filter and its RED derivation).
  - Run each file through `stripSourceComments` from
    `backend/testUtils/stripSourceComments` BEFORE matching — the two backend
    `library.ts` comments and every explanatory docstring legitimately discuss
    the old label, and a naive text match would report the documentation as the
    defect. This is the precedent `labelSuiteI18nCensus.test.ts` already set.
  - Assert the key `steamFinishInSteam` appears in zero scanned files, and
    report offenders BY PATH in the failure message, not just a count.
  - Assert the literal `Finish in Steam` appears in zero scanned files, same
    reporting.
  - Non-vacuity: `expect(files.length).toBeGreaterThan(200)` and explicit
    positive assertions that the three expected new-key sites are present. A
    gate that silently scans nothing guards nothing.
  - A RED block derived FROM THE REAL SOURCE, not a hand-built replica: read
    `MainButton.tsx`, string-replace the new call back to the real pre-fix
    `t('status.steamFinishInSteam', 'Finish in Steam')`, assert the replacement
    actually changed the source, and assert the gate's predicate flags it. A
    grep assertion that has never failed against a known-bad input is not a gate.
  - A catalog block asserting `gamelib.json` holds both approved values and that
    `gamepage.json` STILL holds `status.steamFinishInSteam` — this pins hard
    constraint 2 so a future agent cannot "clean up" the dead key and trip
    `UpstreamChurnError`.

Confirm this new file does not perturb the two existing gates: it must not
contain both `jest.mock('react-i18next'` and `from '../MainButton'` (which is
what `labelSuiteI18nCensus` filters on), and it must not be a `.tsx`. Use
`readFileSync` on `MainButton.tsx` rather than importing it.
  </action>
  <verify>
    <automated>pnpm test -- src/frontend/screens/Game/GamePage/components/__tests__ && grep -rn "steamFinishInSteam" src/ --include="*.ts" --include="*.tsx" | grep -v "steamResumeCopyCensus.test.ts" | grep -v '^\s*\*' | (! grep -v '//' ) && echo "zero residual call sites"</automated>
  </verify>
  <done>`steamResumeCopyCensus.test.ts` passes, including its non-vacuity and RED blocks. The full GamePage components `__tests__` directory passes, `labelSuiteI18nCensus` still reports its census as non-vacuous, and `mainButtonCatalogDefaults` still passes with the new `gamelib:steam.status.resumeInstall` call enrolled automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This plan changes only display strings and test files. No new input crosses any boundary; no parsing, no IPC surface, no network, no filesystem write outside the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ch5-01 | Information disclosure | New user-facing copy | accept | Both strings are static UI labels with no interpolation, no game/user identifiers, and no path or token content. |
| T-ch5-02 | Tampering | `public/locales/` catalogs | mitigate | `pnpm i18n-churn-guard` runs in Task 1's verify and blocks any write outside `gamelib.json`; Task 3 pins the untouched `gamepage.json` key. |
| T-ch5-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs occur in this plan. |
</threat_model>

<verification>
Run from the repo root, after all three tasks:

1. `pnpm codecheck` — clean.
2. `pnpm lint --cache` — clean (or no new findings versus the pre-change baseline).
3. `pnpm test -- src/frontend/screens/Game/GamePage/components/__tests__ src/frontend/hooks/__tests__` — all green.
4. `pnpm i18n-churn-guard` — passes; `git diff --name-only -- public/locales/` lists `gamelib.json` ONLY.
5. `pnpm lint-translations:gamelib` — passes.
6. `pnpm test -- meta/__tests__` — the churn-guard live-tree block passes against the real working tree.
7. Behaviour non-regression by inspection: `git diff` contains no change to any
   `onClick`, `handleInstall`, `openSteamInstallOptions`, install-routing or
   backend executable line. Backend hunks must be comment-only.

<human-check>
A dev instance is already running against this tree. On a Steam game with an
incomplete on-disk native install, confirm the primary button now reads
**Resume Install** (not "Finish in Steam"), the status line beneath the title
reads **Install incomplete — resume the download in GameLib**, and the game's
card in the library list carries **Resume Install**. Clicking must still start
GameLib's own native depot download — unchanged behaviour, confirmed live on
2026-08-19. Do not restart or kill the running instance to perform this check
if a hot reload suffices.
</human-check>
</verification>

<success_criteria>
- All three render sites resolve `gamelib:steam.status.*`; zero non-comment
  occurrences of `steamFinishInSteam` or `Finish in Steam` remain under `src/`.
- The new copy is asserted by tests that measure the RENDERED catalog value; the
  defaults-returning `fakeT` in `hasStatus.reconcile.test.ts` is gone.
- `steam-waiting-for-restart` and `steam-paused` copy and semantics unchanged.
- `public/locales/en/gamepage.json` is byte-identical to HEAD.
- No behavioural diff: no click handler, routing, dispatch or backend logic changed.
</success_criteria>

<output>
Create `.planning/quick/260819-ch5-fix-the-stale-finish-in-steam-label-on-i/260819-ch5-SUMMARY.md` when done.

The SUMMARY must record:
- The complete three-site census and that all three were closed (not just the
  reported button).
- That non-English bundles retain the stale translated copy until the next
  translation sync — a known, accepted consequence.
- That `status.steamFinishInSteam` remains in `gamepage.json` as a deliberately
  dead key, because `i18nCatalogChurnGuard` forbids editing upstream-owned
  catalogs — and that Task 3's gate pins this so it is not "cleaned up" later.
- That `hasStatus.reconcile.test.ts` carried a defaults-returning `t` mock
  outside `labelSuiteI18nCensus`'s scan scope, and how it was closed.
</output>
