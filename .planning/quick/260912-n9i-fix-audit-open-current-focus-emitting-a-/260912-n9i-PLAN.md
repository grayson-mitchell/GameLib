---
phase: quick-260912-n9i
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: ['QT-260912-n9i']
files_modified:
  - .planning/debug/deep-link-open-url-abort.md
  - .planning/debug/download-queue-require-crash.md
  - .planning/debug/humankind-depot-full-stall.md
  - .planning/debug/nile-spawn-app-side-latency.md
  - .planning/todos/pending/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md

must_haves:
  truths:
    - 'An operator running `gsd-sdk query audit-open` reads a sentence, not a YAML key, as the hypothesis of all four in-scope debug sessions.'
    - 'The other 13 debug sessions emit byte-identical hypothesis values to the captured baseline.'
    - 'Every value under `counts` in audit-open output is byte-identical to the captured baseline.'
    - 'The comparison that proves the two statements above is demonstrated capable of FAILING (negative control), so a green result is not a probe that measured nothing.'
    - 'The open todo records item B as DONE with its measured delta, and remains OPEN in pending/ with items A, C and D unclosed.'
    - '`pnpm planning-gates` is 10/10 after the edits, as it was before.'
  artifacts:
    - path: '.planning/debug/deep-link-open-url-abort.md'
      provides: 'One ASCII prose line directly under `## Current Focus`, above the untouched `hypothesis: |` block'
      contains: 'Abort unresolved'
    - path: '.planning/debug/download-queue-require-crash.md'
      provides: 'One ASCII prose line directly under `## Current Focus`, above the untouched `reasoning_checkpoint:` block'
      contains: 'Cause confirmed'
    - path: '.planning/debug/humankind-depot-full-stall.md'
      provides: 'One ASCII prose line directly under `## Current Focus`, above the untouched `reasoning_checkpoint:` block'
      contains: 'Decode is the'
    - path: '.planning/debug/nile-spawn-app-side-latency.md'
      provides: 'One ASCII prose line directly under `## Current Focus`, above the untouched superseded checkpoint key'
      contains: 'refuted'
    - path: '.planning/todos/pending/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md'
      provides: 'A new dated section recording item B DONE, the 1-vs-4 census delta, and the two empty-emitting sessions as a distinct unfixed defect'
      contains: 'ITEM B'
  key_links:
    - from: '.planning/debug/*.md `## Current Focus`'
      to: 'gsd-sdk query audit-open `debug_sessions[].hypothesis`'
      via: 'audit-open.js:46-50 first-non-empty-line slice to 100 chars'
      pattern: 'hypothesis'
---

<objective>
`audit-open` does not parse YAML. It takes the FIRST non-empty line after the `## Current Focus`
heading, slices it to 100 characters, and emits that as a debug session's `hypothesis`
(`audit-open.js:46-50`, gsd-sdk v1.42.3, PATH binary `/Users/graysonmitchell/.local/bin/gsd-sdk`).
Four open debug sessions lead that section with a YAML key, so the operator reads a key name
instead of the finding.

Purpose: make the four sessions legible in `audit-open` output WITHOUT restructuring any YAML.
Output: one prose line inserted into each of four debug files, a record update on the governing
todo, and a programmatic post-condition proving nothing else in the tool's output moved.

MEASURED, DO NOT RE-DERIVE. These were measured live by the orchestrator at HEAD `57e5068f8`
today. Build on them; do not re-run the census.

| file (under `.planning/debug/`) | offending first line | currently emitted as `hypothesis` |
| --- | --- | --- |
| `deep-link-open-url-abort.md` (heading L229) | L231 `hypothesis: \|` | `"hypothesis: \|"` |
| `download-queue-require-crash.md` (heading L8) | L10 `reasoning_checkpoint:` | `"reasoning_checkpoint:"` |
| `humankind-depot-full-stall.md` (heading L37) | L39 `reasoning_checkpoint:` | `"reasoning_checkpoint:"` |
| `nile-spawn-app-side-latency.md` (heading L121) | L123 `reasoning_checkpoint (SUPERSEDED - ...):` | truncated mid-word at 100 chars |

The fourth was NOT in the original brief. The orchestrator found it in the baseline; it is the
same defect with the same remedy, so it was folded in deliberately.

OUT OF SCOPE, RECORD ONLY: `knowledge-base.md` and `steam-install-options-opens-nothing.md` emit an
EMPTY `hypothesis`. Different cause: neither file has a `## Current Focus` heading at all. Writing
one is AUTHORSHIP (deciding what the current focus IS), not a shape fix. Record it; do not fix it.

LEAVE ALONE: `uninstall-game-vanishes` (leads with `- hypothesis: NEITHER family is confirmed...`)
and `wazhack-uninstall-reverts` (leads with a `>` blockquote). Both are already legible prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

Baseline artifact (captured by the orchestrator at HEAD `57e5068f8`, 75025 bytes):
`/private/tmp/claude-501/-Users-graysonmitchell-Projects-GameLib/de4e5ce0-5c62-42b9-a52b-51d32b1b037d/scratchpad/audit-open-BASELINE.json`

Baseline `counts`: debug_sessions 17, quick_tasks 256, threads 0, todos 5, seeds 0, uat_gaps 20,
verification_gaps 15, context_questions 0, total 313.

All scratch files go in that same scratchpad directory, never in the repo and never in `/tmp`.
The verify blocks below use `$SCRATCH`; export it once before running anything:

    export SCRATCH=/private/tmp/claude-501/-Users-graysonmitchell-Projects-GameLib/de4e5ce0-5c62-42b9-a52b-51d32b1b037d/scratchpad

An unset `$SCRATCH` expands to an empty string and turns every scratch path into a bare filename in
the repo root. Assert it is set and that the baseline exists there before the first gate runs.

PLATFORM TRAP, MEASURED BY THE PLANNER — do NOT reintroduce `grep -qv` into any gate here. On this
machine BSD `grep -qv PATTERN file` exits **1 even when non-matching lines exist** (the same
invocation without `-q` prints the offending line and exits 0). An earlier draft of Task 1's gate
used `! grep -qvE ...` and passed against deliberately-perturbed input — it was structurally
incapable of failing. Both gates below are count-based and were exercised against perturbed inputs.
</context>

<hard_constraints>
Violating any of these fails the task even if every gate is green.

1. Do NOT flatten, dedent, quote, re-indent or otherwise restructure ANY block scalar anywhere.
   Quick `260912-9v7` measured that sweep (652 fields / 51 files) and DECLINED it on measurement:
   flattening body `expected: |` regresses `uat render-checkpoint`, which is correct today. This
   task ADDS a prose line and changes nothing else. Every YAML line below each insertion stays
   byte-identical.
2. Do NOT touch anything under `.planning/phases/` or `.planning/milestones/`.
3. Do NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md`. This repo BANS gsd-sdk STATE/ROADMAP
   writes outright (they corrupt the file). Never write either via gsd-sdk, for any reason.
4. Do NOT commit. The working tree is ALREADY DIRTY at baseline (a modified file under
   `.planning/todos/completed/`, plus untracked `.claude/skills/archify/`, two untracked
   `.planning/quick/` dirs and `skills-lock.json`), so any broad `git add -A` or `gsd-sdk commit`
   would absorb unrelated work — `gsd-sdk commit` stages the entire tree. The orchestrator owns the
   commit. Leave your changes in the working tree.
5. Do NOT close the todo and do NOT move it to `completed/`.
6. Do NOT use bare `grep -c` on a PROSE file as a gate — comments and prose count toward the match.
   (Task 1's `grep -c` runs against machine-generated `git diff --numstat` output, which has no
   prose and no comments, and is paired with an exact line-count assertion. That is the exception,
   not a licence to count matches in a document.)
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Insert one prose line under `## Current Focus` in each of the four debug files</name>
  <files>
    .planning/debug/deep-link-open-url-abort.md,
    .planning/debug/download-queue-require-crash.md,
    .planning/debug/humankind-depot-full-stall.md,
    .planning/debug/nile-spawn-app-side-latency.md
  </files>
  <action>
    All four files already have the shape `## Current Focus` / blank line / YAML key. For each,
    insert a prose line plus one blank line immediately after the existing blank line, so the
    result reads: heading, blank, PROSE, blank, the original YAML key line untouched. Do not touch
    the YAML.

    Use these four strings verbatim. Each was already measured by the planner: pure ASCII, 88-92
    characters, so it survives `audit-open`'s 100-char slice whole and cannot be cut mid-token.

    - `deep-link-open-url-abort.md` (before L231 `hypothesis: |`):
      Abort unresolved and not reproducing; the panic hook is armed to self-diagnose the next one.
    - `download-queue-require-crash.md` (before L10 `reasoning_checkpoint:`):
      Cause confirmed and fixed; awaiting a live packaged-app cancel and completion confirmation.
    - `humankind-depot-full-stall.md` (before L39 `reasoning_checkpoint:`):
      Decode is the ceiling: the SEA sidecar spawns no workers, so every chunk decodes inline.
    - `nile-spawn-app-side-latency.md` (before L123, the SUPERSEDED checkpoint key):
      Candidate (a) is refuted; blocked on the live self-warm-up isolation test before any fix.

    Each string was derived by reading that session's own current recorded state. BEFORE inserting,
    read enough of each file to confirm the sentence is TRUE of that session today; if any is false
    or stale, write a truer one instead and say so in the SUMMARY. A replacement must stay pure
    ASCII (no em dash, no smart quotes — a multi-byte character makes the length assertion and the
    tool's own slice disagree) and under 100 characters.

    Note for `nile-spawn-app-side-latency.md`: the section it leads is explicitly SUPERSEDED, and
    the live checkpoint is the `## 2026-08-06 falsification checkpoint` further down. The prose line
    must describe the CURRENT state (candidate (a) refuted, blocked on the decisive live test), not
    the superseded one directly beneath it.
  </action>
  <verify>
    <automated>
    cd /Users/graysonmitchell/Projects/GameLib &amp;&amp; \
    git diff --numstat -- .planning/debug/ &gt; "$SCRATCH/numstat.txt" &amp;&amp; \
    test "$(wc -l &lt; "$SCRATCH/numstat.txt")" -eq 4 &amp;&amp; \
    test "$(grep -cE '^2[[:space:]]+0[[:space:]]+\.planning/debug/' "$SCRATCH/numstat.txt")" -eq 4 &amp;&amp; \
    for f in deep-link-open-url-abort download-queue-require-crash humankind-depot-full-stall nile-spawn-app-side-latency; do \
      awk '/^## Current Focus$/{found=1; next} found &amp;&amp; NF {print; exit}' ".planning/debug/$f.md" \
        | awk '{ if (length($0) &gt;= 100) exit 1; if ($0 ~ /^[a-zA-Z_][a-zA-Z_0-9 ()]*:/) exit 1; n=split($0,a,""); for(i=1;i&lt;=n;i++) if (a[i] !~ /[[:print:]]/) exit 1 }' || exit 1; \
    done
    </automated>
  </verify>
  <done>
    BOTH HALVES OF THIS GATE WERE PRE-VALIDATED BY THE PLANNER AGAINST PERTURBED INPUT, so a green
    here means something:
    - The `awk` prose assertion was run at HEAD `57e5068f8` against all four files UNMODIFIED and
      returned FAIL on every one (each currently leads with a `key:` shape, including nile's
      parenthesised `reasoning_checkpoint (SUPERSEDED ...):`), and PASS against the deep-link prose
      string. If it ever passes against an unmodified file, the gate itself has broken.
    - The numstat assertion returned PASS on an exact four-line `2 0` fixture and FAIL on each of:
      a perturbed `3 1` line, an extra out-of-scope file appended, and a three-file (short) case.

    Result: `git diff --numstat` reports exactly four lines, all `2  0`, all under
    `.planning/debug/` (proving insert-only — 2 lines added, 0 removed, no YAML disturbed, nothing
    else touched), and the first non-empty line after each `## Current Focus` heading is ASCII prose
    under 100 characters that does not begin with a `key:` shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record item B as DONE on the governing todo, without closing it</name>
  <files>.planning/todos/pending/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md</files>
  <action>
    Append a new section to this todo. Follow the file's own established convention: newest section
    near the top, governing where it contradicts what is below, deleting NOTHING. Place it
    immediately after the H1 (`# \`audit-uat\` parses body YAML too...`) and immediately BEFORE the
    existing `## RE-MEASURED 2026-09-12 at HEAD 6c0c2a96d` section. Head it
    `## ITEM B CLOSED 2026-09-12 (quick 260912-n9i) — read this first` and state in its first line
    that it governs where it contradicts the sections below, and that nothing was deleted.

    Record all four of the following:

    1. **Item B is DONE.** Four open debug sessions
       (`deep-link-open-url-abort.md`, `download-queue-require-crash.md`,
       `humankind-depot-full-stall.md`, `nile-spawn-app-side-latency.md`) now lead their
       `## Current Focus` section with a sub-100-character ASCII prose line, so `audit-open` emits
       the finding instead of a key. No YAML was restructured; two lines were added per file.

    2. **The measured delta, and why it matters.** This todo's own census recorded this population
       as **1** field — it classified only BLOCK SCALARS, so the only `audit-open`-reachable field
       it could see was `deep-link-open-url-abort.md:231` `hypothesis: |`. The live tool shows
       **4** sessions reaching the same `audit-open.js:46-50` 100-char slice, because **3 of the 4
       are PLAIN KEYS, not block scalars**. The census's own instrument could not see them: the
       defect is "the first line after the heading is a YAML key", and block-scalar-ness is
       incidental to it. Record this as an instrument limitation, not an arithmetic error.

    3. **Consequence for the RE-MEASURED reachability table.** That table's
       `reaches-as-bare-indicator: 3` counted `deep-link-open-url-abort.md:231` as one of the three.
       That field no longer reaches `audit-open` — the new prose line is emitted instead — so the
       two surviving `reaches-as-bare-indicator` fields are `27-UAT.md:31` and `27-UAT.md:51`, both
       via `audit-uat`. The block scalar itself is UNCHANGED on disk and still reads correctly for
       a human; only what the tool slices has moved.

    4. **A distinct, unfixed defect.** `knowledge-base.md` and
       `steam-install-options-opens-nothing.md` emit an EMPTY `hypothesis`. The cause is different:
       neither has a `## Current Focus` heading at all, so the slice has nothing to take. Writing
       one is authorship — deciding what that session's current focus IS — not a shape fix, so it
       was deliberately NOT done here. Left open and named so the next author does not mistake it
       for the same bug.

    Then state explicitly that the todo STAYS OPEN in `pending/`: items A (the two `reason: |` in
    `27-UAT.md`), C (the `expected: |` whole-file suppression) and D (17 milestone-hidden fields)
    are untouched.

    Leave the frontmatter EXACTLY as it is — `severity: major`, `platform: any`, `ready: human`,
    bare lowercase, unquoted. The CI gate `.planning/todos/todo-frontmatter-gate.py` matches these
    values bare, lowercase and exact; do not requote, reorder or revalue them. Do not change the
    title or `files:` list.
  </action>
  <verify>
    <automated>
    cd /Users/graysonmitchell/Projects/GameLib &amp;&amp; \
    T=.planning/todos/pending/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md &amp;&amp; \
    test -f "$T" &amp;&amp; \
    test ! -e .planning/todos/completed/2026-09-11-audit-uat-reads-block-scalars-in-document-bodies.md &amp;&amp; \
    grep -qn '^severity: major$' "$T" &amp;&amp; grep -qn '^platform: any$' "$T" &amp;&amp; grep -qn '^ready: human$' "$T" &amp;&amp; \
    grep -qn '^## ITEM B CLOSED 2026-09-12' "$T" &amp;&amp; \
    grep -q 'steam-install-options-opens-nothing' "$T" &amp;&amp; grep -q 'knowledge-base' "$T" &amp;&amp; \
    test "$(git diff --numstat -- "$T" | cut -f2)" -eq 0 &amp;&amp; \
    python3 .planning/todos/todo-frontmatter-gate.py
    </automated>
  </verify>
  <done>
    The todo is still in `pending/` and absent from `completed/`; its frontmatter triple is
    byte-unchanged and the frontmatter gate passes; the new `## ITEM B CLOSED` section exists and
    names both empty-emitting sessions; `git diff --numstat` shows ZERO deleted lines on the file,
    proving the update is purely additive.
  </done>
</task>

<task type="auto">
  <name>Task 3: Discharge the pre-registered post-condition against the baseline, with a negative control</name>
  <files>
    (no repo files modified — writes only to the session scratchpad)
  </files>
  <action>
    Discharge this programmatically. Do NOT eyeball a rendered `cat`: a shell render in this repo
    has been measured to silently DROP substrings, so every claim here comes from parsed JSON.

    Step 1 — re-capture. Run `gsd-sdk query audit-open` and write the result to
    `$SCRATCH/audit-open-AFTER.json`. The SDK may answer with an `@file:<path>` indirection instead
    of inline JSON (the init handler does); if the captured output starts with `@file:`, copy the
    file it names to `audit-open-AFTER.json` before comparing. Assert the AFTER file parses as JSON
    before comparing anything.

    Step 2 — pre-flight the baseline. Assert `$SCRATCH/audit-open-BASELINE.json` is 75025 bytes and
    its `counts.total` is 313. A comparison against a baseline you have not verified is a probe that
    measures nothing.

    Step 3 — write ONE Node comparator, `$SCRATCH/compare-audit-open.mjs`, taking two JSON paths as
    `process.argv` (pass paths POSITIONALLY and read them from `argv`; a prior session's entire
    negative-control battery reported a cheerful green because every mutation threw ENOENT on a
    mis-passed path and therefore did nothing). Derive the session identity key from the BASELINE's
    actual shape rather than assuming a field name — inspect one `debug_sessions[]` element first
    and key on whatever names the file (e.g. `file`/`path`/`name`). The comparator exits non-zero
    with a named reason on ANY of:
      a. the SET of debug-session keys differs between baseline and after (nothing added, nothing
         dropped);
      b. any session OUTSIDE the four in-scope files has a `hypothesis` differing by a single byte
         from baseline — this must cover all 13 others, including the 2 empty-emitting ones and the
         2 already-legible ones;
      c. any of the four in-scope sessions' `hypothesis` is not exactly the prose line inserted in
         Task 1, or is >= 100 characters, or ends mid-token (assert it ends with the sentence's
         final character, not a partial word);
      d. any value under `counts` differs from baseline, compared key-by-key over the UNION of both
         objects' keys (a key present in one and absent in the other is a difference, not a skip).

    Step 4 — NEGATIVE CONTROL, before trusting any green. Prove the comparator can fail. Make two
    perturbed COPIES OF THE BASELINE in the scratchpad (never edit the baseline itself):
      - `NEG-hypothesis.json`: flip one character in the `hypothesis` of one NON-in-scope session.
      - `NEG-counts.json`: change one `counts` value (e.g. `todos` 5 -> 6).
    Run the comparator against each. BOTH must exit NON-ZERO, each naming its own reason (b and d
    respectively). If either exits zero, the comparator is blind and its green verdict on the real
    run is worthless — fix the comparator and re-run everything. Record both exit codes in the
    SUMMARY.

    Step 5 — run `pnpm planning-gates`. It was 10/10 before; assert exit 0 AND that the run reports
    10 gates. A pass count is required, not just an exit code: a discovery glob that collapses to
    zero would otherwise report a cheerful green.

    Step 6 — report in the SUMMARY: the comparator's verdict, the two negative-control exit codes,
    the four emitted `hypothesis` strings verbatim as PARSED FROM JSON (not retyped), the
    before/after `counts.total`, and the planning-gates count. If any assertion fails, STOP and
    report — do not adjust the gate to admit what failed.
  </action>
  <verify>
    <automated>
    cd /Users/graysonmitchell/Projects/GameLib &amp;&amp; \
    node "$SCRATCH/compare-audit-open.mjs" "$SCRATCH/audit-open-BASELINE.json" "$SCRATCH/audit-open-AFTER.json" &amp;&amp; \
    ! node "$SCRATCH/compare-audit-open.mjs" "$SCRATCH/audit-open-BASELINE.json" "$SCRATCH/NEG-hypothesis.json" &amp;&amp; \
    ! node "$SCRATCH/compare-audit-open.mjs" "$SCRATCH/audit-open-BASELINE.json" "$SCRATCH/NEG-counts.json" &amp;&amp; \
    pnpm planning-gates
    </automated>
  </verify>
  <done>
    The comparator passes on the real AFTER capture and FAILS on both perturbed baselines (proving
    it can fail); all four in-scope sessions emit their inserted prose intact and under 100
    characters; the other 13 sessions' hypotheses and every `counts` value are byte-identical to the
    baseline; `pnpm planning-gates` reports 10/10.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree -> git history | An over-broad stage would commit unrelated baseline-dirty work |
| `.planning/` docs -> gsd-sdk readers | Text shape here is consumed by `audit-open` / `audit-uat` / gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-n9i-01 | Tampering | `.planning/debug/*.md` YAML blocks | mitigate | Insert-only edit; the numstat gate asserts exactly four `2 0` lines, so any deletion, reflow or stray file fails Task 1 |
| T-n9i-02 | Tampering | unrelated dirty working-tree files | mitigate | Executor does not commit at all (hard constraint 4); orchestrator owns the commit with explicit paths |
| T-n9i-03 | Information disclosure | audit-open output in SUMMARY | accept | Content is project planning prose already in the repo; no secrets, no PII |
| T-n9i-04 | Repudiation | a green gate that measured nothing | mitigate | Task 3's mandatory negative control, plus both Task 1 gates pre-validated against perturbed input by the planner (the first draft used `grep -qv` and could not fail on this platform) |
| T-n9i-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this task; nothing to audit |
</threat_model>

<verification>
1. Four debug files changed, `2` insertions / `0` deletions each, nothing else under `.planning/`
   except the additive todo update.
2. `gsd-sdk query audit-open` emits the four inserted prose lines as those sessions' `hypothesis`.
3. All 13 other sessions' `hypothesis` values and every `counts` value byte-identical to baseline.
4. The comparator is proven capable of failing on both a perturbed hypothesis and a perturbed count.
5. `pnpm planning-gates` 10/10, including `todo-frontmatter-gate.py`.
6. The todo remains in `pending/`, frontmatter triple unchanged, zero deleted lines.
</verification>

<success_criteria>
An operator running `gsd-sdk query audit-open` reads a true sentence for all four sessions instead
of a YAML key; nothing else in the tool's output moved, and that claim rests on a comparison
demonstrated capable of failing. The governing todo records item B as done with the 1-vs-4 census
delta and the separate empty-heading defect, and stays open.
</success_criteria>

<output>
Create `.planning/quick/260912-n9i-fix-audit-open-current-focus-emitting-a-/260912-n9i-SUMMARY.md`
when done. Report in it: the four emitted `hypothesis` strings as parsed from JSON, the two
negative-control exit codes, before/after `counts.total`, the planning-gates count, and any prose
line you replaced because the supplied one was not true of that session.

Do NOT commit (hard constraint 4).
</output>
