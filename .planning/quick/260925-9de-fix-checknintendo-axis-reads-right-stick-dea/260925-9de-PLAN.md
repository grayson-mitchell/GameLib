---
phase: quick-260925-9de
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # ALWAYS written (instrumentation in, instrumentation out, plus the todo):
  - src/frontend/helpers/gamepad.ts
  - src/preload/api/tauriGamepadInput.ts
  - .planning/todos/pending/2026-09-25-no-layout-dispatches-l3-r3-stick-clicks.md
  # H-A BRANCH ONLY -- written ONLY IF Task 1 measures M1 != 3:
  - src/frontend/helpers/gamepad_layouts/nintendo.ts
  - src/frontend/helpers/__tests__/nintendoLayout.test.ts
  # H-B BRANCH ONLY -- written ONLY IF Task 1 measures M1 == 3, or if Task 5 fails after the
  # H-A fix landed (which would mean BOTH hypotheses are true):
  - src/preload/__tests__/gamepadAction.test.ts
autonomous: false
requirements: [QUICK-260925-9de]

must_haves:
  truths:
    # BRANCH-INDEPENDENT -- these hold whichever hypothesis the measurement selects.
    - "The right stick's REAL vertical axis index on the operator's PowerA Advantage Wired Controller for Nintendo Switch 2 was MEASURED off the live pad BEFORE any axis constant was written and BEFORE either hypothesis was selected. No task committed to `axes[4]`, `axes[3]` or any other index on the strength of `checkGamecube`'s constants, the W3C convention, or symmetry with the left stick."
    - "The selection between H-A and H-B is recorded in SUMMARY.md as a decision DRIVEN BY the measured value, naming that value, and naming which of Tasks 2/3/4 therefore did NOT run."
    - "On the operator's pad, pushing the right stick UP scrolls the library toward the top and DOWN scrolls toward the bottom -- confirmed LIVE by the operator at Task 5, not inferred from a synthetic test."
    - "`src/preload/__tests__/gamepadAction.test.ts:268-282` (REQ-34.1-06, rightStickUp decreases scrollTop and rightStickDown increases it) is GREEN and UNEDITED. The sign convention was never the defect and did not regress."
    - "The LEFT stick, the d-pad hat branch, and the four face caps on BOTH mappings behave exactly as quick-260923-qe5 left them -- re-confirmed live at Task 5 and by the nineteen pre-existing `nintendoLayout.test.ts` cases staying green and character-identical."
    - "Every piece of temporary instrumentation added by Task 1 is GONE, proved by a `! grep -nE` residue check over both instrumented files that also forbids a comment mentioning the removed tokens."
    - "The permanent `[GAMEPAD] id=... mapping=... buttons=... axes=...` connect line at `gamepad.ts:634` and its why-it-is-permanent comment survive UNTOUCHED."
    - "A todo records that NO layout in this repo dispatches `buttons[10]`/`buttons[11]` (L3/R3), that `leftClick` is never a button binding, and that `38-C04`'s stick-click clause therefore describes a feature that does not exist -- carrying `severity: medium`, `platform: any`, `ready: code` as bare lowercase keys, with `python meta/runPlanningGates.py` passing."
    - "`38-VERIFICATION.md` and `38-HUMAN-UAT.md` are byte-identical to their pre-task state. The operator re-scores Phase 38 as a separate sitting."
    # CONDITIONAL -- exactly ONE of these two applies. A verifier must read <conditionality> and
    # SUMMARY.md to learn which, and must NOT treat the other one's absence as a gap.
    - "IF M1 != 3 (H-A): `checkNintendo` resolves its four stick-axis indices through a mapping-branched table rather than the hard-coded `axes[0..3]`, and the STANDARD row reproduces today's 0/1/2/3 exactly so standard-mapped Nintendo pads are byte-for-byte unchanged."
    - "IF M1 == 3 (H-B): `checkNintendo` is NOT modified at all, and the change is confined to `doScroll`'s target resolution in `src/preload/api/tauriGamepadInput.ts`."
  artifacts:
    # Branch-independent artifacts only. The conditional artifacts are named in <conditionality>.
    - path: "src/frontend/helpers/gamepad.ts"
      provides: "the permanent [GAMEPAD] connect diagnostic, with Task 1's temporary axis dump added and then fully removed"
      contains: "[GAMEPAD] id="
    - path: ".planning/todos/pending/2026-09-25-no-layout-dispatches-l3-r3-stick-clicks.md"
      provides: "the L3/R3-dispatched-by-nothing finding, with the triage frontmatter the CI gate requires"
      contains: "ready: code"
  key_links:
    - from: "src/frontend/helpers/gamepad.ts updateStatus"
      to: "checkNintendo"
      via: "the single existing dispatch site, which already passes controller.mapping and gains no new branch"
      pattern: "checkNintendo\\(buttons, axes, index, checkAction, controller\\.mapping\\)"
    - from: "checkNintendo's rightStickUp/rightStickDown dispatch"
      to: "doScroll in src/preload/api/tauriGamepadInput.ts"
      via: "window.api.gamepadAction({ action: 'rightStickUp' }) -> tauriGamepadAction's switch"
      pattern: "case 'rightStickUp'"
---

<objective>
On the operator's PowerA Advantage Wired Controller for Nintendo Switch 2 (`mapping: ""`,
17 buttons, 10 axes) the RIGHT STICK is dead: pushing it up or down scrolls nothing.

What is ESTABLISHED, measured live 2026-09-25. Do not upgrade anything else to this status:

- `checkNintendo` assigns all four stick axes at `nintendo.ts:191-194` as
  `axes[0]/axes[1]/axes[2]/axes[3]`, ABOVE the `if (mapping === 'standard')` branch at
  `nintendo.ts:209`. quick-260923-qe5 branched the face buttons and the d-pad on `mapping` and
  deliberately left the AXES unbranched. That gap is what this task closes -- or rules out.
- The LEFT stick moves focus in all four directions, so `axes[0]`/`axes[1]` are correct on this pad.
- Pushing the right stick LEFT/RIGHT prints `[tauriGamepadInput] unhandled gamepad action
  "rightStickLeft"` / `"rightStickRight"` in the DevTools console, from the `default` arm at
  `tauriGamepadInput.ts:394`. **Therefore `axes[2]` IS this pad's right-stick X axis -- confirmed
  live, not deduced.** The horizontal no-op is BY DESIGN (no `case` has ever existed for those two
  actions; the comment at :394 records that Electron's own switch had none either). It is out of
  scope AND it is the discriminator that established `axes[2]`.
- The sign convention is pinned GREEN by `gamepadAction.test.ts:268-282` and is not the defect.

What is NOT established, and what this plan REFUSES to decide by deduction: whether the right
stick's vertical axis is `axes[3]`. Two hypotheses predict the identical observed symptom and
require DIFFERENT fixes in DIFFERENT files:

- **H-A** -- the right stick's Y is NOT `axes[3]`. `checkNintendo` reads an axis the stick does not
  drive, so `rightStickUp`/`rightStickDown` never dispatch at all. Fix: branch the axis indices on
  `mapping`, exactly as the buttons and the hat already are
  (`src/frontend/helpers/gamepad_layouts/nintendo.ts`).
- **H-B** -- the right stick's Y IS `axes[3]`, dispatch is fine, and `doScroll` resolves the wrong
  scroll target. `doScroll` (`tauriGamepadInput.ts:203`) calls
  `document.elementFromPoint(innerWidth/2, innerHeight/2)` and passes the result to
  `findScrollableAncestor` (`:188-195`), which walks up via `isScrollable` (`:181-186` -- false if
  `scrollHeight <= clientHeight`, false if computed `overflowY` is `visible` or `hidden`) and FALLS
  BACK to `document.scrollingElement ?? document.documentElement`. In this app the document itself
  does not scroll; an inner container does. A failed walk is therefore a silent no-op. Fix: target
  resolution (`src/preload/api/tauriGamepadInput.ts`).

The console CANNOT separate them: `rightStickUp`/`rightStickDown` ARE handled, so they log nothing
either way. **Exactly one measurement separates them: the right stick's real Y-axis index.** Task 1
takes that measurement and BLOCKS. Tasks 2 and 3 (H-A) and Task 4 (H-B) are CONDITIONAL on its
result per `<conditionality>` below, and one of the two branches will not run at all.

Purpose: stop `checkNintendo`'s axis reads resting on the same unchecked assumption the face-button
fix existed to remove -- OR prove the assumption was fine and the fault is downstream. Which of
those, is a measurement, not a judgement.

Output: a working right-stick scroll on the operator's pad; tests proving it on BOTH mapping paths
(H-A branch) or proving the scroll target resolves correctly (H-B branch); zero instrumentation
residue; one filed todo.

This unblocks `38-C02` (right-stick scroll sign convention), which has never been scoreable, and
likely helps `38-C05`. **Both stay the operator's to SCORE. This plan makes them scoreable and
claims nothing more.**
</objective>

<conditionality>
## The decision rule. Read this before writing code in Tasks 2, 3 or 4.

Task 1's operator sitting produces four measurements:

| id | what is measured |
|----|------------------|
| **M1** | the axis INDEX whose value changes when the right stick is pushed UP and DOWN, and the SIGN of that change for UP |
| **M2** | confirmation that pushing the right stick LEFT/RIGHT changes `axes[2]` -- already live-confirmed via the `unhandled gamepad action` warning; this re-reads it on the new instrument |
| **M3** | the RESTING value of all ten axes, captured once at first frame |
| **M4** | whether a `[GAMEPAD-SCROLL]` line appears during vertical right-stick motion, and if so its `target`, `usedFallback`, `overflowY`, `scrollTopBefore` and `scrollTopAfter` fields |

M4 is not a bonus. The `[GAMEPAD-SCROLL]` diagnostic sits INSIDE `doScroll`, which is reached only
if `rightStickUp`/`rightStickDown` actually dispatched -- which happens only if `axes[3]` crossed
±0.5. **Its presence or absence is a reading of the same discriminator M1 reads, taken on an
independent instrument.** If the two disagree, that is a finding, not a nuisance: stop and report.

### Which tasks run

| M1 | M4 | verdict | tasks that RUN | tasks that DO NOT RUN |
|----|----|---------|----------------|-----------------------|
| an index **other than 3** | NO `[GAMEPAD-SCROLL]` line | **H-A CONFIRMED.** The code reads an axis the stick does not drive. H-B is UNTESTED -- the scroll path was never reached, so nothing has been learned about it either way. | Tasks 2, 3, 5, 6 | **Task 4 does not run** -- unless Task 5's live check fails; see below |
| **exactly 3** | a line appears, reporting `usedFallback=true` and/or `scrollTopBefore == scrollTopAfter` | **H-A FALSIFIED, H-B CONFIRMED.** Dispatch works; the scroll target is wrong. | Tasks 4, 5, 6 | **Tasks 2 and 3 DO NOT RUN AT ALL.** Do not "branch the axes anyway for tidiness" -- `nintendo.ts` is then not the defective file and must not be touched. |
| **exactly 3** | NO `[GAMEPAD-SCROLL]` line at all | **CONTRADICTORY -- the two instruments disagree.** If `axes[3]` really is the stick's Y, `doScroll` must have been reached and Piece B logs unconditionally on every call. The narrow benign cause is that the axis moved but never crossed the +/-0.5 threshold (limited stick throw, or a deadzone), in which case the operator's push was not full-scale and the sitting must be REPEATED before anything is concluded. | none beyond Task 1 | **STOP AND REPORT.** Ask the operator to re-run the sitting pushing the stick to its full extent; if the line still never appears, re-plan. Do NOT resolve it by picking whichever instrument you find more convincing. |
| **exactly 3** | a line appears, the target IS scrollable and `scrollTopAfter != scrollTopBefore` | **BOTH falsified.** The scroll is happening, somewhere the operator cannot see. | none beyond Task 1 | **STOP AND REPORT.** This plan does not describe that defect. Re-plan. |
| **no axis changes at all** on vertical motion | n/a | **BOTH falsified.** The pad does not expose right-stick Y as an analogue axis on this wire format. | none beyond Task 1 | **STOP AND REPORT.** Re-plan. |
| an index other than 3, **but** a line DOES appear | -- | H-A confirmed, AND `axes[3]` carries something else (a trigger resting at -1 would do it) crossing ±0.5 and firing spurious scrolls. M3 identifies it. | Tasks 2, 3, 5, 6 | Task 4 does not run. Record the spurious dispatch in SUMMARY.md -- the H-A axis branch incidentally stops it, and saying so explicitly is better than discovering it later. |

### The case the predecessor's own history warns about

H-A and H-B are competing EXPLANATIONS of one symptom, but they are **not mutually exclusive**: a
wrong axis index MASKS a broken scroll target, because the target is never asked to resolve.

- **M1 is decisive about H-A.** Index != 3 confirms it; index == 3 falsifies it outright.
- **M1 is decisive about H-B only in the `M1 == 3` row.** On the H-A path, H-B stays UNTESTED until
  the axis is right and the scroll path is exercised for the first time.
- **Therefore: if Tasks 2+3 land and Task 5's live checks 1-3 still fail, BOTH are true.** That is
  not a descope and not a failure of this plan. Enter Task 4, then return to Task 5. Say so in
  SUMMARY.md rather than quietly widening Task 3.

Why this is written down at all: quick-260923-qe5's plan predicted the hat neutral at 1.28571 and
it MEASURED 3.28571; and the parent todo's "shifted shoulder indices" claim was a DEDUCTION that
measurement falsified outright -- L/R/ZL/ZR turned out identical to standard. In this same file,
`checkGamecube` at `nintendo.ts:27-30` reads `rightAxisX = axes[3], rightAxisY = axes[4]` for the
same device family, and the 2026-09-25 live test has ALREADY FALSIFIED `axes[3]` as this pad's
right-stick X. **`axes[4]` is the most inviting wrong answer available. Do not inherit it.**
</conditionality>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260925-9de-fix-checknintendo-axis-reads-right-stick-dea/260925-9de-CONTEXT.md
@.planning/quick/260923-qe5-fix-checknintendo-trusting-chromium-stan/260923-qe5-SUMMARY.md
@src/frontend/helpers/gamepad_layouts/nintendo.ts
@src/frontend/helpers/gamepad.ts
@src/preload/api/tauriGamepadInput.ts
@src/frontend/helpers/__tests__/nintendoLayout.test.ts
@src/preload/__tests__/gamepadAction.test.ts

<interfaces>
<!-- Contracts the executor needs. Read at HEAD ddd2b8ed0, 2026-09-25. Line numbers are from that
     commit -- re-read before trusting any of them. Do NOT go exploring for these. -->

src/frontend/helpers/gamepad_layouts/nintendo.ts (CURRENT, pre-fix):
  export function checkGameCube(buttons, axes, controllerIndex, checkAction): void
    // :27-30  leftAxisX = axes[0], leftAxisY = axes[1], rightAxisX = axes[3], rightAxisY = axes[4]
    // DIFFERENT DEVICE. axes[3] is already live-falsified as the PowerA pad's right-stick X.
  export function isNintendoControllerId(id: string): boolean          // :69
  type NintendoFaceIndices = { action, back, alt, menu }               // module-private TYPE
  const STANDARD_FACE_INDICES / RAW_HID_FACE_INDICES                   // module-private
  export function nintendoFaceIndices(mapping: GamepadMappingType)     // :107-117
  const NON_STANDARD_HAT_AXIS = 9                                      // :124 module-private
  function nintendoHatDirection(hatValue): 'up'|'down'|'left'|'right'|null   // :140 module-private
  export function checkNintendo(
    buttons: readonly GamepadButton[],
    axes: readonly number[],
    controllerIndex: number,
    checkAction: (action: ValidGamepadAction, pressed: boolean, ctrlIdx: number) => void,
    mapping: GamepadMappingType                                        // ALREADY THREADED by qe5
  ): void                                                              // :175
    // :186-194  faceIndices = nintendoFaceIndices(mapping); A/B/Y/X from it;
    //           leftAxisX = axes[0], leftAxisY = axes[1],
    //           rightAxisX = axes[2], rightAxisY = axes[3]   <-- THE UNBRANCHED READS
    // :200-207  the eight stick checkAction calls, thresholds +/-0.5
    // :209-231  if (mapping === 'standard') { d-pad buttons[12..15] + guide buttons[16] }
    //           else { hat axis 9 via nintendoHatDirection, cardinals only, NO guide }
  export function checkN64Clone1(buttons, axes, controllerIndex, checkAction): void   // :235

src/frontend/helpers/gamepad.ts:
  :95    export const initGamepad = () => { ... }
  :110   function checkAction(action, pressed, controllerIndex)
           -- early-returns if controllerIsDisabled or !isFocused; `!pressed` resets triggeredAt
              to 0, which is WHY the test harness needs a PRIMING FRAME
  :188   action = 'leftClick'   // the ONLY place leftClick is ever produced -- DERIVED from
                                // mainAction when shouldSimulateClick(). Never a button binding.
  :296   if (action !== 'leftClick' && action !== 'rightClick') window.api.gamepadAction({ action })
         // => rightStickUp/Down/Left/Right ALL reach window.api.gamepadAction with no metadata
         //    gate and no special case in the switch at :182-282. They ARE observable in the
         //    nintendoLayout harness. Left/Right become a no-op only later, in PRELOAD.
  :558   function updateStatus()  -- the rAF polling loop; `const axes = controller.axes` at :567
  :579   checkNintendo(buttons, axes, index, checkAction, controller.mapping)   // single call site
  :586   the whole dispatch chain sits in try/catch -> a throw silently kills input rather than
         surfacing
  :627   function addgamepad(gamepad)
  :634   the PERMANENT `[GAMEPAD] id=... mapping=... buttons=... axes=...` line, guarded
         `window.api?.logInfo?.(...)` inside try/catch. KEEP. Extend ALONGSIDE, never duplicate.

src/preload/api/tauriGamepadInput.ts  (pure DOM; runs in the RENDERER -- which is why its
                                       console.warn output reaches the app's DevTools console):
  :37    function warn(label: string, error?: unknown): void   // console.warn(`[tauriGamepadInput] ${label}`)
  :181   function isScrollable(el: Element): boolean
           -- false if el.scrollHeight <= el.clientHeight
           -- false if getComputedStyle(el).overflowY is 'visible' or 'hidden'
  :188   function findScrollableAncestor(start: Element | null): Element
           -- walks parentElement; FALLS BACK to document.scrollingElement ?? document.documentElement
  :197   function doScroll(delta: number): void
           -- centre = document.elementFromPoint(innerWidth/2, innerHeight/2)
           -- target = findScrollableAncestor(centre)
           -- target.scrollBy({ top: delta }) if scrollBy is a function, else target.scrollTop += delta
  :354   export function tauriGamepadAction(args): Promise<void>    // WHOLE BODY in try/catch
  :~377  case 'rightStickUp': doScroll(-50);   case 'rightStickDown': doScroll(50)
  :394   default: warn(`unhandled gamepad action "${args.action}"`)
           -- rightStickLeft/rightStickRight land here BY DESIGN. Do NOT add cases. OUT OF SCOPE.

src/frontend/helpers/__tests__/nintendoLayout.test.ts  (464 lines, NINETEEN existing `it(...)`):
  makePad(index, id, opts?: PadOptions)
    PadOptions = { mapping?, buttonCount?, axisCount?, hatAxis?, hatNeutral? }
    defaults: mapping 'standard', buttonCount 17, axisCount 4, no hat, hatNeutral 0
  NON_STANDARD: PadOptions = { mapping: '', buttonCount: 17, axisCount: 10, hatAxis: 9,
                               hatNeutral: 3.28571 }
  SWITCH_PRO_ID, XBOX_ID, POWERA_ID   // POWERA_ID is the verbatim measured id string
  RAW_Y = 0, RAW_B = 1, RAW_A = 2, RAW_X = 3
  HAT_UP / HAT_RIGHT / HAT_DOWN / HAT_LEFT / HAT_UP_RIGHT / HAT_NEUTRAL
  buildHarness(options?: { stubFocusedElement?: boolean }) -> { connect, runFrame, actions }
    fakeWindow.api = { requestAppSettings, gamepadAction, setFullscreen }   // NO logInfo
    fakeDocument = { body.classList.contains -> false, querySelector -> null (or the opt-in stub) }
  pressButton(id, buttonIndex, opts?, harnessOpts?) -> string[]
  moveHat(id, hatValue, opts) -> string[]
    // throws if opts.hatAxis is undefined; makePad, connect, PRIMING FRAME, write axis, one frame
  cleanupGlobals() in afterEach
  OBSERVABILITY: `mainAction` resolves to currentElement()?.click() and is INVISIBLE in this
  DOM-less (node env, no jsdom) harness -- asserting on it is a VACUOUS PASS.
  `rightStick*`, `leftStick*`, `pad*`, `back`, `altAction` all reach window.api.gamepadAction
  and ARE observable.

src/preload/__tests__/gamepadAction.test.ts:
  :268-282  'REQ-34.1-06: rightStickUp decreases scrollTop and rightStickDown increases it'
            Builds a FakeElement (scrollHeight 500, clientHeight 200, overflowY 'auto',
            scrollTop 100) and MOCKS fakeDocument.elementFromPoint to return it. Proves the SIGN
            (100->50 up, 100->150 down) and says NOTHING about real-DOM target resolution.
            MUST STAY GREEN AND UNEDITED.

jest projects (jest.config.js:19-25). Run them as:
  npx jest --selectProjects Frontend --testPathPattern <pattern>
  npx jest --selectProjects Preload  --testPathPattern <pattern>
  NEVER a second bare argument after --selectProjects -- it is swallowed and everything runs.
  Both projects: testEnvironment node (NO jsdom, deliberately), resetMocks: true.

FORMATTING TRAP: src/preload/.prettierrc sets printWidth 120; the repo root default is 80. The
directory a path sits in changes the correct answer. Scope `npx prettier --check` to explicit
paths, NEVER `.`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING OPERATOR CHECKPOINT 1 of 2] Instrument BOTH candidate failure points, then STOP and measure the right stick's real Y axis</name>
  <files>src/frontend/helpers/gamepad.ts, src/preload/api/tauriGamepadInput.ts</files>
  <what-built>
    TWO temporary diagnostics, on two independent instruments, deliberately reading the SAME
    discriminator from opposite ends. Write them, verify they compile, are formatted, and break no
    existing suite -- then STOP. Do not begin Task 2, 3 or 4 until M1-M4 come back.

    Both pieces are TEMPORARY; Task 6 deletes them with a residue grep. Do NOT delete or modify the
    permanent `[GAMEPAD]` connect line at `gamepad.ts:634` or its comment.

    PIECE A -- the axis dump, in `src/frontend/helpers/gamepad.ts`. Re-add in the SHAPE
    quick-260923-qe5's Task 1 used and its Task 5 removed with zero residue (so the shape is proven
    removable). Behind a module-level `const AXIS_DUMP = true`, inside `updateStatus` and placed
    EARLY -- before the `id.match()` chain at :569 -- so it fires regardless of which layout
    function is selected:

      - ONE-SHOT, on the first frame seen for a controller index: emit ALL ten axis values,
        prefixed `[GAMEPAD-AXIS]` and marked `first=true`. This is M3, the resting baseline, and it
        is what lets a permanently-deflected axis (a trigger resting at -1, say) be told apart from
        a stick.
      - PER-FRAME: emit the INDEX and value of every axis whose value changed by more than 0.05
        since the previous frame, prefixed `[GAMEPAD-AXIS]`, printed to 5 DECIMAL PLACES. Five
        places is not cosmetic: qe5's hat values (-0.42857, 0.14286) are indistinguishable at two.
      - Keep previous-frame state in a module-level array so an unchanging axis logs NOTHING. This
        runs at rAF rate; an unfiltered dump is unreadable.
      - Route it through the SAME guarded channel the permanent line uses:
        `window.api?.logInfo?.(...)` inside try/catch. The `nintendoLayout` harness's
        `fakeWindow.api` has NO `logInfo`, and an unguarded call throws inside the rAF loop --
        which `updateStatus`'s try/catch at :586 would swallow while silently killing all input.

      The BUTTON half of qe5's dump is deliberately NOT re-added. Its question was answered on
      2026-09-23 and re-adding it would bury the axis lines in per-press noise. This task's question
      is about axes only.

    PIECE B -- the scroll-target dump, in `src/preload/api/tauriGamepadInput.ts`, inside `doScroll`
    (:197-210), behind a module-level `const SCROLL_DIAG = true`. Emit ONE line per call through the
    EXISTING module-private `warn()` helper at :37 -- NOT `window.api.logInfo`. `warn` is
    `console.warn`, this module runs in the renderer, and the DevTools console is a PROVEN-readable
    channel on this app: it is exactly how the operator read
    `[tauriGamepadInput] unhandled gamepad action "rightStickLeft"` on 2026-09-25. The console
    accepts no INPUT, which does not matter here -- nothing needs to be typed.

      The line is prefixed `[GAMEPAD-SCROLL]` and carries, at minimum:
        `delta`; the centre element (tag + id + first class, or the literal `null`); the resolved
        target (same shape); `usedFallback` (whether the resolved target is identical to
        `document.scrollingElement ?? document.documentElement`); the target's `scrollHeight`,
        `clientHeight` and computed `overflowY`; and `scrollTop` BOTH BEFORE AND AFTER the scroll.
      The before/after pair is the decisive datum: it separates "resolved the wrong target" from
      "resolved the right target and it still did not move".

      WRAP THE WHOLE DIAGNOSTIC IN ITS OWN try/catch. `getComputedStyle` and
      `document.scrollingElement` are not implemented by the `FakeElement`/`fakeDocument` doubles in
      `gamepadAction.test.ts`, and `tauriGamepadAction`'s OUTER try/catch at :355 would SWALLOW a
      diagnostic throw while ALSO skipping the scroll -- the instrument would break the very thing
      it exists to measure, and the test suite would be the only place you found out. An inner
      try/catch makes the diagnostic strictly additive.

    Make NO other change. Do not touch `checkNintendo`, `findScrollableAncestor`, `isScrollable`,
    the action switch, or the `default` arm.
  </what-built>
  <action>
    Write both diagnostics exactly as <what-built> specifies, run the <verify> chain, then STOP and
    hand the <how-to-verify> sitting to the operator. Take NO further action -- no test, no
    `nintendo.ts` edit, no `doScroll` edit -- until M1-M4 come back and the applicable
    <conditionality> decision-table row has been named in writing.
  </action>
  <how-to-verify>
    The app is ALREADY RUNNING under `pnpm tauri:dev` with the pad connected. DO NOT start a second
    dev server. Both edited files are in the RENDERER bundle -- `src/preload/api/tauriGamepadInput.ts`
    is pure DOM and is imported into the renderer graph (proved by its `console.warn` output reaching
    the app's DevTools console) -- so vite HMR picks both up: `pnpm build:sidecar` is NOT needed and
    must not be run.

    1. Do a FULL PAGE RELOAD rather than trusting the HMR patch: `App.tsx` defeats Fast Refresh
       (`"routes" export is incompatible`).
    2. After a reload Chromium exposes no gamepads until the page sees pad activity. Press any
       button on the pad once; that fires `gamepadconnected` and prints the permanent `[GAMEPAD]`
       line.
    3. Open TWO channels and watch BOTH -- they carry different halves of the measurement:
         - `[GAMEPAD]` / `[GAMEPAD-AXIS]`  -> C:\Users\grays\AppData\Local\GameLib\logs\gamelib.log
         - `[GAMEPAD-SCROLL]`              -> the DevTools console
    4. Navigate to the GAMES LIBRARY, i.e. a view holding MORE CONTENT THAN FITS, so that a scroll
       is possible at all. Measuring on an already-short page would produce a false negative.
    5. Perform this exact sequence, PAUSING between steps so the lines stay separable:
       a. Read the one-shot `[GAMEPAD-AXIS] first=true` line -> **M3**, the resting value of all ten
          axes. Note any axis NOT resting near 0.
       b. Push the right stick fully UP, hold ~1 second, release to centre; then fully DOWN, hold,
          release. -> **M1**: which axis INDEX moved, and what value it reached for UP and for DOWN.
       c. While doing (b), watch the DevTools console. -> **M4**: did ANY `[GAMEPAD-SCROLL]` line
          appear? If yes, copy it verbatim including `usedFallback`, `overflowY` and both `scrollTop`
          values. If none appeared, say "NO [GAMEPAD-SCROLL] LINE" explicitly -- that absence is a
          measurement, not a missing result.
       d. Push the right stick fully LEFT, release, fully RIGHT, release. -> **M2**: confirm the
          moving index is 2. The `unhandled gamepad action "rightStickLeft"` warning should reappear;
          that it still does is the cross-check that the instrument is live.
       e. Push the LEFT stick up/down/left/right -> confirms axes 0 and 1 ON THE SAME INSTRUMENT, so
          the left stick's correctness is measured here rather than inferred from "focus moves".

    ASK the orchestrator, VERBATIM: "Task 1 instrumentation is live on both instruments. Please run
    steps a-e above with the operator and paste back every `[GAMEPAD]`/`[GAMEPAD-AXIS]` line from
    gamelib.log and every `[GAMEPAD-SCROLL]` line from the DevTools console, answering M1, M2, M3 and
    M4. If NO `[GAMEPAD-SCROLL]` line appeared, say so explicitly."

    STOP THERE. Do not write a test, do not touch `nintendo.ts`, do not touch `doScroll`'s logic
    until M1-M4 come back.

    WHEN THEY COME BACK, apply `<conditionality>`'s decision table EXACTLY. In particular:
    - Read M1 off the OBSERVATION. Do NOT use `checkGamecube`'s `axes[4]`, do not use "one past
      axes[3]", do not use symmetry with the left stick.
    - If the M1/M4 pair does not EXACTLY match one of the decision table's rows, STOP AND REPORT --
      including any combination the table does not list. Do not pick the reading
      you prefer.
    - Record M1-M4 VERBATIM for SUMMARY.md, and NAME IN WRITING which table row applies, before
      touching anything else.
  </how-to-verify>
  <verify>
    <automated>cd /c/Users/grays/Projects/GameLib && npx prettier --check src/frontend/helpers/gamepad.ts src/preload/api/tauriGamepadInput.ts && pnpm codecheck && npx jest --selectProjects Frontend --testPathPattern "gamepad" && npx jest --selectProjects Preload --testPathPattern "gamepadAction"</automated>
    <!-- FULLY &&-CHAINED ON PURPOSE. A `;` would run prettier, PRINT its diff, and then discard its
         exit code, so an unformatted file would report success. Proved on this repo:
         `( false; echo x; true && true && true )` exits 0.
         The two prettier paths sit in directories with DIFFERENT printWidths (root 80,
         src/preload/.prettierrc 120). Prettier handles that itself, but it is why the paths must be
         explicit and never `.`.
         The two jest runs are the real gate on the guards: if `Frontend` goes red, Piece A's
         `window.api?.logInfo?.()` guard is missing or wrong; if `Preload` goes red, Piece B's INNER
         try/catch is missing and the diagnostic is breaking `doScroll` under the doubles. Fix that
         before asking the operator for anything. -->
    <human-check>Steps a-e above, on the operator's PowerA Advantage Wired Controller for Nintendo Switch 2, with M1, M2, M3 and M4 reported.</human-check>
  </verify>
  <resume-signal>Paste every [GAMEPAD-AXIS] line and every [GAMEPAD-SCROLL] line, and answer M1, M2, M3, M4. State explicitly if NO [GAMEPAD-SCROLL] line appeared.</resume-signal>
  <done>
    Both diagnostics are in the tree; `npx prettier --check` passes on both exact paths;
    `pnpm codecheck` is clean; the existing `Frontend` gamepad suites and the `Preload`
    `gamepadAction` suite are still green; M1-M4 are recorded verbatim; and the applicable row of
    `<conditionality>`'s decision table has been NAMED IN WRITING before any further task begins.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [CONDITIONAL -- runs ONLY IF M1 != 3] Add a generic axis mover to the harness, write the right-stick tests, RED-prove them against pre-fix source</name>
  <files>src/frontend/helpers/__tests__/nintendoLayout.test.ts</files>
  <behavior>
    **RUN THIS TASK ONLY IF Task 1 measured M1 as an axis index OTHER THAN 3 (H-A confirmed).**
    If M1 == 3, SKIP this task entirely and go to Task 4. Record the skip in SUMMARY.md, naming the
    measured value that caused it.

    Where this block names an index, M1's OBSERVATION wins; record any divergence.

    HARNESS CHANGE -- extend in place, do NOT build a parallel harness:

    - Add `moveAxis(id: string, axisIndex: number, value: number, opts: PadOptions): string[]` as a
      SIBLING to `moveHat`. It does exactly what `moveHat` does -- `jest.resetModules()`,
      `buildHarness()`, `initGamepad()`, `makePad`, `connect`, PRIMING FRAME (the released frame
      `checkAction` needs to seed `triggeredAt`), write the axis, one more frame, return
      `harness.actions()` -- but takes the axis index EXPLICITLY instead of reading `opts.hatAxis`.
    - Then REFACTOR `moveHat`'s BODY to delegate: keep its existing
      `if (opts.hatAxis === undefined) throw` guard, then
      `return moveAxis(id, opts.hatAxis, hatValue, opts)`. `moveHat`'s SIGNATURE and BEHAVIOUR are
      unchanged, so the six hat cases keep their exact current meaning. State in a comment that
      this is HARNESS PLUMBING, not a case edit -- "the existing cases are unedited" must remain
      literally true, and a reader needs to see that the delegation does not contradict it.
    - Add `RIGHT_STICK_Y_NON_STANDARD` set to M1's MEASURED index, with a comment giving the
      measurement date and stating it is MEASURED, not derived. Add `LEFT_X = 0`, `LEFT_Y = 1`,
      `RIGHT_X = 2`, with a comment recording that RIGHT_X was confirmed LIVE on 2026-09-25 by the
      `unhandled gamepad action "rightStickLeft"` warning and re-confirmed as M2.

    CASES. The existing NINETEEN `it(...)` cases stay EXACTLY as they are -- not one character.
    Unlike quick-260923-qe5 there is NO signature change here (`checkNintendo` already takes
    `mapping`), so nothing forces an edit and "unedited" is genuinely achievable. If any existing
    case needs editing to pass, the implementation is wrong: STOP rather than adjusting the test.

    (A) "a non-standard Nintendo pad dispatches rightStickUp from its MEASURED Y axis" --
        `moveAxis(POWERA_ID, RIGHT_STICK_Y_NON_STANDARD, -1, NON_STANDARD)` CONTAINS
        `'rightStickUp'` and does NOT contain `'rightStickDown'`.
        If M1's measured UP value was POSITIVE rather than negative, invert this case and (B) and
        SAY SO in a comment. The `< -0.5` / `> 0.5` thresholds and the
        `gamepadAction.test.ts:268-282` sign convention are NOT to be changed to suit the pad; a
        per-device sign inversion belongs in the axis table with its own comment and its own case,
        called out explicitly rather than absorbed.
    (B) "...and rightStickDown from the same axis pushed the other way" --
        `moveAxis(POWERA_ID, RIGHT_STICK_Y_NON_STANDARD, 1, NON_STANDARD)` contains
        `'rightStickDown'` and NOT `'rightStickUp'`.
    (C) CROSS-MAPPING CONTRAST -- the case that stops a naive implementation:
        `moveAxis(POWERA_ID, 3, -1, NON_STANDARD)` contains NEITHER `'rightStickUp'` NOR
        `'rightStickDown'` (axes[3] is not this pad's right-stick Y), WHILE
        `moveAxis(SWITCH_PRO_ID, 3, -1, {})` DOES contain `'rightStickUp'`. Without this pair, an
        implementation that simply moved EVERY Nintendo pad to the new index would satisfy (A) and
        (B) while silently destroying standard-mapped pads.
    (D) REGRESSION BAR, standard mapping, both directions:
        `moveAxis(SWITCH_PRO_ID, 3, -1, {})` -> `'rightStickUp'`;
        `moveAxis(SWITCH_PRO_ID, 3, 1, {})` -> `'rightStickDown'`;
        `moveAxis(SWITCH_PRO_ID, 2, -1, {})` -> `'rightStickLeft'`.
        (`makePad`'s default `axisCount` is 4, so indices 0-3 exist with no options bag.)
        LABEL THESE AS CONTRACT-PRESERVATION: GREEN PRE-FIX by design, NOT RED evidence.
    (E) HORIZONTAL MUST NOT MOVE -- `axes[2]` is this pad's right-stick X, confirmed LIVE:
        `moveAxis(POWERA_ID, RIGHT_X, -1, NON_STANDARD)` contains `'rightStickLeft'`;
        `moveAxis(POWERA_ID, RIGHT_X, 1, NON_STANDARD)` contains `'rightStickRight'`.
        Comment that this is NOT vacuous and NOT a re-fix of the horizontal no-op: those two actions
        DO reach `window.api.gamepadAction` from `gamepad.ts:296` and are therefore observable here;
        they become a no-op only later, in the PRELOAD switch's `default` arm, which is out of
        scope. This case asserts DISPATCH, not effect. GREEN PRE-FIX -- contract preservation.
    (F) LEFT STICK UNMOVED on the non-standard pad:
        `moveAxis(POWERA_ID, LEFT_X, -1, NON_STANDARD)` contains `'leftStickLeft'`;
        `moveAxis(POWERA_ID, LEFT_Y, -1, NON_STANDARD)` contains `'leftStickUp'`. The fix touches
        the axis-read block serving all four sticks; this pins that it moved only the one axis it
        was supposed to. GREEN PRE-FIX -- contract preservation.
    (G) IF AND ONLY IF M3 showed an axis resting OUTSIDE ±0.5: add "a resting deflected axis
        dispatches no right-stick action" -- build the non-standard pad, run ONE frame with every
        axis at its MEASURED resting value, assert no `rightStick*` action appears. This pins the
        spurious-dispatch row of the decision table. If M3 showed nothing resting outside ±0.5, do
        NOT write this case and say so in SUMMARY.md. Do not manufacture a case to look thorough.
  </behavior>
  <action>
    Write the harness change and the cases above. Then RED-PROVE by IN-PLACE EDIT and IN-PLACE
    REVERT of `src/frontend/helpers/gamepad_layouts/nintendo.ts`. **NEVER `git stash`** -- standing
    repo rule. Record the failure text VERBATIM for SUMMARY.md.

    PREDICTED RED, IN BOTH FORMS. Report both; do not report only the one that landed.

    1. **jest form** (`npx jest --selectProjects Frontend --testPathPattern nintendoLayout`):
       a BEHAVIOURAL, value-based failure. Cases (A) and (B) fail because the received action list
       is EMPTY of any `rightStick*` entry -- pre-fix, `checkNintendo` reads `axes[3]`, which these
       cases leave at 0, so nothing crosses ±0.5 and nothing dispatches. Case (C)'s first half fails
       in the OPPOSITE direction: pre-fix, `moveAxis(POWERA_ID, 3, -1, NON_STANDARD)` DOES dispatch
       `'rightStickUp'`, so the "contains neither" assertion fails WITH `'rightStickUp'` PRESENT.
       That contrast -- one assertion failing by absence, its partner failing by presence -- is what
       proves the defect is a WRONG INDEX rather than a dead code path. Record it as such.
       Cases (D), (E), (F) are GREEN PRE-FIX; do not claim them as RED evidence.
    2. **`pnpm codecheck` form: CLEAN -- ZERO TS errors expected.** This is a PREDICTION, not an
       omission. `moveAxis` is new but fully typed and local to the test file, and `checkNintendo`'s
       signature does not change in this task or in Task 3. Under the root tsconfig's
       `isolatedModules: true`, ts-jest transpiles per file WITHOUT type-checking, so arity and
       export errors surface at RUNTIME under jest and only as COMPILE errors under `pnpm codecheck`
       -- which means a clean codecheck alongside a red jest is the signature of a PURE BEHAVIOURAL
       red. It is the opposite of quick-260923-qe5's `controllerButtonLabels` red, which was a
       SIGNATURE-ABSENCE red (runtime `TypeError` under jest; `TS2554`/`TS2305` under codecheck).
       **If `pnpm codecheck` is NOT clean during this RED run, something unintended changed. Stop
       and find out what before proceeding.**

    Note in SUMMARY.md that Task 1's instrumentation is present in the working tree during the RED
    run and is INERT to it -- Piece A only calls `window.api?.logInfo?.()`, and Piece B only runs
    inside `doScroll`, which this Frontend suite never reaches -- so the red is attributable to
    `checkNintendo` alone.

    Make NO edit to `nintendo.ts`, `gamepad.ts` or anything under `src/preload/` in this task beyond
    the temporary in-place RED edit, which MUST be reverted in place before the task ends.
  </action>
  <verify>
    <automated>cd /c/Users/grays/Projects/GameLib && npx prettier --check src/frontend/helpers/__tests__/nintendoLayout.test.ts && git diff --quiet -- src/frontend/helpers/gamepad_layouts/nintendo.ts && npx jest --selectProjects Frontend --testPathPattern "nintendoLayout" 2>&1 | tail -60</automated>
    <!-- HONESTY ABOUT WHAT THIS GATES, so it is not mistaken for enforcement:
         - `npx prettier --check` on the exact written path IS &&-chained BEFORE the pipe, so it
           DOES gate. Explicit path, never `.`.
         - `git diff --quiet -- .../nintendo.ts` is the real machine gate of this task: it proves the
           in-place RED edit was REVERTED IN PLACE and that no source fix leaked into the test
           commit. It exits non-zero if the file differs from HEAD, breaking the chain. It is valid
           here precisely because Task 1 did NOT touch nintendo.ts and Task 3 has not run yet.
         - The jest run is piped through `tail -60`, which makes the exit code `tail`'s. This can
           therefore NEVER report failure -- correct for a step whose PASS CONDITION IS THAT JEST
           FAILS, but it is NOT a gate. The real gate is human: read the output and record the
           verbatim failure text in SUMMARY.md.
         - `pnpm codecheck` is run separately as part of the RED protocol in <action>, and its CLEAN
           result is itself a recorded prediction. It is not chained here because this block ends in
           a non-gating pipe. -->
  </verify>
  <done>
    `nintendoLayout.test.ts` carries `moveAxis`, the delegating `moveHat`, and cases (A)-(F) (plus
    (G) iff M3 warranted it); the nineteen pre-existing `it(...)` cases are character-identical;
    `npx prettier --check` passes on that exact path; `git diff` on `nintendo.ts` is EMPTY; the
    behavioural RED text for (A)/(B)/(C) is captured verbatim; and `pnpm codecheck` was run during
    the RED with its result recorded either way.
  </done>
</task>

<task type="auto">
  <name>Task 3: [CONDITIONAL -- runs ONLY IF M1 != 3] Branch checkNintendo's stick-axis indices on the mapping</name>
  <files>src/frontend/helpers/gamepad_layouts/nintendo.ts</files>
  <action>
    **RUN THIS TASK ONLY IF Task 1 measured M1 as an axis index OTHER THAN 3 (H-A confirmed), and
    only after Task 2's red is recorded.** If M1 == 3, SKIP -- `nintendo.ts` is then not the
    defective file and must not be touched at all, not even "for tidiness".

    Mirror the shape `nintendoFaceIndices` already established in this file. Do not invent a second
    shape.

    1. Add a module-private type `NintendoStickAxes = { leftX: number; leftY: number;
       rightX: number; rightY: number }` and two module-private constant rows:
       - `STANDARD_STICK_AXES = { leftX: 0, leftY: 1, rightX: 2, rightY: 3 }`. **This row reproduces
         exactly what ships today** -- a refactor of the existing constants at :191-194, not a change
         to them.
       - `RAW_HID_STICK_AXES = { leftX: 0, leftY: 1, rightX: 2, rightY: <M1's MEASURED index> }`.
    2. Add a module-private `nintendoStickAxes(mapping: GamepadMappingType): NintendoStickAxes`
       branching on `mapping === 'standard'`, IDENTICALLY to `nintendoFaceIndices` at :107-117 --
       including treating `'xr-standard'` as non-standard, for the reason already written in that
       function's comment. Do NOT use a `!== ''` test.
       **Keep it module-private, not exported.** `nintendoFaceIndices` is exported only because
       `ConsoleMode/controller.ts` is a second consumer; there is no second consumer of the axis
       table, Task 2's cases drive through `initGamepad` rather than importing it, and an export
       nothing consumes is API surface that invites the next reader to consume it wrongly.
    3. Replace the four hard-coded reads at :191-194 with reads through `nintendoStickAxes(mapping)`.
       Leave the eight `checkAction` calls at :200-207 otherwise BYTE-IDENTICAL -- same action names,
       same ±0.5 thresholds, same signs. **The sign convention is NOT the bug**; it is pinned by
       `gamepadAction.test.ts:268-282` and must not move. (If and only if Task 2's case (A) had to be
       inverted because M1's UP value measured POSITIVE, the inversion belongs in this TABLE as an
       explicit per-row sign field with its own comment and its own test -- never as a flipped
       comparison operator in the shared `checkAction` block.)
    4. Document the table with a comment stating, ITEM BY ITEM, which values are MEASURED and which
       are not:
       - `leftX: 0`, `leftY: 1` -- MEASURED 2026-09-25 (Task 1 step e), corroborated by focus moving
         in all four directions.
       - `rightX: 2` -- CONFIRMED LIVE 2026-09-25 by `[tauriGamepadInput] unhandled gamepad action
         "rightStickLeft"` reaching the `default` arm at `tauriGamepadInput.ts:394`; re-confirmed as
         M2.
       - `rightY: <M1>` -- MEASURED 2026-09-25 (Task 1 step b). Give the observed UP and DOWN values.
       And state plainly: **`checkGamecube` at :27-30 uses `axes[3]`/`axes[4]` for a DIFFERENT device
       in this same file, and `axes[3]` was live-FALSIFIED as this pad's right-stick X on 2026-09-25.
       Do not copy that row here.** Same rule the file already applies to `checkN64Clone1`'s hat
       table: derive from observation and say so.
    5. Extend the `checkNintendo` header comment (:161-174) by ONE sentence: the AXIS indices are now
       read from the mapping too, closing the gap quick-260923-qe5 deliberately left open when it
       branched only the buttons and the hat.

    SCOPE FENCE -- do not cross it. Do NOT touch: `checkGameCube`, `checkN64Clone1`,
    `nintendoFaceIndices`, `STANDARD_FACE_INDICES`, `RAW_HID_FACE_INDICES`, `nintendoHatDirection`,
    `NON_STANDARD_HAT_AXIS`, the d-pad/guide branch at :209-231, `isNintendoControllerId`,
    `NINTENDO_ID`, `XBOX_ID`, `gamepad.ts`'s dispatch chain (it ALREADY passes `controller.mapping`
    -- it needs NO edit in this task), anything under `src/preload/`, or `38-VERIFICATION.md` /
    `38-HUMAN-UAT.md`.
  </action>
  <verify>
    <automated>cd /c/Users/grays/Projects/GameLib && npx prettier --check src/frontend/helpers/gamepad_layouts/nintendo.ts && pnpm codecheck && pnpm lint && npx jest --selectProjects Frontend && npx jest --selectProjects Preload</automated>
    <!-- Fully &&-chained; explicit path, never `.`. Both jest projects run whole, not scoped: the
         Frontend project is what proves the nineteen pre-existing nintendoLayout cases plus the
         Console Mode label suite are untouched, and the Preload project proves this change did not
         reach the scroll path at all. `pnpm lint` must report both ceilings PASS. -->
  </verify>
  <done>
    Every Task 2 case is GREEN, INCLUDING the nineteen pre-existing `nintendoLayout` cases, which are
    still character-identical. `pnpm codecheck` (both tsc projects) is clean, `pnpm lint` reports
    both ceilings PASS, `npx prettier --check` passes on the exact path, and `git diff` shows
    `checkGameCube`, `checkN64Clone1` and everything under `src/preload/` untouched by this task.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: [CONDITIONAL -- runs ONLY IF M1 == 3, OR if Task 5 fails after Tasks 2+3 landed] Fix doScroll's scroll-target resolution</name>
  <files>src/preload/api/tauriGamepadInput.ts, src/preload/__tests__/gamepadAction.test.ts</files>
  <behavior>
    **RUN THIS TASK ONLY IF** Task 1 measured M1 == 3 with a `[GAMEPAD-SCROLL]` line showing a
    failed scroll (H-B confirmed, H-A falsified -- Tasks 2 and 3 did not run), **OR** Tasks 2+3
    landed and Task 5's live checks 1-3 STILL fail (both hypotheses true; the wrong index was
    masking a broken target). In the second case, return to Task 5 afterwards.
    If neither condition holds, SKIP this task entirely and record the skip in SUMMARY.md naming the
    measured value that caused it.

    **The exact change is determined by Task 1's `[GAMEPAD-SCROLL]` line, not by this plan.** H-B's
    mechanism is known (the brief derives it; do not re-derive it) but its specific trigger is a
    measurement, and fixing target resolution blind is exactly the failure mode this plan exists to
    avoid. Read the captured line and take the matching row:

    | what the `[GAMEPAD-SCROLL]` line reports | the fix |
    |---|---|
    | (i) `usedFallback=true` -- the ancestor walk found nothing scrollable and fell back to `document.scrollingElement ?? document.documentElement`, whose `scrollTop` did not move | The FALLBACK is the defect. When the walk finds nothing scrollable, do NOT silently scroll a non-scrollable fallback. Resolve the app's real scroll container, and if the final target is still not scrollable, no-op EXPLICITLY through the existing `warn()` helper rather than appearing to succeed. A silent no-op is what made this defect invisible for two phases. |
    | (ii) `usedFallback=false` -- the walk reached a plausible container but `isScrollable` rejected it, and the reported `overflowY` says why (e.g. a value the `visible`/`hidden` test does not cover, or `scrollHeight <= clientHeight` on the wrong node) | Narrow `isScrollable`'s rejection to the values that genuinely cannot scroll, keyed on the REPORTED computed value. Do not broaden it speculatively past what the line shows. |
    | (iii) the target IS scrollable and `scrollTopAfter != scrollTopBefore` | **H-B is also falsified.** The scroll is happening and the fault is below `doScroll` -- a CSS scroll container, the element already at its limit, or `scroll-behavior`. **STOP AND REPORT.** That is a re-plan, not a patch. |

    TEST, in `src/preload/__tests__/gamepadAction.test.ts`, written FIRST and RED-proven:
    - Build a `FakeElement` chain matching what the line reported -- for row (i), a centre element
      whose ancestors are all non-scrollable plus a scrollable container the current walk cannot
      reach; for row (ii), a container with the exact reported `overflowY`. Assert the NEW resolution
      (the scroll lands on the container) or, where no container is resolvable, that the explicit
      `warn` fires and nothing is silently scrolled.
    - **`gamepadAction.test.ts:268-282` (REQ-34.1-06) STAYS GREEN AND UNEDITED.** It mocks
      `elementFromPoint` to return a synthetic scrollable, so it proves the SIGN and says nothing
      about real-DOM target resolution. It is the regression bar for this task; if it needs editing
      to pass, the implementation is wrong -- STOP rather than adjusting it.
    - Keep `doScroll`'s SIGNATURE unchanged (`(delta: number): void`). A signature change would
      ripple into the `rightStickUp`/`rightStickDown` cases for no behavioural gain.

    Leave Task 1's `[GAMEPAD-SCROLL]` diagnostic IN PLACE through this task -- Task 6 removes it. It
    is how Task 5 confirms the new target resolves, and re-adding it later would be a second live
    sitting.
  </behavior>
  <action>
    Write the test first, RED-prove it by IN-PLACE EDIT and IN-PLACE REVERT of
    `src/preload/api/tauriGamepadInput.ts`. **NEVER `git stash`.** Then implement the matching row.

    PREDICTED RED, IN BOTH FORMS:
    1. **jest form** (`npx jest --selectProjects Preload --testPathPattern gamepadAction`): a
       BEHAVIOURAL, value-based failure -- `scrollBy`/`scrollTop` was not applied to the expected
       container (the assertion reports the fallback element, or an unchanged `scrollTop`), or the
       explicit `warn` was not emitted. NOT a `TypeError`: nothing new is imported and no arity
       changes.
    2. **`pnpm codecheck` form: CLEAN -- ZERO TS errors expected.** `doScroll`'s signature does not
       change, no export is added or removed. Under `isolatedModules: true` ts-jest does not
       type-check, so a clean codecheck alongside a red jest is again the signature of a PURE
       BEHAVIOURAL red. If codecheck is NOT clean, something unintended changed -- stop and find out.

    SCOPE FENCE: do NOT add `case 'rightStickLeft'` / `case 'rightStickRight'` to the switch. Their
    absence is BY DESIGN, it is out of scope, and it is the discriminator that established `axes[2]`
    -- removing it would destroy the instrument. Do not touch the `default` arm's `warn`, `doTab`,
    `doEsc`, `doBack`, `doClick`, or `moveFocusDirectionally`.
  </action>
  <verify>
    <automated>cd /c/Users/grays/Projects/GameLib && npx prettier --check src/preload/api/tauriGamepadInput.ts src/preload/__tests__/gamepadAction.test.ts && pnpm codecheck && pnpm lint && npx jest --selectProjects Preload && npx jest --selectProjects Frontend && ! grep -nE "case 'rightStick(Left|Right)'" src/preload/api/tauriGamepadInput.ts</automated>
    <!-- Fully &&-chained; explicit prettier paths at src/preload's printWidth of 120, never `.`.
         The trailing check is written `! grep -nE` DELIBERATELY: ZERO MATCHES IS THE SUCCESS PATH
         here (those cases must never be added), and a bare `grep` exits 1 on no-match, which would
         break the && chain precisely when the check SUCCEEDS. `!` inverts that while still PRINTING
         any offending line before failing.
         GREP-GATE HYGIENE: the pattern matches that literal inside a COMMENT too, so do not write
         an explanatory comment quoting it into this source file -- a correct file would then fail
         its own gate. Describe the omission in prose instead. -->
  </verify>
  <done>
    The new target-resolution case is GREEN; `gamepadAction.test.ts:268-282` is GREEN and
    byte-identical; `doScroll`'s signature is unchanged; no rightStickLeft/rightStickRight case was
    added; `pnpm codecheck` and `pnpm lint` (both ceilings PASS) are clean; `npx prettier --check`
    passes on both exact paths; and BOTH the `Preload` and `Frontend` jest projects are green. The
    behavioural RED text is captured verbatim, along with the decision-table row that selected this
    fix.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [BLOCKING OPERATOR CHECKPOINT 2 of 2] Confirm on the operator's real pad that the right stick scrolls in the direction pushed</name>
  <files></files>
  <what-built>
    Whichever fix the measurement selected -- Task 3's mapping-branched axis table (H-A) or Task 4's
    scroll-target resolution (H-B) -- live in the running app via vite HMR. No new code.

    This checkpoint exists because everything proven so far was proven against SYNTHETIC `Gamepad`
    objects and `FakeElement` doubles built from the SAME report the implementation was built from.
    A suite and an implementation derived from one assumption agree with each other whether or not
    the assumption is true. Task 1 measured; this task confirms the measurement produced a working
    feature.
  </what-built>
  <action>
    Write NO code in this task. Hand the <how-to-verify> sitting to the operator and STOP. Apply
    the branch rules in <how-to-verify> to whatever comes back -- do not patch blind. Do NOT remove
    the instrumentation here; Task 6 owns that, and only after checks 1-8 pass.
  </action>
  <how-to-verify>
    Renderer-only change again: NO `pnpm build:sidecar`, and do NOT start a second dev server. Full
    page reload (`App.tsx` defeats Fast Refresh), then press a pad button to re-expose the gamepad.

    On the GAMES LIBRARY -- a view with more content than fits, so a scroll is possible at all:

      1. Push the right stick UP. The library content scrolls toward the TOP.
      2. Push the right stick DOWN. The library content scrolls toward the BOTTOM.
      3. The direction matches the push, both ways, repeatably. (This is `38-C02`'s substance. The
         operator SCORES `38-C02` in a separate sitting; this plan does not.)
      4. Task 1's `[GAMEPAD-SCROLL]` diagnostic is STILL in the tree at this point. Read it in the
         DevTools console during checks 1-2: it must now report a target that is scrollable, with
         `scrollTopBefore != scrollTopAfter`. If checks 1-3 pass by eye but that line still reports
         `usedFallback=true`, SAY SO -- the scroll is coming from somewhere unexpected, and that is
         a finding rather than a detail.

    REGRESSION CHECKS -- the fix touched code shared with everything quick-260923-qe5 repaired:

      5. The LEFT stick still moves focus in all four directions.
      6. The D-PAD still moves focus in all four directions (the hat branch is adjacent code).
      7. The cap printed A still confirms, B still goes back, Y still does its hint-bar action, X
         still opens the context menu, and every glyph in the ControllerHints bar still agrees with
         what its button does.
      8. Right stick LEFT/RIGHT still does nothing visible AND still prints
         `[tauriGamepadInput] unhandled gamepad action "rightStickLeft"` / `"rightStickRight"`. This
         is the BY-DESIGN no-op and it must NOT have been "fixed": that warning is also the
         instrument that established `axes[2]`, so its disappearance would mean the scope fence was
         crossed.

    ASK the orchestrator, VERBATIM: "The fix is live. Please run checks 1-8 above with the operator
    and report each as pass or fail, plus the `[GAMEPAD-SCROLL]` line from check 4 and anything that
    behaves unexpectedly."

    STOP THERE.

    IF CHECKS 1-3 FAIL, report the specific check number rather than patching blind, and apply
    `<conditionality>`'s coexistence rule:
      - If Tasks 2+3 ran (H-A path) and 1-3 still fail: **H-A AND H-B ARE BOTH TRUE.** The wrong
        axis index was MASKING a broken scroll target. Enter Task 4, then return to this checkpoint.
        This is not a descope -- record it in SUMMARY.md as a second finding.
      - If Task 4 ran (H-B path) and 1-3 still fail: the freshly captured `[GAMEPAD-SCROLL]` line is
        the next evidence. If it now falls into decision-table row 3 (target scrollable, scrollTop
        moving), STOP AND REPORT for a re-plan.
    IF CHECKS 5-8 FAIL, the scope fence was crossed. Report which check and which file.

    Do NOT delete the instrumentation until checks 1-8 pass. Task 6 owns that, and only after this.
  </how-to-verify>
  <verify>
    <automated>cd /c/Users/grays/Projects/GameLib && npx jest --selectProjects Frontend && npx jest --selectProjects Preload</automated>
    <human-check>Checks 1-8 above, on the operator's PowerA Advantage Wired Controller for Nintendo Switch 2, all pass.</human-check>
    <!-- No prettier call: this task writes NO file. Both jest projects run whole, because the point
         of this checkpoint is that nothing regressed anywhere, not that one suite still passes. -->
  </verify>
  <resume-signal>Report checks 1-8 as pass/fail, and paste the [GAMEPAD-SCROLL] line from check 4.</resume-signal>
  <done>
    All eight live checks pass on the operator's pad, and the results are recorded verbatim for
    SUMMARY.md alongside the pre-fix symptom ("right stick UP/DOWN: no scroll whatsoever", measured
    2026-09-25).
  </done>
</task>

<task type="auto">
  <name>Task 6: Remove both diagnostics with a residue proof, file the L3/R3 todo, commit</name>
  <files>src/frontend/helpers/gamepad.ts, src/preload/api/tauriGamepadInput.ts, .planning/todos/pending/2026-09-25-no-layout-dispatches-l3-r3-stick-clicks.md</files>
  <action>
    1. DELETE Piece A from `src/frontend/helpers/gamepad.ts`: the `AXIS_DUMP` constant, the one-shot
       and per-frame `[GAMEPAD-AXIS]` emissions, the previous-frame state array, and anything that
       existed only to feed them.
       **KEEP, UNTOUCHED, the permanent `[GAMEPAD]` connect line at :634 and its
       why-it-is-permanent comment.** It is a different diagnostic with a different justification
       and it is not in scope here.
    2. DELETE Piece B from `src/preload/api/tauriGamepadInput.ts`: the `SCROLL_DIAG` constant, the
       `[GAMEPAD-SCROLL]` emission and its inner try/catch. If Task 4 ran, its FIX stays; only the
       diagnostic goes. Do NOT delete any explicit `warn()` no-op path Task 4 introduced -- that is
       shipped behaviour, not instrumentation, and it is distinguishable because it carries no
       `[GAMEPAD-SCROLL]` prefix.
    3. **RESIDUE TRAP -- read this before writing any comment.** The residue check in `<verify>` is
       `! grep -nE "AXIS_DUMP|SCROLL_DIAG|GAMEPAD-AXIS|GAMEPAD-SCROLL"` over both files, and `grep`
       counts COMMENTS. A comment reading "the GAMEPAD-AXIS dump was removed here" would make a
       correctly-cleaned file FAIL its own gate. Removal means removal INCLUDING every mention:
       describe the history in SUMMARY.md, never in a source comment. (The predecessor's equivalent
       check grepped clean at `0978dba81`, so this is achievable, not aspirational.)
    4. FILE THE TODO at
       `.planning/todos/pending/2026-09-25-no-layout-dispatches-l3-r3-stick-clicks.md`.
       This is a DOCUMENTATION task. **Do NOT implement stick clicks.**

       FRONTMATTER -- `severity: medium`, `platform: any`, `ready: code`, as BARE LOWERCASE values
       (never quoted, never capitalised), in that order, placed immediately after the `files:` list.
       They must sit at ZERO INDENTATION as their own keys -- NOT as `- severity: medium` list
       items continuing the `files:` sequence, which is the one way to get this wrong and still
       have valid YAML. `.planning/todos/todo-frontmatter-gate.py` parses only the text between the
       first `---` and the next `---`, and matches values bare and case-sensitively. `ready: code`
       because nothing about this finding needs hardware: the evidence is entirely in source.

       BODY -- the four source facts, each VERIFIED at HEAD ddd2b8ed0 on 2026-09-25:
       - `standard.ts:24-25` and `genius.ts:27-28` have `L3 = buttons[10]` / `R3 = buttons[11]`
         COMMENTED OUT.
       - `nintendo.ts:25-26`'s LIVE `left = buttons[10], right = buttons[11]` is inside
         `checkGameCube`, where those indices are the GameCube D-PAD, not stick clicks.
       - `ps.ts` has no reference to either index.
       - Therefore NO layout in this repo dispatches `buttons[10]`/`buttons[11]` at all.
       Plus the two derivations that make it consequential:
       - `leftClick` is NEVER a button binding anywhere. It is DERIVED from `mainAction` at
         `gamepad.ts:188` when `shouldSimulateClick()` is true.
       - `rightClick` is always a FACE button (X on Nintendo via `nintendo.ts:199`, Square on PS,
         `contextMenuButton` on standard).
       CONSEQUENCE, stated plainly: `38-C04`'s expected clause "Left/right stick clicks (the
       click-equivalents) activate the element currently under focus or cursor"
       (`38-VERIFICATION.md:110`) describes a feature THAT DOES NOT EXIST, so it can never be
       discharged by a human pressing anything.
       CROSS-REFERENCE the existing sibling todo
       `.planning/todos/pending/2026-09-24-nonstandard-nintendo-pad-stick-clicks-and-guide-unmeasured.md`
       and state the DIFFERENCE precisely, so the two are not mistaken for duplicates and closed as
       one: that one says the stick-click INDICES were never MEASURED on the operator's pad
       (`ready: human` -- it needs the operator and the hardware). This one says NO CODE DISPATCHES
       THOSE INDICES ON ANY PAD, established entirely in source (`ready: code`). **Measuring the
       indices would therefore NOT make `38-C04` dischargeable** -- that is the point, and it is why
       filing this separately matters.
       **DO NOT edit `38-VERIFICATION.md` or `38-HUMAN-UAT.md`.** The operator re-scores Phase 38 as
       a sitting, exactly as was done for `38-S08`.
    5. WRITE SUMMARY.md per `<output>` below.
    6. COMMIT ATOMICALLY, in this order, NEVER with `--no-verify` and never with
       `core.hooksPath=/dev/null` -- not for source, not for planning files, not in any subagent:
       a. `test(quick-260925-9de): ...` -- whichever RED tests were written (Task 2 or Task 4);
       b. `fix(quick-260925-9de): ...` -- the selected fix, plus this task's removal of both
          diagnostics;
       c. `docs(quick-260925-9de): ...` -- the L3/R3 todo and SUMMARY.md.
       End each commit message with:
       the `Co-Authored-By:` trailer EXACTLY as YOUR OWN session's attribution instruction gives it.
       Do NOT copy a model name from this plan: `main` already carries two different trailers
       (`Claude Opus 5 (1M context)` and `Claude Opus 5.5 (1M context)`), so a hardcoded one here
       would conflict with whatever the executing session is actually told to use.
    7. **EXPECT THE PUSH TO BE BLOCKED, AND DO NOT WORK AROUND IT.** A concurrent session is working
       on `main` from another machine with UNCOMMITTED edits to `meta/machineFillGamelib.ts`,
       `meta/__tests__/machineFillGamelib.test.ts` and `meta/__tests__/gamelibCatalogParity.test.ts`
       plus untracked `meta/i18nTranslatorNotes.json`. The pre-push hook type-checks and lints the
       WORKING TREE, not just commits, and their edits currently push `pnpm lint` over its
       638-warning ceiling. COMMIT normally; if the push is blocked, REPORT IT AND STOP. Do NOT
       touch, commit, stash or revert their files. Do NOT raise the 638 ceiling -- CLAUDE.md forbids
       widening a gate to admit what failed. Also do not be confused by the running dev server: it
       carries that session's uncommitted frontend edits via HMR (`NavShell` filter components,
       `Header/index.tsx`, `Library/index.tsx`, `Humble/Keys/*`, `Winetricks/*`), so the rendering
       app is NOT clean `main`.
  </action>
  <verify>
    <automated>cd /c/Users/grays/Projects/GameLib && npx prettier --check src/frontend/helpers/gamepad.ts src/preload/api/tauriGamepadInput.ts .planning/todos/pending/2026-09-25-no-layout-dispatches-l3-r3-stick-clicks.md && ! grep -nE "AXIS_DUMP|SCROLL_DIAG|GAMEPAD-AXIS|GAMEPAD-SCROLL" src/frontend/helpers/gamepad.ts src/preload/api/tauriGamepadInput.ts && grep -nE "\[GAMEPAD\] id=" src/frontend/helpers/gamepad.ts && pnpm codecheck && pnpm lint && npx jest --selectProjects Frontend && npx jest --selectProjects Preload && python meta/runPlanningGates.py && git diff --quiet HEAD -- .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/</automated>
    <!-- CHAINING IS LOAD-BEARING: every stage is `&&`, never `;`. A `;` would run the prettier
         check, PRINT its diff, and then DISCARD its exit code, so an unformatted gamepad.ts would
         pass. That is exactly the defect CLAUDE.md's formatter-gate section exists to prevent.
         THE RESIDUE CHECK IS `! grep -nE` ON PURPOSE: zero matches IS the success path, so a bare
         `grep` (exit 1 on no match) would break the chain precisely when it succeeds. `!` inverts
         that while still PRINTING any residue it finds before failing. It spans BOTH instrumented
         files in one call, so a clean frontend and a dirty preload cannot pass together.
         THE POSITIVE GREP THAT FOLLOWS IS ITS COMPLEMENT: it proves the PERMANENT connect line
         SURVIVED. Without it, deleting that line too would satisfy the residue check and look like
         success.
         `python meta/runPlanningGates.py`, NEVER `pnpm planning-gates`: that script is
         `python3 meta/runPlanningGates.py`, and `python3` on this host is the Microsoft Store alias
         stub. Do NOT "fix" package.json -- that would break CI and the macOS host.
         HONESTY: `.planning` is in `.prettierignore`, so the prettier call on the todo path is
         VACUOUS -- it reports nothing and exits 0. It is there because CLAUDE.md's rule is
         unconditional, not because it proves anything. The gate that actually binds the planning
         half is `runPlanningGates.py`, which includes `todo-frontmatter-gate.py`.
         The closing `git diff --quiet HEAD` over the Phase 38 directory is the machine proof that
         `38-VERIFICATION.md` and `38-HUMAN-UAT.md` were not edited. -->
  </verify>
  <done>
    The residue grep finds NOTHING in either instrumented file; the permanent `[GAMEPAD]` connect
    line and its comment survive; the L3/R3 todo exists in `pending/` with `severity: medium`,
    `platform: any`, `ready: code` bare and in order; `python meta/runPlanningGates.py` passes;
    `pnpm codecheck`, `pnpm lint` (both ceilings PASS) and both jest projects are green; the Phase
    38 directory is byte-identical to HEAD; three atomic commits exist with no hook bypass and the
    required `Co-Authored-By` trailer; and any push block is REPORTED rather than worked around.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USB HID device -> renderer | `navigator.getGamepads()` returns attacker-influenceable array lengths, axis values and a `mapping` string; the layout code indexes straight into them |
| renderer -> sidecar IPC (`logInfo`) | Task 1's axis dump sends per-frame device-supplied numbers across the process boundary into a log file the operator later reads and pastes |
| renderer DOM -> `doScroll` | `elementFromPoint` returns whatever the page renders; `findScrollableAncestor` walks it and calls `scrollBy` on the result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9de-01 | Denial of Service | `checkNintendo` reading an axis index past the end of a short `axes` array | mitigate | `axes[n]` on a short array yields `undefined`, and both `undefined < -0.5` and `undefined > 0.5` are `false`, so no action dispatches and nothing throws. Task 2 case (C) exercises a 10-slot array and case (D) a 4-slot one, so both lengths are covered. `updateStatus`'s try/catch at `gamepad.ts:586` would swallow a throw while silently killing ALL input, so not throwing is what matters. |
| T-9de-02 | Denial of Service | Task 1 Piece B's diagnostic throwing inside `doScroll`, whose caller wraps everything in try/catch | mitigate | The diagnostic carries its OWN inner try/catch so it can never suppress the scroll it measures. Task 1's `<verify>` runs the `Preload` project, whose doubles implement neither `getComputedStyle` nor `document.scrollingElement` -- that run is the test that proves the guard. |
| T-9de-03 | Information disclosure | Task 1 Piece A writing per-frame axis values into `gamelib.log` | mitigate | Deleted in Task 6 with a `! grep -nE` residue proof across both files. While present it records analogue stick timing, which is one more reason it does not ship. It is gated behind a module constant and cannot be enabled by device input. |
| T-9de-04 | Information disclosure | Task 1 Piece B writing element tags, ids and class names to the DevTools console | mitigate | Console-only (never written to `gamelib.log`), removed in Task 6 under the same residue grep, and it emits structural DOM identifiers rather than user data. |
| T-9de-05 | Tampering | a malicious HID device reporting a Nintendo-looking id with a hostile `mapping` string | accept | The worst outcome is wrong navigation bindings on that device -- the class of defect being fixed, not a privilege gain. `mapping` is only ever compared against the literal `'standard'`; it is never used as an index, a key or a path. Carried forward unchanged from T-qe5-04. |
| T-9de-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs NO packages. Every symbol used is already in the repo or in lib.dom (`GamepadMappingType`, `GamepadButton`, `Element`). No legitimacy gate is required, and no package may be added under this row. |
</threat_model>

<verification>
1. `npx jest --selectProjects Frontend` -- green, INCLUDING the nineteen pre-existing
   `nintendoLayout` cases, character-identical.
2. `npx jest --selectProjects Preload` -- green, INCLUDING `gamepadAction.test.ts:268-282`
   (REQ-34.1-06, the sign convention), unedited.
3. `pnpm codecheck` -- both tsc projects clean.
4. `pnpm lint` -- both ceilings PASS. If it fails on the concurrent session's files, report and
   stop; do not raise the 638 ceiling.
5. `npx prettier --check` over every exact path written, per task, NEVER `.` -- noting that
   `src/preload/` formats at printWidth 120 and the repo root at 80.
   PLACEMENT NOTE: the prettier call is the FIRST link of each `&&` chain, not the last. That is
   deliberate and is strictly stronger than trailing placement -- a chain ending in prettier would
   never reach it whenever an earlier jest or codecheck stage failed, which is exactly the run where
   an unformatted file is most likely to be sitting in the tree. Every file-writing task (1, 2, 3,
   4, 6) carries one; Task 5 writes no file and carries none.
6. The residue grep returns success (zero matches) over both instrumented files, AND the positive
   grep for the permanent `[GAMEPAD] id=` line still finds it.
7. `python meta/runPlanningGates.py` -- NOT `pnpm planning-gates`.
8. `git diff --quiet HEAD -- .planning/phases/38-deferred-hardware-and-environment-uat-gates-windows-linux-ma/`
   -- the Phase 38 ledger untouched.
9. Live checks 1-8 from Task 5 on the operator's pad.
</verification>

<out_of_scope>
Named so they cannot be quietly picked up mid-execution:

- **`rightStickLeft` / `rightStickRight` remain a no-op.** They have never had a `case` in
  `tauriGamepadInput.ts`'s switch (nor did Electron's own switch, per the comment at :394). This is
  BY DESIGN, it is explicitly excluded by the brief, and their `unhandled gamepad action` warning is
  the instrument that established `axes[2]` as this pad's right-stick X. "Fixing" it would destroy
  the evidence.
- **`38-VERIFICATION.md` and `38-HUMAN-UAT.md` are NOT edited.** The operator re-scores Phase 38 as
  a sitting, exactly as was done for `38-S08`. `38-C02` becomes SCOREABLE; it is not scored here.
- **`checkN64Clone1` is NOT touched**, including its comment/code contradiction -- a todo is already
  filed (`2026-09-24-checkn64clone1-dpad-code-contradicts-its-own-comment.md`).
- **`checkGameCube` is NOT touched.** Its `axes[3]`/`axes[4]` right-stick row is for a different
  device, it is cited only as the trap not to inherit, and no GameCube hardware is available.
- **Stick clicks (`buttons[10]`/`buttons[11]`) are NOT implemented.** A todo is filed; that is the
  whole deliverable for that finding.
- **Nothing under `meta/` is edited**, and `package.json` is NOT edited -- in particular its
  `planning-gates` script stays `python3 ...`, because changing it would break CI and the macOS host.
- **`nintendoFaceIndices`, `nintendoHatDirection`, `NON_STANDARD_HAT_AXIS` and the d-pad/guide
  branch are NOT touched.** quick-260923-qe5 measured and fixed those; this task owns the axes only.
- **The sign convention is NOT changed.** `gamepadAction.test.ts:268-282` pins it and stays green.
- **No npm package is installed.** Both jest projects stay in the existing node-environment
  harnesses; jsdom is deliberately absent.
- **The concurrent session's files are NOT touched, committed, stashed or reverted**, and the
  638-warning lint ceiling is NOT raised.
</out_of_scope>

<honesty_about_reach>
**Runs anywhere (synthetic pads and FakeElement doubles), proves DISPATCH AND RESOLUTION LOGIC
only:** every case in `nintendoLayout.test.ts` and `gamepadAction.test.ts`. They construct fake
`Gamepad` objects and fake DOM nodes from the report Task 1 measured. They prove the code does the
right thing GIVEN that report. They prove NOTHING about the report itself -- a suite and an
implementation built from the same assumption agree with each other whether or not the assumption is
true. That is exactly why Task 1 measures and Task 5 re-confirms, and why a synthetic pass must
never be reported as evidence about real HID or real-DOM behaviour.

**Needs the operator's specific pad and the running app:** M1-M4 and Task 5's checks 1-8. The axis
index, its values, the resting baseline and the live scroll-target behaviour are DEVICE and DOM
facts; no synthetic test can produce them and no other machine in this project has this pad.

**Proven by neither, and must NOT be claimed:**
- That every non-standard Nintendo-style pad reports its right stick on the same axis index. Only
  ONE device of that class has ever been observed. A different third-party pad with a different raw
  axis order would still be wrong, and this change cannot detect that.
- That non-standard PlayStation, GameCube, N64-clone or Genius pads are correct. Untouched, never
  measured.
- Anything about the horizontal right-stick direction DOING anything. The fix makes it dispatch (it
  already did); the no-op downstream is deliberate and unchanged.
- Anything about Phase 38 SCORES. This makes `38-C02` scoreable and likely helps `38-C05`; the
  operator scores both in a separate live sitting.
- On the H-A path: anything about `doScroll`'s target resolution being correct in general. Task 5
  exercises ONE view (the games library). Other scroll containers are untested.
</honesty_about_reach>

<success_criteria>
Checked for the internal-contradiction class that forced a correction to the predecessor's plan (it
claimed four cases pass "UNEDITED" while another task mandated editing them). Here no task mandates
editing any pre-existing case, because no signature changes -- so "unedited" is claimed only where
it is literally achievable, and the ONE in-place change to existing test-file code (`moveHat`
delegating to `moveAxis`) is named explicitly as harness plumbing rather than hidden behind an
"unedited" claim.

- The right stick's real vertical axis index was MEASURED (M1) before any constant was written and
  before either hypothesis was selected, and the decision-table row that measurement selected is
  named in writing in SUMMARY.md.
- On the operator's pad, right stick UP scrolls toward the top and DOWN toward the bottom --
  confirmed LIVE at Task 5, not inferred.
- EXACTLY ONE of these is true, and SUMMARY.md says which and why:
  (a) M1 != 3 -> Tasks 2+3 ran, `checkNintendo`'s stick axes resolve through a mapping-branched
      table whose STANDARD row is 0/1/2/3 unchanged, and Task 4 did not run; or
  (b) M1 == 3 -> Task 4 ran, `nintendo.ts` was not modified at all, and Tasks 2+3 did not run; or
  (c) both ran, because Task 5 failed after (a) and the coexistence rule was applied.
- The nineteen pre-existing `nintendoLayout.test.ts` `it(...)` cases are character-identical and
  green. `moveHat`'s signature and behaviour are unchanged despite its body delegating to
  `moveAxis`.
- `gamepadAction.test.ts:268-282` is green and byte-identical.
- Every new test asserting NEW behaviour was RED-proven against pre-fix source by in-place edit and
  in-place revert (NEVER `git stash`), with the failure text recorded verbatim in SUMMARY.md, and
  the predicted `pnpm codecheck`-clean result reported alongside the jest red so the red is
  correctly labelled BEHAVIOURAL rather than signature-absence.
- Cases green PRE-FIX (the contract-preservation ones) are labelled as such and NOT claimed as RED
  evidence.
- Both Task 1 diagnostics are gone, with a `! grep -nE` residue proof spanning both files AND a
  positive grep proving the permanent `[GAMEPAD]` connect line survived.
- The L3/R3 todo exists in `pending/` with `severity: medium`, `platform: any`, `ready: code` bare,
  lowercase, in order, at zero indentation; it cross-references the sibling `ready: human` todo and
  states why measuring indices would not make `38-C04` dischargeable; and
  `python meta/runPlanningGates.py` passes.
- Nothing in `.planning/phases/38-*`, `checkN64Clone1`, `checkGameCube`, the
  `rightStickLeft`/`rightStickRight` no-op, `meta/` or `package.json` changed.
</success_criteria>

<output>
Create
`.planning/quick/260925-9de-fix-checknintendo-axis-reads-right-stick-dea/260925-9de-SUMMARY.md`
when done, including:

- M1-M4 VERBATIM, with the resting-axis baseline (M3) in full, and an explicit statement of which
  `<conditionality>` decision-table row applied and therefore which tasks did NOT run.
- A plain statement of whether H-A or H-B (or both) was true, and -- where this plan's own
  expectations differed from the measurement -- what the plan got wrong, in the style of the
  predecessor's neutral-value correction. Do not smooth it over.
- The RED text verbatim, labelled as BEHAVIOURAL, together with the `pnpm codecheck` result that
  accompanied it, so the KIND of evidence is unambiguous.
- Which cases were GREEN PRE-FIX and why that is not RED evidence.
- Task 5's live check results 1-8 set against the pre-fix symptom.
- The `<honesty_about_reach>` split above.
- An explicit statement that `38-C02` (and possibly `38-C05`) is now SCOREABLE, and that this plan
  did not score it.
- Whether the push was blocked by the concurrent session's lint-ceiling breach, and that no
  workaround was attempted.
</output>
