---
phase: quick-260901-8rm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/tauri.conf.json
  - src-tauri/tauri.macos.conf.json
  - src-tauri/tauri.windows.conf.json
  - src-tauri/tauri.linux.conf.json
  - src/backend/__tests__/packagingConfig.test.ts
  - .planning/quick/260901-8rm-shrink-tauri-macos-bundle-narrow-bundle-/260901-8rm-MEASUREMENTS.md
  - .planning/todos/pending/2026-08-28-tauri-macos-bundle-is-786mb-frontenddist-embeds-build-bin-in.md
autonomous: true
requirements: [TODO-2026-08-28-BUNDLE-SIZE-FIX-1]

scoped_out:
  - id: "fix-2-frontendDist"
    what: "Repointing `frontendDist` at a renderer-only directory (~212MB of the 786MB)."
    why: >
      Planner-measured, not assumed. The todo's framing ('point frontendDist at a
      renderer-only build/renderer') is FALSE as written: two live consumers resolve assets
      RELATIVE TO frontendDist and would silently break.
      (a) src/frontend/index.tsx:114 — i18next HttpApi `loadPath: 'locales/{{lng}}/{{ns}}.json'`,
          a relative URL served over `tauri://localhost/` from frontendDist. Move frontendDist
          and the packaged app loses EVERY translation.
      (b) src/preload/api/tauriChildWindows.ts:177 — the About window is a WebviewWindow whose
          url is `about.html?v=...`, also relative to frontendDist. `about.html` is a
          `public/` file that arrives in `build/` only via vite's implicit publicDir copy.
      Neither is named in the todo. Both fail ONLY in a packaged artifact (a `tauri:dev` run
      fetches from devUrl and cannot see either) — the R-34.5-G1-PKG defect class this repo
      has already paid for twice.
      Beyond those two, fix (2) also has to move `preserveRunnerSymlinksPlugin`'s trigger,
      re-point the release workflow's `test -f build/index.html` guard, and rewrite five
      pinned assertions across meta/__tests__/viteRendererConfig.test.ts:87-88,
      src/backend/__tests__/releaseWorkflow.test.ts:437-530 and
      src/backend/__tests__/packagingConfig.test.ts:257-277. It also has at least two viable
      and materially different designs (see Task 3) whose choice needs a measurement, not a
      guess. That is a phase, not a quick task. Task 3 hands it a written, executable brief
      instead of shipping a half-fix.

must_haves:
  truths:
    - "A packaged macOS artifact ships build/bin/arm64/darwin and build/bin/x64/darwin, and does NOT ship arm64/win32, x64/linux or arm64/linux"
    - "That same artifact still ships build/bin/x64/win32/EpicGamesLauncher.exe and GalaxyCommunication.exe — the two Wine-prefix exes legendary/games.ts:936 and launcher.ts:927 copy on macOS AND Linux"
    - "That same artifact still ships build/locales, build/changelog.json, build/webviewPreload.js and build/icon.png — proving Tauri's platform-config merge is a DEEP merge and did not replace the base bundle.resources map"
    - "The shipped Contents/Resources/build/bin tree is at least 100MB smaller than the repo's own build/bin tree, measured in the same run"
    - "The shell binary's __const section is UNCHANGED and `strings` still finds bin/x64/win32/gogdl.exe — fix (1) demonstrably did NOT touch the frontendDist embedding, so the two fixes stay independently attributable"
    - "The base tauri.conf.json contains no wholesale ../build/bin/ resource key, so no platform can silently inherit all six trees"
  artifacts:
    - path: "src-tauri/tauri.macos.conf.json"
      provides: "macOS-only bin resource map: arm64/darwin + x64/darwin dirs, the two win32 Wine exes as FILE entries, legendary.LICENSE"
      contains: "EpicGamesLauncher.exe"
    - path: "src-tauri/tauri.windows.conf.json"
      provides: "Windows-only bin resource map: x64/win32 + arm64/win32 dirs, legendary.LICENSE"
      contains: "arm64/win32"
    - path: "src-tauri/tauri.linux.conf.json"
      provides: "Linux-only bin resource map: x64/linux + arm64/linux dirs, the two win32 Wine exes, legendary.LICENSE"
      contains: "GalaxyCommunication.exe"
    - path: "src/backend/__tests__/packagingConfig.test.ts"
      provides: "Merged-map invariants over base+overlay for all three platforms, plus a negative guard that base carries no bin key"
      contains: "mergedResourceMap"
    - path: ".planning/quick/260901-8rm-shrink-tauri-macos-bundle-narrow-bundle-/260901-8rm-MEASUREMENTS.md"
      provides: "The measured artifact census and the recorded fix-(2) baseline (__const bytes, codegen-assets size, gogdl strings count)"
      contains: "__const"
  key_links:
    - from: "src-tauri/tauri.macos.conf.json"
      to: "Contents/Resources/build/bin/x64/win32/EpicGamesLauncher.exe"
      via: "bundle.resources file entry -> tauri resource copy -> paths.ts:86 fakeEpicExePath"
      pattern: "x64/win32/EpicGamesLauncher.exe"
    - from: "src-tauri/tauri.conf.json (base)"
      to: "Contents/Resources/build/locales"
      via: "deep merge of the platform overlay onto the base bundle.resources map"
      pattern: "build/locales"
---

<objective>
Close fix (1) of the 2026-08-28 bundle-size todo: stop shipping all six `bin/{arch}/{platform}`
runner trees inside every platform's bundle. Narrow `bundle.resources` to per-platform overlays
so a macOS artifact carries the darwin trees plus the two Windows exes it genuinely executes
under Wine, and drops the ~162MB of `arm64/win32`, `x64/linux` and `arm64/linux` it can never run.

Fix (2) (`frontendDist`) is deliberately NOT implemented here — see `scoped_out` in the
frontmatter and Task 3, which converts it from a one-line todo into an executable brief.

Purpose: `Resources/build/bin` is 397MB of the 786MB installed footprint and roughly 60% of it
is unreachable on any given host.

Output: three new per-platform Tauri config overlays, a base config with the wholesale `bin`
entry removed, bidirectional config tests over the merged maps, a measured artifact census, and
a rewritten fix-(2) todo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-28-tauri-macos-bundle-is-786mb-frontenddist-embeds-build-bin-in.md
@src-tauri/tauri.conf.json
@src/backend/__tests__/packagingConfig.test.ts

<interfaces>
<!-- Contracts the executor needs. Measured at plan time. Do NOT re-derive. -->

Current `src-tauri/tauri.conf.json` `bundle.resources` (MAP form — the map form is load-bearing,
see packagingConfig.test.ts:120-145; the array form derives `_up_` targets and was the
R-34.5-G1-PKG half (a) defect):

  "../build/bin/":             "build/bin"          <- THE ENTRY THIS PLAN REMOVES
  "../build/locales/":         "build/locales"
  "../build/changelog.json":   "build/changelog.json"
  "../build/webviewPreload.js":"build/webviewPreload.js"
  "../build/icon.png":         "build/icon.png"

Consumers of `publicDir/bin/...` (publicDir = `<Contents/Resources>/build` when packaged,
`<repo>/public` in dev — src/backend/constants/paths.ts:80-83):

  paths.ts:86   fakeEpicExePath          -> bin/x64/win32/EpicGamesLauncher.exe   (150,248 B)
  paths.ts:90   galaxyCommunicationExePath -> bin/x64/win32/GalaxyCommunication.exe (61,204 B)
  paths.ts:104  builtBridgeShimPath      -> bin/{process.arch}/darwin/steam_api.dll
  paths.ts:118  steamBridgeHelperPath    -> bin/{process.arch}/darwin/steam-bridge-helper
  utils.ts:554-565 archSpecificBinary    -> bin/{process.arch}/{process.platform}/... with an
                                            existence-checked x64 FALLBACK (box64 case)
  vulkan/index.ts:6                      -> bin/{process.arch}/{process.platform}/vulkan-helper

The two win32 exes are NOT Windows-only. `legendary/games.ts:919-937` has an explicit
`if (isWindows) ... else { /* on linux and mac, we copy the fake exe */ }` branch, and
`launcher.ts:927` copies GalaxyCommunication.exe into the Wine prefix with no platform guard.
Both macOS AND Linux need `bin/x64/win32/{EpicGamesLauncher,GalaxyCommunication}.exe`.

Measured source tree (`du -sh public/bin/*/*`, plan time):
  arm64/darwin  93M    x64/darwin  44M
  arm64/linux   35M    x64/linux   37M
  arm64/win32   38M    x64/win32   52M
  public/bin total 300M; build/bin total 351M (symlink-dereferenced copy + steam-bridge output)
  public/bin/legendary.LICENSE  36K  (bundled licence text — must keep shipping)

`public/bin/arm64/darwin/` holds `comet`, `gogdl/`, `legendary/`, `nile/` in the checkout;
`pnpm build-steam-bridge` ADDS `steam_api.dll` + `steam-bridge-helper` into that same directory
at build time. The overlay must therefore map that path as a DIRECTORY entry, never a file list.

Tauri resource-map rules already proven in this repo (packagingConfig.test.ts:120-145, 226-243,
derived from tauri-utils `src/resources.rs`):
  - Directory entries end in `/` and take the walkdir branch (structure preserved via strip_prefix)
  - A GLOB entry (`**/*.json`) uses `dest.join(file_name)` and FLATTENS — never use one here
  - The TARGET value may not contain `..` or `resource_relpath` turns it back into `_up_`
  - A single-file entry states the full target path including filename (precedent:
    `"../build/changelog.json": "build/changelog.json"`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the wholesale bin resource entry with three per-platform overlays</name>
  <files>src-tauri/tauri.conf.json, src-tauri/tauri.macos.conf.json, src-tauri/tauri.windows.conf.json, src-tauri/tauri.linux.conf.json, src/backend/__tests__/packagingConfig.test.ts</files>
  <action>
Delete the `"../build/bin/": "build/bin"` key from `src-tauri/tauri.conf.json`'s
`bundle.resources`. Leave the other four keys exactly as they are. This deletion is mandatory
and is the reason overlays alone are not enough: Tauri merges `tauri.{platform}.conf.json` onto
the base, and a merge can only ADD or OVERRIDE a key — it cannot REMOVE one. Leave the wholesale
key in place and every platform still inherits all six trees no matter what the overlays say.

Create three sibling files next to `tauri.conf.json`, each a minimal JSON object containing only
`bundle.resources`:

`tauri.macos.conf.json` — `../build/bin/arm64/darwin/` -> `build/bin/arm64/darwin`,
`../build/bin/x64/darwin/` -> `build/bin/x64/darwin`,
`../build/bin/x64/win32/EpicGamesLauncher.exe` -> same path under `build/`,
`../build/bin/x64/win32/GalaxyCommunication.exe` -> same path under `build/`,
`../build/bin/legendary.LICENSE` -> `build/bin/legendary.LICENSE`.

`tauri.windows.conf.json` — `../build/bin/x64/win32/` -> `build/bin/x64/win32`,
`../build/bin/arm64/win32/` -> `build/bin/arm64/win32`, plus `legendary.LICENSE`.

`tauri.linux.conf.json` — `../build/bin/x64/linux/` -> `build/bin/x64/linux`,
`../build/bin/arm64/linux/` -> `build/bin/arm64/linux`, the two `x64/win32` exe FILE entries
(Linux runs them under Wine exactly as macOS does — see the interfaces block), plus
`legendary.LICENSE`.

ARCH DECISION, stated because it costs ~46MB and must not be silently assumed away:
`.github/workflows/release-tauri.yml:52-57` still builds BOTH `aarch64-apple-darwin` and
`x86_64-apple-darwin`. The `phase-34-16-closed-partial-x64-retired` decision retired x64 mac from
the *electron-builder* pipeline only and deferred the Tauri cleanup to Phase 34.18, which has not
run. Tauri platform overlays are per-PLATFORM, not per-ARCH, so a static macOS overlay must carry
BOTH darwin trees. The arm64 DMG therefore keeps ~44MB of x64 runners it will never execute, and
this fix saves ~162MB rather than the todo's ~208MB. The alternative — a per-leg
`tauri build --config src-tauri/tauri.macos-arm64.conf.json` flag — recovers that 44MB but makes
a forgotten flag ship a macOS bundle with NO darwin runners at all, and would need matching
changes to `pnpm tauri:dev:packaged`. Fail-safe beats 44MB here. Record the alternative in the
Task 3 brief; do not implement it.

Then update `src/backend/__tests__/packagingConfig.test.ts`. Five existing assertions break or go
vacuous once the base loses its `bin` key — lines 103-110 ('bundle.resources references the bin tree'),
line 184 ('every publicDir-resolved asset class is carried'), and the three map-shape invariants
at lines 165-243 which would now only cover the four non-bin base entries. Add a
`mergedResourceMap(platform)` helper returning `{...base.bundle.resources, ...overlay.bundle.resources}`
and re-run the existing shape invariants (map-form, `build/`-prefixed targets, no `..` in a target,
no `/main` `/preload` `sea-config` `sidecar-prep`, locales-as-directory) over all three merged maps
rather than over the base alone. Then add these NEW assertions, which must fail in both directions:
  - NEGATIVE, the whole point of the change: no key in the BASE map matches `bin/` — if the
    wholesale entry ever returns, every overlay becomes decorative and this test is the only
    thing that notices.
  - Each of the three merged maps contains `x64/win32/EpicGamesLauncher.exe` and
    `x64/win32/GalaxyCommunication.exe` (F1: all three platforms execute them under Wine).
  - macOS merged map contains no key with `linux` and none with `arm64/win32`;
    Windows merged contains none with `darwin` and none with `linux`;
    Linux merged contains none with `darwin`.
  - Anti-vacuity: each overlay file parses, and each merged map has strictly more keys than the
    base map (a typo'd filename that makes the overlay unreadable must not read as "clean").
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib &amp;&amp; node -e "const fs=require('fs');for(const f of ['tauri.conf.json','tauri.macos.conf.json','tauri.windows.conf.json','tauri.linux.conf.json'])JSON.parse(fs.readFileSync('src-tauri/'+f,'utf8'));const b=JSON.parse(fs.readFileSync('src-tauri/tauri.conf.json','utf8')).bundle.resources;const k=Object.keys(b);if(k.length!==4)throw new Error('base bundle.resources has '+k.length+' keys, expected 4');if(k.some(x=>x.includes('bin')))throw new Error('base still carries a bin key: '+k.join(','));console.log('base map OK',k.join(','))" &amp;&amp; pnpm exec jest src/backend/__tests__/packagingConfig.test.ts</automated>
  </verify>
  <done>All four config files parse as JSON; `packagingConfig.test.ts` passes with the new merged-map and negative assertions; the base `bundle.resources` has exactly four keys, none containing `bin`.</done>
</task>

<task type="auto">
  <name>Task 2: Prove it on a real packaged artifact and record the fix-(2) baseline</name>
  <files>.planning/quick/260901-8rm-shrink-tauri-macos-bundle-narrow-bundle-/260901-8rm-MEASUREMENTS.md</files>
  <action>
Build a packaged artifact and census it. `pnpm tauri:dev` proves NOTHING here: it serves over
`devUrl` and resolves no bundled resource at all (`vite.config.ts:12-28`). Use
`pnpm tauri:dev:packaged`, which runs `vite build` then `tauri build --debug` and goes through
`bundle.resources` exactly as a release bundle does.

Prerequisites, in order: `pnpm download-helper-binaries` if `public/bin` is not fully populated
(it is TAG-idempotent, not presence-idempotent — do not `rm -rf` it first), then
`pnpm build-steam-bridge` so `public/bin/arm64/darwin/` carries the shim and helper the overlay's
directory entry is supposed to pick up. Then `pnpm tauri:dev:packaged`.

Do not launch the app to judge this. A `--debug` packaged build runs `node build/main/sidecar.js`
rather than the bundled SEA, so a runtime observation would be measuring the wrong binary. This
task's evidence is entirely file-level census plus `size`/`strings` on the shell binary.

Run the gate script in <verify>. It asserts, bidirectionally:
  POSITIVE  — arm64/darwin and x64/darwin present with a non-zero file count; the two win32 Wine
              exes present as files; legendary.LICENSE present.
  MERGE     — `build/locales`, `build/changelog.json`, `build/webviewPreload.js` and
              `build/icon.png` present. This is the decisive empirical test of F3: if Tauri
              shallow-REPLACED `bundle.resources` instead of deep-merging it, these four base
              entries vanish and the shipped app has no translations. If this half fails, STOP —
              the overlay design is wrong and every overlay must instead restate the four base
              entries.
  NEGATIVE  — arm64/win32, x64/linux and arm64/linux absent, and `x64/win32` contains ONLY the
              two exes (not comet/gogdl/legendary/nile).
  SIZE      — the shipped bin tree is at least 100MB smaller than the repo's own `build/bin`,
              and is itself larger than 100MB (anti-vacuity: an empty tree must not pass).

Then capture the fix-(2) baseline from the SAME artifact and write all numbers to
`260901-8rm-MEASUREMENTS.md`: `size -m` `__const` and `__text` byte counts for
`Contents/MacOS/gamelib-shell`, `du -sh` of `src-tauri/target/debug/build/gamelib-shell-*/out/tauri-codegen-assets`,
`strings <shell> | grep -c 'bin/x64/win32/gogdl.exe'`, and `du -sk` of the whole `.app`.
`__const` must still be LARGE (>150,000,000) and the gogdl count must still be NON-ZERO — fix (1)
must not have moved them. If either changed, fix (1) accidentally touched the frontendDist
embedding and the two fixes are no longer independently attributable; record that and stop.

Note in the measurements file which legs this run does NOT cover: `tauri build --debug` on an
Apple Silicon host exercises the macOS overlay only. The Windows and Linux overlays are covered
by Task 1's config tests and by nothing else until a real CI matrix run. Say so plainly rather
than implying three-platform proof.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && APP=src-tauri/target/debug/bundle/macos/GameLib.app; R="$APP/Contents/Resources/build"; fail=0; test -d "$R/bin" || { echo "FAIL: no bin tree at $R/bin"; exit 1; }; for p in arm64/darwin x64/darwin; do n=$(find "$R/bin/$p" -type f 2>/dev/null | wc -l | tr -d ' '); echo "TREE $p files=$n"; [ "$n" -gt 0 ] || { echo "FAIL: $p missing/empty"; fail=1; }; done; for f in bin/x64/win32/EpicGamesLauncher.exe bin/x64/win32/GalaxyCommunication.exe bin/legendary.LICENSE locales changelog.json webviewPreload.js icon.png; do test -e "$R/$f" && echo "OK  $f" || { echo "FAIL: missing $f"; fail=1; }; done; for p in arm64/win32 x64/linux arm64/linux; do test -e "$R/bin/$p" && { echo "FAIL: $p still shipped"; fail=1; } || echo "OK  absent $p"; done; x=$(find "$R/bin/x64/win32" -type f ! -name EpicGamesLauncher.exe ! -name GalaxyCommunication.exe 2>/dev/null | wc -l | tr -d ' '); echo "x64/win32 extras=$x"; [ "$x" -eq 0 ] || fail=1; a=$(du -sk "$R/bin" | cut -f1); b=$(du -sk build/bin | cut -f1); echo "shipped_bin_kb=$a repo_build_bin_kb=$b saved_kb=$((b-a))"; [ "$a" -gt 100000 ] || { echo "FAIL: shipped bin tree implausibly small"; fail=1; }; [ $((b-a)) -gt 100000 ] || { echo "FAIL: saved less than 100MB"; fail=1; }; c=$(size -m "$APP/Contents/MacOS/gamelib-shell" | awk '/__const/{print $2}' | head -1); g=$(strings "$APP/Contents/MacOS/gamelib-shell" | grep -c 'bin/x64/win32/gogdl.exe'); echo "__const=$c gogdl_strings=$g"; [ "$c" -gt 150000000 ] || { echo "FAIL: __const moved — fix (1) touched the embedding"; fail=1; }; [ "$g" -gt 0 ] || { echo "FAIL: gogdl strings gone — fix (1) touched the embedding"; fail=1; }; grep -q '__const' .planning/quick/260901-8rm-shrink-tauri-macos-bundle-narrow-bundle-/260901-8rm-MEASUREMENTS.md || { echo "FAIL: measurements file missing __const record"; fail=1; }; [ "$fail" -eq 0 ] && echo PASS || { echo OVERALL-FAIL; exit 1; }</automated>
  </verify>
  <done>The gate script prints PASS. `260901-8rm-MEASUREMENTS.md` records the shipped/repo bin sizes, the saved delta, the `.app` total, `__const`, `__text`, the codegen-assets size, the gogdl strings count, and an explicit statement that only the macOS overlay was exercised.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the fix-(2) todo as an executable brief</name>
  <files>.planning/todos/pending/2026-08-28-tauri-macos-bundle-is-786mb-frontenddist-embeds-build-bin-in.md</files>
  <action>
Rewrite the pending todo so fix (2) is planned from evidence rather than re-derived. Mark fix (1)
DONE with a pointer to this quick task and the Task 2 measured numbers. Then replace the two-line
fix (2) paragraph with the following, all of which is planner-measured, not assumed:

WHY THE TODO'S OWN FRAMING IS WRONG. `frontendDist` is not merely "where index.html lives" —
two live consumers resolve assets relative to it and would break silently in a packaged artifact
only: (a) `src/frontend/index.tsx:114` i18next `loadPath: 'locales/{{lng}}/{{ns}}.json'`, a
relative `tauri://localhost/` URL, so `locales/` must remain reachable from frontendDist or the
packaged app loses every translation; (b) `src/preload/api/tauriChildWindows.ts:177` opens the
About window at `about.html?v=...`, likewise relative, and `about.html` is a `public/` file that
only reaches `build/` through vite's implicit publicDir copy. A "renderer-only" directory must
therefore carry `index.html`, `assets/`, `locales/` AND `about.html` at minimum. Whoever plans it
must first enumerate every webview-reachable relative URL (i18n loadPath, WebviewWindow urls,
any relative `fetch` in renderer/preload, and anything `index.html` or `about.html` reference)
rather than assuming the set is closed.

WHY `publicDir` CANNOT SIMPLY MOVE. `build/bin`, `build/locales`, `build/changelog.json`,
`build/webviewPreload.js` and `build/icon.png` are the SOURCE paths of `bundle.resources`, and
they exist only because vite's implicit `public/` -> `outDir` copy puts them there. Moving
`outDir` moves that 300MB copy too. Note the mechanism precisely: `paths.ts`'s runtime
`publicDir` resolution is NOT itself broken by the move (packaged it resolves
`Contents/Resources/build`, which `bundle.resources` populates by explicit target), what breaks
is the repo-side `build/` tree those resource SOURCES read from.

TWO CANDIDATE DESIGNS — pick by measurement, not by preference:
  D1 (the todo's): `publicDir: false`, `outDir: build/renderer`, `frontendDist: ../build/renderer`,
  plus an explicit copy step producing BOTH the full `build/` tree (for bundle.resources) and the
  webview-reachable subset inside `build/renderer/`. Saves ~212MB. Largest blast radius.
  D2 (planner-proposed, not yet costed): leave `frontendDist: "../build"` alone and simply stop
  putting `bin/` into `build/` — `publicDir: false` plus an explicit copy that excludes `bin/`,
  with `bundle.resources` sourcing the runner trees straight from `../public/bin/...` while
  keeping the `build/bin/...` TARGETS unchanged. Nothing webview-reachable moves, so (a) and (b)
  above cannot fire. Open question that decides it: does Tauri's resource copier preserve
  symlinks? `public/bin/*/darwin/*/` contains Python.framework symlinks and Apple's framework
  layout requires `Versions/Current` to be a link or codesign fails "bundle format is ambiguous".
  Under D2 `preserveRunnerSymlinksPlugin` no longer runs over that tree.

FIVE PINNED ASSERTIONS THE FIX MUST UPDATE (each currently green and each would go red):
  meta/__tests__/viteRendererConfig.test.ts:87-88  — pins `outDir === 'build'`, `emptyOutDir === false`
  src/backend/__tests__/packagingConfig.test.ts:257-277 — pins that `vite.config.ts` imports AND
    calls `preserveRunnerSymlinksPlugin()` (F-34.9-01, deliberately still live)
  src/backend/__tests__/releaseWorkflow.test.ts:437-530 — EXECUTES the workflow's prune step
    against a fixture and asserts `build/index.html` survives
  .github/workflows/release-tauri.yml:422-428 — the prune step's own `test -f build/index.html`
    guard, which hard-fails the release job if index.html moves
  vite.config.ts:106 — `emptyOutDir: false` exists only because `build/` is shared; re-reason it.

HOW IT MUST BE PROVEN: `pnpm tauri:dev:packaged` (or a real release bundle), never `tauri:dev`.
Baseline recorded by this quick task in `260901-8rm-MEASUREMENTS.md`. Success = `__const` drops
below ~30,000,000, `strings <shell> | grep -c 'bin/x64/win32/gogdl.exe'` becomes 0, and the app
still renders a non-English locale and opens the About window.

Finally, keep the `steam_api.pdb` (2,818,048 B) / `steam_api_shim.lib` (4,160 B) note as its own
separate item: they ship inside the `arm64/darwin` directory entry this plan keeps, they are
Windows build byproducts of `meta/buildSteamBridgeShims.ts`, and their removal is a different
mechanism (stop emitting them) from resource mapping. Do not fold it into fix (2).
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && F=.planning/todos/pending/2026-08-28-tauri-macos-bundle-is-786mb-frontenddist-embeds-build-bin-in.md; fail=0; for s in "index.tsx:114" "tauriChildWindows.ts:177" "viteRendererConfig.test.ts:87" "releaseWorkflow.test.ts:437" "packagingConfig.test.ts:257" "tauri:dev:packaged" "260901-8rm-MEASUREMENTS.md" "D1" "D2" "steam_api.pdb"; do grep -q "$s" "$F" || { echo "MISSING: $s"; fail=1; }; done; grep -qi "fix (1)" "$F" && grep -qiE "DONE|CLOSED|COMPLETE" "$F" || { echo "MISSING: fix (1) not marked done"; fail=1; }; [ "$fail" -eq 0 ] && echo PASS || { echo OVERALL-FAIL; exit 1; }</automated>
  </verify>
  <done>The todo names all five pinned assertions with file:line, both candidate designs D1/D2, both relative-URL consumers, the `tauri:dev:packaged` proof requirement, the recorded baseline file, and keeps the pdb/lib item separate. Fix (1) is marked done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored runner tree -> shipped bundle | `public/bin/**` is externally-sourced third-party content copied verbatim into a signed artifact |
| build config -> shipped artifact contents | a resource-map edit silently decides what a user's machine can execute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8RM-01 | Denial of Service | `bundle.resources` narrowing | mitigate | Narrowing a tree a live consumer needs bricks a store's game launch with a downstream `ENOENT` six layers away (utils.ts:590's own recorded failure shape). Mitigated by Task 2's POSITIVE census over every path named in the interfaces block, plus Task 1's per-platform Wine-exe assertions. |
| T-8RM-02 | Tampering | Tauri platform-config deep merge | mitigate | If the merge REPLACES rather than merges, the base's `locales`/`changelog.json`/`webviewPreload.js`/`icon.png` silently vanish from the artifact. Task 2's MERGE half asserts all four are present in the built `.app` and halts the plan if not. |
| T-8RM-03 | Information Disclosure | `legendary.LICENSE` | mitigate | Dropping the bundled licence text from a redistributed third-party binary is a compliance regression. All three overlays carry it explicitly; Task 2 asserts it in the artifact. |
| T-8RM-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages. No `package.json`/`Cargo.toml` dependency changes. |
</threat_model>

<verification>
1. `pnpm exec jest src/backend/__tests__/packagingConfig.test.ts` — green, with merged-map and negative assertions.
2. Task 2's artifact gate script prints `PASS` against a real `tauri build --debug` bundle.
3. `260901-8rm-MEASUREMENTS.md` exists and records `__const`, `__text`, codegen-assets size, gogdl strings count, shipped/repo bin sizes and the saved delta.
4. `pnpm codecheck` clean. (This says nothing about CI lint — do not treat it as a lint gate.)
5. Do NOT attempt to fix the repo-wide-red prettier or i18n pre-push gates; push with `--no-verify` if they block.
</verification>

<success_criteria>
- A packaged macOS artifact ships exactly `bin/arm64/darwin`, `bin/x64/darwin`, `bin/x64/win32/{EpicGamesLauncher,GalaxyCommunication}.exe` and `bin/legendary.LICENSE` — and nothing else under `bin/`.
- Measured saving over the repo's own `build/bin` is at least 100MB (expected ~162MB).
- The four non-`bin` base resource entries survive the merge into the artifact.
- `__const` and the gogdl `strings` count are unchanged, proving fix (2) is still entirely open and independently attributable.
- The fix-(2) todo is an executable brief naming both relative-URL consumers, five pinned assertions, and two candidate designs.
</success_criteria>

<output>
Create `.planning/quick/260901-8rm-shrink-tauri-macos-bundle-narrow-bundle-/260901-8rm-SUMMARY.md` when done.
</output>
