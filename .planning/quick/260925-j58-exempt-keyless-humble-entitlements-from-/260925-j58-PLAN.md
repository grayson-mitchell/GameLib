---
phase: quick/260925-j58
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/humble/keyTypePresentation.ts
  - src/backend/humble/__tests__/keyTypePresentation.test.ts
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
  - src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx
  - public/locales/*/gamelib.json
  - public/locales/*/gamelib.mt.json
  - .planning/todos/pending/2026-09-25-humble-keys-revealed-gog-keyless-row-renders-a-finish-activation-dead-end.md
  - .planning/todos/completed/2026-09-25-humble-keys-revealed-gog-keyless-row-renders-a-finish-activation-dead-end.md
autonomous: true
requirements:
  - "todo 2026-09-25-humble-keys-revealed-gog-keyless-row-renders-a-finish-activation-dead-end (severity: medium, area: humble/keys-screen) actioned by operator decision; its `ready: live-gate` is CANCELLED, not deferred"
must_haves:
  truths:
    - "A REVEALED keyless entitlement (gog_keyless / epic_keyless / origin_keyless) renders NO Finish-activation button in the KEY column."
    - "That row's KEY cell is not empty — it carries a non-interactive annotation naming the store the entitlement landed on."
    - "A REVEALED keyed `gog` row still renders 'Finish activation' (CR-01 unregressed)."
    - "A REVEALED `steam` row still renders 'Activate' (260823-op3 unregressed)."
    - "An unrecognised key_type ending in `_keyless` does NOT take the keyless branch — the set is closed, not a suffix match."
    - "The presence baseline stays at totalPairs 0: the new gamelib key exists in all 49 locales."
  artifacts:
    - path: "src/common/humble/keyTypePresentation.ts"
      provides: "isKeylessKeyType closed-set predicate, sited next to REDEEM_URL_BUILDERS"
      contains: "isKeylessKeyType"
    - path: "src/backend/humble/__tests__/keyTypePresentation.test.ts"
      provides: "closed-set proof including the foo_keyless FALSE assertion"
      contains: "foo_keyless"
    - path: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      provides: "keyless exemption inside the revealed branch"
      contains: "isKeylessKeyType"
    - path: "public/locales/en/gamelib.json"
      provides: "humbleKeys.keylessClaimed source string"
      contains: "keylessClaimed"
    - path: ".planning/todos/completed/2026-09-25-humble-keys-revealed-gog-keyless-row-renders-a-finish-activation-dead-end.md"
      provides: "closed todo with its live-gate prescription rewritten, not ticked"
      contains: "status: RESOLVED"
  key_links:
    - from: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      to: "common/humble/keyTypePresentation"
      via: "named import of isKeylessKeyType"
      pattern: "isKeylessKeyType"
    - from: "src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx"
      to: "public/locales/en/gamelib.json"
      via: "tGamelib('gamelib:humbleKeys.keylessClaimed', ...)"
      pattern: "humbleKeys\\.keylessClaimed"
---

<objective>
A REVEALED `gog_keyless` row currently renders "Finish activation", whose `onFinish` is
`openWizard(key, 'finish')` (`Keys/index.tsx:476`) — the reveal/redeem wizard, opened against an
entitlement that has no code. Humble already granted it to the linked store account server-side.

Purpose: extend the repo's existing, twice-written rule — *a code-assuming affordance is never
offered to a keyless entitlement* — to the one branch that still breaks it. This is precedent, not
a new judgement:

- `REDEEM_URL_BUILDERS` (`keyTypePresentation.ts:125-136`, note T-UIC-01) deliberately OMITS
  `gog_keyless` because "a keyless entitlement has no key code".
- `isGiftable` (`viewFilters.ts:142,160`) excludes it because offering a gift there "would promise
  a hand-off the user cannot complete".

"Finish activation" → `openWizard(key, 'finish')` is a code-assuming affordance. Same rule, third
site.

**The live gate the todo prescribed is CANCELLED, by operator decision.** Its only output was
"fix or don't" (step 3: does the wizard error, hang, or show a usable state?). The corrective below
is correct under EVERY branch of that unknown, so the unknown is not worth buying. Do not plan,
schedule, or perform a live run for this task, and do not mint a UAT item.

Output: a closed-set `isKeylessKeyType` predicate, a non-interactive annotation replacing the dead
button, one new `gamelib` string filled across all 49 locales, four regression-pinned component
tests, and the todo closed with its dead prescription rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@src/common/humble/keyTypePresentation.ts
@src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx
@.planning/todos/pending/2026-09-25-humble-keys-revealed-gog-keyless-row-renders-a-finish-activation-dead-end.md

Read-only, do NOT modify: `src/frontend/screens/Humble/Keys/index.tsx` (the caller),
`src/common/humble/viewFilters.ts` (the `isGiftable` precedent),
`src/backend/humble/classify.ts` (`KNOWN_GAME_KEY_TYPES`, line 183).
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do not go re-hunt them. -->

`src/backend/humble/classify.ts:183` — the attesting set. Exactly three of its ten members end in
`_keyless`:

  KNOWN_GAME_KEY_TYPES = { steam, gog, gog_keyless, origin, origin_keyless, uplay, epic,
                           epic_keyless, battlenet, nintendo_direct }

`src/common/humble/keyTypePresentation.ts` — existing exports:

  export const HUMBLE_REDEEM_HELP_URL: string
  export type HumbleStoreLogoId = 'steam' | 'gog' | 'epic'
  export type HumbleKeyTypePresentation =
    | { kind: 'branded'; name: string; logo: HumbleStoreLogoId }
    | { kind: 'named'; name: string }
    | { kind: 'unknown' }
  export function getKeyTypePresentation(keyType: string): HumbleKeyTypePresentation
  export function getRedeemTarget(keyType: string, code: string): HumbleRedeemTarget
  export type HumbleGameLibLoginStore = 'steam' | 'gog' | 'epic'
  export function getGameLibLoginStore(keyType: string): HumbleGameLibLoginStore | null

`src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx` — locals already in scope at
the edit site: `isSteam` (`:355`), `isGogKeyless` (`:363`), `platformDisplay` (`:371`, carries
`.name` / `.logo`), `tGamelib`, `claimAction`, `humbleKey`. The KEY-column chain is
`const claimContent = ...` at `:450`; the branch to edit is the ternary arm opening at `:472`
(`claimAction.revealedAt !== null || humbleKey.state === 'REVEALED'`), whose CR-01 comment runs
`:473-480` and whose button runs `:493-509`. The next arm (`claimAction.keyindexResolved`) opens at
`:511`.

Class names to reuse verbatim (both already styled in `src/frontend/screens/Humble/Keys/index.css`
at `:632` and `:642`): `humbleKeyClaimGroup`, `humbleKeyClaimAnnotation`. **No new CSS.**

Test harness, `.../HumbleKeyRow/__tests__/index.test.tsx` — no DOM, no Router, no renderer. The
component is invoked as a plain function: `HumbleKeyRow(props) as ReactElement`. Helpers already
defined in-file: `makeHumbleKey(overrides)` (`:66`), `collectElements(node)` (`:97`),
`findByClassNamePart(tree, part)` (`:119`), `textContent(node)` (`:129`), `firstRowChild` (`:156`).
`makeClaimAction(overrides)` is local to the `KEY-column scenario resolution` describe block at
`:666` — add the new cases inside that same block so it is in scope. The `react-i18next` mock
returns the DEFAULT string with `{{...}}` interpolated, so the catalog is irrelevant to these tests.

Validation harness for the locale fill — `meta/machineFillGamelib.ts`:

  export function validateTranslation(
    source: string, target: string, glossary: string[],
    options?: { countOptional?: boolean }
  ): string[]   // [] means clean; never throws

Importing that module fires `main()` (network + writes) unless `JEST_WORKER_ID` is set — its
entrypoint guard is `if (!process.env.JEST_WORKER_ID) void main()` (`:1186`). Set
`JEST_WORKER_ID=1` before importing. Glossary terms are `meta/i18nGlossary.json` → `.terms`.

`public/locales/<locale>/gamelib.mt.json` shape:

  { "locale": "de", "model": "claude-sonnet-5", "filledAt": "<ISO>", "keys": [ ...sorted... ] }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the closed-set isKeylessKeyType predicate and its unit tests</name>
  <files>src/common/humble/keyTypePresentation.ts, src/backend/humble/__tests__/keyTypePresentation.test.ts</files>
  <behavior>
    - `isKeylessKeyType('gog_keyless')` → true
    - `isKeylessKeyType('epic_keyless')` → true
    - `isKeylessKeyType('origin_keyless')` → true
    - `isKeylessKeyType('gog')` / `'epic'` / `'origin'` / `'steam'` / `'uplay'` / `'battlenet'` /
      `'nintendo_direct'` / `'generic'` / `''` → false
    - CLOSED-SET PROOF: `isKeylessKeyType('foo_keyless')` → **false**. This single assertion is what
      distinguishes the literal set from an `endsWith('_keyless')` implementation; without it the
      suite cannot tell the two apart. Write it with a comment saying exactly that.
    - Non-vacuity pin: the three `true` members are exactly the three `_keyless` entries of
      `KNOWN_GAME_KEY_TYPES` — assert all three explicitly rather than looping.
  </behavior>
  <action>
Add `export function isKeylessKeyType(keyType: string): boolean` to
`src/common/humble/keyTypePresentation.ts`, sited immediately after `REDEEM_URL_BUILDERS` /
`getRedeemTarget` — next to the T-UIC-01 note that already states the reason a keyless entitlement
gets no code-assuming affordance.

Back it with a module-private literal set (a `Set<string>` or `Record<string, true>`) holding the
three exact literals `gog_keyless`, `epic_keyless`, `origin_keyless`, attested by
`KNOWN_GAME_KEY_TYPES` at `src/backend/humble/classify.ts:183`. It MUST NOT be
`keyType.endsWith('_keyless')` or any other suffix/regex match — a suffix match would admit an
unrecognised, potentially hostile `key_type` into a UI branch that suppresses an affordance, which
is the exact closed-set discipline `REDEEM_URL_BUILDERS` (`:119-129`) and `GAMELIB_LOGIN_STORES`
(`:172-186`) already document in their own comments.

Document it in the same voice as those two neighbouring tables: state that this is a THIRD
independent axis (keyless-ness), not derivable from the other two — `gog_keyless` is branded AND
has a login store AND has no deep link AND is keyless; `epic` is branded and has a login store and
is NOT keyless. Name the three literals and cite `classify.ts` as their attestation. Cross-
reference the T-UIC-01 note and `isGiftable`'s exclusion (`viewFilters.ts:142`) as the two prior
decisions this predicate generalises.

Then add a `describe('isKeylessKeyType', ...)` block to
`src/backend/humble/__tests__/keyTypePresentation.test.ts`, following the existing `test("'steam'
-> branded Steam", ...)` style in that file (it uses `test`, not `it`). Import the new symbol
alongside the existing named imports from `common/humble/keyTypePresentation`.
  </action>
  <verify>
    <automated>npx prettier --check src/common/humble/keyTypePresentation.ts src/backend/humble/__tests__/keyTypePresentation.test.ts</automated>
    <automated>npx jest --selectProjects Backend --testPathPattern keyTypePresentation</automated>
    <automated>pnpm codecheck</automated>
  </verify>
  <done>
`isKeylessKeyType` exported, literal-set backed, documented. The backend run is GREEN **and** its
trailing `Ran all test suites matching /keyTypePresentation/i` line confirms the file actually ran —
`--selectProjects` is case-sensitive and fails OPEN four ways; a green run that matched zero suites
proves nothing. `Backend` and `Meta` are capitalised exactly as written. `grep -c "endsWith" ` over
`src/common/humble/keyTypePresentation.ts` returns 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exempt keyless entitlements from the Finish-activation button in HumbleKeyRow</name>
  <files>src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx, src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx</files>
  <behavior>
    - REVEALED `gog_keyless` + `claimAction`: ZERO `<button>` elements from the claim group, AND a
      `humbleKeyClaimAnnotation` whose text is `Claimed on GOG — no key needed`.
    - REVEALED `epic_keyless` + `claimAction`: same shape, text `Claimed on Epic Games — no key
      needed`.
    - REGRESSION GUARD (CR-01): REVEALED keyed `gog` + `claimAction` still renders a button reading
      `Finish activation`.
    - REGRESSION GUARD (260823-op3): REVEALED `steam` + `claimAction` still renders a button reading
      `Activate`.
  </behavior>
  <action>
**Catalog is deliberately NOT touched in this task.** The new key lands whole — English plus all 48
other locales — in Task 3, because `en` gaining a key that other locales lack is presence-baseline
drift that reds CI (`meta/i18nCatalogPresenceBaseline.json` sits at `totalPairs: 0`). Splitting it
this way keeps every commit independently green; the component tests do not need the catalog,
because the `react-i18next` mock returns the default string.

In `src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx`:

1. Import `isKeylessKeyType` from `common/humble/keyTypePresentation`, adding it to the existing
   named-import block at `:13-18`.
2. Next to `isGogKeyless` at `:363`, add `const isKeyless = isKeylessKeyType(humbleKey.platform)`.
3. Inside the revealed arm that opens at `:472`
   (`claimAction.revealedAt !== null || humbleKey.state === 'REVEALED'`), branch on `isKeyless`.
   When keyless, render ONLY a non-interactive annotation:

   a `<span className="humbleKeyClaimGroup">` wrapping a
   `<span className="humbleKeyClaimAnnotation">` whose content is
   `tGamelib('gamelib:humbleKeys.keylessClaimed', 'Claimed on {{store}} — no key needed', { store: platformDisplay.name })`.

   Reuse those two class names verbatim — they are already styled at `Keys/index.css:632,642`. Add
   no CSS and invent no new class.

**Two constraints a later reader must not "simplify" away — record both as comments at the edit
site:**

- **The annotation is not decoration, and a bare suppression is wrong.** For a keyless key
  `claimAction.revealedAt` is always null (no local `humbleRevealedStore` record can exist for an
  entitlement that was never revealed), so the existing `revealedAt !== null &&` annotation renders
  nothing. Merely hiding the button would leave an EMPTY KEY cell and delete the row's only
  information. That is why this branch gets its own copy.
- **Namechecking the store is correct HERE, unlike D-43-11's "Claim on Humble".** That sibling
  string names the destination the CLICK reaches, and the click opens Humble's embed, not GOG's
  site. This string names where the entitlement actually LANDED: `classify.ts:174-181` records that
  Humble redeems a direct-redeem entitlement straight to the linked store account. Different
  question, different correct answer.

**Do NOT reorder the scenario chain.** The `humbleKey.state === 'REVEALED'` test and its CR-01
comment at `:473-480` are load-bearing: a key revealed on Humble's WEBSITE carries
`redeemed_key_val` and so classifies REVEALED, but has no local reveal record, so `revealedAt` is
null — and the backend refuses to reveal any non-UNREVEALED key (D-66 never-re-reveal). A reorder
regresses the case that comment was written for. The keyless exemption goes INSIDE (or immediately
ahead of) that arm, and the keyed-platform path must come out byte-identical.

Then add the four cases to the `HumbleKeyRow KEY-column scenario resolution` describe block
(`__tests__/index.test.tsx:665`), so the local `makeClaimAction` helper is in scope. Build keys with
`makeHumbleKey({ state: 'REVEALED', platform: '<key_type>' })` and pass `claimAction:
makeClaimAction()`. Assert button absence/presence via
`collectElements(tree).filter((el) => el.type === 'button')` and annotation text via
`findByClassNamePart(tree, 'humbleKeyClaimAnnotation')` + `textContent(...)`, exactly as the
neighbouring `Pitfall C` test at `:812` does. Do not introduce a renderer, a DOM, or a Router — the
suite's no-jsdom, plain-function-invocation convention is documented in its own header comment.
  </action>
  <verify>
    <automated>npx prettier --check src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx src/frontend/screens/Humble/Keys/components/HumbleKeyRow/__tests__/index.test.tsx</automated>
    <automated>npx jest --selectProjects Frontend --testPathPattern HumbleKeyRow</automated>
    <automated>pnpm codecheck</automated>
    <automated>pnpm lint</automated>
  </verify>
  <done>
Four new tests pass and the trailing `Ran all test suites matching /HumbleKeyRow/i` line names the
file. `git diff -- src/frontend/screens/Humble/Keys/components/HumbleKeyRow/index.tsx` shows the
CR-01 comment and the `state === 'REVEALED'` test unmoved, and shows NO edit to
`src/frontend/screens/Humble/Keys/index.tsx` or `src/frontend/screens/Humble/Keys/index.css`. No new
class name appears in the diff.
  </done>
</task>

<task type="auto">
  <name>Task 3: Land humbleKeys.keylessClaimed in all 49 locales, then close the todo and file the follow-up</name>
  <files>public/locales/*/gamelib.json, public/locales/*/gamelib.mt.json, .planning/todos/completed/2026-09-25-humble-keys-revealed-gog-keyless-row-renders-a-finish-activation-dead-end.md, .planning/todos/pending/&lt;new todo&gt;.md</files>
  <action>
**Part A — the catalog, as ONE unit.** Add `humbleKeys.keylessClaimed` =
`Claimed on {{store}} — no key needed` to `public/locales/en/gamelib.json`, and fill it in all 48
other locales in the SAME commit. `en` gaining a key the other locales lack is presence-baseline
drift: `meta/i18nCatalogPresenceBaseline.json` sits at `totalPairs: 0`, R13 in
`meta/__tests__/lintTranslations.test.ts` re-derives the missing pairs live and demands exact
equality in both directions, and `missingPairs()` counts an empty string as missing. **Do not
regenerate the baseline to absorb a gap.**

- **`gamelib.json` only — NEVER `translation.json`.** The i18n churn guard reds CI on any write to
  the upstream catalogs.
- Hand-edit the JSON; do NOT run `pnpm i18n`. The extractor churns the three upstream `en/`
  catalogs on pre-existing unrelated source drift (recorded in `34.8-I18N-CONTRACT.md`), which is a
  different, larger mess than this task.
- Insert at the correct position: the `humbleKeys` block is sorted **case-insensitively
  ascending**. `keylessClaimed` folds to `keylessclaimed`, which sorts BEFORE `keyLoading`
  (`keyloading`) — they differ first at index 4, `e` before `o`. So it goes immediately before
  `keyLoading` in every locale. Verify the whole block is still sorted after the edit rather than
  trusting this note; key order is not gated, so nothing else will catch a misplacement.
- `pnpm machine-fill-gamelib` is UNAVAILABLE here: it hard-codes `api.anthropic.com`, ignores
  `ANTHROPIC_BASE_URL` (set to `http://127.0.0.1:8787` in this environment), and returns HTTP 401
  under a gateway-scoped key. The `!` prefix is not a different shell. **In-session authorship is
  the sanctioned fallback** (precedent: `88416589f`, the single-key
  `installFlows.diskSpaceFreeOfTotal` fill across 49 locales — read that commit before starting).
- Author each of the 48 values, then **validate BEFORE writing** by importing the real
  `validateTranslation` from `meta/machineFillGamelib.ts` with `JEST_WORKER_ID=1` set (its
  entrypoint guard at `:1186` fires `main()` — a network call and writes — without it). Pass
  `meta/i18nGlossary.json`'s `.terms`. Every candidate must return `[]`. Prove the check is
  non-vacuous with a sabotage control: run one candidate with `{{store}}` deleted and confirm it
  reports the dropped-placeholder problem. Record both results in the SUMMARY.
- Every value MUST retain `{{store}}` verbatim and MUST be non-empty.
- Stamp provenance: add `humbleKeys.keylessClaimed` into each `gamelib.mt.json`'s `keys[]` **in
  sorted position** and refresh that manifest's `filledAt`. State the known inaccuracy in the
  SUMMARY rather than concealing it: `model` is ONE global field per manifest, not per key.

**Part B — close the todo.** Move
`.planning/todos/pending/2026-09-25-humble-keys-revealed-gog-keyless-row-renders-a-finish-activation-dead-end.md`
to `.planning/todos/completed/` with `status: RESOLVED` (use `git mv`). **REWRITE, do not tick:**

- Replace the `## What to check (one look, no build required…)` section wholesale. Its three steps
  prescribe a live run that will never happen; leaving them ticked leaves a dead prescription alive
  in the file. State instead that the gate was CANCELLED by operator decision on 2026-09-25 because
  its only output was "fix or don't", and the corrective shipped here is correct under every branch
  of outcome 3 (error, hang, or usable already-claimed state) — so the unknown was not worth buying.
- Correspondingly rewrite the `ready: live-gate` rationale. The frontmatter `ready:` value is now
  moot for a completed todo (the gate is `pending/`-only), but the BODY must not still argue for a
  live run.
- Record what shipped: the predicate, the branch exemption, the new string, the four tests.
- Leave the "Scope note — this is the other half of a pair" and the "Incidental trap" sections
  intact. Do not re-touch the sibling todo
  `2026-09-25-humble-keys-gog-keyless-claim-button-opens-an-embed-with-no-host-lifecycle.md` — it is
  already RESOLVED and closed.

**Part C — file ONE follow-up todo** in `.planning/todos/pending/` covering BOTH remaining sites, so
the deliberately-unwidened scope does not evaporate:

- `src/frontend/screens/Humble/Keys/index.tsx:467-468` routes only `gog_keyless`'s `onClaim` to the
  Humble keys embed, so `epic_keyless` / `origin_keyless` still open the reveal wizard for a code
  that does not exist.
- `isGiftable` / `isGiftableSpare` in `src/common/humble/viewFilters.ts:116,157-160` exclude only
  `gog_keyless`, so the other two still offer a gift affordance.

Say plainly why this task did not fix them: these are different branches carrying a different
destination question (D-43-11's probe measured the reveal endpoint against a `gog_keyless`
entitlement specifically), and changing `isGiftable` alters the gift affordance on rows this task
never touches. `isKeylessKeyType` already exists for whoever picks it up.

The new todo MUST carry the three CI-enforced frontmatter keys **in this exact order** — `severity:`
then `platform:` then `ready:` — bare, lowercase, unquoted, placed after `area:`/`files:` per the
existing files in that directory. The `/gsd-add-todo` template does not emit them and
`pnpm planning-gates` goes red without them. Values: `severity: medium`, `platform: any`,
`ready: code`. Never widen the gate's vocabulary to admit a value that failed.

Finally, check the todo body for a stray unmatched closing envelope tag before writing — the
planning-gates envelope check fires on any planning file.
  </action>
  <verify>
    <automated>npx prettier --check public/locales/*/gamelib.json public/locales/*/gamelib.mt.json</automated>
    <automated>npx jest --selectProjects Meta --testPathPattern "lintTranslations|gamelibCatalogParity"</automated>
    <automated>pnpm lint-translations</automated>
    <automated>pnpm i18n-churn-guard</automated>
    <automated>pnpm planning-gates</automated>
    <automated>node -e "const fs=require('fs');const ls=fs.readdirSync('public/locales');const miss=ls.filter(l=>{const j=JSON.parse(fs.readFileSync('public/locales/'+l+'/gamelib.json','utf8'));const v=j.humbleKeys&&j.humbleKeys.keylessClaimed;return !v||!String(v).includes('{{store}}')});if(miss.length){console.error('MISSING/BROKEN:',miss);process.exit(1)}console.log('all',ls.length,'locales carry the key with {{store}}')"</automated>
  </verify>
  <done>
All 49 `gamelib.json` files carry `humbleKeys.keylessClaimed` with `{{store}}` intact and non-empty;
all 48 `gamelib.mt.json` manifests list it. `meta/i18nCatalogPresenceBaseline.json` is UNCHANGED and
still reads `totalPairs: 0` (`git diff --stat` shows it absent from the diff). `git diff --stat`
shows zero `translation.json` / `gamepage.json` files touched. The trailing
`Ran all test suites matching /lintTranslations|gamelibCatalogParity/i` line names both files. The
todo is in `completed/` with `status: RESOLVED` and its live-gate prescription REWRITTEN (grep the
completed file: no surviving "Open Humble Keys on a build with a synced library" step). The new
pending todo carries `severity: medium` / `platform: any` / `ready: code` bare and in that order.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Humble API → `classify.ts` → `humbleKey.platform` | An attacker-influenced or merely unrecognised `key_type` string crosses into UI branch selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-j58-01 | Tampering | `isKeylessKeyType` in `common/humble/keyTypePresentation.ts` | mitigate | Literal closed set of three exact keys, never a suffix/regex match — an unrecognised `key_type` can only miss and fall through to the keyed path. Pinned by the `foo_keyless → false` assertion in Task 1. |
| T-j58-02 | Information disclosure | the new annotation string | mitigate | `keylessClaimed` interpolates only `platformDisplay.name` — a display name from the module's own untranslated-proper-noun table, never `redeemed_key_val` and never any per-key value. Same D-68/T-14-09 constraint the help-URL branch already honours. |
| T-j58-03 | Denial of service (self-inflicted, CI) | `public/locales/*/gamelib.json` | mitigate | Key and all 48 fills ship as one unit; presence baseline is never regenerated to absorb a gap. Verified by R13 plus the explicit 49-locale node check in Task 3. |
| T-j58-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this task — zero dependency changes. |
</threat_model>

<verification>
Run from the repo root after all three tasks:

- `pnpm codecheck`
- `npx jest --selectProjects Backend Frontend Meta --testPathPattern "keyTypePresentation|HumbleKeyRow|lintTranslations|gamelibCatalogParity"` — read the trailing `Ran all test suites matching` line and confirm it names all four files; `--selectProjects` is case-sensitive and fails open.
- `pnpm lint` · `pnpm lint-translations` · `pnpm i18n-churn-guard` · `pnpm planning-gates`
- `npx prettier --check` over the exact paths written, per task. Explicit paths only, never a bare `.` — `src/preload/.prettierrc` sets a different `printWidth` from the root, so the directory a path sits in changes the correct answer, and a bare `.` drags in unrelated repo debt. No other gate in this repo sees formatting.

Explicitly NOT part of verification: any live app run, any UAT item. The gate is cancelled.
</verification>

<success_criteria>
- A REVEALED `gog_keyless` / `epic_keyless` row renders no Finish-activation button and a populated
  (never empty) KEY cell naming the store.
- REVEALED keyed `gog` still says "Finish activation"; REVEALED `steam` still says "Activate".
- `isKeylessKeyType('foo_keyless') === false` is asserted in the suite.
- `humbleKeys.keylessClaimed` present in all 49 locales with `{{store}}` intact; presence baseline
  untouched at `totalPairs: 0`.
- No change to `Keys/index.tsx`, `viewFilters.ts`, `Keys/index.css`, or any `translation.json`.
- The source todo is in `completed/` with `status: RESOLVED` and its live-gate prescription
  rewritten, not ticked.
- One new `ready: code` todo covers the two deliberately-unfixed sites with correctly ordered
  frontmatter.
</success_criteria>

<output>
Create `.planning/quick/260925-j58-exempt-keyless-humble-entitlements-from-/260925-j58-SUMMARY.md` when done.
</output>
