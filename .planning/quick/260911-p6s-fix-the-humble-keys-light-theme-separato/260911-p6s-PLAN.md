---
quick_id: 260911-p6s
date: 2026-09-11
status: planned
title: 'Fix the Humble Keys light-theme row separator and the GOG store-logo fill, plus a source gate for both'
autonomous: true
baseline_sha: c05e6e570a143328240473f39c1c60c3b999ccaa
files_modified:
  - src/frontend/screens/Humble/Keys/index.css
  - src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts
  - .planning/todos/pending/2026-09-08-humble-key-row-store-logo-fill-currentcolor-unverified-live.md
  - .planning/todos/pending/2026-09-11-humble-key-row-separator-is-invisible-in-light-themes.md
must_haves:
  truths:
    - 'All three --divider sites in Keys/index.css carry a currentColor-derived fallback, not rgba(255,255,255,0.08)'
    - 'A .humbleKeyRowStoreLogo-scoped .gogIcon rule re-routes the GOG svg back onto currentColor'
    - 'A Frontend-project source gate asserts both, and each assertion has a SANITY negative control that proves it is not vacuously true'
    - 'The existing NavShell --divider gates (themeTokens.test.ts census, appShellLayout.test.ts no-fallback grep and universality SANITY) stay green'
    - 'Both todos stay OPEN in pending/ with ready: live-gate and an appended record of what changed'
  artifacts:
    - path: src/frontend/screens/Humble/Keys/index.css
      provides: 'the two CSS fixes'
      contains: 'color-mix(in srgb, currentColor'
    - path: src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts
      provides: 'source-text gate for the divider fallback and the .gogIcon escape'
      contains: 'SANITY'
  key_links:
    - from: src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts
      to: src/frontend/screens/Humble/Keys/index.css
      via: 'readFileSync + stripSourceComments, the themeTokens.test.ts source-gate idiom'
      pattern: 'stripSourceComments'
---

<objective>
Two verified light-theme rendering defects on the Humble Keys screen, both found by the
Phase 43 REQ-43-19 live gate on 2026-09-11. Both diagnoses are COMPLETE and verified —
this plan implements them, it does not re-litigate them.

1. The GOG store logo never resolves through `currentColor`: `gog-logo.svg` carries
   `class="gogIcon"` on its root `<svg>`, and `_colors.scss:101`'s `.gogIcon { fill:
   var(--text-default) }` matches that element DIRECTLY, which beats the `fill:
   currentColor` that `.humbleKeyRowStoreLogo` only supplies by INHERITANCE. Steam and
   Epic carry no class and are correct in both themes.
2. The row separator is effectively invisible in light themes: `--divider` is declared in
   only 2 of 11 theme blocks, so light themes fall through to the literal fallback
   `rgba(255, 255, 255, 0.08)` — white over a light background, measured delta 2 against a
   required >=3.

Purpose: close the two `ready: code` halves of the REQ-43-19 failure set so the next gate
run has something new to measure.
Output: two CSS edits, one new source gate, two todos moved `code` -> `live-gate`.

Verification is DESK-LEVEL ONLY. The colour deltas cannot be confirmed without a packaged
rebuild and a live operator run — that is exactly why both todos move to `live-gate`
rather than closing. Do NOT plan or run a build, a live run, or a screenshot measurement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@src/frontend/screens/Humble/Keys/index.css
@src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts
@.planning/todos/pending/2026-09-08-humble-key-row-store-logo-fill-currentcolor-unverified-live.md
@.planning/todos/pending/2026-09-11-humble-key-row-separator-is-invisible-in-light-themes.md

<measured_facts>
Established before planning; do not re-derive:

- `src/frontend/screens/Humble/Keys/index.css` has exactly THREE `--divider` sites, all
  with the identical white-biased fallback `rgba(255, 255, 255, 0.08)`:
  - `:224` `.humbleKeyRow` `border-bottom` (the one the gate measured)
  - `:588` `.humbleKeysColumnHeader` `border-bottom`
  - `:614` `.humbleKeysSortPicker .MuiOutlinedInput-notchedOutline` `border-color`
- `.humbleKeyRowStoreLogo` is at `:368-377`; it already declares
  `color: var(--text-secondary)` and `fill: currentColor`. `.humbleKeyRowStoreLogo svg`
  follows at `:379-382`.
- `.gogIcon` has exactly three occurrences in `src/`: the asset itself
  (`src/frontend/assets/gog-logo.svg:1`), `src/frontend/styles/_colors.scss:101` (the
  global `fill: var(--text-default)`), and `src/frontend/screens/Game/GamePage/index.css:619`
  (`&.gogIcon`, padding only; its parent sets its own `fill` at `:610`). The scoped escape
  below touches neither.
- `src/frontend/screens/Humble/Keys/__tests__/` exists and contains only `index.test.tsx`.
  NOTHING currently reads `Keys/index.css` as source text — the new gate is the first.
- The Frontend jest project (`src/frontend/jest.config.js`) is `roots: ['<rootDir>/src/frontend']`,
  `testMatch: ['**/__tests__/**/*.test.tsx', '**/__tests__/**/*.test.ts']`,
  `testEnvironment: node` (no jsdom, no CSS engine). A `.test.ts` under that `__tests__/`
  directory is picked up automatically. `themeTokens.test.ts` already imports
  `backend/testUtils/stripSourceComments` from inside this same project, so that import
  path resolves.
- `pnpm lint` = `node meta/lintScoped.cjs`: TWO independent ceilings with ZERO padding —
  `SRC_CEILING = 1124` (glob `.`, ignoring `**/__tests__/**` and `**/__mocks__/**`) and
  `TESTS_CEILING = 638` (globs `**/__tests__/**/*.{ts,tsx}`, `**/__mocks__/**/*.{ts,tsx}`).
  The new test file lands in the TESTS scope. Neither ceiling has a free slot today.
- Scripts: `pnpm codecheck` = `tsc --noEmit`; `pnpm prettier` = `prettier --check .`
  (repo-wide); `pnpm planning-gates` = `python3 meta/runPlanningGates.py`.
</measured_facts>
</context>

<hard_constraints>
These turn EXISTING gates red if violated. Re-read before each edit.

1. Do NOT define `--divider` in `src/frontend/themes.scss`.
   `NavShell/__tests__/themeTokens.test.ts:285` asserts `--divider` is declared in
   strictly FEWER theme blocks than the file defines, deliberately, to stop a contributor
   universalising it. `appShellLayout.test.ts:282` has a SANITY test asserting
   `tokenResolvesInEveryTheme('divider') === false`. Defining it everywhere fails both.
2. Do NOT introduce any no-fallback `var(--divider)`.
   `appShellLayout.test.ts:245-268` greps `/var\(\s*--divider\s*\)/g`. Every edit here
   keeps a fallback; the fallback is the whole point of the fix.
3. Keep the WKWebView fractional-grid-track comment above `.humbleKeyRow`'s
   `border-bottom` (`Keys/index.css:214-223`) INTACT. It is load-bearing guidance about a
   known GameLib trap. You may APPEND a sentence noting the fallback change; do not
   delete, truncate or reword the existing text.
4. Do NOT touch `src/frontend/styles/_colors.scss`. The global `.gogIcon` rule stays. The
   Humble fix is a scoped escape, not a global change.
5. Do NOT touch `src/frontend/screens/Game/GamePage/index.css` or
   `src/frontend/assets/gog-logo.svg`. The asset's known defects (non-square `viewBox`,
   nested `<symbol>`, `className=` instead of `class=`) are recorded in the todo and stay
   out of scope.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Replace the white-biased --divider fallback at all three sites and add the scoped .gogIcon escape</name>
  <files>src/frontend/screens/Humble/Keys/index.css</files>
  <action>
Two independent edits in one stylesheet.

EDIT A — divider fallback, all THREE sites (`:224`, `:588`, `:614`). Replace the literal
`rgba(255, 255, 255, 0.08)` fallback with
`color-mix(in srgb, currentColor 14%, transparent)`, keeping the `var(--divider, ...)`
wrapper and the rest of each declaration byte-identical:

- `.humbleKeyRow` -> `border-bottom: 1px solid var(--divider, color-mix(in srgb, currentColor 14%, transparent));`
- `.humbleKeysColumnHeader` -> the same `border-bottom` value
- `.humbleKeysSortPicker .MuiOutlinedInput-notchedOutline` -> `border-color: var(--divider, color-mix(in srgb, currentColor 14%, transparent));`

Rationale to record in the source (see the comment instruction below): `currentColor` is a
CSS-wide keyword, not a custom property, so it CANNOT be undefined in any theme — which is
precisely the failure mode this codebase has scar tissue for (the CR-01 / `--divider`
defect class documented at length in `NavShell/index.scss:101-159`). `color-mix(in srgb, ...)`
is established prior art in this tree.

Per hard constraint 3, APPEND to the existing `.humbleKeyRow` comment (do not rewrite it) a
short note along the lines of: the fallback was white-biased `rgba(255,255,255,0.08)` until
2026-09-11, which measured delta 2 against a light background (`237 + 0.08*(255-237) =
238.4`, gate measured seam `[238,240,245]` vs background `[237,239,244]`) against REQ-43-19's
`>=3` threshold; `--divider` itself stays out of `themes.scss` on purpose, so the fallback is
the load-bearing half. Add an equivalent one-or-two-line comment above the
`.humbleKeysColumnHeader` and sort-picker sites pointing back to the `.humbleKeyRow` comment
rather than restating it. Keep every factual claim checkable against the todo — a comment
that inverts its cited source is a recurring defect class here.

EDIT B — GOG escape. Immediately after the existing `.humbleKeyRowStoreLogo svg` rule
(`:379-382`), add:

```
.humbleKeyRowStoreLogo .gogIcon {
  fill: currentColor;
}
```

with a comment above it stating the mechanism: `gog-logo.svg`'s root `<svg>` carries
`class="gogIcon"`, and `styles/_colors.scss:101`'s `.gogIcon { fill: var(--text-default) }`
matches that element DIRECTLY, which beats the `fill: currentColor` `.humbleKeyRowStoreLogo`
supplies only by INHERITANCE — a directly-matching declaration wins over an inherited value
regardless of specificity. This descendant selector is `0,2,0` and out-specifies the global
`0,1,0` while leaving every other `.gogIcon` consumer alone. Note that Steam and Epic carry
no class and never needed this. Do NOT change `.humbleKeyRowStoreLogo`'s own `color` or
`fill` declarations.

After editing, run `pnpm exec prettier --write src/frontend/screens/Humble/Keys/index.css`
so the file is committed already-formatted (the repo-wide `prettier --check` is verified in
Task 3).
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && python3 - <<'PY'
import re
s = open('src/frontend/screens/Humble/Keys/index.css').read()
code = re.sub(r'/\*[\s\S]*?\*/', '', s)
assert len(re.findall(r'var\(--divider,\s*color-mix\(in srgb,\s*currentColor 14%,\s*transparent\)\)', code)) == 3, 'expected 3 color-mix divider fallbacks'
assert 'rgba(255, 255, 255, 0.08)' not in code, 'white-biased fallback survives'
assert not re.search(r'var\(\s*--divider\s*\)', code), 'no-fallback var(--divider) introduced'
assert re.search(r'\.humbleKeyRowStoreLogo \.gogIcon\s*\{[^}]*fill:\s*currentColor', code), 'gogIcon escape missing'
assert 'fractional' in s and '1fr' in s, 'WKWebView trap comment truncated'
print('OK')
PY</automated>
  </verify>
  <done>All three `--divider` sites carry the `color-mix` fallback, no `rgba(255, 255, 255, 0.08)` remains in code, no bare `var(--divider)` exists, the scoped `.gogIcon` escape is present, and the WKWebView comment survives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the Humble Keys stylesheet source gate with SANITY negative controls</name>
  <files>src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts</files>
  <behavior>
    - Divider half: the comment-STRIPPED stylesheet contains no `rgba(255, 255, 255, ...)` divider fallback, and exactly three `var(--divider, color-mix(in srgb, currentColor 14%, transparent))` occurrences.
    - No-fallback half: the stripped stylesheet contains no `/var\(\s*--divider\s*\)/` (mirrors `appShellLayout.test.ts:245-268`'s repo-wide rule at this file's scope).
    - GOG half: the stripped stylesheet contains a `.humbleKeyRowStoreLogo .gogIcon` block whose body declares `fill: currentColor`, and the selector is SCOPED — a bare top-level `.gogIcon {` rule must NOT appear in this file.
    - SANITY for each of the above: the same regex fired against a known-bad literal string fixture (the pre-fix `var(--divider, rgba(255, 255, 255, 0.08))` declaration; a bare `var(--divider)`; an unscoped `.gogIcon { fill: currentColor; }`) produces the opposite result — proving no assertion is vacuously true.
    - SANITY for the stripper: a fixture proving that prose inside a `/* ... */` comment which NAMES `rgba(255, 255, 255, 0.08)` does not survive `stripSourceComments`, so the negative assertion cannot be defeated by the comment Task 1 writes (and equally, the positive assertions cannot be SATISFIED by comment prose).
  </behavior>
  <action>
Create `src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts`, following
the source-gate idiom in `src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts`:
a module docstring stating WHY this is a source gate and what it cannot prove, a `REPO_ROOT`
computed with `join(__dirname, ...)` (count the levels for THIS file's depth — do not copy
themeTokens' count blind), and a `read()` helper piping `readFileSync(..., 'utf8')` through
`stripSourceComments` imported from `backend/testUtils/stripSourceComments`.

The docstring must say plainly: this proves the SOURCE says the right thing and can never
prove anything RENDERS correctly — the Frontend jest project is `testEnvironment: 'node'`
with no jsdom and no CSS engine, so it cannot compute inheritance, resolve `currentColor`,
or evaluate `color-mix`. The adjudicator of appearance remains the next REQ-43-19 live gate
run, which is why both todos stay OPEN at `ready: live-gate`.

Assertions exactly as in `<behavior>`. Every positive and negative assertion gets its own
paired `it('SANITY: ...')` that fires the SAME regex against an inline known-bad fixture
string, matching the naming and structure of `appShellLayout.test.ts:165`, `:212`, `:218`,
`:282`. Do not batch several SANITY checks into one `it` unless they share one fixture.

Comment the divider assertion with the reason the white fallback was wrong (the arithmetic
and measurement from the todo) and the reason `currentColor` was chosen over defining
`--divider` per theme — the `themeTokens.test.ts:285` census FORBIDS universalising the
token, so a theme-side fix is not available here. Cross-reference both NavShell gates by
file and test name so a future reader finds the constraint without re-discovering it.

Zero-warning requirement: `meta/lintScoped.cjs`'s `TESTS_CEILING = 638` has NO padding, so
this file must produce zero eslint warnings and zero errors. Prefer plain `it`/`expect`, no
unused imports, no `any`. If a warning is unavoidable, STOP and report rather than bumping
the ceiling silently.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && pnpm exec jest --selectProjects Frontend --passWithNoTests src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts src/frontend/components/UI/NavShell/__tests__/themeTokens.test.ts src/frontend/components/UI/NavShell/__tests__/appShellLayout.test.ts</automated>
  </verify>
  <done>The new gate passes; both existing NavShell `--divider` gates still pass in the same run; the file introduces no eslint warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Update both todos to ready: live-gate and run the full desk verification set</name>
  <files>.planning/todos/pending/2026-09-08-humble-key-row-store-logo-fill-currentcolor-unverified-live.md, .planning/todos/pending/2026-09-11-humble-key-row-separator-is-invisible-in-light-themes.md</files>
  <action>
TODO EDITS — both stay OPEN in `pending/`. Do NOT move either to `completed/`, do NOT change
`status: OPEN`, do NOT change `severity` (both stay `medium`) or `platform` (both stay `any`).
Change ONLY `ready: code` -> `ready: live-gate` in each frontmatter, keeping key order
(`severity`, then `platform`, then `ready`) per CLAUDE.md's todo triage rules. Values stay
bare, lowercase, unquoted.

Append a dated section to each body — do not rewrite existing text, which is the audit trail:

- GOG logo todo: record that option 1 was taken; quote the added rule
  (`.humbleKeyRowStoreLogo .gogIcon { fill: currentColor; }`), state that `_colors.scss:101`
  and `GamePage/index.css:619` were deliberately left untouched, and state what remains
  unproven: that `currentColor` actually reaches the `<use>`/`<symbol>` indirection in
  WKWebView, which only a live render can show. Note the non-square `viewBox` / malformed
  asset findings remain untouched and out of scope.
- Separator todo: record the answer to the question the todo posed — the token is MISSING,
  not badly defined (`--divider` is declared in only 2 of 11 theme blocks in `themes.scss`,
  `:97` `var(--neutral-03)` and `:145` `gray`), so light themes were painting the literal
  white fallback. Record that the fix was applied at all THREE sites in the file, not just
  the measured one, and that defining `--divider` per theme was NOT available as a remedy
  because `themeTokens.test.ts:285` and `appShellLayout.test.ts:282` actively forbid it.

Both appended sections state the closure condition identically: the next Phase 43 REQ-43-19
gate run re-measures seam delta (>=3 in at least one light and one dark theme) and the GOG
glyph against `--text-secondary`. Neither todo closes here.

Also add a one-line pointer in each to the new gate file so a future reader finds the
desk-level evidence.

FULL VERIFICATION SET — run all five and report each verbatim. Per project memory, gate
colour is RE-MEASURED, never assumed:

1. `pnpm codecheck`
2. `pnpm exec jest --selectProjects Frontend --passWithNoTests` (whole Frontend project, not
   just the new file — the existing `Keys/__tests__/index.test.tsx` must stay green too)
3. `pnpm lint` — two ceilings, zero padding. If the TESTS scope exceeds 638, the new test
   file introduced a warning: FIX THE TEST, do not raise the ceiling.
4. `pnpm exec prettier --check src/frontend/screens/Humble/Keys/index.css src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts .planning/todos/pending/2026-09-08-humble-key-row-store-logo-fill-currentcolor-unverified-live.md .planning/todos/pending/2026-09-11-humble-key-row-separator-is-invisible-in-light-themes.md`
   (scoped to touched files, run from the repo root so the real config resolves — never
   against a temp copy)
5. `pnpm planning-gates` (the todo frontmatter gate reads `.planning/todos/pending/`, and
   both todos are edited here)

Do NOT run a build, a packaged run, or any screenshot measurement. If any of the five is red
for a reason that predates `c05e6e570`, say so and NAME the sha you measured the baseline at
— "pre-existing" without a named baseline is not a finding.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib && pnpm codecheck && pnpm exec jest --selectProjects Frontend --passWithNoTests && pnpm lint && pnpm exec prettier --check src/frontend/screens/Humble/Keys/index.css src/frontend/screens/Humble/Keys/__tests__/humbleKeysStylesheet.test.ts && pnpm planning-gates && python3 - <<'PY'
import re, pathlib
for p in pathlib.Path('.planning/todos/pending').glob('*humble-key-row-*'):
    t = p.read_text()
    fm = t.split('---')[1]
    assert re.search(r'^ready: live-gate$', fm, re.M), f'{p.name}: ready not live-gate'
    assert re.search(r'^severity: medium$', fm, re.M), f'{p.name}: severity changed'
    assert re.search(r'^platform: any$', fm, re.M), f'{p.name}: platform changed'
    assert re.search(r'^status: OPEN$', fm, re.M), f'{p.name}: status changed'
    assert 'humbleKeysStylesheet.test.ts' in t, f'{p.name}: no pointer to the new gate'
print('OK')
PY</automated>
  </verify>
  <done>Both todos are still OPEN in `pending/` at `ready: live-gate` with `severity: medium` / `platform: any` intact and an appended record; all five verification commands reported, with any red explicitly attributed to a named baseline sha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Stylesheet values, a test file and two planning records. No untrusted input, no network, no IPC, no filesystem path from a user, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p6s-01 | Tampering | `src/frontend/themes.scss` (NOT edited) | mitigate | Hard constraint 1; Task 2's gate run includes `themeTokens.test.ts` + `appShellLayout.test.ts`, which fail loudly if `--divider` is universalised. |
| T-p6s-02 | Repudiation | the two todo records | mitigate | Task 3 APPENDS and never rewrites; the frontmatter assertion pins `status`/`severity`/`platform` so a scope-reducing edit cannot pass silently. |
| T-p6s-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan — zero new dependencies, no legitimacy gate required. |
</threat_model>

<verification>
Desk-level only, by explicit scope decision. The five commands in Task 3 are the complete
set. The colour deltas CANNOT be confirmed without a packaged rebuild and a live operator
run on this Mac — that is why both todos move to `ready: live-gate` rather than closing.

Known gate traps that apply here, from project memory:
- `pnpm lint` is TWO ceilings (`SRC_CEILING = 1124`, `TESTS_CEILING = 638`), each sitting at
  its exact measured count with no padding. The new test file lands in the TESTS scope.
  RE-MEASURE; do not assume a free slot exists.
- `pnpm prettier` is repo-wide and has re-reddened silently before. Task 3 scopes
  `--check` to the touched files and runs it from the repo root, never against a copy.
- `--selectProjects` is case-sensitive and exits 0 on no match — `--passWithNoTests` is
  paired with it deliberately, and the run must report a nonzero test count.
- CSS `grep -c` over unstripped source counts comment prose. Both the Task 1 verify script
  and the Task 2 gate strip comments before asserting.
</verification>

<success_criteria>
- All three `--divider` sites in `src/frontend/screens/Humble/Keys/index.css` use
  `var(--divider, color-mix(in srgb, currentColor 14%, transparent))`; no
  `rgba(255, 255, 255, 0.08)` survives in code; no bare `var(--divider)` exists.
- `.humbleKeyRowStoreLogo .gogIcon { fill: currentColor; }` is present and scoped;
  `_colors.scss`, `GamePage/index.css` and `gog-logo.svg` are unmodified (`git diff --stat`
  shows exactly the four files in `files_modified`).
- The WKWebView fractional-grid-track comment is intact (appended to at most).
- `humbleKeysStylesheet.test.ts` passes, every assertion has a SANITY negative control, and
  the existing NavShell `--divider` gates stay green.
- Both todos remain OPEN in `pending/` at `ready: live-gate`, `severity: medium`,
  `platform: any`, with an appended record and a pointer to the new gate.
- `pnpm codecheck`, the Frontend jest project, `pnpm lint`, scoped `pnpm exec prettier
  --check`, and `pnpm planning-gates` all reported — green, or red with a named baseline sha.
</success_criteria>

<output>
Create `.planning/quick/260911-p6s-fix-the-humble-keys-light-theme-separato/260911-p6s-SUMMARY.md` when done.
</output>
