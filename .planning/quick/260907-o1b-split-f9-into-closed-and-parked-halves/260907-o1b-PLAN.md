---
quick_id: 260907-o1b
phase: quick-260907-o1b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md
autonomous: true
requirements: [F-9]
actions_todo: .planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md
closes_todo: false
baseline_sha: 8ccd2cd1d

must_haves:
  truths:
    - "A reader opening the F-9 todo sees TWO questions with OPPOSITE dispositions, not one ambiguous one."
    - "Wherever the id=1575 half is described as closed, the SAME sentence carries UNDETERMINED — 'closed' can never be read as 'answered'."
    - "The unpark condition is a command a reader can copy, paste, and run, with its path stated for macOS and for non-macOS."
    - "A reader knows what a non-zero grep count does NOT establish, and which SECOND file holds the other leg."
    - "A reader knows nothing automated runs the unpark grep, stated inside the PARKED section itself."
    - "The file is still in .planning/todos/pending/ and its frontmatter still parses."
    - "The five existing Disposition sections are byte-identical to their state at 8ccd2cd1d."
  artifacts:
    - path: ".planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md"
      provides: "The split record: one CLOSED-AS-UNANSWERABLE section, one PARKED section with a runnable unpark condition, and a rewritten blocked_by"
      contains: "## PARKED 2026-09-07"
  key_links:
    - from: "frontmatter status:"
      to: "the PARKED body section"
      via: "prose status pointing at the section, matching 2026-08-17-humble-slots-still-prompt-unattended-at-startup.md"
      pattern: "status: \"PARKED 2026-09-07"
    - from: "the PARKED section's unpark condition"
      to: "gamelib-shell.log"
      via: "a copy-pasteable grep naming the per-OS path"
      pattern: "gamelib-shell\\.log"
---

<objective>
Split the F-9 todo into its two distinct questions and give each the disposition it actually
warrants — one closes as permanently unanswerable, one parks with a now-mechanically-checkable
unpark condition — and rewrite the `blocked_by` line that quick tasks `260905-omc` and `260907-j8n`
made false as written.

Purpose: the file currently asks ONE question in a voice that implies ONE answer is coming. It is
really two questions. `id=1575`'s co-occurrence can never be answered — parking it implies "later",
and there is no later. The class question CAN now be answered on the next recurrence, because
`260907-j8n` made a recurrence passively self-recording. Leaving both in one undifferentiated
`pending` item means the answerable half inherits the unanswerable half's hopelessness, and the
unanswerable half inherits a false promise of eventual resolution.

Output: one modified file — the F-9 todo. Nothing else. No source change, no test change, no new
file, no move out of `pending/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md
@.planning/todos/pending/2026-08-17-humble-slots-still-prompt-unattended-at-startup.md
@.planning/quick/260907-j8n-persist-rpc-transport-failure-diagnostics/260907-j8n-SUMMARY.md
@.planning/quick/260907-fni-f9-live-capture-shell-scrollback/260907-fni-SUMMARY.md
</context>

<verified_facts>
All of the following were re-verified at `8ccd2cd1d` (the actual HEAD at planning time; the
orchestrator's recon was taken at `4f01e1c2b`). Do not re-derive them — but do re-run the two
`grep` probes at the bottom before you write, because one of them reads a live log file.

**F1 — The file is ALREADY half-parked, and has been since August.** Frontmatter carries
`parked: 2026-08-23` / `parked_by: operator` (lines 9-10) while `status: pending` (line 5), and the
body carries a `## Park` section (line 60). This inconsistency is PRE-EXISTING. This task reconciles
it; it does not introduce it. The new text must say so, so a reader does not conclude that this task
invented the park.

**F2 — `blocked_by` (line 8) is now FALSE AS WRITTEN.** Verbatim at `8ccd2cd1d`:

> `blocked_by: "nothing external — it needs a RECURRENCE, which cannot be scheduled. Not blocked, UNSCHEDULED. Note id=1575 carries no channel name, so the originating request must be located in the same scrollback AT THE TIME; co-occurrence cannot be settled after the fact."`

The clause after "Note" was the justification for parking. `260905-omc` (the diagnostic now names
its channel) and `260907-j8n` (it now persists to a file) removed that obstacle FOR FUTURE
OCCURRENCES. It remains exactly true for `id=1575` itself. The rewrite PRESERVES that distinction —
it does not delete the sentence, it scopes it.

**F3 — the log path, read from source, both branches.** `shell_diag_log_path(home)`
(`src-tauri/src/main.rs:8617`) = `shell_panic_log_path(home).with_file_name("gamelib-shell.log")`,
and `shell_panic_log_path` (`main.rs:8571-8588`) branches on `cfg!(target_os = "macos")`:

| Platform | Path |
|---|---|
| macOS | `$HOME/Library/Logs/GameLib/gamelib-shell.log` |
| everything else (incl. Linux) | `$HOME/.config/gamelib/gamelib-shell.log` |

**F4 — Windows almost certainly has NO such file at all.** `shell_diag` (`main.rs:8632`) resolves
the directory from `std::env::var("HOME")` only. `HOME` is not a standard Windows environment
variable (Windows uses `USERPROFILE`), so on a normal Windows shell `shell_diag_log_path` returns
`None` and the `let ... else { return; }` at 8632-8634 makes the file write a silent no-op — stderr
still gets the line, the file does not. State this. Do NOT ship a path table that implies Windows
coverage it does not have.

**F5 — the file's lines are NOT prefixed `[shell] `.** `shell_diag` (`main.rs:8630-8651`) writes
`eprintln!("[shell] {message}")` to stderr but `format!("{epoch_secs} pid={} {message}\n")` to the
file. Confirmed against the live log:
`1788755786 pid=30321 sidecar process spawned OK`. A grep of `gamelib-shell.log` for `[shell]` finds
nothing. The unpark grep must match the message body only.

**F6 — the four rendered message bodies** (`main.rs:1006`, `1294`, `1306`, `8441`):
- `invoke abandoned (timeout): id=… channel=…`
- `invoke abandoned (sidecar closed): id=… channel=…`
- `response for unknown/timed-out id=… channel=… (dropped)`
- `response frame with a missing or non-string id (dropped)`

**F7 — `grep -c` EXITS 1 on a zero count.** Verified live: `grep -c '…' gamelib-shell.log` printed
`0` and exited `1`. That is not a failure and must be labelled as such next to the command, or a
reader in a `set -e` shell will misread it.

**F8 — the other leg lives in a DIFFERENT file.** The sidecar→Rust leg's timeout diagnostic is
``rustInvoke timed out after ${RUST_INVOKE_TIMEOUT_MS}ms: ${channel}`` (`sidecarRpc.ts:385`), which
goes through the sidecar's logger to `gamelib.log`, NOT `gamelib-shell.log`. Two legs, two files.

**F9 — the `260907-j8n` residual, and one more that follows from F5.**
`meta/captureShellScrollback.ts`'s `analyzeCapture()` keys its `RECURRENCE` verdict exclusively on
`TARGET_DROP_RE`, so it does not recognise the two new `invoke abandoned` lines. And
`TARGET_DROP_RE` is anchored `^\[shell\] …` (`captureShellScrollback.ts:224-226`), so by F5 it could
never match a `gamelib-shell.log` line even for the one shape it does recognise. The harness and the
log therefore observe **different event populations**, in two independent ways. A reader running the
unpark grep needs to know the harness would not have flagged the same event.

**F10 — only from `6fb96c76a` onward.** Persistence to `gamelib-shell.log` began at that commit. A
zero count over a log whose lines predate it establishes nothing.

**F11 — the convention to match** (`.planning/todos/pending/2026-08-17-humble-slots-still-prompt-unattended-at-startup.md`,
the ONLY existing PARKED todo): prose `status: "PARKED <date> — <reason>. See the PARKED section
below for the unpark condition."`, a `## PARKED <date> — <short title>` body section, and an
explicit `**Unpark …**` line.

**F12 — nothing automated reads `gamelib-shell.log`,** and `audit-uat` cannot see this todo at all.
The parked half is only distinguishable from "forgotten" if a human runs the grep. This project has
a recorded failure exactly here (`a-parked-status-buries-the-item-it-exempts`). Do NOT plan to wire
the grep into an automated audit — the operator explicitly deferred that as a broader change. State
the limitation IN the PARKED section.

**F13 — baseline probe, re-run this before writing.** At planning time, over 537 lines:
`grep -c 'invoke abandoned\|unknown/timed-out' ~/Library/Logs/GameLib/gamelib-shell.log` → `0`.
</verified_facts>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the frontmatter — prose PARKED status, and a blocked_by that is true</name>
  <files>.planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md</files>
  <action>
Change EXACTLY TWO LINES of the frontmatter. No other frontmatter key moves, and no body line is
touched in this task.

**Line 5, `status:`** — replace the bare `pending` with the prose form the sibling PARKED todo uses
(F11). It must name BOTH halves and it must carry UNDETERMINED in the same breath as the closure, so
that the status line alone cannot be misread. Write it as a single double-quoted YAML scalar along
these lines, adapting the wording as you see fit but preserving every load-bearing element:

`status: "PARKED 2026-09-07 — SPLIT INTO TWO QUESTIONS. Question 1 (did id=1575 co-occur with a
cookie operation?) is CLOSED AS PERMANENTLY UNANSWERABLE and its answer is recorded as UNDETERMINED,
FOREVER — closed is NOT answered, and it is NOT rounded to no. Question 2 (does this timeout shape
co-occur with a cookie operation as a class?) stays OPEN and is PARKED. See the two dated sections
below; the PARKED one carries a runnable unpark condition."`

Load-bearing elements that must survive whatever wording you land on: the literal token `PARKED
2026-09-07`, the phrase `UNDETERMINED`, an explicit denial that closed means answered, an explicit
denial of rounding to "no", and a pointer to the body sections. Keep it one line (YAML flow scalar,
double-quoted, no embedded unescaped `"`), matching how the sibling todo does it.

**Line 8, `blocked_by:`** — rewrite, do NOT delete. The sentence after "Note" is still TRUE of
`id=1575` and is now FALSE of every future occurrence (F2). The rewrite must carry that split
explicitly. Something along these lines, again one double-quoted line:

`blocked_by: "nothing external. Q1 (id=1575) is not blocked, it is UNANSWERABLE — that id carries no
channel name in the diagnostic as it was emitted, its scrollback was never captured, and no later
code can reconstruct a line that was never written; see the CLOSED section below. Q2 (the class
question) is not blocked either, it is UNSCHEDULED — it needs a RECURRENCE, which cannot be
scheduled. The old 'co-occurrence cannot be settled after the fact' clause was retired for FUTURE
occurrences by 260905-omc (the diagnostic now names its channel) and 260907-j8n (it now persists to
gamelib-shell.log); it remains exactly true for id=1575 itself."`

**Do NOT touch:** `created`, `title`, `source`, `severity`, `resolves_phase`, `parked`,
`parked_by`, `revisit_trigger`, `related_requirement`. In particular `parked: 2026-08-23` and
`parked_by: operator` STAY at their August values — they are the pre-existing park (F1) and
rewriting them would erase the fact that this file has been half-parked for two weeks.
`revisit_trigger` also stays verbatim; the new PARKED section supersedes it in specificity without
overwriting the historical wording.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib &amp;&amp; F=.planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md; test -f "$F" &amp;&amp; node -e "const fs=require('fs');const t=fs.readFileSync(process.argv[1],'utf8');const m=t.match(/^---\n([\s\S]*?)\n---\n/);if(!m)throw new Error('frontmatter did not parse');const fm=m[1];for(const k of ['created','title','source','status','severity','resolves_phase','blocked_by','parked','parked_by','revisit_trigger','related_requirement'])if(!new RegExp('^'+k+':','m').test(fm))throw new Error('missing key '+k);if(!/^parked: 2026-08-23$/m.test(fm))throw new Error('parked: was altered');if(!/^parked_by: operator$/m.test(fm))throw new Error('parked_by: was altered');if(!/^status: \"PARKED 2026-09-07/m.test(fm))throw new Error('status: not in the prose PARKED form');if(!/UNDETERMINED/.test(fm))throw new Error('status/blocked_by lost UNDETERMINED');if(!/id=1575/.test(fm))throw new Error('blocked_by lost the id=1575 scoping');console.log('frontmatter OK');" "$F" &amp;&amp; git diff --numstat -- "$F"</automated>
  </verify>
  <done>
Frontmatter parses; all eleven original keys still present; `parked:`/`parked_by:`/`revisit_trigger:`
byte-identical to `8ccd2cd1d`; `status:` is the prose `PARKED 2026-09-07` form carrying UNDETERMINED
and the closed-is-not-answered denial; `blocked_by:` scopes its old clause to `id=1575` rather than
deleting it. `git diff --numstat` shows exactly 2 added / 2 removed lines for this file at this point.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the two dated sections — one CLOSED AS UNANSWERABLE, one PARKED with a runnable trigger</name>
  <files>.planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md</files>
  <action>
APPEND two new `##` sections to the END of the file, after the `260907-j8n` disposition. Append
only — do not edit, reword, reorder, or reflow ANY existing body line, and in particular do not
touch the five existing `## Disposition` sections or the August `## Park` section. They are the
historical record and each was correct when written.

**Section A — the closed half.** Heading, verbatim shape:

`## CLOSED AS UNANSWERABLE 2026-09-07 (quick task 260907-o1b) — question 1: the answer is UNDETERMINED, FOREVER`

Content requirements, all mandatory:

- Open by stating the split itself: this file has always asked TWO questions, and they want
  OPPOSITE dispositions. Name them plainly. Q1: did `id=1575` specifically co-occur with a cookie
  operation? Q2: does this timeout shape co-occur with a cookie operation as a CLASS?
- State that Q1 is closed because it is **permanently unanswerable**, and give the reason in one
  sentence: that id's scrollback was never captured at the time it fired, and no code written
  afterwards can reconstruct a diagnostic that was never emitted.
- **THE CRITICAL SENTENCE.** Closing is NOT rounding to "no". The recorded answer is UNDETERMINED,
  and it stays UNDETERMINED forever. Write this so that no future reader skimming for a verdict can
  come away with "answered". Every place the word *closed* appears in this section, the word
  UNDETERMINED must appear in the SAME sentence. Say explicitly that the five dispositions above
  each refused to round this to "no", that this closure does not achieve that rounding by accident
  of bookkeeping, and that "closed" here means *the question was retired for lack of any possible
  evidence*, not *the question was resolved*.
- State why closing beats parking for this half: parking implies "later", and for `id=1575` there
  is no later. A park with no possible trigger is indistinguishable from a forgotten item.
- Note the pre-existing frontmatter inconsistency this task reconciled (F1): `parked: 2026-08-23` /
  `parked_by: operator` have coexisted with `status: pending` and a `## Park` section since August.
  Say plainly that this task RECONCILED that, and did not introduce it, and that the August
  `parked:` values were deliberately left at their original date.
- Note that the file deliberately STAYS in `.planning/todos/pending/`, because Q2 is still open —
  only one of the two questions closed, and a move to `completed/` would misrepresent that.

**Section B — the parked half.** Heading must match the sibling todo's convention (F11):

`## PARKED 2026-09-07 — question 2: does this timeout shape co-occur with a cookie operation (the class question)?`

Content requirements, all mandatory:

- One sentence on why this half can park now when it could not before: `260905-omc` made the
  diagnostic name its channel, and `260907-j8n` made it persist to a file — so a recurrence is now
  PASSIVELY recorded, with no operator at the keyboard and no scheduled capture session, and it
  survives a packaged build where LaunchServices discards stderr.
- **An explicit `**Unpark …**` line**, per convention, whose condition is the command below.
- **The command, copy-pasteable, in a fenced block**, with the per-OS path table from F3:

  ```
  # macOS
  grep -c 'invoke abandoned\|unknown/timed-out' ~/Library/Logs/GameLib/gamelib-shell.log
  # Linux (and any non-macOS build that sets HOME)
  grep -c 'invoke abandoned\|unknown/timed-out' ~/.config/gamelib/gamelib-shell.log
  ```

  Alongside it, state ALL of:
  - **F7:** `grep -c` prints `0` and **exits 1** when there are no matches. That is not an error.
  - **F5:** the file's lines are prefixed `{epoch} pid={pid} `, NOT `[shell] `. Do not add `[shell]`
    to the pattern; it will match nothing. Give the one-line real sample as proof of shape:
    `1788755786 pid=30321 sidecar process spawned OK`.
  - **F4:** on Windows there is very likely **no such file at all** — `shell_diag` resolves its
    directory from `HOME` only (`main.rs:8632`), Windows sets `USERPROFILE` instead, and the write
    is then a silent no-op while stderr still gets the line. This unpark condition is macOS/Linux
    only, and says so rather than pretending to cross-platform coverage.
  - **F10:** persistence began at commit `6fb96c76a`. A zero count over lines older than that
    establishes nothing.
  - **F13:** the baseline count at the time of writing (re-run the probe yourself and record the
    number you actually observe, with the line count of the file), so a future reader can tell
    growth from a cold start.

- **What a non-zero count DOES and does NOT establish.** State both sides, unmissably:
  - **DOES:** the timeout recurred, and the line names its `channel` — which is precisely what
    `id=1575` lacked.
  - **DOES NOT:** by itself prove co-occurrence with a cookie operation. That still requires
    correlating the OTHER leg (F8): ``rustInvoke timed out after …ms: humble_login_cookies*``
    (`sidecarRpc.ts:385`), which goes to **`gamelib.log`** — a DIFFERENT file. Name both files and
    both legs explicitly. Two files, two legs; a trigger that names only one is misleading.
- **The `260907-j8n` residual, restated for a reader who runs this grep (F9), both parts:**
  `analyzeCapture()` does not recognise the two new `invoke abandoned` lines — its verdict is keyed
  exclusively on `TARGET_DROP_RE` — so the active capture harness would NOT have raised a
  `RECURRENCE` on an event that this passive grep finds. And `TARGET_DROP_RE` is anchored
  `^\[shell\] `, so by F5 it could not match a `gamelib-shell.log` line even for the one shape it
  does recognise. The harness and the log observe **different event populations**. Point the reader
  at the active complement anyway: `pnpm capture:shell-scrollback` (`260907-fni`) remains the
  stronger, deliberate route; this grep is the weaker always-on one.
- **The limitation, stated IN THIS SECTION (F12), not left implicit.** Nothing automated reads
  `gamelib-shell.log`. `audit-uat` cannot see this todo at all. This parked item is only
  meaningfully different from a forgotten one if a human actually runs the grep above. Reference the
  recorded project failure by name — *a parked status buries the item it exempts*. Then say
  explicitly that wiring this grep into an automated audit was **deliberately deferred** by the
  operator as a broader change, and is therefore NOT filed as a todo by this task.
- **No IPC change.** State plainly that this task changes no channel, no command signature, and no
  frame shape — `.planning/IPC-PORT-INVENTORY.md` discipline is not engaged — so a reviewer need not
  re-derive it.
- Close by restating, one final time, that Q1's answer is and remains UNDETERMINED and is NOT
  rounded to "no", and that this task closes no box, checks nothing, and is `closes_todo: false`.

Prose only. No new frontmatter keys. No new files. Do not create a `.planning/todos/completed/` copy.
  </action>
  <verify>
    <automated>cd /Users/graysonmitchell/Projects/GameLib &amp;&amp; F=.planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md &amp;&amp; test -f "$F" &amp;&amp; test ! -e ".planning/todos/completed/$(basename "$F")" &amp;&amp; git diff --numstat -- "$F" | awk '{if($2!=2){print "FAIL: removed "$2" lines, expected exactly 2 (status + blocked_by)"; exit 1} print "append-only OK: +"$1" -"$2}' &amp;&amp; diff &lt;(git show 8ccd2cd1d:"$F" | sed -n '14,295p') &lt;(sed -n '14,295p' "$F") &amp;&amp; echo "existing body lines 14-295 byte-identical" &amp;&amp; node "$GATE" "$F"</automated>
    <note>`$GATE` is the script in this plan's `&lt;gate_script&gt;` section. Write it verbatim with the Write tool to the session scratchpad (e.g. `$SCRATCH/o1b-gate.cjs`) and set `GATE` to that absolute path. Do not commit it — it is a verification artifact, not a repo file. It was GREEN/RED-proved at planning time against a synthetic fixture: green on a compliant append, red on each of (a) an UNDETERMINED-less closed-sentence, (b) a missing non-macOS path, (c) a missing `audit-uat` limitation, (d) a missing refusal to round to "no".</note>
  </verify>
  <done>
Two new `##` sections appended at the end of the file. `git diff --numstat` shows exactly 2 removed
lines (Task 1's `status:` and `blocked_by:`) and zero other deletions. Existing body lines 14-295 are
byte-identical to `8ccd2cd1d`. Every sentence in the CLOSED section containing "closed"/"closes" also
contains UNDETERMINED. The PARKED section carries an explicit `**Unpark` line, the copy-pasteable
grep with both per-OS paths, the exit-1 caveat, the Windows caveat, the `6fb96c76a` scoping, both
legs and both log files, the `analyzeCapture()` residual, the `audit-uat`/nothing-reads-it
limitation, the no-IPC-change statement, and `closes_todo: false`. The file is still in
`.planning/todos/pending/` and no copy exists in `.planning/todos/completed/`.
  </done>
</task>

</tasks>

<gate_script>
Task 2's content gate, verbatim. Write with the Write tool to the session scratchpad as
`o1b-gate.cjs`, then `node <that path> <the todo file>`. Exits non-zero with a named reason on any
missing element, so a partial append cannot pass.

Two design points worth not undoing:

1. Every content check runs against the APPENDED text only (`t.slice(i)` from the CLOSED heading
   onward), not the whole file. Five of the fourteen tokens (`6fb96c76a`, `gamelib.log`,
   `rustInvoke timed out`, `analyzeCapture()`, `closes_todo`) ALREADY appear in the `260907-j8n`
   disposition above, so a whole-file check would be satisfied vacuously by the existing body and
   would gate nothing.
2. The CLOSED section's HEADING LINE is stripped before sentence-splitting. The mandated heading
   itself contains `UNDETERMINED`, and because it ends without terminal punctuation it glues to the
   first real sentence — leaving it in made the UNDETERMINED check pass vacuously. This was found by
   mutation at planning time, not reasoned about.

```javascript
const fs = require('fs')
const t = fs.readFileSync(process.argv[2], 'utf8')
const i = t.search(/^## CLOSED AS UNANSWERABLE 2026-09-07/m)
if (i < 0) throw new Error('CLOSED section heading missing')
const nw = t.slice(i)
const need = [
  [/^## PARKED 2026-09-07/m, 'PARKED section heading'],
  [/\*\*Unpark/, 'explicit **Unpark line'],
  [/grep -c 'invoke abandoned/, 'copy-pasteable grep'],
  [/Library\/Logs\/GameLib\/gamelib-shell\.log/, 'macOS path'],
  [/\.config\/gamelib\/gamelib-shell\.log/, 'non-macOS path'],
  [/exits?\s+1\b/, 'grep -c exit-1 caveat'],
  [/USERPROFILE/, 'Windows HOME caveat'],
  [/6fb96c76a/, 'persistence-start commit'],
  [/gamelib\.log\b/, 'the second file for the second leg'],
  [/rustInvoke\s+timed\s+out/, 'the sidecar-leg diagnostic'],
  [/analyzeCapture\(\)/, 'the j8n residual'],
  [/audit-uat/, 'the nothing-reads-it limitation'],
  [/IPC/, 'the no-IPC-change statement'],
  [/closes_todo/, 'closes_todo false']
]
const miss = need.filter(([re]) => !re.test(nw)).map(([, n]) => n)
if (miss.length) throw new Error('appended sections missing: ' + miss.join(', '))
const closed = nw
  .slice(0, nw.search(/^## PARKED 2026-09-07/m))
  .split('\n')
  .slice(1)
  .join('\n')
const bad = closed
  .split(/(?<=[.!?])\s+/)
  .filter((s) => /\b(closed|closes|closing|closure)\b/i.test(s))
  .filter((s) => !/UNDETERMINED/.test(s) && !/closes_todo/.test(s))
if (bad.length)
  throw new Error('sentence(s) say closed WITHOUT UNDETERMINED:\n - ' + bad.slice(0, 4).join('\n - '))
if (!/[Nn]ot\s+rounded\s+to|NOT\s+round/.test(nw)) throw new Error('missing the refusal to round to "no"')
console.log('both sections OK; every closed-sentence carries UNDETERMINED')
```
</gate_script>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This task modifies one markdown file under `.planning/`. No code path, no input parsing, no network call, no credential, no schema, and no IPC channel is added, removed, renamed, or reassigned. `.planning/IPC-PORT-INVENTORY.md` discipline is NOT engaged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-O1B-01 | Repudiation | The F-9 todo's historical record | mitigate | Task 2 is append-only and its verify `diff`s existing body lines 14-295 against `8ccd2cd1d`. The five Disposition sections and the August `## Park` section cannot be silently reworded. |
| T-O1B-02 | Tampering | The recorded verdict for `id=1575` | mitigate | The verify gate parses the CLOSED section and FAILS if any sentence containing "closed"/"closes" omits UNDETERMINED. The bookkeeping cannot round the answer to "no" without the gate firing. |
| T-O1B-03 | Information disclosure | The unpark command's log paths | accept | `~/Library/Logs/GameLib/gamelib-shell.log` and `~/.config/gamelib/gamelib-shell.log` are this application's own diagnostic files on the user's own machine, already documented in `main.rs`. No secret, token, or PII is named. |
| T-O1B-04 | Denial of service | `gamelib-shell.log` growth | accept | Log volume was decided deliberately in `260907-j8n` (no rotation, no throttle, bounded per site). This task adds no write site and changes no volume. |
| T-O1B-SC | Tampering | npm/pip/cargo installs | n/a | No package install of any kind in this task. RESEARCH.md's Package Legitimacy Gate is not engaged. |
</threat_model>

<verification>
Docs-only. There is no test to write and none should be invented.

Run, from the repo root, after both tasks:

1. **Append-only except two lines.**
   `git diff --numstat -- .planning/todos/pending/2026-08-23-f9-generic-rpc-timeout-cooccurrence-undetermined.md`
   → removed-count must be exactly `2` (the `status:` line and the `blocked_by:` line).
2. **Existing body untouched.**
   `diff <(git show 8ccd2cd1d:<file> | sed -n '14,295p') <(sed -n '14,295p' <file>)` → empty.
3. **Frontmatter still parses** and retains all eleven original keys, with
   `parked: 2026-08-23`, `parked_by: operator` and `revisit_trigger:` unchanged (Task 1's node check).
4. **Still in `pending/`.** `test -f .planning/todos/pending/<file>` and
   `test ! -e .planning/todos/completed/<file>`.
5. **No source or test change.** `git status --porcelain -- src src-tauri meta` → empty.
6. **Prettier** on the changed file only, if the repo's hook would touch it — note that
   `.husky/pre-push` prettier is RED repo-wide at baseline; do not report a pre-existing repo-wide
   red as this task's failure, and do not run a full-suite command to "confirm" this task.

Baseline sha for every "pre-existing" claim in the SUMMARY: **`8ccd2cd1d`**. Confirm it still matches
`git rev-parse HEAD` before any edit; if it does not, re-verify F1/F2/F3/F5/F6 at the new HEAD and
record the sha you actually used.
</verification>

<success_criteria>
- The F-9 todo presents TWO questions with OPPOSITE, dated dispositions.
- `id=1575` is closed as permanently unanswerable, and **every sentence that says so also says
  UNDETERMINED** — the word "closed" cannot be read as "answered" anywhere in the file.
- The class question is PARKED in the project's established convention (prose `status:`, a
  `## PARKED <date>` section, an explicit `**Unpark …**` line).
- The unpark condition is a runnable command with both per-OS paths, its `grep -c` exit-1 behaviour,
  its Windows non-coverage, its `6fb96c76a` scoping, a recorded baseline count, and an explicit
  statement of what a non-zero count does and does not establish — naming both legs and both files.
- The `260907-j8n` residual is restated so a reader knows the harness and the log see different
  event populations.
- The "nothing automated reads this" limitation is stated inside the PARKED section, with the
  automation wiring explicitly noted as deferred and NOT filed as a todo.
- `blocked_by` is true as written, with its old clause scoped to `id=1575` rather than deleted.
- Exactly one file changed; two frontmatter lines rewritten; everything else appended.
- `closes_todo: false`. No box checked. File stays in `.planning/todos/pending/`.
</success_criteria>

<output>
Create `.planning/quick/260907-o1b-split-f9-into-closed-and-parked-halves/260907-o1b-SUMMARY.md` when done.

The SUMMARY must state: the baseline sha actually used; the observed `git diff --numstat` numbers;
the baseline grep count and log line count observed at execution time; and that `closes_todo: false`.
</output>
