---
phase: quick-260917-u4d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md
  - .planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md
  - .planning/ROADMAP.md
  - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-VERIFICATION.md
  - .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-09-SUMMARY.md
  - src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "The D-32 adtraction todo lives under .planning/todos/completed/ with the same filename"
    - "The todo's frontmatter declares it closed, using the closed:/closed_by: convention already present in completed/"
    - "The todo body records that resolution path 1 was chosen by the operator on 2026-09-17, without deleting any of its existing analysis"
    - "Every live citation that spelled a todos/pending/ path for this todo now spells todos/completed/"
    - "The two quick-task historical audit artifacts are byte-identical to HEAD"
    - "WebViewAdtractionGapDeclared.test.ts still passes 10/10 and its executable lines are unchanged"
    - "pnpm planning-gates is green"
    - "The commit contains only the intended paths -- none of the five pre-existing dirty/untracked entries"
  artifacts:
    - path: ".planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md"
      provides: "The closed todo, content-preserved plus a resolution note"
      contains: "## Resolution"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md"
      via: "markdown path pointer"
      pattern: "todos/completed/2026-09-04-adtraction"
---

<objective>
Close the D-32 adtraction / ad-block-detection todo as **resolution path 1 — "do nothing further"**,
and repoint every live citation that named it by its `pending/` path so nothing rots.

Purpose: the declared gap IS the shipped outcome. The operator has taken this decision; it is not
being re-opened, re-researched, or re-argued.

Output: the todo under `completed/` with a closure record; five repointed citations; one
path-scoped commit.

**This is documentation/bookkeeping ONLY.** No production behaviour changes. No source logic
changes. The already-shipped deliverables — the gap comment and `window.api.logInfo('[WebView] D-32
gap: ...')` call in `src/frontend/screens/WebView/index.tsx`, and the test
`src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts` — **stay exactly as
they are.** They are what this close is blessing, not something to remove or shrink.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md

# Do NOT re-derive whether the gap could be closed with code. That question is settled.
</context>

<preflight>

## Read this before touching anything

**The working tree has PRE-EXISTING unrelated changes. They are not yours. Do not commit them.**

Measured at plan time (`git status --short`):

```
 M .planning/todos/completed/2026-09-11-humble-keys-title-wrap-sort-label-and-owned-badge-contrast-unverified-live.md
?? .claude/skills/archify/
?? .planning/quick/260912-d84-align-the-humble-keys-game-column-header/
?? .planning/spikes/024-epic-store-in-embedded-child-webview/run.log
?? .planning/spikes/024-epic-store-in-embedded-child-webview/shot-epic-INTERACTIVE-challenge-run5.png
?? .planning/spikes/024-epic-store-in-embedded-child-webview/shot-epic-INTERACTIVE-run4-squeezed-58px.png
?? skills-lock.json
```

Run `git status --short` as your first action and confirm **column 1 is blank or `?` for every
line** — i.e. the index is empty. If anything is already staged, STOP and report; do not proceed on
a dirty index.

Rules for the whole plan:
- **Never** `git add -A`, `git add .`, `git commit -a`, or `git stash`. This project has no worktree
  isolation (`workflow.use_worktrees=false`) — you are on `main` in the shared working tree and a
  stash would disturb concurrent work.
- Stage with explicit pathspecs only. Commit with a bare `git commit -m` (no `-a`, no trailing
  pathspec). A bare `git commit` commits **only what is staged**, which is the safe shape here
  because the index starts empty. Avoid `git commit -- <paths>` / `--only`: that form re-reads the
  **working tree** for those paths rather than the index, which defeats the staged-blob assertion
  in Task 1.

## The two populations of citation — this distinction is load-bearing

`grep -rn "2026-09-04-adtraction"` returns **nine** matches across live and historical files.

**LIVE POINTERS — repoint these.** The string is used to *resolve to the file*; if the file moves
and the string does not, the pointer is dead:

| # | Location | What the text actually spells |
|---|----------|-------------------------------|
| 1 | `.planning/ROADMAP.md:4971` | `todos/pending/2026-09-04-...md` (no `.planning/` prefix) |
| 2 | `40-09-SUMMARY.md:27` | `.planning/todos/pending/...md` — inside frontmatter `key-files: created:` list |
| 3 | `40-09-SUMMARY.md:203` | `.planning/todos/pending/...md` — in prose ("Filed `...`") |
| 4 | `40-VERIFICATION.md:280` | `.planning/todos/pending/...md` |
| 5 | `WebViewAdtractionGapDeclared.test.ts:27` | `.planning/todos/pending/...md` — inside a `/** */` docstring |

**VERIFY-ONLY, NO EDIT.** `40-VERIFICATION.md:586` names the todo by **bare slug with no directory
segment at all** (`todo `2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri``).
It contains no `pending/`, so it is already correct and survives the move untouched. Confirm this by
reading the line; do not "helpfully" add a path to it.

**FROZEN HISTORICAL RECORDS — DO NOT TOUCH.** These are the findings of past audit runs. Rewriting
what a past audit observed falsifies the record:

- `.planning/quick/260908-gye-todo-triage-decorations-normalize-todo-f/260908-gye-SUMMARY.md:70`
- `.planning/quick/260908-gye-todo-triage-decorations-normalize-todo-f/260908-gye-PLAN.md:119`
- `.planning/quick/260905-upz-staleness-audit-of-the-41-pending-todos-/260905-upz-AUDIT.md:596`

(All three happen to spell a bare filename or slug, not a `pending/` path — so they will not match
the final `todos/pending/2026-09-04-adtraction` grep either way. Leave them alone regardless.)

**Line numbers above were measured at plan time and may have drifted.** Re-run the discovery grep
and classify each hit by its ACTUAL text before editing. Do not assume all nine contain `pending/`.

```bash
grep -rn "2026-09-04-adtraction" --include="*.ts" --include="*.tsx" --include="*.rs" \
  --include="*.md" --include="*.py" --include="*.mjs" --include="*.cjs" . \
  | grep -v node_modules | grep -v "^./graphify-out"
```

## Facts already established — do not re-derive

- `pnpm planning-gates` = `python3 meta/runPlanningGates.py`, floor `MINIMUM_EXPECTED_GATES = 11`.
- `.planning/todos/todo-frontmatter-gate.py` is scoped to `pending/` **only** and hard-asserts that
  scope in source. `completed/` is deliberately exempt, so `severity`/`platform`/`ready` are not
  required after the move. It also fails if `pending/` globs **zero** files — `pending/` holds 27
  files today, so removing one is safe.
- No gate parses `key-files` or pins the content of `40-VERIFICATION.md` / `40-09-SUMMARY.md`.
- `.husky/pre-commit` is a no-op (its only line is commented out). There is no lint-staged config.
- The test currently passes: `npx jest <path>` → `PASS Frontend`, 10/10, ~0.2s. Jest uses a
  multi-project config (`src/backend`, `src/common`, `src/frontend`, `src/preload`, `meta`); a bare
  path filter resolves correctly against it — this was executed at plan time, not guessed.

</preflight>

<tasks>

<task type="auto">
  <name>Task 1: Move the todo to completed/ and write its closure record</name>
  <files>
    .planning/todos/pending/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md (deleted)
    .planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md (created)
  </files>
  <action>
Order matters: **move first, edit at the new path, then `git add` the new path.** `git mv` stages
the rename using HEAD's blob; edits made before the mv survive in the working tree but remain
unstaged, so the staged blob can be HEAD's content rather than yours. Moving first and adding last
removes the trap instead of working around it. The `R100` / `RM` status codes are the tell that it
armed.

1. `git mv` the file from `pending/` to `completed/`, preserving the filename exactly
   (`2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md`). Do not
   rename, re-slug, or re-date it — this project has an explicit written rule that renaming a todo
   strands its cross-references invisibly.

2. Before writing frontmatter, **inspect the real convention** rather than inventing one. Sample
   several existing closed todos, e.g.:
   `grep -h "^status:\|^closed:\|^closed_by:" .planning/todos/completed/*.md | head -30`.
   The measured distribution at plan time: `status:` is `RESOLVED` (68), `CLOSED` (24), `completed`
   (17); `closed:` appears in 14 files as a bare `YYYY-MM-DD`, `closed_by:` in 13 as a short quoted
   string naming the task or commit. Follow what you actually observe.

3. Edit the frontmatter at the new path. Change `status: pending` to a closed value and add the
   closure keys. Unless your inspection contradicts it, use exactly:

   - `status: CLOSED`
   - `closed: 2026-09-17`
   - `closed_by: "quick task 260917-u4d -- operator decision, resolution path 1 (do nothing further)"`

   Keep `closed:` / `closed_by:` adjacent, immediately after `status:`. **Keep every existing key**
   (`created`, `title`, `area`, `severity`, `platform`, `ready`, `resolves_phase`, `found_by`,
   `files`) — they are harmless in `completed/` and removing them loses information.

   **Keep the value short and on one line.** This repo has two disagreeing frontmatter parsers and a
   multi-sentence prose `status:` has previously caused a parser to invent keys out of the prose.
   The narrative belongs in the body (step 4), not in a frontmatter value.

4. Append a `## Resolution` section to the **end** of the body, after `## Impact`. **Do not delete,
   rewrite, condense, or reorder any existing section** — `## Symptom`, `## Why no equivalent
   exists (the citation)`, `## What shipped instead (Phase 40 Plan 09)`, `## Possible resolution
   paths (not attempted here)` and `## Impact` are the entire value of a closed todo. Write:

   ```
   ## Resolution

   **CLOSED 2026-09-17 — operator decision, resolution path 1 ("do nothing further").**

   The declared gap IS the shipped outcome. This is not a deferral and not a won't-fix pending
   revisit: path 1 above was chosen on its merits, because GOG's affiliate redirect chain works
   normally whenever `track.adtraction.com` is not blocked, and the residual regression is narrow —
   one specific redirect-chain failure mode has no explanatory UI.

   What Phase 40 Plan 09 shipped stays in place as the deliverable this close blesses, not as a
   placeholder awaiting replacement:

   - the gap comment and the once-per-GOG-store-visit `window.api.logInfo('[WebView] D-32 gap: ...')`
     call in `src/frontend/screens/WebView/index.tsx`
   - `src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts` (10 tests),
     which asserts that gap line's gating and content and that no adtraction Dialog or state
     remains reachable

   Paths 2 and 3 are recorded above and remain available, but nobody is carrying them. Anyone
   reopening this should open a new todo citing this one rather than reviving this file.
   ```

5. Stage the new path explicitly: `git add <new-path>`. `git mv` already staged the deletion of the
   old path; confirm rather than assume.
  </action>
  <verify>
    <automated>
# The staged blob must carry the EDITED content, not HEAD's. This is the whole point of the task.
git show :.planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md \
  | head -25 | grep -qE '^status: (CLOSED|RESOLVED|completed)$' \
  && git show :.planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md \
  | grep -q '^closed: 2026-09-17$' \
  && git show :.planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md \
  | grep -q '^## Resolution$' \
  && echo STAGED_BLOB_OK

# The staged blob must NOT still say pending.
! git show :.planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md \
  | grep -q '^status: pending$' && echo NOT_PENDING_OK

# Every original section survived the move.
for s in '## Symptom' '## Why no equivalent exists' '## What shipped instead' '## Possible resolution paths' '## Impact'; do
  git show :.planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md \
    | grep -q "$s" || echo "LOST SECTION: $s"
done

# Old path is gone from the index and the tree.
test ! -e .planning/todos/pending/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md \
  && echo OLD_PATH_GONE
    </automated>
  </verify>
  <done>
`git show :<new-path>` prints the edited content: a closed `status:`, `closed: 2026-09-17`, a
`closed_by:` naming the operator decision, all five original sections intact, and a new
`## Resolution` section. The `pending/` path no longer exists. Nothing else is staged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repoint the live citations, leave the historical records frozen</name>
  <files>
    .planning/ROADMAP.md
    .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-09-SUMMARY.md
    .planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we/40-VERIFICATION.md
    src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts
  </files>
  <action>
Re-run the discovery grep from `<preflight>` and classify **every** hit by its actual text before
editing anything. Do not pattern-match on the plan's line numbers — they were measured at plan time
and the files may have drifted.

For each hit, the edit is **the single substring `pending/` → `completed/` within that todo's path,
and nothing else.** Do not reword the surrounding sentence, do not add parentheticals, do not
"update" tense. These citations name an artifact that still exists; repointing keeps them
resolvable, and that is the entire change. Editorialising a verification record is out of scope.

1. `.planning/ROADMAP.md` — the D-32 bullet's `→ `todos/pending/...`` pointer. Note this one spells
   `todos/pending/` with **no `.planning/` prefix**; match on the text you find, not on a template.

2. `40-09-SUMMARY.md` — **two** hits: one inside the `key-files: created:` frontmatter list, one in
   the prose sentence beginning "Filed". Both are pointers to a file that still exists; both get
   repointed. Do not delete the `key-files` entry — the plan did create that file.

3. `40-VERIFICATION.md` — **one** editable hit (the `.planning/todos/pending/...md` path under
   REQ-40-08). The second hit in that file is a **bare slug with no directory segment** and needs no
   edit: read the line, confirm it contains no `pending/`, and leave it byte-identical.

4. `src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts` — the path sits on a
   single line inside the file's leading `/** */` docstring (measured at plan time as line 27, the
   line reading `` * (`.planning/todos/pending/...md`). ``). The surrounding prose wraps across
   several lines, but the path itself does not — confirm by **reading** the file, because a grep for
   a phrase inside a wrapped comment returns zero for reasons that have nothing to do with
   correctness.

   **Touch nothing but that comment line.** Not one executable line of this test changes. It is a
   source-text gate that reads `WebView/index.tsx` through `stripSourceComments`, so its own
   docstring is inert to its assertions — but it must still pass unchanged in Task 3, and a
   10/10 pass is the proof you did not disturb it.

   The edit grows the line by 2 characters (`pending` → `completed`). Prettier does not reflow block
   comment interiors, so no rewrap should occur; Task 3 checks formatting anyway.

5. **Leave the three `.planning/quick/260908-gye-*` and `.planning/quick/260905-upz-*` hits exactly
   as they are.** They record what a past audit observed. Prove you did not touch them:
   `git status --short .planning/quick/260908-gye-todo-triage-decorations-normalize-todo-f .planning/quick/260905-upz-staleness-audit-of-the-41-pending-todos-`
   must print nothing.

6. Stage the four edited files with explicit pathspecs.
  </action>
  <verify>
    <automated>
# Zero live pointers still spell pending/ for this todo. (The three historical hits spell a bare
# filename or slug and never matched this pattern, so this grep is naturally scoped -- but the
# exclusions are kept explicit so the result is readable.)
COUNT=$(grep -rn "todos/pending/2026-09-04-adtraction" \
  --include="*.ts" --include="*.tsx" --include="*.rs" --include="*.md" --include="*.py" \
  --include="*.mjs" --include="*.cjs" . 2>/dev/null \
  | grep -v node_modules | grep -v "^./graphify-out" \
  | grep -v "^./.planning/quick/260908-gye" | grep -v "^./.planning/quick/260905-upz" | wc -l)
test "$COUNT" -eq 0 && echo NO_STALE_POINTERS || { echo "STALE POINTERS: $COUNT"; exit 1; }

# The repointed pointers exist and resolve to a real file.
grep -q "todos/completed/2026-09-04-adtraction" .planning/ROADMAP.md && echo ROADMAP_OK
D=.planning/phases/40-in-app-store-and-wiki-browsing-under-tauri-embedded-child-we
test "$(grep -c "todos/completed/2026-09-04-adtraction" $D/40-09-SUMMARY.md)" -eq 2 && echo SUMMARY_OK
grep -q "todos/completed/2026-09-04-adtraction" $D/40-VERIFICATION.md && echo VERIF_OK
grep -q "todos/completed/2026-09-04-adtraction" \
  src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts && echo TEST_DOC_OK

# The bare-slug citation survived untouched (it must still be there, still with no directory).
grep -n "todo \`2026-09-04-adtraction" $D/40-VERIFICATION.md

# Historical audit artifacts are byte-identical to HEAD.
test -z "$(git status --short .planning/quick/260908-gye-todo-triage-decorations-normalize-todo-f \
  .planning/quick/260905-upz-staleness-audit-of-the-41-pending-todos-)" && echo HISTORY_FROZEN

# The test's executable half is untouched: every changed line in the test file is a comment line.
git diff --cached -U0 -- src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts \
  | grep -E '^[+-][^+-]' | grep -vE '^[+-]\s*\*' && echo "NON-COMMENT LINE CHANGED -- REVERT" || echo TEST_COMMENT_ONLY
    </automated>
  </verify>
  <done>
Zero live citations spell `todos/pending/` for this todo. ROADMAP, both `40-09-SUMMARY.md` hits,
and the one editable `40-VERIFICATION.md` hit spell `todos/completed/`. The bare-slug
`40-VERIFICATION.md` citation is unchanged. The test's diff contains comment lines only. The three
historical quick-task artifacts show no modification.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove nothing regressed, then commit path-scoped</name>
  <files>(no new edits -- verification and commit only)</files>
  <action>
Run the gates first, commit second, then re-verify against the **commit** rather than the working
tree. A gate that reads the tree can be green over a commit that is wrong — verify both ends.

1. The adtraction test, which must still be 10/10:
   `npx jest src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts`
   (Equivalently `pnpm test <path>`; `pnpm test` = `jest`. Confirm from `package.json` if in doubt.)
   A `PASS Frontend` line plus `Tests: 10 passed, 10 total` is the bar. Anything less means Task 2
   disturbed the test — revert that file's change and re-do it as a comment-only edit.

2. Formatting on the one touched source file, since a +2-character growth inside a block comment is
   the shape that could in principle trip a rewrap:
   `npx prettier --check src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts`

3. `pnpm planning-gates` — must report all gates green (floor is 11 discovered gates; a count below
   that is itself a failure). Note the todo-frontmatter gate now scans 26 files in `pending/` rather
   than 27; that is the expected consequence of the move, not a regression.

4. Commit. **Bare `git commit -m`, nothing else** — no `-a`, no `-A`, no trailing pathspec. The
   index contains exactly the intended paths from Tasks 1 and 2; also stage this plan file and your
   SUMMARY before committing:

   ```
   git add .planning/quick/260917-u4d-close-the-d-32-adtraction-gap-todo-path-
   ```

   Inspect `git diff --cached --name-status` and confirm it lists **only**:

   - `R`/`D`+`A` for the todo move (pending → completed)
   - `M .planning/ROADMAP.md`
   - `M` both phase-40 docs
   - `M src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts`
   - `A` the quick-task plan + summary

   If `2026-09-11-humble-keys-...`, `.claude/skills/archify/`, `skills-lock.json`, the spike
   captures, or `.planning/quick/260912-d84-...` appear, unstage them before committing.

   Commit message:

   ```
   docs(quick-260917-u4d): close the D-32 adtraction todo as resolution path 1 and repoint its citations

   Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
   ```

5. Re-verify against the commit, not the tree: `git show --stat HEAD` must list the expected paths
   and no others, and `git show HEAD:<new-todo-path> | head -25` must show the closed `status:`.
  </action>
  <verify>
    <automated>
npx jest src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts 2>&1 \
  | tee /tmp/u4d-jest.txt | tail -8
grep -q "Tests:       10 passed, 10 total" /tmp/u4d-jest.txt && echo JEST_10_OK || { echo JEST_REGRESSED; exit 1; }

npx prettier --check src/frontend/screens/WebView/__tests__/WebViewAdtractionGapDeclared.test.ts && echo PRETTIER_OK

pnpm planning-gates && echo GATES_OK

# Post-commit: the COMMIT, not the tree.
git show --stat HEAD
git show HEAD:.planning/todos/completed/2026-09-04-adtraction-ad-block-detection-has-no-derivable-signal-under-tauri.md \
  | head -25 | grep -qE '^status: (CLOSED|RESOLVED|completed)$' && echo COMMITTED_BLOB_OK

# No pre-existing dirty entry was swept in.
git show --stat HEAD | grep -E "humble-keys|skills-lock|archify|spikes/024|260912-d84" \
  && { echo "SWEPT UNRELATED WORK -- amend"; exit 1; } || echo NO_UNRELATED_FILES

# The pre-existing dirty entries are still dirty/untracked, i.e. untouched.
git status --short | grep -q "humble-keys-title-wrap" && echo PREEXISTING_PRESERVED
    </automated>
  </verify>
  <done>
Test 10/10 green, prettier clean, `pnpm planning-gates` green. One commit exists on `main` whose
`--stat` lists only the todo move, ROADMAP, the two phase-40 docs, the test file, and the quick-task
artifacts. `git show HEAD:<new-path>` proves the committed blob carries the closed frontmatter. The
five pre-existing dirty/untracked entries remain dirty/untracked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → commit | Unrelated in-flight operator work shares this tree; the commit boundary is where it can leak |
| planning record → future reader | A repointed citation is a claim about where an artifact lives; a rewritten historical record is a falsified claim about what was observed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u4d-01 | Tampering | `git mv` + edit of the todo | mitigate | Move first, edit at the new path, `git add` last; assert the **staged** blob shows the edited `status:` before committing (Task 1 verify) |
| T-u4d-02 | Tampering | Pre-existing dirty/untracked tree entries | mitigate | Explicit pathspecs only; bare `git commit` over an index confirmed empty at preflight; post-commit `--stat` grep rejects the five known entries by name |
| T-u4d-03 | Repudiation | `260908-gye` / `260905-upz` audit artifacts | mitigate | Named explicitly as frozen; `git status --short` over both dirs must print nothing (Task 2 verify) |
| T-u4d-04 | Tampering | `WebViewAdtractionGapDeclared.test.ts` | mitigate | Comment-line-only diff asserted via `git diff --cached -U0` filter; 10/10 jest pass; prettier `--check` |
| T-u4d-05 | Information disclosure | — | accept | No credentials, network, packages, or user data touched; docs-only change |
| T-u4d-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no dependency surface changes |

No package-manager install tasks exist, so the Package Legitimacy Gate does not arm.
</threat_model>

<verification>
Composite check after Task 3 — every line must hold simultaneously:

1. `.planning/todos/completed/2026-09-04-adtraction-...-under-tauri.md` exists; the `pending/` path
   does not.
2. Its frontmatter carries a closed `status:` plus `closed:`/`closed_by:` in this repo's existing
   convention, and retains every original key.
3. Its body retains all five original sections and gains `## Resolution` naming path 1, the
   operator, and 2026-09-17.
4. `grep -rn "todos/pending/2026-09-04-adtraction"` over live paths → **zero**, with the test file's
   docstring line confirmed **by reading**, not by trusting the zero.
5. The `40-VERIFICATION.md` bare-slug citation is unchanged.
6. The three historical quick-task hits are byte-identical to HEAD.
7. `WebViewAdtractionGapDeclared.test.ts`: 10/10 pass, comment-only diff, prettier clean.
8. `pnpm planning-gates` green.
9. `git show --stat HEAD` lists only the intended paths; the five pre-existing dirty/untracked
   entries survive untouched.
10. `src/frontend/screens/WebView/index.tsx` is **not** in the commit — no production file changed.
</verification>

<success_criteria>
The D-32 adtraction todo is closed as resolution path 1 with its reasoning recorded, every live
citation resolves to its new home, the shipped gap comment / `logInfo` call / test are untouched,
the historical audit record is intact, and one path-scoped commit on `main` carries exactly that and
nothing else.
</success_criteria>

<output>
Create `.planning/quick/260917-u4d-close-the-d-32-adtraction-gap-todo-path-/260917-u4d-SUMMARY.md` when done.

The SUMMARY must state, as measured facts and not as intentions:
- the nine grep hits and how each was classified (repointed / verify-only / frozen)
- the exact frontmatter keys written, and the convention sample they were derived from
- the jest result line, the prettier result, and the planning-gates result
- the `git show --stat HEAD` file list
</output>
