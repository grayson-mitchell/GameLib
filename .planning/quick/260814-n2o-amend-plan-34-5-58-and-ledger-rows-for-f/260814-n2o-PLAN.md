---
phase: quick/260814-n2o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
  - .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-58-PLAN.md
autonomous: true
requirements: [QT-260814-n2o-01]

must_haves:
  truths:
    - "`U-34.5-01`'s cell no longer asserts condition (4) FAILED at gate 3 as its final word — the correction that it was UNOBSERVABLE BY CONSTRUCTION until `e76820d8d` sits beneath the preserved gate-3 record"
    - "`U-34.5-10`'s cell records that its log-side cross-check was impossible for the same reason, and that the pilot's 0-prompts-on-re-login is not evidence the 45s/120s fix works"
    - "Both rows are still **OPEN** — nothing in this amendment retires either one"
    - "Plan 34.5-58's condition-(4) grep can distinguish an attempt from a success from a cache hit"
    - "Plan 34.5-58's operator script drives launch and sign-out as the prompt-bearing moments, requires DEBUG lines to be present, and requires per-prompt item-name transcription"
    - "Plan 34.5-58's frontmatter, `<binding_decision>` and `<non_blocking_statement>` are byte-identical to HEAD"
    - "No file outside the two named `.md` files is modified"
  artifacts:
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md"
      provides: "Both target rows corrected against the post-`F-34.5-G6-26` bar, both still OPEN"
      contains: "F-34.5-G6-26"
    - path: ".planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-58-PLAN.md"
      provides: "Post-`e76820d8d` log literals, a three-way condition-(4) grep, and a corrected operator script"
      contains: "issuing keyring_get (may prompt)"
  key_links:
    - from: "34.5-58-PLAN.md Task 1 / Task 3"
      to: "`U-34.5-01` condition (4)"
      via: "a grep for `keyring_get ok present=` — the only line that can satisfy it"
      pattern: "keyring_get ok present="
    - from: "34.5-58-PLAN.md operator script"
      to: "`U-34.5-10`'s per-slot prompt count"
      via: "the operator's per-prompt item name cross-checked against the log's per-slot `issuing` count"
      pattern: "issuing keyring_get \\(may prompt\\)"
    - from: "both amended ledger rows"
      to: "the preserved 2026-08-14 pilot"
      via: "a path reference to the capture directory"
      pattern: "keyring-session-capture"
---

<objective>
Plan `34.5-58-PLAN.md` was authored 2026-08-13. `F-34.5-G6-26` landed 2026-08-14 (commit
`e76820d8d`) and changed what both of that plan's target rows can be measured with. The correction
was written into `deferred-items.md` item 34 only — `grep -c "F-34.5-G6-26" 34.5-UNTESTED-ITEMS.md`
returns **0** — so the two ledger rows that plan 34.5-58's Task 1 reads as its source of truth still
carry the pre-fix claim.

Plan 34.5-58 is an operator-present live session the developer will physically sit through. It must
not be spent measuring against a stale bar. It has NOT been executed (no `34.5-58-SUMMARY.md`
exists), so amending it in place is correct.

Purpose: bring the bar current before the session runs, once, rather than discovering it mid-session.
Output: two edited `.md` files. No source, test, or Rust change of any kind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-58-PLAN.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md
@.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/keyring-session-capture/README.md

**Scope lock — read before editing:**
- The ONLY two files you may modify are the two in `files_modified`. Nothing else. Not
  `34.5-59-PLAN.md`, not `34.5-60-PLAN.md`, not `deferred-items.md`, not `STATE.md`, and no file
  under `src/`, `src-tauri/`, or any `__tests__/` directory.
- `deferred-items.md` **item 34** is the SOURCE OF TRUTH for the correction wording. Mirror its
  phrasing; do not re-derive it and do not edit it.
- `keyring-session-capture/README.md` is the preserved 2026-08-14 PILOT session. Cite it; do not
  edit it.
- D-CYCLE7-C is **LOCKED**. This amendment does not reopen it. `blocking: false`,
  `affects_gate: false`, the `<binding_decision>` block and the entire `<non_blocking_statement>`
  block stay UNTOUCHED.
- Plan 34.5-58's frontmatter (`files_modified`, `wave`, `depends_on`, `autonomous`, `targets`,
  `must_haves`) stays byte-identical to HEAD. Task 3 gates this with a diff.
- Its `<threat_model>` and `<success_criteria>` also stay as they are. Nothing below requires
  changing them: the new log lines carry slot name, presence and length only, never a value, so
  `T-34.5-C7-27`'s existing mitigation already covers them.
- **No secret, token, cookie, session id or account id value may be introduced into either file.**
  Every log literal below is a SHAPE with `<slot>` / `<bool>` / `<n>` / `<err>` placeholders. Keep
  them as shapes.
- Do NOT commit. The orchestrator handles the commit and the STATE.md quick-task row.

<interfaces>
Verified at planning time — use these verbatim, do not re-derive.

`src/common/types/sidecarTransport.ts` — `RUST_KEYRING_GET` = `keyring_get` (:146),
`RUST_KEYRING_SET` = `keyring_set` (:149), `RUST_KEYRING_DELETE` = `keyring_delete` (:152). Every
literal below is the resolved string, not the constant name.

`src/backend/sidecar/keyringTokenStore.ts`, post-`e76820d8d`, all slot-attributed as
`SidecarKeyringSlotStore(<slot>).<method>(): ...`:

| Line | Level | Literal shape | What it means |
|---|---|---|---|
| :254 | INFO | `SidecarKeyringSlotStore(<slot>).getToken(): issuing keyring_get (may prompt)` | Emitted before every REAL Keychain round trip. The ONLY read call that can raise a prompt. |
| :296 | INFO | `SidecarKeyringSlotStore(<slot>).getToken(): keyring_get ok present=<bool> len=<n>` | Read SUCCEEDED. |
| :261 | WARN | `SidecarKeyringSlotStore(<slot>).getToken(): keyring_get failed: <err>` | Read failed. |
| :277 | WARN | `keyring failure memoized slot=<slot> class=<...> ms=120000` | Memo CREATED. |
| :318 | INFO | `SidecarKeyringSlotStore(<slot>).setToken(): keyring_set ok len=<n>` | Write round trip. |
| :344 | INFO | `SidecarKeyringSlotStore(<slot>).clearToken(): keyring_delete ok` | Sign-out delete round trip. |
| :214 | DEBUG | `SidecarKeyringSlotStore(<slot>).getToken(): served from cache, no keyring_get issued` | Warm cache — no round trip, no prompt possible. |
| :221 | DEBUG | `SidecarKeyringSlotStore(<slot>).getToken(): joined in-flight read, no additional keyring_get issued` | Deduped — no round trip, no prompt possible. |

**The memo HIT path (`keyringTokenStore.ts:232-237`) returns early with NO log line of any kind.**
A prompt suppressed by an already-active memo is therefore provable only by the ABSENCE of an
`issuing` line, never by a positive line. This is a known limit of the cross-check.

DEBUG suppression mechanism, confirmed: `src/backend/logger/log_writer.ts` has no level filter —
DEBUG lines are written unless logging is disabled wholesale (`disableLogs` in `GlobalConfig`,
`src/backend/logger/index.ts:101-110`). The reliable check is therefore EMPIRICAL, not
settings-inspection: `grep -c "\[DEBUG\]" <archived log>` must be non-zero for the session.

Baselines measured against the current files (so every gate below is non-vacuous):
- `34.5-UNTESTED-ITEMS.md`: `F-34.5-G6-26` = 0, `keyring-session-capture` = 0, row count
  (`grep -oE '^\| U-34\.5-[0-9]+' | wc -l`) = **33**, `U-34.5-01` and `U-34.5-10` each contain
  `**OPEN**` exactly once and each occupy exactly one line.
- `34.5-58-PLAN.md`: `issuing keyring_get (may prompt)` = 0, `keyring_get ok present=` = 0,
  `served from cache` = 0, `joined in-flight read` = 0, `In dark wake, no UI possible` = 8,
  `affects_gate: false` = 4, and the fixed string `SidecarKeyringSlotStore\|keyring_get` = **1**
  (this is the stale grep Task 3 must remove; the `== 0` gate is RED-proven by that current 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct both ledger rows against the post-F-34.5-G6-26 bar, keeping both OPEN</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md</files>
  <read_first>
    - `.planning/phases/.../deferred-items.md` lines 478-522 (item 34 — the source of truth for the
      correction wording)
    - `.planning/phases/.../keyring-session-capture/README.md` (the pilot)
    - `.planning/phases/.../34.5-UNTESTED-ITEMS.md` — the `U-34.5-01` row, the `U-34.5-10` row, the
      "Interaction with plan 34.5-35" section, and the `## Rules` block
  </read_first>
  <action>
Append a dated **2026-08-14** update to the Status cell of `U-34.5-01` and, SEPARATELY, to the
Status cell of `U-34.5-10`. Per this file's own **Ledger Rule 1**, a row moves only on the
observation named in its own cell — these two do NOT retire together and neither update may be
phrased as covering the other. **BOTH ROWS STAY OPEN.** `F-34.5-G6-26`'s fix makes them measurable;
it does not measure them. Do not change either row's status away from `**OPEN**`, and do not add a
`RETIRED` disposition to either.

Each row is a single markdown table line. Append to the END of the existing Status cell, inside the
same line and the same cell — do not restructure the table, do not split a row across lines, and do
not reorder rows.

**`U-34.5-01` — append, after the preserved gate-3 record:**

Open with a bolded `**UPDATE 2026-08-14 (`F-34.5-G6-26`, commit `e76820d8d`)**` marker. Then:
- State plainly that the gate-3 record immediately above is **left in place as the accurate record
  of what was written then** and is corrected here to a weaker, true claim. **DO NOT DELETE the
  gate-3 sentence** ("zero `SidecarKeyringSlotStore` reads succeeded; both attempts ended
  `keyring_get failed: keyring:timeout`") — the correction sits beneath it.
- The correction: condition (4) was **UNOBSERVABLE BY CONSTRUCTION** until `e76820d8d`.
  `src/backend/sidecar/keyringTokenStore.ts` imported **only** `logWarning`, so the string
  `SidecarKeyringSlotStore` could reach `gamelib.log` **only on a failure path**, and a successful
  read emitted **nothing at all**. A grep for condition (4) could never match on success.
- Mirror item 34's own phrasing for the reframing: the condition was recorded as never held; **"it
  was never observable, which is not the same claim."**
- Name what makes it measurable now: the INFO line
  `SidecarKeyringSlotStore(<slot>).getToken(): keyring_get ok present=<bool> len=<n>`. Note that
  `present=false` is still a SUCCESSFUL read (the healthy no-entry case) and satisfies condition (4)
  — the condition is about the read completing, not about a secret being found.
- Cite the preserved pilot at `keyring-session-capture/` (2026-08-14): conditions (1)(2)(3) held,
  and (4) had **no evidence either way** — not a failure, an absence.
- Close with: this pilot is **NOT plan 34.5-58's run** — it predates the session record 34.5-58
  Task 1 must author and was not captured under the dual-sink standard — and **this row stays
  OPEN.**

**`U-34.5-10` — append, its own separate update with the same dated marker:**

- This row's bar has a log-side half — "cross-checked against the log's own read attempts" — that
  was **impossible for the same reason**: the log recorded no successful attempts, so the
  cross-check could not be performed **even with perfect operator notes**.
- The 2026-08-14 pilot's **"0 prompts on re-login" is NOT evidence the 45s-timeout / 120s-memo fix
  works.** `getToken()` short-circuits on `this.cachedToken` before issuing any `keyring_get`, so a
  warm in-memory cache raises no prompt **by an entirely different mechanism**. The DEBUG line
  `served from cache, no keyring_get issued` exists precisely so a future run cannot conflate the
  two.
- Note that the pilot's counts (2 prompts at launch, 2 at sign-out, 0 on re-login) were
  **unattributable and therefore unscorable** because item names were not recorded: 2 slots x 1
  prompt (which would SATISFY this row) and 1 slot x 2 prompts (which would FAIL it) are
  indistinguishable without them.
- Cite `keyring-session-capture/`, state the pilot is NOT plan 34.5-58's run for the same two
  reasons, and state that **this row stays OPEN.**
- Do not restate `U-34.5-01`'s outcome as if it carried this row, and do not let this row's update
  be read as evidence for that one.

**Then add ONE dated header paragraph** at the top of the file, in the same style as the existing
`**Update 2026-08-14 (plan 34.5-61) — 33 rows, ...**` paragraph and immediately above it. It must
state: 33 rows, **no row added, no row retired, two rows annotated** (`U-34.5-01`, `U-34.5-10`);
that `F-34.5-G6-26` (commit `e76820d8d`) corrected what both rows can be measured with; that
`deferred-items.md` item 34 holds the full record; and that the annotation retires nothing.
**Re-derive the row count with the file's own grep** — `grep -oE '^\| U-34\.5-[0-9]+' | wc -l` —
and paste the number you got. Do not copy 33 forward on trust.

Introduce no token, cookie, session id or account id value. Reference the pilot by directory path
only; do not quote log content beyond the placeholder shapes given in `<interfaces>`.
  </action>
  <verify>
    <automated>L=.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-UNTESTED-ITEMS.md; test "$(grep -oE '^\| U-34\.5-[0-9]+' $L | wc -l | tr -d ' ')" = 33 && test "$(grep -E '^\| U-34\.5-(01|10) \|' $L | grep -cF '**OPEN**')" = 2 && test "$(grep -E '^\| U-34\.5-(01|10) \|' $L | grep -cF 'RETIRED 2026-08-14')" = 0 && test "$(grep -E '^\| U-34\.5-01 \|' $L | grep -cF 'F-34.5-G6-26')" = 1 && test "$(grep -E '^\| U-34\.5-10 \|' $L | grep -cF 'F-34.5-G6-26')" = 1 && test "$(grep -E '^\| U-34\.5-01 \|' $L | grep -cF 'keyring-session-capture')" = 1 && test "$(grep -E '^\| U-34\.5-10 \|' $L | grep -cF 'keyring-session-capture')" = 1 && test "$(grep -E '^\| U-34\.5-01 \|' $L | grep -cF 'keyring_get failed: keyring:timeout')" = 1 && echo LEDGER_OK</automated>
  </verify>
  <acceptance_criteria>
    - The automated gate prints `LEDGER_OK`.
    - Row count is still 33 — no row added, none removed.
    - Both rows still carry `**OPEN**`; neither carries a 2026-08-14 retirement.
    - `U-34.5-01`'s gate-3 `keyring:timeout` sentence is still present (the gate proves it was not
      deleted), with the correction appended beneath it rather than replacing it.
    - Each row's update is self-contained: neither cites the other's evidence as its own.
    - `U-34.5-10`'s update contains both the log-side-half-was-impossible point and the
      warm-cache/`cachedToken` point.
    - The header paragraph states a re-derived row count, not a copied one.
    - No secret, token, cookie, session id or account id value appears anywhere in the diff.
  </acceptance_criteria>
  <done>Plan 34.5-58's own source of truth now describes the bar as it is after `e76820d8d`, and neither row has moved.</done>
</task>

<task type="auto">
  <name>Task 2: Add the post-e76820d8d log literals to plan 34.5-58's interfaces block</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-58-PLAN.md</files>
  <read_first>
    - `.planning/phases/.../34.5-58-PLAN.md` `<context>` / `<interfaces>` block (lines 79-99)
    - This plan's own `<interfaces>` block above — the eight literals, their levels and their line
      numbers are already verified verbatim; do not re-derive them from source
  </read_first>
  <action>
Extend the EXISTING `<interfaces>` block inside plan 34.5-58's `<context>` section. Keep every
bullet already there (the two bootstrap arms, the `devSecretVault.ts` exact-string rule, the
`keyring:unavailable` / `keyring:timeout` Rust split, the 34.5-35 constants, the real-slot list).
Append a new subsection headed to make clear it is the post-`F-34.5-G6-26` observability landed by
commit `e76820d8d` on 2026-08-14, AFTER this plan was first authored.

List all eight literals from this plan's `<interfaces>` table, each with its level (INFO / WARN /
DEBUG), its `keyringTokenStore.ts` line number, and its shape with `<slot>` / `<bool>` / `<n>` /
`<err>` placeholders left as placeholders. State alongside them:
- `RUST_KEYRING_GET` / `RUST_KEYRING_SET` / `RUST_KEYRING_DELETE` resolve to the literal strings
  `keyring_get` / `keyring_set` / `keyring_delete` (`src/common/types/sidecarTransport.ts:146/149/152`),
  so the literals above are what actually appears in `gamelib.log`.
- Every line is **slot-attributed** in the form `SidecarKeyringSlotStore(<slot>).<method>(): ...`.
- The `:254` `issuing keyring_get (may prompt)` line is emitted before **every real Keychain round
  trip** and is the **only** read call that can raise a prompt — so the count of these lines per
  slot is the **log-side counterpart to the operator's observed prompt count**, which is exactly
  what `U-34.5-10`'s bar asks to cross-check against.
- The `:318` `keyring_set ok` and `:344` `keyring_delete ok` lines are also real round trips and
  belong in the same prompt accounting — sign-out is a delete.
- **Known limit, recorded now rather than discovered mid-session:** the memo HIT path
  (`keyringTokenStore.ts:232-237`) returns early with **NO log line of any kind**. A prompt
  suppressed by an already-active memo is provable only by the **ABSENCE** of an `issuing` line,
  never by a positive line. Do not soften this — an operator who expects a "memo hit" line will
  misread its absence as a missing round trip.
- None of these lines carries a secret: slot name, presence and length only.

Change nothing else in this file in this task. In particular do not touch the frontmatter, the
`<binding_decision>` block, the `<non_blocking_statement>` block, the `<threat_model>`, or the
`<success_criteria>`.
  </action>
  <verify>
    <automated>P=.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-58-PLAN.md; for s in 'issuing keyring_get (may prompt)' 'keyring_get ok present=' 'keyring failure memoized slot=' 'keyring_set ok len=' 'keyring_delete ok' 'served from cache' 'joined in-flight read' 'e76820d8d'; do test "$(grep -cF "$s" $P)" -ge 1 || { echo "MISSING: $s"; exit 1; }; done; test "$(grep -cF '232-237' $P)" -ge 1 && echo INTERFACES_OK</automated>
  </verify>
  <acceptance_criteria>
    - The automated gate prints `INTERFACES_OK`.
    - All eight literals appear with their level and their `keyringTokenStore.ts` line number.
    - The memo-hit-emits-nothing limit is stated explicitly, with the `232-237` line reference.
    - Placeholders stay placeholders — no real slot value, token, length or account id.
    - Every pre-existing bullet in the block is still present.
  </acceptance_criteria>
  <done>The executor of plan 34.5-58 receives the log contract in the plan itself and does not have to discover it from source mid-session.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the condition-(4) grep and correct the operator script in plan 34.5-58</name>
  <files>.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-58-PLAN.md</files>
  <read_first>
    - `.planning/phases/.../34.5-58-PLAN.md` Task 1 (lines 103-190), Task 2 (192-238), Task 3 (240-301)
  </read_first>
  <action>
Three edits to plan 34.5-58's task bodies. Nothing outside them.

**(a) The condition-(4) grep — in BOTH Task 1's bar and Task 3's scoring list.**

The plan currently pins `grep -n "SidecarKeyringSlotStore\|keyring_get"` (Task 3's condition-(4)
bullet). That single grep **cannot distinguish an attempt from a success from a cache hit** — it
matches all three. Remove it and require **THREE separate greps**, each recorded with its raw
output **including the empty case**:
  1. `issuing keyring_get (may prompt)` — the ATTEMPT count, recorded **per slot**.
  2. `keyring_get ok present=` — the SUCCESS. **This and only this satisfies condition (4)**, and
     only for a real slot (`steam-refresh-token`, `humble-session`, `humble-csrf`).
  3. `served from cache` and `joined in-flight read` — calls that **could not have prompted**.

State explicitly, in both places: **`present=false` is still a SUCCESSFUL read** (the healthy
no-entry case) and **DOES satisfy condition (4)** — the condition is about the read completing, not
about a secret being found. Also carry forward the memo-hit limit from Task 2's interfaces
subsection: an absent `issuing` line, not a positive line, is the only evidence of a memo-suppressed
prompt.

Keep Task 3's other recorded greps exactly as they are — the two `[bootstrap] secret stores:`
counts, the `[dev-secret-vault]` count, the per-slot `keyring_get failed: keyring:timeout` count,
and the `In dark wake, no UI possible` count.

**(b) Task 1's precondition list — add DEBUG logging, keep the list contiguous.**

Insert a new precondition **immediately after the current precondition 2** (the
`GAMELIB_DEV_SECRET_VAULT` check), making it the new **3**, and renumber the existing 3-7 to **4-8**.
Its content: **DEBUG logging must be ENABLED for the session.** The two disambiguating lines
(`served from cache`, `joined in-flight read`) are DEBUG and settings-dependent; without them a
0-prompt observation is ambiguous again — which is the exact confusion `F-34.5-G6-26` was fixed to
remove. Specify the check as **empirical, not settings-inspection**: confirm logging is not disabled
wholesale (`disableLogs` off), then confirm `grep -c "\[DEBUG\]" <archived log>` is **non-zero** for
this session's log. If it is zero, the DEBUG lines are unavailable and any 0-prompt observation must
be recorded as **AMBIGUOUS**, not as a pass.

Because the list renumbers, **update every cross-reference to a precondition number in this file**
so none goes stale. The known ones: Task 1's acceptance criterion "Preconditions 1-7 are present and
numbered; precondition 3 contains the literal `dark wake`" (the dark-wake precondition becomes 4,
and the list becomes 1-8), Task 2's action "executes preconditions 1-7 in order", and Task 2's
how-to-verify step 4 "per the record's precondition 6". Grep the file for `precondition` and fix
each hit — do not fix only the three named here.

**(c) Task 1's actions and Task 2's how-to-verify — re-scope to the prompt-bearing moments.**

The plan currently prescribes "ONE real login and ONE real logout". The 2026-08-14 pilot observed
**2 prompts at LAUNCH, 2 at SIGN-OUT, and 0 on signing back in**. Re-scope so the moments actually
driven and counted are the ones that raise prompts:
  - **App launch, cold, with tokens already stored** — a counted, prompt-bearing moment.
  - **Sign-out** — a counted, prompt-bearing moment (it issues `keyring_delete`).
  - **Re-login — RETAINED as a step, but reframed**: it is the case that must be **SHOWN to be
    cache-served**, via the `served from cache, no keyring_get issued` DEBUG line, rather than read
    as the timeout/memo fix working. Cite the reason inline: `getToken()` short-circuits on
    `this.cachedToken` before issuing any `keyring_get`, so a warm cache raises no prompt by an
    entirely different mechanism.
  - **Stop implying the login is where prompts are expected.** Remove or rewrite any wording that
    frames a login prompt as the expected observation.

Make **per-prompt item-name transcription MANDATORY** in Task 2's how-to-verify, and state the
reason inline where the operator will read it: the pilot's counts were unattributable and therefore
unscorable **precisely because item names were not written down** — 2 slots x 1 prompt (which
SATISFIES `U-34.5-10`) and 1 slot x 2 prompts (which FAILS it) were indistinguishable. Now that the
log is slot-attributed, require **BOTH** the operator's per-prompt item name **AND** the log's
per-slot `issuing keyring_get (may prompt)` count, cross-checked against each other, with any
mismatch recorded rather than reconciled by judgement.

**Preserve, unchanged:** the three-outcome failure-class section (`keyring:timeout` /
`keyring:unavailable ... In dark wake, no UI possible` / a successful read) and its **ABORT**
semantics for the dark-wake class. You are ADDING observability detail, not replacing the failure
taxonomy. Likewise leave `blocking: false`, `affects_gate: false`, the `<binding_decision>` block,
the whole `<non_blocking_statement>` block, the frontmatter, the `<threat_model>` and the
`<success_criteria>` exactly as they are.
  </action>
  <verify>
    <automated>P=.planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc/34.5-58-PLAN.md; test "$(grep -cF 'SidecarKeyringSlotStore\|keyring_get' $P)" = 0 && test "$(grep -cF 'issuing keyring_get (may prompt)' $P)" -ge 3 && test "$(grep -cF 'keyring_get ok present=' $P)" -ge 3 && test "$(grep -cF 'served from cache' $P)" -ge 3 && test "$(grep -cF 'present=false' $P)" -ge 2 && test "$(grep -cF '[DEBUG]' $P)" -ge 1 && test "$(grep -cF 'In dark wake, no UI possible' $P)" -ge 8 && test "$(grep -cF 'affects_gate: false' $P)" -ge 4 && test -z "$(diff <(git show HEAD:$P | awk 'f&&/^---$/{exit} /^---$/{f=1} f') <(awk 'f&&/^---$/{exit} /^---$/{f=1} f' $P))" && test -z "$(diff <(git show HEAD:$P | awk '/<non_blocking_statement>/,/<\/non_blocking_statement>/') <(awk '/<non_blocking_statement>/,/<\/non_blocking_statement>/' $P))" && test -z "$(git status --porcelain -- src src-tauri)" && test "$(git status --porcelain -- .planning/phases/34.5-tauri-ipc-re-plumb-slice-8-non-steam-runners-wine-and-shortc | grep -c '34.5-59-PLAN.md\|34.5-60-PLAN.md')" = 0 && echo PLAN_AMEND_OK</automated>
  </verify>
  <acceptance_criteria>
    - The automated gate prints `PLAN_AMEND_OK`. Note the first clause is RED-proven: the stale
      `SidecarKeyringSlotStore\|keyring_get` grep is present exactly once in the current file, so
      that gate FAILS against the pre-edit specimen and can only pass once it is removed.
    - Frontmatter and `<non_blocking_statement>` are byte-identical to HEAD (diff gates, both empty).
    - Preconditions run 1-8, contiguous, with DEBUG logging as the new 3 and the dark-wake
      precondition as 4; `grep -n precondition` shows no reference to a number that no longer exists.
    - Task 1's bar and Task 3's scoring list both require the three separate greps, each with raw
      output including the empty case.
    - `present=false` is stated as a PASS for condition (4) in both places.
    - Launch and sign-out are the counted prompt-bearing moments; re-login is retained and framed as
      the cache-served case.
    - Per-prompt item-name transcription is mandatory, with the 2x1-vs-1x2 reason stated inline.
    - The three-outcome failure-class section and its dark-wake ABORT semantics are intact.
    - `git status --porcelain` shows exactly the two files in `files_modified` and nothing else.
  </acceptance_criteria>
  <done>The operator-present session plan measures against the post-`e76820d8d` bar, and no locked decision or gate arithmetic has been touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preserved pilot log -> committed planning doc | The pilot capture is real session evidence; quoting it into a tracked `.md` is the disclosure path |
| a locked decision (D-CYCLE7-C) -> an amendment touching the same file | An edit that widens scope silently reopens a decision the operator closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QT-n2o-01 | Information disclosure | log literals copied into `34.5-58-PLAN.md` / `34.5-UNTESTED-ITEMS.md` | mitigate | Only placeholder SHAPES (`<slot>`, `<bool>`, `<n>`, `<err>`) are written; the pilot is cited by directory path, never by quoted content. The shipped lines carry slot name, presence and length only — never a value — so no real secret exists to leak through them. |
| T-QT-n2o-02 | Tampering | D-CYCLE7-C's locked `blocking: false` / `affects_gate: false` / `<non_blocking_statement>` | mitigate | Task 3's gate diffs the frontmatter and the whole `<non_blocking_statement>` block against `HEAD` and requires both empty, plus `affects_gate: false` count >= 4. An amendment that widens scope fails the gate. |
| T-QT-n2o-03 | Tampering | files outside the two-file scope (`34.5-59`/`-60`, `src/`, `src-tauri/`) | mitigate | Task 3's gate asserts `git status --porcelain -- src src-tauri` is empty and that neither sibling plan appears in the phase directory's status. |
| T-QT-n2o-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package and runs no package manager. If that changes, the Package Legitimacy Gate applies and a blocking human checkpoint precedes the install. |
</threat_model>

<verification>
- `grep -c "F-34.5-G6-26" 34.5-UNTESTED-ITEMS.md` goes 0 -> >= 2, one hit inside each target row.
- Both target rows still contain `**OPEN**`; ledger row count still 33.
- `34.5-58-PLAN.md` no longer contains the ambiguous `SidecarKeyringSlotStore\|keyring_get` grep.
- Frontmatter and `<non_blocking_statement>` diff clean against `HEAD`.
- `git status --porcelain` lists exactly the two files in `files_modified`.
</verification>

<success_criteria>
- Plan 34.5-58's operator-present session, when it runs, measures condition (4) against a grep that
  can actually match a success, and distinguishes a suppressed prompt from a cache-served call.
- The ledger no longer carries the stronger "condition (4) FAILED" claim as its final word, while
  keeping the gate-3 record intact as history.
- Both rows stay OPEN — this amendment retires nothing and measures nothing.
- D-CYCLE7-C, the gate's 4/0/0/0 arithmetic, and the dark-wake failure taxonomy are untouched.
</success_criteria>

<output>
Create `.planning/quick/260814-n2o-amend-plan-34-5-58-and-ledger-rows-for-f/260814-n2o-SUMMARY.md` when done.
</output>
