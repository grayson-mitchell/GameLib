---
phase: quick-260902-9wt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md
  - .planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md
  - .planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md
  - .planning/STATE.md
autonomous: true
requirements: ["QUICK-260902-9wt"]

must_haves:
  truths:
    - "The 2026-08-06 i18n-approach todo lives in .planning/todos/completed/ with status CLOSED, closed 2026-09-02, closed_by quick task 260902-9wt"
    - "Its closure section names each of its five decisions against a concrete artifact that exists on disk today"
    - "Its closure section states in so many words that what remains is COVERAGE, not a decision — the closure is explicitly NOT total"
    - "Its closure section hands that residue to 2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md by filename"
    - "The 2026-08-28 todo is STILL in .planning/todos/pending/ with status: pending — not moved, not closed"
    - "The 2026-08-28 todo's title and prose carry the corrected counts (210 total / 204 translatable / 80 missing per locale) and the widened 46-locale scope"
    - "The 2026-08-28 todo still carries its gate-blindness diagnosis, its HTTP 401 detail, its count-the-keys warning and its do-not-hand-translate warning"
    - "The commit contains our STATE.md row and does NOT contain the concurrent session's Phase 40 Roadmap Evolution line"
    - "The commit touches zero paths under src/, meta/, public/ and does not touch .planning/ROADMAP.md"
    - "The working tree still holds the concurrent session's uncommitted ROADMAP.md and STATE.md changes after the commit"
  artifacts:
    - path: ".planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md"
      provides: "Closed pre-planning-decision todo with a non-total closure record"
      contains: "## CLOSURE RECORD — 2026-09-02, quick task 260902-9wt"
    - path: ".planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md"
      provides: "Widened, still-pending owner of the unrun machine-fill coverage residue"
      contains: "46 locales"
    - path: ".planning/STATE.md"
      provides: "Quick Tasks Completed row for 260902-9wt"
      contains: "260902-9wt"
  key_links:
    - from: ".planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md"
      to: ".planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md"
      via: "closure section hands the coverage residue over by filename"
      pattern: "2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s\\.md"
    - from: ".planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md"
      to: ".planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md"
      via: "inherited-scope section names decision 2 of the now-closed todo as the origin of the 46-locale obligation"
      pattern: "2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe\\.md"
---

<objective>
Close the 2026-08-06 Phase-34.8 i18n approach todo on shipped evidence, and move its
unexecuted residue — machine-fill coverage — onto the 2026-08-28 todo, which is widened in
place from a 2-locale/5-key item into the 48-locale/80-key item it actually is.

Purpose: the 2026-08-06 todo is a PRE-PLANNING DECISION item ("needs a decided approach …
before planning starts"). Planning happened and all five decisions shipped, so the decision
question is settled and the todo is stale as an open item. But the LLM fill was never RUN
beyond de/fr, and 46 locales have no `gamelib.json` at all — that residue is currently owned
by nobody. Closing the 2026-08-06 todo without re-homing that residue would delete the only
record that the obligation exists.

Output: one todo moved to `completed/` with a deliberately non-total closure record; one todo
rewritten in place and left PENDING; one STATE.md Quick Tasks row.

RECORDS ONLY. No code, no catalog edits, no machine-fill run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md
@.planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md
@.planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md

<facts_already_measured>
Measured 2026-09-02 during planning. Do NOT re-derive these, and do NOT run any command that
writes a catalog. The only re-measurement allowed anywhere in this plan is the read-only
locale census in Task 2's verify block (`ls public/locales`).

Phase 34.8 shipped:
- `.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/`
  holds plans `34.8-01`..`34.8-12` with SUMMARYs, plus the `08a`/`08b`/`08c` split
  (each with its own `-CLOSURE.md`), an `-AUDIT.md`, `-CONTEXT.md`, `-RESEARCH.md`,
  `-I18N-CONTRACT.md`, `-PATTERNS.md`, `-RUNTIME-CHECK.md`.
- `34.8-VALIDATION.md` frontmatter: `status: approved`, `nyquist_compliant: true`.

The five decisions, each verified present on disk:
1. Fork-owned namespace — `public/locales/en/gamelib.json` (210 keys). Tooling:
   `package.json:55` `lint-translations:gamelib`, `:56` `i18n-churn-guard`,
   `:59` `gen-i18n-gate-scope`; `meta/i18nGateScope.json`,
   `meta/i18nForkTouchedFiles.json`, `meta/i18nGateAllowlist.json`,
   `meta/i18nCatalogChurnGuard.ts`, `meta/genI18nGateScope.ts`.
2. LLM machine-fill script — `meta/machineFillGamelib.ts`, `package.json:57`
   `machine-fill-gamelib`, hermetic tests `meta/__tests__/machineFillGamelib.test.ts`.
3. Mark MT-origin strings — implemented as a SIDECAR MANIFEST written at
   `machineFillGamelib.ts:781` to `public/locales/<locale>/gamelib.mt.json`. Both
   `public/locales/de/gamelib.mt.json` and `public/locales/fr/gamelib.mt.json` exist.
   `machineFillGamelib.ts:358-366` documents that `filledAt` records when keys were FILLED,
   not when the script last ran, explicitly so "a later Weblate import" reads accurate
   provenance — decision 3's stated PURPOSE is honoured, not just its mechanism.
4. Defer Weblate — honoured; no Weblate project stood up.
5. Glossary non-negotiable — `meta/i18nGlossary.json`, `meta/__tests__/i18nGlossary.test.ts`.

Catalog counts (key-flattened over the real catalogs, 2026-09-02):
| fact | value |
|---|---|
| `en/gamelib.json` total keys | 210 |
| `en/gamelib.json` translatable (non-empty) keys | 204 |
| `en` keys empty in English | 6 (the `redeemKey.*` authoring gap the todo already documents correctly) |
| `de/gamelib.json` and `fr/gamelib.json` keys | 124 each |
| translatable keys ABSENT from `de` | 80 (same for `fr`) |
| `de` keys present-but-empty | 0 |

Locale census: `public/locales/` holds 49 directories. Exactly three carry a
`gamelib.json`: `en`, `de`, `fr`. So 46 locales have ZERO fork-string coverage —
204 translatable keys x 46 locales never filled.

The 2026-08-28 todo's numbers are stale by ~16x: it says en has 135 keys and 5 translatable
keys are missing. English grew 135 -> 210; the gap grew 5 -> 80.
</facts_already_measured>

<repo_state>
A CONCURRENT SESSION IS ACTIVE. Before doing anything, treat these as live and untouchable:
- `.planning/ROADMAP.md` — ~96 uncommitted lines adding Phase 40. NOT OURS. Do not edit, do
  not stage, do not commit.
- `.planning/STATE.md` — one uncommitted added line at working-tree line ~5623, under
  `### Roadmap Evolution`, beginning `- Phase 40 added 2026-09-02:`. NOT OURS.

STATE.md geometry (measured 2026-09-02):
- `git show HEAD:.planning/STATE.md` is 8169 lines; the last Quick Tasks row in HEAD is
  line 6451, matching `^| 260902-9el |`.
- In the working tree that same row is line 6452 (offset by one because of the concurrent
  Phase 40 line above it).
- Row shape has NO leading space and four cells:
  `| <id> | <description> | <date> | [<dirname>](.planning/quick/<dirname>/) |`
</repo_state>

<banned_commands>
`git stash`, `git reset`, `git checkout -- <file>` (fires this repo's post-checkout hook and
triggers a binary download — use `cp` if you need a backup), `git add -A`, `git add .`,
`gsd-sdk query commit` (stages the whole tree), `git commit -- .planning/STATE.md`
(`--only` semantics take the WORKING TREE and would sweep in the Phase 40 line).

Also banned for this task: `pnpm machine-fill-gamelib`, `pnpm i18n`, `pnpm lint-translations*`,
or anything else that writes a file under `public/locales/` or `meta/`.
</banned_commands>

<gate_design_notes>
Two failure shapes this repo produced earlier today. Both are already designed around in the
verify blocks below — do not "simplify" them back:

1. A scope gate over a `git mv` must pass `--no-renames`, or rename detection collapses the
   delete+add into a single path and the gate counts wrong.
2. A grep gate over a todo's frontmatter must be frontmatter-scoped with
   `awk '/^---$/{n++; next} n==1' <file>`, or it false-REDs the moment the executor
   correctly quotes an old frontmatter value as history inside the closure prose.

Every literal string a gate greps for is MANDATED verbatim in the corresponding `<action>`.
That is deliberate: the gate's vocabulary is fixed by the plan, not guessed from the output.
</gate_design_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the 2026-08-06 i18n-approach todo, non-totally, handing the coverage residue onward</name>
  <files>.planning/todos/pending/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md -> .planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md</files>
  <action>
Move the file with `git mv` (which stages both sides of the rename):

    git mv .planning/todos/pending/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md \
           .planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md

Then edit the moved file. Do NOT rewrite the existing `## Problem` / `## Solution` body —
it is the historical record of what was decided, and it stays verbatim.

FRONTMATTER — add these keys, keeping the existing `created`, `title`, `area`, `files`:

    status: CLOSED
    closed: 2026-09-02
    closed_by: "quick task 260902-9wt"

APPEND a new final section. Its heading MUST be exactly (em dash, not hyphen):

    ## CLOSURE RECORD — 2026-09-02, quick task 260902-9wt

Follow the shape of `.planning/todos/completed/2026-08-23-epic-logout-cookie-clear-unobserved-and-unowned.md`
(read it for the house style: bolded lead-in sentences, evidence cited by path and line,
explicit carve-outs at the end). Write the section to cover, in this order:

(a) WHY THIS CLOSES AS A DECISION ITEM. This todo's own `## Problem` says the approach was
    needed "before planning starts". Planning happened. Cite the phase directory
    `.planning/phases/34.8-frontend-i18n-compliance-for-fork-added-code-retrofit-hardco/`
    holding 12 plans (`34.8-01`..`34.8-12`, plus the `08a`/`08b`/`08c` split with per-slice
    `-CLOSURE.md` files), and `34.8-VALIDATION.md` frontmatter reading `status: approved`,
    `nyquist_compliant: true`.

(b) EACH OF THE FIVE DECISIONS AGAINST A BUILT ARTIFACT. One sub-item per decision, each
    naming a concrete path. The literal strings `gamelib.json`, `machineFillGamelib.ts`,
    `gamelib.mt.json`, `Weblate` and `i18nGlossary.json` MUST each appear in this section —
    they are the gate's vocabulary. Use the paths and line numbers from
    `<facts_already_measured>` above; do not re-derive them.
    For decision 3, say explicitly that it landed as a SIDECAR MANIFEST rather than a
    commit-message convention, and that `machineFillGamelib.ts:358-366` makes `filledAt`
    record when keys were FILLED rather than when the script last ran, precisely so a later
    Weblate import reads accurate provenance — so decision 3's stated purpose is honoured,
    not merely its mechanism.

(c) THE CLOSURE IS NOT TOTAL. This is the load-bearing paragraph. It MUST contain the exact
    phrase:

        COVERAGE, not a decision

    State plainly: the approach was decided AND built; it was simply never RUN beyond de/fr.
    Give the numbers — `en/gamelib.json` holds 210 keys (204 translatable); `de` and `fr`
    hold 124 each, so 80 translatable keys are absent from each; and of the 49 directories
    under `public/locales/`, only `en`, `de` and `fr` carry a `gamelib.json` at all, leaving
    46 locales at ZERO fork-string coverage. Name decision 2's own words — "an LLM covers all
    Heroic locales", "a few hundred keys x 50 locales" — and say that clause was never
    executed. Then hand the residue over, naming the successor by filename:

        .planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md

    and say it is being widened by this same quick task and STAYS PENDING.

(d) A CARVE-OUT PARAGRAPH, in the exemplar's style, making the misreadings impossible:
    - This closure must NOT be read as "fork strings are translated." They are not, in 48 of
      49 locales.
    - This closure must NOT be read as an auto-close on Phase 34.8 having closed. It closes on
      the five decisions each having a built artifact, plus the residue having a named owner.
    - The one thing still blocking the residue is a valid raw Anthropic API key — an OPERATOR
      action, not an engineering one. Everything else is built and tested.
  </action>
  <verify>
    <automated>
set -e
F=.planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md
test -f "$F"
test ! -e .planning/todos/pending/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md
# frontmatter-scoped: the closure prose may legitimately quote old values as history
FM=$(awk '/^---$/{n++; next} n==1' "$F")
printf '%s\n' "$FM" | grep -qx 'status: CLOSED'
printf '%s\n' "$FM" | grep -qx 'closed: 2026-09-02'
printf '%s\n' "$FM" | grep -qx 'closed_by: "quick task 260902-9wt"'
# closure-section-scoped: everything below must live in the new section, not elsewhere
CL=$(awk '/^## CLOSURE RECORD — 2026-09-02, quick task 260902-9wt$/,0' "$F")
test -n "$CL"
for s in 'gamelib.json' 'machineFillGamelib.ts' 'gamelib.mt.json' 'Weblate' 'i18nGlossary.json' \
         'COVERAGE, not a decision' '34.8-VALIDATION.md' \
         '2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md' \
         '210' '204' '80' '46' 'Anthropic API key'; do
  printf '%s\n' "$CL" | grep -qF "$s" || { echo "MISSING from closure section: $s"; exit 1; }
done
# the historical body survives verbatim
grep -qF 'Adopt the **hybrid approach** (decided 2026-08-06)' "$F"
grep -qF 'Glossary is non-negotiable' "$F"
echo OK
    </automated>
  </verify>
  <done>The todo is in `completed/`, absent from `pending/`, its frontmatter reads CLOSED / 2026-09-02 / quick task 260902-9wt, its original Problem+Solution body is intact, and its closure section names all five decisions against built artifacts, states the residue is `COVERAGE, not a decision`, and hands it to the 2026-08-28 todo by filename.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the 2026-08-28 todo in place — 210/204/80 counts and the 46 unowned locales — and leave it PENDING</name>
  <files>.planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md</files>
  <action>
Edit IN PLACE. Do NOT `git mv` it. Do NOT set `status: CLOSED`. It stays at its current path
with `status: pending`. Its filename does not change even though its title does — the filename
is a stable handle that the Task 1 closure section already cross-links to.

FRONTMATTER changes:
- `title:` — replace with, verbatim:

    title: "gamelib.json fork strings are unfilled in 48 of 49 locales — 80 keys missing from de/fr, 46 locales have no gamelib.json at all, machine-fill 401s, and the lint gate is structurally blind to all of it"

- `status:` — leave as `pending`.
- `files:` — extend the existing list with `public/locales/` and `meta/i18nGlossary.json`.
- Add two additive keys recording the amendment:

    amended: 2026-09-02
    amended_by: "quick task 260902-9wt"

BODY. Preserve everything still true; correct only what is stale. Structure it as:

1. OPENING COUNTS — replace the stale `135` / `124` / "five are translatable" lead with the
   measured figures: `en/gamelib.json` holds 210 keys, of which 204 are translatable;
   `de/gamelib.json` and `fr/gamelib.json` hold 124 each; 80 translatable keys are ABSENT from
   each, and 0 are present-but-empty.

2. KEEP THE FIVE-KEY TABLE EXACTLY AS IT IS. Do not delete a row and do not reword the English
   column. Reframe it with a sentence above: those five are still missing, and the table
   documents them well — it is now a documented SUBSET of the 80, not the whole gap.

3. KEEP the `redeemKey.*` paragraph — six keys empty in English too, a separate pre-existing
   AUTHORING gap, not a translation gap. Still exactly correct: `en` still has 6 empty values.
   Keep the provenance paragraph about `260827-vpl` / commit `b04601b8a` / the `installFlows.*`
   keys predating it.

4. KEEP the gate-blindness diagnosis section, unchanged in substance: `meta/lintTranslations.ts`
   checks a translated file's OWN keys against English, so a key ABSENT from a translation is
   structurally invisible and the gate stays green at zero coverage. The phrase
   `structurally invisible` must survive verbatim.
   ADD one sentence to it: this is exactly why the gap could grow 5 -> 80 unobserved — this
   todo PREDICTED that shape and was right, and the prediction is now measured.

5. KEEP the HTTP 401 section verbatim in substance, including the fenced error block:
   `ANTHROPIC_API_KEY` is set but 401s — it is not a valid raw API key, an OAuth/subscription
   token will not work, `machineFillGamelib.ts:824` reads it and passes it straight through,
   and the failed run wrote NOTHING with both catalogs verified byte-unchanged, so the D-09
   contract held.

6. NEW SECTION, heading exactly:

       ## Widened 2026-09-02 — 46 locales have ZERO fork-string coverage

   Content: `public/locales/` holds 49 locale directories; only `en`, `de` and `fr` carry a
   `gamelib.json` at all. 46 locales have never had a single fork string filled — 204
   translatable keys x 46 locales. Cite decision 2 of the now-closed
   `2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md`, which said
   "an LLM covers all Heroic locales" and put the volume at "a few hundred keys x 50 locales";
   that clause was never executed beyond de/fr. State that until this amendment the 46-locale
   gap was owned by NOBODY, and that this todo now owns it. Reference the closed todo by
   filename so the two files point at each other.

7. NEW SECTION, heading exactly:

       ## The only blocker is a valid raw Anthropic API key — an OPERATOR action

   Content: every engineering prerequisite is built and tested — `meta/machineFillGamelib.ts`,
   `pnpm machine-fill-gamelib` (`package.json:57`), hermetic tests at
   `meta/__tests__/machineFillGamelib.test.ts`, the glossary at `meta/i18nGlossary.json` with
   `meta/__tests__/i18nGlossary.test.ts`, and the MT-origin sidecar manifest written to
   `public/locales/<locale>/gamelib.mt.json`. Nothing here needs engineering. It needs a key.

8. KEEP the how-to-apply guidance, with both warnings intact:
   - `verify the fill actually happened — count the keys, do not trust exit 0` (a batch
     translation job on this project has previously returned zero results and still exited
     cleanly). The phrase `do not trust exit 0` must survive verbatim.
   - the do-not-hand-translate warning: `emptyAlphabetLetter` is substituted into `emptyBody`
     as a list member, so its grammatical form has to work inside that sentence per language —
     exactly the judgement the glossary-aware fill exists to make. The phrase
     `hand-translate` must survive verbatim.
   Update the run command guidance so it is no longer de/fr-only: `GAMELIB_MT_LOCALES` should
   name the full locale set (or be omitted if the script defaults to all), and the run should
   be verified by counting keys per locale afterwards.

9. KEEP the closing "Not a blocker for Phase 34.11" paragraph.

10. AMENDMENT NOTE — a short final paragraph recording what changed and preserving the old
    figures as history rather than erasing them: the todo previously stated en had 135 keys and
    that 5 translatable keys were missing; English grew 135 -> 210 between 2026-08-28 and
    2026-09-02 and the gap grew 5 -> 80. Note that the old title is superseded but the filename
    is deliberately unchanged.

Nothing in this task may write to `public/locales/`, `meta/` or `src/`.
  </action>
  <verify>
    <automated>
set -e
F=.planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md
test -f "$F"
test ! -e .planning/todos/completed/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md
# frontmatter-scoped: body legitimately quotes the OLD title and OLD counts as history
FM=$(awk '/^---$/{n++; next} n==1' "$F")
printf '%s\n' "$FM" | grep -qx 'status: pending'
printf '%s\n' "$FM" | grep -qx 'amended: 2026-09-02'
printf '%s\n' "$FM" | grep -qx 'amended_by: "quick task 260902-9wt"'
printf '%s\n' "$FM" | grep -q '^title: .*48 of 49 locales'
printf '%s\n' "$FM" | grep -q '^title: .*46 locales have no gamelib.json'
# new content present
for s in '## Widened 2026-09-02 — 46 locales have ZERO fork-string coverage' \
         '## The only blocker is a valid raw Anthropic API key — an OPERATOR action' \
         '2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md' \
         '210' '204' '49' '46'; do
  grep -qF "$s" "$F" || { echo "MISSING: $s"; exit 1; }
done
# preserved content — these are the reason the todo was widened rather than replaced
for s in '| `library.filterPanel.emptyAlphabetLetter` |' \
         'library.filterPanel.emptyAlphabetNumber' \
         'installFlows.pathRejectedTitle' \
         'installFlows.pathRejectedBodyMove' \
         'installFlows.pathRejectedBodyImport' \
         'redeemKey.alreadyOwned' \
         'meta/lintTranslations.ts' \
         'structurally invisible' \
         'HTTP 401' \
         'ANTHROPIC_API_KEY' \
         'machineFillGamelib.ts:824' \
         'byte-unchanged' \
         'do not trust exit 0' \
         'hand-translate' \
         'emptyBody' \
         'Not a blocker for Phase 34.11'; do
  grep -qF "$s" "$F" || { echo "LOST preserved content: $s"; exit 1; }
done
# read-only locale census, re-proving the 46 claim without touching a catalog
test "$(ls public/locales | wc -l | tr -d ' ')" = "49"
test "$(ls public/locales/*/gamelib.json 2>/dev/null | wc -l | tr -d ' ')" = "3"
# nothing outside .planning/ was touched
test -z "$(git status --porcelain --no-renames -- src meta public)"
echo OK
    </automated>
  </verify>
  <done>The 2026-08-28 todo is still in `pending/` with `status: pending`, its title names the 48-of-49 / 46-locale scope, its counts read 210/204/80, both new sections exist, and every preserved element — the five-key table, the redeemKey paragraph, the gate-blindness diagnosis, the 401 detail, the count-the-keys warning and the do-not-hand-translate warning — greps clean. `git status` reports zero changes under `src/`, `meta/` and `public/`.</done>
</task>

<task type="auto">
  <name>Task 3: Commit both todos plus a STATE.md row via a PREPARED INDEX, leaving the concurrent session's work untouched</name>
  <files>.planning/STATE.md, .planning/quick/260902-9wt-close-the-2026-08-06-phase-34-8-i18n-con/260902-9wt-PLAN.md</files>
  <action>
Two things must be true at the end: HEAD gains our STATE.md row, and HEAD does NOT gain the
concurrent session's `- Phase 40 added 2026-09-02:` line. A normal `git commit` of STATE.md
cannot do that, because the working-tree file holds both. So the STATE.md blob is BUILT from
HEAD and installed into the index directly. This exact technique was used successfully earlier
today in commit `e852d729e`.

STEP 0 — REFUSE TO PROCEED ON A DIRTY INDEX.
Run `git diff --cached --name-only`. The only entries permitted are the two todo paths staged
by Task 1's `git mv`. If anything else is staged, STOP and report — do not unstage it, and do
not run `git reset` (banned). Someone else's work may be in there.

STEP 1 — ADD OUR ROW TO THE WORKING TREE.
Compose a single-line row, no leading space, no unescaped `|` inside the description:

    | 260902-9wt | <description> | 2026-09-02 | [260902-9wt-close-the-2026-08-06-phase-34-8-i18n-con](.planning/quick/260902-9wt-close-the-2026-08-06-phase-34-8-i18n-con/) |

The description must be bold-led in the house style and must convey: the 2026-08-06 Phase-34.8
i18n approach todo CLOSED on shipped evidence (all five decisions built — fork `gamelib.json`
namespace, `meta/machineFillGamelib.ts`, the `gamelib.mt.json` MT sidecar manifest, Weblate
deferred, `meta/i18nGlossary.json`); the closure deliberately NOT total, because the fill was
never RUN beyond de/fr; the residue re-homed onto the 2026-08-28 todo, which was widened in
place and STAYS PENDING; the corrected measurements (en 210 keys / 204 translatable, de+fr 124
each, 80 missing per locale, and 46 of 49 locales with no `gamelib.json` at all — previously
owned by nobody); the lint gate's structural blindness being why the gap grew 5 -> 80
unobserved; and that the only remaining blocker is a valid raw Anthropic API key, an OPERATOR
action. Note RECORDS ONLY — zero code, zero catalog edits, no machine-fill run — and that
`.planning/ROADMAP.md` was left untouched and STATE.md was committed via a prepared index so a
concurrent session's in-flight Phase 40 line was not swept in.

Insert that row into the working-tree `.planning/STATE.md` immediately AFTER the line matching
`^| 260902-9el |` (working-tree line ~6452). This keeps the working tree coherent for the
concurrent session.

STEP 2 — BUILD THE PREPARED BLOB FROM HEAD, NOT FROM THE WORKING TREE.

    SCRATCH="$(mktemp -d)"
    git show HEAD:.planning/STATE.md > "$SCRATCH/state-base.md"
    # sanity: the base must NOT already contain the concurrent line or our row
    grep -c '^- Phase 40 added 2026-09-02:' "$SCRATCH/state-base.md"   # expect 0
    grep -c '260902-9wt' "$SCRATCH/state-base.md"                      # expect 0
    grep -n '^| 260902-9el |' "$SCRATCH/state-base.md"                 # expect exactly one hit

Write the same row into `"$SCRATCH/row.txt"` (one line, no trailing newline issues), then
insert it after the `260902-9el` line without a shell-quoting hazard — read the row from the
file rather than interpolating it into an awk program:

    awk 'NR==FNR { row = $0; next }
         { print }
         /^\| 260902-9el \|/ && !ins { print row; ins = 1 }' \
        "$SCRATCH/row.txt" "$SCRATCH/state-base.md" > "$SCRATCH/state-new.md"

Verify the built blob before installing it:

    test "$(wc -l < "$SCRATCH/state-new.md")" -eq "$(( $(wc -l < "$SCRATCH/state-base.md") + 1 ))"
    grep -c '260902-9wt' "$SCRATCH/state-new.md"                       # expect 1
    grep -c '^- Phase 40 added 2026-09-02:' "$SCRATCH/state-new.md"    # expect 0
    diff <(grep -v '260902-9wt' "$SCRATCH/state-new.md") "$SCRATCH/state-base.md"   # expect no output

STEP 3 — INSTALL THE BLOB INTO THE INDEX.

    BLOB=$(git hash-object -w "$SCRATCH/state-new.md")
    git update-index --cacheinfo 100644,"$BLOB",.planning/STATE.md

STEP 4 — STAGE THE PLAN FILE BY EXPLICIT PATH.

    git add .planning/quick/260902-9wt-close-the-2026-08-06-phase-34-8-i18n-con/260902-9wt-PLAN.md

Do NOT use `git add -A` or `git add .` (banned). Do not stage `.planning/ROADMAP.md`.

STEP 5 — BARE COMMIT, NO PATHSPEC.
A bare `git commit` commits exactly the index. Never `git commit -- .planning/STATE.md`: that
is `--only` semantics and would take the WORKING TREE copy, sweeping in the Phase 40 line.

    git commit -m "docs(quick-260902-9wt): close the 34.8 i18n approach todo and re-home its unrun coverage residue"

If any SUMMARY.md is written after this commit, commit it in a SEPARATE follow-up commit by
explicit path — do not amend, and do not re-run the prepared-index dance for it.
  </action>
  <verify>
    <automated>
set -e
# --no-renames so the git mv reads as delete + add and the scope gate counts honestly
CHANGED=$(git show --stat --name-only --no-renames --format= HEAD)
echo "$CHANGED"
# scope: nothing outside .planning/, and never ROADMAP.md
echo "$CHANGED" | grep -qv '^$' || { echo "empty commit"; exit 1; }
if echo "$CHANGED" | grep -qE '^(src|meta|public)/'; then echo "OUT OF SCOPE: code/catalog path in commit"; exit 1; fi
if echo "$CHANGED" | grep -q 'ROADMAP.md'; then echo "OUT OF SCOPE: ROADMAP.md in commit"; exit 1; fi
# the rename, both sides
echo "$CHANGED" | grep -qx '.planning/todos/pending/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md'
echo "$CHANGED" | grep -qx '.planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md'
echo "$CHANGED" | grep -qx '.planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md'
echo "$CHANGED" | grep -qx '.planning/STATE.md'
# the committed STATE.md has OUR row and NOT the concurrent session's line
test "$(git show HEAD:.planning/STATE.md | grep -c '260902-9wt')" = "1"
test "$(git show HEAD:.planning/STATE.md | grep -c '^- Phase 40 added 2026-09-02:')" = "0"
# committed STATE.md differs from HEAD~ by exactly our one added line
test "$(git diff HEAD~1 HEAD --numstat -- .planning/STATE.md | awk '{print $1"/"$2}')" = "1/0"
# the concurrent session's work survives, uncommitted, in the working tree
git status --porcelain -- .planning/ROADMAP.md | grep -q '^ M\|^M ' || { echo "ROADMAP.md no longer dirty — concurrent work may have been swept"; exit 1; }
test "$(grep -c '^- Phase 40 added 2026-09-02:' .planning/STATE.md)" = "1"
test "$(grep -c '260902-9wt' .planning/STATE.md)" = "1"
# index is clean apart from nothing at all
test -z "$(git diff --cached --name-only)"
echo OK
    </automated>
  </verify>
  <done>One commit exists containing the 2026-08-06 rename (both sides), the 2026-08-28 edit, the prepared `.planning/STATE.md` and the plan file — and nothing else. `git show HEAD:.planning/STATE.md` contains our `260902-9wt` row and zero occurrences of `- Phase 40 added 2026-09-02:`. The working tree still shows `.planning/ROADMAP.md` modified and `.planning/STATE.md` carrying both our row and the concurrent session's Phase 40 line. The index is empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> git index -> commit | A concurrent session's uncommitted edits to `.planning/ROADMAP.md` and `.planning/STATE.md` share this boundary with ours. Any pathspec- or tree-based staging crosses it in the wrong direction. |
| todo record -> future reader | A closure record is read later as evidence. Overclaiming here silently retires an unowned obligation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9wt-01 | Tampering | `.planning/STATE.md` commit | mitigate | Blob built from `git show HEAD:` + one inserted row, installed via `git hash-object -w` + `git update-index --cacheinfo`, committed with a BARE `git commit`. Task 3 verify asserts `git diff HEAD~1 HEAD --numstat` is exactly `1/0` and that the committed blob contains zero `- Phase 40 added 2026-09-02:` lines. |
| T-9wt-02 | Tampering | `.planning/ROADMAP.md` | mitigate | Never staged, never edited. Task 3 verify fails if `ROADMAP.md` appears in the commit, and separately fails if it is no longer dirty in the working tree. |
| T-9wt-03 | Repudiation | 2026-08-06 closure record | mitigate | The closure is required to carry the literal phrase `COVERAGE, not a decision`, the 210/204/80/46 figures, and a named successor filename. Task 1's verify greps all of them inside the closure section specifically, not the file at large. |
| T-9wt-04 | Denial of Service | machine-fill residue ownership | mitigate | The 2026-08-28 todo is asserted still present in `pending/` with `status: pending` and asserted absent from `completed/`, so the residue cannot be closed out from under itself by this task. |
| T-9wt-05 | Information Disclosure | `ANTHROPIC_API_KEY` | accept | The todo already discusses the key by NAME only and records that the current value 401s. No key value is read, printed or committed by any task here. |
| T-9wt-06 | Tampering | `src/`, `meta/`, `public/locales/` | mitigate | RECORDS ONLY. Task 2 verify asserts `git status --porcelain -- src meta public` is empty; Task 3 verify fails the commit if any path under those roots appears. |
| T-9wt-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs no packages and runs no package manager. No legitimacy gate needed. |
</threat_model>

<verification>
Run after all three tasks:

    # residue is owned, and owned by something still open
    grep -l '46 locales' .planning/todos/pending/*.md
    # the closed todo points at it, and it points back
    grep -c '2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md' \
      .planning/todos/completed/2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md
    grep -c '2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md' \
      .planning/todos/pending/2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md
    # nothing was built, run or translated
    git show --stat --no-renames HEAD

The scope gate MUST use `--no-renames`. Without it, git collapses the `git mv` into a single
renamed path and the both-sides assertions in Task 3 silently pass over a commit that never
recorded the delete.
</verification>

<success_criteria>
- `2026-08-06-phase-34-8-i18n-context-fork-namespace-llm-machine-fill-defe.md` is in
  `.planning/todos/completed/`, frontmatter `status: CLOSED` / `closed: 2026-09-02` /
  `closed_by: "quick task 260902-9wt"`, original body intact, closure section names all five
  decisions against artifacts that exist on disk.
- That closure section is explicitly NOT total: it contains the phrase
  `COVERAGE, not a decision`, the 210/204/80/46 figures, and the successor filename.
- `2026-08-28-gamelib-json-de-fr-missing-five-keys-machine-fill-401s.md` is STILL in
  `.planning/todos/pending/` with `status: pending`, retitled to name the 48-of-49 /
  46-locale scope, counts corrected to 210/204/80, both new sections present, and every
  preserved element (five-key table, redeemKey paragraph, gate-blindness diagnosis, HTTP 401
  detail, count-the-keys warning, do-not-hand-translate warning) greps clean.
- Exactly one commit, containing only `.planning/` paths, including both sides of the rename,
  the widened todo, the prepared `.planning/STATE.md` and the plan file.
- `git show HEAD:.planning/STATE.md` has one `260902-9wt` row and zero
  `- Phase 40 added 2026-09-02:` lines; `git diff HEAD~1 HEAD --numstat -- .planning/STATE.md`
  is `1 0`.
- The working tree still carries the concurrent session's uncommitted `.planning/ROADMAP.md`
  changes and its Phase 40 STATE.md line, alongside our row.
- `git status --porcelain -- src meta public` is empty. No catalog was written, no
  machine-fill was run.
</success_criteria>

<output>
Create `.planning/quick/260902-9wt-close-the-2026-08-06-phase-34-8-i18n-con/260902-9wt-SUMMARY.md` when done.
Commit it in a SEPARATE follow-up commit by explicit path — do not amend Task 3's commit.
</output>
