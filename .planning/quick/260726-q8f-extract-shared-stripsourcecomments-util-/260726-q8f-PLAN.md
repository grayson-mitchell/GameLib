---
phase: quick/260726-q8f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/testUtils/stripSourceComments.ts
  - src/backend/__tests__/stripSourceComments.test.ts
  - src/backend/appshell/__tests__/appshellModules.test.ts
  - src/backend/crossover_index/__tests__/ratingMap.test.ts
  - src/backend/gamedetails/__tests__/gameDetailsModules.test.ts
  - src/backend/sidecar/__tests__/appShellImportGate.test.ts
  - src/backend/sidecar/__tests__/bootstrapWirings.test.ts
  - src/backend/sidecar/__tests__/dialogStub.test.ts
  - src/backend/sidecar/__tests__/downloadQueueFlows.test.ts
  - src/backend/sidecar/__tests__/electronUntouched.test.ts
  - src/backend/sidecar/__tests__/enrichmentFlows.test.ts
  - src/backend/sidecar/__tests__/gameDetailsImportGate.test.ts
  - src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts
  - src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts
  - src/backend/sidecar/__tests__/testContainment.test.ts
  - src/backend/storeSearch/__tests__/handlers.test.ts
  - src/backend/sidecar/__tests__/structuralContainment.test.ts
autonomous: true
requirements:
  - "34.2-REVIEW-GAP-CYCLE-4 CR-01 (re-scoped out of phase 34.2 by the human override in 34.2-VERIFICATION.md)"

must_haves:
  truths:
    - "Exactly one comment-stripping implementation of this family exists under src/backend (plus the two out-of-scope, different-concern strippers named below)."
    - "A NON-`*`-prefixed block comment naming jest.mock('os'/'node:os'), process.env.HOME assignment, or an expression-body error wrapper CANNOT satisfy a gate built on the shared util."
    - "A code line containing a quoted \"steam://...\" literal survives the util intact (WR-08 property preserved)."
    - "The full backend jest run's per-test verdict set is unchanged except for explained additions — no gate silently flipped pass->fail or fail->pass."
    - "tsc --noEmit exits 0 and prettier --check is clean on every touched file."
  artifacts:
    - path: "src/backend/testUtils/stripSourceComments.ts"
      provides: "The single shared stripSourceComments(source) util"
      contains: "export function stripSourceComments"
    - path: "src/backend/__tests__/stripSourceComments.test.ts"
      provides: "Self-test incl. the non-`*`-prefixed block-comment spelling + the WR-08 non-truncation property"
      contains: "stripSourceComments"
  key_links:
    - from: "src/backend/sidecar/__tests__/testContainment.test.ts"
      to: "src/backend/testUtils/stripSourceComments.ts"
      via: "bare baseUrl import"
      pattern: "from 'backend/testUtils/stripSourceComments'"
    - from: "src/backend/sidecar/__tests__/structuralContainment.test.ts"
      to: "src/backend/testUtils/stripSourceComments.ts"
      via: "bare baseUrl import replacing stripCommentsForNodeOsGate"
      pattern: "from 'backend/testUtils/stripSourceComments'"
---

<objective>
Close the repo-wide vacuous-gate defect at its cause: extract ONE shared comment-stripping
utility that removes block comments before the existing line-prefix filter, self-test it with the
exact spelling four review cycles missed, and replace all 15 local copies.

Purpose: 14 backend test files carry a byte-identical line-prefix-only `stripComments`. It drops a
line only when that line itself begins with a comment marker, so the interior of a block comment
whose lines lack a `*` prefix survives stripping. Every gate built on it is satisfiable by a pure
comment naming the pattern the gate requires — independently reproduced twice by executing the
helper. Four consecutive gap cycles fixed instances; this fixes the pattern.

Output: `src/backend/testUtils/stripSourceComments.ts`, its self-test, and 15 migrated call-site
files, with a before/after per-test verdict diff proving no gate silently flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34.2-tauri-ipc-re-plumb-slice-5-game-details-settings-and-overrid/34.2-REVIEW-GAP-CYCLE-4.md

Project skill: `Skill("spike-findings-gamelib")` — read only if a Steam/native-install question
arises. This task touches no production code and no Steam surface.

<the_defective_helper>
Byte-identical in 13 files as a named `function stripComments(source: string): string`, and as an
INLINE anonymous chain (no function declaration) at
`src/backend/crossover_index/__tests__/ratingMap.test.ts:186-190`:

  source
    .split('\n')
    .filter((line) => !/^\s*(\/\/|\*|\/\*)/.test(line))
    .join('\n')

`grep -rn "filter((line) => !/" src/backend` returns exactly 14 hits today — one per file to
migrate. Do NOT grep for `function stripComments` alone: that misses ratingMap.test.ts.
</the_defective_helper>

<the_correct_helper_already_in_repo>
`src/backend/sidecar/__tests__/structuralContainment.test.ts:265-267`:

  function stripCommentsForNodeOsGate(source: string): string {
    return source.replace(/\/\*[\s\S]*?\*\//g, '').replace(/\/\/.*$/gm, '')
  }

Correct about block comments, but it uses the naive `/\/\/.*$/gm` trailing-comment regex. That
regex is exactly what plan 34.2-28 removed as WR-08 because it truncates a code line containing a
quoted `"steam://..."` literal. The shared util must NOT adopt it. See Task 3 for how this file
is migrated without changing its gate's verdicts.
</the_correct_helper_already_in_repo>

<module_resolution>
`tsconfig.json` sets `"baseUrl": "./src/"`; `src/backend/jest.config.js` sets
`modulePaths: [compilerOptions.baseUrl]` and `moduleDirectories: ['node_modules', '<rootDir>']`.
Bare `backend/...` specifiers therefore resolve in both tsc and jest — the convention already used
by `import { containmentRoot } from 'backend/jest.setupContainment'`
(structuralContainment.test.ts:66). Use `'backend/testUtils/stripSourceComments'`.
</module_resolution>

<placement_constraints_verified_by_orchestrator>
- jest `testMatch` is `**/__tests__/**/*.test.ts`, `roots` is `<rootDir>/src/backend`. The util
  goes OUTSIDE any `__tests__` dir: `src/backend/testUtils/` (new directory).
- The self-test goes at `src/backend/__tests__/stripSourceComments.test.ts` and NOT in
  `src/backend/sidecar/__tests__/`. `testContainment.test.ts` Block C (`:590-630`) runs
  `readdirSync(__dirname)` over that exact directory and requires every `*.test.ts` in it to be
  classified by exactly one declared list — a new file there trips the tripwire.
- `structuralContainment.test.ts`'s `NODE_OS_GATE_EXEMPT_FILES` is a declared 3-entry allowlist
  guarded by its own test. DO NOT edit it. If the migration appears to require an edit there,
  stop and report it as a deviation instead of doing it silently.
</placement_constraints_verified_by_orchestrator>

<out_of_scope_strippers>
- `src/backend/__tests__/tauriConf.test.ts:69` — `stripComments` delegating to `stripHashComments`
  (`#`-style comments, a different concern). LEAVE ALONE.
- `src/backend/__tests__/longRunningChannels.test.ts` — `stripRustLineComments` for Rust source.
  Rust `//` semantics differ and plan 34.2-28 deliberately kept its raw-vs-stripped two-step.
  LEAVE ALONE, and say so explicitly in the SUMMARY.
</out_of_scope_strippers>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the pre-change verdict baseline, create the shared util, and self-test it with a recorded RED proof</name>
  <files>src/backend/testUtils/stripSourceComments.ts, src/backend/__tests__/stripSourceComments.test.ts</files>
  <behavior>
    Baseline (must be captured BEFORE any file is written — Task 3 diffs against it):
    - `npx jest --config src/backend/jest.config.js --json --outputFile <scratchpad>/baseline.json`
      run on the untouched tree. Record suite/test totals and the failing-suite set in the SUMMARY.
      The expected sole failure is `rustInvokeChannel.test.ts` (documented Phase 34.1 baseline).

    stripSourceComments(source) contract:
    - Test 1 (the spelling four cycles missed): a NON-`*`-prefixed block comment whose body names
      `jest.mock('os', mockOsFactory)` and `jest.mock('node:os', mockOsFactory)` produces stripped
      output containing NEITHER `jest.mock('os'` NOR `jest.mock('node:os'`.
    - Test 2: a NON-`*`-prefixed block comment whose body names `process.env.HOME = containmentRoot`
      produces stripped output not containing `process.env.HOME`.
    - Test 3: a NON-`*`-prefixed block comment whose body names an expression-body error wrapper
      (`=> heroicLogWriter.logError(`) produces stripped output not containing that text.
    - Test 4 (regression guard for the spelling the old self-tests DID cover): a `*`-prefixed
      docblock naming the same pattern is still stripped.
    - Test 5 (WR-08 property): the input line
      `const url = 'steam://rungameid/440'` survives stripping with the full literal
      `steam://rungameid/440` intact — assert the stripped output CONTAINS it.
    - Test 6 (WR-08 property, trailing-comment form): the input line
      `const url = 'steam://rungameid/440' // launch it` still yields stripped output containing
      the full `steam://rungameid/440` literal. Document in a code comment that the trailing
      comment text itself intentionally SURVIVES — removing it would require the naive
      `/\/\/.*$/gm` regex that truncates the literal.
    - Test 7: a whole line beginning with `//` is still dropped (the retained line filter).
    - Test 8: real code with no comments passes through byte-identical.

    RED proof (mandatory, hand-executed, recorded verbatim in the SUMMARY):
    temporarily replace the util's body with the OLD line-prefix-only implementation (the four-line
    chain in `<the_defective_helper>` above), run only this test file, and record which of Tests
    1-3 fail. Tests 1, 2 and 3 MUST all fail. Restore the real implementation and confirm
    `git diff` on the util shows only the intended final body.
  </behavior>
  <action>
Create `src/backend/testUtils/stripSourceComments.ts` exporting exactly one function:

`export function stripSourceComments(source: string): string` — it FIRST removes block comments
via `.replace(/\/\*[\s\S]*?\*\//g, '')`, THEN applies the existing line-prefix filter UNCHANGED
(`.split('\n')`, `.filter((line) => !/^\s*(\/\/|\*|\/\*)/.test(line))`, `.join('\n')`).

CRITICAL: do NOT add a `/\/\/.*$/gm` trailing-comment pass and do NOT substitute one for the line
filter. That regex truncates a code line containing a quoted `"steam://..."` literal — the exact
defect plan 34.2-28 fixed as WR-08. Block-comment removal is ADDED BEFORE the existing filter, not
substituted for it.

Give the file a docstring that: names it as the single shared implementation for backend source-text
gates; states the two-stage order and why the line filter is retained (naming WR-08 and the
`steam://` truncation); and states the known, accepted limitation that a trailing `//` comment on a
code line survives.

Write the self-test at `src/backend/__tests__/stripSourceComments.test.ts` covering the eight
behaviours above. Build every fixture with `[...].join('\n')` string arrays (the convention already
used by this repo's gate self-tests) so the fixtures are code, never real comments in the test
file's own source.

TRAP — this file is scanned by `structuralContainment.test.ts`'s backend-wide `node:os` gate
(`usesForbiddenNodeOsBinding`, `:278-282`): any file under `src/backend` whose comment-stripped
source mentions `node:os` must not contain the bare identifiers `homedir` or `userInfo` anywhere.
Test 1's fixture necessarily mentions `node:os`, so the words `homedir` and `userInfo` MUST NOT
appear anywhere in this file — not in fixtures, not in prose, not in test names. Use
`mockOsFactory` as the fixture's second argument instead (the review's own reproduction shape).

Also capture the pre-change baseline JSON as the first step, before writing either file.
  </action>
  <verify>
    <automated>npx jest --config src/backend/jest.config.js --testPathPattern 'stripSourceComments' && npx tsc --noEmit && npx prettier --check src/backend/testUtils/stripSourceComments.ts src/backend/__tests__/stripSourceComments.test.ts && test -s "$SCRATCH/baseline.json" && ! grep -nE '\b(homedir|userInfo)\b' src/backend/__tests__/stripSourceComments.test.ts</automated>
  </verify>
  <done>Baseline JSON captured from the untouched tree; the util exists with block-removal-then-line-filter in that order and no `/\/\/.*$/gm` pass; 8 self-tests pass; Tests 1-3 hand-proven RED against the old implementation with the failure output recorded verbatim; the self-test file contains neither `homedir` nor `userInfo`; tsc and prettier clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the defective helper in all 14 files with an import of the shared util</name>
  <files>src/backend/appshell/__tests__/appshellModules.test.ts, src/backend/crossover_index/__tests__/ratingMap.test.ts, src/backend/gamedetails/__tests__/gameDetailsModules.test.ts, src/backend/sidecar/__tests__/appShellImportGate.test.ts, src/backend/sidecar/__tests__/bootstrapWirings.test.ts, src/backend/sidecar/__tests__/dialogStub.test.ts, src/backend/sidecar/__tests__/downloadQueueFlows.test.ts, src/backend/sidecar/__tests__/electronUntouched.test.ts, src/backend/sidecar/__tests__/enrichmentFlows.test.ts, src/backend/sidecar/__tests__/gameDetailsImportGate.test.ts, src/backend/sidecar/__tests__/loggerCallSiteGuard.test.ts, src/backend/sidecar/__tests__/sidecarRejectionGuard.test.ts, src/backend/sidecar/__tests__/testContainment.test.ts, src/backend/storeSearch/__tests__/handlers.test.ts</files>
  <action>
In each of the 14 files, delete the local helper and add, alongside the file's other imports:

`import { stripSourceComments as stripComments } from 'backend/testUtils/stripSourceComments'`

Use the ALIASED form deliberately. Several of these files carry gates that read their own source
(`bootstrapWirings.test.ts:322` reads `__filename`) or name `stripComments` inside test-name
strings (`gameDetailsImportGate.test.ts:69`) and docstring prose. Aliasing keeps every call site
and every prose reference valid, so the only text that changes per file is the helper definition
block plus one import line — the minimum diff, which is what keeps the hazard in Task 3 tractable.

Per-file specifics:
- Three files define the helper INSIDE a `describe` block and are therefore indented:
  `appshellModules.test.ts:243`, `gameDetailsModules.test.ts:581`, `enrichmentFlows.test.ts:1034`.
  Delete the indented definition; the import still goes at top level with the other imports.
- `ratingMap.test.ts:186-190` has NO function declaration — it is an inline chain assigned to
  `const stripped`. Replace that chain with `const stripped = stripComments(source)` and add the
  same aliased import.
- Where a deleted helper carried a docstring making a now-false claim (e.g. "mirrors
  `electronUntouched.test.ts`'s own `stripComments`", or a description of the line-prefix
  behaviour), replace it with a one- or two-line comment above the import stating that the shared
  util strips block comments first and then applies the line-prefix filter, and pointing at
  `backend/testUtils/stripSourceComments`. Do not leave prose that describes the old behaviour —
  stale prose that over-claims is a repeatedly-flagged finding class in this phase.
- Do not rename call sites, test names, or any other text. Do not reorder existing imports.

Do not run the full sweep in this task; Task 3 owns verification.
  </action>
  <verify>
    <automated>test "$(grep -rn 'filter((line) => !/' src/backend | wc -l | tr -d ' ')" = "1" && npx tsc --noEmit && npx prettier --check $(git diff --name-only -- 'src/backend/**/*.ts' | tr '\n' ' ')</automated>
  </verify>
  <done>All 14 local copies are gone (the defective-body grep returns exactly 1 hit — the shared util's own line filter); every file imports the util under the `stripComments` alias; tsc exits 0; prettier clean on every touched file.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate structuralContainment.test.ts, then prove no gate silently flipped</name>
  <files>src/backend/sidecar/__tests__/structuralContainment.test.ts</files>
  <action>
STEP 1 — migrate the one already-correct copy so exactly one implementation remains.

Delete `stripCommentsForNodeOsGate` (`:265-267`) and add
`import { stripSourceComments } from 'backend/testUtils/stripSourceComments'`, calling it from
`usesForbiddenNodeOsBinding`.

Semantic note the executor must reason about, not assume: the shared util is strictly LESS
aggressive than the deleted local helper on exactly one input shape — a trailing `//` comment on a
code line. Everything the old helper removed, the shared util also removes, except that. So the
only possible verdict change is a NEW false positive: a backend `.ts` file that mentions `node:os`
and mentions `homedir`/`userInfo` only inside a trailing comment.

Run the gate immediately after the swap:
`npx jest --config src/backend/jest.config.js --testPathPattern 'structuralContainment'`
- If green: done. The plain shared-util call stands.
- If a file is newly reported as a violation: do NOT add it to `NODE_OS_GATE_EXEMPT_FILES` and do
  NOT weaken the gate. Instead keep the shared util as the base and layer the trailing-comment
  removal locally inside `usesForbiddenNodeOsBinding` only:
  `stripSourceComments(source).replace(/\/\/.*$/gm, '')`, with a comment stating that the extra
  pass is safe HERE specifically because this gate matches bare identifiers and never string
  literals, and that it must not migrate into the shared util (WR-08). Name the file that forced
  this in the SUMMARY.
Either outcome is acceptable; silently editing the allowlist is not.

Also confirm the file's `NODE_OS_GATE_EXEMPT_FILES` allowlist test still passes untouched, and that
`structuralContainment.test.ts` is not newly caught by its own gate.

STEP 2 — the hazard check. Several migrated files run gates against their own source, and swapping
a definition for an import changes each file's text. A green suite is NOT sufficient evidence.

Run the full sweep to JSON and diff against Task 1's baseline:
`npx jest --config src/backend/jest.config.js --json --outputFile <scratchpad>/after.json`

Compare the `{fullName, status}` pairs from `baseline.json` and `after.json`. Classify EVERY
difference:
- Additions from `stripSourceComments.test.ts` — expected, list them.
- Any pre-existing test whose status changed in EITHER direction — this is the finding the hazard
  predicts. A gate that was passing on comment text and now fails is a GENUINE vacuity discovery,
  not a regression to paper over: report it, and fix it only if the fix is contained (re-point the
  gate at real code). If the underlying source genuinely lacks the pattern the gate asserts, STOP
  and report rather than weakening the gate or reverting the stripper.
- A test that changed from failing to passing must also be explained.
- Any test that disappeared must be explained.

Record the full classification in the SUMMARY. "The suite is green" alone does not satisfy this
task.

Acceptable residual failures in `after.json`: `rustInvokeChannel.test.ts` ONLY (documented Phase
34.1 baseline). `withTimeout.test.ts` / `steamAuthFlows.test.ts` / `enrichmentFlows.test.ts` may
flake on the known `library.ts` leaked-timer issue — if one flakes, re-run and state that in the
SUMMARY.

STEP 3 — final sweep: `npx tsc --noEmit`, `npx prettier --check` on every file touched by this
plan, and the zero-remaining-copies grep. Confirm no production code imports the new util
(`grep -rn "testUtils/stripSourceComments" src | grep -v "__tests__"` should show only the util's
own path), and confirm `package.json` / `pnpm-lock.yaml` are unchanged.
  </action>
  <verify>
    <automated>npx jest --config src/backend/jest.config.js --json --outputFile "$SCRATCH/after.json"; node -e "const b=require(process.env.SCRATCH+'/baseline.json'),a=require(process.env.SCRATCH+'/after.json');const m=r=>new Map(r.testResults.flatMap(s=>s.assertionResults.map(t=>[t.fullName,t.status])));const B=m(b),A=m(a);const d=[...new Set([...B.keys(),...A.keys()])].filter(k=>B.get(k)!==A.get(k)).map(k=>k+': '+B.get(k)+' -> '+A.get(k));console.log(d.join('\n')||'NO VERDICT CHANGES');" && npx tsc --noEmit && test "$(grep -rn 'filter((line) => !/' src/backend | wc -l | tr -d ' ')" = "1"</automated>
  </verify>
  <done>Exactly one comment-stripping implementation of this family remains under src/backend (tauriConf's `#`-stripper and longRunningChannels' Rust stripper are documented as deliberately untouched); the `node:os` gate's verdict set is unchanged and `NODE_OS_GATE_EXEMPT_FILES` is byte-unchanged; the baseline/after verdict diff is printed and every entry classified in the SUMMARY; failing-suite set is exactly `{rustInvokeChannel.test.ts}`; tsc exits 0; prettier clean on all touched files; no production code imports the util; package.json and pnpm-lock.yaml unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan touches test-only code. `src/backend/testUtils/` is imported exclusively by `*.test.ts` files and is never reachable from the Electron main process or the Tauri sidecar bundle. No runtime input crosses any boundary as a result of this change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q8F-01 | Tampering | The shared `stripSourceComments` util — a single point of failure for every backend source-text gate | mitigate | The util ships with an 8-case self-test including the exact non-`*`-prefixed spelling that defeated the old implementation, hand-RED-proofed against that implementation. Task 3's baseline/after verdict diff detects any gate whose behaviour changes. |
| T-Q8F-02 | Repudiation | Gates that pass vacuously, producing evidence that does not exist | mitigate | This plan is the mitigation: block-comment interiors can no longer satisfy a gate. Task 3 requires every verdict change to be classified rather than absorbed, so a newly-failing gate surfaces as a finding. |
| T-Q8F-03 | Denial of Service | Over-aggressive stripping truncating real code (the WR-08 regression class) | mitigate | The line-prefix filter is retained verbatim and the naive `/\/\/.*$/gm` pass is explicitly prohibited in the shared util; self-test Tests 5 and 6 pin the `steam://` literal against truncation. Task 3's optional local layer for the `node:os` gate is scoped to identifier matching only. |
| T-Q8F-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. `package.json` and `pnpm-lock.yaml` are asserted unchanged in Task 3's `<done>`. No Package Legitimacy Gate applies. |
</threat_model>

<verification>
1. `npx jest --config src/backend/jest.config.js` — sole acceptable failing suite is
   `rustInvokeChannel.test.ts`. Known flakes (`withTimeout`, `steamAuthFlows`, `enrichmentFlows`
   via the `library.ts` leaked timer) must be re-run and reported if hit.
2. Baseline/after per-test verdict diff printed and every entry classified — not merely "green".
3. `npx tsc --noEmit` exits 0.
4. `npx prettier --check` clean on all 17 touched files (a prettier regression was a real cycle-3
   finding; CI runs `pnpm prettier`).
5. `grep -rn 'filter((line) => !/' src/backend | wc -l` returns 1.
6. `grep -nE '\b(homedir|userInfo)\b' src/backend/__tests__/stripSourceComments.test.ts` returns
   nothing.
7. `git diff` on `NODE_OS_GATE_EXEMPT_FILES` is empty.
</verification>

<success_criteria>
- One `stripSourceComments` implementation; 15 local copies gone.
- A non-`*`-prefixed block comment can no longer satisfy any gate built on it, proven by a
  self-test that was hand-verified to fail against the old implementation.
- The `steam://` literal non-truncation property (WR-08) is preserved and pinned.
- No gate silently flipped: every per-test verdict change between baseline and after is enumerated
  and explained.
- `tauriConf.test.ts`'s `#`-stripper and `longRunningChannels.test.ts`'s Rust stripper are
  untouched, with that decision stated explicitly in the SUMMARY.
</success_criteria>

<output>
Create `.planning/quick/260726-q8f-extract-shared-stripsourcecomments-util-/260726-q8f-SUMMARY.md` when done.
</output>
</content>
</invoke>
