---
phase: quick-260925-o9b
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QUICK-260925-o9b]
files_modified:
  - .planning/todos/pending/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md
  - .planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md
  - .planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md

must_haves:
  truths:
    - "The report-upstream todo no longer exists under `.planning/todos/pending/` and does exist under `.planning/todos/completed/` under the SAME filename, moved with `git mv` so `git status --porcelain` reports it as a rename (`R`), not as a delete plus an untracked add."
    - "The moved file's ORIGINAL body is byte-identical to its pre-move content -- the only change is one appended section at the end. Its frontmatter still carries `severity: minor`, `platform: any`, `ready: human`, unedited."
    - "The appended section is headed exactly `## Resolution (2026-09-25): closed as obsolete -- no reportable upstream exists` (em dash, not a double hyphen) and records every verified fact listed in Task 1 -- the archival date, the npm deprecation with its frozen version/dates, the gsd-core successor and its file relocation, the three-of-four already-fixed suggestions with their upstream issue numbers, the residual open epic #4629, the deliberate decision NOT to re-report, the untouched GameLib-local workaround, and the absent `gh` CLI."
    - "That section states plainly WHY nothing was reported, rather than leaving the closure to read as an omission."
    - "The section's last line points at the successor todo created in Task 2, by filename."
    - "A new pending todo exists whose frontmatter carries `severity: medium`, `platform: any`, `ready: human` -- bare, lowercase, unquoted, in that exact adjacent order, immediately after `area: tooling` -- and which `pnpm planning-gates` accepts."
    - "The successor todo frames migrate / fork-and-pin / stay-frozen as an OPEN human decision. It does not recommend an answer, and it does not read as a migration plan."
    - "No file outside `.planning/todos/` is modified. No source code changes."
  artifacts:
    - path: ".planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md"
      provides: "The closed report-upstream todo with its obsolescence rationale appended"
      contains: "closed as obsolete"
    - path: ".planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md"
      provides: "The successor decision todo about the deprecated SDK line"
      contains: "open-gsd/gsd-core"
  key_links:
    - from: ".planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md"
      to: ".planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md"
      via: "closing pointer at the end of the Resolution section, naming the successor by filename"
      pattern: "2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc"
    - from: ".planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md"
      to: ".planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md"
      via: "`found_by` citing quick 260925-o9b and the todo it closed"
      pattern: "260925-o9b"
---

<objective>
Close the 2026-09-24 report-upstream todo as obsolete, and open a successor todo that
puts the real remaining question in front of a human.

The todo asked for a defect to be reported to `get-shit-done-cc`. Research on 2026-09-25
established there is nowhere to report it: the named repo was archived read-only on
2026-06-26, the npm package is deprecated and frozen at the version GameLib runs, and
development moved to a different package (`open-gsd/gsd-core`) that has ALREADY fixed
three of this todo's four suggestions. The action item is dead; the underlying exposure
-- GameLib pinned to a line that can never receive a fix -- is not, and becomes its own
decision todo.

Purpose: leave a reader who finds the closed todo in six months able to reconstruct why
nothing was filed without re-doing the research, and leave the live consequence tracked
rather than dissolved into the closure.

Output: one file moved into `completed/` with a Resolution section appended, one new
pending todo. Nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/todos/pending/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md

Two CLAUDE.md conventions govern this plan directly: "Todo triage frontmatter (enforced
by CI)" (Task 2's frontmatter) and "A formatter check belongs in every task's `<verify>`"
(both tasks).

Measured facts about this repo's gates, established 2026-09-25 during planning. Treat
them as known; do not re-derive:

- `.prettierignore` contains `.planning`. A `npx prettier --check .planning/...`
  invocation therefore prints "All matched files use Prettier code style!" and exits 0
  WITHOUT inspecting the file -- confirmed by checking a deliberately misformatted probe
  file placed at `.planning/__prettier_probe.md`, which passed. The check is mandated by
  CLAUDE.md and is run below, but on these paths it is a no-op. Do NOT try to make it
  bite by passing `--ignore-path .gitignore`: that was tried during planning and it fails
  on the EXISTING, correct todo corpus (both todos sampled came back as "code style
  issues"), so forcing it would convict files that are right by repo convention. Match
  the surrounding todo files' formatting by hand instead; that is the actual mechanism
  here.
- `pnpm planning-gates` runs `python3 meta/runPlanningGates.py`. `python3` does NOT
  resolve in this environment's shell (it hits the Microsoft Store shim and prints
  "Python was not found"). `py meta/runPlanningGates.py` works and was measured GREEN at
  13/13 before any edit in this plan. If `pnpm planning-gates` fails on the interpreter
  rather than on a gate, fall back to `py` -- and say so in the SUMMARY rather than
  recording a gate run that did not happen.
- `.planning/todos/todo-frontmatter-gate.py` parses ONLY the block between the first two
  `---` lines, matches values bare and case-sensitively, and scopes to `pending/` only.
  `completed/` is deliberately exempt -- Task 1 must not "fix" anything there.
- `.planning/planning-envelope-tag-gate.py` walks every git-tracked `.md` under
  `.planning/`, including both files written here, and fails on orphan (unpaired)
  angle-bracket envelope tags. Neither file needs any such tag; do not introduce one.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the report-upstream todo to completed/ and append its obsolescence rationale</name>
  <files>.planning/todos/pending/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md -> .planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md</files>
  <action>
Move the file with `git mv` so the rename is tracked:
`git mv .planning/todos/pending/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md .planning/todos/completed/`
The filename does not change. Do not copy-then-delete.

Then APPEND one section to the end of the moved file. Append only: do not edit, reflow,
correct or re-word a single line of the existing body, and do not touch the frontmatter
-- `severity: minor`, `platform: any`, `ready: human` and `found_by` all stay exactly as
they are. (`completed/` is exempt from the frontmatter gate, but the keys are history and
stay.)

The heading, verbatim:
`## Resolution (2026-09-25): closed as obsolete — no reportable upstream exists`
(that is an em dash, U+2014, matching the task brief -- not `--`).

The section must record the following facts. All were verified live on 2026-09-25 in the
parent session. DO NOT re-verify them over the network, do not open a browser, do not run
`npm view`, do not call the GitHub API -- transcribe them accurately, and attribute them
as verified on 2026-09-25. Organise them under short sub-headings of your choosing; the
order below is a sensible one.

**There is nowhere to file.** `gsd-build/get-shit-done` -- the repo named in this todo's
own `bugs` URL -- was archived read-only on 2026-06-26. Its issue tracker shows 0 open
issues; no report can be filed there at all. Its README states development relocated to
"GSD Core in the Open GSD repository".

**The version GameLib runs is frozen.** `get-shit-done-cc` on npm is deprecated, with the
deprecation message "Package no longer supported". `dist-tags.latest` is still 1.42.3,
published 2026-05-16; the registry's `time.modified` is 2026-05-23. The consequence is
the load-bearing one: the defect this todo describes can never be fixed in the version
GameLib runs, no matter who reports it.

**Development continues elsewhere, as a restructured tree.** The active successor is
`open-gsd/gsd-core` -- `pushed_at` 2026-09-25, 163 open issues, default branch `next`,
homepage opengsd.net. The SDK sources this todo cites by path have MOVED:
`sdk/src/query/state-document.ts` is now `src/state-document.cts`, and
`sdk/src/query/state-mutation.ts` is now `src/state.cts`. Every file:line citation in the
body above therefore addresses a tree that no longer exists in that shape.

**Three of this todo's four suggested fixes are already fixed upstream in gsd-core.**
Suggestion 3 (require the bold form to be line-start) is FIXED at
`src/state-document.cts:572` as `^([ \t]*\*\*Field:\*\*[ \t]*)(.*)$`, upstream issue
#4243 -- and note that its in-code comment cites this todo's exact failure mode, a bold
field label quoted mid-sentence capturing the rewrite and destroying the rest of its
line. Suggestion 4 (the `### counts` lookahead trap) is FIXED: the `(?=\n##|$)` lookahead
was replaced by `collectSection(..., { levelBounded: true })` / `tokenizeHeadings` per
ADR-1372 T6. The frontmatter-shadowing half is FIXED body-only per upstream #1255.
Already-closed upstream issues covering this same defect class: #4481 (extract side,
bold-anywhere), #4823 (plain branch, whole-body and case-insensitive), #4469, #4419.

**Suggestions 1-2 remain PARTLY open upstream.** Section-scoping helpers exist --
`stateCurrentPositionSlice` (upstream #2956) and `stateReplaceFieldInSession` (#2444) --
and some callers scope correctly, but `record-session` (`src/state.cts:1991-2023`) and
the phase-complete path (`src/state.cts:6674-6683`) still pass whole-document content.
The in-code comment at `src/state.cts:1975` openly acknowledges that `stateReplaceField`
"replaces the FIRST case-insensitive label match anywhere in the document". This residual
is tracked upstream by open epic #4629, "STATE.md writes declare intent and are
verified".

**Deliberately NOT re-reported upstream, and here is why.** State this plainly, as a
decision with reasons, not as an absence: gsd-core's maintainers already track the
residual in #4629 and document it in their own source comments; GameLib does not run
gsd-core; and the package GameLib DOES run can never receive the fix. A new issue would
duplicate closed work on a codebase this project is not a user of.

**GameLib's protection is unaffected.** The existing local workaround stands -- the
restructured `.planning/STATE.md` plus `.planning/state-sdk-field-anchor-gate.py` (the
13th planning gate) -- and this closure changes nothing about it.

**Operational note.** The `gh` CLI is not installed on this machine (absent from both the
Bash and the PowerShell PATH), so no upstream issue could have been filed from here
regardless.

End the section with a pointer to the successor: the live exposure that outlives this
todo -- GameLib pinned to a deprecated, frozen SDK line -- is now tracked by
`.planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md`
(Task 2). Name it by filename so a reader can find it.
  </action>
  <verify>
    <automated>
git -C . status --porcelain | grep -E '^R.*2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream\.md' &&
test ! -e .planning/todos/pending/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md &&
grep -q '^## Resolution (2026-09-25): closed as obsolete — no reportable upstream exists$' .planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md &&
for tok in '2026-06-26' '1.42.3' '2026-05-16' 'open-gsd/gsd-core' 'state-document.cts' '#4243' 'ADR-1372' '#4629' '#1255' 'state-sdk-field-anchor-gate.py' '2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc'; do grep -qF "$tok" .planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md || { echo "MISSING FACT: $tok"; exit 1; }; done &&
grep -qE '^severity: minor$' .planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md &&
grep -qE '^ready: human$' .planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md &&
git add .planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md &&
git diff --cached -M --diff-filter=R -- '.planning/todos/*2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md' | grep -c '^-[^-]' | grep -qx 0 &&
npx prettier --check .planning/todos/completed/2026-09-24-report-gsd-sdk-unanchored-state-field-replace-upstream.md
    </automated>
  </verify>
  <done>
The file is tracked as a rename into `completed/`, absent from `pending/`, carries the
exact Resolution heading, contains every verified fact token above, kept its
`severity`/`ready` keys, and the staged rename diff removes ZERO existing body lines
(append-only proven, not asserted). `npx prettier --check` run on the exact written path
per CLAUDE.md -- and recorded in the SUMMARY as the no-op it is on a `.prettierignore`d
path, never as evidence the file is formatted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the successor decision todo in pending/</name>
  <files>.planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md</files>
  <action>
Create the file at exactly that path (the directory's `YYYY-MM-DD-<kebab-slug>.md`
convention, dated 2026-09-25).

Frontmatter, in this key order -- the three triage keys MUST be adjacent, bare, lowercase
and unquoted, per CLAUDE.md's enforced convention:

`created` as an ISO 8601 timestamp for 2026-09-25 (match the sibling todos' shape, e.g.
`2026-09-25T00:00:00+13:00`); `title` (double-quoted) set to
`Decide whether to migrate off deprecated get-shit-done-cc 1.42.3 to open-gsd/gsd-core`;
`area: tooling`; then `severity: medium`, `platform: any`, `ready: human`; then `found_by`
(double-quoted) citing research on 2026-09-25 during quick task 260925-o9b, while closing
the 2026-09-24 report-upstream todo -- name that todo's file by path so the chain is
followable; then `files: []`.

The H1 restates the title as a question. The body must state, with no recommendation:

**What GameLib pins.** `get-shit-done-cc` 1.42.3. CLAUDE.md records this pin itself, in
its UAT-shape convention section, describing `get-shit-done-cc` as "pinned `v1.42.3`";
the SDK is installed at `~/AppData/Roaming/npm/node_modules/get-shit-done-cc`.

**Why that pin is now a dead end.** The package is deprecated on npm and its GitHub repo
was archived 2026-06-26. The consequence is what matters and should be stated as such: no
gsd tooling defect GameLib hits can EVER be fixed upstream in that line. Every one of
them is permanently GameLib's to work around locally -- which has already happened once,
in the STATE.md field-anchor defect and the 13th planning gate written to contain it
(`.planning/state-sdk-field-anchor-gate.py`). The cost of staying is not hypothetical; it
is a local gate per upstream defect, forever.

**What the successor is.** `open-gsd/gsd-core` -- a DIFFERENT package, not a version
bump, with a restructured tree (`sdk/src/query/*.ts` became `src/*.cts`). It has already
fixed several STATE.md defects GameLib worked around locally: #4243, #1255, #4481, #4823,
and ADR-1372 T6.

**The decision, and its blast radius.** Three options, stated neutrally and with no
preferred answer: migrate to gsd-core; fork-and-pin the frozen line and carry local
patches; or stay frozen deliberately and keep absorbing defects locally. This is a human
call, not a code change. Blast radius covers every `/gsd-*` workflow, all 13 planning
gates, the shapes of the documents under `.planning/`, and the upstream templates that
CLAUDE.md already flags as unversioned, machine-shared and overwritten by a `gsd` upgrade
(the UAT template and the phase-prompt template).

Frame the whole file as a decision to be made, not a migration to be executed. Do not
prejudge which option is right, do not estimate migration effort, and do not sketch
migration steps -- `ready: human` is the honest state and the body must read that way.

Formatting: match the surrounding pending todos (same wrap width and heading style as
`.planning/todos/pending/2026-09-25-vite-watcher-still-walks-graphify-out-so-graphify-update-can.md`).
Introduce no angle-bracket envelope tags.
  </action>
  <verify>
    <automated>
test -f .planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md &&
awk '/^---$/{n++; next} n==1' .planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md | grep -E '^(area|severity|platform|ready):' | tr '\n' '|' | grep -qx 'area: tooling|severity: medium|platform: any|ready: human|' &&
for tok in '1.42.3' 'open-gsd/gsd-core' '2026-06-26' '260925-o9b' 'state-sdk-field-anchor-gate.py' '#4243' 'ADR-1372'; do grep -qF "$tok" .planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md || { echo "MISSING: $tok"; exit 1; }; done &&
(pnpm planning-gates || py meta/runPlanningGates.py) &&
npx prettier --check .planning/todos/pending/2026-09-25-decide-whether-to-migrate-off-deprecated-get-shit-done-cc.md
    </automated>
  </verify>
  <done>
The successor todo exists with `area`/`severity`/`platform`/`ready` adjacent and in that
exact order with the exact bare values (proven by the pipe-joined exact match, which
fails on a reorder, a quoted value or an interleaved key -- not by four independent
greps). All required facts present. Planning gates report 13/13 with the todo-frontmatter
gate PASSing over the enlarged `pending/` corpus. `npx prettier --check` run on the exact
written path per CLAUDE.md.
  </done>
</task>

</tasks>

<verification>
Whole-task checks, after both tasks:

1. `git status --porcelain` shows exactly: one `R` rename into `completed/`, and one `??`
   (or staged `A`) for the new pending todo. Nothing under `src/`, `meta/`, `src-tauri/`
   or any other tree. If anything else appears, stop and explain before committing.
2. `pnpm planning-gates` (or the `py meta/runPlanningGates.py` fallback) reports
   13/13 PASS. The baseline was 13/13 GREEN before this plan, so any failure here is
   caused by this plan and must be fixed in the todo, never by widening a gate's
   vocabulary -- `.planning/todos/todo-frontmatter-gate.py`'s own docstring says so
   explicitly.
3. `npx prettier --check` over both written paths, scoped to those explicit paths and
   never a bare `.`, per CLAUDE.md. Record in the SUMMARY that `.planning` sits in
   `.prettierignore`, so this exits 0 without reading either file -- measured during
   planning with a deliberately misformatted probe. Reporting it as a passed format gate
   would be the green-check-proving-nothing pattern this repo keeps stamping out.
4. `grep -rn 'report-gsd-sdk-unanchored' .planning --include='*.md' -l` -- any OTHER
   planning document that referenced the todo at its old `pending/` path now points at a
   file that moved. Report what it finds in the SUMMARY; repoint only live pointers, and
   leave historical records (SUMMARYs, completed todos) verbatim.
</verification>

<success_criteria>
- The report-upstream todo lives in `completed/` under its original filename, moved with
  `git mv` and tracked as a rename.
- Its body is unchanged above the appended `## Resolution (2026-09-25): ...` section, and
  its `severity`/`platform`/`ready` keys are intact.
- The Resolution section records every verified 2026-09-25 fact, states the NOT-reporting
  decision as a reasoned choice rather than an omission, and names the successor todo.
- No network verification was performed; the facts were transcribed from the brief.
- The successor todo exists in `pending/`, passes the frontmatter gate, and frames
  migrate / fork-and-pin / stay-frozen as an open human decision with no recommendation.
- 13/13 planning gates pass. No source code touched.
</success_criteria>

<output>
Create `.planning/quick/260925-o9b-close-obsolete-report-upstream-todo-and-/260925-o9b-SUMMARY.md` when done.

Record in it: the measured gate result and which interpreter produced it (`pnpm
planning-gates` or the `py` fallback); the explicit statement that `npx prettier --check`
on `.planning` paths is a no-op because `.planning` is `.prettierignore`d, so formatting
here rests on hand-matching the surrounding corpus; whatever check 4 found and what was
repointed versus deliberately left frozen; and the fact that zero network verification
was done -- the upstream facts are transcribed from research performed in the parent
session on 2026-09-25, not re-measured here.
</output>
