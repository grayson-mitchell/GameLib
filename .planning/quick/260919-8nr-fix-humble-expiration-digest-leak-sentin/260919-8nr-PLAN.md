---
phase: quick-260919-8nr
plan: 01
subsystem: planning/todos
status: complete
---

# Quick Task 260919-8nr: Action the Humble expiration digest leak-sentinel todo

## Original request

Action `.planning/todos/pending/2026-09-15-humble-expiration-digest-leak-sentinel-collides-with-the-date.md`
— replace the bare `"7"` leak sentinel with a distinctive token and freeze the fixture date.

## The premise was void before any code was written

The requested fix **was already shipped four days earlier**, by quick task `260915-g9p`
(commit `f9044be35`, 2026-09-15). The test file already carries `REVEALED_SENTINEL`,
`KEYINDEX_SENTINEL`, `FROZEN_EXPIRATION`, and an exact-equality structural body assertion.
The suite is green at 15/15.

The pending file was a **duplicate capture of an already-closed todo** — the same todo also
existed in `completed/`, and had done since `6652c5519`.

So the plan changed shape: from "fix the test" to "prove the fix is real, then retire the
stale duplicate without discarding anything".

## Tasks

### Task 1 — Prove the shipped fix is real, not a self-heal

The todo's own core warning is that this test **self-heals to green**. A green run therefore
proves nothing on its own, and the prior task's SUMMARY claiming a negative control was run is
a document, not a measurement. Re-run the negative control independently.

- **action**: Inject a real leak into `buildDigestCopy` (`src/backend/humble/expirationAlerts.ts`),
  one secret field per arm, and observe the test go RED at its own named assertion. Revert via
  the Edit tool — never `git checkout --`, which fires this repo's post-checkout hook.
- **verify**: Each arm fails at the assertion naming *its own* sentinel; `git diff --exit-code`
  on the source returns clean afterward; full suite returns to 15/15.
- **done**: Both sentinels observed failing for the right reason, source byte-identical to HEAD.

### Task 2 — Confirm the duplicate carries nothing unique

A todo must never be closed on a false title while untested siblings hide in its body.

- **action**: `diff` the pending copy against the completed copy. Confirm the residual concern
  (date correctness west of UTC) is not orphaned.
- **verify**: The only differences are the resolution frontmatter and the added `Correction`
  section — the completed copy is a strict superset. The residual concern has its own live
  pending todo.
- **done**: Deleting the pending copy provably loses zero information.

### Task 3 — Retire the duplicate and record why it existed

- **action**: `git rm` the pending copy. Append a `Duplicate capture` note to the completed copy
  recording the resurrection mechanism, so the next reader who follows the filename breadcrumb
  understands what happened rather than re-filing it a third time.
- **verify**: Zero pending/completed duplicate pairs remain corpus-wide; `pnpm planning-gates`
  stays 11/11.
- **done**: One copy of the todo, in `completed/`, carrying the full history.

## Explicitly NOT done

- `src/backend/humble/expirationAlerts.ts` is **not** modified. It ends byte-identical to HEAD.
- `src/backend/humble/__tests__/expirationAlerts.test.ts` is **not** modified. The shipped fix
  is correct as-is and needed nothing.
- The sibling todo `2026-09-15-humble-expiry-dates-may-render-one-day-early-west-of-utc.md`
  stays OPEN and untouched. It is a genuine, distinct, unsettled concern requiring a live
  authenticated Humble sync.
